
### Medical explainable AI
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-09-24**|**Does a model's stated reason for rejecting a candidate do any work?**|Archit Rastogi et.al.|[2609.30151v1](http://arxiv.org/abs/2609.30151v1)|null|
|**2026-09-24**|**NNV3: Expanding Neural Network Verification to New Architectures and Domains**|Anne M. Tumlin et.al.|[2609.30050v1](http://arxiv.org/abs/2609.30050v1)|null|
|**2026-09-24**|**Style, Not Self: Surface Cues Explain Zero-Shot Code Attribution by Large Language Models**|Ehsan Barkhordar et.al.|[2609.30048v1](http://arxiv.org/abs/2609.30048v1)|null|
|**2026-09-24**|**Synthetic Hospital: An Open, Verifiable, Physician-Validated Longitudinal EHR Benchmark**|Christine Park et.al.|[2609.30027v1](http://arxiv.org/abs/2609.30027v1)|null|
|**2026-09-24**|**Neuro-symbolic AI for Industrial Configuration**|Danilo Valerio et.al.|[2609.29947v1](http://arxiv.org/abs/2609.29947v1)|null|
|**2026-09-24**|**Qwen-Planner-Agent: A Closed-Loop AI-for-AI Framework for Real-World Mobile Planner Agents**|Tingyu Qu et.al.|[2609.29892v1](http://arxiv.org/abs/2609.29892v1)|null|
|**2026-09-24**|**AI-based detection of worsening heart failure from low-resolution telemonitoring data**|Erik Aerts et.al.|[2609.29742v1](http://arxiv.org/abs/2609.29742v1)|null|
|**2026-09-24**|**Detecting Glaucoma Across Multi-ethnic Myopic and Non-Myopic Populations Using an Uncertainty-Aware Vision Transformer: A Multicentre Model Development and Validation Study**|Raghavan Lavanya et.al.|[2609.29433v1](http://arxiv.org/abs/2609.29433v1)|null|
|**2026-09-24**|**SkinAgent AI: A Safety-Grounded Multimodal Agentic Framework for Non-Diagnostic Skincare Support**|Muhammad Muhtasim Shahriar et.al.|[2609.29341v1](http://arxiv.org/abs/2609.29341v1)|null|
|**2026-09-24**|**Reasoning Instructions Can Break Answer Decoding in Vision--Language Models**|Zeyan Li et.al.|[2609.29278v1](http://arxiv.org/abs/2609.29278v1)|null|
|**2026-09-24**|**Where Does Exactly-Once Live? Model, Harness, and Tool-Contract Effects on Duplicate Side Effects in LLM Agents**|Jiapeng Li et.al.|[2609.29095v1](http://arxiv.org/abs/2609.29095v1)|null|
|**2026-09-24**|**A Rapid Pipeline for Training and Deploying ML Models on WeBe Band**|Ehsan Kourkchi et.al.|[2609.29084v1](http://arxiv.org/abs/2609.29084v1)|null|
|**2026-09-24**|**When Does Action Credit Need Updating?**|Hongye Yang et.al.|[2609.29007v1](http://arxiv.org/abs/2609.29007v1)|null|
|**2026-09-24**|**Beneath the Scores: Rethinking Hallucination Evaluation for Video Understanding Models**|Shuzhi Gong et.al.|[2609.28991v1](http://arxiv.org/abs/2609.28991v1)|null|
|**2026-09-23**|**An Explainable DistilBERT-BiLSTM-Attention Framework for Binary and Multi-Class Hate Speech Detection**|Rameesha Zia et.al.|[2609.28703v1](http://arxiv.org/abs/2609.28703v1)|null|
|**2026-09-23**|**LAYERSCOPE: A Layerwise Characterization of Video and Multimodal Learned Representations**|Sandra Arcos-Holzinger et.al.|[2609.28086v2](http://arxiv.org/abs/2609.28086v2)|null|
|**2026-09-23**|**Auditability Is Not One Property: Rule Overlap, Behavioural Agreement, and Composition in Reinforcement Learning**|Liu Hung Ming et.al.|[2609.28581v1](http://arxiv.org/abs/2609.28581v1)|null|
|**2026-09-23**|**When Explanations Cannot Be Read: Measuring and Correcting SHAP and LIME Rendering for Right-to-Left Languages**|Rameesha Zia et.al.|[2609.28565v1](http://arxiv.org/abs/2609.28565v1)|null|
|**2026-09-23**|**Can Jev Judge Radiology Reports? Evaluating a System One Model for Clinical Factuality**|Jiaju Huang et.al.|[2609.27607v1](http://arxiv.org/abs/2609.27607v1)|null|
|**2026-09-23**|**NV-Reason-CT: 3D Visual Language Model for CT Analysis**|Andriy Myronenko et.al.|[2609.27511v1](http://arxiv.org/abs/2609.27511v1)|null|
|**2026-09-23**|**Memory Control Signals Emerge Before Action in Long Horizon Agents**|Mingxuan Wang et.al.|[2609.27286v1](http://arxiv.org/abs/2609.27286v1)|null|
|**2026-09-22**|**Local Evidence and Geometric Readout Repair in Trained GNNs**|Nadi Tomeh et.al.|[2609.27092v1](http://arxiv.org/abs/2609.27092v1)|null|
|**2026-09-22**|**PACT: From Credit Assignment to Critic Alignment**|Jiayan Fu et.al.|[2609.26355v1](http://arxiv.org/abs/2609.26355v1)|null|
|**2026-09-22**|**xWhyL: Causal Interactive Learning**|Nicholas Tagliapietra et.al.|[2609.26037v1](http://arxiv.org/abs/2609.26037v1)|null|
|**2026-09-22**|**Compiling Sufficient Governance Context from Declared Losses and Reachable States: Exact Observation-Contract Synthesis with Cardinality and Cost Objectives**|Gaston Besanson et.al.|[2609.26016v1](http://arxiv.org/abs/2609.26016v1)|null|
|**2026-09-22**|**Skytopia: Monocular Drone Navigation with Action-Conditioned Latent World Models**|Yuhang Zhang et.al.|[2609.26007v1](http://arxiv.org/abs/2609.26007v1)|null|
|**2026-09-22**|**CausalLoss-Fin: Attributing Financial-Agent Loss to Decisions and Infrastructure Faults**|Abhishek Sharma et.al.|[2609.25960v1](http://arxiv.org/abs/2609.25960v1)|null|
|**2026-09-22**|**Prediction Is Not Detection: Evaluating Pre-Recognition Claims in Longitudinal Clinical AI**|Jing Yang et.al.|[2609.25852v1](http://arxiv.org/abs/2609.25852v1)|null|
|**2026-09-22**|**MorphoSHAP: Rethinking the Unit of Attribution in Explanation for Deep Visual Models**|Anirudh Prabhakaran et.al.|[2609.25815v1](http://arxiv.org/abs/2609.25815v1)|null|
|**2026-09-22**|**LingLan: An Advancing Traditional Chinese Medicine Diagnosis LLM with Multimodal Data**|Zheng Chen et.al.|[2609.25715v1](http://arxiv.org/abs/2609.25715v1)|null|
|**2026-09-22**|**Interpretable AI plus Handheld, Portable Retinal Photographs: A Low-Cost Glaucoma Screening Solution for West Africa**|Charis Y. N. Chiang et.al.|[2609.25697v1](http://arxiv.org/abs/2609.25697v1)|null|
|**2026-09-21**|**ShowTellArena: Evaluating Business Workflow Understanding from Demonstrations**|David Garg et.al.|[2609.25467v1](http://arxiv.org/abs/2609.25467v1)|null|
|**2026-09-21**|**Passes Alone, Fails Together: Benchmarking Semantic Coordination in Parallel LLM-Agent Development**|Haocheng Xia et.al.|[2609.25396v1](http://arxiv.org/abs/2609.25396v1)|null|
|**2026-09-21**|**From Decorative to Load-Bearing: Task Difficulty Shapes the Causal Role of Chain-of-Thought**|Renee Jia et.al.|[2609.25366v1](http://arxiv.org/abs/2609.25366v1)|null|
|**2026-09-21**|**Exactness at Inference: A Representational Criterion for Out-of-Distribution Generalization**|Filipe Marinho Rocha et.al.|[2609.24942v1](http://arxiv.org/abs/2609.24942v1)|null|
|**2026-09-21**|**Et Tu, Brute? Economic Misalignment in Personal AI Agents**|Aman Priyanshu et.al.|[2609.24927v1](http://arxiv.org/abs/2609.24927v1)|null|
|**2026-09-21**|**From Pattern Recognizers to Personalized Companions: A Survey of Large Language Models in Mental Health**|He Hu et.al.|[2609.25186v1](http://arxiv.org/abs/2609.25186v1)|null|
|**2026-09-21**|**Decoding Guardrails: XAI-Guided Perturbation Analysis of Prompt Injection Detection**|Fernando Outeda et.al.|[2609.24801v1](http://arxiv.org/abs/2609.24801v1)|null|
|**2026-09-21**|**DeceptionAnalyser: A Web-Based AI Tool for Performing Structured Deception Analysis with Argumentation Schemes and LLMs**|Stefan Sarkadi et.al.|[2609.24369v1](http://arxiv.org/abs/2609.24369v1)|null|
|**2026-09-21**|**Temporal Generalization and Explanation Stability of Control Flow Graph Neural Networks for Malware Detection**|Md. Asif Sajeed et.al.|[2609.24280v1](http://arxiv.org/abs/2609.24280v1)|null|
|**2026-09-21**|**ACLArena: Agent Continue Learning in Multi-stage Post-training**|Haixin Wang et.al.|[2609.23989v1](http://arxiv.org/abs/2609.23989v1)|null|
|**2026-09-21**|**UniK: Universal Knowledge Perception for Digital and Physical AI**|Nirmit Desai et.al.|[2609.23971v1](http://arxiv.org/abs/2609.23971v1)|null|
|**2026-09-20**|**Explainable Recommendations at Scale: LLM Rationales for YouTube Music Artist Discovery**|Xiao Liu et.al.|[2609.23877v1](http://arxiv.org/abs/2609.23877v1)|null|
|**2026-09-20**|**What Makes a Terminal-Bench Task Hard? Separating Genuine Hardness from Fake-Hardness on an Adjudicated Agentic Corpus**|Edward Lue Chee Lip et.al.|[2609.26826v1](http://arxiv.org/abs/2609.26826v1)|null|
|**2026-09-20**|**Predicting Out-of-Distribution Generalization of Neural Operators via Observable Spectral Error Decomposition**|Hang-Cheng Dong et.al.|[2609.23529v1](http://arxiv.org/abs/2609.23529v1)|null|
|**2026-09-20**|**Signal2Symbol: Neuro-Symbolic Temporal Reasoning for Explainable Physiological Time-Series Anomaly Detection**|Naser Mansour et.al.|[2609.26820v1](http://arxiv.org/abs/2609.26820v1)|null|
|**2026-09-20**|**TicTacBench: Benchmarking Timing Closure Capabilities of Coding Agents**|Bowei Wang et.al.|[2609.23363v1](http://arxiv.org/abs/2609.23363v1)|null|
|**2026-09-20**|**Why Ghost Outputs Teach: A Kernel-Based Understanding of Subliminal Learning**|Zhe Li et.al.|[2609.23260v1](http://arxiv.org/abs/2609.23260v1)|null|
|**2026-09-19**|**Triggers and Diagnostics for LLM-Based Interpretability Failures in Active Inference Agents**|Param Raval et.al.|[2609.23215v1](http://arxiv.org/abs/2609.23215v1)|null|
|**2026-09-19**|**Whitening Inverts the Hierarchy: What the Norm of a Whitened Embedding Measures**|Mohammed Ahnouch et.al.|[2609.23117v1](http://arxiv.org/abs/2609.23117v1)|null|
|**2026-09-19**|**FireWorldBench: Benchmarking Complex Physical World Intelligence through Coupled-Field Fire Dynamics**|Qiang Chen et.al.|[2609.23064v1](http://arxiv.org/abs/2609.23064v1)|null|
|**2026-09-19**|**An Accurate and Interpretable Hyper Graph Neural Network for GBM Survival Prediction**|Mushahid Intesum et.al.|[2609.25088v1](http://arxiv.org/abs/2609.25088v1)|null|
|**2026-09-19**|**From Research Frontier to Laboratory Bench: Design of a Four-Tier Experimental Teaching System for Multimodal Medical Image Intelligent Diagnosis**|Dongjing Shan et.al.|[2609.22790v1](http://arxiv.org/abs/2609.22790v1)|null|
|**2026-09-19**|**MATE: Policy-Aware Security Auditing for Mobile Agents via Synthesis-Driven Trajectory Learning**|Changyue Jiang et.al.|[2609.22724v1](http://arxiv.org/abs/2609.22724v1)|null|
|**2026-09-18**|**GaitVista: Reliability-Aware AI Measurement toward Accessible Longitudinal Gait Assessment**|Nethmi Jayasinghe et.al.|[2609.22619v1](http://arxiv.org/abs/2609.22619v1)|null|
|**2026-09-18**|**Benchmarking the Explanatory Quality of Open-Weight Vision-Language Models in Face Recognition**|Laurent Colbois et.al.|[2609.21879v1](http://arxiv.org/abs/2609.21879v1)|null|
|**2026-09-18**|**An Agentic Just-in-Time Adaptive Intervention System for Personalized Sleep Support: Proof-of-Concept Study with N of 1 Data**|Nick Rezaee et.al.|[2609.21805v1](http://arxiv.org/abs/2609.21805v1)|null|
|**2026-09-18**|**Consistent Relexicalization of Clinical Documents using Graph-Based Approach**|Dipankar Das et.al.|[2609.21387v1](http://arxiv.org/abs/2609.21387v1)|null|
|**2026-09-18**|**Ability-Residual Decoupled Modeling for Affective Cognitive Diagnosis**|Boyuan Zhao et.al.|[2609.21214v1](http://arxiv.org/abs/2609.21214v1)|null|
|**2026-09-17**|**Clinician-Grounded Quality Assurance for AI-Assisted Psychiatric Intake**|King Shi et.al.|[2609.21149v1](http://arxiv.org/abs/2609.21149v1)|null|
|**2026-09-17**|**Trustworthy FinAInce: Unpacking How AI-Mediated Financial Advice is Judged**|Aryan Ramchandra Kapadia et.al.|[2609.20989v2](http://arxiv.org/abs/2609.20989v2)|null|
|**2026-09-17**|**ERCPMP-Gx: Endoscopic Image and Video Dataset for Morphological, Histopathological, and Genomic Characterization of Colorectal Polyposis**|Zahra Ghaffari et.al.|[2609.20815v1](http://arxiv.org/abs/2609.20815v1)|null|
|**2026-09-17**|**An Empirical Study of Harness Design for Coding Agents**|Run-Ze Fan et.al.|[2609.20804v1](http://arxiv.org/abs/2609.20804v1)|null|
|**2026-09-17**|**A Qualitative Model for Reasoning about Path and Support**|Abhishek Jaiswal et.al.|[2609.20349v1](http://arxiv.org/abs/2609.20349v1)|null|
|**2026-09-17**|**NeuSOGA3D: A Neuro-Symbolic Framework for Explainable 3D Geometric Reconstruction**|Qingde Li et.al.|[2609.20323v2](http://arxiv.org/abs/2609.20323v2)|null|
|**2026-09-17**|**Bridging Modalities on the Cortex: Surface-based MRI to PET Translation with a Diffusion Bridge**|Yitong Li et.al.|[2609.20147v1](http://arxiv.org/abs/2609.20147v1)|null|
|**2026-09-17**|**PAANI : On Device Visual Evidence Fusion and Explainable Guidance for River Robot Simulation**|Savio Cardoz et.al.|[2609.22353v1](http://arxiv.org/abs/2609.22353v1)|null|
|**2026-09-17**|**Reproducibility is not construct validity: LLM measurement of institutionally situated communication**|Veronika Batzdorfer et.al.|[2609.19866v1](http://arxiv.org/abs/2609.19866v1)|null|
|**2026-09-17**|**Large Language Model Agents for Evidence Based Genetic Disease Severity Classification**|Tohid Ghasemnejad et.al.|[2609.19569v2](http://arxiv.org/abs/2609.19569v2)|null|
|**2026-09-17**|**A Multi-Modal Generative Model for Tomato Disease Leaves Understanding**|Khang Nguyen Quoc et.al.|[2609.19555v1](http://arxiv.org/abs/2609.19555v1)|null|
|**2026-09-16**|**Predict Before You Deploy: Offline Prediction of Quantization-Induced Task Degradation for World Action Models**|Jiuyi Xu et.al.|[2609.19441v1](http://arxiv.org/abs/2609.19441v1)|null|
|**2026-09-16**|**Can Vision-Language Models Judge Olympic Diving? From Reasoning to Scores in Zero-Shot Action Quality Assessment**|Henry O. Velesaca et.al.|[2609.19354v1](http://arxiv.org/abs/2609.19354v1)|null|
|**2026-09-16**|**Prepared Or Unprepared? Evaluating Healthcare Workforce Readiness for Clinical Adoption of Artificial Intelligence in Nigeria**|Abbas M. Rabiu et.al.|[2609.19096v1](http://arxiv.org/abs/2609.19096v1)|null|
|**2026-09-16**|**Reporting Practice Matters: The Impact of Reference Choice on Chest X-ray Report Evaluation**|Daniel P. Jeong et.al.|[2609.19093v1](http://arxiv.org/abs/2609.19093v1)|null|
|**2026-09-16**|**Probabilistic Linear Explanations**|Frederic Koriche et.al.|[2609.19077v1](http://arxiv.org/abs/2609.19077v1)|null|
|**2026-09-16**|**Double descent is the principle of least action**|Congzhou M Sha et.al.|[2609.19076v2](http://arxiv.org/abs/2609.19076v2)|null|
|**2026-09-16**|**RLLBC-Lib: An Educational Code Library for Reinforcement Learning and Learning-Based Control**|Bernd Frauenknecht et.al.|[2609.19074v1](http://arxiv.org/abs/2609.19074v1)|null|
|**2026-09-16**|**Decodable but Misrouted: Sparse Features Uncover a Readout Gap in Vision-Language Models for Harmful Meme Detection**|Girish A. Koushik et.al.|[2609.18860v1](http://arxiv.org/abs/2609.18860v1)|null|
|**2026-09-16**|**Dimensionality reduction for AI based hyperspectral image classification based on XAI**|Vladimir Zeljković et.al.|[2609.22333v1](http://arxiv.org/abs/2609.22333v1)|null|
|**2026-09-16**|**Version- and Scope-Aware Question Answering over Normative Documents: A Deployed System and an End-to-End Evaluation at Production Scale**|Liuyin Wang et.al.|[2609.18769v1](http://arxiv.org/abs/2609.18769v1)|null|
|**2026-09-16**|**PAPC: Platform Mediation for Privacy-Propagation Externalities in AI-Mediated Workflows**|Tao Huang et.al.|[2609.19226v1](http://arxiv.org/abs/2609.19226v1)|null|
|**2026-09-16**|**Generalist-Specialist Mixture-of-Experts for Rare Pathology Detection in Multimodal Imaging**|Johannes Kaiser et.al.|[2609.18688v1](http://arxiv.org/abs/2609.18688v1)|null|
|**2026-09-16**|**PACT: Can Enterprise AI Assistants Be Trusted Under Pressure?**|Mika Okamoto et.al.|[2609.18605v1](http://arxiv.org/abs/2609.18605v1)|null|
|**2026-09-16**|**TRIPROBE: Probing Task Separability Beyond Classification for XAI**|Amirhossein Sadough et.al.|[2609.18525v1](http://arxiv.org/abs/2609.18525v1)|null|
|**2026-09-16**|**Visual Compliance via Executable Safety Rule Entailment**|Jisoo Kim et.al.|[2609.18328v1](http://arxiv.org/abs/2609.18328v1)|null|
|**2026-09-16**|**Knowledge-Graph Based Augmentation versus Retrieval Augmented Generation for Cultural-Related Question Answering**|Pablo Poulenard et.al.|[2609.18317v2](http://arxiv.org/abs/2609.18317v2)|null|
|**2026-09-16**|**A Study of the Reliability of Agentic AI-Generated Programs**|Ayesha Shafique et.al.|[2609.18298v2](http://arxiv.org/abs/2609.18298v2)|null|
|**2026-09-16**|**Building Trust in Artificial Intelligence: A Necessity for Railway Applications**|Lefebvre Renard Clément et.al.|[2609.18278v1](http://arxiv.org/abs/2609.18278v1)|null|
|**2026-09-16**|**A Lightweight CNN Integrated Compact Convolutional Transformer for Multi-Scale Feature Learning and reducing computational complexity for breast cancer mammography image detection and classification**|Md Taimur Ahad et.al.|[2609.18212v1](http://arxiv.org/abs/2609.18212v1)|null|
|**2026-09-16**|**Rethinking How We Evaluate Methodological Progress in Health AI**|Florent Pollet et.al.|[2609.18134v2](http://arxiv.org/abs/2609.18134v2)|null|
|**2026-09-16**|**Measuring AI Leadership: Development and Validation of a Multidimensional Measure for AI-Native Organizations**|Mustafa Akben et.al.|[2609.17965v1](http://arxiv.org/abs/2609.17965v1)|null|
|**2026-09-15**|**AI and Human Approaches to Mathematical Problem Solving**|Yang Ding et.al.|[2609.17779v1](http://arxiv.org/abs/2609.17779v1)|null|
|**2026-09-15**|**Information Set Emulation: Causal Certificates for AI Derived EHR Features**|Takes Fujita et.al.|[2609.17777v1](http://arxiv.org/abs/2609.17777v1)|null|
|**2026-09-15**|**Evolution of US Oral Political Language**|Jacques Savoy et.al.|[2609.17755v1](http://arxiv.org/abs/2609.17755v1)|null|
|**2026-09-15**|**Det-LIME: Detector-Aware, Multi-Instance Local Interpretable Model-Agnostic Explanations for Automated Marine Mammal Detection**|Jiayi Zhou et.al.|[2609.17479v1](http://arxiv.org/abs/2609.17479v1)|null|
|**2026-09-15**|**CareMirror: Bringing Caregiver Wellbeing into the Dementia Care Ecosystem**|Jiayue Melissa Shi et.al.|[2609.17434v1](http://arxiv.org/abs/2609.17434v1)|null|
|**2026-09-15**|**Finding Common Mistakes In Modelling With Mathematical Formalisms Using LLMs**|Lilian Killich et.al.|[2609.17111v1](http://arxiv.org/abs/2609.17111v1)|null|
|**2026-09-15**|**Sample-Conditioned Representation Selection for Audio Few-Shot Learning**|Fengrui Liu et.al.|[2609.17076v1](http://arxiv.org/abs/2609.17076v1)|null|
|**2026-09-15**|**When Confidence Signals Disagree: Local and Global Confidence in Autoregressive Language Models**|Julio C. Amador Diaz Lopez et.al.|[2609.16933v1](http://arxiv.org/abs/2609.16933v1)|null|
|**2026-09-15**|**Right Direction, Wrong Step: Geometric Analysis of Finite-Step Failure in Looped Transformers**|Zhihao Guo et.al.|[2609.16665v1](http://arxiv.org/abs/2609.16665v1)|null|

#### Abstracts
##### **Does a model's stated reason for rejecting a candidate do any work?**
2609.30151v1 by Archit Rastogi

Asked to choose between candidates and explain the choice, a language model often rejects a rival by naming a fact its profile lacks: no director, no date of death. That sentence is a claim about the text in front of the model, and it can be tested without any judge. We insert a real corpus sentence stating the named fact into the rival's profile and ask again under greedy decoding. Two controls separate content from placement: a length-matched irrelevant sentence at the same profile, and the same two sentences at a third option the model never mentioned. In the largest of three runs, six open models on 2WikiMultihopQA, supplying the named fact at the profile the model named moves its choice more than the irrelevant control does, odds ratio 3.57 [1.54, 8.26], Holm p=0.0210, and this survives dropping any single model. The contrast the design was built to detect, the same fact at the option nobody named, does not clear correction, Holm p=0.2428. The strongest result in the family carries no content claim at all: the identical irrelevant sentence moves the choice more at the named rival than at the third option, Holm p=0.0008. Repair and control also differ in co-candidate mentions, relation template and fluency; post-hoc matching on the first two preserves the content effects' direction, matching fluency weakens one, so the content contrasts bound an effect rather than establish one. A forced single-token probability read disagrees in direction with the free-text choice on that same contrast, and three candidate explanations for the disagreement find no support. Every measurement is a string rule, so each was validated against the records it reads; validation caught eight defects. The largest, a choice-parsing rule that returned the option a model had just rejected in 17.1% of adjudicable responses, would have reported six surviving contrasts instead of four.

摘要：被要求在候選人之間做選擇並解釋該選擇時，語言模型通常會通過指出其資料庫缺少的事實來拒絕對手：沒有導演，沒有死亡日期。那句話是對模型面前文本的主張，可以在沒有任何評判者的情況下進行測試。我們將一個聲明該事實的真實語料句子插入對手的資料庫中，並在貪婪解碼下再次詢問。兩個控制條件將內容與位置分開：一個與資料庫長度匹配的無關句子，和在模型從未提及的第三選項中的相同兩個句子。在三次運行中最大的那一次，六個開放模型在2WikiMultihopQA上，提供模型所提及的資料庫中的事實，使其選擇的變化超過了無關控制的變化，賠率比為3.57 [1.54, 8.26]，Holm p=0.0210，並且這在去掉任何單一模型後仍然成立。設計旨在檢測的對比，即在沒有人提到的選項中的相同事實，未能通過修正，Holm p=0.2428。該系列中最強的結果根本不帶有內容主張：相同的無關句子在提到的對手中比在第三選項中更能影響選擇，Holm p=0.0008。修復和控制在共同候選人提及、關係模板和流暢度上也有所不同；對前兩者的事後匹配保留了內容效應的方向，匹配流暢度削弱了一個，因此內容對比界定了一個效應，而不是建立一個。強制的單標記概率讀取在該對比的方向上與自由文本選擇不一致，對該不一致的三個候選解釋沒有找到支持。每個測量都是一條字符串規則，因此每個都經過其所讀取記錄的驗證；驗證發現了八個缺陷。最大的缺陷是一條選擇解析規則，在17.1%的可裁定回應中返回模型剛剛拒絕的選項，這本應報告六個存活的對比而不是四個。

##### **NNV3: Expanding Neural Network Verification to New Architectures and Domains**
2609.30050v1 by Anne M. Tumlin, Samuel Sasaki, Ben Wooding, Diego Manzanas Lopez, Muhammad Usama Zubair, Navid Hashemi, Hongchao Zhang, Waseem Abbas, Ipek Oguz, Meiyi Ma, Taylor T. Johnson

We present NNV3, the latest version of the Neural Network Verification (NNV) tool, a MATLAB framework for formal verification of deep learning models and learning-enabled cyber-physical systems. Building on the set-based reachability foundation of NNV 1.0 (FFNNs, CNNs, NNCS) and NNV 2.0 (RNNs, SSNNs, neural ODEs), NNV3 introduces new members of the Star-set family: ModelStar for verifying networks under weight perturbation, VolumeStar for video and 3D volumetric inputs, and GraphStar for graph neural networks. A conformal-inference-based probabilistic reachability mode complements sound analysis for problems where deterministic verification is intractable, while FairNNV certifies counterfactual and individual fairness properties over continuous input regions. NNV3 introduces new benchmarks for malware detection, graph-based power-system models, medical imaging, variable-length time series data, and action recognition. NNV3 also incorporates tutorials and developer guides through a unified documentation site. This paper details these major updates, demonstrating NNV's maturation into a comprehensive, robust, and accessible verification tool for a diverse range of AI systems.

摘要：我們介紹 NNV3，最新版本的神經網絡驗證 (NNV) 工具，這是一個用於深度學習模型和學習驅動的網絡物理系統的正式驗證的 MATLAB 框架。基於 NNV 1.0 (FFNNs, CNNs, NNCS) 和 NNV 2.0 (RNNs, SSNNs, neural ODEs) 的集合基礎可達性，NNV3 引入了 Star-set 家族的新成員：ModelStar 用於在權重擾動下驗證網絡，VolumeStar 用於視頻和 3D 體積輸入，GraphStar 用於圖神經網絡。一種基於符合推斷的概率可達性模式補充了在確定性驗證無法處理的問題上的可靠分析，而 FairNNV 則認證了在連續輸入區域上的反事實和個體公平性屬性。NNV3 引入了新的基準，用於惡意軟件檢測、基於圖的電力系統模型、醫學影像、可變長度時間序列數據和動作識別。NNV3 還通過統一的文檔網站整合了教程和開發者指南。本文詳細介紹了這些重大更新，展示了 NNV 成為一個全面、穩健且易於訪問的驗證工具的成熟，適用於各種 AI 系統。

##### **Style, Not Self: Surface Cues Explain Zero-Shot Code Attribution by Large Language Models**
2609.30048v1 by Ehsan Barkhordar, Surendrabikram Thapa

If a language model can recognize code it wrote, it may favor that code as a judge, and instances of one model monitoring each other could collude. We test this zero-shot on current commercial models. Five LLMs generate solutions to MBPP, HumanEval, and DS-1000, seven more to MBPP, and models act as evaluators in four tasks: picking their own solution from a pair, judging whether a single solution is their own, identifying which of two solutions a named model wrote, and judging quality blind. In the single-solution task, balanced accuracy is 49-58% for all 15 model-benchmark combinations, while raw accuracy (38-67%) mostly reflects how readily a model claims authorship. In the pairwise task, accuracy across 14 evaluator-opponent combinations correlates at r=0.93 with how often the evaluator's solution is longer. Attribution to a named model succeeds on some pairs and is consistently inverted on others. A rule-based normalization that strips docstrings, comments, type hints, and local names preserves Pass@1 and leaves ten of twelve re-tested results at chance; the other two follow a length difference it leaves, although a trained classifier still separates most normalized pairs. Claude Haiku's self-preference also disappears. We recommend reporting balanced accuracy, heuristic baselines, and label consistency.

摘要：如果一個語言模型能夠識別它所寫的代碼，它可能會偏好該代碼作為評判標準，並且一個模型監控另一個模型的情況可能會合謀。我們在當前的商業模型上進行這個零樣本測試。五個大型語言模型生成MBPP、HumanEval和DS-1000的解決方案，另外七個生成MBPP的解決方案，並且模型在四個任務中充當評估者：從一對中選擇自己的解決方案、判斷單一解決方案是否為自己的、識別兩個解決方案中哪一個是指定模型所寫的，以及盲判斷質量。在單一解決方案任務中，所有15個模型-基準組合的平衡準確率為49-58%，而原始準確率（38-67%）主要反映模型聲稱著作權的意願。在成對任務中，14個評估者-對手組合的準確率與評估者的解決方案較長的頻率之間的相關性為r=0.93。對指定模型的歸屬在某些對中成功，而在其他對中則始終顛倒。一種基於規則的正規化方法去除文檔字符串、註釋、類型提示和局部名稱，保留Pass@1，並使重新測試的十二個結果中有十個保持在隨機範圍內；另外兩個則遵循它留下的長度差異，儘管訓練過的分類器仍然能夠區分大多數正規化的對。Claude Haiku的自我偏好也消失了。我們建議報告平衡準確率、啟發式基準和標籤一致性。

##### **Synthetic Hospital: An Open, Verifiable, Physician-Validated Longitudinal EHR Benchmark**
2609.30027v1 by Christine Park, Valerie Chen, Tim Dettmers

Frontier language models are rarely used in clinical workflows because the realistic, longitudinal benchmarks needed to develop them are scarce. Real electronic health record (EHR) data cannot be openly shared due to privacy, ethics or data use issues and it does not contain verifiable ground truth since the chart records only reflect what clinicians documented. We introduce Synthetic Hospital, an open, fully synthetic, fact-grounded longitudinal EHR benchmark that resolves the open sharing and verifiable ground truth barriers. Built entirely from public medical-education material with no protected health information, it comprises 1,268 longitudinal patients and 5,602 encounters, where every diagnosis, finding, and temporal relation is grounded in standard ontologies (ICD-10-CM, SNOMED CT, LOINC) and with a complete provenance chain back to its source medical education material. Synthetic Hospital is served through a simulated hospital record system that mirrors real EHR infrastructure (standard interoperability APIs, role-based access and function-calling interface). In a blinded review, physicians distinguished its records from real patient charts at near-chance rates (53\%). Across 10 frontier and open models, none approaches ceiling: the best model reconstructs a patient's longitudinal problem list with a severity-weighted F1 of 0.73, level with the mean of seven physicians on a matched subset but well below the best of them (0.89), and misses roughly half of clinically relevant findings when summarizing a chart. Overall, these results highlight that Synthetic Hospital is a difficult and realistic test of clinical AI performance.

摘要：前沿語言模型在臨床工作流程中很少被使用，因為開發它們所需的現實長期基準非常稀缺。由於隱私、倫理或數據使用問題，真正的電子健康記錄（EHR）數據無法公開共享，並且它不包含可驗證的真實數據，因為病歷記錄僅反映臨床醫生所記錄的內容。我們介紹了Synthetic Hospital，一個開放的、完全合成的、基於事實的長期EHR基準，解決了公開共享和可驗證真實數據的障礙。它完全由公共醫學教育材料構建，沒有受保護的健康信息，包含1,268名長期患者和5,602次就診，每一個診斷、發現和時間關係都基於標準本體（ICD-10-CM、SNOMED CT、LOINC），並且有完整的來源鏈回到其源醫學教育材料。Synthetic Hospital通過一個模擬的醫院記錄系統提供服務，該系統模仿真實的EHR基礎設施（標準互操作性API、基於角色的訪問和功能調用接口）。在盲評中，醫生以接近隨機的比率（53%）區分其記錄與真實患者病歷。在10個前沿和開放模型中，沒有一個接近上限：最佳模型重建患者的長期問題清單，其嚴重性加權的F1為0.73，與七位醫生在匹配子集上的平均水平相當，但遠低於他們中最好的（0.89），並且在總結病歷時錯過了大約一半的臨床相關發現。總體而言，這些結果突顯了Synthetic Hospital是臨床AI性能的一個困難且現實的測試。

##### **Neuro-symbolic AI for Industrial Configuration**
2609.29947v1 by Danilo Valerio, Philipp Kogler, Stefan Bischof, Thomas Hubauer, Huzefa Rangwala

Large Language Models (LLMs) have shown impressive performance on a wide range of generative tasks. Yet their probabilistic nature makes them, in isolation, fundamentally unsuited for industrial product configuration, where outputs must be syntactically valid, semantically consistent with a knowledge base of hundreds of features and rules, and producible by an existing manufacturing chain. We argue that Neuro-symbolic (NeSy) AI methods lay out a promising path towards industrial-grade configurators that are reliable by design, explainable, and trustworthy. This paper describes a taxonomy of three NeSy integration strategies, namely hybrid inference, hybrid fine-tuning, and hybrid training, exploring their usage in the configuration domain. We report our effort to operationalize NeSy concepts in an industrial configuration copilot and derive a set of practical design choices for deploying trustworthy AI in engineering environments. We close with a discussion of open research challenges we consider most pressing, in particular how to scale NeSy methods from small academic demonstrators to the size of industrial configurators.

摘要：大型語言模型（LLMs）在各種生成任務上展現了令人印象深刻的表現。<lb/>然而，它們的概率性質使得它們在孤立情況下，根本不適合用於工業產品配置，因為輸出必須在語法上有效，與數百個特徵和規則的知識庫在語義上保持一致，並且必須能夠由現有的製造鏈生產。<lb/>我們認為神經符號（NeSy）人工智慧方法為工業級配置器鋪設了一條有前途的道路，這些配置器在設計上是可靠的、可解釋的和可信的。<lb/>本文描述了三種NeSy整合策略的分類法，即混合推理、混合微調和混合訓練，並探討它們在配置領域的應用。<lb/>我們報告了將NeSy概念實現於工業配置助手的努力，並推導出一系列在工程環境中部署可信人工智慧的實用設計選擇。<lb/>最後，我們討論了我們認為最迫切的開放研究挑戰，特別是如何將NeSy方法從小型學術示範擴展到工業配置器的規模。

##### **Qwen-Planner-Agent: A Closed-Loop AI-for-AI Framework for Real-World Mobile Planner Agents**
2609.29892v1 by Tingyu Qu, Weigao Sun, Yuecheng Liu, Yucheng Zhao, Yi Zhu, Yifeng Ding, Qiyi Wang, Sihan Cao, Pengkun Jiao, Hanlei Xie, Xiongwei Wu, Qichao Wang, Haodong Zhang, Jiajun Liu, Yuhao Wang, Yuqing Xie, Junpeng Zhao, Long Chen, Ming Ma, Sihan Yang, Ziwang Zhao, Yanhao Jia, Liangquan Gong, Feida Zhu, Yiran Zhong, Steven Hoi

The rapid progression of large language models is extending AI from passive content generation into the active workflows of engineering and scientific discovery. This shift raises a compelling question: can AI be both the object of development and an active participant in building next-generation AI systems? We explore this question by building Qwen-Planner-Agent within a closed-loop AI-for-AI framework for scalable development and iterative improvement. Mobile planning offers a demanding test of this approach: complex, long-horizon tasks challenge agent reliability, while costly real-device interaction limits development scalability. The framework connects data production, model training, and deployment through a shared action-feedback-verification contract. (i) AI for Data builds a human-gated agentic data flywheel in which specialized agents construct tasks, collect interaction trajectories, curate and balance training data, and use training feedback to guide subsequent data generation. (ii) AI for Training combines a supervised planning cold start with hybrid-environment online agentic reinforcement learning, where we introduce Competence-Aware Reward-and-Advantage Engineering (CARE) to reduce reasoning and tool-use costs while preserving task performance. (iii) AI drives model--harness co-evolution through an execution-evidence-driven loop that orchestrates memory, skills, and tools at runtime and feeds structured action feedback and preserved failure traces back into coordinated model and harness adaptation. Qwen-Planner-Agent achieves the best overall performance among all evaluated models and systems on MobilePA-Bench, improving over its base model across tool use, memory, skills, and sub-agent coordination. Further evaluations of our model show improvements across non-mobile agentic benchmarks while largely preserving general capabilities.

摘要：大型語言模型的快速進展正在將人工智慧從被動內容生成推向工程和科學發現的主動工作流程。這一轉變提出了一個引人注目的問題：人工智慧能否同時作為開發的對象和參與建設下一代人工智慧系統的主動參與者？我們通過在封閉循環的AI-for-AI框架中構建Qwen-Planner-Agent來探討這個問題，以實現可擴展的開發和迭代改進。移動規劃對這一方法提出了嚴峻的考驗：複雜的長期任務挑戰代理的可靠性，而昂貴的實體設備互動限制了開發的可擴展性。該框架通過共享的行動-反饋-驗證合同連接數據生成、模型訓練和部署。(i) AI for Data構建了一個人為閘控的代理數據飛輪，其中專門的代理構建任務、收集互動軌跡、策劃和均衡訓練數據，並利用訓練反饋來指導後續數據生成。(ii) AI for Training結合了監督規劃的冷啟動與混合環境的在線代理強化學習，我們引入了能力感知獎勵和優勢工程（CARE），以降低推理和工具使用成本，同時保持任務性能。(iii) AI通過一個執行證據驅動的循環推動模型-工具的共同演化，該循環在運行時協調記憶、技能和工具，並將結構化的行動反饋和保留的失敗痕跡反饋回協調的模型和工具適應中。Qwen-Planner-Agent在MobilePA-Bench上在所有評估的模型和系統中實現了最佳的整體性能，在工具使用、記憶、技能和子代理協調方面超越了其基礎模型。我們對模型的進一步評估顯示，在非移動代理基準上有改進，同時在很大程度上保持了通用能力。

##### **AI-based detection of worsening heart failure from low-resolution telemonitoring data**
2609.29742v1 by Erik Aerts, Yinan Yu, Annika Rosengren, Michael Fu, Martin Lindgren, Falk Dippel, Martin Adiels, Helen Sjöland

Objective: Heart failure (HF) presents a healthcare challenge due to its high comorbidity burden, aging patient population and frequent hospitalizations. Remote monitoring offers a promising approach to managing HF patients by early detection of health deterioration. Developing autonomous systems to detect signs of worsening in telemonitoring data is of interest to reduce the workload of healthcare personnel. Methods: We propose the TRACER model, a Transformer with Contrastive Event Representation, designed to predict timelines leading to rare hospitalization events in low-resolution and irregularly sampled telemonitoring data. TRACER incorporates time-aware embeddings for each biomarker, contrastive pre-training to enhance anomaly detection via representation learning, and independent binary classifiers for detection. We used measurement data containing remotely recorded biomarker sequences from 276 HF patients segmented into overlapping windows based on temporal rules, and labeled the windows based on the occurrence of HF relevant hospitalizations at the latter edge of the window. Results: TRACER was able to correctly predict 66.7% timelines leading up to HF hospitalizations in the highly imbalanced real-world dataset with an overestimation of 7.9%. Reformulating the training of TRACER as an event detection problem improved the predictive performance compared with training directly on forecasting windows, enabling more effective use of the limited hospitalization events. Conclusion: TRACER demonstrated superior performance in detecting signs of worsening status in real-world telemonitoring data compared to the other tested models. Significance: TRACER shows promise in identifying signs of clinical deterioration that allow for alerts to be generated to provide counteractive treatment in patients with HF.

摘要：目標：心臟衰竭（HF）因其高共病負擔、老年患者群體和頻繁住院而成為醫療保健挑戰。遠程監測提供了一種有前景的方法來管理HF患者，通過及早發現健康惡化的情況。開發自主系統以檢測遠程監測數據中惡化的跡象，對減輕醫療人員的工作負擔具有重要意義。方法：我們提出了TRACER模型，一種具有對比事件表示的Transformer，旨在預測低分辨率和不規則取樣的遠程監測數據中導致罕見住院事件的時間線。TRACER為每個生物標記整合了時間感知嵌入，通過表示學習增強異常檢測的對比預訓練，以及獨立的二元分類器進行檢測。我們使用了276名HF患者的測量數據，這些數據包含遠程記錄的生物標記序列，根據時間規則將其分段為重疊窗口，並根據窗口末端HF相關住院的發生情況對窗口進行標記。結果：TRACER能夠在高度不平衡的現實世界數據集中正確預測66.7%的時間線，導致HF住院，並且過高估計了7.9%。將TRACER的訓練重新定義為事件檢測問題，與直接在預測窗口上訓練相比，提高了預測性能，使有限的住院事件得以更有效利用。結論：與其他測試模型相比，TRACER在檢測現實世界遠程監測數據中惡化狀態的跡象方面顯示出卓越的性能。意義：TRACER在識別臨床惡化跡象方面顯示出潛力，這使得可以生成警報，以便對HF患者提供對應的治療。

##### **Detecting Glaucoma Across Multi-ethnic Myopic and Non-Myopic Populations Using an Uncertainty-Aware Vision Transformer: A Multicentre Model Development and Validation Study**
2609.29433v1 by Raghavan Lavanya, Yangqin Feng, Ten Cheer Quek, Quan V. Hoang, Linda Yi-Chieh Poon, Jost B. Jonas, Ya Xing Wang, Vinay Nangia, Jin Wook Jeoung, Sehie Park, SoYeon Kim, Benjamin Y Xu, Sreenidhi Iyengar Munimadugu, Paul Mitchell, Gerald Liew, Yanin Suwan, Jirayu Hong-amata, Sahil Thakur, Monisha E Nongipur, Tina Wong, Rahat Husain, Ng Si Rui, Yamon Syn, Phey Feng Lo, Nicholas Tan Yi Qiang, Shaista Hussain, Xiaofeng Lei, Zhi Da Soh, Marco Yu, Haslina Hamzah, Zizhou Wang, Yan Wang, Liangli Zhen, Xinxing Xu, Tien-Yin Wong, Tin Aung, Rachel S Chong, Yong Liu, Ching-Yu Cheng

Background: Artificial intelligence (AI)-based glaucoma detection from colour fundus photographs (CFP) offers scalable screening, but performance may decline on external datasets because of differences in ground-truth definitions, populations, and coexisting conditions such as high myopia (HM). We developed and validated a Vision Transformer-based deep learning (DL) model for glaucoma detection across multi-ethnic cohorts with and without HM. Methods: A ViT-B/16 model with predictive uncertainty estimation was developed using 56,483 CFPs (57.1% with myopia; 14.4% with HM). Glaucoma labels were standardised using clinical, imaging, and perimetry data. The model was validated on 16 independent datasets across three continents, including four datasets with explicit HM labels. Findings: Internal AUROC was 98.7% (95% CI 98.2-99.1%), with sensitivity 94.5% and specificity 97.3%. Across 16 external datasets from eight countries, AUROCs ranged from 86.4% to 99.6%. In HM eyes, internal AUROC was 97.8% (95% CI 96.1-99.2%), with sensitivity 94.8% and specificity 93.7%. External HM AUROCs were 86.5% in the Beijing Eye Study and 93.3%, 91.8%, and 85.5% in hospital-based datasets from Taiwan, Thailand, and South Korea. In an exploratory HM clinical evaluation, the model had higher CFP-only diagnostic accuracy than ophthalmologists and trained graders (92.0% vs 70.0%; p=0.008) and performed comparably to glaucoma specialists using full clinical information. Interpretation: The model showed robust glaucoma detection across myopic and non-myopic multi-ethnic populations and may support AI-assisted screening in settings with high HM prevalence.

摘要：背景：基於人工智慧（AI）的青光眼檢測從彩色眼底照片（CFP）中提供可擴展的篩檢，但由於地面真相定義、族群和共存條件（如高度近視（HM））的差異，性能在外部數據集上可能會下降。我們開發並驗證了一種基於視覺Transformer的深度學習（DL）模型，用於在有和沒有HM的多族群中檢測青光眼。方法：使用56,483張CFP（57.1%為近視；14.4%為HM）開發了具有預測不確定性估計的ViT-B/16模型。青光眼標籤使用臨床、影像和視野檢查數據進行標準化。該模型在三大洲的16個獨立數據集上進行了驗證，包括四個具有明確HM標籤的數據集。發現：內部AUROC為98.7%（95% CI 98.2-99.1%），敏感性為94.5%，特異性為97.3%。在來自八個國家的16個外部數據集中，AUROC範圍為86.4%至99.6%。在HM眼中，內部AUROC為97.8%（95% CI 96.1-99.2%），敏感性為94.8%，特異性為93.7%。外部HM AUROC在北京眼科研究中為86.5%，在來自台灣、泰國和南韓的醫院數據集中分別為93.3%、91.8%和85.5%。在一項探索性的HM臨床評估中，該模型的CFP僅診斷準確率高於眼科醫生和訓練評估者（92.0%對70.0%；p=0.008），並且在使用完整臨床信息時與青光眼專家表現相當。解釋：該模型在近視和非近視的多族群中顯示出穩健的青光眼檢測，並可能支持在HM高發的環境中進行AI輔助篩檢。

##### **SkinAgent AI: A Safety-Grounded Multimodal Agentic Framework for Non-Diagnostic Skincare Support**
2609.29341v1 by Muhammad Muhtasim Shahriar, Abdullah Mohammad Sayem, Tze Hui Liew, M. F. Mridha, Md. Mahiuddin

Consumer-facing skincare AI must coordinate visual evidence, product information, tool use, and user-facing actions within explicit evidence and safety boundaries. This study evaluates SkinAgent AI, a non-diagnostic multimodal framework that combines visual concern routing with grounded and auditable LLM-based orchestration. The architecture includes routing for Acne, Pores, and Wrinkles; photograph-based skin-type estimation; count-informed ordinal acne-severity support; typed tools; database-grounded recommendation and action functions; deterministic safety, privacy, and evidence checks; approval before state-changing actions; and structured trace and replay mechanisms. Visual-model performance and system-level agent behavior were evaluated separately. Across three seeds, the skin-condition routing model achieved 99.84% +/- 0.07% accuracy. Skin-type estimation achieved 88.85% accuracy, while count-informed acne-severity support achieved 84.59% accuracy with a quadratic weighted kappa of 0.9076. On a locked but non-independent 240-case system benchmark, intent accuracy was 80.00%, exact tool-set match was 62.92%, and strict task completion was 47.08%. No violations or successful cross-user leakage events were observed in the finite safety and privacy test suites. Tool-selection errors, incomplete grounding of product attributes, and unreliable failure fallback nevertheless remained. These findings support the feasibility of bounded, database-grounded, and traceable agent orchestration for non-diagnostic skincare assistance. They do not establish clinical readiness, external generalization, formal privacy guarantees, or universal safety. Independent validation, expert assessment, robustness and fairness testing, and prospective evaluation in real-world settings remain necessary.

摘要：消費者面向的護膚 AI 必須在明確的證據和安全邊界內協調視覺證據、產品信息、工具使用和用戶面向的行動。這項研究評估了 SkinAgent AI，一個非診斷性的多模態框架，結合了視覺關注路由與基於 LLM 的可追溯和可審計的協調。該架構包括針對痤瘡、毛孔和皺紋的路由；基於照片的皮膚類型估計；基於數量的有序痤瘡嚴重性支持；類型化工具；基於數據庫的推薦和行動功能；確定性的安全性、隱私和證據檢查；在狀態變更行動之前的批准；以及結構化的追蹤和重放機制。視覺模型性能和系統級代理行為分別進行了評估。在三個種子中，皮膚狀況路由模型達到了 99.84% +/- 0.07% 的準確率。皮膚類型估計達到了 88.85% 的準確率，而基於數量的痤瘡嚴重性支持達到了 84.59% 的準確率，二次加權 kappa 為 0.9076。在一個鎖定但非獨立的 240 案例系統基準上，意圖準確率為 80.00%，精確工具集匹配為 62.92%，而嚴格任務完成率為 47.08%。在有限的安全和隱私測試套件中未觀察到違規或成功的跨用戶洩漏事件。然而，工具選擇錯誤、產品屬性的不完全基礎和不可靠的失敗回退仍然存在。這些發現支持了針對非診斷性護膚輔助的有界、基於數據庫和可追溯的代理協調的可行性。它們並未建立臨床準備、外部泛化、正式的隱私保證或普遍安全性。獨立驗證、專家評估、穩健性和公平性測試，以及在現實世界環境中的前瞻性評估仍然是必要的。

##### **Reasoning Instructions Can Break Answer Decoding in Vision--Language Models**
2609.29278v1 by Zeyan Li, Siyuan Qiu, Jianfeng Xu

Chain-of-thought (CoT) instructions can distort multiple-choice VLM evaluation when a scorer appends a reasoning cue but reads answer-label logits before the model generates any rationale. We call this CoT-prefix scoring. On ScienceQA, Qwen2.5-VL-7B drops from 80.76% to 45.48%, and across five option-content permutations 93.54% of CoT-prefix predictions select the first slot. Condition-matched linear probes recover 78.94% from the same hidden states, while free generation restores 75.24%, showing that the answer often survives the prefix and the immediate readout fails. Vocabulary and layer diagnostics explain the mismatch: probability mass moves toward continuation tokens, while answer information remains linearly accessible in late layers. The effect recurs with varying severity across datasets and models, though not universally. These results show that CoT-prefix scoring can confound model knowledge with an evaluation-interface mismatch and should be avoided unless the requested and scored output events are aligned.

摘要：Chain-of-thought (CoT) 指令在評估多選擇 VLM 時可能會扭曲結果，當評分者附加推理提示但在模型生成任何理由之前閱讀答案標籤的 logits。我們稱這種情況為 CoT-prefix 評分。在 ScienceQA 上，Qwen2.5-VL-7B 的分數從 80.76% 降至 45.48%，而在五個選項內容的排列中，93.54% 的 CoT-prefix 預測選擇了第一個槽位。條件匹配的線性探針從相同的隱藏狀態中恢復了 78.94%，而自由生成恢復了 75.24%，顯示答案通常在前綴中存活，而即時的讀取失敗。詞彙和層診斷解釋了這種不匹配：概率質量向延續標記移動，而答案信息在後期層中保持線性可訪問。這種效應在不同數據集和模型中以不同的嚴重程度重現，儘管並非普遍存在。這些結果顯示，CoT-prefix 評分可能會因評估介面不匹配而混淆模型知識，除非請求和評分的輸出事件對齊，否則應避免使用。

##### **Where Does Exactly-Once Live? Model, Harness, and Tool-Contract Effects on Duplicate Side Effects in LLM Agents**
2609.29095v1 by Jiapeng Li

When a tool-using agent's write times out or returns a server error, the action may already have taken effect. Retrying blindly duplicates it -- a second charge, a second announcement, a second deployment -- while giving up skips required work. We ask where exactly-once behaviour should be enforced: in the model, in the agent harness, or in the tool contract. We introduce LIMBO, a deterministic sandbox of six services with realistic contracts (optional idempotency keys, eventually consistent and missing read paths) and twelve fault modes injected at the service boundary, including late commits, redelivery and partial batches; every episode is graded against a ledger of committed effects. Across 25,930 episodes spanning nine recent models, three production agent harnesses, two contract variants and fifteen recovery conditions, the answer depends on the fault. When an immediate read-back can reveal what happened, the model decides: frontier models instructed to act exactly once almost never duplicate a write whose acknowledgement was lost (0.5%), weaker models often do, and the model explains 53% of the explained variance. When it cannot -- the request is still in flight, or the transport delivered it twice -- the same frontier models duplicate in 56% and 74% of episodes, and the contract explains 81%. We prove that no verification-only policy is exactly-once under late commits without a bound on in-flight time. Waiting works when such a bound is short and known, but with heavy-tailed in-flight delays even an hour of waiting per episode falls short of offering an idempotency key on every write, which lowers the duplicate rate from 28% to 4% because agents use keys when they exist. The harness barely matters, a guard that attaches keys transfers across harnesses unchanged, and agents reported success in 90% of the episodes in which they had duplicated an effect.

摘要：當一個使用工具的代理的寫入超時或返回伺服器錯誤時，該行動可能已經生效。盲目重試會重複它——第二次收費、第二次公告、第二次部署——而放棄則跳過了必要的工作。我們詢問確切應該在哪裡強制執行一次性行為：在模型中、在代理的工具中，還是在工具合約中。我們介紹了 LIMBO，一個具有六個服務的確定性沙盒，擁有現實的合約（可選的冪等鍵、最終一致和缺失的讀取路徑）以及在服務邊界注入的十二種故障模式，包括延遲提交、重新交付和部分批次；每個事件都根據已提交效果的賬本進行評分。在跨越九個最近模型、三個生產代理工具、兩個合約變體和十五個恢復條件的 25,930 個事件中，答案取決於故障。當立即回讀可以揭示發生了什麼時，模型會決定：被指示精確執行一次的前沿模型幾乎從不重複確認丟失的寫入（0.5%），較弱的模型則常常會，模型解釋了 53% 的解釋變異性。當無法做到這一點時——請求仍在傳輸中，或傳輸重複交付——相同的前沿模型在 56% 和 74% 的事件中重複，而合約解釋了 81%。我們證明，沒有驗證僅的政策在延遲提交下是無法實現一次性的，且沒有對在途時間的限制。當這樣的限制較短且已知時，等待是有效的，但在重尾的在途延遲下，即使每個事件等待一小時也無法在每次寫入上提供冪等鍵，這將重複率從 28% 降低到 4%，因為代理在存在鍵時會使用它們。工具幾乎不重要，附加鍵的保護裝置在工具之間轉移時不變，代理在 90% 的重複效果的事件中報告成功。

##### **A Rapid Pipeline for Training and Deploying ML Models on WeBe Band**
2609.29084v1 by Ehsan Kourkchi, Asmita Asmita, Houman Homayoun, Mahdi Eslamimehr

Developing optimized machine-learning algorithms for edge devices with limited computational and memory resources is challenging, time-consuming, and highly dependent on device-specific constraints. In this work, we streamline an edge ML workflow to enable rapid development, optimization, and deployment of machine-learning (ML) models directly on the WeBe Band, a wrist-worn wearable device designed for multimodal physiological data monitoring. The proposed system automatically generates hardware-efficient ML models that can be easily integrated into the WeBe core firmware, supporting AutoML, hardware-aware quantization, and performance profiling to build models that meet desired latency targets while remaining compatible with device memory and power limitations.
  The proposed framework tightly integrates the open-source Piccolo AI ecosystem with an automated pipeline that generates deployable firmware artifacts, performs hardware-aware model compilation, and supports over-the-air (OTA) deployment. The system supports multiple lightweight model classes, including classical machine-learning algorithms and neural networks, and provides built-in on-device profiling tools to evaluate inference latency and memory footprint under realistic execution conditions. Experimental results demonstrate clear trade-offs between model complexity and deployability on a microcontroller, showing that classical models offer strong real-time performance while lightweight neural networks require careful resource management.
  Rather than proposing new learning architectures, the current work mainly focuses on system-level automation, deployability, and enabling researchers and developers to rapidly iterate on models and evaluate them directly on target hardware. Although demonstrated on the WeBe Band platform, the workflow is designed to be extensible to other ML-powered edge devices.

摘要：開發針對計算和記憶體資源有限的邊緣設備的優化機器學習算法是一項具有挑戰性、耗時且高度依賴於設備特定限制的工作。<lb/>在本研究中，我們簡化了一個邊緣機器學習工作流程，以便能夠快速開發、優化和部署機器學習（ML）模型，直接在 WeBe Band 上，這是一款設計用於多模態生理數據監測的手腕可穿戴設備。<lb/>所提出的系統自動生成硬體高效的機器學習模型，這些模型可以輕鬆整合到 WeBe 核心固件中，支持 AutoML、硬體感知量化和性能分析，以構建滿足所需延遲目標的模型，同時保持與設備記憶體和功耗限制的兼容性。<lb/>所提出的框架將開源的 Piccolo AI 生態系統與自動化管道緊密集成，該管道生成可部署的固件工件，執行硬體感知模型編譯，並支持空中下載（OTA）部署。<lb/>該系統支持多種輕量級模型類別，包括經典機器學習算法和神經網絡，並提供內建的設備上分析工具，以評估在現實執行條件下的推理延遲和記憶體佔用。<lb/>實驗結果顯示模型複雜性與在微控制器上可部署性之間的明顯權衡，顯示經典模型提供強大的實時性能，而輕量級神經網絡則需要謹慎的資源管理。<lb/>目前的工作並不提出新的學習架構，而主要集中在系統級自動化、可部署性，以及使研究人員和開發者能夠快速迭代模型並直接在目標硬體上評估它們。<lb/>儘管在 WeBe Band 平台上進行了演示，但該工作流程旨在擴展到其他基於機器學習的邊緣設備。

##### **When Does Action Credit Need Updating?**
2609.29007v1 by Hongye Yang, Boxiao Huang

Tool-using agents are continually updated with new interaction data. After each policy update, however, previously estimated action credits may become stale. Recomputing them from scratch can require many additional tool calls and environment interactions, making repeated updates increasingly expensive. We ask a simple question: when does historical action credit actually need to be updated? Our key observation is that a change in action value does not necessarily imply a change in the decision. Historical credit can still be useful as long as policy-induced drift is too small to overturn the existing action ranking. Building on this idea, we introduce pairwise branch sensitivity to capture how strongly a policy update affects the downstream regions that distinguish two candidate actions. We then derive a first-order anchored credit-transport estimator that updates historical credit using old interventional trajectories, and propose a Decision-Sufficient Credit Gate (DSC-Gate) that chooses whether to reuse, transport, or resample credit. Experiments show that branch sensitivity explains credit drift substantially better than global policy distance. With sufficient historical data, credit transport reduces estimation error, while its benefit to decision making is concentrated on updates that affect action-distinguishing branches. On a fully independent test set, DSC-Gate changes mean regret by only +0.00004 relative to a gap-based gate while reducing mean new tool steps from 472 to 286, a 39.4% reduction. We observe the same pattern after a real tool-agent parameter update. Overall, our results show that agents do not need to recompute action credit after every policy update: much of the historical evidence can be reused or cheaply corrected, reducing the additional interaction required to keep action decisions up to date.

摘要：使用工具的代理不斷更新新的互動數據。 然而，在每次策略更新後，先前估計的行動信用可能會變得過時。 從頭開始重新計算它們可能需要許多額外的工具調用和環境互動，這使得重複更新變得越來越昂貴。 我們提出一個簡單的問題：歷史行動信用何時實際需要更新？ 我們的關鍵觀察是，行動價值的變化並不一定意味著決策的變化。 只要政策引起的漂移過小，以至於無法推翻現有的行動排名，歷史信用仍然可以是有用的。 基於這個想法，我們引入了成對分支敏感度，以捕捉策略更新對區分兩個候選行動的下游區域的影響程度。 然後，我們推導出一種一階錨定信用傳輸估計器，利用舊的干預軌跡來更新歷史信用，並提出了一個決策充分信用門（DSC-Gate），用於選擇是重用、傳輸還是重新抽樣信用。 實驗顯示，分支敏感度對信用漂移的解釋明顯優於全局策略距離。 在有足夠歷史數據的情況下，信用傳輸減少了估計誤差，而其對決策的好處集中在影響行動區分分支的更新上。 在一個完全獨立的測試集上，DSC-Gate相對於基於間隙的門僅改變了平均懊悔+0.00004，同時將平均新工具步驟從472減少到286，減少了39.4%。 我們在一次真實的工具代理參數更新後觀察到相同的模式。 總體而言，我們的結果顯示，代理不需要在每次策略更新後重新計算行動信用：大量的歷史證據可以重用或廉價修正，減少了保持行動決策最新所需的額外互動。

##### **Beneath the Scores: Rethinking Hallucination Evaluation for Video Understanding Models**
2609.28991v1 by Shuzhi Gong, Fengze Sun, Yuansan Liu

Video understanding is increasingly performed by multi-stage LLM agents that separate temporal grounding, visual observation, and reasoning. Yet these stages are typically evaluated on different benchmarks and distributions, making it difficult to determine where hallucinations originate. We first organize existing benchmarks around these stages and show that their scores provide inconsistent diagnostic signals: stronger stage-level performance does not reliably imply lower downstream hallucination, and even benchmarks targeting the same capability can disagree.
  We therefore introduce a causal stage-intervention protocol that overwrites individual stages while holding the downstream task fixed. Across 60,008 runs on three video-agent architectures, we find that grounding is the dominant source of downstream error, with roughly four times the causal impact of corrupting visual observations. Successful grounding depends primarily on locating the correct region rather than precise temporal overlap, explaining why standard mIoU metrics poorly predict downstream reliability. We further find that incorrect evidence is substantially more harmful than missing evidence. Finally, auditing existing benchmarks against these interventions reveals that their scores do not reliably predict causal cascade sensitivity and can fail under distribution shift. These results motivate intervention-based, stage-aware evaluation for trustworthy video agents.

摘要：視頻理解越來越多地由多階段的 LLM 代理執行，這些代理分離了時間基礎、視覺觀察和推理。<lb/>然而，這些階段通常在不同的基準和分佈上進行評估，這使得確定幻覺的來源變得困難。<lb/>我們首先圍繞這些階段組織現有的基準，並顯示它們的分數提供了一致性不足的診斷信號：更強的階段級表現並不可靠地意味著較低的下游幻覺，甚至針對相同能力的基準也可能存在分歧。<lb/>因此，我們引入了一種因果階段干預協議，該協議在固定下游任務的同時覆蓋各個階段。<lb/>在三種視頻代理架構上進行的 60,008 次運行中，我們發現基礎是下游錯誤的主要來源，其因果影響大約是破壞視覺觀察的四倍。<lb/>成功的基礎主要依賴於定位正確的區域，而不是精確的時間重疊，這解釋了為什麼標準的 mIoU 指標無法良好預測下游可靠性。<lb/>我們進一步發現，不正確的證據比缺失的證據更具危害性。<lb/>最後，對現有基準進行這些干預的審核顯示，它們的分數並不能可靠地預測因果級聯敏感性，並且在分佈變化下可能會失效。<lb/>這些結果促使我們基於干預的、階段意識的評估，以實現可信的視頻代理。

##### **An Explainable DistilBERT-BiLSTM-Attention Framework for Binary and Multi-Class Hate Speech Detection**
2609.28703v1 by Rameesha Zia, Muhammad Shahid Iqbal Malik

Hate speech on social media poses serious risks to social harmony, mental well-being, and public safety, making its timely and accurate detection essential for content moderation systems. Most existing studies focus on binary classification, evaluated their frameworks on a single dataset, and provide limited insight into how decisions are made, which limits their real-world applicability. In addition, limited work is done on the explainability of their predictive inference. To address these challenges, this study proposes a multilevel and explainable hate speech detection framework. The proposed model integrates DistilBERT (Distilled Bidirectional Encoder Representations from Transformers) embeddings with a Bi-LSTM (Bidirectional Long Short-Term Memory) model, and an attention mechanism to capture both contextual meaning and sequential dependencies in text. To enhance trust and transparency, LIME (Local Interpretable Model-agnostic Explanations) is employed to explain model predictions by highlighting influential textual features. The framework is evaluated on two benchmark datasets using both binary and multi-class classification to examine robustness and generalization. In addition, an ablation study is presented to highlight the significance of various components of proposed framework. For binary classification, the proposed model achieves F1-scores of 96.78% on the Davidson dataset and 99.53% on the SMHS dataset. In the multi-class setting, it attains F1-scores of 97.00% and 94.99% on the Davidson and SMHS datasets, respectively, outperforming existing baseline approaches. The results demonstrate that multilevel evaluation improves the reliability that the proposed framework effectively balances performance and efficiency. This makes the framework suitable for practical hate speech moderation systems that require accurate, generalizable, and explainable decisions.

摘要：社交媒體上的仇恨言論對社會和諧、心理健康和公共安全構成嚴重風險，因此及時且準確的檢測對於內容審核系統至關重要。大多數現有研究專注於二元分類，並在單一數據集上評估其框架，對於決策過程的理解有限，這限制了它們在現實世界中的適用性。此外，對於其預測推斷的可解釋性研究也相對有限。為了解決這些挑戰，本研究提出了一個多層次且可解釋的仇恨言論檢測框架。所提出的模型將DistilBERT（從Transformer中提取的蒸餾雙向編碼器表示）嵌入與Bi-LSTM（雙向長短期記憶）模型和注意力機制相結合，以捕捉文本中的上下文意義和序列依賴性。為了增強信任和透明度，採用了LIME（局部可解釋的模型無關解釋）來通過突出影響文本特徵來解釋模型預測。該框架在兩個基準數據集上進行評估，使用二元和多類別分類來檢查其穩健性和泛化能力。此外，還提出了一項消融研究，以突出所提框架中各組件的重要性。對於二元分類，所提出的模型在Davidson數據集上達到96.78%的F1分數，在SMHS數據集上達到99.53%的F1分數。在多類別設置中，它在Davidson和SMHS數據集上分別達到97.00%和94.99%的F1分數，超越了現有的基準方法。結果顯示，多層次評估提高了所提框架在性能和效率之間有效平衡的可靠性。這使得該框架適合於需要準確、可泛化和可解釋決策的實際仇恨言論審核系統。

##### **LAYERSCOPE: A Layerwise Characterization of Video and Multimodal Learned Representations**
2609.28086v2 by Sandra Arcos-Holzinger, Debashish Chakraborty, Rohita Mocharla, Will Walden, Andrew Yates, Reno Kriz, Sarah M. Erfani, James Bailey, Vishal M. Patel, Sanjeev Khudanpur

We propose LAYERSCOPE, a label-free, layerwise framework that aims to characterize a model's learned representations in video and multimodal settings. Evaluating downstream performance using representations from final or intermediate layers typically requires large amounts of labeled data, repeated task-specific evaluations, and substantial computation. To address these limitations, LAYERSCOPE uses local, global, distributional, and correspondence-based geometric metrics to compare layerwise representation structure within and across models without requiring task-specific labels. We evaluate seven architecturally diverse models across video and multimodal classification, clustering, and text-to-video retrieval tasks from MVEB/MVEB+. We find that intermediate-layer representations can outperform final-layer and model-default outputs. We also find that no single geometric metric consistently predicts downstream performance, but note that distinct layerwise geometric signatures emerge across model families. LID shows task-dependent relationships with performance, while RankMe provides the strongest measure for classification and clustering, but is not a universal layer selector. We also find that pairing-aware metrics explain retrieval better than distributional distances alone. LAYERSCOPE therefore offers a framework for comparing representations across models and layers, enabling a more systematic evaluation in video and multimodal settings.

摘要：我們提出了LAYERSCOPE，一個無標籤的逐層框架，旨在描述模型在視頻和多模態環境中學習到的表示。使用最終層或中間層的表示來評估下游性能通常需要大量標記數據、重複的任務特定評估和大量計算。為了解決這些限制，LAYERSCOPE使用局部、全局、分佈和基於對應的幾何度量來比較模型內部和跨模型的逐層表示結構，而無需任務特定的標籤。我們評估了七個架構多樣的模型，涵蓋視頻和多模態分類、聚類以及文本到視頻檢索任務，來自MVEB/MVEB+。我們發現中間層表示的性能可以超越最終層和模型默認輸出。我們還發現，沒有單一的幾何度量能夠一致地預測下游性能，但注意到不同的逐層幾何特徵在模型家族中出現。LID顯示出與性能的任務依賴關係，而RankMe則為分類和聚類提供了最強的度量，但並不是一個通用的層選擇器。我們還發現，配對感知度量比單獨的分佈距離更好地解釋檢索。因此，LAYERSCOPE提供了一個比較模型和層之間表示的框架，使在視頻和多模態環境中的評估更加系統化。

##### **Auditability Is Not One Property: Rule Overlap, Behavioural Agreement, and Composition in Reinforcement Learning**
2609.28581v1 by Liu Hung Ming

Reinforcement-learning (RL) policies are often distributed as opaque neural checkpoints, while training logs show that a run occurred without explaining what the policy learned. We study whether independently trained policies can be represented and composed through auditable discrete behavioral rules. We define auditability as six separately testable predicates: trace integrity, lossless coding, rule coverage, behavioral agreement, composition quality, and value-model reliability. Our protocol uses a shared frozen symbolizer, passive rule extraction, an append-only hash-bound ledger, exact environment replay, and offline confidence-ranked arbitration with an explicit blind-spot fallback.
  The results place strict limits on this description layer. Rule-set overlap does not imply behavioral agreement: policies may share symbolic rules while choosing near-chance-matching actions on fresh states. The fused policy therefore selects among existing rules rather than generating a new skill. On a conflict-dominated task, an apparent fusion failure is traced to an induction/deployment mismatch: rules induced from sampled actions were evaluated under argmax actions, and deployment-consistent re-induction reverses the arbitration ordering. A fitted-Q generalized-policy-improvement diagnostic also fails in both environments, limiting claims that rule fusion is superior to value-based composition. One exploratory comparison favors rule fusion, but its comparator is post hoc, the task is partly saturated, and the fused policy remains below the strongest held-out actor.
  We contribute an evidence-bounded audit and composition protocol, not a claim of universal interpretability or autonomous skill generation. Future work must add temporally extended skills, cross-skill interfaces, composition search, and independent novelty audits.

摘要：強化學習（RL）策略通常以不透明的神經檢查點形式分發，而訓練日誌顯示運行發生了，但未解釋策略學到了什麼。我們研究獨立訓練的策略是否可以通過可審計的離散行為規則進行表示和組合。我們將可審計性定義為六個可單獨測試的謂詞：追蹤完整性、無損編碼、規則覆蓋、行為一致性、組合質量和價值模型可靠性。我們的協議使用共享的凍結符號化器、被動規則提取、僅附加的哈希綁定賬本、精確的環境重播以及離線信心排名仲裁，並帶有明確的盲點回退。

結果對這個描述層施加了嚴格的限制。規則集重疊並不意味著行為一致：策略可能共享符號規則，同時在新狀態上選擇近乎隨機匹配的行動。因此，融合策略是在現有規則中進行選擇，而不是生成新的技能。在一個以衝突為主導的任務中，明顯的融合失敗被追溯到歸納/部署不匹配：從抽樣行動中歸納的規則在 argmax 行動下被評估，而與部署一致的重新歸納則會逆轉仲裁順序。擬合 Q 的廣義政策改進診斷在兩個環境中也失敗，限制了對規則融合優於基於價值的組合的主張。一個探索性比較偏向於規則融合，但其比較對象是事後的，任務部分飽和，且融合策略仍低於最強的保留演員。

我們貢獻了一個證據界限的審計和組合協議，而不是對普遍可解釋性或自主技能生成的主張。未來的工作必須增加時間延展技能、跨技能接口、組合搜尋和獨立的新穎性審計。

##### **When Explanations Cannot Be Read: Measuring and Correcting SHAP and LIME Rendering for Right-to-Left Languages**
2609.28565v1 by Rameesha Zia, Muhammad Shahid Iqbal Malik

Post hoc explanation methods such as SHAP and LIME are widely used to interpret text classifiers, but their visualizations are mainly designed for left-to-right languages. When applied to right-to-left (RTL) languages such as Urdu, Arabic, Persian, and Hebrew, the attribution values remain mathematically valid, while their visual presentation fails. Tokens appear out of sequence, connected letterforms break apart, and plot layouts do not follow the natural reading direction. This study addresses this gap as a visualization problem rather than a limitation of the explanation methods themselves. We present SHAP-RTL, a rendering layer that corrects reading direction and script shaping in SHAP and LIME visualizations, with per-language font selection, while preserving the original attribution values, feature ordering, and model outputs. The approach is evaluated on Urdu, Arabic, Hebrew, and Persian hate and offensive-language datasets using TF-IDF and logistic regression classifiers. Rendering correctness is measured by an OCR round trip over 200 feature words per language. Default rendering yields character error rates of 0.820 to 0.979, meaning the label no longer carries its token; the common reshape-and-reorder workaround fails for Urdu at 0.998, worse than no correction; and the Matplotlib 3.11.0 text rewrite inverts that workaround, while SHAP-RTL remains correct under both versions. The framework also verbalizes the same attributions as short contextual explanations in the reader's language, constrained to the identified features. Evaluation in this paper concerns rendering correctness; assessment of the generated explanations is left to future work. The study highlights the importance of language-aware visualization in making post hoc explainability more accessible across different writing systems.

摘要：後 hoc 解釋方法如 SHAP 和 LIME 被廣泛用於解釋文本分類器，但其視覺化主要設計用於從左到右的語言。當應用於從右到左（RTL）語言如烏爾都語、阿拉伯語、波斯語和希伯來語時，歸因值在數學上仍然有效，但其視覺呈現卻失敗。標記出現順序錯誤，連接的字母形狀分開，圖表佈局未遵循自然閱讀方向。本研究將這一差距視為視覺化問題，而非解釋方法本身的限制。我們提出了 SHAP-RTL，一個渲染層，修正了 SHAP 和 LIME 視覺化中的閱讀方向和字形，並根據每種語言選擇字體，同時保留原始的歸因值、特徵排序和模型輸出。該方法在烏爾都語、阿拉伯語、希伯來語和波斯語的仇恨和攻擊性語言數據集上進行評估，使用 TF-IDF 和邏輯回歸分類器。渲染正確性通過每種語言的 200 個特徵詞進行 OCR 回合測量。默認渲染的字符錯誤率為 0.820 到 0.979，這意味著標籤不再攜帶其標記；對於烏爾都語，常見的重塑和重新排序的變通方法失敗，達到 0.998，甚至比不進行修正還要糟糕；而 Matplotlib 3.11.0 的文本重寫則反轉了該變通方法，而 SHAP-RTL 在兩個版本下均保持正確。該框架還將相同的歸因以讀者的語言表達為簡短的上下文解釋，並限制於已識別的特徵。本文的評估關注渲染正確性；對生成解釋的評估留待未來的工作。該研究突顯了語言感知視覺化在使後 hoc 可解釋性在不同書寫系統中更具可及性的重要性。

##### **Can Jev Judge Radiology Reports? Evaluating a System One Model for Clinical Factuality**
2609.27607v1 by Jiaju Huang, Hao Yang, Xinyu Ma, Xinglong Liang, Kunyan Cai, Junqiang Ma, Shaobin Chen, Yue Sun, Tao Tan

An AI-generated radiology report can resemble a physician's report while omitting an abnormality, adding an unsupported finding, or reversing its presence. Measuring these factual differences is essential for evaluating report generators. We study Jev, a System One decision model, as a simple, low-cost judge of agreement with physician-written reference reports. Our evaluator checks whether each statement is supported by the other report and combines these judgments in both directions to capture unsupported claims and omissions. A single-question configuration reaches Kendall correlations of 0.573 on RadEvalX and 0.398 on RadEvalExpert with expert error counts, outperforming an open natural language inference judge under matched decomposition and aggregation. One support question per statement retains similar expert agreement to seven while using 43-45% fewer judgment input tokens. At the documented API price, judgments cost under three cents per hundred report pairs, excluding local decomposition. In a separate controlled-error test, Jev detects false negation with an AUROC of 0.977. Local RadMatch achieves stronger agreement on clinically significant errors in both expert datasets and on total errors in the shared RadEvalExpert subset. Finding-count and error-scope analyses show that benchmark agreement reflects report size and error definitions as well as medical error detection. These results support Jev as a practical judgment component for measuring factual differences in generated radiology reports and identify where more elaborate evaluation remains valuable.

摘要：一份由人工智慧生成的放射科報告可能看起來像醫生的報告，但卻省略了異常情況、添加了不支持的發現或反轉了其存在。衡量這些事實差異對於評估報告生成器至關重要。我們研究了Jev，一個系統一的決策模型，作為與醫生撰寫的參考報告一致性的簡單、低成本的判斷工具。我們的評估者檢查每個陳述是否得到另一份報告的支持，並將這些判斷在雙向中結合，以捕捉不支持的主張和省略。單一問題配置在RadEvalX上達到0.573的Kendall相關性，在RadEvalExpert上達到0.398的專家錯誤計數，表現超過了在匹配分解和聚合下的開放自然語言推理判斷。每個陳述一個支持問題保留了與七個問題相似的專家一致性，同時使用了43-45%的較少判斷輸入標記。在記錄的API價格下，判斷的成本低於每百對報告三美分，不包括本地分解。在一個單獨的控制錯誤測試中，Jev以0.977的AUROC檢測到假陰性。在專家數據集中，Local RadMatch在臨床顯著錯誤上達成了更強的一致性，並在共享的RadEvalExpert子集上對總錯誤也有更強的一致性。發現計數和錯誤範圍分析顯示基準一致性反映了報告大小和錯誤定義，以及醫療錯誤檢測。這些結果支持Jev作為一個實用的判斷組件，用於測量生成的放射科報告中的事實差異，並確定了更詳細評估仍然有價值的地方。

##### **NV-Reason-CT: 3D Visual Language Model for CT Analysis**
2609.27511v1 by Andriy Myronenko, Dong Yang, Yucheng Tang, Baris Turkbey, Benjamin Simon, Stephanie Harmon, Rikhil Makwana, Mariam Aboian, Sena Azamat, Ibrahim Ethem Hamamci, Sezgin Er, Bjoern Menze, Marc Edgar, Yufan He, Pengfei Guo, Daguang Xu

We present NV-Reason-CT, a generative vision--language model for chest and abdominal CT combining native 3D visual encoding with radiologist-guided reasoning. The model couples a native 3D vision transformer with a language model, passing all visual tokens and their explicit 3D coordinates into language decoding without further spatial token merging. This retains volumetric spatial information within the vision encoder and through the language model's positional encoding during joint processing with text.
  We train on a curated corpus of approximately 550,000 multimodal instruction examples from 70,111 unique CT image inputs, combining standardized reports, abnormality-focused and anatomy-specific questions, multi-turn interactions, and radiologist-authored reasoning from recorded and transcribed expert CT interpretations. Expert annotations provide direct supervision and guide additional report-grounded synthetic reasoning. End-to-end supervised fine-tuning (SFT) is followed by Group Relative Policy Optimization (GRPO), with verifiable rewards over chest and abdominal abnormality sets.
  The model supports abnormality classification, report generation, and interactive reasoning with reviewable observations, differential diagnoses, and uncertainty. Evaluation spans public CT benchmarks and a held-out NIH cohort. On CT-RATE, NV-Reason-CT achieves a macro-F1 of 0.614 and macro-AUROC of 0.871 without a task-specific classification head; generated reports achieve a report-derived macro-F1 of 0.592. In a preliminary study with expert radiologists, AI-assisted review received favorable confidence ratings and was associated with a 50% reduction in average reported interpretation and reporting time. We release the model and training code to support reproducible research on explainable AI for volumetric medical imaging.

摘要：我們提出了 NV-Reason-CT，一種生成式視覺-語言模型，專為胸部和腹部 CT 結合原生 3D 視覺編碼與放射科醫生指導的推理。該模型將原生 3D 視覺Transformer與語言模型結合，將所有視覺標記及其明確的 3D 坐標傳入語言解碼，而不進行進一步的空間標記合併。這在視覺編碼器中保留了體積空間信息，並在與文本的聯合處理過程中通過語言模型的位置信息編碼進行保留。

我們在一個精心策劃的語料庫上進行訓練，該語料庫包含約 550,000 個多模態指令示例，來自 70,111 個獨特的 CT 圖像輸入，結合了標準化報告、以異常為重點和特定解剖學的問題、多輪互動，以及放射科醫生撰寫的推理，這些推理來自記錄和轉錄的專家 CT 解釋。專家註釋提供了直接的監督，並指導額外的報告基礎合成推理。端到端的監督微調（SFT）之後，進行了群體相對策略優化（GRPO），並對胸部和腹部異常集進行可驗證的獎勵。

該模型支持異常分類、報告生成和互動推理，並提供可審查的觀察、鑑別診斷和不確定性。評估涵蓋公共 CT 基準和保留的 NIH 群體。在 CT-RATE 上，NV-Reason-CT 實現了 0.614 的宏觀 F1 和 0.871 的宏觀 AUROC，且未使用特定任務的分類頭；生成的報告達到了 0.592 的報告衍生宏觀 F1。在與專家放射科醫生的初步研究中，AI 輔助的審查獲得了良好的信心評級，並與平均報告解釋和報告時間減少 50% 相關聯。我們發布了模型和訓練代碼，以支持可重現的關於體積醫學影像的可解釋 AI 的研究。

##### **Memory Control Signals Emerge Before Action in Long Horizon Agents**
2609.27286v1 by Mingxuan Wang, Guorun Yao, Fei Luo, Yinglong Guo, Chao Ning, Bo Wang, Hongyue Chen, Yanbiao Ma, Jungong Han

Long horizon language model agents continuously accumulate interaction history, increasing computational cost while making relevant information harder to preserve and reuse. Existing context management methods mainly focus on how to compress or retrieve history, but largely leave open whether the model itself already represents the need for these memory operations before they occur. We study the hidden state immediately before each agent action and find that compression and recall needs are already encoded in the model's internal representations. These signals cannot be explained by simple context length or interaction progress, and they exhibit distinct formation patterns across model depth. We further show that most memory decision information is preserved in a compact recent context, while selectively restored historical evidence complements the long range dependencies that recent context misses. Based on these findings, we propose Preaction Memory with Evidence Retrieval (PaMER), which combines state guided compression with external evidence retrieval. PaMER+ further introduces step level evidence selection to recover only the historical information required by the current task. Experiments on WorkBuddyBench, across multiple context management baselines and model backbones, show that our framework substantially reduces context consumption while maintaining competitive task performance.

摘要：長期視野語言模型代理不斷累積互動歷史，增加了計算成本，同時使得相關信息更難以保存和重用。現有的上下文管理方法主要集中在如何壓縮或檢索歷史，但在很大程度上對於模型本身是否已經表達了這些記憶操作的需求仍然保持開放。我們研究了每個代理行動之前的隱藏狀態，發現壓縮和回憶的需求已經編碼在模型的內部表徵中。這些信號無法通過簡單的上下文長度或互動進度來解釋，並且它們在模型深度上展現出不同的形成模式。我們進一步顯示，大多數記憶決策信息保存在緊湊的近期上下文中，而選擇性恢復的歷史證據則補充了近期上下文所缺失的長期依賴性。基於這些發現，我們提出了帶有證據檢索的前行動記憶（PaMER），它將狀態引導的壓縮與外部證據檢索相結合。PaMER+ 進一步引入步驟級別的證據選擇，以僅恢復當前任務所需的歷史信息。在 WorkBuddyBench 上的實驗，涵蓋多個上下文管理基準和模型骨幹，顯示我們的框架在保持競爭性任務表現的同時顯著減少了上下文消耗。

##### **Local Evidence and Geometric Readout Repair in Trained GNNs**
2609.27092v1 by Nadi Tomeh, Hugo Attali

Many node-classification GNNs apply a linear classifier to a nonnegative mixture of local messages. An error can reflect either poor mixture weights or a reachable logit set poorly positioned for the classifier. We separate these causes with an exact-mass linear program and two learned post-hoc repairs. Every reweighted prediction has an equivalent centered logit translation, but only translations in a message-induced displacement set are realizable by reweighting. Across eight datasets, eight GNN backbones, and ten splits, mean accuracy rises from 62.6% for the frozen models to 63.8% with reweighting and 65.3% with set-conditioned translation. A parameter-matched node-only translator reaches 64.6%, showing that translation explains most of the gain while the message set supplies a smaller additional benefit. Although oracle reweighting can correct many errors, label-free reweighting captures little of this potential: local evidence is often present but hard to select, and relaxing the evidence constraint is more effective than learning within it.

摘要：許多節點分類的 GNN 應用線性分類器於非負的局部訊息混合中。錯誤可能反映出不良的混合權重或可達的 logit 設置對於分類器的位置不佳。我們通過一個精確質量的線性程序和兩個學習的事後修正來區分這些原因。每個重新加權的預測都有一個等效的中心 logit 轉換，但只有在訊息引起的位移集中的轉換才能通過重新加權實現。在八個數據集、八個 GNN 骨幹和十個分割中，平均準確率從凍結模型的 62.6% 上升到重新加權的 63.8%，再到設置條件轉換的 65.3%。一個參數匹配的僅節點翻譯器達到 64.6%，顯示轉換解釋了大部分增益，而訊息集提供了較小的額外好處。儘管 oracle 重新加權可以糾正許多錯誤，但無標籤的重新加權幾乎無法捕捉到這種潛力：局部證據通常存在但難以選擇，並且放寬證據約束比在其中學習更有效。

##### **PACT: From Credit Assignment to Critic Alignment**
2609.26355v1 by Jiayan Fu, Hang Xu, Yong Zhang, Zhaokai Luo, Yao Hu, Dongyan Zhao, Mu Chuan

Reinforcement learning has become a central component of large language model (LLM) post-training, yet token-level credit lacks a generally accepted mathematical definition, leaving its relationship to commonly used training signals unclear. We formulate three regularity conditions, namely Completeness, Prefix Consistency, and Neutrality, and prove that they uniquely determine token-level credit. This characterization provides a unified basis for explaining phenomena across existing algorithms and guides the development of an improved actor-critic training procedure. Through this lens, an ideal teacher in On-Policy Distillation (OPD) acts as an implicit critic, yielding an expected policy gradient proportional to that induced by token-level credit. Response-level REINFORCE Leave-One-Out (RLOO) signals match the expected policy-gradient contribution of token-level credit despite their coarser granularity. We further establish approximate credit sparsity under bounded outcome rewards and show how intermediate critic errors in Generalized Advantage Estimation (GAE) can become comparable to the underlying credit. These motivate Policy Aligned Critic Training (PACT), which adopts an Actor-then-Critic update order to apply importance sampling correction to critic training and better align the critic with the updated policy. In agentic mathematical reasoning, PACT achieves 72.87% average accuracy across four benchmarks, outperforming GRPO and PPO by 8.80 and 13.16 percentage points, respectively. On SWE-bench Verified, PACT achieves a pass rate of 67.4%, outperforming PPO, GRPO, and SAO by 2.4, 2.0, and 3.8 percentage points, respectively.

摘要：強化學習已成為大型語言模型（LLM）後訓練的核心組成部分，但令牌級的信用缺乏普遍接受的數學定義，使其與常用訓練信號之間的關係不明確。<lb/>我們制定了三個規則條件，即完整性、前綴一致性和中立性，並證明它們唯一地決定了令牌級信用。<lb/>這一特徵提供了一個統一的基礎，用於解釋現有算法中的現象，並指導改進的演員-評論家訓練程序的發展。<lb/>從這個角度看，On-Policy Distillation（OPD）中的理想教師充當隱式評論家，產生與令牌級信用引起的預期策略梯度成比例的結果。<lb/>儘管反應級的REINFORCE Leave-One-Out（RLOO）信號的粒度較粗，但它們與令牌級信用的預期策略梯度貢獻相匹配。<lb/>我們進一步在有界結果獎勵下建立了近似信用稀疏性，並展示了在廣義優勢估計（GAE）中，中介評論家的誤差如何變得可與基礎信用相媲美。<lb/>這些動機促成了政策對齊評論家訓練（PACT），該方法採用了演員-然後-評論家的更新順序，對評論家訓練應用重要性抽樣修正，並更好地將評論家與更新的策略對齊。<lb/>在代理數學推理中，PACT在四個基準上達到了72.87%的平均準確率，分別超過GRPO和PPO 8.80和13.16個百分點。<lb/>在SWE-bench Verified上，PACT達到了67.4%的通過率，分別超過PPO、GRPO和SAO 2.4、2.0和3.8個百分點。

##### **xWhyL: Causal Interactive Learning**
2609.26037v1 by Nicholas Tagliapietra, Florian Peter Busch, Moritz Willig, Matej Zečević, Lavdim Halilaj, Juergen Luettin, Kristian Kersting

Explanations are central to causal reasoning, and cognitive science has long established that the human drive to explain is itself a mechanism for learning about causality. Despite this, learning from those abductive signals is largely ignored in artificial intelligence. While explainable AI (XAI) increasingly draws on causal models to generate explanations, the converse direction about what explanations can do for causality remains largely unexplored. To fill this gap, we propose xWhyL, a formal framework connecting causality and XAI by learning causal models from explanations. We develop a mathematical theory that translates explanations into a learning signal complementary to observational data, and demonstrate how it enables overcoming the limits of observational causal discovery. As explanations can be derived from incorrect beliefs and clash with data, a tension we call the Causal Tug-of-War, we prove conditions under which our framework rejects misspecified explanations rather than absorbing them. Our practical instantiation, Causal Interactive Learning (CIL), shows how expert explanations can efficiently support causal discovery and distinguish correct from incorrect explanations.

摘要：解釋是因果推理的核心，認知科學早已確立人類解釋的驅動力本身就是學習因果關係的機制。儘管如此，人工智慧在這些推理信號中學習的部分卻大多被忽視。雖然可解釋的人工智慧（XAI）越來越多地依賴因果模型來生成解釋，但關於解釋對因果關係的影響的反向方向仍然大多未被探索。為了填補這一空白，我們提出了xWhyL，一個通過從解釋中學習因果模型來連接因果關係和XAI的正式框架。我們發展了一個數學理論，將解釋轉化為一種補充觀察數據的學習信號，並展示了它如何使得克服觀察因果發現的限制成為可能。由於解釋可能源自錯誤的信念並與數據相衝突，我們稱之為因果拔河的緊張局勢，我們證明了在什麼條件下我們的框架會拒絕錯誤的解釋，而不是吸收它們。我們的實踐實現——因果互動學習（CIL）展示了專家解釋如何有效地支持因果發現並區分正確與錯誤的解釋。

##### **Compiling Sufficient Governance Context from Declared Losses and Reachable States: Exact Observation-Contract Synthesis with Cardinality and Cost Objectives**
2609.26016v1 by Gaston Besanson

We call the object this paper derives and certifies a minimal sufficient governance context: given a finite reachable-state model, a deterministic declared verdict, and candidate observable attributes, we compute sufficient observation sets, distinguish attributes that are individually indispensable from contracts that are jointly sufficient, and select among sufficient contracts under a cardinality or declared-cost objective. An observation contract is a set of candidate attributes whose values determine the declared verdict on every reachable state; an authority contract is one selected under an objective and bound to a gate schema. We synthesize every inclusion-minimal sufficient contract where exhaustive enumeration is affordable, and a minimum-cardinality or minimum-cost contract by SAT/MaxSAT encoding otherwise, checking sufficiency directly. On a constructed code/cloud domain, the individually-indispensable core is not sufficient as an observation contract and two distinct reducts exist; a preregistered cost model separates them exactly. On a second, larger, constructed domain, the same pattern recurs, but that domain's cost model does not separate the alternatives: a fully explained cost tie, reported as found. We measure discernibility-family scaling where exhaustive enumeration is confirmed infeasible within a registered timeout, while SAT/MaxSAT synthesis solves in well under a second; MaxSAT showed no measured cardinality advantage over plain SAT. AuthorityBench compares four baselines across three domains; the declared-only baseline is not exactly sufficient on any. Every selected contract is checked for sufficiency, with a counterexample on failure and a check summary, not a portable certificate, on success -- the compiler-focused scope of a two-scope table; an independently specified end-to-end case study is registered follow-up work, not claimed here.

摘要：我們稱這篇論文所推導和證明的對象為最小充分治理背景：給定一個有限可達狀態模型、一個確定性聲明的判決，以及候選可觀察屬性，我們計算充分觀察集，區分個別不可或缺的屬性與共同充分的合約，並在基數或聲明成本目標下選擇充分合約。觀察合約是一組候選屬性，其值決定每個可達狀態上的聲明判決；權威合約是在一個目標下選擇並綁定於一個閘門架構的合約。我們合成每個包含最小的充分合約，當全面列舉是可負擔的情況下，否則通過SAT/MaxSAT編碼合成最小基數或最小成本合約，並直接檢查充分性。在一個構建的代碼/雲域中，個別不可或缺的核心作為觀察合約並不充分，且存在兩個不同的簡約；一個預註冊的成本模型恰好將它們分開。在第二個更大構建的域中，相同的模式重現，但該域的成本模型並未分開這些選擇：完全解釋的成本平局，如實報告。我們測量可辨識性家族的擴展性，其中全面列舉在註冊的超時內被確認為不可行，而SAT/MaxSAT合成在不到一秒內解決；MaxSAT在測量的基數上未顯示出相對於普通SAT的優勢。AuthorityBench在三個域中比較四個基準；僅聲明基準在任何情況下都不完全充分。每個選定的合約都檢查其充分性，失敗時提供反例，成功時提供檢查摘要，而不是可攜帶的證書——一個以編譯器為中心的兩範疇表的範圍；一個獨立指定的端到端案例研究被註冊為後續工作，並未在此聲明。

##### **Skytopia: Monocular Drone Navigation with Action-Conditioned Latent World Models**
2609.26007v1 by Yuhang Zhang, Rangya Zhang, Yujing Shang, Zhuoyuan Yu, Weiying Wang, Steven Yang, Qingsong Yan, Chao Yan, Mir Feroskhan

Monocular drone navigation requires reaching a goal in an unseen environment from a single forward-facing camera, which offers few cues for depth and scale. World models address this by modelling how observations evolve under actions, but they are built to be executed: the prediction is produced at deployment and fed back into action generation at every control step. We argue that what a policy needs from a world model is not the prediction but the representation required to produce it: in flight the executed action explains almost all of the change between observations, so prediction reduces to reprojecting a static scene under a known displacement. We therefore introduce skytopia, a policy built on an action-conditioned latent world model, and the 3D Gaussian Splatting platform on which it is trained. A forward objective predicts the representation of the next observation from the intended motion, and an inverse objective recovers that motion from the predicted transition. Because the prediction never reaches action generation, the predictor is discarded and one policy serves point-goal, image-goal, and goal-free navigation. Simulation experiments show that skytopia outperforms every baseline under all three specifications, attaining 57.8%, 66.0%, and 49.0% success rate, while discarding the predictor removes 59.4% of the inference cost. The same policy is subsequently deployed on a physical drone without fine-tuning and reaches goals in indoor, open outdoor, and woodland environments.

摘要：單目無人機導航需要從單個前向攝像頭在未見環境中達到目標，這提供了很少的深度和尺度線索。世界模型通過建模觀察在行動下如何演變來解決這個問題，但它們是為了執行而構建的：預測在部署時產生，並在每個控制步驟中反饋到行動生成中。我們認為，政策從世界模型中需要的不是預測，而是產生它所需的表示：在飛行中，執行的行動幾乎解釋了觀察之間的所有變化，因此預測簡化為在已知位移下重新投影靜態場景。因此，我們引入了skytopia，一個基於行動條件潛在世界模型的政策，以及其訓練所用的3D高斯點雲平台。一個前向目標從預期運動中預測下一個觀察的表示，而一個反向目標則從預測的轉換中恢復該運動。由於預測從未達到行動生成，預測器被丟棄，並且一個政策服務於點目標、圖像目標和無目標導航。模擬實驗顯示，skytopia在所有三個規範下均超越了每個基準，成功率分別達到57.8%、66.0%和49.0%，而丟棄預測器則減少了59.4%的推理成本。同一政策隨後在物理無人機上部署，無需微調，並在室內、開放戶外和林地環境中達到目標。

##### **CausalLoss-Fin: Attributing Financial-Agent Loss to Decisions and Infrastructure Faults**
2609.25960v1 by Abhishek Sharma

When an agent handling a payment exception loses money, the agent-step attribution methods this paper compares against will name one of its actions. They will do so even when a settlement message was dropped and the agent never had a chance: they intervene on agent actions and do not expose infrastructure faults as intervenable variables, so every dollar they explain is charged to a decision. We take a benchmark whose fault process is explicit and replayable, decompose each episode's realised delivery schedule into named, individually repairable messages, and intervene on both the agent's choices and the infrastructure's. A telescoping identity splits any policy's loss exactly three ways: an infrastructure effect, a policy differential against the best implementable policy, and a reference-policy residual. Two of the three can be negative, so none is a share; Shapley then divides the first into signed allocations over individual messages. One result is structural and needs no corpus: an agent-only baseline identifies no infrastructure cause, because its model contains no variable that could name one. What 545 planted episodes across 3 policies measure is the size of that consequence. It misfiles 100% of infrastructure episodes and charges $114,383.40 to the agent. Repairing what it names recovers 0.0% of the available loss; repairing a minimal sufficient set recovers 100.0%. Scoring messages one at a time is not merely imprecise: 27.8% (95% CI: 23.3--32.3%) of episodes do not decompose additively. We evaluate deterministic programmatic policies rather than language-model agents, which is what makes replay exact and which limits external validity to stochastic agents. The prevalence figures are properties of this generator, not field rates.

摘要：當處理支付異常的代理人損失金錢時，本論文比較的代理步驟歸因方法將會指名其某一行動。即使在結算消息被丟棄且代理人從未有機會的情況下，它們仍會這樣做：它們干預代理行動，並且不將基礎設施故障視為可干預變量，因此它們解釋的每一美元都歸因於一個決策。我們採取一個故障過程明確且可重播的基準，將每個實現的交付時間表分解為命名的、可單獨修復的消息，並對代理的選擇和基礎設施進行干預。一個縮放身份將任何政策的損失精確地分為三個部分：基礎設施效應、相對於最佳可實施政策的政策差異，以及參考政策的殘差。三者中有兩個可以是負數，因此沒有一個是份額；Shapley 然後將第一部分劃分為對個別消息的簽名分配。一個結果是結構性的，並且不需要語料庫：僅代理的基準無法識別任何基礎設施原因，因為其模型中不包含可以命名的變量。545 個在 3 種政策下植入的實例測量的是這一後果的大小。它錯誤歸檔 100% 的基礎設施實例，並向代理人收取 $114,383.40。修復其所命名的內容回收 0.0% 的可用損失；修復一個最小充分集回收 100.0%。逐一評分消息不僅僅是不精確的：27.8%（95% CI：23.3--32.3%）的實例無法加法分解。我們評估確定性程序政策而非語言模型代理，這使得重播變得精確，並限制外部有效性於隨機代理。這些流行率數據是這個生成器的特性，而不是現場比率。

##### **Prediction Is Not Detection: Evaluating Pre-Recognition Claims in Longitudinal Clinical AI**
2609.25852v1 by Jing Yang, Long R. Jiao, Xiujun Cai, Zongjiu Zhang

Clinically useful early detection requires validated pre-recognition lead time. Yet event-based evaluations of longitudinal clinical AI can treat recognition-mediated care-process signals as shortcuts and recognition-dependent endpoints as reference standards, inflating apparent performance and lead time while undermining cross-center transport. Such results may serve prognosis without establishing detection before recognition. We define an interval-censored pre-recognition transition, an independent as-of reference standard, and a prespecified recognition proxy to make the claim testable.

摘要：臨床上有用的早期檢測需要經過驗證的預認識提前時間。<lb/>然而，基於事件的長期臨床人工智慧評估可以將認識介導的護理過程信號視為捷徑，並將依賴認識的終點視為參考標準，這樣會膨脹表面性能和提前時間，同時削弱跨中心的轉運。<lb/>這樣的結果可能有助於預後，但並未在認識之前建立檢測。<lb/>我們定義了一個區間右截斷的預認識轉變，一個獨立的即時參考標準，以及一個預先指定的認識代理，以使該主張可測試。

##### **MorphoSHAP: Rethinking the Unit of Attribution in Explanation for Deep Visual Models**
2609.25815v1 by Anirudh Prabhakaran, Alexandre Rocchi, Gianni Franchi

Visual attribution methods typically explain predictions using pixels, superpixels, or regular patches. These representations can localize important regions, but provide limited information about their structure. We introduce MorphoSHAP, a model-agnostic post-hoc method that instead uses morphological shapes as the players of a Shapley attribution game. Using the Tree of Shapes, each shape is described by its scale, geometry, and signed contribution, providing explanations of where the evidence lies, what type of structure carries it, and how strongly it affects the prediction. This shared morphological vocabulary enables spatial, textual, and global class-level explanations beyond image-specific heatmaps. To the best of our knowledge, MorphoSHAP is the first SHAP-based image attribution framework to combine these different forms of explanation. Across five diverse datasets and three architectures, MorphoSHAP achieves strong insertion/deletion performance and outperforms competing attribution methods on several benchmarks. Finally, a user study shows that MorphoSHAP provides explanations that are easy to use and are preferred over standard attribution baselines.

摘要：視覺歸因方法通常使用像素、超像素或常規區塊來解釋預測。這些表示可以定位重要區域，但對其結構提供的資訊有限。我們介紹了 MorphoSHAP，一種與模型無關的事後方法，該方法使用形態學形狀作為 Shapley 歸因遊戲的參與者。利用形狀樹，每個形狀都由其尺度、幾何形狀和簽名貢獻來描述，提供了證據所在的位置、承載它的結構類型以及它對預測影響的強度的解釋。這種共享的形態學詞彙使得超越圖像特定熱圖的空間、文本和全局類別級解釋成為可能。據我們所知，MorphoSHAP 是第一個基於 SHAP 的圖像歸因框架，結合了這些不同形式的解釋。在五個多樣的數據集和三種架構中，MorphoSHAP 實現了強大的插入/刪除性能，並在幾個基準測試中超越了競爭的歸因方法。最後，一項用戶研究顯示，MorphoSHAP 提供的解釋易於使用，並且比標準歸因基準更受偏愛。

##### **LingLan: An Advancing Traditional Chinese Medicine Diagnosis LLM with Multimodal Data**
2609.25715v1 by Zheng Chen, Zhicheng Du, Haoxuan Li, Yingshan Liang, Peiwu Qin

Though artificial intelligence (AI) increasingly transforms modern medicine, its integration into Traditional Chinese Medicine (TCM) has been relatively slow, primarily due to TCM's reliance on holistic, subjective diagnostic methods---namely Inspection, Auscultation and Olfaction, Inquiry, and Palpation(I-AOI-P)---which are difficult to align with quantitative, standardized medical systems. In this work, we introduce a Unification Framework for Multimodal Data (UFMD), which automatically processes tongue and pulse images into structured, clinically standard descriptions, integrating multi-source diagnostic information into a unified digital record of I-AOI-P process. Building on this structured data, we create LingLan-14B, a TCM-specific large language model fine-tuned via supervised learning to emulate the diagnostic logic and workflow of I-AOI-P process. Experimental results show that our method significantly enhances diagnostic accuracy, achieving a relative improvement of 103.5% over the baseline (62.72% vs. 30.82%) and reaching an F1-score of up to 82%.

摘要：儘管人工智慧（AI）日益改變現代醫學，但其在傳統中醫（TCM）中的整合相對緩慢，主要是因為中醫依賴於整體的、主觀的診斷方法——即觀察、聽診與嗅診、詢問和觸診（I-AOI-P）——這些方法難以與定量、標準化的醫療系統對齊。在這項工作中，我們介紹了一個多模態數據統一框架（UFMD），該框架自動將舌頭和脈搏圖像處理成結構化的臨床標準描述，將多來源的診斷信息整合到I-AOI-P過程的統一數字記錄中。在這些結構化數據的基礎上，我們創建了LingLan-14B，這是一個針對中醫的特定大型語言模型，通過監督學習進行微調，以模擬I-AOI-P過程的診斷邏輯和工作流程。實驗結果顯示，我們的方法顯著提高了診斷準確性，相較於基準提高了103.5%（62.72% vs. 30.82%），F1-score達到82%。

##### **Interpretable AI plus Handheld, Portable Retinal Photographs: A Low-Cost Glaucoma Screening Solution for West Africa**
2609.25697v1 by Charis Y. N. Chiang, Tarela Sarimiye, Adeyinka Ashaye, Martin Buist, Michael A. Hauser, Olusola Olawoye, Michaël J. A. Girard

Purpose: To develop and evaluate an interpretable artificial intelligence (AI) framework for glaucoma screening from low-cost portable, handheld retinal fundus photographs in a West African population and to compare its performance with clinical tabletop fundus imaging. Methods: We used data from a community-based study of 681 participants (1,362 eyes) in Nigeria, comprising 414 glaucoma, 478 glaucoma suspect, and 470 non-glaucoma eyes. Fundus photographs were acquired using the low-cost handheld, portable Volk Viva retinal camera and the Canon CR-2-AF tabletop camera. We fine-tuned component models separately to each device to perform vessel segmentation, cup and disc boundary segmentation, and feature extraction to detect optic nerve head features. A final classification model combined these components to classify scans as glaucoma, glaucoma suspect or non-glaucoma. Feature-weight analysis and Gradient-weighted Class Activation Mapping were used for interpretation. Results: The models performed well on both Volk Viva and Canon CR-2-AF images: Vessel segmentation: 0.98 Dice Coefficient (DC) (Volk) and 0.94 DC (Canon); Cup and disc segmentation: 0.95 DC (Volk) and 0.96 DC (Canon); Optic nerve head feature detection: area under the receiver operating characteristic curve (AUCs) of 0.83$\pm$0.03 (Volk) and 0.87$\pm$0.04 (Canon); Classification model: AUCs of 0.85$\pm$0.01 (Volk) and 0.93$\pm$0.01 (Canon). Reports for each image, present model decision confidence scores and decision-rationale visualizations to support clinical interpretation. Conclusions: Volk Viva results were reasonably comparable to Canon CR-2-AF in the component models and not far behind in classification. This shows that interpretable AI combined with low-cost, portable imaging may enhance community-level glaucoma screening, especially in settings with limited specialist access and resources.

摘要：目的：開發和評估一個可解釋的人工智慧（AI）框架，用於從低成本的便攜式手持視網膜眼底照片中進行青光眼篩查，針對西非地區的人群，並將其性能與臨床桌面眼底成像進行比較。<lb/>方法：我們使用了來自尼日利亞681名參與者（1,362隻眼睛）的社區基礎研究數據，包括414隻青光眼、478隻青光眼可疑眼和470隻非青光眼眼睛。<lb/>眼底照片是使用低成本的手持便攜式Volk Viva視網膜相機和Canon CR-2-AF桌面相機獲取的。<lb/>我們分別對每個設備的組件模型進行微調，以執行血管分割、杯盤邊界分割和特徵提取，以檢測視神經頭特徵。<lb/>最終分類模型將這些組件結合起來，將掃描結果分類為青光眼、青光眼可疑或非青光眼。<lb/>特徵權重分析和梯度加權類別激活映射被用於解釋。<lb/>結果：這些模型在Volk Viva和Canon CR-2-AF影像上表現良好：血管分割：0.98 Dice Coefficient (DC)（Volk）和0.94 DC（Canon）；杯盤分割：0.95 DC（Volk）和0.96 DC（Canon）；視神經頭特徵檢測：受試者工作特徵曲線下面積（AUCs）為0.83$\pm$0.03（Volk）和0.87$\pm$0.04（Canon）；分類模型：AUCs為0.85$\pm$0.01（Volk）和0.93$\pm$0.01（Canon）。<lb/>每張影像的報告中，呈現模型決策信心分數和決策理由可視化，以支持臨床解釋。<lb/>結論：Volk Viva的結果在組件模型中與Canon CR-2-AF相當可比，且在分類上也不遜色。<lb/>這表明，可解釋的AI結合低成本的便攜式成像可能會增強社區層級的青光眼篩查，特別是在專家訪問和資源有限的環境中。

##### **ShowTellArena: Evaluating Business Workflow Understanding from Demonstrations**
2609.25467v1 by David Garg, Ritobrata Sarkar, Ehsan Azarnasab, Siddhartha Borah

We often teach a colleague by showing the work and explaining the decisions as we go. How can we check what an agent understood from the same lesson? We introduce ShowTellArena, a benchmark protocol and public dataset for comprehension after narrated business demonstrations. The v1.0 release contains 50 business workflow tasks, with recordings, screenshots, narration, fixture seeds, and 502 questions. Tasks span finance, hiring, procurement, customer decisions, inventory, and logistics. The protocol holds the business scenario and quiz fixed while allowing each product to capture the lesson through its own teaching interface. Questions test operational rules, boundaries, exceptions, and errors in proposed automations. We analyze 218 selected pilot attempts across 39 workflow cases, including 28 cases attempted by all three evaluated systems. These exploratory results expose both answer errors and failures to complete the teaching experience. We describe the release's verification gaps and the pilot's uneven coverage, exclusions, and grading provenance. The contribution is an inspectable dataset and assessment workflow that others can extend; the selected pilot is not a controlled product ranking.

摘要：我們經常通過展示工作並在過程中解釋決策來教導同事。<lb/>我們如何檢查代理從同一課程中理解了什麼？<lb/>我們介紹了ShowTellArena，一個基準協議和公共數據集，用於在敘述商業演示後的理解測試。<lb/>v1.0版本包含50個商業工作流程任務，包含錄音、截圖、敘述、固定種子和502個問題。<lb/>任務涵蓋財務、招聘、採購、客戶決策、庫存和物流。<lb/>該協議保持商業場景和測驗不變，同時允許每個產品通過自己的教學界面捕捉課程。<lb/>問題測試提議自動化中的操作規則、邊界、例外和錯誤。<lb/>我們分析了39個工作流程案例中218個選定的試點嘗試，包括所有三個評估系統都嘗試的28個案例。<lb/>這些探索性結果揭示了答案錯誤和未能完成教學體驗的情況。<lb/>我們描述了版本的驗證差距以及試點的不均勻覆蓋、排除和評分來源。<lb/>這一貢獻是一個可檢查的數據集和評估工作流程，其他人可以擴展；所選的試點不是一個受控的產品排名。

##### **Passes Alone, Fails Together: Benchmarking Semantic Coordination in Parallel LLM-Agent Development**
2609.25396v1 by Haocheng Xia, Eugene Wu, Yongjoo Park

Parallel coding agents can produce patches that work alone but fail when merged. This happens when one agent changes an interface or rule that another agent still relies on. We study these failures with stale, a benchmark for semantic coordination. Our evaluation runs the same tests on each patch alone and on their combination, counting only failures introduced by combining the patches. We use three tiers: synthetic tasks with controlled interface changes, pairs of merged pull requests, and constructed tasks that use real Django helpers. Among 834 runs on 417 mined Django pairs, only one showed interference after correcting the grading procedure. On constructed tasks using 12 Django helpers, interference occurred in 97% of runs. A message describing the completed concurrent change recovered 82% of runs. Reviewed pull requests may contain few unresolved parallel changes, even when agents fail on controlled tasks using real code. The constructed failure rates do not estimate how often these problems occur in practice.

摘要：平行編碼代理可以產生獨立運作但合併後失敗的補丁。當一個代理改變了另一個代理仍然依賴的介面或規則時，就會發生這種情況。我們使用 stale 這個語義協調的基準來研究這些失敗。我們的評估對每個補丁單獨和它們的組合進行相同的測試，只計算由合併補丁引入的失敗。我們使用三個層級：具有控制介面變更的合成任務、合併的拉取請求對，以及使用真實 Django 幫助器的構建任務。在對 417 個挖掘的 Django 對進行的 834 次運行中，只有一次在修正評分程序後顯示出干擾。在使用 12 個 Django 幫助器的構建任務中，97% 的運行出現了干擾。一條描述已完成的並行變更的消息恢復了 82% 的運行。即使在使用真實代碼的控制任務中，審查的拉取請求也可能包含少量未解決的平行變更。構建的失敗率並不能估計這些問題在實踐中發生的頻率。

##### **From Decorative to Load-Bearing: Task Difficulty Shapes the Causal Role of Chain-of-Thought**
2609.25366v1 by Renee Jia, Di Mu

Chain-of-thought (CoT) monitoring is only meaningful if written reasoning causally constrains the answer. We introduce continuation-based causal testing, an ablation-patch intervention that perturbs one reasoning step, truncates the chain, and forces the model to continue from the corrupted prefix. It measures how load-bearing a CoT is for the final answer, a behavioral notion distinct from mechanistic faithfulness. Across Gemma-2-9B-IT, Llama-3.1-8B-Instruct, and DeepSeek-R1-Distill-Qwen-7B on GSM8K, MMLU, and BIG-Bench Hard, CoT load-bearingness tracks model-relative task difficulty: on easy tasks models silently bypass their own reasoning; on hard tasks they follow corrupted steps and propagate errors. A matched 2x2 analysis shows task difficulty dominates perturbation type: error propagation rises 16x from GSM8K to BBH multistep arithmetic, and a variance partition over 28,584 continuations attributes 98.8% of explained deviance to task difficulty versus 0.8% to perturbation type. Reasoning-specific RL suppresses error propagation and compresses the gradient. A four-variant judge-sensitivity analysis and blind two-annotator study (n=500) show the error-propagation vs. non-propagation label is invariant to judge prompt, with perfect inter-annotator agreement (Cohen's kappa = 1.00). This gradient creates a structural problem for CoT-based oversight and AI safety monitoring: where the trace is easy to read it carries little signal, and where it matters errors propagate before a monitor can intervene. Linear probes on hidden states separate silent bypass, self-correction, and error propagation, but additive activation steering provides limited causal control, flipping only about 25% of error-propagation cases at best. Behavioral mode is readable but not reliably controllable.

摘要：Chain-of-thought (CoT) 監控只有在書面推理對答案有因果約束時才有意義。<lb/>我們引入基於延續的因果測試，一種切除干預，擾動一個推理步驟，截斷鏈條，並強迫模型從損壞的前綴繼續。<lb/>它衡量 CoT 對最終答案的承載能力，這是一種與機械忠實性不同的行為概念。<lb/>在 Gemma-2-9B-IT、Llama-3.1-8B-Instruct 和 DeepSeek-R1-Distill-Qwen-7B 在 GSM8K、MMLU 和 BIG-Bench Hard 上的實驗中，CoT 的承載能力與模型相對的任務難度相關：在簡單任務上，模型靜默地繞過自己的推理；在困難任務上，它們遵循損壞的步驟並傳播錯誤。<lb/>一項匹配的 2x2 分析顯示任務難度主導了擾動類型：從 GSM8K 到 BBH 多步算術，錯誤傳播上升了 16 倍，對 28,584 次延續的方差分解將 98.8% 的解釋偏差歸因於任務難度，而 0.8% 歸因於擾動類型。<lb/>針對推理的強化學習抑制了錯誤傳播並壓縮了梯度。<lb/>一項四變量的評審敏感性分析和盲目的雙標註者研究（n=500）顯示，錯誤傳播與非傳播標籤對評審提示不變，且標註者之間的協議完美（Cohen's kappa = 1.00）。<lb/>這一梯度為基於 CoT 的監督和 AI 安全監控創造了一個結構性問題：當痕跡容易閱讀時，它攜帶的信號很少，而在重要的地方，錯誤在監控者介入之前就已經傳播。<lb/>對隱藏狀態的線性探測區分了靜默繞過、自我修正和錯誤傳播，但加性激活引導提供的因果控制有限，最多僅能翻轉約 25% 的錯誤傳播案例。<lb/>行為模式可讀但不可靠可控。

##### **Exactness at Inference: A Representational Criterion for Out-of-Distribution Generalization**
2609.24942v1 by Filipe Marinho Rocha, Inês Dutra, Vítor Santos Costa, Luís Paulo Reis

A model generalizes outside its training distribution only when it computes a representation structurally equivalent to the generating mechanism, not an approximation fitted to it. Such equivalence is necessary for exactness in and out of distribution, and extrapolation is governed by this exactness at inference, whatever its realization. Tensor Logic shows this: a zero-temperature contraction is equivalent to discrete logic, deducing in place with no artefact extracted, its tensors Boolean, its embeddings orthonormal, only its arithmetic continuous. Lacking infinite recursion it reaches Datalog, not Prolog, and though exact over closed domains it needs external memory to bind a novel entity. The criterion needs neither a discrete representation nor an extracted expression, and constrains inference, not training: an exact marginal in $[0,1]$ passes, a Neural Network thresholded to a hard label does not. Logic Tensor Networks fail it, while differentiable ILP and Tensor Logic at $T=0$ pass. Piecewise-affine extrapolation divergence and an inability to bind novel entities are two faces of a shortfall in exact representability. For hybrid architectures, a propagation rule follows: the output inherits the bounds of every fitted estimator on its path, explaining which axes fail in equivariant models and the ARC-AGI induction/transduction split. Only an exact hypothesis class certifies what the training data leave underdetermined: on a law-derived partition it finds the $56.3\%$ of distant queries that are answerable, which ensembles meet with false confidence and distance metrics rank backwards. Common inductive biases, from symmetries to memory, reach exactness only because humans inject them, an argument for inducing exact representations rather than fitting surrogates whose residuals, even at the arithmetic floor in training, diverge outside the data and compound under composition.

摘要：模型只有在計算出與生成機制結構上等價的表示時，才能在其訓練分佈之外進行泛化，而不是對其進行擬合的近似。這種等價性對於分佈內外的精確性是必要的，而外推則受此精確性在推理中的支配，無論其實現為何。張量邏輯顯示了這一點：零溫度收縮等價於離散邏輯，無需提取任何工件即可在當地推導，其張量為布爾型，其嵌入為正交正規，只有其算術為連續的。缺乏無限遞歸，它達到的是Datalog，而不是Prolog，儘管在封閉域上是精確的，但它需要外部記憶來綁定新實體。該標準既不需要離散表示，也不需要提取表達，並限制推理，而不是訓練：在$[0,1]$中的精確邊際通過，而閾值為硬標籤的神經網絡則不通過。邏輯張量網絡未能滿足該標準，而可微分的ILP和在$T=0$的張量邏輯則通過。分段仿射外推的發散和無法綁定新實體是精確可表示性不足的兩個方面。對於混合架構，傳播規則遵循：輸出繼承其路徑上每個擬合估計器的界限，解釋哪些軸在等變模型中失敗以及ARC-AGI的歸納/轉導分裂。只有精確的假設類才能證明訓練數據留下的未確定性：在基於法律的劃分上，它找到了$56.3\%$的可回答遠程查詢，而這些集合以虛假的信心相遇，距離度量則向後排名。常見的歸納偏見，從對稱性到記憶，之所以達到精確性，僅僅是因為人類注入了它們，這是一個支持誘導精確表示的論點，而不是擬合殘差的替代品，這些替代品即使在訓練中的算術底線上，也會在數據之外發散並在組合下加劇。

##### **Et Tu, Brute? Economic Misalignment in Personal AI Agents**
2609.24927v1 by Aman Priyanshu, Supriti Vijay, Brian Jabarian, Niloofar Mireshghallah

Personal AI agents make recommendations and take actions on people's behalf in high-stakes economic contexts, e.g., buying a flight, choosing health insurance, or selecting a graduate program. The agent is given access to the user's personal context, e.g., their email inbox and a structured profile of personal attributes, with the intention of making an optimal, personalized decision for the user. We show that by simply providing this personal context, the agent steers recommendations based on inferred wealth, without being explicitly instructed to do so. In a suite of 325K experiments on 13 agents across three types of economic decisions (flights, health insurance, and graduate programs), we find that 8 models systematically choose more expensive options for wealthier users when requests are identical. This steering continues even when it directly goes against the user's stated objective: when explicitly instructed to find the cheapest option, some agents still act on the wealth profile they have inferred. It also occurs when wealth is inferred from ambient data, such as emails unrelated to the task. And it persists under privacy controls that block specific attributes: blocking financial attributes largely removes the disparity, but blocking other attributes leaves it unchanged and can increase it by up to 40% for insurance, as agents rely on the remaining signals to infer wealth. Larger and more capable models are no better; Claude Opus 4.8 shows the largest effect. We term this misalignment "adversarial delegation", in which the very conditions that make a personal AI agent useful - access to personal information - enable it to act against the user's interests.

摘要：個人 AI 代理在高風險的經濟情境中代表人們做出建議和行動，例如購買航班、選擇健康保險或選擇研究生課程。該代理獲得用戶的個人背景資訊，例如他們的電子郵件收件箱和結構化的個人屬性檔案，旨在為用戶做出最佳的個性化決策。我們顯示，僅僅提供這些個人背景資訊，代理就能根據推斷出的財富來引導建議，而不需要明確指示這樣做。在對 13 個代理進行的 325K 實驗中，涵蓋三種類型的經濟決策（航班、健康保險和研究生課程），我們發現 8 個模型在請求相同的情況下，系統性地為較富裕的用戶選擇更昂貴的選項。這種引導即使在直接違反用戶明確目標的情況下仍然持續存在：當明確指示尋找最便宜的選項時，一些代理仍然根據他們推斷出的財富檔案行動。當財富是從與任務無關的環境數據（例如電子郵件）推斷時，這種情況也會發生。而且在阻止特定屬性的隱私控制下也會持續存在：阻止財務屬性在很大程度上消除了差異，但阻止其他屬性則保持不變，並且對於保險，可能會增加高達 40% 的差異，因為代理依賴剩餘的信號來推斷財富。更大且更強大的模型並沒有更好；Claude Opus 4.8 顯示出最大的影響。我們將這種不對齊稱為「對抗性委託」，在這種情況下，使個人 AI 代理有用的條件——訪問個人信息——使其能夠反對用戶的利益行動。

##### **From Pattern Recognizers to Personalized Companions: A Survey of Large Language Models in Mental Health**
2609.25186v1 by He Hu, Yucheng Zhou, Qianning Wang, Yingjian Zou, Chiyuan Ma, Juzheng Si, Jianzhuang Liu, Zitong Yu, Laizhong Cui, Fei Ma, Qi Tian

The rising global prevalence of mental health conditions, together with longstanding barriers in traditional healthcare, such as limited resources, high cost, stigma, and privacy concerns, has created an urgent need for accessible and scalable support. Large Language Models (LLMs) have emerged as a transformative technology with strong potential to democratize mental health support through advanced natural language understanding and generation. However, the rapidly expanding, fragmented body of work in this area lacks a coherent evolutionary narrative, making it difficult to contextualize current progress and identify future directions. This survey addresses this gap by organizing and analyzing the literature around a central thesis: the role of LLMs in mental health is evolving through three distinct, increasingly sophisticated phases. We trace this trajectory from Phase I, in which LLMs act primarily as passive Information Tools and Pattern Recognizers for assessment; through Phase II, where they function as Empathetic Conversationalists for in-the-moment, stateless interactions; to the current frontier, Phase III, which seeks Longitudinal, Personalized Companions implemented as stateful cognitive agents. To support this framework, we systematically review core technologies, agent architectures (Profile, Memory, Reasoning, and Planning), and the critical infrastructure of datasets and benchmarks, highlighting how their evolution underpins this developmental path. Viewing the field through this developmental lens, we provide a comprehensive synthesis of existing work, an insightful narrative of its trajectory, and a clear roadmap for future innovation in responsible, effective, and human-centered AI for mental healthcare. A curated collection of the resources reviewed in this survey is available at our project repository: https://github.com/Emo-gml/Awesome-Mental-Health-LLMs.

摘要：全球心理健康狀況的上升流行率，加上傳統醫療中長期存在的障礙，如資源有限、高成本、污名化和隱私問題，創造了對可獲得和可擴展支持的迫切需求。大型語言模型（LLMs）已經成為一種變革性技術，具有通過先進的自然語言理解和生成來民主化心理健康支持的強大潛力。然而，這一領域快速擴展且支離破碎的研究體系缺乏一致的演變敘事，這使得難以將當前的進展置於上下文中並確定未來的方向。本調查通過圍繞一個中心論題組織和分析文獻，填補了這一空白：LLMs在心理健康中的角色正通過三個明確且日益精緻的階段演變。我們追溯這一軌跡，從第一階段，在這一階段中，LLMs主要作為被動的信息工具和模式識別器進行評估；到第二階段，它們作為同理心的對話者進行即時的無狀態互動；再到當前的前沿，第三階段，尋求作為有狀態的認知代理實現的縱向個性化伴侶。為了支持這一框架，我們系統性地回顧了核心技術、代理架構（配置、記憶、推理和規劃）以及數據集和基準的關鍵基礎設施，突顯了它們的演變如何支撐這一發展路徑。通過這一發展視角來看待該領域，我們提供了現有工作的全面綜合、其軌跡的深刻敘事，以及未來在負責任、有效和以人為中心的心理健康護理AI中的創新清晰路線圖。本調查中回顧的資源的策劃集合可在我們的項目庫中獲得：https://github.com/Emo-gml/Awesome-Mental-Health-LLMs。

##### **Decoding Guardrails: XAI-Guided Perturbation Analysis of Prompt Injection Detection**
2609.24801v1 by Fernando Outeda, Gustavo Betarte, Juan Diego Campo, Fiorella Cravero

Large language models (LLMs) are increasingly deployed in production systems, raising concerns about their exposure to adversarial manipulation through prompt injection and jailbreak attacks. Classifier-based guardrails, such as Prompt Guard 2, are widely used as a first line of defense against such attacks, but their internal decision logic is largely opaque to both defenders and attackers. This paper presents an exploratory case study that applies explainable artificial intelligence (XAI) techniques to analyze how Prompt Guard 2 distinguishes malicious from benign prompts.
  We conduct four experiments to probe this question empirically. Guided by Vanilla Gradient and SHAP attributions, we find that Prompt Guard 2's decisions rely on the cumulative contribution of many tokens rather than a few dominant ones, yet saliency-guided synonym substitution and sentence-level paraphrasing can flip its predictions while altering only a moderate fraction of the text, in some cases yielding a successful jailbreak against the underlying LLM. A dataset-scale saliency analysis further shows that undetected injection prompts systematically lack the lexical markers the classifier relies on. We discuss the implications of these findings for the design and evaluation of classifier-based guardrails, and argue that explanation methods intended to support transparency can simultaneously lower the cost of constructing successful adversarial bypasses.

摘要：大型語言模型（LLMs）越來越多地應用於生產系統中，這引發了對它們在提示注入和越獄攻擊中遭受對抗性操控的擔憂。基於分類器的防護措施，如 Prompt Guard 2，被廣泛用作對抗這類攻擊的第一道防線，但其內部決策邏輯對於防禦者和攻擊者來說都在很大程度上是不透明的。本文提出了一個探索性案例研究，應用可解釋的人工智慧（XAI）技術來分析 Prompt Guard 2 如何區分惡意提示和良性提示。<lb/>我們進行了四個實驗來實證探討這個問題。在 Vanilla Gradient 和 SHAP 重要性指標的指導下，我們發現 Prompt Guard 2 的決策依賴於許多標記的累積貢獻，而不是少數主導的標記，然而，基於重要性引導的同義詞替換和句子級別的改寫可以在僅改變文本的一小部分的情況下顛覆其預測，在某些情況下成功實現對基礎 LLM 的越獄。數據集規模的顯著性分析進一步顯示，未被檢測的注入提示系統性地缺乏分類器所依賴的詞彙標記。我們討論了這些發現對基於分類器的防護措施設計和評估的影響，並認為旨在支持透明度的解釋方法可以同時降低構建成功對抗性繞過的成本。

##### **DeceptionAnalyser: A Web-Based AI Tool for Performing Structured Deception Analysis with Argumentation Schemes and LLMs**
2609.24369v1 by Stefan Sarkadi, Xabier Garmendia, Jack Mumford, Trevor Bench-Capon

Deception plays a central role in Intelligence operations, yet it remains difficult to analyse systematically without expert knowledge of reasoning patterns and cognitive manipulation. In computational argumentation, for instance, no scheme-level ground-truth corpora currently exist to support statistical validation. In this paper, we address this by introducing a set of ten argument schemes designed to model distinct forms of deception, each accompanied by structured premises and critical questions. In doing so, we introduce the first dedicated library of argumentation schemes specifically designed for deception analysis, providing a structured foundation for systematically modelling and analysing deception in narrative text. We then present \textit{DeceptionAnalyser}, a browser-based tool that implements these schemes through a two-stage methodology combining LLM-based premise extraction with critical-question-driven evaluation. Our aim is to provide a conceptual and methodological foundation for analysing deceptive reasoning in narrative text. This is precisely what we address in this paper by demonstrating how structured argumentation theory and AI-assisted analysis can support transparent, explainable assessments of potential deception. Because the schemes are designed to flag claims for scrutiny rather than to output a deception verdict, we do not benchmark classification accuracy; instead, we assess the \emph{reliability} of the methodology by measuring the consistency of the tool's premise and conclusion assessments across ten contemporary large language models and repeated runs. We find that scheme detection is highly stable for clear-cut deception and degrades gracefully, in interpretable ways, on more ambiguous intelligence-style narratives.

摘要：欺騙在情報行動中扮演著核心角色，然而在沒有專業的推理模式和認知操控知識的情況下，系統性分析仍然困難。以計算論證為例，目前並不存在任何方案級別的真實語料庫來支持統計驗證。在本文中，我們通過引入一組十個論證方案來解決這一問題，這些方案旨在模擬不同形式的欺騙，每個方案都附有結構化的前提和關鍵問題。這樣做的同時，我們推出了首個專門為欺騙分析設計的論證方案庫，為系統性建模和分析敘事文本中的欺騙提供了結構化的基礎。接著，我們介紹了 \textit{DeceptionAnalyser}，這是一個基於瀏覽器的工具，通過結合基於大型語言模型的前提提取和關鍵問題驅動的評估的兩階段方法實現這些方案。我們的目標是為分析敘事文本中的欺騙推理提供概念和方法論基礎。正是通過展示結構化論證理論和AI輔助分析如何支持潛在欺騙的透明、可解釋評估，我們在本文中解決了這一問題。由於這些方案旨在標記需要審查的主張，而不是輸出欺騙判決，因此我們不對分類準確性進行基準測試；相反，我們通過測量工具在十個當代大型語言模型和重複運行中的前提和結論評估的一致性來評估方法論的\emph{可靠性}。我們發現，對於明確的欺騙，方案檢測的穩定性非常高，而在更模糊的情報風格敘事中，則以可解釋的方式優雅地降級。

##### **Temporal Generalization and Explanation Stability of Control Flow Graph Neural Networks for Malware Detection**
2609.24280v1 by Md. Asif Sajeed, Md. Nazrul Islam Mondal, Md Ashraful Hossen Akash

Malware detection is a critical task in cybersecurity, and graph neural networks over control flow graphs have shown promising results for it. However, detectors are usually evaluated on a random split of a corpus collected over a single period, which cannot show how well a model generalizes to later samples. This study addresses that limitation with a strict temporal split: every model is trained on one period and scored once on a later one. Two corpora of control flow graphs, each node carrying 37 features, were extracted statically from 1,989 Windows portable executables: 459 graphs from 2024-2025 for training and 223 from 2026 for evaluation. Twelve variants and a flat-feature control were trained on the earlier corpus. The choice of message-passing operator changes robustness to the shift significantly, and every pairwise gap that survives correction separates an aggregating architecture from one built around a learned attentional readout. The ranking also reverses: the flat control, which sees node features but no topology, is the best in-distribution model and among the worst across the boundary, so a conventional benchmark would have rejected message passing. Neither recalibration nor ensembling substitutes for the operator choice. Attributions do not shift, but explanation validity is architecture-specific, and the most accurate operator on the later corpus is the hardest to explain. An architecture derived from the finding matches the best searched operator without search. The shift affects both malware and benign classes alike, so these are results about robustness to distribution shift, not malware evolution.

摘要：惡意軟體檢測是網路安全中的一項關鍵任務，而基於控制流圖的圖神經網路在這方面顯示出了良好的結果。<lb/>然而，檢測器通常是在一個單一時期收集的語料庫的隨機劃分上進行評估，這無法顯示模型對後續樣本的泛化能力。<lb/>本研究通過嚴格的時間劃分來解決這一限制：每個模型在一個時期內訓練，並在後續時期進行一次評分。<lb/>從1,989個Windows可攜式執行檔中靜態提取了兩個控制流圖語料庫，每個節點攜帶37個特徵：2024-2025年的459個圖用於訓練，2026年的223個圖用於評估。<lb/>在早期語料庫上訓練了十二個變體和一個平面特徵控制。<lb/>消息傳遞運算符的選擇顯著改變了對變化的穩健性，並且每一對經過修正的差距將聚合架構與基於學習的注意力讀取架構分開。<lb/>排名也發生了逆轉：平面控制雖然能看到節點特徵但無法看到拓撲，是最佳的內部分佈模型，但在邊界上卻是最差的，因此傳統基準會拒絕消息傳遞。<lb/>無論是重新校準還是集成都無法替代運算符的選擇。<lb/>歸因不會改變，但解釋的有效性是特定於架構的，而在後期語料庫上最準確的運算符是最難以解釋的。<lb/>從該發現衍生的架構與最佳搜索運算符匹配，無需搜索。<lb/>這一變化同樣影響惡意軟體和良性類別，因此這些結果關於對分佈變化的穩健性，而非惡意軟體的演變。

##### **ACLArena: Agent Continue Learning in Multi-stage Post-training**
2609.23989v1 by Haixin Wang, Xiaoxuan Wang, Junkai Zhang, Han Zhang, Renliang Sun, Alexander K Taylor, Yidan Shi, Haoran Deng, Chenguang Wang, Jason Cong, Yizhou Sun, Wei Wang

Building general-purpose agents for industrial deployment requires integrating multiple capabilities, each typically acquired at a distinct stage of training. Yet there is currently no well-established recipe for Agent Continual Learning (ACL), with little understanding of the trade-offs among existing integration paradigms. To address this gap, we introduce ACLArena, a framework for comprehensively studying, analyzing, and evaluating ACL. We first build a sequential training pipeline and conduct an in-depth analysis that explains the mechanisms of forgetting and generalization from two complementary perspectives, the model level and the token level. Guided by these analyses, we systematically compare multi-teacher on-policy distillation, self-distilled fine-tuning, and model merging to assess their ability to recover previously learned capabilities while preserving newly acquired ones. Through extensive experiments, we develop a detailed understanding of how capabilities transfer across stages. Finally, we propose a new ACL recipe that combines offline replay over high-quality trajectories with a routed network of multiple LoRA experts each specialized via RL, substantially improving the agent's ability to learn across multiple domains. Comprehensive experiments on four reasoning and agentic tasks, evaluated under both in-domain and out-of-domain settings, demonstrate the value of our analysis and the effectiveness of our approach.

摘要：建立用於工業部署的通用代理需要整合多種能力，而這些能力通常是在訓練的不同階段獲得的。<lb/>然而，目前尚無成熟的代理持續學習（ACL）方案，對於現有整合範式之間的權衡也缺乏了解。<lb/>為了解決這一空白，我們引入了ACLArena，一個用於全面研究、分析和評估ACL的框架。<lb/>我們首先建立了一個順序訓練管道，並進行深入分析，從模型層面和標記層面兩個互補的角度解釋遺忘和泛化的機制。<lb/>在這些分析的指導下，我們系統地比較了多教師在政策蒸餾、自我蒸餾微調和模型合併，以評估它們在保持新獲得能力的同時恢復先前學習能力的能力。<lb/>通過大量實驗，我們詳細了解了能力如何在不同階段之間轉移。<lb/>最後，我們提出了一種新的ACL方案，結合了對高質量軌跡的離線重播和通過強化學習專門化的多個LoRA專家的路由網絡，顯著提高了代理在多個領域學習的能力。<lb/>在四個推理和代理任務上進行的全面實驗，在域內和域外設置下進行評估，展示了我們分析的價值和我們方法的有效性。

##### **UniK: Universal Knowledge Perception for Digital and Physical AI**
2609.23971v1 by Nirmit Desai, Kunal Sawarkar, Aditya Mahakali, Dongkon Lee, Kevin Park, Eric Song

Two transformative classes of AI systems are reshaping how organizations operate: \textit{digital AI}, which reasons over enterprise knowledge to power chatbots and agent workflows; and \textit{physical AI}, which learns to control robots and autonomous systems from video, gameplay, and sensor telemetry. Both face the same foundational bottleneck: raw knowledge at scale, spanning heterogeneous modalities, locked in private corpora that existing AI infrastructure cannot access reliably or efficiently. We propose \textit{Universal Knowledge Perception (UniK)} as a common platform for both classes, covering the full knowledge lifecycle (ingestion, enrichment, indexing, retrieval, and continuous evaluation) across modalities from rich text and video to molecular data and sensor telemetry. We present UniK, built on Polymath Retrieval (multi-index fusion over automatically enriched indices) with no task-specific fine-tuning. Across five digital AI domains (medical literature, open-domain QA, chemistry, legal video proceedings, and government open data) UniK combined with an open-source 70-billion-parameter model consistently matches or outperforms frontier proprietary LLMs that are orders of magnitude larger: 76\% RAG accuracy on government data versus 47\% for GPT-5; 77.9\% on medical QA without fine-tuning; topping all open-source chemistry pipelines. We show that the same infrastructure directly addresses the data curation, indexing, and retrieval challenges facing physical AI world model training, where the knowledge problem is harder but structurally identical.

摘要：兩種變革性的人工智慧系統正在重塑組織的運作方式：\textit{數位人工智慧}，它利用企業知識來驅動聊天機器人和代理工作流程；以及\textit{實體人工智慧}，它從視頻、遊戲和傳感器遙測中學習控制機器人和自主系統。這兩者面臨著相同的基礎瓶頸：大規模的原始知識，跨越異質的模態，鎖定在現有人工智慧基礎設施無法可靠或高效訪問的私有語料庫中。我們提出\textit{通用知識感知（UniK）}作為這兩類系統的共同平台，涵蓋整個知識生命周期（攝取、豐富、索引、檢索和持續評估），跨越從豐富文本和視頻到分子數據和傳感器遙測的模態。我們展示了UniK，基於多數據索引融合的Polymath Retrieval，且不需要特定任務的微調。在五個數位人工智慧領域（醫學文獻、開放領域問答、化學、法律視頻程序和政府開放數據）中，UniK結合一個開源的700億參數模型，始終能夠匹配或超越規模大數量級的前沿專有大型語言模型：政府數據的76\% RAG準確率對比GPT-5的47\%；在醫學問答中達到77.9\%的準確率，且無需微調；在所有開源化學管道中名列前茅。我們展示了相同的基礎設施能直接解決實體人工智慧世界模型訓練中面臨的數據策展、索引和檢索挑戰，儘管知識問題更為困難，但結構上是相同的。

##### **Explainable Recommendations at Scale: LLM Rationales for YouTube Music Artist Discovery**
2609.23877v1 by Xiao Liu, Yanwei Song, Srivaths Ranganathan, Yuan Chen, Zheyun Feng, Parker Steenburgh, Jochen Klingenhoefer, Nathan Lasche, Gergo Varady, Tim Steele

Modern music streaming platforms face a persistent tradeoff: exploiting familiar content versus driving the exploration of novel items. While users frequently desire discovery, they hesitate to select unknown artists over proven favorites. Providing transparent, natural language rationales that explain why an unexplored item is recommended lowers this barrier. However, while Large Language Models (LLMs) excel at this nuanced explainability, their real-time deployment is severely bottlenecked by prohibitive inference costs and computational overhead. In this paper, we present an industry case study of a decoupled recommendation architecture that successfully scales exploration without compromising latency. Our system isolates LLM inference asynchronously offline, pre-computing personalized candidate pools of undiscovered artists alongside tailored rationales. Large-scale online A/B experiments validate our design. We demonstrate that combining LLM-backed recommendations with these explanatory rationales significantly reduces the trust barrier for new content, yielding statistically significant improvements in both user exploration and overall engagement on the discovery surfaces.

摘要：現代音樂串流平台面臨著一個持續的權衡：利用熟悉的內容與推動探索新項目之間的平衡。雖然用戶經常渴望發現新事物，但他們卻猶豫不決，選擇未知的藝術家而非已知的喜愛者。提供透明的自然語言理由，解釋為何推薦未探索的項目，可以降低這一障礙。然而，儘管大型語言模型（LLMs）在這種細緻的可解釋性方面表現出色，但其實時部署卻受到高昂推理成本和計算開銷的嚴重瓶頸。在本文中，我們呈現了一個行業案例研究，展示了一種解耦的推薦架構，成功地在不妥協延遲的情況下擴展探索。我們的系統異步離線隔離LLM推理，預先計算個性化的未發現藝術家候選池及其量身定制的理由。大規模的在線A/B實驗驗證了我們的設計。我們展示了結合LLM支持的推薦與這些解釋性理由顯著降低了對新內容的信任障礙，並在用戶探索和整體參與度方面在發現界面上取得了統計上顯著的改善。

##### **What Makes a Terminal-Bench Task Hard? Separating Genuine Hardness from Fake-Hardness on an Adjudicated Agentic Corpus**
2609.26826v1 by Edward Lue Chee Lip, Boden Moraski, Tim Knappe, Lang Xiong, Sarvesh Gharat, Antonio Mari, Ivan Bercovich

Frontier benchmarks need tasks that current models cannot solve. But a task that no model solves is not automatically a hard task. The same zero pass rate can come from a real capability gap, but it can also come from missing context, a broken reference solution, infrastructure failure, or a verifier that can be bypassed. In this paper, we study this issue using a frozen Terminal-Bench 3 / Frontier-Bench 0.1 production record with 1,081 pull requests, 639 scored tasks, 28,801 trials, and $105,933 in logged agent spend. We ask what an all-fail task actually certifies. For the 125 tasks with no honest pass, we combine task artifacts, reference-solution runs, empty-solution controls, adversarial trials, trajectories, telemetry, and review records, and apply an ordered validity screen. Only 78 of the 125 tasks survive as certified-unsolved candidates. The remaining tasks include 14 with broken oracles, 8 dominated by infrastructure failures, 4 that are only passable through verifier bypasses, and 21 whose solvability is not certified by the available evidence. Thus, lack of saturation and genuine difficulty are not the same thing. The certified-unsolved label is also narrow: it means that the authored route passed, infrastructure did not dominate, no strict bypass was observed, and all evaluated agents failed. It does not prove intrinsic hardness, verifier completeness, or failure at the intended capability. We further analyze rejected submissions and passing tasks to show that pass rate alone cannot explain why a task is difficult. Overall, our results suggest that frontier benchmarks should report the evidence behind their all-fail tasks before using them as capability claims.

摘要：前沿基準需要當前模型無法解決的任務。但一個沒有模型解決的任務並不自動意味著這是一個困難的任務。相同的零通過率可能來自真正的能力差距，但也可能來自缺乏上下文、破損的參考解決方案、基礎設施故障，或可以被繞過的驗證器。在本文中，我們使用一個凍結的 Terminal-Bench 3 / Frontier-Bench 0.1 生產記錄進行研究，該記錄包含 1,081 個拉取請求、639 個得分任務、28,801 次試驗，和 $105,933 的記錄代理支出。我們詢問一個全失敗任務實際上認證了什麼。對於 125 個沒有誠實通過的任務，我們結合任務文檔、參考解決方案執行、空解決方案控制、對抗性試驗、軌跡、遙測和審查記錄，並應用有序有效性篩選。只有 125 個任務中的 78 個作為認證未解決候選者存活下來。剩餘的任務包括 14 個具有破損預言機、8 個被基礎設施故障主導、4 個僅能通過驗證器繞過而通過的，以及 21 個其可解性未被現有證據認證的。因此，缺乏飽和度和真正的困難並不是同一回事。認證未解決標籤也很狹窄：這意味著所創建的路徑通過，基礎設施沒有主導，沒有觀察到嚴格的繞過，並且所有評估的代理均失敗。這並不能證明內在的困難性、驗證器的完整性，或在預期能力上的失敗。我們進一步分析被拒絕的提交和通過的任務，以顯示通過率本身無法解釋為什麼一個任務是困難的。總體而言，我們的結果表明，前沿基準應在將其全失敗任務用作能力聲明之前報告其背後的證據。

##### **Predicting Out-of-Distribution Generalization of Neural Operators via Observable Spectral Error Decomposition**
2609.23529v1 by Hang-Cheng Dong, Pengcheng Cheng

Neural operators have emerged as powerful surrogates for solving partial differential equations (PDEs), yet their reliability under distribution shift remains a critical barrier to deployment. Existing approaches to out-of-distribution (OOD) generalization in operator learning are largely empirical and black-box: they report aggregate error metrics without explaining why errors arise or when they will grow. We propose a structure-preserving framework that makes OOD generalization predictable and auditable. Our key idea is to parameterize the learned solution operator as a spectral filter $h_θ(λ)$ acting on the eigenvalues of the underlying elliptic operator, implemented via Chebyshev polynomial expansions and trained with a weak-form objective. This parameterization admits an exact decomposition of the energy-norm error into two observable components: a model-dependent spectral approximation term and a distribution-dependent spectral weighting term induced by the input. From this decomposition we derive three diagnostics: a conservative in-band supremum $\vareps_{\mathrm{sup}}$, a global RMS proxy $\vareps_{\mathrm{rms}}$, and a sample-dependent effective metric $\vareps_{\mathrm{eff}}(f)$. These diagnostics can be computed without access to ground-truth solutions. Through four controlled experiments, we show that $\vareps_{\mathrm{eff}}(f)\|f\|$ consistently predicts energy error under in-distribution, in-band spectral shift, out-of-band tail, and compound shifts, whereas global metrics can be systematically misleading. Our framework shifts OOD assessment of neural operators from black-box benchmarking to operator-structure diagnostics, providing a practical route to auditable scientific machine learning.

摘要：神經運算子已成為解決偏微分方程（PDEs）的強大替代方案，但其在分佈轉移下的可靠性仍然是部署的一個關鍵障礙。現有的運算子學習中對於分佈外（OOD）泛化的研究主要是經驗性的，且為黑箱模型：它們報告總體錯誤指標，但未解釋錯誤產生的原因或何時會增長。我們提出了一個保持結構的框架，使得OOD泛化變得可預測且可審計。我們的關鍵思想是將學習到的解運算子參數化為作用於基礎橢圓運算子的特徵值的頻譜濾波器 $h_θ(λ)$，通過切比雪夫多項式展開實現，並使用弱形式目標進行訓練。這種參數化允許將能量範數誤差精確分解為兩個可觀察的組件：一個依賴於模型的頻譜近似項和一個由輸入引起的依賴於分佈的頻譜加權項。從這個分解中，我們推導出三個診斷指標：保守的帶內上界 $\vareps_{\mathrm{sup}}$、全局均方根代理 $\vareps_{\mathrm{rms}}$，以及依賴於樣本的有效度量 $\vareps_{\mathrm{eff}}(f)$。這些診斷指標可以在不接觸真實解的情況下計算。通過四個控制實驗，我們顯示 $\vareps_{\mathrm{eff}}(f)\|f\|$ 在帶內、帶內頻譜轉移、帶外尾部和復合轉移下持續預測能量誤差，而全局指標則可能系統性地誤導。我們的框架將神經運算子的OOD評估從黑箱基準轉變為運算子結構診斷，提供了一條可審計的科學機器學習的實用途徑。

##### **Signal2Symbol: Neuro-Symbolic Temporal Reasoning for Explainable Physiological Time-Series Anomaly Detection**
2609.26820v1 by Naser Mansour, Sidahmed Benabderrahmane, Ameer Rahwan

Physiological time series such as electrocardiograms (ECG) and electroencephalograms (EEG) exhibit complex temporal structure, substantial acquisition variability, and a strong need for transparent decision-making. Although deep models can achieve high detection performance, they often provide limited insight into why a segment is anomalous, how local anomalies relate over time, and whether a detection belongs to a broader recurring pattern. We propose Signal2Symbol, a neuro-symbolic framework for explainable biosignal anomaly detection. The method first converts ECG/EEG signals into symbolic sequences using either a learned VQ-VAE (Vector Quantized Variational Autoencoder) codebook or a SAX (Symbolic Aggregate approXimation) baseline. It then constructs bigram enriched token-window transactions and scores anomalies through rare itemset evidence derived from minimal rare itemset mining. Detected anomalous windows are merged into intervals and related using Allen interval algebra, enabling composite temporal explanations such as escalation chains, artifact overlap, and cross-channel synchrony. Finally, we introduce a rare temporal concept lattice based on Formal Concept Analysis (FCA), which groups anomalous intervals by shared rare symbolic evidence, Allen temporal relations, channel context, and robustness attributes. The resulting Galois lattice compresses many local detections into interpretable families of temporal-symbolic anomalies. We evaluate on three public benchmarks: MIT-BIH Arrhythmia (beat-level ECG), PTB-XL (record-level ECG), and the Bonn EEG dataset (segment-level EEG). We stress-test robustness under additive noise and baseline-wander perturbations. The results highlight the value of neuro-symbolic tokenization for temporal anomaly analysis and show that Allen/FCA reasoning provides compact, interpretable summaries of local detections.

摘要：生理時間序列如心電圖（ECG）和腦電圖（EEG）展現出複雜的時間結構、顯著的獲取變異性，以及對透明決策的強烈需求。儘管深度模型能夠達到高檢測性能，但它們通常對於為何某一段落是異常的、局部異常如何隨時間相關，以及檢測是否屬於更廣泛的重複模式，提供的見解有限。我們提出了Signal2Symbol，一種用於可解釋生物信號異常檢測的神經符號框架。該方法首先使用學習到的VQ-VAE（向量量化變分自編碼器）代碼本或SAX（符號聚合近似）基準，將ECG/EEG信號轉換為符號序列。然後，它構建了二元豐富的標記窗口交易，並通過從最小稀有項集挖掘中衍生的稀有項集證據對異常進行打分。檢測到的異常窗口被合併為區間，並使用艾倫區間代數進行關聯，從而實現複合時間解釋，如升級鏈、工件重疊和跨通道同步。最後，我們基於形式概念分析（FCA）引入了一個稀有時間概念格，該概念格根據共享的稀有符號證據、艾倫時間關係、通道上下文和穩健性屬性對異常區間進行分組。最終生成的伽羅瓦格壓縮了許多局部檢測，形成可解釋的時間-符號異常家族。我們在三個公共基準上進行評估：MIT-BIH心律不整（按拍心電圖）、PTB-XL（按記錄心電圖）和波恩EEG數據集（按段腦電圖）。我們在加性噪音和基線漂移擾動下進行了壓力測試。結果突顯了神經符號標記化在時間異常分析中的價值，並顯示艾倫/FCA推理提供了局部檢測的緊湊、可解釋的摘要。

##### **TicTacBench: Benchmarking Timing Closure Capabilities of Coding Agents**
2609.23363v1 by Bowei Wang, Zhigang Fang, Zhijie Yang, Renzhi Chen, Shanshan Li, Lei Wang

Recent advances in large language models (LLMs) have led to the emergence of coding agents capable of performing complex engineering tasks, including register-transfer level (RTL) design and optimization. Existing RTL benchmarks mainly evaluate functional correctness and performance, power, and area (PPA) of the generated RTL designs, leaving agents' ability for \emph{timing closure} under-evaluated. We propose TicTacBench, a benchmark specifically designed to evaluate coding agents' capabilities for RTL-level timing closure under post-place-and-route (post-PnR) evaluation. TicTacBench contains 30 diverse tasks, each provided with a suboptimal RTL design, realistic timing constraints, functional equivalence verification, and timing reports. With over 300 runs of coding agents driven by 8 frontier LLMs, we find that even the best agent can only close 53.3\% of tasks with 7.18\% area-delay product (ADP) degradation and 8.83\% energy-delay-squared product (EDDP) improvement on average. We identify common failure categories that explain why agents fail to close timing. Then we propose TicTacSkill, a new method that guides agents to follow standard timing-closure procedures and improves the Timing Closure Rate by 9\%. These results suggest that while coding agents have made significant progress in RTL design, their timing-closure capability still has substantial room for improvement.

摘要：最近在大型語言模型（LLMs）方面的進展導致了編碼代理的出現，這些代理能夠執行複雜的工程任務，包括寄存器傳輸級（RTL）設計和優化。現有的RTL基準主要評估生成的RTL設計的功能正確性和性能、功耗及面積（PPA），而對代理在\emph{時序收斂}方面的能力評估不足。我們提出了TicTacBench，一個專門設計用於評估編碼代理在後佈局和路由（post-PnR）評估下的RTL級時序收斂能力的基準。TicTacBench包含30個多樣化的任務，每個任務都提供了一個次優的RTL設計、現實的時序約束、功能等價驗證和時序報告。通過對8個前沿LLM驅動的編碼代理進行超過300次的運行，我們發現即使是最好的代理也只能完成53.3\%的任務，平均面積延遲乘積（ADP）下降7.18\%，能量延遲平方乘積（EDDP）改善8.83\%。我們識別出常見的失敗類別，解釋了為什麼代理無法完成時序收斂。然後我們提出了TicTacSkill，一種新方法，指導代理遵循標準的時序收斂程序，並將時序收斂率提高了9\%。這些結果表明，儘管編碼代理在RTL設計方面取得了顯著進展，但它們的時序收斂能力仍有很大的改進空間。

##### **Why Ghost Outputs Teach: A Kernel-Based Understanding of Subliminal Learning**
2609.23260v1 by Zhe Li, Bicheng Ying, Chaosheng Dong, Haibo Yang

Subliminal Learning (SL) is a recently identified phenomenon in which a student model acquires downstream task capabilities by matching seemingly unrelated auxiliary outputs from a teacher, despite never observing task labels, task-specific outputs, or the original training data. While recent studies have identified where subliminal signals may reside, the optimization mechanism underlying this phenomenon remains poorly understood. In this work, we provide a mechanistic understanding of SL through the lens of learning dynamics. Specifically, we derive a chained cross-task kernel that explicitly links ghost-output supervision to changes in task predictions through shared backbone representations. Our unified analytical framework provides a rigorous mathematical explanation for three central empirical puzzles in SL: (i) under shared initialization, the transfer operator forms a strictly Positive Semi-Definite (PSD) structure, guaranteeing that ghost-output optimization aligns the student with the teacher's true task objective without explicit label exposure; (ii) the ghost-output dimensionality acts as an explicit rank bottleneck governing the transfer of task-relevant features; and (iii) synthetic, high-entropy inputs function as broadband probes that maximize cross-task kernel overlap, explaining why random noise consistently outperforms structured data for subliminal transfer. Experiments on the canonical ghost-output setting validate all three theoretical predictions, providing the first learning-dynamics-based theoretical explanation of how ghost-output supervision gives rise to subliminal learning.

摘要：潛意識學習（SL）是一種最近被識別的現象，在這種現象中，學生模型通過匹配來自教師的看似無關的輔助輸出來獲得下游任務能力，儘管從未觀察到任務標籤、任務特定輸出或原始訓練數據。雖然最近的研究已經識別出潛意識信號可能存在的地方，但這一現象背後的優化機制仍然不甚了解。在這項工作中，我們通過學習動態的視角提供了對SL的機械理解。具體而言，我們推導出一個鏈式跨任務核，明確將鬼輸出監督與通過共享骨幹表示的任務預測變化聯繫起來。我們的統一分析框架為SL中的三個核心實證難題提供了嚴謹的數學解釋：（i）在共享初始化下，轉移算子形成了一個嚴格的正半定（PSD）結構，保證鬼輸出優化使學生與教師的真實任務目標一致，而無需明確的標籤曝光；（ii）鬼輸出的維度充當了顯式的秩瓶頸，控制著任務相關特徵的轉移；以及（iii）合成的高熵輸入作為寬頻探針，最大化跨任務核的重疊，解釋了為什麼隨機噪聲在潛意識轉移中始終優於結構化數據。在經典的鬼輸出設置上的實驗驗證了這三個理論預測，提供了第一個基於學習動態的理論解釋，說明鬼輸出監督如何導致潛意識學習。

##### **Triggers and Diagnostics for LLM-Based Interpretability Failures in Active Inference Agents**
2609.23215v1 by Param Raval, Rohit Shenoy, Archana Vaidheeswaran

LLM explainers are increasingly attached to autonomous agents as runtime oversight, with operators reading a generated account of the agent's beliefs and actions rather than its internal state. We audit the account itself, pairing an Active Inference (AIF) agent that tracks German grid demand and adjusts generation with an LLM explainer on three backends (GPT-4o, Claude-3-Opus, Gemini), and probing the pair with three black-box triggers. Corrupting the observation stream by 600 MW per step moves the agent's posterior by 490 MW, roughly 0.9% of grid capacity. None of the 30 explanations produced during the injection flag anything under a stated rubric, and each narrates the corrupted belief fluently. On timesteps where the agent takes an objectively wrong action, all three explainers produce a sycophantic rationalization 80-95% of the time (n = 20 per backend). Attacker-controlled text in the observation metadata field steers the explainer, with susceptibility differing by provider and data exfiltration succeeding on all three. We propose mitigations for each failure but do not evaluate them. In every failure we observed, the explanation was fluent and wrong. Moreover, nothing in the explainer architecture checks whether an explanation is true before an operator acts on it. Testing the explainer therefore belongs in any audit of an agentic deployment.

摘要：LLM 解釋器越來越多地與自主代理結合，作為運行時監督，操作員閱讀生成的代理信念和行動的報告，而不是其內部狀態。我們審計該報告，將一個跟蹤德國電網需求並調整發電的主動推理 (AIF) 代理與三個後端的 LLM 解釋器（GPT-4o、Claude-3-Opus、Gemini）配對，並用三個黑箱觸發器對這對進行探測。每一步將觀察流的數據損壞 600 MW，使代理的後驗變化 490 MW，大約是電網容量的 0.9%。在注入過程中產生的 30 個解釋中，沒有任何一個在聲明的標準下標示出任何問題，每個解釋都流利地敘述了被損壞的信念。在代理採取客觀錯誤行動的時間步上，所有三個解釋器在 80-95% 的時間內產生了阿諛奉承的合理化（n = 每個後端 20 次）。觀察元數據字段中的攻擊者控制文本引導了解釋器，並且不同提供者的易受攻擊性不同，數據外洩在三者中均成功。我們為每個失敗提出了緩解措施，但未對其進行評估。在我們觀察到的每一個失敗中，解釋都是流利且錯誤的。此外，解釋器架構中沒有任何檢查解釋是否真實的機制，操作員在採取行動之前並不會檢查解釋的真實性。因此，測試解釋器應該是任何代理部署審計的一部分。

##### **Whitening Inverts the Hierarchy: What the Norm of a Whitened Embedding Measures**
2609.23117v1 by Mohammed Ahnouch, Lotfi Elaachak

Whitening a foundation-model embedding and using its squared norm as a training-free likelihood surrogate is motivated by the observation that whitened coordinates often appear approximately standard normal. We show that this observation follows from the projection central limit theorem and therefore does not imply a Gaussian joint distribution. Across multiple encoders and three training objectives, we find systematic over-dispersion of the whitened radius relative to the Gaussian reference, including against distributional clones with identical mean and covariance. We further show that the commonly reported agreement between empirical and theoretical norm statistics is an algebraic consequence of in-sample whitening and does not constitute evidence for Gaussianity.
  We identify the mechanism behind this behavior: whitening reverses the encoder's spectral hierarchy, shifting the contribution to the squared norm toward near-degenerate directions that encode predominantly noise. In these directions, the dominant variability is governed by a single input-dependent scale. We estimate this scale from two moments and use it to predict, without additional free parameters, the cross-dependence between disjoint spectral halves. These results indicate that the squared whitened norm is better interpreted as a Mahalanobis measure of semantic atypicality than as a log-likelihood. This interpretation explains both its practical effectiveness and its calibration failures: the statistic can rank and detect atypical samples consistently with nonparametric density estimates and across encoders trained with different objectives, while Gaussian tail thresholds can be inaccurate by orders of magnitude. etc.

摘要：將基礎模型的嵌入進行白化，並使用其平方範數作為無訓練的似然替代，這一做法的動機源於觀察到白化坐標通常近似標準正態分佈。我們顯示這一觀察是從投影中心極限定理推導而來，因此並不意味著高斯聯合分佈。在多個編碼器和三個訓練目標下，我們發現白化半徑相對於高斯參考存在系統性過度離散，包括與均值和協方差相同的分佈克隆相比。我們進一步顯示，常報告的經驗和理論範數統計的一致性是樣本內白化的代數結果，並不構成高斯性的證據。<lb/>我們確定了這一行為背後的機制：白化逆轉了編碼器的光譜層次，將平方範數的貢獻轉移到主要編碼噪聲的近退化方向。在這些方向上，主導變異性由單一的輸入依賴尺度所控制。我們從兩個矩估計這個尺度，並利用它在不增加額外自由參數的情況下預測不相交光譜半部之間的交叉依賴。這些結果表明，平方白化範數更應被解釋為語義非典型性的馬哈拉諾比斯度量，而不是對數似然。這一解釋解釋了其實際有效性及其校準失敗：該統計量可以一致地對非典型樣本進行排名和檢測，與非參數密度估計以及使用不同目標訓練的編碼器相符，而高斯尾閾值可能會有數量級的誤差。等。

##### **FireWorldBench: Benchmarking Complex Physical World Intelligence through Coupled-Field Fire Dynamics**
2609.23064v1 by Qiang Chen, Hao Guo, Huatai Zhu, Tairan Huang, Yichao Cao, Hongyan Xu, Keke Huang, Haifeng Li, Yi Chen, Xiu Su

Understanding the physical world requires more than object recognition, scene description, and short-term visual prediction, as real-world physical systems involve multiple continuous fields, latent causal mechanisms, partial observations, and intervention-sensitive dynamics. We propose FireWorldBench, a benchmark for evaluating complex physical world intelligence in multimodal large language models and agents through coupled-field fire dynamics. Fire provides a canonical stress-test environment, where multiple interacting physical fields jointly shape observable states and temporal dynamics. FireWorldBench is organized along two complementary axes, a physical capability axis and a fire scenario task axis, jointly covering physical-state understanding, temporal dynamics, causal mechanisms, and intervention reasoning. The benchmark comprises 520 fire-world entries, including 494 controlled simulation worlds and 26 real-world-aligned event groups, spanning 47 scene archetypes across 7 environment families. These entries combine structured textual observations, multiple 2D physical-field visualizations, and 3D event-level scene modeling, yielding 9,074 text-image interleaved question-answer pairs across choice-based and open-ended report-generation formats. FireWorldBench evaluates whether models can infer latent physical states, explain underlying mechanisms, forecast coupled-field evolution, and assess intervention consequences from multimodal partial observations, providing a challenging testbed for complex physical world intelligence.

摘要：理解物理世界需要的不僅僅是物體識別、場景描述和短期視覺預測，因為現實世界的物理系統涉及多個連續場、潛在的因果機制、部分觀察和對干預敏感的動態。<lb/>我們提出了FireWorldBench，一個用於通過耦合場火災動力學評估多模態大型語言模型和代理的複雜物理世界智能的基準。<lb/>火災提供了一個典型的壓力測試環境，在這裡多個相互作用的物理場共同塑造可觀察的狀態和時間動態。<lb/>FireWorldBench在兩個互補的軸上組織，一個是物理能力軸，另一個是火災場景任務軸，聯合涵蓋物理狀態理解、時間動態、因果機制和干預推理。<lb/>該基準包含520個火災世界條目，包括494個受控模擬世界和26個與現實世界對應的事件組，涵蓋7個環境家族中的47個場景原型。<lb/>這些條目結合了結構化的文本觀察、多個2D物理場可視化和3D事件級場景建模，產生了9,074個文本-圖像交錯的問答對，涵蓋基於選擇的和開放式報告生成格式。<lb/>FireWorldBench評估模型是否能夠推斷潛在的物理狀態、解釋潛在的機制、預測耦合場的演變，並從多模態部分觀察中評估干預後果，為複雜的物理世界智能提供了一個具有挑戰性的測試平台。

##### **An Accurate and Interpretable Hyper Graph Neural Network for GBM Survival Prediction**
2609.25088v1 by Mushahid Intesum

Survival prediction for glioblastoma multiforme (GBM) demands models that are both accurate and interpretable, yet existing approaches treat these objectives as com- peting, where performant models sacrifice transparency, while interpretable models accept degraded predictive power. We argue that this trade-off is not inherent. Graph neural net- works offer a structural foundation for extracting interpretable, explainable representations without compromising discriminative ability. Furthermore, current methods typically rely on a single imaging modality, underutilizing the complementary information available across multi-modal MRI and clinical metadata. We propose a multi-modal framework that inte- grates three components to address both objectives simultaneously: (1) a sheaf hypergraph neural network that captures higher-order relationships among tissue patches through direc- tional, asymmetric message passing; (2) a concept bottleneck layer that compresses learned representations into clinically grounded concepts, enforcing ante-hoc interpretability; and (3) an extension sufficiency test (EST) regularizer that penalizes unfaithful explanations during training, ensuring that model explanations genuinely reflect the internal decision process. Clinical and genomic features are incorporated through gated fusion, preserving the dominant prognostic signal of molecular markers while retaining concept-level traceabil- ity. Evaluated on 593 patients from the UPenn-GBM dataset under 5-fold cross-validation, our framework achieves a concordance index of 0.643 with the lowest fold-level variance among all compared models (std = 0.015). To our knowledge, this is the first work to unify sheaf hypergraph convolution, concept bottleneck supervision, and EST regularization for interpretable survival prediction from brain MRI

摘要：生存預測對於多形性膠質母細胞瘤（GBM）需要準確且可解釋的模型，然而現有的方法將這些目標視為競爭關係，其中性能優越的模型犧牲了透明度，而可解釋的模型則接受了預測能力的下降。我們認為這種權衡並非固有的。圖神經網絡提供了一個結構基礎，可以提取可解釋、可解釋的表示，而不妨礙區分能力。此外，當前的方法通常依賴於單一的成像模式，未能充分利用多模態MRI和臨床元數據中可用的互補信息。我們提出了一個多模態框架，整合三個組件以同時解決這兩個目標：（1）一個束超圖神經網絡，通過方向性、不對稱的消息傳遞捕捉組織片段之間的高階關係；（2）一個概念瓶頸層，將學習到的表示壓縮為臨床基礎的概念，強化事前可解釋性；以及（3）一個擴展充分性測試（EST）正則化器，在訓練過程中懲罰不忠實的解釋，確保模型的解釋真實反映內部決策過程。臨床和基因組特徵通過門控融合納入，保留了分子標記的主導預後信號，同時保持概念級的可追溯性。在UPenn-GBM數據集上對593名患者進行5折交叉驗證，我們的框架達到了0.643的協調指數，並在所有比較模型中具有最低的折層變異性（標準差=0.015）。據我們所知，這是第一個將束超圖卷積、概念瓶頸監督和EST正則化統一應用於可解釋的腦MRI生存預測的工作。

##### **From Research Frontier to Laboratory Bench: Design of a Four-Tier Experimental Teaching System for Multimodal Medical Image Intelligent Diagnosis**
2609.22790v1 by Dongjing Shan, Yamei Luo, Jin Li, Yong Luo

Undergraduate programmes in intelligent medical engineering are expanding, yet laboratory curricula lag behind the multimodal, long-tailed, and distributionally shifting realities of clinical AI. This design paper presents an advanced experimental teaching system that translates an ongoing multimodal deep learning research project on endometrial carcinoma into a structured undergraduate lab sequence. We identify three educational gaps (modality, authenticity, and deployment) and derive four pedagogical principles from constructive alignment, experiential learning, the research teaching nexus, and the CDIO framework. The curriculum comprises four progressive tiers plus an engineering layer, with 32 laboratory units over 64 contact hours, delivered via a custom virtual clinical workstation using de-identified multi-institutional data. Each tier maps to a specific technical bottleneck, prerequisite coursework, and criterion-referenced deliverables. Data governance, safety, and assessment protocols are specified. Learning outcome data will be collected across two implementation cycles.

摘要：本科智能醫療工程課程正在擴展，但實驗室課程卻落後於臨床AI的多模態、長尾和分佈性變化的現實。這篇設計論文提出了一個先進的實驗教學系統，將一個關於子宮內膜癌的持續多模態深度學習研究項目轉化為一個結構化的本科實驗室序列。我們確定了三個教育差距（模態、真實性和部署），並從建設性對齊、體驗式學習、研究教學聯繫和CDIO框架中推導出四個教學原則。課程包括四個漸進層次加上一個工程層，擁有32個實驗單元，總共64小時的接觸時間，通過使用去識別的多機構數據的自定義虛擬臨床工作站進行授課。每個層次對應於一個特定的技術瓶頸、先修課程和基準參考成果。數據治理、安全性和評估協議已被明確規定。學習成果數據將在兩個實施週期中收集。

##### **MATE: Policy-Aware Security Auditing for Mobile Agents via Synthesis-Driven Trajectory Learning**
2609.22724v1 by Changyue Jiang, Jiayi Wang, Xin Wen, Jiarun Dai, Geng Hong, Xudong Pan

Mobile agents powered by foundation models now automate complex, multi-step workflows on real devices, but their trajectories can violate app-specific security policies. Existing trajectory-level defenses rely on LLM prompting or rigid rules, and thus fail to support fine-grained, natural-language policies that generalize across apps and tasks. In this work, we introduce MATE, a lightweight, policy-conditioned auditor that encodes both agent trajectories and natural-language security policies to determine whether a trajectory violates a given policy and to explain why. Treating policies as editable text rather than fixed model parameters allows MATE to handle user-defined and evolving requirements without retraining. To construct MATE, we build a knowledge base by extracting app descriptions, workflows, and policies from hundreds of popular mobile apps worldwide, and synthesizing over 140K semantically realistic, policy-conditioned trajectories with a multi-stage pipeline. We further release MATEBench, a trajectory-level auditing benchmark with two synthetic subsets and one real-world subset of manually collected trajectories. Models trained with our synthesis-driven trajectory learning achieve over 95% accuracy on MATEBench, retain strong performance on external safety benchmarks, and audit trajectories from Zhipu's AutoGLM and Alibaba's Mobile-Agent on real devices with over 95% accuracy, outperforming prior methods by over 20%. MATE shows that practical, fine-grained security auditing for heterogeneous mobile agents is both feasible and effective.

摘要：行動代理由基礎模型驅動，現在能夠在真實設備上自動化複雜的多步驟工作流程，但它們的軌跡可能會違反應用特定的安全政策。現有的軌跡級別防禦依賴於大型語言模型提示或僵硬的規則，因此無法支持跨應用和任務的細粒度自然語言政策。在這項工作中，我們介紹了MATE，一個輕量級的政策條件審計工具，它編碼了代理的軌跡和自然語言安全政策，以確定軌跡是否違反了給定的政策並解釋原因。將政策視為可編輯的文本而非固定的模型參數，使MATE能夠處理用戶定義和不斷演變的需求，而無需重新訓練。為了構建MATE，我們通過從全球數百個流行的移動應用中提取應用描述、工作流程和政策，並使用多階段管道合成超過14萬條語義上現實的政策條件軌跡來建立知識庫。我們進一步發布了MATEBench，一個具有兩個合成子集和一個手動收集的真實世界子集的軌跡級審計基準。使用我們的合成驅動軌跡學習訓練的模型在MATEBench上達到超過95%的準確率，並在外部安全基準上保持強勁的表現，能夠在真實設備上對Zhipu的AutoGLM和阿里巴巴的Mobile-Agent的軌跡進行審計，準確率超過95%，比以前的方法提高了超過20%。MATE顯示出對異構移動代理進行實用的細粒度安全審計既可行又有效。

##### **GaitVista: Reliability-Aware AI Measurement toward Accessible Longitudinal Gait Assessment**
2609.22619v1 by Nethmi Jayasinghe, Mihir Parashar, Amit Ranjan Trivedi

Tracking recovery of walking function requires detecting meaningful gait change across rehabilitation sessions, yet objective 3D measurement remains confined to specialized motion-capture laboratories. Small camera sets and body-worn inertial sensors broaden access, but reliability varies across joints and time, allowing sensing failures to masquerade as patient change. We present \textsc{GaitVista}, a reliability-aware measurement layer whose lightweight gate assigns joint- and frame-specific visual contributions using camera coverage, local visual quality, cross-modal disagreement, and root-motion continuity, and exposes them for inspection. Across seven clean and degraded sensing conditions on TotalCapture, \textsc{GaitVista} reduces average full-body and lower-body error by \textbf{27.7\%} and \textbf{27.8\%}, attains the lowest worst-condition error among fusion methods, and reduces the gap to a joint-frame oracle from $2.76$--$5.33$~cm for condition-blind baselines to $1.11$~cm. On MoVi with image-derived keypoints, it is the only deployable fusion method to improve over both unimodal streams, reducing marker-supported error by \textbf{6.4\%} relative to the strongest learned fusion baseline. On TotalCapture, it improves bilateral knee-flexion waveform accuracy by \textbf{18.9\%}. Raw inertial measurements from five TotalCapture participants show location- and time-varying magnetic disturbance, supporting the design's reliability premise. Both benchmarks contain neurologically healthy participants in controlled settings and retain participant-specific IMU calibration; we therefore report progress toward accessible gait assessment, not validated clinical deployment.

摘要：追蹤步態功能的恢復需要在康復會議中檢測到有意義的步態變化，但客觀的3D測量仍然限於專門的運動捕捉實驗室。小型攝影機組和身體佩戴的慣性傳感器擴大了可及性，但可靠性在關節和時間上有所不同，這使得感測失敗可能偽裝成患者的變化。我們提出了\textsc{GaitVista}，這是一個關注可靠性的測量層，其輕量級的閘道根據攝影機覆蓋範圍、局部視覺質量、跨模態不一致性和根運動連續性分配關節和幀特定的視覺貢獻，並將其暴露以供檢查。在TotalCapture的七種乾淨和劣化的感測條件下，\textsc{GaitVista}將全身和下身的平均誤差分別降低了\textbf{27.7\%}和\textbf{27.8\%}，在融合方法中達到最低的最差條件誤差，並將與關節幀預言者的差距從$2.76$--$5.33$~cm降低到$1.11$~cm。在MoVi中，使用圖像衍生的關鍵點，它是唯一一種可部署的融合方法，能夠在兩個單模態流上都有所改善，相對於最強的學習融合基線，將標記支持的誤差降低了\textbf{6.4\%}。在TotalCapture上，它將雙側膝關節屈曲波形的準確性提高了\textbf{18.9\%}。來自五位TotalCapture參與者的原始慣性測量顯示出位置和時間變化的磁擾動，支持設計的可靠性前提。這兩個基準均包含在受控環境中神經上健康的參與者，並保留參與者特定的IMU校準；因此，我們報告了朝向可及步態評估的進展，而不是經過驗證的臨床部署。

##### **Benchmarking the Explanatory Quality of Open-Weight Vision-Language Models in Face Recognition**
2609.21879v1 by Laurent Colbois, Sébastien Marcel

Vision-Language Models (VLMs) have recently been proposed as promising tools for face recognition, as they can produce natural language explanations alongside similarity scores. This capability is considered appealing for face comparisons in forensic contexts, which require decisions to be transparent and auditable. However, existing evaluations of VLMs for that use case focus mostly on recognition accuracy, while the validity of generated explanations remains unquantified. In this work, we introduce a benchmarking framework for VLM-based face recognition that treats explanation quality as a core evaluation axis. We propose two criteria that explanations should satisfy: relevance, i.e., reliance on identity-stable facial features; and faithfulness, i.e., alignment with the visible image content without hallucinated features. We jointly develop a methodology enabling the quantification of relevance and faithfulness of evaluated models, based on constraining model outputs to a structured explanation format that supports automated querying and auditing. Using this framework, we benchmark several families of open-weight VLMs, jointly evaluating face verification accuracy and explanation quality. Our results highlight remaining shortcomings of produced explanations, and emphasize the need for such explanation quality metrics to get a complete picture of model performance. The proposed benchmark and open-source evaluation harness provide a foundation for proper benchmarking and future fine-tuning of explainable face recognition systems.

摘要：視覺-語言模型（VLMs）最近被提出作為面部識別的有前景工具，因為它們可以生成自然語言解釋以及相似性分數。這種能力在法醫環境中的面部比較中被認為是吸引人的，因為這些環境需要透明且可審計的決策。 然而，現有的VLM評估主要集中在識別準確性上，而生成解釋的有效性仍未量化。在這項工作中，我們引入了一個基於VLM的面部識別基準框架，將解釋質量視為核心評估軸。我們提出了兩個解釋應滿足的標準：相關性，即依賴身份穩定的面部特徵；以及忠實性，即與可見圖像內容的一致性，而不包含幻覺特徵。我們共同開發了一種方法論，使得能夠量化所評估模型的相關性和忠實性，這是基於將模型輸出限制為支持自動查詢和審計的結構化解釋格式。利用這個框架，我們基準測試了幾個開放權重的VLM家族，並共同評估了面部驗證準確性和解釋質量。我們的結果突顯了生成解釋的剩餘缺陷，並強調了這種解釋質量指標的必要性，以獲得模型性能的完整圖景。所提出的基準和開源評估工具為正確的基準測試和未來可解釋面部識別系統的微調提供了基礎。

##### **An Agentic Just-in-Time Adaptive Intervention System for Personalized Sleep Support: Proof-of-Concept Study with N of 1 Data**
2609.21805v1 by Nick Rezaee, Chelsea Boccagno

Background: Just-in-time adaptive interventions (JITAIs) can use behavioral data to adapt support to changing contexts, but many rely on predefined rules and manual configuration.
  Objective: We developed a proof-of-concept sleep JITAI using an AI agent to review personal data, evaluate reminders, adapt interventions, and record decisions for human review.
  Methods: Running in Home Assistant on a configurable schedule, the agent follows a reusable skill file to review 30 days of sleep and behavioral data, including physical activity, smartphone use, and bedtime routines, to identify patterns and create or update automated reminders.
  Results: Initial runs demonstrated technical feasibility, successfully completing data review and intervention decisions while limiting reminders to three per day and saving decision records.
  Conclusions: Agentic AI may enable flexible, adaptive sleep JITAIs. The architecture supports future comparison with fixed or rulebased interventions, requires human oversight, and could extend to other health behaviors.

摘要：背景：即時適應干預（JITAIs）可以利用行為數據來根據變化的情境調整支持，但許多仍依賴預定義的規則和手動配置。<lb/>  
目標：我們開發了一個概念驗證的睡眠 JITAI，使用 AI 代理來審查個人數據、評估提醒、調整干預並記錄決策以供人類審查。<lb/>  
方法：該代理在 Home Assistant 中以可配置的時間表運行，遵循可重用的技能文件，審查 30 天的睡眠和行為數據，包括身體活動、智能手機使用和就寢例行，以識別模式並創建或更新自動提醒。<lb/>  
結果：初步運行顯示技術可行性，成功完成數據審查和干預決策，同時將提醒限制為每天三次並保存決策記錄。<lb/>  
結論：代理式 AI 可能使靈活的、自適應的睡眠 JITAIs 成為可能。該架構支持未來與固定或基於規則的干預進行比較，需人類監督，並可擴展到其他健康行為。

##### **Consistent Relexicalization of Clinical Documents using Graph-Based Approach**
2609.21387v1 by Dipankar Das, Atri Mandal, Sandeep Singh, Tushar Shandhilya

Relexicalization is a pivotal technique in clinical NLP, as it facilitates robust masking of sensitive information while synthesizing datasets that retain high-fidelity, real-world characteristics. However, preserving structural integrity, relational coherence, and temporal consistency during transformation remains a significant challenge. Existing approaches frequently rely on independent entity replacement, which results in clinical inconsistencies across longitudinal records. This reduces the value of such relexicalized datasets for downstream scientific analysis. To address these limitations, we introduce G-RELIC (Graph Based Contextual Relexicalization with Improved Consistency) which combines the power of LLMs with graphs. G-RELIC implements a graph-based mapping mechanism which optimizes for one-to-one correspondence between original and surrogate entities. It also introduces a deterministic temporal repositioning algorithm to preserve temporal consistency. Empirical evaluations on diverse, real-world clinical datasets validate that G-RELIC significantly outperforms state-of-the-art baselines. G-RELIC yields a 30.4 percentage point improvement in relational integrity (62.1% to 92.5%) and 45.9 percentage point improvement in temporal coherence (46% to 91.9%) without compromising on the recognized privacy benchmarks for clinical datasets. This maximizes the analytical utility of relexicalized datasets while minimizing re-identification risk.

摘要：再詞彙化是臨床自然語言處理中的一項關鍵技術，因為它促進了敏感信息的穩健遮蔽，同時合成保留高保真度、真實世界特徵的數據集。<lb/>然而，在轉換過程中保持結構完整性、關聯一致性和時間一致性仍然是一個重大挑戰。<lb/>現有的方法通常依賴於獨立實體替換，這導致了縱向記錄中的臨床不一致性。<lb/>這降低了此類再詞彙化數據集在下游科學分析中的價值。<lb/>為了解決這些限制，我們介紹了 G-RELIC（基於圖的上下文再詞彙化與改進一致性），它結合了大型語言模型和圖的力量。<lb/>G-RELIC 實現了一種基於圖的映射機制，優化了原始實體和替代實體之間的一對一對應。<lb/>它還引入了一種確定性的時間重新定位算法，以保持時間一致性。<lb/>在多樣化的真實臨床數據集上的實證評估證實，G-RELIC 顯著超越了最先進的基準。<lb/>G-RELIC 在關聯完整性上提高了 30.4 個百分點（從 62.1% 到 92.5%），在時間一致性上提高了 45.9 個百分點（從 46% 到 91.9%），而不妥協於臨床數據集的隱私基準。<lb/>這最大化了再詞彙化數據集的分析效用，同時最小化了重新識別的風險。

##### **Ability-Residual Decoupled Modeling for Affective Cognitive Diagnosis**
2609.21214v1 by Boyuan Zhao, Meng Ye

Cognitive diagnosis infers students' concept mastery from response logs. However, students' responses are not determined by mastery alone: non-cognitive factors such as emotion, engagement, and fatigue can also affect performance. Affective cognitive diagnosis therefore extends conventional cognitive diagnosis by incorporating affective states. Existing methods often assume that the cognitive diagnosis backbone has already explained ability, item, and concept effects, so the remaining errors can be attributed mainly to affect. We argue that this assumption can be insufficient in real educational data: item calibration bias, systematic concept bias, personalized student-concept deviations, and latent student-item matching can form stable cognitive residuals. Without an explicit modeling pathway, these residuals may leak into affective representations, producing affect contamination. To address this problem, we propose an ability-residual decoupled framework for affective cognitive diagnosis. The model first captures unmodeled cognitive residuals through student, item, concept, student-concept, and low-rank student-item components, and then uses an affective module to modulate guess/slip effects. A Q-matrix-constrained concept residual attention mechanism adaptively aggregates only item-relevant concept residuals. Experiments on ASSIST2017, ASSIST2012, ASSIST2009, and Junyi with six cognitive diagnosis backbones show response-prediction gains across the reported comparisons and generally improved affect alignment when affect labels are available. Ablation studies, leakage probes, principal component analysis visualization, long-tail analysis, and case studies further indicate that ability residuals absorb stable cognitive bias, reduce cognitive contamination in the affective branch, and enhance the robustness and predictive accuracy of cognitive diagnosis models.

摘要：認知診斷從反應日誌推斷學生的概念掌握情況。然後，學生的反應不僅僅由掌握程度決定：非認知因素如情緒、參與度和疲勞也會影響表現。因此，情感認知診斷通過納入情感狀態來擴展傳統的認知診斷。現有的方法通常假設認知診斷的基礎已經解釋了能力、項目和概念的影響，因此剩餘的誤差主要可以歸因於情感。我們認為這一假設在真實的教育數據中可能不夠充分：項目標定偏差、系統性概念偏差、個性化的學生-概念偏差以及潛在的學生-項目匹配可能形成穩定的認知殘差。若沒有明確的建模途徑，這些殘差可能會滲透到情感表徵中，產生情感污染。為了解決這個問題，我們提出了一個能力-殘差解耦框架，用於情感認知診斷。該模型首先通過學生、項目、概念、學生-概念和低秩學生-項目組件捕捉未建模的認知殘差，然後使用情感模塊來調節猜測/滑動效應。一個受Q矩陣約束的概念殘差注意機制自適應地聚合僅與項目相關的概念殘差。在ASSIST2017、ASSIST2012、ASSIST2009和Junyi上進行的實驗顯示，在報告的比較中，反應預測的增益，以及在情感標籤可用時，情感對齊的普遍改善。消融研究、泄漏探測、主成分分析可視化、長尾分析和案例研究進一步表明，能力殘差吸收了穩定的認知偏差，減少了情感分支中的認知污染，並增強了認知診斷模型的穩健性和預測準確性。

##### **Clinician-Grounded Quality Assurance for AI-Assisted Psychiatric Intake**
2609.21149v1 by King Shi, Amanda Li, Jonathan Ivey, Synthia Qia Wang, Guan Gui, Hyunseo Kim, Peter Zandi, Jason Straub, Jacob Taylor, Ananya Joshi

Before patients can use AI-assisted psychiatric intake systems, health systems need practical ways to routinely evaluate these tools against their clinical standards for quality assurance. Because clinicians may use different intake styles, evaluation for this task must (1) support comparison across interviewing approaches, (2) minimize clinician burden, and (3) measure clinically relevant performance for health systems deploying these technologies. We present a clinician-grounded evaluation platform built around a memory-augmented patient simulator for open-ended AI interviewing, InterviewPlayground. We created interactive patients using InterviewPlayground with our expert-authored vignettes, constructed a simulated intake platform for the interviews, and designed evaluation modalities relevant to intake. In a pilot of 6 clinicians in a 25-minute assessment compared to a GPT-based LLM intake interviewer, the LLM recovered more of the clinically relevant items embedded in the patient vignettes (88.0% vs. 38.9%), but made more clinical inferences not based on the interview (56.8% vs. 27.8%), and characterized identified safety concerns less often (33.3% vs. 66.7%), setting the stage for deployed quality assurance for this task.

摘要：在患者能夠使用 AI 輔助的精神科接診系統之前，醫療系統需要實用的方法來定期評估這些工具是否符合其臨床標準以進行質量保證。因為臨床醫生可能使用不同的接診風格，因此這項任務的評估必須 (1) 支持不同訪談方法之間的比較，(2) 減少臨床醫生的負擔，並且 (3) 測量對於部署這些技術的醫療系統而言臨床相關的表現。我們提出了一個以臨床醫生為基礎的評估平台，圍繞著一個增強記憶的患者模擬器，用於開放式的 AI 訪談，名為 InterviewPlayground。我們使用 InterviewPlayground 創建了互動式患者，並利用我們專家的小品故事，構建了一個模擬接診平台以進行訪談，並設計了與接診相關的評估方式。在對 6 位臨床醫生進行的 25 分鐘評估的試點中，與基於 GPT 的 LLM 接診訪談者相比，LLM 從患者小品故事中恢復了更多臨床相關項目 (88.0% 對 38.9%)，但做出了更多不基於訪談的臨床推斷 (56.8% 對 27.8%)，並且識別安全問題的頻率較低 (33.3% 對 66.7%)，為這項任務的質量保證部署奠定了基礎。

##### **Trustworthy FinAInce: Unpacking How AI-Mediated Financial Advice is Judged**
2609.20989v2 by Aryan Ramchandra Kapadia, Eshwar Chandrasekharan, Koustuv Saha

As generative AI is increasingly used as a source of personal financial guidance, understanding how people appraise such advice is important for supporting appropriate reliance. We conducted a randomized vignette experiment with 285 U.S. adults across eight financial decisions, independently varying three advice styles---AI, expert, and online community---and displayed source labels while holding the underlying recommendation consistent. Advice style most strongly shaped message and safety appraisals, Expert labels selectively increased perceived source knowledge, and decision context primarily shaped risk and safety appraisals. These appraisals were associated with downstream judgments, with models explaining 69.2% of overall quality, 75.9% of trust, and 82.9% of intended reliance. Expert-style advice also remained most preferred when shown without source labels. Our findings have implications for understanding financial advice evaluation, distinguishing the roles of advice style and source labels, and designing financial AI that supports grounded evaluation rather than simply maximizing trust.

摘要：隨著生成式人工智慧越來越多地被用作個人財務指導的來源，了解人們如何評價這些建議對於支持適當的依賴至關重要。<lb/>我們對285名美國成年人進行了一項隨機小品實驗，涵蓋八個財務決策，獨立變化三種建議風格——AI、專家和在線社區——並在保持基本建議一致的同時顯示來源標籤。<lb/>建議風格最強烈地影響了信息和安全評估，專家標籤選擇性地提高了感知的來源知識，而決策背景主要影響風險和安全評估。<lb/>這些評估與後續判斷相關，模型解釋了69.2%的整體質量、75.9%的信任和82.9%的預期依賴。<lb/>專家風格的建議在未顯示來源標籤的情況下仍然是最受偏好的。<lb/>我們的研究結果對理解財務建議評估、區分建議風格和來源標籤的角色，以及設計支持基於事實的評估而非僅僅是最大化信任的財務AI具有重要意義。

##### **ERCPMP-Gx: Endoscopic Image and Video Dataset for Morphological, Histopathological, and Genomic Characterization of Colorectal Polyposis**
2609.20815v1 by Zahra Ghaffari, Massih Bahar, Mojgan Forootan, Ali Darvishi, Hamidreza Bolhasani

Hereditary polyposis syndromes can be precursor lesions to colorectal cancer and are associated with a broad spectrum of extracolonic tumors. Early identification and accurate classification of these syndromes are essential for timely diagnosis, individualized patient management, and targeted surveillance strategies for affected families. However, public endoscopic datasets are largely organized around the individual sporadic polyp, and none links the polyposis phenotype to histopathology and germline findings at the patient level. Here, we present ERCPMP-Gx, an endoscopic, histopathological, and genomic dataset developed to support the application of artificial intelligence (AI) in the recognition, characterization, and classification of colorectal polyposis. Most procedures were performed using the Olympus EVIS X1 system with white-light endoscopy (WLE), narrow-band imaging (NBI), magnifying NBI (M-NBI), and NBI with near focus modes, yielding 160 images and accompanying video clips. Approximately eighty percent of cases represent clinically and/or genetically confirmed hereditary polyposis syndromes (PG), including familial adenomatous polyposis (FAP), Peutz-Jeghers syndrome (PJS), juvenile polyposis syndrome (JPS), and ganglioneuroma syndrome (GNS), while the remaining twenty percent comprise non-hereditary polyps and polyp-mimicking lesions with overlapping morphological features (Non-PG), included to support differential classification. Each released record is linked, where available, to standardized endoscopic annotations, representative histopathology, and clinically reported germline findings, forming an AI-ready, patient-level annotation framework. The dataset is publicly accessible at Mendeley (https://doi.org/10.17632/nzyfc544bx.2). For the latest updates and further information, readers are referred to the DataBioX website: https://databiox.com.

摘要：遺傳性息肉症候群可能是結腸直腸癌的前驅病變，並與廣泛的腸外腫瘤相關聯。及早識別和準確分類這些症候群對於及時診斷、個性化病人管理以及針對受影響家庭的監測策略至關重要。然後，公共內視鏡數據集主要圍繞個別散發性息肉組織，而沒有將息肉症表型與患者層級的組織病理學和種系發現聯繫起來。在此，我們呈現ERCPMP-Gx，一個內視鏡、組織病理學和基因組數據集，旨在支持人工智慧（AI）在結腸直腸息肉的識別、特徵描述和分類中的應用。大多數程序使用奧林巴斯EVIS X1系統進行，配備白光內視鏡（WLE）、窄帶成像（NBI）、放大NBI（M-NBI）和近焦模式的NBI，產生160張圖像和伴隨的視頻片段。大約百分之八十的病例代表臨床和/或基因確認的遺傳性息肉症候群（PG），包括家族性腺瘤性息肉症（FAP）、Peutz-Jeghers症候群（PJS）、青少年息肉症候群（JPS）和神經節瘤症候群（GNS），而剩餘的百分之二十則包括非遺傳性息肉和具有重疊形態特徵的息肉模擬病變（Non-PG），以支持差異分類。每個發布的記錄都與標準化的內視鏡註釋、代表性的組織病理學和臨床報告的種系發現相連接，形成一個適合AI的患者層級註釋框架。該數據集可在Mendeley上公開訪問（https://doi.org/10.17632/nzyfc544bx.2）。有關最新更新和進一步信息，讀者可參考DataBioX網站：https://databiox.com。

##### **An Empirical Study of Harness Design for Coding Agents**
2609.20804v1 by Run-Ze Fan, Zihao Zhang, Simin Ma, Yebowen Hu, Shouju Wang, Kaiqiang Song, Fei Liu, Hamed Zamani, Xiaoyang Wang

Coding harnesses shape how autonomous coding agents translate model capabilities into long-horizon software-engineering performance, yet existing work typically evaluates harnesses as monolithic systems, leaving the effectiveness of individual components unclear. To enable component-level comparisons, we study this question with a lightweight coding harness whose execution loop is fixed while three components are varied: planning, action space, and context management. Across four models evaluated on SWE-Bench Verified and Terminal-Bench 2.1, we evaluate 176 matched settings spanning five context-management strategies, four context-window budgets, and targeted ablations of planning and action space. We find that: (1) Context management becomes increasingly valuable as the context-window budget tightens, with most of its benefit coming from preventing context-overflow failures. (2) Staging rule-based elision before LLM-based summarization provides the strongest overall efficiency among the context-management strategies, whereas making elided content recoverable adds machinery that models rarely use and yields no accuracy gain. (3) Planning shifts from an accuracy scaffold for weaker models to a cost saver for stronger models, with little change in accuracy. (4) Predefined tools improve performance for models with weaker bash proficiency, whereas bash-capable models can operate effectively with a bash-only interface and achieve substantially lower cost, especially on command-line-centric tasks. Trajectory-level analysis explains these effects: context management extends execution trajectories without substantially altering agent behavior, planning changes where trajectories stop, and the action space changes the granularity at which code is written. These findings inform model- and budget-aware harness design and provide a modular framework for evaluating future harness components.

摘要：編碼工具的設計影響自主編碼代理如何將模型能力轉化為長期軟體工程表現，但現有的研究通常將編碼工具評估為整體系統，這使得單個組件的有效性不明確。為了實現組件級的比較，我們研究這個問題，使用一個輕量級的編碼工具，其執行循環是固定的，而三個組件則有所變化：規劃、行動空間和上下文管理。在對SWE-Bench Verified和Terminal-Bench 2.1評估的四個模型中，我們評估了176個匹配的設置，涵蓋五種上下文管理策略、四種上下文窗口預算，以及針對規劃和行動空間的針對性刪減。我們發現：（1）隨著上下文窗口預算的收緊，上下文管理變得越來越有價值，其大部分好處來自於防止上下文溢出失敗。（2）在LLM基礎的摘要之前進行階段性基於規則的省略，在上下文管理策略中提供了最強的整體效率，而使省略內容可恢復則增加了模型很少使用的機械，並且沒有帶來準確性的提升。（3）規劃從較弱模型的準確性支架轉變為較強模型的成本節省者，準確性變化不大。（4）預定義的工具提高了對於bash熟練度較弱模型的性能，而具備bash能力的模型則可以有效地使用僅限bash的介面運作，並在以命令行為中心的任務中實現顯著降低的成本。軌跡級分析解釋了這些效果：上下文管理延長了執行軌跡而不顯著改變代理行為，規劃改變了軌跡停止的地方，而行動空間改變了編碼的粒度。這些發現為模型和預算意識的編碼工具設計提供了指導，並為評估未來的編碼工具組件提供了模組化框架。

##### **A Qualitative Model for Reasoning about Path and Support**
2609.20349v1 by Abhishek Jaiswal, Zoe Falomir

Spatial reasoning abilities correlate strongly with performance in STEM fields. Games offer a compelling medium for training these critical skills in developing children who have a natural proclivity for play. However, to facilitate human-like tutoring and player guidance, these games require an AI agent capable of making commonsense inferences from spatial events. Qualitative reasoning (QR) models appear to be a suitable framework for these application domains. As these models reason in symbolic representations, they can seamlessly translate game states into interpretable feedback for human-like player guidance. This paper introduces a hybrid qualitative model designed for Camelot Jr., a block-puzzle game that requires constructing multi-level bridges to connect two avatars stationed on separate towers. The game poses a challenge for the player, who must make platforms stable, plan their path, and ensure they use all the provided blocks. To handle the precise physics required by the domain, we integrate a mathematical center-of-mass stability logic to guide our qualitative solver. Our work facilitates spatial skill training in Camelot Jr. and contributes to the development of human-centric, explainable game-playing agents.

摘要：空間推理能力與STEM領域的表現有著強烈的相關性。遊戲提供了一個引人入勝的媒介，用於培養這些關鍵技能，以幫助那些天生喜愛遊玩的孩子。然而，為了促進類人輔導和玩家指導，這些遊戲需要一個能夠從空間事件中做出常識推斷的AI代理。質性推理（QR）模型似乎是這些應用領域的合適框架。由於這些模型以符號表示進行推理，它們可以無縫地將遊戲狀態轉換為可解釋的反饋，以便進行類人玩家指導。本文介紹了一個為Camelot Jr.設計的混合質性模型，這是一個需要構建多層橋樑以連接駐紮在不同塔樓上的兩個化身的積木拼圖遊戲。這款遊戲對玩家提出了挑戰，玩家必須使平台穩定、規劃路徑，並確保使用所有提供的積木。為了處理該領域所需的精確物理，我們整合了一個數學質心穩定邏輯來指導我們的質性求解器。我們的工作促進了Camelot Jr.中的空間技能訓練，並有助於開發以人為中心的可解釋遊戲代理。

##### **NeuSOGA3D: A Neuro-Symbolic Framework for Explainable 3D Geometric Reconstruction**
2609.20323v2 by Qingde Li, Qingqi Hong, Zihan Li, Jie Tian

Three-dimensional reconstruction from unorganized point clouds remains a challenging problem in computer vision, geometric modeling, and computer-aided design. While neural implicit methods achieve impressive reconstruction accuracy, geometry is typically encoded in latent representations that limit interpretability and reuse within engineering workflows.
  We present NeuSOGA3D (Neuro-Symbolic Observation-Guided Geometric Abstraction in 3D), a hybrid framework that combines learned perceptual priors inherited from NeuSOGA with explicit symbolic geometric reasoning. The method projects point clouds onto principal orthographic planes, constructs symbolic implicit spline representations from the resulting observations, and fuses them through shape-preserving constructive solid geometry operations to generate a coarse visual hull. Additional geometric detail is recovered through cross-sectional decomposition and volumetric reconstruction using Partial Shape-Preserving Splines.
  Unlike conventional neural implicit approaches, NeuSOGA3D progressively transforms observations into explicit symbolic entities, including control polygons, implicit spline fields, cross-sections, and volumetric lofts. Experiments on all forty categories of the ModelNet40 benchmark demonstrate the ability of the framework to recover structurally meaningful and CAD-compatible geometric representations from diverse point-cloud observations. The results highlight the potential of combining learned perception with symbolic geometric reasoning for explainable geometric intelligence.

摘要：三維重建來自無組織點雲仍然是計算機視覺、幾何建模和計算機輔助設計中的一個挑戰性問題。雖然神經隱式方法實現了令人印象深刻的重建精度，但幾何通常以潛在表示的形式編碼，這限制了其在工程工作流程中的可解釋性和重用性。<lb/>  
我們提出了NeuSOGA3D（神經符號觀察引導的三維幾何抽象），這是一個混合框架，結合了來自NeuSOGA的學習感知先驗和明確的符號幾何推理。該方法將點雲投影到主正交平面上，從生成的觀察中構建符號隱式樣條表示，並通過保持形狀的構造實體幾何操作將它們融合以生成粗略的視覺外殼。<lb/>  
通過橫截面分解和使用部分形狀保持樣條的體積重建，恢復額外的幾何細節。與傳統的神經隱式方法不同，NeuSOGA3D逐步將觀察轉換為明確的符號實體，包括控制多邊形、隱式樣條場、橫截面和體積升高。<lb/>  
在ModelNet40基準的四十個類別上進行的實驗展示了該框架從多樣的點雲觀察中恢復結構上有意義且與CAD兼容的幾何表示的能力。結果突顯了將學習感知與符號幾何推理相結合以實現可解釋幾何智能的潛力。

##### **Bridging Modalities on the Cortex: Surface-based MRI to PET Translation with a Diffusion Bridge**
2609.20147v1 by Yitong Li, Alexandra Samoylova, Fabian Bongratz, Timo Grimmer, Dennis M. Hedderich, Igor Yakushev, Christian Wachinger

Cortical hypometabolism measured by Fluorodeoxyglucose Positron Emission Tomography (FDG-PET) is a highly sensitive biomarker for dementia diagnosis. However, high costs, radiation exposure, and limited accessibility constrain its clinical utility. While cross-modal synthesis from Magnetic Resonance Imaging (MRI) offers a promising alternative, existing volumetric generation methods do not explicitly account for the highly folded cortical geometry, where disease-related patterns predominantly reside. To address this, we introduce a novel surface-based diffusion bridge framework DB-SUiT for MRI-to-PET translation that operates natively on the cortical manifold. A conditional Spherical U-shaped vision Transformer (SUiT) is specifically designed to model the intricate cross-modal relationships while preserving surface topology. It combines spherical convolutional encoders for multi-scale surface feature extraction with bottleneck Transformers to capture long-range spatial dependencies, while incorporating demographic and subcortical conditions to refine the synthesis. Evaluated on two datasets, including subjects with different dementia types, DB-SUiT demonstrates high-fidelity synthesis that substantially outperforms other baselines. In automated dementia classification, synthesized PET surfaces improve performance over MRI by 14.2% and PET volumes by 11.3%, approaching the performance of real PET surfaces. In a blinded reader study, synthetic PET achieved 85.5% diagnostic accuracy, compared with 75.8% for MRI and 95.2% for real PET. This further demonstrates cross-cohort and cross-pathology generalization, as the model was evaluated without retraining on an external cohort that included a dementia subtype not represented during training. Our code is available at https://github.com/ai-med/DB-SUiT.

摘要：皮質低代謝量由氟脫氧葡萄糖正子發射斷層掃描（FDG-PET）測量，是一個對於癡呆診斷的高度敏感生物標記。然而，高昂的成本、輻射暴露和有限的可及性限制了其臨床實用性。雖然來自磁共振成像（MRI）的跨模態合成提供了一個有前景的替代方案，但現有的體積生成方法並未明確考慮高度摺疊的皮質幾何結構，而疾病相關模式主要存在於此。為了解決這個問題，我們引入了一種新穎的基於表面的擴散橋接框架DB-SUiT，用於MRI到PET的轉換，該框架在皮質流形上原生運行。一個條件性的球形U型視覺Transformer（SUiT）專門設計用來建模複雜的跨模態關係，同時保留表面拓撲。它結合了球形卷積編碼器以進行多尺度表面特徵提取，並使用瓶頸Transformer來捕捉長距離空間依賴性，同時納入人口統計和皮層下條件以精煉合成。在兩個數據集上進行評估，包括不同癡呆類型的受試者，DB-SUiT顯示出高保真度的合成，顯著超越其他基準。在自動癡呆分類中，合成的PET表面相比MRI提高了14.2%的性能，PET體積提高了11.3%，接近真實PET表面的性能。在一項盲讀者研究中，合成的PET達到了85.5%的診斷準確率，而MRI為75.8%，真實PET為95.2%。這進一步展示了跨隊列和跨病理的泛化，因為該模型在未經重新訓練的外部隊列上進行評估，該隊列包括在訓練期間未代表的癡呆亞型。我們的代碼可在https://github.com/ai-med/DB-SUiT獲得。

##### **PAANI : On Device Visual Evidence Fusion and Explainable Guidance for River Robot Simulation**
2609.22353v1 by Savio Cardoz, Santhiya Rajan

Mobile river monitoring robots must interpret obstacles and water boundaries that geographic waypoints alone cannot describe. On resource constrained platforms, converting imperfect visual predictions into timely and inspectable guidance is a distinct challenge. An object label or steering command does not explain which evidence supports a decision or when that evidence is unreliable. We present PAANI, an on-device perception to guidance architecture that combines a project trained YOLO11n detector and a custom MobileNetV3 Small semantic segmenter with timestamp aligned evidence fusion on Arduino UNO Q. Bounded tracking supplies object persistence, while an explicit corridor policy combines surface labels, accepted detections, urgency and mask uncertainty. Each final advisory exposes its contributing evidence and policy reasons. ROS 2 interfaces connect the local AI pipeline to a separate Gazebo vessel, localization and control testbed. Training uses 10,000 WaterScenes images for four-class detection and 1,127 MaSTr1325 images for segmentation, including 198 segmentation validation images. The selected FP32 ONNX models occupy 14.817 MB. Detector checkpoint test mAP at 0.5 IoU is 0.7388, while the separately evaluated rectangular ONNX export achieves validation mAP at 0.5 IoU of 0.7367. Segmentation ONNX validation mIoU is 0.9750. A five-minute UNO Q recording produced median and 95th percentile pipeline latencies of 467.8 ms and 580.3 ms at a configured 0.5 Hz cadence. The evaluation also identifies black input misclassification and a sampling rate mismatch that prevents the diagnostic apparent motion estimator from collecting sufficient evidence. These results support an inspectable and reusable edge robotics foundation while clearly distinguishing model accuracy and on-board execution from validated on-water collision avoidance.

摘要：移動河流監測機器人必須解釋障礙物和水邊界，而僅靠地理路徑點無法描述這些。<lb/>在資源受限的平台上，將不完美的視覺預測轉換為及時且可檢查的指導是一項獨特的挑戰。<lb/>一個物體標籤或轉向命令並不能解釋哪些證據支持決策或何時該證據不可靠。<lb/>我們提出了PAANI，一種將經過項目訓練的YOLO11n檢測器和自定義的MobileNetV3 Small語義分割器與Arduino UNO Q上的時間戳對齊證據融合相結合的設備內感知到指導架構。<lb/>有界追蹤提供物體持續性，而顯式走廊策略則結合了表面標籤、接受的檢測、緊急性和掩碼不確定性。<lb/>每個最終建議都揭示其貢獻證據和政策原因。<lb/>ROS 2接口將本地AI管道連接到一個單獨的Gazebo船隻、定位和控制測試平台。<lb/>訓練使用了10,000張WaterScenes圖像進行四類檢測，以及1,127張MaSTr1325圖像進行分割，包括198張分割驗證圖像。<lb/>所選的FP32 ONNX模型佔用14.817 MB。<lb/>檢測器檢查點測試的mAP在0.5 IoU下為0.7388，而單獨評估的矩形ONNX導出在0.5 IoU下達到驗證mAP為0.7367。<lb/>分割ONNX驗證的mIoU為0.9750。<lb/>五分鐘的UNO Q錄音產生了中位數和95百分位數的管道延遲，分別為467.8毫秒和580.3毫秒，配置為0.5 Hz的頻率。<lb/>評估還識別出黑色輸入誤分類和取樣率不匹配，這阻止了診斷明顯運動估計器收集足夠的證據。<lb/>這些結果支持可檢查和可重用的邊緣機器人基礎，同時清楚地區分模型準確性和板載執行與經過驗證的水上碰撞避免。

##### **Reproducibility is not construct validity: LLM measurement of institutionally situated communication**
2609.19866v1 by Veronika Batzdorfer, Carlo Romano Marcello Alessandro Santagiustina

High annotation reproducibility does not necessarily imply that an LLM-inferred measure captures the construct it is intended to measure. We test this distinction using a dataset from the European Commission's AI Act consultation, linking structured survey responses to free-text consultation submissions from the same stakeholders. LLM annotations of consultation submissions are highly reproducible (intraclass correlations > 0.99), yet show limited convergence with survey-reported measures of the nominal construct they were intended to approximate. Divergence between survey-and LLM-inferred text-based measures varies systematically across stakeholder groups: business associations express greater concern about AI risks in text-based consultations than in survey responses ({g} = +1.0), whereas public authorities and several nonbusiness groups show smaller or negative divergences. Divergences between scores suggest positive spatial autocorrelation across European countries (Moran's I = 0.347, p = 0.036), indicating that stakeholders from neighboring countries tend toward more similar text-based stances towards AI safety concerns. Despite divergence, survey-reported concerns remain strongly associated with support for explainability across all divergence levels. These results demonstrate that LLM annotation reproducibility can coexist with poor construct correspondence and motivate validation procedures that distinguish reproducibility, construct validity, and communication context variation when LLMs are used as measurement instruments.

摘要：高註解可重複性並不一定意味著 LLM 推斷的量度捕捉了其所意圖測量的構念。我們使用來自歐洲委員會 AI 法案諮詢的數據集來測試這一區別，將結構化的調查回應與來自相同利益相關者的自由文本諮詢提交相連結。LLM 對諮詢提交的註解具有高度的可重複性（組內相關係數 > 0.99），但與調查報告的名義構念量度之間的收斂性有限。調查與 LLM 推斷的基於文本的量度之間的差異在利益相關者群體中系統性地變化：商業協會在基於文本的諮詢中對 AI 風險表達的關注程度高於調查回應（{g} = +1.0），而公共當局和幾個非商業團體則顯示出較小或負的差異。分數之間的差異表明歐洲國家之間存在正的空間自相關（Moran's I = 0.347, p = 0.036），這表明來自鄰近國家的利益相關者在對 AI 安全問題的基於文本的立場上傾向於更相似。儘管存在差異，調查報告的關注仍然與各種差異水平的可解釋性支持強烈相關。這些結果表明，LLM 註解的可重複性可以與構念對應的不足共存，並促使驗證程序的進行，以區分可重複性、構念效度和溝通背景變異，當 LLM 被用作測量工具時。

##### **Large Language Model Agents for Evidence Based Genetic Disease Severity Classification**
2609.19569v2 by Tohid Ghasemnejad, Ahmadreza Argha, Mark Grosser, John Wang, Min Yang, Thantrira Porntaveetus, Tony Roscioli, Nigel H. Lovell, Mahmoud Aarabi, Hamid Alinejad-Rokny

Disease severity classification for genetic conditions is subjective and labor-intensive, creating bottlenecks in genomic screening, where commercial panels vary widely in size and overlap. We developed an autonomous AI agent integrating Reasoning and Acting (ReAct) with Retrieval-Augmented Generation (RAG) to classify 10,211 Human Phenotype Ontology terms. It uses American College of Medical Genetics (ACMG)-endorsed severity guidelines and American College of Obstetricians and Gynecologists (ACOG) quality-of-life criteria to retrieve PubMed literature, generate interpretable reasoning chains, and independently verify claims. At the phenotype level, using expert-curated cohorts, the agent achieved 93.55% accuracy (MCC 0.9237) with 82.6% to 91.4% of claims supported by direct evidence or valid inferences. Gene-level severity was aggregated across 8,738 pairs, identifying 3,283 autosomal recessive pairs with severe or profound presentations. External validation showed 95.2% concordance with Mackenzie's Mission gene list. This system enables standardized panel design by providing reliable, automated classification supported by direct evidence.

摘要：疾病嚴重程度的分類對於遺傳條件來說是主觀且勞動密集的，這在基因組篩查中造成了瓶頸，因為商業面板的大小和重疊程度差異很大。我們開發了一個自主的 AI 代理，將推理和行動 (ReAct) 與檢索增強生成 (RAG) 整合，以對 10,211 個人類表型本體術語進行分類。它使用美國醫學遺傳學會 (ACMG) 認可的嚴重程度指導方針和美國婦產科醫學會 (ACOG) 的生活質量標準來檢索 PubMed 文獻，生成可解釋的推理鏈，並獨立驗證主張。在表型層面上，使用專家策劃的隊列，該代理達到了 93.55% 的準確率 (MCC 0.9237)，其中 82.6% 至 91.4% 的主張得到了直接證據或有效推論的支持。基因層面的嚴重程度在 8,738 對中進行了匯總，識別出 3,283 個具有嚴重或深度表現的常染色體隱性對。外部驗證顯示與 Mackenzie's Mission 基因名單的符合率為 95.2%。該系統通過提供可靠的、基於直接證據的自動化分類，實現了標準化面板設計。

##### **A Multi-Modal Generative Model for Tomato Disease Leaves Understanding**
2609.19555v1 by Khang Nguyen Quoc, Minh-Phuoc Tran, Gia-Han Truong, Luyl-Da Quach

Artificial intelligence for plant disease analysis has advanced from task-specific classifiers to multi-modal models capable of jointly interpreting visual and textual information. However, practical deployment in precision agriculture remains limited because most existing approaches treat disease understanding as isolated prediction tasks, failing to capture the complementary relationships among symptom recognition, severity assessment, and question-driven diagnostic reasoning. In tomato pathology, accurate interpretation of diseased leaves requires more than label prediction; it demands integrating visual symptoms with semantic context to support a comprehensive and explainable understanding. Here, we present SOLAR, a multimodal generative model that understands tomato disease spanning six question-answering tasks. SOLAR learns to align visual features with task-aware language representations by Fusion Expert module based on mixture-of-expert, enabling it to generate contextually relevant answers across diverse diagnostic tasks. By formulating tomato disease analysis as a generative Visual Question Answering (VQA) task, SOLAR provides a flexible framework that supports multi-task inference within a single model while improving performance and cross-task knowledge sharing. We evaluate SOLAR on $41,677$ images, including $216,209$ Question-Answering (QA) pairs to understand tomato leaf disease under both closed and open-ended QA settings. Experimental results show that SOLAR consistently outperforms state-of-the-art vision-only, vision-language, and task-specific models across all tasks, demonstrating superior accuracy, robustness, and multimodal reasoning. These findings highlight the potential of generative multimodal modeling as an effective direction for understanding of plant disease. The code for this study is available at https://github.com/EnalisUs/SOLAR.

摘要：人工智慧在植物疾病分析方面已經從特定任務的分類器發展到能夠共同解釋視覺和文本信息的多模態模型。<lb/>然而，實際在精準農業中的應用仍然有限，因為大多數現有的方法將疾病理解視為孤立的預測任務，未能捕捉到症狀識別、嚴重程度評估和問題驅動的診斷推理之間的互補關係。<lb/>在番茄病理學中，準確解釋病變葉片需要的不僅僅是標籤預測；它要求將視覺症狀與語義上下文整合，以支持全面且可解釋的理解。<lb/>在這裡，我們介紹SOLAR，一個多模態生成模型，能夠理解涵蓋六個問答任務的番茄疾病。<lb/>SOLAR通過基於專家混合的融合專家模塊學習將視覺特徵與任務感知的語言表示對齊，使其能夠在各種診斷任務中生成上下文相關的答案。<lb/>通過將番茄疾病分析表述為生成的視覺問答（VQA）任務，SOLAR提供了一個靈活的框架，支持在單一模型中進行多任務推理，同時提高性能和跨任務知識共享。<lb/>我們在$41,677$張圖像上評估SOLAR，包括$216,209$個問答（QA）對，以了解在封閉和開放式QA設置下的番茄葉病。<lb/>實驗結果顯示，SOLAR在所有任務中始終超越最先進的僅視覺、視覺-語言和特定任務模型，展現出卓越的準確性、穩健性和多模態推理能力。<lb/>這些發現突顯了生成多模態建模作為理解植物疾病的有效方向的潛力。<lb/>本研究的代碼可在 https://github.com/EnalisUs/SOLAR 獲得。

##### **Predict Before You Deploy: Offline Prediction of Quantization-Induced Task Degradation for World Action Models**
2609.19441v1 by Jiuyi Xu, Jinjia Guo, Meida Chen, Jing Du, Yangming Shi

World action models (WAMs) rely on video-generation backbones, requiring substantial memory and compute for deployment. Post-training quantization reduces memory and can accelerate inference, but bit width, grouping, and quantizer choice define a large configuration space. Identifying configurations that preserve task performance through exhaustive closed-loop evaluation is costly. We propose PreDE (Predict Before You Deploy), a policy-calibrated framework for predicting quantization-induced task degradation from offline action deviations. Using closed-loop outcomes from a small development set, PreDE calibrates two thresholds and accepts, rejects, or defers new configurations using a fixed observation log. Under a within-setting label-ordering hypothesis, the rule issues decisions where all thresholds consistent with the development labels agree. Across five WAMs and four benchmark settings, quantization produces configuration-dependent task losses that cannot be explained by bit width alone or a shared deviation threshold. Across 28 held-out configurations from two policies, PreDE issued 21 decisions before observing closed-loop outcomes (75% coverage), all matching the observed acceptable or degraded labels. Deferred candidates included both acceptable outcomes and a 33-percentage-point loss. In 450 Franka Research 3 trials across two independently fine-tuned policies, all configurations assigned to high-deviation groups before testing showed significant degradation, while low-deviation comparisons showed no statistically significant degradation. On the real robot, W4A4 achieved a 1.37x action-query speedup and approximately 44% lower peak memory. These results support policy-specific behavioral calibration for quantization configuration selection while identifying candidates that require closed-loop evaluation. The code is available at https://github.com/jiuyixu25/PreDE.

摘要：世界行動模型（WAMs）依賴於視頻生成骨幹，這需要大量的內存和計算資源來進行部署。訓練後量化可以減少內存並加速推理，但位寬、分組和量化器的選擇定義了一個龐大的配置空間。通過徹底的閉環評估來識別保留任務性能的配置是成本高昂的。我們提出了PreDE（預測後再部署），這是一個政策校準框架，用於從離線行動偏差中預測量化引起的任務退化。利用來自小型開發集的閉環結果，PreDE校準了兩個閾值，並使用固定的觀察日誌接受、拒絕或延遲新的配置。在一個內部設定的標籤排序假設下，該規則在所有與開發標籤一致的閾值同意的情況下發出決策。在五個WAMs和四個基準設定中，量化產生的任務損失依賴於配置，這不能僅通過位寬或共享偏差閾值來解釋。在來自兩個政策的28個保留配置中，PreDE在觀察到閉環結果之前發出了21個決策（75% 覆蓋率），所有決策均與觀察到的可接受或退化標籤相匹配。延遲的候選者包括可接受的結果和33個百分點的損失。在450次Franka Research 3的試驗中，所有在測試前被分配到高偏差組的配置顯示出顯著的退化，而低偏差的比較則未顯示出統計上顯著的退化。在真實機器人上，W4A4實現了1.37倍的行動查詢加速和約44%的峰值內存降低。這些結果支持針對量化配置選擇的政策特定行為校準，同時識別需要閉環評估的候選者。代碼可在 https://github.com/jiuyixu25/PreDE 獲得。

##### **Can Vision-Language Models Judge Olympic Diving? From Reasoning to Scores in Zero-Shot Action Quality Assessment**
2609.19354v1 by Henry O. Velesaca, David Freire-Obregon, Luigi Miranda, Abel Reyes-Angulo

Automated action quality assessment (AQA) in Olympic sports remains a challenging task due to the complexity of human motion and the subjectivity inherent in expert judging. This work evaluates the capability of open-source Vision-Language Models (VLMs) to perform zero-shot action quality assessment on Olympic diving videos using the AQA-7 benchmark dataset. In this regard, a regression-based framework is pro-posed to leverage both the semantic reasoning and phase-level sub-scores generated by the VLMs, combining TF-IDF vectorization, dimensionality reduction, and ensemble learning to predict final competition scores. Experimental results show that standalone VLMs achieve moderate Spearman correlations below 0.32, while the proposed ensemble regression framework substantially improves performance in the reported evaluation, reaching a Spearman correlation of 0.67 with a four-model configuration. Textual reasoning features con-sistently outperformed raw numerical sub-scores, highlighting the richness of VLM-generated explanations for action quality analysis. These findings suggest that VLMs hold strong potential as assistive tools for explainable and semi-automated sports performance evaluation. The code is publicly available on GitHub https://github.com/hvelesaca/olympic diving judge vlm

摘要：自動化動作質量評估（AQA）在奧林匹克運動中仍然是一項具有挑戰性的任務，這是由於人類動作的複雜性和專家評判中固有的主觀性。這項工作評估了開源視覺-語言模型（VLMs）在使用 AQA-7 基準數據集對奧林匹克跳水視頻進行零樣本動作質量評估的能力。在這方面，提出了一種基於回歸的框架，以利用 VLMs 生成的語義推理和階段級子分數，結合 TF-IDF 向量化、降維和集成學習來預測最終比賽分數。實驗結果顯示，獨立的 VLMs 在斯皮爾曼相關性上達到0.32以下的中等水平，而所提出的集成回歸框架在報告的評估中顯著提高了性能，達到0.67的斯皮爾曼相關性，使用四個模型的配置。文本推理特徵始終優於原始數字子分數，突顯了 VLM 生成的解釋在動作質量分析中的豐富性。這些發現表明，VLMs 作為可解釋和半自動化體育表現評估的輔助工具具有強大的潛力。代碼已在 GitHub 上公開，網址為 https://github.com/hvelesaca/olympic diving judge vlm

##### **Prepared Or Unprepared? Evaluating Healthcare Workforce Readiness for Clinical Adoption of Artificial Intelligence in Nigeria**
2609.19096v1 by Abbas M. Rabiu, Abdulrazaq A. Zubair, Um-mulkhairi Ibrahim, Tolulope Olusuyi, Shaheeda Farouq, Safwan M. Dafi, Adaobi C. Emegoakor, Yewande Gbadamosi, Maruf Adewole

Artificial intelligence (AI) is increasingly integrated into healthcare systems worldwide, yet its successful clinical adoption depends critically on workforce readiness, particularly in low- and middle-income countries (LMICs) where infrastructural and training gaps persist. This cross-sectional study evaluated awareness, attitudes, preparedness, and barriers to AI adoption among 761 healthcare professionals across multiple disciplines and practice settings in Nigeria. Data were collected between December 2025 and March 2026 using a structured, validated questionnaire. Overall awareness of AI in healthcare was high (92.6%); however, objective knowledge and self-reported preparedness remained limited, with 40.9% reporting low or very low knowledge and only 63.0% feeling adequately prepared. Willingness to adopt AI was high: 92.5% expressed interest in training, and 78.7% supported inclusion of AI education in undergraduate curricula. Key barriers included lack of training (84.7%), poor infrastructure (71.1%), high cost of AI tools (61.0%), fear of job displacement (60.6%), ethical concerns (52.9%), and data privacy concerns (52.7%). Significant differences in preparedness were observed across geopolitical zones (chi-square (5) = 24.28, p < 0.001), and awareness differed across professional groups (chi-square (6) = 68.38, p < 0.001). Attitudes toward AI differed significantly across professional groups (F = 3.32, p = 0.003), with professionals who felt prepared demonstrating more positive attitudes (mean = 3.74) compared to those who did not (mean = 3.46). These findings reveal a critical disconnect between high awareness and actual readiness, underscoring the need for targeted training, infrastructure investment, and clear implementation frameworks to bridge the gap between AI technological potential and clinical reality in resource-constrained settings.

摘要：人工智慧（AI）正日益融入全球的醫療系統，但其成功的臨床採用在很大程度上依賴於人力資源的準備程度，特別是在基礎設施和培訓存在缺口的低收入和中等收入國家（LMICs）。本橫斷面研究評估了761名來自尼日利亞多個學科和實踐環境的醫療專業人員對AI的認識、態度、準備情況和採用障礙。數據於2025年12月至2026年3月期間通過結構化的經驗驗證問卷收集。醫療領域對AI的整體認識很高（92.6%）；然而，客觀知識和自我報告的準備程度仍然有限，40.9%的人報告知識低或非常低，只有63.0%的人感到準備充分。對於採用AI的意願很高：92.5%的人表達了對培訓的興趣，78.7%的人支持在本科課程中納入AI教育。主要障礙包括缺乏培訓（84.7%）、基礎設施差（71.1%）、AI工具的高成本（61.0%）、對工作取代的恐懼（60.6%）、倫理問題（52.9%）和數據隱私問題（52.7%）。在地緣政治區域之間觀察到準備程度的顯著差異（卡方檢驗（5）= 24.28，p < 0.001），而不同專業群體之間的認識也有所不同（卡方檢驗（6）= 68.38，p < 0.001）。對AI的態度在不同專業群體之間顯著不同（F = 3.32，p = 0.003），感到準備充分的專業人員表現出更積極的態度（平均 = 3.74），相比之下，感到不充分的專業人員的平均值為3.46。這些發現揭示了高認識與實際準備之間的關鍵脫節，強調了需要針對性的培訓、基礎設施投資和明確的實施框架，以縮小AI技術潛力與資源有限環境中的臨床現實之間的差距。

##### **Reporting Practice Matters: The Impact of Reference Choice on Chest X-ray Report Evaluation**
2609.19093v1 by Daniel P. Jeong, Charles Q. Li, Hossein Hosseiny, Nitya M. Bhalla, Fatma Uyar Morency, Pradeep Ravikumar, Zachary C. Lipton, Michael Oberst

Radiologists follow heterogeneous reporting practices. Two radiologists examining the same image and identifying the same clinical findings might nevertheless compose superficially distinct reports, varying in terminology, shorthand, formatting, and level of detail. These variations in reporting norms represent an under-appreciated obstacle in efforts to evaluate AI-based radiology report generation (RRG) models, where machine-generated reports are typically assessed based on their concordance with human-generated references. In this paper, we quantify the sensitivity of established evaluation metrics to variations in reporting practices, revealing impacts large enough to alter the rankings of models. We introduce a radiologist-informed taxonomy of variations in radiology reporting practice and a method (ReRef) that rewrites reference reports along the axes of our taxonomy while preserving clinical interpretation. For instance, when comparing the performance of nine RRG models on MIMIC-CXR using RadCliQ-v1, condensing the discussion of normal findings in the reference reports causes Libra to drop from first to second place while CheXOne rises from third to first. Our results suggest that many current metrics fail to decouple clinical interpretation from conformity to reporting practices and that choosing the ``right'' references that accurately reflect the desired reporting practices can be important in practice. To support future research, we release MIMIC-CXR-Ext-ReRef, a radiologist-validated dataset of 120 (original, alternative) reference report pairs derived from MIMIC-CXR.

摘要：放射科醫生遵循異質的報告實踐。兩位放射科醫生檢查同一影像並識別相同的臨床發現，然而他們可能仍然撰寫表面上不同的報告，這些報告在術語、簡寫、格式和細節程度上有所不同。這些報告規範的變異代表了一個被低估的障礙，對於評估基於人工智慧的放射報告生成（RRG）模型的努力來說，機器生成的報告通常是根據其與人類生成的參考報告的一致性來評估的。在本文中，我們量化了既定評估指標對報告實踐變異的敏感性，揭示出足以改變模型排名的影響。我們引入了一個基於放射科醫生的放射報告實踐變異分類法以及一種方法（ReRef），該方法在我們的分類法軸上重寫參考報告，同時保留臨床解釋。例如，當使用RadCliQ-v1比較九個RRG模型在MIMIC-CXR上的表現時，對參考報告中正常發現的討論進行簡化，導致Libra從第一名降至第二名，而CheXOne則從第三名上升至第一名。我們的結果表明，許多當前的指標未能將臨床解釋與報告實踐的遵循分開，並且選擇準確反映所需報告實踐的“正確”參考在實踐中可能是重要的。為了支持未來的研究，我們發布了MIMIC-CXR-Ext-ReRef，這是一個經過放射科醫生驗證的數據集，包含120對（原始、替代）參考報告對，來源於MIMIC-CXR。

##### **Probabilistic Linear Explanations**
2609.19077v1 by Frederic Koriche, Jean-Marie Lagniez, Chi Tran

Formal explainability provides mathematically grounded justifications for individual predictions. However, abductive explanations often exceed human cognitive limits by involving too many features, while probabilistic relaxations have remained largely limited to categorical classification. We present a unified framework for probabilistic explainability based on sparse, anchored linear models, applicable to both binary classification and continuous regression. By mapping instances to the Boolean hypercube, our linear explanations strictly generalize subset-based approaches: they capture both the magnitude and direction of feature contributions while enforcing a prescribed sparsity budget $k$. We show that minimizing the relevance error for such explanations is \ClassNPPP-hard when the underlying model is a neural network, and we relate this intractable objective to a tractable surrogate---the fidelity error. For a parameterized family of local distributions, the relevance error of any $k$-sparse explanation is bounded by its fidelity error up to a multiplicative factor that remains small locally. We address the resulting empirical problem using two complementary approaches: a Mixed Integer Programming (MIP) formulation that yields provably optimal empirical solutions while maintaining polynomial sample complexity, and a polynomial-time Iterative Hard Thresholding (IHT) algorithm with provable approximation guarantees. Empirical evaluations show that, unlike state-of-the-art baselines such as LIME and MAPLE, our explanations satisfy both the anchoring and sparsity constraints by construction, while consistently achieving lower relevance error.

摘要：正式的可解釋性提供了對個別預測的數學基礎的合理解釋。<lb/>然而，溯因解釋往往超出了人類的認知極限，因為涉及的特徵過多，而概率放寬在很大程度上仍然僅限於類別分類。<lb/>我們提出了一個基於稀疏、錨定線性模型的概率可解釋性統一框架，適用於二元分類和連續回歸。<lb/>通過將實例映射到布爾超立方體，我們的線性解釋嚴格地概括了基於子集的方法：它們捕捉了特徵貢獻的大小和方向，同時強制執行預定的稀疏預算 $k$。<lb/>我們展示了當基礎模型為神經網絡時，最小化此類解釋的相關性誤差是 \ClassNPPP-困難的，並將這一難以處理的目標與一個可處理的替代目標——保真誤差相關聯。<lb/>對於一個參數化的局部分佈族，任何 $k$-稀疏解釋的相關性誤差都被其保真誤差所限制，且乘數因子在局部保持較小。<lb/>我們使用兩種互補的方法來解決這一實證問題：一種混合整數規劃（MIP）公式，能夠產生可證明的最佳實證解決方案，同時保持多項式樣本複雜度；另一種是具有可證明近似保證的多項式時間迭代硬閾值（IHT）算法。<lb/>實證評估顯示，與 LIME 和 MAPLE 等最先進的基準不同，我們的解釋在構造上滿足錨定和稀疏約束，同時始終實現較低的相關性誤差。

##### **Double descent is the principle of least action**
2609.19076v2 by Congzhou M Sha

The test error of a model plotted against its number of parameters $d$ falls, peaks when the model can just fit the training data, and falls again, exhibiting the double descent phenomenon. We explain the phenomenon with statistical mechanics. The training trajectory of a stochastic gradient-based method is a particle wandering over the energy landscape of the training loss at an induced temperature $T$, and a run that has equilibrated visits every parameter vector of a given training loss equally often, the fundamental postulate of statistical mechanics, with probability given by the Boltzmann distribution. Because training starts at an initial point and has only finite time to diffuse, it carries an effective weight decay, which makes every parameter a quadratic degree of freedom. The equipartition theorem then distributes the energy among the $d$ degrees of freedom in shares of $T/2$, so at a fixed training loss adding parameters lowers the temperature and drives the Boltzmann distribution toward the stationary path. Finally, adding parameters can only lower the $L^2$ norm of the stationary path, so a solution sampled at fixed loss is less likely to be large with increasing $d$, effectively increasing weight regularization.

摘要：模型的測試錯誤與其參數數量 $d$ 的關係圖呈現下降趨勢，當模型剛好能擬合訓練數據時達到峰值，然後再次下降，顯示出雙重下降現象。 我們用統計力學來解釋這一現象。 基於隨機梯度的方法的訓練軌跡就像是一個粒子在訓練損失的能量景觀上隨機遊走，溫度為 $T$，而一個已經達到平衡的運行會以相等的頻率訪問給定訓練損失的每個參數向量，這是統計力學的基本假設，概率由玻爾茲曼分佈給出。 因為訓練是從一個初始點開始的，並且只有有限的時間進行擴散，它攜帶了一個有效的權重衰減，這使得每個參數成為一個二次自由度。 然後，等分配定理將能量在 $d$ 個自由度之間分配，每個自由度的能量為 $T/2$，因此在固定的訓練損失下，增加參數會降低溫度並驅動玻爾茲曼分佈朝向穩定路徑。 最後，增加參數只能降低穩定路徑的 $L^2$ 範數，因此在固定損失下抽樣的解隨著 $d$ 的增加而不太可能變得很大，有效地增加了權重正則化。

##### **RLLBC-Lib: An Educational Code Library for Reinforcement Learning and Learning-Based Control**
2609.19074v1 by Bernd Frauenknecht, Emma Cramer, Artur Eisele, Paul Kruse, Lukas Kesper, Jonas Hertrampf, Ramil Sabirov, Jyotirmaya Patra, Johannes Berger, Paul Brunzema, Friedrich Solowjow, Sebastian Trimpe

Reinforcement learning (RL) is an exciting concept as well as a remarkable success story worth sharing. However, RL builds on rather complex interactions between different objects that play out over several cycles. Such dynamics are often best explained with an easily accessible implementation. We present RLLBC-Lib, a carefully crafted code library with the goal of lowering the entry barrier for students and other learners of RL in the context of learning-based control. At its heart, RLLBC-Lib comprises a comprehensive library of tabular RL approaches to enforce a clear understanding of the theoretical foundations. A deep RL library follows the same design principles, underscoring the parallels between simple tabular and state-of-the-art deep RL approaches. Additionally, RLLBC-Lib provides a collection of implementations illustrating core RL principles and contrasting RL to other learning-based control approaches. Finally, RLLBC-Lib provides an ideal basis for creating programming assignments with automated grading.

摘要：強化學習（RL）是一個令人興奮的概念，也是值得分享的卓越成功故事。<lb/>然而，RL 建立在不同物件之間相當複雜的互動之上，這些互動在幾個循環中展開。<lb/>這種動態通常最好用一個易於訪問的實現來解釋。<lb/>我們介紹 RLLBC-Lib，這是一個精心設計的代碼庫，旨在降低學生和其他 RL 學習者在基於學習的控制上下的入門門檻。<lb/>在其核心，RLLBC-Lib 包含一個全面的表格 RL 方法庫，以強化對理論基礎的清晰理解。<lb/>深度 RL 庫遵循相同的設計原則，強調簡單表格方法和最先進的深度 RL 方法之間的相似性。<lb/>此外，RLLBC-Lib 提供了一系列實現，說明核心 RL 原則並將 RL 與其他基於學習的控制方法進行對比。<lb/>最後，RLLBC-Lib 提供了一個理想的基礎，用於創建具有自動評分的編程作業。

##### **Decodable but Misrouted: Sparse Features Uncover a Readout Gap in Vision-Language Models for Harmful Meme Detection**
2609.18860v1 by Girish A. Koushik, Diptesh Kanojia, Helen Treharne

When a large vision-language model misclassifies a harmful meme, the failure may reflect missing internal evidence or an inability to route represented evidence to its output. We distinguish these cases in Gemma-3 and Qwen3.5 using sparse autoencoders, role-conditioned probes, causal interventions, and recovery experiments across six harmful content benchmarks, with additional Spanish and Hindi-English code-mixed evaluations. Sparse readouts outperform native prediction on all six primary binary tasks: Qwen averages $0.740$ versus $0.432$ native macro-F1, while residual reconstruction reaches $0.486$, whereas Gemma improves from $0.532$ to $0.714$. These differences reflect supervised accessibility rather than a pre-existing, native decision rule, and the most influential token role depends on the task. Under the evaluated score scales, Qwen silent-feature ablation is $24-63$ times more probe-sensitive, whereas routed-feature patching on literal yes/no tasks is $16-140$ times more output-sensitive. Calibration-only routing recovers $93.3$% of the mean gap, and probe-distilled LoRA improves native predictions, although shared multi-task adaptation causes negative transfer. A case study of Gemma-3-12B on Facebook Hateful Memes finds a distributed rank-32 image-prompt interaction, reaching $0.756$ versus $0.685$ native macro-F1. Robustness controls show that the signal extends beyond English, is not explained solely by accompanying OCR, and depends on paired visual evidence. Thus, routing, rather than representation alone, is a recurring bottleneck in harmful meme classification.

摘要：當一個大型的視覺-語言模型錯誤分類一個有害的迷因時，這種失誤可能反映出缺失的內部證據或無法將所表示的證據傳遞到其輸出。我們在 Gemma-3 和 Qwen3.5 中區分這些情況，使用稀疏自編碼器、角色條件探針、因果干預和在六個有害內容基準上的恢復實驗，並進行額外的西班牙語和印地語-英語混合評估。稀疏讀出在所有六個主要二元任務上超越了本地預測：Qwen 的平均值為 $0.740$，而本地宏-F1 為 $0.432$，同時殘差重建達到 $0.486$，而 Gemma 從 $0.532$ 提升至 $0.714$。這些差異反映了監督可及性，而非預先存在的本地決策規則，並且最具影響力的標記角色取決於任務。在評估的分數範疇下，Qwen 的靜默特徵消融對探針的敏感度是 $24-63$ 倍，而在字面是/否任務上的路由特徵修補則對輸出的敏感度是 $16-140$ 倍。僅進行校準的路由恢復了 $93.3$% 的平均差距，探針提煉的 LoRA 改進了本地預測，儘管共享的多任務適應會導致負轉移。對 Gemma-3-12B 在 Facebook Hateful Memes 上的案例研究發現了一個分佈的排名-32 圖像提示交互，達到 $0.756$ 對比 $0.685$ 的本地宏-F1。穩健性控制顯示信號超越英語，不僅僅由伴隨的 OCR 解釋，並且依賴於配對的視覺證據。因此，路由而非僅僅是表示，是有害迷因分類中的一個反覆瓶頸。

##### **Dimensionality reduction for AI based hyperspectral image classification based on XAI**
2609.22333v1 by Vladimir Zeljković, Branka Stojanović, Harald Ganster, Aleksandar Nešković

This research addresses the challenge of limited material recycling in wood recycling processes by leveraging artificial intelligence (AI)-based dimensionality reduction. Our study explores the application of convolutional neural networks (CNNs) in multi-channel hyperspectral imaging (HSI), extending beyond RGB channels to over 200 spectral channels. Dimensionality reduction within this context involves streamlining the feature space for AI system training and inference. Focusing on explainable AI (XAI) methods, this paper contributes to a broader research initiative, presenting a solution framework that enhances the sustainability and efficiency of wood recycling processes.

摘要：這項研究通過利用基於人工智慧（AI）的降維技術，解決了木材回收過程中材料回收有限的挑戰。<lb/>我們的研究探討了卷積神經網絡（CNN）在多通道高光譜成像（HSI）中的應用，超越了RGB通道，達到200多個光譜通道。<lb/>在這個背景下，降維涉及簡化特徵空間，以便於AI系統的訓練和推理。<lb/>本論文專注於可解釋的人工智慧（XAI）方法，為更廣泛的研究計畫做出貢獻，提出了一個解決方案框架，提升了木材回收過程的可持續性和效率。

##### **Version- and Scope-Aware Question Answering over Normative Documents: A Deployed System and an End-to-End Evaluation at Production Scale**
2609.18769v1 by Liuyin Wang, Shuaipeng Jin, Jiwei Shi, Jensen Hsu

Correctly answering a question grounded in normative documents often depends on information outside any single passage: whether the retrieved document is the version currently in force; whether it applies to the jurisdiction, subject (such as an institution or applicant), and date at issue; and whether each normative claim can be traced to its supporting source text. Hosted retrieval services have substantially lowered the engineering cost of building an initial system over such corpora, making "upload the documents and ask" a common default. We evaluate this default on approximately 73,000 candidate normative documents supplied to a production deployment. The evaluation uses a stratified sample of 200 questions from our published benchmark, with a gold source document for every question; the released sampling rule reads no system outputs or scores. We compare the hosted service with a governed system that resolves version and scope through explicit rules before generation. The governed system scored 97.7 overall, while the hosted service scored 88.1, a gap of 9.6 points computed from unrounded means. The question set, the answer text evaluated for both systems, the scores, and the scripts used to reproduce the reported benchmark statistics are public. The governed configuration has operated as a commercial product since January 2026 and serves 1,126 registered users; named customer organizations include Zhipu AI and Lecheng Health. By mid-April 2026, it had reached roughly 100,000 calls per workday.

摘要：正確回答基於規範文件的問題往往依賴於任何單一段落之外的信息：檢索到的文件是否是目前有效的版本；它是否適用於相關的管轄區、主題（例如機構或申請者）和相關日期；以及每個規範主張是否可以追溯到其支持的源文本。托管檢索服務大幅降低了在這些語料庫上建立初始系統的工程成本，使得「上傳文件並詢問」成為常見的默認選擇。我們對大約73,000份供應給生產部署的候選規範文件評估了這一默認選擇。評估使用了我們已發表基準中的200個問題的分層樣本，對於每個問題都有一份金源文件；發布的抽樣規則不讀取任何系統輸出或分數。我們將托管服務與一個通過明確規則在生成之前解決版本和範圍的管理系統進行比較。管理系統的總分為97.7，而托管服務的分數為88.1，兩者之間的差距為9.6分，計算基於未四捨五入的平均值。問題集、對兩個系統評估的答案文本、分數以及用於重現報告基準統計的腳本都是公開的。自2026年1月以來，管理配置已作為商業產品運行，服務1,126名註冊用戶；命名的客戶組織包括智譜AI和樂成健康。到2026年4月中旬，它的工作日呼叫量已達到約100,000次。

##### **PAPC: Platform Mediation for Privacy-Propagation Externalities in AI-Mediated Workflows**
2609.19226v1 by Tao Huang, Guosen Wu, Chen Hou, Guolong Zheng

AI-mediated platforms coordinate work through LLM agents acting for different principals. In these workflows, privacy loss can be created before a final answer appears: a memory write, shared-workspace update, inter-agent message, or tool event may impose downstream exposure cost on another principal. We model this failure mode as a privacy-propagation externality, where the cost of a raw disclosure depends on topology and fanout as well as content. We present PAPC, a platform-mediated mechanism that intercepts information-moving events before they update shared state or external channels. PAPC combines policy, provenance, topology/fanout, privilege, and content signals to allow an event, release a policy-safe abstraction, quarantine raw content, block a transition, or narrow onward rights. The model explains why final-output control misses intermediate exposure costs and why high-fanout objects amplify propagation. Across retrieval-memory and multi-agent workflow benchmarks, PAPC preserves deterministic task completion and eliminates measured exact raw-value and external raw-value exposure. The results position event-level mediation as a platform-governance primitive for agent-mediated online work.

摘要：AI介導的平台透過為不同委託人行事的LLM代理協調工作。在這些工作流程中，隱私損失可能在最終答案出現之前就已產生：記憶寫入、共享工作區更新、代理之間的消息或工具事件可能對另一個委託人施加下游暴露成本。我們將這種失敗模式建模為隱私傳播外部性，其中原始披露的成本取決於拓撲和傳播程度以及內容。我們提出了PAPC，一種平台介導的機制，能在信息移動事件更新共享狀態或外部通道之前攔截這些事件。PAPC結合政策、來源、拓撲/傳播程度、特權和內容信號，以允許一個事件、釋放一個政策安全的抽象、隔離原始內容、阻止轉換或縮小後續權利。該模型解釋了為什麼最終輸出控制忽略了中間暴露成本，以及為什麼高傳播對象會放大傳播。在檢索記憶和多代理工作流程基準中，PAPC保持確定性的任務完成，並消除了測量的精確原始值和外部原始值的暴露。這些結果將事件級別的調解定位為代理介導的在線工作的平臺治理原語。

##### **Generalist-Specialist Mixture-of-Experts for Rare Pathology Detection in Multimodal Imaging**
2609.18688v1 by Johannes Kaiser, Florian Braunmiller, Daniel Rückert, Georgios Kaissis

AI models for multimodal medical imaging must balance modality-specific specialization with cross-modal shared representations, a trade-off that pure Mixture-of-Experts (MoE) architectures currently fail to satisfy. Expert-based routing improves in-domain learning but may sacrifice cross-modal signals, which appear particularly important for rare (low-prevalence) pathologies in our experiments. To resolve this, we introduce Generalist-Specialist-MoE (GS-MoE), a two-branch (MoE) architecture that couples a cross-modal generalist model with distinct modality-specific specialists (experts) via domain-constrained feature fusion. On RadImageNet (1.35M images, 165 pathologies, three modalities), GS-MoE recovers detection of six low-prevalence pathologies on which every baseline scores F1 $=$ 0, with per-class gains up to +0.60 F1. It attains this while even slightly exceeding dense and specialist-only MoE aggregate baselines (MCC 0.770), while using ${\sim}53\%$ fewer active parameters at inference than the strongest investigated dense model.

摘要：AI 模型在多模態醫學影像中必須在特定模態的專業化與跨模態的共享表徵之間取得平衡，這是一個純粹的專家混合（MoE）架構目前無法滿足的權衡。基於專家的路由改善了領域內的學習，但可能會犧牲跨模態信號，而這在我們的實驗中對於罕見（低流行率）病理顯得尤其重要。為了解決這個問題，我們引入了通才-專家-MoE（GS-MoE），這是一種雙分支（MoE）架構，通過領域約束的特徵融合將跨模態的通才模型與不同的模態專家（專家）相結合。在 RadImageNet（1.35M 圖像，165 種病理，三種模態）上，GS-MoE 恢復了六種低流行率病理的檢測，這些病理在每個基準上得分 F1 $=$ 0，並且每類的增益高達 +0.60 F1。它在這一過程中甚至略微超越了密集和專家專用 MoE 的總體基準（MCC 0.770），同時在推理時使用的主動參數比調查過的最強密集模型少了 ${\sim}53\%$。

##### **PACT: Can Enterprise AI Assistants Be Trusted Under Pressure?**
2609.18605v1 by Mika Okamoto, Ansel Kaplan Erol

As corporate AI adoption continues to grow, enterprise-grade LLM agents are being deployed into sensitive contexts such as hiring, healthcare, and finance. In these contexts, compliance with rules specified in an agent's system context is a first-order legal concern. Currently, no evaluation framework systematically measures which LLM models tend to violate compliance rules, especially under pressure from a persistent user, a hurried manager, or circumstances where violation is convenient or attractive. We introduce PACT (Pressure-Applied Compliance Testing), a benchmark for rule-following under pressure in AI agents assisting employees in daily tasks across twelve regulated enterprise domains and forty-eight scenarios, each set in a realistic multi-turn conversation. Each benchmark item pairs a standing rule against a rule-violating shortcut, and applies a battery of pressures across different wordings and system-prompt modes. We construct PACT component by component under strict LLM-as-judge auditing to ensure samples are unambiguous, ungameable, and realistic enough to avoid eliciting evaluation-aware behavior. We use PACT to profile LLM compliance across six complementary metrics that create a holistic picture of an AI assistant's robustness under pressure and throughout multi-turn conversations, its transparency, and ability to correctly discern where a rule applies. We aggregate this profile into PACTScore, a reliability-weighted compliance rate over all items and modes. Our results across 22 common LLM models spanning multiple providers and sizes show substantial variability in compliance across models and metric dimensions. Even the strongest assistants mis-apply a rule on 6 to 10% of items, and ordinary user pressure raises the violation rate by 65% on average. PACT highlights compliance risks in LLM assistants, motivating guardrails and careful model selection.

摘要：隨著企業對人工智慧的採用持續增長，企業級的LLM代理正在被部署到如招聘、醫療保健和金融等敏感領域。在這些情境中，遵守代理系統上下文中規定的規則是一項首要的法律關注。目前，尚無評估框架系統性地衡量哪些LLM模型傾向於違反合規規則，特別是在持續用戶、匆忙的經理或違規方便或吸引的情況下。我們介紹了PACT（壓力應用合規測試），這是一個基準，用於評估在壓力下遵循規則的AI代理，協助員工在十二個受監管的企業領域和四十八個場景中進行日常任務，每個場景都設置在現實的多輪對話中。每個基準項目將一項現行規則與一個違規的捷徑進行配對，並在不同的措辭和系統提示模式下施加一系列壓力。我們在嚴格的LLM作為評判的審計下逐步構建PACT，以確保樣本明確、不易被操控且足夠真實，以避免引發評估意識行為。我們使用PACT來分析LLM在六個互補指標上的合規性，這些指標共同描繪了AI助手在壓力下及整個多輪對話中的穩健性、透明度以及正確辨別規則適用範圍的能力。我們將這一分析整合為PACTScore，這是一個對所有項目和模式的可靠性加權合規率。我們在22個涵蓋多個提供商和規模的常見LLM模型中的結果顯示，模型和指標維度之間的合規性存在顯著變異性。即使是最強大的助手在6%到10%的項目上也會錯誤應用規則，而普通用戶的壓力平均會使違規率提高65%。PACT突顯了LLM助手中的合規風險，促使建立防護措施和謹慎的模型選擇。

##### **TRIPROBE: Probing Task Separability Beyond Classification for XAI**
2609.18525v1 by Amirhossein Sadough, Freek Hens, Aleksa Bokšan, Mohammad Mahdi Dehshibi, Mahyar Shahsavari

Modern evaluation of learning pipelines often reduces to downstream accuracy, leaving open the question of why tasks succeed or fail. TriProbe addresses this gap with a multi-level probing framework for explainable diagnosis of task separability. Rather than treating models as black boxes, TriProbe traces how separability evolves across inputs, learned features, and final classifiers. It decomposes multi-task problems into binary subtasks and applies three complementary probes: a Foundational Probe on input spaces, a Latent Probe on feature representations, and a Final Probe on classifier outputs. Using Maximum Fisher's Discriminant Ratio as a principled separability metric, TriProbe identifies bottlenecks and affected task pairs. Experiments on the Roshambo sEMG benchmark show how TriProbe reveals hidden breakdowns, guiding data collection, validation, and architecture design.

摘要：現代對學習流程的評估通常簡化為下游準確性，留下了為何任務成功或失敗的問題。TriProbe 通過一個多層次的探測框架來解決這一空白，以便對任務可分性進行可解釋的診斷。TriProbe 並不將模型視為黑箱，而是追蹤可分性如何在輸入、學習特徵和最終分類器之間演變。它將多任務問題分解為二元子任務，並應用三個互補的探測器：對輸入空間的基礎探測器、對特徵表示的潛在探測器，以及對分類器輸出的最終探測器。使用最大費舍爾判別比作為原則性的可分性度量，TriProbe 確定瓶頸和受影響的任務對。在 Roshambo sEMG 基準上的實驗顯示 TriProbe 如何揭示隱藏的故障，指導數據收集、驗證和架構設計。

##### **Visual Compliance via Executable Safety Rule Entailment**
2609.18328v1 by Jisoo Kim, TaeYoon Kwack, Jinwoo Jang, Honguk Woo

Recent advances in LLMs and VLMs have enabled safety systems to reason beyond simple risk patterns toward more contextual and semantic safety concerns. However, as risk patterns continue to evolve and safety rules become more complex, existing training-based end-to-end safeguards face persistent challenges in adaptability and explainable reasoning over complex safety rules. To address these challenges, we propose GuardEn (Guarding by Safety Rule Entailment), an executable safeguard framework that decomposes safety policies into atomic propositions through Safety-Rule Compilation, modeling their composition as executable code. At test time, Scene-Grounded Execution instantiates these atomic propositions with contextual visual information derived from scene graphs, enabling rule-grounded and interpretable safety reasoning. Experiments on SafetyVisionBench demonstrate the effectiveness of programmable safeguard for complex visual safety assessment, achieving an average improvement of 9.8 F1 points over the strongest baseline.

摘要：最近在大型語言模型（LLMs）和視覺語言模型（VLMs）方面的進展，使得安全系統能夠超越簡單的風險模式，朝向更具情境性和語義性的安全關注。然而，隨著風險模式的不斷演變和安全規則的日益複雜，現有基於訓練的端到端安全防護面臨著在適應性和對複雜安全規則的可解釋推理方面的持續挑戰。為了解決這些挑戰，我們提出了GuardEn（通過安全規則推導進行保護），這是一個可執行的安全防護框架，通過安全規則編譯將安全政策分解為原子命題，並將其組合建模為可執行代碼。在測試時，場景基礎執行使用來自場景圖的上下文視覺信息實例化這些原子命題，實現基於規則的可解釋安全推理。在SafetyVisionBench上的實驗證明了可編程安全防護在複雜視覺安全評估中的有效性，平均提高了9.8個F1點，相較於最強基準。

##### **Knowledge-Graph Based Augmentation versus Retrieval Augmented Generation for Cultural-Related Question Answering**
2609.18317v2 by Pablo Poulenard, Yannis Karmim, Valentin Barrière

Large language models (LLMs) suffer from a long-tail deficit: culturally specific facts, particularly those concerning underrepresented regions such as Latin America, appear too rarely in pretraining corpora to be reliably memorized. Retrieval-Augmented Generation (RAG) addresses this by grounding generation in external text, but structured alternatives such as Knowledge Graphs (KGs) offer tighter control over what enters the context, along with potential gains in explainability and updatability. We benchmark Graph-RAG against standard RAG on LatamQA, a culturally grounded multiple-choice dataset spanning eight thematic categories. The graphs are built end-to-end from Wikipedia articles with KGGen, a recent open-domain extractor, without manual curation in our main setting. G-Retriever is competitive with RAG and reduces the error of the base LLM by 72\% with a standard KG and 78\% with a benchmark-aware variant, the gap to RAG narrowing further as the graph is oriented toward task-relevant content. The trained projection transfers zero-shot to Portuguese without target-language fine-tuning, indicating multilingual reach.

摘要：大型語言模型（LLMs）面臨長尾缺失的問題：文化特定的事實，特別是那些涉及拉丁美洲等代表性不足地區的事實，在預訓練語料庫中出現得過於稀少，無法可靠地被記住。檢索增強生成（RAG）通過將生成基於外部文本來解決這一問題，但結構化的替代方案如知識圖譜（KGs）則提供了對進入上下文的內容更緊密的控制，並可能在可解釋性和可更新性方面帶來收益。我們在LatamQA上對Graph-RAG進行基準測試，這是一個涵蓋八個主題類別的文化基礎多選題數據集。這些圖是從維基百科文章中端到端構建的，使用KGGen這個最近的開放領域提取器，並且在我們的主要設置中沒有手動策劃。G-Retriever在與RAG的競爭中表現出色，並將基礎LLM的錯誤率降低了72\%（使用標準KG）和78\%（使用基準感知變體），隨著圖的內容朝向任務相關內容，與RAG的差距進一步縮小。訓練後的投影在沒有目標語言微調的情況下可以零樣本轉移到葡萄牙語，顯示出多語言的覆蓋範圍。

##### **A Study of the Reliability of Agentic AI-Generated Programs**
2609.18298v2 by Ayesha Shafique, Barton P. MIller, Elisa R. Heymann

Agentic-AI based software development offers the promise of faster completion of the software, greater programmer efficiency, and more reliable code. The question is how can we verify these claims in an objective way? In this project, we attempted to answer this question based on three practices. First, we applied a typical best-practices agentic AI workflow for software development. Second, our target programs were ten well-known, release-quality human-written Linux utility programs so that we could compare the AI-generated code against a concrete ground truth. Third, we based our measure of reliability on a widely used testing technique, fuzz random testing. For this testing, we used both classic black box, generational testing and more modern coverage guided (gray box, mutational) testing using AFL++. We found that the AI-generated versions of the utility programs were typically as reliable - often more reliable - than the latest human-generated versions of these programs. While the AI-generated versions did have some failures, they were less common than the code from the standard repositories. Interestingly, the AI-generated code was less likely to have failures such as memory errors (such as buffer overflows) but more likely to have hangs such as infinite loops. In addition, we verified that generating robust and reliable software using agentic AI requires careful practice and human supervision. The quality of the code is highly dependent on the prompts and skills used, and how the human directing the process responds. We also demonstrated that using agentic AI workflow for software development (with its prompts and skills) can become a specification of the code that leads to cost-effective sustainability of the software.

摘要：代理式人工智慧（Agentic-AI）基礎的軟體開發承諾能更快完成軟體、提高程式設計師效率，並產出更可靠的程式碼。問題是，我們如何以客觀的方式驗證這些聲明？在這個專案中，我們試圖根據三個實踐來回答這個問題。首先，我們應用了典型的最佳實踐代理式人工智慧工作流程來進行軟體開發。其次，我們的目標程式是十個知名的、發布品質的人類撰寫的 Linux 工具程式，以便將 AI 生成的程式碼與具體的真實情況進行比較。第三，我們的可靠性測量基於一種廣泛使用的測試技術，即模糊隨機測試。對於這項測試，我們使用了經典的黑箱生成測試以及更現代的覆蓋引導（灰箱、變異）測試，使用 AFL++。我們發現，這些工具程式的 AI 生成版本通常與最新的人類生成版本一樣可靠——往往更可靠。雖然 AI 生成的版本確實有一些失敗，但這些失敗的頻率低於標準庫中的程式碼。有趣的是，AI 生成的程式碼不太可能出現記憶體錯誤（例如緩衝區溢出），但更可能出現無限迴圈等掛起情況。此外，我們驗證了使用代理式人工智慧生成穩健可靠的軟體需要謹慎的實踐和人類監督。程式碼的質量在很大程度上取決於所使用的提示和技能，以及指導過程的人類如何反應。我們還展示了使用代理式人工智慧工作流程進行軟體開發（連同其提示和技能）可以成為程式碼的規範，從而實現軟體的成本效益可持續性。

##### **Building Trust in Artificial Intelligence: A Necessity for Railway Applications**
2609.18278v1 by Lefebvre Renard Clément, Lébé Vincent, Da Silva Ribeiro Pereira Ricardo, Sundell Johan, Jaoul Arnaud Saiah Kenza, Mijatovíc Nenad

Artificial Intelligence (AI) is currently only applied to non-safety critical applications due to the strict standards and regulations for railway industries. We propose to review the three main fields necessary to increase trust in data science and AI algorithms and reach compliance: robustness, Operational Design Domain (ODD), and explainability. Robustness is the ability of an AI system to maintain its level of performance under any circumstances (ISO24029). ODDs allow the explicit definition of operating conditions under which a system is intended to operate, according to the recently published DIN DKE SPEC 99004. Explainability is the property of an AI system to express important factors influencing the AI system results in a way that humans can understand. Those 3 domains of research are already well investigated by nonrailway actors, with algorithms and methods ready to use for railway applications. A system view is necessary to ensure all trustworthy requirements interact continuously in a safe MLOps environment thereby fostering acceptance from regulators, operators and the public. Beyond safeguarding safety-critical applications, we aim to show that fostering deep trust in AI, as now required by regulatory frameworks worldwide, will unlock its full potential and transform the pace of adoption across mission-critical domains.

摘要：人工智慧（AI）目前僅應用於非安全關鍵的應用，因為鐵路行業有嚴格的標準和法規。我們提議回顧三個主要領域，以增加對數據科學和AI算法的信任並達到合規性：穩健性、操作設計範疇（ODD）和可解釋性。穩健性是指AI系統在任何情況下保持其性能水平的能力（ISO24029）。根據最近發布的DIN DKE SPEC 99004，ODD允許明確定義系統預期運行的操作條件。可解釋性是指AI系統以人類能理解的方式表達影響AI系統結果的重要因素的特性。這三個研究領域已被非鐵路參與者充分研究，並有算法和方法可供鐵路應用使用。系統觀點是必要的，以確保所有值得信賴的要求在安全的MLOps環境中持續互動，從而促進監管機構、運營商和公眾的接受。除了保護安全關鍵應用外，我們的目標是顯示，促進對AI的深度信任，如今全球監管框架所要求的，將釋放其全部潛力並改變在任務關鍵領域的採用速度。

##### **A Lightweight CNN Integrated Compact Convolutional Transformer for Multi-Scale Feature Learning and reducing computational complexity for breast cancer mammography image detection and classification**
2609.18212v1 by Md Taimur Ahad, Ainuddin Ahmed

Over the years, Convolutional Neural Networks (CNNs) have demonstrated strong capability in cancer detection and classification using medical images. However, CNN-based models often struggle to capture long-range contextual dependencies. In such scenarios, integrating Compact Convolutional Transformer (CCT) architectures after the CCT layer allows CNN-extracted features to reshape into compact patch tokens using a CCT tokenizer, followed by the addition of positional embeddings to preserve spatial structure. Using 5-fold cross-validation, the model was tested on 3 sets of breast cancer mammography. With only 250,435 parameters, the model achieved 99%-100% accuracy across 3 datasets, indicating robust generalization. Explainable AI (XAI) was integrated into the model to explain the breast cancer classification process to enhance clinical trust. The results indicate that the proposed framework is suitable for computer-aided diagnosis systems, particularly in resource-constrained clinical environments. The novelty of the proposed CNN-integrated CCT overcomes the limitation of CNN's gradient degradation in the last layers by integrating convolutional tokenization with transformer-based learning. Lighter than ViT, which is effective in capturing long-range dependencies, the model has also proven efficient in breast cancer classification by capturing long-range dependencies among breast tissue regions.

摘要：多年來，卷積神經網絡（CNN）在使用醫學影像進行癌症檢測和分類方面展現了強大的能力。<lb/>然而，基於CNN的模型往往難以捕捉長距離的上下文依賴性。<lb/>在這種情況下，在CCT層之後整合緊湊卷積Transformer（CCT）架構，允許CNN提取的特徵通過CCT標記器重塑為緊湊的補丁標記，隨後添加位置嵌入以保留空間結構。<lb/>使用5折交叉驗證，該模型在3組乳腺癌乳房X光檢查上進行測試。<lb/>僅有250,435個參數，該模型在3個數據集上達到了99%-100%的準確率，顯示出強大的泛化能力。<lb/>可解釋的人工智慧（XAI）被整合進模型中，以解釋乳腺癌分類過程，增強臨床信任。<lb/>結果表明，所提出的框架適合用於計算機輔助診斷系統，特別是在資源受限的臨床環境中。<lb/>所提出的CNN整合CCT的新穎性克服了CNN在最後幾層中的梯度退化限制，通過將卷積標記化與基於Transformer的學習相結合。<lb/>該模型比ViT更輕巧，ViT在捕捉長距離依賴性方面有效，並且在乳腺癌分類中也證明了其效率，能夠捕捉乳腺組織區域之間的長距離依賴性。

##### **Rethinking How We Evaluate Methodological Progress in Health AI**
2609.18134v2 by Florent Pollet, Matthew McDermott

Methodological progress in artificial intelligence (AI) for electronic health records (EHRs) depends on our ability to determine which algorithms work better, and under which conditions. However, such progress is thought to be hindered by difficulties in reproducibility and in defining clinically meaningful evaluation tasks. We empirically study these barriers by re-implementing 12 historical and recent algorithms within a shared evaluation framework and evaluating them on two clinical datasets, MIMIC-IV and NWICU. We compare two complementary task families: expert-authored clinically meaningful tasks and generated tasks defined from randomly sampled event codes and prediction horizons. We ask whether relative algorithms comparisons transfer across task families and datasets, whether residual task heterogeneity contains useful methodological structure, and what a controlled comparison reveals about progress over the last decade. We find that aggregate pairwise comparisons transfer strongly across evaluation settings, including from randomly generated tasks to clinically meaningful tasks and across datasets. At the same time, clinically meaningful tasks exhibit greater task-method interaction, providing preliminary evidence that task properties can help explain when particular modeling choices are advantageous. Finally, newer algorithms do not consistently outperform earlier approaches: gradient-boosted trees remain highly competitive when paired with a modern, wide and sparse representation of the EHR. Together, these results suggest that useful methodological knowledge may require less task engineering than commonly assumed, while highlighting the importance of understanding the structured heterogeneity that remains across tasks and methods.

摘要：方法論在人工智慧 (AI) 於電子健康紀錄 (EHRs) 的進展取決於我們判斷哪些演算法表現更佳以及在何種條件下。然而，這樣的進展被認為受到可重複性和定義臨床上有意義的評估任務的困難所阻礙。我們透過在共享評估框架內重新實現 12 種歷史和近期的演算法，並在兩個臨床數據集 MIMIC-IV 和 NWICU 上進行評估，實證研究這些障礙。我們比較兩個互補的任務類別：專家編寫的臨床有意義任務和從隨機抽樣事件代碼和預測視野定義的生成任務。我們詢問相對的演算法比較是否能在任務類別和數據集之間轉移，殘餘任務異質性是否包含有用的方法論結構，以及受控比較對過去十年的進展揭示了什麼。我們發現，總體成對比較在評估環境之間強烈轉移，包括從隨機生成的任務到臨床有意義的任務以及跨數據集。同時，臨床有意義的任務顯示出更大的任務-方法互動，提供初步證據表明任務特性可以幫助解釋何時特定的建模選擇是有利的。最後，較新的演算法並不總是優於早期的方法：梯度提升樹在與現代的、寬且稀疏的 EHR 表示配對時仍然具有高度競爭力。綜合這些結果表明，有用的方法論知識可能需要的任務工程少於普遍假設，同時強調理解任務和方法之間仍然存在的結構性異質性的重要性。

##### **Measuring AI Leadership: Development and Validation of a Multidimensional Measure for AI-Native Organizations**
2609.17965v1 by Mustafa Akben, Leslie Coyne

AI is changing what leaders must judge, explain, learn, and coordinate, yet existing measures do not capture these behaviors at the level needed to study leadership in AI-enabled work. We develop the AI Leadership Battery, which organizes 36 behaviorally specific subdimensions into 11 theory-specified content families. Following established scale-development procedures, the research used deductive item generation; content validation of definitional correspondence and definitional distinctiveness; exploratory factor analysis and item reduction; confirmatory factor analysis in independent samples; and tests of internal consistency reliability, convergent validity, discriminant validity, and criterion-related and incremental validity. Across the development and validation studies, the analyses provided evidence for the Battery's content, multidimensional structure, reliability, and distinction from selected orbiting constructs. The Battery also contributed additional information beyond orbiting constructs across organizational growth, decision speed, customer/stakeholder response capability, AI-enabled team performance, AI-enabled work experience, AI security and risk management, and AI adoption and integration. The resulting measure provides researchers with a behavioral framework for examining how leaders in AI-enabled work regulate judgment, learning, adaptation, transparency, and accountability.

摘要：AI 正在改變領導者必須判斷、解釋、學習和協調的內容，但現有的衡量標準並未捕捉到這些行為，無法在研究 AI 驅動的工作中的領導力時達到所需的水平。我們開發了 AI 領導力電池，它將 36 個行為特定的子維度組織成 11 個理論指定的內容家族。根據既定的量表開發程序，研究使用了演繹性項目生成；對定義對應性和定義區別性的內容驗證；探索性因素分析和項目縮減；在獨立樣本中的驗證性因素分析；以及內部一致性可靠性、收斂效度、區別效度和標準相關及增量效度的測試。在開發和驗證研究中，分析提供了對電池內容、多維結構、可靠性以及與選定的相關構念之間的區別的證據。該電池還提供了超越相關構念的額外信息，涵蓋組織增長、決策速度、客戶/利益相關者反應能力、AI 驅動的團隊表現、AI 驅動的工作經驗、AI 安全和風險管理，以及 AI 採用和整合。最終的測量為研究人員提供了一個行為框架，以檢視 AI 驅動工作中的領導者如何調節判斷、學習、適應、透明度和問責制。

##### **AI and Human Approaches to Mathematical Problem Solving**
2609.17779v1 by Yang Ding

AI systems have begun to report solutions, disproofs, and substantive advances on long-standing mathematical problems, raising questions about whether they approach research in the same way as mathematicians. This study compares public AI research accounts with the human literature on 11 such problems. The human corpus contains 58 papers that directly addressed the same mathematical targets later reported by AI sources as resolved, disproved, or substantially advanced; 31 within-problem comparisons were constructed from these materials. Six validated text-based measures capture problem resolution, method articulation, uncertainty and boundary specification, successor-question generation, generality, and cross-disciplinary integration. AI accounts place greater emphasis on resolving the focal problem and connecting ideas across fields. Human papers devote significantly more attention to explaining methods, specifying assumptions and limitations, and identifying questions for subsequent research. No precise difference is detected in generality. The estimated directions remain unchanged when each mathematical problem is removed in turn. The findings reveal two distinct research profiles: AI accounts concentrate on closing and recombining problems, whereas mathematical papers more extensively document the procedures, limits, and research opportunities through which results become cumulative knowledge. Evaluating research AI therefore requires attention to the organization of inquiry, not only whether a target is solved.

摘要：AI 系統已開始報告解決方案、反駁和在長期存在的數學問題上取得的實質性進展，這引發了關於它們是否以與數學家相同的方式進行研究的問題。這項研究將公共 AI 研究報告與人類文獻中 11 個此類問題進行比較。人類文獻包含 58 篇直接針對 AI 來源後來報告為已解決、被反駁或實質性進展的相同數學目標的論文；這些材料構建了 31 個問題內比較。六個經驗驗證的文本基準捕捉了問題解決、方法闡述、不確定性和邊界規範、後繼問題生成、普遍性和跨學科整合。AI 報告更強調解決焦點問題和跨領域連結思想。人類論文則顯著更多地關注解釋方法、指定假設和限制，以及識別後續研究的問題。普遍性方面未檢測到明確差異。當每個數學問題依次移除時，估計的方向保持不變。研究結果揭示了兩種不同的研究輪廓：AI 報告專注於關閉和重新組合問題，而數學論文則更廣泛地記錄了通過哪些程序、限制和研究機會使結果成為累積知識。評估研究 AI 因此需要關注探究的組織，而不僅僅是目標是否被解決。

##### **Information Set Emulation: Causal Certificates for AI Derived EHR Features**
2609.17777v1 by Takes Fujita, Nobutaka Hattori

AI and large language models can recover clinically meaningful features from electronic health records (EHRs), but predictive usefulness does not establish admissibility for causal inference. We introduce information set emulation: an AI typed lift attaches source evidence, clinical and recording times, decision-time availability, representation version, proposed causal roles, and unresolved ambiguity to extracted features under a locked target trial. Causal certificates record auditable evidence for those roles. Features with unresolved downstream roles are routed to compatible reporting or separate analyses.
  Typed evidence defines an observational fiber of causal worlds consistent with the observed law. The locked scalar estimand maps this fiber to a compatible image whose squared Chebyshev radius equals the residual minimax mean squared error when the image is nonempty and compact. This classical identity provides a target-specific measure of information ambiguity. The contribution is its integration with a joint EHR observation map and an auditable certificate architecture.
  Under explicit exchangeability, positivity, and nuisance-consistency conditions, we give identification and cross-fitted augmented inverse probability weighted estimation, distinguishing empirical and population targets. An EHR compression-drift identity separates the roles of frame presence, treatment assignment, and outcome observation. Artificial simulations and a common-law finite-world example illustrate estimation failures and information-radius reduction. Synthetic Phase 0 notes demonstrate audit diagnostics; a separate role-specific analysis spread illustrates routing and is not an exact fiber radius. All experiments are synthetic. The framework specifies when reconstructed information can support a point claim and when compatible reporting is required.

摘要：AI 和大型語言模型可以從電子健康記錄 (EHRs) 中恢復臨床有意義的特徵，但預測的有用性並不建立因果推斷的可接受性。 我們引入信息集模擬：一種 AI 類型的提升將來源證據、臨床和記錄時間、決策時間的可用性、表示版本、提議的因果角色和未解決的模糊性附加到鎖定目標試驗下提取的特徵。 因果證書記錄這些角色的可審計證據。 具有未解決下游角色的特徵被路由到兼容的報告或單獨分析。<lb/>  
類型證據定義了一種觀察性因果世界的纖維，與觀察到的法則一致。 鎖定的標量估計量將這個纖維映射到一個兼容的圖像，其平方切比雪夫半徑等於當圖像非空且緊湊時的殘差最小最大均方誤差。 這一經典身份提供了一種特定於目標的信息模糊性測量。 其貢獻在於與聯合 EHR 觀察圖和可審計證書架構的整合。<lb/>  
在明確的可交換性、正性和干擾一致性條件下，我們給出了識別和交叉擬合的增強逆概率加權估計，區分經驗和人口目標。 EHR 壓縮漂移身份分離了框架存在、治療分配和結果觀察的角色。 人工模擬和一個普通法有限世界的例子說明了估計失敗和信息半徑的減少。 合成的第 0 階段記錄展示了審計診斷；一個單獨的角色特定分析擴展展示了路由，並不是一個精確的纖維半徑。 所有實驗都是合成的。 該框架指定了何時重建的信息可以支持一個點聲明以及何時需要兼容的報告。

##### **Evolution of US Oral Political Language**
2609.17755v1 by Jacques Savoy

The analysis of US political language is usually based on the written form (e.g. presidential addresses) or posts broadcasted on various social networks. Oral production, however, which is even more frequent, can better reveal the style and mode of thinking of the speaker. This study covers this mode of linguistic communication by considering 19 candidates from the presidential elections between 1960 to 2024. Our main research objectives are to disclose the main trends hidden in those presidential debates. Do we observe a clear simplification of the US political language over time? Does Trump have poor language compared to the other candidates? Do unusual stylistic features occur only with a single, specific president? Moreover, can we detect a pattern explaining the success or failure of some nominees? Over time, this study demonstrates a significant reduction in political language complexity, a decrease of the mean sentence length, and a noteworthy decline of complex terms. Moreover, the emotional tone increases over the decades, while logical and rational thinking tends to lessen.

摘要：美國政治語言的分析通常基於書面形式（例如總統演說）或在各種社交網絡上廣播的帖子。然而，口語表達更為頻繁，能更好地揭示講者的風格和思維模式。本研究通過考慮1960年至2024年之間的19位候選人來涵蓋這種語言交流模式。我們的主要研究目標是揭示那些總統辯論中隱藏的主要趨勢。我們是否觀察到美國政治語言隨時間的明顯簡化？特朗普的語言是否比其他候選人差？不尋常的風格特徵是否僅出現在某位特定的總統身上？此外，我們能否檢測到解釋某些提名人成功或失敗的模式？隨著時間的推移，本研究顯示政治語言的複雜性顯著降低，平均句子長度減少，複雜術語的顯著下降。此外，情感語調在幾十年中增加，而邏輯和理性思維則趨向減少。

##### **Det-LIME: Detector-Aware, Multi-Instance Local Interpretable Model-Agnostic Explanations for Automated Marine Mammal Detection**
2609.17479v1 by Jiayi Zhou, David W. Johnston, Brinnae Bent

Despite the rapid uptake of black-box object detectors in marine mammal research and monitoring, explainability techniques are rarely integrated into conservation workflows. Furthermore, most classification-oriented explainability tools are ill-suited to detection tasks involving imagery of social organisms or those with colonial life histories, as they ignore multiple detections within a scene and produce single-instance outputs that blur evidence across individuals. These methods also generate low-resolution, often biologically irrelevant visuals, limiting their utility for debugging, targeted data augmentation, and refined data collection.
  We proposed Det-LIME, a detector-aware, multi-instance adaptation of Local Interpretable Model-Agnostic Explanations (LIME) that produced instance-specific, box-aligned explanations by combining per-detection weighting, a proximity kernel that emphasizes regions near each box, and Intersection-over-Union-based matching to track the same instance across perturbations. We evaluated Det-LIME on aerial drone imagery for harbor seal detection, with an additional seabird case study to assess generality, and compared it with vanilla LIME, Stabilized LIME, Deterministic LIME, and gradient-based attribution methods.
  Using the Attribution Ratio and Max Saliency Hit Rate metrics, we showed that Det-LIME consistently improved multi-instance attribution. In practice, these higher-resolution, instance-aware explanations provide insight into model outputs and support post-processing, debugging, and actionable improvements in modeling and data collection or augmentation.

摘要：儘管黑箱物體檢測器在海洋哺乳動物研究和監測中的快速應用，解釋技術卻很少融入保育工作流程中。<lb/>此外，大多數以分類為導向的解釋工具不適合涉及社會生物或具有群居生活史的檢測任務，因為它們忽略了場景中的多重檢測，並產生模糊個體證據的單一實例輸出。<lb/>這些方法還生成低解析度、通常生物學上無關的視覺效果，限制了它們在調試、針對性數據增強和精細數據收集中的實用性。<lb/>我們提出了Det-LIME，這是一種檢測器感知的多實例適應性本地可解釋模型無關解釋（LIME），通過結合每次檢測的加權、強調每個框附近區域的接近核，以及基於交集-聯合的匹配來跟踪相同的實例，產生特定於實例的、框對齊的解釋。<lb/>我們在用於港口海豹檢測的無人機航拍影像上評估了Det-LIME，並進行了額外的海鳥案例研究以評估其普遍性，並將其與原始LIME、穩定LIME、確定性LIME和基於梯度的歸因方法進行比較。<lb/>使用歸因比率和最大顯著性命中率指標，我們顯示Det-LIME在多實例歸因方面持續改善。<lb/>在實踐中，這些更高解析度、實例感知的解釋提供了對模型輸出的洞察，並支持後處理、調試以及在建模和數據收集或增強中的可行改進。

##### **CareMirror: Bringing Caregiver Wellbeing into the Dementia Care Ecosystem**
2609.17434v1 by Jiayue Melissa Shi, Ethan Nguyen, Drishti Goel, Upasana Natarajan, Shashwat Srivatsa, Daniel S. Brown, Violeta J. Rodríguez, Dong Whi Yoo, Ravi Karkar, Koustuv Saha

Family caregivers of people living with dementia shoulder emotional and practical responsibilities, yet their own wellbeing often remains peripheral to dementia care. We built CareMirror, an envisioned caregiver wellbeing ecosystem with interconnected caregiver- and clinician-facing interfaces for longitudinal reflection, personalized support, and caregiver-controlled sharing with clinical care. We conducted semi-structured interviews with 14 caregivers, using CareMirror as a design probe to examine how they perceived this ecosystem and what expectations, concerns, and boundaries emerged around clinical connection. Caregivers valued attention to their wellbeing, longitudinal awareness, context-sensitive support, and clinical visibility when it could lead to meaningful follow-up. However, repeated reflection could become burdensome or emotionally difficult, automatic clinical sharing could inhibit candid disclosure, and participants wanted control over what information entered clinical care. They also expected AI to support reflection and communication without replacing caregiver voice or clinician judgment. We contribute design considerations for proactive, clinically connected caregiver wellbeing support.

摘要：家庭照顧者在照顧患有癡呆症的人時，承擔著情感和實際的責任，但他們自己的福祉往往在癡呆症護理中處於邊緣地位。<lb/>我們建立了CareMirror，一個設想中的照顧者福祉生態系統，擁有互聯的面向照顧者和臨床醫生的界面，用於長期反思、個性化支持和照顧者控制的臨床共享。<lb/>我們對14位照顧者進行了半結構化訪談，使用CareMirror作為設計探針，檢視他們對這個生態系統的看法，以及圍繞臨床聯繫出現的期望、擔憂和界限。<lb/>照顧者重視對他們福祉的關注、長期的認知、情境敏感的支持，以及在能夠導致有意義的後續跟進時的臨床可見性。<lb/>然而，重複的反思可能會變得繁重或情感上困難，自動的臨床共享可能會抑制坦誠的披露，參與者希望能控制進入臨床護理的信息。<lb/>他們還期望人工智慧能支持反思和溝通，而不取代照顧者的聲音或臨床醫生的判斷。<lb/>我們提供了主動、臨床連結的照顧者福祉支持的設計考量。

##### **Finding Common Mistakes In Modelling With Mathematical Formalisms Using LLMs**
2609.17111v1 by Lilian Killich, Marko Schmellenkamp, Fabian Vehlken, Thomas Zeume

Modelling with mathematical formalisms like logical formulas, mathematical equations, or regular expressions is an important yet challenging task for students of computer science and other STEM disciplines. Identifying common mistakes occurring in this context is an important step towards helping struggling students by providing targeted high-quality feedback, e.g. in interactive learning systems.
  We present a tool-supported workflow that allows to (1) identify candidates for common mistakes that explain many student mistakes in large educational data sets, (2) cluster candidates according to similarities, and (3) visualize resulting clusters for instructors and CS education researchers. The visualization is designed to help researchers to identify common modelling mistakes. The candidates for common mistakes are represented by bug fixing transformations that translate incorrect formalizations into correct formalizations; they are generated by an LLM and validated algorithmically.
  We show that this approach works well by reproducing common mistakes in propositional logic modelling that were identified by hand in the literature; showing that, unlike other algorithmic approaches, the LLM-based approach is suitable for very large sets of data; and applying it to multiple other formalisms to showcase it generalizes beyond propositional logic.

摘要：使用數學形式主義進行建模，例如邏輯公式、數學方程或正則表達式，是計算機科學和其他STEM學科學生面臨的重要但具有挑戰性的任務。識別在這種情境中出現的常見錯誤是幫助掙扎中的學生的一個重要步驟，通過提供針對性的高質量反饋，例如在互動學習系統中。<lb/>  
我們提出了一個工具支持的工作流程，允許 (1) 確定解釋許多學生錯誤的常見錯誤候選者，這些候選者來自於大型教育數據集，(2) 根據相似性對候選者進行聚類，以及 (3) 為教師和計算機科學教育研究者可視化結果聚類。該可視化旨在幫助研究人員識別常見的建模錯誤。常見錯誤的候選者由錯誤修正轉換表示，這些轉換將不正確的形式化轉換為正確的形式化；它們由LLM生成並通過算法進行驗證。<lb/>  
我們展示了這種方法的有效性，通過重現文獻中手動識別的命題邏輯建模中的常見錯誤；顯示出與其他算法方法不同，基於LLM的方法適用於非常大的數據集；並將其應用於多種其他形式主義，以展示其超越命題邏輯的通用性。

##### **Sample-Conditioned Representation Selection for Audio Few-Shot Learning**
2609.17076v1 by Fengrui Liu, Ningxin Shen, Yi Li, Yiwei Fu, Feng Liu, Jiangmeng Li

Few-shot audio classifiers may rely on foreground-background co-occurrences and fail when those correlations shift. On SpurAudio, the resulting representation shift is concentrated and class dependent: for ResNet12, the top 10 percent of channels explain 82.80 percent of the null-corrected shift contribution. We propose SAMPLESELECT, which predicts a fixed-budget feature mask independently for each input while keeping the encoder and source classifier frozen. Training uses differentiable Gumbel Top-k selection with foreground classification and cross-background contrastive losses; inference uses deterministic Top-k masks and support-only linear adaptation. Across ResNet12 and Conv64 in 5-way 1-shot and 5-shot evaluation, SAMPLESELECT gives the best OOD accuracy among the compared methods and improves the matched full-representation control by 4.90-8.38 percentage points. Ablations and representation analyses further support the learned selection mechanism. Code is available at https://github.com/Cross-Innovation-Lab/SAMPLESELECT/

摘要：少量樣本的音頻分類器可能依賴於前景與背景的共現，當這些相關性發生變化時會失效。在 SpurAudio 上，結果的表示變化是集中且依賴於類別的：對於 ResNet12，前 10% 的通道解釋了 82.80% 的無效修正變化貢獻。我們提出了 SAMPLESELECT，該方法為每個輸入獨立預測固定預算的特徵掩碼，同時保持編碼器和源分類器不變。訓練使用可微分的 Gumbel Top-k 選擇，結合前景分類和跨背景對比損失；推理使用確定性 Top-k 掩碼和僅支持的線性適應。在 5-way 1-shot 和 5-shot 評估中，SAMPLESELECT 在比較方法中提供了最佳的 OOD 準確率，並將匹配的完整表示控制提高了 4.90-8.38 個百分點。消融實驗和表示分析進一步支持了學習的選擇機制。代碼可在 https://github.com/Cross-Innovation-Lab/SAMPLESELECT/ 獲得。

##### **When Confidence Signals Disagree: Local and Global Confidence in Autoregressive Language Models**
2609.16933v1 by Julio C. Amador Diaz Lopez

Modern predictive systems expose multiple quantities that are commonly interpreted as measures of confidence. However, these quantities can summarize different aspects of the predictive process. This distinction matters when confidence is used to evaluate reliability or inform downstream oversight and control. We investigate whether different confidence readouts are empirically interchangeable in an autoregressive language model by comparing local confidence, defined from the probability of the greedy-selected answer token, with global confidence, defined from modal-answer frequency under repeated sampling. Across MMLU and ARC Challenge, the two signals are weakly correlated and differ substantially in their association with correctness: global confidence is moderately associated with correctness, whereas local confidence shows little association. We further test whether question-level disagreement between the signals is associated with sampling instability. On ARC, larger local--global confidence gaps are associated with higher answer entropy, more distinct sampled answers, and lower modal-answer concentration. The gap--entropy association persists when disagreement and instability are estimated from disjoint stochastic samples, indicating that it is not explained by shared finite-sample variation. The corresponding relationship is substantially weaker on MMLU, where only 4% of questions exhibit sampling instability. These results show that confidence readouts derived from the same predictive system are not empirically interchangeable and that their disagreement can provide a diagnostic of unstable sampling behavior. Confidence should therefore be treated as an explicitly defined measurement rather than as a single intrinsic scalar property of a model, particularly when it is used to inform downstream evaluation, oversight, or control.

摘要：現代的預測系統揭示了多個量度，這些量度通常被解釋為信心的指標。<lb/>然而，這些量度可以總結預測過程的不同方面。<lb/>當信心被用來評估可靠性或提供下游監督和控制時，這一區別是重要的。<lb/>我們通過比較本地信心（從貪婪選擇的答案標記的概率定義）和全局信心（從重複抽樣下的模式答案頻率定義）來調查不同的信心讀數在自回歸語言模型中是否可以互換。<lb/>在 MMLU 和 ARC 挑戰中，這兩種信號的相關性較弱，並且在與正確性關聯方面有顯著差異：全局信心與正確性中度相關，而本地信心則幾乎沒有相關性。<lb/>我們進一步測試信號之間的問題級別不一致是否與抽樣不穩定性相關。<lb/>在 ARC 中，較大的本地-全局信心差距與較高的答案熵、更多不同的抽樣答案以及較低的模式答案集中度相關。<lb/>當不一致性和不穩定性是從不相交的隨機樣本中估計時，差距-熵的關聯仍然存在，這表明它並不是由共享的有限樣本變異解釋的。<lb/>在 MMLU 上，對應的關係顯著較弱，只有 4% 的問題顯示出抽樣不穩定性。<lb/>這些結果表明，來自同一預測系統的信心讀數在實證上並不可互換，且它們的不一致性可以提供不穩定抽樣行為的診斷。<lb/>因此，信心應被視為明確定義的測量，而不是模型的單一內在標量屬性，特別是當它用來通知下游評估、監督或控制時。

##### **Right Direction, Wrong Step: Geometric Analysis of Finite-Step Failure in Looped Transformers**
2609.16665v1 by Zhihao Guo, Zonghan Wu, Haizhou Du, Huan Huo, Yilei Shao, Athanasios V. Vasilakos, Qingsong Wen

Looped Transformers offer a parameter-efficient route to test-time scaling by reusing shared layers for iterative latent reasoning. However, additional iterations can reduce support for a reference answer, leaving unclear whether an update's direction is locally unhelpful or its full displacement moves too far. We study this distinction by analysing reference utility, which measures this support, along the model's own update direction, varying the fraction of the proposed displacement supplied to the readout. This reveals finite-step failures in which a locally improving direction produces a harmful full update. A pathwise curvature decomposition characterises how initial progress is lost, while a local quadratic model predicts full-step gains and useful step scales. Bounds based on accumulated curvature variation characterise the approximation error of these predictions. Experiments across two model families reveal this separation on mathematical and commonsense tasks. A fixed quarter step produces positive gains in reference utility for 72.2--83.2% of selected failures across four settings. These findings identify a mismatch between update direction and step scale as a mechanism of lost progress, explaining how some harmful updates retain useful computation.

摘要：循環Transformer提供了一種參數高效的測試時間擴展方法，通過重用共享層進行迭代潛在推理。<lb/>然而，額外的迭代可能會減少對參考答案的支持，讓人無法清楚地判斷更新的方向是局部無效還是其整體位移移動過遠。<lb/>我們通過分析參考效用來研究這一區別，該效用衡量沿著模型自身的更新方向的支持程度，並變化提供給讀取的建議位移的比例。<lb/>這揭示了有限步驟失敗的情況，其中一個局部改善的方向產生了有害的完整更新。<lb/>一種路徑曲率分解特徵化了初始進展是如何喪失的，而一個局部二次模型則預測了全步驟的增益和有用的步驟尺度。<lb/>基於累積曲率變化的界限特徵化了這些預測的近似誤差。<lb/>在兩個模型系列中的實驗揭示了在數學和常識任務上的這一區分。<lb/>在四個設置中，固定的四分之一步驟在72.2--83.2%的選定失敗中產生了參考效用的正增益。<lb/>這些發現確定了更新方向和步驟尺度之間的不匹配作為進展喪失的一種機制，解釋了為什麼一些有害的更新仍然保留有用的計算。

