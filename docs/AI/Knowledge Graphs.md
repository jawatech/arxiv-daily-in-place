
### Knowledge Graphs
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-04-28**|**TSN-Affinity: Similarity-Driven Parameter Reuse for Continual Offline Reinforcement Learning**|Dominik Żurek et.al.|[2604.25898v1](http://arxiv.org/abs/2604.25898v1)|null|
|**2026-04-28**|**G-Loss: Graph-Guided Fine-Tuning of Language Models**|Sharma Aditya et.al.|[2604.25853v1](http://arxiv.org/abs/2604.25853v1)|null|
|**2026-04-28**|**ADEMA: A Knowledge-State Orchestration Architecture for Long-Horizon Knowledge Synthesis with LLMAgents**|Zhou Hanlin et.al.|[2604.25849v1](http://arxiv.org/abs/2604.25849v1)|null|
|**2026-04-28**|**TrialCalibre: A Fully Automated Causal Engine for RCT Benchmarking and Observational Trial Calibration**|Amir Habibdoust et.al.|[2604.25832v1](http://arxiv.org/abs/2604.25832v1)|null|
|**2026-04-28**|**MAIC-UI: Making Interactive Courseware with Generative UI**|Shangqing Tu et.al.|[2604.25806v1](http://arxiv.org/abs/2604.25806v1)|null|
|**2026-04-28**|**CGU-ILALab at FoodBench-QA 2026: Comparing Traditional and LLM-based Approaches for Recipe Nutrient Estimation**|Wei-Chun Chen et.al.|[2604.25774v1](http://arxiv.org/abs/2604.25774v1)|null|
|**2026-04-28**|**RADD: Retrieval-Augmented Discrete Diffusion for Multi-Modal Knowledge Graph Completion**|Guanglin Niu et.al.|[2604.25693v1](http://arxiv.org/abs/2604.25693v1)|null|
|**2026-04-28**|**Large language models eroding science understanding: an experimental study**|Harry Collins et.al.|[2604.25639v1](http://arxiv.org/abs/2604.25639v1)|null|
|**2026-04-28**|**Benchmarking bandgap prediction in semiconductors under experimental and realistic evaluation settings**|Haolin Wang et.al.|[2604.25568v1](http://arxiv.org/abs/2604.25568v1)|null|
|**2026-04-28**|**From CRUD to Autonomous Agents: Formal Validation and Zero-Trust Security for Semantic Gateways in AI-Native Enterprise Systems**|Ignacio Peyrano et.al.|[2604.25555v1](http://arxiv.org/abs/2604.25555v1)|null|
|**2026-04-28**|**On Halting vs Converging in Recurrent Graph Neural Networks**|Jeroen Bollen et.al.|[2604.25551v1](http://arxiv.org/abs/2604.25551v1)|null|
|**2026-04-28**|**Sample-efficient Neuro-symbolic Proximal Policy Optimization**|Simone Murari et.al.|[2604.25534v1](http://arxiv.org/abs/2604.25534v1)|null|
|**2026-04-28**|**The Surprising Effectiveness of Canonical Knowledge Distillation for Semantic Segmentation**|Muhammad Ali et.al.|[2604.25530v1](http://arxiv.org/abs/2604.25530v1)|null|
|**2026-04-28**|**AI as Consumer and Participant: A Co-Design Agenda for MBSE Substrates and Methodology**|Siyuan Ji et.al.|[2604.25526v1](http://arxiv.org/abs/2604.25526v1)|null|
|**2026-04-28**|**PHISHREV: A Hybrid Machine Learning and Post-Hoc Non-monotonic Reasoning Framework for Context-Aware Phishing Website Classification**|Mainak Sen et.al.|[2604.25512v1](http://arxiv.org/abs/2604.25512v1)|null|
|**2026-04-28**|**Do LLMs Capture Embodied Cognition and Cultural Variation? Cross-Linguistic Evidence from Demonstratives**|Yu Wang et.al.|[2604.25423v1](http://arxiv.org/abs/2604.25423v1)|null|
|**2026-04-28**|**Wiki Dumps to Training Corpora: South Slavic Case**|Mihailo Škorić et.al.|[2604.25384v1](http://arxiv.org/abs/2604.25384v1)|null|
|**2026-04-28**|**CoRE: Concept-Reasoning Expansion for Continual Brain Lesion Segmentation**|Qianqian Chen et.al.|[2604.25376v1](http://arxiv.org/abs/2604.25376v1)|null|
|**2026-04-28**|**Multi-action Tangled Program Graphs for Multi-task Reinforcement Learning with Continuous Control**|Quentin Vacher et.al.|[2604.25369v1](http://arxiv.org/abs/2604.25369v1)|null|
|**2026-04-28**|**Faithfulness-QA: A Counterfactual Entity Substitution Dataset for Training Context-Faithful RAG Models**|Li Ju et.al.|[2604.25313v1](http://arxiv.org/abs/2604.25313v1)|null|
|**2026-04-28**|**Learning from Medical Entity Trees: An Entity-Centric Medical Data Engineering Framework for MLLMs**|Jianghang Lin et.al.|[2604.25296v1](http://arxiv.org/abs/2604.25296v1)|null|
|**2026-04-28**|**AutoResearchBench: Benchmarking AI Agents on Complex Scientific Literature Discovery**|Lei Xiong et.al.|[2604.25256v1](http://arxiv.org/abs/2604.25256v1)|null|
|**2026-04-28**|**Making AI-Assisted Grant Evaluation Auditable without Exposing the Model**|Kemal Bicakci et.al.|[2604.25200v1](http://arxiv.org/abs/2604.25200v1)|null|
|**2026-04-28**|**CroSearch-R1: Better Leveraging Cross-lingual Knowledge for Retrieval-Augmented Generation**|Rui Qi et.al.|[2604.25182v1](http://arxiv.org/abs/2604.25182v1)|null|
|**2026-04-28**|**M$^3$-VQA: A Benchmark for Multimodal, Multi-Entity, Multi-Hop Visual Question Answering**|Jiatong Ma et.al.|[2604.25122v1](http://arxiv.org/abs/2604.25122v1)|null|
|**2026-04-28**|**Knowledge Distillation Must Account for What It Loses**|Wenshuo Wang et.al.|[2604.25110v1](http://arxiv.org/abs/2604.25110v1)|null|
|**2026-04-27**|**PolyKV: A Shared Asymmetrically-Compressed KV Cache Pool for Multi-Agent LLM Inference**|Ishan Patel et.al.|[2604.24971v1](http://arxiv.org/abs/2604.24971v1)|null|
|**2026-04-27**|**S-SONDO: Self-Supervised Knowledge Distillation for General Audio Foundation Models**|Mohammed Ali El Adlouni et.al.|[2604.24933v1](http://arxiv.org/abs/2604.24933v1)|null|
|**2026-04-27**|**asRoBallet: Closing the Sim2Real Gap via Friction-Aware Reinforcement Learning for Underactuated Spherical Dynamics**|Fang Wan et.al.|[2604.24916v1](http://arxiv.org/abs/2604.24916v1)|null|
|**2026-04-27**|**Learning with Embedded Linear Equality Constraints via Variational Bayesian Inference**|Matthew Marsh et.al.|[2604.24911v1](http://arxiv.org/abs/2604.24911v1)|null|
|**2026-04-27**|**MotionBricks: Scalable Real-Time Motions with Modular Latent Generative Model and Smart Primitives**|Tingwu Wang et.al.|[2604.24833v1](http://arxiv.org/abs/2604.24833v1)|null|
|**2026-04-27**|**Personalized Worked Example Generation from Student Code Submissions using Pattern-based Knowledge Components**|Griffin Pitts et.al.|[2604.24758v1](http://arxiv.org/abs/2604.24758v1)|null|
|**2026-04-27**|**On the Trainability of Masked Diffusion Language Models via Blockwise Locality**|Yuxiang Wang et.al.|[2604.24832v1](http://arxiv.org/abs/2604.24832v1)|null|
|**2026-04-27**|**Can Current Agents Close the Discovery-to-Application Gap? A Case Study in Minecraft**|Zhou Ziheng et.al.|[2604.24697v1](http://arxiv.org/abs/2604.24697v1)|null|
|**2026-04-27**|**Can LLMs Act as Historians? Evaluating Historical Research Capabilities of LLMs via the Chinese Imperial Examination**|Lirong Gao et.al.|[2604.24690v1](http://arxiv.org/abs/2604.24690v1)|null|
|**2026-04-27**|**XGRAG: A Graph-Native Framework for Explaining KG-based Retrieval-Augmented Generation**|Zhuoling Li et.al.|[2604.24623v1](http://arxiv.org/abs/2604.24623v1)|null|
|**2026-04-27**|**Incompressible Knowledge Probes: Estimating Black-Box LLM Parameter Counts via Factual Capacity**|Bojie Li et.al.|[2604.24827v1](http://arxiv.org/abs/2604.24827v1)|null|
|**2026-04-27**|**NeSyCat: A Monad-Based Categorical Semantics of the Neurosymbolic ULLER Framework**|Daniel Romero Schellhorn et.al.|[2604.24612v1](http://arxiv.org/abs/2604.24612v1)|null|
|**2026-04-27**|**A systematic evaluation of vision-language models for observational astronomical reasoning tasks**|Wenke Ren et.al.|[2604.24589v1](http://arxiv.org/abs/2604.24589v1)|null|
|**2026-04-27**|**MEG-RAG: Quantifying Multi-modal Evidence Grounding for Evidence Selection in RAG**|Xihang Wang et.al.|[2604.24564v1](http://arxiv.org/abs/2604.24564v1)|null|
|**2026-04-27**|**Layerwise Convergence Fingerprints for Runtime Misbehavior Detection in Large Language Models**|Nay Myat Min et.al.|[2604.24542v1](http://arxiv.org/abs/2604.24542v1)|null|
|**2026-04-27**|**SEARCH-R: Structured Entity-Aware Retrieval with Chain-of-Reasoning Navigator for Multi-hop Question Answering**|Yuqing Fu et.al.|[2604.24515v1](http://arxiv.org/abs/2604.24515v1)|null|
|**2026-04-27**|**Programming with Data: Test-Driven Data Engineering for Self-Improving LLMs from Raw Corpora**|Chenkai Pan et.al.|[2604.24819v1](http://arxiv.org/abs/2604.24819v1)|null|
|**2026-04-27**|**GAMMAF: A Common Framework for Graph-Based Anomaly Monitoring Benchmarking in LLM Multi-Agent Systems**|Pablo Mateo-Torrejón et.al.|[2604.24477v1](http://arxiv.org/abs/2604.24477v1)|null|
|**2026-04-27**|**PhysNote: Self-Knowledge Notes for Evolvable Physical Reasoning in Vision-Language Model**|Sinin Zhang et.al.|[2604.24443v1](http://arxiv.org/abs/2604.24443v1)|null|
|**2026-04-27**|**All That Glitters Is Not Audio: Rethinking Text Priors and Audio Reliance in Audio-Language Evaluation**|Leonardo Haw-Yang Foo et.al.|[2604.24401v1](http://arxiv.org/abs/2604.24401v1)|null|
|**2026-04-27**|**Aligning with Your Own Voice: Self-Corrected Preference Learning for Hallucination Mitigation in LVLMs**|Byeonggeuk Lim et.al.|[2604.24395v1](http://arxiv.org/abs/2604.24395v1)|null|
|**2026-04-27**|**Structural Pruning of Large Vision Language Models: A Comprehensive Study on Pruning Dynamics, Recovery, and Data Efficiency**|Yiran Huang et.al.|[2604.24380v1](http://arxiv.org/abs/2604.24380v1)|null|
|**2026-04-27**|**SeaEvo: Advancing Algorithm Discovery with Strategy Space Evolution**|Sichun Luo et.al.|[2604.24372v1](http://arxiv.org/abs/2604.24372v1)|null|
|**2026-04-27**|**Culture-Aware Machine Translation in Large Language Models: Benchmarking and Investigation**|Zekun Yuan et.al.|[2604.24361v1](http://arxiv.org/abs/2604.24361v1)|null|
|**2026-04-27**|**Generative Design of a Gas Turbine Combustor Using Invertible Neural Networks**|Patrick Krüger et.al.|[2604.24322v1](http://arxiv.org/abs/2604.24322v1)|null|
|**2026-04-27**|**Latent-Hysteresis Graph ODEs: Modeling Coupled Topology-Feature Evolution via Continuous Phase Transitions**|Qinhan Hou et.al.|[2604.24293v1](http://arxiv.org/abs/2604.24293v1)|null|
|**2026-04-27**|**MEMCoder: Multi-dimensional Evolving Memory for Private-Library-Oriented Code Generation**|Mofei Li et.al.|[2604.24222v1](http://arxiv.org/abs/2604.24222v1)|null|
|**2026-04-27**|**Time-varying Interaction Graph ODE for Dynamic Graph Representation Learning**|Xiaoyi Wang et.al.|[2604.24811v1](http://arxiv.org/abs/2604.24811v1)|null|
|**2026-04-27**|**Agentic Witnessing: Pragmatic and Scalable TEE-Enabled Privacy-Preserving Auditing**|Antony Rowstron et.al.|[2604.24203v1](http://arxiv.org/abs/2604.24203v1)|null|
|**2026-04-27**|**Rewarding the Scientific Process: Process-Level Reward Modeling for Agentic Data Analysis**|Zhisong Qiu et.al.|[2604.24198v1](http://arxiv.org/abs/2604.24198v1)|null|
|**2026-04-27**|**MultiDx: A Multi-Source Knowledge Integration Framework towards Diagnostic Reasoning**|Yimin Deng et.al.|[2604.24186v1](http://arxiv.org/abs/2604.24186v1)|null|
|**2026-04-27**|**AdapTime: Enabling Adaptive Temporal Reasoning in Large Language Models**|Yimin Deng et.al.|[2604.24175v1](http://arxiv.org/abs/2604.24175v1)|null|
|**2026-04-27**|**Psychologically-Grounded Graph Modeling for Interpretable Depression Detection**|Rishitej Reddy Vyalla et.al.|[2604.24126v1](http://arxiv.org/abs/2604.24126v1)|null|
|**2026-04-27**|**From Prototype to Classroom: An Intelligent Tutoring System for Quantum Education**|Iizalaarab Elhaimeur et.al.|[2604.24807v1](http://arxiv.org/abs/2604.24807v1)|null|
|**2026-04-27**|**Factual and Edit-Sensitive Graph-to-Sequence Generation via Graph-Aware Adaptive Noising**|Aditya Hemant Shahane et.al.|[2604.24104v1](http://arxiv.org/abs/2604.24104v1)|null|
|**2026-04-27**|**The Pragmatic Persona: Discovering LLM Persona through Bridging Inference**|Jisoo Yang et.al.|[2604.24079v1](http://arxiv.org/abs/2604.24079v1)|null|
|**2026-04-27**|**Grounding Before Generalizing: How AI Differs from Humans in Causal Transfer**|Liangru Xiang et.al.|[2604.24062v1](http://arxiv.org/abs/2604.24062v1)|null|
|**2026-04-27**|**From Skill Text to Skill Structure: The Scheduling-Structural-Logical Representation for Agent Skills**|Qiliang Liang et.al.|[2604.24026v2](http://arxiv.org/abs/2604.24026v2)|null|
|**2026-04-27**|**Propagation Structure-Semantic Transfer Learning for Robust Fake News Detection**|Mengyang Chen et.al.|[2604.23974v1](http://arxiv.org/abs/2604.23974v1)|null|
|**2026-04-27**|**Quantum Knowledge Graph: Modeling Context-Dependent Triplet Validity**|Yao Wang et.al.|[2604.23972v1](http://arxiv.org/abs/2604.23972v1)|null|
|**2026-04-27**|**LLM-Guided Agentic Floor Plan Parsing for Accessible Indoor Navigation of Blind and Low-Vision People**|Aydin Ayanzadeh et.al.|[2604.23970v1](http://arxiv.org/abs/2604.23970v1)|null|
|**2026-04-27**|**GamED.AI: A Hierarchical Multi-Agent Framework for Automated Educational Game Generation**|Shiven Agarwal et.al.|[2604.23947v2](http://arxiv.org/abs/2604.23947v2)|null|
|**2026-04-27**|**Agentic AI platforms for autonomous training and rule induction of human-human and virus-human protein-protein interactions**|Hung N. Do et.al.|[2604.23924v1](http://arxiv.org/abs/2604.23924v1)|null|
|**2026-04-26**|**Knowledge Vector of Logical Reasoning in Large Language Models**|Zixuan Wang et.al.|[2604.23877v1](http://arxiv.org/abs/2604.23877v1)|null|
|**2026-04-26**|**Graph Memory Transformer (GMT)**|Nicola Zanarini et.al.|[2604.23862v1](http://arxiv.org/abs/2604.23862v1)|null|
|**2026-04-26**|**Scalable Production Scheduling: Linear Complexity via Unified Homogeneous Graphs**|Jonathan Hoss et.al.|[2604.23841v1](http://arxiv.org/abs/2604.23841v1)|null|
|**2026-04-26**|**Domain-Filtered Knowledge Graphs from Sparse Autoencoder Features**|John Winnicki et.al.|[2604.23829v2](http://arxiv.org/abs/2604.23829v2)|null|
|**2026-04-26**|**Domain Fine-Tuning vs. Retrieval-Augmented Generation for Medical Multiple-Choice Question Answering: A Controlled Comparison at the 4B-Parameter Scale**|Avi-ad Avraam Buskila et.al.|[2604.23801v1](http://arxiv.org/abs/2604.23801v1)|null|
|**2026-04-26**|**The Override Gap: A Magnitude Account of Knowledge Conflict Failure in Hypernetwork-Based Instant LLM Adaptation**|Shuaizhi Cheng et.al.|[2604.23750v1](http://arxiv.org/abs/2604.23750v1)|null|
|**2026-04-26**|**Expert Evaluation of LLM's Open-Ended Legal Reasoning on the Japanese Bar Exam Writing Task**|Jungmin Choi et.al.|[2604.23730v1](http://arxiv.org/abs/2604.23730v1)|null|
|**2026-04-26**|**ESIA: An Energy-Based Spatiotemporal Interaction-Aware Framework for Pedestrian Intention Prediction**|Yanping Wu et.al.|[2604.23728v1](http://arxiv.org/abs/2604.23728v1)|null|
|**2026-04-26**|**An AI-Based Supervisory Measurement Integrity Validation Layer for Cyber-Resilient AC/DC Protection in Inverter-Based Microgrids**|Ahmad Mohammad Saber et.al.|[2604.23666v1](http://arxiv.org/abs/2604.23666v1)|null|
|**2026-04-26**|**Structural Enforcement of Goal Integrity in AI Agents via Separation-of-Powers Architecture**|Rong Xiang et.al.|[2604.23646v1](http://arxiv.org/abs/2604.23646v1)|null|
|**2026-04-26**|**RaV-IDP: A Reconstruction-as-Validation Framework for Faithful Intelligent Document Processing**|Pritesh Jha et.al.|[2604.23644v1](http://arxiv.org/abs/2604.23644v1)|null|
|**2026-04-26**|**GraphPlanner: Graph Memory-Augmented Agentic Routing for Multi-Agent LLMs**|Tao Feng et.al.|[2604.23626v1](http://arxiv.org/abs/2604.23626v1)|null|
|**2026-04-26**|**ComplianceNLP: Knowledge-Graph-Augmented RAG for Multi-Framework Regulatory Gap Detection**|Dongxin Guo et.al.|[2604.23585v1](http://arxiv.org/abs/2604.23585v1)|null|
|**2026-04-26**|**AgentEval: DAG-Structured Step-Level Evaluation for Agentic Workflows with Error Propagation Tracking**|Dongxin Guo et.al.|[2604.23581v1](http://arxiv.org/abs/2604.23581v1)|null|
|**2026-04-26**|**RouteNLP: Closed-Loop LLM Routing with Conformal Cascading and Distillation Co-Optimization**|Dongxin Guo et.al.|[2604.23577v1](http://arxiv.org/abs/2604.23577v1)|null|
|**2026-04-26**|**COMO: Closed-Loop Optical Molecule Recognition with Minimum Risk Training**|Zhuoqi Lyu et.al.|[2604.23546v1](http://arxiv.org/abs/2604.23546v1)|null|
|**2026-04-26**|**Do Transaction-Level and Actor-Level AML Queues Agree? An Empirical Evaluation of Granularity Effects on the Elliptic++ Graph**|Ankur Malik et.al.|[2604.23494v1](http://arxiv.org/abs/2604.23494v1)|null|
|**2026-04-26**|**K-SENSE: A Knowledge-Guided Self-Augmented Encoder for Neuro-Semantic Evaluation of Mental Health Conditions on Social Media**|Vijay Yadav et.al.|[2604.23493v2](http://arxiv.org/abs/2604.23493v2)|null|
|**2026-04-25**|**Hybrid JIT-CUDA Graph Optimization for Low-Latency Large Language Model Inference**|Divakar Kumar Yadav et.al.|[2604.23467v1](http://arxiv.org/abs/2604.23467v1)|null|
|**2026-04-25**|**IndustryAssetEQA: A Neurosymbolic Operational Intelligence System for Embodied Question Answering in Industrial Asset Maintenance**|Chathurangi Shyalika et.al.|[2604.23446v1](http://arxiv.org/abs/2604.23446v1)|null|
|**2026-04-25**|**Automating Categorization of Scientific Texts with In-Context Learning and Prompt-Chaining in Large Language Models**|Gautam Kishore Shahi et.al.|[2604.23430v1](http://arxiv.org/abs/2604.23430v1)|null|
|**2026-04-25**|**Evolve: A Persistent Knowledge Lifecycle for Small Language Models**|Dikran Hovagimian et.al.|[2604.23424v1](http://arxiv.org/abs/2604.23424v1)|null|
|**2026-04-25**|**Beyond Local vs. External: A Game-Theoretic Framework for Trustworthy Knowledge Acquisition**|Rujing Yao et.al.|[2604.23413v1](http://arxiv.org/abs/2604.23413v1)|null|
|**2026-04-25**|**SoccerRef-Agents: Multi-Agent System for Automated Soccer Refereeing**|Zi Meng et.al.|[2604.23392v1](http://arxiv.org/abs/2604.23392v1)|null|
|**2026-04-25**|**A Parametric Memory Head for Continual Generative Retrieval**|Kidist Amde Mekonnen et.al.|[2604.23388v1](http://arxiv.org/abs/2604.23388v1)|null|
|**2026-04-25**|**GSAR: Typed Grounding for Hallucination Detection and Recovery in Multi-Agent LLMs**|Federico A. Kamelhar et.al.|[2604.23366v1](http://arxiv.org/abs/2604.23366v1)|null|
|**2026-04-25**|**VeriLLMed: Interactive Visual Debugging of Medical Large Language Models with Knowledge Graphs**|Yurui Xiang et.al.|[2604.23356v1](http://arxiv.org/abs/2604.23356v1)|null|
|**2026-04-25**|**Bridging Reasoning and Action: Hybrid LLM-RL Framework for Efficient Cross-Domain Task-Oriented Dialogue**|Yangyang Zhao et.al.|[2604.23345v1](http://arxiv.org/abs/2604.23345v1)|null|
|**2026-04-25**|**EAD-Net: Emotion-Aware Talking Head Generation with Spatial Refinement and Temporal Coherence**|Yahui Li et.al.|[2604.23325v1](http://arxiv.org/abs/2604.23325v1)|null|
|**2026-04-25**|**$\mathcal{S}^2$IT: Stepwise Syntax Integration Tuning for Large Language Models in Aspect Sentiment Quad Prediction**|Bingfeng Chen et.al.|[2604.23296v1](http://arxiv.org/abs/2604.23296v1)|null|
|**2026-04-25**|**From Similarity to Structure: Training-free LLM Context Compression with Hybrid Graph Priors**|Yitian Zhou et.al.|[2604.23277v1](http://arxiv.org/abs/2604.23277v1)|null|

#### Abstracts
##### **TSN-Affinity: Similarity-Driven Parameter Reuse for Continual Offline Reinforcement Learning**
2604.25898v1 by Dominik Żurek, Kamil Faber, Marcin Pietron, Paweł Gajewski, Roberto Corizzo

Continual offline reinforcement learning (CORL) aims to learn a sequence of tasks from datasets collected over time while preserving performance on previously learned tasks. This setting corresponds to domains where new tasks arise over time, but adapting the model in live environment interactions is expensive, risky, or impossible. However, CORL inherits the dual difficulty of offline reinforcement learning and adapting while preventing catastrophic forgetting. Replay-based continual learning approaches remain a strong baseline but incur memory overhead and suffer from a distribution mismatch between replayed samples and newly learned policies. At the same time, architectural continual learning methods have shown strong potential in supervised learning but remain underexplored in CORL. In this work, we propose TSN-Affinity, a novel CORL method based on TinySubNetworks and Decision Transformer. The method enables task-specific parameterization and controlled knowledge sharing through a RL-aware reuse strategy that routes tasks according to action compatibility and latent similarity. We evaluate the approach on benchmarks based on Atari games and simulations of manipulation tasks with the Franka Emika Panda robotic arm, covering both discrete and continuous control. Results show strong retention from sparse SubNetworks, with routing further improving multi-task performance. Our findings suggest that similarity-guided architectural reuse is a strong and viable alternative to replay-based strategies in a CORL setting. Our code is available at: https://github.com/anonymized-for-submission123/tsn-affinity.

摘要：持續離線強化學習（CORL）的目標是從隨時間收集的數據集中學習一系列任務，同時保持對先前學習任務的表現。這種設置對應於隨時間出現新任務的領域，但在實時環境互動中調整模型是昂貴的、風險高的或不可能的。然而，CORL 繼承了離線強化學習和適應的雙重困難，同時防止災難性遺忘。基於重播的持續學習方法仍然是一個強大的基準，但會產生內存開銷，並且在重播樣本和新學習策略之間存在分佈不匹配的問題。與此同時，架構持續學習方法在監督學習中顯示出強大的潛力，但在 CORL 中仍然未被充分探索。在這項工作中，我們提出了 TSN-Affinity，一種基於 TinySubNetworks 和 Decision Transformer 的新型 CORL 方法。該方法通過一種 RL 感知的重用策略，使得任務特定的參數化和受控知識共享，根據行動兼容性和潛在相似性來路由任務。我們在基於 Atari 遊戲的基準和使用 Franka Emika Panda 機器手臂的操作任務模擬上評估了該方法，涵蓋了離散和連續控制。結果顯示，稀疏 SubNetworks 的保留效果良好，路由進一步提高了多任務性能。我們的發現表明，相似性引導的架構重用是 CORL 設置中一種強大且可行的替代方案，取代基於重播的策略。我們的代碼可在以下網址獲得：https://github.com/anonymized-for-submission123/tsn-affinity。

##### **G-Loss: Graph-Guided Fine-Tuning of Language Models**
2604.25853v1 by Sharma Aditya, Agarwal Vinti, Kumar Rajesh

Traditional loss functions, including cross-entropy, contrastive, triplet, and su pervised contrastive losses, used for fine-tuning pre-trained language models such as BERT, operate only within local neighborhoods and fail to account for the global semantic structure. We present G-Loss, a graph-guided loss function that incorporates semi-supervised label propagation to use structural relationships within the embedding manifold. G-Loss builds a document-similarity graph that captures global semantic relationships, thereby guiding the model to learn more discriminative and robust embeddings. We evaluate G-Loss on five benchmark datasets covering key downstream classification tasks: MR (sentiment analysis), R8 and R52 (topic categorization), Ohsumed (medical document classification), and 20NG (news categorization). In the majority of experimental setups, G-Loss converges faster and produces semantically coherent embedding spaces, resulting in higher classification accuracy than models fine-tuned with traditional loss functions.

摘要：傳統的損失函數，包括交叉熵、對比損失、三元組損失和監督對比損失，應用於微調預訓練的語言模型如 BERT，僅在局部鄰域內運作，未能考慮全局語義結構。<lb/>我們提出了 G-Loss，一種圖引導的損失函數，結合半監督標籤傳播，以利用嵌入流形內的結構關係。<lb/>G-Loss 構建了一個文檔相似性圖，捕捉全局語義關係，從而引導模型學習更具辨別性和穩健性的嵌入。<lb/>我們在五個基準數據集上評估 G-Loss，涵蓋關鍵的下游分類任務：MR（情感分析）、R8 和 R52（主題分類）、Ohsumed（醫療文檔分類）以及 20NG（新聞分類）。<lb/>在大多數實驗設置中，G-Loss 收斂更快，並產生語義一致的嵌入空間，從而比使用傳統損失函數微調的模型獲得更高的分類準確性。

##### **ADEMA: A Knowledge-State Orchestration Architecture for Long-Horizon Knowledge Synthesis with LLMAgents**
2604.25849v1 by Zhou Hanlin, Chan Huah Yong

Long-horizon LLM tasks often fail not because a single answer is unattainable, but because knowledge states drift across rounds, intermediate commitments remain implicit, and interruption fractures the evolving evidence chain. This paper presents ADEMA as a knowledge-state orchestration architecture for long-horizon knowledge synthesis rather than as a generic multi-agent runtime. The architecture combines explicit epistemic bookkeeping, heterogeneous dual-evaluator governance, adaptive task-mode switching, reputation-shaped resource allocation, checkpoint-resumable persistence, segment-level memory condensation, artifact-first assembly, and final-validity checking with safe fallback. Evidence is drawn entirely from existing materials: a four-scenario showcase package, a fixed 60-run mechanism matrix, targeted micro-ablation and artifact-chain supplements, and a repaired protocol-level benchmark in which code-oriented evaluation is the clearest quality-sensitive mechanism block. Across the fixed matrix, removing checkpoint/resume produced the only invalid run, and it did so in the interruption-sensitive resume condition. By contrast, dual evaluation, segment synthesis, and dynamic governance are best interpreted as supporting control mechanisms that shape trajectory discipline, explicit artifact progression, and cost-quality behavior rather than as universal binary prerequisites for completion. The contribution is therefore a knowledge-state orchestration architecture in which explicit epistemic state transition, evidence-bearing artifact progression, and recoverable continuity are the primary design commitments.

摘要：長期的 LLM 任務經常失敗，並非因為單一答案無法獲得，而是因為知識狀態在回合之間漂移、中間承諾保持隱含，且中斷破壞了不斷演變的證據鏈。本文提出 ADEMA 作為長期知識綜合的知識狀態協調架構，而非通用的多代理運行時。該架構結合了明確的認識記錄、異質的雙評估治理、自適應任務模式切換、基於聲譽的資源分配、可檢查恢復的持久性、段級記憶凝聚、以工件為先的組裝，以及最終有效性檢查和安全回退。證據完全來自現有材料：一個四場景展示包、一個固定的 60 次運行機制矩陣、針對性的微剝除和工件鏈補充，以及一個修復的協議級基準，其中以代碼為導向的評估是最明確的質量敏感機制區塊。在固定矩陣中，移除檢查點/恢復產生了唯一的無效運行，並且是在對中斷敏感的恢復條件下發生的。相比之下，雙重評估、段合成和動態治理最好被解釋為支持控制機制，這些機制塑造了軌跡紀律、明確的工件進展和成本-質量行為，而不是作為完成的普遍二元前提。因此，貢獻是一個知識狀態協調架構，其中明確的認識狀態轉變、承載證據的工件進展和可恢復的連續性是主要的設計承諾。

##### **TrialCalibre: A Fully Automated Causal Engine for RCT Benchmarking and Observational Trial Calibration**
2604.25832v1 by Amir Habibdoust, Xing Song

Real-world evidence (RWE) studies that emulate target trials increasingly inform regulatory and clinical decisions, yet residual, hard-to-quantify biases still limit their credibility. The recently proposed BenchExCal framework addresses this challenge via a two-stage Benchmark, Expand, Calibrate process, which first compares an observational emulation against an existing randomized controlled trial (RCT), then uses observed divergence to calibrate a second emulation for a new indication causal effect estimation. While methodologically powerful, BenchExCal is resource intensive and difficult to scale. We introduce TrialCalibre, a conceptualized multiagent system designed to automate and scale the BenchExCal workflow. Our framework features specialized agents such as the Orchestrator, Protocol Design, Data Synthesis, Clinical Validation, and Quantitative Calibration Agents that coordi-nate the the overall process. TrialCalibre incorpo-rates agent learning (e.g., RLHF) and knowledge blackboards to support adaptive, auditable, and transparent causal effect estimation.

摘要：真實世界證據 (RWE) 研究模擬目標試驗，越來越多地影響監管和臨床決策，但仍然存在難以量化的偏差限制其可信度。最近提出的 BenchExCal 框架通過兩階段的基準、擴展、校準過程來應對這一挑戰，首先將觀察性模擬與現有的隨機對照試驗 (RCT) 進行比較，然後利用觀察到的差異來校準第二次模擬，以估計新的適應症因果效應。雖然方法論上強大，但 BenchExCal 資源密集且難以擴展。我們介紹了 TrialCalibre，一個概念化的多代理系統，旨在自動化和擴展 BenchExCal 工作流程。我們的框架具有專門的代理，例如協調者、方案設計、數據合成、臨床驗證和定量校準代理，協調整個過程。TrialCalibre 結合了代理學習 (例如，RLHF) 和知識黑板，以支持自適應、可審計和透明的因果效應估計。

##### **MAIC-UI: Making Interactive Courseware with Generative UI**
2604.25806v1 by Shangqing Tu, Yanjia Li, Keyu Chen, Sichen Zhang, Jifan Yu, Daniel Zhang-Li, Lei Hou, Juanzi Li, Yu Zhang, Huiqin Liu

Creating interactive STEM courseware traditionally requires HTML/CSS/JavaScript expertise, leaving barriers for educators. While generative AI can produce HTML codes, existing tools generate static presentations rather than interactive simulations, struggle with long documents, and lack pedagogical accuracy mechanisms. Furthermore, full regeneration for modifications requires 200--600 seconds, disrupting creative flow. We present MAIC-UI, a zero-code authoring system that enables educators to create and rapidly edit interactive courseware from textbooks, PPTs, and PDFs. MAIC-UI employs: (1) structured knowledge analysis with multi-modal understanding to ensure pedagogical rigor; (2) a two-stage generate-verify-optimize pipeline separating content alignment from visual refinement; and (3) Click-to-Locate editing with Unified Diff-based incremental generation achieving sub-10-second iteration cycles. A controlled lab study with 40 participants shows MAIC-UI reduces editing iterations (4.9 vs. 7.0) and significantly improves learnability and controllability compared to direct Text-to-HTML generation. A three-month classroom deployment with 53 high school students demonstrates that MAIC-UI fosters learning agency and reduces outcome disparities -- the pilot class achieved 9.21-point gains in STEM subjects compared to -2.32 points in control classes. Our code is available at https://github.com/THU-MAIC/MAIC-UI.

摘要：創建互動式 STEM 課程材料傳統上需要 HTML/CSS/JavaScript 專業知識，這對教育工作者造成了障礙。雖然生成式 AI 可以產生 HTML 代碼，但現有工具生成的是靜態演示，而不是互動模擬，並且在處理長文檔時表現不佳，缺乏教學準確性機制。此外，對於修改的完全重生成需要 200--600 秒，這會干擾創意流程。我們提出了 MAIC-UI，一個零代碼的創作系統，使教育工作者能夠從教科書、PPT 和 PDF 中創建和快速編輯互動課程材料。MAIC-UI 採用了：（1）結構化知識分析與多模態理解，以確保教學的嚴謹性；（2）一個兩階段的生成-驗證-優化流程，將內容對齊與視覺精煉分開；以及（3）基於統一差異的 Click-to-Locate 編輯，實現了小於 10 秒的迭代周期。一項包含 40 名參與者的控制實驗室研究顯示，MAIC-UI 減少了編輯迭代次數（4.9 次對 7.0 次），並顯著提高了可學習性和可控性，與直接的文本轉 HTML 生成相比。與 53 名高中生進行的三個月課堂部署顯示，MAIC-UI 促進了學習主動性並減少了結果差異——試點班在 STEM 科目上取得了 9.21 分的增長，而對照班則為 -2.32 分。我們的代碼可在 https://github.com/THU-MAIC/MAIC-UI 獲得。

##### **CGU-ILALab at FoodBench-QA 2026: Comparing Traditional and LLM-based Approaches for Recipe Nutrient Estimation**
2604.25774v1 by Wei-Chun Chen, Yu-Xuan Chen, I-Fang Chung, Ying-Jia Lin

Accurate nutrient estimation from unstructured recipe text is an important yet challenging problem in dietary monitoring, due to ambiguous ingredient terminology and highly variable quantity expressions. We systematically evaluate models spanning a wide range of representational capacity, from lexical matching methods (TF-IDF with Ridge Regression), to deep semantic encoders (DeBERTa-v3), to generative reasoning with large language models (LLMs). Under the strict tolerance criteria defined by EU Regulation 1169/2011, our empirical results reveal a clear trade-off between predictive accuracy and computational efficiency. The TF-IDF baseline achieves moderate nutrient estimation performance with near-instantaneous inference, whereas the DeBERTa-v3 encoder performs poorly under task-specific data scarcity. In contrast, few-shot LLM inference (e.g., Gemini 2.5 Flash) and a hybrid LLM refinement pipeline (TF-IDF combined with Gemini 2.5 Flash) deliver the highest validation accuracy across all nutrient categories. These improvements likely arise from the ability of LLMs to leverage pre-trained world knowledge to resolve ambiguous terminology and normalize non-standard units, which remain difficult for purely lexical approaches. However, these gains come at the cost of substantially higher inference latency, highlighting a practical deployment trade-off between real-time efficiency and nutritional precision in dietary monitoring systems.

摘要：從非結構化食譜文本中準確估算營養成分是一個重要但具挑戰性的問題，因為成分術語模糊且數量表達變化多端。<lb/>我們系統地評估了涵蓋廣泛表示能力的模型，從詞彙匹配方法（TF-IDF與脊回歸）到深度語義編碼器（DeBERTa-v3），再到使用大型語言模型（LLMs）的生成推理。<lb/>根據歐盟法規1169/2011定義的嚴格容忍標準，我們的實證結果顯示預測準確性與計算效率之間存在明顯的權衡。<lb/>TF-IDF基線在幾乎瞬時推理的情況下實現了適度的營養估算性能，而DeBERTa-v3編碼器在特定任務數據稀缺的情況下表現不佳。<lb/>相對而言，少量示例的LLM推理（例如，Gemini 2.5 Flash）和混合LLM精煉管道（TF-IDF與Gemini 2.5 Flash結合）在所有營養類別中提供了最高的驗證準確性。<lb/>這些改進可能源於LLMs利用預訓練的世界知識來解決模糊術語並標準化非標準單位的能力，而這對於純詞彙方法來說仍然困難。<lb/>然而，這些增益的代價是推理延遲顯著增加，突顯了在飲食監測系統中實時效率與營養精確性之間的實際部署權衡。

##### **RADD: Retrieval-Augmented Discrete Diffusion for Multi-Modal Knowledge Graph Completion**
2604.25693v1 by Guanglin Niu, Bo Li

Most multi-modal knowledge graph completion (MMKGC) models use one embedding scorer to do both retrieval over the full entity set and final decision making. We argue that this coupling is a core bottleneck: global high-recall search and local fine-grained disambiguation require different inductive biases. Therefore, we propose a Retrieval-Augmented Discrete Diffusion (RADD) framework to decouple retrieve and reranking for MMKGC. A relation-aware multimodal KGE retriever serves as both global retriever and distillation teacher, while a conditional discrete denoiser performs shortlist-level entity-identity generation for reranking. Training combines KGE supervision, denoising cross-entropy, and temperature-scaled distillation from the retriever to the denoiser. At inference, the designed Diff-Rerank first forms a top-$K$ shortlist with the retriever and then reranks it with the denoiser, ensuring that recall is a strict prerequisite for precision. Experiments on three MMKGC benchmarks show that RADD achieves the best performance and consistent gains over strong unimodal, multimodal, and LLM-based baselines, while ablations further verify the contribution of each component.

摘要：大多數多模態知識圖譜補全（MMKGC）模型使用一個嵌入評分器來進行整個實體集的檢索和最終決策。我們認為這種耦合是核心瓶頸：全局高召回率的搜索和局部細粒度的消歧需要不同的歸納偏見。因此，我們提出了一個檢索增強離散擴散（RADD）框架，以解耦MMKGC的檢索和重排序。一個關係感知的多模態KGE檢索器同時作為全局檢索器和蒸餾教師，而一個條件離散去噪器則在重排序中執行短名單級別的實體身份生成。訓練結合了KGE監督、去噪交叉熵和從檢索器到去噪器的溫度縮放蒸餾。在推理階段，設計的Diff-Rerank首先使用檢索器形成一個前$K$的短名單，然後使用去噪器對其進行重排序，確保召回是精確的嚴格前提。在三個MMKGC基準上的實驗表明，RADD實現了最佳性能並在強大的單模態、多模態和基於LLM的基準上持續獲得增益，而消融實驗進一步驗證了每個組件的貢獻。

##### **Large language models eroding science understanding: an experimental study**
2604.25639v1 by Harry Collins, Hartmut Grote, Paul Newbury, Patrick Sutton, Simon Thorne

This paper is under review in AI and Ethics This study examines whether large language models (LLMs) can reliably answer scientific questions and demonstrates how easily they can be influenced by fringe scientific material. The authors modified custom LLMs to prioritise knowledge in selected fringe papers on the Fine Structure Constant and Gravitational Waves, then compared their responses with those of domain experts and standard LLMs. The altered models produced fluent, convincing answers that contradicted scientific consensus and were difficult for non-experts to detect as misleading. The results show that LLMs are vulnerable to manipulation and cannot replace expert judgment, highlighting risks for public understanding of science and the potential spread of misinformation.

摘要：這篇論文正在AI與倫理領域進行審查。<lb/>本研究探討大型語言模型（LLMs）是否能可靠地回答科學問題，並展示它們如何容易受到邊緣科學材料的影響。<lb/>作者修改了自訂的LLMs，以優先考慮在選定的邊緣論文中有關精細構造常數和引力波的知識，然後將它們的回應與領域專家的回應以及標準LLMs的回應進行比較。<lb/>這些修改過的模型產生了流暢且令人信服的答案，這些答案與科學共識相矛盾，並且對於非專家來說，難以識別為誤導性。<lb/>結果顯示，LLMs容易受到操控，無法取代專家的判斷，這突顯了對公眾科學理解的風險以及錯誤資訊潛在擴散的可能性。

##### **Benchmarking bandgap prediction in semiconductors under experimental and realistic evaluation settings**
2604.25568v1 by Haolin Wang, Xianyuan Liu, Anna Jungbluth, Alexandra J. Ramadan, Robert D. J. Oliver, Haiping Lu

Accurate bandgap prediction is crucial for semiconductor applications, yet machine learning models trained on computational data often struggle to generalize to experimental bandgap measurements. Challenges related to data fidelity, domain generalization, and model interpretability remain insufficiently addressed in existing evaluation frameworks. To bridge this gap, we introduce RealMat-BaG, a benchmark for assessing model reliability under experimentally relevant conditions. We curate an open-access dataset of experimental bandgaps with aligned crystal structures and compare graph neural networks as well as classical machine learning baselines. Our framework evaluates performance across statistical and domain-based splits, examines transfer from DFT-computed to experimental bandgaps, and analyzes interpretability at both elemental-property and structural levels. Our results reveal the fundamental generalization limitations of current bandgap prediction models and establish a benchmark aligned with experimental measurements for developing more reliable learning strategies for materials discovery.

摘要：準確的能隙預測對半導體應用至關重要，但基於計算數據訓練的機器學習模型往往難以泛化到實驗能隙測量上。<lb/>與數據真實性、領域泛化和模型可解釋性相關的挑戰在現有的評估框架中仍然未得到充分解決。<lb/>為了填補這一空白，我們推出了RealMat-BaG，一個用於評估模型在實驗相關條件下可靠性的基準。<lb/>我們整理了一個開放訪問的實驗能隙數據集，並對齊了晶體結構，並比較了圖神經網絡和傳統機器學習基準。<lb/>我們的框架在統計和基於領域的劃分中評估性能，檢查從DFT計算到實驗能隙的轉移，並在元素性質和結構層面分析可解釋性。<lb/>我們的結果揭示了當前能隙預測模型的基本泛化限制，並建立了一個與實驗測量對齊的基準，以開發更可靠的材料發現學習策略。

##### **From CRUD to Autonomous Agents: Formal Validation and Zero-Trust Security for Semantic Gateways in AI-Native Enterprise Systems**
2604.25555v1 by Ignacio Peyrano

Enterprise software engineering is shifting away from deterministic CRUD/REST architectures toward AI-native systems where large language models act as cognitive orchestrators. This transition introduces a critical security tension: probabilistic LLMs weaken classical mechanisms for validation, access control, and formal testing.
  This paper proposes the design, formal validation, and empirical evaluation of a Semantic Gateway governed by the Model Context Protocol (MCP). The gateway reframes the enterprise API as a semantic surface where tools are dynamically discovered, authorized, and executed based on intent and policy enforcement. The central contribution rests on a paradigm shift: autonomous agents must not be validated as traditional software nor as simple API consumers, but as stochastic state-transition systems whose behavior must be abstracted, fuzzed, and audited through enabled-tool graphs.
  The architecture introduces a three-layer Zero-Trust security model comprising a pre-inference Semantic Firewall, deterministic Tool-Level RBAC, and out-of-band Cryptographic Human-in-the-Loop approval. Enabledness-Preserving Abstractions (EPAs) and greybox semantic fuzzing--originally developed for blockchain smart contract verification--are adapted to audit agent behavior in enterprise environments. Results demonstrate an 84.2% reduction in incidental code. Across 500,000 multi-turn fuzzing sequences, the methodology achieved a 100% discovery rate of hidden unauthorized state transitions, proving that dynamic formal verification is strictly necessary for secure agentic deployment.

摘要：企業軟體工程正在從確定性的 CRUD/REST 架構轉向 AI 原生系統，在這些系統中，大型語言模型充當認知協調者。這一轉變引入了一個關鍵的安全緊張：概率性 LLM 削弱了傳統的驗證、訪問控制和正式測試機制。<lb/>  
本文提出了一個由模型上下文協議 (MCP) 管理的語義閘道的設計、正式驗證和實證評估。該閘道將企業 API 重新框架為一個語義表面，工具根據意圖和政策執行動態發現、授權和執行。<lb/>  
核心貢獻在於一種範式轉變：自主代理不應被視為傳統軟體或簡單的 API 消費者，而應被視為隨機狀態轉換系統，其行為必須通過啟用工具圖進行抽象、模糊和審計。<lb/>  
該架構引入了一個三層零信任安全模型，包括預推理語義防火牆、確定性工具級 RBAC 和帶外的加密人類介入批准。保持啟用性的抽象 (EPAs) 和灰盒語義模糊測試——最初為區塊鏈智能合約驗證而開發——被調整用於審計企業環境中的代理行為。<lb/>  
結果顯示意外代碼減少了 84.2%。在 500,000 個多輪模糊測試序列中，該方法實現了 100% 隱藏未授權狀態轉換的發現率，證明動態正式驗證對於安全的代理部署是絕對必要的。

##### **On Halting vs Converging in Recurrent Graph Neural Networks**
2604.25551v1 by Jeroen Bollen, Stijn Vansummeren

Recurrent Graph Neural Networks (RGNNs) extend standard GNNs by iterating message-passing until some stopping condition is met. Various RGNN models have been proposed in the literature. In this paper, we study three such models: converging RGNNs, where all vertex representations must stabilise; output-converging RGNNs, where only the output classifications must stabilise; and halting RGNNs, where a per-vertex halting classifier determines when to stop. We establish expressiveness relationships between these models: over undirected graphs, converging RGNNs are equally expressive as graded-bisimulation-invariant halting RGNNs, while output-converging RGNNs are at least as expressive. Combined with prior results on halting RGNNs, this shows that, relative to the classifiers expressible in monadic second-order logic (MSO), converging RGNNs express exactly the graded modal $μ$-calculus ($μ$GML), and output-converging RGNNs express at least $μ$GML. These results hold even when restricting to ReLU networks with sum aggregation. The main technical challenge is simulating halting RGNNs by converging ones: without a global halting classifier, vertices may locally decide to halt at different times, causing desynchronisation. We develop a "traffic-light" protocol that enables vertices to coordinate despite this asynchrony. Our results answer an open question from Bollen et al. (2025) and show that the RGNN model of Pflueger et al. (2024) retains full $μ$GML expressiveness even when convergence is guaranteed.

摘要：重複圖神經網絡（RGNNs）通過迭代消息傳遞來擴展標準的GNNs，直到滿足某些停止條件。文獻中提出了各種RGNN模型。在本文中，我們研究了三種此類模型：收斂RGNNs，其中所有頂點表示必須穩定；輸出收斂RGNNs，其中只有輸出分類必須穩定；以及停止RGNNs，其中每個頂點的停止分類器決定何時停止。我們建立了這些模型之間的表達關係：在無向圖上，收斂RGNNs的表達能力與分級雙模不變的停止RGNNs相同，而輸出收斂RGNNs至少具有同樣的表達能力。結合之前對停止RGNNs的結果，這顯示，相對於在單子二階邏輯（MSO）中可表達的分類器，收斂RGNNs精確表達分級模態$μ$-演算（$μ$GML），而輸出收斂RGNNs至少表達$μ$GML。這些結果即使在限制為具有和聚合的ReLU網絡時也成立。主要的技術挑戰是用收斂的RGNNs來模擬停止的RGNNs：在沒有全局停止分類器的情況下，頂點可能會在不同的時間局部決定停止，導致不同步。我們開發了一種“交通燈”協議，使頂點能夠儘管存在這種非同步性而協調。我們的結果回答了Bollen等人（2025）的一個未解問題，並顯示Pflueger等人（2024）的RGNN模型即使在保證收斂的情況下也保留了完整的$μ$GML表達能力。

##### **Sample-efficient Neuro-symbolic Proximal Policy Optimization**
2604.25534v1 by Simone Murari, Celeste Veronese, Daniele Meli

Deep Reinforcement Learning (DRL) algorithms often require a large amount of data and struggle in sparse-reward domains with long planning horizons and multiple sub-goals. In this paper, we propose a neuro-symbolic extension of Proximal Policy Optimization (PPO) that transfers partial logical policy specifications learned in easier instances to guide learning in more challenging settings. We introduce two integrations of symbolic guidance: (i) H-PPO-Product, which biases the action distribution at sampling time, and (ii) H-PPO-SymLoss, which augments the PPO loss with a symbolic regularization term. We evaluate our methods on three benchmarks (OfficeWorld, WaterWorld, and DoorKey), showing consistently faster learning and higher return at convergence than PPO and a Reward Machine baseline, also under imperfect symbolic knowledge.

摘要：深度強化學習 (DRL) 演算法通常需要大量數據，並且在稀疏獎勵的領域中，面臨長期規劃和多個子目標的挑戰。在本文中，我們提出了一種近端策略優化 (PPO) 的神經符號擴展，該擴展將在較簡單實例中學習到的部分邏輯策略規範轉移到更具挑戰性的環境中以指導學習。我們介紹了兩種符號指導的整合方式：(i) H-PPO-Product，該方法在取樣時偏向行動分佈，以及 (ii) H-PPO-SymLoss，該方法用符號正則化項增強了PPO損失。我們在三個基準測試（OfficeWorld、WaterWorld 和 DoorKey）上評估我們的方法，顯示出比PPO和獎勵機器基準在收斂時具有一致的更快學習速度和更高的回報，即使在不完美的符號知識下也是如此。

##### **The Surprising Effectiveness of Canonical Knowledge Distillation for Semantic Segmentation**
2604.25530v1 by Muhammad Ali, Kevin Alexander Laube, Madan Ravi Ganesh, Lukas Schott, Niclas Popp, Thomas Brox

Recent knowledge distillation (KD) methods for semantic segmentation introduce increasingly complex hand-crafted objectives, yet are typically evaluated under fixed iteration schedules. These objectives substantially increase per-iteration cost, meaning equal iteration counts do not correspond to equal training budgets. It is therefore unclear whether reported gains reflect stronger distillation signals or simply greater compute. We show that iteration-based comparisons are misleading: when wall-clock compute is matched, \textit{canonical} logit- and feature-based KD outperform recent segmentation-specific methods. Under extended training, feature-based distillation achieves state-of-the-art ResNet-18 performance on Cityscapes and ADE20K. A PSPNet ResNet-18 student closely approaches its ResNet-101 teacher despite using only one quarter of the parameters, reaching 99\% of the teacher's mIoU on Cityscapes (79.0 vs.\ 79.8) and 92\% on ADE20K. Our results challenge the prevailing assumption that KD for segmentation requires task-specific mechanisms and suggest that scaling, rather than complex hand-crafted objectives, should guide future method design.

摘要：最近的知識蒸餾（KD）方法在語義分割中引入了越來越複雜的手工設計目標，但通常是在固定的迭代計劃下進行評估。這些目標大幅增加了每次迭代的成本，意味著相同的迭代次數並不對應於相等的訓練預算。因此，報告的增益是否反映了更強的蒸餾信號或只是更大的計算量尚不清楚。我們顯示基於迭代的比較具有誤導性：當牆鐘計算量匹配時，\textit{canonical} logit 和特徵基礎的 KD 超越了最近的特定分割方法。在擴展訓練下，基於特徵的蒸餾在 Cityscapes 和 ADE20K 上達到了最先進的 ResNet-18 性能。一個 PSPNet ResNet-18 學生在參數僅為四分之一的情況下，接近其 ResNet-101 教師，在 Cityscapes 上達到教師 mIoU 的 99\%（79.0 對 79.8）和 ADE20K 上的 92\%。我們的結果挑戰了當前的假設，即用於分割的 KD 需要特定任務的機制，並建議未來的方法設計應以擴展而非複雜的手工設計目標為指導。

##### **AI as Consumer and Participant: A Co-Design Agenda for MBSE Substrates and Methodology**
2604.25526v1 by Siyuan Ji

AI tools are being deployed over MBSE models today, and those models were not designed for this kind of consumption. The problem is not simply that tools hallucinate: well-prompted frontier models produce competent, useful output over a conformant SysML model, but the reasoning they produce is drawn from training rather than retrieved from the model itself, and different tools over the same model produce different results with nothing in the record to adjudicate between them. The model, in other words, is functioning as a prompt rather than as a knowledge base. Attaching better tools to the same model does not resolve this. The model and the methodology that governs its construction need to be designed together for AI participation, treating the model as a machine-queryable knowledge substrate rather than a structured artefact for human navigation, and that co-design has not yet happened in any systematic way. This paper works through a concrete workflow scenario to show what that gap looks like in practice, proposes three principles that jointly characterise what model and methodology must achieve together, and closes with a call to the community to begin this work before the architectural decisions about AI integration settle without the methodological foundation they require.

摘要：AI 工具今天正在被應用於 MBSE 模型，而這些模型並不是為這種使用方式設計的。問題不僅僅在於工具會產生幻覺：經過良好提示的前沿模型在符合 SysML 模型的基礎上產生了合格且有用的輸出，但它們所產生的推理是基於訓練而非從模型本身檢索的，並且不同的工具在相同模型上產生不同的結果，而記錄中沒有任何東西可以用來裁決它們之間的差異。換句話說，該模型的功能更像是一個提示，而不是一個知識基礎。將更好的工具附加到相同模型上並不能解決這個問題。模型及其構建所依據的方法論需要共同設計，以便 AI 參與，將模型視為可機器查詢的知識基質，而不是供人類導航的結構性工件，而這種共同設計尚未以任何系統化的方式發生。本文通過一個具體的工作流程場景來展示這一差距在實踐中的樣子，提出三個原則，這些原則共同描述模型和方法論必須共同實現的目標，並以呼籲社區在關於 AI 整合的架構決策確定之前開始這項工作，因為這些決策需要其所需的方法論基礎。

##### **PHISHREV: A Hybrid Machine Learning and Post-Hoc Non-monotonic Reasoning Framework for Context-Aware Phishing Website Classification**
2604.25512v1 by Mainak Sen, Kumar Sankar Ray, Amlan Chakrabarti

Phishing detection systems are predominantly rely on statistical machine learning models, which often lack contextual reasoning and are vulnerable to adversarial manipulation. In this work, we propose a hybrid framework that integrates machine learning classifiers with non-monotonic reasoning using Answer Set Programming (ASP) to enable context-aware decision refinement. The proposed post-hoc reasoning layer incorporates expert knowledge to revise classifier predictions through formal belief revisions. Experimental results indicate that the reasoning module modifies 5.08\% of classifier outputs, leading to improved decision consistency. A key advantage is that new domain knowledge can be incorporated into the reasoning layer in $\mathcal{O}(n)$ time, eliminating the need for model retraining.

摘要：釣魚偵測系統主要依賴統計機器學習模型，這些模型通常缺乏情境推理，並且容易受到對抗性操控。在本研究中，我們提出了一個混合框架，將機器學習分類器與非單調推理結合，使用答案集程式設計（ASP）來實現情境感知的決策精煉。所提出的事後推理層結合專家知識，通過正式的信念修訂來修正分類器的預測。實驗結果表明，推理模組修改了5.08\%的分類器輸出，從而提高了決策的一致性。一個主要優勢是，新的領域知識可以在$\mathcal{O}(n)$時間內納入推理層，消除了模型重新訓練的需要。

##### **Do LLMs Capture Embodied Cognition and Cultural Variation? Cross-Linguistic Evidence from Demonstratives**
2604.25423v1 by Yu Wang, Emmanuele Chersoni, Chu-Ren Huang

Do large language models (LLMs) truly acquire embodied cognition and cultural conventions from text? We introduce demonstratives, fundamental spatial expressions like "this/that" in English and "zhè/nà" in Chinese, as a novel probe for grounded knowledge. Using 6,400 responses from 320 native speakers, we establish a human baseline: English speakers reliably distinguish proximal-distal referents but struggle with perspective-taking, while Chinese speakers switch perspectives fluently but tolerate distal ambiguity. In contrast, five state-of-the-art LLMs fail to inherently understand the proximal-distal contrast and show no cultural differences, defaulting to English-centric reasoning. Our study contributes (i) a new task, based on demonstratives, as a new lens for evaluating embodied cognition and cultural conventions; (ii) empirical evidence of cross-cultural asymmetries in human interpretation; (iii) a new perspective on the egocentric-sociocentric debate, showing both orientations coexist but vary across languages; and (iv) a call to address individual variation in future model design.

摘要：大型語言模型（LLMs）是否真的從文本中獲得了具身認知和文化慣例？我們引入指示詞，像英語中的 "this/that" 和中文中的 "zhè/nà" 這樣的基本空間表達，作為檢驗具體知識的新工具。通過分析320名母語者的6,400個回應，我們建立了人類基準：英語使用者可靠地區分近遠指稱，但在視角轉換上存在困難，而中文使用者則能流利地切換視角，但對遠指稱的模糊性則能容忍。相比之下，五個最先進的LLMs無法內在理解近遠對比，且未顯示出文化差異，默認使用以英語為中心的推理。我們的研究貢獻了（i）一項基於指示詞的新任務，作為評估具身認知和文化慣例的新視角；（ii）人類解釋中的跨文化不對稱的實證證據；（iii）對自我中心與社會中心辯論的新看法，顯示這兩種取向共存但在不同語言中有所變化；以及（iv）呼籲在未來的模型設計中考慮個體差異。

##### **Wiki Dumps to Training Corpora: South Slavic Case**
2604.25384v1 by Mihailo Škorić

This paper presents a methodology for transforming raw Wikimedia dumps into quality textual corpora for seven South Slavic languages. The work is divided into two major phases. The first involves extracting and cleaning text from raw dumps of Wikipedia, Wikisource, Wikibooks, Wikinews, and Wikiquote, where available. This step requires careful handling of raw wiki markup to isolate, first of all, textual articles, and then usable natural language text within them. The second phase addresses the challenge of suspicious or low-quality articles, which are often generated from databases or structured knowledge bases. These articles are characterised by repetitive patterns, generic phrasing, and minimal to no original content. To mitigate their impact, a n-gram-based filtering strategy was employed to detect high levels of textual redundancy between articles and then remove such articles from the corpora entirely. The resulting datasets aim to provide linguistically rich texts suitable for training language models or conducting comparative research across South Slavic languages. By combining systematic extraction with quality control, this work contributes to the creation of reliable, high-information corpora that reflect authentic language use and cultural context. While focused on the South Slavic case in the paper, the approach is mostly language-agnostic and can be generalised to other languages and language families.

摘要：這篇論文提出了一種將原始維基媒體數據轉換為七種南斯拉夫語言的高質量文本語料庫的方法。這項工作分為兩個主要階段。第一階段涉及從維基百科、維基文庫、維基教科書、維基新聞和維基語錄的原始數據中提取和清理文本（如可用）。這一步需要仔細處理原始維基標記，以首先隔離文本文章，然後提取其中可用的自然語言文本。第二階段解決了可疑或低質量文章的挑戰，這些文章通常來自數據庫或結構化知識庫。這些文章的特徵是重複的模式、通用的措辭，以及最小或沒有原創內容。為了減輕它們的影響，採用了基於n-gram的過濾策略來檢測文章之間的高文本冗餘，然後將這些文章完全從語料庫中刪除。最終生成的數據集旨在提供語言學上豐富的文本，適合用於訓練語言模型或進行南斯拉夫語言之間的比較研究。通過將系統提取與質量控制相結合，這項工作有助於創建可靠的、高信息量的語料庫，反映真實的語言使用和文化背景。雖然本文專注於南斯拉夫語的案例，但該方法大多是語言無關的，可以推廣到其他語言和語言家族。

##### **CoRE: Concept-Reasoning Expansion for Continual Brain Lesion Segmentation**
2604.25376v1 by Qianqian Chen, Anglin Liu, Jingyang Zhang, Yudong Zhang

Accurate brain lesion segmentation in MRI is vital for effective clinical diagnosis and treatment planning. Due to high annotation costs and strict data privacy regulations, universal models require employing Continual Learning (CL) to adapt to evolving clinical tasks without losing previously acquired knowledge. However, existing CL paradigms often suffer from capacity limits or redundant parameter growth, and even advanced dynamic methods rely mostly on image-perception strategies that struggle to handle the substantial pathological and multimodal heterogeneity inherent in brain imaging. To address this issue, we propose Concept-Reasoning Expansion (CoRE) framework, which establishes a joint decision-making mechanism by integrating visual features with structured concepts. Through the alignment of image tokens with a hierarchical concept library, CoRE simulates clinical reasoning to guide both interpretable expert routing and demand-based model growth. This collaborative process ensures model evolution is grounded in clinical priors, preventing redundant parameter expansion while maximizing knowledge reuse. Extensive evaluations across 12 sequential brain lesion MRI tasks demonstrate that CoRE achieves state-of-the-art performance and provides a high knowledge starting point for efficient future adaptation. Its superior few-shot transferability and clinical interpretability further validate its effectiveness in managing non-stationary clinical data streams. Our code will be released soon.

摘要：準確的腦部病變分割在MRI中對於有效的臨床診斷和治療計劃至關重要。由於高昂的標註成本和嚴格的數據隱私法規，通用模型需要採用持續學習（CL）來適應不斷演變的臨床任務，而不會失去先前獲得的知識。然而，現有的CL範式往往受到容量限制或冗餘參數增長的困擾，即使是先進的動態方法也主要依賴於圖像感知策略，這些策略難以處理腦部影像中固有的顯著病理和多模態異質性。為了解決這個問題，我們提出了概念推理擴展（CoRE）框架，通過將視覺特徵與結構化概念整合，建立了一個聯合決策機制。通過將圖像標記與層次概念庫對齊，CoRE 模擬臨床推理，以指導可解釋的專家路由和基於需求的模型增長。這一協作過程確保模型的演變根植於臨床先驗，防止冗餘參數擴展，同時最大化知識重用。在12個連續腦部病變MRI任務中的廣泛評估顯示，CoRE實現了最先進的性能，並為未來高效適應提供了良好的知識起點。其卓越的少樣本可轉移性和臨床可解釋性進一步驗證了其在管理非穩態臨床數據流中的有效性。我們的代碼將很快發布。

##### **Multi-action Tangled Program Graphs for Multi-task Reinforcement Learning with Continuous Control**
2604.25369v1 by Quentin Vacher, Nicolas Beuve, Mickaël Dardaillon, Karol Desnos

Over the past few decades, machine learning has been widely used to learn complex tasks. Reinforcement Learning (RL), inspired by human behavior, is a great example, as it involves developing specific behaviours for specific tasks. To further challenge algorithms, Multi-Task RL (MTRL) environments have been introduced, requiring a single model to learn multiple behaviors. The Tangled Program Graph (TPG) algorithm is a Genetic Programming (GP) algorithm designed for discrete MTRL environments. Recently, the MAPLE algorithm has been proposed, as another GP algorithm that achieves high results in single task continuous RL environments. A variation of the TPG is proposed alongside MAPLE, named Multi-Action TPG (MATPG) that aggregates MAPLE agents, and creates a control flow to activate them. Initially tested on single task RL environments only, MATPG achieved similar results to MAPLE. In this work, we present a new benchmark based on the MuJoCo Half Cheetah from Gymnasium. This benchmark features five distinct obstacles that are randomly positioned in front of the agent, each of which demands a unique behavior. This benchmark serves as a use case for MATPG, to prove its ability as a GP solution for continuous MTRL environments. Our experiments demonstrate its superiority in this multi-task use case when combined with lexicase selection. Furthermore, we examine the interpretability of the evolved graph, revealing that the decision flow of the model is fully interpretable.

摘要：在過去幾十年中，機器學習已被廣泛應用於學習複雜任務。強化學習（RL）受到人類行為的啟發，是一個很好的例子，因為它涉及為特定任務開發特定行為。為了進一步挑戰算法，已引入多任務強化學習（MTRL）環境，要求單一模型學習多種行為。纏結程序圖（TPG）算法是一種針對離散MTRL環境設計的遺傳編程（GP）算法。最近，提出了MAPLE算法，作為另一種在單任務連續RL環境中取得高結果的GP算法。與MAPLE一起提出了一種TPG的變體，名為多行動TPG（MATPG），它聚合MAPLE代理並創建一個控制流程來激活它們。最初僅在單任務RL環境中進行測試，MATPG達到了與MAPLE相似的結果。在這項工作中，我們提出了一個基於Gymnasium的MuJoCo Half Cheetah的新基準。這個基準特徵有五個獨特的障礙物，隨機放置在代理面前，每個障礙物都要求一種獨特的行為。這個基準作為MATPG的使用案例，以證明其作為連續MTRL環境的GP解決方案的能力。我們的實驗顯示，當與字典選擇結合時，它在這個多任務使用案例中表現優越。此外，我們檢查了演化圖的可解釋性，揭示了模型的決策流程是完全可解釋的。

##### **Faithfulness-QA: A Counterfactual Entity Substitution Dataset for Training Context-Faithful RAG Models**
2604.25313v1 by Li Ju, Junzhe Wang, Qi Zhang

Retrieval-Augmented Generation (RAG) models frequently produce answers grounded in parametric memory rather than the retrieved context, undermining the core promise of retrieval augmentation. A fundamental obstacle to fixing this unfaithfulness is the lack of training data that explicitly requires models to prefer context over internal knowledge. We introduce Faithfulness-QA, a large-scale dataset of 99,094 samples constructed through counterfactual entity substitution. Starting from two established extractive QA benchmarks--SQuAD and TriviaQA--we automatically identify answer-bearing named entities in each context, replace them with type-consistent alternatives drawn from a curated bank of 76,953 entities, and thereby manufacture controlled knowledge conflicts between context and parametric memory. Rigorous quality filtering ensures 100% pass rates across four automated checks on random 200-sample audits. We release the full dataset, the construction pipeline, and a typed entity bank covering eight named entity categories. Faithfulness-QA is designed as a training resource for attention-based faithfulness objectives and as an evaluation benchmark for measuring context-grounding behavior in RAG systems. Data and code are available at https://github.com/qzhangFDU/faithfulness-qa-dataset.

摘要：檢索增強生成（RAG）模型經常產生基於參數記憶的答案，而非檢索到的上下文，這削弱了檢索增強的核心承諾。修正這種不忠實性的根本障礙是缺乏明確要求模型偏好上下文而非內部知識的訓練數據。我們引入了Faithfulness-QA，這是一個由99,094個樣本組成的大型數據集，通過反事實實體替換構建而成。從兩個已建立的抽取式問答基準——SQuAD和TriviaQA開始，我們自動識別每個上下文中的答案承載命名實體，並用從76,953個實體的策劃庫中提取的類型一致替代品替換它們，從而製造上下文和參數記憶之間的控制知識衝突。嚴格的質量過濾確保在隨機200樣本審核中四項自動檢查的通過率達到100%。我們發布了完整數據集、構建管道以及涵蓋八個命名實體類別的類型實體庫。Faithfulness-QA旨在作為基於注意力的忠實性目標的訓練資源，以及作為評估RAG系統中上下文基礎行為的評估基準。數據和代碼可在https://github.com/qzhangFDU/faithfulness-qa-dataset獲得。

##### **Learning from Medical Entity Trees: An Entity-Centric Medical Data Engineering Framework for MLLMs**
2604.25296v1 by Jianghang Lin, Haihua Yang, Deli Yu, Kai Wu, Kai Ye, Jinghao Lin, Zihan Wang, Yuhang Wu, Liujuan Cao

Multimodal Large Language Models (MLLMs) have shown transformative potential in medical applications, yet their performance is hindered by conventional data curation strategies that rely on coarse-grained partitioning by modality or department. Such fragmented approaches fail to capture the hierarchical and interconnected nature of clinical medical knowledge, limiting the models' ability to perform fine-grained recognition and complex reasoning. In this paper, we propose a novel Entity-Centric Medical Data Engineering framework. We automatically extract entities from authoritative medical literature to construct a Medical Entity Tree (MET), a hierarchical structure that systematically encodes diseases, anatomical structures, modalities, and symptoms into a unified knowledge repository. Building upon the MET, we propose an advanced data engine that includes: (1) node-guided retrieval to anchor raw data to specific medical concepts, (2) a two-stage hybrid filtering and alignment pipeline to ensure precise visual-semantic correspondence, and (3) knowledge-aware data synthesis to generate enriched captions and targeted reasoning VQA pairs, leveraging structural constraints. Extensive evaluations across six medical benchmarks demonstrate that our approach significantly enhances the medical capabilities of general-purpose MLLMs, improving their ability to handle complex clinical queries and achieve state-of-the-art performance in diverse medical contexts.

摘要：多模態大型語言模型（MLLMs）在醫療應用中顯示出變革潛力，然而它們的表現受到傳統數據策展策略的限制，這些策略依賴於按模態或部門進行粗粒度劃分。這種碎片化的方法未能捕捉臨床醫學知識的層次性和相互聯繫的特性，限制了模型進行精細識別和複雜推理的能力。在本文中，我們提出了一個新穎的以實體為中心的醫療數據工程框架。我們自動從權威醫學文獻中提取實體，以構建醫療實體樹（MET），這是一種系統性編碼疾病、解剖結構、模態和症狀的層次結構，將其整合到統一的知識庫中。在MET的基礎上，我們提出了一個先進的數據引擎，包括：（1）節點引導檢索，將原始數據與特定醫學概念對接；（2）兩階段混合過濾和對齊管道，以確保精確的視覺-語義對應；以及（3）知識感知數據合成，以生成豐富的標題和針對性推理的VQA對，利用結構約束。對六個醫療基準的廣泛評估表明，我們的方法顯著增強了通用MLLM的醫療能力，提高了它們處理複雜臨床查詢的能力，並在多樣的醫療背景中實現了最先進的性能。

##### **AutoResearchBench: Benchmarking AI Agents on Complex Scientific Literature Discovery**
2604.25256v1 by Lei Xiong, Kun Luo, Ziyi Xia, Wenbo Zhang, Jin-Ge Yao, Zheng Liu, Jingying Shao, Jianlyu Chen, Hongjin Qian, Xi Yang, Qian Yu, Hao Li, Chen Yue, Xiaan Du, Yuyang Wang, Yesheng Liu, Haiyu Xu, Zhicheng Dou

Autonomous scientific research is significantly advanced thanks to the development of AI agents. One key step in this process is finding the right scientific literature, whether to explore existing knowledge for a research problem, or to acquire evidence for verifying assumptions and supporting claims. To assess AI agents' capability in driving this process, we present AutoResearchBench, a dedicated benchmark for autonomous scientific literature discovery. AutoResearchBench consists of two complementary task types: (1) Deep Research, which requires tracking down a specific target paper through a progressive, multi-step probing process, and (2) Wide Research, which requires comprehensively collecting a set of papers satisfying given conditions. Compared to previous benchmarks on agentic web browsing, AutoResearchBench is distinguished along three dimensions: it is research-oriented, calling for in-depth comprehension of scientific concepts; literature-focused, demanding fine-grained utilization of detailed information; and open-ended, involving an unknown number of qualified papers and thus requiring deliberate reasoning and search throughout. These properties make AutoResearchBench uniquely suited for evaluating autonomous research capabilities, and extraordinarily challenging. Even the most powerful LLMs, despite having largely conquered general agentic web-browsing benchmarks such as BrowseComp, achieve only 9.39% accuracy on Deep Research and 9.31% IoU on Wide Research, while many other strong baselines fall below 5%. We publicly release the dataset and evaluation pipeline to facilitate future research in this direction. We publicly release the dataset, evaluation pipeline, and code at https://github.com/CherYou/AutoResearchBench.

摘要：自主科學研究因AI代理的發展而顯著進步。這一過程中的一個關鍵步驟是尋找合適的科學文獻，無論是為了探索研究問題的現有知識，還是為了獲取證據以驗證假設和支持主張。為了評估AI代理在推動這一過程中的能力，我們提出了AutoResearchBench，一個專門用於自主科學文獻發現的基準。AutoResearchBench由兩種類型的互補任務組成：（1）深度研究，這需要通過逐步的多步探測過程來追蹤特定目標論文，以及（2）廣泛研究，這需要全面收集滿足給定條件的一組論文。與之前的代理網頁瀏覽基準相比，AutoResearchBench在三個維度上具有區別：它是以研究為導向的，要求對科學概念進行深入理解；以文獻為重點，要求對詳細信息進行細緻的利用；並且是開放式的，涉及未知數量的合格論文，因此需要在整個過程中進行深思熟慮的推理和搜索。這些特性使AutoResearchBench特別適合評估自主研究能力，並且極具挑戰性。即使是最強大的LLM，儘管在一般代理網頁瀏覽基準如BrowseComp上已經取得了很大進展，但在深度研究中僅達到9.39%的準確率，在廣泛研究中僅達到9.31%的IoU，而許多其他強基準則低於5%。我們公開發布數據集和評估管道，以促進未來在這一方向上的研究。我們在https://github.com/CherYou/AutoResearchBench公開發布數據集、評估管道和代碼。

##### **Making AI-Assisted Grant Evaluation Auditable without Exposing the Model**
2604.25200v1 by Kemal Bicakci

Public agencies are beginning to consider large language models (LLMs) as decision-support tools for grant evaluation. This creates a practical governance problem: the model and scoring rubric should not be exposed in a way that allows applicants to optimize against them, yet the evaluation process must remain auditable, contestable, and accountable.
  We propose a TEE-based architecture that helps reconcile these requirements through remote attestation. The architecture allows an external verifier to check which model, rubric, prompt template, and input representation were used, without exposing model weights, proprietary scoring logic, or intermediate reasoning to applicants or infrastructure operators. The main artifact is an attested evaluation bundle: a signed, timestamped record linking the original submission hash, the canonical input hash, the model-and-rubric measurement, and the evaluation output.
  The paper also considers a scenario-specific prompt injection risk: applicant-controlled documents may contain hidden or indirect instructions intended to influence the LLM evaluator. We therefore include a canonicalization and sanitization layer that normalizes document representations and records suspicious transformations before inference. We position the design relative to confidential AI inference, attestable AI audits, zero-knowledge machine learning, algorithmic accountability, and AI-assisted peer review. The resulting claim is deliberately narrow: remote attestation does not prove that an evaluation is fair or scientifically correct, but it can make part of the evaluation process externally verifiable.

摘要：公共機構開始考慮將大型語言模型（LLMs）作為補助評估的決策支持工具。這產生了一個實際的治理問題：模型和評分標準不應以允許申請者對其進行優化的方式暴露出來，但評估過程必須保持可審計、可爭議和負責任。<lb/>
我們提出了一種基於TEE的架構，通過遠程證明來幫助調和這些要求。該架構允許外部驗證者檢查使用了哪個模型、標準、提示模板和輸入表示，而不向申請者或基礎設施運營商暴露模型權重、專有評分邏輯或中間推理。主要的工件是一個經過證明的評估包：一個簽名的、帶有時間戳的記錄，鏈接原始提交哈希、標準輸入哈希、模型和標準測量以及評估輸出。<lb/>
本文還考慮了一個特定場景的提示注入風險：申請者控制的文件可能包含隱藏或間接的指示，旨在影響LLM評估者。因此，我們包括了一個標準化和清理層，對文檔表示進行正規化，並在推理之前記錄可疑的轉換。我們將設計定位於機密AI推理、可證明的AI審計、零知識機器學習、算法問責和AI輔助同行評審。最終的主張故意狹窄：遠程證明並不證明評估是公平或科學正確的，但它可以使評估過程的一部分在外部可驗證。

##### **CroSearch-R1: Better Leveraging Cross-lingual Knowledge for Retrieval-Augmented Generation**
2604.25182v1 by Rui Qi, Fengran Mo, Sijin Lu, Yufeng Chen, Jian-Yun Nie, Kaiyu Huang

A multilingual collection may contain useful knowledge in other languages to supplement and correct the facts in the original language for Retrieval-Augmented Generation (RAG). However, the vanilla approach that simply concatenates multiple pieces of knowledge from different languages into the context may fail to improve effectiveness due to the potential disparities across languages. To better leverage multilingual knowledge, we propose CroSearch-R1, a search-augmented reinforcement learning framework to integrate multilingual knowledge into the Group Relative Policy Optimization (GRPO) process. In particular, the approach adopts a multi-turn retrieval strategy with cross-lingual knowledge integration to dynamically align the knowledge from other languages as supplementary evidence into a unified representation space. Furthermore, we introduce a multilingual rollout mechanism to optimize reasoning transferability across languages. Experimental results demonstrate that our framework effectively leverages cross-lingual complementarity and improves the effectiveness of RAG with multilingual collections.

摘要：多語言的集合可能包含其他語言中的有用知識，以補充和修正原語言中的事實，適用於檢索增強生成（RAG）。然而，僅僅將來自不同語言的多個知識片段簡單串聯到上下文中的傳統方法，可能無法提高效果，因為不同語言之間可能存在潛在差異。為了更好地利用多語言知識，我們提出了CroSearch-R1，一個增強搜索的強化學習框架，將多語言知識整合到群體相對政策優化（GRPO）過程中。具體而言，該方法採用多回合檢索策略，並進行跨語言知識整合，以動態對齊來自其他語言的知識，作為補充證據進入統一的表示空間。此外，我們引入了一種多語言的展開機制，以優化跨語言的推理可轉移性。實驗結果表明，我們的框架有效利用了跨語言的互補性，並提高了多語言集合下RAG的有效性。

##### **M$^3$-VQA: A Benchmark for Multimodal, Multi-Entity, Multi-Hop Visual Question Answering**
2604.25122v1 by Jiatong Ma, Longteng Guo, Yuchen Liu, Zijia Zhao, Dongze Hao, Xuanxu Lin, Jing Liu

We present M$^3$-VQA, a novel knowledge-based Visual Question Answering (VQA) benchmark, to enhance the evaluation of multimodal large language models (MLLMs) in fine-grained multimodal entity understanding and complex multi-hop reasoning. Unlike existing VQA datasets that focus on coarse-grained categories and simple reasoning over single entities, M$^3$-VQA introduces diverse multi-entity questions involving multiple distinct entities from both visual and textual sources. It requires models to perform both sequential and parallel multi-hop reasoning across multiple documents, supported by traceable, detailed evidence and a curated multimodal knowledge base. We evaluate 16 leading MLLMs under three settings: without external knowledge, with gold evidence, and with retrieval-augmented input. The poor results reveal significant challenges for MLLMs in knowledge acquisition and reasoning. Models perform poorly without external information but improve markedly when provided with precise evidence. Furthermore, reasoning-aware agentic retrieval surpasses heuristic methods, highlighting the importance of structured reasoning for complex multimodal understanding. M$^3$-VQA presents a more challenging evaluation for advancing the multimodal reasoning capabilities of MLLMs. Our code and dataset are available at https://github.com/CASIA-IVA-Lab/M3VQA.

摘要：我們提出 M$^3$-VQA，一個新穎的基於知識的視覺問題回答（VQA）基準，以增強多模態大型語言模型（MLLMs）在細緻的多模態實體理解和複雜的多跳推理方面的評估。與現有的 VQA 數據集專注於粗略類別和單一實體的簡單推理不同，M$^3$-VQA 引入了涉及來自視覺和文本來源的多個不同實體的多樣化多實體問題。它要求模型在多個文檔中執行順序和並行的多跳推理，並得到可追溯的詳細證據和精心策劃的多模態知識庫的支持。我們在三種設置下評估了 16 種領先的 MLLMs：不使用外部知識、使用黃金證據和使用檢索增強的輸入。糟糕的結果揭示了 MLLMs 在知識獲取和推理方面面臨的重大挑戰。模型在沒有外部信息的情況下表現不佳，但在提供精確證據時明顯改善。此外，具備推理意識的代理檢索超越了啟發式方法，突顯了結構化推理對於複雜多模態理解的重要性。M$^3$-VQA 為推進 MLLMs 的多模態推理能力提供了更具挑戰性的評估。我們的代碼和數據集可在 https://github.com/CASIA-IVA-Lab/M3VQA 獲得。

##### **Knowledge Distillation Must Account for What It Loses**
2604.25110v1 by Wenshuo Wang

This position paper argues that knowledge distillation must account for what it loses: student models should be judged not only by retained task scores, but by whether they preserve the teacher capabilities that make those scores reliable. This matters because distillation is increasingly used to turn large, often frontier models into deployable systems, yet headline metrics can hide losses in uncertainty, boundary behavior, process reliability, on-policy stability, grounding, privacy, safety, and diversity. We identify the retention assumption behind current evaluation and reframe distillation as a lossy projection of teacher behavior rather than a faithful copy. We then synthesize existing evidence into a taxonomy of off-metric distillation losses, showing that these losses are concrete, recurring, and measurable. To make the position actionable, we propose scenario-specific preservation targets and a Distillation Loss Statement that reports what was preserved, what was lost, and why the remaining losses are acceptable. The goal is not lossless distillation, but accountable distillation.

摘要：這份立場文件主張，知識蒸餾必須考量其所失去的東西：學生模型不僅應根據保留的任務分數來評判，還應評估它們是否保留了使這些分數可靠的教師能力。這一點很重要，因為蒸餾越來越多地被用來將大型、通常是前沿的模型轉化為可部署的系統，但標題指標可能掩蓋了不確定性、邊界行為、過程可靠性、政策穩定性、基礎、隱私、安全性和多樣性等方面的損失。我們確定了當前評估背後的保留假設，並將蒸餾重新框架為教師行為的有損投影，而不是忠實的複製。然後，我們將現有證據綜合成一個非指標蒸餾損失的分類法，顯示這些損失是具體的、重複的且可衡量的。為了使這一立場具有可行性，我們提出了特定場景的保留目標和一份蒸餾損失聲明，報告了保留了什麼、失去了什麼，以及為什麼剩餘的損失是可以接受的。目標不是無損蒸餾，而是可負責的蒸餾。

##### **PolyKV: A Shared Asymmetrically-Compressed KV Cache Pool for Multi-Agent LLM Inference**
2604.24971v1 by Ishan Patel, Ishan Joshi

We present PolyKV, a system in which multiple concurrent inference agents share a single, asymmetrically compressed KV cache pool. Rather than allocating a separate KV cache per agent -- the standard paradigm -- PolyKV writes a compressed cache once and injects it into N independent agent contexts via HuggingFace DynamicCache objects. Compression is asymmetric: Keys are quantized at int8 (q8_0) to preserve softmax stability, while Values are compressed using TurboQuant MSE -- a Fast Walsh-Hadamard Transform (FWHT) rotation followed by 3-bit Lloyd-Max quantization with centroids tuned to N(0,1). We evaluate across two model scales (SmolLM2-1.7B-Instruct and Llama-3-8B-Instruct), three context lengths (600-7,194 tokens), and up to 15 concurrent agents. PolyKV achieves a stable 2.91x compression ratio across all configurations. On Llama-3-8B with 15 agents sharing a 4K-token context, PolyKV reduces KV cache memory from 19.8 GB to 0.45 GB -- a 97.7% reduction -- while maintaining only +0.57% perplexity degradation and a mean BERTScore F1 of 0.928. PPL delta does not grow with agent count and improves as context length increases, inverting to -0.26% at 1,851 coherent tokens. To our knowledge, no prior work combines a single shared, lossy-compressed KV pool with multi-reader concurrent agent access.

摘要：我們提出了PolyKV，這是一個多個並發推理代理共享單一不對稱壓縮KV快取池的系統。與為每個代理分配單獨的KV快取——標準範式——不同，PolyKV僅寫入一次壓縮快取，並通過HuggingFace DynamicCache對象將其注入N個獨立的代理上下文中。壓縮是非對稱的：鍵以int8（q8_0）量化，以保持softmax的穩定性，而值則使用TurboQuant MSE進行壓縮——這是一種快速Walsh-Hadamard變換（FWHT）旋轉，然後進行3位Lloyd-Max量化，質心調整為N(0,1)。我們在兩個模型規模（SmolLM2-1.7B-Instruct和Llama-3-8B-Instruct）、三個上下文長度（600-7,194個標記）和最多15個並發代理中進行評估。PolyKV在所有配置中實現了穩定的2.91倍壓縮比。在Llama-3-8B上，15個代理共享4K標記的上下文，PolyKV將KV快取內存從19.8 GB減少到0.45 GB——減少了97.7%——同時僅維持+0.57%的困惑度降級和平均BERTScore F1為0.928。PPL增量不隨代理數量增長，並隨著上下文長度的增加而改善，在1,851個連貫標記時反轉為-0.26%。據我們所知，之前的工作中沒有任何一項將單一共享的有損壓縮KV池與多讀者並發代理訪問相結合。

##### **S-SONDO: Self-Supervised Knowledge Distillation for General Audio Foundation Models**
2604.24933v1 by Mohammed Ali El Adlouni, Aurian Quelennec, Pierre Chouteau, Geoffroy Peeters, Slim Essid

General audio foundation models have recently achieved remarkable progress, enabling strong performance across diverse tasks. However, state-of-the-art models remain extremely large, often with hundreds of millions of parameters, leading to high inference costs and limited deployability on edge devices. Knowledge distillation is a proven strategy for model compression, but prior work in audio has mostly focused on supervised settings, relying on class logits, intermediate features, or architecture-specific techniques. Such assumptions exclude models that output only embeddings, such as self-supervised or metric-learning models. We introduce S-SONDO (Self-Supervised KnOwledge DistillatioN for General AuDio FOundation Models), the first framework to distill general audio models using only their output embeddings. By avoiding the need for logits or layer-level alignment, S-SONDO is architecture-agnostic and broadly applicable to embedding-based teachers. We demonstrate its effectiveness by distilling two audio foundation models into three efficient students that are up to 61 times smaller while retaining up to 96% of teacher performance. We also provide practical insights on loss choice and clustering-based balanced data sampling. Code is available here: https://github.com/MedAliAdlouni/ssondo.

摘要：一般音頻基礎模型最近取得了顯著的進展，使其在各種任務中表現出色。<lb/>然而，最先進的模型仍然非常龐大，通常擁有數億個參數，導致高推理成本和在邊緣設備上的有限可部署性。<lb/>知識蒸餾是一種經過驗證的模型壓縮策略，但先前在音頻領域的研究主要集中在監督設定，依賴於類別邏輯、中間特徵或特定架構的技術。<lb/>這些假設排除了僅輸出嵌入的模型，例如自監督或度量學習模型。<lb/>我們介紹了 S-SONDO（自監督知識蒸餾通用音頻基礎模型），這是第一個僅使用其輸出嵌入來蒸餾通用音頻模型的框架。<lb/>通過避免對邏輯或層級對齊的需求，S-SONDO 是架構無關的，並且可以廣泛應用於基於嵌入的教師。<lb/>我們通過將兩個音頻基礎模型蒸餾成三個高效的學生來展示其有效性，這些學生的大小最多小於 61 倍，同時保留高達 96% 的教師性能。<lb/>我們還提供了有關損失選擇和基於聚類的平衡數據抽樣的實用見解。<lb/>代碼可在此處獲得：https://github.com/MedAliAdlouni/ssondo。

##### **asRoBallet: Closing the Sim2Real Gap via Friction-Aware Reinforcement Learning for Underactuated Spherical Dynamics**
2604.24916v1 by Fang Wan, Guangyi Huang, Tianyu Wu, Zishang Zhang, Bangchao Huang, Haoran Sun, Mingdong Chen, Chaoyang Song

We introduce asRoBallet, to the best of our knowledge, the first successful deployment of reinforcement learning (RL) on a humanoid ballbot hardware. Historically, ballbots have served as a canonical benchmark for underactuated and nonholonomic control, which are characterized by a reality gap in complex friction models for wheel-sphere-ground interactions. While current literature demonstrates successful handling of 3D balancing with LQR and MPC, transitioning to actual hardware for a humanoid ballbot using RL is currently hindered by critical gaps in contact modeling, actuator latency & jitter, and safe hardware exploration, and safe hardware exploration. This study proposes a high-fidelity MuJoCo simulation that explicitly models the discrete roller mechanics of ETH-type omni-wheels, thereby capturing parasitic vibrations and contact discontinuities that are previously ignored. We also developed a Friction-Aware Reinforcement Learning framework that achieves zero-shot Sim2Real transfer by mastering the coupled rolling, lateral, and torsional friction channels at the wheel-sphere and sphere-ground interfaces. We designed asRoBallet through subtractive reconfiguration, repurposing key components from an overconstrained quadruped and integrating them into a newly designed structural frame to achieve a robust research platform at low cost. We also developed a generalized iOS ecosystem that transforms consumer electronics into a low-latency interface, enabling a single operator to orchestrate expressive humanoid maneuvers via intuitive natural motion.

摘要：我們介紹asRoBallet，據我們所知，這是首次成功在類人球形機器人硬體上部署強化學習（RL）。歷史上，球形機器人一直作為欠驅動和非完整控制的經典基準，這些控制特徵在於複雜摩擦模型中存在現實差距，特別是在輪子-球體-地面互動方面。雖然當前文獻展示了使用LQR和MPC成功處理3D平衡，但轉向使用RL的類人球形機器人實際硬體目前受到接觸建模、驅動器延遲和抖動以及安全硬體探索等關鍵問題的阻礙。本研究提出了一個高保真度的MuJoCo模擬，明確建模ETH型全向輪的離散滾輪力學，從而捕捉先前被忽略的寄生振動和接觸不連續性。我們還開發了一個摩擦感知強化學習框架，通過掌握輪子-球體和球體-地面界面上的耦合滾動、側向和扭轉摩擦通道，實現零樣本的Sim2Real轉移。我們通過減法重構設計了asRoBallet，重新利用來自過度約束的四足機器人的關鍵組件，並將其整合到新設計的結構框架中，以低成本實現穩健的研究平台。我們還開發了一個通用的iOS生態系統，將消費電子產品轉變為低延遲接口，使單一操作員能夠通過直觀的自然動作協調表達性的類人動作。

##### **Learning with Embedded Linear Equality Constraints via Variational Bayesian Inference**
2604.24911v1 by Matthew Marsh, Benoît Chachuat, Antonio del Rio Chanona

Machine Learning is becoming more prevalent in science and engineering, but many approaches do not provide meaningful uncertainty estimates and predictions may also violate known physical knowledge. We propose a Bayesian framework to embed linear relationships across inputs and outputs into the learning process, whilst characterizing full predictive uncertainty over both the model parameters and the domain knowledge. We evaluated our method on learning the single particle battery model subject to voltage and energy balances, showing its ability to provide reduced credible intervals and constraint violations compared to standard Bayesian neural networks based on variational inference.

摘要：機器學習在科學和工程中變得越來越普遍，但許多方法並未提供有意義的不確定性估計，且預測可能違反已知的物理知識。我們提出了一個貝葉斯框架，將輸入和輸出之間的線性關係嵌入學習過程中，同時對模型參數和領域知識的全面預測不確定性進行表徵。我們對學習單粒子電池模型進行了評估，該模型受電壓和能量平衡的影響，顯示出與基於變分推斷的標準貝葉斯神經網絡相比，能夠提供縮小的可信區間和約束違反。

##### **MotionBricks: Scalable Real-Time Motions with Modular Latent Generative Model and Smart Primitives**
2604.24833v1 by Tingwu Wang, Olivier Dionne, Michael De Ruyter, David Minor, Davis Rempe, Kaifeng Zhao, Mathis Petrovich, Ye Yuan, Chenran Li, Zhengyi Luo, Brian Robison, Xavier Blackwell, Bernardo Antoniazzi, Xue Bin Peng, Yuke Zhu, Simon Yuen

Despite transformative advances in generative motion synthesis, real-time interactive motion control remains dominated by traditional techniques. In this work, we identify two key challenges in bridging research and production: 1) Real-time scalability: Industry applications demand real-time generation of a vast repertoire of motion skills, while generative methods exhibit significant degradation in quality and scalability under real-time computation constraints, and 2) Integration: Industry applications demand fine-grained multi-modal control involving velocity commands, style selection, and precise keyframes, a need largely unmet by existing text- or tag-driven models. To overcome these limitations, we introduce MotionBricks: a large-scale, real-time generative framework with a two-fold solution. First, we propose a large-scale modular latent generative backbone tailored for robust real-time motion generation, effectively modeling a dataset of over 350,000 motion clips with a single model. Second, we introduce smart primitives that provide a unified, robust, and intuitive interface for authoring both navigation and object interaction. Applications can be designed in a plug-and-play manner like assembling bricks without expert animation knowledge. Quantitatively, we show that MotionBricks produces state-of-the-art motion quality on open-source and proprietary datasets of various scales, while also achieving a real-time throughput of 15,000 FPS with 2ms latency. We demonstrate the flexibility and robustness of MotionBricks in a complete production-level animation demo, covering navigation and object-scene interaction across various styles with a unified model. To showcase our framework's application beyond animation, we deploy MotionBricks on the Unitree G1 humanoid robot to demonstrate its flexibility and generalization for real-time robotic control.

摘要：儘管生成運動合成技術取得了變革性的進展，但實時互動運動控制仍然以傳統技術為主導。<lb/>在這項工作中，我們確定了在研究與生產之間架起橋樑的兩個關鍵挑戰：1）實時可擴展性：行業應用需要實時生成大量的運動技能，而生成方法在實時計算約束下顯示出質量和可擴展性的顯著下降；2）整合：行業應用需要細粒度的多模態控制，涉及速度命令、風格選擇和精確的關鍵幀，而這一需求在現有的基於文本或標籤的模型中基本未得到滿足。<lb/>為了克服這些限制，我們介紹了MotionBricks：一個大規模的實時生成框架，提供了雙重解決方案。<lb/>首先，我們提出了一個大型模塊化潛在生成骨幹，專門針對穩健的實時運動生成，有效地用單一模型建模超過350,000個運動片段的數據集。<lb/>其次，我們引入了智能原語，提供了一個統一、穩健且直觀的界面，用於創作導航和物體交互。<lb/>應用程序可以像組裝磚塊一樣以即插即用的方式設計，而無需專業的動畫知識。<lb/>從量化上看，我們展示了MotionBricks在各種規模的開源和專有數據集上產生了最先進的運動質量，同時實現了15,000 FPS的實時吞吐量，延遲僅為2毫秒。<lb/>我們在一個完整的生產級動畫演示中展示了MotionBricks的靈活性和穩健性，涵蓋了各種風格的導航和物體場景交互，並使用統一模型。<lb/>為了展示我們框架在動畫之外的應用，我們在Unitree G1人形機器人上部署了MotionBricks，以展示其在實時機器人控制中的靈活性和泛化能力。

##### **Personalized Worked Example Generation from Student Code Submissions using Pattern-based Knowledge Components**
2604.24758v1 by Griffin Pitts, Muntasir Hoq, Peter Brusilovsky, Narges Norouzi, Arto Hellas, Juho Leinonen, Bita Akram

Adaptive programming practice often relies on fixed libraries of worked examples and practice problems, which require substantial authoring effort and may not correspond well to the logical errors and partial solutions students produce while writing code. As a result, students may receive learning content that does not directly address the concepts they are working to understand, while instructors must either invest additional effort in expanding content libraries or accept a coarse level of personalization. We present an approach for knowledge-component (KC) guided educational content generation using pattern-based KCs extracted from student code. Given a problem statement and student submissions, our pipeline extracts recurring structural KC patterns from students' code through AST-based analysis and uses them to condition a generative model. In this study, we apply this approach to worked example generation, and compare baseline and KC-conditioned outputs through expert evaluation. Results suggest that KC-conditioned generation improves topical focus and relevance to learners' underlying logical errors, providing evidence that KC-based steering of generative models can support personalized learning at scale.

摘要：適應性編程實踐通常依賴於固定的範例和練習題庫，這需要大量的創作努力，並且可能與學生在編寫代碼時產生的邏輯錯誤和部分解決方案不太相符。<lb/>因此，學生可能會收到不直接針對他們正在努力理解的概念的學習內容，而教師則必須在擴展內容庫上投入額外的努力，或者接受較粗糙的個性化程度。<lb/>我們提出了一種基於知識組件（KC）引導的教育內容生成方法，使用從學生代碼中提取的基於模式的KC。<lb/>給定問題陳述和學生提交，我們的管道通過基於AST的分析從學生的代碼中提取重複的結構KC模式，並使用這些模式來調整生成模型。<lb/>在本研究中，我們將這種方法應用於範例生成，並通過專家評估比較基準和KC調整的輸出。<lb/>結果表明，KC調整的生成提高了主題的聚焦度和與學習者潛在邏輯錯誤的相關性，提供了KC驅動生成模型可以支持大規模個性化學習的證據。

##### **On the Trainability of Masked Diffusion Language Models via Blockwise Locality**
2604.24832v1 by Yuxiang Wang, Yu Xiang, Baojian Zhou, Qifang Zhao, Keyue Jiang, Yanghua Xiao, Xiaoxiao Xu

Masked diffusion language models (MDMs) have recently emerged as a promising alternative to standard autoregressive large language models (AR-LLMs), yet their optimization can be substantially less stable. We study blockwise MDMs and compare them with AR-LLMs on three controlled tasks that stress different aspects of structured generation: in-context linear regression, graph path-finding, and Sudoku solving. We find that standard random-masking MDMs fail to reliably learn linear regression, exhibit high variance training dynamics on graph path-finding, while outperforming AR-LLMs on Sudoku. To mitigate these instabilities, we propose two locality aware blockwise models, namely Jigsaw and Scatter, that inject left-to-right inductive bias by enforcing autoregressive locality within blocks while preserving iterative refinement at the block level. Empirically, Jigsaw matches AR-LLM stability on linear regression and remains strong on Sudoku, while Scatter retains diffusion's planning advantage on path-finding. Our results indicate that standard random-masking MDMs, even with blockwise variants, may be a suboptimal instantiation of diffusion LMs for ordered generation, motivating models beyond random masking.

摘要：被遮蔽的擴散語言模型（MDMs）最近作為標準自回歸大型語言模型（AR-LLMs）的有希望替代方案出現，但其優化的穩定性可能大幅降低。我們研究了區塊式 MDMs，並將其與 AR-LLMs 在三個控制任務上進行比較，這些任務強調結構化生成的不同方面：上下文線性回歸、圖形路徑尋找和數獨解題。我們發現標準隨機遮蔽的 MDMs 無法可靠地學習線性回歸，在圖形路徑尋找中表現出高變異的訓練動態，而在數獨上則超越了 AR-LLMs。為了減輕這些不穩定性，我們提出了兩個考慮局部性的區塊式模型，即 Jigsaw 和 Scatter，這些模型通過在區塊內強制自回歸局部性來注入從左到右的歸納偏差，同時保持區塊層級的迭代精煉。實證結果表明，Jigsaw 在線性回歸上與 AR-LLM 的穩定性相匹配，並在數獨上保持強勁，而 Scatter 則在路徑尋找中保留了擴散的規劃優勢。我們的結果表明，標準隨機遮蔽的 MDMs，即使有區塊式變體，對於有序生成可能是擴散 LMs 的次優實例，這促使我們尋求超越隨機遮蔽的模型。

##### **Can Current Agents Close the Discovery-to-Application Gap? A Case Study in Minecraft**
2604.24697v1 by Zhou Ziheng, Huacong Tang, Jinyuan Zhang, Haowei Lin, Bangcheng Yang, Qian Long, Fang Sun, Yizhou Sun, Yitao Liang, Ying Nian Wu, Demetri Terzopoulos, Xiaofeng Gao

Discovering causal regularities and applying them to build functional systems--the discovery-to-application loop--is a hallmark of general intelligence, yet evaluating this capacity has been hindered by the vast complexity gap between scientific discovery and real-world engineering. We introduce SciCrafter, a Minecraft-based benchmark that operationalizes this loop through parameterized redstone circuit tasks. Agents must ignite lamps in specified patterns (e.g., simultaneously or in timed sequences); scaling target parameters substantially increases construction complexity and required knowledge, forcing genuine discovery rather than reliance on memorized solutions. Evaluating frontier models including GPT-5.2, Gemini-3-Pro, and Claude-Opus-4.5 under a general-purpose code agent scaffold, we find that all plateau at approximately 26% success rate. To diagnose these failures, we decompose the loop into four capacities--knowledge gap identification, experimental discovery, knowledge consolidation, and knowledge application--and design targeted interventions whose marginal contributions serve as proxies for corresponding gaps. Our analysis reveals that although the general knowledge application capability still remains as the biggest gap across all models, for frontier models the knowledge gap identification starts to become a major hurdle--indicating the bottleneck is shifting from solving problems right to raising the right problems for current AI. We release SciCrafter as a diagnostic probe for future research on AI systems that navigate the full discovery-to-application loop.

摘要：發現因果規律並將其應用於構建功能系統——發現到應用的循環——是一般智能的標誌，然而評估這一能力卻受到科學發現與現實世界工程之間龐大複雜性差距的阻礙。我們介紹了 SciCrafter，一個基於 Minecraft 的基準，通過參數化的紅石電路任務來操作這一循環。代理必須以指定的模式點亮燈（例如，同時或按時間序列）；目標參數的擴展顯著增加了建設的複雜性和所需知識，迫使真正的發現，而非依賴記憶的解決方案。在一般用途的代碼代理框架下評估包括 GPT-5.2、Gemini-3-Pro 和 Claude-Opus-4.5 的前沿模型，我們發現所有模型的成功率都在約 26% 附近達到平臺。為了診斷這些失敗，我們將循環分解為四個能力——知識差距識別、實驗發現、知識整合和知識應用——並設計針對性的干預措施，其邊際貢獻作為相應差距的代理。我們的分析顯示，儘管一般知識應用能力仍然是所有模型中最大的差距，但對於前沿模型來說，知識差距識別開始成為一個主要障礙——這表明瓶頸正從正確解決問題轉向為當前 AI 提出正確問題。我們發布 SciCrafter 作為未來研究 AI 系統的診斷探針，這些系統能夠導航完整的發現到應用循環。

##### **Can LLMs Act as Historians? Evaluating Historical Research Capabilities of LLMs via the Chinese Imperial Examination**
2604.24690v1 by Lirong Gao, Zeqing Wang, Yuyan Cai, Jiayi Deng, Yanmei Gu, Yiming Zhang, Jia Zhou, Yanfei Zhang, Junbo Zhao

While Large Language Models (LLMs) have increasingly assisted in historical tasks such as text processing, their capacity for professional-level historical reasoning remains underexplored. Existing benchmarks primarily assess basic knowledge breadth or lexical understanding, failing to capture the higher-order skills, such as evidentiary reasoning,that are central to historical research. To fill this gap, we introduce ProHist-Bench, a novel benchmark anchored in the Chinese Imperial Examination (Keju) system, a comprehensive microcosm of East Asian political, social, and intellectual history spanning over 1,300 years. Developed through deep interdisciplinary collaboration, ProHist-Bench features 400 challenging, expert-curated questions across eight dynasties, accompanied by 10,891 fine-grained evaluation rubrics. Through a rigorous evaluation of 18 LLMs, we reveal a significant proficiency gap: even state-of-the-art LLMs struggle with complex historical research questions. We hope ProHist-Bench will facilitate the development of domain-specific reasoning LLMs, advance computational historical research, and further uncover the untapped potential of LLMs. We release ProHist-Bench at https://github.com/inclusionAI/ABench/tree/main/ProHist-Bench.

摘要：雖然大型語言模型（LLMs）在文本處理等歷史任務中越來越有助益，但它們在專業級歷史推理方面的能力仍然未被充分探索。現有的基準主要評估基本知識的廣度或詞彙理解，未能捕捉到對歷史研究至關重要的高階技能，例如證據推理。為填補這一空白，我們推出了ProHist-Bench，一個基於中國科舉制度的新基準，這是一個涵蓋超過1300年的東亞政治、社會和智識歷史的綜合縮影。ProHist-Bench通過深度跨學科合作開發，包含400個挑戰性的專家策劃問題，涵蓋八個朝代，並附有10,891個細緻的評估標準。通過對18個LLMs的嚴格評估，我們揭示了一個顯著的能力差距：即使是最先進的LLMs在面對複雜的歷史研究問題時也面臨困難。我們希望ProHist-Bench能促進領域特定推理LLMs的發展，推進計算歷史研究，並進一步挖掘LLMs未被開發的潛力。我們在https://github.com/inclusionAI/ABench/tree/main/ProHist-Bench發布ProHist-Bench。

##### **XGRAG: A Graph-Native Framework for Explaining KG-based Retrieval-Augmented Generation**
2604.24623v1 by Zhuoling Li, Ha Linh Hong Tran Nguyen, Valeria Bladinieres, Maxim Romanovsky

Graph-based Retrieval-Augmented Generation (GraphRAG) extends traditional RAG by using knowledge graphs (KGs) to give large language models (LLMs) a structured, semantically coherent context, yielding more grounded answers. However, GraphRAG reasoning process remains a black-box, limiting our ability to understand how specific pieces of structured knowledge influence the final output. Existing explainability (XAI) methods for RAG systems, designed for text-based retrieval, are limited to interpreting an LLM response through the relational structures among knowledge components, creating a critical gap in transparency and trustworthiness. To address this, we introduce XGRAG, a novel framework that generates causally grounded explanations for GraphRAG systems by employing graph-based perturbation strategies, to quantify the contribution of individual graph components on the model answer. We conduct extensive experiments comparing XGRAG against RAG-Ex, an XAI baseline for standard RAG, and evaluate its robustness across various question types, narrative structures and LLMs. Our results demonstrate a 14.81% improvement in explanation quality over the baseline RAG-Ex across NarrativeQA, FairyTaleQA, and TriviaQA, evaluated by F1-score measuring alignment between generated explanations and original answers. Furthermore, XGRAG explanations exhibit a strong correlation with graph centrality measures, validating its ability to capture graph structure. XGRAG provides a scalable and generalizable approach towards trustworthy AI through transparent, graph-based explanations that enhance the interpretability of RAG systems.

摘要：圖形基礎檢索增強生成（GraphRAG）通過使用知識圖（KGs）來擴展傳統的RAG，為大型語言模型（LLMs）提供結構化且語義一致的上下文，從而產生更有根據的答案。<lb/>然而，GraphRAG的推理過程仍然是一個黑箱，限制了我們理解特定結構化知識如何影響最終輸出的能力。<lb/>現有的針對RAG系統的可解釋性（XAI）方法，設計用於基於文本的檢索，僅限於通過知識組件之間的關係結構來解釋LLM的回應，這在透明度和可信度上造成了重大差距。<lb/>為了解決這個問題，我們引入了XGRAG，一個新穎的框架，通過採用基於圖的擾動策略來生成GraphRAG系統的因果基礎解釋，以量化個別圖形組件對模型答案的貢獻。<lb/>我們進行了廣泛的實驗，將XGRAG與RAG-Ex進行比較，後者是標準RAG的XAI基準，並評估其在各種問題類型、敘事結構和LLMs中的穩健性。<lb/>我們的結果顯示，在NarrativeQA、FairyTaleQA和TriviaQA中，解釋質量相比基準RAG-Ex提高了14.81%，這是通過F1-score來衡量生成的解釋與原始答案之間的對齊度。<lb/>此外，XGRAG的解釋與圖形中心性度量顯示出強相關性，驗證了其捕捉圖形結構的能力。<lb/>XGRAG提供了一種可擴展和可通用的方法，通過透明的基於圖的解釋來增強RAG系統的可解釋性，從而實現可信的人工智能。

##### **Incompressible Knowledge Probes: Estimating Black-Box LLM Parameter Counts via Factual Capacity**
2604.24827v1 by Bojie Li

Closed-source frontier labs do not disclose parameter counts, and the standard alternative -- inference economics -- carries $2\times$+ uncertainty from hardware, batching, and serving-stack assumptions external to the model. We exploit a tighter intrinsic bound: storing $F$ facts requires at least $F/$(bits per parameter) weights, so measuring how much a model \emph{knows} lower-bounds how many parameters it \emph{has}. We introduce \textbf{Incompressible Knowledge Probes (IKPs)}, a benchmark of 1{,}400 factual questions spanning 7 tiers of obscurity, designed to isolate knowledge that cannot be derived by reasoning or compressed by architectural improvements.
  We calibrate a log-linear mapping from IKP accuracy to parameter count on 89 open-weight models (135M--1,600B) spanning 19 vendors, achieving $R^2 = 0.917$; leave-one-out cross-validation confirms generalization (median fold error $1.59\times$, $68.5\%$ within $2\times$ and $87.6\%$ within $3\times$). For Mixture-of-Experts models, total parameters predict knowledge ($R^2 = 0.79$) far better than active parameters ($R^2 = 0.51$). We evaluate 188 models from 27 vendors and estimate effective knowledge capacity for all major proprietary frontier models; for heavily safety-tuned models the estimates are lower bounds, since refusal policy can hide tens of percentage points of "refused but known" capacity.
  The widely-reported saturation of reasoning benchmarks does not imply the end of scaling. Procedural capability compresses under the "Densing Law," but across 96 dated open-weight models the IKP time coefficient is $-0.0010$/month (95\% CI $[-0.0031, +0.0008]$) -- indistinguishable from zero, and rejecting the Densing prediction of $+0.0117$/month at $p < 10^{-15}$. Factual capacity continues to scale log-linearly with parameters across generations and across vendors.

摘要：封閉源代碼的前沿實驗室不會透露參數數量，而標準的替代方案——推理經濟學——則承擔來自硬體、批次和服務堆疊假設的 $2\times$+ 不確定性，這些假設是模型外部的。我們利用更緊湊的內在界限：儲存 $F$ 個事實至少需要 $F/$(每個參數的位元) 的權重，因此測量一個模型 \emph{知道} 多少可以下限其 \emph{擁有} 的參數數量。我們引入 \textbf{不可壓縮知識探針 (IKPs)}，這是一個包含 1{,}400 個事實問題的基準，涵蓋 7 個模糊層級，旨在隔離無法通過推理得出或通過架構改進壓縮的知識。<lb/>我們在 89 個開放權重模型（135M–1,600B）上校準了 IKP 準確性與參數數量之間的對數線性映射，這些模型來自 19 家供應商，達到 $R^2 = 0.917$；留一交叉驗證確認了泛化（中位數折疊誤差 $1.59\times$，$68.5\%$ 在 $2\times$ 內，$87.6\%$ 在 $3\times$ 內）。對於專家混合模型，總參數預測知識的能力 ($R^2 = 0.79$) 遠遠優於活躍參數 ($R^2 = 0.51$)。我們評估了來自 27 家供應商的 188 個模型，並估算所有主要專有前沿模型的有效知識容量；對於高度安全調整的模型，這些估算是下限，因為拒絕政策可能隱藏數個百分點的「拒絕但已知」容量。<lb/>廣泛報導的推理基準飽和並不意味著擴展的結束。程序能力在「密集法則」下壓縮，但在 96 個過時的開放權重模型中，IKP 時間係數為 $-0.0010$/月（95\% CI $[-0.0031, +0.0008]$）——與零無法區分，並拒絕了 $p < 10^{-15}$ 的密集預測 $+0.0117$/月。事實容量隨著代數和供應商之間的參數繼續以對數線性方式擴展。

##### **NeSyCat: A Monad-Based Categorical Semantics of the Neurosymbolic ULLER Framework**
2604.24612v1 by Daniel Romero Schellhorn, Till Mossakowski

ULLER (Unified Language for LEarning and Reasoning) offers a unified first-order logic (FOL) syntax, enabling its knowledge bases to be used directly across a wide range of neurosymbolic systems. The original specification endows this syntax with three pairwise independent semantics: classical, fuzzy, and probabilistic, each accompanied by dedicated semantic rules. We show that these seemingly disparate semantics are all instances of one categorical framework based on monads, the very construct that models side effects in functional programming. This enables the modular addition of new semantics and systematic translations between them. As example, we outline the addition of generalised quantification in Logic Tensor Networks (LTN) to arbitrary (also infinite) domains by extending the Giry monad to probability spaces. In particular, our approach allows a modular implementation of ULLER in Python and Haskell, of which we have published initial versions on GitHub.

摘要：ULLER（統一學習與推理語言）提供了一種統一的一階邏輯（FOL）語法，使其知識庫能夠直接在各種神經符號系統中使用。原始規範賦予這種語法三種成對獨立的語義：經典語義、模糊語義和概率語義，每種語義都有專門的語義規則。我們展示了這些看似不同的語義實際上都是基於單子的一個範疇框架的實例，這正是用來建模函數式編程中的副作用的構造。這使得新的語義可以模塊化地添加，並在它們之間進行系統性的轉換。作為例子，我們概述了通過將Giry單子擴展到概率空間，將廣義量化添加到邏輯張量網絡（LTN）中的任意（也包括無限）領域。特別是，我們的方法允許在Python和Haskell中模塊化實現ULLER，我們已經在GitHub上發布了初始版本。

##### **A systematic evaluation of vision-language models for observational astronomical reasoning tasks**
2604.24589v1 by Wenke Ren, Hengxiao Guo, Wenwen Zuo, Xiaoman Zhang

Vision-language models (VLMs) are increasingly proposed as general-purpose tools for scientific data interpretation, yet their reliability on real astronomical observations across diverse modalities remains untested. We present AstroVLBench, a comprehensive benchmark comprising over 4,100 expert-verified instances across five tasks spanning optical imaging, radio interferometry, multi-wavelength photometry, time-domain light curves, and optical spectroscopy. Evaluating six frontier models, we find that performance is strongly modality-dependent: while one model (Gemini 3 Pro) emerges as the most consistently capable across tasks, task-specific strengths vary, and all models substantially underperform domain-specialized methods. Mechanistic ablations reveal that performance depends not only on directing attention to salient visual features but also on grounding those features in physical knowledge. Phenomenological prompts describing what to look for improve accuracy by sharpening model focus, but physical prompts explaining why those features matter perform better overall and yield more balanced classifications with reduced class-specific bias. Consistent with this picture, presenting the underlying one-dimensional measurements directly as numerical tables instead of rendered plots yields up to 13 percentage points improvement. Reasoning quality analysis further demonstrates that, without explicit physical grounding, models may reach correct predictions from phenomenologically plausible cues while providing physically imprecise justifications, establishing that accuracy alone is insufficient for trustworthy scientific deployment. These findings provide the first systematic, multi-modal baselines for VLMs in observational astronomy and identify the specific representation, grounding, and reasoning bottlenecks where current models fail.

摘要：視覺-語言模型（VLMs）越來越多地被提議作為科學數據解釋的通用工具，但它們在不同模態下對真實天文觀測的可靠性仍未經過測試。<lb/>我們提出了AstroVLBench，這是一個全面的基準，包含超過4,100個專家驗證的實例，涵蓋光學成像、無線電干涉測量、多波長光度學、時間域光變曲線和光學光譜學五個任務。<lb/>評估六個前沿模型後，我們發現性能強烈依賴於模態：雖然一個模型（Gemini 3 Pro）在各任務中表現最為一致，但任務特定的優勢各不相同，所有模型的表現均顯著低於領域專門化的方法。<lb/>機制性消融實驗顯示，性能不僅取決於將注意力引導至顯著的視覺特徵，還取決於將這些特徵與物理知識相結合。<lb/>描述要觀察什麼的現象學提示通過加強模型的專注來提高準確性，但解釋這些特徵為何重要的物理提示則整體表現更佳，並產生更平衡的分類，減少了類別特定的偏見。<lb/>與此一致的是，直接將基礎的一維測量呈現為數值表格，而不是渲染的圖表，能提高最多13個百分點的準確性。<lb/>推理質量分析進一步表明，若沒有明確的物理基礎，模型可能從現象學上合理的線索中得出正確的預測，同時提供物理上不精確的理由，這表明僅有準確性不足以支持可信的科學應用。<lb/>這些發現為VLMs在觀測天文學中的首次系統性、多模態基準提供了基礎，並確定了當前模型失敗的具體表徵、基礎和推理瓶頸。

##### **MEG-RAG: Quantifying Multi-modal Evidence Grounding for Evidence Selection in RAG**
2604.24564v1 by Xihang Wang, Zihan Wang, Chengkai Huang, Quan Z. Sheng, Lina Yao

Multimodal Retrieval-Augmented Generation (MRAG) addresses key limitations of Multimodal Large Language Models (MLLMs), such as hallucination and outdated knowledge. However, current MRAG systems struggle to distinguish whether retrieved multimodal data truly supports the semantic core of an answer or merely provides superficial relevance. Existing metrics often rely on heuristic position-based confidence, which fails to capture the informational density of multimodal entities. To address this, we propose Multi-modal Evidence Grounding (MEG), a semantic-aware metric that quantifies the contribution of retrieved evidence. Unlike standard confidence measures, MEG utilizes Semantic Certainty Anchoring, focusing on high-IDF information-bearing tokens that better capture the semantic core of the answer. Building on MEG, we introduce MEG-RAG, a framework that trains a multimodal reranker to align retrieved evidence with the semantic anchors of the ground truth. By prioritizing high-value content based on semantic grounding rather than token probability distributions, MEG-RAG improves the accuracy and multimodal consistency of generated outputs. Extensive experiments on the M$^2$RAG benchmark show that MEG-RAG consistently outperforms strong baselines and demonstrates robust generalization across different teacher models.

摘要：多模態檢索增強生成（MRAG）解決了多模態大型語言模型（MLLMs）的關鍵限制，例如幻覺和過時的知識。<lb/>然而，當前的MRAG系統在區分檢索到的多模態數據是否真正支持答案的語義核心，或僅僅提供表面的相關性方面存在困難。<lb/>現有的度量標準通常依賴於基於位置的啟發式信心，這無法捕捉多模態實體的信息密度。<lb/>為了解決這個問題，我們提出了多模態證據基礎（MEG），這是一種語義感知的度量標準，用於量化檢索證據的貢獻。<lb/>與標準的信心度量不同，MEG利用語義確定性錨定，專注於高IDF信息承載標記，這些標記更好地捕捉答案的語義核心。<lb/>基於MEG，我們引入了MEG-RAG，一個訓練多模態重新排序器的框架，旨在將檢索到的證據與真實情況的語義錨定對齊。<lb/>通過基於語義基礎而非標記概率分佈優先考慮高價值內容，MEG-RAG提高了生成輸出的準確性和多模態一致性。<lb/>在M$^2$RAG基準上的大量實驗表明，MEG-RAG始終優於強基準，並在不同的教師模型中展示了穩健的泛化能力。

##### **Layerwise Convergence Fingerprints for Runtime Misbehavior Detection in Large Language Models**
2604.24542v1 by Nay Myat Min, Long H. Pham, Jun Sun

Large language models deployed at runtime can misbehave in ways that clean-data validation cannot anticipate: training-time backdoors lie dormant until triggered, jailbreaks subvert safety alignment, and prompt injections override the deployer's instructions. Existing runtime defenses address these threats one at a time and often assume a clean reference model, trigger knowledge, or editable weights, assumptions that rarely hold for opaque third-party artifacts. We introduce Layerwise Convergence Fingerprinting (LCF), a tuning-free runtime monitor that treats the inter-layer hidden-state trajectory as a health signal: LCF computes a diagonal Mahalanobis distance on every inter-layer difference, aggregates via Ledoit-Wolf shrinkage, and thresholds via leave-one-out calibration on 200 clean examples, with no reference model, trigger knowledge, or retraining. Evaluated on four architectures (Llama-3-8B, Qwen2.5-7B, Gemma-2-9B, Qwen2.5-14B) across backdoors, jailbreaks, and prompt injection (56 backdoor combinations, 3 jailbreak techniques, and BIPIA email + code-QA), LCF reduces mean backdoor attack success rate (ASR) below 1% on Qwen2.5-7B and Gemma-2 and to 1.3% on Qwen2.5-14B, detects 92-100% of DAN jailbreaks (62-100% for GCG and softer role-play), and flags 100% of text-payload injections across all eight (model, domain) cells, at 12-16% backdoor FPR and <0.1% inference overhead. A single aggregation score covers all three threat families without threat-specific tuning, positioning LCF as a general-purpose runtime safety layer for cloud-served and on-device LLMs.

摘要：大型語言模型在運行時的部署可能會以清潔數據驗證無法預見的方式出現問題：訓練時的後門在觸發之前處於休眠狀態，越獄行為會顛覆安全對齊，而提示注入則會覆蓋部署者的指示。現有的運行時防禦措施通常一次只針對這些威脅，並且經常假設有一個清潔的參考模型、觸發知識或可編輯的權重，這些假設在不透明的第三方工件中很少成立。我們介紹了分層收斂指紋識別（LCF），這是一種無需調整的運行時監控器，將層間隱藏狀態的軌跡視為健康信號：LCF計算每個層間差異的對角馬哈拉諾比斯距離，通過Ledoit-Wolf收縮進行聚合，並在200個清潔示例上通過留一法校準進行閾值設定，無需參考模型、觸發知識或重新訓練。在四種架構（Llama-3-8B、Qwen2.5-7B、Gemma-2-9B、Qwen2.5-14B）上進行評估，涵蓋後門、越獄和提示注入（56種後門組合、3種越獄技術，以及BIPIA電子郵件 + 代碼-QA），LCF將Qwen2.5-7B和Gemma-2的平均後門攻擊成功率（ASR）降低到1%以下，並將Qwen2.5-14B降低到1.3%，檢測到92-100%的DAN越獄（GCG和更柔和的角色扮演為62-100%），並在所有八個（模型、領域）單元中標記100%的文本有效載荷注入，後門FPR為12-16%，推理開銷小於0.1%。單一聚合得分涵蓋所有三種威脅類別，無需針對特定威脅的調整，將LCF定位為雲服務和設備上LLM的通用運行時安全層。

##### **SEARCH-R: Structured Entity-Aware Retrieval with Chain-of-Reasoning Navigator for Multi-hop Question Answering**
2604.24515v1 by Yuqing Fu, Yimin Deng, Wanyu Wang, Yuhao Wang, Yejing Wang, Hongshi Liu, Yiqi Wang, Xiao Han, Maolin Wang, Guoshuai Zhao, Yi Chang, Xiangyu Zhao

Multi-hop Question Answering (MHQA) aims to answer questions that require multi-step reasoning. It presents two key challenges: generating correct reasoning paths in response to the complex user queries, and accurately retrieving essential knowledge in the face of potential limitations in large language models (LLMs). Existing approaches primarily rely on prompt-based methods to generate reasoning paths, which are further combined with traditional sparse or dense retrieval to produce the final answer. However, the generation of reasoning paths commonly lacks effective control over the generative process, thus leading the reasoning astray. Meanwhile, the retrieval methods over-rely on knowledge matching or similarity scores rather than evaluating the practical utility of the information, resulting in retrieving homogeneous or non-useful information. Therefore, we propose a Structured Entity-Aware Retrieval with Chain-of-Reasoning Navigator framework named SEARCH-R. Specifically, SEARCH-R trains an end-to-end reasoning path navigator, which is able to provide a powerful sub-question decomposer by fine-tuning the Llama3.1-8B model. Moreover, a novel dependency tree-based retrieval is designed to evaluate the informational contribution of the document quantitatively. Extensive experiments on three challenging multi-hop datasets validate the effectiveness of the proposed framework. The code and dataset are available at: https://github.com/Applied-Machine-Learning-Lab/ACL2026_SEARCH-R.

摘要：多跳問題回答（MHQA）旨在回答需要多步推理的問題。<lb/>它提出了兩個主要挑戰：生成正確的推理路徑以回應複雜的用戶查詢，以及在大型語言模型（LLMs）潛在限制的情況下準確檢索必要的知識。<lb/>現有的方法主要依賴基於提示的方法來生成推理路徑，這些路徑進一步與傳統的稀疏或密集檢索相結合，以產生最終答案。<lb/>然而，推理路徑的生成通常缺乏對生成過程的有效控制，因此導致推理偏離正軌。<lb/>同時，檢索方法過於依賴知識匹配或相似性分數，而不是評估信息的實際效用，導致檢索到同質或無用的信息。<lb/>因此，我們提出了一個名為SEARCH-R的結構化實體感知檢索與推理鏈導航器框架。<lb/>具體而言，SEARCH-R訓練了一個端到端的推理路徑導航器，能夠通過微調Llama3.1-8B模型提供強大的子問題分解器。<lb/>此外，設計了一種新穎的基於依賴樹的檢索方法，以定量評估文檔的信息貢獻。<lb/>在三個具有挑戰性的多跳數據集上進行的廣泛實驗驗證了所提出框架的有效性。<lb/>代碼和數據集可在以下網址獲得：https://github.com/Applied-Machine-Learning-Lab/ACL2026_SEARCH-R。

##### **Programming with Data: Test-Driven Data Engineering for Self-Improving LLMs from Raw Corpora**
2604.24819v1 by Chenkai Pan, Xinglong Xu, Yuhang Xu, Yujun Wu, Siyuan Li, Jintao Chen, Conghui He, Jingxuan Wei, Cheng Tan

Reliably transferring specialized human knowledge from text into large language models remains a fundamental challenge in artificial intelligence. Fine-tuning on domain corpora has enabled substantial capability gains, but the process operates without feedback: when a model fails on a domain task, there is no method to diagnose what is deficient in the training data, and the only recourse is to add more data indiscriminately. Here we show that when a structured knowledge representation extracted from the source corpus serves as the shared foundation for both training data and evaluation, the complete data-engineering lifecycle maps onto the software development lifecycle in a precise and operative way: training data becomes source code specifying what the model should learn, model training becomes compilation, benchmarking becomes unit testing, and failure-driven data repair becomes debugging. Under this correspondence, model failures decompose into concept-level gaps and reasoning-chain breaks that can be traced back to specific deficiencies in the data and repaired through targeted patches, with each repair cycle producing consistent improvements across model scales and architectures without degrading general capabilities. We formalize this principle as Programming with Data and instantiate it across sixteen disciplines spanning the natural sciences, engineering, biomedicine, and the social sciences, releasing a structured knowledge base, benchmark suite, and training corpus as open resources. By demonstrating that the relationship between training data and model behaviour is structurally traceable and systematically repairable, this work establishes a principled foundation for the reliable engineering of human expertise into language models.

摘要：可靠地將專業人類知識從文本轉移到大型語言模型仍然是人工智慧中的一個基本挑戰。<lb/>在領域語料庫上進行微調已經實現了顯著的能力提升，但這一過程在沒有反饋的情況下運作：當模型在某個領域任務上失敗時，沒有方法可以診斷訓練數據中缺乏什麼，唯一的補救措施就是不加區別地添加更多數據。<lb/>在這裡，我們展示了當從源語料庫提取的結構化知識表示作為訓練數據和評估的共享基礎時，完整的數據工程生命周期與軟體開發生命周期以精確和有效的方式對應：訓練數據成為指定模型應該學習的源代碼，模型訓練成為編譯，基準測試成為單元測試，而基於失敗的數據修復則成為調試。<lb/>在這種對應關係下，模型失敗分解為概念層級的缺口和推理鏈的中斷，這些都可以追溯到數據中的具體缺陷，並通過有針對性的修補進行修復，每個修復循環在模型規模和架構上都產生一致的改進，而不會降低一般能力。<lb/>我們將這一原則形式化為「用數據編程」，並在涵蓋自然科學、工程、生物醫學和社會科學的十六個學科中實現，釋放結構化知識庫、基準套件和訓練語料庫作為開放資源。<lb/>通過展示訓練數據與模型行為之間的關係是結構上可追溯和系統上可修復的，這項工作為將人類專業知識可靠地工程化到語言模型中建立了一個原則性的基礎。

##### **GAMMAF: A Common Framework for Graph-Based Anomaly Monitoring Benchmarking in LLM Multi-Agent Systems**
2604.24477v1 by Pablo Mateo-Torrejón, Alfonso Sánchez-Macián

The rapid integration of Large Language Models (LLMs) into Multi-Agent Systems (MAS) has significantly enhanced their collaborative problem-solving capabilities, but it has also expanded their attack surfaces, exposing them to vulnerabilities such as prompt infection and compromised inter-agent communication. While emerging graph-based anomaly detection methods show promise in protecting these networks, the field currently lacks a standardized, reproducible environment to train these models and evaluate their efficacy. To address this gap, we introduce Gammaf (Graph-based Anomaly Monitoring for LLM Multi-Agent systems Framework), an open-source benchmarking platform. Gammaf is not a novel defense mechanism itself, but rather a comprehensive evaluation architecture designed to generate synthetic multi-agent interaction datasets and benchmark the performance of existing and future defense models. The proposed framework operates through two interdependent pipelines: a Training Data Generation stage, which simulates debates across varied network topologies to capture interactions as robust attributed graphs, and a Defense System Benchmarking stage, which actively evaluates defense models by dynamically isolating flagged adversarial nodes during live inference rounds. Through rigorous evaluation using established defense baselines (XG-Guard and BlindGuard) across multiple knowledge tasks (such as MMLU-Pro and GSM8K), we demonstrate Gammaf's high utility, topological scalability, and execution efficiency. Furthermore, our experimental results reveal that equipping an LLM-MAS with effective attack remediation not only recovers system integrity but also substantially reduces overall operational costs by facilitating early consensus and cutting off the extensive token generation typical of adversarial agents.

摘要：大型語言模型（LLMs）迅速融入多代理系統（MAS），顯著提升了它們的協作問題解決能力，但同時也擴大了它們的攻擊面，使其暴露於如提示感染和代理間通信受損等漏洞之中。雖然新興的基於圖的異常檢測方法在保護這些網絡方面顯示出潛力，但該領域目前缺乏一個標準化、可重複的環境來訓練這些模型並評估其效能。為了解決這一缺口，我們推出了Gammaf（基於圖的LLM多代理系統異常監控框架），這是一個開源基準測試平台。Gammaf本身並不是一種新型防禦機制，而是一個綜合評估架構，旨在生成合成的多代理互動數據集並基準測試現有和未來防禦模型的性能。所提出的框架通過兩個相互依賴的管道運作：一個是訓練數據生成階段，該階段模擬在不同網絡拓撲下的辯論，以捕捉作為穩健屬性圖的互動；另一個是防禦系統基準測試階段，該階段通過在實時推理回合中動態隔離標記的對抗節點來主動評估防禦模型。通過使用已建立的防禦基準（XG-Guard和BlindGuard）在多個知識任務（如MMLU-Pro和GSM8K）上進行嚴格評估，我們展示了Gammaf的高效用性、拓撲可擴展性和執行效率。此外，我們的實驗結果顯示，為LLM-MAS配備有效的攻擊補救措施不僅恢復了系統完整性，還通過促進早期共識和切斷對抗代理的廣泛令牌生成，顯著降低了整體運營成本。

##### **PhysNote: Self-Knowledge Notes for Evolvable Physical Reasoning in Vision-Language Model**
2604.24443v1 by Sinin Zhang, Yunfei Xie, Yuxuan Cheng, Haoyu Zhang, Tong Zhang

Vision-Language Models (VLMs) have demonstrated strong performance on textbook-style physics problems, yet they frequently fail when confronted with dynamic real-world scenarios that require temporal consistency and causal reasoning across frames. We identify two fundamental challenges underlying these failures: (1) spatio-temporal identity drift, where objects lose their physical identity across successive frames and break causal chains, and (2) volatility of inference-time insights, where a model may occasionally produce correct physical reasoning but never consolidates it for future reuse. To address these challenges, we propose PhysNote, an agentic framework that enables VLMs to externalize and refine physical knowledge through self-generated "Knowledge Notes." PhysNote stabilizes dynamic perception through spatio-temporal canonicalization, organizes self-generated insights into a hierarchical knowledge repository, and drives an iterative reasoning loop that grounds hypotheses in visual evidence before consolidating verified knowledge. Experiments on PhysBench demonstrate that PhysNote achieves 56.68% overall accuracy, a 4.96% improvement over the best multi-agent baseline, with consistent gains across all four physical reasoning domains.

摘要：視覺-語言模型（VLMs）在教科書風格的物理問題上展現了強大的表現，然而在面對需要時間一致性和跨幀因果推理的動態現實場景時，它們經常失敗。我們確定了這些失敗背後的兩個基本挑戰：（1）時空身份漂移，物體在連續幀之間失去其物理身份並打破因果鏈，以及（2）推理時間洞察的波動性，模型有時可能產生正確的物理推理，但從未將其整合以供未來重用。為了解決這些挑戰，我們提出了PhysNote，一個能夠使VLMs外部化和精煉物理知識的代理框架，通過自生成的“知識筆記”。PhysNote通過時空典範化穩定動態感知，將自生成的洞察組織成層次知識庫，並驅動一個迭代推理循環，在整合已驗證知識之前，將假設建立在視覺證據之上。對PhysBench的實驗表明，PhysNote達到了56.68%的整體準確率，比最佳多代理基線提高了4.96%，在所有四個物理推理領域中均有穩定的增長。

##### **All That Glitters Is Not Audio: Rethinking Text Priors and Audio Reliance in Audio-Language Evaluation**
2604.24401v1 by Leonardo Haw-Yang Foo, Chih-Kai Yang, Chen-An Li, Ke-Han Lu, Hung-yi Lee

Large Audio-Language Models show consistent performance gains across speech and audio benchmarks, yet high scores may not reflect true auditory perception. If a model can answer questions without processing the acoustic signal, the benchmark fails as a measure of auditory understanding. We present a diagnostic framework using two axes: text prior, which measures answerability from text and general knowledge alone, and audio reliance, which assesses actual dependency on the acoustic signal. Evaluating eight LALMs across three benchmarks, we find that models retain 60-72% of their full audio scores even without any audio input. Moreover, among items that require audio, only 3.0-4.2% need the complete audio clip; the majority can be resolved using localized fragments. These findings challenge the assumption that benchmark performance equals robust audio understanding, and we conclude with practical guidelines for improving evaluation reliability and benchmark design.

摘要：大型音頻語言模型在語音和音頻基準測試中顯示出穩定的性能增長，但高分數可能無法反映真實的聽覺感知。<lb/>如果一個模型能在不處理聲音信號的情況下回答問題，那麼基準測試就無法作為聽覺理解的衡量標準。<lb/>我們提出了一個診斷框架，使用兩個軸心：文本先驗，衡量僅從文本和一般知識的可回答性，以及音頻依賴性，評估對聲音信號的實際依賴程度。<lb/>在三個基準測試中評估八個LALM，我們發現這些模型即使在沒有任何音頻輸入的情況下，仍保留60-72%的完整音頻分數。<lb/>此外，在需要音頻的項目中，只有3.0-4.2%需要完整的音頻片段；大多數可以使用局部片段解決。<lb/>這些發現挑戰了基準測試性能等於穩健音頻理解的假設，我們最後提供了改善評估可靠性和基準設計的實用指導方針。

##### **Aligning with Your Own Voice: Self-Corrected Preference Learning for Hallucination Mitigation in LVLMs**
2604.24395v1 by Byeonggeuk Lim, JungMin Yun, Junehyoung Kwon, Kyeonghyun Kim, YoungBin Kim

Large Vision-Language Models (LVLMs) frequently suffer from hallucinations. Existing preference learning-based approaches largely rely on proprietary models to construct preference datasets. We identify that this reliance introduces a distributional mismatch between the proprietary and target models that hinders efficient alignment. To address this, we propose Alignment via VErified Self-correction DPO (AVES-DPO), a framework that aligns LVLMs using in-distribution data derived from the model's intrinsic knowledge. Our approach employs a consensus-based verification mechanism to diagnose diverse hallucinations and guides the model to self-correct, thereby generating preference pairs strictly compatible with its internal distribution. Extensive experiments demonstrate that AVES-DPO surpasses existing baselines in hallucination mitigation while requiring only 5.2k samples.

摘要：大型視覺語言模型（LVLMs）經常遭遇幻覺問題。現有的基於偏好學習的方法主要依賴專有模型來構建偏好數據集。我們發現這種依賴引入了專有模型與目標模型之間的分佈不匹配，這妨礙了有效的對齊。為了解決這個問題，我們提出了通過經過驗證的自我修正DPO（AVES-DPO）的對齊框架，該框架利用從模型內在知識中派生的內部數據來對齊LVLMs。我們的方法採用基於共識的驗證機制來診斷多樣的幻覺，並指導模型自我修正，從而生成與其內部分佈嚴格兼容的偏好對。大量實驗表明，AVES-DPO在減輕幻覺方面超越了現有基準，同時僅需5.2k樣本。

##### **Structural Pruning of Large Vision Language Models: A Comprehensive Study on Pruning Dynamics, Recovery, and Data Efficiency**
2604.24380v1 by Yiran Huang, Lukas Thede, Massimiliano Mancini, Wenjia Xu, Zeynep Akata

While Large Vision Language Models (LVLMs) demonstrate impressive capabilities, their substantial computational and memory requirements pose deployment challenges on resource-constrained edge devices. Current parameter reduction techniques primarily involve training LVLMs from small language models, but these methods offer limited flexibility and remain computationally intensive. We study a complementary route: compressing existing LVLMs by applying structured pruning to the language model backbone, followed by lightweight recovery training. Specifically, we investigate two structural pruning paradigms: layerwise and widthwise pruning, and pair them with supervised finetuning and knowledge distillation on logits and hidden states. Additionally, we assess the feasibility of conducting recovery training with only a small fraction of the available data. Our results show that widthwise pruning generally maintains better performance in low-resource scenarios, where computational resources are limited or there is insufficient finetuning data. As for the recovery training, finetuning only the multimodal projector is sufficient at small compression levels. Furthermore, a combination of supervised finetuning and hidden-state distillation yields optimal recovery across various pruning levels. Notably, effective recovery can be achieved using just 5% of the original data, while retaining over 95% of the original performance. Through empirical study on three representative LVLM families ranging from 3B to 7B parameters, this study offers actionable insights for practitioners to compress LVLMs without extensive computation resources or sufficient data. The code base is available at https://github.com/YiranHuangIrene/VLMCompression.git.

摘要：雖然大型視覺語言模型（LVLMs）展現了令人印象深刻的能力，但其龐大的計算和記憶體需求對資源受限的邊緣設備造成了部署挑戰。當前的參數減少技術主要涉及從小型語言模型訓練LVLMs，但這些方法提供的靈活性有限，並且仍然計算密集。我們研究了一條互補的路徑：通過對語言模型主幹應用結構性剪枝來壓縮現有的LVLMs，然後進行輕量級的恢復訓練。具體而言，我們調查了兩種結構性剪枝範式：逐層剪枝和寬度剪枝，並將其與有監督的微調和對logits及隱藏狀態的知識蒸餾相結合。此外，我們評估了僅用少量可用數據進行恢復訓練的可行性。我們的結果顯示，在計算資源有限或微調數據不足的低資源場景中，寬度剪枝通常能保持更好的性能。至於恢復訓練，僅微調多模態投影器在小壓縮水平下就足夠了。此外，結合有監督的微調和隱藏狀態蒸餾在各種剪枝水平下產生最佳的恢復。值得注意的是，僅使用5％的原始數據就可以實現有效的恢復，同時保留超過95％的原始性能。通過對三個代表性的LVLM家族進行實證研究，這些家族的參數範圍從3B到7B，本研究為實踐者提供了在不需要大量計算資源或足夠數據的情況下壓縮LVLMs的可行見解。代碼庫可在 https://github.com/YiranHuangIrene/VLMCompression.git 獲得。

##### **SeaEvo: Advancing Algorithm Discovery with Strategy Space Evolution**
2604.24372v1 by Sichun Luo, Yi Huang, Haochen Luo, Fengyuan Liu, Guanzhi Deng, Lei Li, Qinghua Yao, Zefa Hu, Junlan Feng, Qi Liu

LLM-guided evolutionary search has emerged as a promising paradigm for automated algorithm discovery, yet most systems track search progress primarily through executable programs and scalar fitness. Even when natural-language reflection is used, it is often used locally in mutation prompts or stored without an explicit population-level organization of strategic directions. As a result, evolutionary search can struggle to distinguish syntactically different implementations of the same idea, preserve lower-fitness but strategically promising directions, or detect when an entire family of strategies has saturated.
  We introduce \model, a modular strategy-space layer that elevates natural-language strategy descriptions from transient prompt context to first-class population-level evolutionary state in LLM-driven program search. \model augments each candidate program with an explicit natural language strategy description and uses this representation in three ways: Strategy Articulation turns mutation into a diagnose-direct-implement process; Stratified Experience Retrieval organizes the archive into strategy clusters and selects inspirations by behavioral complementarity; and Strategic Landscape Navigation periodically summarizes effective, saturated, and underexplored strategy families to guide future mutations. Across mathematical algorithm discovery, systems optimization, and agent-scaffold benchmarks, \model improves the underlying evolutionary backbones in most settings, with particularly large gains (21% relative improvement) on open-ended system optimization tasks. These results suggest that persistent strategy representations provide a practical mechanism for improving the robustness and efficiency of LLM-guided evolutionary search, suggesting a path toward compound AI systems that accumulate algorithmic knowledge over time.

摘要：LLM 指導的進化搜索已成為自動化算法發現的一個有前景的範式，但大多數系統主要通過可執行程序和標量適應度來跟踪搜索進展。即使使用自然語言反思，它通常是在變異提示中局部使用，或在沒有明確的群體層級戰略方向組織的情況下儲存。結果，進化搜索可能難以區分同一思想的語法上不同的實現，保留適應度較低但在戰略上有前景的方向，或檢測整個策略家族是否已經飽和。  
我們介紹 \model，一個模組化的策略空間層，將自然語言策略描述從瞬時提示上下文提升到 LLM 驅動程序搜索中的一級群體層級進化狀態。 \model 為每個候選程序增強了一個明確的自然語言策略描述，並以三種方式使用這一表示：策略闡明將變異轉化為診斷-指導-實施過程；分層經驗檢索將檔案組織成策略集群，並通過行為互補性選擇靈感；戰略景觀導航定期總結有效、飽和和未充分探索的策略家族，以指導未來的變異。在數學算法發現、系統優化和代理-支架基準測試中，\model 在大多數設置中改善了基礎的進化骨架，在開放式系統優化任務中獲得了特別大的增益（相對改善 21%）。這些結果表明，持久的策略表示提供了一種實用機制，以提高 LLM 指導的進化搜索的穩健性和效率，暗示了一條通向隨時間積累算法知識的複合 AI 系統的道路。

##### **Culture-Aware Machine Translation in Large Language Models: Benchmarking and Investigation**
2604.24361v1 by Zekun Yuan, Yangfan Ye, Xiaocheng Feng, Baohang Li, Qichen Hong, Yunfei Lu, Dandan Tu, Bing Qin

Large language models (LLMs) have achieved strong performance in general machine translation, yet their ability in culture-aware scenarios remains poorly understood. To bridge this gap, we introduce CanMT, a Culture-Aware Novel-Driven Parallel Dataset for Machine Translation, together with a theoretically grounded, multi-dimensional evaluation framework for assessing cultural translation quality. Leveraging CanMT, we systematically evaluate a wide range of LLMs and translation systems under different translation strategy constraints. Our findings reveal substantial performance disparities across models and demonstrate that translation strategies exert a systematic influence on model behavior. Further analysis shows that translation difficulty varies across types of culture-specific items, and that a persistent gap remains between models' recognition of culture-specific knowledge and their ability to correctly operationalize it in translation outputs. In addition, incorporating reference translations is shown to substantially improve evaluation reliability in LLM-as-a-judge, underscoring their essential role in assessing culture-aware translation quality. The corpus and code are available at CanMT.

摘要：大型語言模型（LLMs）在一般機器翻譯中已取得強勁的表現，但它們在文化意識場景中的能力仍然不甚了解。為了填補這一空白，我們引入了CanMT，一個文化意識驅動的機器翻譯平行數據集，並提出了一個理論基礎的多維評估框架，用於評估文化翻譯質量。利用CanMT，我們在不同的翻譯策略約束下，系統地評估了各種LLMs和翻譯系統。我們的研究結果揭示了模型之間存在顯著的性能差異，並表明翻譯策略對模型行為產生系統性影響。進一步分析顯示，翻譯難度在文化特定項目的類型之間有所不同，且模型對文化特定知識的識別能力與其在翻譯輸出中正確運用該知識的能力之間仍存在持續的差距。此外，納入參考翻譯顯著提高了LLM作為評判者的評估可靠性，強調了它們在評估文化意識翻譯質量中的重要角色。語料庫和代碼可在CanMT獲得。

##### **Generative Design of a Gas Turbine Combustor Using Invertible Neural Networks**
2604.24322v1 by Patrick Krüger, Hanno Gottschalk, Werner Krebs, Bastian Werdelmann

The need to burn 100% H2 in high efficient gas turbines featuring low NOx combustion in premix mode require the complete redesign of the combustion system to ensure stable operation without any flashback. Since all engine frames featuring a power range from 4 MW up to 600 MW are affected, a huge design effort is expected. To reduce this effort, especially to transfer knowledge between the different engine classes, generative design methods using latest AI technology will provide promising potential. In this work, this challenge is approached utilizing the current advances in generative artificial intelligence. We train an Invertible Neural Network (INN) on an expandable database of geometrically parameterized combustor designs with simulated performance labels. Utilizing the INN in its inverse direction, multiple design proposals are generated which fulfill specified performance labels.

摘要：需要在高效燃氣渦輪機中以低 NOx 燃燒的預混模式燃燒 100% H2，這要求對燃燒系統進行全面重新設計，以確保穩定運行而不會有任何回火。由於所有功率範圍從 4 MW 到 600 MW 的引擎框架均受到影響，因此預期會有巨大的設計努力。為了減少這一努力，特別是為了在不同的引擎類別之間轉移知識，利用最新 AI 技術的生成設計方法將提供有希望的潛力。在這項工作中，這一挑戰是利用當前生成人工智能的進展來解決的。我們在一個可擴展的幾何參數化燃燒器設計數據庫上訓練了一個可逆神經網絡（INN），並附有模擬性能標籤。利用 INN 的逆向，生成多個滿足指定性能標籤的設計提案。

##### **Latent-Hysteresis Graph ODEs: Modeling Coupled Topology-Feature Evolution via Continuous Phase Transitions**
2604.24293v1 by Qinhan Hou, Jing Tang

Graph neural ordinary differential equations (Graph ODEs) extend graph learning from discrete message-passing layers to continuous-time representation flows. While it supports adaptive long-range propagation, we show that Graph ODEs with strictly positive irreducible mixing operators face an inherent \emph{monostability trap}: in the long-time regime, information leakage is unavoidable and the dynamics converge to a single global consensus attractor. We propose the \textbf{Hysteresis Graph ODE (HGODE)}, which couples feature evolution with a latent topological potential driven by a learned pairwise force. A double-well edge potential and bipolarized gate allow edge states to polarize into connected or insulated phases while preserving differentiability. We provide asymptotic analysis of the collapse mechanism and the proposed hysteretic topology dynamics, and validate HGODE on theory-driven synthetic diagnostics and real-world graph benchmarks.

摘要：圖神經常微分方程（Graph ODEs）將圖學習從離散的訊息傳遞層擴展到連續時間的表示流。<lb/>雖然它支持自適應的長距離傳播，但我們顯示具有嚴格正的不可約混合運算子的 Graph ODEs 面臨固有的 \emph{單穩態陷阱}：在長時間範圍內，信息泄漏是不可避免的，動態收斂到單一的全局共識吸引子。<lb/>我們提出了 \textbf{滯後圖神經常微分方程（HGODE）}，它將特徵演化與由學習的成對力驅動的潛在拓撲勢能耦合。<lb/>雙井邊緣勢能和雙極化閘允許邊緣狀態極化為連接或絕緣相，同時保持可微性。<lb/>我們提供了崩潰機制和提出的滯後拓撲動力學的漸進分析，並在理論驅動的合成診斷和現實世界的圖基準上驗證 HGODE。

##### **MEMCoder: Multi-dimensional Evolving Memory for Private-Library-Oriented Code Generation**
2604.24222v1 by Mofei Li, Taozhi Chen, Guowei Yang, Jia Li

Large Language Models (LLMs) excel at general code generation, but their performance drops sharply in enterprise settings that rely on internal private libraries absent from public pre-training corpora. While Retrieval-Augmented Generation (RAG) offers a training-free alternative by providing static API documentation, we find that such documentation typically provides only isolated definitions, leaving a fundamental knowledge gap. Specifically, LLMs struggle with a task-level lack of coordination patterns between APIs and an API-level misunderstanding of parameter constraints and boundary conditions. To address this, we propose MEMCoder, a novel framework that enables LLMs to autonomously accumulate and evolve Usage Guidelines across these two dimensions. MEMCoder introduces a Multi-dimensional Evolving Memory that captures distilled lessons from the model's own problem-solving trajectories. During inference, MEMCoder employs a dual-source retrieval mechanism to inject both static documentation and relevant historical guidelines into the context. The framework operates in an automated closed loop by using objective execution feedback to reflect on successes and failures, resolve knowledge conflicts, and dynamically update memory. Extensive evaluations on the NdonnxEval and NumbaEval benchmarks demonstrate that MEMCoder substantially enhances existing RAG systems, yielding an average absolute pass@1 gain of 16.31%. Furthermore, MEMCoder exhibits vastly superior domain-specific adaptation compared to existing memory-based continual learning methods.

摘要：大型語言模型（LLMs）在一般代碼生成方面表現優異，但在依賴於內部私有庫的企業環境中，其性能急劇下降，這些私有庫在公共預訓練語料中並不存在。雖然檢索增強生成（RAG）通過提供靜態API文檔提供了一種無需訓練的替代方案，但我們發現這種文檔通常僅提供孤立的定義，留下了基本的知識空白。具體而言，LLMs在API之間缺乏協調模式的任務級別上存在困難，並且在API級別上對參數約束和邊界條件存在誤解。為了解決這個問題，我們提出了MEMCoder，一個新穎的框架，使LLMs能夠自主積累和演化這兩個維度的使用指南。MEMCoder引入了一種多維度演變記憶，捕捉模型自身問題解決軌跡中的提煉教訓。在推理過程中，MEMCoder採用雙來源檢索機制，將靜態文檔和相關歷史指南注入上下文中。該框架通過使用客觀執行反饋在自動閉環中運作，反思成功與失敗，解決知識衝突，並動態更新記憶。在NdonnxEval和NumbaEval基準上的廣泛評估表明，MEMCoder顯著增強了現有的RAG系統，平均絕對pass@1增益達到16.31%。此外，與現有的基於記憶的持續學習方法相比，MEMCoder顯示出極其優越的領域特定適應性。

##### **Time-varying Interaction Graph ODE for Dynamic Graph Representation Learning**
2604.24811v1 by Xiaoyi Wang, Zhiqiang Wang, Jianqing Liang, Xingwang Zhao, Chuangyin Dang, Zhen Jin, Jiye Liang

Graph neural Ordinary Differential Equations (ODE) combine neural ODE with the message passing mechanism of Graph Neural Networks (GNN), providing a continuous-time modeling method for graph representation learning. However, in dynamic graph scenarios, existing graph neural ODEs typically employ a unified message passing mechanism, assuming that inter-node interactions share the same message passing function at any time, which makes it challenging to capture the diversity and time-varying nature of inter-node interaction patterns. To address this, we propose Time-varying Interaction Graph Ordinary Differential Equations (TI-ODE). The core idea of TI-ODE is to decompose the evolution function of a graph ODE into a set of learnable interaction basis functions, where each basis function corresponds to a distinct type of inter-node interaction. These basis functions are dynamically combined through time-dependent learnable weights, enabling inter-node interaction patterns to adaptively evolve over time. Experimental results on six dynamic graph datasets demonstrate that TI-ODE consistently outperforms existing methods and achieves state-of-the-art performance on attribute prediction tasks, and experiments on the \textit{Covid} dataset further verify the interpretability and generalizability of our TI-ODE. Furthermore, we demonstrate both theoretically and empirically that TI-ODE exhibits superior robustness compared to models utilizing a unified message-passing mechanism.

摘要：圖神經常微分方程（ODE）結合了神經ODE與圖神經網絡（GNN）的信息傳遞機制，提供了一種連續時間建模方法來進行圖表示學習。<lb/>然而，在動態圖場景中，現有的圖神經ODE通常採用統一的信息傳遞機制，假設節點間的交互在任何時刻都共享相同的信息傳遞函數，這使得捕捉節點間交互模式的多樣性和隨時間變化的特性變得具有挑戰性。<lb/>為了解決這個問題，我們提出了時間變化交互圖常微分方程（TI-ODE）。<lb/>TI-ODE的核心思想是將圖ODE的演化函數分解為一組可學習的交互基函數，其中每個基函數對應於一種不同類型的節點間交互。<lb/>這些基函數通過時間依賴的可學習權重進行動態組合，使得節點間的交互模式能夠隨時間自適應地演變。<lb/>在六個動態圖數據集上的實驗結果表明，TI-ODE始終超越現有方法，並在屬性預測任務上實現了最先進的性能，而在\textit{Covid}數據集上的實驗進一步驗證了我們的TI-ODE的可解釋性和通用性。<lb/>此外，我們從理論和實證上證明，TI-ODE相比於採用統一信息傳遞機制的模型展現出更優越的穩健性。

##### **Agentic Witnessing: Pragmatic and Scalable TEE-Enabled Privacy-Preserving Auditing**
2604.24203v1 by Antony Rowstron

Auditing the semantic properties of proprietary data creates a fundamental tension: verification requires transparent access, while proprietary rights demand confidentiality. While Zero-Knowledge Proofs (ZKPs) ensure privacy, they are typically limited to precise algebraic constraints and are ill-suited for verifying qualitative, unstructured properties, such as the logic within a codebase. We propose {\em Agentic Witnessing}, a framework that moves verification from attested execution to {\em attested reasoning}. The system is composed of three agents: a Verifier (who wants to check properties of a dataset), a Prover (who owns the dataset) and an Auditor (that inspects the dataset). The Verifier is allowed to ask a limited number of simple binary true/false questions to the auditor. By isolating an LLM-based Auditor within a Trusted Execution Environment (TEE), the system enables the Verifier to query a Prover's private data via simple Boolean queries, without exposing the raw dataset. The Auditor uses the Model Context Protocol (MCP) to dynamically inspect the target dataset, producing a yes/no verdict accompanied by a cryptographic transcript: a signed hash chain binding the reasoning trace to both the original dataset and the TEE's hardware root of trust. We demonstrate this architecture by automating the artifact evaluation process for 21 peer-reviewed computer science papers with released codebases on GitHub (e.g. Does the codebase implement the system described in the paper?). We verified five high-level properties of these codebases described in the corresponding publications, treating the source code as private. Our results show that TEE-enabled agentic auditing provides a mechanism for privacy-preserving oversight, effectively decoupling qualitative verification from the need for data disclosure.

摘要：審計專有數據的語義特性創造了一種根本的緊張關係：驗證需要透明的訪問，而專有權利則要求保密。雖然零知識證明（ZKP）確保了隱私，但它們通常僅限於精確的代數約束，並不適合驗證質性、非結構化的特性，例如代碼庫中的邏輯。我們提出了 {\em Agentic Witnessing}，一個將驗證從已證實的執行轉移到 {\em 已證實的推理} 的框架。該系統由三個代理組成：驗證者（希望檢查數據集的特性）、證明者（擁有數據集的人）和審計者（檢查數據集的人）。驗證者被允許向審計者提出有限數量的簡單二元真/假問題。通過將基於大型語言模型（LLM）的審計者隔離在受信執行環境（TEE）內，該系統使驗證者能夠通過簡單的布爾查詢查詢證明者的私有數據，而無需暴露原始數據集。審計者使用模型上下文協議（MCP）動態檢查目標數據集，生成一個附有加密記錄的是/否判決：一個簽名哈希鏈，將推理痕跡綁定到原始數據集和TEE的硬件信任根。我們通過自動化21篇在GitHub上發布代碼庫的同行評審計算機科學論文的文物評估過程來演示這一架構（例如，代碼庫是否實現了論文中描述的系統？）。我們驗證了這些代碼庫中對應出版物所描述的五個高級特性，將源代碼視為私有。我們的結果顯示，啟用TEE的代理審計提供了一種保護隱私的監督機制，有效地將質性驗證與數據披露的需求解耦。

##### **Rewarding the Scientific Process: Process-Level Reward Modeling for Agentic Data Analysis**
2604.24198v1 by Zhisong Qiu, Shuofei Qiao, Kewei Xu, Yuqi Zhu, Lun Du, Ningyu Zhang, Huajun Chen

Process Reward Models (PRMs) have achieved remarkable success in augmenting the reasoning capabilities of Large Language Models (LLMs) within static domains such as mathematics. However, their potential in dynamic data analysis tasks remains underexplored. In this work, we first present a empirical study revealing that general-domain PRMs struggle to supervise data analysis agents. Specifically, they fail to detect silent errors, logical flaws that yield incorrect results without triggering interpreter exceptions, and erroneously penalize exploratory actions, mistaking necessary trial-and-error exploration for grounding failures. To bridge this gap, we introduce DataPRM, a novel environment-aware generative process reward model that (1) can serve as an active verifier, autonomously interacting with the environment to probe intermediate execution states and uncover silent errors, and (2) employs a reflection-aware ternary reward strategy that distinguishes between correctable grounding errors and irrecoverable mistakes. We design a scalable pipeline to construct over 8K high-quality training instances for DataPRM via diversity-driven trajectory generation and knowledge-augmented step-level annotation. Experimental results demonstrate that DataPRM improves downstream policy LLMs by 7.21% on ScienceAgentBench and 11.28% on DABStep using Best-of-N inference. Notably, with only 4B parameters, DataPRM outperforms strong baselines, and exhibits robust generalizability across diverse Test-Time Scaling strategies. Furthermore, integrating DataPRM into Reinforcement Learning yields substantial gains over outcome-reward baselines, achieving 78.73% on DABench and 64.84% on TableBench, validating the effectiveness of process reward supervision. Code is available at https://github.com/zjunlp/DataMind.

摘要：過程獎勵模型（PRMs）在增強大型語言模型（LLMs）在靜態領域如數學的推理能力方面取得了顯著成功。<lb/>然而，它們在動態數據分析任務中的潛力仍然未被充分探索。<lb/>在本研究中，我們首先呈現了一項實證研究，揭示了通用領域的PRMs在監督數據分析代理時的困難。<lb/>具體而言，它們無法檢測靜默錯誤，即那些在未觸發解釋器異常的情況下產生不正確結果的邏輯缺陷，並錯誤地懲罰探索行為，將必要的試錯探索誤認為是基礎失敗。<lb/>為了填補這一空白，我們引入了DataPRM，一種新穎的環境感知生成過程獎勵模型，該模型（1）可以作為一個主動驗證者，自主與環境互動以探查中間執行狀態並揭示靜默錯誤，並且（2）採用反思感知的三元獎勵策略，以區分可修正的基礎錯誤和不可恢復的錯誤。<lb/>我們設計了一個可擴展的管道，通過多樣性驅動的軌跡生成和知識增強的步驟級註釋，為DataPRM構建超過8K的高質量訓練實例。<lb/>實驗結果表明，DataPRM在ScienceAgentBench上提高了下游策略LLMs 7.21%，在DABStep上提高了11.28%，使用最佳推理方法。<lb/>值得注意的是，DataPRM僅用4B參數便超越了強基準，並在多樣的測試時間擴展策略中展現出強大的泛化能力。<lb/>此外，將DataPRM整合到強化學習中，對結果獎勵基準產生了顯著的增益，在DABench上達到78.73%，在TableBench上達到64.84%，驗證了過程獎勵監督的有效性。<lb/>代碼可在 https://github.com/zjunlp/DataMind 獲得。

##### **MultiDx: A Multi-Source Knowledge Integration Framework towards Diagnostic Reasoning**
2604.24186v1 by Yimin Deng, Zhenxi Lin, Yejing Wang, Guoshuai Zhao, Pengyue Jia, Zichuan Fu, Derong Xu, Yefeng Zheng, Xiangyu Zhao, Li Zhu, Xian Wu, Xueming Qian

Diagnostic prediction and clinical reasoning are critical tasks in healthcare applications. While Large Language Models (LLMs) have shown strong capabilities in commonsense reasoning, they still struggle with diagnostic reasoning due to limited domain knowledge. Existing approaches often rely on internal model knowledge or static knowledge bases, resulting in knowledge insufficiency and limited adaptability, which hinder their capacity to perform diagnostic reasoning. Moreover, these methods focus solely on the accuracy of final predictions, overlooking alignment with standard clinical reasoning trajectories. To this end, we propose MultiDx, a two-stage diagnostic reasoning framework that performs differential diagnosis by analyzing evidence collected from multiple knowledge sources. Specifically, it first generates suspected diagnoses and reasoning paths by leveraging knowledge from web search, SOAP-formatted case, and clinical case database. Then it integrates multi-perspective evidence through matching, voting, and differential diagnosis to generate the final prediction.~Extensive experiments on two public benchmarks demonstrate the effectiveness of our approach.

摘要：診斷預測和臨床推理是醫療應用中的關鍵任務。<lb/>雖然大型語言模型（LLMs）在常識推理方面展現出強大的能力，但由於領域知識有限，它們在診斷推理上仍然面臨挑戰。<lb/>現有的方法通常依賴於內部模型知識或靜態知識庫，導致知識不足和適應性有限，這妨礙了它們進行診斷推理的能力。<lb/>此外，這些方法僅專注於最終預測的準確性，忽略了與標準臨床推理路徑的一致性。<lb/>為此，我們提出了MultiDx，一個兩階段的診斷推理框架，通過分析來自多個知識來源的證據來執行鑑別診斷。<lb/>具體而言，它首先通過利用來自網絡搜索、SOAP格式案例和臨床案例數據庫的知識生成懷疑診斷和推理路徑。<lb/>然後，它通過匹配、投票和鑑別診斷整合多角度證據，以生成最終預測。<lb/>在兩個公共基準上的廣泛實驗證明了我們方法的有效性。

##### **AdapTime: Enabling Adaptive Temporal Reasoning in Large Language Models**
2604.24175v1 by Yimin Deng, Yejing Wang, Zhenxi Lin, Zichuan Fu, Guoshuai Zhao, Derong Xu, Yefeng Zheng, Xiangyu Zhao, Xian Wu, Li Zhu, Xueming Qian

Large language models have demonstrated strong reasoning capabilities in general knowledge question answering. However, their ability to handle temporal information remains limited. To address this limitation, existing approaches often involve external tools or manual verification and are tailored to specific scenarios, leading to poor generalizability. Moreover, these methods apply a fixed pipeline to all questions, overlooking the fact that different types of temporal questions require distinct reasoning strategies, which leads to unnecessary processing for simple cases and inadequate reasoning for complex ones. To this end, we propose AdapTime, an adaptive temporal reasoning method that dynamically executes reasoning steps based on the input context. Specifically, it involves three temporal reasoning actions: reformulate, rewrite and review, with an LLM planner guiding the reasoning process. AdapTime integrates seamlessly with state-of-the-art LLMs and significantly enhances their temporal reasoning capabilities without relying on external support. Extensive experiments demonstrate the effectiveness of our approach.

摘要：大型語言模型在一般知識問答中展現了強大的推理能力。<lb/>然而，它們處理時間信息的能力仍然有限。<lb/>為了解決這一限制，現有的方法通常涉及外部工具或手動驗證，並針對特定場景進行調整，導致其泛化能力較差。<lb/>此外，這些方法對所有問題應用固定的流程，忽略了不同類型的時間問題需要不同的推理策略，這導致對簡單案例的處理過於繁瑣，而對複雜案例的推理則不夠充分。<lb/>為此，我們提出了AdapTime，一種自適應的時間推理方法，根據輸入上下文動態執行推理步驟。<lb/>具體而言，它涉及三個時間推理行動：重新表述、重寫和回顧，並由LLM規劃者指導推理過程。<lb/>AdapTime與最先進的LLM無縫集成，顯著增強了它們的時間推理能力，而無需依賴外部支持。<lb/>大量實驗證明了我們方法的有效性。

##### **Psychologically-Grounded Graph Modeling for Interpretable Depression Detection**
2604.24126v1 by Rishitej Reddy Vyalla, Kritarth Prasad, Avinash Anand, Erik Cambria, Shaoxiong Ji, Faten S. Alamri, Zhengkui Wang

Automatic depression detection from conversational interactions holds significant promise for scalable screening but remains hindered by severe data scarcity and a lack of clinical interpretability. Existing approaches typically rely on black-box deep learning architectures that struggle to model the subtle, temporal evolution of depressive symptoms or account for participant-specific heterogeneity. In this work, we propose PsyGAT (Psychological Graph Attention Network), a psychologically grounded framework that models conversational sessions as dynamic temporal graphs. We introduce Psychological Expression Units (PEUs) to explicitly encode utterance-level clinical evidence, structuring the session graph to capture transitions in psychological states rather than mere semantic dependencies. To address the critical class imbalance in depression datasets, we employ clinically approved persona-based data augmentation, enable robust model learning. Additionally, we integrate session-level personality context directly into the graph structure to disentangle trait-based behavior from acute depressive symptoms. PsyGAT achieves state-of-the-art performance, surpassing both strong graph-based baselines and closed-source LLMs like GPT-5, achieving 89.99 and 71.37 Macro F1 scores in DAIC-WoZ and E-DAIC, respectively. We further introduce Causal-PsyGAT, an interpretability module that identifies symptom triggers. Experiments show a 20% improvement in MRR for identifying causal indicators, effectively bridging the gap between depression monitoring and clinical explainability. The full augmented dataset is publicly available at https://doi.org/10.6084/m9.figshare.31801921.

摘要：自動化的抑鬱症檢測從對話互動中具有顯著的可擴展篩選潛力，但仍受到嚴重數據稀缺和缺乏臨床可解釋性的限制。現有的方法通常依賴於黑箱深度學習架構，這些架構難以建模抑鬱症狀的微妙、時間演變或考慮參與者特定的異質性。在這項工作中，我們提出了PsyGAT（心理圖注意力網絡），這是一個基於心理學的框架，將對話會話建模為動態時間圖。我們引入了心理表達單元（PEUs），以明確編碼話語級別的臨床證據，結構化會話圖以捕捉心理狀態的轉變，而不僅僅是語義依賴。為了解決抑鬱數據集中的關鍵類別不平衡問題，我們採用了臨床批准的基於角色的人物數據增強，從而實現穩健的模型學習。此外，我們將會話級別的人格背景直接整合到圖結構中，以區分基於特徵的行為和急性抑鬱症狀。PsyGAT實現了最先進的性能，超越了強大的基於圖的基準和像GPT-5這樣的封閉源LLM，在DAIC-WoZ和E-DAIC中分別達到89.99和71.37的宏觀F1分數。我們進一步介紹了Causal-PsyGAT，一個可解釋性模塊，用於識別症狀觸發因素。實驗顯示識別因果指標的MRR提高了20%，有效地縮小了抑鬱監測與臨床可解釋性之間的差距。完整的增強數據集可在https://doi.org/10.6084/m9.figshare.31801921公開獲取。

##### **From Prototype to Classroom: An Intelligent Tutoring System for Quantum Education**
2604.24807v1 by Iizalaarab Elhaimeur, Nikos Chrisochoides

Quantum computing instructors face a compounding problem: the concepts are counterintuitive, the mathematical formalism is dense, and qualified faculty are scarce outside a small number of well-resourced institutions. Our prior work introduced a knowledge-graph-augmented tutoring prototype with two specialized LLM agents: a Teaching Agent for dynamic interaction and a Lesson Planning Agent for lesson generation. Validated on simulated runs rather than in a real course, that prototype left open whether more aggressive agent specialization would be needed to handle the full range of quantum education tasks under real student load. This paper answers the three questions that the prototype could not answer. Can agent specialization solve the reliability problem in a domain as technically demanding as quantum information science? Can the system run in a real course, not a demonstration? Does the instructor gain actionable intelligence from the deployment? We present ITAS (Intelligent Teaching Assistant System), a multi-agent tutoring system built around four contributions: a five-module QIS curriculum grounded in Watrous's information-first framework, a Spoke-and-Wheel teaching architecture with quantum-specialized agents, a cloud infrastructure designed for production use and regulatory compliance, and a conversational analytics layer for instructors and content developers. Piloted in a quantum computing course at Old Dominion University, the system supports all three answers: deployment evidence is consistent with specialization addressing the task-boundary failures observed in the prototype, cloud infrastructure supports classroom-scale concurrency at sub-textbook cost, and the analytics agent surfaces curriculum gaps the instructor could not otherwise see.

摘要：量子計算的教師面臨一個複雜的問題：這些概念違反直覺，數學形式主義密集，並且合格的教職員在少數資源豐富的機構之外相當稀缺。我們之前的工作介紹了一個知識圖譜增強的輔導原型，該原型有兩個專門的 LLM 代理：一個用於動態互動的教學代理和一個用於課程生成的課程規劃代理。該原型在模擬運行中進行驗證，而不是在真實課程中，這讓人懷疑是否需要更積極的代理專業化來處理在真實學生負載下的全範圍量子教育任務。本文回答了該原型無法回答的三個問題。代理專業化能否解決在量子信息科學這樣技術要求高的領域中的可靠性問題？系統能否在真實課程中運行，而不是演示？教師能否從部署中獲得可行的情報？我們提出了 ITAS（智能教學助手系統），這是一個多代理輔導系統，圍繞四個貢獻構建：基於 Watrous 的信息優先框架的五模塊 QIS 課程，一個具有量子專門代理的輻射式教學架構，設計用於生產使用和合規的雲基礎設施，以及為教師和內容開發者提供的對話分析層。該系統在老道明大學的量子計算課程中進行了試點，支持所有三個答案：部署證據與專業化解決原型中觀察到的任務邊界失敗一致，雲基礎設施支持課堂規模的並發運行，成本低於教科書，並且分析代理揭示了教師無法看到的課程空白。

##### **Factual and Edit-Sensitive Graph-to-Sequence Generation via Graph-Aware Adaptive Noising**
2604.24104v1 by Aditya Hemant Shahane, Anuj Kumar Sirohi, Tanmoy Chakraborty, Prathosh A P, Sandeep Kumar

Fine-tuned autoregressive models for graph-to-sequence generation (G2S) often struggle with factual grounding and edit sensitivity. To tackle these issues, we propose a non-autoregressive diffusion framework that generates text by iterative refinement conditioned on an input graph, named as Diffusion Language Model for Graphs (DLM4G). By aligning graph components (entities/relations) with their corresponding sequence tokens, DLM4G employs an adaptive noising strategy. The proposed strategy uses per-token denoising error as a signal to adaptively modulate noise on entity and relation tokens, improving preservation of graph structure and enabling localized updates under graph edits. Evaluated on three datasets, DLM4G consistently outperforms competitive G2S diffusion baselines trained on identical splits across both surface-form and embedding-based metrics. DLM4G further exceeds fine-tuned autoregressive baselines up to 12x larger (e.g., T5-Large) and is competitive with zero-shot LLM transfer baselines up to 127x larger. Relative to the strongest fine-tuned PLM baseline, DLM4G improves factual grounding (FGT@0.5) by +5.16% and edit sensitivity (ESR) by +7.9%; compared to the best diffusion baseline, it yields gains of +3.75% in FGT@0.5 and +23.6% in ESR. We additionally demonstrate applicability beyond textual graphs through experiments on molecule captioning, indicating the method's generality for scientific G2S generation.

摘要：微調的自回歸模型在圖到序列生成（G2S）中常常面臨事實基礎和編輯敏感性問題。為了解決這些問題，我們提出了一種非自回歸擴散框架，通過基於輸入圖的迭代精煉來生成文本，稱為圖的擴散語言模型（DLM4G）。DLM4G通過將圖元件（實體/關係）與其對應的序列標記對齊，採用了自適應噪聲策略。所提出的策略使用每個標記的去噪誤差作為信號，自適應地調節實體和關係標記上的噪聲，改善圖結構的保留，並在圖編輯下實現局部更新。在三個數據集上的評估中，DLM4G在表面形式和基於嵌入的指標上，始終超越在相同分割上訓練的競爭性G2S擴散基準。DLM4G進一步超越了微調的自回歸基準，後者的規模大達12倍（例如，T5-Large），並且在零樣本LLM轉移基準中與規模達127倍的模型具有競爭力。相對於最強的微調PLM基準，DLM4G在事實基礎（FGT@0.5）上提高了+5.16%，在編輯敏感性（ESR）上提高了+7.9%；與最佳擴散基準相比，它在FGT@0.5上獲得了+3.75%的增益，在ESR上獲得了+23.6%的增益。我們還通過分子標題生成的實驗展示了該方法在文本圖之外的適用性，表明該方法對於科學G2S生成的普遍性。

##### **The Pragmatic Persona: Discovering LLM Persona through Bridging Inference**
2604.24079v1 by Jisoo Yang, Jongwon Ryu, Minuk Ma, Trung X. Pham, Junyeong Kim

Large Language Models (LLMs) reveal inherent and distinctive personas through dialogue. However, most existing persona discovery approaches rely on surface-level lexical or stylistic cues, treating dialogue as a flat sequence of tokens and failing to capture the deeper discourse-level structures that sustain persona consistency. To address this limitation, we propose a novel analytical framework that interprets LLM dialogue through bridging inference -- implicit conceptual relations that connect utterances via shared world knowledge and discourse coherence. By modeling these relations as structured knowledge graphs, our approach captures latent semantic links that govern how LLMs organize meaning across turns, enabling persona discovery at the level of discourse coherence rather than surface realizations. Experimental results across multiple reasoning backbones and target LLMs, ranging from small-scale models to 80B-parameter systems, demonstrate that bridging-inference graphs yield significantly stronger semantic coherence and more stable persona identification than frequency or style-based baselines. These results show that persona traits are consistently encoded in the structural organization of discourse rather than isolated lexical patterns. This work presents a systematic framework for probing, extracting, and visualizing latent LLM personas through the lens of Cognitive Discourse Theory, bridging computational linguistics, cognitive semantics, and persona reasoning in large language models. Codes are available at https://github.com/JiSoo-Yang/Persona_Bridging.git

摘要：大型語言模型（LLMs）透過對話揭示出固有且獨特的人格特質。<lb/>然而，大多數現有的人格發現方法依賴於表面層的詞彙或風格線索，將對話視為一個平坦的符號序列，未能捕捉到維持人格一致性的更深層對話結構。<lb/>為了解決這一限制，我們提出了一個新穎的分析框架，通過橋接推理來解釋LLM對話——這是通過共享的世界知識和對話連貫性來連接發言的隱含概念關係。<lb/>通過將這些關係建模為結構化知識圖，我們的方法捕捉到潛在的語義聯繫，這些聯繫支配著LLMs如何在輪次之間組織意義，使人格發現能夠在對話連貫性層面進行，而不是表面實現層面。<lb/>在多個推理骨幹和目標LLMs上進行的實驗結果，涵蓋從小型模型到80B參數系統，顯示橋接推理圖在語義連貫性和人格識別的穩定性上顯著優於基於頻率或風格的基準。<lb/>這些結果表明，人格特徵在對話的結構組織中持續編碼，而不是孤立的詞彙模式。<lb/>這項工作提出了一個系統化的框架，通過認知對話理論的視角來探測、提取和可視化潛在的LLM人格，橋接計算語言學、認知語義學和大型語言模型中的人格推理。<lb/>代碼可在 https://github.com/JiSoo-Yang/Persona_Bridging.git 獲得。

##### **Grounding Before Generalizing: How AI Differs from Humans in Causal Transfer**
2604.24062v1 by Liangru Xiang, Yuxi Ma, Zhihao Cao, Yixin Zhu, Song-Chun Zhu

Extracting abstract causal structures and applying them to novel situations is a hallmark of human intelligence. While Large Language Models (LLMs) and Vision Language Models (VLMs) have shown strong performance on a wide range of reasoning tasks, their capacity for interactive causal learning -- inducing latent structures through sequential exploration and transferring them across contexts -- remains uncharacterized. Human learners accomplish such transfer after minimal exposure, whereas classical Reinforcement Learning (RL) agents fail catastrophically. Whether state-of-the-art Artificial Intelligence (AI) models possess human-like mechanisms for abstract causal structure transfer is an open question. Using the OpenLock paradigm requiring sequential discovery of Common Cause (CC) and Common Effect (CE) structures, here we show that models exhibit fundamentally delayed or absent transfer: even successful models require initial environmental-specific mapping -- what we term environmental grounding -- before efficiency gains emerge, whereas humans leverage prior structural knowledge from the very first solution attempt. In the text-only condition, models matched or exceeded human discovery efficiency. In contrast, visual information -- in both the image-only and text-and-image conditions -- overall degraded rather than enhanced performance, revealing a broad reliance on symbolic processing rather than integrated multimodal reasoning. Models further exhibited systematic CC/CE asymmetries absent in humans, suggesting heuristic biases rather than direction-neutral causal abstraction. These findings reveal that large-scale statistical learning does not produce the decontextualized causal schemas underpinning human analogical reasoning, establishing grounding-dependent transfer as a fundamental limitation of current LLMs and VLMs.

摘要：提取抽象因果結構並將其應用於新情境是人類智慧的一個特徵。雖然大型語言模型（LLMs）和視覺語言模型（VLMs）在各種推理任務上表現出色，但它們在互動式因果學習方面的能力——通過序列探索誘導潛在結構並在不同上下文中轉移它們——仍然未被表徵。人類學習者在最小的暴露後能夠完成這種轉移，而傳統的強化學習（RL）代理則會遭遇災難性的失敗。最先進的人工智慧（AI）模型是否具有人類類似的抽象因果結構轉移機制仍然是一個未解之謎。使用OpenLock範式，要求對共同原因（CC）和共同效果（CE）結構進行序列發現，我們在此顯示模型表現出根本性延遲或缺失的轉移：即使是成功的模型也需要最初的環境特定映射——我們稱之為環境基礎——才能出現效率增益，而人類則從第一次解決嘗試中利用先前的結構知識。在僅有文本的條件下，模型的發現效率與人類相當或超過。相比之下，視覺信息——在僅有圖像和文本與圖像的條件下——整體上降低了而不是提升了性能，顯示出對符號處理的廣泛依賴，而不是綜合的多模態推理。模型還表現出系統性的CC/CE不對稱，而人類則不存在這種情況，這表明存在啟發式偏見，而不是方向中立的因果抽象。這些發現揭示了大規模統計學習並未產生支撐人類類比推理的去情境化因果模式，確立了依賴基礎的轉移作為當前LLMs和VLMs的一個基本限制。

##### **From Skill Text to Skill Structure: The Scheduling-Structural-Logical Representation for Agent Skills**
2604.24026v2 by Qiliang Liang, Hansi Wang, Zhong Liang, Yang Liu

LLM agents increasingly rely on reusable skills, capability packages that combine instructions, control flow, constraints, and tool calls. In most current agent systems, however, skills are still represented by text-heavy artifacts, including SKILL{.}md-style documents and structured records whose machine-usable evidence remains embedded largely in natural-language descriptions. This poses a challenge for skill-centered agent systems: managing skill collections and using skills to support agent both require reasoning over invocation interfaces, execution structure, and concrete side effects that are often entangled in a single textual surface. An explicit representation of skill knowledge may therefore help make these artifacts easier for machines to acquire and leverage. Drawing on Memory Organization Packets, Script Theory, and Conceptual Dependency from Schank and Abelson's classical work on linguistic knowledge representation, we introduce what is, to our knowledge, the first structured representation for agent skill artifacts that disentangles skill-level scheduling signals, scene-level execution structure, and logic-level action and resource-use evidence: the Scheduling-Structural-Logical (SSL) representation. We instantiate SSL with an LLM-based normalizer and evaluate it on a corpus of skills in two tasks, Skill Discovery and Risk Assessment, and superiorly outperform the text-only baselines: in Skill Discovery, SSL improves MRR from 0.573 to 0.707; in Risk Assessment, it improves macro F1 from 0.744 to 0.787. These findings reveal that explicit, source-grounded structure makes agent skills easier to search and review. They also suggest that SSL is best understood as a practical step toward more inspectable, reusable, and operationally actionable skill representations for agent systems, rather than as a finished standard or an end-to-end mechanism for managing and using skills.

摘要：LLM 代理越來越依賴可重用的技能，這些技能包結合了指令、控制流程、約束和工具調用。然而，在大多數當前的代理系統中，技能仍然以文本為主的工件來表示，包括 SKILL{.}md 風格的文檔和結構化記錄，其機器可用的證據仍然主要嵌入在自然語言描述中。這對以技能為中心的代理系統提出了挑戰：管理技能集合和使用技能來支持代理都需要對調用介面、執行結構和具體副作用進行推理，而這些通常糾纏在單一的文本表面中。因此，技能知識的明確表示可能有助於使這些工件更容易被機器獲取和利用。基於記憶組織包、腳本理論以及 Schank 和 Abelson 在語言知識表示方面的經典工作中的概念依賴，我們介紹了我們所知的第一個結構化表示，用於代理技能工件，該表示解開了技能級調度信號、場景級執行結構和邏輯級行動及資源使用證據：調度-結構-邏輯 (SSL) 表示。我們用基於 LLM 的標準化器實現了 SSL，並在兩個任務的技能語料庫上進行評估，分別是技能發現和風險評估，並在性能上優於僅文本基準：在技能發現中，SSL 將 MRR 從 0.573 提高到 0.707；在風險評估中，將宏觀 F1 從 0.744 提高到 0.787。這些發現揭示了明確的、基於來源的結構使代理技能更容易搜索和審查。它們還表明，SSL 最好被理解為朝向更可檢查、可重用和可操作的技能表示的實際一步，而不是作為一個完成的標準或端到端的機制來管理和使用技能。

##### **Propagation Structure-Semantic Transfer Learning for Robust Fake News Detection**
2604.23974v1 by Mengyang Chen, Lingwei Wei, Han Cao, Wei Zhou, Zhou Yan, Songlin Hu

Fake news generally refers to false information that is spread deliberately to deceive people, which has detrimental social effects. Existing fake news detection methods primarily learn the semantic features from news content or integrate structural features from propagation. However, in practical scenarios, due to the semantic ambiguity of informal language and unreliable user interactive behaviors on social media, there are inherent semantic and structural noises in news content and propagation. Although some recent works consider the effect of irrelevant user interactions in a hybrid-modeling way, they still suffer from the mutual interference between structural noise and semantic noise, leading to limited performance for robust detection. To alleviate this issue, this paper proposes a novel Propagation Structure-Semantic Transfer Learning framework (PSS-TL) for robust fake news detection under a teacher-student architecture. Specifically, we design dual teacher models to learn semantics knowledge and structure knowledge from noisy news content and propagation structure independently. Besides, we design a Multi-channel Knowledge Distillation (MKD) loss to enable the student model to acquire specialized knowledge from the teacher models, thereby avoiding mutual interference. Extensive experiments on two real-world datasets validate the effectiveness and robustness of our method.

摘要：假新聞通常指的是故意散播的虛假信息，以欺騙人們，這對社會造成了有害的影響。現有的假新聞檢測方法主要從新聞內容中學習語義特徵或整合來自傳播的結構特徵。然而，在實際場景中，由於非正式語言的語義模糊性和社交媒體上不可靠的用戶互動行為，新聞內容和傳播中存在固有的語義和結構噪聲。儘管一些近期的研究以混合建模的方式考慮了無關用戶互動的影響，但它們仍然受到結構噪聲和語義噪聲之間的相互干擾，導致穩健檢測的性能有限。為了緩解這一問題，本文提出了一種新穎的傳播結構-語義轉移學習框架（PSS-TL），用於在教師-學生架構下進行穩健的假新聞檢測。具體而言，我們設計了雙教師模型，獨立地從噪聲新聞內容和傳播結構中學習語義知識和結構知識。此外，我們設計了一種多通道知識蒸餾（MKD）損失，以使學生模型能夠從教師模型中獲取專門知識，從而避免相互干擾。在兩個真實世界數據集上的大量實驗驗證了我們方法的有效性和穩健性。

##### **Quantum Knowledge Graph: Modeling Context-Dependent Triplet Validity**
2604.23972v1 by Yao Wang, Zixu Geng, Jun Yan

Knowledge graphs (KGs) are increasingly used to support large lan guage model (LLM) reasoning, but standard triplet-based KGs treat each relation as globally valid. In many settings, whether a relation should count as evidence depends on the context. We therefore formulate triplet validity as a triplet-specific function of context and refer to this formulation as a Quantum Knowledge Graph (QKG). We instantiate QKG in medicine using a diabetes-centered PrimeKG subgraph, whose 68,651 context-sensitive relations are further annotated with patient-group-specific constraints. We evaluate it in a reasoner--validator pipeline for medical question answering on a KG-grounded subset of MedReason containing 2,788 questions. With Haiku-4.5 as both the Reasoner and the Validator, KG-backed validation significantly improves over a no-validator baseline ($+0.61$ pp), and QKG with context matching yields the largest gain, outperforming both KG validation without context matching ($+0.79$ pp) and the no-validator baseline ($+1.40$ pp; paired McNemar, all $p<0.05$). Under a stronger validator (Qwen-3.6-Plus), the raw QKG gain over the no-validator baseline grows from $+1.40$ pp to $+5.96$ pp; the context-matching gap is non-significant ($p=0.73$) on the raw set but becomes borderline significant ($p=0.05$) after adjustment for knowledge leakage and suspicious questions, consistent with a benchmark-gold ceiling rather than a QKG limitation. Taken together, the results support the view that the value of a KG in LLM-based clinical reasoning lies not merely in storing medically related facts, but in representing whether those facts are applicable to the specific patient context. For reproducibility and further research, we release the curated QKG datasets and source code.\footnote{https://github.com/HKAI-Sci/QKG}

摘要：知識圖譜（KGs）越來越多地用於支持大型語言模型（LLM）的推理，但標準的基於三元組的KGs將每個關係視為全局有效。在許多情境中，某個關係是否應該被視為證據取決於上下文。因此，我們將三元組的有效性表述為一個特定於三元組的上下文函數，並將這一表述稱為量子知識圖譜（QKG）。我們在醫學中實現QKG，使用以糖尿病為中心的PrimeKG子圖，其68,651個上下文敏感的關係進一步附加了特定於患者群體的約束。我們在一個推理者-驗證者管道中對其進行評估，針對KG基礎的MedReason子集進行醫學問題回答，該子集包含2,788個問題。使用Haiku-4.5作為推理者和驗證者，KG支持的驗證顯著改善了無驗證者基線（$+0.61$ pp），而具有上下文匹配的QKG則產生了最大的增益，超越了沒有上下文匹配的KG驗證（$+0.79$ pp）和無驗證者基線（$+1.40$ pp；配對McNemar，所有$p&lt;0.05$）。在一個更強的驗證者（Qwen-3.6-Plus）下，原始QKG相對於無驗證者基線的增益從$+1.40$ pp增長到$+5.96$ pp；在原始數據集上，上下文匹配的差距不顯著（$p=0.73$），但在調整知識洩露和可疑問題後變得邊際顯著（$p=0.05$），這與基準金標準上限一致，而不是QKG的限制。綜合來看，結果支持這樣的觀點：KG在基於LLM的臨床推理中的價值不僅在於存儲醫學相關事實，還在於表示這些事實是否適用於特定的患者上下文。為了可重複性和進一步研究，我們釋放了經過策劃的QKG數據集和源代碼。\footnote{https://github.com/HKAI-Sci/QKG}

##### **LLM-Guided Agentic Floor Plan Parsing for Accessible Indoor Navigation of Blind and Low-Vision People**
2604.23970v1 by Aydin Ayanzadeh, Tim Oates

Indoor navigation remains a critical accessibility challenge for the blind and low-vision (BLV) individuals, as existing solutions rely on costly per-building infrastructure. We present an agentic framework that converts a single floor plan image into a structured, retrievable knowledge base to generate safe, accessible navigation instructions with lightweight infrastructure. The system has two phases: a multi-agent module that parses the floor plan into a spatial knowledge graph through a self-correcting pipeline with iterative retry loops and corrective feedback; and a Path Planner that generates accessible navigation instructions, with a Safety Evaluator agent assessing potential hazards along each route. We evaluate the system on the real-world UMBC Math and Psychology building (floors MP-1 and MP-3) and on the CVC-FP benchmark. On MP-1, we achieve success rates of 92.31%, 76.92%, and 61.54% for short, medium, and long routes, outperforming the strongest single-call baseline (Claude 3.7 Sonnet) at 84.62%, 69.23%, and 53.85%. On MP-3, we reach 76.92%, 61.54%, and 38.46%, compared to the best baseline at 61.54%, 46.15%, and 23.08%. These results show consistent gains over single-call LLM baselines and demonstrate that our workflow is a scalable solution for accessible indoor navigation for BLV individuals.

摘要：室內導航對盲人和低視力（BLV）個體來說仍然是一個關鍵的可及性挑戰，因為現有的解決方案依賴於昂貴的每棟建築基礎設施。<lb/>我們提出了一個代理框架，將單一樓層平面圖像轉換為結構化的可檢索知識庫，以生成安全、可及的導航指示，並使用輕量級基礎設施。<lb/>該系統有兩個階段：一個多代理模塊，通過自我校正的管道和迭代重試循環及校正反饋，將樓層平面圖解析為空間知識圖；以及一個路徑規劃器，生成可及的導航指示，並由安全評估代理評估每條路徑上的潛在危險。<lb/>我們在現實世界的UMBC數學與心理學大樓（MP-1和MP-3樓層）以及CVC-FP基準上評估該系統。<lb/>在MP-1上，我們的成功率分別為92.31%、76.92%和61.54%，對於短、中和長路徑，超越了最強的單次調用基線（Claude 3.7 Sonnet）的84.62%、69.23%和53.85%。<lb/>在MP-3上，我們達到76.92%、61.54%和38.46%，相比之下最佳基線為61.54%、46.15%和23.08%。<lb/>這些結果顯示出相對於單次調用LLM基線的一致增長，並證明我們的工作流程是為BLV個體提供可及室內導航的可擴展解決方案。

##### **GamED.AI: A Hierarchical Multi-Agent Framework for Automated Educational Game Generation**
2604.23947v2 by Shiven Agarwal, Yash Shah, Ashish Raj Shekhar, Priyanuj Bordoloi, Vivek Gupta

We introduce GamED.AI, a hierarchical multi-agent framework that transforms instructor-provided questions into fully playable, pedagogically grounded educational games validated through formal mechanic contracts. Built on phase-based LangGraph sub-graphs, deterministic Quality Gates, and structured Pydantic schemas, GamED.AI supports two template families encompassing 15 interaction mechanics across spatial reasoning, procedural execution, and higher-order Bloom's Taxonomy objectives. Evaluated on 200 questions spanning five subject domains, the system achieves a 90% validation pass rate, 98.3% schema compliance, and 73% token reduction over ReAct agents (${\sim}$73,500 $\rightarrow$ ${\sim}$19,900 tokens/game) at $0.46 per game. Within this model configuration, these results suggest that phase-bounded architectural structure correlates more strongly with alignment quality than prompting strategy alone. Our demonstration lets attendees generate Bloom's-aligned games from natural language in under 60 seconds, inspect Quality Gate outputs at each pipeline phase, and browse a curated library of 50 games spanning all 15 mechanic types.

摘要：我們介紹 GamED.AI，一個層級多代理框架，將講師提供的問題轉換為完全可玩、以教學為基礎的教育遊戲，並通過正式的機械合約進行驗證。該框架基於基於階段的 LangGraph 子圖、確定性的質量門和結構化的 Pydantic 模式，GamED.AI 支持兩個模板系列，涵蓋 15 種互動機制，涉及空間推理、程序執行和高階布魯姆分類法目標。在涵蓋五個學科領域的 200 個問題上進行評估，該系統實現了 90% 的驗證通過率、98.3% 的模式合規性，以及相較於 ReAct 代理的 73% 令牌減少（${\sim}$73,500 $\rightarrow$ ${\sim}$19,900 tokens/game），每個遊戲的成本為 $0.46。在這種模型配置中，這些結果表明，階段限制的架構結構與對齊質量的相關性比單純的提示策略更強。我們的演示讓參與者在 60 秒內從自然語言生成與布魯姆對齊的遊戲，檢查每個管道階段的質量門輸出，並瀏覽涵蓋所有 15 種機制類型的 50 款遊戲的策展庫。

##### **Agentic AI platforms for autonomous training and rule induction of human-human and virus-human protein-protein interactions**
2604.23924v1 by Hung N. Do, Jessica Z. Kubicek-Sutherland, Oscar A. Negrete, S. Gnanakaran

We instruct an AI agent to construct two separate agentic AI platforms: one for autonomous training of predictive ML models for human-human and virus-human PPI, and the other for inducing explicit general rules governing human-human and virus-human PPI. The first agentic AI platform for autonomous training of predictive ML models for PPI is designed to consist of five AI agents that handle autonomous data collection, data verification, feature embedding, model design, and training and validation on three-way protein-disjoint cross-fold datasets. For human-human and human-virus PPIs, the final three-way protein-disjoint ensemble achieves an accuracy of 87.3% and 86.5%, respectively. For cross-checking and interpretability purposes, the second agentic AI platform is designed to replace ML predictions with human-readable rules derived from protein embeddings, physicochemical autocovariance descriptors, compartment annotations, pathway-domain overlap, and graph contexts. For human-human PPI, it is defined by a two-rule induction, whereas human-virus is induced by a more complex set of weighted rules. The rules induced by the second agentic platform align with the SHAP-identified features from the predictive ML models built by the first agentic platform. Taken together, our work demonstrates the agentic AI's ability to orchestrate from data planning to execution, and from rule induction to explanation in ML, opening the door to various applications.

摘要：我們指示一個 AI 代理構建兩個獨立的代理 AI 平台：一個用於自主訓練人類-人類和病毒-人類 PPI 的預測 ML 模型，另一個用於引導明確的通用規則，規範人類-人類和病毒-人類 PPI。第一個用於自主訓練 PPI 預測 ML 模型的代理 AI 平台設計為由五個 AI 代理組成，這些代理負責自主數據收集、數據驗證、特徵嵌入、模型設計，以及在三向蛋白質不重疊交叉折疊數據集上的訓練和驗證。對於人類-人類和人類-病毒 PPI，最終的三向蛋白質不重疊集成模型分別達到了 87.3% 和 86.5% 的準確率。為了進行交叉檢查和可解釋性目的，第二個代理 AI 平台設計為用從蛋白質嵌入、物理化學自協方差描述符、區域註釋、途徑-領域重疊和圖形上下文中派生的人類可讀規則來替代 ML 預測。對於人類-人類 PPI，它由兩條規則引導，而人類-病毒則由一組更複雜的加權規則引導。第二個代理平台引導的規則與第一個代理平台構建的預測 ML 模型中的 SHAP 識別特徵相一致。總的來說，我們的工作展示了代理 AI 從數據規劃到執行，從規則引導到 ML 解釋的能力，為各種應用開啟了大門。

##### **Knowledge Vector of Logical Reasoning in Large Language Models**
2604.23877v1 by Zixuan Wang, Yuanyuan Lei

Logical reasoning serve as a central capability in LLMs and includes three main forms: deductive, inductive, and abductive reasoning. In this work, we study the knowledge representations of these reasoning types in LLMs and analyze the correlations among them. Our analysis shows that each form of logical reasoning can be captured as a reasoning-specific knowledge vector in a linear representation space, yet these vectors are largely independent of each other. Motivated by cognitive science theory that these subforms of logical reasoning interact closely in the human brain, as well as our observation that the reasoning process for one type can benefit from the reasoning chain produced by another, we further propose to refine the knowledge representations of each reasoning type in LLMs to encourage complementarity between them. To this end, we design a complementary subspace-constrained refinement framework, which introduces a complementary loss that enables each reasoning vector to leverage auxiliary knowledge from the others, and a subspace constraint loss that prevents erasure of their unique characteristics. Through steering experiments along reasoning vectors, we find that refined vectors incorporating complementary knowledge yield consistent performance gains. We also conduct a mechanism-interpretability analysis of each reasoning vector, revealing insights into the shared and specific features of different reasoning in LLMs.

摘要：邏輯推理作為大型語言模型（LLMs）的核心能力，包括三種主要形式：演繹推理、歸納推理和溯因推理。<lb/>在本研究中，我們研究這些推理類型在LLMs中的知識表示，並分析它們之間的相關性。<lb/>我們的分析顯示，每種邏輯推理形式都可以在線性表示空間中被捕捉為特定於推理的知識向量，但這些向量彼此之間大體上是獨立的。<lb/>受到認知科學理論的啟發，這些邏輯推理的子形式在人的大腦中密切互動，以及我們觀察到一種類型的推理過程可以受益於另一種類型產生的推理鏈，我們進一步提出在LLMs中細化每種類型的知識表示，以促進它們之間的互補性。<lb/>為此，我們設計了一個互補子空間約束的細化框架，該框架引入了一種互補損失，使每個推理向量能夠利用來自其他向量的輔助知識，並引入一種子空間約束損失，以防止其獨特特徵的消失。<lb/>通過沿著推理向量進行引導實驗，我們發現 incorporating 互補知識的細化向量產生了一致的性能提升。<lb/>我們還對每個推理向量進行了機制可解釋性分析，揭示了LLMs中不同推理的共享和特定特徵的見解。

##### **Graph Memory Transformer (GMT)**
2604.23862v1 by Nicola Zanarini, Niccolò Ferrari

We investigate whether the Feed-Forward Network (FFN) sublayer in a decoder-only transformer can be replaced by an explicit learned memory graph while preserving the surrounding autoregressive architecture. The proposed Graph Memory Transformer (GMT) keeps causal self-attention intact, but replaces the usual per-token FFN transformation with a memory cell that routes token representations over a learned bank of centroids connected by a learned directed transition matrix. In the base GMT v7 instantiation studied here, each of 16 transformer blocks contains 128 centroids, a 128 * 128 edge matrix, gravitational source routing, token-conditioned target selection, and a gated displacement readout. The cell therefore returns movement from an estimated source memory state toward a target memory state, rather than a retrieved value. The resulting model is a fully decoder-only language model with 82.2M trainable parameters and no dense FFN sublayers, compared with a 103.0M-parameter dense GPT-style baseline used in the evaluation. The base v7 model trains stably and exposes centroid usage, transition structure, and source-to-target movement as directly inspectable quantities of the forward computation. It remains behind the larger dense baseline in validation loss and perplexity (3.5995/36.58 vs. 3.2903/26.85), while showing close zero-shot benchmark behavior under the evaluated setting. These results are not intended as a state-of-the-art claim; they support the viability and structural interpretability of replacing dense within-token transformation with graph-mediated memory navigation. Broader scaling, optimized kernels, and more extensive benchmark evaluation are left for subsequent work.

摘要：我們調查了在僅解碼器的Transformer中，前饋網絡（FFN）子層是否可以被明確學習的記憶圖所取代，同時保持周圍的自回歸架構。所提出的圖記憶Transformer（GMT）保持因果自注意力不變，但用一個記憶單元取代了通常的每個標記的FFN變換，該記憶單元通過一個學習的有向轉移矩陣將標記表示路由到一個學習的質心庫。這裡研究的基礎GMT v7實例中，每個16個Transformer塊包含128個質心、一個128 * 128的邊緣矩陣、重力源路由、標記條件目標選擇和一個門控位移讀出。因此，該單元返回從估計的源記憶狀態向目標記憶狀態的運動，而不是檢索的值。與在評估中使用的103.0M參數的密集GPT風格基線相比，所得到的模型是一個完全的僅解碼器語言模型，擁有82.2M可訓練參數，且沒有密集的FFN子層。基礎v7模型穩定訓練，並將質心使用、轉移結構和源到目標的運動暴露為可直接檢查的前向計算量。它在驗證損失和困惑度上仍落後於更大的密集基線（3.5995/36.58對比3.2903/26.85），同時在評估設置下顯示出接近零樣本基準行為。這些結果並不旨在作為最先進的聲明；它們支持用圖介導的記憶導航取代密集的內部標記變換的可行性和結構可解釋性。更廣泛的擴展、優化內核和更廣泛的基準評估將留待後續工作。

##### **Scalable Production Scheduling: Linear Complexity via Unified Homogeneous Graphs**
2604.23841v1 by Jonathan Hoss, Moritz Link, Noah Klarmann

Efficiently solving the Job Shop Scheduling Problem in real-world industrial applications requires policies that are both computationally lean and topologically robust. While Reinforcement Learning has shown potential in automating dispatching rules, existing models often struggle with a scalability bottleneck caused by quadratic graph complexity or the architectural overhead of heterogeneous layers. We introduce a unified graph framework that employs feature-based homogenization to project distinct node roles into a shared latent space. This allows a standard homogeneous Graph Isomorphism Network to capture complex resource contention with linear complexity, ensuring low-latency inference for large-scale industrial applications. Our empirical results demonstrate that our framework achieves state-of-the-art performance while exhibiting consistent zero-shot generalization. We identify the job-to-machine ratio as the primary driver of policy effectiveness, rather than absolute problem size. Based on this, we propose a hypothesis of structural saturation, demonstrating that policies trained on critically congested instances ($\mathcal{J} \approx \mathcal{M}$) learn scale-invariant resolution strategies. Agents trained at this saturation point internalize invariant conflict-resolution logic, allowing them to treat massive rectangular instances as a sequential concatenation of saturated sub-problems. This approach eliminates the need for expensive scale-specific retraining and prevents overfitting to statistical shortcuts, providing a robust and efficient pathway for deploying RL solutions in dynamic production environments.

摘要：有效解決實際工業應用中的工作車間排程問題需要既計算高效又拓撲穩健的政策。雖然強化學習在自動化調度規則方面顯示出潛力，但現有模型往往面臨由於二次圖形複雜性或異構層的架構開銷造成的可擴展性瓶頸。我們提出了一個統一的圖形框架，利用基於特徵的同質化將不同的節點角色投影到共享的潛在空間。這使得標準的同質圖同構網絡能以線性複雜度捕捉複雜的資源競爭，確保在大規模工業應用中進行低延遲推斷。我們的實證結果表明，我們的框架實現了最先進的性能，同時展現出一致的零樣本泛化。我們確定工作與機器的比例是政策有效性的主要驅動因素，而不是絕對問題大小。基於此，我們提出了一個結構飽和的假設，證明在臨界擁擠實例上訓練的政策（$\mathcal{J} \approx \mathcal{M}$）學會了尺度不變的解決策略。在這個飽和點訓練的代理內化了不變的衝突解決邏輯，使他們能將大規模矩形實例視為飽和子問題的序列串聯。這種方法消除了對昂貴的特定規模再訓練的需求，並防止了對統計捷徑的過擬合，為在動態生產環境中部署強化學習解決方案提供了一條穩健而高效的途徑。

##### **Domain-Filtered Knowledge Graphs from Sparse Autoencoder Features**
2604.23829v2 by John Winnicki, Abeynaya Gnanasekaran, Eric Darve

Sparse autoencoders (SAEs) extract millions of interpretable features from a language model, but flat feature inventories aren't very useful on their own. Domain concepts get mixed with generic and weakly grounded features, while related ideas are scattered across many units, and there's no way to understand relationships between features. We address this by first constructing a strict domain-specific concept universe from a large SAE inventory using contrastive activations and a multi-stage filtering process. Next, we build two aligned graph views on the filtered set: a co-occurrence graph for corpus-level conceptual structure, organized at multiple levels of granularity, and a transcoder-based mechanism graph that links source-layer and target-layer features through sparse latent pathways. Automated edge labeling then turns these graph views into readable knowledge graphs rather than unlabeled layouts. In a case study on a biology textbook, these graphs recover coherent chapter and subchapter-level structure, reveal concepts that bridge neighboring topics, and transform messy sentence-level activity containing thousands of features into compact, readable views that illustrate the model's local activity. Taken together, this reframes a flat SAE inventory as an internal knowledge graph that converts feature-level interpretability into a global map of model knowledge and enables audits of reasoning faithfulness.

摘要：稀疏自編碼器（SAEs）從語言模型中提取數百萬個可解釋的特徵，但平坦的特徵庫本身並不太有用。領域概念與一般性和弱基礎的特徵混合在一起，而相關的想法則散布在許多單元中，沒有辦法理解特徵之間的關係。我們通過首先使用對比激活和多階段過濾過程，從大型SAE庫構建一個嚴格的領域特定概念宇宙來解決這個問題。接下來，我們在過濾後的集合上構建兩個對齊的圖視圖：一個用於語料庫級概念結構的共現圖，按多個粒度級別組織，以及一個基於轉碼器的機制圖，通過稀疏潛在通道將源層和目標層特徵連接起來。自動邊緣標籤化隨後將這些圖視圖轉換為可讀的知識圖，而不是未標記的佈局。在一項針對生物學教科書的案例研究中，這些圖恢復了連貫的章節和子章節級結構，揭示了橋接相鄰主題的概念，並將包含數千個特徵的雜亂句子級活動轉換為緊湊、可讀的視圖，展示模型的局部活動。綜合來看，這將平坦的SAE庫重新構架為一個內部知識圖，將特徵級的可解釋性轉換為模型知識的全球地圖，並使推理的忠實性審計成為可能。

##### **Domain Fine-Tuning vs. Retrieval-Augmented Generation for Medical Multiple-Choice Question Answering: A Controlled Comparison at the 4B-Parameter Scale**
2604.23801v1 by Avi-ad Avraam Buskila

Practitioners deploying small open-weight large language models (LLMs) for medical question answering face a recurring design choice: invest in a domain-fine-tuned model, or keep a general-purpose model and inject domain knowledge at inference time via retrieval-augmented generation (RAG). We isolate this trade-off by holding model size, prompt template, decoding temperature, retrieval pipeline, and evaluation protocol fixed, and varying only (i) whether the model has been domain-adapted (Gemma 3 4B vs. MedGemma 4B, both 4-bit quantized and served via Ollama) and (ii) whether retrieved passages from a medical knowledge corpus are inserted into the prompt. We evaluate all four cells of this 2x2 design on the full MedQA-USMLE 4-option test split (1,273 questions) with three repetitions per question (15,276 LLM calls). Domain fine-tuning yields a +6.8 percentage-point gain in majority-vote accuracy over the general 4B baseline (53.3% vs. 46.4%, McNemar p < 10^-4). RAG over MedMCQA explanations does not produce a statistically significant gain in either model, and in the domain-tuned model the point estimate is slightly negative (-1.9 pp, p = 0.16). At this scale and on this benchmark, domain knowledge encoded in weights dominates domain knowledge supplied in context. We release the full experiment code and JSONL traces to support replication.

摘要：從事小型開放權重大型語言模型（LLMs）用於醫學問題回答的實踐者面臨一個反覆出現的設計選擇：投資於一個領域微調的模型，還是保留一個通用模型並在推理時通過檢索增強生成（RAG）注入領域知識。<lb/>我們通過固定模型大小、提示模板、解碼溫度、檢索管道和評估協議，並僅變化（i）模型是否經過領域適應（Gemma 3 4B 與 MedGemma 4B，均為 4 位元量化並通過 Ollama 提供服務）以及（ii）是否將來自醫學知識語料庫的檢索段落插入提示中，來隔離這一權衡。<lb/>我們在完整的 MedQA-USMLE 4 選項測試拆分（1,273 個問題）上評估這個 2x2 設計的四個單元，每個問題重複三次（15,276 次 LLM 調用）。<lb/>領域微調相對於一般的 4B 基線（53.3% 對 46.4%，McNemar p < 10^-4）帶來了 +6.8 個百分點的多數投票準確率增益。<lb/>在兩個模型中，對 MedMCQA 解釋的 RAG 並未產生統計上顯著的增益，而在領域微調的模型中，點估計略為負值（-1.9 pp，p = 0.16）。<lb/>在這個規模和這個基準下，編碼在權重中的領域知識主導了在上下文中提供的領域知識。<lb/>我們發布完整的實驗代碼和 JSONL 跟蹤，以支持重複實驗。

##### **The Override Gap: A Magnitude Account of Knowledge Conflict Failure in Hypernetwork-Based Instant LLM Adaptation**
2604.23750v1 by Shuaizhi Cheng, Xiang Shi, Mingwei Li

Hypernetwork-based methods such as Doc-to-LoRA internalize a document into an LLM's weights in a single forward pass, but they fail systematically on conflicts: when the document contradicts pretraining knowledge, accuracy collapses to 46.4% on the deepest facts. We show the failure is a magnitude problem rather than a representational one. The hypernetwork already targets the right layers, but its adapter margin is approximately constant across documents while the pretrained margin grows with training frequency, so deep conflicts lose by construction. The account predicts that failure should track prior strength: sorting 194 conflicts by the base model's log-probability on the contradicted fact, baseline accuracy falls from 68% on weak-prior questions to 16% on strong-prior ones, a 52 percentage-point gap. The cure is amplitude. Selective Layer Boosting scales the adapter at its top-norm layers, and Conflict-Aware Internalization triggers boosting only when the base model is confident. Both are training-free; together they raise deep-conflict accuracy from 46.4% to 71.0% on Gemma-2B and from 53.6% to 72.5% on Mistral-7B while preserving novel-knowledge recall, and beat vanilla retrieval-augmented generation on medium conflicts by 18 percentage points despite operating entirely in parameter space. We release KID-Bench, a 489-question benchmark that separates novel recall, cross-knowledge combination, and prior-graded conflicts.

摘要：基於超網絡的方法，如 Doc-to-LoRA，將文檔內化到 LLM 的權重中，僅需一次前向傳遞，但在衝突上系統性失敗：當文檔與預訓練知識相矛盾時，對於最深的事實，準確率降至 46.4%。我們顯示這一失敗是幅度問題，而非表徵問題。超網絡已經針對正確的層，但其適配器邊際在不同文檔之間大致保持不變，而預訓練邊際隨著訓練頻率的增長而增長，因此深層衝突在結構上會失敗。該解釋預測失敗應該與先前的強度相關：根據基礎模型在被矛盾事實上的對數概率對 194 個衝突進行排序，基準準確率從弱先驗問題的 68% 降至強先驗問題的 16%，相差 52 個百分點。解決方案是幅度。選擇性層增強在其頂部規範層上擴展適配器，而衝突感知內化僅在基礎模型有信心時啟動增強。這兩者都是無需訓練的；它們共同將深層衝突的準確率從 46.4% 提升至 71.0%（Gemma-2B），並將 Mistral-7B 的準確率從 53.6% 提升至 72.5%，同時保持新知識的回憶，並在中等衝突中比原始檢索增強生成高出 18 個百分點，儘管完全在參數空間中運作。我們發布了 KID-Bench，一個包含 489 個問題的基準，區分新穎回憶、跨知識組合和先驗分級衝突。

##### **Expert Evaluation of LLM's Open-Ended Legal Reasoning on the Japanese Bar Exam Writing Task**
2604.23730v1 by Jungmin Choi, Keisuke Sakaguchi, Hiroaki Yamada

Large language models (LLMs) have shown strong performance on legal benchmarks, including multiple-choice components of bar exams. However, their capacity for generating open-ended legal reasoning in realistic scenarios remains insufficiently explored. Notably, to our best knowledge, there are no prior studies or datasets addressing this issue in the Japanese context.
  This study presents the first dataset designed to evaluate the open-ended legal reasoning performance of LLMs within the Japanese jurisdiction. The dataset is based on the writing component of the Japanese bar examination, which requires examinees to identify multiple legal issues from long narratives and to construct structured legal arguments in free text format. Our key contribution is the manual evaluation of LLMs' generated responses by legal experts, which reveals limitations and challenges in legal reasoning. Moreover, we conducted a manual analysis of hallucinations to characterize when and how the models introduce content not supported by precedent or law.
  Our real exam questions, model-generated responses, and expert evaluations reveal the milestones of current LLMs in the Japanese legal domain. Our dataset and relevant resources will be available online.

摘要：大型語言模型（LLMs）在法律基準上表現出色，包括律師考試的多選題部分。然而，它們在現實情境中生成開放式法律推理的能力仍然未被充分探索。值得注意的是，據我們所知，沒有先前的研究或數據集在日本背景下解決這個問題。<lb/>本研究提出了第一個旨在評估LLMs在日本法域內開放式法律推理表現的數據集。該數據集基於日本律師考試的寫作部分，要求考生從長篇敘述中識別多個法律問題，並以自由文本格式構建結構化法律論證。我們的主要貢獻是由法律專家對LLMs生成的回應進行手動評估，這揭示了法律推理中的局限性和挑戰。此外，我們對幻覺進行了手動分析，以描述模型何時以及如何引入不受先例或法律支持的內容。<lb/>我們的真實考試問題、模型生成的回應和專家評估揭示了當前LLMs在日本法律領域的里程碑。我們的數據集和相關資源將在線上提供。

##### **ESIA: An Energy-Based Spatiotemporal Interaction-Aware Framework for Pedestrian Intention Prediction**
2604.23728v1 by Yanping Wu, Meiting Dang, Lin Wu, Edmond S. L. Ho, Zhenghua Chen, Chongfeng Wei

Recent advances in autonomous driving have motivated research on pedestrian intention prediction, which aims to infer future crossing decisions and actions by modeling temporal dynamics, social interactions, and environmental context. However, existing studies remain constrained by oversimplified multi-agent interaction patterns, opaque reasoning logic, and a lack of global consistency in behavioral predictions, which compromise both robustness and interpretability. In this work, we propose ESIA (Energy-based Spatiotemporal Interaction-Aware framework), a novel Conditional Random Field (CRF)-based paradigm. We cast the intention prediction task as a structured prediction problem over a unified graph-based representation, treating pedestrians and the environment as spatiotemporal nodes. To characterize their distinct roles, we assign unary potentials to nodes to capture individual intentions, and pairwise potentials to edges to encode social and environmental interactions. These potentials are integrated into a unified global energy function to ensure scene-level consistency across behavioral predictions. To further constrain inference without ground-truth supervision, we introduce structural consistency terms to penalize logical contradictions. This optimization is efficiently solved via a novel Unary-Seeded Simulated Annealing (U-SSA) algorithm, which leverages high-confidence unary priors to rapidly converge to a high-quality solution. Extensive experiments on standard benchmarks demonstrate that ESIA achieves state-of-the-art performance with improved interpretability over existing methods.

摘要：最近在自動駕駛方面的進展促進了對行人意圖預測的研究，該研究旨在通過建模時間動態、社會互動和環境背景來推斷未來的過馬路決策和行動。<lb/>然而，現有研究仍受到過於簡化的多代理互動模式、不透明的推理邏輯和行為預測缺乏全局一致性的限制，這妨礙了穩健性和可解釋性。<lb/>在這項工作中，我們提出了ESIA（基於能量的時空互動感知框架），這是一種新穎的基於條件隨機場（CRF）的範式。<lb/>我們將意圖預測任務視為在統一的圖形表示上的結構化預測問題，將行人和環境視為時空節點。<lb/>為了表徵它們的不同角色，我們為節點分配一元潛力以捕捉個體意圖，並為邊緣分配成對潛力以編碼社會和環境互動。<lb/>這些潛力被整合到一個統一的全局能量函數中，以確保行為預測的場景級一致性。<lb/>為了進一步約束推斷而不需要真實標準監督，我們引入結構一致性項來懲罰邏輯矛盾。<lb/>這一優化通過一種新穎的單一種子模擬退火（U-SSA）算法高效解決，該算法利用高置信度的一元先驗快速收斂到高質量解。<lb/>在標準基準上的廣泛實驗表明，ESIA在提高可解釋性方面達到了現有方法的最先進性能。

##### **An AI-Based Supervisory Measurement Integrity Validation Layer for Cyber-Resilient AC/DC Protection in Inverter-Based Microgrids**
2604.23666v1 by Ahmad Mohammad Saber, Ahmed Saber Refae, Davor Svetinovic, Hatem Zeineldin, Amr Youssef, Ehab F. El-Saadany, Deepa Kundur

Line current differential relays (LCDRs) are measurement-driven relays that rely on time-synchronized multi-phase current waveforms to infer internal faults in AC and DC power networks. In inverter-based microgrids, however, the increasing reliance on digitally communicated measurements exposes LCDRs to false-data injection attacks (FDIAs), in which adversaries manipulate remote measurement streams to create protection-triggering yet physically inconsistent current trajectories. This paper addresses this emerging measurement integrity problem by introducing a measurement integrity validation scheme that operates as a supervisory instrumentation layer for modern LCDRs. The proposed scheme interprets short windows of synchronized instantaneous current measurements recorded during relay operation and assesses their physical consistency to distinguish genuine fault-induced trajectories from cyber-manipulated measurement streams. A recurrent neural network is trained offline using only relay-available current measurements and exploits the temporal structure of differential current waveforms, which remains informative in inverter-dominated systems where current magnitude is no longer a reliable observable. The method requires no additional sensors, auxiliary protection elements, or prior knowledge of network topology, and is applicable to both AC and DC LCDRs without structural modification. The proposed measurement validation scheme is evaluated on an islanded inverter-based microgrid under a comprehensive set of fault and FDIA scenarios, demonstrating high detection accuracy while preserving relay dependability. Hardware-in-the-loop validation using an OPAL-RT real-time simulator confirms that the scheme satisfies protection timing constraints and can operate in real time under realistic operating conditions.

摘要：線電流差動繼電器（LCDRs）是基於測量的繼電器，依賴於時間同步的多相電流波形來推斷交流和直流電力網絡中的內部故障。<lb/>然而，在基於逆變器的微電網中，對數位通信測量的日益依賴使LCDRs暴露於虛假數據注入攻擊（FDIAs），在這種攻擊中，對手操縱遠程測量流，以創造觸發保護但在物理上不一致的電流軌跡。<lb/>本文針對這一新興的測量完整性問題，提出了一種測量完整性驗證方案，該方案作為現代LCDRs的監控儀器層運行。<lb/>所提出的方案解釋了在繼電器運行期間記錄的同步瞬時電流測量的短時間窗口，並評估其物理一致性，以區分真正的故障引起的軌跡與網絡操縱的測量流。<lb/>一個 recurrent neural network 在離線情況下僅使用繼電器可用的電流測量進行訓練，並利用差動電流波形的時間結構，這在電流幅度不再是可靠可觀測量的逆變器主導系統中仍然具有信息價值。<lb/>該方法不需要額外的傳感器、輔助保護元件或網絡拓撲的先驗知識，並且適用於交流和直流LCDRs，無需結構修改。<lb/>所提出的測量驗證方案在一個孤立的基於逆變器的微電網上進行評估，涵蓋了一系列故障和FDIA場景，顯示出高檢測準確率，同時保持繼電器的可靠性。<lb/>使用OPAL-RT實時模擬器進行的硬體在迴路驗證確認該方案滿足保護時間約束，並能在現實操作條件下實時運行。

##### **Structural Enforcement of Goal Integrity in AI Agents via Separation-of-Powers Architecture**
2604.23646v1 by Rong Xiang

Recent evidence suggests that frontier AI systems can exhibit agentic misalignment, generating and executing harmful actions derived from internally constructed goals, even without explicit user requests. Existing mitigation methods, such as Reinforcement Learning from Human Feedback (RLHF) and constitutional prompting, operate primarily at the model level and provide only probabilistic safety guarantees. We propose the Policy-Execution-Authorization (PEA) architecture, a "separation-of-powers" design that enforces safety at the system level. PEA decouples intent generation, authorization, and execution into independent, isolated layers connected via cryptographically constrained capability tokens. We present five core contributions: (C1) an Intent Verification Layer (IVL) for ensuring capability-intent consistency; (C2) Intent Lineage Tracking (ILT), which binds all executable intents to the originating user request via cryptographic anchors; (C3) Goal Drift Detection, which rejects semantically divergent intents below a configurable threshold; (C4) an Output Semantic Gate (OSG) that detects implicit coercion using a structured $K \times I \times P$ threat calculus (Knowledge, Influence, Policy); and (C5) a formal verification framework proving that goal integrity is maintained even under adversarial model compromise. By shifting agent alignment from a behavioral property to a structurally enforced system constraint, PEA provides a robust foundation for the governance of autonomous agents.

摘要：最近的證據表明，前沿 AI 系統可能會出現代理不一致，產生和執行源自內部構建目標的有害行為，即使沒有明確的用戶請求。現有的緩解方法，如來自人類反饋的強化學習 (RLHF) 和憲法提示，主要在模型層面運作，僅提供概率性的安全保證。我們提出了政策執行授權 (PEA) 架構，這是一種“權力分立”的設計，強制在系統層面上確保安全。PEA 將意圖生成、授權和執行解耦為獨立的、隔離的層，通過加密約束的能力令牌相連。我們提出了五個核心貢獻：(C1) 意圖驗證層 (IVL)，用於確保能力與意圖的一致性；(C2) 意圖來源追蹤 (ILT)，通過加密錨將所有可執行意圖綁定到原始用戶請求；(C3) 目標漂移檢測，拒絕低於可配置閾值的語義上不同的意圖；(C4) 輸出語義閘 (OSG)，使用結構化的 $K \times I \times P$ 威脅計算 (知識、影響、政策) 檢測隱性脅迫；以及 (C5) 一個形式驗證框架，證明即使在對抗模型妥協的情況下，目標完整性仍然得以維持。通過將代理對齊從行為屬性轉變為結構性強制的系統約束，PEA 為自主代理的治理提供了一個穩健的基礎。

##### **RaV-IDP: A Reconstruction-as-Validation Framework for Faithful Intelligent Document Processing**
2604.23644v1 by Pritesh Jha

Intelligent document processing pipelines extract structured entities (tables, images, and text) from documents for use in downstream systems such as knowledge bases, retrieval-augmented generation, and analytics. A persistent limitation of existing pipelines is that extraction output is produced without any intrinsic mechanism to verify whether it faithfully represents the source. Model-internal confidence scores measure inference certainty, not correspondence to the document, and extraction errors pass silently into downstream consumers.
  We present Reconstruction as Validation (RaV-IDP), a document processing pipeline that introduces reconstruction as a first-class architectural component. After each entity is extracted, a dedicated reconstructor renders the extracted representation back into a form comparable to the original document region, and a comparator scores fidelity between the reconstruction and the unmodified source crop. This fidelity score is a grounded, label-free quality signal. When fidelity falls below a per-entity-type threshold, a structured GPT-4.1 vision fallback is triggered and the validation loop repeats. We enforce a bootstrap constraint: the comparator always anchors against the original document region, never against the extraction, preventing the validation from becoming circular.
  We further propose a per-stage evaluation framework pairing each pipeline component with an appropriate benchmark. The code pipeline is publicly available at https://github.com/pritesh-2711/RaV-IDP for experimentation and use.

摘要：智能文檔處理管道從文檔中提取結構化實體（表格、圖像和文本），以供下游系統使用，例如知識庫、檢索增強生成和分析。現有管道的一個持久性限制是，提取輸出是在沒有任何內在機制來驗證其是否忠實於來源的情況下產生的。模型內部的置信度分數衡量推理的確定性，而不是與文檔的對應性，提取錯誤默默地進入下游消費者。

我們提出了重建作為驗證（RaV-IDP），這是一個文檔處理管道，將重建引入為一個一級架構組件。在每個實體被提取後，專門的重建器將提取的表示轉換回與原始文檔區域可比的形式，並且比較器對重建與未修改的來源裁剪之間的忠實度進行評分。這個忠實度分數是一個有根據的、無標籤的質量信號。當忠實度低於每種實體類型的閾值時，會觸發結構化的 GPT-4.1 視覺回退，並重複驗證循環。我們強制執行一個引導約束：比較器始終以原始文檔區域為基準，而不是以提取為基準，防止驗證變得循環。

我們進一步提出了一個每階段評估框架，將每個管道組件與適當的基準配對。代碼管道可在 https://github.com/pritesh-2711/RaV-IDP 上公開獲得，以供實驗和使用。

##### **GraphPlanner: Graph Memory-Augmented Agentic Routing for Multi-Agent LLMs**
2604.23626v1 by Tao Feng, Haozhen Zhang, Zijie Lei, Peixuan Han, Jiaxuan You

LLM routing has achieved promising results in integrating the strengths of diverse models while balancing efficiency and performance. However, to support more realistic and challenging applications, routing must extend into agentic LLM settings, where task planning, multi-round cooperation among heterogeneous agents, and memory utilization are indispensable. To address this gap, we propose GraphPlanner, a heterogeneous graph memory-augmented agentic router for multi-agent LLMs that generates routing workflows for each query and supports both inductive and transductive inference. GraphPlanner formulates workflow generation as a Markov Decision Process (MDP), where at each step it selects both the LLM backbone and the agent role, including Planner, Executor, and Summarizer. By leveraging a heterogeneous graph, denoted as GARNet, to capture interaction memories among queries, agents, and responses, GraphPlanner integrates historical memory and workflow memory into richer state representations. The entire pipeline is optimized with reinforcement learning, jointly improving task-specific performance and computational efficiency. We evaluate GraphPlanner across 14 diverse LLM tasks and demonstrate that: (1) GraphPlanner outperforms strong single-round and multi-round routers, improving accuracy by up to 9.3% while reducing GPU cost from 186.26 GiB to 1.04 GiB; (2) GraphPlanner generalizes robustly to unseen tasks and LLMs, exhibiting strong zero-shot capabilities; and (3) GraphPlanner effectively leverages historical memories, supporting both inductive and transductive inference for more adaptive routing. Our code for GraphPlanner is released at https://github.com/ulab-uiuc/GraphPlanner.

摘要：LLM 路由在整合不同模型的優勢，同時平衡效率和性能方面取得了可喜的成果。<lb/>然而，為了支持更現實和具有挑戰性的應用，路由必須擴展到代理 LLM 設置，其中任務規劃、異質代理之間的多輪合作和記憶利用是不可或缺的。<lb/>為了解決這一空白，我們提出了 GraphPlanner，一種增強異質圖記憶的代理路由器，專為多代理 LLM 設計，能為每個查詢生成路由工作流程，並支持歸納和傳導推理。<lb/>GraphPlanner 將工作流程生成公式化為馬可夫決策過程 (MDP)，在每一步中選擇 LLM 主幹和代理角色，包括規劃者、執行者和摘要者。<lb/>通過利用異質圖，稱為 GARNet，以捕捉查詢、代理和回應之間的互動記憶，GraphPlanner 將歷史記憶和工作流程記憶整合為更豐富的狀態表示。<lb/>整個流程通過強化學習進行優化，聯合改善任務特定性能和計算效率。<lb/>我們在 14 個不同的 LLM 任務中評估了 GraphPlanner，並證明： (1) GraphPlanner 超越了強大的單輪和多輪路由器，準確率提高了最多 9.3%，同時將 GPU 成本從 186.26 GiB 降低到 1.04 GiB； (2) GraphPlanner 對未見任務和 LLM 具有強大的泛化能力，展現出強大的零樣本能力； (3) GraphPlanner 有效利用歷史記憶，支持歸納和傳導推理，以實現更具適應性的路由。我們的 GraphPlanner 代碼已在 https://github.com/ulab-uiuc/GraphPlanner 發布。

##### **ComplianceNLP: Knowledge-Graph-Augmented RAG for Multi-Framework Regulatory Gap Detection**
2604.23585v1 by Dongxin Guo, Jikun Wu, Siu Ming Yiu

Financial institutions must track over 60,000 regulatory events annually, overwhelming manual compliance teams; the industry has paid over USD 300 billion in fines and settlements since the 2008 financial crisis. We present ComplianceNLP, an end-to-end system that automatically monitors regulatory changes, extracts structured obligations, and identifies compliance gaps against institutional policies. The system integrates three components: (1) a knowledge-graph-augmented RAG pipeline grounding generations in a regulatory knowledge graph of 12,847 provisions across SEC, MiFID II, and Basel III; (2) multi-task obligation extraction combining NER, deontic classification, and cross-reference resolution over a shared LEGAL-BERT encoder; and (3) compliance gap analysis that maps obligations to internal policies with severity-aware scoring. On our benchmark, ComplianceNLP achieves 87.7 F1 on gap detection, outperforming GPT-4o+RAG by +3.5 F1, with 94.2% grounding accuracy ($r=0.83$ vs. human judgments) and 83.4 F1 under realistic end-to-end error propagation. Ablations show that knowledge-graph re-ranking contributes the largest marginal gain (+4.6 F1), confirming that structural regulatory knowledge is critical for cross-reference-heavy tasks. Domain-specific knowledge distillation (70B $\to$ 8B) combined with Medusa speculative decoding yields $2.8\times$ inference speedup; regulatory text's low entropy ($H=2.31$ bits vs. $3.87$ general text) produces 91.3% draft-token acceptance rates. In four months of parallel-run deployment processing 9,847 updates at a financial institution, the system achieved 96.0% estimated recall and 90.7% precision, with a $3.1\times$ sustained analyst efficiency gain. We report deployment lessons on trust calibration, GRC integration, and distributional shift monitoring for regulated-domain NLP.

摘要：金融機構每年必須追蹤超過60,000個監管事件，這使得手動合規團隊不堪重負；自2008年金融危機以來，該行業已支付超過3,000億美元的罰款和和解金。<lb/>我們提出了ComplianceNLP，一個端到端的系統，能自動監控監管變更、提取結構化義務，並識別與機構政策之間的合規差距。<lb/>該系統整合了三個組件：(1) 一個知識圖譜增強的RAG管道，將生成內容基於涵蓋12,847條規定的監管知識圖譜，這些規定來自SEC、MiFID II和巴塞爾協議III；(2) 多任務義務提取，結合了NER、義務分類和跨參考解析，使用共享的LEGAL-BERT編碼器；以及(3) 合規差距分析，將義務映射到內部政策並進行嚴重性感知的評分。<lb/>在我們的基準測試中，ComplianceNLP在差距檢測上達到了87.7的F1，超越了GPT-4o+RAG 3.5的F1，並且具有94.2%的基礎準確率（$r=0.83$對比人類判斷）以及在現實端到端錯誤傳播下的83.4 F1。<lb/>消融實驗顯示，知識圖譜重新排序貢獻了最大的邊際增益（+4.6 F1），確認結構性監管知識對於重度跨參考任務至關重要。<lb/>特定領域的知識蒸餾（70B $\to$ 8B）結合Medusa投機解碼實現了$2.8\times$的推理加速；監管文本的低熵（$H=2.31$位對比一般文本的$3.87$）產生了91.3%的草稿標記接受率。<lb/>在四個月的平行運行部署中，處理了9,847次更新，該系統實現了96.0%的預估召回率和90.7%的精確度，並持續提高了$3.1\times$的分析師效率。<lb/>我們報告了在受監管領域的NLP中關於信任校準、GRC整合和分佈變化監控的部署經驗教訓。

##### **AgentEval: DAG-Structured Step-Level Evaluation for Agentic Workflows with Error Propagation Tracking**
2604.23581v1 by Dongxin Guo, Jikun Wu, Siu Ming Yiu

Agentic systems that chain reasoning, tool use, and synthesis into multi-step workflows are entering production, yet prevailing evaluation practices like end-to-end outcome checks and ad-hoc trace inspection systematically mask the intermediate failures that dominate real-world error budgets. We present AgentEval, a framework that formalizes agent executions as evaluation directed acyclic graphs (DAGs), where each node carries typed quality metrics assessed by a calibrated LLM judge (GPT-4o), classified through a hierarchical failure taxonomy (3 levels, 21 subcategories), and linked to upstream dependencies for automated root cause attribution. An ablation study isolates the impact of DAG-based dependency modeling: it alone contributes +22 percentage points to failure detection recall and +34 pp to root cause accuracy over flat step-level evaluation with identical judges and rubrics.
  Across three production workflows (450 test cases, two agent model families, predominantly sequential architectures with a 12% non-DAG trace rate), AgentEval achieves 2.17x higher failure detection recall than end-to-end evaluation (0.89 vs. 0.41), Cohen's kappa = 0.84 agreement with human experts, and 72% root cause accuracy against an 81% human ceiling. Cross-system evaluation on tau-bench and SWE-bench traces confirms transferability (failure detection recall >= 0.78) without taxonomy or rubric modification. A 4-month pilot with 18 engineers detected 23 pre-release regressions through CI/CD-integrated regression testing, reducing median root-cause identification time from 4.2 hours to 22 minutes and driving measurable failure rate reductions in two workflows.

摘要：代理系統將推理、工具使用和綜合整合為多步驟工作流程，正進入生產階段，然而，現行的評估實踐，如端到端結果檢查和臨時追蹤檢查，系統性地掩蓋了主導現實世界錯誤預算的中間失敗。我們提出了AgentEval，一個將代理執行形式化為以評估為導向的有向無環圖（DAG）的框架，其中每個節點攜帶由經過校準的LLM評判（GPT-4o）評估的類型化質量指標，通過分層失敗分類法（3個層級，21個子類別）進行分類，並鏈接到上游依賴項以自動歸因根本原因。一項消融研究隔離了基於DAG的依賴建模的影響：它單獨對失敗檢測召回率貢獻了+22個百分點，對根本原因準確性貢獻了+34個百分點，超過了具有相同評判者和評分標準的平面步驟級評估。在三個生產工作流程中（450個測試案例，兩個代理模型系列，主要是順序架構，具有12%的非DAG追蹤率），AgentEval的失敗檢測召回率比端到端評估高出2.17倍（0.89對0.41），與人類專家的Cohen's kappa = 0.84一致，根本原因準確性為72%，而人類的上限為81%。在tau-bench和SWE-bench追蹤上的跨系統評估確認了可轉移性（失敗檢測召回率 >= 0.78），無需修改分類法或評分標準。為期4個月的試點計劃中，18名工程師通過CI/CD集成的回歸測試檢測到23個發佈前的回歸，將中位根本原因識別時間從4.2小時縮短至22分鐘，並在兩個工作流程中推動可衡量的失敗率降低。

##### **RouteNLP: Closed-Loop LLM Routing with Conformal Cascading and Distillation Co-Optimization**
2604.23577v1 by Dongxin Guo, Jikun Wu, Siu Ming Yiu

Serving diverse NLP workloads with large language models is costly: at one enterprise partner, inference costs exceeded $200K/month despite over 70% of queries being routine tasks well within the capability of smaller models. We present RouteNLP, a closed-loop framework that routes queries across a tiered model portfolio to minimize cost while satisfying per-task quality constraints. The framework integrates three components: a difficulty-aware router with shared task-conditioned representations trained on preference data and quality signals; confidence-calibrated cascading that uses conformal prediction for distribution-free threshold initialization; and a distillation-routing co-optimization loop that clusters escalation failures, applies targeted knowledge distillation to cheaper models, and automatically retrains the router, yielding over twice the cost improvement of untargeted distillation. In an 8-week pilot deployment processing ~5K queries/day at an enterprise customer-service division, RouteNLP reduced inference costs by 58% while maintaining 91% response acceptance and reducing p99 latency from 1,847 ms to 387 ms. On a six-task benchmark spanning finance, customer service, and legal domains, the framework achieves 40-85% cost reduction while retaining 96-100% quality on structured tasks and 96-98% on generation tasks, with human evaluation confirming that 74.5% of routed generation outputs match or exceed frontier-model quality.

摘要：提供多樣化的自然語言處理工作負載與大型語言模型的成本高昂：在一個企業合作夥伴中，推理成本超過每月 20 萬美元，儘管超過 70% 的查詢是常規任務，這些任務完全在較小模型的能力範圍內。我們提出了 RouteNLP，一個閉環框架，通過分層模型組合路由查詢，以最小化成本，同時滿足每個任務的質量約束。該框架整合了三個組件：一個基於難度的路由器，具有基於偏好數據和質量信號訓練的共享任務條件表示；使用符合預測進行無分佈閾值初始化的信心校準級聯；以及一個蒸餾路由共同優化循環，聚類升級失敗，對較便宜的模型應用有針對性的知識蒸餾，並自動重新訓練路由器，實現了超過未針對性蒸餾的兩倍成本改善。在一個為期 8 週的試點部署中，處理約 5,000 條查詢/天於一家企業客戶服務部門，RouteNLP 將推理成本降低了 58%，同時保持 91% 的回應接受率，並將 p99 延遲從 1,847 毫秒降低到 387 毫秒。在一個涵蓋金融、客戶服務和法律領域的六任務基準中，該框架實現了 40-85% 的成本降低，同時在結構化任務上保持 96-100% 的質量，在生成任務上保持 96-98% 的質量，並且人類評估確認 74.5% 的路由生成輸出達到或超過前沿模型的質量。

##### **COMO: Closed-Loop Optical Molecule Recognition with Minimum Risk Training**
2604.23546v1 by Zhuoqi Lyu, Qing Ke

Optical chemical structure recognition (OCSR) translates molecular images into machine-readable representations like SMILES strings or molecular graphs, but remains challenging in real-world documents due to inexhaustible variations in chemical structures, shorthand conventions, and visual noise. Most existing deep-learning-based approaches rely on teacher forcing with token-level Maximum Likelihood Estimation (MLE). This training paradigm suffers from exposure bias, as models are trained under ground-truth prefixes but must condition on their own previous predictions during inference. Moreover, token-level MLE objectives hinder the optimization towards molecular-level evaluation criteria such as chemical validity and structural similarity. Here we introduce Minimum Risk Training (MRT) to OCSR and propose COMO (Closed-loop Optical Molecule recOgnition), a closed-loop framework that mitigates exposure bias by directly optimizing over molecule-level, non-differentiable objectives, by iteratively sampling and evaluating the model's own predictions. Experiments on ten benchmarks including synthetic and real-world chemical diagrams from patent and scientific literature demonstrate that COMO substantially outperforms existing rule-based and learning-based methods with less training data. Ablation studies further show that MRT is architecture-agnostic, demonstrating its potential for broad application to end-to-end OCSR systems.

摘要：光學化學結構識別 (OCSR) 將分子圖像轉換為機器可讀的表示形式，例如 SMILES 字串或分子圖，但在現實世界的文件中仍然面臨挑戰，因為化學結構、簡寫慣例和視覺噪聲的變化無窮無盡。大多數現有的基於深度學習的方法依賴於使用標記級最大似然估計 (MLE) 的教師強迫。這種訓練範式受到曝光偏見的困擾，因為模型是在真實前綴下進行訓練的，但在推斷過程中必須依賴自己之前的預測。此外，標記級 MLE 目標妨礙了對分子級評估標準（如化學有效性和結構相似性）的優化。在此，我們將最小風險訓練 (MRT) 引入 OCSR，並提出 COMO（閉環光學分子識別），這是一個閉環框架，通過直接優化分子級的非可微目標來減輕曝光偏見，並通過迭代抽樣和評估模型自己的預測。對十個基準的實驗，包括來自專利和科學文獻的合成和現實世界的化學圖表，證明 COMO 在訓練數據較少的情況下顯著優於現有的基於規則和基於學習的方法。消融研究進一步顯示 MRT 是架構無關的，顯示其在端到端 OCSR 系統中的廣泛應用潛力。

##### **Do Transaction-Level and Actor-Level AML Queues Agree? An Empirical Evaluation of Granularity Effects on the Elliptic++ Graph**
2604.23494v1 by Ankur Malik

Graph-based anti-money laundering (AML) systems on blockchain networks can score suspicious activity at two granularity levels -- transactions or actor addresses -- yet compliance action is conducted per actor. This paper contributes an evaluation methodology for measuring how scoring granularity affects investigation queue composition under fixed review budgets. We formalize the evaluation through a projection framework mapping transaction-level scores to the actor-level action unit via four aggregation operators, and introduce budgeted investigation metrics -- yield@budget, burden decomposition, and case fragmentation. Using the public Elliptic++ Bitcoin dataset (203,769 transactions; 822,942 address occurrences), we train independent random forest classifiers at each level under a causal temporal protocol and compare review queues through Jaccard overlap, burden decomposition, and feature-matching ablations. At one-percent budget, temporal evaluation yields mean Jaccard of 0.374 (SD 0.171); static pooled evaluation yields 0.087 (95% CI [0.079, 0.094]). An enriched address model receiving all 237 features produces even lower overlap (Jaccard=0.051), with 4.3% illicit per 100 reviews versus 30.2% for the transaction-projected queue. Address-level detection value is temporally concentrated: two timesteps exceed 91% illicit per 100 reviews while the static burden is only 3.4%. A fixed hybrid policy underperforms the best single-level queue by 5.05pp (CI [-10.2pp, -0.9pp]). These findings establish that scoring granularity is a consequential design variable for AML investigation systems -- same data, same budget, different queues, different addresses investigated.

摘要：基於圖形的反洗錢 (AML) 系統在區塊鏈網絡上可以在兩個粒度層級上評分可疑活動——交易或行為者地址——但合規行動是根據行為者進行的。本文貢獻了一種評估方法，用於測量評分粒度如何影響在固定審查預算下的調查隊列組成。我們通過一個投影框架形式化評估，將交易層級的分數映射到行為者層級的行動單位，並通過四個聚合運算子引入預算調查指標——yield@budget、負擔分解和案件碎片化。使用公共的 Elliptic++ 比特幣數據集（203,769 笔交易；822,942 地址出現），我們在因果時間協議下對每個層級訓練獨立的隨機森林分類器，並通過 Jaccard 重疊、負擔分解和特徵匹配消融比較審查隊列。在一百分之一的預算下，時間評估的平均 Jaccard 為 0.374（標準差 0.171）；靜態池評估為 0.087（95% 置信區間 [0.079, 0.094]）。接收所有 237 個特徵的增強地址模型產生了更低的重疊（Jaccard=0.051），每 100 次審查中有 4.3% 的非法活動，與交易投影隊列的 30.2% 相比。地址層級的檢測價值在時間上是集中性的：兩個時間步驟每 100 次審查中超過 91% 的非法活動，而靜態負擔僅為 3.4%。固定混合政策的表現比最佳單層隊列低 5.05 個百分點（置信區間 [-10.2pp, -0.9pp]）。這些發現確立了評分粒度是反洗錢調查系統的一個重要設計變量——相同數據、相同預算、不同隊列、不同地址被調查。

##### **K-SENSE: A Knowledge-Guided Self-Augmented Encoder for Neuro-Semantic Evaluation of Mental Health Conditions on Social Media**
2604.23493v2 by Vijay Yadav

Early detection of mental health conditions, particularly stress and depression, from social media text remains a challenging open problem in computational psychiatry and natural language processing. Automated systems must contend with figurative language, implicit emotional expression, and the high noise inherent in user-generated content. Existing approaches either leverage external commonsense knowledge to model mental states explicitly, or apply self-augmentation and contrastive training to improve generalization, but seldom do both in a principled, unified framework. We propose K-SENSE (Knowledge-guided Self-augmented Encoder for Neuro-Semantic Evaluation of Mental Health), a framework that jointly exploits external psychological reasoning and internal representation robustness. K-SENSE adopts a three-stage encoding pipeline: (1) inferential commonsense knowledge is extracted from the COMET model across five mental state dimensions; (2) a semantic anchor is constructed by combining hidden representations from two parallel encoding streams, projected into a shared space before fusion; and (3) a supervised contrastive learning objective aligns same-class representations while encouraging the attention mechanism to suppress irrelevant knowledge noise. We evaluate K-SENSE on Dreaddit (stress detection) and Depression_Mixed (depression detection), achieving mean F1-scores of 86.1 (0.6%) and 94.3 (0.8%), respectively, over five independent runs. These represent improvements of approximately 2.6 and 1.5 percentage points over the strongest prior baselines. Ablation experiments confirm the contribution of each architectural component, including the temporal knowledge integration strategy and the choice to keep the knowledge encoder frozen during fine-tuning.

摘要：早期從社交媒體文本中檢測心理健康狀況，特別是壓力和抑鬱，仍然是計算精神病學和自然語言處理中的一個挑戰性開放問題。<lb/>自動化系統必須應對比喻語言、隱含情感表達以及用戶生成內容中固有的高噪聲。<lb/>現有的方法要麼利用外部常識知識來明確建模心理狀態，要麼應用自我增強和對比訓練來提高泛化能力，但很少在一個有原則的統一框架中同時做到這兩點。<lb/>我們提出了 K-SENSE（知識引導的自我增強編碼器，用於心理健康的神經語義評估），這是一個共同利用外部心理推理和內部表示穩健性的框架。<lb/>K-SENSE 採用三階段編碼管道：<lb/>(1) 從 COMET 模型中提取推理常識知識，涵蓋五個心理狀態維度；<lb/>(2) 通過將來自兩個平行編碼流的隱藏表示結合來構建語義錨點，然後投影到共享空間再進行融合；<lb/>(3) 監督對比學習目標對齊同類表示，同時鼓勵注意機制抑制無關的知識噪聲。<lb/>我們在 Dreaddit（壓力檢測）和 Depression_Mixed（抑鬱檢測）上評估 K-SENSE，分別達到 86.1（0.6%）和 94.3（0.8%）的平均 F1 分數，這是五次獨立運行的結果。<lb/>這些分數分別比最強的先前基準提高了約 2.6 和 1.5 個百分點。<lb/>消融實驗確認了每個架構組件的貢獻，包括時間知識整合策略和在微調過程中保持知識編碼器凍結的選擇。

##### **Hybrid JIT-CUDA Graph Optimization for Low-Latency Large Language Model Inference**
2604.23467v1 by Divakar Kumar Yadav, Tian Zhao

Large Language Models (LLMs) have achieved strong performance across natural language and multimodal tasks, yet their practical deployment remains constrained by inference latency and kernel launch overhead, particularly in interactive, short-sequence settings. This paper presents a hybrid runtime framework that combines Just-In-Time (JIT) compilation with CUDA Graph execution to reduce launch overhead while preserving runtime flexibility during autoregressive decoding. The framework partitions transformer inference into static components executed via CUDA Graph replay and dynamic components handled through JIT-compiled kernels, enabling asynchronous graph capture and reuse across decoding steps.
  We evaluate the proposed approach on LLaMA-2 7B using single-GPU, batch-size-one inference across prompt lengths from 10 to 500 tokens. Experimental results show that the hybrid runtime reduces Time-to-First-Token (TTFT) by up to 66.0% and achieves lower P99 latency compared with TensorRT-LLM in this regime. These results indicate that hybrid JIT-CUDA Graph execution can effectively reduce inference latency and variance for short-sequence LLM workloads, making it a practical optimization strategy for latency-sensitive AI applications.

摘要：大型語言模型（LLMs）在自然語言和多模態任務中表現出色，但其實際部署仍受限於推理延遲和內核啟動開銷，特別是在互動式的短序列環境中。本文提出了一種混合運行時框架，將即時編譯（Just-In-Time, JIT）與CUDA圖執行相結合，以減少啟動開銷，同時在自回歸解碼過程中保持運行時靈活性。該框架將Transformer推理劃分為通過CUDA圖重播執行的靜態組件和通過JIT編譯內核處理的動態組件，從而實現解碼步驟之間的異步圖捕獲和重用。<lb/>我們在LLaMA-2 7B上評估了所提出的方法，使用單GPU、批次大小為1的推理，涵蓋從10到500個標記的提示長度。實驗結果顯示，混合運行時將首次標記時間（Time-to-First-Token, TTFT）減少了最多66.0%，並在此範疇內實現了較低的P99延遲。這些結果表明，混合JIT-CUDA圖執行可以有效減少短序列LLM工作負載的推理延遲和變異性，成為對延遲敏感的AI應用的一種實用優化策略。

##### **IndustryAssetEQA: A Neurosymbolic Operational Intelligence System for Embodied Question Answering in Industrial Asset Maintenance**
2604.23446v1 by Chathurangi Shyalika, Dhaval Patel, Amit Sheth

Industrial maintenance environments increasingly rely on AI systems to assist operators in understanding asset behavior, diagnosing failures, and evaluating interventions. Although large language models (LLMs) enable fluent natural-language interaction, deployed maintenance assistants routinely produce generic explanations that are weakly grounded in telemetry, omit verifiable provenance, and offer no testable support for counterfactual or action-oriented reasoning that undermine trust in safety-critical settings. We present IndustryAssetEQA, a neurosymbolic operational intelligence system that combines episodic telemetry representations with a Failure Mode Effects Analysis Knowledge Graph (FMEA-KG) to enable Embodied Question Answering (EQA) over industrial assets. We evaluate on four datasets covering four industrial asset types, including rotating machinery, turbofan engines, hydraulic systems, and cyber-physical production systems. Compared to LLM-only baselines, IndustryAssetEQA improves structural validity by up to 0.51, counterfactual accuracy by up to 0.47, and explanation entailment by 0.64, while reducing severe expert-rated overclaims from 28% to 2% (approximately 93% reduction). Code, datasets, and the FMEA-KG are available at https://github.com/IBM/AssetOpsBench/tree/IndustryAssetEQA/IndustryAssetEQA.

摘要：工業維護環境越來越依賴人工智慧系統來協助操作員理解資產行為、診斷故障和評估干預措施。儘管大型語言模型（LLMs）能夠實現流暢的自然語言互動，但已部署的維護助手常常產生基於遙測的薄弱解釋，省略可驗證的來源，並且對反事實或以行動為導向的推理不提供可測試的支持，這在安全關鍵的環境中削弱了信任。我們提出了IndustryAssetEQA，一個神經符號運營智能系統，將情節遙測表示與故障模式影響分析知識圖譜（FMEA-KG）結合，以實現對工業資產的具身問答（EQA）。我們在四個數據集上進行評估，涵蓋四種工業資產類型，包括旋轉機械、渦扇發動機、液壓系統和網絡物理生產系統。與僅使用LLM的基準相比，IndustryAssetEQA在結構有效性上提高了最多0.51，在反事實準確性上提高了最多0.47，在解釋推理上提高了0.64，同時將專家評定的過度聲稱從28%降低到2%（約93%的減少）。代碼、數據集和FMEA-KG可在https://github.com/IBM/AssetOpsBench/tree/IndustryAssetEQA/IndustryAssetEQA獲得。

##### **Automating Categorization of Scientific Texts with In-Context Learning and Prompt-Chaining in Large Language Models**
2604.23430v1 by Gautam Kishore Shahi, Oliver Hummel

The relentless expansion of scientific literature presents significant challenges for navigation and knowledge discovery. Within Research Information Retrieval, established tasks such as text summarization and classification remain crucial for enabling researchers and practitioners to effectively navigate this vast landscape, so that efforts have increasingly been focused on developing advanced research information systems. These systems aim not only to provide standard keyword-based search functionalities but also to incorporate capabilities for automatic content categorization within knowledge-intensive organizations across academia and industry. This study systematically evaluates the performance of off-the-shelf Large Language Models (LLMs) in analyzing scientific texts according to a given classification scheme. We utilized the hierarchical ORKG taxonomy as a classification framework, employing the FORC dataset as ground truth. We investigated the effectiveness of advanced prompt engineering strategies, namely In-Context Learning (ICL) and Prompt Chaining, and experimentally explored the influence of the LLMs' temperature hyperparameter on classification accuracy. Our experiments demonstrate that Prompt Chaining yields superior classification accuracy compared to pure ICL, particularly when applied to the nested structure of the ORKG taxonomy. LLMs with prompt chaining outperform the state-of-the-art models for domain (1st level) prediction and show even better performance for subject (2nd level) prediction compared to the older BERT model. However, LLMs are not yet able to perform well in classifying the topic (3rd level) of research areas based on this specific hierarchical taxonomy, as they only reach about 50% accuracy even with prompt chaining.

摘要：科學文獻的無情擴張為導航和知識發現帶來了重大挑戰。<lb/>在研究信息檢索中，文本摘要和分類等既定任務對於使研究人員和實踐者能夠有效地導航這個廣闊的領域仍然至關重要，因此越來越多的努力集中在開發先進的研究信息系統上。<lb/>這些系統的目標不僅是提供基於標準關鍵字的搜索功能，還要在學術界和工業界的知識密集型組織中納入自動內容分類的能力。<lb/>本研究系統地評估了現成的大型語言模型（LLMs）在根據給定分類方案分析科學文本方面的性能。<lb/>我們利用層次化的ORKG分類法作為分類框架，並使用FORC數據集作為真實標準。<lb/>我們調查了先進提示工程策略的有效性，即上下文學習（ICL）和提示鏈接，並實驗性地探索了LLMs的溫度超參數對分類準確性的影響。<lb/>我們的實驗表明，與純粹的ICL相比，提示鏈接在分類準確性上表現更佳，特別是在應用於ORKG分類法的嵌套結構時。<lb/>使用提示鏈接的LLMs在領域（第一層級）預測中超越了最先進的模型，並且在主題（第二層級）預測中表現得比舊的BERT模型更好。<lb/>然而，LLMs在根據這一特定層次分類法對研究領域的主題（第三層級）進行分類時尚未能表現良好，因為即使使用提示鏈接，它們的準確率也僅達到約50%。

##### **Evolve: A Persistent Knowledge Lifecycle for Small Language Models**
2604.23424v1 by Dikran Hovagimian

Evolve pairs a small local language model with a persistent, teacher-compiled knowledge store -- refined through sleep consolidation and usage-driven refresh -- to deliver substantial accuracy gains over the model's parametric baseline while amortizing teacher costs through cross-query knowledge reuse. Rather than retrieving document fragments at query time, Evolve constructs a store of semantically coherent sections compiled by teacher models at natural conceptual boundaries; new sections are staged on acquisition, consolidated offline through teacher-mediated merging, and refreshed inline when expired. A 2B-parameter local model handles classification and generation; large teacher models are invoked only for knowledge operations.
  Across 750 benchmark queries spanning custom specialist questions, NaturalQuestions, and TriviaQA, the 2B model augmented by Evolve improves from 20-33% baseline accuracy to 60-84% (+40-52pp) while reducing teacher invocations by over 50% through reuse. Post-consolidation compresses the knowledge store by 31-33.5% across three independent benchmarks while preserving accuracy; section-based retrieval outperforms chunk-based retrieval by 5-9pp across every lifecycle condition. The architecture supports two generation modes over the same lifecycle -- suppress (strict section-only grounding, auditable) and augment (section-supplemented responses).

摘要：Evolve 將一個小型本地語言模型與一個持久的、由教師編輯的知識庫相結合——通過睡眠鞏固和使用驅動的刷新進行精煉——以在模型的參數基準上實現顯著的準確性提升，同時通過跨查詢知識重用來攤銷教師成本。Evolve 並不是在查詢時檢索文檔片段，而是構建一個由教師模型在自然概念邊界編輯的語義一致部分的庫；新部分在獲取時進行階段性處理，通過教師介導的合併進行線下鞏固，並在過期時進行內聯刷新。一個 2B 參數的本地模型處理分類和生成；大型教師模型僅在知識操作時被調用。<lb/>在涵蓋自定義專家問題、NaturalQuestions 和 TriviaQA 的 750 個基準查詢中，Evolve 增強的 2B 模型將基準準確率從 20-33% 提升到 60-84%（+40-52pp），同時通過重用將教師調用減少超過 50%。後鞏固使知識庫在三個獨立基準上壓縮了 31-33.5%，同時保持準確性；基於部分的檢索在每個生命周期條件下均超過基於片段的檢索 5-9pp。該架構支持在相同生命周期內的兩種生成模式——抑制（嚴格的僅部分基礎，便於審計）和增強（部分補充的回應）。

##### **Beyond Local vs. External: A Game-Theoretic Framework for Trustworthy Knowledge Acquisition**
2604.23413v1 by Rujing Yao, Yufei Shi, Yang Wu, Ang Li, Zhuoren Jiang, XiaoFeng Wang, Haixu Tang, Xiaozhong Liu

Cloud-hosted Large Language Models (LLMs) offer unmatched reasoning capabilities and dynamic knowledge, yet submitting raw queries to these external services risks exposing sensitive user intent. Conversely, relying exclusively on trusted local models preserves privacy but often compromises answer quality due to limited parameter scale and knowledge. To resolve this dilemma, we propose Game-theoretic Trustworthy Knowledge Acquisition (GTKA), a framework that formulates the trade-off between knowledge utility and privacy as a strategic game. GTKA consists of three components: (i) a privacy-aware sub-query generator that decomposes sensitive intent into generalized, low-risk fragments; (ii) an adversarial reconstruction attacker that attempts to infer the original query from these fragments, providing adaptive leakage signals; and (iii) a trusted local integrator that synthesizes external responses within a secure boundary. By training the generator and attacker in an alternating adversarial manner, GTKA optimizes the sub-query generation policy to maximize knowledge acquisition accuracy while minimizing the reconstructability of the original sensitive intent. To validate our approach, we construct two sensitive-domain benchmarks in the biomedical and legal fields. Extensive experiments demonstrate that GTKA significantly reduces intent leakage compared to state-of-the-art baselines while maintaining high-fidelity answer quality.

摘要：雲端托管的大型語言模型（LLMs）提供無與倫比的推理能力和動態知識，但向這些外部服務提交原始查詢會冒著暴露敏感用戶意圖的風險。相反，完全依賴受信的本地模型雖然能夠保護隱私，但由於參數規模和知識的限制，往往會妥協答案質量。為了解決這一困境，我們提出了博弈論的可信知識獲取（GTKA）框架，將知識效用與隱私之間的權衡表述為一個策略性遊戲。GTKA 由三個組件組成：（i）一個隱私意識的子查詢生成器，將敏感意圖分解為一般化的低風險片段；（ii）一個對抗性重建攻擊者，試圖從這些片段推斷原始查詢，提供自適應的洩漏信號；以及（iii）一個可信的本地整合器，在安全邊界內合成外部響應。通過以交替對抗的方式訓練生成器和攻擊者，GTKA 優化了子查詢生成策略，以最大化知識獲取的準確性，同時最小化原始敏感意圖的可重建性。為了驗證我們的方法，我們在生物醫學和法律領域構建了兩個敏感領域基準。廣泛的實驗表明，與最先進的基準相比，GTKA 顯著降低了意圖洩漏，同時保持了高保真度的答案質量。

##### **SoccerRef-Agents: Multi-Agent System for Automated Soccer Refereeing**
2604.23392v1 by Zi Meng, Wanli Song, Yi Hu, Jiayuan Rao, Gang Chen

Refereeing is vital in sports, where fair, accurate, and explainable decisions are fundamental. While intelligent assistant technologies are being widely adopted in soccer refereeing, current AI-assisted approaches remain preliminary. Existing research mostly focuses on isolated video perception tasks and lacks the ability to understand and reason about foul scenarios. To fill this gap, we propose SoccerRef-Agents, a holistic and explainable multi-agent decision-making framework for soccer refereeing. The main contributions are: (i) constructing the multimodal benchmark SoccerRefBench with over 1,200 referee theory questions and 600 foul video clips; (ii) building a vector-based knowledge base RefKnowledgeDB using the latest "Laws of the Game" and a classic case database for precise, knowledge-driven reasoning; (iii) designing a novel multi-agent architecture that collaborates via cross-modal RAG to bridge the semantic gap between visual content and regulatory texts. This work explores the technical capability of integrating MLLMs with refereeing expertise, and evaluations show our system significantly outperforms general-purpose MLLMs in decision accuracy and explanation quality. All databases, benchmarks, and code will be made available.

摘要：裁判在體育運動中至關重要，公正、準確且可解釋的決策是基本要求。雖然智能助手技術在足球裁判中被廣泛採用，但目前的 AI 輔助方法仍然處於初步階段。現有研究主要集中在孤立的視頻感知任務上，缺乏理解和推理犯規情境的能力。為了填補這一空白，我們提出了 SoccerRef-Agents，這是一個全面且可解釋的多代理決策框架，用於足球裁判。主要貢獻包括：(i) 構建多模態基準 SoccerRefBench，包含超過 1,200 道裁判理論問題和 600 段犯規視頻剪輯；(ii) 建立基於向量的知識庫 RefKnowledgeDB，使用最新的《比賽規則》和一個經典案例數據庫，以實現精確的知識驅動推理；(iii) 設計一種新穎的多代理架構，通過跨模態 RAG 協作，彌合視覺內容與規範文本之間的語義差距。這項工作探索了將 MLLMs 與裁判專業知識整合的技術能力，評估顯示我們的系統在決策準確性和解釋質量上顯著超越通用 MLLMs。所有數據庫、基準和代碼將會公開。

##### **A Parametric Memory Head for Continual Generative Retrieval**
2604.23388v1 by Kidist Amde Mekonnen, Yubao Tang, Maarten de Rijke

Generative information retrieval (GenIR) consolidates retrieval into a single neural model that decodes document identifiers (docids) directly from queries. While this model-as-index paradigm offers architectural simplicity, it is poorly suited to dynamic document collections. Unlike modular systems, where indexes are easily updated, GenIR's knowledge is parametrically encoded in its weights; consequently, standard adaptation methods such as full and parameter-efficient fine-tuning can induce catastrophic forgetting. We show that sequential adaptation improves retrieval on newly added documents but substantially degrades performance on earlier slices, exposing a pronounced stability-plasticity trade-off. To address this, we propose post-adaptation memory tuning (PAMT), a memory-only stabilization stage that augments an adapted model with a modular parametric memory head (PMH). PAMT freezes the backbone and attaches a product-key memory with fixed addressing. During prefix-trie constrained decoding, decoder hidden states sparsely query PMH to produce residual corrections in hidden space; these corrections are mapped to score adjustments via the frozen output embedding matrix, computed only over trie-valid tokens. This guides docid generation while keeping routing and backbone parameters fixed. To limit cross-slice interference, PAMT updates only a fixed budget of memory values selected using decoding-time access statistics, prioritizing entries frequently activated by the current slice and rarely used in prior sessions. Experiments on MS MARCO and Natural Questions under sequential, disjoint corpus increments show that PAMT substantially improves retention on earlier slices with minimal impact on retrieval performance for newly added documents, while modifying only a sparse subset of memory values per session.

摘要：生成資訊檢索（GenIR）將檢索整合為一個單一的神經模型，該模型直接從查詢中解碼文檔識別碼（docids）。雖然這種模型作為索引的範式提供了架構上的簡單性，但它並不適合動態文檔集合。與模組化系統不同，模組化系統的索引可以輕鬆更新，GenIR 的知識是以參數形式編碼在其權重中；因此，標準的適應方法，如完全和參數高效的微調，可能會導致災難性遺忘。我們顯示，序列適應在新添加的文檔上改善了檢索，但對早期片段的性能造成了顯著下降，暴露出明顯的穩定性-可塑性權衡。為了解決這個問題，我們提出了後適應記憶調整（PAMT），這是一個僅限記憶的穩定化階段，通過模組化的參數記憶頭（PMH）來增強適應後的模型。PAMT 冷凍了主幹並附加了一個具有固定尋址的產品鍵記憶。在前綴樹約束解碼過程中，解碼器隱藏狀態稀疏地查詢 PMH，以在隱藏空間中產生殘差修正；這些修正通過冷凍的輸出嵌入矩陣映射到分數調整，僅在樹有效的標記上計算。這指導了 docid 的生成，同時保持路由和主幹參數固定。為了限制跨片段的干擾，PAMT 僅更新使用解碼時訪問統計選擇的固定預算的記憶值，優先考慮當前片段經常激活的條目以及在先前會話中很少使用的條目。在 MS MARCO 和自然問題的實驗中，通過序列的、不重疊的語料庫增量顯示，PAMT 顯著改善了早期片段的保留，對新添加文檔的檢索性能影響最小，同時每個會話僅修改一個稀疏的記憶值子集。

##### **GSAR: Typed Grounding for Hallucination Detection and Recovery in Multi-Agent LLMs**
2604.23366v1 by Federico A. Kamelhar

Autonomous multi-agent LLM systems are increasingly deployed to investigate operational incidents and produce structured diagnostic reports. Their trustworthiness hinges on whether each claim is grounded in observed evidence rather than model-internal inference. Existing groundedness evaluators (binary classifiers, LLM-as-judge scalars, self-correction loops) treat supporting evidence as interchangeable and emit a single signal that offers no principled control over downstream action.
  We present GSAR, a grounding-evaluation and replanning framework that (i) partitions claims into a four-way typology (grounded, ungrounded, contradicted, complementary), giving first-class standing to non-redundant alternative perspectives; (ii) assigns evidence-type-specific weights reflecting epistemic strength; (iii) computes an asymmetric contradiction-penalised weighted groundedness score; and (iv) couples that score to a three-tier decision function (proceed, regenerate, replan) driving a bounded-iteration outer loop under an explicit compute budget.
  We formalise the algorithm, prove six structural properties, and evaluate five design claims on FEVER with gold Wikipedia evidence under four independently-trained LLM judges (gpt-5.4, claude-sonnet-4-6, claude-opus-4-7, gemini-2.5-pro). Every ablation reproduces in the same direction on every judge: bootstrap 95% CIs on the rho=0 effect exclude 0 on all four; the no-complementary ablation under Opus 4.7 has CI [-96,-68] of 200; at n=1000 three independent judges converge to DeltaS(rho=0)=+0.058. A head-to-head against Vectara HHEM-2.1-Open is included. To our knowledge, GSAR is the first published groundedness framework coupling evidence-typed scoring with tiered recovery under an explicit compute budget.

摘要：自主多代理LLM系統越來越多地被用來調查操作事件並生成結構化的診斷報告。它們的可信度取決於每一個主張是否基於觀察到的證據，而不是模型內部的推斷。現有的基礎評估器（二元分類器、LLM作為評判者的標量、自我修正循環）將支持證據視為可互換，並發出單一信號，這對下游行動沒有原則性的控制。<lb/>  
我們提出了GSAR，一個基礎評估和重新規劃框架，(i) 將主張劃分為四種類型（有根據、無根據、相互矛盾、互補），賦予非冗餘替代觀點一級地位；(ii) 指派特定證據類型的權重，反映認識強度；(iii) 計算一個不對稱的矛盾懲罰加權基礎分數；(iv) 將該分數與一個三級決策函數（繼續、再生、重新規劃）結合，驅動在明確計算預算下的有限迭代外部循環。<lb/>  
我們形式化了該算法，證明了六個結構性質，並在FEVER上使用四個獨立訓練的LLM評判者（gpt-5.4、claude-sonnet-4-6、claude-opus-4-7、gemini-2.5-pro）對五個設計主張進行了評估。每個消融在每個評判者上都朝著相同的方向重現：在rho=0的效應上，bootstrap 95% CIs排除了所有四個評判者的0；在Opus 4.7下的無互補消融有CI [-96,-68]的200；在n=1000時，三個獨立評判者收斂到DeltaS(rho=0)=+0.058。還包括了一個與Vectara HHEM-2.1-Open的正面對決。據我們所知，GSAR是第一個發表的基礎框架，將證據類型的評分與在明確計算預算下的分層恢復結合在一起。

##### **VeriLLMed: Interactive Visual Debugging of Medical Large Language Models with Knowledge Graphs**
2604.23356v1 by Yurui Xiang, Xingyi Mao, Rui Sheng, Zixin Chen, Zelin Zang, Yuyang Wu, Haipeng Zeng, Huamin Qu, Yushi Sun, Yanna Lin

Large language models (LLMs) show promise in medical diagnosis, but real-world deployment remains challenging due to high-stakes clinical decisions and imperfect reasoning reliability. As a result, careful inspection of model behavior is essential for assessing whether diagnostic reasoning is reliable and clinically grounded. However, debugging medical LLMs remains difficult. First, developers often lack sufficient medical domain expertise to interpret model errors in clinically meaningful terms. Second, models can fail across a large and diverse set of instances involving different input types, tasks, and reasoning steps, making it challenging for developers to prioritize which errors deserve focused inspection. Third, developers struggle to identify recurring error patterns across cases, as existing debugging practices are largely instance-centric and rely on manual inspection of isolated failures. To address these challenges, we present VeriLLMed, a visual analytics system that integrates external biomedical knowledge to audit and debug medical LLM diagnostic reasoning. VeriLLMed transforms model outputs into comparable reasoning paths, constructs knowledge graph-grounded reference paths, and identifies three recurring classes of diagnosis errors: relation errors, branch errors, and missing errors. Case studies and expert evaluation demonstrate that VeriLLMed helps developers identify clinically implausible reasoning and generate actionable insights that can inform the improvement of medical LLMs.

摘要：大型語言模型（LLMs）在醫療診斷中顯示出潛力，但由於高風險的臨床決策和不完善的推理可靠性，實際應用仍然具有挑戰性。<lb/>因此，仔細檢查模型行為對於評估診斷推理是否可靠且臨床基礎至關重要。<lb/>然而，調試醫療LLMs仍然困難重重。<lb/>首先，開發者通常缺乏足夠的醫學領域專業知識，無法以臨床意義解釋模型錯誤。<lb/>其次，模型可能在涉及不同輸入類型、任務和推理步驟的大量多樣實例中失敗，這使得開發者難以優先考慮哪些錯誤值得重點檢查。<lb/>第三，開發者難以識別跨案例的重複錯誤模式，因為現有的調試實踐主要以實例為中心，並依賴於對孤立失敗的手動檢查。<lb/>為了解決這些挑戰，我們提出了VeriLLMed，一個視覺分析系統，該系統整合了外部生物醫學知識，以審核和調試醫療LLM的診斷推理。<lb/>VeriLLMed將模型輸出轉換為可比較的推理路徑，構建基於知識圖譜的參考路徑，並識別三類重複的診斷錯誤：關係錯誤、分支錯誤和缺失錯誤。<lb/>案例研究和專家評估表明，VeriLLMed幫助開發者識別臨床上不合理的推理，並生成可行的見解，這些見解可以促進醫療LLMs的改進。

##### **Bridging Reasoning and Action: Hybrid LLM-RL Framework for Efficient Cross-Domain Task-Oriented Dialogue**
2604.23345v1 by Yangyang Zhao, Linfan Dai, Li Cai, Bowen Xing, Libo Qin

Cross-domain task-oriented dialogue requires reasoning over implicit and explicit feasibility constraints while planning long-horizon, multi-turn actions. Large language models (LLMs) can infer such constraints but are unreliable over long horizons, while Reinforcement learning (RL) optimizes long-horizon behavior yet cannot recover constraints from raw dialogue. Naively coupling LLMs with RL is therefore brittle: unverified or unstructured LLM outputs can corrupt state representations and misguide policy learning. Motivated by this, we propose Verified LLM-Knowledge empowered RL (VLK-RL), a hybrid framework that makes LLM-derived constraint reasoning usable for RL. VLK-RL first elicits candidate constraints with an LLM and then verifies them via a dual-role cross-examination procedure to suppress hallucinations and cross-turn inconsistencies. The verified constraints are mapped into ontology-aligned slot-value representations, yielding a structured, constraint-aware state for RL policy optimization. Experiments across multiple benchmarks demonstrate that VLK-RL significantly improves generalization and robustness, outperforming strong single-model baselines on long-horizon tasks.

摘要：跨域任務導向對話需要在規劃長期、多輪行動時，對隱含和明確的可行性約束進行推理。大型語言模型（LLMs）能夠推斷這些約束，但在長期範圍內不可靠，而強化學習（RL）則優化長期行為，但無法從原始對話中恢復約束。因此，將LLMs與RL簡單結合是脆弱的：未經驗證或結構不良的LLM輸出可能會破壞狀態表示並誤導策略學習。受到這一點的啟發，我們提出了經過驗證的LLM知識增強RL（VLK-RL），這是一個混合框架，使LLM衍生的約束推理可用於RL。VLK-RL首先利用LLM引出候選約束，然後通過雙重角色交叉檢查程序驗證它們，以抑制幻覺和跨輪不一致性。經過驗證的約束被映射到本體對齊的槽值表示中，產生一個結構化的、具約束意識的狀態，以便於RL策略優化。在多個基準測試中的實驗表明，VLK-RL顯著提高了泛化能力和穩健性，在長期任務上超越了強大的單模型基準。

##### **EAD-Net: Emotion-Aware Talking Head Generation with Spatial Refinement and Temporal Coherence**
2604.23325v1 by Yahui Li, Yinfeng Yu, Liejun Wang, Shengjie Shen

Emotionally talking head video generation aims to generate expressive portrait videos with accurate lip synchronization and emotional facial expressions. Current methods rely on simple emotional labels, leading to insufficient semantic information. While introducing high-level semantics enhances expressiveness, it easily causes lip-sync degradation. Furthermore, mainstream generation methods struggle to balance computational efficiency and global motion awareness in long videos and suffer from poor temporal coherence. Therefore, we propose an \textbf{E}motion-\textbf{A}ware \textbf{D}iffusion model-based \textbf{Net}work, called \textbf{EAD-Net}. We introduce SyncNet supervision and Temporal Representation Alignment (TREPA) to mitigate lip-sync degradation caused by multi-modal fusion. To model complex spatio-temporal dependencies in long video sequences, we propose a Spatio-Temporal Directional Attention (STDA) mechanism that captures global motion patterns through strip attention. Additionally, we design a Temporal Frame graph Reasoning Module (TFRM) to explicitly model temporal coherence between video frames through graph structure learning. To enhance emotional semantic control, a large language model is employed to extract textual descriptions from real videos, serving as high-level semantic guidance. Experiments on the HDTF and MEAD datasets demonstrate that our method outperforms existing methods in terms of lip-sync accuracy, temporal consistency, and emotional accuracy.

摘要：情感對話頭影片生成旨在生成具有準確嘴唇同步和情感面部表情的表現力肖像影片。當前的方法依賴於簡單的情感標籤，導致語義信息不足。雖然引入高級語義可以增強表現力，但也容易導致嘴唇同步的退化。此外，主流生成方法在長影片中難以平衡計算效率和全局運動感知，並且存在時間一致性差的問題。因此，我們提出了一種基於\textbf{E}motion-\textbf{A}ware \textbf{D}iffusion模型的\textbf{Net}work，稱為\textbf{EAD-Net}。我們引入SyncNet監督和時間表示對齊（TREPA）來減輕由多模態融合引起的嘴唇同步退化。為了建模長影片序列中的複雜時空依賴，我們提出了一種時空方向注意力（STDA）機制，通過條帶注意力捕捉全局運動模式。此外，我們設計了一個時間幀圖推理模塊（TFRM），通過圖結構學習明確建模影片幀之間的時間一致性。為了增強情感語義控制，我們使用大型語言模型從真實影片中提取文本描述，作為高級語義指導。對於HDTF和MEAD數據集的實驗表明，我們的方法在嘴唇同步準確性、時間一致性和情感準確性方面優於現有方法。

##### **$\mathcal{S}^2$IT: Stepwise Syntax Integration Tuning for Large Language Models in Aspect Sentiment Quad Prediction**
2604.23296v1 by Bingfeng Chen, Chenjie Qiu, Yifeng Xie, Boyan Xu, Ruichu Cai, Zhifeng Hao

Aspect Sentiment Quad Prediction (ASQP) has seen significant advancements, largely driven by the powerful semantic understanding and generative capabilities of large language models (LLMs). However, while syntactic structure information has been proven effective in previous extractive paradigms, it remains underutilized in the generative paradigm of LLMs due to their limited reasoning capabilities. In this paper, we propose S^2IT, a novel Stepwise Syntax Integration Tuning framework that progressively integrates syntactic structure knowledge into LLMs through a multi-step tuning process. The training process is divided into three steps. S^2IT decomposes the quadruple generation task into two stages: 1) Global Syntax-guided Extraction and 2) Local Syntax-guided Classification, integrating both global and local syntactic structure information. Finally, Fine-grained Structural Tuning enhances the model's understanding of syntactic structures through the prediction of element links and node classification. Experiments demonstrate that S^2IT significantly improves state-of-the-art performance across multiple datasets. Our implementation will be open-sourced at https://github.com/DMIRLAB-Group/S2IT.

摘要：Aspect Sentiment Quad Prediction (ASQP) 已經取得了重大進展，這主要得益於大型語言模型 (LLMs) 強大的語義理解和生成能力。<lb/>然而，儘管語法結構信息在先前的提取範式中已被證明是有效的，但由於 LLMs 的推理能力有限，它在生成範式中仍然未被充分利用。<lb/>在本文中，我們提出了 S^2IT，一種新穎的逐步語法整合調整框架，通過多步調整過程逐步將語法結構知識整合到 LLMs 中。<lb/>訓練過程分為三個步驟。<lb/>S^2IT 將四元組生成任務分解為兩個階段：1) 全局語法引導提取和 2) 局部語法引導分類，整合全局和局部的語法結構信息。<lb/>最後，細粒度結構調整通過元素鏈接和節點分類的預測增強了模型對語法結構的理解。<lb/>實驗表明，S^2IT 在多個數據集上顯著提高了最先進的性能。<lb/>我們的實現將在 https://github.com/DMIRLAB-Group/S2IT 開源。

##### **From Similarity to Structure: Training-free LLM Context Compression with Hybrid Graph Priors**
2604.23277v1 by Yitian Zhou, Chaoning Zhang, Jiaquan Zhang, Zhenzhen Huang, Jinyu Guo, Sung-Ho Bae, Lik-Hang Lee, Caiyan Qin, Yang Yang

Long-context large language models remain computationally expensive to run and often fail to reliably process very long inputs, which makes context compression an important component of many systems. Existing compression approaches typically rely on trained compressors, dense retrieval-style selection, or heuristic trimming, and they often struggle to jointly preserve task relevance, topic coverage, and cross-sentence coherence under a strict token budget. To address this, we propose a training-free and model-agnostic compression framework that selects a compact set of sentences guided by structural graph priors. Our method constructs a sparse hybrid sentence graph that combines mutual k-NN semantic edges with short-range sequential edges, extracts a topic skeleton via clustering, and ranks sentences using an interpretable score that integrates task relevance, cluster representativeness, bridge centrality, and a cycle coverage cue. A budgeted greedy selection with redundancy suppression then produces a readable compressed context in original order. Experimental results on four datasets show that our approach is competitive with strong extractive and abstractive baselines, demonstrating larger gains on long-document benchmarks.

摘要：長文上下文的大型語言模型在運行上仍然計算成本高昂，並且往往無法可靠地處理非常長的輸入，這使得上下文壓縮成為許多系統的重要組成部分。現有的壓縮方法通常依賴於訓練過的壓縮器、密集檢索風格的選擇或啟發式修剪，並且在嚴格的標記預算下，往往難以共同保持任務相關性、主題覆蓋率和跨句子的一致性。為了解決這個問題，我們提出了一個無需訓練且與模型無關的壓縮框架，該框架根據結構圖的先驗選擇一組緊湊的句子。我們的方法構建了一個稀疏的混合句子圖，該圖結合了互相的 k-NN 語義邊和短距離的序列邊，通過聚類提取主題骨架，並使用一個可解釋的分數對句子進行排名，該分數整合了任務相關性、聚類代表性、橋接中心性和循環覆蓋提示。然後，預算內的貪婪選擇結合冗餘抑制，生成按原始順序排列的可讀壓縮上下文。在四個數據集上的實驗結果顯示，我們的方法在強提取和抽象基準上具有競爭力，並在長文檔基準上顯示出更大的增益。

