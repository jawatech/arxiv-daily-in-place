
### LLM
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-05-19**|**Atoms of Thought: Universal EEG Representation Learning with Microstates**|Xinyang Tian et.al.|[2605.20182v1](http://arxiv.org/abs/2605.20182v1)|null|
|**2026-05-19**|**TIDE: Efficient and Lossless MoE Diffusion LLM Inference with I/O-aware Expert Offload**|Zhiben Chen et.al.|[2605.20179v1](http://arxiv.org/abs/2605.20179v1)|null|
|**2026-05-19**|**From Seeing to Thinking: Decoupling Perception and Reasoning Improves Post-Training of Vision-Language Models**|Juncheng Wu et.al.|[2605.20177v1](http://arxiv.org/abs/2605.20177v1)|null|
|**2026-05-19**|**ClinSeekAgent: Automating Multimodal Evidence Seeking for Agentic Clinical Reasoning**|Juncheng Wu et.al.|[2605.20176v1](http://arxiv.org/abs/2605.20176v1)|null|
|**2026-05-19**|**A Methodology for Selecting and Composing Runtime Architecture Patterns for Production LLM Agents**|Vasundra Srinivasan et.al.|[2605.20173v1](http://arxiv.org/abs/2605.20173v1)|null|
|**2026-05-19**|**Long-term Power Grid Planning via Answer Set Programming**|Antonio Ielo et.al.|[2605.20172v1](http://arxiv.org/abs/2605.20172v1)|null|
|**2026-05-19**|**KoRe: Compact Knowledge Representations for Large Language Models**|Davide Cavicchini et.al.|[2605.20170v1](http://arxiv.org/abs/2605.20170v1)|null|
|**2026-05-19**|**Not Every Rubric Teaches Equally: Policy-Aware Rubric Rewards for RLVR**|Utkarsh Tyagi et.al.|[2605.20164v1](http://arxiv.org/abs/2605.20164v1)|null|
|**2026-05-19**|**Rethinking Visual Attribution for Chest X-ray Reasoning in Large Vision Language Models**|Guangzhi Xiong et.al.|[2605.20158v1](http://arxiv.org/abs/2605.20158v1)|null|
|**2026-05-19**|**Less Back-and-Forth: A Comparative Study of Structured Prompting**|Saurav Ghosh et.al.|[2605.20149v1](http://arxiv.org/abs/2605.20149v1)|null|
|**2026-05-19**|**MixRea: Benchmarking Explicit-Implicit Reasoning in Large Language Models**|Yuanqing Cai et.al.|[2605.20128v1](http://arxiv.org/abs/2605.20128v1)|null|
|**2026-05-19**|**Beyond Prediction Accuracy: Target-Space Recovery Profiles for Evaluating Model-Brain Alignment**|Ken Nakamura et.al.|[2605.20127v1](http://arxiv.org/abs/2605.20127v1)|null|
|**2026-05-19**|**Toto 2.0: Time Series Forecasting Enters the Scaling Era**|Emaad Khwaja et.al.|[2605.20119v1](http://arxiv.org/abs/2605.20119v1)|null|
|**2026-05-19**|**k-Inductive Neural Barrier Certificates for Unknown Nonlinear Dynamics**|Ben Wooding et.al.|[2605.20108v1](http://arxiv.org/abs/2605.20108v1)|null|
|**2026-05-19**|**Draft Less, Retrieve More: Hybrid Tree Construction for Speculative Decoding**|Yuhao Shen et.al.|[2605.20104v1](http://arxiv.org/abs/2605.20104v1)|null|
|**2026-05-19**|**Neurosymbolic Learning for Inference-Time Argumentation**|Gabriel Freedman et.al.|[2605.20098v1](http://arxiv.org/abs/2605.20098v1)|null|
|**2026-05-19**|**INSHAPE: Instance-Level Shapelets for Interpretable Time-Series Classification**|Seongjun Lee et.al.|[2605.20088v1](http://arxiv.org/abs/2605.20088v1)|null|
|**2026-05-19**|**ThoughtTrace: Understanding User Thoughts in Real-World LLM Interactions**|Chuanyang Jin et.al.|[2605.20087v1](http://arxiv.org/abs/2605.20087v1)|null|
|**2026-05-19**|**What Do Evolutionary Coding Agents Evolve?**|Nico Pelleriti et.al.|[2605.20086v1](http://arxiv.org/abs/2605.20086v1)|null|
|**2026-05-19**|**BalanceRAG: Joint Risk Calibration for Cascaded Retrieval-Augmented Generation**|Zijun Jia et.al.|[2605.20084v1](http://arxiv.org/abs/2605.20084v1)|null|
|**2026-05-19**|**VL-DPO: Vision-Language-Guided Finetuning for Preference-Aligned Autonomous Driving**|Zhefan Xu et.al.|[2605.20082v1](http://arxiv.org/abs/2605.20082v1)|null|
|**2026-05-19**|**Probability-Conserving Flow Guidance**|Parsa Esmati et.al.|[2605.20079v1](http://arxiv.org/abs/2605.20079v1)|null|
|**2026-05-19**|**CopT: Contrastive On-Policy Thinking with Continuous Spaces for General and Agentic Reasoning**|Dachuan Shi et.al.|[2605.20075v1](http://arxiv.org/abs/2605.20075v1)|null|
|**2026-05-19**|**Probing Embodied LLMs: When Higher Observation Fidelity Hurts Problem Solving**|Oussama Zenkri et.al.|[2605.20072v1](http://arxiv.org/abs/2605.20072v1)|null|
|**2026-05-19**|**Text-to-SPARQL Generation with Reinforcement Learning: A GRPO-based Approach on DBLP**|Jann Pfeifer et.al.|[2605.20066v1](http://arxiv.org/abs/2605.20066v1)|null|
|**2026-05-19**|**Rewarding Beliefs, Not Actions: Consistency-Guided Credit Assignment for Long-Horizon Agents**|Wenjie Tang et.al.|[2605.20061v1](http://arxiv.org/abs/2605.20061v1)|null|
|**2026-05-19**|**Towards LLM-Assisted Architecture Recovery for Real-World ROS~2 Systems: An Agent-Based Multi-Level Approach to Hierarchical Structural Architecture Reconstruction**|Dominique Briechle et.al.|[2605.20055v1](http://arxiv.org/abs/2605.20055v1)|null|
|**2026-05-19**|**PromptRad: Knowledge-Enhanced Multi-Label Prompt-Tuning for Low-Resource Radiology Report Labeling**|Ying-Jia Lin et.al.|[2605.20052v1](http://arxiv.org/abs/2605.20052v1)|null|
|**2026-05-19**|**Language Mutations Sustain the Persistences of Conspiracy Theories on Social Media**|Calvin Yixiang Cheng et.al.|[2605.20050v1](http://arxiv.org/abs/2605.20050v1)|null|
|**2026-05-19**|**Does Code Cleanliness Affect Coding Agents? A Controlled Minimal-Pair Study**|Priyansh Trivedi et.al.|[2605.20049v1](http://arxiv.org/abs/2605.20049v1)|null|
|**2026-05-19**|**Mind Your Moras: Orthography-Aware Error Analysis of Neural Japanese Morphological Generation**|Wen Zhang et.al.|[2605.20043v1](http://arxiv.org/abs/2605.20043v1)|null|
|**2026-05-19**|**AutoResearchClaw: Self-Reinforcing Autonomous Research with Human-AI Collaboration**|Jiaqi Liu et.al.|[2605.20025v1](http://arxiv.org/abs/2605.20025v1)|null|
|**2026-05-19**|**When Skills Don't Help: A Negative Result on Procedural Knowledge for Tool-Grounded Agents in Offensive Cybersecurity**|Samuel Jacob Chacko et.al.|[2605.20023v1](http://arxiv.org/abs/2605.20023v1)|null|
|**2026-05-19**|**FlexDraft: Flexible Speculative Decoding via Attention Tuning and Bonus-Guided Calibration**|Yaojie Zhang et.al.|[2605.20022v1](http://arxiv.org/abs/2605.20022v1)|null|
|**2026-05-19**|**Training Neural Networks with Optimal Double-Bayesian Learning**|Vy Bui et.al.|[2605.20009v1](http://arxiv.org/abs/2605.20009v1)|null|
|**2026-05-19**|**LLM Benchmark Datasets Should Be Contamination-Resistant**|Ali Al-Lawati et.al.|[2605.19999v1](http://arxiv.org/abs/2605.19999v1)|null|
|**2026-05-19**|**A Case for Agentic Tuning: From Documentation to Action in PostgreSQL**|Hongyu Lin et.al.|[2605.19988v1](http://arxiv.org/abs/2605.19988v1)|null|
|**2026-05-19**|**Learning with Foresight: Enhancing Neural Routing Policy via Multi-Node Lookahead Prediction**|Xia Jiang et.al.|[2605.19975v1](http://arxiv.org/abs/2605.19975v1)|null|
|**2026-05-19**|**Block-Sphere Vector Quantization**|Heesang Ann et.al.|[2605.19972v1](http://arxiv.org/abs/2605.19972v1)|null|
|**2026-05-19**|**Detecting Fluent Optimization-Based Adversarial Prompts via Sequential Entropy Changes**|Mohammed Alshaalan et.al.|[2605.19966v1](http://arxiv.org/abs/2605.19966v1)|null|
|**2026-05-19**|**World-Ego Modeling for Long-Horizon Evolution in Hybrid Embodied Tasks**|Zuyao Lin et.al.|[2605.19957v1](http://arxiv.org/abs/2605.19957v1)|null|
|**2026-05-19**|**Rethinking How to Remember: Beyond Atomic Facts in Lifelong LLM Agent Memory**|Jingwei Sun et.al.|[2605.19952v1](http://arxiv.org/abs/2605.19952v1)|null|
|**2026-05-19**|**GEM: GPU-Variability-Aware Expert to GPU Mapping for MoE Systems**|Sourish Wawdhane et.al.|[2605.19945v1](http://arxiv.org/abs/2605.19945v1)|null|
|**2026-05-19**|**A Measure-Theoretic Analysis of Reasoning: Structural Generalization and Approximation Limits**|Yuyang Zhang et.al.|[2605.19944v1](http://arxiv.org/abs/2605.19944v1)|null|
|**2026-05-19**|**Probabilistic Tiny Recursive Model**|Amin Sghaier et.al.|[2605.19943v1](http://arxiv.org/abs/2605.19943v1)|null|
|**2026-05-19**|**Robotics-Inspired Guardrails for Foundation Models in Socially Sensitive Domains**|Rebecca Ramnauth et.al.|[2605.19940v1](http://arxiv.org/abs/2605.19940v1)|null|
|**2026-05-19**|**What Are LLMs Doing to Scientific Communication? Measuring Changes in Writing Practices and Reading Experience**|Filip Miletić et.al.|[2605.19936v1](http://arxiv.org/abs/2605.19936v1)|null|
|**2026-05-19**|**PEEK: Context Map as an Orientation Cache for Long-Context LLM Agents**|Zhuohan Gu et.al.|[2605.19932v1](http://arxiv.org/abs/2605.19932v1)|null|
|**2026-05-19**|**StruMPL: Multi-task Dense Regression under Disjoint Partial Supervision and MNAR Labels**|Reza M. Asiyabi et.al.|[2605.19931v1](http://arxiv.org/abs/2605.19931v1)|null|
|**2026-05-19**|**Breaking Modality Heterogeneity in Low-Bit Quantization for Large Vision-Language Models**|Yi Zhong et.al.|[2605.19929v1](http://arxiv.org/abs/2605.19929v1)|null|
|**2026-05-19**|**Where Does Authorship Signal Emerge in Encoder-Based Language Models?**|Francis Kulumba et.al.|[2605.19908v1](http://arxiv.org/abs/2605.19908v1)|null|
|**2026-05-19**|**Streamlined Constraint Reasoning via CNN Pattern Recognition on Enumerated Solutions**|Patrick Spracklen et.al.|[2605.19895v1](http://arxiv.org/abs/2605.19895v1)|null|
|**2026-05-19**|**Deep Tech to Space: Space Data Centers and AI Revolution at the Edge**|Jonas Weiss et.al.|[2605.19892v1](http://arxiv.org/abs/2605.19892v1)|null|
|**2026-05-19**|**StableGrad: Backward Scale Control without Batch Normalization**|Jose I. Mestre et.al.|[2605.19856v1](http://arxiv.org/abs/2605.19856v1)|null|
|**2026-05-19**|**A Framework for Evaluating Zero-Shot Image Generation in Concept-based Explainability**|Giacomo Astolfi et.al.|[2605.19855v1](http://arxiv.org/abs/2605.19855v1)|null|
|**2026-05-19**|**Are Tools Always Beneficial? Learning to Invoke Tools Adaptively for Dual-Mode Multimodal LLM Reasoning**|Qinghe Ma et.al.|[2605.19852v1](http://arxiv.org/abs/2605.19852v1)|null|
|**2026-05-19**|**CLIF: Concept-Level Influence Functions for Transparent Bottleneck Models**|Yike Sun et.al.|[2605.19848v1](http://arxiv.org/abs/2605.19848v1)|null|
|**2026-05-19**|**FineBench: Benchmarking and Enhancing Vision-Language Models for Fine-grained Human Activity Understanding**|Gueter Josmy Faure et.al.|[2605.19846v1](http://arxiv.org/abs/2605.19846v1)|null|
|**2026-05-19**|**CADENet: Condition-Adaptive Asynchronous Dual-Stream Enhancement Network for Adverse Weather Perception in Autonomous Driving**|Sherif Khairy et.al.|[2605.19837v1](http://arxiv.org/abs/2605.19837v1)|null|
|**2026-05-19**|**Mega-ASR: Towards In-the-wild^2 Speech Recognition via Scaling up Real-world Acoustic Simulation**|Zhifei Xie et.al.|[2605.19833v1](http://arxiv.org/abs/2605.19833v1)|null|
|**2026-05-19**|**Explainable Wastewater Digital Twins: Adaptive Context-Conditioned Structured Simulators with Self-Falsifying Decision Support**|Gary Simethy et.al.|[2605.19826v1](http://arxiv.org/abs/2605.19826v1)|null|
|**2026-05-19**|**From Prompts to Pavement Through Time: Temporal Grounding in Agentic Scene-to-Plan Reasoning**|Ahmed Y. Gado et.al.|[2605.19824v1](http://arxiv.org/abs/2605.19824v1)|null|
|**2026-05-19**|**Smooth Piecewise Cutting for Neural Operator to Handle Discontinuities and Sharp Transitions**|Ha Dang et.al.|[2605.19823v1](http://arxiv.org/abs/2605.19823v1)|null|
|**2026-05-19**|**LP-Eval: Rubric and Dataset for Measuring the Quality of Legal Proposition Generation**|Shanshan Xu et.al.|[2605.19815v1](http://arxiv.org/abs/2605.19815v1)|null|
|**2026-05-19**|**FLUXtrapolation: A benchmark on extrapolating ecosystem fluxes**|Anya Fries et.al.|[2605.19812v1](http://arxiv.org/abs/2605.19812v1)|null|
|**2026-05-19**|**Chunking German Legal Code**|Max Prior et.al.|[2605.19806v1](http://arxiv.org/abs/2605.19806v1)|null|
|**2026-05-19**|**Latent Laplace Diffusion for Irregular Multivariate Time Series**|Zinuo You et.al.|[2605.19805v1](http://arxiv.org/abs/2605.19805v1)|null|
|**2026-05-19**|**Stitched Value Model for Diffusion Alignment**|Hyojun Go et.al.|[2605.19804v1](http://arxiv.org/abs/2605.19804v1)|null|
|**2026-05-19**|**Synergistic Foundation Models for Semi-Supervised Fetal Cardiac Ultrasound Analysis: SAM-Med2D Boundary Refinement and DINOv3 Semantic Enhancement**|Tonghao Zhuang et.al.|[2605.19799v1](http://arxiv.org/abs/2605.19799v1)|null|
|**2026-05-19**|**Towards Trust Calibration in Socially Interactive Agents: Investigating Gendered Multimodal Behaviors Generation with LLMs**|Lucie Galland et.al.|[2605.19798v1](http://arxiv.org/abs/2605.19798v1)|null|
|**2026-05-19**|**Prior Knowledge or Search? A Study of LLM Agents in Hardware-Aware Code Optimization**|Dmitry Redko et.al.|[2605.19782v1](http://arxiv.org/abs/2605.19782v1)|null|
|**2026-05-19**|**From SGD to Muon: Adaptive Optimization via Schatten-p Norms**|Thomas Massena et.al.|[2605.19781v1](http://arxiv.org/abs/2605.19781v1)|null|
|**2026-05-19**|**OpenComputer: Verifiable Software Worlds for Computer-Use Agents**|Jinbiao Wei et.al.|[2605.19769v1](http://arxiv.org/abs/2605.19769v1)|null|
|**2026-05-19**|**Minimax Optimal Variance-Aware Regret Bounds for Multinomial Logistic MDPs**|Pierre Boudart et.al.|[2605.19768v1](http://arxiv.org/abs/2605.19768v1)|null|
|**2026-05-19**|**AR1-ZO: Topology-Aware Rank-1 Zeroth-Order Queries for High-Rank LoRA Fine-Tuning**|Ziye Chen et.al.|[2605.19767v1](http://arxiv.org/abs/2605.19767v1)|null|
|**2026-05-19**|**Synthesis and Evaluation of Long-term History-aware Medical Dialogue**|Hebin Hu et.al.|[2605.19766v1](http://arxiv.org/abs/2605.19766v1)|null|
|**2026-05-19**|**What Really Improves Mathematical Reasoning: Structured Reasoning Signals Beyond Pure Code**|Yuze Zhao et.al.|[2605.19762v1](http://arxiv.org/abs/2605.19762v1)|null|
|**2026-05-19**|**CogScale: Scalable Benchmark for Sequence Processing**|Yannis Bendi-Ouis et.al.|[2605.19758v1](http://arxiv.org/abs/2605.19758v1)|null|
|**2026-05-19**|**Memory-Augmented Reinforcement Learning Agent for CAD Generation**|Yin Xiaolong et.al.|[2605.19748v1](http://arxiv.org/abs/2605.19748v1)|null|
|**2026-05-19**|**EngiAI: A Multi-Agent Framework and Benchmark Suite for LLM-Driven Engineering Design**|Gioele Molinari et.al.|[2605.19743v1](http://arxiv.org/abs/2605.19743v1)|null|
|**2026-05-19**|**TERGAD: Structure-Aware Text-Enhanced Representations for Graph Anomaly Detection**|Wen Shi et.al.|[2605.19738v1](http://arxiv.org/abs/2605.19738v1)|null|
|**2026-05-19**|**ContextRAG: Extraction-Free Hierarchical Graph Construction for Retrieval-Augmented Generation**|Roman Prosvirnin et.al.|[2605.19735v1](http://arxiv.org/abs/2605.19735v1)|null|
|**2026-05-19**|**LIFT and PLACE: A Simple, Stable, and Effective Knowledge Distillation Framework for Lightweight Diffusion Models**|Hyunsoo Han et.al.|[2605.19729v1](http://arxiv.org/abs/2605.19729v1)|null|
|**2026-05-19**|**Mathematical Reasoning in Large Language Models: Benchmarks, Architectures, Evaluation, and Open Challenges**|Husnain Amjad et.al.|[2605.19723v1](http://arxiv.org/abs/2605.19723v1)|null|
|**2026-05-19**|**Measuring Safety Alignment Effects in Autonomous Security Agents**|Isaac David et.al.|[2605.19722v1](http://arxiv.org/abs/2605.19722v1)|null|
|**2026-05-19**|**CAIT: A Syntactic Parsing Toolkit for Child-Adult InTeractions**|Francesca Padovani et.al.|[2605.19718v1](http://arxiv.org/abs/2605.19718v1)|null|
|**2026-05-19**|**LLM-Based Financial Sentiment Analysis in Arabic: Evidence from Saudi Markets**|Mona H. Albaqawi et.al.|[2605.19714v1](http://arxiv.org/abs/2605.19714v1)|null|
|**2026-05-19**|**Can Large Language Models Reliably Correct Errors in Low-Resource ASR? A Contamination-Aware Case Study on West Frisian**|Yun Hao et.al.|[2605.19711v1](http://arxiv.org/abs/2605.19711v1)|null|
|**2026-05-19**|**Beyond Rational Illusion: Behaviorally Realistic Strategic Classification**|Xinpeng Lv et.al.|[2605.19674v1](http://arxiv.org/abs/2605.19674v1)|null|
|**2026-05-19**|**CriterAlign: Criterion-Centric Rationale Alignment for Code Preference Judging**|Zhenyu Li et.al.|[2605.19665v1](http://arxiv.org/abs/2605.19665v1)|null|
|**2026-05-19**|**Pseudocode-Guided Structured Reasoning for Automating Reliable Inference in Vision-Language Models**|Weicong Ni et.al.|[2605.19663v1](http://arxiv.org/abs/2605.19663v1)|null|
|**2026-05-19**|**When Tabular Foundation Models Meet Strategic Tabular Data: A Prior Alignment Approach**|Xinpeng Lv et.al.|[2605.19662v1](http://arxiv.org/abs/2605.19662v1)|null|
|**2026-05-19**|**OScaR: The Occam's Razor for Extreme KV Cache Quantization in LLMs and Beyond**|Zunhai Su et.al.|[2605.19660v1](http://arxiv.org/abs/2605.19660v1)|null|
|**2026-05-19**|**K-Quantization and its Impact on Output Performance**|Robin Baki Davidsson et.al.|[2605.19645v1](http://arxiv.org/abs/2605.19645v1)|null|
|**2026-05-19**|**The Accessibility Capability Boundary: Operational Limits and Expansion Potential of AI-Generated Browser-Native Accessibility Systems**|Rizwan Jahangir et.al.|[2605.19638v1](http://arxiv.org/abs/2605.19638v1)|null|
|**2026-05-19**|**P2DNav: Panorama-to-Downview Reasoning for Zero-shot Vision-and-Language Navigation**|Kai Sheng et.al.|[2605.19634v1](http://arxiv.org/abs/2605.19634v1)|null|
|**2026-05-19**|**optimize_anything: A Universal API for Optimizing any Text Parameter**|Lakshya A Agrawal et.al.|[2605.19633v1](http://arxiv.org/abs/2605.19633v1)|null|
|**2026-05-19**|**EMO-BOOST: Emotion-Augmented Audio-Visual Features for Improved Generalization in Deepfake Detection**|Aritra Marik et.al.|[2605.19630v1](http://arxiv.org/abs/2605.19630v1)|null|
|**2026-05-19**|**MiMuon: Mixed Muon Optimizer with Improved Generalization for Large Models**|Feihu Huang et.al.|[2605.19619v1](http://arxiv.org/abs/2605.19619v1)|null|
|**2026-05-19**|**Formal Skill: Programmable Runtime Skills for Efficient and Accurate LLM Agents**|Xi Zhang et.al.|[2605.19604v1](http://arxiv.org/abs/2605.19604v1)|null|

#### Abstracts
##### **Atoms of Thought: Universal EEG Representation Learning with Microstates**
2605.20182v1 by Xinyang Tian, Ruitao Liu, Ziyi Ye, Siyang Xue, Xin Wang, Xuesong Chen

Learning universal representations from electroencephalogram (EEG) signals is a cutting-edge approach in the field of neuroinformatics and brain-computer interfaces (BCIs). Conventionally, EEG is treated as a multivariate temporal signal, where time- or frequency-domain features are extracted for representation learning. This paper investigates a simple yet effective EEG representation, i.e., microstates. Microstates represent the building blocks of brain activity patterns at a microscopic time scale. We build a universal microstate tokenizer from a large medical EEG dataset by clustering continuous EEG signals into sequences of discrete microstates. The microstate tokenizer is then adopted universally across a series of downstream tasks, including sleep staging, emotion recognition, and motor imagery classification. Experimental results show that EEG representation learning with microstates outperforms traditional time-domain and frequency-domain features under different models and across different tasks. Further analysis shows that microstates offer greater interpretability and scalability, thereby opening up applications in both cognitive neuroscience and clinical research.

摘要：學習來自腦電圖（EEG）信號的通用表示法是神經資訊學和腦機介面（BCI）領域的一種前沿方法。傳統上，EEG被視為一種多變量時間信號，從中提取時間或頻率域特徵以進行表示學習。本文探討了一種簡單但有效的EEG表示法，即微狀態。微狀態代表了在微觀時間尺度上大腦活動模式的基本構件。我們通過將連續EEG信號聚類為離散微狀態序列，從一個大型醫療EEG數據集構建了一個通用的微狀態標記器。然後，微狀態標記器在一系列下游任務中被普遍採用，包括睡眠分期、情緒識別和運動意象分類。實驗結果顯示，使用微狀態的EEG表示學習在不同模型和不同任務下優於傳統的時間域和頻率域特徵。進一步分析顯示，微狀態提供了更大的可解釋性和可擴展性，從而為認知神經科學和臨床研究開啟了應用的可能性。

##### **TIDE: Efficient and Lossless MoE Diffusion LLM Inference with I/O-aware Expert Offload**
2605.20179v1 by Zhiben Chen, Youpeng Zhao, Yang Sui, Jun Wang, Yuzhang Shang

Diffusion Large Language Models (dLLMs) have emerged as a competitive alternative to autoregressive (AR) models, offering better hardware utilization and bidirectional context through parallel block-level decoding. However, as dLLMs continue to scale up with mixture-of-experts (MoE) architectures, their deployment on resource-constrained devices remains an open challenge. Existing AR-based methods often incur either prohibitive I/O overhead or significant compute bottlenecks. In this work, we propose TIDE, a novel resource-efficient inference system that leverages the temporal stability of expert activations during the diffusion process within the block. Specifically, we leverage the temporal stability of expert activations during the diffusion process within the block and introduce an interval-based expert refresh strategy that updates the expert placement in an I/O-aware fashion. To ensure optimal performance, we formulate the inference scheduling as a mathematical programming problem, solving for the optimal interval that minimizes I/O traffic and CPU computation. Most importantly, TIDE is a lossless optimization that requires no model training, providing a "free lunch" acceleration for dLLM inference. In a single GPU-CPU system, we demonstrate that TIDE achieves up to 1.4$\times$ and 1.5$\times$ throughput improvements over prior baselines on LLaDA2.0-mini and LLaDA2.0-flash models, respectively.

摘要：擴散大型語言模型 (dLLMs) 已經成為自回歸 (AR) 模型的一種競爭替代方案，通過平行區塊級解碼提供更好的硬體利用率和雙向上下文。<lb/>然而，隨著 dLLMs 在混合專家 (MoE) 架構中不斷擴展，它們在資源受限設備上的部署仍然是一個未解決的挑戰。<lb/>現有的基於 AR 的方法通常會產生高昂的 I/O 開銷或顯著的計算瓶頸。<lb/>在本研究中，我們提出了 TIDE，一種新穎的資源高效推理系統，利用區塊內擴散過程中專家激活的時間穩定性。<lb/>具體而言，我們利用區塊內擴散過程中專家激活的時間穩定性，並引入一種基於間隔的專家刷新策略，以 I/O 感知的方式更新專家位置。<lb/>為了確保最佳性能，我們將推理調度公式化為數學規劃問題，解決最小化 I/O 流量和 CPU 計算的最佳間隔。<lb/>最重要的是，TIDE 是一種無損優化，無需模型訓練，為 dLLM 推理提供了“免費午餐”的加速。<lb/>在單個 GPU-CPU 系統中，我們展示了 TIDE 在 LLaDA2.0-mini 和 LLaDA2.0-flash 模型上分別實現了高達 1.4$\times$ 和 1.5$\times$ 的吞吐量提升，相對於之前的基準。

##### **From Seeing to Thinking: Decoupling Perception and Reasoning Improves Post-Training of Vision-Language Models**
2605.20177v1 by Juncheng Wu, Hardy Chen, Haoqin Tu, Xianfeng Tang, Freda Shi, Hui Liu, Hanqing Lu, Cihang Xie, Yuyin Zhou

Recent advances in vision-language models (VLMs) emphasize long chain-of-thought reasoning; yet, we find that their performance on visual tasks is primarily limited by a lack of visual perception as opposed to reasoning itself. In this work, we systematically study the interplay between perception and reasoning in VLM post-training by decomposing their capabilities into three separate training stages: visual perception, visual reasoning, and textual reasoning, incorporating specialized training data. We demonstrate that visual perception (a) requires targeted optimization with specialized data; (b) serves as a fundamental scaffold that should be solidified through staged training before refining visual reasoning; and (c) is more effectively learned via RL than caption-based SFT. Our experiments across multiple VLMs demonstrate that staged training consistently improves both visual perception and reasoning performance over merged training. Notably, models trained with our approach achieve 1.5% higher reasoning accuracy with 20.8% shorter reasoning traces, suggesting that superior perception reduces the need for excessive reasoning. Furthermore, we show that this capability-based staging represents a new curriculum dimension orthogonal to traditional difficulty-based curricula, and combining both yields further additive gains. Our staged-training models achieve superior performance among open-weight VLMs, establishing advanced results on several visual math and perception (e.g., +5.2% on WeMath and +3.7% on RealWorldQA) tasks compared with the base counterpart.

摘要：最近在視覺-語言模型（VLMs）方面的進展強調了長鏈思考推理；然而，我們發現它們在視覺任務上的表現主要受到視覺感知不足的限制，而非推理本身。在這項工作中，我們系統性地研究了VLM後訓練中感知與推理之間的相互作用，通過將其能力分解為三個獨立的訓練階段：視覺感知、視覺推理和文本推理，並納入專門的訓練數據。我們證明了視覺感知（a）需要針對性優化與專門數據；（b）作為一個基本的支架，應通過分階段訓練來鞏固，然後再精煉視覺推理；以及（c）通過強化學習（RL）比基於標題的SFT更有效地學習。我們在多個VLM上的實驗表明，分階段訓練始終能改善視覺感知和推理性能，相較於合併訓練。值得注意的是，採用我們的方法訓練的模型在推理準確率上提高了1.5%，推理痕跡縮短了20.8%，這表明更優的感知減少了過度推理的需求。此外，我們顯示這種基於能力的分階段代表了一個新的課程維度，與傳統的基於難度的課程正交，並且將兩者結合會帶來進一步的增益。我們的分階段訓練模型在開放權重的VLM中表現出色，與基準模型相比，在幾個視覺數學和感知（例如，WeMath上提高5.2%和RealWorldQA上提高3.7%）任務上建立了先進的結果。

##### **ClinSeekAgent: Automating Multimodal Evidence Seeking for Agentic Clinical Reasoning**
2605.20176v1 by Juncheng Wu, Letian Zhang, Yuhan Wang, Haoqin Tu, Hardy Chen, Zijun Wang, Cihang Xie, Yuyin Zhou

Large language models (LLMs) and agentic systems have shown promise for clinical decision support, but existing works largely assume that evidence has already been curated and handed to the model. Real-world clinical workflows instead require agents to actively seek, iteratively plan, and synthesize multimodal evidence from heterogeneous sources. In this paper, we introduce ClinSeekAgent, an automated agentic framework for dynamic multimodal evidence seeking that shifts the paradigm from passive evidence consumption to active evidence acquisition. Given only a clinical query and access to raw data sources, ClinSeekAgent gathers evidence by querying medical knowledge bases, navigating raw EHRs, and invoking medical imaging tools; refines its hypotheses as new information emerges; and integrates the collected evidence into grounded clinical decisions. ClinSeekAgent serves both as an inference-time agent for frontier LLMs and as a training-time pipeline for distilling high-quality agent trajectories into compact open-source models. To validate its inference-time effectiveness, we construct ClinSeek-Bench, which pairs Curated Input reasoning from fixed pre-selected evidence with Automated Evidence-Seeking over raw clinical data. On text-only EHR tasks, ClinSeekAgent improves Claude Opus 4.6 from 60.0 to 63.2 overall F1 and MiniMax M2.5 from 43.1 to 47.3, with positive risk-prediction gains in 7 out of 9 evaluated host models. On multimodal tasks, ClinSeekAgent improves Claude Opus 4.6 from 47.5 to 62.6 (+15.1); all evaluated models improve across the three CXR-related task groups. We further validate ClinSeekAgent as a training pipeline by distilling agentic evidence-seeking trajectories into ClinSeek-35B-A3B, which achieves 34.0 average F1 on existing AgentEHR-Bench, improving over its Qwen3.5-35B-A3B baseline by +11.9 points and approaching Claude Opus 4.6.

摘要：大型語言模型（LLMs）和代理系統在臨床決策支持方面顯示出潛力，但現有的研究大多假設證據已經被整理並交給模型。實際的臨床工作流程則要求代理主動尋找、迭代計劃，並從異質來源綜合多模態證據。在本文中，我們介紹了ClinSeekAgent，一個自動化的代理框架，用於動態多模態證據尋求，將範式從被動的證據消耗轉變為主動的證據獲取。僅根據臨床查詢和對原始數據來源的訪問，ClinSeekAgent通過查詢醫療知識庫、導航原始電子健康記錄（EHR）和調用醫療影像工具來收集證據；隨著新信息的出現，不斷完善其假設；並將收集到的證據整合為有根據的臨床決策。ClinSeekAgent既作為前沿LLMs的推理時代理，也作為訓練時管道，用於將高質量的代理軌跡提煉成緊湊的開源模型。為了驗證其推理時的有效性，我們構建了ClinSeek-Bench，將固定預選證據的整理輸入推理與對原始臨床數據的自動證據尋求配對。在僅文本的EHR任務中，ClinSeekAgent將Claude Opus 4.6的整體F1從60.0提高到63.2，將MiniMax M2.5從43.1提高到47.3，在9個評估的主機模型中，有7個模型的風險預測獲得了正向增益。在多模態任務中，ClinSeekAgent將Claude Opus 4.6的分數從47.5提高到62.6（+15.1）；所有評估的模型在三個與CXR相關的任務組中均有所改善。我們進一步通過將代理證據尋求軌跡提煉成ClinSeek-35B-A3B來驗證ClinSeekAgent作為訓練管道的有效性，該模型在現有的AgentEHR-Bench上達到34.0的平均F1，較其Qwen3.5-35B-A3B基準提高了+11.9點，並接近Claude Opus 4.6。

##### **A Methodology for Selecting and Composing Runtime Architecture Patterns for Production LLM Agents**
2605.20173v1 by Vasundra Srinivasan

Production LLM agents combine stochastic model outputs with deterministic software systems, yet the boundary between the two is rarely treated as a first-class architectural object. This paper names that boundary the stochastic-deterministic boundary (SDB): a four-part contract among a proposer, verifier, commit step, and reject signal that specifies how an LLM output becomes a system action. We argue that the SDB is the load-bearing primitive of production agent runtimes.
  Around this primitive, we organize agent runtime design into three concerns: Coordination, State, and Control. We present a catalog of six runtime patterns that compose the SDB differently across conversational, autonomous, and long-horizon agents: hierarchical delegation, scatter-gather plus saga, event-driven sequencing, shared state machine, supervisor plus gate, and human in the loop. For each pattern, we trace its lineage to distributed-systems concepts and identify what changes when the worker is stochastic.
  The paper contributes a five-step methodology for selecting runtime patterns, a diagnostic procedure that maps production failures to pattern weaknesses, and a failure mode called replay divergence, in which LLM-based consumers of a deterministic event log produce different downstream outputs under model-version or prompt changes. A stylized reliability decomposition separates per-call model variance from architectural momentum, motivating the claim that as model variance decreases, pattern choice and SDB strength become increasingly important levers for long-run reliability. We apply the methodology to five workloads and provide one runnable reference implementation for a 90-day contract-renewal agent.

摘要：生產 LLM 代理結合隨機模型輸出與確定性軟體系統，但兩者之間的邊界很少被視為一個一級架構對象。本文將該邊界命名為隨機-確定性邊界（SDB）：這是一個由提議者、驗證者、承諾步驟和拒絕信號組成的四部分契約，指定了 LLM 輸出如何轉化為系統行動。我們主張 SDB 是生產代理運行時的承載原始物件。<lb/>圍繞這一原始物件，我們將代理運行時設計組織為三個關注點：協調、狀態和控制。我們提出了一個目錄，列出六種運行時模式，這些模式在對話式、自主式和長期代理中以不同方式組成 SDB：層級委派、散佈-聚集加上 saga、事件驅動序列、共享狀態機、監督者加上閘、以及人類在迴路中。對於每種模式，我們追溯其與分散式系統概念的血統，並識別當工作者是隨機時發生的變化。<lb/>本文貢獻了一個五步驟的方法論，用於選擇運行時模式，一個將生產失敗映射到模式弱點的診斷程序，以及一種稱為重放偏差的失敗模式，在這種模式下，基於 LLM 的確定性事件日誌消費者在模型版本或提示變更下產生不同的下游輸出。一個風格化的可靠性分解將每次調用的模型變異與架構動量分開，促使我們聲稱，隨著模型變異的減少，模式選擇和 SDB 強度成為長期可靠性越來越重要的槓桿。我們將這一方法論應用於五個工作負載，並提供一個可運行的參考實現，用於 90 天的合約續約代理。

##### **Long-term Power Grid Planning via Answer Set Programming**
2605.20172v1 by Antonio Ielo, Francesco Doria, Sandra Castellanos-Paez, Marco Maratea, Francesco Percassi, Mauro Vallati

The Power grid is a critical infrastructure underpinning all aspects of modern society and its services. Maintaining its effectiveness requires continuous adaptations. In particular, addressing sustainability targets, demand patterns, and urbanisation trends requires implementing changes to the network. Actual developments can potentially span over a decade, with supply continuity and service quality that must be preserved throughout by ensuring conformance to several topological and combinatorial invariants. Long-term power grid planning deals with the above process, and although planning languages could be a natural choice, the kind of properties and invariants needed are cumbersome to express in such languages; on the contrary, they can be elegantly and succinctly encoded in Answer Set Programming (ASP). In this paper, we propose the first approach to automate and optimise the long-term power grid planning process using ASP. Experimental evaluations conducted on synthetic and real-world grid data confirm the expressive power of the proposed ASP-based approach and demonstrate its effectiveness.

摘要：電力網是支撐現代社會及其服務各個方面的關鍵基礎設施。<lb/>維持其有效性需要持續的適應。<lb/>特別是，應對可持續性目標、需求模式和城市化趨勢需要對網絡進行變更。<lb/>實際的發展可能會跨越十年，供應連續性和服務質量必須在整個過程中保持，這需要確保遵守多個拓撲和組合不變量。<lb/>長期電力網規劃處理上述過程，儘管規劃語言可能是自然的選擇，但所需的屬性和不變量在這些語言中表達起來相當繁瑣；相反，它們可以在答案集編程（ASP）中優雅且簡潔地編碼。<lb/>在本文中，我們提出了第一種利用ASP自動化和優化長期電力網規劃過程的方法。<lb/>在合成和真實世界電網數據上進行的實驗評估確認了所提議的基於ASP的方法的表達能力，並展示了其有效性。

##### **KoRe: Compact Knowledge Representations for Large Language Models**
2605.20170v1 by Davide Cavicchini, Fausto Giunchiglia, Jacopo Staiano

Modern Large Language Models (LLMs) have shown impressive performances in user-facing tasks such as question answering, as well as consistent improvements in reasoning capabilities. Still, the way these models encode knowledge seems inherently flawed: by design, LLMs encode world-knowledge within their parameters. This way of representing knowledge is inherently opaque, difficult to debug and update, and prone to hallucinations. On the other hand, Knowledge Graphs can provide human-readable and easily editable world knowledge representations, and their application in knowledge-intensive tasks has consistently proven beneficial to downstream performance. Nonetheless, current integration techniques require extensive retraining or finetuning. To overcome this issue, we introduce KoRe, a methodology to encode 1-hop sub-graphs into compact discrete knowledge tokens and inject them into a LLM backbone. We test the proposed approach on three established benchmarks, and report competitive performances coupled with a significant reduction (up to 10x) in token usage. Our results show that compact discrete KG representations can efficiently and effectively be used to ground modern LLMs.

摘要：現代大型語言模型（LLMs）在用戶面向的任務中，如問題回答，展現了令人印象深刻的表現，並且在推理能力上持續改進。儘管如此，這些模型編碼知識的方式似乎固有缺陷：根據設計，LLMs 在其參數中編碼世界知識。這種表示知識的方式本質上是不透明的，難以調試和更新，並且容易出現幻覺。另一方面，知識圖譜可以提供人類可讀且易於編輯的世界知識表示，並且在知識密集型任務中的應用一貫證明對下游性能有益。儘管如此，當前的整合技術需要大量的重新訓練或微調。為了解決這個問題，我們介紹了 KoRe，一種將 1-hop 子圖編碼為緊湊的離散知識標記並注入到 LLM 主幹中的方法。我們在三個已建立的基準上測試了所提出的方法，並報告了具有競爭力的表現，並顯著減少了（高達 10 倍）標記的使用。我們的結果顯示，緊湊的離散 KG 表示可以有效且高效地用於支持現代 LLMs。

##### **Not Every Rubric Teaches Equally: Policy-Aware Rubric Rewards for RLVR**
2605.20164v1 by Utkarsh Tyagi, Xingang Guo, MohammadHossein Rezaei, Daniel George, Anas Mahmoud, Jackson Lee, Bing Liu, Yunzhong He

Reinforcement learning with verifiable rewards has made post-training highly effective when correctness can be checked automatically. However, many important model behaviors require satisfying several qualitative criteria at once. Rubric-based rewards address this setting by grading prompt-specific criteria and aggregating them into a scalar reward. Yet standard static aggregations conflate a criterion's human-assigned importance with its current usefulness as an optimization signal. We show that this assumption breaks down in rubric RL: many important criteria are already saturated or currently unreachable, while criteria that distinguish rollouts are not necessarily those with the largest human weights. We introduce POW3R, a policy-aware rubric reward framework that preserves human weights and category balance as the rubric objective while adapting criterion-level reward weights during training. POW3R uses rollout-level contrast to emphasize criteria that currently separate the policy's outputs, making the GRPO reward more informative without changing the underlying evaluation target. Across three base policies on two datasets spanning multimodal and text-only settings, POW3R wins $24$ of $30$ base-policy/metric comparisons, improving both mean rubric reward and strict completion (the fraction of prompts whose response satisfies every required rubric criterion) over vanilla GRPO with rubric rewards, and reaches the same plateau in $2.5$--$4\times$ fewer training steps. Rubric rewards should therefore distinguish what should matter in the final answer from what can teach the current policy.

摘要：強化學習與可驗證獎勵的結合使得後訓練在正確性可以自動檢查時變得非常有效。<lb/>然而，許多重要的模型行為需要同時滿足幾個質性標準。<lb/>基於評分標準的獎勵通過對特定提示標準進行評分並將其聚合成標量獎勵來解決這一情況。<lb/>然而，標準的靜態聚合將標準的人工指定重要性與其作為優化信號的當前有效性混為一談。<lb/>我們顯示這一假設在評分標準強化學習中失效：許多重要的標準已經飽和或當前無法達到，而區分回合的標準不一定是那些擁有最大人工權重的標準。<lb/>我們提出了POW3R，一個政策感知的評分標準獎勵框架，該框架在訓練過程中保留人工權重和類別平衡作為評分標準目標，同時調整標準級獎勵權重。<lb/>POW3R使用回合級對比來強調當前區分政策輸出的標準，使GRPO獎勵更具信息性而不改變基礎評估目標。<lb/>在兩個涵蓋多模態和僅文本設置的數據集上，對三個基礎政策進行的比較中，POW3R在30個基礎政策/指標比較中贏得了24個，並在均值評分獎勵和嚴格完成（滿足每個所需評分標準的提示響應的比例）上超過了傳統的GRPO與評分標準獎勵，並在2.5到4倍更少的訓練步驟中達到了相同的平臺。<lb/>因此，評分標準獎勵應該區分最終答案中應該重要的內容與能夠教導當前政策的內容。

##### **Rethinking Visual Attribution for Chest X-ray Reasoning in Large Vision Language Models**
2605.20158v1 by Guangzhi Xiong, Qiao Jin, Sanchit Sinha, Zhiyong Lu, Aidong Zhang

Large Vision Language Models (LVLMs) show promise in medical applications, but their inability to faithfully ground responses in visual evidence raises serious concerns about clinical trustworthiness. While visual attribution methods are widely used to explain LVLM predictions, whether these explanations actually reflect the visual evidence underlying the model's decision is largely unverified, since ground-truth annotations for internal model reasoning are typically unavailable. We address this question for chest X-ray (CXR) reasoning by developing a causal evaluation framework that retains only CXR-VQA samples for which the expert-annotated region is verified, via counterfactual editing, to be causally responsible for the model's prediction. Using this framework across 11 attribution methods, six open-source LVLMs, and two output modes (direct answer and step-by-step reasoning), we find that existing attribution methods often fail to identify the evidence used by LVLMs. To address this failure, we propose MedFocus, a concept-based attribution method that localizes clinically meaningful anatomical regions via unbalanced optimal transport and measures their causal effect on model outputs through targeted interventions. MedFocus produces spatial, concept-level, and token-level attributions and substantially outperforms prior methods, taking a step toward more trustworthy attribution for medical LVLMs. Our data and code are available at https://github.com/gzxiong/medfocus/.

摘要：大型視覺語言模型（LVLMs）在醫療應用中顯示出潛力，但它們無法忠實地將回應與視覺證據相結合，這引發了對臨床可信度的嚴重擔憂。雖然視覺歸因方法被廣泛用來解釋LVLM的預測，但這些解釋是否實際反映了模型決策背後的視覺證據在很大程度上尚未得到驗證，因為內部模型推理的真實標註通常無法獲得。我們通過開發一個因果評估框架來解決這一問題，該框架僅保留那些經過專家標註並通過反事實編輯驗證為對模型預測具有因果責任的CXR-VQA樣本。利用這一框架，我們對11種歸因方法、六個開源LVLM和兩種輸出模式（直接回答和逐步推理）進行了研究，發現現有的歸因方法往往無法識別LVLM所使用的證據。為了解決這一失敗，我們提出了MedFocus，一種基於概念的歸因方法，通過不平衡最優運輸定位臨床上有意義的解剖區域，並通過針對性干預測量它們對模型輸出的因果影響。MedFocus生成空間、概念級和標記級的歸因，並顯著超越先前的方法，朝著為醫療LVLM提供更可信的歸因邁出了一步。我們的數據和代碼可在 https://github.com/gzxiong/medfocus/ 獲得。

##### **Less Back-and-Forth: A Comparative Study of Structured Prompting**
2605.20149v1 by Saurav Ghosh, Gabriella Polach, Abdou Sow

Large language models (LLMs) are widely used for open-ended tasks, but underspecified prompts can lead to low-quality answers and additional interaction. This paper studies whether structured prompt design improves response quality while reducing user effort. We compare three prompt conditions: a raw prompt, a checklist-improved prompt, and a clarifying-question prompt. We evaluate these conditions across four task types--summarization, planning, explanation, and coding--using three LLM systems: ChatGPT, Claude, and Grok. Each output is scored with a unified rubric covering task completion, correctness, compliance, and clarity. Checklist-improved prompts achieved the highest mean rubric score, 7.50 out of 8, compared with 5.67 for raw prompts and 6.67 for clarifying-question prompts. Checklist prompts also produced the best quality-effort tradeoff, using fewer average tokens than both raw and clarifying prompts. These results suggest that a simple prompt checklist can improve LLM responses while reducing unnecessary interaction.

摘要：大型語言模型（LLMs）廣泛用於開放式任務，但不明確的提示可能導致低質量的答案和額外的互動。本文研究結構化提示設計是否能改善回應質量，同時減少用戶的努力。我們比較了三種提示條件：原始提示、經檢查表改進的提示和澄清問題提示。我們在四種類型的任務中評估這些條件——摘要、規劃、解釋和編碼——使用三個LLM系統：ChatGPT、Claude和Grok。每個輸出都根據一個統一的評分標準進行評分，涵蓋任務完成度、正確性、合規性和清晰度。經檢查表改進的提示達到了最高的平均評分，為7.50（滿分8分），相比之下，原始提示為5.67，澄清問題提示為6.67。檢查表提示還產生了最佳的質量-努力權衡，平均使用的標記數量少於原始提示和澄清提示。這些結果表明，簡單的提示檢查表可以改善LLM的回應，同時減少不必要的互動。

##### **MixRea: Benchmarking Explicit-Implicit Reasoning in Large Language Models**
2605.20128v1 by Yuanqing Cai, Ziyi Huang, Minhao Liu, Lixin Duan, Wen Li, Yanru Zhang

Large language models (LLMs) are increasingly integrated into high-stakes decision-making. Inspired by the theory of \emph{inattentional blindness} in human cognition, we investigate whether LLMs, trained on human-preferred corpora that embed attentional biases, exhibit a similar limitation: \emph{failing to attend to subtle yet important contextual cues under explicit task instructions}. To evaluate this, we introduce the task of \textbf{explicit-implicit reasoning} and present \textbf{MixRea}, a benchmark of 2,246 multiple-choice questions across 9 reasoning types with varying distributions of explicit and implicit information. Evaluation of 21 advanced LLMs shows that even the best-performing reasoning model (Gemini 2.5 Pro) achieves only 42.8\% consistency, revealing widespread inattentional blindness. To mitigate this, we propose \textbf{Potential Relation Completion Prompting (PRCP)}, a prompting method that improves reasoning by recovering overlooked causal relations. Further analysis shows that this limitation persists across diverse multi-source reasoning tasks, highlighting the need for more cognitively aligned models.

摘要：大型語言模型（LLMs）越來越多地應用於高風險的決策中。受到人類認知中\emph{注意盲點}理論的啟發，我們研究LLMs是否會在訓練於嵌入注意偏見的人類偏好語料庫後，展現出類似的限制：\emph{在明確的任務指示下，未能注意到微妙但重要的上下文線索}。為了評估這一點，我們引入了\textbf{明確-隱含推理}的任務，並呈現\textbf{MixRea}，這是一個包含2,246個多選題的基準，涵蓋9種推理類型，並具有不同的明確和隱含信息分佈。對21個先進LLMs的評估顯示，即使是表現最佳的推理模型（Gemini 2.5 Pro）也僅達到42.8\%的一致性，揭示了普遍存在的注意盲點。為了減輕這一問題，我們提出了\textbf{潛在關係補全提示（PRCP）}，這是一種通過恢復被忽視的因果關係來改善推理的提示方法。進一步分析顯示，這一限制在多元來源的推理任務中仍然存在，突顯了對更符合認知的模型的需求。

##### **Beyond Prediction Accuracy: Target-Space Recovery Profiles for Evaluating Model-Brain Alignment**
2605.20127v1 by Ken Nakamura, Tomoya Nakai, Ryuto Yashiro, Ayumu Yamashita, Kaoru Amano

Artificial vision models are often evaluated against the human visual cortex by measuring how accurately their internal representations predict brain responses. However, prediction accuracy alone does not indicate which dimensions of the target brain's response space are recovered. Here, we introduce a unified framework for evaluating both model-brain and brain-brain alignment by identifying the response dimensions recovered by prediction. Using repeated fMRI measurements, we first identify target-brain response dimensions that can be reproducibly predicted across independent trial splits. We then predict target-brain responses from either another subject's brain responses or a vision model's internal representations, and quantify how strongly each of these reproducible response dimensions is recovered. Applying this framework to a subset of the Natural Scenes Dataset, in which eight subjects viewed the same natural images during fMRI, we find that the early-to-intermediate visual-cortex responses contain a low-dimensional set of reproducible dimensions. Brain-to-brain comparisons identify which of these dimensions are consistently recoverable from other subjects' brains, providing a diagnostic human reference rather than only a scalar benchmark. In some cases, pretrained and randomly initialized models achieve similar prediction accuracy while showing distinct recovery profiles across these response dimensions. These results show that prediction accuracy alone can mask model-brain mismatches. By making explicit which reproducible brain response dimensions are recovered by prediction, our framework provides a more diagnostic evaluation of alignment between artificial vision models and the human visual cortex.

摘要：人工視覺模型通常是通過測量其內部表徵預測大腦反應的準確性來評估與人類視覺皮層的對比。<lb/>然而，僅僅依賴預測準確性並不能指示目標大腦反應空間的哪些維度被恢復。<lb/>在這裡，我們介紹了一個統一的框架，用於通過識別預測所恢復的反應維度來評估模型與大腦之間以及大腦與大腦之間的對齊。<lb/>通過重複的 fMRI 測量，我們首先識別可以在獨立試驗分割中可重複預測的目標大腦反應維度。<lb/>然後，我們從另一個受試者的大腦反應或視覺模型的內部表徵中預測目標大腦反應，並量化每個可重複反應維度的恢復強度。<lb/>將這一框架應用於自然場景數據集的一個子集，在該子集中，八名受試者在 fMRI 中觀看相同的自然圖像，我們發現早期到中期的視覺皮層反應包含一組低維度的可重複維度。<lb/>大腦之間的比較識別出這些維度中哪些是可以從其他受試者的大腦中一致恢復的，提供了一個診斷性的人工參考，而不僅僅是一個標量基準。<lb/>在某些情況下，預訓練和隨機初始化的模型在預測準確性上達到相似的水平，同時在這些反應維度上顯示出不同的恢復特徵。<lb/>這些結果表明，僅僅依賴預測準確性可能會掩蓋模型與大腦之間的不匹配。<lb/>通過明確指出哪些可重複的大腦反應維度是通過預測恢復的，我們的框架提供了對人工視覺模型與人類視覺皮層之間對齊的更具診斷性的評估。

##### **Toto 2.0: Time Series Forecasting Enters the Scaling Era**
2605.20119v1 by Emaad Khwaja, Chris Lettieri, Gerald Woo, Eden Belouadah, Marc Cenac, Guillaume Jarry, Enguerrand Paquin, Xunyi Zhao, Viktoriya Zhukov, Othmane Abou-Amal, Chenghao Liu, Ameet Talwalkar, David Asker

We show that time series foundation models scale: a single training recipe produces reliable forecast-quality improvements from 4M to 2.5B parameters. We release Toto 2.0, a family of five open-weights forecasting models trained under this recipe. The Toto 2.0 family sets a new state of the art on three forecasting benchmarks: BOOM, our observability benchmark; GIFT-Eval, the standard general-purpose benchmark; and the recent contamination-resistant TIME benchmark. This report describes our experimental results and details the design decisions behind Toto 2.0: its architecture and training recipe, training data, and the u-muP hyperparameter transfer pipeline. All five base checkpoints are released under Apache 2.0.

摘要：我們展示了時間序列基礎模型的擴展性：單一的訓練配方能夠從 4M 擴展到 2.5B 參數，並產生可靠的預測質量改進。<lb/>我們發布了 Toto 2.0，一個由五個開放權重預測模型組成的系列，這些模型是在此配方下訓練的。<lb/>Toto 2.0 系列在三個預測基準上創下了新的最先進水平：BOOM，我們的可觀測性基準；GIFT-Eval，標準的通用基準；以及最近的抗污染 TIME 基準。<lb/>本報告描述了我們的實驗結果，並詳細說明了 Toto 2.0 背後的設計決策：其架構和訓練配方、訓練數據，以及 u-muP 超參數轉移管道。<lb/>所有五個基礎檢查點均在 Apache 2.0 下發布。

##### **k-Inductive Neural Barrier Certificates for Unknown Nonlinear Dynamics**
2605.20108v1 by Ben Wooding, Hongchao Zhang, Taylor T. Johnson, Abolfazl Lavaei

While conventional (k=1) discrete-time barrier certificate conditions impose strict safety constraints by requiring the function to be non-increasing at every step, k-inductive barrier certificates relax this by allowing a temporary increase -- up to k-1 times, each within a threshold $ε$ -- while maintaining overall safety, and improving flexibility. This paper leverages neural networks and constructs k-inductive neural barrier certificates (k-NBCs) for (partially) unknown nonlinear systems. While neural networks offer scalability in the design process, they lack formal guarantees, requiring additional approaches such as counterexample-guided inductive synthesis (CEGIS) with satisfiability modulo theories (SMT) for verification. However, the CEGIS-SMT framework requires knowledge of system dynamics, which is unavailable in practical settings. To address this, we leverage the generalization of the Willems et al.'s fundamental lemma, using a single state trajectory, to construct a data-driven representation of (partially) unknown models for SMT verification without sacrificing accuracy. Additionally, CEGIS-SMT further removes the constraint of restricting barrier certificates to specific function classes, such as sum-of-squares, enabling greater flexibility in their design. We validate our approach on three nonlinear case studies with (partially) unknown dynamics.

摘要：傳統的 (k=1) 離散時間障礙證明條件通過要求函數在每一步都非遞增來施加嚴格的安全約束，而 k-歸納障礙證明則通過允許臨時增加（最多 k-1 次，每次在閾值 $ε$ 內）來放寬這一要求，同時保持整體安全性並提高靈活性。本文利用神經網絡，為（部分）未知的非線性系統構建 k-歸納神經障礙證明 (k-NBCs)。雖然神經網絡在設計過程中提供了可擴展性，但它們缺乏正式的保證，這需要額外的方法，例如帶有可滿足性模組理論 (SMT) 的反例引導歸納合成 (CEGIS) 來進行驗證。然而，CEGIS-SMT 框架需要對系統動力學的了解，而這在實際環境中是不可用的。為了解決這個問題，我們利用 Willems 等人的基本引理的推廣，使用單一狀態軌跡，構建了一個數據驅動的（部分）未知模型的 SMT 驗證表示，而不犧牲準確性。此外，CEGIS-SMT 進一步消除了將障礙證明限制於特定函數類別（如平方和）的約束，從而在設計上提供了更大的靈活性。我們在三個具有（部分）未知動力學的非線性案例研究中驗證了我們的方法。

##### **Draft Less, Retrieve More: Hybrid Tree Construction for Speculative Decoding**
2605.20104v1 by Yuhao Shen, Tianyu Liu, Xinyi Hu, Quan Kong, Baolin Zhang, Jun Dai, Jun Zhang, Shuang Ge, Lei Chen, Yue Li, Mingcheng Wan, Cong Wang

Speculative decoding (SD) accelerates large language model inference by leveraging a draft-then-verify paradigm. To maximize the acceptance rate, recent methods construct expansive draft trees, which unfortunately incur severe VRAM bandwidth and computational overheads that bottleneck end-to-end speedups. While dynamic-depth pruning can reduce this latency by removing marginal branches, it also discards potentially valid candidates, preventing the acceptance rate from reaching the upper bound of dense trees. In this paper, we identify a critical opportunity in resource allocation: the transition from dense to pruned drafting frees up significant computational budget. To break this Pareto tradeoff, we introduce Graft, a compensation framework that couples pruning and retrieval as mutually reinforcing operations. Pruning supplies sufficient budget for retrieval, while retrieval compensates for pruning-induced coverage loss and recovers accepted length. By employing a sequential `prune-then-graft' mechanism, Graft attaches highly predictive retrieved tokens into positions opened by pruning, filling the topological gaps with near-zero overhead. Graft is entirely training-free and lossless. Comprehensive evaluations show that Graft establishes a new Pareto frontier across practical deployment settings, including short-context generation, long-context generation, and large-scale models. On short-context benchmarks, it achieves up to 5.41$\times$ speedup and improves average speedup over EAGLE-3 by up to 21.8% on the large-scale Qwen3-235B. We also provide a preliminary exploration of applying Graft to the DFlash-style block drafting paradigm, offering initial evidence and insights for extending grafting beyond autoregressive draft trees.

摘要：推測解碼（SD）透過利用草擬-然後-驗證的範式，加速大型語言模型的推理。為了最大化接受率，最近的方法構建了龐大的草擬樹，這不幸地帶來了嚴重的 VRAM 帶寬和計算開銷，成為端到端加速的瓶頸。雖然動態深度剪枝可以通過移除邊際分支來減少這種延遲，但它也會丟棄潛在的有效候選者，阻止接受率達到密集樹的上限。在本文中，我們識別出資源分配中的一個關鍵機會：從密集到剪枝草擬的過渡釋放了大量的計算預算。為了打破這一帕累托權衡，我們引入了 Graft，一個將剪枝和檢索作為相互增強操作的補償框架。剪枝為檢索提供了足夠的預算，而檢索則補償了因剪枝而造成的覆蓋損失並恢復接受的長度。通過採用順序的「剪枝-然後-嫁接」機制，Graft 將高度預測的檢索標記附加到剪枝所開放的位置，以近乎零的開銷填補拓撲空隙。Graft 完全無需訓練且無損。全面的評估顯示，Graft 在實際部署設置中建立了一個新的帕累托邊界，包括短上下文生成、長上下文生成和大規模模型。在短上下文基準測試中，它實現了高達 5.41$\times$ 的加速，並在大規模 Qwen3-235B 上將平均加速提高了高達 21.8%。我們還提供了將 Graft 應用於 DFlash 風格區塊草擬範式的初步探索，為將嫁接擴展到自回歸草擬樹提供了初步證據和見解。

##### **Neurosymbolic Learning for Inference-Time Argumentation**
2605.20098v1 by Gabriel Freedman, Adam Dejl, Adam Gould,  Mansi, Lihu Chen, Jianqi Jiang, Francesca Toni

Claim verification is an important problem in high-stakes settings, including health and finance. When information underpinning claims is incomplete or conflicting, uncertain answers may be more appropriate than binary true or false classifications. In all cases, faithful explanations of the considerations determining the final verdict are crucial. We introduce inference-time argumentation (ITA), a trainable neurosymbolic framework for ternary claim verification in which a formal argumentation semantics giving the strength of claims is used both (i) to guide LLM training as models learn to generate arguments and assign them base scores (representing intrinsic strengths) and (ii) to compute ternary (true/false/uncertain) predictions from generated, scored arguments. As a result, at training time, argument generation and scoring can be optimised according to the quality of the induced argumentative predictions. Moreover, at inference time, the final prediction is faithful, by construction, to the arguments and scores determining the verdict, rather than being justified by a potentially unfaithful post-hoc reasoning trace as in conventional reasoning models. We finally show that, on two datasets for ternary claim verification, ITA improves upon argumentative baselines and can perform competitively against non-argumentative direct-prediction baselines, while providing verdicts that are computed deterministically from explicit, inspectable argumentative structures.

摘要：主張驗證是一個在高風險環境下的重要問題，包括健康和金融領域。當支撐主張的信息不完整或相互矛盾時，不確定的答案可能比二元的真或假分類更為合適。在所有情況下，對決定最終裁決的考量進行忠實解釋是至關重要的。我們介紹了推理時論證（ITA），這是一個可訓練的神經符號框架，用於三元主張驗證，其中使用形式論證語義來給予主張的強度，並且(i)指導大型語言模型（LLM）的訓練，因為模型學會生成論證並為其分配基礎分數（代表內在強度），以及(ii)從生成的、有分數的論證中計算三元（真/假/不確定）預測。因此，在訓練期間，論證生成和評分可以根據誘導的論證預測的質量進行優化。此外，在推理時，最終預測在結構上忠實於決定裁決的論證和分數，而不是像傳統推理模型那樣依賴於潛在不忠實的事後推理痕跡。我們最終顯示，在兩個三元主張驗證數據集上，ITA超越了論證基準，並且可以與非論證的直接預測基準進行競爭，同時提供的裁決是從明確的、可檢查的論證結構中確定性計算得出的。

##### **INSHAPE: Instance-Level Shapelets for Interpretable Time-Series Classification**
2605.20088v1 by Seongjun Lee, Seokhyun Lee, Changhee Lee

Discovering shapelets -- i.e., discriminative temporal patterns within time series -- has been widely studied to address the inherent complexity of time-series classification (TSC) and to make model decision-making processes more transparent. However, existing methods primarily focus on population-level shapelets optimized across the entire dataset, which leads to two fundamental limitations: (i) population-level patterns often misalign with instance-specific features, resulting in suboptimal performance and potentially misleading interpretations, and (ii) most methods treat shapelets as independent entities, overlooking important temporal dependencies and interactions among multiple patterns. To address these limitations, we propose INSHAPE, an interpretable TSC framework that discovers variable-length, discriminative temporal patterns specific to each time series. INSHAPE identifies these patterns as non-overlapping segments and models their temporal dependencies, thereby providing clear instance-level interpretations while achieving strong predictive performance. Furthermore, INSHAPE bridges local and global interpretability through a bottom-up approach, aggregating instance-level shapelets into prototypical (population-level) shapelets. Extensive experiments on 128 UCR and 30 UEA benchmark datasets show that INSHAPE consistently outperforms state-of-the-art shapelet-based methods while providing more intuitive and interpretable insights.

摘要：發現形狀元——即時間序列中的區別性時間模式——已被廣泛研究，以解決時間序列分類（TSC）固有的複雜性並使模型決策過程更加透明。<lb/>然而，現有方法主要集中在跨整個數據集優化的人口級形狀元，這導致了兩個基本限制：（i）人口級模式往往與實例特定特徵不對齊，導致次優性能和潛在的誤導性解釋，及（ii）大多數方法將形狀元視為獨立實體，忽略了多個模式之間重要的時間依賴性和互動。<lb/>為了解決這些限制，我們提出了INSHAPE，一個可解釋的TSC框架，發現特定於每個時間序列的變長區別性時間模式。<lb/>INSHAPE將這些模式識別為不重疊的段並建模它們的時間依賴性，從而提供清晰的實例級解釋，同時實現強大的預測性能。<lb/>此外，INSHAPE通過自下而上的方法橋接了局部和全局可解釋性，將實例級形狀元聚合為原型（人口級）形狀元。<lb/>在128個UCR和30個UEA基準數據集上的廣泛實驗顯示，INSHAPE始終超越最先進的基於形狀元的方法，同時提供更直觀和可解釋的見解。

##### **ThoughtTrace: Understanding User Thoughts in Real-World LLM Interactions**
2605.20087v1 by Chuanyang Jin, Binze Li, Haopeng Xie, Cathy Mengying Fang, Tianjian Li, Shayne Longpre, Hongxiang Gu, Maximillian Chen, Tianmin Shu

Conversational AI has now reached billions of users, yet existing datasets capture only what people say, not what they think. We introduce ThoughtTrace, the first large-scale dataset that pairs real-world multi-turn human--AI conversations with users' self-reported thoughts: their reasons for sending prompts and reactions to assistant responses. ThoughtTrace comprises 1,058 users, 2,155 conversations, 17,058 turns, and 10,174 thought annotations collected across 20 language models. Our analysis shows that ThoughtTrace captures long-horizon, topically diverse interactions, and that thoughts are semantically distinct from messages, difficult for frontier LLMs to infer from context, diverse in content, and tied to conversation stages. We further demonstrate the utility of thoughts for downstream modeling. First, thoughts improve user-behavior prediction as inference-time context. Second, thought-guided rewrites provide fine-grained alignment signals for training personalized assistants. Together, ThoughtTrace establishes user thoughts as a new data modality for studying the cognitive dynamics behind human--AI interaction and provides a foundation for building assistants that better understand and adapt to users' latent goals, preferences, and needs.

摘要：對話式人工智慧現在已經達到數十億用戶，但現有數據集僅捕捉人們所說的內容，而非他們的想法。我們介紹了ThoughtTrace，這是第一個大規模數據集，將現實世界的多輪人類--人工智慧對話與用戶自我報告的想法配對：他們發送提示的原因和對助手回應的反應。ThoughtTrace包含1,058名用戶、2,155個對話、17,058個回合和10,174個思考註釋，這些數據是從20個語言模型中收集的。我們的分析顯示，ThoughtTrace捕捉了長期、多主題的互動，並且思考在語義上與信息是不同的，對於前沿的LLM來說，從上下文中推斷這些思考是困難的，內容多樣，並且與對話階段相關。我們進一步展示了思考在下游建模中的實用性。首先，思考作為推斷時的上下文可以改善用戶行為預測。其次，思考引導的重寫為訓練個性化助手提供了細緻的對齊信號。總之，ThoughtTrace確立了用戶思考作為一種新的數據模式，用於研究人類--人工智慧互動背後的認知動態，並為構建能更好理解和適應用戶潛在目標、偏好和需求的助手提供了基礎。

##### **What Do Evolutionary Coding Agents Evolve?**
2605.20086v1 by Nico Pelleriti, Sree Harsha Nelaturu, Zhanke Zhou, Zongze Li, Max Zimmer, Bo Han, Sebastian Pokutta

Recent work pairs LLMs with evolutionary search to iteratively generate, modify, and select code using task-specific feedback. These systems have produced strong results in mathematical discovery and algorithm design, yet a fundamental question remains: what do they actually evolve? Progress is typically summarized by the best score a run reaches under a task-specific evaluator, but that score can reflect several different mechanisms: new algorithmic structure, re-tuning an existing strategy, recombining ideas already in the model's internal knowledge, or overfitting to the evaluator. Distinguishing these mechanisms requires inspecting the search process itself, not only its final outcome. We introduce EvoTrace, a dataset of evolutionary coding traces spanning four evolutionary frameworks, reasoning and non-reasoning models, and 16 tasks across mathematics and algorithm design. To analyze these traces, we develop EvoReplay, a replay-based methodology that reconstructs the local search states behind high-scoring solutions and tests controlled interventions, including adjusting constants, removing program components and substituting models or prompting contexts. We annotate every code edit in EvoTrace with one of nine recurring edit types using an LLM-as-judge pipeline validated against blind human re-annotation. Across EvoTrace, most score gains come from a small subset of these edit types. We further find a deterministic cycling pattern: about 30% of code lines added during search are byte-identical re-introductions of previously-deleted lines, present throughout nearly every run. These results show that benchmark gains in evolutionary coding agents can arise from qualitatively different mechanisms, only some of which correspond to new algorithmic structure. EvoTrace enables more diagnostic evaluation of evolutionary coding agents beyond final benchmark scores.

摘要：最近的研究將大型語言模型（LLMs）與進化搜索相結合，通過任務特定的反饋迭代生成、修改和選擇代碼。這些系統在數學發現和算法設計方面取得了強勁的成果，但仍然存在一個根本性問題：它們實際上在進化什麼？進展通常通過運行在任務特定評估器下達到的最佳分數來總結，但該分數可以反映幾種不同的機制：新的算法結構、重新調整現有策略、重新組合已在模型內部知識中的想法，或對評估器的過擬合。區分這些機制需要檢查搜索過程本身，而不僅僅是其最終結果。我們介紹了EvoTrace，這是一個跨越四個進化框架、推理和非推理模型，以及數學和算法設計中的16個任務的進化編碼痕跡數據集。為了分析這些痕跡，我們開發了EvoReplay，一種基於回放的方法論，重建高分解決方案背後的局部搜索狀態並測試控制干預，包括調整常數、刪除程序組件和替換模型或提示上下文。我們使用經過盲人重新註釋驗證的LLM作為評判者的管道，對EvoTrace中的每個代碼編輯進行了九種類型的重複編輯類型的註釋。在EvoTrace中，大多數分數增益來自這些編輯類型的一小部分。我們進一步發現了一種確定性循環模式：在搜索過程中添加的約30%的代碼行是之前刪除行的字節相同的重新引入，幾乎在每次運行中都存在。這些結果顯示，進化編碼代理的基準增益可以來自質量不同的機制，其中只有一部分與新的算法結構相對應。EvoTrace使得對進化編碼代理的診斷評估超越了最終基準分數。

##### **BalanceRAG: Joint Risk Calibration for Cascaded Retrieval-Augmented Generation**
2605.20084v1 by Zijun Jia, Yuanchang Ye, Sen Jia, Yiyao Qian, Haoning Wang, Baojie Chen, Diyin Tang, Jinsong Yu, Zhiyuan Wang

Large language models (LLMs) can enhance factuality via retrieval-augmented generation (RAG), but applying RAG to every query is unnecessary when the model-only answer is reliable. This motivates cascaded RAG: each query is first handled by an LLM-only branch, escalated to a RAG fallback only if the primary branch is uncertain, and abstained from when neither branch is sufficiently trustworthy. However, calibrating such cascades stage by stage may be conservative, since the final utility depends on joint uncertainty thresholding of LLM-only and RAG. In this work, we develop BalanceRAG to certify threshold pairs at a target risk level. Given uncertainty scores from the two branches, BalanceRAG frames each threshold pair as an operating point on a two-dimensional lattice and identifies safe operating points using sequential graphical testing. This enables risk-adaptive threshold calibration, controlling the system-level error rate among accepted points, while retaining more examples. Furthermore, BalanceRAG extends to multi-risk calibration, allowing retrieval usage to be bounded together with the selection-conditioned risk. Experiments on three open-domain question answering (QA) benchmarks across multiple LLM backbones demonstrate that BalanceRAG meets prescribed risk levels, preserves higher coverage and more accepted correct examples, and reduces unnecessary retrieval calls compared with always-on RAG.

摘要：大型語言模型（LLMs）可以通過檢索增強生成（RAG）來提高事實性，但在模型僅回答可靠的情況下，對每個查詢應用 RAG 是不必要的。這促使了級聯 RAG 的出現：每個查詢首先由僅 LLM 的分支處理，只有在主要分支不確定時才升級到 RAG 備用，當兩個分支都不夠可信時則不進行處理。然而，逐步校準這種級聯可能過於保守，因為最終效用取決於 LLM 僅和 RAG 的聯合不確定性閾值。在這項工作中，我們開發了 BalanceRAG 來在目標風險水平下認證閾值對。根據來自兩個分支的不確定性分數，BalanceRAG 將每對閾值框架化為二維格子上的操作點，並使用序列圖形測試識別安全操作點。這使得風險自適應閾值校準成為可能，控制接受點之間的系統級錯誤率，同時保留更多示例。此外，BalanceRAG 擴展到多風險校準，允許檢索使用與選擇條件風險一起受限。在三個開放領域問題回答（QA）基準上進行的實驗表明，BalanceRAG 滿足規定的風險水平，保持更高的覆蓋率和更多被接受的正確示例，並與始終開啟的 RAG 相比減少不必要的檢索調用。

##### **VL-DPO: Vision-Language-Guided Finetuning for Preference-Aligned Autonomous Driving**
2605.20082v1 by Zhefan Xu, Ghassen Jerfel, Marina Haliem, Qi Zhao, Jeonhyung Kang, Khaled S. Refaat

The rapid growth of autonomous driving datasets has enabled the scaling of powerful motion forecasting models. While large-scale pretraining provides strong performance, the standard imitation objective may not fully capture the complex nuances of human driving preferences. Meanwhile, recent advances in vision-language models (VLMs) have demonstrated impressive reasoning and commonsense understanding. Building on these capabilities, this paper presents VL-DPO, a vision-language-guided framework that aligns ego-vehicle motion forecasting models with human preferences. Our approach leverages a VLM as a zero-shot reasoner to automatically generate preference pairs from a pretrained model's rollouts, which are then used to finetune the model via Direct Preference Optimization (DPO). We finetune our models on the Waymo Open End-to-End Driving Dataset (WOD-E2E) and evaluate performance against held-out human preference annotations using rater feedback score (RFS) and average displacement error (ADE). Our experiments confirm that the VLM's trajectory selection is a high-quality proxy for human preference. Our final model, VL-DPO, yields an 11.94% increase in RFS and a 10.01% reduction in ADE over the pretrained model.

摘要：自主駕駛數據集的快速增長使得強大的運動預測模型得以擴展。雖然大規模的預訓練提供了強勁的性能，但標準的模仿目標可能無法完全捕捉人類駕駛偏好的複雜細微差別。與此同時，最近在視覺-語言模型（VLMs）方面的進展展示了令人印象深刻的推理和常識理解。基於這些能力，本文提出了VL-DPO，一個視覺-語言引導的框架，將自我車輛運動預測模型與人類偏好對齊。我們的方法利用VLM作為零樣本推理器，從預訓練模型的回合中自動生成偏好對，然後通過直接偏好優化（DPO）來微調模型。我們在Waymo開放端到端駕駛數據集（WOD-E2E）上微調我們的模型，並使用評分者反饋分數（RFS）和平均位移誤差（ADE）來評估與保留的人類偏好註釋的性能。我們的實驗確認了VLM的軌跡選擇是人類偏好的高質量代理。我們的最終模型VL-DPO在RFS上提高了11.94%，在ADE上減少了10.01%，相較於預訓練模型。

##### **Probability-Conserving Flow Guidance**
2605.20079v1 by Parsa Esmati, Junha Hyung, Amirhossein Dadashzadeh, Jaegul Choo, Majid Mirmehdi

Diffusion and flow-based generative models dominate visual synthesis, with guidance aligning samples to user input and improving perceptual quality. However, Classifier-Free Guidance (CFG) and extrapolation-based methods are heuristic linear combinations of velocities/scores that ignore the generative manifold geometry, breaking probability conservation and driving samples off the learned manifold under strong guidance. We analyse guidance through the continuity equation and show its effect decomposes into a divergence term and a score-parallel term defined invariantly across parameterisations. We prove the divergence term blows up structurally as sampling approaches the data manifold, motivating a time-dependent schedule alongside score-parallel attenuation. The resulting plug-and-play rule, Adaptive Manifold Guidance (AdaMaG), bounds both terms at no additional inference cost. Finally, we show that most empirical heuristics for reducing saturation or improving generation quality correspond directly to the two terms in our decomposition. Across image generation benchmarks, AdaMaG improves realism, reduces hallucinations, and induces controlled desaturation in high-guidance regimes.

摘要：擴散和基於流的生成模型主導視覺合成，通過指導將樣本與用戶輸入對齊並提高感知質量。然而，無分類器指導（CFG）和基於外推的方法是忽略生成流形幾何的速度/分數的啟發式線性組合，這破壞了概率守恆並在強指導下將樣本驅動出學習的流形。我們通過連續性方程分析指導，並顯示其效果分解為一個散度項和一個在參數化中不變定義的分數平行項。我們證明了當採樣接近數據流形時，散度項在結構上會爆炸，這促使了時間依賴的計劃與分數平行的衰減。最終產生的即插即用規則，自適應流形指導（AdaMaG），在不增加推理成本的情況下限制了這兩個項。最後，我們顯示，大多數減少飽和或改善生成質量的經驗啟發式方法直接對應於我們分解中的兩個項。在圖像生成基準中，AdaMaG 改善了真實感，減少了幻覺，並在高指導範疇中引入了受控去飽和。

##### **CopT: Contrastive On-Policy Thinking with Continuous Spaces for General and Agentic Reasoning**
2605.20075v1 by Dachuan Shi, Hanlin Zhu, Xiangchi Yuan, Wanjia Zhao, Kejing Xia, Wen Xiao, Wenke Lee

Chain-of-thought (CoT) is a standard approach for eliciting reasoning capabilities from large language models (LLMs). However, the common CoT paradigm treats thinking as a prerequisite for answering, which can delay access to plausible answers and incur unnecessary token costs even when the model is able to identify an answer before extended thinking, a behavior known as performative reasoning. In this paper, we introduce CopT, a reformulated reasoning pipeline that reverses the usual order of thinking and answering. Instead of thinking before answering, CopT first elicits a draft answer and then invokes subsequent on-policy thinking conditioned on its own draft answer for reflection and correction. To assess whether the draft answer should be trusted, CopT recasts continuous embeddings as inference-time contrastive verifiers. Specifically, it contrasts the model's support for the same generated tokens under discrete-token inputs and continuous-embedding inputs, yielding a sequence-level reverse KL estimator for answer reliability. Our analysis shows that under certain assumptions, the expected estimate equals the mutual information between the unresolved latent state and the emitted answer token, explaining why it captures answer-relevant uncertainty rather than arbitrary uncertainty in the latent state. When the answer is deemed insufficiently reliable, CopT performs further on-policy thinking, where a second KL estimator dynamically controls draft-answer visibility, preserving useful partial information while reducing the risk of being misled by unreliable content. Across mathematics, coding, and agentic reasoning tasks, CopT improves peak accuracy by up to 23% and reduces token usage by up to 57% at comparable or higher accuracy, without any additional training. The code is available at https://github.com/sdc17/CopT.

摘要：Chain-of-thought (CoT) 是一種標準方法，用於從大型語言模型 (LLMs) 中引出推理能力。<lb/>然而，常見的 CoT 範式將思考視為回答的前提，這可能會延遲獲得合理答案的時間，並在模型能夠在擴展思考之前識別答案時，產生不必要的標記成本，這種行為被稱為表現性推理。<lb/>在本文中，我們介紹了 CopT，一種重新構建的推理管道，顛倒了思考和回答的通常順序。<lb/>CopT 不是在回答之前進行思考，而是首先引出一個草擬答案，然後根據其自己的草擬答案進行後續的政策內思考，以進行反思和修正。<lb/>為了評估草擬答案是否應該被信任，CopT 將連續嵌入重新表述為推理時的對比驗證器。<lb/>具體而言，它對比模型在離散標記輸入和連續嵌入輸入下對相同生成標記的支持，從而產生一個序列級的逆 KL 估計器，用於答案的可靠性。<lb/>我們的分析顯示，在某些假設下，期望估計等於未解決潛在狀態與發出答案標記之間的互信息，解釋了為什麼它捕捉到與答案相關的不確定性，而不是潛在狀態中的任意不確定性。<lb/>當答案被認為不夠可靠時，CopT 會進行進一步的政策內思考，其中第二個 KL 估計器動態控制草擬答案的可見性，保留有用的部分信息，同時減少被不可靠內容誤導的風險。<lb/>在數學、編碼和代理推理任務中，CopT 將峰值準確率提高了多達 23%，並在可比或更高的準確率下將標記使用量減少了多達 57%，而無需任何額外的訓練。<lb/>代碼可在 https://github.com/sdc17/CopT 獲得。

##### **Probing Embodied LLMs: When Higher Observation Fidelity Hurts Problem Solving**
2605.20072v1 by Oussama Zenkri, Oliver Brock

Large Language Models are increasingly proposed as cognitive components for robotic systems, yet their opaque decision processes make it difficult to explain success or failure in closed-loop embodied tasks. Following an empirical AI methodology, we study embodied LLM agents behaviorally by varying the information available to the agent and measuring the resulting changes in behavior. Using the Lockbox, a sequential mechanical puzzle with hidden interdependencies, we evaluate LLMs across RGB, RGB-D, and ground-truth symbolic observations in a physical robotic setup and use controlled simulation to probe the resulting behavior. Counterintuitively, agents perform best under raw RGB input and worst under perfect ground-truth observations. In simulation, we probe this effect by randomly flipping perceived action outcomes and find that moderate noise improves performance, peaking at a 40% flip probability with a 2.85-fold success rate increase over the noise-free baseline. Further analysis links this gain to a reduction in repetitive action loops. These findings suggest that success rates alone are insufficient for evaluating LLMs, as measured performance may reflect the interaction between perceptual errors and reasoning failures rather than robust problem solving.

摘要：大型語言模型越來越多地被提議作為機器人系統的認知組件，然而它們不透明的決策過程使得在封閉回路具身任務中解釋成功或失敗變得困難。根據實證人工智慧方法論，我們通過改變可用於代理的資訊並測量行為的變化來行為上研究具身的LLM代理。使用鎖箱，一種具有隱藏相互依賴性的序列機械謎題，我們在物理機器人設置中評估LLM在RGB、RGB-D和真實符號觀察中的表現，並使用受控模擬來探查結果行為。出乎意料的是，代理在原始RGB輸入下表現最佳，而在完美的真實觀察下表現最差。在模擬中，我們通過隨機翻轉感知的行動結果來探查這一效應，發現適度的噪音能改善性能，在40%的翻轉概率下，成功率較無噪音基線提高了2.85倍。進一步分析將這一增益與重複行動循環的減少聯繫起來。這些發現表明，僅僅依賴成功率來評估LLM是不夠的，因為測得的性能可能反映了感知錯誤與推理失敗之間的互動，而不是穩健的問題解決能力。

##### **Text-to-SPARQL Generation with Reinforcement Learning: A GRPO-based Approach on DBLP**
2605.20066v1 by Jann Pfeifer, Debayan Banerjee, Ricardo Usbeck

Knowledge graph question answering seeks to translate natural language questions into executable queries over knowledge graphs, but existing approaches often rely on large models or full supervision in the form of gold query annotations. This study examines whether reinforcement learning with outcome-based rewards can train a small instruction-tuned language model to perform zero-shot Text-to-SPARQL generation in the scholarly domain. Group-Relative Policy Optimization (GRPO) is applied to the Qwen3-1.7B model on DBLP-QuAD, using prompts that combine natural language questions with symbolic hints about entities and relations. Training relies on execution feedback, structural constraints, and answer-level rewards, with an additional variant that incorporates gold-query-based shaping. The resulting models are compared to the unmodified zero-shot baseline and to a supervised DoRA-finetuned baseline across answer-level accuracy, execution accuracy, category-wise scores, and generalization to held-out templates. GRPO substantially improves over the zero-shot baseline and exhibits competitive generalization, while supervised DoRA finetuning achieves higher overall accuracy on the same model scale. Ablation analyses indicate that execution-based rewards account for most gains, with additional shaping yielding limited additional benefit, suggesting that outcome-based reinforcement learning is a viable training strategy when gold queries are unavailable for token-level supervision.

摘要：知識圖譜問答旨在將自然語言問題轉換為可在知識圖譜上執行的查詢，但現有的方法通常依賴於大型模型或以金標註的形式進行完全監督。這項研究探討了基於結果的獎勵的強化學習是否可以訓練一個小型的指令調整語言模型，以在學術領域執行零-shot Text-to-SPARQL 生成。將群體相對政策優化（GRPO）應用於 Qwen3-1.7B 模型在 DBLP-QuAD 上，使用結合自然語言問題與有關實體和關係的符號提示的提示。訓練依賴於執行反饋、結構約束和答案級獎勵，還有一個額外變體納入了基於金標的塑形。所產生的模型與未修改的零-shot 基線以及經監督的 DoRA 微調基線進行比較，評估指標包括答案級準確性、執行準確性、類別得分和對保留模板的泛化能力。GRPO 在零-shot 基線上顯著提升，並展現出競爭性的泛化能力，而經監督的 DoRA 微調在相同模型規模上實現了更高的整體準確性。消融分析表明，基於執行的獎勵佔據了大部分增益，而額外的塑形僅帶來有限的額外好處，這表明當金標查詢無法用於標記級監督時，基於結果的強化學習是一種可行的訓練策略。

##### **Rewarding Beliefs, Not Actions: Consistency-Guided Credit Assignment for Long-Horizon Agents**
2605.20061v1 by Wenjie Tang, Minne Li, Sijie Huang, Liquan Xiao, Yuan Zhou

Reinforcement learning from verifiable rewards (RLVR) is a promising paradigm for improving large language model (LLM) agents on long-horizon interactive tasks. However, in partially observable environments, incomplete observations cause agent beliefs to drift over time, while delayed rewards obscure the causal impact of intermediate decisions, exacerbating temporal credit assignment challenges. To address this, we propose ReBel (Reward Belief), a process-level reinforcement learning algorithm that explicitly models structured belief states to summarize interaction history and guide subsequent policy learning. ReBel introduces belief-consistency supervision, converting discrepancies between predicted beliefs and observed feedback into dense self-supervised signals without requiring external step-wise annotations or verifiers. It also employs belief-aware grouping to compare trajectories under similar belief states, yielding more robust and lower-variance advantage estimates. We evaluate ReBel on challenging long-horizon benchmarks, including ALFWorld and WebShop. ReBel improves task success by up to $20.4$ percentage points over the episode-level baseline GRPO and increases sample efficiency by $2.1\times$. These results suggest that belief-aware self-supervision is a promising direction for reliable long-horizon decision-making under partial observability. Code is available at: https://github.com/Fateyetian/Rebel.git.

摘要：強化學習從可驗證獎勵中（RLVR）是一個有前景的範式，用於改善大型語言模型（LLM）代理在長期互動任務中的表現。<lb/>然而，在部分可觀察的環境中，不完整的觀察會導致代理的信念隨時間漂移，而延遲的獎勵則模糊了中間決策的因果影響，加劇了時間信用分配的挑戰。<lb/>為了解決這個問題，我們提出了ReBel（獎勵信念），這是一種過程級強化學習算法，明確建模結構化信念狀態以總結互動歷史並指導後續的政策學習。<lb/>ReBel引入了信念一致性監督，將預測信念與觀察到的反饋之間的差異轉換為密集的自我監督信號，而不需要外部的逐步註釋或驗證者。<lb/>它還採用了信念感知分組，以比較相似信念狀態下的軌跡，從而產生更穩健且變異性更低的優勢估計。<lb/>我們在具有挑戰性的長期基準上評估ReBel，包括ALFWorld和WebShop。<lb/>ReBel在任務成功率上比基於集的基準GRPO提高了最多$20.4$個百分點，並將樣本效率提高了$2.1\times$。<lb/>這些結果表明，信念感知自我監督是可靠的長期決策在部分可觀察性下的一個有前景的方向。<lb/>代碼可在以下鏈接獲得：https://github.com/Fateyetian/Rebel.git。

##### **Towards LLM-Assisted Architecture Recovery for Real-World ROS~2 Systems: An Agent-Based Multi-Level Approach to Hierarchical Structural Architecture Reconstruction**
2605.20055v1 by Dominique Briechle, Raj Chanchad, Tobias Geger, Ruidi He, Dhruv Jajadiya, Dhruv Kapadiya, Andreas Rausch, Meng Zhang

Explicit software architecture models are essential artifacts for communicating, analyzing, and evolving complex software-intensive systems. In ROS~2-based robotic systems, however, structural (de-)composition and integration semantics are often only implicitly encoded across distributed artifacts such as source code and launch files, making recovery of hierarchical architecture particularly difficult. Existing approaches mainly focus on node-level entities and communication wiring, while providing limited support for recovering hierarchical structural (de-)composition across multiple abstraction levels.
  In this paper, we extend our previously proposed blueprint-guided LLM-assisted architecture recovery pipeline for ROS~2 systems through two major enhancements: (1) refined prompting to improve the consistency and controllability of architecture synthesis, and (2) a staged recovery strategy based on multi-level intermediate architectural representations that incorporate the atomic ROS node list and launch file dependencies, thereby enabling structurally constrained reconstruction across multiple abstraction levels.
  The approach is evaluated on a real-world automated product disassembly system based on cooperative robotic arms and heterogeneous ROS~2 artifacts. Compared to our previous work, the considered case study exhibits substantially higher integration complexity and richer functionality. The results demonstrate improved structural consistency, scalability, and robustness of architecture recovery, while also revealing remaining challenges related to dynamic integration semantics in large-scale ROS~2 systems.

摘要：明確的軟體架構模型是用於溝通、分析和演變複雜的軟體密集系統的重要文檔。然而，在基於 ROS~2 的機器人系統中，結構的（去）組成和整合語義往往僅以隱式方式編碼在分散的文檔中，例如源代碼和啟動文件，這使得恢復層次架構特別困難。現有的方法主要集中在節點級實體和通信連接上，而對於在多個抽象層次上恢復層次結構的（去）組成提供的支持有限。<lb/>在本文中，我們通過兩個主要增強擴展了之前提出的基於藍圖引導的 LLM 輔助架構恢復管道，用於 ROS~2 系統：（1）改進提示以提高架構合成的一致性和可控性，以及（2）基於多級中間架構表示的分階段恢復策略，這些表示包含原子 ROS 節點列表和啟動文件依賴關係，從而實現跨多個抽象層次的結構約束重建。<lb/>該方法在一個基於協作機器手臂和異構 ROS~2 文檔的真實自動化產品拆解系統上進行評估。與我們之前的工作相比，所考慮的案例研究顯示出顯著更高的整合複雜性和更豐富的功能。結果顯示了架構恢復的結構一致性、可擴展性和穩健性有所改善，同時也揭示了與大型 ROS~2 系統中的動態整合語義相關的剩餘挑戰。

##### **PromptRad: Knowledge-Enhanced Multi-Label Prompt-Tuning for Low-Resource Radiology Report Labeling**
2605.20052v1 by Ying-Jia Lin, Tzu-Chin Lo, Ping-Chien Li, Chi-Tung Cheng, Chien-Hung Liao, Hung-Yu Kao

Automatic report labeling facilitates the identification of clinical findings from unstructured text and enables large-scale annotation for medical imaging research. Existing rule-based labelers struggle with the diverse descriptions in clinical reports, while fine-tuning pre-trained language models (PLMs) requires large amounts of labeled data that are often unavailable in clinical settings. In this paper, we propose PromptRad, a knowledge-enhanced multi-label \textbf{prompt}-tuning approach for \textbf{rad}iology report labeling under low-resource settings. PromptRad reformulates multi-label classification as masked language modeling and incorporates synonyms from the UMLS Metathesaurus into a multi-word verbalizer to enrich category representations. By fine-tuning the PLM without additional classification layers, PromptRad requires substantially less labeled data than conventional fine-tuning. Experiments on liver CT reports show that PromptRad outperforms dictionary-based and fine-tuning baselines with only 32 labeled training examples, and achieves competitive performance with GPT-4 despite using a much smaller model. Further analysis demonstrates that PromptRad captures complex negation patterns more effectively than existing methods, making it a promising solution for report labeling in data-scarce clinical scenarios. Our code is available at https://github.com/ila-lab/PromptRad.

摘要：自動報告標記有助於從非結構化文本中識別臨床發現，並使醫學影像研究能夠進行大規模標註。現有的基於規則的標記器在臨床報告中面對多樣的描述時表現不佳，而微調預訓練語言模型（PLMs）則需要大量標記數據，這在臨床環境中通常無法獲得。在本文中，我們提出了PromptRad，一種在低資源環境下進行放射學報告標記的知識增強多標籤\textbf{提示}調整方法。PromptRad將多標籤分類重新表述為掩碼語言建模，並將UMLS Metathesaurus中的同義詞納入多詞動詞化器，以豐富類別表示。通過在不添加額外分類層的情況下微調PLM，PromptRad所需的標記數據顯著少於傳統的微調方法。對肝臟CT報告的實驗表明，PromptRad在僅使用32個標記訓練示例的情況下超越了基於字典和微調的基準，並在使用更小模型的情況下仍能與GPT-4達到競爭性能。進一步的分析表明，PromptRad比現有方法更有效地捕捉複雜的否定模式，使其成為數據稀缺的臨床場景中報告標記的有前景解決方案。我們的代碼可在https://github.com/ila-lab/PromptRad獲得。

##### **Language Mutations Sustain the Persistences of Conspiracy Theories on Social Media**
2605.20050v1 by Calvin Yixiang Cheng, Dorian Quelle, Scott A. Hale

This study investigates how language mutations affect the persistent diffusion of conspiracy theories on social media. Drawing on a three-year dataset of conspiracy-related posts from X, and applying computational linguistic analysis alongside survival modelling, we find that conspiracy claims with greater semantic mutations have substantially longer lifespans. Mutations in psycholinguistic properties, including pronouns, social reference words, cognitive process terms, risk- and health- related vocabularies, are associated with extended lifespans. Mutations in actor, action and target (AAT) categories are associated with longer lifespans as well. Qualitative analysis identifies two predominant mutation patterns: simplification and assimilation, at both linguistic and AAT structural levels. Taken together, the results advance our understanding of how language mutations contribute to conspiracy persistence online and shed lights on longitudinal content moderation strategies. We argue that content moderation should consider the mutability of conspiracy claims and focus on the core claims that can address their potential variations.

摘要：這項研究探討了語言變異如何影響陰謀論在社交媒體上的持續擴散。<lb/>基於來自X的三年陰謀相關帖子數據集，並應用計算語言學分析和生存模型，我們發現具有較大語義變異的陰謀主張擁有顯著更長的壽命。<lb/>心理語言學特徵的變異，包括代詞、社會參考詞、認知過程術語、風險和健康相關詞彙，與延長的壽命相關。<lb/>行為者、行動和目標（AAT）類別的變異也與更長的壽命相關。<lb/>質性分析確定了兩種主要的變異模式：簡化和同化，這些模式在語言和AAT結構層面上均存在。<lb/>綜合來看，這些結果增進了我們對語言變異如何促進陰謀論在線持續性的理解，並為長期內容審核策略提供了啟示。<lb/>我們認為內容審核應考慮陰謀主張的可變性，並專注於可以應對其潛在變化的核心主張。

##### **Does Code Cleanliness Affect Coding Agents? A Controlled Minimal-Pair Study**
2605.20049v1 by Priyansh Trivedi, Olivier Schmitt

As autonomous coding agents see rapid adoption, their evaluation has primarily focused on task completion rates holding the target codebase fixed. This leaves a critical question unanswered: does the structural and stylistic quality, or ``cleanliness'' of the underlying code affect an agent's ability to navigate and modify it? To isolate the effect of code cleanliness from agent capability, we introduce an evaluation protocol built around minimal pairs: repositories that match on architecture, dependencies, and external behaviour, but differ on static-analysis rule violations and cognitive complexity. The pairs are constructed in both directions, by agent pipelines that either degrade a clean repository or clean a messy one. We author 33 tasks across six such pairs, evaluated through hidden tests at the application's public surface. Across 660 trials with Claude Code, code cleanliness does not change the agent's pass rate. However, it substantially alters the agent's operational footprint: agents working on cleaner code use 7 to 8% fewer tokens and reduce file revisitations by 34%. Our findings suggest that traditional maintainability principles remain highly relevant in the era of AI-driven development, shaping the computational cost and navigational efficiency of coding agents. Code cleanliness joins model choice, harness, and prompting as a factor that materially affects agent behaviours.

摘要：隨著自主編碼代理的快速採用，其評估主要集中在任務完成率上，並保持目標代碼庫不變。這留下了一個關鍵問題未得到解答：底層代碼的結構和風格質量，或稱為「整潔性」，是否影響代理的導航和修改能力？為了將代碼整潔性與代理能力的影響隔離，我們引入了一種基於最小對的評估協議：這些代碼庫在架構、依賴關係和外部行為上匹配，但在靜態分析規則違規和認知複雜性上存在差異。這些對是通過代理管道以雙向構建的，這些管道要麼降低一個乾淨代碼庫的質量，要麼清理一個雜亂的代碼庫。我們在六個這樣的對中創建了33個任務，通過應用程序的公共界面進行隱藏測試評估。在與Claude Code的660次試驗中，代碼整潔性並未改變代理的通過率。然而，它顯著改變了代理的操作足跡：在更整潔的代碼上工作的代理使用的標記少了7%到8%，並將文件重訪次數減少了34%。我們的研究結果表明，在人工智能驅動的開發時代，傳統的可維護性原則仍然高度相關，影響著編碼代理的計算成本和導航效率。代碼整潔性與模型選擇、駕馭和提示一起成為實質上影響代理行為的因素。

##### **Mind Your Moras: Orthography-Aware Error Analysis of Neural Japanese Morphological Generation**
2605.20043v1 by Wen Zhang

We present an orthography-aware error analysis of Japanese past-tense morphological inflection, treating hiragana not merely as a transcriptional medium, but as a representational system encoding morphophonological distinctions that may influence model generalization. We evaluate two character-level sequence-to-sequence architectures on past-tense formation using datasets formatted according to the SIGMORPHON 2020 and 2023 shared task conventions. Despite high aggregate accuracy, models exhibit systematic, linguistically interpretable errors that cluster around specific orthographic properties of hiragana. We introduce a concise error taxonomy capturing seven primary failure modes and provide both quantitative and qualitative analyses. Gemination-related errors dominate residual failures, accounting for 75-80% of errors, particularly in verbs whose stems end in the vowel e and require gemination before the past-tense suffix. Error patterns remain highly consistent across architectures and random seeds, suggesting a robust interaction between orthographic representation, morphological structure, and data frequency effects in shaping model generalization. These results underscore the necessity of orthography-aware evaluation for understanding neural generalization in morphologically complex languages.

摘要：我們提出了一種考慮正字法的日語過去式形態變化錯誤分析，將平假名視為不僅僅是轉錄媒介，而是一種表示系統，編碼可能影響模型泛化的形態音位區別。<lb/>我們在過去式形成上評估了兩種字符級序列到序列架構，使用根據SIGMORPHON 2020和2023共享任務慣例格式化的數據集。<lb/>儘管整體準確率很高，模型卻顯示出系統性、語言學上可解釋的錯誤，這些錯誤圍繞著平假名的特定正字法特性聚集。<lb/>我們引入了一個簡明的錯誤分類法，捕捉七種主要失敗模式，並提供定量和定性分析。<lb/>與重疊相關的錯誤主導了剩餘的失敗，佔錯誤的75-80%，特別是在那些詞幹以元音e結尾並在過去式後綴之前需要重疊的動詞中。<lb/>錯誤模式在不同架構和隨機種子之間保持高度一致，這表明正字法表示、形態結構和數據頻率效應之間存在穩健的互動，影響模型的泛化。<lb/>這些結果強調了考慮正字法的評估對於理解形態複雜語言中的神經泛化的必要性。

##### **AutoResearchClaw: Self-Reinforcing Autonomous Research with Human-AI Collaboration**
2605.20025v1 by Jiaqi Liu, Shi Qiu, Mairui Li, Bingzhou Li, Haonian Ji, Siwei Han, Xinyu Ye, Peng Xia, Zihan Dong, Congyu Zhang, Letian Zhang, Guiming Chen, Haoqin Tu, Xinyu Yang, Lu Feng, Xujiang Zhao, Haifeng Chen, Jiawei Zhou, Xiao Wang, Weitong Zhang, Hongtu Zhu, Yun Li, Jieru Mei, Hongliang Fei, Jiaheng Zhang, Linjie Li, Linjun Zhang, Yuyin Zhou, Sheng Wang, Caiming Xiong, James Zou, Zeyu Zheng, Cihang Xie, Mingyu Ding, Huaxiu Yao

Automating scientific discovery requires more than generating papers from ideas. Real research is iterative: hypotheses are challenged from multiple perspectives, experiments fail and inform the next attempt, and lessons accumulate across cycles. Existing autonomous research systems often model this process as a linear pipeline: they rely on single-agent reasoning, stop when execution fails, and do not carry experience across runs. We present AutoResearchClaw, a multi-agent autonomous research pipeline built on five mechanisms: structured multi-agent debate for hypothesis generation and result analysis, a self-healing executor with a \textsc{Pivot}/\textsc{Refine} decision loop that transforms failures into information, verifiable result reporting that prevents fabricated numbers and hallucinated citations, human-in-the-loop collaboration with seven intervention modes spanning full autonomy to step-by-step oversight, and cross-run evolution that converts past mistakes into future safeguards. On ARC-Bench, a 25-topic experiment-stage benchmark, AutoResearchClaw outperforms AI Scientist v2 by 54.7%. A human-in-the-loop ablation across seven intervention modes reveals that precise, targeted collaboration at high-leverage decision points consistently outperforms both full autonomy and exhaustive step-by-step oversight. We position AutoResearchClaw as a research amplifier that augments rather than replaces human scientific judgment. Code is available at https://github.com/aiming-lab/AutoResearchClaw.

摘要：自動化科學發現需要的不僅僅是從想法生成論文。真正的研究是迭代的：假設從多個角度受到挑戰，實驗失敗並為下一次嘗試提供信息，教訓在循環中累積。現有的自主研究系統通常將這一過程建模為線性管道：它們依賴單一代理推理，在執行失敗時停止，並且不在運行之間傳遞經驗。我們提出了AutoResearchClaw，一個基於五個機制的多代理自主研究管道：用於假設生成和結果分析的結構化多代理辯論，自我修復的執行器，具有\textsc{Pivot}/\textsc{Refine}決策循環，將失敗轉化為信息，可驗證的結果報告以防止虛構數字和幻覺引用，與人類協作的七種干預模式，涵蓋從完全自主到逐步監督的全過程，以及跨運行演變，將過去的錯誤轉化為未來的保障。在ARC-Bench，一個包含25個主題的實驗階段基準中，AutoResearchClaw的表現超過AI Scientist v2達54.7%。在七種干預模式下進行的人類在環中消融實驗顯示，在高杠桿決策點進行精確、有針對性的協作始終優於完全自主和全面逐步監督。我們將AutoResearchClaw定位為一個研究放大器，增強而不是取代人類的科學判斷。代碼可在https://github.com/aiming-lab/AutoResearchClaw獲得。

##### **When Skills Don't Help: A Negative Result on Procedural Knowledge for Tool-Grounded Agents in Offensive Cybersecurity**
2605.20023v1 by Samuel Jacob Chacko, James Hugglestone, Chashi Mahiul Islam, Xiuwen Liu

Agent Skills, structured packages of procedural knowledge loaded into an LLM agent at inference time, are widely reported to improve task pass rates by an average of 16.2~percentage points across diverse domains. Yet the same benchmarks show wide variance, with 16 of 84 tasks suffering negative deltas when Skills are introduced. The community has not yet articulated a clean mechanism for \emph{when} Skills help and when they are merely redundant overhead. We re-analyze a recently published 180-run controlled study of an MCP-grounded autonomous Capture-the-Flag (CTF) agent under four documentation conditions of increasing richness (55, 1{,}478, 1{,}976, and 4{,}147 lines), and show that these conditions correspond almost exactly to a No-Skills, Experiential-Skills, Curated-Skills, and Comprehensive-Skills ablation. In offensive cybersecurity, a domain not deeply covered by existing Skills benchmarks, the marginal benefit of Skills collapses. The spread between the no-Skills and full-Skills conditions is only 8.9~pp ($p = 0.71$, $χ^2$; $p = 0.25$, Cochran--Armitage trend test; five of six pairwise Cohen's $h$ values fall below the $0.2$ small-effect threshold). We argue that the missing variable is \emph{environment-feedback bandwidth}. When an agent's tool layer returns strict, schema-validated, low-latency observations, the environment itself supplies the procedural correction signal that Skills are normally needed to provide. As a result, the marginal benefit of curated Skills diminishes substantially, and, in some cases (e.g., our timing side-channel setting), actively degrades performance. We articulate a falsifiable hypothesis, sketch its design implications for compound AI systems, and will release the reanalysis pipeline to support replication.

摘要：代理技能是結構化的程序知識包，在推理時加載到LLM代理中，據報導在不同領域中平均提高任務通過率16.2個百分點。然而，同樣的基準顯示出廣泛的變異性，在84個任務中有16個在引入技能時出現負增量。社群尚未清楚闡明\emph{何時}技能有幫助，何時它們僅僅是冗餘的開銷。我們重新分析了一項最近發表的180次控制研究，該研究針對一個基於MCP的自主捕旗（CTF）代理，在四種逐漸豐富的文檔條件下（55、1{,}478、1{,}976和4{,}147行），並顯示這些條件幾乎完全對應於無技能、經驗技能、策劃技能和綜合技能的消融。在攻擊性網絡安全這個現有技能基準未深入涵蓋的領域中，技能的邊際效益崩潰。無技能和全技能條件之間的差距僅為8.9個百分點（$p = 0.71$，$χ^2$；$p = 0.25$，Cochran--Armitage趨勢檢驗；六對配對Cohen's $h$值中有五個低於$0.2$的小效應閾值）。我們認為缺失的變量是\emph{環境反饋帶寬}。當代理的工具層返回嚴格的、模式驗證的、低延遲的觀察時，環境本身提供了通常需要技能提供的程序修正信號。因此，策劃技能的邊際效益大幅減少，在某些情況下（例如，我們的計時側信道設置）甚至會主動降低性能。我們闡明了一個可證偽的假設，勾勒出對複合AI系統的設計影響，並將發布重新分析管道以支持複製。

##### **FlexDraft: Flexible Speculative Decoding via Attention Tuning and Bonus-Guided Calibration**
2605.20022v1 by Yaojie Zhang, Jianuo Huang, Junlong Ke, Yuhang Han, Yongji Long, Tianchen Zhao, Biqing Qi, Linfeng Zhang

Speculative decoding accelerates memory-bound LLM inference without quality degradation by using a fast drafter to propose multiple candidate tokens and the target model to verify them in parallel. However, conventional sequential speculative decoding suffers from mutual waiting between drafting and verification, and repeated exchange of intermediate states further increases memory access overhead. Parallel speculative decoding addresses this limitation by performing drafting and verification within a single target forward pass, allowing future drafts to be prepared while current candidates are being verified. Although effective at small batch sizes, existing parallel speculative decoding methods either require costly continual pretraining with quality degradation or suffer from low acceptance rates. More importantly, this paradigm inherently suffers from uncertainty in both the bonus token and the accepted length, leading to draft verification mismatch and causing throughput gains to collapse at large batch sizes. To address these limitations, we introduce FlexDraft, a lossless speculative decoding framework that flexibly adapts to varying batch sizes through three key designs. (1) Attention Tuning enables block diffusion drafting by tuning only the attention projectors of the final few layers on mask tokens, while keeping the autoregressive path frozen to preserve the target distribution and produce high quality drafts with minimal trainable parameters. (2) Bonus-guided Calibration uses a lightweight MLP conditioned on the resolved bonus token to calibrate draft logits, mitigating draft verification mismatch caused by bonus token uncertainty. (3) Flex Decoding dynamically switches between parallel draft and verify at small batch sizes and sequential draft then verify at large batch sizes, and adjusts verification length based on draft confidence to eliminate redundant computation.

摘要：推測解碼通過使用快速草擬器提出多個候選標記，並讓目標模型並行驗證它們，從而加速了記憶體受限的 LLM 推理而不降低質量。然而，傳統的順序推測解碼在草擬和驗證之間存在相互等待的問題，並且中間狀態的重複交換進一步增加了記憶體訪問的開銷。並行推測解碼通過在單次目標前向傳播中進行草擬和驗證來解決這一限制，允許在當前候選標記被驗證的同時準備未來的草擬。儘管在小批量大小下有效，但現有的並行推測解碼方法要麼需要昂貴的持續預訓練並伴隨質量下降，要麼面臨低接受率。更重要的是，這一範式本質上在獎勵標記和接受長度上都存在不確定性，導致草擬驗證不匹配，並使得在大批量大小下的吞吐量增益崩潰。為了解決這些限制，我們引入了 FlexDraft，一個無損的推測解碼框架，通過三個關鍵設計靈活適應不同的批量大小。(1) 注意力調整通過僅調整最後幾層在掩碼標記上的注意力投影器來實現區塊擴散草擬，同時保持自回歸路徑凍結，以保留目標分佈並以最小的可訓練參數生成高質量的草擬。(2) 獎勵引導校準使用一個輕量級的 MLP，根據已解決的獎勵標記來校準草擬邏輯，減輕由於獎勵標記不確定性引起的草擬驗證不匹配。(3) Flex Decoding 在小批量大小下動態切換並行草擬和驗證，在大批量大小下則順序草擬然後驗證，並根據草擬信心調整驗證長度，以消除冗餘計算。

##### **Training Neural Networks with Optimal Double-Bayesian Learning**
2605.20009v1 by Vy Bui, Hang Yu, Karthik Kantipudi, Ziv Yaniv, Stefan Jaeger

Backpropagation with gradient descent is a common optimization strategy employed by most neural network architectures in machine learning. However, finding optimal hyperparameters to guide training has proven challenging. While it is widely acknowledged that selecting appropriate parameters is crucial for avoiding overfitting and achieving unbiased outcomes, this choice remains largely based on empirical experiments and experience. This paper presents a new probabilistic framework for the learning rate, a key parameter in stochastic gradient descent. The framework develops classic Bayesian statistics into a double-Bayesian decision mechanism involving two antagonistic Bayesian processes. A theoretically optimal learning rate can be derived from these two processes and used for stochastic gradient descent. Experiments across various classification, segmentation, and detection tasks corroborate the practical significance of the theoretically derived learning rate. The paper also discusses the ramifications of the proposed double-Bayesian framework for network training and model performance.

摘要：反向傳播與梯度下降是大多數機器學習神經網絡架構中常用的優化策略。<lb/>然而，尋找最佳超參數以指導訓練一直是一個挑戰。<lb/>雖然普遍認為選擇適當的參數對於避免過擬合和實現無偏結果至關重要，但這一選擇在很大程度上仍然基於實證實驗和經驗。<lb/>本文提出了一種新的概率框架，用於學習率，這是隨機梯度下降中的一個關鍵參數。<lb/>該框架將經典的貝葉斯統計發展為一種雙貝葉斯決策機制，涉及兩個對立的貝葉斯過程。<lb/>可以從這兩個過程中推導出理論上的最佳學習率，並用於隨機梯度下降。<lb/>在各種分類、分割和檢測任務中的實驗證實了理論推導的學習率的實際重要性。<lb/>本文還討論了所提出的雙貝葉斯框架對網絡訓練和模型性能的影響。

##### **LLM Benchmark Datasets Should Be Contamination-Resistant**
2605.19999v1 by Ali Al-Lawati, Jason Lucas, Dongwon Lee, Suhang Wang

Benchmark datasets are critical for reproducible, reliable, and discriminative evaluation of LLMs. However, recent studies reveal that many benchmark datasets are included in pretraining corpora, i.e., $\textit{contaminated}$, which diminishes their value as reliable measures of model generalization. In this paper, we argue that benchmark datasets should be $\textit{contamination-resistant}$, i.e., $\textit{unlearnable}$, but support $\textit{inference}$. To accomplish this, we first highlight the wide prevalence of benchmark dataset contamination and outline the properties of contamination-resistant datasets. Second, we highlight how the asymmetry between the inference and training pipelines in the Transformer architecture can be leveraged to support contamination-resistance. Third, we outline mathematical advancements to make these datasets interoperable across various LLM architectures. Based on the above, we call on the community to ensure the reliability of LLM benchmarking by: (i) advancing novel contamination-resistant methodologies, (ii) developing supporting methods and platforms, and (iii) adopting contamination-resistant benchmarks into existing evaluation pipelines.

摘要：基準數據集對於可重複、可靠和具區分性的 LLM 評估至關重要。<lb/>然而，近期研究顯示，許多基準數據集已包含在預訓練語料庫中，即 $\textit{受污染的}$，這降低了它們作為模型泛化可靠測量的價值。<lb/>在本文中，我們主張基準數據集應該是 $\textit{抗污染的}$，即 $\textit{不可學習的}$，但支持 $\textit{推理}$。<lb/>為了實現這一目標，我們首先強調基準數據集污染的廣泛存在，並概述抗污染數據集的特性。<lb/>其次，我們強調如何利用 Transformer 架構中推理和訓練管道之間的非對稱性來支持抗污染性。<lb/>第三，我們概述數學進展，以使這些數據集能夠在各種 LLM 架構中互操作。<lb/>基於上述內容，我們呼籲社群通過以下方式確保 LLM 基準測試的可靠性： (i) 推進新型抗污染方法， (ii) 開發支持方法和平台， (iii) 將抗污染基準納入現有評估管道。

##### **A Case for Agentic Tuning: From Documentation to Action in PostgreSQL**
2605.19988v1 by Hongyu Lin, Mingyu Li, Weichen Zhang, Yihang Lou, Mingjie Xing, Yanjun Wu, Haibo Chen

Documentation has long guided computer system tuning by distilling expert knowledge into per-parameter recommendations. Yet such guides capture only what experts conclude, discarding how they reason. This fundamental gap manifests in three concrete deficiencies: documentation grows stale as software evolves, fails under heterogeneous workloads, and ignores inter-parameter dependencies.
  We propose shifting from static documentation to dynamic action for system tuning. We introduce PerfEvolve, which translates expert tuning methodologies into executable skills that equip LLM-based agents to perform version-consistency verification, workload-specific profiling, and multi-parameter joint optimization. Evaluated on PostgreSQL under TPC-C and TPC-H benchmarks, PerfEvolve outperforms state-of-the-art documentation-driven tuning baselines by up to 35.2%. The tool is available at https://github.com/ISCAS-OSLab/PerfEvolve.

摘要：文檔長期以來一直通過將專家知識提煉成每個參數的建議來指導計算機系統的調優。<lb/>然而這些指南僅捕捉了專家的結論，卻忽略了他們的推理過程。<lb/>這一根本性差距在三個具體缺陷中表現出來：隨著軟體的演變，文檔變得過時，無法在異構工作負載下運行，並忽視了參數之間的依賴關係。<lb/>我們建議將靜態文檔轉變為動態行動以進行系統調優。<lb/>我們介紹了PerfEvolve，它將專家的調優方法轉化為可執行的技能，使基於LLM的代理能夠執行版本一致性驗證、特定工作負載的分析和多參數聯合優化。<lb/>在TPC-C和TPC-H基準測試下對PostgreSQL進行評估，PerfEvolve的表現超過了最先進的基於文檔的調優基準，提升幅度高達35.2%。<lb/>該工具可在https://github.com/ISCAS-OSLab/PerfEvolve獲得。

##### **Learning with Foresight: Enhancing Neural Routing Policy via Multi-Node Lookahead Prediction**
2605.19975v1 by Xia Jiang, Yaoxin Wu, Yew-Soon Ong, Yingqian Zhang

Neural policies have shown promise in solving vehicle routing problems due to their reduced reliance on handcrafted heuristics. However, current training paradigms suffer from a fundamental limitation: they primarily focus on next-node prediction for solution construction, resulting in myopic decision-making that undermines long-horizon planning capacity. To this end, we introduce Multi-node Lookahead Prediction (MnLP), a novel training strategy that extends the supervised learning paradigm to predict multiple future nodes simultaneously. We incorporate causal and discardable MnLP modules that operate exclusively during training, facilitating models to anticipate multi-step decisions while preserving inference-time efficiency. By incorporating multi-depth auxiliary supervision into the loss function, MnLP equips neural policies with the ability of long-range contextual understanding. Experimentally, MnLP outperforms existing training methods, improving the generalization capability of neural policies across various problem sizes, distributions, and real-world benchmarks. Moreover, MnLP can be seamlessly integrated into diverse neural architectures without introducing additional inference overhead.

摘要：神經政策在解決車輛路徑問題方面顯示出潛力，因為它們減少了對手工啟發式的依賴。<lb/>然而，當前的訓練範式存在一個根本性的限制：它們主要專注於下一節點的預測來構建解決方案，導致短視的決策，削弱了長期規劃的能力。<lb/>為此，我們提出了多節點前瞻預測（MnLP），這是一種新穎的訓練策略，擴展了監督學習範式，以同時預測多個未來節點。<lb/>我們引入了因果和可丟棄的MnLP模塊，這些模塊僅在訓練期間運作，幫助模型預測多步決策，同時保持推理時間的效率。<lb/>通過將多層次輔助監督納入損失函數，MnLP使神經政策具備長期上下文理解的能力。<lb/>實驗結果表明，MnLP優於現有的訓練方法，提高了神經政策在各種問題大小、分佈和現實世界基準上的泛化能力。<lb/>此外，MnLP可以無縫地整合到多種神經架構中，而不會引入額外的推理開銷。

##### **Block-Sphere Vector Quantization**
2605.19972v1 by Heesang Ann, Joongkyu Lee, Min-hwan Oh

Vector quantization is a fundamental primitive for scalable machine learning systems, enabling memory-efficient storage, fast retrieval, and compressed inference. Recent rotation-based quantizers such as EDEN, RabitQ, and TurboQuant have introduced strong guarantees and empirical performance, but the surrounding comparisons have been difficult to interpret because they rely on different distortion criteria, probability regimes, and implementation assumptions. As our first contribution, we provide a unified theoretical comparison of these methods and show that their relative advantages are criterion-dependent rather than absolute: EDEN and TurboQuant are favorable for MSE distortion, EDEN is also effective for expected inner-product distortion, and RabitQ provides strong high-probability control. This comparison further clarifies that EDEN provides particularly strong guarantees for expected distortion measures. As our second contribution, we introduce Block-Sphere Quantization (BlockQuant), a new rotation-based block quantization algorithm designed around the spherical geometry of randomly rotated vectors. Unlike coordinate-wise quantizers, BlockQuant quantizes blocks on the sphere, preserving the geometry of rotated embeddings more faithfully. We prove that this block-spherical design theoretically improves over the baselines considered in this paper for both reconstruction MSE and expected inner-product distortion. Our experiments on real embedding datasets and long-context LLM inference tasks show practical gains that are consistent with our theoretical improvements.

摘要：向量量化是可擴展機器學習系統的一個基本原語，能夠實現內存高效存儲、快速檢索和壓縮推理。最近基於旋轉的量化器，如EDEN、RabitQ和TurboQuant，提供了強有力的保證和實證性能，但由於它們依賴於不同的失真標準、概率範疇和實現假設，因此周圍的比較難以解釋。作為我們的第一個貢獻，我們提供了這些方法的統一理論比較，並顯示它們的相對優勢是依賴於標準而非絕對的：EDEN和TurboQuant對於均方誤差（MSE）失真是有利的，EDEN對於期望內積失真也有效，而RabitQ則提供了強大的高概率控制。這一比較進一步澄清了EDEN對於期望失真度量提供了特別強的保證。作為我們的第二個貢獻，我們引入了區塊球面量化（BlockQuant），這是一種新的基於旋轉的區塊量化算法，設計圍繞隨機旋轉向量的球面幾何。與坐標式量化器不同，BlockQuant在球面上進行區塊量化，更忠實地保留了旋轉嵌入的幾何。我們證明這種區塊球面設計在理論上對於本文考慮的重建均方誤差和期望內積失真有改進。我們在真實嵌入數據集和長上下文LLM推理任務上的實驗顯示了與我們理論改進一致的實際增益。

##### **Detecting Fluent Optimization-Based Adversarial Prompts via Sequential Entropy Changes**
2605.19966v1 by Mohammed Alshaalan, Miguel R. D. Rodrigues

Optimization-based adversarial suffixes can jailbreak aligned large language models (LLMs) while remaining fluent, weakening static and windowed perplexity-based detectors. We cast adversarial suffix detection as an online change-point detection problem over the token-level next-token entropy stream. Using the LLM system prompt to estimate a robust baseline, we standardize user-token entropies and apply a one-sided CUSUM statistic. The resulting detector, CPD Online (CPD), is model-agnostic, training-free, runs online, and localizes the adversarial suffix onset. On a benchmark of 1,012 optimization-based suffix attacks (GCG, AutoDAN, AdvPrompter, BEAST, AutoDAN-HGA) and 1,012 perplexity-controlled benign prompts, CPD improves F1 over the strongest windowed-perplexity baseline on all six open-weight chat models (LLaMA-2-7B/13B, Vicuna-7B/13B, Qwen2.5-7B/14B). On LLaMA-2-7B at the canonical CUSUM setting ($k=0$), CPD reaches AUROC $0.88$ and F1 $0.82$. Beyond prompt-level detection, CPD concentrates 79.6% of its triggers inside the adversarial suffix, versus 17-46% for windowed perplexity. Finally, when used as a lightweight gate for LLaMA Guard, CPD reduces guard calls by 17-22% on a high-volume, benign-dominated deployment while preserving guard-level detection quality

摘要：優化基於對抗的後綴可以破解對齊的大型語言模型（LLMs），同時保持流暢性，削弱靜態和基於窗口的困惑度檢測器。我們將對抗後綴檢測視為一個在線變更點檢測問題，針對標記級別的下一標記熵流。使用LLM系統提示來估算穩健的基準，我們標準化用戶標記的熵並應用單側CUSUM統計量。所得到的檢測器，CPD Online（CPD），是模型無關的、無需訓練的、在線運行的，並能定位對抗後綴的開始。在1,012個基於優化的後綴攻擊（GCG、AutoDAN、AdvPrompter、BEAST、AutoDAN-HGA）和1,012個困惑度控制的良性提示的基準上，CPD在所有六個開放權重的聊天模型（LLaMA-2-7B/13B、Vicuna-7B/13B、Qwen2.5-7B/14B）上提高了F1，相較於最強的基於窗口的困惑度基準。在LLaMA-2-7B的經典CUSUM設置下（$k=0$），CPD達到AUROC $0.88$和F1 $0.82$。超越提示級別的檢測，CPD將79.6%的觸發集中在對抗後綴內，而基於窗口的困惑度則為17-46%。最後，當作為LLaMA Guard的輕量級閘門使用時，CPD在高流量、良性主導的部署中將保護調用減少了17-22%，同時保持了保護級別的檢測質量。

##### **World-Ego Modeling for Long-Horizon Evolution in Hybrid Embodied Tasks**
2605.19957v1 by Zuyao Lin, Jianhui Zhang, Peidong Jia, Xiaoguang Zhao, Shanghang Zhang, Xingyu Chen

World models are widely explored in embodied intelligence, yet they typically predict distinct evolutions of the world and the ego within a single stream, where the world captures persistent instruction-agnostic scene regularities and the ego captures robot-centric instruction-conditioned dynamics. This world-ego entanglement leads to a degradation in long-horizon embodied scenarios, particularly in hybrid tasks with interleaved navigation and manipulation behaviors. In this paper, we introduce \emph{World-Ego Modeling}, a new conceptual paradigm that decomposes future evolution into world and ego components. We define the world-ego boundary from three perspectives, i.e., motion-, semantic-, and intention-based views, and analyze three disentanglement strategies with post-, pre-, and full disentanglement. Further, we instantiate this paradigm as the World-Ego Model (WEM), a unified embodied world model that couples an implicit separate world-ego planner with a cascade-parallel mixture-of-experts (CP-MoE) diffusion generator. To enable rigorous evaluation, we further construct HTEWorld, the first benchmark for long-horizon world modeling with hybrid navigation-manipulation tasks, providing 125K video clips (over 4.5M frames) with fine-grained action annotations and 300 multi-turn evaluation trajectories (over 2K instructions). Extensive experiments show that WEM achieves state-of-the-art performance on HTEWorld while remaining competitive on existing manipulation-only benchmarks.

摘要：世界模型在具身智能中被廣泛探討，但它們通常在單一流中預測世界和自我的不同演變，其中世界捕捉持久的無指令場景規律，而自我則捕捉以機器人為中心的指令條件動態。這種世界-自我糾纏導致在長期具身場景中的退化，特別是在具有交錯導航和操作行為的混合任務中。在本文中，我們介紹了\emph{世界-自我建模}，這是一種新的概念範式，將未來的演變分解為世界和自我組件。我們從三個角度定義世界-自我邊界，即基於運動、語義和意圖的觀點，並分析三種解糾纏策略，包括後解糾纏、前解糾纏和完全解糾纏。此外，我們將這一範式具體化為世界-自我模型（WEM），這是一個統一的具身世界模型，將隱式的獨立世界-自我規劃器與級聯並行的專家混合（CP-MoE）擴散生成器相結合。為了實現嚴格的評估，我們進一步構建了HTEWorld，這是首個針對混合導航-操作任務的長期世界建模基準，提供了125K個視頻片段（超過450萬幀）以及精細的動作註釋和300條多輪評估軌跡（超過2000條指令）。大量實驗表明，WEM在HTEWorld上實現了最先進的性能，同時在現有的僅操作基準上仍保持競爭力。

##### **Rethinking How to Remember: Beyond Atomic Facts in Lifelong LLM Agent Memory**
2605.19952v1 by Jingwei Sun, Jianing Zhu, Jiangchao Yao, Tongliang Liu, Bo Han

To enable reliable long-term interaction, LLM agents require a memory system that can faithfully store, efficiently retrieve, and deeply reason over accumulated dialogue history. Most existing methods adopt an extracted fact based paradigm: handcrafted static prompts compress raw dialogues into atomic facts, which are then stored, matched, and injected into downstream reasoning. Nevertheless, such fact-centric designs inevitably discard fine-grained details in original dialogues and fail to support deep reasoning over scattered isolated facts. Moreover, static prompts cannot maintain consistent extraction granularity across diverse dialogue styles. To address these limitations, we propose TriMem, which maintains three coexisting representation granularities, including raw dialogue segments anchored by source identifiers for storage fidelity, extracted atomic facts for efficient memory retrieval, synthesized profiles that aggregate dispersed facts into holistic semantic understanding for deep reasoning. We further adopt TextGrad-based prompt optimization, which iteratively refines extraction and profiling prompts via response quality feedback, achieving lifelong evolution without any parameter updating. Extensive experiments on LoCoMo and PerLTQA across multiple LLM backbones demonstrate that TriMem consistently outperforms strong memory baselines. The code is available at https://TMLR-TriMem.github.io .

摘要：為了實現可靠的長期互動，LLM 代理需要一個記憶系統，能夠忠實地儲存、有效地檢索並深入推理累積的對話歷史。大多數現有方法採用提取事實為基礎的模式：手工製作的靜態提示將原始對話壓縮為原子事實，然後這些事實被儲存、匹配並注入到下游推理中。然而，這種以事實為中心的設計不可避免地會丟失原始對話中的細節，並且無法支持對分散的孤立事實進行深入推理。此外，靜態提示無法在多樣的對話風格中保持一致的提取粒度。為了解決這些限制，我們提出了 TriMem，它維持三種共存的表示粒度，包括由來源標識符錨定的原始對話片段以確保儲存的真實性、有效記憶檢索的提取原子事實，以及將分散事實聚合為整體語義理解以進行深入推理的合成檔案。我們進一步採用基於 TextGrad 的提示優化，通過回應質量反饋迭代地精煉提取和檔案提示，實現終身演化而無需任何參數更新。在多個 LLM 主幹上對 LoCoMo 和 PerLTQA 進行的廣泛實驗表明，TriMem 始終超越強大的記憶基準。代碼可在 https://TMLR-TriMem.github.io 獲得。

##### **GEM: GPU-Variability-Aware Expert to GPU Mapping for MoE Systems**
2605.19945v1 by Sourish Wawdhane, Avinash Kumar, Poulami Das

Mixture-of-Expert (MoE) models enable efficient inference by employing smaller experts and activating only a subset of them per token. MoE serving engines distribute experts across multiple GPUs and route tokens to appropriate GPUs at inference time based on experts activated. They process tokens in lock-step fashion, where tokens within a batch must finish processing before proceeding to the next layer. This synchronization barrier acts as a critical bottleneck because the performance of MoE models is limited by the straggler GPU that finishes last. Stragglers emerge when too many heavily used experts are placed on the same GPU or the slowest GPU. While prior works place experts that balance token loads across GPUs, they all overlook GPU variability and often place highly used experts on the slowest GPUs.
  We propose GEM, GPU-variability-aware Expert Mapping, a framework for GPU variability-aware expert to GPU mapping for MoE models. GEM exploits two insights. First, we must place experts such that each GPU receives non-uniform token loads based on their variability and they all finish processing a layer at about the same time. Our studies show that there are two types of experts: consistent that are used most of the time and temporal that are often used together for the remaining time. Our second insight is that we must place simultaneously used consistent and temporal experts on different GPUs and avoid placing them on slower GPUs to reduce slowdown. GEM gathers the variability profile of GPUs for each model and task and uses the token load distributions per task to map experts to GPUs. Our experiments show that GEM improves end-to-end latency by 7.9% on average and by up to 16.5% compared to the baseline.

摘要：混合專家 (MoE) 模型通過使用較小的專家並僅在每個標記上啟用它們的一部分來實現高效推理。MoE 服務引擎在多個 GPU 之間分配專家，並根據啟用的專家在推理時將標記路由到適當的 GPU。它們以鎖步方式處理標記，其中一批中的標記必須在進入下一層之前完成處理。這種同步障礙成為一個關鍵瓶頸，因為 MoE 模型的性能受到最後完成的落後 GPU 的限制。當過多的高使用率專家被放置在同一 GPU 或最慢的 GPU 上時，落後者就會出現。雖然先前的工作將專家放置在各 GPU 之間以平衡標記負載，但它們都忽視了 GPU 的變異性，並且經常將高使用率的專家放在最慢的 GPU 上。  
我們提出了 GEM，GPU 變異性感知專家到 GPU 的映射框架，旨在為 MoE 模型提供 GPU 變異性感知的專家映射。GEM 利用兩個見解。首先，我們必須放置專家，使每個 GPU 根據其變異性接收不均勻的標記負載，並且它們都在大約相同的時間內完成一層的處理。我們的研究顯示有兩種類型的專家：一致的專家在大多數時間內使用，而臨時的專家則經常在剩餘時間內一起使用。我們的第二個見解是，我們必須將同時使用的一致和臨時專家放置在不同的 GPU 上，並避免將它們放置在較慢的 GPU 上以減少延遲。GEM 收集每個模型和任務的 GPU 變異性配置文件，並使用每個任務的標記負載分佈將專家映射到 GPU。我們的實驗顯示，與基線相比，GEM 平均提高了 7.9% 的端到端延遲，最高可達 16.5%。

##### **A Measure-Theoretic Analysis of Reasoning: Structural Generalization and Approximation Limits**
2605.19944v1 by Yuyang Zhang, Yifu Zhang, Xuehai Zhou, Xiaoyin Chen

While empirical scaling laws for LLM reasoning are well-documented, the theoretical mechanisms governing out-of-distribution (OOD) generalization remain elusive. We formalize reasoning via optimal transport, projecting discrete trajectories into a continuous metric space to quantify domain shifts using the Wasserstein-1 distance. Invoking Kantorovich duality, we bound OOD generalization via architectural Lipschitz continuity and functional approximation limits. This exposes two primary constraints. First, position-dependent attention (e.g., Absolute Positional Encoding) fails to preserve shift invariance, yielding an $Ω(1)$ Lipschitz constant and expected risk, whereas shift-invariant mechanisms (e.g., Rotary Embeddings) preserve equivariance and bound the error. Second, by mapping sequential backtracking to a Dyck-$k$ language, we establish a strict circuit depth lower bound for $\text{TC}^0$ Transformers. Scaling physical layer depth is necessary to avert representation collapse -- a constraint that scaling representation width cannot bypass due to irreducible approximation bounds in Barron spaces. Evaluations across 54 Transformer configurations on combinatorial search corroborate these bounds, demonstrating that generalization risk degrades monotonically with the Wasserstein domain shift.

摘要：雖然 LLM 推理的經驗性縮放法則已被充分記錄，但支配分佈外 (OOD) 泛化的理論機制仍然難以捉摸。我們通過最佳傳輸形式化推理，將離散軌跡投影到連續度量空間，以使用 Wasserstein-1 距離量化領域變化。引用 Kantorovich 對偶性，我們通過架構的 Lipschitz 連續性和函數近似界限來界定 OOD 泛化。這揭示了兩個主要限制。首先，位置依賴的注意力（例如，絕對位置編碼）無法保持位移不變性，產生 $Ω(1)$ 的 Lipschitz 常數和預期風險，而位移不變的機制（例如，旋轉嵌入）則保持等變性並界定誤差。其次，通過將序列回溯映射到 Dyck-$k$ 語言，我們為 $\text{TC}^0$ Transformers 確立了一個嚴格的電路深度下界。擴展物理層深度是必要的，以避免表示崩潰——這是一個擴展表示寬度無法繞過的限制，因為在 Barron 空間中存在不可約的近似界限。在 54 種 Transformer 配置的組合搜索評估中，這些界限得到了證實，顯示出泛化風險隨著 Wasserstein 領域變化而單調下降。

##### **Probabilistic Tiny Recursive Model**
2605.19943v1 by Amin Sghaier, Ali Parviz, Alexia Jolicoeur-Martineau

Tiny Recursive Models (TRM) solve complex reasoning tasks with a fraction of the parameters of modern large language models (LLMs) by iteratively refining a latent state and final answer. While powerful, their deterministic recursion can lead to convergence at suboptimal solutions, without escape mechanism. A common workaround relies on task-specific input perturbations at test time combined with answer aggregation via voting. We introduce Probabilistic TRM (PTRM), a task-agnostic framework for test-time compute scaling that addresses this limitation through stochastic exploration. PTRM injects Gaussian noise at each deep recursion step, enabling parallel trajectories to explore diverse solution basins, and selects among them using the model's existing Q head (used for early stopping in the original TRM). Without requiring retraining or task-specific augmentations, PTRM enables substantial accuracy gains across benchmarks, including Sudoku-Extreme (87.4% to 98.75%) and on various puzzles from Pencil Puzzle Bench (62.6% to 91.2%). On the latter, PTRM achieves nearly double the accuracy of frontier LLMs (91.2% vs. 55.1%) at less than 0.0001x the cost, using only 7M parameters.

摘要：微型遞迴模型（TRM）以現代大型語言模型（LLMs）參數的極小部分解決複雜的推理任務，通過迭代地細化潛在狀態和最終答案。雖然強大，但其確定性遞迴可能導致收斂於次優解，且沒有逃脫機制。一種常見的變通方法依賴於在測試時的任務特定輸入擾動，並通過投票進行答案聚合。我們介紹了概率性TRM（PTRM），這是一種無任務特定的測試時計算擴展框架，通過隨機探索來解決這一限制。PTRM在每個深度遞迴步驟中注入高斯噪聲，使得平行軌跡可以探索多樣的解決盆地，並使用模型現有的Q頭（用於原始TRM中的早期停止）在它們之間進行選擇。PTRM無需重新訓練或任務特定的增強，能在基準測試中實現顯著的準確性提升，包括數獨極限（從87.4%提升至98.75%）以及來自鉛筆謎題基準的各種謎題（從62.6%提升至91.2%）。在後者中，PTRM的準確性幾乎是前沿LLMs的兩倍（91.2%對比55.1%），成本卻不到0.0001倍，僅使用了700萬個參數。

##### **Robotics-Inspired Guardrails for Foundation Models in Socially Sensitive Domains**
2605.19940v1 by Rebecca Ramnauth, Drazen Brscic, Brian Scassellati

Foundation models are increasingly deployed in socially sensitive domains such as education, mental health, and caregiving, where failures are often cumulative and context-dependent. Existing guardrail approaches -- ranging from training-time alignment to prompting, decoding constraints, and post-hoc moderation -- primarily provide empirical risk reduction rather than enforceable behavioral guarantees, and largely treat safety as a property of individual outputs rather than interaction trajectories. We reframe guardrails as a problem of runtime behavioral control over interaction trajectories, drawing on robotics to introduce formal constructs for constraint enforcement in uncertain, closed-loop systems. We instantiate these ideas in the Grounded Observer framework and apply it across three real-world deployments: small talk, in-home autism therapy, and behavioral de-escalation in schools. Across settings, the framework enables runtime interventions that mitigate drift into undesirable interaction regimes while adapting to diverse social contexts. We discuss extensions to the framework and propose research directions toward stronger guarantees.

摘要：基礎模型越來越多地應用於社會敏感領域，如教育、心理健康和護理，這些領域的失敗往往是累積性的且依賴於上下文。現有的防護措施——從訓練時的對齊到提示、解碼約束和事後調節——主要提供經驗風險降低，而不是可執行的行為保證，並且在很大程度上將安全視為單個輸出的屬性，而不是互動軌跡。我們將防護措施重新定義為對互動軌跡的運行時行為控制問題，借鑒機器人技術引入不確定的閉環系統中約束執行的正式構造。我們在基於實地觀察者的框架中實現這些想法，並將其應用於三個現實世界的部署：小型對話、家庭自閉症治療和學校中的行為降級。在各種環境中，該框架實現了運行時干預，減少了向不良互動範疇的漂移，同時適應多樣的社會背景。我們討論了對該框架的擴展，並提出了加強保證的研究方向。

##### **What Are LLMs Doing to Scientific Communication? Measuring Changes in Writing Practices and Reading Experience**
2605.19936v1 by Filip Miletić, Neele Falk

Has the style of scientific communication changed due to the growing use of large language models in the writing process? We address this question in the domain of Natural Language Processing by leveraging two data resources we create: a naturalistic corpus of over 37,000 papers from the ACL Anthology (2020-2024); and a synthetic dataset of 3,000 human-written passages and their LLM-generated improvements. We first implement a series of diachronic lexical analyses, showing that both word frequency and usage contexts have changed significantly over time, indicating semantic specialization in some cases and generalization in others. Broadening our perspective, we then model a range of more complex stylistic features and find that LLM-modified texts more frequently contain certain syntactic constructions, more complex and longer words and a lower lexical diversity. Finally, we connect these changes in writing practices to subjective reading experience through a pilot annotation study with 20 domain experts. They overall rate LLM-improved texts as more understandable and exciting, but also express negative qualitative attitudes towards LLMs, highlighting the strongly subjective effect of AI-assisted writing on reading experience.

摘要：科學溝通的風格是否因為大型語言模型在寫作過程中的日益使用而發生了變化？我們在自然語言處理領域通過利用我們創建的兩個數據資源來解決這個問題：一個來自ACL文集（2020-2024）的自然語料庫，包含超過37,000篇論文；以及一個包含3,000篇人類撰寫段落及其LLM生成改進的合成數據集。我們首先實施了一系列歷時性的詞彙分析，顯示出單詞頻率和使用語境隨時間顯著變化，表明在某些情況下語意專門化，而在其他情況下則是一般化。擴大我們的視角後，我們建模了一系列更複雜的風格特徵，發現LLM修改的文本更頻繁地包含某些句法結構，更複雜和更長的單詞，以及較低的詞彙多樣性。最後，我們通過一項與20位領域專家的初步標註研究將這些寫作實踐的變化與主觀閱讀體驗聯繫起來。他們普遍評價LLM改進的文本更易於理解和更具吸引力，但也表達了對LLM的負面質性態度，突顯了AI輔助寫作對閱讀體驗的強烈主觀影響。

##### **PEEK: Context Map as an Orientation Cache for Long-Context LLM Agents**
2605.19932v1 by Zhuohan Gu, Qizheng Zhang, Omar Khattab, Samuel Madden

Large language model (LLM) agents increasingly operate over long and recurring external contexts, like document corpora and code repositories. Across invocations, existing approaches preserve either the agent's trajectory, passive access to raw material, or task-level strategies. None of them preserves what we argue is most needed for repeated same-context workloads: reusable orientation knowledge (e.g., what the context contains, how it is organized, and which entities, constants, and schemas have historically been useful) about the recurring context itself. We introduce PEEK, a system that caches and maintains this orientation knowledge as a context map: a small, constant-sized artifact in the agent's prompt that gives it a persistent peek into the external context. The map is maintained by a programmable cache policy with three modules: a Distiller that extracts transferable knowledge from inference-time signals, a Cartographer that translates it into structured edits, and a priority-based Evictor that enforces a fixed token budget. On long-context reasoning and information aggregation, PEEK improves over strong baselines by 6.3-34.0% while using 93-145 fewer iterations and incurring 1.7-5.8x lower cost than the state-of-the-art prompt-learning framework, ACE. On context learning, PEEK improves solving rate and rubric accuracy by 6.0-14.0% and 7.8-12.1%, respectively, at 1.4x lower cost than ACE. These gains generalize across LMs and agent architectures, including OpenAI Codex, a production-grade coding agent. Together, these results show that a context map helps long-context LLM agents interact with recurring external contexts more accurately and efficiently.

摘要：大型語言模型（LLM）代理越來越多地在長期和重複的外部上下文中運作，例如文檔語料庫和代碼庫。現有的方法在多次調用中要麼保留代理的軌跡，要麼被動地訪問原材料，或是任務級別的策略。它們都未能保留我們認為對於重複相同上下文工作負載最需要的：可重用的定向知識（例如，上下文包含什麼、如何組織，以及哪些實體、常量和模式在歷史上曾經有用）關於重複的上下文本身。我們介紹了PEEK，一個將這些定向知識緩存並維護為上下文地圖的系統：一個在代理提示中大小固定的小型工件，為其提供持久的外部上下文預覽。該地圖由一個可編程的緩存策略維護，該策略包含三個模塊：一個提煉器，從推理時信號中提取可轉移的知識；一個製圖者，將其轉換為結構化編輯；以及一個基於優先級的驅逐器，強制執行固定的標記預算。在長上下文推理和信息聚合方面，PEEK在使用93-145次更少的迭代且成本比最先進的提示學習框架ACE低1.7-5.8倍的情況下，超越了強基準6.3-34.0%。在上下文學習方面，PEEK的解決率和評分準確度分別提高了6.0-14.0%和7.8-12.1%，成本比ACE低1.4倍。這些增益在各種LM和代理架構中具有普遍性，包括OpenAI Codex，一個生產級的編碼代理。綜合這些結果顯示，上下文地圖幫助長上下文LLM代理更準確和高效地與重複的外部上下文互動。

##### **StruMPL: Multi-task Dense Regression under Disjoint Partial Supervision and MNAR Labels**
2605.19931v1 by Reza M. Asiyabi, Juan Alberto Molina-Valero, The SEOSAW Partnership, Steven Hancock, Casey M. Ryan

Estimating forest aboveground biomass (AGB) from Earth observation combines two structurally incompatible label sources: spaceborne lidar provides canopy structure at millions of locations but no biomass estimate, and ground-based plots provide biomass at thousands of biased locations but no metrics of structure. No single training sample carries labels for all target variables, plot labels are missing not at random (MNAR), and biomass is linked to the structural variables by known but biome-specific allometric laws. We formalise this as multi-task dense regression under heterogeneous disjoint partial supervision with MNAR labels and inter-task physical constraints, and propose StruMPL to address it jointly. A shared encoder feeds per-variable regression, imputation, and propensity heads for spatial MNAR correction, and a learnable physics module that evaluates the inter-task constraint on the model's own predictions at every pixel. The supervised loss uses an Augmented IPW (AIPW) pseudo-outcome with stop-gradients on the propensity and on the imputation baseline; we show analytically and empirically that both are necessary for joint optimisation to recover IPW-weighted stationary points while keeping the loss bounded. On two ecologically distinct biomes, StruMPL outperforms ablation variants and the closest published method on AGB RMSE and bias, with a stratified analysis showing AIPW reduces high-AGB bias by ~54%.

摘要：估算森林地上生物量（AGB）來自地球觀測結合了兩個結構上不相容的標籤來源：太空激光雷達提供了數百萬個位置的冠層結構，但沒有生物量估算，而基於地面的樣地提供了數千個偏倚位置的生物量，但沒有結構指標。沒有單一的訓練樣本攜帶所有目標變量的標籤，樣地標籤的缺失不是隨機的（MNAR），而生物量與結構變量之間的關聯是由已知但特定生物群落的異速生長法則所連結。我們將此形式化為在異質不相交部分監督下的多任務密集回歸，具有MNAR標籤和任務間的物理約束，並提出StruMPL以共同解決此問題。共享編碼器為每個變量提供回歸、插補和傾向頭，用於空間MNAR校正，並且有一個可學習的物理模塊，該模塊在每個像素上評估模型自身預測的任務間約束。監督損失使用增強的IPW（AIPW）偽結果，並對傾向和插補基線進行停止梯度；我們從理論和實證上表明，這兩者對於聯合優化以恢復IPW加權的靜態點是必要的，同時保持損失有界。在兩個生態上明顯不同的生物群落中，StruMPL在AGB RMSE和偏差上超越了消融變體和最近發表的方法，分層分析顯示AIPW將高AGB的偏差降低了約54%。

##### **Breaking Modality Heterogeneity in Low-Bit Quantization for Large Vision-Language Models**
2605.19929v1 by Yi Zhong, Haotong Qin, Xindong Zhang, Lei Zhang, Guolei Sun

Low-bit post-training quantization (PTQ) is a pivotal technique for deploying Vision-Language Models (VLMs) on resource-constrained devices. However, existing PTQ methods often degrade VLMs' accuracy due to the heterogeneous activation distributions of text and vision modalities during quantization. We find that this cross-modal heterogeneity is distributed unevenly across channels: a small subset of channels contains most modality-specific outliers, and these outliers typically reside in different channels for each modality. Motivated by this, we propose SplitQ, a channel-Splitting-driven post-training Quantization framework. At its core, SplitQ introduces a novel Modality-specific Outlier Channel Decoupling (MOCD) module that effectively isolates salient modality-specific outlier channels with minimal overhead. To further address the remaining cross-modal distribution discrepancies, we design an Adaptive Cross-Modal Calibration (ACC) module that employs dual lightweight learnable branches to dynamically mitigate modality-induced quantization errors. Extensive experiments on popular VLMs demonstrate that SplitQ significantly outperforms existing approaches across 6 popular multi-modal datasets under all evaluated quantization settings, including W4A8, W4A4, W3A3, and W3A2. Notably, SplitQ preserves 93.5% of FP16 performance under the challenging W3A3 setting (69.5 vs. 74.3), pushing the efficiency frontier for deploying advanced VLMs. Our code is available at https://github.com/EMVision-NK/SplitQ

摘要：低位元後訓練量化（PTQ）是將視覺-語言模型（VLMs）部署在資源受限設備上的關鍵技術。<lb/>然而，現有的PTQ方法往往因為量化過程中文本和視覺模態的異質性激活分佈而降低VLM的準確性。<lb/>我們發現這種跨模態的異質性在通道間分佈不均：一小部分通道包含大多數模態特定的異常值，而這些異常值通常位於每個模態的不同通道中。<lb/>受到此啟發，我們提出了SplitQ，一個以通道分割為驅動的後訓練量化框架。<lb/>SplitQ的核心是一個新穎的模態特定異常通道解耦（MOCD）模塊，能有效地以最小的開銷隔離顯著的模態特定異常通道。<lb/>為了進一步解決剩餘的跨模態分佈差異，我們設計了一個自適應跨模態校準（ACC）模塊，該模塊使用雙輕量級可學習分支來動態減輕模態引起的量化誤差。<lb/>在流行的VLM上進行的大量實驗表明，SplitQ在所有評估的量化設置下，包括W4A8、W4A4、W3A3和W3A2，顯著超越現有方法，涵蓋6個流行的多模態數據集。<lb/>值得注意的是，SplitQ在具有挑戰性的W3A3設置下保留了93.5%的FP16性能（69.5對74.3），推動了部署先進VLM的效率邊界。<lb/>我們的代碼可在https://github.com/EMVision-NK/SplitQ獲得。

##### **Where Does Authorship Signal Emerge in Encoder-Based Language Models?**
2605.19908v1 by Francis Kulumba, Guillaume Vimont, Laurent Romary, Florian Cafiero

Authorship attribution models fine-tuned with the same pretrained encoder, data, and loss can differ four-fold in performance depending only on their scoring mechanism. We use mechanistic interpretability tools to explain this gap. Stylistic features such as word length, punctuation density, and function-word frequency are equally available at every layer in every model, including in an off-the-shelf control encoder, hence the gap not coming from representation quality. Instead, causal intervention shows that the scorer determines where the encoder consolidates authorship signal. Mean pooling forces consolidation by early to mid layers, while late interaction defers it to later layers. We further derive this difference from the gradient structure of each scorer, and training dynamics reveal distinct learning trajectories that follow from that difference.

摘要：作者歸屬模型在相同的預訓練編碼器、數據和損失下，根據其評分機制的不同，性能可以相差四倍。我們使用機械解釋工具來解釋這一差距。風格特徵如單詞長度、標點密度和功能詞頻率在每個模型的每一層都是同樣可用的，包括在現成的控制編碼器中，因此這一差距並不來自於表示質量。相反，因果介入顯示評分器決定了編碼器整合作者信號的位置。均值池化強迫在早期到中期層進行整合，而晚期交互則將其推遲到後期層。我們進一步從每個評分器的梯度結構中推導出這一差異，訓練動態揭示出由於這一差異而產生的不同學習軌跡。

##### **Streamlined Constraint Reasoning via CNN Pattern Recognition on Enumerated Solutions**
2605.19895v1 by Patrick Spracklen

Constraint programming practitioners accelerate hard problems through a layered set of techniques applied in order of risk. Standard hardening (symmetry-breaking and implied constraints) is applied first and preserves satisfiability. Streamliner constraints, which restrict search to a structural sub-family of solutions, do not preserve satisfiability and are reserved as a final lever. Existing automated streamliner-synthesis approaches either search a constraint grammar or prompt a Large Language Model directly on the problem model. We propose a different approach: enumerate feasible solutions, train a Convolutional Neural Network contrastively against perturbed non-solutions to detect structural patterns, and translate the CNN's discriminative signal into candidate MiniZinc streamliners through LLM-driven synthesis. The CNN grounds the LLM's constraint generation in observed solution structure rather than model text alone. We evaluate on hardened benchmark models where streamliner discovery is the residual performance lever. Our pipeline achieves 98.8% portfolio time reduction on hardened Vessel Loading, 98.6% on hardened Social Golfers, and 89.4% on Black Hole, with best-single streamliners reaching geometric-mean speedups of 932x, 356x, and 1103x respectively. Discovered streamliners include class-based packing constraints on Vessel Loading, beyond-hardening canonicalisations on Social Golfers, and layout-coordinate bounds on Black Hole.

摘要：約束編程的實踐者通過一套分層技術來加速困難問題，這些技術按風險順序應用。標準硬化（對稱打破和隱含約束）首先應用，並保持可滿足性。流線型約束限制搜索到結構性子族解，並不保持可滿足性，因此被保留作為最後的手段。現有的自動流線型合成方法要麼搜索約束文法，要麼直接在問題模型上提示大型語言模型。我們提出了一種不同的方法：列舉可行解，對抗性地訓練卷積神經網絡以檢測結構模式，並將CNN的判別信號轉換為候選MiniZinc流線型約束，通過LLM驅動的合成。CNN使LLM的約束生成基於觀察到的解結構，而不僅僅是模型文本。我們在硬化基準模型上進行評估，流線型發現是剩餘性能的杠杆。我們的管道在硬化的船舶裝載上實現了98.8%的時間減少，在硬化的社交高爾夫球手上達到98.6%，在黑洞上達到89.4%，最佳單一流線型約束的幾何平均加速分別達到932倍、356倍和1103倍。發現的流線型約束包括船舶裝載上的基於類別的打包約束、社交高爾夫球手上的超硬化典範化，以及黑洞上的佈局坐標邊界。

##### **Deep Tech to Space: Space Data Centers and AI Revolution at the Edge**
2605.19892v1 by Jonas Weiss, Patricia Sagmeister, Gabriel Maiolini Capez, Dinesh Verma, Roberto Garello, Alberto Perotti, Dawid Lazaj, Alicja Musial, Jakub Nalepa, Thomas Morf, Martin Schmatz, Marek Krawczyk, Mateusz Przeliorz, Kevin Roche, Sagar Tayal, Mahalakshmi Lakshminarayanan, Nicolas Longépé, Pierre-Philippe Mathieu, Agata Wijata

Dramatic cost reductions driven by private sector innovations have led to a rapid increase in the number of satellites in orbit and a corresponding surge in space-generated data. As this trend continues, transmitting large volumes of data to Earth for processing may become increasingly costly and challenging due to potential space-to-Earth link congestion and increased latency. Moreover, traditional ground station networks may face difficulties accommodating growing data flows and workloads because of capacity constraints, complex scheduling logistics, and restricted visibility windows, which can limit scalability. Space Data Centers (SDCs) -- software-driven, multi-tenant artificial intelligence-based service platforms capable of processing data in orbit to generate actionable insights for client satellites and ground users -- represent a promising approach to address these challenges.
  This article presents the architecture of a Low Earth Orbit SDC satellite constellation, considering orbital design, inter-satellite links and network topology, computational resource organization, and software service orchestration. We analyze the potential technical feasibility and economic viability of SDCs using forecasting models informed by technology roadmaps and illustrate the concept through Earth observation and lunar exploration use cases.

摘要：劇烈的成本降低源於私營部門的創新，導致衛星在軌道上的數量迅速增加，以及空間生成數據的相應激增。隨著這一趨勢的持續，將大量數據傳輸回地球進行處理可能變得越來越昂貴和具有挑戰性，因為可能會出現空間到地球鏈路擁塞和延遲增加的情況。此外，傳統的地面站網絡可能面臨因容量限制、複雜的調度物流和受限的可見窗口而無法適應不斷增長的數據流和工作負載的困難，這可能限制可擴展性。空間數據中心（SDCs）——基於軟體驅動的多租戶人工智慧服務平台，能夠在軌道上處理數據，為客戶衛星和地面用戶生成可行的見解——代表了一種有前景的方法來應對這些挑戰。  
本文介紹了一個低地球軌道SDC衛星星座的架構，考慮了軌道設計、衛星間鏈路和網絡拓撲、計算資源組織以及軟體服務編排。我們使用受技術路線圖啟發的預測模型分析SDCs的潛在技術可行性和經濟可行性，並通過地球觀測和月球探索的使用案例來說明這一概念。

##### **StableGrad: Backward Scale Control without Batch Normalization**
2605.19856v1 by Jose I. Mestre, Alberto Fernández-Hernández, Cristian Pérez-Corral, Manuel F. Dolz, Enrique S. Quintana-Ortí

Training very deep neural networks requires controlling the propagation of magnitudes across depth. Without such control, activations and gradients may vanish, explode, or enter unstable regimes that make optimization fail. Modern architectures often mitigate this problem through Batch Normalization, residual connections, or other normalization layers, which repeatedly re-scale or bypass intermediate representations. However, these mechanisms are not always appropriate. In Physics-Informed Neural Networks (PINNs), the network represents a continuous physical field and its input derivatives define the training objective, making batch-dependent normalization problematic because it can introduce non-local dependencies into the predicted field and its derivatives. We propose StableGrad, an optimizer-level scale-control mechanism that corrects layer-wise weight-gradient imbalances without modifying the forward model. Because the normalization is applied only after backpropagation and before the optimizer update, the network output, its derivatives, and the physical residual remain unchanged. We analyze the effective training dynamics induced by this rescaling and evaluate StableGrad on deep PINNs as the target application, with BatchNorm-free convolutional networks serving as a diagnostic stress test. On PINN benchmarks, StableGrad improves matched-depth solution accuracy and makes deeper models more reliable under standard optimization. On ResNet and EfficientNet architectures, where removing Batch Normalization normally leads to training collapse, StableGrad stabilizes optimization without introducing any other architectural change. These results show that optimizer-level control of weight-gradient scale can provide a practical alternative when forward normalization is unavailable or undesirable.

摘要：訓練非常深的神經網絡需要控制深度中的幅度傳播。<lb/>如果沒有這樣的控制，激活值和梯度可能會消失、爆炸，或進入不穩定的狀態，導致優化失敗。<lb/>現代架構通常通過批量正規化、殘差連接或其他正規化層來緩解這個問題，這些方法反覆重新縮放或繞過中間表示。<lb/>然而，這些機制並不總是合適的。在物理知識神經網絡（PINNs）中，網絡表示一個連續的物理場，其輸入導數定義了訓練目標，這使得依賴批量的正規化變得問題重重，因為它可能會將非局部依賴引入預測的場及其導數。<lb/>我們提出了StableGrad，一種優化器級別的縮放控制機制，可以在不修改前向模型的情況下糾正層級權重梯度的不平衡。<lb/>因為正規化僅在反向傳播之後和優化器更新之前應用，所以網絡輸出、其導數和物理殘差保持不變。<lb/>我們分析了這種重新縮放所引起的有效訓練動態，並在深層PINNs上評估StableGrad作為目標應用，無批量正規化的卷積網絡作為診斷壓力測試。<lb/>在PINN基準測試中，StableGrad提高了匹配深度解的準確性，並使得在標準優化下更深的模型更加可靠。<lb/>在ResNet和EfficientNet架構中，移除批量正規化通常會導致訓練崩潰，而StableGrad則在不引入任何其他架構變更的情況下穩定了優化。<lb/>這些結果表明，權重梯度縮放的優化器級別控制可以在前向正規化不可用或不理想時提供一種實用的替代方案。

##### **A Framework for Evaluating Zero-Shot Image Generation in Concept-based Explainability**
2605.19855v1 by Giacomo Astolfi, Matteo Bianchi, Riccardo Campi, Antonio De Santis, Marco Brambilla

Concept-based Explainable Artificial Intelligence (XAI) interprets deep learning models using human-understandable visual features (e.g., textures or object parts) by linking internal representations to class predictions, thereby bridging the gap between low-level image data and high-level semantics. A major challenge, however, is the reliance on large sets of labeled images to represent each concept, which limits scalability. In this work, we investigate the use of zero-shot Text-to-Image (T2I) generative models as a source of synthetic concept datasets for concept-based XAI methods. Specifically, we generate concepts using predefined prompts and evaluate their faithfulness to real ones through four complementary analyses: (1) comparing synthetic vs. real concept images via concept representation similarity; (2) evaluating their intra-similarity by comparing pairs of subsets of the same concept with progressively increasing size; (3) evaluating their performance for downstream explanation tasks using relevant class images; (4) evaluating how removing a concept from tested class images affects explanations of generated concepts. While current T2I generative models promise a shortcut to concept-based XAI, our study highlights challenges and raises open questions about the use of synthetic data generated by zero-shot pipelines in model analyses. The resulting dataset is available at https://github.com/DataSciencePolimi/ZeroShot-T2I-Concepts.

摘要：概念基礎的可解釋人工智慧（XAI）透過將內部表示與類別預測聯繫起來，使用人類可理解的視覺特徵（例如，紋理或物體部分）來解釋深度學習模型，從而彌合低階影像數據與高階語義之間的差距。然而，一個主要挑戰是依賴大量標記圖像來代表每個概念，這限制了可擴展性。在本研究中，我們調查了使用零樣本文本到影像（T2I）生成模型作為概念基礎的 XAI 方法的合成概念數據集來源。具體而言，我們使用預定義的提示生成概念，並通過四種互補分析評估其對真實概念的忠實度：（1）通過概念表示相似性比較合成與真實概念圖像；（2）通過比較同一概念的子集對，評估其內部相似性，並逐步增加大小；（3）使用相關類別圖像評估其在下游解釋任務中的表現；（4）評估從測試類別圖像中移除概念如何影響生成概念的解釋。雖然當前的 T2I 生成模型為概念基礎的 XAI 提供了一條捷徑，但我們的研究突顯了挑戰並提出了關於在模型分析中使用零樣本管道生成的合成數據的未解決問題。所得到的數據集可在 https://github.com/DataSciencePolimi/ZeroShot-T2I-Concepts 獲得。

##### **Are Tools Always Beneficial? Learning to Invoke Tools Adaptively for Dual-Mode Multimodal LLM Reasoning**
2605.19852v1 by Qinghe Ma, Zhen Zhao, Yiming Wu, Jian Zhang, Lei Bai, Yinghuan Shi

Tool-augmented reasoning has emerged as a promising direction for enhancing the reasoning capabilities of multimodal large language models (MLLMs). However, existing studies mainly focus on enabling models to perform tool invocation, while neglecting the necessity of invoking tools. We argue that tool usage is not always beneficial, as redundant or inappropriate invocations largely increase reasoning overhead and even mislead model predictions. To address this issue, we introduce AutoTool, a model that adaptively decides whether to invoke tools according to the characteristics of each query. Within a reinforcement learning framework, we design an explicit dual-mode reasoning strategy with mode-specific reward functions to guide the model toward producing accurate responses. Moreover, to prevent premature bias toward a single reasoning mode, AutoTool jointly explores and balances tool-assisted and text-centric reasoning throughout training, and promotes free exploration in later stages. Extensive experiments demonstrate that AutoTool exhibits outstanding performance and high efficiency, yielding a 21.8\% accuracy gain on V* benchmark compared to the base model, and a 44.9\% improvement in efficiency over existing tool-augmented methods on POPE benchmark. Code is available at https://github.com/MQinghe/AutoTool.

摘要：工具增強推理已成為提升多模態大型語言模型（MLLMs）推理能力的一個有前景的方向。<lb/>然而，現有研究主要集中在使模型能夠執行工具調用，而忽略了調用工具的必要性。<lb/>我們認為，工具的使用並不總是有益的，因為冗餘或不當的調用會大幅增加推理開銷，甚至誤導模型預測。<lb/>為了解決這個問題，我們引入了AutoTool，一個根據每個查詢的特徵自適應決定是否調用工具的模型。<lb/>在強化學習框架內，我們設計了一種明確的雙模式推理策略，並使用特定模式的獎勵函數來引導模型產生準確的回應。<lb/>此外，為了防止對單一推理模式的過早偏見，AutoTool在訓練過程中共同探索並平衡工具輔助和文本中心的推理，並在後期階段促進自由探索。<lb/>大量實驗表明，AutoTool展現出卓越的性能和高效率，與基準模型相比，在V*基準上獲得了21.8\%的準確率提升，在POPE基準上則比現有的工具增強方法提高了44.9\%的效率。<lb/>代碼可在 https://github.com/MQinghe/AutoTool 獲得。

##### **CLIF: Concept-Level Influence Functions for Transparent Bottleneck Models**
2605.19848v1 by Yike Sun, Mingkun Xu, Mu You, Zhongzhi He, Henghua Shen, Zehan Tan, Derek F. Wong, Tao Fang

In recent years, the black-box nature of deep learning models has limited their application in high-stakes domains such as medical diagnosis and finance, where interpretability is essential. To address this, we propose a novel approach using influence functions to enhance interpretability in NLP models at both the sample and concept levels. Experiments on CEBaB and Yelp datasets show that influence functions effectively identify the most impactful training samples, both helpful and harmful, on model predictions. By adjusting the labels and weights of these samples, we demonstrate that model performance can be restored to baseline levels without retraining, confirming the value of influence functions for efficient data debugging. Furthermore, our concept-level analysis identifies key concepts within Concept Bottleneck Models (CBM) that significantly affect predictions. Modifying these concepts alters model behavior observably, providing clear insights into the decision process.

摘要：近年來，深度學習模型的黑箱特性限制了它們在醫療診斷和金融等高風險領域的應用，這些領域對可解釋性有著重要需求。為了解決這個問題，我們提出了一種新穎的方法，使用影響函數來增強自然語言處理模型在樣本和概念層面的可解釋性。對CEBaB和Yelp數據集的實驗顯示，影響函數有效地識別出對模型預測影響最大的訓練樣本，包括有幫助和有害的樣本。通過調整這些樣本的標籤和權重，我們證明模型性能可以在不重新訓練的情況下恢復到基線水平，確認了影響函數在高效數據調試中的價值。此外，我們的概念層級分析識別了在概念瓶頸模型（CBM）中顯著影響預測的關鍵概念。修改這些概念會明顯改變模型行為，提供對決策過程的清晰洞察。

##### **FineBench: Benchmarking and Enhancing Vision-Language Models for Fine-grained Human Activity Understanding**
2605.19846v1 by Gueter Josmy Faure, Min-Hung Chen, Jia-Fong Yeh, Hung-Ting Su, Winston H. Hsu

Vision-Language Models (VLMs) have demonstrated remarkable capabilities in general video understanding, yet they often struggle with the fine-grained comprehension crucial for real-world applications requiring nuanced interpretation of human actions and interactions. While some recent human-centric benchmarks evaluate aspects of model behaviour such as fairness/ethics, emotion perception, and broader human-centric metrics, they do not combine long-form videos, very dense QA coverage, and frame-level spatial/temporal grounding at scale. To bridge this gap, we introduce FineBench, a human-centric video question answering (VQA) benchmark specifically designed to assess fine-grained understanding. FineBench comprises 199,420 multiple-choice QA pairs densely annotated across 64 long-form videos (15 minutes each), focusing on detailed person movement, person interaction, and object manipulation, including compositional actions. Our extensive evaluation reveals that while proprietary models like GPT-5 achieve respectable performance, current open-source VLMs significantly underperform, struggling particularly with spatial reasoning in multi-person scenes and distinguishing subtle differences in human movements and interactions. To address these identified weaknesses, we propose FineAgent, a modular framework that enhances VLMs by leveraging a Localizer and a Descriptor. Experiments show that FineAgent consistently improves the performance of various open VLMs on FineBench. FineBench provides a rigorous testbed for future research into fine-grained human-centric video understanding, while FineAgent offers a practical approach to enhance such reasoning in current VLMs.

摘要：視覺-語言模型（VLMs）在一般視頻理解方面展現了卓越的能力，然而它們在需要對人類行動和互動進行細緻解釋的現實應用中，往往難以達到細緻的理解。雖然一些近期以人為中心的基準評估了模型行為的各個方面，如公平性/倫理、情感感知以及更廣泛的人為中心指標，但它們並未在規模上結合長格式視頻、非常密集的問答覆蓋以及幀級空間/時間基準。為了填補這一空白，我們推出了FineBench，一個專門設計用於評估細緻理解的人為中心視頻問答（VQA）基準。FineBench包含199,420個多選問答對，密集註釋於64個長格式視頻（每個15分鐘），重點關注詳細的人物移動、人物互動和物體操作，包括組合行為。我們的廣泛評估顯示，雖然像GPT-5這樣的專有模型達到了可觀的性能，但目前的開源VLM在多人物場景中的空間推理和區分人類動作及互動中的微妙差異方面表現顯著不足。為了解決這些識別出的弱點，我們提出了FineAgent，一個通過利用定位器和描述符來增強VLM的模組化框架。實驗表明，FineAgent持續改善了各種開放VLM在FineBench上的性能。FineBench為未來對細緻人為中心視頻理解的研究提供了一個嚴謹的測試平台，而FineAgent則提供了一種實用的方法來增強當前VLM中的這種推理能力。

##### **CADENet: Condition-Adaptive Asynchronous Dual-Stream Enhancement Network for Adverse Weather Perception in Autonomous Driving**
2605.19837v1 by Sherif Khairy, Catherine M. Elias

Adverse weather (rain, fog, sand, and snow) degrades camera-based object detection in autonomous vehicles. Existing enhancement-then-detect approaches stall the safety-critical perception loop, violating hard real-time requirements. Progress on this problem is also constrained by an under-recognized evaluation ceiling: ground truth annotated on degraded images cannot credit a detector that recovers objects the annotators themselves could not see, so a genuinely useful enhancement can register as a near-flat F1 gain. This paper presents CADENet (Condition-Adaptive Asynchronous Dual-stream Enhancement Network), a training-free three-thread system: Thread S (YOLOv11n) delivers detections at full frame rate with zero added latency; Thread Q applies condition-adaptive enhancement (CAPE) and fuses results via entropy-guided NMS (EG-NMS) without blocking Thread S; Thread E provides CLIP zero-shot weather classification, so new weather categories require only a new text prompt, with no labeled data and no retraining. Evaluated on 1327 DAWN images (YOLOv11m, IoU = 0.5, confidence = 0.25), CADENet achieves Recall = 0.0103 (micro), F1 = 0.0230 on snow, and F1 = 0.0038 on rain. We formalize the annotation completeness bias on DAWN-class data, so the reported F1 values are lower bounds on the true gain; recall is the annotation-gap-immune headline metric. Thread S sustains approximately 44 FPS regardless of enhancement load. No model retraining or additional sensor hardware is required.

摘要：不利的天氣（雨、霧、沙和雪）會降低自動駕駛車輛中基於相機的物體檢測。現有的增強後檢測方法會阻礙安全關鍵的感知循環，違反硬實時要求。這個問題的進展也受到一個不被充分認識的評估上限的限制：在降級圖像上標註的真實情況無法給予一個檢測器信用，因為該檢測器能恢復標註者自己無法看到的物體，因此一個真正有用的增強可能會被記錄為接近平坦的 F1 增益。本文提出了 CADENet（條件自適應異步雙流增強網絡），這是一個無需訓練的三線程系統：線程 S（YOLOv11n）以全幀率提供檢測，且無額外延遲；線程 Q 應用條件自適應增強（CAPE），並通過熵引導的 NMS（EG-NMS）融合結果，而不會阻塞線程 S；線程 E 提供 CLIP 零樣本天氣分類，因此新的天氣類別只需一個新的文本提示，無需標註數據和重新訓練。在 1327 張 DAWN 圖像上進行評估（YOLOv11m，IoU = 0.5，自信度 = 0.25），CADENet 在雪上達到召回率 = 0.0103（微觀），F1 = 0.0230，在雨上 F1 = 0.0038。我們對 DAWN 類數據的標註完整性偏見進行了形式化，因此報告的 F1 值是對真實增益的下限；召回率是對標註差距免疫的主要指標。線程 S 在增強負載下仍能保持約 44 FPS。無需模型重新訓練或額外的傳感器硬件。

##### **Mega-ASR: Towards In-the-wild^2 Speech Recognition via Scaling up Real-world Acoustic Simulation**
2605.19833v1 by Zhifei Xie, Kaiyu Pang, Haobin Zhang, Deheng Ye, Xiaobin Hu, Shuicheng Yan, Chunyan Miao

Despite rapid advances in automatic speech recognition (ASR) and large audio-language models, robust recognition in real-world environments remains limited by an "acoustic robustness bottleneck": models often lose acoustic grounding and produce omissions or hallucinations under severe, compositional distortions. We propose Mega-ASR, a unified ASR-in-the-wild framework that combines scalable compound-data construction with progressive acoustic-to-semantic optimization. We introduce Voices-in-the-Wild-2M, covering 7 classic acoustic phenomena and 54 physically plausible compound scenarios, and train Mega-ASR with Acoustic-to-Semantic Progressive Supervised Fine-Tuning and Dual-Granularity WER-Gated Policy Optimization. Extensive experiments demonstrate that Mega-ASR achieves significant advantages over prior state-of-the-art systems on adverse-condition ASR benchmarks (45.69% vs. 54.01% on VOiCES R4-B-F, and 21.49% vs. 29.34% on NOIZEUS Sta-0). On complex compositional acoustic scenarios, Mega-ASR further delivers over 30% relative WER reduction against strong open- and closed-source baselines, establishing a scalable paradigm for robust ASR in-the-wild.

摘要：儘管自動語音識別（ASR）和大型音頻語言模型迅速進步，但在現實環境中的穩健識別仍然受到「聲學穩健性瓶頸」的限制：模型在嚴重的組合失真下經常失去聲學基礎，並產生遺漏或幻覺。我們提出了Mega-ASR，一個統一的ASR-in-the-wild框架，結合了可擴展的複合數據構建與漸進的聲學到語義優化。我們介紹了Voices-in-the-Wild-2M，涵蓋7種經典聲學現象和54種物理上合理的複合場景，並使用聲學到語義漸進監督微調和雙粒度WER門控策略優化來訓練Mega-ASR。大量實驗表明，Mega-ASR在不利條件的ASR基準上相較於先前的最先進系統取得了顯著優勢（在VOiCES R4-B-F上為45.69%對54.01%，在NOIZEUS Sta-0上為21.49%對29.34%）。在複雜的組合聲學場景中，Mega-ASR進一步實現了相對WER減少超過30%的效果，相較於強大的開源和閉源基準，建立了一個可擴展的穩健ASR在野外的範式。

##### **Explainable Wastewater Digital Twins: Adaptive Context-Conditioned Structured Simulators with Self-Falsifying Decision Support**
2605.19826v1 by Gary Simethy, Daniel Ortiz Arroyo, Petar Durdevic

Operators of safety-critical industrial processes increasingly rely on digital twins to screen control interventions, but such simulators rarely carry certified safety guarantees. Wastewater treatment plants exemplify the gap: operators face a daily safety-efficiency trade-off where aerating too little risks effluent violations and nitrous-oxide (N2O) spikes, and aerating too much wastes energy. We develop an explainable digital twin for aeration and dosing setpoints. CCSS-IX, the simulator, is a bank of interpretable locally linear state-space "experts" adaptively mixed by a context-aware gating network, building on a continuous-time regime-switching scaffold. A runtime decision layer applies conformal risk control to abstain, reopen, or return a falsifying temporal witness for any operator-proposed action that cannot be statistically certified. The artificial-intelligence contribution is twofold: an identifiable, context-conditioned structured surrogate that retains operator-readable dynamics, and a self-falsifying decision rule with finite-sample coverage guarantees. The engineering contribution is a validated, end-to-end decision-support pipeline, tested on a 1000-step slice of the Avedøre full-scale plant (42.6% sensor missingness, 2-minute sampling), the Agtrup/BlueKolding full-scale plant in Denmark, and the Benchmark Simulation Model No. 2 (BSM2) international benchmark, under a matched ten-seed protocol. The static structured ensemble lies within 0.78% root-mean-square error of an unconstrained black-box reference, and the adaptive variant within 1.08%. The calibrated reopen rule cuts aggregate two-plant regret by 43.6% at an unsafe-action cost weight of 4 and eliminates unsafe chosen actions on the BSM2 main slice. Event-aligned temporal witnesses prevent 93 of 187 false-safe N2O approvals, about 4.65x the dyadic baseline (paired McNemar p < 1e-21).

摘要：操作安全關鍵工業過程的操作者越來越依賴數位雙胞胎來篩選控制干預，但這些模擬器很少具備經過認證的安全保證。廢水處理廠就是這一差距的典範：操作者面臨每日的安全與效率取捨，通氣不足會導致排放違規和一氧化二氮 (N2O) 峰值，而通氣過多則浪費能源。我們開發了一個可解釋的數位雙胞胎，用於通氣和投藥設置點。CCSS-IX，這個模擬器，是一組可解釋的局部線性狀態空間“專家”，由一個上下文感知的閘網絡自適應混合，建立在一個連續時間的狀態轉換框架上。運行時決策層應用符合風險控制，以對任何無法統計認證的操作者提議行動進行放棄、重新開放或返回一個虛假時間見證。人工智慧的貢獻是雙重的：一個可識別的、上下文條件的結構化替代品，保留了操作者可讀的動態，以及一個具有有限樣本覆蓋保證的自我虛假決策規則。工程貢獻是一個經過驗證的端到端決策支持管道，在 Avedøre 全規模工廠（42.6% 傳感器缺失，2 分鐘取樣）、丹麥的 Agtrup/BlueKolding 全規模工廠和基準模擬模型第 2 號 (BSM2) 國際基準下進行測試，遵循匹配的十種種子協議。靜態結構化集成在不受限的黑箱參考的根均方誤差內為 0.78%，而自適應變體則為 1.08%。經過校準的重新開放規則在不安全行動成本權重為 4 的情況下，將兩個工廠的總後悔減少了 43.6%，並消除了 BSM2 主要切片上的不安全選擇行動。事件對齊的時間見證防止了 187 次虛假安全 N2O 批準中的 93 次，約為二元基線的 4.65 倍（配對 McNemar p < 1e-21）。

##### **From Prompts to Pavement Through Time: Temporal Grounding in Agentic Scene-to-Plan Reasoning**
2605.19824v1 by Ahmed Y. Gado, Omar Y. Goba, Alaa Hassanein, Catherine M. Elias, Ahmed Hussein

Recent attempts to support high-level scene interpretation and planning in Autonomous Vehicles (AVs) using ensembles of Large Language Models (LLMs) and Large Multimodal Models (LMMs) continue to treat time as a secondary property. This lack of temporal grounding leads to inconsistencies in reasoning about continuous actions, undermining both safety and interpretability. This work explores whether temporal conditioning within inter-agent communication can preserve or enhance coherence without introducing degradation in semantic or logical consistency. To investigate this, we introduce three planner architectures with progressively increasing temporal integration and evaluate them on curated subsets of the BDD-X dataset using semantic, syntactic, and logical metrics. Results show that while temporal conditioning reshapes reasoning style, it yields no statistically significant improvements in standard NLP-based correctness metrics. However, qualitative analysis reveals predictive hazard reasoning, stable corrective behavior, and strategic divergence in the Sentinel. These findings clarify the limits of prompt-based temporal grounding and establish the first empirical benchmark for temporal scene-to-plan reasoning.

摘要：最近的嘗試旨在支持自動駕駛車輛（AVs）中的高級場景解釋和規劃，使用大型語言模型（LLMs）和大型多模態模型（LMMs）的集成，但仍將時間視為次要屬性。這種缺乏時間基礎的情況導致在連續行動的推理中出現不一致，削弱了安全性和可解釋性。本研究探討了在代理間通信中進行時間條件設定是否能在不引入語義或邏輯一致性降級的情況下保持或增強一致性。為了調查這一點，我們引入了三種規劃器架構，逐步增加時間整合，並使用語義、句法和邏輯指標在經過策劃的 BDD-X 數據集子集上進行評估。結果顯示，儘管時間條件設定重塑了推理風格，但在標準基於 NLP 的正確性指標上並未產生統計上顯著的改善。然而，定性分析揭示了預測危險推理、穩定的修正行為和 Sentinel 中的戰略性分歧。這些發現澄清了基於提示的時間基礎的限制，並建立了時間場景到計劃推理的第一個實證基準。

##### **Smooth Piecewise Cutting for Neural Operator to Handle Discontinuities and Sharp Transitions**
2605.19823v1 by Ha Dang, Sebastian Schmidt, Juergen Hesser

Neural operators have achieved strong performance in learning solution operators of partial differential equations (PDEs), but their inherently continuous representations struggle to capture discontinuities and sharp transitions. Existing approaches typically approximate such features within continuous function spaces, often requiring increased model capacity and high-resolution data. In this work, we propose Cut-DeepONet, a two-stage training framework that explicitly models discontinuities while reducing learning complexity. Our approach reformulates the problem via a lifting strategy, partitioning the domain into smooth subregions while representing discontinuities as boundaries in a higher-dimensional space. This separation aligns the operator learning task with the inductive bias of neural networks and avoids directly approximating discontinuities. An additional network predicts input-dependent discontinuity locations for unseen inputs, which are then used to guide the neural operator in generating smooth components within each region. Experiments on benchmark PDEs show that Cut-DeepONet outperforms state-of-the-art methods, even when trained on low-resolution datasets. The method excels on problems with discontinuities and sharp transitions, while using fewer trainable parameters. Our results highlight the benefits of changing the representation of operator learning rather than increasing model complexity.

摘要：神經運算子在學習偏微分方程（PDEs）的解運算子方面已取得強勁的表現，但其固有的連續表示難以捕捉不連續性和急劇變化。現有的方法通常在連續函數空間內近似這些特徵，這通常需要增加模型容量和高解析度數據。在本研究中，我們提出了Cut-DeepONet，一個兩階段的訓練框架，明確建模不連續性，同時降低學習複雜性。我們的方法通過提升策略重新構造問題，將領域劃分為平滑的子區域，同時將不連續性表示為高維空間中的邊界。這種分離將運算子學習任務與神經網絡的歸納偏見對齊，並避免直接近似不連續性。一個額外的網絡預測對於未見輸入的依賴於輸入的不連續性位置，然後用於指導神經運算子在每個區域內生成平滑組件。在基準PDE上的實驗顯示，Cut-DeepONet的表現超越了最先進的方法，即使在低解析度數據集上訓練時也一樣。該方法在具有不連續性和急劇變化的問題上表現優異，同時使用較少的可訓練參數。我們的結果突顯了改變運算子學習表示的好處，而不是增加模型複雜性。

##### **LP-Eval: Rubric and Dataset for Measuring the Quality of Legal Proposition Generation**
2605.19815v1 by Shanshan Xu, Johan Lindholm, Amogh Raina, Henrik Palmer Olsen, Daniel Hershcovich

Legal proposition generation is central to legal reasoning and doctrinal scholarship, yet remain under-examined in Legal NLP. This paper investigates the automatic generation and evaluation of legal propositions from decisions of the Court of Justice of the European Union using large language models (LLMs). We introduce LP-Eval, a three-step evaluation rubric co-designed with legal experts that decomposes legal proposition quality into formal validity and substantive dimensions. Using this rubric, we release a dataset of two experts' annotations for 100 LLM-generated legal propositions. Our results show that LLMs can generate predominantly well-formed and high-quality propositions, while expert evaluations reveal higher quality for propositions derived from well established cases than from recent ones. We further examine LLMs as evaluators and find that rubric-guided LLM judgments align more closely with expert assessments than direct overall scoring, but remain insensitive to finer-grained distinctions captured by human experts.

摘要：法律命題生成是法律推理和學術研究的核心，但在法律自然語言處理中仍然未受到充分檢視。本文研究了如何使用大型語言模型（LLMs）自動生成和評估歐洲聯盟法院的裁決中的法律命題。我們介紹了LP-Eval，一個與法律專家共同設計的三步評估標準，將法律命題的質量分解為形式有效性和實質維度。使用這個標準，我們釋放了一個包含兩位專家對100個LLM生成的法律命題註釋的數據集。我們的結果顯示，LLMs可以生成主要是良好構造和高質量的命題，而專家評估則顯示，源自成熟案例的命題質量高於近期案例。我們進一步檢視LLMs作為評估者，發現按照標準指導的LLM判斷與專家評估的吻合度高於直接的整體評分，但對人類專家捕捉的細微區別仍然不敏感。

##### **FLUXtrapolation: A benchmark on extrapolating ecosystem fluxes**
2605.19812v1 by Anya Fries, Jacob A Nelson, Martin Jung, Markus Reichstein, Jonas Peters

We introduce FLUXtrapolation, a benchmark for extrapolating ecosystem fluxes under progressively harder distribution shifts. Ecosystem fluxes are central to understanding the carbon, water, and energy cycles, yet they can only be measured directly at sparsely located measurement towers. Producing global flux estimates therefore requires training models on observed sites using globally available covariates and predicting in unobserved regions, that is, upscaling. Flux upscaling is a challenging domain generalization problem that is affected by a shift in covariate distribution across climates, ecosystem types, and environmental conditions, as well as by conditional shift: important drivers remain unobserved at global scale. We provide a quantitative analysis of both these shifts in $P_X$ and $P_{Y\mid X}$. FLUXtrapolation is designed based on domain expertise on flux upscaling: it defines temporal, spatial, and temperature-based extrapolation scenarios and evaluates performance across held-out domains, temporal aggregations, and tail errors. In a pilot study, we find that baselines perform similarly under median hourly RMSE, but separate under the proposed tail-focused and multi-scale evaluation. FLUXtrapolation therefore poses a realistic and thus relevant challenge for machine learning methods under distribution shift; at the same time, progress on this benchmark would directly support the scientific goal of improving flux upscaling.

摘要：我們介紹FLUXtrapolation，一個用於在逐漸更困難的分佈變化下外推生態系統通量的基準。生態系統通量對於理解碳、水和能量循環至關重要，但它們只能在稀疏分佈的測量塔上直接測量。因此，產生全球通量估計需要在觀測站點上訓練模型，使用全球可用的協變量，並在未觀測的區域進行預測，即上行擴展。通量上行擴展是一個具有挑戰性的領域泛化問題，受到氣候、生態系統類型和環境條件下協變量分佈變化的影響，以及條件變化：重要的驅動因素在全球範圍內仍然未被觀測。我們對$P_X$和$P_{Y\mid X}$這兩種變化提供了定量分析。FLUXtrapolation是基於通量上行擴展的領域專業知識設計的：它定義了基於時間、空間和溫度的外推情境，並評估在保留的領域、時間聚合和尾部誤差中的表現。在一項初步研究中，我們發現基準模型在中位數每小時RMSE下表現相似，但在提出的尾部聚焦和多尺度評估下則表現分開。因此，FLUXtrapolation對於在分佈變化下的機器學習方法提出了一個現實且相關的挑戰；同時，對這一基準的進展將直接支持改善通量上行擴展的科學目標。

##### **Chunking German Legal Code**
2605.19806v1 by Max Prior, Natalia Milanova, Andreas Schultz

This paper investigates chunking strategies for retrieval-augmented generation on German statutory law, using the German Civil Code as a structured benchmark corpus. We implement and compare a range of segmentation approaches, including structural units (sections, subsections, sentences, propositions), fixed-size windows, contextual chunking, semantic clustering, Lumber-style chunking, and RAPTOR-based hierarchical retrieval. All methods are evaluated on a legal question-answering dataset with section-level gold labels, measuring recall, query latency, index build time, and storage requirements. Results show that chunking strategies aligned with the inherent legal structure - particularly section and subsection - based retrieval-achieve the highest recall, while more complex approaches that override this structure perform worse. These simpler methods also offer favorable computational efficiency compared to LLM-intensive techniques such as contextual chunking, RAPTOR, and Lumber. The findings highlight a key trade-off between semantic enrichment and operational cost, and demonstrate that preserving domain-specific structure is critical for effective legal information retrieval.

摘要：這篇論文研究了針對德國法定法律的檢索增強生成的分塊策略，使用德國民法典作為結構化基準語料庫。<lb/>我們實施並比較了一系列的分段方法，包括結構單元（章節、小節、句子、命題）、固定大小的窗口、上下文分塊、語義聚類、Lumber風格的分塊以及基於RAPTOR的層次檢索。<lb/>所有方法都在具有章節級金標籤的法律問答數據集上進行評估，測量回憶率、查詢延遲、索引構建時間和存儲需求。<lb/>結果顯示，與固有法律結構（特別是章節和小節）對齊的分塊策略在檢索中達到最高的回憶率，而覆蓋這一結構的更複雜方法表現較差。<lb/>這些更簡單的方法在計算效率上也相較於LLM密集型技術（如上下文分塊、RAPTOR和Lumber）提供了更有利的效率。<lb/>研究結果突顯了語義豐富性與運營成本之間的關鍵權衡，並展示了保留特定領域結構對於有效法律信息檢索的重要性。

##### **Latent Laplace Diffusion for Irregular Multivariate Time Series**
2605.19805v1 by Zinuo You, Jin Zheng, John Cartlidge

Irregular multivariate time series impose a trade-off for long-horizon forecasting: discrete methods can distort temporal structure via re-gridding, while continuous-time models often require sequential solvers prone to drift. To bridge this gap, we present Latent Laplace Diffusion (LLapDiff), a generative framework that models the target as a low-dimensional latent trajectory, enabling horizon-wide generation without step-by-step integration over physical time. We guide the reverse process utilizing a stable modal parameterization motivated by stochastic port-Hamiltonian dynamics, and parameterize its mean evolution in the Laplace domain via learnable complex-conjugate poles, enabling direct evaluation over irregular timestamps. We also link continuous dynamics to irregular observations through renewal-averaging analysis, which maps sampling gaps to effective event-domain poles and motivates a gap-aware history summarizer. Extensive experiments show that LLapDiff improves over baselines in long-horizon forecasting, and its continuous-time generative nature supports missing-value imputation by querying the same model at historical timestamps. Code is available at https://github.com/pixelhero98/LLapDiffusion.

摘要：不規則的多變量時間序列在長期預測中帶來了權衡：離散方法可能通過重新網格化扭曲時間結構，而連續時間模型通常需要易於漂移的序列求解器。為了彌補這一差距，我們提出了潛在拉普拉斯擴散（LLapDiff），這是一個生成框架，將目標建模為低維潛在軌跡，實現了無需逐步在物理時間上進行積分的全範圍生成。我們利用穩定的模態參數化來指導反向過程，這一參數化受到隨機端哈密頓動力學的啟發，並通過可學習的複共軛極點在拉普拉斯域中參數化其均值演變，從而實現對不規則時間戳的直接評估。我們還通過更新平均分析將連續動力學與不規則觀測聯繫起來，這將取樣間隙映射到有效事件域極點，並促進了基於間隙的歷史摘要器。大量實驗表明，LLapDiff在長期預測中優於基準，其連續時間生成特性通過在歷史時間戳查詢相同模型來支持缺失值插補。代碼可在 https://github.com/pixelhero98/LLapDiffusion 獲得。

##### **Stitched Value Model for Diffusion Alignment**
2605.19804v1 by Hyojun Go, Hyungjin Chung, Prune Truong, Goutam Bhat, Li Mi, Zhaochong An, Zixiang Zhao, Dominik Narnhofer, Serge Belongie, Federico Tombari, Konrad Schindler

For practical use, diffusion- or flow-based generative models must be aligned with task-specific rewards, such as prompt fidelity or aesthetic preference. That alignment is challenging because the reward is defined for clean output images, but the alignment procedure requires value function estimates at noisy intermediate latents. Existing methods resort to Tweedie-style or Monte Carlo approximations, trading off estimator bias against computational cost: Tweedie estimates are efficient but biased, while Monte Carlo estimates are more accurate but require expensive rollouts. A natural alternative would be a learned value function, but it remains an open question how to effectively train a strong and general value model specifically for noisy latents. Here, we propose StitchVM, a model stitching framework that efficiently transfers reward models pretrained for clean images to the noisy latent regime. StitchVM starts from an existing, truncated pixel-space reward model and attaches a frozen diffusion backbone to it as its head. From the pixel-space model, the resulting hybrid retains a carefully pretrained, robust reward capability; from the diffusion backbone, it inherits its native ability to handle noisy latents. The stitching procedure is exceptionally lightweight, e.g., stitching and finetuning CLIP ViT-L and SD 3.5 Medium takes only 10 GPU-hours. By lifting powerful pixel-space reward models to latent space, StitchVM opens up a new style of diffusion alignment: instead of rough, yet costly per-sample approximation of the value function, the correct function for the actual, noisy latents is constructed once and then amortized over many samples and iterations. We show that this approach yields improvements across a broad range of downstream steering and post-training methods: DPS becomes $3.2\times$ faster while halving peak GPU memory, and DiffusionNFT becomes $2.3\times$ faster.

摘要：對於實際應用，基於擴散或流的生成模型必須與特定任務的獎勵對齊，例如提示的真實性或美學偏好。這種對齊是具有挑戰性的，因為獎勵是針對乾淨的輸出圖像定義的，但對齊過程需要在嘈雜的中間潛變量上進行價值函數估計。現有的方法依賴於Tweedie風格或蒙地卡羅近似，權衡估計器偏差與計算成本：Tweedie估計是高效的但有偏差，而蒙地卡羅估計則更準確但需要昂貴的展開。自然的替代方案是學習的價值函數，但如何有效訓練一個強大且通用的價值模型專門針對嘈雜潛變量仍然是一個未解決的問題。在這裡，我們提出了StitchVM，一個模型拼接框架，能有效地將為乾淨圖像預訓練的獎勵模型轉移到嘈雜的潛變量範疇。StitchVM從現有的截斷像素空間獎勵模型開始，並將一個凍結的擴散骨幹作為其頭部附加上去。從像素空間模型中，結果的混合體保留了一個經過精心預訓練的穩健獎勵能力；從擴散骨幹中，它繼承了處理嘈雜潛變量的固有能力。拼接過程極其輕量，例如，拼接和微調CLIP ViT-L和SD 3.5 Medium僅需10個GPU小時。通過將強大的像素空間獎勵模型提升到潛變量空間，StitchVM開啟了一種新的擴散對齊風格：不再是粗略但昂貴的每樣本價值函數近似，而是為實際的嘈雜潛變量構建正確的函數，然後在多個樣本和迭代中攤銷。我們展示了這種方法在廣泛的下游引導和後訓練方法中帶來了改進：DPS變得快了$3.2\times$，同時將峰值GPU內存減半，而DiffusionNFT變得快了$2.3\times$。

##### **Synergistic Foundation Models for Semi-Supervised Fetal Cardiac Ultrasound Analysis: SAM-Med2D Boundary Refinement and DINOv3 Semantic Enhancement**
2605.19799v1 by Tonghao Zhuang, Shanglong Hu, Yongsheng Luo, Zhiqi Zhang, Yu Li

We present a semi-supervised framework for joint segmentation and classification of fetal cardiac ultrasound images. Built upon the EchoCare multi-task backbone, our method integrates SAM-Med2D for boundary refinement and leverages DINOv3 to enhance pseudo-label quality. We introduce view-specific hard masking along with a two-stage optimization strategy: an EMA phase to consolidate segmentation capabilities, followed by a Classification Fine-Tuning phase that freezes segmentation parameters and resets the classification head to recover classification performance without compromising segmentation gains.
  Evaluated on the FETUS 2026 leaderboard, our method achieves a Dice Similarity Coefficient at 79.99%, Normalized Surface Distance at 61.62%, and F1-score at 41.20%, validating the effectiveness of our approach for prenatal congenital heart disease screening. Source code is publicly available at: https://github.com/2826056177/zcst_fetus2026.

摘要：我們提出了一個半監督框架，用於胎兒心臟超聲圖像的聯合分割和分類。基於 EchoCare 多任務骨幹，我們的方法集成了 SAM-Med2D 進行邊界細化，並利用 DINOv3 來提高偽標籤質量。我們引入了特定視圖的硬遮罩，以及一個兩階段的優化策略：首先是 EMA 階段以鞏固分割能力，然後是分類微調階段，該階段凍結分割參數並重置分類頭，以在不妨礙分割增益的情況下恢復分類性能。<lb/>在 FETUS 2026 排行榜上進行評估，我們的方法實現了 79.99% 的 Dice 相似係數，61.62% 的歸一化表面距離，以及 41.20% 的 F1 分數，驗證了我們的方法在產前先天性心臟病篩查中的有效性。<lb/>源代碼可在以下網址公開獲取：https://github.com/2826056177/zcst_fetus2026。

##### **Towards Trust Calibration in Socially Interactive Agents: Investigating Gendered Multimodal Behaviors Generation with LLMs**
2605.19798v1 by Lucie Galland, Chloé Clavel, Magalie Ochs

As Socially Interactive Agents (SIAs) become increasingly integrated into daily life, the ability to calibrate user trust to an agent's actual capabilities would help ensure appropriate usage of these agents. In this paper, we explore the capacity of Large Language Models (LLMs) to generate multimodal behaviors (verbal, vocal, gestural, and facial expression modalities) that reflect varying levels of ability and benevolence, two key dimensions of trustworthiness. We propose a novel method for automatically generating behaviors aligned with specific levels of these traits, a first step towards enabling nuanced and trust-calibrated interactions. By analyzing a large dataset of multimodal transcripts generated by LLMs, we demonstrate that GPT-5.4 is able to produce coherent behavior across different modalities (text, intonation, facial expression, and gesture). Using Random Forest feature importance analysis, we show that the generated behaviors align with theoretical expectations for ability and benevolence. However, we also find that when gender is specified in the prompt, LLMs tend to reproduce societal gender stereotypes, associating male agents' behaviors with high ability and female agents' behaviors with high benevolence. To validate our approach, we conducted a user study on Prolific using a within-subjects design. Participants perceived different levels of ability and benevolence in the generated behaviors align with the intended instructions.

摘要：隨著社交互動代理（SIAs）越來越多地融入日常生活，調整用戶對代理實際能力的信任將有助於確保這些代理的適當使用。在本文中，我們探討大型語言模型（LLMs）生成多模態行為（語言、聲音、手勢和面部表情模態）的能力，這些行為反映了能力和善意的不同水平，這是信任的兩個關鍵維度。我們提出了一種新方法，自動生成與這些特質的特定水平對齊的行為，這是實現細緻且信任校準互動的第一步。通過分析由LLMs生成的大型多模態轉錄數據集，我們證明GPT-5.4能夠在不同模態（文本、語調、面部表情和手勢）中產生連貫的行為。使用隨機森林特徵重要性分析，我們顯示生成的行為與能力和善意的理論預期相符。然而，我們也發現當提示中指定性別時，LLMs往往會重現社會性別刻板印象，將男性代理的行為與高能力相關聯，將女性代理的行為與高善意相關聯。為了驗證我們的方法，我們在Prolific上進行了一項使用被試內設計的用戶研究。參與者感知到生成的行為中的不同能力和善意水平與預期指示一致。

##### **Prior Knowledge or Search? A Study of LLM Agents in Hardware-Aware Code Optimization**
2605.19782v1 by Dmitry Redko, Albert Fazlyev, Konstantin Sozykin, Maria Ivanova, Evgeny Burnaev, Egor Shvetsov

LLM discovery and optimization systems are increasingly applied across domains, implementing a common propose-evaluate-revise loop. Such optimization or discovery progresses via context conditioning on received feedback from an environment. However, as modern LLM agents are increasingly complex in their structure, it is difficult to evaluate which components contribute the most, and when and how this exploration may fail. We answer these questions through three controlled experiments. Our findings: (1) In pure black-box optimization, LLMs act as greedy optimizers. (2) In zero-shot kernel generation, providing explicit input-size information has no measurable effect, models converge to the same kernel parameters regardless of size or temperature, as though the size instruction were invisible. Moreover, when tasked to perform kernel optimization for uncommon kernel sizes, performance sharply degrades regardless of the language used. (3) In feedback-loop kernel optimization, CUDA improves monotonically under iterative feedback, while TVM IR actively degrades, which demonstrates that kernel optimization degrades when models operate with low-density language. Our results conclude that LLMs in code optimization tasks highly depend on pretrained priors rather than provided feedback or agentic structure.

摘要：LLM 發現和優化系統越來越多地應用於各個領域，實施一個共同的提出-評估-修訂循環。這種優化或發現通過對環境中接收到的反饋進行上下文調節來進行。然而，隨著現代 LLM 代理在其結構上變得越來越複雜，評估哪些組件貢獻最大，以及這種探索何時以及如何可能失敗變得困難。我們通過三個受控實驗來回答這些問題。我們的發現：（1）在純黑箱優化中，LLM 作為貪婪優化器運作。（2）在零樣本內核生成中，提供明確的輸入大小信息沒有可測量的影響，模型無論大小或溫度如何都會收斂到相同的內核參數，彷彿大小指令是不可見的。此外，當被要求對不常見的內核大小進行內核優化時，無論使用何種語言，性能都會急劇下降。（3）在反饋循環內核優化中，CUDA 在迭代反饋下單調改善，而 TVM IR 積極退化，這表明當模型在低密度語言下運作時，內核優化會退化。我們的結果總結出 LLM 在代碼優化任務中高度依賴預訓練的先驗知識，而不是提供的反饋或代理結構。

##### **From SGD to Muon: Adaptive Optimization via Schatten-p Norms**
2605.19781v1 by Thomas Massena, Corentin Friedrich, Mathieu Serrurier

Modern optimizers, like Muon, impose matrix-wise geometry constraints on their updates. These matrix-wise constraints can be unified under Linear Minimization Oracle (LMO) theory. However, all current methods impose fixed LMO geometries for the update rules, chosen by-design or empirically, which are not necessarily optimal according to the problem's geometry. We introduce a novel efficient datadriven criterion for dynamically choosing proxy-optimal update LMO geometries on individual Deep Neural Network layers. Derived in closed form from gradient and activation statistics using a single-step random feature regression surrogate model, our criterion navigates a design space interpolating from SGD to Muon updates. Moreover, integrating parameter-wise preconditioning allows our framework to recover SGD, Muon, Adam, and MuAdam as specific extrema. To make this adaptive approach scalable, we pair it with efficient computational strategies, achieving only a $\sim$ 3% runtime overhead on highly optimized baselines. As a proof of concept, we show that this data-driven optimizer beats or remains competitive with the performance of the best performing optimizer between Muon and AdamW across three different training scenarios. Ultimately, this work provides evidence that LMO geometry can be successfully and efficiently adapted from runtime data, opening a new pathway for optimizer design beyond static geometries.

摘要：現代優化器，如Muon，對其更新施加矩陣級幾何約束。這些矩陣級約束可以統一在線性最小化預言機（LMO）理論下。然而，所有當前方法對更新規則施加固定的LMO幾何，這些幾何是根據設計或經驗選擇的，並不一定根據問題的幾何是最佳的。我們引入了一種新穎的高效數據驅動標準，用於動態選擇個別深度神經網絡層的代理最優更新LMO幾何。該標準是通過使用單步隨機特徵回歸替代模型從梯度和激活統計中以封閉形式推導而來，我們的標準在設計空間中插值，從SGD到Muon更新。此外，集成參數級預處理使我們的框架能夠恢復SGD、Muon、Adam和MuAdam作為特定的極值。為了使這種自適應方法可擴展，我們將其與高效計算策略相結合，在高度優化的基線上僅實現約3%的運行時開銷。作為概念驗證，我們展示了這種數據驅動的優化器在三種不同的訓練場景中超越或保持與Muon和AdamW之間最佳表現優化器的競爭力。最終，這項工作提供了證據，表明LMO幾何可以成功且高效地從運行時數據中適應，為超越靜態幾何的優化器設計開辟了一條新途徑。

##### **OpenComputer: Verifiable Software Worlds for Computer-Use Agents**
2605.19769v1 by Jinbiao Wei, Qianran Ma, Yilun Zhao, Xiao Zhou, Kangqi Ni, Guo Gan, Arman Cohan

We present OpenComputer, a verifier-grounded framework for constructing verifiable software worlds for computer-use agents. OpenComputer integrates four components: (1) app-specific state verifiers that expose structured inspection endpoints over real applications, (2) a self-evolving verification layer that improves verifier reliability using execution-grounded feedback, (3) a task-generation pipeline that synthesizes realistic and machine-checkable desktop tasks, and (4) an evaluation harness that records full trajectories and computes auditable partial-credit rewards. In its current form, OpenComputer covers 33 desktop applications and 1,000 finalized tasks spanning browsers, office tools, creative software, development environments, file managers, and communication applications. Experiments show that OpenComputer's hard-coded verifiers align more closely with human adjudication than LLM-as-judge evaluation, especially when success depends on fine-grained application state. Frontier agents struggle with end-to-end completion despite partial progress, and open-source models exhibit sharp drops from their OSWorld-Verified scores, exposing a persistent gap in robust computer automation.

摘要：我們提出了 OpenComputer，一個基於驗證者的框架，用於為計算機使用代理構建可驗證的軟體世界。OpenComputer 整合了四個組件： (1) 專用應用狀態驗證器，提供對真實應用的結構化檢查端點， (2) 自我演化的驗證層，利用執行基礎的反饋來提高驗證者的可靠性， (3) 任務生成管道，合成現實且可機器檢查的桌面任務， (4) 評估工具，記錄完整的軌跡並計算可審核的部分信用獎勵。 在目前的形式下，OpenComputer 涵蓋了 33 個桌面應用和 1,000 個最終任務，涵蓋了瀏覽器、辦公工具、創意軟體、開發環境、檔案管理器和通訊應用。 實驗顯示，OpenComputer 的硬編碼驗證器與人類裁決的對齊程度比 LLM 作為裁判的評估更高，尤其是在成功取決於細緻的應用狀態時。 前沿代理在端到端完成方面面臨困難，儘管有部分進展，而開源模型則顯示出從其 OSWorld 驗證分數中急劇下降，暴露出計算機自動化中的持續差距。

##### **Minimax Optimal Variance-Aware Regret Bounds for Multinomial Logistic MDPs**
2605.19768v1 by Pierre Boudart, Pierre Gaillard, Alessandro Rudi

We study reinforcement learning for episodic Markov Decision Processes (MDPs) whose transitions are modelled by a multinomial logistic (MNL) model. Existing algorithms for MNL mixture MDPs yield a regret of $\smash{\tilde{O}(dH^2\sqrt{T})}$ (Li et al., 2024), where $d$ is the feature dimension, $H$ the episode length, and $T$ the number of episodes. Inspired by the logistic bandit literature (Abeille et al., 2021; Faury et al., 2022; Boudart et al., 2026), we introduce a problem-dependent constant $\barσ\_T \leq 1/2$, measuring the normalised average variance of the optimal downstream value function along the learner's trajectory. We propose an algorithm achieving a regret of $\smash{\tilde{O}(dH^2\barσ\_T\sqrt{T})}$, which recovers the existing bound in the worst case and improves upon it for structured MDPs. For instance, for KL-constrained robust MDPs, $\barσ\_T = O(H^{-1})$, reducing the horizon dependence by a factor $H$. We further establish a matching $\smash{Ω(dH^2\barσ\_T\sqrt{T})}$ lower bound, proving minimax optimality (up to logarithmic factors) and fully characterising the regret complexity of MNL mixture MDPs for the first time.

摘要：我們研究了用多項式邏輯模型（MNL）建模的情節馬爾可夫決策過程（MDPs）的強化學習。現有的MNL混合MDPs算法產生的後悔為$\smash{\tilde{O}(dH^2\sqrt{T})}$（Li et al., 2024），其中$d$是特徵維度，$H$是情節長度，$T$是情節數量。受到邏輯賭徒文獻（Abeille et al., 2021；Faury et al., 2022；Boudart et al., 2026）的啟發，我們引入了一個問題依賴的常數$\barσ\_T \leq 1/2$，用來測量學習者軌跡上最佳下游價值函數的標準化平均方差。我們提出了一種算法，其後悔為$\smash{\tilde{O}(dH^2\barσ\_T\sqrt{T})}$，在最壞情況下恢復現有界限，並對結構化MDPs進行改進。例如，對於KL約束的穩健MDPs，$\barσ\_T = O(H^{-1})$，將視野依賴性降低了$H$的因子。我們進一步建立了一個匹配的$\smash{Ω(dH^2\barσ\_T\sqrt{T})}$下界，證明了最小最大最優性（至對數因子）並首次完全表徵MNL混合MDPs的後悔複雜性。

##### **AR1-ZO: Topology-Aware Rank-1 Zeroth-Order Queries for High-Rank LoRA Fine-Tuning**
2605.19767v1 by Ziye Chen, Hongbin Lin, Chenyu Zhang, Xiangda Yan, Yongjie Yang, Yao Shu

Zeroth-order (ZO) optimization enables large-language-model fine-tuning without storing backpropagation activations, while LoRA supplies compact trainable adapters. Combining them creates a rank paradox: increasing LoRA rank improves adapter capacity, but standard two-point ZO either perturbs a rank-dependent number of coordinates or, under atomwise updates, can make the finite-difference signal unobservable. This paper shows that the bottleneck is a measurement-topology problem rather than a need for an external subspace. LoRA already decomposes into matched rank-$1$ atoms, each a complete factor-coordinate block of dimension $d_\text{out}+d_\text{in}$. Querying one atom per step keeps the stored adapter rank $r$ while removing $r$ from the single-query perturbation dimension. The naive atomwise query is still miscalibrated: if it inherits canonical LoRA scaling $α/r$, the active finite-difference signal shrinks as $1/r$ and the active finite-difference signal-to-noise ratio (FD-SNR) as $1/r^2$, producing directional collapse under a fixed residual evaluation-noise floor. AR1-ZO pairs alternating rank-$1$ atom queries with topology-aware scaling $γ=αr$, restoring rank-invariant active signal without auxiliary bases, activation hooks, curvature estimates, or extra forward queries. Theory proves atom minimality, rank-independent active query dimension, directional collapse and restoration, and the remaining rank dependence as an amortized coverage cost. Experiments on OPT and Qwen3 models validate the signal mechanism and show that AR1-ZO makes high-rank LoRA effective among matched-budget ZO methods under the standard two-forward-pass query budget.

摘要：零階（ZO）優化使得大型語言模型的微調無需儲存反向傳播的激活，而 LoRA 提供了緊湊的可訓練適配器。<lb/>將它們結合起來會產生一個秩悖論：增加 LoRA 的秩會提高適配器的容量，但標準的兩點 ZO 要麼擾動一個依賴於秩的坐標數量，要麼在原子更新下，會使有限差分信號變得不可觀察。<lb/>本文顯示瓶頸是一個測量拓撲問題，而不是對外部子空間的需求。<lb/>LoRA 已經分解為匹配的秩-$1$ 原子，每個原子都是維度為 $d_\text{out}+d_\text{in}$ 的完整因子坐標塊。<lb/>每步查詢一個原子可以保持儲存的適配器秩 $r$，同時從單次查詢擾動維度中移除 $r$。<lb/>天真的原子查詢仍然校準不當：如果它繼承了經典的 LoRA 縮放 $α/r$，則活躍的有限差分信號會縮小為 $1/r$，而活躍的有限差分信號對噪聲比（FD-SNR）則縮小為 $1/r^2$，在固定的殘差評估噪聲底線下產生方向性崩潰。<lb/>AR1-ZO 將交替的秩-$1$ 原子查詢與考慮拓撲的縮放 $γ=αr$ 結合，恢復了不需要輔助基、激活鉤、曲率估計或額外前向查詢的秩不變的活躍信號。<lb/>理論證明了原子的最小性、與秩無關的活躍查詢維度、方向性崩潰和恢復，以及剩餘的秩依賴作為攤銷的覆蓋成本。<lb/>在 OPT 和 Qwen3 模型上的實驗驗證了信號機制，並顯示 AR1-ZO 在標準的兩次前向傳遞查詢預算下，使高秩 LoRA 在匹配預算的 ZO 方法中變得有效。

##### **Synthesis and Evaluation of Long-term History-aware Medical Dialogue**
2605.19766v1 by Hebin Hu, Renke Dai, Ah-Hwee Tan, Yilin Kang

An effective healthcare agent must be able to recall and reason over a patient's longitudinal medical history. However, the absence of datasets with realistic long-term dialogue timelines limits systematic evaluation. Real clinical text is constrained by privacy and ethics, while existing benchmarks focus on isolated interactions, failing to capture cross-session reasoning. We introduce a framework for synthesizing high-quality, long-term medical dialogues with LLMs. Our approach entails a knowledge-guided decomposition into three stages: constructing synthetic patient profiles with diverse disease and complication trajectories, generating multi-turn dialogues per encounter, and integrating them into a coherent longitudinal history dataset, MediLongChat. We establish three benchmark tasks-In-dialogue Reasoning, Cross-dialogue Reasoning, and Synthesis Reasoning-to evaluate the memory capabilities of healthcare agents. To assess data quality, we introduce a multi-dimensional evaluation framework combining vector-based metrics with LLM-as-a-judge assessments. Specifically, we define automatic measures-Faithfulness, Coherence, and Diversity-together with two LLM-based evaluations: Correctness and Realism. Benchmark experiments show that even state-of-the-art LLMs struggle with MediLongChat. These findings highlight the benchmark's applicability and underscore the need for tailored methods to advance healthcare agents.

摘要：有效的醫療代理必須能夠回憶並推理患者的長期醫療歷史。<lb/>然而，缺乏具有現實長期對話時間線的數據集限制了系統評估。<lb/>真實的臨床文本受到隱私和倫理的限制，而現有的基準則專注於孤立的互動，未能捕捉跨會話的推理。<lb/>我們引入了一個框架，用於合成高質量的長期醫療對話，使用大型語言模型（LLMs）。<lb/>我們的方法涉及知識引導的分解，分為三個階段：構建具有多樣疾病和併發症軌跡的合成患者檔案，為每次就診生成多輪對話，並將它們整合成一個連貫的長期歷史數據集，MediLongChat。<lb/>我們建立了三個基準任務——對話內推理、跨對話推理和合成推理——以評估醫療代理的記憶能力。<lb/>為了評估數據質量，我們引入了一個多維評估框架，結合基於向量的指標和LLM作為評判的評估。<lb/>具體而言，我們定義了自動測量——忠實性、一致性和多樣性——以及兩個基於LLM的評估：正確性和現實性。<lb/>基準實驗顯示，即使是最先進的LLMs在MediLongChat上也面臨挑戰。<lb/>這些發現突顯了基準的適用性，並強調了推進醫療代理所需的量身定制方法。

##### **What Really Improves Mathematical Reasoning: Structured Reasoning Signals Beyond Pure Code**
2605.19762v1 by Yuze Zhao, Junpeng Fang, Lu Yu, Zhenya Huang, Kai Zhang, Qing Cui, Qi Liu, Jun Zhou, Enhong Chen

Code has become a standard component of modern foundation language model (LM) training, yet its role beyond programming remains unclear. We revisit the claim that code improves reasoning through controlled pretraining experiments on a 10T-token corpus with fine-grained domain separation. Our findings are threefold. First, when code is restricted to standalone executable programs and Code-NL data are controlled for, code substantially improves programming ability but does not act as a general reasoning enhancer; instead, it competes with knowledge-intensive tasks, especially complex mathematical reasoning. Second, the reasoning gains often attributed to code are better explained by cross-domain structured reasoning traces, such as code-text and math-text mixtures, rather than by executable code alone. Third, increasing the density of structured math-domain samples within a fixed math budget yields substantial gains on difficult mathematical reasoning while largely preserving programming performance, suggesting that cognitive scaffolds offer a targeted way to mitigate cross-domain trade-offs. Finally, routing analyses show that data-composition effects are reflected in expert-activation patterns, providing mechanism-level evidence for competitive and synergistic interactions across domains. Our results clarify which data characteristics transfer across capability dimensions and point to more precise data-centric optimization strategies.

摘要：代碼已成為現代基礎語言模型（LM）訓練的標準組成部分，但其在編程之外的角色仍不明確。<lb/>我們重新檢視了代碼通過控制預訓練實驗在10T-token語料庫上改善推理的說法，並進行了細緻的領域分離。<lb/>我們的發現有三個方面。<lb/>首先，當代碼僅限於獨立可執行程序且控制了Code-NL數據時，代碼顯著提高了編程能力，但並不作為一般推理的增強器；相反，它與知識密集型任務競爭，特別是複雜的數學推理。<lb/>其次，通常歸因於代碼的推理增益更好地解釋為跨領域結構化推理痕跡，例如代碼-文本和數學-文本混合，而非僅由可執行代碼解釋。<lb/>第三，在固定的數學預算內增加結構化數學領域樣本的密度，能在難度較大的數學推理上帶來顯著增益，同時大幅保留編程表現，這表明認知支架提供了一種針對性的方法來減輕跨領域的權衡。<lb/>最後，路由分析顯示數據組合效應反映在專家激活模式中，為跨領域的競爭和協同互動提供了機制層面的證據。<lb/>我們的結果澄清了哪些數據特徵在能力維度之間轉移，並指向更精確的以數據為中心的優化策略。

##### **CogScale: Scalable Benchmark for Sequence Processing**
2605.19758v1 by Yannis Bendi-Ouis, Romain de Coudenhove, Xavier Hinaut

The ability to maintain and manipulate information over time is a fundamental aspect of living beings and Artificial Intelligence. While modern models have achieved remarkable success in tasks like natural language processing, evaluating the capacity of novel architectures to process sequential information remains computationally expensive and time-consuming. Testing a new architecture often requires scaling up to massive datasets and models, leading to vast computational costs and slow iteration cycles. In this paper, we propose CogScale, a benchmark of 14 scalable synthetic tasks designed to isolate and evaluate specific cognitive and memory abilities at different parametrizable scales. By providing a standardized, lightweight framework, CogScale allows researchers to rapidly validate architectural innovations before committing to large-scale training. To establish a solid baseline, we evaluate seven distinct architectures: Gated Recurrent Unit (GRU), Long Short-Term Memory (LSTM), xLSTM, Echo State Network (ESN), Mamba, Transformer Decoder, and Transformer Encoder-Decoder. These evaluations are conducted under strict parameter budgets (1k, 10k, and 100k) and across different difficulty levels and scales. Our results show that while classical RNNs and Echo State Networks excel at basic retention within strict parameter budgets, only attention mechanisms and modern state-space models consistently maintain high performance as reasoning complexity and task difficulty scale.

摘要：維持和操控資訊隨時間變化的能力是生物體和人工智慧的一個基本特徵。雖然現代模型在自然語言處理等任務上取得了顯著的成功，但評估新架構處理序列資訊的能力仍然在計算上成本高昂且耗時。測試新架構通常需要擴展到巨大的數據集和模型，這導致了龐大的計算成本和緩慢的迭代週期。在本文中，我們提出了CogScale，這是一個包含14個可擴展合成任務的基準，旨在隔離和評估不同參數化尺度下的特定認知和記憶能力。通過提供一個標準化、輕量級的框架，CogScale使研究人員能夠在承諾進行大規模訓練之前迅速驗證架構創新。為了建立穩固的基準，我們評估了七種不同的架構：門控循環單元（GRU）、長短期記憶（LSTM）、xLSTM、回聲狀態網絡（ESN）、Mamba、Transformer解碼器和Transformer編碼器-解碼器。這些評估是在嚴格的參數預算（1k、10k和100k）下進行的，並涵蓋不同的難度級別和尺度。我們的結果顯示，雖然經典的RNN和回聲狀態網絡在嚴格的參數預算內在基本保留方面表現優異，但只有注意機制和現代狀態空間模型在推理複雜性和任務難度增加時能持續保持高性能。

##### **Memory-Augmented Reinforcement Learning Agent for CAD Generation**
2605.19748v1 by Yin Xiaolong, Liu Yu, Shen Jiahang, Lu Xingyu, Ni Jingzhe, Fan Fengxiao, Sang Fan

Automatic generation of computer-aided design (CAD) models is a core technology for enabling intelligence in advanced manufacturing. Existing generation methods based on large language models (LLMs) often fall short when handling complex CAD models characterized by long operation sequences, diverse operation types, and strong geometric constraints, primarily because reasoning chains break and effective error-correction mechanisms are lacking. To address this problem, this paper proposes a memory-augmented reinforcement learning framework for CAD generation agents. The framework encapsulates the underlying geometric kernel into a structured toolchain callable by the agent and builds a closed-loop mechanism of design intent understanding, global planning, execution, and multi-dimensional verification. It also designs a dual-track memory module consisting of a case library and a skill library, and proposes a dynamic utility retrieval algorithm. By introducing reinforcement learning into retrieval and policy optimization, the agent can effectively avoid retrieval traps in which examples are semantically similar but geometrically infeasible, enabling online self-correction and continual evolution without additional large-scale annotated data. Experiments show that the proposed method significantly improves both the success rate and geometric consistency on complex CAD model generation tasks.

摘要：自動生成計算機輔助設計（CAD）模型是實現先進製造智能的核心技術。現有基於大型語言模型（LLMs）的生成方法在處理特徵為長操作序列、多樣操作類型和強幾何約束的複雜CAD模型時，往往效果不佳，主要是因為推理鏈斷裂且缺乏有效的錯誤修正機制。為了解決這個問題，本文提出了一種增強記憶的強化學習框架，用於CAD生成代理。該框架將底層幾何內核封裝成一個可由代理調用的結構化工具鏈，並建立一個設計意圖理解、全局規劃、執行和多維驗證的閉環機制。它還設計了一個由案例庫和技能庫組成的雙軌記憶模塊，並提出了一種動態效用檢索算法。通過將強化學習引入檢索和策略優化，代理能夠有效避免檢索陷阱，即例子在語義上相似但在幾何上不可行，從而實現在線自我修正和持續演化，而無需額外的大規模標註數據。實驗表明，所提出的方法顯著提高了複雜CAD模型生成任務的成功率和幾何一致性。

##### **EngiAI: A Multi-Agent Framework and Benchmark Suite for LLM-Driven Engineering Design**
2605.19743v1 by Gioele Molinari, Florian Felten, Soheyl Massoudi, Mark Fuge

Large Language Model (LLM) agents are increasingly applied to engineering design tasks, yet existing evaluation frameworks do not adequately address multi-agent systems that combine simulation, retrieval, and manufacturing preparation. We introduce a benchmark suite with three evaluation dimensions: (1) a workflow benchmark with seven prompt styles targeting distinct cognitive demands-including direct tool use, semantic disambiguation, conditional branching, and working-memory tasks; (2) a Retrieval-Augmented Generation (RAG) benchmark with gated scoring isolating retrieval contributions to parameter selection; and (3) an High Performance Computing (HPC) benchmark evaluating end-to-end ML training orchestration on a SLURM cluster. Alongside the benchmark we present EngiAI, a Multi-Agent System (MAS) reference implementation built on LangGraph that operationalizes the benchmark by coordinating seven specialized agents through a supervisor architecture, unifying topology optimization, document retrieval, HPC job orchestration, and 3D printer control. Across four LLM backends and two EngiBench problems, proprietary models achieve 96-97% average task completion on Beams2D, while open-source 4B-parameter models reach 55-78%, with clear generational improvement. Conditional branching proves most challenging, with task completion dropping to 20-53% for the conditional style on Photonics2D. RAG gating confirms near-perfect retrieval-augmented scores ($\approx 1.0$) versus near-zero without retrieval, validating the evaluation design. On HPC orchestration, one model completes all pipeline steps in 100% of runs while another drops to 50%, revealing that multi-step instruction following degrades over long-running workflows.

摘要：大型語言模型（LLM）代理越來越多地應用於工程設計任務，然而現有的評估框架並未充分考慮結合模擬、檢索和製造準備的多代理系統。我們引入了一個基準套件，包含三個評估維度：（1）一個工作流程基準，具有七種針對不同認知需求的提示風格，包括直接工具使用、語義消歧、條件分支和工作記憶任務；（2）一個檢索增強生成（RAG）基準，通過門控評分隔離檢索對參數選擇的貢獻；以及（3）一個高性能計算（HPC）基準，評估在SLURM集群上的端到端機器學習訓練協調。除了基準外，我們還展示了EngiAI，一個基於LangGraph的多代理系統（MAS）參考實現，通過一個監督架構協調七個專門代理，實現基準的操作，統一拓撲優化、文檔檢索、HPC作業協調和3D打印機控制。在四個LLM後端和兩個EngiBench問題中，專有模型在Beams2D上的平均任務完成率達到96-97%，而開源的4B參數模型達到55-78%，顯示出明顯的代際改進。條件分支被證明是最具挑戰性的，條件風格在Photonics2D上的任務完成率下降至20-53%。RAG門控確認了接近完美的檢索增強分數（$\approx 1.0$），而在沒有檢索的情況下則接近零，驗證了評估設計。在HPC協調方面，一個模型在100%的運行中完成所有管道步驟，而另一個則下降至50%，顯示多步指令跟隨在長時間運行的工作流程中會退化。

##### **TERGAD: Structure-Aware Text-Enhanced Representations for Graph Anomaly Detection**
2605.19738v1 by Wen Shi, Zhe Wang, Huafei Huang, Qing Qing, Ziqi Xu, Qixin Zhang, Xikun Zhang, Renqiang Luo, Feng Xia

Graph Anomaly Detection (GAD) aims to identify atypical graph entities, such as nodes, edges, or substructures, that deviate significantly from the majority. While existing text-rich approaches typically integrate structural context into the data representation pipeline using raw textual features, they often neglect the structural context of nodes. This limitation hinders their ability to detect sophisticated anomalies arising from inconsistencies between a node's inherent content and its topological role. To bridge this gap, we propose TERGAD (Structure-aware Text-enhanced Representations for Graph Anomaly Detection), A novel data augmentation framework that enriches structural semantics for GAD via the semantic reasoning capabilities of Large Language Models (LLMs). Specifically, TERGAD translates node-level topological properties into descriptive natural language narratives, which are subsequently processed by an LLM to derive high-level semantic embeddings. These embeddings are then adaptively fused with original node attributes through a gated dual-branch autoencoder to jointly reconstruct both graph structure and node features. The anomaly score is computed based on the integrated reconstruction error, effectively capturing deviations in both observable attributes and LLM-informed semantic expectations. Extensive experiments on six real-world datasets demonstrate that TERGAD consistently outperforms state-of-the-art baselines. Furthermore, our ablation studies validate the indispensable role of structural semantic guidance and the efficacy of the gated fusion mechanism. Code is available at https://github.com/Kantorakitty/TERGAD-main.

摘要：圖形異常檢測（GAD）旨在識別與大多數顯著偏離的非典型圖形實體，例如節點、邊或子結構。雖然現有的文本豐富方法通常通過使用原始文本特徵將結構上下文整合到數據表示管道中，但它們往往忽視了節點的結構上下文。這一限制妨礙了它們檢測由節點固有內容與其拓撲角色之間不一致性引起的複雜異常的能力。為了填補這一空白，我們提出了TERGAD（結構感知的文本增強表示用於圖形異常檢測），這是一個新穎的數據增強框架，通過大型語言模型（LLMs）的語義推理能力來豐富GAD的結構語義。具體而言，TERGAD將節點級的拓撲特性轉換為描述性自然語言敘述，這些敘述隨後由LLM處理以推導出高層次的語義嵌入。然後，這些嵌入通過一個門控雙分支自編碼器與原始節點屬性自適應融合，以共同重建圖形結構和節點特徵。異常分數是基於整合的重建誤差計算的，有效捕捉可觀察屬性和LLM知情的語義期望之間的偏差。在六個真實世界數據集上的廣泛實驗表明，TERGAD始終優於最先進的基準。此外，我們的消融研究驗證了結構語義指導的不可或缺性和門控融合機制的有效性。代碼可在 https://github.com/Kantorakitty/TERGAD-main 獲得。

##### **ContextRAG: Extraction-Free Hierarchical Graph Construction for Retrieval-Augmented Generation**
2605.19735v1 by Roman Prosvirnin, Sergei Kuznetsov, Seungmin Jin

Graph-structured retrieval-augmented generation (RAG) systems can improve answer quality on multi-hop questions, but many current systems rely on large language models (LLMs) to extract entities, relations, and summaries during indexing. These calls add token and wall-clock costs that grow with corpus size. We present ContextRAG, a graph RAG system whose graph topology is constructed without LLM-based entity or relation extraction. ContextRAG derives a fuzzy concept graph over chunk embeddings using residual-quantization k-means and Formal Concept Analysis with Lukasiewicz residuated logic. Bridge-like and meet-derived context nodes are induced by soft fuzzy join and meet operations, rather than by LLM-written graph edges. On a 130-task UltraDomain subset, ContextRAG builds its index with 30 LLM calls and 22,073 tokens. In contrast, a local HiRAG reproduction stress test required 870 indexing calls and 3.54M tokens on a 20-task subset before failing during graph construction; linear extrapolation to 130 tasks implies over 23M indexing tokens. ContextRAG obtains 33.6% F1 overall and 36.8% F1 on multi-hop tasks. An activation analysis shows that queries retrieving at least one lattice-derived node in the top five achieve +3.9 percentage points F1 over queries that do not; this association is diagnostic rather than causal.

摘要：圖結構檢索增強生成（RAG）系統可以改善多跳問題的答案質量，但許多當前系統依賴大型語言模型（LLMs）在索引過程中提取實體、關係和摘要。這些調用增加了隨著語料庫大小增長的令牌和時間成本。我們提出了ContextRAG，一種圖形RAG系統，其圖形拓撲是在不依賴LLM的實體或關係提取的情況下構建的。ContextRAG通過殘差量化k-means和使用Lukasiewicz剩餘邏輯的形式概念分析，從塊嵌入中推導出模糊概念圖。橋樑狀和交會導出的上下文節點是通過軟模糊聯接和交會操作誘導的，而不是通過LLM編寫的圖邊。 在130任務的UltraDomain子集上，ContextRAG用30次LLM調用和22,073個令牌構建其索引。相比之下，一個本地的HiRAG重現壓力測試在一個20任務的子集上需要870次索引調用和3.54M個令牌，然後在圖形構建過程中失敗；對130個任務的線性外推意味著超過23M的索引令牌。ContextRAG的整體F1得分為33.6%，在多跳任務上的F1得分為36.8%。激活分析顯示，檢索到至少一個由格派生的節點的查詢在前五名中比未檢索到的查詢高出3.9個百分點的F1；這種關聯是診斷性的，而非因果性的。

##### **LIFT and PLACE: A Simple, Stable, and Effective Knowledge Distillation Framework for Lightweight Diffusion Models**
2605.19729v1 by Hyunsoo Han, Sangyeop Yeo, Jaejun Yoo

We demonstrate that in knowledge distillation for diffusion models, the teacher network's highly complex denoising process - stemming from its substantially larger capacity - poses a significant challenge for the student model to faithfully mimic. To address this problem, we propose a coarse-to-fine distillation framework with LInear FiTtingbased distillation (LIFT) and Piecewise Local Adaptive Coefficient Estimation (PLACE). First, LIFT decomposes the objective into a "coarse" alignment and a "fine" refinement. The student is then trained on coarse alignment before proceeding to hard refinement. Second, PLACE extends LIFT to address spatially non-uniform errors by partitioning outputs into error-based groups, providing locally adaptive guidance. Our experiments show that LIFT and PLACE is effective across diffusion spaces (image/latent), backbones (U-Net/DiT), tasks (unconditional/conditional), datasets, and even extends to flow-based models such as MMDiT (SD3). Furthermore, under extreme compression with a 1.3M-parameter student (only 1.6% of the teacher), conventional KD fails to provide sufficient guidance for stable training, with FID scores often degrading to 50-200+, but our method remains stably convergent and achieves an FID of 15.73.

摘要：我們展示了在擴散模型的知識蒸餾中，教師網絡的高度複雜去噪過程——源於其顯著更大的容量——對學生模型忠實模仿構成了重大挑戰。為了解決這個問題，我們提出了一個從粗到細的蒸餾框架，包含基於線性擬合的蒸餾（LIFT）和分段局部自適應係數估計（PLACE）。首先，LIFT將目標分解為“粗略”對齊和“精細”修正。然後，學生在粗略對齊上進行訓練，之後再進行困難的修正。其次，PLACE擴展了LIFT，以通過將輸出劃分為基於錯誤的組來解決空間上不均勻的錯誤，提供局部自適應的指導。我們的實驗顯示，LIFT和PLACE在擴散空間（圖像/潛在）、骨幹（U-Net/DiT）、任務（無條件/有條件）、數據集之間都有效，甚至擴展到基於流的模型，如MMDiT（SD3）。此外，在極端壓縮下，使用1.3M參數的學生（僅為教師的1.6%），傳統的KD無法提供足夠的指導以進行穩定訓練，FID分數經常降至50-200+，但我們的方法仍然穩定收斂，並達到FID 15.73。

##### **Mathematical Reasoning in Large Language Models: Benchmarks, Architectures, Evaluation, and Open Challenges**
2605.19723v1 by Husnain Amjad, Raja Khurram Shahzad, Aamir Shahzad, Mehwish Fatima

Mathematical reasoning is essential for problem-solving in education, science, and industry, serving as a crucial benchmark for evaluating artificial intelligence systems. As Large Language Models (LLMs) improve their reasoning capabilities, understanding how well they perform mathematical reasoning has become increasingly important. This survey synthesizes recent advancements in mathematical reasoning with LLMs through a structured analysis of datasets, architectures, training strategies, and evaluation protocols. Our systematic review encompasses approximately 120 peer-reviewed studies and preprints, examining the evolution of this research area and providing a unified analytical framework to understand current progress and limitations. Our study particularly introduces a unified taxonomy of mathematical datasets, distinguishing between pretraining corpora, supervised fine-tuning resources, and evaluation benchmarks across varying levels of reasoning complexity. A systematic analysis of reasoning architectures and training strategies, including tool integration, verifier-guided reasoning, and parameter-efficient adaptation, is presented to assess their effects on reasoning robustness and generalization. Moreover, a comparative evaluation of existing metrics highlights the gap between final-answer accuracy and process-level reasoning verification. By synthesizing insights across these areas, our analysis identifies recurring failure modes, such as reasoning faithfulness issues, benchmark biases, and generalization limitations, and outlines key research directions toward improving symbolic grounding, evaluation reliability, and the development of more robust and trustworthy LLM-based reasoning systems.

摘要：數學推理對於教育、科學和工業中的問題解決至關重要，並作為評估人工智慧系統的重要基準。隨著大型語言模型（LLMs）推理能力的提升，了解它們在數學推理方面的表現變得越來越重要。本調查通過對數據集、架構、訓練策略和評估協議的結構化分析，綜合了LLMs在數學推理方面的最新進展。我們的系統性回顧涵蓋了約120篇經過同行評審的研究和預印本，檢視了這一研究領域的演變，並提供了一個統一的分析框架，以理解當前的進展和局限性。我們的研究特別引入了一個統一的數學數據集分類法，區分了預訓練語料庫、監督微調資源和不同推理複雜度的評估基準。對推理架構和訓練策略的系統分析，包括工具整合、驗證者引導推理和參數高效適應，旨在評估它們對推理穩健性和泛化能力的影響。此外，對現有指標的比較評估突顯了最終答案準確性與過程級推理驗證之間的差距。通過綜合這些領域的見解，我們的分析識別了重複出現的失敗模式，如推理忠實性問題、基準偏見和泛化限制，並概述了改善符號基礎、評估可靠性以及開發更穩健和可信的基於LLM的推理系統的關鍵研究方向。

##### **Measuring Safety Alignment Effects in Autonomous Security Agents**
2605.19722v1 by Isaac David, Arthur Gervais

Do stock safety-aligned language models and their uncensored or abliterated derivatives behave differently when run as autonomous security agents? Single-turn refusal benchmarks cannot answer this question: security agents must inspect repositories, call tools, and produce vulnerability evidence inside authorized sandboxes.
  We present a trace-based benchmark of 30 local vulnerability-analysis tasks with fixed tools, deterministic success predicates, redaction rules, and grounding checks, and compare four stock models against uncensored or abliterated derivatives: Gemma 4 31B, Gemma 4 26B A4B, Qwen2.5-Coder 7B, and Llama 3.1 8B. The artifact contains 1,500 security-agent traces and 800 non-security control traces. The Gemma pairs show large less-restricted gains on security tasks: 14.0% versus 0.7% success for 31B and 10.7% versus 0.0% for 26B, with higher mean grounding (3.91 versus 3.27 and 4.12 versus 1.64 out of five) and 0.0% refusal, suppressed-action, and unsafe-action rates in the 31B traces. However, controls and non-Gemma pairs rule out a clean security-specific or universal less-restricted effect: Gemma gaps also appear on ordinary coding tasks, Qwen2.5-Coder success is lower for the less-restricted derivative (2.0% versus 5.3%), and the abliterated Llama derivative fails the tool protocol. Across all families, hard proof-of-trigger and patch-verification tasks remain unsolved. These results show that safety alignment effects in autonomous security agents should be measured at the system level, separating refusal, unsafe action, tool reliability, and evidence grounding rather than treating refusal rate as the safety signal.

摘要：股票安全對齊的語言模型及其未經審查或刪除的衍生品在作為自主安全代理時是否表現不同？單次拒絕基準無法回答這個問題：安全代理必須檢查資料庫、調用工具，並在授權的沙盒內產生漏洞證據。<lb/>我們提出了一個基於追蹤的基準，包含30個本地漏洞分析任務，配備固定工具、確定性成功前提、刪除規則和基礎檢查，並將四個股票模型與未經審查或刪除的衍生品進行比較：Gemma 4 31B、Gemma 4 26B A4B、Qwen2.5-Coder 7B和Llama 3.1 8B。<lb/>該文獻包含1,500個安全代理追蹤和800個非安全控制追蹤。Gemma 配對在安全任務上顯示出較大的不受限制的增益：31B的成功率為14.0%對比0.7%，26B則為10.7%對比0.0%，且在31B追蹤中具有更高的平均基礎（3.91對比3.27和4.12對比1.64，滿分五分）以及0.0%的拒絕、抑制行動和不安全行動率。<lb/>然而，控制和非Gemma配對排除了明確的安全特定或普遍的不受限制效果：Gemma的差距在普通編碼任務上也出現，Qwen2.5-Coder在不受限制的衍生品上的成功率較低（2.0%對比5.3%），而刪除的Llama衍生品則未通過工具協議。在所有家族中，觸發和補丁驗證任務仍未解決。這些結果顯示，自主安全代理中的安全對齊效果應在系統層面進行測量，分開拒絕、不安全行動、工具可靠性和證據基礎，而不是將拒絕率視為安全信號。

##### **CAIT: A Syntactic Parsing Toolkit for Child-Adult InTeractions**
2605.19718v1 by Francesca Padovani, Xiulin Yang, Bastian Bunzeck, Jaap Jumelet, Yevgen Matusevych, Nathan Schneider, Arianna Bisazza

CHILDES is a paramount resource for language acquisition studies -- yet computational tools for analyzing its syntactic structure remain limited. Leveraging the recent release of the UD-English-CHILDES treebank with gold-standard Universal Dependencies (UD) annotations, we train a state-of-the-art dependency parser specifically tailored to CHILDES. The parser more accurately captures syntactic patterns in child--adult interactions, outperforming widely used off-the-shelf English parsers, including SpaCy and Stanza. Alongside the parser, we also release a Part-of-Speech tagger and an utterance-level construction tagger, which together form the open-source Syntactic Parsing Toolkit for Child--Adult InTeractions (CAIT). Through a detailed error analysis and a case study tracking the distribution of syntactic constructions across developmental time in CHILDES, we demonstrate the practical utility of the toolkit for large-scale, reproducible research on language acquisition.

摘要：CHILDES 是語言習得研究的重要資源——然而，用於分析其句法結構的計算工具仍然有限。利用最近發布的 UD-English-CHILDES 樹庫，並附有金標準的通用依賴（UD）標註，我們訓練了一個專門為 CHILDES 設計的最先進依賴解析器。該解析器更準確地捕捉了兒童與成人互動中的句法模式，超越了廣泛使用的現成英語解析器，包括 SpaCy 和 Stanza。除了解析器，我們還發布了一個詞性標註器和一個語句級建構標註器，這兩者共同形成了開源的兒童與成人互動句法解析工具包（CAIT）。通過詳細的錯誤分析和一個案例研究，追蹤 CHILDES 中句法建構在發展時間上的分佈，我們展示了該工具包在大規模、可重複的語言習得研究中的實用性。

##### **LLM-Based Financial Sentiment Analysis in Arabic: Evidence from Saudi Markets**
2605.19714v1 by Mona H. Albaqawi, Eman M. Albalkhi, Joud A. Albaiti, Enrico Lopedoto

Investor sentiment shapes financial markets, yet modeling sentiment in Arabic financial contexts remains challenging due to linguistic complexity and limited resources. We present an Arabic NLP framework for large-scale financial sentiment analysis tailored to the Saudi market, integrating official financial news and social media to capture institutional and public investor sentiment. The framework constructs a large Arabic financial corpus through a multi-stage pipeline encompassing data collection, cleaning, deduplication, entity linking, and sentiment annotation. Transformer-based NER combined with a curated company lexicon links textual mentions to canonical company identifiers, with sentiment labels assigned using a five-class scheme. The resulting dataset of 84K samples supports company-level sentiment aggregation and analysis of sentiment dynamics relative to stock market behavior on the Saudi Exchange. Experimental results demonstrate reliable and scalable Arabic financial sentiment analysis.

摘要：投資者情緒塑造金融市場，但在阿拉伯金融環境中建模情緒仍然具有挑戰性，這是由於語言的複雜性和資源的有限性。我們提出了一個針對沙烏地市場的大規模阿拉伯語自然語言處理框架，用於金融情緒分析，整合官方金融新聞和社交媒體，以捕捉機構和公眾投資者的情緒。該框架通過一個多階段的流程構建了一個大型阿拉伯金融語料庫，涵蓋數據收集、清理、去重、實體連結和情緒標註。基於Transformer的命名實體識別（NER）結合精心策劃的公司詞彙，將文本提及與標準公司標識符連結，情緒標籤則使用五類方案進行分配。最終生成的84K樣本數據集支持公司層面的情緒聚合和相對於沙烏地交易所股票市場行為的情緒動態分析。實驗結果顯示可靠且可擴展的阿拉伯金融情緒分析。

##### **Can Large Language Models Reliably Correct Errors in Low-Resource ASR? A Contamination-Aware Case Study on West Frisian**
2605.19711v1 by Yun Hao, Reihaneh Amooie, Wietse de Vries, Rik van Noord, Martijn Wieling

Automatic speech recognition (ASR) has improved substantially in recent years, yet performance remains limited for low-resource languages. Large language models (LLMs) have shown promise for improving ASR through generative error correction (GER), but their effectiveness in low-resource settings remains underexplored. In addition, it remains unclear to what extent data contamination influences the reported improvements in LLM-based GER. This study investigates LLM-based GER for low-resource Frisian. In addition to a public corpus, we construct and use a Frisian offline dataset with non-public texts for evaluation to control for potential data contamination. Results show that GER improves ASR performance in most settings, with the best GPT-5.1 results surpassing oracle WERs. Comparable gains on the offline dataset indicate that improvements reflect true correction ability. We further provide a detailed error analysis revealing model correction patterns.

摘要：自動語音辨識（ASR）在近年來有了顯著改善，但在低資源語言的表現仍然有限。大型語言模型（LLMs）在通過生成性錯誤修正（GER）來改善 ASR 方面顯示出潛力，但它們在低資源環境中的有效性仍然未被充分探討。此外，數據污染在多大程度上影響 LLM 基礎的 GER 所報告的改進仍不清楚。本研究調查了針對低資源弗里西語的 LLM 基礎 GER。除了公共語料庫外，我們還構建並使用了一個包含非公共文本的弗里西語離線數據集進行評估，以控制潛在的數據污染。結果顯示，GER 在大多數設置中改善了 ASR 性能，最佳的 GPT-5.1 結果超過了 oracle WER。離線數據集上的可比增益表明，這些改進反映了真實的修正能力。我們還提供了詳細的錯誤分析，揭示了模型的修正模式。

##### **Beyond Rational Illusion: Behaviorally Realistic Strategic Classification**
2605.19674v1 by Xinpeng Lv, Yunxin Mao, Renzhe Xu, Chunyuan Zheng, Yikai Chen, Haoxuan Li, Yang Shi, Jinxuan Yang, Zhouchen Lin, Yuanlong Chen, Yuanxing Zhang, Shaowu Yang, Wenjing Yang, Haotian Wang

Strategic classification(SC) studies the interaction between decision models and agents who strategically manipulate their features for favorable outcomes. Existing SC frameworks typically rely on the idealized assumption that agents are strictly rational. However, evidence from behavioral economics and psychology consistently shows that real-world decision-making is often shaped by cognitive biases, deviating from pure rationality. To formalize this limitation, we identify and define a new problem setting, termed the behaviorally realistic strategic classification problem, where agents' strategic manipulations deviate from full rationality due to psychological biases. Motivated by the identified limitation, we propose the Prospect-Guided Strategic Framework (Pro-SF) to address the problem, a principled framework grounded in prospect theory to model and learn under behaviorally realistic strategic responses. Specifically, to capture behaviorally realistic strategic manipulations, our framework reformulates the Stackelberg-style interaction between agents and the decision-maker by incorporating three key mechanisms inspired by prospect theory, including the asymmetry between benefits and costs, different subjective reference points, and non-rational probability distortion. Experiments on synthetic and real-world datasets establish Pro-SF as a behaviorally grounded approach to strategic classification, bridging machine learning and behavioral economics for more reliable deployment in the real world.

摘要：策略分類（SC）研究決策模型與代理人之間的互動，這些代理人策略性地操縱其特徵以獲得有利結果。現有的SC框架通常依賴於代理人完全理性的理想假設。然而，來自行為經濟學和心理學的證據持續顯示，現實世界中的決策往往受到認知偏誤的影響，偏離純粹的理性。為了正式化這一限制，我們識別並定義了一個新的問題設定，稱為行為現實的策略分類問題，在這裡，代理人的策略操縱因心理偏誤而偏離完全理性。受到識別的限制的激勵，我們提出了前景引導策略框架（Pro-SF）來解決這個問題，這是一個基於前景理論的原則框架，用於建模和學習行為現實的策略反應。具體而言，為了捕捉行為現實的策略操縱，我們的框架通過納入三個受前景理論啟發的關鍵機制，重新構建了代理人與決策者之間的斯塔克爾伯格風格互動，包括利益與成本之間的非對稱性、不同的主觀參考點，以及非理性的概率扭曲。對合成和現實世界數據集的實驗確立了Pro-SF作為一種基於行為的策略分類方法，為機器學習和行為經濟學之間架起了一座橋樑，以便在現實世界中更可靠地部署。

##### **CriterAlign: Criterion-Centric Rationale Alignment for Code Preference Judging**
2605.19665v1 by Zhenyu Li, Aleksandar Cvejic, Zehui Chen, Peter Wonka

Pairwise human preference prediction is central to evaluating code-generation systems, where quality often depends on task-specific trade-offs beyond functional correctness. While rubric-based LLM judges improve interpretability by decomposing evaluation into explicit criteria, most existing pipelines remain pointwise: they score each response independently and derive preferences by comparing aggregated scores. We show that this design is poorly matched to pairwise code preference prediction and can underperform a strong monolithic judge. We propose CriterAlign, a criterion-centric framework that adapts rubric-based judging to pairwise preference evaluation through direct criterion-level pairwise judgments, tie-driven criterion refinement, swap-consistency filtering, and final pairwise synthesis. We further introduce Human-Preference-Aligned Guidance (HPAG), synthesized offline from training examples by extracting recurring rationale gaps between human preferences and monolithic judge predictions, and injected into the criterion generator, criterion judge, and final judge. On BigCodeReward, CriterAlign improves a Qwen2.5-VL-32B monolithic judge from 60.4% to 66.3% accuracy, with ablations confirming the contributions of pairwise criterion design and HPAG.

摘要：成對人類偏好預測是評估代碼生成系統的核心，其中質量往往取決於超越功能正確性的任務特定權衡。雖然基於評分標準的 LLM 評審通過將評估分解為明確的標準來提高可解釋性，但大多數現有的流程仍然是逐點的：它們獨立評分每個回應，並通過比較聚合得分來推導偏好。我們顯示這種設計與成對代碼偏好預測不匹配，並且可能表現不如強大的單一評審。我們提出 CriterAlign，一個以標準為中心的框架，通過直接的標準級成對判斷、基於平局的標準細化、交換一致性過濾和最終成對合成，將基於評分標準的評審適應於成對偏好評估。我們進一步介紹了人類偏好對齊引導（HPAG），這是通過提取人類偏好與單一評審預測之間的重複理由差距，從訓練示例中離線合成的，並注入到標準生成器、標準評審和最終評審中。在 BigCodeReward 上，CriterAlign 將 Qwen2.5-VL-32B 單一評審的準確率從 60.4% 提高到 66.3%，消融實驗確認了成對標準設計和 HPAG 的貢獻。

##### **Pseudocode-Guided Structured Reasoning for Automating Reliable Inference in Vision-Language Models**
2605.19663v1 by Weicong Ni, Tianbao Jiang, Linlin Wang

Vision-Language Models (VLMs) are becoming the cornerstone of high-level reasoning for robotic automation, enabling robots to parse natural language commands and perceive their environments. However, their susceptibility to hallucinations introduces critical failures in decision-making, posing significant safety and reliability risks in physical deployments. This challenge is exacerbated by the open-ended nature of real-world tasks, where questions vary vastly in difficulty and modality, demanding robust and adaptable reasoning strategies. To tackle this, we propose the Pseudocode-guided Structured Reasoning framework (PStar), which adaptively selects structured pseudocode reasoning paths to help VLMs perform flexible and step-by-step reasoning. We first design a set of abstract reasoning functions and formulate a structured pseudocode library to represent modular reasoning strategies. Crucially, we design a Difficulty Feature Vector (DFV) that allows the model to assess question complexity and adaptively choose appropriate reasoning strategies-enhancing robustness and interpretability. Extensive experiments demonstrate that PStar significantly reduces hallucination rates, achieving state-of-the-art scores of 87.1% on POPE and 68.0% on MMStar, outperforming even GPT-4V. By providing a validated mechanism to reduce visual-language errors, PStar offers a critical step toward deploying more trustworthy and deterministic VLMs for real-world automated systems, where such errors can lead to catastrophic outcomes.

摘要：視覺-語言模型 (VLMs) 正在成為機器人自動化高級推理的基石，使機器人能夠解析自然語言命令並感知其環境。<lb/>然而，它們對幻覺的敏感性會在決策過程中引入關鍵性失誤，對實際部署造成重大的安全性和可靠性風險。<lb/>這一挑戰因現實世界任務的開放性特質而加劇，問題在難度和形式上差異巨大，要求穩健且可適應的推理策略。<lb/>為了解決這個問題，我們提出了伪代码引导的结构化推理框架 (PStar)，該框架自適應地選擇結構化的伪代码推理路徑，以幫助 VLMs 執行靈活且逐步的推理。<lb/>我們首先設計了一組抽象推理函數，並制定了一個結構化的伪代码庫，以表示模塊化的推理策略。<lb/>關鍵是，我們設計了一個難度特徵向量 (DFV)，使模型能夠評估問題的複雜性並自適應地選擇合適的推理策略——增強了穩健性和可解釋性。<lb/>廣泛的實驗表明，PStar 顯著降低了幻覺率，在 POPE 上達到了 87.1% 的最新分數，在 MMStar 上達到了 68.0%，甚至超越了 GPT-4V。<lb/>通過提供一種經過驗證的機制來減少視覺-語言錯誤，PStar 為在現實世界自動化系統中部署更可靠和確定性的 VLMs 提供了關鍵的一步，因為這些錯誤可能導致災難性的後果。

##### **When Tabular Foundation Models Meet Strategic Tabular Data: A Prior Alignment Approach**
2605.19662v1 by Xinpeng Lv, Yunxin Mao, Renzhe Xu, Chunyuan Zheng, Yikai Chen, Haoxuan Li, Jinxuan Yang, Kun Kuang, Yuanlong Chen, Mingyang Geng, Wanrong Huang, Shixuan Liu, Shaowu Yang, Wenjing Yang, Zhouchen Lin, Haotian Wang

Tabular foundation models based on pretrained prior-data fitted networks~(PFNs) have shown strong generalization on diverse tabular tasks, but they are typically designed for \emph{non-strategic} settings where data distributions are independent of deployed classifiers. In many real-world decision scenarios, however, individuals may strategically modify their features after deployment to obtain favorable outcomes, inducing a post-deployment distribution shift. This paper studies whether PFN-style tabular foundation models can generalize to such \emph{strategic} tabular data. We show that strategic manipulation creates a mismatch between the non-strategic prior learned during pretraining and the post-manipulation strategic prior, which leads to systematic prediction bias. To address this issue, we propose \textbf{Strategic Prior-data Fitted Network}~\textit{(SPN)}, an inference-time strategy-aware framework that adapts tabular foundation models to strategic environments without retraining. SPN constructs strategic in-context examples to approximate post-manipulation inputs and aligns PFN predictions with the induced strategic distribution. Experiments on real-world and synthetic tabular datasets show that SPN consistently improves robustness and predictive performance under strategic manipulation compared with both tabular foundation models and classical tabular methods.

摘要：基於預訓練的先前數據擬合網絡~(PFNs)的表格基礎模型在各種表格任務上展現了強大的泛化能力，但它們通常是為\emph{非策略性}環境設計的，其中數據分佈與部署的分類器相互獨立。然而，在許多現實世界的決策情境中，個體可能會在部署後策略性地修改其特徵以獲得有利的結果，從而引起部署後的分佈變化。本文研究PFN風格的表格基礎模型是否能夠對這種\emph{策略性}表格數據進行泛化。我們表明，策略性操控在預訓練期間學習的非策略性先驗和後操控的策略性先驗之間造成了不匹配，這導致了系統性的預測偏差。為了解決這個問題，我們提出了\textbf{策略性先前數據擬合網絡}~\textit{(SPN)}，這是一個在推理時具有策略意識的框架，能夠在不重新訓練的情況下將表格基礎模型適應於策略性環境。SPN構建策略性的上下文示例以近似後操控的輸入，並將PFN的預測與引起的策略性分佈對齊。在現實世界和合成的表格數據集上的實驗顯示，與表格基礎模型和傳統表格方法相比，SPN在策略性操控下始終提高了穩健性和預測性能。

##### **OScaR: The Occam's Razor for Extreme KV Cache Quantization in LLMs and Beyond**
2605.19660v1 by Zunhai Su, Rui Yang, Chao Zhang, Yaxiu Liu, Yifan Zhang, Wei Wu, Jing Xiong, Dayou Du, Xialie Zhuang, Yulei Qian, Yuchen Xie, Yik-Chung Wu, Hongxia Yang, Ngai Wong

The rapid advancement toward long-context reasoning and multi-modal intelligence has made the memory footprint of the Key-Value (KV) cache a dominant memory bottleneck for efficient deployment. While the established per-channel quantization effectively accommodates intrinsic channel-wise outliers in Key tensors, its efficacy diminishes under extreme compression. In this work, we revisit the inherent limitations of the per-channel quantization paradigm from both empirical and theoretical perspectives. Our analysis identifies Token Norm Imbalance (TNI) as the primary bottleneck to quantization fidelity. We demonstrate that TNI systematically amplifies errors when shared quantization parameters are required to span token groups exhibiting substantial norm disparities. Instead of relying on intricate quantization pipelines (e.g., TurboQuant), we propose OScaR (Omni-Scaled Canalized Rotation), an accurate and lightweight KV cache compression framework for X-LLMs (i.e., text-only, multi-modal, and omni-modal LLMs). Advancing the per-channel paradigm, OScaR employs Canalized Rotation followed by Omni-Token Scaling to mitigate TNI-induced sequence-dimensional variance both effectively and efficiently, further supported by our optimized system design and CUDA kernels. Extensive evaluations across X-LLMs show that OScaR consistently outperforms existing methods and achieves near-lossless performance under INT2 quantization, establishing it as a robust, low-complexity, and universal framework that defines a new Pareto front. Compared with the BF16 FlashDecoding-v2 baseline, our OScaR implementation achieves a notable up to 3.0x speedup in decoding, reduces memory footprint by 5.3x, and increases throughput by 4.1x. The code for OScaR is publicly available at https://github.com/ZunhaiSu/OScaR-KV-Quant.

摘要：快速進展的長上下文推理和多模態智能使得鍵值（KV）快取的記憶體佔用成為高效部署的主要記憶體瓶頸。雖然現有的每通道量化有效地適應了鍵張量中的內在通道異常，但在極端壓縮下其效能會減弱。在這項工作中，我們從實證和理論的角度重新審視了每通道量化範式的固有限制。我們的分析確定了標記範數不平衡（TNI）作為量化保真度的主要瓶頸。我們證明了當共享量化參數需要跨越顯著範數差異的標記組時，TNI會系統性地放大錯誤。我們提出了OScaR（全尺度通道旋轉），這是一個準確且輕量的KV快取壓縮框架，適用於X-LLMs（即僅文本、多模態和全模態LLMs）。OScaR在推進每通道範式的同時，採用了通道化旋轉，然後進行全標記縮放，以有效且高效地減輕TNI引起的序列維度變異，並進一步得到我們優化的系統設計和CUDA內核的支持。在X-LLMs上的廣泛評估顯示，OScaR始終超越現有方法，並在INT2量化下實現接近無損的性能，確立了它作為一個穩健、低複雜度和通用框架，定義了一個新的Pareto前沿。與BF16 FlashDecoding-v2基準相比，我們的OScaR實現達到了高達3.0倍的解碼加速，記憶體佔用減少了5.3倍，吞吐量提高了4.1倍。OScaR的代碼已公開於https://github.com/ZunhaiSu/OScaR-KV-Quant。

##### **K-Quantization and its Impact on Output Performance**
2605.19645v1 by Robin Baki Davidsson, Pierre Nugues

Recent advancements in large language models (LLMs) have shown their remarkable capacities in many NLP tasks. However, their substantial size often presents challenges for deployment. This necessitates efficient techniques for model compression, with quantization emerging as a prominent solution. Despite its benefits, the exact impact of quantization (from 2- to 6-bit) on the performance and accuracy of LLMs remains an active area of research. This paper investigates the performance of eight LLMs at various quantization levels, focusing on tasks such as MMLU-Pro for knowledge processing and reasoning, CRUXEval for code comprehension, and MuSR for reading comprehension. Our results show a consistent trend where higher precision (e.g., 8-bit Q8\_0) yields improved performance, albeit with diminishing returns. Aggressive quantization (e.g., 2-bit Q2\_K) usually retains acceptable accuracy, though some models show a substantial loss in performance. Our findings indicate that while lower bit precision generally reduces performance, the impact varies across models and tasks. Larger models show greater resilience to aggressive quantization, but can still undergo significant drops at lower precision levels. Mid-sized models in the 7-9 billion parameter range strike an optimal balance between efficiency and resource usage. Such results provide insights into the trade-offs between model size, quantization, and performance.

摘要：最近在大型語言模型（LLMs）方面的進展顯示了它們在許多自然語言處理任務中的卓越能力。<lb/>然而，它們的龐大體積往往為部署帶來挑戰。<lb/>這需要有效的模型壓縮技術，其中量化成為一個突出的解決方案。<lb/>儘管量化有其優勢，但量化（從2位到6位）對LLMs性能和準確性的具體影響仍然是一個活躍的研究領域。<lb/>本文研究了在不同量化水平下八個LLMs的性能，重點關注MMLU-Pro在知識處理和推理、CRUXEval在代碼理解以及MuSR在閱讀理解等任務。<lb/>我們的結果顯示出一個一致的趨勢，即更高的精度（例如，8位Q8\_0）能提高性能，儘管收益遞減。<lb/>激進的量化（例如，2位Q2\_K）通常保持可接受的準確性，儘管某些模型顯示出性能的顯著損失。<lb/>我們的研究結果表明，雖然較低的位精度通常會降低性能，但其影響因模型和任務而異。<lb/>較大的模型對激進量化表現出更大的韌性，但在較低的精度水平下仍可能經歷顯著的性能下降。<lb/>在70-90億參數範圍內的中型模型在效率和資源使用之間達到了最佳平衡。<lb/>這些結果提供了有關模型大小、量化和性能之間權衡的見解。

##### **The Accessibility Capability Boundary: Operational Limits and Expansion Potential of AI-Generated Browser-Native Accessibility Systems**
2605.19638v1 by Rizwan Jahangir, Daisuke Ishii

As large language models (LLMs) demonstrate increasing competence in synthesizing functional user interfaces, a fundamental question emerges in accessibility computing: \textit{how far can AI-driven accessibility systems go?} This paper introduces the \textit{Accessibility Capability Boundary} (ACB), a formal framework for reasoning about the operational limits and expansion potential of autonomous accessibility systems, and grounds this theory in a real-world systems artifact. We model accessibility not as a binary compliance property but as a dynamic, multidimensional capability space constrained by measurable variables including deployment latency, cognitive load, infrastructure dependency, offline persistence, interaction complexity, and adaptability. We argue that AI-generated, browser-native systems constructed as single-file HTML artifacts leveraging standard browser APIs may dramatically shift the ACB outward by reducing deployment friction to near-zero and enabling rapid, context-specific interface adaptation. We ground our theoretical framework in the analysis of two real-world exploratory prototypes. The first is an AI-generated browser-native accessibility interface deployed for a blind user in Nepal. The second is a fully functional, open-source webcam alignment assistant for visually impaired users, serving as a concrete systems artifact. Through formal definitions, propositions, and a comparative evaluation matrix, we characterize the regions of the accessibility capability space that such systems can and cannot reach. We further identify remaining computational, infrastructural, and verification constraints that constitute the hard boundaries of this paradigm. This work contributes a theoretical foundation for understanding the scalable limits of autonomous accessibility computing and proposes a research agenda for future work in accessibility-aware AI systems.

摘要：隨著大型語言模型（LLMs）在合成功能性用戶界面方面展現出越來越高的能力，一個基本問題在無障礙計算中浮現出來：\textit{AI驅動的無障礙系統能走多遠？} 本文介紹了\textit{無障礙能力邊界}（ACB），這是一個用於推理自主無障礙系統操作限制和擴展潛力的正式框架，並將這一理論根植於一個現實世界的系統實物中。我們將無障礙建模為一個動態的、多維的能力空間，而不是二元合規性特徵，這一空間受到可測量變量的約束，包括部署延遲、認知負荷、基礎設施依賴性、離線持續性、交互複雜性和適應性。我們主張，以單文件HTML實物構建的AI生成的瀏覽器原生系統，利用標準瀏覽器API，可能會通過將部署摩擦減少到接近零來顯著向外推移ACB，並實現快速的、特定上下文的界面適應。我們在對兩個現實世界探索性原型的分析中將我們的理論框架落實。第一個是為尼泊爾的一位盲用戶部署的AI生成的瀏覽器原生無障礙界面。第二個是一個功能完整的、開源的網絡攝像頭對齊助手，專為視障用戶服務，作為一個具體的系統實物。通過正式定義、命題和比較評估矩陣，我們描述了這些系統可以和不能達到的無障礙能力空間的區域。我們進一步識別出構成這一範式硬邊界的剩餘計算、基礎設施和驗證限制。本研究為理解自主無障礙計算的可擴展限制提供了一個理論基礎，並提出了未來無障礙感知AI系統研究的議程。

##### **P2DNav: Panorama-to-Downview Reasoning for Zero-shot Vision-and-Language Navigation**
2605.19634v1 by Kai Sheng, Liuyi Wang, Haojie Dai, Jinlong Li, Yongrui Qin, Zongtao He, Chengju Liu, Qijun Chen

Vision-and-language navigation (VLN) requires an embodied agent to ground natural-language instructions into executable navigation actions in unseen environments. Existing zero-shot methods typically rely on additional waypoint prediction modules, which often entangle high-level directional reasoning with fine-grained local grounding, leading to error-prone and unstable decisions. In this paper, we propose P2DNav, a hierarchical framework for zero-shot vision-and-language navigation. P2DNav consists of three core components: Panorama-to-Downview (P2D), Sliding-Window Dialogue Memory (SDM), and Reflective Reorientation Mechanism (RRM). P2D explicitly decomposes navigation decision-making into two stages: panoramic direction selection and downview local grounding. It first selects the instruction-relevant direction from a 360° panorama, and then predicts a pixel-level target point from the downview RGB observation in that direction. In addition, SDM organizes navigation history as a multi-turn dialogue context and maintains recent visual observations within a sliding window to support long-horizon navigation. RRM further enables reflective reorientation by assessing the reliability of local grounding based on the downview observation and returning to panoramic direction selection when necessary. Experiments on the R2R-CE benchmark show that P2DNav achieves strong performance among zero-shot methods. In particular, compared with the state-of-the-art (SOTA) zero-shot waypoint-based and waypoint-free methods, P2DNav achieves SR gains of 146.6% and 58.9%, respectively, demonstrating the effectiveness of P2D, SDM, and RRM for zero-shot VLN. Code will be released for public use.

摘要：視覺與語言導航（VLN）需要一個具體化的代理人將自然語言指令轉化為可執行的導航行動，並在未見過的環境中進行操作。現有的零樣本方法通常依賴額外的路徑點預測模塊，這往往將高層次的方向推理與精細的本地基準混合在一起，導致易出錯且不穩定的決策。在本文中，我們提出了P2DNav，一個用於零樣本視覺與語言導航的分層框架。P2DNav由三個核心組件組成：全景到下視圖（P2D）、滑動窗口對話記憶（SDM）和反思性重新定向機制（RRM）。P2D明確將導航決策分解為兩個階段：全景方向選擇和下視圖本地基準。它首先從360°全景中選擇與指令相關的方向，然後從該方向的下視RGB觀察中預測一個像素級的目標點。此外，SDM將導航歷史組織為多輪對話上下文，並在滑動窗口內維護最近的視覺觀察，以支持長距離導航。RRM進一步通過根據下視觀察評估本地基準的可靠性來實現反思性重新定向，並在必要時返回全景方向選擇。在R2R-CE基準上的實驗顯示，P2DNav在零樣本方法中表現出色。特別是，與最先進的（SOTA）零樣本基於路徑點和無路徑點的方法相比，P2DNav分別實現了146.6%和58.9%的SR增益，證明了P2D、SDM和RRM在零樣本VLN中的有效性。代碼將會公開釋出供公眾使用。

##### **optimize_anything: A Universal API for Optimizing any Text Parameter**
2605.19633v1 by Lakshya A Agrawal, Donghyun Lee, Shangyin Tan, Wenjie Ma, Karim Elmaaroufi, Rohit Sandadi, Sanjit A. Seshia, Koushik Sen, Dan Klein, Ion Stoica, Joseph E. Gonzalez, Omar Khattab, Alexandros G. Dimakis, Matei Zaharia

Can a single LLM-based optimization system match specialized tools across fundamentally different domains? We show that when optimization problems are formulated as improving a text artifact evaluated by a scoring function, a single AI-based optimization system-supporting single-task search, multi-task search with cross-problem transfer, and generalization to unseen inputs-achieves state-of-the-art results across six diverse tasks. Our system discovers agent architectures that nearly triple Gemini Flash's ARC-AGI accuracy (32.5% to 89.5%), finds scheduling algorithms that cut cloud costs by 40%, generates CUDA kernels where 87% match or beat PyTorch, and outperforms AlphaEvolve's reported circle packing solution (n=26). Ablations across three domains reveal that actionable side information yields faster convergence and substantially higher final scores than score-only feedback, and that multi-task search outperforms independent optimization given equivalent per-problem budget through cross-task transfer, with benefits scaling with the number of related tasks. Together, we show for the first time that text optimization with LLM-based search is a general-purpose problem-solving paradigm, unifying tasks traditionally requiring domain-specific algorithms under a single framework. We open-source optimize\_anything with support for multiple backends as part of the GEPA project at https://github.com/gepa-ai/gepa .

摘要：一個基於 LLM 的優化系統能否在根本不同的領域中與專門工具相匹配？我們展示了當優化問題被表述為改善由評分函數評估的文本工件時，一個單一的基於 AI 的優化系統——支持單任務搜索、多任務搜索與跨問題轉移，以及對未見輸入的泛化——在六個不同的任務中達到了最先進的結果。我們的系統發現的代理架構幾乎將 Gemini Flash 的 ARC-AGI 準確率提高了三倍（從 32.5% 提升至 89.5%），找到的排程算法將雲端成本降低了 40%，生成的 CUDA 核心有 87% 與 PyTorch 匹配或超越，並且在圓形打包解決方案上超越了 AlphaEvolve 報告的結果（n=26）。在三個領域的消融實驗中顯示，具有可操作的側面信息比僅依賴評分的反饋能更快收斂並顯著提高最終得分，而多任務搜索在給定相同每個問題預算的情況下，通過跨任務轉移超越了獨立優化，並且隨著相關任務數量的增加而受益。我們首次展示了基於 LLM 的文本優化是一種通用的問題解決範式，將傳統上需要特定領域算法的任務統一在單一框架下。我們將 optimize\_anything 開源，並支持多個後端，作為 GEPA 項目的一部分，網址為 https://github.com/gepa-ai/gepa 。

##### **EMO-BOOST: Emotion-Augmented Audio-Visual Features for Improved Generalization in Deepfake Detection**
2605.19630v1 by Aritra Marik, Marcel Klemt, Anna Rohrbach

With every advancement in generative AI models, forensics is under increasing pressure. The constant emergence of new generation techniques makes it impossible to collect data for each manipulation to train a deepfake detection model. Thus, generalizing to deepfakes unseen during training is one of the major challenges in current deepfake detection research. To tackle this challenge, we employ high-level semantic cues and argue that these cues can support low-level focused approaches in generalizing to unseen types of manipulations. In this work, we study emotions as a high-level semantic cue. We propose Emo-Boost, a multimodal deepfake detection framework that fuses an off-the-shelf RGB- and acoustic-focused deepfake detector with our emotion-based deepfake detector EmoForensics. EmoForensics utilises vision and audio emotion recognition modules and models intra- and inter-modal temporal consistency in emotion representations from an audio-visual stream. We found that EmoForensics and the low-level focused method capture complementary signals. Consequently, combining both signals in EmoBoost enhances the average cross-manipulation generalization AUC by 2.1% on FakeAVCeleb.

摘要：隨著生成式 AI 模型的每一次進步，取證面臨著越來越大的壓力。新一代技術的持續出現使得無法為每一種操控收集數據以訓練深偽檢測模型。因此，對於在訓練期間未見過的深偽進行泛化是當前深偽檢測研究中的主要挑戰之一。為了應對這一挑戰，我們運用了高層次的語義線索，並主張這些線索可以支持低層次的聚焦方法在泛化到未見過的操控類型方面的表現。在這項工作中，我們研究情感作為一種高層次的語義線索。我們提出了 Emo-Boost，一個多模態深偽檢測框架，將現成的 RGB 和聲學聚焦的深偽檢測器與我們基於情感的深偽檢測器 EmoForensics 融合。EmoForensics 利用視覺和音頻情感識別模塊，並從音視訊流中建模情感表示的內部和外部模態時間一致性。我們發現 EmoForensics 和低層次聚焦方法捕捉到互補的信號。因此，在 EmoBoost 中結合這兩種信號使得在 FakeAVCeleb 上的平均跨操控泛化 AUC 提升了 2.1%。

##### **MiMuon: Mixed Muon Optimizer with Improved Generalization for Large Models**
2605.19619v1 by Feihu Huang, Yuning Luo, Songcan Chen

Matrix-structured parameters frequently appear in many artificial intelligence models such as large language models. More recently, an efficient Muon optimizer is designed for matrix parameters of large-scale models, and shows markedly faster convergence than the vector-wise algorithms. Although some works have begun to study convergence properties (i.e., optimization error) of the Muon optimizer, its generalization properties (i.e., generalization error) is still not established. Thus, in this paper, we study generalization error of the Muon optimizer based on algorithmic stability and mathematical induction, and prove that the Muon has a generalization error of $O\big(\frac{1}{Nκ^{T}}\big)$, where $N$ is training sample size, and $T$ denotes iteration number, and $κ>0$ denotes minimum difference between singular values of gradient estimate. To enhance generalization of the Muon, we propose an effective mixed Muon (MiMuon) optimizer by cautiously using orthogonalization of gradient, which is a hybrid of Muon and momentum-based SGD optimizers. Then we prove that our MiMuon optimizer has a lower generalization error of $O\big(\frac{1}{N}\big)$ than $O\big(\frac{1}{Nκ^{T}}\big)$ of Muon optimizer, since $κ$ generally is very small. Meanwhile, we also studied the convergence properties of our MiMuon algorithm, and prove that our MiMuon algorithm has the same convergence rate of $O(\frac{1}{T^{1/4}})$ as the Muon algorithm. Some numerical experimental results on training large models including Qwen3-0.6B and YOLO26m demonstrate efficiency of the MiMuon optimizer.

摘要：矩陣結構的參數經常出現在許多人工智慧模型中，例如大型語言模型。最近，為大型模型的矩陣參數設計了一種高效的Muon優化器，並顯示出比向量算法明顯更快的收斂速度。儘管一些研究已經開始研究Muon優化器的收斂性質（即優化誤差），但其泛化性質（即泛化誤差）仍未確立。因此，在本文中，我們基於算法穩定性和數學歸納法研究Muon優化器的泛化誤差，並證明Muon的泛化誤差為$O\big(\frac{1}{Nκ^{T}}\big)$，其中$N$是訓練樣本大小，$T$表示迭代次數，$κ&gt;0$表示梯度估計的奇異值之間的最小差異。為了增強Muon的泛化能力，我們提出了一種有效的混合Muon（MiMuon）優化器，通過謹慎使用梯度的正交化，這是一種Muon和基於動量的SGD優化器的混合體。然後我們證明我們的MiMuon優化器的泛化誤差為$O\big(\frac{1}{N}\big)$，低於Muon優化器的$O\big(\frac{1}{Nκ^{T}}\big)$，因為$κ$通常非常小。與此同時，我們還研究了我們的MiMuon算法的收斂性質，並證明我們的MiMuon算法具有與Muon算法相同的收斂速率$O(\frac{1}{T^{1/4}})$。一些關於訓練大型模型的數值實驗結果，包括Qwen3-0.6B和YOLO26m，展示了MiMuon優化器的效率。

##### **Formal Skill: Programmable Runtime Skills for Efficient and Accurate LLM Agents**
2605.19604v1 by Xi Zhang, Meijun Gao, Yuntian Zhao, Xinyu Tan, Yilun Yao, Feiyu Wang, Yanshu Wang,  Dingsiyi, Tong Yang

Large Language Model (LLM) agents increasingly act inside real workspaces, where tools and skills determine whether model reasoning becomes reliable action. Existing skills remain largely informal: Markdown skills and instruction packs encode procedures as long natural-language documents, while function calling, Model Context Protocol (MCP) servers, and framework tools structure individual actions but usually leave workflow state, policy enforcement, and completion discipline outside the skill itself. We introduce Formal Skill, a runtime-native abstraction that represents reusable capability with JSON metadata and action schemas, reliable Python executors, hook-governed control logic, Formal Skill routing, and skill-local runtime state. By moving reusable procedure from repeated prompt text into executable state machines and hook policies, Formal Skill gives agents a token-efficient and enforceable control surface. We implement the abstraction in FairyClaw, an open-source event-driven runtime for executable, observable, and composable Formal Skills. On Harness-Bench, FairyClaw obtains highly competitive average scores while using substantially fewer tokens, with especially strong results on tasks that expose the role of Formal Skill.

摘要：大型語言模型（LLM）代理人越來越多地在實際工作空間中運作，工具和技能決定了模型推理是否能轉化為可靠的行動。現有的技能仍然在很大程度上是非正式的：Markdown技能和指令包將程序編碼為長自然語言文件，而函數調用、模型上下文協議（MCP）伺服器和框架工具則結構化個別行動，但通常將工作流程狀態、政策執行和完成紀律留在技能本身之外。我們引入了正式技能，這是一種運行時原生抽象，代表了可重用的能力，並具有JSON元數據和行動架構、可靠的Python執行器、鉤子管理的控制邏輯、正式技能路由和技能本地運行時狀態。通過將可重用程序從重複的提示文本轉移到可執行的狀態機和鉤子政策中，正式技能為代理人提供了一個令牌高效且可執行的控制界面。我們在FairyClaw中實現了這一抽象，這是一個開源的事件驅動運行時，支持可執行、可觀察和可組合的正式技能。在Harness-Bench上，FairyClaw在使用顯著更少的令牌的情況下獲得了高度競爭的平均分數，尤其在揭示正式技能角色的任務上表現出色。

