
### Knowledge Graphs
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-07-10**|**Scalable Visual Pretraining for Language Intelligence**|Yiming Zhang et.al.|[2607.09657v1](http://arxiv.org/abs/2607.09657v1)|null|
|**2026-07-10**|**Lean-QIT: Towards a Formal Infrastructure for Quantum Information Theory**|Chengkai Zhu et.al.|[2607.09632v1](http://arxiv.org/abs/2607.09632v1)|null|
|**2026-07-10**|**Knowledge Graphs and Explainable AI as Complementary Resources for Urban Mining**|Jan Gronewald et.al.|[2607.09578v1](http://arxiv.org/abs/2607.09578v1)|null|
|**2026-07-10**|**Conceptual Networks for Cross-Linguistic Idiomatic Expressions:A Feature-Based Graph Approach**|Kiran Pala et.al.|[2607.09576v1](http://arxiv.org/abs/2607.09576v1)|null|
|**2026-07-10**|**What VGGT Knows About Overlap: Probing Geometric Foundation Models for Co-Visibility**|Filippo Ziliotto et.al.|[2607.09503v1](http://arxiv.org/abs/2607.09503v1)|null|
|**2026-07-10**|**Ceci n'est pas une pipe: AI systems as semantic abstractions**|Jade Alglave et.al.|[2607.09489v1](http://arxiv.org/abs/2607.09489v1)|null|
|**2026-07-10**|**How Does Bayesian Causal Discovery Fail? Characterising Structural Consequences in Linear Gaussian Networks under Latent Confounding**|Debargha Ghosh et.al.|[2607.09449v1](http://arxiv.org/abs/2607.09449v1)|null|
|**2026-07-10**|**Fictional Worldbuilding: Multi-Agent LLM Collaboration with Hierarchical Context Compression and Iterative Review**|Jingbo Chen et.al.|[2607.09403v1](http://arxiv.org/abs/2607.09403v1)|null|
|**2026-07-10**|**On-Device Adaptive Battery Power Prediction for Electric Vehicles**|Avik Bhatnagar et.al.|[2607.09400v1](http://arxiv.org/abs/2607.09400v1)|null|
|**2026-07-10**|**When Routes Run Out: Adversarial Co-Learning and Explainable Robustness in Quantum Repeater Networks**|Brennan Bell et.al.|[2607.09378v1](http://arxiv.org/abs/2607.09378v1)|null|
|**2026-07-10**|**DKCD: Domain Knowledge-Enhanced Causal Discovery from Unstructured Data**|Xin Li et.al.|[2607.09348v1](http://arxiv.org/abs/2607.09348v1)|null|
|**2026-07-10**|**Towards Detecting Inconsistencies in End-to-end Generated TODs**|Tiziano Labruna et.al.|[2607.09338v1](http://arxiv.org/abs/2607.09338v1)|null|
|**2026-07-10**|**LongMedBench: Benchmarking Medical Agents for Long-Horizon Clinical Decision-Making**|Yanzhen Chen et.al.|[2607.09322v1](http://arxiv.org/abs/2607.09322v1)|null|
|**2026-07-10**|**Toward Auditable AI Scientists: A Hypothesis Evolution Protocol for LLM Agents**|Izumi Takahara et.al.|[2607.09195v1](http://arxiv.org/abs/2607.09195v1)|null|
|**2026-07-10**|**Generative Communications: Overview, Technologies, and Trends**|Wenjun Zhang et.al.|[2607.09183v1](http://arxiv.org/abs/2607.09183v1)|null|
|**2026-07-10**|**Scoped Verification for Reliable Long-Horizon Agentic Context Evolution under Distribution Shift**|Dan C. Hsu et.al.|[2607.09175v1](http://arxiv.org/abs/2607.09175v1)|null|
|**2026-07-10**|**Augmenting Fundamental Analysis with Large Language Models: A RAG-Based System for Generating Investor Briefs**|Bartosz Ziółko et.al.|[2607.09121v1](http://arxiv.org/abs/2607.09121v1)|null|
|**2026-07-10**|**Integrating Large Language Models and Graph Convolutional Networks for Semi-Supervised Image Classification**|Camila Piscioneri Magalhães et.al.|[2607.09104v1](http://arxiv.org/abs/2607.09104v1)|null|
|**2026-07-10**|**A Coreset Selection Framework with Ensemble Aggregation for Image Classification**|Pedro Rocha Dantas et.al.|[2607.09100v1](http://arxiv.org/abs/2607.09100v1)|null|
|**2026-07-10**|**L-MAD: A Systematic Evaluation of Multi-Agent Debate Structures in Legal Reasoning**|Tan-Minh Nguyen et.al.|[2607.09099v1](http://arxiv.org/abs/2607.09099v1)|null|
|**2026-07-10**|**PRecG: Legal Precedent Retrieval with Graph Neural Networks and Rhetorical Role Segmentation**|Devanshu Verma et.al.|[2607.09094v1](http://arxiv.org/abs/2607.09094v1)|null|
|**2026-07-10**|**AgentKGV: Agentic LLM-RAG Framework with Two-Stage Training for the Fact Verification of Knowledge Graphs**|Yumin Heo et.al.|[2607.09092v1](http://arxiv.org/abs/2607.09092v1)|null|
|**2026-07-09**|**Model Agnostic Graph Prompt Learning for Crystal Property Prediction**|Shrimon Mukherjee et.al.|[2607.08996v1](http://arxiv.org/abs/2607.08996v1)|null|
|**2026-07-09**|**The Patchwork Problem in LLM-Generated Code**|Viraaji Mothukuri et.al.|[2607.08981v1](http://arxiv.org/abs/2607.08981v1)|null|
|**2026-07-09**|**Eluna: An Agentic LLM System for Automating Warehouse Operations with Reasoning and Task Execution**|Ning Liu et.al.|[2607.08960v1](http://arxiv.org/abs/2607.08960v1)|null|
|**2026-07-09**|**GATS: Graph-Augmented Tree Search with Layered World Models for Efficient Agent Planning**|Maureese Williams et.al.|[2607.08894v1](http://arxiv.org/abs/2607.08894v1)|null|
|**2026-07-09**|**Workflow as Knowledge: Semantic Persistence for LLM-Mediated Workflows**|Emanuele Quinto et.al.|[2607.08740v1](http://arxiv.org/abs/2607.08740v1)|null|
|**2026-07-09**|**DominoTree: Conditional Tree-Structured Drafting with Domino for Speculative Decoding**|Saw S. Lin et.al.|[2607.08642v2](http://arxiv.org/abs/2607.08642v2)|null|
|**2026-07-09**|**Towards Precision Therapy in Hepatocellular Carcinoma: A Clinical-Reasoning LLM for Risk Stratification and Treatment Guidance**|Peng Cui et.al.|[2607.08602v1](http://arxiv.org/abs/2607.08602v1)|null|
|**2026-07-09**|**The Context Access Divide: Interaction-Level Architecture as a Complementary Dimension of Agentic Inequality**|Masahiro Fujita et.al.|[2607.08495v1](http://arxiv.org/abs/2607.08495v1)|null|
|**2026-07-09**|**Drift-Aware Temporal Graph Rewiring (DATGR) for Adaptive Semantic Modeling in Biomedical Text**|Bharathwaj Vijayakumar et.al.|[2607.08490v1](http://arxiv.org/abs/2607.08490v1)|null|
|**2026-07-09**|**Game Theory Driven Multi-Agent Framework Mitigates Language Model Hallucination**|Runzhe Liu et.al.|[2607.08403v1](http://arxiv.org/abs/2607.08403v1)|null|
|**2026-07-09**|**TRACE: A Two-Channel Robust Attribution Watermark via Complementary Embeddings for LLM-Agent Trajectories**|Zheng Gao et.al.|[2607.08400v1](http://arxiv.org/abs/2607.08400v1)|null|
|**2026-07-09**|**Towards Mechanistically Understanding Why Memorized Knowledge Fails to Generalize in Large Language Model Finetuning**|Lu Dai et.al.|[2607.08393v1](http://arxiv.org/abs/2607.08393v1)|null|
|**2026-07-09**|**Large-Language-Models-as-a-Judge in Theory-Agnostic Adaptive Metric-Alignment for Prototypical Networks in Personality Recognition**|Jing Jie Tan et.al.|[2607.08374v1](http://arxiv.org/abs/2607.08374v1)|null|
|**2026-07-09**|**Self-Adaptive Anomaly Detection with Reinforcement Learning and Human Feedback in Connected Vehicles**|Matthias Weiß et.al.|[2607.08373v1](http://arxiv.org/abs/2607.08373v1)|null|
|**2026-07-09**|**FedOPAL: One-Shot Federated Learning via Analytic Visual Prompt Tuning**|Lingyu Qiu et.al.|[2607.08368v1](http://arxiv.org/abs/2607.08368v1)|null|
|**2026-07-09**|**TypeProbe: Recovering Type Representations from Hidden States of Pre-trained Code Models**|Giuliano Gorgone et.al.|[2607.08339v1](http://arxiv.org/abs/2607.08339v1)|null|
|**2026-07-09**|**XALPHA: A Memory-Driven AI Quant Researcher for Hypothesis-to-Code Alpha Discovery**|Fengyuan Liu et.al.|[2607.08332v1](http://arxiv.org/abs/2607.08332v1)|null|
|**2026-07-09**|**ArtMine: Discovering and Formalizing Artistic Processes**|Kaustubh Kumar et.al.|[2607.08331v1](http://arxiv.org/abs/2607.08331v1)|null|
|**2026-07-09**|**From Legacy Documentation to OSCAL: An MCP-Based Agent Pipeline for Threat-Informed Continuous Compliance in Critical Infrastructure**|Lea Roxanne Muth et.al.|[2607.08288v1](http://arxiv.org/abs/2607.08288v1)|null|
|**2026-07-09**|**PolyUQuest: Verifiable Structure-Aware Web RAG over Heterogeneous Graphs**|Ying Liu et.al.|[2607.08269v1](http://arxiv.org/abs/2607.08269v1)|null|
|**2026-07-09**|**Compete Then Collaborate: Frontier AI Teachers Build a Verifiable Curriculum to Improve a Coding Student Beyond Imitation**|Miseong Shawn Kim et.al.|[2607.08255v1](http://arxiv.org/abs/2607.08255v1)|null|
|**2026-07-09**|**SQuaD-SQL: Efficient Text-to-SQL with Small Language Models via LLM-Guided Knowledge Distillation**|Wangyu Wu et.al.|[2607.08161v1](http://arxiv.org/abs/2607.08161v1)|null|
|**2026-07-09**|**Answer Set Programming Energised! End-to-End Neurosymbolic Reasoning and Learning with ASP and Energy Based Models**|Jakob Suchan et.al.|[2607.08136v1](http://arxiv.org/abs/2607.08136v1)|null|
|**2026-07-09**|**COALA: Robust Contextualized Speech-augmented Language Modeling for ASR via Contrastive Regularizer and Biasing Score Estimation**|Jhih-Rong Guo et.al.|[2607.08117v1](http://arxiv.org/abs/2607.08117v1)|null|
|**2026-07-09**|**PARA-PV: Physics-Aware Retrieval-Augmented PV Prediction Based on Frozen Foundation Model and Distribution Shift Correction**|Hang Fan et.al.|[2607.08079v1](http://arxiv.org/abs/2607.08079v1)|null|
|**2026-07-09**|**Holographic Neural PCFG for Unsupervised Parsing**|Ryosuke Yamaki et.al.|[2607.08063v1](http://arxiv.org/abs/2607.08063v1)|null|
|**2026-07-09**|**DKDNet: Dual Knowledge and Data-Driven Network for Cross-Domain Automatic Modulation Classification**|Shuang Wang et.al.|[2607.08031v1](http://arxiv.org/abs/2607.08031v1)|null|
|**2026-07-09**|**Concretized Proposition Prompting Resolves Composition-Knowledge Dichotomy in Large Language Models**|Changhun Lee et.al.|[2607.08018v1](http://arxiv.org/abs/2607.08018v1)|null|
|**2026-07-09**|**Can We Trust LLM's Logic? Quantifying Uncertainty, Coherence, and Robustness via a Graph-Based Framework**|Riccardo Revalor et.al.|[2607.08017v1](http://arxiv.org/abs/2607.08017v1)|null|
|**2026-07-09**|**Reaction-network reasoning with frontier models for experimentally confirmed catalyst-selectivity hypotheses**|Sutanay Choudhury et.al.|[2607.08003v1](http://arxiv.org/abs/2607.08003v1)|null|
|**2026-07-08**|**path_boost: A Python Package for Interpretable Graph-Level Prediction using Path-Based Gradient Boosting**|Claudio Meggio et.al.|[2607.07935v1](http://arxiv.org/abs/2607.07935v1)|null|
|**2026-07-08**|**Multimodal Unlearning Across Vision, Language, Video, and Audio: Survey of Methods, Datasets, and Benchmarks**|Nobin Sarwar et.al.|[2607.07907v1](http://arxiv.org/abs/2607.07907v1)|null|
|**2026-07-08**|**Mechanistic Interpretability of LLM Jailbreaks via Internal Attribution Graphs**|Anupam Wagle et.al.|[2607.07903v1](http://arxiv.org/abs/2607.07903v1)|null|
|**2026-07-08**|**A Graph Neural Network Model for Real-Time Gesture Recognition Based on sEMG Signals**|Pragatheeswaran Vipulanandan et.al.|[2607.07850v1](http://arxiv.org/abs/2607.07850v1)|null|
|**2026-07-08**|**VectorizationLLM: Smart Vectorization Based AI Assistant**|Ryan Duke et.al.|[2607.07846v1](http://arxiv.org/abs/2607.07846v1)|null|
|**2026-07-08**|**Co-LMLM: Continuous-Query Limited Memory Language Models**|Yair Feldman et.al.|[2607.07707v1](http://arxiv.org/abs/2607.07707v1)|null|
|**2026-07-08**|**From Noisy Traces to Root Causes: Structural Trajectory Analysis and Causal Extraction for Agent Optimization**|Ying Chang et.al.|[2607.07702v1](http://arxiv.org/abs/2607.07702v1)|null|
|**2026-07-08**|**SkillCenter: A Large-Scale Source-Grounded Skill Library for Autonomous AI Agents**|Tianming Sha et.al.|[2607.07676v1](http://arxiv.org/abs/2607.07676v1)|null|
|**2026-07-08**|**Graph-Regularized Deep Learning for EEG-Based Emotion Recognition with Psychologically-Grounded Label Structure**|Dongyang Kuang et.al.|[2607.07773v1](http://arxiv.org/abs/2607.07773v1)|null|
|**2026-07-08**|**CARLA-GS: Decoupling Representation, Reasoning, and Physics Simulation for Autonomous Driving Corner-Case Synthesis**|Kaicong Huang et.al.|[2607.07601v1](http://arxiv.org/abs/2607.07601v1)|null|
|**2026-07-08**|**Collaborative Synthetic Data Generation for Knowledge Transfer in Federated Learning**|Maximilian Andreas Hoefler et.al.|[2607.07565v1](http://arxiv.org/abs/2607.07565v1)|null|
|**2026-07-08**|**Aligning Clinical Needs and AI Capabilities: A Survey on LLMs for Medical Reasoning**|Qi Peng et.al.|[2607.07761v1](http://arxiv.org/abs/2607.07761v1)|null|
|**2026-07-08**|**Stability of Flow Models for Graph Signals**|Martin Schmidt et.al.|[2607.07510v1](http://arxiv.org/abs/2607.07510v1)|null|
|**2026-07-08**|**TimEE: End-to-end Time Series Classification via In-Context Learning**|Jaris Küken et.al.|[2607.07500v1](http://arxiv.org/abs/2607.07500v1)|null|
|**2026-07-08**|**Where to Intervene? Benchmarking Fairness-Aware Learning on Differentially Private Synthetic Tabular Data**|Vinícius Gabriel Angelozzi et.al.|[2607.07471v1](http://arxiv.org/abs/2607.07471v1)|null|
|**2026-07-08**|**InductWave: Inductive Multi-Hop Logical Query Answering on Knowledge Graphs**|Mayank Kharbanda et.al.|[2607.07422v1](http://arxiv.org/abs/2607.07422v1)|null|
|**2026-07-08**|**When Prompts Ignore Structure: Graph-Based Attribute Reasoning for Calibrated VLMs**|Tanay Sodha et.al.|[2607.07395v1](http://arxiv.org/abs/2607.07395v1)|null|
|**2026-07-08**|**TF-Engram: A Train-Free Engram with SSD-Backed Memory for Large Language Models**|Yutang Ma et.al.|[2607.07388v1](http://arxiv.org/abs/2607.07388v1)|null|
|**2026-07-08**|**HumAIN: Human-Aware Implicit Social Robot Navigation**|Daeun Song et.al.|[2607.07357v1](http://arxiv.org/abs/2607.07357v1)|null|
|**2026-07-08**|**Hypergraph Neural Stochastic Diffusion: An SDE Framework for Uncertainty Estimation**|Zhiheng Zhou et.al.|[2607.07330v1](http://arxiv.org/abs/2607.07330v1)|null|
|**2026-07-08**|**CarbonCLIP: Enhance Carbon Prediction from Satellite Imagery via Integrated Street-View Semantics and Temporal Context Training**|Zeru Yang et.al.|[2607.07292v1](http://arxiv.org/abs/2607.07292v1)|null|
|**2026-07-08**|**Understanding Interpretation Difficulty in Harmful Online Communication: Insights from Cybercrime Communities**|Tomohiro Okatsu et.al.|[2607.07277v1](http://arxiv.org/abs/2607.07277v1)|null|
|**2026-07-08**|**DiPhon: Diffusion on Graphons for Scalable Graph Generation**|Sergio Rozada et.al.|[2607.07232v1](http://arxiv.org/abs/2607.07232v1)|null|
|**2026-07-08**|**Architecture Generalization with MetaNCA**|Meet Barot et.al.|[2607.07743v1](http://arxiv.org/abs/2607.07743v1)|null|
|**2026-07-08**|**Navigating Hierarchy: Hyperbolic Learning on Brain Graphs for Disorder Diagnosis**|Yapeng Li et.al.|[2607.07077v1](http://arxiv.org/abs/2607.07077v1)|null|
|**2026-07-08**|**Behavior Leverage Imbalance in Multi-Teacher On-Policy Distillation**|Jiabin Shen et.al.|[2607.07050v1](http://arxiv.org/abs/2607.07050v1)|null|
|**2026-07-08**|**Riemannian Geometry for Pre-trained Language Model Embeddings**|Szczepan Konior et.al.|[2607.07047v2](http://arxiv.org/abs/2607.07047v2)|null|
|**2026-07-08**|**Latent graph encoding of multimodal neuroimaging features with generative AI architectures**|Ishaan Batta et.al.|[2607.07027v1](http://arxiv.org/abs/2607.07027v1)|null|
|**2026-07-08**|**Physics-guided spatiotemporal neural models for fuel density prediction**|Tolga Caglar et.al.|[2607.06999v1](http://arxiv.org/abs/2607.06999v1)|null|
|**2026-07-08**|**Large Behavior Model: A Promptable Digital Twin of the Retail Customer**|Wachiravit Modecrua et.al.|[2607.06993v2](http://arxiv.org/abs/2607.06993v2)|null|
|**2026-07-08**|**Comprehensive Evaluation of Large Language Model Responses: A Multi-Factor Scoring System**|Yiming Gai et.al.|[2607.06940v1](http://arxiv.org/abs/2607.06940v1)|null|
|**2026-07-07**|**LLMs Silently Correct African American English: Auditing and Mitigating Dialect Bias via Activation Steering**|Huan Wu et.al.|[2607.06845v1](http://arxiv.org/abs/2607.06845v1)|null|
|**2026-07-07**|**When Agents Go Rogue: Activation-Based Detection of Malicious Behaviors in Multi-Agent Systems**|Haowen Xu et.al.|[2607.06807v1](http://arxiv.org/abs/2607.06807v1)|null|
|**2026-07-07**|**Enhancing deep learning models for time series classification via knowledge distillation**|Javidan Abdullayev et.al.|[2607.06796v1](http://arxiv.org/abs/2607.06796v1)|null|
|**2026-07-07**|**A Continual Learning Framework for Adaptive Control of Modular Soft Robots**|Nilay Kushawaha et.al.|[2607.06740v1](http://arxiv.org/abs/2607.06740v1)|null|
|**2026-07-07**|**Vision Language Action (VLA) Models for Unmanned Aerial Robotics and Bimanual Manipulation: A Review**|Inkyu Sa et.al.|[2607.06706v1](http://arxiv.org/abs/2607.06706v1)|null|
|**2026-07-07**|**SPEAR: A Simulator for Photorealistic Embodied AI Research**|Mike Roberts et.al.|[2607.06701v1](http://arxiv.org/abs/2607.06701v1)|null|
|**2026-07-07**|**Digital Fragmentation and Generative AI Use Across 103 Million Application Events**|Sumer S. Vaid et.al.|[2607.06681v1](http://arxiv.org/abs/2607.06681v1)|null|
|**2026-07-07**|**Graph Convolutional Attention: A Spectral Perspective on Graph Denoising and Diffusion**|Shervin Khalafi et.al.|[2607.06546v1](http://arxiv.org/abs/2607.06546v1)|null|
|**2026-07-07**|**Hierarchical Acoustic-Semantic Modeling: Modality Separation and Semantic Coherence for Full-Duplex SLMs**|Zhenyu Liu et.al.|[2607.06540v2](http://arxiv.org/abs/2607.06540v2)|null|
|**2026-07-07**|**RSF-GLLM: Bridging the Semantic Gap in Multi-Hop Knowledge Graph QA via Recurrent Soft-Flow and Decoupled LLM Generation**|Sambaran Bandyopadhyay et.al.|[2607.06527v1](http://arxiv.org/abs/2607.06527v1)|null|
|**2026-07-07**|**AirflowAttack: Thermal-Airflow Adversarial Perturbations against Infrared Remote-Sensing Vision-Language Models**|Cong Su et.al.|[2607.06485v1](http://arxiv.org/abs/2607.06485v1)|null|
|**2026-07-07**|**Data Analysis in the Wild: Benchmarking Large Language Models Against Real-World Data Complexities**|So Hasegawa et.al.|[2607.06482v1](http://arxiv.org/abs/2607.06482v1)|null|
|**2026-07-07**|**Analysis-by-Proxy: Localization Signals in VLMs Operating as Condition Encoders**|Yoav Baron et.al.|[2607.06445v1](http://arxiv.org/abs/2607.06445v1)|null|
|**2026-07-07**|**TopoBrick: Agentic Topology Sampling of Exogenous Variables for Zero-Shot Building IoT Forecasting**|Xiachong Lin et.al.|[2607.06349v1](http://arxiv.org/abs/2607.06349v1)|null|
|**2026-07-07**|**Task Decomposition-Guided Reranking for Adaptive Agent Skill Retrieval**|Yanping Chen et.al.|[2607.06283v1](http://arxiv.org/abs/2607.06283v1)|null|
|**2026-07-07**|**Early Language Learning via Spreading Activation and Category Exploration in Complex Networks**|Salvatore Citraro et.al.|[2607.06258v1](http://arxiv.org/abs/2607.06258v1)|null|
|**2026-07-07**|**A toy framework for single and multi-agent human-AI curiosity ecosystems**|Ilya E. Monosov et.al.|[2607.06214v1](http://arxiv.org/abs/2607.06214v1)|null|

#### Abstracts
##### **Scalable Visual Pretraining for Language Intelligence**
2607.09657v1 by Yiming Zhang, Zhonghan Zhao, Wenwei Zhang, Haiteng Zhao, Tianyang Lin, Yunhua Zhou, Demin Song, Kuikun Liu, Haochen Ye, Haian Huang, Yuzhe Gu, Haijun Lv, Qipeng Guo, Bin Liu, Gaoang Wang, Kai Chen

The rapid progress of large foundation models has been driven predominantly by pretraining on large-scale text corpora. However, many forms of knowledge are conveyed through visual representations, where figures, typeset equations, and page layouts carry rich information that cannot be faithfully or completely captured by text alone. Yet current pretraining approaches discard these visual cues by converting visually rich sources, such as documents and web pages, into plain text for learning language intelligence. This paper challenges the default assumption that language models must be trained on text-only representations and shows that Visual Pretraining is a scalable learner for foundation model intelligence. To this end, we conduct a systematic study of unsupervised visual pretraining paradigms that directly leverage visual documents without text extraction. Across multiple backbones and benchmarks, visual pretraining on the same underlying corpora consistently outperforms text-only pretraining, offering an efficient pathway to scalable language intelligence.

摘要：大型基礎模型的快速進展主要是通過在大規模文本語料庫上進行預訓練來驅動的。<lb/>然而，許多形式的知識是通過視覺表現來傳達的，其中圖形、排版方程式和頁面佈局承載著豐富的信息，這些信息無法僅通過文本忠實或完全捕捉。<lb/>然而，當前的預訓練方法通過將視覺豐富的來源（如文件和網頁）轉換為純文本來學習語言智能，從而忽略了這些視覺線索。<lb/>本文挑戰了語言模型必須僅在文本表示上進行訓練的默認假設，並顯示視覺預訓練是一種可擴展的基礎模型智能學習者。<lb/>為此，我們對無監督視覺預訓練範式進行了系統研究，這些範式直接利用視覺文件而不進行文本提取。<lb/>在多個骨幹和基準測試中，對相同底層語料庫的視覺預訓練始終優於僅基於文本的預訓練，提供了一條通往可擴展語言智能的高效途徑。

##### **Lean-QIT: Towards a Formal Infrastructure for Quantum Information Theory**
2607.09632v1 by Chengkai Zhu, Ziao Tang, Guocheng Zhen, Yimeng Cao, Yusheng Zhao, Ranyiliu Chen, Xuanqiang Zhao, Lei Zhang, Xin Wang

Quantum information theory (QIT) characterizes the capabilities and fundamental limits of quantum information processing, underpinning quantum communication, computation, and error correction. Formalizing its coding theorems requires connecting finite-block protocols, analytic inequalities, and asymptotic limits within a unified machine-checked framework. Existing developments, however, lack a reusable operational layer that defines codes, error criteria, achievable rates, and capacities independently of their information-theoretic characterizations. In this work, we present LeanQIT, a Lean 4 library for finite-dimensional QIT. It provides composable, kernel-checked interfaces for quantum states and channels, source and channel codes, finite-block performance criteria, hypothesis testing, one-shot quantities, and asymptotic rate constructions. Using this infrastructure, we formalize Schumacher's quantum source-coding theorem, the Holevo--Schumacher--Westmoreland classical-capacity theorem, and the entanglement-assisted classical-capacity theorem together with its strong converse. By separating operational definitions from analytic characterizations and exposing reusable achievability, converse, and asymptotic components, Lean-QIT provides a machine-readable foundation for formal QIT and a compositional knowledge substrate for emerging AI-assisted formalization, automated proof search, and agentic reasoning in quantum information and computation.

摘要：量子資訊理論 (QIT) 描述了量子資訊處理的能力和基本限制，支撐著量子通訊、計算和錯誤更正。形式化其編碼定理需要在統一的機器檢查框架內連接有限區塊協議、分析不等式和漸近極限。然而，現有的發展缺乏一個可重用的操作層，該層獨立於其資訊理論特徵定義編碼、錯誤標準、可達速率和容量。在這項工作中，我們提出了 LeanQIT，一個用於有限維 QIT 的 Lean 4 庫。它提供了可組合的、內核檢查的量子狀態和通道、源和通道編碼、有限區塊性能標準、假設檢驗、一擊量和漸近速率構造的介面。利用這一基礎設施，我們形式化了舒馬赫的量子源編碼定理、霍雷沃-舒馬赫-韋斯特莫蘭的經典容量定理，以及糾纏輔助的經典容量定理及其強對偶。通過將操作定義與分析特徵分開，並揭示可重用的可達性、對偶和漸近組件，Lean-QIT 為正式的 QIT 提供了一個機器可讀的基礎，並為新興的 AI 輔助形式化、自動證明搜索和在量子資訊和計算中的代理推理提供了一個組合知識基底。

##### **Knowledge Graphs and Explainable AI as Complementary Resources for Urban Mining**
2607.09578v1 by Jan Gronewald, Andreas Emrich, Nijat Mehdiyev

Pre-demolition assessment, the regulated audit process at the heart of urban mining, is an information process in which AI support must serve qualified auditors who remain accountable for the decisions taken. The relevant unit of value is not prediction accuracy alone, but the defensibility of the supported decisions: their legibility, plausibility, sourcing, and contestability. Explainable AI techniques and domain knowledge graphs each address parts of this requirement, and existing taxonomies have catalogued their integration. The literature is descriptively rich but structurally under-specified: what remains less developed is a structural account of why specific integrations produce artefacts neither resource can provide alone. This paper offers a complementarity-theoretic interpretation grounded in the IS resource-based tradition. We propose four consolidated KG-XAI integration modes (Lifting, Constraining, Typing, and Revising), each defined as a typed operation over XAI artefacts and knowledge-graph substrate structures. Each mode unlocks a distinct property of defensibility and contributes to the kind of regulatory artefact pre-demolition assessment demands. A fire-door example from the urban-mining process illustrates the modes using the W3C Linked Building Data stack and valuation extensions.

摘要：預拆除評估，作為城市採礦核心的受規範審核過程，是一個資訊過程，其中 AI 支持必須服務於對所做決策負責的合格審核員。相關的價值單位不僅是預測準確性，而是所支持決策的可辯護性：它們的可讀性、可信性、來源和可爭議性。可解釋的 AI 技術和領域知識圖譜各自解決了這一要求的部分，而現有的分類法已經對它們的整合進行了編目。文獻在描述上非常豐富，但在結構上則不夠明確：尚未發展出具體的結構性說明，為何特定的整合會產生單獨的資源無法提供的工件。本文提供了一種基於 IS 資源基礎傳統的互補性理論解釋。我們提出了四種整合模式的 KG-XAI（提升、約束、類型化和修訂），每種模式都被定義為對 XAI 工件和知識圖譜基礎結構的類型化操作。每種模式解鎖了可辯護性的獨特屬性，並有助於滿足預拆除評估所需的監管工件。來自城市採礦過程的防火門示例使用 W3C 連結建築數據堆疊和估值擴展來說明這些模式。

##### **Conceptual Networks for Cross-Linguistic Idiomatic Expressions:A Feature-Based Graph Approach**
2607.09576v1 by Kiran Pala, Punam Silu, Lixun Yu

We present an interpretable network-based framework for representing idiomatic and figurative meaning across eight typologically diverse languages, totaling 160 conventional expressions, the large majority of which are idiomatic. Each expression is annotated with binary conceptual features (containment, concealment, emotional, social, etc.) derived from cognitive-linguistic theory, and pairwise Jaccard similarities define a weighted graph. Community detection reveals that idioms cluster by conceptual schema rather than by language, producing a structure consistent with cognitive-linguistic predictions. The conceptual network captures unique semantic information not present in distributional embeddings, can be scaled via automatic annotation with LLMs, improves downstream idiom detection, and remains robust when enriched with corpus frequencies. Cross-lingual transfer experiments show that conceptual proximity alone can identify acceptable translation equivalents across five language families, with substantial gains over embedding-based baselines. Ablation studies demonstrate that all three feature dimensions -- schemas, roles, and valence -- contribute non-redundantly to both the network's organizational properties and its performance on idiom detection, and that specific graph-derived signals (community membership, neighbor similarity) are particularly informative. The framework offers an interpretable, cross-linguistically stable representation of idiomatic meaning, combining theoretical grounding with practical utility.

摘要：我們提出了一個可解釋的基於網絡的框架，用於表示八種類型學上多樣的語言中的成語和比喻意義，共計160個常用表達式，其中絕大多數為成語。每個表達式都附有基於認知語言學理論衍生的二元概念特徵（包含、隱藏、情感、社會等），並且成對的Jaccard相似度定義了一個加權圖。社群檢測顯示，成語是根據概念架構而非語言聚集，產生的結構與認知語言學的預測一致。這個概念網絡捕捉了在分佈式嵌入中不存在的獨特語義信息，可以通過與大型語言模型的自動標註進行擴展，改善下游的成語檢測，並在用語料頻率豐富時保持穩健。跨語言轉移實驗顯示，僅憑概念接近性就能識別五個語言家族中的可接受翻譯對應，並且相較於基於嵌入的基準有顯著提升。消融研究表明，所有三個特徵維度——架構、角色和價值——對於網絡的組織特性和其在成語檢測上的表現均有非冗餘的貢獻，並且特定的圖形衍生信號（社群成員資格、鄰居相似度）特別具有信息價值。該框架提供了一個可解釋的、跨語言穩定的成語意義表示，結合了理論基礎和實用性。

##### **What VGGT Knows About Overlap: Probing Geometric Foundation Models for Co-Visibility**
2607.09503v1 by Filippo Ziliotto, Luciano Serafini, Lamberto Ballan, Tommaso Campari

A fundamental challenge in 3D reconstruction and robotic localization is co-visibility: determining which image pairs share overlapping visible surfaces, particularly in scenarios with minimal overlap. We demonstrate that VGGT implicitly encodes co-visibility as an emergent behavior: without any supervision for this task, its internal representations exhibit a clear hierarchical structure mirroring that of large language models, i.e. early layers build a 3D-aware scene representation, while late layers act as dedicated co-visibility reasoners. In particular, we identify layer L17 as a negative anchor that consistently routes non-co-visible pairs for this backbone, regardless of the evaluation setting, providing task-grounded evidence of layer specialization in a geometry-grounded foundation model. Building on this, we introduce Co-VGGT, which freezes VGGT and trains only a lightweight layer-wise mixture-of-experts head (less than 7.5M parameters) to classify co-visibility from RGB alone, treating each layer as a specialized expert whose geometric abstraction is adaptively weighted per input pair. On the Co-VisiON benchmark, Co-VGGT surpasses the human annotation baseline and improves over prior work by more than 25% pairwise and 10% multiview. Pairwise predictions are well-calibrated (ECE=0.030), enabling direct use as edge weights in visibility graphs for downstream SfM and SLAM pipelines without post-hoc correction. Code and data are available.

摘要：一個在 3D 重建和機器人定位中的基本挑戰是共可見性：確定哪些圖像對共享重疊的可見表面，特別是在重疊最小的情境中。我們展示了 VGGT 以一種新興行為隱式編碼共可見性：在這項任務上沒有任何監督的情況下，其內部表現顯示出明確的層次結構，與大型語言模型的結構相呼應，即早期層構建一個 3D 感知的場景表示，而後期層則作為專門的共可見性推理者。特別地，我們識別出 L17 層作為一個負錨點，無論評估設置如何，始終將非共可見對路由到這個主幹，提供了基於任務的證據，證明了幾何基礎模型中的層專業化。在此基礎上，我們介紹了 Co-VGGT，該模型凍結 VGGT，僅訓練一個輕量級的層級混合專家頭（少於 7.5M 參數）來僅根據 RGB 分類共可見性，將每層視為一個專門的專家，其幾何抽象根據每對輸入自適應加權。在 Co-VisiON 基準上，Co-VGGT 超越了人類標註基線，並在成對和多視圖上分別提高了超過 25% 和 10%。成對預測經過良好校準（ECE=0.030），使其能夠直接用作可見性圖中的邊緣權重，用於下游的 SfM 和 SLAM 流程，而無需事後修正。代碼和數據均可用。

##### **Ceci n'est pas une pipe: AI systems as semantic abstractions**
2607.09489v1 by Jade Alglave, Patrick Cousot

An AI system's output is not the fact or world state it appears to describe, but rather an engineered representation. We propose a semantic framework to describe AI systems, to be able to examine the correctness of such representations. To do so, we distinguish what is justified by accepted domain knowledge, what reference sources say, and what the system can currently use. This allows us to give precise definitions to common failures: extrapolation, refuted or unsupported assertion, sources versus knowledge mismatch, stale or refuted source, added hypotheses, unsupported use... We hope our framework gives a useful vocabulary for specifying and checking AI systems whose outputs, citations, tool calls, and world-changing actions must be justified by reliable claims and explicit authority rather than apparent fluency.

摘要：AI 系統的輸出並不是它所描述的事實或世界狀態，而是一種工程化的表徵。<lb/>我們提出了一個語義框架來描述 AI 系統，以便能夠檢查這些表徵的正確性。<lb/>為此，我們區分了被接受的領域知識所證明的內容、參考來源所說的內容，以及系統目前可以使用的內容。<lb/>這使我們能夠對常見的失誤給出精確的定義：外推、被駁斥或未被支持的斷言、來源與知識不匹配、過時或被駁斥的來源、添加的假設、未被支持的使用……<lb/>我們希望我們的框架能提供一個有用的詞彙，以便指定和檢查那些其輸出、引用、工具調用和改變世界的行動必須由可靠的主張和明確的權威來證明，而不是表面的流暢性。

##### **How Does Bayesian Causal Discovery Fail? Characterising Structural Consequences in Linear Gaussian Networks under Latent Confounding**
2607.09449v1 by Debargha Ghosh, Silja Renooij, Anna Kononova

Bayesian causal discovery is widely used for its ability to quantify epistemic uncertainty over directed acyclic graphs (DAGs) through posterior inference. However, its behaviour under latent confounding remains poorly understood, as existing work typically notes that confounding breaks identifiability without characterising how the posterior distribution over DAGs responds. In this work, we analyse posterior behaviour under latent confounding in linear Gaussian causal models, focusing on additive latent confounding between exactly two observed variables. We derive a critical correlation threshold above which the score function favours graphs with a spurious edge between the confounded variables, and show that this threshold decreases with sample size -- more data lowers the correlation required for the spurious edge to be favoured. Beyond this threshold, we characterize two distinct posterior failure regimes determined by the local structure around the confounded variables. Our findings are supported by exact posterior computations on multiple graph structures, demonstrating both the predicted failure regimes.

摘要：貝葉斯因果發現因其能夠通過後驗推斷量化有向無環圖（DAGs）上的認知不確定性而被廣泛使用。<lb/>然而，其在潛在混淆下的行為仍然不甚了解，因為現有的研究通常指出混淆會破壞可識別性，但未能描述後驗分佈對DAGs的反應。<lb/>在本研究中，我們分析了線性高斯因果模型中潛在混淆下的後驗行為，重點關注恰好兩個觀察變量之間的加性潛在混淆。<lb/>我們推導出一個關鍵的相關性閾值，超過該閾值後，得分函數偏好於混淆變量之間存在虛假邊的圖形，並顯示該閾值隨樣本大小的增加而降低——更多數據降低了虛假邊被偏好的所需相關性。<lb/>在這個閾值之外，我們描述了由混淆變量周圍的局部結構決定的兩個不同的後驗失效範疇。<lb/>我們的發現得到了對多個圖結構的精確後驗計算的支持，展示了預測的失效範疇。

##### **Fictional Worldbuilding: Multi-Agent LLM Collaboration with Hierarchical Context Compression and Iterative Review**
2607.09403v1 by Jingbo Chen, He Wang, Wei Yuan, Yuqiao Lai, Zhenyan Lu

Worldbuilding, the construction of coherent fictional worlds, is a foundational task in game design and literary creation. Large Language Models (LLMs) offer new possibilities for automated content generation, but their application to worldbuilding faces three challenges: context explosion that grows linearly with the building process, the tension between creative diversity and content consistency, and the absence of automated quality assurance. This paper presents AutoWorldBuilder, a multi-agent collaborative system that addresses these challenges through five integrated components: a structured concept network with conflict detection; a DAG-based hybrid batch scheduler that groups tasks by semantic locality; a four-layer context compression mechanism achieving approximately 90% token reduction; an iterative review system with specialized Auditor agents that improves proposal pass rates from 42% to over 85%; and a skill-driven agent architecture supporting zero-code extension with differentiated temperature configuration. Two experiments across 20 diverse worldbuilding tasks, using GPT-OSS 120B and DeepSeek v3.2 as LLM backends, demonstrate a 95.0% success rate. The system generated 56-103 self-consistent concepts per world in 18-31 minutes with zero-conflict delivery. The architectural patterns validated here, including layer-as-budget compression, semantic-locality scheduling, and separation of generation and review, transfer to the broader class of knowledge-intensive, multi-agent LLM applications.

摘要：世界建構，即構建一致的虛構世界，是遊戲設計和文學創作中的基礎任務。大型語言模型（LLMs）為自動內容生成提供了新的可能性，但其在世界建構中的應用面臨三個挑戰：隨著建構過程線性增長的上下文爆炸、創意多樣性與內容一致性之間的緊張關係，以及缺乏自動質量保證。本文介紹了AutoWorldBuilder，一個多代理協作系統，通過五個集成組件來解決這些挑戰：一個具有衝突檢測的結構化概念網絡；一個基於DAG的混合批量調度器，根據語義局部性對任務進行分組；一個四層上下文壓縮機制，實現約90%的標記減少；一個具有專門審計代理的迭代審查系統，將提案通過率從42%提高到超過85%；以及一個支持零代碼擴展的技能驅動代理架構，具有差異化的溫度配置。在20個多樣化的世界建構任務中進行的兩次實驗，使用GPT-OSS 120B和DeepSeek v3.2作為LLM後端，顯示出95.0%的成功率。該系統在18-31分鐘內為每個世界生成56-103個自洽概念，並實現零衝突交付。這裡驗證的架構模式，包括層作為預算壓縮、語義局部性調度以及生成與審查的分離，轉移到更廣泛的知識密集型多代理LLM應用類別中。

##### **On-Device Adaptive Battery Power Prediction for Electric Vehicles**
2607.09400v1 by Avik Bhatnagar, Anton Paule, Tobias Schuermann, Sebastian Reiter, Oliver Bringmann

Adaptive power management in Electric Vehicles (EVs) requires accurate power prediction. Although deep learning models have emerged as highly effective for time-series forecasting in this domain, their performance is prone to degradation when exposed to data with distributions different from the training data. We introduce a novel approach that enables on-device learning in resource-constrained EV systems to continuously adapt pretrained battery prediction models to new, unseen data. We leverage existing pretrained models by transforming them into adaptable versions that retain critical hyperparameter knowledge from their initial training. We comprehensively investigate both online and offline model adaptation strategies. Our results demonstrate significant improvements in forecasting performance across various models and time horizons, achieving mean absolute error reductions of up to 7.49\% and 14.88\% with online and offline adaptation techniques, respectively. This study highlights the substantial benefit of on-device adaptation, resulting in enhanced battery power predictions than unadapted model deployments in real-world EV scenarios.

摘要：適應性電力管理在電動車（EV）中需要準確的電力預測。儘管深度學習模型已被證明在此領域的時間序列預測中非常有效，但當面對與訓練數據分佈不同的數據時，它們的性能容易下降。我們提出了一種新穎的方法，使資源受限的電動車系統能夠在設備上進行學習，持續地將預訓練的電池預測模型適應於新的、未見過的數據。我們通過將現有的預訓練模型轉換為可適應的版本來利用它們，這些版本保留了其初始訓練中的關鍵超參數知識。我們全面研究了在線和離線模型適應策略。我們的結果顯示，在各種模型和時間範圍內，預測性能顯著改善，通過在線和離線適應技術，平均絕對誤差分別降低了高達7.49\%和14.88\%。這項研究突顯了設備上適應的重大好處，使得電池電力預測比在現實世界電動車場景中未適應的模型部署更為精確。

##### **When Routes Run Out: Adversarial Co-Learning and Explainable Robustness in Quantum Repeater Networks**
2607.09378v1 by Brennan Bell, Inti Gabriel Mendoza Estrada, Andreas Trügler, Paul Erker

We study an adversarial bandit problem for entanglement-based quantum-network routing over a modest graph corpus. Alice selects an end-to-end repeater route for an Ekert-91 protocol (E91) representing her move, while Eve selects an attack surface, either edge intercept--resend or repeater memory degradation. Payoffs are drawn from cached SeQUeNCe-simulated E91 transcripts, and Alice accepts a turn when the finite-sample statistic violates the Clauser-Horne-Shimony-Holt (CHSH) bound. Performing adversarial co-learning across 50 structured topologies, we find that learned retention tracks a full-matrix minimax reference closely (Pearson $r=0.99$): under a one-surface Eve action model, bottleneck families have zero retention, while non-bottleneck families follow a $1-1/N$ coverage principle. We then fit decision-tree explanation models to graph-, attack-, and route-level topology-corpus targets and report their faithfulness. Finally, we construct prompt records for local language models to summarize the tree evidence, resulting in an open-source explanation workflow for quantum-repeater network games.

摘要：我們研究了一個對抗性賭徒問題，針對基於糾纏的量子網路路由，涉及一個適度的圖形語料庫。愛麗絲為她的行動選擇了一條端到端的中繼路徑，這條路徑是Ekert-91協議（E91）的代表，而伊芙則選擇了一個攻擊面，無論是邊緣攔截--重發還是中繼記憶衰退。收益來自緩存的SeQUeNCe模擬E91記錄，當有限樣本統計違反Clauser-Horne-Shimony-Holt（CHSH）界限時，愛麗絲接受這一回合。在50個結構化拓撲中進行對抗性共同學習，我們發現學習的保留率與完整矩陣的最小最大參考值密切跟蹤（Pearson $r=0.99$）：在單一表面伊芙行動模型下，瓶頸家族的保留率為零，而非瓶頸家族遵循$1-1/N$覆蓋原則。然後，我們為圖形、攻擊和路由層級的拓撲語料庫目標擬合決策樹解釋模型，並報告其忠實性。最後，我們為本地語言模型構建提示記錄，以總結樹證據，從而形成一個開源的量子中繼網路遊戲解釋工作流程。

##### **DKCD: Domain Knowledge-Enhanced Causal Discovery from Unstructured Data**
2607.09348v1 by Xin Li, Jin Li, Shoujin Wang, Kun Yu, Fang Chen

Causal discovery from unstructured data is a challenging yet underexplored task in high-expertise domains such as healthcare, finance, and education. Existing methods typically leverage the general knowledge of large language models (LLMs) to identify causal factors from unstructured data and annotate them into structured data for causal graph construction. However, they remain limited by two key challenges (CHs): (CH1) insufficient identification of latent factors, which are implicit in the data yet essential for causal discovery, due to the lack of domain-specific knowledge; and (CH2) unreliable factor annotation, caused by the lack of domain-grounded reasoning, which propagates errors to the resulting causal graphs. To address these challenges, we introduce a novel Domain Knowledge-enhanced Causal Discovery framework (DKCD) for causal discovery from unstructured data in high-expertise domains with three interconnected components: (1) Knowledge Mining: It retrieves relevant domain knowledge based on observable factors to support subsequent causal reasoning. (2) Knowledge-guided Causal Reasoning: Reasoning with relevant knowledge, it discovers latent causal factors to address CH1 and generates key causal clues for more accurate data annotation to address CH2. (3) Causal Structure Discovery: It constructs the final causal graphs based on a more complete factor set and accurate annotations. Experiments on two domain-specific datasets show that DKCD significantly improves both causal factor identification and causal graph construction.

摘要：因果發現從非結構化數據中是一項具有挑戰性但尚未深入探討的任務，尤其在醫療、金融和教育等高專業領域。現有的方法通常利用大型語言模型（LLMs）的通用知識來識別非結構化數據中的因果因素，並將其註釋為結構化數據，以便構建因果圖。然而，它們受到兩個主要挑戰（CHs）的限制：（CH1）潛在因素的識別不足，這些因素在數據中是隱含的，但對於因果發現至關重要，這是由於缺乏特定領域的知識；以及（CH2）不可靠的因素註釋，這是由於缺乏基於領域的推理，導致錯誤傳播到最終的因果圖。為了解決這些挑戰，我們提出了一個新穎的增強領域知識的因果發現框架（DKCD），旨在從高專業領域的非結構化數據中進行因果發現，該框架包含三個相互關聯的組件：（1）知識挖掘：根據可觀察因素檢索相關的領域知識，以支持後續的因果推理。（2）知識引導的因果推理：利用相關知識進行推理，發現潛在的因果因素以解決CH1，並生成關鍵的因果線索，以便進行更準確的數據註釋以解決CH2。（3）因果結構發現：根據更完整的因素集和準確的註釋構建最終的因果圖。在兩個特定領域的數據集上的實驗表明，DKCD顯著改善了因果因素的識別和因果圖的構建。

##### **Towards Detecting Inconsistencies in End-to-end Generated TODs**
2607.09338v1 by Tiziano Labruna, Giovanni Bonetta, Bernardo Magnini

Generative AI is profoundly transforming the core technologies behind conversational systems, shifting from component-based to end-to-end approaches. However, Large Language Models (LLMs) may still generate inconsistencies, a critical issue particularly in Task-Oriented Dialogues (TODs), where system responses must strictly adhere to information from a domain knowledge base (e.g., restaurants in a city). A single hallucination (e.g., suggesting a non-existent restaurant) can lead to severe task failures. We investigate a method for automatically detecting inconsistencies by conceptualizing TODs as a Constraint Satisfaction Problem (CSP), where variables represent dialogue segments referencing the conversational domain, and constraints among variables capture dialogue properties such as turn coherence and adherence to domain knowledge. We propose a pipeline that first identifies variables in a target dialogue and then applies a CSP solver to identify valid solutions. By comparing the target dialogue with valid variable assignments, we can detect inconsistencies and suggest minimal changes to ensure dialogue consistency. We demonstrate the high accuracy of the CSP-based approach in detecting inconsistencies, and provide a detailed analysis of our findings.

摘要：生成式人工智慧正在深刻改變對話系統背後的核心技術，從基於組件的方法轉向端到端的方法。<lb/>然而，大型語言模型（LLMs）仍可能產生不一致性，這在任務導向對話（TODs）中是一個關鍵問題，因為系統的回應必須嚴格遵循來自領域知識庫的信息（例如，城市中的餐廳）。<lb/>單一的幻覺（例如，建議一個不存在的餐廳）可能導致嚴重的任務失敗。<lb/>我們研究了一種通過將TODs概念化為約束滿足問題（CSP）來自動檢測不一致性的方法，其中變量代表引用對話領域的對話片段，變量之間的約束捕捉對話屬性，如回合一致性和對領域知識的遵循。<lb/>我們提出了一個管道，首先識別目標對話中的變量，然後應用CSP求解器來識別有效的解。<lb/>通過將目標對話與有效的變量分配進行比較，我們可以檢測不一致性並建議最小的變更以確保對話的一致性。<lb/>我們展示了基於CSP的方法在檢測不一致性方面的高準確性，並提供了我們發現的詳細分析。

##### **LongMedBench: Benchmarking Medical Agents for Long-Horizon Clinical Decision-Making**
2607.09322v1 by Yanzhen Chen, Zihan Xu, Xiaocheng Zhang, Zhiting Fan, Weiqi Zhai, Hongxia Xu, Zuozhu Liu

In this work, we introduce LongMedBench, a real-world EHR-based benchmark for long-horizon clinical decision-making. Prior evaluations of LLM-based medical agents have largely emphasized short-context knowledge QA and tool use. However, real-world medical care is inherently longitudinal, and clinicians must aggregate evidence across repeated visits, tests, and evolving treatments. Therefore, long-horizon interaction is essential for realistic assessment. LongMedBench is constructed via a reproducible pipeline that integrates MIMIC-IV admission records and clinical notes into time-series event streams and long-context memory datasets, enabling long-horizon, multi-session interactions between agents and a clinical environment. It comprises 335 patients, with 19.72 inpatient visits per patient on average and 44.91 medical events per visit. Guided by the long-horizon decision process, we propose an evaluation taxonomy with three suites: fact-based QA, temporal reasoning, and long-horizon decision-making. This taxonomy measures how agents understand and leverage historical patient information over extended horizons. Our experiments show that while recent LLMs can make good use of explicit timestamps, they have challenges in implicit time inference; The RAG and agent memory system can improve the performance of information retrieval tasks, but the performance of decision-making tasks is highly dependent on the model's immediate context.

摘要：在本研究中，我們介紹了 LongMedBench，一個基於真實世界電子健康紀錄（EHR）的長期臨床決策基準。先前對基於 LLM 的醫療代理的評估主要強調短期上下文的知識問答和工具使用。然而，真實世界的醫療護理本質上是長期的，臨床醫生必須在多次就診、檢測和不斷演變的治療中整合證據。因此，長期互動對於現實評估至關重要。LongMedBench 是通過一個可重現的流程構建的，該流程將 MIMIC-IV 入院紀錄和臨床筆記整合成時間序列事件流和長上下文記憶數據集，使代理與臨床環境之間能夠進行長期的多會話互動。它包含 335 名患者，每位患者平均有 19.72 次住院就診和每次就診 44.91 次醫療事件。在長期決策過程的指導下，我們提出了一個包含三個套件的評估分類法：基於事實的問答、時間推理和長期決策。這個分類法衡量代理如何理解和利用歷史患者信息以應對延長的時間範圍。我們的實驗顯示，儘管近期的 LLM 能夠很好地利用明確的時間戳，但在隱式時間推斷方面存在挑戰；RAG 和代理記憶系統可以改善信息檢索任務的性能，但決策任務的性能高度依賴於模型的即時上下文。

##### **Toward Auditable AI Scientists: A Hypothesis Evolution Protocol for LLM Agents**
2607.09195v1 by Izumi Takahara, Teruyasu Mizoguchi

Large language model (LLM) agents are increasingly expected to play a central role in AI-driven scientific discovery. Equipped with broad knowledge, flexible reasoning, and tool use, they have the potential to autonomously explore and solve scientific problems by repeatedly proposing hypotheses, testing them, and revising their beliefs in the light of the evidence. In current agents, however, these hypotheses, tests, and belief updates are buried in unstructured logs, and no mechanism lets the agent or the human researcher audit that process. Here we propose the Hypothesis Evolution Protocol (HEP), an agent harness that provides hypothesis generation, evaluation, and evolution as explicit, auditable operations. On materials-science research tasks, a HEP-equipped agent operates the hypothesis--test--evidence--belief cycle that planning-style agents lack, generalizes across research questions, and exploits the protocol more fully as the base LLM becomes more capable. These results mark a step toward auditable AI scientists, whose scientific reasoning can be inspected, verified, and built upon.

摘要：大型語言模型（LLM）代理人越來越被期望在人工智慧驅動的科學發現中扮演核心角色。<lb/>憑藉廣泛的知識、靈活的推理和工具使用能力，它們有潛力通過反覆提出假設、測試假設並根據證據修正信念，自主探索和解決科學問題。<lb/>然而，在當前的代理人中，這些假設、測試和信念更新都埋藏在非結構化的日誌中，且沒有任何機制讓代理人或人類研究者能夠審計該過程。<lb/>在此，我們提出了假設演變協議（HEP），這是一種代理人工具，提供假設生成、評估和演變作為明確的、可審計的操作。<lb/>在材料科學研究任務中，配備HEP的代理人運行假設--測試--證據--信念的循環，這是規劃風格的代理人所缺乏的，並且在研究問題之間進行泛化，隨著基礎LLM能力的提高，充分利用該協議。<lb/>這些結果標誌著朝向可審計的人工智慧科學家邁出了一步，他們的科學推理可以被檢查、驗證並加以構建。

##### **Generative Communications: Overview, Technologies, and Trends**
2607.09183v1 by Wenjun Zhang, Zhiyong Chen, Tong Wu, Guo Lu, Li Song, Feng Yang, Meixia Tao

The groundbreaking development of generative artificial intelligence (AI) is rapidly boosting the ability to generate content such as images and videos, reshaping communication paradigms. This article introduces generative communications (GenCom), a novel paradigm for 6G networks in which large AI models (LAMs) drive semantic understanding, reasoning, and content generation, embedding these into the communication process. Unlike traditional systems that strictly pursue accurate bit transmission, GenCom enables transmitters to convey only minimal yet sufficient information, while receivers leverage shared generative priors and knowledge bases to synthesize the intended output. Communication is thus redefined as controlled generation rather than data reproduction. We formalize the concept of GenCom, clarify its AI-native and generation-driven properties, and present its core mechanisms. A two-layer GenCom architecture supported by key enabling technologies is proposed, and analysis of four representative application scenarios demonstrates that GenCom offers ultra-efficient transmission, semantic-level robustness, and new network functions. Finally, we outline future research directions, including foundational theory and real-time processing, highlighting a promising pathway toward 6G networks.

摘要：開創性的生成式人工智慧（AI）發展迅速提升了生成圖像和視頻等內容的能力，重塑了通信範式。本文介紹了生成通信（GenCom），這是一種新穎的6G網絡範式，其中大型AI模型（LAMs）驅動語義理解、推理和內容生成，並將這些嵌入通信過程中。與傳統系統嚴格追求準確位元傳輸不同，GenCom使發送者能夠僅傳達最小但足夠的信息，而接收者則利用共享的生成先驗和知識庫來合成預期的輸出。因此，通信被重新定義為受控生成，而非數據重複。我們形式化了GenCom的概念，闡明了其AI原生和生成驅動的特性，並介紹了其核心機制。提出了一種由關鍵啟用技術支持的雙層GenCom架構，並對四個代表性應用場景的分析顯示，GenCom提供了超高效的傳輸、語義層級的穩健性和新的網絡功能。最後，我們概述了未來的研究方向，包括基礎理論和實時處理，突顯了通向6G網絡的有希望的途徑。

##### **Scoped Verification for Reliable Long-Horizon Agentic Context Evolution under Distribution Shift**
2607.09175v1 by Dan C. Hsu, Luke Lu

Deployed LLM agents rely on agentic context, the model-external textual control content assembled by an operational harness. In this work, the mutable component of that context is a persistent system-level instruction that is updated from operational experience while the model, tools, and harness remain fixed. Over long evolution horizons, flat-text maintenance makes verification increasingly difficult as accumulated instructions grow and interact. We propose Graph-Regularized Agentic Context Evolution (GRACE), which maintains the persistent instruction component as a typed semantic graph and validates proposed updates within the local typed neighborhoods of modified nodes. Accepted graph updates are reconstructed as incremental edits to the textual instruction checkpoint used at deployment. We evaluate GRACE within a fixed telecom agent harness derived from $τ^2$-bench under a controlled distribution-shift protocol. Across five independent replications, GRACE improves strict reliability, measured by pass^3, from the Gemini 2.5 Flash zero-shot value of 0.091 to 0.673$\pm$0.136 at the final checkpoint. This exceeds a Gemini 3.1 Pro zero-shot reference of 0.242 on the same held-out set, while the flat-text HCE baseline finishes at 0.191$\pm$0.051. These results identify two requirements for reliable long-horizon context evolution, a structural substrate that makes verification local and a consolidation mechanism that keeps accumulated instruction content usable.

摘要：部署的 LLM 代理依賴於代理上下文，即由操作性工具組裝的模型外部文本控制內容。在這項工作中，該上下文的可變組件是一個持久的系統級指令，該指令根據操作經驗進行更新，而模型、工具和工具組則保持固定。在長期演化的視野中，平面文本的維護使得驗證變得越來越困難，因為累積的指令不斷增長並相互作用。我們提出了圖正則化代理上下文演化（GRACE），它將持久的指令組件維護為一個類型化的語義圖，並在修改節點的本地類型化鄰域內驗證提議的更新。接受的圖更新被重建為在部署時使用的文本指令檢查點的增量編輯。我們在一個固定的電信代理工具中評估 GRACE，該工具源自於 $τ^2$-bench，並在受控的分佈轉移協議下進行。在五次獨立的複製中，GRACE 將嚴格可靠性（通過 pass^3 測量）從 Gemini 2.5 Flash 的零樣本值 0.091 提高到最終檢查點的 0.673$\pm$0.136。這超過了在同一保留集上 Gemini 3.1 Pro 的零樣本參考值 0.242，而平面文本 HCE 基準則以 0.191$\pm$0.051 結束。這些結果確定了可靠的長期上下文演化的兩個要求：一個使驗證本地化的結構基底，以及一個保持累積指令內容可用的整合機制。

##### **Augmenting Fundamental Analysis with Large Language Models: A RAG-Based System for Generating Investor Briefs**
2607.09121v1 by Bartosz Ziółko, Kacper Dobrzeniewski

In this study, we examine the opportunities brought by Large Language Models (LLMs) to various aspects of fundamental analysis of companies based on their reports as well as data and documents describing macroeconomic situation like GDP and inflation changes as well as documents filled to the U.S. Securities and Exchange Commission (SEC) which can be found in EDGAR. We were preprocessing those data and than sending via API to gpt-4o model in a Retrieval-Augmented Generation (RAG) like regime. We prepared as well a document describing an exemplar investor knowledge based on Kitchin cycles. We were scanning data important for analysis of 9 companies for 4 weeks. Using LLM we were producing automatic briefs about them. They were sent to nine participants who are individual investors to evaluate usefulness of such approach to data analysis.

摘要：在這項研究中，我們檢視大型語言模型（LLMs）為公司基本面分析帶來的機會，這些分析基於公司的報告以及描述宏觀經濟情況的數據和文件，例如GDP和通脹變化，以及提交給美國證券交易委員會（SEC）的文件，這些文件可以在EDGAR中找到。<lb/>我們對這些數據進行了預處理，然後通過API發送到gpt-4o模型，採用檢索增強生成（RAG）類似的模式。<lb/>我們還準備了一份描述基於基奇循環的示例投資者知識的文件。<lb/>我們掃描了9家公司重要的數據，持續了4週。<lb/>使用LLM，我們自動生成了有關它們的簡報。<lb/>這些簡報發送給九位個人投資者，以評估這種數據分析方法的實用性。

##### **Integrating Large Language Models and Graph Convolutional Networks for Semi-Supervised Image Classification**
2607.09104v1 by Camila Piscioneri Magalhães, Lucas Pascotti Valem

While the growing availability of image data has driven significant advances, labeling datasets remains costly and time-consuming. Therefore, semi-supervised approaches such as Graph Convolutional Networks (GCNs), which learn from both labeled and unlabeled data, have emerged as a promising solution. One of the primary challenges in applying GCNs to image classification is graph construction, since, unlike in citation networks or similar domains, images typically do not come with a predefined structural representation. For visual data, most studies construct graphs based on the similarity between feature vectors from pretrained deep learning backbones, typically by employing kNN or reciprocal kNN algorithms. Although Large Language Models (LLMs) have shown remarkable capability in capturing high-level semantics, their integration with GCNs for image classification remains underexplored. Aiming to fill this gap, our approach uses a Vision Language Model (VLM) to generate textual image descriptions, which are then processed by an LLM to estimate semantic similarity scores between connected images. These scores guide the pruning of edges in kNN and reciprocal kNN graphs, filtering out semantically irrelevant neighbors. Experimental results reveal that leveraging LLMs for graph refinement can improve classification accuracy, particularly for kNN graphs and some backbones. The source code is publicly available at http://gcnllm.lucasvalem.com.

摘要：雖然影像數據的日益可用推動了重大進展，但標註數據集仍然成本高昂且耗時。因此，半監督方法如圖卷積網絡（GCNs），能夠從標註和未標註數據中學習，已成為一個有前景的解決方案。將GCNs應用於影像分類的主要挑戰之一是圖的構建，因為與引用網絡或類似領域不同，影像通常沒有預定義的結構表示。對於視覺數據，大多數研究基於來自預訓練深度學習骨幹的特徵向量之間的相似性來構建圖，通常通過使用kNN或互惠kNN算法。儘管大型語言模型（LLMs）在捕捉高層語義方面展現出卓越的能力，但它們與GCNs在影像分類中的整合仍然未被充分探索。為了填補這一空白，我們的方法使用視覺語言模型（VLM）生成文本影像描述，然後由LLM處理以估算連接影像之間的語義相似性分數。這些分數指導kNN和互惠kNN圖中的邊緣修剪，過濾掉語義上不相關的鄰居。實驗結果顯示，利用LLMs進行圖的精煉可以提高分類準確性，特別是對於kNN圖和某些骨幹。源代碼可在http://gcnllm.lucasvalem.com公開獲取。

##### **A Coreset Selection Framework with Ensemble Aggregation for Image Classification**
2607.09100v1 by Pedro Rocha Dantas, Lucas Pascotti Valem

The rapid growth of image data has produced large-scale datasets, raising concerns about the time and memory costs of model training. Selecting representative training subsets, however, remains challenging: individual sample contributions are unclear, and model behavior varies across datasets and runs. We address these challenges with a framework that combines coreset selection with an ensemble aggregation over multiple runs. For coreset selection, we propose SCOre-Stratified Selection (SCOSS), which partitions the training data into intervals based on a chosen score and samples from each interval. The ensemble combines predictions from multiple runs, each performed on an independently sampled training subset. As baselines, we use moderate and random selection, each in original and class-balanced versions. We assess the framework with Simple Graph Convolution (SGC) and Support Vector Machine (SVM) classifiers under different sampling ratios. Experiments show that SCOSS is competitive with baselines, often the best choice for SGC, and enables favorable trade-offs between accuracy and efficiency. On the fine-grained dataset, SGC with SCOSS outperforms SVMs when using fewer labeled samples. The code and supplementary materials are publicly available at http://scoss.lucasvalem.com.

摘要：影像數據的快速增長產生了大規模數據集，這引發了對模型訓練時間和內存成本的擔憂。然而，選擇具有代表性的訓練子集仍然具有挑戰性：個別樣本的貢獻不明確，且模型行為在不同數據集和運行之間變化。我們通過一個框架來解決這些挑戰，該框架將核心集選擇與多次運行的集成聚合相結合。對於核心集選擇，我們提出了SCOre-Stratified Selection (SCOSS)，它根據選定的分數將訓練數據劃分為區間，並從每個區間中進行抽樣。該集成結合了來自多次運行的預測，每次運行都是在獨立抽樣的訓練子集上進行的。作為基準，我們使用中等和隨機選擇，分別以原始和類別平衡版本進行。我們在不同抽樣比率下，使用簡單圖卷積（SGC）和支持向量機（SVM）分類器來評估該框架。實驗表明，SCOSS與基準相比具有競爭力，通常是SGC的最佳選擇，並實現了準確性和效率之間的有利權衡。在細粒度數據集上，使用SCOSS的SGC在標記樣本較少的情況下超越了SVM。代碼和補充材料可在http://scoss.lucasvalem.com公開獲得。

##### **L-MAD: A Systematic Evaluation of Multi-Agent Debate Structures in Legal Reasoning**
2607.09099v1 by Tan-Minh Nguyen, Hoang-Trung Nguyen, Huu-Dong Nguyen, Dinh-Truong Do, Thi-Hai-Yen Vuong, Le-Minh Nguyen

While multi-agent debate (MAD) frameworks have shown significant potential in general reasoning, their effectiveness in highly structured, knowledge-heavy legal domains remains under-explored. In this work, we introduce the Legal Multi-Agent Debate (L-MAD) framework to systematically evaluate different debate structures and aggregation methods within Legal Textual Entailment. By assigning distinct expert personas to multiple agents, L-MAD improves upon strong single-agent baselines by up to 8\%. Furthermore, analyzing how debate scales reveals a clear trade-off: increasing the agent population reduces inconsistency and improves accuracy, whereas extending discussion rounds induces a detrimental \textit{over-deliberation drift} where agents reinforce each other's mistakes. Ultimately, our findings outline the practical boundaries and safety margins of deploying collaborative multi-agent systems in high-stakes legal reasoning environments.

摘要：雖然多代理辯論（MAD）框架在一般推理方面顯示出顯著潛力，但其在高度結構化、知識密集的法律領域中的有效性仍然未被充分探索。在這項工作中，我們介紹了法律多代理辯論（L-MAD）框架，以系統地評估法律文本推理中的不同辯論結構和聚合方法。通過為多個代理分配不同的專家角色，L-MAD 在強大的單代理基準之上提高了多達 8\%。此外，分析辯論的擴展顯示出明顯的權衡：增加代理人數量會減少不一致性並提高準確性，而延長討論回合則會導致有害的 \textit{過度深思漂移}，使代理人強化彼此的錯誤。最終，我們的研究結果勾勒出在高風險法律推理環境中部署協作多代理系統的實際界限和安全邊際。

##### **PRecG: Legal Precedent Retrieval with Graph Neural Networks and Rhetorical Role Segmentation**
2607.09094v1 by Devanshu Verma, Vasudha Bhatnagar, Vikas Kumar, Balaji Ganesan

Legal precedent retrieval is a fundamental task in legal case preparation, planning, litigation strategy, and legal research. Current approaches for automatic precedent retrieval map legal documents to a low-dimensional semantic space and compute similarity based on the proximity of their representations. These approaches treat legal documents as monolithic texts, ignoring the rhetorical organization of the legal technicalities. Ergo, they overlook nuanced legal meanings and fail to distinguish the contextual significance of legal entities and concepts that vary based on their rhetorical roles within the document.
  To address this insufficiency, we propose the PRecG pipeline that computes the similarity between pairs of legal judgments by hierarchically learning their representations. The process begins by decomposing each document into distinct semantic units (segments) based on the rhetorical roles of sentences. For each rhetorical segment, a knowledge graph is constructed to capture the legal entities and their relationships within the segment. Contextual representations of the entities are then learned and aggregated to derive segment-level embeddings. These embeddings are further integrated to produce a unified document-level representation, and finally, the semantic similarity between a pair of documents is computed. We validate the performance of the proposed approach through extensive experiments on a benchmark Indian legal dataset, comparing it against state-of-the-art baselines to demonstrate its effectiveness.

摘要：法律先例檢索是法律案件準備、規劃、訴訟策略和法律研究中的基本任務。當前自動先例檢索的方法將法律文件映射到低維語義空間，並根據其表示的接近度計算相似性。這些方法將法律文件視為一體化的文本，忽略了法律技術細節的修辭組織。因此，它們忽視了細微的法律意義，未能區分法律實體和概念的上下文意義，這些意義根據它們在文件中的修辭角色而有所不同。<lb/>為了解決這一不足，我們提出了PRecG管道，通過分層學習法律判決的表示來計算法律判決對之間的相似性。該過程首先根據句子的修辭角色將每個文件分解為不同的語義單元（段落）。對於每個修辭段落，構建一個知識圖譜，以捕捉該段落內的法律實體及其關係。然後學習並聚合這些實體的上下文表示，以推導段落級嵌入。這些嵌入進一步整合以生成統一的文件級表示，最後計算一對文件之間的語義相似性。我們通過在基準印度法律數據集上進行廣泛實驗來驗證所提方法的性能，並將其與最先進的基準進行比較，以展示其有效性。

##### **AgentKGV: Agentic LLM-RAG Framework with Two-Stage Training for the Fact Verification of Knowledge Graphs**
2607.09092v1 by Yumin Heo, Hyeon-gu Lee, Sumin Seo, Youngjoong Ko

Knowledge graphs (KGs) are often automatically constructed from large-scale corpora, but they inevitably contain factual errors due to noisy sources and extraction failures, and verifying them reliably at industrial scale remains a critical challenge. To address this, we propose AgentKGV, the Agentic LLM-RAG framework for KG fact Verification, that integrates dynamic routing and iterative query rewriting, which handles surface-form mismatch in document-level retrieval. To make this framework more accurate and cost-efficient for industrial deployment, we further introduce a two-stage training strategy: turn-level distillation-based SFT that transfers reasoning ability from a large teacher model into a small model for stable query rewriting and reasoning, and trajectory-level GRPO that optimizes the search policy to reduce unnecessary retrieval at scale. On the long-tail-predicate split of the open-domain T-REx benchmark, our framework improves macro-F1 over single-turn RAG by 5.5 \%p, and two-stage training does it further by 9.4 \%p. GRPO also cuts the average number of search calls from 3.24 to 1.63 without lowering accuracy.

摘要：知識圖譜（KGs）通常是從大規模語料庫自動構建的，但由於來源雜訊和提取失敗，它們不可避免地包含事實錯誤，並且在工業規模上可靠地驗證這些錯誤仍然是一個重大挑戰。為了解決這個問題，我們提出了AgentKGV，這是一個用於KG事實驗證的Agentic LLM-RAG框架，該框架整合了動態路由和迭代查詢重寫，能夠處理文檔級檢索中的表面形式不匹配。為了使這個框架在工業部署中更加準確和成本效益高，我們進一步引入了一種兩階段的訓練策略：基於回合的蒸餾式SFT，將大型教師模型的推理能力轉移到小型模型中，以實現穩定的查詢重寫和推理，以及軌跡級GRPO，優化搜索策略以減少不必要的檢索。在開放域T-REx基準的長尾謂詞分割上，我們的框架在宏觀F1指標上比單回合RAG提高了5.5 \%p，而兩階段訓練進一步提高了9.4 \%p。GRPO還將平均搜索調用次數從3.24減少到1.63，而不降低準確性。

##### **Model Agnostic Graph Prompt Learning for Crystal Property Prediction**
2607.08996v1 by Shrimon Mukherjee, Kishalay Das, Partha Basuchowdhuri, Pawan Goyal, Niloy Ganguly

Graph Neural Networks have emerged as a powerful tool for the fast and accurate prediction of various crystal properties. These models often encode domain-specific knowledge into their graph encoding modules, which increases their parameter size and makes their performance heavily dependent on domain expertise. Added to this, explicitly incorporating all chemical and structural features, that might influence a specific crystal property into the GNN encoder, is a challenging task. In this work, we propose a soft prompt learning framework that captures latent features essential for property prediction, which are not explicitly provided to the GNN. We introduce a novel multilevel graph prompt learning framework comprising both node-level and graph-level soft prompts. At the node level, we capture the local chemical semantics of different atom types, while at the graph level, we encode the global structural symmetry of the crystal graph. Our proposed prompt learning framework is lightweight and seamlessly integrates with any existing GNN encoder. Extensive experiments on popular benchmark datasets show that incorporating prompt learning significantly improves (3\% - 15\%) the performance of state-of-the-art GNN models in crystal property prediction tasks. Furthermore, the learned soft prompts enable cross-property knowledge transfer, enhancing prediction performance for properties with limited training data. Code is available at https://github.com/shrimonmuke0202/Prompt.git

摘要：圖神經網絡已成為快速且準確預測各種晶體性質的強大工具。這些模型通常將特定領域的知識編碼到其圖編碼模塊中，這增加了它們的參數大小，並使其性能在很大程度上依賴於領域專業知識。此外，將所有可能影響特定晶體性質的化學和結構特徵明確地納入GNN編碼器是一項具有挑戰性的任務。在這項工作中，我們提出了一個軟提示學習框架，捕捉對性質預測至關重要的潛在特徵，這些特徵並未明確提供給GNN。我們引入了一種新穎的多層次圖提示學習框架，包含節點級和圖級的軟提示。在節點級別，我們捕捉不同原子類型的局部化學語義，而在圖級別，我們編碼晶體圖的全局結構對稱性。我們提出的提示學習框架輕量且能無縫整合到任何現有的GNN編碼器中。在流行基準數據集上的大量實驗顯示，納入提示學習顯著提高了最先進的GNN模型在晶體性質預測任務中的性能（3\% - 15\%）。此外，學習到的軟提示使得跨性質知識轉移成為可能，增強了對訓練數據有限的性質的預測性能。代碼可在 https://github.com/shrimonmuke0202/Prompt.git 獲得。

##### **The Patchwork Problem in LLM-Generated Code**
2607.08981v1 by Viraaji Mothukuri, Reza M. Parizi

LLM-generated code often compiles, passes tests, and appears correct, yet breaks once deployed. The root cause is frequently structural rather than logical. A generated endpoint references configuration keys never declared in the project, an import targets a package that does not exist in any registry, or a new route omits the authentication guard applied to every sibling endpoint. Each patch is locally valid but globally incoherent, and standard CI toolchains rarely surface these failures. As LLM-powered coding tools see widespread adoption, this blind spot poses a growing risk to software quality. We call this the \textbf{patchwork problem}. This paper formalizes structural coherence as consistency invariants over graph representations of repository artifacts, including import, call, dependency, configuration, schema, resource, control-flow, and routing graphs, and introduces an eight-category failure taxonomy distinguishing defects specific to LLM generation from those merely amplified by it. We present a hybrid verification framework that delegates to mature static analysis tools where they already excel and deploys purpose-built detectors for cross-cutting invariants underserved by existing toolchains, targeting provable constraint violations rather than heuristic pattern matching. Empirical evaluation across two frontier models under four prompting strategies reveals that the vast majority of structural failures evade type checking, testing, and SAST entirely, and that failure patterns diverge qualitatively between models in ways that challenge model-agnostic mitigation strategies. External validation on real-world AI-generated repositories confirms that these failures are not artifacts of controlled experimentation but are prevalent wherever LLMs write code with minimal human oversight.

摘要：LLM 生成的代碼通常能夠編譯、通過測試，並且看起來正確，但一旦部署就會出現問題。根本原因通常是結構性的，而非邏輯性的。生成的端點引用了在項目中從未聲明的配置鍵，導入指向不存在於任何註冊表中的包，或者新的路由省略了應用於每個兄弟端點的身份驗證保護。每個修補程序在本地都是有效的，但在全局上卻不一致，而標準的 CI 工具鏈很少能夠發現這些失敗。隨著 LLM 驅動的編程工具被廣泛採用，這一盲點對軟體質量構成了日益增長的風險。我們稱之為 \textbf{拼湊問題}。本文將結構一致性形式化為對倉庫工件的圖形表示的穩定性不變式，包括導入、調用、依賴、配置、架構、資源、控制流和路由圖，並引入了一個八類失敗分類法，區分 LLM 生成特有的缺陷與僅僅被其放大的缺陷。我們提出了一個混合驗證框架，將成熟的靜態分析工具的優勢委派給它們已經擅長的領域，並部署專門構建的檢測器，用於現有工具鏈未能充分服務的交叉不變式，針對可證明的約束違反，而非啟發式模式匹配。在四種提示策略下對兩個前沿模型的實證評估顯示，絕大多數結構性失敗完全逃避了類型檢查、測試和 SAST，且失敗模式在模型之間的質量差異挑戰了模型無關的緩解策略。對現實世界 AI 生成的倉庫進行的外部驗證確認，這些失敗不是受控實驗的產物，而是在 LLM 以最小的人類監督編寫代碼的地方普遍存在。

##### **Eluna: An Agentic LLM System for Automating Warehouse Operations with Reasoning and Task Execution**
2607.08960v1 by Ning Liu, Kalle Kujanpää, Zhaoxuan Zhu, P Aditya Sreekar, Kaiwen Liu, Chuanneng Sun, Jorge Marchena Menendez, Matthew Bales, Tianyu Yang, Shahnawaz Alam, Rose Yu, Baoyuan Liu, Kristina Klinkner, Shervin Malmasi

Warehouse operations are governed by Standard Operating Procedures (SOPs) that encode complex, multi-system decision logic, which must be executed reliably under strict time constraints, yet LLM agents lack mechanisms to enforce procedural compliance and degrade under the context overload full SOP specifications introduce. We present Eluna, a production-deployed agentic system for reliable SOP execution. Eluna is a graph-guided, multi-agent framework that encodes SOPs as directed acyclic graphs with progressive disclosure and delegates independent tasks to parallel sub-agents, each with persistent code execution and live data access. To meet production latency and accuracy needs, we use asymmetric episodic distillation where a strong teacher is improved through episodic error memories, then a smaller student is fine-tuned on the corrected trajectories with memory stripped, internalizing corrections without inference-time overhead. On a 13-task benchmark and two production applications, our fine-tuned models match or exceed their teacher, beat all larger off-the-shelf baselines, and reach 94% expert agreement on the ticket processing application.

摘要：倉庫操作受到標準作業程序（SOP）的約束，這些程序編碼了複雜的多系統決策邏輯，必須在嚴格的時間限制下可靠執行，然而LLM代理缺乏強制遵循程序的機制，並在完整SOP規範引入的上下文過載下退化。我們提出Eluna，一個用於可靠SOP執行的生產部署代理系統。Eluna是一個圖形引導的多代理框架，將SOP編碼為有向無環圖，並逐步披露，將獨立任務委派給平行子代理，每個子代理都有持久的代碼執行和即時數據訪問。為了滿足生產延遲和準確性需求，我們使用不對稱的情節蒸餾，其中強大的教師通過情節錯誤記憶進行改進，然後一個較小的學生在去除記憶的修正軌跡上進行微調，內化修正而不增加推理時間的開銷。在一個13任務的基準測試和兩個生產應用中，我們微調的模型達到或超過了其教師，打敗所有較大的現成基準，並在票務處理應用中達到94%的專家一致性。

##### **GATS: Graph-Augmented Tree Search with Layered World Models for Efficient Agent Planning**
2607.08894v1 by Maureese Williams, Dymitr Nowicki

Large Language Model (LLM) agents have shown promise in multi-step planning tasks, but existing approaches like LATS (Language Agent Tree Search) and ReAct rely heavily on LLM inference during planning, leading to high computational costs and stochastic behavior. We present \textbf{GATS} (Graph-Augmented Tree Search), a planning framework that combines systematic UCB1-based tree search with a layered world model to eliminate LLM calls during inference while achieving superior planning performance. Our three-layer world model integrates: (L1) exact symbolic action matching, (L2) statistics learned from execution logs, and (L3) LLM-based prediction for unknown actions. On synthetic planning tasks with branching paths and dead-ends, GATS achieves \textbf{100\% success rate} compared to 92 % for LATS and 64\% for ReAct. On a comprehensive stress test spanning 12 challenging scenarios -- including coding workflows, web navigation, and long-horizon tasks -- GATS maintains \textbf{100\% success} while LATS drops to 88.9 % and ReAct to 23.9%. GATS requires \textbf{zero LLM calls per task} during planning (vs. 37 per task for LATS) and produces deterministic plans with zero variance across runs. Our results demonstrate that systematic search with learned world models can substantially outperform LLM-guided exploration for agent planning.

摘要：大型語言模型（LLM）代理在多步規劃任務中顯示出潛力，但現有的方法如 LATS（語言代理樹搜索）和 ReAct 在規劃過程中過度依賴 LLM 推理，導致高計算成本和隨機行為。我們提出了 \textbf{GATS}（圖增強樹搜索），這是一個規劃框架，結合了基於 UCB1 的系統性樹搜索和分層世界模型，以消除推理過程中的 LLM 調用，同時實現優越的規劃性能。我們的三層世界模型整合了：（L1）精確的符號行動匹配，（L2）從執行日誌中學習的統計數據，以及（L3）基於 LLM 的未知行動預測。在具有分支路徑和死胡同的合成規劃任務中，GATS 的成功率達到 \textbf{100\%}，而 LATS 為 92\%，ReAct 為 64\%。在涵蓋 12 個挑戰性場景的全面壓力測試中——包括編碼工作流程、網頁導航和長期任務——GATS 仍保持 \textbf{100\% 的成功率}，而 LATS 降至 88.9\%，ReAct 降至 23.9\%。GATS 在規劃過程中每個任務需要 \textbf{零次 LLM 調用}（而 LATS 每個任務需要 37 次），並且在多次運行中產生的計劃是確定性的且變異為零。我們的結果顯示，使用學習的世界模型進行系統性搜索可以顯著超越 LLM 指導的探索，以進行代理規劃。

##### **Workflow as Knowledge: Semantic Persistence for LLM-Mediated Workflows**
2607.08740v1 by Emanuele Quinto, Carlo Andrea Rozzi, Francesco Zanitti

Large language model (LLM) applications increasingly use explicit workflows for tool use, retrieval, branching, checkpointing, and human approval. Existing workflow systems already address many execution concerns. This paper proposes a Lisp-inspired but language-independent conceptual model: symbolic forms, object identity, and live-image thinking are used as explanatory lenses, not implementation commitments. In this model, workflow definitions, workflow instances, inference records, context snapshots, and dependency relations are represented as persistent knowledge objects in a shared knowledge substrate. Its central semantic distinction is between derive and infer: derive is deterministic computation over available state; infer is mediated LLM judgment under declared context and executor-controlled capability policy.
  The result is a preliminary conceptual account of semantic persistence: workflows do not merely produce knowledge and leave traces, but can themselves be represented as inspectable, resumable, and reviewable knowledge objects, while formal transition semantics remain future work.

摘要：大型語言模型（LLM）應用越來越多地使用明確的工作流程來進行工具使用、檢索、分支、檢查點和人類批准。現有的工作流程系統已經解決了許多執行方面的問題。本文提出了一種受Lisp啟發但與語言無關的概念模型：符號形式、對象身份和即時影像思維被用作解釋性透鏡，而不是實施承諾。在這個模型中，工作流程定義、工作流程實例、推理記錄、上下文快照和依賴關係被表示為共享知識基底中的持久知識對象。其核心語義區別在於推導和推斷：推導是對可用狀態的確定性計算；推斷是在聲明的上下文和執行者控制的能力政策下的中介LLM判斷。  
結果是一個初步的語義持久性概念賬戶：工作流程不僅僅是產生知識並留下痕跡，而是可以被表示為可檢查、可恢復和可回顧的知識對象，而正式的過渡語義仍然是未來的工作。

##### **DominoTree: Conditional Tree-Structured Drafting with Domino for Speculative Decoding**
2607.08642v2 by Saw S. Lin, Jyh-Shing Roger Jang

Speculative decoding accelerates LLM inference by drafting several tokens and verifying them in parallel. Block-diffusion drafters such as DFlash produce a draft block in one pass but model only per-position marginals, and best-first tree methods such as DDTree expand candidate trees from those marginals. The released Domino drafter adds a GRU-based causal correction that makes each draft token distribution path-dependent, a structure DDTree's factorized formulation cannot represent. We introduce DominoTree, a training-free best-first draft tree scored by Domino's conditional, non-factorized correction along each root-to-node path, made practical by restricting the per-node correction to a candidate top-M set. On Qwen3-4B across eight benchmarks, DominoTree reaches up to 6.6x speedup over autoregressive decoding and the highest mean accepted length of any evaluated method, up to 10.7 tokens per round, at every tested temperature. DominoTree constructs its tree with a GPU-native CUDA-graph builder that is bit-identical to a reference Python implementation, so acceptance is unchanged, while keeping per-round tree construction cheap. With this builder as default, DominoTree improves throughput over the released Domino decoder, the drafter it builds on, at every tested temperature: 9% to 10% overall on Qwen3-4B and up to 22% on Alpaca. It also outperforms DDTree and CaDDTree at every tested temperature, not only under greedy decoding. On Qwen3-8B, DominoTree keeps the highest accepted length at every temperature and gives a 24% throughput gain over DDTree at T=0; at higher temperature its edge over DDTree and CaDDTree narrows to a tie and a small loss, while its aggregate gains over DFlash and Domino persist.

摘要：投機解碼通過並行草擬多個標記並驗證它們來加速LLM推理。像DFlash這樣的區塊擴散草擬器在一次通過中產生草擬區塊，但僅模型每個位置的邊際分佈，而像DDTree這樣的最佳優先樹方法則從這些邊際擴展候選樹。釋出的Domino草擬器增加了一個基於GRU的因果修正，使每個草擬標記的分佈路徑依賴，這是DDTree的因子化公式無法表示的結構。我們引入了DominoTree，一個無需訓練的最佳優先草擬樹，根據Domino的條件非因子化修正沿每個根到節點的路徑進行打分，通過將每個節點的修正限制為候選的前M集合來使其實用。在Qwen3-4B的八個基準測試中，DominoTree在自回歸解碼上達到最多6.6倍的加速，並且在每個測試的溫度下，接受的平均長度是所有評估方法中最高的，每輪最多10.7個標記。DominoTree使用一個與參考Python實現位元相同的GPU本地CUDA圖構建器來構建其樹，因此接受度不變，同時保持每輪樹構建的成本低廉。以這個構建器為默認，DominoTree在每個測試的溫度下改善了釋出Domino解碼器的吞吐量，這是它所建立的草擬器：在Qwen3-4B上整體提高了9%到10%，在Alpaca上提高了最多22%。它在每個測試的溫度下也超越了DDTree和CaDDTree，不僅僅是在貪婪解碼下。在Qwen3-8B上，DominoTree在每個溫度下保持最高的接受長度，並在T=0時比DDTree提高了24%的吞吐量；在較高的溫度下，它相對於DDTree和CaDDTree的優勢縮小到平局和小幅損失，而其對DFlash和Domino的總體增益仍然持續。

##### **Towards Precision Therapy in Hepatocellular Carcinoma: A Clinical-Reasoning LLM for Risk Stratification and Treatment Guidance**
2607.08602v1 by Peng Cui, Jitao Wang, Siyan Xue, Yao Huang, Haoming Xia, Dong Li, Dengxiang Liu, Weilin Wang, Liping Liu, Leida Zhang, Yunfu Cui, Tao Peng, Daolin Ji, Haitao Zhao, Wei Zhang, Xiaojuan Wang, Weijie Ma, Zongren Ding, Jinlong Li, Yuan Ding, Jiajing Zhao, Zhiyu Chen, Chengkun Yang, Ziyue Huang, Jiaqi Liu, Fusheng Liu, Yang Zhou, Xiaojuan Wang, Zhongquan Sun, Shiyun Bao, Xiaojun Wang, Ming Yang, Guangxin Li, Bin Shu, Yong Liao, Hongxuan Li, Yao Tang, Shizhong Yang, Yongyi Zeng, Yufeng Yuan, Yinpeng Dong, Jihui Hao, Jun Zhu, Jiahong Dong

Hepatocellular carcinoma (HCC) is a common malignancy and a leading cause of cancer-related mortality. Current guidelines and staging systems provide coarse categories, but often miss within-stage heterogeneity and the clinical context in electronic medical records (EMRs). We present HCC-STAR (Hepatocellular Carcinoma Staging, Treatment And pRognosis), a clinically aligned large language model that reads routine EMR narratives and jointly outputs risk score-based staging, ranked guideline-consistent treatments with evidence-based rationales, and individualized survival estimates. We curated about 30,000 HCC cases from SEER and expanded them into EMR-style narrative training data using a clinician-validated, prompt-based augmentation workflow. On this corpus, we developed a knowledge-aligned reasoning framework optimized with a step-verifiable composite reward, moving beyond text-level memorization of clinical guidelines. In a multi-center cohort of 6,668 patients from 12 hospitals in China, HCC-STAR achieved state-of-the-art performance in treatment recommendation and risk stratification compared with clinical guidelines and competitive models, including GPT-5 and Gemini-2.5 Pro. Hypothetical overall-survival analysis showed a median survival of 51 months under adherence to HCC-STAR recommendations, compared with 29 and 32 months under BCLC and CNLC. In clinician-centric evaluations, blinded hepatobiliary specialists rated HCC-STAR's reasoning and evidence-based justifications as trustworthy. The model surpassed resident and attending physicians in treatment accuracy and helped physicians make more accurate decisions faster when used as an assistant. These findings support HCC-STAR as a reliable and verifiable decision-support system for risk stratification and precision therapy in HCC.

摘要：肝細胞癌（HCC）是一種常見的惡性腫瘤，也是癌症相關死亡的主要原因。當前的指導方針和分期系統提供了粗略的類別，但往往忽略了分期內的異質性和電子病歷（EMR）中的臨床背景。我們提出了HCC-STAR（肝細胞癌分期、治療和預後），這是一個臨床對齊的大型語言模型，能夠閱讀常規EMR敘述並共同輸出基於風險評分的分期、按證據基礎的合理性排名的指導方針一致的治療方案，以及個體化的生存預測。我們從SEER整理了約30,000個HCC病例，並使用臨床驗證的基於提示的增強工作流程將其擴展為EMR風格的敘述訓練數據。在這個語料庫上，我們開發了一個知識對齊的推理框架，並用步驟可驗證的綜合獎勵進行優化，超越了對臨床指導方針的文本級記憶。在來自中國12家醫院的6,668名患者的多中心隊列中，HCC-STAR在治療建議和風險分層方面達到了與臨床指導方針和競爭模型（包括GPT-5和Gemini-2.5 Pro）相比的最先進表現。假設的整體生存分析顯示，在遵循HCC-STAR建議的情況下，媒介生存期為51個月，而在BCLC和CNLC下分別為29個月和32個月。在以臨床醫生為中心的評估中，盲測的肝膽專家將HCC-STAR的推理和基於證據的理由評價為值得信賴。該模型在治療準確性方面超越了住院醫師和主治醫師，並在作為助手使用時幫助醫生更快地做出更準確的決策。這些發現支持HCC-STAR作為一個可靠且可驗證的決策支持系統，用於HCC的風險分層和精準治療。

##### **The Context Access Divide: Interaction-Level Architecture as a Complementary Dimension of Agentic Inequality**
2607.08495v1 by Masahiro Fujita

Sharp et al. (2025) introduce "agentic inequality" as a framework for analyzing disparities in access to AI agents across three dimensions: availability, quality, and quantity. These person- and organization-level dimensions characterize who can access agents and at what capability, but do not address a structurally important divide operating at a finer level: the individual interaction. Two users with nominally equivalent agent access may experience qualitatively different AI utility depending on whether the system can autonomously retrieve context from the user's knowledge corpus (Dynamic Context Retrieval) or requires the user to manually identify and attach relevant documents at each query (Manual Attachment). We term this the Context Access Divide (CAD). For knowledge-intensive workers whose intellectual capital spans tens of thousands of files, the CAD constitutes a qualitative threshold in AI usefulness: below it, the cognitive burden of context curation falls on the human, reproducing the inefficiencies AI is meant to eliminate. We propose contextuality -- the degree to which an AI system autonomously accesses a user's accumulated knowledge capital -- as a dimension of AI-mediated inequality that complements, but is not reducible to, the Sharp et al. framework. We formalize the CAD with a probabilistic model grounded in the fan effect literature in cognitive psychology, demonstrating that manual context attachment leads to a combinatorial collapse in task-success probability as corpus size and task conjunctivity grow, while dynamic retrieval architectures are structurally insulated from this collapse. We analyze the technical basis of this divide in the Model Context Protocol (MCP) and retrieval-augmented generation (RAG) architectures, and examine its implications for knowledge-work stratification and AI platform governance.

摘要：Sharp 等人 (2025) 提出了「代理不平等」作為分析 AI 代理訪問差異的框架，涵蓋三個維度：可用性、質量和數量。這些個人和組織層面的維度描述了誰可以訪問代理以及以何種能力訪問，但並未解決在更細微層面上運作的結構性重要分歧：個體互動。兩個名義上擁有相等代理訪問權的用戶，根據系統是否能夠自主從用戶的知識庫中檢索上下文（動態上下文檢索）或是否需要用戶在每次查詢時手動識別並附加相關文件（手動附加），可能會體驗到質量上不同的 AI 實用性。我們將其稱為上下文訪問分歧（CAD）。對於那些知識密集型工作者來說，他們的智力資本涵蓋數以萬計的文件，CAD 構成了 AI 有用性的質量門檻：在此之下，上下文策劃的認知負擔落在人類身上，重現了 AI 本應消除的低效。我們提出「上下文性」—— AI 系統自主訪問用戶累積的知識資本的程度——作為 AI 媒介不平等的一個維度，這補充了 Sharp 等人的框架，但並不能簡化為該框架。我們用一個基於認知心理學中風扇效應文獻的概率模型來形式化 CAD，證明手動上下文附加會隨著語料庫大小和任務聯結性的增長而導致任務成功概率的組合崩潰，而動態檢索架構在結構上則能夠抵禦這種崩潰。我們分析了這一分歧在模型上下文協議（MCP）和檢索增強生成（RAG）架構中的技術基礎，並檢視其對知識工作分層和 AI 平台治理的影響。

##### **Drift-Aware Temporal Graph Rewiring (DATGR) for Adaptive Semantic Modeling in Biomedical Text**
2607.08490v1 by Bharathwaj Vijayakumar, Sahana K. Varadaraju

Biomedical language evolves rapidly as new discoveries emerge, causing traditional text models to lose semantic fidelity over time. Static embeddings and co-occurrence graphs cannot capture such evolution, leading to performance degradation in retrieval and knowledge discovery tasks. This paper introduces a Drift-Aware Temporal Graph Rewiring (DATGR) framework that models concept evolution by dynamically updating co-occurrence edges based on estimated semantic drift. Instead of retraining embeddings for each time slice, DATGR performs lightweight, feedback-driven rewiring using a logistic update rule applied to edge weights. Evaluated on the Biomedical Multi-Relation Corpus (BIOMRC), the method achieved a mean Area Under the Receiver Operating Characteristic (AUROC) improvement of approximately 0.066 absolute difference (0.699 vs. 0.633) over a static baseline. Area Under the Precision-Recall Curve (AUPRC) remained comparable (0.738 vs. 0.744), showing that drift-aware adaptation enhances link-prediction recall without a loss in precision. These results demonstrate that edge-level adaptation effectively captures temporal semantic change in evolving biomedical text while remaining computationally efficient and interpretable.

摘要：生物醫學語言隨著新發現的出現而迅速演變，導致傳統文本模型隨時間的推移失去語義真實性。靜態嵌入和共現圖無法捕捉這種演變，導致檢索和知識發現任務的性能下降。本文介紹了一個漂移感知的時間圖重連結（DATGR）框架，通過根據估計的語義漂移動態更新共現邊來建模概念演變。DATGR並不需要對每個時間片段重新訓練嵌入，而是使用應用於邊權重的邏輯更新規則進行輕量級的、基於反饋的重連結。在生物醫學多關係語料庫（BIOMRC）上進行評估，該方法在靜態基線上實現了大約0.066的平均接收者操作特徵曲線（AUROC）改善（0.699對比0.633）。精確率-召回曲線下面積（AUPRC）保持相當（0.738對比0.744），顯示出漂移感知的適應性在不損失精確度的情況下增強了連結預測的召回率。這些結果表明，邊級適應有效捕捉了不斷演變的生物醫學文本中的時間語義變化，同時保持計算效率和可解釋性。

##### **Game Theory Driven Multi-Agent Framework Mitigates Language Model Hallucination**
2607.08403v1 by Runzhe Liu, Biquan Bie, Zihao Wang, Yuchao Ma, Yexin Liu, Xinghai Li, Harry Yang, Wenbo Yang, Jinzhe Cao, Shengyang Tao

The application of lightweight Large Language Models in rule-based scientific domains remains severely limited by their tendency to mimic linguistic patterns rather than reproduce axiomatic reasoning, causing frequent hallucinations. Here, we show that G-Frame, an adaptive multi-agent framework integrating Bayesian and team game principles, establishes an automated closed-loop for high-quality data synthesis and model training. By forcing the internalization of domain constraints through structured reasoning, we synthesized a specialized corpus of 363,045 chains-of-thought and 199,589 question-answer pairs. The resulting 7B model OmniChem achieves performance parity with GPT 4o mini on custom benchmarks and ChemBench while exhibiting a 79.46% reduction in hallucinations relative to its base architecture. We further demonstrate the advanced capabilities of OmniChem in molecular design and synthesis planning. This work establishes a scalable paradigm utilizing adaptive multi-agents to overcome inherent reasoning deficiencies, offering a feasible pathway for accelerating knowledge discovery in specialized scientific fields.

摘要：輕量級大型語言模型在基於規則的科學領域中的應用仍然受到其模仿語言模式而非重現公理推理的傾向的嚴重限制，導致頻繁的幻覺。在這裡，我們展示了 G-Frame，一個整合貝葉斯和團隊遊戲原則的自適應多代理框架，建立了一個自動化的閉環，用於高質量數據合成和模型訓練。通過強制內化領域約束，進行結構化推理，我們合成了一個專門的語料庫，其中包含 363,045 條思維鏈和 199,589 對問題-答案。所得到的 7B 模型 OmniChem 在自定義基準和 ChemBench 上達到了與 GPT 4o mini 的性能平起平坐，同時相對於其基礎架構顯示出 79.46% 的幻覺減少。我們進一步展示了 OmniChem 在分子設計和合成規劃方面的先進能力。本研究建立了一個可擴展的範式，利用自適應多代理克服固有的推理缺陷，提供了一條可行的途徑，加速專業科學領域的知識發現。

##### **TRACE: A Two-Channel Robust Attribution Watermark via Complementary Embeddings for LLM-Agent Trajectories**
2607.08400v1 by Zheng Gao, Xiaoyu Li, Xiaoyan Feng, Jiaojiao Jiang, Yang Song, Yulei Sui, Zhenchang Xing, Liming Zhu

LLM agents reach users through resellers, who may rebrand a developer's agent or substitute a cheaper model. When provenance is disputed, attribution rests on the trajectory log (the record of tool calls, observations, and executed actions, not the model's reasoning), which the reseller stores and processes to meter usage. A watermark must therefore survive an adversary with full read/write access to the very evidence it is detected from; existing agent watermarks do not, as their attribution is read straight off that log. We present TRACE, to our knowledge the first agent watermark that is distortion-free in its action choices, self-synchronizing under deletion, and unconditionally invariant under rewriting. Deletion desynchronizes a position-derived key and rewriting alters content, so a deletion-robust key must come from content and a rewrite-robust key from position, and no single key serves both. A trajectory, however, has room for two watermarks. TRACE superposes a selection channel that sets which action is chosen, keyed on local content with a distortion-free sampler, so the agent's distribution is provably unchanged and detection resynchronizes after deletions, and a tally channel that sets how many records each decision group holds, keyed on the log's skeleton alone, which no rewriting can touch. We prove this behavioral watermark's signal is bought with decision entropy, each decision paying at least half its entropy and deterministic decisions nothing, and that erasing both channels forces the reseller to corrupt the trajectories it resells. On ToolBench and ALFWorld, TRACE matches the unwatermarked agent's success rate while its selection channel reaches detection scores near z = 100 on long-horizon trajectories, stays detectable under 70% step deletion, and keeps a tally channel exactly unchanged under LLM rewriting of any strength.

摘要：LLM 代理透過轉售商接觸用戶，轉售商可能會重新品牌開發者的代理或替換為更便宜的模型。當來源受到質疑時，歸屬權依賴於軌跡日誌（工具調用、觀察和執行行動的記錄，而非模型的推理），轉售商存儲並處理該日誌以計量使用量。因此，水印必須能夠抵抗擁有完全讀寫權限的對手，這些對手可以接觸到檢測所依賴的證據；現有的代理水印無法做到這一點，因為它們的歸屬權是直接從該日誌中讀取的。我們提出了 TRACE，據我們所知，這是第一個在行動選擇上無失真的代理水印，能在刪除下自我同步，並在重寫下無條件不變。刪除會使基於位置的密鑰不同步，而重寫會改變內容，因此一個抗刪除的密鑰必須來自內容，而一個抗重寫的密鑰必須來自位置，且沒有單一的密鑰能同時滿足兩者。然而，一條軌跡有空間容納兩個水印。TRACE 超疊了一個選擇通道，設置選擇哪個行動，基於本地內容使用無失真的抽樣器，因此代理的分佈是可證明不變的，並且在刪除後檢測會重新同步，還有一個計數通道，設置每個決策組持有的記錄數量，僅基於日誌的骨架，這是任何重寫都無法觸及的。我們證明這個行為水印的信號是以決策熵為代價的，每個決策至少支付其熵的一半，而確定性決策則不支付任何費用，並且刪除這兩個通道會迫使轉售商損壞其轉售的軌跡。在 ToolBench 和 ALFWorld 上，TRACE 的成功率與未加水印的代理相匹配，而其選擇通道在長期軌跡上達到接近 z = 100 的檢測分數，並在 70% 步驟刪除下保持可檢測性，且在任何強度的 LLM 重寫下計數通道保持完全不變。

##### **Towards Mechanistically Understanding Why Memorized Knowledge Fails to Generalize in Large Language Model Finetuning**
2607.08393v1 by Lu Dai, Ziyang Rao, Yili Wang, Hanqing Wang, Hao Liu, Hui Xiong

Fine-tuning LLMs to inject new knowledge faces a critical challenge: LLMs can quickly memorize new facts, yet fail to use them for downstream reasoning tasks. We formalize this failure as the \textit{\textbf{Knowing--Using Gap}}, characterized by an accuracy gap and a temporal lag between memorization and generalization. To understand this phenomenon, we fine-tune LLMs with unseen knowledge and monitor the spatial permeation dynamics of the knowledge internally using a novel intervention technique called self-patching. Self-patching identifies activation locations where relocating representations substantially improves failed generalization cases. These results are consistent with a knowledge-circuit misalignment hypothesis: memorized representations can exist internally but may not be routed to computation-effective layers. To demonstrate the practicality of this diagnostic finding, we design a simple heuristic strategy which recovers 58--75\% of the oracle headroom in generalization failure. Experiments are done cross-domain for the robustness of this finding.

摘要：微調大型語言模型（LLMs）以注入新知識面臨一個關鍵挑戰：LLMs可以迅速記住新事實，但卻無法將其應用於下游推理任務。我們將這一失敗形式化為\textit{\textbf{知識--使用差距}}，其特徵是記憶與泛化之間的準確性差距和時間延遲。為了理解這一現象，我們使用一種名為自我修補的新型干預技術，對LLMs進行未見知識的微調，並監測知識在內部的空間滲透動態。自我修補識別出激活位置，在這些位置重新定位表示可以顯著改善失敗的泛化案例。這些結果與知識電路錯位假說一致：記憶的表示可以存在於內部，但可能未能路由到計算有效的層。為了展示這一診斷發現的實用性，我們設計了一個簡單的啟發式策略，恢復58--75\%的預言頭空間於泛化失敗中。實驗在跨領域進行，以驗證這一發現的穩健性。

##### **Large-Language-Models-as-a-Judge in Theory-Agnostic Adaptive Metric-Alignment for Prototypical Networks in Personality Recognition**
2607.08374v1 by Jing Jie Tan, Ban-Hoe Kwan, Danny Wee-Kiat Ng, Yan-Chai Hum, Shih-Yu Lo, Po-An Chen, Noriyuki Kawarazaki, Kosuke Takano, Anissa Mokraoui

Personality recognition has traditionally been constrained by theory-dependent formulations, where models are trained to fit predefined psychological taxonomies rather than uncovering shared underlying behavioral structure. This limits generalization, as personality itself is better understood as theory-invariant, while existing annotations reflect only partial and sometimes inconsistent views of the same latent traits. In this work, we introduce JAM ((J)udge for (A)daptive (M)etric-Alignment), a theory-agnostic framework that shifts learning from adapting to predefined personality theories toward discovering unified latent pseudo-facets that capture shared psychological structure. Rather than constraining the model to any personality taxonomy during training or inference, the framework learns generalizable psychological representations and can infer an individual's latent psychological profile directly from the textual samples, without requiring theory-specific labels. JAM achieves this through an Attention-Pooled Graph Prototypical Network that learns structured representations via clustering in embedding space, together with a Cross-Theory Harmonization (CTH) approach that integrates (i) Human-Guided Linkage and (ii) Machine-Induced Consensus to unify heterogeneous datasets without relying on predefined labels. To further improve robustness and data quality, we incorporate an LLM-as-a-Judge mechanism operating in two configurations, (i) LLM-before-the-loop and (ii) LLM-in-the-loop which identifies ambiguous samples to guide adaptive metric learning. Experiments show that JAM improves cross-framework generalization and performance, establishing a strong step toward theory-agnostic personality inference and supporting low-resource personality theories. The related code repository, model weights, and artifacts are available at https://research.jingjietan.com/JAM

摘要：個性識別傳統上受到依賴理論的公式限制，模型被訓練以適應預定義的心理分類，而不是揭示共享的潛在行為結構。這限制了泛化，因為個性本身更應被理解為理論不變，而現有的註釋僅反映部分且有時不一致的同一潛在特徵的觀點。在這項工作中，我們介紹了JAM ((J)udge for (A)daptive (M)etric-Alignment)，一個與理論無關的框架，將學習從適應預定義的個性理論轉向發現統一的潛在偽面向，這些面向捕捉共享的心理結構。該框架在訓練或推理期間不將模型限制於任何個性分類，而是學習可泛化的心理表徵，並能直接從文本樣本中推斷個體的潛在心理檔案，而無需理論特定的標籤。JAM通過一個注意力聚合圖原型網絡實現這一點，該網絡通過在嵌入空間中的聚類學習結構化表徵，並結合一種跨理論協調（CTH）方法，整合（i）人類引導的聯結和（ii）機器誘導的共識，以統一異質數據集，而不依賴預定義的標籤。為了進一步提高穩健性和數據質量，我們納入了一個LLM-as-a-Judge機制，該機制在兩種配置下運行，（i）迴圈前的LLM和（ii）迴圈中的LLM，識別模糊樣本以指導自適應度量學習。實驗表明，JAM改善了跨框架的泛化和性能，為理論無關的個性推斷邁出了堅實的一步，並支持低資源的個性理論。相關的代碼庫、模型權重和文檔可在https://research.jingjietan.com/JAM獲得。

##### **Self-Adaptive Anomaly Detection with Reinforcement Learning and Human Feedback in Connected Vehicles**
2607.08373v1 by Matthias Weiß, Athreya Hosahalli Prakash, Maurice Artelt, Falk Dettinger, Nasser Jazdi, Michael Weyrich

Connected vehicles are autonomous cyber-physical systems whose behavior must be continuously monitored during operation to detect deviations from normal operation before they propagate into failures. Such evaluation is challenging because the systems themselves evolve: over-the-air updates, configuration changes, and shifting workloads alter the definition of normal behavior, causing static diagnostic methods to degrade silently over time. Existing approaches typically address either automated model adaptation or operator integration in isolation, rather than as a single coordinated supervisory loop.
  This paper presents an online anomaly detection framework for autonomous CPS that integrates three coordinated mechanisms. A factorized deep Q-network with self-attention selects the most suitable detector from a candidate pool for each monitored service, exploiting inter-service dependencies in the microservice topology. An ensemble of three statistical drift detectors monitors the input distribution and raises an alarm only when all three concur, prioritizing precision over recall. A human-in-the-loop retraining mechanism, built around a pending transition buffer and a 60/40 prioritized replay strategy, allows the operator to incorporate expert knowledge while preserving the system's learned response to prior data distributions.
  The framework is evaluated on a connected-vehicle testbed running an automated valet parking application across seven backend microservices. The attention-augmented agent achieves an F1 score of 0.69, compared to at most 0.11 for any single detector applied uniformly. Following a real software update that induces measurable concept drift, F1 drops to 0.52; after operator-triggered retraining, performance recovers to 0.65 on the new distribution while remaining at 0.69 on the prior one, demonstrating sustained adaptation without catastrophic forgetting.

摘要：連網車輛是自主的網路物理系統，其行為必須在運行期間持續監控，以便在偏離正常運行之前檢測到異常，防止其演變為故障。這種評估具有挑戰性，因為系統本身會不斷演變：空中更新、配置變更和工作負載的變化改變了正常行為的定義，導致靜態診斷方法隨時間默默退化。現有的方法通常單獨處理自動化模型適應或操作員整合，而不是作為一個協調的監控循環。

本文提出了一個針對自主CPS的在線異常檢測框架，整合了三個協調的機制。一個具有自我注意力的分解深度Q網絡從候選池中為每個監控服務選擇最合適的檢測器，利用微服務拓撲中的服務間依賴關係。三個統計漂移檢測器的集成監控輸入分佈，僅在三者一致時才發出警報，優先考慮精確度而非召回率。一個以人為中心的再訓練機制，圍繞著一個待處理轉換緩衝區和60/40優先重播策略，允許操作員在保留系統對先前數據分佈的學習響應的同時，融入專家知識。

該框架在一個連網車輛測試平台上進行評估，該平台運行一個自動代客停車應用，涵蓋七個後端微服務。增強注意力的代理達到了0.69的F1分數，而任何單一檢測器均勻應用的分數最多為0.11。在一次實際的軟體更新後，導致可測量的概念漂移，F1下降至0.52；經過操作員觸發的再訓練後，性能在新分佈上恢復至0.65，而在先前分佈上保持在0.69，顯示出持續適應而不會產生災難性遺忘。

##### **FedOPAL: One-Shot Federated Learning via Analytic Visual Prompt Tuning**
2607.08368v1 by Lingyu Qiu, Daniela Annunziata, Stefano Izzo, Fabio Giampaolo, Francesco Piccialli

With the widespread deployment of basic models in edge intelligence, communication bandwidth has become a core bottleneck restricting the scalability of federated learning. Although one-shot federated learning alleviates this problem by minimizing communication rounds, existing iterative fine-tuning or knowledge distillation methods still face challenges such as high server-side computational costs and hyperparameter sensitivity. Analytical federated learning achieves efficient gradientfree aggregation using least-squares closed-form solutions, but in environments with non-independent and identically distributed data, its static feature assumptions fail, leading to feature manifold misalignment and severely impairing model performance. To address this contradiction, this paper proposes the FedOPAL framework. This framework adapts the visual prompts as feature rectifiers, actively correcting the feature distribution of heterogeneous data to a linearly separable space by applying local proximal constraints, thereby satisfying the theoretical assumptions of analytical federated learning. Experimental results show that FedOPAL not only significantly outperforms the original analytical methods on several benchmarks, but also achieves accuracy comparable to state-of-the-art iterative methods while maintaining zero server-side training costs, providing a new engineering paradigm for efficient collaboration of large models on the edge.

摘要：隨著基本模型在邊緣智能中的廣泛部署，通信帶寬已成為限制聯邦學習可擴展性的核心瓶頸。雖然一次性聯邦學習通過最小化通信回合來緩解這一問題，但現有的迭代微調或知識蒸餾方法仍然面臨高伺服器端計算成本和超參數敏感性等挑戰。分析性聯邦學習使用最小二乘閉式解實現高效的無梯度聚合，但在具有非獨立同分佈數據的環境中，其靜態特徵假設失效，導致特徵流形不對齊，嚴重損害模型性能。為了解決這一矛盾，本文提出了FedOPAL框架。該框架將視覺提示作為特徵校正器，通過應用局部近端約束，主動將異構數據的特徵分佈校正為線性可分的空間，從而滿足分析性聯邦學習的理論假設。實驗結果顯示，FedOPAL不僅在幾個基準上顯著超越了原始的分析方法，還在保持零伺服器端訓練成本的同時達到了與最先進的迭代方法相當的準確性，為大型模型在邊緣的高效協作提供了一種新的工程範式。

##### **TypeProbe: Recovering Type Representations from Hidden States of Pre-trained Code Models**
2607.08339v1 by Giuliano Gorgone, Fausto Carcassi

State-of-the-art code models achieve impressive performance, yet the extent to which they internally encode type information remains poorly understood. We probe the residual streams of pretrained code models for internal type representations using a parallel dataset of Java and Python code examples. Our results show that cross-lingual type representations emerge even from untyped code. Moreover, we test whether hidden states linearly encode the result type implied by typed function application by training probes on one language to infer argument and result types in the other. Finally, we find that this structure is partly robust to lexical perturbations and cross-language syntactic variations. To the best of our knowledge, prior work on interpretability of code models has not directly targeted formal type semantics or cross-lingual type representations. We release our code and datasets.

摘要：最先進的程式碼模型達到了令人印象深刻的性能，但它們在內部編碼類型信息的程度仍然不甚了解。<lb/>我們使用一個並行的 Java 和 Python 程式碼示例數據集來探查預訓練程式碼模型的殘餘流，以獲取內部類型表示。<lb/>我們的結果顯示，即使是未類型化的程式碼，也會出現跨語言的類型表示。<lb/>此外，我們測試隱藏狀態是否線性編碼由類型函數應用所暗示的結果類型，通過在一種語言上訓練探針來推斷另一種語言中的參數和結果類型。<lb/>最後，我們發現這種結構對詞彙擾動和跨語言語法變異具有一定的穩健性。<lb/>據我們所知，之前關於程式碼模型可解釋性的研究並未直接針對形式類型語義或跨語言類型表示。<lb/>我們發布了我們的程式碼和數據集。

##### **XALPHA: A Memory-Driven AI Quant Researcher for Hypothesis-to-Code Alpha Discovery**
2607.08332v1 by Fengyuan Liu, Yuchen Fu, Yuqi Wang, Qi Liu

Financial markets are noisy, non-stationary, and high-dimensional, making it difficult to discover predictive and robust trading signals. Alpha discovery has evolved from manual factor design to machine learning, evolutionary search, and recent LLM-based frameworks, improving the efficiency of factor generation, search, and evaluation. However, existing methods still mostly automate isolated steps, rather than functioning as end-to-end quant researchers that can absorb external knowledge, close the hypothesis-to-code validation loop, and learn from accumulated discovery feedback. To fill this gap, we introduce XAlpha, a memory-driven AI Quant Researcher for continuous hypothesis-to-code alpha discovery. XAlpha maintains a multi-source research memory system that integrates report-grounded financial knowledge with discovery feedback from prior generations and research cycles. Guided by this memory system, a Macro Brain plans research themes and selects suitable Archetypes; a Micro Brain transforms the planned hypothesis pool into executable factor code and verifies ex-ante tri-alignment among the hypothesis idea, code logic, and financial plausibility; and a Cross Brain consolidates empirical outcomes into generation-level feedback, cycle-level summaries, and archetype-level research cues for future exploration. In this way, XAlpha turns alpha mining from isolated factor generation into a closed-loop research process that continuously reads, hypothesizes, implements, validates, reflects, and evolves. Experiments on CSI300 show that XAlpha achieves stronger overall alpha discovery performance than representative baselines.

摘要：金融市場是嘈雜的、非穩定的和高維度的，這使得發現可預測和穩健的交易信號變得困難。Alpha 發現已經從手動因子設計演變為機器學習、進化搜索以及最近的基於 LLM 的框架，從而提高了因子生成、搜索和評估的效率。然而，現有的方法仍然主要自動化孤立的步驟，而不是作為能夠吸收外部知識、閉合假設到代碼驗證循環並從累積的發現反饋中學習的端到端量化研究者。為了填補這一空白，我們介紹了 XAlpha，一個以記憶驅動的 AI 量化研究者，用於持續的假設到代碼的 alpha 發現。XAlpha 維護一個多來源的研究記憶系統，將基於報告的金融知識與來自先前世代和研究周期的發現反饋整合在一起。在這個記憶系統的指導下，宏觀大腦計劃研究主題並選擇合適的原型；微觀大腦將計劃的假設池轉化為可執行的因子代碼，並驗證假設想法、代碼邏輯和金融合理性之間的前期三重對齊；而交叉大腦則將實證結果整合為世代級反饋、周期級總結和原型級研究提示，以便未來探索。通過這種方式，XAlpha 將 alpha 開採從孤立的因子生成轉變為一個閉環的研究過程，持續地閱讀、假設、實施、驗證、反思和演變。對 CSI300 的實驗顯示，XAlpha 在整體 alpha 發現性能上超越了代表性的基準。

##### **ArtMine: Discovering and Formalizing Artistic Processes**
2607.08331v1 by Kaustubh Kumar, Ashutosh Ranjan, Vivek Srivastava, Blessin Varkey, Shirish Karande

Understanding how artworks are created requires reasoning about the iterative decisions, material operations, and contextual influences that shape artistic production. While recent generative AI systems can synthesize artworks with high fidelity, they primarily model distributions over finished artifacts rather than the creative processes underlying their creation. In practice, artistic workflows are only partially documented through fragmented sources such as archival records, preparatory studies, correspondence, etc., making process-level understanding difficult to formalize computationally. In this work, we introduce ArtMine, a framework for discovering and formalizing artistic processes from heterogeneous historical evidence. Our approach synthesizes heterogeneous artwork evidence into a structured repository, from which a Peircean abductive agent infers evidence-grounded production steps. These steps are converted into a compositional graph and rendering prompt, then optimized through self-reflection over deviations between the generated and reference artworks. We provide a preliminary proof-of-concept case study using open-domain historical sources across multiple artists and artistic movements, demonstrating that fragmented documentary evidence can support coherent, interpretable, and auditable representations of artistic workflows. By modeling creative processes rather than only final artifacts, our work moves toward process-centred human-AI co-creativity systems that can support artistic interpretation, creative education, reflective collaboration, and computational studies of cultural production.

摘要：理解藝術作品是如何創作的，需要對塑造藝術生產的迭代決策、材料操作和背景影響進行推理。雖然最近的生成性人工智慧系統可以以高保真度合成藝術作品，但它們主要是對完成的工件進行分佈建模，而不是對其創作背後的創意過程進行建模。實際上，藝術工作流程僅通過檔案記錄、準備性研究、通信等零散來源部分記錄，這使得在計算上正式化過程級理解變得困難。在這項工作中，我們介紹了ArtMine，一個從異質歷史證據中發現和正式化藝術過程的框架。我們的方法將異質的藝術作品證據合成到一個結構化的資料庫中，從中一個佩爾斯式的推斷代理推斷基於證據的生產步驟。這些步驟被轉換成一個組合圖和渲染提示，然後通過對生成的藝術作品與參考作品之間的偏差進行自我反思來優化。我們提供了一個初步的概念驗證案例研究，使用多位藝術家和藝術運動的開放領域歷史來源，展示了零散的文獻證據可以支持一致的、可解釋的和可審計的藝術工作流程表示。通過對創意過程的建模，而不僅僅是最終工件，我們的工作朝著以過程為中心的人類-人工智慧共同創作系統邁進，這些系統可以支持藝術解釋、創意教育、反思性合作以及文化生產的計算研究。

##### **From Legacy Documentation to OSCAL: An MCP-Based Agent Pipeline for Threat-Informed Continuous Compliance in Critical Infrastructure**
2607.08288v1 by Lea Roxanne Muth, Marian Margraf

In critical infrastructure, operational technology environments often cannot be actively scanned, and yet active system feedback is needed for risk assessment and compliance. This paper presents a non-invasive, MCP-grounded multi-agent pipeline that converts natural-language system descriptions into source-verified knowledge graph and audit-ready artifacts in the NIST OSCAL format for continuous automated compliance management. The architecture decouples LLM-based reasoning from deterministic knowledge retrieval against authoritative threat-intelligence sources, reducing the risk of fabricated vulnerabilities and hallucinated attack paths.
  In an evidence-based synthetic scenario of a water utility, the pipeline achieves 0.90 CVE recall and perfect D3FEND recall. It generates a schema-valid OSCAL System Security Plan and an OSCAL Security Assessment Report. Nevertheless, the core insight is not that grounding via MCP eliminates errors (e.g., hallucinations) entirely from the pipeline, but that it shifts errors into the first phase of asset extraction from the natural language description. Here, a single incorrectly extracted entity can lead to genuine but irrelevant CVEs in subsequent stages of the pipeline, which consumes time and resources. However, it makes the remaining risk visible, verifiable, and suitable for a time-efficient manual review, since the infrastructure (e.g., version numbers, OS, etc.) is typically known.

摘要：在關鍵基礎設施中，操作技術環境通常無法進行主動掃描，然而卻需要主動系統反饋來進行風險評估和合規性檢查。本文提出了一個非侵入性的、基於MCP的多代理管道，將自然語言系統描述轉換為源驗證的知識圖譜和符合NIST OSCAL格式的審計準備文檔，以實現持續自動化合規管理。該架構將基於LLM的推理與針對權威威脅情報來源的確定性知識檢索解耦，降低了虛構漏洞和幻覺攻擊路徑的風險。<lb/>在一個基於證據的水務公用事業合成場景中，該管道實現了0.90的CVE召回率和完美的D3FEND召回率。它生成了一個符合架構的OSCAL系統安全計劃和一份OSCAL安全評估報告。儘管如此，核心見解並不是通過MCP的基礎消除了管道中的錯誤（例如，幻覺），而是將錯誤轉移到從自然語言描述中提取資產的第一階段。在這裡，單個錯誤提取的實體可能導致在管道後續階段中出現真正但不相關的CVE，這會消耗時間和資源。然而，它使剩餘的風險可見、可驗證，並適合進行高效的手動審查，因為基礎設施（例如，版本號、操作系統等）通常是已知的。

##### **PolyUQuest: Verifiable Structure-Aware Web RAG over Heterogeneous Graphs**
2607.08269v1 by Ying Liu, Yi Ye, Quanyu Feng, Mingxi Ye, Mingtao Zhang, Haoyang Li, Chen Jason Zhang, Qing Li

Existing retrieval-augmented generation (RAG) systems treat web pages as flat text, losing the structural and semantic signals encoded in HTML. We present PolyUQuest, a verifiable, structure-aware web RAG framework built on a heterogeneous graph that unifies hyperlink topology between pages, DOM hierarchy within pages, and entity-relation knowledge across pages. A two-tier router dispatches each query to one of three retrieval modes matched to its structural need, including direct block retrieval, cross-page graph traversal, and multi-hop entity reasoning. Every answer is fully verifiable, as each cited block carries its source page, heading path, and entity links so that users can trace any claim back to its structural evidence. We evaluate on the official websites of the Hong Kong Polytechnic University (PolyU), comprising 4,240 pages, 31,086 DOM blocks, 29,119 entities, and 37,680 relations, together with a multi-type evaluation benchmark. PolyUQuest outperforms existing RAG systems in answer correctness, coverage, and faithfulness, while consuming significantly fewer LLM tokens per query. The demonstration provides an interactive interface for inspecting cited answers, comparing retrieval traces across routing modes, and exploring evidence graph paths. PolyUQuest is being prepared for deployment as a student-facing QA service at PolyU.

摘要：現有的檢索增強生成 (RAG) 系統將網頁視為平面文本，失去了在 HTML 中編碼的結構和語義信號。我們提出了 PolyUQuest，一個可驗證的、結構感知的網頁 RAG 框架，建立在一個異構圖上，統一了頁面之間的超鏈接拓撲、頁面內的 DOM 層級，以及跨頁面的實體-關係知識。一個雙層路由器將每個查詢分派到三種檢索模式之一，以滿足其結構需求，包括直接區塊檢索、跨頁面圖遍歷和多跳實體推理。每個答案都是完全可驗證的，因為每個引用的區塊都帶有其來源頁面、標題路徑和實體鏈接，使用者可以追溯任何主張的結構證據。我們在香港理工大學 (PolyU) 的官方網站上進行評估，涵蓋 4,240 頁面、31,086 個 DOM 區塊、29,119 個實體和 37,680 個關係，以及一個多類型評估基準。PolyUQuest 在答案正確性、覆蓋範圍和忠實度方面超越了現有的 RAG 系統，同時每個查詢消耗的 LLM 令牌顯著較少。該演示提供了一個互動界面，用於檢查引用的答案、比較不同路由模式下的檢索痕跡，以及探索證據圖路徑。PolyUQuest 正在準備作為面向學生的 QA 服務在 PolyU 部署。

##### **Compete Then Collaborate: Frontier AI Teachers Build a Verifiable Curriculum to Improve a Coding Student Beyond Imitation**
2607.08255v1 by Miseong Shawn Kim

Large language models increasingly serve as teachers generating training data for smaller students. Prior multi-teacher knowledge distillation methods merge outputs without determining which frontier model teaches best, often relying on an LLM judge biased toward its own outputs. We introduce a compete-then-collaborate framework where four frontier AI teachers (Claude, Codex-GPT, Grok, Gemini) are ranked head-to-head by an execution-based judge (unit tests and stdin-stdout checks) with fairness controls, and then collaborate to build a verifiable curriculum for a student (Qwen2.5-Coder). We report three findings. (1) Under execution verification, all teachers solve standard problems near-perfectly after self-correction (99-100%) due to a saturation effect, but harder competition problems separate them (Gemini 77% > Claude 69% = Codex 69% > Grok 50%); however, the robust student-side results do not depend on teacher ranking. (2) Imitation (SFT) on verified solutions does not improve, and can degrade, an already-competent student at 7B and 32B (e.g., from 76.7% to 72.7% on MBPP-test, and 5.9% to 2.9% on competition problems). (3) Using the same collaborative curriculum as a reinforcement learning with verifiable rewards (RLVR) environment improves the student (from 5.9% to 8.8% peak on competition problems, a +49% relative gain), reversing SFT's direction. The value of AI-teacher collaboration lies not in pooling answers to imitate, but in jointly constructing a verifiable environment where the student learns by doing. We release a reproducible on-prem pipeline (NVIDIA GB10) with framework patches for running GRPO on a bleeding-edge stack.

摘要：大型語言模型越來越多地充當教師，為較小的學生生成訓練數據。以往的多教師知識蒸餾方法在合併輸出時未能確定哪個前沿模型教學效果最佳，通常依賴於一個偏向自身輸出的LLM評判者。我們引入了一個競爭後合作的框架，其中四個前沿AI教師（Claude, Codex-GPT, Grok, Gemini）由一個基於執行的評判者（單元測試和標準輸入-標準輸出檢查）進行頭對頭排名，並設有公平控制，然後合作構建一個可驗證的課程供學生（Qwen2.5-Coder）使用。我們報告了三個發現。(1) 在執行驗證下，所有教師在自我修正後幾乎完美地解決標準問題（99-100%），這是由於飽和效應，但更難的競爭問題將它們區分開來（Gemini 77% > Claude 69% = Codex 69% > Grok 50%）；然而，穩健的學生端結果不依賴於教師排名。(2) 在經過驗證的解決方案上進行模仿（SFT）並不會改善，甚至可能會降低已經合格的7B和32B學生的表現（例如，在MBPP測試中從76.7%降至72.7%，在競爭問題中從5.9%降至2.9%）。(3) 使用相同的合作課程作為具有可驗證獎勵的強化學習環境（RLVR）能改善學生的表現（在競爭問題上從5.9%提升至8.8%的峰值，相對增長49%），逆轉了SFT的方向。AI教師合作的價值不在於聚合答案以進行模仿，而在於共同構建一個可驗證的環境，讓學生通過實踐學習。我們發布了一個可重現的本地管道（NVIDIA GB10），並附帶了運行GRPO的框架補丁，以支持最新技術堆棧。

##### **SQuaD-SQL: Efficient Text-to-SQL with Small Language Models via LLM-Guided Knowledge Distillation**
2607.08161v1 by Wangyu Wu, Xiaojian Lin, Rong Fu, Zaiyang Yu, Xuhang Chen, Wenjun Yu, Zhenhong Chen

Text-to-SQL is a fundamental task in natural language processing that enables users to interact with structured databases using natural language. While large language models (LLMs) have demonstrated remarkable performance on this task, their substantial computational requirements hinder deployment in resource-constrained settings. In this paper, we introduce SQuaD-SQL (Small-Qualified and Distilled for SQL), a novel approach that empowers small language models (SLMs) to approach the performance of LLMs on the Text-to-SQL task while significantly improving efficiency through knowledge distillation and synthetic data generation. Our method comprises three key components: (1) LLM-based synthetic data generation, where structured knowledge is extracted from LLMs via carefully designed prompting strategies; (2) parameter-efficient fine-tuning, enabling full model training on a single consumer-grade GPU; and (3) domain-adaptive fine-tuning, where domain-specific synthetic data further enhances performance in targeted domains. Experiments on the WikiSQL dataset demonstrate that SQuaD-SQL achieves an execution accuracy of 86.9% on the test set, approaching the performance of LLMs while offering faster inference and lower memory usage. These results suggest that, with proper training strategies, SLMs can serve as practical and efficient alternatives for Text-to-SQL applications in resource-limited environments.

摘要：Text-to-SQL 是自然語言處理中的一項基本任務，使得用戶能夠使用自然語言與結構化數據庫進行互動。雖然大型語言模型（LLMs）在這項任務上表現出色，但其龐大的計算需求限制了在資源受限環境中的部署。在本文中，我們介紹了 SQuaD-SQL（Small-Qualified and Distilled for SQL），這是一種新穎的方法，讓小型語言模型（SLMs）在 Text-to-SQL 任務上接近 LLMs 的性能，同時通過知識蒸餾和合成數據生成顯著提高效率。我們的方法包含三個關鍵組件：（1）基於 LLM 的合成數據生成，通過精心設計的提示策略從 LLM 中提取結構化知識；（2）參數高效的微調，使得在單個消費級 GPU 上進行完整模型訓練成為可能；以及（3）領域自適應微調，其中領域特定的合成數據進一步提升在目標領域的性能。對 WikiSQL 數據集的實驗表明，SQuaD-SQL 在測試集上的執行準確率達到 86.9%，接近 LLM 的性能，同時提供更快的推理速度和更低的內存使用。這些結果表明，通過適當的訓練策略，SLMs 可以作為資源有限環境中 Text-to-SQL 應用的實用且高效的替代方案。

##### **Answer Set Programming Energised! End-to-End Neurosymbolic Reasoning and Learning with ASP and Energy Based Models**
2607.08136v1 by Jakob Suchan, Julius Monsen, Salim Baloch, Mehul Bhatt

We present a general neurosymbolic reasoning and learning methodology based on a modular integration of answer set programming with an energy based model substrate. Key contributions are: (1) supporting joint optimisation in the continuous latent space through explicit ASP-based declarative semantics fully incorporating background knowledge, constraints, non-monotonic inference; and (2) advancing recent works at the interface of answer sets, probabilistic logic, and answer set modulo theories by providing a generalised model and practical platform for ASP-centric robust, end-to-end training for applications in dynamic domains (e.g., involving perception and interaction). We provide a practical implementation, and demonstrate basic use and application (with MNIST), and evaluate with the visual question-answering benchmark Clevr and the multi-object tracking benchmark MOT.

摘要：我們提出了一種基於模組化整合答案集程式設計與基於能量模型基底的一般神經符號推理與學習方法。主要貢獻包括：(1) 通過明確的基於ASP的宣告語義，完全納入背景知識、約束條件和非單調推理，支持在連續潛在空間中的聯合優化；以及(2) 通過提供一個通用模型和實用平台，推進在答案集、概率邏輯和答案集模組理論交界處的近期研究，實現以ASP為中心的穩健端到端訓練，應用於動態領域（例如，涉及感知和互動）。我們提供了一個實用的實現，並展示基本的使用和應用（使用MNIST），並通過視覺問答基準Clevr和多物體追蹤基準MOT進行評估。

##### **COALA: Robust Contextualized Speech-augmented Language Modeling for ASR via Contrastive Regularizer and Biasing Score Estimation**
2607.08117v1 by Jhih-Rong Guo, Bi-Cheng Yan, Tien-Hong Lo, Berlin Chen

Contextual biasing seeks to integrate external knowledge into automatic speech recognition (ASR) systems to accurately recognize domain-specific entities. In this paper, we propose COALA (Contextualized ASR Leveraging Biasing Scoring), a robust framework designed to enhance speech-augmented language models (SLMs) in complex multi-entity scenarios. Considering the inherent context-window limitations of SLMs, identifying relevant target entities from a large-scale biasing list is crucial for effective recognition. To this end, COALA maps SLM latent representations into a specialized discriminative space to quantify the matching intensity between audio segments and candidate entities. Furthermore, we address the training collapse in prior study when handling multi-target utterances-where multiple rare words co-occur. Experimental results on the LibriSpeech benchmark demonstrate that COALA consistently achieves superior contextual biasing performance across various biasing list scales.

摘要：上下文偏見旨在將外部知識整合到自動語音識別（ASR）系統中，以準確識別特定領域的實體。<lb/>在本文中，我們提出了COALA（上下文化的ASR利用偏見評分），這是一個旨在增強語音增強語言模型（SLMs）在複雜多實體場景中的穩健框架。<lb/>考慮到SLMs固有的上下文窗口限制，從大規模偏見列表中識別相關目標實體對於有效識別至關重要。<lb/>為此，COALA將SLM潛在表示映射到一個專門的區分空間，以量化音頻片段與候選實體之間的匹配強度。<lb/>此外，我們解決了先前研究中處理多目標發話時的訓練崩潰問題——即多個稀有詞同時出現的情況。<lb/>在LibriSpeech基準測試上的實驗結果顯示，COALA在各種偏見列表規模中始終實現了卓越的上下文偏見性能。

##### **PARA-PV: Physics-Aware Retrieval-Augmented PV Prediction Based on Frozen Foundation Model and Distribution Shift Correction**
2607.08079v1 by Hang Fan, Weican Liu, Ying Lu, Dunnan Liu, Long Cheng, Wei Wei

Accurate photovoltaic (PV) power forecasting is essential for reliable grid dispatch and renewable energy integration, yet it remains challenging because PV generation is jointly shaped by weather variability, day-night transitions, regime-dependent dynamics, and strict physical constraints. We propose PARA-PV, a Physics-Aware Retrieval-Augmented framework that embeds physical knowledge throughout the forecasting process. The framework first encodes multivariate PV observations into patch-level representations and, through a physics-aware retrieval-augmented learner, retrieves historical patches and analog trajectories that are consistent with the current window in temporal shape, power level, PV operating state, and intra-day period; this yields a physically grounded base forecast. To supplement local memory with broader temporal knowledge, the base forecast is then calibrated against a frozen Chronos time-series foundation-model prior through a lightweight residual adapter, so that general temporal regularities are adapted to PV-specific dynamics without overriding the physically grounded prediction. Because residual conditional distribution shifts persist when weather and diurnal regimes change, a physics-aware distribution shift correction module subsequently adjusts the preliminary forecast using power, weather, timestamp, and day/night conditions, applying gated mean-shift and scale corrections selectively. Finally, a physics-constrained loss function partitions the samples into peak, ramping, night-time, and regular regimes and adaptively reweights their error contributions, preventing the dominant regular regime from suppressing learning of operationally critical states. Our code is available at https://github.com/weican1103/PARA-PV.

摘要：準確的光伏（PV）發電預測對於可靠的電網調度和可再生能源整合至關重要，但由於光伏發電受到天氣變化、晝夜轉換、制度依賴動態和嚴格物理限制的共同影響，這仍然是一項挑戰。我們提出了PARA-PV，一個物理知識嵌入的檢索增強框架，該框架在整個預測過程中嵌入物理知識。該框架首先將多變量光伏觀測編碼為區塊級表示，然後通過一個物理知識檢索增強學習器，檢索與當前時間窗口在時間形狀、功率水平、光伏運行狀態和日內周期一致的歷史區塊和類似軌跡；這產生了一個物理基礎的基本預測。為了用更廣泛的時間知識補充本地記憶，基本預測隨後通過輕量級的殘差適配器與一個固定的Chronos時間序列基礎模型進行校準，以便將一般的時間規律適應於光伏特定的動態，而不會覆蓋物理基礎的預測。由於當天氣和晝夜制度變化時，殘差條件分佈的變化持續存在，因此一個物理知識分佈變化修正模塊隨後使用功率、天氣、時間戳和晝夜條件調整初步預測，選擇性地應用閘控均值偏移和比例修正。最後，一個物理約束的損失函數將樣本劃分為峰值、上升、夜間和常規制度，並自適應地重新加權它們的誤差貢獻，防止主導的常規制度壓制對操作關鍵狀態的學習。我們的代碼可在https://github.com/weican1103/PARA-PV獲得。

##### **Holographic Neural PCFG for Unsupervised Parsing**
2607.08063v1 by Ryosuke Yamaki, Daichi Mochihashi, Nobutaka Shimada, Tadahiro Taniguchi

Unsupervised constituency parsing aims to accurately induce latent tree structures from raw text alone. Recent neural parameterizations of PCFGs achieve strong performance in both supervised and unsupervised parsing, yet rely on high-capacity black-box networks for rule scoring -- as exemplified by the Neural PCFG family -- leaving rule probabilities without an interpretable mathematical form. In this paper, we propose Holographic Neural PCFG (Hol-PCFG), which recasts PCFG rule scoring as algebraic relation modeling among grammar-symbol embeddings. Hol-PCFG adapts Holographic Embeddings (Nickel et al., 2016), which scores knowledge-graph triples via circular correlation, to the left-child, right-child, and lexical-emission relations over torus-constrained embeddings, giving every rule probability a closed form that carries the intrinsic structure of grammar rules by construction. Hol-PCFG achieves state-of-the-art parsing performance in six languages while cutting rule-scoring parameters by 99.94% relative to the baseline model and training more stably. Additionally, we demonstrate that Hol-PCFG can parse Japanese directly from characters without any morphological segmentation, retaining nearly the same morpheme-level performance.

摘要：無監督的成分分析旨在僅從原始文本中準確地誘導潛在的樹狀結構。最近的神經參數化PCFG在監督和無監督解析中都取得了強勁的表現，但依賴於高容量的黑箱網絡來進行規則評分——以神經PCFG家族為例——使得規則概率沒有可解釋的數學形式。在本文中，我們提出了全息神經PCFG（Hol-PCFG），它將PCFG規則評分重新表述為語法符號嵌入之間的代數關係建模。Hol-PCFG適應了全息嵌入（Nickel et al., 2016），通過圓形相關性對知識圖譜三元組進行評分，並應用於圓環約束嵌入的左子節點、右子節點和詞彙發射關係，從而使每個規則概率都具有封閉形式，並通過構造承載語法規則的內在結構。Hol-PCFG在六種語言中實現了最先進的解析性能，同時將規則評分參數相對於基線模型減少了99.94%，並且訓練更加穩定。此外，我們展示了Hol-PCFG可以直接從字符解析日語，而無需任何形態學分段，並保持幾乎相同的語素級別性能。

##### **DKDNet: Dual Knowledge and Data-Driven Network for Cross-Domain Automatic Modulation Classification**
2607.08031v1 by Shuang Wang, Chenxu Wang, Hantong Xing, Hanlin Mo, Lirong Han, Licheng Jiao

The dynamics of communication environments induce significant distribution shifts across domains, challenging the generalization of deep learning-based automatic modulation classification (AMC) models. While existing UDA methods alleviate this problem by aligning source and target features, they give limited consideration to modulation-specific structures that remain informative across domain conditions. In this paper, we consider signal prior knowledge, grounded in communication protocols and physical principles, as a potential way to enhance cross-domain representation learning. Given that different priors may vary in modulation discriminability, domain stability, and complementarity, this paper first analyzes five commonly adopted signal representations that instantiate different signal priors. From them, in-phase/quadrature (IQ), amplitude--phase (AP), and autocorrelation function (ACF) are selected as compact prior-guided inputs. Based on that, a dual knowledge and data-driven network (DKDNet) is proposed for cross-domain AMC. The multi-representation feature encoder (MRFE) and dynamic lightweight fusion unit (DLFU) are designed to achieve unified representation learning and adaptive feature fusion, and the resulting fused features are optimized with modulation classification and adversarial domain alignment objectives. Experiments on both simulated and public datasets validate the rationality of the prior selection and demonstrate the superiority of the proposed method.

摘要：通訊環境的動態引起了跨領域的重要分佈變化，挑戰了基於深度學習的自動調變分類（AMC）模型的泛化能力。雖然現有的無監督領域適應（UDA）方法通過對齊源特徵和目標特徵來緩解這一問題，但它們對於在不同領域條件下仍然具有信息性的調變特定結構考慮有限。在本文中，我們考慮基於通訊協議和物理原則的信號先驗知識，作為增強跨領域表示學習的潛在方法。考慮到不同的先驗可能在調變可辨識性、領域穩定性和互補性方面有所不同，本文首先分析了五種常用的信號表示，這些表示實現了不同的信號先驗。在這些表示中，選擇了同相/正交（IQ）、幅度-相位（AP）和自相關函數（ACF）作為緊湊的先驗引導輸入。基於此，提出了一種雙重知識和數據驅動網絡（DKDNet）用於跨領域AMC。多表示特徵編碼器（MRFE）和動態輕量融合單元（DLFU）被設計用於實現統一的表示學習和自適應特徵融合，並且生成的融合特徵通過調變分類和對抗領域對齊目標進行優化。在模擬和公共數據集上的實驗驗證了先驗選擇的合理性，並展示了所提方法的優越性。

##### **Concretized Proposition Prompting Resolves Composition-Knowledge Dichotomy in Large Language Models**
2607.08018v1 by Changhun Lee, Minguk Jeon, Jongkyung Shin, Chiehyeon Lim

LLMs often struggle to balance compositionality with knowledgeability, a challenge we define as Composition-Knowledge Dichotomy. To address this, we propose Concretized Proposition Prompting (CPP), a framework that explicitly concretizes propositions relevant to questions. The results demonstrate that CPP significantly enhances reasoning performance, particularly in medical benchmarks where precise knowledge is paramount, while being competitive on math benchmarks where deductive reasoning is prioritized. Additional experiments reveal that CPP is scalable to various foundation models and parameter sizes, being a fundamental paradigm that bridges the gap between composition- and knowledge-based approaches. Consequently, CPP resolves the composition-knowledge dichotomy by providing a solid foundation for logically organized and factually grounded reasoning.

摘要：LLMs 經常難以平衡組合性與知識性，我們將這一挑戰定義為組合-知識二分法。為了解決這個問題，我們提出了具體化命題提示（CPP），這是一個明確具體化與問題相關命題的框架。結果顯示，CPP 顯著提升了推理表現，特別是在對精確知識要求極高的醫學基準測試中，同時在以演繹推理為重點的數學基準測試中也具競爭力。額外的實驗顯示，CPP 能夠擴展到各種基礎模型和參數大小，成為一個基本範式，橋接了基於組合和基於知識的方法之間的差距。因此，CPP 通過提供一個邏輯組織和事實基礎推理的堅實基礎，解決了組合-知識二分法。

##### **Can We Trust LLM's Logic? Quantifying Uncertainty, Coherence, and Robustness via a Graph-Based Framework**
2607.08017v1 by Riccardo Revalor, Jalees Rehman, Debjit Pal

Large-Language Models (LLMs) can be prone to flawed and unfaithful reasoning that decoding strategies like Self-Consistency (SC) fail to detect as they evaluate only final-answer agreement while ignoring the logical validity of intermediate steps. This raises three fundamental questions: How can we reliably quantify uncertainty in LLM reasoning? Can semantic, structural, and causal awareness select more faithful reasoning compared to naïve majority voting? and How robust is reasoning topology under adversarial conditions? To address these questions, we introduce GRAPHEVAL, a graph-based reasoning framework that re-frames uncertainty quantification (UQ) as a holistic reasoning fidelity problem. We propose a novel UQ metric, Graph Reasoning Coherence Score (GRCS), that quantifies semantic-structural consensus of the reasoning space and captures pathological mode collapse and confident hallucinations. We find that GRCS is the only metric that is consistently negatively correlated with reasoning faithfulness across both more capable and smaller models. Additionally, we introduce Graph Self-Consistency (GSC), a medoid-based decoding strategy that trades nominal accuracy for reasoning fidelity, exposing the degree to which SC is inflated by unfaithful lucky guesses in smaller models, while preserving or improving accuracy in more capable ones. Finally, through adversarial medoid ablation, we demonstrate that the GSC-selected path acts as a "load-bearing path" and forcing models away from it degrades reasoning faithfulness and, in targeted cases, causes drops in accuracy.

摘要：大型語言模型（LLMs）可能會出現錯誤和不可靠的推理，而像自我一致性（SC）這樣的解碼策略無法檢測到這一點，因為它們僅評估最終答案的一致性，卻忽略了中間步驟的邏輯有效性。這引發了三個基本問題：我們如何可靠地量化LLM推理中的不確定性？語義、結構和因果意識是否能選擇出比天真的多數投票更可靠的推理？推理拓撲在對抗條件下的穩健性如何？為了解決這些問題，我們介紹了GRAPHEVAL，一個基於圖的推理框架，將不確定性量化（UQ）重新定義為一個整體推理忠實度問題。我們提出了一種新穎的UQ指標，圖推理一致性得分（GRCS），它量化了推理空間的語義-結構共識並捕捉病態模式崩潰和自信的幻覺。我們發現GRCS是唯一一個在更強大和較小模型中都與推理忠實度持續負相關的指標。此外，我們介紹了圖自我一致性（GSC），這是一種基於中位數的解碼策略，將名義準確性與推理忠實度進行權衡，揭示了在較小模型中，SC因不可靠的幸運猜測而被膨脹的程度，同時在更強大的模型中保持或提高準確性。最後，通過對抗性中位數消融，我們證明了GSC選擇的路徑充當了“承重路徑”，而將模型強行推離這條路徑會降低推理忠實度，並在特定情況下導致準確性下降。

##### **Reaction-network reasoning with frontier models for experimentally confirmed catalyst-selectivity hypotheses**
2607.08003v1 by Sutanay Choudhury, Anwesha Banerjee, Udishnu Sanyal, Jorin Dawidowicz, Chiezugolum Ijeoma Odilinye, Jesun Firoz, Liney Arnadottir, Simone Raugei, Johannes Lercher, Arnab Dutta

Catalysts are essential for sustainable chemical manufacturing, yet discovering novel architectures remains a bottleneck dominated by trial-and-error experimentation and computationally intensive screening. In complex reactions such as electrochemical carbon dioxide reduction, product selectivity is governed by dynamic interfacial, electrolyte, and potential factors as well as kinetic pathway competition. Conventional descriptor-based machine learning and computational potentials struggle to resolve these mechanistic branch points, primarily relying on static ground-state descriptors or bulk structural correlations rather than end-to-end topological pathway analysis. Here, we show that frontier language models, when strictly constrained to reason over explicit reaction networks, can discover novel catalysts by identifying the physical levers that govern pathway competition. We developed a human-AI co-thinking framework that enforces network invariance to extract testable hypotheses from complex chemical graphs. Applied to CO2 electroreduction, the framework identified ketene desorption and hydroxide capture as the acetate-forming pathway, and predicted a distinct adsorbed CO and CH2 coupling route to ketene. By isolating actionable control levers, specifically local alkalinity, controlled iron incorporation, and restricted interfacial proton-donor accessibility, the framework guided the prospective synthesis of a copper-iron oxide catalyst demonstrating a threefold increase in acetate selectivity over matched Cu-rich baselines. This mechanism-guided reasoning architecture shifts the computational paradigm from retrospective statistical prediction to forward-looking hypothesis generation, providing a broadly applicable blueprint for mechanism-guided materials discovery.

摘要：催化劑對於可持續化學製造至關重要，但發現新穎的結構仍然是一個瓶頸，這一過程主要依賴於試錯實驗和計算密集型篩選。在複雜反應中，如電化學二氧化碳還原，產物選擇性受動態界面、電解質和潛在因素以及動力學途徑競爭的影響。傳統的基於描述符的機器學習和計算潛能難以解析這些機制分支點，主要依賴靜態基態描述符或體積結構相關性，而不是端到端的拓撲途徑分析。在這裡，我們展示了前沿語言模型在嚴格限制於明確反應網絡進行推理時，可以通過識別控制途徑競爭的物理杠杆來發現新穎的催化劑。我們開發了一個人類-AI共同思考框架，強制執行網絡不變性，以從複雜的化學圖中提取可測試的假設。應用於CO2電還原，該框架識別了酮烯脫附和氫氧化物捕獲作為生成醋酸的途徑，並預測了一條將吸附的CO和CH2耦合到酮烯的不同路徑。通過孤立可操作的控制杠杆，具體包括局部鹼度、控制鐵的掺入和限制界面質子供體的可及性，該框架指導了銅鐵氧化物催化劑的前瞻性合成，顯示出相較於匹配的銅富基準，醋酸選擇性提高了三倍。這種機制引導的推理架構將計算範式從回顧性統計預測轉變為前瞻性假設生成，為機制引導的材料發現提供了一個廣泛適用的藍圖。

##### **path_boost: A Python Package for Interpretable Graph-Level Prediction using Path-Based Gradient Boosting**
2607.07935v1 by Claudio Meggio, Johan Pensar, Riccardo De Bin

We present path_boost, a Python package for interpretable supervised learning on graph-structured input data. The package implements PathBoost, a gradient boosting algorithm that automatically discovers predictive labeled paths within graphs during the learning process. Unlike graph neural networks, which are generally difficult to interpret, PathBoost produces an additive prediction model over path-based features that explicitly reveals which substructures drive predictions. To avoid an exhaustive enumeration of all possible paths, the algorithm iteratively selects and extends paths during learning based on their predictive power, using boosting to combine weak learners into a strong ensemble. The package supports both regression and binary classification. Key features include compatibility with scikit-learn workflows, support for custom base learners and selectors, automatic starting node selection, parallel training across anchor nodes, and built-in variable importance computation. We demonstrate PathBoost on molecular property prediction of transition metal compounds, where atoms serve as nodes and bonds as edges, and further benchmark PathBoost against an established graph neural network and a graph kernel method across six molecular datasets. The package is available on PyPI and GitHub under an open-source license.

摘要：我們介紹 path_boost，一個用於圖結構輸入數據的可解釋監督學習的 Python 套件。<lb/>該套件實現了 PathBoost，一種梯度提升算法，能夠在學習過程中自動發現圖中的預測標記路徑。<lb/>與通常難以解釋的圖神經網絡不同，PathBoost 生成了一個基於路徑特徵的加法預測模型，明確揭示了哪些子結構驅動預測。<lb/>為了避免對所有可能路徑的全面列舉，該算法在學習過程中根據其預測能力迭代選擇和擴展路徑，使用提升法將弱學習者組合成強集成。<lb/>該套件支持回歸和二元分類。<lb/>主要特點包括與 scikit-learn 工作流程的兼容性、支持自定義基學習者和選擇器、自動起始節點選擇、跨錨點的並行訓練，以及內置的變量重要性計算。<lb/>我們在過渡金屬化合物的分子性質預測上演示了 PathBoost，其中原子作為節點，鍵作為邊，並進一步將 PathBoost 與一個已建立的圖神經網絡和一種圖核方法在六個分子數據集上進行基準測試。<lb/>該套件在 PyPI 和 GitHub 上以開源許可證提供。

##### **Multimodal Unlearning Across Vision, Language, Video, and Audio: Survey of Methods, Datasets, and Benchmarks**
2607.07907v1 by Nobin Sarwar, Shubhashis Roy Dipta, Zheyuan Liu, Vaidehi Patil

With the growing adoption of VLMs, DMs, LLMs, and AFMs, these multimodal foundation models can inadvertently encode sensitive, copyrighted, biased, or unsafe cross-modal associations that originate from their training data. Retraining after deletion requests or policy updates is often impractical, and targeted forgetting remains difficult because knowledge is distributed across shared representations. Multimodal unlearning addresses this challenge by enabling selective removal across modalities while retaining overall utility. This survey offers a unified, system-oriented view of multimodal unlearning across vision, language, audio, and video, grounded in recent advances, emerging applications, and open problems. Our taxonomy enables systematic comparison across model architectures and modalities, clarifying trade-offs among deletion strength, retention, efficiency, reversibility, and robustness. This survey highlights open problems and practical considerations to support future research and deployment of multimodal unlearning. We release a curated repository: https://smsnobin77.github.io/Awesome-Multimodal-Unlearning/

摘要：隨著 VLM、DM、LLM 和 AFM 的日益普及，這些多模態基礎模型可能無意中編碼來自其訓練數據的敏感、受版權保護、偏見或不安全的跨模態關聯。 在刪除請求或政策更新後進行重新訓練通常是不切實際的，針對性的遺忘仍然困難，因為知識分佈在共享表示中。 多模態遺忘通過在保留整體效用的同時實現跨模態的選擇性移除來解決這一挑戰。 本調查提供了一個統一的、以系統為導向的多模態遺忘觀點，涵蓋視覺、語言、音頻和視頻，基於最近的進展、新興應用和未解決的問題。 我們的分類法使不同模型架構和模態之間的系統性比較成為可能，澄清了刪除強度、保留、效率、可逆性和穩健性之間的權衡。 本調查突出了未解決的問題和實際考量，以支持未來多模態遺忘的研究和部署。 我們發布了一個策劃的資源庫：https://smsnobin77.github.io/Awesome-Multimodal-Unlearning/

##### **Mechanistic Interpretability of LLM Jailbreaks via Internal Attribution Graphs**
2607.07903v1 by Anupam Wagle, Ifrat Ikhtear Uddin, Chaowei Zhang, Longwei Wang

Large language models (LLMs) exhibit remarkable capabilities but remain highly vulnerable to adversarial prompts and jailbreak attacks. Existing approaches primarily analyze these failures through input-output behaviors or attribution methods, offering limited insight into how adversarial perturbations alter the model's internal reasoning. Consequently, the mechanisms underlying unsafe or incorrect behaviors remain poorly understood. We introduce a mechanistic framework for diagnosing LLM vulnerabilities using paired internal computation graphs, which represent prompt-specific inference as structured causal interactions among latent features. By constructing and aligning computation graphs for clean and attacked prompts, we reveal that adversarial attacks induce systematic transformations of internal reasoning, including suppression of safety-relevant components, emergence of attack-specific features, and rerouting of computation paths. Building on this representation, we propose a unified framework that (i) decomposes computation into invariant, suppressed, and emergent structures, (ii) identifies recurring vulnerability motifs associated with failure modes, and (iii) performs causal interventions on nodes, paths, and subgraphs to directly evaluate their contributions to attack success. This enables a transition from descriptive attribution to causal diagnosis of model failures. Experiments across multiple open-source LLMs and diverse adversarial and jailbreak benchmarks demonstrate that structural deviations in internal computation graphs strongly correlate with unsafe behaviors. Furthermore, targeted interventions on identified vulnerability motifs improve model robustness, establishing internal computation graphs as a principled foundation for understanding, diagnosing, and mitigating LLM vulnerabilities.

摘要：大型語言模型（LLMs）展現出卓越的能力，但仍對對抗性提示和越獄攻擊高度脆弱。現有的方法主要通過輸入-輸出行為或歸因方法分析這些失敗，對於對抗性擾動如何改變模型的內部推理提供了有限的洞察。因此，導致不安全或不正確行為的機制仍然不甚了解。我們引入了一個機械框架，用於診斷LLM脆弱性，利用成對的內部計算圖，這些圖表示特定提示推理作為潛在特徵之間的結構性因果互動。通過構建和對齊乾淨和攻擊的提示的計算圖，我們揭示了對抗性攻擊引起的內部推理的系統性轉變，包括對安全相關組件的抑制、攻擊特定特徵的出現，以及計算路徑的重新路由。在這一表示的基礎上，我們提出了一個統一框架，該框架（i）將計算分解為不變、抑制和新興結構，（ii）識別與失敗模式相關的重複脆弱性主題，以及（iii）對節點、路徑和子圖進行因果干預，以直接評估它們對攻擊成功的貢獻。這使得從描述性歸因轉向模型失敗的因果診斷成為可能。針對多個開源LLM和各種對抗性及越獄基準的實驗表明，內部計算圖中的結構偏差與不安全行為之間存在強烈的相關性。此外，對識別出的脆弱性主題進行有針對性的干預提高了模型的穩健性，確立了內部計算圖作為理解、診斷和減輕LLM脆弱性的原則基礎。

##### **A Graph Neural Network Model for Real-Time Gesture Recognition Based on sEMG Signals**
2607.07850v1 by Pragatheeswaran Vipulanandan, Kamal Premaratne, Manohar Murthi

For seemless control of advanced hand prostheses and augmented reality, accurate and immediate hand gestures recognition is essential. Surface electromyography (sEMG) signals obtained from the forearm are commonly employed for this purpose. In this paper, we present a novel approach for sEMG representation that utilizes graph networks which contain information about muscle activation patterns in the forearm. Based on these graph networks, we have developed a machine learning algorithm capable of real-time hand gesture recognition using a graph neural network. The algorithm's performance was evaluated using sEMG signals acquired from myoband, which has 8 electrodes placed around the forearm, involving 8 healthy subjects. The proposed method demonstrated an average classification accuracy of 99\%, surpassing the performance of state-of-the-art techniques. The average time for both graph construction and prediction stood at 48ms utilizing a M1 pro CPU, rendering the approach well-suited for real-time applications.

摘要：對於高級手部義肢和擴增實境的無縫控制，準確且即時的手勢識別是必不可少的。從前臂獲取的表面肌電圖 (sEMG) 信號通常用於此目的。在本文中，我們提出了一種新穎的 sEMG 表示方法，利用包含前臂肌肉激活模式信息的圖形網絡。基於這些圖形網絡，我們開發了一種能夠使用圖神經網絡進行即時手勢識別的機器學習算法。該算法的性能是使用從 myoband 獲取的 sEMG 信號進行評估的，myoband 在前臂周圍放置了 8 個電極，涉及 8 名健康受試者。所提出的方法顯示出平均分類準確率達到 99\%，超越了當前最先進技術的性能。圖形構建和預測的平均時間為 48 毫秒，使用 M1 pro CPU，使得該方法非常適合即時應用。

##### **VectorizationLLM: Smart Vectorization Based AI Assistant**
2607.07846v1 by Ryan Duke

VectorizationLLM is a specialized Large Language Model based on Google open-weight LLMs. The model is designed to assist students to learn smart vectorization, time/wave vector analysis, piecewise functions, Fourier analysis, and differential equations in MATLAB. The course application is CTEC 247: Applied Computational Analysis II by the Department of Electrical & Computer Engineering Technology at New York Institute of Technology Old Westbury. The LLM model is designed to be an instructive assistant, providing detailed explanations of concepts with examples from in-class notes without providing direct answers to questions. The model is designed with a RAG (Retrieval Augmented Generation) knowledge base and system prompt architecture. Examples in both code, text, and images are provided in the LLM responses.

摘要：VectorizationLLM 是一個專門的基於 Google 開放權重 LLM 的大型語言模型。<lb/>該模型旨在幫助學生學習智能向量化、時間/波向量分析、分段函數、傅里葉分析和 MATLAB 中的微分方程。<lb/>課程應用為 CTEC 247：應用計算分析 II，由紐約科技學院舊西堡的電氣與計算機工程技術系提供。<lb/>該 LLM 模型旨在成為一個教學助手，提供詳細的概念解釋，並用課堂筆記中的例子來說明，而不直接回答問題。<lb/>該模型設計有 RAG（檢索增強生成）知識庫和系統提示架構。<lb/>LLM 回應中提供了代碼、文本和圖像的示例。

##### **Co-LMLM: Continuous-Query Limited Memory Language Models**
2607.07707v1 by Yair Feldman, Linxi Zhao, Nathan Godey, Dongyoung Go, Yilun Hua, Kilian Q. Weinberger, Jennifer J. Sun, Yoav Artzi

Limited memory language models (LMLMs) externalize factual knowledge during pretraining to a knowledge base (KB), rather than memorizing it in their weights. During generation, the model then fetches knowledge from the KB as needed. This recently introduced paradigm provides multiple advantages, including knowledge control capabilities that remain beyond conventional LLMs. We propose continuous-query LMLM (CO-LMLM), where the KB pairs continuous keys with textual knowledge values, a significant departure from prior reliance on relational KB and queries. CO-LMLM generates flexible vector queries at minimal cost, while still integrating human-readable and attributable retrieved knowledge into its generation. We pair this design with an annotation pipeline that tags free-form factual spans in arbitrary text, removing prior work's restriction to Wikipedia. Across pretraining on Wikipedia and FineWeb-Edu and at multiple model scales, CO-LMLM outperforms prior LMLMs and vanilla LLMs in both perplexity and factual precision. At 360M scale, this includes lower perplexity than models pretrained on 40x more data, and SimpleQA-verified performance that is in line with gpt-4o-mini and higher than Claude Sonnet 4.5.

摘要：有限記憶語言模型（LMLMs）在預訓練期間將事實知識外部化到知識庫（KB），而不是將其記憶在權重中。<lb/>在生成過程中，模型根據需要從知識庫中獲取知識。<lb/>這一最近引入的範式提供了多種優勢，包括超越傳統大型語言模型的知識控制能力。<lb/>我們提出了連續查詢 LMLM（CO-LMLM），其中知識庫將連續鍵與文本知識值配對，這與先前對關係知識庫和查詢的依賴有著顯著的不同。<lb/>CO-LMLM 以最低的成本生成靈活的向量查詢，同時仍然將可讀且可歸因的檢索知識整合到其生成中。<lb/>我們將這一設計與一個標註管道相結合，該管道標記任意文本中的自由形式事實範圍，消除了先前工作對維基百科的限制。<lb/>在對維基百科和 FineWeb-Edu 進行預訓練以及在多個模型規模下，CO-LMLM 在困惑度和事實精確度上均超越了先前的 LMLMs 和原始 LLMs。<lb/>在 360M 規模下，這包括比在 40 倍更多數據上預訓練的模型更低的困惑度，並且 SimpleQA 驗證的性能與 gpt-4o-mini 相符，且高於 Claude Sonnet 4.5。

##### **From Noisy Traces to Root Causes: Structural Trajectory Analysis and Causal Extraction for Agent Optimization**
2607.07702v1 by Ying Chang, Jiahang Xu, Xuan Feng, Chenyuan Yang, Peng Cheng, Yuqing Yang

The optimization of long-horizon agents increasingly relies on reflection-based mechanisms, where a large language model (LLM) acts as an optimizer to diagnose agent failures and improve agent policies. However, real execution traces are difficult to use directly for optimization: large trace collections are often redundant and heterogeneous, making optimization inefficient and prone to overfitting to low-value failures; meanwhile, each individual trajectory also contains many irrelevant steps, while naive context reduction methods such as truncation or sliding windows can discard causally important evidence and produce misleading optimization signals. To resolve this dilemma, we introduce STRACE (Structural TRajectory Analysis and Causal Extraction), a framework that constructs high signal-noise optimization contexts for more precise and effective optimization. At the batch level, STRACE mines failure patterns to filter redundant traces and retain representative failures; within each selected trace, it performs causal localization over a textual dependency graph to remove non-causal steps and identify the true root-cause module for optimization. Empirical results demonstrate that STRACE significantly outperforms standard context-filtering baselines. Notably, on a challenging formal verification task (VeruSAGE-Bench), it successfully optimizes human-expert designed agents, delivering $1.4\times$ success-rate improvement (42.5% to 58.5%). The code is available at https://github.com/moomight/STRACE .

摘要：長期代理的優化越來越依賴於基於反思的機制，其中大型語言模型（LLM）作為優化器來診斷代理失敗並改進代理策略。<lb/>然而，實際執行痕跡難以直接用於優化：大量的痕跡集合往往是冗餘和異質的，這使得優化效率低下並容易過擬合於低價值的失敗；同時，每個單獨的軌跡也包含許多不相關的步驟，而簡單的上下文縮減方法，如截斷或滑動窗口，可能會丟棄因果上重要的證據並產生誤導性的優化信號。<lb/>為了解決這一困境，我們引入了STRACE（結構性軌跡分析與因果提取），這是一個構建高信號噪聲優化上下文的框架，以實現更精確和有效的優化。<lb/>在批處理層面，STRACE 挖掘失敗模式以過濾冗餘痕跡並保留代表性失敗；在每個選定的痕跡內，它對文本依賴圖進行因果定位，以移除非因果步驟並識別真正的根本原因模塊進行優化。<lb/>實證結果顯示，STRACE 顯著超越了標準的上下文過濾基準。<lb/>值得注意的是，在一個具有挑戰性的形式驗證任務（VeruSAGE-Bench）中，它成功優化了人類專家設計的代理，實現了 $1.4\times$ 的成功率提升（從 42.5% 提升至 58.5%）。<lb/>代碼可在 https://github.com/moomight/STRACE 獲得。

##### **SkillCenter: A Large-Scale Source-Grounded Skill Library for Autonomous AI Agents**
2607.07676v1 by Tianming Sha, Yue Zhao, Lichao Sun, Yushun Dong

Autonomous AI agents can execute complex tasks with limited human review, yet they often lack the grounded operational knowledge to make their outputs not just executable but correct, secure, and maintainable. We introduce SkillCenter, to our knowledge the largest open skill library for agents by total count: 216,938 structured skills across 24 domain bundles. A SkillGate-filtered pipeline contributes 114,565 source-grounded skills from peer-reviewed journals, ArXiv, and over 24,000 technical sources, integrated with 102,373 community skills from GitHub and the ClawHub marketplace. We present the end-to-end framework that builds the pipeline subset: multi-source acquisition, an LLM-based quality gate (SkillGate), template-driven generation, iterative source-grounding, and quality-controlled publishing. Source grounding is a traceability guarantee: each retained claim maps to an exact quotation in its source. All skills ship as offline-searchable SQLite FTS5 bundles.

摘要：自主 AI 代理可以在有限的人類審查下執行複雜任務，但它們通常缺乏基礎的操作知識，使得它們的輸出不僅可執行，還必須是正確、安全和可維護的。我們介紹 SkillCenter，據我們所知，這是目前最大的開放技能庫，總計有 216,938 種結構化技能，涵蓋 24 個領域包。一個經過 SkillGate 過濾的管道提供了來自同行評審期刊、ArXiv 和超過 24,000 個技術來源的 114,565 種來源基礎技能，並整合了來自 GitHub 和 ClawHub 市場的 102,373 種社群技能。我們展示了構建該管道子集的端到端框架：多來源獲取、基於 LLM 的質量門檻（SkillGate）、模板驅動生成、迭代來源基礎和質量控制發布。來源基礎是一種可追溯性保證：每個保留的主張都映射到其來源中的精確引用。所有技能以離線可搜尋的 SQLite FTS5 包裝發送。

##### **Graph-Regularized Deep Learning for EEG-Based Emotion Recognition with Psychologically-Grounded Label Structure**
2607.07773v1 by Dongyang Kuang, Zizheng Ma, Yushan Zhang, Xiaocong Zeng

EEG-based emotion recognition is critical for mental health monitoring and affective brain-computer interfaces, yet existing deep learning approaches often treat emotion classes as isolated labels, ignoring their psychological interdependencies. We propose a graph-regularized learning framework that conceptualizes emotions as nodes in a graph where edges encode proximity based on dimensional emotion theories. We adapt three complementary regularization strategies--Graph Label Smoothing (intuitive soft labeling), Commuting distance on graph via Graph Laplacian (spectral graph theory), and Sliced Wasserstein Distance (optimal transport on graph)--ordered by increasing computational complexity. These strategies penalize model predictions that deviate from the established emotion topology. Our framework is evaluated across three representative backbone architectures: AudioTransformer (pure transformer), Conformer (CNN-transformer hybrid), and DCGNN (causal graph neural network), demonstrating architecture-agnostic benefits. Experiments on SEED-IV (4 classes) and SEED-V (5 classes) datasets show consistent improvements: best case up to +5.42% accuracy and 39% reduction in psychologically implausible misclassifications. Ultimately, our framework help raise the upper bound of performance achievable with standard approaches. Code will be released.

摘要：EEG 基於情緒識別對於心理健康監測和情感腦機介面至關重要，但現有的深度學習方法往往將情緒類別視為孤立的標籤，忽視了它們之間的心理相互依賴性。我們提出了一個圖形正則化學習框架，將情緒概念化為圖中的節點，其中邊緣根據維度情緒理論編碼接近性。我們調整了三種互補的正則化策略——圖標籤平滑（直觀的軟標籤）、通過圖拉普拉斯的通勤距離（光譜圖理論）和切片瓦瑟斯坦距離（圖上的最優運輸）——按計算複雜性遞增的順序排列。這些策略懲罰偏離既定情緒拓撲的模型預測。我們的框架在三種代表性的主幹架構中進行評估：AudioTransformer（純Transformer）、Conformer（CNN-Transformer混合）和 DCGNN（因果圖神經網絡），顯示出與架構無關的好處。在 SEED-IV（4 類）和 SEED-V（5 類）數據集上的實驗顯示出一致的改進：最佳情況下準確率提高了 +5.42%，心理上不合理的錯誤分類減少了 39%。最終，我們的框架有助於提高標準方法可達到的性能上限。代碼將會發布。

##### **CARLA-GS: Decoupling Representation, Reasoning, and Physics Simulation for Autonomous Driving Corner-Case Synthesis**
2607.07601v1 by Kaicong Huang, Meng Ma, Ruimin Ke

Safety evaluation for autonomous driving is dominated by rare, safety-critical interactions, motivating simulators that can deliberately synthesize corner cases with photorealistic observations. Corner-case generation is inherently a multi-source problem spanning visual representation, scene reasoning, and vehicle trajectory generation and control. Prior knowledge- and model-based approaches typically focus on scene or trajectory components in isolation, while diffusion-based methods attempt end-to-end generation but still struggle to ensure spatiotemporal consistency and physical realism. To unify these aspects within a single framework, we propose CARLA-GS, a modular corner-case synthesis pipeline that decouples visual representation, semantic reasoning, and physics-based execution while maintaining tight cross-module coupling. Starting from real driving data, we reconstruct an editable gaussian scene with additional geometry-consistent constraints. A multi-agent LLM then performs scene-level reasoning to identify risky interactions and generate intent-level waypoint trajectories, while the low-level motion control is delegated to CARLA, where a PID controller ensures kinematic and dynamic feasibility. The simulated vehicle states are finally re-projected into the gaussian scene for ego-centric rendering. This design enables high-level semantic reasoning, low-level physically executable motion, and photorealistic corner-case generation within a unified pipeline. Experiments on the Waymo Open Dataset show, both quantitatively and qualitatively, that our framework enables controllable corner-case generation and produces photorealistic, spatiotemporally consistent videos aligned with semantic intent and physically feasible motion.

摘要：安全評估自動駕駛主要受稀有且關鍵的安全互動影響，這促使了能夠故意合成邊緣案例並提供照片真實觀察的模擬器。邊緣案例生成本質上是一個多來源問題，涵蓋視覺表現、場景推理以及車輛軌跡生成與控制。先前的知識和模型基礎方法通常專注於場景或軌跡組件的孤立分析，而基於擴散的方法則嘗試進行端到端的生成，但仍然難以確保時空一致性和物理真實性。為了在單一框架內統一這些方面，我們提出了CARLA-GS，一個模組化的邊緣案例合成管道，該管道解耦了視覺表現、語義推理和基於物理的執行，同時保持緊密的跨模組耦合。從真實駕駛數據開始，我們重建了一個可編輯的高斯場景，並添加了幾何一致性約束。一個多代理LLM然後執行場景級推理，以識別風險互動並生成意圖級的路徑點軌跡，而低級運動控制則委派給CARLA，在那裡PID控制器確保運動學和動力學的可行性。模擬的車輛狀態最終重新投影到高斯場景中以進行自我中心的渲染。這種設計使得高級語義推理、低級物理可執行運動和照片真實的邊緣案例生成能夠在統一的管道中實現。在Waymo Open Dataset上的實驗顯示，我們的框架無論在量化還是質化上，都能實現可控的邊緣案例生成，並生成與語義意圖和物理可行運動一致的照片真實、時空一致的視頻。

##### **Collaborative Synthetic Data Generation for Knowledge Transfer in Federated Learning**
2607.07565v1 by Maximilian Andreas Hoefler, Karsten Mueller, Wojciech Samek

One-shot federated learning (OSFL) addresses the communication overhead of federated learning by limiting training to a single round, but doing so without sacrificing model quality is non-trivial, particularly when client data distributions diverge. Recent work has addressed this challenge by aggregating client knowledge on the server through the construction of transferable synthetic datasets or distillates. However, most of these methods lack formal privacy guarantees, leaving a gap in jointly achieving low communication, robustness to heterogeneity, and rigorous privacy. We propose FedKT-CSD (Federated Knowledge Transfer via Collaborative Synthetic Data), a framework inspired by neural image compression that closes this gap by leveraging publicly pretrained autoencoders as a shared latent space. Each client encodes its private data in a single forward pass, computes class-conditional latent statistics, and transmits these to the server. The server aggregates these statistics via secure aggregation, adds calibrated differential privacy noise, and decodes a synthetic dataset for training a global model and further downstream tasks. This design provides formal $(\varepsilon,δ)$-differential privacy by construction, while keeping client-side computation and communication lightweight. Despite operating under privacy constraints, FedKT-CSD is competitive with and even outperforms non-private baselines across diverse datasets and heterogeneity settings, and scales to a large number of clients. Our code is available at: https://github.com/an7123/FedKT-CSD

摘要：一次性聯邦學習 (OSFL) 通過將訓練限制為單輪來解決聯邦學習的通信開銷，但在不犧牲模型質量的情況下做到這一點並非易事，特別是當客戶端數據分佈發生偏差時。最近的研究通過構建可轉移的合成數據集或蒸餾品，在服務器上聚合客戶端知識來應對這一挑戰。然而，這些方法大多缺乏正式的隱私保證，導致在實現低通信、對異質性的穩健性和嚴格隱私方面存在差距。我們提出了 FedKT-CSD（通過協作合成數據的聯邦知識轉移），這是一個受神經圖像壓縮啟發的框架，通過利用公開預訓練的自編碼器作為共享潛在空間來填補這一空白。每個客戶端在單次前向傳遞中編碼其私有數據，計算類條件潛在統計，並將這些數據傳輸到服務器。服務器通過安全聚合來聚合這些統計數據，添加經過校準的差分隱私噪聲，並解碼出一個合成數據集以訓練全局模型及進一步的下游任務。這一設計通過構建提供了正式的 $(\varepsilon,δ)$-差分隱私，同時保持客戶端計算和通信的輕量級。儘管在隱私約束下運行，FedKT-CSD 在各種數據集和異質性設置中與非私有基準競爭，甚至超越它們，並且能擴展到大量客戶端。我們的代碼可在以下網址獲得：https://github.com/an7123/FedKT-CSD

##### **Aligning Clinical Needs and AI Capabilities: A Survey on LLMs for Medical Reasoning**
2607.07761v1 by Qi Peng, Jiatong Li, Sirui Huang, Yiyang Jiang, Kaisong Gong, Ronger Ding, Shijie Ye, Changmeng Zheng, Yi Cai, Xiaobo Yang, Jin Huang, Xiao-Yong Wei, Qing Li

Large language models (LLMs) have emerged as important tools in healthcare, showing growing potential for clinical reasoning and patient care. This survey examines recent progress in medical LLMs, focusing on reasoning applications and requirements. We present a dual-view approach that connects clinical practice with computational methods. On the clinical side, we establish a five-level competency scheme following Miller's Pyramid, progressing from knowledge recall to dynamic case management. On the computational side, we link deductive, inductive, and abductive reasoning patterns to common medical goals and tasks. We also introduce a benchmark dataset spanning five levels of medical reasoning capability and report results on 18 state-of-the-art models, revealing that medical specialist models excel in diagnosis-centric tasks while general models lead in decision support and dialogue. We conclude by discussing current progress and open challenges, including data limitations, hallucination, and grounding issues, and outline directions toward safer, more reliable, and workflow-ready systems.

摘要：大型語言模型（LLMs）已成為醫療保健中重要的工具，顯示出在臨床推理和病人護理方面日益增長的潛力。<lb/>本調查檢視了醫療LLMs的近期進展，重點關注推理應用和需求。<lb/>我們提出了一種雙視角的方法，將臨床實踐與計算方法相連接。<lb/>在臨床方面，我們建立了一個五級能力框架，遵循米勒金字塔，從知識回憶進展到動態案例管理。<lb/>在計算方面，我們將演繹、歸納和推斷推理模式與常見的醫療目標和任務相連接。<lb/>我們還介紹了一個涵蓋五個層級的醫療推理能力基準數據集，並報告了18個最先進模型的結果，顯示醫療專家模型在以診斷為中心的任務中表現優異，而通用模型在決策支持和對話中領先。<lb/>我們最後討論了當前的進展和未解決的挑戰，包括數據限制、幻覺和基礎問題，並概述了朝向更安全、更可靠和適合工作流程的系統的方向。

##### **Stability of Flow Models for Graph Signals**
2607.07510v1 by Martin Schmidt, Gonzalo Mateos

Generating signals on graphs requires permutation-equivariant models that exhibit stability with respect to relative structural perturbations. While favorable stability properties of Graph Neural Networks (GNNs) have been well documented, it is unclear how structural errors propagate through the dynamics of continuous generative flow models that are gaining traction for graph signal generation. In this paper, we analyze continuous normalized flow models parameterized by GNNs and show that permutation equivariance is preserved for both the resulting continuous-time ordinary differential equations and their discrete numerical approximations used as graph signal samplers. Our primary contribution is to derive explicit stability bounds on the generated probability distributions, which quantify how relative graph perturbations affect the final sampled signals. Motivated by these theoretical bounds, we introduce a stability-promoting regularized flow matching strategy that actively penalizes the spatial Lipschitz constant of the vector field during model training. Experiments using synthetic smooth signals on stochastic block model graphs and real-world fMRI signals on brain connectomes demonstrate that this bound-oriented approach yields generative models that are more robust to structural noise, without sacrificing output quality.

摘要：生成圖形上的信號需要具有置換不變性的模型，這些模型對於相對結構擾動展現穩定性。雖然圖神經網絡（GNNs）的有利穩定性特性已被充分記錄，但尚不清楚結構錯誤如何通過日益受到關注的連續生成流模型的動態傳播，這些模型用於圖信號生成。在本文中，我們分析了由GNNs參數化的連續正規化流模型，並顯示置換不變性在生成的連續時間常微分方程及其用作圖信號取樣器的離散數值近似中得以保留。我們的主要貢獻是推導出生成概率分佈的明確穩定性界限，這量化了相對圖擾動如何影響最終取樣信號。受到這些理論界限的啟發，我們引入了一種促進穩定性的正則化流匹配策略，該策略在模型訓練期間主動懲罰向量場的空間Lipschitz常數。使用隨機區塊模型圖上的合成平滑信號和腦連接組上的真實fMRI信號的實驗表明，這種以界限為導向的方法產生的生成模型對結構噪聲更具魯棒性，而不犧牲輸出質量。

##### **TimEE: End-to-end Time Series Classification via In-Context Learning**
2607.07500v1 by Jaris Küken, Shi Bin Hoo, Martin Mráz, Frank Hutter, Lennart Purucker

Time series classification (TSC) is dominated by a two-stage paradigm: train a feature encoder -- either from scratch on the target dataset or via pretraining on large corpora -- and then fit a task-specific classifier on top. While effective, this decoupling optimizes representation learning independently of the classification objective, requires per-dataset training, and prevents the model from exploiting label information during inference. We introduce TimEE, a 4.5M-parameter foundation model for end-to-end TSC via in-context learning. Given a labeled support set and a query time series, TimEE directly outputs a predicted class distribution in a single forward pass with no per-dataset training required. Following the prior-data fitted network (PFN) framework, TimEE is meta-trained exclusively on synthetic TSC tasks, where each task contains time series with distinct class identities arising from structured distributional shifts in the generative process. Despite seeing no real time series during pre-training, TimEE ranks first in ROC AUC (and third on accuracy) on the UCR benchmark among all compared methods, which include both foundation models and supervised deep learning baselines. To our knowledge, TimEE is the first purely synthetic-pretrained model to reach state-of-the-art performance on the UCR benchmark. These results establish end-to-end ICL with synthetic priors as a compelling, largely unexplored direction for TSC, with scaling, prior design, and richer generation mechanisms as natural avenues for improvement. Code is publicly available at http://github.com/automl/timee.

摘要：時間序列分類（TSC）主要受到兩階段範式的主導：訓練一個特徵編碼器——要麼從頭開始在目標數據集上訓練，要麼通過在大型語料庫上進行預訓練——然後在其上擬合一個特定任務的分類器。雖然這種方法有效，但這種解耦使得表示學習獨立於分類目標進行優化，需要針對每個數據集進行訓練，並且在推斷過程中無法利用標籤信息。我們介紹了TimEE，一個擁有450萬參數的基礎模型，通過上下文學習實現端到端的TSC。給定一個標記的支持集和一個查詢時間序列，TimEE在單次前向傳遞中直接輸出預測的類別分佈，無需針對每個數據集進行訓練。根據先前數據擬合網絡（PFN）框架，TimEE僅在合成TSC任務上進行元訓練，其中每個任務包含來自生成過程中結構性分佈變化的不同類別身份的時間序列。儘管在預訓練期間未見過真實的時間序列，TimEE在UCR基準上在所有比較方法中排名第一的ROC AUC（準確率第三），這些方法包括基礎模型和監督式深度學習基準。據我們所知，TimEE是第一個純合成預訓練模型，在UCR基準上達到最先進的性能。這些結果確立了具有合成先驗的端到端ICL作為TSC的一個引人注目的、大多數未被探索的方向，擴展、先驗設計和更豐富的生成機制是改進的自然途徑。代碼可在http://github.com/automl/timee公開獲取。

##### **Where to Intervene? Benchmarking Fairness-Aware Learning on Differentially Private Synthetic Tabular Data**
2607.07471v1 by Vinícius Gabriel Angelozzi, Héber H. Arcolezi

Machine learning models are increasingly deployed in high-stakes domains, raising concerns about both privacy and fairness. Differential Privacy (DP) has become a gold standard for privacy-preserving data analysis, while fairness-aware mechanisms aim to mitigate discrimination against underrepresented groups. However, these objectives can conflict: DP often amplifies disparities across demographic groups, and little is known about whether established fairness interventions remain effective under DP constraints. In this work, we present, to our knowledge, the first systematic evaluation of fairness interventions on differentially private synthetic tabular data. Our benchmark centers on the Adaptive Iterative Mechanism (AIM), identified as the state-of-the-art marginal-based DP synthesizer (Cormode et al. 2025). We thus evaluate fairness interventions across four datasets, multiple group fairness metrics, and three categories of mitigation strategies (pre-processing, in-processing, and post-processing) under a wide range of privacy budgets. We compare four pipeline configurations: (Baseline) training on original data; (DP-only) training on DP synthetic data; (Fair-only) applying fairness mechanisms on original data; and (DP+Fair) combining fairness mechanisms with DP synthetic data. Our results demonstrate that while DP alone can degrade both utility and fairness, applying fairness interventions can partially restore equitable outcomes. Among them, post-processing methods tend to provide more stable fairness-utility trade-offs across privacy budgets and synthesizers, achieving strong fairness improvements while preserving competitive utility relative to other intervention stages. We release all code, data, and experimental artifacts in an open-source repository to ensure full reproducibility and to support future research on the privacy-fairness-utility trade-off.

摘要：機器學習模型越來越多地應用於高風險領域，這引發了對隱私和公平性的擔憂。差分隱私（DP）已成為隱私保護數據分析的黃金標準，而公平性意識機制旨在減輕對弱勢群體的歧視。然而，這些目標可能會相互衝突：DP往往會擴大不同人口群體之間的差異，而目前對於在DP約束下，既有的公平性介入措施是否仍然有效知之甚少。在這項工作中，我們提出了迄今為止對差分私有合成表格數據進行公平性介入措施的首次系統評估。我們的基準集中於自適應迭代機制（AIM），該機制被認定為最先進的邊際基礎DP合成器（Cormode et al. 2025）。因此，我們在四個數據集上評估公平性介入措施，涵蓋多個群體公平性指標和三類緩解策略（預處理、處理中和後處理），並在廣泛的隱私預算下進行評估。我們比較了四種管道配置：（基線）在原始數據上訓練；（僅DP）在DP合成數據上訓練；（僅公平）在原始數據上應用公平性機制；以及（DP+公平）將公平性機制與DP合成數據結合。我們的結果表明，雖然僅用DP會降低效用和公平性，但應用公平性介入措施可以部分恢復公平的結果。在這些方法中，後處理方法在不同的隱私預算和合成器中往往能提供更穩定的公平-效用權衡，實現顯著的公平性改善，同時保持相對於其他介入階段的競爭性效用。我們在一個開源庫中釋放所有代碼、數據和實驗文物，以確保完全可重現性並支持未來對隱私、公平性和效用權衡的研究。

##### **InductWave: Inductive Multi-Hop Logical Query Answering on Knowledge Graphs**
2607.07422v1 by Mayank Kharbanda, Michael Cochez, Rajiv Ratn Shah, Raghava Mutharaju

Logical Multi-Hop Query Answering over Knowledge Graphs (KGs) can be formulated as querying, with an implicit completeness assumption. Current works mainly focus on Existential First Order Logic (EFO) queries. These EFO queries contain conjunction, disjunction, and negation operators. Most existing works employ transductive reasoning, meaning they are not capable of reasoning over entities unseen during training. In the real world, there is a resource scarcity, and we cannot train a model with all the nodes of a large KG. Hence, we propose InductWave, a wavelet-based inductive embedding method for logical query answering on large KGs. Here, the training graph consists of fewer nodes than the test graph. Our model performs on par with the baseline models while having half the number of message-passing layers. It outperforms all of them in most cases, with 75% of the layers. These fewer resource requirements enable us to evaluate InductWave on massive graphs, such as Wiki-KG. We test our model using extensive experiments across varying train-test graph proportions of the FB15k-(237) dataset, comparing it with the state-of-the-art models. The code and datasets for the model are available at https://github.com/kracr/inductwave/.

摘要：邏輯多跳查詢回答在知識圖譜 (KGs) 上可以被表述為查詢，並隱含著完整性假設。當前的研究主要集中在存在性一階邏輯 (EFO) 查詢上。這些 EFO 查詢包含合取、析取和否定運算符。大多數現有的研究採用傳導推理，這意味著它們無法對訓練期間未見過的實體進行推理。在現實世界中，資源稀缺，我們無法用大型 KG 的所有節點來訓練模型。因此，我們提出了 InductWave，一種基於小波的誘導嵌入方法，用於在大型 KG 上進行邏輯查詢回答。在這裡，訓練圖的節點數少於測試圖的節點數。我們的模型在性能上與基準模型相當，同時擁有一半的消息傳遞層數。在大多數情況下，它在 75% 的層數上超越了所有其他模型。這些較少的資源需求使我們能夠在大型圖上評估 InductWave，例如 Wiki-KG。我們使用廣泛的實驗來測試我們的模型，涵蓋 FB15k-(237) 數據集的不同訓練-測試圖比例，並將其與最先進的模型進行比較。模型的代碼和數據集可在 https://github.com/kracr/inductwave/ 獲得。

##### **When Prompts Ignore Structure: Graph-Based Attribute Reasoning for Calibrated VLMs**
2607.07395v1 by Tanay Sodha, Aditya Sharma, Ramya Hebbalaguppe, Vinti Agarwal, Pranav Murthy Yeluripaty

Reliable confidence estimation remains a key limitation of test-time adaptation in vision-language models (VLMs), where prompt tuning improves zero-shot accuracy but often degrades calibration due to entropy-driven overconfidence. Prior approaches mitigate this using LLM-derived class attributes and contrastive regularization, yet treat attributes independently, ignoring their relational structure. We propose ARGTCA, which represents (class, attribute) pairs as nodes in a Symbolic Attribute Graph and trains a Graph Attention Network (GAT) using contrastive objectives to produce structurally informed embeddings that capture inter-attribute dependencies. We introduce two attribute selection strategies: ARGTCA-DIV for intra-class diversity and ARGTCA-DISC for inter-class discrimination. Experiments across nine benchmarks show that ARGTCA-DIV reduces average Expected Calibration Error (ECE) by approximately ~37% over baselines, while ARGTCA-DISC consistently performs as the second-best variant, reducing average ECE by approximately ~17% over baselines. These results suggest that modeling symbolic attribute interactions provides a principled approach for reliable test-time adaptation in VLMs.

摘要：可靠的信心估計仍然是視覺-語言模型（VLMs）中測試時適應的一個主要限制，其中提示調整提高了零-shot 準確性，但通常因熵驅動的過度自信而降低了校準。先前的方法使用 LLM 派生的類別屬性和對比正則化來緩解這個問題，但獨立處理屬性，忽略了它們的關係結構。我們提出了 ARGTCA，將（類別，屬性）對表示為符號屬性圖中的節點，並使用對比目標訓練圖注意力網絡（GAT），以產生結構性信息嵌入，捕捉屬性間的依賴關係。我們引入了兩種屬性選擇策略：ARGTCA-DIV 用於類內多樣性，ARGTCA-DISC 用於類間區分。在九個基準上的實驗顯示，ARGTCA-DIV 對比基線將平均期望校準誤差（ECE）降低了約 ~37%，而 ARGTCA-DISC 始終表現為第二最佳變體，將平均 ECE 降低了約 ~17% 相對於基線。這些結果表明，建模符號屬性交互提供了一種可靠的測試時適應的原則性方法，適用於 VLMs。

##### **TF-Engram: A Train-Free Engram with SSD-Backed Memory for Large Language Models**
2607.07388v1 by Yutang Ma, Kecheng Huang, Xikun Jiang, Zili Shao

Large Language Models (LLMs) store factual knowledge and domain-specific patterns implicitly in dense Transformer parameters, making knowledge expansion costly through pretraining, fine-tuning, retrieval augmentation, or longer contexts. Engram-style memory offers a compact hidden-state injection pathway, but existing GPU-resident designs often rely on hash-based compression, causing unrelated phrases to collide in shared slots and weakening phrase-level semantic fidelity. We present TF-Engram, a train-free Engram system that constructs phrase-specific semantic memory offline from external corpora, stores large memory tables across a GPU--DRAM--SSD hierarchy, and uses Early-Exit Guided Predictive Prefetching to hide external-memory latency during autoregressive decoding. On Qwen3-0.6B, TF-Engram improves the average downstream score from 57.6 to 59.4, outperforming both the frozen backbone and a parameter-matched LoRA baseline. System evaluation shows that large TF-Engram tables can be built with moderate offline cost, SSD-backed storage substantially reduces GPU memory demand, and predictive prefetching recovers much of the throughput loss caused by external memory access. These results demonstrate that static phrase memory can be integrated into LLM inference as a scalable, train-free, and low-overhead system component.

摘要：大型語言模型（LLMs）在密集的Transformer參數中隱式地儲存事實知識和特定領域的模式，使得透過預訓練、微調、檢索增強或更長的上下文進行知識擴展變得昂貴。恩格拉姆風格的記憶提供了一個緊湊的隱藏狀態注入路徑，但現有的GPU常駐設計通常依賴基於哈希的壓縮，導致不相關的短語在共享槽位中碰撞，並削弱短語級的語義真實性。我們提出了TF-Engram，一個無需訓練的恩格拉姆系統，該系統從外部語料庫離線構建短語特定的語義記憶，並在GPU-DRAM-SSD層次結構中儲存大型記憶表，使用早期退出引導預測預取來隱藏自回歸解碼過程中的外部記憶延遲。在Qwen3-0.6B上，TF-Engram將平均下游得分從57.6提高到59.4，超越了凍結的主幹和參數匹配的LoRA基準。系統評估顯示，大型TF-Engram表可以以適度的離線成本構建，SSD支持的存儲大幅減少了GPU的記憶體需求，而預測預取則恢復了因外部記憶訪問造成的吞吐量損失。這些結果表明，靜態短語記憶可以作為可擴展、無需訓練且低開銷的系統組件集成到LLM推理中。

##### **HumAIN: Human-Aware Implicit Social Robot Navigation**
2607.07357v1 by Daeun Song, Nhat Le, Jeffrey Chen, Mohammad Nazeri, Amirreza Payandeh, Rohan Chandra, Reuth Mirsky, Ross Mead, Ling Xiao, Xuesu Xiao

Effective social robot navigation requires sensitivity to human behavior, often revealed through subtle skeletal cues like gait and orientation. We present Human-Aware Implicit Social Robot Navigation (HumAIN), a novel framework that fuses implicit social cues directly into the planning loop via knowledge distillation. We first employ a transformer-based teacher model that fuses rich multi-modal inputs, including historic images, skeletal keypoints, robot state, and a robot's target goal, to learn robust, human-aware representations for the robot's future trajectory planning. To enable real-time deployment, we then distill this knowledge into a lightweight student model. By optimizing for both trajectory reconstruction and latent feature alignment with the teacher, the student learns to infer complex social dynamics from minimal inputs. Bridging the prediction-planning gap with an efficient distilled architecture, our method enables robots to reason about human behavior in a manner that is adaptive, robust, and socially compliant. We validate HumAIN through extensive experiments, where it improves trajectory prediction metrics by an average of 29.8% across all metrics compared to state-of-the-art baselines. These results highlight the benefit of using implicit, whole-body cues to achieve human-like navigation awareness on resource-constrained platforms.

摘要：有效的社交機器人導航需要對人類行為的敏感性，這通常通過微妙的骨架線索如步態和方向來揭示。<lb/>我們提出了人類感知隱式社交機器人導航（HumAIN），這是一個新穎的框架，通過知識蒸餾將隱式社交線索直接融合到規劃循環中。<lb/>我們首先使用基於Transformer的教師模型，融合豐富的多模態輸入，包括歷史圖像、骨架關鍵點、機器人狀態和機器人的目標，以學習機器人未來軌跡規劃的穩健人類感知表示。<lb/>為了實現實時部署，我們然後將這些知識蒸餾到輕量級的學生模型中。<lb/>通過優化軌跡重建和與教師的潛在特徵對齊，學生學會從最小的輸入中推斷複雜的社交動態。<lb/>我們的方法通過高效的蒸餾架構彌補預測與規劃之間的差距，使機器人能夠以適應性、穩健和社會合規的方式推理人類行為。<lb/>我們通過廣泛的實驗驗證了HumAIN，結果顯示其在所有指標上相比於最先進的基準提高了平均29.8%的軌跡預測指標。<lb/>這些結果突顯了使用隱式全身線索在資源受限的平台上實現類人導航感知的好處。

##### **Hypergraph Neural Stochastic Diffusion: An SDE Framework for Uncertainty Estimation**
2607.07330v1 by Zhiheng Zhou, Mengyao Zhou, Dengyi Zhao, Xingqin Qi, Guiying Yan

Hypergraph neural networks have shown powerful capability in modeling higher-order relations, yet their predictive uncertainty remains underexplored. Unlike pairwise graphs, uncertainty in hypergraphs arises not only from noisy attributes and ambiguous labels, but also from variations in node-hyperedge incidence structures and complex higher-order dependencies. Existing approaches mainly estimate uncertainty from final predictions or rely on computationally expensive ensembles and Bayesian inference, limiting their ability to capture uncertainty evolution during representation learning. In this paper, we propose Hypergraph Neural Stochastic Diffusion(HyperNSD), a stochastic differential equation framework for uncertainty estimation on hypergraphs. HyperNSD models hypergraph representations as stochastic processes evolving over node-hyperedge incidence structures. A learnable drift function captures deterministic higher-order diffusion dynamics, while a learnable stochastic forcing function characterizes structural ambiguity and representation noise. Predictive uncertainty is directly quantified through the variability of stochastic representation trajectories, providing an intrinsic uncertainty measure beyond post-hoc confidence scores. We formulate HyperNSD with neural drift and diffusion networks, enabling joint learning of prediction and uncertainty propagation. Theoretical analyses establish well posedness, perturbation stability,permutation equivariance, and numerical convergence of the proposed stochastic dynamics. Experiments on multiple hypergraph benchmarks demonstrate that HyperNSD achieves reliable uncertainty estimation for out-of-distribution and misclassification detection while preserving competitive prediction accuracy. These results provide a principled stochastic-dynamical framework for trustworthy higher-order representation learning.

摘要：超圖神經網絡在建模高階關係方面展現出強大的能力，但其預測不確定性仍然未被充分探索。與成對圖不同，超圖中的不確定性不僅來自於嘈雜的屬性和模糊的標籤，還來自於節點-超邊發生結構的變化和複雜的高階依賴關係。現有的方法主要從最終預測中估計不確定性，或依賴計算成本高昂的集成和貝葉斯推斷，這限制了它們在表示學習過程中捕捉不確定性演變的能力。在本文中，我們提出了超圖神經隨機擴散（HyperNSD），這是一個用於超圖不確定性估計的隨機微分方程框架。HyperNSD將超圖表示建模為在節點-超邊發生結構上演變的隨機過程。一個可學習的漂移函數捕捉確定性的高階擴散動力學，而一個可學習的隨機強迫函數則表徵結構模糊性和表示噪聲。預測不確定性通過隨機表示軌跡的變異性直接量化，提供了一種超越事後置信分數的內在不確定性度量。我們用神經漂移和擴散網絡來公式化HyperNSD，實現預測和不確定性傳播的聯合學習。理論分析確立了所提出的隨機動力學的良好適定性、擾動穩定性、置換等變性和數值收斂性。在多個超圖基準上的實驗表明，HyperNSD在保持競爭性預測準確性的同時，實現了對於分佈外和錯誤分類檢測的可靠不確定性估計。這些結果為值得信賴的高階表示學習提供了一個原則性的隨機動力學框架。

##### **CarbonCLIP: Enhance Carbon Prediction from Satellite Imagery via Integrated Street-View Semantics and Temporal Context Training**
2607.07292v1 by Zeru Yang, Fang-Ying Gong, Steve H. L. Yim, Chau Yuen

Accurately estimating urban carbon emissions is critical for sustainable urban planning, yet many existing approaches remain difficult to apply consistently across cities due to data-source heterogeneity and the lack of fine-grained semantic-temporal context in remote sensing data. We propose CarbonCLIP, a task-oriented multimodal distillation framework that improves satellite-based carbon emission prediction by transferring contextual knowledge into a unified satellite representation through dual-branch contrastive learning. Unlike conventional methods that rely on static visual features, CarbonCLIP explicitly bridges the gap between top-down satellite views and ground-level human activities. Specifically, the spatial branch uses fine-grained textual descriptions automatically generated from street-view images by Large Multimodal Models (LMMs) to provide semantic priors reflecting building functions, infrastructure, and urban activities, while the temporal branch employs a month encoder to encode temporal priors associated with monthly emission variation. CarbonCLIP requires multimodal data only during the pretraining phase; during inference, it relies solely on satellite imagery, thereby supporting scalable deployment when ground-level data are unavailable at inference. Experiments on Beijing and Singapore demonstrate that CarbonCLIP outperforms baselines in both study cities. The results validate that our method effectively transfers multimodal knowledge into satellite representations, offering a robust solution for satellite-based urban carbon modeling.

摘要：準確估算城市碳排放對於可持續城市規劃至關重要，然而，由於數據來源的異質性以及遙感數據中缺乏細粒度的語義-時間背景，許多現有的方法在不同城市間的一致應用仍然困難。我們提出了CarbonCLIP，一個以任務為導向的多模態蒸餾框架，通過雙分支對比學習將上下文知識轉移到統一的衛星表示中，從而改善基於衛星的碳排放預測。與依賴靜態視覺特徵的傳統方法不同，CarbonCLIP 明確地彌合了自上而下的衛星視圖與地面人類活動之間的差距。具體而言，空間分支使用由大型多模態模型（LMMs）自動生成的來自街景圖像的細粒度文本描述，以提供反映建築功能、基礎設施和城市活動的語義先驗，而時間分支則使用月份編碼器來編碼與每月排放變化相關的時間先驗。CarbonCLIP 只在預訓練階段需要多模態數據；在推理過程中，它僅依賴衛星影像，因此在推理時地面數據不可用時支持可擴展的部署。對北京和新加坡的實驗表明，CarbonCLIP 在這兩個研究城市中均優於基準。結果驗證了我們的方法有效地將多模態知識轉移到衛星表示中，為基於衛星的城市碳建模提供了一個穩健的解決方案。

##### **Understanding Interpretation Difficulty in Harmful Online Communication: Insights from Cybercrime Communities**
2607.07277v1 by Tomohiro Okatsu, Naoki Takada, Yin Min Pa Pa, Katsunari Yoshioka, Tatsunori Mori

Harmful online communication often contains slang, coded terms, abbreviations, and community-specific expressions, which make messages difficult to interpret. This paper presents an exploratory study of interpretation difficulty in Discord chats related to cybercrime. We construct reference interpretations of purposefully selected difficult messages, which were reviewed by an expert. We then use them to evaluate human and large language model (LLM) interpretations under different context conditions. The results show that local context alone is often insufficient for humans, while external knowledge and extended conversational context substantially improve human interpretation. For LLMs, local context also improves interpretation, and the larger model performs better. We further conduct a qualitative error analysis and propose a preliminary classification of factors that make harmful chats difficult to interpret. These findings suggest that harmful-content analysis should treat interpretation as an evidence-integration problem, rather than as message-level classification alone.

摘要：有害的線上溝通通常包含俚語、隱語、縮寫和特定社群的表達方式，這使得訊息難以解讀。本文呈現了一項探索性研究，探討與網路犯罪相關的 Discord 聊天中解讀的困難。我們構建了經過專家審查的故意選擇的困難訊息的參考解讀。然後，我們利用這些解讀來評估人類和大型語言模型（LLM）在不同情境條件下的解讀。結果顯示，僅有的地方性上下文對於人類來說通常是不足的，而外部知識和延伸的對話上下文則顯著改善了人類的解讀。對於 LLM，地方性上下文同樣改善了解讀，且較大的模型表現更佳。我們進一步進行了質性錯誤分析，並提出了一個初步分類，指出使有害聊天難以解讀的因素。這些發現表明，有害內容分析應將解讀視為一個證據整合問題，而不僅僅是訊息層級的分類。

##### **DiPhon: Diffusion on Graphons for Scalable Graph Generation**
2607.07232v1 by Sergio Rozada, Yiming Qin, Manuel Madeira, Pascal Frossard, Alejandro Ribeiro

Diffusion models represent a leading paradigm for graph generation, with notable impact in domains such as molecular design. Yet, scaling these models to large graphs remains an open problem. We approach this question in the dense-graph setting through the lens of graphons, the size-agnostic limit objects of dense graph sequences, to study how structural graph statistics behave across node-size scales. This perspective leads to DiPhon, a diffusion framework for size-scalable graph generation. Specifically, we formulate a continuous diffusion process on the graphon space via a Jacobi stochastic differential equation (SDE), and propose DiPhon, a discretized graph-level process that mimics these dynamics on finite graphs. We further derive the corresponding reverse-time process, which requires access to the marginal score. For the Jacobi process, this score interestingly admits a tractable form, which we estimate from data via graph denoising and plug into the reverse process to generate graph samples. We prove that DiPhon matches exactly the first moment of the marginal distributions induced by the continuous graphon process, and approximates the second moment up to a closed-form discrepancy. Thus, DiPhon inherits key size-agnostic statistical properties of the graphon dynamics, providing a principled route toward scalable graph generation. Empirically, we demonstrate this scalability by training on small graphs and generating progressively larger graphs at inference time, without retraining, while preserving their core topological properties.

摘要：擴散模型代表了圖形生成的主要範式，在分子設計等領域具有顯著影響。然而，將這些模型擴展到大圖仍然是一個未解決的問題。我們通過圖形的密集圖設定，從圖形的視角出發，研究結構圖形統計在節點大小尺度上的行為。這一視角導致了DiPhon，一個用於大小可擴展圖形生成的擴散框架。具體而言，我們通過雅可比隨機微分方程（SDE）在圖形空間上形成一個連續擴散過程，並提出DiPhon，一個離散的圖級過程，模擬這些在有限圖形上的動態。我們進一步推導出相應的反向時間過程，這需要訪問邊際分數。對於雅可比過程，這個分數有趣地承認了一種可處理的形式，我們通過圖形去噪從數據中估計它，並將其插入反向過程以生成圖形樣本。我們證明DiPhon完全匹配由連續圖形過程引起的邊際分佈的第一矩，並近似第二矩，直到一個封閉形式的差異。因此，DiPhon繼承了圖形動態的關鍵大小無關統計特性，為可擴展圖形生成提供了一條原則性的途徑。在實證上，我們通過在小圖上訓練並在推理時生成逐漸增大的圖，而不需要重新訓練，同時保留其核心拓撲特性，來展示這種可擴展性。

##### **Architecture Generalization with MetaNCA**
2607.07743v1 by Meet Barot, Daniel Berenberg, Sina Khajehabdollahi

Self-organization is an emergent property of life, driven by the collective behavior of individual components acting on local information. Biological neurons, through local interactions transmitted through synapses, are able to learn efficiently and can adapt their connections over an organism's lifespan. Motivated by these desirable properties of adaptability and local interaction, neural cellular automata (NCA) models have been successful at learning morphogenesis solely through local update rules, demonstrating stability over many updates and robustness to perturbations. In this work, we introduce Meta Neural Cellular Automata (MetaNCA), a framework that learns local rules which self-organize the weights of artificial neural networks. A learned rule network iteratively updates the weights of a task network using only local interactions on the computation graph. We propose a novel Weight Transformer architecture for the local rule network, which uses linear attention to aggregate signals from neighboring weights and hidden states. Once trained, the rule network generates task networks of diverse architectures without backpropagation. We show that MetaNCA generates weights for feedforward MLPs, CNNs, and ResNets on MNIST and CIFAR-100, scaling to networks of 2 million parameters. We further show that MetaNCA generalizes to architectures not seen during meta-training, and that architectural diversity in the training phase strengthens this generalization.

摘要：自組織是生命的一種突現特性，由個別組件在局部信息上進行集體行為驅動。生物神經元通過突觸傳遞的局部互動，能夠有效學習並能夠在有機體的生命週期內適應其連接。受到這些適應性和局部互動的理想特性的啟發，神經細胞自動機（NCA）模型成功地僅通過局部更新規則學習形態生成，並在多次更新中顯示出穩定性和對擾動的魯棒性。在這項工作中，我們介紹了元神經細胞自動機（MetaNCA），這是一個學習局部規則的框架，該規則自組織人工神經網絡的權重。一個學習到的規則網絡通過僅在計算圖上的局部互動，迭代地更新任務網絡的權重。我們提出了一種新穎的權重轉換器架構，用於局部規則網絡，該架構使用線性注意力來聚合來自相鄰權重和隱藏狀態的信號。一旦訓練完成，規則網絡可以在不進行反向傳播的情況下生成多樣化架構的任務網絡。我們展示了MetaNCA為MNIST和CIFAR-100上的前饋多層感知器（MLP）、卷積神經網絡（CNN）和殘差網絡（ResNet）生成權重，並擴展到200萬參數的網絡。我們進一步展示了MetaNCA能夠泛化到在元訓練期間未見過的架構，並且訓練階段的架構多樣性增強了這種泛化。

##### **Navigating Hierarchy: Hyperbolic Learning on Brain Graphs for Disorder Diagnosis**
2607.07077v1 by Yapeng Li, Bo Jiang, Ziyan Zhang, Dongdong Chen, Zhengzheng Tu

Functional brain networks exhibit a hierarchical organization across ROI, community, and whole-brain levels, supporting local processing, inter-community coordination, and global integration. Recent studies have demonstrated that brain community-aware modeling is beneficial for both diagnosis and biomarker identification of brain networks. However, existing brain graph modeling methods often struggle to model ROI-community interactions, thereby failing to fully exploit the hierarchy across ROI, community, and whole-brain network levels. To address this issue, inspired by deep hyperbolic learning in modeling hierarchical structures, we propose a novel framework, termed Hyperbolic Learning on Brain Graphs (HLBG), for brain network analysis. The core idea of HLBG is to exploit the inherent hierarchical geometry of hyperbolic space to model the hierarchical relationships among ROIs, functional communities, and the whole-brain network, thereby learning hierarchy-aware and highly discriminative representations for brain network data. Specifically, HLBG first projects representations from ROIs, communities, and the whole-brain network into Lorentzian hyperbolic space. Then, the multi-level hierarchy is imposed via two geometric entailment constraints. In addition, we introduce a new Graph-aware Mamba (GaMamba) model, which incorporates topology-derived structural prompts into Mamba to capture long-range dependencies while preserving graph topological information. Experiments on ABIDE-I and REST-MDD demonstrate that HLBG outperforms state-of-the-art methods and identifies disorder-relevant functional biomarkers.

摘要：功能性腦網絡在ROI、社群和全腦層面上展現出層級組織，支持局部處理、社群間協調和全球整合。最近的研究表明，腦社群感知建模對於腦網絡的診斷和生物標記識別是有益的。然而，現有的腦圖建模方法往往難以建模ROI-社群互動，從而未能充分利用ROI、社群和全腦網絡層面之間的層級結構。為了解決這個問題，我們受到深層超曲率學習在建模層級結構中的啟發，提出了一個新框架，稱為腦圖上的超曲率學習（HLBG），用於腦網絡分析。HLBG的核心思想是利用超曲率空間的固有層級幾何來建模ROI、功能社群和全腦網絡之間的層級關係，從而學習具有層級感知和高度區分性的腦網絡數據表示。具體而言，HLBG首先將來自ROI、社群和全腦網絡的表示投影到洛倫茲超曲率空間中。然後，通過兩個幾何推導約束施加多層級層級結構。此外，我們引入了一種新的圖感知Mamba（GaMamba）模型，該模型將拓撲衍生的結構提示納入Mamba，以捕捉長距離依賴性，同時保留圖的拓撲信息。在ABIDE-I和REST-MDD上的實驗表明，HLBG的表現超過了最先進的方法，並識別出與疾病相關的功能生物標記。

##### **Behavior Leverage Imbalance in Multi-Teacher On-Policy Distillation**
2607.07050v1 by Jiabin Shen, Guang Chen, Chengjun Mao

Agentic language models must learn when to call tools, when to consume tool responses, and when to answer directly. This makes multi-teacher on-policy distillation a natural training strategy: one teacher can specialize in tool calls, another in direct responses, and the student can learn from both on
  its own generated distribution. We show that this strategy can induce a behavior shift that is invisible from aggregate losses alone. In a two-teacher tool-use setting, vanilla generalized knowledge distillation improves tool-call recall but also moves the model toward over-calling, where it calls tools
  on examples that should be answered directly. Aggregate explanations are insufficient: tool-call samples do not receive more token exposure, and full-sequence per-token divergence is not larger for the tool-call teacher. We instead analyze behavior leverage imbalance: local token-level signals at mode-
  entry and structural positions, such as <tool_call> and function names, can have disproportionate control over the global generation mode. We propose Soft Clamp, a per-token divergence calibration method that dynamically compresses extreme token-level Jensen-Shannon divergence while preserving nonzero
  gradients. On APIGen-MT, Soft Clamp reduces over-calling from 13.7% to 9.0% relative to vanilla GKD while matching its decision accuracy. In a BFCL multi-turn diagnostic, it also lowers tool-call loops and repeated calls among GKD variants. These results suggest that multi-teacher OPD should monitor
  where teacher signals act, not only how large they are in aggregate.

摘要：代理語言模型必須學會何時呼叫工具、何時消耗工具回應以及何時直接回答。這使得多教師的政策蒸餾成為一種自然的訓練策略：一位教師可以專注於工具呼叫，另一位則專注於直接回應，而學生可以從兩者中學習，基於其自身生成的分佈。我們顯示這一策略可以引發行為轉變，這在僅僅依賴總體損失時是無法察覺的。在一個雙教師的工具使用環境中，普通的廣義知識蒸餾提高了工具呼叫的召回率，但也使模型趨向於過度呼叫，即在應該直接回答的例子上呼叫工具。總體解釋是不足夠的：工具呼叫樣本並未獲得更多的標記曝光，且全序列每標記的偏差對於工具呼叫教師並未更大。我們反而分析了行為杠杆不平衡：在模式進入和結構位置（如 <tool_call> 和函數名稱）上的局部標記級信號，可能對全局生成模式有不成比例的控制。我們提出了 Soft Clamp，一種每標記偏差校準方法，動態壓縮極端標記級的詹森-香農偏差，同時保持非零梯度。在 APIGen-MT 上，Soft Clamp 將過度呼叫從 13.7% 降低到 9.0%，相對於普通的 GKD，同時保持其決策準確性。在 BFCL 多輪診斷中，它還降低了工具呼叫循環和 GKD 變體之間的重複呼叫。這些結果表明，多教師 OPD 應該監控教師信號的作用位置，而不僅僅是它們在總體中的大小。

##### **Riemannian Geometry for Pre-trained Language Model Embeddings**
2607.07047v2 by Szczepan Konior, Alexandre Quemy, Przemysław Klocek, Bartłomiej Sobieski, Grégoire Cattan

Understanding the geometric structure of pre-trained language model embeddings matters for interpretability and safety. We ask whether sentence-level classification signal lives in the Riemannian geometry of contextual token embeddings, and probe it by extracting per-token pullback metrics from a learned encoder's analytical Jacobian and aggregating them with the Fréchet mean on the symmetric positive definite (SPD) manifold; we call this procedure Riemannian Mean Pooling (RMP). Across three datasets with non-trivial linguistic structure (CoLA, CREAK, RTE), RMP outperforms Euclidean mean pooling, while on FEVER-Symmetric, a benchmark constructed to remove annotation-driven lexical artifacts, the method correctly stays at chance. Ablations show that a randomly initialised encoder combined with Fréchet aggregation already beats Euclidean pooling on two of the three signal-bearing datasets, localising the source of the gain to the geometric aggregation rather than to learned manifold structure; the trained encoder contributes additional signal specifically on CREAK, the most knowledge-heavy of the three signal-bearing datasets.

摘要：理解預訓練語言模型嵌入的幾何結構對於可解釋性和安全性至關重要。<lb/>我們詢問句子級分類信號是否存在於上下文標記嵌入的黎曼幾何中，並通過從學習的編碼器的解析雅可比矩陣中提取每個標記的回拉度量來探究它，並使用對稱正定（SPD）流形上的Fréchet均值進行聚合；我們稱這一過程為黎曼均值池化（RMP）。<lb/>在三個具有非平凡語言結構的數據集（CoLA、CREAK、RTE）中，RMP的表現優於歐幾里得均值池化，而在FEVER-Symmetric上，這是一個旨在消除標註驅動的詞彙工件的基準，該方法正確地保持在隨機水平。<lb/>消融實驗顯示，隨機初始化的編碼器結合Fréchet聚合已經在三個信號承載數據集中的兩個上超越了歐幾里得池化，將增益的來源定位於幾何聚合而非學習的流形結構；訓練的編碼器在CREAK上特別貢獻了額外的信號，該數據集是三個信號承載數據集中知識最重的。

##### **Latent graph encoding of multimodal neuroimaging features with generative AI architectures**
2607.07027v1 by Ishaan Batta, Meenu Ajith, Vince Calhoun

While generative models enable encoding of complex neuroimaging data for feature generation and reconstruction, developing optimal architectural frameworks with appropriate encoding and latent space processes is crucial for studying structural and functional properties of the brain. We design a multimodal generative framework for structural and functional magnetic resonance imaging (MRI) features through systematic evaluation of encoding strategies, latent multimodal fusion, and generative model selection. Using structural gray matter volume (GMV) and static functional network connectivity (sFNC) features from a large neuroimaging dataset, we analyze generative frameworks involving variational autoencoders (VAEs), transformers, generative adversarial networks (GANs), and diffusion models. Architectures that employ modality-aware graph encoding of functional connectivity into a lower-dimensional latent space outperform vectorized encoders or direct data space approaches. The proposed multimodal graph VAE (gMMVAE) surpasses alternative generative variants across multiple metrics for generation fidelity, reconstruction quality, efficiency, and latent space discriminability, highlighting its potential for robust multimodal neuroimaging analysis.

摘要：雖然生成模型能夠編碼複雜的神經影像數據以進行特徵生成和重建，但開發具有適當編碼和潛在空間過程的最佳架構框架對於研究大腦的結構和功能特性至關重要。<lb/>我們設計了一個多模態生成框架，通過系統評估編碼策略、潛在多模態融合和生成模型選擇來提取結構和功能性磁共振成像（MRI）特徵。<lb/>利用來自大型神經影像數據集的結構灰質體積（GMV）和靜態功能網絡連接性（sFNC）特徵，我們分析了涉及變分自編碼器（VAEs）、Transformer、生成對抗網絡（GANs）和擴散模型的生成框架。<lb/>採用模態感知圖形編碼功能連接到較低維度潛在空間的架構，表現優於向量化編碼器或直接數據空間方法。<lb/>所提出的多模態圖形變分自編碼器（gMMVAE）在生成保真度、重建質量、效率和潛在空間可區分性等多個指標上超越了其他生成變體，突顯了其在穩健的多模態神經影像分析中的潛力。

##### **Physics-guided spatiotemporal neural models for fuel density prediction**
2607.06999v1 by Tolga Caglar, Jaynil Jaiswal, Saqib Azim, Yudhir Gala, Mai H. Nguyen, Ilkay Altintas

This paper presents a physics-guided machine learning (PGML) framework for fuel density prediction, integrating physics constraints and domain knowledge into deep learning models to enhance model accuracy and stability. We explore three deep learning architectures -- ConvLSTM, Adaptive Fourier Neural Operator (AFNONet), and Video Vision Transformer (ViViT) -- to model the spatiotemporal evolution of fuel density. Our approach incorporates differentiable physics-informed terms in the loss function, including a mass-conserving fuel transport term and a rate-of-spread estimation. Experimental results, averaged across multiple independent trials, demonstrate that the proposed PGML framework outperforms purely data-driven baselines without physics constraints in both accuracy and stability. This framework enables computationally efficient, physically plausible fire forecasting to support adaptive prescribed burn management.

摘要：這篇論文提出了一個物理引導的機器學習（PGML）框架，用於燃料密度預測，將物理約束和領域知識整合到深度學習模型中，以提高模型的準確性和穩定性。<lb/>我們探索了三種深度學習架構——ConvLSTM、Adaptive Fourier Neural Operator (AFNONet) 和 Video Vision Transformer (ViViT)——來建模燃料密度的時空演變。<lb/>我們的方法在損失函數中納入了可微分的物理知識項，包括一個質量守恆的燃料運輸項和擴散速率估計。<lb/>實驗結果在多次獨立試驗中取平均，顯示所提出的PGML框架在準確性和穩定性上均優於沒有物理約束的純數據驅動基準。<lb/>該框架使得計算高效且物理上合理的火災預測成為可能，以支持自適應的預定燃燒管理。

##### **Large Behavior Model: A Promptable Digital Twin of the Retail Customer**
2607.06993v2 by Wachiravit Modecrua, Krittin Pachtrachai, Touchapon Kraisingkorn

Customer behavior modeling underpins recommendation, marketing, and decision support, yet existing approaches either optimize predictive accuracy without explaining decisions or simulate users without grounding them in real behavioral data. We present the Large Behavioral Model (LBM) that learns customer decision making directly from large-scale retail transactions through a unified Person-Environment formulation. Customer state is represented by a behavioral profile derived from historical purchases, while product context is incorporated through retrieval-augmented generation. The model is trained using continued pre-training on verbalized behavioral data, supervised fine-tuning for decision generation, and reinforcement learning with verifiable rewards for evidence-based calibration. We evaluate the proposed framework on purchase prediction, hard-negative discrimination, basket completion, promotion response, and cross-domain voucher redemption. The model consistently outperforms frontier general-purpose language models on in-domain retail tasks while demonstrating strong zero-shot and fine-tuned transfer across retailers and decision domains. Ablation studies show that continued pre-training is the primary driver of behavioral generalization, retrieval is most effective when applied during both training and inference, and reinforcement learning improves reliance on explicit behavioral evidence over generic language-model priors. These results demonstrate that behavioral knowledge encoded in transaction histories can be effectively learned by language models, providing a scalable foundation for customer digital twins and behavior simulation.

摘要：客戶行為建模是推薦、行銷和決策支持的基礎，但現有的方法要麼優化預測準確性而不解釋決策，要麼模擬用戶卻不以真實行為數據為基礎。我們提出了大型行為模型（LBM），該模型通過統一的人-環境公式直接從大規模零售交易中學習客戶的決策過程。客戶狀態由歷史購買衍生的行為檔案表示，而產品背景則通過檢索增強生成來納入。該模型使用對口語化行為數據的持續預訓練、針對決策生成的監督微調，以及帶有可驗證獎勵的增強學習進行訓練，以實現基於證據的校準。我們在購買預測、困難負樣本區分、購物籃完成、促銷反應和跨領域優惠券兌換上評估了所提出的框架。該模型在內部零售任務中持續超越前沿通用語言模型，同時在零售商和決策領域之間展示出強大的零樣本和微調轉移能力。消融研究表明，持續預訓練是行為泛化的主要驅動因素，檢索在訓練和推理期間應用時最有效，而增強學習則提高了對明確行為證據的依賴程度，超過了通用語言模型的先驗知識。這些結果表明，交易歷史中編碼的行為知識可以被語言模型有效學習，為客戶數位雙胞胎和行為模擬提供可擴展的基礎。

##### **Comprehensive Evaluation of Large Language Model Responses: A Multi-Factor Scoring System**
2607.06940v1 by Yiming Gai, Junde Lu, Xuefei Huang

The remarkable performance of large language models (LLMs) in linguistic tasks underscores an urgent need for comprehensive evaluation of their response quality. Prevailing methods, often confined to singular dimensions, fall short of capturing the full spectrum of model capabilities. This study introduces a multifactor scoring paradigm, integrating accuracy, conciseness, factual consistency, readability, and coherence, complemented by a graphical user interface (GUI) for visualizing outcomes. Evaluations on the TruthfulQA dataset unveil mainstream LLMs' strengths in reasoning tasks (peaking at a composite score of 0.6104) alongside pervasive limitations in navigating complex facts and ambiguities. Transcending the narrow lens of traditional metrics, this framework offers a transparent, adaptable avenue to illuminate model potential and deficiencies. Though presently focused on English tasks, its horizons beckon toward multilingual domains. This work carves a novel path for knowledge engineering and model refinement.

摘要：大型語言模型（LLMs）在語言任務中的卓越表現凸顯了對其回應質量進行全面評估的迫切需求。現行方法往往局限於單一維度，無法捕捉模型能力的全貌。這項研究引入了一種多因素評分範式，整合了準確性、簡潔性、事實一致性、可讀性和連貫性，並輔以圖形用戶界面（GUI）來可視化結果。對TruthfulQA數據集的評估揭示了主流LLMs在推理任務中的優勢（最高綜合得分為0.6104），同時也顯示出在處理複雜事實和模糊性方面的普遍限制。超越傳統指標的狹隘視角，這一框架提供了一條透明且可調整的途徑，以揭示模型的潛力和不足。儘管目前專注於英語任務，但其前景向多語言領域延伸。這項工作為知識工程和模型精煉開辟了一條新路。

##### **LLMs Silently Correct African American English: Auditing and Mitigating Dialect Bias via Activation Steering**
2607.06845v1 by Huan Wu, Ali Emami, Muhammad Furquan Hassan, Osaretin Igbinoba, Osakpolor Idusuyi, Osamede Igbinoba, Faiza Khan Khattak, Laleh Seyyed-Kalantari

African American English (AAE), a rule-governed dialect spoken by over 30 million people, is routinely misinterpreted and "corrected" by large language models (LLMs). Across six instruction-tuned LLMs (14B to 70B), we show that state-of-the-art models systematically prefer Standard American English (SAE) continuations even when the preceding context is in AAE, effectively rewriting AAE into SAE. We present an end-to-end framework to audit and mitigate this bias. For auditing, we introduce conditional Dialect Group Invariance (cDGI), which isolates true model bias from translator-induced artifacts, and a feature-level localization analysis that identifies which AAE markers most strongly trigger bias; we find that syntactic constructions, especially negative concord (e.g., "ain't nobody"), are universal triggers across all models. For mitigation, we introduce, to our knowledge, the first application of activation steering to dialect bias: a training-free, test-time method that extracts dialect directions via causal tracing and injects them into bias-relevant layers. Activation steering reduces bias 5 to 20 times more than prompting while preserving SAE fluency. To enable this work, we release REAL-AAE , the largest real-AAE parallel corpus to date: 17,479 AAE/SAE/ AAE_back triplets from natural tweets (2 to 6 times larger than prior real-AAE resources), validated automatically (BERTScore F1 = 0.95) and by three native AAE speakers (83.0% semantic agreement).

摘要：非裔美國英語（AAE）是一種由超過 3000 萬人使用的受規則約束的方言，經常被大型語言模型（LLMs）誤解和“修正”。在六個經過指令調整的 LLM（14B 到 70B）中，我們展示了最先進的模型系統性地偏好標準美國英語（SAE）延續，即使前文上下文是 AAE，實際上將 AAE 重新寫入 SAE。我們提出了一個端到端的框架來審核和減輕這種偏見。為了進行審核，我們引入了條件方言群體不變性（cDGI），它將真正的模型偏見與翻譯者引起的工件隔離開來，並進行特徵級的定位分析，以識別哪些 AAE 標記最強烈地觸發偏見；我們發現語法結構，特別是負一致（例如，“ain't nobody”），是所有模型的普遍觸發因素。為了減輕這種偏見，我們引入了我們所知的第一個激活引導應用於方言偏見：一種無需訓練的測試時方法，通過因果追踪提取方言方向，並將其注入到與偏見相關的層中。激活引導在保持 SAE 流暢性的同時，將偏見減少 5 到 20 倍，超過了提示方法。為了支持這項工作，我們發布了 REAL-AAE，迄今為止最大的真實 AAE 平行語料庫：來自自然推文的 17,479 個 AAE/SAE/AAE_back 三元組（比以前的真實 AAE 資源大 2 到 6 倍），經過自動驗證（BERTScore F1 = 0.95）和三位母語 AAE 語者驗證（83.0% 的語義一致性）。

##### **When Agents Go Rogue: Activation-Based Detection of Malicious Behaviors in Multi-Agent Systems**
2607.06807v1 by Haowen Xu, Xue Tan, Lei Ma, Zhihao Zhang, Chao Wang, Qingze Wang, Ping Chen, Jun Dai, Xiaoyan Sun

While enabling effective collaboration on complex tasks, LLM-based Multi-Agent Systems (MAS) face critical security challenges due to vulnerabilities at the agent and interaction levels. Most existing MAS security defenses are built upon two core assumptions: semantically-explicit malicious attacks and explicit graph-based modeling of the MAS topology and agent-level interactions. In practice, real-world attacks are becoming more semantically stealthy, while MAS execution is typically asynchronous without the temporal alignment assumed by graph-based propagation models. To address these limitations, we propose AcMAS, an activation-based framework for malicious-behavior detection in MAS. By analyzing internal reasoning states in the activation space of local agents, AcMAS detects even stealthy attacks in a synchronization-robust fashion, without relying on explicit interaction graphs. Moreover, our activation analysis provides critical signals to guide AcMAS in restoring the functionality of compromised agents, rather than the disruptive agent isolation commonly used by the state-of-the-art methods. Comprehensive evaluation demonstrates that AcMAS significantly outperforms graph-based baselines against stealthy attacks, by +0.22 F1 in synchronous settings (0.94 vs. 0.72) and by +0.55 F1 in asynchronous settings (0.93 vs. 0.38), with generalization across diverse open-source LLM backbones, attack intensity, and MAS scale.

摘要：在促進複雜任務的有效協作時，基於大型語言模型（LLM）的多代理系統（MAS）面臨由於代理和互動層面的脆弱性所帶來的重大安全挑戰。大多數現有的MAS安全防禦是建立在兩個核心假設之上：語義明確的惡意攻擊和明確的基於圖的MAS拓撲及代理級互動建模。實際上，現實世界的攻擊變得越來越具有語義隱蔽性，而MAS的執行通常是異步的，並不具備基於圖的傳播模型所假設的時間對齊。為了解決這些局限性，我們提出了AcMAS，一種基於激活的惡意行為檢測框架。通過分析本地代理在激活空間中的內部推理狀態，AcMAS以同步穩健的方式檢測即使是隱蔽的攻擊，而不依賴於明確的互動圖。此外，我們的激活分析提供了關鍵信號，以指導AcMAS恢復受損代理的功能，而不是當前最先進方法中常用的破壞性代理隔離。全面的評估顯示，AcMAS在對抗隱蔽攻擊方面顯著超越基於圖的基準，在同步設置中提高了+0.22 F1（0.94對0.72），在異步設置中提高了+0.55 F1（0.93對0.38），並在多樣的開源LLM骨幹、攻擊強度和MAS規模中實現了泛化。

##### **Enhancing deep learning models for time series classification via knowledge distillation**
2607.06796v1 by Javidan Abdullayev, Maxime Devanne, Jonathan Weber, Germain Forestier

Deep learning has achieved remarkable success in various domains including time series analysis, computer vision and natural language processing. However, high computational and memory demands of state-of-the-art architectures pose challenges for deployment in resource-limited environments. Knowledge Distillation (KD) addresses this by transferring knowledge from a large teacher model to a smaller, more efficient student model while maintaining competitive performance. In this work, we investigate the effectiveness of KD for Time Series Classification (TSC) across three architectures: the classical Fully Convolutional Network (FCN), the convolutional Inception model and the transformer-based ConvTran model. We evaluate our approach on UCR Archive, the largest benchmark repository of time series datasets, by modifying architectural components such as convolutional filters, Inception modules and attention heads across the three architectures. Our results consistently show that KD most effectively benefits student models of intermediate complexity across all three architectures, with the distilled FCN student reducing parameters by a factor of 38, the distilled Inception student achieving nearly the same performance as the teacher with 42% fewer parameters and the distilled ConvTran student with 2 attention heads showing the most significant improvement through distillation. To encourage further research and reproducibility, we provide our implementation at https://github.com/MSD-IRIMAS/KD-4-TSC.

摘要：深度學習在各個領域取得了顯著的成功，包括時間序列分析、計算機視覺和自然語言處理。<lb/>然而，最先進架構的高計算和記憶體需求對於在資源有限的環境中部署提出了挑戰。<lb/>知識蒸餾（KD）通過將知識從大型教師模型轉移到較小、更高效的學生模型來解決這一問題，同時保持競爭力的性能。<lb/>在本研究中，我們探討了KD在時間序列分類（TSC）中的有效性，涵蓋三種架構：經典的全卷積網絡（FCN）、卷積的Inception模型和基於Transformer的ConvTran模型。<lb/>我們在UCR Archive上評估我們的方法，這是最大的時間序列數據集基準庫，通過修改三種架構中的建築組件，如卷積濾波器、Inception模塊和注意力頭。<lb/>我們的結果一致顯示，KD在所有三種架構中對中等複雜度的學生模型最有效，蒸餾的FCN學生將參數減少了38倍，蒸餾的Inception學生在參數減少42%的情況下達到了幾乎與教師相同的性能，而蒸餾的ConvTran學生則在只有2個注意力頭的情況下顯示出通過蒸餾獲得的最顯著改進。<lb/>為了鼓勵進一步的研究和可重現性，我們在https://github.com/MSD-IRIMAS/KD-4-TSC提供了我們的實現。

##### **A Continual Learning Framework for Adaptive Control of Modular Soft Robots**
2607.06740v1 by Nilay Kushawaha, Muhammad Sunny Nazeer, Baljinder Singh Bal, Cecilia Laschi, Egidio Falotico

Soft robots have attracted significant attention in applications such as medical intervention, rehabilitation, and robotic manipulation due to their inherent compliance, flexibility, and high degrees of freedom. Modular soft robots (MSRs), composed of multiple interconnected segments, represent an emerging class of robotic systems with highly deformable and reconfigurable structures capable of performing complex tasks. However, designing controllers for MSRs remains challenging due to their nonlinear dynamics, modeling complexity, and hyper-redundant nature. Existing approaches typically require controllers to be retrained from scratch whenever the robot morphology changes. In this work, we address these challenges through a continual learning inspired control framework capable of incrementally adapting to changes in robot morphology while preserving previously acquired knowledge. Specifically, the proposed framework enables the controller to sequentially learn new MSR configurations without forgetting previously learned ones. In addition, for MSRs with fixed configurations, the same framework can be employed in a distributed manner to learn module-specific dynamics, enabling localized control and improved precision. The proposed approach is validated through closed-loop trajectory tracking experiments in simulation using a tendon-driven soft robot, as well as on a real-world three-module pneumatic soft robotic arm. Furthermore, we demonstrate the adaptive capabilities of the framework through a reaching experiment in which the controller selectively activates only the necessary modules to reach a virtual target position, thereby reducing computational overhead.

摘要：柔性機器人因其固有的順應性、靈活性和高自由度，在醫療介入、康復和機器人操作等應用中引起了廣泛關注。模組化柔性機器人（MSRs）由多個互聯的部分組成，代表了一類新興的機器人系統，具有高度可變形和可重構的結構，能夠執行複雜的任務。然而，由於其非線性動力學、建模複雜性和超冗餘特性，為MSRs設計控制器仍然具有挑戰性。現有的方法通常要求在機器人形態發生變化時，控制器必須從頭開始重新訓練。在這項工作中，我們通過一種受持續學習啟發的控制框架來解決這些挑戰，該框架能夠在保持先前獲得知識的同時，逐步適應機器人形態的變化。具體而言，所提出的框架使控制器能夠順序學習新的MSR配置，而不會遺忘先前學習的配置。此外，對於具有固定配置的MSRs，該框架可以以分佈式方式應用，以學習模組特定的動力學，從而實現局部控制和提高精度。所提出的方法通過在模擬中使用一個由腱驅動的柔性機器人進行的閉環軌跡跟踪實驗，以及在現實世界中的三模組氣動柔性機器手臂上進行的實驗進行了驗證。此外，我們通過一個達到實驗展示了該框架的自適應能力，在該實驗中，控制器僅選擇性地激活必要的模組以達到虛擬目標位置，從而減少計算開銷。

##### **Vision Language Action (VLA) Models for Unmanned Aerial Robotics and Bimanual Manipulation: A Review**
2607.06706v1 by Inkyu Sa, Chanoh Park, Hea-Min Lee, Donghee Noh, Ho Seok Ahn

Vision Language Action (VLA) models unify visual perception, natural-language understanding, and action generation within a single foundation model, allowing a robot to follow instructions such as fold the towel or fly to the red building directly from camera images. Because VLAs inherit world knowledge from internet-scale pre-training, they have become the dominant framework for learning-based manipulation, with bimanual coordination serving as the most demanding testbed: two arms with 7 degrees of freedom each must move in concert to fold, assemble, and reorient objects. Unmanned aerial robotics faces a structurally similar challenge: a drone must coordinate thrust, attitude, and increasingly gripper commands from visual observations under strict latency and payload constraints. This review covers 183 contributions spanning 2017-2026 and organized along seven dimensions: VLA architectures, training recipes, action representations, bimanual coordination (2022-2026), unmanned aerial vehicle (UAV) navigation and control (2017-2026), language grounding, and cross-cutting concerns including memory and world models. We show that the coordination strategies, training recipes, and action representations developed for bimanual VLAs transfer to unmanned aerial systems and identify fourteen research directions across both domains.

摘要：視覺語言行動（VLA）模型將視覺感知、自然語言理解和行動生成統一於單一基礎模型中，使機器人能夠直接從攝像頭影像中遵循指令，例如摺疊毛巾或飛往紅色建築。由於VLA從互聯網規模的預訓練中繼承了世界知識，它們已成為基於學習的操控的主導框架，其中雙手協調作為最具挑戰性的測試平台：兩隻手臂各有7個自由度，必須協同運動以摺疊、組裝和重新定位物體。無人機機器人面臨著結構上類似的挑戰：無人機必須在嚴格的延遲和載重限制下，協調推力、姿態和越來越多的夾具命令，這些都是基於視覺觀察。這篇綜述涵蓋了2017年至2026年期間的183項貢獻，並沿著七個維度進行組織：VLA架構、訓練配方、行動表徵、雙手協調（2022-2026）、無人飛行器（UAV）導航和控制（2017-2026）、語言基礎，以及包括記憶和世界模型在內的跨領域問題。我們顯示，為雙手VLA開發的協調策略、訓練配方和行動表徵可以轉移到無人航空系統，並確定了兩個領域中的十四個研究方向。

##### **SPEAR: A Simulator for Photorealistic Embodied AI Research**
2607.06701v1 by Mike Roberts, Renhan Wang, Rushikesh Zawar, Rachith Dey-Prakash, Quentin Leboutet, Stephan R. Richter, Matthias Müller, German Ros, Rui Tang, Stefan Leutenegger, Yannick Hold-Geoffroy, Kalyan Sunkavalli, Vladlen Koltun

Interactive simulators have become powerful tools for training embodied agents and generating synthetic visual data, but existing photorealistic simulators suffer from limited generality, programmability, and rendering speed. We address these limitations by introducing SPEAR: A Simulator for Photorealistic Embodied AI Research. At its core, SPEAR is a Python library that can connect to, and programmatically control, any Unreal Engine (UE) application via a modular plugin architecture. SPEAR exposes over 14K unique UE functions to Python, representing an order-of-magnitude increase in programmable functionality over existing UE-based simulators. Additionally, a single SPEAR instance can render 1920x1080 photorealistic beauty images directly into a user's NumPy array at 73 frames per second - an order of magnitude faster than existing UE plugins - while also providing ground truth image modalities that are not available in any existing UE-based simulator (e.g., a non-diffuse intrinsic image decomposition, material IDs, and physically based shading parameters). Finally, SPEAR introduces an expressive high-level programming model that enables users to specify complex graphs of UE work with arbitrary data dependencies among work items, and to execute these graphs deterministically within a single UE frame. We demonstrate the utility of SPEAR through a diverse collection of example applications: controlling multiple embodied agents with distinct action spaces (e.g., humans, cars, and robots) across several in-the-wild UE projects; rendering photorealistic city-scale environments; manipulating UE's procedural content generation systems; rendering synchronized multi-view images of detailed human faces; coordinating an interactive co-simulation with the MuJoCo physics simulator; and editing scenes with natural language via an AI coding assistant.

摘要：互動模擬器已成為訓練具身代理和生成合成視覺數據的強大工具，但現有的照片真實感模擬器在通用性、可編程性和渲染速度方面存在限制。我們通過引入SPEAR來解決這些限制：一個用於照片真實感具身AI研究的模擬器。在其核心，SPEAR是一個Python庫，可以通過模塊化插件架構連接並以編程方式控制任何虛幻引擎（UE）應用程序。SPEAR向Python暴露了超過14K個獨特的UE函數，這代表著相較於現有的基於UE的模擬器，其可編程功能提高了一個量級。此外，單個SPEAR實例可以以73幀每秒的速度直接將1920x1080的照片真實感美麗圖像渲染到用戶的NumPy數組中，這比現有的UE插件快了一個量級，同時還提供了在任何現有的基於UE的模擬器中都無法獲得的真實圖像模態（例如，非散射內在圖像分解、材質ID和基於物理的陰影參數）。最後，SPEAR引入了一個表達性強的高級編程模型，使得用戶能夠指定具有任意數據依賴關係的UE工作複雜圖形，並在單個UE幀內確定性地執行這些圖形。我們通過多樣的示例應用程序展示了SPEAR的實用性：在幾個現實世界的UE項目中控制具有不同行動空間的多個具身代理（例如，人類、汽車和機器人）；渲染照片真實感的城市規模環境；操縱UE的程序內容生成系統；渲染詳細人臉的同步多視圖圖像；協調與MuJoCo物理模擬器的互動共同模擬；以及通過AI編碼助手以自然語言編輯場景。

##### **Digital Fragmentation and Generative AI Use Across 103 Million Application Events**
2607.06681v1 by Sumer S. Vaid, Ashley V. Whillans

Knowledge workers switch between applications thousands of times per day, spending nearly a tenth of the work year transitioning between digital applications in a process called digital fragmentation. Whether this fragmentation reflects who an employee is, where they work, or what kind of day they are having, has remained an open question. We analyzed 103 million application events recorded second-by-second from 1,017 employees across eight organizations that largely employ knowledge workers (e.g., law, financial services). Day-to-day variation in fragmentation within individual employees accounted for 44.6% of the variation in digital fragmentation, slightly exceeding stable individual differences between employees (35.8%), and far exceeding variation between organizations (19.6%). Fragmentation rose over the work week and reset after weekends and holidays. Higher-than-typical use of communication applications coincided with more fragmented work. Generative AI use also occurred on more fragmented days, but the period following AI use was marked by narrower, longer, and more predictable application use. These findings identify the workday as a key level for understanding and intervening on digital fragmentation and suggest that AI may help structure fragmented work rather than merely intensify it.

摘要：知識工作者每天在應用程式之間切換數千次，花費近十分之一的工作年在數位應用程式之間轉換，這一過程稱為數位碎片化。這種碎片化是否反映了員工的身份、工作地點或他們的日常狀況，仍然是一個未解之謎。我們分析了來自八個主要雇用知識工作者的組織（例如法律、金融服務）中1,017名員工記錄的1.03億個應用事件，這些事件是逐秒記錄的。個別員工的日常碎片化變化佔數位碎片化變化的44.6%，略高於員工之間穩定的個體差異（35.8%），並且遠高於組織之間的變化（19.6%）。在工作周內，碎片化隨著時間上升，並在周末和假期後重置。高於典型水平的通訊應用程式使用與更為碎片化的工作同時發生。生成式AI的使用也發生在更為碎片化的日子，但在使用AI後的時期，應用程式的使用變得更為狹窄、持久且可預測。這些發現將工作日確定為理解和介入數位碎片化的關鍵層面，並表明AI可能有助於結構化碎片化的工作，而不僅僅是加劇它。

##### **Graph Convolutional Attention: A Spectral Perspective on Graph Denoising and Diffusion**
2607.06546v1 by Shervin Khalafi, Igor Krawczuk, Sergio Rozada, Charilaos Kanatsoulis, Antonio G Marques, Alejandro Ribeiro

Denoising graphs is a fundamental problem in graph learning and the core operation of graph diffusion models. Attention-based architectures like graph transformers have recently shown promise in denoising graphs. However, our principled understanding of attention-based graph denoising remains limited, making it unclear whether standard attention is the right mechanism for this task. Here we show that, under a denoising objective, linear attention is suboptimal and can only learn an average spectral denoising filter over the training distribution. This creates a fundamental limitation as graphs often vary spectrally across the distribution. To overcome this limitation, we introduce Spectral Attention, which directly utilizes the input graph spectrum and provably outperforms linear attention by a margin governed by the spectral diversity of the distribution. We then derive Graph Convolutional Attention (GCA), a practical and permutation-equivariant realization of this idea that implements spectral denoising through graph-filtered queries and keys. For stochastic block models, GCA provably matches the idealized Spectral Attention mechanism. We further show that the softmax operation, that follows the attention, provides additional denoising by approximately projecting noisy eigenvectors onto the clean eigenspace. Empirically, replacing linear attention with GCA consistently improves graph denoising and diffusion on synthetic and real datasets, with gains strongly correlated with spectral diversity. In DiGress, GCA matches standard graph-transformer performance without computing expensive structural features, and when combined with the recently proposed PEARL positional encodings, avoids explicit eigendecomposition computations resulting in faster inference without degrading quality. The code can be found here: github.com/shervinkhalafi/graph_conv_att

摘要：去噪圖是圖學習中的一個基本問題，也是圖擴散模型的核心操作。基於注意力的架構，如圖Transformer，最近在去噪圖方面顯示出了潛力。然而，我們對基於注意力的圖去噪的原則性理解仍然有限，這使得標準注意力是否是這項任務的正確機制變得不清楚。在這裡，我們展示了在去噪目標下，線性注意力是次優的，並且只能學習訓練分佈上的平均頻譜去噪濾波器。這造成了一個基本的限制，因為圖在分佈上往往會在頻譜上變化。為了克服這一限制，我們引入了頻譜注意力，它直接利用輸入圖的頻譜，並且在理論上比線性注意力的表現更好，這一優勢由分佈的頻譜多樣性所決定。接著，我們推導出圖卷積注意力（GCA），這是一個實用且具置換等變性的實現，通過圖過濾的查詢和鍵來實現頻譜去噪。對於隨機塊模型，GCA 在理論上與理想化的頻譜注意力機制相匹配。我們進一步展示了，隨後的 softmax 操作通過將噪聲特徵向量大致投影到乾淨的特徵空間中提供額外的去噪。經驗上，將線性注意力替換為 GCA 一致地改善了合成和真實數據集上的圖去噪和擴散，並且增益與頻譜多樣性強相關。在 DiGress 中，GCA 在不計算昂貴的結構特徵的情況下，達到了標準圖Transformer的性能，並且當與最近提出的 PEARL 位置編碼結合時，避免了顯式特徵值分解計算，從而實現了更快的推斷而不降低質量。代碼可以在這裡找到：github.com/shervinkhalafi/graph_conv_att

##### **Hierarchical Acoustic-Semantic Modeling: Modality Separation and Semantic Coherence for Full-Duplex SLMs**
2607.06540v2 by Zhenyu Liu, Xuanyu Zhang, Yunxin Li, Qixun Teng, Shenyuan Jiang, Haolan Chen, Minjun Zhao, Fanbo Meng, Yu Xu, Yancheng He, Baotian Hu, Haizhou Li, Min Zhang

Developing seamless, high-performance, native intelligent full-duplex Spoken Language Models (SLMs) remains a critical challenge and long-standing goal for the speech and NLP community. Despite notable progress, recent endeavors are fundamentally constrained by severe modality interference, which causes substantial knowledge degradation and compromises semantic integrity -- ultimately making full-duplex SLMs feel unnatural and unintelligent. In this paper, through an exhaustive fine-grained analysis of model optimization dynamics, we uncover the root cause of such performance degradation, revealing that modality interference arises from inherent gradient conflicts between acoustic and semantic modeling when the two modalities are forced to share a deep parameter space. Guided by this key insight, we introduce Lychee-FD, a native end-to-end full-duplex framework designed to mitigate modality interference. Importantly, we propose a hierarchical parameter separation strategy that decouples conflicting modalities in deep layers while preserving cross-modality coherence via a dedicated semantic alignment channel. Extensive experiments on multiple full-duplex benchmarks demonstrate that our method significantly advances the state of the art, yielding substantial improvements in both speech intelligence (+7.4% on Spoken QA) and full-duplex interaction fluidity (+28.5% on FullDuplexBench 1.5) without compromising inference efficiency. To the best of our knowledge, this work is the first to achieve two key advances: 1) uncovering and elucidating the root cause of modality interference in full-duplex SLMs, and 2) designing an elegant hierarchical model together with a practical solution for seamless, high-performance, native intelligent full-duplex SLMs.

摘要：開發無縫、高效能、原生智能的全雙工語言模型（SLMs）仍然是語音和自然語言處理社群面臨的關鍵挑戰和長期目標。儘管取得了顯著進展，近期的努力仍然受到嚴重的模態干擾的根本限制，這導致了知識的顯著退化並妨礙了語義的完整性——最終使得全雙工SLMs感覺不自然且缺乏智能。在本文中，通過對模型優化動態的徹底細緻分析，我們揭示了這種性能退化的根本原因，顯示模態干擾源於聲學和語義建模之間的固有梯度衝突，當這兩種模態被迫共享一個深層參數空間時便會出現此問題。在這一關鍵見解的指導下，我們引入了Lychee-FD，一個旨在減輕模態干擾的原生端到端全雙工框架。重要的是，我們提出了一種層次參數分離策略，該策略在深層中解耦衝突的模態，同時通過專門的語義對齊通道保持跨模態的一致性。在多個全雙工基準上的廣泛實驗表明，我們的方法顯著推進了技術的最前沿，在語音智能（在Spoken QA上提高了7.4%）和全雙工互動流暢性（在FullDuplexBench 1.5上提高了28.5%）方面均取得了實質性改進，且不妨礙推理效率。據我們所知，這項工作是首次實現兩項關鍵進展：1）揭示並闡明全雙工SLMs中模態干擾的根本原因，以及2）設計出一個優雅的層次模型，並提供一個實用的解決方案，以實現無縫、高效能、原生智能的全雙工SLMs。

##### **RSF-GLLM: Bridging the Semantic Gap in Multi-Hop Knowledge Graph QA via Recurrent Soft-Flow and Decoupled LLM Generation**
2607.06527v1 by Sambaran Bandyopadhyay, Ananth Muppidi

Multi-hop Question Answering over Knowledge Graphs faces a critical challenge: traditional retrieve-then-read pipelines break differentiability, preventing the retriever from learning to bridge the semantic gap where intermediate nodes lack lexical overlap with the query. To address this, we propose RSF-GLLM, a framework decoupling differentiable graph reasoning from answer generation. Our Recurrent Soft-Flow (RSF) module employs a GRU-guided query updater to propagate continuous relevance scores, utilizing a dynamic gating mechanism to traverse semantically dissimilar bridge nodes via structural cues. We introduce flow sparsity regularization to theoretically guarantee convergence from soft probabilities to discrete reasoning paths. These paths are extracted and textualized to fine-tune a Large Language Model (LLM), ensuring generation is grounded in factual topology. Experiments on WebQSP and CWQ demonstrate that RSF-GLLM achieves competitive performance with superior inference efficiency compared to LLM based computationally expensive approaches.

摘要：多跳問題回答在知識圖譜上面臨一個關鍵挑戰：傳統的檢索-再讀取管道破壞了可微分性，阻止了檢索器學習彌合中間節點與查詢之間缺乏詞彙重疊的語義差距。為了解決這個問題，我們提出了RSF-GLLM，一個將可微分圖推理與答案生成解耦的框架。我們的遞迴軟流（RSF）模組利用GRU引導的查詢更新器來傳播連續的相關性分數，利用動態閘機制通過結構線索遍歷語義上不相似的橋接節點。我們引入流稀疏正則化，理論上保證從軟概率收斂到離散推理路徑。這些路徑被提取並轉換為文本，以微調大型語言模型（LLM），確保生成的內容基於事實拓撲。在WebQSP和CWQ上的實驗表明，RSF-GLLM在推理效率上優於基於LLM的計算昂貴的方法，實現了競爭力的性能。

##### **AirflowAttack: Thermal-Airflow Adversarial Perturbations against Infrared Remote-Sensing Vision-Language Models**
2607.06485v1 by Cong Su, Jiaju Han, Xuemeng Sun, Chengyin Hu, Qike Zhang, Jiujiang Guo, Yiwei Wei, Jiahuan Long

Vision-language models (VLMs) are increasingly deployed on infrared (IR) remote sensing imagery in security-critical settings, yet their adversarial robustness remains unexamined. We present AirflowAttack, to our knowledge the first adversarial attack for IR remote-sensing VLMs and the first to weaponize thermal-airflow turbulence as the perturbation prior. A lightweight generator synthesizes a single input-agnostic perturbation regularized toward physically plausible airflow patterns. Optimized on one surrogate CLIP model, it attains a mean zero-shot scene-classification attack success rate (ASR, the fraction of samples whose top-1 class changes) of 48.5% across five diverse CLIP backbones, far exceeding four IR-specific physical baselines (27.7--37.0%). Applied to six state-of-the-art VLMs, it cuts scene-classification accuracy by up to 38.2% relative, yet paradoxically makes some models more confident in their IR analysis, confabulating the perturbation as genuine thermal evidence such as temperature gradients and convection. Ablations show the airflow prior raises physical plausibility at no measurable cost to attack success. Together with a benchmark spanning eleven models and four tasks, these findings expose critical vulnerabilities in the rapidly expanding IR VLM ecosystem.

摘要：視覺-語言模型（VLMs）在安全關鍵環境中越來越多地應用於紅外（IR）遙感影像，但其對抗穩健性仍未被檢視。<lb/>我們提出了AirflowAttack，據我們所知，這是首個針對IR遙感VLM的對抗攻擊，也是首個將熱氣流湍流作為擾動先驗的武器化攻擊。<lb/>一個輕量級生成器合成單一輸入無關的擾動，並朝著物理上合理的氣流模式進行正則化。<lb/>在一個替代的CLIP模型上進行優化，它在五個不同的CLIP骨幹上達到48.5%的平均零-shot場景分類攻擊成功率（ASR，即頂級類別變化的樣本比例），遠超過四個IR特定的物理基準（27.7--37.0%）。<lb/>應用於六個最先進的VLM，它相對減少了場景分類的準確率高達38.2%，然而矛盾的是，這使得某些模型在其IR分析中變得更有信心，將擾動錯誤地解釋為真實的熱證據，例如溫度梯度和對流。<lb/>消融實驗顯示，氣流先驗在不影響攻擊成功率的情況下提高了物理合理性。<lb/>結合涵蓋十一個模型和四個任務的基準，這些發現揭示了快速擴展的IR VLM生態系統中的關鍵脆弱性。

##### **Data Analysis in the Wild: Benchmarking Large Language Models Against Real-World Data Complexities**
2607.06482v1 by So Hasegawa, Shailaja Keyur Sampat, Lei Liu, Wei-Peng Chen

Current benchmarks for evaluating Large Language Models (LLMs) in data analysis often fail to reflect real-world settings. They typically focus on fact retrieval from small tables and overlook the challenges of large multi-tabular datasets, external knowledge integration, and exploratory insight discovery. We introduce DataGovBench, a benchmark derived from governmental open data designed to evaluate LLMs in practical scenarios. The benchmark includes two tasks: Table QA that requires solving complex decomposable questions and producing textual answers or visualizations, and Table Insight that evaluates the ability of models to generate expert-level findings through exploratory data analysis. Comprehensive experiments with state-of-the-art LLMs, both with and without agentic frameworks, reveal significant performance gaps across both tasks. These results suggest that current LLM-based systems remain far from satisfying the demands of real-world data analytics. DataGovBench provides a challenging benchmark for advancing research on LLMs capable of both answering analytical queries and discovering insights from data. Code and sample data are available at https://github.com/SoHasegawa/datagovbench.

摘要：目前評估大型語言模型（LLMs）在數據分析中的基準往往無法反映現實世界的情境。<lb/>它們通常集中於從小型表格中檢索事實，並忽視了大型多表數據集、外部知識整合和探索性見解發現的挑戰。<lb/>我們介紹了DataGovBench，這是一個源自政府開放數據的基準，旨在評估LLMs在實際場景中的表現。<lb/>該基準包括兩個任務：表格問答（Table QA），需要解決複雜的可分解問題並生成文本答案或可視化，和表格見解（Table Insight），評估模型通過探索性數據分析生成專家級發現的能力。<lb/>對最先進的LLMs進行的綜合實驗，無論是否使用代理框架，都顯示出兩個任務之間存在顯著的性能差距。<lb/>這些結果表明，目前基於LLM的系統仍然遠未滿足現實世界數據分析的需求。<lb/>DataGovBench為推進能夠回答分析查詢並從數據中發現見解的LLMs研究提供了一個具有挑戰性的基準。<lb/>代碼和示例數據可在 https://github.com/SoHasegawa/datagovbench 獲得。

##### **Analysis-by-Proxy: Localization Signals in VLMs Operating as Condition Encoders**
2607.06445v1 by Yoav Baron, Sara Dorfman, Roni Paiss, Daniel Cohen-Or, Or Patashnik

Vision-Language Models (VLMs) are increasingly utilized as the conditioning backbone for diffusion-based image editing due to their remarkable multimodal reasoning capabilities. While standalone VLMs demonstrate strong localization capabilities, editing pipelines frequently struggle to maintain this accuracy, particularly in complex, multi-entity scenes. In this work, we investigate this performance gap, hypothesizing that it stems from treating the VLM as a condition encoder. In this role, the model is restricted to a single forward pass, preventing the autoregressive generation process for which it was optimized, thereby failing to fully expose its capabilities. To investigate whether this spatial understanding persists when the VLM is used as a condition encoder, we introduce Analysis-by-Proxy. In this framework, we train a lightweight, interpretable proxy model on the VLM's intermediate representations using an auxiliary localization task. By analyzing the VLM through this proxy, we uncover the specific VLM representations that encode localization information. Our findings expose a fundamental mismatch between how spatial knowledge is represented within a VLM condition encoder and how it is extracted by current editing pipelines. We reveal that under single-pass constraints, the localization signal does not reliably propagate to the predefined layer configurations commonly used for conditioning. Instead, this crucial signal remains hidden within intermediate representations, at locations that vary depending on the input prompt. Using our introduced Analysis-by-Proxy framework, we reveal the fundamental failures of existing condition extraction strategies in editing pipelines, opening the door to more principled design of conditioning architectures.

摘要：視覺-語言模型（VLMs）因其卓越的多模態推理能力，越來越多地被用作基於擴散的圖像編輯的條件基礎。雖然獨立的VLM顯示出強大的定位能力，但編輯流程經常難以維持這種準確性，特別是在複雜的多實體場景中。在這項工作中，我們調查了這一性能差距，假設這源於將VLM視為條件編碼器。在這個角色中，模型被限制為單次前向傳遞，阻止了其為之優化的自回歸生成過程，從而未能充分展現其能力。為了調查當VLM被用作條件編碼器時，這種空間理解是否持續存在，我們引入了代理分析。在這個框架中，我們在VLM的中間表示上訓練了一個輕量級、可解釋的代理模型，使用輔助定位任務。通過這個代理分析VLM，我們揭示了編碼定位信息的特定VLM表示。我們的發現揭示了VLM條件編碼器內部空間知識的表示方式與當前編輯流程提取方式之間的根本不匹配。我們揭示，在單次傳遞的約束下，定位信號並不可靠地傳播到常用於條件設定的預定層配置中。相反，這一關鍵信號仍然隱藏在中間表示中，位置根據輸入提示而異。通過我們引入的代理分析框架，我們揭示了現有編輯流程中條件提取策略的根本失敗，為更具原則性的條件架構設計打開了大門。

##### **TopoBrick: Agentic Topology Sampling of Exogenous Variables for Zero-Shot Building IoT Forecasting**
2607.06349v1 by Xiachong Lin, Du Yin, Arian Prabowo, Hao Xue, Wen Hu, Imran Razzak, Matthew Amos, Sam Behrens, Flora D. Salim

Building sensors are embedded in physical topology, spatial hierarchy, and operational context, yet existing forecasters often treat them as isolated time series or rely on fixed covariate sets. We present TopoBrick, a training-free framework for zero-shot building IoT (Internet-of-Things) forecasting. TopoBrick uses building knowledge graphs to construct a compact structural skeleton and employs an agentic topology sampler to select target-specific exogenous variables. The selected variables are organized by deployment-time availability, separating past-known sensor states from future-known calendar, schedule, and meteorological exogenous variables. Across three real-world buildings, TopoBrick outperforms strong zero-shot foundation-model baselines and remains competitive with fully trained building-specific models. Ablations show that topology-aware sampling is more reliable than random, ontology-only, or fixed-hop selection, especially for physically coupled HVAC and weather-driven sensing variables.

摘要：建築傳感器嵌入在物理拓撲、空間層級和操作上下文中，但現有的預測模型通常將它們視為孤立的時間序列或依賴固定的協變量集。我們提出了TopoBrick，一個無需訓練的零-shot建築物物聯網（IoT）預測框架。TopoBrick利用建築知識圖來構建一個緊湊的結構骨架，並使用代理拓撲抽樣器來選擇特定目標的外生變量。所選變量按部署時可用性進行組織，將過去已知的傳感器狀態與未來已知的日曆、時間表和氣象外生變量分開。在三個真實建築中，TopoBrick的表現超過了強大的零-shot基礎模型基準，並且在與完全訓練的建築特定模型相比時仍具競爭力。消融實驗顯示，考慮拓撲的抽樣比隨機、僅基於本體或固定跳數的選擇更可靠，特別是對於物理耦合的HVAC和氣象驅動的感測變量。

##### **Task Decomposition-Guided Reranking for Adaptive Agent Skill Retrieval**
2607.06283v1 by Yanping Chen, Weijie Shi, Wen Yang, Jiajie Xu

Skill usage can significantly enhance the ability of modern agent systems to complete complex tasks. However, the growing scale of skill libraries makes accurate skill selection increasingly challenging. In real-world scenarios, ambiguous semantic matching often arises between a specific task requirement and multiple generic yet semantically similar candidate skills. Moreover, existing methods tend to overlook the dynamic influence of task difficulty and skill applicability when selecting the optimal target skill set. To address these issues, we propose SkillReranker, an inference-time reranking framework for adaptive skill selection. Specifically, we first perform semantic decomposition on both the task and skill sides, yielding informative subtask and execution-state descriptions as well as transition-state descriptions that characterize each skill's functionality. These descriptions are then used to construct a directed acyclic execution graph, where intermediate task states are modeled as nodes and candidate skills as edges, thereby establishing a structured task-skill correspondence. On this basis, SkillReranker determines whether each state node satisfies the split condition to identify subtask intervals. For each task interval, we employ a cross-encoder to perform comprehensive scoring over candidate skills and select the most suitable ones to form the final target skill set. Experiments on ALFWorld and ScienceWorld with three backbone LLMs show that SkillReranker effectively improves task performance, reduces environment interaction steps, and lowers token consumption compared with existing skill selection baselines.

摘要：技能的使用可以顯著增強現代代理系統完成複雜任務的能力。<lb/>然而，技能庫的擴大使得準確的技能選擇變得越來越具挑戰性。<lb/>在現實場景中，特定任務需求與多個通用但語義相似的候選技能之間經常出現模糊的語義匹配。<lb/>此外，現有方法在選擇最佳目標技能集時往往忽略了任務難度和技能適用性的動態影響。<lb/>為了解決這些問題，我們提出了SkillReranker，一個用於自適應技能選擇的推理時重新排序框架。<lb/>具體而言，我們首先對任務和技能雙方進行語義分解，產生信息豐富的子任務和執行狀態描述，以及描述每個技能功能的過渡狀態描述。<lb/>然後，這些描述被用來構建一個有向無環執行圖，其中中間任務狀態被建模為節點，候選技能則作為邊，從而建立結構化的任務-技能對應關係。<lb/>在此基礎上，SkillReranker 確定每個狀態節點是否滿足拆分條件，以識別子任務區間。<lb/>對於每個任務區間，我們使用交叉編碼器對候選技能進行全面評分，並選擇最合適的技能以形成最終的目標技能集。<lb/>在 ALFWorld 和 ScienceWorld 上進行的實驗顯示，使用三個主幹 LLM 的 SkillReranker 有效提高了任務性能，減少了環境互動步驟，並降低了令牌消耗，與現有的技能選擇基準相比。

##### **Early Language Learning via Spreading Activation and Category Exploration in Complex Networks**
2607.06258v1 by Salvatore Citraro

Is word acquisition in children uneven with respect to semantic and lexical categories? To answer this question, we model early language learning as a search on a graph-based mental lexicon, driven by two interacting processes: spreading activation and an enforced exploration (rather than exploitation) of lexical categories. We evaluate model performance on four languages (German, English, Dutch, and Rioplatense Spanish), using CDIs as ground-truth data for lexical categories, normative ages derived from the Wordbank repository, and state-of-the-art resources for reconstructing graphs of word similarities. We find that spreading activation outperforms a shortest path baseline in simulating normative word acquisition. At the category level, we highlight complex transitions between CDIs. By studying their sequences in terms of burstiness and average persistence time within the same CDI, we find that spreading activation better captures the exploration dynamics observed empirically. Overall, our findings suggest that vocabulary development can be understood through the non-trivial interplay between activation dynamics and some degree of constraints regulating the visiting of lexical categories in complex networks.

摘要：兒童的詞彙獲取在語義和詞彙類別上是否不均衡？為了回答這個問題，我們將早期語言學習建模為在基於圖的心理詞彙上的搜索，這一過程由兩個相互作用的過程驅動：擴散激活和對詞彙類別的強制探索（而非利用）。我們在四種語言（德語、英語、荷蘭語和里奧普拉滕西西班牙語）上評估模型性能，使用CDI作為詞彙類別的真實數據，從Wordbank資料庫導出的標準年齡，以及用於重建單詞相似度圖的最先進資源。我們發現擴散激活在模擬標準詞彙獲取方面優於最短路徑基準。在類別層面上，我們突出了CDI之間的複雜過渡。通過研究它們在同一CDI內的序列，從突發性和平均持續時間的角度，我們發現擴散激活更好地捕捉到實證觀察到的探索動態。總的來說，我們的研究結果表明，詞彙發展可以通過激活動態與某種程度的約束在複雜網絡中調節詞彙類別訪問之間的非平凡相互作用來理解。

##### **A toy framework for single and multi-agent human-AI curiosity ecosystems**
2607.06214v1 by Ilya E. Monosov

This paper offers a toy framework for considering curiosity as an ecosystem. First, it suggests that a single agent's inquiry policy (how, when, and why an agent asks a question) depends on how the agent values immediate uncertainty reduction, costs, delayed return, and the value of keeping the question open. A key concept in the framework is that the weights on these decision-related terms can change with experience. For example, a period of cheap, quickly answered questions may change the cost of inquiry on a short timescale and change which kinds of questions the agent is drawn to answer over a longer timescale. Second, these ideas are extended to many agents exploring a shared knowledge landscape, and there the framework tracks inquiry volume, topic diversity, frontier-directed inquiry, redundancy, and reusable knowledge. The result is a conceptual toy framework for studying curiosity ecology and for future efforts towards designing multi-agent AI systems for discovery. It serves as a companion piece for a paper currently under review in Trends in Neurosciences.

摘要：這篇論文提供了一個玩具框架，用於將好奇心視為一個生態系統。首先，它建議單一代理的詢問政策（代理如何、何時以及為什麼提出問題）取決於代理對立即不確定性降低、成本、延遲回報以及保持問題開放的價值的評估。該框架中的一個關鍵概念是，這些與決策相關的術語的權重可以隨著經驗而改變。例如，一段時間內便宜且快速回答的問題可能會在短期內改變詢問的成本，並在更長期內改變代理被吸引回答的問題類型。其次，這些想法擴展到多個代理探索共享知識空間，在那裡框架追蹤詢問量、主題多樣性、前沿導向的詢問、冗餘和可重用知識。其結果是一個概念性的玩具框架，用於研究好奇心生態學，以及未來設計多代理AI系統以促進發現的努力。它作為一篇目前正在《神經科學趨勢》期刊審稿中的論文的伴隨作品。

