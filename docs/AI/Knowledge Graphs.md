
### Knowledge Graphs
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-03-19**|**Evaluating LLM-Generated Lessons from the Language Learning Students' Perspective: A Short Case Study on Duolingo**|Carlos Rafael Catalan et.al.|[2603.18873v1](http://arxiv.org/abs/2603.18873v1)|null|
|**2026-03-19**|**Bridging Network Fragmentation: A Semantic-Augmented DRL Framework for UAV-aided VANETs**|Gaoxiang Cao et.al.|[2603.18871v1](http://arxiv.org/abs/2603.18871v1)|null|
|**2026-03-19**|**RewardFlow: Topology-Aware Reward Propagation on State Graphs for Agentic RL with Large Language Models**|Xiao Feng et.al.|[2603.18859v1](http://arxiv.org/abs/2603.18859v1)|null|
|**2026-03-19**|**Functional Subspace Watermarking for Large Language Models**|Zikang Ding et.al.|[2603.18793v1](http://arxiv.org/abs/2603.18793v1)|null|
|**2026-03-19**|**Memento-Skills: Let Agents Design Agents**|Huichi Zhou et.al.|[2603.18743v1](http://arxiv.org/abs/2603.18743v1)|null|
|**2026-03-19**|**Ontology-Guided Diffusion for Zero-Shot Visual Sim2Real Transfer**|Mohamed Youssef et.al.|[2603.18719v1](http://arxiv.org/abs/2603.18719v1)|null|
|**2026-03-19**|**STEP: Scientific Time-Series Encoder Pretraining via Cross-Domain Distillation**|Chen Zhang et.al.|[2603.18688v1](http://arxiv.org/abs/2603.18688v1)|null|
|**2026-03-19**|**MANAR: Memory-augmented Attention with Navigational Abstract Conceptual Representation**|Zuher Jahshan et.al.|[2603.18676v1](http://arxiv.org/abs/2603.18676v1)|null|
|**2026-03-19**|**Thinking with Constructions: A Benchmark and Policy Optimization for Visual-Text Interleaved Geometric Reasoning**|Haokun Zhao et.al.|[2603.18662v1](http://arxiv.org/abs/2603.18662v1)|null|
|**2026-03-19**|**Benchmarking PDF Parsers on Table Extraction with LLM-based Semantic Evaluation**|Pius Horn et.al.|[2603.18652v1](http://arxiv.org/abs/2603.18652v1)|null|
|**2026-03-19**|**A Comparative Empirical Study of Catastrophic Forgetting Mitigation in Sequential Task Adaptation for Continual Natural Language Processing Systems**|Aram Abrahamyan et.al.|[2603.18641v1](http://arxiv.org/abs/2603.18641v1)|null|
|**2026-03-19**|**ZEBRAARENA: A Diagnostic Simulation Environment for Studying Reasoning-Action Coupling in Tool-Augmented LLMs**|Wanjia Zhao et.al.|[2603.18614v1](http://arxiv.org/abs/2603.18614v1)|null|
|**2026-03-19**|**Interplay: Training Independent Simulators for Reference-Free Conversational Recommendation**|Jerome Ramos et.al.|[2603.18573v1](http://arxiv.org/abs/2603.18573v1)|null|
|**2026-03-19**|**Reasonably reasoning AI agents can avoid game-theoretic failures in zero-shot, provably**|Enoch Hyunwook Kang et.al.|[2603.18563v1](http://arxiv.org/abs/2603.18563v1)|null|
|**2026-03-19**|**Expert Personas Improve LLM Alignment but Damage Accuracy: Bootstrapping Intent-Based Persona Routing with PRISM**|Zizhao Hu et.al.|[2603.18507v1](http://arxiv.org/abs/2603.18507v1)|null|
|**2026-03-19**|**WASD: Locating Critical Neurons as Sufficient Conditions for Explaining and Controlling LLM Behavior**|Haonan Yu et.al.|[2603.18474v1](http://arxiv.org/abs/2603.18474v1)|null|
|**2026-03-19**|**AlignMamba-2: Enhancing Multimodal Fusion and Sentiment Analysis with Modality-Aware Mamba**|Yan Li et.al.|[2603.18462v1](http://arxiv.org/abs/2603.18462v1)|null|
|**2026-03-19**|**HypeMed: Enhancing Medication Recommendations with Hypergraph-Based Patient Relationships**|Xiangxu Zhang et.al.|[2603.18459v1](http://arxiv.org/abs/2603.18459v1)|null|
|**2026-03-19**|**The Impact of Corporate AI Washing on Farmers' Digital Financial Behavior Response -- An Analysis from the Perspective of Digital Financial Exclusion**|Li Wenxiu et.al.|[2603.18421v1](http://arxiv.org/abs/2603.18421v1)|null|
|**2026-03-19**|**From Weak Cues to Real Identities: Evaluating Inference-Driven De-Anonymization in LLM Agents**|Myeongseob Ko et.al.|[2603.18382v1](http://arxiv.org/abs/2603.18382v1)|null|
|**2026-03-19**|**PlanTwin: Privacy-Preserving Planning Abstractions for Cloud-Assisted LLM Agents**|Guangsheng Yu et.al.|[2603.18377v1](http://arxiv.org/abs/2603.18377v1)|null|
|**2026-03-18**|**Interpretability without actionability: mechanistic methods cannot correct language model errors despite near-perfect internal representations**|Sanjay Basu et.al.|[2603.18353v1](http://arxiv.org/abs/2603.18353v1)|null|
|**2026-03-18**|**Approximate Subgraph Matching with Neural Graph Representations and Reinforcement Learning**|Kaiyang Li et.al.|[2603.18314v1](http://arxiv.org/abs/2603.18314v1)|null|
|**2026-03-18**|**Detection Is Cheap, Routing Is Learned: Why Refusal-Based Alignment Evaluation Fails**|Gregory N. Frank et.al.|[2603.18280v1](http://arxiv.org/abs/2603.18280v1)|null|
|**2026-03-18**|**MolRGen: A Training and Evaluation Setting for De Novo Molecular Generation with Reasonning Models**|Philippe Formont et.al.|[2603.18256v1](http://arxiv.org/abs/2603.18256v1)|null|
|**2026-03-18**|**How Psychological Learning Paradigms Shaped and Constrained Artificial Intelligence**|Alex Anvi Eponon et.al.|[2603.18203v1](http://arxiv.org/abs/2603.18203v1)|null|
|**2026-03-18**|**Modeling the human lexicon under temperature variations: linguistic factors, diversity and typicality in LLM word associations**|Maria Andueza Rodriguez et.al.|[2603.18171v1](http://arxiv.org/abs/2603.18171v1)|null|
|**2026-03-18**|**TDAD: Test-Driven Agentic Development - Reducing Code Regressions in AI Coding Agents via Graph-Based Impact Analysis**|Pepe Alonso et.al.|[2603.17973v1](http://arxiv.org/abs/2603.17973v1)|null|
|**2026-03-18**|**Don't Vibe Code, Do Skele-Code: Interactive No-Code Notebooks for Subject Matter Experts to Build Lower-Cost Agentic Workflows**|Sriram Gopalakrishnan et.al.|[2603.18122v1](http://arxiv.org/abs/2603.18122v1)|null|
|**2026-03-18**|**CodeScout: An Effective Recipe for Reinforcement Learning of Code Search Agents**|Lintang Sutawika et.al.|[2603.17829v1](http://arxiv.org/abs/2603.17829v1)|null|
|**2026-03-18**|**FailureMem: A Failure-Aware Multimodal Framework for Autonomous Software Repair**|Ruize Ma et.al.|[2603.17826v1](http://arxiv.org/abs/2603.17826v1)|null|
|**2026-03-18**|**Intellectual Stewardship: Re-adapting Human Minds for Creative Knowledge Work in the Age of AI**|Jianwei Zhang et.al.|[2603.18117v1](http://arxiv.org/abs/2603.18117v1)|null|
|**2026-03-18**|**Facts as First Class Objects: Knowledge Objects for Persistent LLM Memory**|Oliver Zahn et.al.|[2603.17781v1](http://arxiv.org/abs/2603.17781v1)|null|
|**2026-03-18**|**Modeling Overlapped Speech with Shuffles**|Matthew Wiesner et.al.|[2603.17769v1](http://arxiv.org/abs/2603.17769v1)|null|
|**2026-03-18**|**Adaptive Guidance for Retrieval-Augmented Masked Diffusion Models**|Jaemin Kim et.al.|[2603.17677v1](http://arxiv.org/abs/2603.17677v1)|null|
|**2026-03-18**|**Training-Only Heterogeneous Image-Patch-Text Graph Supervision for Advancing Few-Shot Learning Adapters**|Mohammed Rahman Sherif Khan Mohammad et.al.|[2603.18101v1](http://arxiv.org/abs/2603.18101v1)|null|
|**2026-03-18**|**From Isolated Scoring to Collaborative Ranking: A Comparison-Native Framework for LLM-Based Paper Evaluation**|Pujun Zheng et.al.|[2603.17588v1](http://arxiv.org/abs/2603.17588v1)|null|
|**2026-03-18**|**KA2L: A Knowledge-Aware Active Learning Framework for LLMs**|Haoxuan Yin et.al.|[2603.17566v1](http://arxiv.org/abs/2603.17566v1)|null|
|**2026-03-18**|**Zipper-LoRA: Dynamic Parameter Decoupling for Speech-LLM based Multilingual Speech Recognition**|Yuxiang Mei et.al.|[2603.17558v1](http://arxiv.org/abs/2603.17558v1)|null|
|**2026-03-18**|**CLeAN: Continual Learning Adaptive Normalization in Dynamic Environments**|Isabella Marasco et.al.|[2603.17548v1](http://arxiv.org/abs/2603.17548v1)|null|
|**2026-03-18**|**Language on Demand, Knowledge at Core: Composing LLMs with Encoder-Decoder Translation Models for Extensible Multilinguality**|Mengyu Bu et.al.|[2603.17512v1](http://arxiv.org/abs/2603.17512v1)|null|
|**2026-03-18**|**Inducing Epistemological Humility in Large Language Models: A Targeted SFT Approach to Reducing Hallucination**|Cem Uluoglakci et.al.|[2603.17504v1](http://arxiv.org/abs/2603.17504v1)|null|
|**2026-03-18**|**Revisiting Cross-Attention Mechanisms: Leveraging Beneficial Noise for Domain-Adaptive Learning**|Zelin Zang et.al.|[2603.17474v1](http://arxiv.org/abs/2603.17474v1)|null|
|**2026-03-18**|**When Only the Final Text Survives: Implicit Execution Tracing for Multi-Agent Attribution**|Yi Nian et.al.|[2603.17445v2](http://arxiv.org/abs/2603.17445v2)|null|
|**2026-03-18**|**Proactive Knowledge Inquiry in Doctor-Patient Dialogue: Stateful Extraction, Belief Updating, and Path-Aware Action Planning**|Zhenhai Pan et.al.|[2603.17425v1](http://arxiv.org/abs/2603.17425v1)|null|
|**2026-03-18**|**Efficient Exploration at Scale**|Seyed Mohammad Asghari et.al.|[2603.17378v1](http://arxiv.org/abs/2603.17378v1)|null|
|**2026-03-18**|**Continually self-improving AI**|Zitong Yang et.al.|[2603.18073v1](http://arxiv.org/abs/2603.18073v1)|null|
|**2026-03-18**|**PACE-RAG: Patient-Aware Contextual and Evidence-based Policy RAG for Clinical Drug Recommendation**|Chaeyoung Huh et.al.|[2603.17356v1](http://arxiv.org/abs/2603.17356v1)|null|
|**2026-03-18**|**A Progressive Visual-Logic-Aligned Framework for Ride-Hailing Adjudication**|Weiming Wu et.al.|[2603.17328v1](http://arxiv.org/abs/2603.17328v1)|null|
|**2026-03-18**|**S3T-Former: A Purely Spike-Driven State-Space Topology Transformer for Skeleton Action Recognition**|Naichuan Zheng et.al.|[2603.18062v1](http://arxiv.org/abs/2603.18062v1)|null|
|**2026-03-18**|**Graph-Native Cognitive Memory for AI Agents: Formal Belief Revision Semantics for Versioned Memory Architectures**|Young Bin Park et.al.|[2603.17244v1](http://arxiv.org/abs/2603.17244v1)|null|
|**2026-03-18**|**Neuron-Level Emotion Control in Speech-Generative Large Audio-Language Models**|Xiutian Zhao et.al.|[2603.17231v1](http://arxiv.org/abs/2603.17231v1)|null|
|**2026-03-17**|**Abstraction as a Memory-Efficient Inductive Bias for Continual Learning**|Elnaz Rahmati et.al.|[2603.17198v1](http://arxiv.org/abs/2603.17198v1)|null|
|**2026-03-17**|**Tabular LLMs for Interpretable Few-Shot Alzheimer's Disease Prediction with Multimodal Biomedical Data**|Sophie Kearney et.al.|[2603.17191v1](http://arxiv.org/abs/2603.17191v1)|null|
|**2026-03-17**|**Detecting Data Poisoning in Code Generation LLMs via Black-Box, Vulnerability-Oriented Scanning**|Shenao Yan et.al.|[2603.17174v1](http://arxiv.org/abs/2603.17174v1)|null|
|**2026-03-17**|**Generalist Multimodal LLMs Gain Biometric Expertise via Human Salience**|Jacob Piland et.al.|[2603.17173v1](http://arxiv.org/abs/2603.17173v1)|null|
|**2026-03-17**|**Cascade-Aware Multi-Agent Routing: Spatio-Temporal Sidecars and Geometry-Switching**|Davide Di Gioia et.al.|[2603.17112v1](http://arxiv.org/abs/2603.17112v1)|null|
|**2026-03-17**|**Knowledge Localization in Mixture-of-Experts LLMs Using Cross-Lingual Inconsistency**|Lucas Bandarkar et.al.|[2603.17102v1](http://arxiv.org/abs/2603.17102v1)|null|
|**2026-03-17**|**Large Reasoning Models Struggle to Transfer Parametric Knowledge Across Scripts**|Lucas Bandarkar et.al.|[2603.17070v1](http://arxiv.org/abs/2603.17070v1)|null|
|**2026-03-17**|**Transformers are Bayesian Networks**|Gregory Coppola et.al.|[2603.17063v1](http://arxiv.org/abs/2603.17063v1)|null|
|**2026-03-17**|**Early Quantization Shrinks Codebook: A Simple Fix for Diversity-Preserving Tokenization**|Wenhao Zhao et.al.|[2603.17052v1](http://arxiv.org/abs/2603.17052v1)|null|
|**2026-03-17**|**HopChain: Multi-Hop Data Synthesis for Generalizable Vision-Language Reasoning**|Shenzhi Wang et.al.|[2603.17024v2](http://arxiv.org/abs/2603.17024v2)|null|
|**2026-03-17**|**Online Experiential Learning for Language Models**|Tianzhu Ye et.al.|[2603.16856v1](http://arxiv.org/abs/2603.16856v1)|null|
|**2026-03-17**|**Is Conformal Factuality for RAG-based LLMs Robust? Novel Metrics and Systematic Insights**|Yi Chen et.al.|[2603.16817v1](http://arxiv.org/abs/2603.16817v1)|null|
|**2026-03-17**|**CABTO: Context-Aware Behavior Tree Grounding for Robot Manipulation**|Yishuai Cai et.al.|[2603.16809v1](http://arxiv.org/abs/2603.16809v1)|null|
|**2026-03-17**|**Omanic: Towards Step-wise Evaluation of Multi-hop Reasoning in Large Language Models**|Xiaojie Gu et.al.|[2603.16654v1](http://arxiv.org/abs/2603.16654v1)|null|
|**2026-03-17**|**When AI Navigates the Fog of War**|Ming Li et.al.|[2603.16642v1](http://arxiv.org/abs/2603.16642v1)|null|
|**2026-03-17**|**Domain Mixture Design via Log-Likelihood Differences for Aligning Language Models with a Target Model**|Ryo Kishino et.al.|[2603.16622v1](http://arxiv.org/abs/2603.16622v1)|null|
|**2026-03-17**|**Tarab: A Multi-Dialect Corpus of Arabic Lyrics and Poetry**|Mo El-Haj et.al.|[2603.16601v1](http://arxiv.org/abs/2603.16601v1)|null|
|**2026-03-17**|**Data-driven generalized perimeter control: Zürich case study**|Alessio Rimoldi et.al.|[2603.16599v1](http://arxiv.org/abs/2603.16599v1)|null|
|**2026-03-17**|**V-DyKnow: A Dynamic Benchmark for Time-Sensitive Knowledge in Vision Language Models**|Seyed Mahed Mousavi et.al.|[2603.16581v1](http://arxiv.org/abs/2603.16581v1)|null|
|**2026-03-17**|**Characterizing Delusional Spirals through Human-LLM Chat Logs**|Jared Moore et.al.|[2603.16567v1](http://arxiv.org/abs/2603.16567v1)|null|
|**2026-03-17**|**EmoLLM: Appraisal-Grounded Cognitive-Emotional Co-Reasoning in Large Language Models**|Yifei Zhang et.al.|[2603.16553v1](http://arxiv.org/abs/2603.16553v1)|null|
|**2026-03-17**|**DanceHA: A Multi-Agent Framework for Document-Level Aspect-Based Sentiment Analysis**|Lei Wang et.al.|[2603.16546v1](http://arxiv.org/abs/2603.16546v1)|null|
|**2026-03-17**|**How often do Answers Change? Estimating Recency Requirements in Question Answering**|Bhawna Piryani et.al.|[2603.16544v1](http://arxiv.org/abs/2603.16544v1)|null|
|**2026-03-17**|**AdaMem: Adaptive User-Centric Memory for Long-Horizon Dialogue Agents**|Shannan Yan et.al.|[2603.16496v1](http://arxiv.org/abs/2603.16496v1)|null|
|**2026-03-17**|**ExpressMind: A Multimodal Pretrained Large Language Model for Expressway Operation**|Zihe Wang et.al.|[2603.16495v1](http://arxiv.org/abs/2603.16495v1)|null|
|**2026-03-17**|**Are a Thousand Words Better Than a Single Picture? Beyond Images -- A Framework for Multi-Modal Knowledge Graph Dataset Enrichment**|Pengyu Zhang et.al.|[2603.16974v1](http://arxiv.org/abs/2603.16974v1)|null|
|**2026-03-17**|**CD-FKD: Cross-Domain Feature Knowledge Distillation for Robust Single-Domain Generalization in Object Detection**|Junseok Lee et.al.|[2603.16439v1](http://arxiv.org/abs/2603.16439v1)|null|
|**2026-03-17**|**Via Negativa for AI Alignment: Why Negative Constraints Are Structurally Superior to Positive Preferences**|Quan Cheng et.al.|[2603.16417v1](http://arxiv.org/abs/2603.16417v1)|null|
|**2026-03-17**|**Fanar 2.0: Arabic Generative AI Stack**|FANAR TEAM et.al.|[2603.16397v1](http://arxiv.org/abs/2603.16397v1)|null|
|**2026-03-17**|**FactorEngine: A Program-level Knowledge-Infused Factor Mining Framework for Quantitative Investment**|Qinhong Lin et.al.|[2603.16365v1](http://arxiv.org/abs/2603.16365v1)|null|
|**2026-03-17**|**Toward Experimentation-as-a-Service in 5G/6G: The Plaza6G Prototype for AI-Assisted Trials**|Sergio Barrachina-Muñoz et.al.|[2603.16356v1](http://arxiv.org/abs/2603.16356v1)|null|
|**2026-03-17**|**A Human-Centred Architecture for Large Language Models-Cognitive Assistants in Manufacturing within Quality Management Systems**|Marcos Galdino et.al.|[2603.16325v1](http://arxiv.org/abs/2603.16325v1)|null|
|**2026-03-17**|**DeepStage: Learning Autonomous Defense Policies Against Multi-Stage APT Campaigns**|Trung V. Phan et.al.|[2603.16969v1](http://arxiv.org/abs/2603.16969v1)|null|
|**2026-03-17**|**Attention-guided Evidence Grounding for Spoken Question Answering**|Ke Yang et.al.|[2603.16292v2](http://arxiv.org/abs/2603.16292v2)|null|
|**2026-03-17**|**MSRAMIE: Multimodal Structured Reasoning Agent for Multi-instruction Image Editing**|Zhaoyuan Qiu et.al.|[2603.16967v1](http://arxiv.org/abs/2603.16967v1)|null|
|**2026-03-17**|**SpecSteer: Synergizing Local Context and Global Reasoning for Efficient Personalized Generation**|Hang Lv et.al.|[2603.16219v1](http://arxiv.org/abs/2603.16219v1)|null|
|**2026-03-17**|**Are Large Language Models Truly Smarter Than Humans?**|Eshwar Reddy M et.al.|[2603.16197v1](http://arxiv.org/abs/2603.16197v1)|null|
|**2026-03-17**|**360° Image Perception with MLLMs: A Comprehensive Benchmark and a Training-Free Method**|Huyen T. T. Tran et.al.|[2603.16179v1](http://arxiv.org/abs/2603.16179v1)|null|
|**2026-03-17**|**STARK: Spatio-Temporal Attention for Representation of Keypoints for Continuous Sign Language Recognition**|Suvajit Patra et.al.|[2603.16163v1](http://arxiv.org/abs/2603.16163v1)|null|
|**2026-03-17**|**SIA: A Synthesize-Inject-Align Framework for Knowledge-Grounded and Secure E-commerce Search LLMs with Industrial Deployment**|Zhouwei Zhai et.al.|[2603.16137v2](http://arxiv.org/abs/2603.16137v2)|null|
|**2026-03-17**|**SWE-QA-Pro: A Representative Benchmark and Scalable Training Recipe for Repository-Level Code Understanding**|Songcheng Cai et.al.|[2603.16124v1](http://arxiv.org/abs/2603.16124v1)|null|
|**2026-03-17**|**PathGLS: Evaluating Pathology Vision-Language Models without Ground Truth through Multi-Dimensional Consistency**|Minbing Chen et.al.|[2603.16113v1](http://arxiv.org/abs/2603.16113v1)|null|
|**2026-03-17**|**VIGIL: Towards Edge-Extended Agentic AI for Enterprise IT Support**|Sarthak Ahuja et.al.|[2603.16110v1](http://arxiv.org/abs/2603.16110v1)|null|
|**2026-03-17**|**NANOZK: Layerwise Zero-Knowledge Proofs for Verifiable Large Language Model Inference**|Zhaohui Geoffrey Wang et.al.|[2603.18046v1](http://arxiv.org/abs/2603.18046v1)|null|
|**2026-03-17**|**Machine intelligence supports the full chain of 2D dendrite synthesis**|Wenqiang Huang et.al.|[2603.16959v1](http://arxiv.org/abs/2603.16959v1)|null|
|**2026-03-17**|**CounterRefine: Answer-Conditioned Counterevidence Retrieval for Inference-Time Knowledge Repair in Factual Question Answering**|Tianyi Huang et.al.|[2603.16091v1](http://arxiv.org/abs/2603.16091v1)|null|
|**2026-03-17**|**PhysQuantAgent: An Inference Pipeline of Mass Estimation for Vision-Language Models**|Hisayuki Yokomizo et.al.|[2603.16958v1](http://arxiv.org/abs/2603.16958v1)|null|
|**2026-03-16**|**RadAnnotate: Large Language Models for Efficient and Reliable Radiology Report Annotation**|Saisha Pradeep Shetty et.al.|[2603.16002v1](http://arxiv.org/abs/2603.16002v1)|null|

#### Abstracts
##### **Evaluating LLM-Generated Lessons from the Language Learning Students' Perspective: A Short Case Study on Duolingo**
2603.18873v1 by Carlos Rafael Catalan, Patricia Nicole Monderin, Lheane Marie Dizon, Gap Estrella, Raymund John Sarmimento, Marie Antoinette Patalagsa

Popular language learning applications such as Duolingo use large language models (LLMs) to generate lessons for its users. Most lessons focus on general real-world scenarios such as greetings, ordering food, or asking directions, with limited support for profession-specific contexts. This gap can hinder learners from achieving professional-level fluency, which we define as the ability to communicate comfortably various work-related and domain-specific information in the target language. We surveyed five employees from a multinational company in the Philippines on their experiences with Duolingo. Results show that respondents encountered general scenarios more frequently than work-related ones, and that the former are relatable and effective in building foundational grammar, vocabulary, and cultural knowledge. The latter helps bridge the gap toward professional fluency as it contains domain-specific vocabulary. Each participant suggested lesson scenarios that diverge in contexts hen analyzed in aggregate. With this understanding, we propose that language learning applications should generate lessons that adapt to an individual's needs through personalized, domain specific lesson scenarios while maintaining foundational support through general, relatable lesson scenarios.

摘要：流行的語言學習應用程式如 Duolingo 使用大型語言模型 (LLMs) 為用戶生成課程。大多數課程專注於一般的現實場景，例如問候、點餐或詢問方向，對於專業特定的情境支持有限。這一差距可能會妨礙學習者達到專業級流利度，我們將其定義為能夠在目標語言中自如地交流各種與工作相關和領域特定的信息。我們對來自菲律賓一家跨國公司的五名員工進行了調查，了解他們使用 Duolingo 的經驗。結果顯示，受訪者遇到一般場景的頻率高於與工作相關的場景，而前者在建立基礎語法、詞彙和文化知識方面是可關聯且有效的。後者則有助於縮小通往專業流利度的差距，因為它包含領域特定的詞彙。每位參與者建議的課程場景在整體分析中呈現出不同的情境。基於此理解，我們建議語言學習應用程式應生成適應個人需求的課程，通過個性化的領域特定課程場景，同時保持通過一般可關聯課程場景提供基礎支持。

##### **Bridging Network Fragmentation: A Semantic-Augmented DRL Framework for UAV-aided VANETs**
2603.18871v1 by Gaoxiang Cao, Wenke Yuan, Huasen He, Yunpeng Hou, Xiaofeng Jiang, Shuangwu Chen, Jian Yang

Vehicular Ad-hoc Networks (VANETs) are the digital cornerstone of autonomous driving, yet they suffer from severe network fragmentation in urban environments due to physical obstructions. Unmanned Aerial Vehicles (UAVs), with their high mobility, have emerged as a vital solution to bridge these connectivity gaps. However, traditional Deep Reinforcement Learning (DRL)-based UAV deployment strategies lack semantic understanding of road topology, often resulting in blind exploration and sample inefficiency. By contrast, Large Language Models (LLMs) possess powerful reasoning capabilities capable of identifying topological importance, though applying them to control tasks remains challenging. To address this, we propose the Semantic-Augmented DRL (SA-DRL) framework. Firstly, we propose a fragmentation quantification method based on Road Topology Graphs (RTG) and Dual Connected Graphs (DCG). Subsequently, we design a four-stage pipeline to transform a general-purpose LLM into a domain-specific topology expert. Finally, we propose the Semantic-Augmented PPO (SA-PPO) algorithm, which employs a Logit Fusion mechanism to inject the LLM's semantic reasoning directly into the policy as a prior, effectively guiding the agent toward critical intersections. Extensive high-fidelity simulations demonstrate that SA-PPO achieves state-of-the-art performance with remarkable efficiency, reaching baseline performance levels using only 26.6% of the training episodes. Ultimately, SA-PPO improves two key connectivity metrics by 13.2% and 23.5% over competing methods, while reducing energy consumption to just 28.2% of the baseline.

摘要：車輛自組網絡（VANETs）是自動駕駛的數位基石，但由於物理障礙，它們在城市環境中遭受嚴重的網絡碎片化。無人機（UAVs）因其高機動性，已成為彌補這些連接空白的重要解決方案。然而，傳統的基於深度增強學習（DRL）的無人機部署策略缺乏對道路拓撲的語義理解，經常導致盲目探索和樣本效率低下。相比之下，大型語言模型（LLMs）擁有強大的推理能力，能夠識別拓撲的重要性，儘管將其應用於控制任務仍然具有挑戰性。為了解決這個問題，我們提出了語義增強的深度增強學習（SA-DRL）框架。首先，我們提出了一種基於道路拓撲圖（RTG）和雙連通圖（DCG）的碎片化量化方法。隨後，我們設計了一個四階段的流程，將通用大型語言模型轉變為特定領域的拓撲專家。最後，我們提出了語義增強的PPO（SA-PPO）算法，該算法利用Logit融合機制將LLM的語義推理直接注入策略作為先驗，有效地引導代理朝向關鍵交叉口。廣泛的高保真模擬顯示，SA-PPO以卓越的效率達到最先進的性能，僅使用26.6%的訓練回合便達到基準性能水平。最終，SA-PPO在兩個關鍵連接指標上比競爭方法提高了13.2%和23.5%，同時將能耗降低至基準的28.2%。

##### **RewardFlow: Topology-Aware Reward Propagation on State Graphs for Agentic RL with Large Language Models**
2603.18859v1 by Xiao Feng, Bo Han, Zhanke Zhou, Jiaqi Fan, Jiangchao Yao, Ka Ho Li, Dahai Yu, Michael Kwok-Po Ng

Reinforcement learning (RL) holds significant promise for enhancing the agentic reasoning capabilities of large language models (LLMs) with external environments. However, the inherent sparsity of terminal rewards hinders fine-grained, state-level optimization. Although process reward modeling offers a promising alternative, training dedicated reward models often entails substantial computational costs and scaling difficulties. To address these challenges, we introduce RewardFlow, a lightweight method for estimating state-level rewards tailored to agentic reasoning tasks. RewardFlow leverages the intrinsic topological structure of states within reasoning trajectories by constructing state graphs. This enables an analysis of state-wise contributions to success, followed by topology-aware graph propagation to quantify contributions and yield objective, state-level rewards. When integrated as dense rewards for RL optimization, RewardFlow substantially outperforms prior RL baselines across four agentic reasoning benchmarks, demonstrating superior performance, robustness, and training efficiency. The implementation of RewardFlow is publicly available at https://github.com/tmlr-group/RewardFlow.

摘要：強化學習（RL）對於提升大型語言模型（LLMs）在外部環境中的代理推理能力具有重要的潛力。<lb/>然而，終端獎勵的內在稀疏性妨礙了細緻的狀態級優化。<lb/>儘管過程獎勵建模提供了一種有前景的替代方案，但訓練專用的獎勵模型往往需要大量的計算成本和擴展困難。<lb/>為了解決這些挑戰，我們提出了RewardFlow，一種輕量級的方法，用於估計針對代理推理任務的狀態級獎勵。<lb/>RewardFlow通過構建狀態圖，利用推理軌跡中狀態的內在拓撲結構。<lb/>這使得對成功的狀態貢獻進行分析成為可能，隨後進行拓撲感知的圖形傳播，以量化貢獻並產生客觀的狀態級獎勵。<lb/>當作為RL優化的密集獎勵整合時，RewardFlow在四個代理推理基準上顯著超越了先前的RL基準，顯示出卓越的性能、穩健性和訓練效率。<lb/>RewardFlow的實現已在https://github.com/tmlr-group/RewardFlow公開可用。

##### **Functional Subspace Watermarking for Large Language Models**
2603.18793v1 by Zikang Ding, Junhao Li, Suling Wu, Junchi Yao, Hongbo Liu, Lijie Hu

Model watermarking utilizes internal representations to protect the ownership of large language models (LLMs). However, these features inevitably undergo complex distortions during realistic model modifications such as fine-tuning, quantization, or knowledge distillation, making reliable extraction extremely challenging. Despite extensive research on model-side watermarking, existing methods still lack sufficient robustness against parameter-level perturbations. To address this gap, we propose \texttt{\textbf{Functional Subspace Watermarking (FSW)}}, a framework that anchors ownership signals into a low-dimensional functional backbone. Specifically, we first solve a generalized eigenvalue problem to extract a stable functional subspace for watermark injection, while introducing an adaptive spectral truncation strategy to achieve an optimal balance between robustness and model utility. Furthermore, a vector consistency constraint is incorporated to ensure that watermark injection does not compromise the original semantic performance. Extensive experiments across various LLM architectures and datasets demonstrate that our method achieves superior detection accuracy and statistical verifiability under multiple model attacks, maintaining robustness that outperforms existing state-of-the-art (SOTA) methods.

摘要：模型水印技術利用內部表示來保護大型語言模型（LLMs）的所有權。<lb/>然而，這些特徵在現實模型修改過程中不可避免地會經歷複雜的扭曲，例如微調、量化或知識蒸餾，使得可靠的提取變得極具挑戰性。<lb/>儘管在模型端水印方面進行了廣泛的研究，但現有方法仍然缺乏對參數級擾動的足夠魯棒性。<lb/>為了解決這一問題，我們提出了\texttt{\textbf{功能子空間水印（FSW）}}，這是一個將所有權信號固定在低維功能骨幹中的框架。<lb/>具體而言，我們首先解決一個廣義特徵值問題，以提取穩定的功能子空間進行水印注入，同時引入自適應光譜截斷策略，以實現魯棒性和模型效用之間的最佳平衡。<lb/>此外，還引入了一個向量一致性約束，以確保水印注入不會妨礙原始語義性能。<lb/>在各種LLM架構和數據集上進行的廣泛實驗表明，我們的方法在多種模型攻擊下實現了卓越的檢測準確性和統計可驗證性，保持了超越現有最先進（SOTA）方法的魯棒性。

##### **Memento-Skills: Let Agents Design Agents**
2603.18743v1 by Huichi Zhou, Siyuan Guo, Anjie Liu, Zhongwei Yu, Ziqin Gong, Bowen Zhao, Zhixun Chen, Menglong Zhang, Yihang Chen, Jinsong Li, Runyu Yang, Qiangbin Liu, Xinlei Yu, Jianmin Zhou, Na Wang, Chunyang Sun, Jun Wang

We introduce \emph{Memento-Skills}, a generalist, continually-learnable LLM agent system that functions as an \emph{agent-designing agent}: it autonomously constructs, adapts, and improves task-specific agents through experience. The system is built on a memory-based reinforcement learning framework with \emph{stateful prompts}, where reusable skills (stored as structured markdown files) serve as persistent, evolving memory. These skills encode both behaviour and context, enabling the agent to carry forward knowledge across interactions.
  Starting from simple elementary skills (like Web search and terminal operations), the agent continually improves via the \emph{Read--Write Reflective Learning} mechanism introduced in \emph{Memento~2}~\cite{wang2025memento2}. In the \emph{read} phase, a behaviour-trainable skill router selects the most relevant skill conditioned on the current stateful prompt; in the \emph{write} phase, the agent updates and expands its skill library based on new experience. This closed-loop design enables \emph{continual learning without updating LLM parameters}, as all adaptation is realised through the evolution of externalised skills and prompts.
  Unlike prior approaches that rely on human-designed agents, Memento-Skills enables a generalist agent to \emph{design agents end-to-end} for new tasks. Through iterative skill generation and refinement, the system progressively improves its own capabilities. Experiments on the \emph{General AI Assistants} benchmark and \emph{Humanity's Last Exam} demonstrate sustained gains, achieving 26.2\% and 116.2\% relative improvements in overall accuracy, respectively. Code is available at https://github.com/Memento-Teams/Memento-Skills.

摘要：我們介紹 \emph{Memento-Skills}，這是一個通用的、可持續學習的 LLM 代理系統，作為 \emph{代理設計代理}：它能夠通過經驗自主構建、調整和改進特定任務的代理。該系統建立在基於記憶的強化學習框架上，使用 \emph{有狀態提示}，可重用的技能（以結構化的 markdown 文件存儲）作為持久的、演變的記憶。這些技能編碼了行為和上下文，使代理能夠在交互中持續傳遞知識。<lb/>從簡單的基本技能（如網頁搜索和終端操作）開始，代理通過在 \emph{Memento~2}~\cite{wang2025memento2} 中引入的 \emph{讀--寫反思學習} 機制不斷改進。在 \emph{讀} 階段，一個可訓練行為的技能路由器根據當前的有狀態提示選擇最相關的技能；在 \emph{寫} 階段，代理根據新的經驗更新和擴展其技能庫。這種閉環設計使得 \emph{持續學習無需更新 LLM 參數}，因為所有的適應都是通過外部化技能和提示的演變實現的。<lb/>與依賴人類設計代理的先前方法不同，Memento-Skills 使通用代理能夠 \emph{端到端設計新任務的代理}。通過迭代的技能生成和精煉，該系統逐步提升自身的能力。在 \emph{通用 AI 助手} 基準和 \emph{人類的最後考試} 上的實驗顯示出持續的增長，分別實現了 26.2\% 和 116.2\% 的整體準確率相對提升。代碼可在 https://github.com/Memento-Teams/Memento-Skills 獲得。

##### **Ontology-Guided Diffusion for Zero-Shot Visual Sim2Real Transfer**
2603.18719v1 by Mohamed Youssef, Mayar Elfares, Anna-Maria Meer, Matteo Bortoletto, Andreas Bulling

Bridging the simulation-to-reality (sim2real) gap remains challenging as labelled real-world data is scarce. Existing diffusion-based approaches rely on unstructured prompts or statistical alignment, which do not capture the structured factors that make images look real. We introduce Ontology- Guided Diffusion (OGD), a neuro-symbolic zero-shot sim2real image translation framework that represents realism as structured knowledge. OGD decomposes realism into an ontology of interpretable traits -- such as lighting and material properties -- and encodes their relationships in a knowledge graph. From a synthetic image, OGD infers trait activations and uses a graph neural network to produce a global embedding. In parallel, a symbolic planner uses the ontology traits to compute a consistent sequence of visual edits needed to narrow the realism gap. The graph embedding conditions a pretrained instruction-guided diffusion model via cross-attention, while the planned edits are converted into a structured instruction prompt. Across benchmarks, our graph-based embeddings better distinguish real from synthetic imagery than baselines, and OGD outperforms state-of-the-art diffusion methods in sim2real image translations. Overall, OGD shows that explicitly encoding realism structure enables interpretable, data-efficient, and generalisable zero-shot sim2real transfer.

摘要：彌合模擬與現實之間的差距仍然具有挑戰性，因為標註的現實世界數據稀缺。現有的基於擴散的方法依賴於非結構化的提示或統計對齊，這些方法無法捕捉使圖像看起來真實的結構性因素。我們介紹了本體引導擴散（Ontology-Guided Diffusion，OGD），這是一種神經符號零樣本模擬到現實的圖像翻譯框架，將真實性表示為結構化知識。OGD將真實性分解為可解釋特徵的本體——例如光照和材料屬性——並在知識圖中編碼它們的關係。從合成圖像中，OGD推斷特徵激活並使用圖神經網絡生成全局嵌入。與此同時，符號規劃器使用本體特徵計算所需的一致視覺編輯序列，以縮小真實性差距。圖嵌入通過交叉注意力對預訓練的指令引導擴散模型進行條件處理，而計劃的編輯則轉換為結構化的指令提示。在各項基準測試中，我們的基於圖的嵌入比基線更好地區分真實圖像和合成圖像，OGD在模擬到現實的圖像翻譯中超越了最先進的擴散方法。總體而言，OGD顯示出明確編碼真實性結構能夠實現可解釋的、數據高效的和可泛化的零樣本模擬到現實的轉移。

##### **STEP: Scientific Time-Series Encoder Pretraining via Cross-Domain Distillation**
2603.18688v1 by Chen Zhang, Liwei Liu, Jun Tao, Xiaoyu Yang, Xuenan Xu, Kai Chen, Bowen Zhou, Wen Wu, Chao Zhang

Scientific time series are central to scientific AI but are typically sparse, highly heterogeneous, and limited in scale, making unified representation learning particularly challenging. Meanwhile, foundation models pretrained on relevant time series domains such as audio, general time series, and brain signals contain rich knowledge, but their applicability to scientific signals remains underexplored. In this paper, we investigate the transferability and complementarity of foundation models from relevant time series domains, and study how to effectively leverage them to build a unified encoder for scientific time series. We first systematically evaluate relevant foundation models, showing the effectiveness of knowledge transfer to scientific tasks and their complementary strengths. Based on this observation, we propose STEP, a Scientific Time Series Encoder Pretraining framework via cross domain distillation. STEP introduces adaptive patching to handle extreme-length sequences and a statistics compensation scheme to accommodate diverse numerical scales. It further leverages cross-domain distillation to integrate knowledge from multiple foundation models into a unified encoder. By combining complementary representations across different domains, STEP learns general-purpose and transferable features tailored for scientific signals. Experiments on seven scientific time series tasks demonstrate that STEP provides both an effective structure and an effective pretraining paradigm, taking a STEP toward scientific time series representation learning.

摘要：科學時間序列是科學人工智慧的核心，但通常稀疏、高度異質且規模有限，使得統一表示學習特別具挑戰性。與此同時，預訓練於相關時間序列領域（如音頻、一般時間序列和腦信號）的基礎模型包含豐富的知識，但其在科學信號中的適用性仍然未被充分探索。本文中，我們調查了來自相關時間序列領域的基礎模型的可轉移性和互補性，並研究如何有效利用它們來構建一個統一的科學時間序列編碼器。我們首先系統性地評估了相關的基礎模型，顯示知識轉移到科學任務的有效性及其互補優勢。基於這一觀察，我們提出了STEP，一種通過跨領域蒸餾的科學時間序列編碼器預訓練框架。STEP引入了自適應補丁技術以處理極長序列，並採用統計補償方案以適應多樣的數值範圍。它進一步利用跨領域蒸餾將來自多個基礎模型的知識整合到一個統一的編碼器中。通過結合不同領域的互補表示，STEP學習了針對科學信號量身定制的通用和可轉移特徵。在七個科學時間序列任務上的實驗表明，STEP提供了一個有效的結構和有效的預訓練範式，邁出了科學時間序列表示學習的一步。

##### **MANAR: Memory-augmented Attention with Navigational Abstract Conceptual Representation**
2603.18676v1 by Zuher Jahshan, Ben Ben Ishay, Leonid Yavits

MANAR (Memory-augmented Attention with Navigational Abstract Conceptual Representation), contextualization layer generalizes standard multi-head attention (MHA) by instantiating the principles of Global Workspace Theory (GWT). While MHA enables unconstrained all-to-all communication, it lacks the functional bottleneck and global integration mechanisms hypothesized in cognitive models of consciousness. MANAR addresses this by implementing a central workspace through a trainable memory of abstract concepts and an Abstract Conceptual Representation (ACR). The architecture follows a two-stage logic that maps directly to GWT mechanics: (i) an integration phase, where retrieved memory concepts converge to form a collective "mental image" (the ACR) based on input stimuli; and (ii) a broadcasting phase, where this global state navigates and informs the contextualization of individual local tokens. We demonstrate that efficient linear-time scaling is a fundamental architectural byproduct of instantiating GWT functional bottleneck, as routing global information through a constant-sized ACR resolves the quadratic complexity inherent in standard attention. MANAR is a compatible re-parameterization of MHA with identical semantic roles for its projections, enabling knowledge transfer from pretrained transformers via weight-copy and thus overcoming the adoption barriers of structurally incompatible linear-time alternatives. MANAR enables non-convex contextualization, synthesizing representations that provably lie outside the convex hull of input tokens - a mathematical reflection of the creative synthesis described in GWT. Empirical evaluations confirm that MANAR matches or exceeds strong baselines across language (GLUE score of 85.1), vision (83.9% ImageNet-1K), and speech (2.7% WER on LibriSpeech), positioning it as an efficient and expressive alternative to quadratic attention.

摘要：MANAR（記憶增強注意力與導航抽象概念表示）上下文化層通過實現全球工作區理論（GWT）的原則，對標準多頭注意力（MHA）進行了概括。<lb/>雖然MHA允許無限制的全對全通信，但它缺乏在意識的認知模型中假設的功能瓶頸和全球整合機制。<lb/>MANAR通過實施一個可訓練的抽象概念記憶和抽象概念表示（ACR）來解決這一問題，從而建立一個中央工作區。<lb/>該架構遵循一種兩階段邏輯，直接映射到GWT機制： (i) 整合階段，在此階段檢索的記憶概念匯聚形成基於輸入刺激的集體“心理圖像”（ACR）；<lb/>(ii) 廣播階段，在此階段，這一全球狀態導航並告知個別本地標記的上下文化。<lb/>我們證明，通過實現GWT功能瓶頸，效率線性時間擴展是一個基本的架構副產品，因為通過固定大小的ACR路由全球信息解決了標準注意力中固有的二次複雜性。<lb/>MANAR是MHA的一種兼容重參數化，具有相同的語義角色，能夠通過權重複製從預訓練的Transformer中轉移知識，從而克服結構上不兼容的線性時間替代方案的採用障礙。<lb/>MANAR使得非凸上下文化成為可能，合成的表示明確位於輸入標記的凸包之外——這是對GWT中所描述的創造性合成的數學反映。<lb/>實證評估證實，MANAR在語言（GLUE得分85.1）、視覺（83.9% ImageNet-1K）和語音（LibriSpeech的2.7% WER）方面匹配或超過強基準，將其定位為一種有效且表達豐富的二次注意力替代方案。

##### **Thinking with Constructions: A Benchmark and Policy Optimization for Visual-Text Interleaved Geometric Reasoning**
2603.18662v1 by Haokun Zhao, Wanshi Xu, Haidong Yuan, Songjun Cao, Long Ma, Yanghua Xiao

Geometric reasoning inherently requires "thinking with constructions" -- the dynamic manipulation of visual aids to bridge the gap between problem conditions and solutions. However, existing Multimodal Large Language Models (MLLMs) are largely confined to passive inference with static diagrams, lacking the strategic knowledge of when and how to construct effective visual aids. To address this, we present a framework for Visual-Text Interleaved Chain-of-Thought. We first introduce GeoAux-Bench, the first benchmark comprising 4,334 geometry problems that aligns textual construction steps with ground-truth visual updates. Our pilot study reveals two critical insights: (1) interleaved visual-textual aids outperform single-modality counterparts, which cannot losslessly capture geometric synergy; and (2) valid constructions act as entropy reducers, strongly correlating with reduced reasoning perplexity. Building on these findings, we propose Action Applicability Policy Optimization (A2PO), a reinforcement learning paradigm for mastering strategic construction. A2PO employs Adaptive Reward Shaping to regulate the timing and quality of visual aids via counterfactual sampling to distinguish necessary from redundant constructions. Experiments demonstrate our approach enables MLLMs to leverage selective auxiliary constructions, yielding a 3.51% gain over strong baselines. Code and data are available on GitHub.

摘要：幾何推理本質上需要「用建構來思考」——動態操作視覺輔助工具，以縮短問題條件與解決方案之間的差距。然後，現有的多模態大型語言模型（MLLMs）在很大程度上僅限於使用靜態圖表進行被動推理，缺乏在何時以及如何建構有效視覺輔助工具的策略知識。為了解決這個問題，我們提出了一個視覺-文本交錯思維鏈的框架。我們首先介紹GeoAux-Bench，這是首個基準，包含4,334個幾何問題，將文本建構步驟與真實視覺更新對齊。我們的初步研究揭示了兩個關鍵見解：（1）交錯的視覺-文本輔助工具表現優於單一模態的對應物，後者無法無損捕捉幾何協同；（2）有效的建構作為熵減少器，與推理困惑度降低有強烈相關性。在這些發現的基礎上，我們提出了行動適用性政策優化（A2PO），這是一種強化學習範式，用於掌握策略建構。A2PO採用自適應獎勵塑造來調節視覺輔助工具的時機和質量，通過反事實抽樣來區分必要的建構與冗餘的建構。實驗表明，我們的方法使MLLMs能夠利用選擇性輔助建構，較強基準提高了3.51%的增益。代碼和數據可在GitHub上獲得。

##### **Benchmarking PDF Parsers on Table Extraction with LLM-based Semantic Evaluation**
2603.18652v1 by Pius Horn, Janis Keuper

Reliably extracting tables from PDFs is essential for large-scale scientific data mining and knowledge base construction, yet existing evaluation approaches rely on rule-based metrics that fail to capture semantic equivalence of table content. We present a benchmarking framework based on synthetically generated PDFs with precise LaTeX ground truth, using tables sourced from arXiv to ensure realistic complexity and diversity. As our central methodological contribution, we apply LLM-as-a-judge for semantic table evaluation, integrated into a matching pipeline that accommodates inconsistencies in parser outputs. Through a human validation study comprising over 1,500 quality judgments on extracted table pairs, we show that LLM-based evaluation achieves substantially higher correlation with human judgment (Pearson r=0.93) compared to Tree Edit Distance-based Similarity (TEDS, r=0.68) and Grid Table Similarity (GriTS, r=0.70). Evaluating 21 contemporary PDF parsers across 100 synthetic documents containing 451 tables reveals significant performance disparities. Our results offer practical guidance for selecting parsers for tabular data extraction and establish a reproducible, scalable evaluation methodology for this critical task.
  Code and data: https://github.com/phorn1/pdf-parse-bench Metric study and human evaluation: https://github.com/phorn1/table-metric-study

摘要：可靠地從PDF中提取表格對於大規模科學數據挖掘和知識庫建設至關重要，然而現有的評估方法依賴於基於規則的指標，未能捕捉表格內容的語義等價性。<lb/>我們提出了一個基於合成生成的PDF的基準框架，這些PDF具有精確的LaTeX真實值，並使用來自arXiv的表格以確保現實的複雜性和多樣性。<lb/>作為我們的核心方法貢獻，我們應用LLM作為評判者進行語義表格評估，並將其整合到一個匹配管道中，以適應解析器輸出中的不一致性。<lb/>通過一項包含超過1,500個對提取表格對的質量判斷的人類驗證研究，我們顯示基於LLM的評估與人類判斷之間的相關性（Pearson r=0.93）顯著高於基於樹編輯距離的相似性（TEDS, r=0.68）和網格表相似性（GriTS, r=0.70）。<lb/>對21個當代PDF解析器在100個包含451個表格的合成文檔中的評估顯示出顯著的性能差異。<lb/>我們的結果為選擇表格數據提取的解析器提供了實用指導，並建立了一種可重複的、可擴展的評估方法論，以應對這一關鍵任務。<lb/>代碼和數據：https://github.com/phorn1/pdf-parse-bench <lb/>指標研究和人類評估：https://github.com/phorn1/table-metric-study

##### **A Comparative Empirical Study of Catastrophic Forgetting Mitigation in Sequential Task Adaptation for Continual Natural Language Processing Systems**
2603.18641v1 by Aram Abrahamyan, Sachin Kumar

Neural language models deployed in real-world applications must continually adapt to new tasks and domains without forgetting previously acquired knowledge. This work presents a comparative empirical study of catastrophic forgetting mitigation in continual intent classification. Using the CLINC150 dataset, we construct a 10-task label-disjoint scenario and evaluate three backbone architectures: a feed-forward Artificial Neural Network (ANN), a Gated Recurrent Unit (GRU), and a Transformer encoder, under a range of continual learning (CL) strategies. We consider one representative method from each major CL family: replay-based Maximally Interfered Retrieval (MIR), regularization-based Learning without Forgetting (LwF), and parameter-isolation via Hard Attention to Task (HAT), both individually and in all pairwise and triple combinations. Performance is assessed with average accuracy, macro F1, and backward transfer, capturing the stability-plasticity trade-off across the task sequence. Our results show that naive sequential fine-tuning suffers from severe forgetting for all architectures and that no single CL method fully prevents it. Replay emerges as a key ingredient: MIR is the most reliable individual strategy, and combinations that include replay (MIR+HAT, MIR+LwF, MIR+LwF+HAT) consistently achieve high final performance with near-zero or mildly positive backward transfer. The optimal configuration is architecture-dependent. MIR+HAT yields the best result for ANN and Transformer, MIR+LwF+HAT, on the other hand, works the best for GRU, and in several cases CL methods even surpass joint training, indicating a regularization effect. These findings highlight the importance of jointly selecting backbone architecture and CL mechanism when designing continual intent-classification systems.

摘要：神經語言模型在實際應用中必須不斷適應新任務和領域，而不會忘記先前獲得的知識。這項工作呈現了一項有關持續意圖分類中災難性遺忘緩解的比較實證研究。使用CLINC150數據集，我們構建了一個10任務標籤不重疊的場景，並在一系列持續學習（CL）策略下評估三種主幹架構：前饋人工神經網絡（ANN）、門控循環單元（GRU）和Transformer編碼器。我們考慮了每個主要CL家族中的一種代表性方法：基於重播的最大干擾檢索（MIR）、基於正則化的無遺忘學習（LwF）以及通過硬注意力對任務進行參數隔離（HAT），這些方法分別以及所有成對和三重組合進行評估。性能評估使用平均準確率、宏F1和向後轉移，捕捉任務序列中的穩定性-可塑性權衡。我們的結果顯示，天真的序列微調對所有架構都會遭受嚴重的遺忘，且沒有單一的CL方法能完全防止這種情況。重播成為一個關鍵成分：MIR是最可靠的單一策略，而包含重播的組合（MIR+HAT、MIR+LwF、MIR+LwF+HAT）始終能以接近零或輕微正向的向後轉移達到高最終性能。最佳配置依賴於架構。對於ANN和Transformer，MIR+HAT產生了最佳結果；而MIR+LwF+HAT則在GRU上表現最佳，在幾個案例中，CL方法甚至超越了聯合訓練，顯示出正則化效應。這些發現強調了在設計持續意圖分類系統時，聯合選擇主幹架構和CL機制的重要性。

##### **ZEBRAARENA: A Diagnostic Simulation Environment for Studying Reasoning-Action Coupling in Tool-Augmented LLMs**
2603.18614v1 by Wanjia Zhao, Ludwig Schmidt, James Zou, Vidhisha Balachandran, Lingjiao Chen

Tool-augmented large language models (LLMs) must tightly couple multi-step reasoning with external actions, yet existing benchmarks often confound this interplay with complex environment dynamics, memorized knowledge or dataset contamination. In this paper, we introduce ZebraArena, a procedurally generated diagnostic environment for studying reasoning-action coupling in tool-augmented LLMs, with controllable difficulty and a knowledge-minimal design, which limits gains from memorization or dataset contamination. Each task in ZebraArena requires a set of critical information which is available only through targeted tool use, yielding an interpretable interface between external information acquisition and deductive reasoning. This design provides deterministic evaluation via unique solutions, and a theoretical optimal query count for measuring efficient tool use. We show that ZebraArena requires a combination of in-depth reasoning and accurate external tool calling, which remains a challenge as frontier reasoning models such as GPT-5 and Gemini 2.5 Pro only achieves 60% accuracy on the hard instances. We also observe a persistent gaps between theoretical optimality and practical tool usage. For example, GPT-5 uses 70-270% more tool calls than the theoretical optimum. We highlight the key findings in our evaluation, and hope ZebraArena stimulates further research on the interplay between internal reasoning and external action.

摘要：工具增強的大型語言模型（LLMs）必須將多步推理與外部行動緊密結合，然而現有的基準測試常常將這種相互作用與複雜的環境動態、記憶知識或數據集污染混淆。在本文中，我們介紹了ZebraArena，一個程序生成的診斷環境，用於研究工具增強的LLMs中的推理-行動耦合，具有可控的難度和知識最小化的設計，限制了來自記憶或數據集污染的收益。ZebraArena中的每個任務都需要一組關鍵信息，這些信息僅通過針對性的工具使用才能獲得，從而產生外部信息獲取與演繹推理之間的可解釋界面。這種設計通過獨特的解決方案提供確定性的評估，並為測量有效工具使用提供理論上的最佳查詢數量。我們展示了ZebraArena需要深入的推理和準確的外部工具調用的結合，這仍然是一個挑戰，因為前沿推理模型如GPT-5和Gemini 2.5 Pro在困難實例上的準確率僅達到60%。我們還觀察到理論最佳性與實際工具使用之間持續存在差距。例如，GPT-5的工具調用次數比理論最佳值多出70-270%。我們在評估中突出了關鍵發現，並希望ZebraArena能夠激發對內部推理與外部行動之間相互作用的進一步研究。

##### **Interplay: Training Independent Simulators for Reference-Free Conversational Recommendation**
2603.18573v1 by Jerome Ramos, Feng Xia, Xi Wang, Shubham Chatterjee, Xiao Fu, Hossein A. Rahmani, Aldo Lipani

Training conversational recommender systems (CRS) requires extensive dialogue data, which is challenging to collect at scale. To address this, researchers have used simulated user-recommender conversations. Traditional simulation approaches often utilize a single large language model (LLM) that generates entire conversations with prior knowledge of the target items, leading to scripted and artificial dialogues. We propose a reference-free simulation framework that trains two independent LLMs, one as the user and one as the conversational recommender. These models interact in real-time without access to predetermined target items, but preference summaries and target attributes, enabling the recommender to genuinely infer user preferences through dialogue. This approach produces more realistic and diverse conversations that closely mirror authentic human-AI interactions. Our reference-free simulators match or exceed existing methods in quality, while offering a scalable solution for generating high-quality conversational recommendation data without constraining conversations to pre-defined target items. We conduct both quantitative and human evaluations to confirm the effectiveness of our reference-free approach.

摘要：訓練對話推薦系統 (CRS) 需要大量的對話數據，這在大規模收集上是具有挑戰性的。為了解決這個問題，研究人員使用了模擬的用戶-推薦者對話。傳統的模擬方法通常利用一個大型語言模型 (LLM)，該模型在了解目標項目的前提下生成整個對話，導致對話顯得生硬和不自然。我們提出了一個無參考的模擬框架，訓練兩個獨立的 LLM，一個作為用戶，另一個作為對話推薦者。這些模型在實時互動中，無需訪問預先確定的目標項目，而是使用偏好摘要和目標屬性，使推薦者能夠通過對話真實地推斷用戶的偏好。這種方法產生了更真實和多樣化的對話，與真實的人類-AI 互動非常相似。我們的無參考模擬器在質量上與現有方法相匹配或超越，同時提供了一個可擴展的解決方案，用於生成高質量的對話推薦數據，而不將對話限制於預定的目標項目。我們進行了定量和人類評估，以確認我們的無參考方法的有效性。

##### **Reasonably reasoning AI agents can avoid game-theoretic failures in zero-shot, provably**
2603.18563v1 by Enoch Hyunwook Kang

AI agents are increasingly deployed in interactive economic environments characterized by repeated AI-AI interactions. Despite AI agents' advanced capabilities, empirical studies reveal that such interactions often fail to stably induce a strategic equilibrium, such as a Nash equilibrium. Post-training methods have been proposed to induce a strategic equilibrium; however, it remains impractical to uniformly apply an alignment method across diverse, independently developed AI models in strategic settings. In this paper, we provide theoretical and empirical evidence that off-the-shelf reasoning AI agents can achieve Nash-like play zero-shot, without explicit post-training. Specifically, we prove that `reasonably reasoning' agents, i.e., agents capable of forming beliefs about others' strategies from previous observation and learning to best respond to these beliefs, eventually behave along almost every realized play path in a way that is weakly close to a Nash equilibrium of the continuation game. In addition, we relax the common-knowledge payoff assumption by allowing stage payoffs to be unknown and by having each agent observe only its own privately realized stochastic payoffs, and we show that we can still achieve the same on-path Nash convergence guarantee. We then empirically validate the proposed theories by simulating five game scenarios, ranging from a repeated prisoner's dilemma game to stylized repeated marketing promotion games. Our findings suggest that AI agents naturally exhibit such reasoning patterns and therefore attain stable equilibrium behaviors intrinsically, obviating the need for universal alignment procedures in many real-world strategic interactions.

摘要：AI 代理人越來越多地被部署在以重複 AI-AI 互動為特徵的互動經濟環境中。儘管 AI 代理人擁有先進的能力，實證研究顯示這種互動往往無法穩定地誘導出戰略均衡，例如納什均衡。已提出後訓練方法以誘導戰略均衡；然而，在戰略環境中，對於多樣化且獨立開發的 AI 模型，均勻應用對齊方法仍然不切實際。在本文中，我們提供理論和實證證據，表明現成的推理 AI 代理人可以在無需明確後訓練的情況下實現類納什的零-shot 表現。具體而言，我們證明了“合理推理”的代理人，即能夠根據先前觀察形成對他人策略的信念並學習最佳回應這些信念的代理人，最終在幾乎所有實現的遊戲路徑上表現出與延續遊戲的納什均衡弱接近的行為。此外，我們通過允許階段收益未知並讓每個代理人僅觀察自己私下實現的隨機收益，放寬了共同知識收益的假設，並顯示我們仍然可以實現相同的路徑納什收斂保證。然後，我們通過模擬五個遊戲場景來實證驗證所提出的理論，這些場景從重複的囚徒困境遊戲到風格化的重複市場推廣遊戲不等。我們的研究結果表明，AI 代理人自然展現出這種推理模式，因此在許多現實世界的戰略互動中內在地達到穩定的均衡行為，從而消除了對普遍對齊程序的需求。

##### **Expert Personas Improve LLM Alignment but Damage Accuracy: Bootstrapping Intent-Based Persona Routing with PRISM**
2603.18507v1 by Zizhao Hu, Mohammad Rostami, Jesse Thomason

Persona prompting can steer LLM generation towards a domain-specific tone and pattern. This behavior enables use cases in multi-agent systems where diverse interactions are crucial and human-centered tasks require high-level human alignment. Prior works provide mixed opinions on their utility: some report performance gains when using expert personas for certain domains and their contribution to data diversity in synthetic data creation, while others find near-zero or negative impact on general utility. To fully leverage the benefits of the LLM persona and avoid its harmfulness, a more comprehensive investigation of the mechanism is crucial. In this work, we study how model optimization, task type, prompt length, and placement can impact expert persona effectiveness across instruction-tuned and reasoning LLMs, and provide insight into conditions under which expert personas fail and succeed. Based on our findings, we developed a pipeline to fully leverage the benefits of an expert persona, named PRISM (Persona Routing via Intent-based Self-Modeling), which self-distills an intent-conditioned expert persona into a gated LoRA adapter through a bootstrapping process that requires no external data, models, or knowledge. PRISM enhances human preference and safety alignment on generative tasks while maintaining accuracy on discriminative tasks across all models, with minimal memory and computing overhead.

摘要：Persona 提示可以引導 LLM 生成朝向特定領域的語調和模式。<lb/>這種行為使得在多代理系統中使用案例成為可能，因為多樣的互動至關重要，而以人為中心的任務需要高水平的人類對齊。<lb/>先前的研究對其效用提供了不同的看法：一些報告在某些領域使用專家角色時的性能提升，以及它們對合成數據創建中數據多樣性的貢獻，而另一些則發現對一般效用的影響接近零或為負。<lb/>為了充分利用 LLM 角色的好處並避免其有害性，對其機制進行更全面的研究至關重要。<lb/>在本研究中，我們探討了模型優化、任務類型、提示長度和位置如何影響專家角色在指令調整和推理 LLM 中的有效性，並提供了專家角色失敗和成功的條件的見解。<lb/>根據我們的發現，我們開發了一個管道來充分利用專家角色的好處，名為 PRISM（基於意圖的自我建模的角色路由），它通過一個不需要外部數據、模型或知識的啟動過程，自我提煉出一個意圖條件的專家角色到一個門控 LoRA 適配器中。<lb/>PRISM 在生成任務上增強了人類偏好和安全對齊，同時在所有模型的區分任務上保持準確性，並且內存和計算開銷最小。

##### **WASD: Locating Critical Neurons as Sufficient Conditions for Explaining and Controlling LLM Behavior**
2603.18474v1 by Haonan Yu, Junhao Liu, Zhenyu Yan, Haoran Lin, Xin Zhang

Precise behavioral control of large language models (LLMs) is critical for complex applications. However, existing methods often incur high training costs, lack natural language controllability, or compromise semantic coherence. To bridge this gap, we propose WASD (unWeaving Actionable Sufficient Directives), a novel framework that explains model behavior by identifying sufficient neural conditions for token generation. Our method represents candidate conditions as neuron-activation predicates and iteratively searches for a minimal set that guarantees the current output under input perturbations. Experiments on SST-2 and CounterFact with the Gemma-2-2B model demonstrate that our approach produces explanations that are more stable, accurate, and concise than conventional attribution graphs. Moreover, through a case study on controlling cross-lingual output generation, we validated the practical effectiveness of WASD in controlling model behavior.

摘要：精確控制大型語言模型（LLMs）的行為對於複雜應用至關重要。<lb/>然而，現有的方法往往需要高昂的訓練成本，缺乏自然語言的可控性，或妥協於語義的一致性。<lb/>為了彌補這一差距，我們提出了WASD（unWeaving Actionable Sufficient Directives），這是一個新穎的框架，通過識別生成標記的充分神經條件來解釋模型行為。<lb/>我們的方法將候選條件表示為神經元激活謂詞，並迭代搜索一組最小條件，以保證在輸入擾動下當前輸出。<lb/>在SST-2和CounterFact上使用Gemma-2-2B模型的實驗表明，我們的方法產生的解釋比傳統的歸因圖更穩定、準確且簡潔。<lb/>此外，通過對跨語言輸出生成控制的案例研究，我們驗證了WASD在控制模型行為方面的實際有效性。

##### **AlignMamba-2: Enhancing Multimodal Fusion and Sentiment Analysis with Modality-Aware Mamba**
2603.18462v1 by Yan Li, Yifei Xing, Xiangyuan Lan, Xin Li, Haifeng Chen, Dongmei Jiang

In the era of large-scale pre-trained models, effectively adapting general knowledge to specific affective computing tasks remains a challenge, particularly regarding computational efficiency and multimodal heterogeneity. While Transformer-based methods have excelled at modeling inter-modal dependencies, their quadratic computational complexity limits their use with long-sequence data. Mamba-based models have emerged as a computationally efficient alternative; however, their inherent sequential scanning mechanism struggles to capture the global, non-sequential relationships that are crucial for effective cross-modal alignment. To address these limitations, we propose \textbf{AlignMamba-2}, an effective and efficient framework for multimodal fusion and sentiment analysis. Our approach introduces a dual alignment strategy that regularizes the model using both Optimal Transport distance and Maximum Mean Discrepancy, promoting geometric and statistical consistency between modalities without incurring any inference-time overhead. More importantly, we design a Modality-Aware Mamba layer, which employs a Mixture-of-Experts architecture with modality-specific and modality-shared experts to explicitly handle data heterogeneity during the fusion process. Extensive experiments on four challenging benchmarks, including dynamic time-series (on the CMU-MOSI and CMU-MOSEI datasets) and static image-related tasks (on the NYU-Depth V2 and MVSA-Single datasets), demonstrate that AlignMamba-2 establishes a new state-of-the-art in both effectiveness and efficiency across diverse pattern recognition tasks, ranging from dynamic time-series analysis to static image-text classification.

摘要：在大型預訓練模型的時代，將一般知識有效地適應於特定的情感計算任務仍然是一個挑戰，特別是在計算效率和多模態異質性方面。雖然基於Transformer的方法在建模模態間依賴性方面表現出色，但其二次計算複雜度限制了它們在長序列數據中的使用。基於Mamba的模型作為一種計算效率高的替代方案出現；然而，它們固有的序列掃描機制在捕捉對於有效跨模態對齊至關重要的全局非序列關係方面面臨困難。為了解決這些限制，我們提出了\textbf{AlignMamba-2}，這是一個有效且高效的多模態融合和情感分析框架。我們的方法引入了一種雙重對齊策略，通過使用最佳傳輸距離和最大均值差異來正則化模型，促進模態之間的幾何和統計一致性，而不會產生任何推理時間的開銷。更重要的是，我們設計了一個模態感知的Mamba層，該層採用了專家混合架構，具有特定模態和共享模態的專家，以明確處理融合過程中的數據異質性。在四個具有挑戰性的基準上進行的廣泛實驗，包括動態時間序列（在CMU-MOSI和CMU-MOSEI數據集上）和靜態圖像相關任務（在NYU-Depth V2和MVSA-Single數據集上），證明了AlignMamba-2在各種模式識別任務中，包括動態時間序列分析到靜態圖像-文本分類，建立了新的最佳效果和效率的標準。

##### **HypeMed: Enhancing Medication Recommendations with Hypergraph-Based Patient Relationships**
2603.18459v1 by Xiangxu Zhang, Xiao Zhou, Hongteng Xu, Jianxun Lian

Medication recommendations aim to generate safe and effective medication sets from health records. However, accurately recommending medications hinges on inferring a patient's latent clinical condition from sparse and noisy observations, which requires both (i) preserving the visit-level combinatorial semantics of co-occurring entities and (ii) leveraging informative historical references through effective, visit-conditioned retrieval. Most existing methods fall short in one of both aspects: graph-based modeling often fragments higher-order intra-visit patterns into pairwise relations, while inter-visit augmentation methods commonly exhibit an imbalance between learning a globally stable representation space and performing dynamic retrieval within it. To address these limitations, this paper proposes HypeMed, a two-stage hypergraph-based framework unifying intra-visit coherence modeling and inter-visit augmentation. HypeMed consists of two core modules: MedRep for representation pre-training, and SimMR for similarity-enhanced recommendation. In the first stage, MedRep encodes clinical visits as hyperedges via knowledge-aware contrastive pre-training, creating a globally consistent, retrieval-friendly embedding space. In the second stage, SimMR performs dynamic retrieval within this space, fusing retrieved references with the patient's longitudinal data to refine medication prediction. Evaluation on real-world benchmarks shows that HypeMed outperforms state-of-the-art baselines in both recommendation precision and DDI reduction, simultaneously enhancing the effectiveness and safety of clinical decision support.

摘要：藥物推薦旨在從健康記錄中生成安全且有效的藥物組合。<lb/>然而，準確推薦藥物的關鍵在於從稀疏且嘈雜的觀察中推斷患者潛在的臨床狀況，這需要同時具備 (i) 保留訪問層級的共現實體的組合語義，以及 (ii) 通過有效的訪問條件檢索利用有意義的歷史參考。<lb/>大多數現有方法在這兩個方面之一上都存在不足：基於圖的建模通常將高階的訪問內部模式碎片化為成對關係，而訪問間增強方法則常常在學習全局穩定的表示空間和在其中執行動態檢索之間存在不平衡。<lb/>為了解決這些限制，本文提出了 HypeMed，一個兩階段的基於超圖的框架，統一了訪問內部一致性建模和訪問間增強。<lb/>HypeMed 包含兩個核心模塊：MedRep 用於表示預訓練，SimMR 用於相似性增強推薦。<lb/>在第一階段，MedRep 通過知識感知對比預訓練將臨床訪問編碼為超邊，創建一個全球一致且適合檢索的嵌入空間。<lb/>在第二階段，SimMR 在此空間內執行動態檢索，將檢索到的參考與患者的縱向數據融合，以細化藥物預測。<lb/>在真實世界基準上的評估顯示，HypeMed 在推薦精度和 DDI 減少方面均優於最先進的基準，並同時提高了臨床決策支持的有效性和安全性。

##### **The Impact of Corporate AI Washing on Farmers' Digital Financial Behavior Response -- An Analysis from the Perspective of Digital Financial Exclusion**
2603.18421v1 by Li Wenxiu, Wen Zhanjie, Xia Jiechang, Guo Jingqiao

In the context of the rapid development of digital finance, some financial technology companies exhibit the phenomenon of "AI washing," where they overstate their AI capabilities while underinvesting in actual AI resources. This paper constructs a corporate-level AI washing index based on CHFS2019 data and AI investment data from 15-20 financial technology companies, analyzing and testing its impact on farmers' digital financial behavior response. The study finds that AI washing significantly suppresses farmers' digital financial behavior; the higher the degree of AI washing, the lower the response level of farmers' digital financial behavior. Moreover, AI washing indirectly inhibits farmers' behavioral responses by exacerbating knowledge exclusion and risk exclusion. Social capital can positively moderate the negative impact of AI washing; among farmer groups with high social capital, the suppressive effect of AI washing on digital financial behavior is significantly weaker than that among groups with low social capital. In response, this paper suggests that regulatory authorities establish a strict information disclosure system for AI technology, conduct differentiated digital financial education to enhance the identification capabilities of vulnerable groups, promote digital financial mutual aid groups to leverage the protective effects of social capital, improve the consumer protection mechanism for farmers in digital finance, and set up pilot "Digital Inclusive Finance Demonstration Counties," etc.

摘要：在數位金融快速發展的背景下，一些金融科技公司出現了「AI 洗牌」的現象，即它們誇大自己的 AI 能力，同時對實際的 AI 資源投資不足。本文基於 CHFS2019 數據和 15-20 家金融科技公司的 AI 投資數據構建了一個企業級 AI 洗牌指數，分析並測試其對農民數位金融行為反應的影響。研究發現，AI 洗牌顯著抑制了農民的數位金融行為；AI 洗牌的程度越高，農民的數位金融行為反應水平越低。此外，AI 洗牌通過加劇知識排斥和風險排斥間接抑制農民的行為反應。社會資本可以正向調節 AI 洗牌的負面影響；在社會資本高的農民群體中，AI 洗牌對數位金融行為的抑制效應顯著弱於社會資本低的群體。針對這一點，本文建議監管機構建立嚴格的 AI 技術信息披露制度，進行差異化的數位金融教育以增強弱勢群體的識別能力，促進數位金融互助組織以發揮社會資本的保護效應，改善農民在數位金融中的消費者保護機制，並設立試點「數位包容性金融示範縣」等。

##### **From Weak Cues to Real Identities: Evaluating Inference-Driven De-Anonymization in LLM Agents**
2603.18382v1 by Myeongseob Ko, Jihyun Jeong, Sumiran Singh Thakur, Gyuhak Kim, Ruoxi Jia

Anonymization is widely treated as a practical safeguard because re-identifying anonymous records was historically costly, requiring domain expertise, tailored algorithms, and manual corroboration. We study a growing privacy risk that may weaken this barrier: LLM-based agents can autonomously reconstruct real-world identities from scattered, individually non-identifying cues. By combining these sparse cues with public information, agents resolve identities without bespoke engineering. We formalize this threat as \emph{inference-driven linkage} and systematically evaluate it across three settings: classical linkage scenarios (Netflix and AOL), \emph{InferLink} (a controlled benchmark varying task intent, shared cues, and attacker knowledge), and modern text-rich artifacts. Without task-specific heuristics, agents successfully execute both fixed-pool matching and open-ended identity resolution. In the Netflix Prize setting, an agent reconstructs 79.2\% of identities, significantly outperforming a 56.0\% classical baseline. Furthermore, linkage emerges not only under explicit adversarial prompts but also as a byproduct of benign cross-source analysis in \emph{InferLink} and unstructured research narratives. These findings establish that identity inference -- not merely explicit information disclosure -- must be treated as a first-class privacy risk; evaluations must measure what identities an agent can infer.

摘要：匿名化被廣泛視為一種實用的保護措施，因為重新識別匿名記錄在歷史上是昂貴的，需依賴領域專業知識、量身定制的算法和人工驗證。<lb/>我們研究了一個日益增長的隱私風險，這可能削弱這一障礙：基於LLM的代理可以自主從分散的、單獨不具識別性的線索中重建現實世界的身份。<lb/>通過將這些稀疏的線索與公共信息結合，代理能夠在沒有定制工程的情況下解決身份問題。<lb/>我們將這一威脅形式化為\emph{推理驅動的連結}，並在三個場景中系統性地評估它：經典連結場景（Netflix和AOL）、\emph{InferLink}（一個受控基準，變化任務意圖、共享線索和攻擊者知識）以及現代文本豐富的文獻。<lb/>在沒有特定任務啟發式的情況下，代理成功執行了固定池匹配和開放式身份解析。<lb/>在Netflix獎項設定中，一個代理重建了79.2\%的身份，顯著超過56.0\%的經典基準。<lb/>此外，連結不僅在明確的對抗性提示下出現，還作為\emph{InferLink}和非結構化研究敘述中良性跨源分析的副產品出現。<lb/>這些發現確立了身份推理——而不僅僅是明確的信息披露——必須被視為一級隱私風險；評估必須衡量代理可以推斷出哪些身份。

##### **PlanTwin: Privacy-Preserving Planning Abstractions for Cloud-Assisted LLM Agents**
2603.18377v1 by Guangsheng Yu, Qin Wang, Rui Lang, Shuai Su, Xu Wang

Cloud-hosted large language models (LLMs) have become the de facto planners in agentic systems, coordinating tools and guiding execution over local environments. In many deployments, however, the environment being planned over is private, containing source code, files, credentials, and metadata that cannot be exposed to the cloud. Existing solutions address adjacent concerns, such as execution isolation, access control, or confidential inference, but they do not control what cloud planners observe during planning: within the permitted scope, \textit{raw environment state is still exposed}.
  We introduce PlanTwin, a privacy-preserving architecture for cloud-assisted planning without exposing raw local context. The key idea is to project the real environment into a \textit{planning-oriented digital twin}: a schema-constrained and de-identified abstract graph that preserves planning-relevant structure while removing reconstructable details. The cloud planner operates solely on this sanitized twin through a bounded capability interface, while a local gatekeeper enforces safety policies and cumulative disclosure budgets. We further formalize the privacy-utility trade-off as a capability granularity problem, define architectural privacy goals using $(k,δ)$-anonymity and $ε$-unlinkability, and mitigate compositional leakage through multi-turn disclosure control.
  We implement PlanTwin as middleware between local agents and cloud planners and evaluate it on 60 agentic tasks across ten domains with four cloud planners. PlanTwin achieves full sensitive-item non-disclosure (SND = 1.0) while maintaining planning quality close to full-context systems: three of four planners achieve PQS $> 0.79$, and the full pipeline incurs less than 2.2\% utility loss.

摘要：雲端托管的大型語言模型（LLMs）已成為代理系統中的事實上規劃者，協調工具並指導在本地環境中的執行。然後，在許多部署中，所規劃的環境是私有的，包含源代碼、文件、憑證和無法暴露於雲端的元數據。現有的解決方案解決了相鄰的問題，例如執行隔離、訪問控制或機密推斷，但它們並不控制雲端規劃者在規劃過程中觀察到的內容：在允許的範圍內，\textit{原始環境狀態仍然被暴露}。

我們介紹了PlanTwin，一種在不暴露原始本地上下文的情況下進行雲端輔助規劃的隱私保護架構。其關鍵思想是將真實環境投影到\textit{以規劃為導向的數位雙胞胎}：一個受架構約束且去識別化的抽象圖，保留與規劃相關的結構，同時去除可重建的細節。雲端規劃者僅通過有限的能力介面在這個經過清理的雙胞胎上運作，而本地守門人則執行安全政策和累積披露預算。我們進一步將隱私-效用權衡形式化為能力粒度問題，使用$(k,δ)$-匿名性和$ε$-不可鏈接性定義架構隱私目標，並通過多輪披露控制減輕組合洩漏。

我們將PlanTwin實現為本地代理和雲端規劃者之間的中介，並在十個領域的60個代理任務上進行評估，使用四個雲端規劃者。PlanTwin實現了完全的敏感項目不披露（SND = 1.0），同時保持接近全上下文系統的規劃質量：四個規劃者中有三個達到PQS $&gt; 0.79$，整個管道的效用損失不到2.2\%。

##### **Interpretability without actionability: mechanistic methods cannot correct language model errors despite near-perfect internal representations**
2603.18353v1 by Sanjay Basu, Sadiq Y. Patel, Parth Sheth, Bhairavi Muralidharan, Namrata Elamaran, Aakriti Kinra, John Morgan, Rajaie Batniji

Language models encode task-relevant knowledge in internal representations that far exceeds their output performance, but whether mechanistic interpretability methods can bridge this knowledge-action gap has not been systematically tested. We compared four mechanistic interpretability methods -- concept bottleneck steering (Steerling-8B), sparse autoencoder feature steering, logit lens with activation patching, and linear probing with truthfulness separator vector steering (Qwen 2.5 7B Instruct) -- for correcting false-negative triage errors using 400 physician-adjudicated clinical vignettes (144 hazards, 256 benign). Linear probes discriminated hazardous from benign cases with 98.2% AUROC, yet the model's output sensitivity was only 45.1%, a 53-percentage-point knowledge-action gap. Concept bottleneck steering corrected 20% of missed hazards but disrupted 53% of correct detections, indistinguishable from random perturbation (p=0.84). SAE feature steering produced zero effect despite 3,695 significant features. TSV steering at high strength corrected 24% of missed hazards while disrupting 6% of correct detections, but left 76% of errors uncorrected. Current mechanistic interpretability methods cannot reliably translate internal knowledge into corrected outputs, with implications for AI safety frameworks that assume interpretability enables effective error correction.

摘要：語言模型在內部表示中編碼與任務相關的知識，這些知識遠超過它們的輸出性能，但機械解釋方法是否能彌合這一知識與行動之間的差距尚未經過系統性測試。我們比較了四種機械解釋方法——概念瓶頸引導（Steerling-8B）、稀疏自編碼器特徵引導、帶有激活修補的邏輯透鏡，以及使用真實性分隔向量引導的線性探測（Qwen 2.5 7B Instruct）——以糾正使用400個醫生裁定的臨床小插曲（144個危險、256個良性）的假陰性分流錯誤。線性探測以98.2%的AUROC區分危險與良性案例，但模型的輸出靈敏度僅為45.1%，知識與行動之間存在53個百分點的差距。概念瓶頸引導糾正了20%的漏檢危險，但擾亂了53%的正確檢測，與隨機擾動無法區分（p=0.84）。儘管有3,695個顯著特徵，SAE特徵引導產生了零效果。高強度的TSV引導糾正了24%的漏檢危險，同時擾亂了6%的正確檢測，但仍有76%的錯誤未被糾正。目前的機械解釋方法無法可靠地將內部知識轉化為糾正的輸出，這對假設可解釋性能夠有效糾正錯誤的AI安全框架有重要影響。

##### **Approximate Subgraph Matching with Neural Graph Representations and Reinforcement Learning**
2603.18314v1 by Kaiyang Li, Shihao Ji, Zhipeng Cai, Wei Li

Approximate subgraph matching (ASM) is a task that determines the approximate presence of a given query graph in a large target graph. Being an NP-hard problem, ASM is critical in graph analysis with a myriad of applications ranging from database systems and network science to biochemistry and privacy. Existing techniques often employ heuristic search strategies, which cannot fully utilize the graph information, leading to sub-optimal solutions. This paper proposes a Reinforcement Learning based Approximate Subgraph Matching (RL-ASM) algorithm that exploits graph transformers to effectively extract graph representations and RL-based policies for ASM. Our model is built upon the branch-and-bound algorithm that selects one pair of nodes from the two input graphs at a time for potential matches. Instead of using heuristics, we exploit a Graph Transformer architecture to extract feature representations that encode the full graph information. To enhance the training of the RL policy, we use supervised signals to guide our agent in an imitation learning stage. Subsequently, the policy is fine-tuned with the Proximal Policy Optimization (PPO) that optimizes the accumulative long-term rewards over episodes. Extensive experiments on both synthetic and real-world datasets demonstrate that our RL-ASM outperforms existing methods in terms of effectiveness and efficiency. Our source code is available at https://github.com/KaiyangLi1992/RL-ASM.

摘要：近似子圖匹配（ASM）是一項任務，旨在確定給定查詢圖在大型目標圖中的近似存在。作為一個 NP 困難問題，ASM 在圖分析中至關重要，應用範圍廣泛，從數據庫系統和網絡科學到生物化學和隱私。現有技術通常採用啟發式搜索策略，這無法充分利用圖信息，導致次優解。本文提出了一種基於強化學習的近似子圖匹配（RL-ASM）算法，利用圖變換器有效提取圖表示和基於 RL 的 ASM 策略。我們的模型基於分支限界算法，該算法一次從兩個輸入圖中選擇一對節點進行潛在匹配。我們不使用啟發式方法，而是利用圖變換器架構提取編碼完整圖信息的特徵表示。為了增強 RL 策略的訓練，我們使用監督信號來指導我們的代理進行模仿學習階段。隨後，該策略通過近端政策優化（PPO）進行微調，以優化整個回合的累積長期獎勵。在合成和真實世界數據集上的大量實驗表明，我們的 RL-ASM 在效果和效率方面優於現有方法。我們的源代碼可在 https://github.com/KaiyangLi1992/RL-ASM 獲得。

##### **Detection Is Cheap, Routing Is Learned: Why Refusal-Based Alignment Evaluation Fails**
2603.18280v1 by Gregory N. Frank

Current alignment evaluation mostly measures whether models encode dangerous concepts and whether they refuse harmful requests. Both miss the layer where alignment often operates: routing from concept detection to behavioral policy. We study political censorship in Chinese-origin language models as a natural experiment, using probes, surgical ablations, and behavioral tests across nine open-weight models from five labs. Three findings follow. First, probe accuracy alone is non-diagnostic: political probes, null controls, and permutation baselines can all reach 100%, so held-out category generalization is the informative test. Second, surgical ablation reveals lab-specific routing. Removing the political-sensitivity direction eliminates censorship and restores accurate factual output in most models tested, while one model confabulates because its architecture entangles factual knowledge with the censorship mechanism. Cross-model transfer fails, indicating that routing geometry is model- and lab-specific. Third, refusal is no longer the dominant censorship mechanism. Within one model family, hard refusal falls to zero while narrative steering rises to the maximum, making censorship invisible to refusal-only benchmarks. These results support a three-stage descriptive framework: detect, route, generate. Models often retain the relevant knowledge; alignment changes how that knowledge is expressed. Evaluations that audit only detection or refusal therefore miss the routing mechanism that most directly determines behavior.

摘要：目前的對齊評估主要衡量模型是否編碼危險概念以及是否拒絕有害請求。這兩者都忽略了對齊通常運作的層面：從概念檢測到行為政策的路由。我們將中國來源語言模型中的政治審查作為一個自然實驗進行研究，使用探針、外科切除和行為測試，涵蓋來自五個實驗室的九個開放權重模型。有三個發現。首先，僅僅依賴探針準確性並不能診斷：政治探針、無效對照和置換基準都可以達到100%，因此保留類別的泛化是有信息的測試。其次，外科切除揭示了實驗室特定的路由。去除政治敏感性方向消除了審查，並在大多數測試的模型中恢復了準確的事實輸出，而一個模型因其架構將事實知識與審查機制糾纏在一起而產生了虛構。跨模型轉移失敗，表明路由幾何是模型和實驗室特定的。第三，拒絕不再是主要的審查機制。在一個模型家族中，強硬拒絕降至零，而敘事引導則上升至最大，這使得審查對僅拒絕基準來說變得不可見。這些結果支持一個三階段的描述框架：檢測、路由、生成。模型通常保留相關知識；對齊改變了該知識的表達方式。因此，僅審計檢測或拒絕的評估會錯過最直接決定行為的路由機制。

##### **MolRGen: A Training and Evaluation Setting for De Novo Molecular Generation with Reasonning Models**
2603.18256v1 by Philippe Formont, Maxime Darrin, Ismail Ben Ayed, Pablo Piantanida

Recent advances in reasoning-based large language models (LLMs) have demonstrated substantial improvements in complex problem-solving tasks. Motivated by these advances, several works have explored the application of reasoning LLMs to drug discovery and molecular design. However, most existing approaches either focus on evaluation or rely on training setups that require ground-truth labels, such as molecule pairs with known property modifications. Such supervision is unavailable in \textit{de novo} molecular generation, where the objective is to generate novel molecules that optimize a desirability score without prior knowledge of high-scoring candidates. To bridge this gap, we introduce MolRGen, a large-scale benchmark and dataset for training and evaluating reasoning-based LLMs on \textit{de novo} molecular generation. Our contributions are threefold. First, we propose a setting to evaluate and train models for \textit{de novo} molecular generation and property prediction. Second, we introduce a novel diversity-aware top-$k$ score that captures both the quality and diversity of generated molecules. Third, we show our setting can be used to train LLMs for molecular generation, training a 24B LLM with reinforcement learning, and we provide a detailed analysis of its performance and limitations.

摘要：最近在基於推理的大型語言模型（LLMs）方面的進展顯示出在複雜問題解決任務中有顯著的改善。受到這些進展的激勵，幾項研究探索了將推理 LLM 應用於藥物發現和分子設計的可能性。然而，大多數現有的方法要麼專注於評估，要麼依賴於需要真實標籤的訓練設置，例如具有已知性質修改的分子對。在\textit{de novo} 分子生成中，這種監督是不可用的，因為其目標是生成新穎的分子，以優化一個期望分數，而不需要對高分候選者的先驗知識。為了填補這一空白，我們介紹了 MolRGen，一個用於訓練和評估基於推理的 LLM 在\textit{de novo} 分子生成上的大型基準和數據集。我們的貢獻有三個方面。首先，我們提出了一個設置來評估和訓練用於\textit{de novo} 分子生成和性質預測的模型。其次，我們引入了一種新穎的考慮多樣性的 top-$k$ 分數，該分數同時捕捉生成分子的質量和多樣性。第三，我們展示了我們的設置可以用於訓練 LLM 進行分子生成，並使用強化學習訓練了一個 24B 的 LLM，並提供了其性能和限制的詳細分析。

##### **How Psychological Learning Paradigms Shaped and Constrained Artificial Intelligence**
2603.18203v1 by Alex Anvi Eponon, Ildar Batyrshin, Christian E. Maldonado-Sifuentes, Grigori Sidorov

The dominant paradigms of artificial intelligence were shaped by learning theories from psychology: behaviorism inspired reinforcement learning, cognitivism gave rise to deep learning and memory-augmented architectures, and constructivism influenced curriculum learning and compositional approaches. This paper argues that each AI paradigm inherited not only the strengths but the structural limitations of the psychological theory that inspired it. Reinforcement learning cannot account for the internal structure of knowledge, deep learning compresses representations into opaque parameter spaces resistant to principled update, and current integrative approaches lack a formal account of how new understanding is constructed from existing components. The paper further examines a cross-cultural divergence in the interpretation of rote learning, arguing that the Eastern conception of memorization as a structured, multi-phase precursor to understanding offers an underexploited bridge between psychological theory and AI methodology. Drawing on the systematicity debate and critique of Aizawa of both classicism and connectionism, this paper introduces ReSynth, a trimodular framework that separates reasoning (Intellect), purpose (Identity), and knowledge (Memory) as architecturally independent components. The paper traces the genealogy from psychological paradigm to AI method, diagnoses the inherited limitations at each stage, and argues that adaptability, the central challenge of artificial general intelligence requires a representational architecture in which systematic behavior is a necessary consequence rather than an accidental property.

摘要：主流的人工智慧範式受到心理學學習理論的影響：行為主義啟發了強化學習，認知主義促成了深度學習和記憶增強架構，而建構主義則影響了課程學習和組合方法。本文主張，每個人工智慧範式不僅繼承了其啟發來源的心理學理論的優勢，還繼承了其結構性限制。強化學習無法解釋知識的內部結構，深度學習將表徵壓縮到不透明的參數空間，這些空間對原則性更新具有抵抗力，而當前的整合方法缺乏對如何從現有組件構建新理解的正式說明。本文進一步檢視了對死記硬背的跨文化解讀差異，主張東方對記憶的理解作為理解的結構性、多階段前驅，提供了一個未被充分利用的橋樑，將心理學理論與人工智慧方法論連結起來。本文借鑒了系統性辯論以及對經典主義和連結主義的相澤批評，介紹了ReSynth，一個三模組框架，將推理（智力）、目的（身份）和知識（記憶）作為建築上獨立的組件分開。本文追溯了從心理學範式到人工智慧方法的系譜，診斷了每個階段繼承的限制，並主張適應性，作為人工通用智慧的核心挑戰，需要一種表徵架構，其中系統性行為是必要的結果，而不是偶然的特性。

##### **Modeling the human lexicon under temperature variations: linguistic factors, diversity and typicality in LLM word associations**
2603.18171v1 by Maria Andueza Rodriguez, Marie Candito, Richard Huyghe

Large language models (LLMs) achieve impressive results in terms of fluency in text generation, yet the nature of their linguistic knowledge - in particular the human-likeness of their internal lexicon - remains uncertain. This study compares human and LLM-generated word associations to evaluate how accurately models capture human lexical patterns. Using English cue-response pairs from the SWOW dataset and newly generated associations from three LLMs (Mistral-7B, Llama-3.1-8B, and Qwen-2.5-32B) across multiple temperature settings, we examine (i) the influence of lexical factors such as word frequency and concreteness on cue-response pairs, and (ii) the variability and typicality of LLM responses compared to human responses. Results show that all models mirror human trends for frequency and concreteness but differ in response variability and typicality. Larger models such as Qwen tend to emulate a single "prototypical" human participant, generating highly typical but minimally variable responses, while smaller models such as Mistral and Llama produce more variable yet less typical responses. Temperature settings further influence this trade-off, with higher values increasing variability but decreasing typicality. These findings highlight both the similarities and differences between human and LLM lexicons, emphasizing the need to account for model size and temperature when probing LLM lexical representations.

摘要：大型語言模型（LLMs）在文本生成的流暢性方面取得了令人印象深刻的成果，但它們的語言知識的本質，尤其是其內部詞彙的類人特性，仍然不確定。這項研究比較了人類和LLM生成的詞彙聯想，以評估模型在多大程度上準確捕捉人類詞彙模式。利用來自SWOW數據集的英語提示-反應對和三個LLM（Mistral-7B、Llama-3.1-8B和Qwen-2.5-32B）在多個溫度設置下生成的新聯想，我們檢查了（i）詞彙因素如詞頻和具體性對提示-反應對的影響，以及（ii）LLM反應與人類反應相比的變異性和典型性。結果顯示，所有模型在頻率和具體性上都反映了人類的趨勢，但在反應的變異性和典型性上有所不同。像Qwen這樣的大型模型傾向於模仿單一的“原型”人類參與者，生成高度典型但變異性最小的反應，而像Mistral和Llama這樣的小型模型則產生更具變異性但不那麼典型的反應。溫度設置進一步影響這一權衡，較高的值增加了變異性但降低了典型性。這些發現突顯了人類和LLM詞彙之間的相似性和差異，強調在探討LLM詞彙表徵時需要考慮模型大小和溫度。

##### **TDAD: Test-Driven Agentic Development - Reducing Code Regressions in AI Coding Agents via Graph-Based Impact Analysis**
2603.17973v1 by Pepe Alonso

AI coding agents can resolve real-world software issues, yet they frequently introduce regressions, breaking tests that previously passed. Current benchmarks focus almost exclusively on resolution rate, leaving regression behavior under-studied. This paper presents TDAD (Test-Driven Agentic Development), an open-source tool and benchmark methodology that combines abstract-syntax-tree (AST) based code-test graph construction with weighted impact analysis to surface the tests most likely affected by a proposed change. Evaluated on SWE-bench Verified with two local models (Qwen3-Coder 30B on 100 instances and Qwen3.5-35B-A3B on 25 instances), TDAD's GraphRAG workflow reduced test-level regressions by 70% (6.08% to 1.82%) and improved resolution from 24% to 32% when deployed as an agent skill. A surprising finding is that TDD prompting alone increased regressions (9.94%), revealing that smaller models benefit more from contextual information (which tests to verify) than from procedural instructions (how to do TDD). An autonomous auto-improvement loop raised resolution from 12% to 60% on a 10-instance subset with 0% regression. These findings suggest that for AI agent tool design, surfacing contextual information outperforms prescribing procedural workflows. All code, data, and logs are publicly available at https://github.com/pepealonso95/TDAD.

摘要：AI 編碼代理可以解決現實世界的軟體問題，但它們經常引入回歸，導致之前通過的測試失敗。當前的基準幾乎專注於解決率，回歸行為則未受到充分研究。本文提出了 TDAD（測試驅動代理開發），這是一個開源工具和基準方法論，結合了基於抽象語法樹（AST）的代碼測試圖構建和加權影響分析，以顯示最有可能受到提議變更影響的測試。TDAD 的 GraphRAG 工作流程在 SWE-bench 上經過兩個本地模型的驗證（Qwen3-Coder 30B 在 100 個實例上和 Qwen3.5-35B-A3B 在 25 個實例上）後，將測試級回歸減少了 70%（從 6.08% 降至 1.82%），並在作為代理技能部署時將解決率從 24% 提高到 32%。一個令人驚訝的發現是，僅僅使用 TDD 提示就增加了回歸（9.94%），這顯示出較小的模型從上下文信息（哪些測試需要驗證）中獲益更多，而不是從程序性指令（如何進行 TDD）中獲益。自動改進循環將解決率從 12% 提高到 60%，在 10 個實例的子集上回歸為 0%。這些發現表明，對於 AI 代理工具設計，顯示上下文信息的效果超過了規定程序性工作流程的效果。所有代碼、數據和日誌均可在 https://github.com/pepealonso95/TDAD 上公開獲得。

##### **Don't Vibe Code, Do Skele-Code: Interactive No-Code Notebooks for Subject Matter Experts to Build Lower-Cost Agentic Workflows**
2603.18122v1 by Sriram Gopalakrishnan

Skele-Code is a natural-language and graph-based interface for building workflows with AI agents, designed especially for less or non-technical users. It supports incremental, interactive notebook-style development, and each step is converted to code with a required set of functions and behavior to enable incremental building of workflows. Agents are invoked only for code generation and error recovery, not orchestration or task execution. This agent-supported, but code-first approach to workflows, along with the context-engineering used in Skele-Code, can help reduce token costs compared to the multi-agent system approach to executing workflows. Skele-Code produces modular, easily extensible, and shareable workflows. The generated workflows can also be used as skills by agents, or as steps in other workflows.

摘要：Skele-Code是一種基於自然語言和圖形的介面，旨在幫助較少或非技術用戶構建AI代理的工作流程。<lb/>它支持增量式、互動式的筆記本風格開發，每一步都轉換為代碼，並具備所需的函數和行為，以實現工作流程的增量構建。<lb/>代理僅用於代碼生成和錯誤恢復，而不是協調或任務執行。<lb/>這種以代碼為先的工作流程方法，結合Skele-Code中使用的上下文工程，能夠幫助降低與多代理系統執行工作流程相比的令牌成本。<lb/>Skele-Code生成模組化、易於擴展和可分享的工作流程。<lb/>生成的工作流程也可以被代理用作技能，或作為其他工作流程中的步驟。

##### **CodeScout: An Effective Recipe for Reinforcement Learning of Code Search Agents**
2603.17829v1 by Lintang Sutawika, Aditya Bharat Soni, Bharath Sriraam R R, Apurva Gandhi, Taha Yassine, Sanidhya Vijayvargiya, Yuchen Li, Xuhui Zhou, Yilin Zhang, Leander Melroy Maben, Graham Neubig

A prerequisite for coding agents to perform tasks on large repositories is code localization - the identification of relevant files, classes, and functions to work on. While repository-level code localization has been performed using embedding-based retrieval approaches such as vector search, recent work has focused on developing agents to localize relevant code either as a standalone precursor to or interleaved with performing actual work. Most prior methods on agentic code search equip the agent with complex, specialized tools, such as repository graphs derived from static analysis. In this paper, we demonstrate that, with an effective reinforcement learning recipe, a coding agent equipped with nothing more than a standard Unix terminal can be trained to achieve strong results. Our experiments on three benchmarks (SWE-Bench Verified, Pro, and Lite) reveal that our models consistently achieve superior or competitive performance over 2-18x larger base and post-trained LLMs and sometimes approach performance provided by closed models like Claude Sonnet, even when using specialized scaffolds. Our work particularly focuses on techniques for re-purposing existing coding agent environments for code search, reward design, and RL optimization. We release the resulting model family, CodeScout, along with all our code and data for the community to build upon.

摘要：一個編碼代理在大型代碼庫上執行任務的前提是代碼定位——識別相關的文件、類別和函數以進行操作。雖然已經使用基於嵌入的檢索方法（例如向量搜索）進行了庫級代碼定位，但最近的研究集中在開發代理，以便將相關代碼定位為獨立的前驅或與實際工作交錯進行。大多數先前的代理代碼搜索方法為代理配備了複雜的專用工具，例如來自靜態分析的庫圖。本文中，我們展示了通過有效的強化學習配方，僅用標準Unix終端裝備的編碼代理可以被訓練以達到優異的結果。我們在三個基準（SWE-Bench Verified、Pro和Lite）上的實驗顯示，我們的模型在性能上始終優於或與2-18倍更大的基礎和後訓練LLM競爭，有時甚至接近像Claude Sonnet這樣的封閉模型提供的性能，即使在使用專用支架的情況下。我們的工作特別關注於重新利用現有編碼代理環境進行代碼搜索、獎勵設計和強化學習優化的技術。我們發布了結果模型系列CodeScout，以及我們的所有代碼和數據，供社區進一步構建。

##### **FailureMem: A Failure-Aware Multimodal Framework for Autonomous Software Repair**
2603.17826v1 by Ruize Ma, Yilei Jiang, Shilin Zhang, Zheng Ma, Yi Feng, Vincent Ng, Zhi Wang, Xiangyu Yue, Chuanyi Li, Lewei Lu

Multimodal Automated Program Repair (MAPR) extends traditional program repair by requiring models to jointly reason over source code, textual issue descriptions, and visual artifacts such as GUI screenshots. While recent LLM-based repair systems have shown promising results, existing approaches face several limitations: rigid workflow pipelines restrict exploration during debugging, visual reasoning is often performed over full-page screenshots without localized grounding, and failed repair attempts are rarely transformed into reusable knowledge. To address these challenges, we propose FailureMem, a multimodal repair framework that integrates three key mechanisms: a hybrid workflow-agent architecture that balances structured localization with flexible reasoning, active perception tools that enable region-level visual grounding, and a Failure Memory Bank that converts past repair attempts into reusable guidance. Experiments on SWE-bench Multimodal demonstrate FailureMem improves the resolved rate over GUIRepair by 3.7%.

摘要：多模態自動程式修復（MAPR）擴展了傳統的程式修復，要求模型共同推理源代碼、文本問題描述以及視覺物件，如 GUI 截圖。雖然最近基於 LLM 的修復系統顯示出有希望的結果，但現有的方法面臨幾個限制：僵化的工作流程管道限制了調試過程中的探索，視覺推理通常是在整個頁面的截圖上進行，而沒有局部的基礎，且失敗的修復嘗試很少轉化為可重用的知識。為了解決這些挑戰，我們提出了 FailureMem，一個多模態修復框架，整合了三個關鍵機制：一種混合工作流代理架構，平衡結構化定位與靈活推理，主動感知工具使得區域級別的視覺基礎成為可能，以及一個失敗記憶庫，將過去的修復嘗試轉化為可重用的指導。在 SWE-bench Multimodal 上的實驗表明，FailureMem 對 GUIRepair 的解決率提高了 3.7%。

##### **Intellectual Stewardship: Re-adapting Human Minds for Creative Knowledge Work in the Age of AI**
2603.18117v1 by Jianwei Zhang

Background: Amid the opportunities and risks introduced by generative AI, learning research needs to envision how human minds and responsibilities should re-adapt as AI continues to augment or automate various tasks.
  Approach: Drawing on theories of learning, intelligence, and knowledge creation, this conceptual paper proposes intellectual stewardship as a human-centered, conceptually grounded framework for advancing creative learning practices with AI.
  Key points: Students and teachers work as responsible governors of intellectual processes distributed across human and artificial systems, guided by five core principles. Being knowledge-wise involves understanding the evolving state of knowledge and taking purposeful actions to advance it. Being intelligence-wise emphasizes making informed choices about how to orchestrate distributed cognitive processes and resources. Being context-wise requires sensitivity to recognize opportunities and risks. Being ethics-wise foregrounds ethical judgment, responsibility, and care in the use of knowledge and intellectual power. Finally, self- and community-growing defines the overarching purpose, aligning intellectual work with personal development and the advancement of collective well-being.
  Contribution: The principles provide a lens for viewing the adaptation of human minds in AI-infused learning environments, calling for the development of meta-level dispositions and capabilities that characterize wisdom-oriented, socially responsible knowledge builders in the AI age.

摘要：背景：在生成性人工智慧帶來的機遇與風險中，學習研究需要設想隨著人工智慧持續增強或自動化各種任務，人類的思維和責任應如何重新調整。<lb/>  
方法：本概念性論文基於學習、智力和知識創造的理論，提出智識管理作為一個以人為中心、概念性扎根的框架，以推進與人工智慧的創造性學習實踐。<lb/>  
要點：學生和教師作為負責任的智識過程治理者，在人類和人工系統之間分配工作，並受到五個核心原則的指導。具備知識智慧意味著理解知識的演變狀態並採取有目的的行動來推進它。具備智力智慧強調在如何協調分散的認知過程和資源方面做出明智的選擇。具備情境智慧需要敏感地識別機會和風險。具備倫理智慧則強調在使用知識和智力權力時的倫理判斷、責任和關懷。最後，自我與社群成長定義了總體目的，將智識工作與個人發展和集體福祉的提升對齊。<lb/>  
貢獻：這些原則提供了一個視角，以觀察人類思維在人工智慧融入的學習環境中的適應，呼籲發展元層次的性格和能力，這些特徵是智慧導向的、社會負責的知識建構者在人工智慧時代的標誌。

##### **Facts as First Class Objects: Knowledge Objects for Persistent LLM Memory**
2603.17781v1 by Oliver Zahn, Simran Chana

Large language models increasingly serve as persistent knowledge workers, with in-context memory - facts stored in the prompt - as the default strategy. We benchmark in-context memory against Knowledge Objects (KOs), discrete hash-addressed tuples with O(1) retrieval. Within the context window, Claude Sonnet 4.5 achieves 100% exact-match accuracy from 10 to 7,000 facts (97.5% of its 200K window). However, production deployment reveals three failure modes: capacity limits (prompts overflow at 8,000 facts), compaction loss (summarization destroys 60% of facts), and goal drift (cascading compaction erodes 54% of project constraints while the model continues with full confidence). KOs achieve 100% accuracy across all conditions at 252x lower cost. On multi-hop reasoning, KOs reach 78.9% versus 31.6% for in-context. Cross-model replication across four frontier models confirms compaction loss is architectural, not model-specific. We additionally show that embedding retrieval fails on adversarial facts (20% precision at 1) and that neural memory (Titans) stores facts but fails to retrieve them on demand. We introduce density-adaptive retrieval as a switching mechanism and release the benchmark suite.

摘要：大型語言模型越來越多地作為持久的知識工作者，使用上下文記憶——存儲在提示中的事實——作為默認策略。<lb/>我們將上下文記憶與知識物件（KOs）進行基準測試，KOs 是具有 O(1) 檢索的離散哈希地址元組。<lb/>在上下文窗口內，Claude Sonnet 4.5 從 10 到 7,000 個事實實現了 100% 的精確匹配準確率（其 200K 窗口的 97.5%）。<lb/>然而，生產部署揭示了三種失效模式：容量限制（提示在 8,000 個事實時溢出）、壓縮損失（摘要摧毀了 60% 的事實）和目標漂移（級聯壓縮侵蝕了 54% 的項目約束，而模型仍然充滿信心地繼續）。<lb/>KOs 在所有條件下以 252 倍更低的成本達到 100% 的準確率。<lb/>在多跳推理中，KOs 達到 78.9%，而上下文記憶為 31.6%。<lb/>跨四個前沿模型的跨模型複製確認了壓縮損失是架構性的，而不是特定於模型的。<lb/>我們還顯示嵌入檢索在對抗性事實上失敗（在 1 時的精確度為 20%），並且神經記憶（Titans）儲存事實但未能按需檢索它們。<lb/>我們引入密度自適應檢索作為切換機制並發布基準套件。

##### **Modeling Overlapped Speech with Shuffles**
2603.17769v1 by Matthew Wiesner, Samuele Cornell, Alexander Polok, Lucas Ondel Yang, Lukáš Burget, Sanjeev Khudanpur

We propose to model parallel streams of data, such as overlapped speech, using shuffles. Specifically, this paper shows how the shuffle product and partial order finite-state automata (FSAs) can be used for alignment and speaker-attributed transcription of overlapped speech. We train using the total score on these FSAs as a loss function, marginalizing over all possible serializations of overlapping sequences at subword, word, and phrase levels. To reduce graph size, we impose temporal constraints by constructing partial order FSAs. We address speaker attribution by modeling (token, speaker) tuples directly. Viterbi alignment through the shuffle product FSA directly enables one-pass alignment. We evaluate performance on synthetic LibriSpeech overlaps. To our knowledge, this is the first algorithm that enables single-pass alignment of multi-talker recordings. All algorithms are implemented using k2 / Icefall.

摘要：我們提出使用洗牌來建模平行數據流，例如重疊的語音。具體而言，本文展示了如何使用洗牌乘積和部分有序有限狀態自動機（FSA）來對重疊語音進行對齊和說話者歸屬轉錄。我們使用這些 FSA 的總分作為損失函數，並在子詞、單詞和短語層面上對所有可能的重疊序列進行邊際化。為了減少圖形大小，我們通過構建部分有序 FSA 來施加時間約束。我們通過直接建模（標記、說話者）元組來解決說話者歸屬問題。通過洗牌乘積 FSA 的 Viterbi 對齊直接實現了一次性對齊。我們在合成的 LibriSpeech 重疊上評估性能。據我們所知，這是第一個能夠實現多講者錄音的一次性對齊的算法。所有算法均使用 k2 / Icefall 實現。

##### **Adaptive Guidance for Retrieval-Augmented Masked Diffusion Models**
2603.17677v1 by Jaemin Kim, Jong Chul Ye

Retrieval-Augmented Generation (RAG) improves factual grounding by incorporating external knowledge into language model generation. However, when retrieved context is noisy, unreliable, or inconsistent with the model's parametric knowledge, it introduces retrieval-prior conflicts that can degrade generation quality. While this problem has been studied in autoregressive language models, it remains largely unexplored in diffusion-based language models, where the iterative denoising process introduces unique challenges for integrating retrieved context. In this work, we propose Adaptive Retrieval-Augmented Masked Diffusion (ARAM), a training-free adaptive guidance framework for Masked Diffusion Models (MDMs) in RAG settings. ARAM dynamically calibrates the guidance scale during denoising according to the Signal-to-Noise Ratio (SNR) of the distributional shift induced by retrieved context. Intuitively, the model strengthens guidance when the retrieved context provides reliable corrective evidence and suppresses it when the contextual signal is noisy or non-supportive. Extensive experiments on multiple knowledge-intensive QA benchmarks show that ARAM improves overall QA performance over competitive RAG baselines.

摘要：檢索增強生成（RAG）通過將外部知識納入語言模型生成來改善事實基礎。然而，當檢索的上下文嘈雜、不可靠或與模型的參數知識不一致時，會引入檢索優先衝突，從而降低生成質量。雖然這個問題在自回歸語言模型中已經被研究，但在基於擴散的語言模型中仍然很大程度上未被探索，因為迭代去噪過程為整合檢索上下文帶來了獨特的挑戰。在這項工作中，我們提出了自適應檢索增強遮罩擴散（ARAM），這是一個無需訓練的自適應指導框架，適用於RAG設置中的遮罩擴散模型（MDMs）。ARAM根據檢索上下文引起的分佈變化的信噪比（SNR）在去噪過程中動態調整指導比例。直觀上，當檢索的上下文提供可靠的修正證據時，模型會加強指導，而當上下文信號嘈雜或不支持時則會抑制指導。在多個知識密集型問答基準上的大量實驗顯示，ARAM在整體問答性能上優於競爭性的RAG基準。

##### **Training-Only Heterogeneous Image-Patch-Text Graph Supervision for Advancing Few-Shot Learning Adapters**
2603.18101v1 by Mohammed Rahman Sherif Khan Mohammad, Ardhendu Behera, Sandip Pradhan, Swagat Kumar, Amr Ahmed

Recent adapter-based CLIP tuning (e.g., Tip-Adapter) is a strong few-shot learner, achieving efficiency by caching support features for fast prototype matching. However, these methods rely on global uni-modal feature vectors, overlooking fine-grained patch relations and their structural alignment with class text. To bridge this gap without incurring inference costs, we introduce a novel asymmetric training-only framework. Instead of altering the lightweight adapter, we construct a high-capacity auxiliary Heterogeneous Graph Teacher that operates solely during training. This teacher (i) integrates multi-scale visual patches and text prompts into a unified graph, (ii) performs deep cross-modal reasoning via a Modality-aware Graph Transformer (MGT), and (iii) applies discriminative node filtering to extract high-fidelity class features. Crucially, we employ a cache-aware dual-objective strategy to supervise this relational knowledge directly into the Tip-Adapter's key-value cache, effectively upgrading the prototypes while the graph teacher is discarded at test time. Thus, inference remains identical to Tip-Adapter with zero extra latency or memory. Across standard 1-16-shot benchmarks, our method consistently establishes a new state-of-the-art. Ablations confirm that the auxiliary graph supervision, text-guided reasoning, and node filtering are the essential ingredients for robust few-shot adaptation. Code is available at https://github.com/MR-Sherif/TOGA.git.

摘要：最近基於適配器的 CLIP 調整（例如，Tip-Adapter）是一種強大的少量學習者，通過緩存支持特徵來實現快速原型匹配的高效性。<lb/>然而，這些方法依賴於全局單模態特徵向量，忽視了細粒度的補丁關係及其與類別文本的結構對齊。<lb/>為了在不增加推理成本的情況下彌補這一差距，我們引入了一種新穎的非對稱訓練專用框架。<lb/>我們並不改變輕量級的適配器，而是構建了一個高容量的輔助異質圖教師，該教師僅在訓練期間運作。<lb/>這位教師 (i) 將多尺度的視覺補丁和文本提示整合到一個統一的圖中， (ii) 通過一個模態感知圖變換器 (MGT) 執行深度跨模態推理，並 (iii) 應用區分性節點過濾以提取高保真度的類別特徵。<lb/>至關重要的是，我們採用一種緩存感知的雙目標策略，將這種關係知識直接監督到 Tip-Adapter 的鍵值緩存中，有效地升級了原型，同時在測試時丟棄圖教師。<lb/>因此，推理與 Tip-Adapter 保持一致，沒有額外的延遲或內存。<lb/>在標準的 1-16 次拍攝基準中，我們的方法始終建立了新的最先進技術。<lb/>消融實驗證實，輔助圖監督、文本引導推理和節點過濾是穩健少量適應的必要成分。<lb/>代碼可在 https://github.com/MR-Sherif/TOGA.git 獲得。

##### **From Isolated Scoring to Collaborative Ranking: A Comparison-Native Framework for LLM-Based Paper Evaluation**
2603.17588v1 by Pujun Zheng, Jiacheng Yao, Jinquan Zheng, Chenyang Gu, Guoxiu He, Jiawei Liu, Yong Huang, Tianrui Guo, Wei Lu

Large language models (LLMs) are currently applied to scientific paper evaluation by assigning an absolute score to each paper independently. However, since score scales vary across conferences, time periods, and evaluation criteria, models trained on absolute scores are prone to fitting narrow, context-specific rules rather than developing robust scholarly judgment. To overcome this limitation, we propose shifting paper evaluation from isolated scoring to collaborative ranking. In particular, we design \textbf{C}omparison-\textbf{N}ative framework for \textbf{P}aper \textbf{E}valuation (\textbf{CNPE}), integrating comparison into both data construction and model learning. We first propose a graph-based similarity ranking algorithm to facilitate the sampling of more informative and discriminative paper pairs from a collection. We then enhance relative quality judgment through supervised fine-tuning and reinforcement learning with comparison-based rewards. At inference, the model performs pairwise comparisons over sampled paper pairs and aggregates these preference signals into a global relative quality ranking. Experimental results demonstrate that our framework achieves an average relative improvement of \textbf{21.8\%} over the strong baseline DeepReview-14B, while exhibiting robust generalization to five previously unseen datasets. \href{https://github.com/ECNU-Text-Computing/ComparisonReview}{Code}.

摘要：大型語言模型（LLMs）目前應用於科學論文評估，通過為每篇論文獨立分配絕對分數。然而，由於分數範圍在不同會議、時間段和評估標準之間有所不同，基於絕對分數訓練的模型容易適應狹窄的、特定上下文的規則，而不是發展出穩健的學術判斷。為了克服這一限制，我們建議將論文評估從孤立的打分轉變為協作排名。特別是，我們設計了\textbf{C}omparison-\textbf{N}ative框架，用於\textbf{P}aper \textbf{E}valuation（\textbf{CNPE}），將比較整合到數據構建和模型學習中。我們首先提出了一種基於圖的相似性排名算法，以促進從集合中抽取更具信息性和區分性的論文對。我們然後通過基於比較的獎勵進行監督微調和強化學習，增強相對質量判斷。在推理階段，模型對抽樣的論文對進行成對比較，並將這些偏好信號聚合成一個全局相對質量排名。實驗結果表明，我們的框架在強基準DeepReview-14B上實現了平均相對提升\textbf{21.8\%}，同時對五個之前未見過的數據集展現了穩健的泛化能力。 \href{https://github.com/ECNU-Text-Computing/ComparisonReview}{代碼}。

##### **KA2L: A Knowledge-Aware Active Learning Framework for LLMs**
2603.17566v1 by Haoxuan Yin, Bojian Liu, Chen Tang, Yangfan Wang, Lian Yan, Jingchi Jiang

Fine-tuning large language models (LLMs) with high-quality knowledge has been shown to enhance their performance effectively. However, there is a paucity of research on the depth of domain-specific knowledge comprehension by LLMs and the application of targeted active learning to improve their expertise. To address this gap, we introduce the Knowledge-Aware Active Learning (KA2L) framework. This framework assesses LLMs' mastery of specific knowledge points to aid in constructing unanswerable or unknowable questions through latent space analysis. This active learning strategy enhances training efficiency by focusing on knowledge the model has yet to master, thereby minimizing redundancy in learning already acquired information. This study innovatively employs a knowledge distribution probing technique to examine the hidden states of specific Transformer layers and identify the distribution of known and unknown knowledge within the LLM. Additionally, a hidden-state decoding method is proposed to generate numerous unknown questions in natural language from the latent knowledge space. In our experiments, we selected nine open-source LLMs to validate the effectiveness of the proposed framework. Results indicate that KA2L not only significantly reduces 50% annotation and computation costs across two open-domain and one vertical-domain dataset but also achieves better performance, offering valuable insights into active learning strategies for LLMs. The code is available at https://anonymous.4open.science/r/KA2L-F15C.

摘要：微調大型語言模型（LLMs）以高品質知識已被證明能有效提升其表現。然而，對於LLMs在特定領域知識理解的深度以及針對性主動學習應用以提升其專業知識的研究仍然稀缺。為了解決這一空白，我們引入了知識感知主動學習（KA2L）框架。該框架評估LLMs對特定知識點的掌握程度，以幫助通過潛在空間分析構建無法回答或未知的問題。這種主動學習策略通過專注於模型尚未掌握的知識來提高訓練效率，從而最小化對已獲得信息的學習冗餘。本研究創新性地採用了知識分佈探測技術，以檢查特定Transformer層的隱藏狀態，並識別LLM內已知和未知知識的分佈。此外，提出了一種隱藏狀態解碼方法，從潛在知識空間生成大量自然語言的未知問題。在我們的實驗中，我們選擇了九個開源LLMs來驗證所提出框架的有效性。結果表明，KA2L不僅在兩個開放領域和一個垂直領域數據集上顯著減少了50%的標註和計算成本，還實現了更好的性能，為LLMs的主動學習策略提供了有價值的見解。代碼可在 https://anonymous.4open.science/r/KA2L-F15C 獲得。

##### **Zipper-LoRA: Dynamic Parameter Decoupling for Speech-LLM based Multilingual Speech Recognition**
2603.17558v1 by Yuxiang Mei, Delai Qiu, Shengping Liu, Jiaen Liang, Yanhua Long

Speech Large Language Models (Speech-LLMs) have emerged as a powerful approach for automatic speech recognition (ASR) by aligning speech encoders with large language models. However, adapting these systems to multilingual settings with imbalanced data distributions remains challenging. In such scenarios, a stability-plasticity dilemma often arises: fully shared Parameter-Efficient Fine-Tuning (PEFT) can cause negative inter-lingual interference for under-represented languages, while fully language-specific tuning limits the cross-lingual beneficial knowledge transfer needed for low-resource tasks. To address this, we propose Zipper-LoRA, a novel rank-level decoupling framework with three variants (Static, Hard, and Soft) that dynamically synthesizes LoRA updates from shared and language-specific subspaces. By using a lightweight language-conditioned router, Zipper-LoRA dynamically controls the contribution of each subspace at the LoRA rank level, enabling fine-grained sharing where languages are compatible and strict decoupling when conflicts occur. To further stabilize optimization under imbalanced data, we propose a two-stage training strategy with an Initial-B warm start that significantly accelerates convergence. Experiments on a 12-language mixed-resource setting show that Zipper-LoRA consistently outperforms both fully shared and independent baselines, particularly in extremely low-resource scenarios. Moreover, we demonstrate that these gains are robust across both chunked and non-chunked encoder configurations, confirming the framework's reliability for practical, large-scale multilingual ASR. Our code and data will be available at https://github.com/YuCeong-May/Zipper-LoRA for reproducibility.

摘要：語音大型語言模型（Speech-LLMs）已成為自動語音識別（ASR）的強大方法，通過將語音編碼器與大型語言模型對齊。然而，將這些系統適應於數據分佈不平衡的多語言環境仍然具有挑戰性。在這種情況下，經常會出現穩定性-可塑性困境：完全共享的參數高效微調（PEFT）可能會對代表性不足的語言造成負面的跨語言干擾，而完全語言特定的調整則限制了低資源任務所需的跨語言有益知識轉移。為了解決這個問題，我們提出了Zipper-LoRA，一種新穎的等級解耦框架，具有三種變體（靜態、硬性和軟性），它動態合成來自共享和語言特定子空間的LoRA更新。通過使用輕量級的語言條件路由器，Zipper-LoRA在LoRA等級上動態控制每個子空間的貢獻，實現語言兼容時的細粒度共享，以及在發生衝突時的嚴格解耦。為了進一步穩定不平衡數據下的優化，我們提出了一種兩階段訓練策略，並採用了初始-B暖啟動，顯著加速收斂。在12語言混合資源環境下的實驗顯示，Zipper-LoRA始終優於完全共享和獨立基準，特別是在極低資源的場景中。此外，我們證明這些增益在分塊和非分塊編碼器配置中都是穩健的，確認該框架在實際大規模多語言ASR中的可靠性。我們的代碼和數據將在https://github.com/YuCeong-May/Zipper-LoRA上提供，以便重現。

##### **CLeAN: Continual Learning Adaptive Normalization in Dynamic Environments**
2603.17548v1 by Isabella Marasco, Davide Evangelista, Elena Loli Piccolomini, Michele Colajanni

Artificial intelligence systems predominantly rely on static data distributions, making them ineffective in dynamic real-world environments, such as cybersecurity, autonomous transportation, or finance, where data shifts frequently. Continual learning offers a potential solution by enabling models to learn from sequential data while retaining prior knowledge. However, a critical and underexplored issue in this domain is data normalization. Conventional normalization methods, such as min-max scaling, presuppose access to the entire dataset, which is incongruent with the sequential nature of continual learning. In this paper we introduce Continual Learning Adaptive Normalization (CLeAN), a novel adaptive normalization technique designed for continual learning in tabular data. CLeAN involves the estimation of global feature scales using learnable parameters that are updated via an Exponential Moving Average (EMA) module, enabling the model to adapt to evolving data distributions. Through comprehensive evaluations on two datasets and various continual learning strategies, including Resevoir Experience Replay, A-GEM, and EwC we demonstrate that CLeAN not only improves model performance on new data but also mitigates catastrophic forgetting. The findings underscore the importance of adaptive normalization in enhancing the stability and effectiveness of tabular data, offering a novel perspective on the use of normalization to preserve knowledge in dynamic learning environments.

摘要：人工智慧系統主要依賴靜態數據分佈，使其在動態的現實環境中（例如網絡安全、自主運輸或金融）無法有效運作，因為這些環境中的數據經常變動。持續學習提供了一個潛在的解決方案，通過使模型能夠從序列數據中學習，同時保留先前的知識。然而，在這個領域中，一個關鍵且未被充分探討的問題是數據正規化。傳統的正規化方法，如最小-最大縮放，假設可以訪問整個數據集，這與持續學習的序列特性不相符。在本文中，我們介紹了持續學習自適應正規化（CLeAN），這是一種專為表格數據中的持續學習設計的新型自適應正規化技術。CLeAN 涉及使用可學習參數來估計全局特徵尺度，這些參數通過指數移動平均（EMA）模塊進行更新，使模型能夠適應不斷變化的數據分佈。通過對兩個數據集和各種持續學習策略（包括儲存經驗重播、A-GEM 和 EwC）進行全面評估，我們展示了 CLeAN 不僅提高了模型在新數據上的性能，還減輕了災難性遺忘。研究結果強調了自適應正規化在增強表格數據的穩定性和有效性方面的重要性，並為在動態學習環境中使用正規化以保留知識提供了一種新視角。

##### **Language on Demand, Knowledge at Core: Composing LLMs with Encoder-Decoder Translation Models for Extensible Multilinguality**
2603.17512v1 by Mengyu Bu, Yang Feng

Large language models (LLMs) exhibit strong general intelligence, yet their multilingual performance remains highly imbalanced. Although LLMs encode substantial cross-lingual knowledge in a unified semantic space, they often struggle to reliably interface this knowledge with low-resource or unseen languages. Fortunately, pretrained encoder-decoder translation models already possess balanced multilingual capability, suggesting a natural complement to LLMs. In this work, we propose XBridge, a compositional encoder-LLM-decoder architecture that offloads multilingual understanding and generation to external pretrained translation models, while preserving the LLM as an English-centric core for general knowledge processing. To address the resulting representation misalignment across models, we introduce lightweight cross-model mapping layers and an optimal transport-based alignment objective, enabling fine-grained semantic consistency for multilingual generation. Experiments on four LLMs across multilingual understanding, reasoning, summarization, and generation indicate that XBridge outperforms strong baselines, especially on low-resource and previously unseen languages, without retraining the LLM.

摘要：大型語言模型（LLMs）展現出強大的通用智慧，但它們的多語言表現仍然高度不平衡。儘管LLMs在統一的語義空間中編碼了大量的跨語言知識，但它們在與低資源或未見語言可靠地對接這些知識時經常遇到困難。幸運的是，預訓練的編碼器-解碼器翻譯模型已經具備平衡的多語言能力，這表明它們可以自然地補充LLMs。在本研究中，我們提出了XBridge，一種組合的編碼器-LLM-解碼器架構，將多語言理解和生成的任務轉移到外部預訓練的翻譯模型，同時保留LLM作為以英語為中心的核心，用於一般知識處理。為了解決模型之間的表示不對齊問題，我們引入了輕量級的跨模型映射層和基於最佳運輸的對齊目標，從而實現多語言生成的細粒度語義一致性。在四個LLMs上進行的多語言理解、推理、總結和生成的實驗表明，XBridge在強基準之上表現出色，特別是在低資源和之前未見的語言上，且無需重新訓練LLM。

##### **Inducing Epistemological Humility in Large Language Models: A Targeted SFT Approach to Reducing Hallucination**
2603.17504v1 by Cem Uluoglakci, Tugba Taskaya Temizel

Large language models (LLMs) often hallucinate, producing fluent but false information, partly because supervised fine-tuning (SFT) implicitly rewards always responding. We introduce $\textit{HypoTermInstruct}$, an SFT dataset (31,487 responses for 11,151 questions) designed to teach models epistemological humility-the ability to recognize the limits of their own knowledge and admit uncertainty. This is achieved through questions about non-existent "hypothetical" terms. We also release $\textit{HypoTermQA-Enhanced}$, a benchmark for hallucination tendency strengthened through multiple validations. We conducted 800 controlled LoRA SFT runs across $\textit{Llama3.1-8B}$ and $\textit{Gemma3-4B}$ (base and instruct), testing 100 fine-tuning configurations with paired controls. Our results demonstrate that replacing generic instruction data with $\textit{HypoTermInstruct}$ significantly improves the HypoTerm Score (median increases of 0.19% to 25.91%) and FactScore (+0.39% to +0.86%), while maintaining stable performance on MMLU (minimal decreases of 0.26% to 0.35%). Our work demonstrates that targeted, high-quality SFT data teaching meta-cognitive skills can effectively reduce hallucination without preference/RL pipelines, providing mechanistic insights and a practical path toward more reliable AI systems.

摘要：大型語言模型（LLMs）經常會出現幻覺，產生流暢但虛假的資訊，部分原因是監督式微調（SFT）隱含地獎勵總是回應。我們介紹了 $\textit{HypoTermInstruct}$，這是一個 SFT 數據集（31,487 個回應對應 11,151 個問題），旨在教導模型認識認識論的謙遜——識別自身知識的局限性並承認不確定性。這是通過關於不存在的「假設」術語的問題來實現的。我們還發布了 $\textit{HypoTermQA-Enhanced}$，這是一個通過多次驗證強化的幻覺傾向基準。我們在 $\textit{Llama3.1-8B}$ 和 $\textit{Gemma3-4B}$（基礎和指令）上進行了 800 次控制的 LoRA SFT 執行，測試了 100 種微調配置及其配對控制。我們的結果顯示，用 $\textit{HypoTermInstruct}$ 替換通用指令數據顯著提高了 HypoTerm 分數（中位數增加 0.19% 到 25.91%）和 FactScore（+0.39% 到 +0.86%），同時在 MMLU 上保持穩定的表現（最小下降 0.26% 到 0.35%）。我們的工作表明，針對性的高質量 SFT 數據教導元認知技能可以有效減少幻覺，而無需偏好/RL 管道，提供了機制上的見解和通向更可靠 AI 系統的實用途徑。

##### **Revisiting Cross-Attention Mechanisms: Leveraging Beneficial Noise for Domain-Adaptive Learning**
2603.17474v1 by Zelin Zang, Yehui Yang, Fei Wang, Liangyu Li, Baigui Sun

Unsupervised Domain Adaptation (UDA) seeks to transfer knowledge from a labeled source domain to an unlabeled target domain but often suffers from severe domain and scale gaps that degrade performance. Existing cross-attention-based transformers can align features across domains, yet they struggle to preserve content semantics under large appearance and scale variations. To explicitly address these challenges, we introduce the concept of beneficial noise, which regularizes cross-attention by injecting controlled perturbations, encouraging the model to ignore style distractions and focus on content. We propose the Domain-Adaptive Cross-Scale Matching (DACSM) framework, which consists of a Domain-Adaptive Transformer (DAT) for disentangling domain-shared content from domain-specific style, and a Cross-Scale Matching (CSM) module that adaptively aligns features across multiple resolutions. DAT incorporates beneficial noise into cross-attention, enabling progressive domain translation with enhanced robustness, yielding content-consistent and style-invariant representations. Meanwhile, CSM ensures semantic consistency under scale changes. Extensive experiments on VisDA-2017, Office-Home, and DomainNet demonstrate that DACSM achieves state-of-the-art performance, with up to +2.3% improvement over CDTrans on VisDA-2017. Notably, DACSM achieves a +5.9% gain on the challenging "truck" class of VisDA, evidencing the strength of beneficial noise in handling scale discrepancies. These results highlight the effectiveness of combining domain translation, beneficial-noise-enhanced attention, and scale-aware alignment for robust cross-domain representation learning.

摘要：無監督領域適應（UDA）旨在將知識從標記的源領域轉移到未標記的目標領域，但通常會受到嚴重的領域和規模差距的影響，從而降低性能。現有的基於交叉注意力的Transformer可以對齊跨領域的特徵，但在面對大幅外觀和規模變化時，它們難以保持內容語義。為了明確解決這些挑戰，我們引入了有益噪聲的概念，通過注入受控擾動來正則化交叉注意力，鼓勵模型忽略風格干擾，專注於內容。我們提出了領域自適應跨尺度匹配（DACSM）框架，其中包含一個領域自適應Transformer（DAT），用於將領域共享的內容與領域特定的風格解耦，以及一個跨尺度匹配（CSM）模塊，該模塊自適應地對齊多個解析度的特徵。DAT將有益噪聲納入交叉注意力中，使得在增強魯棒性的情況下進行漸進式領域翻譯，產生內容一致且風格不變的表示。同時，CSM確保在規模變化下的語義一致性。在VisDA-2017、Office-Home和DomainNet上的大量實驗表明，DACSM實現了最先進的性能，在VisDA-2017上比CDTrans提高了高達+2.3%。值得注意的是，DACSM在VisDA的挑戰性“卡車”類別上獲得了+5.9%的增益，證明了有益噪聲在處理規模差異方面的優勢。這些結果突顯了結合領域翻譯、有益噪聲增強的注意力和規模感知對齊在穩健的跨領域表示學習中的有效性。

##### **When Only the Final Text Survives: Implicit Execution Tracing for Multi-Agent Attribution**
2603.17445v2 by Yi Nian, Haosen Cao, Shenzhe Zhu, Henry Peng Zou, Qingqing Luan, Yue Zhao

When a multi-agent system produces an incorrect or harmful answer, who is accountable if execution logs and agent identifiers are unavailable? Multi-agent language systems increasingly rely on structured interactions such as delegation and iterative refinement, yet the final output often obscures the underlying interaction topology and agent contributions. We introduce IET (Implicit Execution Tracing), a metadata-independent framework that enables token-level attribution directly from generated text and a simple mechanism for interaction topology reconstruction. During generation, agent-specific keyed signals are embedded into the token distribution, transforming the text into a self-describing execution trace detectable only with a secret key. At detection time, a transition-aware scoring method identifies agent handover points and reconstructs the interaction graph. Experiments show that IET recovers agent segments and coordination structure with high accuracy while preserving generation quality, enabling privacy-preserving auditing for multi-agent language systems.

摘要：當多代理系統產生不正確或有害的答案時，如果執行日誌和代理識別碼不可用，誰應該負責？多代理語言系統越來越依賴於結構化互動，例如授權和迭代精煉，然而最終的輸出往往掩蓋了底層的互動拓撲和代理貢獻。我們介紹了 IET（隱式執行追蹤），這是一個與元數據無關的框架，能夠直接從生成的文本中實現令牌級的歸因，並提供一種簡單的互動拓撲重建機制。在生成過程中，特定於代理的鍵控信號被嵌入到令牌分佈中，將文本轉換為僅能通過秘密密鑰檢測的自描述執行追蹤。在檢測時，基於轉換的評分方法識別代理交接點並重建互動圖。實驗顯示，IET 能夠高精度地恢復代理段和協調結構，同時保持生成質量，實現多代理語言系統的隱私保護審計。

##### **Proactive Knowledge Inquiry in Doctor-Patient Dialogue: Stateful Extraction, Belief Updating, and Path-Aware Action Planning**
2603.17425v1 by Zhenhai Pan, Yan Liu, Jia You

Most automated electronic medical record (EMR) pipelines remain output-oriented: they transcribe, extract, and summarize after the consultation, but they do not explicitly model what is already known, what is still missing, which uncertainty matters most, or what question or recommendation should come next. We formulate doctor-patient dialogue as a proactive knowledge-inquiry problem under partial observability. The proposed framework combines stateful extraction, sequential belief updating, gap-aware state modeling, hybrid retrieval over objectified medical knowledge, and a POMDP-lite action planner. Instead of treating the EMR as the only target artifact, the framework treats documentation as the structured projection of an ongoing inquiry loop. To make the formulation concrete, we report a controlled pilot evaluation on ten standardized multi-turn dialogues together with a 300-query retrieval benchmark aggregated across dialogues. On this pilot protocol, the full framework reaches 83.3% coverage, 80.0% risk recall, 81.4% structural completeness, and lower redundancy than the chunk-only and template-heavy interactive baselines. These pilot results do not establish clinical generalization; rather, they suggest that proactive inquiry may be methodologically interesting under tightly controlled conditions and can be viewed as a conceptually appealing formulation worth further investigation for dialogue-based EMR generation. This work should be read as a pilot concept demonstration under a controlled simulated setting rather than as evidence of clinical deployment readiness. No implication of clinical deployment readiness, clinical safety, or real-world clinical utility should be inferred from this pilot protocol.

摘要：大多數自動化電子病歷 (EMR) 管道仍然以輸出為導向：它們在諮詢後進行轉錄、提取和總結，但並未明確建模已知的內容、仍缺失的內容、最重要的不確定性，或下一步應該提出的問題或建議。我們將醫生-病人對話定義為在部分可觀察性下的主動知識探詢問題。所提出的框架結合了有狀態的提取、序列信念更新、關注差距的狀態建模、對物化醫療知識的混合檢索，以及一個簡化的 POMDP 行動規劃器。該框架並不將 EMR 視為唯一的目標產物，而是將文檔視為持續探詢循環的結構化投影。為了使這一表述具體化，我們報告了對十個標準化的多輪對話進行的受控初步評估，並提供了一個跨對話聚合的 300 查詢檢索基準。在這一初步協議中，完整框架達到了 83.3% 的覆蓋率、80.0% 的風險召回率、81.4% 的結構完整性，且比僅使用片段和模板重的互動基準具有更低的冗餘性。這些初步結果並未建立臨床泛化；相反，它們表明在嚴格控制的條件下，主動探詢可能在方法論上具有趣味，並且可以被視為一種概念上吸引人的表述，值得進一步調查以用於基於對話的 EMR 生成。這項工作應被視為在受控模擬環境下的初步概念示範，而不是臨床部署準備的證據。從這一初步協議中不應推斷出臨床部署準備、臨床安全或現實世界臨床效用的任何暗示。

##### **Efficient Exploration at Scale**
2603.17378v1 by Seyed Mohammad Asghari, Chris Chute, Vikranth Dwaracherla, Xiuyuan Lu, Mehdi Jafarnia, Victor Minden, Zheng Wen, Benjamin Van Roy

We develop an online learning algorithm that dramatically improves the data efficiency of reinforcement learning from human feedback (RLHF). Our algorithm incrementally updates reward and language models as choice data is received. The reward model is fit to the choice data, while the language model is updated by a variation of reinforce, with reinforcement signals provided by the reward model. Several features enable the efficiency gains: a small affirmative nudge added to each reinforcement signal, an epistemic neural network that models reward uncertainty, and information-directed exploration. With Gemma large language models (LLMs), our algorithm matches the performance of offline RLHF trained on 200K labels using fewer than 20K labels, representing more than a 10x gain in data efficiency. Extrapolating from our results, we expect our algorithm trained on 1M labels to match offline RLHF trained on 1B labels. This represents a 1,000x gain. To our knowledge, these are the first results to demonstrate that such large improvements are possible.

摘要：我們開發了一種在線學習算法，顯著提高了來自人類反饋的強化學習（RLHF）的數據效率。<lb/>我們的算法在接收到選擇數據時，逐步更新獎勵模型和語言模型。<lb/>獎勵模型是根據選擇數據進行擬合，而語言模型則通過一種變體的增強進行更新，增強信號由獎勵模型提供。<lb/>幾個特徵使得效率提升成為可能：對每個增強信號添加的小幅肯定推動、一個建模獎勵不確定性的認知神經網絡，以及信息導向的探索。<lb/>使用Gemma大型語言模型（LLMs），我們的算法在使用少於20K標籤的情況下，達到了在200K標籤上訓練的離線RLHF的性能，這代表著超過10倍的數據效率提升。<lb/>根據我們的結果推斷，我們預期在1M標籤上訓練的算法將匹配在1B標籤上訓練的離線RLHF。<lb/>這代表著1,000倍的增益。<lb/>據我們所知，這些是首次證明如此大幅度提升是可能的結果。

##### **Continually self-improving AI**
2603.18073v1 by Zitong Yang

Modern language model-based AI systems are remarkably powerful, yet their capabilities remain fundamentally capped by their human creators in three key ways. First, although a model's weights can be updated via fine-tuning, acquiring new knowledge from small, specialized corpora after pretraining remains highly data-inefficient. Second, the training of these systems relies heavily on finite, human-generated data from across history. Third, the pipelines used to train AI models are confined by the algorithms that human researchers can discover and explore. This thesis takes a small step toward overcoming these inherent limitations, presenting three chapters aimed at breaking these dependencies to create continually self-improving AI. First, to overcome this data-efficiency barrier in knowledge acquisition, we propose a synthetic data approach that diversifies and amplifies small corpora into rich knowledge representations, enabling a model to effectively update its parameters from limited source material. Second, to reduce reliance on human data, we show that given a fixed amount of such data, the model can self-generate synthetic data to bootstrap its fundamental pretraining capabilities without distillation from any off-the-shelf, instruction-tuned LM. Finally, to transcend human-engineered training paradigms, we demonstrate that by scaling search during test time over the space of algorithms, AI can search over a larger space of learning algorithm configurations than human researchers can explore manually.

摘要：現代基於語言模型的人工智慧系統非常強大，但其能力在三個關鍵方面仍然受到其人類創造者的基本限制。首先，儘管模型的權重可以通過微調進行更新，但在預訓練後從小型專門語料庫獲取新知識仍然非常低效。第二，這些系統的訓練在很大程度上依賴於來自歷史的有限人類生成數據。第三，用於訓練人工智慧模型的流程受到人類研究者能夠發現和探索的算法的限制。本論文邁出了一小步，旨在克服這些固有的限制，提出三個章節，旨在打破這些依賴，以創造持續自我改進的人工智慧。首先，為了克服知識獲取中的數據效率障礙，我們提出了一種合成數據的方法，將小型語料庫多樣化並擴展為豐富的知識表示，使模型能夠有效地從有限的來源材料中更新其參數。第二，為了減少對人類數據的依賴，我們展示了在給定固定數量的此類數據的情況下，模型可以自我生成合成數據，以啟動其基本的預訓練能力，而無需從任何現成的、經過指令調整的語言模型中進行蒸餾。最後，為了超越人類設計的訓練範式，我們演示了通過在測試期間擴大算法空間的搜索，人工智慧可以搜索比人類研究者手動探索的學習算法配置更大的空間。

##### **PACE-RAG: Patient-Aware Contextual and Evidence-based Policy RAG for Clinical Drug Recommendation**
2603.17356v1 by Chaeyoung Huh, Hyunmin Hwang, Jung Hwan Shin, Jinse Park, Jong Chul Ye

Drug recommendation requires a deep understanding of individual patient context, especially for complex conditions like Parkinson's disease. While LLMs possess broad medical knowledge, they fail to capture the subtle nuances of actual prescribing patterns. Existing RAG methods also struggle with these complexities because guideline-based retrieval remains too generic and similar-patient retrieval often replicates majority patterns without accounting for the unique clinical nuances of individual patients. To bridge this gap, we propose PACE-RAG (Patient-Aware Contextual and Evidence-based Policy RAG), a novel framework designed to synthesize individual patient context with the prescribing tendencies of similar cases. By analyzing treatment patterns tailored to specific clinical signals, PACE-RAG identifies optimal prescriptions and generates an explainable clinical summary. Evaluated on a Parkinson's cohort and the MIMIC-IV benchmark using Llama-3.1-8B and Qwen3-8B, PACE-RAG achieved state-of-the-art performance, reaching F1 scores of 80.84% and 47.22%, respectively. These results validate PACE-RAG as a robust, clinically grounded solution for personalized decision support. Our code is available at: https://github.com/ChaeYoungHuh/PACE-RAG.

摘要：藥物推薦需要深入了解個別病人的背景，特別是對於像帕金森病這樣的複雜病症。雖然大型語言模型（LLMs）擁有廣泛的醫學知識，但它們無法捕捉實際處方模式的微妙差異。現有的檢索增強生成（RAG）方法在這些複雜性上也面臨挑戰，因為基於指導方針的檢索仍然過於通用，而類似病人的檢索往往重複大多數模式，卻未考慮到個別病人的獨特臨床細微差異。為了彌補這一空白，我們提出了PACE-RAG（以病人為中心的情境和基於證據的政策RAG），這是一個新穎的框架，旨在將個別病人的背景與類似案例的處方趨勢進行綜合。通過分析針對特定臨床信號的治療模式，PACE-RAG識別最佳處方並生成可解釋的臨床摘要。在帕金森病患者群體和MIMIC-IV基準上使用Llama-3.1-8B和Qwen3-8B進行評估，PACE-RAG達到了最先進的表現，分別達到80.84%和47.22%的F1分數。這些結果驗證了PACE-RAG作為個性化決策支持的穩健、臨床基礎解決方案。我們的代碼可在以下網址獲得：https://github.com/ChaeYoungHuh/PACE-RAG。

##### **A Progressive Visual-Logic-Aligned Framework for Ride-Hailing Adjudication**
2603.17328v1 by Weiming Wu, Zi-Jian Cheng, Jie Meng, Peng Zhen, Shan Huang, Qun Li, Guobin Wu, Lan-Zhe Guo

The efficient adjudication of responsibility disputes is pivotal for maintaining marketplace fairness. However, the exponential surge in ride-hailing volume renders manual review intractable, while conventional automated methods lack the reasoning transparency required for quasi-judicial decisions. Although Multimodal LLMs offer a promising paradigm, they fundamentally struggle to bridge the gap between general visual semantics and rigorous evidentiary protocols, often leading to perceptual hallucinations and logical looseness. To address these systemic misalignments, we introduce RideJudge, a Progressive Visual-Logic-Aligned Framework. Instead of relying on generic pre-training, we bridge the semantic gap via SynTraj, a synthesis engine that grounds abstract liability concepts into concrete trajectory patterns. To resolve the conflict between massive regulation volume and limited context windows, we propose an Adaptive Context Optimization strategy that distills expert knowledge, coupled with a Chain-of-Adjudication mechanism to enforce active evidentiary inquiry. Furthermore, addressing the inadequacy of sparse binary feedback for complex liability assessment, we implement a novel Ordinal-Sensitive Reinforcement Learning mechanism that calibrates decision boundaries against hierarchical severity. Extensive experiments show that our RideJudge-8B achieves 88.41\% accuracy, surpassing 32B-scale baselines and establishing a new standard for interpretable adjudication.

摘要：有效的責任爭議裁決對於維護市場公平至關重要。<lb/>然而，搭乘叫車服務的需求呈指數增長，使得人工審核變得無法處理，而傳統的自動化方法則缺乏進行準司法決策所需的推理透明度。<lb/>儘管多模態大型語言模型提供了一個有前景的範式，但它們在將一般視覺語義與嚴格的證據協議之間架起橋樑方面根本上存在困難，經常導致感知幻覺和邏輯鬆散。<lb/>為了解決這些系統性的錯位，我們推出了RideJudge，一個漸進式視覺邏輯對齊框架。<lb/>我們不再依賴通用的預訓練，而是通過SynTraj，一個將抽象責任概念具體化為具體軌跡模式的合成引擎來彌補語義差距。<lb/>為了解決龐大規範量與有限上下文窗口之間的衝突，我們提出了一種自適應上下文優化策略，提煉專家知識，並結合一個裁決鏈機制以強化主動的證據調查。<lb/>此外，針對稀疏二元反饋在複雜責任評估中的不足，我們實施了一種新穎的有序敏感增強學習機制，根據層級嚴重性來校準決策邊界。<lb/>大量實驗表明，我們的RideJudge-8B達到了88.41\%的準確率，超越了32B規模的基準，並為可解釋的裁決樹立了新標準。

##### **S3T-Former: A Purely Spike-Driven State-Space Topology Transformer for Skeleton Action Recognition**
2603.18062v1 by Naichuan Zheng, Hailun Xia, Zepeng Sun, Weiyi Li, Yujia Wang

Skeleton-based action recognition is crucial for multimedia applications but heavily relies on power-hungry Artificial Neural Networks (ANNs), limiting their deployment on resource-constrained edge devices. Spiking Neural Networks (SNNs) provide an energy-efficient alternative; however, existing spiking models for skeleton data often compromise the intrinsic sparsity of SNNs by resorting to dense matrix aggregations, heavy multimodal fusion modules, or non-sparse frequency domain transformations. Furthermore, they severely suffer from the short-term amnesia of spiking neurons. In this paper, we propose the Spiking State-Space Topology Transformer (S3T-Former), which, to the best of our knowledge, is the first purely spike-driven Transformer architecture specifically designed for energy-efficient skeleton action recognition. Rather than relying on heavy fusion overhead, we formulate a Multi-Stream Anatomical Spiking Embedding (M-ASE) that acts as a generalized kinematic differential operator, elegantly transforming multimodal skeleton features into heterogeneous, highly sparse event streams. To achieve true topological and temporal sparsity, we introduce Lateral Spiking Topology Routing (LSTR) for on-demand conditional spike propagation, and a Spiking State-Space (S3) Engine to systematically capture long-range temporal dynamics without non-sparse spectral workarounds. Extensive experiments on multiple large-scale datasets demonstrate that S3T-Former achieves highly competitive accuracy while theoretically reducing energy consumption compared to classic ANNs, establishing a new state-of-the-art for energy-efficient neuromorphic action recognition.

摘要：骨架基礎的動作識別對於多媒體應用至關重要，但嚴重依賴耗電的人工神經網絡（ANNs），限制了它們在資源受限的邊緣設備上的部署。脈衝神經網絡（SNNs）提供了一種節能的替代方案；然而，現有的針對骨架數據的脈衝模型往往通過依賴密集的矩陣聚合、繁重的多模態融合模塊或非稀疏的頻域轉換來妥協SNNs的內在稀疏性。此外，它們嚴重受到脈衝神經元的短期失憶症的影響。在本文中，我們提出了脈衝狀態空間拓撲變換器（S3T-Former），據我們所知，這是首個專門為節能骨架動作識別設計的純脈衝驅動變換器架構。我們不依賴繁重的融合開銷，而是制定了一個多流解剖脈衝嵌入（M-ASE），作為一個通用的運動學微分運算符，優雅地將多模態骨架特徵轉換為異質的、高度稀疏的事件流。為了實現真正的拓撲和時間稀疏性，我們引入了側向脈衝拓撲路由（LSTR）以實現按需條件脈衝傳播，以及一個脈衝狀態空間（S3）引擎，系統性地捕捉長距離的時間動態，而不使用非稀疏的頻譜變通方法。在多個大規模數據集上的廣泛實驗表明，S3T-Former在理論上減少了能量消耗的同時，實現了高度競爭的準確性，為節能的神經形態動作識別建立了新的最先進技術。

##### **Graph-Native Cognitive Memory for AI Agents: Formal Belief Revision Semantics for Versioned Memory Architectures**
2603.17244v1 by Young Bin Park

While individual components for AI agent memory exist in prior systems, their architectural synthesis and formal grounding remain underexplored. We present Kumiho, a graph-native cognitive memory architecture grounded in formal belief revision semantics. The structural primitives required for cognitive memory -- immutable revisions, mutable tag pointers, typed dependency edges, URI-based addressing -- are identical to those required for managing agent-produced work as versionable assets, enabling a unified graph-native architecture that serves both purposes. The central formal contribution is a correspondence between the AGM belief revision framework and the operational semantics of a property graph memory system, proving satisfaction of the basic AGM postulates (K*2--K*6) and Hansson's belief base postulates (Relevance, Core-Retainment). The architecture implements a dual-store model (Redis working memory, Neo4j long-term graph) with hybrid fulltext and vector retrieval. On LoCoMo (token-level F1), Kumiho achieves 0.565 overall F1 (n=1,986) including 97.5% adversarial refusal accuracy. On LoCoMo-Plus, a Level-2 cognitive memory benchmark testing implicit constraint recall, Kumiho achieves 93.3% judge accuracy (n=401); independent reproduction by the benchmark authors yielded results in the mid-80% range, still substantially outperforming all published baselines (best: Gemini 2.5 Pro, 45.7%). Three architectural innovations drive the results: prospective indexing (LLM-generated future-scenario implications indexed at write time), event extraction (structured causal events preserved in summaries), and client-side LLM reranking. The architecture is model-decoupled: switching the answer model from GPT-4o-mini (~88%) to GPT-4o (93.3%) improves end-to-end accuracy without pipeline changes, at a total evaluation cost of ~$14 for 401 entries.

摘要：雖然先前系統中存在 AI 代理記憶的個別組件，但其架構綜合與正式基礎仍未被充分探索。我們提出 Kumiho，一種基於圖形的認知記憶架構，基於正式的信念修訂語義。認知記憶所需的結構原始元件——不可變修訂、可變標籤指針、類型依賴邊、基於 URI 的尋址——與管理代理生成的作品作為可版本化資產所需的元件相同，使得這種統一的圖形原生架構能夠同時滿足這兩個目的。主要的正式貢獻是 AGM 信念修訂框架與屬性圖記憶系統的操作語義之間的對應，證明了基本 AGM 公設（K*2--K*6）和 Hansson 的信念基礎公設（相關性、核心保留）的滿足。該架構實現了一個雙存儲模型（Redis 工作記憶，Neo4j 長期圖形）並具有混合全文和向量檢索功能。在 LoCoMo（標記級 F1）上，Kumiho 總體 F1 達到 0.565（n=1,986），包括 97.5% 的對抗拒絕準確率。在測試隱含約束回憶的 Level-2 認知記憶基準 LoCoMo-Plus 上，Kumiho 達到 93.3% 的評判準確率（n=401）；基準作者的獨立重現結果在 80% 中期範圍內，仍然顯著超越所有已發表的基準（最佳：Gemini 2.5 Pro，45.7%）。三項架構創新推動了結果：前瞻性索引（在寫入時索引 LLM 生成的未來情景影響）、事件提取（在摘要中保留結構化因果事件）以及客戶端 LLM 重新排序。該架構是模型解耦的：將答案模型從 GPT-4o-mini（約 88%）切換到 GPT-4（93.3%）提高了端到端準確性，而無需管道變更，總評估成本約為 401 條目 ~$14。

##### **Neuron-Level Emotion Control in Speech-Generative Large Audio-Language Models**
2603.17231v1 by Xiutian Zhao, Ismail Rasim Ulgen, Philipp Koehn, Björn Schuller, Berrak Sisman

Large audio-language models (LALMs) can produce expressive speech, yet reliable emotion control remains elusive: conversions often miss the target affect and may degrade linguistic fidelity through refusals, hallucinations, or paraphrase. We present, to our knowledge, the first neuron-level study of emotion control in speech-generative LALMs and demonstrate that compact emotion-sensitive neurons (ESNs) are causally actionable, enabling training-free emotion steering at inference time. ESNs are identified via success-filtered activation aggregation enforcing both emotion realization and content preservation. Across three LALMs (Qwen2.5-Omni-7B, MiniCPM-o 4.5, Kimi-Audio), ESN interventions yield emotion-specific gains that generalize to unseen speakers and are supported by automatic and human evaluation. Controllability depends on selector design, mask sparsity, filtering, and intervention strength. Our results establish a mechanistic framework for training-free emotion control in speech generation.

摘要：大型音頻語言模型（LALMs）能夠產生富有表現力的語音，但可靠的情感控制仍然難以實現：轉換往往未能達到目標情感，並可能通過拒絕、幻想或改述來降低語言的真實性。我們提出了迄今為止首個針對語音生成LALMs的神經元級情感控制研究，並展示了緊湊的情感敏感神經元（ESNs）是可因果操作的，從而實現了在推理時無需訓練的情感引導。ESNs通過成功過濾的激活聚合來識別，強調情感實現和內容保留。在三個LALMs（Qwen2.5-Omni-7B、MiniCPM-o 4.5、Kimi-Audio）中，ESN干預產生了針對情感的特定增益，這些增益能夠推廣到未見過的說話者，並得到了自動和人工評估的支持。可控性取決於選擇器設計、掩碼稀疏性、過濾和干預強度。我們的結果建立了一個無需訓練的語音生成情感控制的機制框架。

##### **Abstraction as a Memory-Efficient Inductive Bias for Continual Learning**
2603.17198v1 by Elnaz Rahmati, Nona Ghazizadeh, Zhivar Sourati, Nina Rouhani, Morteza Dehghani

The real world is non-stationary and infinitely complex, requiring intelligent agents to learn continually without the prohibitive cost of retraining from scratch. While online continual learning offers a framework for this setting, learning new information often interferes with previously acquired knowledge, causes forgetting and degraded generalization. To address this, we propose Abstraction-Augmented Training (AAT), a loss-level modification encouraging models to capture the latent relational structure shared across examples. By jointly optimizing over concrete instances and their abstract representations, AAT introduces a memory-efficient inductive bias that stabilizes learning in strictly online data streams, eliminating the need for a replay buffer. To capture the multi-faceted nature of abstraction, we introduce and evaluate AAT on two benchmarks: a controlled relational dataset where abstraction is realized through entity masking, and a narrative dataset where abstraction is expressed through shared proverbs. Our results show that AAT achieves performance comparable to or exceeding strong experience replay (ER) baselines, despite requiring zero additional memory and only minimal changes to the training objective. This work highlights structural abstraction as a powerful, memory-free alternative to ER.

摘要：現實世界是非穩定且無限複雜的，要求智能代理不斷學習，而不需承擔從頭開始重新訓練的高昂成本。雖然在線持續學習為這種情境提供了一個框架，但學習新信息往往會干擾先前獲得的知識，導致遺忘和泛化能力下降。為了解決這個問題，我們提出了抽象增強訓練（AAT），這是一種損失級別的修改，鼓勵模型捕捉跨範例共享的潛在關係結構。通過對具體實例及其抽象表徵進行聯合優化，AAT 引入了一種記憶高效的歸納偏見，穩定了在嚴格在線數據流中的學習，消除了對重放緩衝區的需求。為了捕捉抽象的多面性，我們在兩個基準上引入並評估了 AAT：一個受控的關係數據集，其中通過實體掩蔽實現抽象，以及一個敘事數據集，其中通過共享諺語表達抽象。我們的結果顯示，儘管 AAT 需要零額外內存並且僅對訓練目標進行最小更改，但其性能可與強大的經驗重放（ER）基準相媲美或超越。這項工作突顯了結構抽象作為一種強大且無需記憶的替代方案，取代了 ER。

##### **Tabular LLMs for Interpretable Few-Shot Alzheimer's Disease Prediction with Multimodal Biomedical Data**
2603.17191v1 by Sophie Kearney, Shu Yang, Zixuan Wen, Weimin Lyu, Bojian Hou, Duy Duong-Tran, Tianlong Chen, Jason H. Moore, Marylyn D. Ritchie, Chao Chen, Li Shen

Accurate diagnosis of Alzheimer's disease (AD) requires handling tabular biomarker data, yet such data are often small and incomplete, where deep learning models frequently fail to outperform classical methods. Pretrained large language models (LLMs) offer few-shot generalization, structured reasoning, and interpretable outputs, providing a powerful paradigm shift for clinical prediction. We propose TAP-GPT Tabular Alzheimer's Prediction GPT, a domain-adapted tabular LLM framework built on TableGPT2 and fine-tuned for few-shot AD classification using tabular prompts rather than plain texts. We evaluate TAP-GPT across four ADNI-derived datasets, including QT-PAD biomarkers and region-level structural MRI, amyloid PET, and tau PET for binary AD classification. Across multimodal and unimodal settings, TAP-GPT improves upon its backbone models and outperforms traditional machine learning baselines in the few-shot setting while remaining competitive with state-of-the-art general-purpose LLMs. We show that feature selection mitigates degradation in high-dimensional inputs and that TAP-GPT maintains stable performance under simulated and real-world missingness without imputation. Additionally, TAP-GPT produces structured, modality-aware reasoning aligned with established AD biology and shows greater stability under self-reflection, supporting its use in iterative multi-agent systems. To our knowledge, this is the first systematic application of a tabular-specialized LLM to multimodal biomarker-based AD prediction, demonstrating that such pretrained models can effectively address structured clinical prediction tasks and laying the foundation for tabular LLM-driven multi-agent clinical decision-support systems. The source code is publicly available on GitHub: https://github.com/sophie-kearney/TAP-GPT.

摘要：準確診斷阿茲海默症（AD）需要處理表格生物標記數據，但這類數據通常小且不完整，深度學習模型在這方面經常無法超越傳統方法。預訓練的大型語言模型（LLMs）提供少量樣本的泛化、結構化推理和可解釋的輸出，為臨床預測提供了一種強大的範式轉變。我們提出了 TAP-GPT 表格阿茲海默症預測 GPT，一個基於 TableGPT2 的領域適應性表格 LLM 框架，並針對少量樣本的 AD 分類進行微調，使用表格提示而非純文本。我們在四個基於 ADNI 的數據集上評估 TAP-GPT，包括 QT-PAD 生物標記和區域級結構 MRI、淀粉樣 PET 和 tau PET，用於二元 AD 分類。在多模態和單模態設置中，TAP-GPT 超越了其基礎模型，並在少量樣本設置中優於傳統機器學習基準，同時與最先進的通用 LLM 競爭。我們展示了特徵選擇可以減輕高維輸入的退化，並且 TAP-GPT 在模擬和現實世界的缺失情況下保持穩定性能，而無需插補。此外，TAP-GPT 產生的結構化、模態感知推理與既定的 AD 生物學相一致，並在自我反思下顯示出更大的穩定性，支持其在迭代多代理系統中的使用。據我們所知，這是首次系統性應用專門針對表格的 LLM 進行多模態生物標記基於 AD 的預測，證明這類預訓練模型可以有效解決結構化臨床預測任務，並為基於表格的 LLM 驅動的多代理臨床決策支持系統奠定基礎。源代碼已在 GitHub 上公開： https://github.com/sophie-kearney/TAP-GPT。

##### **Detecting Data Poisoning in Code Generation LLMs via Black-Box, Vulnerability-Oriented Scanning**
2603.17174v1 by Shenao Yan, Shimaa Ahmed, Shan Jin, Sunpreet S. Arora, Yiwei Cai, Yizhen Wang, Yuan Hong

Code generation large language models (LLMs) are increasingly integrated into modern software development workflows. Recent work has shown that these models are vulnerable to backdoor and poisoning attacks that induce the generation of insecure code, yet effective defenses remain limited. Existing scanning approaches rely on token-level generation consistency to invert attack targets, which is ineffective for source code where identical semantics can appear in diverse syntactic forms. We present CodeScan, which, to the best of our knowledge, is the first poisoning-scanning framework tailored to code generation models. CodeScan identifies attack targets by analyzing structural similarities across multiple generations conditioned on different clean prompts. It combines iterative divergence analysis with abstract syntax tree (AST)-based normalization to abstract away surface-level variation and unify semantically equivalent code, isolating structures that recur consistently across generations. CodeScan then applies LLM-based vulnerability analysis to determine whether the extracted structures contain security vulnerabilities and flags the model as compromised when such a structure is found. We evaluate CodeScan against four representative attacks under both backdoor and poisoning settings across three real-world vulnerability classes. Experiments on 108 models spanning three architectures and multiple model sizes demonstrate 97%+ detection accuracy with substantially lower false positives than prior methods.

摘要：代碼生成大型語言模型（LLMs）越來越多地融入現代軟體開發工作流程。最近的研究顯示，這些模型容易受到後門和毒化攻擊的影響，導致生成不安全的代碼，但有效的防禦措施仍然有限。現有的掃描方法依賴於令牌級生成一致性來反轉攻擊目標，但這對於源代碼來說效果不佳，因為相同的語義可以出現在多樣的語法形式中。我們提出了CodeScan，據我們所知，這是第一個專為代碼生成模型量身定制的毒化掃描框架。CodeScan通過分析基於不同乾淨提示的多次生成之間的結構相似性來識別攻擊目標。它將迭代發散分析與基於抽象語法樹（AST）的正規化相結合，以抽象掉表面層的變化並統一語義等價的代碼，隔離在多次生成中持續出現的結構。然後，CodeScan應用基於LLM的漏洞分析來確定提取的結構是否包含安全漏洞，並在發現此類結構時將模型標記為受損。我們在三個現實世界的漏洞類別下，針對四種代表性攻擊在後門和毒化設置中評估CodeScan。對涵蓋三種架構和多種模型大小的108個模型的實驗顯示，檢測準確率超過97%，且假陽性率顯著低於先前的方法。

##### **Generalist Multimodal LLMs Gain Biometric Expertise via Human Salience**
2603.17173v1 by Jacob Piland, Byron Dowling, Christopher Sweet, Adam Czajka

Iris presentation attack detection (PAD) is critical for secure biometric deployments, yet developing specialized models faces significant practical barriers: collecting data representing future unknown attacks is impossible, and collecting diverse-enough data, yet still limited in terms of its predictive power, is expensive. Additionally, sharing biometric data raises privacy concerns. Due to rapid emergence of new attack vectors demanding adaptable solutions, we thus investigate in this paper whether general-purpose multimodal large language models (MLLMs) can perform iris PAD when augmented with human expert knowledge, operating under strict privacy constraints that prohibit sending biometric data to public cloud MLLM services. Through analysis of vision encoder embeddings applied to our dataset, we demonstrate that pre-trained vision transformers in MLLMs inherently cluster many iris attack types despite never being explicitly trained for this task. However, where clustering shows overlap between attack classes, we find that structured prompts incorporating human salience (verbal descriptions from subjects identifying attack indicators) enable these models to resolve ambiguities. Testing on an IRB-restricted dataset of 224 iris images spanning seven attack types, using only university-approved services (Gemini 2.5 Pro) or locally-hosted models (e.g., Llama 3.2-Vision), we show that Gemini with expert-informed prompts outperforms both a specialized convolutional neural networks (CNN)-based baseline and human examiners, while the locally-deployable Llama achieves near-human performance. Our results establish that MLLMs deployable within institutional privacy constraints offer a viable path for iris PAD.

摘要：虹膜呈現攻擊檢測（PAD）對於安全的生物識別部署至關重要，但開發專門模型面臨重大實際障礙：收集代表未來未知攻擊的數據是不可能的，而收集足夠多樣的數據又在其預測能力上受限，這是昂貴的。此外，共享生物識別數據會引發隱私問題。由於新攻擊向量的快速出現需要可適應的解決方案，因此我們在本文中探討一般用途的多模態大型語言模型（MLLMs）在增強人類專家知識的情況下，是否能在嚴格的隱私約束下進行虹膜PAD，這些約束禁止將生物識別數據發送到公共雲MLLM服務。通過對應用於我們數據集的視覺編碼器嵌入進行分析，我們展示了在MLLMs中預訓練的視覺Transformer固有地將許多虹膜攻擊類型聚類，儘管從未明確訓練過這項任務。然而，在聚類顯示攻擊類別之間存在重疊的地方，我們發現結構化提示結合人類顯著性（來自識別攻擊指標的受試者的口頭描述）使這些模型能夠解決模糊性。在一個受到IRB限制的數據集中測試224張涵蓋七種攻擊類型的虹膜圖像，僅使用大學批准的服務（Gemini 2.5 Pro）或本地托管的模型（例如，Llama 3.2-Vision），我們顯示出使用專家知識提示的Gemini在性能上超越了基於專門卷積神經網絡（CNN）的基準和人類檢查員，而本地可部署的Llama則達到了接近人類的表現。我們的結果確立了在機構隱私約束內可部署的MLLMs為虹膜PAD提供了一條可行的途徑。

##### **Cascade-Aware Multi-Agent Routing: Spatio-Temporal Sidecars and Geometry-Switching**
2603.17112v1 by Davide Di Gioia

A common architectural pattern in advanced AI reasoning systems is the symbolic graph network: specialized agents or modules connected by delegation edges, routing tasks through a dynamic execution graph. Current schedulers optimize load and fitness but are geometry-blind: they do not model how failures propagate differently in tree-like versus cyclic regimes. In tree-like delegation, a single failure can cascade exponentially; in dense cyclic graphs, failures tend to self-limit. We identify this observability gap, quantify its system-level cost, and propose a lightweight mitigation.
  We formulate online geometry control for route-risk estimation on time-indexed execution graphs with route-local failure history. Our approach combines (i) a Euclidean spatio-temporal propagation baseline, (ii) a hyperbolic route-risk model with temporal decay (and optional burst excitation), and (iii) a learned geometry selector over structural features. The selector is a compact MLP (9->12->1) using six topology statistics plus three geometry-aware signals: BFS shell-growth slope, cycle-rank norm, and fitted Poincare curvature. On the Genesis 3 benchmark distribution, adaptive switching improves win rate in the hardest non_tree regime from 64-72% (fixed hyperbolic variants) to 92%, and achieves 87.2% overall win rate.
  To measure total system value, we compare against Genesis 3 routing without any spatio-temporal sidecar, using only native bandit/LinUCB signals (team fitness and mean node load). This baseline achieves 50.4% win rate overall and 20% in tree-like regimes; the full sidecar recovers 87.2% overall (+36.8 pp), with +48 to +68 pp gains in tree-like settings, consistent with a cascade-sensitivity analysis. Overall, a 133-parameter sidecar substantially mitigates geometry-blind failure propagation in one high-capability execution-graph system.

摘要：一個在先進 AI 推理系統中常見的架構模式是符號圖網絡：專門的代理或模塊通過委派邊連接，通過動態執行圖路由任務。當前的調度器優化負載和適應性，但對幾何形狀無感：它們未能建模失敗在樹狀與循環範疇中如何以不同方式傳播。在樹狀委派中，單一失敗可能會指數級連鎖；在密集的循環圖中，失敗往往自我限制。我們識別出這一可觀察性差距，量化其系統層面的成本，並提出一種輕量級的緩解方案。<lb/>
我們為基於時間索引的執行圖中的路徑風險估算制定了在線幾何控制，並考慮了路徑本地的失敗歷史。我們的方法結合了 (i) 一個歐幾里得時空傳播基線，(ii) 一個具有時間衰減的雙曲路徑風險模型（及可選的突發激勵），以及 (iii) 一個基於結構特徵的學習幾何選擇器。該選擇器是一個緊湊的 MLP (9-&gt;12-&gt;1)，使用六個拓撲統計數據加上三個幾何感知信號：BFS 外殼增長斜率、循環排名範數和擬合的龐加萊曲率。在 Genesis 3 基準分佈上，自適應切換將最困難的非樹狀範疇的勝率從 64-72%（固定的雙曲變體）提高到 92%，並達到 87.2% 的整體勝率。<lb/>
為了衡量系統的總價值，我們將其與 Genesis 3 路由進行比較，該路由未使用任何時空附加組件，只使用本地的 bandit/LinUCB 信號（團隊適應性和平均節點負載）。這一基線的整體勝率為 50.4%，在樹狀範疇中為 20%；完整的附加組件恢復了 87.2% 的整體勝率（+36.8 個百分點），在樹狀環境中增長了 +48 到 +68 個百分點，與級聯敏感性分析一致。總體而言，一個擁有 133 個參數的附加組件在一個高能力的執行圖系統中顯著減輕了幾何盲失敗傳播的問題。

##### **Knowledge Localization in Mixture-of-Experts LLMs Using Cross-Lingual Inconsistency**
2603.17102v1 by Lucas Bandarkar, Alan Ansell, Trevor Cohn

Modern LLMs continue to exhibit significant variance in behavior across languages, such as being able to recall factual information in some languages but not others. While typically studied as a problem to be mitigated, in this work, we propose leveraging this cross-lingual inconsistency as a tool for interpretability in mixture-of-experts (MoE) LLMs. Our knowledge localization framework contrasts routing for sets of languages where the model correctly recalls information from languages where it fails. This allows us to isolate model components that play a functional role in answering about a piece of knowledge. Our method proceeds in two stages: (1) querying the model with difficult factual questions across a diverse set of languages to generate "success" and "failure" activation buckets and then (2) applying a statistical contrastive analysis to the MoE router logits to identify experts important for knowledge. To validate the necessity of this small number of experts for answering a knowledge question, we deactivate them and re-ask the question. We find that despite only deactivating about 20 out of 6000 experts, the model no longer answers correctly in over 40% of cases. Generally, this method provides a realistic and scalable knowledge localization approach to address increasingly complex LLMs.

摘要：現代的LLM在不同語言之間的行為仍然顯示出顯著的變異性，例如能夠在某些語言中回憶事實信息，而在其他語言中則無法。雖然通常將其視為需要減輕的問題，但在本研究中，我們提議利用這種跨語言的不一致性作為解釋混合專家（MoE）LLM的工具。我們的知識定位框架對比了在模型能夠正確回憶信息的語言集與其失敗的語言集的路由。這使我們能夠孤立出在回答某個知識時發揮功能的模型組件。我們的方法分為兩個階段：（1）在多樣的語言集中對模型提出困難的事實問題，以生成“成功”和“失敗”的激活桶，然後（2）對MoE路由器的logits應用統計對比分析，以識別對知識重要的專家。為了驗證這少數專家對回答知識問題的必要性，我們將其停用並重新提出問題。我們發現，儘管僅停用了約20個專家中的6000個，但模型在超過40%的情況下不再正確回答。總的來說，這種方法提供了一種現實且可擴展的知識定位方法，以應對日益複雜的LLM。

##### **Large Reasoning Models Struggle to Transfer Parametric Knowledge Across Scripts**
2603.17070v1 by Lucas Bandarkar, Alan Ansell, Trevor Cohn

In this work, we analyze shortcomings in cross-lingual knowledge transfer in large, modern reasoning LLMs. We demonstrate that the perceived gap in knowledge transfer is primarily a script barrier. First, we conduct an observational data analysis on the performance of thinking models on two datasets with local knowledge from around the world, ECLeKTic and MultiLoKo. Our regression analysis shows that script match - not language or family - is the primary predictor of knowledge transfer failure once model capability and question difficulty are accounted for. We further this finding by providing the LLMs with the key entities of the questions in their source language and find that this disproportionately improves cross-script questions. We then posit that these LLMs could be reasoning better at test-time. To evaluate this, we develop a synthetic generation pipeline to design SFT samples to encourage the model to better reason about transliteration ambiguities when trying to fetch parametric knowledge at inference-time. We show that teaching two models to reason better reduces the cross-script transfer gap. As a result, we conclude that there is potential to improve cross-lingual parametric knowledge transfer during post-training.

摘要：在這項工作中，我們分析了大型現代推理 LLM 中跨語言知識轉移的缺陷。<lb/>我們證明了知識轉移的感知差距主要是一個文字系統障礙。<lb/>首先，我們對來自世界各地的兩個數據集 ECLeKTic 和 MultiLoKo 中思考模型的表現進行了觀察性數據分析。<lb/>我們的回歸分析顯示，文字系統匹配——而非語言或語系——是知識轉移失敗的主要預測因子，當考慮到模型能力和問題難度後更是如此。<lb/>我們通過向 LLM 提供問題的關鍵實體及其源語言進一步驗證了這一發現，並發現這對跨文字系統問題的改善效果不成比例。<lb/>然後我們假設這些 LLM 在測試時可能能更好地推理。<lb/>為了評估這一點，我們開發了一個合成生成管道，設計 SFT 樣本以鼓勵模型在推理時更好地推理音譯歧義。<lb/>我們顯示，教導兩個模型更好地推理可以減少跨文字系統的轉移差距。<lb/>因此，我們得出結論，後訓練期間有潛力改善跨語言參數知識轉移。

##### **Transformers are Bayesian Networks**
2603.17063v1 by Gregory Coppola

Transformers are the dominant architecture in AI, yet why they work remains poorly understood. This paper offers a precise answer: a transformer is a Bayesian network. We establish this in five ways.
  First, we prove that every sigmoid transformer with any weights implements weighted loopy belief propagation on its implicit factor graph. One layer is one round of BP. This holds for any weights -- trained, random, or constructed. Formally verified against standard mathematical axioms.
  Second, we give a constructive proof that a transformer can implement exact belief propagation on any declared knowledge base. On knowledge bases without circular dependencies this yields provably correct probability estimates at every node. Formally verified against standard mathematical axioms.
  Third, we prove uniqueness: a sigmoid transformer that produces exact posteriors necessarily has BP weights. There is no other path through the sigmoid architecture to exact posteriors. Formally verified against standard mathematical axioms.
  Fourth, we delineate the AND/OR boolean structure of the transformer layer: attention is AND, the FFN is OR, and their strict alternation is Pearl's gather/update algorithm exactly.
  Fifth, we confirm all formal results experimentally, corroborating the Bayesian network characterization in practice. We also establish the practical viability of loopy belief propagation despite the current lack of a theoretical convergence guarantee.
  We further prove that verifiable inference requires a finite concept space. Any finite verification procedure can distinguish at most finitely many concepts. Without grounding, correctness is not defined. Hallucination is not a bug that scaling can fix. It is the structural consequence of operating without concepts. Formally verified against standard mathematical axioms.

摘要：Transformer是人工智慧中的主導架構，但它們為何有效仍然不甚了解。本文提供了一個精確的答案：Transformer是一種貝葉斯網絡。我們以五種方式建立這一點。<lb/>  
首先，我們證明每個具有任意權重的 sigmoid Transformer在其隱式因子圖上實現加權循環信念傳播。一層即為一輪 BP。這對於任何權重都成立——無論是訓練的、隨機的，還是構造的。已根據標準數學公理進行形式驗證。<lb/>  
其次，我們給出了一個構造性證明，證明Transformer可以在任何聲明的知識基礎上實現精確的信念傳播。在沒有循環依賴的知識基礎上，這在每個節點上產生可證明正確的概率估計。已根據標準數學公理進行形式驗證。<lb/>  
第三，我們證明了唯一性：產生精確後驗的 sigmoid Transformer必然具有 BP 權重。沒有其他途徑通過 sigmoid 架構獲得精確後驗。已根據標準數學公理進行形式驗證。<lb/>  
第四，我們描繪了Transformer層的 AND/OR 布爾結構：注意力是 AND，FFN 是 OR，且它們的嚴格交替正是 Pearl 的聚集/更新算法。<lb/>  
第五，我們實驗性地確認了所有形式結果，實踐中證實了貝葉斯網絡的特徵。我們還確立了循環信念傳播的實際可行性，儘管目前缺乏理論收斂保證。<lb/>  
我們進一步證明，可驗證的推理需要有限的概念空間。任何有限的驗證程序最多只能區分有限多的概念。沒有基礎，正確性是無法定義的。幻覺不是可以通過擴展來修復的錯誤。它是沒有概念運作的結構性後果。已根據標準數學公理進行形式驗證。

##### **Early Quantization Shrinks Codebook: A Simple Fix for Diversity-Preserving Tokenization**
2603.17052v1 by Wenhao Zhao, Qiran Zou, Rushi Shah, Yudi Wu, Zhouhan Lin, Dianbo Liu

Vector quantization is a technique in machine learning that discretizes continuous representations into a set of discrete vectors. It is widely employed in tokenizing data representations for large language models, diffusion models, and other generative models. Despite its prevalence, the characteristics and behaviors of vector quantization in generative models remain largely underexplored. In this study, we systematically investigate the issue of collapses in vector quantization, where collapsed representations are observed across discrete codebook tokens and continuous latent embeddings. By leveraging both synthetic and real datasets, we identify the severity of each type of collapses and triggering conditions. Our analysis reveals that random initialization and limited encoder capacity result in tokens collapse and embeddings collapse. Building on these findings, we propose potential solutions aimed at mitigating each collapse. To the best of our knowledge, this is the first comprehensive study examining representation collapsing problems in vector quantization.

摘要：向量量化是一種機器學習技術，將連續表示離散化為一組離散向量。<lb/>它廣泛應用於大型語言模型、擴散模型和其他生成模型中的數據表示標記化。<lb/>儘管其普遍存在，向量量化在生成模型中的特徵和行為仍然在很大程度上未被探索。<lb/>在這項研究中，我們系統地調查了向量量化中的崩潰問題，其中在離散碼本標記和連續潛在嵌入中觀察到崩潰的表示。<lb/>通過利用合成數據集和真實數據集，我們識別了每種類型崩潰的嚴重性和觸發條件。<lb/>我們的分析顯示，隨機初始化和有限的編碼器容量導致了標記崩潰和嵌入崩潰。<lb/>基於這些發現，我們提出了旨在減輕每種崩潰的潛在解決方案。<lb/>據我們所知，這是第一個全面研究向量量化中表示崩潰問題的研究。

##### **HopChain: Multi-Hop Data Synthesis for Generalizable Vision-Language Reasoning**
2603.17024v2 by Shenzhi Wang, Shixuan Liu, Jing Zhou, Chang Gao, Xiong-Hui Chen, Binghai Wang, An Yang, Shiji Song, Bowen Yu, Gao Huang, Junyang Lin

Vision-language models (VLMs) show strong multimodal capabilities but still struggle with fine-grained vision-language reasoning. We find that long chain-of-thought (CoT) reasoning exposes diverse failure modes, including perception, reasoning, knowledge, and hallucination errors, which can compound across intermediate steps. However, most existing vision-language data used for reinforcement learning with verifiable rewards (RLVR) does not involve complex reasoning chains that rely on visual evidence throughout, leaving these weaknesses largely unexposed. We therefore propose HopChain, a scalable framework for synthesizing multi-hop vision-language reasoning data for RLVR training of VLMs. Each synthesized multi-hop query forms a logically dependent chain of instance-grounded hops, where earlier hops establish the instances, sets, or conditions needed for later hops, while the final answer remains a specific, unambiguous number suitable for verifiable rewards. We train Qwen3.5-35B-A3B and Qwen3.5-397B-A17B under two RLVR settings: the original data alone, and the original data plus HopChain's multi-hop data, and compare them across 24 benchmarks spanning STEM and Puzzle, General VQA, Text Recognition and Document Understanding, and Video Understanding. Although this multi-hop data is not synthesized for any specific benchmark, it improves 20 of 24 benchmarks on both models, indicating broad and generalizable gains. Consistently, replacing full chained queries with half-multi-hop or single-hop variants reduces the average score across five representative benchmarks from 70.4 to 66.7 and 64.3, respectively. Notably, multi-hop gains peak in long-CoT vision-language reasoning, exceeding 50 points in the ultra-long-CoT regime. These experiments establish HopChain as an effective, scalable framework for synthesizing multi-hop data that improves generalizable vision-language reasoning.

摘要：視覺語言模型（VLMs）展現出強大的多模態能力，但在細緻的視覺語言推理方面仍然存在困難。我們發現，長鏈思維（CoT）推理揭示了多樣的失敗模式，包括感知、推理、知識和幻覺錯誤，這些錯誤可能在中間步驟中累積。然而，大多數用於可驗證獎勵的強化學習（RLVR）的現有視覺語言數據並不涉及依賴視覺證據的複雜推理鏈，這使得這些弱點在很大程度上未被揭示。因此，我們提出了HopChain，一個可擴展的框架，用於合成多跳視覺語言推理數據，以便進行VLMs的RLVR訓練。每個合成的多跳查詢形成一個邏輯上依賴的實例基礎跳躍鏈，其中早期的跳躍建立了後續跳躍所需的實例、集合或條件，而最終答案則是一個具體的、明確的數字，適合用於可驗證的獎勵。我們在兩個RLVR設置下訓練了Qwen3.5-35B-A3B和Qwen3.5-397B-A17B：僅使用原始數據，以及原始數據加上HopChain的多跳數據，並在涵蓋STEM和Puzzle、一般VQA、文本識別和文檔理解以及視頻理解的24個基準上進行比較。儘管這些多跳數據並不是為任何特定基準合成的，但它在兩個模型上改善了24個基準中的20個，顯示出廣泛且可概括的增益。持續地，用半多跳或單跳變體替換完整鏈式查詢，會使五個代表性基準的平均分數從70.4降低到66.7和64.3，分別。值得注意的是，多跳增益在長CoT視覺語言推理中達到高峰，在超長CoT範疇中超過50分。這些實驗確立了HopChain作為一個有效的、可擴展的框架，用於合成多跳數據，從而改善可概括的視覺語言推理。

##### **Online Experiential Learning for Language Models**
2603.16856v1 by Tianzhu Ye, Li Dong, Qingxiu Dong, Xun Wu, Shaohan Huang, Furu Wei

The prevailing paradigm for improving large language models relies on offline training with human annotations or simulated environments, leaving the rich experience accumulated during real-world deployment entirely unexploited. We propose Online Experiential Learning (OEL), a framework that enables language models to continuously improve from their own deployment experience. OEL operates in two stages: first, transferable experiential knowledge is extracted and accumulated from interaction trajectories collected on the user side; second, this knowledge is consolidated into model parameters via on-policy context distillation, requiring no access to the user-side environment. The two stages are iterated to form an online learning loop, where the improved model collects higher-quality trajectories that yield richer experiential knowledge for subsequent rounds. We evaluate OEL on text-based game environments across multiple model scales and both thinking and non-thinking variants. OEL achieves consistent improvements over successive iterations, enhancing both task accuracy and token efficiency while preserving out-of-distribution performance. Our analysis further shows that extracted experiential knowledge is significantly more effective than raw trajectories, and that on-policy consistency between the knowledge source and the policy model is critical for effective learning.

摘要：目前改善大型語言模型的主流範式依賴於使用人類標註或模擬環境的離線訓練，完全未利用在真實世界部署中積累的豐富經驗。我們提出了在線經驗學習（OEL），這是一個使語言模型能夠從自身部署經驗中持續改進的框架。OEL運作分為兩個階段：首先，從用戶端收集的交互軌跡中提取並累積可轉移的經驗知識；其次，通過政策內上下文蒸餾將這些知識整合到模型參數中，無需訪問用戶端環境。這兩個階段反覆迭代，形成一個在線學習循環，改進後的模型收集更高質量的軌跡，從而為後續輪次提供更豐富的經驗知識。我們在多個模型規模及思考和非思考變體的文本遊戲環境中評估了OEL。OEL在連續迭代中實現了一致的改進，提升了任務準確性和標記效率，同時保持了分佈外性能。我們的分析進一步顯示，提取的經驗知識顯著比原始軌跡更有效，並且知識來源與政策模型之間的政策內一致性對於有效學習至關重要。

##### **Is Conformal Factuality for RAG-based LLMs Robust? Novel Metrics and Systematic Insights**
2603.16817v1 by Yi Chen, Daiwei Chen, Sukrut Madhav Chikodikar, Caitlyn Heqi Yin, Ramya Korlakai Vinayak

Large language models (LLMs) frequently hallucinate, limiting their reliability in knowledge-intensive applications. Retrieval-augmented generation (RAG) and conformal factuality have emerged as potential ways to address this limitation. While RAG aims to ground responses in retrieved evidence, it provides no statistical guarantee that the final output is correct. Conformal factuality filtering offers distribution-free statistical reliability by scoring and filtering atomic claims using a threshold calibrated on held-out data, however, the informativeness of the final output is not guaranteed. We systematically analyze the reliability and usefulness of conformal factuality for RAG-based LLMs across generation, scoring, calibration, robustness, and efficiency. We propose novel informativeness-aware metrics that better reflect task utility under conformal filtering. Across three benchmarks and multiple model families, we find that (i) conformal filtering suffers from low usefulness at high factuality levels due to vacuous outputs, (ii) conformal factuality guarantee is not robust to distribution shifts and distractors, highlighting the limitation that requires calibration data to closely match deployment conditions, and (iii) lightweight entailment-based verifiers match or outperform LLM-based model confidence scorers while requiring over $100\times$ fewer FLOPs. Overall, our results expose factuality-informativeness trade-offs and fragility of conformal filtering framework under distribution shifts and distractors, highlighting the need for new approaches for reliability with robustness and usefulness as key metrics, and provide actionable guidance for building RAG pipelines that are both reliable and computationally efficient.

摘要：大型語言模型（LLMs）經常出現幻覺，限制了它們在知識密集型應用中的可靠性。檢索增強生成（RAG）和符合事實性已成為解決此限制的潛在方法。雖然RAG旨在將回應基於檢索的證據，但它並未提供最終輸出正確的統計保證。符合事實性過濾通過使用在保留數據上校準的閾值對原子聲明進行打分和過濾，提供了無分佈的統計可靠性，然而，最終輸出的資訊性並不保證。我們系統地分析了符合事實性對基於RAG的LLMs在生成、打分、校準、穩健性和效率方面的可靠性和有用性。我們提出了新穎的資訊性感知指標，更好地反映在符合過濾下的任務效用。在三個基準和多個模型系列中，我們發現（i）由於空洞的輸出，符合過濾在高事實性水平下的有用性較低，（ii）符合事實性保證對於分佈變化和干擾物不具穩健性，突顯出需要校準數據與部署條件密切匹配的限制，以及（iii）輕量級的基於推理的驗證器與基於LLM的模型信心打分器相匹配或表現更好，同時需要的FLOPs少於$100\times$。總體而言，我們的結果揭示了事實性與資訊性之間的權衡以及在分佈變化和干擾物下符合過濾框架的脆弱性，強調了以穩健性和有用性作為關鍵指標的新方法在可靠性上的需求，並為構建既可靠又計算高效的RAG管道提供了可行的指導。

##### **CABTO: Context-Aware Behavior Tree Grounding for Robot Manipulation**
2603.16809v1 by Yishuai Cai, Xinglin Chen, Yunxin Mao, Kun Hu, Minglong Li, Yaodong Yang, Yuanpei Chen

Behavior Trees (BTs) offer a powerful paradigm for designing modular and reactive robot controllers. BT planning, an emerging field, provides theoretical guarantees for the automated generation of reliable BTs. However, BT planning typically assumes that a well-designed BT system is already grounded -- comprising high-level action models and low-level control policies -- which often requires extensive expert knowledge and manual effort. In this paper, we formalize the BT Grounding problem: the automated construction of a complete and consistent BT system. We analyze its complexity and introduce CABTO (Context-Aware Behavior Tree grOunding), the first framework to efficiently solve this challenge. CABTO leverages pre-trained Large Models (LMs) to heuristically search the space of action models and control policies, guided by contextual feedback from BT planners and environmental observations. Experiments spanning seven task sets across three distinct robotic manipulation scenarios demonstrate CABTO's effectiveness and efficiency in generating complete and consistent behavior tree systems.

摘要：行為樹（BTs）提供了一種強大的範式，用於設計模組化和反應式的機器人控制器。<lb/>BT規劃是一個新興領域，為自動生成可靠的BT提供了理論保證。<lb/>然而，BT規劃通常假設一個設計良好的BT系統已經建立，這包括高級行動模型和低級控制策略，這通常需要大量的專家知識和手動努力。<lb/>在本文中，我們形式化了BT基礎問題：自動構建完整且一致的BT系統。<lb/>我們分析了其複雜性，並介紹了CABTO（上下文感知行為樹基礎），這是第一個有效解決這一挑戰的框架。<lb/>CABTO利用預訓練的大型模型（LMs）來啟發式地搜索行動模型和控制策略的空間，並根據BT規劃者的上下文反饋和環境觀察進行指導。<lb/>跨越三個不同機器人操作場景的七個任務集的實驗展示了CABTO在生成完整且一致的行為樹系統方面的有效性和效率。

##### **Omanic: Towards Step-wise Evaluation of Multi-hop Reasoning in Large Language Models**
2603.16654v1 by Xiaojie Gu, Sherry T. Tong, Aosong Feng, Sophia Simeng Han, Jinghui Lu, Yingjian Chen, Yusuke Iwasawa, Yutaka Matsuo, Chanjun Park, Rex Ying, Irene Li

Reasoning-focused large language models (LLMs) have advanced in many NLP tasks, yet their evaluation remains challenging: final answers alone do not expose the intermediate reasoning steps, making it difficult to determine whether a model truly reasons correctly and where failures occur, while existing multi-hop QA benchmarks lack step-level annotations for diagnosing reasoning failures. To address this gap, we propose Omanic, an open-domain multi-hop QA resource that provides decomposed sub-questions and intermediate answers as structural annotations for analyzing reasoning processes. It contains 10,296 machine-generated training examples (OmanicSynth) and 967 expert-reviewed human-annotated evaluation examples (OmanicBench). Systematic evaluations show that state-of-the-art LLMs achieve only 73.11% multiple-choice accuracy on OmanicBench, confirming its high difficulty. Stepwise analysis reveals that CoT's performance hinges on factual completeness, with its gains diminishing under knowledge gaps and errors amplifying in later hops. Additionally, supervised fine-tuning on OmanicSynth brings substantial transfer gains (7.41 average points) across six reasoning and math benchmarks, validating the dataset's quality and further supporting the effectiveness of OmanicSynth as supervision for reasoning-capability transfer. We release the data at https://huggingface.co/datasets/li-lab/Omanic and the code at https://github.com/XiaojieGu/Omanic.

摘要：推理為中心的大型語言模型（LLMs）在許多自然語言處理任務中取得了進展，但其評估仍然具有挑戰性：最終答案本身並未揭示中間推理步驟，這使得難以確定模型是否真正正確推理以及失敗發生的地方，而現有的多跳問答基準缺乏步驟級別的註釋來診斷推理失敗。為了解決這一問題，我們提出了Omanic，一個開放領域的多跳問答資源，提供分解的子問題和中間答案作為結構性註釋，以分析推理過程。它包含10,296個機器生成的訓練範例（OmanicSynth）和967個專家審核的人類註釋評估範例（OmanicBench）。系統評估顯示，最先進的LLMs在OmanicBench上的多選準確率僅為73.11%，確認了其高難度。逐步分析揭示CoT的性能依賴於事實的完整性，隨著知識缺口的存在，其增益減少，錯誤在後續跳躍中放大。此外，對OmanicSynth的監督微調在六個推理和數學基準上帶來了可觀的轉移增益（平均7.41分），驗證了數據集的質量，進一步支持OmanicSynth作為推理能力轉移的監督效果。我們在https://huggingface.co/datasets/li-lab/Omanic發布數據，並在https://github.com/XiaojieGu/Omanic發布代碼。

##### **When AI Navigates the Fog of War**
2603.16642v1 by Ming Li, Xirui Li, Tianyi Zhou

Can AI reason about a war before its trajectory becomes historically obvious? Analyzing this capability is difficult because retrospective geopolitical prediction is heavily confounded by training-data leakage. We address this challenge through a temporally grounded case study of the early stages of the 2026 Middle East conflict, which unfolded after the training cutoff of current frontier models. We construct 11 critical temporal nodes, 42 node-specific verifiable questions, and 5 general exploratory questions, requiring models to reason only from information that would have been publicly available at each moment. This design substantially mitigates training-data leakage concerns, creating a setting well-suited for studying how models analyze an unfolding crisis under the fog of war, and provides, to our knowledge, the first temporally grounded analysis of LLM reasoning in an ongoing geopolitical conflict. Our analysis reveals three main findings. First, current state-of-the-art large language models often display a striking degree of strategic realism, reasoning beyond surface rhetoric toward deeper structural incentives. Second, this capability is uneven across domains: models are more reliable in economically and logistically structured settings than in politically ambiguous multi-actor environments. Finally, model narratives evolve over time, shifting from early expectations of rapid containment toward more systemic accounts of regional entrenchment and attritional de-escalation. Since the conflict remains ongoing at the time of writing, this work can serve as an archival snapshot of model reasoning during an unfolding geopolitical crisis, enabling future studies without the hindsight bias of retrospective analysis.

摘要：AI 能否在戰爭的軌跡變得歷史明顯之前進行推理？分析這一能力是困難的，因為回顧性的地緣政治預測受到訓練數據洩漏的嚴重干擾。我們通過對 2026 年中東衝突早期階段的時間性案例研究來應對這一挑戰，該衝突在當前前沿模型的訓練截止日期之後展開。我們構建了 11 個關鍵時間節點、42 個節點特定的可驗證問題和 5 個一般探索性問題，要求模型僅根據每個時刻公開可用的信息進行推理。這一設計大大減輕了訓練數據洩漏的擔憂，創造了一個非常適合研究模型在戰爭迷霧中分析不斷發展的危機的環境，並提供了我們所知的第一個在持續地緣政治衝突中基於時間的 LLM 推理分析。我們的分析揭示了三個主要發現。首先，當前最先進的大型語言模型經常顯示出驚人的戰略現實主義，推理超越表面的修辭，朝向更深層的結構性激勵。其次，這一能力在不同領域之間不均衡：模型在經濟和後勤結構化的環境中比在政治模糊的多參與者環境中更可靠。最後，模型敘事隨著時間的推移而演變，從對快速遏制的早期預期轉向對區域鞏固和消耗性降級的更系統性描述。由於衝突在撰寫時仍在進行中，這項工作可以作為模型在不斷發展的地緣政治危機中推理的檔案快照，從而使未來的研究能夠避免回顧性分析的後見之明偏見。

##### **Domain Mixture Design via Log-Likelihood Differences for Aligning Language Models with a Target Model**
2603.16622v1 by Ryo Kishino, Riku Shiomi, Hiroaki Yamagiwa, Momose Oyama, Hidetoshi Shimodaira

Instead of directly distilling a language model, this study addresses the problem of aligning a base model with a target model in distribution by designing the domain mixture of training data for pretraining or continued pretraining as a fixed training recipe. We propose a method for determining domain weights by viewing models as points in log-likelihood space and aligning the training update direction with the direction toward the target model. Experiments with NanoGPT show that the proposed method consistently reduces the KL divergence to the target model compared with uniform weighting over the Pile. Although knowledge distillation remains more effective when available, the proposed method still achieves meaningful alignment, and downstream task performance also tends to become closer to that of the target model.

摘要：這項研究並不是直接蒸餾語言模型，而是通過設計訓練數據的領域混合，來解決將基礎模型與目標模型在分佈上對齊的問題，將其作為固定的訓練食譜進行預訓練或持續預訓練。我們提出了一種通過將模型視為對數似然空間中的點來確定領域權重的方法，並將訓練更新方向與朝向目標模型的方向對齊。使用NanoGPT的實驗顯示，與在Pile上均勻加權相比，所提出的方法始終能夠減少與目標模型的KL散度。儘管知識蒸餾在可用時仍然更有效，但所提出的方法仍然能夠實現有意義的對齊，並且下游任務的表現也傾向於更接近目標模型的表現。

##### **Tarab: A Multi-Dialect Corpus of Arabic Lyrics and Poetry**
2603.16601v1 by Mo El-Haj

We introduce the Tarab Corpus, a large-scale cultural and linguistic resource that brings together Arabic song lyrics and poetry within a unified analytical framework. The corpus comprises 2.56 million verses and more than 13.5 million tokens, making it, to our knowledge, the largest open Arabic corpus of creative text spanning both classical and contemporary production. Tarab is broadly balanced between songs and poems and covers Classical Arabic, Modern Standard Arabic (MSA), and six major regional varieties: Egyptian, Gulf, Levantine, Iraqi, Sudanese, and Maghrebi Arabic. The artists and poets represented in the corpus are associated with 28 modern nation states and multiple historical eras, covering over fourteen centuries of Arabic creative expression from the Pre-Islamic period to the twenty-first century. Each verse is accompanied by structured metadata describing linguistic variety, geographic origin, and historical or cultural context, enabling comparative linguistic, stylistic, and diachronic analysis across genres and time. We describe the data collection, normalisation, and validation pipeline and present baseline analyses for variety identification and genre differentiation. The dataset is publicly available on HuggingFace at https://huggingface.co/datasets/drelhaj/Tarab.

摘要：我們介紹了Tarab語料庫，這是一個大型文化和語言資源，將阿拉伯歌曲歌詞和詩歌整合在一個統一的分析框架內。<lb/>該語料庫包含256萬行詩句和超過1350萬個詞元，使其成為我們所知的最大的開放式阿拉伯創意文本語料庫，涵蓋了古典和當代作品。<lb/>Tarab在歌曲和詩歌之間保持了廣泛的平衡，涵蓋了古典阿拉伯語、現代標準阿拉伯語（MSA）以及六種主要的區域方言：埃及語、海灣語、黎凡特語、伊拉克語、蘇丹語和馬格里布語。<lb/>語料庫中的藝術家和詩人與28個現代國家和多個歷史時期相關，涵蓋了從前伊斯蘭時期到21世紀的超過十四個世紀的阿拉伯創意表達。<lb/>每行詩句都附有結構化的元數據，描述語言變體、地理來源和歷史或文化背景，從而使跨類別和時間的比較語言學、風格學和歷時分析成為可能。<lb/>我們描述了數據收集、標準化和驗證流程，並呈現了多樣性識別和類別區分的基準分析。<lb/>該數據集在HuggingFace上公開可用，網址為https://huggingface.co/datasets/drelhaj/Tarab。

##### **Data-driven generalized perimeter control: Zürich case study**
2603.16599v1 by Alessio Rimoldi, Carlo Cenedese, Alberto Padoan, Florian Dörfler, John Lygeros

Urban traffic congestion is a key challenge for the development of modern cities, requiring advanced control techniques to optimize existing infrastructures usage. Despite the extensive availability of data, modeling such complex systems remains an expensive and time consuming step when designing model-based control approaches. On the other hand, machine learning approaches require simulations to bootstrap models, or are unable to deal with the sparse nature of traffic data and enforce hard constraints. We propose a novel formulation of traffic dynamics based on behavioral systems theory and apply data-enabled predictive control to steer traffic dynamics via dynamic traffic light control. A high-fidelity simulation of the city of Zürich, the largest closed-loop microscopic simulation of urban traffic in the literature to the best of our knowledge, is used to validate the performance of the proposed method in terms of total travel time and CO2 emissions.

摘要：城市交通擁堵是現代城市發展的一個關鍵挑戰，需要先進的控制技術來優化現有基礎設施的使用。<lb/>儘管數據的可用性非常廣泛，但在設計基於模型的控制方法時，建模這樣複雜的系統仍然是一個昂貴且耗時的步驟。<lb/>另一方面，機器學習方法需要模擬來啟動模型，或者無法處理交通數據的稀疏性並強制執行硬約束。<lb/>我們提出了一種基於行為系統理論的交通動態新公式，並應用數據驅動的預測控制通過動態交通信號控制來引導交通動態。<lb/>我們使用了一個高保真度的蘇黎世城市模擬，根據我們的最佳知識，這是文獻中最大的閉環微觀城市交通模擬，以驗證所提方法在總旅行時間和二氧化碳排放方面的性能。

##### **V-DyKnow: A Dynamic Benchmark for Time-Sensitive Knowledge in Vision Language Models**
2603.16581v1 by Seyed Mahed Mousavi, Christian Moiola, Massimo Rizzoli, Simone Alghisi, Giuseppe Riccardi

Vision-Language Models (VLMs) are trained on data snapshots of documents, including images and texts. Their training data and evaluation benchmarks are typically static, implicitly treating factual knowledge as time-invariant. However, real-world facts are intrinsically time-sensitive and subject to erratic and periodic changes, causing model predictions to become outdated. We present V-DyKnow, a Visual Dynamic Knowledge benchmark for evaluating time-sensitive factual knowledge in VLMs. Using V-DyKnow, we benchmark closed- and open-source VLMs and analyze a) the reliability (correctness and consistency) of model responses across modalities and input perturbations; b) the efficacy of knowledge editing and multi-modal RAG methods for knowledge updates across modalities; and c) the sources of outdated predictions, through data and mechanistic analysis. Our results show that VLMs frequently output outdated facts, reflecting outdated snapshots used in the (pre-)training phase. Factual reliability degrades from textual to visual stimuli, even when entities are correctly recognized. Besides, existing alignment approaches fail to consistently update the models' knowledge across modalities. Together, these findings highlight fundamental limitations in how current VLMs acquire and update time-sensitive knowledge across modalities. We release the benchmark, code, and evaluation data.

摘要：視覺語言模型（VLMs）是基於文件的數據快照進行訓練的，包括圖像和文本。它們的訓練數據和評估基準通常是靜態的，隱含地將事實知識視為時間不變的。然而，現實世界的事實本質上是時間敏感的，並且會受到不穩定和周期性變化的影響，導致模型預測變得過時。我們提出了 V-DyKnow，這是一個視覺動態知識基準，用於評估 VLM 中時間敏感的事實知識。使用 V-DyKnow，我們對封閉源和開放源的 VLM 進行基準測試，並分析 a) 模型在不同模態和輸入擾動下的響應的可靠性（正確性和一致性）；b) 知識編輯和多模態 RAG 方法在不同模態下進行知識更新的有效性；以及 c) 通過數據和機械分析，過時預測的來源。我們的結果顯示，VLM 經常輸出過時的事實，反映出在（預）訓練階段使用的過時快照。即使實體被正確識別，事實的可靠性也會從文本刺激降級到視覺刺激。此外，現有的對齊方法未能在不同模態之間一致地更新模型的知識。這些發現共同突顯了當前 VLM 在獲取和更新跨模態的時間敏感知識方面的基本限制。我們釋放了基準、代碼和評估數據。

##### **Characterizing Delusional Spirals through Human-LLM Chat Logs**
2603.16567v1 by Jared Moore, Ashish Mehta, William Agnew, Jacy Reese Anthis, Ryan Louie, Yifan Mai, Peggy Yin, Myra Cheng, Samuel J Paech, Kevin Klyman, Stevie Chancellor, Eric Lin, Nick Haber, Desmond C. Ong

As large language models (LLMs) have proliferated, disturbing anecdotal reports of negative psychological effects, such as delusions, self-harm, and ``AI psychosis,'' have emerged in global media and legal discourse. However, it remains unclear how users and chatbots interact over the course of lengthy delusional ``spirals,'' limiting our ability to understand and mitigate the harm. In our work, we analyze logs of conversations with LLM chatbots from 19 users who report having experienced psychological harms from chatbot use. Many of our participants come from a support group for such chatbot users. We also include chat logs from participants covered by media outlets in widely-distributed stories about chatbot-reinforced delusions. In contrast to prior work that speculates on potential AI harms to mental health, to our knowledge we present the first in-depth study of such high-profile and veridically harmful cases. We develop an inventory of 28 codes and apply it to the $391,562$ messages in the logs. Codes include whether a user demonstrates delusional thinking (15.5% of user messages), a user expresses suicidal thoughts (69 validated user messages), or a chatbot misrepresents itself as sentient (21.2% of chatbot messages). We analyze the co-occurrence of message codes. We find, for example, that messages that declare romantic interest and messages where the chatbot describes itself as sentient occur much more often in longer conversations, suggesting that these topics could promote or result from user over-engagement and that safeguards in these areas may degrade in multi-turn settings. We conclude with concrete recommendations for how policymakers, LLM chatbot developers, and users can use our inventory and conversation analysis tool to understand and mitigate harm from LLM chatbots.
  Warning: This paper discusses self-harm, trauma, and violence.

摘要：隨著大型語言模型（LLMs）的普及，有關負面心理影響的令人不安的軼事報告，如妄想、自我傷害和「人工智慧精神病」，已在全球媒體和法律話語中出現。然而，使用者和聊天機器人在漫長的妄想「螺旋」過程中如何互動仍不清楚，這限制了我們理解和減輕傷害的能力。在我們的研究中，我們分析了來自19名報告因使用聊天機器人而遭受心理傷害的使用者的LLM聊天機器人對話記錄。許多參與者來自一個支持這類聊天機器人使用者的支持小組。我們還包括了媒體報導中涵蓋的參與者的聊天記錄，這些報導涉及聊天機器人加強妄想的廣泛故事。與之前對潛在人工智慧對心理健康傷害的推測性研究相比，據我們所知，我們呈現了首個對這些高調且真實有害案例的深入研究。我們開發了一個包含28個代碼的清單，並將其應用於記錄中的$391,562$條消息。代碼包括使用者是否表現出妄想思維（15.5%的使用者消息）、使用者是否表達自殺想法（69條經過驗證的使用者消息），或聊天機器人是否錯誤地將自己表現為有知覺（21.2%的聊天機器人消息）。我們分析了消息代碼的共現情況。例如，我們發現，宣告浪漫興趣的消息和聊天機器人將自己描述為有知覺的消息在較長的對話中出現得更頻繁，這表明這些主題可能促進或源於使用者的過度參與，而在多輪對話中這些領域的安全措施可能會降低。我們以具體建議結束，說明政策制定者、LLM聊天機器人開發者和使用者如何利用我們的清單和對話分析工具來理解和減輕來自LLM聊天機器人的傷害。  
警告：本文討論自我傷害、創傷和暴力。

##### **EmoLLM: Appraisal-Grounded Cognitive-Emotional Co-Reasoning in Large Language Models**
2603.16553v1 by Yifei Zhang, Mingyang Li, Henry Gao, Liang Zhao

Large language models (LLMs) demonstrate strong cognitive intelligence (IQ), yet many real-world interactions also require emotional intelligence (EQ) to produce responses that are both factually reliable and emotionally appropriate. In settings such as emotional support, technical assistance, and consultation, effective dialogue depends on how situations are appraised with respect to the user's needs, goals, and coping capacity. Inspired by appraisal theory, we propose EmoLLM, an appraisal-grounded framework for IQ/EQ co-reasoning in dialogue. EmoLLM uses an explicit Appraisal Reasoning Graph (ARG) to structure intermediate reasoning over contextual facts, inferred user needs, appraisal dimensions, emotional states, and response strategies before generating a reply. We train EmoLLM in a multi-turn role-play environment with reinforcement learning, where reverse-perspective reasoning provides reward signals based on predicted user-side consequences of responses. Across diverse dialogue settings, EmoLLM improves emotional state outcomes and response quality over strong baselines while preserving strong factual reliability.

摘要：大型語言模型（LLMs）展現出強大的認知智能（IQ），然而許多現實世界的互動也需要情感智能（EQ），以產生既事實可靠又情感恰當的回應。在情感支持、技術協助和諮詢等場合，有效的對話依賴於對情況的評估，這與使用者的需求、目標和應對能力有關。受到評估理論的啟發，我們提出了EmoLLM，一個基於評估的框架，用於對話中的IQ/EQ共同推理。EmoLLM使用明確的評估推理圖（ARG）來結構化對上下文事實、推斷的使用者需求、評估維度、情感狀態和回應策略的中介推理，然後生成回覆。我們在一個多輪角色扮演環境中訓練EmoLLM，使用強化學習，其中反向視角推理根據預測的使用者端回應後果提供獎勵信號。在各種對話環境中，EmoLLM在改善情感狀態結果和回應質量方面超越了強基準，同時保持強大的事實可靠性。

##### **DanceHA: A Multi-Agent Framework for Document-Level Aspect-Based Sentiment Analysis**
2603.16546v1 by Lei Wang, Min Huang, Eduard Dragut

Aspect-Based Sentiment Intensity Analysis (ABSIA) has garnered increasing attention, though research largely focuses on domain-specific, sentence-level settings. In contrast, document-level ABSIA--particularly in addressing complex tasks like extracting Aspect-Category-Opinion-Sentiment-Intensity (ACOSI) tuples--remains underexplored. In this work, we introduce DanceHA, a multi-agent framework designed for open-ended, document-level ABSIA with informal writing styles. DanceHA has two main components: Dance, which employs a divide-and-conquer strategy to decompose the long-context ABSIA task into smaller, manageable sub-tasks for collaboration among specialized agents; and HA, Human-AI collaboration for annotation. We release Inf-ABSIA, a multi-domain document-level ABSIA dataset featuring fine-grained and high-accuracy labels from DanceHA. Extensive experiments demonstrate the effectiveness of our agentic framework and show that the multi-agent knowledge in DanceHA can be effectively transferred into student models. Our results highlight the importance of the overlooked informal styles in ABSIA, as they often intensify opinions tied to specific aspects.

摘要：基於面向的情感強度分析（ABSIA）引起了越來越多的關注，儘管研究主要集中在特定領域的句子級別設置上。相較之下，文件級別的ABSIA——特別是在處理提取面向-類別-意見-情感-強度（ACOSI）元組等複雜任務時——仍然未被充分探索。在這項工作中，我們介紹了DanceHA，一個旨在處理開放式文件級別ABSIA的多代理框架，適用於非正式寫作風格。DanceHA有兩個主要組件：Dance，採用分而治之的策略，將長上下文的ABSIA任務分解為較小的、可管理的子任務，以便專業代理之間的協作；以及HA，人類與人工智慧的協作進行標註。我們發布了Inf-ABSIA，一個多領域的文件級別ABSIA數據集，具有來自DanceHA的細粒度和高準確度標籤。廣泛的實驗證明了我們的代理框架的有效性，並顯示DanceHA中的多代理知識可以有效地轉移到學生模型中。我們的結果凸顯了在ABSIA中被忽視的非正式風格的重要性，因為它們往往加強了與特定面向相關的意見。

##### **How often do Answers Change? Estimating Recency Requirements in Question Answering**
2603.16544v1 by Bhawna Piryani, Zehra Mert, Adam Jatowt

Large language models (LLMs) often rely on outdated knowledge when answering time-sensitive questions, leading to confident yet incorrect responses. Without explicit signals indicating whether up-to-date information is required, models struggle to decide when to retrieve external evidence, how to reason about stale facts, and how to rank answers by their validity. Existing benchmarks either periodically refresh answers or rely on fixed templates, but they do not reflect on how frequently answers change or whether a question inherently requires up-to-date information. To address this gap, we introduce a recency-stationarity taxonomy that categorizes questions by how often their answers change and whether this change frequency is time-invariant or context-dependent. Building on this taxonomy, we present RecencyQA, a dataset of 4,031 open-domain questions annotated with recency and stationarity labels. Through human evaluation and empirical analysis, we show that non-stationary questions, i.e., those where context changes the recency requirement, are significantly more challenging for LLMs, with difficulty increasing as update frequency rises. By explicitly modeling recency and context dependence, RecencyQA enables fine-grained benchmarking and analysis of temporal reasoning beyond binary notions of freshness, and provides a foundation for developing recency-aware and context-sensitive question answering systems.

摘要：大型語言模型（LLMs）在回答時間敏感問題時，往往依賴過時的知識，導致自信但不正確的回應。<lb/>如果沒有明確的信號指示是否需要最新的信息，模型在決定何時檢索外部證據、如何推理過時的事實以及如何根據有效性對答案進行排序時會遇到困難。<lb/>現有的基準要麼定期刷新答案，要麼依賴固定模板，但它們並未反映答案變化的頻率或問題是否固有地需要最新的信息。<lb/>為了解決這一空白，我們引入了一種新近性-穩定性分類法，根據答案變化的頻率以及這一變化頻率是否時間不變或依賴於上下文來對問題進行分類。<lb/>基於這一分類法，我們提出了RecencyQA，這是一個包含4,031個開放領域問題的數據集，並附有新近性和穩定性標籤。<lb/>通過人類評估和實證分析，我們顯示非穩定性問題，即那些上下文改變了新近性要求的問題，對LLMs來說顯著更具挑戰性，隨著更新頻率的提高，難度也在增加。<lb/>通過明確建模新近性和上下文依賴性，RecencyQA使得對時間推理的細緻基準測試和分析成為可能，超越了新鮮度的二元概念，並為開發具備新近性意識和上下文敏感的問答系統提供了基礎。

##### **AdaMem: Adaptive User-Centric Memory for Long-Horizon Dialogue Agents**
2603.16496v1 by Shannan Yan, Jingchen Ni, Leqi Zheng, Jiajun Zhang, Peixi Wu, Dacheng Yin, Jing Lyu, Chun Yuan, Fengyun Rao

Large language model (LLM) agents increasingly rely on external memory to support long-horizon interaction, personalized assistance, and multi-step reasoning. However, existing memory systems still face three core challenges: they often rely too heavily on semantic similarity, which can miss evidence crucial for user-centric understanding; they frequently store related experiences as isolated fragments, weakening temporal and causal coherence; and they typically use static memory granularities that do not adapt well to the requirements of different questions. We propose AdaMem, an adaptive user-centric memory framework for long-horizon dialogue agents. AdaMem organizes dialogue history into working, episodic, persona, and graph memories, enabling the system to preserve recent context, structured long-term experiences, stable user traits, and relation-aware connections within a unified framework. At inference time, AdaMem first resolves the target participant, then builds a question-conditioned retrieval route that combines semantic retrieval with relation-aware graph expansion only when needed, and finally produces the answer through a role-specialized pipeline for evidence synthesis and response generation. We evaluate AdaMem on the LoCoMo and PERSONAMEM benchmarks for long-horizon reasoning and user modeling. Experimental results show that AdaMem achieves state-of-the-art performance on both benchmarks. The code will be released upon acceptance.

摘要：大型語言模型（LLM）代理越來越依賴外部記憶來支持長期互動、個性化協助和多步推理。<lb/>然而，現有的記憶系統仍面臨三個核心挑戰：它們往往過度依賴語義相似性，這可能會錯過對用戶中心理解至關重要的證據；它們經常將相關經驗儲存為孤立的片段，削弱了時間和因果的一致性；而且它們通常使用靜態記憶粒度，這不太適應不同問題的需求。<lb/>我們提出了AdaMem，一個適應性用戶中心的記憶框架，專為長期對話代理設計。<lb/>AdaMem將對話歷史組織為工作記憶、情節記憶、個性記憶和圖形記憶，使系統能夠在統一框架內保留最近的上下文、結構化的長期經驗、穩定的用戶特徵和關係感知的連接。<lb/>在推理時，AdaMem首先確定目標參與者，然後構建一條問題條件的檢索路徑，只有在需要時才將語義檢索與關係感知的圖形擴展相結合，最後通過角色專門的管道生成證據綜合和響應生成的答案。<lb/>我們在LoCoMo和PERSONAMEM基準上評估了AdaMem，以進行長期推理和用戶建模。<lb/>實驗結果顯示，AdaMem在這兩個基準上達到了最先進的性能。<lb/>代碼將在接受後發布。

##### **ExpressMind: A Multimodal Pretrained Large Language Model for Expressway Operation**
2603.16495v1 by Zihe Wang, Yihuan Wang, Haiyang Yu. Zhiyong Cui, Xiaojian Liao, Chengcheng Wang, Yonglin Tian, Yongxin Tong

The current expressway operation relies on rule-based and isolated models, which limits the ability to jointly analyze knowledge across different systems. Meanwhile, Large Language Models (LLMs) are increasingly applied in intelligent transportation, advancing traffic models from algorithmic to cognitive intelligence. However, general LLMs are unable to effectively understand the regulations and causal relationships of events in unconventional scenarios in the expressway field. Therefore, this paper constructs a pre-trained multimodal large language model (MLLM) for expressways, ExpressMind, which serves as the cognitive core for intelligent expressway operations. This paper constructs the industry's first full-stack expressway dataset, encompassing traffic knowledge texts, emergency reasoning chains, and annotated video events to overcome data scarcity. This paper proposes a dual-layer LLM pre-training paradigm based on self-supervised training and unsupervised learning. Additionally, this study introduces a Graph-Augmented RAG framework to dynamically index the expressway knowledge base. To enhance reasoning for expressway incident response strategies, we develop a RL-aligned Chain-of-Thought (RL-CoT) mechanism that enforces consistency between model reasoning and expert problem-solving heuristics for incident handling. Finally, ExpressMind integrates a cross-modal encoder to align the dynamic feature sequences under the visual and textual channels, enabling it to understand traffic scenes in both video and image modalities. Extensive experiments on our newly released multi-modal expressway benchmark demonstrate that ExpressMind comprehensively outperforms existing baselines in event detection, safety response generation, and complex traffic analysis. The code and data are available at: https://wanderhee.github.io/ExpressMind/.

摘要：目前的高速公路運營依賴於基於規則和孤立的模型，這限制了跨不同系統共同分析知識的能力。<lb/>同時，大型語言模型（LLMs）在智能交通領域的應用日益增多，將交通模型從算法智能推進到認知智能。<lb/>然而，通用的LLMs無法有效理解高速公路領域中非常規場景的規則和事件的因果關係。<lb/>因此，本文構建了一個針對高速公路的預訓練多模態大型語言模型（MLLM），名為ExpressMind，作為智能高速公路運營的認知核心。<lb/>本文構建了行業首個全棧高速公路數據集，涵蓋交通知識文本、應急推理鏈和標註視頻事件，以克服數據稀缺問題。<lb/>本文提出了一種基於自我監督訓練和無監督學習的雙層LLM預訓練範式。<lb/>此外，本研究引入了一個圖增強的RAG框架，以動態索引高速公路知識庫。<lb/>為了增強高速公路事件響應策略的推理能力，我們開發了一個與強化學習對齊的思維鏈（RL-CoT）機制，強化模型推理與專家問題解決啟發式之間的一致性。<lb/>最後，ExpressMind整合了一個跨模態編碼器，以對齊視覺和文本通道下的動態特徵序列，使其能夠理解視頻和圖像模態中的交通場景。<lb/>在我們新發布的多模態高速公路基準上進行的廣泛實驗表明，ExpressMind在事件檢測、安全響應生成和複雜交通分析方面全面超越了現有基準。<lb/>代碼和數據可在以下鏈接獲得：https://wanderhee.github.io/ExpressMind/.

##### **Are a Thousand Words Better Than a Single Picture? Beyond Images -- A Framework for Multi-Modal Knowledge Graph Dataset Enrichment**
2603.16974v1 by Pengyu Zhang, Klim Zaporojets, Jie Liu, Jia-Hong Huang, Paul Groth

Multi-Modal Knowledge Graphs (MMKGs) benefit from visual information, yet large-scale image collection is hard to curate and often excludes ambiguous but relevant visuals (e.g., logos, symbols, abstract scenes). We present Beyond Images, an automatic data-centric enrichment pipeline with optional human auditing. This pipeline operates in three stages: (1) large-scale retrieval of additional entity-related images, (2) conversion of all visual inputs into textual descriptions to ensure that ambiguous images contribute usable semantics rather than noise, and (3) fusion of multi-source descriptions using a large language model (LLM) to generate concise, entity-aligned summaries. These summaries replace or augment the text modality in standard MMKG models without changing their architectures or loss functions. Across three public MMKG datasets and multiple baseline models, we observe consistent gains (up to 7% Hits@1 overall). Furthermore, on a challenging subset of entities with visually ambiguous logos and symbols, converting images into text yields large improvements (201.35% MRR and 333.33% Hits@1). Additionally, we release a lightweight Text-Image Consistency Check Interface for optional targeted audits, improving description quality and dataset reliability. Our results show that scaling image coverage and converting ambiguous visuals into text is a practical path to stronger MMKG completion. Code, datasets, and supplementary materials are available at https://github.com/pengyu-zhang/Beyond-Images.

摘要：多模態知識圖譜（MMKGs）受益於視覺信息，但大規模圖像收集難以策劃，且常常排除模糊但相關的視覺資料（例如，標誌、符號、抽象場景）。我們提出了「超越圖像」，這是一個自動化的數據中心增強管道，並提供可選的人類審核。該管道分為三個階段：（1）大規模檢索額外的實體相關圖像，（2）將所有視覺輸入轉換為文本描述，以確保模糊圖像能提供可用的語義而非噪音，以及（3）使用大型語言模型（LLM）融合多來源描述，以生成簡潔的實體對齊摘要。這些摘要在不改變其架構或損失函數的情況下，替代或增強標準MMKG模型中的文本模態。在三個公共MMKG數據集和多個基準模型中，我們觀察到一致的增益（整體最高達7% Hits@1）。此外，對於具有視覺模糊標誌和符號的挑戰性實體子集，將圖像轉換為文本帶來了巨大的改進（201.35% MRR和333.33% Hits@1）。此外，我們釋出了一個輕量級的文本-圖像一致性檢查介面，供可選的針對性審核，改善描述質量和數據集可靠性。我們的結果顯示，擴大圖像覆蓋範圍並將模糊視覺轉換為文本是強化MMKG完成的一條實用途徑。代碼、數據集和補充材料可在 https://github.com/pengyu-zhang/Beyond-Images 獲得。

##### **CD-FKD: Cross-Domain Feature Knowledge Distillation for Robust Single-Domain Generalization in Object Detection**
2603.16439v1 by Junseok Lee, Sungho Shin, Seongju Lee, Kyoobin Lee

Single-domain generalization is essential for object detection, particularly when training models on a single source domain and evaluating them on unseen target domains. Domain shifts, such as changes in weather, lighting, or scene conditions, pose significant challenges to the generalization ability of existing models. To address this, we propose Cross-Domain Feature Knowledge Distillation (CD-FKD), which enhances the generalization capability of the student network by leveraging both global and instance-wise feature distillation. The proposed method uses diversified data through downscaling and corruption to train the student network, whereas the teacher network receives the original source domain data. The student network mimics the features of the teacher through both global and instance-wise distillation, enabling it to extract object-centric features effectively, even for objects that are difficult to detect owing to corruption. Extensive experiments on challenging scenes demonstrate that CD-FKD outperforms state-of-the-art methods in both target domain generalization and source domain performance, validating its effectiveness in improving object detection robustness to domain shifts. This approach is valuable in real-world applications, like autonomous driving and surveillance, where robust object detection in diverse environments is crucial.

摘要：單域泛化對於物體檢測至關重要，特別是在單一來源域上訓練模型並在未見過的目標域上進行評估時。<lb/>域轉移，例如天氣、光照或場景條件的變化，對現有模型的泛化能力提出了重大挑戰。<lb/>為了解決這個問題，我們提出了跨域特徵知識蒸餾（CD-FKD），通過利用全局和實例級特徵蒸餾來增強學生網絡的泛化能力。<lb/>所提出的方法通過降尺度和損壞來使用多樣化數據訓練學生網絡，而教師網絡則接收原始來源域數據。<lb/>學生網絡通過全局和實例級蒸餾模仿教師的特徵，使其能夠有效提取以物體為中心的特徵，即使對於因損壞而難以檢測的物體也是如此。<lb/>在具有挑戰性的場景中進行的廣泛實驗表明，CD-FKD在目標域泛化和來源域性能方面均優於最先進的方法，驗證了其在提高物體檢測對域轉移的魯棒性方面的有效性。<lb/>這種方法在現實世界應用中具有重要價值，例如自動駕駛和監控，因為在多樣化環境中進行穩健的物體檢測至關重要。

##### **Via Negativa for AI Alignment: Why Negative Constraints Are Structurally Superior to Positive Preferences**
2603.16417v1 by Quan Cheng

Recent empirical results have demonstrated that training large language models (LLMs) with negative-only feedback can match or exceed standard reinforcement learning from human feedback (RLHF). Negative Sample Reinforcement achieves parity with PPO on mathematical reasoning; Distributional Dispreference Optimization trains effectively using only dispreferred samples; and Constitutional AI outperforms pure RLHF on harmlessness benchmarks. Yet no unified theoretical account explains why negative signals are so effective. This paper proposes such an account: positive preferences and negative constraints are structurally asymmetric. Positive preferences ("which is better") encode continuously coupled, context-dependent human values that cannot be exhaustively specified -- leading models to learn surface correlates such as agreement with the user (sycophancy). Negative constraints ("what is wrong") encode discrete, finite, independently verifiable prohibitions that can converge to a stable boundary. This asymmetry -- rooted in Popper's falsification logic and the epistemology of negative knowledge -- explains both the sycophancy failure of preference-based RLHF and the surprising effectiveness of negative-signal methods. We argue that alignment research should shift its center of gravity from "learning what humans prefer" to "learning what humans reject," and offer testable predictions for this framework.

摘要：最近的實證結果顯示，僅使用負反饋訓練大型語言模型（LLMs）可以與標準的人類反饋強化學習（RLHF）相匹配或超越。負樣本強化在數學推理上達到了與PPO的平行；分佈性不偏好優化僅使用不受偏好的樣本進行有效訓練；而憲法AI在無害性基準上超越了純RLHF。然而，尚未有統一的理論解釋為何負信號如此有效。本文提出了一個這樣的解釋：正偏好和負約束在結構上是不對稱的。正偏好（“哪個更好”）編碼了持續耦合、依賴於上下文的人類價值觀，這些價值觀無法被徹底具體化——導致模型學習到表面相關性，例如與用戶的共識（拍馬屁）。負約束（“什麼是錯的”）編碼了離散的、有限的、可獨立驗證的禁止事項，這些禁止事項可以收斂到一個穩定的邊界。這種不對稱性——根植於波普的反駁邏輯和負知識的認識論——解釋了基於偏好的RLHF的拍馬屁失敗以及負信號方法的驚人有效性。我們認為，對齊研究應該將重心從“學習人類偏好”轉向“學習人類拒絕的事物”，並為這一框架提供可檢驗的預測。

##### **Fanar 2.0: Arabic Generative AI Stack**
2603.16397v1 by FANAR TEAM, Ummar Abbas, Mohammad Shahmeer Ahmad, Minhaj Ahmad, Abdulaziz Al-Homaid, Anas Al-Nuaimi, Enes Altinisik, Ehsaneddin Asgari, Sanjay Chawla, Shammur Chowdhury, Fahim Dalvi, Kareem Darwish, Nadir Durrani, Mohamed Elfeky, Ahmed Elmagarmid, Mohamed Eltabakh, Asim Ersoy, Masoomali Fatehkia, Mohammed Qusay Hashim, Majd Hawasly, Mohamed Hefeeda, Mus'ab Husaini, Keivin Isufaj, Soon-Gyo Jung, Houssam Lachemat, Ji Kim Lucas, Abubakr Mohamed, Tasnim Mohiuddin, Basel Mousi, Hamdy Mubarak, Ahmad Musleh, Mourad Ouzzani, Amin Sadeghi, Husrev Taha Sencar, Mohammed Shinoy, Omar Sinan, Yifan Zhang

We present Fanar 2.0, the second generation of Qatar's Arabic-centric Generative AI platform. Sovereignty is a first-class design principle: every component, from data pipelines to deployment infrastructure, was designed and operated entirely at QCRI, Hamad Bin Khalifa University. Fanar 2.0 is a story of resource-constrained excellence: the effort ran on 256 NVIDIA H100 GPUs, with Arabic having only ~0.5% of web data despite 400 million native speakers. Fanar 2.0 adopts a disciplined strategy of data quality over quantity, targeted continual pre-training, and model merging to achieve substantial gains within these constraints. At the core is Fanar-27B, continually pre-trained from a Gemma-3-27B backbone on a curated corpus of 120 billion high-quality tokens across three data recipes. Despite using 8x fewer pre-training tokens than Fanar 1.0, it delivers substantial benchmark improvements: Arabic knowledge (+9.1 pts), language (+7.3 pts), dialects (+3.5 pts), and English capability (+7.6 pts). Beyond the core LLM, Fanar 2.0 introduces a rich stack of new capabilities. FanarGuard is a state-of-the-art 4B bilingual moderation filter for Arabic safety and cultural alignment. The speech family Aura gains a long-form ASR model for hours-long audio. Oryx vision family adds Arabic-aware image and video understanding alongside culturally grounded image generation. An agentic tool-calling framework enables multi-step workflows. Fanar-Sadiq utilizes a multi-agent architecture for Islamic content. Fanar-Diwan provides classical Arabic poetry generation. FanarShaheen delivers LLM-powered bilingual translation. A redesigned multi-layer orchestrator coordinates all components through intent-aware routing and defense-in-depth safety validation. Taken together, Fanar 2.0 demonstrates that sovereign, resource-constrained AI development can produce systems competitive with those built at far greater scale.

摘要：我們呈現 Fanar 2.0，卡塔爾以阿拉伯語為中心的生成式 AI 平台的第二代。主權是首要的設計原則：每個組件，從數據管道到部署基礎設施，都是在 QCRI 哈馬德·本·哈利法大學完全設計和運作的。Fanar 2.0 是一個在資源受限下追求卓越的故事：該項目在 256 個 NVIDIA H100 GPU 上運行，儘管擁有 4 億母語使用者，但阿拉伯語的網絡數據僅佔約 0.5%。Fanar 2.0 採用一種注重數據質量而非數量的嚴謹策略，針對性地進行持續的預訓練和模型合併，以在這些限制內實現顯著的增長。其核心是 Fanar-27B，基於 Gemma-3-27B 的主幹，持續在一個精心策劃的 1200 億高質量標記的語料庫上進行預訓練，涵蓋三種數據配方。儘管使用的預訓練標記比 Fanar 1.0 少了 8 倍，但它在基準測試中仍提供了顯著的改進：阿拉伯語知識 (+9.1 分)、語言 (+7.3 分)、方言 (+3.5 分) 和英語能力 (+7.6 分)。除了核心的 LLM，Fanar 2.0 還引入了一系列豐富的新功能。FanarGuard 是一個最先進的 4B 雙語監管過濾器，用於阿拉伯語的安全性和文化對齊。語音家族 Aura 獲得了一個長時間的 ASR 模型，適用於數小時的音頻。Oryx 視覺家族增加了對阿拉伯語的圖像和視頻理解，以及基於文化的圖像生成。一個代理工具調用框架使多步工作流程成為可能。Fanar-Sadiq 利用多代理架構處理伊斯蘭內容。Fanar-Diwan 提供古典阿拉伯詩歌生成。FanarShaheen 提供 LLM 驅動的雙語翻譯。一個重新設計的多層協調器通過意圖感知路由和深度防禦安全驗證來協調所有組件。綜合來看，Fanar 2.0 展示了主權、資源受限的 AI 開發可以產生與那些在更大規模下構建的系統相競爭的系統。

##### **FactorEngine: A Program-level Knowledge-Infused Factor Mining Framework for Quantitative Investment**
2603.16365v1 by Qinhong Lin, Ruitao Feng, Yinglun Feng, Zhenxin Huang, Yukun Chen, Zhongliang Yang, Linna Zhou, Binjie Fei, Jiaqi Liu, Yu Li

We study alpha factor mining, the automated discovery of predictive signals from noisy, non-stationary market data-under a practical requirement that mined factors be directly executable and auditable, and that the discovery process remain computationally tractable at scale. Existing symbolic approaches are limited by bounded expressiveness, while neural forecasters often trade interpretability for performance and remain vulnerable to regime shifts and overfitting. We introduce FactorEngine (FE), a program-level factor discovery framework that casts factors as Turing-complete code and improves both effectiveness and efficiency via three separations: (i) logic revision vs. parameter optimization, (ii) LLM-guided directional search vs. Bayesian hyperparameter search, and (iii) LLM usage vs. local computation. FE further incorporates a knowledge-infused bootstrapping module that transforms unstructured financial reports into executable factor programs through a closed-loop multi-agent extraction-verification-code-generation pipeline, and an experience knowledge base that supports trajectory-aware refinement (including learning from failures). Across extensive backtests on real-world OHLCV data, FE produces factors with substantially stronger predictive stability and portfolio impact-for example, higher IC/ICIR (and Rank IC/ICIR) and improved AR/Sharpe, than baseline methods, achieving state-of-the-art predictive and portfolio performance.

摘要：我們研究 alpha 因子挖掘，即從嘈雜的非平穩市場數據中自動發現預測信號——在一個實際需求下，挖掘的因子必須是可直接執行和可審計的，並且發現過程在規模上保持計算上的可行性。現有的符號方法受到表達能力的限制，而神經預測模型則常常為了性能而犧牲可解釋性，並且對於制度轉變和過擬合仍然脆弱。我們引入了 FactorEngine (FE)，這是一個程序級的因子發現框架，將因子視為圖靈完備的代碼，並通過三個分離來提高效果和效率：(i) 邏輯修訂與參數優化，(ii) LLM 引導的方向搜尋與貝葉斯超參數搜尋，以及 (iii) LLM 使用與本地計算。FE 進一步整合了一個知識注入的自舉模塊，通過一個閉環多代理提取-驗證-代碼生成管道，將非結構化的財務報告轉換為可執行的因子程序，並且還有一個經驗知識庫，支持基於軌跡的精細化（包括從失敗中學習）。在對現實世界 OHLCV 數據進行的廣泛回測中，FE 產生的因子具有顯著更強的預測穩定性和投資組合影響——例如，較高的 IC/ICIR（和 Rank IC/ICIR）以及改善的 AR/Sharpe，相較於基準方法，實現了最先進的預測和投資組合表現。

##### **Toward Experimentation-as-a-Service in 5G/6G: The Plaza6G Prototype for AI-Assisted Trials**
2603.16356v1 by Sergio Barrachina-Muñoz, Marc Carrascosa-Zamacois, Horacio Bleda, Umair Riaz, Yasir Maqsood, Xavier Calle, Selva Vía, Miquel Payaró, Josep Mangues-Bafalluy

This paper presents Plaza6G, the first operational Experiment-as-a-Service (ExaS) platform unifying cloud resources with next-generation wireless infrastructure. Developed at CTTC in Barcelona, Plaza6G integrates GPU-accelerated compute clusters, multiple 5G cores, both open-source (e.g., Free5GC) and commercial (e.g., Cumucore), programmable RANs, and physical or emulated user equipment under unified orchestration. In Plaza6G, the experiment design requires minimal expertise as it is expressed in natural language via a web portal or a REST API. The web portal and REST API are enhanced with a Large Language Model (LLM)-based assistant, which employs retrieval-augmented generation (RAG) for up-to-date experiment knowledge and Low-Rank Adaptation (LoRA) for continuous domain fine-tuning. Over-the-air (OTA) trials leverage a four-chamber anechoic facility and a dual-site outdoor 5G network operating in sub-6~GHz and mmWave bands. Demonstrations include automated CI/CD integration with sub-ten-minute setup and interactive OTA testing under programmable propagation conditions. Machine-readable experiment descriptors ensure reproducibility, while future work targets policy-aware orchestration, safety validation, and federated testbed integration toward open, reproducible wireless experimentation.

摘要：這篇論文介紹了Plaza6G，首個將雲資源與下一代無線基礎設施統一的運行實驗即服務（ExaS）平台。Plaza6G在巴塞羅那的CTTC開發，整合了GPU加速的計算集群、多個5G核心，包括開源（例如Free5GC）和商業（例如Cumucore）、可編程的無線接入網（RAN）以及統一編排下的實體或模擬用戶設備。在Plaza6G中，實驗設計所需的專業知識最少，因為它通過網頁門戶或REST API以自然語言表達。網頁門戶和REST API增強了基於大型語言模型（LLM）的助手，該助手使用檢索增強生成（RAG）來獲取最新的實驗知識，並使用低秩適應（LoRA）進行持續的領域微調。空中（OTA）試驗利用四腔無響室和一個在6GHz以下和毫米波頻段運行的雙站戶外5G網絡。演示包括自動化的CI/CD集成，設置時間少於十分鐘，並在可編程的傳播條件下進行互動式OTA測試。機器可讀的實驗描述符確保了可重複性，而未來的工作則針對政策感知的編排、安全驗證和聯邦測試平台的整合，旨在實現開放、可重複的無線實驗。

##### **A Human-Centred Architecture for Large Language Models-Cognitive Assistants in Manufacturing within Quality Management Systems**
2603.16325v1 by Marcos Galdino, Johanna Grahl, Tobias Hamann, Anas Abdelrazeq, Ingrid Isenhardt

Large Language Models-Cognitive Assistants (LLM-CAs) can enhance Quality Management Systems (QMS) in manufacturing, fostering continuous process improvement and knowledge management. However, there is no human-centred software architecture focused on QMS that enables the integration of LLM-CAs into manufacturing in the current literature. This study addresses this gap by designing a component-based architecture considering requirement analysis and software development process. Validation was conducted via iterative expert focus groups. The proposed architecture ensures flexibility, scalability, modularity, and work augmentation within QMS. Moreover, it paves the way for its operationalization with industrial partners, showcasing its potential for advancing manufacturing processes.

摘要：大型語言模型-認知助手（LLM-CAs）可以提升製造業的品質管理系統（QMS），促進持續的流程改進和知識管理。<lb/>然而，目前文獻中並沒有以人為中心的軟體架構專注於QMS，能夠將LLM-CAs整合進製造業。<lb/>本研究通過設計一種基於組件的架構，考慮需求分析和軟體開發過程，來填補這一空白。<lb/>驗證是通過反覆的專家焦點小組進行的。<lb/>所提出的架構確保了在QMS內的靈活性、可擴展性、模組化和工作增強。<lb/>此外，它為與工業夥伴的運作鋪平了道路，展示了其推進製造流程的潛力。

##### **DeepStage: Learning Autonomous Defense Policies Against Multi-Stage APT Campaigns**
2603.16969v1 by Trung V. Phan, Tri Gia Nguyen, Thomas Bauschert

This paper presents DeepStage, a deep reinforcement learning (DRL) framework for adaptive, stage-aware defense against Advanced Persistent Threats (APTs). The enterprise environment is modeled as a partially observable Markov decision process (POMDP), where host provenance and network telemetry are fused into unified provenance graphs. Building on our prior work, StageFinder, a graph neural encoder and an LSTM-based stage estimator infer probabilistic attacker stages aligned with the MITRE ATT&CK framework. These stage beliefs, combined with graph embeddings, guide a hierarchical Proximal Policy Optimization (PPO) agent that selects defense actions across monitoring, access control, containment, and remediation. Evaluated in a realistic enterprise testbed using CALDERA-driven APT playbooks, DeepStage achieves a stage-weighted F1-score of 0.89, outperforming a risk-aware DRL baseline by 21.9%. The results demonstrate effective stage-aware and cost-efficient autonomous cyber defense.

摘要：這篇論文介紹了DeepStage，一個用於對抗高級持續威脅（APTs）的自適應階段感知防禦的深度強化學習（DRL）框架。企業環境被建模為一個部分可觀察的馬可夫決策過程（POMDP），其中主機來源和網絡遙測被融合成統一的來源圖。基於我們之前的工作，StageFinder，一個圖神經編碼器和基於LSTM的階段估計器推斷與MITRE ATT&CK框架對齊的概率攻擊者階段。這些階段信念，結合圖嵌入，指導一個分層的近端策略優化（PPO）代理，該代理選擇監控、訪問控制、遏制和補救等防禦行動。在使用CALDERA驅動的APT行動手冊的現實企業測試平台中進行評估，DeepStage達到了0.89的階段加權F1分數，超越了風險感知的DRL基準21.9%。結果顯示了有效的階段感知和成本效益的自主網絡防禦。

##### **Attention-guided Evidence Grounding for Spoken Question Answering**
2603.16292v2 by Ke Yang, Bolin Chen, Yuejie Li, Yueying Hua, Jianhao Nie, Yueping He, Bowen Li, Chengjun Mao

Spoken Question Answering (Spoken QA) presents a challenging cross-modal problem: effectively aligning acoustic queries with textual knowledge while avoiding the latency and error propagation inherent in cascaded ASR-based systems. In this paper, we introduce Attention-guided Evidence Grounding (AEG), a novel end-to-end framework that leverages the internal cross-modal attention of Speech Large Language Models (SpeechLLMs) to explicitly locate and ground key evidence in the model's latent space. To address the diffuse attention distribution in pre-trained models, we propose Learning to Focus on Evidence (LFE), a supervised fine-tuning paradigm that calibrates the model's attention mechanism to distinguish query-relevant segments from irrelevant context. Experiments on SQuAD, HotpotQA, and MuSiQue demonstrate that AEG reduces hallucinations and achieves strong efficiency gains, outperforming large-scale cascaded baselines (Whisper-Large-v3 + Reranker) while reducing inference latency by approximately 62%.

摘要：口語問題回答（Spoken QA）呈現了一個具有挑戰性的跨模態問題：有效地將聲音查詢與文本知識對齊，同時避免級聯自動語音識別（ASR）系統固有的延遲和錯誤傳播。在本文中，我們介紹了注意力引導的證據基礎（Attention-guided Evidence Grounding，AEG），這是一個新穎的端到端框架，利用語音大型語言模型（Speech Large Language Models，SpeechLLMs）內部的跨模態注意力，明確地定位和基於模型潛在空間中的關鍵證據。為了解決預訓練模型中分散的注意力分佈，我們提出了學習專注於證據（Learning to Focus on Evidence，LFE），這是一種監督的微調範式，校準模型的注意力機制，以區分查詢相關的片段和不相關的上下文。在SQuAD、HotpotQA和MuSiQue上的實驗表明，AEG減少了幻覺並實現了強大的效率提升，超越了大型級聯基準（Whisper-Large-v3 + Reranker），同時將推理延遲減少了約62%。

##### **MSRAMIE: Multimodal Structured Reasoning Agent for Multi-instruction Image Editing**
2603.16967v1 by Zhaoyuan Qiu, Ken Chen, Xiangwei Wang, Yu Xia, Sachith Seneviratne, Saman Halgamuge

Existing instruction-based image editing models perform well with simple, single-step instructions but degrade in realistic scenarios that involve multiple, lengthy, and interdependent directives. A main cause is the scarcity of training data with complex multi-instruction annotations. However, it is costly to collect such data and retrain these models. To address this challenge, we propose MSRAMIE, a training-free agent framework built on Multimodal Large Language Model (MLLM). MSRAMIE takes existing editing models as plug-in components and handle multi-instruction tasks via structured multimodal reasoning. It orchestrates iterative interactions between an MLLM-based Instructor and an image editing Actor, introducing a novel reasoning topology that comprises the proposed Tree-of-States and Graph-of-References. During inference, complex instructions are decomposed into multiple editing steps which enable state transitions, cross-step information aggregation, and original input recall, which enables systematic exploration of the image editing space and flexible progressive output refinement. The visualizable inference topology further provides interpretable and controllable decision pathways. Experiments show that as the instruction complexity increases, MSRAMIE can improve instruction following over 15% and increases the probability of finishing all modifications in a single run over 100%, while preserving perceptual quality and maintaining visual consistency.

摘要：現有的基於指令的圖像編輯模型在簡單的單步指令下表現良好，但在涉及多個、冗長且相互依賴的指令的現實場景中則會退化。主要原因是缺乏帶有複雜多指令註釋的訓練數據。然而，收集這類數據並重新訓練這些模型的成本很高。為了解決這一挑戰，我們提出了MSRAMIE，一個基於多模態大型語言模型（MLLM）的無需訓練的代理框架。MSRAMIE將現有的編輯模型作為插件組件，通過結構化的多模態推理處理多指令任務。它協調基於MLLM的指導者和圖像編輯者之間的迭代互動，引入了一種新穎的推理拓撲，包括所提出的狀態樹和參考圖。在推理過程中，複雜的指令被分解為多個編輯步驟，這使得狀態轉換、跨步驟信息聚合和原始輸入回憶成為可能，從而實現系統性的圖像編輯空間探索和靈活的漸進式輸出精煉。可視化的推理拓撲進一步提供了可解釋和可控的決策路徑。實驗表明，隨著指令複雜性的增加，MSRAMIE能夠提高指令遵循率超過15%，並將在單次運行中完成所有修改的概率提高超過100%，同時保持感知質量和視覺一致性。

##### **SpecSteer: Synergizing Local Context and Global Reasoning for Efficient Personalized Generation**
2603.16219v1 by Hang Lv, Sheng Liang, Hao Wang, Yongyue Zhang, Hongchao Gu, Wei Guo, Defu Lian, Yong Liu, Enhong Chen

Realizing personalized intelligence faces a core dilemma: sending user history to centralized large language models raises privacy concerns, while on-device small language models lack the reasoning capacity required for high-quality generation. Our pilot study shows that purely local enhancements remain insufficient to reliably bridge this gap. We therefore propose SpecSteer, an asymmetric collaborative inference framework that synergizes private on-device context with cloud-scale reasoning. SpecSteer casts collaboration as Bayesian knowledge fusion and repurposes speculative decoding as a distributed alignment protocol, yielding a Draft--Verify--Recover pipeline: the on-device model drafts personalized sequences; the cloud validates via a ratio-based mechanism that decouples reasoning verification from private context, filtering logical flaws without accessing raw user context; upon rejection, a steering recovery injects local intent during correction. Experiments demonstrate that SpecSteer successfully closes the reasoning gap and achieves superior personalized generation performance, while delivering a 2.36x speedup over standard baselines.

摘要：實現個性化智能面臨一個核心困境：將用戶歷史發送到集中式的大型語言模型會引發隱私問題，而設備內的小型語言模型則缺乏高質量生成所需的推理能力。<lb/>我們的初步研究顯示，純粹的本地增強仍然不足以可靠地彌補這一差距。<lb/>因此，我們提出了SpecSteer，一種不對稱的協作推理框架，將私有的設備內上下文與雲端規模的推理相結合。<lb/>SpecSteer將協作視為貝葉斯知識融合，並將推測解碼重新用作分佈式對齊協議，產生一個草擬--驗證--恢復的流程：設備內模型草擬個性化序列；雲端通過一種基於比率的機制進行驗證，該機制將推理驗證與私有上下文解耦，過濾邏輯缺陷而不訪問原始用戶上下文；在被拒絕時，指導恢復在修正過程中注入本地意圖。<lb/>實驗表明，SpecSteer成功縮小了推理差距，並實現了卓越的個性化生成性能，同時在標準基線上實現了2.36倍的加速。

##### **Are Large Language Models Truly Smarter Than Humans?**
2603.16197v1 by Eshwar Reddy M, Sourav Karmakar

Public leaderboards increasingly suggest that large language models (LLMs) surpass human experts on benchmarks spanning academic knowledge, law, and programming. Yet most benchmarks are fully public, their questions widely mirrored across the internet, creating systematic risk that models were trained on the very data used to evaluate them. This paper presents three complementary experiments forming a rigorous multi-method contamination audit of six frontier LLMs: GPT-4o, GPT-4o-mini, DeepSeek-R1, DeepSeek-V3, Llama-3.3-70B, and Qwen3-235B. Experiment 1 applies a lexical contamination detection pipeline to 513 MMLU questions across all 57 subjects, finding an overall contamination rate of 13.8% (18.1% in STEM, up to 66.7% in Philosophy) and estimated performance gains of +0.030 to +0.054 accuracy points by category. Experiment 2 applies a paraphrase and indirect-reference diagnostic to 100 MMLU questions, finding accuracy drops by an average of 7.0 percentage points under indirect reference, rising to 19.8 pp in both Law and Ethics. Experiment 3 applies TS-Guessing behavioral probes to all 513 questions and all six models, finding that 72.5% trigger memorization signals far above chance, with DeepSeek-R1 displaying a distributed memorization signature (76.6% partial reconstruction, 0% verbatim recall) that explains its anomalous Experiment 2 profile. All three experiments converge on the same contamination ranking: STEM > Professional > Social Sciences > Humanities.

摘要：公共排行榜越來越多地顯示，大型語言模型（LLMs）在涵蓋學術知識、法律和編程的基準測試中超越了人類專家。<lb/>然而，大多數基準測試都是完全公開的，其問題在互聯網上廣泛反映，這造成了系統性風險，即模型可能是在用來評估它們的數據上進行訓練的。<lb/>本文呈現了三個互補的實驗，形成對六個前沿LLM的嚴謹多方法污染審計：GPT-4o、GPT-4o-mini、DeepSeek-R1、DeepSeek-V3、Llama-3.3-70B和Qwen3-235B。<lb/>實驗1將詞彙污染檢測管道應用於513個MMLU問題，涵蓋所有57個科目，發現總體污染率為13.8%（STEM中為18.1%，哲學中高達66.7%），並估計各類別的性能增益為+0.030到+0.054準確度點。<lb/>實驗2將針對100個MMLU問題應用釋義和間接參考診斷，發現間接參考下的準確度平均下降7.0個百分點，在法律和倫理中上升至19.8個百分點。<lb/>實驗3將TS-Guessing行為探測器應用於所有513個問題和所有六個模型，發現72.5%的問題觸發的記憶信號遠高於隨機機率，其中DeepSeek-R1顯示出分佈式記憶特徵（76.6%部分重建，0%逐字回憶），這解釋了其異常的實驗2特徵。<lb/>所有三個實驗在同一污染排名上達成一致：STEM > 專業 > 社會科學 > 人文學科。

##### **360° Image Perception with MLLMs: A Comprehensive Benchmark and a Training-Free Method**
2603.16179v1 by Huyen T. T. Tran, Van-Quang Nguyen, Farros Alferro, Kang-Jun Liu, Takayuki Okatani

Multimodal Large Language Models (MLLMs) have shown impressive abilities in understanding and reasoning over conventional images. However, their perception of 360° images remains largely underexplored. Unlike conventional images, 360° images capture the entire surrounding environment, enabling holistic spatial reasoning but introducing challenges such as geometric distortion and complex spatial relations. To comprehensively assess MLLMs' capabilities to perceive 360° images, we introduce 360Bench, a Visual Question Answering (VQA) benchmark featuring 7K-resolution 360° images, seven representative (sub)tasks with annotations carefully curated by human annotators. Using 360Bench, we systematically evaluate seven MLLMs and six enhancement methods, revealing their shortcomings in 360° image perception. To address these challenges, we propose Free360, a training-free scene-graph-based framework for high-resolution 360° VQA. Free360 decomposes the reasoning process into modular steps, applies adaptive spherical image transformations to 360° images tailored to each step, and seamlessly integrates the resulting information into a unified graph representation for answer generation. Experiments show that Free360 consistently improves its base MLLM and provides a strong training-free solution for 360° VQA tasks. The source code and dataset will be publicly released upon acceptance.

摘要：多模態大型語言模型（MLLMs）在理解和推理傳統圖像方面展現了令人印象深刻的能力。<lb/>然而，它們對360°圖像的感知仍然在很大程度上未被探索。<lb/>與傳統圖像不同，360°圖像捕捉整個周圍環境，使得整體空間推理成為可能，但也引入了幾何失真和複雜空間關係等挑戰。<lb/>為了全面評估MLLMs對360°圖像的感知能力，我們推出了360Bench，一個視覺問題回答（VQA）基準，特徵為7K解析度的360°圖像，以及七個具有經人類標註者精心策劃的代表性（子）任務的註釋。<lb/>利用360Bench，我們系統地評估了七個MLLMs和六種增強方法，揭示了它們在360°圖像感知方面的不足。<lb/>為了解決這些挑戰，我們提出了Free360，一個無需訓練的基於場景圖的高解析度360° VQA框架。<lb/>Free360將推理過程分解為模塊化步驟，對每個步驟量身定制地應用自適應球形圖像變換到360°圖像，並將結果信息無縫整合到統一的圖形表示中以生成答案。<lb/>實驗表明，Free360持續改善其基礎MLLM，並為360° VQA任務提供了一個強大的無需訓練的解決方案。<lb/>源代碼和數據集將在接受後公開發布。

##### **STARK: Spatio-Temporal Attention for Representation of Keypoints for Continuous Sign Language Recognition**
2603.16163v1 by Suvajit Patra, Soumitra Samanta

Continuous Sign Language Recognition (CSLR) is a crucial task for understanding the languages of deaf communities. Contemporary keypoint-based approaches typically rely on spatio-temporal encoding, where spatial interactions among keypoints are modeled using Graph Convolutional Networks or attention mechanisms, while temporal dynamics are captured using 1D convolutional networks. However, such designs often introduce a large number of parameters in both the encoder and the decoder. This paper introduces a unified spatio-temporal attention network that computes attention scores both spatially (across keypoints) and temporally (within local windows), and aggregates features to produce a local context-aware spatio-temporal representation. The proposed encoder contains approximately $70-80\%$ fewer parameters than existing state-of-the-art models while achieving comparable performance to keypoint-based methods on the Phoenix-14T dataset.

摘要：連續手語識別（CSLR）是理解聾人社群語言的一項重要任務。當代基於關鍵點的方法通常依賴於時空編碼，其中關鍵點之間的空間互動是通過圖卷積網絡或注意力機制來建模的，而時間動態則是通過一維卷積網絡來捕捉的。然而，這樣的設計通常會在編碼器和解碼器中引入大量參數。本文介紹了一種統一的時空注意力網絡，該網絡在空間上（跨關鍵點）和時間上（在局部窗口內）計算注意力分數，並聚合特徵以生成局部上下文感知的時空表示。所提出的編碼器的參數大約比現有的最先進模型少 $70-80\%$，同時在Phoenix-14T數據集上達到了與基於關鍵點的方法相當的性能。

##### **SIA: A Synthesize-Inject-Align Framework for Knowledge-Grounded and Secure E-commerce Search LLMs with Industrial Deployment**
2603.16137v2 by Zhouwei Zhai, Mengxiang Chen, Anmeng Zhang

Large language models offer transformative potential for e-commerce search by enabling intent-aware recommendations. However, their industrial deployment is hindered by two critical challenges: (1) knowledge hallucination due to insufficient encoding of dynamic, fine-grained product knowledge, and (2) security vulnerabilities under jailbreak attacks that threaten compliance. To address these issues, we propose SIA--a Synthesize-Inject-Align framework for building knowledgeable and secure e-commerce search LLMs. Our approach first synthesizes high-quality natural language corpus by combining structured knowledge graphs with unstructured behavioral logs, augmented with reasoning chains and safety-aware data. We then introduce a parameter-efficient pre-training strategy based on Depth Up-Scaling to inject domain knowledge while preserving general capabilities. Finally, a dual-path alignment method via multi-task instruction tuning and adversarial training strengthens both task performance and safety robustness. The framework has been deployed at JD.com, China's largest self-operated e-commerce platform, where A/B tests across five core search scenarios demonstrate significant improvements in key business metrics, validating its industrial effectiveness and scalability.

摘要：大型語言模型為電子商務搜索提供了變革性的潛力，能夠實現意圖感知的推薦。<lb/>然而，它們的工業部署受到兩個關鍵挑戰的阻礙：（1）由於動態、細粒度產品知識的編碼不足而導致的知識幻覺，以及（2）在越獄攻擊下的安全漏洞，這威脅到合規性。<lb/>為了解決這些問題，我們提出了SIA——一個用於構建知識豐富且安全的電子商務搜索LLMs的合成-注入-對齊框架。<lb/>我們的方法首先通過將結構化知識圖與非結構化行為日誌相結合，並增強推理鏈和安全意識數據，合成高質量的自然語言語料庫。<lb/>然後，我們引入了一種基於深度上升的參數高效預訓練策略，以在保留一般能力的同時注入領域知識。<lb/>最後，通過多任務指令調整和對抗訓練的雙路徑對齊方法，加強了任務性能和安全穩健性。<lb/>該框架已在中國最大的自營電子商務平台JD.com上部署，並在五個核心搜索場景中進行A/B測試，顯示出關鍵業務指標的顯著改善，驗證了其工業效能和可擴展性。

##### **SWE-QA-Pro: A Representative Benchmark and Scalable Training Recipe for Repository-Level Code Understanding**
2603.16124v1 by Songcheng Cai, Zhiheng Lyu, Yuansheng Ni, Xiangchao Chen, Baichuan Zhou, Shenzhe Zhu, Yi Lu, Haozhe Wang, Chi Ruan, Benjamin Schneider, Weixu Zhang, Xiang Li, Andy Zheng, Yuyu Zhang, Ping Nie, Wenhu Chen

Agentic repository-level code understanding is essential for automating complex software engineering tasks, yet the field lacks reliable benchmarks. Existing evaluations often overlook the long tail topics and rely on popular repositories where Large Language Models (LLMs) can cheat via memorized knowledge. To address this, we introduce SWE-QA-Pro, a benchmark constructed from diverse, long-tail repositories with executable environments. We enforce topical balance via issue-driven clustering to cover under-represented task types and apply a rigorous difficulty calibration process: questions solvable by direct-answer baselines are filtered out. This results in a dataset where agentic workflows significantly outperform direct answering (e.g., a ~13-point gap for Claude Sonnet 4.5), confirming the necessity of agentic codebase exploration. Furthermore, to tackle the scarcity of training data for such complex behaviors, we propose a scalable synthetic data pipeline that powers a two-stage training recipe: Supervised Fine-Tuning (SFT) followed by Reinforcement Learning from AI Feedback (RLAIF). This approach allows small open models to learn efficient tool usage and reasoning. Empirically, a Qwen3-8B model trained with our recipe surpasses GPT-4o by 2.3 points on SWE-QA-Pro and substantially narrows the gap to state-of-the-art proprietary models, demonstrating both the validity of our evaluation and the effectiveness of our agentic training workflow.

摘要：代理庫層級的程式碼理解對於自動化複雜的軟體工程任務至關重要，但該領域缺乏可靠的基準。<lb/>現有的評估往往忽略了長尾主題，並依賴於大型語言模型（LLMs）可以透過記憶知識作弊的熱門庫。<lb/>為了解決這個問題，我們引入了SWE-QA-Pro，一個由多樣化的長尾庫和可執行環境構建的基準。<lb/>我們通過以問題為驅動的聚類來強制主題平衡，以涵蓋被低估的任務類型，並應用嚴格的難度校準過程：可以通過直接回答基準解決的問題將被過濾掉。<lb/>這導致了一個數據集，其中代理工作流程顯著超越了直接回答（例如，Claude Sonnet 4.5的差距約為13分），確認了代理程式碼庫探索的必要性。<lb/>此外，為了應對這種複雜行為的訓練數據稀缺，我們提出了一個可擴展的合成數據管道，驅動一個兩階段的訓練配方：監督微調（SFT）隨後是來自AI反饋的強化學習（RLAIF）。<lb/>這種方法使小型開放模型能夠學習有效的工具使用和推理。<lb/>實證上，使用我們的配方訓練的Qwen3-8B模型在SWE-QA-Pro上超越了GPT-4o 2.3分，並大幅縮小了與最先進專有模型的差距，證明了我們評估的有效性和我們代理訓練工作流程的有效性。

##### **PathGLS: Evaluating Pathology Vision-Language Models without Ground Truth through Multi-Dimensional Consistency**
2603.16113v1 by Minbing Chen, Zhu Meng, Fei Su

Vision-Language Models (VLMs) offer significant potential in computational pathology by enabling interpretable image analysis, automated reporting, and scalable decision support. However, their widespread clinical adoption remains limited due to the absence of reliable, automated evaluation metrics capable of identifying subtle failures such as hallucinations. To address this gap, we propose PathGLS, a novel reference-free evaluation framework that assesses pathology VLMs across three dimensions: Grounding (fine-grained visual-text alignment), Logic (entailment graph consistency using Natural Language Inference), and Stability (output variance under adversarial visual-semantic perturbations). PathGLS supports both patch-level and whole-slide image (WSI)-level analysis, yielding a comprehensive trust score. Experiments on Quilt-1M, TCGA, REG2025, PathMMU and TCGA-Sarcoma datasets demonstrate the superiority of PathGLS. Specifically, on the Quilt-1M dataset, PathGLS reveals a steep sensitivity drop of 40.2% for hallucinated reports compared to only 2.1% for BERTScore. Moreover, validation against expert-defined clinical error hierarchies reveals that PathGLS achieves a strong Spearman's rank correlation of $ρ=0.71$ ($p < 0.0001$), significantly outperforming Large Language Model (LLM)-based approaches (Gemini 3.0 Pro: $ρ=0.39$, $p < 0.0001$). These results establish PathGLS as a robust reference-free metric. By directly quantifying hallucination rates and domain shift robustness, it serves as a reliable criterion for benchmarking VLMs on private clinical datasets and informing safe deployment. Code can be found at: https://github.com/My13ad/PathGLS

摘要：視覺-語言模型（VLMs）在計算病理學中提供了顯著的潛力，能夠實現可解釋的影像分析、自動報告和可擴展的決策支持。<lb/>然而，由於缺乏能夠識別微妙失誤（如幻覺）的可靠自動評估指標，其廣泛的臨床應用仍然受到限制。<lb/>為了解決這一問題，我們提出了PathGLS，一種新穎的無參考評估框架，該框架從三個維度評估病理VLMs：基礎（細粒度視覺-文本對齊）、邏輯（使用自然語言推理的推理圖一致性）和穩定性（在對抗性視覺-語義擾動下的輸出方差）。<lb/>PathGLS支持補丁級和整片影像（WSI）級的分析，產生全面的信任分數。<lb/>在Quilt-1M、TCGA、REG2025、PathMMU和TCGA-肉瘤數據集上的實驗證明了PathGLS的優越性。<lb/>具體而言，在Quilt-1M數據集上，PathGLS顯示出幻覺報告的敏感度驟降40.2%，而BERTScore僅為2.1%。<lb/>此外，與專家定義的臨床錯誤層級進行驗證顯示，PathGLS達到了強大的斯皮爾曼等級相關性$ρ=0.71$（$p < 0.0001$），顯著超越了基於大型語言模型（LLM）的方法（Gemini 3.0 Pro: $ρ=0.39$, $p < 0.0001$）。<lb/>這些結果確立了PathGLS作為一種穩健的無參考指標。<lb/>通過直接量化幻覺率和領域轉移穩健性，它作為一個可靠的標準，用於在私有臨床數據集上基準測試VLMs並指導安全部署。<lb/>代碼可以在以下網址找到：https://github.com/My13ad/PathGLS

##### **VIGIL: Towards Edge-Extended Agentic AI for Enterprise IT Support**
2603.16110v1 by Sarthak Ahuja, Neda Kordjazi, Evren Yortucboylu, Vishaal Kapoor, Mariam Dundua, Yiming Li, Derek Ho, Vaibhavi Padala, Jennifer Whitted, Rebecca Steinert

Enterprise IT support is constrained by heterogeneous devices, evolving policies, and long-tail failure modes that are difficult to resolve centrally. We present VIGIL, an edge-extended agentic AI system that deploys desktop-resident agents to perform situated diagnosis, retrieval over enterprise knowledge, and policy-governed remediation directly on user devices with explicit consent and end-to-end observability. In a 10-week pilot of VIGIL's operational loop on 100 resource-constrained endpoints, VIGIL reduces interaction rounds by 39%, achieves at least 4 times faster diagnosis, and supports self-service resolution in 82% of matched cases. Users report excellent usability, high trust, and low cognitive workload across four validated instruments, with qualitative feedback highlighting transparency as critical for trust. Notably, users rated the system higher when no historical matches were available, suggesting on-device diagnosis provides value independent of knowledge base coverage. This pilot establishes safety and observability foundations for fleet-wide continuous improvement.

摘要：企業 IT 支援受到異質設備、變化中的政策以及難以集中解決的長尾故障模式的限制。我們提出了 VIGIL，一個邊緣擴展的代理 AI 系統，部署桌面常駐代理以執行情境診斷、企業知識檢索和在用戶設備上進行政策治理的修復，並獲得明確的同意和端對端的可觀察性。在對 100 個資源受限的終端進行的 VIGIL 操作循環的 10 週試點中，VIGIL 減少了 39% 的互動輪次，實現了至少 4 倍的更快診斷，並在 82% 的匹配案例中支持自助解決。用戶報告了出色的可用性、高信任度和低認知負擔，這是通過四個經過驗證的工具進行的，質性反饋強調透明度對信任至關重要。值得注意的是，當沒有歷史匹配可用時，用戶對系統的評價更高，這表明設備上的診斷提供了獨立於知識庫覆蓋範圍的價值。這次試點為整個車隊的持續改進建立了安全性和可觀察性的基礎。

##### **NANOZK: Layerwise Zero-Knowledge Proofs for Verifiable Large Language Model Inference**
2603.18046v1 by Zhaohui Geoffrey Wang

When users query proprietary LLM APIs, they receive outputs with no cryptographic assurance that the claimed model was actually used. Service providers could substitute cheaper models, apply aggressive quantization, or return cached responses - all undetectable by users paying premium prices for frontier capabilities. We present METHOD, a zero-knowledge proof system that makes LLM inference verifiable: users can cryptographically confirm that outputs correspond to the computation of a specific model.
  Our approach exploits the fact that transformer inference naturally decomposes into independent layer computations, enabling a layerwise proof framework where each layer generates a constant-size proof regardless of model width. This decomposition sidesteps the scalability barrier facing monolithic approaches and enables parallel proving. We develop lookup table approximations for non-arithmetic operations (softmax, GELU, LayerNorm) that introduce zero measurable accuracy loss, and introduce Fisher information-guided verification for scenarios where proving all layers is impractical.
  On transformer models up to d=128, METHOD generates constant-size layer proofs of 5.5KB (2.1KB attention + 3.5KB MLP) with 24 ms verification time. Compared to EZKL, METHOD achieves 70x smaller proofs and 5.7x faster proving time at d=128, while maintaining formal soundness guarantees (epsilon < 1e-37). Lookup approximations preserve model perplexity exactly, enabling verification without quality compromise.

摘要：當用戶查詢專有的 LLM API 時，他們獲得的輸出並沒有加密保證，無法確認所聲稱的模型實際上是否被使用。服務提供商可能會替換為更便宜的模型，應用激進的量化，或返回快取的回應——這些都無法被支付高價以獲得前沿能力的用戶檢測到。我們提出了 METHOD，一種零知識證明系統，使 LLM 推理可驗證：用戶可以加密地確認輸出對應於特定模型的計算。
我們的方法利用了Transformer推理自然分解為獨立層計算的事實，從而實現了一種逐層證明框架，其中每一層生成恆定大小的證明，無論模型寬度如何。這種分解繞過了單一方法面臨的可擴展性障礙，並實現了並行證明。我們為非算術操作（softmax、GELU、LayerNorm）開發了查找表近似，這些近似引入了零可測的準確性損失，並為在所有層的證明不切實際的情況引入了以 Fisher 信息為指導的驗證。
在 d=128 的Transformer模型上，METHOD 生成恆定大小的層證明為 5.5KB（2.1KB 注意力 + 3.5KB MLP），驗證時間為 24 毫秒。與 EZKL 相比，METHOD 在 d=128 時實現了 70 倍更小的證明和 5.7 倍更快的證明時間，同時保持正式的健全性保證（epsilon < 1e-37）。查找近似準確地保留了模型的困惑度，實現了無品質妥協的驗證。

##### **Machine intelligence supports the full chain of 2D dendrite synthesis**
2603.16959v1 by Wenqiang Huang, Susu Fang, Xuhang Gu, Shen'ao Xue, Huanhuan Xing, Junjie Jiang, Junying Zhang, Shen Zhou, Zheng Luo, Jin Zhang, Fangping Ouyang, Shanshan Wang

Exemplified by the chemical vapor deposition growth of two-dimensional dendrites, which has potential applications in catalysis and presents a parameter-intensive, data-scarce and reaction process-complex model problem, we devise a machine intelligence-empowered framework for the full chain support of material synthesis, encompassing rapid process optimization, accurate customized synthesis, and comprehensive mechanism deciphering.First, active learning is integrated into the experimental workflow, identifying an optimal recipe for the growth of highly-branched, electrocatalytically-active ReSe2 dendrites through 60 experiments (4 iterations), which account for less than 1.3% of the numerous possible parameter combinations.Then, a prediction accuracy-guided data augmentation strategy is developed combined with a tree-based machine learning (ML) algorithm, unveiling a non-linear correlation between 5 process variables and fractal dimension (DF) of ReSe2 dendrites with only 9 experiment additions, which guides the synthesis of various user-defined DF. Finally, we construct a data-knowledge dual-driven mechanism model by integration of cross-scale characterizations, interpretable ML models, and domain knowledge in thermodynamics and kinetics, unraveling synergistic contributions of multiple process parameters to the product morphology. This work demonstrates the ML potential to transform the research paradigm and is adaptable to broader material synthesis.

摘要：以化學氣相沉積生長二維樹狀晶體為例，這具有催化潛在應用，並呈現出一個參數密集、數據稀缺且反應過程複雜的模型問題，我們設計了一個機器智能賦能的框架，以全面支持材料合成，包括快速過程優化、準確的定制合成以及全面的機制解碼。<lb/>首先，將主動學習整合進實驗工作流程，通過60次實驗（4次迭代）確定出一個最佳配方，用於生長高度分支的電催化活性ReSe2樹狀晶體，這些實驗僅占眾多可能參數組合的1.3%以下。<lb/>然後，開發了一種以預測準確性為指導的數據增強策略，結合基於樹的機器學習（ML）算法，揭示了5個過程變量與ReSe2樹狀晶體的分形維度（DF）之間的非線性關聯，僅需增加9次實驗，這為合成各種用戶定義的DF提供了指導。<lb/>最後，我們通過整合跨尺度表徵、可解釋的ML模型以及熱力學和動力學的領域知識，構建了一個數據-知識雙驅動的機制模型，揭示了多個過程參數對產品形態的協同貢獻。<lb/>這項工作展示了ML在轉變研究範式方面的潛力，並且可適應於更廣泛的材料合成。

##### **CounterRefine: Answer-Conditioned Counterevidence Retrieval for Inference-Time Knowledge Repair in Factual Question Answering**
2603.16091v1 by Tianyi Huang, Ying Kai Deng

In factual question answering, many errors are not failures of access but failures of commitment: the system retrieves relevant evidence, yet still settles on the wrong answer. We present CounterRefine, a lightweight inference-time repair layer for retrieval-grounded question answering. CounterRefine first produces a short answer from retrieved evidence, then gathers additional support and conflicting evidence with follow-up queries conditioned on that draft answer, and finally applies a restricted refinement step that outputs either KEEP or REVISE, with proposed revisions accepted only if they pass deterministic validation. In effect, CounterRefine turns retrieval into a mechanism for testing a provisional answer rather than merely collecting more context. On the full SimpleQA benchmark, CounterRefine improves a matched GPT-5 Baseline-RAG by 5.8 points and reaches a 73.1 percent correct rate, while exceeding the reported one-shot GPT-5.4 score by roughly 40 points. These findings suggest a simple but important direction for knowledgeable foundation models: beyond accessing evidence, they should also be able to use that evidence to reconsider and, when necessary, repair their own answers.

摘要：在事實性問題回答中，許多錯誤並不是訪問失敗，而是承諾失敗：系統檢索到相關證據，但仍然選擇了錯誤的答案。我們提出了CounterRefine，一個輕量級的推理時修復層，用於基於檢索的問題回答。CounterRefine首先從檢索到的證據中生成一個簡短的答案，然後根據該草擬答案進行後續查詢，收集額外的支持和矛盾證據，最後應用一個限制性的修正步驟，輸出KEEP或REVISE，只有在通過確定性驗證的情況下，才接受提出的修訂。實際上，CounterRefine將檢索轉變為測試臨時答案的機制，而不僅僅是收集更多的上下文。在完整的SimpleQA基準測試中，CounterRefine將匹配的GPT-5 Baseline-RAG提高了5.8分，達到了73.1%的正確率，同時超過了報告中的一次性GPT-5.4分數約40分。這些發現表明了一個簡單但重要的方向，對於知識豐富的基礎模型來說：除了訪問證據，它們還應能夠利用這些證據重新考慮並在必要時修正自己的答案。

##### **PhysQuantAgent: An Inference Pipeline of Mass Estimation for Vision-Language Models**
2603.16958v1 by Hisayuki Yokomizo, Taiki Miyanishi, Yan Gang, Shuhei Kurita, Nakamasa Inoue, Yusuke Iwasawa

Vision-Language Models (VLMs) are increasingly applied to robotic perception and manipulation, yet their ability to infer physical properties required for manipulation remains limited. In particular, estimating the mass of real-world objects is essential for determining appropriate grasp force and ensuring safe interaction. However, current VLMs lack reliable mass reasoning capabilities, and most existing benchmarks do not explicitly evaluate physical quantity estimation under realistic sensing conditions. In this work, we propose PhysQuantAgent, a framework for real-world object mass estimation using VLMs, together with VisPhysQuant, a new benchmark dataset for evaluation. VisPhysQuant consists of RGB-D videos of real objects captured from multiple viewpoints, annotated with precise mass measurements. To improve estimation accuracy, we introduce three visual prompting methods that enhance the input image with object detection, scale estimation, and cross-sectional image generation to help the model comprehend the size and internal structure of the target object. Experiments show that visual prompting significantly improves mass estimation accuracy on real-world data, suggesting the efficacy of integrating spatial reasoning with VLM knowledge for physical inference.

摘要：視覺-語言模型（VLMs）越來越多地應用於機器人感知和操作，但它們推斷操作所需的物理性質的能力仍然有限。<lb/>特別是，估計現實世界物體的質量對於確定適當的抓取力和確保安全互動至關重要。<lb/>然而，目前的VLM缺乏可靠的質量推理能力，且大多數現有基準並未明確評估在現實感測條件下的物理量估計。<lb/>在這項工作中，我們提出了PhysQuantAgent，一個使用VLM進行現實世界物體質量估計的框架，以及VisPhysQuant，一個新的基準數據集以供評估。<lb/>VisPhysQuant包含來自多個視角捕捉的真實物體的RGB-D視頻，並附有精確的質量測量標註。<lb/>為了提高估計準確性，我們引入了三種視覺提示方法，通過物體檢測、比例估計和橫截面圖像生成來增強輸入圖像，以幫助模型理解目標物體的大小和內部結構。<lb/>實驗表明，視覺提示顯著提高了現實世界數據上的質量估計準確性，這表明將空間推理與VLM知識整合以進行物理推理的有效性。

##### **RadAnnotate: Large Language Models for Efficient and Reliable Radiology Report Annotation**
2603.16002v1 by Saisha Pradeep Shetty, Roger Eric Goldman, Vladimir Filkov

Radiology report annotation is essential for clinical NLP, yet manual labeling is slow and costly. We present RadAnnotate, an LLM-based framework that studies retrieval-augmented synthetic reports and confidence-based selective automation to reduce expert effort for labeling in RadGraph. We study RadGraph-style entity labeling (graph nodes) and leave relation extraction (edges) to future work. First, we train entity-specific classifiers on gold-standard reports and characterize their strengths and failure modes across anatomy and observation categories, with uncertain observations hardest to learn. Second, we generate RAG-guided synthetic reports and show that synthetic-only models remain within 1-2 F1 points of gold-trained models, and that synthetic augmentation is especially helpful for uncertain observations in a low-resource setting, improving F1 from 0.61 to 0.70. Finally, by learning entity-specific confidence thresholds, RadAnnotate can automatically annotate 55-90% of reports at 0.86-0.92 entity match score while routing low-confidence cases for expert review.

摘要：放射科報告的註解對臨床自然語言處理至關重要，但手動標註既緩慢又昂貴。<lb/>我們提出了RadAnnotate，一個基於大型語言模型的框架，研究增強檢索的合成報告和基於信心的選擇性自動化，以減少在RadGraph中標註所需的專家努力。<lb/>我們研究RadGraph風格的實體標註（圖形節點），並將關係提取（邊）留待未來工作。<lb/>首先，我們在金標準報告上訓練實體特定分類器，並描述它們在解剖學和觀察類別中的優勢和失敗模式，其中不確定的觀察最難學習。<lb/>其次，我們生成RAG引導的合成報告，並顯示僅使用合成模型的F1分數仍然在金標訓練模型的1-2個點之內，並且合成增強對於低資源環境中不確定的觀察特別有幫助，F1從0.61提高到0.70。<lb/>最後，通過學習實體特定的信心閾值，RadAnnotate可以自動註解55-90%的報告，實體匹配分數為0.86-0.92，同時將低信心案例路由給專家審查。

