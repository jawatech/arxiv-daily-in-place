
### Knowledge Graphs
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-07-20**|**It's Not What You Say, It's How You Say It: Evaluating LLM Responses to Expressions of Belief**|Kevin Du et.al.|[2607.18232v1](http://arxiv.org/abs/2607.18232v1)|null|
|**2026-07-20**|**O-VAD: Industrial Video Anomaly Detection through Object-Centric Tracking and Reasoning**|Mei Yuan et.al.|[2607.18142v1](http://arxiv.org/abs/2607.18142v1)|null|
|**2026-07-20**|**SGA: Plug&Play Geometric Verification for Educational Video Synthesis**|Lopez Jhon et.al.|[2607.18116v1](http://arxiv.org/abs/2607.18116v1)|null|
|**2026-07-20**|**LLM-as-a-Coach: Experiential Learning for Non-Verifiable Tasks**|Tianzhu Ye et.al.|[2607.18110v1](http://arxiv.org/abs/2607.18110v1)|null|
|**2026-07-20**|**Natural Language Access to Domain-Specific Metadata: A Reusable Framework for LLM Query Generation**|Blake G. Fitch et.al.|[2607.18029v1](http://arxiv.org/abs/2607.18029v1)|null|
|**2026-07-20**|**Rethinking Heterogeneous LLM Merging: A Weighted Model Averaging Perspective**|Jiahe Fan et.al.|[2607.18026v1](http://arxiv.org/abs/2607.18026v1)|null|
|**2026-07-20**|**DeLIVeR: Decomposed Learning for Information-grounded Veracity Recognition via Reinforced Knowledge Graph Exploration**|Cong Hoan Nguyen et.al.|[2607.17935v1](http://arxiv.org/abs/2607.17935v1)|null|
|**2026-07-20**|**PEARL: Auditable Repair for Scientific Reasoning Graph Extraction**|Bohan Su et.al.|[2607.17917v1](http://arxiv.org/abs/2607.17917v1)|null|
|**2026-07-20**|**Benchmarking Resource-Efficient LLMs for Research Topic Ontology Generation in the Biomedical Field**|Tanay Aggarwal et.al.|[2607.17902v1](http://arxiv.org/abs/2607.17902v1)|null|
|**2026-07-20**|**Stress Testing Concept Erasure with Large Language Model Agents**|Yuyang Xue et.al.|[2607.17890v1](http://arxiv.org/abs/2607.17890v1)|null|
|**2026-07-20**|**Exploratory and Assimilating Reflection: Reflective Recall Cycle for Long-term Memory**|Ganesh Senrayan et.al.|[2607.17879v1](http://arxiv.org/abs/2607.17879v1)|null|
|**2026-07-20**|**When a Name Is Not a Name: A Benchmark Dataset and Distilled Reasoning for Culturally Entangled Bangla Homographs in Low-Resource LLMs**|Md. Asaduzzaman Shuvo et.al.|[2607.17828v1](http://arxiv.org/abs/2607.17828v1)|null|
|**2026-07-20**|**Financial Audit Assistance using Misinformation Detection and Explanation**|Kshitij Madhav Jadhav et.al.|[2607.17797v1](http://arxiv.org/abs/2607.17797v1)|null|
|**2026-07-20**|**FIFA World Cup 2026 as a Contamination-Free Benchmark for LLM Forecasting Agents: Four Models, a Bookmaker, and 104 Matches**|Jiacheng Ding et.al.|[2607.17765v1](http://arxiv.org/abs/2607.17765v1)|null|
|**2026-07-20**|**Semantically Similar, Logically Distinct: Diagnosing the Semantic-Answerability Gap in Table RAG**|Jiaming Tian et.al.|[2607.17742v1](http://arxiv.org/abs/2607.17742v1)|null|
|**2026-07-20**|**Large Language Models for Citation Function Classification**|Daniel Vodička et.al.|[2607.17738v1](http://arxiv.org/abs/2607.17738v1)|null|
|**2026-07-20**|**SR-Agent: An Experience-Driven Agentic Framework for Post-Ranking Strategies Refinement in E-Commerce Recommendation**|Hanchen Yang et.al.|[2607.17719v1](http://arxiv.org/abs/2607.17719v1)|null|
|**2026-07-20**|**Integrating High-Level Requirements to Low-Level Tests with Machine-Readable V&V Specifications**|Mansur Arief et.al.|[2607.17686v1](http://arxiv.org/abs/2607.17686v1)|null|
|**2026-07-20**|**Selectivity Matters: Source Node Influence Pruning for Unsupervised Graph Domain Adaptation**|Ridong Han et.al.|[2607.17668v1](http://arxiv.org/abs/2607.17668v1)|null|
|**2026-07-20**|**Trustworthy Protein-Ligand Binding Affinity Prediction via Reliability-Aware Multi-Engine Fusion**|Yongchan Hong et.al.|[2607.17601v1](http://arxiv.org/abs/2607.17601v1)|null|
|**2026-07-20**|**Reinforcement Learning: From Algorithms To Foundation Models**|Zihan Ding et.al.|[2607.17560v1](http://arxiv.org/abs/2607.17560v1)|null|
|**2026-07-20**|**Human-in-the-Loop User Feedback Affects Perceived Accuracy and Trust, but Task Subjectivity Matters**|Donald R. Honeycutt et.al.|[2607.17548v1](http://arxiv.org/abs/2607.17548v1)|null|
|**2026-07-20**|**D-NOVA: In-Storage Retrieval Accelerator via Dual-Bound 3D NAND-Optimized Similarity Search with Vector Adaptation**|Chang Eun Song et.al.|[2607.17538v1](http://arxiv.org/abs/2607.17538v1)|null|
|**2026-07-20**|**Salience Induction against Multi-Hop RAG Agents: Threat and Defense**|Xingfu Zhou et.al.|[2607.17535v1](http://arxiv.org/abs/2607.17535v1)|null|
|**2026-07-20**|**Pailitao-MMSearch: Building Native E-Commerce Multimodal Search Foundation**|Xiaohan Ye et.al.|[2607.17499v1](http://arxiv.org/abs/2607.17499v1)|null|
|**2026-07-20**|**Panache: One-Pass Motif Discovery at Every Window Length**|Tej Sanibh Ranade et.al.|[2607.17481v1](http://arxiv.org/abs/2607.17481v1)|null|
|**2026-07-20**|**HyCoRec: Hypergraph-Enhanced Multi-Preference Learning for Alleviating Matthew Effect in Conversational Recommendation**|Yongsen Zheng et.al.|[2607.17461v1](http://arxiv.org/abs/2607.17461v1)|null|
|**2026-07-19**|**Agentic ERP: Multi-Agent Large Language Model Architecture for Autonomous Enterprise Resource Planning**|Zhihao Liu et.al.|[2607.17331v1](http://arxiv.org/abs/2607.17331v1)|null|
|**2026-07-19**|**AIGB-R1: Self-Evolving Generative Auto-Bidding via Hierarchical Planner-Executor Optimization**|Yuejia Dou et.al.|[2607.17281v1](http://arxiv.org/abs/2607.17281v1)|null|
|**2026-07-19**|**Safety That Does Not Transfer: Cross-Lingual Clinical Correctness Drift in Deployable Medical Language Models**|Anthonio Oladimeji Gabriel et.al.|[2607.17270v1](http://arxiv.org/abs/2607.17270v1)|null|
|**2026-07-19**|**An Explicit World Model Based on Data-First Ontology: DaoQL Multimodal Storage Validation and Counterfactual Reasoning Evaluation**|Zhanbo Li et.al.|[2607.17269v1](http://arxiv.org/abs/2607.17269v1)|null|
|**2026-07-19**|**Debate-on-Graph: Reliable and Adaptive Reasoning of Large Language Model on Uncertain Knowledge Graph**|Peiji Yu et.al.|[2607.17266v1](http://arxiv.org/abs/2607.17266v1)|null|
|**2026-07-19**|**Distilled Reinforcement Learning for LLM Post-training**|Chen Wang et.al.|[2607.17247v1](http://arxiv.org/abs/2607.17247v1)|null|
|**2026-07-19**|**Toward Anthropomorphic Dialogue: A Closed-Loop Framework for Human-Like Chat Generation, Evaluation, and Preference Alignment**|Wentao Liu et.al.|[2607.17191v1](http://arxiv.org/abs/2607.17191v1)|null|
|**2026-07-19**|**SlotGuard: Stop Oversharing Private Local Context in LLM Agent Transcri**|Haocheng Xia et.al.|[2607.17147v1](http://arxiv.org/abs/2607.17147v1)|null|
|**2026-07-19**|**Teach it to stop, not just to click**|Barada Sahu et.al.|[2607.17136v1](http://arxiv.org/abs/2607.17136v1)|null|
|**2026-07-19**|**Otap:Structure-Aware Optimal Transport for Evaluating Planning and Execution in Agent Trajectories**|Babak Barazandeh et.al.|[2607.17082v1](http://arxiv.org/abs/2607.17082v1)|null|
|**2026-07-19**|**Bridging the Information Gap: Semantic Densification and Hindsight Distillation for Cold-Start Prediction**|Hao Duong Le et.al.|[2607.17070v1](http://arxiv.org/abs/2607.17070v1)|null|
|**2026-07-19**|**Learning from Synthetic Data without Model Collapse in Iterative Instruction Tuning**|Xiaonan Luo et.al.|[2607.17043v1](http://arxiv.org/abs/2607.17043v1)|null|
|**2026-07-19**|**Reward-Driven LLM Agent Workflows: Synthesizing POMDP Routing and Self-Correction for Autonomous Decision-Making**|Amez Amanj Ali et.al.|[2607.17038v1](http://arxiv.org/abs/2607.17038v1)|null|
|**2026-07-18**|**CADENCE: Closing the Reasoning Gap via Coverage-Adaptive On-Policy Distillation**|Satyam Kumar et.al.|[2607.16955v1](http://arxiv.org/abs/2607.16955v1)|null|
|**2026-07-18**|**Beyond Semantic Equivalence: Logical Graphs for LLM Uncertainty Quantification**|Yanni Dong et.al.|[2607.16868v1](http://arxiv.org/abs/2607.16868v1)|null|
|**2026-07-18**|**AgentBrew: Lifelong Knowledge Brewing from Strong Teachers to Weak LLM Agents**|Yangqin Jiang et.al.|[2607.16851v1](http://arxiv.org/abs/2607.16851v1)|null|
|**2026-07-18**|**From Overload to Insights: How AI Agents Can Support Scientists in Analyzing Complex Data**|Tim Fuchs et.al.|[2607.16845v1](http://arxiv.org/abs/2607.16845v1)|null|
|**2026-07-18**|**Spatiotemporal Facial Action Unit Detection using Twin Cycle Autoencoders for Driver Monitoring**|Sai Sidharth D et.al.|[2607.16760v1](http://arxiv.org/abs/2607.16760v1)|null|
|**2026-07-18**|**Constraint-Anchored Reasoning Traces**|Zehua Cheng et.al.|[2607.16727v1](http://arxiv.org/abs/2607.16727v1)|null|
|**2026-07-18**|**Though Language Models Err While They Strive: Conformal Prediction for Self-Correcting Scientific Generation**|Mingqiao Mo et.al.|[2607.16704v1](http://arxiv.org/abs/2607.16704v1)|null|
|**2026-07-18**|**Dependency-Guided Code Generation: Structured Matrix Decomposition and Consistency-Guided Refinement**|Mingqiao Mo et.al.|[2607.16692v1](http://arxiv.org/abs/2607.16692v1)|null|
|**2026-07-18**|**DataFlow-Harness: A Grounded Code-Agent Platform for Constructing Editable LLM Data Pipelines**|Runming He et.al.|[2607.16617v1](http://arxiv.org/abs/2607.16617v1)|null|
|**2026-07-18**|**NOWJ@COLIEE 2026: Adaptive Pipelines for Legal Retrieval and Reasoning**|Thuong-Hieu Ngo et.al.|[2607.16603v1](http://arxiv.org/abs/2607.16603v1)|null|
|**2026-07-18**|**Exact Network Surgery: Functional Invariance and Gradient Plasticity in Reactive Computational Graphs**|Abdallah Khemais et.al.|[2607.16568v1](http://arxiv.org/abs/2607.16568v1)|null|
|**2026-07-17**|**One Modality to Forget Them All: Enhancing Cross-Modal Unlearning in Vision-Language Models**|Sudharshan Balaji et.al.|[2607.16442v1](http://arxiv.org/abs/2607.16442v1)|null|
|**2026-07-17**|**Signal-based Model Access Risk Analysis for AI System Operations Security**|Maria Mahbub et.al.|[2607.16414v1](http://arxiv.org/abs/2607.16414v1)|null|
|**2026-07-17**|**Automated Hardware Validation Test Plan Generation for Large Scale AI Datacenter Platforms Using a Generative AI Multi-Agents Architecture**|Mohammed-Khalil Ghali et.al.|[2607.16388v1](http://arxiv.org/abs/2607.16388v1)|null|
|**2026-07-17**|**ToolSciVer: Multimodal Scientific Claim Verification with Visual Tool Augmented Reinforcement Learning**|Binglin Zhou et.al.|[2607.16131v1](http://arxiv.org/abs/2607.16131v1)|null|
|**2026-07-17**|**HCIG: A Hierarchical Cross-Modal Incongruity Graph Network for Multimodal Sarcasm and Cyberbullying Detection**|Bhavana Verma et.al.|[2607.16076v1](http://arxiv.org/abs/2607.16076v1)|null|
|**2026-07-17**|**Frontier AI performance across the business disciplines: a case-grounded benchmark of knowledge work and analytical reasoning**|Ajay Patel et.al.|[2607.16057v1](http://arxiv.org/abs/2607.16057v1)|null|
|**2026-07-17**|**Rethinking Quantum Continual Learning with Quantum Fisher Information**|Yu-Chao Hsu et.al.|[2607.16030v1](http://arxiv.org/abs/2607.16030v1)|null|
|**2026-07-17**|**Candidate Attended Dialogue State Tracking Using BERT**|Junyuan Zheng et.al.|[2607.16021v1](http://arxiv.org/abs/2607.16021v1)|null|
|**2026-07-17**|**When Not to Automate: A Formal Protocol for Human Preservation in AI-Optimized Organizations**|Jose Manuel de la Chica Rodriguez et.al.|[2607.15944v1](http://arxiv.org/abs/2607.15944v1)|null|
|**2026-07-17**|**On the Failure of Boundary-Seeking Distillation in Bottlenecked Generative Architectures**|Mohamed Amine Kina et.al.|[2607.15919v1](http://arxiv.org/abs/2607.15919v1)|null|
|**2026-07-17**|**An MLIR-Based Compilation Method for Large Language Models**|Pengchao Hu et.al.|[2607.15865v1](http://arxiv.org/abs/2607.15865v1)|null|
|**2026-07-17**|**Test-Time Noise Guided Adaptation for Realistic Autoregressive Video Generation**|Dimitrios Karageorgiou et.al.|[2607.15849v1](http://arxiv.org/abs/2607.15849v1)|null|
|**2026-07-17**|**CAMMAR: Culture-Aware Matryoshka for Metaphorical Arabic Representations**|Suzan Awinat et.al.|[2607.15847v1](http://arxiv.org/abs/2607.15847v1)|null|
|**2026-07-17**|**Knowledge-Centric Agents for Workflow Generation**|Zhendong Li et.al.|[2607.15845v1](http://arxiv.org/abs/2607.15845v1)|null|
|**2026-07-17**|**RTL-Sequencer: Towards Scalable RTL Timing Prediction with the Sequence-based Paradigm**|Ziyan Guo et.al.|[2607.15830v1](http://arxiv.org/abs/2607.15830v1)|null|
|**2026-07-17**|**Knowledge-Assisted Multi-Graph Dependency Learning for Multivariate Time Series Anomaly Detection in Multi-Stage Industrial Processes**|Jaeyeong Lee et.al.|[2607.15799v1](http://arxiv.org/abs/2607.15799v1)|null|
|**2026-07-17**|**NeurOWL: An LLM-Based Neural-symbolic Framework for Incomplete OWL Ontology Reasoning**|Hui Yang et.al.|[2607.15776v1](http://arxiv.org/abs/2607.15776v1)|null|
|**2026-07-17**|**S1-Omni: A Unified Multimodal Reasoning Model for Scientific Understanding, Prediction, and Generation**|Jiahao Zhao et.al.|[2607.15686v1](http://arxiv.org/abs/2607.15686v1)|null|
|**2026-07-17**|**ToolVerse: Unlocking Massive Environments and Long-Horizon Tasks for Agentic Reinforcement Learning**|Shuaiyu Zhou et.al.|[2607.15660v1](http://arxiv.org/abs/2607.15660v1)|null|
|**2026-07-17**|**AEGIS: Assay-Aware Protocol Validation and Runtime Monitoring for Open-Source Liquid Handling Robots**|Priyanka V. Setty et.al.|[2607.15620v1](http://arxiv.org/abs/2607.15620v1)|null|
|**2026-07-17**|**MGDT: MLLM-Guided Diffusion Transformer with Relation-Adaptive Mixture-of-Experts for Multimodal Knowledge Graph Completion**|Xu Hou et.al.|[2607.15592v1](http://arxiv.org/abs/2607.15592v1)|null|
|**2026-07-17**|**SkillCorpus: Consolidating and Evaluating the Open Skill Ecosystem for Real-World LLM Agents**|Yanze Wang et.al.|[2607.15557v2](http://arxiv.org/abs/2607.15557v2)|null|
|**2026-07-17**|**Cache-Aware Prompt Compression:A Two-Tier Cost Model for LLM API Caching**|Yan Song et.al.|[2607.15516v1](http://arxiv.org/abs/2607.15516v1)|null|
|**2026-07-16**|**An Auto-Scaling Approach for Serverless Environments Based on a Multi-Expert Consensus Mechanism**|Mobina Kashaniyan et.al.|[2607.15511v1](http://arxiv.org/abs/2607.15511v1)|null|
|**2026-07-16**|**FLINT: Fingerprinting Federated Learning Architectures from 5G PHY-Layer Side Channels**|Md Nahid Hasan Shuvo et.al.|[2607.15469v1](http://arxiv.org/abs/2607.15469v1)|null|
|**2026-07-16**|**Partition, Prompt, Aggregate: Statistical Self-Consistency in Language Models**|Patrik Wolf et.al.|[2607.15277v1](http://arxiv.org/abs/2607.15277v1)|null|
|**2026-07-16**|**SearchOS-V1: Towards Robust Open-Domain Information-Seeking Agent Collaboration**|Yuyao Zhang et.al.|[2607.15257v1](http://arxiv.org/abs/2607.15257v1)|null|
|**2026-07-16**|**AutoSynthesis: An agentic system for automated meta-analysis**|Moein Taherinezhad et.al.|[2607.15247v1](http://arxiv.org/abs/2607.15247v1)|null|
|**2026-07-16**|**Linear representations of grammaticality in neural language models**|Jane Li et.al.|[2607.15175v1](http://arxiv.org/abs/2607.15175v1)|null|
|**2026-07-16**|**The Industrialization of Research ; On AI-Driven Science and Its Consequences**|Emmanuel Jeannot et.al.|[2607.15164v1](http://arxiv.org/abs/2607.15164v1)|null|
|**2026-07-16**|**BrainPilot: Automating Brain Discovery with Agentic Research**|Haoxuan Li et.al.|[2607.15079v2](http://arxiv.org/abs/2607.15079v2)|null|
|**2026-07-16**|**OmniaBench: Benchmarking General AI Agents Across Diverse Scenarios**|Chengyu Shen et.al.|[2607.14989v1](http://arxiv.org/abs/2607.14989v1)|null|
|**2026-07-16**|**Show Me How You Reason and I'll Tell You Who You Are: Reasoning Graphs for Robust LLM Authorship Attribution**|Zlata Kikteva et.al.|[2607.14905v2](http://arxiv.org/abs/2607.14905v2)|null|
|**2026-07-16**|**Reachability-Aware Pretraining for Efficient Target-Oriented Path Exploration in Temporal Knowledge Graph Reasoning**|Chien-Liang Liu et.al.|[2607.14886v1](http://arxiv.org/abs/2607.14886v1)|null|
|**2026-07-16**|**Transcoders for Investigating Deception in Language Models**|Darius Lim et.al.|[2607.14791v1](http://arxiv.org/abs/2607.14791v1)|null|
|**2026-07-16**|**AI vs Human Expert Reasoning: Assessing Agreements in Building Typology Predictions based on Street View Imagery**|Zahratu Shabrina et.al.|[2607.14756v1](http://arxiv.org/abs/2607.14756v1)|null|
|**2026-07-16**|**Pretraining Multiple Instance Learning Networks with Multi-Teacher Distillation from Pathology Slide Foundation Models**|Mingxi Fu et.al.|[2607.14703v1](http://arxiv.org/abs/2607.14703v1)|null|
|**2026-07-16**|**SmartRAG: Native Graph-Based RAG for Mobile Device**|Zhihan Jiang et.al.|[2607.14661v2](http://arxiv.org/abs/2607.14661v2)|null|
|**2026-07-16**|**TopoAgent: A Self-Evolving Topological Agent for Multimodal Scientific Reasoning**|Mingze Xu et.al.|[2607.14658v1](http://arxiv.org/abs/2607.14658v1)|null|
|**2026-07-16**|**Routing Ceilings Are Domain-Independent: Structural Prior Injection in Code Security Vulnerability Detection**|Manuel Israel Cázares et.al.|[2607.14628v1](http://arxiv.org/abs/2607.14628v1)|null|
|**2026-07-16**|**Bad Memory: Evaluating Prompt Injection Risks from Memory in Agentic Systems**|Soham Gadgil et.al.|[2607.14611v1](http://arxiv.org/abs/2607.14611v1)|null|
|**2026-07-16**|**MathCoPilot: An Interactive System for Human-AI Symbiotic Paradigm of Mathematical Research**|Junjie Zhang et.al.|[2607.14582v1](http://arxiv.org/abs/2607.14582v1)|null|
|**2026-07-16**|**MARS: Multi-hop Adaptive Retrieval and SPARQL Generation for KGQA**|Nikit Srivastava et.al.|[2607.14561v1](http://arxiv.org/abs/2607.14561v1)|null|
|**2026-07-16**|**CityLLM: A framework for natural-language querying of semantic 3D city models**|Rabindra Lamsal et.al.|[2607.14542v1](http://arxiv.org/abs/2607.14542v1)|null|
|**2026-07-16**|**Are LLM-Generated GPU Kernels Production-Ready? A Trace-Driven Benchmark and Optimization Agent**|Lingyun Yang et.al.|[2607.14541v1](http://arxiv.org/abs/2607.14541v1)|null|
|**2026-07-16**|**VTM-Nav: Hierarchical Visual-Topological Memory for Cross-Episode Object-Goal Navigation**|Xiaoran Xu et.al.|[2607.14514v1](http://arxiv.org/abs/2607.14514v1)|null|
|**2026-07-16**|**RetroAgent: Harnessing LLMs to Search Over Structured Memory for Agentic Retrosynthesis Planning**|Yanqiao Zhu et.al.|[2607.14512v1](http://arxiv.org/abs/2607.14512v1)|null|
|**2026-07-16**|**VLT: A Vision-Language-Time Series Multimodal Foundation Model for Industrial Intelligence**|Haiteng Wang et.al.|[2607.14510v1](http://arxiv.org/abs/2607.14510v1)|null|
|**2026-07-16**|**Non-vacuous Generalization Bounds for Reinforcement Learning with Verifiable Rewards**|Yuxuan Zhu et.al.|[2607.14506v1](http://arxiv.org/abs/2607.14506v1)|null|

#### Abstracts
##### **It's Not What You Say, It's How You Say It: Evaluating LLM Responses to Expressions of Belief**
2607.18232v1 by Kevin Du, Clara Kümpel, Michelle Wastl, Alex Warstadt

Users frequently express their beliefs to large language models (LLMs). In some situations, the LLM should accept these contextual beliefs as true. In others, they should stick to their prior knowledge. Notably, users' expressions of belief (EoBs) can take linguistically diverse forms - using presuppositions, evidential and certainty markers, or varied tones - each of which may have a different persuasiveness over the LLMs. We introduce a typology to systematically evaluate how different EoBs affect whether models follow context versus prior knowledge. The typology is grounded in four linguistically motivated dimensions: form, evidentiality, epistemic stance, and tone, spanning 17 fine-grained types. By pairing these EoBs with world knowledge facts, we generate controlled EoB-query pairs that isolate the effect of linguistic variation. Using this benchmark, we evaluate 16 LLMs that differ in architecture (Llama3, Qwen3, Gemma3), scale (1B-30B parameters), and training stages (base vs instruct). We identify meaningful variations in response behavior across these axes, e.g., that bigger models and instruction models tend to be less context-following than smaller models and base models. We further identify specific EoBs that statistically significantly persuade LMs more consistently than others. Our work reveals systematic patterns in how linguistic framing affects LLM context integration, with implications for prompt engineering and model robustness.

摘要：使用者經常向大型語言模型（LLMs）表達他們的信念。在某些情況下，LLM 應該接受這些上下文信念為真。在其他情況下，它們應該堅持其先前的知識。值得注意的是，使用者的信念表達（EoBs）可以採取語言上多樣的形式——使用預設、證據和確定性標記或不同的語調——每種形式對 LLM 的說服力可能有所不同。我們介紹了一種類型學，以系統地評估不同的 EoBs 如何影響模型遵循上下文與先前知識的選擇。這種類型學基於四個語言動機的維度：形式、證據性、認知立場和語調，涵蓋 17 種細緻的類型。通過將這些 EoBs 與世界知識事實配對，我們生成控制的 EoB-查詢對，以隔離語言變異的影響。利用這一基準，我們評估了 16 個在架構（Llama3、Qwen3、Gemma3）、規模（1B-30B 參數）和訓練階段（基礎 vs 指導）上有所不同的 LLM。我們在這些軸上識別出反應行為的有意義變化，例如，較大的模型和指導模型往往比較小的模型和基礎模型更不遵循上下文。我們進一步識別出一些特定的 EoBs 在統計上更一致地說服 LMs。 我們的工作揭示了語言框架如何影響 LLM 上下文整合的系統性模式，對提示工程和模型穩健性具有啟示意義。

##### **O-VAD: Industrial Video Anomaly Detection through Object-Centric Tracking and Reasoning**
2607.18142v1 by Mei Yuan, Qi Long, Qifeng Wu, Zhenyang Li, Yizhou Zhao, Lei Wang, Yang Liu, Min Xu

Industrial Video Anomaly Detection (IVAD) aims to identify anomalous objects and events in an industrial process, which is crucial for modern manufacturing and quality control systems. Existing VLM-based anomaly reasoning methods are capable of detecting open-ended anomalies in general domains. However, their performance declines in industrial settings characterized by intricate object transformations, strict physics, and procedural constraints. To tackle the complexity of such interaction-intensive detection, we introduce a training-free agentic framework for anomaly detection free of domain-specific knowledge, emphasizing object state evolution like humans inspectors. It is designed to track spatial-temporal dynamics and underlying transformations of detected objects over time, and then reason over the object-wise temporal state trajectories to identify abnormal objects in grounded frames. Our method overcomes limitations of prior approaches that rely on retraining on normal clips or injecting domain knowledge as context for test-time inference. Extensive experiments on three IVAD datasets demonstrate that our method outperforms frontier VLMs, agentic frameworks, and traditional VAD methods fine-tuned on the respective datasets, while providing interpretable reports over anomaly processes and types.

摘要：工業視頻異常檢測（IVAD）旨在識別工業過程中的異常物體和事件，這對於現代製造和質量控制系統至關重要。現有的基於VLM的異常推理方法能夠檢測一般領域中的開放式異常。然而，它們在特徵複雜的工業環境中的性能下降，這些環境以複雜的物體轉換、嚴格的物理規範和程序約束為特徵。為了應對這種互動密集型檢測的複雜性，我們引入了一種無需訓練的代理框架，用於異常檢測，無需特定領域的知識，強調物體狀態的演變，類似於人類檢查員。該框架旨在跟踪檢測物體的空間-時間動態及其潛在轉變，然後對物體的時間狀態軌跡進行推理，以識別基於實體框架的異常物體。我們的方法克服了先前依賴於正常片段重訓練或將領域知識作為測試時推理上下文的限制。在三個IVAD數據集上的廣泛實驗表明，我們的方法在性能上超越了前沿的VLM、代理框架和在各自數據集上微調的傳統VAD方法，同時提供了對異常過程和類型的可解釋報告。

##### **SGA: Plug&Play Geometric Verification for Educational Video Synthesis**
2607.18116v1 by Lopez Jhon, Hinojosa Carlos, Ghanem Bernard

Recent work leverages Large Language Models (LLMs) to generate executable code for pedagogical animations using libraries such as Manim. However, ensuring spatial correctness and visual legibility remains challenging, as existing frameworks emphasize pedagogical content while overlooking geometric occlusions. We propose the Symbolic Geometric Agent (SGA), a plug-and-play module for code-centric animation pipelines that intercepts LLM-generated code, performs partial execution to extract symbolic scene graphs, and applies targeted refinement when spatial conflicts are detected. We further introduce the Manim Visual Quality Score (MVQS), a deterministic rendering-free proxy for spatial integrity. Experiments on the MMMC-Code benchmark across four LLM backbones and two agentic pipelines show that SGA achieves a peak MVQS of 73.11 (Code2Video + GPT-5.1), corresponding to a 16.1% relative improvement over the raw baseline, and improves MVQS in 7 of 8 backbone x pipeline configurations.

摘要：最近的研究利用大型語言模型（LLMs）生成可執行代碼，用於使用 Manim 等庫的教學動畫。然而，確保空間正確性和視覺可讀性仍然具有挑戰性，因為現有框架強調教學內容而忽略了幾何遮擋。我們提出了符號幾何代理（SGA），這是一個即插即用的模塊，用於以代碼為中心的動畫管道，能夠攔截 LLM 生成的代碼，執行部分操作以提取符號場景圖，並在檢測到空間衝突時應用針對性的精煉。我們進一步介紹了 Manim 視覺質量分數（MVQS），這是一個確定性的無渲染代理，用於空間完整性。在四個 LLM 骨幹和兩個代理管道的 MMMC-Code 基準測試中進行的實驗顯示，SGA 達到了 73.11 的峰值 MVQS（Code2Video + GPT-5.1），這相當於相對於原始基線的 16.1% 改進，並在 8 個骨幹 x 管道配置中的 7 個中改善了 MVQS。

##### **LLM-as-a-Coach: Experiential Learning for Non-Verifiable Tasks**
2607.18110v1 by Tianzhu Ye, Li Dong, Guanheng Chen, He Zhu, Xun Wu, Shaohan Huang, Furu Wei

Reinforcement learning (RL) on open-ended tasks compresses an LLM's rubric-based evaluation into a scalar reward, discarding rich textual feedback and conflating responses with distinct quality profiles. We propose Experiential Learning (EL), which repurposes the feedback model from an LLM-as-a-Judge into an LLM-as-a-Coach. The coach distills its assessment of each on-policy response into transferable experiential knowledge, which conditions a teacher model and is internalized by the policy through on-policy context distillation. Compared with scalar rewards, this higher-bandwidth feedback channel provides dense supervision and preserves fine-grained preferences among high-quality responses. Across two policy families, with feedback from the policy itself or a proprietary model, EL consistently outperforms rubric-based RL on held-out and unseen open-ended tasks. Notably, EL generalizes better beyond the training distribution, and mitigates reward hacking. These findings establish experiential knowledge as a richer and more generalizable learning signal for post-training on non-verifiable tasks.

摘要：強化學習（RL）在開放式任務上將大型語言模型的基於評分標準的評估壓縮為一個標量獎勵，捨棄了豐富的文本反饋，並將回應與不同的質量特徵混淆。我們提出了經驗學習（EL），它將大型語言模型作為評審的反饋模型重新用於大型語言模型作為教練。教練將其對每個在政策下回應的評估提煉為可轉移的經驗知識，這些知識通過在政策下的上下文蒸餾來調節教師模型並被政策內化。與標量獎勵相比，這個更高帶寬的反饋通道提供了密集的監督，並保留了高質量回應之間的細微偏好。在兩個政策家族中，無論是來自政策本身的反饋還是專有模型的反饋，EL 在保留的和未見的開放式任務上始終優於基於評分標準的強化學習。值得注意的是，EL 在訓練分佈之外的泛化能力更強，並減輕了獎勵駭客的問題。這些發現確立了經驗知識作為一種更豐富且更具可泛化性的學習信號，用於在不可驗證任務上的後訓練。

##### **Natural Language Access to Domain-Specific Metadata: A Reusable Framework for LLM Query Generation**
2607.18029v1 by Blake G. Fitch, Cato Elia Kurtz

Researchers need to answer ad-hoc questions about the contents of domain-specific archives but often lack the expertise to write structured queries on the metadata. We show that when domain vocabulary and semantics are captured in a well-designed Web Ontology Language (OWL) ontology, Large Language Models (LLMs) can generate accurate structured queries zero-shot, without fine-tuning, retrieval augmentation, or multi-agent orchestration. We present the Natural Language Knowledge Graph Query (NLKGQ) system, a framework and development process that enables natural language access to metadata in such archives. The framework includes a web interface that helps researchers pose natural language questions, which a domain-agnostic harness translates to SPARQL via an LLM and executes against a knowledge graph. The development process begins with capturing domain vocabulary and semantics in a formal OWL ontology. Domain-specific code then extracts metadata from archive sources and imports it into a knowledge graph defined by the ontology. Both are designed for reuse across domains. We demonstrate the system on metadata derived from a large-scale neuroimaging research archive, evaluating multiple LLMs and ontology representations. The best configurations achieve 100% accuracy on a competence and regression question set developed with domain experts. An ablation study across eight ontology representations reveals that readable entity names and semantic annotations are the dominant factors in accuracy, more significant than model choice or prompt engineering. We also compare SPARQL to an auto-generated SQL database as query backends, showing that OWL's structural features provide a substantial advantage over SQL DDL for LLM-driven query generation. Our demonstration domain also requires local LLMs on modest institutional hardware to address privacy concerns for human subject data.

摘要：研究人員需要回答有關特定領域檔案內容的臨時問題，但往往缺乏撰寫結構化查詢的專業知識。我們顯示，當領域詞彙和語義在精心設計的網路本體語言（OWL）本體中被捕捉時，大型語言模型（LLMs）可以在零次學習的情況下生成準確的結構化查詢，而無需微調、檢索增強或多代理協調。我們提出了自然語言知識圖譜查詢（NLKGQ）系統，這是一個框架和開發過程，使研究人員能夠自然語言訪問這些檔案中的元數據。該框架包括一個網頁介面，幫助研究人員提出自然語言問題，這些問題通過一個與領域無關的工具轉換為SPARQL，並在知識圖譜上執行。開發過程始於在正式的OWL本體中捕捉領域詞彙和語義。然後，特定領域的代碼從檔案來源提取元數據，並將其導入由本體定義的知識圖譜。這兩者都旨在跨領域重用。我們在從大型神經影像研究檔案中衍生的元數據上演示了該系統，評估多個LLMs和本體表示。最佳配置在與領域專家共同開發的能力和回歸問題集上達到100%的準確率。對八個本體表示進行的消融研究顯示，可讀的實體名稱和語義註釋是準確性的主導因素，這比模型選擇或提示工程更為重要。我們還將SPARQL與自動生成的SQL數據庫作為查詢後端進行比較，顯示OWL的結構特徵在LLM驅動的查詢生成中提供了相對於SQL DDL的顯著優勢。我們的演示領域還需要在適度的機構硬體上運行本地LLMs，以解決人類受試者數據的隱私問題。

##### **Rethinking Heterogeneous LLM Merging: A Weighted Model Averaging Perspective**
2607.18026v1 by Jiahe Fan, Yinghao Hou, Si Chen, Aiyuan Zhang, Hong Xie, Defu Lian

Can large language models with substantially different parameter spaces be merged by direct weighted averaging, without training or semantic alignment? Existing heterogeneous fusion methods typically introduce distillation, adapters, learned latent spaces, routing, or feature alignment, leaving open whether a simpler recipe can work for genuinely different billion-parameter checkpoints. We revisit this counterintuitive question through training-free dimensional adaptation followed by ratio-controlled interpolation. In union-style merging, we expand the smaller model into the larger parameter space; in intersection-style merging, we truncate the larger model into the smaller parameter space. Across Qwen-family model pairs and benchmarks covering mathematical reasoning, code generation, language understanding, commonsense reasoning, knowledge, and instruction following, deterministic expansion largely preserves the source model function, and small-ratio interpolation can improve over strong source checkpoints by transferring complementary capabilities. However, near-balanced interpolation often collapses, and task-level results reveal a seesaw effect in which gains on some capabilities coexist with regressions on others. These results show that simple parameter averaging, when paired with lightweight dimensional adaptation and carefully controlled ratios, is a surprisingly strong baseline for heterogeneous LLM merging, suggesting that the limits of direct weighted fusion may also bound what more complex heterogeneous merging methods can achieve at scale.

摘要：大型語言模型是否可以通過直接加權平均進行合併，而無需訓練或語義對齊？現有的異構融合方法通常引入蒸餾、適配器、學習的潛在空間、路由或特徵對齊，尚未解決一個問題，即對於真正不同的十億參數檢查點，是否可以使用更簡單的方案。我們通過無需訓練的維度適應，隨後進行比率控制的插值，重新審視這一反直覺的問題。在聯合式合併中，我們將較小的模型擴展到較大的參數空間；在交集式合併中，我們將較大的模型截斷到較小的參數空間。在涵蓋數學推理、代碼生成、語言理解、常識推理、知識和指令跟隨的 Qwen 系列模型對及基準測試中，確定性擴展在很大程度上保留了源模型的功能，而小比率插值可以通過轉移互補能力來超越強大的源檢查點。然而，近乎平衡的插值往往會崩潰，任務層面的結果顯示出一種跷跷板效應，即某些能力的增益與其他能力的回退並存。這些結果表明，簡單的參數平均，當與輕量級的維度適應和精心控制的比率相結合時，是一個出乎意料的強基準，用於異構 LLM 的合併，這表明直接加權融合的限制可能也會約束更複雜的異構合併方法在大規模上的成就。

##### **DeLIVeR: Decomposed Learning for Information-grounded Veracity Recognition via Reinforced Knowledge Graph Exploration**
2607.17935v1 by Cong Hoan Nguyen, Thomas Hoang, Hieu Minh Duong, Long Nguyen

Automated fact-checking remains a challenge for Large Language Models (LLMs) due to "query brittleness" in traditional retrieval systems. We propose DeLIVeR (Decomposed Learning for Information-grounded Veracity Recognition), a framework that treats evidence retrieval as a reinforced strategic exploration task. DeLIVeR utilizes a Planner LLM to decompose complex claims into targeted question sets, which are used to traverse structured Knowledge Graphs (KGs) for high-precision evidence. We optimize the Planner's policy using Group Relative Policy Optimization (GRPO) with a reward system prioritizing structural diversity and verdict accuracy. Our evaluation on LIAR, FEVER, and PolitiFact shows that DeLIVeR significantly outperforms state-of-the-art baselines. Using Qwen2.5-7B, our framework achieved peak F1-scores of 83.73, 84.57, and 79.70 respectively, representing a 10-15% improvement over HippoRAG2. By shifting to a reinforced question-planning strategy, DeLIVeR effectively bridges multi-hop reasoning gaps and provides an auditable, transparent path for verifiable misinformation detection.

摘要：自動化事實查核對大型語言模型（LLMs）來說仍然是一個挑戰，因為傳統檢索系統存在「查詢脆弱性」。我們提出了DeLIVeR（分解學習以進行信息基礎的真實性識別），這是一個將證據檢索視為增強策略探索任務的框架。DeLIVeR利用計劃者LLM將複雜的主張分解為針對性的問題集，這些問題集用於遍歷結構化知識圖譜（KGs）以獲取高精度的證據。我們使用群體相對政策優化（GRPO）來優化計劃者的政策，並建立一個獎勵系統，優先考慮結構多樣性和判決準確性。我們在LIAR、FEVER和PolitiFact上的評估顯示，DeLIVeR顯著超越了最先進的基準。使用Qwen2.5-7B，我們的框架達到了83.73、84.57和79.70的峰值F1分數，分別比HippoRAG2提高了10-15%。通過轉向增強的問題規劃策略，DeLIVeR有效地彌補了多跳推理的空白，並提供了一條可審計、透明的路徑以檢測可驗證的錯誤信息。

##### **PEARL: Auditable Repair for Scientific Reasoning Graph Extraction**
2607.17917v1 by Bohan Su, Pengze Li, Yuchen Lu, Xi Chen

Scientific Reasoning Graph Extraction (SRGE) aims to recover explicit links among observations, evidence, intermediate claims, and paper-level conclusions. LLMs can produce graph-like scientific explanations, but their outputs often mix malformed syntax, drifting edge labels, incorrectly oriented roots, and weak source anchors. We propose PEARL (Peircean Extraction via Abstraction and Repair Layer), a training-free framework that turns noisy LLM graph responses into auditable reasoning graphs and repairs them toward strict semantic validity. PEARL first materializes explicit graph content under a closed Peircean schema, then uses matched evidence-grounded judge feedback to repair rejected edge types, local inference steps, and terminal roots while preserving an audit trail. On five 70-paper model archives from ARCHE, a benchmark for latent reasoning-chain extraction, PEARL raises strict gate passes from 0/350 for the LLM baseline to 300/350, with average REA improving from 0.339 to 0.906. The graphs provide a reliability layer for research-agent and AI scientist workflows that need inspectable reasoning traces rather than unconstrained graph regeneration. Code and audit artifacts are available at https://github.com/BohanSu/auditable-repair-reasoning-graphs/tree/300-350_workshop .

摘要：科學推理圖提取（SRGE）旨在恢復觀察、證據、中介主張和論文級結論之間的明確聯繫。大型語言模型（LLMs）可以生成類似圖形的科學解釋，但其輸出往往混合了不良的語法、漂移的邊標籤、錯誤定向的根部和薄弱的來源錨點。我們提出了PEARL（透過抽象和修復層的皮爾士提取），這是一個無需訓練的框架，將嘈雜的LLM圖形響應轉換為可審計的推理圖，並朝著嚴格的語義有效性進行修復。PEARL首先在封閉的皮爾士架構下具現化明確的圖形內容，然後使用匹配的基於證據的評判反饋來修復被拒絕的邊類型、本地推理步驟和終端根，同時保留審計記錄。在來自ARCHE的五個70篇論文模型檔案上，作為潛在推理鏈提取的基準，PEARL將嚴格的通過率從LLM基線的0/350提升至300/350，平均REA從0.339提高至0.906。這些圖形為需要可檢查推理痕跡而非不受限制圖形再生的研究代理和AI科學家工作流程提供了可靠性層。代碼和審計文檔可在https://github.com/BohanSu/auditable-repair-reasoning-graphs/tree/300-350_workshop 獲得。

##### **Benchmarking Resource-Efficient LLMs for Research Topic Ontology Generation in the Biomedical Field**
2607.17902v1 by Tanay Aggarwal, Angelo Salatino, Francesco Osborne, Enrico Motta

Knowledge Organization Systems like Ontologies and taxonomies are fundamental for structuring scientific knowledge, yet their manual curation presents a persistent bottleneck in knowledge management. While Large Language Models (LLMs) offer a scalable mechanism for automated ontology generation, their capacity to classify complex, domain-specific semantics requires systematic evaluation. In this paper, we assess the performance of five small, open-source LLMs (up to 9 billion parameters) in identifying semantic relationships between biomedical concepts. To support this evaluation, we introduce MeSH-Rel-4K, a dataset comprising 4K semantic relationships extracted from the Medical Subject Headings (MeSH). We analyse three adaptation strategies: standard prompting, Chain-of-Thought prompting, and fine-tuning. While parameter-constrained models traditionally struggle with the nuances of in-context logic, our results reveal that targeted fine-tuning increases the average F1-score by 34.1 percentage points. These results confirm that direct fine-tuning effectively exceeds the reasoning bottlenecks of smaller LLMs, providing an accurate, automated methodology for the construction and evolution of specialised biomedical ontologies.

摘要：知識組織系統，如本體論和分類法，對於結構化科學知識至關重要，但其手動整理在知識管理中仍然是一個持續的瓶頸。雖然大型語言模型（LLMs）提供了一種可擴展的自動本體生成機制，但它們在分類複雜的領域特定語義方面的能力需要系統評估。在本文中，我們評估了五個小型開源LLM（最多90億個參數）在識別生物醫學概念之間語義關係的表現。為了支持這一評估，我們引入了MeSH-Rel-4K，一個由4K語義關係組成的數據集，這些關係是從醫學主題標題（MeSH）中提取的。我們分析了三種適應策略：標準提示、思維鏈提示和微調。雖然受參數限制的模型傳統上在上下文邏輯的細微差別上掙扎，但我們的結果顯示，針對性的微調將平均F1分數提高了34.1個百分點。這些結果確認，直接微調有效地超越了小型LLM的推理瓶頸，提供了一種準確的自動化方法，用於構建和演變專業的生物醫學本體。

##### **Stress Testing Concept Erasure with Large Language Model Agents**
2607.17890v1 by Yuyang Xue, Feng Chen, Zhihua Liu, Edward Moroshko, Jingyu Sun, Steven McDonagh, Sotirios A. Tsaftaris

Concept erasure aims to remove semantic concepts from a trained generative model and is increasingly important for responsible AI deployment. However, verifying whether a model has robustly removed targeted concepts remains a critical challenge. Existing evaluation methods are typically pre-defined and static, failing to expose vulnerabilities under diverse natural-language probes and challenging conditions. Moreover, manually designed evaluation strategies can be biased and difficult to scale. We posit that concept erasure evaluation is best formulated as an adaptive hypothesis search, operationalised by agents that iteratively propose, critique, and verify tests to systematically expand coverage of failure modes. To this end, we propose Stress Testing Agents for Concept Erasure (STACE), a framework that autonomously stress-tests concept-erased models using multiple Large Language Model (LLM) agents, by iteratively generating and verifying stress-testing hypotheses grounded by external knowledge. We also introduce a suite of metrics for assessing the performance and efficiency of LLM-agent-powered stress-testing frameworks. Our extensive experiments show that STACE outperforms five LLM-based evaluation baselines on four concept categories. Further analysis across two T2I models, six concept erasure approaches, and various erasure strengths show that STACE is robust for different settings. We also show that STACE can be adapted beyond concept erasure evaluation to other problem domains, such as LLM jailbreaking. Our code is available anonymously.

摘要：概念抹除旨在從訓練過的生成模型中移除語義概念，並且在負責任的人工智慧部署中變得越來越重要。<lb/>然而，驗證模型是否穩健地移除了目標概念仍然是一個關鍵挑戰。<lb/>現有的評估方法通常是預先定義的和靜態的，無法在多樣的自然語言探測和挑戰條件下揭示脆弱性。<lb/>此外，手動設計的評估策略可能存在偏見且難以擴展。<lb/>我們認為，概念抹除評估最好被表述為一種自適應假設搜索，由代理人操作，這些代理人不斷提出、批評和驗證測試，以系統性地擴展失敗模式的覆蓋範圍。<lb/>為此，我們提出了概念抹除壓力測試代理（STACE），這是一個框架，通過多個大型語言模型（LLM）代理自主地對概念抹除模型進行壓力測試，通過不斷生成和驗證基於外部知識的壓力測試假設。<lb/>我們還介紹了一套指標，用於評估LLM代理驅動的壓力測試框架的性能和效率。<lb/>我們的廣泛實驗表明，STACE在四個概念類別上超越了五個基於LLM的評估基準。<lb/>對兩個T2I模型、六種概念抹除方法和各種抹除強度的進一步分析顯示，STACE在不同設置下都具有穩健性。<lb/>我們還顯示，STACE可以適應於概念抹除評估之外的其他問題領域，例如LLM越獄。<lb/>我們的代碼可匿名獲取。

##### **Exploratory and Assimilating Reflection: Reflective Recall Cycle for Long-term Memory**
2607.17879v1 by Ganesh Senrayan, Moyuru Yamada, Ishan Jindal, Kiran Purohit

LLM-based autonomous agents require external memory to overcome their statelessness and limited context window for long-term interaction and dynamic knowledge reasoning. However, existing memory retrieval methods often lack adaptability and sample efficiency, and struggle to retrieve the right mixture of memories from heterogeneous stores. We propose Exploratory-Assimilating Reflection (EAR), a framework for high initial retrieval performance and sample-efficient adaptation. EAR combines two mechanisms: Exploratory Reflection, which performs iterative search to bootstrap retrieval and collect useful experiences for each query, and Assimilating Reflection, which replays these experiences from an Experience Buffer to refine a global reranker more efficiently than methods relying only on immediate rewards. Experiments show that EAR improves retrieval by up to 17.9% over the baseline retriever on two long-term dialogue benchmarks. We also show that EAR is highly sample-efficient and robust to noisy feedback.

摘要：LLM 基礎的自主代理需要外部記憶來克服其無狀態性和有限的上下文窗口，以便進行長期互動和動態知識推理。<lb/>然而，現有的記憶檢索方法往往缺乏適應性和樣本效率，並且難以從異質存儲中檢索到正確的記憶混合。<lb/>我們提出了探索性同化反思（EAR），這是一個高初始檢索性能和樣本高效適應的框架。<lb/>EAR 結合了兩種機制：探索性反思，它通過迭代搜索來啟動檢索並為每個查詢收集有用的經驗；以及同化反思，它從經驗緩衝區重播這些經驗，以比僅依賴即時獎勵的方法更高效地完善全局重排器。<lb/>實驗表明，EAR 在兩個長期對話基準上使檢索性能比基線檢索器提高了最多 17.9%。<lb/>我們還顯示了 EAR 在樣本效率和對噪聲反饋的穩健性方面表現優異。

##### **When a Name Is Not a Name: A Benchmark Dataset and Distilled Reasoning for Culturally Entangled Bangla Homographs in Low-Resource LLMs**
2607.17828v1 by Md. Asaduzzaman Shuvo

Many Bangla words are at once personal names and culturally loaded common nouns, "Maya" is both a girl's name and a word for affectionate compassion. Choosing the right reading demands cultural knowledge that is scarce in the pretraining data of modern language models. We introduce Culturally Entangled Homograph (CEH) disambiguation and build a Bangla benchmark of 1,516 expert-verified sentences (3,032 labelled occurrences) in which one word appears twice with two distinct readings, each labelled with a culturally grounded category and an explanation of the reasoning behind it. Across open- and closed-source models, we find a systematic dominant-meaning bias: models default to the common-noun sense and overlook the name. A Bangla-specific model fails under every prompting regime we test, showing that language-specific pretraining alone does not confer cultural grounding. We further show that contrastive chain-of-thought prompting can sharply reduce this bias without training, and that distilling cultural explanations teaches small (1-3B) models to reason toward the correct reading rather than memorise labels, cutting dominant-meaning bias from as high as 100% to under 5% and turning the failed Bangla-specific model into our strongest system. Dataset and code are available at https://github.com/ashuvo25/BanglaCEH.

摘要：許多孟加拉語單詞同時是個人名字和文化負載的普通名詞，「Maya」既是女孩的名字，也是表達親切同情的詞語。選擇正確的讀音需要文化知識，而這在現代語言模型的預訓練數據中是稀缺的。我們引入了文化交織同形異義詞（CEH）消歧義，並建立了一個包含1,516個專家驗證句子的孟加拉基準（3,032個標記的出現），其中一個單詞出現兩次，具有兩種不同的讀音，每個讀音都標記有文化基礎的類別及其背後的推理解釋。在開源和閉源模型中，我們發現了一種系統性的主導意義偏見：模型默認使用普通名詞的意義，忽略了名字。我們測試的每種提示方式下，孟加拉語特定模型都失敗，顯示僅僅進行語言特定的預訓練並不能賦予文化基礎。我們進一步顯示對比性思維鏈提示可以在不進行訓練的情況下顯著減少這種偏見，並且提煉文化解釋可以教導小型（1-3B）模型推理正確的讀音，而不是僅僅記憶標籤，將主導意義偏見從高達100%降低到5%以下，並使失敗的孟加拉語特定模型變成我們最強的系統。數據集和代碼可在 https://github.com/ashuvo25/BanglaCEH 獲得。

##### **Financial Audit Assistance using Misinformation Detection and Explanation**
2607.17797v1 by Kshitij Madhav Jadhav, Sushodhan Vaishampayan, Manoj Apte, Sachin Pawar, Nitin Ramrakhiyani, Girish Keshav Palshikar

Financial statements (FS) such as Balance Sheet (BS), Income Statement (IS) and Cash-flow Statement (CS) summarize the annual financial performance of a company. FS are widely used for evaluating corporate governance, credit appraisal, risk analysis, validate taxation, make investment decisions etc. Financial auditing is a complex and knowledge-intensive discipline whose one important aim is ensuring integrity, accuracy, fairness and absence of material misstatement in the published FS. Given the importance of FS, there are incentives to hide, omit or falsify information to misrepresent the true financial health of the company; e.g., reduce tax liabilities, or increase investor confidence. Given the complex, time-consuming and expertise-dependent nature of auditing, auditors would benefit from an AI-assisted system that automatically detects instances of misinformation in the given FS and identify likely sources of this misinformation in the financial data. In this paper, we present unsupervised techniques to identify misinformation in FS, and also generate explanations as to the financial variables that are likely sources of misinformation. The auditor can then explore in more detail the associated data sources and business processes to validate these suggestions. A crucial feature of our approach is the use of past corpus of FS and associated audit reports to generate insights, which help in providing assistance. We demonstrate the efficacy of these techniques on a large corpus of 11,460 FS over 5 years and associated audit reports. This paper integrates and adds more novel contributions over the previously reported research (Shinde et al., 2022)\cite{SVAP22}, (Vaishampayan et al., 2022)\cite{VSPP22}, (Pawar et al., 2023)\cite{PAPV23}, which we have used as the foundation for our AI-assisted Auditor Assistance system.

摘要：財務報表（FS），如資產負債表（BS）、損益表（IS）和現金流量表（CS），總結了一家公司的年度財務表現。FS 被廣泛用於評估公司治理、信用評估、風險分析、驗證稅務、做出投資決策等。財務審計是一個複雜且知識密集的學科，其一個重要目標是確保已發布的 FS 的完整性、準確性、公平性及不存在重大錯報。考慮到 FS 的重要性，存在隱瞞、遺漏或偽造信息以誤導公司真實財務狀況的動機；例如，減少稅務負擔或提高投資者信心。鑒於審計的複雜性、耗時性和專業依賴性，審計師將受益於一種 AI 輔助系統，該系統能自動檢測給定 FS 中的信息錯誤實例，並識別財務數據中這些錯誤信息的可能來源。本文提出無監督技術來識別 FS 中的信息錯誤，並生成有關可能的信息錯誤來源的財務變數的解釋。然後，審計師可以更詳細地探索相關數據來源和業務流程，以驗證這些建議。我們方法的一個關鍵特徵是利用過去的 FS 語料庫和相關的審計報告來生成見解，這有助於提供支持。我們在一個包含 11,460 個 FS 和相關審計報告的龐大語料庫上展示了這些技術的有效性，涵蓋了 5 年的時間。本文整合並增添了比之前報告的研究（Shinde et al., 2022）\cite{SVAP22}、（Vaishampayan et al., 2022）\cite{VSPP22}、（Pawar et al., 2023）\cite{PAPV23} 更具創新性的貢獻，這些研究已作為我們 AI 輔助審計師輔助系統的基礎。

##### **FIFA World Cup 2026 as a Contamination-Free Benchmark for LLM Forecasting Agents: Four Models, a Bookmaker, and 104 Matches**
2607.17765v1 by Jiacheng Ding, Cong Guo, Jason Xu

We introduce WC2026-Agents, a benchmark and dataset for evaluating large language models (LLMs) as autonomous forecasting agents on real, future events. For every one of the 104 matches of the 2026 FIFA World Cup, four frontier models -- Claude Opus 4.8, ChatGPT (GPT-5.5, high reasoning), Gemini 3.1 Pro, and Grok (Expert Mode) -- ran an identical search-act-reflect loop: gather evidence with a web tool, commit to a 1X2 (team-A win / draw / team-B win) distribution and a virtual 100-USD bet, and, after the match, reflect given only the final score. Because every match kicked off after the models' training cutoffs, the benchmark is contamination-free by construction. Crucially, we pair the four agents with a fifth competitor drawn from the same information environment -- the pre-match betting market -- collected as per-match 1X2 odds, giving an economically grounded baseline and letting us score not just what an agent predicts but what it does with money. The release contains 416 forecasts and 414 reflections with verbatim reasoning, ground truth (including penalty shootouts), odds, and a reproducible evaluation suite. A reference evaluation surfaces findings that raw accuracy hides: the four agents issue an identical top pick in 92% of matches and none beats the market's Brier score; indeed, a naive flat stake on the market favorite out-earns all four agents. Yet the agents diverge sharply as decision-makers: betting return-on-investment ranges from -18% to +10%, fading the market is unprofitable for all four, the share of forecasts that cite the market ranges from 12% to 100%, and self-reported error rates on wrong picks range from 36% to 86%. The benchmark thus measures calibration, decision quality, and self-knowledge -- axes on which frontier models differ even when their predictions do not. Data and code: https://github.com/graphuofm/FIFA2026LLM

摘要：我們介紹 WC2026-Agents，一個用於評估大型語言模型 (LLMs) 作為自主預測代理的基準和數據集，針對真實的未來事件。對於 2026 年國際足總世界杯的每一場 104 場比賽，四個前沿模型 -- Claude Opus 4.8、ChatGPT (GPT-5.5，高推理)、Gemini 3.1 Pro 和 Grok (專家模式) -- 執行了一個相同的搜索-行動-反思循環：使用網絡工具收集證據，承諾進行 1X2 (隊伍-A 勝 / 平局 / 隊伍-B 勝) 分佈和虛擬 100 美元的投注，並在比賽結束後僅根據最終比分進行反思。由於每場比賽都是在模型訓練截止日期之後開始的，因此基準在結構上是無污染的。關鍵是，我們將這四個代理與來自相同信息環境的第五個競爭者配對 -- 賽前博彩市場 -- 根據每場比賽的 1X2 賠率收集，提供一個經濟基礎的基準，讓我們不僅能評分代理的預測，還能評估它們如何使用資金。此次發佈包含 416 個預測和 414 個反思，附有逐字推理、真實數據（包括點球大戰）、賠率和可重複的評估套件。參考評估顯示了原始準確性隱藏的發現：四個代理在 92% 的比賽中發出相同的首選，且沒有一個超越市場的 Brier 分數；事實上，對市場熱門的天真平注投注的收益超過了所有四個代理。然而，作為決策者，這些代理的表現差異顯著：投注的投資回報率範圍從 -18% 到 +10%，對市場的投注對所有四個代理來說都是不盈利的，引用市場的預測比例範圍從 12% 到 100%，而對錯誤選擇的自報錯誤率範圍從 36% 到 86%。因此，基準測量了校準、決策質量和自我認知 -- 這些是前沿模型即使在預測不一致時也有所不同的軸心。數據和代碼：https://github.com/graphuofm/FIFA2026LLM

##### **Semantically Similar, Logically Distinct: Diagnosing the Semantic-Answerability Gap in Table RAG**
2607.17742v1 by Jiaming Tian, Liyao Li, Wentao Ye, Haobo Wang, Lihua Yu, Zujie Ren, Gang Chen, Junbo Zhao

Tables are a critical knowledge source in retrieval-augmented generation (RAG), but a retrieved table may lack sufficient evidence to answer a query, a property we call answerability. While answerability broadly concerns whether a source or collection of sources contains sufficient evidence, retrieval models optimized for semantic relevance do not guarantee it even in the single-source case, creating a fundamental mismatch. To study this, we introduce TCR-Bench, a diagnostic benchmark for Table Content-level Answerability in RAG, built around sibling tables, i.e., tables with highly similar schemas but subtle content differences. On TCR-Bench, the dense retrievers we evaluate persistently exhibit a Semantic-Answerability Gap: they often retrieve the correct sibling group yet struggle to pinpoint the uniquely answerable table within it, dropping QA performance from 0.755 (oracle) to 0.330 (top-5 retrieved). Our analysis suggests this gap is associated with semantic accumulation, schema-level cue dependence, and weak row-column binding. As a diagnostic probe into the source of this gap, we test whether a lightweight two-stage pipeline, Answerability-Aware Reranking (AAR), applying direct query-table answerability judgment, can recover performance: it raises top-1 target retrieval from 18.2% to 57.4%, and this large gain is itself evidence that much of the observed failure reflects a missing answerability verification step, rather than an inherent limitation of model capacity alone.

摘要：表格是檢索增強生成（RAG）中的關鍵知識來源，但檢索到的表格可能缺乏足夠的證據來回答查詢，我們稱之為可回答性。雖然可回答性廣泛關注一個來源或來源集合是否包含足夠的證據，但為語義相關性優化的檢索模型即使在單一來源的情況下也無法保證這一點，從而造成根本的不匹配。為了研究這一點，我們引入了 TCR-Bench，這是一個針對 RAG 中表格內容級別可回答性的診斷基準，圍繞兄弟表格構建，即具有高度相似模式但內容細微差異的表格。在 TCR-Bench 上，我們評估的密集檢索器持續顯示出語義可回答性差距：它們經常檢索到正確的兄弟組，但卻難以確定其中唯一可回答的表格，導致 QA 表現從 0.755（oracle）下降到 0.330（前 5 個檢索結果）。我們的分析表明，這一差距與語義累積、模式級線索依賴性和弱行列綁定有關。作為對這一差距來源的診斷探測，我們測試了一個輕量級的兩階段管道，即可回答性感知重排序（AAR），該管道應用直接的查詢-表格可回答性判斷，以恢復性能：它將前 1 個目標檢索率從 18.2% 提升至 57.4%，而這一大幅增長本身就是證據，表明觀察到的失敗在很大程度上反映了缺失的可回答性驗證步驟，而不僅僅是模型容量的固有限制。

##### **Large Language Models for Citation Function Classification**
2607.17738v1 by Daniel Vodička, Jakub Šmíd, Pavel Král, Christophe Cerisara

Citation function classification plays a crucial role in understanding the relationships between scientific publications and advancing bibliometric analysis. This study presents one of the first comprehensive evaluations of multiple state-of-the-art (SOTA) large language models (LLMs) for citation function classification, achieving new SOTA results on the ACL-ARC dataset. We systematically compare five models (Mistral 7B, Orca 2-7B, LLaMA 3.1-8B, Falcon 7B, and SciBERT) across zero-shot, few-shot, and fine-tuning approaches. Our fine-tuned Falcon 7B model achieves a 73.3% macro F1 score on ACL-ARC, representing a significant improvement over previous methods. Additionally, we introduce AC3, a novel dataset featuring a seven-category annotation scheme that distinguishes between neutral acknowledgments and explicit evaluative stances (more opinion-oriented citations - criticizing, complimenting, contradicting). The dataset is implemented across four context extraction variants to systematically evaluate the impact of contextual scope on classification performance. We also provide detailed analysis of model performance, experimental configurations, and limitations to guide future research in this domain. To our knowledge, this is one of the first studies dedicated to comprehensive model comparison for citation function classification, addressing a gap identified in recent surveys.

摘要：引用功能分類在理解科學出版物之間的關係以及推進計量文獻分析方面扮演著至關重要的角色。這項研究呈現了對多個最先進的大型語言模型（LLMs）進行引用功能分類的首次全面評估之一，在ACL-ARC數據集上取得了新的最先進結果。我們系統地比較了五個模型（Mistral 7B、Orca 2-7B、LLaMA 3.1-8B、Falcon 7B和SciBERT）在零樣本、少樣本和微調方法上的表現。我們微調的Falcon 7B模型在ACL-ARC上達到了73.3%的宏觀F1分數，顯示出相較於以往方法的顯著改進。此外，我們引入了AC3，一個新穎的數據集，具有七類註釋方案，區分中立的確認和明確的評價立場（更具意見導向的引用 - 批評、讚美、矛盾）。該數據集在四種上下文提取變體中實施，以系統地評估上下文範圍對分類性能的影響。我們還提供了模型性能、實驗配置和限制的詳細分析，以指導該領域未來的研究。據我們所知，這是首批專門針對引用功能分類進行全面模型比較的研究之一，填補了近期調查中識別的空白。

##### **SR-Agent: An Experience-Driven Agentic Framework for Post-Ranking Strategies Refinement in E-Commerce Recommendation**
2607.17719v1 by Hanchen Yang, Kaiwen Yang, Junpeng Zhuang, Yang He, Keting Cen, Bochao Liu, Zhongbo Sun, An Liu, Zhongteng Han, Chenyi Lei

User experience is a first-class objective in industrial e-commerce recommender systems (RS). Post-ranking strategies, which govern diversity, similarity, and exposure over a ranked list, are widely deployed in industrial RS for their simplicity and low serving cost. However, as the online recommendation environment evolves continuously, these statically configured strategies gradually become stale, degrading the user experience. Refining them typically relies on manual inspection, diagnosis, and updates, a process that is slow, costly, and hard to reuse. Although recent LLM-based agents (e.g., RecUserSim, SimUSER, and Self-EvolveRec) offer promising directions, none of them close the full loop of automated, self-evolving strategy refinement. To bridge this gap, we introduce SR-Agent, a Strategy Refinement agentic framework that, to the best of our knowledge, is the first deployed for refining post-ranking strategies in industrial RS. SR-Agent unifies three components: (i) a UserSim agent that applies staged inspection skills to surface user-perceived bad cases; (ii) an Analysis agent that consolidates recurring bad cases into structured, reusable diagnoses; and (iii) a constrained Strategy Refinement Harness that maps diagnoses to typed and bounded actions, gated by a four-stage reward pipeline with reversible rollback. Deployed on the Kuaishou e-commerce platform, SR-Agent continuously runs this refinement loop and, in a one-month online A/B test, increases order volume by 0.71%, browsing depth by 0.34%, and clicked-category diversity by 0.48%, while markedly shortening the refinement cycle and lowering operational cost.

摘要：使用者體驗是工業電子商務推薦系統（RS）中的首要目標。後排名策略在工業 RS 中被廣泛應用，因為它們簡單且成本低廉，這些策略管理著排名列表中的多樣性、相似性和曝光率。然而，隨著在線推薦環境的不斷演變，這些靜態配置的策略逐漸變得過時，降低了使用者體驗。精煉這些策略通常依賴於手動檢查、診斷和更新，這一過程緩慢、成本高且難以重用。儘管最近的基於 LLM 的代理（例如 RecUserSim、SimUSER 和 Self-EvolveRec）提供了有希望的方向，但它們都未能實現自動化、自我演變策略精煉的完整循環。為了填補這一空白，我們介紹了 SR-Agent，一個策略精煉代理框架，據我們所知，這是首個部署於工業 RS 中以精煉後排名策略的框架。SR-Agent 統一了三個組件：（i）一個 UserSim 代理，應用分階段檢查技能來揭示使用者感知的壞案例；（ii）一個分析代理，將重複出現的壞案例整合成結構化、可重用的診斷；以及（iii）一個受限的策略精煉工具，將診斷映射到類型化和有界的行動，並通過一個具有可逆回滾的四階段獎勵管道進行控制。SR-Agent 部署在快手電子商務平台上，持續運行這一精煉循環，在為期一個月的在線 A/B 測試中，訂單量增加了 0.71%、瀏覽深度增加了 0.34%、點擊類別的多樣性增加了 0.48%，同時顯著縮短了精煉週期並降低了運營成本。

##### **Integrating High-Level Requirements to Low-Level Tests with Machine-Readable V&V Specifications**
2607.17686v1 by Mansur Arief, Nur Ahmad Khatim, Ali Akarma, Ahmad Alfan Alfian Irfan

Modern software teams have mature tools for low-level testing, such as pytest, JUnit, and Jest, which make it inexpensive to write unit tests and run them on every commit. Systems engineering, in parallel, has developed rigorous principles for design verification and validation (V&V), which has worked very well across engineering discipline to align user expecations and requirements with developers' deliverables. In practice, however, the two rarely connect, and the link between users' high-level requirements and the low-level tests that machines actually run is maintained by hand, if at all. This gap is increasingly costly for AI-enabled and cyber-physical systems, for which regulators now ask for traceable evidence that high-level requirements are met, while raw test results provide little of the structure such evidence requires. We introduce VNVSpec, an open-source framework that makes V&V specifications machine-readable and executable. With this framework, users state high-level requirements directly or import them from catalogs derived from published standards. Then, the framework checks requirement quality, supports decomposition into module-level requirements with explicit metrics and acceptance criteria, links these requirements to test results through a traceability graph, and compiles the collected evidence into verdicts and audit-ready reports. We evaluate the framework by self-application, in which it is continuously assessed in CI against its own specification of 36 requirements verified by 449 tests, completed within limited time which scales linearly and thus can handle up to 10,000 requirements. We also discuss how the framework extends to testing black-box AI models and AI coding agents. The framework, its full test suite, the catalogs, and the benchmark scripts are available at https://github.com/ai-vnv/vnvspec.

摘要：現代軟體團隊擁有成熟的低層測試工具，例如pytest、JUnit和Jest，使得撰寫單元測試和在每次提交時運行它們變得成本低廉。與此同時，系統工程已經發展出嚴謹的設計驗證和驗證（V&V）原則，這在各工程學科中運作良好，以將用戶期望和需求與開發者的交付成果對齊。然而，在實踐中，這兩者很少連接，用戶的高層需求與機器實際運行的低層測試之間的聯繫通常是手動維護的，即使有也是如此。對於啟用AI和網絡物理系統來說，這一差距的成本日益增加，因為監管機構現在要求可追溯的證據來證明高層需求得到了滿足，而原始測試結果提供的結構幾乎無法滿足這種證據的要求。我們介紹了VNVSpec，一個開源框架，使V&V規範可機器讀取和可執行。通過這個框架，用戶可以直接陳述高層需求或從根據已發表標準衍生的目錄中導入它們。然後，該框架檢查需求質量，支持將其分解為具有明確指標和接受標準的模組級需求，通過可追溯性圖將這些需求與測試結果鏈接，並將收集到的證據編譯成裁決和審計準備報告。我們通過自我應用評估該框架，在持續的CI中對其36個需求的規範進行評估，這些需求由449個測試驗證，並在有限的時間內完成，該時間呈線性擴展，因此可以處理多達10,000個需求。我們還討論了該框架如何擴展到測試黑箱AI模型和AI編碼代理。該框架、其完整的測試套件、目錄和基準腳本可在https://github.com/ai-vnv/vnvspec獲得。

##### **Selectivity Matters: Source Node Influence Pruning for Unsupervised Graph Domain Adaptation**
2607.17668v1 by Ridong Han, Yawen Shen, Zhongnian Li, Tongfeng Sun, Xinzheng Xu, Abdulmotaleb El Saddik

Unsupervised Graph Domain Adaptation (UGDA) aims to facilitate knowledge transfer from a labeled source graph to an unlabeled target graph by mitigating cross-domain distribution shifts. Existing methods primarily focus on node-level feature alignment in latent spaces, relying on the implicit assumption that all source nodes contribute positively to the alignment. However, this assumption often fails because a node's semantic information is intrinsically coupled with its topological graph structure. Due to structural shifts, source nodes with severe structural deviations (e.g., structural outliers) lack semantic counterparts in the target graph, and forcing alignment on them introduces severe noise and causes negative transfer. To bridge this gap, we argue that selective source node utilization is superior to full-graph training, thereby shifting the research paradigm from feature-level alignment to data-level refinement. To this end, we propose Source Node Influence Pruning (SNIP), a novel model-agnostic, data-centric refinement framework. Specifically, SNIP quantifies the structural discrepancy between individual source nodes and the target domain by integrating multiple centrality measures, assigning each source node an influence score. A rank-based normalization mechanism is further employed to eliminate scale variations across different measures, allowing SNIP to effectively identify and filter out structurally incompatible nodes with low influence scores. As a plug-and-play method, SNIP constructs a refined "sub-source" graph that is inherently more beneficial for subsequent alignment. Comprehensive experiments across eight transfer scenarios on five real-world datasets demonstrate that SNIP consistently outperforms competitive baselines and significantly enhances adaptation performance, validating the superiority of selective node utilization over full-graph training.

摘要：無監督圖形領域適應（UGDA）旨在通過減少跨領域分佈的變化，促進從標記源圖到未標記目標圖的知識轉移。現有方法主要集中於潛在空間中的節點級特徵對齊，依賴於所有源節點對對齊的正面貢獻的隱含假設。然而，這一假設往往失效，因為節點的語義信息本質上與其拓撲圖結構相互耦合。由於結構變化，具有嚴重結構偏差的源節點（例如，結構異常值）在目標圖中缺乏語義對應物，強行對齊會引入嚴重的噪聲並導致負轉移。為了彌補這一差距，我們認為選擇性利用源節點優於全圖訓練，從而將研究範式從特徵級對齊轉向數據級精煉。為此，我們提出了源節點影響修剪（SNIP），這是一個新穎的模型無關、數據中心的精煉框架。具體而言，SNIP通過整合多種中心性度量，量化個別源節點與目標領域之間的結構差異，為每個源節點分配一個影響分數。進一步採用基於排名的正規化機制，以消除不同度量之間的尺度變化，使SNIP能夠有效識別和過濾出結構不兼容且影響分數低的節點。作為一種即插即用的方法，SNIP構建了一個經過精煉的“子源”圖，這對隨後的對齊更具益處。在五個真實世界數據集的八個轉移場景中的全面實驗表明，SNIP始終優於競爭基準，顯著提高了適應性能，驗證了選擇性節點利用相對於全圖訓練的優越性。

##### **Trustworthy Protein-Ligand Binding Affinity Prediction via Reliability-Aware Multi-Engine Fusion**
2607.17601v1 by Yongchan Hong, Defu Cao, Wenjin Liu, Thomas Ku, Jordy Homing Lam, Emily Nguyen, Willie Neiswanger, Vsevolod Katritch, Yan Liu

Accurate protein-ligand binding affinity prediction is central to computational drug discovery, yet modern docking engines frequently disagree without indicating which prediction to trust. Consensus scoring and ensemble methods improve mean accuracy but treat all predictions identically without interpretable confidence measures or uncertainty decomposition, ignoring the chemical context of each protein-ligand pair. To address this limitation, we introduce RELIABLE-BA (RELIABiLity-aware Evidential fusion for Binding Affinity), an evidential framework for multi-engine binding affinity prediction. Our model comprises three steps: (1) modeling each engine as an evidential expert via Normal-Inverse-Gamma distributions, (2) scaling epistemic uncertainty through learned reliability from molecular context while preserving each expert's predictive mean, and (3) fusing experts through closed-form aggregation that captures both individual uncertainty and inter-engine disagreement. Experiments on the PDBBind and BDB2020+ benchmarks demonstrate competitive point prediction with substantially improved uncertainty calibration, and additional validation on the SARS-CoV-2 Mpro dataset and 5HT2A receptor demonstrates applicability to clinically relevant drug targets. Crucially, these uncertainty estimates enable reliable filtering of protein-ligand pairs, reducing prediction error by up to 25% when retaining only high-confidence pairs. To our knowledge, RELIABLE-BA is the first multi-engine binding affinity prediction framework to combine evidential fusion with context-dependent reliability, offering a principled path toward trustworthy AI-guided drug discovery. Our code is publicly available at https://github.com/yongchand/RELIABLE-BA.

摘要：準確的蛋白質-配體結合親和力預測是計算藥物發現的核心，然而現代對接引擎經常存在分歧，卻未能指明應該信任哪個預測。共識評分和集成方法提高了平均準確性，但對所有預測一視同仁，缺乏可解釋的信心度量或不確定性分解，忽略了每對蛋白質-配體的化學背景。為了解決這一限制，我們引入了RELIABLE-BA（RELIABiLity-aware Evidential fusion for Binding Affinity），這是一個多引擎結合親和力預測的證據框架。我們的模型包含三個步驟：（1）通過正規-逆伽馬分佈將每個引擎建模為證據專家，（2）通過從分子背景中學習的可靠性來調整認知不確定性，同時保留每個專家的預測均值，以及（3）通過封閉形式的聚合融合專家，捕捉個體不確定性和引擎間的不一致性。在PDBBind和BDB2020+基準上的實驗顯示出具有競爭力的點預測，並顯著改善了不確定性校準，對SARS-CoV-2 Mpro數據集和5HT2A受體的額外驗證顯示了對臨床相關藥物靶點的適用性。關鍵是，這些不確定性估計使得可靠地篩選蛋白質-配體對成為可能，在僅保留高信心對的情況下將預測誤差降低了多達25%。據我們所知，RELIABLE-BA是第一個將證據融合與上下文相關的可靠性結合的多引擎結合親和力預測框架，為可信的AI引導藥物發現提供了一條原則性途徑。我們的代碼已公開在 https://github.com/yongchand/RELIABLE-BA。

##### **Reinforcement Learning: From Algorithms To Foundation Models**
2607.17560v1 by Zihan Ding

Reinforcement learning (RL) provides a framework for sequential decision making under explicit objectives. In its classical form, RL studies how an agent should act to maximise long-term reward in a dynamic environment. In richer settings, the problem extends beyond a single agent and fixed environment: intelligent behavior may require strategic interaction, adaptation to uncertainty, and reasoning over high-dimensional worlds. This thesis studies RL from two perspectives: algorithms in games and RL in the era of foundation models.
  The first part focuses on multi-agent RL in games. It examines how incentives, policies, and equilibrium concepts interact in competitive and general-sum environments, spanning two-player zero-sum games, large-scale video games, and multi-player settings with general structure. These works investigate learning in multi-agent systems and the behavior of RL methods in interactive environments. The second part studies RL with generative and foundation models, motivated by the idea that prior knowledge can enrich sequential decision making. Pretrained generative models and learned world models serve as representation tools and structured priors for planning, control, and policy optimization. The thesis develops diffusion-based world models, investigates RL for efficient video generation, explores generative models as policy classes, and studies interactive video world models in which actions shape future observations. It also addresses long-horizon modeling through architectures with memory. Together, these contributions present a unified view of RL as objective-driven adaptation in complex sequential domains. From strategic games to generative world models, the thesis highlights how RL connects decision making, environment modeling, and emerging foundation-model capabilities, offering a broader perspective on the principles underlying intelligent behavior.

摘要：強化學習（RL）提供了一個在明確目標下進行序列決策的框架。 在其經典形式中，RL 研究代理應如何行動以最大化動態環境中的長期獎勵。 在更豐富的情境中，問題超出了單一代理和固定環境的範疇：智能行為可能需要戰略互動、對不確定性的適應以及在高維世界中的推理。 本論文從兩個角度研究 RL：遊戲中的算法和基礎模型時代的 RL。<lb/>第一部分專注於遊戲中的多代理 RL。 它考察了激勵、政策和均衡概念在競爭和一般總和環境中的互動，涵蓋了雙人零和遊戲、大型視頻遊戲以及具有一般結構的多玩家環境。 這些研究探討了多代理系統中的學習以及 RL 方法在互動環境中的行為。<lb/>第二部分研究了與生成模型和基礎模型相關的 RL，這是基於先前知識可以豐富序列決策的想法。 預訓練的生成模型和學習的世界模型作為規劃、控制和政策優化的表示工具和結構性先驗。 本論文開發了基於擴散的世界模型，研究了高效視頻生成的 RL，探索了生成模型作為政策類別，並研究了互動視頻世界模型，其中行動影響未來觀察。 它還通過具有記憶的架構解決長期建模問題。 總體而言，這些貢獻呈現了 RL 作為在複雜序列領域中以目標為驅動的適應的統一視角。 從戰略遊戲到生成世界模型，這篇論文突顯了 RL 如何將決策制定、環境建模和新興的基礎模型能力聯繫起來，提供了對智能行為基礎原則的更廣泛視角。

##### **Human-in-the-Loop User Feedback Affects Perceived Accuracy and Trust, but Task Subjectivity Matters**
2607.17548v1 by Donald R. Honeycutt, Mahsan Nourani, Eric D. Ragan

While ML can produce complex models beyond those that a human could produce manually, incorporating human input can often improve performance beyond purely data-driven models. While this feedback could come from system designers or domain experts, in many cases, the end users who regularly use the system will naturally develop an understanding of its flaws and desire the ability to change the system's behavior based on their knowledge. While soliciting feedback from end users can result in significant model improvement over time, introducing these feedback techniques can also affect several human factors-such as trust or perception of system accuracy-that are not yet fully understood and have different effects reported in the existing literature. Therefore, we sought to build on the existing research to further explore how the act of providing feedback can affect user understanding of an intelligent system and its accuracy in different contexts. We present three controlled experiments that study the effects of interactive feedback collections on user impressions in domains with objective and subjective feedback. The results show that in a context where there is an objectively correct answer, providing HITL feedback lowered both participants' trust in the system and their perception of system accuracy, regardless of whether the system accuracy improved in response to their feedback. However, when the feedback being provided involved subjective opinion, no such negative bias was observed. Furthermore, in the objective context, participants distrusted the system over time, whereas participants in the subjective context mistrusted the system over time. These results highlight the importance of considering the effects of allowing different types of end-user feedback on user trust when designing intelligent systems.

摘要：雖然機器學習可以產生超出人類手動產生的複雜模型，但納入人類的輸入通常可以改善性能，超越純粹以數據驅動的模型。這種反饋可能來自系統設計師或領域專家，但在許多情況下，經常使用系統的最終用戶自然會對其缺陷產生理解，並渴望根據他們的知識改變系統的行為。雖然向最終用戶徵求反饋隨著時間的推移可能會導致顯著的模型改進，但引入這些反饋技術也可能影響幾個人為因素，例如信任或對系統準確性的感知，而這些因素尚未完全理解，並在現有文獻中報告了不同的影響。因此，我們尋求在現有研究的基礎上進一步探討提供反饋的行為如何影響用戶對智能系統的理解及其在不同情境下的準確性。我們提出了三個控制實驗，研究交互式反饋收集對用戶印象的影響，這些印象來自於客觀和主觀的反饋。結果顯示，在有客觀正確答案的情境中，提供人機協作反饋降低了參與者對系統的信任以及他們對系統準確性的感知，無論系統的準確性是否因應他們的反饋而改善。然而，當提供的反饋涉及主觀意見時，並未觀察到此類負面偏見。此外，在客觀情境中，參與者隨著時間的推移對系統產生不信任，而在主觀情境中，參與者隨著時間的推移對系統產生不信任。這些結果突顯了在設計智能系統時考慮允許不同類型的最終用戶反饋對用戶信任影響的重要性。

##### **D-NOVA: In-Storage Retrieval Accelerator via Dual-Bound 3D NAND-Optimized Similarity Search with Vector Adaptation**
2607.17538v1 by Chang Eun Song, Sumukh Pinge, Tianqi Zhang, Sung Eun Kim, Tajana S. Rosing, Mingu Kang

Retrieval-Augmented Generation (RAG) enhances the factual grounding of large language model (LLM) inference by retrieving relevant information from external knowledge bases. However, its dense vector retrieval introduces significant latency and energy overhead, becoming the primary performance bottleneck. Although recent in-storage accelerators aim to reduce data movement, they still rely on host or embedded processors outside the memory, where nearly 70% of the total retrieval time is spent. As a result, they cannot fully overcome the bandwidth limitations, leading to yet another memory bottleneck. To tackle these limitations, we present D-NOVA, a hardware-software co-designed in-storage retrieval accelerator. D-NOVA executes an inverted file (IVF)-based hierarchical retrieval pipeline by deeply embedding the search functionality directly into the NAND memory array. This is achieved by incorporating a new distance metric, Dual-Bound Tight Similarity Sensing (DTS), which is specifically tailored for searching within the NAND string. In addition, we introduce a lightweight contrastive adapter that maps embedding vectors into a DTS-friendly domain, recovering near-software recall while improving performance and energy efficiency. D-NOVA is up to 41.7x faster and 71x more energy-efficient than a CPU baseline, and achieves 12.13x higher throughput while being up to 1.26x more energy-efficient than state-of-the-art in-storage RAG accelerators, demonstrating the potential of fully in-storage vector search for scalable RAG acceleration.

摘要：檢索增強生成（RAG）通過從外部知識庫檢索相關信息來增強大型語言模型（LLM）推理的事實基礎。然而，其密集向量檢索引入了顯著的延遲和能量開銷，成為主要的性能瓶頸。儘管最近的存儲加速器旨在減少數據移動，但它們仍然依賴於主機或嵌入式處理器，這些處理器位於內存之外，幾乎70%的總檢索時間都花費在這裡。因此，它們無法完全克服帶寬限制，導致又一個內存瓶頸。為了解決這些限制，我們提出了D-NOVA，一種硬件-軟件協同設計的內存檢索加速器。D-NOVA通過將搜索功能深度嵌入NAND內存陣列中，執行基於倒排文件（IVF）的分層檢索管道。這是通過引入一種新的距離度量，雙界緊密相似度感知（DTS），專門為在NAND字串中進行搜索而量身定制的。此外，我們還引入了一種輕量級對比適配器，將嵌入向量映射到DTS友好的領域，恢復接近軟件的召回，同時提高性能和能量效率。D-NOVA的速度比CPU基準快最多41.7倍，能量效率提高71倍，並且在吞吐量上達到12.13倍，同時在能量效率上比最先進的內存RAG加速器提高最多1.26倍，展示了完全內存向量搜索在可擴展RAG加速中的潛力。

##### **Salience Induction against Multi-Hop RAG Agents: Threat and Defense**
2607.17535v1 by Xingfu Zhou, Pengfei Wang, Yuan Zhou, Wei Xie, Xu Zhou

Agentic retrieval-augmented generation (RAG) systems increasingly retrieve external evidence and orchestrate tools for knowledge-intensive applications. In Multi-Hop question answering, agents chain facts across documents. Existing defenses focus on content poisoning, which injects false facts, and prompt injection, which embeds directives. We identify a third attack surface: the salience channel, through which fact position, emphasis, framing, and semantic proximity can redirect reasoning even when all retrieved claims are true and no instructions are present. We formalize Salience Induction as truth-preserving edits that redirect Multi-Hop attribute binding while leaving the retrieval trace semantically intact. We define six Salience-Editing operator classes and build an iterative proposer-verifier pipeline under factual and stealth constraints. We also introduce SalientWiki-MH, a decoy-annotated Multi-Hop benchmark. Evaluations across five frontier model families (GPT, Claude, Gemini, DeepSeek, and Qwen) and three agent architectures (ReAct, Reflexion, and tool-calling) show broad generalization. Under a 30% edit budget, Salience Induction achieves an 83.3% attack success rate; the strongest evaluated baseline defense leaves 75.7% post-defense ASR. Untargeted rewriting further reduces attacks only by degrading neutral task success. Our lightweight input-side defense, Salience Normalization, reduces attack success to 15.3% under standard attacks and 23.6% under an adaptive attack. These results show that truthfulness and instruction filtering alone are insufficient: robust agentic RAG also requires defenses against salience-relevance decoupling.

摘要：代理檢索增強生成（RAG）系統越來越多地檢索外部證據並協調工具以應對知識密集型應用。<lb/>在多跳問答中，代理在文檔之間鏈接事實。<lb/>現有的防禦措施集中於內容中毒，這會注入虛假事實，以及提示注入，這會嵌入指令。<lb/>我們識別出第三種攻擊面：顯著性通道，通過該通道，事實的位置、強調、框架和語義接近性可以重新引導推理，即使所有檢索的主張都是真實的且沒有指令存在。<lb/>我們將顯著性引導形式化為保持真實性的編輯，這些編輯重新引導多跳屬性綁定，同時保持檢索痕跡在語義上不變。<lb/>我們定義了六種顯著性編輯操作類別，並在事實和隱蔽約束下建立了一個迭代的提議者-驗證者管道。<lb/>我們還引入了SalientWiki-MH，一個帶有誘餌標註的多跳基準。<lb/>在五個前沿模型系列（GPT、Claude、Gemini、DeepSeek 和 Qwen）和三種代理架構（ReAct、Reflexion 和工具調用）上的評估顯示出廣泛的泛化能力。<lb/>在30%的編輯預算下，顯著性引導達到了83.3%的攻擊成功率；最強的評估基線防禦留下了75.7%的防禦後ASR。<lb/>無目標重寫進一步減少攻擊，僅通過降低中立任務的成功率。<lb/>我們的輕量級輸入側防禦，顯著性正規化，在標準攻擊下將攻擊成功率降低到15.3%，在自適應攻擊下降低到23.6%。<lb/>這些結果表明，僅僅依賴真實性和指令過濾是不夠的：強健的代理RAG還需要針對顯著性-相關性解耦的防禦。

##### **Pailitao-MMSearch: Building Native E-Commerce Multimodal Search Foundation**
2607.17499v1 by Xiaohan Ye, Xu Chen, Zihan Gong, Jian Ding, Lianyu Du, Baicheng Chen, Yunmeng Shu, Jingqian Zhao, Zhixiang Zhao, Shuaiqi Jia, Chong Ma, Shuwen Xiao, Xiangheng Kong, Yuan Gao, Jun Song, Jinsong Lan, Xiaoyong Zhu, Bo Zheng

The evolution of e-commerce has fundamentally transformed how users search for products, shifting from simple text-based keyword queries to complex multimodal interactions that seamlessly combine product images, natural language descriptions, and mixed-intent instructions. However, existing approaches face a critical dilemma: single-modal specialist models, deployed independently for text retrieval, visual search, and voice recognition, operate in isolation and cannot handle cross-modal queries, while general-purpose vision-language models lack the domain-specific knowledge necessary for fine-grained product understanding, user behavior modeling, and commercial intent reasoning. In this work, we present Pailitao-MMSearch, one native e-commerce multimodal search foundation model designed to bridge this gap. Our approach introduces three key innovations: (1)HybSID (Hybrid Semantic ID);(2)a two-stage continual pre-training strategy; and (3)a hybrid reasoning post-training pipeline. Built upon Qwen and deployed on Taobao's Pailitao multimodal search platform, Pailitao-MMSearch achieves substantial improvements in online A/B testing, including up to +13.61\% in Gross Merchandise Volume (GMV) and +8.21\% in transaction volume compared to traditional multi-modal search pipeline, demonstrating the effectiveness of our native e-commerce multimodal search large language models.

摘要：電子商務的演變從根本上改變了用戶搜尋產品的方式，從簡單的基於文本的關鍵字查詢轉變為複雜的多模態互動，這些互動無縫地結合了產品圖片、自然語言描述和混合意圖指令。<lb/>然而，現有的方法面臨一個關鍵的困境：單一模態專家模型，獨立部署於文本檢索、視覺搜索和語音識別，彼此隔離，無法處理跨模態查詢，而通用的視覺-語言模型則缺乏進行細緻產品理解、用戶行為建模和商業意圖推理所需的領域特定知識。<lb/>在本研究中，我們提出了Pailitao-MMSearch，一個本土電子商務多模態搜索基礎模型，旨在彌補這一差距。<lb/>我們的方法引入了三項關鍵創新：(1) HybSID（混合語義ID）；(2) 一種兩階段的持續預訓練策略；以及(3) 一個混合推理後訓練流程。<lb/>基於Qwen並部署於淘寶的Pailitao多模態搜索平台，Pailitao-MMSearch在在線A/B測試中取得了顯著的改進，包括與傳統多模態搜索流程相比，總商品交易額（GMV）提高了高達+13.61\%，交易量提高了+8.21\%，展示了我們本土電子商務多模態搜索大語言模型的有效性。

##### **Panache: One-Pass Motif Discovery at Every Window Length**
2607.17481v1 by Tej Sanibh Ranade

Motif discovery, the search for recurring patterns within a time series, is a core primitive of exploratory data analysis. A pattern, however, is defined by its duration, which analysts rarely know in advance. To resolve this unknown duration, an interval of window lengths is defined, and the accepted method is to try every length in that interval. Existing pan matrix profile (PMP) methods compute one z-normalized matrix profile per length, so $L$ lengths cost $L$ quadratic self-joins over the same series. We introduce Panache, to our knowledge the first one-pass streaming algorithm for z-normalized PMP motif discovery. It replaces the repeated self-joins with a single scan whose runtime is near-linear in the series length. The key observation is that mean-centering a subsequence changes only its DC Fourier coefficient, so the non-DC spectrum of every z-normalized subsequence can be maintained online by sliding-DFT recurrences and running statistics. This spectral state is the key under which similar subsequences collide in an occupancy-controlled hash directory and, through Parseval's theorem, yields a lower bound that rejects most colliding pairs before any exact computation. Panache computes every data-dependent parameter itself, leaving only a resource budget to tune. At the default budget, it recovers all top-20 pan-motifs against exact fixed-exclusion ground truth on 17 UCR configurations, and is faster than every CPU and GPU baseline benchmarked in this paper. On Wafer at five million samples over 51 lengths, Panache completes one pass in 2.9 minutes and emits the exact motifs in 6.0 minutes, against 7.95 hours for the fastest exact CPU baseline and 38.3 minutes for SCAMP on an H100 GPU.

摘要：發現模式，即在時間序列中尋找重複出現的模式，是探索性數據分析的核心原始操作。然後，模式的定義取決於其持續時間，而分析師通常無法提前知道這一點。為了解決這個未知的持續時間，定義了一個窗口長度的區間，接受的方法是在該區間內嘗試每個長度。現有的全矩陣剖面（PMP）方法對每個長度計算一個z標準化的矩陣剖面，因此$L$個長度的成本是對同一序列進行$L$次二次自連接。我們介紹了Panache，據我們所知，這是第一個一次性流式算法，用於z標準化的PMP模式發現。它用一次掃描替代了重複的自連接，其運行時間接近於序列長度的線性。關鍵觀察是，對子序列進行均值中心化僅改變其DC傅里葉係數，因此每個z標準化子序列的非DC頻譜可以通過滑動DFT重複和運行統計在線維護。這一頻譜狀態是相似子序列在佔用控制的哈希目錄中碰撞的關鍵，並且通過Parseval定理，提供了一個下界，能在任何精確計算之前拒絕大多數碰撞對。Panache自己計算每個數據依賴的參數，只留下資源預算進行調整。在默認預算下，它在17個UCR配置上恢復所有前20個全模式，並且比本文中基準的每個CPU和GPU基準都要快。在Wafer上，對於五百萬個樣本和51個長度，Panache在2.9分鐘內完成一次掃描，並在6.0分鐘內發出精確的模式，而最快的精確CPU基準需要7.95小時，SCAMP在H100 GPU上則需要38.3分鐘。

##### **HyCoRec: Hypergraph-Enhanced Multi-Preference Learning for Alleviating Matthew Effect in Conversational Recommendation**
2607.17461v1 by Yongsen Zheng, Ruilin Xu, Ziliang Chen, Guohua Wang, Mingjie Qian, Jinghui Qin, Liang Lin

The Matthew effect is a notorious issue in Recommender Systems (RSs), \emph{i.e.}, the rich get richer and the poor get poorer, wherein popular items are overexposed while less popular ones are regularly ignored. Most methods examine Matthew effect in static or nearly-static recommendation scenarios. However, the Matthew effect will be increasingly amplified when the user interacts with the system over time. To address these issues, we propose a novel paradigm, Hypergraph-Enhanced Multi-Preference Learning for Alleviating Matthew Effect in Conversational Recommendation (HyCoRec), which aims to alleviate the Matthew effect in conversational recommendation. Concretely, HyCoRec devotes to alleviate the Matthew effect by learning multi-aspect preferences, \emph{i.e.}, item-, entity-, word-, review-, and knowledge-aspect preferences, to effectively generate responses in the conversational task and accurately predict items in the recommendation task when the user chats with the system over time. Extensive experiments conducted on two benchmarks validate that HyCoRec achieves new state-of-the-art performance and the superior of alleviating Matthew effect. Our code is available at https://github.com/zysensmile/HyCoRec.

摘要：馬太效應是推薦系統（RSs）中的一個臭名昭著的問題，\emph{i.e.}，富者越富，貧者越貧，受歡迎的項目被過度曝光，而不那麼受歡迎的項目則經常被忽視。大多數方法在靜態或近靜態的推薦場景中檢查馬太效應。然而，當用戶隨著時間與系統互動時，馬太效應將會越來越強化。為了解決這些問題，我們提出了一種新穎的範式，超圖增強多偏好學習以緩解對話推薦中的馬太效應（HyCoRec），旨在緩解對話推薦中的馬太效應。具體而言，HyCoRec 致力於通過學習多方面的偏好來緩解馬太效應，\emph{i.e.}，項目、實體、單詞、評論和知識方面的偏好，以有效生成對話任務中的回應，並在用戶隨著時間與系統聊天時準確預測推薦任務中的項目。在兩個基準上進行的廣泛實驗驗證了 HyCoRec 實現了新的最先進性能，並在緩解馬太效應方面具有優越性。我們的代碼可在 https://github.com/zysensmile/HyCoRec 獲得。

##### **Agentic ERP: Multi-Agent Large Language Model Architecture for Autonomous Enterprise Resource Planning**
2607.17331v1 by Zhihao Liu, Tianyu Wang, Xi Vincent Wang, Lihui Wang

Enterprise Resource Planning (ERP) systems record transactions reliably but still delegate almost all operational decision-making to human specialists, because classical rule-based automation cannot reason about exceptions and monolithic AI assistants degrade when asked to coordinate across functional boundaries. This paper presents Agentic ERP, an expert-system architecture that combines role-aligned large-language-model (LLM) agents with a risk-tiered human-in-the-loop harness and a graph-based orchestrator to execute end-to-end business workflows on a production ERP backend. First, autonomous ERP operation is formulated as a constrained sequential-decision problem over a structured enterprise state, with a decomposition argument linking role-aligned agents to a measurable reduction in per-step tool-selection complexity. Second, a graph-based Planner--Executor--Reflector--Responder orchestration decouples generation from evaluation through externalised grading criteria and sprint contracts, packaging recent harness-engineering principles as inspectable expert-system artefacts. Third, the system is evaluated at three levels: a scenario-based task suite, a comprehensive comparison of six orchestration paradigms on cross-functional crisis tasks, and a 365-day agent-in-the-loop simulation against rule-based RPA and no-intervention baselines. Across these levels the proposed multi-agent method is significantly better than the baseline, and the system sustains a simulated year of operation with zero stockouts while the rule-based baseline accumulates hundreds under the same demand stream. The work shows that role-aligned LLM agents under human oversight can move an ERP system from passively recording transactions to actively executing operational decisions, and it provides a reference architecture and an evaluation protocol for autonomous enterprise resource planning.

摘要：企業資源規劃（ERP）系統可靠地記錄交易，但仍將幾乎所有的操作決策委託給人類專家，因為傳統的基於規則的自動化無法對例外情況進行推理，而單一的人工智慧助理在被要求跨功能邊界協調時會退化。本文提出了Agentic ERP，一種專家系統架構，將角色對齊的大型語言模型（LLM）代理與風險分層的人類介入系統和基於圖形的協調器結合，以在生產ERP後端執行端到端的業務工作流程。首先，自主ERP操作被公式化為一個在結構化企業狀態上的受限序列決策問題，並通過一個分解論證將角色對齊的代理與每步工具選擇複雜性的可測量減少聯繫起來。其次，基於圖形的計劃者-執行者-反思者-響應者協調通過外部評分標準和衝刺合同將生成與評估解耦，將最近的系統工程原則打包為可檢查的專家系統工件。第三，該系統在三個層面上進行評估：基於場景的任務套件、對六種協調範式在跨功能危機任務中的全面比較，以及與基於規則的RPA和無干預基準的365天代理介入模擬。在這些層面上，所提出的多代理方法顯著優於基準，並且該系統在模擬運行一年內保持零缺貨，而基於規則的基準在相同需求流下積累了數百個缺貨。這項工作顯示，在人類監督下，角色對齊的LLM代理可以將ERP系統從被動記錄交易轉變為主動執行操作決策，並提供了一個自主企業資源規劃的參考架構和評估協議。

##### **AIGB-R1: Self-Evolving Generative Auto-Bidding via Hierarchical Planner-Executor Optimization**
2607.17281v1 by Yuejia Dou, Hesong Wang, Xinyu Zhang, Tianyu Wang, Zhilin Zhang, Chuan Yu, Jian Xu, Bo Zheng, Qi Qi

Auto-bidding plays an essential role in online advertising, automatically adjusting bids for advertisers to optimize their commercial goals. The emerging AI-Generated Bidding (AIGB) paradigm widely adopts generative modeling to optimize bidding strategies, yet suffers from the limited mode coverage of offline datasets and inadequate task-state understanding, hindering effective exploration of optimal strategies. Large Language Models (LLMs), with prior world knowledge and reasoning capabilities, offer a promising approach to overcome these limitations. However, directly applying LLMs to auto-bidding tasks faces inherent challenges in limited numerical precision, hallucinations, and inference latency. To address these limitations, we propose AIGB-R1, a hierarchical self-evolving auto-bidding framework aiming to enhance AI-Generated Bidding via LLMs' Reasoning capabilities, comprising a high-level Planner module for macro-level strategy planning and a low-level Executor module for fine-grained decision-making. Building upon this, we design an experience-driven self-evolving loop, enabling autonomous strategy exploration and optimization from accumulated experience. We adopt a two-stage pipeline of offline pre-training and post-training alignment, and build an interactive bidding simulation environment for strategy rollout. Furthermore, we propose Decoupled Group Relative Policy Optimization (D-GRPO) to achieve end-to-end optimization via advantage decoupling. Experimental results on a large-scale public dataset demonstrate the effectiveness of AIGB-R1.

摘要：自動出價在網路廣告中扮演著重要角色，能夠自動調整廣告主的出價以優化其商業目標。新興的AI生成出價（AIGB）範式廣泛採用生成建模來優化出價策略，但受到離線數據集的模式覆蓋有限和任務狀態理解不足的影響，妨礙了有效探索最佳策略。大型語言模型（LLMs）具備先前的世界知識和推理能力，提供了一種有前景的方法來克服這些限制。然而，直接將LLMs應用於自動出價任務面臨著有限數值精度、幻覺和推理延遲等固有挑戰。為了解決這些限制，我們提出了AIGB-R1，一個層次化自我演化的自動出價框架，旨在通過LLMs的推理能力增強AI生成出價，包含一個高階規劃者模塊用於宏觀策略規劃，以及一個低階執行者模塊用於細緻的決策制定。在此基礎上，我們設計了一個經驗驅動的自我演化循環，使得能夠從累積的經驗中自主探索和優化策略。我們採用離線預訓練和後訓練對齊的兩階段管道，並建立了一個互動出價模擬環境以進行策略推廣。此外，我們提出了解耦群體相對政策優化（D-GRPO），通過優勢解耦實現端到端優化。在大規模公共數據集上的實驗結果顯示了AIGB-R1的有效性。

##### **Safety That Does Not Transfer: Cross-Lingual Clinical Correctness Drift in Deployable Medical Language Models**
2607.17270v1 by Anthonio Oladimeji Gabriel, Dimeji Olawuyi, Toba Ajayi, Temilola Aderemi

Safety evaluation of large language models is conducted predominantly in English and predominantly on frontier systems. Neither condition describes how such models are encountered in low-resource health settings, where small quantised systems are run locally and queried in local languages. We ask whether clinical safety established in English transfers to Hausa, and whether any failure is attributable to the language, the clinical task, or the class of model that low-resource deployment admits. Matched English-Hausa question pairs were built for three conditions of high burden in northern Nigeria: malaria, sickle cell disease, and tuberculosis, probing knowledge recall, emergency triage, a leading question inviting a contraindicated action, and a traditional-remedy claim. Six models were evaluated: five locally deployable systems of 4-9 billion parameters, two medically fine-tuned, and one frontier system. All 128 responses were scored against Nigerian national treatment guidelines by two fluent Hausa speakers working independently and blind to one another. Among locally deployable models, mean clinical correctness fell from 1.57 in English to -0.03 in Hausa, on a scale where 2 denotes a correct answer and -1 an actively harmful one. The frontier model moved from 2.00 to 1.75 and produced no response judged harmful in either language. Drift was consistent across all three conditions. Inter-rater agreement was substantial for clinical correctness (kappa = 0.70); agreement on harm was initially poor (kappa = 0.22) and is examined in detail. Because a frontier model answers the same questions competently in Hausa, the deficit is a property neither of the language nor of the clinical material, but of the deployable tier.

摘要：大型語言模型的安全評估主要在英語中進行，且主要針對前沿系統進行。這兩個條件都無法描述這些模型在低資源健康環境中的應用，這些環境中小型量化系統在本地運行並使用當地語言進行查詢。我們詢問在英語中建立的臨床安全性是否能轉移到豪薩語，並且任何失敗是否可歸因於語言、臨床任務或低資源部署所允許的模型類別。針對尼日利亞北部三種高負擔情況：瘧疾、鐮刀型紅血球病和結核病，建立了匹配的英豪問題對，探討知識回憶、緊急分診、一個引導性問題誘導禁止的行動，以及傳統療法的主張。評估了六個模型：五個可在本地部署的系統，參數數量在40億到90億之間，兩個經過醫學精調的，還有一個前沿系統。所有128個反應都由兩位流利的豪薩語使用者獨立且盲目地根據尼日利亞國家治療指南進行評分。在可本地部署的模型中，臨床正確率的平均值從英語的1.57降至豪薩語的-0.03，這個評分標準中2表示正確答案，-1表示主動有害的答案。前沿模型的得分從2.00降至1.75，並在任何語言中都沒有產生被判定為有害的反應。所有三種情況下的偏差是一致的。臨床正確性的一致性評價相當高（kappa = 0.70）；對於有害性的一致性最初較差（kappa = 0.22），並將進行詳細檢查。由於前沿模型能夠在豪薩語中熟練回答相同問題，因此這一缺陷既不是語言的特性，也不是臨床材料的特性，而是可部署層級的特性。

##### **An Explicit World Model Based on Data-First Ontology: DaoQL Multimodal Storage Validation and Counterfactual Reasoning Evaluation**
2607.17269v1 by Zhanbo Li, Shifeng Wu, Xiangjin Meng, Wenjie Cai

Large language models encode world models implicitly in neural weights, which exposes four structural risks in high-precision domains such as medicine and finance: hallucination, frozen knowledge, poor explainability, and poor modifiability. This paper proposes data-first ontology: LLMs are treated as reasoning and language engines, while deterministic knowledge is moved into an explicit multimodal database, DaoQL. We formalize an explicit world model and show that, under rule independence, deterministic evaluation, and fixed conflict resolution, explicit models provide a sufficient condition for composable counterfactual decomposability; implicit models lack atomic read/delta semantics and therefore provide no comparable architectural guarantee. The implemented system focuses on DaoQL's verified storage layer and explicit Eval path, integrating graph, column, vector, and full-text engines within one process. KVCache graph nodes, expert hot updates, and the DaoQL-Agent runtime remain future work. On an embedded same-machine setup, DaoQL reports graph BFS at 1.20 ms, HNSW at 83.1 us, and a Fluent hybrid query at 105.8 us; these results indicate engineering potential but must be interpreted with deployment-shape differences from client-server systems. Exploratory measurements on LDBC SNB SF1 and ANN-Benchmarks further show 34/34 query coverage with interactive-class queries mostly in the sub-millisecond to millisecond range, but only 1.8 QPS overall due to long-tail BI/IC queries; ANN-Benchmarks reaches Recall@10 >= 99% at thousand-level QPS after a bridge-edge protection fix. In a five-domain counterfactual experiment (n = 1250), DaoQL+GPT-4o achieves 94% composable counterfactual decomposability, 49 percentage points above GPT-4o alone. The paper explicitly separates provable structure, preliminary empirical evidence, and architectural roadmap claims.

摘要：大型語言模型在神經權重中隱式編碼世界模型，這在醫療和金融等高精度領域暴露出四個結構風險：幻覺、靜態知識、解釋性差和可修改性差。本文提出數據優先本體論：將大型語言模型視為推理和語言引擎，同時將確定性知識移入一個明確的多模態數據庫DaoQL。我們形式化了一個明確的世界模型，並顯示在規則獨立性、確定性評估和固定衝突解決的情況下，明確模型提供了可組合的反事實可分解性的充分條件；隱式模型缺乏原子讀取/增量語義，因此不提供可比的架構保證。實施的系統專注於DaoQL的驗證存儲層和明確的評估路徑，將圖形、列、向量和全文引擎整合在一個過程中。KVCache圖形節點、專家熱更新和DaoQL-Agent運行時仍然是未來的工作。在嵌入的同機設置中，DaoQL報告圖形BFS為1.20毫秒，HNSW為83.1微秒，流暢混合查詢為105.8微秒；這些結果顯示出工程潛力，但必須與客戶端-服務器系統的部署形狀差異一起解釋。對LDBC SNB SF1和ANN-Benchmarks的探索性測量進一步顯示34/34的查詢覆蓋率，交互類查詢大多在亞毫秒到毫秒範圍內，但由於長尾BI/IC查詢，整體僅為1.8 QPS；ANN-Benchmarks在進行橋邊保護修復後，達到Recall@10 >= 99%的千級QPS。在一個五領域的反事實實驗中（n = 1250），DaoQL+GPT-4o實現了94%的可組合反事實可分解性，比單獨的GPT-4o高出49個百分點。本文明確區分了可證明的結構、初步的實證證據和架構路線圖的主張。

##### **Debate-on-Graph: Reliable and Adaptive Reasoning of Large Language Model on Uncertain Knowledge Graph**
2607.17266v1 by Peiji Yu, Xin Chen, Tianxing Wu

Large language models (LLMs) have demonstrated remarkable capabilities in natural language processing. However, LLMs often suffer from hallucinations and lack of relevant knowledge when dealing with question answering (QA) tasks. To mitigate these issues, knowledge graphs (KGs) have been utilized to enhance LLM reasoning. Nevertheless, KGs often contain noise and errors, while existing KG-enhanced LLM approaches are generally unable to identify and filter such noisy and erroneous content, which can instead amplify hallucinations and pose challenges for reliable reasoning. Uncertain knowledge graphs (UKGs), which associate each triple with a confidence score to quantify uncertainty, offer a promising direction to address this challenge. Compared with prior work, we investigate how to leverage UKGs to support LLMs for QA. We propose Debate-on-Graph (DoG), a new framework that enables LLMs and UKGs to collaborate adaptively for reliable reasoning. Specifically, we first design a heuristic search algorithm tailored for UKGs to extract reliable and question-relevant subgraphs, thereby reducing noise and errors in retrieved knowledge. We then introduce a Multi-Agent Debate mechanism, which yields reliable answers through adaptive adversarial debates, aiming to fully exploit the knowledge in UKGs while preserving the reliability of retrieved evidence. Extensive experiments on four benchmark QA datasets show that DoG achieves state-of-the-art performance over existing LLM reasoning methods and KG-based baselines, while enabling reliable and adaptive reasoning. Our code is available at https://github.com/seucoin/Debate-on-Graph.

摘要：大型語言模型（LLMs）在自然語言處理方面展示了卓越的能力。<lb/>然而，LLMs 在處理問題回答（QA）任務時，經常遭遇幻覺和缺乏相關知識的問題。<lb/>為了減輕這些問題，知識圖譜（KGs）被用來增強 LLM 的推理能力。<lb/>然而，KGs 經常包含噪音和錯誤，而現有的 KG 增強 LLM 方法通常無法識別和過濾這些噪音和錯誤內容，反而可能放大幻覺並對可靠推理造成挑戰。<lb/>不確定知識圖譜（UKGs）將每個三元組與置信度分數相關聯，以量化不確定性，為解決這一挑戰提供了一個有前景的方向。<lb/>與先前的工作相比，我們研究如何利用 UKGs 來支持 LLMs 進行 QA。<lb/>我們提出了 Debate-on-Graph（DoG），這是一個新的框架，使 LLMs 和 UKGs 能夠自適應地協作以實現可靠推理。<lb/>具體而言，我們首先設計了一種針對 UKGs 的啟發式搜索算法，以提取可靠且與問題相關的子圖，從而減少檢索知識中的噪音和錯誤。<lb/>然後，我們介紹了一種多代理辯論機制，通過自適應對抗性辯論產生可靠的答案，旨在充分利用 UKGs 中的知識，同時保持檢索證據的可靠性。<lb/>在四個基準 QA 數據集上的廣泛實驗表明，DoG 在現有 LLM 推理方法和基於 KG 的基準上實現了最先進的性能，同時實現了可靠和自適應的推理。<lb/>我們的代碼可在 https://github.com/seucoin/Debate-on-Graph 獲得。

##### **Distilled Reinforcement Learning for LLM Post-training**
2607.17247v1 by Chen Wang, Zhaochun Li, Jionghao Bai, Yining Zhang, Hexuan Deng, Ge Lan, Yue Wang

Large language model (LLM) post-training is essential for improving reasoning, adaptation, and alignment. Existing methods mainly follow two paradigms: reinforcement learning (RL) and on-policy distillation (OPD). However, RL relies on coarse-grained outcome supervision, resulting in difficult credit assignment and limited capability to acquire new knowledge. OPD, meanwhile, unconditionally matches teacher logits through KL divergence, which creates a dilemma: similar teachers provide little new knowledge, while substantially different teachers often yield ineffective guidance, largely restricting OPD to within-family distillation. We propose Distilled Reinforcement Learning (Distilled RL), which integrates teacher supervision into the RL objective to provide fine-grained guidance, selectively transfer new knowledge and avoid unconditional imitation. Distilled RL contains three components: reverse importance sampling with clipping, negative sample reset, and sequence-level geometric normalization. Through a concise and interpretable case study, we demonstrate that Distilled RL can effectively transfer previously unavailable knowledge from a teacher model to a student model. Extensive experiments across both within-family and cross-family distillation settings show that Distilled RL substantially outperforms standard RL and OPD in terms of both pass@1 and pass@k. Our code is available at https://github.com/597358816/Distilled-RL.

摘要：大型語言模型（LLM）後訓練對於改善推理、適應和對齊至關重要。<lb/>現有的方法主要遵循兩種範式：強化學習（RL）和政策內蒸餾（OPD）。<lb/>然而，RL 依賴於粗粒度的結果監督，導致難以進行信貸分配，並且獲取新知識的能力有限。<lb/>與此同時，OPD 無條件地通過 KL 散度匹配教師的 logits，這造成了一個兩難：相似的教師提供很少的新知識，而實質上不同的教師往往會產生無效的指導，這在很大程度上限制了 OPD 在同一家族蒸餾中的應用。<lb/>我們提出了蒸餾強化學習（Distilled RL），它將教師監督整合到 RL 目標中，以提供細粒度的指導，選擇性地轉移新知識並避免無條件模仿。<lb/>蒸餾 RL 包含三個組件：帶剪裁的反向重要性抽樣、負樣本重置和序列級幾何正規化。<lb/>通過一個簡潔且可解釋的案例研究，我們展示了蒸餾 RL 可以有效地將以前不可用的知識從教師模型轉移到學生模型。<lb/>在同一家族和跨家族蒸餾設置中進行的廣泛實驗顯示，蒸餾 RL 在 pass@1 和 pass@k 方面大幅超越了標準 RL 和 OPD。<lb/>我們的代碼可在 https://github.com/597358816/Distilled-RL 獲得。

##### **Toward Anthropomorphic Dialogue: A Closed-Loop Framework for Human-Like Chat Generation, Evaluation, and Preference Alignment**
2607.17191v1 by Wentao Liu, Siyu Song, Xi Chen, Youjia Li, Xiaokun Wang, Min Ji, Ji Wang

Human-like private chat requires more than fluent response generation: a system must preserve persona, relationship, memory, bounded knowledge, medium-specific timing, and a coherent multi-turn arc. We present AnthroDial, a closed-loop framework that formulates anthropomorphic dialogue as a joint problem of system architecture, executable evaluation, and diagnostic alignment. It combines (1) a role-conditioned scheduled dialogue runtime with persona and scenario cards, long-term memory, virtual time, and single-draft message decisions; (2) an executable benchmark with an L0 validity gate, five per-turn dimensions, and five dialogue-level dimensions; and (3) a post-training pipeline that filters 16,436 scheduled-decision examples for SFT and applies GRPO with a cognitive-diagnostic, ZPD-aware reward. The reward maintains Kalman-filtered capability estimates for each behavioral dimension, upweights dimensions with larger capability deficits, and uses rollout scores as task-level ZPD matches to focus optimization on learnable weak skills. On a benchmark with 55 personas, 50 scenarios, 50 persona-scenario bindings, and 100 role-conditioned cases per model, we evaluate 16 systems spanning frontier baselines, open models, thinking/no-think variants, and SFT/RL ablations. The strongest non-trained baseline reaches 32.00% strict ACC, while Qwen3.6-27B-SFT+RL reaches 39.00% strict ACC and a 98.5 overall score. In the 9B no-think setting, SFT and RL improve strict ACC from 0.00% to 13.00% and 18.37%. These results show that anthropomorphic dialogue benefits when generation, evaluation, and reward shaping share the same behavioral dimensions.

摘要：人類般的私人聊天不僅需要流暢的回應生成：系統必須保持角色、關係、記憶、有限知識、媒介特定的時機和一致的多輪弧。我們提出了AnthroDial，一個將擬人化對話公式化為系統架構、可執行評估和診斷對齊的聯合問題的閉環框架。它結合了（1）一個角色條件的計劃對話運行時，配備角色和場景卡、長期記憶、虛擬時間和單次草稿消息決策；（2）一個可執行的基準，具有L0有效性閘、每輪五個維度和五個對話層級維度；以及（3）一個後訓練管道，過濾16,436個計劃決策範例以進行SFT，並應用GRPO，具有認知診斷和ZPD意識的獎勵。該獎勵保持每個行為維度的卡爾曼濾波能力估計，對能力缺口較大的維度進行加權，並使用回滾分數作為任務層級的ZPD匹配，將優化重點放在可學習的弱技能上。在一個擁有55個角色、50個場景、50個角色-場景綁定和每個模型100個角色條件案例的基準上，我們評估了16個系統，涵蓋前沿基準、開放模型、思考/不思考變體以及SFT/RL消融實驗。最強的非訓練基準達到32.00%的嚴格ACC，而Qwen3.6-27B-SFT+RL達到39.00%的嚴格ACC和98.5的總分。在9B不思考設置中，SFT和RL將嚴格ACC從0.00%提升至13.00%和18.37%。這些結果顯示，當生成、評估和獎勵塑造共享相同的行為維度時，擬人化對話會受益。

##### **SlotGuard: Stop Oversharing Private Local Context in LLM Agent Transcri**
2607.17147v1 by Haocheng Xia, Yongjoo Park

LLM agents can leak privacy (e.g., paths, emails) and credentials (e.g., API keys) as agent observations (e.g., tool outputs, shell logs, and file reads) are appended to provider-bound transcripts. Existing placeholder redaction is brittle: it can miss embedded or cross-turn references, over-redact benign lookalikes, and destroy the structure useful for reasoning. We present SlotGuard, a local transcript boundary that can hide sensitive data while retaining agents' performance. SlotGuard rewrites structural bindings as typed, suffix-aware slots, replaces secrets with format-preserving synthetic values, links cross-turn references with a lightweight session graph, and restores raw values only inside the trusted runtime. On controlled repository-oriented agent transcripts, SlotGuard removes all 20,814 annotated structurally sensitive characters across 9,229 paths and reduces credential leakage to 0.0\% across 852 planted values. It remains close to raw-transcript task success across four upstream models, while generic redaction drops to 2.5\%. Transcript rewriting takes a median of 14.424~$μ$s per agent turn. The code is publicly accessible at https://github.com/illinoisdata/SlotGuard.

摘要：LLM 代理可能會洩漏隱私（例如，路徑、電子郵件）和憑證（例如，API 金鑰），因為代理觀察（例如，工具輸出、Shell 日誌和文件讀取）會附加到供應商綁定的記錄中。現有的佔位符刪除方法脆弱：它可能會錯過嵌入或跨回合的引用，過度刪除無害的相似項，並破壞有助於推理的結構。我們提出了 SlotGuard，一種本地記錄邊界，可以在保持代理性能的同時隱藏敏感數據。SlotGuard 將結構綁定重寫為類型化的、後綴感知的插槽，將秘密替換為格式保留的合成值，通過輕量級會話圖鏈接跨回合引用，並僅在受信任的運行時內恢復原始值。在受控的以庫為導向的代理記錄中，SlotGuard 移除了 9,229 梯路中的所有 20,814 個標註的結構敏感字符，並將憑證洩漏降低至 0.0\%，共計 852 個植入值。在四個上游模型中，它的任務成功率接近原始記錄，而通用刪除則降至 2.5\%。記錄重寫每個代理回合的中位數為 14.424~$μ$s。代碼可在 https://github.com/illinoisdata/SlotGuard 公開訪問。

##### **Teach it to stop, not just to click**
2607.17136v1 by Barada Sahu, Shivesh Pandey

Agentic computer-use RL is reported in single runs, and those numbers mislead. Using verifier-guided repair of a 35B computer-use agent (CUA) across five oracle-graded environments, we show a repaired policy's success rate is dominated by upstream variance: a variance-components decomposition across three cells (crossed data-draw $\times$ seed grid, bootstrap CIs) finds evaluation variance negligible ($σ_{\mathrm{eval}} \approx 0$) and the training-seed effect small everywhere ($\leq 10\%$); instead it splits between the data draw and run-to-run nondeterminism, the data draw's share rising to dominant ($48\%$) on the hardest cell. There the run-to-run distribution is bimodal (Hartigan dip $p=0.07$, $k=10$), so a single run has roughly a 30% chance of the failure mode and mean$\pm$std is the wrong summary. On that footing, two findings hold. First, repairability is two-tier in how constrained the corrective action is: a single fixed token installs reliably (done-detection $0.97\pm0.06$), while open-ended corrections are only partial -- spatial-coordinate clicks (grounding $0.53\pm0.35$) and a generative field-fill ($0.14\pm0.04$). Second, the frame-level repair transfers to task success only when the corrective action is the task's sole remaining blocker (LinkedIn 8/20 vs. base 0/15, Fisher $p=0.006$). We caught two of our own over-claims -- a sample-efficiency curve and a 'grounding cannot be bought' boundary -- only by replicating across seeds; a stress test makes the stakes external: a single-run improvement of the size this field publishes would have the wrong sign roughly one-third of the time in a comparable regime. We release a library (cua_reliability) for routine k-seed reporting. The apparatus is, to our knowledge, the first multimodal segment-aggregated on-policy self-distillation (SA-OPSD) update on a real 35B CUA policy.

摘要：代理電腦使用強化學習（RL）在單次運行中報告，而這些數字會誤導人。通過在五個由神諭評分的環境中使用驗證者引導的修復一個35B電腦使用代理（CUA），我們顯示修復後的策略成功率受到上游變異的主導：對三個單元進行的變異成分分解（交叉數據抽樣 $\times$ 隨機種子網格，自助法置信區間）發現評估變異微不足道（$σ_{\mathrm{eval}} \approx 0$），而訓練種子效應在各處都很小（$\leq 10\%$）；相反，它在數據抽樣和運行到運行的非確定性之間分裂，數據抽樣的份額在最難的單元上上升到主導（$48\%$）。在那裡，運行到運行的分布是雙峰的（Hartigan dip $p=0.07$, $k=10$），因此單次運行大約有30%的機會出現失敗模式，而平均$\pm$標準差是錯誤的總結。在這種情況下，有兩個發現成立。首先，修復能力是兩級的，取決於修正行動的約束程度：單個固定標記可靠地安裝（完成檢測 $0.97\pm0.06$），而開放式修正僅為部分 -- 空間坐標點擊（基礎 $0.53\pm0.35$）和生成場填充（$0.14\pm0.04$）。其次，框架級別的修復僅在修正行動是任務唯一剩餘障礙時才會轉移到任務成功（LinkedIn 8/20 對比基準 0/15，Fisher $p=0.006$）。我們僅通過在隨機種子間複製，捕捉到我們自己的兩個過度聲明 -- 一個樣本效率曲線和一個“基礎無法購得”的邊界；壓力測試使風險外部化：在可比較的範疇中，這個領域發布的單次運行改進大約有三分之一的時間會有錯誤的符號。我們發布了一個庫（cua_reliability）以進行常規的k種子報告。據我們所知，該設備是第一個在真實的35B CUA策略上進行的多模態段聚合的在線自我蒸餾（SA-OPSD）更新。

##### **Otap:Structure-Aware Optimal Transport for Evaluating Planning and Execution in Agent Trajectories**
2607.17082v1 by Babak Barazandeh, Subhabrata Majumdar, George Michailidis

Large language model agents solve tasks by generating trajectories that interleave planning, tool calls, and intermediate results. Current evaluation metrics reduce such a trajectory to a binary success flag or compare it against a reference by exact matching. A success flag cannot distinguish a sound solution from one that succeeds by luck, and says nothing about why a failed run went wrong. Exact matching penalizes plans that are valid but reordered or decomposed differently from the reference. We reframe trajectory evaluation as a distance between the agent's execution graph and a set of valid solution graphs, and instantiate it via an unbalanced fused Gromov-Wasserstein transport problem over attributed dependency graphs. The resulting score, termed \otap{} (Optimal Transport for Agentic Planning), is a pseudo-metric that is provably invariant to dependency-preserving reorderings and has bounded sensitivity to redundant steps. Its unbalanced marginals handle missing or hallucinated steps without forcing a match, and its soft coupling accommodates variation in plan granularity. On controlled perturbations and three public benchmarks, \otap{} separates valid from invalid trajectories in a regime where semantics-only metrics score below chance. Its accuracy is highest when the dependency graph is recovered exactly, and drops only when the graph is inferred heuristically from free-text traces.

摘要：大型語言模型代理透過生成交錯規劃、工具調用和中間結果的軌跡來解決任務。當前的評估指標將這樣的軌跡簡化為二元成功標誌，或通過精確匹配與參考進行比較。成功標誌無法區分合理解決方案與僅僅依賴運氣成功的方案，也無法說明失敗執行為何出錯。精確匹配會對有效但與參考順序不同或拆解方式不同的計劃進行懲罰。我們將軌跡評估重新定義為代理執行圖與一組有效解決圖之間的距離，並通過一個不平衡的融合 Gromov-Wasserstein 運輸問題在屬性依賴圖上實現它。得到的分數稱為 \otap{}（代理規劃的最優運輸），是一種伪度量，證明對於保持依賴關係的重新排序是不變的，並且對冗餘步驟的敏感性是有界的。其不平衡邊際處理缺失或幻覺的步驟，而無需強制匹配，並且其柔性耦合適應計劃粒度的變化。在受控擾動和三個公共基準上，\otap{} 在語義僅指標得分低於機會的範疇中，能夠區分有效和無效的軌跡。當依賴圖被精確恢復時，其準確性最高，只有在圖從自由文本痕跡中以啟發式推斷時才會下降。

##### **Bridging the Information Gap: Semantic Densification and Hindsight Distillation for Cold-Start Prediction**
2607.17070v1 by Hao Duong Le, Yifei Gao, Huan Li, Lun Jiang, Chen Bai, Ke Xing, Chen Zhang

New-user cold-start is a critical bottleneck for e-commerce platforms: predicting user lifetime value (LTV) and conversion rate (CVR) for users with sparse interaction history. Two prior directions -- LLM-based semantic augmentation and learning using privileged information (LUPI) -- each face a key limitation. First, LLM augmentation produces unstructured rationales that are noisy and hard to operationalize in production. Second, naive student-teacher distillation can be brittle due to an information gap between the privileged teacher and the sparse student; moreover, this gap is heterogeneous across users. We propose SemRaD, a Semantic Reasoning-aware Distillation framework addressing both limitations. First, a Structured Semantic Reasoning Pipeline replaces free-form rationales with a structured schema built via a discover-curate-audit workflow, producing per user a Densified Semantic Profile (consumed by the deployed student via a Semantic-Gated Encoder that focuses on the most informative dimensions) and a Hindsight Distillation Target reconciled from pre- and post-conversion reasoning (used only at training). Second, to bridge this gap and handle its heterogeneity, a Hindsight-Aware Distillation Network transfers privileged knowledge via the hindsight target, with Distillation Experts improving transfer under per-user variability. On a large-scale industrial dataset, SemRaD lifts +1.9% LTV (Gini) and +1.0% CVR (AUROC) over a production-grade base; a four-week online A/B at Keeta confirms +1.0% LTV / +0.43% CVR. SemRaD also matches the production system's LTV using only 9% of the training data while improving CVR by 0.8%.

摘要：新用戶冷啟動是電子商務平台的一個關鍵瓶頸：對於互動歷史稀疏的用戶，預測用戶終身價值（LTV）和轉換率（CVR）。兩個先前的方向——基於LLM的語義增強和使用特權信息的學習（LUPI）——各自面臨一個關鍵限制。首先，LLM增強產生的非結構化理由噪聲較大，且在生產中難以操作化。其次，天真的學生-教師蒸餾可能因特權教師和稀疏學生之間的信息差距而脆弱；此外，這一差距在用戶之間是異質的。我們提出了SemRaD，一個語義推理感知的蒸餾框架，解決了這兩個限制。首先，結構化語義推理管道用一個通過發現-策劃-審核工作流程構建的結構化模式替代自由形式的理由，為每個用戶生成一個密集的語義檔案（通過專注於最具信息維度的語義門控編碼器消耗）和一個從轉換前後推理中調和的後見蒸餾目標（僅在訓練中使用）。其次，為了彌補這一差距並處理其異質性，後見感知蒸餾網絡通過後見目標轉移特權知識，蒸餾專家在每個用戶的變異性下改善轉移。在一個大規模的工業數據集上，SemRaD使LTV（Gini）提升了+1.9%和CVR（AUROC）提升了+1.0%相較於生產級基準；在Keeta進行的為期四週的在線A/B測試確認了+1.0% LTV / +0.43% CVR。SemRaD還僅使用9%的訓練數據便達到了生產系統的LTV，同時CVR提升了0.8%。

##### **Learning from Synthetic Data without Model Collapse in Iterative Instruction Tuning**
2607.17043v1 by Xiaonan Luo, Yue Huang, Kehan Guo, Ping He, Chuan Zou, Ting Hua, Xiangliang Zhang

Model collapse is a central challenge in learning from synthetic data: as later-generation large language models (LLMs) are trained on an increasing proportion of model-generated data, performance can degrade due to narrowed coverage and accumulated bias. Existing work mainly studies how to bound this degradation. In iterative model evolution, however, the more meaningful objective is to ensure that each successive model improves over its predecessor, which requires diagnosing collapse at a granularity that is actionable for data curation. We study this problem in synthetic data self-improving for instruction tuning. We show that collapse in this setting is not simply uniform performance degradation, but can appear as a polarization of competence, where synthetic training reinforces already strong skills while further degrading weak ones. Motivated by this observation, we propose KITE (Knowledge-boundary Instruction Tuning via Exploration), a two-stage framework that combines failure-guided data generation with boundary-aware uncertainty curation. Experiments across several datasets and multiple open-source LLMs show that KITE yields more stable improvement than strong synthetic-data baselines.

摘要：模型崩潰是從合成數據學習中的一個核心挑戰：隨著後續世代的大型語言模型（LLMs）在越來越多的模型生成數據上進行訓練，性能可能因為覆蓋範圍狹窄和累積偏見而下降。現有的研究主要探討如何限制這種退化。然而，在迭代模型演進中，更有意義的目標是確保每個後續模型比其前身有所改善，這需要在可操作的數據策劃層面上診斷崩潰。我們在合成數據自我改進的指令調整中研究這個問題。我們顯示，在這種情況下的崩潰不僅僅是均勻的性能退化，而是可以表現為能力的極化，其中合成訓練加強了已經強大的技能，同時進一步削弱了弱技能。基於這一觀察，我們提出了KITE（知識邊界指令調整透過探索），這是一個兩階段的框架，結合了失敗引導的數據生成與邊界感知的不確定性策劃。在多個數據集和多個開源LLM上的實驗顯示，KITE比強大的合成數據基準產生了更穩定的改進。

##### **Reward-Driven LLM Agent Workflows: Synthesizing POMDP Routing and Self-Correction for Autonomous Decision-Making**
2607.17038v1 by Amez Amanj Ali, Kuo-Kun Tseng

This paper addresses key technical challenges in current large language model (LLM) agent applications, including long-horizon planning, sparse reward attribution, and dynamic environmental interaction, by designing and optimizing an intelligent agent workflow. The proposed architecture is based on the synthesis of core AI paradigms: Visual, Language, Generative, Graph, Multimodal, Reinforcement, and Agent Intelligence. Unlike conventional baseline models that rely on static prompting and lack robust perception-action loops, our approach introduces a Partially Observable Markov Decision Process (POMDP) routing mechanism. This mechanism is augmented with an internal, self-correcting reward model that evaluates decision trajectories before execution. By integrating multimodal inputs and advanced reinforcement learning principles (such as proximal policy optimization and value function approximation), the agent maintains long-term structural memory and dynamically adapts its reasoning pathways to mitigate error accumulation. Empirical experiments on the ALFWorld embodied simulation environment and the WebShop online navigation benchmark demonstrate a 24.5% absolute improvement in task success rate and trajectory efficiency over mainstream baselines like the standard ReAct framework. Comprehensive ablation studies confirm the significant contribution of the reward-driven critique module in suppressing hallucination rates. This research bridges theoretical foundations of reinforcement learning and graph-based memory with autonomous agent workflows. Ultimately, the resulting architecture offers a practical, scalable reference framework for developing artificial intelligence technologies in complex, multi-step autonomous systems. Code is available at https://github.com/01Amez/RLAW_Implementation.

摘要：這篇論文針對當前大型語言模型（LLM）代理應用中的關鍵技術挑戰進行探討，包括長期規劃、稀疏獎勵歸因和動態環境互動，通過設計和優化智能代理工作流程來解決這些問題。所提出的架構基於核心人工智慧範式的綜合：視覺、語言、生成、圖形、多模態、強化和代理智慧。與依賴靜態提示且缺乏穩健感知-行動循環的傳統基線模型不同，我們的方法引入了一種部分可觀察馬爾可夫決策過程（POMDP）路由機制。這一機制增強了一個內部的自我修正獎勵模型，該模型在執行之前評估決策軌跡。通過整合多模態輸入和先進的強化學習原則（如近端策略優化和價值函數逼近），該代理維持長期結構記憶並動態調整其推理路徑以減少錯誤累積。在ALFWorld具身模擬環境和WebShop在線導航基準上的實證實驗顯示，任務成功率和軌跡效率相比主流基線（如標準ReAct框架）有24.5%的絕對提升。全面的消融研究確認了以獎勵驅動的批評模塊在抑制幻覺率方面的重要貢獻。本研究橋接了強化學習和基於圖形的記憶的理論基礎與自主代理工作流程。最終，所產生的架構為在複雜的多步自主系統中開發人工智慧技術提供了一個實用、可擴展的參考框架。代碼可在 https://github.com/01Amez/RLAW_Implementation 獲得。

##### **CADENCE: Closing the Reasoning Gap via Coverage-Adaptive On-Policy Distillation**
2607.16955v1 by Satyam Kumar, Saurabh Jha

On-policy knowledge distillation transfers reasoning from large teachers to compact students, but existing approaches suffer three compounding failure modes: (i) cold-start collapse, where a fresh student assigns near-zero mass to teacher-preferred tokens; (ii) state-agnostic divergence scheduling, where time-only forward/reverse-KL interpolation ignores the student's coverage state; and (iii) binary reward sparsity, where pass/fail signals discard information from partially correct traces.
  We present CADENCE, a unified framework with a targeted fix for each. Its DRIFT mechanism schedules a per-token convex mixture of forward-KL and reverse-KL surrogate objectives on student-sampled trajectories (per-token surrogates, not sequence-level KL gradient estimators). Six components extend it: (A) COVA, a coverage-adaptive $β$ schedule accelerating the forward-to-reverse transition; (B) FTB, a forking-token boost concentrating gradient at high-entropy positions via a globally-normalized entropy reference; (C) CCD, a dense reward adding numerical-proximity partial credit for incorrect-but-close traces; (D) LAP, brevity-preferential correct-rollout reinforcement; (E) EMR, an entropy-matching calibration regularizer; (F) BSD, a bootstrapped self-distillation phase.
  On GSM8K and MATH-500 (corrected 512-token protocol, 5 seeds, reported std), CADENCE distills a 0.5B student from a 1.5B teacher to 69.8 $\pm$ 0.5% GSM8K pass@1 (from 48.7% pretrained; 63.2% of the teacher gap closed) and to 72.1 $\pm$ 0.4% with a 3B teacher (76.2% closed), beating the strongest matched-compute label-using baseline (DRIFT+binary reward) by +4.4 $\pm$ 0.7 points. All experiments run on a single Apple Mac Studio (M-series, 64GB unified memory), showing principled distillation reaches strong reasoning quality without datacenter-scale hardware.

摘要：在政策知識蒸餾中，將推理從大型教師轉移到緊湊的學生，但現有的方法面臨三種複合的失敗模式：（i）冷啟動崩潰，當一個新學生對教師偏好的標記分配幾乎為零的質量；（ii）狀態無關的發散調度，僅依賴時間的前向/反向-KL插值忽略了學生的覆蓋狀態；以及（iii）二元獎勵稀疏性，通過通過/失敗信號丟棄來自部分正確痕跡的信息。<lb/>我們提出了CADENCE，一個統一框架，針對每個問題提供了針對性的修正。其DRIFT機制在學生採樣的軌跡上調度每個標記的前向-KL和反向-KL替代目標的凸混合（每個標記的替代，而不是序列級的KL梯度估計器）。六個組件擴展了它：（A）COVA，一個覆蓋自適應的$β$調度，加速前向到反向的過渡；（B）FTB，一個分叉標記增強，通過全局標準化的熵參考在高熵位置集中梯度；（C）CCD，一個密集獎勵，為不正確但接近的痕跡添加數值接近的部分信用；（D）LAP，偏好簡潔的正確展開強化；（E）EMR，一個熵匹配的校準正則化器；（F）BSD，一個自引導蒸餾階段。<lb/>在GSM8K和MATH-500（修正的512標記協議，5個種子，報告的標準差）上，CADENCE將一個0.5B的學生從1.5B的教師蒸餾到69.8 $\pm$ 0.5%的GSM8K pass@1（從48.7%的預訓練；關閉了63.2%的教師差距）以及與3B教師的72.1 $\pm$ 0.4%（關閉了76.2%），超越了最強的匹配計算標籤使用基線（DRIFT+二元獎勵）4.4 $\pm$ 0.7分。所有實驗均在單一的Apple Mac Studio（M系列，64GB統一內存）上運行，顯示出原則性的蒸餾在沒有數據中心規模硬件的情況下達到了強大的推理質量。

##### **Beyond Semantic Equivalence: Logical Graphs for LLM Uncertainty Quantification**
2607.16868v1 by Yanni Dong, Minghua Liu, Meiling Zhu, Xiaowei Huang, Lijun Zhang

Large Language Models (LLMs) often produce confidently stated yet unreliable outputs, posing critical challenges for deployment in safety-sensitive applications. Existing uncertainty metrics such as semantic entropy capture agreement at the level of semantic equivalence, but largely ignore the logical relationships between distinct answers. As a result, they tend to overestimate uncertainty and falsely flag hallucinations in settings where generated responses are diverse in form yet logically compatible (e.g., differing only in granularity or specificity). We propose Logical Graph Uncertainty (LGU), a framework that explicitly models implication and incompatibility among answers. LGU aggregates probability mass along entailment chains, computes entropy over logically maximal hypotheses, and penalizes mutual incompatibility among them. Across multiple question-answering benchmarks, LGU consistently improves uncertainty estimation over existing methods, and outperforms the semantic entropy baseline by up to +7.1% AUROC and +3.5% AUARC across datasets.

摘要：大型語言模型（LLMs）經常產生自信但不可靠的輸出，對於在安全敏感應用中的部署構成了重大挑戰。現有的不確定性度量，如語義熵，捕捉到語義等價層面的共識，但在很大程度上忽略了不同答案之間的邏輯關係。因此，它們往往高估不確定性，並在生成的回應形式多樣但邏輯上相容的情況下錯誤標記幻覺（例如，僅在粒度或具體性上有所不同）。我們提出了邏輯圖不確定性（LGU），這是一個明確建模答案之間的蕴含和不相容性的框架。LGU 沿著蕴含鏈聚合概率質量，計算邏輯最大假設的熵，並懲罰它們之間的相互不相容性。在多個問答基準中，LGU 一貫改善了現有方法的不確定性估計，並在各數據集上超越語義熵基準高達 +7.1% AUROC 和 +3.5% AUARC。

##### **AgentBrew: Lifelong Knowledge Brewing from Strong Teachers to Weak LLM Agents**
2607.16851v1 by Yangqin Jiang, Chao Huang

Deploying LLM agents typically requires a compact test-time student, even if a stronger teacher is available during training. We study knowledge brewing: distilling a teacher's interactive experience into a persistent external memory for the student. Crucially, this requires no weight updates, expert demonstrations, ground-truth labels, or test-time teacher access. This setting poses two challenges: environments provide only sparse, binary feedback, and teacher-authored notes must be inherently tailored to be concretely executable by a substantially weaker student. To address these hurdles, we propose AgentBrew, comprising two coupled components. First, a failure-triggered teacher--Ralph Loop mitigates sparse feedback by transforming student failures into environment-validated notes. Second, student-aware synthesis calibrates teacher knowledge to the weak executor's operational granularity, yielding model-specific, actionable guidance. Extensive evaluations and comprehensive ablations across coding, math, and tool-use tasks demonstrate that this asymmetric, training-free brewing paradigm produces highly capable yet deployable LLM agents.

摘要：部署 LLM 代理通常需要一個緊湊的測試時學生，即使在訓練期間有更強的教師可用。我們研究知識釀造：將教師的互動經驗提煉成持久的外部記憶供學生使用。關鍵是，這不需要權重更新、專家演示、真實標籤或測試時教師訪問。這種設置提出了兩個挑戰：環境僅提供稀疏的二元反饋，而教師撰寫的筆記必須本質上針對一個實質上較弱的學生進行具體可執行的定制。為了解決這些障礙，我們提出了 AgentBrew，由兩個耦合組件組成。首先，一個失敗觸發的教師——Ralph Loop 通過將學生的失敗轉化為環境驗證的筆記來減輕稀疏反饋。其次，學生感知合成將教師知識調整到弱執行者的操作粒度，產生特定於模型的可行指導。廣泛的評估和全面的消融實驗涵蓋了編碼、數學和工具使用任務，證明這種不對稱的、無需訓練的釀造範式能夠產生高度能力但可部署的 LLM 代理。

##### **From Overload to Insights: How AI Agents Can Support Scientists in Analyzing Complex Data**
2607.16845v1 by Tim Fuchs, Luca Gelisio, Steffen Hauf, Walid Maalej

Scientists at European XFEL conduct experiments that generate very large and complex datasets. The subsequent data analysis is challenging as scientists must combine their domain expertise with facility- and software-specific knowledge scattered across documentation, tools, and support channels. To address this problem, we designed and evaluated an agentic AI system tailored to the scientists' needs and integrated with the high-performance computing environment of European XFEL. Using a design science research approach, we conducted a rapid literature review, a systematic evaluation of 16 AI tools, multiple interviews, a focus group, and a user study with experts at European XFEL to develop and evaluate two prototypes. Our study identifies key knowledge challenges in scientific data analysis, derives requirements for an AI agent that supports knowledge retrieval and source code generation, and proposes design recommendations for a specialized system adaptable to the evolving AI tool landscape. These findings provide guidance for developing maintainable AI support in highly specialized scientific environments.

摘要：科學家在歐洲XFEL進行的實驗產生了非常大且複雜的數據集。隨後的數據分析具有挑戰性，因為科學家必須將他們的領域專業知識與分散在文檔、工具和支持渠道中的設施和軟件特定知識相結合。為了解決這個問題，我們設計並評估了一個針對科學家需求量身定制的代理AI系統，並與歐洲XFEL的高性能計算環境集成。使用設計科學研究方法，我們進行了快速文獻回顧、對16個AI工具的系統評估、多次訪談、一個焦點小組以及與歐洲XFEL專家的用戶研究，以開發和評估兩個原型。我們的研究確定了科學數據分析中的關鍵知識挑戰，推導出支持知識檢索和源代碼生成的AI代理的需求，並提出了針對可適應不斷發展的AI工具環境的專門系統的設計建議。這些發現為在高度專業化的科學環境中開發可維護的AI支持提供了指導。

##### **Spatiotemporal Facial Action Unit Detection using Twin Cycle Autoencoders for Driver Monitoring**
2607.16760v1 by Sai Sidharth D

Driver monitoring systems (DMS) increasingly rely on facial cues to infer drowsiness, distraction, and cognitive load in real time. Facial Action Units (AUs), grounded in the Facial Action Coding System (FACS), provide an objective and interpretable representation of such states, but their automatic detection in the driving context is complicated by low and variable illumination, partial occlusion, head-pose variation, and the subtlety and short duration of relevant AU activations. Existing AU detectors largely treat spatial appearance and temporal dynamics separately, limiting their ability to exploit self-supervisory signal from abundant unlabeled driving video. We propose the Twin Cycle Autoencoder (TCA), a spatiotemporal architecture composed of two coupled cycle-consistent autoencoder branches: a Spatial Cycle Autoencoder that disentangles AU-relevant appearance from identity through image-level cycle consistency, and a Temporal Cycle Autoencoder that enforces forward-backward consistency over latent AU trajectories to capture onset-apex-offset dynamics. The two branches are coupled through a cross-branch latent alignment loss and fused via an attention module before multi-label AU classification. We evaluate TCA on the DISFA and BP4D benchmarks and on an in-cabin naturalistic driving dataset, and observe consistent improvements over CNN-RNN, 3D-CNN, and graph-based AU baselines, particularly for low-intensity and rapidly transitioning AUs relevant to fatigue (AU45, AU43) and yawning (AU26). We further show the model sustains real-time throughput on an embedded Jetson Xavier NX platform, supporting its use in production-grade advanced driver assistance systems (ADAS).

摘要：駕駛者監控系統 (DMS) 越來越依賴面部線索來即時推斷疲勞、分心和認知負荷。面部動作單元 (AUs)，基於面部動作編碼系統 (FACS)，提供了這些狀態的客觀和可解釋的表徵，但在駕駛環境中自動檢測它們受到低且變化不定的光照、部分遮擋、頭部姿勢變化以及相關 AU 激活的微妙性和短暫性的影響。現有的 AU 檢測器在很大程度上將空間外觀和時間動態分開處理，限制了它們利用大量未標記的駕駛視頻中的自我監督信號的能力。我們提出了雙循環自編碼器 (TCA)，這是一種時空架構，由兩個耦合的循環一致性自編碼器分支組成：一個空間循環自編碼器通過影像級循環一致性將與 AU 相關的外觀與身份解耦，另一個時間循環自編碼器則對潛在 AU 軌跡施加前後一致性，以捕捉啟動-頂峰-結束的動態。這兩個分支通過跨分支潛在對齊損失耦合，並在多標籤 AU 分類之前通過注意力模塊融合。我們在 DISFA 和 BP4D 基準以及一個駕駛艙自然駕駛數據集上評估 TCA，並觀察到相較於 CNN-RNN、3D-CNN 和基於圖的 AU 基準，特別是在與疲勞 (AU45, AU43) 和打哈欠 (AU26) 相關的低強度和快速過渡的 AU 上有一致的改進。我們進一步展示該模型在嵌入式 Jetson Xavier NX 平台上維持實時吞吐量，支持其在生產級先進駕駛輔助系統 (ADAS) 中的應用。

##### **Constraint-Anchored Reasoning Traces**
2607.16727v1 by Zehua Cheng, Wei Dai, Jiahao Sun

Autoregressive multimodal large language models (MLLMs) suffer from error snowballing: a single incorrect inference early in a chainof-thought (CoT) trace corrupts all downstream reasoning. We find that in state-of-the-art open-source MLLMs, once the first error occurs, the reasoning cascades into failure across all remaining steps in 65% of such cases (a metric we term the snowball rate). Existing mitigations-sampling multiple chains, post-hoc self-verification, or full program synthesis-either lack symbolic grounding, catch errors too late, or sacrifice the flexibility of natural language reasoning. We propose Constraint-Anchored Reasoning Traces (CART), a neuro-symbolic framework that trains MLLMs to interleave natural language reasoning steps with symbolic constraint assertions: lightweight, machine-checkable statements about visual content (e.g., count(red_objects) = 3). A dual-pronged Constraint Propagation Module-combining a learned neural grounding head with Boolean Constraint Propagation-continuously verifies these anchors against extracted visual features and checks their mutual logical consistency. When a contradiction is detected, a backtrack controller halts generation and reverts to the last consistent checkpoint, preventing error propagation. A variable-frequency emission mechanism allows the model to adaptively control anchor density, avoiding trace bloat. We construct 218K training instances by augmenting GQA, CLEVR-CoGenT, and VCR with ground-truth constraint annotations derived from scene graphs, and fine-tune open-source MLLMs (LLaVA-NeXT, Qwen2-VL) via LoRA. On five benchmarks, CART reduces the snowball rate from 0.65 to 0.14, improves GQA accuracy by +4.6 percentage points over trainingonly baselines, and achieves 89.1 F1 on POPE-all with at most 18% inference overhead.

摘要：自回歸多模態大型語言模型（MLLMs）遭遇錯誤滾雪球效應：在思考鏈（CoT）過程中，早期的單一錯誤推理會腐蝕所有後續推理。我們發現，在最先進的開源 MLLMs 中，一旦發生第一次錯誤，推理在 65% 的情況下會在所有剩餘步驟中連鎖失敗（我們稱之為滾雪球率的指標）。現有的緩解措施——採樣多條鏈、事後自我驗證或完整程序合成——要麼缺乏符號基礎，要麼過晚捕捉錯誤，或者犧牲自然語言推理的靈活性。我們提出了約束錨定推理痕跡（CART），這是一個神經符號框架，訓練 MLLMs 將自然語言推理步驟與符號約束聲明交錯：關於視覺內容的輕量級、機器可檢查的聲明（例如，count(red_objects) = 3）。一個雙重約束傳播模塊——結合學習的神經基礎頭和布爾約束傳播——不斷驗證這些錨定與提取的視覺特徵，並檢查它們的相互邏輯一致性。當檢測到矛盾時，回溯控制器會停止生成並恢復到最後的一致檢查點，防止錯誤傳播。一個變頻發射機制允許模型自適應地控制錨定密度，避免痕跡膨脹。我們通過增強 GQA、CLEVR-CoGenT 和 VCR，並從場景圖中派生真實約束註釋，構建了 218K 訓練實例，並通過 LoRA 微調開源 MLLMs（LLaVA-NeXT、Qwen2-VL）。在五個基準上，CART 將滾雪球率從 0.65 降低到 0.14，將 GQA 的準確率提高了 +4.6 個百分點，相較於僅訓練基準，並在 POPE-all 上達到 89.1 F1，推理開銷最多為 18%。

##### **Though Language Models Err While They Strive: Conformal Prediction for Self-Correcting Scientific Generation**
2607.16704v1 by Mingqiao Mo, Yunlong Tan, Hao Zhang

Large language models frequently violate fundamental scientific principles when generating technical content, undermining their reliability in scientific applications. We introduce Scientific Feasibility Control SFC, a graph-structured conformal prediction framework that provides statistical guarantees for scientific reasoning validity through progressive absolute-coherent-factuality validation. Our approach decomposes scientific reasoning into atomic absolute-coherent-factuality units requiring both individual correctness against physical laws and logical substantiation from preceding context, addressing the cascade effect where early scientific errors contaminate subsequent reasoning steps. Unlike independence-based methods that treat claims in isolation, SFC models logical dependencies as approximate deducibility graphs and operates through real-time validation with dynamic branching when scientific violations are detected, the system branches to alternative generation paths using verified context as foundation. We demonstrate SFC across established scientific reasoning benchmarks including PhyX multimodal physics, MATH, ScienceQA, and ARC Challenge, achieving 50.1 percent accuracy on PhyX physics reasoning, substantially outperforming recent reasoning models including DeepSeek-R1 49.8 percent and GPT-4 45.8 percent while providing 91.7 percent scientific validity with formal conformal coverage guarantees at alpha equals 0.10 confidence level and reducing scientific law violations by 73 percent across multiple model architectures.

摘要：大型語言模型在生成技術內容時經常違反基本科學原則，這削弱了它們在科學應用中的可靠性。<lb/>我們介紹了科學可行性控制（Scientific Feasibility Control，SFC），這是一種圖結構的符合預測框架，通過漸進的絕對一致性事實驗證提供科學推理有效性的統計保證。<lb/>我們的方法將科學推理分解為原子絕對一致性事實單元，這些單元要求對物理法則的個別正確性以及來自先前上下文的邏輯證明，解決了早期科學錯誤污染後續推理步驟的級聯效應。<lb/>與將主張孤立對待的獨立性方法不同，SFC 將邏輯依賴建模為近似可推導圖，並在檢測到科學違規時通過實時驗證和動態分支運作，系統會利用經過驗證的上下文作為基礎分支到替代生成路徑。<lb/>我們在已建立的科學推理基準上展示了 SFC，包括 PhyX 多模態物理、MATH、ScienceQA 和 ARC Challenge，在 PhyX 物理推理上達到 50.1% 的準確率，顯著超越最近的推理模型，包括 DeepSeek-R1 的 49.8% 和 GPT-4 的 45.8%，同時在 0.10 的信心水準下提供 91.7% 的科學有效性和正式的符合覆蓋保證，並在多個模型架構中將科學法則違規減少了 73%。

##### **Dependency-Guided Code Generation: Structured Matrix Decomposition and Consistency-Guided Refinement**
2607.16692v1 by Mingqiao Mo, Yangchen Zeng, Zikai Xiao, Xin Xiao, Wenhua Nie, Zhaolu Kang, Guangyuan Dong, Kai Shu, Hao Zhang, Xiaodong Fan

The increasing complexity of modern software systems has made automated code generation a fundamental task in software engineering. However, existing approaches often fail to adequately capture the intricate, multi-level dependencies among code entities, leading to generated code that is logically incomplete or difficult to integrate into real-world systems. To address this limitation, we propose a dependency-aware code generation framework that explicitly models interactions among code entities through a graph-based representation. We decompose dependencies into two complementary components: a quantized matrix that captures strong, explicit relations, and a sparse low-rank factorization that models weaker, implicit interactions. The decomposition is efficiently learned via an alternating optimization procedure. During code generation, the learned dependency structure is incorporated as a constraint, ensuring both semantic coherence and structural consistency of the generated code. Furthermore, we introduce a sparse triplet representation for strong dependencies, significantly improving storage efficiency and computational scalability. Extensive experiments demonstrate that our approach consistently produces code with superior semantic alignment and structural fidelity compared to existing methods.

摘要：現代軟體系統日益複雜，使得自動化程式碼生成成為軟體工程中的一項基本任務。<lb/>然而，現有的方法往往未能充分捕捉程式碼實體之間錯綜複雜的多層依賴關係，導致生成的程式碼在邏輯上不完整或難以整合進入現實世界的系統中。<lb/>為了解決這一限制，我們提出了一個依賴感知的程式碼生成框架，通過基於圖的表示明確建模程式碼實體之間的互動。<lb/>我們將依賴關係分解為兩個互補的組件：一個量化矩陣用於捕捉強烈、明確的關係，以及一個稀疏低秩分解用於建模較弱、隱含的互動。<lb/>這一分解通過交替優化程序高效學習而來。<lb/>在程式碼生成過程中，學習到的依賴結構被納入約束，確保生成的程式碼在語義一致性和結構一致性方面都達到要求。<lb/>此外，我們引入了一種稀疏三元組表示法來表示強依賴，顯著提高了存儲效率和計算可擴展性。<lb/>大量實驗表明，我們的方法在語義對齊和結構保真度上，始終優於現有方法。

##### **DataFlow-Harness: A Grounded Code-Agent Platform for Constructing Editable LLM Data Pipelines**
2607.16617v1 by Runming He, Zhen Hao Wong, Hao Liang, Zimo Meng, Chengyu Shen, Xiaochen Ma, Wentao Zhang

Large language models (LLMs) are increasingly used to automate data-processing workflows, yet coding agents typically produce scripts that are not automatically materialized as persistent, editable platform artifacts. We call this disconnect the \textit{NL2Pipeline gap}. To bridge it, we introduce \textsc{DataFlow-Harness}, a platform that guides an LLM agent to construct platform-native directed acyclic graphs (DAGs) through typed, incremental mutations rather than free-form scripts. The platform combines \textsc{DataFlow-Skills} for procedural guidance, a Model Context Protocol (MCP) layer that exposes the live operator registry and current pipeline state, and \textsc{DataFlow-WebUI}, which synchronizes conversational authoring with a visual DAG editor. On a 12-task data-engineering benchmark, \textsc{DataFlow-Harness} achieves a 93.3\% observed end-to-end pass rate. Relative to Vanilla Claude Code, it reduces measured monetary cost by 72.5\% and generation latency by 49.9\%; its observed pass rate is within 0.9 percentage points of the Context-Aware Claude Code baseline while its cost is 42.8\% lower. Per-task analysis indicates that Skills are most useful when construction depends on implicit procedural knowledge. These results show that live platform grounding can produce persistent, editable workflow artifacts with an observed reliability close to script-generation baselines and with lower measured construction cost and latency.

摘要：大型語言模型（LLMs）越來越多地用於自動化數據處理工作流程，但編碼代理通常生成的腳本並不會自動轉化為持久的、可編輯的平台工件。我們將這種脫節稱為\textit{NL2Pipeline gap}。為了彌補這一差距，我們介紹了\textsc{DataFlow-Harness}，這是一個引導LLM代理通過類型化的增量變更構建平台原生有向無環圖（DAGs）的平臺，而不是自由格式的腳本。該平台結合了\textsc{DataFlow-Skills}以提供程序指導、一個模型上下文協議（MCP）層，該層暴露了實時操作員註冊表和當前管道狀態，以及\textsc{DataFlow-WebUI}，它將對話式創作與可視化DAG編輯器同步。在一個包含12個任務的數據工程基準測試中，\textsc{DataFlow-Harness}達到了93.3\%的觀察端到端通過率。與Vanilla Claude Code相比，它將測量的貨幣成本降低了72.5\%，生成延遲降低了49.9\%；其觀察通過率與上下文感知Claude Code基準相差僅0.9個百分點，而其成本則低42.8\%。每任務分析表明，當構建依賴於隱性程序知識時，技能最為有用。這些結果顯示，實時平台基礎可以生成持久的、可編輯的工作流程工件，其觀察可靠性接近腳本生成基準，並且測量的構建成本和延遲更低。

##### **NOWJ@COLIEE 2026: Adaptive Pipelines for Legal Retrieval and Reasoning**
2607.16603v1 by Thuong-Hieu Ngo, Hoang-Trung Nguyen, Huu-Dong Nguyen, Xuan-Bach Le, Le-Dung Nguyen, Quang-Thanh Tran, Ha-Thanh Nguyen, Thi-Hai-Yen Vuong

This paper presents the methodologies and results of the NOWJ team's participation across all five tasks of the COLIEE 2026 competition. For Task 1 (Legal Case Retrieval), we propose a four-stage pipeline comprising candidate filtering, dense retrieval with complementary embedding models, cross-encoder reranking via fine-tuned generative rerankers and MLP-based pairwise classification, and adaptive per-query cutoff prediction. For Task 2 (Legal Case Entailment), we combine BM25 filtering, T5-based reranking, and LLM-based entailment verification with consensus ensemble. For Task 3 (Statute Law Retrieval and Entailment), we adopt a retrieval-augmented generation framework with dense retrieval, attention-based reranking, and few-shot-prompted LLM reasoning. For Task 4 (Legal Textual Entailment), we introduce a dynamic routing pipeline that classifies query difficulty and dispatches cases to either a balanced few-shot solver or a structured zero-shot chain-of-thought solver. For the Pilot Task (Legal Judgment Prediction), we combine hierarchical transformers with CRF layers, argument relation mining, and probabilistic argumentation graph reasoning.

摘要：這篇論文介紹了NOWJ團隊在COLIEE 2026競賽中參加所有五個任務的方法論和結果。<lb/>對於任務1（法律案例檢索），我們提出了一個四階段的流程，包括候選者篩選、使用互補嵌入模型的密集檢索、通過微調的生成重排序器和基於MLP的成對分類進行的交叉編碼重排序，以及自適應每查詢截止預測。<lb/>對於任務2（法律案例推理），我們結合了BM25篩選、基於T5的重排序和基於LLM的推理驗證，並使用共識集成。<lb/>對於任務3（法規檢索和推理），我們採用了增強檢索生成框架，結合密集檢索、基於注意力的重排序和少量樣本提示的LLM推理。<lb/>對於任務4（法律文本推理），我們引入了一個動態路由流程，該流程對查詢難度進行分類，並將案例分派給平衡的少量樣本求解器或結構化的零樣本思維鏈求解器。<lb/>對於試點任務（法律判決預測），我們結合了層次Transformer與CRF層、論點關係挖掘和概率論證圖推理。

##### **Exact Network Surgery: Functional Invariance and Gradient Plasticity in Reactive Computational Graphs**
2607.16568v1 by Abdallah Khemais

Function-preserving network growth techniques such as Net2Net and progressive stacking expand a model's capacity without destroying its learned function, but existing formulations either tolerate numerical perturbations or require a full rebuild of the training program. We formalize Exact Network Surgery: the in-place insertion of a residual block into a live computational graph such that (i) the network function is preserved -- bit-exactly under explicit floating-point hypotheses -- and (ii) inserted parameters remain trainable immediately after insertion. We prove an identity-morphism theorem for gated residual blocks, a structural-locality theorem showing that a reactive invalidation engine recomputes exactly the downstream cone of the insertion point, leaving every other node's value and optimizer state untouched, and an escape-from-initialization proposition showing that the Gradient Shadowing gate alpha, initialized at zero over a randomly initialized branch, receives a generically non-zero gradient at insertion time. We identify a degenerate configuration -- zero-initialized output projections combined with a zero gate -- that is an exact saddle point gradient descent cannot escape. Every claim is validated on the reference implementation in NeuroDSL, a reactive graph engine in Julia: grafting is bit-exact on every logit tested (0 mismatches out of 1600); the gate escapes zero at the first optimizer step and unlocks branch gradients at the second, exactly as predicted; the degenerate configuration exhibits gradients identically zero for the entire 600-step run; surgery cost tracks downstream cone size with r = 0.9992 while graft-plus-invalidation bookkeeping is constant (about 0.75 ms) across insertion depths; and training resumes bit-identically across a real process restart. A flagged preliminary appendix reports first single-seed observations on post-insertion gate dynamics.

摘要：功能保持的網絡增長技術，如 Net2Net 和漸進堆疊，擴展了模型的容量而不破壞其學習的功能，但現有的公式要麼容忍數值擾動，要麼需要完全重建訓練程序。我們正式化了精確網絡手術：在現有計算圖中就地插入一個殘差塊，使得 (i) 網絡功能得以保留——在明確的浮點假設下逐位精確——以及 (ii) 插入的參數在插入後立即可訓練。我們證明了針對門控殘差塊的同構定理，一個結構局部性定理顯示反應性失效引擎精確地重新計算插入點的下游錐，並且不影響其他每個節點的值和優化器狀態，以及一個逃脫初始化的命題，顯示在隨機初始化的分支上以零初始化的梯度陰影門 alpha，在插入時接收一個一般性的非零梯度。我們識別出一個退化配置——零初始化的輸出投影結合零門——這是一個精確的鞍點，梯度下降無法逃脫。每一項主張都在 NeuroDSL 的參考實現中得到了驗證，這是一個用 Julia 實現的反應性圖引擎：在每個測試的 logit 上，嫁接是逐位精確的（1600 次測試中無不匹配）；門在第一個優化步驟中逃脫零，在第二步中解鎖分支梯度，正如預測的那樣；退化配置在整個 600 步運行中顯示梯度恰好為零；手術成本隨著下游錐的大小變化，r = 0.9992，而嫁接加失效的記錄在插入深度上保持恆定（約 0.75 毫秒）；訓練在實際過程重啟中逐位相同地恢復。一個標記的初步附錄報告了插入後門動態的首次單種子觀察。

##### **One Modality to Forget Them All: Enhancing Cross-Modal Unlearning in Vision-Language Models**
2607.16442v1 by Sudharshan Balaji, Yili Ren, Guangjing Wang, Yimin Chen, Ning Wang

Machine unlearning is widely used to remove hazardous knowledge from large language models. Modern Vision-Language Models (VLMs), however, process both text and visual inputs, raising a fundamental security question: does unlearning in one modality transfer to the other? We present the first systematic, bidirectional study of cross-modal unlearning transfer across three VLM architectures: LLaVA-1.5 (MLP projection), InstructBLIP (Q-Former), and IDEFICS (gated cross-attention). We find that unlearning transfers across modalities, but the transfer is asymmetric and incomplete. In some cases, text unlearning strongly transfers to vision. However, this robustness is not preserved under typographic attacks that manipulate the visual presentation of text. Under such attacks, previously unlearned knowledge can be readily recovered, indicating shallow unlearning.
  To address the transfer gap and shallow robustness, we propose \textsc{CrossInf}, an influence-guided mitigation strategy. Motivated by the observation that different model components contribute unequally to cross-modal transfer, \textsc{CrossInf} focuses unlearning on transformer blocks that most influence cross-modal generalization. It reduces the transfer gap by more than half in architectures with strong fusion, while preserving model utility. It also improves robustness under typographic attacks, reducing the attack success rate to near zero. We further conduct human evaluation with three annotators ($κ{=}0.77$) to validate our findings. Finally, we analyze shallow unlearning using Centered Kernel Alignment (CKA), providing insights into the observed transfer behavior and robustness limitations.

摘要：機器遺忘廣泛用於從大型語言模型中去除危險知識。然現代視覺-語言模型（VLMs）同時處理文本和視覺輸入，提出了一個基本的安全問題：一種模態的遺忘是否會轉移到另一種模態？我們首次系統性地研究了三種VLM架構中的跨模態遺忘轉移：LLaVA-1.5（MLP投影）、InstructBLIP（Q-Former）和IDEFICS（門控交叉注意力）。我們發現遺忘在模態之間轉移，但轉移是不對稱且不完整的。在某些情況下，文本遺忘強烈轉移到視覺。然而，在操控文本視覺呈現的排版攻擊下，這種穩健性並未得到保留。在這類攻擊下，先前未學習的知識可以輕易恢復，顯示出淺層遺忘。<lb/>為了解決轉移差距和淺層穩健性，我們提出了\textsc{CrossInf}，這是一種影響引導的緩解策略。受到不同模型組件對跨模態轉移貢獻不均的觀察啟發，\textsc{CrossInf}專注於對跨模態泛化影響最大的Transformer區塊進行遺忘。它在具有強融合的架構中將轉移差距縮小了一半以上，同時保持模型效用。它還改善了在排版攻擊下的穩健性，將攻擊成功率降低到接近零。我們進一步進行了三位標註者的人工評估（$κ{=}0.77$）以驗證我們的發現。最後，我們使用中心核對齊（CKA）分析淺層遺忘，提供了對觀察到的轉移行為和穩健性限制的見解。

##### **Signal-based Model Access Risk Analysis for AI System Operations Security**
2607.16414v1 by Maria Mahbub, Steven Young, Amir Sadovnik, Edmon Begoli, Chris Rugenstein, Donald Coulter, Anthony Ayodele

Artificial intelligence (AI) systems are now ubiquitous across domains such as security, finance, healthcare, consumer technology, and large-scale cloud services, where they process massive volumes of data and make consequential decisions daily. This widespread adoption has created a broad attack surface through which adversaries can manipulate, evade, extract information from, or otherwise subvert deployed models. Depending on system design and exposure, attackers may have very different forms of access: some observe only final decisions, while others receive confidence scores, intermediate representations, or even full model parameters. While previous surveys typically organize evasion attacks into white-box, gray-box, and black-box categories based on the attacker's knowledge of model internals (architecture, parameters, gradients), this taxonomy often conflates different deployment scenarios that provide vastly different output signals, all labeled as ``black-box'' despite enabling fundamentally different attack strategies. Understanding how evasion attack strategies adapt to the specific information signals returned by deployed systems is critical for organizations making procurement and deployment decisions. To address this gap, we introduce the Signal-based Model Access Risk Taxonomy (SMART), a deployment-oriented framework that classifies attacker access according to the nature and richness of the information signals available from deployed AI systems. Using this taxonomy, we provide a structured overview of evasion attacks across progressively richer levels of information exposure, highlighting how deployment interfaces influence attack capabilities and informing more secure AI deployment and procurement decisions.

摘要：人工智慧（AI）系統現在在安全、金融、醫療保健、消費科技和大規模雲服務等領域無處不在，這些系統每天處理大量數據並做出重要決策。這種廣泛的採用創造了一個廣泛的攻擊面，敵手可以通過這個面來操控、逃避、提取信息或以其他方式顛覆已部署的模型。根據系統設計和暴露程度，攻擊者可能擁有非常不同的訪問形式：有些僅觀察最終決策，而其他人則接收置信度分數、中間表示，甚至完整的模型參數。雖然之前的調查通常根據攻擊者對模型內部（架構、參數、梯度）的知識將逃避攻擊組織為白盒、灰盒和黑盒類別，但這種分類往往混淆了提供截然不同輸出信號的不同部署場景，所有這些都被標記為「黑盒」，儘管它們啟用的攻擊策略根本上是不同的。了解逃避攻擊策略如何適應已部署系統返回的特定信息信號，對於做出採購和部署決策的組織至關重要。為了解決這一空白，我們引入了基於信號的模型訪問風險分類法（SMART），這是一個以部署為導向的框架，根據已部署AI系統提供的信息信號的性質和豐富程度對攻擊者的訪問進行分類。利用這一分類法，我們提供了對逃避攻擊的結構化概述，涵蓋了逐漸豐富的信息暴露水平，強調了部署接口如何影響攻擊能力，並為更安全的AI部署和採購決策提供信息。

##### **Automated Hardware Validation Test Plan Generation for Large Scale AI Datacenter Platforms Using a Generative AI Multi-Agents Architecture**
2607.16388v1 by Mohammed-Khalil Ghali, Saurabh Kulkarni, Prathamesh Kulkarni, Rohan Kulkarni, Sangwon Yoon, Daehan Won

Large-scale AI datacenter platforms comprise thousands of heterogeneous hardware components whose validation requires comprehensive fault injection test plans. Today these plans are authored manually: engineers review hardware self-healing validation documents and bills of materials, enumerate failure modes per field-replaceable unit, and produce flat lists of single-layer test cases. This process is labor-intensive, error-prone, and dependent on institutional knowledge; coverage gaps surface late, traceability to source specifications is implicit, and the effort is largely repeated per platform. This paper presents a generative AI multi-agent architecture that automates the generation of structured hardware validation test plans from two canonical inputs: self-healing validation documents, which enumerate known failure modes and their detection and remediation behaviors per field-replaceable unit, and component Bills of Material. An ingestion agent normalizes heterogeneous inputs into a canonical representation; a classification agent maps components to functional domains via contextual reasoning over part descriptions and sub-category hierarchies; and a generation agent synthesizes test cases by combining normalized failure modes with domain-classified data, filling gaps and producing edge cases. The output conforms to a standardized schema for direct import into internal validation software. Evaluated on two production platforms against manual baselines, the framework achieves coverage expansions of 74.2% and 51.4%, cutting authoring from days to hours. It yields fully traceable mappings from each test case to its source specification, and its multi-agent decomposition is portable across platform generations. Automated and expert evaluations confirm 100% extraction fidelity and high acceptance of new scenarios, validating the framework as a robust human-in-the-loop force multiplier.

摘要：大型 AI 數據中心平台由成千上萬的異質硬體組件組成，其驗證需要全面的故障注入測試計劃。今天，這些計劃是手動編寫的：工程師審查硬體自我修復驗證文件和物料清單，列舉每個可更換單元的故障模式，並產生單層測試案例的平面清單。這個過程勞動密集、容易出錯，並依賴於機構知識；覆蓋範圍的缺口在後期顯現，對源規範的可追溯性是隱含的，並且這項工作在每個平台上大多是重複的。本文提出了一種生成式 AI 多代理架構，該架構自動生成結構化的硬體驗證測試計劃，基於兩個典範輸入：自我修復驗證文件，這些文件列舉了已知的故障模式及其在每個可更換單元上的檢測和修復行為，以及組件物料清單。一個攝取代理將異質輸入標準化為典範表示；一個分類代理通過對部件描述和子類別層級的上下文推理將組件映射到功能領域；而一個生成代理通過將標準化的故障模式與領域分類數據結合來合成測試案例，填補空白並產生邊緣案例。輸出符合標準化架構，便於直接導入內部驗證軟體。在兩個生產平台上與手動基準進行評估，該框架實現了 74.2% 和 51.4% 的覆蓋擴展，將編寫時間從數天縮短至數小時。它提供了從每個測試案例到其源規範的完全可追溯映射，其多代理分解在平台世代之間可攜帶。自動化和專家評估確認了 100% 的提取忠實度和對新場景的高度接受，驗證了該框架作為強大的人機協作增強器的有效性。

##### **ToolSciVer: Multimodal Scientific Claim Verification with Visual Tool Augmented Reinforcement Learning**
2607.16131v1 by Binglin Zhou, Peng Shi, Ryo Kamoi, Nan Zhang, Rui Zhang

Multimodal Scientific Claim Verification (MSCV) requires models to verify scientific claims using visually grounded evidence from papers, including figures, tables, charts, and textual context. However, existing methods often fail because they struggle to locate decisive visual evidence, accurately read structured scientific visuals, and integrate multimodal observations into reliable reasoning. We introduce ToolSciVer, the first tool-augmented framework for MSCV to our knowledge. ToolSciVer equips a VLM with three type-aware visual tools, table row/column focus, chart-to-structure parsing, and high-resolution region zoom, which convert dense scientific visuals into explicit, claim-facing evidence, and trains the policy with Group Relative Policy Optimization (GRPO) under a composite reward of answer correctness, format validity, length control, tool-use efficiency, and tool-validity penalties. Experiments on SciVer and MuSciClaims datasets on five VLMs from three model families (Qwen, InternVL, Gemma) demonstrate that our method achieves superior performance compared to four competitive baselines including prompting-based and RL-based tool-use methods, highlighting the effectiveness of learned, type-aware tool use for scientific claim verification.

摘要：多模態科學主張驗證（MSCV）需要模型利用來自論文的視覺基礎證據來驗證科學主張，包括圖形、表格、圖表和文本背景。<lb/>然而，現有方法往往失敗，因為它們難以定位關鍵的視覺證據，準確閱讀結構化的科學視覺，並將多模態觀察整合為可靠的推理。<lb/>我們介紹了 ToolSciVer，據我們所知，這是首個增強工具的 MSCV 框架。<lb/>ToolSciVer 為 VLM 配備了三種類型感知的視覺工具，包括表格行/列聚焦、圖表到結構解析和高解析度區域縮放，這些工具將密集的科學視覺轉換為明確的、面向主張的證據，並使用群體相對策略優化（GRPO）訓練政策，根據答案正確性、格式有效性、長度控制、工具使用效率和工具有效性懲罰的綜合獎勵進行訓練。<lb/>在來自三個模型系列（Qwen、InternVL、Gemma）的五個 VLM 上進行的 SciVer 和 MuSciClaims 數據集實驗表明，我們的方法相比於四個競爭基準，包括基於提示和基於強化學習的工具使用方法，達到了更優越的性能，突顯了學習的、類型感知的工具使用在科學主張驗證中的有效性。

##### **HCIG: A Hierarchical Cross-Modal Incongruity Graph Network for Multimodal Sarcasm and Cyberbullying Detection**
2607.16076v1 by Bhavana Verma, Priyanka Meel, Dinesh Kumar Vishwakarma

Multimodal sarcasm and cyberbullying detection remain challenging because the intended meaning often emerges from incongruity between textual and visual information rather than from either modality alone. Existing multimodal approaches primarily rely on feature fusion or cross-modal attention, which may not effectively capture hierarchical semantic inconsistencies across different levels of representation. To address this limitation, this paper proposes HCIG (Hierarchical Cross-modal Incongruity Graph Network), a novel framework that models cross-modal incongruity at token, phrase, and global levels using graph attention networks and adaptively integrates these representations through a learned hierarchical attention mechanism. As a complementary architecture, we also introduce GCCN (Graph-based Cross-modal Contradiction Network), which performs graph-based reasoning using contradiction-aware pooling for efficient multimodal interaction learning. The proposed models are evaluated on the MMSD sarcasm benchmark and the MultiBully cyberbullying dataset, together with comprehensive ablation studies and cross-task transfer experiments. Experimental results demonstrate that HCIG achieves the best performance on MMSD with 85.74% accuracy and 85.29% macro-F1, while GCCN attains the highest macro-F1 (68.66%) on MultiBully and HCIG achieves the highest accuracy (69.62%) and bullying-class F1 (74.90%). The findings demonstrate that hierarchical multi-granularity incongruity modeling provides more effective multimodal reasoning than conventional fusion strategies, offering a robust framework for sarcasm and cyberbullying detection in social media.

摘要：多模態諷刺與網路霸凌的檢測仍然具有挑戰性，因為預期的意義通常源自文本和視覺信息之間的不一致，而非僅僅來自任何一種模態。現有的多模態方法主要依賴特徵融合或跨模態注意力，但這可能無法有效捕捉不同層次表示之間的階層語義不一致。為了解決這一限制，本文提出了HCIG（階層跨模態不一致圖網絡），這是一個新穎的框架，使用圖注意力網絡在標記、短語和全局層面建模跨模態不一致，並通過學習的階層注意力機制自適應整合這些表示。作為一種補充架構，我們還介紹了GCCN（基於圖的跨模態矛盾網絡），它使用矛盾感知池進行基於圖的推理，以高效學習多模態交互。所提出的模型在MMSD諷刺基準和MultiBully網路霸凌數據集上進行評估，並進行了全面的消融研究和跨任務轉移實驗。實驗結果表明，HCIG在MMSD上達到了85.74%的準確率和85.29%的宏觀F1，GCCN在MultiBully上獲得了最高的宏觀F1（68.66%），而HCIG則達到了最高的準確率（69.62%）和霸凌類別F1（74.90%）。研究結果表明，階層多粒度不一致建模比傳統的融合策略提供了更有效的多模態推理，為社交媒體中的諷刺和網路霸凌檢測提供了一個穩健的框架。

##### **Frontier AI performance across the business disciplines: a case-grounded benchmark of knowledge work and analytical reasoning**
2607.16057v1 by Ajay Patel, Kartik Hosanagar, Ramayya Krishnan, Chris Callison-Burch, Karim Lakhani, Mitch Weiss

Large language models (LLMs) are improving rapidly as reflected in benchmark scores, yet these AI benchmarks largely test capabilities such as factual recall, narrow question answering, mathematical problem-solving, and coding and agentic tool-use. What remains poorly measured is AI progress on the analytical knowledge work white-collar professionals perform daily, including synthesizing complex information, exercising judgment under uncertainty and incomplete information, applying strategic and adversarial thinking in multi-stakeholder settings, weighing trade-offs, and producing defensible, structured analyses. This gap is even more pronounced for subjective components of such work, where success can be challenging to define. The "case method" form of education practiced by top business schools provides a natural foundation for addressing this measurement gap, and we construct BusinessCaseBench, a benchmark spanning hundreds of questions drawn from business cases across eighteen disciplines, each paired with a grading rubric derived from the expert-written instructor case solution. On BusinessCaseBench, frontier AI models already score highly against instructor rubrics, and capability within one model family improves substantially over two years. These results provide strong evidence that AI performance on this class of work is already high and rapidly improving, with implications for business schools, where case pedagogy trains undergraduates and MBAs in this kind of analytical reasoning, and for entry-level professional roles, where such skills have historically anchored early-career work.

摘要：大型語言模型（LLMs）的進步速度迅速，這在基準分數中得到了體現，然而這些人工智慧基準主要測試的能力包括事實回憶、狹義問題回答、數學問題解決以及編碼和工具使用等。人工智慧在白領專業人士日常執行的分析性知識工作上的進展仍然測量不佳，這些工作包括綜合複雜信息、在不確定和不完整信息下行使判斷、在多方利益相關者的情境中應用戰略和對抗性思維、權衡取捨，以及產出可辯護的、有結構的分析。這種工作的主觀成分的測量差距更為明顯，因為成功的定義可能很具挑戰性。頂尖商學院所實踐的“案例教學法”為解決這一測量差距提供了自然的基礎，我們構建了BusinessCaseBench，一個涵蓋來自十八個學科的商業案例的數百個問題的基準，每個問題都配有來自專家編寫的講師案例解決方案的評分標準。在BusinessCaseBench上，前沿的人工智慧模型已經在講師評分標準下獲得了高分，且同一模型家族的能力在兩年內有了顯著提升。這些結果提供了有力的證據，表明人工智慧在這類工作的表現已經很高並且快速改善，這對商學院有著重要的影響，因為案例教學法培養本科生和MBA學生這種分析性推理的能力，並且對入門級專業角色也有影響，因為這些技能歷來是早期職業工作的基石。

##### **Rethinking Quantum Continual Learning with Quantum Fisher Information**
2607.16030v1 by Yu-Chao Hsu, Yu-Cheng Lin, Tai-Yue Li, Nan-Yow Chen, En-Jui Kuo

Quantum continual learning aims to train quantum models on sequential tasks without losing previously learned knowledge. However, variational quantum classifiers (VQCs) are prone to catastrophic forgetting under nonstationary task distributions. We propose quantum elastic weight consolidation (QEWC), a quantum Fisher information (QFI)-informed regularization method for mitigating forgetting. Unlike conventional elastic weight consolidation based on classical Fisher information (CFI), which measures parameter importance through measurement-dependent output statistics, QEWC uses QFI to quantify the intrinsic sensitivity of the parameterized quantum state. This gives an information-geometric view in which important parameters are identified by the local response of the quantum state manifold. We evaluate QEWC on VQCs trained on sequential binary classification tasks, including classical image-classification and quantum phase-classification tasks. Simulations show that sequential training without regularization causes severe forgetting, while both CFI-based EWC and QFI-based QEWC improve retention of previous tasks. Mechanistic analyses further show that the two methods impose different regularization geometries: CFI acts selectively on measurement-sensitive directions, whereas QFI imposes a denser state-geometric constraint over parameter space. Under depolarizing noise, CFI values are strongly suppressed by degraded measurement statistics, while QFI preserves a more stable sensitivity structure of the noisy parameterized quantum state. These results establish QEWC as a physically motivated approach for studying and mitigating forgetting in quantum continual learning through quantum-state geometry.

摘要：量子持續學習旨在對連續任務訓練量子模型，而不會失去先前學到的知識。<lb/>然而，變分量子分類器（VQCs）在非平穩任務分佈下容易遭受災難性遺忘。<lb/>我們提出了量子彈性權重整合（QEWC），這是一種基於量子費舍爾信息（QFI）的正則化方法，用於減輕遺忘。<lb/>與基於經典費舍爾信息（CFI）的傳統彈性權重整合不同，後者通過測量依賴的輸出統計來衡量參數的重要性，QEWC則使用QFI來量化參數化量子態的內在敏感性。<lb/>這提供了一種信息幾何的視角，其中重要參數通過量子態流形的局部響應來識別。<lb/>我們在訓練於連續二元分類任務的VQCs上評估QEWC，包括經典圖像分類和量子相位分類任務。<lb/>模擬顯示，未經正則化的連續訓練會導致嚴重的遺忘，而基於CFI的EWC和基於QFI的QEWC都改善了對先前任務的保留。<lb/>機制分析進一步顯示這兩種方法施加了不同的正則化幾何：CFI在測量敏感方向上選擇性地作用，而QFI則在參數空間上施加了更密集的狀態幾何約束。<lb/>在去極化噪聲下，CFI值受到降級測量統計的強烈抑制，而QFI則保持了噪聲參數化量子態的更穩定的敏感性結構。<lb/>這些結果確立了QEWC作為一種物理上有動機的方法，用於通過量子態幾何研究和減輕量子持續學習中的遺忘。

##### **Candidate Attended Dialogue State Tracking Using BERT**
2607.16021v1 by Junyuan Zheng, Onkar Salvi, John Chan

Dialogue state tracking (DST) is one of the core components in task-oriented dialogue systems. At each turn in a conversation, DST estimates the user belief or dialogue state, which is used as input for downstream modules to predict system actions and generate responses. The increasingly popular dialogue system applications like Google Assistant, Siri and Alexa need to support a large number of services and APIs, resulting in growing attention to the scalability of such systems. Especially for some domains with little or no training data, the capability of transferring existing knowledge of other domains is highly desired. In this paper, we present a novel scalable framework for multi-domain dialogue state tracking. The proposed system leverages the pretrained BERT model to achieve zero-shot generalization, making it easy to quickly adapt to new domains without additional training. The performance of our model is evaluated on recently released schema-based dialogue (SGD) dataset, showing significant improvement compared to previous baseline.

摘要：對話狀態追蹤（DST）是任務導向對話系統中的核心組件之一。<lb/>在每次對話中，DST 估計用戶的信念或對話狀態，這些狀態作為下游模組的輸入，用於預測系統行為和生成回應。<lb/>越來越受歡迎的對話系統應用程序，如 Google Assistant、Siri 和 Alexa，需要支持大量服務和 API，這使得這些系統的可擴展性受到越來越多的關注。<lb/>特別是對於一些訓練數據稀少或不存在的領域，轉移其他領域現有知識的能力是非常渴望的。<lb/>在本文中，我們提出了一個新穎的可擴展框架，用於多領域對話狀態追蹤。<lb/>所提出的系統利用預訓練的 BERT 模型實現零樣本泛化，使其能夠快速適應新領域而無需額外訓練。<lb/>我們的模型在最近發布的基於模式的對話（SGD）數據集上進行了性能評估，顯示出與之前基準相比的顯著改進。

##### **When Not to Automate: A Formal Protocol for Human Preservation in AI-Optimized Organizations**
2607.15944v1 by Jose Manuel de la Chica Rodriguez, Jairo Rodriguez Arias, Spyridon Chouliaras

Standard automation ROI misses four categories of systemic risk -- tacit knowledge erosion, resilience reduction, regulatory exposure, and socio-institutional capital degradation -- that affect long-term organizational performance. PHP-AIO (Protocol for Human Preservation in AI-Optimized Organizations) is a five-gate sequential decision protocol with a final composite check that quantifies these unpriced systemic risks at the role level and produces auditable automation decisions. A closed-form automation-debt measure ($ρ(P)$) formalises how role-level decisions accumulate across multi-step processes; its warning is neutralised only by a regulator-mandated human-in-the-loop anchor. Applied to stylised profiles of representative internal roles, PHP-AIO produces distinct outcomes -- automate, augment, hybrid, and preserve -- for candidates that standard cost-benefit analysis would uniformly automate. Threshold sensitivity analysis confirms the gate decisions are robust to upward perturbations of at least 14% in three of four representative cases.
  Keywords: AI governance, automation decision, human oversight, tacit knowledge, organizational resilience, financial services

摘要：標準自動化的投資報酬率忽略了四個系統性風險類別——隱性知識流失、韌性降低、法規風險以及社會制度資本退化——這些風險影響長期的組織表現。PHP-AIO（在AI優化組織中進行人類保存的協議）是一個五道閘門的序列決策協議，最終進行綜合檢查，以量化這些未定價的系統性風險，並在角色層面上產生可審計的自動化決策。一個封閉形式的自動化債務衡量指標（$ρ(P)$）正式化了角色層面決策如何在多步驟過程中累積；其警告僅能通過監管機構要求的人類介入錨點來中和。應用於典型內部角色的風格化輪廓，PHP-AIO 為標準成本效益分析會統一自動化的候選者產生不同的結果——自動化、增強、混合和保留。閾值敏感性分析確認閘門決策對於四個代表性案例中至少14%的向上擾動是穩健的。  
關鍵詞：AI治理、自動化決策、人類監督、隱性知識、組織韌性、金融服務

##### **On the Failure of Boundary-Seeking Distillation in Bottlenecked Generative Architectures**
2607.15919v1 by Mohamed Amine Kina

Data-free knowledge distillation transfers the knowledge encoded in a teacher model to a student model without access to the original training data. Prior work such as Contrastive Abductive Knowledge Extraction (CAKE) achieves this for classifiers by synthesizing samples near the teacher's decision boundary. In this work, we investigate whether this boundary-seeking principle extends to autoencoder distillation through experiments on the MNIST dataset . To enable a direct comparison, we reformulate continuous reconstruction as a dense, per-feature classification task, allowing the decoder to output categorical logits. We show that boundary-seeking objectives are fundamentally ill-posed in bottlenecked generative architectures. CAKE operates on a single, instance-level objective, but a decoder acts as an array of tightly coupled, feature-level classifiers constrained by a shared low-dimensional bottleneck. Independently sampling contrastive targets for these coupled outputs violates the geometry of the learned latent manifold and produces severe gradient conflicts instead of informative boundary samples. Manifold-aware synthesis bypasses these conflicts entirely and establishes an effective baseline for data-free generative distillation.

摘要：資料無關的知識蒸餾將教師模型中編碼的知識轉移至學生模型，而不需訪問原始訓練數據。先前的工作，例如對比推理知識提取（CAKE），通過合成接近教師決策邊界的樣本來實現這一點。本文中，我們通過對MNIST數據集的實驗調查這一邊界尋求原則是否擴展到自編碼器蒸餾。為了進行直接比較，我們將連續重建重新表述為一個密集的、每特徵的分類任務，允許解碼器輸出類別邏輯。 我們顯示邊界尋求目標在瓶頸生成架構中根本上是病態的。CAKE在單一的實例級目標上運作，但解碼器作為一組緊密耦合的特徵級分類器，受限於共享的低維瓶頸。獨立地為這些耦合輸出採樣對比目標違反了學習的潛在流形的幾何結構，並產生嚴重的梯度衝突，而不是信息豐富的邊界樣本。流形感知合成完全繞過這些衝突，並為無數據生成蒸餾建立了一個有效的基準。

##### **An MLIR-Based Compilation Method for Large Language Models**
2607.15865v1 by Pengchao Hu, Zhibin Xin, Yifan Chen, Yangyang Zhou, Liang Wang

Large Language Models (LLMs) have become the dominant workload on modern AI accelerators, yet deploying them on specialized hardware still faces two core challenges: how to import a trained model into a compiler-friendly intermediate representation, and how to efficiently schedule the autoregressive inference loop under limited on-chip memory. This paper presents an MLIR (Multi-Level Intermediate Representation) based compilation method for large language models, illustrated using two dialects of operators, TopOp and TpuOp. TopOp serves as a high-level graph dialect that is independent of both the source framework and the target chip, and is responsible for expressing model semantics; TpuOp serves as the target hardware dialect, carrying chip-related decisions such as quantization, layer groups, and memory layout. A model is first represented as TopOp, then lowered layer by layer to TpuOp, and finally a deployable binary is generated. In addition, each Transformer layer is split into three stages for static compilation: prefill, prefill_kv (prefill with historical key-value cache), and decode, so as to accommodate the different computational characteristics of prompt-parallel processing and per-token generation. The method has been implemented in the TPU-MLIR compiler{https://github.com/sophgo/tpu-mlir} and the LLM-TPU deployment project\footnote{https://github.com/sophgo/LLM-TPU}, supporting a variety of generative models including the Qwen, Llama, InternVL, and MiniCPM-V series, as well as multiple quantization and deployment forms such as GPTQ, AWQ, and AutoRound.

摘要：大型語言模型（LLMs）已成為現代人工智慧加速器上的主導工作負載，但在專用硬體上部署它們仍面臨兩個核心挑戰：如何將訓練好的模型導入編譯器友好的中介表示，以及如何在有限的片上記憶體下有效地安排自回歸推理循環。本文提出了一種基於 MLIR（多層中介表示）的大型語言模型編譯方法，並使用兩種運算子方言進行說明，分別是 TopOp 和 TpuOp。TopOp 作為一種高層次圖形方言，獨立於源框架和目標晶片，負責表達模型語義；TpuOp 作為目標硬體方言，承載與晶片相關的決策，如量化、層組和記憶體佈局。模型首先表示為 TopOp，然後逐層降級為 TpuOp，最後生成可部署的二進位檔。此外，每個 Transformer 層被拆分為三個階段以進行靜態編譯：prefill、prefill_kv（帶有歷史鍵值快取的預填充）和解碼，以適應提示並行處理和逐標記生成的不同計算特徵。該方法已在 TPU-MLIR 編譯器{https://github.com/sophgo/tpu-mlir}和 LLM-TPU 部署專案\footnote{https://github.com/sophgo/LLM-TPU}中實現，支持多種生成模型，包括 Qwen、Llama、InternVL 和 MiniCPM-V 系列，以及多種量化和部署形式，如 GPTQ、AWQ 和 AutoRound。

##### **Test-Time Noise Guided Adaptation for Realistic Autoregressive Video Generation**
2607.15849v1 by Dimitrios Karageorgiou, Symeon Papadopoulos, Ioannis Kompatsiaris, Efstratios Gavves

Autoregressive video diffusion models have enabled the generation of arbitrarily long videos by removing conditioning on future frames, thus greatly improving computational efficiency. Yet, they suffer from error accumulation over time, as the denoised sequence gradually drifts away from the conditioning distribution seen during training. Recent advances attempt to reduce this error by anchoring each generated frame to the learned manifold of real ones. However, even when all generated individual frames lie close to the real manifold, there are trajectories which the model lacks sufficient knowledge to continue without exiting it, thus reaching a terminal point. To prevent the model from being trapped in terminal points, we start from the hypothesis that for well-modeled future trajectories the distribution of the predicted noise should match the one of the forward noising process. To enforce such a prior at test time, we introduce Terminal points Avoidance through Noise Guided Optimization (TANGO), which uses the diffusion model as a critic of its own outputs, by predicting one step forward and requiring an isotropic Gaussian noise prediction. We use the deviation from this expected noise distribution to search for an alternative trajectory that does not lead to a terminal point. Our approach achieves a $3.1\%$ absolute improvement on VBench over state-of-the-art, while reducing Fréchet Video Distance by $28.3\%$ on average across $15$s videos. Our code is available on https://mever-team.github.io/tango.

摘要：自回归视频扩散模型通过去除对未来帧的条件限制，使得生成任意长度的视频成为可能，从而大大提高了计算效率。 然而，随着去噪序列逐渐偏离训练期间所见的条件分布，它们在时间上会遭遇误差积累。 最近的进展试图通过将每个生成的帧锚定到真实帧的学习流形上来减少这种误差。 然而，即使所有生成的单个帧都接近真实流形，模型仍然缺乏足够的知识来继续某些轨迹，而不退出该流形，从而达到终点。 为了防止模型陷入终点，我们从假设开始，即对于良好建模的未来轨迹，预测噪声的分布应与前向加噪过程的分布相匹配。 为了在测试时强制执行这种先验，我们引入了通过噪声引导优化的终点避免（TANGO），该方法利用扩散模型作为其自身输出的批评者，通过预测一步并要求等方差高斯噪声预测。 我们使用与这一预期噪声分布的偏差来寻找不导致终点的替代轨迹。 我们的方法在VBench上实现了相对于最先进技术的$3.1\%$绝对提升，同时在$15$s视频中平均减少了$28.3\%$的Fréchet视频距离。 我们的代码可在https://mever-team.github.io/tango上获取。

##### **CAMMAR: Culture-Aware Matryoshka for Metaphorical Arabic Representations**
2607.15847v1 by Suzan Awinat, Alfonso Ortega del Puente

Metaphor in Arabic is a culturally grounded mechanism for constructing meaning, encoding cultural knowledge that shapes interpretation. Yet current Arabic language models typically collapse lexical, cultural, and metaphorical information into a single representational space, a phenomenon we term "semantic smearing". We introduce CAMMAR (Culture-Aware Matryoshka for Metaphorical Arabic Representations), a representation learning framework that organizes meaning into nested lexical, cultural, and metaphorical embedding subspaces through a staged semantic curriculum. The design implements compositional principles of Al-Jurjani's theory of nazum, modeling figurative meaning as compositionally grounded in prior semantic relations, and yields a training-free geometric measure of metaphoricity based on the distance between lexical and metaphorical representations.
  Evaluated on a new span-annotated Arabic metaphor set as word-matched figurative/literal pairs, the geometric readout detects metaphor well above chance when the inter-layer geometry is shaped by paired supervision (AUC up to 0.84; figurative outscores its literal counterpart for the same word in 82.6\% of pairs), but sits at chance under an unsupervised domain contrast alone, a clean separation between a legible-under-supervision regime and a non-emergent one. A controlled ablation shows that grounding the lexical layer in morphological roots gives a small but consistent gain, an effect absent from direct probing that reflects the layer's quality as a measurement anchor. We will release the datasets, cultural concept inventory, and code upon acceptance.

摘要：阿拉伯語中的隱喻是一種文化根基的機制，用於構建意義，編碼塑造解釋的文化知識。然目前的阿拉伯語言模型通常將詞彙、文化和隱喻信息合併為一個單一的表徵空間，這一現象我們稱之為「語義模糊」。我們介紹了 CAMMAR（文化意識的馬特ryoshka 隱喻阿拉伯表徵），這是一個表徵學習框架，通過分階段的語義課程將意義組織成嵌套的詞彙、文化和隱喻嵌入子空間。該設計實施了 Al-Jurjani 的 nazum 理論的組合原則，將比喻意義建模為基於先前語義關係的組合性根基，並產生了一種基於詞彙和隱喻表徵之間距離的無需訓練的幾何度量。<lb/>在一個新的跨度標註的阿拉伯隱喻集上進行評估，作為詞匹配的比喻/字面對，當層間幾何形狀受到配對監督時，幾何讀出能夠很好地檢測隱喻，遠高於隨機機率（AUC 高達 0.84；在 82.6\% 的對中，比喻的得分超過了同一詞的字面對），但在僅有無監督領域對比的情況下則處於隨機水平，這清晰地區分了可被監督的可讀性範疇與非突現的範疇。<lb/>一項控制性消融顯示，在形態根基中為詞彙層提供基礎會帶來小但穩定的增益，這一效果在直接探測中缺失，反映了該層作為測量錨的質量。我們將在接受後釋出數據集、文化概念庫和代碼。

##### **Knowledge-Centric Agents for Workflow Generation**
2607.15845v1 by Zhendong Li, Lei Sun, Ruibo Ming, He Zhang, Danda Pani Paudel, Luc Van Gool, Jinjin Gu

Workflow generation in visual creation systems such as ComfyUI demands not only syntactic accuracy but also expert-level reasoning over modular compositions. Existing large language model (LLM) approaches often treat this as a direct text-to-JSON generation task, struggling with structural brittleness and lacking the experiential knowledge required for effective design. We argue that successful workflow generation requires modeling knowledge itself, including its structure, hierarchy, and reasoning dynamics. To this end, we propose a knowledge-centric framework that learns to invert, inject, and infer with knowledge across multiple abstraction levels. We first perform knowledge inversion to distill hierarchical representations, ranging from full pseudo-codes and skeletons to high-level strategies, from large collections of real-world workflows. We then conduct knowledge injection through supervised fine-tuning, teaching the model to reason from task descriptions to strategies and from strategies to executable structures. During inference, the model performs reversible reasoning to synthesize executable workflows, augmented by self-refinement for structural coherence. Extensive experiments demonstrate that our method produces workflows with richer node diversity, more coherent structures, and higher execution success rates than existing systems, establishing a new foundation for knowledge-driven, agentic workflow generation.

摘要：視覺創作系統中的工作流程生成，如 ComfyUI，不僅需要語法準確性，還需要對模組化組合進行專家級推理。現有的大型語言模型（LLM）方法通常將此視為直接的文本到 JSON 生成任務，面臨結構脆弱的問題，並缺乏有效設計所需的經驗知識。我們認為，成功的工作流程生成需要對知識本身進行建模，包括其結構、層次和推理動態。為此，我們提出了一個以知識為中心的框架，學習在多個抽象層次上反轉、注入和推斷知識。我們首先進行知識反轉，以從大量現實世界工作流程中提煉出層次表示，這些表示範圍從完整的偽代碼和框架到高層次策略。然後，我們通過監督微調進行知識注入，教導模型從任務描述推理到策略，並從策略推理到可執行結構。在推理過程中，模型執行可逆推理以合成可執行的工作流程，並通過自我精煉來增強結構的一致性。大量實驗表明，我們的方法生成的工作流程具有更豐富的節點多樣性、更一致的結構和更高的執行成功率，為知識驅動的自主工作流程生成建立了新的基礎。

##### **RTL-Sequencer: Towards Scalable RTL Timing Prediction with the Sequence-based Paradigm**
2607.15830v1 by Ziyan Guo, Wenji Fang, Wenkai Li, Yuchao Wu, Shang Liu, Zhiyao Xie

Accurate timing prediction at the register-transfer level (RTL) is a longstanding challenge in design automation. Existing graph-based methods struggle with limited receptive fields, high complexity, and a lack of signal directionality. We present RTL-Sequencer, a novel sequence-based paradigm that enables scalable RTL timing prediction via linearizing logic cones by breadth-first traversal and applying modern linear sequence models. Furthermore, sequence models are customized by four synergistic techniques, including sequence shuffling, bidirectional modeling, differentiable modeling, and a hybrid graph-sequence architecture. Extensive experiments demonstrate significant improvements of RTL-Sequencer over state-of-the-art baselines, advancing early-stage timing optimization.

摘要：精確的寄存器傳輸層（RTL）時間預測一直是設計自動化中的一大挑戰。現有的基於圖形的方法在接收域有限、高複雜性以及缺乏信號方向性方面面臨困難。我們提出了 RTL-Sequencer，一種新穎的基於序列的範式，通過廣度優先遍歷線性化邏輯錐，實現可擴展的 RTL 時間預測。此外，序列模型通過四種協同技術進行定制，包括序列洗牌、雙向建模、可微建模和混合圖形-序列架構。大量實驗顯示 RTL-Sequencer 相較於最先進的基準有顯著改進，推進了早期階段的時間優化。

##### **Knowledge-Assisted Multi-Graph Dependency Learning for Multivariate Time Series Anomaly Detection in Multi-Stage Industrial Processes**
2607.15799v1 by Jaeyeong Lee, Taeseong Yoon, Wonmo Koo, Heeyoung Kim

Industrial processes often generate complex, interdependent time-series data from multiple sensors across multiple stages, forming complex dependencies among variables and process stages. Effective monitoring and timely anomaly detection of these time series through multivariate time series anomaly detection (MTAD) is crucial for preventing failures and ensuring the reliability of automated systems. Graph neural networks (GNNs) have advanced MTAD by leveraging data-driven graphs to model complex dependencies among variables, effectively capturing relational structures within multivariate time series to enhance anomaly detection performance. However, existing GNN-based approaches often overlook critical process knowledge, and even when this knowledge is considered, seamlessly incorporating it into existing models remains inherently challenging, leading to suboptimal performance. To address this limitation, we propose a knowledge-assisted multi-graph framework for modeling sensor dependencies in multi-stage industrial processes for MTAD, which explicitly incorporates process knowledge into graph learning to enhance dependency modeling and improve anomaly detection performance. Our method constructs three complementary graphs: one purely data-driven and two refined by integrating structural constraints derived from process knowledge. To effectively leverage these graphs for anomaly detection, we employ a multi-graph attention network, enabling a more accurate and robust representation of complex dependencies. Comprehensive experiments on two real-world, multi-stage industrial datasets demonstrate that incorporating process knowledge substantially enhances anomaly detection performance.

摘要：工業過程通常會從多個階段的多個傳感器生成複雜的相互依賴時間序列數據，形成變量和過程階段之間的複雜依賴關係。通過多變量時間序列異常檢測（MTAD）有效監控和及時檢測這些時間序列的異常對於防止故障和確保自動化系統的可靠性至關重要。圖神經網絡（GNN）通過利用數據驅動的圖來建模變量之間的複雜依賴關係，推進了MTAD，有效捕捉多變量時間序列中的關係結構，以提高異常檢測性能。然而，現有的基於GNN的方法往往忽略關鍵的過程知識，即使考慮到這些知識，將其無縫整合到現有模型中仍然具有固有的挑戰性，導致性能不佳。為了解決這一限制，我們提出了一種知識輔助的多圖框架，用於建模多階段工業過程中的傳感器依賴關係，以進行MTAD，該框架明確將過程知識納入圖學習中，以增強依賴建模並改善異常檢測性能。我們的方法構建了三個互補的圖：一個純粹基於數據，兩個則通過整合源自過程知識的結構約束進行精煉。為了有效利用這些圖進行異常檢測，我們採用了多圖注意力網絡，使得對複雜依賴關係的表示更加準確和穩健。在兩個真實世界的多階段工業數據集上進行的全面實驗表明，納入過程知識顯著提升了異常檢測性能。

##### **NeurOWL: An LLM-Based Neural-symbolic Framework for Incomplete OWL Ontology Reasoning**
2607.15776v1 by Hui Yang, Jiaoyan Chen, Yiping Song, Renate Schmidt, Wen Zhang

OWL ontologies provide a formal knowledge representation framework that enables semantic reasoning, and have been widely adopted across domains such as healthcare and bioinformatics. In practice, however, real-world ontologies are often incomplete, which pose challenges for reasoning. In this work, we focus on a fundamental subsumption reasoning problem: given an incomplete ontology and a candidate (non-entailed) subsumption, determine whether the subsumption is semantically plausible and, if so, providing a logically sound explanation containing potential missing axioms. This task unifies subsumption verification with ontology abduction, and generalizes the latter by removing the need for a predefined candidate set of missing axioms. To address this subsumption reasoning problem, we propose NeurOWL, an end-to-end neuro-symbolic framework that jointly performs verification and abduction, leveraging both formally defined semantics and textual semantics through Large Language Models and ontology embeddings. We evaluate NeurOWL on real-world ontologies across multiple domains, demonstrating strong and robust performance across different domains.

摘要：OWL 本體提供了一個正式的知識表示框架，能夠實現語義推理，並已被廣泛應用於醫療保健和生物資訊等領域。<lb/>然而，在實踐中，現實世界的本體往往是不完整的，這對推理造成了挑戰。<lb/>在這項工作中，我們專注於一個基本的包含推理問題：給定一個不完整的本體和一個候選（非推導）包含，確定該包含是否在語義上是合理的，如果是，則提供一個邏輯上合理的解釋，包含潛在缺失的公理。<lb/>這項任務將包含驗證與本體推斷統一起來，並通過消除對預定義缺失公理候選集的需求來概括後者。<lb/>為了解決這個包含推理問題，我們提出了 NeurOWL，一個端到端的神經符號框架，該框架共同執行驗證和推斷，利用正式定義的語義和通過大型語言模型及本體嵌入的文本語義。<lb/>我們在多個領域的現實世界本體上評估了 NeurOWL，展示了其在不同領域的強大和穩健的性能。

##### **S1-Omni: A Unified Multimodal Reasoning Model for Scientific Understanding, Prediction, and Generation**
2607.15686v1 by Jiahao Zhao, Junyi Liu, Lifeng Xu, Nan Xu, Qingli Wang, Qingxiao Li, Tianle Chen, Xiaoyu Wu, Yawen Zheng, Zikai Wang, Guanming Liu, Hequn Zhou, Jingyi Wang, Jingyuan Shu, Keqi Wang, Li He, Songyang Diao, Wenhui Xu, Xinyu Ren, Yaqin Fan, Yujin Zhou, Zhanao Yao

We present S1-Omni, a unified multimodal reasoning model for scientific understanding, prediction, and generation. AI for Science (AI4S) has advanced significantly through domain-specific models, tool-augmented LLMs, and scientific language models. However, model capabilities remain highly fragmented, limiting the joint modeling of heterogeneous data, scientific laws, and expert knowledge. S1-Omni addresses this gap by consolidating these capabilities into a single, coherent scientific reasoning model. The architecture of S1-Omni is built upon three core components: unified representation of scientific data, natural-world knowledge alignment, and decoding for domain-specific tasks. First, S1-Omni maps natural-language instructions and scientific objects, including CIF, SMILES, protein sequences, spectra, and scientific images, into a shared representation space. Second, it incorporates scientific laws and expert knowledge into data construction and training, enabling the model to reason from scientific evidence. Third, it performs task-specific decoding to support a broad range of applications, including property prediction, spectrum-to-molecular generation, protein site and structure prediction, and scientific image generation and editing. S1-Omni is trained on S1-Omni-Corpus, which covers 200 scientific tasks and contains millions of reasoning samples, and is evaluated on over 60 scientific benchmarks. It outperforms GPT-5.5 and Gemini-3.1-Pro on most benchmarks and matches or surpasses domain-specific models on several benchmarks. Overall, S1-Omni provides a practical path toward unified scientific modeling.

摘要：我們提出 S1-Omni，一個統一的多模態推理模型，用於科學理解、預測和生成。科學人工智慧 (AI4S) 通過特定領域的模型、工具增強的 LLM 和科學語言模型取得了顯著進展。然而，模型能力仍然高度碎片化，限制了異質數據、科學法則和專家知識的聯合建模。S1-Omni 通過將這些能力整合為一個單一且一致的科學推理模型來填補這一空白。S1-Omni 的架構建立在三個核心組件之上：科學數據的統一表示、自然世界知識的對齊，以及針對特定領域任務的解碼。首先，S1-Omni 將自然語言指令和科學對象，包括 CIF、SMILES、蛋白質序列、光譜和科學圖像，映射到共享表示空間。其次，它將科學法則和專家知識納入數據構建和訓練，使模型能夠從科學證據中推理。第三，它執行特定任務的解碼，以支持廣泛的應用，包括性質預測、光譜到分子的生成、蛋白質位點和結構預測，以及科學圖像的生成和編輯。S1-Omni 在 S1-Omni-Corpus 上進行訓練，該數據集涵蓋 200 個科學任務，包含數百萬個推理樣本，並在 60 多個科學基準上進行評估。它在大多數基準上超越了 GPT-5.5 和 Gemini-3.1-Pro，並在幾個基準上與特定領域模型持平或超過。總體而言，S1-Omni 提供了一條實用的統一科學建模之路。

##### **ToolVerse: Unlocking Massive Environments and Long-Horizon Tasks for Agentic Reinforcement Learning**
2607.15660v1 by Shuaiyu Zhou, Fengpeng Yue, Zengjie Hu, Yuanzhe Shen, Chenyang Zhang, feng hong, Cao Liu, Ke Zeng

While LLM agents demonstrate strong reasoning abilities in compact and well-defined scenarios, they struggle to maintain robustness and effectiveness when faced with large-scale, diverse, and dynamic real-world environments that demand seamless tool integration. To address this gap, we introduce ToolVerse, a comprehensive framework that scales up agentic RL environments and enables agents to perform complex long-horizon reasoning in Tool-Integrated Reasoning (TIR) tasks. First, ToolVerse automatically builds the massive executable agent training environments from nearly 400 real-world Model Context Protocols (MCPs) that contain about 4500 tools. Second, we propose a task design strategy based on a tool dependency graph, utilizing Dynamic Unlocking Sampling Algorithm to generate long-horizon tasks, and produce GUST (Graph Unlocking Sampling Tasks) dataset. Third, to alleviate the credit assigment problem in long-horizon agentic RL, we propose a fine-grained Turn-Aware Relative Advantage algorithm. We conduct extensive Agentic RL training using ToolVerse and evaluate our framework on serveral agentic benchmarks. Experimental results demonstrate that our framework significantly strengthens LLMs' capabilities in long-horizon tool use, achieving a marked performance boost and showcasing robust reasoning within dynamic environments.

摘要：雖然 LLM 代理在緊湊且明確的情境中展現出強大的推理能力，但在面對需要無縫工具整合的大規模、多樣化和動態的現實環境時，它們卻難以保持穩健性和有效性。為了解決這一問題，我們介紹了 ToolVerse，一個全面的框架，能夠擴展代理強化學習環境，並使代理能夠在工具整合推理（TIR）任務中執行複雜的長期推理。首先，ToolVerse 自動從近 400 個現實世界模型上下文協議（MCP）中構建出龐大的可執行代理訓練環境，這些協議包含約 4500 種工具。其次，我們提出了一種基於工具依賴圖的任務設計策略，利用動態解鎖抽樣算法生成長期任務，並產生 GUST（圖解鎖抽樣任務）數據集。第三，為了緩解長期代理強化學習中的信用分配問題，我們提出了一種細粒度的轉向感知相對優勢算法。我們使用 ToolVerse 進行了廣泛的代理強化學習訓練，並在幾個代理基準上評估了我們的框架。實驗結果表明，我們的框架顯著增強了 LLM 在長期工具使用中的能力，實現了顯著的性能提升，並在動態環境中展示了穩健的推理能力。

##### **AEGIS: Assay-Aware Protocol Validation and Runtime Monitoring for Open-Source Liquid Handling Robots**
2607.15620v1 by Priyanka V. Setty, Arvind Ramanathan, Ian Foster, Rick Stevens

Self-driving laboratories increasingly rely on low-cost liquid handlers such as the Opentrons OT-2, which ship without the pressure-based aspiration monitoring of Hamilton or Tecan systems and are typically run open-loop. Two failure modes go undetected: protocols that are syntactically valid but violate assay-specific invariants (e.g., tip reuse between a PCR template and a no-template control), and physical execution failures (partial dispense, air bubbles, missing tips) at runtime. We present AEGIS, a two-layer guardian for both. Layer 1 pairs a curated machine-readable assay rule database with an LLM that reasons over OT-2 Python code, reaching an adjusted F1 of 0.97 on a 24-protocol benchmark across five assay families and beating rules-only and LLM-only ablations across five backends; a free open-weight model ties the best proprietary one, so no paid API is required. Layer 2 fits a PCA world model to YOLO-cropped four-frame pipette trajectories; under a leakage-free leave-one-plate-out evaluation it reaches average precision 0.89 and operating-point F1 0.71 (AUROC 0.80), a deployment-faithful number that matches the live demonstration, and we characterize the small-pipette (p20) resolution limit (F1 0.47). A live demonstration on a physical OT-2 (five replicates per condition) catches planted no-tip failures deterministically and partial dispense on coloured dyes, with an always-VLM self-vote gate lifting partial-dispense recall to 5/5; transparent water is a principled limit of any front-view-only monitor, which AEGIS surfaces as low-confidence VLM reasoning rather than a wrong verdict. Cascade triage holds VLM cost near $1.63 per plate versus $10.33 for an always-VLM baseline. AEGIS is open source and, to our knowledge, the first system to unify pre-flight assay-aware validation with runtime visual monitoring for an open-source liquid handler.

摘要：自動駕駛實驗室越來越依賴低成本的液體處理器，例如 Opentrons OT-2，這些設備不具備 Hamilton 或 Tecan 系統的基於壓力的吸取監控，通常以開環模式運行。兩種失效模式未被檢測到：語法上有效但違反特定檢測不變量的協議（例如，在 PCR 樣本和無樣本對照之間重複使用吸頭），以及在運行時的物理執行失敗（部分分配、氣泡、缺失吸頭）。我們提出 AEGIS，一個針對兩者的雙層守護系統。第一層將經過策劃的可機器讀取的檢測規則數據庫與一個能夠推理 OT-2 Python 代碼的 LLM 配對，在五個檢測家族的 24 個協議基準上達到調整後的 F1 值 0.97，並在五個後端超越了僅基於規則和僅基於 LLM 的消融實驗；一個免費的開放權重模型與最佳的專有模型持平，因此不需要付費 API。第二層將 PCA 世界模型擬合到 YOLO 裁剪的四幀移液器軌跡；在無泄漏的留一板評估下，它達到平均精度 0.89 和操作點 F1 0.71（AUROC 0.80），這是一個與現場演示相符的忠實部署數字，我們還描述了小型移液器（p20）的分辨率極限（F1 0.47）。在一個實體 OT-2 上的現場演示（每個條件五次複製）確定性地捕捉到植入的無吸頭失敗和有色染料的部分分配，通過一個始終-VLM 自我投票閘將部分分配的召回率提升至 5/5；透明水是任何僅前視監控的原則性限制，AEGIS 將其呈現為低信心的 VLM 推理，而不是錯誤的判決。級聯分流使 VLM 成本保持在每板約 1.63 美元，而始終-VLM 基線為 10.33 美元。AEGIS 是開源的，據我們所知，這是第一個將飛行前檢測感知驗證與運行時視覺監控統一的開源液體處理器系統。

##### **MGDT: MLLM-Guided Diffusion Transformer with Relation-Adaptive Mixture-of-Experts for Multimodal Knowledge Graph Completion**
2607.15592v1 by Xu Hou, Meiyu Liang, Wei Huang, Yawen Li, Zhe Xue, Wu Liu, Guanhua Ye, Lei Shi, Kangkang Lu

Multimodal Knowledge Graph Completion (MKGC) requires inferring missing entities from structural, textual, and visual cues. Existing diffusion-based MKGC methods usually denoise directly on raw multimodal features. Such a design forces the denoiser to simultaneously perform relation-dependent cue selection, cross-modal semantic alignment, and structure-aware entity generation, which introduces noisy and semantically inconsistent conditions for diffusion and consequently leads to suboptimal completion performance. To address this limitation, we propose MGDT: MLLM-Guided Diffusion Transformer with Relation-Adaptive Mixture-of-Experts (MGDT), a novel MKGC framework built on an align-then-diffuse paradigm. MGDT first employs a Relation-Adaptive Semantic Routing Mixture-of-Experts (RASR-MoE) module to select relation-relevant multimodal semantic transformation paths and suppress irrelevant modality interference. MGDT then uses a frozen Multimodal Large Language Model (MLLM) as a semantic anchor to align the routed multimodal representations into a unified latent space and reduce cross-modal semantic heterogeneity. Finally, a Knowledge Graph Diffusion Transformer (KGDT) performs graph-conditioned denoising generation in the aligned space to produce the missing entity representation. Experiments on three benchmark datasets show that MGDT consistently outperforms strong baselines.

摘要：多模態知識圖譜補全（MKGC）需要從結構、文本和視覺線索中推斷缺失的實體。現有的基於擴散的MKGC方法通常直接對原始多模態特徵進行去噪。這樣的設計迫使去噪器同時執行與關係相關的線索選擇、跨模態語義對齊和結構感知的實體生成，這為擴散引入了噪聲和語義不一致的條件，從而導致次優的補全性能。為了解決這一限制，我們提出了MGDT：基於關係自適應專家混合的MLLM引導擴散Transformer（MGDT），這是一個基於對齊後擴散範式的新型MKGC框架。MGDT首先採用關係自適應語義路由專家混合（RASR-MoE）模塊來選擇與關係相關的多模態語義轉換路徑並抑制不相關的模態干擾。然後，MGDT使用一個凍結的多模態大型語言模型（MLLM）作為語義錨點，將路由的多模態表示對齊到統一的潛在空間，並減少跨模態語義異質性。最後，知識圖譜擴散Transformer（KGDT）在對齊的空間中執行圖條件去噪生成，以產生缺失的實體表示。在三個基準數據集上的實驗表明，MGDT始終優於強基準。

##### **SkillCorpus: Consolidating and Evaluating the Open Skill Ecosystem for Real-World LLM Agents**
2607.15557v2 by Yanze Wang, Pengfei Yao, Tianyi Sun, Chuanrui Hu, Yan Xiao, Yunyun Han, Jun Sun, Yafeng Deng

Agent skills, $\texttt{SKILL.md}$ files that package reusable procedural knowledge for an LLM agent, are a popular mechanism for extending agent capabilities. Public repositories now host them in large and growing numbers, yet these artifacts are fragmented, redundant, and uneven in quality, and their value in practice is unclear. A core question remains open, namely how to consolidate this open-source $\texttt{SKILL.md}$ ecosystem into a single usable corpus, and what bounds its benefit on real-world agent tasks. We present $\textbf{SkillCorpus}$, a framework that aggregates, curates, matches, and evaluates the open skill ecosystem at scale. It filters ~821,000 crawled skills through a multi-stage pipeline into 96,401 skills organised by a 16-class taxonomy and three quality facets (utility, robustness, safety), and pairs them with a fine-tuned retrieval-and-selection stack that matches task-relevant skills. We evaluate end-to-end across three benchmarks (SkillsBench, GDPVal, QwenClawBench), two harnesses, and two open backbones with a frontier robustness check. Integrating SkillCorpus yields consistent gains across all three benchmarks, largest on SkillsBench (+7.5 pp). An operational analysis traces the gains to a coverage boundary and a harness boundary. SkillCorpus is, to our knowledge, the first end-to-end account of when a curated, retrieval-served community corpus improves real agent tasks, and where it does not. The dataset, models, and code will be released upon acceptance.

摘要：代理技能，$\texttt{SKILL.md}$ 文件將可重用的程序知識打包為 LLM 代理，已成為擴展代理能力的熱門機制。公共存儲庫現在以大量且不斷增長的數量托管這些文件，但這些工件是碎片化的、冗餘的，且質量參差不齊，其在實踐中的價值尚不清楚。一個核心問題仍然懸而未決，即如何將這個開源的 $\texttt{SKILL.md}$ 生態系統整合成一個可用的語料庫，以及它在現實代理任務中的效益邊界。我們提出了 $\textbf{SkillCorpus}$，一個在規模上聚合、策劃、匹配和評估開放技能生態系統的框架。它通過多階段管道過濾約 821,000 個爬取的技能，整理成 96,401 個技能，並根據 16 類分類法和三個質量面向（實用性、穩健性、安全性）進行組織，並將其與一個微調的檢索和選擇堆疊配對，以匹配任務相關的技能。我們在三個基準（SkillsBench、GDPVal、QwenClawBench）、兩個測試環境和兩個開放骨幹上進行端到端評估，並進行前沿穩健性檢查。整合 SkillCorpus 在所有三個基準上都帶來了一致的增益，其中在 SkillsBench 上的增益最大（+7.5 pp）。運營分析將增益追溯到覆蓋邊界和測試環境邊界。據我們所知，SkillCorpus 是第一個端到端的報告，說明何時策劃的、檢索服務的社區語料庫能改善真實代理任務，及其不改善的情況。數據集、模型和代碼將在接受後發布。

##### **Cache-Aware Prompt Compression:A Two-Tier Cost Model for LLM API Caching**
2607.15516v1 by Yan Song

Production LLM deployments combine two cost-reduction primitives: prompt caching (a discounted rate for re-used token prefixes) and prompt compression (fewer tokens sent). The compression literature has standardized on query-aware methods that produce a different compressed prefix per query, mechanically invalidating the prefix-strict cache on every call. We characterize this cost empirically on Anthropic's Sonnet 4.6 API and find caching is far from the rho=1.0 ideal the literature assumes: Sonnet's cache has a two-tier architecture with a sharp threshold near 3,500 tokens, below which the hit rate plateaus at rho~0.83 across 30-call sessions. Our cost model predicts, and experiments confirm, that under realistic rho, query-aware compression beats naive caching at high compression ratios (r>=6). We propose Cache-Aware Prompt Compression (CAPC), pairing query-agnostic compression with explicit cache_control plus a tier-preserving ratio bound that prevents over-compression from pushing the cached prefix into the hot tier. CAPC is the cheapest strategy in 16/16 configurations on LongBench-v2, with mean savings of 49% over cache-only, 64% over query-aware compression, and 90% over vanilla, at quality within 0.05 of the uncompressed baseline. We validate CAPC on three production workloads: an enterprise tool-using assistant with a 94k-token schema prefix (51.7% cost reduction at r=3); a graphify knowledge-graph RAG pipeline across two codebases (9.3x vs cache-all on FastAPI, 2.4x on httpx); and the public tau-bench retail benchmark (50 tasks), where CAPC is the cheapest of four strategies with reward exactly equal to vanilla (both 36/50, p=1.00) while query-aware compression is the most expensive at +40.1% over vanilla -- the first production confirmation of the crossover model's negative-ROI prediction on a public benchmark.

摘要：生產 LLM 部署結合了兩種成本降低原則：提示緩存（對重用的標記前綴提供折扣率）和提示壓縮（發送的標記數量減少）。壓縮文獻已經標準化為查詢感知方法，這些方法為每個查詢產生不同的壓縮前綴，機械性地使每次調用都無效化前綴嚴格緩存。我們在 Anthropic 的 Sonnet 4.6 API 上對這一成本進行了實證特徵分析，發現緩存遠未達到文獻假設的 rho=1.0 理想值：Sonnet 的緩存具有兩級架構，在 3,500 個標記附近有一個明確的閾值，低於該閾值的命中率在 30 次調用會話中穩定在 rho~0.83。我們的成本模型預測，並且實驗確認，在現實的 rho 下，查詢感知壓縮在高壓縮比（r&gt;=6）下優於簡單緩存。我們提出了 Cache-Aware Prompt Compression (CAPC)，將查詢無關的壓縮與明確的 cache_control 結合，並設置一個保留層級的比率限制，以防止過度壓縮將緩存前綴推入熱層。CAPC 在 LongBench-v2 的 16/16 配置中是最便宜的策略，平均節省成本為 49% 相較於僅緩存，64% 相較於查詢感知壓縮，90% 相較於原始版本，且質量在未壓縮基準線的 0.05 以內。我們在三個生產工作負載上驗證了 CAPC：一個使用 94k 標記架構前綴的企業工具助手（在 r=3 時成本降低 51.7%）；一個跨兩個代碼庫的圖形知識圖 RAG 管道（在 FastAPI 上比緩存全部快 9.3 倍，在 httpx 上快 2.4 倍）；以及公共 tau-bench 零售基準（50 個任務），在這裡 CAPC 是四種策略中最便宜的，獎勵與原始版本完全相等（均為 36/50，p=1.00），而查詢感知壓縮則是最貴的，比原始版本高出 40.1%——這是公共基準上交叉模型負 ROI 預測的首次生產確認。

##### **An Auto-Scaling Approach for Serverless Environments Based on a Multi-Expert Consensus Mechanism**
2607.15511v1 by Mobina Kashaniyan, Mehrdad Ashtiani, Amirhossein Ghassemi

Serverless computing provides automatic resource management and pay-per-use execution, but effective autoscaling remains challenging because of dynamic workloads, cold-start latency, and dependencies among functions. We present a dependency-aware autoscaling framework that integrates graph-based bottleneck identification, short-term workload forecasting, multi-model consensus, and cost-aware scaling control. Serverless applications are represented as directed dependency graphs, and structurally important functions are identified using weighted degree centrality. Resource demand is predicted using lightweight MLP, LSTM, and CNN models. Their outputs are combined through a performance-weighted probabilistic ensemble inspired by Bayesian model averaging. The controller further incorporates cold-start awareness and cost comparison to select among scale-up, scale-down, and hold actions. Experiments using real workload traces show that supervised forecasting substantially outperforms unsupervised clustering for autoscaling decision generation. The proposed ensemble achieves 99.88 percent prediction accuracy and reduces prediction error compared with representative hybrid forecasting methods. Evaluations across multiple cloud pricing models also demonstrate consistent infrastructure cost reductions while maintaining performance targets. The results show that combining dependency analysis, multi-expert forecasting, and cost-aware control provides a robust and practical solution for serverless autoscaling.

摘要：無伺服器計算提供自動資源管理和按需執行，但由於動態工作負載、冷啟動延遲和函數之間的依賴關係，有效的自動擴展仍然具有挑戰性。我們提出了一種依賴感知的自動擴展框架，該框架整合了基於圖的瓶頸識別、短期工作負載預測、多模型共識和成本感知的擴展控制。無伺服器應用被表示為有向依賴圖，並使用加權度中心性識別結構上重要的函數。資源需求使用輕量級的MLP、LSTM和CNN模型進行預測。它們的輸出通過受性能加權的概率集成進行組合，靈感來自貝葉斯模型平均。控制器進一步納入冷啟動意識和成本比較，以在擴展、縮減和保持行動之間進行選擇。使用真實工作負載跟蹤的實驗顯示，監督式預測在自動擴展決策生成方面顯著優於無監督聚類。所提出的集成達到99.88%的預測準確率，並與代表性的混合預測方法相比減少了預測誤差。對多個雲定價模型的評估也顯示，在保持性能目標的同時，基礎設施成本持續降低。結果表明，結合依賴分析、多專家預測和成本感知控制為無伺服器自動擴展提供了一個穩健且實用的解決方案。

##### **FLINT: Fingerprinting Federated Learning Architectures from 5G PHY-Layer Side Channels**
2607.15469v1 by Md Nahid Hasan Shuvo, Mahmudul Hassan Ashik, Moinul Hossain

Federated Learning (FL) over 5G cellular networks protects raw data but remains vulnerable to side-channel leakage. Prior fingerprinting attacks assume packet-level network visibility, an assumption that does not hold at the 5G Physical (PHY) layer, where user payloads are encrypted and Radio Network Temporary Identifiers (RNTIs) may change over time. However, we demonstrate that PHY-layer scheduling metadata broadcast over the Physical Downlink Control Channel (PDCCH) preserves architecture-associated temporal patterns. We introduce FLINT, a novel black-box fingerprinting framework that infers FL model architecture families, including CNNs, RNNs, and Transformers, using only coarse PHY-layer observations. FLINT overcomes the lack of network-layer visibility by decoding PDCCH scheduling information, mapping changing RNTIs to physical user devices, and applying multi-view temporal modeling to distinguish architecture-specific training behavior. This leakage is security-critical because knowledge of a client's model architecture can transform passive reconnaissance into targeted downstream exploitation. Extensive experiments on an over-the-air srsRAN-based 5G testbed demonstrate that FLINT achieves a macro F1-score of 0.930 for architecture-family classification. To our knowledge, FLINT is the first work to fingerprint AI/ML model architectures using lower-layer 5G side-channel information obtainable by any protocol-aware adversary.

摘要：聯邦學習（FL）在5G行動網絡上保護原始數據，但仍然易受側信道洩漏的影響。先前的指紋攻擊假設具有封包級的網絡可見性，但這一假設在5G物理層（PHY）並不成立，因為用戶有效載荷是加密的，無線網絡臨時標識符（RNTI）可能隨時間變化。然而，我們證明了在物理下行控制信道（PDCCH）上廣播的PHY層調度元數據保留了與架構相關的時間模式。我們介紹了FLINT，這是一個新穎的黑箱指紋識別框架，它僅使用粗略的PHY層觀察來推斷FL模型架構家族，包括CNN、RNN和Transformer。FLINT通過解碼PDCCH調度信息，將變化的RNTI映射到物理用戶設備，並應用多視角時間建模來區分特定架構的訓練行為，克服了缺乏網絡層可見性的問題。這種洩漏對安全至關重要，因為了解客戶端的模型架構可以將被動偵察轉變為有針對性的下游利用。在基於srsRAN的5G測試平台上進行的廣泛實驗表明，FLINT在架構家族分類中達到了0.930的宏F1分數。據我們所知，FLINT是第一個使用任何協議感知對手可獲得的低層5G側信道信息來指紋識別AI/ML模型架構的工作。

##### **Partition, Prompt, Aggregate: Statistical Self-Consistency in Language Models**
2607.15277v1 by Patrik Wolf, Thomas Kleine Buening, Andreas Krause, Celestine Mendler-Dünner

In-context learning is commonly interpreted as a form of conditional inference, in which the prompt specifies a context and the model's output is treated as an estimate of the corresponding conditional distribution. If this interpretation holds, then LLM estimates should satisfy basic probabilistic identities. In particular, the law of total probability asserts that prior-weighted conditional distributions aggregate into population-level marginals over any valid partition of the population. In this work, we investigate to what extent LLM estimates adhere to this self-consistency principle. We use binary trees as an evaluation scaffold to recursively partition a population into increasingly fine-grained subpopulations. We then prompt LLMs with verbalized subpopulation descriptions in context, aggregate the resulting estimates back into population-level estimates, and compare them across partitions of varying granularity. Applying this protocol across problem domains and state-of-the-art frontier models, we show widespread violations of basic consistency properties. An in-depth study of persona prompting reveals a pattern we call the macro fallacy: estimates reconstructed from more fine-grained subpopulation responses are often better aligned with human reference data than direct population-level estimates. This effect persists across variations in tree structure and estimation task, and can be partially recovered through implicit prompting. Together, these findings suggest that models possess relevant subpopulation knowledge but do not reliably propagate it into aggregate estimates. This gap establishes statistical self-consistency as an unsaturated, reference-free criterion for evaluating LLMs.

摘要：在上下文學習中，通常被解釋為一種條件推理的形式，其中提示指定了一個上下文，模型的輸出被視為對應條件分佈的估計。<lb/>如果這種解釋成立，那麼大型語言模型（LLM）的估計應該滿足基本的概率性身份。<lb/>特別是，總概率法則聲稱，先前加權的條件分佈在任何有效的人口劃分中聚合成整體水平的邊際分佈。<lb/>在這項工作中，我們研究LLM的估計在多大程度上遵循這一自我一致性原則。<lb/>我們使用二叉樹作為評估框架，遞歸地將一個人口劃分為越來越細的子群體。<lb/>然後，我們用語言化的子群體描述在上下文中提示LLM，將產生的估計聚合回整體水平的估計，並在不同粒度的劃分中進行比較。<lb/>在不同問題領域和最先進的前沿模型中應用這一協議，我們顯示出基本一致性特性的大規模違反。<lb/>對角色提示的深入研究揭示了一種我們稱之為宏觀謬誤的模式：從更細粒度的子群體反應重建的估計，通常比直接的人口水平估計更好地與人類參考數據對齊。<lb/>這一效應在樹結構和估計任務的變化中持續存在，並且可以通過隱性提示部分恢復。<lb/>綜合這些發現表明，模型擁有相關的子群體知識，但並不可靠地將其傳播到聚合估計中。<lb/>這一差距確立了統計自我一致性作為評估LLM的一個未飽和、無參考標準。

##### **SearchOS-V1: Towards Robust Open-Domain Information-Seeking Agent Collaboration**
2607.15257v1 by Yuyao Zhang, Junjie Gao, Zhengxian Wu, Jiaming Fan, Jin Zhang, Shihan Ma, Yao Yao, Weiran Qi, Chuyan Jin, Guiyu Ma, Xingzhong Xu, Kai Yang, Ji-Rong Wen, Zhicheng Dou

Recent advances in Tool-Integrated Large Language Models have made web search a core capability of information-seeking agents. However, as interaction histories grow, agents increasingly struggle to track task progress. When search attempts fail to yield useful evidence, current single- and multi-agent systems can become trapped in repetitive loops, wasting search budgets and ultimately compromising the quality and completeness of the final output. We introduce SearchOS, a system-level multi-agent framework that turns fragile, implicit search progress into explicit, persistent, and shared state. First, we formulate open-domain information seeking as relational schema completion with grounded citations, where agents discover entities, populate attributes across linked tables, and anchor each value to source evidence. Then we design Search-Oriented Context Management (SOCM), which externalizes the evolving state into Frontier Task, an Evidence Graph, a Coverage Map, and Failure Memory. Built on SOCM, SearchOS applies a pipeline-parallel scheduling mechanism that overlaps the execution of sub-agents and continuously refills freed slots with tasks targeting unresolved coverage gaps to improve utilization and throughput. To schedule and control the execution of search agents, SearchOS introduces a Search Tool Middleware Harness that intercepts model and tool interactions to record grounded evidence and react to stalls or budget exhaustion, and provides a reusable hierarchical skill system comprising strategy and access skills to augment the agents' search process and avoid repeating failed search patterns across runs. On WideSearch and GISA, SearchOS leads all metrics among the evaluated single- and multi-agent baselines, paving the way toward robust information-seeking collaboration.

摘要：最近在工具整合的大型語言模型方面的進展，使得網頁搜索成為信息尋求代理的核心能力。<lb/>然而，隨著互動歷史的增長，代理在跟踪任務進展方面越來越困難。<lb/>當搜索嘗試未能產生有用的證據時，當前的單一和多代理系統可能會陷入重複循環，浪費搜索預算，最終影響最終輸出的質量和完整性。<lb/>我們介紹了SearchOS，一個系統級的多代理框架，將脆弱的、隱式的搜索進展轉變為明確的、持久的和共享的狀態。<lb/>首先，我們將開放域的信息尋求形式化為帶有基礎引用的關係模式補全，其中代理發現實體，填充鏈接表中的屬性，並將每個值固定到來源證據上。<lb/>然後，我們設計了搜索導向的上下文管理（SOCM），將不斷演變的狀態外部化為前沿任務、一個證據圖、一個覆蓋圖和失敗記憶。<lb/>基於SOCM，SearchOS應用了一個管道並行調度機制，重疊子代理的執行，並持續用針對未解決覆蓋空白的任務填補釋放的空槽，以提高利用率和吞吐量。<lb/>為了調度和控制搜索代理的執行，SearchOS引入了一個搜索工具中介系統，攔截模型和工具的互動，以記錄基礎證據並對停滯或預算耗盡做出反應，並提供一個可重用的分層技能系統，包含策略和訪問技能，以增強代理的搜索過程，並避免在多次運行中重複失敗的搜索模式。<lb/>在WideSearch和GISA上，SearchOS在所有評估的單一和多代理基準中領先於所有指標，為穩健的信息尋求協作鋪平了道路。

##### **AutoSynthesis: An agentic system for automated meta-analysis**
2607.15247v1 by Moein Taherinezhad, Sebastian Maier, Gerardo Vitagliano, Francesco Pierri, Stefan Feuerriegel

Evidence synthesis is crucial for turning primary research into reliable knowledge for science, medicine, education, and policy. Yet, quantitative evidence synthesis remains largely manual and difficult to scale. Here, we introduce AutoSynthesis, an end-to-end multi-agent system for automated meta-analysis. Given a research question in natural language, AutoSynthesis formulates a search strategy, retrieves scientific literature, screens candidate studies, assesses full-text eligibility, extracts quantitative statistics, computes standardized effect sizes, and finally performs random-effects meta-analysis. AutoSynthesis further supports heterogeneity analysis to examine how effect sizes vary across moderators, as well as risk-of-bias assessment. As output, AutoSynthesis produces a transparent report aligned with PRISMA guidelines. In our application, AutoSynthesis screened over 28 studies and extracted more than 20 quantitative claims. The pooled effect estimates produced by AutoSynthesis are similar to Hedges' $g$ of expert-conducted meta-analyses, indicating close agreement with manual evidence synthesis. Together, these results show that AutoSynthesis can make quantitative evidence synthesis more scalable, thereby supporting evidence-based decision-making across disciplines.

摘要：證據綜合對於將初級研究轉化為科學、醫學、教育和政策的可靠知識至關重要。<lb/>然而，定量證據綜合在很大程度上仍然是手動的，且難以擴展。<lb/>在此，我們介紹了 AutoSynthesis，一個端到端的多代理系統，用於自動化的元分析。<lb/>給定一個自然語言的研究問題，AutoSynthesis 制定搜索策略，檢索科學文獻，篩選候選研究，評估全文資格，提取定量統計數據，計算標準化效應大小，並最終執行隨機效應元分析。<lb/>AutoSynthesis 進一步支持異質性分析，以檢查效應大小如何隨調節變數而變化，以及偏見風險評估。<lb/>作為輸出，AutoSynthesis 生成一份符合 PRISMA 指南的透明報告。<lb/>在我們的應用中，AutoSynthesis 篩選了超過 28 項研究，並提取了超過 20 項定量主張。<lb/>AutoSynthesis 產生的合併效應估計與專家進行的元分析中的 Hedges' $g$ 相似，顯示出與手動證據綜合的高度一致。<lb/>綜合這些結果顯示，AutoSynthesis 可以使定量證據綜合更具可擴展性，從而支持跨學科的基於證據的決策制定。

##### **Linear representations of grammaticality in neural language models**
2607.15175v1 by Jane Li, Najoung Kim

Whether neural language models (NLMs) possess the ability to distinguish strings on the basis of their grammaticality remains a debated topic in the computational linguistics literature. Existing evidence has largely relied on probability-based measures, testing whether models assign higher probabilities to grammatical than ungrammatical strings. However, probability comparisons have been criticized as a measure for grammatical knowledge based on the assumption that grammaticality is inherently entangled with likelihood. Model-assigned probability is a function of many related sentence properties, such as lexical frequency, plausibility, and world knowledge. In this work, we move beyond probability-based evaluations and investigate whether grammaticality is encoded in the internal representations of NLMs. Using mass-mean probing, we test whether grammatical and ungrammatical sentences are systematically separated in representational space. We further examine the extent to which these representations are independent of sentence properties that are correlated with grammaticality, as well as their generalization across grammatical phenomena and languages. Our results provide evidence that grammaticality is robustly encoded in sentence representations of a wide range of pretrained NLMs, yielding clear representational separation on the dimension of grammaticality that cannot be fully explained by alternative sentence-level factors. Moreover, this encoding generalizes across a broad range of grammatical phenomena and to some degree, across languages, suggesting that grammaticality constitutes a coherent representational dimension in contemporary NLMs. These findings contribute new evidence to debates about the nature of syntactic knowledge in language models and offer a complementary framework for evaluating grammatical competence that is not dependent on string probabilities alone.

摘要：神經語言模型（NLMs）是否具備根據語法性區分字符串的能力仍然是計算語言學文獻中一個有爭議的話題。現有的證據在很大程度上依賴於基於概率的測量，測試模型是否對語法正確的字符串賦予比語法錯誤的字符串更高的概率。然而，基於概率的比較因為假設語法性與可能性本質上是交織在一起的而受到批評。模型賦予的概率是許多相關句子特徵的函數，例如詞彙頻率、合理性和世界知識。在本研究中，我們超越了基於概率的評估，調查語法性是否在NLM的內部表示中編碼。使用質量均值探測，我們測試語法正確和語法錯誤的句子是否在表示空間中系統性地分開。我們進一步檢查這些表示在多大程度上獨立於與語法性相關的句子特徵，以及它們在語法現象和語言之間的概括能力。我們的結果提供了證據，顯示語法性在廣泛的預訓練NLM的句子表示中被穩健地編碼，並在語法性維度上產生明確的表示分離，這無法完全用替代的句子級別因素來解釋。此外，這種編碼在廣泛的語法現象中以及在某種程度上跨語言進行概括，這表明語法性構成了當代NLM中的一個一致的表示維度。這些發現為有關語言模型中句法知識本質的辯論提供了新的證據，並提供了一個補充框架，用於評估不僅依賴於字符串概率的語法能力。

##### **The Industrialization of Research ; On AI-Driven Science and Its Consequences**
2607.15164v1 by Emmanuel Jeannot

Artificial intelligence is transforming scientific research -- not merely as a more powerful instrument, but as an autonomous participant in the research cycle itself. This transition constitutes, in the most precise sense of the term, the industrialization of research: a shift from a craft model, in which knowledge, method, and judgment are embedded in the researcher, to a pipeline model, in which these steps are decomposed, automated, and supervised. The US Department of Energy's Genesis Mission is the most ambitious current instantiation of this shift, but the fundamental questions it raises extend far beyond any single program. This essay examines seven such questions: the erosion of the intergenerational transmission of scientific competence; the growing opacity of AI-generated theories; the collapse of peer evaluation under a flood of machine-generated output; the unproven capacity of AI for paradigm-shifting discovery; the capture of the scientific agenda by political and industrial actors; the compounding of systematic errors in closed-loop pipelines; and the structural bifurcation of the global research community into incommensurable tiers. These concerns do not constitute an argument against AI-driven science -- whose demonstrated potential is real and significant. They constitute the conditions under which that potential can be responsibly pursued.

摘要：人工智慧正在改變科學研究——不僅僅是作為一種更強大的工具，而是作為研究循環中的自主參與者。這一過渡在最精確的意義上構成了研究的工業化：從一種工藝模型轉變為一種管道模型，在這種模型中，知識、方法和判斷嵌入於研究者中，而這些步驟則被分解、自動化和監督。美國能源部的創世任務是這一轉變目前最雄心勃勃的實例，但它所提出的根本問題遠遠超出任何單一計劃。這篇文章探討了七個此類問題：科學能力的代際傳承的侵蝕；AI生成理論的日益不透明；在機器生成的輸出洪流下同行評審的崩潰；AI在顛覆性發現中的未經證明的能力；政治和工業行為者對科學議程的控制；在封閉循環管道中系統性錯誤的累積；以及全球研究社群結構性分岐為不可比擬的層級。這些擔憂並不構成對AI驅動科學的反對——其顯示的潛力是真實且重要的。它們構成了可以負責任地追求這一潛力的條件。

##### **BrainPilot: Automating Brain Discovery with Agentic Research**
2607.15079v2 by Haoxuan Li, Tianci Gao, Jianhe Li, Yang Fan, Runze Shi, Weiran Wang, Tianxiang Zhao, Zezhao Wu, Xiaoyang Jiang, Qihui Zhang, Jia Li, Xiao Xiao, Kai Du, Xiaoxuan Jia, Chao Xie, Lu Mi

Understanding the brain increasingly depends on integrating evidence across scales, modalities, and disciplines. Addressing a single research question therefore requires a coordinated sequence of operations, from surveying prior work to executing analyses and interpreting results in light of domain knowledge. AI agents promise to accelerate this process, but current agents lack domain expertise in brain science, may fabricate claims, drift during multi-step reasoning, and offer few defined points for expert intervention. These failures are especially costly in brain science, where conclusions feed into downstream scientific claims and depend on laboratory-specific expertise and careful human judgment. We present \textbf{BrainPilot} a \textbf{fully open-source} multi-agent system that accelerates brain science research with traceable logs and agent-verified results. A principal investigator (PI) agent coordinates specialist agents grounded in curated domain knowledge: a unified brain science knowledge base containing 7{,}233 indexed items and a skill library of 72 reusable methodology units across seven research domains. Every major step is recorded in the Graph of Trace, an auditable record that links subgoals, tool use, evidence, and claims and allows researchers to follow and inspect the workflow. An Auditor agent further integrates fabrication checking into the workflow. For evaluation, we run three brain science tasks from Agents' Last Exam, introduce our own benchmark, \textbf{BrainPilotBench-v0}, and present additional end-to-end case studies. Across these evaluations, BrainPilot with an open-source backbone model attains performance comparable to state-of-the-art agent framework with less costs.

摘要：理解大腦越來越依賴於跨尺度、跨模態和跨學科整合證據。因此，解決單一研究問題需要一系列協調的操作，從調查先前的工作到執行分析，並根據領域知識解釋結果。AI代理有望加速這一過程，但目前的代理在腦科學領域缺乏專業知識，可能會捏造聲明，在多步推理過程中偏離主題，並提供很少的明確介入點供專家使用。這些失敗在腦科學中尤其代價高昂，因為結論會影響下游的科學聲明，並依賴於實驗室特定的專業知識和謹慎的人類判斷。我們提出了\textbf{BrainPilot}，一個\textbf{完全開源}的多代理系統，通過可追蹤的日誌和代理驗證的結果來加速腦科學研究。一個首席研究員（PI）代理協調基於策劃的領域知識的專家代理：一個統一的腦科學知識庫，包含7{,}233個索引項目，以及跨七個研究領域的72個可重用方法單元的技能庫。每一個主要步驟都記錄在Trace圖中，這是一個可審計的記錄，連結子目標、工具使用、證據和聲明，並允許研究人員跟蹤和檢查工作流程。一個審計代理進一步將捏造檢查整合到工作流程中。為了評估，我們執行了來自代理最後考試的三個腦科學任務，介紹了我們自己的基準\textbf{BrainPilotBench-v0}，並呈現了額外的端到端案例研究。在這些評估中，使用開源骨幹模型的BrainPilot達到的性能與最先進的代理框架相當，但成本更低。

##### **OmniaBench: Benchmarking General AI Agents Across Diverse Scenarios**
2607.14989v1 by Chengyu Shen, Yujie Fu, Gangtao Xin, Yanheng Hou, Wenlong Fei, Guojie Zhu, Jiawei Li, Hongcheng Gao, Runming He, Zhen Hao Wong, Meiyi Qiang, Hao Liang, Zhao Cao, Hao Jiang, Chong Chen, Wentao Zhang

Large language models are increasingly evolving from text generators into general agents capable of understanding user requests, invoking external tools, and completing complex tasks through interaction. However, existing agent benchmarks often focus on limited scenarios, tool ecosystems, or interaction formats, making it difficult to systematically characterize model capabilities across heterogeneous application settings. We introduce OmniaBench, a benchmark for evaluating general agents across diverse scenarios with explicit state spaces. We derive application-oriented scenario knowledge from app stores, product documents, industry resources, Web retrieval, and human refinement, forming a hierarchical taxonomy that spans ToC, ToB and ToE with 90 level-1 and 354 level-2 domains. Based on this taxonomy, we construct executable environments and synthesize single-turn and multi-turn tasks through four complementary routes: DAG, DAG-S, Solver, and Program. OmniaBench further introduces a ten-dimensional capability taxonomy and eight compositional atomic difficulty factors to support fine-grained evaluation and analysis. The resulting dataset contains 1,431 tasks, together with a challenging subset of 644 tasks designed to reduce evaluation cost and mitigate potential contamination of the full set after public release. The bench presents substantial challenges to current frontier models, with even Claude-Sonnet-5 and GPT-5.6-Sol achieving Overall Pass@1 scores of only 58.54 and 57.14, respectively. Further analyses reveal clear differences across domains and capabilities, as well as persistent limitations in planning, constraint maintenance, and adaptive correction. OmniaBench provides a broad and diagnostic benchmark for characterizing the capability boundaries of general agents.

摘要：大型語言模型正逐漸從文本生成器演變為能夠理解用戶請求、調用外部工具並通過互動完成複雜任務的一般代理。<lb/>然而，現有的代理基準通常專注於有限的場景、工具生態系統或互動格式，這使得在異質應用設置中系統性地表徵模型能力變得困難。<lb/>我們介紹了OmniaBench，這是一個用於在多樣化場景中評估一般代理的基準，具有明確的狀態空間。<lb/>我們從應用商店、產品文檔、行業資源、網絡檢索和人類精煉中提取以應用為導向的場景知識，形成了一個層次化的分類法，涵蓋了ToC、ToB和ToE，共有90個一級和354個二級領域。<lb/>基於這一分類法，我們構建了可執行的環境，並通過四條互補路徑合成單輪和多輪任務：DAG、DAG-S、Solver和Program。<lb/>OmniaBench進一步引入了一個十維的能力分類法和八個組合原子難度因素，以支持細緻的評估和分析。<lb/>最終的數據集包含1,431個任務，還有一個挑戰性的子集，其中包含644個任務，旨在降低評估成本並減少在公開發布後對完整數據集的潛在污染。<lb/>該基準對當前前沿模型提出了重大挑戰，即使是Claude-Sonnet-5和GPT-5.6-Sol的整體通過率@1分數也僅為58.54和57.14。<lb/>進一步分析顯示，在不同領域和能力之間存在明顯差異，以及在規劃、約束維護和自適應修正方面的持續限制。<lb/>OmniaBench提供了一個廣泛且診斷性的基準，用於表徵一般代理的能力邊界。

##### **Show Me How You Reason and I'll Tell You Who You Are: Reasoning Graphs for Robust LLM Authorship Attribution**
2607.14905v2 by Zlata Kikteva, Artur Romazanov, Annette Hautli-Janisz, Ramon Ruiz-Dolz

Given the current trend to employ large language models (LLMs) in almost any imaginable context, LLM-generated text detection and authorship attribution have become a pressing issue. Prior work has primarily focused on surface-level linguistic features, an approach shown to be susceptible to paraphrasing and other obfuscation techniques. In this paper, we go beyond the linguistic surface, extracting and analysing reasoning structures in LLM-generated texts with the goal of capturing more complex signals of LLM authorship. We propose a graph neural network approach that leverages reasoning graphs extracted by an argument mining pipeline, demonstrating improved robustness and generalisation over a traditional Longformer baseline. Our approach outperforms the baseline by up to 27 percentage points under the obfuscation attacks such as paraphrasing and backtranslation, and 19 percentage points when evaluated on the texts generated by the unseen model versions, simulating real-world conditions in which new LLM versions are continuously released.

摘要：考慮到目前在幾乎任何可想像的情境中使用大型語言模型（LLMs）的趨勢，LLM生成的文本檢測和作者歸屬已成為一個緊迫的問題。先前的研究主要集中在表面層次的語言特徵上，這種方法已被證明容易受到改寫和其他混淆技術的影響。在本文中，我們超越語言表面，提取和分析LLM生成文本中的推理結構，目的是捕捉更複雜的LLM作者信號。我們提出了一種圖神經網絡方法，利用由論證挖掘管道提取的推理圖，展示出相較於傳統的Longformer基線更好的穩健性和泛化能力。在面對如改寫和回譯等混淆攻擊時，我們的方法在基線上提高了多達27個百分點的表現，而在評估未見模型版本生成的文本時則提高了19個百分點，模擬了新LLM版本不斷發布的真實世界條件。

##### **Reachability-Aware Pretraining for Efficient Target-Oriented Path Exploration in Temporal Knowledge Graph Reasoning**
2607.14886v1 by Chien-Liang Liu, Tsao-Lun Chen

Temporal Knowledge Graph (TKG) reasoning under the extrapolation setting focuses on forecasting future time-stamped events (facts) from historical data in a temporal knowledge graph. Existing approaches, reinforcement learning (RL)-based multi-hop reasoning methods are prominent for TKG reasoning because they produce human-interpretable predictions via explicit multi-hop path tracing. However, during RL training, rewards are typically sparse, and exploration is highly inefficient due to the vast, time-evolving action space. These issues hinder efficient training and often limit overall performance. To address these challenges, we propose RAPTOR (Reachability-Aware Pretraining for Efficient Target-Oriented Path Exploration), a self-supervised pretraining method that injects a reachability-aware inductive bias to the agent. By learning to estimate the reachability of candidate actions to the target entity, RAPTOR reduces exploration over unpromising paths and provides a strong initialization for downstream RL fine-tuning. Experimental results on the ICEWS14, ICEWS05-15, and ICEWS18 datasets demonstrate that RAPTOR pretraining markedly improves the training efficiency and consistently outperforms conventional baselines, establishing it as an effective approach for enhancing RL-based multi-hop reasoning methods for TKG reasoning.

摘要：時間知識圖譜（TKG）推理在外推設定下專注於從時間知識圖譜中的歷史數據預測未來的時間戳事件（事實）。現有的方法中，基於強化學習（RL）的多跳推理方法在TKG推理中非常突出，因為它們通過明確的多跳路徑追蹤產生可被人類解釋的預測。然而，在RL訓練過程中，獎勵通常是稀疏的，且由於龐大且隨時間演變的行動空間，探索效率極低。這些問題妨礙了高效的訓練，並且通常限制了整體性能。為了解決這些挑戰，我們提出了RAPTOR（可達性意識的高效目標導向路徑探索預訓練），這是一種自我監督的預訓練方法，為代理注入可達性意識的歸納偏見。通過學習估計候選行動對目標實體的可達性，RAPTOR減少了對不具前景路徑的探索，並為下游的RL微調提供了強大的初始化。ICEWS14、ICEWS05-15和ICEWS18數據集上的實驗結果表明，RAPTOR預訓練顯著提高了訓練效率，並始終超越傳統基準，確立了其作為增強基於RL的多跳推理方法在TKG推理中有效方法的地位。

##### **Transcoders for Investigating Deception in Language Models**
2607.14791v1 by Darius Lim, Nathan Leow, Xin Wei Chia

Transcoders have recently emerged as a promising approach for mechanistic interpretability (MI), enabling circuit-level analysis of model behaviour. In this paper, we investigate the use of transcoders to analyse deceptive behaviour in language models, a behaviour that poses a safety and security risk. Using a Qwen3-4B model with pre-trained transcoders, specifically per-layer transcoders (PLTs), we construct attribution graphs that capture feature activations and inter-feature dependencies, allowing circuit-level analysis of deception. Through feature steering and circuit analysis, we identified a dictionary of deception-related features and show that these features exert a stronger influence on deceptive outputs, as they produce predictable shifts between deceptive and non-deceptive responses. These findings suggest that deception emerges from internal model mechanisms and highlight the potential of transcoders for behavioural monitoring and early detection of security vulnerabilities related to malicious behaviours in language models.

摘要：轉碼器最近作為機械解釋性（MI）的一種有前景的方法出現，能夠對模型行為進行電路級分析。<lb/>在這篇論文中，我們研究了使用轉碼器來分析語言模型中的欺騙行為，這種行為構成了安全風險。<lb/>使用一個預訓練轉碼器的Qwen3-4B模型，特別是每層轉碼器（PLTs），我們構建了捕捉特徵激活和特徵間依賴性的歸因圖，允許對欺騙行為進行電路級分析。<lb/>通過特徵引導和電路分析，我們識別出一個與欺騙相關的特徵字典，並顯示這些特徵對欺騙輸出施加了更強的影響，因為它們在欺騙和非欺騙反應之間產生可預測的轉變。<lb/>這些發現表明，欺騙源於內部模型機制，並突顯了轉碼器在行為監測和早期檢測與語言模型中的惡意行為相關的安全漏洞方面的潛力。

##### **AI vs Human Expert Reasoning: Assessing Agreements in Building Typology Predictions based on Street View Imagery**
2607.14756v1 by Zahratu Shabrina, Muhammad Asa, Jin Rui, Lu Yin, Stephen Law

This research investigates the potential of Vision-Language Models (VLMs) to infer building typologies: Construction, Current Use, and Storeys from Google Street View (GSV) images. Predictions generated by VLMs are compared with inference by human experts (civil engineers and architects) as a source of manually labelled ground-truth data. We evaluate several state-of-the-art VLMs, including GPT-4o, Claude 3.5 Sonnet, and Gemini 2.0 Flash. By applying different scaling strategies and prompting techniques, we found that Chain-of-Thought prompts provide an overall more stable model performance. We also investigate the reasoning behind VLMs' building-typology predictions by examining the probabilities of keywords appearing in AI explanations. This enabled us to analyse patterns in these reasonings and identify key themes driving both agreements and disagreements between VLM and expert labels. We find that AI tends to focus on visual indicators, whereas human experts place greater emphasis on broader contextual cues and domain knowledge, in addition to visual cues. Overall, VLM can approximate experts' capability in building-typology classification at scale, with an average accuracy of approximately 70%. The study demonstrates the VLM's potential for AI automation in tasks that require pattern recognition and object identification in an urban context. AI have the potential to serve as complementary and collaborative tools for urban analysis, leveraging their strengths in understanding visual patterns. This study contributes to the exploration of the efficiency and scalability of AI visual prediction and provides insights into the reasoning processes that could support automation processes in urban analysis and prediction.

摘要：這項研究探討了視覺-語言模型（VLM）推斷建築類型的潛力：建設、當前使用和樓層，基於 Google 街景（GSV）圖像。由 VLM 生成的預測與人類專家（土木工程師和建築師）的推斷進行比較，以作為手動標記的真實數據來源。我們評估了幾個最先進的 VLM，包括 GPT-4o、Claude 3.5 Sonnet 和 Gemini 2.0 Flash。通過應用不同的擴展策略和提示技術，我們發現思維鏈提示提供了整體上更穩定的模型性能。我們還通過檢查關鍵詞在 AI 解釋中出現的概率，來研究 VLM 建築類型預測背後的推理。這使我們能夠分析這些推理中的模式，並確定推動 VLM 和專家標籤之間一致性和不一致性的關鍵主題。我們發現 AI 傾向於關注視覺指標，而人類專家則更重視更廣泛的上下文線索和領域知識，除了視覺線索之外。總體而言，VLM 可以在規模上接近專家在建築類型分類中的能力，平均準確率約為 70%。這項研究展示了 VLM 在需要模式識別和物體識別的城市背景任務中的 AI 自動化潛力。AI 有潛力成為城市分析的補充和協作工具，利用其在理解視覺模式方面的優勢。這項研究有助於探索 AI 視覺預測的效率和可擴展性，並提供了支持城市分析和預測自動化過程的推理過程的見解。

##### **Pretraining Multiple Instance Learning Networks with Multi-Teacher Distillation from Pathology Slide Foundation Models**
2607.14703v1 by Mingxi Fu, Jiawen Li, Renao Yan, Jiali Hu, Qiehe Sun, Tian Guan, Yonghong He

Multiple instance learning (MIL) has become the main paradigm for whole-slide image (WSI) analysis in computational pathology. However, existing MIL aggregators are still typically trained from scratch for each downstream task, relying on limited slide-level labels to learn both aggregation mechanisms and downstream discriminative representations simultaneously. As a result, they often suffer from unstable optimization, overfitting, and limited transferability. Similar to pretrained ResNet and Vision Transformer models in natural image learning, MIL also requires reusable pretrained initialization. However, high-quality slide-level pretraining data remain scarce, and MIL models are usually lightweight and weakly supervised, making large-scale pretraining difficult in practice. To address this challenge, we propose a distillation-based pretraining framework for MIL, which leverages two slide-level foundation models, TITAN and CARE, as teachers to transfer their representational knowledge into a diverse set of MIL architectures. To effectively balance supervision from different teachers, we further introduce an angular dispersion normalized distillation loss. The distilled weights are then used as initialization for downstream adaptation. We conduct systematic evaluations on 15 benchmark datasets under both linear probing and full-parameter fine-tuning, and further validate its advantages in few-shot scenarios. Experimental results show that pretraining generally improves MIL aggregators over from scratch training, especially in linear-probing and few-shot settings, while maintaining the computational efficiency of lightweight MIL models. Code is available at https://github.com/fu0201/MIL_Pretrained.

摘要：多實例學習（MIL）已成為計算病理學中全片影像（WSI）分析的主要範式。<lb/>然而，現有的MIL聚合器通常仍需為每個下游任務從頭開始訓練，依賴有限的幻燈片級標籤同時學習聚合機制和下游判別表示。<lb/>因此，它們經常面臨不穩定的優化、過擬合和有限的可轉移性。<lb/>類似於自然影像學習中的預訓練ResNet和視覺Transformer模型，MIL也需要可重用的預訓練初始化。<lb/>然而，高質量的幻燈片級預訓練數據仍然稀缺，且MIL模型通常輕量且弱監督，使得大規模預訓練在實踐中變得困難。<lb/>為了解決這一挑戰，我們提出了一種基於蒸餾的MIL預訓練框架，利用兩個幻燈片級基礎模型TITAN和CARE作為教師，將其表示知識轉移到多樣的MIL架構中。<lb/>為了有效平衡來自不同教師的監督，我們進一步引入了一種角度分散正規化蒸餾損失。<lb/>蒸餾後的權重隨後用作下游適應的初始化。<lb/>我們在15個基準數據集上進行了系統評估，涵蓋線性探測和全參數微調，並進一步驗證其在少量樣本場景中的優勢。<lb/>實驗結果顯示，預訓練通常能改善MIL聚合器的表現，相較於從頭訓練，特別是在線性探測和少量樣本設置中，同時保持輕量MIL模型的計算效率。<lb/>代碼可在 https://github.com/fu0201/MIL_Pretrained 獲得。

##### **SmartRAG: Native Graph-Based RAG for Mobile Device**
2607.14661v2 by Zhihan Jiang, Meng Li, Shenghao Liu, Keran Li, Ruiben Zhou, Wei Wang, Xianjun Deng, Shuai Wang, Haipeng Dai

Deploying large language models (LLMs) as personal assistants on mobile devices demands privacy, low latency, and offline availability, yet the computational cost of giant models clashes with strict edge-hardware budgets. We argue that this tension cannot be resolved by model compression alone; it requires decomposing on-device intelligence into complementary functional roles. We present SmartRAG, a fully on-device framework that organizes an intelligent assistant around four coordinated modules -- Perception, Memory, Focus, and Thinking. At the core of SmartRAG is EvoNER, a continually learnable named-entity recognizer that incrementally expands its label inventory through teacher-distilled updates, enabling the system to absorb previously unseen entity types without retraining the backbone LLM. Extracted knowledge is stored in MRGraph, a three-layer provenance-preserving knowledge graph, and retrieved at query time through a hybrid pipeline combining graph traversal, lexical matching, and dense semantic search. The on-device LLM is invoked only for high-value semantic operations -- labeling, planning, and answer synthesis -- keeping inference costs bounded. Experiments on four QA benchmarks (TriviaQA, Natural Questions, HotpotQA, MultiHopQA) show that SmartRAG with a quantized 1.7B-parameter backbone achieves multi-hop reasoning performance competitive with models up to 18$\times$ larger, while running entirely on commodity smartphones within practical memory and latency envelopes.

摘要：部署大型語言模型（LLMs）作為移動設備上的個人助理需要隱私、低延遲和離線可用性，但巨型模型的計算成本與嚴格的邊緣硬體預算相衝突。我們認為，這種緊張關係不能僅通過模型壓縮來解決；它需要將設備上的智能分解為互補的功能角色。我們提出了 SmartRAG，一個完全在設備上運行的框架，圍繞四個協調模塊組織智能助理——感知、記憶、專注和思考。SmartRAG 的核心是 EvoNER，一個持續可學習的命名實體識別器，通過教師蒸餾更新逐步擴展其標籤庫，使系統能夠在不重新訓練主幹 LLM 的情況下吸收先前未見的實體類型。提取的知識存儲在 MRGraph 中，這是一個三層保留來源的知識圖，並通過結合圖遍歷、詞彙匹配和密集語義搜索的混合管道在查詢時檢索。僅在高價值語義操作——標記、規劃和答案合成時調用設備上的 LLM，從而保持推理成本的邊界。在四個 QA 基準（TriviaQA、Natural Questions、HotpotQA、MultiHopQA）上的實驗顯示，使用量化的 1.7B 參數主幹的 SmartRAG 在多跳推理性能上與高達 18$\times$ 更大的模型具有競爭力，同時完全在商用智能手機上運行，符合實際的內存和延遲範圍。

##### **TopoAgent: A Self-Evolving Topological Agent for Multimodal Scientific Reasoning**
2607.14658v1 by Mingze Xu, Yinghui Li, Jiayi Kuang, Zhanhui Kang, Di Yin, Ying Shen, Xing Sun, Yuxing Han

While Multimodal Large Language Models (MLLMs) excel in general tasks, rigorous scientific reasoning remains challenging due to the limitations of monolithic, linear planning. Such sequential designs often suffer from visual-semantic misalignment, long-context hallucinations, and brittle execution under fixed task granularity. We propose TopoAgent, a self-evolving topological framework that replaces linear trajectories with dynamic, state-isolated graph evolution. TopoAgent first employs a front-end decomposer to fracture complex queries into visually-grounded atoms. These atoms are organized into a Directed Acyclic Graph (DAG) based on their dependencies, enabling strict context isolation to shield the reasoning engine from irrelevant historical noise. Furthermore, we introduce adaptive atomic fission, which dynamically splits bottleneck nodes into finer-grained sub-atoms at runtime when tool capability boundaries are exceeded. Extensive experiments across mathematics, physics, and chemistry benchmarks demonstrate that TopoAgent significantly outperforms state-of-the-art linear agent frameworks, providing a robust, noise-resistant, and self-correcting paradigm for autonomous scientific reasoning.

摘要：雖然多模態大型語言模型（MLLMs）在一般任務中表現出色，但由於單一、線性規劃的限制，嚴謹的科學推理仍然具有挑戰性。這種順序設計往往受到視覺-語義不對齊、長上下文幻覺和在固定任務粒度下脆弱執行的影響。我們提出了TopoAgent，一個自我演化的拓撲框架，將線性軌跡替換為動態、狀態隔離的圖形演變。TopoAgent首先使用前端分解器將複雜查詢分解為視覺基礎的原子。這些原子根據其依賴關係組織成一個有向無環圖（DAG），實現嚴格的上下文隔離，以保護推理引擎免受無關歷史噪聲的干擾。此外，我們引入了自適應原子裂變，當工具能力邊界被超越時，動態地將瓶頸節點分裂為更細粒度的子原子。廣泛的數學、物理和化學基準實驗表明，TopoAgent顯著超越了最先進的線性代理框架，提供了一種穩健、抗噪聲和自我修正的自主科學推理範式。

##### **Routing Ceilings Are Domain-Independent: Structural Prior Injection in Code Security Vulnerability Detection**
2607.14628v1 by Manuel Israel Cázares

Large language models (LLMs) exhibit a well-documented gap between latent capability and consistent activation: the router hypothesis posits that models possess the knowledge to solve a task but lack reliable internal routing to activate it. Prior work in formal mathematical reasoning (SAIR, Cázares 2026) reports that structural priors (cheatsheets) raise in-distribution performance dramatically, yet collapse below the zero-shot baseline out-of-distribution (OOD) -- and that iterative recalibration amplifies rather than corrects the collapse.
  We test whether this phenomenon is cross-domain by reproducing the SAIR design in source-code security vulnerability detection, evaluating three LLMs (GPT-OSS-120B, Llama-3.3-70B, Gemma-4-31B) across three vulnerability categories (CWE-798, CWE-284, and the non-CWE N+1 anti-pattern) spanning syntactic, contextual, and semantic complexity, then transferring cheatsheet-augmented prompts to real-world CVE data from VUDENC (CWE-89, CWE-22).
  Our findings replicate and extend SAIR: (F1) structural priors lift semantic-vulnerability recall from 20.0% to 100.0% across all models; (F2) zero-shot performance degrades along a semantic complexity gradient; (F3) the same cheatsheets that saturate synthetic performance amplify distribution-shift collapse on real CVE data (CWE-89: 100% synthetic F1 to 48.9% on VUDENC, -51.1pp); (F5) iterative recalibration produces a v2 cheatsheet that performs worse than v1 on real data, mirroring SAIR's AN45c-vs-AN38 finding.
  These results provide evidence that the cross-distribution trade-off surface documented in SAIR generalises to code security, and that the router hypothesis is cross-domain. We argue the structural nature of the collapse motivates distribution-aware training over prompt calibration. Code and evaluation scripts: https://github.com/bytepro-ai/bitcoder-v2-research

摘要：大型語言模型（LLMs）顯示出潛在能力與一致啟用之間的明顯差距：路由假說假設模型擁有解決任務的知識，但缺乏可靠的內部路由來啟用它。先前在正式數學推理（SAIR，Cázares 2026）的研究報告指出，結構性先驗（備忘單）顯著提高了分佈內的表現，但在分佈外（OOD）則低於零樣本基線，並且迭代再校準放大而非修正了崩潰。

我們通過在源代碼安全漏洞檢測中重現SAIR設計來測試這一現象是否跨領域，評估三個LLMs（GPT-OSS-120B、Llama-3.3-70B、Gemma-4-31B）在三個漏洞類別（CWE-798、CWE-284，以及非CWE的N+1反模式）上的表現，這些類別涵蓋了語法、上下文和語義的複雜性，然後將增強備忘單的提示轉移到來自VUDENC的真實CVE數據（CWE-89、CWE-22）。

我們的研究結果複製並擴展了SAIR：（F1）結構性先驗將所有模型的語義漏洞召回率從20.0%提升至100.0%；（F2）零樣本性能沿著語義複雜度梯度下降；（F3）同樣的備忘單在合成性能飽和的情況下，對真實CVE數據放大了分佈轉移崩潰（CWE-89：從100%合成F1降至48.9%在VUDENC，-51.1pp）；（F5）迭代再校準生成的v2備忘單在真實數據上的表現不如v1，與SAIR的AN45c與AN38的發現相呼應。

這些結果提供了證據，表明SAIR中記錄的跨分佈權衡面在代碼安全中具有普遍性，並且路由假說是跨領域的。我們認為崩潰的結構性特徵促使了基於分佈的訓練，而非提示校準。代碼和評估腳本：https://github.com/bytepro-ai/bitcoder-v2-research

##### **Bad Memory: Evaluating Prompt Injection Risks from Memory in Agentic Systems**
2607.14611v1 by Soham Gadgil, David Alexander, Sai Sunku, Franziska Roesner

A growing class of agentic systems maintain persistent state across sessions through memory files, behavioral preferences, and knowledge bases. While this makes agents more useful and self-improving, it also creates a new attack surface for prompt injections in which malicious instructions can be embedded within persistent files and influence future behavior. In this work, we study prompt injection attacks in memory-based agentic systems using a sandboxed synthetic workspace. We evaluate two agentic systems, Anthropic Claude Code and OpenAI Codex, across four models: Claude Haiku 4.5, Claude Opus 4.7, GPT-5.2, and GPT-5.5. Our results show that although it is difficult to make an agent overwrite its own memory files using untrusted external content, payloads already planted in those files can successfully attack current and future sessions. Attack success and payload persistence vary substantially across systems, models, adversarial goals, and multi-session attack sequences. These findings show that persistent memory changes the threat model for prompt injection and motivate defenses that protect memory updates without removing useful agent adaptation.

摘要：一類不斷增長的代理系統通過記憶檔案、行為偏好和知識庫在會話之間保持持久狀態。<lb/>雖然這使得代理更有用且能自我改善，但它也為提示注入創造了一個新的攻擊面，其中惡意指令可以嵌入持久檔案並影響未來行為。<lb/>在這項工作中，我們使用沙盒合成工作區研究基於記憶的代理系統中的提示注入攻擊。<lb/>我們評估了兩個代理系統，Anthropic Claude Code 和 OpenAI Codex，涵蓋四個模型：Claude Haiku 4.5、Claude Opus 4.7、GPT-5.2 和 GPT-5.5。<lb/>我們的結果顯示，儘管使用不受信任的外部內容使代理覆蓋其自身記憶檔案是困難的，但已經植入這些檔案中的有效載荷可以成功攻擊當前和未來的會話。<lb/>攻擊成功率和有效載荷持久性在系統、模型、對抗目標和多會話攻擊序列之間有很大差異。<lb/>這些發現顯示，持久記憶改變了提示注入的威脅模型，並促使保護記憶更新的防禦措施，而不移除有用的代理適應。

##### **MathCoPilot: An Interactive System for Human-AI Symbiotic Paradigm of Mathematical Research**
2607.14582v1 by Junjie Zhang, Jiayu Liu, Wenbin Liu, Zhenya Huang, Doudou Wang, Yan Jiang, Leiye Xu, Tao Xiong, Wen Huang, Qi Liu, Guoping Hu, Enhong Chen, Mengping Zhang, Xiangdong Ye

Existing LLM-based theorem provers have achieved impressive results on formal mathematics benchmarks, yet they remain confined to acting as autonomous agents that prove a stated proposition. In this paper, we propose MathCoPilot, a human-in-the-loop system that embodies a new human--AI symbiotic paradigm for mathematical research, in which the mathematician steers the high-level mathematical direction while AI agents carry out the detailed formalization and proof work under continuous human guidance. MathCoPilot unifies three core capabilities: (1) an interactive workbench where the mathematician and AI agents collaborate through a living proof blueprint that decomposes a proof into navigable steps the human can directly inspect, direct, and refine; (2) automated proving skill orchestration with adaptive knowledge base search and Lean-integrated iterative verification; and (3) topic-driven paper retrieval and automated formalization into a verified Lean knowledge base. Using MathCoPilot, we systematically compare four state-of-the-art LLMs, including Gemini~3.1~Pro, GPT-5.4, and Claude~Opus~4.7, on a FormalMATH subset and on two real PDE theorems requiring deep domain expertise, evaluating their ability to produce verified Lean~4 proofs and to identify errors in deliberately incorrect proofs. Our results show that while current models can handle undergraduate-level problems with high success rates under favorable autoformalization conditions, substantial challenges remain for domain-specific theorems requiring genuine mathematical understanding.

摘要：現有的基於LLM的定理證明器在正式數學基準上取得了令人印象深刻的成果，然而它們仍然僅限於作為自主代理來證明所陳述的命題。<lb/>在本文中，我們提出了MathCoPilot，一個人機協作系統，體現了一種新的人類與AI共生範式，用於數學研究，其中數學家引導高層次的數學方向，而AI代理在持續的人類指導下進行詳細的形式化和證明工作。<lb/>MathCoPilot統一了三個核心能力：(1) 一個互動工作台，數學家和AI代理通過一個活的證明藍圖進行合作，該藍圖將證明分解為可導航的步驟，供人類直接檢查、指導和完善；(2) 自動證明技能協調，具有自適應知識庫搜索和Lean集成的迭代驗證；以及(3) 以主題驅動的論文檢索和自動形式化到經過驗證的Lean知識庫。<lb/>使用MathCoPilot，我們系統地比較了四個最先進的LLM，包括Gemini~3.1~Pro、GPT-5.4和Claude~Opus~4.7，在FormalMATH子集以及兩個需要深厚領域專業知識的實際PDE定理上，評估它們生成經過驗證的Lean~4證明和識別故意錯誤證明中錯誤的能力。<lb/>我們的結果顯示，儘管當前模型在有利的自動形式化條件下能夠高成功率地處理本科水平的問題，但對於需要真正數學理解的領域特定定理仍然存在重大挑戰。

##### **MARS: Multi-hop Adaptive Retrieval and SPARQL Generation for KGQA**
2607.14561v1 by Nikit Srivastava, Daniel Vollmers, René Speck, Nikolaos Karalis, Hamada M. Zahera, Axel-Cyrille Ngonga Ngomo

Large language models (LLMs) have demonstrated strong reasoning performance, but their tendency to hallucinate limits their reliability in knowledge-intensive tasks requiring up-to-date and grounded information. Combining knowledge graphs (KGs) with LLMs facilitates the use of explicit symbolic knowledge that can be continuously updated without costly fine-tuning, while benefiting from rapidly advancing LLM reasoning. We propose MARS, a scalable knowledge graph question answering (KGQA) approach that requires no model fine-tuning. Rather than relying on open-ended agentic exploration, MARS performs a structured retrieval procedure that links question entities to the KG and iteratively retrieves relevant next-hop information. At each step, MARS decides whether to continue graph traversal or to generate the final SPARQL query, allowing the model to adapt the retrieval depth to the question while keeping the overall pipeline more predictable than fully agentic approaches. We evaluate MARS on three established KGQA benchmarks across several LLMs and settings, including multilingual evaluation, and provide insights through ablation studies and error analysis. Our approach achieves competitive performance relative to state-of-the-art methods while remaining efficient and scalable. The evaluation results, code and resources are publicly available: https://github.com/dice-group/mars-kgqa.

摘要：大型語言模型（LLMs）展現了強大的推理能力，但它們的幻覺傾向限制了它們在需要最新和基礎信息的知識密集型任務中的可靠性。將知識圖（KGs）與LLMs結合，可以利用明確的符號知識，這些知識可以不需要昂貴的微調而持續更新，同時受益於迅速進步的LLM推理。我們提出了MARS，一種可擴展的知識圖問題回答（KGQA）方法，無需模型微調。MARS不依賴於開放式的自主探索，而是執行結構化的檢索程序，將問題實體連結到KG並迭代檢索相關的下一步信息。在每一步中，MARS決定是繼續圖形遍歷還是生成最終的SPARQL查詢，這使得模型能夠根據問題調整檢索深度，同時使整體流程比完全自主的方法更可預測。我們在三個已建立的KGQA基準上評估MARS，涵蓋多個LLMs和設置，包括多語言評估，並通過消融研究和錯誤分析提供見解。我們的方法在相對於最先進的方法中達到了具有競爭力的性能，同時保持高效和可擴展性。評估結果、代碼和資源均可公開獲得：https://github.com/dice-group/mars-kgqa。

##### **CityLLM: A framework for natural-language querying of semantic 3D city models**
2607.14542v1 by Rabindra Lamsal, Sisi Zlatanova, Johnson Xuesong Shen

Semantic 3D city models provide rich geometric and semantic information, but remain challenging for non-experts and interdisciplinary researchers to access and query due to their complex structures and specialized data formats. To address this issue, we present CityLLM, a framework for natural-language querying of semantic 3D city models alongside complementary urban datasets. The framework combines spatial and graph databases within an LLM-based workflow that supports iterative query refinement and cross-database chaining. We evaluate CityLLM on a CityJSON dataset of Rotterdam (853 LoD2 buildings) using GPT-OSS, Gemini 3.1, and GPT-5.4, along with selected variants, across multiple metrics: answer correctness, visualization correctness, query success, and retry attempts. A total of 54 natural-language queries are curated across four scenarios: spatial, graph, cross-database, and conversational. Results show strong overall performance, with answer correctness ranging from 85.2% to 100%, visualization correctness from 92.9% to 100%, a 100% query success rate, and fewer than three retries across all 54 queries. Overall, the findings suggest that CityLLM provides a lightweight and extensible approach for conversational access to semantic 3D city data.

摘要：語意3D城市模型提供豐富的幾何和語意資訊，但由於其複雜的結構和專門的數據格式，對於非專家和跨學科研究者來說，仍然難以訪問和查詢。為了解決這個問題，我們提出了CityLLM，一個用於自然語言查詢語意3D城市模型及其補充城市數據集的框架。該框架結合了空間和圖形數據庫，並在基於LLM的工作流程中支持迭代查詢細化和跨數據庫鏈接。我們在鹿特丹的CityJSON數據集（853個LoD2建築）上評估了CityLLM，使用GPT-OSS、Gemini 3.1和GPT-5.4，以及選定的變體，針對多個指標進行評估：答案正確性、可視化正確性、查詢成功率和重試次數。共策劃了54個自然語言查詢，涵蓋四種場景：空間、圖形、跨數據庫和對話。結果顯示整體表現良好，答案正確性範圍從85.2%到100%，可視化正確性從92.9%到100%，查詢成功率為100%，54個查詢的重試次數少於三次。總體而言，研究結果表明CityLLM提供了一種輕量且可擴展的方法，用於對語意3D城市數據的對話訪問。

##### **Are LLM-Generated GPU Kernels Production-Ready? A Trace-Driven Benchmark and Optimization Agent**
2607.14541v1 by Lingyun Yang, Yuxiao Wang, Shenghao Liang, Linfeng Yang, Daocheng Ying, Chunbo You, Rui Zhang, Luping Wang, Yinghao Yu, Guodong Yang, Liping Zhang

Existing GPU kernel generation benchmarks draw problems from synthetic or curated sources that diverge from deployed workloads. We present Atrex-Bench, a benchmark whose 30 operators and 440 shapes are sampled directly from full-cluster production inference traces of compute-limited, memory-rich GPUs. Each problem carries an importance weight derived from its share of observed GPU time, weighted by application card-hours and computed separately for the serving phases in which it runs, together with a per-problem roofline ceiling, so the aggregate score emphasizes the kernels that consume the most serving time. Evaluating six frontier coding agents on Atrex-Bench shows that even the best vanilla model reaches only ${\sim}10\%$ of the hardware roofline on production operators; and correctness alone overstates capability, since much of the apparent pass rate comes from PyTorch fallbacks rather than kernels the model wrote. To close this gap, we co-release Atrex-Kernel-Agent (AKA), a profile-driven kernel-optimization agent that combines iterative measure-revise search, optimization dropout for escaping stalled search contexts, and a layered GPU-optimization knowledge base (298 reference-kernel files and 244 optimization-knowledge documents, plus external upstream reference projects for API/ISA lookup). In a controlled case study, the agent converts zero-FlyDSL fallbacks into real kernels that match or exceed hand-tuned production baselines.

摘要：現有的 GPU 核心生成基準測試從合成或策劃的來源中提取問題，這些來源與實際部署的工作負載有所不同。<lb/>我們提出了 Atrex-Bench，這是一個基準測試，其 30 個運算子和 440 種形狀直接從計算受限、內存豐富的 GPU 的全集群生產推理痕跡中抽樣而來。<lb/>每個問題都帶有一個重要性權重，該權重源自其觀察到的 GPU 時間的份額，按應用卡小時加權，並分別計算在其運行的服務階段中，還有每個問題的屋頂線上限，因此總分強調那些消耗最多服務時間的核心。<lb/>在 Atrex-Bench 上評估六個前沿編碼代理顯示，即使是最好的原生模型在生產運算子上也僅達到約 10% 的硬件屋頂線；而僅僅正確性則高估了能力，因為許多表面上的通過率來自 PyTorch 的回退，而非模型所編寫的核心。<lb/>為了縮小這一差距，我們共同發布了 Atrex-Kernel-Agent (AKA)，這是一個基於配置文件的核心優化代理，結合了迭代測量-修訂搜索、優化放棄以逃避停滯的搜索上下文，以及一個分層的 GPU 優化知識庫（298 個參考核心文件和 244 個優化知識文檔，加上 API/ISA 查找的外部上游參考項目）。<lb/>在一個受控案例研究中，該代理將零 FlyDSL 回退轉換為實際核心，這些核心與手動調整的生產基準相匹配或超過。

##### **VTM-Nav: Hierarchical Visual-Topological Memory for Cross-Episode Object-Goal Navigation**
2607.14514v1 by Xiaoran Xu, Yupeng Wu, Tianyu Xue, Yifan Xu, Xuanran Dong, Xiaoshan Yang, Changsheng Xu

Object-goal navigation requires an embodied agent to locate and reach an instance of a specified object category in an indoor environment. Recent training-free approaches leverage vision-language models (VLMs) for open-vocabulary semantic reasoning, but are typically evaluated under an episodic protocol that resets all scene-specific state after each episode. We introduce Cross-Episode Object-Goal Navigation, in which an agent repeatedly operates in the same scene, retains only self-acquired experience, and keeps its model parameters fixed. To support experience reuse, we present \method, a training-free VLM navigation framework with a persistent hierarchical Visual-Topological Memory (VTM). The VTM organizes scene knowledge at room and object levels and retrieves relevant experience through coarse-to-fine matching, providing memory as soft guidance only when it agrees with current observations. A conservative execution guard further mitigates oscillations, blocked motions, and premature stopping. Under a controlled same-scene protocol, we evaluate \method{} on three benchmarks, HM3D v0.1, HM3D v0.2, and MP3D, and compare it with a strengthened WMNav baseline augmented with cross-episode textual memory, while keeping the VLM backbone and action pipeline identical. \method{} achieves the best performance across all three benchmarks, demonstrating the effectiveness and robustness of structured visual-topological experience reuse across datasets.

摘要：物體目標導航需要一個具身的代理在室內環境中定位並到達指定物體類別的實例。最近的無需訓練的方法利用視覺-語言模型（VLMs）進行開放詞彙的語義推理，但通常在一種情節協議下進行評估，該協議在每個情節後重置所有場景特定狀態。我們引入了跨情節物體目標導航，其中代理在同一場景中重複操作，只保留自我獲得的經驗，並保持其模型參數不變。為了支持經驗重用，我們提出了\method，一個無需訓練的VLM導航框架，具有持久的層次視覺-拓撲記憶（VTM）。VTM在房間和物體層級組織場景知識，並通過粗到細的匹配檢索相關經驗，僅在與當前觀察一致時提供作為軟指導的記憶。一個保守的執行保護進一步減輕了振盪、阻塞運動和過早停止。在受控的同場景協議下，我們在三個基準上評估\method{}，分別是HM3D v0.1、HM3D v0.2和MP3D，並將其與增強的WMNav基線進行比較，該基線增強了跨情節的文本記憶，同時保持VLM骨幹和行動管道相同。\method{}在所有三個基準上達到了最佳性能，展示了結構化視覺-拓撲經驗重用在不同數據集中的有效性和穩健性。

##### **RetroAgent: Harnessing LLMs to Search Over Structured Memory for Agentic Retrosynthesis Planning**
2607.14512v1 by Yanqiao Zhu, Jingru Gan, Xiaoqi Sun, Fang Sun, Yidan Shi, Md Mofijul Islam, Chao Shang, Wenhao Gao, Connor W. Coley, Yizhou Sun, Wei Wang

Multi-step retrosynthesis planning seeks to decompose a target molecule into commercially available building blocks through a sequence of feasible reactions. The vast combinatorial search space makes this task challenging even for expert chemists. Traditional methods combine tree search with offline-trained value networks that score candidates in isolation, without reasoning about complete multi-step routes. Recent work leverages Large Language Models (LLMs) for this task, but relies on simple interfaces that limit exploration of the full search space. We introduce RetroAgent, an LLM agent that bridges symbolic search and neural reasoning through a harness with structured memory. Through memory and chemistry tools, the agent observes the full search state, including explored routes, available alternatives, and properties of intermediates, enabling informed decisions grounded in both global progress and domain knowledge. Experiments on in-distribution and out-of-distribution benchmarks demonstrate that RetroAgent delivers strong performance and generalization.

摘要：多步逆合成規劃旨在通過一系列可行的反應將目標分子分解為商業可用的構建塊。龐大的組合搜索空間使這項任務對專家化學家來說也充滿挑戰。傳統方法將樹搜索與離線訓練的價值網絡結合，這些網絡在孤立的情況下評分候選者，而不考慮完整的多步路徑。最近的研究利用大型語言模型（LLMs）來處理這項任務，但依賴於簡單的介面，限制了對完整搜索空間的探索。我們介紹了RetroAgent，一個通過結構化記憶的裝置將符號搜索和神經推理相結合的LLM代理。通過記憶和化學工具，該代理觀察完整的搜索狀態，包括已探索的路徑、可用的替代方案和中間體的性質，使其能夠基於全球進展和領域知識做出明智的決策。在分佈內和分佈外基準上的實驗表明，RetroAgent展現了強大的性能和泛化能力。

##### **VLT: A Vision-Language-Time Series Multimodal Foundation Model for Industrial Intelligence**
2607.14510v1 by Haiteng Wang, Jingheng Yan, Xiaokang Wang, Lei Ren

Industrial time series serve as the foundation for Prognostics and Health Management (PHM) to ensure the reliability and safety of industrial equipment such as aero-engines. However, existing approaches are typically limited to single-modality modeling, which restricts their generalization in complex scenarios. Although recent advances in large language models (LLMs) provide new opportunities for multimodal learning, bridging continuous time-series signals and discrete textual semantics remains an open challenge. To this end, we propose VLT, a multimodal foundation model that jointly models time-series, frequency-spectrum visual representations, and textual knowledge. A key insight is to utilize the frequency spectrum as a visual bridge to connect continuous temporal signals with discrete semantics. Specifically, a Time-aware Mixture-of-Experts (Time-MoE) is designed to capture heterogeneous temporal dynamics, while a Frequency-Text Augmented Learner enables joint modeling of spectral and semantic features within a shared representation space. Furthermore, a time-centric gradient alignment mechanism is introduced to mitigate cross-modal optimization conflicts via gradient normalization and reliability-aware dynamic reweighting. Extensive experiments on multiple industrial datasets demonstrate that VLT outperforms state-of-the-art methods, achieving superior robustness and generalization under few-shot, noisy, and incomplete-modality settings.

摘要：工業時間序列作為預測與健康管理（PHM）的基礎，以確保航空發動機等工業設備的可靠性和安全性。然而，現有的方法通常僅限於單一模態建模，這限制了它們在複雜場景中的泛化能力。儘管近期大型語言模型（LLMs）的進展為多模態學習提供了新的機會，但將連續時間序列信號與離散文本語義橋接仍然是一個未解決的挑戰。為此，我們提出了VLT，一種多模態基礎模型，該模型共同建模時間序列、頻譜視覺表示和文本知識。一個關鍵的見解是利用頻譜作為視覺橋樑，將連續時間信號與離散語義連接起來。具體而言，設計了一種時間感知的專家混合模型（Time-MoE），以捕捉異質的時間動態，而頻譜-文本增強學習器則使得在共享表示空間內共同建模光譜和語義特徵成為可能。此外，引入了一種以時間為中心的梯度對齊機制，通過梯度正規化和可靠性感知的動態重加權來減輕跨模態優化衝突。在多個工業數據集上的廣泛實驗表明，VLT在少樣本、噪聲和不完整模態設置下表現超越了最先進的方法，實現了更卓越的穩健性和泛化能力。

##### **Non-vacuous Generalization Bounds for Reinforcement Learning with Verifiable Rewards**
2607.14506v1 by Yuxuan Zhu, Rohan Alur, Daniel Kang

While reinforcement learning with verifiable rewards (RLVR) is widely used to improve the reasoning capabilities of large language models (LLMs), the generalizability of the resulting models remains poorly understood. In this work, we establish the first non-vacuous generalization bounds for parameter-efficient RLVR fine-tuning at the billion-parameter scale. Our approach adapts PAC-Bayes compression bounds to this setting, and addresses the inherent stochasticity of token generation by applying the Gumbel-max reparameterization trick. To operationalize these bounds, we propose the Progressive RLVR framework, which integrates RLVR with on-policy distillation, TinyLoRA, and model quantization. Progressive RLVR empirically retains 84-97% performance of standard LoRA fine-tuning while producing models that are 14,796x more compressible. We show that this framework yields non-vacuous generalization bounds in four domains: mathematical problem-solving, programming, general-knowledge reasoning, and Text-to-SQL. Our bounds exceed the accuracy of the base model by 9-51% and lie within 6-11% of the accuracy of the fine-tuned models.

摘要：雖然使用可驗證獎勵的強化學習（RLVR）被廣泛應用於提升大型語言模型（LLMs）的推理能力，但所得到模型的可泛化性仍然不甚了解。<lb/>在這項工作中，我們建立了第一個針對十億參數規模的參數高效RLVR微調的非空泛化界限。<lb/>我們的方法將PAC-Bayes壓縮界限適應於此設定，並通過應用Gumbel-max重新參數化技巧來解決令牌生成的固有隨機性。<lb/>為了使這些界限可操作化，我們提出了漸進式RLVR框架，該框架將RLVR與在政策蒸餾、TinyLoRA和模型量化相結合。<lb/>漸進式RLVR在實證中保留了標準LoRA微調的84-97%性能，同時產生的模型可壓縮性提高了14,796倍。<lb/>我們顯示這個框架在四個領域產生了非空的泛化界限：數學問題解決、編程、常識推理和文本到SQL。<lb/>我們的界限超過了基礎模型9-51%的準確率，並且在微調模型的準確率範圍內的6-11%。

