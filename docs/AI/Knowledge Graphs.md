
### Knowledge Graphs
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-06-10**|**Doc-to-Atom: Learning to Compile and Compose Memory Atoms**|Xingjian Diao et.al.|[2606.12400v1](http://arxiv.org/abs/2606.12400v1)|null|
|**2026-06-10**|**Which Models Are Our Models Built On? Auditing Invisible Dependencies in Modern LLMs**|Sanjay Adhikesaven et.al.|[2606.12385v1](http://arxiv.org/abs/2606.12385v1)|null|
|**2026-06-10**|**CCKS: Consensus-based Communication and Knowledge Sharing**|Jinyuan Zu et.al.|[2606.12281v1](http://arxiv.org/abs/2606.12281v1)|null|
|**2026-06-10**|**The Impossibility of Eliciting Latent Knowledge**|Korbinian Friedl et.al.|[2606.12268v1](http://arxiv.org/abs/2606.12268v1)|null|
|**2026-06-10**|**Adaptive Multi-Resolution Procedural Knowledge Compression for Large Language Models**|Changyue Wang et.al.|[2606.12203v1](http://arxiv.org/abs/2606.12203v1)|null|
|**2026-06-10**|**OpenMedReason: Scientific Reasoning Supervision for Medical Vision-Language Models**|Negin Baghbanzadeh et.al.|[2606.12169v1](http://arxiv.org/abs/2606.12169v1)|null|
|**2026-06-10**|**Soft-Prompt Tuning for Fair and Efficient LLM Benchmark Evaluation**|Selen Erkan et.al.|[2606.12117v1](http://arxiv.org/abs/2606.12117v1)|null|
|**2026-06-10**|**MSUE: Multi-Modal Soccer Understanding Expert**|Litao Li et.al.|[2606.12106v1](http://arxiv.org/abs/2606.12106v1)|null|
|**2026-06-10**|**FORT-Searcher: Synthesizing Shortcut-Resistant Search Tasks for Training Deep Search Agents**|Jia Deng et.al.|[2606.12087v1](http://arxiv.org/abs/2606.12087v1)|null|
|**2026-06-10**|**IntElicit: Eliciting and Assessing Contextualized Creativity via Dialogue Policy Optimization**|Mingjia Li et.al.|[2606.12086v1](http://arxiv.org/abs/2606.12086v1)|null|
|**2026-06-10**|**Automating Geometry-Intensive Compliance Checking in BIM: Graph-Based Semantic Reasoning Framework**|Zixuan Xiao et.al.|[2606.12065v1](http://arxiv.org/abs/2606.12065v1)|null|
|**2026-06-10**|**MODF-SIR: A Multi-agent Omni-modal Distilled Framework for Social Intelligence Reasoning**|Shang Ma et.al.|[2606.12018v1](http://arxiv.org/abs/2606.12018v1)|null|
|**2026-06-10**|**The Art of Interrogation: Consistency Amplifies Factuality in Spatial Reasoning**|Theo Uscidda et.al.|[2606.11918v1](http://arxiv.org/abs/2606.11918v1)|null|
|**2026-06-10**|**GraspLLM: Towards Zero-Shot Generalization on Text-Attributed Graphs with LLMs**|Hengyi Feng et.al.|[2606.11898v1](http://arxiv.org/abs/2606.11898v1)|null|
|**2026-06-10**|**Fine-tuning Multi-modal LLMs with ART: Art-based Reinforcement Training**|Michal Chudoba et.al.|[2606.11854v1](http://arxiv.org/abs/2606.11854v1)|null|
|**2026-06-10**|**Task-Aware Structured Memory for Dynamic Multi-modal In-Context Learning**|Zhirui Chen et.al.|[2606.11853v1](http://arxiv.org/abs/2606.11853v1)|null|
|**2026-06-10**|**From Uniform to Learned Graph Priors: Diffusion for Structure Discovery**|Qi Shao et.al.|[2606.11831v1](http://arxiv.org/abs/2606.11831v1)|null|
|**2026-06-10**|**WorldReasoner: Evaluating Whether Language Model Agents Forecast Events with Valid Reasoning**|Yizhou Chi et.al.|[2606.11816v1](http://arxiv.org/abs/2606.11816v1)|null|
|**2026-06-10**|**Fast Speech Foundation Model Distillation Using Interleaved Stacking**|Eungbeom Kim et.al.|[2606.11766v1](http://arxiv.org/abs/2606.11766v1)|null|
|**2026-06-10**|**From Prompts to Tokens: Internalizing Causal Supervision in Vision-Language Model for Multi-Image Causal Reasoning**|Haoping Yu et.al.|[2606.11745v1](http://arxiv.org/abs/2606.11745v1)|null|
|**2026-06-10**|**Hey Chat, Can You Teach Me? Structuring Socratic Dialogue for Human Learning in the Wild**|Sidney Tio et.al.|[2606.11744v1](http://arxiv.org/abs/2606.11744v1)|null|
|**2026-06-10**|**T2S: A Rehearsal-Based Approach for Extraction-Resistant Model Watermarking**|Jian-Ping Mei et.al.|[2606.11698v1](http://arxiv.org/abs/2606.11698v1)|null|
|**2026-06-10**|**Can AI Reason Like an Urban Planner? Benchmarking Large Language Models Against Professional Judgment**|Yijie Deng et.al.|[2606.11678v1](http://arxiv.org/abs/2606.11678v1)|null|
|**2026-06-10**|**Lung-R1: A Knowledge Graph-Guided LLM for Pulmonary Diagnostic Reasoning**|Haoyang Zeng et.al.|[2606.11675v1](http://arxiv.org/abs/2606.11675v1)|null|
|**2026-06-10**|**TAROT: Task-Adaptive Refinement of LLM-prior Graphs for Few-shot Tabular Learning**|Ruxue Shi et.al.|[2606.11640v1](http://arxiv.org/abs/2606.11640v1)|null|
|**2026-06-10**|**Physics-Distilled Neural Network enabled by Large Language Models for Manufacturing Process-Property Predictive Modeling**|Ge Song et.al.|[2606.11605v1](http://arxiv.org/abs/2606.11605v1)|null|
|**2026-06-10**|**Model-Based and Data-Driven Hierarchical Control and Topology Co-Design for Robust Networked Systems**|Shirantha Welikala et.al.|[2606.11596v1](http://arxiv.org/abs/2606.11596v1)|null|
|**2026-06-10**|**GraphInfer-Bench: Benchmarking LLM's Inference Capability on Graphs**|Zhuoyi Peng et.al.|[2606.11562v1](http://arxiv.org/abs/2606.11562v1)|null|
|**2026-06-10**|**LLMs+Graphs: Toward Graph-Native, Synergistic AI Systems**|Arijit Khan et.al.|[2606.11560v1](http://arxiv.org/abs/2606.11560v1)|null|
|**2026-06-10**|**Privacy-Preserving Federated Autoencoder for ECG Anomaly Detection on Edge Devices**|Kaan Arda Akyol et.al.|[2606.11556v1](http://arxiv.org/abs/2606.11556v1)|null|
|**2026-06-10**|**SkillJuror: Measuring How Agent Skill Organization Changes Runtime Behavior**|Zhiyu Chen et.al.|[2606.11543v1](http://arxiv.org/abs/2606.11543v1)|null|
|**2026-06-09**|**Hubs or Fringes: Pretraining Data Selection via Web Graph Centrality**|Vedant Badoni et.al.|[2606.11499v1](http://arxiv.org/abs/2606.11499v1)|null|
|**2026-06-09**|**Building Social World Models with Large Language Models**|Haofei Yu et.al.|[2606.11482v1](http://arxiv.org/abs/2606.11482v1)|null|
|**2026-06-09**|**INFRAMIND: Infrastructure-Aware Multi-Agent Orchestration**|Ahasan Kabir et.al.|[2606.11440v1](http://arxiv.org/abs/2606.11440v1)|null|
|**2026-06-09**|**Towards a Bridge Layer Between Bibliographic and Formalized Mathematical Knowledge**|A. Mayeux et.al.|[2606.11430v1](http://arxiv.org/abs/2606.11430v1)|null|
|**2026-06-09**|**A PubMed-Scale Dataset of Structured Biomedical Abstracts**|Chia-Hsuan Chang et.al.|[2606.11361v1](http://arxiv.org/abs/2606.11361v1)|null|
|**2026-06-09**|**Quantized Stochastic Primal-Dual Methods for Distributed Optimization under Relaxed Global Geometry**|Susmit Sarkar et.al.|[2606.11339v1](http://arxiv.org/abs/2606.11339v1)|null|
|**2026-06-09**|**A Unifying Lens on Supervised Fine-Tuning Through Target Distribution Design**|Tong Xie et.al.|[2606.11189v1](http://arxiv.org/abs/2606.11189v1)|null|
|**2026-06-09**|**FlowBank: Query-Adaptive Agentic Workflows Optimization through Precompute-and-Reuse**|Lingzhi Yuan et.al.|[2606.11290v1](http://arxiv.org/abs/2606.11290v1)|null|
|**2026-06-09**|**Flaws in the LLM Automation Narrative**|George Perrett et.al.|[2606.11166v1](http://arxiv.org/abs/2606.11166v1)|null|
|**2026-06-09**|**ABC-Bench: An Agentic Bio-Capabilities Benchmark for Biosecurity**|Andrew Bo Liu et.al.|[2606.11150v1](http://arxiv.org/abs/2606.11150v1)|null|
|**2026-06-09**|**Towards Autonomous Accelerator Design: FPGA Accelerator Generation with SECDA**|Vinamra Sharma et.al.|[2606.11117v1](http://arxiv.org/abs/2606.11117v1)|null|
|**2026-06-09**|**FADA: Accessible fetal ultrasound interpretation and annotation with a selectively distilled unified vision-language model**|Mahmood Alzubaidi et.al.|[2606.11106v1](http://arxiv.org/abs/2606.11106v1)|null|
|**2026-06-09**|**PhantomBench: Benchmarking the Non-existential Threat of Language Models**|Haeji Jung et.al.|[2606.11105v1](http://arxiv.org/abs/2606.11105v1)|null|
|**2026-06-09**|**Null-Space Constrained Low-Rank Adaptation for Response-Specified Large Language Model Unlearning**|Bocheng Ju et.al.|[2606.10989v1](http://arxiv.org/abs/2606.10989v1)|null|
|**2026-06-09**|**Provenance Tracking in AI Compilers through the Lens of Coalgebra**|Zilu Tian et.al.|[2606.10937v1](http://arxiv.org/abs/2606.10937v1)|null|
|**2026-06-09**|**Trace Only What You Need: Structure-Aware On-Demand Hypergraph Memory for Long-Document Question Answering**|Xiangjun Zai et.al.|[2606.10921v1](http://arxiv.org/abs/2606.10921v1)|null|
|**2026-06-09**|**Large-scale semantic mapping of learner agency and autonomy reveals what measurement and generative AI research overlook**|Fei Qin et.al.|[2606.10881v1](http://arxiv.org/abs/2606.10881v1)|null|
|**2026-06-09**|**Pushing the Limits of LLM Tool Calling via Experiential Knowledge Integration and Activation**|Yupu Hao et.al.|[2606.10875v1](http://arxiv.org/abs/2606.10875v1)|null|
|**2026-06-09**|**Attention-Discounted Adaptive Sampler for Masked Diffusion Language Models**|Yusuf Sahin et.al.|[2606.10829v1](http://arxiv.org/abs/2606.10829v1)|null|
|**2026-06-09**|**Earth-OneVision: Extending Remote Sensing Multimodal Large Language Models to More Sensor Modalities and Tasks**|Miaoxin Cai et.al.|[2606.10819v1](http://arxiv.org/abs/2606.10819v1)|null|
|**2026-06-09**|**Boosting ECG Classification Performance by Pre-training with Synthesized Data**|Naoki Nonaka et.al.|[2606.10802v1](http://arxiv.org/abs/2606.10802v1)|null|
|**2026-06-09**|**A Bayesian Network Approach for Enhancing Security-Focused Decision Support Systems**|Carolina Fernández-Martínez et.al.|[2606.10782v1](http://arxiv.org/abs/2606.10782v1)|null|
|**2026-06-09**|**Recovering the Zipfian Distribution in Unsupervised Term Discovery**|Danel Slabbert et.al.|[2606.10781v1](http://arxiv.org/abs/2606.10781v1)|null|
|**2026-06-09**|**Detecting Knowledge Gaps from Conversational AI Interactions Using Curriculum Prerequisite Graphs**|Youssef Medhat et.al.|[2606.10736v1](http://arxiv.org/abs/2606.10736v1)|null|
|**2026-06-09**|**REAL: A Reasoning-Enhanced Graph Framework for Long-Term Memory Management of LLMs**|Keer Lu et.al.|[2606.10694v1](http://arxiv.org/abs/2606.10694v1)|null|
|**2026-06-09**|**Divide and Cooperate: Role-Decomposed Multi-Agent LLM Training with Cross-Agent Learning Signals**|Jaewan Park et.al.|[2606.10684v1](http://arxiv.org/abs/2606.10684v1)|null|
|**2026-06-09**|**Are We Evaluating Knowledge or Phrasing? Mitigating MCQA Sensitivity with ParaEval**|João Maria Janeiro et.al.|[2606.10657v1](http://arxiv.org/abs/2606.10657v1)|null|
|**2026-06-09**|**How Does Reasoning Flow? Tracing Attention-Induced Information Flow for Targeted RL in LLMs**|Zhichen Dong et.al.|[2606.10646v1](http://arxiv.org/abs/2606.10646v1)|null|
|**2026-06-09**|**Causal Ensemble Agent: Hierarchical Causal Discovery with LLM-guided Expert Reweighting**|Xinyu Li et.al.|[2606.10607v1](http://arxiv.org/abs/2606.10607v1)|null|
|**2026-06-09**|**From Data Heterogeneity to Convergence: A Data-Centric Review of Federated Learning**|Huong Nguyen et.al.|[2606.10595v1](http://arxiv.org/abs/2606.10595v1)|null|
|**2026-06-09**|**Benchmarking Knowledge Editing using Logical Rules**|Tatiana Moteu Ngoli et.al.|[2606.10554v1](http://arxiv.org/abs/2606.10554v1)|null|
|**2026-06-09**|**Representation-Aware Advantage Estimation: Your Reward Model Provides More Than A Scalar Output**|Guozheng Li et.al.|[2606.10528v1](http://arxiv.org/abs/2606.10528v1)|null|
|**2026-06-09**|**Cross-Modal Knowledge Distillation without Paired Data: Theoretical Foundation and Algorithm**|Trong Khiem Tran et.al.|[2606.10504v1](http://arxiv.org/abs/2606.10504v1)|null|
|**2026-06-09**|**MoE Enhanced Federated Learning for Spatiotemporal Prediction**|Zhehao Dai et.al.|[2606.10499v1](http://arxiv.org/abs/2606.10499v1)|null|
|**2026-06-09**|**Decoupling Thought from Speech: Knowledge-Grounded Counterfactual Reasoning for Resilient Multi-Agent Argumentation**|Jakub Masłowski et.al.|[2606.10475v1](http://arxiv.org/abs/2606.10475v1)|null|
|**2026-06-09**|**Detecting Speculative Language in Biomedical Texts using Recurrent Neural Tensor Networks**|Dhruv Dixit et.al.|[2606.10471v1](http://arxiv.org/abs/2606.10471v1)|null|
|**2026-06-09**|**ERAlign: Energy-based Representation Alignment of GNNs and LLMs on Text-attributed Graphs**|Xianlin Zeng et.al.|[2606.10461v1](http://arxiv.org/abs/2606.10461v1)|null|
|**2026-06-09**|**SpenseGPT: Practical One-shot Pruning Enabling Sparse and Dense GEMMs for LLM Inference**|Jaeseong Lee et.al.|[2606.10445v1](http://arxiv.org/abs/2606.10445v1)|null|
|**2026-06-09**|**Vision-Assisted Foundation Model for Solving Multi-Task Vehicle Routing Problems**|Shuangchun Gui et.al.|[2606.10431v1](http://arxiv.org/abs/2606.10431v1)|null|
|**2026-06-09**|**Which LoRA? An Empirical Study on the Effectiveness of LoRA Techniques During Multilingual Instruction Tuning**|Thamali Wijewardhana et.al.|[2606.10428v1](http://arxiv.org/abs/2606.10428v1)|null|
|**2026-06-09**|**When Poison Fails After Retrieval: Revisiting Corpus Poisoning under Chunking and Reranking Pipelines**|Xi Nie et.al.|[2606.11265v1](http://arxiv.org/abs/2606.11265v1)|null|
|**2026-06-09**|**FOGO: Forgetting-aware Orthogonalization Optimizer**|Toan Nguyen et.al.|[2606.10406v1](http://arxiv.org/abs/2606.10406v1)|null|
|**2026-06-09**|**Do Vision-Language Models See or Guess? Measuring and Reducing Textual-Prior Reliance with a Phrasing-Controlled Benchmark**|Pratham Singla et.al.|[2606.10400v1](http://arxiv.org/abs/2606.10400v1)|null|
|**2026-06-09**|**Instruction Finetuning DeepSeek-R1-8B Model Using LoRA and NEFTune**|Wu Yuerong et.al.|[2606.10392v1](http://arxiv.org/abs/2606.10392v1)|null|
|**2026-06-09**|**PADD: Path-Aligned Decompression Distillation for Non-Router Teacher to Guide MoE Student Learning**|Xinyue Peng et.al.|[2606.10369v1](http://arxiv.org/abs/2606.10369v1)|null|
|**2026-06-09**|**ReflectiChain: Epistemic Grounding in LLM-Driven World Models for Supply Chain Resilience**|Jia Luo et.al.|[2606.10359v1](http://arxiv.org/abs/2606.10359v1)|null|
|**2026-06-09**|**KG-SoftMAP: Soft Knowledge-Graph Priors for Bayesian Network Structure Learning from Sparse Discrete Data**|Guoliang Xu et.al.|[2606.10358v1](http://arxiv.org/abs/2606.10358v1)|null|
|**2026-06-09**|**Content-Induced Spatial-Spectral Aggregation Network for Change Detection in Remote Sensing Images**|Yunlong Liu et.al.|[2606.10328v1](http://arxiv.org/abs/2606.10328v1)|null|
|**2026-06-09**|**From Context-Aware to Conflict-Aware: Generalizing Contrastive Decoding for Knowledge Conflict in LLMs**|Runze Jiang et.al.|[2606.10298v1](http://arxiv.org/abs/2606.10298v1)|null|
|**2026-06-09**|**When Metrics Disagree: A Meta-Analysis of Knowledge-Graph-Completion Model Benchmarking**|Haji Gul et.al.|[2606.10287v1](http://arxiv.org/abs/2606.10287v1)|null|
|**2026-06-09**|**Energy-Efficient On-Device RAG on a Mobile NPU: System Design and Benchmark on Snapdragon X Elite**|Zhiyuan Cheng et.al.|[2606.11257v1](http://arxiv.org/abs/2606.11257v1)|null|
|**2026-06-08**|**A Source Domain is All You Need: Source-Only Cross-OS Transfer Learning for APT Anomaly Detection via Semantic Alignment and Optimal Transport**|Sidahmed Benabderrahmanea et.al.|[2606.10216v1](http://arxiv.org/abs/2606.10216v1)|null|
|**2026-06-08**|**Gaming AI-Assisted Peer Reviews Poses New Risks to the Scientific Community**|Lin Li et.al.|[2606.10159v1](http://arxiv.org/abs/2606.10159v1)|null|
|**2026-06-08**|**MetaPlate: Counterfactual-Guided RAG-LLM Tool for Personalized Food Recommendation and Hyperglycemia Prevention**|Asiful Arefeen et.al.|[2606.10120v2](http://arxiv.org/abs/2606.10120v2)|null|
|**2026-06-08**|**CodeAlchemy: Synthetic Code Rewriting at Scale**|Ankit Gupta et.al.|[2606.10087v1](http://arxiv.org/abs/2606.10087v1)|null|
|**2026-06-08**|**VFUSE: Virulent Feature Understanding with Sparse autoEncoders**|Michael Yu et.al.|[2606.10080v1](http://arxiv.org/abs/2606.10080v1)|null|
|**2026-06-08**|**Temporal Sheaf Neural Networks with Dynamic Orthogonal Transport**|Md Sadek Hossain Asif et.al.|[2606.10071v1](http://arxiv.org/abs/2606.10071v1)|null|
|**2026-06-08**|**FASE: Fast Adaptive Semantic Entropy for Code Quality**|Shizhe Lin et.al.|[2606.09800v1](http://arxiv.org/abs/2606.09800v1)|null|
|**2026-06-08**|**Difference-Aware Retrieval Policies for Imitation Learning**|Quinn Pfeifer et.al.|[2606.09758v1](http://arxiv.org/abs/2606.09758v1)|null|
|**2026-06-08**|**The Neutral Mask: How RLHF Provides Shallow Alignment while Leaving Partisan Structure Intact in a Large Language Model**|Wendy K. Tam et.al.|[2606.09735v1](http://arxiv.org/abs/2606.09735v1)|null|
|**2026-06-08**|**SearchSwarm: Towards Delegation Intelligence in Agentic LLMs for Long-Horizon Deep Research**|Pu Ning et.al.|[2606.09730v1](http://arxiv.org/abs/2606.09730v1)|null|
|**2026-06-08**|**Beyond Probabilistic Similarity: Structural, Temporal, and Causal Limitations of Retrieval-Augmented Generation in the Legal Domain**|Hudson de Martim et.al.|[2606.09724v1](http://arxiv.org/abs/2606.09724v1)|null|
|**2026-06-08**|**Correlation Is Not Enough: Embedding Human Metadata for Individual Causal Discovery**|Suraj Biswas et.al.|[2606.09672v1](http://arxiv.org/abs/2606.09672v1)|null|
|**2026-06-08**|**Do Video Foundation Models Understand Intuitive Physics? A Layerwise Probing Analysis**|Samuele Punzo et.al.|[2606.09646v1](http://arxiv.org/abs/2606.09646v1)|null|
|**2026-06-08**|**Automated IEP Generation from Traditional Chinese Parent-Teacher Interviews via Corpus-Grounded Feature Diffusion**|Kuanlin Chen et.al.|[2606.09603v1](http://arxiv.org/abs/2606.09603v1)|null|
|**2026-06-08**|**AI Scientists Are Only as Good as Their Evidence: A Stratified Ablation of Proprietary Data and Reasoning Skills in Drug-Asset Valuation**|Yinan Wang et.al.|[2606.09556v1](http://arxiv.org/abs/2606.09556v1)|null|
|**2026-06-08**|**Detecting Differences Is Not Understanding Structure: Large Language Models Fail at Graph Isomorphism**|Kumar Thushalika et.al.|[2606.09484v1](http://arxiv.org/abs/2606.09484v1)|null|
|**2026-06-08**|**DECSELFMASK: Leveraging Unlabeled Text via Self-Relevance-Guided Masking for Decoder-Only Classification**|Pietro Ferrazzi et.al.|[2606.09466v2](http://arxiv.org/abs/2606.09466v2)|null|
|**2026-06-08**|**LargeMonitor: Monitoring Online Task-Free Continual Learning via Large Pretrained Models**|Mingqi Yuan et.al.|[2606.09430v1](http://arxiv.org/abs/2606.09430v1)|null|

#### Abstracts
##### **Doc-to-Atom: Learning to Compile and Compose Memory Atoms**
2606.12400v1 by Xingjian Diao, Wenbo Li, Yashas Malur Saidutta, Avinash Amballa, Lazar Valkov, Srinivas Chappidi

Long input sequences are central to document understanding and multi-step reasoning in Large Language Models, yet the quadratic cost of attention makes inference both memory-intensive and slow. Context distillation mitigates this by compressing contextual information into model parameters, and recent work such as Doc-to-LoRA amortizes context distillation into a single forward pass that generates one LoRA adapter per document. However, producing a single monolithic adapter for all queries leads to irrelevant-query interference, limited compositional recall, and poor scalability to long-document reasoning. To address these challenges, we propose Doc-to-Atom (Doc2Atom), a compositional parametric memory framework that decomposes each document into semantically typed knowledge atoms. Each atom is compiled into an independent micro-LoRA adapter and a provenance retrieval key. At inference time, a lightweight query router selects and assembles only the relevant atoms into a query-specific adapter, which is then injected into a frozen base model. The entire system is trained end-to-end through a multi-objective distillation framework. Experiments on six diverse QA benchmarks demonstrate that Doc2Atom outperforms Doc-to-LoRA baselines while reducing the memory cost of document internalization.

摘要：長輸入序列對於大型語言模型中的文檔理解和多步推理至關重要，然而注意力的二次成本使得推理既耗費內存又緩慢。上下文蒸餾通過將上下文信息壓縮到模型參數中來減輕這一問題，最近的工作如Doc-to-LoRA將上下文蒸餾攤銷到一個單一的前向傳遞中，為每個文檔生成一個LoRA適配器。然而，為所有查詢生成一個單一的整體適配器會導致無關查詢干擾、有限的組合回憶以及對長文檔推理的擴展性差。為了解決這些挑戰，我們提出了Doc-to-Atom (Doc2Atom)，這是一個組合參數記憶框架，將每個文檔分解為語義類型的知識原子。每個原子被編譯成獨立的微型LoRA適配器和來源檢索鍵。在推理時，一個輕量級的查詢路由器選擇並組裝僅與查詢相關的原子到一個查詢特定的適配器中，然後注入到一個凍結的基礎模型中。整個系統通過多目標蒸餾框架進行端到端訓練。在六個不同的QA基準上的實驗表明，Doc2Atom在減少文檔內部化的內存成本的同時，超越了Doc-to-LoRA基準。

##### **Which Models Are Our Models Built On? Auditing Invisible Dependencies in Modern LLMs**
2606.12385v1 by Sanjay Adhikesaven, Haoxiang Sun, Sewon Min

Modern LLM training pipelines increasingly rely on other models to generate data, filter corpora, judge outputs, and guide development decisions. These dependencies are recursive: a model may depend on an upstream artifact whose own dependencies are documented only in separate releases and artifacts. As a result, the full dependency structure is fragmented across heterogeneous public artifacts, with complexity and recursive depth far outpacing humans' ability to trace. We introduce ModSleuth, an agentic system that recursively reconstructs LLM dependency graphs from public artifacts with source-grounded evidence. We find that the primary challenge is no longer information extraction, but defining what constitutes a dependency and reconciling artifact references across inconsistent documentation. We address these challenges through a formalization that distinguishes direct and indirect dependencies, represents heterogeneous pipeline roles through operation-centered relationships, and resolves artifact identities across names, versions, and repositories. Applying ModSleuth to four public-artifact-rich LLM releases, we recover 1,060 source-verified dependencies and construct large-scale dependency graphs of modern LLM development. These graphs reveal multi-hop license obligations, train-evaluation coupling, discrepancies between released and training-time artifacts, and documentation inconsistencies that would otherwise be difficult to uncover. We release ModSleuth and the resulting dependency graphs to support transparent analysis of the increasingly complex ecosystems underlying modern LLMs.

摘要：現代的 LLM 訓練管道越來越依賴其他模型來生成數據、篩選語料庫、評估輸出並指導開發決策。這些依賴是遞歸的：一個模型可能依賴於上游的工件，而這些工件的依賴僅在單獨的版本和工件中記錄。因此，完整的依賴結構在異質的公共工件中是碎片化的，複雜性和遞歸深度遠遠超過人類追蹤的能力。我們介紹了 ModSleuth，一個代理系統，能夠從公共工件中遞歸重建 LLM 依賴圖，並提供基於來源的證據。我們發現主要挑戰不再是信息提取，而是定義什麼構成依賴以及在不一致的文檔中調和工件參考。我們通過一種形式化來解決這些挑戰，區分直接和間接依賴，通過以操作為中心的關係來表示異質管道角色，並解決跨名稱、版本和存儲庫的工件身份問題。將 ModSleuth 應用於四個富含公共工件的 LLM 發布，我們恢復了 1,060 個經過來源驗證的依賴，並構建了現代 LLM 開發的大規模依賴圖。這些圖揭示了多跳許可義務、訓練-評估耦合、發布和訓練時工件之間的差異，以及其他難以發現的文檔不一致。我們發布了 ModSleuth 和生成的依賴圖，以支持對現代 LLM 背後日益複雜的生態系統進行透明分析。

##### **CCKS: Consensus-based Communication and Knowledge Sharing**
2606.12281v1 by Jinyuan Zu, Xiaowei Lv, Yongcai Wang, Deying Li, Yunjun Han, Wenping Chen, Fengyi Zhang, Naiqi Wu

In Decentralized Training and Decentralized Execution (DTDE) for cooperative Multi-Agent Reinforcement Learning (MARL), action-advising-based knowledge sharing promotes interpretable and scalable cooperation among agents. However, current action advising approaches often adhere too much to the teacher's guidance without evaluating teacher-student compatibility, which causes excessive advising, suboptimal stability, and degraded performance. To overcome these challenges, this paper presents a Consensus-based Communication and Knowledge Sharing (CCKS) framework, which allows agents to adopt recommendations based on consensus-derived constraints and to follow the teacher's instructions more smartly. This mechanism enables agents to balance exploration and learning from experienced teachers, improving overall performance. The key is the consensus model construction, for which we propose to employ contrastive learning to construct consensus models based on local observations in the agents' training phase. In action selection, agents score and choose actions based on consensus and shared knowledge. Designed as a plug-and-play solution, CCKS integrates seamlessly with existing DTDE algorithms. Experiments conducted in the Google Research Football environment and the complex StarCraft II Multi-Agent Challenge demonstrate that the integration with CCKS significantly improves cooperation efficiency, learning speed, and overall performance compared with current DTDE baselines. The code is available at https://github.com/yuanxpy/CCKS.

摘要：在合作式多智能體強化學習（MARL）的去中心化訓練和去中心化執行（DTDE）中，基於行動建議的知識分享促進了智能體之間可解釋且可擴展的合作。<lb/>然而，目前的行動建議方法往往過於依賴教師的指導，而未評估教師與學生的相容性，這導致了過度建議、次優穩定性和性能下降。<lb/>為了克服這些挑戰，本文提出了一個基於共識的通信和知識分享（CCKS）框架，該框架允許智能體根據共識導出的約束來採納建議，並更智能地遵循教師的指示。<lb/>這一機制使智能體能夠在探索與向經驗豐富的教師學習之間取得平衡，從而提高整體性能。<lb/>關鍵在於共識模型的構建，為此我們提議利用對比學習來根據智能體訓練階段的局部觀察構建共識模型。<lb/>在行動選擇中，智能體根據共識和共享知識對行動進行評分和選擇。<lb/>CCKS被設計為即插即用的解決方案，與現有的DTDE算法無縫整合。<lb/>在Google Research Football環境和複雜的StarCraft II多智能體挑戰中進行的實驗表明，與CCKS的整合顯著提高了合作效率、學習速度和整體性能，與目前的DTDE基準相比更是如此。<lb/>代碼可在 https://github.com/yuanxpy/CCKS 獲得。

##### **The Impossibility of Eliciting Latent Knowledge**
2606.12268v1 by Korbinian Friedl, Francis Rhys Ward, Paul Yushin Rapoport, Tom Everitt, Jonathan Richens

Advanced AI systems have extensive knowledge of their environments; in fact, their knowledge may (far) exceed that of their developers or users. Consequently, a desirable property for an AI system is that it is honest -- that it accurately reports its beliefs about the world. Designing an AI system to be honest may be difficult, especially if we want to ask it questions about latent variables in the environment -- variables which are hidden from the human interacting with it. This gives rise to the problem of eliciting latent knowledge (ELK): the problem of training an AI agent to honestly report its beliefs. In this paper, we make ELK formally precise using Causal Influence Diagrams (CIDs). CIDs can be used to describe the relationship between an agent's training environment and its subjective representation of the world. We use CIDs to formalise the distinction between observable and latent variables, to specify what exactly it means for an agent to be honest, and to formally define goal misgeneralisation. We show that, under certain circumstances, developers can incentivise an agent to honestly answer questions by providing correct feedback during training. However, a natural, but undesirable, way for an agent to generalise is to provide answers which humans would evaluate as true, rather than honest answers. We prove an impossibility theorem stating: There is no feedback-based training strategy that depends only on agent behaviour and with certainty produces an honest agent, even if feedback is perfect during training.

摘要：先進的人工智慧系統對其環境擁有廣泛的知識；事實上，它們的知識可能（遠）超過其開發者或使用者的知識。<lb/>因此，對於一個人工智慧系統來說，一個理想的特性是誠實——即它準確地報告其對世界的信念。<lb/>設計一個誠實的人工智慧系統可能是困難的，特別是如果我們想要詢問它有關環境中潛在變數的問題——這些變數對與之互動的人類來說是隱藏的。<lb/>這引發了潛在知識引出問題（ELK）：訓練一個人工智慧代理誠實報告其信念的問題。<lb/>在本文中，我們使用因果影響圖（CIDs）對ELK進行正式的精確化。<lb/>CIDs可用於描述代理的訓練環境與其主觀世界表徵之間的關係。<lb/>我們使用CIDs來形式化可觀察變數和潛在變數之間的區別，明確指定代理誠實的確切含義，並正式定義目標錯誤概括。<lb/>我們展示，在某些情況下，開發者可以通過在訓練過程中提供正確的反饋來激勵代理誠實回答問題。<lb/>然而，代理一種自然但不理想的概括方式是提供人類會評價為真實的答案，而不是誠實的答案。<lb/>我們證明了一個不可能性定理，聲明：沒有依賴於代理行為的基於反饋的訓練策略，能夠確定性地產生一個誠實的代理，即使在訓練過程中反饋是完美的。

##### **Adaptive Multi-Resolution Procedural Knowledge Compression for Large Language Models**
2606.12203v1 by Changyue Wang, Weihang Su, Qingyao Ai, Yichen Tang, Runzhong Qiao, Xuancheng Li, Min Zhang, Yiqun Liu

Large language models (LLMs) are widely used to tackle complex tasks with autonomous workflows. Recently, reusable natural language skills have emerged as a popular paradigm to inject procedural knowledge into LLM applications. Since popular skills are often invoked repeatedly, placing their full text in every context significantly increases prefill cost and latency. While text compression techniques have the potential to solve this problem, most existing methods are designed to compress factual knowledge in documents instead of procedural knowledge, making them insufficient for skill compression. In this paper, we argue that an effective skill compression method should: 1) preserve logical dependencies among workflows and tool protocols, 2) enable lightweight, offline compression for frequently updated community skills, and 3) be adaptable to varying complexities across skills. To address this, we present SKIM (SKIll coMpression), an adaptive multi-resolution soft token compression framework for procedural skills. Depending on the complexity of each skill, SKIM creates different numbers of soft tokens that not only improve the efficiency of LLM inference, but also preserve the effectiveness of skill usage. Experiments indicate that SKIM compresses skills to 30 to 60 percent of their original token length while preserving task performance better than existing compression methods.We have released our code at https://github.com/bebr2/SKIM .

摘要：大型語言模型（LLMs）被廣泛用於處理具有自主工作流程的複雜任務。<lb/>最近，可重用的自然語言技能作為一種流行的範式出現，用以將程序知識注入LLM應用中。<lb/>由於流行的技能經常被重複調用，將其完整文本放入每個上下文中會顯著增加預填成本和延遲。<lb/>雖然文本壓縮技術有潛力解決這個問題，但大多數現有方法是設計用來壓縮文檔中的事實知識，而不是程序知識，這使得它們對技能壓縮來說不夠充分。<lb/>在本文中，我們主張一種有效的技能壓縮方法應該：1）保留工作流程和工具協議之間的邏輯依賴，2）能夠為經常更新的社區技能實現輕量級的離線壓縮，並且3）能夠適應不同技能之間的複雜性變化。<lb/>為了解決這個問題，我們提出了SKIM（SKIll coMpression），一種適應性的多解析度軟令牌壓縮框架，用於程序技能。<lb/>根據每個技能的複雜性，SKIM創建不同數量的軟令牌，不僅提高了LLM推理的效率，還保留了技能使用的有效性。<lb/>實驗表明，SKIM將技能壓縮到其原始令牌長度的30%到60%，同時在保持任務性能方面優於現有的壓縮方法。我們已在https://github.com/bebr2/SKIM上發布了我們的代碼。

##### **OpenMedReason: Scientific Reasoning Supervision for Medical Vision-Language Models**
2606.12169v1 by Negin Baghbanzadeh, Pritam Sarkar, Michael Colacci, Abeer Badawi, Adibvafa Fallahpour, Arash Afkanpour, Leonid Sigal, Ali Etemad, Elham Dolatabadi

High-stakes clinical use of large vision-language models (LVLMs) requires reasoning that is grounded in visual evidence and clinical knowledge, not just correct final answers. We introduce OpenMedReason, a large-scale, open multimodal medical reasoning corpus comprising approximately 450K image-question-answer instances whose reasoning traces are primarily derived from curated biomedical, human-authored scientific articles. OpenMedReason provides high-fidelity supervision beyond synthetic chains of thought, covering diverse medical domain vision modalities such as radiological scans, microscopic images, visible light photographs, charts, and others. We complement it with OpenMedReason-Bench, a held-out benchmark that allows fine-grained evaluation of LVLMs along three complementary axes of capability, including perception, medical knowledge, and rationale, enabling diagnostic evaluation beyond final-answer accuracy. OpenMedReason is a rich training resource that exhibits its effectiveness in both supervised fine-tuning (SFT) and reinforcement-based alignment. Training with OpenMedReason yields a 20% average improvement in VQA accuracy over the base model and achieves performance within 4.2% of the strongest comparable-scale medical LVLMs. Fine-grained performance analysis confirms that the gains are not concentrated in any single axis: OpenMedReason improves perception, medical knowledge, and rationale jointly, and its reasoning traces are preferred over those of the base model in 86.1% of pairwise comparisons. We release the code and dataset at huggingface.co/datasets/neginb/OpenMedReason.

摘要：高風險臨床使用大型視覺-語言模型 (LVLMs) 需要基於視覺證據和臨床知識的推理，而不僅僅是正確的最終答案。<lb/>我們介紹了 OpenMedReason，一個大規模的開放多模態醫療推理語料庫，包含約 450K 的圖像-問題-答案實例，其推理過程主要來自經過策劃的生物醫學和人類撰寫的科學文章。<lb/>OpenMedReason 提供了超越合成思維鏈的高保真監督，涵蓋了多樣的醫療領域視覺模態，如放射掃描、顯微圖像、可見光照片、圖表等。<lb/>我們用 OpenMedReason-Bench 進行補充，這是一個保留的基準，允許在三個互補的能力軸上對 LVLMs 進行細緻的評估，包括感知、醫療知識和推理，使診斷評估超越最終答案的準確性。<lb/>OpenMedReason 是一個豐富的訓練資源，顯示其在監督細調 (SFT) 和基於增強的對齊中的有效性。<lb/>使用 OpenMedReason 進行訓練使 VQA 準確性平均提高 20%，並在最強的可比規模醫療 LVLMs 中達到性能在 4.2% 內。<lb/>細緻的性能分析確認這些增益並不集中於任何單一軸：OpenMedReason 共同改善了感知、醫療知識和推理，其推理過程在 86.1% 的成對比較中優於基準模型。<lb/>我們在 huggingface.co/datasets/neginb/OpenMedReason 上發布了代碼和數據集。

##### **Soft-Prompt Tuning for Fair and Efficient LLM Benchmark Evaluation**
2606.12117v1 by Selen Erkan, Bastian Boll, Kristian Kersting, Björn Deiseroth, Letitia Parcalabescu

Benchmark scores often misrepresent a large language model's (LLM's) knowledge, because they rely, e.g., on the model's ability to follow specific formatting requirements. This especially penalizes base models that may know the correct answers but lack the ability -- typically introduced in post-training -- to structure them as instructed. To overcome this, we propose soft-prompt tuning, an efficient, fair, and architecture-agnostic model evaluation. By optimizing only 10 soft-prompt vectors (roughly 0.0006% parameters for a 7B model) over a short tuning period, we adapt models to specific benchmark formats, closing gaps in format-following and ensuring that underlying knowledge is accurately reflected in benchmark scores. This allows one to fairly compare different base models -- trained with various pre-training recipes -- on benchmarks without the need for full post-training. We evaluated soft-prompt tuning across 7 models and 7 datasets. The results show that (a) soft-prompt tuning saturates format-following within 80 steps (~640 samples) making it highly efficient, (b) soft-prompt tuning significantly outperforms zero- and few-shot prompting, surfacing base model knowledge that standard prompting misses, that (c) even post-trained models can benefit from soft-prompts to maximize format compliance, and that (d) soft-prompted base model performance predicts post-trained model rankings more reliably than zero- and few-shot baselines, offering a low-cost proxy for downstream model quality. Our contributions include (1) metrics which disentangle format-following and knowledge accuracy, (2) a fairer benchmarking protocol of LLM knowledge, and (3) a cost- and memory-effective recipe to identify optimal pre-training strategies early in LLM development.

摘要：基準分數常常錯誤地代表大型語言模型（LLM）的知識，因為它們依賴於模型遵循特定格式要求的能力。這特別對那些可能知道正確答案但缺乏在後訓練中通常引入的能力來按照指示結構化答案的基礎模型造成懲罰。為了解決這個問題，我們提出了軟提示調整，這是一種高效、公平且與架構無關的模型評估方法。通過在短期調整期間僅優化10個軟提示向量（對於一個7B模型大約是0.0006%的參數），我們使模型適應特定的基準格式，縮小格式遵循的差距，並確保基礎知識在基準分數中得到準確反映。這使得人們可以公平地比較不同的基礎模型——這些模型是用各種預訓練配方訓練的——在基準上，而無需進行完整的後訓練。我們在7個模型和7個數據集上評估了軟提示調整。結果顯示：（a）軟提示調整在80步內飽和格式遵循（約640個樣本），使其非常高效；（b）軟提示調整顯著優於零次和少次提示，揭示了標準提示所忽略的基礎模型知識；（c）即使是後訓練模型也能從軟提示中受益，以最大化格式遵循；以及（d）軟提示的基礎模型性能比零次和少次基準更可靠地預測後訓練模型的排名，為下游模型質量提供了一個低成本的代理。我們的貢獻包括（1）解開格式遵循和知識準確性的度量；（2）一個更公平的LLM知識基準協議；以及（3）一個成本和內存有效的配方，以便在LLM開發早期識別最佳的預訓練策略。

##### **MSUE: Multi-Modal Soccer Understanding Expert**
2606.12106v1 by Litao Li, Yibo Yu, Yufeng Hu, Zhuo Yang, Jiali Wen, Yixin Chen, Yixi Zhou

This paper presents our solution to the 2026 SoccerNet VQA Challenge. We first develop a cost-effective data synthesis pipeline driven by a Vision-Language Model (VLM), which systematically restructures raw domain data into diverse VQA samples, including concise answers and long-form responses. Second, we propose MSUE, a multi-expert question answering architecture that employs a Large Language Model (LLM) to dynamically dispatch questions to text, image, and video experts. These experts are instantiated as a strong text baseline Gemini3-Flash, a fine-tuned Qwen3-VL, and an external knowledge base, respectively, working collaboratively to enhance VQA performance. MSUE achieves an accuracy of \textbf{0.95} on the challenge benchmark, securing third place in the leaderboard.

摘要：本論文提出了我們對2026年SoccerNet VQA挑戰賽的解決方案。<lb/>我們首先開發了一個由視覺-語言模型（VLM）驅動的成本效益數據合成管道，<lb/>該管道系統性地將原始領域數據重構為多樣的VQA樣本，包括簡潔的答案和長篇回應。<lb/>其次，我們提出了MSUE，一種多專家問答架構，<lb/>該架構利用大型語言模型（LLM）動態地將問題分派給文本、圖像和視頻專家。<lb/>這些專家分別被實例化為強大的文本基線Gemini3-Flash、微調的Qwen3-VL和外部知識庫，<lb/>共同協作以提升VQA性能。<lb/>MSUE在挑戰基準上達到了\textbf{0.95}的準確率，<lb/>在排行榜上獲得第三名。

##### **FORT-Searcher: Synthesizing Shortcut-Resistant Search Tasks for Training Deep Search Agents**
2606.12087v1 by Jia Deng, Yimeng Chen, Xiaoqing Xiang, Ziyang Zeng, Shuo Tang, Wayne Xin Zhao, Feng Chang, Chuan Hao, Yuan Wei, Ran Tao, Bryan Dai, Ji-Rong Wen

Training deep search agents requires verifiable questions whose answers remain unavailable until sufficient evidence has been acquired through search. Existing synthesis methods often increase apparent difficulty by enriching graph structures, but structural complexity alone does not guarantee realized search difficulty: the intended search process can collapse through a cheaper identifying route. We formalize this gap with a shortcut-aware difficulty framework and identify four actionable shortcut risks: evidence co-coverage, single-clue selectivity, exposed constants, and prior-knowledge binding. To diagnose their realized effects, we use trajectory signatures including solving cost, answer hit time, and prior-shortcut rate. Guided by this framework, we introduce FORT, a Framework of Shortcut-Resistant Training-Data Synthesis. FORT constructs shortcut-resistant training data by controlling shortcut risks across entity selection, evidence graph construction, question formulation, and adversarial refinement. Experiments show that FORT induces longer pre-answer search and fewer shortcut patterns than existing open-source deep search datasets. Using the resulting trajectories, we train FORT-Searcher with supervised fine-tuning (SFT) only, and it achieves the best overall performance among comparable-size open-source search agents on challenging deep search benchmarks. Relevant resources will be made available at https://github.com/RUCAIBox/FORT-Searcher.

摘要：訓練深度搜尋代理需要可驗證的問題，其答案在通過搜尋獲得足夠證據之前無法獲得。現有的綜合方法通常通過豐富圖形結構來增加表面難度，但僅僅依賴結構複雜性並不能保證實際的搜尋難度：預期的搜尋過程可能會通過更便宜的識別路徑崩潰。我們用一個考慮捷徑的難度框架來形式化這一差距，並確定四個可行的捷徑風險：證據共同覆蓋、單一線索選擇性、暴露常數和先驗知識綁定。為了診斷它們的實際影響，我們使用軌跡簽名，包括解決成本、答案命中時間和先前捷徑比率。在這一框架的指導下，我們引入了FORT，一個捷徑抗性訓練數據合成框架。FORT通過控制實體選擇、證據圖構建、問題表述和對抗性精煉中的捷徑風險來構建捷徑抗性的訓練數據。實驗表明，FORT引導的預答案搜尋時間更長，且捷徑模式比現有的開源深度搜尋數據集更少。利用這些結果軌跡，我們僅使用監督微調（SFT）訓練FORT-Searcher，並在具有挑戰性的深度搜尋基準上實現了與相似規模的開源搜尋代理相比最佳的整體性能。相關資源將在 https://github.com/RUCAIBox/FORT-Searcher 上提供。

##### **IntElicit: Eliciting and Assessing Contextualized Creativity via Dialogue Policy Optimization**
2606.12086v1 by Mingjia Li, Jin Wu, Hong Qian, Wenhao Huang, Yiyang Huang, Yiwen Zhang, Chanjin Zheng, Xiangfeng Wang, Aimin Zhou, Jiajun Guo

Contextualized assessment offers high ecological validity for evaluating creativity but introduces a critical challenge: observed performance may be confounded with cognitive proficiency (domain knowledge) and agency (willingness to engage). Meanwhile, in the age of generative AI, creative problem solving increasingly occurs in tool-mediated and human--AI interactive environments, making fully static assessment less aligned with contemporary creative practice. To address these issues, this paper proposes IntElicit, a framework for eliciting and assessing contextualized creativity via dialogue policy optimization. IntElicit functions as a constrained adaptive AI Interviewer: it provides non-directive knowledge and agency scaffolds in multi-turn interaction to reduce non-creative confounders, while preserving participants' responsibility for generating the creative content being evaluated. Specifically, to tackle sparse rewards and potential reward hacking (e.g., answer dictation) in open-ended educational dialogue, IntElicit introduces a decomposed process reward mechanism. This mechanism aligns the policy with pedagogical elicitation, rewarding prompts that draw out participant reasoning rather than producing optimal answers on their behalf. Extensive experiments, including participant simulation and a human subject study (N=64), show that IntElicit improves elicited creative outcomes over expert-designed baselines. Together, the results suggest that interactive elicitation can reveal creative potential that static FPSP-style assessment may miss, providing a formative and diagnostic lens for contextualized creativity assessment in AI-mediated learning contexts.

摘要：情境評估為評估創造力提供了高生態效度，但也帶來了一個關鍵挑戰：觀察到的表現可能與認知能力（領域知識）和主動性（參與意願）混淆。<lb/>同時，在生成式人工智慧的時代，創造性問題解決越來越多地發生在工具介導和人類—AI互動的環境中，使得完全靜態的評估與當代創造性實踐的對應性降低。<lb/>為了解決這些問題，本文提出了IntElicit，一個通過對話政策優化來引出和評估情境創造力的框架。<lb/>IntElicit作為一個受限的自適應AI面試官運作：它在多輪互動中提供非指導性的知識和主動性支架，以減少非創造性的混淆因素，同時保留參與者對生成被評估創造內容的責任。<lb/>具體而言，為了應對稀疏的獎勵和潛在的獎勵駭客（例如，答案口述）在開放式教育對話中的問題，IntElicit引入了一個分解的過程獎勵機制。<lb/>這個機制使政策與教學引導相一致，獎勵引出參與者推理的提示，而不是代表他們產生最佳答案。<lb/>廣泛的實驗，包括參與者模擬和一項人類受試者研究（N=64），顯示IntElicit在引出創造性成果方面優於專家設計的基準。<lb/>總體而言，結果表明互動引導可以揭示靜態FPSP風格評估可能忽略的創造潛力，為AI介導學習情境中的情境創造力評估提供了一個形成性和診斷性的視角。

##### **Automating Geometry-Intensive Compliance Checking in BIM: Graph-Based Semantic Reasoning Framework**
2606.12065v1 by Zixuan Xiao, Pei Troh Koh, Jun Ma, Jack C. P. Cheng

Automating compliance check for geometry-intensive regulations remains a significant technical bottleneck in Building Information Modeling (BIM), primarily due to the semantic disparity between high-level regulatory logic and structured IFC data. Existing methods, often reliant on static rule templates, struggle to traverse multi-hop reasoning chains or resolve latent spatial dependencies across multiple building entities. To address these challenges, a Spatial-Geometric Reasoning System for Building Information Modeling (SGR-BIM) is proposed as an integrative graph-driven reasoning framework. SGR-BIM dynamically constructs a cross-modal knowledge graph that aligns user intent, regulatory semantics, and BIM geometry, enabling interpretable reasoning without rigid hard-coding. Validated on 679 expert-verified queries from fire safety codes, the framework achieves 84.3% accuracy, representing an 8.6% improvement over enhanced-tool single-agent baselines. This research provides a graph-based semantic reasoning paradigm, enhancing the transparency and flexibility of automated geometric compliance check workflows in the Architecture, Engineering, and Construction (AEC) industry.

摘要：自動化幾何密集型法規的合規檢查仍然是建築資訊建模（BIM）中的一個重大技術瓶頸，主要是由於高層次法規邏輯與結構化IFC數據之間的語義差異。現有方法通常依賴靜態規則模板，難以穿越多跳推理鏈或解決多個建築實體之間潛在的空間依賴關係。為了解決這些挑戰，提出了一個建築資訊建模的空間幾何推理系統（SGR-BIM），作為一個綜合的圖驅動推理框架。SGR-BIM動態構建一個跨模態知識圖，對齊用戶意圖、法規語義和BIM幾何，實現可解釋的推理，而無需僵化的硬編碼。在679個經專家驗證的火災安全法規查詢上進行驗證，該框架達到了84.3%的準確率，較增強工具單一代理基準提高了8.6%。這項研究提供了一個基於圖的語義推理範式，增強了建築、工程和施工（AEC）行業自動化幾何合規檢查工作流程的透明度和靈活性。

##### **MODF-SIR: A Multi-agent Omni-modal Distilled Framework for Social Intelligence Reasoning**
2606.12018v1 by Shang Ma, Jisheng Dang, Wencan Zhang, Yifan Zhang, Bimei Wang, Hong Peng, Bin Hu, Qi Tian, Tat-Seng Chua

We propose a multi-agent collaborative framework built upon a lightweight Multimodal Large Language Model (MLLM), specifically designed for social intelligence reasoning. A key feature of our approach is that both the training and inference phases are augmented via knowledge distillation. Within this architecture, multi-modal data pertinent to social intelligence is precisely localized. Furthermore, relevant long-tail events are identified, extracted, and rendered as formatted, explicit text. This formatting strategy prevents critical long-tail information from being overshadowed by head events and environmental noise during the tokenization process. Specifically, we integrate Test-Time Adaptation (TTA) across the entire reasoning pipeline, encompassing the extraction and representation of long-tail events, Chain-of-Thought (CoT) prompting, and self-reflection. This TTA mechanism is also distillation-enhanced, utilizing Low-Rank Adaptation (LoRA) to fine-tune the foundation model exclusively for instance-level reasoning. Extensive evaluations against various open-source and proprietary AI models across multiple benchmarks demonstrate the effectiveness of the proposed framework. With around 30% of training data from IntentTrain, we achieve state-of-the-art results. Codes are available at https://github.com/eeee-sys/MODF-SIR, demo is available at https://huggingface.co/spaces/Harry-1234/MODF-SIR, LoRA is available at https://huggingface.co/Harry-1234/MODF-SIR and the dataset for training router is available at https://huggingface.co/datasets/Harry-1234/IntentRouterTrain.

摘要：我們提出了一個基於輕量級多模態大型語言模型（MLLM）的多代理協作框架，專門設計用於社會智能推理。我們方法的一個關鍵特徵是訓練和推理階段都通過知識蒸餾進行增強。在這個架構中，與社會智能相關的多模態數據被精確定位。此外，相關的長尾事件被識別、提取並呈現為格式化的明確文本。這種格式化策略防止了關鍵的長尾信息在標記過程中被頭事件和環境噪音所掩蓋。具體而言，我們在整個推理流程中整合了測試時適應（TTA），涵蓋了長尾事件的提取和表示、思維鏈（CoT）提示以及自我反思。這一TTA機制也得到了蒸餾增強，利用低秩適應（LoRA）專門對基礎模型進行微調以實現實例級推理。對各種開源和專有AI模型在多個基準上的廣泛評估顯示了所提框架的有效性。通過約30%的來自IntentTrain的訓練數據，我們實現了最先進的結果。代碼可在 https://github.com/eeee-sys/MODF-SIR 獲得，演示可在 https://huggingface.co/spaces/Harry-1234/MODF-SIR 獲得，LoRA可在 https://huggingface.co/Harry-1234/MODF-SIR 獲得，訓練路由器的數據集可在 https://huggingface.co/datasets/Harry-1234/IntentRouterTrain 獲得。

##### **The Art of Interrogation: Consistency Amplifies Factuality in Spatial Reasoning**
2606.11918v1 by Theo Uscidda, Marta Tintore Gazulla, Maks Ovsjanikov, Federico Tombari, Leonidas Guibas

Current Large Reasoning Models (LRMs) exhibit remarkable general capabilities but significantly underperform in spatial reasoning tasks. Existing approaches treat this gap as a knowledge deficit, relying on supervised fine-tuning (SFT) to ingest labeled spatial data from external vision sources or synthetic engines. In contrast, we argue that for many tasks, spatial reasoning capabilities are already present in pre-trained LRMs but require alignment through logical coherence under geometric 2D and 3D constraints. In this work, we propose a self-supervised reinforcement learning (RL) framework that targets the internal reasoning process without requiring ground-truth annotations. By formalizing the notion of consistency verifiers -- reward functions that check for geometric and semantic consistency under transformations -- we demonstrate that models can improve their spatial reasoning abilities. We use both image transformations, like flipping, and textual transformations, like swapping the order of objects in the question, and propose a new optimal transport-based RL strategy, OT-GRPO, which is a minimal-matching variant of group relative policy optimization tailored to pairwise verifiers. We show that this label-free consistency training approaches the accuracy of models trained with ground-truth supervision and achieves similar generalization across diverse tasks and data domains.

摘要：目前的大型推理模型（LRMs）展現出卓越的通用能力，但在空間推理任務中表現顯著不足。現有的方法將這一差距視為知識缺失，依賴於監督式微調（SFT）來從外部視覺來源或合成引擎中攝取標記的空間數據。相反，我們主張對於許多任務，空間推理能力已經存在於預訓練的LRMs中，但需要通過幾何2D和3D約束下的邏輯一致性來進行對齊。在這項工作中，我們提出了一個自我監督的強化學習（RL）框架，旨在針對內部推理過程，而不需要真實標註。通過形式化一致性驗證器的概念——檢查在變換下幾何和語義一致性的獎勵函數——我們展示了模型可以提高其空間推理能力。我們使用了圖像變換，如翻轉，以及文本變換，如在問題中交換物體的順序，並提出了一種基於最優運輸的RL策略OT-GRPO，這是一種針對成對驗證器的最小匹配變體的群體相對政策優化。我們顯示這種無標籤的一致性訓練接近於使用真實監督訓練的模型的準確性，並在不同任務和數據領域中達到了類似的泛化能力。

##### **GraspLLM: Towards Zero-Shot Generalization on Text-Attributed Graphs with LLMs**
2606.11898v1 by Hengyi Feng, Zeang Sheng, Meiyi Qiang, Meiyi Qiang, Wentao Zhang

Research on Text-Attributed Graphs (TAGs) has gained significant attention recently due to its broad applications across various real-world data scenarios, such as citation networks, e-commerce platforms, social media, and web pages. Inspired by the remarkable semantic understanding ability of Large Language Models (LLMs), there have been numerous attempts to integrate LLMs into TAGs. However, existing methods still struggle to generalize across diverse graphs and tasks, and their ability to capture transferable graph structural patterns remains limited. To address this, we introduce the GraspLLM, a framework that combines Graph structural comprehension with semantic understanding prowess of LLMs to enhance the cross-dataset and cross-task generalizability. Specifically, we represent node texts from different graphs in a unified semantic space with a frozen general embedding model, on top of which we perform motif-aware contrastive learning across multiple motif-induced adjacency matrices to extract dataset-agnostic structural information. Then, with our proposed optimal contextual subgraph, we extract the most contextually relevant subgraph for each target node and align these subgraphs to the token space of LLM via an alignment projector. Extensive experiments on TAG benchmark datasets spanning diverse domains reveal that GraspLLM consistently outperforms previous LLM-based methods for TAGs, especially in zero-shot scenarios, highlighting its strong generalizability across different datasets and tasks. Our code is available at https://github.com/Heinz217/GraspLLM.

摘要：研究文本屬性圖 (TAGs) 近來受到廣泛關注，因為它在各種現實世界數據場景中的應用廣泛，例如引用網絡、電子商務平台、社交媒體和網頁。受到大型語言模型 (LLMs) 卓越的語義理解能力啟發，已有許多嘗試將 LLMs 整合到 TAGs 中。然而，現有方法在不同圖形和任務之間的泛化能力仍然存在困難，並且捕捉可轉移圖結構模式的能力仍然有限。為了解決這個問題，我們提出了 GraspLLM，一個將圖結構理解與 LLMs 的語義理解能力相結合的框架，以增強跨數據集和跨任務的泛化能力。具體而言，我們使用一個凍結的通用嵌入模型，將來自不同圖的節點文本表示在統一的語義空間中，在此基礎上，我們在多個基於動機的鄰接矩陣上進行動機感知對比學習，以提取與數據集無關的結構信息。然後，通過我們提出的最佳上下文子圖，我們為每個目標節點提取最具上下文相關性的子圖，並通過對齊投影器將這些子圖對齊到 LLM 的標記空間。對涵蓋不同領域的 TAG 基準數據集進行的廣泛實驗顯示，GraspLLM 在 TAGs 上始終優於先前基於 LLM 的方法，特別是在零樣本場景中，突顯了其在不同數據集和任務之間的強泛化能力。我們的代碼可在 https://github.com/Heinz217/GraspLLM 獲得。

##### **Fine-tuning Multi-modal LLMs with ART: Art-based Reinforcement Training**
2606.11854v1 by Michal Chudoba, Sergey Alyaev, Petra Galuscakova, Tomasz Wiktorski

There are two main Parameter-Efficient Fine-Tuning (PEFT) techniques for Large Language Models (LLMs). While Low-Rank Adaptation (LoRA) introduces additional weights between the LLM layers, Soft Prompting introduces additional fine-tuning-specific raw tokens to an LLM input. However, both require modification to the computational graphs of precompiled, preoptimized LLMs. As a result, neither is fully supported in high-throughput engines like vLLM. We propose fine-tuning with ART (Art-based Reinforcement Training). The method injects information into a frozen Multimodal Large Language Model (MLLM) by optimizing only its raw visual input, thus enabling the soft-token approach on pre-compiled computational graphs. It relies on backpropagation of gradients back into a plain pixel array and thus supports any fine-tuning objective. Moreover, the optimized visual input can be stylized as task-relevant computational artworks. The approach's effectiveness is confirmed for different sizes of a popular open Qwen architecture and for several textual benchmarks. Specifically, ART reaches accuracy competitive with LoRA across mathematics and structured-tool-use benchmarks.

摘要：有兩種主要的參數高效微調（PEFT）技術用於大型語言模型（LLMs）。低秩適應（LoRA）在LLM層之間引入額外的權重，而軟提示則在LLM輸入中引入額外的微調專用原始標記。然而，兩者都需要對預編譯、預優化的LLM的計算圖進行修改。因此，這兩者在高吞吐量引擎如vLLM中都不完全受支持。我們提出使用ART（基於藝術的增強訓練）進行微調。該方法通過僅優化其原始視覺輸入，將信息注入到一個凍結的多模態大型語言模型（MLLM）中，從而在預編譯的計算圖上啟用軟標記方法。它依賴於將梯度反向傳播回普通像素數組，因此支持任何微調目標。此外，優化的視覺輸入可以被風格化為與任務相關的計算藝術作品。該方法的有效性在不同大小的流行開放Qwen架構和幾個文本基準上得到了證實。具體而言，ART在數學和結構化工具使用基準上達到了與LoRA相競爭的準確性。

##### **Task-Aware Structured Memory for Dynamic Multi-modal In-Context Learning**
2606.11853v1 by Zhirui Chen, Ziwei Chen, Ling Shao

Multi-modal large language models (MLLMs) depend on in-context learning (ICL) for rapid task adaptation, but their scalability is severely limited by finite context windows and the growing cost of key-value (KV) caches in long multi-modal sequences. Existing memory compression approaches typically rely on rigid token removal or sample-dependent importance estimation, which introduces bias, disrupts semantic structure, particularly for visual representations, and yields static memories that cannot adapt to new queries. We introduce TASM (Task-Aware Structured Memory), a training-free framework that addresses these limitations through task-aware, structure-preserving, and dynamically accessible memory construction. TASM employs task-vector guided compression to replace sample-specific signals with a task-level direction that captures shared relevance across demonstrations. To preserve the underlying manifold, it applies semantics-aware token merging via bipartite graph matching, aggregating tokens without destructive pruning. Finally, TASM structures memory into a hierarchy comprising a compact Core Memory and a Latent Bank, facilitating query-adaptive dynamic retrieval. Evaluations confirm TASM maintains high performance under heavy compression, effectively balancing efficiency with adaptability.

摘要：多模態大型語言模型（MLLMs）依賴於上下文學習（ICL）以快速適應任務，但其可擴展性受到有限上下文窗口和長多模態序列中鍵值（KV）快取日益增長的成本的嚴重限制。現有的記憶壓縮方法通常依賴於僵化的標記移除或樣本依賴的重要性估計，這引入了偏差，破壞了語義結構，特別是對於視覺表示，並產生無法適應新查詢的靜態記憶。我們引入了 TASM（任務感知結構記憶），這是一個無需訓練的框架，通過任務感知、結構保留和動態可訪問的記憶構建來解決這些限制。TASM 採用任務向量引導壓縮，將樣本特定信號替換為捕捉跨示範共享相關性的任務級方向。為了保留底層流形，它通過二分圖匹配應用語義感知的標記合併，聚合標記而不進行破壞性修剪。最後，TASM 將記憶結構化為一個層次，包含一個緊湊的核心記憶和一個潛在庫，促進查詢自適應的動態檢索。評估結果確認 TASM 在重度壓縮下保持高性能，有效平衡了效率與適應性。

##### **From Uniform to Learned Graph Priors: Diffusion for Structure Discovery**
2606.11831v1 by Qi Shao, Hao Guo, Jiawen Chen, Duxin Chen, Wenwu Yu

Neural relational inference (NRI) methods discover interaction graphs from trajectories through variational reasoning on discrete potential edges. However, these methods typically rely on oversimplified, factorized graph priors. Such priors, typically nearing uniform distributions, treat edges as independent entities. This systemic misalignment does not match the real-world systems and yields diffuse and indecisive edge posteriors limiting the reliability of structural discovery. To address this, we propose \textit{Diff-prior}, a diffusion-parameterized adaptive prior used to calibrate latent graph distribution rather than generate graphs. Our core insight is to reframe prior integration as a learnable denoising-style calibration that organizes scattered, uncertain edge posteriors into a more reliable overall structure which can be trained by the diffusion model. Diff-prior learns an adaptive structure prior that performs structured calibration on the edge posteriors during inference, guiding it towards a distribution closer to the underlying structure. The diff-prior operates before structural sampling and acts as a denoising calibrator directly on the encoder edge distribution, which provides a generic training paradigm over structured variables. Experiments on standard benchmarks validated our framework, and the results indicate that Diff-prior improves the performance of structure inference and generates more decisive edge posteriors across multiple NRI-family architectures. The code is available on https://github.com/Hardy158118/Diffprior.

摘要：神經關係推斷（NRI）方法透過對離散潛在邊的變分推理從軌跡中發現互動圖。<lb/>然而，這些方法通常依賴於過於簡化的因子化圖先驗。<lb/>這些先驗通常接近均勻分佈，將邊視為獨立的實體。<lb/>這種系統性的錯位與現實世界系統不符，產生擴散且不確定的邊後驗，限制了結構發現的可靠性。<lb/>為了解決這個問題，我們提出了\textit{Diff-prior}，這是一種擴散參數化的自適應先驗，用於校準潛在圖分佈，而不是生成圖。<lb/>我們的核心見解是將先驗整合重新框架為可學習的去噪風格校準，將分散的不確定邊後驗組織成更可靠的整體結構，這可以通過擴散模型進行訓練。<lb/>Diff-prior學習一種自適應結構先驗，在推理過程中對邊後驗進行結構化校準，引導其朝向更接近潛在結構的分佈。<lb/>Diff-prior在結構採樣之前運作，並作為直接對編碼器邊分佈進行去噪校準的工具，這提供了一種針對結構變量的通用訓練範式。<lb/>在標準基準上的實驗驗證了我們的框架，結果表明Diff-prior提高了結構推斷的性能，並在多個NRI家族架構中生成了更具決斷性的邊後驗。<lb/>代碼可在https://github.com/Hardy158118/Diffprior上獲得。

##### **WorldReasoner: Evaluating Whether Language Model Agents Forecast Events with Valid Reasoning**
2606.11816v1 by Yizhou Chi, Eric Chamoun, Zifeng Ding, Andreas Vlachos

Forecasting real-world events requires language-model agents to reason under uncertainty from incomplete, time-bounded information. Yet evaluating whether agents genuinely forecast requires more than final-answer accuracy: a model may be correct by recalling memorized training facts, citing fabricated evidence, or producing an unsupported causal story. We present WorldReasoner, an evaluation framework for temporally valid event forecasting. Each task gives an agent a resolved forecasting question, a simulated forecast date, and access only to evidence available before that date; after resolution, the framework scores the submitted probability, cited evidence, and optional causal event graph. WorldReasoner reports three complementary axes: outcome quality against resolved answers, evidence quality over cited sources, and reasoning quality against post-resolution hindsight graphs. The benchmark is built by an agentic construction pipeline that generates forecasting questions, collects time-stamped evidence, and builds hindsight reference graphs at scale, yielding 345 resolved tasks derived from 14,141 articles with graphs covering 8,087 extracted events. Across six controlled agent settings, temporally valid retrieval is the strongest driver of outcome accuracy; causal graph construction improves key-event recovery; and correct graph-enabled forecasts are more strongly grounded in key events and relevant sources, yet agents still struggle to convert grounded evidence into calibrated probabilities.

摘要：預測真實世界事件需要語言模型代理在不確定性下從不完整的、時間有限的信息中進行推理。<lb/>然而，評估代理是否真正進行預測需要的不僅僅是最終答案的準確性：一個模型可能通過回憶記憶中的訓練事實、引用虛構的證據或產生不支持的因果故事來正確。<lb/>我們提出了WorldReasoner，一個用於時間有效事件預測的評估框架。<lb/>每個任務為代理提供一個解決的預測問題、一個模擬的預測日期，以及僅能訪問在該日期之前可用的證據；在解決後，該框架評分提交的概率、引用的證據和可選的因果事件圖。<lb/>WorldReasoner報告三個互補的軸心：針對解決答案的結果質量、針對引用來源的證據質量，以及針對解決後的回顧圖的推理質量。<lb/>該基準是通過一個代理建構管道建立的，該管道生成預測問題、收集時間戳證據，並大規模構建回顧參考圖，產生了345個來自14,141篇文章的解決任務，這些圖涵蓋了8,087個提取的事件。<lb/>在六個受控的代理設置中，時間有效的檢索是結果準確性的最強驅動因素；因果圖的構建改善了關鍵事件的恢復；並且正確的圖形啟用預測更強烈地基於關鍵事件和相關來源，但代理仍然難以將基於證據的內容轉化為經過校準的概率。

##### **Fast Speech Foundation Model Distillation Using Interleaved Stacking**
2606.11766v1 by Eungbeom Kim, Kyogu Lee

Distilling a large speech foundation model (SFM) into an efficient student model has been successfully applied to low-resource environments. Although distillation reduces inference latency, it requires an additional student model training. However, the training efficiency of SFM distillation remains underexplored. In this work, we explore training acceleration of SFM distillation to speed up model deployment. We examine the potential of stacking, in which the model depth is progressively increased through training until the target model depth is reached. While existing stacking methods improve training speed, they suffer from performance degradation. To handle this limitation, we propose interleaved stacking, a novel stacking method that consistently preserves layer position throughout the stacking process. This property is particularly critical in SFMs, in which each layer encodes distinct layer-specific knowledge. We validate the effectiveness of the proposed method on SUPERB.

摘要：將大型語音基礎模型（SFM）蒸餾成高效的學生模型已成功應用於資源有限的環境中。<lb/>儘管蒸餾減少了推理延遲，但它需要額外的學生模型訓練。<lb/>然而，SFM蒸餾的訓練效率仍然未被充分探索。<lb/>在這項工作中，我們探索了SFM蒸餾的訓練加速，以加快模型部署。<lb/>我們檢視了堆疊的潛力，其中模型深度透過訓練逐步增加，直到達到目標模型深度。<lb/>雖然現有的堆疊方法提高了訓練速度，但它們遭遇了性能下降的問題。<lb/>為了解決這一限制，我們提出了交錯堆疊，一種新穎的堆疊方法，該方法在整個堆疊過程中始終保持層位置。<lb/>這一特性在SFM中尤為關鍵，因為每一層編碼了獨特的層特定知識。<lb/>我們在SUPERB上驗證了所提方法的有效性。

##### **From Prompts to Tokens: Internalizing Causal Supervision in Vision-Language Model for Multi-Image Causal Reasoning**
2606.11745v1 by Haoping Yu, Yuanxi Li, Jing Ma

Visual causal reasoning is essential for understanding and intervening in the physical world, requiring identification of causal variables from visual inputs and reasoning over intervention effects. Despite recent progress, large vision--language models (VLMs) remain brittle at such tasks, especially for interventional and counterfactual queries over multi-image inputs. Most existing explorations inject causal knowledge via textual prompts, leaving causal mechanisms external to model execution and limiting reliable control during inference. To address this problem, we propose BridgeVLM, which internalizes visual causal reasoning by inducing a causal graph from multi-image inputs and converting it into structured Causal Tokens executed by RAMP layers injected into the LLM decoder for causal message passing. We further introduce a unified training interface M3S for fine-grained causal supervision from different granularities (local/global level). BridgeVLM achieves 54.4% accuracy on intervention tasks on CausalVLBench (vs. 33.2% with prompt-level supervision), improves results on Causal3D from 43.6% to 49.0%, and substantially improves causal structure learning on CausalVLBench ($F_1$: 33.4% $\rightarrow$ 75.1%).

摘要：視覺因果推理對於理解和介入物理世界至關重要，這需要從視覺輸入中識別因果變量並推理介入效果。儘管最近取得了一些進展，但大型視覺-語言模型（VLMs）在這類任務上仍然脆弱，特別是在多圖像輸入的介入和反事實查詢上。現有的大多數探索通過文本提示注入因果知識，將因果機制置於模型執行之外，並限制了推理過程中的可靠控制。為了解決這個問題，我們提出了BridgeVLM，它通過從多圖像輸入中誘導因果圖並將其轉換為結構化的因果標記，內化視覺因果推理，這些因果標記由RAMP層注入到LLM解碼器中以進行因果信息傳遞。我們進一步引入了一個統一的訓練介面M3S，以便從不同的粒度（局部/全局層級）進行細緻的因果監督。BridgeVLM在CausalVLBench的介入任務上達到了54.4%的準確率（相比之下，使用提示級別監督為33.2%），在Causal3D上的結果從43.6%提升至49.0%，並在CausalVLBench上顯著改善了因果結構學習（$F_1$: 33.4% $\rightarrow$ 75.1%）。

##### **Hey Chat, Can You Teach Me? Structuring Socratic Dialogue for Human Learning in the Wild**
2606.11744v1 by Sidney Tio, Arunesh Sinha, Pradeep Varakantham

Large language models are now widely used for everyday learning, but the underlying interactions are typically unstructured chats rather than following a curriculum. Unlike formal online learning systems, these interactions carry no prior record of the student, so any estimate of what the student already knows must be inferred from the dialogue itself. We show that this gap is not closed by scaling models alone. Frontier and education-tuned LLMs perform poorly when asked to tutor a student over an extended session, because doing so requires three things at once. The tutor must sequence a curriculum, conduct Socratic dialogue, and infer the student's knowledge state from that dialogue. We propose separating these responsibilities. Given a student query, our system constructs a prerequisite knowledge graph in which subtopics are nodes and dependencies are edges, and frames tutoring as deciding which node to teach next and how many dialogue turns to spend on it before moving on. A lightweight PPO policy handles this sequencing decision, while an LLM conducts the Socratic exchange at the chosen node and returns a signal of student progress. Across held-out STEM and non-STEM topics, our PPO-paired tutor outperforms heuristic baselines, frontier general-purpose models, and a model specialised for Socratic dialogue: on both the rate at which students reach full curriculum mastery and the number of turns required. Explicit curriculum structure delivers gains that scaling the underlying model does not.

摘要：大型語言模型現在被廣泛用於日常學習，但其背後的互動通常是非結構化的對話，而不是遵循課程。與正式的在線學習系統不同，這些互動沒有學生的先前記錄，因此對學生已經知道的內容的任何估計都必須從對話本身推斷。我們顯示僅僅擴大模型並不能填補這一空白。當被要求在長時間的會話中輔導學生時，前沿和教育調整的LLM表現不佳，因為這樣做同時需要三件事情。輔導者必須安排課程，進行蘇格拉底式對話，並從該對話中推斷學生的知識狀態。我們建議將這些責任分開。根據學生的查詢，我們的系統構建一個先決知識圖，其中子主題是節點，依賴關係是邊，並將輔導框架化為決定下一個教學的節點以及在轉移之前要花多少對話輪次。輕量級的PPO策略處理這一排序決策，而LLM則在選定的節點進行蘇格拉底式交流並返回學生進展的信號。在保留的STEM和非STEM主題中，我們的PPO配對輔導者在學生達到完整課程掌握的速度和所需的輪次上表現優於啟發式基準、前沿通用模型以及專門針對蘇格拉底對話的模型：明確的課程結構帶來的收益是擴大基礎模型所無法實現的。

##### **T2S: A Rehearsal-Based Approach for Extraction-Resistant Model Watermarking**
2606.11698v1 by Jian-Ping Mei, Weibin Zhang, Ao Yao, Tiantian Zhu, Jie Xiao

Model watermarking safeguards AI model intellectual property by embedding distinctive knowledge that induces unique behavioral signatures. The primary technical challenge lies in ensuring watermark robustness against various post-processing attacks on the watermarked model. Model extraction attacks emerge as the most severe threat, where adversaries exploit prediction outputs to train surrogate models that illegally replicate the original model's functionality. In this work, we propose a rehearsal-based watermark embedding framework to enhance the robustness of model watermarks against model extraction attacks. By simulating the extraction process, our method leverages the loss of a \textit{simulated stolen model} on a trigger set as a training signal to fine-tune the watermark knowledge within the target model. This fine-tuning step encourages the watermark to be embedded in a way that boosts transferability, thereby increasing its chances of persisting and remaining detectable in stolen models. Comprehensive experiments conducted under diverse settings demonstrate that the proposed method significantly improves the robustness of model watermarks against both model extraction and subsequent watermark removal attacks.

摘要：模型水印透過嵌入獨特的知識來保護 AI 模型的智慧財產，這些知識會引發獨特的行為特徵。主要的技術挑戰在於確保水印對於各種後處理攻擊的穩健性。模型提取攻擊被視為最嚴重的威脅，對手利用預測輸出來訓練替代模型，非法複製原始模型的功能。在本研究中，我們提出了一種基於重複訓練的水印嵌入框架，以增強模型水印對抗模型提取攻擊的穩健性。通過模擬提取過程，我們的方法利用在觸發集上“模擬被盜模型”的損失作為訓練信號，以微調目標模型中的水印知識。這一微調步驟促使水印以提高可轉移性的方式嵌入，從而增加其在被盜模型中持續存在和可檢測的機會。在多種環境下進行的全面實驗顯示，所提出的方法顯著提高了模型水印對抗模型提取和隨後水印移除攻擊的穩健性。

##### **Can AI Reason Like an Urban Planner? Benchmarking Large Language Models Against Professional Judgment**
2606.11678v1 by Yijie Deng, He Zhu, Wen Wang, Junyou Su, Minxin Chen, Wenjia Zhang

Problem, Research Strategy, and Findings: The rise of large language models (LLMs) raises a key question for urban planning: which forms of professional planning knowledge can AI replicate, and which still require human judgment? Although AI tools are increasingly used in planning practice, there is still no systematic framework for testing whether they can reason with the contextual sensitivity, value awareness, and institutional literacy central to planning expertise. This paper introduces Urban Planning Bench (UPBench), a domain-specific evaluation framework that assesses LLM reasoning through a 4x5 matrix of four knowledge pillars and five cognitive levels adapted from Bloom's revised taxonomy. Evaluating 25 LLMs with automated scoring and expert review, we find a non-monotonic cognitive curve: models perform better on higher-order analytical tasks than on factual recall and integrative judgment. This suggests that planning knowledge often treated as lower-order is deeply shaped by institutional, jurisdictional, and temporal context, making it hard for LLMs to generalize. We summarize these limits as four epistemic diagnostics: regulatory hallucination, conceptual conflation, wickedness paralysis, and phronetic deficit.
  Takeaway for Practice: The findings support differential delegation in planning. LLMs can assist with cross-disciplinary synthesis, literature review, scenario generation, and preliminary policy analysis. However, they remain unreliable for jurisdiction-specific regulation, normative conflict resolution, and context-sensitive procedure. Agencies should require verification for AI-assisted regulatory analysis, while planning education should emphasize institutional literacy, normative judgment, and contextual sensitivity.

摘要：問題、研究策略與發現：大型語言模型（LLMs）的興起對城市規劃提出了一個關鍵問題：哪些形式的專業規劃知識可以被人工智慧複製，而哪些仍然需要人類的判斷？儘管人工智慧工具在規劃實踐中越來越常被使用，但目前仍然沒有系統性的框架來測試它們是否能夠具備對規劃專業知識至關重要的情境敏感性、價值意識和制度素養。本文介紹了城市規劃基準（UPBench），這是一個特定領域的評估框架，通過一個4x5的矩陣來評估LLM的推理，該矩陣由四個知識支柱和五個認知層次組成，這些層次是根據布魯姆修訂的分類法進行調整的。通過自動評分和專家審查對25個LLM進行評估，我們發現了一個非單調的認知曲線：模型在高階分析任務上的表現優於事實回憶和綜合判斷。這表明，通常被視為低階的規劃知識深受制度、管轄區和時間背景的影響，使得LLM難以進行概括。我們將這些限制總結為四個認識診斷：監管幻覺、概念混淆、惡性癱瘓和實踐缺失。<lb/>實踐啟示：這些發現支持規劃中的差異化委派。LLMs可以協助跨學科的綜合、文獻回顧、情境生成和初步政策分析。然而，它們在特定管轄區的監管、規範衝突解決和情境敏感程序方面仍然不可靠。機構應要求對AI輔助的監管分析進行驗證，而規劃教育應強調制度素養、規範判斷和情境敏感性。

##### **Lung-R1: A Knowledge Graph-Guided LLM for Pulmonary Diagnostic Reasoning**
2606.11675v1 by Haoyang Zeng, Yuanxi Fu, Rongzhen Li, Yuming Yang, Xiao Sun, Jingwang Huang, Gujie Shao, Guohui Xiang, Quan Lu, Dongfan Ye, Xuetao Chen, Jiang Zhong, Kaiwen Wei, Zhi Xu

Diagnosing pulmonary diseases requires integrating heterogeneous evidence amid phenotypic variability and cross-disease overlap. Although large language models (LLMs) have shown progress on pulmonary knowledge question answering (QA) and information-processing tasks, reliable pulmonary diagnosis requires patient-specific, relation-aware reasoning over electronic medical record (EMR) evidence rather than isolated knowledge recall. We define this gap between pulmonary knowledge and case-level diagnostic reasoning as the Pulmonary Knowledge-to-Diagnosis Gap. To address it, we introduce LungKG, the first structured pulmonary knowledge graph for diagnostic knowledge organization and record-grounded reasoning. LungKG contains 59,038 nodes and 164,308 edges across 15 entity types and 112 relation types, serving as both a reusable pulmonary knowledge resource and the foundation for LungKG-guided model adaptation. Built on LungKG, we propose Lung-R1, a LungKG-guided pulmonary LLM trained through KG-constrained reasoning-chain construction and KG-guided reinforcement learning. In a 20-system evaluation, Lung-R1-14B achieves state-of-the-art performance across Choice, Pulmonary-QA, and EMR Diagnosis, reaching an EMR Diagnosis score of 4.3583 and surpassing the strongest non-Lung-R1 baseline by 0.1476 points. These results demonstrate the value of LungKG-guided training for EMR-based pulmonary diagnosis.

摘要：診斷肺部疾病需要整合異質證據，考慮表型變異性和跨疾病重疊。儘管大型語言模型（LLMs）在肺部知識問題回答（QA）和信息處理任務上已顯示出進展，但可靠的肺部診斷需要針對患者的特定情況進行關係感知推理，基於電子病歷（EMR）證據，而不是孤立的知識回憶。我們將肺部知識與案例級診斷推理之間的差距定義為肺部知識到診斷的差距。為了解決這一問題，我們介紹了LungKG，這是第一個結構化的肺部知識圖，用於診斷知識組織和基於記錄的推理。LungKG包含59,038個節點和164,308條邊，涵蓋15種實體類型和112種關係類型，既作為可重用的肺部知識資源，也為LungKG引導的模型適應奠定基礎。基於LungKG，我們提出了Lung-R1，一個通過KG約束推理鏈構建和KG引導強化學習訓練的LungKG引導肺部LLM。在20個系統的評估中，Lung-R1-14B在Choice、Pulmonary-QA和EMR Diagnosis上達到了最先進的性能，EMR Diagnosis得分為4.3583，超過了最強的非Lung-R1基準0.1476分。這些結果展示了LungKG引導訓練在基於EMR的肺部診斷中的價值。

##### **TAROT: Task-Adaptive Refinement of LLM-prior Graphs for Few-shot Tabular Learning**
2606.11640v1 by Ruxue Shi, Yili Wang, Mengnan Du, Hangting Ye, Yi Chang, Xin Wang

Few-shot tabular learning provides a cost-effective approach for real-world applications where annotation is costly and collecting sufficient samples for new tasks is difficult. Existing Traditional and LLM-based methods have demonstrated effectiveness in few-shot scenarios. However, traditional methods need additional training on unlabeled or generated data, which incur significant computational overhead. In addition, LLM-based methods that directly feed raw tabular data into LLMs raise privacy and compliance concerns. More importantly, both paradigms largely overlook the semantic relationships between features, which provide structural and semantic prior for constructing a semantic graph. Semantic graph is essential for modeling meaningful feature interactions in few-shot scenarios. In this paper, we propose TAROT, a GNN-based framework that encodes the structural and semantic prior by constructing and refining a task-adaptive semantic graph from this prior, thereby improving predictive performance in few-shot tabular learning. TAROT first encodes heterogeneous tabular data into unified node semantic representations via a Unified Semantic Tabular Node Encoder (USTNE). Then, it prompts LLMs to infer the semantic relationship between features based on the task description and feature names to construct a semantic graph. To mitigate structural noise introduced by the hallucination of LLMs, TAROT introduces Task-adaptive Semantic Graph Refinement that prunes spurious or task-unrelated edges and adds missing task-related ones, aligning the graph structure with the downstream objective. Finally, a GNN performs message passing over the refined graph to capture task-related semantic dependencies for prediction. Extensive experiments on various few-shot tabular learning benchmarks demonstrate the superior performance of TAROT, establishing it as a state-of-the-art approach in this domain.

摘要：少樣本表格學習為現實世界應用提供了一種具成本效益的方法，因為標註成本高且為新任務收集足夠樣本困難。現有的傳統方法和基於大型語言模型（LLM）的方法在少樣本場景中已顯示出有效性。然而，傳統方法需要在未標註或生成數據上進行額外訓練，這會產生顯著的計算開銷。此外，將原始表格數據直接輸入LLM的基於LLM的方法引發了隱私和合規性問題。更重要的是，這兩種範式在很大程度上忽視了特徵之間的語義關係，而這些關係為構建語義圖提供了結構性和語義性先驗。語義圖對於在少樣本場景中建模有意義的特徵交互至關重要。在本文中，我們提出了TAROT，一種基於圖神經網絡（GNN）的框架，通過從這些先驗中構建和完善一個任務自適應的語義圖來編碼結構性和語義性先驗，從而提高少樣本表格學習中的預測性能。TAROT首先通過統一語義表格節點編碼器（USTNE）將異質表格數據編碼為統一的節點語義表示。然後，它促使LLM根據任務描述和特徵名稱推斷特徵之間的語義關係，以構建語義圖。為了減輕LLM的幻覺所引入的結構噪音，TAROT引入了任務自適應語義圖精煉，該過程修剪虛假或與任務無關的邊，並添加缺失的與任務相關的邊，將圖結構與下游目標對齊。最後，GNN在精煉後的圖上執行消息傳遞，以捕捉與任務相關的語義依賴性以進行預測。在各種少樣本表格學習基準上的廣泛實驗證明了TAROT的卓越性能，確立了它在該領域的最先進方法。

##### **Physics-Distilled Neural Network enabled by Large Language Models for Manufacturing Process-Property Predictive Modeling**
2606.11605v1 by Ge Song, Kiarash Naghavi Khanghah, Anandkumar Patel, Rajiv Malhotra, Hongyi Xu

Predicting process-property relationships in manufacturing is often challenged by high experimental costs and the limited interpretability of complex 'black-box' models. This paper proposes a novel knowledge distillation framework designed to achieve high-accuracy predictions in data-scarce scenarios. The framework integrates analytical physics priors, which are systematically extracted from scientific literature via Large Language Models, into a privileged teacher model. We employ a Graph-Masked Attention layer to capture the complex physical dependencies among input variables showing strict setpoints or a combination of static and high-frequency temporal signatures. This privileged knowledge is distilled into a lightweight student predictor for inference. The feasibility and robustness of the framework are evaluated through a comprehensive experiment across five diverse manufacturing processes. To ensure statistical reliability, given the small dataset sizes, a repeated K-fold cross-validation technique is employed to quantify model stability and generalization. Results indicate that the proposed framework consistently achieves high predictive accuracy across all evaluated domains. Most importantly, the architecture demonstrates significant fault tolerance by maintaining robust predictive performance even in scenarios where LLM-derived analytical priors are suboptimal or incomplete. Furthermore, the student predictor achieves an inference frequency exceeding 6000 Hz, which facilitates real-time edge deployment on standard industrial hardware. This work provides a scalable solution for bridging the gap between theoretical physics and real-time industrial monitoring in data-limited environments.

摘要：預測製造中的過程-性質關係常常面臨高實驗成本和複雜的「黑箱」模型的有限可解釋性挑戰。本文提出了一種新穎的知識蒸餾框架，旨在實現數據稀缺場景中的高準確性預測。該框架將從科學文獻中系統提取的分析物理先驗，通過大型語言模型整合到一個特權教師模型中。我們採用圖掩蔽注意力層來捕捉輸入變量之間的複雜物理依賴性，這些變量顯示出嚴格的設定點或靜態與高頻時間特徵的組合。這些特權知識被蒸餾成一個輕量級的學生預測器以進行推斷。通過在五種不同的製造過程中進行全面實驗來評估該框架的可行性和穩健性。為了確保統計可靠性，考慮到小數據集的大小，採用了重複的K折交叉驗證技術來量化模型的穩定性和泛化能力。結果表明，所提出的框架在所有評估領域中始終實現高預測準確性。最重要的是，該架構顯示出顯著的容錯能力，即使在LLM衍生的分析先驗不理想或不完整的情況下，仍能保持穩健的預測性能。此外，學生預測器的推斷頻率超過6000 Hz，這使得在標準工業硬件上實現實時邊緣部署成為可能。這項工作為在數據有限的環境中彌合理論物理與實時工業監控之間的鴻溝提供了一個可擴展的解決方案。

##### **Model-Based and Data-Driven Hierarchical Control and Topology Co-Design for Robust Networked Systems**
2606.11596v1 by Shirantha Welikala, Zihao Song, Hai Lin, Panos J. Antsaklis

In this paper, we consider a class of networked systems comprising an interconnected set of linear subsystems, disturbance inputs, and performance outputs. Using dissipativity theory, we first propose a model-based hierarchical control design strategy to ensure the closed-loop networked system is dissipative from its disturbance inputs to performance outputs. This involves designing local controllers for each subsystem to enforce local dissipativity guarantees, which are then exploited to co-design distributed global controllers and the interconnection topology to enforce global dissipativity guarantees while optimizing interconnection topology costs. The overall design process requires only solving a sequence of linear matrix inequality (LMI) problems, thereby retaining compositionality and decentralizability while avoiding non-convex, iterative design processes that are inefficient and centralized. This model-based hierarchical control design strategy assumes the knowledge of the subsystem dynamics, which may not hold in many real-world networked systems. Motivated by this, we also propose a data-driven hierarchical control design strategy that assumes only the availability of rich input-state-output trajectory data from the subsystems. The proposed data-driven design process assumes that the unknown disturbances affecting the subsystem dynamics are bounded by a quadratic matrix inequality (relaxing conventional bounds) and accounts for this by using the matrix S-lemma. Finally, the effectiveness of the proposed model-based and data-driven hierarchical control designs is illustrated for a networked system representing a DC microgrid, with the aim of enforcing robust (dissipative) voltage regulation and current sharing.

摘要：在本文中，我們考慮一類由互聯的線性子系統、擾動輸入和性能輸出組成的網絡系統。<lb/>利用耗散理論，我們首先提出一種基於模型的分層控制設計策略，以確保閉環網絡系統從其擾動輸入到性能輸出是耗散的。<lb/>這涉及為每個子系統設計局部控制器，以強制執行局部耗散性保證，然後利用這些保證共同設計分佈式全局控制器和互聯拓撲，以強制執行全局耗散性保證，同時優化互聯拓撲成本。<lb/>整體設計過程僅需解決一系列線性矩陣不等式（LMI）問題，從而保持組合性和去中心化，同時避免效率低下且集中化的非凸迭代設計過程。<lb/>這種基於模型的分層控制設計策略假設已知子系統動態，但在許多現實世界的網絡系統中可能並不成立。<lb/>因此，我們還提出了一種數據驅動的分層控制設計策略，僅假設可獲得來自子系統的豐富輸入-狀態-輸出軌跡數據。<lb/>所提出的數據驅動設計過程假設影響子系統動態的未知擾動受到二次矩陣不等式的界限（放寬傳統界限），並通過使用矩陣S-引理來考慮這一點。<lb/>最後，所提出的基於模型和數據驅動的分層控制設計在代表直流微電網的網絡系統中得到了有效性展示，旨在強制執行穩健（耗散）電壓調節和電流共享。

##### **GraphInfer-Bench: Benchmarking LLM's Inference Capability on Graphs**
2606.11562v1 by Zhuoyi Peng, Jingzhou Jiang, Hanlin Gu, Lixin Fan, Yi Yang

Graph analysis underlies many applications whose answers cannot be looked up in a single record or retrieved along a path: laundering rings, drug repurposing, user preference, and scientific theme are all inferred from a node together with its neighbourhood. We introduce GraphInfer-Bench, a benchmark for whether LLMs can perform this graph inference: producing an open-ended answer that no single node supports and no path retrieves. Existing graph-QA protocols cannot test this capability: algorithm simulation, node classification, single-node description, KG-QA, and GraphRAG all admit answers retrievable from one node or along a path. GraphInfer-Bench defines five tasks along Description (what a region is) and Comparison (how regions differ), each constructed so the ground truth lives in no single node. The release contains 42,000 samples across six real-world graphs, produced automatically and screened by a four-layer quality-control protocol. We evaluate four method families against the same tasks: graph-token alignment models, zero-shot frontier closed-source LLMs, Graph2Text supervised fine-tuning, and plain GNNs as a structural reference. No method family closes the gap. Graph-token alignment partially handles description tasks (relational, theme) but collapses on comparison tasks. Frontier LLMs lead on outlier detection and community partition among LLM-based methods but lag on masked-node prediction. Graph2Text SFT is the strongest LLM-based method on the description side yet falls behind frontier LLMs on comparison. Across every task, plain GNNs match or beat the strongest LLM-based row, with the largest margin on community detection. GraphInfer-Bench surfaces graph inference as an open capability gap rather than a property of any one architecture.

摘要：圖形分析是許多應用的基礎，這些應用的答案無法在單一記錄中查找或沿著某條路徑檢索：洗錢環、藥物再利用、用戶偏好和科學主題都是從一個節點及其鄰域推斷而來的。我們推出了GraphInfer-Bench，這是一個基準，用於檢測LLMs是否能夠執行這種圖形推斷：產生一個沒有單一節點支持且無法沿著任何路徑檢索的開放式答案。現有的圖形問答協議無法測試這一能力：算法模擬、節點分類、單節點描述、KG-QA和GraphRAG都允許從一個節點或沿著一條路徑檢索答案。GraphInfer-Bench定義了五個任務，涵蓋描述（某個區域是什麼）和比較（區域之間的差異），每個任務的設計使得真實答案不會存在於任何單一節點中。該釋放包含42,000個樣本，來自六個真實世界的圖形，自動生成並經過四層質量控制協議篩選。我們針對相同任務評估了四個方法系列：圖形標記對齊模型、零樣本前沿封閉源LLMs、Graph2Text監督微調，以及作為結構參考的普通GNN。沒有任何方法系列縮小了差距。圖形標記對齊在描述任務（關係、主題）上部分處理，但在比較任務上失敗。前沿LLMs在異常檢測和社區劃分方面領先於基於LLM的方法，但在遮蔽節點預測上落後。Graph2Text SFT在描述方面是最強的基於LLM的方法，但在比較上落後於前沿LLMs。在每個任務中，普通GNN的表現與最強的基於LLM的方法相當或更好，在社區檢測上差距最大。GraphInfer-Bench將圖形推斷呈現為一個開放的能力差距，而不是任何一種架構的特性。

##### **LLMs+Graphs: Toward Graph-Native, Synergistic AI Systems**
2606.11560v1 by Arijit Khan, Longxu Sun, Xin Huang

Large Language Models (LLMs) have advanced rapidly, but their limitations in structured and multi-hop reasoning underscore the need for graph-native, synergistic artificial intelligence (AI) systems. Graph-structured data underpins critical applications across social, biological, financial, transportation, web, and knowledge domains, making it essential to understand how LLMs can leverage graph computation for grounded, context-rich inference. Three complementary synergies are emerging: LLMs augmented with graph computation for retrieval and reasoning; bidirectional integration between LLMs and knowledge graphs (KGs), where LLMs support KG construction and curation while KGs enforce semantic constraints and factual consistency; and AI agents strengthened by graph algorithms for planning, decision making, and multi-step reasoning. In parallel, LLMs introduce new capabilities for graph data management and graph machine learning (ML) through natural language interfaces and hybrid LLM-graph neural network (GNN) pipelines. This tutorial synthesizes the algorithms, systems, and design principles driving these converging directions, offering data science and data mining researchers a unified perspective on integrating LLMs, graph data management, graph mining, graph ML, and agentic computation into next-generation graph-native AI systems.

摘要：大型語言模型（LLMs）迅速發展，但它們在結構化和多跳推理方面的局限性凸顯了對圖形原生、協同人工智慧（AI）系統的需求。圖形結構數據支撐著社會、生物、金融、交通、網絡和知識領域的關鍵應用，因此理解LLMs如何利用圖形計算進行基於實體的、豐富上下文的推理至關重要。三種互補的協同效應正在出現：增強LLMs的圖形計算以進行檢索和推理；LLMs與知識圖（KGs）之間的雙向整合，其中LLMs支持KG的構建和管理，而KG則強制執行語義約束和事實一致性；以及通過圖形算法增強的AI代理，用於規劃、決策和多步推理。與此同時，LLMs通過自然語言接口和混合LLM-圖形神經網絡（GNN）管道引入了圖形數據管理和圖形機器學習（ML）的新能力。本教程綜合了推動這些融合方向的算法、系統和設計原則，為數據科學和數據挖掘研究人員提供了將LLMs、圖形數據管理、圖形挖掘、圖形ML和代理計算整合到下一代圖形原生AI系統中的統一視角。

##### **Privacy-Preserving Federated Autoencoder for ECG Anomaly Detection on Edge Devices**
2606.11556v1 by Kaan Arda Akyol, Jakub Kacper Szeląg, Aydin Abadi, Maha Alghamdi, Ghadah Albalawi, Ghouse Ibrahim Kaleelullah, Hilal Tutus, Sarah Al Subaiei, Shardul Kapse, Syed Mohammed Raheeb, Mujeeb Ahmed, Rehmat Ullah

Continuous electrocardiography (ECG) monitoring could surface rhythm abnormalities before they escalate into cardiovascular events. However, a deployable system must satisfy three requirements simultaneously: legal-grade privacy (GDPR, HIPAA), real-time inference on constrained edge hardware, and detection quality under non-IID cross-hospital data.
  We design and evaluate an end-to-end federated system addressing all three for unsupervised 12-lead ECG anomaly detection on PTB-XL dataset, combining three autoencoder families (VanillaAE, ConvAE, VAE), Flower-based federated averaging (FedAvg) across ten simulated hospitals, client-side differentially private SGD (DP-SGD) with a Rényi-DP accountant, and 8-bit integer (INT8) post-training quantization with Raspberry Pi 4 benchmarking. Our main contributions are: an empirical characterization of how these mechanisms compose, practical DP-specific recommendations, and technical and security insights for a clinically sensitive setting. Federated learning matches or exceeds the centralized baseline across all architectures (ConvAE federated area under the ROC curve, AUROC, $0.782$), and an $\varepsilon$ sweep identifies $\varepsilon=4$ as the recommended clinical operating point. INT8 quantization roughly halves model size and cuts Pi 4 latency by up to $44%$ with $<0.12%$ AUROC loss. Crucially, DP and quantization penalties are empirically independent, so practitioners need not trade a strong privacy guarantee for a compact edge footprint. To our knowledge, this is the first system combining federated learning, formal $(\varepsilon,δ)$-DP, unsupervised reconstruction-based detection, and quantized AArch64 deployment.

摘要：持續的心電圖（ECG）監測可以在心血管事件升級之前發現心律異常。然後，一個可部署的系統必須同時滿足三個要求：法律級別的隱私（GDPR、HIPAA）、在受限邊緣硬體上的即時推斷，以及在非獨立同分佈的跨醫院數據下的檢測質量。<lb/>  
我們設計並評估了一個端到端的聯邦系統，針對PTB-XL數據集進行無監督的12導聯ECG異常檢測，解決了這三個問題，結合了三個自編碼器家族（VanillaAE、ConvAE、VAE）、基於Flower的聯邦平均（FedAvg）在十個模擬醫院之間進行、客戶端差異隱私隨機梯度下降（DP-SGD）與Rényi-DP會計師，以及使用Raspberry Pi 4基準測試的8位整數（INT8）後訓練量化。我們的主要貢獻是：對這些機制如何組合的實證特徵描述、實用的DP特定建議，以及對臨床敏感環境的技術和安全見解。聯邦學習在所有架構中都匹配或超過集中基準（ConvAE的ROC曲線下的聯邦面積，AUROC，$0.782$），並且一個$\varepsilon$掃描確定$\varepsilon=4$為推薦的臨床操作點。INT8量化大約將模型大小減半，並將Pi 4的延遲減少多達$44\%$，且AUROC損失小於$0.12\%$。關鍵是，DP和量化懲罰在實證上是獨立的，因此從業者不必為了緊湊的邊緣佔用而犧牲強大的隱私保證。據我們所知，這是第一個結合聯邦學習、正式的$(\varepsilon,δ)$-DP、無監督重建基礎檢測和量化AArch64部署的系統。

##### **SkillJuror: Measuring How Agent Skill Organization Changes Runtime Behavior**
2606.11543v1 by Zhiyu Chen, Zihan Guo, Bo Huang, Bingwei Lu, Jianghao Lin, Yuanjian Zhou, Weinan Zhang

Agent Skills augment large language model (LLM) agents with procedural knowledge at inference time, but current benchmarks rarely distinguish what a Skill says from how it is organized. We study this distinction through Progressive Disclosure, where a concise root file points agents to supporting resources on demand, and compare it with a normalized flat baseline. We present SkillJuror, a framework for evaluating Skill writing paradigms through semantically controlled variants, matched multi-trial evaluations, and trajectory evidence while holding task knowledge fixed. In an 82-task SkillsBench study, Progressive Disclosure changes runtime behavior before aggregate outcomes: distinct Skill resources touched per trajectory rise from 1.18 to 3.85, and effective uptake events rise from 1.33 to 3.92. It also yields 17 additional verifier-passing trials out of 410 matched trials (+4.1%) over the normalized flat baseline. The benefit is task-dependent. Progressive Disclosure helps when supporting resources guide implementation, checking, or repair, but is weaker when success hinges on exact output conventions, numerical thresholds, or long artifact-generation pipelines. These results show that Skill organization is not mere presentation: it can change how agents search and apply procedural knowledge, while outcome gains depend on whether the exposed resources are actionable for the task. Code is available at https://github.com/zhiyuchen-ai/skill-juror.

摘要：代理技能在推理時增強大型語言模型（LLM）代理的程序知識，但目前的基準很少區分技能的內容與其組織方式。<lb/>我們通過漸進式披露來研究這一區別，其中一個簡潔的根文件根據需求指向支持資源，並將其與標準化的平坦基準進行比較。<lb/>我們提出了SkillJuror，一個通過語義控制變體、匹配的多次試驗評估和軌跡證據來評估技能寫作範式的框架，同時保持任務知識不變。<lb/>在一項82任務的SkillsBench研究中，漸進式披露在總體結果之前改變了運行時行為：每條軌跡接觸的不同技能資源從1.18上升到3.85，有效的獲取事件從1.33上升到3.92。<lb/>它還在410次匹配試驗中產生了17個額外的驗證者通過試驗（+4.1%），超過了標準化的平坦基準。<lb/>這一好處是依賴於任務的。<lb/>當支持資源指導實施、檢查或修復時，漸進式披露有助於任務，但當成功取決於精確的輸出約定、數值閾值或長期產物生成管道時，其效果較弱。<lb/>這些結果表明，技能組織並非僅僅是展示：它可以改變代理搜索和應用程序知識的方式，而結果增益則取決於所暴露的資源是否對任務可行。<lb/>代碼可在 https://github.com/zhiyuchen-ai/skill-juror 獲得。

##### **Hubs or Fringes: Pretraining Data Selection via Web Graph Centrality**
2606.11499v1 by Vedant Badoni, Danqi Chen, Xinyi Wang

The performance of modern language models depends critically on pretraining data composition. Yet existing data selection methods rely on auxiliary classifiers for document scoring or mixture optimization, adding computational overhead and dependence on labeled data. We propose WebGraphMix, a lightweight data selection framework that computes structural centrality scores over the Common Crawl host-level web graph and uses them to vary the proportion of central versus peripheral documents in the pretraining mixture. We hypothesize that central hosts expose models to reusable abstractions, while peripheral hosts encode specialized, long-tail knowledge. WebGraphMix computes centrality scores efficiently at web scale, requiring no model training, labeled data, or downstream supervision. We integrate WebGraphMix into the DataComp-LM pipeline and train models at 400M and 1B parameter scales with 8B and 28B tokens respectively, evaluating on 23 tasks ranging from factual knowledge to symbolic reasoning. Our experiments show that central and peripheral web regions encode complementary capabilities. Mixture combining both at a ratio of 1:1 achieves 41.4% on average, compared to 39.8% for uniform sampling. Combining structural scores with document-level quality classifier scores further improves performance to 43.8%. These findings demonstrate that web graph topology is a meaningful axis for pretraining data curation, capturing information that is largely orthogonal to existing content-based approaches.

摘要：現代語言模型的性能在很大程度上依賴於預訓練數據的組成。<lb/>然而，現有的數據選擇方法依賴於輔助分類器進行文檔打分或混合優化，這增加了計算開銷並依賴於標記數據。<lb/>我們提出了WebGraphMix，一種輕量級的數據選擇框架，該框架計算Common Crawl主機級別網絡圖的結構中心性分數，並利用這些分數來改變預訓練混合中中心文檔與周邊文檔的比例。<lb/>我們假設中心主機使模型接觸到可重用的抽象，而周邊主機則編碼專門的長尾知識。<lb/>WebGraphMix在網絡規模上高效計算中心性分數，無需模型訓練、標記數據或下游監督。<lb/>我們將WebGraphMix整合到DataComp-LM管道中，並在400M和1B參數規模上訓練模型，分別使用8B和28B標記，並在23個任務上進行評估，這些任務涵蓋了從事實知識到符號推理的範疇。<lb/>我們的實驗顯示，中心和周邊的網絡區域編碼了互補的能力。<lb/>以1:1的比例混合兩者的結果平均達到41.4%，而均勻抽樣則為39.8%。<lb/>將結構分數與文檔級質量分類器分數結合進一步提高了性能，達到43.8%。<lb/>這些發現表明，網絡圖拓撲是一個有意義的預訓練數據策劃軸心，捕捉到的信息在很大程度上與現有的基於內容的方法正交。

##### **Building Social World Models with Large Language Models**
2606.11482v1 by Haofei Yu, Yining Zhao, Guanyu Lin, Jiaxuan You

Understanding and predicting how social beliefs evolve in response to events -- from policy changes to scientific breakthroughs -- remains a fundamental challenge in social science. Given LLMs' commonsense knowledge and social intelligence, we ask: Can LLMs model the dynamics of social beliefs following social events? In this work, we introduce the concept of the Social World Model (SWM), a general framework designed to capture how social beliefs evolve in response to major events. SWM learns state-transition functions for social beliefs by mining temporal patterns in social data and optimizing the evidence lower bound, without the need for explicit human annotations linking events to belief shifts, or for expensive census data. To evaluate SWM, we introduce a benchmark, SWM-bench, derived from real-world prediction markets, specifically Kalshi and Polymarket. SWM-bench includes over 12k data points for social belief prediction tasks spanning diverse domains such as politics, finance, and cryptocurrency. Our experimental results show that SWM significantly outperforms time-series foundation models, achieving state-of-the-art results on Kalshi data and demonstrating competitive performance on Polymarket data, while offering interpretable insights into the underlying mechanisms of social belief dynamics.

摘要：理解和預測社會信念如何隨著事件（從政策變更到科學突破）而演變，仍然是社會科學中的一個基本挑戰。考慮到LLMs的常識知識和社會智慧，我們提出：LLMs能否模擬社會事件後社會信念的動態？在這項工作中，我們引入了社會世界模型（SWM）的概念，這是一個旨在捕捉社會信念如何隨重大事件而演變的通用框架。SWM通過挖掘社會數據中的時間模式並優化證據下界，學習社會信念的狀態轉換函數，而無需明確的人類註釋將事件與信念變化聯繫起來，也無需昂貴的普查數據。為了評估SWM，我們引入了一個基準，SWM-bench，該基準源自現實世界的預測市場，特別是Kalshi和Polymarket。SWM-bench包含超過12,000個數據點，用於涵蓋政治、金融和加密貨幣等多個領域的社會信念預測任務。我們的實驗結果顯示，SWM的表現顯著優於時間序列基礎模型，在Kalshi數據上達到最先進的結果，並在Polymarket數據上展現出競爭力的表現，同時提供了對社會信念動態基本機制的可解釋見解。

##### **INFRAMIND: Infrastructure-Aware Multi-Agent Orchestration**
2606.11440v1 by Ahasan Kabir, Jiaqi Xue, Mengxin Zheng, Qian Lou

Existing multi-agent LLM orchestration methods, ranging from brute-force ensembles to learned routers, select models and topologies based on task and model features. However, these methods do not consider the runtime state of the serving infrastructure. On shared GPU clusters under concurrent load, this infrastructure blindness causes systematic resource underutilization: preferred models accumulate deep request queues while equally capable alternatives sit idle. In multi-agent pipelines, where each query triggers multiple sequential model calls, these delays then compound across every downstream step. Closing this gap is challenging because the relevant infrastructure signals (queue depths, KV-cache pressure, latencies) are dynamic and noisy, and they must drive three different decisions: planning, per-step routing, and scheduling. We introduce INFRAMIND, a framework that makes the entire multi-agent stack infrastructure-aware. An infra-aware planner conditions topology and role selection on real-time system load and remaining budget, biasing toward simpler graphs under congestion and richer ones at low load. An infra-aware executor then observes per-model queue depths, cache utilization, and response latencies at each agent step to decide which model to call and how deeply to reason; a budget-aware scheduler further reorders each model's queue so that urgent requests are served first. Cast as a hierarchical constrained MDP and solved end-to-end via reinforcement learning, the system learns to balance quality against latency automatically. Across five benchmarks, INFRAMIND delivers up to +7.6 pp accuracy over the prior baseline at low load with up to 7x lower latency, and sustains up to 99.9% SLO compliance under high load where every baseline drops below 50%.

摘要：現有的多智能體 LLM 協調方法，從暴力集成到學習路由器，根據任務和模型特徵選擇模型和拓撲。<lb/>然而，這些方法並未考慮服務基礎設施的運行時狀態。<lb/>在共享 GPU 集群下的並發負載中，這種基礎設施盲點導致系統性資源未充分利用：首選模型積累了深度請求隊列，而同樣能夠的替代方案則閒置。<lb/>在多智能體管道中，每個查詢觸發多個連續的模型調用，這些延遲在每一步驟中進一步累積。<lb/>填補這一空白具有挑戰性，因為相關的基礎設施信號（隊列深度、KV 緩存壓力、延遲）是動態且嘈雜的，並且它們必須驅動三個不同的決策：規劃、每步路由和調度。<lb/>我們介紹了 INFRAMIND，一個使整個多智能體堆棧具備基礎設施感知的框架。<lb/>一個具備基礎設施感知的規劃器根據實時系統負載和剩餘預算對拓撲和角色選擇進行條件設定，在擁擠時偏向於較簡單的圖形，而在低負載時則偏向於較豐富的圖形。<lb/>一個具備基礎設施感知的執行器則在每個智能體步驟中觀察每個模型的隊列深度、緩存利用率和響應延遲，以決定調用哪個模型以及推理的深度；一個具備預算感知的調度器進一步重新排序每個模型的隊列，以便優先處理緊急請求。<lb/>該系統被視為一個分層約束 MDP，並通過強化學習端到端求解，自動學習在質量與延遲之間取得平衡。<lb/>在五個基準測試中，INFRAMIND 在低負載下提供了比先前基準高出 +7.6 pp 的準確度，並且延遲降低了最多 7 倍，在高負載下保持了高達 99.9% 的服務水平協議合規性，而每個基準則降至 50% 以下。

##### **Towards a Bridge Layer Between Bibliographic and Formalized Mathematical Knowledge**
2606.11430v1 by A. Mayeux

Mathematical knowledge is split between bibliographic databases (e.g., MathSciNet, zbMATH Open) and formal proof libraries (e.g., Lean mathlib), preventing unified access between published results and their formalizations. We propose a relational bridge-database that aligns publication metadata with formal artifacts, providing an interoperability layer between mathematical literature and machine-verifiable proofs. We introduce a paper-level formalization score that measures how much of a publication is covered in formal systems. As a feasibility study, we show how such scores can be estimated via cross-document alignment between informal texts and Lean formalizations, enabling large-scale analysis of formalization coverage. This framework is a first step toward integrating bibliographic and formal mathematical ecosystems into scalable, machine-actionable knowledge graphs linking publications to formal proof objects.

摘要：數學知識分散在文獻資料庫（例如，MathSciNet、zbMATH Open）和正式證明庫（例如，Lean mathlib）之間，這阻礙了已發表結果與其形式化之間的統一訪問。我們提出了一個關聯橋接資料庫，將出版元數據與正式文物對齊，提供數學文獻與機器可驗證證明之間的互操作性層。我們引入了一個論文級別的形式化分數，衡量一篇出版物在正式系統中被覆蓋的程度。作為可行性研究，我們展示了如何通過非正式文本與Lean形式化之間的跨文檔對齊來估計這些分數，從而實現對形式化覆蓋的規模化分析。這一框架是將文獻和正式數學生態系統整合成可擴展的、機器可操作的知識圖譜，將出版物與正式證明對象連結的第一步。

##### **A PubMed-Scale Dataset of Structured Biomedical Abstracts**
2606.11361v1 by Chia-Hsuan Chang, Haerin Song, Brian Ondov, Hua Xu

Structured abstracts are important for biomedical literature processing, by facilitating information retrieval, text mining, and knowledge synthesis. However, a vast portion of abstracts indexed in PubMed remain unstructured, presenting a significant bottleneck for downstream text-processing workflows and applications. To resolve this limitation, we introduce Structured PubMed, a comprehensive corpus of section-labeled biomedical abstracts compiled from the complete PubMed database, encompassing over 23.2 million research-article records. The corpus is divided into two distinct subsets: a collection of 5.9 million author-structured abstracts parsed from official XML files, and an automatically labeled collection of 17.2 million originally unstructured abstracts structured via a verbatim-extraction Large Language Model pipeline. Every record is harmonized under a unified five-section schema and mapped to its original PubMed identifier, publication type, and publication date. This dataset can be utilized to train sentence-classification models, benchmark text-segmentation architectures, and perform large-scale, section-specific information extraction at an unprecedented PubMed-wide scale.

摘要：結構化摘要對於生物醫學文獻處理非常重要，因為它促進了信息檢索、文本挖掘和知識綜合。<lb/>然而，PubMed中索引的絕大部分摘要仍然是非結構化的，這對下游文本處理工作流程和應用程序造成了重大瓶頸。<lb/>為了解決這一限制，我們推出了結構化PubMed，這是一個全面的生物醫學摘要語料庫，包含來自完整PubMed數據庫的部分標記摘要，涵蓋超過2320萬條研究文章記錄。<lb/>該語料庫分為兩個不同的子集：一個是從官方XML文件中解析出的590萬條作者結構化摘要的集合，另一個是通過逐字提取大型語言模型管道結構化的1720萬條最初非結構化摘要的自動標記集合。<lb/>每條記錄都在統一的五部分架構下進行了協調，並映射到其原始的PubMed標識符、出版類型和出版日期。<lb/>這個數據集可以用來訓練句子分類模型、基準文本分割架構，並在前所未有的PubMed範圍內進行大規模、特定部分的信息提取。

##### **Quantized Stochastic Primal-Dual Methods for Distributed Optimization under Relaxed Global Geometry**
2606.11339v1 by Susmit Sarkar, Abhinav Raghuvanshi, Kushal Chakrabarti, Mayank Baranwal

We study distributed optimization with stochastic gradients and finite-bit communication modeled by random (unbiased) quantization. We propose q-PDGD, a quantized stochastic primal-dual method, and analyze it under relaxed global geometry. Under restricted secant inequality (RSI), a constant step-size yields linear contraction to an explicit neighborhood determined by gradient noise, quantization distortion, and network connectivity, while a diminishing step-size achieves O(1/k) convergence without shared-minimizer assumptions. Under Polyak-Lojasiewicz (PL) inequality, we obtain linear-to-neighborhood convergence in the same stochastic quantized setting. Our results match the best-known centralized stochastic rates in oracle complexity, and are supported by experiments demonstrating the predicted tradeoffs between quantization level, step-size choice, and graph structure.

摘要：我們研究具有隨機梯度和有限位元通信的分散優化，這些通信由隨機（無偏）量化建模。我們提出了 q-PDGD，一種量化的隨機原始-對偶方法，並在放寬的全局幾何下進行分析。在限制割線不等式（RSI）下，恆定的步長產生線性收斂到由梯度噪聲、量化失真和網絡連通性決定的明確鄰域，而逐漸減小的步長則在不需要共享最小化者假設的情況下實現 O(1/k) 收斂。在 Polyak-Lojasiewicz（PL）不等式下，我們在相同的隨機量化設置中獲得線性到鄰域的收斂。我們的結果與在預言者複雜度中已知的最佳集中隨機速率相匹配，並且有實驗支持預測的量化水平、步長選擇和圖結構之間的權衡。

##### **A Unifying Lens on Supervised Fine-Tuning Through Target Distribution Design**
2606.11189v1 by Tong Xie, Yuanhao Ban, Yunqi Hong, Sohyun An, Yihang Chen, Cho-Jui Hsieh

Supervised fine-tuning (SFT) typically maximizes the likelihood of every token in a demonstrated trajectory. However, an observed token can be non-unique, noisy, or misaligned with the model prior. Strictly fitting toward this one-hot target may be suboptimal, especially when the pretrained model encodes a rich knowledge prior. In this work, we reinterpret SFT as target distribution design: instead of studying only the loss objective, we analyze the token-level target that the loss drives the model to match. We introduce the Q-target framework, which decomposes SFT supervision into two explicit choices: (1) how strongly to rely on the observed token, and (2) how to allocate the remaining probability mass over alternatives. This perspective unifies many existing SFT variants as implicit choices of the target distribution Q. Building on this view, we propose Target-SFT which constructs the training objective directly from the desired target distribution. This method consistently outperforms across the ten reasoning dataset-model settings evaluated, showing the effectiveness of this target-based approach. Overall, our formulation reveals a more fundamental design principle for SFT training and opens a broader search space for SFT objectives.

摘要：監督微調 (SFT) 通常會最大化每個在示範軌跡中的標記的可能性。<lb/>然而，觀察到的標記可能是非唯一的、嘈雜的，或與模型的先驗不一致。<lb/>嚴格地朝著這個單一目標進行擬合可能不是最佳的，特別是當預訓練模型編碼了豐富的知識先驗時。<lb/>在本研究中，我們將 SFT 重新詮釋為目標分佈設計：我們不僅研究損失目標，還分析損失驅動模型匹配的標記級目標。<lb/>我們引入了 Q-target 框架，將 SFT 監督分解為兩個明確的選擇：(1) 依賴觀察到的標記的強度，和 (2) 如何將剩餘的概率質量分配給其他選擇。<lb/>這種觀點統一了許多現有的 SFT 變體，作為目標分佈 Q 的隱含選擇。<lb/>基於這一觀點，我們提出了 Target-SFT，該方法直接從所需的目標分佈構建訓練目標。<lb/>這種方法在評估的十個推理數據集-模型設置中始終表現優於其他方法，顯示了這種基於目標的方法的有效性。<lb/>總體而言，我們的公式揭示了 SFT 訓練的一個更基本的設計原則，並為 SFT 目標開辟了更廣泛的搜索空間。

##### **FlowBank: Query-Adaptive Agentic Workflows Optimization through Precompute-and-Reuse**
2606.11290v1 by Lingzhi Yuan, Chenghao Deng, Fangxu Yu, Souradip Chakraborty, Mohammad Rostami, Furong Huang

Large Language Model (LLM)-based multi-agent systems are increasingly powerful, but current agentic workflow optimization paradigms make an unsatisfying trade-off. Task-level methods spend substantial offline compute yet deploy only a single workflow, leaving complementary candidates unused, while query-level methods synthesize a new workflow per query at substantial inference cost. Our motivating analysis shows these paradigms are more complementary than competing: workflows discovered during offline search often solve different subsets of queries, and many queries handled by expensive query-level generation can already be solved by cheaper precomputed workflows. This suggests a different objective: rather than searching for one universally best workflow or regenerating one per instance, we should build a compact bank of reusable, complementary workflows and select among them adaptively at inference time. Doing so requires solving three coupled problems: generating complementary rather than redundant candidates, compressing them into a small deployable portfolio, and assigning each query to the right workflow under a performance-cost trade-off. To this end, we present FlowBank, a three-stage framework for portfolio-based agentic workflow optimization. Diversifying proposes DiverseFlow to steer search toward under-covered queries and produce a high-coverage candidate pool. Curating proposes CuraFlow to compress this pool into a compact portfolio with minimal redundancy. Matching casts deployment as edge-value prediction on a query-workflow bipartite graph and routes each incoming query to the portfolio member with the best predicted utility. Across five benchmarks, FlowBank achieves the highest average score among the evaluated methods while remaining cost-competitive, improving over the strongest automated and handcrafted baselines by 4.26% and 14.92% relative, respectively.

摘要：大型語言模型（LLM）基礎的多代理系統越來越強大，但當前的代理工作流程優化範式卻做出了不令人滿意的權衡。任務級別的方法花費大量的離線計算，但僅部署單一工作流程，讓互補的候選者未被利用，而查詢級別的方法則在相當大的推理成本下為每個查詢合成新的工作流程。我們的動機分析顯示這些範式更具互補性而非競爭性：在離線搜索中發現的工作流程通常解決不同子集的查詢，而許多由昂貴的查詢級生成處理的查詢，實際上已經可以由較便宜的預計算工作流程解決。這暗示著一個不同的目標：與其尋找一個普遍最佳的工作流程或為每個實例重新生成一個，不如建立一個緊湊的可重用互補工作流程庫，並在推理時自適應地從中選擇。這樣做需要解決三個耦合問題：生成互補而非冗餘的候選者，將它們壓縮成一個小型可部署的投資組合，以及在性能與成本的權衡下將每個查詢分配給正確的工作流程。為此，我們提出了FlowBank，一個基於投資組合的代理工作流程優化的三階段框架。多樣化提出了DiverseFlow，旨在引導搜索朝向未被充分覆蓋的查詢，並產生一個高覆蓋率的候選池。策展提出了CuraFlow，將這個候選池壓縮成一個最小冗餘的緊湊投資組合。匹配將部署視為查詢-工作流程二分圖上的邊值預測，並將每個進來的查詢路由到預測效用最佳的投資組合成員。在五個基準測試中，FlowBank在評估的方法中實現了最高的平均分數，同時保持成本競爭力，分別比最強的自動化和手工基準提高了4.26%和14.92%。

##### **Flaws in the LLM Automation Narrative**
2606.11166v1 by George Perrett, Javae Elliott, Jennifer Hill, Marc Scott

Large Language Models (LLMs) are increasingly described as performing at the level of human experts on knowledge economy tasks. These claims are primarily based on how LLMs perform on benchmarking tasks that measure average performance across standardized datasets. Primary limitations of many benchmarking tasks are that they often measure performance based on content directly included in LLM training data, and they frequently do not assess the reliability of LLM performance or the magnitude of LLM errors. However, in high stakes contexts, these qualities are critically important. Through a novel LLM benchmarking task that requires writing computer code to complete a data analysis task, we compare the performance of a frontier LLM against submissions from human experts and explicitly measure the variance of responses and the magnitude of errors. Our study reveals that the human experts perform better on average on a range of metrics and demonstrate less variability in performance. Our results provide evidence that LLMs do not consistently perform at the level of human experts and demonstrate the importance of measuring variance and assessing error magnitude in LLM benchmark evaluations.

摘要：大型語言模型（LLMs）越來越多地被描述為在知識經濟任務中達到人類專家的水平。這些說法主要基於LLMs在基準任務中的表現，這些任務測量的是在標準化數據集上的平均表現。許多基準任務的主要限制在於，它們通常根據直接包含在LLM訓練數據中的內容來衡量表現，並且經常不評估LLM表現的可靠性或LLM錯誤的大小。然而，在高風險的情境中，這些特質是至關重要的。通過一項新穎的LLM基準任務，要求編寫計算機代碼以完成數據分析任務，我們將前沿LLM的表現與人類專家的提交進行比較，並明確測量反應的變異性和錯誤的大小。我們的研究顯示，人類專家在多個指標上的平均表現更佳，並且表現的變異性較小。我們的結果提供了證據表明LLMs並不總是能夠達到人類專家的水平，並且顯示了在LLM基準評估中測量變異性和評估錯誤大小的重要性。

##### **ABC-Bench: An Agentic Bio-Capabilities Benchmark for Biosecurity**
2606.11150v1 by Andrew Bo Liu, Samira Nedungadi, Bryce Cai, Alex Kleinman, Harmon Bhasin, Seth Donoughe

Large language models (LLMs) are rapidly acquiring capabilities relevant to biological research, from literature synthesis to interpretation of experimental data. Increasingly, LLM agents can also perform in silico biology tasks that previously required experienced human biologists. These emerging AI capabilities offer new opportunities for scientific discovery and biomedical advances, but they also shift the landscape of biosecurity risks. To address this, we introduce the Agentic Bio-Capabilities Benchmark (ABC-Bench), a suite of tasks to measure agentic biosecurity-relevant capabilities. ABC-Bench evaluates LLM agents on both benign and dual-use biology tasks: writing code to operate liquid handling robots, designing DNA fragments for in vitro assembly, and evading DNA synthesis screening. These tasks require a combination of biology and software expertise. All tested LLM agents outperformed the median expert human baseliner on all three tasks. Agents performed highly on tasks drawing on published knowledge and well-documented protocols, and more weakly on a task requiring novel bioinformatics reasoning. In three wet-lab validation experiments, we found that OpenAI's o4-mini-high produced scripts that, when run on an OpenTrons liquid handling robot, successfully assembled DNA with expected sequences.

摘要：大型語言模型（LLMs）正在迅速獲得與生物研究相關的能力，從文獻綜述到實驗數據的解釋。越來越多的 LLM 代理也能執行以前需要經驗豐富的人類生物學家的計算生物學任務。這些新興的 AI 能力為科學發現和生物醫學進步提供了新的機會，但它們也改變了生物安全風險的格局。為了解決這個問題，我們介紹了代理生物能力基準（ABC-Bench），這是一套用於測量與生物安全相關的代理能力的任務。ABC-Bench 在良性和雙用途生物學任務上評估 LLM 代理：編寫操作液體處理機器人的代碼、設計用於體外組裝的 DNA 片段，以及躲避 DNA 合成篩查。這些任務需要生物學和軟件專業知識的結合。所有測試的 LLM 代理在這三項任務上均超過了中位數專家人類基準。代理在依賴已發表知識和良好記錄的協議的任務上表現優異，而在需要新穎生物信息學推理的任務上表現較弱。在三個濕實驗室驗證實驗中，我們發現 OpenAI 的 o4-mini-high 生成的腳本在 OpenTrons 液體處理機器人上運行時，成功組裝了具有預期序列的 DNA。

##### **Towards Autonomous Accelerator Design: FPGA Accelerator Generation with SECDA**
2606.11117v1 by Vinamra Sharma, Xingjian Fu, Jude Haris, José Cano

Designing FPGA-based accelerators for modern artificial intelligence workloads requires exploring a large and complex hardware design space that involves architectural parameters, data flow strategies, and memory hierarchies, making the process very time consuming. While existing methodologies such as SECDA enable rapid hardware-software co-design through SystemC simulation and FPGA execution, identifying efficient accelerator configurations remains a largely manual process requiring extensive domain knowledge. SECDA-DSE is a framework that integrates Large Language Models (LLMs) into the SECDA ecosystem to guide design space exploration (DSE) of FPGA-based accelerators. It combines a structured DSE Explorer for generating candidate architectures with an LLM Stack that performs reasoning-guided exploration using retrieval-augmented generation and chain-of-thought prompting, coupled with a feedback loop for iterative and reinforced refinement. Building on our previous work introducing SECDA-DSE, this paper extends its evaluation by generating three accelerator designs, including element-wise vector multiplication, 2D convolution, and matrix transpose, and performing end-to-end execution on FPGA hardware. The results show that SECDA-DSE can generate SECDA-compliant accelerator designs that are successfully synthesized and executed on FPGA hardware. Furthermore, the generated designs capture kernel-specific trade-offs between compute parallelism and data movement, highlighting the potential of LLM-guided exploration to adapt architectural configurations across diverse workloads while reducing exploration time and the need for extensive human expertise.

摘要：設計基於FPGA的加速器以應對現代人工智慧工作負載需要探索一個龐大且複雜的硬體設計空間，這涉及架構參數、數據流策略和記憶體層次，使得這個過程非常耗時。雖然現有的方法論如SECDA通過SystemC模擬和FPGA執行實現了快速的硬體-軟體共同設計，但識別高效的加速器配置仍然是一個主要依賴手動過程，需要廣泛的領域知識。SECDA-DSE是一個將大型語言模型（LLMs）整合進SECDA生態系統的框架，以指導基於FPGA的加速器的設計空間探索（DSE）。它結合了一個結構化的DSE探索器，用於生成候選架構，以及一個LLM堆疊，該堆疊使用檢索增強生成和思維鏈提示進行推理引導的探索，並配合一個反饋循環以進行迭代和強化的精煉。在我們之前的工作中介紹SECDA-DSE的基礎上，本文通過生成三個加速器設計，包括元素級向量乘法、2D卷積和矩陣轉置，並在FPGA硬體上進行端到端執行，擴展了其評估。結果顯示SECDA-DSE可以生成符合SECDA標準的加速器設計，這些設計成功地在FPGA硬體上進行合成和執行。此外，生成的設計捕捉了計算並行性和數據移動之間的內核特定權衡，突顯了LLM引導探索在適應多樣工作負載的架構配置方面的潛力，同時減少探索時間和對廣泛人類專業知識的需求。

##### **FADA: Accessible fetal ultrasound interpretation and annotation with a selectively distilled unified vision-language model**
2606.11106v1 by Mahmood Alzubaidi, Uzair Shah, Raden Muaz, Ines Abbes, Nader Mohammed, Abdullatif Magram, Khalid Alyafei, Mowafa Househ, Marco Agus

A global shortage of trained sonographers limits prenatal ultrasound screening in low- and middle-income countries, where over half of pregnant women receive no skilled sonography. Current deep learning approaches address detection, segmentation, or classification in isolation, each demanding a separate model and expert-specified labels at inference. We present FADA, a unified vision-language model built on Qwen3.5-VL that performs clinical interpretation, classification, detection, and segmentation through a single interpretation-first pipeline without external labels. FADA distills knowledge from four domain-specific foundation models (FetalCLIP, UltraSAM, USF-MAE, UltraFedFM) via offline pre-computed feature caching. Selective distillation, which applies feature alignment only to annotation tasks while interpretation relies on standard fine-tuning, consistently outperforms full distillation across most evaluation axes. The recommended variant, FADA-SKD, achieves 0.8820 mean Dice for segmentation, 0.7671 mAP@0.50 for detection, and 100% structured interpretation compliance. Expert sonographer validation across 237 images confirms clinically acceptable outputs in both autonomous and human-in-the-loop modes, with 73.5% of interpretations scoring perfectly under clinician guidance. The system is trainable on a single consumer GPU and deployable without cloud connectivity. We validate edge deployment by running the compressed 0.8B model on a commodity smartphone (Qualcomm Snapdragon 7 Gen 1, 12 GB RAM) using llama.cpp with GGUF quantization, completing the full 5-phase pipeline in approximately 60 seconds entirely offline. This establishes a practical pathway for integrating AI-assisted fetal assessment with portable ultrasound devices, directly addressing diagnostic access gaps in resource-constrained settings. Code, models, and data are available at https://github.com/mahmoodphd/FADA.

摘要：全球訓練有素的超聲醫生短缺限制了低收入和中等收入國家的產前超聲篩查，這些國家中有超過一半的孕婦沒有接受專業超聲檢查。當前的深度學習方法分別處理檢測、分割或分類，每個方法都需要一個單獨的模型和專家指定的標籤進行推斷。我們提出了FADA，一個基於Qwen3.5-VL的統一視覺-語言模型，通過單一的解釋優先管道執行臨床解釋、分類、檢測和分割，而不需要外部標籤。FADA通過離線預計算特徵緩存從四個特定領域的基礎模型（FetalCLIP、UltraSAM、USF-MAE、UltraFedFM）提煉知識。選擇性蒸餾僅將特徵對齊應用於標註任務，而解釋則依賴於標準微調，這在大多數評估指標上始終優於完全蒸餾。推薦變體FADA-SKD在分割中達到0.8820的平均Dice，在檢測中達到0.7671的mAP@0.50，以及100%的結構化解釋合規性。對237張圖像的專家超聲醫生驗證確認了在自主和人類參與模式下的臨床可接受輸出，其中73.5%的解釋在臨床醫生指導下得分完美。該系統可以在單個消費者GPU上進行訓練，並且可在沒有雲連接的情況下部署。我們通過在一部普通智能手機（高通Snapdragon 7 Gen 1，12 GB RAM）上運行壓縮的0.8B模型來驗證邊緣部署，使用llama.cpp進行GGUF量化，並在完全離線的情況下約60秒內完成完整的5階段管道。這為將AI輔助的胎兒評估與可攜式超聲設備整合建立了一條實用的途徑，直接解決了資源有限環境中的診斷接入差距。代碼、模型和數據可在https://github.com/mahmoodphd/FADA獲得。

##### **PhantomBench: Benchmarking the Non-existential Threat of Language Models**
2606.11105v1 by Haeji Jung, Hila Gonen

Hallucinations, where language models (LMs) generate factually ungrounded responses, pose serious risks, as users tend to blindly rely on them. This is particularly concerning in high-stakes domains, where consequences of such model behavior can lead to significant harms. Despite notable progress in understanding hallucinations, it remains unclear how reliably these models can recognize the limits of their knowledge. We introduce PhantomBench, the first large-scale benchmark of its kind, comprising more than 60K non-existent terms and entities derived from real concepts across diverse domains. Using our benchmark, we evaluate a total of 21 models of various types and sizes. We show staggering hallucination rates across the board (with average rates as high as 86.7% in some cases), and note that even frontier models surprisingly fail to abstain on non-existent concepts, especially when the input presumes their existence. We then show that PhantomBench can serve as a proxy for studying model behavior on rare concepts for which models are more prone to hallucinate. We also provide a pipeline to construct PhantomBench, enabling scalable generation of non-existent concepts tailored to the specific needs of researchers and practitioners.

摘要：幻覺，即語言模型（LMs）生成事實上無根據的回應，帶來了嚴重的風險，因為用戶往往會盲目依賴它們。這在高風險領域尤其令人擔憂，因為這種模型行為的後果可能導致重大損害。儘管在理解幻覺方面取得了顯著進展，但這些模型能多可靠地識別其知識的界限仍然不清楚。我們介紹了 PhantomBench，這是首個此類的大規模基準，包含超過 60K 個不存在的術語和實體，這些術語和實體源自於不同領域的真實概念。利用我們的基準，我們評估了總共 21 種不同類型和大小的模型。我們顯示出驚人的幻覺率（在某些情況下平均高達 86.7%），並指出即使是前沿模型在面對不存在的概念時也令人驚訝地無法避免，特別是當輸入假設它們存在時。我們接著展示了 PhantomBench 可以作為研究模型在稀有概念上的行為的代理，因為模型在這些概念上更容易產生幻覺。我們還提供了一個構建 PhantomBench 的流程，能夠根據研究人員和從業者的具體需求可擴展地生成不存在的概念。

##### **Null-Space Constrained Low-Rank Adaptation for Response-Specified Large Language Model Unlearning**
2606.10989v1 by Bocheng Ju, Jianhua Wang, Chengliang Liu, Xiaolin Chang

Large language model unlearning aims to suppress designated undesirable knowledge while preserving benign capabilities. Many unlearning objectives focus on suppressing undesired answers, while recent target-guided variants specify replacement behavior but still leave update locality largely unconstrained. This paper introduces \emph{Null-Space Constrained Response-Specified Unlearning} (NSRU), a projection-constrained low-rank framework for controlled LLM unlearning. NSRU uses an explicitly structured safe target response to specify the desired behavior for each forget query, while suppressing the original undesired content. To localize adaptation, NSRU estimates per-module retain subspaces from benign hidden representations and uses an orthogonal-projected low-rank parameterization to confine LoRA updates to the null space of the retain subspace. The resulting objective jointly optimizes safe-target learning, undesired-response suppression, and retention preservation under this constrained parameterization. We provide a local first-order analysis showing that the projected update reduces retain-side perturbations while preserving editable directions for shaping forget-query behavior. Experiments on TOFU show that NSRU effectively suppresses extractable forget-set knowledge while improving retain QA performance, model utility, and safe-target alignment over representative baselines. On WMDP, NSRU keeps hazardous-domain accuracy near the random-choice region while preserving broad and domain-adjacent MMLU utility. Ablation studies support the complementary roles of safe-target supervision, undesired-response suppression, retention loss, and null-space projected updates, while sensitivity and robustness analyses indicate stable behavior across the tested hyperparameter and prompt variations.

摘要：大型語言模型的去學習旨在抑制指定的不良知識，同時保留良性的能力。許多去學習目標專注於抑制不希望的答案，而最近的目標導向變體則明確指定替代行為，但仍然在更新的局部性上大致不受限制。本文介紹了\emph{Null-Space Constrained Response-Specified Unlearning}（NSRU），這是一個受限於投影的低秩框架，用於控制大型語言模型的去學習。NSRU使用明確結構的安全目標響應來指定每個遺忘查詢所需的行為，同時抑制原始的不希望內容。為了局部化適應，NSRU從良性的隱藏表示中估計每個模塊的保留子空間，並使用正交投影的低秩參數化將LoRA更新限制在保留子空間的零空間內。由此產生的目標在這一受限參數化下共同優化安全目標學習、不希望的響應抑制和保留保持。我們提供了一個局部的一階分析，顯示投影更新減少了保留側的擾動，同時保留了可編輯的方向以塑造遺忘查詢行為。在TOFU上的實驗表明，NSRU有效地抑制了可提取的遺忘集知識，同時改善了保留問答性能、模型效用和安全目標對齊，相較於代表性基準。在WMDP上，NSRU保持危險領域的準確性接近隨機選擇區域，同時保留廣泛和領域相鄰的MMLU效用。消融研究支持安全目標監督、不希望的響應抑制、保留損失和零空間投影更新的互補角色，而敏感性和穩健性分析則表明在測試的超參數和提示變化下行為穩定。

##### **Provenance Tracking in AI Compilers through the Lens of Coalgebra**
2606.10937v1 by Zilu Tian, Liying Liu

AI compilers aggressively rewrite computation graphs through normalization, lowering, and optimization, making it difficult to track the provenance of tensors and operators across compilation. Reliable provenance is essential for attaching platform-specific postprocessing, debugging compiler behavior, and validating transformations, yet existing solutions are either invasive or ad hoc under non-injective graph rewrites.
  We present a lightweight, generative approach to provenance tracking based on observational semantics. Instead of propagating identifiers through compiler passes, we observe graph transformations and reason about provenance in terms of observable computational actions. We formalize this approach using a coalgebraic model and bisimulation, which preserves provenance even when intermediate nodes are eliminated. Furthermore, we implement this approach in a prototype AI compiler COVAN, demonstrating stable provenance across compilation pipelines with minimal engineering overhead.

摘要：AI 編譯器透過正規化、降低和優化積極重寫計算圖，這使得在編譯過程中追蹤張量和運算子的來源變得困難。可靠的來源追蹤對於附加平台特定的後處理、調試編譯器行為和驗證轉換至關重要，但現有的解決方案在非單射圖重寫下要麼是侵入性的，要麼是臨時的。<lb/>  
我們提出了一種基於觀察語義的輕量級生成方法來進行來源追蹤。與其在編譯器過程中傳播標識符，我們觀察圖變換並根據可觀察的計算行為推理來源。<lb/>  
我們使用共代數模型和雙模擬形式化了這種方法，即使在消除中間節點時也能保留來源。此外，我們在原型 AI 編譯器 COVAN 中實現了這種方法，展示了在編譯管道中以最小的工程開銷實現穩定的來源追蹤。

##### **Trace Only What You Need: Structure-Aware On-Demand Hypergraph Memory for Long-Document Question Answering**
2606.10921v1 by Xiangjun Zai, Xingyu Tan, Chen Chen, Xiaoyang Wang, Wenjie Zhang

Long-document question answering (QA) requires large language models (LLMs) to reason over evidence scattered across lengthy documents, where answers often depend on event order, section-level context, and cross-part evidence connections. Although retrieval-augmented generation (RAG) reduces the input context by retrieving relevant evidence, existing structured RAG methods still face three limitations: costly query-agnostic knowledge organization, insufficient use of original document structure, and no reuse of historical reasoning experience. To address these limitations, we propose DocTrace, a multi-agent RAG framework for long-document QA that supports query-triggered knowledge organization, document-structure-aware and experience-guided reasoning. DocTrace preserves document hierarchy with a lightweight document structural tree index, constructs agent-shared hypergraph-structured working memory on demand during reasoning, and stores successful reasoning plans in graph-structured experience memory for future reuse, enabling adaptive exploration across related long-document questions. Experiments on four long-document QA datasets show that DocTrace achieves the best performance on three datasets, surpassing the strongest baseline, ComoRAG, by up to 8.85% in F1 and 4.40% in EM, while reducing the overall computational cost by 53.32%

摘要：長文問題回答（QA）需要大型語言模型（LLMs）對散布在冗長文檔中的證據進行推理，答案通常依賴於事件順序、部分級上下文和跨部分的證據連結。儘管檢索增強生成（RAG）通過檢索相關證據來減少輸入上下文，但現有的結構化RAG方法仍面臨三個限制：昂貴的查詢無關知識組織、對原始文檔結構的使用不足以及無法重用歷史推理經驗。為了解決這些限制，我們提出了DocTrace，一個支持查詢觸發的知識組織、文檔結構感知和經驗指導推理的多代理RAG框架。DocTrace通過輕量級文檔結構樹索引保留文檔層級，根據需求在推理過程中構建代理共享的超圖結構工作記憶，並將成功的推理計劃存儲在圖結構經驗記憶中以供未來重用，從而實現對相關長文檔問題的自適應探索。在四個長文檔QA數據集上的實驗表明，DocTrace在三個數據集上達到了最佳性能，F1指標超過最強基準ComoRAG達8.85%，EM指標達4.40%，同時將整體計算成本降低了53.32%。

##### **Large-scale semantic mapping of learner agency and autonomy reveals what measurement and generative AI research overlook**
2606.10881v1 by Fei Qin, Xiaobo Liu, Yaowen Zhang, Xuming Li, Fei Wang, Mutlu Cukurova, Jingjing Chen, Yu Zhang

Learner agency and autonomy are foundational to personal development, yet a pervasive "jingle-jangle" fallacy (i.e. identical terms denoting different constructs, distinct terms denoting identical ones) has substantially hindered cumulative knowledge. Treating meaning as a phenomenon constituted through use in linguistic practice, we extracted 8,954 definitions and 2,700 scale items from over 14,000 publications, to investigate how researchers actually used learner agency and autonomy with a semantic analysis pipeline. The definitional landscape of two constructs resolves into three dimensions: regulation and control of learning (task), intrinsic motivation and internal decision-making (person), and social-relational action (sociocultural), thereby empirically quantifying the jingle-jangle fallacy. Existing scales, however, systematically underrepresent the sociocultural dimension. Critically, current generative AI research in education concentrates on learning regulation and control, narrowing the behavioral repertoire that AI-mediated learning environments are designed to cultivate. Beyond conceptual clarification, this work carries direct implications for conceptualization, measurement, and practice towards supporting the multidimensional learner agency and autonomy.

摘要：學習者的能動性和自主性是個人發展的基礎，然而普遍存在的「叮噹-搖鈴」謬誤（即相同的術語表示不同的構念，不同的術語表示相同的構念）實質上阻礙了累積知識。將意義視為通過語言實踐中的使用構成的現象，我們從超過14,000篇出版物中提取了8,954個定義和2,700個量表項目，以調查研究者實際上如何使用學習者的能動性和自主性，並進行語義分析。這兩個構念的定義景觀分解為三個維度：學習的調節和控制（任務）、內在動機和內部決策（個人）以及社會關係行動（社會文化），從而實證量化了叮噹-搖鈴謬誤。然而，現有的量表系統性地低估了社會文化維度。關鍵是，目前在教育領域的生成性人工智慧研究集中於學習的調節和控制，縮小了人工智慧介導的學習環境所設計的行為範疇。除了概念澄清，這項工作對於支持多維度的學習者能動性和自主性在概念化、測量和實踐方面具有直接的影響。

##### **Pushing the Limits of LLM Tool Calling via Experiential Knowledge Integration and Activation**
2606.10875v1 by Yupu Hao, Zhuoran Jin, Huanxuan Liao, Kang Liu, Jun Zhao

Large language models (LLMs) rely on tool use to act as autonomous agents, yet often fail in multi-step execution due to insufficient tool-related knowledge and ineffective knowledge activation. Therefore, we present a systematic study on how knowledge influences tool-use performance, covering the stages of knowledge acquisition, activation, and internalization. In the knowledge acquisition stage, we acquire and evaluate various forms of experiential knowledge, and our analysis shows that simple instance-level knowledge can already provide strong and reliable gains, while abstract intent-level knowledge offers limited benefits. At inference time, to activate knowledge, we find that prompting LLM to expand the depth of reasoning yields diminishing returns, whereas expanding the width of reasoning by parallel sampling with aggregation more effectively activates latent experiential knowledge. At training time, for knowledge internalization, post-training with knowledge-augmented data further improves performance, with reinforcement learning outperforming supervised fine-tuning. Based on these insights, we propose the Knowledge-Augmented Tool Execution (KATE), a knowledge-augmented tool execution framework that integrates experiential knowledge with reasoning-width-expanded inference and knowledge-aware training. Experiments on BFCL-V3 and AppWorld demonstrate consistent and substantial improvements over strong baselines across model scales. Our Code is available at https://github.com/hypasd-art/KATE.

摘要：大型語言模型（LLMs）依賴工具使用來作為自主代理，但由於工具相關知識不足和知識激活不有效，常常在多步執行中失敗。<lb/>因此，我們提出了一項系統性研究，探討知識如何影響工具使用表現，涵蓋知識獲取、激活和內化的階段。<lb/>在知識獲取階段，我們獲取並評估各種形式的經驗知識，我們的分析顯示，簡單的實例級知識已經能提供強大且可靠的增益，而抽象的意圖級知識則提供有限的好處。<lb/>在推理時，為了激活知識，我們發現提示LLM擴展推理的深度會產生遞減的回報，而通過平行取樣和聚合擴展推理的寬度則更有效地激活潛在的經驗知識。<lb/>在訓練時，為了知識內化，使用增強知識的數據進行後訓練進一步提高了性能，強化學習的表現超過了監督微調。<lb/>基於這些見解，我們提出了知識增強工具執行（KATE），這是一個將經驗知識與推理寬度擴展的推理和知識感知訓練相結合的知識增強工具執行框架。<lb/>在BFCL-V3和AppWorld上的實驗顯示，在各模型規模上，相較於強基線有一致且顯著的改進。<lb/>我們的代碼可在 https://github.com/hypasd-art/KATE 獲得。

##### **Attention-Discounted Adaptive Sampler for Masked Diffusion Language Models**
2606.10829v1 by Yusuf Sahin, Ahmed Rockey Saikia, Volkan Cevher, Paolo Favaro

Masked diffusion language models can reduce inference steps by revealing multiple tokens per denoising iteration, but this parallelism is fragile: positions that are individually confident may be unsafe to commit together when their predictions are coupled. Existing training-free samplers such as Top-\(k\), Fast-dLLM, and EB-Sampler mainly control how many tokens to reveal, while often ranking candidates by token-wise scores that ignore interactions within the selected set. We propose ADAS, a training-free reranking rule for parallel masked diffusion decoding. ADAS leaves the base sampler's stopping rule unchanged and modifies only subset construction: it greedily discounts a candidate when it attends strongly to already selected positions whose predictions remain uncertain. Unlike graph-constrained methods that turn attention into hard compatibility constraints, ADAS keeps attention continuous and uses it as a soft marginal penalty. Across LLaDA-8B-Base and Dream-7B-Base on GSM8K, MATH500, HumanEval, and MBPP, plugging ADAS into Top-\(k\), Fast-dLLM, and EB-Sampler improves low-NFE performance at matched denoiser evaluations by \(9.11\) and \(10.46\) percentage points on average, respectively, with \(3.1\%\) per-forward runtime overhead. These results show that soft attention-discounted reranking is a simple and modular way to improve quality in highly parallel decoding for masked diffusion language models.

摘要：被遮蔽的擴散語言模型可以通過每次去噪迭代揭示多個標記來減少推理步驟，但這種並行性是脆弱的：當它們的預測相互關聯時，單獨自信的位置可能不安全一起提交。現有的無需訓練的取樣器，如 Top-\(k\)、Fast-dLLM 和 EB-Sampler 主要控制揭示多少標記，同時通常根據忽略所選集合內部互動的標記分數來排名候選者。我們提出了 ADAS，一種用於並行遮蔽擴散解碼的無需訓練的重新排名規則。ADAS 保持基礎取樣器的停止規則不變，僅修改子集構建：當候選者強烈關注於預測仍然不確定的已選位置時，它會貪婪地折扣該候選者。與將注意力轉化為硬兼容性約束的圖約束方法不同，ADAS 保持注意力的連續性，並將其用作軟邊際懲罰。在 GSM8K、MATH500、HumanEval 和 MBPP 上的 LLaDA-8B-Base 和 Dream-7B-Base 中，將 ADAS 插入 Top-\(k\)、Fast-dLLM 和 EB-Sampler，平均提高了在匹配去噪器評估中的低 NFE 性能 \(9.11\) 和 \(10.46\) 個百分點，並且每次前向運行的開銷為 \(3.1\%\)。這些結果顯示，軟注意力折扣重新排名是一種簡單且模塊化的方式，可以提高遮蔽擴散語言模型的高並行解碼質量。

##### **Earth-OneVision: Extending Remote Sensing Multimodal Large Language Models to More Sensor Modalities and Tasks**
2606.10819v1 by Miaoxin Cai, Guanqun Wang, Wei Zhang, Guangyao Zhou, Yin Zhuang, Tong Zhang, Hao Wang, He Chen, Jun Li

RS-MLLMs enable natural-language understanding and spatial reasoning over earth observation imagery. However, existing models support only a narrow range of sensor types and tasks, yielding a fragmented view of the earth and leaving cross-modal geoscientific knowledge largely unexploited. This work presents Earth-OneVision, a 2B RS-MLLM that unifies six sensor modalities (i.e., optical, SAR, infrared, multispectral, temporal, and video) and cross-sensor fusion across 9 task categories within a single autoregressive framework. Three dedicated mechanisms address three bottlenecks. Full-Granularity Vision-Language Alignment (FGVLA) aligns multi-level visual features with the multi-dimensional language space. Spatial-Linguistic Isomorphic Serialization (SLIS) unifies heterogeneous spatial outputs as autoregressive tokens. Progressive Cross-Modality Adaptation (PCMA) decomposes the compound domain gap into sequential stages, tackling the viewpoint and imaging physics gaps in turn. To support joint training, MMRS-OneVision is constructed with ~34M QA pairs spanning all six sensor modalities and cross-sensor fusion across 9 task categories, substantially exceeding existing RS multimodal instruction datasets. With only 2B parameters, Earth-OneVision achieves competitive or state-of-the-art results across extensive benchmarks, consistently matching or outperforming 4B-72B RS-MLLMs. It achieves 87.52% P@0.5 on the OPT-RSVG testset for optical visual grounding and 80.68% on the SAR VQA benchmark SARLANG-Bench, exceeding 7B models by over 7%. It further achieves 75.74% recall on the BigEarthNet-MS testset for multispectral classification, and 81.94% MCQ accuracy on EarthMind-Bench for cross-modality reasoning.

摘要：RS-MLLMs 使自然語言理解和空間推理能夠應用於地球觀測影像。<lb/>然而，現有模型僅支持有限範圍的傳感器類型和任務，導致對地球的觀察呈現出片段化的視角，並使跨模態地球科學知識大部分未被利用。<lb/>本研究提出了 Earth-OneVision，一個 2B RS-MLLM，統一了六種傳感器模態（即光學、SAR、紅外線、多光譜、時間和視頻）以及在單一自回歸框架內的跨傳感器融合，涵蓋 9 個任務類別。<lb/>三個專門的機制解決了三個瓶頸。<lb/>全粒度視覺-語言對齊（FGVLA）將多層次的視覺特徵與多維語言空間對齊。<lb/>空間-語言同構序列化（SLIS）將異質的空間輸出統一為自回歸標記。<lb/>漸進式跨模態適應（PCMA）將複合領域差距分解為連續階段，依次解決視角和成像物理差距。<lb/>為了支持聯合訓練，MMRS-OneVision 構建了約 3400 萬 QA 對，涵蓋所有六種傳感器模態及 9 個任務類別的跨傳感器融合，顯著超過現有的 RS 多模態指令數據集。<lb/>僅用 2B 參數，Earth-OneVision 在廣泛的基準測試中達到了競爭性或最先進的結果，始終與 4B-72B RS-MLLMs 相匹配或超越。<lb/>在 OPT-RSVG 測試集上，其光學視覺定位達到 87.52% P@0.5，而在 SAR VQA 基準 SARLANG-Bench 上達到 80.68%，超過 7B 模型超過 7%。<lb/>此外，在 BigEarthNet-MS 測試集上，其多光譜分類的召回率達到 75.74%，在 EarthMind-Bench 上的跨模態推理中達到 81.94% 的 MCQ 準確率。

##### **Boosting ECG Classification Performance by Pre-training with Synthesized Data**
2606.10802v1 by Naoki Nonaka, Jun Seita

Deep Neural Networks (DNNs) typically require extensive datasets for effective training. In the medical domain, acquiring large-scale data is often challenging due to privacy concerns and the rarity of certain diseases. To address this data scarcity, we investigate the efficacy of training DNN models using synthetic data, generated based on domain-specific medical knowledge. Specifically, we develop a knowledge-driven Gaussian-composition synthesis algorithm for single-lead II ECGs, in which each heartbeat is represented by Gaussian-shaped P, Q, R, S, and T wave components. Using this simulator, we generate synthetic data for four abnormal electrocardiogram (ECG) classes: atrial fibrillation (AF), atrial flutter (AFLT), premature ventricular complex (PVC), and Wolff-Parkinson-White Syndrome (WPW). We evaluate the utility of this synthetic data by conducting abnormal ECG classification using ten different DNN architectures. Our results demonstrate that synthetic-to-real training improves classification performance for three of the four target abnormalities, with the largest architecture-averaged gain of $33.2\%$ observed for AFLT. Further analysis reveals that the performance enhancement from synthetic data is more pronounced with smaller real-world datasets. These findings suggest that domain-knowledge-based synthetic ECGs can serve as a useful pre-training resource, particularly in scenarios where real-world data are limited or difficult to obtain.

摘要：深度神經網絡（DNNs）通常需要大量數據集以進行有效的訓練。<lb/>在醫療領域，由於隱私問題和某些疾病的稀有性，獲取大規模數據往往具有挑戰性。<lb/>為了解決這一數據稀缺問題，我們研究了使用基於特定領域醫療知識生成的合成數據來訓練DNN模型的有效性。<lb/>具體而言，我們為單導聯II型心電圖（ECG）開發了一種基於知識驅動的高斯組合合成算法，其中每一次心跳由高斯形狀的P、Q、R、S和T波組件表示。<lb/>利用這個模擬器，我們為四種異常心電圖（ECG）類別生成合成數據：心房顫動（AF）、心房撲動（AFLT）、早期心室複雜（PVC）和沃爾夫-帕金森-懷特綜合症（WPW）。<lb/>我們通過使用十種不同的DNN架構進行異常ECG分類來評估這些合成數據的實用性。<lb/>我們的結果顯示，合成到真實的訓練提高了四種目標異常中三種的分類性能，其中AFLT的架構平均增益最大，達到$33.2\%$。<lb/>進一步分析顯示，合成數據帶來的性能提升在較小的真實世界數據集上更為明顯。<lb/>這些發現表明，基於領域知識的合成ECG可以作為有用的預訓練資源，特別是在真實數據有限或難以獲得的情況下。

##### **A Bayesian Network Approach for Enhancing Security-Focused Decision Support Systems**
2606.10782v1 by Carolina Fernández-Martínez, Shuaib Siddiqui, Vanesa Daza

The adoption and integration of heterogeneous stacks in most of today's open-source based networks brings clear benefits like interoperability and availability of advanced features. Yet, on the other hand the increasing number of interconnecting components and moving parts requires maintaining an ever increasing base of interdisciplinary knowledge of different tools in different domains to ensure proper operation. To alleviate such efforts, this work proposes a Decision Support System (DSS) to guide infrastructure operators through the selection of security approaches (e.g. tools) to adopt in their environments. This framework easily captures the end-user high-level requirements on the security triad for different domains and runs inference on the designated models to provide the identified tools (security mechanisms) that better serve such needs. The presented DSS aims at delivering an understandable and extensible framework to accommodate varying requirements and Bayesian Network (BN) models. The architecture and modelling of the system are proposed, aligned with its theoretical framework. Its performance is evaluated in terms of time and prediction accuracy.

摘要：當今大多數基於開源的網絡中，異質堆疊的採用和整合帶來了明顯的好處，如互操作性和先進功能的可用性。<lb/>然而，另一方面，越來越多的互連組件和運行部件需要維護不斷增加的跨學科知識基礎，以確保不同領域中不同工具的正常運作。<lb/>為了減輕這種努力，本研究提出了一個決策支持系統（DSS），以指導基礎設施運營商選擇在其環境中採用的安全方法（例如工具）。<lb/>該框架輕鬆捕捉終端用戶對不同領域安全三角的高級需求，並對指定模型進行推斷，以提供更好滿足這些需求的識別工具（安全機制）。<lb/>所提出的DSS旨在提供一個可理解且可擴展的框架，以適應不同的需求和貝葉斯網絡（BN）模型。<lb/>系統的架構和建模被提出，與其理論框架保持一致。<lb/>其性能在時間和預測準確性方面進行了評估。

##### **Recovering the Zipfian Distribution in Unsupervised Term Discovery**
2606.10781v1 by Danel Slabbert, Simon Malan, Herman Kamper

Unsupervised term discovery involves segmenting unlabelled speech into word- or syllable-like units and clustering these into a lexicon of candidate types. True lexicons follow a Zipfian distribution, yet the dominant centre-based clustering approach -- K-means -- produces a more uniform distribution due to an inductive bias toward spherical clusters. In this paper we revisit graph-based clustering as a bottom-up alternative, where segment embeddings are connected by pairwise similarity and partitioned using the Leiden algorithm. We show that graph clustering substantially outperforms centre-based approaches (K-means, GMM, BIRCH) in both word- and syllable-level lexicon discovery across three languages, producing more Zipf-like distributions. Another bottom-up approach, agglomerative clustering with average linkage, also performs well, although it is computationally less efficient and allows for less control over the resulting distribution. Our work calls into question the dominance of centre-based clustering for term discovery, and promotes graph clustering as an attractive alternative.

摘要：無監督的術語發現涉及將未標記的語音分割成類似單詞或音節的單位，並將這些單位聚類成候選類型的詞彙表。真正的詞彙表遵循齊夫分佈，然而主導的基於中心的聚類方法——K-means——由於對球形聚類的歸納偏見，產生了更均勻的分佈。在本文中，我們重新審視基於圖的聚類作為一種自下而上的替代方法，其中段落嵌入通過成對相似性連接，並使用Leiden算法進行劃分。我們顯示圖聚類在三種語言的單詞和音節級詞彙發現中，顯著優於基於中心的方法（K-means、GMM、BIRCH），產生了更接近齊夫分佈的結果。另一種自下而上的方法，即使用平均連接的聚合聚類，也表現良好，儘管它在計算上效率較低，並且對最終分佈的控制較少。我們的工作質疑了基於中心的聚類在術語發現中的主導地位，並推廣圖聚類作為一種有吸引力的替代方案。

##### **Detecting Knowledge Gaps from Conversational AI Interactions Using Curriculum Prerequisite Graphs**
2606.10736v1 by Youssef Medhat, Junsoo Park, Ploy Thajchayapong, Ashok K. Goel

Large online courses generate thousands of student questions directed at conversational AI teaching assistants, yet these interaction logs remain largely untapped as diagnostic signals. We present a pipeline that maps student questions from a conversational AI teaching assistant to curriculum topics using a few-shot text classifier, grounded in a GPT-4-extracted prerequisite knowledge graph of course concepts. Evaluated on 1,340 question events from 164 students in a graduate-level AI course, our classifier achieves 80.0% accuracy across 43 labels (42 curriculum topics plus an "unknown" abstention class). Topic-level question volume correlates significantly with student self-reported difficulty from an independent mid-semester survey (rho = 0.491, p = 0.008, n = 28 topics), providing convergent evidence that the classified question stream reflects genuine topic difficulty. These results demonstrate that conversational AI interaction logs, mapped onto curriculum structure, carry actionable signals about topic-level knowledge gaps and provide instructors with a curriculum-grounded view of which topics warrant attention.

摘要：大型線上課程產生數以千計的學生問題，這些問題針對對話式AI教學助理，但這些互動記錄在診斷信號上仍然未被充分利用。<lb/>我們提出了一個管道，將對話式AI教學助理的學生問題映射到課程主題，使用基於GPT-4提取的課程概念前置知識圖的少量示例文本分類器。<lb/>在對164名研究生AI課程學生的1,340個問題事件進行評估時，我們的分類器在43個標籤（42個課程主題加上一個“未知”棄權類別）上達到80.0%的準確率。<lb/>主題級問題量與學生在獨立的學期中期調查中自報的難度顯著相關（rho = 0.491，p = 0.008，n = 28個主題），提供了收斂證據，表明分類後的問題流反映了真實的主題難度。<lb/>這些結果表明，映射到課程結構的對話式AI互動記錄，帶有關於主題級知識差距的可行信號，並為教師提供了基於課程的視角，了解哪些主題值得關注。

##### **REAL: A Reasoning-Enhanced Graph Framework for Long-Term Memory Management of LLMs**
2606.10694v1 by Keer Lu, Liwei Chen, Guoqing Jiang, Zhiheng Qin, Yunhuai Liu, Wentao Zhang

Large Language Models (LLMs) are increasingly expected to interact with users over long time horizons. However, due to their finite context window, LLMs cannot retain all past interactions, making long-term memory management essential for storing, updating, and retrieving historical information beyond the context limit. Although recent memory systems attempt to address this issue by storing historical information externally, existing approaches suffer from three key limitations: flat text-based memory organizations fail to capture explicit relations among memories, structured memory systems often destructively overwrite evolving facts, and current retrieval mechanisms remain query-agnostic and passive when evidence is incomplete. REAL constructs long-term conversational memory as a temporal and confidence-aware directed property graph, where each atomic fact is represented with entities, relations, valid-time intervals, confidence scores, and exploration intent labels. During memory construction, REAL adopts a non-destructive temporal update strategy that preserves parallel fact versions and their validity intervals, enabling faithful tracking of fact evolution. During retrieval, REAL anchors query-relevant root entities, decouples their exploration intents, and performs semantic evaluator-guided hybrid beam search to extract compact memory subgraphs. It further incorporates counterfactual inference to repair unreliable retrieval states and recover missing memory evidence through implicit logical relations. Comprehensive experiments demonstrate that REAL substantially improves long-term memory performance over flat-text, graph-based, and existing memory baselines, achieving an average improvement of 22.72\%.

摘要：大型語言模型（LLMs）越來越被期望能夠與用戶進行長期的互動。<lb/>然而，由於其有限的上下文窗口，LLMs 無法保留所有過去的互動，這使得長期記憶管理對於存儲、更新和檢索超出上下文限制的歷史信息變得至關重要。<lb/>儘管最近的記憶系統試圖通過將歷史信息外部存儲來解決這一問題，但現有的方法存在三個主要限制：基於文本的平面記憶組織未能捕捉記憶之間的明確關係，結構化記憶系統往往會破壞性地覆蓋不斷演變的事實，而當證據不完整時，當前的檢索機制仍然是查詢無關和被動的。<lb/>REAL 將長期對話記憶構建為一個時間性和信心感知的有向屬性圖，其中每個原子事實都用實體、關係、有效時間區間、信心分數和探索意圖標籤表示。<lb/>在記憶構建過程中，REAL 採用非破壞性的時間更新策略，保留平行事實版本及其有效性區間，使得能夠忠實追蹤事實的演變。<lb/>在檢索過程中，REAL 鎖定查詢相關的根實體，解耦它們的探索意圖，並執行語義評估器引導的混合束搜索以提取緊湊的記憶子圖。<lb/>它進一步結合反事實推理來修復不可靠的檢索狀態，並通過隱含的邏輯關係恢復缺失的記憶證據。<lb/>綜合實驗表明，REAL 在長期記憶性能上顯著改善了平面文本、基於圖形的和現有記憶基準，實現了平均 22.72\% 的提升。

##### **Divide and Cooperate: Role-Decomposed Multi-Agent LLM Training with Cross-Agent Learning Signals**
2606.10684v1 by Jaewan Park, Solbee Cho, Jay-Yoon Lee

Modern language agents which perform multi-step reasoning have shown strong performance in knowledge-intensive question answering. However, existing approaches typically couple evidence acquisition and answer generation within a single policy. This forces a single model to play multiple potentially conflicting roles, inducing a combinatorial explosion in the policy space and hindering efficient exploration. It also introduces a credit assignment problem during training: a search action that retrieves sufficient evidence may still be penalized when generation fails, and vice versa. We propose DAC (Divide and Cooperate), a role-decomposed multi-agent training framework that divides agentic search into two cooperative subtasks, each handled by a dedicated agent trained with role-specific learning signals. The generator serves a dual role as both an answer producer and an evidence sufficiency verifier, abstaining when retrieved evidence is insufficient. This abstention signal is incorporated into the search agent's reward, providing structured cross-agent learning signals that improve credit assignment. Conversely, the searcher exposes the generator to diverse and challenging evidence environments by hard-positive evidence augmentation, improving its robustness. Experiments on general and multi-hop QA benchmarks show that DAC, implemented via parameter-efficient LoRA modules over a shared backbone, achieves strong performance against prior baselines that rely on full fine-tuning of monolithic models.

摘要：現代語言代理人進行多步推理在知識密集型問題回答中表現出色。<lb/>然而，現有的方法通常將證據獲取和答案生成結合在單一策略中。<lb/>這迫使單一模型扮演多個可能衝突的角色，導致策略空間的組合爆炸，並妨礙有效的探索。<lb/>它還在訓練過程中引入了信用分配問題：檢索足夠證據的搜索行動在生成失敗時可能仍會受到懲罰，反之亦然。<lb/>我們提出DAC（Divide and Cooperate），一個角色分解的多代理訓練框架，將代理搜索分為兩個合作的子任務，每個子任務由專門的代理處理，並使用角色特定的學習信號進行訓練。<lb/>生成器同時擔任答案生成者和證據充分性驗證者的雙重角色，當檢索的證據不足時會選擇不生成。<lb/>這一選擇不生成的信號被納入搜索代理的獎勵中，提供結構化的跨代理學習信號，改善信用分配。<lb/>相對地，搜索者通過強正證據增強將生成器暴露於多樣且具有挑戰性的證據環境中，提高其穩健性。<lb/>在一般和多跳QA基準上的實驗顯示，通過參數高效的LoRA模塊在共享骨幹上實現的DAC，對比依賴於對單體模型進行全面微調的先前基準，取得了強勁的表現。

##### **Are We Evaluating Knowledge or Phrasing? Mitigating MCQA Sensitivity with ParaEval**
2606.10657v1 by João Maria Janeiro, Mathurin Videau, Andrea Caciolai, Benjamin Piwowarski, Patrick Gallinari, Loic Barrault

Multiple-choice (MCQA) benchmarks are the standard for evaluating pretrained large language models, but their reliance on log-likelihood scoring makes them unreliable. Specifically, standard scores are highly sensitive to the exact phrasing (surface form) of the answers, conflating a model's familiarity with a specific phrase with its actual capability. We demonstrate this flaw using a controlled testbed of 1B-8B models trained on the same knowledge. Despite having identical knowledge, standard metrics falsely report a performance gap of over 2 points. To solve this, we propose ParaEval, an evaluation framework that queries models using multiple paraphrases per answer option. By scoring each model based on its most favorable phrasing, ParaEval successfully reduces the false performance gap to below 1 point. We confirm that these evaluation artifacts, and the improvements from ParaEval, persist in frontier 70B and 120B open-source models. Ultimately, ParaEval provides a robust and efficient way to evaluate true underlying capability rather than surface-form familiarity.

摘要：多選擇題（MCQA）基準是評估預訓練大型語言模型的標準，但它們對對數似然評分的依賴使其不可靠。<lb/>具體而言，標準分數對答案的確切措辭（表面形式）非常敏感，將模型對特定短語的熟悉程度與其實際能力混淆在一起。<lb/>我們使用一個受控的測試平台，展示了這一缺陷，該平台包含在相同知識上訓練的1B-8B模型。<lb/>儘管擁有相同的知識，標準指標卻錯誤地報告出超過2分的性能差距。<lb/>為了解決這個問題，我們提出了ParaEval，一個使用多個同義詞查詢每個答案選項的評估框架。<lb/>通過根據每個模型最有利的措辭進行評分，ParaEval成功地將虛假的性能差距降低到1分以下。<lb/>我們確認這些評估工件以及ParaEval帶來的改進在最前沿的70B和120B開源模型中仍然存在。<lb/>最終，ParaEval提供了一種穩健且高效的方式來評估真正的潛在能力，而非表面形式的熟悉度。

##### **How Does Reasoning Flow? Tracing Attention-Induced Information Flow for Targeted RL in LLMs**
2606.10646v1 by Zhichen Dong, Yang Li, Yuhan Sun, Weixun Wang, Yijia Luo, Zinian Peng, Taiheng Ye, Chao Yang, Wenbo Su, Yu Cheng, Bo Zheng, Junchi Yan

Token-level credit assignment remains a key obstacle for reinforcement learning (RL) in large language models (LLMs), where RL recipes typically treat all tokens equally, failing to distinguish decisive reasoning steps from routine formatting or fluent filler. Recent attempts leverage model-internal signals to assign finer-grained credit, but these are often point-wise heuristics that ignore the global structure of information propagation. We propose FlowTracer, an RL framework that traces answer-targeted reasoning flow on an attention-induced directed acyclic graph in which nodes correspond to tokens and edge capacities come from aggregated attention weights and derives token credit from this global structure. The edge capacities are reweighted to retain only the influence that can reach the answer region, while enforcing local flow conservation so intermediate tokens neither lose nor gain effective mass due to path length or irrelevant branches. On this graph, FlowTracer extracts an information-flow backbone connecting the question to the answer and scores tokens by flow throughput, revealing high-impact hubs and aggregation checkpoints that mediate long-range dependencies. These derived importances are used to shape token-level rewards, enabling learning signals to focus precisely on the tokens that route information toward (or away from) correct answers and delivering consistent performance gains across a range of reasoning tasks.

摘要：Token-level credit assignment 仍然是大型語言模型 (LLMs) 中強化學習 (RL) 的一個關鍵障礙，因為 RL 方法通常將所有標記視為平等，未能區分決定性推理步驟與例行格式或流暢填充的區別。最近的嘗試利用模型內部信號來分配更細緻的信用，但這些通常是忽略信息傳播全局結構的逐點啟發式方法。我們提出了 FlowTracer，一個 RL 框架，該框架在一個由注意力引起的有向無循環圖上追蹤針對答案的推理流程，其中節點對應於標記，邊的容量來自聚合的注意力權重，並從這一全局結構中推導標記信用。邊的容量被重新加權，以僅保留能夠到達答案區域的影響，同時強制執行局部流量守恆，這樣中間標記就不會因為路徑長度或無關分支而失去或獲得有效質量。在這個圖上，FlowTracer 提取了一個信息流骨幹，將問題與答案連接起來，並通過流量通量對標記進行評分，揭示出高影響力的樞紐和聚合檢查點，這些樞紐和檢查點調解長距離依賴性。這些推導的重要性被用來塑造標記級別的獎勵，使學習信號能夠精確地集中在那些將信息路由朝向（或遠離）正確答案的標記上，並在一系列推理任務中實現一致的性能提升。

##### **Causal Ensemble Agent: Hierarchical Causal Discovery with LLM-guided Expert Reweighting**
2606.10607v1 by Xinyu Li, Yuanyuan Wang, Haoxuan Li, Chuan Zhou, Erdun Gao, Bo Han, Tongliang Liu, Kun Zhang, Howard Bondell, Mingming Gong

Causal discovery aims to uncover causal structures from observational data, which is crucial for real-world decision-making. However, different causal discovery algorithms can produce divergent results that conflict with each other, complicating the identification of accurate causal graphs. Traditional approaches rely on numerical values and statistical assumptions, often ignoring rich domain-specific information, such as feature descriptions, which could also help structure learning. While recent works explore using Large Language Models (LLMs) to infer causal relations via direct queries, such methods can be unreliable due to a lack of alignment with the actual data. To address these limitations, we propose Causal Ensemble Agent (CEA), a novel framework that aggregates structural insights from statistical discovery experts across different graph levels via linear opinion pooling, and uses an LLM as a meta-referee to dynamically reweight experts when the aggregated confidence is close to the decision boundary, thereby composing an improved and more complete causal graph. Extensive experiments on both synthetic and real-world datasets demonstrate that CEA achieves the strongest overall performance across a wide range of causal discovery methods, highlighting the effectiveness of using LLMs for meta-analysis in causal discovery.

摘要：因果發現旨在從觀察數據中揭示因果結構，這對於現實世界的決策至關重要。<lb/>然而，不同的因果發現算法可能會產生相互矛盾的結果，這使得準確識別因果圖變得更加複雜。<lb/>傳統方法依賴於數值和統計假設，常常忽視豐富的領域特定信息，例如特徵描述，這些信息也可以幫助結構學習。<lb/>雖然最近的研究探索使用大型語言模型（LLMs）通過直接查詢推斷因果關係，但這些方法可能不可靠，因為缺乏與實際數據的對齊。<lb/>為了解決這些限制，我們提出了因果集成代理（CEA），這是一個新穎的框架，通過線性意見聚合來聚合來自不同圖層的統計發現專家的結構見解，並使用LLM作為元裁判，在聚合信心接近決策邊界時動態重新加權專家，從而組成一個改進的、更完整的因果圖。<lb/>在合成和現實世界數據集上的廣泛實驗表明，CEA在各種因果發現方法中達到了最強的整體性能，突顯了在因果發現中使用LLM進行元分析的有效性。

##### **From Data Heterogeneity to Convergence: A Data-Centric Review of Federated Learning**
2606.10595v1 by Huong Nguyen, Mickaël Bettinelli, Amirhossein Ghaffari, Alexandre Benoit, Hong-Tri Nguyen, Susanna Pirttikangas, Lauri Lovén

Federated Learning (FL) has emerged as a promising solution for data hunger in centralized learning. This paradigm enables privacy with multiple clients to train a shared-task model collaboratively without exposing their local data. While being a key component in any learning system, data is also a primary source of vulnerabilities and challenges, and a major determinant of a stable and well-converged training. Existing FL reviews describe general foundations, security practices, opportunities, challenges, and applications, without delving into diverse aspects of data and considering problems from the data perspective. They rarely provide a data-lens synthesis that links concrete data properties, split protocols, and defenses to convergence speed and stability. This survey fills that gap with three advances. First, we analyze non-IID into measurable traits and rank their influence on convergence as strong, medium, or light, explaining the mechanisms behind each and reconciling evidence across images, texts, and graphs. Second, we connect experimental splitting practices to the real phenomena they emulate, expose the artifacts they introduce, and show how those artifacts affect target accuracy. Third, we analyze how data-related vulnerabilities and their proposed defenses affect convergence, reporting performance under clean and adversarial conditions to make the convergence-robustness trade-off explicit. To our knowledge, this is the first survey to provide a complete understanding of data-related challenges that govern FL. With clear takeaways distilled for each concern, our work serves as actionable guidance, helping practitioners design their system with predictable convergence and stability.

摘要：聯邦學習（FL）已成為解決集中式學習中數據需求的一個有前景的解決方案。<lb/>這一範式使得多個客戶端能夠在不暴露其本地數據的情況下，共同訓練一個共享任務模型以保護隱私。<lb/>數據作為任何學習系統的關鍵組成部分，同時也是脆弱性和挑戰的主要來源，並且是穩定和良好收斂訓練的主要決定因素。<lb/>現有的FL評論描述了一般基礎、保安實踐、機會、挑戰和應用，但未深入探討數據的多樣性及從數據角度考慮問題。<lb/>它們很少提供一個數據視角的綜合，將具體數據特性、拆分協議和防禦措施與收斂速度和穩定性聯繫起來。<lb/>本調查填補了這一空白，提出了三項進展。<lb/>首先，我們將非獨立同分佈（non-IID）分析為可測量的特徵，並根據其對收斂的影響將其分為強、中等或輕微，解釋每種影響背後的機制，並調和來自圖像、文本和圖表的證據。<lb/>其次，我們將實驗拆分實踐與其模擬的真實現象聯繫起來，揭示它們引入的工件，並展示這些工件如何影響目標準確性。<lb/>第三，我們分析與數據相關的脆弱性及其提出的防禦措施如何影響收斂，報告在乾淨和對抗條件下的性能，以明確收斂與穩健性之間的權衡。<lb/>據我們所知，這是第一篇提供對統治FL的數據相關挑戰的完整理解的調查。<lb/>我們的工作為每個問題提煉出清晰的要點，作為可行的指導，幫助從業者設計其系統，以實現可預測的收斂和穩定性。

##### **Benchmarking Knowledge Editing using Logical Rules**
2606.10554v1 by Tatiana Moteu Ngoli, NDah Jean Kouagou, Hamada M. Zahera, Axel-Cyrille Ngonga Ngomo

Large Language Models (LLMs) are increasingly deployed in real-world applications that require access to up-to-date knowledge. However, retraining LLMs is computationally expensive. Therefore, knowledge editing techniques are crucial for maintaining current information and correcting erroneous assertions within pre-trained models. Current benchmarks for knowledge editing primarily focus on recalling edited facts, often neglecting their logical consequences. To address this limitation, we introduce a new benchmark designed to evaluate how knowledge editing methods handle the logical consequences of a single fact edit. Our benchmark extracts relevant logical rules from a knowledge graph for a given edit. Then, it generates multi-hop questions based on these rules to assess the impact on logical consequences. Our findings indicate that while existing knowledge editing approaches can accurately insert direct assertions into LLMs, they frequently fail to inject entailed knowledge. Specifically, experiments with popular methods like ROME and FT reveal a substantial performance gap, up to 24%, between evaluations on directly edited knowledge and on entailed knowledge. This highlights the critical need for semantics-aware evaluation frameworks in knowledge editing.

摘要：大型語言模型（LLMs）越來越多地應用於需要獲取最新知識的現實世界應用中。<lb/>然而，重新訓練LLMs的計算成本非常高。<lb/>因此，知識編輯技術對於維護當前信息和糾正預訓練模型中的錯誤陳述至關重要。<lb/>目前的知識編輯基準主要集中在回憶編輯過的事實，往往忽略了它們的邏輯後果。<lb/>為了解決這一限制，我們引入了一個新的基準，旨在評估知識編輯方法如何處理單一事實編輯的邏輯後果。<lb/>我們的基準從知識圖中提取與給定編輯相關的邏輯規則。<lb/>然後，它根據這些規則生成多跳問題，以評估對邏輯後果的影響。<lb/>我們的研究結果表明，雖然現有的知識編輯方法可以準確地將直接陳述插入LLMs，但它們經常無法注入所包含的知識。<lb/>具體而言，對於流行方法如ROME和FT的實驗顯示，在直接編輯的知識和所包含的知識之間，評估結果存在高達24%的顯著性能差距。<lb/>這凸顯了在知識編輯中對語義感知評估框架的迫切需求。

##### **Representation-Aware Advantage Estimation: Your Reward Model Provides More Than A Scalar Output**
2606.10528v1 by Guozheng Li, Xiyan Fu, Yiwen Guo

Current reinforcement learning from human feedback (RLHF) methods primarily rely on scalar rewards from a trained reward model (RM). While effective, scalar rewards are often noisy and fail to capture fine-grained preference differences, whereas RM hidden states encode richer semantic and preference information. We introduce the representation-aware advantage estimation, which leverages RM hidden states and models them as auxiliary signals for better advantage estimation. Specifically, we propose the Graph-based Advantage Estimation (GraphAE), treat each sampled group as a graph, where nodes correspond to responses and edges capture their similarity in the RM hidden space. Then advantages are computed via graph propagation, enabling each sample to incorporate contextual information from its neighbors. GraphAE is lightweight and can be seamlessly integrated into existing group-based RL algorithms. We apply GraphAE to GRPO, GSPO and RLOO, and conduct extensive experiments on different models and benchmarks. Empirical results show consistent improvements across three benchmarks, with gains of up to + 6.3 on Arena-Hard-v0.1, + 8.27 on AlpacaEval 2.0, and + 0.22 on MT-Bench. These results demonstrate that leveraging RM representations leads to more sample efficient and robust RLHF.

摘要：目前的基於人類反饋的強化學習 (RLHF) 方法主要依賴於來自訓練獎勵模型 (RM) 的標量獎勵。雖然有效，但標量獎勵通常噪聲較大，無法捕捉細微的偏好差異，而 RM 的隱藏狀態則編碼了更豐富的語義和偏好信息。我們引入了基於表示的優勢估計，利用 RM 的隱藏狀態，並將其建模為輔助信號以改善優勢估計。具體而言，我們提出了基於圖的優勢估計 (GraphAE)，將每個抽樣組視為一個圖，其中節點對應於響應，邊緣捕捉它們在 RM 隱藏空間中的相似性。然後，通過圖的傳播計算優勢，使每個樣本能夠從其鄰居中整合上下文信息。GraphAE 輕量且可以無縫集成到現有的基於組的 RL 算法中。我們將 GraphAE 應用於 GRPO、GSPO 和 RLOO，並在不同模型和基準上進行了廣泛的實驗。實證結果顯示在三個基準上均有一致的改進，在 Arena-Hard-v0.1 上增益高達 +6.3，在 AlpacaEval 2.0 上增益為 +8.27，在 MT-Bench 上增益為 +0.22。這些結果表明，利用 RM 表示可以實現更高的樣本效率和更穩健的 RLHF。

##### **Cross-Modal Knowledge Distillation without Paired Data: Theoretical Foundation and Algorithm**
2606.10504v1 by Trong Khiem Tran, Anh Duc Chu, Quang Hung Pham, Phi Le Nguyen, Trong Nghia Hoang

Cross-modal knowledge distillation (CMKD) studies how a (large) teacher model trained on one type of data (e.g., images) can guide a (smaller) student model building on another type of data (e.g., text/audio). Existing CMKD methods often require paired multi-modal data with aligned semantics, but obtaining such paired data are often costly and impractical. To mitigate this limitation, we develop a new CMKD framework for the more challenging setting where paired data are unavailable. In particular, we establish a cross-modal distributional relationship between teacher and student models, which reveals two fundamental quantities governing effective distillation: feature alignment and label alignment. These quantities characterize semantic discrepancy between modalities at the levels of representation and prediction distributions, respectively. Motivated by this insight, we propose a principled framework, with theoretical guarantees, that enables effective cross-modal knowledge distillation by aligning distributions rather than individual samples. Extensive experiments across a wide range of multimodal benchmarks show that our framework is highly effective in both unpaired and paired data settings, improving significantly over prior work.

摘要：跨模態知識蒸餾 (CMKD) 研究如何在一種數據類型（例如圖像）上訓練的（大型）教師模型可以指導在另一種數據類型（例如文本/音頻）上構建的（較小型）學生模型。現有的 CMKD 方法通常需要具有對齊語義的配對多模態數據，但獲得這樣的配對數據往往成本高昂且不切實際。為了減輕這一限制，我們開發了一個新的 CMKD 框架，適用於配對數據不可用的更具挑戰性的情境。具體而言，我們建立了教師和學生模型之間的跨模態分佈關係，這揭示了兩個支配有效蒸餾的基本量：特徵對齊和標籤對齊。這些量分別表徵了表示和預測分佈層面上模態之間的語義差異。受到這一見解的啟發，我們提出了一個有理論保證的原則框架，通過對齊分佈而非單個樣本來實現有效的跨模態知識蒸餾。在廣泛的多模態基準測試中進行的廣泛實驗表明，我們的框架在配對和非配對數據設置中都非常有效，顯著超越了先前的工作。

##### **MoE Enhanced Federated Learning for Spatiotemporal Prediction**
2606.10499v1 by Zhehao Dai, Xiao Han, Zhaolin Deng, Zijian Zhang, Xiangyu Zhao, Guojiang Shen, Xiangjie Kong

Traffic prediction is fundamental to intelligent transportation systems and urban computing, yet many cities continue to suffer from traffic data scarcity due to limited sensor deployment and uneven urban development. Cross-city knowledge transfer has thus attracted increasing attention, enabling data-rich cities to assist data-scarce ones. However, centralized approaches raise privacy concerns, while existing federated methods struggle with pronounced spatiotemporal heterogeneity across cities. To address these challenges, we propose MoE-FedTP, a personalized federated cross-city spatiotemporal prediction framework based on lightweight Mixture-of-Experts (MoE) networks. MoE-FedTP first employs spatiotemporal neural networks to extract features from both source and target cities, then introduces a set of expert networks derived from different source cities through partial parameter sharing. A gating mechanism dynamically fuses the experts to capture diverse traffic dynamics, achieving fine-grained modeling of urban heterogeneity while preserving privacy. Experiments on four real-world traffic datasets show that MoE-FedTP consistently outperforms state-of-the-art cross-city and federated learning baselines, demonstrating its effectiveness in enhancing prediction accuracy for data-scarce cities.

摘要：交通預測是智慧交通系統和城市計算的基礎，但許多城市仍然因為感測器部署有限和城市發展不均而遭受交通數據稀缺的困擾。因此，跨城市知識轉移吸引了越來越多的關注，使數據豐富的城市能夠協助數據稀缺的城市。然而，集中式方法引發了隱私問題，而現有的聯邦方法在城市之間的時空異質性上面臨挑戰。為了解決這些挑戰，我們提出了 MoE-FedTP，一個基於輕量級專家混合模型（Mixture-of-Experts, MoE）網絡的個性化聯邦跨城市時空預測框架。MoE-FedTP 首先利用時空神經網絡從源城市和目標城市中提取特徵，然後引入一組通過部分參數共享從不同源城市衍生的專家網絡。一個閘控機制動態融合這些專家，以捕捉多樣的交通動態，實現對城市異質性的細緻建模，同時保護隱私。在四個真實世界交通數據集上的實驗表明，MoE-FedTP 始終超越最先進的跨城市和聯邦學習基準，證明了其在提高數據稀缺城市預測準確性方面的有效性。

##### **Decoupling Thought from Speech: Knowledge-Grounded Counterfactual Reasoning for Resilient Multi-Agent Argumentation**
2606.10475v1 by Jakub Masłowski, Jarosław A. Chudziak

Multi-agent debate frameworks have been shown to improve large language model performance in convergent tasks, but they are currently optimized in a way that heavily favors final output accuracy rather than stability of the process. During long-horizon exchanges reactive systems under sustained perturbations often experience logic degradation, argument repetition, and role drift. To structurally prevent the identity loss and maintain the process fidelity, we introduce Knowledge-Grounded Counterfactual Reasoning (KG-CFR), a dual-stage architecture that enforces a strict separation of concerns between a private, retrieval-augmented planning buffer, and a public execution layer. We assess this system in Dynamic Resource Allocation under Uncertainty (DRAU), a dedicated 1v1v1 environment, introducing diversity as distinct from standard debate settings. Over 270 completely factorial crisis simulation trajectories with stochastic environmental shocks, KG-CFR prevents judge-detected critical post-shock degradation (defined as a quality shift, $Δ\le -0.20$) in more than 95% of perturbed runs, increasing the overall argument quality from 0.694 to 0.822. Our primary contribution is the demonstration of architectural decoupling being an important factor of systemic resilience enhancement under sustained pressure without quality loss. Furthermore, we introduce custom vector metrics for discourse divergence and plan-execution alignment that provide strong, directionally consistent evidence of operational stability. Our ablation experiments suggest that the proper doctrinal grounding can be an equally important factor for argument quality, as the prospective planning. KG-CFR, according to our initial metric evaluations, reduces semantic looping, by preserving the agent's consistency with the original plan.

摘要：多智能體辯論框架已被證明能改善大型語言模型在收斂任務中的表現，但目前的優化方式過於偏重最終輸出準確性，而非過程的穩定性。在長期的交流中，反應系統在持續的擾動下經常會出現邏輯退化、論點重複和角色漂移。為了結構性地防止身份喪失並維持過程的忠實度，我們引入了知識基礎的反事實推理（KG-CFR），這是一種雙階段架構，強制將私人檢索增強規劃緩衝區與公共執行層嚴格分開。我們在不確定性下的動態資源分配（DRAU）中評估該系統，這是一個專門的1v1v1環境，引入了與標準辯論設置不同的多樣性。在270多條完全因子化的危機模擬軌跡中，KG-CFR在超過95%的擾動運行中防止了法官檢測到的關鍵後擾動退化（定義為質量轉變，$Δ\le -0.20$），將整體論點質量從0.694提高到0.822。我們的主要貢獻是展示架構解耦是持續壓力下系統韌性增強的重要因素，且不會損失質量。此外，我們引入了自定義向量度量，以衡量話語偏差和計劃執行對齊，這提供了強有力且方向一致的運營穩定性證據。我們的消融實驗表明，適當的教義基礎對論點質量可能是與前瞻性規劃同樣重要的因素。根據我們的初步度量評估，KG-CFR通過保持代理與原始計劃的一致性來減少語義循環。

##### **Detecting Speculative Language in Biomedical Texts using Recurrent Neural Tensor Networks**
2606.10471v1 by Dhruv Dixit

In this investigation, we delve into the automated detection of speculative language within biomedical articles by utilizing distributed sentence representations and advanced deep learning techniques. The implications of such identification extend to information retrieval, multi-document summarization, and the exploration of new knowledge. Our exploration encompasses two distinct approaches for acquiring distributed sentence representations: the Paragraph Vector model and the Recursive Neural Tensor Network. These methodologies are then rigorously compared against three foundational baseline algorithms: Support Vector Machines, Naive Bayes, and pattern matching. Our findings reveal that the Recursive Neural Tensor Network (RNTN) demonstrates a slight performance edge (F1 = 0.885) over the top-performing baseline, the linear bigram SVM (F1 = 0.881). Meanwhile, the Paragraph Vector model proves less effective (F1 = 0.368), even after extensive training using an expansive, unlabeled dataset. We engage in a comprehensive discourse on the factors influencing these performance disparities and provide insightful recommendations for future research directions.

摘要：在這項研究中，我們深入探討了利用分散式句子表示法和先進的深度學習技術，自動檢測生物醫學文章中的推測性語言。這種識別的影響擴展到信息檢索、多文件摘要和新知識的探索。我們的探索涵蓋了兩種獲取分散式句子表示法的不同方法：段落向量模型和遞歸神經張量網絡。這些方法隨後與三個基礎基準算法進行了嚴格比較：支持向量機、朴素貝葉斯和模式匹配。我們的研究結果顯示，遞歸神經張量網絡（RNTN）的性能略優於表現最佳的基準線性二元SVM（F1 = 0.881），其F1值為0.885。與此同時，段落向量模型的效果較差（F1 = 0.368），即使在使用大量未標記數據集進行廣泛訓練後也是如此。我們對影響這些性能差異的因素進行了全面的討論，並為未來的研究方向提供了有見地的建議。

##### **ERAlign: Energy-based Representation Alignment of GNNs and LLMs on Text-attributed Graphs**
2606.10461v1 by Xianlin Zeng, Fan Xia, Xiangyu Chen

Text-attributed Graphs (TAGs) incorporate textual node attributes with graph structures to describe rich relational semantics. Recent efforts to integrate Graph Neural Networks (GNNs) and Large Language Models (LLMs) have shown promise for learning on TAGs, yet achieving well-aligned representations remains challenging. Prior studies largely rely on heuristics that perform coarse-grained matching. They lack sufficient constraints and ignore distributional alignment, leading to representation drift and limited generalization. Building on Energy-based Models (EBMs), we propose an Energy-based Representation Alignment (ERAlign) framework that projects GNN-encoded graph structure and LLM-derived text embeddings in a shared latent space to achieve distribution consistency. Concretely, layer-wise alignment is quantified by a distance metric and optimized via an EBM objective. By decreasing energy values, our framework yields well-aligned representations for downstream tasks. During training, we introduce Energy Discrepancy (ED) to avoid high sampling costs associated with intractable normalization. ED also carries theoretical guarantees of higher training efficiency and reduced energy landscape distortion. Empirical evaluations on eight TAG datasets demonstrate that ERAlign obtains state-of-the-art performance across varying levels of supervision and cross-task transfer scenarios.

摘要：文本屬性圖（TAGs）結合了文本節點屬性和圖結構，以描述豐富的關係語義。最近將圖神經網絡（GNNs）和大型語言模型（LLMs）整合的努力顯示出在TAGs上學習的潛力，但實現良好對齊的表示仍然具有挑戰性。先前的研究主要依賴於執行粗略匹配的啟發式方法。它們缺乏足夠的約束，並忽略了分佈對齊，導致表示漂移和有限的泛化能力。在基於能量的模型（EBMs）的基礎上，我們提出了一個基於能量的表示對齊（ERAlign）框架，該框架將GNN編碼的圖結構和LLM衍生的文本嵌入投影到共享的潛在空間中，以實現分佈一致性。具體而言，層級對齊通過距離度量進行量化，並通過EBM目標進行優化。通過降低能量值，我們的框架為下游任務產生了良好對齊的表示。在訓練過程中，我們引入了能量差異（ED），以避免與難以處理的正規化相關的高抽樣成本。ED還具有更高訓練效率和減少能量景觀失真理論保證。在八個TAG數據集上的實證評估顯示，ERAlign在不同的監督水平和跨任務轉移場景中獲得了最先進的性能。

##### **SpenseGPT: Practical One-shot Pruning Enabling Sparse and Dense GEMMs for LLM Inference**
2606.10445v1 by Jaeseong Lee, Seung-won Hwang, Samyam Rajbhandari

Semi-structured 2:4 sparsity is widely supported by modern accelerators, providing up to a 2x theoretical speedup. However, its strict 50% sparsity constraint often causes non-negligible accuracy degradation under post-training pruning. Meanwhile, existing relaxed sparsity formats either require specialized compiler support or introduce runtime overheads that limit end-to-end speedup. We propose Spense, a practical hybrid sparse-dense format that splits each weight matrix into a 2:4 sparse region and a dense region. This design relaxes the effective sparsity constraint while remaining compatible with existing high-performance sparse and dense GEMM libraries, avoiding both custom compiler support and input activation expansion. Building on this format, we introduce SpenseGPT, a one-shot post-training pruning method that produces sparse and dense regions. Notably, we show that selecting the right dense regions is important, and we devise two different strategies to choose them. Experiments on Qwen3-32B and Seed-OSS-36B demonstrate that our method achieves up to 1.2x end-to-end decoding speedup on B200 GPUs with FP8 precision, while preserving accuracy. To the best of our knowledge, this is the first one-shot pruning demonstration of real-world end-to-end LLM decoding speedup from semi-structured sparse tensor cores on recent GPUs such as B200s, while maintaining model quality.

摘要：半結構化的 2:4 稀疏性在現代加速器中得到了廣泛支持，提供高達 2 倍的理論加速。然而，其嚴格的 50% 稀疏性限制常常導致在訓練後修剪過程中出現不可忽視的準確性下降。與此同時，現有的放寬稀疏性格式要麼需要專門的編譯器支持，要麼引入運行時開銷，限制了端到端的加速。我們提出了 Spense，一種實用的混合稀疏-密集格式，將每個權重矩陣分割為 2:4 的稀疏區域和密集區域。這種設計放寬了有效的稀疏性限制，同時仍然與現有的高性能稀疏和密集 GEMM 庫兼容，避免了自定義編譯器支持和輸入激活擴展。在此格式的基礎上，我們引入了 SpenseGPT，一種一次性訓練後修剪方法，產生稀疏和密集區域。值得注意的是，我們顯示選擇正確的密集區域是重要的，並設計了兩種不同的策略來選擇它們。在 Qwen3-32B 和 Seed-OSS-36B 上的實驗表明，我們的方法在 B200 GPU 上以 FP8 精度實現了高達 1.2 倍的端到端解碼加速，同時保持了準確性。據我們所知，這是首次在最近的 GPU（如 B200s）上展示的來自半結構化稀疏張量核心的真實世界端到端 LLM 解碼加速的一次性修剪示範，同時保持模型質量。

##### **Vision-Assisted Foundation Model for Solving Multi-Task Vehicle Routing Problems**
2606.10431v1 by Shuangchun Gui, Zhiguang Cao, Wen Song, Yew-Soon Ong

Multi-task vehicle routing problems play a critical role in enhancing efficiency across various industries and service sectors. These problems consist of multiple variants that optimize routing costs while meeting diverse customer constraints. Existing multi-task VRP solvers solely utilize a graph-based modality, limiting their ability to address variants with multiple constraints. As a format to represent complex semantics, vision modality shows great potential for encoding diverse VRP constraints. This motivates us to learn patch-level semantics from the vision images, and then integrate them into a graph-based model to solve various VRP variants simultaneously. However, directly applying this approach to multi-task VRPs presents three challenges: 1) existing VRP images lack constraint representations, which are essential for multi-task VRPs, 2) the fixed receptive field of individual patches cannot effectively accommodate varying requirements across tasks, and 3) imbalanced pixel distribution among constraints may cause the model to overlook constraints with fewer pixels. In this paper, we propose a vision-assisted foundation model (VaFM) to address these challenges. In the vision modality, input images tailored to all constraints are encoded by a convolutional neural network. The obtained patch embeddings are fused with graph-based nodes to generate solutions, with an auxiliary task designed to address the pixel-imbalanced issue. The performance of VaFM is evaluated across 16 different VRP variants. The experimental results demonstrate the superiority of VaFM over state-of-the-art methods, especially for variants with complex constraints.

摘要：多任務車輛路徑問題在提高各行各業和服務部門的效率方面扮演著關鍵角色。這些問題由多種變體組成，旨在優化路徑成本，同時滿足多樣的客戶約束。現有的多任務 VRP 解決方案僅使用基於圖形的模式，限制了它們處理具有多重約束的變體的能力。作為表示複雜語義的格式，視覺模式在編碼多樣的 VRP 約束方面顯示出巨大的潛力。這促使我們從視覺圖像中學習補丁級語義，然後將其整合到基於圖形的模型中，以同時解決各種 VRP 變體。然而，將這種方法直接應用於多任務 VRP 會面臨三個挑戰：1）現有的 VRP 圖像缺乏約束表示，這對於多任務 VRP 至關重要，2）單個補丁的固定感受野無法有效適應任務之間的變化要求，3）約束之間的不平衡像素分佈可能導致模型忽略像素較少的約束。在本文中，我們提出了一種視覺輔助基礎模型（VaFM）來應對這些挑戰。在視覺模式中，針對所有約束定制的輸入圖像由卷積神經網絡編碼。獲得的補丁嵌入與基於圖形的節點融合以生成解決方案，並設計了一個輔助任務來解決像素不平衡問題。VaFM 的性能在 16 種不同的 VRP 變體中進行評估。實驗結果顯示 VaFM 在最先進的方法中具有優越性，特別是對於具有複雜約束的變體。

##### **Which LoRA? An Empirical Study on the Effectiveness of LoRA Techniques During Multilingual Instruction Tuning**
2606.10428v1 by Thamali Wijewardhana, Napoleon H. Reyes, Surangika Ranathunga

We investigate whether commonly available LoRA variants have an advantage over basic LoRA in multilingual instruction tuning. Experiments involving LoRA and four other variants on two datasets across diverse target languages show that there is no significant advantage in using more complex LoRA variants instead of basic LoRA, with respect to balancing cross-lingual transfer and knowledge retention. An analysis of hidden embeddings reveal that layer-wise language representation remains largely similar across LLMs fine-tuned with different LoRA techniques, suggesting that architectural novelty of LoRA techniques may not translate into better cross-lingual adaptation.

摘要：我們探討常見的 LoRA 變體在多語言指令調整中是否相較於基本 LoRA 有優勢。<lb/>涉及 LoRA 和其他四個變體的實驗在兩個數據集上進行，涵蓋多種目標語言，顯示使用更複雜的 LoRA 變體相較於基本 LoRA 在平衡跨語言轉移和知識保留方面並沒有顯著優勢。<lb/>對隱藏嵌入的分析顯示，使用不同 LoRA 技術微調的 LLM 在層級語言表示上大致相似，這表明 LoRA 技術的架構新穎性可能無法轉化為更好的跨語言適應。

##### **When Poison Fails After Retrieval: Revisiting Corpus Poisoning under Chunking and Reranking Pipelines**
2606.11265v1 by Xi Nie, Hongwei Li, Shenghao Wu, Mingxuan Li, Jiachen Li, Wenbo Jiang

Retrieval-Augmented Generation (RAG) systems are vulnerable to corpus poisoning attacks that manipulate downstream model outputs through malicious knowledge injection. Existing studies mainly evaluate poisoning under simplified retrieval settings, overlooking practical RAG pipelines involving document chunking, dense retrieval, reranking, and grounded generation. In this paper, we revisit corpus poisoning under realistic multi-stage retrieval pipelines and show that many existing attacks substantially degrade after reranking despite achieving high retrieval-stage relevance. We identify retrieval granularity mismatch as a key reason for this failure: document-level adversarial signals are often fragmented during chunking, while rerankers favor locally coherent and answer-bearing passages rather than globally optimized semantic similarity. Based on this observation, we propose Chunk-aware and Rerank-Consistent Poisoning (CRCP), a poisoning framework that jointly optimizes retrieval relevance, reranker consistency, and chunk-boundary robustness. CRCP explicitly models chunking transformations during optimization to generate locally self-contained adversarial passages that remain effective under varying chunking configurations. Experiments on standard RAG benchmarks with multiple retrievers and rerankers show that existing poisoning methods are highly sensitive to chunk size and reranking strategies, whereas CRCP achieves substantially higher attack success rates and stronger robustness across realistic retrieval pipelines. Our findings highlight an important realism gap in current RAG security evaluation and suggest that poisoning in modern RAG systems should be studied as a multi-stage retrieval consistency problem rather than a retrieval-only problem.

摘要：檢索增強生成（RAG）系統容易受到語料庫毒害攻擊，這種攻擊通過惡意知識注入來操縱下游模型的輸出。現有研究主要在簡化的檢索設置下評估毒害，忽略了涉及文檔分塊、密集檢索、重排序和基於內容生成的實際 RAG 管道。在本文中，我們重新檢視現實的多階段檢索管道下的語料庫毒害，並顯示許多現有攻擊在重排序後顯著降級，儘管在檢索階段達到了高相關性。我們確定檢索粒度不匹配是這一失敗的關鍵原因：文檔級對抗信號在分塊過程中往往被碎片化，而重排序器則偏好局部一致且包含答案的段落，而不是全局優化的語義相似性。基於這一觀察，我們提出了考慮分塊和重排序一致性的毒害（CRCP），這是一個毒害框架，旨在聯合優化檢索相關性、重排序一致性和分塊邊界的穩健性。CRCP 在優化過程中明確建模分塊轉換，以生成在不同分塊配置下仍然有效的局部自包含對抗段落。在多個檢索器和重排序器的標準 RAG 基準上進行的實驗顯示，現有的毒害方法對分塊大小和重排序策略非常敏感，而 CRCP 在現實檢索管道中實現了顯著更高的攻擊成功率和更強的穩健性。我們的研究結果突顯了當前 RAG 安全評估中的一個重要現實差距，並表明在現代 RAG 系統中的毒害應該作為一個多階段檢索一致性問題來研究，而不是僅僅作為檢索問題。

##### **FOGO: Forgetting-aware Orthogonalization Optimizer**
2606.10406v1 by Toan Nguyen, Yang Liu, Trung Le, Celso de Melo, Flora D. Salim

We argue that forgetting is not confined to continual learning but is a general optimization phenomenon: during standard training, dominant mini-batch gradients suppress rare but useful update directions, causing short-term forgetting at every step. When such knowledge is never revisited, these losses compound into long-term forgetting-the classical failure mode of continual learning. We introduce FOGO, a scalable optimizer that continuously detects and resolves gradient interference across both regimes. FOGO spectrally orthogonalizes momentum updates to prevent dominant directions from monopolizing optimization, then stores representative past directions in a compact codebook memory built on random projection, where pairwise distances are provably preserved in low-dimensional space. At each step, conflicts between the current update and stored directions are resolved via lightweight orthogonal correction and lifted back through a proximal step, with minimal overhead and no data storage. Across class-imbalanced classification, continual visual learning under domain and class shifts, continual fine-tuning of LLaVA-7B, and GPT-2 pretraining, FOGO consistently improves convergence and knowledge retention, outperforming Adam and Muon.

摘要：我們主張，遺忘並不僅限於持續學習，而是一種普遍的優化現象：在標準訓練過程中，主導的迷你批次梯度會壓制稀有但有用的更新方向，導致每一步的短期遺忘。當這些知識從未被重新訪問時，這些損失會累積成長期遺忘——這是持續學習的經典失敗模式。我們引入了FOGO，一種可擴展的優化器，能夠持續檢測和解決兩種情境下的梯度干擾。FOGO以光譜正交化動量更新，以防止主導方向壟斷優化，然後將代表性的過去方向存儲在基於隨機投影的緊湊代碼簿內存中，這裡的成對距離在低維空間中是可證明保留的。在每一步中，當前更新與存儲方向之間的衝突通過輕量級正交修正來解決，並通過近端步驟提升回去，這樣的開銷最小且不需要數據存儲。在類別不平衡分類、在領域和類別轉變下的持續視覺學習、LLaVA-7B的持續微調以及GPT-2的預訓練中，FOGO始終改善收斂和知識保留，超越了Adam和Muon。

##### **Do Vision-Language Models See or Guess? Measuring and Reducing Textual-Prior Reliance with a Phrasing-Controlled Benchmark**
2606.10400v1 by Pratham Singla, Shivank Garg, Vihan Singh, Paras Chopra

Vision-language models (VLMs) are increasingly deployed where answers must follow from what is in the image, yet they often answer from textual priors, the question's phrasing together with memorized world knowledge, rather than from the image itself, which inflates benchmark scores and yields confident but ungrounded answers. Existing benchmarks rarely isolate this behavior, since each image is usually paired with a single fixed question. To measure the reliance, we build a 540-image benchmark across six reasoning categories and generate four question variants over the same images, so that phrasing rather than image content is the controlled variable. The hardest variant is written directly from the image to minimize text leakage. We benchmark eleven VLMs spanning small open-weight models to large closed-source systems: every model degrades on the hardest variant, and open models fall furthest. Our central diagnostic is a no-image ablation, which collapses the open-weight models to their text-only floor (1 to 9 percent). Three further analyses, LLM-rated difficulty, low base-to-final textual similarity, and human re-annotation, corroborate genuine image-dependence. In-context exemplars that match how a variant was built recover the most accuracy, and GRPO post-training of a small VLM yields consistent gains across all four variants that transfer to a held-out out-of-distribution set. Textual-prior reliance is measurable and partly trainable away.

摘要：視覺語言模型（VLMs）越來越多地被應用於需要根據圖像內容給出答案的情境，但它們經常根據文本先驗、問題的措辭以及記憶中的世界知識來回答，而不是直接從圖像本身回答，這導致基準分數膨脹並產生自信但缺乏根據的答案。現有的基準很少單獨隔離這種行為，因為每個圖像通常只與一個固定問題配對。為了測量這種依賴性，我們建立了一個涵蓋六個推理類別的540幅圖像基準，並針對相同圖像生成四個問題變體，以便措辭而非圖像內容成為受控變數。最難的變體是直接從圖像撰寫，以最小化文本泄漏。我們對十一個VLM進行基準測試，涵蓋從小型開放權重模型到大型封閉源系統：每個模型在最難的變體上都有所下降，而開放模型的下降最為明顯。我們的主要診斷是一個無圖像消融，這使得開放權重模型的表現降至其僅基於文本的底線（1%到9%）。三項進一步分析，即LLM評估的難度、低基準到最終文本相似度，以及人類重新標註，證實了真正的圖像依賴性。與變體構建方式匹配的上下文示例能夠恢復最高的準確性，而小型VLM的GRPO後訓練在所有四個變體上都帶來了一致的增益，這些增益轉移到一個保留的分佈外數據集上。文本先驗的依賴性是可測量的，並且部分可以通過訓練消除。

##### **Instruction Finetuning DeepSeek-R1-8B Model Using LoRA and NEFTune**
2606.10392v1 by Wu Yuerong, Mingni Luo

Financial named-entity recognition (NER) is essential for translating unstructured financial reports and news into structured knowledge graphs. However, general-purpose large language models (LLMs) often misclassify financial entities or ignore domain-specific patterns. This paper investigates the use of DeepSeek-R1-8B, a recent open-source large language model, combined with Low-Rank Adaptation (LoRA) and Noisy Embedding Fine-Tuning (NEFTune) for financial NER. Each annotated sentence in our corpus of 1693 samples is converted into an instruction-input-output triple. We insert lightweight LoRA matrices into the Transformer layers and apply NEFTune to improve generalisation by adding uniform noise to embedding vectors during training. Experiments show that the LoRA-adapted DeepSeek-R1-8B achieves a micro-F1 of 0.901 on seven entity types (Company, Date, Location, Money, Person, Product and Quantity), and adding NEFTune further boosts the micro-F1 to 0.912, outperforming Llama3-8B, Qwen3-8B, Baichuan2-7B, T5 and BERT-Base baselines.

摘要：金融命名實體識別 (NER) 對於將非結構化的財務報告和新聞轉換為結構化知識圖譜至關重要。<lb/>然而，通用的大型語言模型 (LLMs) 通常會錯誤分類財務實體或忽略特定領域的模式。<lb/>本文探討了使用 DeepSeek-R1-8B，這是一個最近的開源大型語言模型，結合低秩適應 (LoRA) 和噪聲嵌入微調 (NEFTune) 進行財務 NER 的方法。<lb/>我們的語料庫中 1693 個樣本的每個標註句子都被轉換為指令-輸入-輸出三元組。<lb/>我們在 Transformer 層中插入輕量級的 LoRA 矩陣，並應用 NEFTune 通過在訓練過程中向嵌入向量添加均勻噪聲來提高泛化能力。<lb/>實驗表明，經過 LoRA 調整的 DeepSeek-R1-8B 在七種類型的實體（公司、日期、地點、金錢、人物、產品和數量）上達到了 0.901 的微 F1 分數，並且添加 NEFTune 進一步將微 F1 提升至 0.912，超越了 Llama3-8B、Qwen3-8B、Baichuan2-7B、T5 和 BERT-Base 的基準。

##### **PADD: Path-Aligned Decompression Distillation for Non-Router Teacher to Guide MoE Student Learning**
2606.10369v1 by Xinyue Peng, Yi Qian, Jiaojiao Lin, Wenjian Shao, Yanming Liu

As large language models (LLMs) continue to scale, it becomes increasingly challenging to grow model capacity under fixed computation budgets. We propose Path-Aligned Decompression Distillation (PADD), a framework for distilling knowledge from dense teachers without explicit routing into mixture-of-experts (MoE) students while learning high-quality routing policies. PADD organizes knowledge distillation into four stages in two phases: an initialization phase (Stage I) that builds diverse functionality in the student's experts through teacher neuron clustering and student-expert warmup, and a training phase (Stages II--IV) that integrates online adaptive distillation, path-refined policy optimization, and reward-augmented load balancing in a single training pipeline. Experiments on mathematical reasoning benchmarks demonstrate that PADD yields substantial gains over strong baselines at the same inference cost and that the MoE student can match or surpass its dense teacher. They also demonstrate effective teacher-to-student knowledge distillation and stable routing behavior.

摘要：隨著大型語言模型（LLMs）持續擴展，在固定計算預算下增長模型容量變得越來越具挑戰性。我們提出了路徑對齊解壓蒸餾（PADD），這是一個從密集教師中提取知識的框架，無需明確路由到混合專家（MoE）學生，同時學習高質量的路由策略。PADD將知識蒸餾組織成四個階段，分為兩個階段：初始化階段（第一階段）通過教師神經元聚類和學生-專家預熱來建立學生專家的多樣化功能，以及訓練階段（第二至第四階段）在單一訓練管道中整合在線自適應蒸餾、路徑精煉策略優化和增強獎勵的負載平衡。在數學推理基準上的實驗表明，PADD在相同推理成本下相較於強基準獲得了顯著增益，並且MoE學生可以匹配或超越其密集教師。這些實驗還展示了有效的教師到學生的知識蒸餾和穩定的路由行為。

##### **ReflectiChain: Epistemic Grounding in LLM-Driven World Models for Supply Chain Resilience**
2606.10359v1 by Jia Luo

AI agents in supply chains face a fundamental epistemic gap: large language models (LLMs) interpret policies but lack physical grounding, while reinforcement learning (RL) optimizes flows but is semantically blind to unstructured constraints. We introduce REFLECTICHAIN, bridging this gap through a Generative Supply Chain World Model (SC-WM) - encoding heterogeneous supply networks into a 6-dim graph-latent space with physical conservation - and Double-Loop Learning that separates epistemic uncertainty (KL-trust-region-bounded policy adaptation) from aleatoric uncertainty (stochastic latent rollouts). On Semi-Sim, a 10-node semiconductor benchmark with SIR risk propagation, 6 perturbation types, and 10 policy constraint templates, REFLECTICHAIN improves Rationale Consistency Score by 33.0% (p < 0.0001, d = 2.78), maintains 82.3% operability under adversarial shocks, and exhibits anti-fragile behavior (+40.2% gain under moderate pressure). We identify three operational epistemic mechanisms - uncertainty separation, knowledge-boundary detection, and empirical Bayesian policy updating - and discuss five limitation categories.

摘要：AI 代理在供應鏈中面臨一個基本的認知差距：大型語言模型（LLMs）解釋政策但缺乏物理基礎，而強化學習（RL）優化流動卻對非結構性約束語義盲目。我們引入了 REFLECTICHAIN，通過生成供應鏈世界模型（SC-WM）來彌補這一差距——將異質供應網絡編碼為具有物理守恆的 6 維圖形潛在空間——以及雙迴路學習，將認知不確定性（KL-信任區域限制的政策適應）與隨機不確定性（隨機潛在展開）分開。在 Semi-Sim 上，一個具有 SIR 風險傳播、6 種擾動類型和 10 種政策約束模板的 10 節點半導體基準，REFLECTICHAIN 將理由一致性得分提高了 33.0%（p < 0.0001，d = 2.78），在對抗性衝擊下保持 82.3% 的可操作性，並顯示出反脆弱行為（在中等壓力下增長 40.2%）。我們確定了三種操作性認知機制——不確定性分離、知識邊界檢測和經驗貝葉斯政策更新——並討論了五種限制類別。

##### **KG-SoftMAP: Soft Knowledge-Graph Priors for Bayesian Network Structure Learning from Sparse Discrete Data**
2606.10358v1 by Guoliang Xu, James E. Corter

Learning Bayesian network (BN) structure from sparse discrete data is hard: when each instance records only a few variables, most variable pairs lack the joint observations needed for reliable scoring, and data-only methods recover little structure. Imperfect domain knowledge, expressible as a weighted directed knowledge graph (KG), is often available. We propose KG-SoftMAP, which encodes such a KG as a soft, confidence-weighted, data-overridable edge prior and maximizes a MAP objective combining the BDeu score with a logit-form prior; the KG may be expert-curated or LLM-extracted. On controlled synthetic benchmarks, the only setting with ground-truth DAGs, KG-SoftMAP recovers partial directed structure at $ρ=0.05$ (DF1 $0.14$ to $0.29$, versus near-zero baselines) and substantially more once $ρ\geq0.2$ (DF1 $0.46$ to $0.96$), when paired with an informative but imperfect KG; recovery degrades gracefully as KG quality drops. On real sparse educational data, which has no ground-truth DAG, we evaluate deployment-facing measures only: prediction, calibration, and KG-consistency. The learned BN is best read as a diagnostic model: on SAF it trails logistic regression by $0.03$ F1_FAIL while providing KG-consistent edges, calibrated joint probabilities, and inference from arbitrary observed concept subsets; when no meaningful KG exists, discriminative logistic regression is preferable.

摘要：從稀疏離散數據中學習貝葉斯網絡（BN）結構是困難的：當每個實例僅記錄幾個變量時，大多數變量對缺乏可靠評分所需的聯合觀察，而僅依賴數據的方法幾乎無法恢復結構。通常可用的不完美領域知識可以表達為加權有向知識圖（KG）。我們提出KG-SoftMAP，將這樣的KG編碼為柔性、信心加權、可被數據覆蓋的邊緣先驗，並最大化結合BDeu分數和邏輯形式先驗的MAP目標；KG可以是專家策劃的或LLM提取的。在受控的合成基準上，唯一具有真實DAG的設置，KG-SoftMAP在$ρ=0.05$時恢復部分有向結構（DF1從$0.14$到$0.29$，與接近零的基準相比），而當$ρ\geq0.2$時（DF1從$0.46$到$0.96$）則恢復的結構顯著更多，當與一個信息豐富但不完美的KG配對時；隨著KG質量下降，恢復的效果會優雅地退化。在沒有真實DAG的真實稀疏教育數據上，我們僅評估面向部署的指標：預測、校準和KG一致性。學習到的BN最好被視為診斷模型：在SAF上，它的F1_FAIL比邏輯回歸低$0.03$，同時提供KG一致的邊緣、校準的聯合概率，以及從任意觀察到的概念子集進行推斷；當沒有有意義的KG存在時，區別性邏輯回歸更可取。

##### **Content-Induced Spatial-Spectral Aggregation Network for Change Detection in Remote Sensing Images**
2606.10328v1 by Yunlong Liu, Zekai Zhang

The integration of spatial and spectral information is beneficial to the improvement of change detection performance. However, existing methods cannot efficiently suppress the influences of spatial and spectral differences in unchanged areas. To address these issues, in this paper we propose a content-guided spatial-spectral integration network (CSI-Net) for the fusion of global spatial details and spectral difference information. Specifically, the proposed CSI-Net is composed of a spatial reasoning (SR) module, a spectral difference (SD) module, and a content-guided integration (CGI) module. In the SR module, the spatial information is learned by cascaded graph convolution blocks for global modeling. The SD module is responsible for the extraction of spectral features, by calculating the means and variances of features to reduce the impact of spectral differences in unchanged regions. In addition, in order to integrate the spatial-spectral features efficiently, we design a CGI module to further take advantage of their complementary information. In this module, high-level content information is introduced as a guide for a proper interaction. Due to the efficient spatial-spectral fusion, the proposed CSI-Net can learn the changed features better while achieving a suppression of spectral differences. Experimental results on LEVIR-CD, WHU-CD, and CLCD datasets demonstrate that the proposed CSI-Net produces better performance compared to state-of-the-art methods, and is applicable to different scenarios

摘要：空間和光譜信息的整合有助於提高變化檢測的性能。<lb/>然而，現有的方法無法有效抑制在未變化區域中空間和光譜差異的影響。<lb/>為了解決這些問題，本文提出了一種內容引導的空間-光譜整合網絡（CSI-Net），用於全球空間細節和光譜差異信息的融合。<lb/>具體而言，所提出的CSI-Net由一個空間推理（SR）模塊、一個光譜差異（SD）模塊和一個內容引導整合（CGI）模塊組成。<lb/>在SR模塊中，通過級聯圖卷積塊學習空間信息以進行全局建模。<lb/>SD模塊負責提取光譜特徵，通過計算特徵的均值和方差來減少在未變化區域中光譜差異的影響。<lb/>此外，為了有效整合空間-光譜特徵，我們設計了一個CGI模塊，以進一步利用它們的互補信息。<lb/>在此模塊中，引入高級內容信息作為適當交互的指導。<lb/>由於高效的空間-光譜融合，所提出的CSI-Net能夠更好地學習變化特徵，同時抑制光譜差異。<lb/>在LEVIR-CD、WHU-CD和CLCD數據集上的實驗結果表明，所提出的CSI-Net相比於最先進的方法產生了更好的性能，並且適用於不同場景。

##### **From Context-Aware to Conflict-Aware: Generalizing Contrastive Decoding for Knowledge Conflict in LLMs**
2606.10298v1 by Runze Jiang, Taiqiang Wu, Yan Wang, Bingyu Zhu, Longtao Huang

When large language models generate from retrieved or augmented contexts, conflicts between external context and parametric priors remain a central reliability bottleneck. Existing contrastive decoding methods follow a \emph{context-aware} paradigm that unilaterally amplifies context over parametric priors, overwriting correct priors when the context is erroneous. We generalize this to the \textbf{conflict-aware} paradigm that dynamically allocates authority between prior and context based on conflict signals, rather than presupposing context trustworthiness. We show that the affine combination of prior and context logits yields a \textbf{power family} with an inherent \textbf{regime asymmetry}: extrapolation amplifies errors unboundedly when the prior is correct, interpolation under-corrects when the context is correct, and no static regime covers both. Existing contrastive decoding methods are instances of this family, mostly extrapolative. To evaluate both conflict directions, we propose TriState-Bench, a model-aware evaluation protocol that calibrates per-model prior knowledge to measure three conflict states: correction, resistance, and agreement. To resolve the asymmetry, we propose Adaptive Regime Routing (ARR), which routes between regimes at each step, lifting resistance EM from below 6 to 16--33 without sacrificing correction or agreement. Our code is available at https://github.com/keith-Jiang/conflict-aware-decoding.

摘要：當大型語言模型從檢索或增強的上下文生成時，外部上下文與參數先驗之間的衝突仍然是中心可靠性瓶頸。現有的對比解碼方法遵循一種\emph{上下文感知}的範式，單方面放大上下文而忽略參數先驗，在上下文錯誤時覆蓋正確的先驗。我們將此概括為\textbf{衝突感知}範式，根據衝突信號動態分配先驗和上下文之間的權限，而不是假定上下文的可信度。我們展示了先驗和上下文logits的仿射組合產生了一個\textbf{功率家族}，其固有的\textbf{範式不對稱性}：當先驗正確時，外推無限制地放大錯誤，當上下文正確時，插值則不足以校正，且沒有靜態範式能同時涵蓋兩者。現有的對比解碼方法是這個家族的實例，大多數是外推性的。為了評估這兩種衝突方向，我們提出了TriState-Bench，一種模型感知的評估協議，對每個模型的先驗知識進行校準，以測量三種衝突狀態：修正、抵抗和一致性。為了解決不對稱性，我們提出了自適應範式路由（ARR），該方法在每一步之間路由範式，將抵抗EM從6以下提升至16-33，而不犧牲修正或一致性。我們的代碼可在https://github.com/keith-Jiang/conflict-aware-decoding獲得。

##### **When Metrics Disagree: A Meta-Analysis of Knowledge-Graph-Completion Model Benchmarking**
2606.10287v1 by Haji Gul, Ajaz Ahmad Bhat

Evaluating Knowledge Graph Completion (KGC) models remains challenging because standard assessment relies on isolated rank-based metrics such as MRR, Hits$@$k, and Mean Rank, which often produce conflicting model orderings across datasets. A model that leads on MRR may trail on Hits@1, and strong performance on one dataset may not generalize to another. This fragmentation hinders comparison, enables selective reporting, and obscures real progress. We reframe KGC evaluation as a Multi-Criteria Decision-Making (MCDM) problem and present a meta-analysis of seven aggregators across five tests: consistency, cross-dataset stability, metric independence, robustness under noise, and generalizability. Each test is averaged over leave-one-model-out (LOMO) and leave-one-group-out (LOGO) removals so that reliability reflects aggregator behavior across diverse model subsets. Across tail $(h,r,?)$ and relation $(h,?,t)$ prediction, Pareto-optimal analysis identifies Z-score as the most balanced aggregator, which ranks DualE highest for tail prediction and FMS (Flow-Modulated Scoring) highest for relation prediction. A test-sensitivity analysis using the same removals shows that consistency and stability are largely removal-invariant, while generalizability and independence are the most sensitive. The framework resolves evaluation inconsistencies and offers evidence-based guidance for aggregator selection and model benchmarking in KGC.

摘要：評估知識圖譜補全（KGC）模型仍然具有挑戰性，因為標準評估依賴於孤立的基於排名的指標，如MRR、Hits$@$k和平均排名，這些指標在不同數據集之間經常產生相互矛盾的模型排序。一個在MRR上表現良好的模型可能在Hits@1上表現不佳，而在一個數據集上的強勁表現可能無法推廣到另一個數據集。這種碎片化妨礙了比較，促進了選擇性報告，並掩蓋了實際進展。我們將KGC評估重新構建為多準則決策問題（MCDM），並呈現對五個測試中七個聚合器的元分析：一致性、跨數據集穩定性、指標獨立性、噪聲下的穩健性和可推廣性。每個測試都在留一模型（LOMO）和留一組（LOGO）移除的基礎上進行平均，以便可靠性反映聚合器在不同模型子集中的行為。在尾部 $(h,r,?)$ 和關係 $(h,?,t)$ 預測中，Pareto最優分析確定Z-score為最平衡的聚合器，該聚合器在尾部預測中將DualE排在最高，而在關係預測中將FMS（流調制計分）排在最高。使用相同的移除進行的測試敏感性分析顯示，一致性和穩定性在很大程度上是不變的，而可推廣性和獨立性則是最敏感的。該框架解決了評估不一致性，並為KGC中的聚合器選擇和模型基準測試提供了基於證據的指導。

##### **Energy-Efficient On-Device RAG on a Mobile NPU: System Design and Benchmark on Snapdragon X Elite**
2606.11257v1 by Zhiyuan Cheng, Longying Lai

Retrieval-Augmented Generation (RAG) pipelines are compute-intensive, combining embedding, retrieval, reranking, and large language model (LLM) generation. Running them entirely on-device benefits privacy, latency, and offline use, but the energy cost of CPU inference is a major barrier. We present what is, to our knowledge, the first end-to-end RAG pipeline that runs all neural stages -- embedding, reranking, and LLM generation -- on the Qualcomm Hexagon NPU of the Snapdragon X Elite. Profiling on a Dell XPS 13 laptop, we compare NPU-accelerated RAG against CPU and OpenCL/Adreno GPU baselines on indexing and query workloads. On indexing, the NPU achieves 9.1x higher embedding throughput and 12.3x less system energy. On a 120-query Wikipedia-passage benchmark, it delivers 18.1x faster LLM prefilling, 4.0x lower end-to-end query latency, and 4.0x less system energy than the CPU baseline; the same workload on the integrated GPU is 1.7x slower than CPU and uses 6.5x more energy than the NPU. A GPT-4.1 LLM-as-judge evaluation finds NPU answer quality on par with CPU and GPU within evaluator noise (mean 9.32 vs. 8.95 vs. 9.03 on a 1-10 rubric), with 86.7% of queries scoring identically across all three backends. On the Snapdragon X Elite / Hexagon class of laptop SoC, the NPU thus enables practical, energy-efficient on-device RAG without quality regression -- a sustainable path toward green edge intelligence that we expect to generalize to comparable mobile NPUs (Apple Neural Engine, Intel NPU, MediaTek APU) as their software stacks mature.

摘要：檢索增強生成（RAG）管道是計算密集型的，結合了嵌入、檢索、重新排序和大型語言模型（LLM）生成。將它們完全在設備上運行有助於隱私、延遲和離線使用，但 CPU 推理的能量成本是一個主要障礙。我們提出了我們所知的第一個端到端 RAG 管道，該管道在 Snapdragon X Elite 的 Qualcomm Hexagon NPU 上運行所有神經階段——嵌入、重新排序和 LLM 生成。在 Dell XPS 13 筆記本電腦上進行分析，我們將 NPU 加速的 RAG 與 CPU 和 OpenCL/Adreno GPU 基準進行了比較，針對索引和查詢工作負載。在索引方面，NPU 實現了 9.1 倍更高的嵌入吞吐量和 12.3 倍更低的系統能量。在一個 120 查詢的維基百科段落基準上，它提供了 18.1 倍更快的 LLM 預填充、4.0 倍更低的端到端查詢延遲，以及比 CPU 基準低 4.0 倍的系統能量；在集成 GPU 上執行相同的工作負載比 CPU 慢 1.7 倍，並且使用的能量是 NPU 的 6.5 倍。一項 GPT-4.1 LLM 作為評估者的評估發現 NPU 的答案質量與 CPU 和 GPU 在評估者噪聲範圍內相當（平均 9.32 對 8.95 對 9.03，滿分 10 分），有 86.7% 的查詢在所有三個後端中得分相同。在 Snapdragon X Elite / Hexagon 類型的筆記本 SoC 上，NPU 使得在設備上實現實用的、能源高效的 RAG 成為可能，且不會降低質量——這是一條可持續的綠色邊緣智能之路，我們預期這將普遍適用於類似的移動 NPU（Apple Neural Engine、Intel NPU、MediaTek APU），隨著它們的軟件堆棧成熟。

##### **A Source Domain is All You Need: Source-Only Cross-OS Transfer Learning for APT Anomaly Detection via Semantic Alignment and Optimal Transport**
2606.10216v1 by Sidahmed Benabderrahmanea, Petko Valtchev, James Cheney, Talal Rahwan

Advanced Persistent Threats (APTs) are stealthy, multi-stage cyberattacks whose detection is difficult due to scarce labeled traces, severe class imbalance, and the challenge of generating realistic malicious behavior. These challenges are amplified in cross-operating-system (cross-OS) settings, where a detector trained on one source platform must be deployed on an unlabeled target platform without access to target-domain labels. We study this source-only cross-OS APT detection problem using system-level provenance traces and propose a transport-based framework for ranking anomalous target processes under zero target supervision. The framework abstracts process behavior into structured natural-language descriptions, embeds them using pretrained language models, and constructs a source-normal reference for target scoring. It combines three evidence channels: semantic deviation from source-normal prototypes, structural deviation captured by graph autoencoding, and geometric deviation measured through Optimal Transport (OT). The main contribution is an OT-based barycentric anomaly score that projects target embeddings onto the source-normal manifold and quantifies residual transport mismatch. We further introduce entropy-weighted, angle-aware, and density-aware OT variants to capture uncertainty, directional drift, and sparse-support behavior. Evaluation on DARPA Transparent Computing data spanning Linux, Windows, BSD, and Android, across two APT scenarios and twelve cross-OS transfer pairs, shows that the proposed framework improves ROC-AUC and nDCG over source-only anomaly-detection baselines. The results demonstrate that source-only provenance modeling, combined with semantic abstraction and OT-based anomaly scoring, can support practical cross-platform APT detection without target-domain supervision.

摘要：進階持續威脅（APTs）是隱蔽的多階段網路攻擊，由於標記痕跡稀少、類別不平衡嚴重以及生成現實惡意行為的挑戰，使得其檢測變得困難。這些挑戰在跨作業系統（cross-OS）環境中更為明顯，在這種情況下，訓練於一個源平台的檢測器必須在沒有目標領域標籤的情況下部署到一個未標記的目標平台。我們使用系統級來源追蹤研究這一僅源的跨作業系統 APT 檢測問題，並提出了一個基於運輸的框架，用於在零目標監督下對異常目標進程進行排名。該框架將進程行為抽象為結構化的自然語言描述，使用預訓練的語言模型進行嵌入，並構建一個源正常參考以進行目標評分。它結合了三個證據通道：來自源正常原型的語義偏差、通過圖自編碼捕獲的結構偏差，以及通過最優運輸（OT）測量的幾何偏差。主要貢獻是一個基於 OT 的重心異常分數，將目標嵌入投影到源正常流形上，並量化殘餘運輸不匹配。我們進一步引入了熵加權、角度感知和密度感知的 OT 變體，以捕捉不確定性、方向漂移和稀疏支持行為。在涵蓋 Linux、Windows、BSD 和 Android 的 DARPA 透明計算數據上，針對兩個 APT 場景和十二個跨作業系統轉移對的評估顯示，所提出的框架在 ROC-AUC 和 nDCG 上優於僅源的異常檢測基準。結果表明，僅源的來源建模結合語義抽象和基於 OT 的異常評分，可以支持實用的跨平台 APT 檢測，而無需目標領域的監督。

##### **Gaming AI-Assisted Peer Reviews Poses New Risks to the Scientific Community**
2606.10159v1 by Lin Li, Qi Zhang, Xander Davies, Jianing Qiu, Yarin Gal

AI is increasingly used to support scientific peer review, from manuscript screening, reviewer assistance to editorial triage. Although such systems promise to reduce reviewer burden and accelerate publication, their robustness to strategic manipulation remains poorly understood. Here we show that AI-mediated peer review is vulnerable to a simple, low-cost manipulation: superficial rephrasing of the manuscript abstract. Without changing the underlying scientific content and communication, and even without knowledge of the reviewing model, adversarially rewritten abstracts substantially improve AI review outcomes. We see this across disciplines and publication venues, for both human-written and AI-generated papers. Our strongest attack achieves an attack-success-rate of about 38%, increasing acceptance ratings by +1.31 for Gemini 3 Flash reviewers and by +0.88 for GPT 5.4 Mini reviewers on a 10-point scale. When the original AI review suggests 'reject', the success rate rises to more than 50%. This effect extends beyond overall score inflation, increasing review confidence and scores on core scientific criteria such as soundness, significance and perceived contribution. The attack is practical, requiring only about 5 minutes and $1 for a 10-page AI conference submission, and is hard to distinguish from ordinary scientific editing. Inflated AI reviews could bias downstream human decision-making, shifting editorial recommendations from rejection towards acceptance. These findings reveal a general vulnerability in AI-assisted scientific evaluation: when AI-generated review influence editorial decisions, authors may be incentivized to optimize manuscripts for AI judgment rather than scientific merit. Our results suggest that AI tools should not be treated as neutral evaluators in high-stakes peer review without systematic robustness testing, transparent safeguards and careful human oversight.

摘要：AI 越來越多地被用來支持科學同行評審，從手稿篩選、審稿人協助到編輯篩選。雖然這些系統承諾減輕審稿人的負擔並加速發表，但它們對策略性操縱的穩健性仍然不甚了解。這裡我們顯示，AI 媒介的同行評審容易受到一種簡單、低成本的操縱：對手稿摘要的表面重述。在不改變基礎科學內容和交流的情況下，甚至在不知曉評審模型的情況下，對抗性重寫的摘要顯著改善了 AI 評審結果。我們在各個學科和發表場所都觀察到了這一現象，無論是人類撰寫的論文還是 AI 生成的論文。我們最強的攻擊達到了約 38% 的攻擊成功率，對於 Gemini 3 Flash 審稿人，接受率提高了 +1.31，而對於 GPT 5.4 Mini 審稿人，則提高了 +0.88，滿分為 10 分。當原始 AI 評審建議“拒絕”時，成功率上升到超過 50%。這一效果超越了整體分數膨脹，增強了對核心科學標準（如健全性、重要性和感知貢獻）的評審信心和分數。這一攻擊是實際可行的，對於一篇 10 頁的 AI 會議投稿，只需約 5 分鐘和 1 美元，且難以與普通的科學編輯區分開來。膨脹的 AI 評審可能會對下游的人類決策產生偏見，將編輯建議從拒絕轉向接受。這些發現揭示了 AI 輔助科學評估的一個普遍脆弱性：當 AI 生成的評審影響編輯決策時，作者可能會被激勵去優化手稿以符合 AI 的評判，而非科學價值。我們的結果表明，AI 工具在高風險的同行評審中不應被視為中立的評估者，而應進行系統的穩健性測試、透明的保障措施和謹慎的人類監督。

##### **MetaPlate: Counterfactual-Guided RAG-LLM Tool for Personalized Food Recommendation and Hyperglycemia Prevention**
2606.10120v2 by Asiful Arefeen, Carol Johnston, Hassan Ghasemzadeh

Postprandial hyperglycemia is a key risk factor for metabolic disorders; however, existing dietary guidance is often static, impractical, and insufficiently personalized, providing recommendations that are difficult to follow or not impactful. While recent advances leverage continuous glucose monitoring (CGM) and machine learning to predict glycemic responses, these approaches are largely predictive and lack actionable guidance. Moreover, recommendation systems are often misaligned with user goals and require extensive input. We present MetaPlate, a counterfactual explanation (CF) guided, context-aware decision-support framework that generates personalized meal recommendations to mitigate postprandial glucose excursions in healthy adults. MetaPlate integrates multimodal data, including CGM readings, wearable-derived physiological signals, and user-provided meal inputs from $25$ individuals to model pre-meal context. A machine learning model predicts glucose response, while a CF optimization module adjusts meal composition modifying macronutrient amounts to maintain glucose levels within a target range ($\leq 140$ mg/dL). An LLM-based retrieval-augmented generation (RAG) layer enhances interpretability by producing human-readable recommendations using constrained search of the USDA food database. We evaluate MetaPlate via a structured expert-in-the-loop assessment with registered dietitians (RDs), comparing performance before and after prompt refinement. Results show improvements in meal realism, portion suitability, and recommendation likelihood, with expert feedback indicating a shift from clinically implausible outputs to actionable, contextually appropriate recommendations. Our findings emphasize the importance of domain knowledge and structured constraints in LLM-driven systems and highlight the potential of MetaPlate as a real-time personalized dietary decision-support tool.

摘要：餐後高血糖是代謝疾病的主要風險因素；然而，現有的飲食指導往往是靜態的、不切實際的，且個性化程度不足，提供的建議難以遵循或缺乏影響力。儘管最近的進展利用持續血糖監測（CGM）和機器學習來預測血糖反應，但這些方法主要是預測性的，缺乏可行的指導。此外，推薦系統往往與用戶目標不一致，並需要大量的輸入。我們提出了MetaPlate，一個以反事實解釋（CF）為指導的、具上下文感知的決策支持框架，旨在生成個性化的餐飲建議，以減少健康成人的餐後血糖波動。MetaPlate整合了多模態數據，包括CGM讀數、可穿戴設備獲得的生理信號，以及來自$25$名個體的用戶提供的餐飲輸入，以建模餐前上下文。一個機器學習模型預測血糖反應，而CF優化模塊則調整餐飲組成，修改宏量營養素的數量，以保持血糖水平在目標範圍內（$\leq 140$ mg/dL）。一個基於LLM的檢索增強生成（RAG）層通過使用對美國農業部食品數據庫的約束搜索，增強了可解釋性，生成可讀的建議。我們通過與註冊營養師（RDs）的結構化專家循環評估來評估MetaPlate，並比較提示優化前後的表現。結果顯示在餐飲真實性、份量適合性和建議可能性方面有所改善，專家反饋表明從臨床上不切實際的輸出轉變為可行的、上下文適當的建議。我們的研究結果強調了領域知識和結構性約束在LLM驅動系統中的重要性，並突顯了MetaPlate作為實時個性化飲食決策支持工具的潛力。

##### **CodeAlchemy: Synthetic Code Rewriting at Scale**
2606.10087v1 by Ankit Gupta, Aditya Prasad, Rameswar Panda

Pre-training on raw code teaches syntax but provides sparse signal for diverse real-world task formats. While synthetic data has proven transformative for language models, code remains largely unexplored beyond limited quality improvements. We present CodeAlchemy, a synthetic data generation framework that transforms publicly sourced code into semantically-rich training data through 5 strategies: CodeEnhance (quality-aware rewriting), CodeQA (template-based problems), CodeDev (developer tasks), CodeDialogue (multi-turn conversations), and CodeTrace (execution traces). We process 3 corpora across 15 languages to generate 500B+ tokens of synthetic data plus 350B reasoning tokens, orders of magnitude more than prior efforts. CodeTrace instruments and executes 1.3M+ files across 14 languages and 5K libraries, capturing control flow, state tracking, and library knowledge. We introduce DevEval (developer tasks) and TraceEval (execution prediction) benchmarks; frontier models like Claude Sonnet 4.5 achieve only 5.6% exact match on TraceEval, revealing critical gaps in semantic understanding. Our 3B models achieve 83.5% on HumanEval, 63.2% on MBPP, 8.09% win rate on DevEval, and 15.36 ROUGE-2 on TraceEval, outperforming frontier models 10x the size including 27B Gemma-3 and 32B Granite-4.0.

摘要：預訓練原始代碼教會語法，但對於多樣化的現實世界任務格式提供的信號稀疏。雖然合成數據對於語言模型已證明具有變革性，但代碼在有限的質量改進之外仍然大多未被探索。我們提出了 CodeAlchemy，一個合成數據生成框架，通過五種策略將公開來源的代碼轉換為語義豐富的訓練數據：CodeEnhance（質量意識重寫）、CodeQA（基於模板的問題）、CodeDev（開發者任務）、CodeDialogue（多輪對話）和 CodeTrace（執行痕跡）。我們處理了 15 種語言的 3 個語料庫，生成了超過 5000 億個合成數據標記以及 350 億個推理標記，這是之前努力的數量級。CodeTrace 在 14 種語言和 5000 個庫中對 130 萬多個文件進行了儀器化和執行，捕捉控制流、狀態跟踪和庫知識。我們介紹了 DevEval（開發者任務）和 TraceEval（執行預測）基準；像 Claude Sonnet 4.5 這樣的前沿模型在 TraceEval 上僅達到 5.6% 的精確匹配，揭示了語義理解中的關鍵差距。我們的 30 億模型在 HumanEval 上達到 83.5%，在 MBPP 上達到 63.2%，在 DevEval 上的勝率為 8.09%，在 TraceEval 上的 ROUGE-2 為 15.36，超越了包括 270 億 Gemma-3 和 320 億 Granite-4.0 在內的前沿模型，規模是其 10 倍。

##### **VFUSE: Virulent Feature Understanding with Sparse autoEncoders**
2606.10080v1 by Michael Yu, Matthew L. Olson

Generative models have shown remarkable progress in a variety of domains such as protein design, but such power enables the opaque generation of hazardous proteins. In this work, we introduce VFUSE (Virulent Feature Understanding with Sparse autoEncoders), a mechanistic interpretability approach that trains SAEs on diffusion-transformer activations to audit protein models for hazard-aware features. We apply VFUSE to RoseTTAFold3 and RFDiffusion3, popular open-weight models for protein folding and synthesis. We find that for certain blocks, linear probes detect hazardous designs significantly better when fit in the SAE latent space over the original model's representations: improving interpretability without sacrificing model performance. Furthermore, we identify monosemantic features from the SAE that fire only on hazardous designs at up to AUROC $0.84$ ($q < 10^{-13}$). To our knowledge this is the first SAE trained on an all-atom diffusion model and the first feature-level virulence audit of a protein design model, paving the way towards safe and interpretable protein design.

摘要：生成模型在多個領域（如蛋白質設計）中顯示出顯著的進展，但這種能力使得危險蛋白質的生成變得不透明。在這項工作中，我們介紹了 VFUSE（使用稀疏自編碼器的致病特徵理解），這是一種機械解釋性的方法，通過在擴散Transformer的激活上訓練 SAE 來審核蛋白質模型的危險意識特徵。我們將 VFUSE 應用於 RoseTTAFold3 和 RFDiffusion3，這是流行的開放權重模型，用於蛋白質摺疊和合成。我們發現，對於某些區塊，當在 SAE 潛在空間中擬合時，線性探針能顯著更好地檢測到危險設計，這比原始模型的表示更有效：提高了解釋性而不犧牲模型性能。此外，我們從 SAE 中識別出僅在危險設計上激活的單義特徵，AUROC 高達 $0.84$ ($q &lt; 10^{-13}$)。據我們所知，這是第一個在全原子擴散模型上訓練的 SAE，也是第一個蛋白質設計模型的特徵級致病性審核，為安全和可解釋的蛋白質設計鋪平了道路。

##### **Temporal Sheaf Neural Networks with Dynamic Orthogonal Transport**
2606.10071v1 by Md Sadek Hossain Asif, Tanzila Khan, Md. Mosaddek Khan

We introduce Temporal Sheaf Neural Networks (TSNN), a temporal link prediction framework that equips each node with a time-varying orthogonal frame and compares node states only after explicit transport between local coordinate systems. In contrast to existing continuous-time graph models that operate in a shared global embedding space, TSNN models node-specific and evolving interaction semantics through dynamic local frames. The model parameterizes per-node frames via efficient low-rank Householder products, preserves stored hidden states exactly under frame updates, and uses a geometric-residual decoder that anchors predictions on transported distances while learning residual corrections. All computations are strictly causal and use only the pre-event history. We show that the symmetric degree-normalized sheaf Laplacian is orthogonally similar to the symmetric normalized graph Laplacian, with the random-walk normalized form similar in the corresponding degree metric; the full-active, feature-scaled diffusion used by TSNN is exactly a metric-gradient step on the combinatorial sheaf Dirichlet energy, with a degree-free monotone-descent and non-expansiveness guarantee. Frame drift perturbs updates only linearly. Across TGB v2 link-prediction and temporal-heterogeneous leaderboards, together with the DGB benchmark suite, TSNN matches or surpasses the strongest prior methods on most benchmarks, with the largest improvements on graphs exhibiting strong node-role heterogeneity. Ablations confirm the distinct benefit of dynamic frames, orthogonal transport, and geometric-residual decoding.

摘要：我們介紹了時間束神經網絡（TSNN），這是一種時間鏈接預測框架，為每個節點配備了隨時間變化的正交框架，並僅在明確的局部坐標系之間進行傳輸後比較節點狀態。與現有的在共享全局嵌入空間中運作的連續時間圖模型相比，TSNN通過動態局部框架建模節點特定和不斷演變的交互語義。該模型通過高效的低秩Householder乘積對每個節點的框架進行參數化，在框架更新下精確保留存儲的隱藏狀態，並使用幾何殘差解碼器，將預測固定在傳輸距離上，同時學習殘差修正。所有計算都是嚴格因果的，僅使用事件前的歷史。我們展示了對稱度正規化束拉普拉斯算子在正交上與對稱正規化圖拉普拉斯算子相似，隨機漫步正規化形式在相應的度量標準中也類似；TSNN使用的全活躍、特徵縮放擴散恰好是組合束Dirichlet能量上的度量梯度步驟，並具有無度的單調下降和非擴展性保證。框架漂移僅以線性方式擾動更新。在TGB v2鏈接預測和時間異質性排行榜中，連同DGB基準套件，TSNN在大多數基準上匹配或超越了最強的先前方法，對於顯示強節點角色異質性的圖形，改進幅度最大。消融實驗確認了動態框架、正交傳輸和幾何殘差解碼的明顯好處。

##### **FASE: Fast Adaptive Semantic Entropy for Code Quality**
2606.09800v1 by Shizhe Lin, Ladan Tahvildari

Multi-agent code generation offers a promising paradigm for autonomous software development by simulating the human software engineering lifecycle. However, system reliability remains hindered by LLM hallucinations and error propagation across interacting agents. While semantic entropy provides a principled way to quantify uncertainty without ground-truth answers, current methods often rely on costly LLM-driven equivalence checks. In this work, we introduce Fast Adaptive Semantic Entropy (FASE), a novel metric that approximates functional correctness based on the minimum spanning tree of structural and semantic dissimilarity graphs. Evaluations on HumanEval and BigCodeBench demonstrate that FASE outperforms state-of-the-art semantic entropy by LLM entailment, achieving a 25% average improvement in Spearman correlation and a 19% increase in ROCAUC score against Pass@1 from ground-truth test cases when using the Qwen3-Embedding-8B model. Furthermore, by eliminating costly LLM-driven equivalence evaluation, FASE incurs negligible computational overhead, requiring only approximately 0.3% of the runtime cost of traditional semantic entropy approaches. These results position FASE as a practical, cost-effective solution for optimizing uncertainty quantification in real-world multi-agent workflows.

摘要：多代理程式碼生成提供了一個有前景的範式，用於通過模擬人類軟體工程生命周期來實現自主軟體開發。<lb/>然而，系統的可靠性仍然受到 LLM 幻覺和互動代理之間的錯誤傳播的阻礙。<lb/>雖然語義熵提供了一種原則性的方法來量化不確定性，而不需要真實答案，但當前的方法往往依賴於昂貴的 LLM 驅動的等價性檢查。<lb/>在這項工作中，我們介紹了快速自適應語義熵（FASE），這是一種新穎的度量，基於結構和語義不相似圖的最小生成樹來近似功能正確性。<lb/>在 HumanEval 和 BigCodeBench 上的評估顯示，FASE 在 LLM 蕴涵的最先進語義熵方面表現優於其他方法，實現了 25% 的斯皮爾曼相關性平均提升，以及在使用 Qwen3-Embedding-8B 模型時，對於來自真實測試案例的 Pass@1 的 ROCAUC 分數提高了 19%。<lb/>此外，通過消除昂貴的 LLM 驅動的等價性評估，FASE 產生的計算開銷微乎其微，僅需傳統語義熵方法約 0.3% 的運行時間成本。<lb/>這些結果使 FASE 成為優化現實世界多代理工作流程中不確定性量化的實用且具成本效益的解決方案。

##### **Difference-Aware Retrieval Policies for Imitation Learning**
2606.09758v1 by Quinn Pfeifer, Ethan Pronovost, Paarth Shah, Khimya Khetarpal, Siddhartha Srinivasa, Abhishek Gupta

Parametric imitation learning via behavior cloning can suffer from poor generalization to out-of-distribution states due to compounding errors during deployment. We show that reusing the training data during inference via a semi-parametric retrieval-based imitation learning approach can alleviate this challenge. We present Difference-Aware Retrieval Policies for Imitation Learning (DARP), a semi-parametric retrieval-based imitation learning approach that addresses this limitation by reparameterizing the imitation learning problem in terms of local neighborhood structure rather than direct state-to-action mappings. Instead of learning a global policy, DARP trains a model to predict actions based on $k$-nearest neighbors from expert demonstrations, their corresponding actions, and the relative distance vectors between neighbor states and query states. DARP requires no additional assumptions beyond those made for standard behavior cloning -- it does not require additional data collection, online expert feedback, or task-specific knowledge. We demonstrate consistent performance improvements of 15-46% over standard behavior cloning across diverse domains, including continuous control and robotic manipulation, and across different representations, including high-dimensional visual features. Code and demos are available at https://weirdlabuw.github.io/darp-site/.

摘要：參數化模仿學習透過行為複製可能會因為在部署過程中累積的錯誤而導致對於分布外狀態的泛化能力差。我們展示了通過一種半參數檢索基礎的模仿學習方法，在推理過程中重用訓練數據可以緩解這一挑戰。我們提出了差異感知檢索策略（Difference-Aware Retrieval Policies for Imitation Learning, DARP），這是一種半參數檢索基礎的模仿學習方法，通過重新參數化模仿學習問題，使其以局部鄰域結構而非直接的狀態到行動映射來解決這一限制。DARP並不學習全局策略，而是訓練模型根據來自專家演示的$k$-最近鄰、其相應的行動以及鄰居狀態與查詢狀態之間的相對距離向量來預測行動。DARP不需要超出標準行為複製所做的額外假設——它不需要額外的數據收集、在線專家反饋或任務特定知識。我們展示了在各種領域（包括連續控制和機器人操作）以及不同表示（包括高維視覺特徵）中，DARP相對於標準行為複製的一致性能提升為15-46%。代碼和演示可在 https://weirdlabuw.github.io/darp-site/ 獲得。

##### **The Neutral Mask: How RLHF Provides Shallow Alignment while Leaving Partisan Structure Intact in a Large Language Model**
2606.09735v1 by Wendy K. Tam

The ambition behind alignment training is to make large language models safe and useful. The primary mechanism, reinforcement learning from human feedback (RLHF), shapes the behavior of deployed language models by aligning them with ``human values.'' Yet the process is opaque. What values are being encoded; whose values are they; and how does RLHF encode them? A growing body of evidence suggests that RLHF produces only functional compliance rather than deep alignment. We offer a mechanistic case study of this phenomenon for partisan political orientation with a comparison of the internal representations of Llama 3.1 8B before and after RLHF. We show that RLHF does not remove the structured partisan direction in the base model. Instead, it compresses the variance of the partisan signal to generate consistently balanced and non-partisan output. Sparse autoencoder decomposition reveals that policy-encoding features, which activate sporadically in the base model, are completely inactive in the Instruct model. Feature-level steering experiments confirm the causal disconnect. RLHF thus encodes a norm of political neutrality, not by erasing the model's knowledge of partisanship, but by severing the causal pathway from partisan geometry to output generation. Importantly, this neutrality is functional, not structural so that the underlying geometry that enables partisan steering remains intact. The mechanisms that bypass RLHF's guardrails, such as inferring and amplifying a user's partisan identity, reactivate partisan generation. If RLHF operates by disconnecting rather than removing value-laden structure, then the same pattern may hold for other value domains, and the aligned model's behavior may be more fragile than its outputs suggest.

摘要：抱負背後的對齊訓練是使大型語言模型安全且有用。主要機制，來自人類反饋的強化學習（RLHF），通過將其與「人類價值觀」對齊來塑造部署語言模型的行為。然而，這個過程是不透明的。哪些價值觀被編碼；它們是誰的價值觀；RLHF是如何編碼它們的？越來越多的證據表明，RLHF僅產生功能上的遵從，而非深層次的對齊。我們提供了一個機械性案例研究，探討這一現象在偏見政治取向中的表現，並比較了Llama 3.1 8B在RLHF之前和之後的內部表示。我們顯示RLHF並未去除基礎模型中的結構性偏見方向。相反，它壓縮了偏見信號的變異性，以生成一致平衡且非偏見的輸出。稀疏自編碼器分解顯示，在基礎模型中偶爾激活的政策編碼特徵，在指令模型中完全不活躍。特徵級的引導實驗確認了因果斷裂。因此，RLHF編碼了一種政治中立的規範，並不是通過抹去模型對黨派的知識，而是通過切斷從黨派幾何到輸出生成的因果途徑。重要的是，這種中立是功能性的，而非結構性的，因此使得能夠進行黨派引導的基礎幾何保持完整。繞過RLHF防護措施的機制，例如推斷和放大用戶的黨派身份，重新激活了黨派生成。如果RLHF是通過斷開而非去除價值負載結構來運作，那麼相同的模式可能適用於其他價值領域，且對齊模型的行為可能比其輸出所暗示的更脆弱。

##### **SearchSwarm: Towards Delegation Intelligence in Agentic LLMs for Long-Horizon Deep Research**
2606.09730v1 by Pu Ning, Quan Chen, Kun Tao, Xinyu Tang, Tianshu Wang, Qianggang Cao, Xinyu Kong, Zujie Wen, Zhiqiang Zhang, Jun Zhou

Large language models are increasingly expected to handle complex, long-horizon real-world tasks whose context demands can grow without bound, yet model context windows remain inherently finite. Recent work explores a paradigm where a main agent decomposes tasks and dispatches subtasks to subagents, which execute and return only summarized results, conserving the main agent's context budget. However, performing this well requires delegation intelligence: the ability to decompose complex tasks, determine when and what to delegate, and integrate returned results into the ongoing workflow. Training data for this capability is scarce in naturally occurring text, and to our knowledge, how to synthesize such data and train models to acquire this capability remains largely unexplored in the open-source community. To bridge this gap, we present a preliminary exploration targeting deep research, a representative long-horizon agent task. Specifically, we design a harness that guides the model toward high-quality task decomposition and delegation, while constraining subagents to return results properly to support the main agent's workflow. The harness-guided trajectories naturally encode correct delegation decisions, which we use as supervised fine-tuning data to internalize delegation intelligence into model weights. Our resulting model, SearchSwarm-30B-A3B, achieves 68.1 on BrowseComp and 73.3 on BrowseComp-ZH, the best results among all models of comparable scale. We will release our harness, model weights, and training data to facilitate future research.

摘要：大型語言模型越來越被期望能處理複雜的、長期的現實世界任務，這些任務的上下文需求可能無限增長，然而模型的上下文窗口本質上仍然是有限的。最近的研究探索了一種範式，其中主要代理分解任務並將子任務派遣給子代理，這些子代理執行並僅返回摘要結果，以節省主要代理的上下文預算。然而，良好地執行這一點需要委派智慧：分解複雜任務的能力、確定何時以及委派什麼的能力，以及將返回結果整合到持續工作流程中的能力。這種能力的訓練數據在自然出現的文本中非常稀缺，據我們所知，如何合成這樣的數據並訓練模型以獲得這種能力在開源社區中仍然基本未被探索。為了填補這一空白，我們提出了一個針對深度研究的初步探索，這是一個具有代表性的長期代理任務。具體而言，我們設計了一個鞍具，指導模型朝著高質量的任務分解和委派發展，同時約束子代理正確返回結果以支持主要代理的工作流程。鞍具引導的軌跡自然編碼了正確的委派決策，我們將其用作監督式微調數據，以將委派智慧內化到模型權重中。我們的最終模型SearchSwarm-30B-A3B在BrowseComp上達到68.1，在BrowseComp-ZH上達到73.3，這是所有可比規模模型中最佳的結果。我們將發布我們的鞍具、模型權重和訓練數據，以促進未來的研究。

##### **Beyond Probabilistic Similarity: Structural, Temporal, and Causal Limitations of Retrieval-Augmented Generation in the Legal Domain**
2606.09724v1 by Hudson de Martim

Retrieval-Augmented Generation (RAG) has become a standard architectural response to unreliability in legal AI, yet high-profile failures, including fabricated citations submitted to courts and anachronistic legal content presented as current, continue to appear across jurisdictions. We argue that these failures are not residual confabulations to be eliminated by scaling language models, but symptoms of an architectural mismatch between probabilistic retrieval and the hierarchical, temporal, and institutional structure of legal knowledge. We develop the argument in three moves. First, we articulate the ontological commitment of legal knowledge as a triad of properties derivable from classical legal theory: hierarchical and mereological structure, diachronic dynamism under operational closure, and causal traceability of institutional provenance grounded in the duty of justification. Second, we identify three corresponding pathologies of retrieval (mereological blindness, diachronic blindness, and causal opacity), each developed with an operational definition, a failure mechanism, a canonical example, and detection criteria for diagnostic use. Third, we review the state of the art through this lens, showing that existing approaches address these requirements unevenly and do not yet compose into a paradigm that treats them as co-constitutive. From this analysis we derive four architectural commitments that characterize the deterministic-by-design direction for legal retrieval: ontological primacy, event reification, bitemporal correctness, and deterministic interaction protocols. The framework concerns quaestio juris (which norms apply and in what state) rather than the downstream tasks that act on identified norms, and addresses legislative and constitutional retrieval primarily, with interpretive time as an explicit extension.

摘要：檢索增強生成（RAG）已成為法律人工智慧中對不可靠性的標準架構回應，然而，包括向法院提交的虛構引用和作為當前呈現的過時法律內容等高調失敗，仍在各個司法管轄區中出現。我們認為這些失敗並非可通過擴展語言模型來消除的殘餘虛構，而是概率檢索與法律知識的分層、時間性和制度結構之間的架構不匹配的症狀。我們將論點分為三個部分來發展。首先，我們闡明法律知識的本體承諾，作為從經典法律理論中推導出的三元性質：分層和部分結構、在運作閉合下的歷時動態，以及基於辯護義務的制度來源的因果可追溯性。其次，我們確定了檢索的三種相應病理（部分盲點、歷時盲點和因果不透明），每種病理都具有操作定義、失敗機制、典型例子和診斷使用的檢測標準。第三，我們通過這個視角回顧了當前的技術狀態，顯示現有方法對這些要求的處理不均勻，尚未組成一個將它們視為共同構成的範式。從這一分析中，我們推導出四個架構承諾，這些承諾特徵化了法律檢索的設計決定論方向：本體優先性、事件具象化、雙時間正確性和決定論互動協議。該框架關注法律問題（哪些規範適用以及在何種狀態下）而非對已識別規範進行操作的下游任務，並主要針對立法和憲法檢索，將解釋時間作為明確的延伸。

##### **Correlation Is Not Enough: Embedding Human Metadata for Individual Causal Discovery**
2606.09672v1 by Suraj Biswas, Saurabh Gupta, Pritam Mukherjee

Ask a pretrained biomedical language model whether "cortisol 28 ug/dL" and "stock-market volatility" are related, and it returns a cosine similarity of 0.83 on a scale where 1.0 means identical. The two share no mechanism. This is not a corner case: every off-the-shelf biomedical encoder we tested (BioBERT, PubMedBERT, BioM-ELECTRA) scores unrelated cross-domain pairs between 0.76 and 0.92 when the answer should be near zero. Accuracy on cross-domain discrimination is 0%.
  Retrieval systems survive this, because a language model downstream filters the noise. A Large Behavioural Model (LBM), a foundation model whose subject is a person rather than a sentence, does not: it reasons over a graph of a user's life and treats embedding proximity as evidence that two events are causally linked. False proximity writes a false causal edge, and everything downstream inherits the error. Here, embedding geometry is not a tuning knob; it is correctness.
  We report the fix. A contrastive pass over 72,034 pairs raises PubMedBERT BIOSSES correlation from 0.633 to 0.828 and within-vs-across-domain separation from 1.05x to 1.63x. A second pass, BODHI, mines hard negatives from edges absent in a biomedical knowledge graph and lifts separation to 2.30x and the discrimination gap to +0.392, at a 4.5% BIOSSES cost. On an Intel Xeon 6737P with AMX, OpenVINO cuts single-query latency from 1367 ms to 10 ms (133x) and reaches 555 sentences/sec. One finding contradicts standard advice: FP16 beats INT8 on this silicon at every serving batch size, and we explain why. The same model on a no-AMX Ice Lake instance runs 13-27x slower. We release the benchmark suite, training corpora, the BODHI generator, and the OpenVINO scripts.

摘要：詢問一個預訓練的生物醫學語言模型「皮質醇 28 ug/dL」和「股市波動性」是否相關，它返回的餘弦相似度為 0.83，該比例的範圍是 1.0 表示完全相同。這兩者之間沒有任何機制。這並不是一個邊緣案例：我們測試的每一個現成的生物醫學編碼器（BioBERT、PubMedBERT、BioM-ELECTRA）在應該接近零的情況下，對於不相關的跨領域對的得分介於 0.76 和 0.92 之間。跨領域辨識的準確率為 0%。  
檢索系統能夠在這種情況下生存，因為下游的語言模型過濾了噪音。一個大型行為模型（LBM），一個以人而非句子為主題的基礎模型，則無法做到：它對用戶生活的圖進行推理，並將嵌入的接近性視為兩個事件因果連結的證據。虛假的接近性寫下虛假的因果邊，所有下游的內容都繼承了這個錯誤。在這裡，嵌入幾何不是調整旋鈕；它是正確性。  
我們報告了修正方案。對 72,034 對的對比通過將 PubMedBERT BIOSSES 的相關性從 0.633 提高到 0.828，並將領域內與跨領域的分離從 1.05 倍提高到 1.63 倍。第二次通過，BODHI，從生物醫學知識圖中缺失的邊緣挖掘難負樣本，並將分離提高到 2.30 倍，辨識差距提高到 +0.392，BIOSSES 成本為 4.5%。在搭載 AMX 的 Intel Xeon 6737P 上，OpenVINO 將單查詢延遲從 1367 毫秒降低到 10 毫秒（133 倍），每秒達到 555 句子。一個發現與標準建議相悖：在這種矽片上，FP16 在每個服務批次大小上都優於 INT8，我們解釋了原因。同一模型在沒有 AMX 的 Ice Lake 實例上運行速度慢 13-27 倍。我們發布了基準套件、訓練語料庫、BODHI 生成器和 OpenVINO 腳本。

##### **Do Video Foundation Models Understand Intuitive Physics? A Layerwise Probing Analysis**
2606.09646v1 by Samuele Punzo, Niccolò Caselli, Ippokratis Pantelidis, Francesco Massafra, Salvatore Lo Sardo, Mohammadreza Salehi

We study whether pretrained video foundation models encode intuitive-physics information in their frozen representations, and how this information varies across model families, layers, and probe types. Using frozen-feature probing on IntPhys2 and Minimal Video Pairs (MVP), we compare predictive joint-embedding models (V-JEPA), masked reconstruction models (VideoMAE), and a diffusion-based video generator (LTX-Video). V-JEPA achieves the strongest overall results across benchmarks, especially with probes that model temporal dynamics, while VideoMAE remains competitive and LTX-Video recovers weaker but non-trivial signal. Layerwise analyses show that physics-relevant information is weakest in early layers and becomes most accessible at intermediate-to-late depth, and temporal controls show that disrupting frame order substantially reduces performance, especially on MVP. Together, these results suggest that intuitive-physics knowledge emerges reliably in pretrained video representations, but its accessibility depends strongly on pretraining paradigm, representational depth, and readout mechanism.

摘要：我們研究預訓練的視訊基礎模型是否在其凍結表示中編碼直觀物理信息，以及這些信息在模型家族、層和探測類型之間的變化。<lb/>通過在 IntPhys2 和 Minimal Video Pairs (MVP) 上進行凍結特徵探測，我們比較了預測聯合嵌入模型 (V-JEPA)、遮蔽重建模型 (VideoMAE) 和基於擴散的視訊生成器 (LTX-Video)。<lb/>V-JEPA 在各基準測試中取得了最強的整體結果，特別是在建模時間動態的探測器上，而 VideoMAE 仍然具有競爭力，LTX-Video 則恢復了較弱但非平凡的信號。<lb/>逐層分析顯示，與物理相關的信息在早期層中最弱，在中到晚期深度時最易於訪問，而時間控制顯示，打亂幀順序會顯著降低性能，特別是在 MVP 上。<lb/>綜合這些結果表明，直觀物理知識在預訓練的視訊表示中可靠地出現，但其可訪問性在很大程度上依賴於預訓練範式、表示深度和讀取機制。

##### **Automated IEP Generation from Traditional Chinese Parent-Teacher Interviews via Corpus-Grounded Feature Diffusion**
2606.09603v1 by Kuanlin Chen, Cheng-En Ou

Writing Individualized Education Programs (IEPs) is a high-labor, knowledge-intensive document burden; English-language research has demonstrated that generative AI can significantly reduce drafting time, yet automated IEP generation in Traditional Chinese remains virtually unexplored due to domain data scarcity, strict privacy regulations, and the absence of local evaluation benchmarks. We propose a low-resource fine-tuning pipeline centered on Corpus-Grounded Feature Diffusion (CGFD): (1) 25 dual-expert high-score seed transcripts are selected via a tau threshold with flag-aware score caps; (2) a FeatureProfile (sentence length, structure, quantification templates) is extracted from seeds and injected into LLM prompts alongside Verbalized-Sampling-style diversity control to drive diffusion; (3) 15 expert gold seeds are used as diffusion anchors, targeting 585 samples; 567 valid diffusion samples are obtained, yielding a 582-sample training set used to fine-tune Breeze-7B with QLoRA; (4) schema-constrained inference via Grammar-Constrained Decoding (GCD) enforces a hierarchical SMART Goal Ladder schema at inference time. Ablation results on a 55-sample schema stress set reveal an unexpected finding: GCD is counterproductive under Traditional Chinese token budgets -- the no-GCD path achieves 100% schema pass rate at 34% lower median latency, outperforming GCD on both reliability and speed. On the n=10 formal hold-out, the no-GCD inference path achieves BERTScore F1 = 0.779, exceeding GPT-5.4 (0.726), DeepSeek-V3.2 (0.703), Gemini-3-Flash-Preview (0.703), and Llama-4-Maverick (0.700) zero-shot baselines while maintaining fully local, air-gapped inference. This system addresses a gap in Traditional Chinese special-education NLP and offers a scalable, privacy-preserving local inference solution under an industrial engineering paradigm.

摘要：撰寫個別化教育計畫（IEPs）是一項高勞動、高知識密集的文檔負擔；英語研究已證明生成式人工智慧可以顯著減少草擬時間，但由於領域數據稀缺、嚴格的隱私法規和缺乏當地評估基準，傳統中文的自動IEP生成幾乎未被探索。我們提出了一個以語料庫為基礎的特徵擴散（CGFD）為中心的低資源微調管道： (1) 通過tau閾值和標記感知的分數上限選擇25個雙專家高分種子轉錄本； (2) 從種子中提取特徵檔案（句子長度、結構、量化模板），並將其注入LLM提示中，與口語化抽樣風格的多樣性控制一起推動擴散； (3) 使用15個專家金種子作為擴散錨點，目標為585個樣本；獲得567個有效擴散樣本，產生582個樣本的訓練集，用於使用QLoRA微調Breeze-7B； (4) 通過語法約束解碼（GCD）進行架構約束推理，在推理時強制執行一個分層的SMART目標梯度架構。對55個樣本架構壓力集的消融結果揭示了一個意外的發現：在傳統中文的標記預算下，GCD是適得其反的——無GCD路徑在34%較低的中位延遲下達到100%的架構通過率，並在可靠性和速度上超越GCD。在n=10的正式保留中，無GCD推理路徑達到BERTScore F1 = 0.779，超過GPT-5.4（0.726）、DeepSeek-V3.2（0.703）、Gemini-3-Flash-Preview（0.703）和Llama-4-Maverick（0.700）的零-shot基準，同時保持完全本地、隔離的推理。該系統填補了傳統中文特殊教育NLP的空白，並在工業工程範式下提供了一個可擴展的、保護隱私的本地推理解決方案。

##### **AI Scientists Are Only as Good as Their Evidence: A Stratified Ablation of Proprietary Data and Reasoning Skills in Drug-Asset Valuation**
2606.09556v1 by Yinan Wang

AI Scientist agents are often evaluated as if capability were mainly a function of model quality, prompting, or reasoning scaffolds. We test a different hypothesis in drug-asset valuation: for knowledge-intensive scientific decisions, the limiting factor is often the evidence substrate the agent can access. We run a controlled three-arm ablation on a production valuation agent: A is a plain web-only LLM analyst, B adds public structured tools plus a 14-dimension valuation playbook, verifier, objectivity policy and red-team, and C adds the proprietary Noah AI corpus of curated pipeline, trial and deal intelligence. Across a 13-asset stratified benchmark, B improves calibration and audit discipline: tier-in-range accuracy rises from 0.80 to 0.89 and objectivity from 3.16 to 3.30. But B does not remove the factual ceiling. Under capability-superset accounting, A and B recover only 0.25 and 0.38 of the curated gold competitive record, while C recovers 0.96; on the curated long-tail subset, C reaches 0.93 vs. 0.26/0.30. Raw blind-panel decision quality is similar for A and B (7.01 vs. 6.96), so we introduce completeness-aware decision utility: informed decision-quality = decision-quality x gold-coverage. On this metric, C reaches 7.43 vs. 1.76/2.57 for A/B. Even a perfect non-proprietary-data report would be capped at 3.83 by B's coverage. The result is not that reasoning scaffolds are unimportant; they improve calibration and discipline. Rather, proprietary evidence sets the upper bound of what the AI Scientist can know and therefore decide.

摘要：AI 科學家代理通常被評估為能力主要是模型質量、提示或推理支架的函數。我們在藥物資產估值中測試一個不同的假設：對於知識密集型的科學決策，限制因素通常是代理可以訪問的證據基礎。我們對一個生產估值代理進行了受控的三臂消融實驗：A 是一個純網絡的 LLM 分析師，B 增加了公共結構化工具以及一個 14 維的估值手冊、驗證者、客觀性政策和紅隊，而 C 則增加了專有的 Noah AI 精選管道、試驗和交易智能。 在一個 13 資產的分層基準中，B 改善了校準和審計紀律：範疇內準確度從 0.80 上升到 0.89，客觀性從 3.16 上升到 3.30。但 B 並未消除事實上限。在能力超集會計下，A 和 B 只恢復了 0.25 和 0.38 的精選黃金競爭記錄，而 C 恢復了 0.96；在精選的長尾子集上，C 達到 0.93，對比 A/B 的 0.26/0.30。A 和 B 的原始盲評決策質量相似（7.01 對 6.96），因此我們引入了考慮完整性的決策效用：知情決策質量 = 決策質量 x 黃金覆蓋率。在這一指標上，C 達到 7.43，而 A/B 則為 1.76/2.57。即使是一份完美的非專有數據報告，也會因為 B 的覆蓋率而被限制在 3.83。結果並不是推理支架不重要；它們改善了校準和紀律。相反，專有證據設置了 AI 科學家所能知道的上限，因此也決定了他們的決策。

##### **Detecting Differences Is Not Understanding Structure: Large Language Models Fail at Graph Isomorphism**
2606.09484v1 by Kumar Thushalika, Sukumar Kishanthan, Asela Hevapathige

Large language models (LLMs) have shown impressive performance on diverse reasoning tasks, yet their capacity for structural reasoning in graphs remains unclear. We investigate whether LLMs can genuinely understand graph isomorphism -a fundamental problem in graph theory. While LLMs achieve near-perfect accuracy on isomorphism detection, we show this performance is illusory. When identical graphs are presented with permuted node labels, LLMs fail to identify their isomorphism. This finding suggests that LLMs exploit patterns rather than reasoning about abstract graph structure. Since permutation invariance is a fundamental requirement for valid structural reasoning, these results indicate that success on graph reasoning benchmarks should not be interpreted as evidence of genuine topological understanding.

摘要：大型語言模型（LLMs）在各種推理任務上表現出色，但它們在圖形中的結構推理能力仍不明朗。<lb/>我們調查 LLMs 是否能真正理解圖同構——圖論中的一個基本問題。<lb/>雖然 LLMs 在同構檢測上達到了近乎完美的準確性，但我們顯示這種表現是虛幻的。<lb/>當相同的圖形以置換的節點標籤呈現時，LLMs 無法識別它們的同構性。<lb/>這一發現表明，LLMs 利用模式而不是推理抽象的圖結構。<lb/>由於置換不變性是有效結構推理的基本要求，這些結果表明，在圖推理基準上的成功不應被解釋為真正的拓撲理解的證據。

##### **DECSELFMASK: Leveraging Unlabeled Text via Self-Relevance-Guided Masking for Decoder-Only Classification**
2606.09466v2 by Pietro Ferrazzi, Matteo Merler, Giovanni Bonetta, Alberto Lavelli, Bernardo Magnini

Classification tasks require annotated data, which can often be expensive, time-consuming, or even unfeasible to collect. This is the case of the medical domain, where large datasets often have few annotated examples. To address this, we propose DecSelfMask (Decoder Self-learning by Masking), an approach to enhance decoder-only performance on classification tasks. We build on common self-learning approaches by leveraging a model to create training examples from unlabeled data to propose a novel relevance-guided masking strategy. We use relevance attribution methods to determine what portions of unannotated texts are relevant for a task. We then create self-supervised training examples by masking out those portions, training the model to reconstruct them via next-token-prediction. We hypothesize that those examples convey knowledge about the structure and semantics of unannotated data that can be useful for downstream performance. We test our approach on 136 tasks from a collection of 1.9M clinical notes from an Italian hospital. We quantify DecSelfMask's impact on downstream tasks on 5 models of different scales and families, including a probing analysis. Experiments show consistent gains, outperforming standard supervised fine-tuning approaches (+19.9 points in Macro F1), synthetic label generation (+12.5), and continual pretraining (+6.3), as well as common baselines.

摘要：分類任務需要標註數據，而這通常是昂貴的、耗時的，甚至是不可行的收集。這在醫療領域尤為如此，因為大型數據集通常只有少量的標註示例。為了解決這個問題，我們提出了DecSelfMask（解碼器自學習通過遮罩），這是一種增強僅解碼器在分類任務上性能的方法。我們基於常見的自學習方法，利用模型從未標註數據中創建訓練示例，提出了一種新穎的相關性引導遮罩策略。我們使用相關性歸因方法來確定未標註文本中哪些部分與任務相關。然後，我們通過遮罩這些部分來創建自監督訓練示例，訓練模型通過下一個標記預測來重建它們。我們假設這些示例傳達了有關未標註數據的結構和語義的知識，這對下游性能是有用的。我們在來自意大利醫院的190萬臨床筆記的136個任務上測試我們的方法。我們量化了DecSelfMask對5個不同規模和類別模型下游任務的影響，包括探測分析。實驗顯示出一致的增益，超越了標準的監督微調方法（+19.9點的宏F1）、合成標籤生成（+12.5）和持續預訓練（+6.3），以及常見基準。

##### **LargeMonitor: Monitoring Online Task-Free Continual Learning via Large Pretrained Models**
2606.09430v1 by Mingqi Yuan, Xiaoquan Sun, Shihao Luo, Jiayu Chen

Online task-free continual learning (TFCL) requires intelligent agents to sequentially accumulate knowledge from an unbounded, non-stationary data stream under strict single-pass constraints and without any explicit task identifiers. Existing online TFCL paradigms primarily rely on parameter-efficient prompt tuning or dynamic structure expansion driven by training-coupled optimization dynamics, such as empirical loss fluctuations or evolving latent distances. As a result, these training-coupled solvers remain agnostic to the structural origins of distribution drift, mechanically enforcing a fixed strategy across fundamentally distinct streaming variations. To address this gap, we propose LargeMonitor, a framework that leverages large pretrained foundation models to autonomously orchestrate task-free continuous adaptation. Specifically, LargeMonitor introduces a decoupled detection module utilizing the frozen, stable representation space of large vision models (LVMs) to achieve robust, zero-shot drift detection without training-dependent interference or brittle threshold tuning. Upon a confirmed drift, the framework activates a context-aware diagnostic module driven by large multimodal models (LMMs) to interpret the precise semantic etiologies of the stream variation (e.g., novel class emergence vs. environmental domain shift). This dual-stage capability empowers the continuous learner to dynamically deploy adaptive and shift-specific optimization strategies. Extensive experiments across multiple TFCL settings and benchmarks demonstrate that LargeMonitor achieves precise, robust detection and diagnosis of complex data streams while consistently improving the performance of existing online TFCL algorithms.

摘要：線上無任務持續學習（TFCL）要求智能代理在嚴格的單次通過約束下，從無界限的非穩定數據流中順序積累知識，而不需要任何明確的任務標識符。現有的線上TFCL範式主要依賴於參數高效的提示調整或由訓練相關的優化動態驅動的動態結構擴展，例如經驗損失波動或演變的潛在距離。因此，這些訓練相關的解決方案對於分佈漂移的結構來源保持無知，機械地在根本不同的流式變化中強制執行固定策略。為了解決這一問題，我們提出了LargeMonitor，一個利用大型預訓練基礎模型自主協調無任務持續適應的框架。具體而言，LargeMonitor引入了一個解耦檢測模塊，利用大型視覺模型（LVMs）的凍結、穩定表示空間來實現穩健的零-shot漂移檢測，無需依賴訓練的干擾或脆弱的閾值調整。在確認漂移後，該框架啟動一個由大型多模態模型（LMMs）驅動的上下文感知診斷模塊，以解釋流變化的精確語義病因（例如，新的類別出現與環境領域轉移）。這種雙階段能力使持續學習者能夠動態部署自適應和特定於變化的優化策略。在多個TFCL設置和基準測試中的廣泛實驗表明，LargeMonitor能夠精確、穩健地檢測和診斷複雜數據流，同時持續改善現有線上TFCL算法的性能。

