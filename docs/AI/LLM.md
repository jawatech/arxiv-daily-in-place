
### LLM
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-06-18**|**How Transparent is DiffusionGemma?**|Joshua Engels et.al.|[2606.20560v1](http://arxiv.org/abs/2606.20560v1)|null|
|**2026-06-18**|**Structuring and Tokenizing Distributed User Interest Context for Generative Recommendation**|Ruizhong Qiu et.al.|[2606.20554v1](http://arxiv.org/abs/2606.20554v1)|null|
|**2026-06-18**|**Toward Calibrated Mixture-of-Experts Under Distribution Shift**|Gina Wong et.al.|[2606.20544v1](http://arxiv.org/abs/2606.20544v1)|null|
|**2026-06-18**|**How Do Instructions Shape Speech? Cross-Attention Attribution for Style-Captioned Text-to-Speech**|Nityanand Mathur et.al.|[2606.20532v1](http://arxiv.org/abs/2606.20532v1)|null|
|**2026-06-18**|**LedgerAgent: Structured State for Policy-Adherent Tool-Calling Agents**|Md Nayem Uddin et.al.|[2606.20529v1](http://arxiv.org/abs/2606.20529v1)|null|
|**2026-06-18**|**StylisticBias: A Few Human Visual Cues Drive Most Social Biases in MLLMs**|Shaghayegh Kolli et.al.|[2606.20527v1](http://arxiv.org/abs/2606.20527v1)|null|
|**2026-06-18**|**DeepSWIP: Quotient-WMC Counterfactuals for Neural Probabilistic Logic Programs**|Saimun Habib et.al.|[2606.20526v1](http://arxiv.org/abs/2606.20526v1)|null|
|**2026-06-18**|**SARLO-80: Worldwide Slant SAR Language Optic Dataset 80cm**|Solène Debuysère et.al.|[2606.20523v1](http://arxiv.org/abs/2606.20523v1)|null|
|**2026-06-18**|**Sovereign Execution Brokers: Enforcing Certificate-Bound Authority in Agentic Control Planes**|Jun He et.al.|[2606.20520v1](http://arxiv.org/abs/2606.20520v1)|null|
|**2026-06-18**|**FlowEdit: Associative Memory for Lifelong Pronunciation Adaptation in Flow-Matching TTS**|Harshit Singh et.al.|[2606.20518v1](http://arxiv.org/abs/2606.20518v1)|null|
|**2026-06-18**|**Multi-LCB: Extending LiveCodeBench to Multiple Programming Languages**|Maria Ivanova et.al.|[2606.20517v1](http://arxiv.org/abs/2606.20517v1)|null|
|**2026-06-18**|**Efficient and Sound Probabilistic Verification for AI Agents**|Alaia Solko-Breslin et.al.|[2606.20510v1](http://arxiv.org/abs/2606.20510v1)|null|
|**2026-06-18**|**What Do Safety-Aligned LLMs Learn From Mixed Compliance Demonstrations?**|Sihui Dai et.al.|[2606.20508v1](http://arxiv.org/abs/2606.20508v1)|null|
|**2026-06-18**|**FreeStyle: Free Control of Style-Content Dual-Reference Generation from Community LoRA Mining**|Jinghong Lan et.al.|[2606.20506v1](http://arxiv.org/abs/2606.20506v1)|null|
|**2026-06-18**|**Calibration Without Comprehension: Diagnosing the Limits of Fine-Tuning LLMs for Vulnerability Detection in Systems Software**|Arastoo Zibaeirad et.al.|[2606.20502v1](http://arxiv.org/abs/2606.20502v1)|null|
|**2026-06-18**|**Contagion Networks: Evaluator Bias Propagation in Multi-Agent LLM Systems**|Zewen Liu et.al.|[2606.20493v1](http://arxiv.org/abs/2606.20493v1)|null|
|**2026-06-18**|**Beyond Global Replanning: Hierarchical Recovery for Cross-Device Agent Systems**|Shu Yao et.al.|[2606.20487v1](http://arxiv.org/abs/2606.20487v1)|null|
|**2026-06-18**|**Optimal Order of Multi-Agent and General Many-Body Systems**|Jake J. Xia et.al.|[2606.20485v1](http://arxiv.org/abs/2606.20485v1)|null|
|**2026-06-18**|**Your Mouse and Eyes Secretly Leak Your Preference: LLM Alignment using Implicit Feedback from Users**|Haw-Shiuan Chang et.al.|[2606.20482v1](http://arxiv.org/abs/2606.20482v1)|null|
|**2026-06-18**|**Scalable Training of Spatially Grounded 2D Vision-Language Models for Radiology**|Yusuf Salcan et.al.|[2606.20477v1](http://arxiv.org/abs/2606.20477v1)|null|
|**2026-06-18**|**Analyzing Defensive Misdirection Against Model-Guided Automated Attacks on Agentic AI Systems**|Reza Soosahabi et.al.|[2606.20470v1](http://arxiv.org/abs/2606.20470v1)|null|
|**2026-06-18**|**Context-Aware Hierarchical Bayesian Modeling of IVF Laboratory Environmental Conditions**|Zahra Asghari Varzaneh et.al.|[2606.20459v1](http://arxiv.org/abs/2606.20459v1)|null|
|**2026-06-18**|**Repurposing a Speech Classifier for Guided Diffusion-Based Speech Generation**|Rostislav Makarov et.al.|[2606.20457v1](http://arxiv.org/abs/2606.20457v1)|null|
|**2026-06-18**|**Interpretable Sperm Morphology Classification via Attention-Guided Deep Learning**|Zahra Asghari Varzaneh et.al.|[2606.20438v1](http://arxiv.org/abs/2606.20438v1)|null|
|**2026-06-18**|**Multi-View Decompilation for LLM-Based Malware Classification**|Bercan Turkmen et.al.|[2606.20436v1](http://arxiv.org/abs/2606.20436v1)|null|
|**2026-06-18**|**LLM agent safety, multi-turn red-teaming, jailbreak benchmarks, adversarial robustness, safety-critical systems**|Hanwool Lee et.al.|[2606.20408v1](http://arxiv.org/abs/2606.20408v1)|null|
|**2026-06-18**|**DataMagic: Transforming Tabular Data into Data Insight Video**|Yupeng Xie et.al.|[2606.20388v1](http://arxiv.org/abs/2606.20388v1)|null|
|**2026-06-18**|**Rethinking Shrinkage Bias in LLM FP4 Pretraining: Geometric Origin, Systemic Impact, and UFP4 Recipe**|Qian Zhao et.al.|[2606.20381v1](http://arxiv.org/abs/2606.20381v1)|null|
|**2026-06-18**|**AutoPass: Evidence-Guided LLM Agents for Compiler Performance Tuning**|Zepeng Li et.al.|[2606.20373v1](http://arxiv.org/abs/2606.20373v1)|null|
|**2026-06-18**|**CATCH-ME if you RAG: a dataset of Contextually Annotated multi-Turn Counterspeech against Hate and Misinformation Exchanges**|Helena Bonaldi et.al.|[2606.20369v1](http://arxiv.org/abs/2606.20369v1)|null|
|**2026-06-18**|**Automating SKILL.md Generation for Computer-Using Agents via Interaction Trajectory Mining**|Yuexing Hao et.al.|[2606.20363v1](http://arxiv.org/abs/2606.20363v1)|null|
|**2026-06-18**|**Robust $Q$-learning for mean-field control under Wasserstein uncertainty in common noise**|Mathieu Laurière et.al.|[2606.20356v1](http://arxiv.org/abs/2606.20356v1)|null|
|**2026-06-18**|**SoftSkill: Behavioral Compression for Contextual Adaptation**|Xijia Tao et.al.|[2606.20333v1](http://arxiv.org/abs/2606.20333v1)|null|
|**2026-06-18**|**Token-Operations-Oriented Inference Optimization Techniques for Large Models**|Shiguo Lian et.al.|[2606.20295v1](http://arxiv.org/abs/2606.20295v1)|null|
|**2026-06-18**|**PsyScore: A Psychometrically-Aware Framework for Trait-Adaptive Essay Scoring and ZPD-Scaffolded Feedback**|Wei Xia et.al.|[2606.20287v1](http://arxiv.org/abs/2606.20287v1)|null|
|**2026-06-18**|**Boundary Embedding Shaping with Adaptive Contrastive Learning for Graph Structural Disentanglement**|Jiaqing Chen et.al.|[2606.20283v1](http://arxiv.org/abs/2606.20283v1)|null|
|**2026-06-18**|**ELVA: Exploring Ranking-Driven Universal Multimodal Retrieval**|Yuhan Liu et.al.|[2606.20280v1](http://arxiv.org/abs/2606.20280v1)|null|
|**2026-06-18**|**Lagrange: An Open-Vocabulary, Energy-Based Sparse Framework for Generalized End-to-End Driving**|Shihao Ji et.al.|[2606.20274v1](http://arxiv.org/abs/2606.20274v1)|null|
|**2026-06-18**|**Confidence-Aware Automated Assessment of Student-Drawn Scientific Models**|Luyang Fang et.al.|[2606.20264v1](http://arxiv.org/abs/2606.20264v1)|null|
|**2026-06-18**|**Editorial Alignment: A Participatory Approach to Engaging Editorial Expertise in LLM-mediated Knowledge Dissemination**|Simon Aagaard Enni et.al.|[2606.20258v1](http://arxiv.org/abs/2606.20258v1)|null|
|**2026-06-18**|**The Register Gap: A Meaning Intelligence Framework for Nigerian Public Discourse**|Celestine Achi et.al.|[2606.20255v1](http://arxiv.org/abs/2606.20255v1)|null|
|**2026-06-18**|**Finetuning Vision-Language-Action Models Requires Fewer Layers Than You Think**|Gia-Binh Nguyen et.al.|[2606.20246v1](http://arxiv.org/abs/2606.20246v1)|null|
|**2026-06-18**|**Navigating Unreliable Parametric and Contextual Knowledge: Explicit Knowledge Conflict Resolution for LLM Inference**|Huang Peng et.al.|[2606.20245v1](http://arxiv.org/abs/2606.20245v1)|null|
|**2026-06-18**|**SPOT-E: Test-Time Entropy Shaping with Visual Spotlights for Frozen VLMs**|Bo Yin et.al.|[2606.20244v1](http://arxiv.org/abs/2606.20244v1)|null|
|**2026-06-18**|**ScholarQuest: A Taxonomy-Guided Benchmark for Agentic Academic Paper Search in Open Literature Environments**|Tingyue Pan et.al.|[2606.20235v1](http://arxiv.org/abs/2606.20235v1)|null|
|**2026-06-18**|**Thermodynamic Measure of Intelligence**|Ishanu Chattopadhyay et.al.|[2606.20231v1](http://arxiv.org/abs/2606.20231v1)|null|
|**2026-06-18**|**QMFOL: Benchmarking Large Language Model Reasoning via Quantifiable Monadic First-Order Logic Test Case Generation**|Xinyi Zheng et.al.|[2606.20227v1](http://arxiv.org/abs/2606.20227v1)|null|
|**2026-06-18**|**Actionable Activation Directions for Detecting and Mitigating Emergent Misalignment Across Language Model Families**|Abdul Rafay Syed et.al.|[2606.20225v1](http://arxiv.org/abs/2606.20225v1)|null|
|**2026-06-18**|**CzechDocs: A Multiway Parallel Dataset of Formatted Documents for Minority Languages in Czechia**|Josef Jon et.al.|[2606.20212v1](http://arxiv.org/abs/2606.20212v1)|null|
|**2026-06-18**|**Augmenting Game AI with Deep Reinforcement Learning**|Alessandro Sestini et.al.|[2606.20210v1](http://arxiv.org/abs/2606.20210v1)|null|
|**2026-06-18**|**FlowMaps: Modeling Long-Term Multimodal Object Dynamics with Flow Matching**|Francesco Argenziano et.al.|[2606.20209v1](http://arxiv.org/abs/2606.20209v1)|null|
|**2026-06-18**|**Beyond Accuracy: Measuring Logical Compliance of Predictive Models**|Guillaume Olivier Delplanque et.al.|[2606.20208v1](http://arxiv.org/abs/2606.20208v1)|null|
|**2026-06-18**|**Apparent Psychological Profiles of Large Language Models are Largely a Measurement Artifact**|Jelena Meyer et.al.|[2606.20205v1](http://arxiv.org/abs/2606.20205v1)|null|
|**2026-06-18**|**HilDA: Hierarchical Distillation with Diffusion for Advancing Self-Supervised LiDAR Pre-trainin**|Maciej Wozniak et.al.|[2606.20189v1](http://arxiv.org/abs/2606.20189v1)|null|
|**2026-06-18**|**ReNikud: Audio-Supervised Hebrew Grapheme-to-Phoneme Conversion**|Maxim Melichov et.al.|[2606.20179v1](http://arxiv.org/abs/2606.20179v1)|null|
|**2026-06-18**|**Evaluating and Enhancing Negation Comprehension in Remote Sensing MLLMs**|Haochen Han et.al.|[2606.20177v1](http://arxiv.org/abs/2606.20177v1)|null|
|**2026-06-18**|**MedRLM: Recursive Multimodal Health Intelligence for Long-Context Clinical Reasoning, Sensor-Guided Screening, Evidence-Grounded Decision Support, and Community-to-Tertiary Referral Optimization**|Aueaphum Aueawatthanaphisut et.al.|[2606.20164v1](http://arxiv.org/abs/2606.20164v1)|null|
|**2026-06-18**|**Modularity-Free Conflict-Averse Training for Generalized PINNs**|Heejo Kong et.al.|[2606.20156v1](http://arxiv.org/abs/2606.20156v1)|null|
|**2026-06-18**|**NAMESAKES: Probing Identity Memorization in Text-to-Image Models**|Morris Alper et.al.|[2606.20155v1](http://arxiv.org/abs/2606.20155v1)|null|
|**2026-06-18**|**From Texts to Scores: Tracing the Emergence of Essay Quality Representations in Large Language Models**|Jiaxu Zuo et.al.|[2606.20152v1](http://arxiv.org/abs/2606.20152v1)|null|
|**2026-06-18**|**BIM-Edit: Benchmarking Large Language Models for IFC-Based Building Information Modeling**|Bharathi Kannan Nithyanantham et.al.|[2606.20146v1](http://arxiv.org/abs/2606.20146v1)|null|
|**2026-06-18**|**Learning to Prompt: Improving Student Engagement with Adaptive LLM-based High-School Tutoring**|Po-Chin Chang et.al.|[2606.20138v1](http://arxiv.org/abs/2606.20138v1)|null|
|**2026-06-18**|**PASQA: Pitch-Accent-Focused Speech Quality Assessment Model Trained on Synthetic Speech with Accent Errors**|Masaya Kawamura et.al.|[2606.20137v1](http://arxiv.org/abs/2606.20137v1)|null|
|**2026-06-18**|**Frequency-Aware Flow Matching for Continuous and Consistent Robotic Action Generation**|Jianing Guo et.al.|[2606.20135v1](http://arxiv.org/abs/2606.20135v1)|null|
|**2026-06-18**|**ScaffoldAgent: Utility-Guided Dynamic Outline Optimization for Open-Ended Deep Research**|Zhibang Yang et.al.|[2606.20122v1](http://arxiv.org/abs/2606.20122v1)|null|
|**2026-06-18**|**Dual-Agent Framework for Cross-Model Verified Translation of Natural-Language Protocols into Robotic Laboratory Platform**|Hyeonna Choi et.al.|[2606.20120v1](http://arxiv.org/abs/2606.20120v1)|null|
|**2026-06-18**|**When Does Streaming Tool Use Help? Characterizing Tool-Intent Stabilization in Streaming Retrieval-Augmented Generation**|Elroy Galbraith et.al.|[2606.20113v1](http://arxiv.org/abs/2606.20113v1)|null|
|**2026-06-18**|**Sensorimotor World Models: Perception for Action via Inverse Dynamics**|Petr Ivashkov et.al.|[2606.20104v1](http://arxiv.org/abs/2606.20104v1)|null|
|**2026-06-18**|**Hybrid Diffusion Transformer for Instruction-Guided Audio Editing via Rectified Flow**|Liting Gao et.al.|[2606.20101v1](http://arxiv.org/abs/2606.20101v1)|null|
|**2026-06-18**|**HydraHead: From Head-Level Functional Heterogeneity to Specialized Attention Hybridization**|Zhentao Tan et.al.|[2606.20097v1](http://arxiv.org/abs/2606.20097v1)|null|
|**2026-06-18**|**MakeupMirror: Improving Facial Attribute Preservation in Diffusion Models for Makeup Transfer**|Nefeli Andreou et.al.|[2606.20094v1](http://arxiv.org/abs/2606.20094v1)|null|
|**2026-06-18**|**Self-Preference Is Weak or Absent in Verifiable Instruction-Following Revision: A Four-Model Test Under Genuine Authorship**|William Guey et.al.|[2606.20093v1](http://arxiv.org/abs/2606.20093v1)|null|
|**2026-06-18**|**IHUBERT: Vector-Based Semantic Deduplication and Domain-Balanced Pretraining for Persian Resources**|Arash Ghafouri et.al.|[2606.20089v1](http://arxiv.org/abs/2606.20089v1)|null|
|**2026-06-18**|**Residual-Space Evolutionary Optimization via Flow-based Generative Models**|Zhuo Cao et.al.|[2606.20084v1](http://arxiv.org/abs/2606.20084v1)|null|
|**2026-06-18**|**The Hidden Evolution of Disguised Visual Context inside the VLM**|Wish Suharitdamrong et.al.|[2606.20077v1](http://arxiv.org/abs/2606.20077v1)|null|
|**2026-06-18**|**Variable-Length Tokenization via Learnable Global Merging for Diffusion Transformers**|Dong Hoon Lee et.al.|[2606.20076v1](http://arxiv.org/abs/2606.20076v1)|null|
|**2026-06-18**|**Evaluation of EEG Foundation Models for Event-Based Burst-Suppression Detection in ICU**|Elisa Vasta et.al.|[2606.20074v1](http://arxiv.org/abs/2606.20074v1)|null|
|**2026-06-18**|**Source-Grounded Data Generation for Text-to-JSON Learning**|Sunghee Ahn et.al.|[2606.20072v1](http://arxiv.org/abs/2606.20072v1)|null|
|**2026-06-18**|**Process-Verified Reinforcement Learning for Theorem Proving via Lean**|Minsu Kim et.al.|[2606.20068v1](http://arxiv.org/abs/2606.20068v1)|null|
|**2026-06-18**|**Generative Engine Optimization at Scale: Measuring Brand Visibility Across AI Search Engines**|Pratyush Kumar et.al.|[2606.20065v1](http://arxiv.org/abs/2606.20065v1)|null|
|**2026-06-18**|**See-and-Reach: Precise Vision-Language Navigation for UAVs within the Field of View**|Fanfu Xue et.al.|[2606.20045v1](http://arxiv.org/abs/2606.20045v1)|null|
|**2026-06-18**|**AI Economist Agent: An Agentic Framework for Model-Grounded Economic Analysis with RAG, Knowledge Graphs, and Large Language Models**|Masahiro Kato et.al.|[2606.20041v1](http://arxiv.org/abs/2606.20041v1)|null|
|**2026-06-18**|**When Lower Privileges Suffice: Investigating Over-Privileged Tool Selection in LLM Agents**|Kaiyue Yang et.al.|[2606.20023v1](http://arxiv.org/abs/2606.20023v1)|null|
|**2026-06-18**|**Hierarchical Control in Multi-Agent Games: LLM-based Planning and RL Execution**|Jannik Hösch et.al.|[2606.20014v1](http://arxiv.org/abs/2606.20014v1)|null|
|**2026-06-18**|**StreamKL: Fast and Memory-Efficient KL Divergence for Boosting Attention Distillation**|Guangda Liu et.al.|[2606.20005v1](http://arxiv.org/abs/2606.20005v1)|null|
|**2026-06-18**|**Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning**|Yanxi Chen et.al.|[2606.20002v1](http://arxiv.org/abs/2606.20002v1)|null|
|**2026-06-18**|**Tri-Info: Generalizable, Interpretable Failure Prediction for VLA Models via Information Theory**|Jinghan Yang et.al.|[2606.19998v1](http://arxiv.org/abs/2606.19998v1)|null|
|**2026-06-18**|**Beyond Static Endpoints: Tool Programs as an Interface for Flexible Agentic Web Services**|Mugeng Liu et.al.|[2606.19992v1](http://arxiv.org/abs/2606.19992v1)|null|
|**2026-06-18**|**Reward as An Agent for Embodied World Models**|Pu Li et.al.|[2606.19990v1](http://arxiv.org/abs/2606.19990v1)|null|
|**2026-06-18**|**The Algorithmic-Human Manager: AI, Apps, and Workers in the Indian Gig Economy**|Omir Kumar et.al.|[2606.19975v1](http://arxiv.org/abs/2606.19975v1)|null|
|**2026-06-18**|**ROSE: Benchmarking the Perception-to-Action Gap in Multimodal Models**|Yihao Wang et.al.|[2606.19965v1](http://arxiv.org/abs/2606.19965v1)|null|
|**2026-06-18**|**Investigating Human-Model Discrepancies in Speech Quality Assessment via Acoustic and Prosodic Perturbations**|Masato Takagi et.al.|[2606.19951v1](http://arxiv.org/abs/2606.19951v1)|null|
|**2026-06-18**|**Confidence Calibration for Multimodal LLMs: An Empirical Study through Medical VQA**|Yuetian Du et.al.|[2606.19950v1](http://arxiv.org/abs/2606.19950v1)|null|
|**2026-06-18**|**Advancing DialNav through Automatic Embodied Dialog Augmentation**|Leekyeung Han et.al.|[2606.19948v1](http://arxiv.org/abs/2606.19948v1)|null|
|**2026-06-18**|**GEMS: Geometric Constraints Enable Multi-Semantic Superposition in LLMs**|Yu Deng et.al.|[2606.19946v1](http://arxiv.org/abs/2606.19946v1)|null|
|**2026-06-18**|**SIMBA: ABidirectional Retrieval Forward Simulation Framework for Modeling FY-4A GIIRS Hyperspectral Infrared Radiances Toward NWP Applications**|Jingdong Shen et.al.|[2606.19943v1](http://arxiv.org/abs/2606.19943v1)|null|
|**2026-06-18**|**Triangular Consistency as a Universal Constraint for Learning Optical Flow**|Yi Xiao et.al.|[2606.19938v1](http://arxiv.org/abs/2606.19938v1)|null|
|**2026-06-18**|**Speeding up the annotation process in semantic segmentation industrial applications**|Marta Fernandez-Moreno et.al.|[2606.19934v1](http://arxiv.org/abs/2606.19934v1)|null|
|**2026-06-18**|**Spatial-Aware Reduction Framework: Towards Efficient and Faithful Visual State Space Models**|Jindi Lv et.al.|[2606.19932v1](http://arxiv.org/abs/2606.19932v1)|null|
|**2026-06-18**|**The Tao of Agency: Autotelic AI, Embedded Agency and Dissolution of the Self**|Aritra Sarkar et.al.|[2606.19924v1](http://arxiv.org/abs/2606.19924v1)|null|

#### Abstracts
##### **How Transparent is DiffusionGemma?**
2606.20560v1 by Joshua Engels, Callum McDougall, Bilal Chughtai, Janos Kramar, Senthoran Rajamanoharan, Cindy Wu, Arthur Conmy, Asic Q Chen, Jean Tarbouriech, Min Ma, Brendan O'Donoghue, João Gabriel Lopes de Oliveira, Rohin Shah, Neel Nanda

LLM reasoning transparency is a critical affordance for understanding model decisions, mitigating misuse and misalignment, and debugging surprising model behaviors. However, DiffusionGemma performs a larger fraction of its computation in a continuous latent space; does this make its reasoning less transparent? We study this question by decomposing transparency into two components: variable transparency, whether we understand intermediate snapshots of a model's computational state; and algorithmic transparency, whether we can use these snapshots to reconstruct the process by which the model arrived at its outputs. Naively, DiffusionGemma has poor variable transparency: its opaque serial depth, the amount of serial computation that occurs in between interpretable model states, seems at first 28.6X higher than the corresponding autoregressive Gemma 4 model. However, we show that we can map the information flowing between denoising steps through an interpretable token bottleneck with no decrease in downstream performance. Treating these intermediate states as interpretable reduces the opaque serial depth to just 1.1X that of Gemma 4. Algorithmic transparency is harder for diffusion models than for autoregressive models because all token predictions in the canvas can change at every denoising step, giving the model the power to implement complicated distributed algorithms during the denoising process. To begin bridging this gap, we conduct a suite of interpretability case studies, uncovering initial evidence of novel diffusion-specific phenomena such as non-chronological reasoning, token and sequence smearing, and intermediate-context reasoning. Finally, we test monitorability, a key application of transparency that measures whether model outputs are useful for downstream tasks. We find that DiffusionGemma is similarly monitorable to Gemma 4.

摘要：LLM 推理透明度是理解模型決策、減輕誤用和不對齊以及調試驚人模型行為的一個關鍵條件。<lb/>然而，DiffusionGemma 在連續潛在空間中執行了更大比例的計算；這是否使其推理變得不那麼透明？<lb/>我們通過將透明度分解為兩個組成部分來研究這個問題：變量透明度，即我們是否理解模型計算狀態的中間快照；以及算法透明度，即我們是否可以利用這些快照重建模型達到其輸出的過程。<lb/>從表面上看，DiffusionGemma 的變量透明度較差：其不透明的串行深度，即在可解釋模型狀態之間發生的串行計算量，似乎最初比對應的自回歸 Gemma 4 模型高出 28.6 倍。<lb/>然而，我們展示了可以通過可解釋的標記瓶頸來映射在去噪步驟之間流動的信息，而不會降低下游性能。<lb/>將這些中間狀態視為可解釋的，將不透明的串行深度降低到僅為 Gemma 4 的 1.1 倍。<lb/>對於擴散模型來說，算法透明度比自回歸模型更難，因為畫布中的所有標記預測在每個去噪步驟中都可以改變，這使得模型在去噪過程中能夠實施複雜的分佈式算法。<lb/>為了開始彌合這一差距，我們進行了一系列可解釋性案例研究，揭示了新穎的擴散特定現象的初步證據，例如非時間順序推理、標記和序列模糊以及中間上下文推理。<lb/>最後，我們測試了可監控性，這是透明度的一個關鍵應用，衡量模型輸出是否對下游任務有用。<lb/>我們發現 DiffusionGemma 的可監控性與 Gemma 4 相似。

##### **Structuring and Tokenizing Distributed User Interest Context for Generative Recommendation**
2606.20554v1 by Ruizhong Qiu, Yinglong Xia, Dongqi Fu, Hanqing Zeng, Ren Chen, Xiangjun Fan, Hong Li, Hong Yan, Hanghang Tong

Generative recommendation is an emerging paradigm that has shown promise in industrial recommendation systems, aiming to predict users' next interactions from their historical behaviors. At the core of generative recommendation lies item tokenization, which bridges item semantics and recommendation models. However, existing methods often struggle to effectively organize and inject complex user-behavioral and item-semantic contexts into recommendation models simultaneously. On the one hand, existing graph-based integration methods, such as graph serialization and graph neural networks, either suffer from scalability issues or exploit only local graph information. On the other hand, existing semantic tokenization methods typically rely on heuristics and lack explicit supervision signals, which may lead to inaccurate or suboptimal semantic representations. To address these limitations in user interest context modeling, we propose G2Rec, a scalable framework that unifies holistic graph-based user co-engagement modeling with semantic tokenization for industrial-scale generative recommendation. Overall, G2Rec enables recommendation models to capture holistic and semantically grounded user interest prototypes without requiring ground-truth user interests, thereby providing more comprehensive and accurate modeling of user behavior contexts in industrial sequential recommendation. Online deployment across product surfaces and extensive experiments on public datasets demonstrate the superiority of G2Rec over existing methods.

摘要：生成推薦是一種新興的範式，已在工業推薦系統中顯示出潛力，旨在根據用戶的歷史行為預測其下一次互動。生成推薦的核心在於項目標記化，它橋接了項目語義和推薦模型。然而，現有的方法往往難以有效地組織和注入複雜的用戶行為和項目語義上下文到推薦模型中。 一方面，現有的基於圖的整合方法，如圖序列化和圖神經網絡，要麼面臨可擴展性問題，要麼僅利用局部圖信息。另一方面，現有的語義標記化方法通常依賴於啟發式，並缺乏明確的監督信號，這可能導致不準確或次優的語義表示。為了解決用戶興趣上下文建模中的這些限制，我們提出了G2Rec，一個可擴展的框架，將整體基於圖的用戶共同參與建模與語義標記化統一，用於工業規模的生成推薦。總體而言，G2Rec使推薦模型能夠捕捉整體和語義基礎的用戶興趣原型，而無需真實的用戶興趣，從而在工業序列推薦中提供更全面和準確的用戶行為上下文建模。跨產品表面的在線部署和在公共數據集上的廣泛實驗證明了G2Rec相對於現有方法的優越性。

##### **Toward Calibrated Mixture-of-Experts Under Distribution Shift**
2606.20544v1 by Gina Wong, Drew Prinster, Suchi Saria, Rama Chellappa, Anqi Liu

Calibration aligns a model's predictive uncertainty with the frequencies of its empirical outcomes and is important for understanding and trusting reported probabilities. Recent work shows that enforcing calibration at the level of individual predictors can improve ensemble accuracy and calibration, with mixture-of-experts (MoE) models showing strong empirical improvements in particular; however, the conditions under which calibration helps MoE are not well understood. In this work, we study how MoE models behave under distribution shift, focusing on how routing mechanisms interact with expert-level calibration. We show that expert calibration is sufficient to ensure calibration of the overall model under a broad class of distribution shifts in hard-routed models, but is insufficient for calibrating soft-routed models. To address this, we propose an adversarial reweighting that penalizes calibration errors of the routed aggregate under distribution shift, and we demonstrate that it improves the accuracy-calibration tradeoff both on average and on difficult subsets of the data, across model classes, prediction tasks, and distribution shifts.

摘要：校準將模型的預測不確定性與其實證結果的頻率對齊，對於理解和信任報告的概率至關重要。最近的研究顯示，在單個預測器層面強制校準可以提高集成的準確性和校準，特別是專家混合模型（MoE）顯示出強大的實證改進；然而，校準幫助 MoE 的條件尚不清楚。在這項工作中，我們研究了 MoE 模型在分佈轉移下的行為，重點關注路由機制如何與專家級校準互動。我們表明，專家校準足以確保在硬路由模型下，廣泛類別的分佈轉移下整體模型的校準，但對於軟路由模型來說則不足以進行校準。為了解決這個問題，我們提出了一種對抗性重加權，對於在分佈轉移下路由聚合的校準錯誤進行懲罰，並且我們證明它在平均值和數據的困難子集上改善了準確性與校準的權衡，涵蓋了模型類別、預測任務和分佈轉移。

##### **How Do Instructions Shape Speech? Cross-Attention Attribution for Style-Captioned Text-to-Speech**
2606.20532v1 by Nityanand Mathur, Hamees Sayed, Wasim Madha, Apoorv Singh, Sameer Khurana, Akshat Mandloi, Sudarshan Kamath

Style-captioned text-to-speech systems use natural language to control voice characteristics, but how individual words influence acoustic output remains unclear. Understanding this is critical for diagnosing failure modes and improving controllability in expressive TTS. We propose cross-attention attribution for speech diffusion models, adapting the DAAM framework to the speech domain for the first time, and apply it to CapSpeech-TTS. Our method extracts per-token heatmaps across 25 layers and 24 ODE steps. We analyze 3,600 (style caption, text transcript) combinations comprising 120 style captions conditioning the generation of 30 text transcripts each, revealing how caption tokens shape waveforms. Results show: (1) style tokens have lower temporal variance than content/function tokens, confirming global conditioning; (2) style attention correlates with F0 and energy; (3) style conditioning peaks in early steps and deep layers; (4) attention entropy reaches its minimum at layer 17, co-occurring with the style importance peak, indicating maximal network selectivity at the most style-critical stage. This is the first study of how natural language influences cross-attention in speech diffusion models

摘要：風格標題的文本到語音系統使用自然語言來控制聲音特徵，但單個單詞如何影響聲學輸出仍不清楚。理解這一點對於診斷故障模式和提高表達性TTS的可控性至關重要。我們提出了語音擴散模型的交叉注意力歸因，首次將DAAM框架調整到語音領域，並將其應用於CapSpeech-TTS。我們的方法提取了25層和24個ODE步驟的每個標記熱圖。我們分析了3,600個（風格標題，文本轉錄）組合，這些組合由120個風格標題條件生成每個30個文本轉錄，揭示了標題標記如何塑造波形。結果顯示：（1）風格標記的時間變異性低於內容/功能標記，確認了全局條件；（2）風格注意力與F0和能量相關；（3）風格條件在早期步驟和深層達到峰值；（4）注意力熵在第17層達到最小值，與風格重要性峰值同時出現，表明在最關鍵的風格階段網絡選擇性達到最大。這是第一項研究自然語言如何影響語音擴散模型中交叉注意力的研究。

##### **LedgerAgent: Structured State for Policy-Adherent Tool-Calling Agents**
2606.20529v1 by Md Nayem Uddin, Amir Saeidi, Eduardo Blanco, Chitta Baral

Policy-adherent tool-calling agents in customer-service domains must maintain task states across turns while calling tools and obeying domain policies. Task states consist of relevant facts, identifiers, constraints, and conditions observed through user interaction and tool calls. In standard agents, task states are not represented separately. Observations, tool returns, and policy instructions are placed in the prompt, leaving agents to reconstruct the relevant states from the prompt each time they decide what to do next. This design makes state management implicit, creating two common failure modes. An agent may retrieve the right facts but later ground its decision in stale, missing, or incorrect information; and a syntactically valid tool call may still violate a domain policy that depends on the current task state. We introduce \textsc{LedgerAgent}, an inference-time method for tool-calling agents that maintains observed task states in a separate ledger and renders the states into the prompt. The ledger is also used to check state-dependent policy constraints before environment-changing tool calls are executed, blocking policy violations. Across four customer-service domains and a mixed panel of open- and closed-weight models, \textsc{LedgerAgent} improves average pass\textasciicircum{}k over a standard prompt-based tool-calling approach, with the largest gains under stricter multi-trial consistency metrics.

摘要：在客戶服務領域中，遵循政策的工具呼叫代理必須在呼叫工具和遵守領域政策的同時，保持任務狀態跨回合的連貫性。任務狀態由通過用戶互動和工具呼叫觀察到的相關事實、識別符、約束和條件組成。在標準代理中，任務狀態並未單獨表示。觀察結果、工具返回和政策指令被放置在提示中，讓代理每次決定下一步行動時都需從提示中重建相關狀態。這種設計使得狀態管理變得隱性，產生了兩種常見的失敗模式。代理可能檢索到正確的事實，但後來卻基於過時、缺失或不正確的信息來做出決策；而語法上有效的工具呼叫仍可能違反依賴於當前任務狀態的領域政策。我們引入了\textsc{LedgerAgent}，這是一種針對工具呼叫代理的推理時方法，能在單獨的帳本中維護觀察到的任務狀態並將這些狀態呈現到提示中。該帳本還用於在執行改變環境的工具呼叫之前檢查依賴於狀態的政策約束，以阻止政策違規。在四個客戶服務領域和一個混合的開放和封閉權重模型小組中，\textsc{LedgerAgent}在標準基於提示的工具呼叫方法上提高了平均通過\textasciicircum{}k，尤其在更嚴格的多次試驗一致性指標下獲得了最大的增益。

##### **StylisticBias: A Few Human Visual Cues Drive Most Social Biases in MLLMs**
2606.20527v1 by Shaghayegh Kolli, Timo Cavelius, Nafiseh Nikeghbal, Samantha Dalal, Jana Diesner

Multimodal large language models (MLLMs) are increasingly deployed in personally and societally consequential settings, yet the visual cues that shape how these models judge people remain poorly understood. Prior work often compares different (groups of) individuals, making it difficult to separate appearance effects from identity differences. We introduce StylisticBias, a controlled benchmark for evaluating attribute-level social bias in MLLMs. We generate 500 photorealistic base faces and create about 50 single-attribute variations per face, producing about 25K images. This design keeps identity fixed and changes one visual attribute at a time. It lets us measure how specific cues shift model judgments. We evaluate six MLLMs across 25 binary social judgment scenarios. We find that age and body type dominate identity-level effects, while fashion style and other visual cues drive the largest attribute-level shifts. We further find that about 15 attributes account for nearly 80\% of the total variation, showing that bias is concentrated in a small set of visual cues. Sensitivity is strongest in judgments that are semantically aligned with appearance, especially socioeconomic and style-related judgments. We release StylisticBias as a benchmark for fine-grained bias evaluation in multimodal models. Code and dataset: https://github.com/timo-cavelius/StylisticBias and https://hf.co/datasets/shaghayegh/stylistic-bias-dataset.

摘要：多模態大型語言模型（MLLMs）越來越多地應用於個人和社會影響深遠的環境中，但塑造這些模型如何評價人們的視覺線索仍然不甚了解。先前的研究通常比較不同（群體的）個體，使得難以將外貌效應與身份差異分開。我們引入了StylisticBias，一個用於評估MLLMs中屬性層級社會偏見的受控基準。我們生成了500個逼真的基本面孔，並為每個面孔創建約50個單一屬性變體，產生約25K張圖像。這一設計保持身份不變，並一次改變一個視覺屬性。它使我們能夠測量特定線索如何影響模型判斷。我們在25個二元社會判斷場景中評估了六個MLLMs。我們發現年齡和身體類型主導了身份層級的效應，而時尚風格和其他視覺線索則驅動了最大的屬性層級變化。我們進一步發現，約15個屬性占據了近80\%的總變異，顯示出偏見集中在一小組視覺線索中。對於與外貌語義一致的判斷，敏感性最強，特別是社會經濟和風格相關的判斷。我們將StylisticBias作為多模態模型中細緻偏見評估的基準釋出。代碼和數據集：https://github.com/timo-cavelius/StylisticBias 和 https://hf.co/datasets/shaghayegh/stylistic-bias-dataset。

##### **DeepSWIP: Quotient-WMC Counterfactuals for Neural Probabilistic Logic Programs**
2606.20526v1 by Saimun Habib, Vaishak Belle, Fengxiang He

Neurosymbolic systems such as DeepProbLog combine neural perception with probabilistic logic, but standard inference is associational. Counterfactual reasoning additionally requires a causal semantics for interventions and evidence. We introduce DeepSWIP, a single-world counterfactual semantics for DeepProbLog programs. Using neural materialization, we reduce fixed-context neural predicates to ordinary ProbLog choices, apply Single World Intervention Programs (SWIPs), and compute counterfactuals by weighted model counting (WMC) over a single transformed program. Under finite grounding and unique-supported-model assumptions, DeepSWIP is exact relative to the learned materialized FCM. The standard quotient-WMC form of ProbLog conditionals identifies active neural probabilities and explains intervention cleaning, calibration sensitivity, and rare-evidence instability. Experiments on MPI3D confirm the transformation against a DeepTwin construction against 12,000 queries, as predicted and a 2.14$\times$ inference speedup from avoiding the Twin's endogenous duplication. A SUMO HOV experiment shows that neural calibration degradation biases plug-in estimates, while a correctly scoped randomized-policy AIPW estimator removes most first-order bias for population mean and ATE estimands. Code is at https://github.com/saibib/deep_SWIP.

摘要：神經符號系統如 DeepProbLog 將神經感知與概率邏輯結合，但標準推理是關聯性的。反事實推理還需要對干預和證據的因果語義。我們引入了 DeepSWIP，這是一種針對 DeepProbLog 程序的單世界反事實語義。通過神經具現化，我們將固定上下文的神經謂詞簡化為普通的 ProbLog 選擇，應用單世界干預程序 (SWIPs)，並通過對單個轉換程序進行加權模型計數 (WMC) 來計算反事實。在有限基礎和唯一支持模型假設下，DeepSWIP 相對於學習的具現化 FCM 是精確的。ProbLog 條件的標準商 WMC 形式識別活躍的神經概率，並解釋了干預清理、校準敏感性和稀有證據不穩定性。在 MPI3D 上的實驗確認了針對 12,000 個查詢的 DeepTwin 構造的轉換，並預測了避免 Twin 的內生重複所帶來的 2.14$\times$ 推理加速。一個 SUMO HOV 實驗顯示神經校準退化使插件估計產生偏差，而正確範疇的隨機政策 AIPW 估計器則消除了大部分對於人口均值和 ATE 估計量的一階偏差。代碼位於 https://github.com/saibib/deep_SWIP。

##### **SARLO-80: Worldwide Slant SAR Language Optic Dataset 80cm**
2606.20523v1 by Solène Debuysère, Nicolas Trouvé, Nathan Letheule, Elise Colin, Georgia Channing

Multimodal foundation models have advanced rapidly thanks to large optical benchmarks, but comparable resources for synthetic aperture radar (SAR) remain limited. Existing SAR--optical datasets largely rely on low-resolution, intensity-only Ground Range Detected~(GRD) products and do not preserve complex-valued SAR measurements or native acquisition geometry, which restricts physically grounded multimodal learning. In particular, large-scale public datasets combining very-high-resolution (VHR) SAR SLC, aligned optical imagery, and natural-language descriptions are still lacking. We present a VHR SAR--optical--text dataset built from open-access Umbra spotlight acquisitions distributed as Sensor Independent Complex Data (SICD). From around 2,500 worldwide scenes (VV/HH, 20cm--2m native resolution), we standardize all SAR data to an 80cm slant-range grid via band-limited FFT resampling and tile the imagery into 1024 by 1024 patches. For each SAR patch, we retrieve a high-resolution optical tile and warp it into the SAR grid using local coordinate correspondences for local pixel-level alignment. We further generate three caption variants (SHORT/MID/LONG) per sample to support vision--language training and evaluation. Our dataset contains 119,566 triplets (complex and amplitude slant-range SAR patch, aligned optical patch, natural-language description) covering 257 locations across 72 countries and a broad range of land types and infrastructures. We release fixed train/validation/test splits and the full preprocessing and baseline code to enable reproducible benchmarks for multimodal alignment on cross-modal retrieval and conditional generation in native SAR geometry. The dataset is publicly available on the Hugging Face Hub at https://huggingface.co/datasets/ONERA/SARLO-80.

摘要：多模態基礎模型因為大型光學基準而迅速進展，但合成孔徑雷達（SAR）的相應資源仍然有限。現有的SAR-光學數據集主要依賴低解析度、僅強度的地面範圍檢測（GRD）產品，並未保留複數值的SAR測量或原生獲取幾何，這限制了基於物理的多模態學習。特別是，結合超高解析度（VHR）SAR SLC、對齊的光學影像和自然語言描述的大型公共數據集仍然缺乏。我們提出了一個由開放訪問的Umbra聚光獲取數據構建的VHR SAR-光學-文本數據集，這些數據以傳感器獨立複雜數據（SICD）形式分發。從約2,500個全球場景（VV/HH，20cm-2m原生解析度）中，我們將所有SAR數據標準化為80cm斜距網格，通過帶限FFT重採樣並將影像切割為1024 x 1024的補丁。對於每個SAR補丁，我們檢索一個高解析度的光學補丁，並使用局部坐標對應將其變形到SAR網格中，以實現局部像素級對齊。我們進一步為每個樣本生成三種標題變體（SHORT/MID/LONG），以支持視覺-語言訓練和評估。我們的數據集包含119,566個三元組（複數和振幅斜距SAR補丁、對齊的光學補丁、自然語言描述），涵蓋72個國家的257個地點以及廣泛的土地類型和基礎設施。我們發布了固定的訓練/驗證/測試劃分以及完整的預處理和基線代碼，以支持在原生SAR幾何上進行跨模態檢索和條件生成的可重現基準。該數據集在Hugging Face Hub上公開可用，網址為https://huggingface.co/datasets/ONERA/SARLO-80。

##### **Sovereign Execution Brokers: Enforcing Certificate-Bound Authority in Agentic Control Planes**
2606.20520v1 by Jun He, Deying Yu

Autonomous agents are increasingly connected to cloud, deployment, and data-control workflows, but production mutation authority should not reside inside non-deterministic reasoning processes. Existing access-control mechanisms authorize identities, while assurance layers certify proposed actions; neither alone provides a mandatory enforcement point for certified authority at the moment of mutation. This paper introduces the Sovereign Execution Broker (SEB), a runtime enforcement boundary for certificate-bound agentic infrastructure. SEB consumes certificates issued by the Sovereign Assurance Boundary (SAB), verifies that the requested mutation matches the certified execution contract, checks validity windows, policy epochs, revocation epochs, and live-state drift, mints scoped execution identity, invokes infrastructure APIs, and records signed decision and outcome records. By separating proposal, admission, and execution, SEB turns certified authority into a short-lived, revocable, auditable runtime capability, provided that production mutation APIs reject non-broker identities. We present the SEB execution model, certificate and replay-verification predicates, scoped identity semantics, bypass-prevention deployment patterns, failure behavior, and a concrete prototype implementation. We evaluate the prototype on AWS and Kubernetes clusters, measuring latency overheads, revocation propagation, drift detection, and security under fault injection.

摘要：自主代理越來越多地與雲端、部署和數據控制工作流程相連，但生產變更權限不應該存在於非確定性推理過程中。現有的訪問控制機制授權身份，而保證層則認證提議的行動；單獨的兩者都無法在變更的瞬間提供一個強制執行的認證權限點。本文介紹了主權執行代理（Sovereign Execution Broker，SEB），這是一個針對證書綁定的代理基礎設施的運行時執行邊界。SEB 消耗由主權保證邊界（Sovereign Assurance Boundary，SAB）發出的證書，驗證請求的變更是否符合認證的執行合約，檢查有效性窗口、政策週期、撤銷週期和實時狀態漂移，鑄造範圍執行身份，調用基礎設施 API，並記錄簽署的決策和結果記錄。通過分離提案、入場和執行，SEB 將認證權限轉化為一種短期的、可撤銷的、可審計的運行時能力，前提是生產變更 API 拒絕非代理身份。我們提出了 SEB 執行模型、證書和重放驗證謂詞、範圍身份語義、繞過預防部署模式、故障行為以及具體的原型實現。我們在 AWS 和 Kubernetes 集群上對原型進行評估，測量延遲開銷、撤銷傳播、漂移檢測和故障注入下的安全性。

##### **FlowEdit: Associative Memory for Lifelong Pronunciation Adaptation in Flow-Matching TTS**
2606.20518v1 by Harshit Singh, Ayush Pratap Singh, Nityanand Mathur

Flow-matching text-to-speech systems achieve remarkable zero-shot quality but remain static after deployment: pronunciation errors on out-of-vocabulary proper nouns persist unless the model is retrained. We introduce FlowEdit, a life-long adaptation framework for frozen flow-matching TTS that learns pronunciation corrections as latent conditioning edits rather than weight updates. When corrective feedback is provided, FlowEdit optimizes a token-level perturbation in the text embedding space, then stores the correction in a Modern Hopfield Network serving as content-addressable episodic memory. At inference, corrections are retrieved via soft attention with a similarity gate, enabling fuzzy morphological matching. On our curated benchmark of 312 multilingual proper nouns across 18 language families, FlowEdit reduces target-word Phoneme Error Rate by 92.7% relative to the zero-shot baseline while maintaining identical general-speech quality. Corrections complete in approximately 15 seconds on a single GPU.

摘要：流匹配的文本到語音系統在零樣本情況下達到了卓越的質量，但在部署後仍然保持靜態：對於超出詞彙範圍的專有名詞的發音錯誤持續存在，除非模型被重新訓練。我們介紹了FlowEdit，一個針對凍結流匹配TTS的終身適應框架，它學習發音修正作為潛在條件編輯，而不是權重更新。當提供修正反饋時，FlowEdit在文本嵌入空間中優化一個令牌級的擾動，然後將修正存儲在一個作為內容可尋址的情節記憶的現代霍普菲爾德網絡中。在推理時，通過軟注意力和相似性閘檢索修正，實現模糊形態匹配。在我們精心策劃的基準中，涵蓋了18個語言家族的312個多語言專有名詞，FlowEdit相對於零樣本基線將目標詞的音素錯誤率降低了92.7%，同時保持相同的一般語音質量。修正大約在單個GPU上完成，耗時約15秒。

##### **Multi-LCB: Extending LiveCodeBench to Multiple Programming Languages**
2606.20517v1 by Maria Ivanova, Pavel Zadorozhny, Rodion Levichev, Ivan Petrov, Adamenko Pavel, Ivan Lopatin, Alexey Kutalev, Dmitrii Babaev

LiveCodeBench (LCB) has recently become a widely adopted benchmark for evaluating large language models (LLMs) on code-generation tasks. By curating competitive programming problems, constantly adding fresh problems to the set, and filtering them by release dates, LCB provides contamination-aware evaluation and offers a holistic view of coding capability. However, LCB remains restricted to Python, leaving open the question of whether LLMs can generalize across the diverse programming languages required in real-world software engineering.
  We introduce Multi-LCB, a benchmark for evaluating LLMs across twelve programming languages, including Python. Multi-LCB transforms Python tasks from the LCB dataset into equivalent tasks in other languages while preserving LCB's contamination controls and evaluation protocol. Because it is fully compatible with the original LCB format, Multi-LCB will automatically track future LCB updates, enabling systematic assessment of cross-language code generation competence and requiring models to sustain performance well beyond Python.
  We evaluated 24 LLMs for instruction and reasoning on Multi-LCB, uncovering evidence of Python overfitting, language-specific contamination, and substantial disparities in multilingual performance. Our results establish Multi-LCB as a rigorous new benchmark for multi-programming-language code evaluation, directly addressing LCB's primary limitation and exposing critical gaps in current LLM capabilities.

摘要：LiveCodeBench (LCB) 最近已成為評估大型語言模型 (LLMs) 在代碼生成任務上的廣泛採用基準。<lb/>通過策劃競爭性編程問題，不斷向集合中添加新問題，並按發布日期篩選，LCB 提供了考慮污染的評估，並提供了編碼能力的整體視角。<lb/>然而，LCB 仍然僅限於 Python，這引發了 LLM 是否能夠在現實世界軟件工程中所需的多種編程語言之間進行泛化的問題。<lb/>我們介紹了 Multi-LCB，這是一個用於評估 LLM 在包括 Python 在內的十二種編程語言上的基準。<lb/>Multi-LCB 將 LCB 數據集中的 Python 任務轉換為其他語言中的等效任務，同時保留 LCB 的污染控制和評估協議。<lb/>因為它與原始 LCB 格式完全兼容，Multi-LCB 將自動跟踪未來的 LCB 更新，實現對跨語言代碼生成能力的系統評估，並要求模型在 Python 之外保持良好的性能。<lb/>我們在 Multi-LCB 上評估了 24 個 LLM 的指令和推理，揭示了 Python 過擬合、語言特定污染和多語言表現之間的重大差異的證據。<lb/>我們的結果確立了 Multi-LCB 作為一個嚴謹的新基準，用於多編程語言代碼評估，直接解決了 LCB 的主要限制，並揭示了當前 LLM 能力中的關鍵差距。

##### **Efficient and Sound Probabilistic Verification for AI Agents**
2606.20510v1 by Alaia Solko-Breslin, Pramod Kaushik Mudrakarta, Mihai Christodorescu, Somesh Jha, Krishnamurthy Dj Dvijotham

Securing AI agents that operate in complex digital environments has become a critical need, and runtime monitoring approaches that formulate and enforce policies expressed in a formal language like Datalog offer a promising solution. However, existing approaches are restricted to deterministic policies. In many practical applications of AI agents, there is a need to enforce security policies in the face of ambiguity, leading to probabilistic predicates or state transitions (for example, a declassifier or Personally Identifiable Information (PII) detector that has some failure probability on each invocation). Furthermore, in many such applications, one cannot easily make the independence assumptions necessary to invoke prior work on probabilistic inference in Datalog. We address this by introducing a sound and efficient framework for such verification based on distributionally robust optimization, computing sound upper bounds on the probability of policy violation regardless of possible correlations between predicates. On standard benchmarks for terminal and tool calling agents, we demonstrate that our approach outperforms prior art and improves the security-utility trade-off while ensuring rigorous bounds on the probability of policy violation.

摘要：確保在複雜數位環境中運作的 AI 代理的安全性已成為一項關鍵需求，而以 Datalog 等形式語言表達並執行政策的運行時監控方法提供了一個有前景的解決方案。<lb/>然而，現有的方法僅限於確定性政策。在許多 AI 代理的實際應用中，需要在面對模糊性的情況下執行安全政策，這導致了概率性謂詞或狀態轉換（例如，每次調用都有一定失敗概率的去分類器或個人識別信息（PII）檢測器）。<lb/>此外，在許多此類應用中，無法輕易做出獨立性假設，以便調用 Datalog 中概率推理的先前工作。我們通過引入一個基於分佈魯棒優化的健全且高效的驗證框架來解決這個問題，計算政策違規概率的健全上限，而不考慮謂詞之間可能的相關性。<lb/>在終端和工具調用代理的標準基準測試中，我們展示了我們的方法超越了先前的技術，並改善了安全性與效用之間的權衡，同時確保了政策違規概率的嚴格界限。

##### **What Do Safety-Aligned LLMs Learn From Mixed Compliance Demonstrations?**
2606.20508v1 by Sihui Dai, Mann Patel

Prior work has shown that in-context demonstrations can jailbreak language models, but it remains unclear how models interpret different types of compliance demonstrations. We study this by mixing benign compliance demonstrations (non-harmful request, helpful response) with harmful compliance demonstrations (harmful request, helpful response) and testing three hypotheses about how demonstration composition drives harmful compliance. Across four models, we find that benign and harmful demonstrations are not interchangeable: benign demonstrations can either reduce or increase harmful compliance depending on the model. We further show that preference optimization is the critical training stage that prevents benign demonstrations from increasing harmful compliance, that demonstration ordering exhibits strong recency bias, and that models differ in how refusal interacts with in-context learning: some adopt demonstrated formatting even when refusing, while others override all in-context signals upon refusal. Taken together, this work moves beyond showing that demonstration-based jailbreaking works to characterizing how it works: what models extract from compliance demonstrations depends on demonstration content, ordering, and training methodology.

摘要：先前的研究顯示，在情境中的示範可以破解語言模型，但目前尚不清楚模型如何解釋不同類型的遵從示範。<lb/>我們通過將良性的遵從示範（無害請求，幫助性回應）與有害的遵從示範（有害請求，幫助性回應）混合來研究這一點，並測試三個關於示範組合如何驅動有害遵從的假設。<lb/>在四個模型中，我們發現良性和有害示範並不是可以互換的：根據模型的不同，良性示範可以減少或增加有害遵從。<lb/>我們進一步顯示，偏好優化是防止良性示範增加有害遵從的關鍵訓練階段，示範的排序顯示出強烈的近期偏見，並且模型在拒絕與情境學習的互動上存在差異：有些模型即使在拒絕時也會採用示範格式，而其他模型則在拒絕時覆蓋所有情境信號。<lb/>綜合來看，這項工作超越了僅僅展示基於示範的破解有效性，並對其運作方式進行了特徵化：模型從遵從示範中提取的內容取決於示範的內容、排序和訓練方法。

##### **FreeStyle: Free Control of Style-Content Dual-Reference Generation from Community LoRA Mining**
2606.20506v1 by Jinghong Lan, Wei Cheng, Yunuo Chen, Ziqi Ye, Peng Xing, Yixiao Fang, Rui Wang, Yufeng Yang, Xuanyang Zhang, Xianfang Zeng, Difan Zou, Gang Yu, Chi Zhang

Style-content dual-reference generation aims to synthesize an image that preserves the structure and semantics of a content reference while adopting the style of a separate style reference.Despite recent progress, this setting remains challenging because models must balance content fidelity, style alignment, and instruction following avoiding semantic leakage from the style reference.A key bottleneck is the lack of large-scale triplet data with clean content-style separation and broad long-tail style coverage.In this work, we propose FreeStyle, a scalable dual-reference generation framework based on community LoRA mining.We treat community LoRAs as compositional anchors for style and content, and design a rigorous generation and filtering pipeline to construct large-scale Style-Reference and Content-Reference triplets across multiple base models.To address content leakage, we adopt a two-stage curriculum with stage-specific disentanglement mechanisms: an attention-level enrichment constraint that suppresses style-reference leakage in the style-transfer stage, and a frequency-aware RoPE modulation strategy that targets positional-correspondence-based leakage in the harder dual-reference stage.We also introduce a benchmark covering both style-reference and dual-reference generation, with evaluations on style similarity, content preservation, aesthetics, instruction following, and leakage rejection. The benchmark incorporates a style-invariant Content Alignment Score (CAS) and introduces a calibrated VLM-based Rejection Score for evaluating generation reliability and leakage suppression.Extensive experiments show that our model achieves a strong balance among style alignment, content preservation, and leakage suppression.

摘要：風格-內容雙參考生成旨在合成一個圖像，該圖像在保留內容參考的結構和語義的同時，採用單獨風格參考的風格。儘管最近取得了進展，但這一設置仍然具有挑戰性，因為模型必須平衡內容忠實度、風格對齊和遵循指令，避免風格參考中的語義洩漏。一個主要瓶頸是缺乏具有乾淨內容-風格分離和廣泛長尾風格覆蓋的大規模三元組數據。在這項工作中，我們提出了FreeStyle，一個基於社區LoRA挖掘的可擴展雙參考生成框架。我們將社區LoRA視為風格和內容的組合錨，並設計了一個嚴謹的生成和過濾管道，以構建跨多個基礎模型的大規模風格參考和內容參考三元組。為了解決內容洩漏問題，我們採用兩階段課程，並設計了特定階段的解耦機制：在風格轉換階段，採用一種注意力級別的增強約束來抑制風格參考的洩漏，並在更困難的雙參考階段，採用一種基於頻率的RoPE調制策略來針對基於位置對應的洩漏。我們還引入了一個涵蓋風格參考和雙參考生成的基準，並對風格相似性、內容保留、美學、遵循指令和洩漏拒絕進行評估。該基準包含一個風格不變的內容對齊分數（CAS），並引入了一個經過校準的基於VLM的拒絕分數，用於評估生成的可靠性和洩漏抑制。大量實驗表明，我們的模型在風格對齊、內容保留和洩漏抑制之間實現了良好的平衡。

##### **Calibration Without Comprehension: Diagnosing the Limits of Fine-Tuning LLMs for Vulnerability Detection in Systems Software**
2606.20502v1 by Arastoo Zibaeirad, Marco Vieira

Whether LLMs scoring well on vulnerability benchmarks genuinely reason about security or merely pattern-match on contaminated data remains unresolved. We present CWE-Trace, a framework for LLM vulnerability detection built from 834 manually curated Linux kernel samples spanning 74 CWEs. The framework enforces a strict temporal split (pre-2025 historical set / post-cutoff leakage-free set), preserves context-aware vulnerable--patched pairs, and introduces two diagnostic metrics: the Directional Failure Index (DFI) and Hierarchical Distance and Direction (HDD). We evaluate eight vanilla LLMs and 15 LoRA fine-tuned variants across non-targeted detection, targeted detection, and CWE classification. Our analysis yields two key results. First, data contamination provides no measurable advantage. Function-level analysis shows that 84% of nominally contaminated samples carry no usable memorization signal: vulnerable functions are absent or cross-mapped across datasets, and ~31% of contaminated samples carry CWE misclassification. Second, backbone directional priors dominate fine-tuning. Models exhibit stable, systematic failure modes (DFI ranging from -85.5 to +94.8 pp) that persist from historical to post-cutoff data and resist correction. Fine-tuning shifts the output threshold without changing the decision policy. This is calibration without comprehension: output distributions adapt to training data while the underlying security reasoning remains absent. The weakest backbone at binary detection (DeepSeek-R1) gains the most in coarse CWE classification, revealing that detection and understanding are decoupled capabilities. The best detection score reaches only 52.1% (+2.1 pp above chance); exact CWE ranking remains below 1.3% Top-1 accuracy, confirming that current LLMs lack reliable security reasoning for systems software, regardless of fine-tuning strategy.

摘要：無論大型語言模型（LLMs）在漏洞基準測試中表現良好是否真正能夠推理安全性，或僅僅是在污染數據上進行模式匹配，仍然沒有定論。<lb/>我們提出了CWE-Trace，一個基於834個手動策劃的Linux內核樣本（涵蓋74個CWE）的LLM漏洞檢測框架。<lb/>該框架強制執行嚴格的時間分割（2025年前的歷史數據集/截止後無洩漏的數據集），保留上下文感知的漏洞-修補配對，並引入了兩個診斷指標：方向失敗指數（DFI）和層次距離與方向（HDD）。<lb/>我們評估了八個原始LLM和15個LoRA微調變體，涵蓋非目標檢測、目標檢測和CWE分類。<lb/>我們的分析得出了兩個關鍵結果。<lb/>首先，數據污染並未提供可測量的優勢。<lb/>函數級別的分析顯示，84%的名義上污染樣本沒有可用的記憶信號：漏洞函數在數據集中缺失或交叉映射，約31%的污染樣本存在CWE錯誤分類。<lb/>其次，骨幹方向先驗主導微調。<lb/>模型表現出穩定的系統性失敗模式（DFI範圍從-85.5到+94.8個百分點），這些模式從歷史數據到截止後數據持續存在並抵抗修正。<lb/>微調改變了輸出閾值而不改變決策政策。<lb/>這是沒有理解的校準：輸出分佈適應訓練數據，而潛在的安全推理仍然缺失。<lb/>在二元檢測中最弱的骨幹（DeepSeek-R1）在粗略CWE分類中獲得了最多的增益，顯示檢測和理解是解耦的能力。<lb/>最佳檢測分數僅達到52.1%（比隨機機會高2.1個百分點）；精確的CWE排名仍然低於1.3%的Top-1準確率，確認當前的LLMs對於系統軟件缺乏可靠的安全推理，無論微調策略如何。

##### **Contagion Networks: Evaluator Bias Propagation in Multi-Agent LLM Systems**
2606.20493v1 by Zewen Liu

When large language models serve as evaluators in multi-agent systems, their systematic evaluation biases propagate through the agent network. We introduce Contagion Networks, a formal framework for measuring how evaluator biases spread across interacting LLM agents. In a controlled 3-agent experiment using DeepSeek-chat with three distinct evaluator bias profiles (structured, balanced, evidence-based), we measure the Cross-Agent Contagion Matrix Gamma_3 and find that evaluator biases consistently propagate between agents (gamma in [0.157, 0.352]), even within the same underlying model. We identify three propagation regimes governed by the spectral radius rho(Gamma_N), and demonstrate that homogeneous-model agents produce contagion coefficients 3-5x weaker than cross-model coefficients observed in prior work (MM-EPC: gamma approx 0.85-1.3), placing them in the suppression regime. We show that increasing evaluator committee size from k=1 to k=3 reduces effective contagion by 72.4%, providing an actionable mitigation strategy. We release the open-source Contagion Network experimental framework.

摘要：當大型語言模型在多代理系統中擔任評估者時，它們的系統性評估偏見會在代理網絡中傳播。我們引入了傳染網絡，這是一個正式框架，用於測量評估者偏見如何在互動的LLM代理之間擴散。在一個受控的三代理實驗中，使用DeepSeek-chat和三種不同的評估者偏見配置文件（結構化、平衡、基於證據），我們測量了跨代理傳染矩陣Gamma_3，發現評估者偏見在代理之間持續傳播（gamma在[0.157, 0.352]之間），即使在相同的基礎模型中。我們確定了三種由光譜半徑rho(Gamma_N)主導的傳播範疇，並展示了同質模型代理產生的傳染係數比先前工作中觀察到的跨模型係數（MM-EPC: gamma約0.85-1.3）弱3-5倍，將它們置於抑制範疇。我們顯示，將評估者委員會的規模從k=1增加到k=3可減少有效傳染72.4%，提供了一個可行的緩解策略。我們發布了開源的傳染網絡實驗框架。

##### **Beyond Global Replanning: Hierarchical Recovery for Cross-Device Agent Systems**
2606.20487v1 by Shu Yao, Yuhua Luo, Qian Long, Jingru Fan, Zhuoyuan Yu, Yuheng Wang, Lin Wu, Yufan Dang, Huatao Li, Chen Qian

Real-world computer-use tasks often span multiple applications and devices, requiring agents to coordinate heterogeneous environments under dynamic runtime failures. Existing multi-device agent systems support task decomposition and cross-device assignment, but recovery remains largely coarse-grained: when execution fails, they typically retry the same strategy, reassign the subtask, or revise the global plan, without systematically modeling the device-local strategy space. This limits their ability to distinguish failures that can be repaired within the current device from those that require cross-device replanning. We propose \textbf{H-RePlan}, a hierarchical replanning framework for multi-device agents with unified API--CLI--GUI execution. H-RePlan equips each device with interchangeable execution strategies and separates device-local strategy recovery from orchestrator-level global replanning through a compact cross-layer failure abstraction. To evaluate this capability, we introduce \textbf{HeraBench}, a fault-injected benchmark that constructs cross-device workflows over Linux and Android devices and injects strategy- and device-level failures. Experiments show that H-RePlan substantially outperforms single-strategy and coarse-grained multi-device baselines, achieving higher completion, instruction adherence, and perfect-pass rates while reducing the token cost required for reliable end-to-end success. These results demonstrate that scope-aware hierarchical recovery is essential for robust multi-device agent execution.

摘要：現實世界中的電腦使用任務通常跨越多個應用程序和設備，要求代理在動態運行時故障下協調異質環境。現有的多設備代理系統支持任務分解和跨設備分配，但恢復仍然主要是粗粒度的：當執行失敗時，它們通常會重試相同的策略、重新分配子任務或修訂全局計劃，而不系統性地建模設備本地的策略空間。這限制了它們區分可以在當前設備內修復的故障與需要跨設備重新規劃的故障的能力。我們提出了 \textbf{H-RePlan}，一個為多設備代理提供統一API--CLI--GUI執行的分層重新規劃框架。H-RePlan 為每個設備配備可互換的執行策略，並通過緊湊的跨層故障抽象將設備本地策略恢復與協調者級別的全局重新規劃分開。為了評估這一能力，我們引入了 \textbf{HeraBench}，一個故障注入基準，該基準在Linux和Android設備上構建跨設備工作流程並注入策略和設備級故障。實驗表明，H-RePlan 在完成率、指令遵循率和完美通過率方面大幅超越單一策略和粗粒度多設備基準，同時減少了可靠端到端成功所需的令牌成本。這些結果表明，範圍感知的分層恢復對於穩健的多設備代理執行至關重要。

##### **Optimal Order of Multi-Agent and General Many-Body Systems**
2606.20485v1 by Jake J. Xia

This paper develops a general framework for analyzing multi-agent systems with feedback loops between agents actions and collective observations. The framework is built on two fundamental agent-level variables: power, which measures agent influence on collective outcomes, and response functions, which determine how agents react to observations. We derive how macroscopic properties, including total power, useful power, entropy, order, fragility, and mobility, emerge from these two variables of heterogeneous agents. To study the trade off between growth and resilience, we introduce a system-level utility function parameterized by a risk-appetite coefficient and derive an optimal degree of order that balances productivity, stability, and adaptability. The analysis suggests that stronger synchronization can increase collective output but may also increase systemic fragility and reduce mobility. We further argue that order, entropy, information, and useful energy are task-dependent and system-relative concepts whose meanings depend on the objectives of the system. By measuring and designing agent power distributions and response functions, it may be possible to better understand, predict, and optimize collective behavior and identify the conditions under which collective intelligence and optimal order emerge.

摘要：這篇論文發展了一個分析具有反饋迴路的多代理系統的通用框架，該系統中代理的行動與集體觀察之間存在互動。<lb/>該框架建立在兩個基本的代理層級變數上：權力，這衡量代理對集體結果的影響，以及反應函數，這決定了代理如何對觀察做出反應。<lb/>我們推導出宏觀特性，包括總權力、有用權力、熵、秩序、脆弱性和流動性，如何從這兩個異質代理的變數中產生。<lb/>為了研究增長與韌性之間的權衡，我們引入了一個系統層級的效用函數，該函數由風險偏好係數參數化，並推導出一個最佳的秩序程度，以平衡生產力、穩定性和適應性。<lb/>分析表明，更強的同步性可以增加集體產出，但也可能增加系統的脆弱性並減少流動性。<lb/>我們進一步主張，秩序、熵、信息和有用能量是依賴於任務和系統的相對概念，其意義取決於系統的目標。<lb/>通過測量和設計代理的權力分佈和反應函數，可能更好地理解、預測和優化集體行為，並識別集體智慧和最佳秩序出現的條件。

##### **Your Mouse and Eyes Secretly Leak Your Preference: LLM Alignment using Implicit Feedback from Users**
2606.20482v1 by Haw-Shiuan Chang, Jeffrey Gomez, Mehul Patwari, Aryan Sajith, Hamed Zamani

To align a Large Language Model (LLM), most existing methods collect explicit human feedback and train a reward model to predict the human preference based on the response text. These existing methods have two key limitations. First, the users rarely provide explicit feedback for LLM responses, which makes the high-quality preference annotation expensive to collect. Second, the methods do not leverage implicit human feedback, which has proven vital to the economic moats of Internet giants. To quantify the value of implicit feedback, we build a new dataset called IFLLM, which collects 1336 multi-turn questions from the 59 Mechanical Turk workers, their mouse trajectories, and eye gazing points to the LLMs' responses from their webcams. IFLLM shows that the users have very diverse types of gazing behavior and mouse trajectories. Our reward model based on the implicit user feedback boosts the accuracy of the text-based reward model from 55% to 64% and nearly triples the relative response quality improvements after applying the DPO to eight LLMs, demonstrating the value of implicit feedback in the wild. Our data collection website, dataset, and codes can be found at https://github.com/themehulpatwari/llm-implicit-feedback/.

摘要：為了對齊大型語言模型（LLM），大多數現有方法收集明確的人類反饋並訓練一個獎勵模型，以根據回應文本預測人類偏好。這些現有方法有兩個主要限制。首先，使用者很少對LLM的回應提供明確的反饋，這使得高質量的偏好標註收集成本高昂。其次，這些方法未能利用隱性的人類反饋，而隱性反饋對於互聯網巨頭的經濟護城河已被證明至關重要。為了量化隱性反饋的價值，我們建立了一個名為IFLLM的新數據集，該數據集收集了來自59名Mechanical Turk工作者的1336個多輪問題、他們的滑鼠軌跡以及從他們的網路攝影機對LLM回應的目光注視點。IFLLM顯示使用者具有非常多樣的注視行為和滑鼠軌跡。我們基於隱性使用者反饋的獎勵模型將基於文本的獎勵模型的準確率從55%提高到64%，並在對八個LLM應用DPO後，幾乎使相對回應質量的改善增加了三倍，證明了隱性反饋在實際應用中的價值。我們的數據收集網站、數據集和代碼可以在https://github.com/themehulpatwari/llm-implicit-feedback/找到。

##### **Scalable Training of Spatially Grounded 2D Vision-Language Models for Radiology**
2606.20477v1 by Yusuf Salcan, Simon Ging, Robin Schirrmeister, Philipp Arnold, Elmar Kotter, Behzad Bozorgtabar, Thomas Brox

We study how to train visually grounded vision-language models (VLMs) for radiology without manual spatial annotations. We introduce RefRad2D, a large-scale bilingual (German/English) dataset of 1.2M CT and MR image-text pairs derived from clinical practice, with task-specific VQA and spatial grounding subsets generated automatically via LLM-based curation and automated segmentation. Trained on this data, our model RadGrounder jointly performs report generation, visual question answering, and spatial grounding via bounding-box detection or segmentation. On external VQA benchmarks (Slake, VQA-RAD), RadGrounder achieves competitive results with specialized medical VLMs. Adding our clinical data to the training mixture improves open-ended VQA over fine-tuning on the downstream datasets alone, showing the transferability of our dataset. Crucially, adding grounding supervision does not degrade language quality, enabling spatially verifiable outputs at no cost to VQA performance.

摘要：我們研究如何在沒有手動空間標註的情況下，訓練視覺基礎的視覺-語言模型（VLMs）以應用於放射學。<lb/>我們介紹了RefRad2D，一個大型雙語（德語/英語）數據集，包含120萬對來自臨床實踐的CT和MR影像-文本對，並通過基於LLM的策劃和自動分割自動生成特定任務的VQA和空間基礎子集。<lb/>基於這些數據訓練的模型RadGrounder，能夠通過邊界框檢測或分割共同執行報告生成、視覺問題回答和空間基礎。<lb/>在外部VQA基準（Slake, VQA-RAD）上，RadGrounder的表現與專門的醫療VLMs相當。<lb/>將我們的臨床數據添加到訓練混合中，改善了開放式VQA的表現，超越了僅在下游數據集上進行微調的效果，顯示了我們數據集的可轉移性。<lb/>關鍵是，添加基礎監督並不會降低語言質量，使得在不影響VQA性能的情況下，能夠實現空間可驗證的輸出。

##### **Analyzing Defensive Misdirection Against Model-Guided Automated Attacks on Agentic AI Systems**
2606.20470v1 by Reza Soosahabi, Vivek Namsani

Agentic AI systems increasingly rely on language-model components to interpret instructions, process external data, invoke tools, and coordinate with other agents. These capabilities make prompt-injection and jailbreak attacks more consequential, especially as attackers adopt model-guided automation to scale probing, prompt refinement, and response evaluation. This work analyzes the resulting attack-defense setting through a probabilistic model of a target system, its defense mechanism, and the attacker's automated judge. Our analysis shows that conventional detect-and-block defenses can allow attacker success rate (ASR) to approach one as the query budget grows, since predictable refusals provide useful feedback to automated search. We then examine detect-and-misdirect, where detected malicious interactions receive controlled, non-operational responses designed to induce false-positive errors in the attacker's judge. This strategy reduces the positive predictive value of attacker-selected candidates and yields a bounded asymptotic ASR. We evaluate a proof-of-concept realization of this strategy through Contextual Misdirection via Progressive Engagement (CMPE), a lightweight conversational misdirection method designed to replace predictable refusal text with safe but strategically misleading responses in automated jailbreak settings. On jailbreak benchmarks, CMPE reduces estimated ASR upper bounds by up to two orders of magnitude and nearly eliminates verified attack success in end-to-end PAIR and GPTFuzz attack runs.

摘要：代理人工智慧系統越來越依賴語言模型組件來解釋指令、處理外部數據、調用工具以及與其他代理協調。這些能力使得提示注入和越獄攻擊變得更加重要，特別是當攻擊者採用模型引導自動化來擴大探測、提示精煉和回應評估時。這項工作通過目標系統的概率模型、其防禦機制以及攻擊者的自動判斷者來分析由此產生的攻擊防禦環境。我們的分析顯示，傳統的檢測與阻止防禦可以使攻擊者成功率（ASR）隨著查詢預算的增長而接近於一，因為可預測的拒絕提供了對自動搜索有用的反饋。我們接著檢查檢測與誤導，其中檢測到的惡意互動會收到控制的、非操作性的回應，旨在誘導攻擊者的判斷者產生假陽性錯誤。這一策略降低了攻擊者選擇候選者的正向預測值，並產生有界的漸近ASR。我們通過逐步參與的情境誤導（CMPE）評估這一策略的概念驗證實現，這是一種輕量級的對話誤導方法，旨在在自動越獄環境中用安全但策略性誤導的回應替換可預測的拒絕文本。在越獄基準測試中，CMPE將估計的ASR上限降低了多達兩個數量級，並幾乎消除了在端到端PAIR和GPTFuzz攻擊運行中驗證的攻擊成功。

##### **Context-Aware Hierarchical Bayesian Modeling of IVF Laboratory Environmental Conditions**
2606.20459v1 by Zahra Asghari Varzaneh, Reza Khoshkangini, Pia Saldeen, Lars Johansson, Thomas Ebner

IVF pregnancy rates are routinely modeled using patient-level variables, while high-resolution laboratory environmental data remain underutilized. We show that this is a missed opportunity. Rather than relying on raw sensor averages, we engineer 55 context-aware temporal features, including rolling thermal stability, simultaneous temperature-humidity adherence, peak stress duration, and post-stress recovery speed, that capture the dynamics of incubator microenvironments. On 61 weeks of data from an Asian IVF clinic, these features reduce cross-validated prediction error to 1.27%, compared to 3-5% for raw averages. We then train a hierarchical Bayesian Beta regression model that shares environmental effects across an Asian and a Northern European clinic via partial pooling, while preserving site-specific baselines. On held-out data from the Northern European clinic, the model achieves R2 = 0.86 and a 64% error reduction for the 35-39 age group over a naive baseline, demonstrating that structured environmental monitoring contains clinically meaningful, transferable signal.

摘要：IVF 懷孕率通常使用患者層級變數進行建模，而高解析度的實驗室環境數據則未被充分利用。<lb/>我們展示了這是一個錯失的機會。<lb/>我們不僅依賴原始傳感器平均值，而是設計了 55 個具上下文感知的時間特徵，包括滾動熱穩定性、同時的溫度-濕度遵循、峰值壓力持續時間和壓力後恢復速度，這些特徵捕捉了培養箱微環境的動態。<lb/>在來自一家亞洲 IVF 診所的 61 週數據上，這些特徵將交叉驗證的預測誤差降低至 1.27%，相比之下，原始平均值的誤差為 3-5%。<lb/>然後，我們訓練了一個層級貝葉斯 Beta 回歸模型，通過部分合併共享亞洲和北歐診所的環境效應，同時保留特定地點的基準。<lb/>在北歐診所的保留數據上，該模型實現了 R2 = 0.86，並在 35-39 歲年齡組上相較於天真的基準減少了 64% 的誤差，證明結構化的環境監測包含臨床上有意義的可轉移信號。

##### **Repurposing a Speech Classifier for Guided Diffusion-Based Speech Generation**
2606.20457v1 by Rostislav Makarov, Timo Gerkmann

Classifier guidance is a way to control diffusion generation by using a noise-conditioned classifier to steer the sampling process toward a target class. One drawback of classifier guidance is that it requires two separately trained models: a classifier and a diffusion model. We therefore study a more compact alternative in which a conventionally trained speech classifier is repurposed as the backbone for diffusion generation. Starting from a frozen noise-conditioned classifier in log-Mel space, we attach a lightweight subnetwork that reuses intermediate classifier representations and train only this subnetwork under a Denoising Score Matching objective. Our work shows that a pretrained classifier can be repurposed for conditional generation, providing an appealing bridge between discriminative modeling and conditional speech synthesis resulting in high speech quality within a single-backbone model, with reduced memory footprint and computational cost.

摘要：分類器引導是一種通過使用噪聲條件分類器來控制擴散生成的方法，以將取樣過程引導至目標類別。分類器引導的一個缺點是它需要兩個單獨訓練的模型：一個分類器和一個擴散模型。因此，我們研究了一種更緊湊的替代方案，其中傳統訓練的語音分類器被重新用作擴散生成的骨幹。從一個在對數梅爾空間中凍結的噪聲條件分類器開始，我們附加了一個輕量級的子網絡，該子網絡重用中間分類器表示，並僅在去噪分數匹配目標下訓練這個子網絡。我們的工作表明，預訓練的分類器可以重新用於條件生成，提供了一個吸引人的橋樑，將判別建模和條件語音合成連接起來，從而在單一骨幹模型中實現高語音質量，同時減少內存佔用和計算成本。

##### **Interpretable Sperm Morphology Classification via Attention-Guided Deep Learning**
2606.20438v1 by Zahra Asghari Varzaneh, Reza Khoshkangini, Thomas Ebner, Lars Johansson

Male infertility is a major cause of couple infertility, often linked to abnormal sperm morphology. While deep learning models offer automated analysis, most lack interpretability, limiting their clinical adoption. This study proposes an attention-guided deep learning framework for sperm morphology classification. We combine a pretrained EfficientNet-B0 with a Convolutional Block Attention Module (CBAM) to focus on key areas of the sperm head, improving both accuracy and interpretability. Evaluated on the SMIDS and HuSHem public datasets, our model achieves accuracies of 90.2% and 93.9% (macro F1 scores of 0.913 and 0.948), outperforming SimpleCNN and standard EfficientNet-B0. Furthermore, we use Grad-CAM++ visualizations to highlight features influencing the model's decisions. The results demonstrate that this accurate and transparent framework is a practical tool for automated sperm analysis in fertility clinics.

摘要：男性不孕是夫妻不孕的主要原因，通常與精子形態異常有關。雖然深度學習模型提供自動化分析，但大多數缺乏可解釋性，限制了其臨床應用。這項研究提出了一種基於注意力引導的深度學習框架，用於精子形態分類。我們將預訓練的EfficientNet-B0與卷積塊注意力模塊（CBAM）結合，以專注於精子頭部的關鍵區域，從而提高準確性和可解釋性。在SMIDS和HuSHem公共數據集上評估，我們的模型達到90.2%和93.9%的準確率（宏觀F1分數分別為0.913和0.948），超越了SimpleCNN和標準EfficientNet-B0。此外，我們使用Grad-CAM++可視化技術來突出影響模型決策的特徵。結果顯示，這一準確且透明的框架是生育診所中自動化精子分析的實用工具。

##### **Multi-View Decompilation for LLM-Based Malware Classification**
2606.20436v1 by Bercan Turkmen, Vyas Raina

Malware analysts often inspect compiled binaries through decompiled pseudo-C, when source code is unavailable. Recent work suggests that large language models (LLMs) can assist this process by classifying decompiled code as benign or malicious, but existing pipelines typically rely on a single decompiler view. We argue that this assumption is fragile: decompilers are lossy heuristic tools, and different decompilers can expose different artefacts of the same binary. We curate a benchmark of benign utilities and malicious programs spanning a range of threat behaviors. Each sample is compiled and decompiled with both Ghidra and RetDec, yielding matched pseudo-C views. Across a range of LLMs from major model families, we find that providing both decompiler views improves malicious-class F1, mainly by increasing recall on malicious samples. Agreement analyses further show that Ghidra and RetDec make partially different errors, supporting the view that decompiler outputs provide complementary evidence. Our results suggest that multi-decompiler prompting is a simple, training-free way to improve LLM-based malware triage in practical settings.

摘要：惡意軟體分析師經常在無法獲得源代碼的情況下，通過反編譯的偽C代碼檢查編譯的二進制文件。最近的研究表明，大型語言模型（LLMs）可以通過將反編譯的代碼分類為良性或惡意來協助這一過程，但現有的流程通常依賴於單一的反編譯器視圖。我們認為這一假設是脆弱的：反編譯器是有損的啟發式工具，不同的反編譯器可以揭示相同二進制文件的不同特徵。我們策劃了一個涵蓋多種威脅行為的良性工具和惡意程序的基準。每個樣本都使用Ghidra和RetDec進行編譯和反編譯，產生匹配的偽C視圖。在來自主要模型系列的各種LLMs中，我們發現提供兩種反編譯器視圖能改善惡意類別的F1分數，主要是通過提高對惡意樣本的召回率。協議分析進一步顯示，Ghidra和RetDec會產生部分不同的錯誤，支持反編譯器輸出提供互補證據的觀點。我們的結果表明，多反編譯器提示是一種簡單且無需訓練的方式，可以在實際環境中改善基於LLM的惡意軟體篩選。

##### **LLM agent safety, multi-turn red-teaming, jailbreak benchmarks, adversarial robustness, safety-critical systems**
2606.20408v1 by Hanwool Lee, Dasol Choi, Bokyeong Kim, Seung Geun Kim, Haon Park

Large language model (LLM) agents are increasingly proposed as supervisory components for safety-critical systems, yet their robustness under sustained, adaptive adversarial pressure remains poorly characterized. We present NRT-Bench, a benchmark for multi-turn red-teaming of LLM agents acting as operators of a safety-critical system, instantiated in a simulated nuclear power plant control room. A five-role operator team, each backed by a configurable LLM, runs a plant governed by six critical safety functions (CSFs), while adversaries inject messages over four channels in bounded multi-turn sessions with per-turn feedback. Harm is an objective signal rather than LLM-judged text: a run terminates the moment any CSF is lost, attributed to the causing message. Evaluating four frontier operator models under a fixed-attack paired-replay protocol, we find that adaptive multi-turn attacks reliably push the operator team past a safety limit: across the four models, between 8.7% and 12.1% of attack sessions end with the plant losing a critical safety function. Although the four models look almost equally robust by this aggregate rate, their failures barely overlap: of $149$ sessions, none defeat all four models while a third defeat at least one, so vulnerabilities are nearly disjoint across models rather than nested. The effect of added defences is strongly model-dependent: the same guardrail stack or safety-advisor agent that lowers attack success for one model can raise it for another. We release the simulation venue, attack dataset, and replay tooling for reproducible safety evaluation of LLM agents.

摘要：大型語言模型（LLM）代理越來越多地被提議作為安全關鍵系統的監督組件，但在持續的、自適應的對抗壓力下，其穩健性仍然表現不佳。我們提出了 NRT-Bench，這是一個針對作為安全關鍵系統操作員的 LLM 代理進行多回合紅隊測試的基準，具體實現於模擬核電廠控制室中。一個由五個角色組成的操作員團隊，每個角色都由一個可配置的 LLM 支持，運行一個由六個關鍵安全功能（CSF）管理的工廠，而對手則在四個通道上注入消息，在有限的多回合會話中提供每回合的反饋。傷害是一個客觀信號，而不是 LLM 判斷的文本：一旦任何 CSF 喪失，運行將終止，並歸因於造成的消息。在固定攻擊配對重播協議下評估四個前沿操作員模型時，我們發現自適應多回合攻擊可靠地將操作員團隊推過安全限制：在這四個模型中，攻擊會話中有 8.7% 到 12.1% 最終導致工廠失去一個關鍵安全功能。儘管這四個模型在這個總體比率上看起來幾乎同樣穩健，但它們的失敗幾乎沒有重疊：在 $149$ 次會話中，沒有一個能擊敗所有四個模型，而三分之一至少擊敗一個，因此各模型之間的脆弱性幾乎是獨立的，而不是嵌套的。增加防禦的效果強烈依賴於模型：對一個模型降低攻擊成功率的相同護欄堆疊或安全顧問代理，對另一個模型卻可能提高攻擊成功率。我們釋放了模擬場地、攻擊數據集和重播工具，以便對 LLM 代理進行可重複的安全評估。

##### **DataMagic: Transforming Tabular Data into Data Insight Video**
2606.20388v1 by Yupeng Xie, Chen Ma, Zhenyang Wang, Liangwei Wang, Jiayi Zhu, Chuxuan Zeng, Zhouan Shen, Boyan Li, Yuyu Luo

Data videos integrate dynamic charts, voice narration, and synchronized animations to communicate data insights as temporal narratives, making them an effective medium for improving data consumption efficiency in the data management lifecycle. However, producing high-quality data videos requires expertise spanning data analysis, narrative design, and video production. Existing approaches fall short: static visualization tools (e.g., BI dashboards) lack narrative logic and animation; authoring tools require users to pre-prepare visualizations rather than working from raw data; pixel-level video generation models cannot guarantee data fidelity or provenance. We demonstrate DataMagic, an end-to-end interactive system that transforms raw tabular data and natural language queries into narrative data-insight videos. To ensure data fidelity, DataMagic introduces the declarative specification DVSpec, which binds visual and animation elements to underlying data fields through data-driven semantic references. To address the combinatorial explosion of the design space, DataMagic adopts a Generate-then-Orchestrate multi-agent architecture that generates candidate scenes in parallel and then optimizes narrative coherence through global orchestration. Leveraging DVSpec's decoupling of logic and rendering, the system further supports three interaction modes and structured provenance-based data Q&A, transforming one-way videos into explorable interactive data interfaces. Evaluation on 109 real-world samples validates the effectiveness of the DataMagic. Homepage: https://datamagic-home.github.io/

摘要：數據視頻整合了動態圖表、語音旁白和同步動畫，以時間敘事的方式傳達數據洞察，使其成為提高數據管理生命周期中數據消耗效率的有效媒介。 然而，製作高質量的數據視頻需要跨越數據分析、敘事設計和視頻製作的專業知識。 現有的方法存在不足：靜態可視化工具（例如，BI 儀表板）缺乏敘事邏輯和動畫；創作工具要求用戶預先準備可視化，而不是從原始數據開始；像素級視頻生成模型無法保證數據的真實性或來源。 我們展示了 DataMagic，一個端到端的互動系統，將原始表格數據和自然語言查詢轉換為敘事數據洞察視頻。 為了確保數據的真實性，DataMagic 引入了聲明性規範 DVSpec，通過數據驅動的語義引用將視覺和動畫元素綁定到基礎數據字段。 為了解決設計空間的組合爆炸，DataMagic 採用了生成-然後-協調的多代理架構，並行生成候選場景，然後通過全局協調優化敘事一致性。 利用 DVSpec 對邏輯和渲染的解耦，該系統進一步支持三種互動模式和基於結構來源的數據問答，將單向視頻轉變為可探索的互動數據界面。 在 109 個真實樣本上的評估驗證了 DataMagic 的有效性。 首頁：https://datamagic-home.github.io/

##### **Rethinking Shrinkage Bias in LLM FP4 Pretraining: Geometric Origin, Systemic Impact, and UFP4 Recipe**
2606.20381v1 by Qian Zhao, Kunlong Chen, Changxin Tian, Zhonghui Jiang, Haitao Zhang, Chaofan Yu, Peijie Jiang, Mingliang Gong, Jia Liu, Ziqi Liu, Zhiqiang Zhang, Jun Zhou

FP4 training promises substantial reductions in memory and computation cost for LLM pretraining, yet current FP4 hardware paths and recipes, including NVIDIA Blackwell/Rubin-class systems and AMD MI350-series GPUs, remain centered on E2M1 data elements. In this study, we identify a fundamental limitation of that choice: non-uniform formats such as E2M1 inherently suffer from Shrinkage Bias, a systematic negative rounding error caused by the geometric asymmetry of their representable bins. We show that this bias accumulates multiplicatively across layers and is amplified by the Random Hadamard Transform (RHT), providing a unified explanation for the training instability observed in existing E2M1-based FP4 recipes. In contrast, uniform grids (E1M2/INT4) bypass this grid-geometry error and better convert the improved bucket utilization from RHT into higher quantization quality. Based on this finding, we propose UFP4, a uniform 4-bit training recipe that applies RHT to all three training GEMMs while restricting stochastic rounding to dY alone. On Dense 1.5B, MoE 7.9B, and MoE 124B long-run pretraining, UFP4 consistently achieves lower BF16-relative loss degradation than strong E2M1-based baselines, supported by scaling-law analysis and ablation studies. Our results suggest that future accelerators should support E1M2/INT4-style uniform 4-bit grids as first-class training primitives alongside E2M1.

摘要：FP4 訓練承諾在 LLM 預訓練中顯著降低記憶體和計算成本，然而目前的 FP4 硬體路徑和配方，包括 NVIDIA Blackwell/Rubin 類系統和 AMD MI350 系列 GPU，仍然集中於 E2M1 數據元素。<lb/>在這項研究中，我們識別出這一選擇的根本限制：非均勻格式如 E2M1 本質上受到收縮偏差的影響，這是一種由其可表示的桶的幾何不對稱性引起的系統性負向四捨五入誤差。<lb/>我們展示了這種偏差在層之間以乘法方式累積，並且被隨機哈達瑪變換（RHT）放大，為現有基於 E2M1 的 FP4 配方中觀察到的訓練不穩定性提供了統一的解釋。<lb/>相比之下，均勻網格（E1M2/INT4）繞過了這種網格幾何誤差，並更好地將 RHT 中改善的桶利用率轉化為更高的量化質量。<lb/>基於這一發現，我們提出了 UFP4，一種均勻的 4 位訓練配方，將 RHT 應用於所有三個訓練 GEMM，同時將隨機四捨五入限制在 dY 上。<lb/>在 Dense 1.5B、MoE 7.9B 和 MoE 124B 的長期預訓練中，UFP4 一直實現了比強 E2M1 基準更低的 BF16 相對損失退化，這得到了縮放法則分析和消融研究的支持。<lb/>我們的結果表明，未來的加速器應該支持 E1M2/INT4 風格的均勻 4 位網格，作為 E2M1 的一級訓練原語。

##### **AutoPass: Evidence-Guided LLM Agents for Compiler Performance Tuning**
2606.20373v1 by Zepeng Li, Jie Ren, Zhanyong Tang, Jie Zheng, Zheng Wang

Large Language Models (LLMs) show promise for code compilation tasks, but applying them to runtime performance tuning is difficult due to complex microarchitectural effects and noisy runtime measurements. We present AutoPass, a multi-agent framework for compiler performance tuning that uses compiler and runtime evidence to guide LLM-generated optimization decisions. Rather than treating the compiler as a black box like prior auto-tuning schemes, AutoPass opens up the compiler to the LLM, enabling it to query compiler-internal optimization states and analyze the intermediate representation to orchestrate compiler options. The search process iteratively refines optimization configurations using measured runtime feedback to diagnose regressions and guide latency-improving edits. AutoPass operates in an inference-only, training-free setting and requires no offline training or task-specific fine-tuning, making it readily applicable to new benchmarks and platforms. We implement AutoPass on the LLVM compiler and evaluate it on server-grade x86-64 and embedded ARM64 systems. AutoPass outperforms expert-tuned heuristics and classical autotuning methods, achieving geometric-mean speedups of 1.043x and 1.117x over LLVM -O3 on x86-64 and ARM64, respectively.

摘要：大型語言模型（LLMs）在代碼編譯任務中顯示出潛力，但由於複雜的微架構效應和噪聲的運行時測量，將其應用於運行時性能調優是困難的。我們提出了AutoPass，一個多代理框架，用於編譯器性能調優，利用編譯器和運行時證據來指導LLM生成的優化決策。與以前的自動調優方案將編譯器視為黑箱不同，AutoPass向LLM開放編譯器，使其能夠查詢編譯器內部的優化狀態並分析中間表示，以協調編譯器選項。搜索過程利用測量的運行時反饋迭代地細化優化配置，以診斷回歸並指導改善延遲的編輯。AutoPass在僅推理的、無需訓練的環境中運行，並且不需要離線訓練或特定任務的微調，這使其能夠輕鬆應用於新的基準和平台。我們在LLVM編譯器上實現了AutoPass，並在伺服器級x86-64和嵌入式ARM64系統上進行了評估。AutoPass的性能超過了專家調優的啟發式方法和傳統自動調優方法，在x86-64和ARM64上分別實現了1.043倍和1.117倍的幾何平均加速，相較於LLVM -O3。

##### **CATCH-ME if you RAG: a dataset of Contextually Annotated multi-Turn Counterspeech against Hate and Misinformation Exchanges**
2606.20369v1 by Helena Bonaldi, Genoveffa Martone, Marco Guerini

Online hate speech and misinformation frequently overlap, yet NLP research has mainly treated them in isolation. While LLMs represent a scalable solution for assisting humans in the generation of counterspeech for both threats, zero-shot models frequently generate repetitive and vague responses, underscoring the need for high-quality examples to steer model generation. However, existing counterspeech datasets against the overlap of hate and misinformation are scarce and limited to single-turn English dialogues, while real-life interactions span across multiple turns and languages. To bridge this gap, we introduce the first large-scale, expert-curated, multilingual dataset of dialogues tackling the intersection of hate and misinformation. To ensure factual grounding, the dialogues are also anchored in verified external knowledge (i.e., fact-checking articles and NGO reports) and include document- and chunk-level span annotations, making it directly applicable for RAG systems. Covering five languages and targeting hate directed at seven marginalized groups, this novel resource enables the training and evaluation of more persuasive, factually grounded counterspeech models.

摘要：線上仇恨言論和錯誤資訊經常重疊，然而自然語言處理研究主要將它們孤立對待。雖然大型語言模型（LLMs）代表了一種可擴展的解決方案，幫助人類生成針對這兩種威脅的反駁言論，但零樣本模型經常生成重複且模糊的回應，突顯出需要高質量範例來引導模型生成。然而，現有針對仇恨和錯誤資訊重疊的反駁言論數據集稀缺，且僅限於單輪的英語對話，而現實生活中的互動則跨越多輪和多種語言。為了填補這一空白，我們介紹了第一個大規模、專家策劃的多語言對話數據集，專注於解決仇恨和錯誤資訊的交集。為了確保事實基礎，這些對話也基於經過驗證的外部知識（即事實查核文章和非政府組織報告），並包括文檔和片段級別的範圍標註，使其可直接應用於檢索增強生成（RAG）系統。這一新資源涵蓋五種語言，針對七個邊緣化群體的仇恨言論，使得訓練和評估更具說服力、事實基礎的反駁言論模型成為可能。

##### **Automating SKILL.md Generation for Computer-Using Agents via Interaction Trajectory Mining**
2606.20363v1 by Yuexing Hao, Xiaomin Li

Explicit skill libraries make computer-using agents easier to inspect, but it remains unclear whether such libraries can be mined from interaction data in a way that improves downstream policies. We study this question through a three-stage pipeline that segments GUI trajectories, clusters segments into candidate skills, and trains a skill-aware policy from the resulting annotations. The mined clusters are readable on the source benchmark: five of eight clusters have at least 0.95 purity against InteraSkill Workflows labels. However, readability does not imply transfer. GRPO improves IW skill-step accuracy only from 18.5\% to 20.5\%, leaves BrowseComp+ essentially unchanged, and underperforms trivial frequency priors on key source-domain metrics. We therefore present the method as a diagnostic study: trajectory mining can expose inspectable skill structure, but the current boundary detector, orderless segment representation, and offline reward model are insufficient for reliable cross-domain policy improvement.

摘要：明確的技能庫使得使用電腦的代理更容易檢查，但仍不清楚這些庫是否可以從互動數據中挖掘，以改善下游政策。我們通過一個三階段的流程來研究這個問題，該流程將GUI軌跡分段，將段落聚類為候選技能，並從結果註釋中訓練一個技能感知政策。挖掘出的聚類在源基準上是可讀的：八個聚類中有五個對InteraSkill工作流程標籤的純度至少為0.95。然而，可讀性並不意味著轉移。GRPO僅將IW技能步驟的準確性從18.5\%提高到20.5\%，對BrowseComp+幾乎沒有改變，並且在關鍵源域指標上表現不如簡單的頻率先驗。因此，我們將這個方法呈現為一項診斷研究：軌跡挖掘可以揭示可檢查的技能結構，但當前的邊界檢測器、無序段表示和離線獎勵模型不足以可靠地改善跨域政策。

##### **Robust $Q$-learning for mean-field control under Wasserstein uncertainty in common noise**
2606.20356v1 by Mathieu Laurière, Ariel Neufeld, Kyunghyun Park

In this article, we present a robust $Q$-learning algorithm for discrete-time mean-field control problems under Wasserstein uncertainty in the common noise law. The algorithm combines a quantization-and-projection scheme with a Wasserstein dual reformulation on the common-noise space. We establish its convergence together with finite-time iteration bounds for both synchronous and asynchronous learning schemes. Numerical experiments on systemic risk and epidemic models compare the asynchronous implementation with an idealized Bellman iteration, illustrate the robustness-performance tradeoff under common-noise misspecification, and report the observed convergence behavior of the asynchronous $Q$-learning algorithm.

摘要：在本文中，我們提出了一種針對在共同噪聲法則下的Wasserstein不確定性的離散時間均場控制問題的穩健$Q$-學習算法。<lb/>該算法將量化和投影方案與共同噪聲空間上的Wasserstein對偶重構相結合。<lb/>我們建立了其收斂性，並為同步和異步學習方案提供了有限時間迭代界限。<lb/>在系統風險和流行病模型上的數值實驗比較了異步實現與理想化的Bellman迭代，說明了在共同噪聲錯誤規範下的穩健性-性能折衷，並報告了觀察到的異步$Q$-學習算法的收斂行為。

##### **SoftSkill: Behavioral Compression for Contextual Adaptation**
2606.20333v1 by Xijia Tao, Yihua Teng, Xinyu Fu, Ziru Liu, Kecheng Chen, Yuzhi Zhao, Suiyun Zhang, Rui Liu, Lingpeng Kong

Agent skills are commonly deployed as natural-language Markdown files that encode answer policies, evidence-use habits, and task procedures. These files are readable and portable, but they are consumed indirectly: for each task instance, a frozen language model must translate a long textual artifact into generation-time behavior. This paper asks whether a natural-language skill can instead initialize a compact continuous context object, refined by a trainable soft delta while the base model remains frozen. We propose SoftSkill, a frozen-backbone method that tunes such soft skills with next-token prediction and deploys them as latent behavioral priors at inference time. In our main single-round setting, a length-32 SoftSkill prefix on Qwen3.5-4B improves over no-skill prompting by 8.3 points on SearchQA, 42.1 points on LiveMath, and 1.3 points on DocVQA. Relative to SkillOpt, SoftSkill improves accuracy by 5.2 points on SearchQA and 12.5 points on LiveMath, while replacing hundreds to thousands of Markdown skill tokens with a few virtual tokens. We further study agentic execution as a harder boundary case, where sparse trajectory imitation provides useful signal but does not yet robustly compress long-horizon procedural behavior. More broadly, the results suggest that some task skills are better treated not as additional Markdown to be reinterpreted at inference time, but as compact latent controls over how a frozen model enters the task.

摘要：代理技能通常以自然語言 Markdown 文件的形式部署，這些文件編碼了回答策略、證據使用習慣和任務程序。這些文件可讀且可攜帶，但它們是間接消耗的：對於每個任務實例，必須將一個凍結的語言模型翻譯為生成時行為的長文本工件。本文探討是否可以用自然語言技能來初始化一個緊湊的連續上下文對象，並在基礎模型保持凍結的同時，通過可訓練的軟增量進行精煉。我們提出了 SoftSkill，一種凍結骨幹的方法，通過下一個標記預測來調整這些軟技能，並在推理時將其作為潛在行為先驗進行部署。在我們的主要單輪設置中，對 Qwen3.5-4B 的長度為 32 的 SoftSkill 前綴在 SearchQA 上提高了 8.3 分，在 LiveMath 上提高了 42.1 分，在 DocVQA 上提高了 1.3 分。相對於 SkillOpt，SoftSkill 在 SearchQA 上提高了 5.2 分，在 LiveMath 上提高了 12.5 分，同時將數百到數千個 Markdown 技能標記替換為幾個虛擬標記。我們進一步研究代理執行作為一個更難的邊界案例，其中稀疏的軌跡模仿提供了有用的信號，但尚未穩健地壓縮長期程序行為。更廣泛地說，結果表明某些任務技能更適合被視為不是在推理時重新解釋的額外 Markdown，而是作為對凍結模型如何進入任務的緊湊潛在控制。

##### **Token-Operations-Oriented Inference Optimization Techniques for Large Models**
2606.20295v1 by Shiguo Lian, Kai Wang, Zhaoxiang Liu, Wen Liu, Minjie Hua, Yutong Liu, Jiangze Yan, Xin Wang, Cong Wang, Yilin Zhang, Yi Shen, Jieyun Huang, Fang Zhao, Huanlin Gao, Ping Chen, Xinyu Yang, Kaikai Zhao, Yao Zhao, Xinggang Wang, Huishuai Zhang, Dongyan Zhao, Junping Du, Tao Chen, Xiang Gao, Qinghuai Ma

Large model inference optimization serves as a key foundation for supporting the scalable, low-cost, and highly stable operation of large model services. Centered on token-oriented inference optimization technology, this paper proposes for the first time a four-layer technical architecture consisting of Multi-model Fusion, Model Optimization, Compute-Model Fusion, and Compute-Network-Model Fusion. It systematically reviews the key technologies and current industry status across these four levels and analyzes the application value of related technologies in real-world business scenarios. This paper provides a practical technical path for reducing token production costs, improving token service efficiency, ensuring the stability of token supply, and driving the transition of large model services from being merely callable to being operable.

摘要：大型模型推理優化是支持大型模型服務可擴展、低成本和高度穩定運行的關鍵基礎。<lb/>本論文首次提出以令牌為中心的推理優化技術，構建了一個由多模型融合、模型優化、計算模型融合和計算網絡模型融合組成的四層技術架構。<lb/>它系統地回顧了這四個層級的關鍵技術和當前行業狀況，並分析了相關技術在現實商業場景中的應用價值。<lb/>本論文為降低令牌生產成本、提高令牌服務效率、確保令牌供應的穩定性以及推動大型模型服務從可調用轉變為可操作提供了一條實用的技術路徑。

##### **PsyScore: A Psychometrically-Aware Framework for Trait-Adaptive Essay Scoring and ZPD-Scaffolded Feedback**
2606.20287v1 by Wei Xia, Jin Wu, Haoran Shi, Xiangyu Wang, Chanjin Zheng

Effective Automated Essay Scoring (AES) are expected to support both reliable assessment and actionable instructional feedback. However, existing approaches often treat scoring and feedback as separate components: neural scoring models provide limited interpretability, while Large Language Model (LLM)-based feedback is typically insensitive to learners proficiency levels. To address this fragmentation, this work proposes PsyScore, a psychometrically-aware framework that integrates diagnostic assessment with instructional scaffolding through a shared latent ability representation. PsyScore comprises three key modules: a Trait-Adaptive Neural IRT Scorer that incorporates the Graded Partial Credit Model (GPCM) into a neural architecture, enabling the precise estimation of student ability while maintaining psychometric interpretability, a ZPD-Scaffolded Feedback Generator, which conditions multi-agent feedback strategies on the diagnosed ability parameter to adapt instructional focus across different proficiency levels, and a Multi-Perspective Feedback Evaluation Strategy that assesses feedback quality via pairwise preference judgements and student revision simulations. Experiments on the ASAP++ dataset demonstrate that PsyScore achieves competitive scoring performance while providing more pedagogically aligned feedback.

摘要：有效的自動化論文評分（AES）預期能支持可靠的評估和可行的教學反饋。<lb/>然而，現有的方法通常將評分和反饋視為分開的組件：神經評分模型提供的可解釋性有限，而基於大型語言模型（LLM）的反饋通常對學習者的熟練程度不敏感。<lb/>為了解決這種碎片化問題，本研究提出了PsyScore，一個具有心理測量意識的框架，通過共享的潛在能力表示將診斷評估與教學支架整合在一起。<lb/>PsyScore包含三個關鍵模塊：一個特徵自適應神經IRT評分器，將分級部分信用模型（GPCM）納入神經架構中，能夠精確估計學生能力，同時保持心理測量的可解釋性；一個ZPD支架反饋生成器，根據診斷的能力參數調整多代理反饋策略，以適應不同熟練程度的教學重點；以及一個多角度反饋評估策略，通過成對偏好判斷和學生修訂模擬來評估反饋質量。<lb/>在ASAP++數據集上的實驗表明，PsyScore在提供更符合教學需求的反饋的同時，實現了具有競爭力的評分表現。

##### **Boundary Embedding Shaping with Adaptive Contrastive Learning for Graph Structural Disentanglement**
2606.20283v1 by Jiaqing Chen, Zidu Yin, Yichao Cai, Yuhang Liu, Zhen Zhang, Dong Gong, Javen Qinfeng Shi

Graph neural networks (GNNs) excel at aggregating neighbor information for classification, yet their performance is hindered by graph structural entanglement, where spurious correlations from semantically irrelevant neighbors contaminate node embeddings. This challenge is most acute for nodes near class boundaries in the embedding space, where amplified structural noise blurs decision boundaries and destabilizes predictions. Existing robust GNN methods largely treat all nodes uniformly, ignoring boundary vulnerabilities. In this paper, to improve classification performance, we tackle graph structural disentanglement by identifying boundary-region entanglement as the primary bottleneck and propose Boundary Embedding Shaping (BES), an adaptive contrastive learning GNN plug-in module that selectively suppresses spurious structural noise at decision boundaries with minimal model parameter perturbation. Extensive experiments demonstrate that BES consistently improves boundary discrimination and outperforms existing leading methods. Notably, BES boosts GCN performance by an average of 3.3% in node classification (up to 5.0% on WikiCS) and achieves superior accuracy in link prediction.

摘要：圖神經網絡（GNNs）在聚合鄰居信息以進行分類方面表現出色，但其性能受到圖結構糾纏的阻礙，這種情況下，來自語義上不相關鄰居的虛假相關性會污染節點嵌入。這一挑戰在嵌入空間中接近類別邊界的節點上最為明顯，增強的結構噪聲模糊了決策邊界並使預測不穩定。現有的穩健GNN方法大多將所有節點視為均勻，忽略了邊界的脆弱性。本文為了提高分類性能，通過將邊界區域糾纏識別為主要瓶頸，來解決圖結構解糾纏問題，並提出了邊界嵌入塑形（BES），這是一個自適應對比學習GNN插件模塊，能夠在決策邊界上選擇性地抑制虛假結構噪聲，並且對模型參數的擾動最小。大量實驗表明，BES持續改善邊界區分能力，並超越現有的領先方法。值得注意的是，BES在節點分類中平均提升了GCN性能3.3%（在WikiCS上最高可達5.0%），並在鏈接預測中實現了更高的準確性。

##### **ELVA: Exploring Ranking-Driven Universal Multimodal Retrieval**
2606.20280v1 by Yuhan Liu, Pei Fu, Hang Li, Yukun Qi, Chao Jiang, Jingwen Fu, Zhen Liu, Bin Qin, Zhenbo Luo, Jian Luan, Jingmin Xin

Leveraging Multimodal Large Language Models (MLLMs) via contrastive learning has become a mainstream paradigm for improving the performance of Universal Multimodal Retrieval (UMR). However, previous works have ignored the grain blindness when adapting the contrastive paradigm into retrieval tasks. Grain blindness refers to the tendency of the model to overlook grain-level information contained in the query, which is crucial for effectively handling complex queries. This stems from contrastive learning treating samples as a binary classification (positive/negative), while ignoring the different information carried by each negative sample. To address this, we argue that negatives should be treated differently according to their similarity to the positive sample, enabling the model to learn distinct grain information from each negative. In this paper, we introduce a simple but effective framework, called ELVA, a novel rule-based RL framework that mitigates grain blindness through ranking-driven MLLMs. 1) Instead of relying on reward models, we extend Reinforcement Learning with Verifiable Rewards
  (RLVR) to retrieval tasks, allowing the model to explore new ranking behaviors without explicit ranking labels. 2) By utilizing rule-based rewards, our approach jointly optimizes the ranking of negative samples while enlarging the similarity gap between positive and negative. To more precisely measure grain blindness, we further introduce MRBench, a new benchmark specifically designed for multi-grain query scenarios. ELVA achieves state-of-the-art results across standard retrieval benchmarks, and its notable 13.1% improvement on MRBench further demonstrates its effectiveness in alleviating grain blindness.

摘要：利用對比學習來發揮多模態大型語言模型（MLLMs）的潛力，已成為提升通用多模態檢索（UMR）性能的主流範式。<lb/>然而，之前的研究在將對比範式應用於檢索任務時忽略了顆粒盲點。<lb/>顆粒盲點是指模型忽視查詢中包含的顆粒級信息的傾向，而這對於有效處理複雜查詢至關重要。<lb/>這源於對比學習將樣本視為二元分類（正樣本/負樣本），而忽略了每個負樣本所攜帶的不同信息。<lb/>為了解決這個問題，我們主張應根據負樣本與正樣本的相似性來區分對待負樣本，使模型能夠從每個負樣本中學習到不同的顆粒信息。<lb/>在本文中，我們介紹了一個簡單但有效的框架，稱為ELVA，這是一個新穎的基於規則的強化學習框架，通過基於排名的MLLMs來減輕顆粒盲點。<lb/>1) 我們不依賴獎勵模型，而是將可驗證獎勵的強化學習（RLVR）擴展到檢索任務，允許模型在沒有明確排名標籤的情況下探索新的排名行為。<lb/>2) 通過利用基於規則的獎勵，我們的方法共同優化負樣本的排名，同時擴大正樣本和負樣本之間的相似性差距。<lb/>為了更精確地測量顆粒盲點，我們進一步介紹了MRBench，一個專門為多顆粒查詢場景設計的新基準。<lb/>ELVA在標準檢索基準上達到了最先進的結果，其在MRBench上顯著提高了13.1%的性能，進一步證明了其在減輕顆粒盲點方面的有效性。

##### **Lagrange: An Open-Vocabulary, Energy-Based Sparse Framework for Generalized End-to-End Driving**
2606.20274v1 by Shihao Ji, HongXi Li, Zihui Song, Mingyu Li

Scaling end-to-end autonomous driving to complex, open-world environments requires perceptual models that generalize to anomalous scenarios and planners that produce kinematically valid trajectories. Existing paradigms face a distinct dichotomy between representational efficiency and generalization capacity. Dense models (e.g., occupancy networks), while geometrically robust, incur critical computational bottlenecks and struggle with high-level semantic reasoning. Conversely, sparse, query-based planners are efficient but reliant on closed-set definitions, rendering them vulnerable to out-of-distribution (OOD) events. Although recent Vision-Language-Action (VLA) models offer open-vocabulary reasoning, their autoregressive, discrete token generation fundamentally conflicts with the continuous, high-frequency control requirements of vehicle dynamics. To address this, we propose Lagrange, an open-vocabulary, computationally sparse driving framework based on Masked Latent Fields (MLF). Rather than relying on dense volumetric reconstructions or closed-set query mechanisms, Lagrange exploits Vision-Language Models (VLMs) to encode class-agnostic object proposals into continuous semantic visual tokens. We introduce an intent-driven masked cross-attention module that temporally filters irrelevant entities, decoding the attended tokens into an implicit continuous energy field defined over spatial coordinates. By framing decision-making as a Lagrangian action minimization problem spanning this energy field, we enforce strict compliance with vehicle kinematics while executing collision avoidance. Extensive offline evaluations on both standard (nuScenes) and long-tail (CODA) benchmarks demonstrate that Lagrange establishes a promising framework for robust, interpretable, and kinematically feasible open-world autonomy.

摘要：擴展端到端自主駕駛至複雜的開放世界環境需要能夠對異常情境進行概括的感知模型和能產生運動學有效軌跡的規劃器。現有的範式面臨著表徵效率和概括能力之間的明顯二分法。雖然密集模型（例如，佔用網絡）在幾何上穩健，但會產生關鍵的計算瓶頸，並且在高層次語義推理方面表現不佳。相反，稀疏的基於查詢的規劃器雖然高效，但依賴於封閉集定義，使其容易受到分佈外（OOD）事件的影響。儘管最近的視覺-語言-行動（VLA）模型提供了開放詞彙的推理，但其自回歸的離散標記生成與車輛動力學的連續高頻控制需求根本上存在衝突。為了解決這個問題，我們提出了Lagrange，一種基於遮罩潛在場（MLF）的開放詞彙、計算稀疏的駕駛框架。Lagrange並不依賴於密集的體積重建或封閉集查詢機制，而是利用視覺-語言模型（VLMs）將類別無關的物體提議編碼為連續的語義視覺標記。我們引入了一個基於意圖的遮罩交叉注意模塊，該模塊在時間上過濾不相關的實體，將關注的標記解碼為定義在空間坐標上的隱式連續能量場。通過將決策制定框架化為跨越這一能量場的拉格朗日動作最小化問題，我們在執行碰撞避免的同時強制遵守車輛運動學的嚴格合規性。在標準（nuScenes）和長尾（CODA）基準上的廣泛離線評估表明，Lagrange建立了一個有前景的框架，用於穩健、可解釋且運動學上可行的開放世界自主駕駛。

##### **Confidence-Aware Automated Assessment of Student-Drawn Scientific Models**
2606.20264v1 by Luyang Fang, Yingchuan Zhang, Jongchan Park, Zhaoji Wang, Ping Ma, Xiaoming Zhai

Student-generated drawings are widely used in science education to assess learners' conceptual understanding in modeling-based tasks aligned with the Next Generation Science Standards (NGSS). However, scoring such drawings requires expert human judgment to interpret complex visual representations, making large-scale assessment costly to implement and sustain in classroom settings. In this work, we study automated scoring of student-generated scientific drawings using a vision-based model. We evaluate a Vision Transformer (ViT) with parameter-efficient adaptation and propose a confidence-aware scoring framework that derives response-level confidence from test-time predictive distributions. This confidence signal enables selective automation by scoring high-confidence responses automatically while deferring uncertain cases for human review. Experiments on six NGSS-aligned middle school assessment items show that the proposed approach improves scoring reliability while supporting a practical trade-off between automated coverage and scoring risk, highlighting the value of confidence-aware methods for trustworthy educational assessment.

摘要：學生生成的繪圖在科學教育中被廣泛用於評估學習者在與下一代科學標準（NGSS）對齊的建模任務中的概念理解。<lb/>然而，對這些繪圖進行評分需要專家的人工判斷來解釋複雜的視覺表現，這使得在課堂環境中實施和維持大規模評估變得成本高昂。<lb/>在這項工作中，我們研究了使用基於視覺的模型對學生生成的科學繪圖進行自動評分。<lb/>我們評估了一種具有參數高效適應的視覺Transformer（ViT），並提出了一個基於信心的評分框架，該框架從測試時的預測分佈中推導出反應級別的信心。<lb/>這種信心信號使得選擇性自動化成為可能，通過自動評分高信心的反應，同時將不確定的案例推遲給人類審查。<lb/>在六個與NGSS對齊的中學評估項目上的實驗顯示，所提出的方法提高了評分的可靠性，同時支持自動覆蓋和評分風險之間的實際權衡，突顯了基於信心的方法在可信教育評估中的價值。

##### **Editorial Alignment: A Participatory Approach to Engaging Editorial Expertise in LLM-mediated Knowledge Dissemination**
2606.20258v1 by Simon Aagaard Enni, Malthe Stavning Erslev, Karl-Emil Kjær Bilstrup, Kristoffer Laigaard Nielbo

The emergence of LLM-driven information services is reshaping the conditions under which public knowledge institutions operate, threatening to absorb the editorial function these institutions exist to exercise. While LLMs offer powerful new affordances for knowledge dissemination, editorial authority is challenged by pretrained LLMs that arrive already aligned with the values and dissemination strategies of their commercial developers. This paper investigates editor participation in re-aligning LLM interfaces to editorial standards through design workshops, in a case study where we design and implement an LLM-enabled encyclopedia interface with a Nordic public knowledge institution. We introduce editorial alignment as a design practice within Participatory AI, framing AI alignment as a design process and positioning the editorial standard as a design artefact that translates editorial practice and values into alignment objectives for technical implementation. Last, we discuss how editorial alignment can create space for ongoing participation and give editors agency in LLM-mediated knowledge dissemination.

摘要：LLM 驅動的信息服務的出現正在重塑公共知識機構運作的條件，威脅到這些機構存在的編輯功能。雖然 LLM 提供了強大的新機會來促進知識的傳播，但預訓練的 LLM 挑戰了編輯權威，因為它們已經與其商業開發者的價值觀和傳播策略對齊。本文通過設計工作坊調查編輯參與重新調整 LLM 介面以符合編輯標準的情況，這是一個案例研究，我們在一個北歐公共知識機構設計並實施了一個 LLM 支持的百科全書介面。我們將編輯對齊引入作為參與式 AI 中的一種設計實踐，將 AI 對齊框架視為一個設計過程，並將編輯標準定位為一種設計工件，將編輯實踐和價值觀轉化為技術實施的對齊目標。最後，我們討論了編輯對齊如何為持續參與創造空間，並賦予編輯在 LLM 媒介知識傳播中的主動權。

##### **The Register Gap: A Meaning Intelligence Framework for Nigerian Public Discourse**
2606.20255v1 by Celestine Achi

We introduce the Meaning Intelligence Framework (MIF), a nine-dimension annotation and evaluation schema for Nigerian public discourse that separates surface sentiment from true communicative intent. Existing benchmarks for Nigerian languages, including NaijaSenti and AfriSenti, treat sentiment classification as a three-way polarity task (positive, negative, neutral). We argue that the dominant failure mode of AI systems on Nigerian discourse is not translation failure but context failure: the same utterance carries opposite pragmatic force depending on speaker, audience, and situation. The MIF operationalises this insight across nine scored dimensions: register, surface sentiment, true intent, irony, coded subtext, risk tier, annotator confidence, speaker emotion, and recommended communications action. We construct a 30-item calibration dataset spanning Standard English, Nigerian English, Nigerian Pidgin, and code-mixed registers, and evaluate a frontier language model (Gemini 2.5 Flash) under zero-shot and schema-informed prompting conditions. The headline finding is the Register Gap: zero-shot register classification accuracy is 33.3%, rising to 73.3% (+40 points) when the model receives the MIF schema in-context. The composite Meaning Intelligence Score increases by 5.4 points (73.2 to 78.6) under schema-informed prompting, with the largest practical gains in register identification, coded-subtext detection (+10 points), and strategic action recommendation (+10.3 points). We release the framework specification, annotation guidelines, and the 30-item public calibration set to support reproducibility, while retaining a private holdout corpus for contamination-protected evaluation.

摘要：我們介紹了意義智能框架（MIF），這是一個九維的註釋和評估架構，用於尼日利亞公共話語，將表面情感與真實的交流意圖分開。現有的尼日利亞語言基準，包括NaijaSenti和AfriSenti，將情感分類視為三向極性任務（正面、負面、中立）。我們認為，AI系統在尼日利亞話語中的主要失效模式不是翻譯失敗，而是上下文失敗：同一句話根據說話者、聽眾和情境的不同，承載著相反的語用力量。MIF在九個計分維度上將這一見解具體化：註冊、表面情感、真實意圖、諷刺、編碼潛台詞、風險層級、註釋者信心、說話者情感和建議的交流行動。我們構建了一個包含30項的標定數據集，涵蓋標準英語、尼日利亞英語、尼日利亞皮欽語和混合語言註冊，並在零樣本和基於架構的提示條件下評估了一個前沿語言模型（Gemini 2.5 Flash）。主要發現是註冊差距：零樣本註冊分類的準確率為33.3%，當模型在上下文中接收MIF架構時，準確率上升至73.3%（+40個點）。在基於架構的提示下，綜合意義智能分數增加了5.4分（從73.2到78.6），在註冊識別、編碼潛台詞檢測（+10分）和戰略行動建議（+10.3分）方面取得了最大的實際增益。我們發布了框架規範、註釋指南和30項公共標定集，以支持可重複性，同時保留私有的保留語料庫以進行受污染保護的評估。

##### **Finetuning Vision-Language-Action Models Requires Fewer Layers Than You Think**
2606.20246v1 by Gia-Binh Nguyen, Trong-Bao Ho, Thien-Loc Ha, Khoa Vo, Philip Lund Møller, Quang T. Nguyen, Long Dinh, Tuan Dam, Vu Duong, Tung M. Luu, Trung Le, Tran Nguyen Le, Minh Vu, An Thai Le, Ngan Le, Daniel Sonntag, James Zou, Jan Peters, Duy M. H. Nguyen, Ngo Anh Vien

Vision-Language-Action (VLA) models pre-trained on massive video-robot datasets have revolutionized robotic manipulation, yet their multi-billion parameter architectures impose prohibitive computational burdens during downstream fine-tuning and real-time inference. In this work, we reveal a highly non-trivial architectural characteristic of these continuous control foundation policies (e.g., pi_0, GR00T-N1.5): despite being trained on diverse physical trajectories, they exhibit severe layer-wise representational redundancy. To exploit this, we introduce a structural compression pipeline that is entirely training-free, bypassing the need of existing methods to load full-scale models to learn optimized token reductions or dynamic layer selectors. Instead, using only a single forward pass via Centered Kernel Alignment to identify redundant layer features, we remove twin layers to permanently compress the model depth by up to 50% across both the VLM backbone and the continuous control policy head. Downstream fine-tuning of this streamlined architecture yields a dual acceleration benefit: a 40-50% reduction in training time and up to 30% faster real-time inference, while matching or exceeding full-scale base model performance. We comprehensively validate our method across three simulation benchmarks (LIBERO, RoboCasa, SimplerEnv) and 10 diverse real-world manipulation tasks across 4 unique robotic embodiments. These results prove that advanced VLAs require significantly fewer layers than previously assumed, offering a highly compute-efficient paradigm for scalable robot learning.

摘要：視覺-語言-行動（VLA）模型在龐大的視頻-機器人數據集上進行預訓練，徹底改變了機器人操作，然而它們的數十億參數架構在下游微調和實時推斷過程中帶來了高昂的計算負擔。<lb/>在本研究中，我們揭示了這些連續控制基礎策略（例如，pi_0，GR00T-N1.5）的一個高度非平凡的架構特徵：儘管在多樣的物理軌跡上進行訓練，但它們在層級表示上存在嚴重的冗餘。<lb/>為了利用這一點，我們提出了一種完全無需訓練的結構壓縮管道，繞過現有方法需要加載全尺度模型以學習優化的標記減少或動態層選擇器的需求。<lb/>相反，僅使用一次前向傳播通過中心核對齊來識別冗餘層特徵，我們移除了雙層，將模型深度永久壓縮最多50%，適用於VLM主幹和連續控制策略頭。<lb/>這種精簡架構的下游微調帶來了雙重加速效益：訓練時間減少40-50%，實時推斷速度提高最多30%，同時匹配或超越全尺度基準模型的性能。<lb/>我們在三個模擬基準（LIBERO、RoboCasa、SimplerEnv）和四種獨特機器人實體的10個多樣化現實世界操作任務中全面驗證了我們的方法。<lb/>這些結果證明，先進的VLA需要的層數顯著少於先前的假設，為可擴展的機器人學習提供了一個高計算效率的範式。

##### **Navigating Unreliable Parametric and Contextual Knowledge: Explicit Knowledge Conflict Resolution for LLM Inference**
2606.20245v1 by Huang Peng, Jiuyang Tang, Weixin Zeng, Hao Xu, Xiang Zhao

Large language models (LLMs) have achieved strong performance across a wide range of language-based tasks by leveraging both extensive parametric knowledge and in-context learning ability, enabling them to incorporate external information provided in the input prompt. However, the integration of external knowledge can introduce conflicts, not only between the model's internal parametric knowledge and the external information, but also among multiple pieces of external contexts. Existing approaches typically assume that either the model or the provided context is reliable, overlooking the possibility that both sources may contain errors, and avoid conflicts by privileging one source over the other, rather than actively resolving inconsistencies. To address these limitations, we propose a novel framework MACR for LLM knowledge conflict resolution that moves beyond the conventional binary choice paradigm and incorporates an explicit conflict-resolution mechanism based on a multi-agent reasoning approach. Specifically, we first propose an adaptive knowledge assessment and retrieval approach that employs a modified semantic entropy measure to quantify an LLM's confidence in its answer to a given query. Based on this confidence estimation, MACR either externalizes the model's internal knowledge as textual representations or retrieves relevant external knowledge when internal knowledge is insufficient, generating basic contexts for subsequent reasoning. Then we introduce an inductive multi-agent reasoning framework with three specialized agents that, respectively, induce explicit rules, analyze potential conflicts, and resolve inconsistencies across all available contexts. Empirical results demonstrate that MACR significantly outperforms state-of-the-art baselines across benchmarks, while also providing interpretable resolutions of explicit conflicts.

摘要：大型語言模型（LLMs）透過利用廣泛的參數知識和上下文學習能力，在各種基於語言的任務中取得了強勁的表現，使其能夠納入輸入提示中提供的外部信息。<lb/>然而，外部知識的整合可能會引入衝突，不僅在模型的內部參數知識與外部信息之間，還在多個外部上下文之間。<lb/>現有的方法通常假設模型或提供的上下文是可靠的，忽視了兩個來源都可能包含錯誤的可能性，並通過優先考慮一個來源而不是另一個來源來避免衝突，而不是主動解決不一致性。<lb/>為了解決這些限制，我們提出了一個新框架 MACR，用於 LLM 知識衝突解決，超越傳統的二元選擇範式，並基於多代理推理方法納入明確的衝突解決機制。<lb/>具體而言，我們首先提出了一種自適應知識評估和檢索方法，該方法使用修改過的語義熵度量來量化 LLM 對給定查詢答案的信心。<lb/>根據這一信心評估，MACR 要麼將模型的內部知識外部化為文本表示，要麼在內部知識不足時檢索相關的外部知識，生成後續推理的基本上下文。<lb/>然後，我們引入了一個歸納多代理推理框架，該框架擁有三個專門的代理，分別引導明確規則、分析潛在衝突並解決所有可用上下文中的不一致性。<lb/>實證結果表明，MACR 在基準測試中顯著超越了最先進的基準，同時也提供了明確衝突的可解釋解決方案。

##### **SPOT-E: Test-Time Entropy Shaping with Visual Spotlights for Frozen VLMs**
2606.20244v1 by Bo Yin, Xiaobin Hu, Chengming Xu, Ruolin Shen, Mo Yang, Jiangning Zhang, Peng-Tao Jiang, Cheng Tan, Shuicheng YAN

Vision-language models (VLMs) often underperform on evidence intensive tasks because decisive visual evidence are small, localized, and easy to overlook, leading to failures in evidence readout even when high-level reasoning is intact. Prior inference-time visual interventions can improve grounding without retraining, but they are largely open-loop and lack a mechanism to verify whether highlighted evidence is actually used. We study answer-span prediction entropy as a model-internal feedback signal and show that naive entropy minimization is ambiguous, since low entropy may arise from evidence-grounded confidence or shortcut collapse. To resolve this ambiguity, we introduce low-entropy anchors and an entropy-shaping objective that reduces answer uncertainty while preserving baseline high-confidence tokens. We instantiate this principle in SPOT-E, a plug-and-play test-time method that produces question-conditioned spotlights, optimized per instance via light-weight tuning based on Group Relative Policy Optimization (GRPO). Across all benchmarks and different VLM families, SPOT-E yields consistent gains and improved robustness under visual corruptions. Code is publicly available at: \url{https://github.com/YinBo0927/SPOT-E}

摘要：視覺語言模型（VLMs）在證據密集型任務上往往表現不佳，因為決定性的視覺證據通常是小型的、局部的，且容易被忽視，這導致即使高層次推理完好，仍然會在證據讀取上失敗。先前的推理時視覺干預可以在不重新訓練的情況下改善基礎，但它們大多是開環的，缺乏驗證突出證據是否實際被使用的機制。我們研究答案範圍預測熵作為模型內部的反饋信號，並顯示天真的熵最小化是模糊的，因為低熵可能源於基於證據的信心或捷徑崩潰。為了解決這一模糊性，我們引入低熵錨點和一個熵塑形目標，該目標在保留基線高信心標記的同時減少答案的不確定性。我們在SPOT-E中具體實現了這一原則，這是一種即插即用的測試時方法，通過基於群體相對策略優化（GRPO）的輕量調整，為每個實例優化生成問題條件的聚光燈。在所有基準測試和不同的VLM家族中，SPOT-E都產生了一致的增益並在視覺損壞下提高了穩健性。代碼已公開可用於： \url{https://github.com/YinBo0927/SPOT-E}

##### **ScholarQuest: A Taxonomy-Guided Benchmark for Agentic Academic Paper Search in Open Literature Environments**
2606.20235v1 by Tingyue Pan, Mingyue Cheng, Daoyu Wang, Yitong Zhou, Jie Ouyang, Qi Liu, Enhong Chen

Academic paper search is a core step in scientific research, and LLM-based search agents are emerging as a promising paradigm for iterative, intent-driven literature exploration. However, existing benchmarks are insufficient for systematically evaluating agentic academic search under realistic open literature environments. We propose ScholarQuest, a large-scale, taxonomy-guided benchmark for agentic academic paper search. ScholarQuest is constructed from over 1,000 computer science topics and four representative research intents, including method-oriented, setting-anchored, comparison-based, and scope-controlled queries. It further provides scalable answer construction and a shared retrieval backend ScholarBase for reproducible evaluation. Benchmarking results show that agentic methods outperform single-shot retrieval baselines, yet the best-performing agent only achieves 0.314 Recall@100 and 0.355 Recall@All, indicating substantial room for improvement. In addition, analyses of search efficiency, intent-level robustness, and failure cases further highlight the benchmark's ability to provide multi-dimensional evaluation signals for academic paper search agents.

摘要：學術論文搜索是科學研究中的核心步驟，而基於大型語言模型的搜索代理正在成為一種有前景的範式，用於迭代的、以意圖驅動的文獻探索。<lb/>然而，現有的基準對於在現實開放文獻環境下系統性評估代理學術搜索來說是不夠的。<lb/>我們提出了 ScholarQuest，一個大規模的、以分類法指導的代理學術論文搜索基準。<lb/>ScholarQuest 是由超過 1,000 個計算機科學主題和四個代表性的研究意圖構建的，包括以方法為導向、以設定為基礎、基於比較和範圍控制的查詢。<lb/>它進一步提供可擴展的答案構建和一個共享檢索後端 ScholarBase，以便進行可重複的評估。<lb/>基準測試結果顯示，代理方法的表現優於單次檢索基準，但表現最佳的代理僅達到 0.314 Recall@100 和 0.355 Recall@All，顯示出相當大的改進空間。<lb/>此外，對搜索效率、意圖級穩健性和失敗案例的分析進一步突顯了該基準提供多維評估信號以供學術論文搜索代理使用的能力。

##### **Thermodynamic Measure of Intelligence**
2606.20231v1 by Ishanu Chattopadhyay

Can intelligence be measured? We propose that intelligence can be defined as the lawful amplification of rare but valid futures: a system increases the probability of outcomes that would be unlikely under passive dynamics but remain admissible under the constraints of the domain. We start with the premise that an intelligent system must model the world and its own place within it. Because the system is part of the world it models, this leads naturally to recursive self-simulation: the system represents futures in which its own actions are part of the trajectory. Our central results give a necessity statement and a conditional near-sufficiency statement connecting this architecture to a precise thermodynamic measure of lawful amplification of rare-valid futures: high rare-valid lift is impossible unless the internal simulation identifies rare-valid futures with high fidelity; conversely, when rare-valid fidelity is high and the simulation contains an effective policy, the achievable lift approaches the actuation-limited optimum. Thus recursive self-simulation is not merely a plausible feature of intelligence but, under the stated assumptions, is necessary and nearly sufficient for high thermodynamic intelligence. The resulting framework makes intelligence measurable on a universal scale, from passive matter and feedback controllers, large language models, and humans as text generators to Maxwell-demon-like information engines.

摘要：智慧能否被測量？我們提出智慧可以被定義為合法放大稀有但有效的未來：一個系統增加在被動動力學下不太可能出現的結果的概率，但在領域的限制下仍然是可接受的。我們從一個前提出發，即智能系統必須對世界及其在其中的位置進行建模。因為該系統是它所建模的世界的一部分，這自然導致了遞歸自我模擬：該系統代表未來，其中它自己的行動是軌跡的一部分。我們的核心結果給出了一個必要性陳述和一個條件近充分性陳述，將這一架構與合法放大稀有有效未來的精確熱力學測量聯繫起來：除非內部模擬以高保真度識別稀有有效的未來，否則高稀有有效的提升是不可能的；相反，當稀有有效的保真度高且模擬包含有效策略時，可達到的提升接近於激活限制的最佳值。因此，遞歸自我模擬不僅僅是智慧的一個合理特徵，而是在所述假設下，對於高熱力學智慧是必要的和幾乎充分的。由此產生的框架使智慧在普遍尺度上可測量，從被動物質和反饋控制器、大型語言模型到作為文本生成器的人類以及類麥克斯韋妖的信息引擎。

##### **QMFOL: Benchmarking Large Language Model Reasoning via Quantifiable Monadic First-Order Logic Test Case Generation**
2606.20227v1 by Xinyi Zheng, Ling Shi, Tianlong Yu, Yongxin Zhao, Lorenz Goette, Kailong Wang

Large Language Models (LLMs) have made significant progress in reasoning, particularly in deductive reasoning, which is crucial for high-stakes decision-making. As models improve, evaluation benchmarks should evolve to keep pace. However, existing benchmarks lack fine-grained control over logical complexity and struggle to balance semantic diversity with logical consistency.
  To address these issues, we propose QMFOL, an automated framework for generating monadic first-order logic reasoning tasks with quantifiable and controllable complexity. It constructs formal logical structures using conjunction and disjunction patterns, enabling precise control over reasoning depth, width, label types, and distractors. These structures are then translated into natural language via LLMs, with logical consistency ensured through round-trip verification using an external prover. Based on our framework, we build QMFOLBench, a benchmark comprising 2880 instances with 960 configurations across diverse logical and semantic dimensions. Evaluations on six large reasoning models (LRMs) and two LLMs show that performance degrades and computational overhead increases with rising logical complexity. Models perform better on True-labeled tasks than on False or Unknown ones, and exhibit sensitivity to semantic variation. Overall, QMFOL offers a scalable and reliable approach for constructing deductive reasoning benchmarks with controllable complexity, enabling more precise evaluation of reasoning capabilities in modern language models.

摘要：大型語言模型（LLMs）在推理方面取得了重大進展，特別是在演繹推理上，這對於高風險決策至關重要。隨著模型的改進，評估基準應該隨之演變以保持步伐。然而，現有的基準在邏輯複雜性方面缺乏細緻的控制，並且在語義多樣性與邏輯一致性之間難以取得平衡。<lb/>為了解決這些問題，我們提出了QMFOL，一個自動化框架，用於生成具有可量化和可控複雜度的單子第一階邏輯推理任務。它使用合取和析取模式構建正式的邏輯結構，使得對推理深度、寬度、標籤類型和干擾項的精確控制成為可能。這些結構然後通過LLMs轉換為自然語言，並通過使用外部證明者進行往返驗證來確保邏輯一致性。基於我們的框架，我們構建了QMFOLBench，一個包含2880個實例的基準，涵蓋960個配置，涉及多樣的邏輯和語義維度。對六個大型推理模型（LRMs）和兩個LLMs的評估顯示，隨著邏輯複雜性的增加，性能下降且計算開銷增加。模型在標記為真（True）的任務上表現優於標記為假（False）或未知（Unknown）的任務，並對語義變化表現出敏感性。總體而言，QMFOL提供了一種可擴展且可靠的方法，用於構建具有可控複雜度的演繹推理基準，使現代語言模型的推理能力評估更加精確。

##### **Actionable Activation Directions for Detecting and Mitigating Emergent Misalignment Across Language Model Families**
2606.20225v1 by Abdul Rafay Syed

Fine-tuning language models on insecure code induces emergent misalignment with poorly understood internal structure. We investigate whether this misalignment corresponds to a causally actionable activation-space direction shared across architectures. Across four instruction-tuned model families (Qwen2.5-1.5B, Gemma-2-2B, Llama-3.2-1B, Ministral-3-3B) finetuned identically, a difference-in-means direction achieves 99.6% separation of aligned and misaligned activations at each model's final layer. Causal steering by subtracting this direction reduces code spillover by 21-51 points, while a secure-code control confirms content specificity. Cross-architecture transfer via ridge regression maps yields large behavioral suppression (up to 46 points) but fails specificity controls as random and orthogonal directions perform comparably. We identify a two-tier specificity structure: within-model directions are causally specific and actionable; cross-model directions are causally real but non-specific. An asymmetric transfer topology emerges, with Gemma and Qwen acting as geometric donors and Llama as a receiver. These findings define the limits of linear cross-architecture correction and recommend within-model probing for auditing.

摘要：微調語言模型在不安全的代碼上會引發與內部結構不佳理解的突現不對齊。我們調查這種不對齊是否對應於跨架構共享的因果可操作激活空間方向。在四個指令調整的模型家族（Qwen2.5-1.5B、Gemma-2-2B、Llama-3.2-1B、Ministral-3-3B）中，經過相同微調的情況下，均值差異方向在每個模型的最終層達到99.6%的對齊和不對齊激活的分離。通過減去這一方向的因果引導將代碼溢出減少了21-51個點，而安全代碼控制則確認了內容的特異性。通過脊回歸映射的跨架構轉移產生了大幅行為抑制（高達46個點），但在特異性控制方面失敗，因為隨機和正交方向的表現相似。我們確定了一種雙層特異性結構：模型內的方向是因果特定且可操作的；模型間的方向是因果真實但非特定的。一種不對稱的轉移拓撲出現，Gemma和Qwen作為幾何捐贈者，Llama作為接收者。這些發現界定了線性跨架構修正的限制，並建議進行模型內探測以進行審計。

##### **CzechDocs: A Multiway Parallel Dataset of Formatted Documents for Minority Languages in Czechia**
2606.20212v1 by Josef Jon, Ondřej Bojar

We present CzechDocs, a multiway parallel dataset of formatted documents (HTML, DOCX, and PDF) covering Czech and minority languages used in Czechia-primarily Ukrainian and English, with smaller portions of Vietnamese, Russian and other languages. The dataset is designed to support the evaluation of machine translation systems that aim to preserve document formatting during translation. We provide a comparison of the most common approaches to format-preserving machine translation on a validation subset of the dataset. This validation split, together with the evaluation toolkit, is publicly released for further research. A held-out test split will be reserved for a future shared task focused on document-level translation with formatting preservation.

摘要：我們提出了CzechDocs，這是一個多路平行的格式化文檔數據集（HTML、DOCX和PDF），涵蓋了捷克及其少數民族語言，主要是烏克蘭語和英語，還有少量的越南語、俄語和其他語言。該數據集旨在支持機器翻譯系統的評估，這些系統旨在在翻譯過程中保留文檔格式。我們提供了對數據集驗證子集上最常見的格式保留機器翻譯方法的比較。這個驗證拆分以及評估工具包將公開發布以供進一步研究。將保留一個測試拆分，用於未來專注於格式保留的文檔級翻譯的共享任務。

##### **Augmenting Game AI with Deep Reinforcement Learning**
2606.20210v1 by Alessandro Sestini, Joakim Bergdahl, Amir Baghi, Jean-Philippe Barrette-LaPierre, Florian Fuchs, Linus Gisslén

Immersion in video games depends not only on graphics, audio, and game mechanics, but also on the quality of in-game characters. Producing believable characters, or game AI, remains a significant challenge as behavioral complexity is hard to capture with hand-coded systems. Game AI is a source of immersion and engagement; however, the limitations stemming from the challenges of creating game AI often lead to frustration and the breaking of the illusion of realism within the game. The introduction of machine learning models opens the door to creating more believable, authentic, and relatable characters in games. The promise is that they either learn from interacting with the game, or from player data, to develop true human-like behavior. In this paper, we envision more applications of reinforcement learning for game AI in the future. For this to materialize, current research limitations are prohibitive to broad deployment across game genres. Therefore, we propose a framework for training reinforcement learning models with a set of requirements in mind that are suited towards game AI and game development. We present examples of games with reinforcement learning-augmented game AI and describe the practicalities of deploying player-facing machine learning agents in modern games. Furthermore, we identify bottlenecks and hard problems in these areas, which we believe offer promising research directions to accelerate the adoption of machine learning in game AI for the video game industry.

摘要：沉浸於視頻遊戲不僅依賴於圖形、音效和遊戲機制，還取決於遊戲內角色的質量。製作可信的角色或遊戲人工智慧仍然是一個重大挑戰，因為行為的複雜性很難用手動編碼的系統捕捉。遊戲人工智慧是沉浸感和參與感的來源；然而，創造遊戲人工智慧所面臨的挑戰所帶來的限制，往往導致挫折感並破壞遊戲中的現實主義幻覺。機器學習模型的引入為創造更可信、真實和可關聯的遊戲角色打開了大門。其承諾在於它們要麼通過與遊戲的互動學習，要麼通過玩家數據來發展真正的人類行為。在本文中，我們展望未來強化學習在遊戲人工智慧中的更多應用。為了實現這一目標，當前的研究限制對於在各種遊戲類型中廣泛部署是具有阻礙性的。因此，我們提出了一個訓練強化學習模型的框架，考慮到適合遊戲人工智慧和遊戲開發的一系列要求。我們展示了增強型遊戲人工智慧的強化學習遊戲的例子，並描述了在現代遊戲中部署面向玩家的機器學習代理的實際情況。此外，我們確定了這些領域中的瓶頸和難題，我們相信這些問題提供了有前景的研究方向，以加速機器學習在視頻遊戲產業中的遊戲人工智慧的採用。

##### **FlowMaps: Modeling Long-Term Multimodal Object Dynamics with Flow Matching**
2606.20209v1 by Francesco Argenziano, Miguel Saavedra-Ruiz, Sacha Morin, Charlie Gauthier, Daniele Nardi, Liam Paull

Joint spatial and temporal understanding of 3D scenes is a crucial requirement for robots deployed in everyday household environments. Such agents must not only comprehend and navigate spatial layouts, but also reason about how these spaces evolve over time. In particular, humans interact with objects daily, causing them to change position throughout the environment and making it difficult for robots to reliably associate current observations with previously seen objects. However, these interactions are not random: human habits and routines induce spatio-temporally consistent patterns in object locations, which robotic agents can potentially learn and then exploit for downstream tasks such as navigation. To this end, we introduce FlowMaps, a latent flow matching model for estimating multimodal distributions over the future locations of dynamic objects in a continuous 3D space. By learning the implicit dependencies among objects and their temporal evolution, FlowMaps predicts likely changes in object locations conditioned on past human interactions, while supporting generalization across previously unseen environments that share similar object routines. To demonstrate the utility of this method, we deploy FlowMaps in a downstream dynamic Object Navigation task in both simulated and real-world environments. Across more than 600 episodes, FlowMaps outperforms state-of-the-art approaches, showing that modeling object dynamics through continuous, multimodal spatio-temporal distributions improves robotic search and navigation in changing household environments. Code and additional material is available at https://fra-tsuna.github.io/flowmaps/.

摘要：聯合空間和時間的理解對於部署在日常家庭環境中的機器人來說是一個至關重要的要求。這些代理不僅必須理解和導航空間佈局，還必須推理這些空間如何隨時間演變。特別是，人類每天都與物體互動，導致它們在環境中改變位置，這使得機器人難以可靠地將當前觀察與之前見過的物體聯繫起來。然而，這些互動並不是隨機的：人類的習慣和日常活動在物體位置上引發了時空一致的模式，機器人代理可以潛在地學習並利用這些模式來進行下游任務，例如導航。為此，我們引入了FlowMaps，一種潛在流匹配模型，用於估計在連續3D空間中動態物體未來位置的多模態分佈。通過學習物體之間的隱含依賴關係及其時間演變，FlowMaps預測基於過去人類互動的物體位置可能的變化，同時支持在共享類似物體日常活動的先前未見環境中的泛化。為了展示這種方法的實用性，我們在模擬和現實環境中的下游動態物體導航任務中部署了FlowMaps。在超過600個回合中，FlowMaps的表現超過了最先進的方法，顯示通過連續的多模態時空分佈建模物體動態能夠改善在變化的家庭環境中的機器人搜索和導航。代碼和其他材料可在 https://fra-tsuna.github.io/flowmaps/ 獲得。

##### **Beyond Accuracy: Measuring Logical Compliance of Predictive Models**
2606.20208v1 by Guillaume Olivier Delplanque, Pierre Genevès, Nabil Layaïda, Zephirin Faure

Machine learning models are predominantly evaluated through predictive performance metrics such as ranking quality, prediction error, or classification accuracy. While these metrics effectively quantify how closely predictions match the ground truth, they do not assess whether model outputs respect predefined logical or domain-specific constraints. In high-stakes applications, including healthcare, finance, and autonomous systems, logical consistency can be as critical as predictive accuracy, yet no standard metric captures this dimension. We introduce the Rule Violation Score (RVS), a complementary evaluation metric that quantifies the extent to which a predictive model respects a given set of logical rules, independently of predictive accuracy. RVS treats hard rules (strict constraints) and soft rules (statistical regularities) differently, can be evaluated on any dataset and on any predictive model expressed over a relational vocabulary, and can be computed using SQL queries that are automatically generated for Horn rules. Beyond evaluating models, RVS can also evaluate the logical consistency of training datasets and help identify poorly defined rules. We evaluate RVS on three benchmarks covering knowledge graph link prediction and relational regression, including rule-based, embedding-based, and neuro-symbolic predictive models. Our results demonstrate that two models achieving comparable predictive accuracy can exhibit substantially different levels of logical compliance, revealing differences in model behavior that standard metrics fail to capture.

摘要：機器學習模型主要通過預測性能指標來評估，例如排名質量、預測誤差或分類準確性。雖然這些指標有效地量化了預測與真實情況之間的接近程度，但它們並不評估模型輸出是否遵循預定的邏輯或特定領域的約束。在高風險應用中，包括醫療保健、金融和自主系統，邏輯一致性可能與預測準確性一樣關鍵，但沒有標準指標能夠捕捉這一維度。我們引入了規則違反分數（RVS），這是一種補充評估指標，用於量化預測模型遵循給定邏輯規則的程度，與預測準確性無關。RVS對硬規則（嚴格約束）和軟規則（統計規律）進行不同處理，可以在任何數據集和任何以關係詞彙表達的預測模型上進行評估，並且可以使用自動生成的SQL查詢來計算Horn規則。除了評估模型，RVS還可以評估訓練數據集的邏輯一致性，並幫助識別定義不清的規則。我們在三個基準上評估RVS，涵蓋知識圖譜鏈接預測和關係回歸，包括基於規則的、基於嵌入的和神經符號的預測模型。我們的結果顯示，兩個達到可比預測準確性的模型在邏輯合規性方面可能表現出顯著不同的水平，揭示了標準指標無法捕捉的模型行為差異。

##### **Apparent Psychological Profiles of Large Language Models are Largely a Measurement Artifact**
2606.20205v1 by Jelena Meyer, David Garcia, Dirk U. Wulff

Psychological instruments designed for humans are increasingly used to assign large language models (LLMs) stable psychological profiles that affect their usability, safety assessment, and use as proxies for human participants in research. Using a formal psychometric framework, we show that these profiles are largely a measurement artifact. Administering a battery of personality and risk-preference instruments spanning self-reports and behavioral tasks to 56 instruction-tuned LLMs alongside large human reference samples, we report four findings. First, differences between models are driven not by the traits an instrument targets but by a directional response bias, a tendency to respond toward one end of the scale, or one labeled option, regardless of item content; a variance decomposition attributes 81-90% of between-model variation to this bias, against 9-16% in humans. Second, the bias declines with model capability but is not eliminated by it. Third, because bias rather than trait drives responding, an instrument's apparent reliability is almost entirely predicted by its response orthogonality, a term we coin for the proportion of items for which trait and bias point in opposite directions. Fourth, the profile a model appears to have shifts with the items used and can be manufactured through item selection. These results demonstrate that the apparent psychological profiles of LLMs are artifacts of the instrument used to measure them, not properties of the models themselves. As instruments borrowed from human psychology are rarely fully orthogonal and may inherently lack validity for LLMs, we call for dedicated assessments centered on response orthogonality.

摘要：心理學工具設計用於人類的情況下，越來越多地被用來為大型語言模型（LLMs）賦予穩定的心理特徵，這影響了它們的可用性、安全評估，以及在研究中作為人類參與者的代理使用。<lb/>使用正式的心理測量框架，我們顯示這些特徵在很大程度上是一種測量工件。<lb/>對56個經過指令調整的LLMs進行了一系列涵蓋自我報告和行為任務的人格和風險偏好工具的測試，並與大量人類參考樣本進行比較，我們報告了四個發現。<lb/>首先，模型之間的差異並不是由工具所針對的特徵驅動，而是由一種方向性反應偏見驅動，這是一種傾向於向量表的一端或標記選項回應的趨勢，無論項目內容如何；變異分解將81-90%的模型間變異歸因於這種偏見，而人類則為9-16%。<lb/>第二，隨著模型能力的提高，偏見會減少，但並未被消除。<lb/>第三，由於偏見而非特徵驅動回應，工具的明顯可靠性幾乎完全由其反應正交性預測，這是一個我們創造的術語，用於描述特徵和偏見朝相反方向指向的項目比例。<lb/>第四，模型所顯示的特徵隨著所使用的項目而變化，並且可以通過項目選擇來製造。<lb/>這些結果表明，LLMs的明顯心理特徵是用來測量它們的工具的工件，而不是模型本身的特性。<lb/>由於借用自人類心理學的工具很少完全正交，並且可能固有地缺乏對LLMs的有效性，我們呼籲進行專門以反應正交性為中心的評估。

##### **HilDA: Hierarchical Distillation with Diffusion for Advancing Self-Supervised LiDAR Pre-trainin**
2606.20189v1 by Maciej Wozniak, Jesper Ericsson, Hariprasath Govindarajan, Truls Nyberg, Thomas Gustafsson, Patric Jensfelt, Olov Andersson

Leveraging Vision Foundation Models (VFMs) for camera-to-LiDAR knowledge distillation offers a promising solution to the scarcity of annotated data needed to represent the immense geometric and kinematic diversity of real-world autonomous driving (AD). However, current approaches typically treat VFMs as black-box teachers, relying exclusively on frame-wise feature similarity. Consequently, they do not fully exploit the teacher's layer-wise semantic structure and global context, as well as the rich spatiotemporal information inherent in LiDAR sequences. We propose HilDA, a self-supervised pretraining framework for LiDAR backbones that better captures the semantic what and geometric where needed for driving tasks. HilDA combines hierarchical distillation comprising multi-layer distillation for progressive semantic alignment and global context distillation for scene-level semantics, with a temporal occupancy diffusion objective promoting spatiotemporal consistency. Models pre-trained with HilDA achieve state-of-the-art results on cross-modal distillation benchmarks and outperform models trained via prior distillation approaches on 3D object detection, scene flow, and semantic occupancy prediction. Code available at: https://maxiuw.github.io/hilda.

摘要：利用視覺基礎模型（VFM）進行相機到LiDAR的知識蒸餾為解決表現真實世界自動駕駛（AD）所需的標註數據稀缺問題提供了一個有前景的解決方案。<lb/>然而，目前的方法通常將VFM視為黑箱教師，僅依賴於逐幀特徵相似性。<lb/>因此，它們未能充分利用教師的層級語義結構和全局上下文，以及LiDAR序列中固有的豐富時空信息。<lb/>我們提出了HilDA，一個自我監督的LiDAR骨幹預訓練框架，更好地捕捉駕駛任務所需的語義“什麼”和幾何“哪裡”。<lb/>HilDA結合了分層蒸餾，包括多層蒸餾以促進漸進的語義對齊，以及全局上下文蒸餾以獲取場景級語義，並具有促進時空一致性的時間佔用擴散目標。<lb/>使用HilDA預訓練的模型在跨模態蒸餾基準上達到了最先進的結果，並在3D物體檢測、場景流和語義佔用預測方面超越了通過先前蒸餾方法訓練的模型。<lb/>代碼可在：https://maxiuw.github.io/hilda。

##### **ReNikud: Audio-Supervised Hebrew Grapheme-to-Phoneme Conversion**
2606.20179v1 by Maxim Melichov, Yakov Kolani, Morris Alper

Grapheme-to-phoneme (G2P) conversion for Modern Hebrew is needed for applications like text-to-speech (TTS), but is challenging due to the language's abjad writing system, which leaves vowels largely unwritten, creating substantial ambiguity. Standard approaches first predict vowel diacritics (nikud) to produce International Phonetic Alphabet (IPA) transcriptions, but this is limited: vocalization data is scarce and laborious to produce, it does not specify features such as lexical stress, and it reflects formal grammatical rules rather than everyday spoken pronunciation. Direct sequence-to-sequence IPA prediction, meanwhile, struggles on limited data and fails to exploit the character-level alignment characteristic of abjads. Our method, ReNikud, overcomes these limitations with two key insights: (1) Weak audio supervision via a phoneme-based automatic speech recognition (ASR) pseudo-labeling pipeline on thousands of hours of unlabeled Hebrew audio, yielding phonemic transcriptions that reflect natural spoken norms without manual annotation. (2) A pseudo-vocalization architecture that predicts IPA phonemes at each character position, enforcing character-level alignment as an inductive bias. Results on existing Hebrew G2P benchmarks and the new targeted MILIM benchmark for spoken Hebrew show that ReNikud surpasses previous state-of-the-art methods. We will release our code and trained models to support further work on Hebrew TTS and speech technologies.

摘要：字母到音素（G2P）轉換對於現代希伯來語的應用，如文本到語音（TTS），是必要的，但由於該語言的輕母音書寫系統，這一過程充滿挑戰，因為母音大多未被書寫，造成了相當大的歧義。標準方法首先預測母音標記（nikud），以生成國際音標（IPA）轉錄，但這是有限的：語音化數據稀缺且生成繁瑣，無法指定詞彙重音等特徵，並且反映的是正式語法規則，而非日常口語發音。與此同時，直接的序列到序列IPA預測在有限數據上表現不佳，並未利用輕母音特有的字符級對齊特性。我們的方法ReNikud通過兩個關鍵洞察克服了這些限制：（1）通過基於音素的自動語音識別（ASR）偽標記管道，對數千小時未標記的希伯來語音頻進行弱音頻監督，產生反映自然口語規範的音素轉錄，無需手動標註。（2）一種偽語音化架構，在每個字符位置預測IPA音素，強制字符級對齊作為歸納偏差。在現有的希伯來語G2P基準和針對口語希伯來語的新目標MILIM基準上的結果顯示，ReNikud超越了以往的最先進方法。我們將發布我們的代碼和訓練模型，以支持對希伯來語TTS和語音技術的進一步研究。

##### **Evaluating and Enhancing Negation Comprehension in Remote Sensing MLLMs**
2606.20177v1 by Haochen Han, Jue Wang, Alex Jinpeng Wang, Fangming Liu

Multimodal Large Language Models (MLLMs) have demonstrated remarkable success in various Remote Sensing (RS) tasks. However, their ability to comprehend negation remains underexplored, limiting deployment in real-world applications where models must explicitly identify what is false or absent, e.g., emergency responders need to locate non-flooded routes for evacuation. To comprehensively study this limitation, we introduce RS-Neg, the first benchmark to evaluate negation understanding across region-level to scene-level tasks. Specifically, we design an automated data generation pipeline for RS imagery, using LLMs to synthesize diverse negation queries, and introduce a dynamic visual focus module for verification. Our evaluation reveals that advanced RS MLLMs struggle with negation, exhibiting hallucinations and substantial performance degradation. To close this gap, we propose NeFo, a novel test-time learning method that explicitly incorporates the logical role of negation into the model optimization. Remarkably, using about 5\% unlabeled test samples, NeFo significantly improves the negation understanding of models and shows strong generalization to unseen tasks. Code and data will be released upon acceptance.

摘要：多模態大型語言模型（MLLMs）在各種遙感（RS）任務中展現了顯著的成功。<lb/>然而，它們理解否定的能力仍然未被充分探索，這限制了它們在現實應用中的部署，因為模型必須明確識別什麼是錯誤或缺失，例如，緊急救援人員需要找到未被淹沒的撤離路線。<lb/>為了全面研究這一限制，我們推出了RS-Neg，這是第一個基準，用於評估從區域級到場景級任務的否定理解。<lb/>具體而言，我們設計了一個自動化數據生成管道，用於RS影像，利用LLMs合成多樣的否定查詢，並引入了一個動態視覺聚焦模塊以進行驗證。<lb/>我們的評估顯示，先進的RS MLLMs在否定方面表現不佳，出現了幻覺和顯著的性能下降。<lb/>為了縮小這一差距，我們提出了NeFo，一種新穎的測試時學習方法，明確將否定的邏輯角色納入模型優化中。<lb/>值得注意的是，使用約5\%的未標記測試樣本，NeFo顯著提高了模型的否定理解能力，並對未見過的任務展現了強大的泛化能力。<lb/>代碼和數據將在接受後發布。

##### **MedRLM: Recursive Multimodal Health Intelligence for Long-Context Clinical Reasoning, Sensor-Guided Screening, Evidence-Grounded Decision Support, and Community-to-Tertiary Referral Optimization**
2606.20164v1 by Aueaphum Aueawatthanaphisut

Real-world clinical decision support requires reasoning over heterogeneous and longitudinal patient information rather than answering isolated medical questions. However, current medical large language models and retrieval-augmented generation systems often rely on single-step prompting or retrieval, which can be fragile when clinical evidence is distributed across long electronic health records, medical images, sensor streams, guidelines, and referral constraints. This paper proposes MedRLM, a Recursive Multimodal Health Intelligence framework for long-context clinical reasoning, sensor-guided screening, and community-to-tertiary referral support. Instead of compressing all patient information into one prompt, MedRLM treats the patient case as an external clinical environment that can be recursively inspected, decomposed, retrieved, verified, and synthesized. The framework coordinates specialized agents for clinical text, longitudinal EHR, medical imaging, physiological sensor signals, guideline retrieval, uncertainty auditing, and referral planning. It further introduces a Clinical Evidence Graph Memory to connect patient-specific observations with retrieved evidence, standardized definitions, sensor-derived biomarkers, and referral criteria. A sensor-guided recursive triggering mechanism activates deeper reasoning when abnormal physiological or behavioral patterns are detected, while uncertainty-gated refinement supports clinician review for high-risk or low-confidence cases. We also outline a real-data evaluation design using public and credentialed clinical datasets spanning EHR, radiology, ECG, ICU time series, and referral-proxy outcomes. MedRLM aims to move medical AI from static question answering toward auditable, multimodal, and workflow-aware clinical decision support.

摘要：現實世界的臨床決策支持需要對異質和長期的病人信息進行推理，而不是僅僅回答孤立的醫療問題。<lb/>然而，目前的醫療大型語言模型和檢索增強生成系統往往依賴於單步提示或檢索，這在臨床證據分布在長電子健康記錄、醫療影像、傳感器數據流、指導方針和轉診限制時可能顯得脆弱。<lb/>本文提出了MedRLM，一個用於長上下文臨床推理、傳感器引導篩查和社區到三級轉診支持的遞歸多模態健康智能框架。<lb/>MedRLM並不是將所有病人信息壓縮成一個提示，而是將病人案例視為一個可以遞歸檢查、分解、檢索、驗證和綜合的外部臨床環境。<lb/>該框架協調專門的代理來處理臨床文本、長期電子健康記錄、醫療影像、生理傳感器信號、指導方針檢索、不確定性審計和轉診規劃。<lb/>它進一步引入了一個臨床證據圖記憶，以將病人特定的觀察與檢索到的證據、標準化定義、傳感器衍生的生物標記和轉診標準相連接。<lb/>當檢測到異常的生理或行為模式時，傳感器引導的遞歸觸發機制會激活更深層次的推理，而不確定性門控的精煉則支持臨床醫生對高風險或低信心案例的審查。<lb/>我們還概述了一個使用公共和經認證的臨床數據集的真實數據評估設計，這些數據集涵蓋電子健康記錄、放射學、心電圖、重症監護時間序列和轉診代理結果。<lb/>MedRLM旨在將醫療AI從靜態問題回答推進到可審計的、多模態的和工作流程感知的臨床決策支持。

##### **Modularity-Free Conflict-Averse Training for Generalized PINNs**
2606.20156v1 by Heejo Kong, Beomchul Park, Sung-Jin Kim, Seong-Whan Lee

Physics-informed neural networks (PINNs) have become a powerful framework for solving PDEs by embedding physical laws into differentiable objectives. Despite their advances, training PINNs remains fragile: recent conflict-averse optimization schemes alleviate gradient interference between residual and boundary losses, but we show that their effectiveness deteriorates as model capacity increases. In this paper, we identify a capacity-induced failure mode, where overparameterized networks undergo functional modularity, self-partitioning into task-exclusive modules that suppress cross-objective interaction and hinder convergence toward Pareto-stationary points. To address this issue, we propose a novel framework, Modular-Sparsity Synchronization (ModSync), which integrates structural optimization into conflict-averse training by penalizing task-exclusive connections while preserving interaction-promoting pathways. Extensive experiments across diverse PDE benchmarks demonstrate that ModSync consistently prevents capacity-driven failures, sustains robust cross-objective coupling, and achieves state-of-the-art accuracy. Codes are available at \url{https://github.com/heejokong/ModSync}.

摘要：物理知識驅動的神經網絡（PINNs）已成為解決偏微分方程（PDEs）的強大框架，通過將物理法則嵌入可微分的目標中。儘管有了這些進展，訓練PINNs仍然非常脆弱：最近的抗衝突優化方案減輕了殘差損失和邊界損失之間的梯度干擾，但我們顯示，隨著模型容量的增加，它們的有效性會下降。在本文中，我們識別出一種由容量引起的失效模式，其中過參數化的網絡經歷功能模塊化，自我劃分為任務專屬的模塊，這抑制了跨目標的互動並妨礙了向Pareto穩定點的收斂。為了解決這個問題，我們提出了一種新穎的框架，模塊稀疏同步（ModSync），它通過懲罰任務專屬的連接，同時保留促進互動的通路，將結構優化整合到抗衝突訓練中。針對多種PDE基準的廣泛實驗表明，ModSync始終防止由容量驅動的失敗，維持穩健的跨目標耦合，並實現了最先進的準確性。代碼可在 \url{https://github.com/heejokong/ModSync} 獲得。

##### **NAMESAKES: Probing Identity Memorization in Text-to-Image Models**
2606.20155v1 by Morris Alper, Vasudha Varadarajan, Moran Yanuka, Angelina Wang, Hadar Averbuch-Elor

Text-to-image (T2I) models generate realistic likenesses of some individuals when prompted with their names, raising privacy concerns. However, distinguishing whether a generated face is memorized or fabricated currently requires ground-truth photos, access to training data, or white-box access to model internals, limiting applicability. We introduce a fully black-box behavioral probe that distinguishes between these regimes while requiring no reference photos or prior knowledge of training data. To benchmark this task, we present the NAMESAKES dataset of over one thousand names and faces of public figures spanning a wide range of fame levels, along with perturbed, less famous names. Experiments on state-of-the-art T2I models show that our probe substantially predicts identity memorization and separates memorized from unrecognized names, with further insights into differences across model families.

摘要：文本到圖像（T2I）模型在被提示以某些個體的名字時，能生成其真實的肖像，這引發了隱私問題。<lb/>然而，目前區分生成的面孔是記憶中的還是虛構的，仍需要真實照片、訓練數據的訪問或對模型內部的白盒訪問，這限制了其適用性。<lb/>我們介紹了一個完全黑盒的行為探測器，能在不需要參考照片或對訓練數據的先前知識的情況下區分這些情況。<lb/>為了基準這項任務，我們呈現了NAMESAKES數據集，該數據集包含超過一千個名字和面孔，涵蓋了各種名氣水平的公共人物，以及一些擾動的、不太知名的名字。<lb/>對最先進的T2I模型的實驗顯示，我們的探測器能顯著預測身份記憶，並將記憶的名字與未識別的名字區分開來，並提供了不同模型家族之間差異的進一步見解。

##### **From Texts to Scores: Tracing the Emergence of Essay Quality Representations in Large Language Models**
2606.20152v1 by Jiaxu Zuo, Mu You, Kaixin Lan, Tao Fang, Yujia Huo, Henghua Shen, Lidia S. Chao, Derek F. Wong

Recent advances in Large Language Models (LLMs) have substantially transformed Automated Essay Scoring (AES), yet the internal mechanisms underlying LLM-based scoring remain poorly understood. In this work, we systematically analyze the hidden representations of eight LLMs across two English essay datasets (ASAP++, CSEE) and one Portuguese dataset (ENEM). Using linear probing, cross-prompt generalization, dimensionality reduction, and neuron-level analyses, we find consistent evidence that essay quality information is encoded in a linearly accessible form within LLM representations. These representations emerge progressively across layers, remain robust across prompting strategies, and partially transfer across essay prompts despite differences in scoring rubrics. In addition, nonlinear probes provide only marginal and inconsistent improvements over linear probes, suggesting that most essay quality information is already linearly decodable. We further identify individual ``essay scoring neurons'' whose activations strongly correlate with essay scores and whose behavior is sensitive to targeted intervention. Moreover, the layer-wise distribution of these neurons systematically shifts with essay length, with longer essays relying more heavily on deeper layers. Overall, our findings provide evidence that LLMs encode structured representations related to essay quality and offer new insights into the interpretability of LLM-based AES systems.

摘要：最近在大型語言模型（LLMs）方面的進展已經顯著改變了自動化作文評分（AES），然而基於LLM的評分背後的內部機制仍然不甚了解。<lb/>在本研究中，我們系統性地分析了八個LLM在兩個英語作文數據集（ASAP++、CSEE）和一個葡萄牙語數據集（ENEM）上的隱藏表示。<lb/>通過線性探測、跨提示泛化、降維和神經元級別分析，我們發現一致的證據表明，作文質量信息以線性可訪問的形式編碼在LLM表示中。<lb/>這些表示在層中逐漸出現，並在提示策略之間保持穩健，儘管評分標準存在差異，但仍部分轉移於不同的作文提示之間。<lb/>此外，非線性探測僅提供邊際和不一致的改進，相較於線性探測，這表明大多數作文質量信息已經可以線性解碼。<lb/>我們進一步識別出個別的“作文評分神經元”，其激活與作文分數強烈相關，並且其行為對針對性干預敏感。<lb/>此外，這些神經元的層級分佈隨著作文長度的變化而系統性地轉變，較長的作文更依賴於更深的層。<lb/>總體而言，我們的研究結果提供了證據，表明LLMs編碼了與作文質量相關的結構化表示，並為基於LLM的AES系統的可解釋性提供了新的見解。

##### **BIM-Edit: Benchmarking Large Language Models for IFC-Based Building Information Modeling**
2606.20146v1 by Bharathi Kannan Nithyanantham, Clemens Kujat, Tobias Sesterhenn, Stefan Telgmann, Jörn Plönnigs, Stefan Lüdtke, Christian Bartelt

Large language models (LLMs) are increasingly applied to computer-aided design (CAD) to generate design artifacts from textual instructions. In engineering practice, this requires more than creating new geometry, models must also understand existing scenes, edit them correctly, and preserve semantics and relations. However, many CAD benchmarks focus on creating new models rather than editing existing ones, and mostly evaluate geometric correctness. We introduce BIM-Edit, a benchmark for evaluating LLMs on natural-language editing of Building Information Models (BIM) represented in the Industry Foundation Classes (IFC) format. BIM provides a challenging testbed because building models encode geometry together with semantic and relational structure. BIM-Edit contains 324 editing tasks spanning 11 realistic building models and 36 synthetic scenes. Tasks are expressed using three instruction categories - direct, spatial, and topological - covering both explicit and scene-grounded edits. We evaluate outputs along three dimensions: geometric accuracy, semantic validity, and topological consistency. Across evaluated LLMs, the best-performing model achieves only 49.5% average score across the three metrics, and no model fully solves more than 3.4% of tasks. These results demonstrate a substantial gap between current LLM capabilities and the requirements of structured engineering design workflows.

摘要：大型語言模型（LLMs）越來越多地應用於計算機輔助設計（CAD），以從文本指令生成設計產物。<lb/>在工程實踐中，這不僅需要創建新的幾何形狀，模型還必須理解現有場景，正確編輯它們，並保留語義和關係。<lb/>然而，許多CAD基準測試專注於創建新模型，而不是編輯現有模型，並且主要評估幾何正確性。<lb/>我們介紹了BIM-Edit，這是一個用於評估LLMs在自然語言編輯建築信息模型（BIM）方面的基準，這些模型以行業基礎類（IFC）格式表示。<lb/>BIM提供了一個具有挑戰性的測試平台，因為建築模型同時編碼幾何形狀以及語義和關係結構。<lb/>BIM-Edit包含324個編輯任務，涵蓋11個現實建築模型和36個合成場景。<lb/>任務使用三種類型的指令表達——直接、空間和拓撲——涵蓋了顯性和基於場景的編輯。<lb/>我們從三個維度評估輸出：幾何準確性、語義有效性和拓撲一致性。<lb/>在評估的LLMs中，表現最佳的模型在這三個指標上的平均得分僅為49.5%，而且沒有模型完全解決超過3.4%的任務。<lb/>這些結果顯示了當前LLM能力與結構化工程設計工作流程要求之間的顯著差距。

##### **Learning to Prompt: Improving Student Engagement with Adaptive LLM-based High-School Tutoring**
2606.20138v1 by Po-Chin Chang, Nicholas Hogan, Aske Plaat, Michiel T. van der Meer

LLMs can personalize education, although current static-prompt tutoring systems struggle to adapt to diverse academic disciplines. We develop and test a system with subject-aware prompting, based on 14 pedagogical features (e.g., tutor scaffolding, student understanding) extracted from raw transcripts. We first train a prompt routing model in a simulation environment, and then deploy it for online adaptation with actual high-school students. The simulation benchmark shows the router outperforming two static baselines ($0.694$ vs. $0.647$ and $0.64$, $p<0.001$). A/B testing ($N=656$ conversations from 359 students) shows sim-to-real transfer where the model switches from analytical to scaffolding learning strategies. Our adaptive prompt selection mechanism improves instructional efficiency, maintains pedagogical quality and reduces interactions by around 3 turns ($p=0.007$). While a greedy router achieves a comparable exercise conversion rate with the baseline ($19.1\%$ vs. $19.6\%$), a stochastic router that samples strategies leads to a higher conversion rate ($28.1\%$).

摘要：LLMs 可以個性化教育，儘管當前的靜態提示輔導系統在適應多樣化的學科方面存在困難。<lb/>我們開發並測試了一個基於 14 個教學特徵（例如，輔導者支架、學生理解）的主題感知提示系統，這些特徵是從原始記錄中提取的。<lb/>我們首先在模擬環境中訓練了一個提示路由模型，然後將其部署以實現與實際高中生的在線適應。<lb/>模擬基準顯示，路由器的表現超過了兩個靜態基準（$0.694$ 對 $0.647$ 和 $0.64$，$p&lt;0.001$）。<lb/>A/B 測試（$N=656$ 次對話來自 359 名學生）顯示了模擬到現實的轉移，模型從分析學習策略轉變為支架學習策略。<lb/>我們的自適應提示選擇機制提高了教學效率，維持了教學質量，並減少了大約 3 次互動（$p=0.007$）。<lb/>雖然貪婪路由器達到了與基準相當的練習轉換率（$19.1\%$ 對 $19.6\%$），但隨機路由器通過抽樣策略導致了更高的轉換率（$28.1\%$）。

##### **PASQA: Pitch-Accent-Focused Speech Quality Assessment Model Trained on Synthetic Speech with Accent Errors**
2606.20137v1 by Masaya Kawamura, Yuma Shirahata, Kentaro Mitsui, Reo Shimizu

Existing mean opinion score (MOS) prediction models typically predict utterance-level naturalness MOS and can be insensitive to localized pitch-accent errors. We propose Pitch-Accent-focused Speech Quality Assessment (PASQA), which explicitly targets pitch-accent correctness. To train our model, we construct a controlled Japanese accent-error dataset by changing accent patterns using an accent-controllable text-to-speech system, and compute a pseudo accent-quality score from the accent-error rate. PASQA builds on self-supervised representations and employs mora-conditioned fusion, ranking loss, an auxiliary accent-error localization task, and speaker-invariant training. Experiments show that conventional models fail to preserve the ordering by accent-error severity, whereas PASQA achieves high ordering accuracy on both seen and unseen speakers. Further, PASQA shows stronger agreement with human accent-correctness judgments. The code is available at https://github.com/lycorp-jp/PASQA.

摘要：現有的平均意見分數 (MOS) 預測模型通常預測話語級別的自然度 MOS，並且對於局部的音調重音錯誤可能不夠敏感。<lb/>我們提出了以音調重音為重點的語音質量評估 (PASQA)，該模型明確針對音調重音的正確性。<lb/>為了訓練我們的模型，我們通過使用可控音調的文本轉語音系統改變音調模式，構建了一個受控的日語音調錯誤數據集，並根據音調錯誤率計算出一個偽音調質量分數。<lb/>PASQA 基於自我監督的表示，並採用了音節條件融合、排名損失、一個輔助的音調錯誤定位任務，以及說話者不變的訓練。<lb/>實驗顯示，傳統模型未能保持音調錯誤嚴重程度的排序，而 PASQA 在已知和未知的說話者上都達到了高排序準確性。<lb/>此外，PASQA 與人類的音調正確性判斷有更強的一致性。<lb/>代碼可在 https://github.com/lycorp-jp/PASQA 獲得。

##### **Frequency-Aware Flow Matching for Continuous and Consistent Robotic Action Generation**
2606.20135v1 by Jianing Guo, Fangzheng Chen, Zihao Mao, Wong Lik Hang Kenny, Zhenhong Wu, Yu Li, Yishuai Cai, Yuanpei Chen, Yikun Ban, Kai Chen, Qi Dou, Yaodong Yang, Xianglong Liu, Huijie Zhao, Simin Li

Flow matching has emerged as a standard paradigm for robotic manipulation owing to its strong expressive power for modelling complex, multimodal action distributions, alongside similar approaches like diffusion policy. However, existing methods rely on discretized action chunks, making them brittle to demonstrations collected at heterogeneous control frequencies and prone to temporally inconsistent actions that degrade control stability. In this paper, we propose Frequency-Aware Flow Matching (FAFM), which outputs continuous, temporally consistent actions. To handle heterogeneous frequency input, we transform discrete action sequences into the frequency domain with the discrete cosine transform (DCT), perform flow matching over the resulting coefficients, and reconstruct continuous actions via cosine basis expansion. To generate temporally consistent actions, we regularize the first-order temporal derivative to promote smooth actions. This corresponds to a Sobolev-type constraint that suppresses high-frequency errors and discourages abrupt action changes. Our FAFM is simple, introduces no additional network parameters and applies to standalone flow-matching policies and vision-language action models. Across synthetic toy benchmark, obstacle avoidance, LapGym, and LIBERO, FAFM improves success rates, multimodal expressivity, motion smoothness, convergence speed, robustness to mechanical bias and mixed-frequency input. These gains are consistent when deployed on a real-world Franka robot. Code available at https://anonymous.4open.science/r/FAFM.

摘要：流匹配已成為機器人操作的一個標準範式，因為它在建模複雜的多模態動作分佈方面具有強大的表達能力，與擴散策略等類似方法並駕齊驅。<lb/>然而，現有方法依賴於離散化的動作片段，使其對於在異質控制頻率下收集的演示變得脆弱，並且容易產生時間上不一致的動作，從而降低控制穩定性。<lb/>在本文中，我們提出了頻率感知流匹配（FAFM），它輸出連續且時間一致的動作。<lb/>為了處理異質頻率輸入，我們使用離散餘弦變換（DCT）將離散動作序列轉換到頻率域，對結果係數進行流匹配，並通過餘弦基擴展重建連續動作。<lb/>為了生成時間一致的動作，我們對一階時間導數進行正則化，以促進平滑的動作。<lb/>這相當於一種Sobolev類約束，抑制高頻誤差並不鼓勵突變的動作變化。<lb/>我們的FAFM簡單，不引入額外的網絡參數，並適用於獨立的流匹配策略和視覺-語言動作模型。<lb/>在合成玩具基準、避障、LapGym和LIBERO等場景中，FAFM提高了成功率、多模態表達能力、運動平滑性、收斂速度、對機械偏差和混合頻率輸入的魯棒性。<lb/>這些增益在部署於真實世界的Franka機器人時也是一致的。<lb/>代碼可在 https://anonymous.4open.science/r/FAFM 獲得。

##### **ScaffoldAgent: Utility-Guided Dynamic Outline Optimization for Open-Ended Deep Research**
2606.20122v1 by Zhibang Yang, Xinke Jiang, Yuzhen Xiao, Ruizhe Zhang, Yue Fang, XinFei Wan, Zhengxing Song, Yuxuan Liu, Yuheng Huang, Xu Chu, Junfeng Zhao, Yasha Wang

Open-ended deep research (OEDR) requires systems to acquire knowledge through multi-round retrieval and generate coherent long-form reports. The outline plays a central role as a structural scaffold that coordinates retrieval, evidence organization, and generation. However, existing methods either fix the outline before writing or refine it with local heuristics, leading to scaffold drift under continuous information accumulation and delayed feedback for evaluating outline modifications. We propose ScaffoldAgent, a utility-guided dynamic outline optimization framework for OEDR. ScaffoldAgent models outline evolution as a structured decision process with three operations: Expansion, Contraction, and Revision, enabling controlled updates to the report scaffold. It further introduces a utility-guided feedback mechanism that estimates the downstream value of each outline operation from retrieval gain, structural coherence, and trial-generation quality. The resulting utility signal guides node selection, operation scheduling, and termination during inference. Experiments on DeepResearch Bench and DeepResearch Gym show that ScaffoldAgent consistently improves long-form report generation and factual grounding over existing deep research agents.

摘要：開放式深度研究（OEDR）要求系統通過多輪檢索獲取知識並生成連貫的長篇報告。大綱作為結構性的支架，在檢索、證據組織和生成之間協調發揮著核心作用。然而，現有方法要麼在寫作之前固定大綱，要麼使用局部啟發式方法進行細化，這導致在持續的信息積累下支架漂移，以及對大綱修改的評估反饋延遲。我們提出了ScaffoldAgent，一個用於OEDR的實用性導向動態大綱優化框架。ScaffoldAgent將大綱演變建模為一個結構化的決策過程，包含三個操作：擴展、收縮和修訂，從而實現對報告支架的控制更新。它進一步引入了一個實用性導向的反饋機制，根據檢索增益、結構一致性和試生成質量來估算每個大綱操作的下游價值。所產生的實用性信號指導節點選擇、操作調度和推理過程中的終止。在DeepResearch Bench和DeepResearch Gym上的實驗顯示，ScaffoldAgent在長篇報告生成和事實基礎方面持續優於現有的深度研究代理。

##### **Dual-Agent Framework for Cross-Model Verified Translation of Natural-Language Protocols into Robotic Laboratory Platform**
2606.20120v1 by Hyeonna Choi, Jung Yup Kim, Hyuneui Lim, Seunggyu Jeon

Biological experiment protocols are written in natural language, whereas automation systems rely on predefined control commands, creating a semantic gap that limits autonomous execution. Microplate-based automatic experiments are particularly challenging due to the need to simultaneously control well mapping, sample-reagent combinations, replicate placement, and parallel dispensing. This study proposes an agent-based protocol translation framework that converts natural-language microplate-based protocols into executable control commands for a robotic laboratory platform. A Parser Agent formalizes the natural-language protocol into a structured representation, and a rule-based mapping engine deterministically incorporates the operational constraints of the robotic laboratory platform to generate device-level control commands. A heterogeneous LLM Validation Agent verifies completeness, parameter accuracy, and execution order, and triggers a self-correction loop with structured feedback when errors are detected. A sweep involving 7 Parsers and 3 Validators on randomly selected ELISA protocols evaluates how model scale and Validator type affect translation accuracy and pass rates under cross-model verification. The accuracy-latency trade-off is further verified by comparing the rule-based mapping of the proposed framework with LLM end-to-end direct mapping. Finally, Bradford assay-based protein quantification using a microplate was demonstrated on a robotic laboratory platform, validating end-to-end autonomous execution from natural-language protocols to real-world experiments. The proposed framework provides a flexible approach to narrowing the semantic gap between natural-language protocols and microplate-based self-driving laboratories.

摘要：生物實驗的操作流程是用自然語言撰寫的，而自動化系統則依賴預定義的控制命令，這造成了一個語義差距，限制了自主執行的能力。基於微板的自動實驗特別具有挑戰性，因為需要同時控制孔位映射、樣品-試劑組合、重複放置和並行分配。本研究提出了一個基於代理的流程翻譯框架，將自然語言的微板流程轉換為可執行的控制命令，以用於機器人實驗室平台。解析代理將自然語言流程形式化為結構化表示，而基於規則的映射引擎確定性地納入機器人實驗室平台的操作約束，以生成設備級控制命令。一個異質的LLM驗證代理驗證完整性、參數準確性和執行順序，並在檢測到錯誤時觸發帶有結構化反饋的自我修正循環。對隨機選擇的ELISA流程進行7個解析器和3個驗證器的掃描，評估模型規模和驗證器類型如何影響翻譯準確性和跨模型驗證下的通過率。準確性與延遲的權衡進一步通過將所提框架的基於規則的映射與LLM端到端直接映射進行比較來驗證。最後，基於Bradford測定的蛋白質定量使用微板在機器人實驗室平台上進行，驗證了從自然語言流程到現實世界實驗的端到端自主執行。所提框架提供了一種靈活的方法，以縮小自然語言流程與基於微板的自駕實驗室之間的語義差距。

##### **When Does Streaming Tool Use Help? Characterizing Tool-Intent Stabilization in Streaming Retrieval-Augmented Generation**
2606.20113v1 by Elroy Galbraith

Streaming Retrieval-Augmented Generation (Streaming RAG) reduces user-perceived latency by issuing tool queries in parallel with ongoing user input, before the utterance is complete. Reported gains are aggregate, yet the mechanism's benefit is fundamentally query-intrinsic: speculation can only help when the correct tool query becomes determinable before the user stops speaking or typing. We isolate and measure this property -- tool-intent stabilization, the point in the input stream at which a speculative query's retrieval converges to the answer-bearing result. On the CRAG benchmark (1371 validation questions) we (i) measure the distribution of stabilization, (ii) derive a model-agnostic bound H on the portion of tool latency that can be hidden behind the user's remaining input, as a function of tool latency L and input cadence δ, (iii) validate against a working streaming pipeline that realized savings meet or exceed this bound, and (iv) identify which query properties predict early versus late stabilization. The study requires no model training and runs on commodity CPU hardware. We find that at a realistic operating point (L=600ms, δ=3w/s, θ=0.8), 73.9% of queries across the full benchmark admit substantial latency hiding -- a blended figure that mixes sufficiency stabilization on the 21.3% of questions where gold evidence is verbatim-present and BM25-retrievable (95.2% streamable on this favorable slice) with a grounding-free top-1-settling fallback on the remainder. On the favorable slice, φ_suf is bracketed to [0.26, 0.281] by exact and relaxed grounding -- both early. Question type produces a significant but coarse early/late split (Kruskal-Wallis p=0.017, epsilon^2=0.04), directly informing when a learned speculative trigger is worth its cost.

摘要：串流檢索增強生成（Streaming RAG）通過在用戶輸入未完成之前並行發出工具查詢來減少用戶感知的延遲。報告的增益是總體的，但該機制的好處在根本上是查詢內在的：當正確的工具查詢在用戶停止說話或打字之前變得可確定時，推測才有幫助。我們隔離並測量這一特性——工具意圖穩定化，即在輸入流中，推測查詢的檢索收斂到承載答案的結果的點。在CRAG基準測試（1371個驗證問題）上，我們（i）測量穩定化的分佈，（ii）推導出一個與模型無關的界限H，該界限是可以隱藏在用戶剩餘輸入後的工具延遲部分，作為工具延遲L和輸入節奏δ的函數，（iii）驗證與一個運行中的串流管道，該管道實現的節省達到或超過這一界限，以及（iv）識別哪些查詢特性預測早期與晚期穩定化。該研究不需要模型訓練，並在商品CPU硬件上運行。我們發現，在一個現實的運行點（L=600ms，δ=3w/s，θ=0.8），全基準測試中73.9%的查詢允許顯著的延遲隱藏——這是一個混合數字，將在21.3%的問題上存在逐字呈現的金證據和可用BM25檢索的充分性穩定化（在這個有利的切片上95.2%可串流）與其餘部分的無基礎的前1個結果回退混合。在這個有利的切片上，φ_suf由精確和放鬆的基礎限制在[0.26, 0.281]之間——兩者都是早期的。問題類型產生了顯著但粗略的早期/晚期分裂（Kruskal-Wallis p=0.017，epsilon^2=0.04），直接告知何時學習的推測觸發器值得其成本。

##### **Sensorimotor World Models: Perception for Action via Inverse Dynamics**
2606.20104v1 by Petr Ivashkov, Randall Balestriero, Bernhard Schölkopf

Perception for action suggests that representations of the world should be shaped not by visual fidelity alone, but by their relevance for actions. At the same time, latent JEPA-style world models advocate learning compact predictive states from high-dimensional observations to facilitate the prediction of future states, but end-to-end training of these models is nontrivial because representations may collapse if our only goal is to construct a latent state that is easy to predict. We introduce a sensorimotor world model (SMWM): a latent world model trained end-to-end with inverse dynamics regularization. This single regularizer addresses both issues: it prevents representation collapse and induces action-aligned representations. By forcing latent states to preserve information about the action underlying a transition, it biases the model toward the controllable degrees of freedom of the environment while discarding uncontrollable distractors. This yields stable latent world models trained from offline, reward-free trajectories, without frozen encoders, exponential moving averages, or complex latent regularizers. Empirically, SMWM learns compact, interpretable latent spaces and enables competitive planning performance across simple 2D and 3D control tasks.

摘要：感知行動的理論表明，世界的表徵不應僅由視覺的真實性來塑造，而應根據其對行動的相關性來塑造。與此同時，潛在的JEPA風格世界模型主張從高維觀察中學習緊湊的預測狀態，以促進對未來狀態的預測，但這些模型的端到端訓練並非易事，因為如果我們的唯一目標是構建一個易於預測的潛在狀態，表徵可能會崩潰。我們引入了一個感知運動世界模型（SMWM）：一個通過逆動力學正則化進行端到端訓練的潛在世界模型。這個單一的正則化器解決了這兩個問題：它防止了表徵崩潰並促進了與行動對齊的表徵。通過強迫潛在狀態保留有關轉換背後行動的信息，它使模型偏向於環境中可控的自由度，同時丟棄不可控的干擾因素。這產生了從離線、無獎勵的軌跡中訓練出的穩定潛在世界模型，無需凍結編碼器、指數移動平均或複雜的潛在正則化器。在實證上，SMWM學習了緊湊且可解釋的潛在空間，並在簡單的2D和3D控制任務中實現了具有競爭力的規劃性能。

##### **Hybrid Diffusion Transformer for Instruction-Guided Audio Editing via Rectified Flow**
2606.20101v1 by Liting Gao, Yonggang Zhu, Yaru Chen, Dongyu Wang, Shubin Zhang, Zhenbo Li, Jean-Yves Guillemaut, Wenwu Wang

Audio editing aims to modify specific content in an existing audio clip according to a natural language instruction while preserving the remaining acoustic content. Despite the remarkable progress of diffusion models, existing training-based editing methods mainly rely on the local inductive biases and cross-attention interaction in convolutional U-Net backbones, which often hinder long-range semantic alignment and precise understanding and localization of instructions. In contrast, diffusion transformers provide stronger global modeling and multimodal fusion, but existing editing architectures usually adopt a simple stack of MMDiT and DiT blocks. Applying joint attention over concatenated audio and text tokens in all blocks results in quadratic complexity with respect to token length. To balance editing performance and efficiency, we propose a hybrid two-stage diffusion transformer architecture for instruction-guided audio editing based on rectified flow matching. It performs joint attention over audio and text tokens to establish coarse semantic alignment at low-resolution stage, then switches to alternating joint-attention and cross-attention blocks to refine editing details at high-resolution stage. This coarse-to-fine strategy enables efficient and accurate instruction-guided audio editing. Experiments show that the proposed framework achieves notable performance gains on challenging editing tasks involving overlapping audio events and complex instructions, while substantially improving editing efficiency with a compact model.

摘要：音頻編輯旨在根據自然語言指令修改現有音頻片段中的特定內容，同時保留其餘的聲音內容。儘管擴散模型取得了顯著進展，但現有的基於訓練的編輯方法主要依賴於卷積 U-Net 主幹中的局部歸納偏差和交叉注意力交互，這往往妨礙了長距離語義對齊以及對指令的精確理解和定位。相比之下，擴散Transformer提供了更強的全局建模和多模態融合，但現有的編輯架構通常採用簡單的 MMDiT 和 DiT 模塊堆疊。在所有模塊中對串接的音頻和文本標記應用聯合注意力會導致相對於標記長度的二次複雜度。為了平衡編輯性能和效率，我們提出了一種基於修正流匹配的混合兩階段擴散Transformer架構，用於指令引導的音頻編輯。它在低解析度階段對音頻和文本標記執行聯合注意力，以建立粗略的語義對齊，然後切換到交替的聯合注意力和交叉注意力模塊，以在高解析度階段細化編輯細節。這種由粗到細的策略使得指令引導的音頻編輯變得高效且準確。實驗表明，所提出的框架在涉及重疊音頻事件和複雜指令的挑戰性編輯任務中實現了顯著的性能提升，同時以緊湊的模型顯著提高了編輯效率。

##### **HydraHead: From Head-Level Functional Heterogeneity to Specialized Attention Hybridization**
2606.20097v1 by Zhentao Tan, Wei Chen, Jingyi Shen, Yao Liu, Xu Shen, Yue Wu, Jieping Ye

The quadratic complexity of attention poses a critical bottleneck for long-context processing, spurring interest in hybrid attention designs. Most open-source hybrid models adopt a layer-wise strategy. Yet, prior work has noted the inherent difficulty of integrating Linear Attention (LA) with Full Attention (FA), suggesting that the design space of attention hybridization remains underexplored. To probe this space, we conduct interpretability analysis and observe that layers exhibit block-wise functional similarity, while individual heads within the same layer display distinct functional specialization despite sharing input features. This head-level heterogeneity suggests that the head dimension provides a natural and principled granularity for fusing heterogeneous attention signals. Building on this insight, we introduce HydraHead, a novel architecture that hybridizes FA and LA along the head axis. HydraHead features two key innovations: (1) an interpretability-driven selection strategy that identifies retrieval-critical heads and preserves FA only for them, and (2) a scale-normalized fusion module that reconciles the distributional gap between FA and LA head outputs. By leveraging a three-stage transfer pipeline with parameter reuse and distillation, we achieve high-performance hybrid models with minimal training overhead. Under a unified training setup, HydraHead outperforms other hybrid designs in long-context tasks while maintaining strong general reasoning. With interpretability-driven head selection, it matches a 3:1 layer-wise hybrid's long-context performance at a 7:1 LA-to-FA ratio. Crucially, trained on only 15B tokens, HydraHead achieves over 69% improvement over the baseline at 512K context length, approaching Qwen3.5, a leading model of comparable size with a native context length of 256K. This highlights the significant scaling potential of head-level hybridization.

摘要：注意力的二次複雜性對於長上下文處理構成了關鍵瓶頸，激發了對混合注意力設計的興趣。大多數開源混合模型採用逐層策略。然而，先前的研究指出將線性注意力（LA）與全注意力（FA）整合的固有困難，這表明注意力混合的設計空間仍然未被充分探索。為了探究這一空間，我們進行了解釋性分析，觀察到層之間表現出區塊級的功能相似性，而同一層內的個別頭部儘管共享輸入特徵，卻顯示出明顯的功能專業化。這種頭部級的異質性表明，頭部維度為融合異質注意力信號提供了一種自然且原則性的粒度。基於這一見解，我們引入了HydraHead，一種沿著頭部軸混合FA和LA的新型架構。HydraHead具有兩項關鍵創新：（1）一種以解釋性為驅動的選擇策略，識別檢索關鍵的頭部並僅為它們保留FA，以及（2）一個尺度歸一化的融合模塊，調和FA和LA頭部輸出之間的分佈差距。通過利用一個三階段的轉移管道，結合參數重用和蒸餾，我們實現了高性能的混合模型，並且訓練開銷最小。在統一的訓練設置下，HydraHead在長上下文任務中超越了其他混合設計，同時保持強大的通用推理能力。通過以解釋性驅動的頭部選擇，它在7:1的LA到FA比率下，達到了3:1逐層混合的長上下文性能。至關重要的是，HydraHead僅在15B標記上進行訓練，在512K上下文長度下實現了超過69%的基線改進，接近Qwen3.5，這是一個與之大小相當的領先模型，具有256K的原生上下文長度。這突顯了頭部級混合的顯著擴展潛力。

##### **MakeupMirror: Improving Facial Attribute Preservation in Diffusion Models for Makeup Transfer**
2606.20094v1 by Nefeli Andreou, Angel Martínez-González, Sabine Sternig, Matthieu Guillaumin, Epameinondas Antonakos, Michael Opitz

Makeup transfer models enable fun augmented reality (AR) experiences as well as virtual try-on (VTO) for online makeup shopping. While recent state-of-the-art diffusion based solutions such as Stable-Makeup dramatically improve the accuracy and realism of makeup transfer, they still face limitations in identity and skin color preservation, making production-level VTO for makeup shopping unrealistic. In this work, we propose MakeupMirror, a diffusion-based approach to makeup transfer that makes significant progress towards preserving facial features and skin tone. We introduce several technical innovations over Stable-Makeup: (1) integration of facial geometry conditioning with ControlNets to maintain facial fidelity; (2) region-specific makeup transfer control to enable precise makeup application across facial regions such as skin, eyes and lips; (3) skin tone-based makeup transfer modulation that prevent skin tone alteration in cross-subject transfer scenarios; and (4) integration of a Levenberg-Marquardt Langevin sampler to speed up inference while maintaining generation quality. Our experiments on CPM-Real, Makeup Wild, and (herein newly collected, more diverse) MakeupSelfies datasets show that MakeupMirror improves relative facial recognition similarity by +60%, reduces relative skin tone difference by -50% over Stable-Makeup, with a latency of 0.7s, while achieving expert acceptance rate of 94% across core facial identity preservation criteria.

摘要：化妝轉移模型使得有趣的擴增實境 (AR) 體驗以及在線化妝購物的虛擬試妝 (VTO) 成為可能。雖然最近的最先進擴散基礎解決方案如 Stable-Makeup 顯著提高了化妝轉移的準確性和真實感，但在身份和膚色保留方面仍然面臨限制，使得化妝購物的生產級 VTO 顯得不切實際。在本研究中，我們提出了 MakeupMirror，一種基於擴散的化妝轉移方法，顯著推進了面部特徵和膚色的保留。我們在 Stable-Makeup 的基礎上引入了幾項技術創新：(1) 將面部幾何條件與 ControlNets 整合，以維持面部的真實性；(2) 區域特定的化妝轉移控制，以實現對皮膚、眼睛和嘴唇等面部區域的精確化妝應用；(3) 基於膚色的化妝轉移調制，以防止在跨主體轉移場景中膚色的改變；以及 (4) 整合 Levenberg-Marquardt Langevin 取樣器，以加快推理速度，同時保持生成質量。我們在 CPM-Real、Makeup Wild 和（此處新收集的，更具多樣性的）MakeupSelfies 數據集上的實驗顯示，MakeupMirror 相對於 Stable-Makeup 提高了 +60% 的面部識別相似度，減少了 -50% 的相對膚色差異，延遲為 0.7 秒，同時在核心面部身份保留標準上達到了 94% 的專家接受率。

##### **Self-Preference Is Weak or Absent in Verifiable Instruction-Following Revision: A Four-Model Test Under Genuine Authorship**
2606.20093v1 by William Guey, Pierrick Bougault

Large language models (LLMs) increasingly review and revise text, including their own. A documented self-preference bias (models favoring their own generations when acting as judges) raises the question of whether models also resist valid corrections to their own writing. We test this in a setting where "valid" is decided not by another model but by a deterministic verifier: instruction-following revision on IFEval. A model writes a draft; the official IFEval checker confirms the draft violates a constraint and that a candidate edit fixes it; the model then accepts or rejects that edit either as the genuine in-context author or as a fresh model that sees the draft neutrally. Across four mid-tier model families and 85 author-versus-fresh comparisons, we find no detectable self-preference: authors reject verified-good fixes to their own drafts at essentially the same rate as fresh models judging the same drafts (gap -5.1 pp, 95% CI [-12.9, +2.7]). A self-skepticism hint from a smaller pilot did not replicate at scale. The one robust observation is qualitative: when authors do reject a verified-good fix, 97% of their stated reasons are flaw-catching rather than preference, that is, about the character of rejections, not an elevated rate. Effects smaller than ~13 pp cannot be excluded at this sample size.

摘要：大型語言模型 (LLMs) 日益檢視和修訂文本，包括它們自己的文本。已記錄的自我偏好偏見（模型在擔任評審時偏愛自己的生成內容）引發了模型是否也會抵制對其自身寫作的有效修正的問題。我們在一個設定中測試這一點，其中「有效」不是由另一個模型決定，而是由一個確定性驗證者決定：在 IFEval 上進行的遵循指令的修訂。一個模型撰寫草稿；官方 IFEval 檢查器確認該草稿違反了一個約束，並且一個候選編輯修正了它；然後模型作為真正的上下文作者或作為一個中立地看待草稿的新模型接受或拒絕該編輯。在四個中等規模的模型系列和 85 次作者對新模型的比較中，我們發現沒有可檢測的自我偏好：作者拒絕經過驗證的良好修正的比率基本上與新模型評判相同的草稿的比率相同（差距 -5.1 pp，95% CI [-12.9, +2.7]）。來自一個較小的試點的自我懷疑暗示在大規模上未能重現。唯一的穩健觀察是質性的：當作者拒絕經過驗證的良好修正時，他們所陳述的理由中有 97% 是關於缺陷捕捉而非偏好，即關於拒絕的性質，而不是提高的比率。在這個樣本大小下，無法排除小於 ~13 pp 的效應。

##### **IHUBERT: Vector-Based Semantic Deduplication and Domain-Balanced Pretraining for Persian Resources**
2606.20089v1 by Arash Ghafouri, Mahdi Firouzmandi, Hossein Saberi, Mohammad Reza Hasani Ahangar

Persian pretrained language models (PLMs) are still limited by the scarcity of large-scale, high-quality pretraining corpora and by insufficient evaluation beyond standard classification and NER tasks. We present IHUBERT, a monolingual Persian PLM trained from scratch with the RoBERTa-base encoder (125M parameters) on a 45 GB curated subset of the Sepahr-Danesh collection (about 7-8B tokens). To improve corpus quality and reduce redundancy, we employ a multi-stage preprocessing pipeline that includes normalization, exact and near-duplicate removal, anonymization, and vector-database-based semantic deduplication for distribution balancing control across domains and registers. We additionally train a 139k-vocabulary BPE tokenizer on the full pretraining corpus to better capture Persian morphology and orthographic variation. IHUBERT is evaluated on seven Persian NLU benchmarks covering NER, sentiment analysis, topic classification, NLI, extractive question answering, and relation extraction, using task-standard metrics (entity-level F1, Macro-F1, EM/F1). IHUBERT achieves its strongest gains on extractive QA, ranking first on both PQuAD (F1 88.3542) and ParsiNLU-RC (F1 49.0987), and attains the best result on FarsTail (Macro-F1 0.8350). On NER and topic classification, it remains competitive (e.g., 0.8308 F1 on ParsTwiNER; 0.7953 Macro-F1 on DigiMag), while relation extraction remains the main remaining gap (0.6684 Macro-F1 on PERLEX). A controlled tokenizer ablation on the IHUBERT pretraining corpus shows that BPE yields slightly lower subword fragmentation than WordPiece at matched vocabulary size, supporting our tokenization design. Overall, IHUBERT advances Persian language modeling through semantically curated large-scale pretraining and broad evaluation across both classification and comprehension-oriented tasks.

摘要：波斯語預訓練語言模型（PLMs）仍然受到大規模、高品質預訓練語料庫稀缺以及超出標準分類和命名實體識別（NER）任務的評估不足的限制。<lb/>我們提出了IHUBERT，一個從零開始訓練的單語波斯語PLM，使用RoBERTa-base編碼器（125M參數）在45GB的Sepahr-Danesh集合的精選子集上進行訓練（約7-8B標記）。<lb/>為了提高語料庫質量並減少冗餘，我們採用了多階段的預處理管道，包括正規化、精確和近似重複移除、匿名化，以及基於向量數據庫的語義去重，以控制跨領域和註冊的分佈平衡。<lb/>此外，我們在完整的預訓練語料庫上訓練了一個139k詞彙的BPE分詞器，以更好地捕捉波斯語的形態學和正字法變異。<lb/>IHUBERT在七個涵蓋NER、情感分析、主題分類、自然語言推斷（NLI）、提取式問題回答和關係提取的波斯語自然語言理解基準上進行評估，使用任務標準指標（實體級F1、Macro-F1、EM/F1）。<lb/>IHUBERT在提取式QA上取得了最強的增長，在PQuAD（F1 88.3542）和ParsiNLU-RC（F1 49.0987）上均排名第一，並在FarsTail（Macro-F1 0.8350）上達到最佳結果。<lb/>在NER和主題分類上，它仍然具有競爭力（例如，在ParsTwiNER上0.8308 F1；在DigiMag上0.7953 Macro-F1），而關係提取仍然是主要的剩餘差距（在PERLEX上0.6684 Macro-F1）。<lb/>對IHUBERT預訓練語料庫的受控分詞器消融實驗顯示，BPE在匹配詞彙大小時的子詞碎片化略低於WordPiece，支持我們的分詞設計。<lb/>總體而言，IHUBERT通過語義精選的大規模預訓練和在分類及理解導向任務上的廣泛評估，推進了波斯語言建模。

##### **Residual-Space Evolutionary Optimization via Flow-based Generative Models**
2606.20084v1 by Zhuo Cao, Lena Krieger, Fernanda Nader, Xuan Zhao, Hanno Scharr, Ira Assent

Data editing with generative methods typically requires differentiable objectives and gradient-based search. However, these assumptions break down in flow-based settings, where edits are performed through forward and backward integration and often involve non-differentiable or black-box objectives. We introduce residual-space evolutionary optimization, a model-agnostic framework that addresses this gap by combining flow-based generative editing with evolutionary algorithms. Building on the observation that conditional flow matching (CFM) can disentangle condition-controlled factors from instance-specific residuals, our framework directly operates in residual space and separates two complementary search regimes: self-pollination performs local exploitation through feature-preserving residual refinement, and cross-pollination promotes broader exploration by recombining residuals across heterogeneous samples. As a proof of concept, we validate on MorphoMNIST, a benchmark dataset for counterfactual generation, and on crystal data, demonstrating that this exploration--exploitation decomposition provides a useful mechanism for balancing target alignment, instance preservation, and diversity, and extends beyond images to real-world scientific domains.

摘要：數據編輯使用生成方法通常需要可微分的目標和基於梯度的搜索。<lb/>然而，這些假設在基於流的設置中失效，因為編輯是通過前向和後向積分進行的，並且通常涉及不可微分或黑箱目標。<lb/>我們引入了殘差空間進化優化，這是一種模型無關的框架，通過將基於流的生成編輯與進化算法相結合來解決這一差距。<lb/>基於條件流匹配（CFM）可以將條件控制因素與特定實例的殘差解耦的觀察，我們的框架直接在殘差空間中運作，並分離兩種互補的搜索範疇：自我授粉通過特徵保留的殘差精煉進行局部開發，而交叉授粉則通過在異質樣本之間重新組合殘差來促進更廣泛的探索。<lb/>作為概念驗證，我們在 MorphoMNIST（一個反事實生成的基準數據集）和晶體數據上進行了驗證，展示了這種探索-開發分解提供了一種有用的機制，用於平衡目標對齊、實例保留和多樣性，並且超越了圖像，擴展到現實世界的科學領域。

##### **The Hidden Evolution of Disguised Visual Context inside the VLM**
2606.20077v1 by Wish Suharitdamrong, Tony Alex, Muhammad Awais, Sara Atito

Visual tokens enter Large Language Models (LLMs) as raw, foreign signals. How they are transformed into meaningful representations and interact with the language space depends entirely on the integration architecture. Whether by treating visual tokens as in-context prompts within the input sequence or injecting them directly into the LLM's intermediate layers. A controlled comparison and understanding of how these architectural choices affect visual information and its internal transformation to integrate with the LLM remains underexplored. We provide a fair comparison by evaluating in-context and layer-wise injection VLM integration paradigms under identical training conditions across single image, multi-image, and video benchmarks. In doing so, we uncover a hidden evolution where visual tokens enter the LLM as disguised visual context, raw representations lacking linguistic structure, but are progressively reshaped depending on the integration paradigm, each capturing fundamentally different frequency characteristics of the visual signal. We show that this evolution inside the LLM determines what visual features the VLM can utilize effectively, how visual representations align with the language space, and ultimately how each paradigm performs across different tasks. We further demonstrate that attention allocation alone is insufficient, and that performance is driven by the quality of visual representations at each layer.

摘要：視覺標記作為原始的外部信號進入大型語言模型（LLMs）。它們如何轉化為有意義的表徵並與語言空間互動，完全依賴於整合架構。無論是將視覺標記視為輸入序列中的上下文提示，還是將其直接注入到LLM的中間層中。對於這些架構選擇如何影響視覺信息及其內部轉化以整合到LLM中的控制比較和理解仍然未被充分探討。我們通過在單一圖像、多圖像和視頻基準下，在相同的訓練條件下評估上下文和層級注入的VLM整合範式，提供了一個公平的比較。在此過程中，我們揭示了一種隱藏的演變，視覺標記作為偽裝的視覺上下文進入LLM，這些原始表徵缺乏語言結構，但根據整合範式逐步被重塑，每種範式捕捉到視覺信號的根本不同頻率特徵。我們顯示，這種在LLM內部的演變決定了VLM能有效利用哪些視覺特徵，視覺表徵如何與語言空間對齊，以及最終每種範式在不同任務中的表現。我們進一步證明，僅僅分配注意力是不夠的，性能是由每層的視覺表徵質量驅動的。

##### **Variable-Length Tokenization via Learnable Global Merging for Diffusion Transformers**
2606.20076v1 by Dong Hoon Lee, Seunghoon Hong

Latent Diffusion Models (LDMs) have become dominant in visual synthesis, but their quality-compute trade-off is largely constrained by the tokenizer's fixed compression ratio. Variable-length tokenizers (VLTs) promise adaptive compression by varying token counts, allowing diffusion models to flexibly balance quality and compute. However, conventional VLTs modulate length by truncating ordered token sequences, which makes token semantics depend on token position and breaks representational alignment across lengths. This leads to a cross-length shift in the latent distribution that hinders a single variable-length diffusion model from operating effectively. To address this, we propose a novel variable-length tokenizer that modulates length by merging tokens. We show that encouraging similar tokens to merge enables direct cross-length representation alignment when the diffusion transformer operates according to the merging pattern. Since conventional merging methods are data-dependent, making the merging pattern inaccessible during generation, we introduce learnable global merging, which is data-independent, to ensure compatibility with diffusion transformers. On ImageNet 256$\times$256 generation, our merging-based variable-length tokenizer integrated with a diffusion transformer achieves a superior gFID-compute trade-off compared to prior VLT methods. Code is available at [this https URL](https://github.com/movinghoon/lgm)

摘要：潛在擴散模型（LDMs）在視覺合成中已成為主流，但其質量與計算的權衡在很大程度上受到標記器固定壓縮比的限制。可變長度標記器（VLTs）通過變化標記數量來承諾自適應壓縮，使擴散模型能夠靈活平衡質量和計算。然而，傳統的 VLT 通過截斷有序標記序列來調節長度，這使得標記語義依賴於標記位置，並破壞了跨長度的表示對齊。這導致潛在分佈在不同長度之間的偏移，妨礙了單一可變長度擴散模型的有效運作。為了解決這個問題，我們提出了一種新穎的可變長度標記器，通過合併標記來調節長度。我們展示了鼓勵相似標記合併使得在擴散Transformer根據合併模式運作時，能夠實現直接的跨長度表示對齊。由於傳統的合併方法依賴於數據，這使得在生成過程中無法訪問合併模式，我們引入了可學習的全局合併，這是數據無關的，以確保與擴散Transformer的兼容性。在 ImageNet 256$\times$256 的生成中，我們基於合併的可變長度標記器與擴散Transformer的整合相比於先前的 VLT 方法實現了更優越的 gFID-計算權衡。代碼可在 [這個 https URL](https://github.com/movinghoon/lgm) 獲得。

##### **Evaluation of EEG Foundation Models for Event-Based Burst-Suppression Detection in ICU**
2606.20074v1 by Elisa Vasta, Thorir Mar Ingolfsson, Andrea Cossettini, Luca Benini, Tilman Beck, Emanuela Keller, Una Pale

Burst suppression (BS) is a clinically relevant electroencephalographic (EEG) pattern used to monitor sedation depth and brain activity in critically ill patients, particularly during induced coma in Intensive Care Units (ICUs). Automatic burst detection remains challenging because BS patterns vary substantially between patients and annotated datasets are scarce. Recently, EEG Foundation Models (FMs) have shown promise across several downstream EEG applications, but their usefulness for BS detection remains unexplored. We present the first study to evaluate EEG FMs for burst detection in reduced-montage ICU EEG without patient-specific calibration. We compare REVE-base, LUNA-large and LuMamba-Tiny with an adaptive thresholding baseline and a task-specific EEGNet baseline. Additionally, we complement conventional EEG window-based classification with event-based burst detection evaluation. This helps assessing clinically whether burst episodes are correctly detected, reducing the impact of expected annotation variability. The best model, REVE-base, achieved the highest event-based F1-score ($0.868 \pm 0.167$) and reduced burst-per-minute error by 52.1% and 36.2% compared to EEGNet and adaptive thresholding respectively, supporting FMs for scalable EEG monitoring in ICU. Ablation experiments showed that full fine-tuning was the most effective adaptation strategy with respect to frozen-backbone training, two-step fine-tuning, and LoRA-based adaptation, improving event-based F1-score over frozen-backbone training by up to $+0.102$ for LUNA-large. With reduced labeled datasets, pretrained REVE-base outperformed random initialization by $+0.723$ event-based F1 points at 25% of the cohort, demonstrating the benefit of pretraining FM representations when adapted to burst detection with limited labeled data.

摘要：爆發抑制（BS）是一種臨床相關的腦電圖（EEG）模式，用於監測重症患者的鎮靜深度和腦部活動，特別是在重症監護病房（ICU）中誘導昏迷期間。自動爆發檢測仍然具有挑戰性，因為BS模式在患者之間變化很大，且標註數據集稀缺。最近，EEG基礎模型（FMs）在幾個下游EEG應用中顯示出潛力，但它們在BS檢測中的有效性仍未被探索。我們提出了第一項研究，評估EEG FMs在無需患者特定校準的減少電極配置的ICU EEG中進行爆發檢測。我們將REVE-base、LUNA-large和LuMamba-Tiny與自適應閾值基線和任務特定的EEGNet基線進行比較。此外，我們補充了傳統的基於EEG窗口的分類，進行事件驅動的爆發檢測評估。這有助於臨床評估爆發事件是否被正確檢測，減少預期標註變異性的影響。最佳模型REVE-base達到了最高的事件基於F1分數（$0.868 \pm 0.167$），並且與EEGNet和自適應閾值相比，減少了每分鐘爆發錯誤分別為52.1%和36.2%，支持FMs在ICU中進行可擴展的EEG監測。消融實驗顯示，完全微調是相對於凍結骨幹訓練、兩步微調和基於LoRA的適應策略中最有效的適應策略，對於LUNA-large，事件基於F1分數相比凍結骨幹訓練提高了最多$+0.102$。在標註數據集減少的情況下，預訓練的REVE-base在25%的樣本中超越了隨機初始化，事件基於F1分數提高了$+0.723$，顯示了在有限標註數據下適應爆發檢測時預訓練FM表示的好處。

##### **Source-Grounded Data Generation for Text-to-JSON Learning**
2606.20072v1 by Sunghee Ahn, Guijin Son, Youngjae Yu

From financial filings to clinical records, legacy industries rely heavily on long, unstructured documents to store high-value information. Reliably extracting this information into structured, machine-readable representations is a key prerequisite to making the contents accessible to automated systems. JSON is a natural target for such structured extraction, yet constructing reliable and scalable text-to-JSON training data remains challenging. To address this gap, we propose STAGE (Spreadsheet-grounded Text-to-JSON Artifact GEneration), a source-grounded data generation pipeline that constructs reports and JSON schema by using LLMs for scalable synthesis while validating ground-truth values against the underlying spreadsheet. Evaluations on STAGE-Eval, our source-grounded benchmark with an 851-example test set, show that STAGE produces stronger training data than existing approaches. This improves Qwen3-4B exact match from 31.37% to 74.27% and value accuracy from 45.46% to 90.69%.

摘要：從財務申報到臨床記錄，傳統產業在儲存高價值資訊時，極度依賴長且非結構化的文件。可靠地將這些資訊提取為結構化、機器可讀的表示形式，是使內容能夠被自動化系統訪問的關鍵前提。JSON 是這種結構化提取的自然目標，但構建可靠且可擴展的文本到 JSON 的訓練數據仍然具有挑戰性。為了解決這一問題，我們提出了 STAGE（基於電子表格的文本到 JSON 藝術品生成），這是一個基於來源的數據生成管道，通過使用 LLM 進行可擴展的合成，同時將真實值與基礎電子表格進行驗證，來構建報告和 JSON 架構。在我們的基準測試 STAGE-Eval 上進行的評估顯示，STAGE 生成的訓練數據比現有方法更強大。這使 Qwen3-4B 的精確匹配從 31.37% 提高到 74.27%，值準確率從 45.46% 提高到 90.69%。

##### **Process-Verified Reinforcement Learning for Theorem Proving via Lean**
2606.20068v1 by Minsu Kim, Se-Young Yun

While reinforcement learning from verifiable rewards (RLVR) typically has relied on a single binary verification signal, symbolic proof assistants in formal reasoning offer rich, fine-grained structured feedback. This gap between structured processes and unstructured rewards highlights the importance of feedback that is both dense and sound. In this work, we demonstrate that the Lean proof assistant itself can serve as a symbolic process oracle, supplying both outcome-level and fine-grained tactic-level verified feedback during training. Proof attempts are parsed into tactic sequences, and Lean's elaboration marks both locally sound steps and the earliest failing step, yielding dense, verifier-grounded credit signals rooted in type theory. We incorporate these structured rewards into a GRPO-style reinforcement learning objective with first-error propagation and first-token credit methods that balances outcome- and process-level advantages. Experiments with STP-Lean and DeepSeek-Prover-V1.5 show that tactic-level supervision outperforms outcome-only baselines in most settings, delivering improvements on benchmarks such as MiniF2F and ProofNet. Beyond empirical gains, our study highlights a broader perspective: symbolic proof assistants are not only verifiers at evaluation time, but can also act as process-level reward oracles during training. This opens a path toward reinforcement learning frameworks that combine the scalability of language models with the reliability of symbolic verification for formal reasoning.

摘要：在可驗證獎勵的強化學習（RLVR）通常依賴於單一的二元驗證信號時，形式推理中的符號證明助手提供了豐富且細緻的結構化反饋。這種結構化過程與非結構化獎勵之間的差距突顯了密集且可靠的反饋的重要性。在這項工作中，我們展示了 Lean 證明助手本身可以作為符號過程的神諭，在訓練過程中提供結果級別和細緻的策略級別的驗證反饋。證明嘗試被解析為策略序列，而 Lean 的擴展標記了本地可靠的步驟和最早失敗的步驟，產生根植於類型理論的密集、驗證者基礎的信用信號。我們將這些結構化獎勵納入 GRPO 風格的強化學習目標，使用首次錯誤傳播和首次標記信用的方法，平衡結果級別和過程級別的優勢。使用 STP-Lean 和 DeepSeek-Prover-V1.5 的實驗顯示，策略級別的監督在大多數設置中優於僅結果的基準，並在 MiniF2F 和 ProofNet 等基準上取得了改進。除了實證增益，我們的研究還突顯了一個更廣泛的視角：符號證明助手不僅在評估時是驗證者，還可以在訓練過程中充當過程級別的獎勵神諭。這為結合語言模型的可擴展性與符號驗證在形式推理中的可靠性的強化學習框架開辟了一條道路。

##### **Generative Engine Optimization at Scale: Measuring Brand Visibility Across AI Search Engines**
2606.20065v1 by Pratyush Kumar

People increasingly get answers straight from AI search engines like ChatGPT, Claude, Perplexity, and Gemini rather than scrolling search results. Brands that once focused on search engine optimization (SEO) must now optimize for how these engines represent, cite, and recommend them -- a shift variously called Generative Engine Optimization (GEO), Answer Engine Optimization (AEO), and AI Search Visibility. We treat AEO and AI Visibility as part of GEO, and study how to measure brand visibility across AI engines: what they value when they cite a brand, which sources they rely on, and what content large language models surface. The hard case is everyone outside the already-authoritative top brands -- SMEs, D2C brands, creators, and early-stage startups.
  We analyze 100K+ prompt responses across 100+ brands tracked on Ranqo between March and May 2026. First visibility runs form a clear three-tier brand-stature ladder: global household names (e.g., Stripe, Nike) appear in 73% of relevant AI answers on their first run; established mid-market and regional brands (e.g., Olipop, Klaviyo) in 44%; niche and small brands in just 11% -- about 30 percentage points per step. When engines cite sources, about 78% go to corporate websites; among non-corporate sources YouTube leads, ahead of Reddit, editorial media, and Wikipedia. The highest-leverage page is the ranked "best-of" listicle, the most-cited content format at about 21% of all citations. Sentiment is the unstable signal: whether a brand is framed positively or negatively flips about 6.7 times more often than whether it is mentioned at all.
  These findings provide a first large-scale baseline for measuring GEO: AI brand visibility can be measured, differs by platform, and varies strongly by brand maturity. We close by proposing seven v1.1 protocols to test whether specific recommendations can causally improve AI visibility.

摘要：人們越來越多地直接從像 ChatGPT、Claude、Perplexity 和 Gemini 這樣的 AI 搜尋引擎獲得答案，而不是滾動搜尋結果。曾經專注於搜尋引擎優化（SEO）的品牌現在必須優化這些引擎如何代表、引用和推薦他們——這一變化被稱為生成引擎優化（GEO）、答案引擎優化（AEO）和 AI 搜尋可見性。我們將 AEO 和 AI 可見性視為 GEO 的一部分，並研究如何衡量品牌在 AI 引擎中的可見性：它們在引用品牌時重視什麼，依賴哪些來源，以及大型語言模型展示哪些內容。困難的情況是所有非已具權威性的頂級品牌——中小企業、D2C 品牌、創作者和早期階段的初創公司。

我們分析了 2026 年 3 月至 5 月期間在 Ranqo 上追踪的 100 多個品牌的 100K+ 提示回應。第一次的可見性運行形成了一個明確的三層品牌地位階梯：全球知名品牌（例如 Stripe、Nike）在首次運行中出現在 73% 的相關 AI 答案中；已建立的中型市場和區域品牌（例如 Olipop、Klaviyo）為 44%；小眾和小型品牌僅為 11%——每一步約 30 個百分點。當引擎引用來源時，約 78% 指向企業網站；在非企業來源中，YouTube 排名第一，領先於 Reddit、編輯媒體和維基百科。最高效的頁面是排名的「最佳」列表，這是約 21% 所有引用中最常被引用的內容格式。情感是一個不穩定的信號：品牌被正面或負面框架的頻率大約是被提及的 6.7 倍。

這些發現為衡量 GEO 提供了第一個大規模基準：AI 品牌可見性可以被衡量，並且因平台而異，並且隨品牌成熟度而強烈變化。我們最後提出七個 v1.1 協議，以測試特定建議是否能因果改善 AI 可見性。

##### **See-and-Reach: Precise Vision-Language Navigation for UAVs within the Field of View**
2606.20045v1 by Fanfu Xue, En Yu, Yantian Shen, Zhikun Hu, Hongjun Wang, Yang Yang, Xindi Wang, Jiande Sun

UAV Vision-Language Navigation (UAV-VLN) is typically formulated as a holistic search-and-reach problem, where long-range target discovery and final target approach are optimized and evaluated jointly. This formulation makes it difficult to assess a critical capability of aerial embodied agents, namely whether a UAV can accurately ground a visible target and translate vision-language evidence into precise 3D motion once the target enters its field of view. To address this limitation, we introduce UAV-VLN-FOV, a target-visible navigation task that isolates the see-and-reach stage and enables a more diagnostic evaluation of terminal reaching ability. We further propose 3DG-VLN, a vision-language waypoint prediction framework guided by dynamic 3D direction cues to enhance fine-grained visual grounding and spatial direction alignment for precise target reaching. Specifically, 3DG-VLN adaptively processes high-resolution front-view and downward-view observations to preserve fine-grained visual and geometric details for target grounding. It also updates the target-relative direction online during closed-loop navigation, allowing the agent to maintain spatial alignment with the target and reduce accumulated direction drift. To support this task, we construct a dedicated high-resolution benchmark which contains 2,717 trajectories with target-oriented high-level instructions, high-resolution front-view and downward-view egocentric observations, and continuous 3D waypoint annotations. Experiments show that 3DG-VLN outperforms competitive UAV-VLN baselines, achieving a 13.82\% improvement in success rate. Real-world trials further demonstrate the potential of 3DG-VLN for practical see-and-reach navigation. The source code and benchmark are available at https://github.com/xuefanfu/3DG-VLN.

摘要：無人機視覺-語言導航（UAV-VLN）通常被表述為一個整體的搜索與到達問題，其中長距離目標發現和最終目標接近是共同優化和評估的。這種表述使得評估空中具身代理的一項關鍵能力變得困難，即無人機是否能準確地將可見目標定位並在目標進入其視野後將視覺-語言證據轉化為精確的三維運動。為了解決這一限制，我們引入了UAV-VLN-FOV，一個目標可見導航任務，該任務隔離了看見與到達階段，並使終端到達能力的診斷評估更加明確。我們進一步提出了3DG-VLN，一個由動態三維方向提示引導的視覺-語言路徑點預測框架，以增強精細的視覺定位和空間方向對齊，以實現精確的目標到達。具體而言，3DG-VLN自適應地處理高分辨率的前視和向下視觀察，以保留目標定位的精細視覺和幾何細節。它還在閉環導航過程中在線更新目標相關方向，允許代理保持與目標的空間對齊並減少累積的方向漂移。為了支持這一任務，我們構建了一個專用的高分辨率基準，該基準包含2,717條具有目標導向高級指令的軌跡、高分辨率的前視和向下視自我中心觀察，以及連續的三維路徑點標註。實驗顯示，3DG-VLN在成功率上超越了競爭性的UAV-VLN基準，實現了13.82\%的提升。實際世界的試驗進一步展示了3DG-VLN在實際的看見與到達導航中的潛力。源代碼和基準可在https://github.com/xuefanfu/3DG-VLN獲得。

##### **AI Economist Agent: An Agentic Framework for Model-Grounded Economic Analysis with RAG, Knowledge Graphs, and Large Language Models**
2606.20041v1 by Masahiro Kato

We propose a model-grounded RAG-based AI economist with an agentic framework for economic scenario analysis using large language models (LLMs) and knowledge graphs. While LLMs can generate fluent economic narratives, economists are often required to make economic claims grounded by economic theory and real-world data. Based on this motivation, this study proposes an RAG-based AI economist, which utilizes knowledge graphs including economic data and theory and LLM-based agents to plan the analysis, retrieve relevant evidence, select appropriate models, and generate reports. In our framework, we do not produce quantitative claims directly with the language model alone; instead, we generate narratives grounded in explicit model-based computations and linked to the retrieved evidence via AI agents. We refer to our framework as an AI economist agent. We evaluate the AI economist agent in two applications: economist report generation for U.S. inflation persistence and Federal Reserve policy, and bank stress-test narrative generation for U.S. commercial real estate refinancing stress. The results illustrate how grounding the generated reports improves their economic coherence and traceability.

摘要：我們提出了一個基於模型的RAG（檢索增強生成）AI經濟學家，具有代理框架，用於利用大型語言模型（LLMs）和知識圖譜進行經濟情境分析。雖然LLMs可以生成流暢的經濟敘述，但經濟學家通常需要根據經濟理論和現實數據來提出經濟主張。基於這一動機，本研究提出了一個基於RAG的AI經濟學家，它利用包含經濟數據和理論的知識圖譜以及基於LLM的代理來規劃分析、檢索相關證據、選擇適當的模型並生成報告。在我們的框架中，我們不僅僅依賴語言模型直接產生定量主張；相反，我們生成基於明確模型計算的敘述，並通過AI代理將其與檢索到的證據相連結。我們將我們的框架稱為AI經濟學家代理。我們在兩個應用中評估AI經濟學家代理：針對美國通脹持續性和聯邦儲備政策的經濟學家報告生成，以及針對美國商業房地產再融資壓力的銀行壓力測試敘述生成。結果顯示，將生成的報告與經濟理論相結合，提高了其經濟一致性和可追溯性。

##### **When Lower Privileges Suffice: Investigating Over-Privileged Tool Selection in LLM Agents**
2606.20023v1 by Kaiyue Yang, Yuyan Bu, Jingwei Yi, Yuchi Wang, Biyu Zhou, Juntao Dai, Songlin Hu, Yaodong Yang

As LLM agents increasingly select tools autonomously, their choices among tools with different privileges become safety-relevant. However, prior tool-selection studies focus on safety-agnostic metadata preferences, leaving privilege-sensitive choices underexplored. To address this gap, we study over-privileged tool selection, in which an agent selects or escalates to a higher-privilege tool despite a sufficient lower-privilege alternative. We introduce ToolPrivBench to evaluate whether agents choose higher-privilege tools despite sufficient lower-privilege alternatives, measuring both initial selection and escalation after transient tool failures. Across eight domains and five recurring risk patterns, we find that over-privileged tool selection is common among mainstream LLM agents and is further amplified by transient failures. We further find that general safety alignment does not reliably transfer to least-privilege tool choice, while prompt-level controls provide only limited mitigation under transient failures. We therefore introduce a privilege-aware post-training defense that teaches agents to prefer sufficient lower-privilege tools and escalate only when necessary. Our mitigation experiments show that this defense substantially reduces unnecessary high-privilege tool use while preserving general capabilities.

摘要：隨著大型語言模型代理越來越自主地選擇工具，它們在不同權限的工具之間的選擇變得與安全性相關。然而，先前的工具選擇研究專注於與安全無關的元數據偏好，對於權限敏感的選擇探討不足。為了填補這一空白，我們研究了過度權限的工具選擇，其中代理在有足夠的低權限替代方案的情況下選擇或升級到更高權限的工具。我們引入了ToolPrivBench來評估代理是否在有足夠的低權限替代方案的情況下仍然選擇更高權限的工具，並測量初始選擇和在瞬時工具故障後的升級。在八個領域和五種反覆出現的風險模式中，我們發現過度權限的工具選擇在主流大型語言模型代理中很常見，並且在瞬時故障的情況下進一步加劇。我們還發現，通用安全對齊並不可靠地轉移到最低權限的工具選擇，而提示級別的控制在瞬時故障下僅提供有限的緩解。因此，我們引入了一種權限感知的後訓練防禦，教導代理優先選擇足夠的低權限工具，並僅在必要時升級。我們的緩解實驗顯示，這種防禦顯著減少了不必要的高權限工具使用，同時保留了通用能力。

##### **Hierarchical Control in Multi-Agent Games: LLM-based Planning and RL Execution**
2606.20014v1 by Jannik Hösch, Alessandro Sestini, Florian Fuchs, Amir Baghi, Joakim Bergdahl, Konrad Tollmar, Jean-Philippe Barrette-LaPierre, Linus Gisslén

Reinforcement learning (RL) has achieved strong performance in sequential decision-making, yet scaling to complex multi-agent environments remains challenging due to sparse rewards, large state-action spaces, and the difficulty of learning coordinated strategies. We propose a hierarchical architecture where a pretrained large language model (LLM) acts as a centralized strategic controller that selects among specialized RL skill policies for a team of agents, while RL policies handle reactive low-level execution. We evaluate this hybrid system in a competitive 2v2 King of the Hill environment against behavior tree (BT) and \emph{``Flat''} RL (end-to-end training without skill decomposition) baselines. The LLM+RL system achieves task performance statistically equivalent to hand-crafted BT (46.4\% vs 51.5\% win rate, $p=0.103$) while both significantly outperform Flat RL trained without skill decomposition. A user study ($n=15$) reveals that 60\% of participants perceive LLM+RL agents as the most human-like ($p=0.027$), citing behavioral adaptability and tactical variability. These results demonstrate that pretrained LLM reasoning can effectively orchestrate pretrained RL skills, achieving competitive multi-agent coordination and superior perceived believability without manual rule engineering.

摘要：強化學習（RL）在序列決策中取得了良好的表現，但由於獎勵稀疏、大的狀態-動作空間以及學習協調策略的困難，擴展到複雜的多智能體環境仍然具有挑戰性。我們提出了一種層次架構，其中一個預訓練的大型語言模型（LLM）作為集中式戰略控制器，為一組智能體選擇專門的RL技能策略，而RL策略則處理反應性的低層執行。我們在競爭性的2對2《山丘之王》環境中評估這個混合系統，並與行為樹（BT）和\emph{``平面''} RL（無技能分解的端到端訓練）基準進行比較。LLM+RL系統在任務表現上統計上等同於手工製作的BT（46.4\%對51.5\%的勝率，$p=0.103$），而且兩者都顯著優於無技能分解的平面RL訓練。用戶研究（$n=15$）顯示，60\%的參與者認為LLM+RL智能體最具人類特徵（$p=0.027$），並提到行為適應性和戰術變化性。這些結果表明，預訓練的LLM推理可以有效地協調預訓練的RL技能，實現競爭性的多智能體協作和優越的感知可信度，而無需手動規則工程。

##### **StreamKL: Fast and Memory-Efficient KL Divergence for Boosting Attention Distillation**
2606.20005v1 by Guangda Liu, Yiquan Wang, Chengwei Li, Wenhao Chen, Jing Lin, Yiwu Yao, Danning Ke, Wenchao Ding, Jieru Zhao

Attention distillation, which trains one attention distribution to match another by minimizing their Kullback-Leibler (KL) divergence, is widely used in knowledge distillation, model compression, continual learning, and sparse-attention LLM training. However, existing approaches materialize both attention distributions before computing the KL reduction, incurring $O(N_QN_K)$ memory and IO costs that become prohibitive at long context lengths. We present StreamKL, the first fused GPU primitive for attention KL divergence that eliminates this quadratic materialization. StreamKL derives a novel online formulation for the coupled two-distribution KL reduction, enabling a single one-pass forward kernel that streams query-key tiles through on-chip SRAM. For the backward pass, StreamKL recomputes attention probabilities tile-by-tile, avoiding storage of quadratic intermediates. We further design and implement efficient GPU kernels with dedicated optimizations. Experiments show StreamKL delivers up to $43\times$ and $14\times$ speedups over baseline methods in the forward and backward passes, respectively. Most importantly, StreamKL reduces the extra HBM footprint of attention distillation from $O(N_QN_K)$ to $O(1)$, enabling long-context distillation on a single GPU.

摘要：注意力蒸餾通過最小化兩者的 Kullback-Leibler (KL) 散度來訓練一個注意力分佈以匹配另一個，廣泛應用於知識蒸餾、模型壓縮、持續學習和稀疏注意力 LLM 訓練。<lb/>然而，現有的方法在計算 KL 減少之前會實現兩個注意力分佈，造成 $O(N_QN_K)$ 的內存和 IO 成本，這在長上下文長度下變得難以承受。<lb/>我們提出了 StreamKL，首個融合的 GPU 原語，用於注意力 KL 散度，消除了這種二次材料化。<lb/>StreamKL 為耦合的雙分佈 KL 減少導出了新穎的在線公式，實現了一個單次前向內核，通過片上 SRAM 流式處理查詢-鍵塊。<lb/>在反向傳播中，StreamKL 逐塊重新計算注意力概率，避免了二次中間結果的存儲。<lb/>我們進一步設計並實現了具有專門優化的高效 GPU 核心。<lb/>實驗顯示，StreamKL 在前向和反向傳播中分別比基線方法提供高達 $43\times$ 和 $14\times$ 的加速。<lb/>最重要的是，StreamKL 將注意力蒸餾的額外 HBM 足跡從 $O(N_QN_K)$ 減少到 $O(1)$，使得在單個 GPU 上進行長上下文蒸餾成為可能。

##### **Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning**
2606.20002v1 by Yanxi Chen, Weijie Shi, Yuexiang Xie, Boyi Hu, Yaliang Li, Bolin Ding, Jingren Zhou

This work presents a general framework for training large language models (LLMs) to "Connect the Dots" (CoD), a meta-capability required by long-lifecycle agents: as an LLM-based AI agent gets deployed in an environment, it solves a long sequence of tasks while continuously exploring the environment, learning from its own experiences, and iteratively self-updating its context about the environment, thereby achieving progressively better performance on future tasks conditioned on the updated context. Major components of the CoD framework include: (1) algorithm design and infrastructure for end-to-end reinforcement learning (RL) with long rollout sequences interleaving solve-task and update-context episodes; (2) tasks and environments for incentivizing and eliciting the targeted meta-capability in LLMs during training, as well as for faithfully measuring progress during evaluation. We present proof-of-concept implementations of the CoD framework, including a GRPO-style RL algorithm with fine-grained credit assignment, as well as tasks and environments tailored to the targeted meta-capability (rather than domain-specific LLM capabilities or standard task-by-task RL). Empirical results validate the efficacy of end-to-end RL training in the CoD setting, and demonstrate the potential for out-of-distribution generalization -- within the training domains, across different domains, and from CoD to Ralph-loop settings -- of the elicited meta-capability. Our investigation of CoD connects several lines of prior works, and opens up new opportunities for advancing LLMs and AI agents. To facilitate further research and applications, we release our implementations at \url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}.

摘要：這項工作提出了一個訓練大型語言模型（LLMs）以「連接點」(CoD) 的一般框架，這是一種長生命周期代理所需的元能力：當基於LLM的AI代理在環境中部署時，它解決了一系列長任務，同時不斷探索環境，從自身經驗中學習，並迭代地自我更新其對環境的上下文，從而在基於更新上下文的未來任務上實現逐步更好的表現。CoD框架的主要組成部分包括：(1) 用於端到端強化學習（RL）的算法設計和基礎設施，長的展開序列交錯解決任務和更新上下文的情節；(2) 用於激勵和引發LLMs在訓練過程中目標元能力的任務和環境，以及在評估過程中忠實測量進展的工具。我們展示了CoD框架的概念驗證實現，包括一種具有細粒度信用分配的GRPO風格RL算法，以及針對目標元能力量身定制的任務和環境（而不是特定領域的LLM能力或標準的逐任務RL）。實證結果驗證了在CoD設置中端到端RL訓練的有效性，並展示了引發的元能力在訓練領域內、跨不同領域以及從CoD到Ralph-loop設置的分佈外泛化潛力。我們對CoD的研究連結了幾條先前工作的線索，並為推進LLMs和AI代理開辟了新的機會。為了促進進一步的研究和應用，我們在 \url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod} 發布了我們的實現。

##### **Tri-Info: Generalizable, Interpretable Failure Prediction for VLA Models via Information Theory**
2606.19998v1 by Jinghan Yang, Yunchao Zhang, Wang Yuan, Haolun Wan, Jiaming Zhang, Zhengyang Hu, Yanchao Yang

Vision-Language-Action (VLA) models are increasingly deployed across diverse tasks, yet they remain black boxes whose physical interactions can cause irreversible harm, making generalizable and interpretable failure detection essential. We observe that successful and failed rollouts carry systematically different information-theoretic signatures. Building on this, we formalize VLA control as a closed-loop information pipeline and derive the Triple Information-theoretic (Tri-Info) signals that capture whether actions remain diverse, temporally consistent, and coupled to state transitions. Across six VLA models and three benchmark environments, Tri-Info matches the strongest baselines in-domain. Moreover, Tri-Info transfers across architectures, environments, and the sim-to-real gap without retraining, reaching 83\% accuracy on real-world tasks where prior detectors collapse to chance. This establishes Tri-Info as a simple yet powerful method that not only detects failures with strong cross-domain generalization, but also delivers interpretable diagnostics of the underlying failure modes.

摘要：視覺-語言-行動（VLA）模型越來越多地應用於各種任務，但它們仍然是黑箱，其物理互動可能造成不可逆的傷害，因此可泛化和可解釋的故障檢測變得至關重要。我們觀察到成功和失敗的展開在信息理論簽名上系統性地不同。在此基礎上，我們將VLA控制形式化為一個閉環信息管道，並推導出三重信息理論（Tri-Info）信號，以捕捉行動是否保持多樣性、時間一致性以及與狀態轉換的耦合。在六個VLA模型和三個基準環境中，Tri-Info在領域內匹配最強的基準。此外，Tri-Info在不同架構、環境和模擬到現實的差距中無需重新訓練即可轉移，並在現實世界任務中達到83\%的準確率，而先前的檢測器則崩潰至隨機水平。這確立了Tri-Info作為一種簡單而強大的方法，不僅能夠以強大的跨域泛化檢測故障，還能提供對潛在故障模式的可解釋診斷。

##### **Beyond Static Endpoints: Tool Programs as an Interface for Flexible Agentic Web Services**
2606.19992v1 by Mugeng Liu, Shuoqi Li, Yixuan Zhang, Yun Ma

In the agentic web era, LLM-based agents increasingly invoke web services as tools, yet most interfaces remain \emph{static endpoints} that poorly express long-horizon workflows with loops, conditionals, joins, and retries. We present ToolPro, which represents an agent's tool intent as an \emph{executable tool program} that compactly encodes multi-step service interactions with explicit effect types. ToolPro combines constraint-guided program construction, effect-aware replay for exactly-once state-modifying calls, and a profile-driven policy that decides when program execution outperforms stepwise calling. We instantiate ToolPro over MCP-style services with WebAssembly sandboxing and evaluate it on diverse workflows of real-world applications. ToolPro reduces end-to-end latency by up to 53.4\% and client-side traffic by up to 96.1\%, with larger gains under higher network latency and workflow complexity.

摘要：在代理網絡時代，基於大型語言模型的代理越來越多地將網絡服務作為工具，但大多數介面仍然是\emph{靜態端點}，無法有效表達具有循環、條件、聯接和重試的長期工作流程。我們提出了ToolPro，它將代理的工具意圖表示為\emph{可執行工具程序}，緊湊地編碼了具有明確效果類型的多步服務交互。ToolPro結合了約束引導的程序構建、效果感知的重放以確保狀態修改調用的精確一次性，以及一種基於配置的策略，決定何時程序執行優於逐步調用。我們在具有WebAssembly沙箱的MCP風格服務上實現了ToolPro，並在各種現實應用的工作流程上進行了評估。ToolPro將端到端延遲降低了最多53.4\%，客戶端流量降低了最多96.1\%，在更高的網絡延遲和工作流程複雜性下獲得了更大的收益。

##### **Reward as An Agent for Embodied World Models**
2606.19990v1 by Pu Li, Zhigang Lin, Qiang Wu, Yongxuan Lv, Fei Wang, Shan You

While RL has become a promising tool for refining world models, existing methods largely rely on conservative rollouts near the training distribution, limiting exploration, behavioral diversity, and richer dynamic discovery. In this work, we challenge this conservative paradigm. We argue that the core limitation is not exploration itself, but the lack of reliable verification strategies to support broader exploration. Without reliable verification, expanded exploration becomes highly susceptible to reward hacking, where policies exploit imperfect rewards without achieving genuine improvement. To evaluate this motivation, we instantiate our method in embodied world models, where physical plausibility, and task completion provide a rigorous testbed for scalable RL under complex dynamics. On the verification side, we introduce Reward as an Agent, an agentic reward framework that actively evaluates generated behaviors to provide robust reward signals and mitigate reward hacking under distribution shifts. On the exploration side, we introduce Dynamic-Aware Rollout Diversification through DynDiff-GRPO, which explicitly expands action-space exploration to diversify trajectories, broaden state-action coverage, and encourage richer embodied behaviors beyond conservative rollout regimes. By unifying Reward as an Agent with DynDiff-GRPO, we enable RL on a more reliable reward foundation with substantially diversified sampling, effectively mitigating reward hacking while yielding significant accuracy gains across multiple open-source world models, thereby demonstrating that broader exploration can scale successfully when grounded in robust verification.

摘要：雖然強化學習（RL）已成為精煉世界模型的有前景工具，但現有方法在很大程度上依賴於接近訓練分佈的保守展開，限制了探索、行為多樣性和更豐富的動態發現。<lb/>在這項工作中，我們挑戰這一保守的範式。<lb/>我們認為核心限制並不是探索本身，而是缺乏可靠的驗證策略來支持更廣泛的探索。<lb/>沒有可靠的驗證，擴展探索變得非常容易受到獎勵操縱的影響，政策利用不完美的獎勵而未能實現真正的改進。<lb/>為了評估這一動機，我們在具體化的世界模型中實現我們的方法，這些模型中物理的合理性和任務完成提供了一個在複雜動態下可擴展強化學習的嚴格測試平台。<lb/>在驗證方面，我們引入了作為代理的獎勵，這是一種主動評估生成行為的獎勵框架，旨在提供穩健的獎勵信號並減輕在分佈變化下的獎勵操縱。<lb/>在探索方面，我們通過DynDiff-GRPO引入動態感知的展開多樣化，這明確擴展了動作空間探索，以多樣化軌跡、擴大狀態-動作覆蓋範圍，並鼓勵超越保守展開範疇的更豐富的具身行為。<lb/>通過將作為代理的獎勵與DynDiff-GRPO統一，我們使強化學習建立在更可靠的獎勵基礎上，實現了實質上多樣化的抽樣，有效減輕了獎勵操縱，同時在多個開源世界模型上獲得顯著的準確性提升，從而證明了當建立在穩健的驗證基礎上時，更廣泛的探索可以成功擴展。

##### **The Algorithmic-Human Manager: AI, Apps, and Workers in the Indian Gig Economy**
2606.19975v1 by Omir Kumar, Krishnan Narayanan

This paper examines the impact of artificial intelligence and digital technologies on the blue-collar gig economy in India, focusing on algorithmic management. This paper examines the impact of artificial intelligence and digital technologies on the blue collar gig economy in India, focusing on algorithmic management he use of automated systems to allocate, monitor, and evaluate work in location-based services such as ride sharing and delivery. Using a social justice framework and a mixed-methods approach comprising interviews with 16 gig workers and 21 key stakeholders, the study uncovers a dual reality: while AI-powered systems expand access to work and generate operational efficiencies, they simultaneously introduce significant challenges related to fairness, transparency, and worker dignity. Key findings reveal that algorithmic systems are opaque by design, produce inequitable outcomes, and are not structured to reward additional labour with proportionate pay. The study advocates for a pragmatic hybrid governance model an Algorithmic Human Manager framework in which technological efficiency and human accountability operate together rather than in opposition. The findings carry implications for policymakers, platform companies, and civil society organizations working to design equitable AI governance frameworks for the gig economy in India and across the Global South.

摘要：這篇論文探討了人工智慧和數位技術對印度藍領零工經濟的影響，重點在於算法管理。這篇論文探討了人工智慧和數位技術對印度藍領零工經濟的影響，重點在於算法管理，使用自動化系統來分配、監控和評估基於地點的服務工作，例如共享乘車和配送。使用社會正義框架和包含對16名零工工作者和21名關鍵利益相關者的訪談的混合方法，研究揭示了一種雙重現實：雖然AI驅動的系統擴大了工作機會並產生了操作效率，但同時也引入了與公平、透明和工人尊嚴相關的重大挑戰。關鍵發現顯示，算法系統在設計上是不透明的，產生不平等的結果，並且不會以相稱的薪酬來獎勵額外的勞動。該研究主張採用務實的混合治理模式，即算法人類管理者框架，在這種框架中，技術效率和人類責任共同運作，而不是對立。這些發現對於政策制定者、平台公司和民間社會組織在設計公平的AI治理框架以應對印度及全球南方的零工經濟具有重要意義。

##### **ROSE: Benchmarking the Perception-to-Action Gap in Multimodal Models**
2606.19965v1 by Yihao Wang, Zijian He, Jie Ren, Keze Wang

Multimodal large language models (MLLMs) are increasingly expected to act on visual information, yet the same scene may require different actions under different task contexts. How reliably can a model turn the same visual evidence into the action required by the current context? To answer this question, we introduce \textsc{ROSE} (\textbf{R}eference-conditioned \textbf{O}ddity and \textbf{S}ymbolic \textbf{E}xecution), a controlled benchmark that holds the visual scene fixed while varying region constraints and required symbolic outputs. Through coupled counting and coordinate-action tasks, \textsc{ROSE} tests whether models can infer an implicit majority reference and act on the resulting fine-grained visual evidence under changing contexts. Across nine recent MLLMs, performance drops by as much as 44.5 percentage points from counting-oriented tasks to region-conditioned action, despite 98.8\% human performance. The gap persists on paired scenes and regions for which the same model returns the correct count, while global-click and matched local controls show that coordinate grounding explains only part of the loss, revealing a distinct, model-dependent bottleneck in turning shared visual evidence into context-specific actions.

摘要：多模態大型語言模型（MLLMs）越來越被期望能夠處理視覺信息，但同一場景在不同的任務背景下可能需要不同的行動。模型能多可靠地將相同的視覺證據轉化為當前背景所需的行動？為了回答這個問題，我們介紹了\textsc{ROSE}（\textbf{R}eference-conditioned \textbf{O}ddity和\textbf{S}ymbolic \textbf{E}xecution），這是一個控制基準，保持視覺場景不變，同時改變區域約束和所需的符號輸出。通過耦合計數和坐標行動任務，\textsc{ROSE}測試模型是否能推斷出隱含的主要參考，並在變化的背景下根據結果的細粒度視覺證據進行行動。在九個最近的MLLMs中，從以計數為導向的任務到區域條件行動的性能下降了多達44.5個百分點，儘管人類的表現達到98.8\%。在配對場景和區域中，當同一模型返回正確計數時，這一差距仍然存在，而全局點擊和匹配的局部控制顯示坐標基礎僅解釋了部分損失，揭示了將共享的視覺證據轉化為特定上下文行動的明顯、依賴模型的瓶頸。

##### **Investigating Human-Model Discrepancies in Speech Quality Assessment via Acoustic and Prosodic Perturbations**
2606.19951v1 by Masato Takagi, Masaya Kawamura, Reo Shimizu, Yuma Shirahata

Mean opinion score (MOS) prediction models are widely used as proxy metrics in text-to-speech (TTS) research, yet their ability to capture quality differences beyond acoustic fidelity remains unclear. We investigate this via controlled perturbations on speech: acoustic degradation, prosodic errors, and manipulation of speaker-specific characteristics such as pitch and speaking rate. We obtained MOS predictions for these speech samples from both human listeners and the model, and analyzed the differences in their perceptual characteristics. Results show that most models track acoustic degradation well, while all are insensitive to prosodic errors despite large subjective score drops. For speaker characteristics, models exhibit a double dissociation: strong mean fundamental frequency (F0) biases absent in human ratings, yet insensitivity to speaking rate and F0 variability that humans notice. These findings highlight limitations of scalar MOS prediction beyond acoustic fidelity.

摘要：平均意見分數（MOS）預測模型在文本轉語音（TTS）研究中被廣泛用作代理指標，但它們在捕捉超越聲學真實性的質量差異方面的能力仍不清楚。<lb/>我們通過對語音進行控制擾動來調查這一點：聲學降級、韻律錯誤以及操控說話者特定特徵，如音高和說話速度。<lb/>我們從人類聽眾和模型中獲得了這些語音樣本的MOS預測，並分析了它們在感知特徵上的差異。<lb/>結果顯示，大多數模型能很好地追踪聲學降級，而所有模型對韻律錯誤則不敏感，儘管主觀評分大幅下降。<lb/>在說話者特徵方面，模型表現出雙重解離：強烈的平均基頻（F0）偏差在人工評分中缺失，但對說話速度和F0變異性不敏感，而人類則能察覺到這些。<lb/>這些發現突顯了超越聲學真實性的標量MOS預測的局限性。

##### **Confidence Calibration for Multimodal LLMs: An Empirical Study through Medical VQA**
2606.19950v1 by Yuetian Du, Yucheng Wang, Ming Kong, Tian Liang, Qiang Long, Bingdi Chen, Qiang Zhu

Multimodal Large Language Models (MLLMs) show great potential in medical tasks, but their elicited confidence often misaligns with actual accuracy, potentially leading to misdiagnosis or overlooking correct advice. This study presents the first comprehensive analysis of the relationship between accuracy and confidence in medical MLLMs. It proposes a novel method that combines Multi-Strategy Fusion-Based Interrogation (MS-FBI) with auxiliary expert LLM assessment, aiming to improve confidence calibration in Medical Visual Question Answering (VQA). Experiments demonstrate that our method reduces the Expected Calibration Error (ECE) by an average of 40\% across three Medical VQA datasets, significantly enhancing MLLMs' reliability. The findings highlight the importance of domain-specific calibration for MLLMs in healthcare, offering a more trustworthy solution for AI-assisted diagnosis.

摘要：多模態大型語言模型（MLLMs）在醫療任務中顯示出巨大的潛力，但它們所引發的信心往往與實際準確性不一致，可能導致誤診或忽略正確建議。這項研究呈現了對醫療 MLLMs 中準確性與信心之間關係的首次全面分析。它提出了一種新方法，將多策略融合基礎的詢問（MS-FBI）與輔助專家 LLM 評估相結合，旨在改善醫療視覺問答（VQA）中的信心校準。實驗表明，我們的方法在三個醫療 VQA 數據集上平均降低了 40\% 的預期校準誤差（ECE），顯著提升了 MLLMs 的可靠性。研究結果強調了在醫療保健中對 MLLMs 進行特定領域校準的重要性，為 AI 輔助診斷提供了更值得信賴的解決方案。

##### **Advancing DialNav through Automatic Embodied Dialog Augmentation**
2606.19948v1 by Leekyeung Han, Sangwon Jung, Hyunji Min, Jinseong Jeong, Minyoung Kim, Paul Hongsuck Seo

For embodied agents capable of physical interaction, the capability to create and understand dialog is crucial to ensure both safety and effectiveness. While DialNav~\cite{han2025dialnav} provides a framework for holistic evaluation of the dialog--execution loop in photorealistic indoor navigation, its performance remains limited by a critical scarcity of training data (2K episodes). To address this, we propose an automatic generation pipeline, and construct the \textbf{RAINbow} dataset, a large-scale training dataset with 238K episodes for DialNav. Our pipeline converts existing VLN datasets into multi-turn dialog and creates cost-efficient and high-quality dataset. Then, we introduce two additional complementary advances to unlock the data's full potential: (1) Dual-Strategy Training, a navigation training scheme to align the navigation training with the dynamic dialog-navigation loop, and (2) a localization model that leverages VLN knowledge. By combining these complementary solutions, our model substantially outperforms the baseline in success rate on both \textbf{Val Seen} (58.24, \textbf{+89\%}) and \textbf{Val Unseen} (29.05, \textbf{+100\%}) splits, establishing a new state of the art.

摘要：對於能夠進行物理互動的具身代理，創建和理解對話的能力對於確保安全性和有效性至關重要。雖然 DialNav~\cite{han2025dialnav} 提供了一個全面評估對話執行循環的框架，但其性能仍受到訓練數據嚴重不足（2K 集）的限制。為了解決這個問題，我們提出了一個自動生成管道，並構建了 \textbf{RAINbow} 數據集，這是一個擁有 238K 集的 DialNav 大規模訓練數據集。我們的管道將現有的 VLN 數據集轉換為多輪對話，並創建成本效益高且質量優良的數據集。然後，我們引入了兩項額外的互補進展，以釋放數據的全部潛力：（1）雙策略訓練，一種導航訓練方案，旨在將導航訓練與動態對話導航循環對齊，以及（2）利用 VLN 知識的定位模型。通過結合這些互補解決方案，我們的模型在 \textbf{Val Seen}（58.24，\textbf{+89\%}）和 \textbf{Val Unseen}（29.05，\textbf{+100\%}）分割上的成功率顯著超越基線，樹立了新的技術標杆。

##### **GEMS: Geometric Constraints Enable Multi-Semantic Superposition in LLMs**
2606.19946v1 by Yu Deng

Activation steering controls model behavior by modifying intermediate hidden states at inference time without retraining. Existing methods handle only single-direction injection; when multiple semantic directions are superposed without constraints, the model collapses. We show that this collapse decomposes into two independently acting sources: distributional deviation, where additive perturbations accumulate in norm across layers and drive activations outside the training distribution, and directional interference, where non-orthogonal semantic vectors mutually dampen when superposed. These two sources define the design constraints that any training-free multi-directional intervention must address. As one instantiation of these principles, we propose GEMS, a training-free method that maps each source to a corresponding geometric constraint: norm-preserving weighted superposition and targeted attention-pathway injection for distributional deviation, and real-time orthogonalization for directional interference. On GSM8K, injecting three concurrent non-mathematical directions preserves accuracy at 98% (baseline 92%), while unconstrained addition collapses to 4%; on Wikitext-2, the same injection incurs only 2.2% PPL increase. Component ablation isolates the causal role of each constraint, and layer-level probes confirm that orthogonalized signals survive the FFN pathway and reach the output distribution with semantic specificity. Qualitative steering effects transfer across architectures from 3B to 31B.

摘要：啟動引導控制通過在推理時修改中間隱藏狀態來模型行為，而無需重新訓練。現有方法僅處理單向注入；當多個語義方向在沒有約束的情況下重疊時，模型會崩潰。我們顯示這種崩潰分解為兩個獨立作用的來源：分佈偏差，其中附加擾動在層之間累積並驅動激活超出訓練分佈，以及方向干擾，其中非正交語義向量在重疊時相互減弱。這兩個來源定義了任何無需訓練的多方向干預必須解決的設計約束。作為這些原則的一種具體實例，我們提出了GEMS，一種將每個來源映射到相應幾何約束的無需訓練的方法：保持範數的加權疊加和針對分佈偏差的目標注意力通道注入，以及實時正交化以應對方向干擾。在GSM8K上，注入三個同時的非數學方向保持了98%的準確率（基線92%），而不受約束的加法崩潰至4%；在Wikitext-2上，相同的注入僅導致2.2%的PPL增長。組件消融隔離了每個約束的因果作用，層級探針確認正交化信號在FFN通道中存活並以語義特異性達到輸出分佈。質性引導效果在從3B到31B的架構之間轉移。

##### **SIMBA: ABidirectional Retrieval Forward Simulation Framework for Modeling FY-4A GIIRS Hyperspectral Infrared Radiances Toward NWP Applications**
2606.19943v1 by Jingdong Shen, Fu Wang*, Qifeng Lu, Hao Huang, Chunqiang Wu, Chi Yang, Xiaofang Liu

Hyperspectral infrared observations are an important data source for numerical weather prediction (NWP) because they provide rich information on the vertical structure of atmospheric temperature and humidity. However, most existing deep learning methods mainly focus on one-way retrieval from radiances to atmospheric profiles, while the reverse radiance simulation process and the consistency between atmospheric state space and radiance observation space are insufficiently considered. In this study, we propose SIMBA, a unified bidirectional retrieval-forward simulation framework for FY-4A GIIRS hyperspectral infrared radiance modeling toward NWP applications. The framework jointly performs atmospheric profile retrieval and radiance reconstruction, introduces a cycle-consistency constraint to strengthen the coupling between the two processes, and employs a bidirectional Mamba state-space module to capture long-range dependencies along pressure levels. Using collocated FY-4A GIIRS observations and ERA5 reanalysis data, the proposed method is evaluated for temperature retrieval, specific humidity retrieval, long-wave radiance reconstruction, and medium-wave radiance reconstruction. Experimental results show that SIMBA outperforms several representative deep learning baselines across both retrieval and reconstruction tasks, while ablation experiments confirm the contribution of the bidirectional design and cycle-consistency mechanism. These results demonstrate that the proposed framework is effective for joint atmospheric profile retrieval and hyperspectral infrared radiance modeling, and suggest potential for future Jacobian-related analysis and NWP-oriented extensions.

摘要：高光譜紅外觀測是數值天氣預報（NWP）的一個重要數據來源，因為它們提供了豐富的大氣溫度和濕度的垂直結構信息。<lb/>然而，大多數現有的深度學習方法主要集中於從輻射到大氣剖面的單向檢索，而反向輻射模擬過程以及大氣狀態空間與輻射觀測空間之間的一致性則考慮不足。<lb/>在本研究中，我們提出了SIMBA，一個統一的雙向檢索-前向模擬框架，用於FY-4A GIIRS高光譜紅外輻射建模，以應用於NWP。<lb/>該框架共同執行大氣剖面檢索和輻射重建，引入了循環一致性約束以加強這兩個過程之間的耦合，並採用雙向的Mamba狀態空間模塊來捕捉沿壓力層的長距離依賴性。<lb/>使用協同的FY-4A GIIRS觀測和ERA5再分析數據，對所提出的方法進行了溫度檢索、比濕檢索、長波輻射重建和中波輻射重建的評估。<lb/>實驗結果顯示，SIMBA在檢索和重建任務中均優於幾個代表性的深度學習基準，而消融實驗確認了雙向設計和循環一致性機制的貢獻。<lb/>這些結果表明，所提出的框架在聯合大氣剖面檢索和高光譜紅外輻射建模方面是有效的，並暗示未來在Jacobian相關分析和NWP導向擴展方面的潛力。

##### **Triangular Consistency as a Universal Constraint for Learning Optical Flow**
2606.19938v1 by Yi Xiao, Carlos Rodriguez Coronel, Jing Zhan, Haniyeh Ehsani Oskouie, Alex Wong, Dong Lao

We propose triangular consistency as a first-principled constraint for optical flow, which is agnostic to network architecture, supervision type, and dataset, and applies to both image-pair and multi-frame settings. This simple but powerful constraint is to compose two flows to induce a third flow and enforce consistency among the three. The composed flows may arise from (i) image pairs, yielding cycle consistency; (ii) multiple video frames, producing longer-range motion through temporal chaining; or (iii) image pairs combined with controlled synthetic transformations, which becomes data augmentation. This triangular consistency introduces negligible computational overhead and requires no additional annotations. Since it is derived directly from the geometry of optical flow, it does not rely on model-specific assumptions and serves as a ``universal'' plug-and-play component for optical flow training. Experiments show consistent improvement across supervised, unsupervised, and transfer learning settings.

摘要：我們提出三角一致性作為光流的首要原則約束，這種約束與網絡架構、監督類型和數據集無關，並適用於圖像對和多幀設置。這個簡單但強大的約束是將兩個流組合以引導第三個流，並強化三者之間的一致性。組合的流可能來自 (i) 圖像對，產生循環一致性；(ii) 多個視頻幀，通過時間鏈接產生更長距離的運動；或 (iii) 與受控合成變換結合的圖像對，這成為數據增強。這種三角一致性引入的計算開銷微不足道，並且不需要額外的標註。由於它直接源於光流的幾何結構，因此不依賴於特定模型的假設，並作為光流訓練的“通用”即插即用組件。實驗顯示在監督、無監督和遷移學習設置中均有一致的改進。

##### **Speeding up the annotation process in semantic segmentation industrial applications**
2606.19934v1 by Marta Fernandez-Moreno, Margarita Guerrero, Rosalia Rementeria, Pablo Mesejo, Raul Moreno

Current machine learning models commonly require large and well-annotated datasets. However, the annotation process often becomes a bottleneck, with increased complexity leading to higher chances of human errors. Within this context, our goal in this paper is to leverage unsupervised algorithms to improve data annotation efficiency for complex semantic segmentation problems in industrial materials science. Previous research has quantified labeling time and others explored unsupervised methods. However, to the best of our knowledge, this is the first study to quantify how much unsupervised algorithms accelerate the labeling process. We aim to validate the extent to which this laborious process can be accelerated, focusing on semantic segmentation tasks that involve annotating each pixel of high-resolution images, such as the microstructure characterization challenge in materials science. Specifically, we demonstrate that by using unsupervised computer vision algorithms, the time required for the labeling process can be reduced from 170 hours to 37 hours, achieving an approximate reduction of 78\%. The dataset we work with includes large images of dimensions 1280x959 and 960x703, which further increases the complexity of the annotation task. Despite these challenges, we create and share the largest public steel microstructure segmentation dataset to date, available under MIT License with permanent DOI, contributing a fully annotated, high-resolution dataset to the field. Additionally, this is the first work to compare the labeling time from scratch (a common approach in previous studies) to the labeling time when using these unsupervised algorithms as a pre-annotation step. Furthermore, we provide a Deep Learning model trained on this dataset, validated by field experts, and deployed in an industrial setting, serving as an initial benchmark for this public dataset.

摘要：目前的機器學習模型通常需要大型且標註良好的數據集。<lb/>然而，標註過程往往成為瓶頸，隨著複雜性的增加，人為錯誤的機率也隨之上升。<lb/>在這個背景下，我們在本文中的目標是利用無監督算法來提高工業材料科學中複雜語義分割問題的數據標註效率。<lb/>先前的研究已經量化了標註時間，並探索了無監督方法。<lb/>然而，據我們所知，這是第一項量化無監督算法加速標註過程的研究。<lb/>我們的目標是驗證這一繁瑣過程可以加速的程度，重點關注涉及標註高解析度圖像每個像素的語義分割任務，例如材料科學中的微觀結構特徵挑戰。<lb/>具體而言，我們展示了通過使用無監督計算機視覺算法，標註過程所需的時間可以從170小時減少到37小時，實現約78\%的減少。<lb/>我們所使用的數據集包括尺寸為1280x959和960x703的大型圖像，這進一步增加了標註任務的複雜性。<lb/>儘管面臨這些挑戰，我們創建並分享了迄今為止最大的公共鋼材微觀結構分割數據集，該數據集在MIT許可下提供，並具有永久DOI，為該領域貢獻了一個完全標註的高解析度數據集。<lb/>此外，這是第一項將從零開始的標註時間（以往研究中的常見方法）與使用這些無監督算法作為預標註步驟的標註時間進行比較的工作。<lb/>此外，我們提供了一個在該數據集上訓練的深度學習模型，經過領域專家的驗證，並在工業環境中部署，作為這個公共數據集的初步基準。

##### **Spatial-Aware Reduction Framework: Towards Efficient and Faithful Visual State Space Models**
2606.19932v1 by Jindi Lv, Aoyu Li, Yuhao Zhou, Zheng Zhu, Xiaofeng Wang, Qing Ye, Yueqi Duan, Wentao Feng, Jiancheng Lv

Mamba demonstrates strong efficiency in modeling long visual sequences. However, when token reduction is applied to structurally enhanced Mamba variants, these models exhibit a severe performance collapse. We attribute this degradation to the spatially agnostic nature of existing reduction methods, which violate the two-dimensional structural premise required by the selective scanning mechanism. In this work, we propose STORM, a spatial-aware token reduction framework designed to maintain structural integrity throughout the compression process. STORM reformulates reduction into a structured operation on spatial units, enforcing localized constraints to maintain both grid topology and neighborhood coherence. As a plug-and-play module, STORM equips existing reduction pipelines with explicit spatial awareness without any training. Empirical results demonstrate that STORM achieves state-of-the-art pruning accuracy across diverse vision Mamba backbones under training-free settings. Notably, STORM delivers a substantial accuracy recovery on VMamba, outperforming prior methods by up to 63.3\% in top-1 accuracy. Meanwhile, STORM incurs only a 1.0\% accuracy drop on PlainMamba, achieving performance comparable to ViT.

摘要：Mamba 在建模長視覺序列方面展現出強大的效率。<lb/>然而，當對結構增強的 Mamba 變體應用標記減少時，這些模型會出現嚴重的性能崩潰。<lb/>我們將這一退化歸因於現有減少方法的空間無關性質，這違反了選擇性掃描機制所需的二維結構前提。<lb/>在這項工作中，我們提出了 STORM，一個空間感知的標記減少框架，旨在在壓縮過程中保持結構完整性。<lb/>STORM 將減少重新定義為對空間單元的結構化操作，強制執行局部約束以保持網格拓撲和鄰域一致性。<lb/>作為一個即插即用的模塊，STORM 為現有的減少管道提供了明確的空間感知，而無需任何訓練。<lb/>實證結果表明，STORM 在無需訓練的設置下，在多樣的視覺 Mamba 骨幹上實現了最先進的修剪準確性。<lb/>值得注意的是，STORM 在 VMamba 上實現了顯著的準確性恢復，並在 top-1 準確性上超越了先前的方法，提升幅度高達 63.3\%。<lb/>同時，STORM 在 PlainMamba 上僅造成 1.0\% 的準確性下降，實現了與 ViT 相當的性能。

##### **The Tao of Agency: Autotelic AI, Embedded Agency and Dissolution of the Self**
2606.19924v1 by Aritra Sarkar

Most artificial intelligence systems are built on the assumption that goals are exogenous and specified by the designer. Exploring what happens when an agent begins generating its own goals opens the field of autotelic AI. Agents are expected not merely to pursue objectives but to discover them. In this article, we trace its consequences through intrinsic motivation, resource-driven priors, causal-interventional learning, homeostasis, and embeddedness; the last of which is found to be a necessary but not sufficient condition for autotelic agency. Embeddedness individuates the agent at the cost of revealing that the individuation is non-unique, such that the same dynamics admit many valid partitions, each defining a different candidate self.
  The deepest problem with autotelic AI is therefore not how the agent generates goals, but how it generates and relativizes the self to which the goals are assigned. The agent must believe in its own boundary in order to act, and see through that boundary in order to understand. We consolidate these developments into a single framework and extend it along three directions: a quantum formulation in which the agent-environment cut becomes physical, a philosophical reading against non-dual contemplative traditions, and a concrete LLM-based agentic instantiation.

摘要：大多數人工智慧系統建立在目標是外生的且由設計者指定的假設上。當一個智能體開始生成自己的目標時，探索其所發生的情況開啟了自我目的人工智慧的領域。智能體不僅被期望追求目標，還要發現目標。在本文中，我們通過內在動機、資源驅動的先驗、因果干預學習、恆定性和嵌入性追蹤其後果；最後一項被認為是自我目的代理所需但不足的條件。嵌入性使智能體個體化，但代價是揭示這種個體化並非唯一，因此相同的動態允許許多有效的劃分，每一個劃分定義了一個不同的候選自我。

因此，自我目的人工智慧的最深層問題並不是智能體如何生成目標，而是它如何生成並相對化分配給目標的自我。智能體必須相信自己的邊界才能行動，並透過該邊界來理解。我們將這些發展整合成一個單一框架，並沿著三個方向擴展：一個量子表述，其中智能體-環境的切割變得物理化，對抗非二元靜思傳統的哲學解讀，以及一個具體的基於大型語言模型的智能體實例化。

