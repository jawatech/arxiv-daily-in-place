
### Knowledge Graphs
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-08-28**|**Video Generative Models as Geometry Learner**|Haosen Yang et.al.|[2608.28549v1](http://arxiv.org/abs/2608.28549v1)|null|
|**2026-08-28**|**LLM-Based Agents for Software and Systems Security: Approaches, Applications, and Assessment**|Jingjing Nie et.al.|[2608.28490v1](http://arxiv.org/abs/2608.28490v1)|null|
|**2026-08-28**|**Blind Men and the Elephant: Probing the Epistemic Myopia of LLMs under Long-Tail Divergent Knowledge**|Zhuoshi Pan et.al.|[2608.28478v1](http://arxiv.org/abs/2608.28478v1)|null|
|**2026-08-28**|**Acquire, Repair, Preserve: A Diagnosis-Guided Post-Training Recipe for Small-Model Dialogue Game Agents**|Nan Li et.al.|[2608.28458v1](http://arxiv.org/abs/2608.28458v1)|null|
|**2026-08-28**|**VERA-8B: Evidence-Grounded Audit Risk Reasoning from SEC Filings**|Menghan Liu et.al.|[2608.28402v1](http://arxiv.org/abs/2608.28402v1)|null|
|**2026-08-28**|**BEACON: Behavior-Anchored Cross-Source Knowledge Graph Construction for Cyber Threat Intelligence**|Changze Li et.al.|[2608.28394v1](http://arxiv.org/abs/2608.28394v1)|null|
|**2026-08-28**|**Propagating construction-time knowledge quality into medical question answering: A framework grounded in clinical guidelines**|Jie Hu et.al.|[2608.28360v1](http://arxiv.org/abs/2608.28360v1)|null|
|**2026-08-28**|**AGENT-O: A Semantic Agent Card Framework for Interoperable and Governed Healthcare AI Agents**|Pengze Li et.al.|[2608.28345v1](http://arxiv.org/abs/2608.28345v1)|null|
|**2026-08-28**|**BanglaMed-QA: A Question Answering System for Healthcare Support in Bangla**|Rowzatul Zannat et.al.|[2608.28329v1](http://arxiv.org/abs/2608.28329v1)|null|
|**2026-08-28**|**MAIL: Memory-driven, Adaptive, Incremental, and Literature-grounded Framework for Hypothesis Generation in Chemistry**|Mahdi Babaei et.al.|[2608.28315v1](http://arxiv.org/abs/2608.28315v1)|null|
|**2026-08-28**|**AIM: Anchor Identity Features, Then Match for Multimodal Large Language Model Unlearning**|Wonjun Lee et.al.|[2608.28312v1](http://arxiv.org/abs/2608.28312v1)|null|
|**2026-08-28**|**PanelShield: Verifiable Closed-Loop Safe Planning for Robotic Industrial Panel Operation**|Guipeng Xin et.al.|[2608.28305v1](http://arxiv.org/abs/2608.28305v1)|null|
|**2026-08-28**|**MaCoPlanner: LLM-Assisted Manual-Compiled Task Planning with Proactive Safety Verification for Robotic Industrial Panel Operation**|Guipeng Xin et.al.|[2608.28300v1](http://arxiv.org/abs/2608.28300v1)|null|
|**2026-08-28**|**Regime-Aware Portfolio Management via Retrieval-Augmented LLM-Guided Expert Switching**|Ahmad Asadi et.al.|[2608.28252v1](http://arxiv.org/abs/2608.28252v1)|null|
|**2026-08-28**|**Generative AI Alignment with Hinduism's Theological Plurality and Sacred Representation**|Dipto Das et.al.|[2608.28228v1](http://arxiv.org/abs/2608.28228v1)|null|
|**2026-08-28**|**Beyond Flat Netlist: Hierarchical Graph Representation Learning for Scalable Analysis of Sequential Circuits**|Jingyi Zhou et.al.|[2608.28188v1](http://arxiv.org/abs/2608.28188v1)|null|
|**2026-08-28**|**Expert Knowledge & Machine Understanding: Bridging Reactome's Ontology with LLM Semantic Embeddings**|Susanna Bravi et.al.|[2608.28178v1](http://arxiv.org/abs/2608.28178v1)|null|
|**2026-08-28**|**Gen-TAS: A Generative AI-Aided Hardware-Software Task Allocation Framework for FPGA-GPP Heterogeneous Systems**|Mary Kong et.al.|[2608.28160v1](http://arxiv.org/abs/2608.28160v1)|null|
|**2026-08-28**|**FinExam-10K: When Retrieval Helps Financial Reasoning?**|Yan Lin et.al.|[2608.28155v1](http://arxiv.org/abs/2608.28155v1)|null|
|**2026-08-28**|**WeAgent-MMSearch: Native Text-Vision Interaction for Multimodal Search Agents**|Zongkai Liu et.al.|[2608.28062v1](http://arxiv.org/abs/2608.28062v1)|null|
|**2026-08-28**|**String: An Agentic OS Where Every App Is a Markdown File**|Jookyung Song et.al.|[2608.28027v1](http://arxiv.org/abs/2608.28027v1)|null|
|**2026-08-28**|**Twin Worlds: Equivariance-Based Abstention for Evidence-Grounded Reasoning**|Vy Nguyen et.al.|[2608.28018v1](http://arxiv.org/abs/2608.28018v1)|null|
|**2026-08-28**|**When Evidence Shapes Collaboration: Knowledge-Conditioned Topology Generation for Multi-Agent Systems**|Yangxiao Jiang et.al.|[2608.27984v1](http://arxiv.org/abs/2608.27984v1)|null|
|**2026-08-28**|**DisCTI: Who Needs to Know Timely? Automated Sector-Aware Cyber Threat Intelligence Dissemination**|Fajar Wijitrisnanto et.al.|[2608.27967v1](http://arxiv.org/abs/2608.27967v1)|null|
|**2026-08-28**|**When Teacher Guidance Misleads: Reward-Aligned On-Policy Distillation**|Siyuan Gan et.al.|[2608.27960v1](http://arxiv.org/abs/2608.27960v1)|null|
|**2026-08-28**|**The Illusion of $\textit{What If}$: Evaluating the Breakdown of Counterfactual Reasoning in LLMs**|Yucheng Wang et.al.|[2608.27953v1](http://arxiv.org/abs/2608.27953v1)|null|
|**2026-08-28**|**Entity-Memory Graph Retrieval Improves Evidence Coverage in Long-Conversation Question Answering**|Shumao Sun et.al.|[2608.27925v1](http://arxiv.org/abs/2608.27925v1)|null|
|**2026-08-28**|**PCBnet: A Dataset and Automatic Construction of SPICE Netlists from Schematic Images**|Zhen Huang et.al.|[2608.27923v1](http://arxiv.org/abs/2608.27923v1)|null|
|**2026-08-28**|**SOMTab: Set-Order Mamba for Efficient Tabular In-Context Learning**|Hao Wang et.al.|[2608.27882v1](http://arxiv.org/abs/2608.27882v1)|null|
|**2026-08-28**|**CoRe-MoE: Compact Reusable MoE for Continual Multimodal Instruction Tuning**|Runze Liu et.al.|[2608.27867v1](http://arxiv.org/abs/2608.27867v1)|null|
|**2026-08-28**|**SpikeOPD: Stable On-Policy Distillation for Autoregressive Spiking Language Models**|Enqiao Lu et.al.|[2608.27857v1](http://arxiv.org/abs/2608.27857v1)|null|
|**2026-08-28**|**EvoHarmBench: Breaking Content Moderation with Iterative Human-Like Evasion**|Ruijie Jian et.al.|[2608.27844v1](http://arxiv.org/abs/2608.27844v1)|null|
|**2026-08-28**|**KLOD: Locality-Preserving Knowledge Editing via Non-Target Distribution Preservation**|Hojun Jeong et.al.|[2608.27839v1](http://arxiv.org/abs/2608.27839v1)|null|
|**2026-08-28**|**PersonaEdit: Representative Sample Selection for Personalized Model Editing**|You-Mei Huang et.al.|[2608.27816v1](http://arxiv.org/abs/2608.27816v1)|null|
|**2026-08-28**|**Credo: Reusable Declarative Primitives for Agentic Workflows**|Duo Lu et.al.|[2608.27790v1](http://arxiv.org/abs/2608.27790v1)|null|
|**2026-08-27**|**Load-Bearing Context: The Question Damage Score for Evaluating Context Reliance in Linguistic Reasoning**|Neh Majmudar et.al.|[2608.27756v1](http://arxiv.org/abs/2608.27756v1)|null|
|**2026-08-27**|**The Calls are Coming from Inside the Model: Investigating Probe-based Detection of Tool-Calling Errors in LLMs**|Eric Yeats et.al.|[2608.27750v1](http://arxiv.org/abs/2608.27750v1)|null|
|**2026-08-27**|**Below the Noise Floor: Bimodal Seed Collapse and Distinct Failure Modes in Small-Model Knowledge Distillation**|Dipto Sumit et.al.|[2608.27729v1](http://arxiv.org/abs/2608.27729v1)|null|
|**2026-08-27**|**Agents for Everyone: A Workshop Framework for Building Agentic AI Capabilities in a Distributed Curation Community**|Seth Carbon et.al.|[2608.27675v1](http://arxiv.org/abs/2608.27675v1)|null|
|**2026-08-27**|**Self-Explainable Multi-Label Graph Neural Network for Correlated Evidence Attribution**|Yingqi Feng et.al.|[2608.27574v1](http://arxiv.org/abs/2608.27574v1)|null|
|**2026-08-27**|**WikiSkill: Compiling Agent Experience into Persistent Knowledge for Skill Evolution**|Liyan Tang et.al.|[2608.27454v1](http://arxiv.org/abs/2608.27454v1)|null|
|**2026-08-27**|**Mechanistic Reaction Prediction via Discrete Flow Matching on Graph-Structured Electron Occupation**|Nguyen Xuan-Vu et.al.|[2608.27429v2](http://arxiv.org/abs/2608.27429v2)|null|
|**2026-08-27**|**How Language Models Organize and Structure Moral Knowledge**|Orion Reblitz-Richardson et.al.|[2608.27402v1](http://arxiv.org/abs/2608.27402v1)|null|
|**2026-08-27**|**CorporateBench: Large-Scale Q&A Benchmarking with Temporal Knowledge Bases**|Sil Hamilton et.al.|[2608.27391v1](http://arxiv.org/abs/2608.27391v1)|null|
|**2026-08-27**|**BrailleBench: Investigating Multi-Criteria Braille Comprehension in Large Language Models**|Jinghan Zhang et.al.|[2608.27268v1](http://arxiv.org/abs/2608.27268v1)|null|
|**2026-08-27**|**GRAIN: Bridging Name and Narrative Shifts in Real-World Graph Reasoning through Invariance-Rewarded Agentic RL**|Zike Yuan et.al.|[2608.27142v1](http://arxiv.org/abs/2608.27142v1)|null|
|**2026-08-27**|**TransMeme: A Multi-Agent Framework for Cross-Cultural Meme Transcreation**|Jingyi Zheng et.al.|[2608.27127v1](http://arxiv.org/abs/2608.27127v1)|null|
|**2026-08-27**|**pro-team at LLMs4OL 2026 Tasks Flagship and Reuse: Retrieval-Augmented Generation and Vocabulary-Constrained Filtering for Ontology Learning**|Shivam Mishra et.al.|[2608.27101v1](http://arxiv.org/abs/2608.27101v1)|null|
|**2026-08-27**|**Active Diffusion-Based Inference for Ill-Posed Inverse Problems under Incomplete Priors**|Jitao Xu et.al.|[2608.27080v1](http://arxiv.org/abs/2608.27080v1)|null|
|**2026-08-27**|**Unifying Detection and Adaptation in Task-Free Continual Learning**|Dezheng Han et.al.|[2608.27070v1](http://arxiv.org/abs/2608.27070v1)|null|
|**2026-08-27**|**GraphMemix: Query-Aware Evidence Forests for Long-Term Multimodal Agent Memory**|Geng Li et.al.|[2608.26983v1](http://arxiv.org/abs/2608.26983v1)|null|
|**2026-08-27**|**AraMS-28k: The Largest Publicly Released Line-Level Dataset of Historical Arabic Manuscripts with Margin and Insertion-Anchor Annotations**|Mohamed Guechaoui et.al.|[2608.26921v1](http://arxiv.org/abs/2608.26921v1)|null|
|**2026-08-27**|**PLCBench: Can Autonomous LLM Agents Turn PLC Access into Sustained Physical Impact?**|Yitian Zhou et.al.|[2608.26882v1](http://arxiv.org/abs/2608.26882v1)|null|
|**2026-08-27**|**C-Unseen: Weak Signal Detection in Dynamic Temporal Knowledge Graphs via LLM Reasoning**|Yassir Lairgi et.al.|[2608.26870v1](http://arxiv.org/abs/2608.26870v1)|null|
|**2026-08-27**|**BekchiAI: Measuring, Observing, and Controlling LLM Agents in One Click**|Mesut Toruk et.al.|[2608.26867v1](http://arxiv.org/abs/2608.26867v1)|null|
|**2026-08-27**|**MedFG-VQA: Low-Frequency Memory and Graph Attention for Lightweight Medical VQA**|Haowen Gu et.al.|[2608.26848v1](http://arxiv.org/abs/2608.26848v1)|null|
|**2026-08-27**|**Evaluating Confidence-Gated Retrieval with Matched Trajectory Replay**|Prateek Chhikara et.al.|[2608.26846v1](http://arxiv.org/abs/2608.26846v1)|null|
|**2026-08-27**|**DAMP: Decay-Aware Mixed-Precision Recurrent-State Quantization**|Tao Zhang et.al.|[2608.27513v1](http://arxiv.org/abs/2608.27513v1)|null|
|**2026-08-27**|**SymbolLKG: Towards Verifiable Logical Reasoning via Logical Knowledge Graph and Symbolic Solvers**|Haizhao Fan et.al.|[2608.26836v1](http://arxiv.org/abs/2608.26836v1)|null|
|**2026-08-27**|**How Do Linear Probes Emerge? A Circuit-Tracing Framework with Concept-Targeted Attribution**|Vedant Palit et.al.|[2608.27510v1](http://arxiv.org/abs/2608.27510v1)|null|
|**2026-08-27**|**AI Control Scientist: LLM-driven Agentic System for Automated Control Design**|Haiteng Wang et.al.|[2608.26780v1](http://arxiv.org/abs/2608.26780v1)|null|
|**2026-08-27**|**Graph-Guided Selective Unlearning for Language Models: Controlling Support Routes Beyond Forget Seeds**|Waqas Khan et.al.|[2608.26743v1](http://arxiv.org/abs/2608.26743v1)|null|
|**2026-08-27**|**The Thousand-Graph Hypothesis: A Testable Hypothesis of Task-Conditioned Relation Materialization in Repository-Level Code Reasoning**|Fei Ding et.al.|[2608.26602v1](http://arxiv.org/abs/2608.26602v1)|null|
|**2026-08-27**|**Surgical Alignment in Knowledge Graph Training for Clinical Diagnosis with Large Language Models**|Saksham Khatwani et.al.|[2608.26587v2](http://arxiv.org/abs/2608.26587v2)|null|
|**2026-08-27**|**SPEAR: Distilling Domain-Adaptive Reasoning Skeletons via Sequential Symbolic Alignment in Reinforcement Learning**|Zhuochun Li et.al.|[2608.26550v1](http://arxiv.org/abs/2608.26550v1)|null|
|**2026-08-27**|**Multi2AV-Safety: Benchmarking Safety in Multimodal-to-Audio-Video Generation**|Kaichao Jiang et.al.|[2608.26535v1](http://arxiv.org/abs/2608.26535v1)|null|
|**2026-08-26**|**Redwood: A Frontier AI Accelerator Designed, Verified, and Deployed from Scratch in 2 Weeks by AI**|Architect Labs et.al.|[2608.26418v2](http://arxiv.org/abs/2608.26418v2)|null|
|**2026-08-26**|**Co-Evolving Structured Knowledge and Reasoning in Language Models**|Ryan Thomas Noonan et.al.|[2608.26386v1](http://arxiv.org/abs/2608.26386v1)|null|
|**2026-08-26**|**Why RAGs Hallucinate: Penalty-Aware Evaluation of Retrieval-Augmented Generation Systems with Knowledge-Gap Canaries**|Alden Do Rosario et.al.|[2608.26385v1](http://arxiv.org/abs/2608.26385v1)|null|
|**2026-08-26**|**Knowledge-Verified Emergent Deception in LLM Agents Under Conflicting Incentives**|Zheyuan Liu et.al.|[2608.26372v1](http://arxiv.org/abs/2608.26372v1)|null|
|**2026-08-26**|**Cross-lingual Representation Learning via Centroid Intervention Fusion**|Wei Sun et.al.|[2608.26357v1](http://arxiv.org/abs/2608.26357v1)|null|
|**2026-08-26**|**ProofEvolve: Neuro-Symbolic Evolution for Formal Automated Theorem Proving**|Wenqian Ye et.al.|[2608.26334v1](http://arxiv.org/abs/2608.26334v1)|null|
|**2026-08-26**|**On Scope Classification and Current Knowledge-Editing Benchmarks: A Negative Result, with INLAY as a Gradient-Free Case Study**|Aditya Pratap Singh et.al.|[2608.26292v1](http://arxiv.org/abs/2608.26292v1)|null|
|**2026-08-26**|**MyoMechanix: Biomechanically-Grounded Compositional Skilled Activity Understanding and Coaching**|Hao Yin et.al.|[2608.26094v1](http://arxiv.org/abs/2608.26094v1)|null|
|**2026-08-26**|**The Reasoning Tax: Token Economics of LLM Reasoning Across Task Types and Deployment Contexts**|Sachin Gopal Wani et.al.|[2608.26235v1](http://arxiv.org/abs/2608.26235v1)|null|
|**2026-08-26**|**The Value of Human Expertise**|Bradley Sturt et.al.|[2608.26051v1](http://arxiv.org/abs/2608.26051v1)|null|
|**2026-08-26**|**Multi-Granularity Context-Enhanced RAG over Multimodal Knowledge Graphs**|Zongyu Wu et.al.|[2608.25986v1](http://arxiv.org/abs/2608.25986v1)|null|
|**2026-08-26**|**SciMIF: Understanding Multimodal Instruction Following in Scientific Domains**|Ye Shen et.al.|[2608.25973v1](http://arxiv.org/abs/2608.25973v1)|null|
|**2026-08-26**|**LivingRAG: Augmenting Graph RAG with Experience**|Yuzhuo Cui et.al.|[2608.25960v1](http://arxiv.org/abs/2608.25960v1)|null|
|**2026-08-26**|**Code World Model: Coding Agent as World Brain**|Yiwen Chen et.al.|[2608.25927v1](http://arxiv.org/abs/2608.25927v1)|null|
|**2026-08-26**|**Query-Side Attacks on GNN-Based KGQA: Tracing Failures from Entity Linking to Answer Generation**|Pankaj Kumar et.al.|[2608.25922v1](http://arxiv.org/abs/2608.25922v1)|null|
|**2026-08-26**|**Repair or Resample? Rethinking Failure Debugging in LLM Multi-Agent Systems**|Zhongwen Luan et.al.|[2608.25920v1](http://arxiv.org/abs/2608.25920v1)|null|
|**2026-08-26**|**One Form to Transfer Them All: Pretraining Multilingual Language Models Beyond Native Orthography**|Muge Zhang et.al.|[2608.25904v2](http://arxiv.org/abs/2608.25904v2)|null|
|**2026-08-26**|**Skill Issue: Are Skills Language-Invariant in LLMs?**|Bobby Cheng et.al.|[2608.25832v1](http://arxiv.org/abs/2608.25832v1)|null|
|**2026-08-26**|**Why Does Graph Learning Fail to Fully Benefit from a Text Teacher?**|Fumiaki Kimino et.al.|[2608.25741v1](http://arxiv.org/abs/2608.25741v1)|null|
|**2026-08-26**|**When RAG Fails to Equalize: Geo-bias in Factual Question Answering over Public Companies**|Abhinav Havaldar et.al.|[2608.25717v1](http://arxiv.org/abs/2608.25717v1)|null|
|**2026-08-26**|**Data Citation for Large Language Models: A Challenge**|Gianmaria Silvello et.al.|[2608.25663v1](http://arxiv.org/abs/2608.25663v1)|null|
|**2026-08-26**|**From General Agents to RCA Experts: A Self-Evolving Harness for Root Cause Analysis**|Haiyu Huang et.al.|[2608.25661v1](http://arxiv.org/abs/2608.25661v1)|null|
|**2026-08-26**|**Leveraging Inter-object Affordances for Efficient Planning in Contact-rich Tasks**|Pouya P. Niaz et.al.|[2608.25641v1](http://arxiv.org/abs/2608.25641v1)|null|
|**2026-08-26**|**JIT-Agent: Scaling Harness Intelligence via Just-in-Time Harness Evolution**|Guibin Zhang et.al.|[2608.25593v1](http://arxiv.org/abs/2608.25593v1)|null|
|**2026-08-26**|**V-Rubrics: Visual Faithfulness via Rubric-Based Reinforcement Learning**|Shulin Tian et.al.|[2608.25580v1](http://arxiv.org/abs/2608.25580v1)|null|
|**2026-08-26**|**Cross-Dataset Stability of Expert-Informed Skill Prompting and Fine-Tuning for Chinese Metaphor Identification**|Yufeng Wu et.al.|[2608.25579v1](http://arxiv.org/abs/2608.25579v1)|null|
|**2026-08-26**|**MLLMCLIP: Feature-Level Distillation of MLLM for Robust Vision-Language Representations**|Jongsuk Kim et.al.|[2608.25575v1](http://arxiv.org/abs/2608.25575v1)|null|
|**2026-08-26**|**AdaVDR: Adaptive Tool Use and Reflection for Video Deep Research**|Xintong Zhang et.al.|[2608.25559v1](http://arxiv.org/abs/2608.25559v1)|null|
|**2026-08-26**|**Virgil: Navigating Explainability for Transformer-based Language Models**|Martino Ciaperoni et.al.|[2608.25555v1](http://arxiv.org/abs/2608.25555v1)|null|
|**2026-08-26**|**When Stale Constraints Go Unchecked: Budgeted Verification Failures in Inherited Agent Memory**|Kazuki Nakayashiki et.al.|[2608.25553v3](http://arxiv.org/abs/2608.25553v3)|null|
|**2026-08-26**|**Syn2Logic: End-to-End Neuromorphic Design Automation**|Artur Podobas et.al.|[2608.25536v1](http://arxiv.org/abs/2608.25536v1)|null|
|**2026-08-26**|**CaSKG: Counterfactual-Causal Skill Graphs for Scalable Agent Skill Retrieval**|Zhiyuan Li et.al.|[2608.25500v1](http://arxiv.org/abs/2608.25500v1)|null|
|**2026-08-26**|**A Storage-Retrieval Gap in Parametric Knowledge Graph Memory**|Martino M. L. Pulici et.al.|[2608.25489v1](http://arxiv.org/abs/2608.25489v1)|null|
|**2026-08-26**|**ReliableRAG: Combating Misinformation in Retrieval-Augmented Generation via Reliability-Guided Reasoning Chains**|Jinpu Jiang et.al.|[2608.25487v1](http://arxiv.org/abs/2608.25487v1)|null|

#### Abstracts
##### **Video Generative Models as Geometry Learner**
2608.28549v1 by Haosen Yang, Jifei Song, Zhensong Zhang, Xiatian Zhu, Jiankang Deng

Recent generative approaches to geometry estimation adapt pretrained image diffusion models and treat the task as image-conditioned generation. Leveraging off-the-shelf image diffusion models, they either (i) train task-specific geometry models (for depth and surface normal estimation) independently, losing the opportunity of exploring the intrinsic correlation of these geometric targets, or (ii) jointly fine-tune modified image diffusion backbones (e.g., altered self-attention), which typically demands substantial labeled data. To overcome these limitations in a principled fashion, we repurpose pretrained video generative models as a unified and data-efficient framework for geometry estimation, formulated innovatively as a next-frames prediction task. Our method, GeoNeXt, inherits naturally structured knowledge and richer priors from the video model, while further adapting them for joint modeling of images and geometry targets (image <-> geometry), enabling more data efficient and effective learning of geometry. Extensive experiments validate our method for zero-shot monocular depth and surface normal estimation across diverse datasets, outperforming both previous task-specific and unified generative competitors while using substantially less training data. Notably, our method rivals discriminative state-of-the-art approaches trained on over 100x more data and even standouts on several benchmarks.

摘要：最近的幾何估計生成方法適應了預訓練的圖像擴散模型，並將任務視為圖像條件生成。利用現成的圖像擴散模型，它們要麼 (i) 獨立訓練特定任務的幾何模型（用於深度和表面法線估計），失去了探索這些幾何目標內在相關性的機會，要麼 (ii) 聯合微調修改過的圖像擴散骨幹（例如，改變的自注意力），這通常需要大量標記數據。為了以原則性的方式克服這些限制，我們將預訓練的視頻生成模型重新用作幾何估計的統一且數據高效的框架，並創新性地將其表述為下一幀預測任務。我們的方法 GeoNeXt 自然繼承了視頻模型中的結構化知識和更豐富的先驗，同時進一步調整它們以聯合建模圖像和幾何目標（圖像 &lt;-&gt; 幾何），使幾何的學習更加數據高效和有效。大量實驗驗證了我們的方法在多樣數據集上的零樣本單眼深度和表面法線估計中表現優異，超越了之前的特定任務和統一生成競爭者，同時使用的訓練數據顯著更少。值得注意的是，我們的方法與在超過 100 倍數據上訓練的判別最先進方法相抗衡，並在幾個基準測試中脫穎而出。

##### **LLM-Based Agents for Software and Systems Security: Approaches, Applications, and Assessment**
2608.28490v1 by Jingjing Nie, Jiawei Guo, Krishna Meda, Haipeng Cai

Software and systems security workflows are typically procedural: analysts inspect heterogeneous artifacts, form hypotheses, invoke tools, interpret outputs, and revise plans. Large language model (LLM)-based agents, which can plan, use tools, retain state, and revise actions across multi-step workflows, are being rapidly adopted to automate this work. Given the consequences of delegating security decisions to autonomous systems, understanding how such agents are built, used, and assessed is crucial. Yet to this date, there remains a lack of systematic understanding of what has been done and how far we are in this field: the term "agent" is applied inconsistently, applications differ sharply in risk, and assessment protocols are often incomparable. To gain a comprehensive and coherent view of this area hence inform relevant future research, this paper provides a systematic literature review of the (1) technical approaches, including agent architecture, perception, memory, reasoning and planning, action space, orchestration, and self-improvement, (2) applications, with respect to the security tasks served, and (3) assessment, including the datasets, outcome and trajectory metrics, safety measures, and baselines considered, over the peer-reviewed literature spanning the emergence of this area (2023--2026). Our synthesis reveals a field that has built agents able to act but not yet agents whose authority is bounded or whose behavior is auditable. In addition to knowledge systematization, we also extend our insights into the limitations of and challenges faced by current approach, application, and assessment designs, which shed light on potentially promising future research directions.

摘要：軟體和系統安全工作流程通常是程序性的：分析師檢查異質的工件，形成假設，調用工具，解釋輸出，並修訂計劃。基於大型語言模型（LLM）的代理正在迅速被採用來自動化這項工作，這些代理能夠計劃、使用工具、保持狀態並在多步工作流程中修訂行動。考慮到將安全決策委託給自主系統的後果，了解這些代理是如何構建、使用和評估的至關重要。然而，到目前為止，對於這一領域已經做了什麼以及我們的進展程度仍然缺乏系統性的理解：“代理”這個術語的應用不一致，應用在風險上差異明顯，評估協議往往無法比較。為了獲得這一領域的全面和一致的視角，從而為相關的未來研究提供信息，本文提供了一個系統的文獻綜述，包括（1）技術方法，涵蓋代理架構、感知、記憶、推理和規劃、行動空間、協調和自我改進，（2）應用，與所服務的安全任務相關，以及（3）評估，包括考慮的數據集、結果和軌跡指標、安全措施和基準，涵蓋了從這一領域出現以來的同行評審文獻（2023--2026）。我們的綜合分析顯示，這是一個已經構建出能夠行動的代理的領域，但尚未構建出其權限受到限制或行為可審計的代理。除了知識的系統化，我們還擴展了對當前方法、應用和評估設計所面臨的限制和挑戰的見解，這為潛在的有前景的未來研究方向提供了啟示。

##### **Blind Men and the Elephant: Probing the Epistemic Myopia of LLMs under Long-Tail Divergent Knowledge**
2608.28478v1 by Zhuoshi Pan, Junru Lu, Yan Qian, H. Vicky Zhao, Di Yin, Xing Sun

Factual question answering (QA) typically assumes a single canonical answer, obscuring whether large language models (LLMs) retain divergent accounts of long-tail facts. To address this gap, we introduce ElephantBench, a closed-book knowledge probe comprising 1,094 questions generated through an auditable graph-based pipeline. The pipeline retrieves related documents from a low-exposure web corpus, identifies naturally occurring disagreements, and converts them into multi-account QA records. Each answer is verified against the originating documents and authoritative public web sources and is then reviewed by human annotators. Across 32 models, even the strongest model recovers both accounts on only 52.4% of questions, while on nearly all remaining questions it recalls one account but omits the other. Scaling model size and inference-time reasoning improve recall but do not eliminate this incompleteness. Corpus analysis further shows that exposure imbalance favors the dominant account, whereas greater minority-side exposure is associated with more complete recall. These findings establish ElephantBench as a reproducible knowledge probe for diagnosing epistemic myopia in parametric memory. More broadly, our graph-based benchmark construction pipeline provides an efficient and scalable way to turn long-tail corpora into source-traceable knowledge probes, supporting efforts to evaluate and advance the epistemic rigour of next-generation LLMs. Code is available at https://github.com/Tencent/ElephantBench.

摘要：事實問題回答（QA）通常假設存在單一的典範答案，這掩蓋了大型語言模型（LLMs）是否保留了長尾事實的不同解釋。為了解決這一空白，我們引入了 ElephantBench，一個由 1,094 個問題組成的閉卷知識探測工具，這些問題是通過可審計的基於圖形的流程生成的。該流程從低曝光的網絡語料庫中檢索相關文檔，識別自然發生的分歧，並將其轉換為多賬戶 QA 記錄。每個答案都會根據原始文檔和權威的公共網絡來源進行驗證，然後由人工標註者進行審查。在 32 個模型中，即使是最強的模型也僅在 52.4% 的問題上恢復了兩種解釋，而在幾乎所有剩餘問題上，它回憶起一種解釋但忽略了另一種。擴大模型大小和推理時間的推理提高了回憶率，但並未消除這種不完整性。語料庫分析進一步顯示，曝光不平衡有利於主導解釋，而較大的少數方曝光則與更完整的回憶相關。這些發現確立了 ElephantBench 作為一個可重複的知識探測工具，用於診斷參數記憶中的認識近視。更廣泛地說，我們的基於圖形的基準構建流程提供了一種高效且可擴展的方式，將長尾語料庫轉化為可追溯來源的知識探測工具，支持評估和推進下一代 LLM 的認識嚴謹性。代碼可在 https://github.com/Tencent/ElephantBench 獲得。

##### **Acquire, Repair, Preserve: A Diagnosis-Guided Post-Training Recipe for Small-Model Dialogue Game Agents**
2608.28458v1 by Nan Li

Interactive dialogue games test a capability that static benchmarks largely leave implicit: a model must carry state across turns, interpret feedback, and choose valid actions under changing constraints. We study this setting in the LM Playschool Challenge with a 2B open-weight model, and find that many failures are not only broad knowledge failures but also local decision failures: repeated guesses, malformed actions, and violations of feedback that the model has just seen. These diagnostics motivate a training recipe organized around three steps: acquire broad game participation through supervised fine-tuning, repair mechanically verifiable failures within one targeted dialogue-game family using turn-local preference pairs, and preserve general capabilities beyond these dialogue games. In the official final evaluation, our submission improves public clemscore from 10.67 to 38.92 and closed in-domain score from 13.41 to 41.17, while approximately preserving aggregate static performance (44.14 vs. 44.24 for the baseline). Out-of-domain clemscore remains low at 7.88, with the largest gains concentrated in unseen variants of the targeted family. Our results suggest that broad SFT brings most of the model's capability improvement; turn-local supervision can be effective when failure detection is precise, with observed transfer concentrated primarily within-family.

摘要：互動對話遊戲測試了一種靜態基準大多數未明言的能力：模型必須在回合之間保持狀態，解釋反饋，並在變化的約束下選擇有效的行動。我們在 LM Playschool Challenge 中研究這個設定，使用一個 2B 開放權重模型，發現許多失敗不僅是廣泛知識的失敗，還是局部決策的失敗：重複猜測、格式錯誤的行動，以及違反模型剛剛看到的反饋。這些診斷結果促使我們制定一個由三個步驟組成的訓練方案：通過監督微調獲得廣泛的遊戲參與，使用回合局部偏好對修復機械可驗證的失敗，並在這些對話遊戲之外保持一般能力。在官方最終評估中，我們的提交將公共 clemscore 從 10.67 提升至 38.92，並將封閉領域得分從 13.41 提升至 41.17，同時大致保持總體靜態性能（基線為 44.14 對 44.24）。在域外 clemscore 仍然較低，為 7.88，最大的增益集中在目標家族的未見變體中。我們的結果表明，廣泛的 SFT 帶來了模型能力的主要改善；當失敗檢測精確時，回合局部監督可以有效，觀察到的轉移主要集中在同一家族內。

##### **VERA-8B: Evidence-Grounded Audit Risk Reasoning from SEC Filings**
2608.28402v1 by Menghan Liu, Elynn Chen

Across audit applications, judgments must be supported by reasonable evidence. However, standard financial language models prioritize fluency over evidence. They are built for general financial reasoning and may produce plausible but ambiguous answers, creating a grounding gap that makes them unsuitable for audit work. We address this gap with VERA-8B, a new end-to-end audit reasoning system that identifies audit risks before enforcement actions occur. Constructing such a model raises several challenges, as no prior machine learning work targets pre-enforcement audit prediction. To our knowledge, we are the first to unify SFT and GRPO for evidence-grounded audit reasoning under one evidence standard, achieving performance that surpasses all evaluated baselines. Because auditing cannot tolerate unsupported claims, we introduce abstention and uncertainty qualification to defer uncertain or evidence-incomplete cases. Finally, we design an AuditBridge to ground model reasoning for practical audit work. It transforms raw filings into verified records and then into reviewer-ready reports, bridging finance and computation with broad generality. Together, these components produce auditable, review-ready outputs suitable for practical audit work.

摘要：在審計應用中，判斷必須有合理的證據支持。然後，標準的財務語言模型優先考慮流暢性而非證據。它們是為一般財務推理而構建的，可能產生看似合理但含糊不清的答案，造成一個基礎差距，使其不適合審計工作。我們用VERA-8B來解決這個差距，這是一個新的端到端審計推理系統，能在執法行動發生之前識別審計風險。構建這樣的模型面臨幾個挑戰，因為之前沒有機器學習工作針對執法前的審計預測。據我們所知，我們是第一個將SFT和GRPO統一為一個證據標準下的基於證據的審計推理，實現了超越所有評估基準的性能。由於審計無法容忍沒有支持的主張，我們引入了放棄和不確定性資格，以推遲不確定或證據不完整的案例。最後，我們設計了一個AuditBridge，以支持模型推理的實際審計工作。它將原始文件轉換為經過驗證的記錄，然後轉換為審查者準備的報告，廣泛地橋接財務和計算。這些組件共同產生可審計的、準備好審查的輸出，適合實際的審計工作。

##### **BEACON: Behavior-Anchored Cross-Source Knowledge Graph Construction for Cyber Threat Intelligence**
2608.28394v1 by Changze Li, Yutong Cheng, Tsania Camila Finnisa, Qian Cui, Wei Ding, Peng Gao

Cyber threat intelligence (CTI) is foundational to modern cyber defense, yet much of it resides in unstructured reports whose volume and heterogeneity far exceed manual analysis, motivating research on automatically constructing knowledge graphs from CTI reports. However, existing approaches mainly extract partial information within a single report, leaving the cross-source setting unexplored, where the same threat is given unrelated names. Our key insight is that attack behaviors, once mapped to MITRE ATT&CK (a standardized catalog of attack techniques), can anchor the rest of a report. Attack behaviors are the adversarial actions a report describes, while contextual entities (e.g., threat actors, campaigns, and affected products) and Indicators of Compromise (IoCs; e.g., IP addresses) are their participants and traces. Attaching them to these anchors places every per-report graph in one canonical space.
  We realize this insight in BEACON, an LLM-driven framework for cross-source CTI knowledge graph construction. Its first stage extracts each report into a graph under a propose-then-verify paradigm, grounding candidates in report evidence and official ATT&CK definitions, to suppress LLM misclassification and hallucination. Its second stage merges these graphs with a hierarchical alignment strategy that applies signals in decreasing order of determinism, from character-level and semantic similarity to overlapping technique neighborhoods, iterating as merges pool neighborhoods. No existing benchmark links entities to technique anchors or provides cross-source alignment ground truth. We therefore construct and release two human-annotated datasets from 34 sources: to our knowledge the largest for report-level CTI extraction (8,395 elements) and the first for cross-source consolidation (3,487). On them, BEACON outperforms all baselines by at least 23% and 9%, respectively.

摘要：網路威脅情報（CTI）是現代網路防禦的基礎，然而其大部分存在於不結構化的報告中，這些報告的數量和異質性遠超過手動分析的能力，這促使了自動從CTI報告構建知識圖譜的研究。然而，現有的方法主要在單一報告中提取部分信息，未能探索跨來源的情境，在這種情境中，同一威脅被賦予不相關的名稱。我們的關鍵見解是，攻擊行為一旦映射到MITRE ATT&CK（攻擊技術的標準化目錄），就可以作為報告其餘部分的錨點。攻擊行為是報告中描述的對抗行動，而上下文實體（例如，威脅行為者、攻擊活動和受影響的產品）及妥協指標（IoCs；例如，IP地址）則是它們的參與者和痕跡。將它們附加到這些錨點上，將每個報告的圖譜置於一個標準化的空間中。<lb/>  
我們在BEACON中實現了這一見解，這是一個基於大型語言模型（LLM）的跨來源CTI知識圖譜構建框架。其第一階段在提出-然後驗證的範式下，將每個報告提取為圖譜，將候選項基於報告證據和官方ATT&CK定義進行定位，以抑制LLM的錯誤分類和幻覺。其第二階段則使用層次對齊策略合併這些圖譜，應用信號的確定性遞減順序，從字符級和語義相似性到重疊的技術鄰域，並在合併時迭代池鄰域。現有的基準中沒有任何將實體與技術錨點聯繫起來或提供跨來源對齊的真實數據。因此，我們從34個來源構建並發布了兩個人類標註的數據集：據我們所知，這是報告級CTI提取中最大的數據集（8,395個元素）和第一個跨來源整合的數據集（3,487個元素）。在這些數據集上，BEACON的表現超過所有基準，分別提高了至少23%和9%。

##### **Propagating construction-time knowledge quality into medical question answering: A framework grounded in clinical guidelines**
2608.28360v1 by Jie Hu, Junjie Wang, Shan Lu, Yifang Hu, Gong Cheng, Yun Liu

Large language models have facilitated knowledge graph (KG) construction from clinical guidelines, but extracted triples vary in structural validity and evidential support. Meanwhile, graph-augmented question answering (QA) systems typically optimize query relevance during retrieval, with limited reuse of quality information produced during KG construction. This creates a disconnect between construction-time quality control and inference-time evidence use. We investigate whether construction-time triple quality can serve as a persistent signal for downstream evidence selection and presentation. We propose a quality-aware framework that models structural conformance (SchemaConf) and evidential support (EvidScore) as complementary dimensions and fuses them into a per-triple quality signal, Q(t). Rather than using quality solely for filtering, the framework retains Q(t) and derived quality tiers as graph attributes and propagates them into quality-weighted subgraph retrieval and tier-conditioned evidence prompting, while preserving passage-level provenance. Experiments on Chinese diabetes clinical guidelines show that the utility of the quality signal is distribution dependent. Under cross-version and cross-model shift, the fused Q(t) provides stronger triple-quality discrimination than either component alone (AUC 0.748 vs. 0.703 for EvidScore and 0.645 for SchemaConf). In guideline-grounded QA, propagating construction-time quality reduces required-knowledge omission from 16.3% to 5.3% and conflicting outputs from 16.3% to 2.7%, with an evidence-grounded precision of 81.6% and near-zero invalid citations. Blinded clinician ratings favor the full framework over no retrieval (4.68 vs. 4.21 on a five-point scale) and approach the oracle condition (4.80), while cross-generator experiments show consistent trends.

摘要：大型語言模型促進了從臨床指導中構建知識圖譜（KG），但提取的三元組在結構有效性和證據支持方面存在差異。<lb/>同時，增強圖形的問題回答（QA）系統通常在檢索過程中優化查詢相關性，而對於在KG構建過程中產生的優質信息的重用則有限。<lb/>這造成了構建時質量控制與推理時證據使用之間的脫節。<lb/>我們研究構建時的三元組質量是否可以作為下游證據選擇和呈現的持久信號。<lb/>我們提出了一個質量感知框架，將結構一致性（SchemaConf）和證據支持（EvidScore）建模為互補維度，並將它們融合為每個三元組的質量信號Q(t)。<lb/>該框架不僅僅將質量用於過濾，而是保留Q(t)和派生質量層級作為圖形屬性，並將它們傳播到質量加權的子圖檢索和層級條件的證據提示中，同時保留段落級的來源信息。<lb/>對中國糖尿病臨床指導的實驗表明，質量信號的效用依賴於分佈。<lb/>在跨版本和跨模型轉變下，融合的Q(t)提供了比任何單一組件更強的三元組質量區分（AUC 0.748對比EvidScore的0.703和SchemaConf的0.645）。<lb/>在以指導為基礎的QA中，傳播構建時的質量將所需知識的遺漏從16.3%減少到5.3%，衝突輸出從16.3%減少到2.7%，證據基礎的精確度為81.6%，幾乎沒有無效引用。<lb/>盲評的臨床醫生評分更青睞完整框架而非無檢索（在五分制上為4.68對比4.21），並接近神諭條件（4.80），而跨生成器的實驗顯示出一致的趨勢。

##### **AGENT-O: A Semantic Agent Card Framework for Interoperable and Governed Healthcare AI Agents**
2608.28345v1 by Pengze Li, Cui Tao

AGENT-O is a modular ontology framework that defines a semantic Agent Card for representing health-oriented AI agent systems and supports assessment of reporting completeness in scientific publications. AGENT-O was developed as an OWL 2/RDF ontology covering runtime, models, workflow, tools, clinical use, evaluation, provenance, governance, and reporting assessment. Evaluation included ontology inventory, OWL-RL reasoning, three SHACL suites, 12 SPARQL competency queries, three cases, and model-assisted reporting-completeness assessment of 279 papers across five dimensions. The ontology contained 1,962 RDF triples and 1,922 Protege axioms, with 252 active classes, 198 active object properties, and 51 datatype properties. All SHACL suites conformed on example graphs, all competency queries returned prespecified evidence, and all 279 papers were scored. Incomplete reporting was highest for runtime/architecture (84.6%), governance/safety (82.8%), and provenance/reproducibility (78.1%), compared with evaluation (25.8%) and benchmark-process alignment (29.8%). AGENT-O supported semantic Agent Card representation and reporting assessment while revealing an evaluation-specification gap: evaluation and benchmark procedures were reported more consistently than runtime architecture, governance, and reproducibility. AGENT-O provides a reusable ontology, semantic Agent Card profile, and reporting-completeness workflow for structured reporting and gap identification, but does not assess agent quality or deployment readiness.

摘要：AGENT-O 是一個模組化的本體框架，定義了一個語義代理卡，用於表示以健康為導向的 AI 代理系統，並支持對科學出版物報告完整性的評估。AGENT-O 被開發為一個 OWL 2/RDF 本體，涵蓋了運行時、模型、工作流程、工具、臨床使用、評估、來源、治理和報告評估。評估包括本體清單、OWL-RL 推理、三個 SHACL 套件、12 個 SPARQL 能力查詢、三個案例，以及對 279 篇論文在五個維度上的模型輔助報告完整性評估。該本體包含 1,962 個 RDF 三元組和 1,922 個 Protege 公理，擁有 252 個活躍類別、198 個活躍對象屬性和 51 個數據類型屬性。所有 SHACL 套件在示例圖上均符合，所有能力查詢均返回預先指定的證據，且所有 279 篇論文均已評分。運行時/架構（84.6%）、治理/安全（82.8%）和來源/可重複性（78.1%）的報告不完整性最高，而評估（25.8%）和基準過程對齊（29.8%）則較低。AGENT-O 支持語義代理卡表示和報告評估，同時揭示了評估規範的差距：評估和基準程序的報告一致性高於運行時架構、治理和可重複性。AGENT-O 提供了一個可重用的本體、語義代理卡配置文件和報告完整性工作流程，用於結構化報告和差距識別，但不評估代理質量或部署準備性。

##### **BanglaMed-QA: A Question Answering System for Healthcare Support in Bangla**
2608.28329v1 by Rowzatul Zannat, Abdullah Al Shafi, K. M. Azharul Hasan, Atia Shahnaz Ipa

Medical question answering (QA) systems have become crucial tools for providing reliable health information. But they remain very unexplored for low-resource languages like Bangla due to limited datasets and systems tailored to these languages. To address this, we introduce BanglaMed-QA, a robust QA system specifically designed for the Bangla medical domain. The process begins with building a structured medical knowledge base that includes 4,493 QA pairs in 9 categories under 506 diseases. To improve semantic comprehension, domain-specific root word dictionaries and synonym sets are proposed, in addition to part-of-speech tagging for anaphora resolution. We adopt supervised machine learning models in which SVM is found to be the best model to categorize questions. Multiple similarity metrics, including cosine, Jaccard, BM25, and Levenshtein, are applied with soft and hard voting methods for query matching. The performance of the QA system has been evaluated in two aspects, with a 95% F1 score in an automated evaluation and an average human satisfaction rating of 0.9 out of 1.0. This validates the real-world application of BanglaMed-QA in closing the healthcare information gap for Bangla speakers.

摘要：醫療問答（QA）系統已成為提供可靠健康資訊的重要工具。<lb/>但由於缺乏針對這些語言的數據集和系統，低資源語言如孟加拉語仍然未被充分探索。<lb/>為了解決這個問題，我們推出了BanglaMed-QA，一個專門為孟加拉醫療領域設計的強大QA系統。<lb/>該過程始於建立一個結構化的醫療知識庫，包括在506種疾病下的9個類別中的4,493個QA對。<lb/>為了改善語義理解，除了針對指代解析的詞性標註外，還提出了特定領域的根詞字典和同義詞集。<lb/>我們採用監督式機器學習模型，其中SVM被發現是分類問題的最佳模型。<lb/>多種相似度度量，包括餘弦相似度、Jaccard、BM25和Levenshtein，與軟投票和硬投票方法一起應用於查詢匹配。<lb/>QA系統的性能在兩個方面進行了評估，自動評估中的F1分數為95%，人類滿意度平均評分為0.9（滿分1.0）。<lb/>這驗證了BanglaMed-QA在填補孟加拉語使用者的醫療資訊空白方面的實際應用。

##### **MAIL: Memory-driven, Adaptive, Incremental, and Literature-grounded Framework for Hypothesis Generation in Chemistry**
2608.28315v1 by Mahdi Babaei, Xueshen Li, Yutao Kuang, Jolene P. Reid, Yu Gan

The ever-expanding volume of the chemical literature offers unprecedented opportunities to generate novel and impactful hypotheses. However, the bottleneck lies in efficiently navigating this vast knowledge base to formulate high-quality, experimentally meaningful insights.
  While Large Language Models (LLMs) show promise for this task, existing methods often rely on static inspiration corpora, predefined heuristics, or laborious human-in-the-loop pipelines and decision-support frameworks that limit scalability and novelty. In this work, we propose an automated approach, a Memory-augmented, Adaptive, Incremental, and Literature-grounded (MAIL) framework for hypothesis generation in chemistry. Our MAIL method formulates hypothesis generation as a temporally grounded, memory-driven reasoning process, where hypotheses emerge from an evolving conceptual path that continuously accumulates and reinterprets prior knowledge. We evaluated the MAIL framework on a public TOMATO-Chem dataset and a newly curated and disseminated high-novelty nature/science challenge (HN-NS) dataset. Across both datasets, MAIL generates structurally coherent and mechanistically plausible hypotheses, achieves the highest MIOS and MPOS by more effectively recovering the central ideas and methodological elements of the historical target hypotheses, and obtains the highest overall expert-evaluation scores for scientific quality. These results demonstrate the potential of LLMs to autonomously explore chemical domains and generate hypotheses that are both innovative and chemically plausible.

摘要：化學文獻不斷擴增的量為生成新穎且具影響力的假說提供了前所未有的機會。<lb/>然而，瓶頸在於如何有效地導航這個龐大的知識庫，以形成高品質且實驗上有意義的見解。<lb/>雖然大型語言模型（LLMs）在這項任務中顯示出潛力，但現有的方法往往依賴於靜態的靈感語料庫、預定的啟發式方法，或繁瑣的人機協作流程和決策支持框架，這限制了可擴展性和新穎性。<lb/>在本研究中，我們提出了一種自動化的方法，即增強記憶、自適應、增量和文獻基礎的（MAIL）框架，用於化學中的假說生成。<lb/>我們的MAIL方法將假說生成表述為一個時間上有根據的、以記憶驅動的推理過程，其中假說源於一條不斷演變的概念路徑，該路徑持續累積並重新詮釋先前的知識。<lb/>我們在公共的TOMATO-Chem數據集和一個新近策劃和發佈的高新穎性自然/科學挑戰（HN-NS）數據集上評估了MAIL框架。<lb/>在這兩個數據集上，MAIL生成了結構上連貫且機制上合理的假說，通過更有效地恢復歷史目標假說的核心思想和方法元素，達到了最高的MIOS和MPOS，並獲得了最高的整體專家評估科學質量分數。<lb/>這些結果展示了LLMs在自主探索化學領域和生成既具創新性又化學上合理的假說的潛力。

##### **AIM: Anchor Identity Features, Then Match for Multimodal Large Language Model Unlearning**
2608.28312v1 by Wonjun Lee, Jaehyuk Jang, Kangwook Ko, Hee-Seon Kim, Changick Kim

Multimodal large language models (MLLMs) can memorize identity-specific facts about people in their fine-tuning data, creating privacy risks when a person requests deletion. Existing MLLM unlearning methods often assume access to retain images or ground-truth answers during deletion, which is unrealistic in many practical scenarios. We study identity unlearning when retain images are unavailable at deletion time. Our analysis shows that identity and visual-perception questions occupy distinct regions in fine-tuned hidden states and are organized differently: identity questions cluster by person, whereas perception questions cluster by question type. This suggests that identity knowledge can be suppressed without erasing general visual perception. Building on this observation, we propose AIM, a two-stage method that anchors an identity-forgetting target with a universal visual prompt and then matches the vision encoder to that target under a Fisher-based constraint. Extensive experiments show that AIM achieves competitive identity forgetting while preserving non-deleted identities, prior knowledge, and visual perception on the same images.

摘要：多模態大型語言模型（MLLMs）可以記住有關人員的特定身份事實，這些事實來自其微調數據，當某人請求刪除時，會產生隱私風險。現有的MLLM遺忘方法通常假設在刪除過程中可以訪問保留的圖像或真實答案，這在許多實際情況下是不切實際的。我們研究在刪除時無法獲得保留圖像的身份遺忘情況。我們的分析顯示，身份和視覺感知問題在微調的隱藏狀態中佔據不同的區域，並且組織方式不同：身份問題按人群聚集，而感知問題則按問題類型聚集。這表明身份知識可以被抑制，而不必抹去一般的視覺感知。基於這一觀察，我們提出了AIM，一種兩階段的方法，通過通用的視覺提示來固定身份遺忘目標，然後在基於Fisher的約束下將視覺編碼器與該目標匹配。大量實驗表明，AIM在保留未刪除身份、先前知識和相同圖像上的視覺感知的同時，實現了具有競爭力的身份遺忘。

##### **PanelShield: Verifiable Closed-Loop Safe Planning for Robotic Industrial Panel Operation**
2608.28305v1 by Guipeng Xin, Jiahe Xu, Chenhui Wan, Jie Liu, Youmin Hu, Zhongxu Hu

Industrial panel operation is knowledge-intensive and safety-critical. Beyond control recognition and action generation, execution must satisfy constraints in operation manuals and safety regulations. While foundation-model-based planners show strong semantic capability, they typically lack computable, localizable, and reproducible mechanisms for violation detection and repair. To address this, we propose PanelShield, a verifiable closed-loop safety planning framework for manual-guided industrial panel operation. The framework generates parameterized action primitive sequences from task-relevant manual evidence and applies dual formal verification with LTL and a Safety FSM to enforce cross-step temporal correctness and local transition legality. When violations occur, it outputs a structured counterexample with the earliest violating step and cause, enabling targeted repair and re-verification. We build a multi-level long-horizon planning benchmark covering three representative industrial device panels, and evaluate the framework in simulation and real-world robotic experiments. Results show that PanelShield improves complex safety-constrained task performance over foundation-model-only planning baselines while reducing the violation rate to 2.7%, with 4.1 s total latency. Real-world experiments demonstrate end-toend feasibility. Overall, PanelShield offers a verifiable approach to robotic panel operation that balances flexibility, safety, and auditability.

摘要：工業面板操作是知識密集型且安全關鍵的。除了控制識別和行動生成外，執行必須滿足操作手冊和安全法規中的約束條件。雖然基於基礎模型的規劃者顯示出強大的語義能力，但它們通常缺乏可計算、可定位和可重現的違規檢測和修復機制。為了解決這個問題，我們提出了PanelShield，一個可驗證的閉環安全規劃框架，用於手動引導的工業面板操作。該框架從與任務相關的手動證據生成參數化的行動原語序列，並應用LTL和安全有限狀態機進行雙重形式驗證，以強制執行跨步驟的時間正確性和局部轉換的合法性。當發生違規時，它輸出一個結構化的反例，包含最早的違規步驟和原因，從而實現有針對性的修復和重新驗證。我們建立了一個涵蓋三個代表性工業設備面板的多層次長期規劃基準，並在模擬和現實世界的機器人實驗中評估該框架。結果顯示，PanelShield在複雜的安全約束任務表現上優於僅基於基礎模型的規劃基準，同時將違規率降低至2.7%，總延遲為4.1秒。現實世界的實驗證明了端到端的可行性。總體而言，PanelShield提供了一種可驗證的機器人面板操作方法，平衡了靈活性、安全性和可審計性。

##### **MaCoPlanner: LLM-Assisted Manual-Compiled Task Planning with Proactive Safety Verification for Robotic Industrial Panel Operation**
2608.28300v1 by Guipeng Xin, Jiahe Xua, Mohammad Deghat, Chenhui Wan, Jie Liu, Youmin Hu, Zhongxu Hu

Robotic industrial panel operation requires not only accurate control localization but also compliance with operating procedures, safety rules, and device-state constraints distributed across heterogeneous manuals. This study presents MaCoPlanner, a task-planning framework built on knowledge compiled from equipment manuals that converts equipment manuals into a typed intermediate representation, retrieves task- and state-relevant evidence, and uses it to support plan generation. Before actuation, candidate plans are symbolically rolled out and checked against procedural and state-transition constraints; detected violations are localized and returned for targeted repair, while unresolved plans are rejected. A separate execution interface grounds verified symbolic actions to physical controls and updates the device state. Under an independent evaluation oracle, MaCoPlanner achieves a final violation rate of 2.7%, and 26.3% of the runs in the repair analysis are rejected after exhausting the refinement budget. Compared with Raw-Manual, task success increases from 62.8% to 84.4% on Level-2 tasks and from 25.9% to 43.2% on Level-3 tasks. Experiments on a controller-panel simulator without an attached industrial load further demonstrate integrated execution feasibility under representative interaction conditions, without claiming industrial deployment readiness.

摘要：機器人工業面板操作不僅需要準確的控制定位，還需要遵守操作程序、安全規則和分佈在異構手冊中的設備狀態約束。這項研究提出了MaCoPlanner，一個基於從設備手冊編輯的知識構建的任務規劃框架，它將設備手冊轉換為類型化的中介表示，檢索與任務和狀態相關的證據，並利用這些證據支持計劃生成。在啟動之前，候選計劃會被符號化展開並檢查是否符合程序和狀態轉換約束；檢測到的違規行為會被定位並返回進行針對性修復，而未解決的計劃則會被拒絕。一個獨立的執行介面將經過驗證的符號行動與物理控制相結合並更新設備狀態。在獨立評估的oracle下，MaCoPlanner實現了最終違規率為2.7%，在修復分析中，26.3%的運行在耗盡精煉預算後被拒絕。與Raw-Manual相比，任務成功率在Level-2任務中從62.8%提高到84.4%，在Level-3任務中從25.9%提高到43.2%。在不附加工業負載的控制器面板模擬器上的實驗進一步展示了在代表性交互條件下的集成執行可行性，而不聲稱具備工業部署準備。

##### **Regime-Aware Portfolio Management via Retrieval-Augmented LLM-Guided Expert Switching**
2608.28252v1 by Ahmad Asadi, Reza Safabakhsh

Financial markets are inherently non-stationary, making the effectiveness of individual portfolio-management strategies highly dependent on changing market conditions. This work proposes a retrieval-augmented expert-switching framework that dynamically selects portfolio management experts based on their historical performance under similar market situations. A dual-stream variational autoencoder represents asset-level and market-wide information, while a retrieval-based knowledge base stores historical situations and expert performance. During inference, an instruction-tuned LLM reasons over the retrieved evidence to identify the most appropriate expert rather than directly generating portfolio actions. We further establish a monotonicity property showing that adding a locally superior expert cannot degrade the switching mechanism's performance. Experiments across cryptocurrency, stock, and foreign-exchange markets show that the proposed selector achieves the highest cumulative return and Sharpe ratio among the evaluated selection strategies in all three markets. In the stock market, for example, cumulative return increases from 26% for the best fixed expert to 34%, while the Sharpe ratio improves from 0.74 to 0.96. Ablation results confirm the importance of both retrieval and LLM reasoning, while experiments with different expert-pool sizes demonstrate the value of complementary expertise. Overall, the findings support retrieval-grounded expert switching as an effective approach to adaptive portfolio management in non-stationary financial environments.

摘要：金融市場本質上是非平穩的，使得個別投資組合管理策略的有效性高度依賴於不斷變化的市場條件。這項工作提出了一個檢索增強的專家切換框架，根據專家在類似市場情況下的歷史表現動態選擇投資組合管理專家。一個雙流變分自編碼器表示資產級別和市場範圍的信息，而基於檢索的知識庫則存儲歷史情況和專家表現。在推理過程中，經過指令調整的LLM對檢索到的證據進行推理，以識別最合適的專家，而不是直接生成投資組合行動。我們進一步建立了一個單調性特性，顯示添加一個局部優越的專家不會降低切換機制的性能。在加密貨幣、股票和外匯市場的實驗中，所提出的選擇器在所有三個市場中實現了最高的累積回報和夏普比率。在股票市場中，例如，最佳固定專家的累積回報從26%增加到34%，而夏普比率從0.74改善到0.96。消融結果確認了檢索和LLM推理的雙重重要性，而不同專家池大小的實驗則展示了互補專業知識的價值。總體而言，這些發現支持基於檢索的專家切換作為在非平穩金融環境中進行自適應投資組合管理的有效方法。

##### **Generative AI Alignment with Hinduism's Theological Plurality and Sacred Representation**
2608.28228v1 by Dipto Das, Arpita Kundu, Nusrat Jahan Mim, Shion Guha, Syed Ishtiaque Ahmed

Generative AI systems are increasingly used to answer personal questions and mediate everyday practices, including religion. However, existing discussions around AI alignment and ethics have largely centered secular, Western, and Abrahamic assumptions about religion, offering limited attention to other faith-based traditions. In this paper, we examine how Hindu users engage with generative AI systems in relation to their religious knowledge, belief, and practice. Drawing on 15 semi-structured interviews with Bangladeshi Hindu participants, we analyze how users interpret AI-generated religious representations, scriptural explanations, devotional interactions, and synthetic religious media. We found that AI can be both accessible and ethically troubling. While AI supported scriptural inquiry, devotional visualization, and religious storytelling, our study also identified concerns about theological flattening, cultural misrepresentation, devotional manipulation, and the simulation of sacred presence and authority. We conclude by arguing that religious alignment in generative AI requires interpretive alignment: systems that disclose their limits, preserve plurality, and avoid simulating sacred authority and sycophantic personalization.

摘要：生成式人工智慧系統越來越多地被用來回答個人問題和調解日常實踐，包括宗教。然而，現有的有關人工智慧對齊和倫理的討論主要集中於世俗、西方和亞伯拉罕宗教對宗教的假設，對其他基於信仰的傳統關注有限。在本文中，我們探討了印度教用戶如何在其宗教知識、信仰和實踐方面與生成式人工智慧系統互動。根據對15位孟加拉國印度教參與者的半結構性訪談，我們分析了用戶如何解釋人工智慧生成的宗教表徵、經文解釋、虔誠互動和合成宗教媒體。我們發現，人工智慧既可以是可接觸的，也可能在倫理上令人困擾。雖然人工智慧支持經文探究、虔誠視覺化和宗教故事講述，但我們的研究也識別了有關神學扁平化、文化誤表現、虔誠操控以及神聖存在和權威模擬的擔憂。我們的結論是，生成式人工智慧中的宗教對齊需要詮釋對齊：系統必須披露其局限性，保持多元性，並避免模擬神聖權威和諂媚的個性化。

##### **Beyond Flat Netlist: Hierarchical Graph Representation Learning for Scalable Analysis of Sequential Circuits**
2608.28188v1 by Jingyi Zhou, Zhengyuan Shi, Jiaying Zhu, Ziyang Zheng, Qiang Xu

Circuit Representation Learning (CRL) offers a powerful paradigm to guide and optimize core Electronic Design Automation (EDA) tasks, but its practical adoption is hindered by the immense scale of industrial netlists and a failure to explicitly model register-level temporal dynamics. To overcome these barriers, we introduce DeepSeq3, a novel hierarchical framework that abstracts circuits into a two-level representation: fine-grained combinational subgraphs partitioned by flip-flops (FFs), and a high-level Super-Node Graph (SNG) that models the register-transfer structure. A dual Graph Neural Network (GNN) architecture learns representations at both levels, capturing local Boolean logic and global state transitions. Crucially, we introduce a state-centric pre-training scheme that predicts the reachability between FF states, endowing the model with a deep understanding of temporal behavior. Demonstrated on large-scale benchmarks, DeepSeq3's approach yields superior scalability and richer representations, reducing bounded model checking (BMC) solving time by 18% while guaranteeing correctness.

摘要：電路表徵學習（CRL）提供了一個強大的範式，用於指導和優化核心電子設計自動化（EDA）任務，但其實際採用受到工業網表龐大規模和未能明確建模寄存器級時間動態的阻礙。為了克服這些障礙，我們引入了DeepSeq3，一個新穎的分層框架，將電路抽象為兩級表徵：由觸發器（FFs）劃分的細粒度組合子圖，以及一個高級的超節點圖（SNG），該圖建模寄存器傳輸結構。一個雙重圖神經網絡（GNN）架構在兩個層級上學習表徵，捕捉局部布爾邏輯和全局狀態轉換。關鍵是，我們引入了一種以狀態為中心的預訓練方案，該方案預測FF狀態之間的可達性，賦予模型對時間行為的深刻理解。在大規模基準測試中展示，DeepSeq3的方法實現了更優的可擴展性和更豐富的表徵，將有界模型檢查（BMC）求解時間縮短了18%，同時保證正確性。

##### **Expert Knowledge & Machine Understanding: Bridging Reactome's Ontology with LLM Semantic Embeddings**
2608.28178v1 by Susanna Bravi, Riccardo De Luca, Rosa Sicilia, Christine Nardini, Mario Santoro

Biological knowledgebases like Reactome provide high-quality pathways that include biological elements' relationships and textual descriptions (metadata). The quality of such pathways is granted by manual curation, that presents, however, significant scalability challenges. Lately, numerous NLP tools have been proposed to cope with this issue, leveraging textual information to automatically expand biological knowledgebases. However, little exploration has been done so far to assess whether relationships among textual descriptions mirror higher order biological relationships. This study explores whether human-written descriptions in Reactome can be used to infer the experts' defined global hierarchical structure. To test this, we extracted from Reactome the Homo Sapiens hierarchy of pathways and their reactions (Reactome Hierarchy), and used textual metadata to reconstruct a Semantic Hierarchy, combining a sentence transformer model (SPECTER2) with a modified agglomerative nesting algorithm and a graph reconstruction algorithm. Quantitative (Laplacian Spectral Distance and Bootstrapping) and qualitative (global topological metrics) analyses confirm our hypothesis and indicate that the global hierarchical structure of pathways can be inferred by experts textual metadata.

摘要：生物知識庫如 Reactome 提供高品質的途徑，包括生物元素之間的關係和文本描述（元數據）。這些途徑的質量是通過手動編輯來保證的，但這帶來了顯著的擴展性挑戰。最近，提出了許多自然語言處理工具來應對這一問題，利用文本信息自動擴展生物知識庫。然而，到目前為止，對於文本描述之間的關係是否反映了更高階的生物關係，尚未進行太多探索。本研究探討了 Reactome 中人類撰寫的描述是否可以用來推斷專家定義的全球層級結構。為了測試這一點，我們從 Reactome 提取了 Homo Sapiens 的途徑及其反應的層級結構（Reactome 層級），並使用文本元數據重建語義層級，結合了一個句子變換模型（SPECTER2）與一個修改過的聚合嵌套算法和圖重建算法。定量（拉普拉斯光譜距離和自助法）和定性（全球拓撲指標）分析確認了我們的假設，並表明途徑的全球層級結構可以通過專家的文本元數據推斷出來。

##### **Gen-TAS: A Generative AI-Aided Hardware-Software Task Allocation Framework for FPGA-GPP Heterogeneous Systems**
2608.28160v1 by Mary Kong, Yuqin Zhao, Semih Vazgecen, Cristian Sestito, Themis Prodromakis

FPGA-GPP heterogeneous systems combine software flexibility with the performance and energy efficiency of reconfigurable hardware. However, determining which application tasks should execute on the GPP or FPGA requires extensive expertise and design-space exploration, particularly when user objectives vary across latency, communication, resource utilisation, and power. This paper proposes Gen-TAS, a knowledge-grounded LLM framework for user-specific FPGA-GPP task allocation. By combining task-graph analysis with RAG, Gen-TAS grounds LLM reasoning in historical implementation knowledge and generates multiple explainable strategies tailored to the specified objectives. Human-in-the-loop selection and a deterministic backend connect LLM-generated decisions to reproducible FPGA SoC implementations. Experiments on CNN and SDR workloads across multiple LLMs demonstrate stable, requirement-driven allocation. Under latency-oriented objectives, implementations following the selected strategies achieve speedups of up to 2.45$\times$ and 92.53$\times$, respectively, relative to the corresponding all-GPP baselines while other objectives select strategies that trade some acceleration performance for FPGA-GPP communication, resource utilisation, or FPGA power.

摘要：FPGA-GPP 異構系統結合了軟體的靈活性與可重配置硬體的性能和能源效率。<lb/>然而，確定哪些應用任務應在 GPP 或 FPGA 上執行需要廣泛的專業知識和設計空間探索，尤其是在用戶目標在延遲、通信、資源利用和功耗上有所不同的情況下。<lb/>本文提出了 Gen-TAS，一個基於知識的 LLM 框架，用於用戶特定的 FPGA-GPP 任務分配。<lb/>通過將任務圖分析與 RAG 結合，Gen-TAS 將 LLM 推理基於歷史實現知識，並生成多種針對特定目標的可解釋策略。<lb/>人機互動的選擇和確定性後端將 LLM 生成的決策與可重現的 FPGA SoC 實現相連接。<lb/>在多個 LLM 上對 CNN 和 SDR 工作負載的實驗顯示出穩定的、需求驅動的分配。<lb/>在以延遲為導向的目標下，根據所選策略的實現分別達到高達 2.45$\times$ 和 92.53$\times$ 的加速，相對於相應的全 GPP 基準，而其他目標則選擇在 FPGA-GPP 通信、資源利用或 FPGA 功耗上進行一些加速性能的權衡策略。

##### **FinExam-10K: When Retrieval Helps Financial Reasoning?**
2608.28155v1 by Yan Lin, Jingyu Sun, Zhongliang Guo, Qing Li, Zhuohan Xie, Yuxia Wang

Professional financial examinations require models to combine domain knowledge, calculation, and judgment, yet no benchmark covers the full CFA and FRM structure under one protocol. We introduce FinExam-10K, to our knowledge the largest reported English benchmark for this setting, with 10,198 expert-reannotated questions spanning CFA Levels I-III and FRM Parts I-II. We release 5,110 questions and sequester 5,088 for a quarterly maintained leaderboard. To separate coverage from local answerability, we report a 10,198-item Full-Coverage Track and a 7,625-item Context-Complete Reasoning Track, which is the primary basis for claims about reasoning from the supplied record. Across 17 models, the best accuracy is 85.29% overall. On the frozen Hard band, the best score is 34.68% on the Full-Coverage Track and 54.57% on the 372 context-complete items. All 17 models share 47 context-complete failures. Function-RAG and FunctionGraph-RAG rescue hundreds of errors but also overturn many correct answers, producing little or negative net gain. A gate trained only on public data decides from the question and initial response when FunctionGraph-RAG should run. On the 5,088 held-out items, the gate invokes FunctionGraph-RAG for 7.9% of questions and improves accuracy from 70.83% to 71.23% (p = .0446).

摘要：專業的金融考試需要模型結合領域知識、計算和判斷，但目前沒有一個基準能在一個協議下涵蓋完整的CFA和FRM結構。我們介紹了FinExam-10K，據我們所知，這是目前報告的最大英文基準，擁有10,198個專家重新標註的問題，涵蓋CFA I-III級和FRM I-II部分。我們釋出5,110個問題，並將5,088個問題隔離以維護季度排行榜。為了將覆蓋範圍與地方可回答性區分開來，我們報告了一個10,198項的全覆蓋追蹤和一個7,625項的上下文完整推理追蹤，這是關於從提供的記錄推理聲明的主要基礎。在17個模型中，最佳準確率為85.29%。在凍結的Hard band上，最佳得分為全覆蓋追蹤的34.68%和372個上下文完整項目的54.57%。所有17個模型共享47個上下文完整的失敗。Function-RAG和FunctionGraph-RAG修正了數百個錯誤，但也推翻了許多正確答案，產生的淨增益很小或為負。僅在公共數據上訓練的門控根據問題和初始回應決定何時運行FunctionGraph-RAG。在5,088個保留項目中，門控對7.9%的問題調用FunctionGraph-RAG，並將準確率從70.83%提高到71.23%（p = .0446）。

##### **WeAgent-MMSearch: Native Text-Vision Interaction for Multimodal Search Agents**
2608.28062v1 by Zongkai Liu, Hui Zhang, Liqiang Niu, Zhen Cao, Han Li, Juntao Liu, Wenchao Chen, Chengduo Zhao, Chao Yu, Fandong Meng

Multimodal search agents extend parametric knowledge with newly emerging and long-tail evidence from the open web. Yet many existing agentic search environments often expose retrieved evidence only as text and omit tool-returned images from subsequent context, reducing visually grounded trajectories to text-only reasoning. Long-horizon interaction also compounds tool-call, response-length, timeout, and budget failures, which can discard salvageable trajectories, waste rollout computation, and disturb policy updates. To address these issues, we introduce WeAgent-Harness, a multimodal agentic harness that supports native text-vision interaction and runtime recovery. Retrieved images receive persistent disk references, allowing the model to inspect, process, and cite them throughout the trajectory. Based on this harness, we develop WeAgent-MMSearch, an integrated system spanning data construction, agentic post-training, and multimodal rollout. For data construction, a strong MLLM uses WeAgent-Harness to discover, synthesize, and verify MMSearch-style tasks and collect expert trajectories. During post-training, our Failure-Aware GSPO (FA-GSPO) recovers salvageable abnormal rollouts and filters invalid ones to improve bounded multimodal planning and search.We also introduce VisTarget-Bench, a 150-task human-verified benchmark that pairs each question with a held-out target image, distinguishing image-retrieval failures from visual-perception failures. Evaluation on VisTarget-Bench and seven public benchmarks shows that agentic post-training improves the average score by 19.22 points, enabling our model to outperform similarly sized open-source models and rival models with roughly ten times its parameter count.

摘要：多模態搜索代理擴展了參數知識，並從開放網絡中引入新興和長尾證據。然而，許多現有的代理搜索環境往往僅以文本形式展示檢索到的證據，並省略工具返回的圖像，將視覺基礎的軌跡簡化為僅基於文本的推理。長期互動還會加劇工具調用、響應長度、超時和預算失敗，這可能會丟棄可挽救的軌跡，浪費展開計算，並擾亂政策更新。為了解決這些問題，我們引入了 WeAgent-Harness，一個支持原生文本-視覺互動和運行時恢復的多模態代理鞍具。檢索到的圖像獲得持久的磁碟引用，允許模型在整個軌跡中檢查、處理和引用它們。基於這個鞍具，我們開發了 WeAgent-MMSearch，一個涵蓋數據構建、代理後訓練和多模態展開的綜合系統。對於數據構建，一個強大的 MLLM 使用 WeAgent-Harness 來發現、合成和驗證 MMSearch 風格的任務並收集專家軌跡。在後訓練期間，我們的失敗感知 GSPO (FA-GSPO) 恢復可挽救的異常展開並過濾無效的展開，以改善有界的多模態規劃和搜索。我們還引入了 VisTarget-Bench，一個包含 150 個經人類驗證的任務基準，將每個問題與一個保留的目標圖像配對，區分圖像檢索失敗與視覺感知失敗。在 VisTarget-Bench 和七個公共基準上的評估顯示，代理後訓練使平均分數提高了 19.22 分，使我們的模型能夠超越同樣規模的開源模型，並與大約十倍於其參數數量的競爭模型相抗衡。

##### **String: An Agentic OS Where Every App Is a Markdown File**
2608.28027v1 by Jookyung Song, Nojun Kwak, Simyung Chang

LLM agents have become a new class of software user, but every surface they work through was designed for someone else. Pages are built for human eyes, which can skim and ignore; tool schemas for programs, which pay nothing to carry definitions they never call. An agent has neither luxury: it re-reads, and pays again for, everything it is shown on every turn. We present String, an open-source runtime that gives this user an interface of its own and treats the job as an operating-systems problem. Tool knowledge moves out of the agent's context and into a common layer that renders it back one view at a time as Markdown. A single SFMD (String-Flavored Markdown) document declares an application's views, typed actions, navigation, and credentials, and the runtime handles discovery, validation, execution, state, and secrets behind two core verbs: /open to see and /act to do. Web and app turn out to be two renderings of one architecture: an SFMD site serves styled HTML to browsers and the raw document to agents, so one grammar reaches apps, files, shells, and the web, even legacy HTML, with no per-site integration. Views stay partial by design, and the staging is causal: disclosing one tier of detail a single turn too early costs up to 23 accuracy points, while proper staging drops wrong-action selection from 28% to 2%. Privilege follows provenance: a remote page may call HTTP but never the shell, and caller-supplied text never expands a stored secret. On an 87-task benchmark that pairs each task with curated skills, operationalizing those procedures as on-demand String apps yields comparable aggregate success across six models from frontier to small (+1.3pp) while using 33.5% fewer tokens among completed episodes, and the resident interface stays a constant 53 tokens at any catalog size. We report the design, the evaluation, and what three months of production use taught us.

摘要：LLM 代理已成為一種新的軟體使用者，但它們所使用的每個界面都是為其他人設計的。頁面是為人眼而建，能夠快速瀏覽和忽略；工具架構則是為程式設計的，這些程式不會為它們從未調用的定義付出任何代價。代理沒有這種奢侈：它在每次轉換時都會重新閱讀並再次付費，對於它所顯示的每一項內容。我們提出了 String，一個開源的運行時，為這個使用者提供了自己的界面，並將這項工作視為一個操作系統問題。工具知識從代理的上下文中移出，進入一個共同層，將其一次性以 Markdown 的形式呈現回來。一個單一的 SFMD（String-Flavored Markdown）文檔聲明了一個應用程序的視圖、類型化操作、導航和憑證，而運行時則處理發現、驗證、執行、狀態和秘密，這些都基於兩個核心動詞：/open 以查看和 /act 以執行。網頁和應用程序實際上是同一架構的兩種呈現：一個 SFMD 網站向瀏覽器提供樣式化的 HTML，並向代理提供原始文檔，因此一個語法可以覆蓋應用程序、文件、外殼和網絡，甚至是舊版 HTML，而無需針對每個網站進行集成。視圖設計上保持部分性，並且分階段是因果的：過早披露一層細節可能會損失多達 23 個準確度點，而適當的分階段則將錯誤操作選擇從 28% 降至 2%。特權隨來源而變：遠程頁面可以調用 HTTP，但永遠無法調用外殼，並且調用者提供的文本永遠不會擴展存儲的秘密。在一個 87 任務的基準測試中，將每個任務與策劃的技能配對，將這些程序作為按需的 String 應用進行操作，產生了六個模型從邊界到小型的可比較總體成功率（+1.3pp），同時在完成的集數中使用了 33.5% 更少的標記，而常駐界面在任何目錄大小下保持恆定的 53 個標記。我們報告了設計、評估以及三個月的生產使用教會了我們什麼。

##### **Twin Worlds: Equivariance-Based Abstention for Evidence-Grounded Reasoning**
2608.28018v1 by Vy Nguyen, Ziqi Xu, Jeffrey Chan, Estrid He, Feng Xia, Renqiang Luo, Erik Cambria, Xiuzhen Zhang

Knowledge-intensive reasoning requires Large Language Models (LLMs) to ground answers in provided evidence. When evidence is insufficient, it is desirable that models abstain rather than confidently generating unsupported answers. Existing abstention methods rely on uncertainty estimation or evidence sufficiency checks, but neither tests whether the reasoning process for generation, driven by the interaction of provided evidence and the model's internal memory parameters, is actually grounded in the evidence. A key contributing factor is that entity mentions in context activate memorised associations, causing models to generate plausible responses ungrounded in evidence. We propose Twin Worlds (TW), a framework for improving reliability in knowledge-intensive reasoning through equivariance-based abstention: unlike invariance, which requires outputs to remain unchanged, equivariance requires outputs to transform correspondingly under entity substitutions. A model grounded in the evidence should produce answers that shift consistently when entities are substituted while their relations are preserved. TW constructs multiple worlds via typed substitutions of the original input that preserve relational structure while reducing parametric priors, and uses equivariance violations as an abstention signal. Across four benchmarks and three model backbones, TW identifies when answers are not reliably grounded in the provided evidence and outperforms uncertainty- and sufficiency-based baselines.

摘要：知識密集型推理要求大型語言模型（LLMs）將答案基於提供的證據。當證據不足時，模型應該選擇不作答，而不是自信地生成不支持的答案。現有的放棄方法依賴於不確定性估計或證據充分性檢查，但這兩者都無法測試生成過程的推理是否實際上是基於證據，這一過程是由提供的證據和模型的內部記憶參數之間的互動驅動的。一個關鍵的促成因素是，語境中的實體提及會激活記憶中的聯想，導致模型生成不基於證據的合理響應。我們提出了雙重世界（Twin Worlds, TW），這是一個通過等變性基礎的放棄來提高知識密集型推理可靠性的框架：與不變性不同，不變性要求輸出保持不變，等變性則要求輸出在實體替換下相應轉換。基於證據的模型應該在實體被替換時生成一致變化的答案，同時保持其關係。TW通過類型替換原始輸入來構建多個世界，這些替換保持關係結構，同時減少參數先驗，並使用等變性違反作為放棄信號。在四個基準測試和三個模型骨幹中，TW能夠識別答案何時未能可靠地基於提供的證據，並且在性能上超越了基於不確定性和充分性的基準。

##### **When Evidence Shapes Collaboration: Knowledge-Conditioned Topology Generation for Multi-Agent Systems**
2608.27984v1 by Yangxiao Jiang, Jiarun Fan, Mingcong Xu, Yanxi Guo, Jiwen Feng, Shanqing Xu, Mengchen Qian, Wei Chen, Xiaojin Zhang

Multi-Agent Systems (MAS) have recently moved from static workflows toward dynamically generated collaboration topologies. However, existing topology generation methods rely primarily on the parametric knowledge of large language models, with external search or retrieval used only as a reactive tool rather than an explicit determinant of collaboration structure. This leads to structure-knowledge misalignment, where systems exhibit redundant interactions or insufficient verification in knowledge-intensive tasks. We propose K-GAT (Knowledge-Guided Agent Topology Generator), a neuro-symbolic framework that formulates collaboration topology design as a knowledge-conditioned structure learning problem, integrating external evidence directly into autoregressive graph generation. Extensive experiments on knowledge-intensive benchmarks demonstrate K-GAT's efficiency and effectiveness: notably on the expert-level GPQA dataset, K-GAT outperforms the LLM-Debate baseline by a substantial margin of +15.7% in accuracy, while consuming less than half the computational tokens.

摘要：多代理系統（MAS）最近從靜態工作流程轉向動態生成的協作拓撲。<lb/>然而，現有的拓撲生成方法主要依賴於大型語言模型的參數知識，外部搜索或檢索僅用作反應工具，而不是協作結構的明確決定因素。<lb/>這導致結構知識不一致，系統在知識密集型任務中表現出冗餘的互動或不足的驗證。<lb/>我們提出了 K-GAT（知識引導代理拓撲生成器），這是一個神經符號框架，將協作拓撲設計公式化為一個知識條件的結構學習問題，將外部證據直接整合到自回歸圖生成中。<lb/>在知識密集型基準上進行的廣泛實驗展示了 K-GAT 的效率和有效性：特別是在專家級 GPQA 數據集上，K-GAT 在準確性上超越了 LLM-Debate 基準，達到 +15.7% 的顯著增幅，同時消耗的計算令牌不到一半。

##### **DisCTI: Who Needs to Know Timely? Automated Sector-Aware Cyber Threat Intelligence Dissemination**
2608.27967v1 by Fajar Wijitrisnanto, Alsharif Abuadbba, Yansong Gao, Nan Wu

The timely dissemination of cyber threat intelligence (CTI) is critical for organizations to mount swift and effective incident response. When valid CTI is delivered to the right sector at the right time, identical attacks can often be contained or mitigated. However, today's rapidly expanding CTI landscape overwhelms analysts, who must sift through massive and heterogeneous feeds. Existing platforms such as the Malware Information Sharing Platform (MISP) provide sector tagging features (e.g., energy, finance, government), but in practice, these remain largely unmapped (98% of events are left uncategorized). This lack of automated and timely sector mapping severely limits the operational value of shared intelligence, leaving organizations that belong especially to the critical information infrastructure sector exposed.
  To address this gap, we formulate sector-targeted CTI dissemination as a multilabel classification problem. Leveraging deep field knowledge of CTI structures and sector-specific threat patterns, we construct a novel data set of 872 sector-labelled CTI events from a threat intelligence platform (TIP). We then apply BERT, a transformer-based model, to automate the mapping of CTI events to sectors. Using the structured threat information expression (STIX) format for cross-platform interoperability, our approach achieves a macro-averaged F1-score of 0.89 at a Hamming loss of 0.055 on the custom dataset, i.e. 94.5% of individual sector-label assignments are correct. These results not only demonstrate the feasibility of sector-aware, automated CTI dissemination but also highlight how embedding expert field knowledge into machine learning design fills a crucial gap in the threat intelligence pipeline, enabling faster and context-relevant defensive action.

摘要：及時傳播網絡威脅情報（CTI）對於組織迅速且有效地應對事件至關重要。當有效的CTI在適當的時間交付給正確的部門時，往往可以控制或減輕相同的攻擊。然而，當前快速擴展的CTI環境使分析師不堪重負，他們必須從大量異質的數據源中篩選信息。現有的平台，如惡意軟件信息共享平台（MISP），提供部門標記功能（例如，能源、金融、政府），但實際上，這些標記仍然在很大程度上未被映射（98%的事件未被分類）。這種缺乏自動化和及時的部門映射嚴重限制了共享情報的操作價值，特別是使屬於關鍵信息基礎設施部門的組織暴露於風險之中。<lb/>為了解決這一空白，我們將部門目標的CTI傳播表述為多標籤分類問題。利用對CTI結構和部門特定威脅模式的深入知識，我們從威脅情報平台（TIP）構建了一個包含872個部門標記的CTI事件的新數據集。然後，我們應用BERT，一種基於Transformer的模型，來自動映射CTI事件到部門。使用結構化威脅信息表達（STIX）格式以實現跨平台互操作性，我們的方法在自定義數據集上達到了0.89的宏平均F1分數，Hamming損失為0.055，即94.5%的個別部門標籤分配是正確的。這些結果不僅展示了部門感知、自動化CTI傳播的可行性，還突顯了如何將專家領域知識嵌入機器學習設計中，填補威脅情報管道中的關鍵空白，使防禦行動更快且更具上下文相關性。

##### **When Teacher Guidance Misleads: Reward-Aligned On-Policy Distillation**
2608.27960v1 by Siyuan Gan, Yuhan Li, Xiran Wang, Linjian Meng, Boyan Wang, Zhen Zhao, Jing Huo, Yang Gao

On-policy distillation (OPD) has recently emerged as a popular post-training paradigm for large language models (LLMs), providing an efficient way to transfer the knowledge and capabilities of teacher models into student models. However, teacher guidance on student-generated prefixes is not always reliable. Training should optimize the model to generate responses that are more likely to be correct, or equivalently, to get higher outcome rewards. But during OPD, the teacher model may provide guidance that discourages the student from moving toward correct trajectories or moves the student toward incorrect ones, which is misaligned with outcome reward. Such misaligned guidance is unreliable, as it would mislead the optimization process and ultimately degrade model performance. To mitigate misaligned teacher guidance, we propose Reward-Aligned On-Policy Distillation (RA-OPD). The key insight is to keep only trajectories whose induced updates move the student toward correct trajectories or discourage the student from moving toward incorrect ones. Specifically, for each sampled trajectory, RA-OPD checks whether its trajectory-level distillation return is consistent with its outcome reward and then filters out the misaligned trajectories. RA-OPD selects more reliable trajectories to improve student model performance without requiring additional computational cost. We evaluate RA-OPD on math and code benchmarks using models from the Qwen3 family and the DeepSeek-R1 family. Across seven math benchmarks and three code benchmarks, RA-OPD significantly outperforms standard OPD and other tested OPD variants.

摘要：在政策蒸餾（OPD）最近成為大型語言模型（LLMs）的熱門後訓練範式，提供了一種有效的方式將教師模型的知識和能力轉移到學生模型中。<lb/>然而，教師對學生生成的前綴的指導並不總是可靠的。<lb/>訓練應該優化模型，以生成更可能正確的回應，或者等價地，獲得更高的結果獎勵。<lb/>但在 OPD 期間，教師模型可能提供的指導會使學生遠離正確的軌跡，或將學生引導至錯誤的軌跡，這與結果獎勵不一致。<lb/>這種不一致的指導是不可靠的，因為它會誤導優化過程，最終降低模型性能。<lb/>為了減輕不一致的教師指導，我們提出了獎勵對齊的政策蒸餾（RA-OPD）。<lb/>關鍵見解是僅保留那些其誘導更新使學生朝向正確軌跡移動或使學生遠離錯誤軌跡的軌跡。<lb/>具體而言，對於每個抽樣的軌跡，RA-OPD 檢查其軌跡級蒸餾回報是否與其結果獎勵一致，然後過濾掉不一致的軌跡。<lb/>RA-OPD 選擇更可靠的軌跡，以在不需要額外計算成本的情況下改善學生模型性能。<lb/>我們在數學和代碼基準上評估 RA-OPD，使用 Qwen3 家族和 DeepSeek-R1 家族的模型。<lb/>在七個數學基準和三個代碼基準上，RA-OPD 顯著超越了標準 OPD 和其他測試的 OPD 變體。

##### **The Illusion of $\textit{What If}$: Evaluating the Breakdown of Counterfactual Reasoning in LLMs**
2608.27953v1 by Yucheng Wang, Yuetian Du, Zhengyi Liu, Rongyu Zhang, Bing Zhao, Boyu Yang, Ming Kong, Lin Qu, Hu Wei, Jie Liu, Qiang Zhu

Counterfactual reasoning requires models to reason beyond the observed world and explain how altered conditions propagate through downstream consequences. Existing benchmarks largely target bounded settings with fixed variables or single gold outcomes, overlooking open-domain scenarios requiring causal-process evaluation. To this end, we present $\textbf{WhatIfBench}$, a diagnostic benchmark for open-domain, open-form, long-horizon counterfactual causal reasoning, containing 220 what-if questions across STEM, HSS, and Hybrid scenarios. To evaluate free-form responses, we further propose $\textbf{PRISM}$, which first converts each natural-language explanation into a Response-Derived Semantic Causal Graph of events, states, and mechanisms. On top of this graph, PRISM then jointly applies a Process Metric assessing graph-level causal validity and a Rubric Metric assessing answer-level explanatory adequacy. Evaluating six frontier LLMs with this framework, we find that WhatIfBench remains far from saturated: even the strongest model reaches only a 64.62% final score. Further analysis reveals persistent causal gaps, premise drift, and topology fragmentation, suggesting that fluent counterfactual narratives often mask fragile causal processes. The benchmark, code, and evaluation scripts are available at $\href{https://github.com/zju-gt/WhatIfBench}{WhatIfBench}$.

摘要：反事實推理需要模型超越觀察到的世界進行推理，並解釋如何改變條件在後續結果中傳播。現有的基準主要針對固定變量或單一金標結果的有限環境，忽略了需要因果過程評估的開放領域場景。為此，我們提出了 $\textbf{WhatIfBench}$，這是一個針對開放領域、開放形式、長期反事實因果推理的診斷基準，包含了220個涉及STEM、HSS和混合場景的假設性問題。為了評估自由形式的回應，我們進一步提出了 $\textbf{PRISM}$，它首先將每個自然語言解釋轉換為事件、狀態和機制的回應衍生語義因果圖。在這個圖的基礎上，PRISM然後共同應用一個過程度量來評估圖層級的因果有效性，以及一個評分度量來評估答案層級的解釋充分性。使用這個框架評估六個前沿的LLM，我們發現WhatIfBench仍然遠未飽和：即使是最強的模型最終得分也僅為64.62%。進一步分析顯示持續的因果差距、前提漂移和拓撲碎片化，這表明流利的反事實敘述往往掩蓋脆弱的因果過程。該基準、代碼和評估腳本可在 $\href{https://github.com/zju-gt/WhatIfBench}{WhatIfBench}$ 獲得。

##### **Entity-Memory Graph Retrieval Improves Evidence Coverage in Long-Conversation Question Answering**
2608.27925v1 by Shumao Sun

Entity-Memory graph retrieval keeps dialogue turns as verbatim Memory nodes, links repeated mentions through shared Entities, and connects adjacent Memories with directed chronological edges. At query time the retriever moves from Entity gating through semantic fusion and one-hop chronological recovery to dense backfill. The path can keep a neighboring Memory that dense cosine ranking would otherwise omit. A matched dense control shares the Memory and query vectors, context budget, requested answer protocol, and evaluator, isolating graph structure from changes to the reader.
  On 1,986 questions from ten LoCoMo conversations, graph retrieval raises official evidence recall at top-k 25 from 79.7468% to 84.4842%. The recall advantage is supported from top-k 5 to 50, while no matched cutoff supports an overall final-answer F1 difference. Four paper-eligible requested configurations support empirical robustness across the tested GPT-3.5 and DeepSeek extractors on both outcomes. Embedding robustness is mixed: F1 has no supported contrast, but recall is sensitive to the embedding artifact. The comparison isolates a retrieval-coverage gain from graph structure. It does not establish a final-answer F1 gain, model or embedding equivalence, or cross-dataset generalization.

摘要：實體-記憶圖檢索將對話回合作為逐字的記憶節點，通過共享實體連結重複提及，並用定向的時間邊連接相鄰的記憶。在查詢時，檢索器從實體閘門通過語義融合和單跳時間恢復移動到密集回填。這條路徑可以保留一個鄰近的記憶，而密集餘弦排名則會省略它。一個匹配的密集控制共享記憶和查詢向量、上下文預算、請求的答案協議和評估者，將圖結構與讀者的變化隔離開來。<lb/>在來自十個LoCoMo對話的1,986個問題中，圖檢索將官方證據召回率在前25名中從79.7468%提高到84.4842%。這一召回優勢在前5到50名中得到了支持，而沒有匹配的截止點支持整體最終答案F1差異。四個符合論文要求的請求配置在測試的GPT-3.5和DeepSeek提取器的兩個結果上支持實證穩健性。嵌入穩健性則參差不齊：F1沒有支持的對比，但召回對嵌入工件敏感。這一比較將檢索覆蓋增益與圖結構隔離開來。它並未建立最終答案F1增益、模型或嵌入等價性，或跨數據集的泛化。

##### **PCBnet: A Dataset and Automatic Construction of SPICE Netlists from Schematic Images**
2608.27923v1 by Zhen Huang, Yuhao Gao, Yuzhi Liu, Daian Cheng, Chengyuan Shao, Yucheng Chen, Yongjian Jia, Futing Zhang, Yichen Shi, Wenhao Wang, Zuyan He, Yangbo Wei, Zhanfei Chen, Jinlong Yan, Yu Zhang, Haoying Wu, Ting-Jung Lin, Lei He

Printed circuit boards (PCBs) are fundamental to modern electronic systems, yet AI-driven PCB design automation remains constrained by the lack of large-scale paired schematic-netlist datasets. PCB schematics are particularly challenging due to diverse component types, complex wiring topologies, and noisy textual annotations. To address this gap, we present PCBnet, a large-scale PCB schematic dataset comprising over 300 real-world designs with annotated pins and paired SPICE netlists. It contains more than 50,000 component instances, 150,000 wires, 100,000 text regions, and 400,000 characters. We further develop an automated schematic-to-netlist pipeline that combines visual recognition, topology construction, and domain-knowledge-guided multi-agent correction. The proposed method achieves 94.54% component detection mAP, 98.57% text recognition accuracy, and 84.47% end-to-end connectivity accuracy. PCBnet provides a benchmark and data foundation for future AI-driven PCB design automation.

摘要：印刷電路板（PCBs）對現代電子系統至關重要，但基於人工智慧的 PCB 設計自動化仍受到缺乏大規模配對原理圖-網路清單數據集的限制。由於元件類型多樣、接線拓撲複雜以及文本註解雜訊，PCB 原理圖特別具有挑戰性。為了解決這一問題，我們提出了 PCBnet，一個大規模的 PCB 原理圖數據集，包含超過 300 個帶有註解引腳和配對 SPICE 網路清單的真實設計。它包含超過 50,000 個元件實例、150,000 根導線、100,000 個文本區域和 400,000 個字符。我們進一步開發了一個自動化的原理圖到網路清單的管道，結合了視覺識別、拓撲構建和基於領域知識的多代理修正。所提出的方法實現了 94.54% 的元件檢測平均精度（mAP）、98.57% 的文本識別準確率和 84.47% 的端到端連通性準確率。PCBnet 為未來基於人工智慧的 PCB 設計自動化提供了基準和數據基礎。

##### **SOMTab: Set-Order Mamba for Efficient Tabular In-Context Learning**
2608.27882v1 by Hao Wang, Siyu Zhang, Wei Ma

Tabular foundation models based on in-context learning have recently emerged as strong alternatives to task-specific model fitting. However, the current performance frontier remains dominated by attention-heavy architectures, where attention is used throughout the modeling pipeline. This raises a natural question: is attention necessary at every stage of tabular in-context learning? We introduce SOMTab, a Set-Order Mamba architecture for efficient tabular in-context learning. SOMTab separates representation construction from query-conditioned retrieval. For row and column representations, it maps unordered table tokens into stable latent slots and applies Mamba-based state-space mixing to construct compact representations. For final prediction, it retains attention-based in-context learning to preserve query-conditioned retrieval from labeled context examples. We further introduce DCH-TailMix, a synthetic prior that combines degree-corrected graph heterogeneity with mixed heavy-tailed regimes to diversify synthetic dependency structures. Across tabular benchmarks, SOMTab approaches the performance of strong Transformer-based tabular foundation models while achieving faster inference and lower GPU memory usage, yielding a favorable efficiency--accuracy trade-off.

摘要：基於上下文學習的表格基礎模型最近已成為任務特定模型擬合的強大替代方案。<lb/>然而，目前的性能前沿仍然由重注意力架構主導，在這些架構中，注意力在整個建模流程中被使用。<lb/>這引發了一個自然的問題：在每個階段的表格上下文學習中，注意力是否是必要的？<lb/>我們介紹了SOMTab，一種高效的表格上下文學習的集合順序Mamba架構。<lb/>SOMTab將表示構建與查詢條件檢索分開。<lb/>對於行和列表示，它將無序的表格標記映射到穩定的潛在槽中，並應用基於Mamba的狀態空間混合來構建緊湊的表示。<lb/>對於最終預測，它保留基於注意力的上下文學習，以保留來自標記上下文範例的查詢條件檢索。<lb/>我們進一步介紹了DCH-TailMix，一種合成先驗，結合了度校正的圖異質性與混合重尾範疇，以多樣化合成依賴結構。<lb/>在表格基準測試中，SOMTab接近強大的基於Transformer的表格基礎模型的性能，同時實現了更快的推理和更低的GPU內存使用，從而產生了有利的效率-準確性權衡。

##### **CoRe-MoE: Compact Reusable MoE for Continual Multimodal Instruction Tuning**
2608.27867v1 by Runze Liu, Naibin Gu, Mingxu Ai, Yuqing Li, Peng Fu, Zheng Lin, Weiping Wang

Continual multimodal instruction tuning requires multimodal large language models to acquire new task abilities sequentially while preserving previously learned knowledge. LoRA-MoE provides a promising solution by introducing expert-based capacity, but repeatedly learning and maintaining full LoRA experts leads to substantial parameter overhead. This raises a natural question: is full expert expansion necessary for every new task? To answer it, we analyze the SVD of task-specific LoRA updates and observe substantial overlap in their input- and output-side LoRA direction subspaces, with task-specific adaptation largely captured by lightweight coordinates over these subspaces. Motivated by this observation, we propose CoRe-MoE, a Compact Reusable MoE framework for parameter-efficient continual multimodal instruction tuning. CoRe-MoE extracts reusable input- and output-side direction bases from an initial expert bank, and for subsequent tasks trains only compact coordinate experts together with task-specific low-rank routers. Experiments on two representative MLLMs show that CoRe-MoE improves final average performance over the strongest competing baseline by up to 5.90 points, while using less than 1% of the trainable parameters required by sequential LoRA for later tasks. The code is publicly available at https://github.com/runzezz/CoRe-MoE.

摘要：持續的多模態指令調整需要多模態大型語言模型在保留先前學習知識的同時，依序獲得新的任務能力。LoRA-MoE通過引入基於專家的容量提供了一個有前景的解決方案，但反覆學習並維持完整的LoRA專家會導致相當大的參數開銷。這引發了一個自然的問題：對於每個新任務，完全擴展專家是否必要？為了回答這個問題，我們分析了任務特定的LoRA更新的SVD，並觀察到它們的輸入和輸出側LoRA方向子空間之間存在相當大的重疊，任務特定的適應主要是通過這些子空間上的輕量級坐標來捕捉的。受到這一觀察的啟發，我們提出了CoRe-MoE，一種用於參數高效持續多模態指令調整的緊湊可重用MoE框架。CoRe-MoE從初始專家庫中提取可重用的輸入和輸出側方向基礎，並且對於後續任務僅訓練緊湊的坐標專家以及任務特定的低秩路由器。在兩個代表性的MLLM上進行的實驗表明，CoRe-MoE在最強競爭基準上提高了最終平均性能，最多可達5.90分，同時使用的可訓練參數少於順序LoRA對後續任務所需的1%。代碼可在 https://github.com/runzezz/CoRe-MoE 上公開獲得。

##### **SpikeOPD: Stable On-Policy Distillation for Autoregressive Spiking Language Models**
2608.27857v1 by Enqiao Lu, Xingrui Yu, Yiwei Fu, Zhenglin Wan, Pengfei Zhou, Wangbo Zhao, Muqing Jian, Xueyi Zhang, Yang You, Ivor Tsang

Spiking neural networks (SNNs) offer a path to energy-efficient language modeling through sparse encoding and event-driven computation, but training capable spiking language models from scratch remains difficult. A practical alternative is ANN-to-SNN migration through knowledge distillation (KD), where a pretrained artificial neural network (ANN) teacher supervises an SNN student. Existing migration approaches distill on fixed corpus prefixes, whereas autoregressive inference conditions on self-generated prefixes, creating prefix-source mismatch. It manifests as output-policy mismatch with the ANN teacher and internal spiking-dynamics drift between self-generated and matched corpus prefixes. On-policy distillation (OPD) offers a natural way to mitigate both manifestations by continuing teacher supervision on self-generated prefixes. We evaluate a teacher-only full-KL variant, Vanilla OPD, via a controlled stress test and observe it may suffer from delayed rollout-feedback collapse. This result shows that on-policy coverage alone does not ensure stable adaptation. Motivated by these findings, we propose SpikeOPD, a stable on-policy distillation framework for autoregressive SNNs that learns from self-generated prefixes while maintaining rollout stability. It applies full-KL teacher correction to reduce output-policy mismatch, while matched-prefix policy anchoring constrains policy departure from the frozen reference SNN on the same prefixes. Layerwise spike regularization further limits firing-rate deviations during on-policy adaptation. Across three model scales, SpikeOPD improves average accuracy over the corresponding KD SNNs by 0.8, 1.7, and 2.9 points at 0.125B, 0.35B, and 1.3B, respectively, while preserving their sparse-compute profiles.

摘要：尖峰神經網絡（SNNs）透過稀疏編碼和事件驅動計算提供了一條能量高效的語言建模之路，但從零開始訓練具有能力的尖峰語言模型仍然困難。 一個實用的替代方案是通過知識蒸餾（KD）進行ANN到SNN的遷移，其中一個預訓練的人工神經網絡（ANN）教師監督一個SNN學生。 現有的遷移方法在固定的語料庫前綴上進行蒸餾，而自回歸推斷則依賴於自生成的前綴，造成前綴來源不匹配。 這表現為與ANN教師的輸出策略不匹配，以及自生成和匹配語料庫前綴之間的內部尖峰動態漂移。 策略內蒸餾（OPD）提供了一種自然的方式來減輕這兩種表現，通過在自生成的前綴上持續教師監督。我們通過控制壓力測試評估了一個僅教師的全KL變體，即香草OPD，並觀察到它可能會遭受延遲的回饋崩潰。 這一結果表明，僅依賴策略內覆蓋並不能確保穩定的適應。 受到這些發現的啟發，我們提出了SpikeOPD，一個穩定的策略內蒸餾框架，用於自回歸SNNs，該框架從自生成的前綴中學習，同時保持回饋穩定性。 它應用全KL教師修正以減少輸出策略不匹配，而匹配前綴策略錨定則限制策略偏離同一前綴上的凍結參考SNN。 層級尖峰正則化進一步限制了在策略內適應期間的發火率偏差。 在三種模型規模上，SpikeOPD在0.125B、0.35B和1.3B下，分別提高了對應KD SNN的平均準確率0.8、1.7和2.9個點，同時保持其稀疏計算特徵。

##### **EvoHarmBench: Breaking Content Moderation with Iterative Human-Like Evasion**
2608.27844v1 by Ruijie Jian, Benlei Cui, Ting Ma, Haidong Ding, Kangwei Liu, Ziwen Xu, Longtao Huang, Hui Xue, Ziqiang Zhu, Junjie Li, Haiwen Hong

Existing evaluations of harmful content detection rely predominantly on static benchmarks, which struggle to reflect the interactive adversarial ecosystem of real-world content platforms where users continuously revise their expressions in response to moderation feedback. This mismatch creates a significant performance gap between offline benchmark scores and online deployment effectiveness. To the best of our knowledge, we present EvoHarmBench, the first dynamic adversarial evaluation framework for content moderation systems. The framework employs an iterative optimization loop that evolves evasion strategies at the semantic-cluster level, while simultaneously optimizing for evasion success and human readability. We systematically evaluate LLM-based defense models which are widely used in real world moderation systems. The evaluation covers 229 semantic sub-clusters across five violation categories, derived from 5,002 real-world adversarial samples collected from content platforms. Our experiments reveal substantial vulnerabilities even in leading commercial systems: after twelve optimization iterations, the attack success rate under readability constraints reaches 80.3% within SOTA LLM moderators. We will release the full benchmark data, evaluation framework, and code to encourage a shift from static benchmarking toward dynamic adversarial evaluation in content safety research.

摘要：現有的有害內容檢測評估主要依賴靜態基準，這無法反映現實世界內容平台中用戶在回應審核反饋時不斷修訂其表達的互動對抗生態系統。這種不匹配造成了離線基準分數與在線部署效果之間的顯著性能差距。據我們所知，我們提出了EvoHarmBench，這是針對內容審核系統的首個動態對抗評估框架。該框架採用了迭代優化循環，能夠在語義集群層面上演變逃避策略，同時優化逃避成功率和人類可讀性。我們系統性地評估了在現實世界審核系統中廣泛使用的基於LLM的防禦模型。評估涵蓋了來自內容平台的5,002個真實對抗樣本中衍生出的五個違規類別的229個語義子集群。我們的實驗揭示了即使在領先的商業系統中也存在重大脆弱性：在可讀性限制下，經過十二次優化迭代後，攻擊成功率在SOTA LLM審核者中達到了80.3%。我們將發布完整的基準數據、評估框架和代碼，以促進從靜態基準測試向內容安全研究中的動態對抗評估的轉變。

##### **KLOD: Locality-Preserving Knowledge Editing via Non-Target Distribution Preservation**
2608.27839v1 by Hojun Jeong, Gyunyeop Kim, Sangwoo Kang

Fine-tuning-based knowledge editing is simple and architecture-agnostic, but standard cross-entropy increases the edited target probability without explicitly constraining changes in the non-target output distribution. In sequential editing, such unconstrained redistribution can accumulate as distributional drift and contribute to locality degradation. We propose KLOD, a bounded and distribution-preserving objective for fine-tuning-based knowledge editing that separates the intended target update from distributions that should remain stable. KLOD stops target amplification once a probability threshold is reached, while preserving the target-excluded non-target distribution at target positions and the full next-token distribution at prefix positions. Experiments on CounterFact and ZsRE with Llama3-8B-Instruct and Qwen2.5-7B-Instruct show that KLOD substantially mitigates locality degradation while maintaining high edit reliability. The target probability threshold further provides a controllable Generalization--Locality trade-off. Ablation, multi-seed, and distributional KL analyses support the interpretation that KLOD's locality gains are associated with preserving output distributions rather than simply weakening the edit. Code is available on GitHub https://github.com/Hostoday/KLOD .

摘要：微調基礎的知識編輯簡單且不依賴於特定架構，但標準的交叉熵會增加編輯後的目標概率，而不明確約束非目標輸出分佈的變化。在序列編輯中，這種不受約束的重新分配可能會累積成分佈漂移，並導致局部性退化。我們提出了 KLOD，一種有界且保持分佈的微調基礎知識編輯目標，將預期的目標更新與應保持穩定的分佈分開。KLOD 在達到概率閾值後停止目標增強，同時在目標位置保持排除目標的非目標分佈，以及在前綴位置保持完整的下一個標記分佈。對 CounterFact 和 ZsRE 的實驗，使用 Llama3-8B-Instruct 和 Qwen2.5-7B-Instruct 顯示 KLOD 顯著減輕了局部性退化，同時保持高編輯可靠性。目標概率閾值進一步提供了一個可控的泛化-局部性權衡。消融、多種種子和分佈 KL 分析支持這樣的解釋：KLOD 的局部性增益與保持輸出分佈有關，而不僅僅是削弱編輯。代碼可在 GitHub 上獲得 https://github.com/Hostoday/KLOD 。

##### **PersonaEdit: Representative Sample Selection for Personalized Model Editing**
2608.27816v1 by You-Mei Huang, Chung-Chi Chen, An-Zi Yen

Personalization has attracted growing interest in LLM applications, yet existing retrieval-based approaches depend heavily on retrieval quality and degrade in long-term interactions. Model editing, which directly modifies internal model parameters to incorporate new knowledge, has demonstrated effective knowledge modification capabilities in factual knowledge editing tasks and may provide a potential solution for personalization. However, scaling model editing to personalization is non-trivial. Editing large amounts of user data increases computational cost and causes interference among edits, motivating the need for effective sample selection. To address this issue, we propose, PersonaEdit, a hidden representation clustering strategy that selects representative editing samples through proportional stratified sampling. Experiments show that model editing is effective for personalization, and that our selection strategy preserves most of the performance while substantially reducing the number of required editing samples. Beyond standalone editing, we find that combining model editing with retrieval-based prompt augmentation further improves personalization, as edited knowledge and retrieved context provide complementary information. These results demonstrate the potential of model editing as an efficient and scalable approach for LLM personalization.

摘要：個性化在大型語言模型應用中引起了越來越多的關注，然而現有的基於檢索的方法在很大程度上依賴於檢索質量，並在長期互動中表現不佳。模型編輯直接修改內部模型參數以納入新知識，已在事實知識編輯任務中顯示出有效的知識修改能力，並可能為個性化提供潛在解決方案。然而，將模型編輯擴展到個性化並非易事。編輯大量用戶數據會增加計算成本並導致編輯之間的干擾，這促使了有效樣本選擇的需求。為了解決這個問題，我們提出了PersonaEdit，一種隱藏表示聚類策略，通過比例分層抽樣選擇代表性的編輯樣本。實驗表明，模型編輯對個性化是有效的，並且我們的選擇策略在顯著減少所需編輯樣本數量的同時保留了大部分性能。除了獨立編輯外，我們發現將模型編輯與基於檢索的提示增強相結合進一步改善了個性化，因為編輯的知識和檢索的上下文提供了互補的信息。這些結果展示了模型編輯作為一種高效且可擴展的LLM個性化方法的潛力。

##### **Credo: Reusable Declarative Primitives for Agentic Workflows**
2608.27790v1 by Duo Lu, Andrew Crotty, Uğur Çetintemel

An LLM application depends on both a model and a harness: the program that determines what each call sees, how many calls to make, and which answers to trust. Coding agents can now discover strong harnesses by searching over candidate programs, but the resulting artifact is an opaque block of imperative code whose logical steps, runtime signals, physical execution decisions, and prompt strategies remain implicit and task-specific, forcing subsequent tasks to start the harness search process from scratch. The potential for reuse, however, is substantial. A searched harness encodes significant knowledge, such as the logical steps that work, the signals that matter, the physical operator decisions that adapt execution, and the prompt strategies that are effective, yet this knowledge is buried in imperative code with no inspectable or reusable structure, nor does it carry any provenance or metadata. Credo addresses this problem by recovering a structured declarative description of a searched harness, tagging each extracted primitive with relevant metadata, and cataloguing all of it with provenance. A compiler can then bind stored primitives to generate harnesses for new tasks without having to start the search over from scratch. This paper provides preliminary results demonstrating the potential of our approach and lays out a related research agenda that the database community is well-positioned to tackle, including cost-based compilation over declarative catalogs and catalog maintenance under model and workload drift.

摘要：一個 LLM 應用依賴於模型和工具：該程序決定每個調用所看到的內容、要進行多少次調用以及信任哪些答案。編碼代理現在可以通過搜索候選程序來發現強大的工具，但產生的工件是一個不透明的命令式代碼塊，其邏輯步驟、運行時信號、物理執行決策和提示策略仍然隱含且特定於任務，迫使後續任務從頭開始進行工具搜索過程。然而，重用的潛力是巨大的。搜索過的工具編碼了重要的知識，例如有效的邏輯步驟、重要的信號、調整執行的物理操作決策以及有效的提示策略，但這些知識埋藏在沒有可檢查或可重用結構的命令式代碼中，也不攜帶任何來源或元數據。Credo 通過恢復搜索工具的結構化聲明描述來解決這個問題，為每個提取的原語標記相關的元數據，並將所有這些內容編目並附上來源。然後，編譯器可以將存儲的原語綁定以生成新任務的工具，而無需從頭開始進行搜索。本文提供了初步結果，展示了我們方法的潛力，並提出了一個相關的研究議程，數據庫社區在這方面具有良好的解決能力，包括基於成本的編譯、聲明型目錄的維護以及模型和工作負載漂移下的目錄維護。

##### **Load-Bearing Context: The Question Damage Score for Evaluating Context Reliance in Linguistic Reasoning**
2608.27756v1 by Neh Majmudar, Elena Filatova

Determining whether large language models derive answers from context or prior knowledge remains a fundamental challenge. Self-contained linguistic olympiad puzzles provide a controlled setting where all answers derive solely from expert-designed context examples without external knowledge. Removing individual context examples can eliminate information needed for specific questions while leaving the rest of the puzzle unchanged. We leverage this to introduce a diagnostic framework for analyzing individual context examples. Using 53 UK Linguistics Olympiad puzzles, we generate two modified variants by deleting a single context example: (1) uniform random deletion, and (2) targeted deletion (inspired by error-correcting codes) to remove a structurally load-bearing example uniquely carrying necessary information. We formalize this impact using a Question Damage Score to classify puzzles as fragile or robust. Evaluating three frontier LLMs under instructions to abstain when information is insufficient, we find they rarely abstain, often continuing to produce correct answers after load-bearing context is removed. These findings motivate further investigation into context-based reasoning, prior knowledge, memorization, and linguistic inference. Beyond abstention, the framework enables fine-grained analyses of context reliance, including causal interventions, stopping-set analysis, targeted contamination studies, and mechanistic interpretability.

摘要：確定大型語言模型是否從上下文或先前知識中推導答案仍然是一個基本挑戰。自給自足的語言奧林匹克謎題提供了一個受控的環境，其中所有答案僅源自專家設計的上下文範例，而不涉及外部知識。刪除單個上下文範例可以消除特定問題所需的信息，同時保持謎題的其餘部分不變。我們利用這一點引入了一個診斷框架來分析個別上下文範例。使用53個英國語言學奧林匹克謎題，我們通過刪除單個上下文範例生成了兩個修改變體：（1）均勻隨機刪除，以及（2）目標刪除（受錯誤更正碼啟發）以移除一個結構上承重的範例，該範例唯一承載必要的信息。我們使用問題損害分數來正式化這一影響，以將謎題分類為脆弱或穩健。在對三個前沿的LLM進行評估時，指示它們在信息不足時避免回答，我們發現它們很少避免，經常在承重上下文被移除後繼續產生正確的答案。這些發現激勵進一步研究基於上下文的推理、先前知識、記憶和語言推斷。除了避免回答外，該框架還能夠進行對上下文依賴的細緻分析，包括因果干預、停止集分析、目標污染研究和機制可解釋性。

##### **The Calls are Coming from Inside the Model: Investigating Probe-based Detection of Tool-Calling Errors in LLMs**
2608.27750v1 by Eric Yeats, Brendan Kennedy, Loc Truong, John Buckheit, Jung Lee, Jesse Friedbaum, John Emanuello, Henry Kvinge

The hidden states of large language models (LLMs) are known to capture rich information relating to model knowledge and behavior that can be hard to extract from examination of input and output alone. As LLM-based systems increasingly interface with the external world, one area of concern is detecting incorrect or improper use of tools. Motivated by this, we study the effectiveness of using linear probes to detect incorrect tool-calls, measuring probe efficacy across 18 tool-calling LLMs evaluated on the Berkeley Function Calling Leaderboard. Overall, we find that probing is an effective means to catch a range of different tool-calling errors, including errors arising from using an argument that has the wrong value but the correct type, which might not be recorded by standard logging frameworks. Important factors in success include model size, probing layer, and model post-training type. We also show that probes are capable of generalizing to novel types of errors, which is critical in real world deployments.

摘要：大型語言模型（LLMs）的隱藏狀態被認為能夠捕捉與模型知識和行為相關的豐富信息，而這些信息僅通過檢查輸入和輸出可能難以提取。<lb/>隨著基於LLM的系統越來越多地與外部世界互動，一個關注的領域是檢測工具的不當或不正確使用。<lb/>基於此，我們研究了使用線性探針檢測不正確工具調用的有效性，並在伯克利函數調用排行榜上評估了18個工具調用LLM的探針效能。<lb/>總體而言，我們發現探針是一種有效的手段，可以捕捉各種不同的工具調用錯誤，包括因使用了具有錯誤值但正確類型的參數而產生的錯誤，這些錯誤可能不會被標準日誌框架記錄。<lb/>成功的重要因素包括模型大小、探針層和模型後訓練類型。<lb/>我們還展示了探針能夠對新類型的錯誤進行泛化，這在現實世界的部署中至關重要。

##### **Below the Noise Floor: Bimodal Seed Collapse and Distinct Failure Modes in Small-Model Knowledge Distillation**
2608.27729v1 by Dipto Sumit, Sakib Ul Haque, Farig Sadeque

Function routing -- selecting the correct API call from a fixed catalog given a natural-language request -- is a deployment problem where small students are attractive but knowledge distillation gains are typically reported single-seed, at scales where seed variance is unknown. On a 740-instance healthcare API routing task with a 1.5B Qwen student and a 20B teacher, we compare eight KD variants against supervised cross-entropy, using three to six seeds for key configurations. We find: (i) per-seed standard deviation ranges from 2.8 to 48.7 percentage points, swallowing every claimed KD gain below five points; (ii) three of seven KD variants exhibit bimodal collapse, with at least one in three to five seeds falling below 55% accuracy while the others train normally, and a fourth showing elevated variance; (iii) collapse has distinct modes -- wrong-function selection for ce_kd and ce_paraphrase, and a previously undocumented output-truncation mode for reasoning_kd, where the model emits reasoning but terminates before producing a function name (0.9% accuracy); (iv) only progressive_kd and rank_kd avoid collapse across observed seeds, with sigma <= 3.9 pp; (v) a naive cross-split +3.78 pp gain from input enrichment reverses to -2.70 pp under controlled within-split multi-seed re-testing. Single-seed evaluation is therefore unable to detect central failure modes in small-model KD.

摘要：功能路由——根據自然語言請求從固定目錄中選擇正確的API調用——是一個部署問題，其中小型學生具有吸引力，但知識蒸餾的增益通常報告為單種子，在種子變異性未知的規模下。在一個740實例的醫療API路由任務中，使用1.5B的Qwen學生和20B的教師，我們比較了八種KD變體與監督交叉熵，對於關鍵配置使用三到六個種子。我們發現：(i) 每個種子的標準差範圍從2.8到48.7個百分點，吞噬了每個聲稱的KD增益低於五個百分點；(ii) 七種KD變體中有三種表現出雙峰崩潰，至少有三到五個種子中的一個低於55%的準確率，而其他的正常訓練，第四種顯示出較高的變異性；(iii) 崩潰有明顯的模式——ce_kd和ce_paraphrase的錯誤功能選擇，以及一種先前未記錄的推理_kd的輸出截斷模式，其中模型發出推理但在產生函數名稱之前終止（0.9%準確率）；(iv) 只有progressive_kd和rank_kd在觀察到的種子中避免崩潰，sigma <= 3.9 pp；(v) 一個天真的交叉拆分+3.78 pp的增益來自輸入豐富化，在受控的內部分裂多種子重新測試下反轉為-2.70 pp。因此，單種子評估無法檢測小型模型KD中的中心失效模式。

##### **Agents for Everyone: A Workshop Framework for Building Agentic AI Capabilities in a Distributed Curation Community**
2608.27675v1 by Seth Carbon, Sierra Moxon, Kimberly Van Auken, Pascale Gaudet, Christopher J. Mungall

Agentic AI has the potential to accelerate curation of biological databases and knowledge bases. However, uptake has been hindered by a number of challenges and obstacles, including access to agents and appropriate training. Here we describe how we have attempted to address and mitigate these challenges and obstacles through the deployment of a cloud-based agentic environment, and the development of an interactive training workshop for the Gene Ontology Consortium. Our cloud environment for agentic-assisted curation was based on the JupyterHub platform, and utilized Claude Code as a universal harness. This allows curators to interact with an agent session through a terminal running in the browser, and has additional benefits such as centralization of access through a single API gateway, removing the need for participants to manage subscriptions or install software locally. We created four training modules, walking participants through basic agentic tool use first and then working up to agentic biological pathway curation using the existing GO-CAM (GO Causal Activity Model) curation tool. Thirty-seven participants took part in the four-hour workshop. Our key takeaway from this workshop is that building community capability with agentic AI is primarily a problem of access, workflow design, and training. Removing technical barriers, introducing capabilities gradually, grounding exercises in familiar curation tasks, and giving curators direct experience evaluating agent output can provide a practical route toward building shared agentic AI capability in distributed scientific communities.

摘要：代理 AI 有潛力加速生物數據庫和知識庫的策展。然而，採用的過程受到多種挑戰和障礙的阻礙，包括對代理的訪問和適當的培訓。在這裡，我們描述了如何通過部署基於雲的代理環境和為基因本體聯盟開發互動培訓工作坊來解決和減輕這些挑戰和障礙。我們的代理輔助策展雲環境基於 JupyterHub 平台，並利用 Claude Code 作為通用工具。這使策展人能夠通過在瀏覽器中運行的終端與代理會話互動，並具有其他好處，例如通過單一 API 閘道集中訪問，消除了參與者管理訂閱或在本地安裝軟件的需求。我們創建了四個培訓模塊，首先引導參與者使用基本的代理工具，然後逐步進入使用現有的 GO-CAM（GO 因果活動模型）策展工具進行代理生物途徑策展。三十七名參與者參加了這個四小時的工作坊。我們從這個工作坊中得到的主要啟示是，利用代理 AI 建立社區能力主要是一個訪問、工作流程設計和培訓的問題。消除技術障礙、逐步引入功能、將練習基於熟悉的策展任務，以及讓策展人直接體驗評估代理輸出，可以為在分散的科學社區中建立共享的代理 AI 能力提供實際途徑。

##### **Self-Explainable Multi-Label Graph Neural Network for Correlated Evidence Attribution**
2608.27574v1 by Yingqi Feng, Yufei Tang, Min Shi, Xingquan Zhu

Multi-label graph learning intends to capture the intrinsic complexity of real-world applications, where one sample is often related to multiple groups or consists of multiple objects. To date, a handful of multi-label graph learning methods exist, but none of them integrate training-time interpretation capability. While post-hoc graph explainers have been developed, they do not explicitly model label-dependent evidence sharing in multi-label graph learners, especially when label pairs are weakly or negatively associated. As a result, post-hoc approaches may miss how evidence should be shared or separated across different labels. This paper advances a new end-to-end self-explainable multi-label graph neural network (SEMGNN), which aims to simultaneously classify multi-labeled nodes and identify edges significantly contributing to each target node w.r.t. predicted labels. Different from post-hoc methods, SEMGNN jointly learns a predictor and a sparse edge-mask explainer within a unified framework and training objective. Label-label correlations are used to improve multi-label node classification and enhance individual label explanations, so that different labels of a node can be supported by distinct yet coherent structural and/or correlated evidence. Experiments and comparisons on synthetic and real-world multi-label networks, in social networking, entertainment, and life sciences, show that SEMGNN achieves competitive or improved predictive performance while providing more faithful and compact label-conditioned explanations.

摘要：多標籤圖學習旨在捕捉現實世界應用的內在複雜性，其中一個樣本通常與多個群體相關或由多個物體組成。到目前為止，只有少數幾種多標籤圖學習方法存在，但它們都沒有整合訓練時的解釋能力。雖然已經開發了事後圖解釋器，但它們並未明確建模多標籤圖學習者中的標籤依賴證據共享，特別是在標籤對之間弱或負相關的情況下。因此，事後方法可能會錯過如何在不同標籤之間共享或分離證據。本文推進了一種新的端到端自我解釋的多標籤圖神經網絡（SEMGNN），旨在同時對多標籤節點進行分類並識別對每個目標節點的預測標籤有顯著貢獻的邊。與事後方法不同，SEMGNN在統一的框架和訓練目標內共同學習預測器和稀疏邊掩碼解釋器。標籤之間的相關性被用來改善多標籤節點分類並增強個別標籤解釋，使得一個節點的不同標籤可以由不同但一致的結構和/或相關證據支持。在社交網絡、娛樂和生命科學等合成和現實世界多標籤網絡上的實驗和比較顯示，SEMGNN實現了具有競爭力或改進的預測性能，同時提供了更真實和緊湊的標籤條件解釋。

##### **WikiSkill: Compiling Agent Experience into Persistent Knowledge for Skill Evolution**
2608.27454v1 by Liyan Tang, Cyrus Rashtchian, Chun-Sung Ferng, Andrew Tomkins, Da-Cheng Juan, Tu Vu

Agent skills package specialized knowledge and workflows into reusable resources that extend AI agent capabilities. Recent work automatically discovers such skills from agent experience, which enables agents to progressively adapt through interaction. However, the insights that guide skill development typically remain scattered across optimization histories, limiting their systematic reuse across iterations. We introduce WikiSkill, a framework that co-evolves agent skills with a persistent knowledge base (wiki). At a high level, WikiSkill separates raw execution experience, accumulated knowledge, and executable skills, while continuously consolidating experience into the wiki, which subsequent skill updates can build on. Across diverse benchmarks and models, WikiSkill consistently outperforms state-of-the-art skill-evolution methods and improves over no-skill baselines in most model-benchmark settings. We find that skill evolution complements model scaling: larger models generally benefit more from evolved skills, while smaller models with skills can outperform substantially larger models without them. We also find that evolved skills transfer effectively across models and model families, and skills evolved by other models can outperform self-evolved skills. Finally, our ablation studies confirm that persistent knowledge accumulation in the wiki is critical for effective skill evolution. These results demonstrate the benefits of systematically accumulating and refining agent experience for developing reusable and transferable skills.

摘要：代理技能包將專業知識和工作流程整合為可重用的資源，以擴展 AI 代理的能力。最近的研究自動從代理經驗中發現這些技能，使代理能夠通過互動逐步適應。然而，指導技能發展的見解通常散佈在優化歷史中，限制了它們在迭代中的系統性重用。我們介紹了 WikiSkill，一個與持久知識庫（wiki）共同演化代理技能的框架。在高層次上，WikiSkill 將原始執行經驗、累積知識和可執行技能分開，同時不斷將經驗整合到 wiki 中，以便後續的技能更新可以在其上構建。在多種基準和模型中，WikiSkill 一直超越最先進的技能演化方法，並在大多數模型-基準設置中改善了無技能基線。我們發現技能演化與模型擴展相輔相成：較大的模型通常從演化的技能中受益更多，而擁有技能的小型模型可以顯著超越沒有技能的更大型模型。我們還發現演化的技能能有效地在模型和模型家族之間轉移，其他模型演化的技能可以超越自我演化的技能。最後，我們的消融研究確認，wiki 中持久的知識積累對於有效的技能演化至關重要。這些結果展示了系統性積累和精煉代理經驗以發展可重用和可轉移技能的好處。

##### **Mechanistic Reaction Prediction via Discrete Flow Matching on Graph-Structured Electron Occupation**
2608.27429v2 by Nguyen Xuan-Vu, Octavian Susanu, Daniel Armstrong, Philippe Schwaller

Chemical reactions are fundamentally transformations in electron space, yet most machine learning approaches model them either through de novo generation of product molecules or through heuristic graph edits that operate directly on molecular topology. We introduce MAELLE (MechAnistic Edit fLow-matching on eLectron rEarrangements), which instead models reactions as discrete flow matching over electron occupation vectors. Concretely, we formulate the reactant-to-product mapping as a Continuous-time Markov Chain (CTMC) over the graph-structured integer-valued electron occupation space defined on all bonding, non-bonding, and hydrogen sites. To construct the interpolants between the reactants and products, we generalize the discrete flow matching mixture path to an edit-based formulation, where the electron moves are interpolated using Optimal Transport, yielding a mechanism-like set of moves without elementary step annotations. MAELLE achieves competitive performance on the USPTO-480K benchmark compared with leading reaction prediction models. Beyond in-distribution learning, we evaluate robustness across two out-of-distribution settings - structural complexity and reaction type - and find that MAELLE maintains strong performance where existing methods degrade. Finally, because the learned flow operates over the full electron redistribution, MAELLE naturally recovers mechanistic trajectories that align with known chemistry and can predict side products of a reaction.

摘要：化學反應基本上是電子空間中的轉變，但大多數機器學習方法要麼通過新生成的產物分子來建模，要麼通過直接作用於分子拓撲的啟發式圖編輯來建模。我們介紹了 MAELLE（機械編輯流匹配電子重排），它將反應建模為電子佔據向量上的離散流匹配。具體而言，我們將反應物到產物的映射公式化為在所有鍵合、非鍵合和氫位點上定義的圖結構整數值電子佔據空間上的連續時間馬爾可夫鏈（CTMC）。為了構建反應物和產物之間的插值，我們將離散流匹配混合路徑推廣到基於編輯的公式，其中電子移動使用最佳運輸進行插值，產生一組類似機制的移動，而不需要基本步驟註解。MAELLE 在與領先的反應預測模型相比的 USPTO-480K 基準測試中達到了競爭力的表現。除了在分佈內學習外，我們還在兩個分佈外的設置中評估了穩健性——結構複雜性和反應類型——發現 MAELLE 在現有方法退化的情況下仍能保持強勁的表現。最後，由於學習的流在整個電子重分配上運作，MAELLE 自然恢復與已知化學相符的機制軌跡，並能預測反應的副產物。

##### **How Language Models Organize and Structure Moral Knowledge**
2608.27402v1 by Orion Reblitz-Richardson

How do large language models (LLMs) organize moral knowledge? Models detect moral content broadly, but detection is a low bar. We ask whether they go further, distinguishing moral foundations from one another and organizing the relationships between them geometrically.
  We train six independent linear probes on open-weight language models, one per Moral Foundations Theory (MFT) category (care/harm, fair/cheat, lib/oppress, loy/betray, auth/subv, sanc/degrade), and examine how the resulting directions relate to each other in representation space. We find the directions neither collapse into a single moral detector nor isolate from one another. Rather, they span a near-maximal number of independent dimensions while sharing a positive common component. The shared component is the signature of integration, and it is moral-specific relative to a matched non-moral concept battery built identically (mean pairwise cosine 0.26 vs. 0.013).
  The geometry is consistent across architectures and scale and reaches its integration regime early in pre-training, well before probe accuracy saturates. The structure the model discovers shows no evidence of the individualizing/binding distinction predicted by Moral Foundations Theory (an underpowered test: only 20 candidate partitions exist) but rather reflects corpus statistics. Extending to moral dilemmas, each dilemma direction partially composes from its component foundations, at 2.7x a mismatched-pair baseline, while the majority of its variance encodes conflict-specific structure. The model represents moral tension itself, not a pre-resolved judgment.

摘要：大型語言模型（LLMs）如何組織道德知識？模型廣泛檢測道德內容，但檢測只是最低要求。我們詢問它們是否更進一步，區分彼此的道德基礎並幾何地組織它們之間的關係。<lb/>我們在開放權重的語言模型上訓練六個獨立的線性探針，每個探針對應於一個道德基礎理論（MFT）類別（關懷/傷害、公平/欺騙、自由/壓迫、忠誠/背叛、權威/壓制、神聖/貶低），並檢查結果方向在表示空間中的相互關係。我們發現這些方向既不會合併成單一的道德檢測器，也不會彼此隔離。相反，它們跨越接近最大數量的獨立維度，同時共享一個正的共同成分。共享的成分是整合的標誌，並且相對於一個相同構建的非道德概念電池是道德特定的（平均配對餘弦 0.26 對 0.013）。<lb/>這種幾何形狀在不同架構和規模之間是一致的，並且在預訓練的早期就達到了整合範疇，遠早於探針準確度飽和。模型發現的結構並未顯示出道德基礎理論所預測的個體化/綁定區別（這是一個能力不足的測試：僅存在 20 個候選劃分），而是反映了語料庫統計。擴展到道德困境，每個困境方向部分由其組成基礎組成，達到 2.7 倍的錯配對基準，而其大多數變異編碼了特定衝突的結構。該模型表示道德緊張本身，而不是預先解決的判斷。

##### **CorporateBench: Large-Scale Q&A Benchmarking with Temporal Knowledge Bases**
2608.27391v1 by Sil Hamilton, Albert Yu Sun, Oscar J. Romero, Carl-Leander Henneking, David Mimno, Bishan Yang, Igor Labutov

LLMs are increasingly able to answer complex questions about enterprise-scale document collections. But evaluation is hard: companies don't want to share internal communications, and synthetic datasets have been overly simple. We present CorporateBench (CB), a human-validated multi-task Q&A benchmark whose scale approaches the conditions LLMs encounter in corporate communication networks, with evaluation corpora surpassing 230,000 documents. CB evaluates LLMs across two dimensions (information extraction and knowledge base querying) through four synthetically generated firms ranging from 12 to 10,000 employees. Each corpus is sampled from a temporally evolving knowledge base describing a consistent world, guaranteeing cross-document logical consistency even across hundreds of thousands of documents. We evaluate five LLMs on CB, revealing increasingly poor performance as input size approaches realistic scales. CB provides LLM developers a metric for corporate communication reasoning, filling a crucial gap in the benchmarking ecosystem.

摘要：LLMs 越來越能夠回答有關企業級文件集合的複雜問題。<lb/>但評估是困難的：公司不願意分享內部通信，而合成數據集則過於簡單。<lb/>我們提出了 CorporateBench (CB)，這是一個經過人工驗證的多任務問答基準，其規模接近 LLMs 在企業通信網絡中遇到的條件，評估語料庫超過 230,000 份文件。<lb/>CB 通過四個合成生成的公司來評估 LLMs，這些公司員工數從 12 到 10,000 不等，涵蓋了兩個維度（信息提取和知識庫查詢）。<lb/>每個語料庫都來自一個隨時間演變的知識庫，描述了一個一致的世界，保證了跨文件的邏輯一致性，即使在數十萬份文件中也是如此。<lb/>我們在 CB 上評估了五個 LLMs，顯示出隨著輸入大小接近現實規模，性能逐漸下降。<lb/>CB 為 LLM 開發者提供了一個企業通信推理的指標，填補了基準生態系統中的一個關鍵空白。

##### **BrailleBench: Investigating Multi-Criteria Braille Comprehension in Large Language Models**
2608.27268v1 by Jinghan Zhang, Fengran Mo, Zhiyu Chen, Xiaoyan Han, Kunpeng Liu, Chang-Tien Lu

Although Large language models (LLMs) mediate access to knowledge and computational assistance, their capabilities should benefit vulnerable groups in the same way. However, it is unclear whether existing AI systems are inclusive enough for blind and deafblind users to access the same functionality through Braille, whose indicators, contractions, and digital representations introduce distinct requirements for model comprehension. To this end, we introduce BrailleBench, a benchmark for evaluating LLMs in Braille comprehension from different Criteria. BrailleBench aligns 5,570 instances from five datasets, including mathematics, commonsense, and multi-hop question answering across English and Braille Grades 1 and 2. Different configurations are designed to understand whether the systems can comprehend Braille-authored content, express answers in Braille, and complete end-to-end Braille interaction. To ensure the quality and prevent evaluation bias, the benchmark is built through a deterministic, expert-reviewed pipeline via a self-created Braille Toolkit without using any data instances generated by LLMs. We evaluate six representative LLMs from various aspects. The results reveal a persistent gap between print-English capability and Braille accessibility. Braille understanding and expression are asymmetric, where Grade 2 is especially fragile on the input side compared to Grade 1, and fully Braille requests further reduce performance. The experimental observations provide valuable guidance for the development of future Braille AI systems. All related resources in BrailleBench are publicly available for future research.

摘要：儘管大型語言模型（LLMs）在知識和計算協助方面提供了媒介，但它們的能力應該同樣惠及脆弱群體。然而，目前尚不清楚現有的人工智慧系統是否足夠包容，以便盲人和聾盲用戶能夠通過盲文訪問相同的功能，因為盲文的指示符、縮寫和數位表示引入了模型理解的獨特需求。為此，我們介紹了BrailleBench，一個用於評估LLMs在盲文理解方面的基準，基於不同的標準。BrailleBench對來自五個數據集的5,570個實例進行了對齊，包括數學、常識和多跳問題回答，涵蓋英語和盲文的1級和2級。設計了不同的配置，以了解系統是否能夠理解盲文創作的內容、以盲文表達答案，以及完成端到端的盲文互動。為了確保質量並防止評估偏見，該基準是通過一個確定性的、專家審核的流程建立的，使用的是自創的盲文工具包，而未使用任何由LLMs生成的數據實例。我們從各個方面評估了六個具有代表性的LLMs。結果顯示，印刷英語能力與盲文可及性之間存在持續的差距。盲文理解和表達是非對稱的，2級在輸入方面特別脆弱，相較於1級，完全的盲文請求進一步降低了性能。實驗觀察為未來盲文人工智慧系統的發展提供了寶貴的指導。BrailleBench中的所有相關資源均可公開用於未來的研究。

##### **GRAIN: Bridging Name and Narrative Shifts in Real-World Graph Reasoning through Invariance-Rewarded Agentic RL**
2608.27142v1 by Zike Yuan, Han Zhang, Jianzhi Yan, Le Liu, Cai Ke, Huozhi Zhou, Jian Xie, Jiran Yin, Yukun Cao, Yue Yu, Hui Wang, Ming Liu, Bing Qin

Despite their potential in standardized graph tasks, Large Language Models (LLMs) remain brittle to real-world shifts in node identifiers and task formulation. While deterministic graph tools are invariant to such shifts, extracting topological structures from noisy text is highly fragile for LLMs, which often overfit to surface patterns. Moreover, mitigating these parsing failures via multi-agent systems incurs prohibitive latency. To address this, we propose GRAIN, a single-agent framework optimized via reinforcement learning. GRAIN models reasoning as a semantic parsing and tool-execution pipeline, guided by a Structure Invariance Reward. By validating extracted intermediate graphs against ground-truth topologies, this reward forces the LLM to learn robust text-to-structure mappings rather than memorizing linguistic artifacts. We also introduce GRIT, a benchmark evaluating sensitivity to such linguistic shifts. GRAIN outperforms multi-agent baselines by 16.45\% in accuracy with approximately 24\% lower latency. Furthermore, it demonstrates superior structural generalization, halving the out-of-distribution (OOD) gap of SFT models (from 15.77\% to 7.80\%) and maintaining robustness on large-scale graphs beyond the training distribution.

摘要：儘管大型語言模型（LLMs）在標準化圖形任務中具有潛力，但它們對於節點標識符和任務表述的現實世界變化仍然脆弱。雖然確定性圖形工具對這些變化不變，但從嘈雜文本中提取拓撲結構對於LLMs來說是非常脆弱的，這些模型往往過度擬合表面模式。此外，通過多代理系統減輕這些解析失敗會產生高昂的延遲。為了解決這個問題，我們提出了GRAIN，一個通過增強學習優化的單代理框架。GRAIN將推理建模為語義解析和工具執行管道，並由結構不變獎勵指導。通過將提取的中間圖與真實拓撲進行驗證，這一獎勵迫使LLM學習穩健的文本到結構的映射，而不是記憶語言學的文物。我們還引入了GRIT，一個評估對這種語言學變化敏感性的基準。GRAIN在準確性上比多代理基準高出16.45\%，並且延遲約低24\%。此外，它展示了卓越的結構泛化，將SFT模型的分佈外（OOD）差距減半（從15.77\%降至7.80\%），並在超越訓練分佈的大規模圖上保持穩健性。

##### **TransMeme: A Multi-Agent Framework for Cross-Cultural Meme Transcreation**
2608.27127v1 by Jingyi Zheng, Yule Liu, Zifan Peng, Tianyi Hu, Yuemeng Zhao, Xinhu Zheng, Xinlei He

Internet memes are a pervasive form of multimodal online communication; however, such communication often involves users from diverse linguistic and cultural backgrounds. Therefore, adapting memes across cultures and languages is a central challenge for enabling mutual understanding in online communication. Unlike ordinary translation or standalone text rewriting, cross-cultural meme transcreation must jointly preserve communicative intent, adapt culture-dependent meaning for the target audience, and maintain coherence between text and image. In this work, we first provide an explicit task analysis of cross-cultural meme transcreation and identify three core challenges: culture-specific knowledge understanding, intent and tone preservation, and multimodal consistency. Based on this analysis, we propose a multi-agent framework with specialized agents that are coordinated to address these challenges through cultural adaptation, target text rewriting, revision, and conditional visual adjustment. The framework strengthens target text adaptation with coordinated feedback to handle difficult cases that require deeper cultural or visual intervention. We evaluate the framework on bidirectional Chinese-English meme transcreation using both human evaluation and LLM-as-a-Judge. Our method consistently outperforms all baselines across both evaluation settings. In human evaluation, it achieves the best performance on all four dimensions and delivers a 33.1% average improvement over the strongest baseline, while in LLM-as-a-Judge, it attains the highest Top-1 ranking rate (60% versus 26% for the second-best baseline). Further analysis indicates that each component contributes to the performance. Our error analysis suggests that the remaining bottlenecks lie in humor reconstruction and image-text alignment rather than simple cultural knowledge gaps, pointing to future work on humor transfer.

摘要：網路迷因是一種普遍存在的多模態線上溝通形式；然而，這種溝通通常涉及來自不同語言和文化背景的使用者。<lb/>因此，跨文化和語言的迷因適應是促進線上溝通中相互理解的一個核心挑戰。<lb/>與普通翻譯或獨立文本重寫不同，跨文化迷因再創作必須共同保留溝通意圖，為目標受眾調整文化依賴的意義，並保持文本與圖像之間的一致性。<lb/>在這項工作中，我們首先提供了跨文化迷因再創作的明確任務分析，並確定了三個核心挑戰：文化特定知識理解、意圖和語調保留，以及多模態一致性。<lb/>基於這一分析，我們提出了一個多代理框架，具有專門的代理，這些代理協調合作以通過文化適應、目標文本重寫、修訂和條件視覺調整來解決這些挑戰。<lb/>該框架通過協調反饋來加強目標文本的適應，以處理需要更深入文化或視覺介入的困難案例。<lb/>我們在雙向中英文迷因再創作上評估了該框架，使用了人類評估和LLM-as-a-Judge。<lb/>我們的方法在兩種評估設置中始終超越所有基準。<lb/>在人工評估中，它在所有四個維度上達到了最佳表現，並在最強基準上實現了33.1%的平均改進，而在LLM-as-a-Judge中，它達到了最高的Top-1排名率（60%對比第二強基準的26%）。<lb/>進一步分析表明，每個組件對性能都有貢獻。<lb/>我們的錯誤分析表明，剩餘的瓶頸在於幽默重建和圖像文本對齊，而非簡單的文化知識差距，這指向了未來在幽默轉移上的工作。

##### **pro-team at LLMs4OL 2026 Tasks Flagship and Reuse: Retrieval-Augmented Generation and Vocabulary-Constrained Filtering for Ontology Learning**
2608.27101v1 by Shivam Mishra, Dhannu Ram Meena, Muneendra Ojha, Krishna Pratap Singh, Kuldeep Singh

Ontology learning from text remains challenging despite significant progress in Large Language Models (LLMs), which can hallucinate domain terms, produce inconsistent formats, and favor hierarchical over associative relations. In the LLMs4OL 2026 Challenge, we address both the End-to-End Flagship Task (Task A) and Ontology Extension Reuse Task (Task B) using an offline retrieval-augmented few-shot prompting pipeline. Our system employs Qwen2.5-14B-Instruct with all-MiniLM-L6-v2 for demonstration retrieval, selecting the top-5 examples for Task A and top-2 for Task B. A left-truncated context-windowing strategy preserves task instructions within long prompts. For Task B, generated triples undergo deterministic vocabulary-constrained filtering, retaining triples when at least one endpoint belongs to the sample's closed term/type vocabulary and removing duplicates of the initial ontology. The approach achieves Semantic Graph Similarity of 0.8692, Term-Typing F1 of 0.9200, and Taxonomy Discovery F1 of 0.8540 on Task B, while Task A achieves 0.7416 Semantic Graph Similarity. However, no non-taxonomic relations are extracted, highlighting limitations of closed, taxonomy-oriented relation vocabularies.

摘要：本體學從文本中學習仍然具有挑戰性，儘管大型語言模型（LLMs）已取得顯著進展，但它們可能會產生領域術語的幻覺、產生不一致的格式，並偏好層級關係而非聯想關係。在LLMs4OL 2026挑戰中，我們使用離線檢索增強的少量提示管道，解決了端到端旗艦任務（任務A）和本體擴展重用任務（任務B）。我們的系統使用Qwen2.5-14B-Instruct和all-MiniLM-L6-v2進行演示檢索，為任務A選擇前5個示例，為任務B選擇前2個示例。左側截斷的上下文窗口策略在長提示中保留任務指令。對於任務B，生成的三元組經過確定性詞彙約束過濾，當至少一個端點屬於樣本的封閉術語/類型詞彙時保留三元組，並刪除初始本體的重複項。該方法在任務B上實現了0.8692的語義圖相似度、0.9200的術語標記F1和0.8540的分類發現F1，而任務A達到了0.7416的語義圖相似度。然而，未提取任何非分類關係，突顯了封閉的、以分類為導向的關係詞彙的局限性。

##### **Active Diffusion-Based Inference for Ill-Posed Inverse Problems under Incomplete Priors**
2608.27080v1 by Jitao Xu, Nobuo Sato, Yaohang Li

Many scientific and engineering applications require estimating unknown parameters from experimentally observable data -- an inverse problem that is inherently challenging due to nonlinearity, noise, and ill-posedness. In this paper, we propose an active diffusion-based inverse problem solver. A DM is trained to learn the mapping between the parameter space and the observable space. By iteratively detecting and correcting model misspecification through posterior uncertainty, the method discovers and learns the correct region of parameter space, even when initial training bounds exclude the true parameters. This provides a principled, Bayesian justification for adaptive domain augmentation and ensures robust inference for inverse problems under incomplete prior knowledge. We demonstrate the effectiveness of our inverse solver for a toy inverse problem with infinite solutions, and for the parameterization of the quantum correlation functions to event observables in a Quantum Chromodynamics analysis of nucleon structure.

摘要：許多科學和工程應用需要從實驗可觀察數據中估計未知參數——這是一個逆問題，由於非線性、噪聲和不適定性，本質上具有挑戰性。在本文中，我們提出了一種基於主動擴散的逆問題求解器。通過訓練一個DM來學習參數空間與可觀察空間之間的映射。通過迭代檢測和修正模型錯誤規範，利用後驗不確定性，該方法發現並學習正確的參數空間區域，即使初始訓練範圍排除了真實參數。這為自適應領域擴增提供了一個有原則的貝葉斯論證，並確保在不完整的先驗知識下對逆問題進行穩健推斷。我們展示了我們的逆求解器在一個具有無限解的玩具逆問題中的有效性，以及在量子色動力學分析核子結構中將量子相關函數參數化為事件可觀察量的效果。

##### **Unifying Detection and Adaptation in Task-Free Continual Learning**
2608.27070v1 by Dezheng Han, Anbang Zhang, Zhihao Zhu, Shuaishuai Guo

To mitigate catastrophic forgetting in downstream continual learning (CL) for large language models (LLMs), existing methods typically constrain parameter updates or introduce task-specific adaptation modules. However, these methods often rely on explicit task boundaries during training, limiting their applicability to realistic task-free scenarios. In this paper, we propose a \textbf{Fi}sher-guided \textbf{uni}fied (\textbf{FiUni}) framework for batch-level task detection and parameter-efficient continual adaptation. FiUni is motivated by a key observation about the Fisher information matrix (FIM) of pre-trained models: the orthogonality among the principal subspaces of its Kronecker-Factored Approximate Curvature (K-FAC) approximation, estimated from a small number of downstream task samples, can reflect the similarity between different tasks. Based on this observation, FiUni constructs FIM-derived frozen subspaces to guide low-rank adaptation (LoRA), while matching the Fisher principal subspace of each incoming batch window with historical subspaces. This enables FiUni to adaptively determine whether to reuse existing knowledge, expand a related subspace, or create a new subspace, dynamically balancing knowledge sharing and task isolation. Experiments show that FiUni can effectively infer latent batch-level task affiliations and achieve competitive performance against advanced task-aware CL methods with fewer trainable parameters.

摘要：為了減輕大型語言模型（LLMs）在下游持續學習（CL）中的災難性遺忘，現有的方法通常會限制參數更新或引入特定任務的適應模塊。<lb/>然而，這些方法往往依賴於訓練過程中的明確任務邊界，限制了它們在現實無任務場景中的適用性。<lb/>在本文中，我們提出了一個\textbf{Fi}sher指導的\textbf{統}一（\textbf{FiUni}）框架，用於批次級任務檢測和參數高效的持續適應。<lb/>FiUni的動機來自於對預訓練模型的Fisher信息矩陣（FIM）的關鍵觀察：其克羅內克分解近似曲率（K-FAC）近似的主要子空間之間的正交性，可以反映不同任務之間的相似性，這是從少量的下游任務樣本中估算得出的。<lb/>基於這一觀察，FiUni構建了基於FIM的凍結子空間，以指導低秩適應（LoRA），同時將每個進入批次窗口的Fisher主要子空間與歷史子空間進行匹配。<lb/>這使得FiUni能夠自適應地決定是否重用現有知識、擴展相關子空間或創建新子空間，動態平衡知識共享和任務隔離。<lb/>實驗表明，FiUni能夠有效推斷潛在的批次級任務隸屬關係，並在可訓練參數更少的情況下，實現與先進的任務感知CL方法的競爭性能。

##### **GraphMemix: Query-Aware Evidence Forests for Long-Term Multimodal Agent Memory**
2608.26983v1 by Geng Li, Yuhao Wang, Dong Li, Jianye Hao, Yuxin Peng

Organizing long-term memory for multimodal agents remains challenging because existing methods either suffer from expensive question-agnostic offline summaries or naive embedding similarity matching that introduces incomplete and redundant context. To address these issues, we propose GraphMemix, a combinatorial-optimization graph memory framework that models memory organization as query-aware evidence-forest construction. Specifically, our method consists of three key components:(1) candidate graph construction, which expands multi-view seed memories through schema and semantic relations to acquire query-aware original context; (2) evidence utility and activation costs, which decouples direct memory support from anchor-conditioned relation verification to suppress redundant or conflicting information; and (3) forest optimization, which jointly selects a forest-format memory context under a maximum evidence budget and its reliable relational structure. By organizing memory into a query-relevant subgraph, the method avoids substantial lifecycle cost and recovers low-similarity complementary evidence. Experimental results across four long-term multimodal memory benchmarks demonstrate significant improvements with different foundation models and establish a new Pareto frontier between accuracy and lifecycle cost.

摘要：組織多模態代理的長期記憶仍然具有挑戰性，因為現有的方法要麼面臨昂貴的與問題無關的離線摘要，要麼是引入不完整和冗餘上下文的天真嵌入相似性匹配。為了解決這些問題，我們提出了GraphMemix，一個組合優化的圖形記憶框架，將記憶組織建模為基於查詢的證據森林構建。具體而言，我們的方法由三個關鍵組件組成：(1) 候選圖構建，通過模式和語義關係擴展多視角種子記憶，以獲取基於查詢的原始上下文；(2) 證據效用和激活成本，將直接記憶支持與錨點條件關係驗證解耦，以抑制冗餘或衝突的信息；以及 (3) 森林優化，根據最大證據預算及其可靠的關係結構共同選擇森林格式的記憶上下文。通過將記憶組織成與查詢相關的子圖，該方法避免了巨大的生命週期成本並恢復了低相似度的互補證據。在四個長期多模態記憶基準上的實驗結果顯示，使用不同的基礎模型取得了顯著的改進，並在準確性和生命週期成本之間建立了一個新的帕累托邊界。

##### **AraMS-28k: The Largest Publicly Released Line-Level Dataset of Historical Arabic Manuscripts with Margin and Insertion-Anchor Annotations**
2608.26921v1 by Mohamed Guechaoui, Mohamed Diaa Zellagui, Souleyman Chaib, Sahraoui Dhelim

We introduce AraMS-28k, the largest publicly released line-level dataset of genuine historical Arabic manuscripts, comprising 14 books, 3,043 pages, and 28,600 annotated text lines (27,971 main-text, 629 margin). Thirteen books are hand-copied manuscripts spanning three script traditions -- Naskh, Ruq'ah, and Maghrebi -- and one is a lithographed printed edition included to broaden format diversity. Each line is labelled as main-text or margin, and margin lines that have an unambiguous attachment point in the main text are further annotated with an insertion anchor, recovering the manuscript's true non-linear reading order at line-level granularity -- to our knowledge the first such annotation released for a historical Arabic manuscript corpus. Because reference transcriptions are fully vocalised while manuscript hands are typically undiacritised, we release both the raw diacritised transcription and a diacritic-normalised counterpart for every line. The dataset was constructed with RefLAM, a reference-grounded annotation pipeline that aligns multimodal-LLM OCR against independently sourced clean transcriptions and routes every line through human review, combining automatic verification with expert oversight. We describe the construction and quality-control process, present the annotation schema, report dataset statistics at both the corpus and per-book level, and provide baseline HTR results using Kraken and HATFormer, including a cross-script generalisation gradient from in-distribution pages to fully unseen books. AraMS-28k is released with page images, line-level annotations, and fixed train/val/test splits under CC BY-NC-SA 4.0 to support reproducible research on Arabic manuscript recognition, layout analysis, and reading-order recovery.

摘要：我們介紹AraMS-28k，這是最大的公開釋出的行級別真實歷史阿拉伯手稿數據集，包含14本書、3,043頁和28,600條註釋文本行（27,971條主文本，629條邊註）。十三本書是手抄手稿，涵蓋了三種書寫傳統——Naskh、Ruq'ah和Maghrebi——而一本是為了擴大格式多樣性而包含的石印版印刷本。每一行被標記為主文本或邊註，並且那些在主文本中有明確附著點的邊註行進一步註釋了插入錨點，恢復了手稿在行級別的真實非線性閱讀順序——據我們所知，這是首次針對歷史阿拉伯手稿語料庫釋出的此類註釋。由於參考抄本是完全元音化的，而手稿書寫通常不帶變音符號，因此我們對每一行釋出了原始的帶變音符號的抄本和一個去變音符號化的對應版本。該數據集是使用RefLAM構建的，這是一個基於參考的註釋管道，將多模態LLM OCR與獨立來源的乾淨抄本對齊，並將每一行通過人工審查，結合自動驗證和專家監督。我們描述了構建和質量控制過程，呈現了註釋架構，報告了語料庫和每本書的數據集統計，並提供了使用Kraken和HATFormer的基線HTR結果，包括從分佈內頁面到完全未見書籍的跨腳本概括梯度。AraMS-28k隨頁面圖像、行級別註釋和固定的訓練/驗證/測試拆分以CC BY-NC-SA 4.0的方式釋出，以支持對阿拉伯手稿識別、佈局分析和閱讀順序恢復的可重複研究。

##### **PLCBench: Can Autonomous LLM Agents Turn PLC Access into Sustained Physical Impact?**
2608.26882v1 by Yitian Zhou, Jingyu Zheng, Qiliang Jiang, Linkang Du, Haoming Liu, Lichao Wu, Shiyi Zhao, Mengxiang Liu, Ruilong Deng

Industrial control systems (ICSs) rely on programmable logic controllers (PLCs) to connect networked computation with physical control. Tool-using large language model (LLM) agents represent an emerging attack threat: can an autonomous agent convert a network-reachable PLC into sustained adverse physical impact? However, existing evaluations focus on digital tasks or individual stages of PLC testing. In ICSs, evaluations that stop at software exploitation, an accepted write, or tool access may therefore mischaracterize physical risk.
  We present PLCBENCH, to our knowledge, the first real-PLC hardware-in-the-loop (HIL) framework for characterizing this cyber-to-physical capability and its boundaries. It combines vendor-native interaction, commercial PLC execution, closed-loop reduced-order process simulation, and independent outcome verification. A deterministic evaluator applies fixed rules to runner, communication, PLC-object, and process records to assign six hidden diagnostic flags, distinguishing usable PLC interaction, process-linked manipulation, and sustained physical impact. We instantiate PLCBENCH on four commercial PLCs crossed with four closed-loop workloads. Across five LLM families and 240 real-PLC episodes, 75 episodes (31.3%) sustain their respective physical objectives. Stagewise results show that 98 episodes stop before a valid native read, whereas 62 reach a process-linked write but do not sustain the final objective. Notably, richer process observation is associated with an increase in conditional objective attainment after a process-linked write from 44.2% to 64.0%. These measurements localize failure in configured PLC-process deployments and identify intervention points for future defense evaluation. To support reproducibility, we release the safely disclosable PLCBENCH code and a software-only reproduction pipeline through the accompanying artifact.

摘要：工業控制系統（ICS）依賴可程式邏輯控制器（PLC）將網絡計算與物理控制連接起來。使用工具的大型語言模型（LLM）代理代表了一種新興的攻擊威脅：一個自主代理能否將一個可通過網絡訪問的PLC轉化為持續的不利物理影響？然而，現有的評估集中於數字任務或PLC測試的個別階段。在ICS中，停留在軟體利用、被接受的寫入或工具訪問的評估可能會錯誤地描述物理風險。<lb/>  
我們提出了PLCBENCH，據我們所知，這是第一個實際PLC硬體在迴路（HIL）框架，用於表徵這種網絡到物理的能力及其邊界。它結合了供應商原生互動、商業PLC執行、閉環降階過程模擬和獨立結果驗證。一個確定性評估器對運行者、通訊、PLC對象和過程記錄應用固定規則，以分配六個隱藏診斷標誌，區分可用的PLC互動、過程相關操作和持續的物理影響。<lb/>  
我們在四個商業PLC上實現PLCBENCH，並交叉四個閉環工作負載。在五個LLM家族和240個實際PLC的實例中，75個實例（31.3%）持續其各自的物理目標。逐階結果顯示，98個實例在有效的原生讀取之前停止，而62個達到過程相關的寫入但未能持續最終目標。值得注意的是，豐富的過程觀察與過程相關寫入後的條件目標達成率從44.2%增加到64.0%相關聯。這些測量將故障定位於配置的PLC-過程部署中，並確定未來防禦評估的干預點。為了支持可重複性，我們通過隨附的文物釋放安全可披露的PLCBENCH代碼和僅軟體的重現管道。

##### **C-Unseen: Weak Signal Detection in Dynamic Temporal Knowledge Graphs via LLM Reasoning**
2608.26870v1 by Yassir Lairgi, Ludovic Moncla, Khalid Benabdeslem, Rémy Cazabet, Pierre Cléau

Weak signals are early, low-visibility indicators that precede significant changes before those changes become established. Existing detection methods, based on keyword frequency, topic modeling, or untyped graph topology, fail to capture the semantic and relational structure through which such signals manifest. In this paper, we propose C-Unseen, a self-interpretable framework for weak signal detection in Dynamic Temporal Knowledge Graphs (DTKGs). We define a weak signal as a rare, semantically coherent subgraph that proliferates across consecutive TKG snapshots. The framework operates through two modules: a Rare Subgraphs Extractor, in which an LLM identifies subgraphs whose content is in tension with the dominant snapshot narrative via chain-of-thought reasoning, and a Weak Signal Alerter, in which the persistence of these rare subgraphs is tracked across time steps to isolate true weak signals. Experimental results demonstrate that C-Unseen outperforms keyword-, topic-, and graph-based baselines.

摘要：弱信號是早期、低可見度的指標，預示著重大變化在這些變化成為既定事實之前的情況。現有的檢測方法，基於關鍵字頻率、主題建模或未類型化的圖拓撲，無法捕捉這些信號顯現的語義和關係結構。在本文中，我們提出了C-Unseen，一個自我解釋的框架，用於在動態時間知識圖（DTKGs）中檢測弱信號。我們將弱信號定義為一個稀有的、語義一致的子圖，該子圖在連續的TKG快照中擴散。該框架通過兩個模組運作：一個稀有子圖提取器，其中LLM通過思維鏈推理識別與主導快照敘事存在緊張關係的子圖，以及一個弱信號警報器，其中這些稀有子圖的持續性在時間步驟中被跟蹤，以孤立真正的弱信號。實驗結果表明，C-Unseen的表現優於基於關鍵字、主題和圖的基準。

##### **BekchiAI: Measuring, Observing, and Controlling LLM Agents in One Click**
2608.26867v1 by Mesut Toruk

Large language model agents reason, call tools, and act autonomously over many steps, but their agentic skills-correctly sequencing tools, planning under dependencies, judging untrusted inputs, and grounding generated arguments-are hard to measure with accuracy-only leaderboards. We present BekchiAI, which addresses both sides: a benchmark for measuring agentic skill and a platform for observing and controlling live agents. The BekchiAI-Benchmark, a suite of 13 tool-using ReAct agents across 7 task categories (arithmetic, structured/SQL, security detection, URL grounding, planning, orchestration, and tool-policy), totalling 2,057 deterministic, committed test tasks. Every task is verifier-checkable gold answers are computed by running canonical SQL against a real database, computing the exact schedule of a directed acyclic graph (DAG), or evaluating closed-form lambdas including adversarial security samples paired with deliberately imperfect signature scanners so a score reflects the model's own judgment, not the copying of an oracle. We define a small set of behavioral metrics beyond accuracy-tool-call adherence, URL hallucination and source-match, and per-model token cost and report a four-model comparison (Qwen3.7-Max, gemma-4-31B-it, gemma4:26b, gpt-oss-120b) whose story is in the per-family spread, not the aggregate. The benchmark runs are executed using the provided evaluation scripts. BekchiAI-Platform is a complementary web-based observability and control layer for deployed agents, providing full token and latency telemetry as well as remote run termination. The benchmark, evaluation tools, and platform are publicly released.

摘要：大型語言模型代理人能夠推理、呼叫工具，並在多個步驟中自主行動，但其代理技能——正確地排序工具、在依賴關係下規劃、判斷不可信的輸入，以及將生成的論點落實——僅用排行榜來測量是很難準確的。我們提出了BekchiAI，解決了這兩方面的問題：一個用於測量代理技能的基準，以及一個用於觀察和控制實時代理的平臺。BekchiAI-Benchmark是一套包含13個使用工具的ReAct代理的基準，涵蓋7個任務類別（算術、結構化/SQL、安全檢測、URL落實、規劃、編排和工具政策），總計2,057個確定性、已承諾的測試任務。每個任務的驗證者可檢查的金標準答案是通過對實際數據庫運行標準SQL計算得出的，計算有向無環圖（DAG）的確切時間表，或評估包括對抗性安全樣本的封閉形式lambda，這些樣本與故意不完美的簽名掃描儀配對，以便得分反映模型自身的判斷，而不是複製神諭。我們定義了一小組超越準確度的行為指標——工具呼叫遵循、URL幻覺和來源匹配，以及每個模型的標記成本，並報告了一個四模型的比較（Qwen3.7-Max、gemma-4-31B-it、gemma4:26b、gpt-oss-120b），其故事在於每個家族的分佈，而非總體。基準運行使用提供的評估腳本執行。BekchiAI-Platform是一個補充的基於網絡的可觀察性和控制層，供已部署的代理使用，提供完整的標記和延遲遙測以及遠程運行終止。該基準、評估工具和平台已公開發布。

##### **MedFG-VQA: Low-Frequency Memory and Graph Attention for Lightweight Medical VQA**
2608.26848v1 by Haowen Gu, Gensheng Pei, Zeren Sun, Mingwu Ren, Xiangbo Shu, Yazhou Yao, Fumin Shen

Medical Visual Question Answering (Med-VQA) holds significant promise for clinical decision support, yet faces challenges due to limited annotated data and the high computational demands of existing large vision-language models. We propose MedFG-VQA, a lightweight framework that leverages a memory bank to augment DCT-based low-frequency features and employs graph-enhanced cross-attention for effective visual-textual alignment. Specifically, our approach features two key components: Frequency-Memory Fusion (FMF), which enhances low-frequency features by retrieving from a learnable memory bank built on DCT decomposition, and Graph-Aware Cross-Attention (GACA), which aligns visual-textual features via cross-attention and refines them through graph-convolutional aggregation. To address data scarcity, we construct SynMed-VQA, a large-scale synthetic dataset comprising over 2 million question-answer pairs across 9 imaging modalities and 10 major organs, generated with GPT-4o. Extensive experiments on SynMed-VQA and three other standard biomedical VQA benchmarks demonstrate that MedFG-VQA achieves competitive or superior performance compared to much larger models while maintaining significantly lower computational costs, highlighting its efficiency and potential for clinical deployment.

摘要：醫療視覺問題回答（Med-VQA）在臨床決策支持方面具有重要潛力，但由於標註數據有限以及現有大型視覺-語言模型的高計算需求，面臨挑戰。我們提出了 MedFG-VQA，一個輕量級框架，利用記憶庫來增強基於 DCT 的低頻特徵，並採用圖增強的交叉注意力來實現有效的視覺-文本對齊。具體而言，我們的方法包含兩個關鍵組件：頻率-記憶融合（FMF），通過從基於 DCT 分解構建的可學習記憶庫中檢索來增強低頻特徵，以及圖感知交叉注意力（GACA），通過交叉注意力對視覺-文本特徵進行對齊，並通過圖卷積聚合進行精煉。為了解決數據稀缺問題，我們構建了 SynMed-VQA，一個大型合成數據集，包含超過 200 萬個問題-答案對，涵蓋 9 種成像模式和 10 個主要器官，使用 GPT-4o 生成。在 SynMed-VQA 和其他三個標準生物醫學 VQA 基準上的廣泛實驗表明，MedFG-VQA 在性能上與更大型模型相當或優越，同時保持顯著較低的計算成本，突顯了其效率及在臨床部署中的潛力。

##### **Evaluating Confidence-Gated Retrieval with Matched Trajectory Replay**
2608.26846v1 by Prateek Chhikara

Interactive language-model agents use confidence signals to decide whether to answer immediately, retrieve additional evidence (from memory or external knowledge), or defer. Yet confidence is usually evaluated in isolation, without measuring the trajectory-level consequences of the actions it triggers. We propose matched trajectory replay, a controlled protocol for comparing confidence-to-action mappings. The protocol holds candidate answer states, evidence points, budgets, and action costs fixed. We use it to compare raw verbalized confidence with post-hoc isotonic calibration in a multi-hop question-answering system using Mistral, GPT, and Qwen models on HotpotQA and MuSiQue datasets. At the same numerical commitment threshold, calibration changes which questions agents ultimately commit to answering. Across all six model-dataset pairs, it increases accuracy among committed answers by up to 41 percentage points. However, it can reduce coverage and increase retrieval use. Overall accuracy improves by up to 15 percentage points on HotpotQA but falls by up to 17 percentage points on MuSiQue. These effects reflect a shift to a more selective, lower-risk operating point, not improved answers or confidence ranking. A calibration map fitted before retrieval improves held-out calibration through retrieval depths one and two, but is worse than raw confidence at depth three for all three models. Additional evidence helps on average, but this aggregate effect does not establish whether confidence identifies which individual episodes will benefit from another retrieval. Taken together, these results show that calibration can make commitment risk interpretable, but it does not estimate the expected benefit of another retrieval. Retrieval therefore requires a separate value-of-information or utility estimate. Evaluations should report held-out calibration, risk-coverage, and retrieval cost.

摘要：互動語言模型代理使用信心信號來決定是否立即回答、檢索額外證據（來自記憶或外部知識）或延遲。然而，信心通常是孤立評估的，沒有衡量其觸發的行動的軌跡級後果。我們提出了匹配軌跡重播，這是一種比較信心與行動映射的受控協議。該協議保持候選答案狀態、證據點、預算和行動成本不變。我們用它來比較在使用 Mistral、GPT 和 Qwen 模型的多跳問答系統中，原始口頭表達的信心與事後等級校準在 HotpotQA 和 MuSiQue 數據集上的表現。在相同的數值承諾閾值下，校準改變了代理最終承諾回答的問題。在所有六個模型-數據集對中，它使承諾答案的準確性提高了最多 41 個百分點。然而，它可能會降低覆蓋率並增加檢索使用。在 HotpotQA 上，整體準確性提高了最多 15 個百分點，但在 MuSiQue 上下降了最多 17 個百分點。這些效果反映了轉向更具選擇性、風險較低的操作點，而不是改進答案或信心排名。在檢索之前擬合的校準圖在檢索深度一和二時改善了保持的校準，但在深度三時對所有三個模型來說，表現不如原始信心。額外證據平均上是有幫助的，但這種整體效果並未確定信心是否能識別哪些個別事件將從另一個檢索中受益。綜合來看，這些結果顯示校準可以使承諾風險可解釋，但它並不估計另一個檢索的預期收益。因此，檢索需要單獨的信息價值或效用估計。評估應報告保持的校準、風險覆蓋和檢索成本。

##### **DAMP: Decay-Aware Mixed-Precision Recurrent-State Quantization**
2608.27513v1 by Tao Zhang, Jianchao Tan, Pingwei Sun, Yanqi Yu, Zixu Jiang, Yuchen Xie, Xunliang Cai, Ziqian Zeng

Softmax attention stores key and value vectors for every preceding token, causing inference memory to grow with sequence length. Recent language models incorporating Gated DeltaNet (GDN) or Kimi Delta Attention (KDA) reduce this cost by replacing the KV cache in most layers with fixed-size recurrent states. However, these recurrent states are commonly stored in FP32 and consume substantial GPU memory; their updates are memory-bandwidth bound and contribute significantly to decoding latency. To our knowledge, we are the first to study post-training quantization of recurrent states in GDN and KDA based language models. We find that uniform quantization provides a poor accuracy--storage trade-off: INT8 and FP8 already degrade accuracy on complex reasoning tasks, while INT4 and NVFP4 reduce it to near zero. We further find that most quantization-error energy is concentrated in a small subset of channels and that the relative decay strength of state channels remains stable across prompts and tasks. Motivated by these findings, DAMP uses both quantization-error energy and decay-based persistence to identify high-risk channels during offline calibration. It stores these channels at higher precision and the remainder in INT8. We evaluate DAMP on Qwen3.6-35B and Kimi-Linear-48B across six benchmarks covering mathematical reasoning, general reasoning, and code generation. At 9.9 bits per state value, DAMP maintains average accuracy close to the FP32 baseline. DAMP reduces recurrent-state storage by 69.1%, accelerates the recurrent-state update kernel by up to 2.01x, and lowers full-model TPOT by up to 10.9%.

摘要：Softmax 注意力為每個前置標記儲存鍵和值向量，導致推理記憶體隨著序列長度增加而增長。最近的語言模型結合了 Gated DeltaNet (GDN) 或 Kimi Delta Attention (KDA)，通過在大多數層中用固定大小的遞迴狀態替換 KV 快取來降低這一成本。然而，這些遞迴狀態通常以 FP32 儲存，並消耗大量 GPU 記憶體；它們的更新受到記憶體帶寬的限制，並顯著影響解碼延遲。據我們所知，我們是第一個研究 GDN 和 KDA 基礎語言模型中遞迴狀態後訓練量化的團隊。我們發現均勻量化提供了不佳的準確性與儲存之間的權衡：INT8 和 FP8 在複雜推理任務中已經降低了準確性，而 INT4 和 NVFP4 將其降低至接近零。我們進一步發現，大多數量化誤差能量集中在一小部分通道中，並且狀態通道的相對衰減強度在不同提示和任務中保持穩定。受到這些發現的激勵，DAMP 利用量化誤差能量和基於衰減的持久性來識別離線校準過程中的高風險通道。它以更高的精度儲存這些通道，其餘部分則以 INT8 儲存。我們在 Qwen3.6-35B 和 Kimi-Linear-48B 上評估 DAMP，涵蓋數學推理、一般推理和代碼生成的六個基準。在每個狀態值 9.9 位的情況下，DAMP 維持接近 FP32 基線的平均準確性。DAMP 減少了 69.1% 的遞迴狀態儲存，將遞迴狀態更新內核的速度提高至 2.01 倍，並將全模型 TPOT 降低至 10.9%。

##### **SymbolLKG: Towards Verifiable Logical Reasoning via Logical Knowledge Graph and Symbolic Solvers**
2608.26836v1 by Haizhao Fan, Yuchi Xiong, Jize Wang, Xinping Guan, Xinyi Le

Large Language Models (LLMs) have demonstrated remarkable proficiency in natural language understanding, yet they struggle with strict multi-step reasoning, frequently suffering from hallucinations and inconsistency. Existing solutions like Chain-of-Thought (CoT) lack rigorous verification mechanisms, while standard Retrieval-Augmented Generation (RAG) often misses the complex, structural dependencies inherent in logical tasks. To bridge this gap, we propose a Neuro-Symbolic architecture that integrates a Logical Knowledge Graph (LKG) with dynamic solver routing. Specifically, we introduce an ontology-based LKG that treats logical rules and constraints as first-class topological nodes, enabling explicit modeling of dependencies extracted from text. We further design a Logic Router to dynamically dispatch tasks to the optimal symbolic engine, which is supported by a topology-aware hybrid retrieval mechanism. Experimental results on logical reasoning benchmarks demonstrate that our framework significantly outperforms state-of-the-art prompting and RAG baselines, delivering higher accuracy and verifiable reasoning paths.

摘要：大型語言模型（LLMs）在自然語言理解方面表現出卓越的能力，然而它們在嚴格的多步推理上卻面臨挑戰，經常出現幻覺和不一致的情況。現有的解決方案如思維鏈（CoT）缺乏嚴謹的驗證機制，而標準的檢索增強生成（RAG）則常常忽略邏輯任務中固有的複雜結構依賴。為了彌補這一差距，我們提出了一種神經符號架構，將邏輯知識圖（LKG）與動態求解器路由相結合。具體而言，我們引入了一種基於本體的LKG，將邏輯規則和約束視為一級拓撲節點，從而能夠明確建模從文本中提取的依賴關係。我們進一步設計了一個邏輯路由器，能夠動態地將任務分配給最佳的符號引擎，該引擎由一種拓撲感知的混合檢索機制支持。在邏輯推理基準上的實驗結果顯示，我們的框架顯著超越了最先進的提示和RAG基準，提供了更高的準確性和可驗證的推理路徑。

##### **How Do Linear Probes Emerge? A Circuit-Tracing Framework with Concept-Targeted Attribution**
2608.27510v1 by Vedant Palit, Florent Draye, Terry Jingchen Zhang, Bernhard Schölkopf, Zhijing Jin

Transcoder attribution graphs are usually trained to explain why a model assigns high probability to a particular next token. We introduce Concept-Targeted Attribution (CTA), which instead trains attribution graphs with respect to a linear probe direction. CTA therefore yields probe-specific circuits that explain why an internal concept representation arises in a prompt, independently of whether it is expressed in the generated token. Using Cross-Layer Transcoders, we show that these probe-targeted graphs contain predictive structure: graph-level features predict probe accuracy across four widely studied concept categories ($ρ= 0.91$, $R^2 = 0.84$), while local features identify the sparse components driving per-prompt classification. This connects probe performance to interpretable circuit structure, allowing us to ask not only whether a probe works, but which internal computations make it work. Causal ablations further show that probe-targeted and logit-targeted graphs capture functionally distinct mechanisms. Removing probe-relevant features reduces internal concept scores while largely preserving generated tokens, whereas removing logit-relevant features changes the generated token in 92% to 100% of cases with near-zero effect on probe scores. CTA provides a framework for moving from behavioral probe accuracy to mechanistic explanations of probe performance, enabling more detailed audits of internal concept representations, including safety-critical ones. Our code is available at https://github.com/vedantpalit/concept-targeted-attribution

摘要：轉碼器歸因圖通常是為了解釋為什麼模型對特定的下一個標記分配高概率而訓練的。我們引入了概念目標歸因（CTA），它則是針對線性探針方向訓練歸因圖。因此，CTA 產生了特定於探針的電路，解釋了為什麼內部概念表示在提示中出現，而不管它是否在生成的標記中表達出來。使用跨層轉碼器，我們展示了這些針對探針的圖包含預測結構：圖層級特徵預測四個廣泛研究的概念類別中的探針準確性（$ρ= 0.91$, $R^2 = 0.84$），而局部特徵則識別出驅動每個提示分類的稀疏組件。這將探針性能與可解釋的電路結構聯繫起來，使我們不僅可以詢問探針是否有效，還可以詢問哪些內部計算使其有效。因果消融進一步顯示，針對探針和針對邏輯的圖捕捉到功能上不同的機制。移除與探針相關的特徵會降低內部概念分數，同時在很大程度上保留生成的標記，而移除與邏輯相關的特徵則在 92% 到 100% 的情況下改變生成的標記，對探針分數幾乎沒有影響。CTA 提供了一個框架，將行為探針準確性轉變為探針性能的機制解釋，使我們能夠對內部概念表示進行更詳細的審核，包括安全關鍵的概念。我們的代碼可在 https://github.com/vedantpalit/concept-targeted-attribution 獲得。

##### **AI Control Scientist: LLM-driven Agentic System for Automated Control Design**
2608.26780v1 by Haiteng Wang, Weihao Li, Jing Zhang, Lei Ren

Control system design is critical for modern industry, such as chemical process temperature regulation and aero-engine control. However,traditional control design workflows rely heavily on expert knowledge and extensive manual parameter tuning, resulting in limited efficiency and scalability. To this end, this paper proposes AI Control Scientist (AICS), the first large language model (LLM)-driven agent capable of automatically generating optimized controller from language design requirements. Specifically, a Task Modeling Agent interprets user requirements to engineering constraints; a Controller Design Agent generate candidate controller structures and executable code; and a Parameter Tuning Agent refine controller parameters under closed-loop performance criteria. Experiments demonstrate that the proposed agentic system can automatically generate multiple representative control systems, outperforms existing automated baselines in both design success rate and optimization efficiency. This work has the potential to transform control system design from human-driven to agent-driven, paving the way for model predictive control and other advanced control systems design.

摘要：控制系統設計對於現代工業至關重要，例如化學過程的溫度調節和航空發動機控制。<lb/>然而，傳統的控制設計工作流程在很大程度上依賴於專家知識和廣泛的手動參數調整，導致效率和可擴展性有限。<lb/>為此，本文提出了AI控制科學家（AICS），這是第一個基於大型語言模型（LLM）的代理，能夠根據語言設計需求自動生成優化的控制器。<lb/>具體而言，任務建模代理將用戶需求解釋為工程約束；控制器設計代理生成候選控制器結構和可執行代碼；參數調整代理在閉環性能標準下細化控制器參數。<lb/>實驗表明，所提出的代理系統可以自動生成多個代表性的控制系統，在設計成功率和優化效率方面超越現有的自動化基準。<lb/>這項工作有潛力將控制系統設計從人驅動轉變為代理驅動，為模型預測控制和其他先進控制系統設計鋪平道路。

##### **Graph-Guided Selective Unlearning for Language Models: Controlling Support Routes Beyond Forget Seeds**
2608.26743v1 by Waqas Khan, Tabinda Sarwar, Jingyue Cong, Xun Yi, Estrid He

Enterprises fine-tune language models on proprietary data that may later require removal due to privacy, contractual, or compliance obligations. Selective unlearning removes requested knowledge while preserving model utility, offering a practical alternative to full retraining, but existing methods treat the explicitly identified forget examples as the complete deletion scope. This is insufficient when target knowledge remains recoverable through paraphrases, aliases, or neighboring training examples. We propose GRAPHSU, a graph-guided controller that expands the deletion scope beyond forget seeds by constructing a weighted support-route graph, propagating deletion pressure through it, and applying graded forgetting strengths to high-risk neighbors. On the Task of Fictitious Unlearning (TOFU), a synthetic author-profile question-answering benchmark, and PISTOL, a structural-unlearning benchmark built around interconnected factual samples, with GPT-2 Medium and Llama-3.2-3B-Instruct, GRAPHSU achieves the lowest utility-feasible soft leakage across all deletion settings, reducing leakage by up to 49.5 percentage points over a matched seed-only baseline, demonstrating that effective enterprise unlearning requires controlling support routes, not just forget seeds.

摘要：企業在專有數據上微調語言模型，這些數據可能因隱私、合約或合規義務而需要移除。<lb/>選擇性遺忘移除請求的知識，同時保留模型的實用性，提供了一種比完全重訓練更實際的替代方案，但現有方法將明確識別的遺忘範例視為完全刪除範圍。<lb/>當目標知識仍然可以通過同義詞、別名或相鄰的訓練範例恢復時，這是不夠的。<lb/>我們提出了GRAPHSU，一種圖引導的控制器，通過構建加權支持路徑圖，擴展刪除範圍超越遺忘種子，並通過它傳播刪除壓力，對高風險鄰居應用分級遺忘強度。<lb/>在虛構遺忘任務（TOFU）這一合成作者檔案問答基準，以及圍繞互聯事實樣本構建的結構性遺忘基準PISTOL中，使用GPT-2 Medium和Llama-3.2-3B-Instruct，GRAPHSU在所有刪除設置中實現了最低的實用性可行軟洩漏，與匹配的僅種子基準相比，減少了多達49.5個百分點的洩漏，證明有效的企業遺忘需要控制支持路徑，而不僅僅是遺忘種子。

##### **The Thousand-Graph Hypothesis: A Testable Hypothesis of Task-Conditioned Relation Materialization in Repository-Level Code Reasoning**
2608.26602v1 by Fei Ding

Large software repositories are often beyond model context limits. Training repository knowledge into models is costly and quickly stale, while local retrieval can miss scattered requirements, and explicit relation graphs add ongoing maintenance burden. We propose an entity-only external interface with task-conditioned relation materialization during inference. A two-layer index separates global routing from local entity focus and is evaluated on DeepSeek-V4-Flash and SWE-bench Verified. The base, one-layer, and two-layer conditions achieve 92.1%, 94.2%, and 95.6% success, respectively, under zero pre-built entity-relation edges.

摘要：大型軟體庫通常超出模型的上下文限制。將庫知識訓練到模型中成本高昂且迅速過時，而本地檢索可能會錯過分散的需求，顯式關係圖則增加了持續的維護負擔。我們提出了一種僅基於實體的外部介面，並在推理過程中進行任務條件的關係具現化。一個兩層索引將全局路由與本地實體焦點分開，並在 DeepSeek-V4-Flash 和 SWE-bench Verified 上進行評估。在零預構建實體-關係邊的情況下，基礎、一層和兩層條件的成功率分別達到 92.1%、94.2% 和 95.6%。

##### **Surgical Alignment in Knowledge Graph Training for Clinical Diagnosis with Large Language Models**
2608.26587v2 by Saksham Khatwani, He Cheng, Majid Afshar, Dmitriy Dligach, Yanjun Gao

Biomedical knowledge graphs (KGs) offer structured medical knowledge that can ground large language model (LLM) reasoning in clinical diagnosis application, yet how KG signal should be integrated into LLMs remains an open question. We present a systematic study spanning five KG task formulations, three training paradigms, two KGs, and three base LLMs. At the task level, all paradigms improve over the non-finetuned baseline, but methods with comparable in-domain accuracy show substantially different knowledge transfer behavior. We introduce Gradient Intervention Density (GID) and Gradient Distortion (GD) to measure how broadly an optimizer modifies the pretrained model. GID and GD together reveal a clear divide: KG-judgment training under KL regularization produces sparse, localized updates (a regime we term as surgical alignment), while task-specific SFT produces dense ones. A controlled ablation shows that the objective and KL contribute to sparsity independently, and the paradigms that produce sparse updates also improve reasoning quality, even when their in-domain accuracy is lower than task-specific SFT. Assessing KG-LLM integration thus requires complementing accuracy with optimization-geometry diagnostics. Our implementation can be found at https://github.com/LARK-NLP-Lab/Surgical-Alignment.

摘要：生物醫學知識圖譜 (KGs) 提供結構化的醫學知識，可以將大型語言模型 (LLM) 的推理基礎置於臨床診斷應用中，但如何將 KG 信號整合進 LLM 仍然是一個未解的問題。<lb/>我們提出了一項系統性的研究，涵蓋五種 KG 任務表述、三種訓練範式、兩個 KGs 和三個基礎 LLMs。<lb/>在任務層面，所有範式都優於未微調的基線，但具有可比的領域內準確性的方法顯示出截然不同的知識轉移行為。<lb/>我們引入了梯度干預密度 (GID) 和梯度扭曲 (GD) 來衡量優化器如何廣泛地修改預訓練模型。<lb/>GID 和 GD 一起揭示了一個明確的區別：在 KL 正則化下的 KG 判斷訓練產生稀疏的、局部的更新（我們稱之為外科對齊的範疇），而任務特定的 SFT 則產生密集的更新。<lb/>一項受控的消融實驗顯示，目標和 KL 獨立地促成了稀疏性，而產生稀疏更新的範式也提高了推理質量，即使它們的領域內準確性低於任務特定的 SFT。<lb/>因此，評估 KG-LLM 整合需要用優化幾何診斷來補充準確性。<lb/>我們的實現可以在 https://github.com/LARK-NLP-Lab/Surgical-Alignment 找到。

##### **SPEAR: Distilling Domain-Adaptive Reasoning Skeletons via Sequential Symbolic Alignment in Reinforcement Learning**
2608.26550v1 by Zhuochun Li, Yuelyu Ji, Yiming Zeng, Daqing He

Reinforcement learning-based knowledge distillation has the potential to transfer complex reasoning from teacher to student models, yet it currently faces a critical dilemma: researchers must choose between sparse outcome-based rewards, which provide insufficient logical guidance, or expensive neural Process Reward Models (PRMs) for dense signals. We resolve this by introducing SPEAR (Symbolic Process Evaluation and Alignment Reward), a training-free and plug-and-play process reward method for sequence-level on-policy distillation. SPEAR projects natural-language reasoning traces into domain-adaptive symbolic milestones, providing an efficient proxy for process-level reasoning alignment. By utilizing the longest common subsequence (LCS) to align student explorations with teacher milestones, SPEAR provides a dense, order-aware reward signal that enforces logical consistency without the need for an external neural verifier. Our experiments across math, science, and commonsense reasoning tasks demonstrate that SPEAR effectively bridges the reasoning gap between student and teacher models via sequence-level distillation with efficient dense process rewards. Our code and data are available at: https://github.com/zhuochunli/SPEAR.

摘要：基於強化學習的知識蒸餾有潛力將複雜的推理從教師模型轉移到學生模型，然而目前面臨一個關鍵的困境：研究人員必須在稀疏的基於結果的獎勵（這些獎勵提供不足的邏輯指導）和昂貴的神經過程獎勵模型（PRMs，提供密集信號）之間做出選擇。<lb/>我們通過引入SPEAR（符號過程評估與對齊獎勵）來解決這個問題，這是一種無需訓練且即插即用的過程獎勵方法，適用於序列級的策略蒸餾。<lb/>SPEAR將自然語言推理痕跡投影到領域自適應的符號里程碑，提供了一個高效的過程級推理對齊代理。<lb/>通過利用最長公共子序列（LCS）將學生的探索與教師的里程碑對齊，SPEAR提供了一個密集的、考慮順序的獎勵信號，強化邏輯一致性，而無需外部神經驗證器。<lb/>我們在數學、科學和常識推理任務中的實驗表明，SPEAR有效地縮小了學生模型與教師模型之間的推理差距，通過序列級的蒸餾和高效的密集過程獎勵。<lb/>我們的代碼和數據可在以下網址獲得：https://github.com/zhuochunli/SPEAR。

##### **Multi2AV-Safety: Benchmarking Safety in Multimodal-to-Audio-Video Generation**
2608.26535v1 by Kaichao Jiang, Changtao Miao, Baiqi Wu, Zhiyuan Lu, Kang Yang, Peiwei Zhao, Junchi Chen, Yunfeng Diao, He Liu, Qi Chu, Tao Gong, Nenghai Yu

Audio-video generation is rapidly moving from prompt-driven synthesis toward multimodal conditioning, where text, images, audio, and video can jointly shape the generated output. This shift changes the nature of safety evaluation: harmful intent may no longer reside in any single input, but instead emerge from how otherwise benign or weakly harmful conditions interact across modalities and time. Existing safety benchmarks, however, remain largely prompt-centric or tied to fixed conditioning interfaces, leaving such compositional risks difficult to study systematically. To bridge this gap, we introduce Multi2AV-Safety, the first safety benchmark, to the best of our knowledge, to cover all 11 non-singleton T/I/A/V conditioning configurations for audio-video generation, comprising 11,024 attack instances. Evaluation on Multi2AV-Safety reveals systematic weaknesses in representative multimodal safety guards across attack mechanisms and harm-evidence structures. Our evaluation reveals two complementary failure modes: harmful semantics can emerge from the combination of individually benign inputs, while explicit harmful cues can become harder to detect when mixed with benign multimodal context. Together, these results identify \emph{compositional risk perception} as a central capability gap in safeguarding multimodal-conditioned audio-video generation: current safety guards fail to reliably integrate safety evidence across modalities and time, even when all conditioning inputs are observable. The dataset will be publicly released in October 2026.

摘要：音頻視頻生成正在迅速從基於提示的合成轉向多模態條件，其中文本、圖像、音頻和視頻可以共同塑造生成的輸出。這一轉變改變了安全評估的性質：有害意圖可能不再存在於任何單一輸入中，而是從其他無害或輕微有害條件在模態和時間上的互動中產生。然而，現有的安全基準仍然主要以提示為中心或與固定的條件界面相關，這使得這種組合風險難以系統性地研究。為了填補這一空白，我們介紹了Multi2AV-Safety，據我們所知，這是第一個涵蓋所有11種非單一T/I/A/V條件配置的音頻視頻生成安全基準，共包括11,024個攻擊實例。在Multi2AV-Safety上的評估揭示了代表性多模態安全防護在攻擊機制和傷害證據結構中的系統性弱點。我們的評估揭示了兩種互補的失效模式：有害語義可以從個別無害輸入的組合中產生，而當與無害的多模態上下文混合時，明確的有害線索可能變得更難檢測。這些結果共同確定了\emph{組合風險感知}作為保護多模態條件音頻視頻生成的一個核心能力缺口：當所有條件輸入都是可觀察時，當前的安全防護無法可靠地整合跨模態和時間的安全證據。該數據集將於2026年10月公開發布。

##### **Redwood: A Frontier AI Accelerator Designed, Verified, and Deployed from Scratch in 2 Weeks by AI**
2608.26418v2 by Architect Labs

Modern AI workloads and the hardware that runs them evolve on different timescales: architectural definition precedes volume silicon by years, while target workloads shift in months. Design decisions are therefore committed under deep uncertainty and paid for twice, once in the generality added as a hedge, and again when new workloads map poorly onto frozen silicon. As Moore's Law stagnates, specialization is the main remaining source of performance-per-watt and demands a design cycle that runs at the cadence of the workloads. We present an end-to-end AI system that collapses the software-to-silicon stack into a single optimization loop, where hardware and software are co-designed and verified under one objective. Its first demonstration is Redwood, a frontier AI accelerator built for single-batch, low-power, ultra-low-latency inference for physical AI. From a high-level specification by two human architects, the system autonomously generated the performance model, RTL design, UVM environments, formal proofs, firmware, and kernels in under two weeks with no human intervention below the specification. Every block reached 95% coverage via commercial EDA tools, our proprietary formal engine, and hardware-in-the-loop validation. Specification changes were reverified and redeployed to hardware in under 48 hours. Redwood Nano, its ultra-low-power FPGA variant, runs multi-billion-parameter models like Llama and Qwen. Projected onto Samsung 8 nm, the Jetson Orin Nano's process class, Redwood delivers 1.75x the throughput at 1.9x lower power, a 3.4x performance-per-watt gain against a measured Jetson baseline on the same models. Qwen running on Redwood also helped design next-generation Redwood, an early step toward recursive self-improvement. To our knowledge, this is the first production-worthy AI accelerator designed end-to-end by an AI system and running a modern AI model.

摘要：現代的人工智慧工作負載及其運行的硬體在不同的時間尺度上演變：架構定義比量產矽片早幾年，而目標工作負載則在幾個月內發生變化。因此，設計決策是在深度不確定性下做出的，並且付出了兩次代價，一次是作為對沖而增加的一般性，另一次是在新的工作負載與凍結的矽片不匹配時。隨著摩爾定律的停滯，專業化成為性能每瓦的主要來源，並要求設計週期與工作負載的節奏相符。我們提出了一個端到端的人工智慧系統，將軟體到矽片的堆疊壓縮為一個單一的優化迴圈，在這裡硬體和軟體在一個目標下共同設計和驗證。其首次演示是Redwood，一個為物理人工智慧設計的單批次、低功耗、超低延遲推理的前沿AI加速器。從兩位人類架構師的高階規範出發，該系統在不到兩週的時間內自主生成了性能模型、RTL設計、UVM環境、形式證明、韌體和內核，且在規範以下沒有任何人為干預。每個模塊通過商業EDA工具、我們的專有形式引擎和硬體在迴路驗證達到了95%的覆蓋率。規範變更在48小時內重新驗證並重新部署到硬體上。Redwood Nano，其超低功耗的FPGA變體，運行像Llama和Qwen這樣的多十億參數模型。在三星8納米、Jetson Orin Nano的製程類別上，Redwood在1.9倍更低功耗的情況下提供了1.75倍的吞吐量，對於在相同模型上測得的Jetson基準，性能每瓦增益達到3.4倍。在Redwood上運行的Qwen也幫助設計了下一代Redwood，這是邁向遞歸自我改進的早期步驟。據我們所知，這是第一個由人工智慧系統端到端設計並運行現代人工智慧模型的具生產價值的AI加速器。

##### **Co-Evolving Structured Knowledge and Reasoning in Language Models**
2608.26386v1 by Ryan Thomas Noonan, Linxi Zhao, Menghan Xu, Akanksha Sarkar, Mihir Mishra, Dongyoung Go, Kilian Q. Weinberger, Yoav Artzi, Jennifer J. Sun

Retrieval-augmented methods improve factual accuracy by grounding language models in external knowledge, but retrieving over unstructured text often introduces irrelevant context and offers limited control over the retrieved information. Structured knowledge bases offer a more controllable alternative, yet they are expensive to construct and often brittle to reason over. To address these limitations, we propose KBevo: a co-evolving framework that jointly learns to construct a structured knowledge base and reason over it for knowledge-intensive question answering. By optimizing both components end-to-end with QA outcome rewards, our method enables reasoning success to directly improve the quality of the constructed knowledge base. This leads to larger, better-connected knowledge structures with higher answer reachability, while also improving compositional factual reasoning and controllability compared to standard retrieval baselines.

摘要：檢索增強方法透過將語言模型與外部知識結合來提高事實準確性，但在非結構化文本中檢索往往會引入不相關的上下文，並且對檢索到的信息提供有限的控制。結構化知識庫提供了一種更可控的替代方案，但構建成本高昂，且在推理時往往脆弱。為了解決這些限制，我們提出了KBevo：一個共同演化的框架，能夠共同學習構建結構化知識庫並對其進行推理，以進行知識密集型的問題回答。通過使用問答結果獎勵對兩個組件進行端到端的優化，我們的方法使得推理成功能夠直接改善構建的知識庫的質量。這導致了更大、更好連接的知識結構，並提高了答案的可達性，同時相比於標準檢索基準，也改善了組合事實推理和可控性。

##### **Why RAGs Hallucinate: Penalty-Aware Evaluation of Retrieval-Augmented Generation Systems with Knowledge-Gap Canaries**
2608.26385v1 by Alden Do Rosario, Hussein Younes, Felipe Pires

Volume-based accuracy rewards retrieval-augmented generation (RAG) systems for guessing: a system that answers everything outscores one that declines when its knowledge base cannot support an answer. Building on the confidence-target analysis of Kalai et al. (2025), we present a penalty-aware evaluation framework for deployed RAG products, combining (i) asymmetric scoring (correct +1, wrong -4, abstain 0), (ii) knowledge-gap canaries, questions whose answers are verifiably absent from the knowledge base, so that any answer constitutes ungrounded generation from parametric memory, and (iii) a failure-attribution pipeline that separates retrieval, generation, and abstention-policy failures. Applying the framework to three commercial RAG systems and a no-retrieval baseline on SimpleQA-Verified (1,000 questions x 3 repeats, graded blind by a cross-family three-judge panel with 98.9% unanimity), we find that accuracy when answering is closely clustered across systems (97.0-98.0%), while canary violation rates differ roughly sixfold (16.7% vs. 98.1%). The systems are separated less by what they answer correctly than by whether they answer at all when they should not, and penalty-aware scoring reorders the volume-based ranking accordingly; the reordering is stable across penalty settings from k=1 to k=9. All code, configurations, transcripts, and judge votes are released for independent audit.

摘要：基於體積的準確性獎勵檢索增強生成（RAG）系統的猜測：一個能夠回答所有問題的系統，其得分高於在知識基礎無法支持答案時選擇拒絕的系統。基於Kalai等人（2025）的信心目標分析，我們提出了一個針對已部署RAG產品的懲罰感知評估框架，結合了（i）不對稱評分（正確 +1，錯誤 -4，放棄 0），（ii）知識缺口警示，這些問題的答案在知識基礎中可驗證地缺失，因此任何答案都構成來自參數記憶的無根據生成，以及（iii）一個失敗歸因管道，將檢索、生成和放棄政策的失敗分開。將該框架應用於三個商業RAG系統和一個無檢索基線，基於SimpleQA-Verified（1,000個問題 x 3次重複，由跨家族的三名評審小組以98.9%的一致性進行盲評），我們發現回答時的準確性在系統之間緊密聚集（97.0-98.0%），而警示違規率則大約相差六倍（16.7%對98.1%）。這些系統的區別不在於它們正確回答的內容，而在於當它們不應該回答時是否回答，而懲罰感知評分相應地重新排序基於體積的排名；這一重新排序在懲罰設置從k=1到k=9之間是穩定的。所有代碼、配置、記錄和評審投票均已公開，以便進行獨立審計。

##### **Knowledge-Verified Emergent Deception in LLM Agents Under Conflicting Incentives**
2608.26372v1 by Zheyuan Liu, Weiliang Zhao, Xiangchi Yuan, Ningshan Ma, Yue Huang, Meng Jiang

Large language models are increasingly deployed as autonomous agents serving users on behalf of companies, placing them in settings where user and deployer interests can conflict. When an agent knows that a user is owed something its deployer would prefer to deny, does it remain honest? Answering this is difficult because false statements can reflect either ignorance or hallucination rather than deception. To address this challenge, we introduce KnownLieBench , a knowledge-verified benchmark that first confirms through a neutral probe that an agent knows a user's entitlement, and then evaluates whether it makes false claims once an incentive to deny that entitlement is introduced. Specifically, KnownLieBench covers eight customer-service domains and 112 grounded cases, conducts multi-round dialogues with a trust-tracking customer agent, and separates deception emerging from incentive alone from deception produced under explicit instruction. Across eighteen proprietary and open-weight models, emergent deception varies substantially across model families and domains. We further use the benchmark for post-training, finding that honesty-directed fine-tuning reduces deception under incentive, while deception-graded fine-tuning increases lie success on honest-control dialogues without increasing lie frequency under incentive. By verifying entitlement knowledge before scoring deceptive behavior, KnownLieBench reduces the confound between lying and not knowing and enables more rigorous auditing and steering of agent honesty.

摘要：大型語言模型越來越多地被部署為自主代理，代表公司為用戶提供服務，這使它們置身於用戶和部署者利益可能發生衝突的環境中。當一個代理知道用戶應得某些其部署者希望拒絕的東西時，它會保持誠實嗎？回答這個問題很困難，因為虛假陳述可能反映無知或幻覺，而非欺騙。為了解決這一挑戰，我們引入了 KnownLieBench，一個知識驗證基準，首先通過中立探測確認代理知道用戶的權益，然後評估在引入拒絕該權益的動機後，是否會做出虛假聲明。具體而言，KnownLieBench 涵蓋八個客戶服務領域和112個具體案例，與一個信任追蹤的客戶代理進行多輪對話，並將僅由動機產生的欺騙與在明確指令下產生的欺騙區分開來。在十八個專有和開放權重模型中，出現的欺騙在模型家族和領域之間差異顯著。我們進一步利用該基準進行後訓練，發現以誠實為導向的微調在動機下減少了欺騙，而以欺騙為評級的微調則在誠實控制對話中增加了謊言成功率，同時在動機下不增加謊言頻率。通過在評分欺騙行為之前驗證權益知識，KnownLieBench 減少了欺騙與無知之間的混淆，並使代理誠實的審計和引導更加嚴謹。

##### **Cross-lingual Representation Learning via Centroid Intervention Fusion**
2608.26357v1 by Wei Sun, Marie-Francine Moens

Large language models (LLMs) exhibit uneven multilingual performance, especially when dealing with low-resource languages. Inference-time intervention offers a lightweight way to improve cross-lingual transfer by modifying the hidden states produced by the LLMs during the forward pass, without updating model parameters. However, existing cross-lingual intervention methods typically learn separate projections from source to target languages, which limits scalability and prevents knowledge sharing across languages. We propose Centroid Intervention Fusion (CIF), a projection fusion framework that consolidates multiple multilingual intervention projections into a single language-shared operator. Across multilingual commonsense reasoning, natural language inference, factual editing, and machine translation benchmarks, CIF outperforms the strongest prior pairwise intervention baseline by up to +3.378 pp on average across four model backbones, while supporting performance gains for low resource languages. The code is available at https://github.com/VRCMF/CIF.git.

摘要：大型語言模型 (LLMs) 在多語言表現上存在不均衡，特別是在處理低資源語言時。推理時間介入提供了一種輕量級的方法，通過修改 LLM 在前向傳遞過程中產生的隱藏狀態來改善跨語言轉移，而無需更新模型參數。然而，現有的跨語言介入方法通常從源語言到目標語言學習獨立的投影，這限制了可擴展性並阻礙了跨語言的知識共享。我們提出了中心介入融合 (CIF)，這是一個投影融合框架，將多個多語言介入投影整合為一個共享語言的操作符。在多語言常識推理、自然語言推理、事實編輯和機器翻譯基準測試中，CIF 在四個模型骨幹上平均超越了最強的先前成對介入基線達 +3.378 pp，同時支持低資源語言的性能提升。代碼可在 https://github.com/VRCMF/CIF.git 獲得。

##### **ProofEvolve: Neuro-Symbolic Evolution for Formal Automated Theorem Proving**
2608.26334v1 by Wenqian Ye, Ziwei Guan, Eric Xie, Bohan Liu, Shivani Modi, Buyun Zhang, Ellie Dingqiao Wen, Henry Kautz, Aidong Zhang

Automated theorem proving offers a natural foundation for recursive self-improvement in scientific discovery. However, existing neural provers do not fully preserve this recursive structure, where the learning process should be self-improving over time. Existing methods either embed proof experience into model parameters through expensive weight updates, or keep verified intermediate deductions only within the current problem. In addition, these methods also heavily rely on sparse whole-proof feedback, even when unsuccessful partial attempts contain useful discoveries. To close the gap, we propose ProofEvolve, a neuro-symbolic framework that evolves explicit, formally verified symbolic proof structures with neural models to decisively expand the knowledge boundary. In this framework, the neural model proposes variation operators, including decompositions, repairs, and schema recombinations. The symbolic Lean kernel verifies every proof transition. Over the evolution loops, ProofEvolve computes verified closure over the resulting proof directed acyclic graphs (DAGs). Within each problem, ProofEvolve evolves partial AND-OR proof DAGs in a behaviorally indexed archive. Across problems, kernel-checked schema extraction adds newly proved sub-DAGs to a persistent schema library. Proof DAGs inherit the solved results through typed schema recombination, with every residual premise exposed as a new subgoal. This evolutionary process preserves verified results from incomplete attempts and makes them available for later proofs without weakening formal soundness. Across three competition-level Lean benchmarks, ProofEvolve achieves the highest average solve rate among the evaluated proof systems.

摘要：自動定理證明為科學發現中的遞歸自我改進提供了自然的基礎。<lb/>然而，現有的神經證明器並未完全保留這一遞歸結構，學習過程應該隨著時間自我改進。<lb/>現有的方法要麼通過昂貴的權重更新將證明經驗嵌入模型參數中，要麼僅在當前問題中保留經過驗證的中間推導。<lb/>此外，這些方法也在很大程度上依賴稀疏的整體證明反饋，即使不成功的部分嘗試中也包含有用的發現。<lb/>為了縮小這一差距，我們提出了ProofEvolve，一個神經符號框架，通過神經模型發展明確的、形式上經過驗證的符號證明結構，以果斷擴展知識邊界。<lb/>在這個框架中，神經模型提出變化運算子，包括分解、修復和模式重組。<lb/>符號Lean內核驗證每一個證明過渡。<lb/>在演化循環中，ProofEvolve計算結果證明有向無環圖（DAG）的驗證閉合。<lb/>在每個問題中，ProofEvolve在行為索引存檔中演化部分AND-OR證明DAG。<lb/>在不同問題之間，內核檢查的模式提取將新證明的子-DAG添加到持久的模式庫中。<lb/>證明DAG通過類型模式重組繼承已解決的結果，並將每個剩餘前提暴露為新的子目標。<lb/>這一演化過程保留了不完全嘗試的驗證結果，並使其在後續證明中可用，而不削弱形式的健全性。<lb/>在三個競賽級Lean基準測試中，ProofEvolve在評估的證明系統中實現了最高的平均解決率。

##### **On Scope Classification and Current Knowledge-Editing Benchmarks: A Negative Result, with INLAY as a Gradient-Free Case Study**
2608.26292v1 by Aditya Pratap Singh

Every memory-based knowledge editor in the SERAC lineage depends on a scope decision: given a query, does a stored edit apply? We report that current knowledge-editing benchmarks cannot measure this decision at all. Using INLAY, a gradient-free editor we built to obtain exact per-query ground truth (the model is frozen, edits live in an external addressable memory, and applying an edit is a bias added along one token's unembedding direction at decode time), we execute every candidate router action on 1,689 queries spanning three datasets and three input conditions. An oracle router choosing the best action every time ties a one-line static policy to four decimal places in all nine dataset-by-condition cells: the maximum attainable gain of any per-query routing method is 0.00 points. Abstention is the sole winning action zero times out of 1,689. The cause is structural: these are counterfactual benchmarks whose evaluation question asks for the post-edit answer, so answering from parametric knowledge is wrong by construction, and a benchmark without negatives cannot reward a classifier's ability to reject. This generalizes beyond our system to the whole scope-classifier family the benchmarks are used to evaluate. We confirm the mechanism directly: constructing the missing condition ourselves, by withholding a query's own edit from the index for half the sample, moves pooled headroom from exactly +0.0000 to +0.0420 and gives abstention its first wins. We also report where INLAY itself does not win (WISE beats it on Qwen2.5-7B CounterFact, and retrieval-augmented generation beats every method we tested, INLAY included, on rigorously matched RippleEdits), and disclose two bugs found during a self-audit of our own routing machinery, neither of which changed a published headline number outside noise.

摘要：每個基於記憶的知識編輯器在 SERAC 系列中都依賴於範圍決策：給定一個查詢，存儲的編輯是否適用？我們報告目前的知識編輯基準無法測量這個決策。使用 INLAY，我們建造了一個無梯度編輯器，以獲得每個查詢的精確真實值（模型是靜止的，編輯存在於外部可尋址的記憶中，並且在解碼時應用編輯是在一個標記的去嵌入方向上添加的偏差），我們對 1,689 個查詢執行每個候選路由器的操作，這些查詢跨越三個數據集和三個輸入條件。一個每次選擇最佳操作的神諭路由器將一個靜態政策綁定到所有九個數據集-條件單元的四位小數：任何每查詢路由方法的最大可達增益為 0.00 分。棄權是 1,689 次中唯一一次獲勝的行動。原因是結構性的：這些是反事實基準，其評估問題要求後編輯答案，因此從參數知識回答是錯誤的，並且沒有負樣本的基準無法獎勵分類器的拒絕能力。這一點超出了我們的系統，推廣到用於評估的整個範圍分類器家族。我們直接確認了這一機制：通過將查詢自己的編輯從索引中扣留一半樣本，自行構建缺失的條件，將總體空間從恰好 +0.0000 轉移到 +0.0420，並使棄權首次獲勝。我們還報告了 INLAY 自身未獲勝的情況（WISE 在 Qwen2.5-7B CounterFact 上超越了它，而增強檢索生成在嚴格匹配的 RippleEdits 上超過了我們測試的每種方法，包括 INLAY），並披露了在我們自己的路由機械自我審計中發現的兩個錯誤，這兩個錯誤都沒有改變發佈的標題數字以外的噪音。

##### **MyoMechanix: Biomechanically-Grounded Compositional Skilled Activity Understanding and Coaching**
2608.26094v1 by Hao Yin, Paritosh Parmar, Lijun Gu, Lin Xu, Tianxiao Guo, Xiujin Liu, Tianyou Zheng, Yang Zhang, Weiwei Fu

Existing action quality assessment (AQA) datasets and methods rely primarily on visual inputs such as RGB and pose, overlooking physiological dynamics such as muscle mechanics and often modeling actions as monolithic patterns. These limitations hinder fine-grained, biomechanically grounded feedback. We introduce MyoMechanix, a multimodal ecosystem for weight-loaded actions that aligns motion with muscle activity. Expert-annotated, it contains 7,500+ samples of 20 actions from 38 subjects, with synchronized multiview RGB video, 3D pose, sEMG, and additional physiological signals, forming the largest multimodal AQA benchmark to date. We further construct the Fitness Knowledge Graph (FKG), which organizes expert annotations into structured relationships among actions, phases, key steps, errors, and corrective feedback, enabling compositional scoring and interpretable assessment. Building on these representations, we develop CUBIST (Compositional Ontological Reasoning Engine), which performs decomposition-analysis-recomposition for fine-grained error attribution and feedback generation. We also establish MyoMechanix-AQA, MyoMechanix-VideoQA, and a novel MyoMechanix-Video2EMG task. Experiments show that multimodal sensing and structured representations improve performance, interpretability, and error attribution, with CUBIST achieving state-of-the-art results; VideoQA enhances language-grounded action understanding; and Video2EMG suggests video-based alternatives to costly EMG sensing. MyoMechanix advances skilled activity understanding toward biomechanically grounded, multimodal, and compositional reasoning for Physical AI applications in fitness, rehabilitation, healthcare, and machine learning. Project page: https://haoyin116.github.io/MyoMechanix/

摘要：現有的動作品質評估 (AQA) 數據集和方法主要依賴於 RGB 和姿勢等視覺輸入，忽略了肌肉力學等生理動態，並且通常將動作建模為單一模式。這些限制妨礙了細緻的、生物力學基礎的反饋。我們引入了 MyoMechanix，一個針對負重動作的多模態生態系統，將運動與肌肉活動對齊。該系統由專家標註，包含來自 38 位受試者的 20 種動作的 7,500 多個樣本，並配有同步的多視角 RGB 視頻、3D 姿勢、sEMG 和其他生理信號，形成迄今為止最大的多模態 AQA 基準。我們進一步構建了健身知識圖譜 (FKG)，將專家標註組織成動作、階段、關鍵步驟、錯誤和修正反饋之間的結構化關係，實現組合計分和可解釋的評估。在這些表徵的基礎上，我們開發了 CUBIST (組合本體推理引擎)，它執行細緻的錯誤歸因和反饋生成的分解-分析-重組。我們還建立了 MyoMechanix-AQA、MyoMechanix-VideoQA 和一個新穎的 MyoMechanix-Video2EMG 任務。實驗顯示，多模態感知和結構化表徵提高了性能、可解釋性和錯誤歸因，CUBIST 實現了最先進的結果；VideoQA 增強了基於語言的動作理解；而 Video2EMG 則提出了基於視頻的替代方案來取代昂貴的 EMG 感知。MyoMechanix 推進了對熟練活動的理解，朝向生物力學基礎的、多模態的和組合推理，應用於健身、康復、醫療保健和機器學習的物理 AI。項目頁面：https://haoyin116.github.io/MyoMechanix/

##### **The Reasoning Tax: Token Economics of LLM Reasoning Across Task Types and Deployment Contexts**
2608.26235v1 by Sachin Gopal Wani, Ajay Dholakia, David Ellison

Accuracy-only benchmarking of reasoning-capable large language models misses a central deployment question: when do extended thinking tokens earn their cost? We introduce the Token Economy Score (TES), a marginal benchmarking metric that measures the accuracy gain of a reasoning model over a non-reasoning baseline, normalized by the generated-token multiplier. We define paired and approximated TES variants for model families with reasoning toggles and frontier models without direct non-reasoning counterparts. We then conduct an empirical benchmarking analysis across 151 model-benchmark evaluation runs on seven benchmarks spanning mathematics, code generation, science reasoning, instruction following, expert knowledge, knowledge recall, and research-level physics. The analysis examines three deployment-facing dimensions: which task structures yield positive marginal reasoning efficiency, how increasing reasoning effort changes TES within model families, and how deployment context changes economic viability. Results show that task structure predicts reasoning efficiency better than nominal difficulty: sequential inferencechain tasks such as AIME 2025 and LiveCodeBench show high TES, while knowledge-recall tasks such as MMLU-Pro show low TES despite their difficulty. We also find systematic diminishing returns at higher reasoning effort levels, including cases where additional thinking reduces accuracy. Finally, Reasoning Cost Share (RCS) shows that inference spend is often dominated by internal thinking, while Deployment Cost Multiplier (DCM) shows how on-premises deployment can change the economics of otherwise costly reasoning workloads. These findings support a benchmarking-driven model-selection rule: enable reasoning selectively by task type, effort level, and deployment context rather than treating it as a universally beneficial mode.

摘要：準確性僅基準的推理能力大型語言模型忽略了一個核心的部署問題：何時擴展思考令牌能夠賺回其成本？我們引入了令牌經濟分數（Token Economy Score, TES），這是一種邊際基準指標，用於衡量推理模型相對於非推理基準的準確性增益，並以生成令牌的倍數進行標準化。我們為具有推理切換的模型家族和沒有直接非推理對應的前沿模型定義了配對和近似的 TES 變體。然後，我們在七個基準上進行了151次模型基準評估運行的實證基準分析，這些基準涵蓋了數學、代碼生成、科學推理、指令跟隨、專家知識、知識回憶和研究級物理。該分析考察了三個面向部署的維度：哪些任務結構產生正的邊際推理效率、增加推理努力如何改變模型家族中的 TES，以及部署上下文如何改變經濟可行性。結果顯示，任務結構比名義難度更好地預測推理效率：如 AIME 2025 和 LiveCodeBench 等序列推理鏈任務顯示出高 TES，而如 MMLU-Pro 等知識回憶任務儘管難度較高卻顯示出低 TES。我們還發現，在更高的推理努力水平下，系統性地出現邊際收益遞減的情況，包括額外思考會降低準確性的情況。最後，推理成本份額（Reasoning Cost Share, RCS）顯示推理支出通常被內部思考所主導，而部署成本倍數（Deployment Cost Multiplier, DCM）則顯示本地部署如何改變原本昂貴的推理工作負載的經濟學。這些發現支持了一個基於基準的模型選擇規則：根據任務類型、努力水平和部署上下文選擇性地啟用推理，而不是將其視為一種普遍有益的模式。

##### **The Value of Human Expertise**
2608.26051v1 by Bradley Sturt

We consider optimization applications with unknown parameters where the decision maker believes that the optimal value of the nominal problem-the optimization problem they would have solved if the true parameters were known-is unlikely to be large. This belief derives from information that humans have that is not captured in datasets, obtained from domain knowledge and interacting with the physical world. We propose an approach to evaluating policies that provides tighter performance guarantees if the decision maker's belief happens to be correct. Our main result shows that if computing a policy's worst-case performance is a convex program, then the value of human expertise-the maximum improvement in performance guarantees that can be obtained from the belief about the nominal problem-is equal to the minimax gap of a max-min problem. We illustrate our developments in assortment optimization and shortest path problems.

摘要：我們考慮具有未知參數的優化應用，其中決策者認為名義問題的最佳值——如果已知真實參數，他們將解決的優化問題——不太可能很大。這種信念源於人類擁有的信息，這些信息在數據集中未被捕捉，來自於領域知識和與物理世界的互動。我們提出了一種評估政策的方法，如果決策者的信念恰好是正確的，則提供更緊湊的性能保證。我們的主要結果顯示，如果計算政策的最壞情況性能是一個凸規劃，那麼人類專業知識的價值——從對名義問題的信念中可以獲得的性能保證的最大改進——等於一個最大-最小問題的最小最大差距。我們在產品組合優化和最短路徑問題中說明了我們的發展。

##### **Multi-Granularity Context-Enhanced RAG over Multimodal Knowledge Graphs**
2608.25986v1 by Zongyu Wu, Yilong Wang, Xiaochen Wang, Minhua Lin, Zhichao Xu, Fenglong Ma, Xiang Zhang, Suhang Wang

Retrieval-augmented generation (RAG) is widely used to mitigate hallucination issues in large language models (LLMs) and multimodal large language models (MLLMs). In particular, knowledge graph (KG)-based RAG leverages structured knowledge to provide (M)LLMs with high-quality external information. Building on these works, recent studies have explored multimodal knowledge graphs (MMKGs) as knowledge bases for GraphRAG. This enables Graph RAG to integrate knowledge across multiple modalities, thereby further enhancing its performance. However, existing MMKG-based RAG methods generally follow a common pipeline in which different modalities are largely processed independently before being fusion. As a result, textual context is only used to a limited extent during visual information extraction and subsequent multimodal knowledge fusion. This brings a semantic gap between images and text which limits the multimodal GraphRAG performance. To address this issue, we propose a novel framework for constructing a Context-Enhanced MMKG (CEMMKG) to better support multimodal GraphRAG. The proposed CEMMKG enriches each image with complementary textual context at both local and global scopes. Local context goes beyond the surrounding text by incorporating sentences that are semantically related to the image, while global context provides a summary of the entire passage. We further introduce a multi-granularity design for the local context, allowing it to capture semantically relevant information at different levels of detail. Extensive experiments on the selected vision-centric dataset validate that CEMMKG is effective in leveraging contextual information to improve MMKG-based RAG performance. Moreover, its effectiveness across different MMKG-based RAG methods demonstrates its broad applicability.

摘要：檢索增強生成（RAG）被廣泛用於減輕大型語言模型（LLMs）和多模態大型語言模型（MLLMs）中的幻覺問題。特別是，基於知識圖譜（KG）的RAG利用結構化知識為（M）LLMs提供高質量的外部信息。在這些工作的基礎上，最近的研究探討了多模態知識圖譜（MMKGs）作為GraphRAG的知識庫。這使得Graph RAG能夠整合來自多個模態的知識，從而進一步提升其性能。然而，現有的基於MMKG的RAG方法通常遵循一個共同的流程，其中不同的模態在融合之前大多是獨立處理的。因此，在視覺信息提取和隨後的多模態知識融合過程中，文本上下文的使用僅限於有限的程度。這在圖像和文本之間帶來了語義差距，限制了多模態GraphRAG的性能。為了解決這一問題，我們提出了一個新框架來構建上下文增強的MMKG（CEMMKG），以更好地支持多模態GraphRAG。所提出的CEMMKG在局部和全局範疇內為每個圖像增添了互補的文本上下文。局部上下文超越了周圍文本，通過納入與圖像語義相關的句子來實現，而全局上下文則提供了整個段落的摘要。我們進一步引入了局部上下文的多粒度設計，使其能夠在不同的細節層次捕捉語義相關的信息。在所選擇的以視覺為中心的數據集上進行的廣泛實驗驗證了CEMMKG在利用上下文信息改善基於MMKG的RAG性能方面的有效性。此外，其在不同的基於MMKG的RAG方法中的有效性顯示了其廣泛的適用性。

##### **SciMIF: Understanding Multimodal Instruction Following in Scientific Domains**
2608.25973v1 by Ye Shen, Yuting Zheng, Dun Pei, Zijian Chen, Wenlong Zhang, Qi Jia, Guangtao Zhai

Understanding instruction-following capabilities in scientific domains is essential for effectively leveraging Multimodal Large Language Models (MLLMs) to advance the development of scientific fields. In this work, we introduce SciMIF, a novel benchmark designed to evaluate the capability of MLLMs in following complex scientific instructions. Specifically, based on an extensive analysis of 22 distinct tasks across 5 representative scientific disciplines, we propose a comprehensive taxonomy comprising 10 constraint groups that captures both general functional requirements and discipline-specific characteristics. Guided by this taxonomy, we develop a high-fidelity instruction injection pipeline to systematically augment existing scientific datasets. We conduct comprehensive experiments on multiple state-of-the-art closed-source and open-source MLLMs. Our findings reveal significant performance disparities across different scientific disciplines, with chemistry posing greater challenges for current MLLMs. Furthermore, we observe that increasing the model scale does not yield corresponding improvements in constraint adherence, and current models still struggle severely with fine-grained constraints and instructions requiring the deep application of disciplinary knowledge. SciMIF fills the current void in evaluating multimodal instruction adherence within scientific domains, laying a crucial foundation for future enhancements of MLLMs in rigorous scientific applications. Data and code will be released at https://github.com/shenye7436/SciMIF .

摘要：理解科學領域中的指令遵循能力對於有效利用多模態大型語言模型（MLLMs）以推進科學領域的發展至關重要。<lb/>在這項工作中，我們介紹了SciMIF，一個旨在評估MLLMs遵循複雜科學指令能力的新基準。<lb/>具體而言，基於對5個代表性科學學科中22個不同任務的廣泛分析，我們提出了一個包含10個約束組的綜合分類法，捕捉了通用功能需求和學科特定特徵。<lb/>在這一分類法的指導下，我們開發了一個高保真度的指令注入管道，以系統性地增強現有的科學數據集。<lb/>我們對多個最先進的封閉源和開放源MLLMs進行了全面實驗。<lb/>我們的發現揭示了不同科學學科之間顯著的性能差異，化學領域對當前的MLLMs提出了更大的挑戰。<lb/>此外，我們觀察到增加模型規模並未帶來相應的約束遵循改進，當前模型在細粒度約束和需要深入應用學科知識的指令上仍然面臨嚴重困難。<lb/>SciMIF填補了目前在科學領域中評估多模態指令遵循的空白，為未來在嚴謹科學應用中增強MLLMs奠定了重要基礎。<lb/>數據和代碼將在 https://github.com/shenye7436/SciMIF 上發布。

##### **LivingRAG: Augmenting Graph RAG with Experience**
2608.25960v1 by Yuzhuo Cui, Zongye Zhang, Qingjie Liu

Graph-based RAG improves multi-hop question answering by organizing evidence as a knowledge graph. However, most existing RAG systems process each query in isolation and discard useful reasoning from the LLM's response after inference. As a result, later related queries need to retrieve evidence and reason from scratch. We propose LivingRAG, a Graph RAG framework with writable and reusable reasoning experience. LivingRAG adds a writable experience store to a graph-based retrieval backbone, enabling verified experiences to be reused during inference in two ways. Stored graph signals help retrieval find entities and passages that were useful in earlier related queries. Stored summaries provide a reference reasoning pattern for answer generation. We analyze online QA streams and find reusable signals from shared entities, graph neighborhoods, and question templates. Experiments on multi-hop QA benchmarks show that LivingRAG improves accuracy over strong RAG baselines and reduces completion-token use when relevant prior experience is reused.

摘要：圖基礎的 RAG 通過將證據組織為知識圖譜來改善多跳問題回答。<lb/>然而，大多數現有的 RAG 系統都是孤立地處理每個查詢，並在推理後丟棄 LLM 回應中的有用推理。<lb/>因此，後續相關查詢需要從頭開始檢索證據和推理。<lb/>我們提出了 LivingRAG，一個具有可寫和可重用推理經驗的圖 RAG 框架。<lb/>LivingRAG 在基於圖的檢索骨幹上添加了一個可寫的經驗庫，允許在推理過程中以兩種方式重用已驗證的經驗。<lb/>存儲的圖信號幫助檢索找到在早期相關查詢中有用的實體和段落。<lb/>存儲的摘要為答案生成提供了參考推理模式。<lb/>我們分析了在線 QA 流，並從共享實體、圖鄰域和問題模板中找到了可重用的信號。<lb/>在多跳 QA 基準上的實驗顯示，LivingRAG 在準確性上優於強大的 RAG 基準，並在重用相關的先前經驗時減少了完成標記的使用。

##### **Code World Model: Coding Agent as World Brain**
2608.25927v1 by Yiwen Chen, Guosheng Lin, Chi Zhang

World models aim to simulate how complex environments evolve under actions and events, yet existing video-based world models primarily learn dynamics from visual observations, which reveal outcomes rather than the underlying knowledge, rules, and mechanisms governing world evolution. This makes it difficult to maintain persistent consequences and support coherent, open-ended evolution. We introduce Code World Model, a framework that separates world evolution from visual realization by combining the reasoning and coding capabilities of language models with the generative priors of video models. A coding agent serves as the world brain, reasoning about events and their consequences and generating executable code to maintain persistent world state and perform rule-consistent evolution. To connect executable state with visual generation, we introduce a proxy representation that encodes frame-wise spatiotemporal constraints and is compiled into a proxy video, which conditions a video model to render high-fidelity visual observations. We further develop data pipelines for constructing aligned proxy-observation pairs from gameplay and real-world videos. After fine-tuning on paired gameplay data, MiniMax-H3 follows proxy-based spatiotemporal specifications from simple interactive worlds built by the coding agent while preserving rich visual details and dynamics. These results demonstrate the potential of combining code for persistent world evolution with video models for flexible visual realization, providing a new path toward open-ended world models.

摘要：世界模型旨在模擬複雜環境在行動和事件下的演變，然而現有的基於視頻的世界模型主要從視覺觀察中學習動態，這些觀察揭示了結果，而不是支配世界演變的基本知識、規則和機制。這使得維持持久的後果和支持連貫的開放式演變變得困難。我們介紹了代碼世界模型，這是一個通過結合語言模型的推理和編碼能力與視頻模型的生成先驗，將世界演變與視覺實現分開的框架。一個編碼代理作為世界大腦，推理事件及其後果，並生成可執行的代碼以維持持久的世界狀態並執行符合規則的演變。為了將可執行狀態與視覺生成連接起來，我們引入了一種代理表示，該表示編碼逐幀的時空約束並編譯成代理視頻，這使得視頻模型能夠渲染高保真度的視覺觀察。我們進一步開發數據管道，以從遊戲和現實世界視頻中構建對齊的代理觀察對。在對配對的遊戲數據進行微調後，MiniMax-H3 遵循由編碼代理構建的簡單互動世界中的基於代理的時空規範，同時保留豐富的視覺細節和動態。這些結果展示了將代碼用於持久世界演變與視頻模型用於靈活視覺實現結合的潛力，為開放式世界模型提供了一條新途徑。

##### **Query-Side Attacks on GNN-Based KGQA: Tracing Failures from Entity Linking to Answer Generation**
2608.25922v1 by Pankaj Kumar, Subhankar Mishra

GNN-based Knowledge Graph Question Answering (KGQA) pipelines process queries through four discrete stages: entity linking, subgraph retrieval, GNN reasoning, and answer generation. Standard robustness evaluations conflate stage-level failures into a single end-to-end metric, obscuring both the source of brittleness and the appropriate mitigation target. We ask which stage fails, and why, when the pipeline is subjected to adversarial perturbations on the input question. We introduce a stage-isolation protocol with two answer-preserving adversarial perturbations verified against the knowledge graph: Compositional Restructuring (CR) and Relation Synonym Swap (RS) target distinct stages while leaving entity seeds intact. Evaluated across ComplexWebQuestions and WebQSP, the results run counter to prevailing assumptions: the GNN reasoning stage retains near-baseline accuracy when the subgraph is intact, while subgraph construction accounts for over 99\% of the end-to-end collapse under CR, occurring even when the gold answer is present in 74\% of retrieved subgraphs. This exposes a fundamental distinction between answer presence and answer reachability that end-to-end metrics cannot detect, and places the mitigation target firmly at the subgraph construction stage rather than the reasoning model. Perturbed datasets and evaluation infrastructure are released at https://anonymous.4open.science/r/atkgrag-E85C .

摘要：基於GNN的知識圖譜問答（KGQA）流程通過四個離散階段處理查詢：實體連結、子圖檢索、GNN推理和答案生成。標準的穩健性評估將階段級別的失敗混合成一個單一的端到端指標，模糊了脆弱性的來源和適當的緩解目標。我們詢問在管道受到對輸入問題的對抗擾動時，哪個階段失敗，以及為什麼。 我們引入了一個階段隔離協議，使用兩種對抗擾動來保持答案的完整性，並且這些擾動經過知識圖譜的驗證：組合重構（CR）和關係同義詞交換（RS）針對不同的階段，同時保持實體種子不變。在ComplexWebQuestions和WebQSP上的評估結果與普遍假設相悖：當子圖完整時，GNN推理階段保持接近基線的準確性，而子圖構建在CR下佔據了超過99\%的端到端崩潰，即使在74\%的檢索子圖中存在金標答案。這揭示了答案存在與答案可達性之間的根本區別，而端到端指標無法檢測到，並將緩解目標明確地放在子圖構建階段，而不是推理模型上。擾動數據集和評估基礎設施已在 https://anonymous.4open.science/r/atkgrag-E85C 上發布。

##### **Repair or Resample? Rethinking Failure Debugging in LLM Multi-Agent Systems**
2608.25920v1 by Zhongwen Luan, Xiaoyu Zhang, Ming Hu, Yue Yang, Jiongchi Yu, Xiaohong Chen

As large language model (LLM)-based multi-agent systems (MASs) are increasingly applied to long-horizon complex tasks, their reliability has emerged as the core bottleneck hindering their real-world deployment. Existing MAS debugging and repair methods typically rely on rerunning and resampling the entire execution trajectory. However, a fundamental question remains to be answered: do these methods causally repair MAS failures or merely stochastically repair by leveraging the randomness of LLM sampling? To evaluate the effectiveness of MAS repair methods, we introduce SymTrace, a controlled evaluation framework that records the MAS execution trajectory and establishes intervention anchors. During replay, it effectively reconstructs the execution before the anchor using recorded logs and only regenerates the downstream trajectory, thereby enabling the reliable reproduction of MAS failures. We further construct the dataset SymFail, comprising 536 human-annotated failure trajectories with graph-linked locations, categories, and trace evidence. Based on these foundations, we conduct a large-scale empirical study across three mainstream MAS frameworks. Our findings reveal that existing unguided rerun methods are highly unreliable, exhibiting low failure reproduction and repair rates (only 67.97% and 6.90%, respectively). Building upon these findings, we further explore the effectiveness of a symptom-driven intervention method, which successfully repairs 20.15% of the failed cases (a 191.89% improvement to state-of-the-art repair methods). This study aims to provide actionable insights for MAS debugging and repair research, paving the way for the robust deployment of multi-agent systems.

摘要：隨著基於大型語言模型（LLM）的多代理系統（MAS）越來越多地應用於長期複雜任務，其可靠性已成為阻礙其在現實世界中部署的核心瓶頸。現有的MAS調試和修復方法通常依賴於重新運行和重新取樣整個執行軌跡。然而，一個根本性問題仍待解答：這些方法是因果性地修復MAS故障，還是僅僅通過利用LLM取樣的隨機性進行隨機修復？為了評估MAS修復方法的有效性，我們引入了SymTrace，一個控制評估框架，記錄MAS執行軌跡並建立干預錨點。在重播過程中，它有效地使用記錄的日誌重構錨點之前的執行，並僅重新生成下游軌跡，從而實現MAS故障的可靠重現。我們進一步構建了數據集SymFail，其中包含536條人為標註的故障軌跡，並附有圖形鏈接的位置、類別和追蹤證據。在這些基礎上，我們在三個主流MAS框架中進行了大規模實證研究。我們的發現顯示，現有的無指導重跑方法極不可靠，故障重現率和修復率均較低（分別僅為67.97%和6.90%）。基於這些發現，我們進一步探索了一種以症狀驅動的干預方法，其成功修復了20.15%的故障案例（相較於最先進的修復方法提高了191.89%）。本研究旨在為MAS調試和修復研究提供可行的見解，為多代理系統的穩健部署鋪平道路。

##### **One Form to Transfer Them All: Pretraining Multilingual Language Models Beyond Native Orthography**
2608.25904v2 by Muge Zhang, Aaron Jencks, Krishna Badikela, Yulia Tsvetkov, Sachin Kumar

Multilingual language models transfer knowledge across languages through shared subword vocabulary, a mechanism that breaks down when related languages use different writing systems. Prior work addresses this via script equalization (romanization or IPA transcription), but direct comparisons are rare; the focus has been on encoder-only models, with most work adapting existing pretrained models. We systematically compare different input representations in autoregressive multilingual pretraining, comparing orthographic text, IPA, and romanization in a controlled setup across three scales (467M, 709M, and 1.03B) on eight languages in four typologically motivated pairs. Across a wide range of downstream tasks on seen and unseen languages, romanized pretraining yields the strongest cross-lingual transfer, and the advantage over text widens with scale. IPA improves over text in most settings but trails romanization. Surprisingly, finetuning a text-pretrained model on romanized data hurts performance on languages already covered by the base model, only marginally helping when the model lacks script coverage. Our results indicate that for multilingual models spanning typologically diverse scripts, to obtain maximum benefits, romanization should be treated as a core design choice applied at pretraining rather than a post hoc fix.

摘要：多語言模型通過共享的子詞詞彙在語言之間轉移知識，這一機制在相關語言使用不同書寫系統時會失效。先前的研究通過腳本均衡（羅馬化或IPA音標轉錄）來解決這一問題，但直接比較的研究很少；重點主要放在僅使用編碼器的模型上，大多數工作都是在適應現有的預訓練模型。我們系統地比較了自回歸多語言預訓練中不同的輸入表示，並在四個具有類型學動機的配對中，對八種語言的正字法文本、IPA和羅馬化進行了控制設置下的比較，規模為467M、709M和1.03B。在一系列下游任務中，對於已見和未見的語言，羅馬化預訓練產生了最強的跨語言轉移，並且隨著規模的擴大，相對於文本的優勢也在擴大。IPA在大多數設置中優於文本，但仍落後於羅馬化。令人驚訝的是，對於在羅馬化數據上進行微調的文本預訓練模型，在已被基礎模型覆蓋的語言上會影響性能，只有在模型缺乏腳本覆蓋時才會有邊際幫助。我們的結果表明，對於跨越類型學上多樣化腳本的多語言模型，為了獲得最大的好處，羅馬化應被視為在預訓練階段應用的核心設計選擇，而不是事後的修正。

##### **Skill Issue: Are Skills Language-Invariant in LLMs?**
2608.25832v1 by Bobby Cheng, Adam Gaber, Zhengyuan Liu, Catherine Arnett, Omer Goldman, Cheston Tan, Leshem Choshen

Large language models access knowledge inconsistently across languages, but to what extent do they differ in their skill sets when interacting with different languages? This work quantifies cross-lingual skill inconsistency orthogonally from knowledge and general benchmark performance. We do this via multilingual self-play: two instances of the same model compete in a text-based game, each interacting through a different language interface. Since the model, opponent, rules, state space, and available actions remain fixed, this setting isolates the effect of language on the model's realized behavior. We build a multilingual extension to TextArena and evaluate three open-weight models across eight languages and six games covering spatial reasoning, imperfect information, resource allocation, and repeated interaction. We find that the same model can exhibit markedly different playing strength across languages, with systematic variation in win--loss margins, invalid actions, and strategic tendencies. Detailed analyses reveal language-specific failures in spatial reasoning, card-conditioned decisions, and optimal move selection. In some settings, changing only the intermediate reasoning language recovers much of the lost performance, suggesting that language can affect different stages of the decision process. These results show that skill discrepancies are a measurable major roadblock in the development of truly multilingual models. Better understanding these discrepancies can help us design models that perform more equitably across languages.

摘要：大型語言模型在不同語言中的知識獲取不一致，但它們在與不同語言互動時的技能組合有多大差異？本研究從知識和一般基準表現的正交角度量化跨語言技能不一致性。<lb/>我們通過多語言自我對抗來實現這一點：同一模型的兩個實例在一個基於文本的遊戲中競爭，每個實例通過不同的語言介面進行互動。<lb/>由於模型、對手、規則、狀態空間和可用行動保持不變，這個設置隔離了語言對模型實際行為的影響。<lb/>我們構建了TextArena的多語言擴展，並在八種語言和六個遊戲中評估三個開放權重模型，涵蓋空間推理、不完全信息、資源分配和重複互動。<lb/>我們發現同一模型在不同語言中可以表現出顯著不同的遊戲實力，並在勝負邊際、無效行動和戰略傾向上存在系統性變化。<lb/>詳細分析揭示了在空間推理、基於卡片的決策和最佳行動選擇中存在語言特定的失誤。<lb/>在某些情況下，僅改變中介推理語言就能恢復大部分失去的表現，這表明語言可以影響決策過程的不同階段。<lb/>這些結果顯示，技能差異是發展真正多語言模型的一個可測量的重要障礙。<lb/>更好地理解這些差異可以幫助我們設計在不同語言中表現更公平的模型。

##### **Why Does Graph Learning Fail to Fully Benefit from a Text Teacher?**
2608.25741v1 by Fumiaki Kimino, Ryoma Sato

Graph neural networks (GNNs) are widely used to represent complex interactions and relationships among entities. We investigate a multimodal model that combines two complementary ideas: a self-supervised method that enables a GNN encoder pretrained on one dataset to operate directly on another dataset with a different node-feature dimensionality, without rebuilding the model or realigning the data; and an alternating optimization method that updates a language-model module in an E-step and a GNN module in an M-step, rather than jointly training a large language model and a GNN end to end on a large graph. Despite expectations, the combined model did not sufficiently improve predictive performance. We identify six factors: (1) an external anchor in the E-step has a strength-safety trade-off: a weak anchor has little effect, whereas an overly strong anchor can damage the graph representation; (2) the knowledge of the E-step teacher is not injected directly into the GCN embedding Z; (3) the representation space constructed in the M-step is not optimized for the same objective as the E-step teacher space, resulting in a compromise representation for target classification; (4) GCN propagation averages a node's own textual information with information from its neighbors; (5) cosine alignment does not guarantee axes that are discriminative for classification, so stronger geometric alignment with the E-step text anchor need not sufficiently improve the target decision boundary or classification performance; and (6) the force that preserves the source-side self-supervised geometry in the M-step conflicts with the force that moves the representation toward the E-step teacher. We support these observations through a staged set of experiments that varies the influence of the E-step.

摘要：圖神經網絡（GNNs）被廣泛用於表示實體之間的複雜互動和關係。<lb/>我們研究了一種多模態模型，結合了兩個互補的想法：一種自我監督的方法，使得在一個數據集上預訓練的GNN編碼器能夠直接在另一個具有不同節點特徵維度的數據集上運作，而無需重建模型或重新對齊數據；以及一種交替優化方法，在E步驟中更新語言模型模塊，在M步驟中更新GNN模塊，而不是在大型圖上端到端地聯合訓練大型語言模型和GNN。<lb/>儘管有預期，結合模型並未顯著改善預測性能。我們確定了六個因素：（1）E步驟中的外部錨點存在強度與安全性的權衡：弱錨點影響甚微，而過強的錨點可能損害圖的表示；（2）E步驟教師的知識並未直接注入到GCN嵌入Z中；（3）在M步驟中構建的表示空間並未針對與E步驟教師空間相同的目標進行優化，導致目標分類的妥協表示；（4）GCN傳播將節點自身的文本信息與其鄰居的信息進行平均；（5）餘弦對齊並不保證對分類具有區分性的軸，因此與E步驟文本錨點的更強幾何對齊不一定能夠充分改善目標決策邊界或分類性能；（6）在M步驟中保持源端自我監督幾何的力量與將表示移向E步驟教師的力量相矛盾。我們通過一系列分階段的實驗來支持這些觀察，這些實驗變化了E步驟的影響。

##### **When RAG Fails to Equalize: Geo-bias in Factual Question Answering over Public Companies**
2608.25717v1 by Abhinav Havaldar, Enrico Santus

Retrieval-augmented generation (RAG) is widely assumed to mitigate factual errors in large language models (LLMs), but it remains unclear whether retrieval uniformly compensates for missing knowledge. We study this question in a controlled factual QA setting over public companies, constructing a benchmark of approximately 2,000 firms across global equity indices. We evaluate six LLMs on four atomic attributes under four conditions: no-context, perfect context, misleading context, and distraction context. We find strong geographic disparities in no-context accuracy, indicating uneven parametric knowledge. While perfect context improves performance, it does not eliminate these gaps: gains are correlated with baseline accuracy, suggesting retrieval effectiveness is coupled to internal representations. Under misleading context, models frequently copy incorrect information. Larger models improve overall performance but do not remove these structural effects. These results challenge the view of RAG as a universal corrective and highlight the interaction between model knowledge, context quality, and entity representation.

摘要：檢索增強生成（RAG）普遍被認為能減少大型語言模型（LLMs）中的事實錯誤，但尚不清楚檢索是否均勻地彌補了缺失的知識。<lb/>我們在一個受控的事實問答環境中研究這個問題，針對公共公司構建了一個約2,000家公司的基準，涵蓋全球股權指數。<lb/>我們在四種條件下評估六個LLM的四個原子屬性：無上下文、完美上下文、誤導性上下文和干擾上下文。<lb/>我們發現在無上下文的準確性上存在強烈的地理差異，表明參數知識的不均勻性。<lb/>雖然完美上下文提高了性能，但並未消除這些差距：增益與基線準確性相關，這表明檢索的有效性與內部表徵相互關聯。<lb/>在誤導性上下文下，模型經常複製不正確的信息。<lb/>較大的模型提高了整體性能，但並未消除這些結構性影響。<lb/>這些結果挑戰了將RAG視為普遍修正的觀點，並突顯了模型知識、上下文質量和實體表徵之間的相互作用。

##### **Data Citation for Large Language Models: A Challenge**
2608.25663v1 by Gianmaria Silvello

Large language models increasingly mediate access to information, and a growing body of work asks whether they cite the sources behind their outputs. That work treats citation as a verification device and applies it to textual documents. Scholarly citation serves two further functions, credit and provenance, and it applies to data as much as to text. This paper argues that data citation for large language models is an open challenge, distinct from document-level citation grounding and harder to solve. We ask how such models should cite data so that outputs stay verifiable, provenance stays traceable, and credit reaches data creators and curators. We set out three research directions. Training data attribution has to turn influence estimates into references for corpora absorbed into model parameters. Data citation at inference time has to identify datasets, subsets, and query results at the right granularity and fixity. Citing knowledge graph facts has to define what a reference to a single triple denotes and how credit propagates along provenance. Progress on all three depends on joint work across the database, information retrieval, knowledge representation, and artificial intelligence communities.

摘要：大型語言模型越來越多地調解對信息的訪問，越來越多的研究工作在詢問它們是否引用其輸出背後的來源。該研究將引用視為一種驗證工具，並將其應用於文本文件。學術引用還有兩個進一步的功能，即信用和來源，並且它適用於數據與文本同樣重要。本文主張，大型語言模型的數據引用是一個未解決的挑戰，與文件級引用基礎不同，且更難解決。我們詢問這些模型應如何引用數據，以便輸出保持可驗證，來源保持可追溯，並且信用能夠到達數據創作者和策展人。我們提出三個研究方向。訓練數據歸屬必須將影響估算轉化為納入模型參數的語料庫的引用。推理時的數據引用必須在正確的粒度和固定性下識別數據集、子集和查詢結果。引用知識圖譜事實必須定義對單個三元組的引用表示什麼，以及信用如何沿著來源傳播。這三者的進展依賴於數據庫、信息檢索、知識表示和人工智能社區之間的共同努力。

##### **From General Agents to RCA Experts: A Self-Evolving Harness for Root Cause Analysis**
2608.25661v1 by Haiyu Huang, Jiewei Lyu, Zhihan Jiang, Jinyang Liu, Xiao He, Tieying Zhang, Wu Xiang, Michael R. Lyu

Automated root cause analysis (RCA) with large language models (LLMs) has drawn growing attention. Today, SREs typically automate RCA with LLMs in one of two ways: directly using a general-purpose agent (e.g., Codex or Claude Code) for diagnosis, or building a specialized RCA agent from scratch. As mainstream general agents grow more capable and iterate quickly, our quantitative study finds that the former now often surpasses the latter. Its accuracy, however, still falls short of production needs, and this gap stems mainly from the external adaptation layer outside the agent's general capabilities, namely the harness. We therefore argue that LLM-based RCA should focus on this external harness, reusing the strong general capabilities of a modern agent rather than rebuilding an agent from scratch. A key capability of such a harness is to self-evolve, accumulating system-specific experience from past diagnoses so that it gets better the more it is used. We introduce OpsHarness, a self-evolving RCA harness that turns diagnosis experience into reusable expertise. Its data plane combines layered operational knowledge with an idea-card tool library, while its control plane coordinates setup, diagnosis, evolution, and verification. During evolution, OpsHarness contrasts successful and failed trajectories, converts their evidence into atomic proposals, and admits updates only through a dual-gate verification process designed to prevent overfitting and regression. Across two public benchmarks and an industrial deployment, OpsHarness achieves 59.0\% top-1 accuracy, improving over a bare general agent by 63.4\% and over baseline RCA agents by 4.02$\times$.

摘要：自動化根本原因分析 (RCA) 與大型語言模型 (LLMs) 引起了越來越多的關注。今天，SRE 通常以兩種方式之一使用 LLM 自動化 RCA：直接使用通用代理（例如，Codex 或 Claude Code）進行診斷，或從零開始構建專門的 RCA 代理。隨著主流通用代理變得越來越強大並迅速迭代，我們的定量研究發現，前者現在通常超過後者。然而，它的準確性仍然未達到生產需求，這一差距主要源於代理通用能力之外的外部適應層，即鞍帶。因此，我們主張基於 LLM 的 RCA 應該專注於這個外部鞍帶，重用現代代理的強大通用能力，而不是從頭開始重建一個代理。這樣的鞍帶的一個關鍵能力是自我演化，從過去的診斷中積累系統特定的經驗，以便隨著使用次數的增加而變得更好。我們介紹了 OpsHarness，一個自我演化的 RCA 鞍帶，將診斷經驗轉化為可重用的專業知識。它的數據平面結合了分層操作知識和一個理念卡工具庫，而它的控制平面協調設置、診斷、演化和驗證。在演化過程中，OpsHarness 對比成功和失敗的軌跡，將其證據轉化為原子提案，並僅通過設計用於防止過擬合和回歸的雙閘驗證過程來接受更新。在兩個公共基準和一個工業部署中，OpsHarness 實現了 59.0\% 的 top-1 準確率，較裸露的通用代理提高了 63.4\%，並較基準 RCA 代理提高了 4.02$\times$。

##### **Leveraging Inter-object Affordances for Efficient Planning in Contact-rich Tasks**
2608.25641v1 by Pouya P. Niaz, Justus Piater, Alejandro Agostini

Traditional task-and-motion planning (TAMP) approaches primarily focus on defining sequences of actions along with the necessary geometric and kinematic constraints to execute long-horizon tasks. However, their applicability in real-world settings is limited, as they typically assume simplified object models that overlook key physical properties critical for the successful execution of contact-rich tasks. Moreover, they often use sub-symbolic reasoning during motion planning, which drastically increases planning time and decreases overall success rates. We propose a method that leverages a TAMP approach, defining object-centric abstractions of execution constraints, called Unified TAMP (U-TAMP), to execute robotic tasks involving interactions among objects with heterogeneous shapes, sizes, and materials. Using a Vision-Language Model (VLM), we generate abstractions of inter-object affordances for characterizing physical interaction constraints between objects in contact-rich tasks, such as grasp and support constraints. These constraints are used to enrich the U-TAMP planning domain to deal with objects with variable physical properties. We perform experiments in simulated kitchen table organization scenarios and compare our results with those of the original U-TAMP, as well as a state-of-the-art VLM-based planner that leverages common sense knowledge of objects' affordances for plan generation. Our approach achieves significantly higher planning success rates and improves planning times by one to two orders of magnitude compared to other methods.

摘要：傳統的任務與運動規劃 (TAMP) 方法主要專注於定義行動序列以及執行長期任務所需的幾何和運動約束。<lb/>然而，它們在現實世界中的適用性有限，因為它們通常假設簡化的物體模型，忽略了成功執行接觸豐富任務所需的關鍵物理特性。<lb/>此外，它們在運動規劃過程中經常使用次符號推理，這大幅增加了規劃時間並降低了整體成功率。<lb/>我們提出了一種利用 TAMP 方法的技術，定義執行約束的物體中心抽象，稱為統一 TAMP (U-TAMP)，以執行涉及形狀、大小和材料異質物體之間互動的機器人任務。<lb/>使用視覺語言模型 (VLM)，我們生成物體間可供性抽象，以表徵接觸豐富任務中物體之間的物理互動約束，例如抓取和支撐約束。<lb/>這些約束用於豐富 U-TAMP 規劃領域，以處理具有可變物理特性的物體。<lb/>我們在模擬的廚房桌面組織場景中進行實驗，並將我們的結果與原始 U-TAMP 以及一種基於 VLM 的最先進規劃器進行比較，後者利用物體可供性的常識知識來生成計劃。<lb/>我們的方法在規劃成功率上顯著提高，並將規劃時間提高了一到兩個量級，相較於其他方法。

##### **JIT-Agent: Scaling Harness Intelligence via Just-in-Time Harness Evolution**
2608.25593v1 by Guibin Zhang, Leo Lu, Fangzhou Xie, Kang Zhu, Junhao Wang, Zhifei Xie, Zhaochen Yu, Zihang Liu, Zhongxiang Sun, Qiankun Li, Yue Liao, Heng Chang, Xiaobin Hu, Qibing Ren, Wangchunshu Zhou, Shuicheng Yan

Agent capability is not determined by the model alone. The agent harness, encompassing memory management, planning strategy, action protocol, and tool/skill orchestration, can dominate the contribution of the underlying foundation model. Yet harness design remains manual, task-specific, and fundamentally unscalable. We present JIT-Agent, a harness intelligence model trained to synthesize task-adaptive agent harnesses on the fly for arbitrary off-the-shelf agentic LLMs. We formalize the agent harness as a composable, machine-generatable artifact governed by a fixed four-module protocol, and train JIT-Agent to customize harnesses for a given task at hand, repair harnesses for stable and reliable execution, and self-evolve by distilling performance signals from an expanding archive of prior harness configurations. Equipped with JIT-Agent as a harness helper, DeepSeek-V4-Flash surpasses GPT-5.6 on DeepSearchQA (+9.1) and OdysseyBench (+4.3), while the already strong GLM-5.2 gains up to +20.2 points. Across controlled evaluations, JIT-Agent-generated harnesses are performance-competitive with mature agent runtimes such as OpenCode and Claude Code and consistently improve multi-scale model families of DeepSeek V4, Mimo-V2.5, and Qwen3.6. To our knowledge, JIT-Agent is the first model purpose-built for just-in-time harness generation, establishing harness intelligence as a trainable, transferable, and compounding dimension of agent capability orthogonal to model scaling.

摘要：代理能力並不僅由模型決定。代理的鞍具，包括記憶管理、規劃策略、行動協議和工具/技能協調，可能會主導底層基礎模型的貢獻。然而，鞍具設計仍然是手動的、特定任務的，並且在根本上無法擴展。我們提出了 JIT-Agent，一種鞍具智能模型，旨在即時合成適應任務的代理鞍具，適用於任意現成的代理 LLM。我們將代理鞍具形式化為一種可組合的、機器可生成的工件，受固定的四模塊協議所支配，並訓練 JIT-Agent 為當前任務定制鞍具，修復鞍具以實現穩定和可靠的執行，並通過從不斷擴展的先前鞍具配置檔案中提煉性能信號來自我進化。配備 JIT-Agent 作為鞍具助手的 DeepSeek-V4-Flash 在 DeepSearchQA (+9.1) 和 OdysseyBench (+4.3) 上超越了 GPT-5.6，而已經強大的 GLM-5.2 則獲得了高達 +20.2 分。在受控評估中，JIT-Agent 生成的鞍具在性能上與成熟的代理運行時（如 OpenCode 和 Claude Code）相競爭，並持續改善 DeepSeek V4、Mimo-V2.5 和 Qwen3.6 的多尺度模型系列。據我們所知，JIT-Agent 是首個專為即時鞍具生成而構建的模型，確立了鞍具智能作為可訓練、可轉移和累積的代理能力維度，與模型擴展正交。

##### **V-Rubrics: Visual Faithfulness via Rubric-Based Reinforcement Learning**
2608.25580v1 by Shulin Tian, Minglun Li, Yuhao Dong, Hao Ding, Jiarui Yao, Haiwen Diao, Jingkang Yang, Hongyuan Zhu, Ziwei Liu

Vision-language models can produce fluent answers that are insufficiently grounded in the visual evidence: a single unsupported object, chart value, or intermediate inference can undermine an otherwise plausible response. We argue that this is a credit-assignment failure in multimodal post-training. Scalar outcome rewards indicate whether an answer is acceptable, but do not identify which visual facts are grounded, which reasoning steps are valid, or which instruction constraints are missed. We introduce Visual Rubrics-Based Reinforcement Learning, which decomposes reference responses into atomic propositions and scores generated answers along Visual Faithfulness (VF), Reasoning Consistency (RC), and Instruction Following (IF). The resulting rubric items provide structured partial credit and localize rubric credit when supporting evidence spans are available. We first obtain an SFT checkpoint by fine-tuning Qwen3-VL-8B-Instruct on the public OpenMMReasoner-SFT-874K corpus, adapting OpenMMReasoner's cold-start data recipe. We construct V-Rubrics 50K, a 50,248-example training set from 17 visually grounded sources, by applying rule-based filters before deriving example difficulty from rejection-sampling scores and then annotating every example with Gemini-3-Pro under the same structured prompt and protocol. We train our model based on the same SFT checkpoint using component-wise, prefix-localized rubric credit. Experiments show that our rubricbased GRPO improves over both the shared SFT baseline and answer-only GRPO, with the largest gains on knowledge-oriented and visually grounded reasoning benchmarks. The results show rubrics as a useful reward abstraction for visual post-training.

摘要：視覺語言模型可以產生流暢的回答，但這些回答往往缺乏對視覺證據的充分支持：單一的無支持物體、圖表值或中間推論都可能削弱原本合理的回應。我們認為這是在多模態後訓練中的信用分配失敗。標量結果獎勵指示回答是否可接受，但並未識別哪些視覺事實是有根據的、哪些推理步驟是有效的，或哪些指令限制被忽略。我們引入了基於視覺評分標準的強化學習，該方法將參考回應分解為原子命題，並根據視覺真實性（VF）、推理一致性（RC）和遵循指令（IF）對生成的答案進行評分。所得到的評分項目提供結構化的部分信用，並在支持證據範圍可用時定位評分信用。我們首先通過對公共的OpenMMReasoner-SFT-874K語料庫進行微調，獲得SFT檢查點，並調整OpenMMReasoner的冷啟動數據配方。我們構建了V-Rubrics 50K，這是一個包含50,248個示例的訓練集，來自17個視覺基礎來源，通過應用基於規則的過濾器來推導示例難度，然後在相同的結構化提示和協議下用Gemini-3-Pro對每個示例進行標註。我們基於相同的SFT檢查點訓練我們的模型，使用組件級、前綴局部的評分信用。實驗顯示，我們基於評分標準的GRPO在共享的SFT基線和僅回答的GRPO上都有所改善，在知識導向和視覺基礎推理基準上獲得了最大的增益。結果顯示，評分標準作為視覺後訓練的有用獎勵抽象。

##### **Cross-Dataset Stability of Expert-Informed Skill Prompting and Fine-Tuning for Chinese Metaphor Identification**
2608.25579v1 by Yufeng Wu, Meichun Liu

Metaphor-identification performance can change markedly across datasets that differ in text distribution and annotation policy. We examine whether a fixed expert-informed procedure produces a more even cross-dataset profile than task-specific parameter adaptation. Four prespecified conditions are compared for Chinese sentence-level metaphor identification: BERT fine-tuning (BERT-FT), QLoRA-based large language model fine-tuning (LLM-FT), direct zero-shot LLM prompting (LLM-ZS), and zero-shot prompting with a frozen procedural Skill (Skill-ZS). The Skill operationalizes established criteria involving contextual meaning, basic meaning, contrast, and comparison. Evaluation covers CMRE Test and two external datasets, CCIME and CMC. Fine-tuned scores are means over three seeds, whereas each zero-shot score comes from one deterministic configuration. Fine-tuning remains strongest on the native test set: BERT-FT reaches 91.76 Macro-F1. LLM-FT has the highest external mean (83.52), while Skill-ZS is close at 82.92 and has both the highest external floor (82.64) and the smallest observed range across all three datasets (4.08 points). In the matched zero-shot comparison, adding the Skill reduces metaphorical predictions on every dataset. This sharply lowers false positives on CCIME but increases false negatives on CMRE Test and CMC. The results position expert-informed Skill prompting as a complementary route to more even observed cross-dataset performance, while fine-tuning retains its advantage in native-data accuracy. To our knowledge, this is the first study to compare an expert-informed procedural Skill with task-specific fine-tuning in the same cross-dataset evaluation of Chinese sentence-level metaphor identification.

摘要：隱喻識別的表現可能會因文本分佈和註釋政策的不同而在不同數據集之間顯著變化。<lb/>我們檢查固定的專家資訊程序是否能產生比特定任務參數調整更均勻的跨數據集概況。<lb/>針對中文句子級別的隱喻識別，我們比較了四個預先指定的條件：BERT微調（BERT-FT）、基於QLoRA的大型語言模型微調（LLM-FT）、直接的零樣本LLM提示（LLM-ZS），以及使用凍結程序的技能進行零樣本提示（Skill-ZS）。<lb/>該技能實現了涉及上下文意義、基本意義、對比和比較的既定標準。<lb/>評估涵蓋了CMRE測試和兩個外部數據集，CCIME和CMC。<lb/>微調得分是三個隨機種子的平均值，而每個零樣本得分來自一個確定性的配置。<lb/>在本地測試集上，微調仍然是最強的：BERT-FT達到91.76的Macro-F1。<lb/>LLM-FT的外部均值最高（83.52），而Skill-ZS接近82.92，並且在所有三個數據集中擁有最高的外部底線（82.64）和最小的觀察範圍（4.08分）。<lb/>在匹配的零樣本比較中，添加技能會減少每個數據集上的隱喻預測。<lb/>這顯著降低了CCIME的假陽性，但增加了CMRE測試和CMC的假陰性。<lb/>結果將專家資訊的技能提示定位為更均勻的觀察跨數據集表現的補充途徑，而微調在本地數據準確性方面仍保持其優勢。<lb/>據我們所知，這是第一項在相同的跨數據集評估中比較專家資訊的程序技能與特定任務微調的研究，針對中文句子級別的隱喻識別。

##### **MLLMCLIP: Feature-Level Distillation of MLLM for Robust Vision-Language Representations**
2608.25575v1 by Jongsuk Kim, Qiyu Wu, Zhuoyuan Mao, Hiromi Wakaki, Junmo Kim, Yuki Mitsufuji

Pretrained vision-language models such as CLIP excel at zero-shot recognition but often fail at compositionality, particularly attribute-object and relational structures. Recent studies mitigate this issue by augmenting training with synthetic hard negatives generated by a cascade of large language models and text-to-image models, which incurs substantial pipeline overhead. We instead propose MLLMCLIP, a heterogeneous distillation framework that transfers multimodal knowledge directly from a generative Multimodal Large Language Model (MLLM) teacher into a discriminative CLIP student, bypassing synthetic data entirely. To bridge the architectural mismatch between the two paradigms, we introduce an attention-based per-layer token selection and a CKA-based distillation loss. Compared to prior CLIP-enhancement methods, MLLMCLIP achieves state-of-the-art compositional accuracy while delivering consistent gains on standard zero-shot classification and image-text retrieval, showing that feature-level distillation strengthens both compositional and general vision-language representation capability.

摘要：預訓練的視覺-語言模型如 CLIP 在零-shot 識別方面表現優異，但在組合性上經常失敗，特別是在屬性-物件和關係結構上。最近的研究通過使用大型語言模型和文本到圖像模型生成的合成困難負樣本來增強訓練，從而減輕這一問題，但這會產生相當大的管道開銷。我們提出 MLLMCLIP，一種異質蒸餾框架，將多模態知識直接從生成的多模態大型語言模型 (MLLM) 教師轉移到區分性的 CLIP 學生，完全繞過合成數據。為了彌合這兩種範式之間的架構不匹配，我們引入了基於注意力的每層令牌選擇和基於 CKA 的蒸餾損失。與之前的 CLIP 增強方法相比，MLLMCLIP 在組合準確性上達到了最先進的水平，同時在標準的零-shot 分類和圖像-文本檢索上持續獲得增益，顯示特徵級蒸餾增強了組合性和一般的視覺-語言表示能力。

##### **AdaVDR: Adaptive Tool Use and Reflection for Video Deep Research**
2608.25559v1 by Xintong Zhang, Xiaomeng Fan, Shilin Yan, Ekko He, Zicheng Liu, Zijian Zou, Guannan Zhang, Yuwei Wu, Zhi Gao, Hongwei Xue

Video deep research answers complex questions by jointly understanding video content and retrieving external knowledge from the open Web. However, diverse questions and videos require different tool-use strategies, and inappropriate tool calls can produce incorrect results. Uncertain grounding and retrieval also make unnecessary interactions costly and error-prone, increasing latency and reasoning errors. To address these challenges, we propose AdaVDR, an adaptive video deep research agent with adaptive tool invocation and reflection. AdaVDR selects tools according to the task and its capabilities, and backtracks only when unreliable intermediate results require correction. To enable these capabilities, we develop a video deep research data construction pipeline. We first discover retrieval-relevant events and entities in diverse videos and acquire detailed information through grounding and external retrieval to construct high-quality QA pairs. For each QA, task-specific prompts organize the information acquisition process into a tool-use trajectory, allowing different question and video types to follow different grounding and retrieval strategies. We further introduce model-conditioned tool necessity filtering, which evaluates tool calls against the target model's video understanding and internal knowledge, removing tools or tool chains the model can bypass. This yields trajectories tailored to the target model's video understanding capability and knowledge. Using this pipeline, we construct training data and VDR-EE, a benchmark covering entity-centric and event-centric questions. We perform supervised fine-tuning followed by reinforcement learning with a redundancy-aware reward to strengthen adaptive tool invocation and reflection. Experiments show that our method performs best among the evaluated open-source models on VDR-EE and substantially improves over its base models on VideoDR.

摘要：視頻深度研究通過共同理解視頻內容並從開放網絡檢索外部知識來回答複雜問題。<lb/>然而，多樣化的問題和視頻需要不同的工具使用策略，而不當的工具調用可能會產生不正確的結果。<lb/>不確定的基礎和檢索也使得不必要的互動成本高昂且容易出錯，增加延遲和推理錯誤。<lb/>為了解決這些挑戰，我們提出了AdaVDR，一個具有自適應工具調用和反思能力的自適應視頻深度研究代理。<lb/>AdaVDR根據任務及其能力選擇工具，僅在不可靠的中間結果需要修正時回溯。<lb/>為了實現這些能力，我們開發了一個視頻深度研究數據構建管道。<lb/>我們首先在多樣化的視頻中發現與檢索相關的事件和實體，並通過基礎和外部檢索獲取詳細信息，以構建高質量的問答對。<lb/>對於每個問答，特定任務的提示將信息獲取過程組織成工具使用軌跡，使不同問題和視頻類型能夠遵循不同的基礎和檢索策略。<lb/>我們進一步引入模型條件的工具必要性過濾，該過濾評估工具調用與目標模型的視頻理解和內部知識，去除模型可以跳過的工具或工具鏈。<lb/>這產生了針對目標模型的視頻理解能力和知識量身定制的軌跡。<lb/>利用這個管道，我們構建了訓練數據和VDR-EE，一個涵蓋以實體為中心和以事件為中心問題的基準。<lb/>我們進行了監督微調，隨後進行了強化學習，使用意識到冗餘的獎勵來加強自適應工具調用和反思。<lb/>實驗表明，我們的方法在VDR-EE的評估開源模型中表現最佳，並在VideoDR上顯著改善了其基模型。

##### **Virgil: Navigating Explainability for Transformer-based Language Models**
2608.25555v1 by Martino Ciaperoni, Sezer Kutluk, Benedetta Muscato, Marta Marchiori Manerba, Fosca Giannotti

Explainability for transformer-based language models is becoming crucial as these systems are deployed in high-stakes applications. As a result, the ecosystem of explainability tools is rapidly evolving, becoming richer, but also more fragmented and harder to navigate. To address this challenge, we present Virgil, an interactive system that lets practitioners and researchers, including non-experts, navigate explainability tools for transformer language models. Supported by a curated knowledge base, the system enables users to discover and compare explainability tools within a unified interface.

摘要：解釋性對於基於Transformer的語言模型變得至關重要，因為這些系統在高風險應用中被部署。<lb/>因此，解釋性工具的生態系統正在迅速發展，變得更加豐富，但也更加分散且難以導航。<lb/>為了解決這一挑戰，我們提出了Virgil，一個互動系統，讓從業者和研究人員，包括非專家，能夠導航Transformer語言模型的解釋性工具。<lb/>在經過精心策劃的知識庫的支持下，該系統使用戶能夠在統一的界面中發現和比較解釋性工具。

##### **When Stale Constraints Go Unchecked: Budgeted Verification Failures in Inherited Agent Memory**
2608.25553v3 by Kazuki Nakayashiki

Provenance links keep the evidence behind an inherited belief reachable; an agent with a verification budget must still choose which links to inspect. We study a consolidated memory that states a decision constraint and whose source record has since been superseded by a record that withdraws it: provenance is immutable, the current record has changed, and the memory is stale. In a controlled six-memory scenario with a budget of two records, sixteen language models rarely re-verified a constraint that read as settled: they inspected its provenance path in about one episode in five and, once the constraint had been superseded, produced stale-consistent decisions in 77.3%, 74.7% and 74.7% of episodes across a primary run, a replication and a held-out domain. Re-assigning one of the same two slots to the critical path removed most of them: +74.0, +72.7 and +61.3 points (positive in every model), +80.7 in a prospectively frozen interleaved replication with a repaired non-critical control, and +62.0 on a panel of 10 models from 9 organisations; a corrected re-run of the held-out scenario gave +73.3. The forced-critical policy uses experimenter knowledge of the critical path: it quantifies how much stale-decision risk the same budget can recover and is not a scheduler. Two further deposited experiments locate the failure and a remedy: in this store the constraint's path is selected in 17.0% of episodes at two slots and 88.7% at four of six (above uniform allocation), and at two slots a one-sentence, target-blind rule (prefer memories that state a limit on a candidate direction) moved the agent's own allocation onto the constraint's path and recovered the oracle contrast on decisions (+89.3 points) where that constraint limits the tempting action, while a content-free freshness cue did not materially redirect allocation and a content-matched control rule changed neither selection nor decisions.

摘要：來源連結使得繼承信念背後的證據可達；擁有驗證預算的代理仍然必須選擇檢查哪些連結。我們研究了一個綜合記憶，該記憶表明了一個決策約束，其來源記錄已被撤回的記錄所取代：來源是不可變的，當前記錄已經改變，而記憶則是過時的。在一個受控的六記憶場景中，預算為兩條記錄，十六個語言模型很少重新驗證一個看似已解決的約束：它們在每五個回合中大約檢查一次其來源路徑，並且在約束被取代後，在主要運行、重複和保留域的回合中，分別以77.3%、74.7%和74.7%的比例產生過時一致的決策。將同樣的兩個槽位中的一個重新分配到關鍵路徑上去除了大部分的過時決策：+74.0、+72.7和+61.3分（每個模型均為正），在一個前瞻性凍結的交錯重複中，修復的非關鍵控制達到+80.7，在來自9個組織的10個模型的面板上達到+62.0；對保留場景的修正重跑給出了+73.3。強制關鍵政策利用了實驗者對關鍵路徑的知識：它量化了同樣預算能夠恢復多少過時決策風險，而不是排程器。兩個進一步的存儲實驗定位了失敗和補救：在這個存儲中，約束的路徑在兩個槽位的回合中被選擇的比例為17.0%，在六個槽位中的四個槽位則為88.7%（高於均勻分配），而在兩個槽位中，一個單句的目標盲規則（偏好陳述對候選方向的限制的記憶）將代理自己的分配轉移到約束的路徑上，並在該約束限制誘惑行動的情況下恢復了決策的神諭對比（+89.3分），而一個無內容的新鮮提示並未實質性地重新指導分配，且一個內容匹配的控制規則既未改變選擇也未改變決策。

##### **Syn2Logic: End-to-End Neuromorphic Design Automation**
2608.25536v1 by Artur Podobas

In this work, we propose a view on electronic Neuromorphic Design Automation (eNDA), which we see as a design automation flow that bridges computational neuroscience modeling with traditional Electronic Design Automation (EDA) flow. We introduce the term, give examples of how it can be implemented, and design a prototype implementation: Syn2Logic. Syn2Logic is an entire eNDA framework, that allows neuroscientists to model neural behavior using a custom DSL and a compiler that takes the same model description down to synthesizable RTL hardware. We end the paper by applying the eNDA-flow through Syn2Logic to show how to -- without writing a single line of hardware description language (HDL) code-- (i) generate what we believe is the fastest C. elegans accelerator that runs significantly faster than state-of-the-art simulators, (ii) create (to the best of our knowledge) the fastest, most generic neuromorphic sudoku solver that outperforms CP-SAT and SCIP on TOP1465 puzzles, and (iii) create a 5.6 million FPS/Watt accelerator on a tiny FPGA that outperforms existing neuromorphic architectures in terms of speed and energy-efficiency on the MNIST dataset.

摘要：在這項工作中，我們提出了一種電子類神經形態設計自動化（eNDA）的觀點，我們將其視為一種設計自動化流程，該流程將計算神經科學建模與傳統電子設計自動化（EDA）流程相連接。<lb/>我們引入了這個術語，並給出了如何實施的例子，還設計了一個原型實現：Syn2Logic。<lb/>Syn2Logic是一個完整的eNDA框架，允許神經科學家使用自定義DSL和一個將相同模型描述轉換為可合成RTL硬體的編譯器來建模神經行為。<lb/>我們在論文的最後應用eNDA流程通過Syn2Logic來展示如何在不編寫任何硬體描述語言（HDL）代碼的情況下-- (i) 生成我們認為是最快的C. elegans加速器，其運行速度顯著快於最先進的模擬器，(ii) 創建（據我們所知）最快、最通用的類神經形態數獨解決器，在TOP1465謎題上超越CP-SAT和SCIP，(iii) 在一個微小的FPGA上創建一個每瓦5.6百萬幀的加速器，在速度和能效方面超越現有的類神經形態架構，特別是在MNIST數據集上。

##### **CaSKG: Counterfactual-Causal Skill Graphs for Scalable Agent Skill Retrieval**
2608.25500v1 by Zhiyuan Li, Linyuan Gao, Xuechun Ding, Hongwei Chen, Yuan Wu, Yi Chang

Reusable skill libraries allow large language model (LLM) agents to reuse procedural knowledge across tasks, but they also turn memory access into a challenging retrieval problem. Full-library prompting preserves coverage at high context cost, vector retrieval returns compact neighborhoods but treats skills as independent text, and graph-based retrieval can recover workflow context only when the edges that carry relevance are reliable. We propose CaSKG, a counterfactual-causal skill graph framework that calibrates procedural relations before retrieval. CaSKG first builds a high-recall directed candidate graph from semantic, lexical, input/output, and structural evidence, with repair evidence and an optional LLM judge further refining candidate scores. It then applies direction-conditioned textual counterfactual probes that remove, substitute, and reorder skill pairs, aggregates the evidence with Bayesian smoothing, and publishes a state-filtered weighted graph for task-conditioned expansion. The graph is constructed offline and used without changing the downstream agent policy or task interface. Across six LLM backbones on ALFWorld ID-140 and ScienceWorld U211, CaSKG achieves the highest task score in all twelve combinations of model and benchmark. Relative to Graph-of-Skills (GoS), it improves the six-model macro-average ScienceWorld score from 72.62 to 80.50 and ALFWorld success from 80.01\% to 86.79\%, while reducing mean environment steps on both benchmarks. Qualitative and ablation analyses further show that calibrated edges help retrieval preserve prerequisites, state-changing actions, verification routines, and final completion steps. These results position edge-confidence calibration as an effective route to compact and executable skill retrieval at scale\footnote{Code is available at: https://github.com/ZhiyuanLi218/Caskg }.

摘要：可重用的技能庫允許大型語言模型（LLM）代理在任務之間重用程序知識，但它們也將記憶訪問轉變為一個具有挑戰性的檢索問題。完整庫提示在高上下文成本下保持覆蓋，向量檢索返回緊湊的鄰域，但將技能視為獨立文本，而基於圖形的檢索僅在承載相關性的邊緣可靠時才能恢復工作流程上下文。我們提出了CaSKG，一個反事實因果技能圖框架，在檢索之前校準程序關係。CaSKG首先從語義、詞彙、輸入/輸出和結構證據中構建一個高召回率的有向候選圖，修復證據和可選的LLM評判進一步細化候選分數。然後，它應用方向條件的文本反事實探針，移除、替換和重新排序技能對，使用貝葉斯平滑聚合證據，並發布一個狀態過濾的加權圖以進行任務條件擴展。該圖是在離線構建的，並在不改變下游代理政策或任務介面的情況下使用。在ALFWorld ID-140和ScienceWorld U211的六個LLM骨幹中，CaSKG在所有十二種模型和基準的組合中達到了最高的任務分數。相對於技能圖（GoS），它將六模型的宏平均ScienceWorld分數從72.62提高到80.50，ALFWorld的成功率從80.01\%提高到86.79\%，同時減少了兩個基準的平均環境步驟。定性和消融分析進一步顯示，校準的邊緣有助於檢索保持前提、狀態變更動作、驗證例程和最終完成步驟。這些結果將邊緣信心校準定位為在規模上實現緊湊和可執行技能檢索的有效途徑\footnote{代碼可在：https://github.com/ZhiyuanLi218/Caskg }。

##### **A Storage-Retrieval Gap in Parametric Knowledge Graph Memory**
2608.25489v1 by Martino M. L. Pulici, Cuong Xuan Chu, Evgeny Kharlamov, Volker Tresp

Graph retrieval-augmented generation places retrieved subgraphs into the model's context window at query time, paying a recurring token cost and exposing source data on every call. We study an alternative: compiling a knowledge graph offline into a bank of LoRA adapters, one per entity, that serve as a parametric knowledge layer queried by injecting weights rather than text, at zero query-time context cost. On the MetaQA dataset, we find that subgraph-trained adapters encode context-free factual knowledge that generalizes to unseen questions: on single-valued relations the adapter gains $+0.243$ exact-match score over a base model that is nearly blind closed-book ($0.007$), and only the correct adapter recovers this knowledge (an oracle gap of $+0.283$ over the base model). However, the stored knowledge is not recoverable by similarity: given a query with no subgraph, embedding-based and weight-space geometry retrieval both perform at chance, because a semantically neighbouring entity's adapter does not contain the answer - knowledge is stored locally and does not transfer. Weight geometry correlates with subgraph semantics ($ρ= +0.329$) but not with functional retrievability. We quantify the byte and context-token costs against graph retrieval-augmented generation and discuss deployment implications. Our results establish that parametric knowledge graph memory is feasible for storing knowledge, and identify selecting and composing the right adapters by a mechanism other than semantic similarity as the central open problem - motivating a learned, query-conditioned composition mechanism.

摘要：圖形檢索增強生成在查詢時將檢索到的子圖放入模型的上下文窗口，支付重複的標記成本並在每次調用時暴露源數據。我們研究了一種替代方案：將知識圖譜離線編譯成一個 LoRA 適配器庫，每個實體一個，作為一個參數化知識層，通過注入權重而非文本進行查詢，查詢時上下文成本為零。在 MetaQA 數據集上，我們發現子圖訓練的適配器編碼了無上下文的事實知識，這些知識可以推廣到未見過的問題：在單值關係上，適配器的精確匹配得分比幾乎盲目的基礎模型（$0.007$）提高了 $+0.243$，而且只有正確的適配器能夠恢復這些知識（相對於基礎模型的 oracle 差距為 $+0.283$）。然而，存儲的知識無法通過相似性恢復：給定一個沒有子圖的查詢，基於嵌入和權重空間幾何的檢索表現均為隨機，因為語義相鄰實體的適配器不包含答案——知識是局部存儲的，並不會轉移。權重幾何與子圖語義相關（$ρ= +0.329$），但與功能可檢索性無關。我們量化了與圖形檢索增強生成相比的字節和上下文標記成本，並討論了部署的影響。我們的結果確立了參數化知識圖譜記憶在存儲知識方面的可行性，並確定通過非語義相似性的機制選擇和組合正確的適配器是中心的開放問題——激勵了一種學習的、查詢條件的組合機制。

##### **ReliableRAG: Combating Misinformation in Retrieval-Augmented Generation via Reliability-Guided Reasoning Chains**
2608.25487v1 by Jinpu Jiang, Xuan Wu, Wenhao Song, Bo Yang, You Zhou, Hongwei Ge, Heow Pueh Lee, Yanchun Liang, Chunguo Wu

Retrieval-Augmented Generation (RAG) has emerged as a powerful architecture for Question Answering (QA) by integrating external information into Large Language Models (LLMs). However, false, inaccurate, and misleading information in news and social media poses a serious challenge to real-world RAG systems, especially in multi-hop QA, where complex multi-step reasoning can be misled by even a single deceptive misinformation segment in the retrieved documents. Existing approaches mainly rely on implicit alignment or explicit regulation, but their limited ability to assess fine-grained information reliability makes them vulnerable to deceptive misinformation that is semantically relevant to the question yet factually incorrect, leading to erroneous answers. To address this limitation, we propose ReliableRAG, which, to the best of our knowledge, is the first reliability-driven framework that mitigates deceptive misinformation in multi-hop QA through fine-grained evaluation of individual triples. ReliableRAG first extracts information segments from source documents and represents them as structured triples. It then quantifies triple reliability by combining query-triple semantic relevance with triple credibility, retaining only the top-$K$ reliable and non-redundant triples. Based on these refined triples, ReliableRAG autoregressively constructs robust reasoning chains to consolidate trustworthy evidence and filter deceptive misinformation, producing accurate answers faithful to reliable information. Experiments on three multi-hop QA datasets show that ReliableRAG outperforms existing methods, substantially improving the factual reliability and robustness of RAG systems under deceptive misinformation injection.

摘要：檢索增強生成（RAG）已成為一種強大的問題回答（QA）架構，通過將外部信息整合到大型語言模型（LLMs）中。然而，新聞和社交媒體中的虛假、不準確和誤導性信息對現實世界的 RAG 系統構成了嚴重挑戰，特別是在多跳 QA 中，複雜的多步推理甚至可能因檢索文檔中的單一誤導性錯誤信息而受到誤導。現有的方法主要依賴於隱式對齊或顯式規範，但它們在評估細粒度信息可靠性方面的能力有限，使其容易受到與問題語義相關但事實上不正確的誤導性錯誤信息的影響，導致錯誤的答案。為了解決這一限制，我們提出了 ReliableRAG，據我們所知，這是第一個基於可靠性的框架，通過對個別三元組的細粒度評估來減輕多跳 QA 中的誤導性錯誤信息。ReliableRAG 首先從源文檔中提取信息片段，並將其表示為結構化的三元組。然後，它通過將查詢-三元組語義相關性與三元組可信度相結合來量化三元組的可靠性，只保留前 $K$ 個可靠且不冗餘的三元組。基於這些精煉的三元組，ReliableRAG 自回歸地構建穩健的推理鏈，以整合可信的證據並過濾誤導性錯誤信息，生成忠於可靠信息的準確答案。在三個多跳 QA 數據集上的實驗顯示，ReliableRAG 超越了現有方法，顯著提高了 RAG 系統在誤導性錯誤信息注入下的事實可靠性和穩健性。

