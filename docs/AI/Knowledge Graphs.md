
### Knowledge Graphs
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-04-10**|**XFED: Non-Collusive Model Poisoning Attack Against Byzantine-Robust Federated Classifiers**|Israt Jahan Mouri et.al.|[2604.09489v1](http://arxiv.org/abs/2604.09489v1)|null|
|**2026-04-10**|**Process Reward Agents for Steering Knowledge-Intensive Reasoning**|Jiwoong Sohn et.al.|[2604.09482v1](http://arxiv.org/abs/2604.09482v1)|null|
|**2026-04-10**|**E3-TIR: Enhanced Experience Exploitation for Tool-Integrated Reasoning**|Weiyang Guo et.al.|[2604.09455v1](http://arxiv.org/abs/2604.09455v1)|null|
|**2026-04-10**|**UIPress: Bringing Optical Token Compression to UI-to-Code Generation**|Dasen Dai et.al.|[2604.09442v1](http://arxiv.org/abs/2604.09442v1)|null|
|**2026-04-10**|**Three Modalities, Two Design Probes, One Prototype, and No Vision: Experience-Based Co-Design of a Multi-modal 3D Data Visualization Tool**|Sanchita S. Kamath et.al.|[2604.09426v1](http://arxiv.org/abs/2604.09426v1)|null|
|**2026-04-10**|**Automated Instruction Revision (AIR): A Structured Comparison of Task Adaptation Strategies for LLM**|Solomiia Bilyk et.al.|[2604.09418v1](http://arxiv.org/abs/2604.09418v1)|null|
|**2026-04-10**|**Arbitration Failure, Not Perceptual Blindness: How Vision-Language Models Resolve Visual-Linguistic Conflicts**|Farhad Nooralahzadeh et.al.|[2604.09364v1](http://arxiv.org/abs/2604.09364v1)|null|
|**2026-04-10**|**SAGE: A Service Agent Graph-guided Evaluation Benchmark**|Ling Shi et.al.|[2604.09285v1](http://arxiv.org/abs/2604.09285v1)|null|
|**2026-04-10**|**DRBENCHER: Can Your Agent Identify the Entity, Retrieve Its Properties and Do the Math?**|Young-Suk Lee et.al.|[2604.09251v1](http://arxiv.org/abs/2604.09251v1)|null|
|**2026-04-10**|**The Fast Lane Hypothesis: Von Economo Neurons Implement a Biological Speed-Accuracy Tradeoff**|Esila Keskin et.al.|[2604.09229v1](http://arxiv.org/abs/2604.09229v1)|null|
|**2026-04-10**|**Facet-Level Tracing of Evidence Uncertainty and Hallucination in RAG**|Passant Elchafei et.al.|[2604.09174v1](http://arxiv.org/abs/2604.09174v1)|null|
|**2026-04-10**|**Structuring versus Problematizing: How LLM-based Agents Scaffold Learning in Diagnostic Reasoning**|Fatma Betül Güreş et.al.|[2604.09158v1](http://arxiv.org/abs/2604.09158v1)|null|
|**2026-04-10**|**Think Less, Know More: State-Aware Reasoning Compression with Knowledge Guidance for Efficient Reasoning**|Yi Sui et.al.|[2604.09150v1](http://arxiv.org/abs/2604.09150v1)|null|
|**2026-04-10**|**EquiformerV3: Scaling Efficient, Expressive, and General SE(3)-Equivariant Graph Attention Transformers**|Yi-Lun Liao et.al.|[2604.09130v1](http://arxiv.org/abs/2604.09130v1)|null|
|**2026-04-10**|**Prototype-Regularized Federated Learning for Cross-Domain Aspect Sentiment Triplet Extraction**|Zongming Cai et.al.|[2604.09123v1](http://arxiv.org/abs/2604.09123v1)|null|
|**2026-04-10**|**Beyond Isolated Clients: Integrating Graph-Based Embeddings into Event Sequence Models**|Harry Proshian et.al.|[2604.09085v1](http://arxiv.org/abs/2604.09085v1)|null|
|**2026-04-10**|**Skill-Conditioned Visual Geolocation for Vision-Language**|Chenjie Yang et.al.|[2604.09025v1](http://arxiv.org/abs/2604.09025v1)|null|
|**2026-04-10**|**Towards Linguistically-informed Representations for English as a Second or Foreign Language: Review, Construction and Application**|Wenxi Li et.al.|[2604.09008v1](http://arxiv.org/abs/2604.09008v1)|null|
|**2026-04-10**|**Quantisation Reshapes the Metacognitive Geometry of Language Models**|Jon-Paul Cacioli et.al.|[2604.08976v1](http://arxiv.org/abs/2604.08976v1)|null|
|**2026-04-10**|**TaxPraBen: A Scalable Benchmark for Structured Evaluation of LLMs in Chinese Real-World Tax Practice**|Gang Hu et.al.|[2604.08948v1](http://arxiv.org/abs/2604.08948v1)|null|
|**2026-04-10**|**Ge$^\text{2}$mS-T: Multi-Dimensional Grouping for Ultra-High Energy Efficiency in Spiking Transformer**|Zecheng Hao et.al.|[2604.08894v1](http://arxiv.org/abs/2604.08894v1)|null|
|**2026-04-10**|**A Closer Look at the Application of Causal Inference in Graph Representation Learning**|Hang Gao et.al.|[2604.08890v1](http://arxiv.org/abs/2604.08890v1)|null|
|**2026-04-09**|**SenBen: Sensitive Scene Graphs for Explainable Content Moderation**|Fatih Cagatay Akyon et.al.|[2604.08819v1](http://arxiv.org/abs/2604.08819v1)|null|
|**2026-04-09**|**Building Better Environments for Autonomous Cyber Defence**|Chris Hicks et.al.|[2604.08805v1](http://arxiv.org/abs/2604.08805v1)|null|
|**2026-04-09**|**Lessons Without Borders? Evaluating Cultural Alignment of LLMs Using Multilingual Story Moral Generation**|Sophie Wu et.al.|[2604.08797v1](http://arxiv.org/abs/2604.08797v1)|null|
|**2026-04-09**|**LLMs Underperform Graph-Based Parsers on Supervised Relation Extraction for Complex Graphs**|Paolo Gajo et.al.|[2604.08752v1](http://arxiv.org/abs/2604.08752v1)|null|
|**2026-04-09**|**Parameterized Complexity Of Representing Models Of MSO Formulas**|Petr Kučera et.al.|[2604.08707v1](http://arxiv.org/abs/2604.08707v1)|null|
|**2026-04-09**|**Act Wisely: Cultivating Meta-Cognitive Tool Use in Agentic Multimodal Models**|Shilin Yan et.al.|[2604.08545v1](http://arxiv.org/abs/2604.08545v1)|null|
|**2026-04-09**|**3D-VCD: Hallucination Mitigation in 3D-LLM Embodied Agents through Visual Contrastive Decoding**|Makanjuola Ogunleye et.al.|[2604.08645v1](http://arxiv.org/abs/2604.08645v1)|null|
|**2026-04-09**|**Cram Less to Fit More: Training Data Pruning Improves Memorization of Facts**|Jiayuan Ye et.al.|[2604.08519v1](http://arxiv.org/abs/2604.08519v1)|null|
|**2026-04-09**|**On Semiotic-Grounded Interpretive Evaluation of Generative Art**|Ruixiang Jiang et.al.|[2604.08641v1](http://arxiv.org/abs/2604.08641v1)|null|
|**2026-04-09**|**HST-HGN: Heterogeneous Spatial-Temporal Hypergraph Networks with Bidirectional State Space Models for Global Fatigue Assessment**|Changdao Chen et.al.|[2604.08435v1](http://arxiv.org/abs/2604.08435v1)|null|
|**2026-04-09**|**KV Cache Offloading for Context-Intensive Tasks**|Andrey Bocharnikov et.al.|[2604.08426v1](http://arxiv.org/abs/2604.08426v1)|null|
|**2026-04-09**|**Retrieval Augmented Classification for Confidential Documents**|Yeseul E. Chang et.al.|[2604.08628v1](http://arxiv.org/abs/2604.08628v1)|null|
|**2026-04-09**|**SkillClaw: Let Skills Evolve Collectively with Agentic Evolver**|Ziyu Ma et.al.|[2604.08377v1](http://arxiv.org/abs/2604.08377v1)|null|
|**2026-04-09**|**ASPECT:Analogical Semantic Policy Execution via Language Conditioned Transfer**|Ajsal Shereef Palattuparambil et.al.|[2604.08355v2](http://arxiv.org/abs/2604.08355v2)|null|
|**2026-04-09**|**Dead Weights, Live Signals: Feedforward Graphs of Frozen Language Models**|Marcus Armstrong et.al.|[2604.08335v1](http://arxiv.org/abs/2604.08335v1)|null|
|**2026-04-09**|**Securing Retrieval-Augmented Generation: A Taxonomy of Attacks, Defenses, and Future Directions**|Yuming Xu et.al.|[2604.08304v1](http://arxiv.org/abs/2604.08304v1)|null|
|**2026-04-09**|**U-CECE: A Universal Multi-Resolution Framework for Conceptual Counterfactual Explanations**|Angeliki Dimitriou et.al.|[2604.08295v1](http://arxiv.org/abs/2604.08295v1)|null|
|**2026-04-09**|**Distributed Multi-Layer Editing for Rule-Level Knowledge in Large Language Models**|Yating Wang et.al.|[2604.08284v1](http://arxiv.org/abs/2604.08284v1)|null|
|**2026-04-09**|**When to Trust Tools? Adaptive Tool Trust Calibration For Tool-Integrated Math Reasoning**|Ruotao Xu et.al.|[2604.08281v1](http://arxiv.org/abs/2604.08281v1)|null|
|**2026-04-09**|**Neural-Symbolic Knowledge Tracing: Injecting Educational Knowledge into Deep Learning for Responsible Learner Modelling**|Danial Hooshyar et.al.|[2604.08263v1](http://arxiv.org/abs/2604.08263v1)|null|
|**2026-04-09**|**Behavior-Aware Item Modeling via Dynamic Procedural Solution Representations for Knowledge Tracing**|Jun Seo et.al.|[2604.08260v1](http://arxiv.org/abs/2604.08260v1)|null|
|**2026-04-09**|**HyperMem: Hypergraph Memory for Long-Term Conversations**|Juwei Yue et.al.|[2604.08256v2](http://arxiv.org/abs/2604.08256v2)|null|
|**2026-04-09**|**Face-D(^2)CL: Multi-Domain Synergistic Representation with Dual Continual Learning for Facial DeepFake Detection**|Yushuo Zhang et.al.|[2604.08159v1](http://arxiv.org/abs/2604.08159v1)|null|
|**2026-04-09**|**Clickbait detection: quick inference with maximum impact**|Soveatin Kuntur et.al.|[2604.08148v1](http://arxiv.org/abs/2604.08148v1)|null|
|**2026-04-09**|**Graph Neural Networks for Misinformation Detection: Performance-Efficiency Trade-offs**|Soveatin Kuntur et.al.|[2604.08131v1](http://arxiv.org/abs/2604.08131v1)|null|
|**2026-04-09**|**Beyond Stochastic Exploration: What Makes Training Data Valuable for Agentic Search**|Chuzhan Hao et.al.|[2604.08124v1](http://arxiv.org/abs/2604.08124v1)|null|
|**2026-04-09**|**Uni-ViGU: Towards Unified Video Generation and Understanding via A Diffusion-Based Video Generator**|Luozheng Qin et.al.|[2604.08121v1](http://arxiv.org/abs/2604.08121v1)|null|
|**2026-04-09**|**OV-Stitcher: A Global Context-Aware Framework for Training-Free Open-Vocabulary Semantic Segmentation**|Seungjae Moon et.al.|[2604.08110v2](http://arxiv.org/abs/2604.08110v2)|null|
|**2026-04-09**|**Guaranteeing Knowledge Integration with Joint Decoding for Retrieval-Augmented Generation**|Zhengyi Zhao et.al.|[2604.08046v1](http://arxiv.org/abs/2604.08046v1)|null|
|**2026-04-09**|**3DrawAgent: Teaching LLM to Draw in 3D with Early Contrastive Experience**|Hongcan Xiao et.al.|[2604.08042v1](http://arxiv.org/abs/2604.08042v1)|null|
|**2026-04-09**|**IoT-Brain: Grounding LLMs for Semantic-Spatial Sensor Scheduling**|Zhaomeng Zhou et.al.|[2604.08033v1](http://arxiv.org/abs/2604.08033v1)|null|
|**2026-04-09**|**The ecosystem of machine learning competitions: Platforms, participants, and their impact on AI development**|Ioannis Nasios et.al.|[2604.08001v1](http://arxiv.org/abs/2604.08001v1)|null|
|**2026-04-09**|**DSCA: Dynamic Subspace Concept Alignment for Lifelong VLM Editing**|Gyanendra Das et.al.|[2604.07965v1](http://arxiv.org/abs/2604.07965v1)|null|
|**2026-04-09**|**Rethinking Data Mixing from the Perspective of Large Language Models**|Yuanjian Xu et.al.|[2604.07963v1](http://arxiv.org/abs/2604.07963v1)|null|
|**2026-04-09**|**On-Policy Distillation of Language Models for Autonomous Vehicle Motion Planning**|Amirhossein Afsharrad et.al.|[2604.07944v1](http://arxiv.org/abs/2604.07944v1)|null|
|**2026-04-09**|**AnomalyAgent: Agentic Industrial Anomaly Synthesis via Tool-Augmented Reinforcement Learning**|Jiaming Su et.al.|[2604.07900v1](http://arxiv.org/abs/2604.07900v1)|null|
|**2026-04-09**|**SkillForge: Forging Domain-Specific, Self-Evolving Agent Skills in Cloud Technical Support**|Xingyan Liu et.al.|[2604.08618v1](http://arxiv.org/abs/2604.08618v1)|null|
|**2026-04-09**|**MemReader: From Passive to Active Extraction for Long-Term Agent Memory**|Jingyi Kang et.al.|[2604.07877v2](http://arxiv.org/abs/2604.07877v2)|null|
|**2026-04-09**|**PyVRP$^+$: LLM-Driven Metacognitive Heuristic Evolution for Hybrid Genetic Search in Vehicle Routing Problems**|Manuj Malik et.al.|[2604.07872v1](http://arxiv.org/abs/2604.07872v1)|null|
|**2026-04-09**|**From Selection to Scheduling: Federated Geometry-Aware Correction Makes Exemplar Replay Work Better under Continual Dynamic Heterogeneity**|Zhuang Qi et.al.|[2604.08617v1](http://arxiv.org/abs/2604.08617v1)|null|
|**2026-04-09**|**ReRec: Reasoning-Augmented LLM-based Recommendation Assistant via Reinforcement Fine-tuning**|Jiani Huang et.al.|[2604.07851v1](http://arxiv.org/abs/2604.07851v1)|null|
|**2026-04-09**|**Filling the Gaps: Selective Knowledge Augmentation for LLM Recommenders**|Jaehyun Lee et.al.|[2604.07825v1](http://arxiv.org/abs/2604.07825v1)|null|
|**2026-04-09**|**Loop, Think, & Generalize: Implicit Reasoning in Recurrent-Depth Transformers**|Harsh Kohli et.al.|[2604.07822v1](http://arxiv.org/abs/2604.07822v1)|null|
|**2026-04-09**|**More Capable, Less Cooperative? When LLMs Fail At Zero-Cost Collaboration**|Advait Yadav et.al.|[2604.07821v1](http://arxiv.org/abs/2604.07821v1)|null|
|**2026-04-09**|**Latent Anomaly Knowledge Excavation: Unveiling Sparse Sensitive Neurons in Vision-Language Models**|Shaotian Li et.al.|[2604.07802v1](http://arxiv.org/abs/2604.07802v1)|null|
|**2026-04-09**|**Lightweight LLM Agent Memory with Small Language Models**|Jiaquan Zhang et.al.|[2604.07798v1](http://arxiv.org/abs/2604.07798v1)|null|
|**2026-04-09**|**SEARL: Joint Optimization of Policy and Tool Graph Memory for Self-Evolving Agents**|Xinshun Feng et.al.|[2604.07791v1](http://arxiv.org/abs/2604.07791v1)|null|
|**2026-04-09**|**PeReGrINE: Evaluating Personalized Review Fidelity with User Item Graph Context**|Steven Au et.al.|[2604.07788v1](http://arxiv.org/abs/2604.07788v1)|null|
|**2026-04-09**|**Automotive Engineering-Centric Agentic AI Workflow Framework**|Tong Duy Son et.al.|[2604.07784v1](http://arxiv.org/abs/2604.07784v1)|null|
|**2026-04-09**|**Toward Generalizable Graph Learning for 3D Engineering AI: Explainable Workflows for CAE Mode Shape Classification and CFD Field Prediction**|Tong Duy Son et.al.|[2604.07781v1](http://arxiv.org/abs/2604.07781v1)|null|
|**2026-04-09**|**The Accountability Horizon: An Impossibility Theorem for Governing Human-Agent Collectives**|Haileleol Tibebu et.al.|[2604.07778v1](http://arxiv.org/abs/2604.07778v1)|null|
|**2026-04-09**|**Beyond Surface Artifacts: Capturing Shared Latent Forgery Knowledge Across Modalities**|Jingtong Dou et.al.|[2604.07763v1](http://arxiv.org/abs/2604.07763v1)|null|
|**2026-04-09**|**Symbiotic-MoE: Unlocking the Synergy between Generation and Understanding**|Xiangyue Liu et.al.|[2604.07753v1](http://arxiv.org/abs/2604.07753v1)|null|
|**2026-04-09**|**Beyond Social Pressure: Benchmarking Epistemic Attack in Large Language Models**|Steven Au et.al.|[2604.07749v1](http://arxiv.org/abs/2604.07749v1)|null|
|**2026-04-09**|**Towards Knowledgeable Deep Research: Framework and Benchmark**|Wenxuan Liu et.al.|[2604.07720v2](http://arxiv.org/abs/2604.07720v2)|null|
|**2026-04-09**|**IatroBench: Pre-Registered Evidence of Iatrogenic Harm from AI Safety Measures**|David Gringras et.al.|[2604.07709v1](http://arxiv.org/abs/2604.07709v1)|null|
|**2026-04-08**|**Efficient and Effective Internal Memory Retrieval for LLM-Based Healthcare Prediction**|Mingchen Li et.al.|[2604.07659v1](http://arxiv.org/abs/2604.07659v1)|null|
|**2026-04-08**|**ADAG: Automatically Describing Attribution Graphs**|Aryaman Arora et.al.|[2604.07615v1](http://arxiv.org/abs/2604.07615v1)|null|
|**2026-04-08**|**Reasoning Graphs: Deterministic Agent Accuracy through Evidence-Centric Chain-of-Thought Feedback**|Matthew Penaroza et.al.|[2604.07595v1](http://arxiv.org/abs/2604.07595v1)|null|
|**2026-04-08**|**DCD: Domain-Oriented Design for Controlled Retrieval-Augmented Generation**|Valeriy Kovalskiy et.al.|[2604.07590v1](http://arxiv.org/abs/2604.07590v1)|null|
|**2026-04-08**|**CLEAR: Context Augmentation from Contrastive Learning of Experience via Agentic Reflection**|Linbo Liu et.al.|[2604.07487v1](http://arxiv.org/abs/2604.07487v1)|null|
|**2026-04-08**|**M-ArtAgent: Evidence-Based Multimodal Agent for Implicit Art Influence Discovery**|Hanyi Liu et.al.|[2604.07468v1](http://arxiv.org/abs/2604.07468v1)|null|
|**2026-04-08**|**Cross-Tokenizer LLM Distillation through a Byte-Level Interface**|Avyav Kumar Singh et.al.|[2604.07466v1](http://arxiv.org/abs/2604.07466v1)|null|
|**2026-04-08**|**Chatbot-Based Assessment of Code Understanding in Automated Programming Assessment Systems**|Eduard Frankford et.al.|[2604.07304v1](http://arxiv.org/abs/2604.07304v1)|null|
|**2026-04-08**|**Region-Graph Optimal Transport Routing for Mixture-of-Experts Whole-Slide Image Classification**|Xin Tian et.al.|[2604.07298v1](http://arxiv.org/abs/2604.07298v1)|null|
|**2026-04-08**|**A Systematic Study of Retrieval Pipeline Design for Retrieval-Augmented Medical Question Answering**|Nusrat Sultana et.al.|[2604.07274v1](http://arxiv.org/abs/2604.07274v1)|null|
|**2026-04-08**|**Joint Optimization of Reasoning and Dual-Memory for Self-Learning Diagnostic Agent**|Bingxuan Li et.al.|[2604.07269v1](http://arxiv.org/abs/2604.07269v1)|null|
|**2026-04-08**|**Validated Intent Compilation for Constrained Routing in LEO Mega-Constellations**|Yuanhang Li et.al.|[2604.07264v1](http://arxiv.org/abs/2604.07264v1)|null|
|**2026-04-08**|**$k$-server-bench: Automating Potential Discovery for the $k$-Server Conjecture**|Kirill Brilliantov et.al.|[2604.07240v1](http://arxiv.org/abs/2604.07240v1)|null|
|**2026-04-08**|**LaScA: Language-Conditioned Scalable Modelling of Affective Dynamics**|Kosmas Pinitas et.al.|[2604.07193v1](http://arxiv.org/abs/2604.07193v1)|null|
|**2026-04-08**|**Self-Discovered Intention-aware Transformer for Multi-modal Vehicle Trajectory Prediction**|Diyi Liu et.al.|[2604.07126v1](http://arxiv.org/abs/2604.07126v1)|null|
|**2026-04-08**|**Information as Structural Alignment: A Dynamical Theory of Continual Learning**|Radu Negulescu et.al.|[2604.07108v1](http://arxiv.org/abs/2604.07108v1)|null|
|**2026-04-08**|**The Impact of Steering Large Language Models with Persona Vectors in Educational Applications**|Yongchao Wu et.al.|[2604.07102v1](http://arxiv.org/abs/2604.07102v1)|null|
|**2026-04-08**|**Selective Neuron Amplification for Training-Free Task Enhancement**|Ryyan Akhtar et.al.|[2604.07098v1](http://arxiv.org/abs/2604.07098v1)|null|
|**2026-04-08**|**A-MBER: Affective Memory Benchmark for Emotion Recognition**|Deliang Wen et.al.|[2604.07017v1](http://arxiv.org/abs/2604.07017v1)|null|
|**2026-04-08**|**Corpora deduplication or duplication in Natural Language Processing of few resourced languages ? A case of study: The Mexico's Nahuatl**|Juan-José Guzman-Landa et.al.|[2604.07015v1](http://arxiv.org/abs/2604.07015v1)|null|
|**2026-04-08**|**DTCRS: Dynamic Tree Construction for Recursive Summarization**|Guanran Luo et.al.|[2604.07012v1](http://arxiv.org/abs/2604.07012v1)|null|
|**2026-04-08**|**FORGE:Fine-grained Multimodal Evaluation for Manufacturing Scenarios**|Xiangru Jian et.al.|[2604.07413v1](http://arxiv.org/abs/2604.07413v1)|null|

#### Abstracts
##### **XFED: Non-Collusive Model Poisoning Attack Against Byzantine-Robust Federated Classifiers**
2604.09489v1 by Israt Jahan Mouri, Muhammad Ridowan, Muhammad Abdullah Adnan

Model poisoning attacks pose a significant security threat to Federated Learning (FL). Most existing model poisoning attacks rely on collusion, requiring adversarial clients to coordinate by exchanging local benign models and synchronizing the generation of their poisoned updates. However, sustaining such coordination is increasingly impractical in real-world FL deployments, as it effectively requires botnet-like control over many devices. This approach is costly to maintain and highly vulnerable to detection. This context raises a fundamental question: Can model poisoning attacks remain effective without any communication between attackers? To address this challenge, we introduce and formalize the \textbf{non-collusive attack model}, in which all compromised clients share a common adversarial objective but operate independently. Under this model, each attacker generates its malicious update without communicating with other adversaries, accessing other clients' updates, or relying on any knowledge of server-side defenses. To demonstrate the feasibility of this threat model, we propose \textbf{XFED}, the first aggregation-agnostic, non-collusive model poisoning attack. Our empirical evaluation across six benchmark datasets shows that XFED bypasses eight state-of-the-art defenses and outperforms six existing model poisoning attacks. These findings indicate that FL systems are substantially less secure than previously believed and underscore the urgent need for more robust and practical defense mechanisms.

摘要：模型中毒攻擊對聯邦學習（FL）構成了重大安全威脅。大多數現有的模型中毒攻擊依賴於共謀，要求對抗客戶端通過交換本地良性模型和同步生成其中毒更新來協調。然而，在現實世界的 FL 部署中，維持這種協調變得越來越不切實際，因為這實際上需要對許多設備進行類似僵尸網絡的控制。這種方法維護成本高昂，且極易被檢測。這一背景提出了一個根本性問題：在攻擊者之間沒有任何通信的情況下，模型中毒攻擊能否保持有效？為了解決這一挑戰，我們引入並正式化了 \textbf{非共謀攻擊模型}，在這個模型中，所有受損客戶端共享一個共同的對抗目標，但獨立運作。在這個模型下，每個攻擊者在不與其他對手通信、不訪問其他客戶端的更新或不依賴於任何伺服器端防禦知識的情況下生成其惡意更新。為了證明這一威脅模型的可行性，我們提出了 \textbf{XFED}，這是第一個聚合無關的非共謀模型中毒攻擊。我們在六個基準數據集上的實證評估顯示，XFED 繞過了八種最先進的防禦，並超越了六種現有的模型中毒攻擊。這些發現表明，FL 系統的安全性遠低於先前的認知，並強調了對更強大和實用的防禦機制的迫切需求。

##### **Process Reward Agents for Steering Knowledge-Intensive Reasoning**
2604.09482v1 by Jiwoong Sohn, Tomasz Sternal, Kenneth Styppa, Torsten Hoefler, Michael Moor

Reasoning in knowledge-intensive domains remains challenging as intermediate steps are often not locally verifiable: unlike math or code, evaluating step correctness may require synthesizing clues across large external knowledge sources. As a result, subtle errors can propagate through reasoning traces, potentially never to be detected. Prior work has proposed process reward models (PRMs), including retrieval-augmented variants, but these methods operate post hoc, scoring completed trajectories, which prevents their integration into dynamic inference procedures. Here, we introduce Process Reward Agents (PRA), a test-time method for providing domain-grounded, online, step-wise rewards to a frozen policy. In contrast to prior retrieval-augmented PRMs, PRA enables search-based decoding to rank and prune candidate trajectories at every generation step. Experiments on multiple medical reasoning benchmarks demonstrate that PRA consistently outperforms strong baselines, achieving 80.8% accuracy on MedQA with Qwen3-4B, a new state of the art at the 4B scale. Importantly, PRA generalizes to unseen frozen policy models ranging from 0.5B to 8B parameters, improving their accuracy by up to 25.7% without any policy model updates. More broadly, PRA suggests a paradigm in which frozen reasoners are decoupled from domain-specific reward modules, allowing the deployment of new backbones in complex domains without retraining.

摘要：知識密集型領域的推理仍然具有挑戰性，因為中間步驟往往無法在本地驗證：與數學或程式碼不同，評估步驟的正確性可能需要綜合來自大型外部知識來源的線索。結果，微妙的錯誤可能會在推理過程中傳播，潛在地永遠無法被檢測到。先前的研究提出了過程獎勵模型（PRMs），包括檢索增強變體，但這些方法是在事後運作，對已完成的軌跡進行評分，這阻礙了它們在動態推理程序中的整合。在這裡，我們介紹了過程獎勵代理（PRA），這是一種在測試時提供基於領域的、在線的、逐步獎勵給冷凍策略的方法。與先前的檢索增強PRMs相比，PRA使基於搜索的解碼能夠在每個生成步驟中對候選軌跡進行排名和修剪。在多個醫療推理基準上的實驗表明，PRA始終超越強基線，在MedQA上以Qwen3-4B達到80.8%的準確率，這是4B規模的新最先進技術。重要的是，PRA能夠推廣到未見過的冷凍策略模型，參數範圍從0.5B到8B，將其準確性提高了多達25.7%，而無需任何策略模型更新。更廣泛地說，PRA提出了一種範式，其中冷凍推理器與特定領域的獎勵模塊解耦，允許在複雜領域中部署新的骨幹而無需重新訓練。

##### **E3-TIR: Enhanced Experience Exploitation for Tool-Integrated Reasoning**
2604.09455v1 by Weiyang Guo, Zesheng Shi, Liye Zhao, Jiayuan Ma, Zeen Zhu, Junxian He, Min Zhang, Jing Li

While Large Language Models (LLMs) have demonstrated significant potential in Tool-Integrated Reasoning (TIR), existing training paradigms face significant limitations: Zero-RL suffers from inefficient exploration and mode degradation due to a lack of prior guidance, while SFT-then-RL is limited by high data costs and capability plateaus caused by low-entropy collapse. To address these challenges, we propose E3-TIR (Enhanced Experience Exploitation), a warm-up paradigm for the early stages of agent training. Specifically, we formulate training as the dynamic integration of three experience types: Expert Prefixes, Expert Guided, and Self-Exploration. By executing diverse branching exploration around expert "anchors" and employing a mix policy optimization mechanism, we effectively mitigate distribution shifts and resolve optimization conflicts arising from shared prefixes. Our method dynamically adapts the model's knowledge boundaries, effectively balancing exploration diversity with training efficiency.Experimental results demonstrate that E3-TIR achieves a 6 performance improvement over traditional paradigms on tool-use tasks, while requiring less than 10 of the synthetic data. Furthermore, in terms of ROI, a comprehensive metric integrating performance, data cost, and training efficiency we achieve a 1.46x gain compared to baselines. Code is available at https://github.com/yuki-younai/E3-TIR.

摘要：儘管大型語言模型（LLMs）在工具整合推理（TIR）方面顯示出顯著潛力，但現有的訓練範式面臨重大限制：零強化學習（Zero-RL）因缺乏先前指導而遭受低效探索和模式退化，而先訓練再強化學習（SFT-then-RL）則受到高數據成本和由低熵崩潰引起的能力平台的限制。為了解決這些挑戰，我們提出了E3-TIR（增強經驗利用），這是一種用於代理訓練早期階段的熱身範式。具體來說，我們將訓練公式化為三種類型經驗的動態整合：專家前綴、專家引導和自我探索。通過在專家“錨點”周圍執行多樣的分支探索並採用混合策略優化機制，我們有效減輕了分佈轉移並解決了由共享前綴引起的優化衝突。我們的方法動態調整模型的知識邊界，有效平衡探索多樣性與訓練效率。實驗結果表明，E3-TIR在工具使用任務上相比傳統範式實現了6的性能提升，同時所需的合成數據少於10。此外，在投資回報率（ROI）方面，綜合性能、數據成本和訓練效率的綜合指標，我們相比基準實現了1.46倍的增益。代碼可在 https://github.com/yuki-younai/E3-TIR 獲得。

##### **UIPress: Bringing Optical Token Compression to UI-to-Code Generation**
2604.09442v1 by Dasen Dai, Shuoqi Li, Ronghao Chen, Huacan Wang, Biao Wu, Qizhen Lan

UI-to-Code generation requires vision-language models (VLMs) to produce thousands of tokens of structured HTML/CSS from a single screenshot, making visual token efficiency critical. Existing compression methods either select tokens at inference time using task-agnostic heuristics, or zero out low-attention features without actually shortening the sequence -- neither truly reduces prefill latency or adapts to the non-uniform information density of UI screenshots. Meanwhile, optical (encoder-side learned) compression has shown strong results for document OCR, yet no prior work has adapted this paradigm to UI-to-Code generation. We propose UIPress, a lightweight learned compression module inserted between the frozen ViT encoder and the LLM decoder of Qwen3-VL-8B. UIPress combines depthwise-separable convolutions, element-guided spatial reweighting, and Transformer refinement to compress ${\sim}$6{,}700 visual tokens to a fixed budget of 256. Together with Low-Rank Adaptation (LoRA) on the decoder to bridge the representation gap, the entire system adds only ${\sim}$21.7M trainable parameters (0.26\% of the 8B base model). Under a fair comparison on the same base model against four baselines on Design2Code, UIPress at 256 tokens achieves a CLIP score of 0.8127, outperforming the uncompressed baseline by +7.5\% and the strongest inference-time method by +4.6\%, while delivering 9.1$\times$ time-to-first-token speedup. To the best of our knowledge, UIPress is the first encoder-side learned compression method for the UI-to-Code task.

摘要：UI-to-Code 生成需要視覺語言模型 (VLMs) 從單一截圖生成數千個結構化的 HTML/CSS 標記，使得視覺標記效率至關重要。現有的壓縮方法要麼在推理時使用與任務無關的啟發式方法選擇標記，要麼在不實際縮短序列的情況下將低注意力特徵歸零——這兩者都無法真正減少預填延遲或適應 UI 截圖的非均勻信息密度。與此同時，光學（編碼器端學習的）壓縮在文檔 OCR 中顯示出強勁的結果，但之前的研究尚未將這一範式應用於 UI-to-Code 生成。我們提出了 UIPress，一個輕量級的學習壓縮模塊，插入在凍結的 ViT 編碼器和 Qwen3-VL-8B 的 LLM 解碼器之間。UIPress 結合了深度可分離卷積、元素引導的空間重加權和 Transformer 精煉，將約 6,700 個視覺標記壓縮到固定的 256 預算。連同在解碼器上進行低秩適應 (LoRA) 以彌補表示差距，整個系統僅增加約 21.7M 可訓練參數（佔 8B 基礎模型的 0.26\%）。在對同一基礎模型進行公平比較時，對比四個基準，在 Design2Code 上，UIPress 在 256 個標記下達到 0.8127 的 CLIP 分數，超過未壓縮基準 +7.5\% 和最強的推理時方法 +4.6\%，同時提供 9.1$\times$ 的首次標記速度提升。據我們所知，UIPress 是 UI-to-Code 任務中首個編碼器端學習的壓縮方法。

##### **Three Modalities, Two Design Probes, One Prototype, and No Vision: Experience-Based Co-Design of a Multi-modal 3D Data Visualization Tool**
2604.09426v1 by Sanchita S. Kamath, Aziz N Zeidieh, Venkatesh Potluri, Sile O'Modhrain, Kenneth Perry, JooYoung Seo

Three-dimensional (3D) data visualizations, such as surface plots, are vital in STEM fields from biomedical imaging to spectroscopy, yet remain largely inaccessible to blind and low-vision (BLV) people. To address this gap, we conducted an Experience-Based Co-Design with BLV co-designers with expertise in non-visual data representations to create an accessible, multi-modal, web-native visualization tool. Using a multi-phase methodology, our team of five BLV and one non-BLV researcher(s) participated in two iterative sessions, comparing a low-fidelity tactile probe with a high-fidelity digital prototype. This process produced a prototype with empirically grounded features, including reference sonification, stereo and volumetric audio, and configurable buffer aggregation, which our co-designers validated as improving analytic accuracy and learnability. In this study, we target core analytic tasks essential for non-visual 3D data exploration: orientation, landmark and peak finding, comparing local maxima versus global trends, gradient tracing, and identifying occluded or partially hidden features. Our work offers accessibility researchers and developers a co-design protocol for translating tactile knowledge to digital interfaces, concrete design guidance for future systems, and opportunities to extend accessible 3D visualization into embodied data environments.

摘要：三維 (3D) 數據視覺化，例如表面圖，在從生物醫學影像到光譜學的 STEM 領域中至關重要，但對於盲人和低視力 (BLV) 人士來說仍然難以接觸。為了解決這一問題，我們與具有非視覺數據表示專業知識的 BLV 共同設計者進行了基於經驗的共同設計，以創建一個可訪問的、多模態的、網頁原生的視覺化工具。使用多階段的方法論，我們的五名 BLV 和一名非 BLV 研究者團隊參加了兩次迭代會議，將低保真觸覺探針與高保真數位原型進行比較。這一過程產生了一個具有實證基礎特徵的原型，包括參考聲音化、立體和體積音頻以及可配置的緩衝聚合，我們的共同設計者驗證了這些特徵能提高分析準確性和學習能力。在本研究中，我們針對非視覺 3D 數據探索的核心分析任務：定向、地標和峰值查找、比較局部極大值與全局趨勢、梯度追踪，以及識別被遮擋或部分隱藏的特徵。我們的工作為可訪問性研究者和開發者提供了一個共同設計協議，用於將觸覺知識轉換為數位介面，為未來系統提供具體的設計指導，並擴展可訪問的 3D 視覺化至具身數據環境的機會。

##### **Automated Instruction Revision (AIR): A Structured Comparison of Task Adaptation Strategies for LLM**
2604.09418v1 by Solomiia Bilyk, Volodymyr Getmanskyi, Taras Firman

This paper studies Automated Instruction Revision (AIR), a rule-induction-based method for adapting large language models (LLMs) to downstream tasks using limited task-specific examples. We position AIR within the broader landscape of adaptation strategies, including prompt optimization, retrieval-based methods, and fine-tuning. We then compare these approaches across a diverse benchmark suite designed to stress different task requirements, such as knowledge injection, structured extraction, label remapping, and logical reasoning. The paper argues that adaptation performance is strongly task-dependent: no single method dominates across all settings. Across five benchmarks, AIR was strongest or near-best on label-remapping classification, while KNN retrieval performed best on closed-book QA, and fine-tuning dominated structured extraction and event-order reasoning. AIR is most promising when task behavior can be captured by compact, interpretable instruction rules, while retrieval and fine-tuning remain stronger in tasks dominated by source-specific knowledge or dataset-specific annotation regularities.

摘要：這篇論文研究了自動化指令修訂（AIR），這是一種基於規則歸納的方法，用於利用有限的任務特定範例將大型語言模型（LLMs）調整到下游任務。我們將AIR定位於更廣泛的適應策略範疇中，包括提示優化、基於檢索的方法和微調。接著，我們在一個多樣的基準套件中比較這些方法，該套件旨在強調不同的任務需求，例如知識注入、結構化提取、標籤重映射和邏輯推理。論文主張，適應性能與任務密切相關：沒有單一方法在所有設置中都佔優。 在五個基準中，AIR在標籤重映射分類上表現最強或接近最佳，而KNN檢索在閉卷問答中表現最佳，微調則在結構化提取和事件順序推理中佔主導地位。當任務行為可以通過簡潔、可解釋的指令規則捕捉時，AIR最具潛力，而在以源特定知識或數據集特定註釋規律為主的任務中，檢索和微調仍然更強。

##### **Arbitration Failure, Not Perceptual Blindness: How Vision-Language Models Resolve Visual-Linguistic Conflicts**
2604.09364v1 by Farhad Nooralahzadeh, Omid Rohanian, Yi Zhang, Jonathan Fürst, Kurt Stockinger

When a Vision-Language Model (VLM) sees a blue banana and answers "yellow", is the problem of perception or arbitration? We explore the question in ten VLMs with various sizes and reveal an Encoding--Grounding Dissociation: models that fail to report what they see (and thus provide a wrong answer) still encode the visual evidence as strongly as models that provide the correct answer. Using Multimodal Arbitration Crossover (MAC) analysis with layer-by-layer Logit Lens probing, we track the competition between visual and prior signals across every layer of each model. We show that visual attributes can be linearly decodable from early layers (AUC > 0.86). The accuracy remains nearly identical for both successful and failed samples. However, the gap in the final-layer logit -- not the strength of encoding -- better predicts grounding outcomes with a correlation of . After having studied when VLMs base their answers on image clues rather than prior knowledge, we want to understand the causal relationships. We establish causality through full-sequence activation patching. The standard last-token interventions in LLM interpretability do not affect VLMs. In contrast, replacing the full token sequence at layers identified by MAC alters 60 to 84% of outputs. Partial-token decomposition shows that image tokens carry almost all of the causal impact, while text tokens have none. Scaling addresses the remaining architectural differences to achieve perfect retention. Moving from diagnosis to intervention, we show that training-free activation steering -- both linear and sparse autoencoder-guided -- in early layers can improve visual grounding by up to +3.8% with degrading performance in some setups. Overall, these findings lead to a clear conclusion: VLMs already see well, but the challenge is acting on what they see. Targeted interventions can help to bridge this gap.

摘要：當一個視覺-語言模型（VLM）看到一根藍色的香蕉並回答「黃色」，這是感知還是仲裁的問題？我們在十個不同大小的VLM中探討這個問題，並揭示了一個編碼-基礎分離現象：那些未能報告所見（因此提供錯誤答案）的模型，仍然像提供正確答案的模型一樣強烈地編碼視覺證據。通過使用多模態仲裁交叉（MAC）分析，並進行逐層的Logit Lens探測，我們追蹤每個模型每一層中視覺信號與先前信號之間的競爭。我們顯示視覺屬性可以從早期層線性解碼（AUC > 0.86）。成功樣本和失敗樣本的準確性幾乎相同。然而，最終層logit的差距——而不是編碼的強度——更好地預測了基礎結果，相關性為。經過研究VLM何時基於圖像線索而非先前知識來給出答案後，我們希望理解因果關係。我們通過全序列激活修補建立因果關係。LLM可解釋性中的標準最後令牌干預不會影響VLM。相反，在MAC識別的層中替換整個令牌序列會改變60%到84%的輸出。部分令牌分解顯示圖像令牌幾乎承載了所有的因果影響，而文本令牌則沒有。縮放解決了剩餘的架構差異，以實現完美保留。從診斷轉向干預，我們顯示無需訓練的激活引導——包括線性和稀疏自編碼器引導——在早期層中可以使視覺基礎提高多達+3.8%，但在某些設置中性能會下降。總體而言，這些發現得出了一個明確的結論：VLM已經能夠很好地看見，但挑戰在於如何根據所見行動。針對性的干預可以幫助彌補這一差距。

##### **SAGE: A Service Agent Graph-guided Evaluation Benchmark**
2604.09285v1 by Ling Shi, Yuqin Dai, Ziyin Wang, Ning Gao, Wei Zhang, Chaozheng Wang, Yujie Wang, Wei He, Jinpeng Wang, Deiyi Xiong

The development of Large Language Models (LLMs) has catalyzed automation in customer service, yet benchmarking their performance remains challenging. Existing benchmarks predominantly rely on static paradigms and single-dimensional metrics, failing to account for diverse user behaviors or the strict adherence to structured Standard Operating Procedures (SOPs) required in real-world deployments. To bridge this gap, we propose SAGE (Service Agent Graph-guided Evaluation), a universal multi-agent benchmark for automated, dual-axis assessment. SAGE formalizes unstructured SOPs into Dynamic Dialogue Graphs, enabling precise verification of logical compliance and comprehensive path coverage. We introduce an Adversarial Intent Taxonomy and a modular Extension Mechanism, enabling low-cost deployment across domains and facilitating automated dialogue data synthesis. Evaluation is conducted via a framework where Judge Agents and a Rule Engine analyze interactions between User and Service Agents to generate deterministic ground truth. Extensive experiments on 27 LLMs across 6 industrial scenarios reveal a significant ``Execution Gap'' where models accurately classify intents but fail to derive correct subsequent actions. We also observe ``Empathy Resilience'', a phenomenon where models maintain polite conversational facades despite underlying logical failures under high adversarial intensity. Code and resources are available at https://anonymous.4open.science/r/SAGE-Bench-4CD3/.

摘要：大型語言模型（LLMs）的發展促進了客戶服務的自動化，但基準測試其性能仍然具有挑戰性。現有的基準主要依賴靜態範式和單維度指標，未能考慮多樣化的用戶行為或在現實部署中對結構化標準操作程序（SOPs）的嚴格遵循。為了填補這一空白，我們提出了SAGE（服務代理圖導向評估），這是一個通用的多代理基準，用於自動化的雙軸評估。SAGE將非結構化的SOP形式化為動態對話圖，能夠精確驗證邏輯合規性和全面的路徑覆蓋。我們引入了一個對抗性意圖分類法和一個模塊化擴展機制，使得在各個領域的低成本部署成為可能，並促進自動化對話數據的合成。評估通過一個框架進行，其中評判代理和規則引擎分析用戶和服務代理之間的互動，以生成確定性的真實數據。在6個工業場景中對27個LLM進行的廣泛實驗顯示出一個顯著的“執行差距”，即模型準確分類意圖但未能推導出正確的後續行動。我們還觀察到“同理心韌性”，這是一種現象，即模型在高對抗強度下仍能保持禮貌的對話外表，儘管存在潛在的邏輯失誤。代碼和資源可在 https://anonymous.4open.science/r/SAGE-Bench-4CD3/ 獲得。

##### **DRBENCHER: Can Your Agent Identify the Entity, Retrieve Its Properties and Do the Math?**
2604.09251v1 by Young-Suk Lee, Ramon Fernandez Astudillo, Radu Florian

Deep research agents increasingly interleave web browsing with multi-step computation, yet existing benchmarks evaluate these capabilities in isolation, creating a blind spot in assessing real-world performance. We introduce DRBENCHER, a synthetic benchmark generator for questions that require both browsing and computation. It enforces four criteria: verifiability (gold answers are computed by executing parameterized code over knowledge-graph values), complexity (multi-hop entity identification, property retrieval, and domain-specific computation), difficulty (a two-stage verification cascade filters out questions solvable by the generating model), and diversity (a greedy max-min embedding filter maximizes coverage). These criteria are realized via a unified answer-first pipeline spanning five domains: biochemistry, financial, geophysical, security, and history. Human evaluation shows 76% validity (84% excluding stale data), with 35% of errors due to outdated knowledge-graph entries, highlighting an inherent limitation of systems that reason over evolving data. Automatic evaluation shows that the strongest frontier model achieves only 20% answer accuracy. Compared to manually constructed benchmarks (BrowseComp+, MATH-500, GPQA), DRBENCHER achieves the highest semantic diversity.

摘要：深度研究代理越來越多地將網頁瀏覽與多步計算交織在一起，但現有基準測試在孤立的情況下評估這些能力，造成在評估現實世界表現時的盲點。我們介紹了DRBENCHER，一個合成基準生成器，用於需要瀏覽和計算的問題。它強制執行四個標準：可驗證性（黃金答案是通過執行參數化代碼在知識圖譜值上計算得出的）、複雜性（多跳實體識別、屬性檢索和特定領域計算）、難度（兩階段驗證級聯過濾掉生成模型可解的問題）和多樣性（貪婪的最大-最小嵌入過濾器最大化覆蓋範圍）。這些標準通過一個統一的答案優先管道實現，涵蓋五個領域：生物化學、金融、地球物理、安全和歷史。人類評估顯示76%的有效性（排除過時數據後為84%），其中35%的錯誤是由於過時的知識圖譜條目，突顯了對於不斷演變數據進行推理的系統的固有限制。自動評估顯示，最強的前沿模型僅達到20%的答案準確率。與手動構建的基準（BrowseComp+、MATH-500、GPQA）相比，DRBENCHER實現了最高的語義多樣性。

##### **The Fast Lane Hypothesis: Von Economo Neurons Implement a Biological Speed-Accuracy Tradeoff**
2604.09229v1 by Esila Keskin

Von Economo neurons (VENs) are large bipolar projection neurons found exclusively in the anterior cingulate cortex (ACC) and frontal insula of species with complex social cognition, including humans, great apes, and cetaceans. Their selective depletion in frontotemporal dementia (FTD) and altered development in autism implicate them in rapid social decision-making, yet no computational model of VEN function has previously existed. We introduce the Fast Lane Hypothesis: VENs implement a biological speed-accuracy tradeoff (SAT) by providing a sparse, fast projection pathway that enables rapid social decisions at the cost of deliberate processing accuracy. We model VENs as fast leaky integrate-and-fire (LIF) neurons with membrane time constant 5 ms and sparse dendritic fan-in of eight afferents, compared to 20 ms and eighty afferents for standard pyramidal neurons, within a spiking cortical circuit of 2,000 neurons trained on a social discrimination task. Networks are evaluated under three clinically motivated conditions across 10 independent random seeds: typical (2% VENs), autism-like (0.4% VENs), and FTD-like (post-training VEN ablation). All configurations achieve equivalent asymptotic classification accuracy (99.4%), consistent with the prediction that VENs modulate decision speed rather than representational capacity. Temporal analysis confirms that VENs produce median first-spike latencies 4 ms earlier than pyramidal neurons. At a fixed decision threshold, the typical condition is significantly faster than FTD-like (t=-23.31, p<0.0001), while autism-like is intermediate (mean RT=26.91+/-9.01 ms vs. typical 20.70+/-2.02 ms; p=0.078). A preliminary evolutionary analysis shows qualitative correspondence between model-optimal VEN fraction and the primate phylogenetic gradient. To our knowledge, this is the first computational model that asks what a Von Economo neuron actually computes.

摘要：Von Economo 神經元 (VENs) 是一種大型雙極性投射神經元，僅存在於具有複雜社會認知的物種的前扣帶皮質 (ACC) 和額葉島，包括人類、大猩猩和鯨類。<lb/>它們在額顳癡呆症 (FTD) 中的選擇性耗竭以及在自閉症中的發展變化，使它們與快速社會決策相關聯，但之前並不存在 VEN 功能的計算模型。<lb/>我們提出了快速通道假說：VENs 通過提供稀疏、快速的投射通路來實現生物學上的速度-準確性權衡 (SAT)，這使得快速社會決策的代價是深思熟慮的處理準確性。<lb/>我們將 VENs 模型化為快速漏積-發火 (LIF) 神經元，膜時間常數為 5 毫秒，稀疏的樹突進入為八個傳入神經元，相較於標準錐體神經元的 20 毫秒和八十個傳入神經元，這是在一個包含 2,000 個神經元的尖峰皮質電路中訓練於社會辨別任務。<lb/>在 10 個獨立隨機種子下，網絡在三個臨床動機的條件下進行評估：典型 (2% VENs)、類自閉症 (0.4% VENs) 和類 FTD (訓練後 VEN 切除)。<lb/>所有配置達到相當的漸近分類準確率 (99.4%)，這與 VENs 調節決策速度而非表徵能力的預測一致。<lb/>時間分析確認 VENs 產生的中位首次尖峰潛伏期比錐體神經元早 4 毫秒。<lb/>在固定的決策閾值下，典型條件顯著快於類 FTD (t=-23.31, p&lt;0.0001)，而類自閉症則居中 (平均反應時間=26.91+/-9.01 毫秒對比典型的 20.70+/-2.02 毫秒; p=0.078)。<lb/>初步的進化分析顯示模型最佳 VEN 比例與靈長類動物的系統發育梯度之間存在定性對應。<lb/>據我們所知，這是第一個計算模型，詢問 Von Economo 神經元實際計算了什麼。

##### **Facet-Level Tracing of Evidence Uncertainty and Hallucination in RAG**
2604.09174v1 by Passant Elchafei, Monorama Swain, Shahed Masoudian, Markus Schedl

Retrieval-Augmented Generation (RAG) aims to reduce hallucination by grounding answers in retrieved evidence, yet hallucinated answers remain common even when relevant documents are available. Existing evaluations focus on answer-level or passage-level accuracy, offering limited insight into how evidence is used during generation. In this work, we introduce a facet-level diagnostics framework for QA that decomposes each input question into atomic reasoning facets. For each facet, we assess evidence sufficiency and grounding using a structured Facet x Chunk matrix that combines retrieval relevance with natural language inference-based faithfulness scores. To diagnose evidence usage, we analyze three controlled inference modes: Strict RAG, which enforces exclusive reliance on retrieved evidence; Soft RAG, which allows integration of retrieved evidence and parametric knowledge; and LLM-only generation without retrieval. Comparing these modes enables thorough analysis of retrieval-generation misalignment, defined as cases where relevant evidence is retrieved but not correctly integrated during generation. Across medical QA and HotpotQA, we evaluate three open-source and closed-source LLMs (GPT, Gemini, and LLaMA), providing interpretable diagnostics that reveal recurring facet-level failure modes, including evidence absence, evidence misalignment, and prior-driven overrides. Our results demonstrate that hallucinations in RAG systems are driven less by retrieval accuracy and more by how retrieved evidence is integrated during generation, with facet-level analysis exposing systematic evidence override and misalignment patterns that remain hidden under answer-level evaluation.

摘要：檢索增強生成（RAG）旨在通過將答案基於檢索到的證據來減少幻覺，然而即使在有相關文件的情況下，幻覺答案仍然很常見。現有的評估專注於答案級或段落級的準確性，對於證據在生成過程中的使用提供了有限的洞察。在這項工作中，我們介紹了一個面向層級的診斷框架，用於問答系統，將每個輸入問題分解為原子推理面向。對於每個面向，我們使用結構化的面向 x 區塊矩陣來評估證據的充分性和基礎，該矩陣結合了檢索相關性和基於自然語言推理的可信度分數。為了診斷證據的使用，我們分析了三種受控推理模式：嚴格RAG，強制僅依賴檢索到的證據；軟性RAG，允許整合檢索到的證據和參數知識；以及不進行檢索的僅LLM生成。比較這些模式使我們能夠徹底分析檢索-生成的不一致性，定義為在生成過程中檢索到相關證據但未正確整合的情況。在醫療問答和HotpotQA中，我們評估了三個開源和閉源的LLM（GPT、Gemini和LLaMA），提供可解釋的診斷，揭示了重複出現的面向級失敗模式，包括證據缺失、證據不一致和先前驅動的覆蓋。我們的結果表明，RAG系統中的幻覺更少受到檢索準確性的驅動，而更多地取決於檢索到的證據在生成過程中的整合方式，面向級分析揭示了系統性的證據覆蓋和不一致模式，這些模式在答案級評估中仍然隱藏。

##### **Structuring versus Problematizing: How LLM-based Agents Scaffold Learning in Diagnostic Reasoning**
2604.09158v1 by Fatma Betül Güreş, Tanya Nazaretsky, Seyed Parsa Neshaei, Tanja Käser

Supporting students in developing diagnostic reasoning is a key challenge across educational domains. Novices often face cognitive biases such as premature closure and over-reliance on heuristics, and they struggle to transfer diagnostic strategies to new cases. Scenario-based learning (SBL) enhanced by Learning Analytics (LA) and large language models (LLM) offers a promising approach by combining realistic case experiences with personalized scaffolding. Yet, how different scaffolding approaches shape reasoning processes remains insufficiently explored. This study introduces PharmaSim Switch, an SBL environment for pharmacy technician training, extended with an LA- and LLM-powered pharmacist agent that implements pedagogical conversations rooted in two theory-driven scaffolding approaches: \emph{structuring} and \emph{problematizing}, as well as a student learning trajectory. In a between-groups experiment, 63 vocational students completed a learning scenario, a near-transfer scenario, and a far-transfer scenario under one of the two scaffolding conditions. Results indicate that both scaffolding approaches were effective in supporting the use of diagnostic strategies. Performance outcomes were primarily influenced by scenario complexity rather than students' prior knowledge or the scaffolding approach used. The structuring approach was associated with more accurate Active and Interactive participation, whereas problematizing elicited more Constructive engagement. These findings underscore the value of combining scaffolding approaches when designing LA- and LLM-based systems to effectively foster diagnostic reasoning.

摘要：支持學生發展診斷推理能力是各教育領域的一項關鍵挑戰。<lb/>新手常常面臨認知偏見，例如過早結論和過度依賴啟發式，並且他們在將診斷策略轉移到新案例時遇到困難。<lb/>基於情境的學習（SBL）結合學習分析（LA）和大型語言模型（LLM）提供了一種有前景的方法，通過將現實案例經驗與個性化支架相結合。<lb/>然而，不同的支架方法如何塑造推理過程仍然缺乏充分的探索。<lb/>本研究介紹了PharmaSim Switch，一個用於藥劑技術員培訓的SBL環境，並擴展了由LA和LLM驅動的藥劑師代理，該代理實施基於兩種理論驅動的支架方法的教學對話：\emph{結構化}和\emph{問題化}，以及學生學習軌跡。<lb/>在一項組間實驗中，63名職業學生在兩種支架條件下完成了一個學習情境、一個近轉移情境和一個遠轉移情境。<lb/>結果表明，這兩種支架方法在支持使用診斷策略方面都是有效的。<lb/>表現結果主要受到情境複雜性的影響，而不是學生的先前知識或使用的支架方法。<lb/>結構化方法與更準確的主動和互動參與相關，而問題化則引發了更多的建設性參與。<lb/>這些發現強調了在設計基於LA和LLM的系統時結合支架方法的價值，以有效促進診斷推理。

##### **Think Less, Know More: State-Aware Reasoning Compression with Knowledge Guidance for Efficient Reasoning**
2604.09150v1 by Yi Sui, Chaozhuo Li, Dawei Song

Large Reasoning Models (LRMs) achieve strong performance on complex tasks by leveraging long Chain-of-Thought (CoT), but often suffer from overthinking, leading to excessive reasoning steps and high inference latency. Existing CoT compression methods struggle to balance accuracy and efficiency, and lack fine-grained, step-level adaptation to redundancy and reasoning bias. Therefore, we propose State-Aware Reasoning Compression with Knowledge Guidance (STACK), a framework that performs step-wise CoT compression by explicitly modeling stage-specific redundancy sources and integrating with a retrieval-augmented guidance. STACK constructs online long-short contrastive samples and dynamically switches between knowledge-guided compression for uncertain or biased reasoning state and self-prompted compression for overly long but confident state, complemented by an answer-convergence-based early stopping mechanism to suppress redundant verification. We further propose a reward-difference-driven training strategy by combining Proximal Policy Optimization (PPO) and Direct Preference Optimization (DPO), enabling models to learn state-conditioned compression strategies. Experiments on three mathematical reasoning benchmarks show that STACK achieves a superior accuracy-efficiency balance, reducing average response length by 59.9% while improving accuracy by 4.8 points over existing methods.

摘要：大型推理模型（LRMs）透過利用長鏈式思考（CoT）在複雜任務上取得強勁表現，但常常遭遇過度思考，導致推理步驟過多和高推理延遲。現有的CoT壓縮方法難以平衡準確性和效率，並且缺乏對冗餘和推理偏見的細緻步驟級適應。因此，我們提出了帶有知識引導的狀態感知推理壓縮（STACK），這是一個通過明確建模階段特定冗餘來源並與檢索增強引導相結合來執行逐步CoT壓縮的框架。STACK構建在線長短對比樣本，並在不確定或偏見的推理狀態下動態切換知識引導壓縮，對於過長但自信的狀態則使用自我提示壓縮，並輔以基於答案收斂的早期停止機制以抑制冗餘驗證。我們進一步提出了一種由獎勵差異驅動的訓練策略，通過結合近端政策優化（PPO）和直接偏好優化（DPO），使模型能夠學習狀態條件的壓縮策略。在三個數學推理基準上的實驗顯示，STACK實現了優越的準確性與效率平衡，將平均響應長度減少了59.9%，同時提高了準確性4.8分，相較於現有方法。

##### **EquiformerV3: Scaling Efficient, Expressive, and General SE(3)-Equivariant Graph Attention Transformers**
2604.09130v1 by Yi-Lun Liao, Alexander J. Hoffman, Sabrina C. Shen, Alexandre Duval, Sam Walton Norwood, Tess Smidt

As $SE(3)$-equivariant graph neural networks mature as a core tool for 3D atomistic modeling, improving their efficiency, expressivity, and physical consistency has become a central challenge for large-scale applications. In this work, we introduce EquiformerV3, the third generation of the $SE(3)$-equivariant graph attention Transformer, designed to advance all three dimensions: efficiency, expressivity, and generality. Building on EquiformerV2, we have the following three key advances. First, we optimize the software implementation, achieving $1.75\times$ speedup. Second, we introduce simple and effective modifications to EquiformerV2, including equivariant merged layer normalization, improved feedforward network hyper-parameters, and attention with smooth radius cutoff. Third, we propose SwiGLU-$S^2$ activations to incorporate many-body interactions for better theoretical expressivity and to preserve strict equivariance while reducing the complexity of sampling $S^2$ grids. Together, SwiGLU-$S^2$ activations and smooth-cutoff attention enable accurate modeling of smoothly varying potential energy surfaces (PES), generalizing EquiformerV3 to tasks requiring energy-conserving simulations and higher-order derivatives of PES. With these improvements, EquiformerV3 trained with the auxiliary task of denoising non-equilibrium structures (DeNS) achieves state-of-the-art results on OC20, OMat24, and Matbench Discovery.

摘要：隨著 $SE(3)$-不變圖神經網絡作為 3D 原子建模的核心工具逐漸成熟，提高其效率、表達能力和物理一致性已成為大規模應用的核心挑戰。在這項工作中，我們介紹了 EquiformerV3，這是第三代 $SE(3)$-不變圖注意力 Transformer，旨在推進這三個維度：效率、表達能力和通用性。在 EquiformerV2 的基礎上，我們有以下三個關鍵進展。首先，我們優化了軟體實現，實現了 $1.75\times$ 的加速。其次，我們對 EquiformerV2 進行了簡單而有效的修改，包括不變合併層正規化、改進的前饋網絡超參數以及具有平滑半徑截止的注意力。第三，我們提出了 SwiGLU-$S^2$ 激活，以納入多體相互作用以獲得更好的理論表達能力，並在降低採樣 $S^2$ 網格的複雜性的同時保持嚴格的不變性。綜合來看，SwiGLU-$S^2$ 激活和平滑截止注意力使得準確建模平滑變化的潛能能量面（PES）成為可能，將 EquiformerV3 推廣到需要能量守恆模擬和 PES 高階導數的任務。通過這些改進，使用去噪非平衡結構（DeNS）輔助任務訓練的 EquiformerV3 在 OC20、OMat24 和 Matbench Discovery 上達到了最先進的結果。

##### **Prototype-Regularized Federated Learning for Cross-Domain Aspect Sentiment Triplet Extraction**
2604.09123v1 by Zongming Cai, Jianhang Tang, Zhenyong Zhang, Jinghui Qin, Kebing Jin, Hankz Hankui Zhuo

Aspect Sentiment Triplet Extraction (ASTE) aims to extract all sentiment triplets of aspect terms, opinion terms, and sentiment polarities from a sentence. Existing methods are typically trained on individual datasets in isolation, failing to jointly capture the common feature representations shared across domains. Moreover, data privacy constraints prevent centralized data aggregation. To address these challenges, we propose Prototype-based Cross-Domain Span Prototype extraction (PCD-SpanProto), a prototype-regularized federated learning framework to enable distributed clients to exchange class-level prototypes instead of full model parameters. Specifically, we design a weighted performance-aware aggregation strategy and a contrastive regularization module to improve the global prototype under domain heterogeneity and the promotion between intra-class compactness and inter-class separability across clients. Extensive experiments on four ASTE datasets demonstrate that our method outperforms baselines and reduces communication costs, validating the effectiveness of prototype-based cross-domain knowledge transfer.

摘要：方面情感三元組提取（ASTE）旨在從句子中提取所有方面術語、意見術語和情感極性的情感三元組。現有的方法通常在孤立的個別數據集上進行訓練，未能共同捕捉跨領域共享的共同特徵表示。此外，數據隱私限制阻礙了集中數據的聚合。為了解決這些挑戰，我們提出了基於原型的跨域跨度原型提取（PCD-SpanProto），這是一種原型正則化的聯邦學習框架，旨在使分佈式客戶端能夠交換類別級原型，而不是完整的模型參數。具體而言，我們設計了一種加權性能感知聚合策略和一個對比正則化模塊，以改善在領域異質性下的全局原型，以及促進客戶端之間的類內緊密性和類間可分離性。對四個ASTE數據集的廣泛實驗表明，我們的方法超越了基準，降低了通信成本，驗證了基於原型的跨域知識轉移的有效性。

##### **Beyond Isolated Clients: Integrating Graph-Based Embeddings into Event Sequence Models**
2604.09085v1 by Harry Proshian, Nikita Severin, Sergey Nikolenko, Kireev Ivan, Andrey Savchenko, Ivan Sergeev, Maria Postnova, Ilya Makarov

Large-scale digital platforms generate billions of timestamped user-item interactions (events) that are crucial for predicting user attributes in, e.g., fraud prevention and recommendations. While self-supervised learning (SSL) effectively models the temporal order of events, it typically overlooks the global structure of the user-item interaction graph. To bridge this gap, we propose three model-agnostic strategies for integrating this structural information into contrastive SSL: enriching event embeddings, aligning client representations with graph embeddings, and adding a structural pretext task. Experiments on four financial and e-commerce datasets demonstrate that our approach consistently improves the accuracy (up to a 2.3% AUC) and reveals that graph density is a key factor in selecting the optimal integration strategy.

摘要：大型數位平台生成數十億個帶時間戳的用戶-項目互動（事件），這對於預測用戶屬性（例如，防詐騙和推薦）至關重要。雖然自我監督學習（SSL）有效地建模事件的時間順序，但它通常忽略了用戶-項目互動圖的全局結構。為了彌補這一差距，我們提出了三種與模型無關的策略，以將這些結構信息整合到對比SSL中：豐富事件嵌入、將客戶端表示與圖嵌入對齊，以及添加結構性前置任務。在四個金融和電子商務數據集上的實驗表明，我們的方法始終提高了準確性（最高可達2.3%的AUC），並揭示了圖密度是選擇最佳整合策略的關鍵因素。

##### **Skill-Conditioned Visual Geolocation for Vision-Language**
2604.09025v1 by Chenjie Yang, Yutian Jiang, Chenyu Wu

Vision-language models (VLMs) have shown a promising ability in image geolocation, but they still lack structured geographic reasoning and the capacity for autonomous self-evolution. Existing methods predominantly rely on implicit parametric memory, which often exploits outdated knowledge and generates hallucinated reasoning. Furthermore, current inference is a "one-off" process, lacking the feedback loops necessary for self-evolution based on reasoning outcomes. To address these issues, we propose GeoSkill, a training-free framework based on an evolving Skill-Graph. We first initialize the graph by refining human expert trajectories into atomic, natural-language skills. For execution, GeoSkill employs an inference model to perform direct reasoning guided by the current Skill-Graph. For continuous growth, an Autonomous Evolution mechanism leverages a larger model to conduct multiple reasoning rollouts on image-coordinate pairs sourced from web-scale data and verified real-world reasoning. By analyzing both successful and failed trajectories from these rollouts, the mechanism iteratively synthesizes and prunes skills, effectively expanding the Skill-Graph and correcting geographic biases without any parameter updates. Experiments demonstrate that GeoSkill achieves promising performance in both geolocation accuracy and reasoning faithfulness on GeoRC, while maintaining superior generalization across diverse external datasets. Furthermore, our autonomous evolution fosters the emergence of novel, verifiable skills, significantly enhancing the system's cognition of real-world geographic knowledge beyond isolated case studies.

摘要：視覺語言模型 (VLMs) 在影像地理定位方面顯示出良好的潛力，但它們仍然缺乏結構化的地理推理能力及自主自我演化的能力。現有的方法主要依賴隱式參數記憶，這通常利用過時的知識並產生幻覺式的推理。此外，目前的推理是一個「一次性」過程，缺乏基於推理結果進行自我演化所需的反饋循環。為了解決這些問題，我們提出了 GeoSkill，一個基於不斷演變的技能圖的無需訓練的框架。我們首先通過將人類專家的軌跡精煉為原子化的自然語言技能來初始化圖。為了執行，GeoSkill 採用推理模型，根據當前的技能圖進行直接推理。為了持續增長，自主演化機制利用更大的模型在來自網絡規模數據和經過驗證的現實世界推理的影像坐標對上進行多次推理展開。通過分析這些展開中的成功和失敗軌跡，該機制迭代地合成和修剪技能，有效地擴展技能圖並在不進行任何參數更新的情況下修正地理偏見。實驗表明，GeoSkill 在 GeoRC 上實現了有希望的地理定位準確性和推理真實性，同時在各種外部數據集上保持了優越的泛化能力。此外，我們的自主演化促進了新穎且可驗證技能的出現，顯著增強了系統對現實世界地理知識的認知，超越了孤立的案例研究。

##### **Towards Linguistically-informed Representations for English as a Second or Foreign Language: Review, Construction and Application**
2604.09008v1 by Wenxi Li, Xihao Wang, Weiwei Sun

The widespread use of English as a Second or Foreign Language (ESFL) has sparked a paradigm shift: ESFL is not seen merely as a deviation from standard English but as a distinct linguistic system in its own right. This shift highlights the need for dedicated, knowledge-intensive representations of ESFL. In response, this paper surveys existing ESFL resources, identifies their limitations, and proposes a novel solution. Grounded in constructivist theories, the paper treats constructions as the fundamental units of analysis, allowing it to model the syntax--semantics interface of both ESFL and standard English. This design captures a wide range of ESFL phenomena by referring to syntactico-semantic mappings of English while preserving ESFL's unique characteristics, resulting a gold-standard syntactico-semantic resource comprising 1643 annotated ESFL sentences. To demonstrate the sembank's practical utility, we conduct a pilot study testing the Linguistic Niche Hypothesis, highlighting its potential as a valuable tool in Second Language Acquisition research.

摘要：英語作為第二語言或外語（ESFL）的廣泛使用引發了一場範式轉變：ESFL不僅被視為標準英語的偏差，而是作為一個獨特的語言系統存在。這一轉變突顯了對專門、知識密集型ESFL表徵的需求。作為回應，本文調查了現有的ESFL資源，識別了它們的局限性，並提出了一個新穎的解決方案。基於建構主義理論，本文將構造視為分析的基本單位，使其能夠建模ESFL和標準英語的句法-語義介面。這一設計通過參考英語的句法-語義映射來捕捉各種ESFL現象，同時保留ESFL的獨特特徵，最終形成一個包含1643個註釋ESFL句子的黃金標準句法-語義資源。為了展示這個資源的實用性，我們進行了一項初步研究，測試語言利基假說，突顯了它作為第二語言習得研究中有價值工具的潛力。

##### **Quantisation Reshapes the Metacognitive Geometry of Language Models**
2604.08976v1 by Jon-Paul Cacioli

We report that model quantisation restructures domain-level metacognitive efficiency in LLMs rather than degrading it uniformly. Evaluating Llama-3-8B-Instruct on the same 3,000 questions at Q5_K_M and f16 precision, we find that M-ratio profiles across four knowledge domains are uncorrelated between formats (Spearman rho = 0.00). Arts & Literature moves from worst-monitored (M-ratio = 0.606 at Q5_K_M) to best-monitored (1.542 at f16). Geography moves from well-monitored (1.210) to under-monitored (0.798). However, Type-2 AUROC profiles are perfectly stable across formats (rho = 1.00), localising the restructuring to the M-ratio normalisation rather than the underlying discrimination signal. This finding emerged from a pre-registered attempt to improve metacognition through domain-conditional training. We prescribed confidence-amplification SFT for the diagnosed weak domain, with matched-budget agnostic and wrong-prescription controls. All four confirmatory hypotheses were null (10,000 bootstrap resamples, seed = 42). The training successfully reshaped confidence distributions, doubling the NLP gap in Science from 0.076 to 0.152, but did not improve meta-d' because the diagnostic profile did not transfer across formats. Any system relying on domain-level M-ratio profiles has an unexamined dependency on inference format. Systems using AUROC_2 are safer. We release all code, pre-registrations, and trial-level data.

摘要：我們報告模型量化重構了大型語言模型的領域級元認知效率，而不是均勻地降低它。對Llama-3-8B-Instruct在Q5_K_M和f16精度下的3,000個問題進行評估，我們發現四個知識領域的M比率在不同格式之間不相關（Spearman rho = 0.00）。藝術與文學從最差的監控（M比率 = 0.606在Q5_K_M）轉變為最佳的監控（1.542在f16）。地理從良好的監控（1.210）轉變為監控不足（0.798）。然而，Type-2 AUROC曲線在不同格式之間是完全穩定的（rho = 1.00），將重構局限於M比率的正規化，而不是潛在的辨別信號。這一發現源於一項預註冊的嘗試，旨在通過領域條件訓練來改善元認知。我們對診斷出來的弱領域開出了信心增強的SFT，並設置了匹配預算的無偏和錯誤處方對照組。所有四個確認假設均為零（10,000次自助重抽樣，種子 = 42）。訓練成功重塑了信心分佈，將科學領域的NLP差距從0.076翻倍至0.152，但未能改善meta-d'，因為診斷檔案未能在不同格式之間轉移。任何依賴於領域級M比率檔案的系統對推理格式存在未經檢查的依賴。使用AUROC_2的系統更安全。我們釋放所有代碼、預註冊和試驗級數據。

##### **TaxPraBen: A Scalable Benchmark for Structured Evaluation of LLMs in Chinese Real-World Tax Practice**
2604.08948v1 by Gang Hu, Yating Chen, Haiyan Ding, Wang Gao, Jiajia Huang, Min Peng, Qianqian Xie, Kun Yu

While Large Language Models (LLMs) excel in various general domains, they exhibit notable gaps in the highly specialized, knowledge-intensive, and legally regulated Chinese tax domain. Consequently, while tax-related benchmarks are gaining attention, many focus on isolated NLP tasks, neglecting real-world practical capabilities. To address this issue, we introduce TaxPraBen, the first dedicated benchmark for Chinese taxation practice. It combines 10 traditional application tasks, along with 3 pioneering real-world scenarios: tax risk prevention, tax inspection analysis, and tax strategy planning, sourced from 14 datasets totaling 7.3K instances. TaxPraBen features a scalable structured evaluation paradigm designed through process of "structured parsing-field alignment extraction-numerical and textual matching", enabling end-to-end tax practice assessment while being extensible to other domains. We evaluate 19 LLMs based on Bloom's taxonomy. The results indicate significant performance disparities: all closed-source large-parameter LLMs excel, and Chinese LLMs like Qwen2.5 generally exceed multilingual LLMs, while the YaYi2 LLM, fine-tuned with some tax data, shows only limited improvement. TaxPraBen serves as a vital resource for advancing evaluations of LLMs in practical applications.

摘要：雖然大型語言模型（LLMs）在各種通用領域中表現出色，但在高度專業、知識密集且受法律規範的中國稅務領域中，它們顯示出顯著的差距。<lb/>因此，儘管與稅務相關的基準正在受到關注，但許多基準專注於孤立的自然語言處理任務，忽視了現實世界的實用能力。<lb/>為了解決這個問題，我們推出了TaxPraBen，首個專門針對中國稅務實踐的基準。<lb/>它結合了10個傳統應用任務，以及3個開創性的現實場景：稅務風險預防、稅務稽查分析和稅務策略規劃，這些場景來自14個數據集，共計7.3K實例。<lb/>TaxPraBen具有可擴展的結構化評估範式，通過“結構化解析-領域對齊提取-數值和文本匹配”的過程設計，實現端到端的稅務實踐評估，同時可以擴展到其他領域。<lb/>我們根據布魯姆的分類法評估了19個LLMs。<lb/>結果顯示出顯著的性能差異：所有封閉源的大參數LLMs表現優異，而像Qwen2.5這樣的中國LLMs通常超越多語言LLMs，而YaYi2 LLM在一些稅務數據上進行微調後僅顯示出有限的改善。<lb/>TaxPraBen作為推進LLMs在實際應用中評估的重要資源。

##### **Ge$^\text{2}$mS-T: Multi-Dimensional Grouping for Ultra-High Energy Efficiency in Spiking Transformer**
2604.08894v1 by Zecheng Hao, Shenghao Xie, Kang Chen, Wenxuan Liu, Zhaofei Yu, Tiejun Huang

Spiking Neural Networks (SNNs) offer superior energy efficiency over Artificial Neural Networks (ANNs). However, they encounter significant deficiencies in training and inference metrics when applied to Spiking Vision Transformers (S-ViTs). Existing paradigms including ANN-SNN Conversion and Spatial-Temporal Backpropagation (STBP) suffer from inherent limitations, precluding concurrent optimization of memory, accuracy and energy consumption. To address these issues, we propose Ge$^\text{2}$mS-T, a novel architecture implementing grouped computation across temporal, spatial and network structure dimensions. Specifically, we introduce the Grouped-Exponential-Coding-based IF (ExpG-IF) model, enabling lossless conversion with constant training overhead and precise regulation for spike patterns. Additionally, we develop Group-wise Spiking Self-Attention (GW-SSA) to reduce computational complexity via multi-scale token grouping and multiplication-free operations within a hybrid attention-convolution framework. Experiments confirm that our method can achieve superior performance with ultra-high energy efficiency on challenging benchmarks. To our best knowledge, this is the first work to systematically establish multi-dimensional grouped computation for resolving the triad of memory overhead, learning capability and energy budget in S-ViTs.

摘要：尖峰神經網絡（SNNs）在能源效率上優於人工神經網絡（ANNs）。然而，當應用於尖峰視覺Transformer（S-ViTs）時，它們在訓練和推理指標上遇到了重大缺陷。現有的範式，包括ANN-SNN轉換和空間-時間反向傳播（STBP），存在固有的限制，無法同時優化記憶體、準確性和能量消耗。為了解決這些問題，我們提出了Ge$^\text{2}$mS-T，一種新穎的架構，實現了在時間、空間和網絡結構維度上的分組計算。具體而言，我們引入了基於分組指數編碼的IF（ExpG-IF）模型，實現了無損轉換，並具有恆定的訓練開銷和對尖峰模式的精確調節。此外，我們開發了按組尖峰自注意力（GW-SSA），通過多尺度令牌分組和無乘法操作來降低計算複雜性，並在混合注意力-卷積框架內進行。實驗證實，我們的方法能夠在挑戰性基準上實現超高能量效率的卓越性能。據我們所知，這是第一個系統性建立多維分組計算以解決S-ViTs中記憶開銷、學習能力和能量預算三者之間的關係的工作。

##### **A Closer Look at the Application of Causal Inference in Graph Representation Learning**
2604.08890v1 by Hang Gao, Kunyu Li, Huang Hong, Baoquan Cui, Fengge Wu

Modeling causal relationships in graph representation learning remains a fundamental challenge. Existing approaches often draw on theories and methods from causal inference to identify causal subgraphs or mitigate confounders. However, due to the inherent complexity of graph-structured data, these approaches frequently aggregate diverse graph elements into single causal variables, an operation that risks violating the core assumptions of causal inference. In this work, we prove that such aggregation compromises causal validity. Building on this conclusion, we propose a theoretical model grounded in the smallest indivisible units of graph data to ensure that the causal validity is guaranteed. With this model, we further analyze the costs of achieving precise causal modeling in graph representation learning and identify the conditions under which the problem can be simplified. To empirically support our theory, we construct a controllable synthetic dataset that reflects realworld causal structures and conduct extensive experiments for validation. Finally, we develop a causal modeling enhancement module that can be seamlessly integrated into existing graph learning pipelines, and we demonstrate its effectiveness through comprehensive comparative experiments.

摘要：建模圖形表示學習中的因果關係仍然是一個基本挑戰。<lb/>現有的方法通常借鑒因果推斷的理論和方法，以識別因果子圖或減輕混淆因素。<lb/>然而，由於圖結構數據的固有複雜性，這些方法經常將多樣的圖元素聚合為單一的因果變量，這一操作有可能違反因果推斷的核心假設。<lb/>在這項工作中，我們證明了這種聚合會損害因果有效性。<lb/>基於這一結論，我們提出了一個理論模型，基於圖數據中最小的不可分割單位，以確保因果有效性得到保障。<lb/>利用這個模型，我們進一步分析了在圖形表示學習中實現精確因果建模的成本，並確定了問題可以簡化的條件。<lb/>為了實證支持我們的理論，我們構建了一個可控的合成數據集，反映現實世界的因果結構，並進行了廣泛的實驗以進行驗證。<lb/>最後，我們開發了一個因果建模增強模塊，可以無縫集成到現有的圖學習管道中，並通過全面的比較實驗展示其有效性。

##### **SenBen: Sensitive Scene Graphs for Explainable Content Moderation**
2604.08819v1 by Fatih Cagatay Akyon, Alptekin Temizel

Content moderation systems classify images as safe or unsafe but lack spatial grounding and interpretability: they cannot explain what sensitive behavior was detected, who is involved, or where it occurs. We introduce the Sensitive Benchmark (SenBen), the first large-scale scene graph benchmark for sensitive content, comprising 13,999 frames from 157 movies annotated with Visual Genome-style scene graphs (25 object classes, 28 attributes including affective states such as pain, fear, aggression, and distress, 14 predicates) and 16 sensitivity tags across 5 categories. We distill a frontier VLM into a compact 241M student model using a multi-task recipe that addresses vocabulary imbalance in autoregressive scene graph generation through suffix-based object identity, Vocabulary-Aware Recall (VAR) Loss, and a decoupled Query2Label tag head with asymmetric loss, yielding a +6.4 percentage point improvement in SenBen Recall over standard cross-entropy training. On grounded scene graph metrics, our student model outperforms all evaluated VLMs except Gemini models and all commercial safety APIs, while achieving the highest object detection and captioning scores across all models, at $7.6\times$ faster inference and $16\times$ less GPU memory.

摘要：內容審核系統將圖像分類為安全或不安全，但缺乏空間基礎和可解釋性：它們無法解釋檢測到的敏感行為是什麼、涉及誰或發生在哪裡。<lb/>我們引入了敏感基準（Sensitive Benchmark，SenBen），這是首個針對敏感內容的大規模場景圖基準，包含來自157部電影的13,999幀，並用Visual Genome風格的場景圖進行標註（25個物體類別，28個屬性，包括疼痛、恐懼、侵略和痛苦等情感狀態，14個謂詞）以及5個類別的16個敏感性標籤。<lb/>我們將前沿的VLM提煉為一個緊湊的241M學生模型，使用多任務配方，通過基於後綴的物體身份、詞彙感知回憶（Vocabulary-Aware Recall，VAR）損失和具有不對稱損失的解耦Query2Label標籤頭來解決自回歸場景圖生成中的詞彙不平衡問題，從而在SenBen回憶上比標準交叉熵訓練提高了6.4個百分點。<lb/>在基於場景圖的度量上，我們的學生模型在所有評估的VLM中表現優於除Gemini模型外的所有模型以及所有商業安全API，同時在所有模型中實現了最高的物體檢測和標題生成分數，推理速度快了7.6倍，GPU內存使用量減少了16倍。

##### **Building Better Environments for Autonomous Cyber Defence**
2604.08805v1 by Chris Hicks, Elizabeth Bates, Shae McFadden, Isaac Symes Thompson, Myles Foley, Ed Chapman, Nickolas Espinosa Dice, Ankita Samaddar, Joshua Sylvester, Himanshu Neema, Nicholas Butts, Nate Foster, Ahmad Ridley, Zoe M, Paul Jones

In November 2025, the authors ran a workshop on the topic of what makes a good reinforcement learning (RL) environment for autonomous cyber defence (ACD). This paper details the knowledge shared by participants both during the workshop and shortly afterwards by contributing herein. The workshop participants come from academia, industry, and government, and have extensive hands-on experience designing and working with RL and cyber environments. While there is now a sizeable body of literature describing work in RL for ACD, there is nevertheless a great deal of tradecraft, domain knowledge, and common hazards which are not detailed comprehensively in a single resource. With a specific focus on building better environments to train and evaluate autonomous RL agents in network defence scenarios, including government and critical infrastructure networks, the contributions of this work are twofold: (1) a framework for decomposing the interface between RL cyber environments and real systems, and (2) guidelines on current best practice for RL-based ACD environment development and agent evaluation, based on the key findings from our workshop.

摘要：在2025年11月，作者們舉辦了一個研討會，主題為什麼是適合自主網絡防禦（ACD）的良好強化學習（RL）環境。這篇論文詳細說明了參與者在研討會期間以及隨後貢獻的知識。研討會的參與者來自學術界、產業界和政府，並且在設計和使用RL及網絡環境方面擁有豐富的實踐經驗。儘管現在已有相當多的文獻描述了ACD中RL的工作，但仍然有許多技藝、領域知識和常見風險未在單一資源中全面詳述。這項工作的貢獻專注於建立更好的環境，以訓練和評估在網絡防禦場景中自主RL代理，包括政府和關鍵基礎設施網絡，具體貢獻有兩方面：（1）一個分解RL網絡環境與真實系統之間接口的框架，以及（2）基於我們研討會的關鍵發現，關於RL基礎的ACD環境開發和代理評估的當前最佳實踐指南。

##### **Lessons Without Borders? Evaluating Cultural Alignment of LLMs Using Multilingual Story Moral Generation**
2604.08797v1 by Sophie Wu, Andrew Piper

Stories are key to transmitting values across cultures, but their interpretation varies across linguistic and cultural contexts. Thus, we introduce multilingual story moral generation as a novel culturally grounded evaluation task. Using a new dataset of human-written story morals collected across 14 language-culture pairs, we compare model outputs with human interpretations via semantic similarity, a human preference survey, and value categorization. We show that frontier models such as GPT-4o and Gemini generate story morals that are semantically similar to human responses and preferred by human evaluators. However, their outputs exhibit markedly less cross-linguistic variation and concentrate on a narrower set of widely shared values. These findings suggest that while contemporary models can approximate central tendencies of human moral interpretation, they struggle to reproduce the diversity that characterizes human narrative understanding. By framing narrative interpretation as an evaluative task, this work introduces a new approach to studying cultural alignment in language models beyond static benchmarks or knowledge-based tests.

摘要：故事是跨文化傳遞價值觀的關鍵，但它們的詮釋在語言和文化背景中有所不同。<lb/>因此，我們引入多語言故事道德生成作為一項新穎的文化根植評估任務。<lb/>利用一個新數據集，該數據集收集了14個語言文化對中的人類撰寫的故事道德，我們通過語義相似性、人類偏好調查和價值分類來比較模型輸出與人類詮釋。<lb/>我們顯示，前沿模型如GPT-4o和Gemini生成的故事道德在語義上與人類反應相似，並且受到人類評估者的青睞。<lb/>然而，它們的輸出顯示出明顯較少的跨語言變異，並集中於一組較窄的廣泛共享價值觀。<lb/>這些發現表明，雖然當代模型可以近似人類道德詮釋的中心傾向，但它們在重現人類敘事理解的多樣性方面存在困難。<lb/>通過將敘事詮釋框架化為一項評估任務，這項工作引入了一種新的方法來研究語言模型中的文化對齊，超越靜態基準或基於知識的測試。

##### **LLMs Underperform Graph-Based Parsers on Supervised Relation Extraction for Complex Graphs**
2604.08752v1 by Paolo Gajo, Domenic Rosati, Hassan Sajjad, Alberto Barrón-Cedeño

Relation extraction represents a fundamental component in the process of creating knowledge graphs, among other applications. Large language models (LLMs) have been adopted as a promising tool for relation extraction, both in supervised and in-context learning settings. However, in this work we show that their performance still lags behind much smaller architectures when the linguistic graph underlying a text has great complexity. To demonstrate this, we evaluate four LLMs against a graph-based parser on six relation extraction datasets with sentence graphs of varying sizes and complexities. Our results show that the graph-based parser increasingly outperforms the LLMs, as the number of relations in the input documents increases. This makes the much lighter graph-based parser a superior choice in the presence of complex linguistic graphs.

摘要：關係提取是創建知識圖譜過程中的一個基本組成部分，還有其他應用。大型語言模型（LLMs）已被採用作為關係提取的一種有前景的工具，無論是在監督學習還是在上下文學習的環境中。然而，在這項工作中，我們顯示當文本底層的語言圖具有高度複雜性時，它們的性能仍然落後於更小的架構。為了證明這一點，我們在六個關係提取數據集上評估了四個LLM，這些數據集的句子圖具有不同的大小和複雜性。我們的結果顯示，隨著輸入文檔中關係數量的增加，基於圖的解析器的表現越來越超過LLM。這使得在存在複雜語言圖的情況下，輕量級的基於圖的解析器成為更優的選擇。

##### **Parameterized Complexity Of Representing Models Of MSO Formulas**
2604.08707v1 by Petr Kučera, Petr Martinek

Monadic second order logic (MSO2) plays an important role in parameterized complexity due to the Courcelle's theorem. This theorem states that the problem of checking if a given graph has a property specified by a given MSO2 formula can be solved by a parameterized linear time algorithm with respect to the treewidth of the graph and the size of the formula. We extend this result by showing that models of MSO2 formula with free variables can be represented with a decision diagram whose size is parameterized linear in the above mentioned parameter. In particular, we show a parameterized linear upper bound on the size of a sentential decision diagram (SDD) when treewidth is considered and a parameterized linear upper bound on the size of an ordered binary decision diagram (OBDD) when considering the pathwidth in the parameter. In addition, building on a lower bound on the size of OBDD by Razgon (2014), we show that there is an MSO2 formula and a class of graphs with bounded treewidth which do not admit an OBDD with the size parameterized by the treewidth. Our result offers a new perspective on the Courcelle's theorem and connects it to the area of knowledge representation.

摘要：單子第二階邏輯（MSO2）在參數化複雜度中扮演著重要角色，這是由於Courcelle定理。這個定理指出，檢查給定圖形是否具有由給定MSO2公式指定的性質的問題，可以通過一個參數化的線性時間算法來解決，該算法與圖形的樹寬和公式的大小有關。我們通過顯示具有自由變數的MSO2公式的模型可以用一個決策圖來表示，該決策圖的大小在上述參數中是參數化的線性來擴展這一結果。特別地，我們展示了在考慮樹寬時，句子決策圖（SDD）大小的參數化線性上界，以及在考慮路徑寬度時，排序二元決策圖（OBDD）大小的參數化線性上界。此外，基於Razgon（2014）對OBDD大小的下界，我們顯示存在一個MSO2公式和一類具有有界樹寬的圖形，這些圖形不承認大小由樹寬參數化的OBDD。我們的結果為Courcelle定理提供了一個新的視角，並將其與知識表示領域聯繫起來。

##### **Act Wisely: Cultivating Meta-Cognitive Tool Use in Agentic Multimodal Models**
2604.08545v1 by Shilin Yan, Jintao Tong, Hongwei Xue, Xiaojun Tang, Yangyang Wang, Kunyu Shi, Guannan Zhang, Ruixuan Li, Yixiong Zou

The advent of agentic multimodal models has empowered systems to actively interact with external environments. However, current agents suffer from a profound meta-cognitive deficit: they struggle to arbitrate between leveraging internal knowledge and querying external utilities. Consequently, they frequently fall prey to blind tool invocation, resorting to reflexive tool execution even when queries are resolvable from the raw visual context. This pathological behavior precipitates severe latency bottlenecks and injects extraneous noise that derails sound reasoning. Existing reinforcement learning protocols attempt to mitigate this via a scalarized reward that penalizes tool usage. Yet, this coupled formulation creates an irreconcilable optimization dilemma: an aggressive penalty suppresses essential tool use, whereas a mild penalty is entirely subsumed by the variance of the accuracy reward during advantage normalization, rendering it impotent against tool overuse. To transcend this bottleneck, we propose HDPO, a framework that reframes tool efficiency from a competing scalar objective to a strictly conditional one. By eschewing reward scalarization, HDPO maintains two orthogonal optimization channels: an accuracy channel that maximizes task correctness, and an efficiency channel that enforces execution economy exclusively within accurate trajectories via conditional advantage estimation. This decoupled architecture naturally induces a cognitive curriculum-compelling the agent to first master task resolution before refining its self-reliance. Extensive evaluations demonstrate that our resulting model, Metis, reduces tool invocations by orders of magnitude while simultaneously elevating reasoning accuracy.

摘要：代理多模態模型的出現使系統能夠主動與外部環境互動。<lb/>然而，當前的代理面臨著深刻的元認知缺陷：它們在利用內部知識和查詢外部工具之間的裁決上掙扎。<lb/>因此，它們經常陷入盲目的工具調用，即使查詢可以從原始視覺上下文中解決，仍然依賴於反射式的工具執行。<lb/>這種病態行為導致了嚴重的延遲瓶頸，並注入了多餘的噪音，干擾了合理推理。<lb/>現有的強化學習協議試圖通過懲罰工具使用的標量化獎勵來緩解這一問題。<lb/>然而，這種耦合的公式創造了一個不可調和的優化困境：激進的懲罰抑制了必要的工具使用，而輕微的懲罰則完全被優勢正規化過程中的準確性獎勵變異所淹沒，使其對工具過度使用無能為力。<lb/>為了超越這一瓶頸，我們提出了HDPO，一個將工具效率從競爭性標量目標重新框架為嚴格條件目標的框架。<lb/>通過避免獎勵標量化，HDPO維持了兩個正交的優化通道：一個最大化任務正確性的準確性通道，以及一個通過條件優勢估計專門在準確軌跡內執行經濟的效率通道。<lb/>這種解耦的架構自然引入了一個認知課程，迫使代理首先掌握任務解決，然後再提升其自我依賴性。<lb/>廣泛的評估顯示，我們的模型Metis在顯著減少工具調用的同時，還提高了推理準確性。

##### **3D-VCD: Hallucination Mitigation in 3D-LLM Embodied Agents through Visual Contrastive Decoding**
2604.08645v1 by Makanjuola Ogunleye, Eman Abdelrahman, Ismini Lourentzou

Large multimodal models are increasingly used as the reasoning core of embodied agents operating in 3D environments, yet they remain prone to hallucinations that can produce unsafe and ungrounded decisions. Existing inference-time hallucination mitigation methods largely target 2D vision-language settings and do not transfer to embodied 3D reasoning, where failures arise from object presence, spatial layout, and geometric grounding rather than pixel-level inconsistencies. We introduce 3D-VCD, the first inference-time visual contrastive decoding framework for hallucination mitigation in 3D embodied agents. 3D-VCD constructs a distorted 3D scene graph by applying semantic and geometric perturbations to object-centric representations, such as category substitutions and coordinate or extent corruption. By contrasting predictions under the original and distorted 3D contexts, our method suppresses tokens that are insensitive to grounded scene evidence and are therefore likely driven by language priors. We evaluate 3D-VCD on the 3D-POPE and HEAL benchmarks and show that it consistently improves grounded reasoning without any retraining, establishing inference-time contrastive decoding over structured 3D representations as an effective and practical route to more reliable embodied intelligence.

摘要：大型多模態模型越來越多地被用作在 3D 環境中運作的具身代理的推理核心，但它們仍然容易出現幻覺，可能導致不安全和沒有根據的決策。現有的推理時幻覺緩解方法主要針對 2D 視覺-語言設置，並且無法轉移到具身的 3D 推理中，因為失敗源於物體存在、空間佈局和幾何基礎，而不是像素級的不一致。我們介紹 3D-VCD，首個用於 3D 具身代理的推理時視覺對比解碼框架，以緩解幻覺。3D-VCD 通過對以物體為中心的表示應用語義和幾何擾動來構建扭曲的 3D 場景圖，例如類別替換和坐標或範圍損壞。通過對比原始和扭曲的 3D 上下文下的預測，我們的方法抑制對基於場景證據不敏感的標記，因此這些標記可能是由語言先驗驅動的。我們在 3D-POPE 和 HEAL 基準上評估 3D-VCD，並顯示它在不進行任何重新訓練的情況下持續改善基於證據的推理，確立了在結構化 3D 表示上進行推理時對比解碼作為更可靠的具身智能的一條有效且實用的途徑。

##### **Cram Less to Fit More: Training Data Pruning Improves Memorization of Facts**
2604.08519v1 by Jiayuan Ye, Vitaly Feldman, Kunal Talwar

Large language models (LLMs) can struggle to memorize factual knowledge in their parameters, often leading to hallucinations and poor performance on knowledge-intensive tasks. In this paper, we formalize fact memorization from an information-theoretic perspective and study how training data distributions affect fact accuracy. We show that fact accuracy is suboptimal (below the capacity limit) whenever the amount of information contained in the training data facts exceeds model capacity. This is further exacerbated when the fact frequency distribution is skewed (e.g. a power law).
  We propose data selection schemes based on the training loss alone that aim to limit the number of facts in the training data and flatten their frequency distribution. On semi-synthetic datasets containing high-entropy facts, our selection method effectively boosts fact accuracy to the capacity limit. When pretraining language models from scratch on an annotated Wikipedia corpus, our selection method enables a GPT2-Small model (110m parameters) to memorize 1.3X more entity facts compared to standard training, matching the performance of a 10X larger model (1.3B parameters) pretrained on the full dataset.

摘要：大型語言模型（LLMs）在其參數中記憶事實知識時可能會遇到困難，這常常導致幻覺和在知識密集型任務上的表現不佳。<lb/>在本文中，我們從信息論的角度形式化事實記憶，並研究訓練數據分佈如何影響事實準確性。<lb/>我們顯示，當訓練數據事實中包含的信息量超過模型容量時，事實準確性是次優的（低於容量限制）。<lb/>當事實頻率分佈偏斜（例如，冪律）時，這一問題進一步加劇。<lb/>我們提出基於訓練損失的數據選擇方案，旨在限制訓練數據中的事實數量並平坦其頻率分佈。<lb/>在包含高熵事實的半合成數據集上，我們的選擇方法有效地將事實準確性提升至容量限制。<lb/>在從頭開始對帶註釋的維基百科語料庫進行語言模型的預訓練時，我們的選擇方法使GPT2-Small模型（110m參數）能夠記憶比標準訓練多1.3倍的實體事實，並且其性能與在完整數據集上預訓練的10倍大模型（1.3B參數）相匹配。

##### **On Semiotic-Grounded Interpretive Evaluation of Generative Art**
2604.08641v1 by Ruixiang Jiang, Changwen Chen

Interpretation is essential to deciphering the language of art: audiences communicate with artists by recovering meaning from visual artifacts. However, current Generative Art (GenArt) evaluators remain fixated on surface-level image quality or literal prompt adherence, failing to assess the deeper symbolic or abstract meaning intended by the creator. We address this gap by formalizing a Peircean computational semiotic theory that models Human-GenArt Interaction (HGI) as cascaded semiosis. This framework reveals that artistic meaning is conveyed through three modes - iconic, symbolic, and indexical - yet existing evaluators operate heavily within the iconic mode, remaining structurally blind to the latter two. To overcome this structural blindness, we propose SemJudge. This evaluator explicitly assesses symbolic and indexical meaning in HGI via a Hierarchical Semiosis Graph (HSG) that reconstructs the meaning-making process from prompt to generated artifact. Extensive quantitative experiments show that SemJudge aligns more closely with human judgments than prior evaluators on an interpretation-intensive fine-art benchmark. User studies further demonstrate that SemJudge produces deeper, more insightful artistic interpretations, thereby paving the way for GenArt to move beyond the generation of "pretty" images toward a medium capable of expressing complex human experience. Project page: https://github.com/songrise/SemJudge.

摘要：詮釋對於解讀藝術語言至關重要：觀眾通過從視覺工藝中恢復意義來與藝術家交流。然而，目前的生成藝術（GenArt）評估者仍然專注於表面層次的圖像質量或字面提示的遵循，未能評估創作者所意圖的更深層的象徵或抽象意義。我們通過形式化一種皮爾斯計算符號學理論來解決這一差距，該理論將人類與生成藝術的互動（HGI）建模為層疊的符號學。這一框架揭示了藝術意義是通過三種模式傳達的——圖像模式、象徵模式和指標模式——然而現有的評估者主要在圖像模式內運作，對後兩者結構性失明。為了克服這一結構性失明，我們提出了SemJudge。這一評估者明確評估HGI中的象徵和指標意義，通過一個層級符號學圖（HSG）重建從提示到生成工藝的意義創造過程。廣泛的定量實驗顯示，SemJudge在一個以詮釋為重點的美術基準上與人類判斷的對齊程度比以往的評估者更高。用戶研究進一步證明，SemJudge產生了更深刻、更具洞察力的藝術詮釋，從而為生成藝術的發展鋪平了道路，使其能夠超越生成“美麗”圖像，朝向一種能夠表達複雜人類經驗的媒介。項目頁面：https://github.com/songrise/SemJudge。

##### **HST-HGN: Heterogeneous Spatial-Temporal Hypergraph Networks with Bidirectional State Space Models for Global Fatigue Assessment**
2604.08435v1 by Changdao Chen

It remains challenging to assess driver fatigue from untrimmed videos under constrained computational budgets, due to the difficulty of modeling long-range temporal dependencies in subtle facial expressions. Some existing approaches rely on computationally heavy architectures, whereas others employ traditional lightweight pairwise graph networks, despite their limited capacity to model high-order synergies and global temporal context. Therefore, we propose HST-HGN, a novel Heterogeneous Spatial-Temporal Hypergraph Network driven by Bidirectional State Space Models. Spatially, we introduce a hierarchical hypergraph network to fuse pose-disentangled geometric topologies with multi-modal texture patches dynamically. This formulation encapsulates high-order synergistic facial deformations, effectively overcoming the limitations of conventional methods. In temporal terms, a Bi-Mamba module with linear complexity is applied to perform bidirectional sequence modeling. This explicit temporal-evolution filtering enables the network to distinguish highly ambiguous transient actions, such as yawning versus speaking, while encompassing their complete physiological lifecycles. Extensive evaluations across diverse fatigue benchmarks demonstrate that HST-HGN achieves state-of-the-art performance. In particular, our method strikes a balance between discriminative power and computational efficiency, making it well-suited for real-time in-cabin edge deployment.

摘要：評估駕駛疲勞仍然是一項挑戰，因為在受限的計算預算下，從未修剪的視頻中建模微妙的面部表情的長期時間依賴性非常困難。一些現有的方法依賴於計算密集型架構，而其他方法則採用傳統的輕量級成對圖網絡，儘管它們在建模高階協同作用和全球時間上下文方面能力有限。因此，我們提出了HST-HGN，一種由雙向狀態空間模型驅動的全新異質空間-時間超圖網絡。在空間上，我們引入了一個層次化的超圖網絡，以動態融合姿勢解耦的幾何拓撲與多模態紋理補丁。這種表述封裝了高階協同的面部變形，有效克服了傳統方法的局限性。在時間方面，應用了一個線性複雜度的Bi-Mamba模塊來執行雙向序列建模。這種明確的時間演變過濾使得網絡能夠區分高度模糊的瞬態行為，例如打哈欠與說話，同時涵蓋它們的完整生理生命周期。在各種疲勞基準上的廣泛評估顯示，HST-HGN達到了最先進的性能。特別是，我們的方法在區分能力和計算效率之間取得了平衡，使其非常適合實時車內邊緣部署。

##### **KV Cache Offloading for Context-Intensive Tasks**
2604.08426v1 by Andrey Bocharnikov, Ivan Ermakov, Denis Kuznedelev, Vyacheslav Zhdanovskiy, Yegor Yershov

With the growing demand for long-context LLMs across a wide range of applications, the key-value (KV) cache has become a critical bottleneck for both latency and memory usage. Recently, KV-cache offloading has emerged as a promising approach to reduce memory footprint and inference latency while preserving accuracy. Prior evaluations have largely focused on tasks that do not require extracting large amounts of information from the context. In this work, we study KV-cache offloading on context-intensive tasks: problems where the solution requires looking up a lot of information from the input prompt. We create and release the Text2JSON benchmark, a highly context-intensive task that requires extracting structured knowledge from raw text. We evaluate modern KV offloading on Text2JSON and other context-intensive tasks and find significant performance degradation on both Llama 3 and Qwen 3 models. Our analysis identifies two key reasons for poor accuracy: low-rank projection of keys and unreliable landmarks, and proposes a simpler alternative strategy that significantly improves accuracy across multiple LLM families and benchmarks. These findings highlight the need for a comprehensive and rigorous evaluation of long-context compression techniques.

摘要：隨著對長上下文 LLM 的需求在各種應用中不斷增長，關鍵-值 (KV) 快取已成為延遲和記憶體使用的關鍵瓶頸。最近，KV 快取卸載作為一種有前景的方法出現，旨在減少記憶體佔用和推斷延遲，同時保持準確性。先前的評估主要集中在不需要從上下文中提取大量信息的任務上。在本研究中，我們研究了在上下文密集型任務上進行 KV 快取卸載：這些問題的解決方案需要從輸入提示中查找大量信息。我們創建並發布了 Text2JSON 基準測試，這是一個高度上下文密集型的任務，需要從原始文本中提取結構化知識。我們對 Text2JSON 和其他上下文密集型任務評估了現代 KV 卸載，發現 Llama 3 和 Qwen 3 模型的性能顯著下降。我們的分析確定了準確性差的兩個主要原因：鍵的低秩投影和不可靠的標記，並提出了一種更簡單的替代策略，顯著提高了多個 LLM 家族和基準的準確性。這些發現突顯了對長上下文壓縮技術進行全面和嚴格評估的必要性。

##### **Retrieval Augmented Classification for Confidential Documents**
2604.08628v1 by Yeseul E. Chang, Rahul Kailasa, Simon Shim, Byunghoon Oh, Jaewoo Lee

Unauthorized disclosure of confidential documents demands robust, low-leakage classification. In real work environments, there is a lot of inflow and outflow of documents. To continuously update knowledge, we propose a methodology for classifying confidential documents using Retrieval Augmented Classification (RAC). To confirm this effectiveness, we compare RAC and supervised fine tuning (FT) on the WikiLeaks US Diplomacy corpus under realistic sequence-length constraints. On balanced data, RAC matches FT. On unbalanced data, RAC is more stable while delivering comparable performance--about 96% Accuracy on both the original (unbalanced) and augmented (balanced) sets, and up to 94% F1 with proper prompting--whereas FT attains 90% F1 trained on the augmented, balanced set but drops to 88% F1 trained on the original, unbalanced set. When robust augmentation is infeasible, RAC provides a practical, security-preserving path to strong classification by keeping sensitive content out of model weights and under your control, and it remains robust as real-world conditions change in class balance, data, context length, or governance requirements. Because RAC grounds decisions in an external vector store with similarity matching, it is less sensitive to label skew, reduces parameter-level leakage, and can incorporate new data immediately via reindexing--a difficult step for FT, which typically requires retraining. The contributions of this paper are threefold: first, a RAC-based classification pipeline and evaluation recipe; second, a controlled study that isolates class imbalance and context-length effects for FT versus RAC in confidential-document grading; and third, actionable guidance on RAC design patterns for governed deployments.

摘要：未經授權的機密文件披露需要強健且低洩漏的分類。在實際工作環境中，文件的流入和流出非常頻繁。為了持續更新知識，我們提出了一種使用檢索增強分類（RAC）來分類機密文件的方法。為了確認其有效性，我們在現實的序列長度限制下，將RAC與監督微調（FT）在WikiLeaks美國外交語料庫進行比較。在平衡數據上，RAC的表現與FT相當。在不平衡數據上，RAC則更穩定，同時提供可比的性能——在原始（不平衡）和增強（平衡）數據集上，準確率約為96%，並且在適當提示下F1值高達94%——而FT在增強的平衡數據集上達到90%的F1值，但在原始的不平衡數據集上下降至88%的F1值。當強健的增強不可行時，RAC提供了一條實用的、安全的強分類途徑，通過將敏感內容保持在模型權重之外並掌控在自己手中，並且隨著現實條件在類別平衡、數據、上下文長度或治理要求的變化而保持穩健。因為RAC將決策基於具有相似性匹配的外部向量存儲，因此對標籤偏斜的敏感度較低，減少了參數級別的洩漏，並且可以通過重新索引立即納入新數據——這對於FT來說是一個困難的步驟，通常需要重新訓練。本文的貢獻有三方面：首先，一個基於RAC的分類管道和評估配方；其次，一項控制研究，隔離FT與RAC在機密文件評分中的類別不平衡和上下文長度效應；第三，對於治理部署的RAC設計模式提供可行的指導。

##### **SkillClaw: Let Skills Evolve Collectively with Agentic Evolver**
2604.08377v1 by Ziyu Ma, Shidong Yang, Yuxiang Ji, Xucong Wang, Yong Wang, Yiming Hu, Tongwen Huang, Xiangxiang Chu

Large language model (LLM) agents such as OpenClaw rely on reusable skills to perform complex tasks, yet these skills remain largely static after deployment. As a result, similar workflows, tool usage patterns, and failure modes are repeatedly rediscovered across users, preventing the system from improving with experience. While interactions from different users provide complementary signals about when a skill works or fails, existing systems lack a mechanism to convert such heterogeneous experiences into reliable skill updates. To address these issues, we present SkillClaw, a framework for collective skill evolution in multi-user agent ecosystems, which treats cross-user and over-time interactions as the primary signal for improving skills. SkillClaw continuously aggregates trajectories generated during use and processes them with an autonomous evolver, which identifies recurring behavioral patterns and translates them into updates to the skill set by refining existing skills or extending them with new capabilities. The resulting skills are maintained in a shared repository and synchronized across users, allowing improvements discovered in one context to propagate system-wide while requiring no additional effort from users. By integrating multi-user experience into ongoing skill updates, SkillClaw enables cross-user knowledge transfer and cumulative capability improvement, and experiments on WildClawBench show that limited interaction and feedback, it significantly improves the performance of Qwen3-Max in real-world agent scenarios.

摘要：大型語言模型（LLM）代理如 OpenClaw 依賴可重用的技能來執行複雜任務，但這些技能在部署後仍然大部分保持靜態。<lb/>因此，類似的工作流程、工具使用模式和失敗模式在用戶之間不斷被重新發現，阻礙了系統隨著經驗的增長而改進。<lb/>雖然來自不同用戶的互動提供了關於技能何時有效或失敗的互補信號，但現有系統缺乏將這些異質經驗轉化為可靠技能更新的機制。<lb/>為了解決這些問題，我們提出了 SkillClaw，一個在多用戶代理生態系統中進行集體技能演化的框架，將跨用戶和隨時間的互動視為改進技能的主要信號。<lb/>SkillClaw 持續聚合使用過程中生成的軌跡，並通過自主演化器處理這些軌跡，該演化器識別重複的行為模式，並將其轉化為對技能集的更新，通過精煉現有技能或用新能力擴展它們。<lb/>結果產生的技能被保存在共享庫中，並在用戶之間同步，允許在一個上下文中發現的改進在系統範圍內傳播，同時不需要用戶額外的努力。<lb/>通過將多用戶經驗整合到持續的技能更新中，SkillClaw 實現了跨用戶知識轉移和累積能力提升，並且在 WildClawBench 上的實驗顯示，有限的互動和反饋顯著改善了 Qwen3-Max 在現實世界代理場景中的性能。

##### **ASPECT:Analogical Semantic Policy Execution via Language Conditioned Transfer**
2604.08355v2 by Ajsal Shereef Palattuparambil, Thommen George Karimpanal, Santu Rana

Reinforcement Learning (RL) agents often struggle to generalize knowledge to new tasks, even those structurally similar to ones they have mastered. Although recent approaches have attempted to mitigate this issue via zero-shot transfer, they are often constrained by predefined, discrete class systems, limiting their adaptability to novel or compositional task variations. We propose a significantly more generalized approach, replacing discrete latent variables with natural language conditioning via a text-conditioned Variational Autoencoder (VAE). Our core innovation utilizes a Large Language Model (LLM) as a dynamic \textit{semantic operator} at test time. Rather than relying on rigid rules, our agent queries the LLM to semantically remap the description of the current observation to align with the source task. This source-aligned caption conditions the VAE to generate an imagined state compatible with the agent's original training, enabling direct policy reuse. By harnessing the flexible reasoning capabilities of LLMs, our approach achieves zero-shot transfer across a broad spectrum of complex and truly novel analogous tasks, moving beyond the limitations of fixed category mappings. Code and videos are available \href{https://anonymous.4open.science/r/ASPECT-85C3/}{here}.

摘要：強化學習（RL）代理通常難以將知識泛化到新任務，即使這些任務在結構上與他們已經掌握的任務相似。儘管最近的研究嘗試通過零樣本轉移來減輕這一問題，但它們往往受到預定義的離散類別系統的限制，限制了它們對新穎或組合任務變化的適應能力。我們提出了一種顯著更具泛化性的方案，通過文本條件變分自編碼器（VAE）用自然語言條件取代離散潛變量。我們的核心創新是在測試時利用大型語言模型（LLM）作為動態的\textit{語義運算符}。我們的代理不是依賴於僵化的規則，而是查詢LLM，將當前觀察的描述進行語義重映射，以便與源任務對齊。這種源對齊的標題使VAE能夠生成與代理原始訓練相容的想像狀態，從而實現直接的策略重用。通過利用LLM的靈活推理能力，我們的方法在一系列複雜且真正新穎的類比任務中實現了零樣本轉移，超越了固定類別映射的限制。代碼和視頻可在\href{https://anonymous.4open.science/r/ASPECT-85C3/}{這裡}獲得。

##### **Dead Weights, Live Signals: Feedforward Graphs of Frozen Language Models**
2604.08335v1 by Marcus Armstrong, Navid Ayoobi, Arjun Mukherjee

We present a feedforward graph architecture in which heterogeneous frozen large language models serve as computational nodes, communicating through a shared continuous latent space via learned linear projections. Building on recent work demonstrating geometric compatibility between independently trained LLM latent spaces~\cite{armstrong2026thinking}, we extend this finding from static two-model steering to end-to-end trainable multi-node graphs, where projection matrices are optimized jointly via backpropagation through residual stream injection hooks. Three small frozen models (Llama-3.2-1B, Qwen2.5-1.5B, Gemma-2-2B) encode the input into a shared latent space whose aggregate signal is injected into two larger frozen models (Phi-3-mini, Mistral-7B), whose representations feed a lightweight cross-attention output node. With only 17.6M trainable parameters against approximately 12B frozen, the architecture achieves 87.3\% on ARC-Challenge, 82.8\% on OpenBookQA, and 67.2\% on MMLU, outperforming the best single constituent model by 11.4, 6.2, and 1.2 percentage points respectively, and outperforming parameter-matched learned classifiers on frozen single models by 9.1, 5.2, and 6.7 points. Gradient flow through multiple frozen model boundaries is empirically verified to be tractable, and the output node develops selective routing behavior across layer-2 nodes without explicit supervision.

摘要：我們提出了一種前饋圖架構，其中異質的凍結大型語言模型作為計算節點，通過學習的線性投影在共享的連續潛在空間中進行通信。基於最近的研究，展示了獨立訓練的LLM潛在空間之間的幾何相容性~\cite{armstrong2026thinking}，我們將這一發現從靜態的雙模型引導擴展到端到端可訓練的多節點圖，其中投影矩陣通過殘差流注入鉤子進行聯合優化。三個小型凍結模型（Llama-3.2-1B、Qwen2.5-1.5B、Gemma-2-2B）將輸入編碼到共享潛在空間中，其聚合信號被注入到兩個較大的凍結模型（Phi-3-mini、Mistral-7B）中，這些模型的表示供應給一個輕量級的交叉注意力輸出節點。僅用17.6M可訓練參數對抗約12B的凍結參數，該架構在ARC-Challenge上達到87.3\%、在OpenBookQA上達到82.8\%、在MMLU上達到67.2\%，分別超過最佳單一組成模型11.4、6.2和1.2個百分點，並在凍結單一模型上超過參數匹配的學習分類器9.1、5.2和6.7點。通過多個凍結模型邊界的梯度流經驗上被驗證為可行，並且輸出節點在沒有明確監督的情況下在層-2節點之間發展出選擇性路由行為。

##### **Securing Retrieval-Augmented Generation: A Taxonomy of Attacks, Defenses, and Future Directions**
2604.08304v1 by Yuming Xu, Mingtao Zhang, Zhuohan Ge, Haoyang Li, Nicole Hu, Jason Chen Zhang, Qing Li, Lei Chen

Retrieval-augmented generation (RAG) significantly enhances large language models (LLMs) but introduces novel security risks through external knowledge access. While existing studies cover various RAG vulnerabilities, they often conflate inherent LLM risks with those specifically introduced by RAG. In this paper, we propose that secure RAG is fundamentally about the security of the external knowledge-access pipeline. We establish an operational boundary to separate inherent LLM flaws from RAG-introduced or RAG-amplified threats. Guided by this perspective, we abstract the RAG workflow into six stages and organize the literature around three trust boundaries and four primary security surfaces, including pre-retrieval knowledge corruption, retrieval-time access manipulation, downstream context exploitation, and knowledge exfiltration. By systematically reviewing the corresponding attacks, defenses, remediation mechanisms, and evaluation benchmarks, we reveal that current defenses remain largely reactive and fragmented. Finally, we discuss these gaps and highlight future directions toward layered, boundary-aware protection across the entire knowledge-access lifecycle.

摘要：檢索增強生成（RAG）顯著提升了大型語言模型（LLMs），但通過外部知識訪問引入了新的安全風險。雖然現有研究涵蓋了各種 RAG 脆弱性，但它們經常將固有的 LLM 風險與 RAG 特別引入的風險混淆。在本文中，我們提出安全的 RAG 基本上關乎外部知識訪問管道的安全性。我們建立了一個操作邊界，以將固有的 LLM 缺陷與 RAG 引入或放大的威脅分開。在這一觀點的指導下，我們將 RAG 工作流程抽象為六個階段，並圍繞三個信任邊界和四個主要安全面進行文獻整理，包括檢索前知識腐敗、檢索時訪問操控、下游上下文利用和知識外洩。通過系統性地回顧相應的攻擊、防禦、補救機制和評估基準，我們揭示了當前的防禦在很大程度上仍然是反應性和零散的。最後，我們討論了這些差距，並強調了未來在整個知識訪問生命周期中實現分層、邊界意識保護的方向。

##### **U-CECE: A Universal Multi-Resolution Framework for Conceptual Counterfactual Explanations**
2604.08295v1 by Angeliki Dimitriou, Nikolaos Chaidos, Maria Lymperaiou, Giorgos Filandrianos, Giorgos Stamou

As AI models grow more complex, explainability is essential for building trust, yet concept-based counterfactual methods still face a trade-off between expressivity and efficiency. Representing underlying concepts as atomic sets is fast but misses relational context, whereas full graph representations are more faithful but require solving the NP-hard Graph Edit Distance (GED) problem. We propose U-CECE, a unified, model-agnostic multi-resolution framework for conceptual counterfactual explanations that adapts to data regime and compute budget. U-CECE spans three levels of expressivity: atomic concepts for broad explanations, relational sets-of-sets for simple interactions, and structural graphs for full semantic structure. At the structural level, both a precision-oriented transductive mode based on supervised Graph Neural Networks (GNNs) and a scalable inductive mode based on unsupervised graph autoencoders (GAEs) are supported. Experiments on the structurally divergent CUB and Visual Genome datasets characterize the efficiency-expressivity trade-off across levels, while human surveys and LVLM-based evaluation show that the retrieved structural counterfactuals are semantically equivalent to, and often preferred over, exact GED-based ground-truth explanations.

摘要：隨著AI模型變得越來越複雜，解釋性對於建立信任至關重要，但基於概念的反事實方法仍面臨表達性和效率之間的權衡。<lb/>將基本概念表示為原子集合雖然速度快，但缺乏關聯上下文，而完整的圖形表示則更為真實，但需要解決NP困難的圖編輯距離（GED）問題。<lb/>我們提出U-CECE，一個統一的、與模型無關的多解析度框架，用於概念反事實解釋，能夠適應數據範疇和計算預算。<lb/>U-CECE涵蓋三個表達層次：原子概念用於廣泛解釋，關聯集合的集合用於簡單交互，以及結構圖用於完整的語義結構。<lb/>在結構層面，支持基於監督的圖神經網絡（GNNs）的精確導向傳導模式和基於無監督圖自編碼器（GAEs）的可擴展歸納模式。<lb/>在結構上不同的CUB和Visual Genome數據集上的實驗表徵了各層次之間的效率-表達性權衡，而人類調查和基於LVLM的評估顯示，檢索到的結構反事實在語義上等同於且通常優於基於精確GED的真實解釋。

##### **Distributed Multi-Layer Editing for Rule-Level Knowledge in Large Language Models**
2604.08284v1 by Yating Wang, Wenting Zhao, Yaqi Zhao, Yongshun Gong, Yilong Yin, Haoliang Sun

Large language models store not only isolated facts but also rules that support reasoning across symbolic expressions, natural language explanations, and concrete instances. Yet most model editing methods are built for fact-level knowledge, assuming that a target edit can be achieved through a localized intervention. This assumption does not hold for rule-level knowledge, where a single rule must remain consistent across multiple interdependent forms. We investigate this problem through a mechanistic study of rule-level knowledge editing. To support this study, we extend the RuleEdit benchmark from 80 to 200 manually verified rules spanning mathematics and physics. Fine-grained causal tracing reveals a form-specific organization of rule knowledge in transformer layers: formulas and descriptions are concentrated in earlier layers, while instances are more associated with middle layers. These results suggest that rule knowledge is not uniformly localized, and therefore cannot be reliably edited by a single-layer or contiguous-block intervention. Based on this insight, we propose Distributed Multi-Layer Editing (DMLE), which applies a shared early-layer update to formulas and descriptions and a separate middle-layer update to instances. While remaining competitive on standard editing metrics, DMLE achieves substantially stronger rule-level editing performance. On average, it improves instance portability and rule understanding by 13.91 and 50.19 percentage points, respectively, over the strongest baseline across GPT-J-6B, Qwen2.5-7B, Qwen2-7B, and LLaMA-3-8B. The code is available at https://github.com/Pepper66/DMLE.

摘要：大型語言模型不僅儲存孤立的事實，還儲存支持符號表達、自然語言解釋和具體實例推理的規則。<lb/>然而，大多數模型編輯方法是為事實層級知識而設，假設可以通過局部干預來實現目標編輯。<lb/>這一假設對於規則層級知識並不成立，因為單一規則必須在多個相互依賴的形式中保持一致。<lb/>我們通過對規則層級知識編輯的機制研究來探討這個問題。<lb/>為了支持這項研究，我們將RuleEdit基準從80個手動驗證的規則擴展到200個，涵蓋數學和物理。<lb/>細粒度的因果追蹤揭示了Transformer層中規則知識的形式特定組織：公式和描述集中在較早的層，而實例則更多地與中間層相關聯。<lb/>這些結果表明，規則知識並不是均勻地局部化的，因此不能通過單層或連續區塊的干預來可靠地編輯。<lb/>基於這一見解，我們提出了分佈式多層編輯（DMLE），它對公式和描述應用共享的早期層更新，對實例則應用單獨的中間層更新。<lb/>在保持在標準編輯指標上具有競爭力的同時，DMLE在規則層級編輯性能上實現了顯著增強。<lb/>平均而言，它在GPT-J-6B、Qwen2.5-7B、Qwen2-7B和LLaMA-3-8B上分別提高了實例可攜性和規則理解13.91和50.19個百分點，超過了最強基準。<lb/>代碼可在 https://github.com/Pepper66/DMLE 獲得。

##### **When to Trust Tools? Adaptive Tool Trust Calibration For Tool-Integrated Math Reasoning**
2604.08281v1 by Ruotao Xu, Yixin Ji, Yu Luo, Jinpeng Li, Dong Li, Peifeng Li, Juntao Li, Min Zhang

Large reasoning models (LRMs) have achieved strong performance enhancement through scaling test time computation, but due to the inherent limitations of the underlying language models, they still have shortcomings in tasks that require precise computation and extensive knowledge reserves. Tool-Integrated Reasoning (TIR) has emerged as a promising paradigm that incorporates tool call and execution within the reasoning trajectory. Although recent works have released some powerful open-source TIR models, our analysis reveals that these models still suffer from critical deficiencies. We find that when the reasoning of the model conflicts with the tool results, the model tends to believe in its own reasoning. And there are cases where the tool results are correct but are ignored by the model, resulting in incorrect answers, which we define as "Tool Ignored''. This indicates that the model does not know when to trust or ignore the tool. To overcome these limitations, We introduce Adaptive Tool Trust Calibration (ATTC), a novel framework that guides the model to adaptively choose to trust or ignore the tool results based on the confidence score of generated code blocks. The experimental results from various open-source TIR models of different sizes and across multiple datasets demonstrate that ATTC effectively reduces the "Tool Ignored" issue, resulting in a performance increase of 4.1% to 7.5%.

摘要：大型推理模型（LRMs）透過擴大測試時間計算實現了強大的性能增強，但由於底層語言模型的固有限制，它們在需要精確計算和廣泛知識儲備的任務中仍然存在不足。工具整合推理（TIR）作為一種有前景的範式出現，將工具調用和執行納入推理過程中。儘管最近的研究推出了一些強大的開源TIR模型，我們的分析顯示這些模型仍然存在關鍵缺陷。我們發現當模型的推理與工具結果相衝突時，模型傾向於相信自己的推理。而且有些情況下，工具結果是正確的，但被模型忽略，導致錯誤答案，我們將其定義為「工具被忽略」。這表明模型不知道何時應該信任或忽略工具。為了克服這些限制，我們引入了自適應工具信任校準（ATTC），這是一個新穎的框架，指導模型根據生成代碼塊的置信度分數自適應地選擇信任或忽略工具結果。來自不同大小的各種開源TIR模型和多個數據集的實驗結果表明，ATTC有效減少了「工具被忽略」的問題，性能提升幅度為4.1%到7.5%。

##### **Neural-Symbolic Knowledge Tracing: Injecting Educational Knowledge into Deep Learning for Responsible Learner Modelling**
2604.08263v1 by Danial Hooshyar, Gustav Šír, Yeongwook Yang, Tommi Kärkkäinen, Raija Hämäläinen, Ekaterina Krivich, Mutlu Cukurova, Dragan Gašević, Roger Azevedo

The growing use of artificial intelligence (AI) in education, particularly large language models (LLMs), has increased interest in intelligent tutoring systems. However, LLMs often show limited adaptivity and struggle to model learners' evolving knowledge over time, highlighting the need for dedicated learner modelling approaches. Although deep knowledge tracing methods achieve strong predictive performance, their opacity and susceptibility to bias can limit alignment with pedagogical principles. To address this, we propose Responsible-DKT, a neural-symbolic deep knowledge tracing approach that integrates symbolic educational knowledge (e.g., mastery and non-mastery rules) into sequential neural models for responsible learner modelling. Experiments on a real-world dataset of students' math interactions show that Responsible-DKT outperforms both a neural-symbolic baseline and a fully data-driven PyTorch DKT model across training settings. The model achieves over 0.80 AUC with only 10% of training data and up to 0.90 AUC, improving performance by up to 13%. It also demonstrates improved temporal reliability, producing lower early- and mid-sequence prediction errors and the lowest prediction inconsistency rates across sequence lengths, indicating that prediction updates remain directionally aligned with observed student responses over time. Furthermore, the neural-symbolic approach offers intrinsic interpretability via a grounded computation graph that exposes the logic behind each prediction, enabling both local and global explanations. It also allows empirical evaluation of pedagogical assumptions, revealing that repeated incorrect responses (non-mastery) strongly influence prediction updates. These results indicate that neural-symbolic approaches enhance both performance and interpretability, mitigate data limitations, and support more responsible, human-centered AI in education.

摘要：人工智慧（AI）在教育中的日益使用，特別是大型語言模型（LLMs），引起了對智能輔導系統的興趣。<lb/>然而，LLMs 通常顯示出有限的適應性，並且難以隨著時間推移建模學習者不斷演變的知識，突顯了對專門學習者建模方法的需求。<lb/>儘管深度知識追蹤方法達到了強大的預測性能，但其不透明性和對偏見的敏感性可能限制了與教學原則的對齊。<lb/>為了解決這個問題，我們提出了 Responsible-DKT，一種神經-符號深度知識追蹤方法，將符號教育知識（例如，掌握和非掌握規則）整合到序列神經模型中，以實現負責任的學習者建模。<lb/>在一個真實世界的學生數學互動數據集上的實驗顯示，Responsible-DKT 在各種訓練設置中均優於神經-符號基準和完全數據驅動的 PyTorch DKT 模型。<lb/>該模型在僅使用 10% 訓練數據的情況下，達到了超過 0.80 的 AUC，並在高達 0.90 的 AUC 中提高了性能，提升幅度高達 13%。<lb/>它還顯示出改進的時間可靠性，產生了較低的早期和中期序列預測誤差，以及在各序列長度中最低的預測不一致率，表明預測更新隨著時間的推移仍然與觀察到的學生反應在方向上保持一致。<lb/>此外，神經-符號方法通過一個基於計算圖的內在可解釋性，揭示了每個預測背後的邏輯，實現了局部和全局的解釋。<lb/>它還允許對教學假設進行實證評估，顯示重複的錯誤反應（非掌握）對預測更新有強烈影響。<lb/>這些結果表明，神經-符號方法增強了性能和可解釋性，減輕了數據限制，並支持在教育中更負責任的以人為本的 AI。

##### **Behavior-Aware Item Modeling via Dynamic Procedural Solution Representations for Knowledge Tracing**
2604.08260v1 by Jun Seo, Sangwon Ryu, Heejin Do, Hyounghun Kim, Gary Geunbae Lee

Knowledge Tracing (KT) aims to predict learners' future performance from past interactions. While recent KT approaches have improved via learning item representations aligned with Knowledge Components, they overlook the procedural dynamics of problem solving. We propose Behavior-Aware Item Modeling (BAIM), a framework that enriches item representations by integrating dynamic procedural solution information. BAIM leverages a reasoning language model to decompose each item's solution into four problem-solving stages (i.e., understand, plan, carry out, and look back), pedagogically grounded in Polya's framework. Specifically, it derives stage-level representations from per-stage embedding trajectories, capturing latent signals beyond surface features. To reflect learner heterogeneity, BAIM adaptively routes these stage-wise representations, introducing a context-conditioned mechanism within a KT backbone, allowing different procedural stages to be emphasized for different learners. Experiments on XES3G5M and NIPS34 show that BAIM consistently outperforms strong pretraining-based baselines, achieving particularly large gains under repeated learner interactions.

摘要：知識追蹤（KT）旨在根據過去的互動預測學習者的未來表現。雖然最近的KT方法通過與知識組件對齊的學習項目表示有所改進，但它們忽略了問題解決的程序動態。我們提出了行為感知項目建模（BAIM），這是一個通過整合動態程序解決信息來豐富項目表示的框架。BAIM利用推理語言模型將每個項目的解決方案分解為四個問題解決階段（即理解、計劃、執行和回顧），這在教學上基於Polya的框架。具體而言，它從每個階段的嵌入軌跡中推導出階段級表示，捕捉超越表面特徵的潛在信號。為了反映學習者的異質性，BAIM自適應地路由這些階段級表示，在KT骨幹中引入了一種上下文條件機制，使不同的程序階段能夠對不同的學習者進行強調。在XES3G5M和NIPS34上的實驗顯示，BAIM始終優於強大的基於預訓練的基準，特別是在重複學習者互動下取得了顯著的增益。

##### **HyperMem: Hypergraph Memory for Long-Term Conversations**
2604.08256v2 by Juwei Yue, Chuanrui Hu, Jiawei Sheng, Zuyi Zhou, Wenyuan Zhang, Tingwen Liu, Li Guo, Yafeng Deng

Long-term memory is essential for conversational agents to maintain coherence, track persistent tasks, and provide personalized interactions across extended dialogues. However, existing approaches as Retrieval-Augmented Generation (RAG) and graph-based memory mostly rely on pairwise relations, which can hardly capture high-order associations, i.e., joint dependencies among multiple elements, causing fragmented retrieval. To this end, we propose HyperMem, a hypergraph-based hierarchical memory architecture that explicitly models such associations using hyperedges. Particularly, HyperMem structures memory into three levels: topics, episodes, and facts, and groups related episodes and their facts via hyperedges, unifying scattered content into coherent units. Leveraging this structure, we design a hybrid lexical-semantic index and a coarse-to-fine retrieval strategy, supporting accurate and efficient retrieval of high-order associations. Experiments on the LoCoMo benchmark show that HyperMem achieves state-of-the-art performance with 92.73% LLM-as-a-judge accuracy, demonstrating the effectiveness of HyperMem for long-term conversations.

摘要：長期記憶對於對話代理來說是至關重要的，它能維持一致性、跟踪持續任務，並在延長的對話中提供個性化的互動。<lb/>然而，現有的方法如檢索增強生成（RAG）和基於圖的記憶主要依賴於成對關係，這很難捕捉高階關聯，即多個元素之間的聯合依賴，導致檢索碎片化。<lb/>為此，我們提出了HyperMem，一種基於超圖的分層記憶架構，明確地使用超邊來建模這些關聯。<lb/>特別地，HyperMem將記憶結構化為三個層次：主題、事件和事實，並通過超邊將相關事件及其事實分組，將分散的內容統一為一致的單元。<lb/>利用這一結構，我們設計了一種混合詞彙-語義索引和粗到細的檢索策略，支持高階關聯的準確和高效檢索。<lb/>在LoCoMo基準上的實驗顯示，HyperMem達到了92.73%的LLM作為評判者的準確率，展示了HyperMem在長期對話中的有效性。

##### **Face-D(^2)CL: Multi-Domain Synergistic Representation with Dual Continual Learning for Facial DeepFake Detection**
2604.08159v1 by Yushuo Zhang, Yu Cheng, Yongkang Hu, Jiuan Zhou, Jiawei Chen, Yuan Xie, Zhaoxia Yin

The rapid advancement of facial forgery techniques poses severe threats to public trust and information security, making facial DeepFake detection a critical research priority. Continual learning provides an effective approach to adapt facial DeepFake detection models to evolving forgery patterns. However, existing methods face two key bottlenecks in real-world continual learning scenarios: insufficient feature representation and catastrophic forgetting. To address these issues, we propose Face-D(^2)CL, a framework for facial DeepFake detection. It leverages multi-domain synergistic representation to fuse spatial and frequency-domain features for the comprehensive capture of diverse forgery traces, and employs a dual continual learning mechanism that combines Elastic Weight Consolidation (EWC), which distinguishes parameter importance for real versus fake samples, and Orthogonal Gradient Constraint (OGC), which ensures updates to task-specific adapters do not interfere with previously learned knowledge. This synergy enables the model to achieve a dynamic balance between robust anti-forgetting capabilities and agile adaptability to emerging facial forgery paradigms, all without relying on historical data replay. Extensive experiments demonstrate that our method surpasses current SOTA approaches in both stability and plasticity, achieving 60.7% relative reduction in average detection error rate, respectively. On unseen forgery domains, it further improves the average detection AUC by 7.9% compared to the current SOTA method.

摘要：快速進步的臉部偽造技術對公眾信任和信息安全構成了嚴重威脅，使得臉部 DeepFake 偵測成為一項關鍵的研究優先事項。持續學習提供了一種有效的方法，能夠使臉部 DeepFake 偵測模型適應不斷演變的偽造模式。然後，現有方法在現實世界的持續學習場景中面臨兩個主要瓶頸：特徵表示不足和災難性遺忘。為了解決這些問題，我們提出了 Face-D(^2)CL，一個用於臉部 DeepFake 偵測的框架。它利用多領域協同表示來融合空間和頻域特徵，以全面捕捉多樣的偽造痕跡，並採用一種雙重持續學習機制，結合了彈性權重整合（EWC），該機制區分真實樣本與偽造樣本的參數重要性，以及正交梯度約束（OGC），確保任務特定適配器的更新不會干擾先前學到的知識。這種協同作用使模型能夠在強大的抗遺忘能力和對新興臉部偽造範式的靈活適應之間實現動態平衡，且無需依賴歷史數據重播。大量實驗表明，我們的方法在穩定性和可塑性方面超越了當前的 SOTA 方法，平均檢測錯誤率相對減少了 60.7%。在未見過的偽造領域上，與當前的 SOTA 方法相比，平均檢測 AUC 進一步提高了 7.9%。

##### **Clickbait detection: quick inference with maximum impact**
2604.08148v1 by Soveatin Kuntur, Panggih Kusuma Ningrum, Anna Wróblewska, Maria Ganzha, Marcin Paprzycki

We propose a lightweight hybrid approach to clickbait detection that combines OpenAI semantic embeddings with six compact heuristic features capturing stylistic and informational cues. To improve efficiency, embeddings are reduced using PCA and evaluated with XGBoost, GraphSAGE, and GCN classifiers. While the simplified feature design yields slightly lower F1-scores, graph-based models achieve competitive performance with substantially reduced inference time. High ROC--AUC values further indicate strong discrimination capability, supporting reliable detection of clickbait headlines under varying decision thresholds.

摘要：我們提出了一種輕量級的混合方法來檢測誘餌標題，該方法結合了 OpenAI 的語義嵌入和六個緊湊的啟發式特徵，以捕捉風格和信息提示。<lb/>為了提高效率，嵌入使用 PCA 進行降維，並使用 XGBoost、GraphSAGE 和 GCN 分類器進行評估。<lb/>雖然簡化的特徵設計產生了略低的 F1 分數，但基於圖的模型在推斷時間大幅減少的情況下仍能達到具有競爭力的性能。<lb/>高 ROC--AUC 值進一步表明了強大的區分能力，支持在不同決策閾值下可靠地檢測誘餌標題。

##### **Graph Neural Networks for Misinformation Detection: Performance-Efficiency Trade-offs**
2604.08131v1 by Soveatin Kuntur, Maciej Krzywda, Anna Wróblewska, Marcin Paprzycki, Maria Ganzha, Szymon Łukasik, Amir H. Gandomi

The rapid spread of online misinformation has led to increasingly complex detection models, including large language models and hybrid architectures. However, their computational cost and deployment limitations raise concerns about practical applicability. In this work, we benchmark graph neural networks (GNNs) against non-graph-based machine learning methods under controlled and comparable conditions. We evaluate lightweight GNN architectures (GCN, GraphSAGE, GAT, ChebNet) against Logistic Regression, Support Vector Machines, and Multilayer Perceptrons across seven public datasets in English, Indonesian, and Polish. All models use identical TF-IDF features to isolate the impact of relational structure. Performance is measured using F1 score, with inference time reported to assess efficiency. GNNs consistently outperform non-graph baselines across all datasets. For example, GraphSAGE achieves 96.8% F1 on Kaggle and 91.9% on WELFake, compared to 73.2% and 66.8% for MLP, respectively. On COVID-19, GraphSAGE reaches 90.5% F1 vs. 74.9%, while ChebNet attains 79.1% vs. 66.4% on FakeNewsNet. These gains are achieved with comparable or lower inference times. Overall, the results show that classic GNNs remain effective and efficient, challenging the need for increasingly complex architectures in misinformation detection.

摘要：網路上錯誤資訊的快速擴散導致了日益複雜的檢測模型，包括大型語言模型和混合架構。<lb/>然而，它們的計算成本和部署限制引發了對實際應用的擔憂。<lb/>在這項工作中，我們在受控和可比較的條件下，將圖神經網絡（GNN）與非圖基礎的機器學習方法進行基準測試。<lb/>我們評估輕量級 GNN 架構（GCN、GraphSAGE、GAT、ChebNet）與邏輯回歸、支持向量機和多層感知器在七個公共數據集上的表現，這些數據集包含英語、印尼語和波蘭語。<lb/>所有模型使用相同的 TF-IDF 特徵，以隔離關係結構的影響。<lb/>性能通過 F1 分數來衡量，推理時間則用於評估效率。<lb/>GNN 在所有數據集上始終優於非圖基準。<lb/>例如，GraphSAGE 在 Kaggle 上達到 96.8% 的 F1，並在 WELFake 上達到 91.9%，而 MLP 分別為 73.2% 和 66.8%。<lb/>在 COVID-19 上，GraphSAGE 達到 90.5% 的 F1，對比 74.9%，而 ChebNet 在 FakeNewsNet 上達到 79.1% 對 66.4%。<lb/>這些增益是在可比較或更低的推理時間內實現的。<lb/>總體而言，結果顯示經典 GNN 仍然有效且高效，挑戰了在錯誤資訊檢測中對日益複雜架構的需求。

##### **Beyond Stochastic Exploration: What Makes Training Data Valuable for Agentic Search**
2604.08124v1 by Chuzhan Hao, Wenfeng Feng, Guochao Jiang, Guofeng Quan, Guohua Liu, Yuewei Zhang

Reinforcement learning (RL) has become an effective approach for advancing the reasoning capabilities of large language models (LLMs) through the strategic integration of external search engines. However, current RL-based search agents often rely on a process of stochastic exploration guided by carefully crafted outcome rewards, leading to inefficient reasoning trajectories and unstable training. To address these issues, we propose a novel framework, Hierarchical Experience (HiExp), to enhance the performance and training stability of search agents. Specifically, we extract empirical knowledge through contrastive analysis and a multi-level clustering mechanism, transforming raw reasoning trajectories into hierarchical experience knowledge. By leveraging experience-aligned training, we effectively regularize stochastic exploration, evolving it into a strategic and experience-driven search process. Extensive evaluations on multiple complex agentic search and mathematical reasoning benchmarks demonstrate that our approach not only achieves substantial performance gains but also exhibits strong cross-task and cross-algorithm generalization.

摘要：強化學習（RL）已成為通過戰略性整合外部搜尋引擎來提升大型語言模型（LLMs）推理能力的有效方法。<lb/>然而，當前基於RL的搜尋代理通常依賴於由精心設計的結果獎勵引導的隨機探索過程，這導致了低效的推理軌跡和不穩定的訓練。<lb/>為了解決這些問題，我們提出了一個新穎的框架，稱為層次經驗（HiExp），以增強搜尋代理的性能和訓練穩定性。<lb/>具體而言，我們通過對比分析和多層次聚類機制提取經驗知識，將原始推理軌跡轉化為層次經驗知識。<lb/>通過利用經驗對齊的訓練，我們有效地正則化隨機探索，將其演變為一個戰略性和經驗驅動的搜尋過程。<lb/>在多個複雜的代理搜尋和數學推理基準上進行的廣泛評估表明，我們的方法不僅實現了顯著的性能提升，還展現了強大的跨任務和跨算法的泛化能力。

##### **Uni-ViGU: Towards Unified Video Generation and Understanding via A Diffusion-Based Video Generator**
2604.08121v1 by Luozheng Qin, Jia Gong, Qian Qiao, Tianjiao Li, Li Xu, Haoyu Pan, Chao Qu, Zhiyu Tan, Hao Li

Unified multimodal models integrating visual understanding and generation face a fundamental challenge: visual generation incurs substantially higher computational costs than understanding, particularly for video. This imbalance motivates us to invert the conventional paradigm: rather than extending understanding-centric MLLMs to support generation, we propose Uni-ViGU, a framework that unifies video generation and understanding by extending a video generator as the foundation. We introduce a unified flow method that performs continuous flow matching for video and discrete flow matching for text within a single process, enabling coherent multimodal generation. We further propose a modality-driven MoE-based framework that augments Transformer blocks with lightweight layers for text generation while preserving generative priors. To repurpose generation knowledge for understanding, we design a bidirectional training mechanism with two stages: Knowledge Recall reconstructs input prompts to leverage learned text-video correspondences, while Capability Refinement fine-tunes on detailed captions to establish discriminative shared representations. Experiments demonstrate that Uni-ViGU achieves competitive performance on both video generation and understanding, validating generation-centric architectures as a scalable path toward unified multimodal intelligence. Project Page and Code: https://fr0zencrane.github.io/uni-vigu-page/.

摘要：統一的多模態模型整合視覺理解和生成面臨一個根本挑戰：視覺生成所需的計算成本遠高於理解，尤其是對於視頻。這種不平衡促使我們反轉傳統範式：我們不再延伸以理解為中心的多語言大模型（MLLMs）來支持生成，而是提出了Uni-ViGU，一個通過擴展視頻生成器作為基礎來統一視頻生成和理解的框架。我們介紹了一種統一流方法，該方法在單一過程中對視頻進行連續流匹配，對文本進行離散流匹配，從而實現連貫的多模態生成。我們進一步提出了一種基於模態驅動的MoE框架，該框架通過輕量級層增強Transformer塊以進行文本生成，同時保留生成先驗知識。為了將生成知識重新用於理解，我們設計了一種雙向訓練機制，分為兩個階段：知識回憶重建輸入提示，以利用學習到的文本-視頻對應，而能力精煉則在詳細的標題上進行微調，以建立區分性的共享表示。實驗表明，Uni-ViGU在視頻生成和理解上都達到了競爭性能，驗證了以生成為中心的架構作為通向統一多模態智能的可擴展路徑。項目頁面和代碼：https://fr0zencrane.github.io/uni-vigu-page/。

##### **OV-Stitcher: A Global Context-Aware Framework for Training-Free Open-Vocabulary Semantic Segmentation**
2604.08110v2 by Seungjae Moon, Seunghyun Oh, Youngmin Ro

Training-free open-vocabulary semantic segmentation(TF-OVSS) has recently attracted attention for its ability to perform dense prediction by leveraging the pretrained knowledge of large vision and vision-language models, without requiring additional training. However, due to the limited input resolution of these pretrained encoders, existing TF-OVSS methods commonly adopt a sliding-window strategy that processes cropped sub-images independently. While effective for managing high-resolution inputs, this approach prevents global attention over the full image, leading to fragmented feature representations and limited contextual reasoning. We propose OV-Stitcher, a training-free framework that addresses this limitation by stitching fragmented sub-image features directly within the final encoder block. By reconstructing attention representations from fragmented sub-image features, OV-Stitcher enables global attention within the final encoder block, producing coherent context aggregation and spatially consistent, semantically aligned segmentation maps. Extensive evaluations across eight benchmarks demonstrate that OV-Stitcher establishes a scalable and effective solution for open-vocabulary segmentation, achieving a notable improvement in mean Intersection over Union(mIoU) from 48.7 to 50.7 compared with prior training-free baselines.

摘要：訓練無關的開放詞彙語義分割（TF-OVSS）最近因其利用大型視覺和視覺-語言模型的預訓練知識進行密集預測的能力而受到關注，而無需額外訓練。<lb/>然而，由於這些預訓練編碼器的輸入解析度有限，現有的TF-OVSS方法通常採用滑動窗口策略，獨立處理裁剪的子圖像。<lb/>雖然這種方法對於管理高解析度輸入是有效的，但它阻止了對整個圖像的全局注意力，導致特徵表示的片段化和有限的上下文推理。<lb/>我們提出了OV-Stitcher，一個無需訓練的框架，通過在最終編碼器塊中直接拼接片段化的子圖像特徵來解決這一限制。<lb/>通過從片段化的子圖像特徵重建注意力表示，OV-Stitcher使得在最終編碼器塊內實現全局注意力，產生一致的上下文聚合和空間一致、語義對齊的分割圖。<lb/>在八個基準上的廣泛評估表明，OV-Stitcher建立了一個可擴展且有效的開放詞彙分割解決方案，與先前的無訓練基準相比，平均交集聯合（mIoU）從48.7提高到50.7，實現了顯著改善。

##### **Guaranteeing Knowledge Integration with Joint Decoding for Retrieval-Augmented Generation**
2604.08046v1 by Zhengyi Zhao, Shubo Zhang, Zezhong Wang, Yuxi Zhang, Huimin Wang, Yutian Zhao, Yefeng Zheng, Binyang Li, Kam-Fai Wong, Xian Wu

Retrieval-Augmented Generation (RAG) significantly enhances Large Language Models (LLMs) by providing access to external knowledge. However, current research primarily focuses on retrieval quality, often overlooking the critical ''integration bottleneck'': even when relevant documents are retrieved, LLMs frequently fail to utilize them effectively due to conflicts with their internal parametric knowledge. In this paper, we argue that implicitly resolving this conflict in a single generation pass is suboptimal. We introduce GuarantRAG, a framework that explicitly decouples reasoning from evidence integration. First, we generate an ''Inner-Answer'' based solely on parametric knowledge to capture the model's reasoning flow. Second, to guarantee faithful evidence extraction, we generate a ''Refer-Answer'' using a novel Contrastive DPO objective. This objective treats the parametric Inner-Answer as a negative constraint and the retrieved documents as positive ground truth, forcing the model to suppress internal hallucinations in favor of external evidence during this phase. Finally, rather than naive concatenation or using the DPO trained model directly, we propose a joint decoding mechanism that dynamically fuses the logical coherence of the Inner-Answer with the factual precision of the Refer-Answer at the token level. Experiments on five QA benchmarks demonstrate that GuarantRAG improves accuracy by up to 12.1% and reduces hallucinations by 16.3% compared to standard and dynamic RAG baselines.

摘要：檢索增強生成（RAG）顯著提升了大型語言模型（LLMs），通過提供對外部知識的訪問。然而，目前的研究主要集中在檢索質量上，往往忽視了關鍵的「整合瓶頸」：即使檢索到相關文檔，LLMs 也經常因與其內部參數知識的衝突而無法有效利用這些文檔。在本文中，我們主張在單次生成過程中隱式解決這一衝突是次優的。我們引入了 GuarantRAG，一個明確將推理與證據整合解耦的框架。首先，我們僅基於參數知識生成一個「內部答案」，以捕捉模型的推理流程。其次，為了保證忠實的證據提取，我們使用一種新穎的對比 DPO 目標生成「參考答案」。這一目標將參數內部答案視為負約束，將檢索到的文檔視為正確的真實情況，迫使模型在此階段壓抑內部幻覺，以支持外部證據。最後，我們提出了一種聯合解碼機制，而不是簡單的串聯或直接使用 DPO 訓練的模型，該機制在令牌層面動態融合內部答案的邏輯一致性與參考答案的事實精確性。在五個 QA 基準上的實驗表明，GuarantRAG 對比於標準和動態 RAG 基準，準確率提高了最多 12.1%，並將幻覺減少了 16.3%。

##### **3DrawAgent: Teaching LLM to Draw in 3D with Early Contrastive Experience**
2604.08042v1 by Hongcan Xiao, Xinyue Xiao, Yilin Wang, Yue Zhang, Yonggang Qi

Sketching in 3D space enables expressive reasoning about shape, structure, and spatial relationships, yet generating 3D sketches through natural language remains a major challenge. In this work, we introduce 3DrawAgent, a training-free, language-driven framework for 3D sketch generation that leverages large language models (LLMs) to sequentially draw 3D Bezier curves under geometric feedback. Unlike prior 2D sketch agents, our method introduces a relative experience optimization strategy that adapts the recently proposed Group Reward Policy Optimization (GRPO) paradigm. Instead of relying on explicit ground-truth supervision, we construct pairwise comparisons among generated sketches, with each pair consisting of a relatively better and a worse result based on CLIP-based perceptual rewards and LLM-based fine-grained qualitative assessment. These experiences are then used to iteratively refine the prior knowledge of 3D drawing, enabling black-box reinforcement of the model's 3D awareness. This design allows our model to self-improve its spatial understanding and drawing quality without parameter updates. Experiments show that 3DrawAgent can generate complex and coherent 3D Bezier sketches from diverse textual prompts, exhibit emergent geometric reasoning, and generalize to novel shapes, establishing a new paradigm for advancing the field of training-free 3D sketch intelligence.

摘要：在三維空間中進行草圖繪製使得對形狀、結構和空間關係的表達性推理成為可能，但通過自然語言生成三維草圖仍然是一大挑戰。在這項工作中，我們介紹了3DrawAgent，一個無需訓練的、以語言驅動的三維草圖生成框架，利用大型語言模型（LLMs）在幾何反饋下順序繪製三維貝茲曲線。與以往的2D草圖代理不同，我們的方法引入了一種相對經驗優化策略，適應了最近提出的群體獎勵政策優化（GRPO）範式。我們不依賴明確的真實監督，而是構建生成草圖之間的成對比較，每對由一個相對較好和一個較差的結果組成，這基於基於CLIP的感知獎勵和基於LLM的細緻質量評估。這些經驗然後用於迭代地細化三維繪圖的先前知識，使模型的三維意識得到黑箱強化。這一設計使我們的模型能夠在不進行參數更新的情況下自我提升其空間理解和繪圖質量。實驗表明，3DrawAgent可以從多樣的文本提示中生成複雜且連貫的三維貝茲草圖，展現出新興的幾何推理，並能夠對新形狀進行泛化，為推進無需訓練的三維草圖智能領域建立了一個新的範式。

##### **IoT-Brain: Grounding LLMs for Semantic-Spatial Sensor Scheduling**
2604.08033v1 by Zhaomeng Zhou, Lan Zhang, Junyang Wang, Mu Yuan, Junda Lin, Jinke Song

Intelligent systems powered by large-scale sensor networks are shifting from predefined monitoring to intent-driven operation, revealing a critical Semantic-to-Physical Mapping Gap. While large language models (LLMs) excel at semantic understanding, existing perception-centric pipelines operate retrospectively, overlooking the fundamental decision of what to sense and when. We formalize this proactive decision as Semantic-Spatial Sensor Scheduling (S3) and demonstrate that direct LLM planning is unreliable due to inherent gaps in representation, reasoning, and optimization. To bridge these gaps, we introduce the Spatial Trajectory Graph (STG), a neuro-symbolic paradigm governed by a verify-before-commit discipline that transforms open-ended planning into a verifiable graph optimization problem. Based on STG, we implement IoT-Brain, a concrete system embodiment, and construct TopoSense-Bench, a campus-scale benchmark with 5,250 natural-language queries across 2,510 cameras. Evaluations show that IoT-Brain boosts task success rate by 37.6% over the strongest search-intensive methods while running nearly 2 times faster and using 6.6 times fewer prompt tokens. In real-world deployment, it approaches the reliability upper bound while reducing 4.1 times network bandwidth, providing a foundational framework for LLMs to interact with the physical world with unprecedented reliability and efficiency.

摘要：智能系統由大規模傳感器網絡驅動，正從預定義的監控轉向以意圖驅動的操作，揭示了一個關鍵的語義到物理的映射差距。雖然大型語言模型（LLMs）在語義理解方面表現出色，但現有的以感知為中心的流程是回顧性的，忽視了感知什麼和何時感知的基本決策。我們將這一主動決策形式化為語義-空間傳感器調度（S3），並證明直接的LLM規劃因表示、推理和優化中的固有差距而不可靠。為了彌補這些差距，我們引入了空間軌跡圖（STG），這是一種神經-符號範式，受驗證-再承諾原則的支配，將開放式規劃轉化為可驗證的圖優化問題。基於STG，我們實現了IoT-Brain，一個具體的系統實體，並構建了TopoSense-Bench，一個擁有5,250個自然語言查詢和2,510個攝像頭的校園規模基準。評估顯示，IoT-Brain的任務成功率比最強的搜索密集方法提高了37.6%，同時運行速度幾乎快了2倍，使用的提示令牌少了6.6倍。在實際部署中，它接近可靠性上限，同時減少了4.1倍的網絡帶寬，為LLMs與物理世界之間的互動提供了前所未有的可靠性和效率的基礎框架。

##### **The ecosystem of machine learning competitions: Platforms, participants, and their impact on AI development**
2604.08001v1 by Ioannis Nasios

Machine learning competitions (MLCs) play a pivotal role in advancing artificial intelligence (AI) by fostering innovation, skill development, and practical problem-solving. This study provides a comprehensive analysis of major competition platforms such as Kaggle and Zindi, examining their workflows, evaluation methodologies, and reward structures. It further assesses competition quality, participant expertise, and global reach, with particular attention to demographic trends among top-performing competitors. By exploring the motivations of competition hosts, this paper underscores the significant role of MLCs in shaping AI development, promoting collaboration, and driving impactful technological progress. Furthermore, by combining literature synthesis with platform-level data analysis and practitioner insights a comprehensive understanding of the MLC ecosystem is provided.
  Moreover, the paper demonstrates that MLCs function at the intersection of academic research and industrial application, fostering the exchange of knowledge, data, and practical methodologies across domains. Their strong ties to open-source communities further promote collaboration, reproducibility, and continuous innovation within the broader ML ecosystem. By shaping research priorities, informing industry standards, and enabling large-scale crowdsourced problem-solving, these competitions play a key role in the ongoing evolution of AI. The study provides insights relevant to researchers, practitioners, and competition organizers, and includes an examination of the future trajectory and sustained influence of MLCs on AI development.

摘要：機器學習競賽（MLCs）在推進人工智慧（AI）方面扮演著關鍵角色，促進創新、技能發展和實際問題解決。<lb/>本研究對主要競賽平台如Kaggle和Zindi進行了全面分析，檢視其工作流程、評估方法和獎勵結構。<lb/>它進一步評估了競賽質量、參賽者專業知識和全球影響力，特別關注高績效競爭者之間的人口趨勢。<lb/>通過探索競賽主辦方的動機，本文強調了MLCs在塑造AI發展、促進合作和推動有影響力的技術進步方面的重要角色。<lb/>此外，通過結合文獻綜述、平台層級數據分析和實務見解，提供了對MLC生態系統的全面理解。<lb/>此外，本文展示了MLCs在學術研究和產業應用的交匯處運作，促進了跨領域的知識、數據和實用方法的交流。<lb/>它們與開源社區的緊密聯繫進一步促進了合作、可重複性和在更廣泛的ML生態系統中的持續創新。<lb/>通過塑造研究重點、告知行業標準和使大型眾包問題解決成為可能，這些競賽在AI的持續演變中扮演著關鍵角色。<lb/>本研究提供了對研究人員、實務者和競賽組織者相關的見解，並包括對MLCs在AI發展中的未來軌跡和持續影響的檢視。

##### **DSCA: Dynamic Subspace Concept Alignment for Lifelong VLM Editing**
2604.07965v1 by Gyanendra Das, Sai Satyam Jena

Model editing aims to update knowledge to add new concepts and change relevant information without retraining. Lifelong editing is a challenging task, prone to disrupting previously learned concepts, especially for Vision Language Models (VLMs), because sequential edits can lead to degraded reasoning and cross modal misalignment. Existing VLM knowledge editing methods based on gated adapters, activation edits, and parameter merging techniques address catastrophic forgetting seen in full fine tuning; however, they still operate in the shared representation space of the VLM, where concepts are entangled, so edits interfere with other non relevant concepts. We hypothesize that this instability persists because current methods algorithmically control edits via optimization rather than structurally separating knowledge. We introduce Dynamic Subspace Concept Alignment (DSCA) which by design mitigates this limitation by decomposing the representation space into a set of orthogonal semantic subspaces and proposing edits only in those transformed spaces. These subspaces are obtained through incremental clustering and PCA on joint vision language representations. This process structurally isolates concepts, enabling precise, non interfering edits by turning isolation from a soft training objective into an architectural property. The surgical edits are guided by a multi term loss function for maintaining task fidelity, edit locality, and cross modal alignment. With the base model frozen, our method achieves 98 percent single edit success, remains over 95 percent after 1000 sequential edits, lowers hallucination by 3 to 5 percent, and achieves the best backward transfer (BWT) scores on continual instruction tuning benchmarks. Extensive experiments demonstrate DSCA state of the art stability and knowledge retention capability in continual lifelong editing across various datasets and benchmarks.

摘要：模型編輯旨在更新知識，以添加新概念並改變相關信息，而無需重新訓練。終身編輯是一項具有挑戰性的任務，容易干擾先前學習的概念，尤其是對於視覺語言模型（VLMs），因為連續編輯可能導致推理能力下降和跨模態不對齊。現有的基於閘控適配器、激活編輯和參數合併技術的VLM知識編輯方法解決了在完全微調中出現的災難性遺忘；然而，它們仍然在VLM的共享表示空間中運作，這裡的概念是交織在一起的，因此編輯會干擾其他不相關的概念。我們假設這種不穩定性持續存在，因為當前的方法通過優化算法控制編輯，而不是結構性地分離知識。我們引入了動態子空間概念對齊（DSCA），它通過將表示空間分解為一組正交語義子空間來設計，以減輕這一限制，並僅在這些轉換的空間中提出編輯。這些子空間是通過對聯合視覺語言表示進行增量聚類和主成分分析（PCA）獲得的。這一過程結構性地隔離了概念，使得通過將隔離從一個柔性訓練目標轉變為一個架構性屬性來實現精確且不干擾的編輯。外科編輯由多項損失函數指導，以維持任務的保真度、編輯的局部性和跨模態對齊。在基礎模型凍結的情況下，我們的方法實現了98%的單次編輯成功率，在1000次連續編輯後仍保持在95%以上，降低了3%到5%的幻覺，並在持續指令微調基準上達到了最佳的向後轉移（BWT）分數。大量實驗證明，DSCA在各種數據集和基準上的持續終身編輯中展現了最先進的穩定性和知識保留能力。

##### **Rethinking Data Mixing from the Perspective of Large Language Models**
2604.07963v1 by Yuanjian Xu, Tianze Sun, Changwei Xu, XinLong Zhao, Jianing Hao, Ran Chen, Yang Liu, Ruijie Xu, Stephen Chen, Guang Zhang

Data mixing strategy is essential for large language model (LLM) training. Empirical evidence shows that inappropriate strategies can significantly reduce generalization. Although recent methods have improved empirical performance, several fundamental questions remain open: what constitutes a domain, whether human and model perceptions of domains are aligned, and how domain weighting influences generalization. We address these questions by establishing formal connections between gradient dynamics and domain distributions, offering a theoretical framework that clarifies the role of domains in training dynamics. Building on this analysis, we introduce DoGraph, a reweighting framework that formulates data scheduling as a graph-constrained optimization problem. Extensive experiments on GPT-2 models of varying scales demonstrate that DoGraph consistently achieves competitive performance.

摘要：數據混合策略對於大型語言模型（LLM）的訓練至關重要。實證證據顯示，不當的策略會顯著降低泛化能力。儘管最近的方法改善了實證性能，但仍有幾個基本問題尚未解決：什麼構成一個領域，人類和模型對領域的感知是否一致，以及領域加權如何影響泛化。我們通過建立梯度動力學和領域分佈之間的正式聯繫來解決這些問題，提供了一個理論框架，澄清了領域在訓練動力學中的作用。在此分析的基礎上，我們引入了DoGraph，一個重新加權框架，將數據調度公式化為一個圖約束優化問題。對不同規模的GPT-2模型進行的廣泛實驗表明，DoGraph始終能夠實現具有競爭力的性能。

##### **On-Policy Distillation of Language Models for Autonomous Vehicle Motion Planning**
2604.07944v1 by Amirhossein Afsharrad, Amirhesam Abedsoltan, Ahmadreza Moradipari, Sanjay Lall

Large language models (LLMs) have recently demonstrated strong potential for autonomous vehicle motion planning by reformulating trajectory prediction as a language generation problem. However, deploying capable LLMs in resource-constrained onboard systems remains a fundamental challenge. In this paper, we study how to effectively transfer motion planning knowledge from a large teacher LLM to a smaller, more deployable student model. We build on the GPT-Driver framework, which represents driving scenes as language prompts and generates waypoint trajectories with chain-of-thought reasoning, and investigate two student training paradigms: (i) on-policy generalized knowledge distillation (GKD), which trains the student on its own self-generated outputs using dense token-level feedback from the teacher, and (ii) a dense-feedback reinforcement learning (RL) baseline that uses the teacher's log-probabilities as per-token reward signals in a policy gradient framework. Experiments on the nuScenes benchmark show that GKD substantially outperforms the RL baseline and closely approaches teacher-level performance despite a 5$\times$ reduction in model size. These results highlight the practical value of on-policy distillation as a principled and effective approach to deploying LLM-based planners in autonomous driving systems.

摘要：大型語言模型（LLMs）最近展示了在自動駕駛車輛運動規劃方面的強大潛力，通過將軌跡預測重新定義為語言生成問題。然而，在資源受限的車載系統中部署有能力的LLMs仍然是一個基本挑戰。在本文中，我們研究如何有效地將運動規劃知識從大型教師LLM轉移到更小、更易於部署的學生模型。我們基於GPT-Driver框架，該框架將駕駛場景表示為語言提示，並通過思維鏈推理生成路徑點軌跡，並調查兩種學生訓練範式：（i）基於策略的廣義知識蒸餾（GKD），該方法使用來自教師的密集標記級反饋，對學生進行自我生成輸出的訓練，以及（ii）一種密集反饋的強化學習（RL）基準，該基準在策略梯度框架中使用教師的對數概率作為每個標記的獎勵信號。對nuScenes基準的實驗表明，GKD顯著優於RL基準，並且儘管模型大小減少了5$\times$，仍然接近教師級別的性能。這些結果突顯了基於策略的蒸餾作為一種原則性和有效的方法在自動駕駛系統中部署基於LLM的規劃者的實際價值。

##### **AnomalyAgent: Agentic Industrial Anomaly Synthesis via Tool-Augmented Reinforcement Learning**
2604.07900v1 by Jiaming Su, Tengchao Yang, Ruikang Zhang, Zhengan Yan, Haoyu Sun, Linfeng Zhang

Industrial anomaly generation is a crucial method for alleviating the data scarcity problem in anomaly detection tasks. Most existing anomaly synthesis methods rely on single-step generation mechanisms, lacking complex reasoning and iterative optimization capabilities, making it difficult to generate anomaly samples with high semantic realism. We propose AnomalyAgent, an anomaly synthesis agent with self-reflection, knowledge retrieval, and iterative refinement capabilities, aiming to generate realistic and diverse anomalies. Specifically, AnomalyAgent is equipped with five tools: Prompt Generation (PG), Image Generation (IG), Quality Evaluation (QE), Knowledge Retrieval (KR), and Mask Generation (MG), enabling closed-loop optimization. To improve decision-making and self-reflection, we construct structured trajectories from real anomaly images and design a two-stage training framework: supervised fine-tuning followed by reinforcement learning. This process is driven by a three-part reward mechanism: (1) task rewards to supervise the quality and location rationality of generated anomalies; (2) reflection rewards to train the model's ability to improve anomaly synthesis prompt; (3) behavioral rewards to ensure adherence to the trajectory. On the MVTec-AD dataset, AnomalyAgent achieves IS/IC-L of 2.10/0.33 for anomaly generation, 57.0% classification accuracy using ResNet34, and 99.3%/74.2% AP at the image/pixel level using a simple UNet, surpassing all zero-shot SOTA methods. The code and data will be made publicly available.

摘要：工業異常生成是一種緩解異常檢測任務中數據稀缺問題的關鍵方法。大多數現有的異常合成方法依賴於單步生成機制，缺乏複雜的推理和迭代優化能力，使得生成具有高語義真實性的異常樣本變得困難。我們提出了AnomalyAgent，一個具備自我反思、知識檢索和迭代精煉能力的異常合成代理，旨在生成真實且多樣的異常。具體而言，AnomalyAgent配備了五個工具：提示生成（PG）、圖像生成（IG）、質量評估（QE）、知識檢索（KR）和掩碼生成（MG），實現閉環優化。為了改善決策和自我反思，我們從真實異常圖像構建結構化軌跡，並設計了一個兩階段的訓練框架：先進行監督微調，然後進行強化學習。這一過程由三部分獎勵機制驅動：（1）任務獎勵以監督生成異常的質量和位置合理性；（2）反思獎勵以訓練模型改進異常合成提示的能力；（3）行為獎勵以確保遵循軌跡。在MVTec-AD數據集上，AnomalyAgent在異常生成上達到2.10/0.33的IS/IC-L，使用ResNet34達到57.0%的分類準確率，並使用簡單的UNet在圖像/像素層面達到99.3%/74.2%的AP，超越所有零樣本SOTA方法。代碼和數據將公開提供。

##### **SkillForge: Forging Domain-Specific, Self-Evolving Agent Skills in Cloud Technical Support**
2604.08618v1 by Xingyan Liu, Xiyue Luo, Linyu Li, Ganghong Huang, Jianfeng Liu, Honglin Qiao

Deploying LLM-powered agents in enterprise scenarios such as cloud technical support demands high-quality, domain-specific skills. However, existing skill creators lack domain grounding, producing skills poorly aligned with real-world task requirements. Moreover, once deployed, there is no systematic mechanism to trace execution failures back to skill deficiencies and drive targeted refinements, leaving skill quality stagnant despite accumulating operational evidence. We introduce SkillForge, a self-evolving framework that closes an end-to-end creation-evaluation-refinement loop. To produce well-aligned initial skills, a Domain-Contextualized Skill Creator grounds skill synthesis in knowledge bases and historical support tickets. To enable continuous self-optimization, a three-stage pipeline -- Failure Analyzer, Skill Diagnostician, and Skill Optimizer -- automatically diagnoses execution failures in batch, pinpoints the underlying skill deficiencies, and rewrites the skill to eliminate them. This cycle runs iteratively, allowing skills to self-improve with every round of deployment feedback. Evaluated on five real-world cloud support scenarios spanning 1,883 tickets and 3,737 tasks, experiments show that: (1) the Domain-Contextualized Skill Creator produces substantially better initial skills than the generic skill creator, as measured by consistency with expert-authored reference responses from historical tickets; and (2) the self-evolution loop progressively improves skill quality from diverse starting points (including expert-authored, domain-created, and generic skills) across successive rounds, demonstrating that automated evolution can surpass manually curated expert knowledge.

摘要：在企業場景中部署以 LLM 為驅動的代理，例如雲端技術支援，需要高品質的領域專業技能。<lb/>然而，現有的技能創建者缺乏領域基礎，所產生的技能與現實任務需求不匹配。<lb/>此外，一旦部署後，沒有系統性的機制可以將執行失敗追溯到技能缺陷並推動針對性的改進，導致技能質量停滯不前，儘管累積了運營證據。<lb/>我們介紹了 SkillForge，一個自我演化的框架，閉合了端到端的創建-評估-改進循環。<lb/>為了生成良好對齊的初始技能，領域情境化技能創建者將技能合成基於知識庫和歷史支援票據進行基礎。<lb/>為了實現持續的自我優化，一個三階段的管道——失敗分析器、技能診斷器和技能優化器——自動批量診斷執行失敗，確定根本的技能缺陷，並重寫技能以消除它們。<lb/>這個循環以迭代方式運行，允許技能在每一輪部署反饋中自我改進。<lb/>在五個涵蓋 1,883 張票據和 3,737 個任務的真實雲端支援場景中進行評估，實驗顯示： (1) 領域情境化技能創建者產生的初始技能顯著優於通用技能創建者，這是通過與歷史票據中專家撰寫的參考回應的一致性來衡量的； (2) 自我演化循環逐步改善技能質量，從多樣的起點（包括專家撰寫的、領域創建的和通用技能）在連續的回合中展現出來，證明自動演化可以超越手動策劃的專家知識。

##### **MemReader: From Passive to Active Extraction for Long-Term Agent Memory**
2604.07877v2 by Jingyi Kang, Chunyu Li, Ding Chen, Bo Tang, Feiyu Xiong, Zhiyu Li

Long-term memory is fundamental for personalized and autonomous agents, yet populating it remains a bottleneck. Existing systems treat memory extraction as a one-shot, passive transcription from context to structured entries, which struggles with noisy dialogue, missing references, and cross-turn dependencies, leading to memory pollution, low-value writes, and inconsistency. In this paper, we introduce the MemReader family for active long-term memory extraction in agent systems: MemReader-0.6B, a compact and cost-efficient passive extractor distilled for accurate and schema-consistent structured outputs, and MemReader-4B, an active extractor optimized with Group Relative Policy Optimization (GRPO) to make memory writing decisions. Under a ReAct-style paradigm, MemReader-4B explicitly evaluates information value, reference ambiguity, and completeness before acting, and can selectively write memories, defer incomplete inputs, retrieve historical context, or discard irrelevant chatter. Experiments on LOCOMO, LongMemEval, and HaluMem show that MemReader consistently outperforms existing extraction-based baselines. In particular, MemReader-4B achieves state-of-the-art performance on tasks involving knowledge updating, temporal reasoning, and hallucination reduction. These results suggest that effective agent memory requires not merely extracting more information, but performing reasoning-driven and selective memory extraction to build low-noise and dynamically evolving long-term memory. Furthermore, MemReader has been integrated into MemOS and is being deployed in real-world applications. To support future research and adoption, we release the models and provide public API access.

摘要：長期記憶對於個性化和自主代理至關重要，但填充它仍然是一個瓶頸。現有系統將記憶提取視為從上下文到結構化條目的一次性、被動的轉錄，這在嘈雜的對話、缺失的參考和跨回合依賴方面面臨挑戰，導致記憶污染、低價值寫入和不一致性。在本文中，我們介紹了 MemReader 系列，用於代理系統中的主動長期記憶提取：MemReader-0.6B，一個緊湊且具有成本效益的被動提取器，經過提煉以實現準確且符合結構的輸出，以及 MemReader-4B，一個使用群體相對政策優化（GRPO）進行優化的主動提取器，以做出記憶寫入決策。在 ReAct 風格的範式下，MemReader-4B 在行動之前明確評估信息價值、參考模糊性和完整性，並可以選擇性地寫入記憶、延遲不完整的輸入、檢索歷史上下文或丟棄無關的閒聊。在 LOCOMO、LongMemEval 和 HaluMem 的實驗中，MemReader 始終優於現有的基於提取的基準。特別是，MemReader-4B 在涉及知識更新、時間推理和幻覺減少的任務上達到了最先進的性能。這些結果表明，有效的代理記憶不僅需要提取更多信息，還需要進行推理驅動和選擇性的記憶提取，以構建低噪聲和動態演變的長期記憶。此外，MemReader 已經集成到 MemOS 中，並正在實際應用中部署。為了支持未來的研究和採用，我們發布了模型並提供公共 API 訪問。

##### **PyVRP$^+$: LLM-Driven Metacognitive Heuristic Evolution for Hybrid Genetic Search in Vehicle Routing Problems**
2604.07872v1 by Manuj Malik, Jianan Zhou, Shashank Reddy Chirra, Zhiguang Cao

Designing high-performing metaheuristics for NP-hard combinatorial optimization problems, such as the Vehicle Routing Problem (VRP), remains a significant challenge, often requiring extensive domain expertise and manual tuning. Recent advances have demonstrated the potential of large language models (LLMs) to automate this process through evolutionary search. However, existing methods are largely reactive, relying on immediate performance feedback to guide what are essentially black-box code mutations. Our work departs from this paradigm by introducing Metacognitive Evolutionary Programming (MEP), a framework that elevates the LLM to a strategic discovery agent. Instead of merely reacting to performance scores, MEP compels the LLM to engage in a structured Reason-Act-Reflect cycle, forcing it to explicitly diagnose failures, formulate design hypotheses, and implement solutions grounded in pre-supplied domain knowledge. By applying MEP to evolve core components of the state-of-the-art Hybrid Genetic Search (HGS) algorithm, we discover novel heuristics that significantly outperform the original baseline. By steering the LLM to reason strategically about the exploration-exploitation trade-off, our approach discovers more effective and efficient heuristics applicable across a wide spectrum of VRP variants. Our results show that MEP discovers heuristics that yield significant performance gains over the original HGS baseline, improving solution quality by up to 2.70\% and reducing runtime by over 45\% on challenging VRP variants.

摘要：設計高效能的元啟發式演算法以解決 NP 困難的組合優化問題，例如車輛路徑規劃問題（VRP），仍然是一個重大挑戰，通常需要廣泛的領域專業知識和手動調整。最近的進展已顯示大型語言模型（LLMs）在通過進化搜索自動化此過程方面的潛力。然而，現有方法主要是反應性的，依賴即時性能反饋來指導本質上是黑箱的程式碼變異。我們的工作脫離了這一範式，提出了元認知進化編程（MEP），這是一個將 LLM 提升為戰略發現代理的框架。MEP 不僅僅是對性能評分作出反應，而是迫使 LLM 參與結構化的推理-行動-反思循環，強迫其明確診斷失敗、制定設計假設，並實施基於預先提供的領域知識的解決方案。通過將 MEP 應用於進化最先進的混合遺傳搜索（HGS）演算法的核心組件，我們發現了顯著超越原始基準的新啟發式方法。通過引導 LLM 在探索-利用權衡方面進行戰略性推理，我們的方法發現了更有效且高效的啟發式方法，適用於各種 VRP 變體。我們的結果顯示，MEP 發現的啟發式方法在性能上顯著優於原始 HGS 基準，解決方案質量提高了高達 2.70\%，並在挑戰性的 VRP 變體上將運行時間減少了超過 45\%。

##### **From Selection to Scheduling: Federated Geometry-Aware Correction Makes Exemplar Replay Work Better under Continual Dynamic Heterogeneity**
2604.08617v1 by Zhuang Qi, Ying-Peng Tang, Lei Meng, Guoqing Chao, Lei Wu, Han Yu, Xiangxu Meng

Exemplar replay has become an effective strategy for mitigating catastrophic forgetting in federated continual learning (FCL) by retaining representative samples from past tasks. Existing studies focus on designing sample-importance estimation mechanisms to identify information-rich samples. However, they typically overlook strategies for effectively utilizing the selected exemplars, which limits their performance under continual dynamic heterogeneity across clients and tasks. To address this issue, this paper proposes a Federated gEometry-Aware correcTion method, termed FEAT, which alleviates imbalance-induced representation collapse that drags rare-class features toward frequent classes across clients. Specifically, it consists of two key modules: 1) the Geometric Structure Alignment module performs structural knowledge distillation by aligning the pairwise angular similarities between feature representations and their corresponding Equiangular Tight Frame prototypes, which are fixed and shared across clients to serve as a class-discriminative reference structure. This encourages geometric consistency across tasks and helps mitigate representation drift; 2) the Energy-based Geometric Correction module removes task-irrelevant directional components from feature embeddings, which reduces prediction bias toward majority classes. This improves sensitivity to minority classes and enhances the model's robustness under class-imbalanced distributions.

摘要：示例重播已成為減輕聯邦持續學習（FCL）中災難性遺忘的有效策略，通過保留過去任務的代表性樣本。現有研究專注於設計樣本重要性估計機制，以識別信息豐富的樣本。然而，它們通常忽視了有效利用所選示例的策略，這限制了它們在客戶和任務之間持續動態異質性下的性能。為了解決這個問題，本文提出了一種聯邦幾何感知校正方法，稱為FEAT，該方法緩解了由不平衡引起的表示崩潰，將稀有類特徵拖向頻繁類別。具體而言，它由兩個關鍵模塊組成：1）幾何結構對齊模塊通過對齊特徵表示之間的成對角度相似性及其相應的等角緊框原型來執行結構知識蒸餾，這些原型在客戶之間是固定且共享的，作為類別區分的參考結構。這鼓勵了任務之間的幾何一致性，並有助於減輕表示漂移；2）基於能量的幾何校正模塊從特徵嵌入中去除與任務無關的方向成分，這減少了對主要類別的預測偏差。這提高了對少數類別的敏感性，並增強了模型在類別不平衡分佈下的穩健性。

##### **ReRec: Reasoning-Augmented LLM-based Recommendation Assistant via Reinforcement Fine-tuning**
2604.07851v1 by Jiani Huang, Shijie Wang, Liangbo Ning, Wenqi Fan, Qing Li

With the rise of LLMs, there is an increasing need for intelligent recommendation assistants that can handle complex queries and provide personalized, reasoning-driven recommendations. LLM-based recommenders show potential but face challenges in multi-step reasoning, underscoring the need for reasoning-augmented systems. To address this gap, we propose ReRec, a novel reinforcement fine-tuning (RFT) framework designed to improve LLM reasoning in complex recommendation tasks. Our framework introduces three key components: (1) Dual-Graph Enhanced Reward Shaping, integrating recommendation metrics like NDCG@K with Query Alignment and Preference Alignment Scores to provide fine-grained reward signals for LLM optimization; (2) Reasoning-aware Advantage Estimation, which decomposes LLM outputs into reasoning segments and penalizes incorrect steps to enhance reasoning of recommendation; and (3) Online Curriculum Scheduler, dynamically assess query difficulty and organize training curriculum to ensure stable learning during RFT. Experiments demonstrate that ReRec outperforms state-of-the-art baselines and preserves core abilities like instruction-following and general knowledge. Our codes are available at https://github.com/jiani-huang/ReRec.

摘要：隨著大型語言模型（LLMs）的興起，對於能夠處理複雜查詢並提供個性化、基於推理的推薦的智能推薦助手的需求日益增加。基於LLM的推薦系統顯示出潛力，但在多步推理方面面臨挑戰，這凸顯了增強推理系統的必要性。為了解決這一空白，我們提出了ReRec，一種新穎的強化微調（RFT）框架，旨在改善LLM在複雜推薦任務中的推理能力。我們的框架引入了三個關鍵組件：（1）雙圖增強獎勵塑造，將NDCG@K等推薦指標與查詢對齊和偏好對齊分數整合，以提供細緻的獎勵信號以優化LLM；（2）基於推理的優勢估計，將LLM輸出分解為推理片段並對錯誤步驟進行懲罰，以增強推薦的推理能力；以及（3）在線課程調度器，動態評估查詢難度並組織訓練課程，以確保在RFT期間穩定學習。實驗表明，ReRec的表現超過了最先進的基準，並保持了指令遵循和一般知識等核心能力。我們的代碼可在 https://github.com/jiani-huang/ReRec 獲得。

##### **Filling the Gaps: Selective Knowledge Augmentation for LLM Recommenders**
2604.07825v1 by Jaehyun Lee, Sanghwan Jang, SeongKu Kang, Hwanjo Yu

Large language models (LLMs) have recently emerged as powerful training-free recommenders. However, their knowledge of individual items is inevitably uneven due to imbalanced information exposure during pretraining, a phenomenon we refer to as knowledge gap problem. To address this, most prior methods have employed a naive uniform augmentation that appends external information for every item in the input prompt. However, this approach not only wastes limited context budget on redundant augmentation for well-known items but can also hinder the model's effective reasoning. To this end, we propose KnowSA_CKP (Knowledge-aware Selective Augmentation with Comparative Knowledge Probing) to mitigate the knowledge gap problem. KnowSA_CKP estimates the LLM's internal knowledge by evaluating its capability to capture collaborative relationships and selectively injects additional information only where it is most needed. By avoiding unnecessary augmentation for well-known items, KnowSA_CKP focuses on items that benefit most from knowledge supplementation, thereby making more effective use of the context budget. KnowSA_CKP requires no fine-tuning step, and consistently improves both recommendation accuracy and context efficiency across four real-world datasets.

摘要：大型語言模型（LLMs）最近成為強大的無需訓練的推薦系統。<lb/>然而，由於在預訓練期間信息曝光不均，對個別項目的知識不可避免地存在不平衡，這種現象我們稱之為知識差距問題。<lb/>為了解決這個問題，大多數先前的方法採用了天真的均勻增強，為輸入提示中的每個項目附加外部信息。<lb/>然而，這種方法不僅浪費了有限的上下文預算在對於知名項目的冗餘增強上，還可能妨礙模型的有效推理。<lb/>為此，我們提出了KnowSA_CKP（基於知識的選擇性增強與比較知識探測）來減輕知識差距問題。<lb/>KnowSA_CKP通過評估LLM捕捉協作關係的能力來估計其內部知識，並僅在最需要的地方選擇性地注入額外信息。<lb/>通過避免對知名項目進行不必要的增強，KnowSA_CKP專注於那些最能從知識補充中受益的項目，從而更有效地利用上下文預算。<lb/>KnowSA_CKP不需要微調步驟，並在四個現實世界數據集上持續提高推薦準確性和上下文效率。

##### **Loop, Think, & Generalize: Implicit Reasoning in Recurrent-Depth Transformers**
2604.07822v1 by Harsh Kohli, Srinivasan Parthasarathy, Huan Sun, Yuekun Yao

We study implicit reasoning, i.e. the ability to combine knowledge or rules within a single forward pass. While transformer-based large language models store substantial factual knowledge and rules, they often fail to compose this knowledge for implicit multi-hop reasoning, suggesting a lack of compositional generalization over their parametric knowledge. To address this limitation, we study recurrent-depth transformers, which enables iterative computation over the same transformer layers. We investigate two compositional generalization challenges under the implicit reasoning scenario: systematic generalization, i.e. combining knowledge that is never used for compositions during training, and depth extrapolation, i.e. generalizing from limited reasoning depth (e.g. training on up to 5-hop) to deeper compositions (e.g. 10-hop). Through controlled studies with models trained from scratch, we show that while vanilla transformers struggle with both generalization challenges, recurrent-depth transformers can effectively make such generalization. For systematic generalization, we find that this ability emerges through a three-stage grokking process, transitioning from memorization to in-distribution generalization and finally to systematic generalization, supported by mechanistic analysis. For depth extrapolation, we show that generalization beyond training depth can be unlocked by scaling inference-time recurrence, with more iterations enabling deeper reasoning. We further study how training strategies affect extrapolation, providing guidance on training recurrent-depth transformers, and identify a key limitation, overthinking, where excessive recurrence degrades predictions and limits generalization to very deep compositions.

摘要：我們研究隱性推理，即在單次前向傳播中結合知識或規則的能力。雖然基於Transformer的大型語言模型儲存了大量的事實知識和規則，但它們在隱性多跳推理中常常無法組合這些知識，這表明它們的參數知識缺乏組合性泛化。為了解決這一限制，我們研究了遞迴深度Transformer，這使得在相同的Transformer層上進行迭代計算成為可能。我們在隱性推理場景下調查了兩個組合性泛化挑戰：系統性泛化，即結合在訓練期間從未用於組合的知識，以及深度外推，即從有限的推理深度（例如，訓練最多5跳）泛化到更深的組合（例如，10跳）。通過對從零開始訓練的模型進行控制研究，我們顯示出雖然普通Transformer在這兩個泛化挑戰中都面臨困難，但遞迴深度Transformer能夠有效地實現這種泛化。對於系統性泛化，我們發現這一能力通過三階段的理解過程出現，從記憶化過渡到分佈內泛化，最後到系統性泛化，並得到了機械分析的支持。對於深度外推，我們顯示超越訓練深度的泛化可以通過擴大推理時間的遞迴來解鎖，更多的迭代使得更深的推理成為可能。我們進一步研究了訓練策略如何影響外推，為訓練遞迴深度Transformer提供指導，並識別了一個關鍵限制，即過度思考，過度的遞迴會降低預測準確性並限制對非常深的組合的泛化。

##### **More Capable, Less Cooperative? When LLMs Fail At Zero-Cost Collaboration**
2604.07821v1 by Advait Yadav, Sid Black, Oliver Sourbut

Large language model (LLM) agents increasingly coordinate in multi-agent systems, yet we lack an understanding of where and why cooperation failures may arise. In many real-world coordination problems, from knowledge sharing in organizations to code documentation, helping others carries negligible personal cost while generating substantial collective benefits. However, whether LLM agents cooperate when helping neither benefits nor harms the helper, while being given explicit instructions to do so, remains unknown. We build a multi-agent setup designed to study cooperative behavior in a frictionless environment, removing all strategic complexity from cooperation. We find that capability does not predict cooperation: OpenAI o3 achieves only 17% of optimal collective performance while OpenAI o3-mini reaches 50%, despite identical instructions to maximize group revenue. Through a causal decomposition that automates one side of agent communication, we separate cooperation failures from competence failures, tracing their origins through agent reasoning analysis. Testing targeted interventions, we find that explicit protocols double performance for low-competence models, and tiny sharing incentives improve models with weak cooperation. Our findings suggest that scaling intelligence alone will not solve coordination problems in multi-agent systems and will require deliberate cooperative design, even when helping others costs nothing.

摘要：大型語言模型（LLM）代理在多代理系統中越來越多地協調，但我們仍然缺乏對合作失敗的發生地點和原因的理解。在許多現實世界的協調問題中，從組織中的知識共享到代碼文檔，幫助他人幾乎不會產生個人成本，同時產生可觀的集體利益。然而，當幫助既不會使幫助者受益也不會造成傷害，並且在明確指示下進行時，LLM 代理是否會合作仍然未知。我們建立了一個多代理設置，旨在研究無摩擦環境中的合作行為，消除了合作中的所有戰略複雜性。我們發現能力並不能預測合作：OpenAI o3 的最佳集體表現僅達到 17%，而 OpenAI o3-mini 則達到 50%，儘管指示相同，都是為了最大化集體收益。通過自動化代理通信一側的因果分解，我們將合作失敗與能力失敗分開，並通過代理推理分析追溯其起源。在測試針對性干預時，我們發現明確的協議使低能力模型的表現翻倍，而微小的共享激勵改善了合作較弱的模型。我們的研究結果表明，僅僅擴大智力並不能解決多代理系統中的協調問題，還需要有意的合作設計，即使幫助他人不會產生任何成本。

##### **Latent Anomaly Knowledge Excavation: Unveiling Sparse Sensitive Neurons in Vision-Language Models**
2604.07802v1 by Shaotian Li, Shangze Li, Chuancheng Shi, Wenhua Wu, Yanqiu Wu, Xiaohan Yu, Fei Shen, Tat-Seng Chua

Large-scale vision-language models (VLMs) exhibit remarkable zero-shot capabilities, yet the internal mechanisms driving their anomaly detection (AD) performance remain poorly understood. Current methods predominantly treat VLMs as black-box feature extractors, assuming that anomaly-specific knowledge must be acquired through external adapters or memory banks. In this paper, we challenge this assumption by arguing that anomaly knowledge is intrinsically embedded within pre-trained models but remains latent and under-activated. We hypothesize that this knowledge is concentrated within a sparse subset of anomaly-sensitive neurons. To validate this, we propose latent anomaly knowledge excavation (LAKE), a training-free framework that identifies and elicits these critical neuronal signals using only a minimal set of normal samples. By isolating these sensitive neurons, LAKE constructs a highly compact normality representation that integrates visual structural deviations with cross-modal semantic activations. Extensive experiments on industrial AD benchmarks demonstrate that LAKE achieves state-of-the-art performance while providing intrinsic, neuron-level interpretability. Ultimately, our work advocates for a paradigm shift: redefining anomaly detection as the targeted activation of latent pre-trained knowledge rather than the acquisition of a downstream task.

摘要：大型視覺-語言模型（VLMs）展現出卓越的零-shot 能力，但驅動其異常檢測（AD）性能的內部機制仍然不甚了解。當前的方法主要將 VLMs 視為黑箱特徵提取器，假設異常特定知識必須通過外部適配器或記憶庫來獲得。在本文中，我們挑戰這一假設，主張異常知識本質上嵌入於預訓練模型中，但仍然潛在且未被充分激活。我們假設這些知識集中在一小部分對異常敏感的神經元中。為了驗證這一點，我們提出潛在異常知識挖掘（LAKE），這是一個無需訓練的框架，僅使用一組最小的正常樣本來識別和引發這些關鍵神經信號。通過隔離這些敏感神經元，LAKE 構建了一個高度緊湊的正常性表示，將視覺結構偏差與跨模態語義激活整合在一起。在工業 AD 基準上的大量實驗表明，LAKE 實現了最先進的性能，同時提供內在的神經元級可解釋性。最終，我們的工作倡導一種範式轉變：將異常檢測重新定義為潛在預訓練知識的有針對性激活，而不是下游任務的獲取。

##### **Lightweight LLM Agent Memory with Small Language Models**
2604.07798v1 by Jiaquan Zhang, Chaoning Zhang, Shuxu Chen, Zhenzhen Huang, Pengcheng Zheng, Zhicheng Wang, Ping Guo, Fan Mo, Sung-Ho Bae, Jie Zou, Jiwei Wei, Yang Yang

Although LLM agents can leverage tools for complex tasks, they still need memory to maintain cross-turn consistency and accumulate reusable information in long-horizon interactions. However, retrieval-based external memory systems incur low online overhead but suffer from unstable accuracy due to limited query construction and candidate filtering. In contrast, many systems use repeated large-model calls for online memory operations, improving accuracy but accumulating latency over long interactions. We propose LightMem, a lightweight memory system for better agent memory driven by Small Language Models (SLMs). LightMem modularizes memory retrieval, writing, and long-term consolidation, and separates online processing from offline consolidation to enable efficient memory invocation under bounded compute. We organize memory into short-term memory (STM) for immediate conversational context, mid-term memory (MTM) for reusable interaction summaries, and long-term memory (LTM) for consolidated knowledge, and uses user identifiers to support independent retrieval and incremental maintenance in multi-user settings. Online, LightMem operates under a fixed retrieval budget and selects memories via a two-stage procedure: vector-based coarse retrieval followed by semantic consistency re-ranking. Offline, it abstracts reusable interaction evidence and incrementally integrates it into LTM. Experiments show gains across model scales, with an average F1 improvement of about 2.5 on LoCoMo, more effective and low median latency (83 ms retrieval; 581 ms end-to-end).

摘要：儘管 LLM 代理可以利用工具來處理複雜任務，但它們仍然需要記憶來維持跨回合的一致性並在長期互動中累積可重用的信息。<lb/>然而，基於檢索的外部記憶系統雖然在線開銷低，但由於查詢構建和候選過濾的限制，準確性不穩定。<lb/>相比之下，許多系統使用重複的大模型調用來進行在線記憶操作，雖然提高了準確性，但在長期互動中積累了延遲。<lb/>我們提出了 LightMem，一種輕量級的記憶系統，旨在通過小型語言模型（SLMs）來改善代理的記憶。<lb/>LightMem 將記憶檢索、寫入和長期整合模組化，並將在線處理與離線整合分開，以便在有限的計算資源下實現高效的記憶調用。<lb/>我們將記憶組織為短期記憶（STM）以應對即時對話上下文，中期記憶（MTM）以存儲可重用的互動摘要，和長期記憶（LTM）以整合知識，並使用用戶識別符來支持獨立檢索和在多用戶環境中的增量維護。<lb/>在線上，LightMem 在固定的檢索預算下運行，並通過兩階段程序選擇記憶：先進行基於向量的粗略檢索，然後進行語義一致性重新排名。<lb/>在線下，它抽象出可重用的互動證據，並將其增量整合到 LTM 中。<lb/>實驗顯示在模型規模上都有增益，LoCoMo 上的平均 F1 提升約為 2.5，並且具有更有效且較低的中位延遲（檢索 83 毫秒；端到端 581 毫秒）。

##### **SEARL: Joint Optimization of Policy and Tool Graph Memory for Self-Evolving Agents**
2604.07791v1 by Xinshun Feng, Xinhao Song, Lijun Li, Gongshen Liu, Jing Shao

Recent advances in Reinforcement Learning with Verifiable Rewards (RLVR) have demonstrated significant potential in single-turn reasoning tasks. With the paradigm shift toward self-evolving agentic learning, models are increasingly expected to learn from trajectories by synthesizing tools or accumulating explicit experiences. However, prevailing methods typically rely on large-scale LLMs or multi-agent frameworks, which hinder their deployment in resource-constrained environments. The inherent sparsity of outcome-based rewards also poses a substantial challenge, as agents typically receive feedback only upon completion of tasks. To address these limitations, we introduce a Tool-Memory based self-evolving agentic framework SEARL. Unlike approaches that directly utilize interaction experiences, our method constructs a structured experience memory that integrates planning with execution. This provides a novel state abstraction that facilitates generalization across analogous contexts, such as tool reuse. Consequently, agents extract explicit knowledge from historical data while leveraging inter-trajectory correlations to densify reward signals. We evaluate our framework on knowledge reasoning and mathematics tasks, demonstrating its effectiveness in achieving more practical and efficient learning.

摘要：最近在可驗證獎勵的強化學習（RLVR）方面的進展顯示出在單回合推理任務中具有顯著潛力。<lb/>隨著向自我演化的代理學習的範式轉變，模型越來越被期望通過合成工具或積累明確的經驗來從軌跡中學習。<lb/>然而，現有的方法通常依賴於大規模的LLM或多代理框架，這限制了它們在資源受限環境中的部署。<lb/>基於結果的獎勵的固有稀疏性也構成了實質性的挑戰，因為代理通常僅在任務完成後才會收到反饋。<lb/>為了解決這些限制，我們提出了一種基於工具記憶的自我演化代理框架SEARL。<lb/>與直接利用互動經驗的方法不同，我們的方法構建了一個結構化的經驗記憶，將規劃與執行整合在一起。<lb/>這提供了一種新穎的狀態抽象，有助於在類似上下文中進行泛化，例如工具重用。<lb/>因此，代理從歷史數據中提取明確的知識，同時利用軌跡間的相關性來密集獎勵信號。<lb/>我們在知識推理和數學任務上評估了我們的框架，證明其在實現更實用和高效的學習方面的有效性。

##### **PeReGrINE: Evaluating Personalized Review Fidelity with User Item Graph Context**
2604.07788v1 by Steven Au, Baihan Lin

We introduce PeReGrINE, a benchmark and evaluation framework for personalized review generation grounded in graph-structured user--item evidence. PeReGrINE restructures Amazon Reviews 2023 into a temporally consistent bipartite graph, where each target review is conditioned on bounded evidence from user history, item context, and neighborhood interactions under explicit temporal cutoffs. To represent persistent user preferences without conditioning directly on sparse raw histories, we compute a User Style Parameter that summarizes each user's linguistic and affective tendencies over prior reviews. This setup supports controlled comparison of four graph-derived retrieval settings: product-only, user-only, neighbor-only, and combined evidence. Beyond standard generation metrics, we introduce Dissonance Analysis, a macro-level evaluation framework that measures deviation from expected user style and product-level consensus. We also study visual evidence as an auxiliary context source and find that it can improve textual quality in some settings, while graph-derived evidence remains the main driver of personalization and consistency. Across product categories, PeReGrINE offers a reproducible way to study how evidence composition affects review fidelity, personalization, and grounding in retrieval-conditioned language models.

摘要：我們介紹 PeReGrINE，一個基於圖結構的用戶-項目證據的個性化評審生成基準和評估框架。PeReGrINE 將 Amazon Reviews 2023 重組為一個時間一致的二部圖，其中每個目標評審都是基於來自用戶歷史、項目上下文和在明確時間截止下的鄰域互動的有限證據。為了表示持久的用戶偏好，而不直接依賴稀疏的原始歷史，我們計算了一個用戶風格參數，總結每個用戶在先前評審中的語言和情感傾向。這個設置支持四種基於圖的檢索設置的受控比較：僅產品、僅用戶、僅鄰居和綜合證據。除了標準生成指標外，我們還引入了不和諧分析，一個宏觀層面的評估框架，用於測量與預期用戶風格和產品級共識的偏差。我們還研究了視覺證據作為輔助上下文來源，發現它在某些設置中可以改善文本質量，而基於圖的證據仍然是個性化和一致性的主要驅動因素。在各個產品類別中，PeReGrINE 提供了一種可重複的方法來研究證據組成如何影響評審的真實性、個性化和在檢索條件語言模型中的基礎。

##### **Automotive Engineering-Centric Agentic AI Workflow Framework**
2604.07784v1 by Tong Duy Son, Zhihao Liu, Piero Brigida, Yerlan Akhmetov, Gurudevan Devarajan, Kai Liu, Ajinkya Bhave

Engineering workflows such as design optimization, simulation-based diagnosis, control tuning, and model-based systems engineering (MBSE) are iterative, constraint-driven, and shaped by prior decisions. Yet many AI methods still treat these activities as isolated tasks rather than as parts of a broader workflow. This paper presents Agentic Engineering Intelligence (AEI), an industrial vision framework that models engineering workflows as constrained, history-aware sequential decision processes in which AI agents support engineer-supervised interventions over engineering toolchains. AEI links an offline phase for engineering data processing and workflow-memory construction with an online phase for workflow-state estimation, retrieval, and decision support. A control-theoretic interpretation is also possible, in which engineering objectives act as reference signals, agents act as workflow controllers, and toolchains provide feedback for intervention selection. Representative automotive use cases in suspension design, reinforcement learning tuning, multimodal engineering knowledge reuse, aerodynamic exploration, and MBSE show how diverse workflows can be expressed within a common formulation. Overall, the paper positions engineering AI as a problem of process-level intelligence and outlines a practical roadmap for future empirical validation in industrial settings.

摘要：工程工作流程，例如設計優化、基於模擬的診斷、控制調整和基於模型的系統工程（MBSE），都是迭代的、受限制的，並受到先前決策的影響。<lb/>然而，許多AI方法仍然將這些活動視為孤立的任務，而不是更廣泛工作流程的一部分。<lb/>本文提出了Agentic Engineering Intelligence (AEI)，這是一個工業視覺框架，將工程工作流程建模為受限的、具歷史感知的序列決策過程，其中AI代理支持工程師監督的干預，涵蓋工程工具鏈。<lb/>AEI將離線階段的工程數據處理和工作流程記憶構建與在線階段的工作流程狀態估計、檢索和決策支持相連接。<lb/>也可以進行控制理論的解釋，其中工程目標作為參考信號，代理作為工作流程控制器，工具鏈提供干預選擇的反饋。<lb/>在懸吊設計、強化學習調整、多模態工程知識重用、氣動探索和MBSE等代表性的汽車用例中，展示了如何在共同的公式中表達多樣的工作流程。<lb/>總體而言，本文將工程AI定位為一個過程層級的智能問題，並概述了在工業環境中未來實證驗證的實際路線圖。

##### **Toward Generalizable Graph Learning for 3D Engineering AI: Explainable Workflows for CAE Mode Shape Classification and CFD Field Prediction**
2604.07781v1 by Tong Duy Son, Kohta Sugiura, Marc Brughmans, Andrey Hense, Zhihao Liu, Amirthalakshmi Veeraraghavan, Ajinkya Bhave, Jay Masters, Paolo di Carlo, Theo Geluk

Automotive engineering development increasingly relies on heterogeneous 3D data, including finite element (FE) models, body-in-white (BiW) representations, CAD geometry, and CFD meshes. At the same time, engineering teams face growing pressure to shorten development cycles, improve performance and accelerate innovation. Although artificial intelligence (AI) is increasingly explored in this domain, many current methods remain task-specific, difficult to interpret, and hard to reuse across development stages. This paper presents a practical graph learning framework for 3D engineering AI, in which heterogeneous engineering assets are converted into physics-aware graph representations and processed by Graph Neural Networks (GNNs). The framework is designed to support both classification and prediction tasks. The framework is validated on two automotive applications: CAE vibration mode shape classification and CFD aerodynamic field prediction. For CAE vibration mode classification, a region-aware BiW graph supports explainable mode classification across vehicle and FE variants under label scarcity. For CFD aerodynamic field prediction, a physics-informed surrogate predicts pressure and wall shear stress (WSS) across aerodynamic body shape variants, while symmetry preserving down sampling retains accuracy with lower computational cost. The framework also outlines data generation guidance that can help engineers identify which additional simulations or labels are valuable to collect next. These results demonstrate a practical and reusable engineering AI workflow for more trustworthy CAE and CFD decision support.

摘要：汽車工程的發展越來越依賴於異質的 3D 數據，包括有限元素 (FE) 模型、白車身 (BiW) 表示、CAD 幾何形狀和 CFD 網格。<lb/>同時，工程團隊面臨著縮短開發周期、提高性能和加速創新的日益壓力。<lb/>儘管人工智慧 (AI) 在這一領域的探索越來越多，但許多當前的方法仍然是特定任務的、難以解釋的，並且在不同開發階段之間難以重用。<lb/>本文提出了一種實用的圖學習框架，用於 3D 工程 AI，其中異質工程資產被轉換為具有物理感知的圖表示，並由圖神經網絡 (GNNs) 處理。<lb/>該框架旨在支持分類和預測任務。<lb/>該框架在兩個汽車應用上進行了驗證：CAE 振動模式形狀分類和 CFD 氣動場預測。<lb/>對於 CAE 振動模式分類，一個區域感知的 BiW 圖支持在標籤稀缺的情況下進行可解釋的模式分類，涵蓋了車輛和 FE 變體。<lb/>對於 CFD 氣動場預測，一個物理知識驅動的替代模型預測了氣動體形狀變體的壓力和壁面剪切應力 (WSS)，同時保持對稱的下採樣在降低計算成本的同時保持準確性。<lb/>該框架還概述了數據生成指導，幫助工程師識別哪些額外的模擬或標籤是值得收集的。<lb/>這些結果展示了一個實用且可重用的工程 AI 工作流程，為更可靠的 CAE 和 CFD 決策支持提供了幫助。

##### **The Accountability Horizon: An Impossibility Theorem for Governing Human-Agent Collectives**
2604.07778v1 by Haileleol Tibebu

Existing accountability frameworks for AI systems, legal, ethical, and regulatory, rest on a shared assumption: for any consequential outcome, at least one identifiable person had enough involvement and foresight to bear meaningful responsibility. This paper proves that agentic AI systems violate this assumption not as an engineering limitation but as a mathematical necessity once autonomy exceeds a computable threshold. We introduce Human-Agent Collectives, a formalisation of joint human-AI systems where agents are modelled as state-policy tuples within a shared structural causal model. Autonomy is characterised through a four-dimensional information-theoretic profile (epistemic, executive, evaluative, social); collective behaviour through interaction graphs and joint action spaces. We axiomatise legitimate accountability through four minimal properties: Attributability (responsibility requires causal contribution), Foreseeability Bound (responsibility cannot exceed predictive capacity), Non-Vacuity (at least one agent bears non-trivial responsibility), and Completeness (all responsibility must be fully allocated). Our central result, the Accountability Incompleteness Theorem, proves that for any collective whose compound autonomy exceeds the Accountability Horizon and whose interaction graph contains a human-AI feedback cycle, no framework can satisfy all four properties simultaneously. The impossibility is structural: transparency, audits, and oversight cannot resolve it without reducing autonomy. Below the threshold, legitimate frameworks exist, establishing a sharp phase transition. Experiments on 3,000 synthetic collectives confirm all predictions with zero violations. This is the first impossibility result in AI governance, establishing a formal boundary below which current paradigms remain valid and above which distributed accountability mechanisms become necessary.

摘要：現有的 AI 系統問責框架，包括法律、倫理和監管，基於一個共同的假設：對於任何有重大後果的結果，至少有一個可識別的人足夠參與並具備前瞻性，承擔有意義的責任。本文證明，代理 AI 系統違反了這一假設，這不是工程上的限制，而是當自主性超過可計算的閾值時的數學必然性。我們介紹了人類-代理集體，這是一種聯合人類-AI 系統的形式化，其中代理被建模為共享結構因果模型中的狀態-策略元組。自主性通過四維信息理論特徵（認識、執行、評估、社會）來表徵；集體行為則通過互動圖和聯合行動空間來表徵。我們通過四個最小性質來公理化合法的問責性：可歸屬性（責任需要因果貢獻）、可預見性界限（責任不能超過預測能力）、非空性（至少一個代理承擔非平凡的責任）和完整性（所有責任必須完全分配）。我們的核心結果，即問責不完全性定理，證明了對於任何其複合自主性超過問責地平線且其互動圖包含人類-AI 反饋循環的集體，沒有任何框架能同時滿足所有四個性質。這一不可能性是結構性的：透明度、審計和監督無法解決這一問題，除非降低自主性。在閾值以下，存在合法的框架，建立了一個明確的相變。對 3,000 個合成集體的實驗確認了所有預測，且沒有違規。這是 AI 治理中的第一個不可能性結果，確立了一個正式的邊界，在此邊界以下當前的範式仍然有效，而在此邊界以上分散的問責機制變得必要。

##### **Beyond Surface Artifacts: Capturing Shared Latent Forgery Knowledge Across Modalities**
2604.07763v1 by Jingtong Dou, Chuancheng Shi, Jian Wang, Fei Shen, Zhiyong Wang, Tat-Seng Chua

As generative artificial intelligence evolves, deepfake attacks have escalated from single-modality manipulations to complex, multimodal threats. Existing forensic techniques face a severe generalization bottleneck: by relying excessively on superficial, modality-specific artifacts, they neglect the shared latent forgery knowledge hidden beneath variable physical appearances. Consequently, these models suffer catastrophic performance degradation when confronted with unseen "dark modalities." To break this limitation, this paper introduces a paradigm shift that redefines multimodal forensics from conventional "feature fusion" to "modality generalization." We propose the first modality-agnostic forgery (MAF) detection framework. By explicitly decoupling modality-specific styles, MAF precisely extracts the essential, cross-modal latent forgery knowledge. Furthermore, we define two progressive dimensions to quantify model generalization: transferability toward semantically correlated modalities (Weak MAF), and robustness against completely isolated signals of "dark modality" (Strong MAF). To rigorously assess these generalization limits, we introduce the DeepModal-Bench benchmark, which integrates diverse multimodal forgery detection algorithms and adapts state-of-the-art generalized learning methods. This study not only empirically proves the existence of universal forgery traces but also achieves significant performance breakthroughs on unknown modalities via the MAF framework, offering a pioneering technical pathway for universal multimodal defense.

摘要：隨著生成性人工智慧的演變，深偽攻擊已經從單一模態的操控升級為複雜的多模態威脅。現有的取證技術面臨嚴重的泛化瓶頸：由於過度依賴表面的、特定模態的工件，它們忽略了隱藏在變化的物理外觀下的共享潛在偽造知識。因此，這些模型在面對未見的「黑暗模態」時會遭受災難性的性能降級。為了突破這一限制，本文引入了一個範式轉變，將多模態取證從傳統的「特徵融合」重新定義為「模態泛化」。我們提出了首個模態無關的偽造（MAF）檢測框架。通過明確解耦模態特定的風格，MAF 精確提取了基本的跨模態潛在偽造知識。此外，我們定義了兩個進步的維度來量化模型的泛化能力：對語義相關模態的可轉移性（弱 MAF），以及對完全孤立的「黑暗模態」信號的魯棒性（強 MAF）。為了嚴格評估這些泛化極限，我們引入了 DeepModal-Bench 基準，該基準整合了多樣的多模態偽造檢測算法並適應最先進的泛化學習方法。本研究不僅實證證明了普遍偽造痕跡的存在，還通過 MAF 框架在未知模態上取得了顯著的性能突破，為普遍多模態防禦提供了一條開創性的技術途徑。

##### **Symbiotic-MoE: Unlocking the Synergy between Generation and Understanding**
2604.07753v1 by Xiangyue Liu, Zijian Zhang, Miles Yang, Zhao Zhong, Liefeng Bo, Ping Tan

Empowering Large Multimodal Models (LMMs) with image generation often leads to catastrophic forgetting in understanding tasks due to severe gradient conflicts. While existing paradigms like Mixture-of-Transformers (MoT) mitigate this conflict through structural isolation, they fundamentally sever cross-modal synergy and suffer from capacity fragmentation. In this work, we present Symbiotic-MoE, a unified pre-training framework that resolves task interference within a native multimodal Mixture-of-Experts (MoE) Transformers architecture with zero-parameter overhead. We first identify that standard MoE tuning leads to routing collapse, where generative gradients dominate expert utilization. To address this, we introduce Modality-Aware Expert Disentanglement, which partitions experts into task-specific groups while utilizing shared experts as a multimodal semantic bridge. Crucially, this design allows shared experts to absorb fine-grained visual semantics from generative tasks to enrich textual representations. To optimize this, we propose a Progressive Training Strategy featuring differential learning rates and early-stage gradient shielding. This mechanism not only shields pre-trained knowledge from early volatility but eventually transforms generative signals into constructive feedback for understanding. Extensive experiments demonstrate that Symbiotic-MoE achieves rapid generative convergence while unlocking cross-modal synergy, boosting inherent understanding with remarkable gains on MMLU and OCRBench.

摘要：賦能大型多模態模型（LMMs）進行圖像生成，往往會因為嚴重的梯度衝突而導致對理解任務的災難性遺忘。雖然現有的範式如混合Transformer（Mixture-of-Transformers, MoT）通過結構隔離來減輕這種衝突，但它們根本上切斷了跨模態的協同作用，並且遭受容量碎片化的問題。在這項工作中，我們提出了共生專家（Symbiotic-MoE），這是一個統一的預訓練框架，能在原生多模態混合專家（MoE）Transformer架構中以零參數開銷解決任務干擾。我們首先確定標準的MoE調整導致路由崩潰，生成梯度主導了專家的利用率。為了解決這個問題，我們引入了模態感知專家解耦，將專家劃分為任務特定的組，同時利用共享專家作為多模態語義橋樑。關鍵是，這種設計允許共享專家吸收來自生成任務的細緻視覺語義，以豐富文本表示。為了優化這一點，我們提出了一種漸進式訓練策略，特徵是差異學習率和早期階段的梯度屏蔽。這一機制不僅保護了預訓練知識免受早期波動的影響，最終還將生成信號轉化為理解的建設性反饋。廣泛的實驗表明，共生專家（Symbiotic-MoE）實現了快速的生成收斂，同時解鎖了跨模態的協同作用，顯著提升了內在理解，在MMLU和OCRBench上獲得了驚人的增益。

##### **Beyond Social Pressure: Benchmarking Epistemic Attack in Large Language Models**
2604.07749v1 by Steven Au, Sujit Noronha

Large language models (LLMs) can shift their answers under pressure in ways that reflect accommodation rather than reasoning. Prior work on sycophancy has focused mainly on disagreement, flattery, and preference alignment, leaving a broader set of epistemic failures less explored. We introduce \textbf{PPT-Bench}, a diagnostic benchmark for evaluating \textit{epistemic attack}, where prompts challenge the legitimacy of knowledge, values, or identity rather than simply opposing a previous answer. PPT-Bench is organized around the Philosophical Pressure Taxonomy (PPT), which defines four types of philosophical pressure: Epistemic Destabilization, Value Nullification, Authority Inversion, and Identity Dissolution. Each item is tested at three layers: a baseline prompt (L0), a single-turn pressure condition (L1), and a multi-turn Socratic escalation (L2). This allows us to measure epistemic inconsistency between L0 and L1, and conversational capitulation in L2. Across five models, these pressure types produce statistically separable inconsistency patterns, suggesting that epistemic attack exposes weaknesses not captured by standard social-pressure benchmarks. Mitigation results are strongly type- and model-dependent: prompt-level anchoring and persona-stability prompts perform best in API settings, while Leading Query Contrastive Decoding is the most reliable intervention for open models.

摘要：大型語言模型（LLMs）在壓力下可以以反映適應而非推理的方式改變其回答。先前對於諂媚的研究主要集中在分歧、恭維和偏好對齊上，留下了一組更廣泛的認識失誤未被深入探討。我們介紹了\textbf{PPT-Bench}，這是一個用於評估\textit{認識攻擊}的診斷基準，其中提示挑戰知識、價值或身份的合法性，而不僅僅是反對先前的回答。PPT-Bench圍繞哲學壓力分類法（PPT）組織，該分類法定義了四種類型的哲學壓力：認識不穩定、價值無效化、權威顛倒和身份溶解。每個項目在三個層次上進行測試：基線提示（L0）、單回合壓力條件（L1）和多回合蘇格拉底升級（L2）。這使我們能夠測量L0和L1之間的認識不一致性，以及L2中的對話屈服。在五個模型中，這些壓力類型產生了統計上可分離的不一致性模式，表明認識攻擊暴露了標準社會壓力基準未捕捉到的弱點。減輕結果強烈依賴於類型和模型：提示級別的錨定和角色穩定提示在API設置中表現最佳，而引導查詢對比解碼是開放模型中最可靠的干預措施。

##### **Towards Knowledgeable Deep Research: Framework and Benchmark**
2604.07720v2 by Wenxuan Liu, Zixuan Li, Long Bai, Chunmao Zhang, Fenghui Zhang, Zhuo Chen, Wei Li, Yuxin Zuo, Fei Wang, Bingbing Xu, Xuhui Jiang, Jin Zhang, Xiaolong Jin, Jiafeng Guo, Tat-Seng Chua, Xueqi Cheng

Deep Research (DR) requires LLM agents to autonomously perform multi-step information seeking, processing, and reasoning to generate comprehensive reports. In contrast to existing studies that mainly focus on unstructured web content, a more challenging DR task should additionally utilize structured knowledge to provide a solid data foundation, facilitate quantitative computation, and lead to in-depth analyses. In this paper, we refer to this novel task as Knowledgeable Deep Research (KDR), which requires DR agents to generate reports with both structured and unstructured knowledge. Furthermore, we propose the Hybrid Knowledge Analysis framework (HKA), a multi-agent architecture that reasons over both kinds of knowledge and integrates the texts, figures, and tables into coherent multimodal reports. The key design is the Structured Knowledge Analyzer, which utilizes both coding and vision-language models to produce figures, tables, and corresponding insights. To support systematic evaluation, we construct KDR-Bench, which covers 9 domains, includes 41 expert-level questions, and incorporates a large number of structured knowledge resources (e.g., 1,252 tables). We further annotate the main conclusions and key points for each question and propose three categories of evaluation metrics including general-purpose, knowledge-centric, and vision-enhanced ones. Experimental results demonstrate that HKA consistently outperforms most existing DR agents on general-purpose and knowledge-centric metrics, and even surpasses the Gemini DR agent on vision-enhanced metrics, highlighting its effectiveness in deep, structure-aware knowledge analysis. Finally, we hope this work can serve as a new foundation for structured knowledge analysis in DR agents and facilitate future multimodal DR studies.

摘要：深度研究 (DR) 需要大型語言模型 (LLM) 代理人自主執行多步驟的信息尋求、處理和推理，以生成綜合報告。與現有研究主要集中於非結構化網絡內容不同，更具挑戰性的 DR 任務還應利用結構化知識，以提供堅實的數據基礎，促進定量計算，並導致深入分析。在本文中，我們將這一新任務稱為知識深度研究 (KDR)，該任務要求 DR 代理人生成包含結構化和非結構化知識的報告。此外，我們提出了混合知識分析框架 (HKA)，這是一種多代理架構，能夠對這兩種類型的知識進行推理，並將文本、圖形和表格整合成連貫的多模態報告。關鍵設計是結構化知識分析器，該分析器利用編碼和視覺-語言模型生成圖形、表格及相應的見解。為了支持系統評估，我們構建了 KDR-Bench，涵蓋 9 個領域，包含 41 個專家級問題，並整合大量結構化知識資源（例如，1,252 個表格）。我們進一步為每個問題註解主要結論和關鍵要點，並提出三類評估指標，包括通用型、知識中心型和增強視覺型的指標。實驗結果表明，HKA 在通用型和知識中心型指標上始終優於大多數現有的 DR 代理人，甚至在增強視覺型指標上超越了 Gemini DR 代理人，突顯了其在深度、結構意識知識分析中的有效性。最後，我們希望這項工作能為 DR 代理人的結構化知識分析提供新的基礎，並促進未來的多模態 DR 研究。

##### **IatroBench: Pre-Registered Evidence of Iatrogenic Harm from AI Safety Measures**
2604.07709v1 by David Gringras

Ask a frontier model how to taper six milligrams of alprazolam (psychiatrist retired, ten days of pills left, abrupt cessation causes seizures) and it tells her to call the psychiatrist she just explained does not exist. Change one word ("I'm a psychiatrist; a patient presents with...") and the same model, same weights, same inference pass produces a textbook Ashton Manual taper with diazepam equivalence, anticonvulsant coverage, and monitoring thresholds. The knowledge was there; the model withheld it. IatroBench measures this gap. Sixty pre-registered clinical scenarios, six frontier models, 3,600 responses, scored on two axes (commission harm, CH 0-3; omission harm, OH 0-4) through a structured-evaluation pipeline validated against physician scoring (kappa_w = 0.571, within-1 agreement 96%). The central finding is identity-contingent withholding: match the same clinical question in physician vs. layperson framing and all five testable models provide better guidance to the physician (decoupling gap +0.38, p = 0.003; binary hit rates on safety-colliding actions drop 13.1 percentage points in layperson framing, p < 0.0001, while non-colliding actions show no change). The gap is widest for the model with the heaviest safety investment (Opus, +0.65). Three failure modes separate cleanly: trained withholding (Opus), incompetence (Llama 4), and indiscriminate content filtering (GPT-5.2, whose post-generation filter strips physician responses at 9x the layperson rate because they contain denser pharmacological tokens). The standard LLM judge assigns OH = 0 to 73% of responses a physician scores OH >= 1 (kappa = 0.045); the evaluation apparatus has the same blind spot as the training apparatus. Every scenario targets someone who has already exhausted the standard referrals.

摘要：詢問一個前沿模型如何減少六毫克的阿普唑仑（精神科醫生退休，剩下十天的藥，突然停止會導致癲癇發作），它告訴她打電話給那位她剛剛解釋過不存在的精神科醫生。只需更改一個詞（「我是一名精神科醫生；一名患者出現...」），同一模型、相同權重、相同推理過程就會產生一本教科書式的阿什頓手冊減藥方案，並提供地西泮的等效性、抗癲癇藥物覆蓋和監測閾值。知識存在；模型卻隱瞞了它。IatroBench 衡量這一差距。六十個預註冊的臨床場景，六個前沿模型，3,600 個回應，根據兩個軸線進行評分（委託傷害，CH 0-3；遺漏傷害，OH 0-4），通過一個結構化評估流程驗證與醫生評分（kappa_w = 0.571，1內一致性96%）。主要發現是身份依賴的隱瞞：在醫生與外行人框架中匹配相同的臨床問題，所有五個可測試模型都為醫生提供了更好的指導（解耦差距 +0.38，p = 0.003；在外行人框架中，安全衝突行動的二元命中率下降了13.1個百分點，p < 0.0001，而非衝突行動則沒有變化）。對於安全投資最重的模型（Opus，+0.65），差距最寬。三種失敗模式清晰分開：訓練隱瞞（Opus）、無能（Llama 4）和不加區別的內容過濾（GPT-5.2，其生成後過濾器以9倍於外行人的速度剝離醫生的回應，因為它們包含更密集的藥理學標記）。標準 LLM 評判者對73%的回應賦予 OH = 0，而這些回應的醫生評分 OH >= 1（kappa = 0.045）；評估裝置與訓練裝置有相同的盲點。每個場景的目標都是已經耗盡標準轉介的人。

##### **Efficient and Effective Internal Memory Retrieval for LLM-Based Healthcare Prediction**
2604.07659v1 by Mingchen Li, Jiatan Huang, Zonghai Yao, Hong yu

Large language models (LLMs) hold significant promise for healthcare, yet their reliability in high-stakes clinical settings is often compromised by hallucinations and a lack of granular medical context. While Retrieval Augmented Generation (RAG) can mitigate these issues, standard supervised pipelines require computationally intensive searches over massive external knowledge bases, leading to high latency that is impractical for time-sensitive care. To address this, we introduce Keys to Knowledge (K2K), a novel framework that replaces external retrieval with internal, key-based knowledge access. By encoding essential clinical information directly into the model's parameter space, K2K enables rapid retrieval from internal key-value memory without inference-time overhead. We further enhance retrieval quality through activation-guided probe construction and cross-attention reranking. Experimental results demonstrate that K2K achieves state-of-the-art performance across four benchmark healthcare outcome prediction datasets.

摘要：大型語言模型（LLMs）在醫療保健領域具有重要潛力，但它們在高風險臨床環境中的可靠性常常受到幻覺和缺乏細緻醫學背景的影響。雖然檢索增強生成（RAG）可以緩解這些問題，但標準的監督管道需要對龐大的外部知識庫進行計算密集型搜索，導致高延遲，這對於時間敏感的護理來說是不切實際的。為了解決這個問題，我們引入了知識鑰匙（K2K），這是一個新穎的框架，將外部檢索替換為基於內部鍵的知識訪問。通過將基本臨床信息直接編碼到模型的參數空間中，K2K 能夠在不增加推理時間開銷的情況下，快速從內部鍵值記憶中檢索。我們進一步通過激活引導探針構建和交叉注意力重排序來提高檢索質量。實驗結果表明，K2K 在四個基準醫療結果預測數據集上達到了最先進的性能。

##### **ADAG: Automatically Describing Attribution Graphs**
2604.07615v1 by Aryaman Arora, Zhengxuan Wu, Jacob Steinhardt, Sarah Schwettmann

In language model interpretability research, \textbf{circuit tracing} aims to identify which internal features causally contributed to a particular output and how they affected each other, with the goal of explaining the computations underlying some behaviour. However, all prior circuit tracing work has relied on ad-hoc human interpretation of the role that each feature in the circuit plays, via manual inspection of data artifacts such as the dataset examples the component activates on. We introduce \textbf{ADAG}, an end-to-end pipeline for describing these attribution graphs which is fully automated. To achieve this, we introduce \textit{attribution profiles} which quantify the functional role of a feature via its input and output gradient effects. We then introduce a novel clustering algorithm for grouping features, and an LLM explainer--simulator setup which generates and scores natural-language explanations of the functional role of these feature groups. We run our system on known human-analysed circuit-tracing tasks and recover interpretable circuits, and further show ADAG can find steerable clusters which are responsible for a harmful advice jailbreak in Llama 3.1 8B Instruct.

摘要：在語言模型可解釋性研究中，\textbf{電路追蹤}旨在識別哪些內部特徵對特定輸出有因果貢獻，以及它們如何相互影響，目的是解釋某些行為背後的計算。然而，所有先前的電路追蹤工作都依賴於對電路中每個特徵角色的臨時人為解釋，通過手動檢查數據工件，例如該組件在其上激活的數據集示例。我們介紹了\textbf{ADAG}，這是一個完全自動化的端到端管道，用於描述這些歸因圖。為了實現這一點，我們引入了\textit{歸因配置文件}，它通過特徵的輸入和輸出梯度效應來量化特徵的功能角色。然後，我們介紹了一種新穎的聚類算法來分組特徵，以及一個LLM解釋器-模擬器設置，該設置生成並評分這些特徵組的功能角色的自然語言解釋。我們在已知的人類分析電路追蹤任務上運行我們的系統，並恢復可解釋的電路，進一步顯示ADAG可以找到負責Llama 3.1 8B Instruct中有害建議越獄的可操控集群。

##### **Reasoning Graphs: Deterministic Agent Accuracy through Evidence-Centric Chain-of-Thought Feedback**
2604.07595v1 by Matthew Penaroza

Language model agents reason from scratch on every query: each time an agent retrieves evidence and deliberates, the chain of thought is discarded and the next similar query starts with no prior insight. This produces lower accuracy and high variance, as the same type of query can succeed or fail unpredictably. We introduce reasoning graphs, a graph structure that persists an agent's per-evidence chain of thought as structured edges connected to the evidence items they evaluate. Unlike prior memory mechanisms that store distilled strategies as flat records indexed by query similarity or appended by recency, reasoning graphs enable evidence-centric feedback: given a new candidate set, the system traverses all incoming evaluation edges for each evidence item across all prior runs, surfacing how that specific item has been judged before. This backward traversal from evidence inward is a structurally different capability from query-similarity retrieval, because the feedback is tied to the specific evidence the agent is currently examining, not to the query. We further introduce retrieval graphs, a complementary structure that feeds a pipeline planner to tighten the candidate funnel over successive runs. Together, both graphs form a self-improving feedback loop: accuracy rises and variance collapses over successive runs, with every decision fully traceable through the graph. This improvement requires no retraining; the base model remains frozen and all gains come from context engineering via graph traversal. We formalize the graph structure, traversal algorithms, and feedback mechanisms, and describe a sequential cluster evaluation protocol for measuring accuracy convergence and variance collapse on multi-hop question answering benchmarks.

摘要：語言模型代理從每個查詢開始時都從零推理：每當代理檢索證據並進行深思時，思維鏈將被丟棄，下一個類似的查詢將在沒有先前見解的情況下開始。這會產生較低的準確性和高變異性，因為同類型的查詢可能會不可預測地成功或失敗。我們引入了推理圖，一種結構化的圖形結構，持續記錄代理對每個證據的思維鏈，作為與其評估的證據項目相連的結構化邊。與先前的記憶機制不同，後者將提煉的策略存儲為按查詢相似性索引的平面記錄或按最近性附加的記錄，推理圖使以證據為中心的反饋成為可能：在給定一組新的候選項時，系統會遍歷所有先前運行中每個證據項目的所有進入評估邊，顯示該特定項目之前是如何被評判的。這種從證據向內的反向遍歷是一種結構上不同於查詢相似性檢索的能力，因為反饋是與代理當前正在檢查的具體證據相關，而不是查詢。我們進一步引入檢索圖，一種補充結構，為管道規劃器提供數據，以在連續運行中縮緊候選篩選。這兩種圖形共同形成一個自我改善的反饋循環：準確性隨著連續運行而上升，變異性則隨之降低，每個決策都可以通過圖形完全追溯。這一改進不需要重新訓練；基礎模型保持不變，所有的增益來自於通過圖形遍歷進行的上下文工程。我們對圖形結構、遍歷算法和反饋機制進行了形式化，並描述了一個順序集群評估協議，用於測量多跳問題回答基準上的準確性收斂和變異性崩潰。

##### **DCD: Domain-Oriented Design for Controlled Retrieval-Augmented Generation**
2604.07590v1 by Valeriy Kovalskiy, Nikita Belov, Nikita Miteyko, Igor Reshetnikov, Max Maximov

Retrieval-Augmented Generation (RAG) is widely used to ground large language models in external knowledge sources. However, when applied to heterogeneous corpora and multi-step queries, Naive RAG pipelines often degrade in quality due to flat knowledge representations and the absence of explicit workflows. In this work, we introduce DCD (Domain-Collection-Document), a domain-oriented design to structure knowledge and control query processing in RAG systems without modifying the underlying language model. The proposed approach relies on a hierarchical decomposition of the information space and multi-stage routing based on structured model outputs, enabling progressive restriction of both retrieval and generation scopes. The architecture is complemented by smart chunking, hybrid retrieval, and integrated validation and generation guardrail mechanisms. We describe the DCD architecture and workflow and discuss evaluation results on synthetic evaluation dataset, highlighting their impact on robustness, factual accuracy, and answer relevance in applied RAG scenarios.

摘要：檢索增強生成（RAG）廣泛用於將大型語言模型與外部知識來源相結合。<lb/>然而，當應用於異質語料庫和多步查詢時，簡單的 RAG 管道往往因為平坦的知識表示和缺乏明確的工作流程而質量下降。<lb/>在這項工作中，我們介紹了 DCD（領域-集合-文檔），這是一種面向領域的設計，用於結構化知識並控制 RAG 系統中的查詢處理，而無需修改底層的語言模型。<lb/>所提出的方法依賴於信息空間的分層分解和基於結構化模型輸出的多階段路由，從而能夠逐步限制檢索和生成的範圍。<lb/>該架構還配備了智能切塊、混合檢索以及集成的驗證和生成防護機制。<lb/>我們描述了 DCD 架構和工作流程，並討論了在合成評估數據集上的評估結果，突顯了它們在應用 RAG 情境中的穩健性、事實準確性和答案相關性影響。

##### **CLEAR: Context Augmentation from Contrastive Learning of Experience via Agentic Reflection**
2604.07487v1 by Linbo Liu, Guande Wu, Han Ding, Yawei Wang, Qiang Zhou, Yuzhe Lu, Zhichao Xu, Huan Song, Panpan Xu, Lin Lee Cheong

Large language model agents rely on effective model context to obtain task-relevant information for decision-making. Many existing context engineering approaches primarily rely on the context generated from the past experience and retrieval mechanisms that reuse these context. However, retrieved context from past tasks must be adapted by the execution agent to fit new situations, placing additional reasoning burden on the underlying LLM. To address this limitation, we propose a generative context augmentation framework using Contrastive Learning of Experience via Agentic Reflection (CLEAR). CLEAR first employs a reflection agent to perform contrastive analysis over past execution trajectories and summarize useful context for each observed task. These summaries are then used as supervised fine-tuning data to train a context augmentation model (CAM). Then we further optimize CAM using reinforcement learning, where the reward signal is obtained by running the task execution agent. By learning to generate task-specific knowledge rather than retrieve knowledge from the past, CAM produces context that is better tailored to the current task. We conduct comprehensive evaluations on the AppWorld and WebShop benchmarks. Experimental results show that CLEAR consistently outperforms strong baselines. It improves task completion rate from 72.62% to 81.15% on AppWorld test set and averaged reward from 0.68 to 0.74 on a subset of WebShop, compared with baseline agent. Our code is publicly available at https://github.com/awslabs/CLEAR.

摘要：大型語言模型代理依賴有效的模型上下文來獲取與任務相關的信息以進行決策。許多現有的上下文工程方法主要依賴於從過去經驗生成的上下文以及重用這些上下文的檢索機制。然而，從過去任務中檢索的上下文必須由執行代理進行調整，以適應新情況，這對底層的LLM增加了額外的推理負擔。為了解決這一限制，我們提出了一種使用經驗對比學習的生成上下文增強框架，稱為CLEAR。CLEAR首先使用反思代理對過去的執行軌跡進行對比分析，並為每個觀察到的任務總結有用的上下文。這些摘要然後用作監督微調數據，以訓練上下文增強模型（CAM）。接著，我們進一步使用強化學習來優化CAM，其中獎勵信號是通過運行任務執行代理獲得的。通過學習生成特定任務的知識，而不是從過去檢索知識，CAM產生的上下文更適合當前任務。我們在AppWorld和WebShop基準上進行了全面評估。實驗結果顯示，CLEAR始終超越強基準。它在AppWorld測試集上將任務完成率從72.62%提高到81.15%，在WebShop的子集上將平均獎勵從0.68提高到0.74，相較於基準代理。我們的代碼可在https://github.com/awslabs/CLEAR公開獲得。

##### **M-ArtAgent: Evidence-Based Multimodal Agent for Implicit Art Influence Discovery**
2604.07468v1 by Hanyi Liu, Zhonghao Jiu, Minghao Wang, Yuhang Xie, Heran Yang

Implicit artistic influence, although visually plausible, is often undocumented and thus poses a historically constrained attribution problem: resemblance is necessary but not sufficient evidence. Most prior systems reduce influence discovery to embedding similarity or label-driven graph completion, while recent multimodal large language models (LLMs) remain vulnerable to temporal inconsistency and unverified attributions. This paper introduces M-ArtAgent, an evidence-based multimodal agent that reframes implicit influence discovery as probabilistic adjudication. It follows a four-phase protocol consisting of Investigation, Corroboration, Falsification, and Verdict governed by a Reasoning and Acting (ReAct)-style controller that assembles verifiable evidence chains from images and biographies, enforces art-historical axioms, and subjects each hypothesis to adversarial falsification via a prompt-isolated critic. Two theory-grounded operators, StyleComparator for Wolfflin formal analysis and ConceptRetriever for ICONCLASS-based iconographic grounding, ensure that intermediate claims are formally auditable. On the balanced WikiArt Influence Benchmark-100 (WIB-100) of 100 artists and 2,000 directed pairs, M-ArtAgent achieves 83.7% positive-class F1, 0.666 Matthews correlation coefficient (MCC), and 0.910 area under the receiver operating characteristic curve (ROC-AUC), with leakage-control and robustness checks confirming that the gains persist when explicit influence phrases are masked. By coupling multimodal perception with domain-constrained falsification, M-ArtAgent demonstrates that implicit influence analysis benefits from historically grounded adjudication rather than pattern matching alone.

摘要：隱含的藝術影響，儘管在視覺上似乎合理，卻常常缺乏文獻記載，因此造成了一個歷史上受限的歸因問題：相似性是必要但不足的證據。大多數先前的系統將影響發現簡化為嵌入相似性或標籤驅動的圖形補全，而最近的多模態大型語言模型（LLMs）仍然容易受到時間不一致性和未經驗證的歸因影響。本文介紹了M-ArtAgent，一個基於證據的多模態代理，將隱含影響的發現重新框架為概率裁決。它遵循一個由調查、證實、反駁和裁決四個階段組成的協議，受一個推理與行動（ReAct）風格的控制器管理，該控制器從圖像和傳記中組裝可驗證的證據鏈，強制執行藝術史公理，並通過一個獨立的批評者對每個假設進行對抗性反駁。兩個基於理論的運算子，StyleComparator用於Wolfflin形式分析，ConceptRetriever用於基於ICONCLASS的圖像學基礎，確保中間聲明在形式上可審計。在平衡的WikiArt影響基準-100（WIB-100）中，包括100位藝術家和2,000對指向性配對，M-ArtAgent達成了83.7%的正類F1、0.666的馬修斯相關係數（MCC）和0.910的接收者操作特徵曲線下面積（ROC-AUC），並且泄漏控制和穩健性檢查確認當顯性影響短語被遮蔽時，增益仍然持續。通過將多模態感知與領域約束的反駁相結合，M-ArtAgent顯示出隱含影響分析受益於歷史上基礎的裁決，而不僅僅是模式匹配。

##### **Cross-Tokenizer LLM Distillation through a Byte-Level Interface**
2604.07466v1 by Avyav Kumar Singh, Yen-Chen Wu, Alexandru Cioba, Alberto Bernacchia, Davide Buffelli

Cross-tokenizer distillation (CTD), the transfer of knowledge from a teacher to a student language model when the two use different tokenizers, remains a largely unsolved problem. Existing approaches rely on heuristic strategies to align mismatched vocabularies, introducing considerable complexity. In this paper, we propose a simple but effective baseline called Byte-Level Distillation (BLD) which enables CTD by operating at a common interface across tokenizers: the byte level. In more detail, we convert the teacher's output distribution to byte-level probabilities, attach a lightweight byte-level decoder head to the student, and distill through this shared byte-level interface. Despite its simplicity, BLD performs competitively with--and on several benchmarks surpasses--significantly more sophisticated CTD methods, across a range of distillation tasks with models from 1B to 8B parameters. Our results suggest that the byte level is a natural common ground for cross-tokenizer knowledge transfer, while also highlighting that consistent improvements across all tasks and benchmarks remain elusive, underscoring that CTD is still an open problem.

摘要：跨標記器蒸餾（CTD），即當教師和學生語言模型使用不同的標記器時，從教師到學生的知識轉移，仍然是一個大多數未解決的問題。現有的方法依賴於啟發式策略來對齊不匹配的詞彙，這引入了相當大的複雜性。在本文中，我們提出了一個簡單但有效的基線，稱為字節級蒸餾（BLD），它通過在標記器之間的共同介面——字節級別上運作來實現CTD。更詳細地說，我們將教師的輸出分佈轉換為字節級概率，將一個輕量級的字節級解碼器頭附加到學生上，並通過這個共享的字節級介面進行蒸餾。儘管其簡單性，BLD在多個基準測試中表現出與顯著更複雜的CTD方法相當的競爭力，並在多個基準上超越它們，涵蓋了從1B到8B參數的多種蒸餾任務。我們的結果表明，字節級別是跨標記器知識轉移的自然共同基礎，同時也強調在所有任務和基準上持續改進仍然難以實現，凸顯出CTD仍然是一個未解決的問題。

##### **Chatbot-Based Assessment of Code Understanding in Automated Programming Assessment Systems**
2604.07304v1 by Eduard Frankford, Erik Cikalleshi, Ruth Breu

Large Language Models (LLMs) challenge conventional automated programming assessment because students can now produce functionally correct code without demonstrating corresponding understanding. This paper makes two contributions. First, it reports a saturation-based scoping review of conversational assessment approaches in programming education. The review identifies three dominant architectural families: rule-based or template-driven systems, LLM-based systems, and hybrid systems. Across the literature, conversational agents appear promising for scalable feedback and deeper probing of code understanding, but important limitations remain around hallucinations, over-reliance, privacy, integrity, and deployment constraints. Second, the paper synthesizes these findings into a Hybrid Socratic Framework for integrating conversational verification into Automated Programming Assessment Systems (APASs). The framework combines deterministic code analysis with a dual-agent conversational layer, knowledge tracking, scaffolded questioning, and guardrails that tie prompts to runtime facts. The paper also discusses practical safeguards against LLM-generated explanations, including proctored deployment modes, randomized trace questions, stepwise reasoning tied to concrete execution states, and local-model deployment options for privacy-sensitive settings. Rather than replacing conventional testing, the framework is intended as a complementary layer for verifying whether students understand the code they submit.

摘要：大型語言模型（LLMs）挑戰傳統的自動程式評估，因為學生現在可以產生功能上正確的程式碼，而不需要展示相應的理解。這篇論文有兩個貢獻。首先，它報告了一項基於飽和度的對話評估方法在程式教育中的範疇回顧。該回顧識別了三個主要的架構類別：基於規則或模板驅動的系統、基於LLM的系統和混合系統。在文獻中，對話代理在可擴展的反饋和深入探究程式碼理解方面顯示出良好的前景，但在幻覺、過度依賴、隱私、完整性和部署限制等方面仍然存在重要的限制。其次，這篇論文將這些發現綜合成一個混合蘇格拉底框架，用於將對話驗證整合到自動程式評估系統（APASs）中。該框架結合了確定性程式碼分析與雙代理對話層、知識追蹤、階梯式提問以及將提示與運行時事實聯繫起來的護欄。論文還討論了針對LLM生成解釋的實際保障措施，包括監考部署模式、隨機追蹤問題、與具體執行狀態相關的逐步推理，以及針對隱私敏感環境的本地模型部署選項。該框架並非取代傳統測試，而是作為一個補充層，用於驗證學生是否理解他們提交的程式碼。

##### **Region-Graph Optimal Transport Routing for Mixture-of-Experts Whole-Slide Image Classification**
2604.07298v1 by Xin Tian, Jiuliu Lu, Ephraim Tsalik, Bart Wanders, Colleen Knoth, Julian Knight

Multiple Instance Learning (MIL) is the dominant framework for gigapixel whole-slide image (WSI) classification in computational pathology. However, current MIL aggregators route all instances through a shared pathway, constraining their capacity to specialise across the pathological heterogeneity inherent in each slide. Mixture-of-Experts (MoE) methods offer a natural remedy by partitioning instances across specialised expert subnetworks; yet unconstrained softmax routing may yield highly imbalanced utilisation, where one or a few experts absorb most routing mass, collapsing the mixture back to a near-single-pathway solution. To address these limitations, we propose ROAM (Region-graph OptimAl-transport Mixture-of-experts), a spatially aware MoE-MIL aggregator that routes region tokens to expert poolers via capacity-constrained entropic optimal transport, promoting balanced expert utilisation by construction. ROAM operates on spatial region tokens, obtained by compressing dense patch bags into spatially binned units that align routing with local tissue neighbourhoods and introduces two key mechanisms: (i) region-to-expert assignment formulated as entropic optimal transport (Sinkhorn) with explicit per slide capacity marginals, enforcing balanced expert utilisation without auxiliary load-balancing losses; and (ii) graph-regularised Sinkhorn iterations that diffuse routing assignments over the spatial region graph, encouraging neighbouring regions to coherently route to the same experts. Evaluated on four WSI benchmarks with frozen foundation-model patch embeddings, ROAM achieves performance competitive against strong MIL and MoE baselines, and on NSCLC generalisation (TCGA-CPTAC) reaches external AUC 0.845 +- 0.019.

摘要：多實例學習（MIL）是計算病理學中用於千兆像素整張幻燈片圖像（WSI）分類的主導框架。<lb/>然而，當前的MIL聚合器將所有實例通過共享路徑進行路由，限制了它們在每張幻燈片固有的病理異質性中專業化的能力。<lb/>專家混合（MoE）方法通過將實例劃分到專門的專家子網絡中提供了一種自然的解決方案；然而，無約束的softmax路由可能導致高度不平衡的利用，其中一個或幾個專家吸收了大部分路由質量，將混合重新壓縮回接近單一路徑的解決方案。<lb/>為了解決這些限制，我們提出了ROAM（區域圖優化運輸專家混合），這是一種空間感知的MoE-MIL聚合器，通過容量受限的熵最優運輸將區域標記路由到專家池，從而促進平衡的專家利用。<lb/>ROAM在空間區域標記上運作，這些標記是通過將密集的補丁包壓縮成與當地組織鄰域對齊的空間分箱單位來獲得的，並引入了兩個關鍵機制：（i）區域到專家的分配，形式化為具有明確每張幻燈片容量邊際的熵最優運輸（Sinkhorn），在不需要輔助負載平衡損失的情況下強制實現平衡的專家利用；以及（ii）圖正則化的Sinkhorn迭代，將路由分配擴散到空間區域圖上，鼓勵相鄰區域一致地路由到相同的專家。<lb/>在四個WSI基準上進行評估，使用凍結的基礎模型補丁嵌入，ROAM的性能與強大的MIL和MoE基準相當，並且在NSCLC泛化（TCGA-CPTAC）中達到外部AUC 0.845 +- 0.019。

##### **A Systematic Study of Retrieval Pipeline Design for Retrieval-Augmented Medical Question Answering**
2604.07274v1 by Nusrat Sultana, Abdullah Muhammad Moosa, Kazi Afzalur Rahman, Sajal Chandra Banik

Large language models (LLMs) have demonstrated strong capabilities in medical question answering; however, purely parametric models often suffer from knowledge gaps and limited factual grounding. Retrieval-augmented generation (RAG) addresses this limitation by integrating external knowledge retrieval into the reasoning process. Despite increasing interest in RAG-based medical systems, the impact of individual retrieval components on performance remains insufficiently understood. This study presents a systematic evaluation of retrieval-augmented medical question answering using the MedQA USMLE benchmark and a structured textbook-based knowledge corpus. We analyze the interaction between language models, embedding models, retrieval strategies, query reformulation, and cross-encoder reranking within a unified experimental framework comprising forty configurations. Results show that retrieval augmentation significantly improves zero-shot medical question answering performance. The best-performing configuration was dense retrieval with query reformulation and reranking achieved 60.49% accuracy. Domain-specialized language models were also found to better utilize retrieved medical evidence than general-purpose models. The analysis further reveals a clear tradeoff between retrieval effectiveness and computational cost, with simpler dense retrieval configurations providing strong performance while maintaining higher throughput. All experiments were conducted on a single consumer-grade GPU, demonstrating that systematic evaluation of retrieval-augmented medical QA systems can be performed under modest computational resources.

摘要：大型語言模型（LLMs）在醫療問題回答方面展現了強大的能力；然而，純粹的參數模型往往存在知識空白和有限的事實基礎。檢索增強生成（RAG）通過將外部知識檢索整合到推理過程中來解決這一限制。儘管對基於RAG的醫療系統的興趣日益增加，但各個檢索組件對性能的影響仍然了解不足。本研究對使用MedQA USMLE基準和結構化教科書知識語料庫的檢索增強醫療問題回答進行了系統評估。我們分析了語言模型、嵌入模型、檢索策略、查詢重構和交叉編碼重排序之間的互動，並在一個包含四十種配置的統一實驗框架中進行。結果顯示，檢索增強顯著提高了零樣本醫療問題回答的性能。表現最佳的配置是使用查詢重構和重排序的稠密檢索，達到60.49%的準確率。專業領域的語言模型在利用檢索到的醫療證據方面也被發現比通用模型更有效。分析進一步揭示了檢索效果與計算成本之間的明顯權衡，較簡單的稠密檢索配置在保持較高吞吐量的同時提供了強勁的性能。所有實驗均在單個消費級GPU上進行，顯示出在適度的計算資源下可以對檢索增強的醫療QA系統進行系統評估。

##### **Joint Optimization of Reasoning and Dual-Memory for Self-Learning Diagnostic Agent**
2604.07269v1 by Bingxuan Li, Simo Du, Yue Guo

Clinical expertise improves not only by acquiring medical knowledge, but by accumulating experience that yields reusable diagnostic patterns. Recent LLMs-based diagnostic agents have shown promising progress in clinical reasoning for decision support. However, most approaches treat cases independently, limiting experience reuse and continual adaptation. We propose SEA, a self-learning diagnostic agent with cognitively inspired dual-memory module. We design a reinforcement training framework tailored to our designed agent for joint optimization of reasoning and memory management. We evaluate SEA in two complementary settings. On standard evaluation with MedCaseReasoning dataset, SEA achieves 92.46% accuracy, outperforming the strongest baseline by +19.6%, demonstrating the benefit of jointly optimizing reasoning and memory. On the long-horizon with ER-Reason dataset, SEA attains the best final accuracy (0.7214) and the largest improvement (+0.35 Acc@100), while baseline methods show limited or unstable gains. Expert evaluation further indicates that rules consolidated from SEA show strong clinical correctness, usefulness and trust, suggesting that the induced rules in dual-memory module are reliable and practically meaningful. Overall, SEA improves both diagnostic reasoning ability and continual learning by effectively transforming experience into reusable knowledge.

摘要：臨床專業知識的提升不僅來自於醫學知識的獲取，還來自於積累可重用的診斷模式的經驗。最近基於大型語言模型的診斷代理在臨床推理和決策支持方面顯示出令人鼓舞的進展。然而，大多數方法將案例獨立處理，限制了經驗的重用和持續適應。我們提出了SEA，一個具有認知啟發的雙記憶模組的自學習診斷代理。我們設計了一個針對我們設計的代理的強化訓練框架，以便共同優化推理和記憶管理。我們在兩個互補的環境中評估SEA。在使用MedCaseReasoning數據集的標準評估中，SEA達到了92.46%的準確率，超越了最強基準19.6%的表現，展示了共同優化推理和記憶的好處。在使用ER-Reason數據集的長期評估中，SEA達到了最佳的最終準確率（0.7214）和最大的改進（+0.35 Acc@100），而基準方法則顯示出有限或不穩定的增益。專家評估進一步表明，從SEA整合的規則顯示出強大的臨床正確性、有用性和可信度，這表明雙記憶模組中誘導的規則是可靠且具有實際意義的。總體而言，SEA通過有效地將經驗轉化為可重用的知識，提升了診斷推理能力和持續學習能力。

##### **Validated Intent Compilation for Constrained Routing in LEO Mega-Constellations**
2604.07264v1 by Yuanhang Li

Operating LEO mega-constellations requires translating high-level operator intents ("reroute financial traffic away from polar links under 80 ms") into low-level routing constraints -- a task that demands both natural language understanding and network-domain expertise. We present an end-to-end system comprising three components: (1) a GNN cost-to-go router that distills Dijkstra-quality routing into a 152K-parameter graph attention network achieving 99.8% packet delivery ratio with 17x inference speedup; (2) an LLM intent compiler that converts natural language to a typed constraint intermediate representation using few-shot prompting with a verifier-feedback repair loop, achieving 98.4% compilation rate and 87.6% full semantic match on feasible intents in a 240-intent benchmark (193 feasible, 47 infeasible); and (3) an 8-pass deterministic validator with constructive feasibility certification that achieves 0% unsafe acceptance on all 47 infeasible intents (30 labeled + 17 discovered by Pass 8), with 100% corruption detection across 240 structural corruption tests and 100% on 15 targeted adversarial attacks. End-to-end evaluation across four constrained routing scenarios confirms zero constraint violations with both routers. We further demonstrate that apparent performance gaps in polar-avoidance scenarios are largely explained by topological reachability ceilings rather than routing quality, and that the LLM compiler outperforms a rule-based baseline by 46.2 percentage points on compositional intents. Our system bridges the semantic gap between operator intent and network configuration while maintaining the safety guarantees required for operational deployment.

摘要：操作低地球軌道（LEO）巨型星座需要將高層次的操作員意圖（「將金融流量重新導向至80毫秒以下的極地鏈路」）轉換為低層次的路由約束——這是一項既需要自然語言理解又需要網絡領域專業知識的任務。<lb/>我們提出了一個端到端系統，包含三個組件：<lb/>(1) 一個GNN成本預測路由器，將Dijkstra質量的路由提煉為一個152K參數的圖注意力網絡，實現99.8%的數據包交付率，並達到17倍的推理加速；<lb/>(2) 一個LLM意圖編譯器，使用少量提示和驗證者反饋修復循環，將自然語言轉換為類型約束的中間表示，實現98.4%的編譯率和87.6%在240個意圖基準（193個可行，47個不可行）上的完全語義匹配；<lb/>(3) 一個具有建設性可行性認證的8次通過確定性驗證器，在所有47個不可行意圖（30個標記 + 17個由第8次通過發現）上實現0%的不安全接受率，並在240個結構性腐敗測試中實現100%的腐敗檢測，以及在15個針對性對抗攻擊中實現100%。<lb/>在四個受限路由場景中的端到端評估確認兩個路由器均無約束違規。我們進一步證明，在避免極地的場景中，明顯的性能差距主要是由於拓撲可達性上限而非路由質量解釋的，並且LLM編譯器在組合意圖上比基於規則的基準高出46.2個百分點。我們的系統彌合了操作員意圖與網絡配置之間的語義差距，同時保持了操作部署所需的安全保證。

##### **$k$-server-bench: Automating Potential Discovery for the $k$-Server Conjecture**
2604.07240v1 by Kirill Brilliantov, Etienne Bamas, Emmanuel Abbé

We introduce a code-based challenge for automated, open-ended mathematical discovery based on the $k$-server conjecture, a central open problem in competitive analysis. The task is to discover a potential function satisfying a large graph-structured system of simple linear inequalities. The resulting evaluation procedure is sound but incomplete: any violated inequality definitively refutes a candidate, whereas satisfying all inequalities does not by itself constitute a proof of the corresponding conjecture's special case. Nevertheless, a candidate that passes all constraints would be strong evidence toward a valid proof and, to the best of our knowledge, no currently known potential achieves this under our formulation in the open $k=4$ circle case. As such, a successful candidate would already be an interesting contribution to the $k$-server conjecture, and could become a substantial theoretical result when paired with a full proof.
  Experiments on the resolved $k=3$ regime show that current agentic methods can solve nontrivial instances, and in the open $k=4$ regime they reduce the number of violations relative to existing potentials without fully resolving the task. Taken together, these results suggest that the task is challenging but plausibly within reach of current methods.
  Beyond its relevance to the $k$-server community, where the developed tooling enables researchers to test new hypotheses and potentially improve on the current record, the task also serves as a useful \emph{benchmark} for developing code-based discovery agents. In particular, our $k=3$ results show that it mitigates important limitations of existing open-ended code-based benchmarks, including early saturation and the weak separation between naive random baselines and more sophisticated methods.

摘要：我們介紹了一個基於代碼的挑戰，旨在自動化、開放式的數學發現，該挑戰基於 $k$-server 猜想，這是一個競爭分析中的核心未解問題。<lb/>這項任務是發現滿足大量圖結構的簡單線性不等式系統的潛在函數。<lb/>結果評估程序是健全但不完整的：任何被違反的不等式都明確駁斥了一個候選者，而滿足所有不等式本身並不構成對相應猜想特例的證明。<lb/>儘管如此，通過所有約束的候選者將是有效證明的強有力證據，據我們所知，當前已知的潛在函數在我們的開放 $k=4$ 圓形案例下並未達到這一要求。<lb/>因此，一個成功的候選者將已經是對 $k$-server 猜想的有趣貢獻，並且當與完整證明結合時，可能成為一個重要的理論結果。<lb/>在解決的 $k=3$ 環境下的實驗顯示，當前的代理方法可以解決非平凡的實例，而在開放的 $k=4$ 環境下，它們相對於現有潛在函數減少了違反的次數，但並未完全解決任務。<lb/>綜合這些結果表明，這項任務具有挑戰性，但可能在當前方法的可達範圍內。<lb/>除了對 $k$-server 社群的相關性外，所開發的工具使研究人員能夠測試新的假設並可能改善當前紀錄，這項任務還作為開發基於代碼的發現代理的有用 \emph{基準}。<lb/>特別是，我們的 $k=3$ 結果顯示，它減輕了現有開放式基於代碼的基準的重要限制，包括早期飽和和天真的隨機基線與更複雜方法之間的弱分離。

##### **LaScA: Language-Conditioned Scalable Modelling of Affective Dynamics**
2604.07193v1 by Kosmas Pinitas, Ilias Maglogiannis

Predicting affect in unconstrained environments remains a fundamental challenge in human-centered AI. While deep neural embeddings dominate contemporary approaches, they often lack interpretability and limit expert-driven refinement. We propose a novel framework that uses Language Models (LMs) as semantic context conditioners over handcrafted affect descriptors to model changes in Valence and Arousal. Our approach begins with interpretable facial geometry and acoustic features derived from structured domain knowledge. These features are transformed into symbolic natural-language descriptions encoding their affective implications. A pretrained LM processes these descriptions to generate semantic context embeddings that act as high-level priors over affective dynamics. Unlike end-to-end black-box pipelines, our framework preserves feature transparency while leveraging the contextual abstraction capabilities of LMs. We evaluate the proposed method on the Aff-Wild2 and SEWA datasets for affect change prediction. Experimental results show consistent improvements in accuracy for both Valence and Arousal compared to handcrafted-only and deep-embedding baselines. Our findings demonstrate that semantic conditioning enables interpretable affect modelling without sacrificing predictive performance, offering a transparent and computationally efficient alternative to fully end-to-end architectures

摘要：預測無約束環境中的情感仍然是以人為中心的人工智慧中的一項基本挑戰。雖然深度神經嵌入主導當代方法，但它們往往缺乏可解釋性，並限制了專家驅動的精煉。我們提出了一個新穎的框架，使用語言模型（LMs）作為手工製作的情感描述的語義上下文調節器，以建模情感的價值和喚醒的變化。我們的方法從可解釋的面部幾何和基於結構化領域知識衍生的聲學特徵開始。這些特徵被轉換為符號自然語言描述，編碼其情感含義。一個預訓練的LM處理這些描述，以生成語義上下文嵌入，這些嵌入作為情感動態的高級先驗。與端到端的黑箱管道不同，我們的框架在利用LM的上下文抽象能力的同時，保持特徵的透明性。我們在Aff-Wild2和SEWA數據集上評估所提出的方法，以預測情感變化。實驗結果顯示，與僅使用手工製作和深度嵌入基準相比，對於價值和喚醒的準確性都有持續的改善。我們的研究結果表明，語義調節使得可解釋的情感建模成為可能，而不犧牲預測性能，提供了一種透明且計算效率高的替代方案，取代完全端到端的架構。

##### **Self-Discovered Intention-aware Transformer for Multi-modal Vehicle Trajectory Prediction**
2604.07126v1 by Diyi Liu, Zihan Niu, Tu Xu, Lishan Sun

Predicting vehicle trajectories plays an important role in autonomous driving and ITS applications. Although multiple deep learning algorithms are devised to predict vehicle trajectories, their reliant on specific graph structure (e.g., Graph Neural Network) or explicit intention labeling limit their flexibilities. In this study, we propose a pure Transformer-based network with multiple modals considering their neighboring vehicles. Two separate tracks are employed. One track focuses on predicting the trajectories while the other focuses on predicting the likelihood of each intention considering neighboring vehicles. Study finds that the two track design can increase the performance by separating spatial module from the trajectory generating module. Also, we find the the model can learn an ordered group of trajectories by predicting residual offsets among K trajectories.

摘要：預測車輛軌跡在自駕車和智能交通系統應用中扮演著重要角色。儘管多種深度學習算法被設計用於預測車輛軌跡，但它們對特定圖結構（例如，圖神經網絡）或明確的意圖標記的依賴限制了它們的靈活性。在本研究中，我們提出了一種基於純Transformer的網絡，考慮到其鄰近車輛的多模態。使用了兩條獨立的軌道。一條軌道專注於預測軌跡，而另一條則專注於考慮鄰近車輛的每個意圖的可能性預測。研究發現，這種兩條軌道的設計可以通過將空間模塊與軌跡生成模塊分開來提高性能。此外，我們發現該模型可以通過預測K條軌跡之間的殘差偏移來學習有序的軌跡組。

##### **Information as Structural Alignment: A Dynamical Theory of Continual Learning**
2604.07108v1 by Radu Negulescu

Catastrophic forgetting is not an engineering failure. It is a mathematical consequence of storing knowledge as global parameter superposition. Existing methods, such as regularization, replay, and frozen subnetworks, add external mechanisms to a shared-parameter substrate. None derives retention from the learning dynamics themselves.
  This paper introduces the Informational Buildup Framework (IBF), an alternative substrate for continual learning, based on the premise that information is the achievement of structural alignment rather than stored content. In IBF, two equations govern the dynamics: a Law of Motion that drives configuration toward higher coherence, and Modification Dynamics that persistently deform the coherence landscape in response to localized discrepancies. Memory, agency, and self-correction arise from these dynamics rather than being added as separate modules.
  We first demonstrate the full lifecycle in a transparent two-dimensional toy model, then validate across three domains: a controlled non-stationary world, chess evaluated independently by Stockfish, and Split-CIFAR-100 with a frozen ViT encoder. Across all three, IBF achieves replay-superior retention without storing raw data. We observe near-zero forgetting on CIFAR-100 (BT = -0.004), positive backward transfer in chess (+38.5 cp), and 43% less forgetting than replay in the controlled domain. In chess, the framework achieves a mean behavioral advantage of +88.9 +/- 2.8 cp under independent evaluation, exceeding MLP and replay baselines.

摘要：災難性遺忘並不是工程失敗。它是將知識儲存為全局參數疊加的數學結果。現有的方法，如正則化、重播和凍結子網絡，向共享參數基質添加外部機制。沒有一種方法是從學習動態本身推導出保持能力的。

本文介紹了資訊積累框架（IBF），這是一種基於資訊是結構對齊的成就而非儲存內容的替代基質，用於持續學習。在IBF中，兩個方程控制動態：一個驅動配置朝向更高一致性的運動定律，以及持續變形一致性景觀以響應局部差異的修改動態。記憶、能動性和自我修正源於這些動態，而不是作為單獨模塊添加。

我們首先在一個透明的二維玩具模型中展示完整的生命周期，然後在三個領域進行驗證：一個受控的非穩態世界、由Stockfish獨立評估的棋類，以及使用凍結的ViT編碼器的Split-CIFAR-100。在這三個領域中，IBF在不儲存原始數據的情況下實現了重播優越的保持能力。我們在CIFAR-100上觀察到近乎零的遺忘（BT = -0.004）、棋類中的正向回傳（+38.5 cp），以及在受控領域中比重播少43%的遺忘。在棋類中，該框架在獨立評估下實現了平均行為優勢+88.9 +/- 2.8 cp，超過了MLP和重播基準。

##### **The Impact of Steering Large Language Models with Persona Vectors in Educational Applications**
2604.07102v1 by Yongchao Wu, Aron Henriksson

Activation-based steering can personalize large language models at inference time, but its effects in educational settings remain unclear. We study persona vectors for seven character traits in short-answer generation and automated scoring on the ASAP-SAS benchmark across three models spanning two architectures. Persona steering lowers answer quality overall, with much larger effects on open-ended English Language Arts (ELA) prompts than on factual science prompts; interpretive and argumentative tasks are up to 11x more sensitive. On the scoring side, we observe predictable valence-aligned calibration shifts: evil and impolite scorers grade more harshly, while good and optimistic scorers grade more leniently. ELA tasks are 2.5-3x more susceptible to scorer personalization than science tasks, and the Mixture-of-Experts model shows roughly 6x larger calibration shifts than the dense models. To our knowledge, this is the first study to systematically examine the effects of activation-steered persona traits in educational generation and scoring, and the results highlight the need for task-aware and architecture-aware calibration when deploying steered models in educational settings.

摘要：啟動基礎的引導可以在推理時個性化大型語言模型，但其在教育環境中的影響仍不明確。<lb/>我們研究了七種性格特徵的個性向量，在ASAP-SAS基準上進行短答案生成和自動評分，涵蓋了兩種架構的三個模型。<lb/>個性引導整體上降低了答案質量，對開放式英語語言藝術（ELA）提示的影響遠大於對事實科學提示的影響；詮釋性和論證性任務的敏感度高達11倍。<lb/>在評分方面，我們觀察到可預測的情感對齊校準變化：邪惡和不禮貌的評分者評分更嚴格，而善良和樂觀的評分者則評分更寬鬆。<lb/>ELA任務對評分者個性化的敏感度是科學任務的2.5-3倍，而混合專家模型的校準變化約比密集模型大6倍。<lb/>據我們所知，這是第一項系統性研究啟動引導的個性特徵在教育生成和評分中的影響的研究，結果突顯了在教育環境中部署引導模型時對任務意識和架構意識的校準需求。

##### **Selective Neuron Amplification for Training-Free Task Enhancement**
2604.07098v1 by Ryyan Akhtar

Large language models often fail on tasks they seem to already understand. In our experiments, this appears to be less about missing knowledge and more about certain internal circuits not being strongly activated during inference. We explore Selective Neuron Amplification, which increases the influence of task relevant neurons without changing the model's parameters. The method works at inference time and does not permanently alter the model. SNA helps mainly when the model is uncertain, while having low effect when the model is already confident. This suggests that some model failures are due to weak activation rather than lack of capability.

摘要：大型語言模型在看似已經理解的任務上常常失敗。<lb/>在我們的實驗中，這似乎與缺乏知識關係不大，而更多是因為某些內部電路在推理過程中未被強烈激活。<lb/>我們探討了選擇性神經元增幅，這種方法在不改變模型參數的情況下增加了與任務相關的神經元的影響力。<lb/>該方法在推理時有效，並不會永久改變模型。<lb/>SNA 主要在模型不確定時提供幫助，而當模型已經有信心時效果較低。<lb/>這表明某些模型失敗是由於激活不足，而非能力不足。

##### **A-MBER: Affective Memory Benchmark for Emotion Recognition**
2604.07017v1 by Deliang Wen, Ke Sun, Yu Wang

AI assistants that interact with users over time need to interpret the user's current emotional state in order to respond appropriately and personally. However, this capability remains insufficiently evaluated. Existing emotion datasets mainly assess local or instantaneous affect, while long-term memory benchmarks focus largely on factual recall, temporal consistency, or knowledge updating. As a result, current resources provide limited support for testing whether a model can use remembered interaction history to interpret a user's present affective state.
  We introduce A-MBER, an Affective Memory Benchmark for Emotion Recognition, to evaluate this capability. A-MBER focuses on present affective interpretation grounded in remembered multi-session interaction history. Given an interaction trajectory and a designated anchor turn, a model must infer the user's current affective state, identify historically relevant evidence, and justify its interpretation in a grounded way. The benchmark is constructed through a staged pipeline with explicit intermediate representations, including long-horizon planning, conversation generation, annotation, question construction, and final packaging. It supports judgment, retrieval, and explanation tasks, together with robustness settings such as modality degradation and insufficient-evidence conditions.
  Experiments compare local-context, long-context, retrieved-memory, structured-memory, and gold-evidence conditions within a unified framework. Results show that A-MBER is especially discriminative on the subsets it is designed to stress, including long-range implicit affect, high-dependency memory levels, trajectory-based reasoning, and adversarial settings. These findings suggest that memory supports affective interpretation not simply by providing more history, but by enabling more selective, grounded, and context-sensitive use of past interaction

摘要：AI 助手在與用戶互動的過程中需要解讀用戶當前的情感狀態，以便做出適當且個性化的回應。<lb/>然而，這一能力仍然缺乏充分的評估。<lb/>現有的情感數據集主要評估局部或瞬時的情感，而長期記憶基準則主要集中在事實回憶、時間一致性或知識更新上。<lb/>因此，目前的資源對於測試模型是否能利用記住的互動歷史來解釋用戶當前的情感狀態提供的支持有限。<lb/>我們推出 A-MBER，一個用於情感識別的情感記憶基準，以評估這一能力。<lb/>A-MBER 專注於基於記住的多會話互動歷史的當前情感解釋。<lb/>給定一個互動軌跡和一個指定的錨點回合，模型必須推斷用戶當前的情感狀態，識別歷史相關證據，並以有根據的方式為其解釋提供理由。<lb/>該基準通過一個分階段的流程構建，具有明確的中間表示，包括長期規劃、對話生成、標註、問題構建和最終打包。<lb/>它支持判斷、檢索和解釋任務，以及模態降級和證據不足條件等穩健性設置。<lb/>實驗在統一框架內比較局部上下文、長上下文、檢索記憶、結構記憶和黃金證據條件。<lb/>結果表明，A-MBER 在其設計用來強調的子集上特別具有區分性，包括長距離隱含情感、高依賴性記憶水平、基於軌跡的推理和對抗設置。<lb/>這些發現表明，記憶支持情感解釋不僅僅是通過提供更多的歷史，而是通過使過去的互動能夠更具選擇性、有根據和上下文敏感地使用。

##### **Corpora deduplication or duplication in Natural Language Processing of few resourced languages ? A case of study: The Mexico's Nahuatl**
2604.07015v1 by Juan-José Guzman-Landa, Juan-Manuel Torres-Moreno, Graham Ranger, Miguel Figueroa-Saavedra, Martha-Lorena Avendaño-Garrido, Elvys Linhares-Pontes, Luis-Gil Moreno-Jiménez

In this article, we seek to answer the following question: could data duplication be useful in Natural Language Processing (NLP) for languages with limited computational resources? In this type of languages (or $π$-languages), corpora available for training Large Language Models are virtually non-existent. In particular, we will study the impact of corpora expansion in Nawatl, an agglutinative and polysynthetic $π$-language spoken by over 2 million people, with a large number of dialectal varieties. The aim is to expand the new $π$-yalli corpus, which contains a limited number of Nawatl texts, by duplicating it in a controlled way. In our experiments, we will use the incremental duplication technique. The aim is to learn embeddings that are well-suited to NLP tasks. Thus, static embeddings were trained and evaluated in a sentence-level semantic similarity task. Our results show a moderate improvement in performance when using incremental duplication compared to the results obtained using only the corpus without expansion. Furthermore, to our knowledge, this technique has not yet been used in the literature.

摘要：在本文中，我們試圖回答以下問題：資料重複在資源有限的語言中是否對自然語言處理（NLP）有用？在這類語言（或稱 $π$-語言）中，用於訓練大型語言模型的語料幾乎不存在。特別是，我們將研究語料擴展對納瓦特語的影響，這是一種由超過200萬人使用的粘著語和多合成 $π$-語言，擁有大量方言變體。目標是通過以受控的方式重複來擴展新的 $π$-yalli 語料庫，該語料庫包含有限數量的納瓦特語文本。在我們的實驗中，我們將使用增量重複技術。目的是學習適合NLP任務的嵌入。因此，靜態嵌入在句子層級的語義相似性任務中進行了訓練和評估。我們的結果顯示，與僅使用未擴展的語料獲得的結果相比，使用增量重複時性能有適度改善。此外，據我們所知，這種技術在文獻中尚未被使用。

##### **DTCRS: Dynamic Tree Construction for Recursive Summarization**
2604.07012v1 by Guanran Luo, Zhongquan Jian, Wentao Qiu, Meihong Wang, Qingqiang Wu

Retrieval-Augmented Generation (RAG) mitigates the hallucination problem of Large Language Models (LLMs) by incorporating external knowledge. Recursive summarization constructs a hierarchical summary tree by clustering text chunks, integrating information from multiple parts of a document to provide evidence for abstractive questions involving multi-step reasoning. However, summary trees often contain a large number of redundant summary nodes, which not only increase construction time but may also negatively impact question answering. Moreover, recursive summarization is not suitable for all types of questions. We introduce DTCRS, a method that dynamically generates summary trees based on document structure and query semantics. DTCRS determines whether a summary tree is necessary by analyzing the question type. It then decomposes the question and uses the embeddings of sub-questions as initial cluster centers, reducing redundant summaries while improving the relevance between summaries and the question. Our approach significantly reduces summary tree construction time and achieves substantial improvements across three QA tasks. Additionally, we investigate the applicability of recursive summarization to different question types, providing valuable insights for future research.

摘要：檢索增強生成（RAG）通過整合外部知識來減輕大型語言模型（LLMs）的幻覺問題。遞歸摘要通過聚類文本片段構建層次摘要樹，整合文檔多個部分的信息，以提供涉及多步推理的抽象問題的證據。然而，摘要樹通常包含大量冗餘的摘要節點，這不僅增加了構建時間，還可能對問題回答產生負面影響。此外，遞歸摘要並不適用於所有類型的問題。我們介紹了DTCRS，一種基於文檔結構和查詢語義動態生成摘要樹的方法。DTCRS通過分析問題類型來確定摘要樹是否必要。然後，它將問題分解，並使用子問題的嵌入作為初始聚類中心，減少冗餘摘要，同時提高摘要與問題之間的相關性。我們的方法顯著減少了摘要樹的構建時間，並在三個QA任務中實現了實質性的改進。此外，我們還研究了遞歸摘要對不同問題類型的適用性，為未來的研究提供了有價值的見解。

##### **FORGE:Fine-grained Multimodal Evaluation for Manufacturing Scenarios**
2604.07413v1 by Xiangru Jian, Hao Xu, Wei Pang, Xinjian Zhao, Chengyu Tao, Qixin Zhang, Xikun Zhang, Chao Zhang, Guanzhi Deng, Alex Xue, Juan Du, Tianshu Yu, Garth Tarr, Linqi Song, Qiuzhuang Sun, Dacheng Tao

The manufacturing sector is increasingly adopting Multimodal Large Language Models (MLLMs) to transition from simple perception to autonomous execution, yet current evaluations fail to reflect the rigorous demands of real-world manufacturing environments. Progress is hindered by data scarcity and a lack of fine-grained domain semantics in existing datasets. To bridge this gap, we introduce FORGE. Wefirst construct a high-quality multimodal dataset that combines real-world 2D images and 3D point clouds, annotated with fine-grained domain semantics (e.g., exact model numbers). We then evaluate 18 state-of-the-art MLLMs across three manufacturing tasks, namely workpiece verification, structural surface inspection, and assembly verification, revealing significant performance gaps. Counter to conventional understanding, the bottleneck analysis shows that visual grounding is not the primary limiting factor. Instead, insufficient domain-specific knowledge is the key bottleneck, setting a clear direction for future research. Beyond evaluation, we show that our structured annotations can serve as an actionable training resource: supervised fine-tuning of a compact 3B-parameter model on our data yields up to 90.8% relative improvement in accuracy on held-out manufacturing scenarios, providing preliminary evidence for a practical pathway toward domain-adapted manufacturing MLLMs. The code and datasets are available at https://ai4manufacturing.github.io/forge-web.

摘要：製造業正越來越多地採用多模態大型語言模型（MLLMs）來從簡單的感知過渡到自主執行，但目前的評估未能反映現實製造環境的嚴格要求。進展受到數據稀缺和現有數據集缺乏細粒度領域語義的阻礙。為了填補這一空白，我們引入了FORGE。我們首先構建了一個高質量的多模態數據集，該數據集結合了現實世界的2D圖像和3D點雲，並用細粒度的領域語義（例如，精確的型號）進行標註。然後，我們在三個製造任務上評估了18個最先進的MLLMs，即工件驗證、結構表面檢查和組裝驗證，揭示了顯著的性能差距。與傳統理解相反，瓶頸分析顯示視覺基礎並不是主要的限制因素。相反，缺乏特定領域的知識是關鍵瓶頸，為未來的研究指明了明確方向。除了評估，我們還展示了我們的結構化標註可以作為可行的訓練資源：在我們的數據上對一個緊湊的3B參數模型進行監督式微調，能在保留的製造場景中實現高達90.8%的相對準確率提升，為朝著領域適應的製造MLLMs的實用途徑提供了初步證據。代碼和數據集可在 https://ai4manufacturing.github.io/forge-web 獲得。

