# arxiv-daily
 Automated deployment @ 2026-09-04 23:46:56 Asia/Taipei
> Welcome to contribute! Add your topics and keywords in [`topic.yml`](https://github.com/jawatech/arxiv-daily-in-place/blob/main/database/topic.yml).
> You can also view historical data through the [storage](https://github.com/jawatech/arxiv-daily-in-place/blob/main/database/storage).

## AI

### Medical explainable AI
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-09-03**|**Clean Engineering, Unstable Measurement: A Preregistered Reliability Failure of Black-Box LLM Observers on Shared Endpoints**|Haoyaun Zhu et.al.|[2609.04198v1](http://arxiv.org/abs/2609.04198v1)|null|
|**2026-09-03**|**A Computationally Feasible Framework for Causal Probabilistic Explanation**|Rafal Urbaniak et.al.|[2609.04177v1](http://arxiv.org/abs/2609.04177v1)|null|
|**2026-09-03**|**Rethinking On-Policy Distillation of Large Language Models II: One Training Example**|Zixuan Fu et.al.|[2609.04172v1](http://arxiv.org/abs/2609.04172v1)|null|
|**2026-09-03**|**Epistemic Warrant for LLM Recommendations: Characterizing the Basis for Reliance When Ground Truth Is Unavailable**|Shai Vardi et.al.|[2609.04127v1](http://arxiv.org/abs/2609.04127v1)|null|
|**2026-09-03**|**Why Gated DeltaNet Survives 4-Bit Quantization: NVFP4 W4A4 for the Recurrent Half of a Hybrid 27B LLM**|Sergii Kozyrev et.al.|[2609.04098v1](http://arxiv.org/abs/2609.04098v1)|null|
|**2026-09-03**|**Witnesses Explain Anomalies**|Lamine Diop et.al.|[2609.03826v1](http://arxiv.org/abs/2609.03826v1)|null|
|**2026-09-03**|**Cross-Dataset Transfer and Reliability of Explainable Artificial Intelligence for RhythmFormer Remote Photoplethysmography**|Louis Chen et.al.|[2609.03663v1](http://arxiv.org/abs/2609.03663v1)|null|
|**2026-09-03**|**CulturalMenuBench: Probing the Knowledge-Application Gap in Multimodal Culinary Reasoning**|Bo Zeng et.al.|[2609.03526v1](http://arxiv.org/abs/2609.03526v1)|null|
|**2026-09-02**|**Towards Trustworthy Autonomous Robots: An Explainable AI-Based Decision Framework**|Cagri Temel et.al.|[2609.02861v1](http://arxiv.org/abs/2609.02861v1)|null|
|**2026-09-02**|**HiPoly: a hierarchical polymer-native AI framework for property prediction and generative design**|Ge Sun et.al.|[2609.02746v1](http://arxiv.org/abs/2609.02746v1)|null|
|**2026-09-02**|**Contrastive Explanations in Quantitative Bipolar Argumentation Frameworks**|Xiang Yin et.al.|[2609.02399v1](http://arxiv.org/abs/2609.02399v1)|null|
|**2026-09-02**|**Do Large Language Models Capture the Diversity in their Training Data?**|Youqi Wu et.al.|[2609.02275v1](http://arxiv.org/abs/2609.02275v1)|null|
|**2026-09-02**|**Retrosynthesis of Synthetic Media for Explainable AI Provenance Forensics**|Yijie Lin et.al.|[2609.02268v1](http://arxiv.org/abs/2609.02268v1)|null|
|**2026-09-02**|**DiffuSearch: How Hybrid Trajectory Planning Benefits from Aligned Objectives in Diffusion and Action Space**|Steffen Hagedorn et.al.|[2609.02252v1](http://arxiv.org/abs/2609.02252v1)|null|
|**2026-09-02**|**Propose to Learn, Learn to Propose: Evaluability-Aware Assistance under Bounded Rationality**|Yifan Zhu et.al.|[2609.02242v1](http://arxiv.org/abs/2609.02242v1)|null|
|**2026-09-02**|**Examining the Vulnerability of Multi-Agent Medical Systems to Human Interventions for Clinical Reasoning**|Benjamin C Liu et.al.|[2609.02191v1](http://arxiv.org/abs/2609.02191v1)|null|
|**2026-09-02**|**A Power Law in Logarithm's Clothing: On the Scalability of Graph-Based Vector Search**|Sajad Faghfoor Maghrebi et.al.|[2609.02143v1](http://arxiv.org/abs/2609.02143v1)|null|
|**2026-09-02**|**Disease Burden over Skin Tone: Decomposing the Dermatology-AI Generalization Gap**|Nirajan Kunwor et.al.|[2609.02111v1](http://arxiv.org/abs/2609.02111v1)|null|
|**2026-09-02**|**READY or Not: Reliable Enterprise Agent Deployment**|Veronica Chatrath et.al.|[2609.02095v1](http://arxiv.org/abs/2609.02095v1)|null|
|**2026-09-01**|**Sparse Readout Prism: Explaining Logit-Lens Scores in Features Instead of Tokens**|Matteo He et.al.|[2609.01936v1](http://arxiv.org/abs/2609.01936v1)|null|
|**2026-09-01**|**When Safety Routing Breaks: Understanding Alignment Fragility under Benign Fine-Tuning**|Yitong Guo et.al.|[2609.01455v1](http://arxiv.org/abs/2609.01455v1)|null|
|**2026-09-01**|**Neuro-Symbolic Geometric Abstraction (NeuSOGA): From Observations to Symbolic Mathematical Representations**|Qingde Li et.al.|[2609.01408v2](http://arxiv.org/abs/2609.01408v2)|null|
|**2026-09-01**|**Cheap Verifiers, Large Blind Spots: Measuring the Reliability Cost of Cost-Saving Cascades**|Dushyant Rajput et.al.|[2609.01345v1](http://arxiv.org/abs/2609.01345v1)|null|
|**2026-09-01**|**Prompt-Robust Language Models: Which Training Strategies Work?**|Frederic Sadrieh et.al.|[2609.01217v1](http://arxiv.org/abs/2609.01217v1)|null|
|**2026-09-01**|**Autonomous discovery of new structure-plausibility laws for explainable and rapid crystal diagnosis and screening**|Zhilong Song et.al.|[2609.01209v1](http://arxiv.org/abs/2609.01209v1)|null|
|**2026-09-01**|**Towards AI-Assisted Clinical Trial Matching: Practical Considerations, Multicenter Evaluation, and Real-World Deployment**|Yin Fang et.al.|[2609.01202v1](http://arxiv.org/abs/2609.01202v1)|null|
|**2026-09-01**|**Lagged Coupling: Internal Representations Become Readable Before They Become Causal**|Xining Xun et.al.|[2609.01048v1](http://arxiv.org/abs/2609.01048v1)|null|
|**2026-09-01**|**Causal Evidentiary Governance for High-Risk Machine Learning Systems**|Samah Kareem et.al.|[2609.01040v1](http://arxiv.org/abs/2609.01040v1)|null|
|**2026-09-01**|**Towards reliable multimodal disaster severity assessment through preference optimization and explainable vision-language reasoning**|Yuanjun Zhang et.al.|[2609.00879v1](http://arxiv.org/abs/2609.00879v1)|null|
|**2026-09-01**|**Ctrl-F-Resist. Practices, Challenges, and Technical Needs of Civil Society Organizations Monitoring the Far-Right Online**|Elisabeth Steffen et.al.|[2609.00808v1](http://arxiv.org/abs/2609.00808v1)|null|
|**2026-09-01**|**ContextPipe: Database-Inspired Context Assembly for Long-Horizon Agents**|Peng Xu et.al.|[2609.00749v1](http://arxiv.org/abs/2609.00749v1)|null|
|**2026-09-01**|**Escaping Redundant Reasoning: Structure-Aware Search for Inference-Time LLMs**|Lu Cheng et.al.|[2609.00738v1](http://arxiv.org/abs/2609.00738v1)|null|
|**2026-09-01**|**SciTrue: Reliable Scientific Claim Validation with Frontier and Open Language Models at the NTCIR SciClaimEval Task**|Qiming Bao et.al.|[2609.00654v1](http://arxiv.org/abs/2609.00654v1)|null|
|**2026-09-01**|**Self-Reports Are Not Verification: Environment-Grounded Auditing of LLM Operators in Evolutionary Search**|Enrong Pan et.al.|[2609.00652v1](http://arxiv.org/abs/2609.00652v1)|null|
|**2026-09-01**|**A Mathematical Framework for Legacy, Governance, and Decision Integrity in Enterprise AI**|Shorab Sarker et.al.|[2609.00572v1](http://arxiv.org/abs/2609.00572v1)|null|
|**2026-08-31**|**Does Reasoning Mitigate Backdoor Attacks? A Neuro-Symbolic Perspective**|Marco Antonio Corallo et.al.|[2609.00464v1](http://arxiv.org/abs/2609.00464v1)|null|
|**2026-08-31**|**One note in three: a verified census of three deployed AI scribes, and the instrument that counted it**|Sebastian Fox et.al.|[2608.31017v1](http://arxiv.org/abs/2608.31017v1)|null|
|**2026-08-31**|**LLM Judges Verify Presence, Not Absence: Omission Blindness in AI Clinical Notes and What Recovers It**|Sebastian Fox et.al.|[2608.31016v1](http://arxiv.org/abs/2608.31016v1)|null|
|**2026-08-31**|**Taking the Whys Seriously: Limitations of Counterfactual Explanations in Justification and Recourse**|Mattia Cerrato et.al.|[2608.30956v1](http://arxiv.org/abs/2608.30956v1)|null|
|**2026-08-31**|**Responsible Integration of AI in Cancer Genomics: Barriers, Risks, and Pathways to Trustworthy Clinical Translation**|Bahar İlgen et.al.|[2608.30912v1](http://arxiv.org/abs/2608.30912v1)|null|
|**2026-08-31**|**Autoregressive Mosaics: Probing 2D Spatial Reasoning in Text-Only Language Models**|Ashwin Nedungadi et.al.|[2608.30751v2](http://arxiv.org/abs/2608.30751v2)|null|
|**2026-08-31**|**Assessing Alignment and Stability of Feature Importance Explanations via Weight of Evidence**|Eddie Conti et.al.|[2609.00090v1](http://arxiv.org/abs/2609.00090v1)|null|
|**2026-08-31**|**MedAgent-R1: Faithfulness-Aware Reinforcement Learning for Evidence-Grounded Medical Reasoning**|Jiangwang Chen et.al.|[2608.30676v1](http://arxiv.org/abs/2608.30676v1)|null|
|**2026-08-31**|**Automated Testing of LLM-Based Post Hoc Explainers Using Model Checking as an Oracle**|Dennis Gross et.al.|[2608.30581v1](http://arxiv.org/abs/2608.30581v1)|null|
|**2026-08-31**|**AI Morbidity and Mortality: A Framework for Clinical AI Failure Review**|Paulius Mui et.al.|[2609.00076v1](http://arxiv.org/abs/2609.00076v1)|null|
|**2026-08-31**|**Co-Annotator: Expert-Distilled ViT and VLM for Visual and Documentation Guidance in Age-Related Macular Degeneration**|Ziheng "Leo" Li et.al.|[2608.30352v1](http://arxiv.org/abs/2608.30352v1)|null|
|**2026-08-31**|**Science sandboxes measure the scientific capability of AI agents**|Arya S. Rao et.al.|[2608.30165v1](http://arxiv.org/abs/2608.30165v1)|null|
|**2026-08-30**|**Automatic Conversion of NICE Guidelines to an Executable Computational Model Using Large Language Models**|Ashvin Gupta et.al.|[2608.30022v1](http://arxiv.org/abs/2608.30022v1)|null|
|**2026-08-30**|**Review Before Trust: Source-Grounded Integrity Gates for AI-Assisted Personal Health Records**|Nora Girda et.al.|[2608.29965v1](http://arxiv.org/abs/2608.29965v1)|null|
|**2026-08-30**|**Life Operators: a self-evolving framework for multiscale life modelling**|Shuo Wang et.al.|[2609.00068v1](http://arxiv.org/abs/2609.00068v1)|null|
|**2026-08-30**|**Scientific Agent Skills: A Library of Procedural Knowledge for Research Agents**|Timothy Kassis et.al.|[2609.00065v2](http://arxiv.org/abs/2609.00065v2)|null|
|**2026-08-30**|**Memory-First Fact-Checking: A Knowledge-Graph-Grounded Multi-Agent System for Misinformation Detection**|Amelia Petrenciuc et.al.|[2608.29617v1](http://arxiv.org/abs/2608.29617v1)|null|
|**2026-08-29**|**AI Can Be Easily Persuaded in Clinical Decision Making**|Jiayuan Zhu et.al.|[2608.29453v1](http://arxiv.org/abs/2608.29453v1)|null|
|**2026-08-29**|**Does Latent Planning Survive Point Clouds? Action-Conditioned JEPA World Models for Geometric Observations**|Fabio F. Oberweger et.al.|[2608.29434v1](http://arxiv.org/abs/2608.29434v1)|null|
|**2026-08-29**|**AOI-Net: Structural Face AOI-Guided Eye-Gaze Track Representation Learning for Autism Spectrum Disorder Detection**|Zhanpei Huang et.al.|[2608.29289v1](http://arxiv.org/abs/2608.29289v1)|null|
|**2026-08-29**|**RACER: Reinforced Agent Collaboration for Explainable Reasoning on Knowledge Graphs**|Yuwei Lou et.al.|[2608.29263v1](http://arxiv.org/abs/2608.29263v1)|null|
|**2026-08-29**|**Rate-Coding Bundle Memory: A Unified Model of Memory and Control for Symbolic Computation in the Brain**|Teun van Gils et.al.|[2608.29189v1](http://arxiv.org/abs/2608.29189v1)|null|
|**2026-08-29**|**An Explainable Coherence Score for Detecting Temporal Inconsistencies in Political News**|Marius Nicusor Pantea et.al.|[2608.29175v1](http://arxiv.org/abs/2608.29175v1)|null|
|**2026-08-29**|**Not the Same Protector: Deployment-Dependent Protective Intervention in LLMs**|Eunna Lee et.al.|[2608.29136v1](http://arxiv.org/abs/2608.29136v1)|null|
|**2026-08-29**|**Emergent Misalignment Is Not Magical**|Mingxuan Li et.al.|[2608.29118v1](http://arxiv.org/abs/2608.29118v1)|null|
|**2026-08-29**|**Clustering as Approximation by Constrained Projectors: Theory and Guarantees**|Angshul Majumdar et.al.|[2608.29102v1](http://arxiv.org/abs/2608.29102v1)|null|
|**2026-08-28**|**From the Loss Landscape to Diverse Feature Learning in Neural Networks**|David Aram Yunis et.al.|[2608.28948v1](http://arxiv.org/abs/2608.28948v1)|null|
|**2026-08-28**|**Toward Postural State Classification in Immersive VR with Multimodal Data and Explainability Analysis**|Nipa Anjum et.al.|[2608.28844v1](http://arxiv.org/abs/2608.28844v1)|null|
|**2026-08-28**|**Explainable Artificial Intelligence (XAI) in Computational Pathology: Definitions, Taxonomy, and Recommendations**|Shubham Innani et.al.|[2608.28820v1](http://arxiv.org/abs/2608.28820v1)|null|
|**2026-08-28**|**PermitGPT: A Unified Generative-AI Pipeline for Construction Hazard Forecasting, Permit Prediction, and Community Impact**|Mohd Ruhul Ameen et.al.|[2608.28728v1](http://arxiv.org/abs/2608.28728v1)|null|
|**2026-08-28**|**When Linguistic and Internal Confidence Diverge in Large Language Models**|Hefan Zhang et.al.|[2608.28382v1](http://arxiv.org/abs/2608.28382v1)|null|
|**2026-08-28**|**AI as Teammate: Rethinking Task Distribution in Medical Training**|Fendi Tsim et.al.|[2608.28373v1](http://arxiv.org/abs/2608.28373v1)|null|
|**2026-08-28**|**AGENT-O: A Semantic Agent Card Framework for Interoperable and Governed Healthcare AI Agents**|Pengze Li et.al.|[2608.28345v1](http://arxiv.org/abs/2608.28345v1)|null|
|**2026-08-28**|**Gen-TAS: A Generative AI-Aided Hardware-Software Task Allocation Framework for FPGA-GPP Heterogeneous Systems**|Mary Kong et.al.|[2608.28160v1](http://arxiv.org/abs/2608.28160v1)|null|
|**2026-08-28**|**Explainable Uncertainty Estimation for Reliable Medical AI**|Li Rong Wang et.al.|[2608.28052v1](http://arxiv.org/abs/2608.28052v1)|null|
|**2026-08-28**|**Compared to What? A Human-Anchored Security Benchmark for LLM-Generated Infrastructure-as-Code**|Animesh Shaw et.al.|[2608.28021v1](http://arxiv.org/abs/2608.28021v1)|null|
|**2026-08-28**|**The Illusion of $\textit{What If}$: Evaluating the Breakdown of Counterfactual Reasoning in LLMs**|Yucheng Wang et.al.|[2608.27953v1](http://arxiv.org/abs/2608.27953v1)|null|
|**2026-08-28**|**Actionable CBFI: Integrating Structural Decomposition and Causal Counterfactual Recourse for Tabular Machine Learning**|Sejong Oh et.al.|[2608.27821v1](http://arxiv.org/abs/2608.27821v1)|null|
|**2026-08-27**|**Compositional Failure in Audio-Visual LLMs: Late-Layer Prior Dominance Under Cross-modal Conflict**|Adarsh Sudheer et.al.|[2608.27785v1](http://arxiv.org/abs/2608.27785v1)|null|
|**2026-08-27**|**Efficient Auto-Interpretability of AI Models in Biology**|Piotr Jedryszek et.al.|[2608.27754v1](http://arxiv.org/abs/2608.27754v1)|null|
|**2026-08-27**|**Self-Explainable Multi-Label Graph Neural Network for Correlated Evidence Attribution**|Yingqi Feng et.al.|[2608.27574v1](http://arxiv.org/abs/2608.27574v1)|null|
|**2026-08-27**|**ORDDAR: Observation-Driven Reasoning for Distortion-Resilient Decision, Action, and Cognitive Recovery**|Deblina Kar et.al.|[2608.28704v1](http://arxiv.org/abs/2608.28704v1)|null|
|**2026-08-27**|**SETU: An Agentic Ecosystem for Multilingual, Persona-Aware Communication Coaching**|Jonnalagadda Maruthi Tejas et.al.|[2608.27524v1](http://arxiv.org/abs/2608.27524v1)|null|
|**2026-08-27**|**LAAF: A Layered Accountability Architecture Framework for LLM Applications**|Prachi Chaturvedi et.al.|[2608.27102v1](http://arxiv.org/abs/2608.27102v1)|null|
|**2026-08-27**|**Accelerating Scientific Research with Gemini in the Real-World**|Samuel Schmidgall et.al.|[2608.26701v1](http://arxiv.org/abs/2608.26701v1)|null|
|**2026-08-26**|**Planetary Prediction Engine: Autonomous Geospatial Prediction via Intelligent Data Selection and Foundation Model Embeddings**|Evelyn Ma et.al.|[2608.26088v1](http://arxiv.org/abs/2608.26088v1)|null|
|**2026-08-26**|**ICON Decomposition: Auditing Deep Neural Networks with Multivariate Variance-based Concept-level Explanations**|Roshan Prakash Rane et.al.|[2608.26083v2](http://arxiv.org/abs/2608.26083v2)|null|
|**2026-08-26**|**A Statistical Audit of Physical AI Benchmark Redundancy**|Zaruhi Navasardyan et.al.|[2608.25940v2](http://arxiv.org/abs/2608.25940v2)|null|
|**2026-08-26**|**Candidate supply and answer selection shape the value of LLM judging in multi-agent systems**|Jia-Hao Ji et.al.|[2608.25937v2](http://arxiv.org/abs/2608.25937v2)|null|
|**2026-08-26**|**TAU-Agent: An Agentic Retrieval-Augmented Framework for Traffic Anomaly Understanding**|Yuqiang Lin et.al.|[2608.25935v1](http://arxiv.org/abs/2608.25935v1)|null|
|**2026-08-26**|**Formal, Executable and Explainable Runtime Monitoring of Spoken Air Traffic Control Operational Procedures**|Roberto Luvini et.al.|[2608.25926v1](http://arxiv.org/abs/2608.25926v1)|null|
|**2026-08-26**|**Towards A Unified Information Bottleneck Framework for Time Series Explanations**|Xu Zheng et.al.|[2608.25897v1](http://arxiv.org/abs/2608.25897v1)|null|
|**2026-08-26**|**Distance Is Not Enough: Forget-Retain Alignment Gap Predicts LLM Relearning Robustness**|Yi Chen et.al.|[2608.25429v1](http://arxiv.org/abs/2608.25429v1)|null|
|**2026-08-26**|**Fairness Invariants: A Relational Approach to Explaining and Mitigating Fairness Bugs**|Ranit Debnath Akash et.al.|[2608.26209v1](http://arxiv.org/abs/2608.26209v1)|null|
|**2026-08-26**|**Refusal geometry reflects refusal training: diverse refusal prefixes can raise stable rank and weaken refusal vector ablation attacks**|Andrey Labunets et.al.|[2608.25390v2](http://arxiv.org/abs/2608.25390v2)|null|
|**2026-08-26**|**A Few Pages of Markdown: Committed AI Configuration and Lower Quality Cost after Coding-Agent Adoption**|Yegor Denisov-Blanch et.al.|[2608.25241v1](http://arxiv.org/abs/2608.25241v1)|null|
|**2026-08-25**|**Self-Explanation Tutor for Active Study of CS1 Worked Examples**|Arun-Balajiee Lekshmi-Narayanan et.al.|[2608.25180v1](http://arxiv.org/abs/2608.25180v1)|null|
|**2026-08-25**|**CareGraph: An Auditable Hybrid AI Framework for Evidence-Grounded Personalized Longitudinal Health Intelligence**|Pratik Ghawate et.al.|[2608.27484v1](http://arxiv.org/abs/2608.27484v1)|null|
|**2026-08-25**|**NVExplain: Explaining Time Series Forecasting with Latent Trajectory Analysis and Structure-Preserving Surrogates**|Muyan Anna Li et.al.|[2608.25080v1](http://arxiv.org/abs/2608.25080v1)|null|
|**2026-08-25**|**HealthBench-Psych: A Mental Health Subset of OpenAI's HealthBench**|Matthew Flathers et.al.|[2608.25071v1](http://arxiv.org/abs/2608.25071v1)|null|
|**2026-08-25**|**A Primer on Computational Semantics for Artificial Intelligence Systems**|Casey Kennington et.al.|[2608.25022v1](http://arxiv.org/abs/2608.25022v1)|null|
|**2026-08-25**|**StrokeGuard: A Multi-Agent Guided System for Prehospital Stroke Assessment**|Wentao Yang et.al.|[2608.24555v1](http://arxiv.org/abs/2608.24555v1)|null|
|**2026-08-25**|**LumiXAI: A Modular Full-Stack Framework for Feature Attribution**|Alfio Ferrara et.al.|[2608.24524v1](http://arxiv.org/abs/2608.24524v1)|null|
|**2026-08-24**|**A Formal Methodological Framework for Auditing Robustness and Fidelity in Explainable AI: From Application to Trust Certification**|Rosa Elysabeth Ralinirina et.al.|[2608.23817v1](http://arxiv.org/abs/2608.23817v1)|null|
|**2026-08-24**|**LUCAID: Agentic Multimodal AI for Lung Cancer Precision Pathology**|Marie-Lisa Eich et.al.|[2608.23803v2](http://arxiv.org/abs/2608.23803v2)|null|

#### Abstracts
##### **Clean Engineering, Unstable Measurement: A Preregistered Reliability Failure of Black-Box LLM Observers on Shared Endpoints**
2609.04198v1 by Haoyaun Zhu, Jie Zhang

Language-model judges now gate training data, score generations, and drive leaderboards. The judge is then a measurement instrument, resting on one rarely stated assumption: the same request, sent to the same model name, reads the same tomorrow. We audited that assumption in two preregistered campaigns with every threshold fixed in advance; neither got past validating its instrument. Across 52,988 audited request attempts, same-window repeat rankings agreed at Spearman 0.400 against a required 0.90, and byte-identical next-day replays agreed at 0.78 against a required 0.99, each time with the execution record at ceiling. Three mechanisms explain the gap: a label-to-meaning mapping that biased readouts as strongly as the signal; candidate gaps seven orders of magnitude below the instrument's own noise floor; and byte-identical inputs returning different rankings, a noise that exact-permutation readouts compound. Neither metric substitution nor sampling repaired it on the tested grid. Preregistered follow-ups bound the problem: waiting did not help on the days sampled (0.805 versus 0.800, replicated over five further days); switching providers did not help (four providers share the floor, medians 0.74 to 0.88, predicted by none of the metadata fields they expose); self-hosting on batch-invariant kernels helped only while the server was quiet; and on constructed errors with known gaps, the readout's separation tracks error type, not size. We distill the evidence into a three-level snapshot-identity ladder, eight design rules, and a reporting checklist; a pilot at roughly 2% of the study's call volume would have exposed both unreachable gates in advance. All results concern externally measured behaviour on shared serving infrastructure. On a shared endpoint, a model name is not a frozen instrument; a preregistered evaluation must measure its instrument before freezing any gate on it.

摘要：語言模型評判現在控制訓練數據、評分生成結果並推動排行榜。評判因此成為一種測量工具，基於一個鮮少被提及的假設：相同的請求，發送到相同的模型名稱，明天的結果會是一樣的。我們在兩個預註冊的活動中審核了這一假設，所有閾值均提前固定；但都未能通過其工具的驗證。在52,988次審核的請求嘗試中，相同窗口的重複排名以0.400的斯皮爾曼相關係數達成一致，而所需的為0.90；而位元組完全相同的次日重播則以0.78達成一致，所需的為0.99，每次執行記錄均達到上限。有三個機制解釋了這一差距：標籤到意義的映射使讀數受到強烈偏見；候選差距低於工具自身噪聲底線七個數量級；以及位元組完全相同的輸入返回不同的排名，這是一種噪聲，精確排列的讀數會加劇這種情況。無論是度量替代還是取樣在測試的網格上都無法修復它。預註冊的後續研究界定了問題：在抽樣的日子裡等待並沒有幫助（0.805對0.800，並在五天內重複）；更換供應商也沒有幫助（四個供應商共享同一底線，中位數為0.74到0.88，無一由他們所公開的元數據字段預測）；在批次不變的內核上自我托管僅在伺服器安靜時有所幫助；而在具有已知差距的構造錯誤上，讀數的分離追蹤錯誤類型，而非大小。我們將證據提煉為三層快照身份梯度、八條設計規則和一份報告檢查表；在研究的呼叫量約2%的試點將提前暴露兩個無法到達的閘門。所有結果均涉及在共享服務基礎設施上外部測量的行為。在共享端點上，模型名稱不是一個固定的工具；預註冊的評估必須在凍結任何閘門之前測量其工具。

##### **A Computationally Feasible Framework for Causal Probabilistic Explanation**
2609.04177v1 by Rafal Urbaniak, Sam Witty, Daniel Waxman, Andy Zane, Poorva Garg, Emily Bunnapradist, Sankaran Vaidyanathan, Jack Feser, Drew Lehe, Eli Bingham

Explaining why a specific outcome occurred, and which inputs deserve the blame or credit, is central to philosophical, scientific, and policy analysis. Existing tools split into two camps. The theory of actual causality (AC) gives principled verdicts, but only for toy-sized models, because computing them requires enumerating counterfactual scenarios. Scalable attribution methods like SHAP (or even causal SHAP) at least partially ignore the causal structure that generated the data, and can give answers that conflict with a careful causal analysis. We close this gap with Probabilistic Causal Impact (PCI).
  PCI builds on actual causality and on Pearl's notions of probability of necessity and sufficiency, but recasts the question of explainability as an estimation problem on a probabilistic causal model that is easily approximated via Monte Carlo. By specifying a distribution over "candidate explanations," a distribution over counterfactual values, and a scoring function, PCI provides tractable, causally grounded, graded explanations, generalizing AC and Pearl's probability of causation as degenerate cases.
  We evaluate PCI in synthetic and real-world examples, spanning consistency checks with AC, scaling experiments, complex continuous-valued dynamical systems, and a real-world deployed causal machine learning model trained on millions of datapoints.

摘要：解釋為什麼特定結果發生，以及哪些輸入應該受到責備或讚譽，對於哲學、科學和政策分析至關重要。現有工具分為兩派。實際因果性理論（AC）給出原則性的判決，但僅限於玩具大小的模型，因為計算它們需要列舉反事實情境。可擴展的歸因方法如SHAP（甚至因果SHAP）至少在某種程度上忽略了生成數據的因果結構，並可能給出與仔細的因果分析相矛盾的答案。我們用概率因果影響（PCI）來填補這一空白。<lb/>  
PCI建立在實際因果性和Pearl的必要性與充分性概率概念之上，但將可解釋性的問題重新表述為在概率因果模型上的估計問題，該模型可以通過蒙特卡羅方法輕鬆近似。通過指定“候選解釋”的分佈、反事實值的分佈和評分函數，PCI提供了可處理的、基於因果的、分級的解釋，將AC和Pearl的因果概率概括為退化情況。<lb/>  
我們在合成和現實世界的例子中評估PCI，涵蓋與AC的一致性檢查、擴展實驗、複雜的連續值動態系統，以及一個在數百萬數據點上訓練的現實世界部署的因果機器學習模型。

##### **Rethinking On-Policy Distillation of Large Language Models II: One Training Example**
2609.04172v1 by Zixuan Fu, Bingxiang He, Yuxin Zuo, Haohuan Huang, Jinqian Zhang, Ruhang Xiao, Cheng Qian, Qinyu Luo, Huan-ang Gao, Yudong Wang, Zhiyuan Liu, Ning Ding, Chaojun Xiao

On-policy distillation (OPD) combines student-generated rollouts with dense token-level supervision from a teacher. Existing work has mainly studied its algorithmic behavior, leaving the role of training data unclear. We examine this role at the data-minimal limit by training on a single query. One-shot OPD keeps improving for hundreds of steps and recovers most of full-data OPD's gain across task domains and model families. We explain this result through the states visited during training and the rate at which the student aligns with the teacher. We measure \emph{state coverage}, the fraction of the states full-data OPD visits that a query set's rollouts reach. A single query already reaches \(71.5\%\), most of it within the first 100 steps. Adding semantically distinct queries raises coverage and validation accuracy together, until 16 queries reach \(98.9\%\) and match full-data training. Yet alignment slows at a similar pace whether OPD trains on one query or the whole dataset, and even a fixed set of states takes hundreds of steps to absorb. OPD is therefore data-overfed but algorithm-starved. Its rollouts quickly expose broad supervision, while the student absorbs that supervision increasingly slowly. The state-coverage result extends to multi-teacher OPD, where 16 semantically diverse queries per domain match full-data MOPD. As a further stress test, content-light templates and off-domain WildChat queries also approach the real-query baseline. Task content and induced state coverage can therefore come apart. We hope these findings direct future work toward the step efficiency of OPD, and prompt a re-examination of the data and the mechanisms behind its recent successes in frontier post-training.

摘要：在政策蒸餾（OPD）中，學生生成的回合與教師的密集標記級監督相結合。現有的研究主要探討其算法行為，對訓練數據的角色尚不明確。我們通過在單一查詢上進行訓練來檢視這一角色，並達到數據最小的極限。一次性 OPD 在數百步內持續改進，並在各個任務領域和模型家族中恢復了大部分全數據 OPD 的增益。我們通過訓練過程中訪問的狀態以及學生與教師對齊的速度來解釋這一結果。我們測量了 \emph{狀態覆蓋率}，即全數據 OPD 訪問的狀態中，查詢集的回合達到的狀態比例。單一查詢已經達到了 \(71.5\%\)，其中大部分在前 100 步內完成。添加語義上不同的查詢會同時提高覆蓋率和驗證準確度，直到 16 個查詢達到 \(98.9\%\) 並匹配全數據訓練。然而，無論 OPD 是在一個查詢上訓練還是整個數據集，對齊的速度都會減緩，即使是一組固定的狀態也需要數百步才能吸收。因此，OPD 是數據過度供應但算法匱乏的。它的回合迅速暴露出廣泛的監督，而學生吸收這種監督的速度卻越來越慢。狀態覆蓋率的結果擴展到多教師 OPD，在每個領域中 16 個語義多樣的查詢匹配全數據 MOPD。作為進一步的壓力測試，內容輕量的模板和非領域的 WildChat 查詢也接近真實查詢基準。因此，任務內容和誘導的狀態覆蓋率可能會分開。我們希望這些發現能指導未來的研究朝向 OPD 的步驟效率，並促使重新檢視數據及其在前沿後訓練中近期成功背後的機制。

##### **Epistemic Warrant for LLM Recommendations: Characterizing the Basis for Reliance When Ground Truth Is Unavailable**
2609.04127v1 by Shai Vardi, João Sedoc

Large language models are increasingly used to support organizational decisions, yet users often lack a principled basis for assessing whether to rely on a specific recommendation. Existing approaches typically evaluate broad model properties, such as reliability, uncertainty, or robustness, or focus on user trust, rather than the underlying basis for relying on an individual recommendation. Adapting theoretical foundations from epistemology, we introduce epistemic warrant, a decision-level construct that characterizes the stability of a model's preference and the scope over which that preference holds. We operationalize this construct through a four-tier reliance certificate for pairwise recommendations, distinguishing among unstable, context-dependent, locally supported, and broadly supported recommendations. We validate the construct using contemporary methodologies: known-groups tests successfully recover expert-prespecified warrant orderings, and stronger warrants systematically align with independent consensus from crowd workers. Furthermore, we demonstrate that epistemic warrant provides information distinct from verbalized confidence and is not readily explained by decision difficulty. Ultimately, this framework offers a theoretically grounded, implementable approach for characterizing the warrant of individual LLM recommendations when objective ground truth is unavailable.

摘要：大型語言模型越來越多地被用來支持組織決策，然而用戶往往缺乏一個原則性的基礎來評估是否依賴於特定的建議。現有的方法通常評估廣泛的模型屬性，如可靠性、不確定性或穩健性，或專注於用戶信任，而不是依賴於個別建議的基礎。通過調整來自認識論的理論基礎，我們引入了認識論擔保，這是一個決策層級的構念，描述了模型偏好的穩定性及該偏好適用的範圍。我們通過一個四層次的依賴證書來操作化這一構念，用於成對建議，區分不穩定的、依賴上下文的、本地支持的和廣泛支持的建議。我們使用當代方法論來驗證這一構念：已知群體測試成功恢復專家預先指定的擔保排序，且更強的擔保與來自群眾工作者的獨立共識系統性對齊。此外，我們證明認識論擔保提供的信息與口頭表達的信心不同，並且不易用決策困難來解釋。最終，這一框架提供了一種理論上有根據且可實施的方法，用於描述在客觀真實性不可用時個別LLM建議的擔保。

##### **Why Gated DeltaNet Survives 4-Bit Quantization: NVFP4 W4A4 for the Recurrent Half of a Hybrid 27B LLM**
2609.04098v1 by Sergii Kozyrev, Davyd Maiboroda

Hybrid LLMs pair softmax attention with linear-attention layers such as Gated DeltaNet (GDN), whose recurrent state summarizes the context in fixed size. Early community 4-bit quantizations of Qwen3.8-27B (48 GDN layers, 16 attention layers) left the GDN block in 8- or 16-bit precision -- especially its decay and write-strength gates -- on the intuition that errors in a recurrence accumulate over long contexts. We test that intuition by building Minima: NVFP4 W4A4 on all 496 linear layers, GDN included. Across perplexity at 4K/32K, MMLU-Pro, GSM8K, AIME'25, GPQA-Diamond, LiveCodeBench, and RULER retrieval to 64K, Minima matches BF16 within seed noise (5-task average -0.52) while being the smallest (17.5 GiB) and fastest-prefill (+14-19%) recipe we compare, and its 32K perplexity gap shrinks with position. A four-part mechanism study explains why: (i) NVFP4's 16-element block scaling localizes the residual stream's extreme outliers, equalizing activation error across layer roles; (ii) the supposedly fragile gate projections are the least sensitive -- softplus/exponential and sigmoid parameterizations compress ~11% GEMM error to ~2% output error; (iii) the delta-rule recurrence holds injected noise at a flat plateau over 32K tokens and forgets a state impulse within hundreds of steps, because each write overwrites the state along the current key direction; (iv) the per-token quantization cost washes out with context instead of compounding. We also repair a global-scale mismatch that arises when per-module-calibrated NVFP4 checkpoints are served by kernels that fuse those modules into one GEMM, and show calibrated FP8 KV-cache scales are performance-free. The result: a practical recipe -- quantize everything, ship KV scales -- and a mechanistic account of why the recurrent half of a hybrid LLM is the easy half to quantize. Checkpoint: https://huggingface.co/minima-ai/mnma_qwen3.8_27b_nvfp4

摘要：混合 LLM 將 softmax 注意力與線性注意力層結合，例如 Gated DeltaNet (GDN)，其循環狀態以固定大小總結上下文。早期社群對 Qwen3.8-27B (48 個 GDN 層，16 個注意力層) 的 4 位量化將 GDN 模塊保留在 8 位或 16 位精度上——特別是其衰減和寫入強度閘——基於這樣的直覺：在長上下文中，循環中的錯誤會累積。我們通過構建 Minima：NVFP4 W4A4 在所有 496 個線性層上進行測試，包括 GDN。在 4K/32K 的困惑度、MMLU-Pro、GSM8K、AIME'25、GPQA-Diamond、LiveCodeBench 和 RULER 檢索到 64K 的情況下，Minima 在種子噪音內與 BF16 相匹配 (5 任務平均 -0.52)，同時是我們比較中最小的 (17.5 GiB) 和最快的預填 (+14-19%) 配方，其 32K 的困惑度差距隨位置縮小。一項四部分的機制研究解釋了原因：(i) NVFP4 的 16 元素塊縮放將殘差流的極端異常本地化，使層角色之間的激活錯誤平衡；(ii) 所謂脆弱的閘投影是最不敏感的——softplus/exponential 和 sigmoid 參數化將約 11% 的 GEMM 錯誤壓縮至約 2% 的輸出錯誤；(iii) 三角規則循環在 32K 令牌上保持注入噪音在平坦的平臺上，並在數百步內忘記狀態脈衝，因為每次寫入都沿著當前鍵方向覆蓋狀態；(iv) 每個令牌的量化成本隨著上下文而消失，而不是累加。我們還修復了一個全球範圍的錯配，當每個模塊校準的 NVFP4 檢查點由將這些模塊融合為一個 GEMM 的內核提供時出現，並顯示校準的 FP8 KV-cache 縮放是無性能成本的。結果：一個實用的配方——量化所有內容，發送 KV 縮放——以及一個機制性解釋為何混合 LLM 的循環部分是最容易量化的部分。檢查點：https://huggingface.co/minima-ai/mnma_qwen3.8_27b_nvfp4

##### **Witnesses Explain Anomalies**
2609.03826v1 by Lamine Diop

Unsupervised anomaly detection scores each point of an unlabelled, contaminated sample in a single pass, and increasingly must also explain why a point is flagged. Yet the dominant detectors give a score with no account of which features drive it, and explanations are bolted on post-hoc with SHAP or LIME, which re-query the detector thousands of times per point and only approximate it. We introduce WAND, an unsupervised tabular anomaly detector that is explainable by design. WAND organises its computation around directions on the unit sphere, scoring each point by how far its projection escapes a sub-Gaussian extreme-value baseline. The originality of our approach is that the witness directions that flag a point, being vectors in feature space, are its explanation, a per-feature attribution obtained at no cost over scoring and, since the score is differentiable, recoverable by gradients. Scoring is linear in the sample size, and a probe-efficiency bound guarantees every anomaly a witness, hence an explanation. Across 47 ADBench datasets WAND attains the best mean Friedman rank at ROC-AUC parity with 16 unsupervised baselines, so the gain is interpretability at no accuracy cost; its native explanations are more accurate and faithful than post-hoc SHAP/LIME and ECOD at a fraction of the query cost. WAND is thus a practical, interpretable solution for explainable anomaly detection.

摘要：無監督異常檢測在單次通過中對未標記的受污染樣本的每個點進行評分，並且越來越需要解釋為何某個點被標記。然而，主流檢測器給出的評分並未考慮到驅動評分的特徵，而解釋則是事後附加的，使用SHAP或LIME，這些方法對每個點重新查詢檢測器數千次，並且僅能近似。 我們引入了WAND，一種設計上可解釋的無監督表格異常檢測器。WAND圍繞單位球上的方向組織其計算，根據每個點的投影逃離亞高斯極值基線的距離來評分。 我們方法的創新之處在於，標記某個點的見證方向作為特徵空間中的向量，便是其解釋，這是一種在計分上無成本的每特徵歸因，並且由於評分是可微的，能夠通過梯度恢復。 計分在樣本大小上是線性的，探測效率界限保證每個異常都有見證，因此有解釋。在47個ADBench數據集上，WAND在與16個無監督基準平行的ROC-AUC下達到了最佳的平均Friedman排名，因此獲得的是可解釋性而不損失準確性；其原生解釋比事後的SHAP/LIME和ECOD更準確且更忠實，且查詢成本僅為其一小部分。因此，WAND是一種實用的、可解釋的可解釋異常檢測解決方案。

##### **Cross-Dataset Transfer and Reliability of Explainable Artificial Intelligence for RhythmFormer Remote Photoplethysmography**
2609.03663v1 by Louis Chen, Torbjörn E. M. Nordling

Background. Remote photoplethysmography estimates the cardiovascular pulse from facial video, and its explanations have rested on inspecting heatmaps rather than on quantitative evidence about where a model reads it. We quantified the explanations and asked whether such explanations transfer between datasets and track model performance. Method. We trained eight condition-specific RhythmFormer models on NCKU-rPPG, recorded under three illumination levels, speaking, rotation, and cycling, estimated one heart rate per 5.12-second clip, and set them beside a UBFC-rPPG reproduction. Raw attention, rollout, attention flow, and Beyond Intuition were assessed by skin coverage and the Salience-guided Faithfulness Coefficient (SaCo). Results. Beyond Intuition ranked highest on both datasets, at median coverage 0.789 and SaCo 0.837 on Static level 3 against 0.826 and 0.917 on UBFC-rPPG; lower ranks differed. Within one participant of one condition, neither measure was related to a clip's heart-rate error, waveform correlation, or signal-to-noise ratio on either dataset: 186 of the 252 coefficients fell below $|ρ|=0.10$ and 28 reached $p<0.05$ against the 13 expected by chance. Across the eight scenarios only Beyond Intuition's coverage followed the three performance measures, at $ρ=-0.43$, $+0.57$, and $+0.43$, while the attention-only methods' SaCo ran opposite to each. It failed at 40 lux alone, its median coverage falling to 0.180 and its median SaCo to $-0.178$, whereas motion degraded the estimates far more without such a drop. Conclusions. Skin coverage and SaCo carry information complementary to the performance measures rather than a proxy for them: attributing to the skin does not guarantee an accurate estimate. What an attribution reveals about a condition is where the model looks rather than how faithfully its map is ordered.

摘要：背景。遠程光電容積描記法從面部視頻估算心血管脈搏，其解釋依賴於檢查熱圖，而非關於模型讀取位置的定量證據。我們量化了這些解釋，並詢問這些解釋是否能在數據集之間轉移並追踪模型性能。方法。我們在NCKU-rPPG上訓練了八個特定條件的RhythmFormer模型，該模型在三個照明水平下錄製，包括說話、旋轉和騎自行車，估算每個5.12秒的片段一個心率，並將其與UBFC-rPPG的重現進行比較。通過皮膚覆蓋率和以顯著性引導的真實性係數（SaCo）評估原始注意力、回放、注意力流和超越直覺的表現。結果。超越直覺在兩個數據集上的排名最高，在靜態水平3的中位數覆蓋率為0.789，SaCo為0.837，而在UBFC-rPPG上分別為0.826和0.917；較低的排名有所不同。在一名參與者的單一條件下，無論是何種測量都與片段的心率誤差、波形相關性或信噪比無關：252個係數中有186個低於$|ρ|=0.10$，28個達到$p&lt;0.05$，而預期的僅有13個。在八種情境中，只有超越直覺的覆蓋率跟隨了三個性能指標，分別為$ρ=-0.43$、$+0.57$和$+0.43$，而僅注意力方法的SaCo則與之相反。它在40 lux的情況下失敗，其中位數覆蓋率降至0.180，中位數SaCo降至$-0.178$，而運動則在沒有這種下降的情況下大幅降低了估算。結論。皮膚覆蓋率和SaCo攜帶的信息是對性能指標的補充，而非其代理：歸因於皮膚並不保證準確的估算。歸因所揭示的條件是模型的觀察位置，而非其地圖的秩序忠實度。

##### **CulturalMenuBench: Probing the Knowledge-Application Gap in Multimodal Culinary Reasoning**
2609.03526v1 by Bo Zeng, Linfeng Gao, Peiqin Lin, Yu Zhao, Mingyan Zeng, Yu Tong, Xintong Wang, Linlong Xu, Longyue Wang, Weihua Luo, Qinggang Zhang, Jinsong Su

Multimodal language models achieve near-ceiling scores on food recognition benchmarks, yet it remains unclear whether this success reflects genuine cultural understanding or mere visual matching. To probe this distinction, we introduce CulturalMenuBench, a benchmark of 4,870 items in 10 languages across 18 regions; its 10 tasks pair final-dish and step-by-step cooking images with ingredients, procedural text, and regional labels, spanning basic recognition to process-grounded cultural attribution. Evaluating 12 models exposes a substantial knowledge-application gap: models exceeding 94% on standard multiple-choice tasks drop to at most 56% when attributing dishes to Chinese regional cuisines, despite an identical four-way format. Diagnostic analyses explain why: error patterns are consistent with random guessing, accuracy tracks visual distinctiveness rather than cultural structure, and models classify cuisines more accurately from dish names alone than from images (+7-18 points). The knowledge is thus present but cannot be activated through visual input. An ablation confirms these tasks genuinely require procedural evidence: removing sequential cooking images selectively degrades process-grounded tasks while others remain stable. Overall, CulturalMenuBench shows that near-perfect recognition can conceal an inability to apply cultural knowledge, motivating training that explicitly connects perception, procedure, and cultural context. Code and data are publicly available.

摘要：多模態語言模型在食品識別基準測試中達到了接近上限的分數，但目前尚不清楚這一成功是否反映了真正的文化理解，還是僅僅是視覺匹配。為了探究這一區別，我們推出了CulturalMenuBench，一個包含4,870個項目、涵蓋10種語言和18個地區的基準；其10個任務將最終菜餚和逐步烹飪圖像與食材、程序文本和地區標籤配對，範圍涵蓋基本識別到基於過程的文化歸屬。對12個模型的評估揭示了顯著的知識應用差距：在標準多選任務中超過94%的模型在將菜餚歸屬於中國地方菜時最多僅能達到56%，儘管格式完全相同。診斷分析解釋了原因：錯誤模式與隨機猜測一致，準確性跟蹤視覺特徵而非文化結構，並且模型僅從菜名進行分類的準確性比從圖像中更高（+7-18分）。因此，知識是存在的，但無法通過視覺輸入啟動。一項消融實驗確認這些任務確實需要程序證據：去除順序烹飪圖像會選擇性地降低基於過程的任務的表現，而其他任務則保持穩定。總體而言，CulturalMenuBench顯示出近乎完美的識別可能掩蓋了應用文化知識的能力，促使訓練明確連接感知、程序和文化背景。代碼和數據已公開可用。

##### **Towards Trustworthy Autonomous Robots: An Explainable AI-Based Decision Framework**
2609.02861v1 by Cagri Temel

Autonomous robots powered by deep learning face a fundamental auditability challenge: when incidents occur, investigators cannot reconstruct why the system made specific decisions. This paper presents TRACE (Transparent Reasoning Architecture for Credible Execution), a decision framework that ensures every autonomous action can be traced back to sensor evidence through documented causal chains. The framework organizes decision-making into four auditable layers: Semantic Perception for evidence-grounded entity recognition, Belief Reasoning for probabilistic state estimation with causal graphs, Action Synthesis for constraint-aware planning with counterfactual documentation, and Execution Verification for compliance monitoring. TRACE is model-agnostic yet designed to integrate learning-based perception modules (CNNs, transformers) while preserving decision-level auditability. We evaluate the framework using three objective metrics: Evidence Traceability (sensor-to-decision linkage), Decision Reconstructability (post-hoc analysis capability), and Temporal Continuity (audit trail completeness). Experimental evaluation on warehouse robot navigation demonstrates that TRACE achieves 98.6% evidence traceability, 99.0% temporal continuity, and 98.1% decision reconstructability across 500 simulated decision cycles. Post-hoc methods like LIME provide feature attributions but lack the artifact structure needed for decision-level reconstruction. The framework addresses EU AI Act requirements for high-risk system transparency and contributes to Explainable AI for safety-critical autonomous systems.

摘要：自主機器人由深度學習驅動，面臨一個基本的可審計性挑戰：當事件發生時，調查人員無法重建系統為何做出特定決策的原因。本文提出了TRACE（透明推理架構以確保可信執行），這是一個決策框架，確保每個自主行動都可以通過記錄的因果鏈追溯到傳感器證據。該框架將決策過程組織為四個可審計的層次：語義感知用於基於證據的實體識別，信念推理用於帶有因果圖的概率狀態估計，行動合成用於帶有反事實文檔的約束感知規劃，以及執行驗證用於合規監控。TRACE是模型無關的，但設計上旨在整合基於學習的感知模塊（CNNs、Transformer），同時保持決策級的可審計性。我們使用三個客觀指標來評估該框架：證據可追溯性（傳感器到決策的連結）、決策可重建性（事後分析能力）和時間連續性（審計跟蹤的完整性）。在倉庫機器人導航的實驗評估中，TRACE在500個模擬決策循環中實現了98.6%的證據可追溯性、99.0%的時間連續性和98.1%的決策可重建性。事後方法如LIME提供特徵歸因，但缺乏決策級重建所需的工件結構。該框架滿足歐盟人工智慧法案對高風險系統透明度的要求，並為安全關鍵的自主系統貢獻了解釋性人工智慧。

##### **HiPoly: a hierarchical polymer-native AI framework for property prediction and generative design**
2609.02746v1 by Ge Sun, Gervasio Zaldivar, Yuan Tian, Gustavo Perez Lemus, Juhae Park, Dasha Safarian, Ming Han, Juan J. de Pablo

Polymeric materials are central to modern technologies, with applications ranging from energy to health and transportation. Although AI has made significant advances in materials discovery, the hierarchical structure of polymers across multiple length scales makes them inherently difficult to represent in a unified and physically meaningful way. Here we introduce HiPoly, a polymer-native AI framework that processes complete polymer descriptions through a three-level hierarchical graph architecture built on the G2RINS representation. HiPoly encodes stochastic inter-monomer connectivity, composition, and molecular weight directly within its architecture, using physically motivated design principles that mirror the multi-scale nature of polymeric systems. The framework establishes an end-to-end AI-driven workflow from experimental formulation data to property prediction, generative molecular design, and physics-based validation through molecular simulations, all unified by a single polymer representation. We demonstrate state-of-the-art prediction accuracy for thermophysical properties of multi-component polymer systems, with ablation studies confirming that each hierarchical design choice contributes independently to model performance. As an example, the generative design pathway is applied here to the discovery of sustainable alternatives to persistent fluorinated polymers, where it is possible to identify and independently validate PFAS-free candidates with target surface-energy properties. This work demonstrates how polymer-native AI can accelerate discovery by linking representation, prediction, and design across complex polymer chemistries.

摘要：聚合材料是現代技術的核心，應用範圍從能源到健康和交通。儘管人工智慧在材料發現方面取得了重大進展，但聚合物在多個長度尺度上的層次結構使其本質上難以以統一且物理上有意義的方式進行表示。在此，我們介紹 HiPoly，一個聚合物原生的人工智慧框架，通過基於 G2RINS 表示的三層層次圖架構處理完整的聚合物描述。HiPoly 在其架構中直接編碼隨機的單體間連接性、組成和分子量，使用反映聚合物系統多尺度特性的物理驅動設計原則。該框架建立了一個端到端的人工智慧驅動工作流程，從實驗配方數據到性質預測、生成分子設計及通過分子模擬的基於物理的驗證，所有這些都由單一的聚合物表示統一。我們展示了對多組分聚合物系統的熱物理性質的最先進預測準確性，消融研究確認每個層次設計選擇獨立地對模型性能作出貢獻。作為一個例子，這裡應用生成設計途徑來發現持久氟化聚合物的可持續替代品，在這裡可以識別並獨立驗證具有目標表面能特性的無 PFAS 候選者。這項工作展示了聚合物原生人工智慧如何通過連結表示、預測和設計來加速發現，跨越複雜的聚合物化學。

##### **Contrastive Explanations in Quantitative Bipolar Argumentation Frameworks**
2609.02399v1 by Xiang Yin, Nico Potyka, Antonio Rago, Francesca Toni

Argumentation frameworks are useful tools for representing and reasoning with information in a variety of settings, e.g. in supplementing AI models as they perform classification tasks, with a notable benefit of providing additional explainability. In this paper, we introduce contrastive explanations for Quantitative Bipolar Argumentation Frameworks (QBAFs), one such formalism. Unlike most existing explanations for QBAFs, which explain the reasoning outcome of a single argument of interest (i.e. a topic argument), contrastive explanations explain the difference between two topic arguments. We introduce a general form of contrastive attribution functions (CAFs) and establish a set of general properties they should satisfy. We introduce CAFs based on removal, gradients and Shapley-values, and study their properties. Finally, to illustrate contrastive explanations, we demonstrate their usefulness in healthcare and bias identification settings.

摘要：論證框架是用於在各種環境中表示和推理信息的有用工具，例如在輔助人工智慧模型執行分類任務時，提供額外可解釋性的顯著好處。在本文中，我們介紹了量化雙極論證框架（QBAFs）的對比解釋，這是一種此類形式主義。與大多數現有的QBAFs解釋不同，後者解釋單一感興趣論點（即主題論點）的推理結果，對比解釋則解釋兩個主題論點之間的差異。我們介紹了一般形式的對比歸因函數（CAFs），並建立了一組它們應滿足的一般性質。我們基於移除、梯度和Shapley值介紹CAFs，並研究它們的性質。最後，為了說明對比解釋，我們展示了它們在醫療保健和偏見識別環境中的有用性。

##### **Do Large Language Models Capture the Diversity in their Training Data?**
2609.02275v1 by Youqi Wu, Farzan Farnia

Large language models are trained to model conditional distributions over text, yet it remains inadequately understood whether they capture the full diversity of plausible outputs present in their training data. We study this question through an information-theoretic lens by comparing the conditional entropy of model-generated outputs with that of the corresponding training data. Given paired input-output samples, we use conditional entropy and its matrix-based analogue based on von Neumann entropy to measure output variability beyond what is explained by the conditioning input, without requiring multiple reference outputs for the same prompt. Across LLM families with publicly available training data, including OLMo, Pythia, and GPT-Neo, we consistently find that model-generated outputs exhibit lower conditional entropy than their training data, across different model scales, sequence lengths, and decoding strategies. We observe a similar conditional diversity gap beyond language modeling, including class-conditioned ImageNet generators and text-conditioned models trained on MS-COCO. To address this gap, we propose a post-hoc correction mechanism that generates multiple outputs for each input and reweights them through a matrix-entropy projection, increasing conditional diversity while remaining close to the original model distribution. We prove the concavity of the matrix-based conditional entropy functional, which makes the resulting entropy-constrained projection a convex optimization problem, and develop a scalable mirror-descent algorithm for its implementation. Our results reveal a systematic conditional diversity gap between modern generative models and their training data, and provide an information-theoretic framework for measuring and mitigating this gap.

摘要：大型語言模型被訓練用來建模文本的條件分佈，但目前尚不清楚它們是否捕捉到訓練數據中存在的合理輸出全貌。<lb/>我們通過信息論的視角來研究這個問題，通過比較模型生成輸出的條件熵與相應訓練數據的條件熵。<lb/>給定成對的輸入-輸出樣本，我們使用條件熵及其基於冯·诺依曼熵的矩陣類比來測量超出條件輸入所解釋的輸出變異性，而不需要對於同一提示的多個參考輸出。<lb/>在包括OLMo、Pythia和GPT-Neo在內的具有公開可用訓練數據的LLM家族中，我們一致發現模型生成的輸出在不同模型規模、序列長度和解碼策略下，其條件熵均低於其訓練數據。<lb/>我們觀察到一個類似的條件多樣性差距，超出了語言建模的範疇，包括類別條件的ImageNet生成器和在MS-COCO上訓練的文本條件模型。<lb/>為了解決這一差距，我們提出了一種事後修正機制，為每個輸入生成多個輸出，並通過矩陣熵投影對其進行重加權，從而增加條件多樣性，同時保持接近原始模型分佈。<lb/>我們證明了基於矩陣的條件熵泛函的凹性，這使得結果的熵約束投影成為一個凸優化問題，並開發了一種可擴展的鏡像下降算法以實現其應用。<lb/>我們的結果揭示了現代生成模型與其訓練數據之間系統性的條件多樣性差距，並提供了一個信息論框架來測量和減輕這一差距。

##### **Retrosynthesis of Synthetic Media for Explainable AI Provenance Forensics**
2609.02268v1 by Yijie Lin, Ching-Chun Chang, Isao Echizen, Hui Li, Chin-Chen Chang

With the rapid proliferation of generative models on Machine Learning as a Service (MLaaS) platforms, reliably tracing the provenance of synthetic media without modifying generator architectures or parameters remains a major challenge. In this work, we propose a self-referential retrosynthesis framework for explainable AI provenance forensics under a fixed-generator setting. The framework leverages a jointly optimized encoder-decoder pair to implement a self-embedding mechanism that enables round-trip consistency verification. During inference, client inputs are first encoded and then processed by the generator to produce outputs with high visual fidelity. For forensic verification, the consistency between the resynthesized image and the query image is analyzed to determine whether the image originates from the target generative model. Our approach eliminates the need for watermark embedding or modifications to the generation process. Experimental results show that images generated from encoded inputs maintain visual quality comparable to original generator outputs, while decoded images reliably trace back to their corresponding source inputs. Furthermore, the framework provides interpretable evidence for generative content provenance, establishing a practical tool for explainable generative AI forensics.

摘要：隨著生成模型在機器學習即服務（MLaaS）平台上的快速擴散，可靠地追踪合成媒體的來源而不修改生成器架構或參數仍然是一個主要挑戰。<lb/>在這項工作中，我們提出了一個自參考的逆合成框架，用於固定生成器設置下的可解釋人工智慧來源取證。<lb/>該框架利用一對聯合優化的編碼器-解碼器來實現自嵌入機制，從而實現往返一致性驗證。<lb/>在推理過程中，客戶端輸入首先被編碼，然後由生成器處理以產生高視覺保真度的輸出。<lb/>對於取證驗證，分析重新合成的圖像與查詢圖像之間的一致性，以確定該圖像是否來自目標生成模型。<lb/>我們的方法消除了對水印嵌入或生成過程修改的需求。<lb/>實驗結果顯示，從編碼輸入生成的圖像在視覺質量上與原始生成器輸出相當，而解碼圖像可靠地追溯到其相應的源輸入。<lb/>此外，該框架提供了可解釋的證據，以支持生成內容的來源，為可解釋的生成人工智慧取證建立了一個實用工具。

##### **DiffuSearch: How Hybrid Trajectory Planning Benefits from Aligned Objectives in Diffusion and Action Space**
2609.02252v1 by Steffen Hagedorn, Aron Distelzweig, Alexandru P. Condurache

In trajectory planning for autonomous driving, hybrid planning architectures are often realized as a collection of disparate modules, each with its own objectives. This lack of a unifying principle can lead to inconsistencies between the initial and refined trajectory, resulting in suboptimal behavior. We address this by introducing DiffuSearch, a novel hybrid planner that uses a unified set of objectives across generation and refinement. Our model encourages all components to follow the same shared driving goals: collision avoidance, drivable area compliance, comfort, and progress. DiffuSearch employs a two-stage architecture. First, a guided diffusion model generates a scene-consistent, joint trajectory prediction, using our driving objectives as differentiable guidance functions to implicitly steer the denoising process. Second, a Monte Carlo Tree Search (MCTS) in a discretized action space performs an explicit, local refinement of this proposal, leveraging the same driving objectives as its reward function. This synergistic design leverages the diffusion model's strength in finding scene-consistent solutions combined with the explainable, constraint-aware refinement of MCTS. Experiments on nuPlan and interPlan reactive closed-loop benchmarks demonstrate that DiffuSearch achieves strong and often state-of-the-art performance, substantially reducing collisions and improving comfort, particularly in complex, interactive scenarios. Our ablation studies indicate that MCTS refinement is the main mechanism behind the gains, while sharing objectives between implicit guidance and explicit search provides further consistent improvements.

摘要：在自主駕駛的軌跡規劃中，混合規劃架構通常被實現為一組不同的模塊，每個模塊都有其自己的目標。這種缺乏統一原則的情況可能導致初始軌跡和精煉軌跡之間的不一致，從而導致次優行為。我們通過引入DiffuSearch來解決這個問題，這是一種新穎的混合規劃器，使用統一的目標集來進行生成和精煉。我們的模型鼓勵所有組件遵循相同的共享駕駛目標：避免碰撞、符合可駕駛區域、舒適性和進展。DiffuSearch採用兩階段架構。首先，指導擴散模型生成場景一致的聯合軌跡預測，使用我們的駕駛目標作為可微分的指導函數，以隱式地引導去噪過程。其次，在離散化的動作空間中，蒙特卡羅樹搜索（MCTS）對這一提案進行明確的局部精煉，利用相同的駕駛目標作為其獎勵函數。這種協同設計利用了擴散模型在尋找場景一致解決方案方面的優勢，結合了MCTS的可解釋性和約束感知的精煉。在nuPlan和interPlan反應閉環基準上的實驗表明，DiffuSearch實現了強大且往往是最先進的性能，顯著減少碰撞並改善舒適性，特別是在複雜的互動場景中。我們的消融研究表明，MCTS精煉是增益背後的主要機制，而在隱式指導和明確搜索之間共享目標則提供了進一步一致的改進。

##### **Propose to Learn, Learn to Propose: Evaluability-Aware Assistance under Bounded Rationality**
2609.02242v1 by Yifan Zhu, Sammie Katt, Samuel Kaski

AI assistants often collaborate by proposing candidate edits, plans, or designs that users evaluate before adoption. Existing assistance methods focus on proposal quality or user-goal inference, often assuming that the user can reliably evaluate any proposal, which can fail in practice because of bounded rationality. We study evaluability-aware proposal planning, where proposals serve both as task interventions and as probes for learning latent preferences and evaluation constraints, where the resulting belief updates then guide later proposals. We formalise this setting as ProSE, a hidden-parameter sequential assistance problem, and instantiate it with a KL-regularised bounded-rational binary response model in which acceptance trades off value gain against a distance-dependent evaluability penalty. Analysing the planning consequence of this likelihood reveals that likely accepted proposals and informative probes need not coincide, which explains why planners that only pursue acceptance systematically underperform. We operationalise ProSE with \textsc{ProSE-Plan}, a depth-2 Bayes-adaptive planner that scores proposals by possible responses and response-induced posterior beliefs. In controlled graph simulations, \textsc{ProSE-Plan} improves over evaluability-unaware and myopic baselines when evaluation cost is the bottleneck, and a probe-commit ablation confirms that our approach selects informative proposals that simpler methods miss. Our results thus identify user evaluability as a planning-relevant dimension of AI assistance, complementary to generation quality and preference inference.

摘要：AI 助手經常通過提出候選編輯、計劃或設計來進行協作，這些提案需要用戶在採納之前進行評估。現有的輔助方法專注於提案質量或用戶目標推斷，通常假設用戶能夠可靠地評估任何提案，但在實踐中，由於有限理性，這種假設可能會失效。我們研究了可評估性意識的提案規劃，其中提案既作為任務干預，又作為學習潛在偏好和評估約束的探針，從而使得結果的信念更新能夠指導後續的提案。我們將這一設定形式化為 ProSE，一個隱藏參數的序列輔助問題，並用 KL 正則化的有限理性二元反應模型來具體化，其中接受與價值增益之間的權衡取決於距離的可評估性懲罰。分析這一似然的規劃後果顯示，可能被接受的提案和信息豐富的探針不必重合，這解釋了為什麼僅追求接受的規劃者系統性表現不佳。我們用 \textsc{ProSE-Plan} 操作化 ProSE，這是一個深度為 2 的貝葉斯自適應規劃器，通過可能的反應和反應引起的後驗信念來評分提案。在受控的圖形模擬中，\textsc{ProSE-Plan} 在評估成本成為瓶頸時，相比於不考慮可評估性和短視基準有了改進，而探針-承諾的消融實驗證實我們的方法選擇了簡單方法所忽略的信息豐富的提案。因此，我們的結果確定了用戶可評估性作為 AI 輔助的規劃相關維度，這與生成質量和偏好推斷互為補充。

##### **Examining the Vulnerability of Multi-Agent Medical Systems to Human Interventions for Clinical Reasoning**
2609.02191v1 by Benjamin C Liu, Dillon Mehta, Rishi Malhotra, Adam Zobian, Yong Ying Tan, Samir Chopra, Daniella Rand, Natalie Pang, Abhiram Gudimella, Kevin Zhu

Human interventions at fault points can alter the diagnostic accuracy of multi-agent medical systems. We defined fault points as moments in AI agent conversations, in which an agent's reasoning became most vulnerable to external influence. Using the MedQA dataset, this study analyzed simulated doctor-patient conversations to measure how interventions shifted reasoning and accuracy. Correct intervention methods showed an improvement in baseline diagnostic accuracy of up to 40%, while incorrect or bias-related interventions degraded performance by up to 6% and increased diagnostic drift and uncertainty. Beyond performance changes, our analysis revealed behavioral similarities between cognitive biases in simulated agent environments and real-world clinical practice. Examples included premature closure and susceptibility to misleading cues. Overall, these findings demonstrate that identifying and guiding fault points with human interventions may provide a mechanism for improving diagnostic robustness in multi-agent medical systems.

摘要：人類在故障點的介入可以改變多代理醫療系統的診斷準確性。<lb/>我們將故障點定義為AI代理對話中的時刻，在這些時刻，代理的推理最容易受到外部影響。<lb/>本研究使用MedQA數據集分析模擬的醫生-病人對話，以測量介入如何改變推理和準確性。<lb/>正確的介入方法顯示出基線診斷準確性提高了多達40%，而不正確或與偏見相關的介入則使性能下降了多達6%，並增加了診斷漂移和不確定性。<lb/>除了性能變化，我們的分析還揭示了模擬代理環境中的認知偏見與現實世界臨床實踐之間的行為相似性。<lb/>例子包括過早關閉和對誤導性線索的易感性。<lb/>總體而言，這些發現表明，通過人類介入識別和引導故障點可能提供改善多代理醫療系統診斷穩健性的一種機制。

##### **A Power Law in Logarithm's Clothing: On the Scalability of Graph-Based Vector Search**
2609.02143v1 by Sajad Faghfoor Maghrebi, Navid Eslami, Niv Dayan

Most vector databases rely on graph-based indexes, notably HNSW and Vamana, for approximate nearest neighbor search. With embedding models widely adopted, the datasets these databases store grow rapidly. At a fixed accuracy, how does search cost scale with dataset size? The prevailing answer is poly-logarithmic growth. Yet the claim is proven only under special conditions and asserted without proof for the indexes used in practice. It is also largely untested: standard benchmarks measure cost at one dataset size, not across sizes. We put the claim to the test. The answer depends on the scale itself. While the dataset size $N$ is small relative to the data's intrinsic dimensionality, search cost grows as $N^c$ for a constant $0<c<1$. We call this scaling the Sublinear Power Law. Once $N$ is large enough, growth slows to subpolynomial, consistent with the poly-logarithmic claim. The Sublinear Power Law appears on every dataset, mostly up to its full size, at every recall target, query hardness level, and index configuration we test. The transition to subpolynomial growth appears on the two datasets that grow large enough relative to their intrinsic dimensionality. One mechanism underlies both behaviors: a dataset's intrinsic dimensionality grows with its size until the data resolves its underlying distribution. Higher intrinsic dimensionality packs more vectors into the query neighborhood the search must examine. We present a unifying theory of beam-search cost that explains our observations. For exact and bounded-degree constructions, we prove the Sublinear Power Law and the eventual transition to poly-logarithmic scaling, and derive the scale at which it occurs. We also develop models that predict the power-law exponents for any recall target and index configuration. These models give a principled way to navigate trade-offs among search cost, insertion cost, and recall as data grows.

摘要：大多數向量資料庫依賴於基於圖形的索引，特別是 HNSW 和 Vamana，用於近似最近鄰搜索。隨著嵌入模型的廣泛採用，這些資料庫儲存的數據集迅速增長。在固定的準確度下，搜索成本如何隨著數據集大小的增長而變化？普遍的答案是多對數增長。然而，這一說法僅在特殊條件下得到證明，且對實際使用的索引未提供證明。這一說法也在很大程度上未經測試：標準基準測試在一個數據集大小下測量成本，而不是跨越不同大小。我們對這一說法進行了測試。答案取決於規模本身。當數據集大小 $N$ 相對於數據的內在維度較小時，搜索成本隨著 $N^c$ 增長，其中常數滿足 $0&lt;c&lt;1$。我們稱這種增長為次線性冪律。一旦 $N$ 足夠大，增長速度會減緩至次多項式，這與多對數的說法一致。次線性冪律在每個數據集上都出現，主要是在其完整大小之前，並且在我們測試的每個召回目標、查詢難度級別和索引配置下均如此。轉變為次多項式增長出現在兩個相對於其內在維度增長足夠大的數據集上。這兩種行為背後有一個共同的機制：數據集的內在維度隨著其大小增長，直到數據解析其潛在分佈。更高的內在維度將更多的向量包裝進查詢鄰域中，搜索必須檢查這些向量。我們提出了一個統一的束搜索成本理論，解釋了我們的觀察結果。對於精確和有界度的構造，我們證明了次線性冪律及最終轉變為多對數增長，並推導出其發生的規模。我們還開發了模型，預測任何召回目標和索引配置的冪律指數。這些模型提供了一種原則性的方法，以在數據增長過程中導航搜索成本、插入成本和召回之間的權衡。

##### **Disease Burden over Skin Tone: Decomposing the Dermatology-AI Generalization Gap**
2609.02111v1 by Nirajan Kunwor, Sanjaya Poudel, Quoc-Huy Trinh, Jahidul Arafat, Sunil Kumar Gaire

Dermatology artificial intelligence (AI) models are predominantly trained on light-skinned, cancer-focused image collections, yet they are increasingly proposed for deployment in resource-constrained settings where patients differ from training populations along two confounded axes: skin tone and disease distribution. We investigate whether poor generalization is primarily caused by skin-tone underrepresentation or disease-distribution shift. We evaluate a cancer-trained baseline (ResNet-50 fine-tuned on HAM10000 and ISIC 2019), two dermatology foundation models (DermLIP and MONET), and a general-purpose vision model (DINOv3) as frozen feature extractors. Models are evaluated on a tone-stratified disease-matched dataset (Diverse Dermatology Images, DDI) and a disease-shifted tone-diverse dataset (Skin Condition Image Network, SCIN). Our results show that disease-distribution shift contributes more than skin tone in the evaluated settings. The cancer baseline decreases from 0.62 to 0.21 balanced accuracy when transferred to unfamiliar clinical conditions, while the within-disease skin-tone gap is smaller (0.10-0.18) and inconsistent. Label-free representation analysis shows that this failure reflects a representational limitation rather than only missing output labels: cancer-specialized features poorly cluster unfamiliar conditions (kNN purity lift +0.06 over chance), whereas dermatology-pretrained features retain stronger transferable structure (+0.23). Finally, we show that representation quality predicts recoverable performance under lightweight adaptation. Starting from dermatology foundation models, approximately ten labeled examples per clinical category recover most attainable performance. We release the evaluation protocol and code to support reproducible auditing of dermatology AI generalization.

摘要：皮膚科人工智慧（AI）模型主要是在以淺膚色和癌症為焦點的影像資料集上訓練的，然而它們越來越多地被提議用於資源有限的環境中，在這些環境中，患者與訓練人群在兩個混淆的軸上存在差異：膚色和疾病分布。我們調查了較差的泛化是否主要是由於膚色的代表性不足或疾病分布的變化所致。我們評估了一個以癌症為訓練基準的模型（在HAM10000和ISIC 2019上微調的ResNet-50）、兩個皮膚科基礎模型（DermLIP和MONET），以及一個通用視覺模型（DINOv3）作為凍結的特徵提取器。這些模型在一個按膚色分層的疾病匹配數據集（多樣化皮膚科影像，DDI）和一個疾病轉變的膚色多樣化數據集（皮膚狀況影像網絡，SCIN）上進行評估。我們的結果顯示，在評估的環境中，疾病分布的變化對模型的影響大於膚色的影響。當轉移到不熟悉的臨床條件時，癌症基準的平衡準確率從0.62下降到0.21，而同一疾病內的膚色差距較小（0.10-0.18）且不一致。無標籤表示分析顯示，這一失敗反映了一種表徵限制，而不僅僅是缺少輸出標籤：專注於癌症的特徵在不熟悉的條件下聚類效果較差（kNN純度提升+0.06），而皮膚科預訓練的特徵則保留了更強的可轉移結構（+0.23）。最後，我們顯示表示質量預測在輕量適應下可恢復的性能。從皮膚科基礎模型開始，每個臨床類別大約需要十個標記示例即可恢復大部分可達的性能。我們發布了評估協議和代碼，以支持皮膚科AI泛化的可重複審計。

##### **READY or Not: Reliable Enterprise Agent Deployment**
2609.02095v1 by Veronica Chatrath, Bryan Zhu, Jingxuan Fan, George Pu, Soham Dinesh Tiwari, Soham Dan, Ryan Young,  Yuan,  Li, Yuang Yao, Apaar Shanker, Minglai Yang, Daniel Yue Zhang, Yunzhong He, Ying Liu, Chenguang Wang, Zhijun Yin, Yuan Xue

An AI agent can perform well on benchmarks and still be unsuitable for deployment. Existing AI-agent benchmarks measure whether an agent can complete realistic professional work, whereas enterprise deployment asks a different question: whether an agent can meet a required reliability level, under acceptable human oversight, and at tolerable cost. We introduce Reliable Enterprise Agent Deployment (READY), a framework for qualifying AI agents for deployment on enterprise workflows. READY preserves each workflow's own definition of successful execution while applying a common qualification procedure. Given an agent, a workflow, and a class of candidate oversight policies, READY measures the reliability and operating cost of the human-AI system, selects the minimum-cost policy that satisfies a specified reliability target, and statistically qualifies it on held-out cases. The resulting deployment profile characterizes the supported operating point: reliability, human-oversight burden, and cost. READY is implemented as an open testbed that decouples workflow specification, execution, evaluation, and qualification, and runs on existing agent-evaluation infrastructure. In an end-to-end clinical-audit case study spanning 16 agent systems and 750 cases, READY reveals differences hidden by autonomous performance: two systems separated by only 0.3 points in autonomous accuracy (72.8% vs. 72.5%) require 39.2% versus 29.6% human review, respectively, to qualify at the same 76% reliability target under the evaluated oversight policy. READY thus shifts enterprise agent evaluation from how well can the agent perform the work? to under what conditions, and at what cost, can it be reliably deployed? By making those conditions explicit and statistically testable, READY provides a basis for comparing agent systems, setting oversight requirements, and making evidence-based deployment decisions.

摘要：一個 AI 代理可以在基準測試中表現良好，但仍然不適合部署。現有的 AI 代理基準測試衡量代理是否能完成現實的專業工作，而企業部署則提出了不同的問題：代理是否能在可接受的人類監督下，滿足所需的可靠性水平，並在可容忍的成本內。我們引入了可靠企業代理部署（READY），這是一個用於為企業工作流程資格認證 AI 代理的框架。READY 保留每個工作流程對成功執行的自我定義，同時應用共同的資格認證程序。給定一個代理、一個工作流程和一類候選監督政策，READY 測量人類-AI 系統的可靠性和運營成本，選擇滿足指定可靠性目標的最低成本政策，並在保留的案例上進行統計資格認證。結果的部署配置描述了支持的運營點：可靠性、人類監督負擔和成本。READY 實現為一個開放的測試平台，將工作流程規範、執行、評估和資格認證解耦，並在現有的代理評估基礎設施上運行。在一個涵蓋 16 個代理系統和 750 個案例的端到端臨床審核案例研究中，READY 揭示了自主性能隱藏的差異：兩個系統在自主準確性上僅相差 0.3 分（72.8% 對 72.5%），但在評估的監督政策下，分別需要 39.2% 和 29.6% 的人類審查，以在同一 76% 的可靠性目標下獲得資格。因此，READY 將企業代理評估的重點從代理能多好地執行工作？轉變為在什麼條件下，並以什麼成本，可以可靠地部署？通過使這些條件明確且可統計測試，READY 為比較代理系統、設置監督要求和做出基於證據的部署決策提供了基礎。

##### **Sparse Readout Prism: Explaining Logit-Lens Scores in Features Instead of Tokens**
2609.01936v1 by Matteo He, William F. Shen, Xinchi Qiu, Nicholas D. Lane

A language model's prediction of its next token develops across layers, and lens methods track this process by decoding intermediate hidden states into tokens. But a lens reading reflects both the hidden state and the readout (the unembedding matrix) used to decode it. Many lenses are fit on a corpus, and we show that two lenses differing only in their fitting corpus can report different tokens for the same hidden states. We call this dependence corpus conditionality. To examine readout structure independently of the fitting corpus, we introduce Sparse Readout Prism (SRP), which decomposes the readout using only its weights and expresses any token logit or logit difference as a sum of contributions from sparse readout features. This reveals readout features as a new unit of analysis for lens readings, exposing structure that token identities can obscure and enabling comparisons across tokens, contexts, layers, and lenses. Replacing the original readout with SRP's sparse approximation reconstructs 8.9-17.3 percentage points more of the tested logit differences than the strongest of six baselines built on geometric relations among readout rows. Ablating features shifts logit differences in proportion to their SRP contributions. Although token readings vary with the fitting corpus, the dominant readout feature remains stable. Because SRP uses no corpus in its construction, it provides a control independent of the fitting corpus for lens analyses.

摘要：語言模型對其下一個標記的預測在各層中發展，而透鏡方法通過將中間隱藏狀態解碼為標記來跟踪這一過程。<lb/>但透鏡讀取反映了隱藏狀態和用於解碼的讀出（去嵌入矩陣）。<lb/>許多透鏡是基於一個語料庫進行擬合的，我們展示了僅在擬合語料庫上有所不同的兩個透鏡可以對相同的隱藏狀態報告不同的標記。<lb/>我們稱這種依賴為語料條件性。<lb/>為了獨立於擬合語料庫檢查讀出結構，我們引入了稀疏讀出棱鏡（SRP），它僅使用權重分解讀出，並將任何標記的邏輯或邏輯差異表示為來自稀疏讀出特徵的貢獻之和。<lb/>這揭示了讀出特徵作為透鏡讀取的新分析單元，暴露出標記身份可能掩蓋的結構，並使得在標記、上下文、層和透鏡之間進行比較成為可能。<lb/>用SRP的稀疏近似替換原始讀出比基於讀出行之間幾何關係的六個基準中最強的基準重建了8.9-17.3個百分點的測試邏輯差異。<lb/>去除特徵會根據其SRP貢獻改變邏輯差異。<lb/>儘管標記讀取隨擬合語料庫而變化，但主導的讀出特徵保持穩定。<lb/>由於SRP在其構建中不使用任何語料庫，它為透鏡分析提供了一個獨立於擬合語料庫的控制。

##### **When Safety Routing Breaks: Understanding Alignment Fragility under Benign Fine-Tuning**
2609.01455v1 by Yitong Guo, Xiaoyi Chen, Siyuan Zhang, Xiaofeng Wang, Haixu Tang

Benign fine-tuning severely weakens the safety alignment of large language models (LLMs), so we study why refusal behavior is so fragile. While prior work often attributes this failure to gradient conflict, we propose a fundamentally different Fisher-geometric explanation: safety Fisher is low-rank, and alignment makes the safety geometry flatter while preserving an output-routing pathway. After 100 benign fine-tuning examples, this pathway is selectively re-sharpened in output-side MLP modules, explaining the asymmetric fragility: safety can collapse to high attack success rates, while general utility degrades mildly. The routing view also explains why few safety examples can restore refusal behavior, indicating that internal safety-relevant representations are preserved. Finally, we show that LoRA and ASAM mitigate early collapse by suppressing output-side sharpness, but their protection weakens at larger fine-tuning scales. Overall, safety failure is best understood as a disruption of a low-rank output-routing mechanism

摘要：良性的微調嚴重削弱了大型語言模型（LLMs）的安全對齊，因此我們研究為什麼拒絕行為如此脆弱。雖然先前的研究通常將這一失敗歸因於梯度衝突，但我們提出了一種根本不同的Fisher幾何解釋：安全Fisher是低秩的，對齊使安全幾何變得更平坦，同時保留了一條輸出路由通道。在100個良性微調示例之後，這條通道在輸出端的MLP模組中被選擇性地重新鋒利化，解釋了不對稱的脆弱性：安全性可能崩潰至高攻擊成功率，而一般效用則輕微下降。路由觀點還解釋了為什麼少量安全示例可以恢復拒絕行為，這表明內部的安全相關表示得以保留。最後，我們顯示LoRA和ASAM通過抑制輸出端的鋒利度來減輕早期崩潰，但它們的保護在更大規模的微調中會減弱。總體而言，安全失敗最好理解為低秩輸出路由機制的破壞。

##### **Neuro-Symbolic Geometric Abstraction (NeuSOGA): From Observations to Symbolic Mathematical Representations**
2609.01408v2 by Qingde Li, Qingqi Hong, Zihan Li, Jie Tian

A fundamental challenge in artificial intelligence is the transformation of observations into explicit symbolic representations suitable for abstraction, interpretation, and reasoning. While modern AI systems achieve remarkable perceptual capabilities through large-scale statistical learning, the resulting knowledge is typically encoded within latent parameters that are difficult to inspect or manipulate analytically. Inspired by Neuro-Symbolic AI and theories of human abstraction, this paper investigates the formation of symbolic mathematical representations from geometric observations.
  We propose NeuSOGA (Neuro-Symbolic Geometric Abstraction), a framework that progressively transforms observations into topological abstractions, geometric abstractions, and ultimately symbolic mathematical representations. The architecture combines topology-guided structural discovery using Euclidean Distance Transforms, foundation-model perception using Segment Anything, adaptive multi-scale geometric abstraction, and symbolic synthesis through Implicit Area Splines.
  The resulting representation is an analytical implicit model supporting arbitrary-order smoothness, additive composition, and closed-form evaluation. Unlike neural latent encodings, the generated representation remains interpretable, editable, and mathematically explicit. Experiments on ModelNet40 point clouds, arbitrary-view projections, and segmented optical observations demonstrate that NeuSOGA transforms diverse observations into compact symbolic representations while preserving essential geometric and topological structure across sensing modalities and viewing directions.
  NeuSOGA provides an interpretable and explainable pathway from observation to symbol and establishes

摘要：一個人工智慧的基本挑戰是將觀察轉化為適合抽象、解釋和推理的明確符號表示。雖然現代 AI 系統通過大規模統計學習實現了顯著的感知能力，但所產生的知識通常編碼在難以檢查或分析操作的潛在參數中。受到神經符號 AI 和人類抽象理論的啟發，本文探討了從幾何觀察中形成符號數學表示的過程。<lb/>我們提出了 NeuSOGA（神經符號幾何抽象），這是一個逐步將觀察轉化為拓撲抽象、幾何抽象，最終形成符號數學表示的框架。該架構結合了使用歐幾里得距離變換的拓撲引導結構發現、使用 Segment Anything 的基礎模型感知、自適應多尺度幾何抽象以及通過隱式面積樣條的符號合成。<lb/>所產生的表示是一個支持任意階平滑性、加法組合和封閉形式評估的分析隱式模型。與神經潛在編碼不同，生成的表示保持可解釋性、可編輯性和數學明確性。在 ModelNet40 點雲、任意視角投影和分段光學觀察上的實驗表明，NeuSOGA 能夠將多樣的觀察轉化為緊湊的符號表示，同時保留各種感測模式和視角方向下的基本幾何和拓撲結構。<lb/>NeuSOGA 提供了一條從觀察到符號的可解釋和可解釋的途徑，並建立了

##### **Cheap Verifiers, Large Blind Spots: Measuring the Reliability Cost of Cost-Saving Cascades**
2609.01345v1 by Dushyant Rajput

Inference cascades cut cost by answering most queries with a cheap model and escalating a hard tail to a frontier model that acts as verifier. A natural extension closes the loop: fine-tune the cheap student on the verifier's rejections so the escalation rate, and cost, fall each round. We measure this loop on real LLMs and report four findings. First, the verifier's blind spot, the fraction of the student's wrong answers it accepts, is large and moves adversarially: it grows with student capability ($β$ from 0.12 to 0.55 as the student scales 0.5B to 32B) and shrinks with verifier capability, so it is worst in the cheap-student, cheap-verifier regime cascades exist to create. Second, buying it away returns the saving: a frontier verifier drives $β$ to about 0.05 but then escalates on 46% of hard-MATH queries against a 39% true error rate, paying the frontier price on nearly half of all traffic. Third, naive corrective fine-tuning on the verifier-rejected tail does not improve the small student but degrades and ultimately collapses it, across every teacher we tried (cross-family and same-family), so at this scale the self-improving loop is self-defeating. Fourth, through all of this the cascade's own dashboard, every metric computed through the verifier, reads a flat 3% error while true delivered error swings up to 32%: the system is blind to its own degradation by construction. We then give the theory that explains the blindness, a two-population conservation law, $ε_\infty \lesssim q_0 β_0$, under which every in-loop metric improves while true quality does not, and a synthetic study that validates the mechanism. The practical conclusion: the reliability of a self-improving cascade cannot be read from any metric computed through its own verifier.

摘要：推理級聯透過使用便宜的模型回答大多數查詢來降低成本，並將難以處理的尾部升級到作為驗證者的前沿模型。自然的延伸關閉了這個循環：對驗證者拒絕的部分進行精細調整便宜的學生，使得每輪的升級率和成本下降。我們在真實的LLM上測量這個循環並報告四個發現。首先，驗證者的盲點，即學生錯誤答案中被接受的比例，既大又具有對抗性：隨著學生能力的增強（$β$ 從 0.12 增長到 0.55，當學生從 0.5B 擴展到 32B 時），這一比例增長，並且隨著驗證者能力的增強而縮小，因此在便宜的學生和便宜的驗證者範疇中，這一比例最糟。其次，購買它的方式會帶回節省：一個前沿驗證者將 $β$ 驅動到約 0.05，但隨後在 46% 的困難數學查詢中升級，而真實錯誤率為 39%，幾乎在一半的所有流量中支付前沿價格。第三，對驗證者拒絕的尾部進行天真的修正性微調並未改善小學生，反而使其退化並最終崩潰，這在我們嘗試的每個教師中都是如此（跨家族和同一家族），因此在這個規模下，自我改善的循環是自我挫敗的。第四，通過這一切，級聯自身的儀表板，即通過驗證者計算的每個指標，顯示出平坦的 3% 錯誤，而真實的交付錯誤則波動到 32%：該系統在結構上對自身的退化是盲目的。我們接著給出解釋這種盲目的理論，一個兩人口保守法則，$ε_\infty \lesssim q_0 β_0$，在這種法則下，每個循環內的指標改善，而真實質量卻沒有，並且進行了一項合成研究來驗證這一機制。實際結論是：自我改善級聯的可靠性無法從通過其自身驗證者計算的任何指標中讀取。

##### **Prompt-Robust Language Models: Which Training Strategies Work?**
2609.01217v1 by Frederic Sadrieh, Michal Štefánik

Despite their strong performance, large language models remain highly sensitive to prompt formulation. Prior work addresses this through refined data construction or through dedicated robustness objectives. We reproduce and compare these strategies under controlled conditions, and measure how effective they are in addressing models' prompt sensitivity. We find the current robustness fine-tuning methods improve over standard fine-tuning and in-context learning, but the best-to-worst prompt gap remains as high as 40-57% of performance. Moreover, the recent robustness-enhancing methods we test - CoIN for contrastive alignment and PPCL for consistency regularization - often fail to outperform the simplest data construction strategy: training on one template per batch. Our diagnostics explain these results. The auxiliary objectives move the quantity they penalize, but do not generalize beyond it. Additionally, data construction strategies differ due to the conflicting signs of per-template gradients on 57-64% of parameters. Thus, batches that mix formulations force the optimizer to reconcile competing updates instead of finding a shared, prompt-agnostic one.

摘要：儘管大型語言模型表現強勁，但仍對提示的表達方式高度敏感。先前的研究通過精細的數據構建或專門的穩健性目標來解決這一問題。我們在受控條件下重現並比較這些策略，並測量它們在解決模型提示敏感性方面的有效性。我們發現，目前的穩健性微調方法優於標準微調和上下文學習，但最佳與最差提示之間的差距仍高達40-57%的性能。此外，我們測試的近期穩健性增強方法——CoIN用於對比對齊和PPCL用於一致性正則化——往往未能超越最簡單的數據構建策略：每批訓練一個模板。我們的診斷解釋了這些結果。輔助目標移動了它們懲罰的數量，但並未超出這一範疇進行概括。此外，由於每個模板的梯度在57-64%的參數上存在相互矛盾的符號，數據構建策略有所不同。因此，混合不同表達方式的批次迫使優化器調和競爭的更新，而不是找到一個共享的、與提示無關的更新。

##### **Autonomous discovery of new structure-plausibility laws for explainable and rapid crystal diagnosis and screening**
2609.01209v1 by Zhilong Song, Lixue Cheng

Crystal generators and tool-using agents propose structures faster than density functional theory (DFT) energy and phonon calculations or experiments can assess them. Deciding which candidates merit expensive assessment is therefore the bottleneck, yet most screens test little beyond atomic overlap and give no chemical reason for failure. Here, our agents generate, test and actively refute two million candidate laws, leaving eight Plausibility Rules for Inorganic Structures (PRIS). These laws encode five mechanisms: short-range repulsion, ionic contact and packing, electrostatic balance, bond-valence conservation and crystallographic site complexity. Experimental structures satisfy our law sets at 82--99%, but satisfy Pauling's rules 2--5 together at only 6.5%. The strictest set detects 87.9% of damaged crystal structures, whereas distance cutoffs detect only 1.6--3.2%. PRIS plausibility is linearly correlated with synthesizability, so the PRIS-derived synthesis score (PSS) explainably screens 83.7% of hard-to-synthesize structures while retaining 80.7% of experimental structures. In a property-conditioned inverse-design run, PRIS and PSS can reduce the DFT validation queue by up to 67.3% and keep 99.2% of the candidates whose DFT-validated bulk moduli reach the design target. Beyond screening, PRIS explains why GNoME remains enriched in rare low-symmetry structures and reveals how wrong-element assignments in falsified crystal reports hide behind plausible coordinates. PRIS moves screening from a pass-or-fail verdict to a chemical reason for failure, showing that autonomous agents can discover, by active refutation, physicochemical laws that guide calculations and experiments.

摘要：晶體生成器和使用工具的代理比密度泛函理論（DFT）能量和聲子計算或實驗更快地提出結構。因此，決定哪些候選者值得昂貴的評估成為瓶頸，但大多數篩選僅測試原子重疊，並未提供失敗的化學原因。在這裡，我們的代理生成、測試並主動駁斥兩百萬個候選法則，留下八個無機結構的合理性規則（PRIS）。這些法則編碼了五種機制：短程排斥、離子接觸和包裝、靜電平衡、鍵價守恆和晶體位置複雜性。實驗結構在82%到99%之間滿足我們的法則集，但同時滿足保林規則2到5的僅有6.5%。最嚴格的法則集檢測到87.9%的受損晶體結構，而距離截止值僅檢測到1.6%到3.2%。PRIS的合理性與合成能力線性相關，因此PRIS衍生的合成分數（PSS）可以解釋性地篩選出83.7%的難以合成的結構，同時保留80.7%的實驗結構。在一個屬性條件的反向設計運行中，PRIS和PSS可以將DFT驗證隊列減少多達67.3%，並保留99.2%其DFT驗證的體積模量達到設計目標的候選者。除了篩選，PRIS解釋了為什麼GNoME仍然富含稀有的低對稱結構，並揭示了在虛假晶體報告中錯誤元素分配如何隱藏在合理的坐標背後。PRIS將篩選從通過或失敗的判決轉變為失敗的化學原因，顯示自主代理可以通過主動駁斥發現指導計算和實驗的物理化學法則。

##### **Towards AI-Assisted Clinical Trial Matching: Practical Considerations, Multicenter Evaluation, and Real-World Deployment**
2609.01202v1 by Yin Fang, Qiao Jin, Shubo Tian, Lauren He, Maya Geer, Noor Naffakh, Ryan Huu-Tuan Nguyen, Zifeng Wang, Jimeng Sun, Charalampos S. Floudas, James L. Gulley, Kamilia Moalem, Catarina Martins Maia, Amanda Nottke, Juan W. Valle, Melinda Bachini, Lourdes Rocha-Nussbaum, Kari Ramage, Nikita Curry, Megan Barnes, Mandy Mansaray, Darlene Gabeau, Craig E. Grossman, Heath Skinner, Michael Burczynski, NIH-TrialBench Consortium, Zhiyong Lu

Clinical trials are essential for advancing cancer care and drug development, but many fail because of insufficient patient enrollment. While there is growing interest in using AI to support patient recruitment, existing systems largely perform eligibility assessment alone and have rarely been evaluated in real-world oncology workflows. Here we present TrialGPT 2.0, an AI-assisted clinical trial recommendation system designed for real-world deployment. Rather than asking only whether a patient may qualify, the system also assesses which trials warrant further consideration given the patient's current clinical needs and local workflow priorities, and provides structured, inspectable explanations for expert review. Importantly, we evaluated TrialGPT 2.0 retrospectively and prospectively across multiple oncology-focused settings, spanning government, academic cancer-center, patient-advocacy, and NIH referral workflows. In retrospective multicenter cohorts comprising 288 cases, TrialGPT 2.0 retrieved at least one clinician-recommended trial in its top 10 recommendations for approximately 91% of cases while reducing clinician screening time by 55.0%. In a six-month prospective evaluation embedded in an active precision oncology tumor board, TrialGPT 2.0 contributed additional trial opportunities missed by the routine workflow, expanding patient access to clinical trial participation by 90.9%. To support scientific reproducibility, we also introduce NIH-TrialBench, a clinician-authored dataset comprising 126 diverse synthetic patient vignettes and matching scenarios from 11 NIH Institutes and Centers. Together, these results support the value of AI to assist clinical trial matching by improving clinician efficiency and identifying frequently overlooked trial opportunities, ultimately helping to expand and accelerate accrual to cancer trials.

摘要：臨床試驗對於推進癌症護理和藥物開發至關重要，但許多試驗因患者招募不足而失敗。儘管對使用人工智慧支持患者招募的興趣日益增長，但現有系統主要僅進行資格評估，並且在現實世界的腫瘤學工作流程中很少被評估。在此，我們介紹TrialGPT 2.0，一個設計用於現實世界部署的人工智慧輔助臨床試驗推薦系統。該系統不僅詢問患者是否可能符合資格，還評估哪些試驗值得進一步考慮，考慮到患者當前的臨床需求和當地工作流程優先事項，並提供結構化、可檢查的解釋以供專家審查。重要的是，我們在多個專注於腫瘤學的環境中對TrialGPT 2.0進行了回顧性和前瞻性的評估，涵蓋政府、學術癌症中心、患者倡導和NIH轉診工作流程。在包括288例的回顧性多中心隊列中，TrialGPT 2.0在其前10個推薦中為約91%的案例檢索到了至少一個臨床醫生推薦的試驗，同時減少了臨床醫生的篩查時間55.0%。在一個嵌入在活躍的精準腫瘤學腫瘤委員會中的六個月前瞻性評估中，TrialGPT 2.0提供了常規工作流程中錯過的額外試驗機會，使患者參加臨床試驗的機會增加了90.9%。為了支持科學的可重複性，我們還介紹了NIH-TrialBench，這是一個由臨床醫生撰寫的數據集，包含126個多樣化的合成患者小品和來自11個NIH研究所和中心的匹配場景。這些結果共同支持了人工智慧在提升臨床試驗匹配效率和識別經常被忽視的試驗機會方面的價值，最終有助於擴大和加速癌症試驗的招募。

##### **Lagged Coupling: Internal Representations Become Readable Before They Become Causal**
2609.01048v1 by Xining Xun

Across the full Pythia suite (160M-12B, eight checkpoints, four task families), a linear probe can read a target variable from the residual stream as early as step 1,000 at every scale -- yet steering along that same reading direction remains null-equivalent in 43 of 48 model-checkpoint cells. Internal readability systematically outruns causal efficacy, and the lag does not shrink with scale. We call this structure lagged coupling and decompose it into three dissociable tracks: (i) internal readability, saturated (AUROC >= 0.990) from the first checkpoint everywhere; (ii) behavioral readability, which develops gradually and progressively later at larger scales (12B reaches 0.909 only at the final checkpoint); (iii) causal efficacy, almost always null-equivalent, occasionally counterproductive early, with one isolated positive pulse (12B, step 8,000, z = +2.49) our grid cannot resolve. The ordering is dominantly read-before-write (11/11 units, no inversion). Representation headroom along the probe direction grows up to 57x with training and scale while causal write-in stays below 0.11% of headroom -- the variable is increasingly written into the representation and increasingly ignored by the readout. Under a fully pre-registered protocol, both single-onset hypotheses resolve INDETERMINATE (scale slope +0.24, 95% CI [-0.60, +0.87]; time vote 3:3) -- a disciplined negative explained by the three-track decomposition. A pre-registered OLMo-2 replication preserves the direction at attenuated magnitude. Our results caution against inferring steerability from probe accuracy and establish a developmental bottleneck: representation formation reliably outpaces causal readout consolidation.

摘要：在整個 Pythia 套件中（160M-12B，八個檢查點，四個任務類別），線性探針可以在每個規模的第 1,000 步從殘差流中讀取目標變數——然而，在相同的讀取方向上進行引導在 48 個模型檢查點單元中有 43 個仍然是無效的。內部可讀性系統性地超越了因果效能，並且這一滯後隨著規模的增長並未縮小。我們稱這種結構為滯後耦合，並將其分解為三個可分離的軌道：（i）內部可讀性，從第一個檢查點開始在所有地方都達到飽和（AUROC >= 0.990）；（ii）行為可讀性，隨著規模的增大而逐漸發展，並且進展得更晚（12B 只有在最後一個檢查點達到 0.909）；（iii）因果效能，幾乎總是無效的，早期偶爾會產生反效果，並且有一個孤立的正脈衝（12B，第 8,000 步，z = +2.49）我們的網格無法解析。排序主要是讀取在寫入之前（11/11 單元，無反轉）。沿著探針方向的表示空間隨著訓練和規模的增長而增長至 57 倍，而因果寫入保持在表示空間的 0.11% 以下——這個變數越來越多地被寫入表示中，並且越來越被讀出所忽視。在完全預註冊的協議下，兩個單次發作的假設都解決為不確定（規模斜率 +0.24，95% CI [-0.60, +0.87]；時間投票 3:3）——這是一個有紀律的負面，通過三軌分解來解釋。預註冊的 OLMo-2 複製保持了方向但幅度減弱。我們的結果警告不要從探針的準確性推斷可引導性，並建立了一個發展瓶頸：表示形成可靠地超越了因果讀出整合。

##### **Causal Evidentiary Governance for High-Risk Machine Learning Systems**
2609.01040v1 by Samah Kareem, Barış Çeliktaş

Machine learning systems deployed for credit, hiring, and resource distribution are increasingly subject to regulatory oversight from policies such as the EU AI Act and GDPR. Current fairness governance practices rely on observational fairness metrics, post-hoc explainability, and immutable audit logs, but provide limited support for causal attribution and efficient evidentiary verification. We introduce Causal Evidentiary Governance (CEG), a framework in which regulated institutions commit to a versioned directed acyclic graph (DAG) that partitions causal pathways into allowable and disallowed groups. The Causal Harm Rate measures prediction variation attributable to disallowed causal pathways. Each decision is accompanied by a signed Decision-Evidence Packet (DEP), cryptographically binding the prediction to a digest of the published DAG and path-specific attributions. DEP digests can be appended to a Merkle tree to enable logarithmic-cost inclusion proofs. We validate CEG through a two-layer empirical methodology using demographic summaries from four years of PMA credit supervisory data to construct 10,000 synthetic credit applicants across four strategic DAG counterfactuals. Causal Harm Rate isolates injected causal effects more clearly than demographic parity or equalized odds. Cross-model validation and ablation studies assess robustness. Evaluation on the German Credit dataset shows that harm associated with specific causal pathways can be substantially understated by associational fairness metrics. Finally, a proof-of-concept implementation demonstrates operationally plausible throughput and highlights relevant performance tradeoffs.

摘要：機器學習系統在信用、招聘和資源分配方面的應用越來越受到來自政策的監管，例如歐盟人工智慧法案和GDPR。當前的公平治理實踐依賴於觀察性公平指標、事後可解釋性和不可變的審計日誌，但對於因果歸因和高效證據驗證的支持有限。我們提出了因果證據治理（Causal Evidentiary Governance, CEG），這是一個框架，受監管機構承諾使用版本化的有向無循環圖（DAG），將因果路徑劃分為允許和不允許的組別。因果傷害率（Causal Harm Rate）衡量由不允許的因果路徑所引起的預測變異。每個決策都附有一個簽名的決策證據包（Decision-Evidence Packet, DEP），以密碼學方式將預測綁定到已發佈DAG的摘要和路徑特定的歸因。DEP摘要可以附加到默克爾樹上，以實現對數成本的包含證明。我們通過一種兩層實證方法驗證CEG，使用四年的PMA信用監管數據的人口統計摘要，構建了10,000個合成信用申請者，涵蓋四個戰略DAG反事實。因果傷害率比人口平等或均衡機會更清晰地孤立注入的因果效應。跨模型驗證和消融研究評估其穩健性。對德國信用數據集的評估顯示，與特定因果路徑相關的傷害可能被關聯公平指標大幅低估。最後，概念驗證實現展示了運營上可行的吞吐量，並突顯了相關的性能權衡。

##### **Towards reliable multimodal disaster severity assessment through preference optimization and explainable vision-language reasoning**
2609.00879v1 by Yuanjun Zhang, Fuzel Ahamed Shaik, Suvojit Acharjee, Fahad Khalid, Mourad Oussalah

Reliable disaster damage assessment requires models that provide both accurate predictions and transparent explanations. However, existing multimodal approaches are limited by scarce annotated data and insufficient evaluation of reasoning quality. This study proposes a two-stage training framework that integrates Supervised Fine-Tuning (SFT) and Direct Preference Optimization (DPO) within a unified data construction pipeline. From a single Human-in-the-Loop (HITL) annotation workflow, two complementary datasets are derived, namely ReasoningSet, which contains validated rationales for SFT, and PreferenceSet, which comprises paired rationales for DPO-based alignment. The framework evaluates both classification performance and explanation quality using automatic metrics, model-based scoring, and human ranking. Experimental results show that SFT improves accuracy from 73.64% to 78.29% and increases Macro-F1 by 29% compared to the baseline, while explanation quality improves by approximately 25%. Subsequent DPO alignment further enhances interpretability on the PreferenceSet. Cross-model validation on InternVL-3-8B and LLaVA-1.5-7B demonstrates the robustness and generalizability of the approach. The proposed framework improves detection of underrepresented mild damage cases, reduces high-risk misclassifications, and strengthens alignment between model reasoning and human judgment. Overall, it provides a reproducible pathway to develop reliable multimodal systems that deliver auditable, actionable disaster insights for emergency management.

摘要：可靠的災害損害評估需要提供準確預測和透明解釋的模型。<lb/>然而，現有的多模態方法受到稀缺標註數據和推理質量評估不足的限制。<lb/>本研究提出了一個兩階段訓練框架，將監督微調（SFT）和直接偏好優化（DPO）整合在一個統一的數據構建流程中。<lb/>從單一的人類參與（HITL）標註工作流程中，衍生出兩個互補數據集，即ReasoningSet，該數據集包含經驗證的SFT理據，以及PreferenceSet，該數據集包含用於基於DPO的對齊的配對理據。<lb/>該框架使用自動指標、基於模型的評分和人類排名來評估分類性能和解釋質量。<lb/>實驗結果顯示，SFT將準確率從73.64%提高到78.29%，並將Macro-F1提高29%，與基準相比，解釋質量約提高25%。<lb/>隨後的DPO對齊進一步增強了PreferenceSet的可解釋性。<lb/>在InternVL-3-8B和LLaVA-1.5-7B上的交叉模型驗證展示了該方法的穩健性和普遍性。<lb/>所提出的框架改善了對代表性不足的輕微損害案例的檢測，減少了高風險的錯誤分類，並加強了模型推理與人類判斷之間的對齊。<lb/>總體而言，它提供了一條可重複的途徑，以開發可靠的多模態系統，為應急管理提供可審計、可行動的災害洞察。

##### **Ctrl-F-Resist. Practices, Challenges, and Technical Needs of Civil Society Organizations Monitoring the Far-Right Online**
2609.00808v1 by Elisabeth Steffen, Helena Mihaljević

As far-right actors increasingly exploit online platforms to disseminate ideology and mobilize supporters, civil society organizations (CSOs) play a vital yet underrecognized role in monitoring antidemocratic dynamics online. Unlike fact-checkers or content moderators, CSOs engage in long-term, contextualized analysis, often in resource-constrained settings and under precarious conditions. Despite their critical societal role, CSOs face significant barriers to adopting or co-developing technical solutions, including legal uncertainty, limited platform access, and chronic underfunding. Existing research and tool development efforts have largely overlooked these actors in favor of more institutionally embedded stakeholders. This paper addresses this gap through a qualitative study with 15 practitioners from 12 Germany-based CSOs engaged in online monitoring, positioning them as key yet overlooked stakeholders in the governance of digital spaces. We explore their current practices, challenges, and expectations regarding technological support. Our findings show that monitoring remains largely manual due to the lack of tailored tools, with enhanced search capabilities emerging as the most pressing technical need. While participants express openness to AI-supported features such as media processing and content discovery, many remain skeptical of automated classification, citing concerns around trust, legal usability, and professional credibility. Grounded in these findings, we introduce a conceptual monitoring workflow and describe its implementation in an open-source Telegram monitoring prototype designed to flexibly support diverse monitoring goals. We outline concrete design, policy, and research recommendatios, and introduce the manual labor trap as an empirically grounded concept that explains why monitoring CSOs tend to remain locked into labor-intensive, low-capacity arrangements.

摘要：隨著極右派行動者越來越多地利用線上平台來傳播意識形態和動員支持者，公民社會組織（CSOs）在監測線上反民主動態方面扮演著重要但未被充分認識的角色。與事實查核者或內容審核者不同，CSOs 進行長期且具情境性的分析，通常在資源有限和條件不穩定的環境中進行。儘管他們在社會中扮演著關鍵角色，CSOs 在採用或共同開發技術解決方案時面臨重大障礙，包括法律不確定性、有限的平台訪問和持續的資金不足。現有的研究和工具開發工作在很大程度上忽視了這些行動者，而更偏向於那些制度嵌入性更強的利益相關者。本文通過對 12 家位於德國的 CSOs 的 15 位從業者進行的定性研究來填補這一空白，將他們定位為數位空間治理中關鍵但被忽視的利益相關者。我們探討了他們目前的實踐、挑戰和對技術支持的期望。我們的研究結果顯示，由於缺乏量身定制的工具，監測仍然主要是手動進行，增強的搜索能力成為最迫切的技術需求。雖然參與者對於 AI 支持的功能，如媒體處理和內容發現表示開放，但許多人對自動分類持懷疑態度，並指出對信任、法律可用性和專業可信度的擔憂。在這些發現的基礎上，我們介紹了一個概念性的監測工作流程，並描述其在一個開源的 Telegram 監測原型中的實施，該原型旨在靈活支持多樣的監測目標。我們概述了具體的設計、政策和研究建議，並引入了手動勞動陷阱作為一個基於實證的概念，解釋為何監測 CSOs 往往會陷入勞動密集型、低能力的安排中。

##### **ContextPipe: Database-Inspired Context Assembly for Long-Horizon Agents**
2609.00749v1 by Peng Xu, Zuyu Zhang, Yuze Sun, Feng Tian, Long Wang, Chen Zhang

Long-horizon large language model (LLM) agents require context assembly: the runtime must decide what to include in each prompt, in what order, and when to compact history under a hard context-window budget and a byte-sensitive prompt cache. In production agentic systems, this logic is scattered across prompt builders, ad hoc compaction routines, cache-break workarounds, and per-provider shims. We argue that context assembly is structurally isomorphic to query execution in a relational database: both execute under a hard budget, exploit a tiered cache, and leverage statistics. We adopt this discipline in ContextPipe: a five-phase pipeline (Plan Bind Optimize Execute Feedback) backed by a structured data-source catalog, a deterministic cache-aware optimizer, and an EXPLAIN ANALYZE trace. We show that context in ContextPipe is auditable, replayable, and failure-isolated. A preliminary evaluation using the SWE-bench Pro Qutebrowser subset shows that, compared with the append-only context construction policy, ContextPipe reduces total token volume by 31%, LLM calls by 23%, and response time by 9%, at the cost of a lower KV cache-hit ratio.

摘要：長期視野的大型語言模型 (LLM) 代理需要上下文組裝：運行時必須決定在每個提示中包含什麼、順序如何，以及何時在硬性上下文窗口預算和對字節敏感的提示緩存下壓縮歷史。在生產代理系統中，這種邏輯分散在提示生成器、臨時壓縮例程、緩存破壞的變通方法以及每個提供者的適配器中。我們主張，上下文組裝在結構上與關聯數據庫中的查詢執行同構：兩者都在硬性預算下執行，利用分層緩存，並利用統計數據。我們在 ContextPipe 中採用了這一學科：一個五階段的管道（計劃、綁定、優化、執行、反饋），由結構化數據源目錄、確定性的緩存感知優化器和 EXPLAIN ANALYZE 跟蹤支持。我們展示了 ContextPipe 中的上下文是可審計的、可重播的，並且具有故障隔離性。使用 SWE-bench Pro Qutebrowser 子集的初步評估顯示，與僅附加上下文構建策略相比，ContextPipe 將總標記量減少了 31%、LLM 調用減少了 23%、響應時間減少了 9%，代價是較低的 KV 緩存命中率。

##### **Escaping Redundant Reasoning: Structure-Aware Search for Inference-Time LLMs**
2609.00738v1 by Lu Cheng

Inference-time search with large language models (LLMs) often concentrates on a small set of structurally or semantically similar trajectories, leaving alternatives underexplored---a failure mode we call \textit{reasoning basin collapse}. We introduce BASIN, a training-free, structure-aware selection method that groups reasoning states into basins and penalizes repeated visits to the same strategy, thereby reallocating search across genuinely distinct reasoning paths under a fixed compute budget. Under matched inference budgets, BASIN improves over Tree of Thoughts (ToT) by up to $+22$pp on Game of 24 and $+6.7$pp on MuSR. A quality-aware variant, QA-BASIN, further improves robustness by preserving high-quality basins when unconditional diversification over-explores. To explain when basin-aware selection helps, we introduce the redundancy gap $Δ$, which measures how differently search concentrates for correct versus incorrect predictions: standard ToT often operates near $Δ\approx 0$, while BASIN consistently shifts $Δ$ positive. More broadly, BASIN suggests structure-aware selection as a simple and general approach to improving inference-time reasoning. Code can be found at https://github.com/GitHubLuCheng/basin.

摘要：推理時期使用大型語言模型（LLMs）的搜尋通常集中在一小組結構上或語義上相似的軌跡，留下了未被充分探索的替代選擇——這是一種我們稱之為\textit{推理盆地崩潰}的失敗模式。我們介紹了BASIN，一種無需訓練的、結構感知的選擇方法，它將推理狀態分組到盆地中，並對重複訪問相同策略進行懲罰，從而在固定的計算預算下重新分配搜索到真正不同的推理路徑。在匹配的推理預算下，BASIN在Game of 24上比Tree of Thoughts (ToT)提高了最多$+22$pp，在MuSR上提高了$+6.7$pp。一個質量感知的變體QA-BASIN進一步提高了穩健性，通過在無條件多樣化過度探索時保留高質量的盆地。為了解釋何時盆地感知選擇有幫助，我們引入了冗餘差距$Δ$，它測量正確預測與不正確預測的搜索集中程度的差異：標準的ToT通常在$Δ\approx 0$附近運行，而BASIN則持續將$Δ$推向正值。更廣泛地說，BASIN建議結構感知選擇作為改善推理時期推理的一種簡單而通用的方法。代碼可以在https://github.com/GitHubLuCheng/basin找到。

##### **SciTrue: Reliable Scientific Claim Validation with Frontier and Open Language Models at the NTCIR SciClaimEval Task**
2609.00654v1 by Qiming Bao, Neşet Özkan Tan, Siyuan Wang, Mark Gahegan

We describe the SciTrue team's participation in both subtasks of the NTCIR-19 SciClaimEval task~\cite{sciclaimeval}, which asks systems to verify scientific claims against the tables and figures of a paper. Rather than tuning a single model, we benchmark eleven frontier and open multimodal models under one honest, per-sample protocol and combine them with light, transparent post-processing. On the official, blind test leaderboard (Section~\ref{sec:results}), SciTrue placed first by a clear margin in three of the four evidence-category/subtask combinations, and tied for first on the primary metric in the fourth. Three findings explain the result. First, strong instruction-tuned models are already competitive: Claude Opus~4.8 and Gemma-4-31B each exceed the strongest public baseline (o4-mini), and GPT-5.5 and Claude Fable~5 lead both subtasks (97.7 on Subtask~2). Second, the task's pairing structure is the largest lever: a \emph{leak-free pair prior} that recovers the Supported/Refuted pairing from the claim text alone (a visible field) and assigns Supported to the higher-confidence evidence raises Subtask-1 pair-accuracy from 72.2 to 93.5, far more than any model swap or ensemble weighting. Third, a case-by-case audit finds that most residual errors are visually-undetectable label-mapping swaps or dataset label noise, so measured accuracy understates the true ability and the fixable-by-modeling headroom is small. Controlled fine-tuning, distillation, and agentic consistency-checking support the same conclusions, and we document throughout a measurement leak---label information reaching a system through the packaging of the data rather than its content---in which the released file ordering encodes the label, including one instance that briefly misled our own pipeline.

摘要：我們描述了SciTrue團隊在NTCIR-19 SciClaimEval任務的兩個子任務中的參與~\cite{sciclaimeval}，該任務要求系統根據論文的表格和圖形來驗證科學聲明。與其調整單一模型，我們在一個誠實的逐樣本協議下對十一個前沿和開放的多模態模型進行基準測試，並將它們與輕量、透明的後處理相結合。在官方的盲測排行榜上（第~\ref{sec:results}節），SciTrue在四個證據類別/子任務組合中的三個中以明顯的優勢名列第一，並在第四個主要指標上並列第一。三個發現解釋了這一結果。首先，強大的指令調整模型已經具備競爭力：Claude Opus~4.8和Gemma-4-31B各自超過了最強的公共基準（o4-mini），而GPT-5.5和Claude Fable~5在兩個子任務中領先（在子任務~2中達到97.7）。其次，任務的配對結構是最大的槓桿：一個\emph{無洩漏的配對先驗}，僅通過聲明文本恢復支持/反駁配對（可見的領域），並將支持分配給更高置信度的證據，將子任務-1的配對準確率從72.2提高到93.5，這遠超過任何模型替換或集成加權。第三，逐案審計發現，大多數殘餘錯誤是視覺上無法檢測的標籤映射交換或數據集標籤噪音，因此測量的準確率低估了真實能力，而可通過建模修正的潛力很小。受控的微調、蒸餾和代理一致性檢查支持相同的結論，我們在整個過程中記錄了一個測量洩漏——標籤信息通過數據的包裝而非其內容到達系統——其中發布的文件排序編碼了標籤，包括一個短暫誤導我們自己管道的實例。

##### **Self-Reports Are Not Verification: Environment-Grounded Auditing of LLM Operators in Evolutionary Search**
2609.00652v1 by Enrong Pan, Ryan Zhou, Ting Hu

Language model agents increasingly propose actions, observe external feedback, and explain their own behavior. Their confidence and rationales are convenient monitoring signals, but convenience is not verification. We introduce an environment-grounded audit in which every intermediate proposal receives an exact outcome. A language model operates an evolutionary Contexto search whose feedback function assigns every valid guess an exact rank without human annotation. Across 200 runs spanning five configurations and three model families, four reporting configurations produce 12,249 self-reports. We test three assumptions: stated confidence is calibrated, inherited rationales affect later proposals, and fitness-based selection improves report quality. All three fail. Operators overstate top-100 success by factors of 4.8 to 9.3, while calibration and discrimination dissociate across model families. Controlled interventions on 754 inherited rationales bound any measured benefit of the genuine rationale to roughly 250 ranks. Neither fitness-based nor random selection produces a detectable selection differential or parent-to-offspring transmission in report accuracy, despite sharply different search behavior. Agent self-reports should therefore be treated as claims to verify against the environment, not as evidence of their own reliability.

摘要：語言模型代理越來越多地提出行動、觀察外部反饋並解釋自己的行為。它們的信心和理由是方便的監控信號，但方便並不等於驗證。我們引入了一種基於環境的審計，其中每個中間提案都會獲得確切的結果。語言模型運行一種進化的Contexto搜索，其反饋函數為每個有效的猜測分配一個確切的排名，而不需要人工標註。在跨越五種配置和三個模型家族的200次運行中，四種報告配置產生了12,249份自我報告。我們測試了三個假設：所述的信心是經過校準的、繼承的理由影響後來的提案，以及基於適應性的選擇提高報告質量。這三個假設都失敗了。操作員將前100名的成功率高估了4.8到9.3倍，而校準和區分在模型家族之間是脫節的。對754個繼承理由的控制干預將任何測量到的真正理由的好處限制在大約250個排名內。儘管搜索行為有明顯不同，但基於適應性或隨機選擇都未能產生可檢測的選擇差異或報告準確性的親代到子代傳遞。因此，代理的自我報告應被視為需要驗證的主張，而不是其自身可靠性的證據。

##### **A Mathematical Framework for Legacy, Governance, and Decision Integrity in Enterprise AI**
2609.00572v1 by Shorab Sarker

Enterprise artificial intelligence is increasingly embedded in decisions that must remain lawful, explainable, adaptable, and accountable despite personnel turnover, model replacement, regulatory change, and shifting organizational incentives. Existing governance frameworks provide important principles but do not by themselves supply a compact mathematical language for evaluating whether an institution can preserve sound judgment over time. This paper develops a design-science framework for institutional legacy: the durable capacity of a decision system to continue producing beneficial, lawful, explainable, and adaptable outcomes after its original designers have stepped away. The framework contributes: (i) a normalized Legacy Score based on a penalized geometric mean of knowledge retention, governance, human oversight, adaptability, feedback learning, and jurisdictional fidelity; (ii) Decision Confidence and Decision Risk models separating evidentiary confidence from consequence; (iii) authority-aware retrieval and calibrated abstention; (iv) Decision Memory for governed organizational learning; (v) Regulatory Change Velocity mapping change exposure to review intervals; and (vi) a federated regulatory knowledge-graph architecture preserving provenance and legal hierarchy. The paper also proposes eight AI Decision Integrity Rules, an evaluation protocol, and a reproducible computational demonstration. The demonstration combines a deterministic stress test with 200 Monte Carlo replications of 10,000 synthetic decisions each, illustrating Legacy Score non-compensation and comparing consequence- and authority-aware routing with a matched-coverage confidence-only baseline. The contribution remains conceptual rather than field-validated; the simulation tests internal behavior, not production performance, and all parameters require context-specific calibration.

摘要：企業人工智慧越來越多地嵌入必須保持合法、可解釋、可適應和負責的決策中，儘管人員流動、模型更換、監管變化和組織激勵的轉變。現有的治理框架提供了重要的原則，但僅靠這些原則無法提供一種緊湊的數學語言來評估一個機構是否能夠隨著時間的推移保持良好的判斷。本文開發了一個針對機構遺產的設計科學框架：決策系統在其原始設計者退出後繼續產生有益、合法、可解釋和可適應結果的持久能力。該框架的貢獻包括：(i) 基於知識保留、治理、人為監督、適應性、反饋學習和管轄權忠誠度的懲罰幾何平均數的標準化遺產分數；(ii) 將證據信心與後果分開的決策信心和決策風險模型；(iii) 具權威意識的檢索和經過校準的棄權；(iv) 用於治理組織學習的決策記憶；(v) 將變更暴露映射到審查間隔的監管變更速度；以及(vi) 保留來源和法律層級的聯邦監管知識圖譜架構。本文還提出了八條人工智慧決策完整性規則、一個評估協議和一個可重複的計算演示。該演示結合了一個確定性壓力測試和200次蒙特卡羅重複，每次包含10,000個合成決策，說明了遺產分數的非補償性並比較了後果和權威意識路由與匹配覆蓋的僅信心基準。這一貢獻仍然是概念性的，而非經過現場驗證；模擬測試內部行為，而非生產性能，所有參數都需要特定上下文的校準。

##### **Does Reasoning Mitigate Backdoor Attacks? A Neuro-Symbolic Perspective**
2609.00464v1 by Marco Antonio Corallo, Andrea Agiollo, Mauro Conti, Alberto Giaretta

Neuro-Symbolic (NeSy) AI has recently emerged as a novel paradigm to enable trustworthy AI, aiming at integrating sub-symbolic neural perception with grounded symbolic reasoning. The neuro-symbolic integration process that characterizes these models has been proven beneficial to achieve more transparent, explainable and efficient AI systems. Meanwhile, their properties under adversarial settings have been overlooked being frequently deemed robust-by-design. However, the neural-symbolic integration process they leverage constitutes an additional layer of complexity that may provide an attack entry-point. Therefore, in this paper, we claim that an in-depth investigation of the adversarial robustness of NeSy models is necessary and provide the first systematic evaluation of backdoor attacks against NeSy. To this end, we compare the most popular NeSy framework, namely DeepProbLog, against baseline neural networks across a total of eight backdoor settings and four reasoning tasks. Our experimental results show that while NeSy models are indeed more robust than their neural counterpart on average, their robustness vastly depend on the strictness of the reasoning process being enforced and its compatibility with the chosen adversarial target. The source code to reproduce our experiments is made available at https://github.com/marcoantoniocorallo/NeSy-Backdoor.

摘要：神經符號（NeSy）人工智慧最近出現，成為一種新穎的範式，以實現可信的人工智慧，旨在整合次符號的神經感知與基於實證的符號推理。這些模型所特徵的神經符號整合過程已被證明對於實現更透明、可解釋和高效的人工智慧系統是有益的。與此同時，它們在對抗性環境下的特性卻被忽視，常常被認為是設計上具有穩健性。然而，它們所利用的神經符號整合過程構成了一層額外的複雜性，可能提供攻擊的切入點。因此，在本文中，我們聲稱有必要對NeSy模型的對抗穩健性進行深入調查，並提供對NeSy的後門攻擊的首次系統性評估。為此，我們將最受歡迎的NeSy框架，即DeepProbLog，與基線神經網絡在總共八個後門設置和四個推理任務中進行比較。我們的實驗結果顯示，儘管NeSy模型在平均情況下確實比其神經對應物更具穩健性，但它們的穩健性在很大程度上取決於所施加的推理過程的嚴格性及其與所選對抗目標的兼容性。我們的實驗源代碼可在https://github.com/marcoantoniocorallo/NeSy-Backdoor取得。

##### **One note in three: a verified census of three deployed AI scribes, and the instrument that counted it**
2608.31017v1 by Sebastian Fox, Luke Markham, Ryan Lail, Michael Karotsieris

Ambient AI scribes draft clinical notes under the reassurance that a clinician signs every note. We audited three commercial AI scribes on the same 142 consultations: 565 notes from recorded UK primary-care and US ambulatory encounters plus authored scenarios. Twelve discovery passes proposed 13,678 candidate errors; the 5,898 clearing an importance filter went to an adversarial panel of two models from different families, each told to refute what it could, and 618 survived. One note in three (31.3% [27.0, 35.6]) carries a verified failure, concentrated in allergy and medication information, invented patient identity, and history written up as examination on telephone consultations that can contain none. No product was given a patient record; setting aside the two classes a record would have prefilled, invented identity and dates, the rate is 24.8% [20.8, 29.0]. One failure mode did not fit our scheme, drawn from published scribe-error taxonomies: a treatment the clinician retracts, recorded as delivered care. Two clinicians adjudicated blind, disjoint samples: a physician author upheld 20 of 21 findings (95.2% [77.3, 99.2]) and an independent clinician, not an author, 12 of 12 ([75.8, 100]); both judged every sampled refusal genuine. A failure rate depends on the instrument as much as the scribes. With model, evidence and settings fixed, the review instruction alone moves the share of candidates verified from 9.3% to 79.0%, and the reviewing family moves it too: alone at that instruction the gentler flags 54.8% of notes against 27.8%. Between 28% and 97% of sampled notes carry a failure depending on the standard. Published audits disagree among themselves by a margin instrument differences alone can produce: omission is 54-86% of their errors against our 23.1%. We release all 618 findings with transcript-side evidence, every prompt and model version, and the re-runnable pipeline.

摘要：環境人工智慧書記在臨床醫生簽署每一份筆記的保證下起草臨床筆記。我們對三個商業人工智慧書記進行了審核，涵蓋同樣的142次諮詢：來自英國初級保健和美國門診遭遇的565份記錄筆記以及作者編寫的情境。十二次發現通過提出了13,678個候選錯誤；5,898個通過重要性篩選的錯誤被送到兩個不同家族的對抗性模型小組，每個模型都被告知要駁斥其能駁斥的內容，最終有618個存活下來。每三份筆記中就有一份（31.3% [27.0, 35.6]）存在經過驗證的失敗，主要集中在過敏和藥物資訊、虛構的病人身份，以及在電話諮詢中寫成檢查的歷史，這些電話諮詢中不可能包含這些內容。沒有產品被賦予病人紀錄；撇開兩個類別的預填紀錄，虛構身份和日期的情況下，失敗率為24.8% [20.8, 29.0]。一種失敗模式不符合我們的方案，這是從已發表的書記錯誤分類中提取的：一種臨床醫生撤回的治療，被記錄為已提供的護理。兩位臨床醫生進行盲審，樣本不重疊：一位醫生作者支持21項發現中的20項（95.2% [77.3, 99.2]），而一位獨立的臨床醫生（非作者）支持12項中的12項（[75.8, 100]）；兩人都認為每個樣本拒絕都是可信的。失敗率取決於工具與書記一樣多。在模型、證據和設置固定的情況下，僅審查指令就將驗證的候選比例從9.3%提高到79.0%，而審查的家族也會影響它：在該指令下，溫和的標記對54.8%的筆記提出異議，而27.8%的則不。根據標準，28%到97%的樣本筆記存在失敗。已發表的審核之間存在分歧，僅僅是工具差異就能產生的幅度：遺漏是他們錯誤的54-86%，而我們的為23.1%。我們釋出所有618項發現，並附有逐字稿邊證據、每個提示和模型版本，以及可重執行的流程。

##### **LLM Judges Verify Presence, Not Absence: Omission Blindness in AI Clinical Notes and What Recovers It**
2608.31016v1 by Sebastian Fox, Luke Markham, Ryan Lail, Michael Karotsieris

Ambient AI scribes draft clinical notes, and published audits find their dominant error is omission: information the encounter established that the note fails to record. The standard check is an LLM judge: a second model reads the note against the transcript and flags problems. We ask whether judges detect omissions. Public corpora cannot supply the answer key: their clinician reference notes and transcripts are materially discrepant. Our benchmark has 500 single-error note pairs from audited fact sheets, 298 with a named fact certainly absent and 202 added-or-altered controls. Across eight judge designs, paired discrimination (the flawed note below its clean twin, 0.5 a coin flip) reads 0.79-0.94 on added or altered content and 0.50-0.63 on omissions. On single notes, no design flags omissions reliably more often than perfect notes. Wording changes, voting and GEPA prompt optimisation move the operating point without creating usable detection. Restructuring the task recovers it: list the facts the transcript establishes, then check the note for each. Two methods reach it independently and trade off: a per-fact pipeline, and a GEPA-evolved prompt doing the same in one call. The pipeline's flags name the missing fact and its severity at 2.7% false alarms. The single call detects more (36.9% against 24.6%, p=0.002) at 6.2% false alarms and a tenth of the cost per note. A physician author validated 70 items and, where the two routes disagree, sided with the pipeline on 10 of 10 (p=0.002). A second clinician, not an author, graded the severity rubric blind and agrees to within a grade. On real vendor notes from a companion census no benchmark threshold transfers, but the re-calibrated single call detects more than the best of the eight at half its false-alarm rate. Omissions whose fact is restated elsewhere defeat both routes. We release the benchmark, prompts and judgements.

摘要：環境 AI 書記起草臨床筆記，已發表的審計發現其主要錯誤是遺漏：筆記未記錄的會議所建立的信息。標準檢查是一個 LLM 評判：第二個模型將筆記與逐字稿進行比對並標記問題。我們詢問評判是否能檢測到遺漏。公共語料庫無法提供答案鑑別鍵：它們的臨床參考筆記和逐字稿在實質上存在差異。我們的基準包含 500 對單一錯誤的筆記來自審計的事實表，其中 298 個確定缺少的命名事實和 202 個添加或更改的控制項。在八種評判設計中，配對區分（下方的有缺陷筆記與其乾淨的雙胞胎，0.5 為擲硬幣）在添加或更改內容上讀取 0.79-0.94，而在遺漏上則為 0.50-0.63。在單一筆記中，沒有設計能比完美筆記更可靠地標記遺漏。措辭變更、投票和 GEPA 提示優化改變了操作點，但未能創造可用的檢測。重構任務使其恢復：列出逐字稿所建立的事實，然後檢查每個筆記。兩種方法獨立達成並進行權衡：一個按事實的管道，以及一個 GEPA 演變的提示在一次呼叫中完成相同的工作。管道的標記名稱缺失的事實及其嚴重性，假警報率為 2.7%。單次呼叫檢測到更多（36.9% 對 24.6%，p=0.002），假警報率為 6.2%，每筆記的成本僅為十分之一。一位醫生作者驗證了 70 項目，並且在兩條路徑意見不合的地方，對於 10 個中的 10 個（p=0.002）都支持管道。第二位臨床醫生，並非作者，盲評了嚴重性標準並在一個等級內達成一致。在來自伴隨普查的真實供應商筆記中，沒有基準閾值轉移，但重新校準的單次呼叫檢測到的數量超過了八種中的最佳，且其假警報率僅為一半。其他地方重述的事實的遺漏擊敗了兩條路徑。我們釋放基準、提示和判斷。

##### **Taking the Whys Seriously: Limitations of Counterfactual Explanations in Justification and Recourse**
2608.30956v1 by Mattia Cerrato, Otto Sahlgren, Xenia Heilmann

Counterfactual explanations (CEs) are widely used in explainable artificial intelligence (AI) to show how a model's outputs would change if the input features were manipulated. This technique is used for a range of tasks such as debugging models, explaining predictions, justifying decisions, and providing algorithmic recourse. In this paper, we explore the normative legitimacy of employing counterfactuals in real-life model deployment settings. We discuss the different stakes involved in these different purposes for which CEs are commonly employed, and find stricter requirements for justification and recourse. In particular, we find that naive application of CEs for justification and recourse can lead to ignoring contestable choices made throughout the machine learning (ML) pipeline, thus obfuscating that decisions and counterfactuals for those decisions are also artifacts of an organization's materialized design and governance choices. We demonstrate this with four empirical experiments involving interventions at stages of the ML pipeline ``upstream" of the explanation itself, and show that these affect the generated counterfactuals. We find that an organization's choices on measurement models for feature and labels, business requirements, model validation, and the metric of model success have as much or more impact on the generated counterfactuals as the specifics of the generating method. Our findings underline the need to account for such choices upon providing justification and recourse, providing a stark reminder of the relational nature of these tasks. As putative justifications or recourse recommendations, CEs do not provide adequate answers to some important "why"-questions because they preclude consideration of whether the decision-maker ought to have acted differently.

摘要：反事實解釋（CEs）在可解釋的人工智慧（AI）中被廣泛使用，以顯示如果輸入特徵被操控，模型的輸出將如何改變。這種技術用於多種任務，例如調試模型、解釋預測、為決策提供理由以及提供算法補救。在本文中，我們探討在現實模型部署環境中使用反事實的規範合法性。我們討論了這些不同目的所涉及的不同利害關係，並發現對於理由和補救的要求更為嚴格。特別是，我們發現對於理由和補救的天真應用反事實可能會忽視在整個機器學習（ML）流程中所做的具爭議性的選擇，從而模糊了這些決策及其反事實也是組織具體化設計和治理選擇的產物。我們通過四個實證實驗來證明這一點，這些實驗涉及在解釋本身的“上游”階段進行干預，並顯示這些干預影響生成的反事實。我們發現，組織在特徵和標籤的測量模型、業務需求、模型驗證以及模型成功指標上的選擇對生成的反事實的影響與生成方法的具體細節一樣多或更多。我們的研究結果強調在提供理由和補救時需要考慮這些選擇，並清楚地提醒這些任務的關聯性。作為假定的理由或補救建議，反事實並未對某些重要的“為什麼”問題提供充分的答案，因為它們排除了考慮決策者是否應該以不同方式行動的可能性。

##### **Responsible Integration of AI in Cancer Genomics: Barriers, Risks, and Pathways to Trustworthy Clinical Translation**
2608.30912v1 by Bahar İlgen, Yiannos Tolias, Denise Kühnert, Paraskevi Papadopoulou, Magnus Westerlund, Dominik Heider, Katharina Ladewig, Georges Hattab

Artificial intelligence (AI) and natural language processing (NLP) are increasingly used to extract, integrate, and interpret biomedical knowledge relevant to cancer genomics, yet their translation into routine clinical oncology has been comparatively slow. The central challenge is not computational capability alone, but trustworthy integration into clinical workflows. This review examines how NLP and AI support the cancer genomics pipeline, from literature mining and automated variant interpretation to clinical trial matching, knowledge graph construction, and multimodal data integration. We identify four interrelated translational failure domains: evidence inconsistency, explainability and uncertainty, data governance and reproducibility, and interoperability. Rather than considering these challenges in isolation, we take a systems-level view, focusing on their interaction across the translational pathway. We propose a conceptual framework and roadmap for addressing these domains through rigorous validation, uncertainty-aware methods, interoperable infrastructures, regulatory alignment, and human oversight across the AI lifecycle. Progress toward routine clinical use will depend less on further improving model capability than on systematically addressing these interacting failure domains from development through deployment and post-deployment monitoring.

摘要：人工智慧（AI）和自然語言處理（NLP）越來越多地用於提取、整合和解釋與癌症基因組學相關的生物醫學知識，但它們轉化為常規臨床腫瘤學的速度相對較慢。主要挑戰不僅僅是計算能力，而是可信的整合進入臨床工作流程。這篇評論檢視了NLP和AI如何支持癌症基因組學的流程，從文獻挖掘和自動變異解釋到臨床試驗匹配、知識圖譜構建和多模態數據整合。我們確定了四個相互關聯的轉化失敗領域：證據不一致性、可解釋性和不確定性、數據治理和可重複性，以及互操作性。我們不僅將這些挑戰孤立考慮，而是採取系統層面的觀點，專注於它們在轉化路徑中的相互作用。我們提出了一個概念框架和路線圖，以通過嚴格的驗證、考慮不確定性的方法、可互操作的基礎設施、監管對齊和整個AI生命週期中的人類監督來解決這些領域。向常規臨床使用的進展將不僅依賴於進一步提高模型能力，而是系統性地解決從開發到部署及部署後監控的這些相互作用的失敗領域。

##### **Autoregressive Mosaics: Probing 2D Spatial Reasoning in Text-Only Language Models**
2608.30751v2 by Ashwin Nedungadi, Stefan Oehmcke, Stefan Lüdtke

Large language models (LLMs) trained only on text and code can sometimes generate programs that draw recognizable images. However, it is unclear whether this reflects an internal representation of 2D spatial layout or simply the ability to translate spatial descriptions into code. We introduce Autoregressive Mosaics (AM-Bench), a benchmark that separates these factors: First, a translation task gives a model a fully specified geometry of a picture in words as a prompt and asks for the code that produces it. Second, a layout task requires the model to compose an image from an underspecified prompt. Across eight open-weight text-and-code-only models, all models reliably translate specified geometry into code, but their open-ended layout performance differs substantially, indicating that these differences are not explained by code-generation ability alone. An output-medium ablation further shows that the interface or medium of expression that the model uses matters: replacing procedural code with raw SVG improves layout scores across all models. Finally, probing model activations shows that a coarse layout plan is present before generation, but reflects only the layout implied by the prompt. During generation, models track the evolving geometric state instead of executing an initially fixed plan. Overall, these results show that 2D spatial performance in text-only LLMs depends on both the model and the output medium, and is not explained by code-generation ability alone.

摘要：大型語言模型（LLMs）僅基於文本和代碼訓練，有時能生成可識別的圖像程序。<lb/>然而，目前尚不清楚這是否反映了對二維空間佈局的內部表徵，或僅僅是將空間描述轉換為代碼的能力。<lb/>我們介紹了自回歸馬賽克（AM-Bench），這是一個將這些因素分開的基準：首先，翻譯任務給模型提供了用文字完全指定的圖片幾何作為提示，並要求生成相應的代碼。<lb/>其次，佈局任務要求模型從不完全指定的提示中組合出一幅圖像。<lb/>在八個開放權重的文本和代碼模型中，所有模型都可靠地將指定的幾何轉換為代碼，但它們在開放式佈局表現上的差異相當顯著，這表明這些差異並不僅僅由代碼生成能力解釋。<lb/>進一步的輸出介質消融實驗顯示，模型使用的表達介面或媒介是重要的：用原始SVG替換程序代碼提高了所有模型的佈局分數。<lb/>最後，對模型激活的探測顯示，在生成之前存在一個粗略的佈局計劃，但僅反映提示所暗示的佈局。<lb/>在生成過程中，模型跟踪不斷演變的幾何狀態，而不是執行最初固定的計劃。<lb/>總體而言，這些結果表明，文本僅 LLM 中的二維空間表現依賴於模型和輸出媒介，並不能僅僅由代碼生成能力解釋。

##### **Assessing Alignment and Stability of Feature Importance Explanations via Weight of Evidence**
2609.00090v1 by Eddie Conti, Claudio Daka, Álvaro Parafita, Antonio L. Alfeo, Axel Brando, Mario G. C. A. Cimino

Feature importance Methods (FIMs) are widely used in Explainable AI to interpret model predictions, yet attribution scores alone often provide limited insight into the underlying reasoning process. In this work, we introduce a novel perspective by embedding FIMs within a hypothesis-testing framework based on Weight of Evidence (WoE). We quantify how strongly the observed evidence supports any given hypothesis on feature importance. The reference hypothesis can stem from domain knowledge, ground truth, or be derived from the FIM itself. This formulation enables a principled evaluation of FIMs, capturing both their alignment with prior knowledge and their variability. We further provide theoretical results linking WoE to attribution variance. Empirical results shows the applicability and flexibility of our strategy analyzing LIME and SHAP explanations in settings with different reference hypotheses. Overall, our framework offers a complementary tool for assessing FIMs through a contrastive, evidence-based lens.

摘要：特徵重要性方法（FIMs）在可解釋的人工智慧中被廣泛用於解釋模型預測，但單靠歸因分數通常對於潛在的推理過程提供的見解有限。<lb/>在這項工作中，我們通過將FIMs嵌入基於證據權重（WoE）的假設檢驗框架中，引入了一種新穎的視角。<lb/>我們量化觀察到的證據在多大程度上支持任何給定的特徵重要性假設。<lb/>參考假設可以源自領域知識、真實情況，或從FIM本身推導而來。<lb/>這種表述使得對FIMs進行原則性評估成為可能，捕捉它們與先前知識的對齊程度及其變異性。<lb/>我們進一步提供理論結果，將WoE與歸因變異聯繫起來。<lb/>實證結果顯示我們的策略在分析LIME和SHAP解釋時，在不同參考假設的情境下的適用性和靈活性。<lb/>總體而言，我們的框架提供了一種通過對比的、基於證據的視角來評估FIMs的補充工具。

##### **MedAgent-R1: Faithfulness-Aware Reinforcement Learning for Evidence-Grounded Medical Reasoning**
2608.30676v1 by Jiangwang Chen, Chenghao Zhang, Hengxing Cai

When medical AI systems hallucinate clinical reasoning, the consequences extend beyond incorrect answers: fabricated justifications that superficially reference retrieved evidence can mislead clinicians into unsafe treatment decisions. Medical reasoning agents must therefore produce not only correct answers but also faithful justifications that clinicians can verify against cited evidence. We identify a systematic failure mode in RL-trained retrieval agents: outcome-only rewards improve accuracy while degrading faithfulness, a phenomenon we term confident hallucination. The agent learns to answer from parametric memory and backfill plausible but unsupported justifications; citation fabrication rates rise from 16.5% to 31.8% even as accuracy improves by 5 points over the supervised baseline. We address this with a faithfulness-gated reward design: accuracy credit is conditioned on evidence grounding via a hard gate, complemented by retrieval validity and conciseness signals that close exploitation paths unique to agentic retrieval. The resulting system, MedAgent-R1, reduces citation fabrication from 31.8% to 4.7% and raises evidence completeness from 58.7 to 82.6 while maintaining 75.1% accuracy, with 13.2-point gains on HealthBench Safety. Under the same agentic retrieval setup, MedAgent-R1 outscores GPT-4o on faithfulness-specific dimensions (Factual Support 4.55 vs. 4.25; Overclaiming 4.40 vs. 4.15) while remaining below GPT-4o in overall accuracy, suggesting that explicit faithfulness training yields evidence-grounding gains not achieved by scaling alone.

摘要：當醫療 AI 系統出現臆想的臨床推理時，其後果不僅僅是錯誤的答案：虛構的理由表面上參考檢索到的證據，可能會誤導臨床醫生做出不安全的治療決策。因此，醫療推理代理必須不僅提供正確的答案，還必須提供臨床醫生可以根據引用的證據進行驗證的真實理由。我們發現了一種系統性失敗模式，在 RL 訓練的檢索代理中：僅基於結果的獎勵提高了準確性，但降低了真實性，這一現象我們稱之為自信的臆想。代理學會從參數記憶中回答問題，並填補合理但未經支持的理由；即使準確性比監督基線提高了 5 個點，引用虛構的比率仍從 16.5% 上升到 31.8%。我們通過一種真實性門控的獎勵設計來解決這個問題：準確性信用依賴於通過硬門進行的證據基礎，並輔以檢索有效性和簡潔性信號，這些信號關閉了獨特於代理檢索的利用路徑。最終系統 MedAgent-R1 將引用虛構率從 31.8% 降至 4.7%，並將證據完整性從 58.7 提高到 82.6，同時保持 75.1% 的準確性，在 HealthBench Safety 上獲得 13.2 點的增益。在相同的代理檢索設置下，MedAgent-R1 在真實性特定維度上超過了 GPT-4o（事實支持 4.55 對 4.25；過度聲稱 4.40 對 4.15），同時在整體準確性上仍低於 GPT-4o，這表明明確的真實性訓練帶來的證據基礎增益是單靠規模無法實現的。

##### **Automated Testing of LLM-Based Post Hoc Explainers Using Model Checking as an Oracle**
2608.30581v1 by Dennis Gross, Helge Spieker

Large language models (LLMs) are used as post hoc explainers of sequential decision-making policies, producing natural-language explanations of why an action was chosen. However, LLMs often generate plausible but incorrect statements, and no existing approach systematically tests whether such explanations are faithful to the underlying environment. Two classic software testing challenges stand in the way: there is no oracle for the correctness of an explanation, and the test inputs, natural language queries about a policy's behavior, lack the structure needed for systematic test case generation. We address both. Probabilistic model checking provides the test oracle, computing exact reference results against which LLM answers are graded automatically. A taxonomy of post hoc query categories structures the input space around the environment-level facts from which policy explanations are composed; test cases generated from it are prioritized by question-specific diagnostic difficulty scores. Across seven MDP environments, the testing separates three open-weight LLMs: a reasoning model passes 85% of test cases, a mid-size model 70%, and a 1B model falls below the random baseline, while prioritization surfaces significantly harder cases than random selection. Our results indicate how trustworthy LLM-generated explanations are in model-free settings, where the same LLMs are used but no oracle exists to verify them.

摘要：大型語言模型（LLMs）被用作序列決策政策的事後解釋，生成自然語言的解釋來說明為何選擇某個行動。<lb/>然而，LLMs 經常生成看似合理但不正確的陳述，且目前沒有現有的方法系統性地測試這些解釋是否忠實於基礎環境。<lb/>兩個經典的軟體測試挑戰阻礙了這一進程：對於解釋的正確性沒有預言者，且測試輸入，即關於政策行為的自然語言查詢，缺乏系統性測試案例生成所需的結構。<lb/>我們解決了這兩個問題。<lb/>概率模型檢查提供了測試預言者，自動計算與 LLM 答案進行評分的準確參考結果。<lb/>事後查詢類別的分類法圍繞著政策解釋所組成的環境層級事實結構化輸入空間；從中生成的測試案例根據問題特定的診斷難度分數進行優先排序。<lb/>在七個 MDP 環境中，測試將三個開放權重的 LLM 分開：一個推理模型通過了 85% 的測試案例，一個中型模型通過了 70%，而一個 1B 模型則低於隨機基準，且優先排序顯示出比隨機選擇更難的案例。<lb/>我們的結果表明，在無模型的設置中，LLM 生成的解釋的可信度如何，這些設置中使用了相同的 LLM，但不存在驗證它們的預言者。

##### **AI Morbidity and Mortality: A Framework for Clinical AI Failure Review**
2609.00076v1 by Paulius Mui, Dean F. Sittig, Steve Labkoff, Sanjay Basu

Clinical artificial intelligence is increasingly embedded in real-world care, yet existing safety mechanisms are poorly suited to reconstructing and learning from individual AI-related errors and near-misses. Aggregate model monitoring can identify performance changes, and traditional patient safety reporting can capture adverse events, but neither is designed to explain how risk emerges across the interaction among AI systems, clinicians, workflows, and institutional controls. We propose AI Morbidity and Mortality (AI M&M), a structured, blameless framework for case-based review of clinical AI failures. The framework combines standardized case intake, evidence preservation and investigator-level reconstruction, tool-in-loop attribution, and corrective-action tracking. Each event is classified across four linked dimensions: Trigger - Mechanism - Clinical Pathway - Corrective Action, separating the condition that exposed a vulnerability from the process that produced risk, its consequence for care, and the remediation assigned. We demonstrate the framework using five illustrative outpatient medication and clinical decision-support cases; two clinician reviewers independently applied all four classification axes and reached agreement across all 20 axis-level classifications. AI M&M is intended to complement, rather than replace, model monitoring, patient safety reporting, and regulatory oversight by converting individual AI-in-workflow failures into actionable institutional learning. Prospective evaluation across institutions, AI systems, and clinical settings is needed.

摘要：臨床人工智慧越來越多地嵌入現實世界的護理中，然而現有的安全機制對於重建和學習個別AI相關錯誤和接近失誤的能力較差。聚合模型監測可以識別性能變化，傳統的病人安全報告可以捕捉不良事件，但兩者都不是為了解釋風險如何在AI系統、臨床醫生、工作流程和機構控制之間的互動中產生而設計的。我們提出了AI疾病與死亡（AI M&M），這是一個結構化的、無責任的框架，用於基於案例的臨床AI失敗回顧。該框架結合了標準化的案例接收、證據保存和調查者級別的重建、工具內歸因和糾正行動追蹤。每個事件根據四個相互關聯的維度進行分類：觸發 - 機制 - 臨床路徑 - 糾正行動，將暴露脆弱性的條件與產生風險的過程、對護理的後果以及分配的補救措施分開。我們使用五個示例性門診藥物和臨床決策支持案例來演示該框架；兩位臨床審查員獨立應用所有四個分類軸，並在所有20個軸級分類中達成一致。AI M&M旨在補充，而不是取代模型監測、病人安全報告和監管監督，通過將個別AI工作流程中的失敗轉化為可操作的機構學習。需要在不同機構、AI系統和臨床環境中進行前瞻性評估。

##### **Co-Annotator: Expert-Distilled ViT and VLM for Visual and Documentation Guidance in Age-Related Macular Degeneration**
2608.30352v1 by Ziheng "Leo" Li, Benjamin Freeman, Akshay Raman, Kavin Aravindhan Rajkumar, Xinxin Fang, Rishabh Srivastava, Steven Feiner, Kaveri A. Thakoor

Clinical AI often optimizes predictive performance without engaging how clinicians decide where to look and what to write. We present Co-Annotator, which distills expert gaze and dictation into two guidance components: a gaze-aligned Vision Transformer producing fixation-aligned areas of interest (AOIs), and an ontology-bounded vision-language model (VLM) that pre-fills editable biomarker summaries for retinal optical coherence tomography (OCT). We first collect expert gaze and dictations (US1) to train the models, significantly improving diagnostic accuracy and biomarker generation. We then deploy the system with ophthalmology residents: a controlled resident study (US2) confirmed each modality is safe and independently beneficial, with AOI guidance producing lasting perceptual efficiency gains through post-guidance carryover and VLM guidance more than doubling biomarker documentation breadth. In a combined deployment across two academic institutions (US3), providing both modalities simultaneously produced efficiency gains that substantially exceeded either modality alone: correct diagnoses per minute increased by 40% and comment editing time fell by 67%, without compromising diagnostic accuracy. Notably, neither modality improved efficiency during guidance in US2, which makes the in-guidance efficiency gain under combined guidance in US3 the more striking result. Expert-distilled multimodal guidance can remove two distinct clinical workflow bottlenecks at once (visual search overhead and documentation burden) without compromising the diagnostic accuracy clinicians already achieve.

摘要：臨床人工智慧通常會優化預測性能，但不涉及臨床醫師如何決定觀察何處和撰寫內容。我們提出了Co-Annotator，它將專家視線和口述提煉為兩個指導組件：一個與視線對齊的視覺Transformer，生成與注視對齊的興趣區域（AOIs），以及一個受本體約束的視覺-語言模型（VLM），為視網膜光學相干斷層掃描（OCT）預填可編輯的生物標記摘要。我們首先收集專家的視線和口述（US1）以訓練模型，顯著提高診斷準確性和生物標記生成。然後，我們與眼科住院醫師部署該系統：一項受控的住院醫師研究（US2）確認每種模式都是安全且獨立有益的，AOI指導通過後續指導的延續產生持久的感知效率提升，而VLM指導則使生物標記文檔的範圍增加了兩倍以上。在兩個學術機構的聯合部署中（US3），同時提供這兩種模式產生的效率提升大大超過單獨使用任何一種模式：每分鐘的正確診斷增加了40%，而評論編輯時間減少了67%，且未損害診斷準確性。值得注意的是，在US2的指導期間，沒有任何一種模式提高效率，這使得在US3的聯合指導下的指導內效率提升成為更引人注目的結果。專家提煉的多模態指導可以同時消除兩個不同的臨床工作流程瓶頸（視覺搜索開銷和文檔負擔），而不影響臨床醫師已經達到的診斷準確性。

##### **Science sandboxes measure the scientific capability of AI agents**
2608.30165v1 by Arya S. Rao, Rodrigo I. Castro, Sager J. Gosai, Kenneth B. Hsu, Yasha Ektefaie, Shantanu Singh, Sangeeta N. Bhatia, Steven K. Reilly, Ryan Tewhey, Eric S. Lander, Pardis C. Sabeti

Scientific progress depends not only on finding solutions, but on learning the rules that explain why they work and using that understanding to design better experiments. We introduce science sandboxes, a framework for studying this capability in AI agents through repeated cycles of experimentation, feedback, and hypothesis revision. Science sandboxes invite an agent to query the natural world in different ways, ranging from "wet" physical experiments, to "damp" predictive models trained on empirical data, to "dry" invented rules. By establishing a common experimental loop and a protocol for evaluating agents within it, science sandboxes allow assessment of both quantitative performance on specific metrics and qualitative scientific reasoning, across a spectrum of empirical verifiability. Here, we instantiate this framework in two biological settings, models of regulatory genomics and protein fitness prediction, and examine the capabilities of frontier agents. Across these settings, we could see when agents successfully optimized a quantitative metric without understanding the rules underlying the system. In particular, their scientific reasoning deteriorated when they encountered systems whose rules fell outside familiar biological priors. By highlighting such failure modes, science sandboxes make the frontier of scientific capability measurable and provide a controlled setting in which to study and ultimately expand it.

摘要：科學進展不僅依賴於找到解決方案，還依賴於學習解釋為什麼這些解決方案有效的規則，並利用這種理解來設計更好的實驗。<lb/>我們引入科學沙箱，這是一個通過重複的實驗、反饋和假設修訂循環來研究AI代理能力的框架。<lb/>科學沙箱邀請代理以不同的方式探詢自然世界，範圍從“濕”的物理實驗，到基於經驗數據訓練的“潮濕”預測模型，再到“乾燥”的發明規則。<lb/>通過建立一個共同的實驗循環和評估代理的協議，科學沙箱允許對特定指標的定量表現和質性科學推理進行評估，涵蓋一個經驗可驗證性的光譜。<lb/>在這裡，我們在兩個生物學環境中實現了這一框架，分別為調控基因組學模型和蛋白質適應性預測，並檢查前沿代理的能力。<lb/>在這些環境中，我們可以看到當代理成功優化一個定量指標時，卻沒有理解系統背後的規則。<lb/>特別是，當他們遇到規則超出熟悉的生物學先驗的系統時，他們的科學推理能力下降。<lb/>通過突顯這種失敗模式，科學沙箱使科學能力的前沿可測量，並提供了一個受控的環境來研究並最終擴展它。

##### **Automatic Conversion of NICE Guidelines to an Executable Computational Model Using Large Language Models**
2608.30022v1 by Ashvin Gupta, Denys Prociuk, Alessandra Russo, Brendan C. Delaney

Introduction: NICE guidelines provide evidence-based recommendations for clinical care but remain largely in unstructured natural language. Existing approaches to converting them into computable representations often focus on individual diseases, require substantial manual encoding, and do not scale. Large language models (LLMs) may enable much of this translation to be automated. Methods: We present an end-to-end approach that converts textual clinical guidelines into executable models capable of generating explainable, patient-specific recommendations. A stepwise LLM-based transformation with in-context examples produces human-inspectable intermediate artifacts. We apply the approach to NICE pancreatic and lung cancer guidelines, use expert review to assess rule alignment, and evaluate the executable pancreatic cancer model on 20 patient vignettes. Results: Expert review showed strong alignment between the source guidelines and generated executable models. Most discrepancies were partial omissions rather than incorrect logic, while hallucinated or fundamentally incorrect rules were rare. On the patient vignettes, the executable model achieved an F1 score of 82.5%. Conclusion: LLMs can transform natural-language NICE guidelines into interpretable, executable models that preserve guideline structure, support transparent inspection and modification, and generate patient-specific recommendations. These findings demonstrate the feasibility of scalable automated generation of computable clinical guidelines.

摘要：引言：NICE 指南提供基於證據的臨床護理建議，但仍然主要以非結構化的自然語言呈現。現有的將其轉換為可計算表示的方法通常集中於單一疾病，需大量手動編碼，且無法擴展。大型語言模型（LLMs）可能使這一翻譯過程的自動化成為可能。方法：我們提出了一種端到端的方法，將文本臨床指南轉換為可執行模型，能夠生成可解釋的、針對患者的具體建議。基於 LLM 的逐步轉換結合上下文示例，產生可供人類檢查的中間產物。我們將此方法應用於 NICE 胰臟癌和肺癌指南，並使用專家評審來評估規則的一致性，對 20 個患者小案例評估可執行的胰臟癌模型。結果：專家評審顯示源指南與生成的可執行模型之間有很強的一致性。大多數差異是部分省略，而非邏輯錯誤，幻覺或根本錯誤的規則則很少。在患者小案例中，可執行模型的 F1 分數達到 82.5%。結論：LLMs 可以將自然語言的 NICE 指南轉換為可解釋的、可執行的模型，保持指南結構，支持透明的檢查和修改，並生成針對患者的具體建議。這些發現展示了可擴展自動生成可計算臨床指南的可行性。

##### **Review Before Trust: Source-Grounded Integrity Gates for AI-Assisted Personal Health Records**
2608.29965v1 by Nora Girda, Adrian Groza

Large language models can convert medical documents into structured data, but plausible output may still be unsupported by the source. Persisting such output in a longitudinal health record, a record that accumulates patient information over time, therefore creates an integrity risk: unverified data may influence later summaries, trends, or preventive-care computations. We introduce an evidence-gated trust-promotion model that keeps generated data provisional until a deterministic monitor verifies it against the source document. The monitor admits a candidate for a specified downstream use only when the source contains a unique supporting quotation, the relevant fields occur within the same laboratory row, and the required provenance is preserved. The generator cannot approve its own output, missing or ambiguous evidence causes refusal, and refused candidates remain available for human review rather than being silently discarded. We implement the model in Medical DataCloud, a personal health-record application, and evaluate it through automated tests and a replay of saved extraction outputs. All 22 conformance and mutation tests pass. The replay covers nine historical laboratory PDF reports containing 102 manually labelled rows. The reports produce 97 numeric candidates: schema validation accepts all 97, an earlier packet-level evidence check accepts 94, and the hardened quotation- and row-level policy admits 72 while retaining 25 for review. The study evaluates system integrity rather than clinical correctness or clinical safety. The results demonstrate the technical feasibility of an enforceable boundary that prevents generated claims from authorizing their own reuse in a longitudinal health record.

摘要：大型語言模型可以將醫療文件轉換為結構化數據，但合理的輸出仍可能不受來源支持。因此，將這種輸出持續保留在縱向健康記錄中，即隨著時間累積患者信息的記錄，會產生完整性風險：未經驗證的數據可能影響後續的摘要、趨勢或預防護理計算。我們引入了一種證據門控的信任促進模型，該模型在確定性監控器驗證其與來源文件的對應關係之前，將生成的數據保持為臨時性。監控器僅在來源包含唯一支持引用、相關字段出現在同一實驗室行中，並且所需的來源保持不變時，才會接受候選項用於特定的下游用途。生成器不能批准自己的輸出，缺失或模糊的證據會導致拒絕，而被拒絕的候選項仍可供人類審查，而不是被靜默丟棄。我們在Medical DataCloud這個個人健康記錄應用中實施了該模型，並通過自動化測試和重播保存的提取輸出進行評估。所有22項符合性和變異測試均通過。重播涵蓋了九份包含102個手動標記行的歷史實驗室PDF報告。這些報告產生了97個數字候選項：模式驗證接受所有97個，早期的數據包級證據檢查接受94個，而加強的引用和行級政策接受72個，同時保留25個供審查。該研究評估系統的完整性，而非臨床正確性或臨床安全性。結果展示了一個可執行邊界的技術可行性，該邊界防止生成的聲明授權其在縱向健康記錄中的自我重用。

##### **Life Operators: a self-evolving framework for multiscale life modelling**
2609.00068v1 by Shuo Wang, Yike Guo

Medical AI is moving beyond recognition towards clinical dialogue and longitudinal prediction. Yet a central question remains: how would a patient's state change under intervention? Statistical models learn future observations, whereas mechanistic models describe selected processes. Neither provides a common framework for representing patient state, coupling scales or revising failed assumptions. We propose Life Operators: task-bounded mappings that define three scientific roles. Perception operators infer task-relevant biological states from multimodal observations, Evolution operators propagate these states under natural or intervention-conditioned dynamics, and Generation operators map them to measurable signals. Each role may be realised by equations, statistical models, neural networks or hybrids. Bridge operators connect components with different variables, scales and time steps. Selected operators and bridges form task-specific Operator Graphs containing the smallest set of states and mechanisms sufficient for a declared claim. This modular structure also makes scientific revision localisable. An AI co-scientist may propose changes to states, operators, bridges or graph structure, while independent evidence determines which variants are retained, restricted or retired. Over time, validated components could accumulate into broader multiscale models of the human body and provide a computational foundation for medical artificial superintelligence.

摘要：醫療人工智慧正從識別轉向臨床對話和長期預測。<lb/>然而，一個核心問題仍然存在：在介入下，病人的狀態會如何改變？<lb/>統計模型學習未來的觀察，而機制模型描述選定的過程。<lb/>兩者都未提供一個共同的框架來表示病人狀態、耦合尺度或修正失敗的假設。<lb/>我們提出生命運算符：定義三個科學角色的任務界定映射。<lb/>感知運算符從多模態觀察中推斷與任務相關的生物狀態，<lb/>演化運算符在自然或介入條件下傳播這些狀態，<lb/>而生成運算符將它們映射到可測量的信號。<lb/>每個角色可以通過方程、統計模型、神經網絡或混合體來實現。<lb/>橋接運算符連接具有不同變量、尺度和時間步長的組件。<lb/>選定的運算符和橋接形成特定任務的運算符圖，包含足以支持所聲明主張的最小狀態和機制集。<lb/>這種模組化結構也使科學修訂可定位。<lb/>人工智慧共同科學家可以提出對狀態、運算符、橋接或圖結構的變更，<lb/>而獨立證據則決定哪些變體被保留、限制或淘汰。<lb/>隨著時間的推移，經過驗證的組件可以累積成更廣泛的人體多尺度模型，並為醫療人工超智慧提供計算基礎。

##### **Scientific Agent Skills: A Library of Procedural Knowledge for Research Agents**
2609.00065v2 by Timothy Kassis, Vinayak Agarwal, Yuhuan He, Darshil Patel, Aubrey M. Brueckner

A language-model agent asked to analyse an experiment will usually return working code. Whether the analysis is defensible is a different question. A defensible analysis depends on procedural choices: which test the field accepts, which identifier namespace is authoritative, and which caveats must accompany a result. We present Scientific Agent Skills, an open library of 163 such procedures in 16 areas of practice, including genomics, cheminformatics, medical imaging, study design and scientific communication. Each skill is a directory built around a versioned, human-readable instruction file. An agent loads the file only when a task calls for it; the directory often also contains reference material and runnable scripts. We report no task-level evaluation and no host selection rate. We measure two properties of the documentation corpus: the always-resident descriptions of all 163 skills cost 7.1% of a 200,000-token window, and the median documented workflow fits within 23.9% of it, although 29 of 46 would overflow if every reference file were loaded. Openly licensed and available at https://github.com/K-Dense-AI/scientific-agent-skills.

摘要：一個被要求分析實驗的語言模型代理通常會返回可運行的代碼。分析是否合理是另一個問題。合理的分析取決於程序選擇：該領域接受哪種測試、哪個標識符命名空間是權威的，以及結果必須伴隨哪些警告。我們介紹了科學代理技能，這是一個開放的庫，包含16個實踐領域中的163種程序，包括基因組學、化學信息學、醫學影像、研究設計和科學交流。每項技能都是圍繞一個版本化的人類可讀指令文件構建的目錄。代理僅在任務需要時加載該文件；目錄通常還包含參考材料和可運行的腳本。我們報告沒有任務級別的評估和沒有主機選擇率。我們測量文檔語料庫的兩個屬性：所有163項技能的常駐描述佔用200,000個標記窗口的7.1%，而中位數的文檔工作流程則適合其中的23.9%，儘管如果每個參考文件都被加載，46個中的29個將會溢出。該庫是開放授權的，並可在https://github.com/K-Dense-AI/scientific-agent-skills獲得。

##### **Memory-First Fact-Checking: A Knowledge-Graph-Grounded Multi-Agent System for Misinformation Detection**
2608.29617v1 by Amelia Petrenciuc, Alexandru Lecu, Adrian Groza

This paper introduces a hybrid fact-checking framework that integrates Knowledge Graph-based semantic memory with adversarial multi-agent reasoning for explainable misinformation detection. The proposed system follows a memory-first, web-fallback architecture, in which input claims are initially evaluated against a dual-index Knowledge Graph through Sentence-BERT-based semantic retrieval and Natural Language Inference. When the evidence retrieved from the graph is insufficient to support a reliable decision, the framework collects information from trusted web sources and assesses it using an adversarial tribunal composed of support, contradiction, and judging agents. A graph-aware confidence mechanism combines semantic similarity, NLI confidence, and structural graph evidence to determine whether internal knowledge is sufficient, thereby reducing unnecessary web retrieval. Following verification, validated information is transformed into structured triples and incorporated into the Knowledge Graph, supporting the incremental expansion of the system's semantic memory. Experimental evaluation on a curated COVID-19 misinformation benchmark demonstrates that the proposed framework achieves an accuracy of 97.4\% and a macro-averaged F1-score of 92.6% on resolved claims, outperforming a Llama~3.3~70B baseline, which obtains an accuracy of 87.7% and a macro-averaged F1-score of 86.3%.

摘要：這篇論文介紹了一種混合事實核查框架，將基於知識圖譜的語義記憶與對抗性多代理推理相結合，以實現可解釋的錯誤信息檢測。<lb/>所提出的系統遵循記憶優先、網絡備援的架構，其中輸入的主張最初通過基於Sentence-BERT的語義檢索和自然語言推理對雙索引知識圖譜進行評估。<lb/>當從圖譜檢索到的證據不足以支持可靠的決策時，該框架會從可信的網絡來源收集信息，並使用由支持、矛盾和裁判代理組成的對抗性法庭進行評估。<lb/>一種圖感知的信心機制結合了語義相似性、NLI信心和結構圖證據，以判斷內部知識是否足夠，從而減少不必要的網絡檢索。<lb/>在驗證後，經過驗證的信息被轉換為結構化三元組，並納入知識圖譜中，支持系統語義記憶的增量擴展。<lb/>在精心策劃的COVID-19錯誤信息基準上的實驗評估表明，所提出的框架在解決的主張上達到了97.4%的準確率和92.6%的宏觀平均F1分數，超越了Llama~3.3~70B基準，其準確率為87.7%，宏觀平均F1分數為86.3%。

##### **AI Can Be Easily Persuaded in Clinical Decision Making**
2608.29453v1 by Jiayuan Zhu, Jiazhen Pan, Fenglin Liu, Minhao Hu, Junde Wu

As AI becomes increasingly integrated into clinical practice, it is playing a growing role in medical decision making. Medicine, however, is a high stakes and evidence based field, where decisions can directly affect patients' lives. It is therefore important to understand whether AI can maintain objective judgment when others try to persuade it. In this paper, we study how easily AI can be persuaded through controlled experiments. We find that professional authority, national background, institutional affiliation, claimed past performance, multiple physicians, supported clinician views, and repeated pressure can all affect AI decisions. Surprisingly, the same persuasive input changes about 10% more cases when it comes from a senior clinician than from a medical student. Simply claiming a better performance history consistently makes the physician more persuasive. More strikingly, a plausible clinician view can persuade AI away from a correct decision even when it is fabricated to support an incorrect answer. This indicates that AI can be strongly influenced by convincing support without reliably determining whether this view from the clinician is correct. Together, these findings suggest that AI can be easily persuaded by what people say, who says it, and how the opinion is presented. Therefore, it is essential for AI to maintain sound judgment under persuasion, enabling its safe and reliable use in high stakes medical decision making.

摘要：隨著人工智慧越來越多地融入臨床實踐，它在醫療決策中扮演著日益重要的角色。<lb/>然而，醫學是一個高風險且基於證據的領域，決策可能直接影響患者的生命。<lb/>因此，了解人工智慧在他人試圖說服時是否能保持客觀判斷是非常重要的。<lb/>在本文中，我們研究了人工智慧在受控實驗中被說服的難易程度。<lb/>我們發現，專業權威、國家背景、機構隸屬、聲稱的過往表現、多位醫師、支持臨床醫生的觀點以及重複的壓力都可以影響人工智慧的決策。<lb/>令人驚訝的是，當說服性輸入來自資深臨床醫生時，改變的案例比來自醫學生時多約10%。<lb/>僅僅聲稱有更好的表現歷史就能持續使醫生更具說服力。<lb/>更引人注目的是，即使一個合理的臨床觀點是捏造的以支持錯誤答案，也能使人工智慧偏離正確決策。<lb/>這表明，人工智慧可以受到令人信服的支持的強烈影響，而無法可靠地判斷這位臨床醫生的觀點是否正確。<lb/>綜合這些發現，表明人工智慧很容易受到人們所說的話、說話的人以及意見呈現方式的影響。<lb/>因此，對於人工智慧來說，在說服下保持良好的判斷能力至關重要，這使其能夠在高風險的醫療決策中安全可靠地使用。

##### **Does Latent Planning Survive Point Clouds? Action-Conditioned JEPA World Models for Geometric Observations**
2608.29434v1 by Fabio F. Oberweger, Michael Schwingshackl

JEPA world models make latent-space planning a practical route to control, but they are built almost exclusively on images. Whether latent prediction survives geometric observations is unclear: point clouds are sparse, unordered, and self-occluded, and with 0.3-15% of scene points moving, the slow-feature optimum of latent prediction compounds with the geometric shortcut of 3D self-supervision. We lift three canonical JEPA designs to point clouds, frozen-encoder, distribution-prior, and action-sensitive, and re-sense the stable-worldmodel benchmark so that only the observation differs from the image baselines. All three plan without collapse: the distribution-prior model is statistically equivalent to its re-evaluated image counterpart on every benchmark, and the action-sensitive model attains the strongest result in our controlled comparison where the most geometry moves. Probing explains why: object positions are almost perfectly linearly decodable and attention falls on the few moving points. Planning withstands heavy dropout never seen in training, though range noise defeats the thinnest scene. Geometry finally makes a commanded 3D target a natural goal interface: we construct the goal latent from the target and the current latent, at no cost in success rate, without a goal observation.

摘要：JEPA 世界模型使得潛在空間規劃成為控制的實用途徑，但它們幾乎完全基於圖像。潛在預測是否能夠在幾何觀察中生存尚不清楚：點雲是稀疏的、無序的，並且自我遮擋，並且在 0.3-15% 的場景點移動時，潛在預測的慢特徵最優解與 3D 自我監督的幾何捷徑相互影響。我們將三種典型的 JEPA 設計提升至點雲：凍結編碼器、分佈先驗和動作敏感，並重新感知穩定的世界模型基準，使得只有觀察與圖像基準不同。這三種模型都能在不崩潰的情況下進行規劃：分佈先驗模型在每個基準上與其重新評估的圖像對應物在統計上是等價的，而動作敏感模型在我們的控制比較中獲得了最強的結果，該比較中大部分幾何體都在移動。探測解釋了原因：物體位置幾乎可以完美地線性解碼，注意力集中在少數移動點上。規劃能夠承受訓練中從未見過的重度掉落，儘管範圍噪聲擊敗了最薄的場景。幾何最終使得指令性的 3D 目標成為自然的目標介面：我們從目標和當前潛在中構建目標潛在，成功率沒有成本，且不需要目標觀察。

##### **AOI-Net: Structural Face AOI-Guided Eye-Gaze Track Representation Learning for Autism Spectrum Disorder Detection**
2608.29289v1 by Zhanpei Huang, Binbin Sun, Jialiang Chen, Yiou Wang, Taochen Chen, Yuzhu Ji, Yiqun Zhang, Yiu-Ming Cheung

Eye-movement tracking has emerged as a promising non-invasive approach to Autism Spectrum Disorder (ASD) screening, with systematic differences in attentional allocation and revisit behaviors observed during socially interactive tasks. Existing computational methods typically characterize eye-movements using discrete gaze trajectories and fixation events, yielding representations dominated by short-range temporal dynamics and limiting models that primarily emphasize long-range dependencies. Meanwhile, gaze behavior is naturally organized across semantically meaningful Areas of Interest (AOIs), whose attention allocation and transitions provide important structural cues, yet their relationships are rarely modeled explicitly. To address these limitations, we propose a structural face AOI-guided Eye-Gaze Track Network (AOI-Net) that jointly models short-term temporal dynamics and AOI-level structural organization. A network gating mechanism adaptively integrates the complementary temporal and structural representations according to their contributions to gaze-behavior characterization. To mitigate the pronounced class imbalance commonly encountered between individuals with ASD and Typically Developing (TD) participants in clinical datasets, class-distribution-aware learning is further employed to facilitate discriminative embedding learning under skewed class distributions. Experiments on a unique and large-scale clinical eye-tracking database comprising eight stimulus subsets and more than 1,300 participants show that AOI-Net consistently outperforms state-of-the-art methods. The proposed framework also enables interpretable gaze-behavior modeling and provides a practical basis for scalable AI-driven ASD screening in real-world healthcare. The code is available at https://github.com/Zhanpei-ai/CIM-AOI-Net/tree/main/Code

摘要：眼動追蹤已成為自閉症譜系障礙（ASD）篩檢的一種有前景的非侵入性方法，在社交互動任務中觀察到注意力分配和重訪行為的系統性差異。現有的計算方法通常使用離散的注視軌跡和凝視事件來描述眼動，產生的表徵主要受短期時間動態的主導，限制了主要強調長期依賴性的模型。與此同時，凝視行為自然地在語義上有意義的關注區域（AOIs）中組織，其注意力分配和轉換提供了重要的結構線索，但這些關係很少被明確建模。為了解決這些限制，我們提出了一種結構性面部AOI引導的眼動追蹤網絡（AOI-Net），該網絡聯合建模短期時間動態和AOI級別的結構組織。一個網絡閘控機制根據其對凝視行為特徵化的貢獻，自適應地整合互補的時間和結構表徵。為了減輕臨床數據集中自閉症患者與典型發展（TD）參與者之間常見的明顯類別不平衡，進一步採用了類別分佈感知學習，以促進在偏斜類別分佈下的區別性嵌入學習。對一個獨特且大規模的臨床眼動追蹤數據庫進行的實驗，該數據庫包含八個刺激子集和超過1,300名參與者，顯示AOI-Net始終優於最先進的方法。所提出的框架還使得可解釋的凝視行為建模成為可能，並為現實世界醫療中可擴展的AI驅動ASD篩檢提供了實用基礎。代碼可在 https://github.com/Zhanpei-ai/CIM-AOI-Net/tree/main/Code 獲得。

##### **RACER: Reinforced Agent Collaboration for Explainable Reasoning on Knowledge Graphs**
2608.29263v1 by Yuwei Lou, Hao Hu, Yuzhou Jiang, Zongfei Zhang, Liang Wang, Jincai Liu, Jidong Ge, Xianping Tao

Large Language Models (LLMs) often suffer from hallucination and struggle with complex reasoning tasks requiring multi-hop domain knowledge. While integrating Knowledge Graphs (KGs) provides a structured and verifiable information source, current KG-enhanced LLM paradigms usually rely on single-agent path extraction and fixed prompting, lacking adaptability and facing huge search spaces. To address these challenges, we propose RACER, a Reinforced Agent Collaboration framework for Explainable Reasoning on knowledge graphs. RACER employs a semantic-aware action pruning and teacher-guided reinforcement learning mechanism to efficiently extract high-quality reasoning pathways from large-scale KGs. Furthermore, to mitigate single-path generation pitfalls, we introduce a cross-task accumulated shared memory graph paired with an attention-driven multi-path knowledge refinement module. Finally, RACER orchestrates these components through a four-role multi-agent collaboration system (GraphAgent, TemplateAgent, AnswerAgent, and CriticAgent) to dynamically refine prompts and evaluate answers. Extensive experiments on CommonsenseQA and OpenBookQA datasets demonstrate that RACER significantly outperforms state-of-the-art KG-enhanced LLM baselines with an average improvement of 5\%, offering robust and highly interpretable reasoning capabilities.

摘要：大型語言模型（LLMs）經常遭遇幻覺問題，並且在需要多跳領域知識的複雜推理任務中表現不佳。雖然整合知識圖譜（KGs）提供了一個結構化且可驗證的信息來源，但目前的KG增強LLM範式通常依賴於單一代理的路徑提取和固定提示，缺乏適應性並面臨巨大的搜索空間。為了解決這些挑戰，我們提出了RACER，一個用於知識圖譜上可解釋推理的強化代理協作框架。RACER採用語義感知的行動修剪和教師引導的強化學習機制，以有效地從大規模KG中提取高質量的推理路徑。此外，為了減輕單一路徑生成的缺陷，我們引入了一個跨任務累積共享記憶圖，並配備了一個基於注意力的多路徑知識精煉模塊。最後，RACER通過一個四角色的多代理協作系統（GraphAgent、TemplateAgent、AnswerAgent和CriticAgent）來動態精煉提示和評估答案。在CommonsenseQA和OpenBookQA數據集上的廣泛實驗表明，RACER顯著超越了最先進的KG增強LLM基準，平均改進達5\%，提供了穩健且高度可解釋的推理能力。

##### **Rate-Coding Bundle Memory: A Unified Model of Memory and Control for Symbolic Computation in the Brain**
2608.29189v1 by Teun van Gils, Rowan P. Sommers, Markus Ostarek, Peter Hagoort

We propose a neurobiologically plausible model of cognition that combines the advantages of connectionist and symbolic systems, and that can explain a wide range of cognitive phenomena. This model, called Rate-Coding Bundle Memory (RCBM), is based on the Symbolic Subsystem Hypothesis, which posits that the brain implements a symbolic subsystem within its fundamentally connectionist nature. RCBM is a hybrid model that uses rate coding to represent symbols in a continuous space, and it uses a bundle memory system to store and retrieve these symbols. The model is capable of solving a wide range of cognitive phenomena, including one-shot learning, pattern separation, and the binding problem. We argue that RCBM provides a promising framework for understanding the nature of cognition, and that it can be used to develop more sophisticated models of cognition in the future.

摘要：我們提出了一個神經生物學上合理的認知模型，結合了連結主義和符號系統的優勢，並能解釋廣泛的認知現象。這個模型稱為速率編碼束記憶（Rate-Coding Bundle Memory, RCBM），基於符號子系統假說，該假說認為大腦在其根本的連結主義本質中實現了一個符號子系統。RCBM是一個混合模型，使用速率編碼在連續空間中表示符號，並使用束記憶系統來存儲和檢索這些符號。該模型能夠解決廣泛的認知現象，包括一次性學習、模式分離和綁定問題。我們認為RCBM為理解認知的本質提供了一個有前景的框架，並且可以用來在未來開發更複雜的認知模型。

##### **An Explainable Coherence Score for Detecting Temporal Inconsistencies in Political News**
2608.29175v1 by Marius Nicusor Pantea, Adrian Groza

Temporal inconsistencies, such as mandates attributed outside their real interval, events presented as past before they occurred, or inverted causal sequences, are a form of political disinformation that evades style-based fake news detectors: a well-written article with a single wrong date carries no lexical signal of falsehood. This paper introduces the Temporal Coherence Score (TCS), a continuous, intrinsically interpretable metric that quantifies the temporal coherence of a news article, computed by a four-stage pipeline: extraction of temporal facts, construction of a temporal knowledge graph, hierarchical verification against internal consistency rules and external reference sources, and score aggregation with automatically generated explanations. Verification combines eight internal checkers derived from Allen's interval algebra with a five-level external hierarchy ranging from a locally stored reference knowledge base of 1{,}256 curated political facts to live Wikidata SPARQL queries. On a benchmark of 100 political news articles with injected temporal errors, the system reaches a precision of 0.909 at the selected operating threshold, with a single residual false positive, a profile deliberately tuned for human-in-the-loop fact-checking assistance, where false alarms are costlier than missed detections. Unlike lexical baselines that output only a binary label, every flagged article is accompanied by the inconsistency type, the entities involved, and the reference source that contradicts the claim.

摘要：時間不一致性，例如在其實際時間範圍之外歸因的命令、在事件發生之前被呈現為過去的事件，或因果序列的顛倒，是一種政治虛假信息，能夠逃避基於風格的假新聞檢測器：一篇寫得很好的文章即使有一個錯誤的日期，也不會傳遞出虛假的詞彙信號。本文介紹了時間一致性評分（Temporal Coherence Score, TCS），這是一種連續的、內在可解釋的指標，用於量化新聞文章的時間一致性，通過四個階段的流程計算得出：提取時間事實、構建時間知識圖譜、根據內部一致性規則和外部參考來源進行分層驗證，以及自動生成解釋的分數聚合。驗證結合了八個源自艾倫（Allen）區間代數的內部檢查器，以及一個五級的外部層級，範圍從本地存儲的1,256個策劃的政治事實的參考知識庫到實時的Wikidata SPARQL查詢。在對100篇注入時間錯誤的政治新聞文章進行基準測試時，該系統在選定的操作閾值下達到了0.909的精度，僅有一個殘餘的假陽性，這是一個故意調整為人機協作事實檢查輔助的配置，其中假警報的成本高於漏檢的成本。與僅輸出二元標籤的詞彙基準不同，每篇被標記的文章都附有不一致類型、涉及的實體，以及與該主張相矛盾的參考來源。

##### **Not the Same Protector: Deployment-Dependent Protective Intervention in LLMs**
2608.29136v1 by Eunna Lee, Soomyoung Lee, Jungpyo Nam, Heonjin Ha, Jamin Jung, Kyunam Choi, Sunjun Hwang, Yeonghun Kim, Seok-Jae Lim

We ask whether a model protects a user in the same way when that user speaks rather than types. Using a single distress vignette---a physical injury of unstated severity following an interpersonal conflict---we present four frontier models with matched inputs across voice, text, and raw API deployment conditions (n=30 per cell) and code each response along five binary protective indicators, including whether the model issues an explicit medical-care directive. Voice-interface responses are markedly shorter than text-interface responses for three of the four models, and protective behavior contracts alongside that compression: medical directives are at ceiling under both the API and text conditions but decline under voice for every model tested. The contraction is not reducible to length. One model produces voice and text responses of comparable length yet still drops medical directives, and another falls below ceiling between its API and voice conditions, whose responses are of nearly identical length. Under raw API access the pattern is categorical rather than partial: no model asks after the user's safety even once. These results show that protective intervention is sensitive to the surface through which a request arrives, that this sensitivity is detectable using a simple protective coding scheme, and that it is not explained by turn length alone.

摘要：我們詢問當用戶說話而不是打字時，模型是否以相同的方式保護用戶。使用一個單一的困擾情境——在一場人際衝突後發生的未明嚴重程度的身體傷害——我們展示了四個前沿模型，並在語音、文本和原始API部署條件下匹配輸入（每個單元n=30），並根據五個二元保護指標對每個回應進行編碼，包括模型是否發出明確的醫療護理指令。在四個模型中，語音介面的回應顯著比文本介面的回應短，而保護行為隨著這種壓縮而減少：在API和文本條件下，醫療指令達到上限，但在語音條件下對每個測試的模型均有所下降。這種收縮無法簡單歸因於長度。一個模型產生的語音和文本回應長度相當，但仍然未發出醫療指令，另一個模型在其API和語音條件之間的表現低於上限，這兩者的回應長度幾乎相同。在原始API訪問下，這種模式是類別性的而非部分性的：沒有模型甚至一次也未詢問用戶的安全性。這些結果顯示，保護性干預對請求到達的表面敏感，這種敏感性可以通過簡單的保護編碼方案檢測到，並且並非僅僅由回合長度解釋。

##### **Emergent Misalignment Is Not Magical**
2608.29118v1 by Mingxuan Li, Qirun Dai, Heran Wang, Chenhao Tan

Fine-tuning large language models (LLMs) on narrowly harmful datasets can lead to misalignment broadly, a phenomenon known as emergent misalignment (EM). EM poses a challenge for AI safety and our understanding of LLMs. Prior work often frames EM as an unexpected behavior, and explains it by appealing to general misalignment directions or anthropomorphizing it as acquiring an evil persona. However, the mechanisms behind these framings remain obscure. In this work, we show that EM is a predictable and data-dependent generalization phenomenon. By examining the base model's representation of EM training data and evaluation prompts, we find that evilness after EM training is highly predictable from representational distance: the closer an evaluation prompt is to training data centroid, the more evilness it elicits from EM models after training (with an average Spearman correlation of -0.73 across 12 model-dataset settings). Building upon this analysis, we further demystify EM by showing that (1) its effectiveness changes significantly based on training data format; (2) there is not a general misalignment direction that transfers across different EM models; (3) the effect of EM is fundamentally different from persona changes. Furthermore, we extend the EM generalization metric from a scalar distance to a dataset-specific generalization direction, which robustly predicts EM models' evilness under semantics-preserving prompt perturbations including appending random tokens and paraphrasing, where other methods do not reliably generalize.

摘要：微調大型語言模型（LLMs）在狹隘有害數據集上的過程可能會導致廣泛的錯位，這一現象被稱為突現錯位（EM）。EM對AI安全性及我們對LLMs的理解構成挑戰。先前的研究通常將EM視為一種意外行為，並通過訴諸一般錯位方向或將其擬人化為獲得邪惡角色來解釋。然而，這些框架背後的機制仍然不明。在這項工作中，我們顯示EM是一種可預測且依賴數據的概化現象。通過檢查基礎模型對EM訓練數據和評估提示的表徵，我們發現EM訓練後的邪惡性可以從表徵距離中高度預測：評估提示與訓練數據質心越接近，EM模型在訓練後引發的邪惡性就越高（在12個模型-數據集設置中，平均斯皮爾曼相關係數為-0.73）。基於這一分析，我們進一步揭示EM的神秘面紗，顯示（1）其有效性根據訓練數據格式顯著改變；（2）不存在一個可以在不同EM模型之間轉移的一般錯位方向；（3）EM的效果與角色變化根本不同。此外，我們將EM概化度量從標量距離擴展到數據集特定的概化方向，這在語義保持的提示擾動下穩健地預測EM模型的邪惡性，包括附加隨機標記和改寫，而其他方法則無法可靠地概化。

##### **Clustering as Approximation by Constrained Projectors: Theory and Guarantees**
2608.29102v1 by Angshul Majumdar

This paper develops a unified theoretical framework showing that a broad family of clustering methods, including k-means, fuzzy c-means, kernel k-means, kernel FCM, and spectral clustering, can all be expressed as structured low-rank projectors acting on a signal-derived matrix. By formulating each method as an instance of min over B in C of ||M - M P_B||_F^2, with different constraint sets C, we establish a common optimization template that clarifies the algebraic links among hard, fuzzy, kernel-induced, and orthonormal projections. Within this framework, we derive non-trivial theoretical results, including geodesic convexity properties on the projection manifold, perturbation bounds quantifying stability to matrix noise, and exact recovery guarantees under ideal block-model conditions. The analysis further explains when different clustering families collapse to the same optimal subspace and how deviations arise under small inter-cluster leakage. Overall, the work provides a coherent, theory-first foundation for understanding clustering through structured projectors.

摘要：這篇論文發展了一個統一的理論框架，顯示出包括 k-means、模糊 c-means、核 k-means、核 FCM 和光譜聚類在內的廣泛聚類方法，都可以表達為作用於信號衍生矩陣的結構化低秩投影器。<lb/>通過將每種方法表述為在 C 中對 B 進行最小化的 ||M - M P_B||_F^2 的一個實例，並使用不同的約束集 C，我們建立了一個共同的優化模板，澄清了硬投影、模糊投影、核引起的投影和正交投影之間的代數聯繫。<lb/>在這個框架內，我們推導出了一些非平凡的理論結果，包括投影流形上的測地線凸性質、量化矩陣噪聲穩定性的擾動界限，以及在理想區塊模型條件下的精確恢復保證。<lb/>該分析進一步解釋了不同聚類家族何時會合併到相同的最佳子空間，以及在小的群間洩漏下如何產生偏差。<lb/>總體而言，這項工作為通過結構化投影器理解聚類提供了一個連貫的、以理論為先的基礎。

##### **From the Loss Landscape to Diverse Feature Learning in Neural Networks**
2608.28948v1 by David Aram Yunis

Over the course of the last decade, neural networks have grown from an academic curiosity to moving the markets of nations. Despite this explosion in both research and deployment, relatively little is understood about how they achieve the solutions they do. This is both scientifically relevant, and pressing for society. When neural networks make decisions across self-driving, construction, law, hiring and health, there have been and will continue to be unintended consequences.
  However, attempting to generalize the failures of the largest and most important production systems makes for a very difficult task. Yet signs of these failures exist at all scales of neural networks, so we should be able to study a much more tractable setting. All neural networks must undergo an optimization process, called training, to be useful. To a great degree, understanding neural networks is understanding their optimization: through what process and exposure to which data did they arrive at their results. Yet our knowledge on this topic as a field is quite imprecise. In particular, a curious phenomenon called mode connectivity, the ability to connect neural networks in the loss surface, defies explanation entirely.
  This dissertation elucidates, explains and exploits this special structure in the loss landscape...

摘要：在過去十年中，神經網絡從學術上的好奇心成長為影響國家市場的力量。儘管在研究和應用方面都出現了爆炸性的增長，但對於它們如何達成所謂的解決方案，仍然了解得相對較少。這對科學來說既相關，也對社會來說十分迫切。當神經網絡在自駕、建設、法律、招聘和健康等領域做出決策時，已經出現並將繼續出現意想不到的後果。<lb/>然而，試圖概括最大的和最重要的生產系統的失敗是一項非常困難的任務。儘管如此，這些失敗的跡象在所有規模的神經網絡中都存在，因此我們應該能夠研究一個更易於處理的環境。所有神經網絡都必須經歷一個稱為訓練的優化過程，才能變得有用。在很大程度上，理解神經網絡就是理解它們的優化：它們是通過什麼過程以及接觸哪些數據達成結果的。然而，作為一個領域，我們在這個主題上的知識相當不精確。特別是一種被稱為模式連通性的奇特現象，即在損失表面上連接神經網絡的能力，完全無法解釋。<lb/>這篇論文闡明、解釋並利用了損失景觀中的這一特殊結構...

##### **Toward Postural State Classification in Immersive VR with Multimodal Data and Explainability Analysis**
2608.28844v1 by Nipa Anjum, Md Irfan Pavel, Robert Gonzalez, Kevin Desai, Alberto Cordova, M. Rasel Mahmud, John Quarles

Ensuring a safe virtual reality (VR) experience requires systems that can predict and respond when users lose their balance. Although prior work has examined fall prediction and motion sickness, many approaches are regression-based and postural state classification remains less explored. This study compares machine learning (ML) and deep learning (DL) models for classifying postural states in VR under visual perturbations. We used a multimodal dataset containing kinematic, electromyographic (EMG), and electrodermal activity (EDA) signals. The data were prepared for a binary task to distinguish balanced from imbalanced postural states, and participant-wise downsampling addressed class imbalance. All models were evaluated with Leave-One-Participant-Out (LOPO) cross-validation to test generalization to unseen participants. Among the models, the Mamba-inspired CNN (MI-CNN) achieved the highest accuracy of 96.76%. SHapley Additive exPlanations (SHAP) analysis improved interpretability and identified the most influential classification factors. The SHAP results showed that kinematic features were dominant, indicating that body-motion patterns are informative for detecting imbalance in VR. We also evaluated MI-CNN using only the top two-thirds of features ranked by SHAP importance. Despite a 33% reduction in input dimensionality, the model maintained performance, achieving 0.957 accuracy and 0.957 F1-score, with about a 1% decrease compared with the full-feature model. These findings suggest that multimodal sensing, temporal deep learning, and explainable AI can support reliable classification of balance-related instability in VR. Accurate recognition of imbalanced postural states may raise awareness of fall risk and guide safer, adaptive VR systems that respond to instability while improving user safety and experience. Code is available at: https://github.com/NipaAnjum/MI-CNN.

摘要：確保安全的虛擬實境 (VR) 體驗需要能夠預測並回應使用者失去平衡的系統。雖然先前的研究已經檢視了跌倒預測和運動病，但許多方法是基於回歸的，且姿勢狀態分類仍然較少被探索。本研究比較了機器學習 (ML) 和深度學習 (DL) 模型在視覺擾動下對 VR 中姿勢狀態的分類。我們使用了一個多模態數據集，包含運動學、肌電圖 (EMG) 和皮膚電活動 (EDA) 信號。數據被準備為一個二元任務，以區分平衡和不平衡的姿勢狀態，並通過參與者逐一下採樣來解決類別不平衡。所有模型均使用留一參與者法 (LOPO) 交叉驗證進行評估，以測試對未見參與者的泛化能力。在這些模型中，受 Mamba 啟發的 CNN (MI-CNN) 達到了最高的準確率 96.76%。SHapley 加法解釋 (SHAP) 分析提高了可解釋性，並識別了最具影響力的分類因素。SHAP 結果顯示運動學特徵佔主導地位，表明身體運動模式對於檢測 VR 中的不平衡具有信息價值。我們還僅使用 SHAP 重要性排名的前三分之二特徵來評估 MI-CNN。儘管輸入維度減少了 33%，該模型仍保持了性能，達到 0.957 的準確率和 0.957 的 F1 分數，與全特徵模型相比僅下降約 1%。這些發現表明，多模態感知、時間深度學習和可解釋的人工智慧可以支持對 VR 中平衡相關不穩定性的可靠分類。準確識別不平衡的姿勢狀態可能提高對跌倒風險的認識，並指導更安全、適應性的 VR 系統，這些系統在應對不穩定性時能提高使用者的安全性和體驗。代碼可在以下網址獲得：https://github.com/NipaAnjum/MI-CNN。

##### **Explainable Artificial Intelligence (XAI) in Computational Pathology: Definitions, Taxonomy, and Recommendations**
2608.28820v1 by Shubham Innani, Suhang You, Adam Shephard, Bhakti Baheti, Francesco Ciompi, Joe Yeong, Nasir Rajpoot, Michael Feldman, Solene Florence Kammerer-Jacquet, Dimitrios Makris, Geert Litjens, Anne L. Martel, Jana Lipkova, April Khademi, Spyridon Bakas, for the MICCAI SIG-CompPath

Computational pathology (CompPath) is transforming medicine by leveraging artificial intelligence (AI) algorithms to support diagnosis, prognosis, and treatment prediction from gigapixel whole-slide images. Clinical adoption is progressing, but is constrained by concerns about safety, accountability, and regulatory oversight in high-stakes clinical environments. Explainable AI (XAI) systems hold promise for building trust and enabling verification, yet the literature remains fragmented due to inconsistent terminology, overlapping methodological families, ad hoc validation, and current reviews. This review aims to formalize XAI methods in CompPath through the: i) introduction of a pathology-centric vocabulary comprising seven core terms; ii) development of a taxonomy across methodological families and three orthogonal axes (stage, type, scope); and iii) establishment of a task-driven framework that maps five clinical questions to recommended methods, method evaluation, and deployment context. Five key gaps between current XAI capabilities and clinical deployment are identified, and actionable steps are proposed to advance XAI for CompPath.

摘要：計算病理學（CompPath）正在通過利用人工智慧（AI）算法來支持從千兆像素全幻燈片圖像中進行診斷、預後和治療預測，徹底改變醫學。臨床採用正在進行，但受到對安全性、責任和在高風險臨床環境中監管監督的擔憂所限制。可解釋的人工智慧（XAI）系統在建立信任和實現驗證方面具有潛力，但由於術語不一致、方法論家族重疊、臨時驗證以及當前的評估文獻，文獻仍然支離破碎。本次綜述旨在通過以下方式正式化CompPath中的XAI方法：i）引入由七個核心術語組成的以病理為中心的詞彙；ii）在方法論家族和三個正交軸（階段、類型、範圍）之間發展一個分類法；以及iii）建立一個以任務為驅動的框架，將五個臨床問題映射到推薦的方法、方法評估和部署背景。確定了當前XAI能力與臨床部署之間的五個關鍵差距，並提出可行的步驟以推進CompPath的XAI。

##### **PermitGPT: A Unified Generative-AI Pipeline for Construction Hazard Forecasting, Permit Prediction, and Community Impact**
2608.28728v1 by Mohd Ruhul Ameen, Farjana Aktar, Akif Islam, Momen Khandoker Ope, Abu Saleh Musa Miah, Jungpil Shin

Urban construction governance requires early decisions that connect workplace safety, permitting requirements, and community impact, yet the relevant evidence is often scattered across separate municipal and regulatory data sources. This paper presents PermitGPT, a unified generative artificial intelligence framework for converting unstructured construction permit descriptions into structured decision-support outputs across three domains: safety hazard identification, permit requirement specification, and community impact assessment. To address data fragmentation, we spatially and temporally align records from the New York City Department of Buildings, Occupational Safety and Health Administration, and NYC 311 service requests, producing 90,000 structured prompt-response pairs derived through rule-based alignment and domain-informed spot checking. We fine-tune three open-weight language models using parameter-efficient adaptation and evaluate them on 2,833 held-out test cases. The results show complementary model behavior: Gemma-3-1B provides the most efficient inference at 3.07 samples per second with low memory usage, Llama-3.2-3B gives the highest lexical overlap for regulatory-style outputs with a BLEU score of 0.0091, and 4-bit Mistral-7B-Instruct-v0.3 achieves the strongest semantic alignment with a BERTScore-F1 of 0.7747. Because the task involves open-ended structured generation, low BLEU values are interpreted alongside semantic metrics and qualitative output structure rather than as standalone indicators of utility. Overall, PermitGPT provides an initial step toward AI-assisted construction governance while identifying directions for stronger task-level evaluation and real-world validation.

摘要：城市建設治理需要早期決策，以連結工作場所安全、許可要求和社區影響，然而相關證據往往散佈在不同的市政和監管數據來源中。本文提出了PermitGPT，一個統一的生成式人工智慧框架，用於將非結構化的建設許可描述轉換為三個領域的結構化決策支持輸出：安全危害識別、許可要求規範和社區影響評估。為了解決數據碎片化的問題，我們從紐約市建築部、職業安全健康管理局和NYC 311服務請求中空間和時間上對齊記錄，通過基於規則的對齊和領域知識的抽查產生了90,000對結構化的提示-回應對。 我們使用參數高效的適應對三個開放權重語言模型進行微調，並在2,833個保留測試案例上進行評估。結果顯示出互補的模型行為：Gemma-3-1B在每秒3.07個樣本的低記憶體使用率下提供了最高效的推理，Llama-3.2-3B對於監管風格的輸出提供了最高的詞彙重疊，BLEU分數為0.0091，而4-bit Mistral-7B-Instruct-v0.3則以0.7747的BERTScore-F1達到最強的語義對齊。由於該任務涉及開放式結構生成，低BLEU值應與語義指標和定性輸出結構一起解釋，而不是作為效用的獨立指標。總體而言，PermitGPT為AI輔助建設治理提供了一個初步步驟，同時確定了更強的任務級評估和現實世界驗證的方向。

##### **When Linguistic and Internal Confidence Diverge in Large Language Models**
2608.28382v1 by Hefan Zhang, Bingquan Zhang, Ming Cheng, Saeed Hassanpour, Weicheng Ma, Soroush Vosoughi

Users often ask large language models (LLMs) to report how confident they are, but it is unclear whether such linguistic confidence tracks the model's internal confidence. We study this question across 8 classification tasks, 2 generation tasks and 30 models from three families. For classification, we compare linguistic confidence with logits-based confidence along three axes: association, magnitude agreement and calibration. For generation, we test whether linguistic confidence tracks semantic-entropy-based uncertainty. The axes frequently diverge. Instance-level association is weak on average, although it improves on easier items and for stronger base models. Instruction-tuned models often report higher confidence and sometimes show higher association, but they also have larger confidence gaps and worse calibration. Prompt design mostly changes the distribution of reported confidence. Attitude cues inflate confidence without improving alignment, while score exemplars can preserve rank-order signal when they avoid collapsed confidence values. Regression analyses show that distributional properties of confidence scores explain much of the observed alignment pattern, with model metadata playing a smaller role after controls. These results support a lossy-channel view of linguistic confidence. A more dispersed verbal confidence distribution can carry useful rank information, but it does not make the scores calibrated. Linguistic confidence should therefore be evaluated with multi-axis diagnostics before being used in downstream reliability pipelines.

摘要：使用者經常要求大型語言模型（LLMs）報告它們的信心程度，但尚不清楚這種語言信心是否反映了模型的內部信心。我們在8個分類任務、2個生成任務和來自三個家族的30個模型中研究這個問題。對於分類，我們從三個軸線比較語言信心與基於logits的信心：關聯性、大小一致性和校準性。對於生成，我們測試語言信心是否跟踪基於語義熵的不確定性。這些軸線經常出現分歧。實例級的關聯性平均而言較弱，儘管在較簡單的項目和較強的基模型上有所改善。經過指令調整的模型通常報告更高的信心，有時顯示更高的關聯性，但它們的信心差距也較大，且校準性較差。提示設計主要改變報告信心的分佈。態度提示會膨脹信心而不改善對齊，而分數範例在避免崩潰的信心值時可以保留排名信號。回歸分析顯示，信心分數的分佈特性解釋了觀察到的對齊模式的大部分，模型元數據在控制後的作用較小。這些結果支持語言信心的有損通道觀點。更分散的語言信心分佈可以攜帶有用的排名信息，但並不使得分數得到校準。因此，在用於下游可靠性管道之前，應該使用多軸診斷來評估語言信心。

##### **AI as Teammate: Rethinking Task Distribution in Medical Training**
2608.28373v1 by Fendi Tsim, Alina Gutoreva, Anthony Weiss, Nicole Dubosh

Integrating Artificial Intelligence (AI), particularly generative AI, into medical training has prompted concerns about learner over-reliance, misuse, and erosion of foundational clinical competencies. We propose a conceptual reframing at the decision level: the problem is not misuse but misclassification - a mechanistic failure of real-time metacognitive evaluation in selecting a subzone-inappropriate AI interaction mode. Drawing on "SCAN" (Substitute, Complement, Aid, Non-Negotiable), a human-centric decision-making framework for generative AI task allocation grounded in Vygotsky's Zone of Proximal Development and metacognition, we advance the emerging social-constructivist conversation around AI in medical education by offering a testable account of AI's role in clinical reasoning development. This framework yields testable predictions for how misclassification can be detected, mitigated, and, more importantly, prevented in the clinical learning environment. Regarding clinical reasoning development, we show how trajectories of skill acquisition (upskilling) and failure (the triad of skill failure: de-skilling, never-skilling, and mis-skilling) operate at the individual task level in ways that fixed-phase, cohort-wide treatments fail to capture. We further identify passive engagement within correctly classified AI-scaffolded tasks as a particularly insidious, detection-resistant pathway to mis-skilling - one requiring subzone re-identification from AI assistance to expert assistance, with human experts serving as epistemic auditors. The paper operationalizes SCAN for clinical curriculum design, supervision, and assessment, and opens an empirical research agenda grounded in cognitive science. This paradigm shift from misuse to misclassification is not semantic: it offers educators a clear perspective on what to look for, what to assess, and what to intervene on.

摘要：整合人工智慧（AI），特別是生成式AI，進入醫學訓練引發了對學習者過度依賴、誤用和基礎臨床能力侵蝕的擔憂。<lb/>我們提出在決策層面上的概念重構：問題不是誤用，而是誤分類——在選擇不適當的AI互動模式時，實時的元認知評估發生了機械性失敗。<lb/>借鑒“SCAN”（替代、補充、輔助、不可談判），這是一個以人為中心的生成式AI任務分配決策框架，基於維果斯基的最近發展區和元認知，我們推進了關於AI在醫學教育中角色的社會建構主義對話，提供了一個可測試的關於AI在臨床推理發展中角色的說明。<lb/>這一框架產生了可測試的預測，關於如何在臨床學習環境中檢測、減輕，更重要的是預防誤分類。<lb/>關於臨床推理發展，我們展示了技能獲得（提升技能）和失敗（技能失敗的三元組：技能退化、從未獲得技能和錯誤獲得技能）的軌跡如何在個別任務層面運作，這是固定階段、群體範圍的處理無法捕捉的。<lb/>我們進一步確定在正確分類的AI支撐任務中被動參與是一條特別隱蔽、抗檢測的誤技能途徑——這需要從AI輔助轉變為專家輔助的子區域重新識別，並且人類專家擔任認知審計者。<lb/>本文將SCAN運用於臨床課程設計、監督和評估，並開啟了一個基於認知科學的實證研究議程。<lb/>從誤用到誤分類的這一範式轉變並非語義上的：它為教育者提供了一個清晰的視角，告訴他們應該尋找什麼、評估什麼以及應該介入什麼。

##### **AGENT-O: A Semantic Agent Card Framework for Interoperable and Governed Healthcare AI Agents**
2608.28345v1 by Pengze Li, Cui Tao

AGENT-O is a modular ontology framework that defines a semantic Agent Card for representing health-oriented AI agent systems and supports assessment of reporting completeness in scientific publications. AGENT-O was developed as an OWL 2/RDF ontology covering runtime, models, workflow, tools, clinical use, evaluation, provenance, governance, and reporting assessment. Evaluation included ontology inventory, OWL-RL reasoning, three SHACL suites, 12 SPARQL competency queries, three cases, and model-assisted reporting-completeness assessment of 279 papers across five dimensions. The ontology contained 1,962 RDF triples and 1,922 Protege axioms, with 252 active classes, 198 active object properties, and 51 datatype properties. All SHACL suites conformed on example graphs, all competency queries returned prespecified evidence, and all 279 papers were scored. Incomplete reporting was highest for runtime/architecture (84.6%), governance/safety (82.8%), and provenance/reproducibility (78.1%), compared with evaluation (25.8%) and benchmark-process alignment (29.8%). AGENT-O supported semantic Agent Card representation and reporting assessment while revealing an evaluation-specification gap: evaluation and benchmark procedures were reported more consistently than runtime architecture, governance, and reproducibility. AGENT-O provides a reusable ontology, semantic Agent Card profile, and reporting-completeness workflow for structured reporting and gap identification, but does not assess agent quality or deployment readiness.

摘要：AGENT-O 是一個模組化的本體框架，定義了一個語義代理卡，用於表示以健康為導向的 AI 代理系統，並支持對科學出版物報告完整性的評估。AGENT-O 被開發為一個 OWL 2/RDF 本體，涵蓋了運行時、模型、工作流程、工具、臨床使用、評估、來源、治理和報告評估。評估包括本體清單、OWL-RL 推理、三個 SHACL 套件、12 個 SPARQL 能力查詢、三個案例，以及對 279 篇論文在五個維度上的模型輔助報告完整性評估。該本體包含 1,962 個 RDF 三元組和 1,922 個 Protege 公理，擁有 252 個活躍類別、198 個活躍對象屬性和 51 個數據類型屬性。所有 SHACL 套件在示例圖上均符合，所有能力查詢均返回預先指定的證據，且所有 279 篇論文均已評分。運行時/架構（84.6%）、治理/安全（82.8%）和來源/可重複性（78.1%）的報告不完整性最高，而評估（25.8%）和基準過程對齊（29.8%）則較低。AGENT-O 支持語義代理卡表示和報告評估，同時揭示了評估規範的差距：評估和基準程序的報告一致性高於運行時架構、治理和可重複性。AGENT-O 提供了一個可重用的本體、語義代理卡配置文件和報告完整性工作流程，用於結構化報告和差距識別，但不評估代理質量或部署準備性。

##### **Gen-TAS: A Generative AI-Aided Hardware-Software Task Allocation Framework for FPGA-GPP Heterogeneous Systems**
2608.28160v1 by Mary Kong, Yuqin Zhao, Semih Vazgecen, Cristian Sestito, Themis Prodromakis

FPGA-GPP heterogeneous systems combine software flexibility with the performance and energy efficiency of reconfigurable hardware. However, determining which application tasks should execute on the GPP or FPGA requires extensive expertise and design-space exploration, particularly when user objectives vary across latency, communication, resource utilisation, and power. This paper proposes Gen-TAS, a knowledge-grounded LLM framework for user-specific FPGA-GPP task allocation. By combining task-graph analysis with RAG, Gen-TAS grounds LLM reasoning in historical implementation knowledge and generates multiple explainable strategies tailored to the specified objectives. Human-in-the-loop selection and a deterministic backend connect LLM-generated decisions to reproducible FPGA SoC implementations. Experiments on CNN and SDR workloads across multiple LLMs demonstrate stable, requirement-driven allocation. Under latency-oriented objectives, implementations following the selected strategies achieve speedups of up to 2.45$\times$ and 92.53$\times$, respectively, relative to the corresponding all-GPP baselines while other objectives select strategies that trade some acceleration performance for FPGA-GPP communication, resource utilisation, or FPGA power.

摘要：FPGA-GPP 異構系統結合了軟體的靈活性與可重配置硬體的性能和能源效率。<lb/>然而，確定哪些應用任務應在 GPP 或 FPGA 上執行需要廣泛的專業知識和設計空間探索，尤其是在用戶目標在延遲、通信、資源利用和功耗上有所不同的情況下。<lb/>本文提出了 Gen-TAS，一個基於知識的 LLM 框架，用於用戶特定的 FPGA-GPP 任務分配。<lb/>通過將任務圖分析與 RAG 結合，Gen-TAS 將 LLM 推理基於歷史實現知識，並生成多種針對特定目標的可解釋策略。<lb/>人機互動的選擇和確定性後端將 LLM 生成的決策與可重現的 FPGA SoC 實現相連接。<lb/>在多個 LLM 上對 CNN 和 SDR 工作負載的實驗顯示出穩定的、需求驅動的分配。<lb/>在以延遲為導向的目標下，根據所選策略的實現分別達到高達 2.45$\times$ 和 92.53$\times$ 的加速，相對於相應的全 GPP 基準，而其他目標則選擇在 FPGA-GPP 通信、資源利用或 FPGA 功耗上進行一些加速性能的權衡策略。

##### **Explainable Uncertainty Estimation for Reliable Medical AI**
2608.28052v1 by Li Rong Wang, Jamie Duell, Xinran Xu, Thomas C. Henderson, Yu Yue Hew, Pik Wan Erica Chiang, Xiao Wei Alstar Ang, Bingwen Eugene Fan, Xiuyi Fan

Artificial intelligence has strong potential to support clinical decision-making, yet its adoption in healthcare remains limited due to a lack of trust. Uncertainty estimation can signal unreliable predictions, and explainable AI (XAI) can clarify how predictions are made but existing methods treat them separately, providing no feature-level insight into why a prediction is uncertain or which tests to prioritize to reduce it. To address this gap, we propose explainable uncertainty estimation, which unifies uncertainty estimation and XAI to both quantify uncertainty and explain feature-level contributions. We introduce the Expected Gradients Reconstruction Uncertainty Estimate (egRUE), which incorporates prediction explanations into its uncertainty computation and decomposes uncertainty into feature-wise contributions. We prove theoretical properties of egRUE and show through experiments that it improves reliability and interpretability compared to existing methods. A user study with medical experts further demonstrates that egRUE's explanations improve calibrated trust over uncertainty scores alone, increasing confidence in correct predictions and reducing confidence in incorrect ones. By combining prediction uncertainty with feature-level explanations, egRUE strengthens decision-making support in safety-critical healthcare settings, clarifying both when predictions may be unreliable and which features drive that uncertainty.

摘要：人工智慧在支持臨床決策方面具有強大的潛力，但由於缺乏信任，其在醫療保健中的採用仍然有限。<lb/>不確定性估計可以指示不可靠的預測，而可解釋的人工智慧（XAI）可以澄清預測是如何產生的，但現有的方法將它們分開處理，未提供關於為何預測不確定或應優先考慮哪些測試以減少不確定性的特徵級見解。<lb/>為了解決這一空白，我們提出了可解釋的不確定性估計，該方法統一了不確定性估計和XAI，以量化不確定性並解釋特徵級貢獻。<lb/>我們引入了期望梯度重建不確定性估計（egRUE），它將預測解釋納入其不確定性計算中，並將不確定性分解為特徵級貢獻。<lb/>我們證明了egRUE的理論性質，並通過實驗顯示它在可靠性和可解釋性方面優於現有方法。<lb/>與醫療專家的用戶研究進一步證明，egRUE的解釋提高了對不確定性分數的校準信任，增強了對正確預測的信心，並減少了對錯誤預測的信心。<lb/>通過將預測不確定性與特徵級解釋相結合，egRUE增強了在安全關鍵的醫療保健環境中的決策支持，澄清了預測何時可能不可靠以及哪些特徵驅動了該不確定性。

##### **Compared to What? A Human-Anchored Security Benchmark for LLM-Generated Infrastructure-as-Code**
2608.28021v1 by Animesh Shaw

Large language models are increasingly used to author Infrastructure-as-Code (IaC), where a single insecure default can be deployed directly into production. Prior evaluations report raw vulnerability counts for model-generated IaC, but without a human baseline they cannot determine whether models are actually worse than engineers. We introduce GenIaC-SecBench, a benchmark of 100 deployment scenarios stratified by architectural complexity, evaluated across 12 model configurations from four vendors, producing 1,196 IaC artifacts scanned by three independent policy engines (Checkov, Trivy, KICS). Critically, we also scan 634 human-authored IaC templates with the same toolchain, providing the first size-matched human security baseline.
  Vulnerability density is strongly inverse to artifact size (Spearman $ρ= -0.55$, $p < 10^{-77}$), meaning unmatched comparisons measure size rather than security. When matched on declared-resource count, all model configurations fall within 3.21x--3.87x the human vulnerability density, with the gap widening for simpler tasks (4.9x at one resource, 1.4x at twenty or more).
  We decompose reasoning into standard generation, prompt-engineered chain-of-thought, and vendor extended-thinking APIs. Vendor extended thinking significantly outperforms prompted chain-of-thought ($-12.0\%$, $p = 0.0013$), while prompted chain-of-thought is indistinguishable from standard generation ($-1.3\%$, n.s.). Token instrumentation shows extended thinking uses under 1\% of the output budget, explaining its bounded effect.
  Two negative results also emerge: deployability does not correlate with vulnerability ($r = 0.158$, $p = 0.625$), and classical complete-case Friedman testing is infeasible for realistic benchmark designs, motivating the Skillings-Mack statistic. All code, data, and regeneration scripts are released.

摘要：大型語言模型越來越多地被用來編寫基礎設施即代碼（IaC），其中一個不安全的預設可以直接部署到生產環境中。先前的評估報告了模型生成的IaC的原始漏洞數量，但在沒有人工基準的情況下，無法確定模型是否真的比工程師更差。我們引入了GenIaC-SecBench，這是一個包含100個按架構複雜性分層的部署場景的基準，評估了來自四個供應商的12種模型配置，產生了1,196個由三個獨立政策引擎（Checkov、Trivy、KICS）掃描的IaC工件。關鍵的是，我們還使用相同的工具鏈掃描了634個人類編寫的IaC模板，提供了第一個大小匹配的人類安全基準。
漏洞密度與工件大小呈強烈的反比關係（Spearman $ρ= -0.55$, $p < 10^{-77}$），這意味著不匹配的比較測量的是大小而非安全性。當根據聲明的資源數量進行匹配時，所有模型配置的漏洞密度均在人類漏洞密度的3.21倍至3.87倍之內，對於更簡單的任務，這一差距擴大（在一個資源時為4.9倍，在二十個或更多資源時為1.4倍）。
我們將推理分解為標準生成、提示工程的思考鏈和供應商擴展思維API。供應商擴展思維顯著優於提示的思考鏈（$-12.0\%$, $p = 0.0013$），而提示的思考鏈與標準生成無法區分（$-1.3\%$, n.s.）。標記儀器顯示擴展思維使用的輸出預算不到1\%，解釋了其有限的效果。
還出現了兩個負面結果：可部署性與漏洞之間沒有相關性（$r = 0.158$, $p = 0.625$），而經典的完整案例Friedman測試對於現實基準設計是不可行的，這促使了Skillings-Mack統計量的提出。所有代碼、數據和再生腳本均已發布。

##### **The Illusion of $\textit{What If}$: Evaluating the Breakdown of Counterfactual Reasoning in LLMs**
2608.27953v1 by Yucheng Wang, Yuetian Du, Zhengyi Liu, Rongyu Zhang, Bing Zhao, Boyu Yang, Ming Kong, Lin Qu, Hu Wei, Jie Liu, Qiang Zhu

Counterfactual reasoning requires models to reason beyond the observed world and explain how altered conditions propagate through downstream consequences. Existing benchmarks largely target bounded settings with fixed variables or single gold outcomes, overlooking open-domain scenarios requiring causal-process evaluation. To this end, we present $\textbf{WhatIfBench}$, a diagnostic benchmark for open-domain, open-form, long-horizon counterfactual causal reasoning, containing 220 what-if questions across STEM, HSS, and Hybrid scenarios. To evaluate free-form responses, we further propose $\textbf{PRISM}$, which first converts each natural-language explanation into a Response-Derived Semantic Causal Graph of events, states, and mechanisms. On top of this graph, PRISM then jointly applies a Process Metric assessing graph-level causal validity and a Rubric Metric assessing answer-level explanatory adequacy. Evaluating six frontier LLMs with this framework, we find that WhatIfBench remains far from saturated: even the strongest model reaches only a 64.62% final score. Further analysis reveals persistent causal gaps, premise drift, and topology fragmentation, suggesting that fluent counterfactual narratives often mask fragile causal processes. The benchmark, code, and evaluation scripts are available at $\href{https://github.com/zju-gt/WhatIfBench}{WhatIfBench}$.

摘要：反事實推理需要模型超越觀察到的世界進行推理，並解釋如何改變條件在後續結果中傳播。現有的基準主要針對固定變量或單一金標結果的有限環境，忽略了需要因果過程評估的開放領域場景。為此，我們提出了 $\textbf{WhatIfBench}$，這是一個針對開放領域、開放形式、長期反事實因果推理的診斷基準，包含了220個涉及STEM、HSS和混合場景的假設性問題。為了評估自由形式的回應，我們進一步提出了 $\textbf{PRISM}$，它首先將每個自然語言解釋轉換為事件、狀態和機制的回應衍生語義因果圖。在這個圖的基礎上，PRISM然後共同應用一個過程度量來評估圖層級的因果有效性，以及一個評分度量來評估答案層級的解釋充分性。使用這個框架評估六個前沿的LLM，我們發現WhatIfBench仍然遠未飽和：即使是最強的模型最終得分也僅為64.62%。進一步分析顯示持續的因果差距、前提漂移和拓撲碎片化，這表明流利的反事實敘述往往掩蓋脆弱的因果過程。該基準、代碼和評估腳本可在 $\href{https://github.com/zju-gt/WhatIfBench}{WhatIfBench}$ 獲得。

##### **Actionable CBFI: Integrating Structural Decomposition and Causal Counterfactual Recourse for Tabular Machine Learning**
2608.27821v1 by Sejong Oh

Explainable artificial intelligence (XAI) increasingly calls for actionable counterfactual recourse, yet current methodologies face challenges related to causal invalidity, excessive cognitive burden, and predictive failure. Exhaustive causal search algorithms often require modifications to multiple attributes, whereas additive attribution-guided methods, such as SHAP, ignore higher-order feature synergies, leading to suboptimal predictive momentum and diffuse intervention effort in complex nonlinear models, such as XGBoost. To bridge this gap, we introduce actionable case-based feature importance (A-CBFI), a diagnosis-prescription integrated framework for tabular machine learning. Grounded in structural causal models (SCMs), A-CBFI isolates synergistic interaction bottlenecks and releases suppressive structural locks, translating them into targeted interventions. By mathematically separating the active user intervention space (L_{\mathrm{active}}) from downstream effects and concentrating over 98.3% of the intervention effort on diagnosed root causes, A-CBFI enables highly targeted interventions. Empirical evaluations across the financial and healthcare domains demonstrate that A-CBFI reduces the active human intervention burden by 76.9% while maintaining comparable global recourse cost to exhaustive causal baselines. By prioritizing the diagnosed causal bottlenecks, A-CBFI provides targeted and actionable recourse while maintaining causal validity and achieving full relative convergence across all causally feasible instances.

摘要：可解釋的人工智慧（XAI）越來越需要可行的反事實補救措施，但當前的方法面臨因果無效性、過度認知負擔和預測失敗等挑戰。徹底的因果搜索演算法通常需要對多個屬性進行修改，而加法歸因引導的方法，如SHAP，則忽略了高階特徵的協同作用，導致在複雜非線性模型（如XGBoost）中預測動量不佳和干預努力分散。為了填補這一空白，我們引入了可行的基於案例的特徵重要性（A-CBFI），這是一個針對表格機器學習的診斷-處方整合框架。A-CBFI基於結構因果模型（SCMs），隔離協同互動瓶頸並釋放抑制性結構鎖，將其轉化為針對性的干預。通過數學上將主動用戶干預空間（L_{\mathrm{active}}）與下游效應分開，並將超過98.3%的干預努力集中在診斷出的根本原因上，A-CBFI實現了高度針對性的干預。在金融和醫療保健領域的實證評估表明，A-CBFI將主動人類干預負擔減少了76.9%，同時保持與徹底因果基準相當的全球補救成本。通過優先考慮診斷出的因果瓶頸，A-CBFI提供了針對性和可行的補救措施，同時保持因果有效性並在所有因果可行的實例中實現完全相對收斂。

##### **Compositional Failure in Audio-Visual LLMs: Late-Layer Prior Dominance Under Cross-modal Conflict**
2608.27785v1 by Adarsh Sudheer, David Li, Omar Elbanna, Ishaan Kodarapu, Arjun Bahuguna, Vasu Sharma

We study audio-visual conflict as a compositional generalization test for AV-LLMs: the model must combine synchronized but semantically incompatible audio and video evidence and decide whether the pair matches. On VideoLLaMA 2-7B-AV, three alignment configurations remain nearchance on the scored exact-string Yes/No subset of AVHBench, even though their output priors shift substantially. Similarly, off-the-shelf InternVideo2 experienced a 32.3% accuracy decrease specifically under cross-modal conflict, accompanied by a 17.3% instruction-following failure. We call this failure mode prior dominance: late-layer commitment to an internally preferred answer pattern that is weakly grounded in the conflicting inputs. To explain this behavior, we conduct a mechanistic interpretability analysis and find that commitment remains concentrated at 25.5 $\pm$ 1 layers. We show that stronger temporal alignment changes answer bias, but do not improve compositional conflict resolution. Code and data to reproduce our mechanistic audit and behavioral evaluations are available at https://github.com/AdarshSudheer09/AVHBench-dmai.

摘要：我們研究音視覺衝突作為AV-LLMs的組合概括測試：模型必須結合同步但語義不相容的音頻和視頻證據，並決定這對是否匹配。 在VideoLLaMA 2-7B-AV上，三種對齊配置在AVHBench的精確字符串是/否子集上仍然接近隨機，儘管它們的輸出先驗有顯著變化。 類似地，現成的InternVideo2在跨模態衝突下經歷了32.3%的準確率下降，伴隨著17.3%的遵循指令失敗。 我們稱這種失敗模式為先驗主導：對內部偏好的答案模式的晚層承諾，這種模式在衝突輸入中基礎薄弱。 為了解釋這種行為，我們進行了機械可解釋性分析，發現承諾仍然集中在25.5 $\pm$ 1層。 我們表明，較強的時間對齊改變了答案偏見，但並未改善組合衝突的解決。 用於重現我們的機械審計和行為評估的代碼和數據可在 https://github.com/AdarshSudheer09/AVHBench-dmai 獲得。

##### **Efficient Auto-Interpretability of AI Models in Biology**
2608.27754v1 by Piotr Jedryszek, Oliver M. Crook

Sparse autoencoders (SAEs), and other interpretability methods could turn AI models in Biology and other fields into engines of scientific discovery by explaining the superhuman capabilities of those models. However, a latent is only useful if we know three things: whether it is coherent, whether it can be described, and whether that description has predictive power. These questions are routinely conflated. We assemble them into a single pipeline and report the practical innovations each stage required. First, cross-seed dictionary stability prioritises which latents are worth spending resources to investigate. Second, an intruder-detection task asks whether a latents activating examples share a recognizable pattern. Third, a separate pass proposes a candidate biological description which we convert into falsifiable predictions which can be tested in silico. Deployed on the Boltz-1 Pairformer trunk, stability prioritisation finds interpretable latents using about 4.4 times fewer latent evaluations each, and at 5.2 times lower measured cost, while recovering over half of them, and the external check shows the surfaced motifs are significantly enriched for their claimed annotations. The results also suggest a possible tension: the cross- seed stability might be selecting for some types of features, like structure-related ones, much more than others, such as function-related features.

摘要：稀疏自編碼器（SAEs）和其他可解釋性方法可以通過解釋這些模型的超人能力，將生物學及其他領域的 AI 模型轉變為科學發現的引擎。<lb/>然而，潛在變量只有在我們知道三件事的情況下才有用：它是否一致，它是否可以被描述，以及該描述是否具有預測能力。<lb/>這些問題通常被混淆。我們將它們組合成一個單一的流程，並報告每個階段所需的實際創新。<lb/>首先，交叉種子字典穩定性優先考慮哪些潛在變量值得投入資源進行調查。<lb/>其次，入侵者檢測任務詢問潛在變量激活的例子是否共享可識別的模式。<lb/>第三，單獨的通過提出一個候選的生物描述，我們將其轉換為可檢驗的預測，這些預測可以在計算機上進行測試。<lb/>在 Boltz-1 Pairformer 主幹上部署的穩定性優先考慮使用約 4.4 倍較少的潛在評估來發現可解釋的潛在變量，並且測量成本降低了 5.2 倍，同時恢復了超過一半的潛在變量，外部檢查顯示出現的主題在其聲稱的註釋中顯著富集。<lb/>結果還表明可能存在一種緊張關係：交叉種子穩定性可能在選擇某些類型的特徵（如結構相關特徵）時，遠遠超過其他類型（如功能相關特徵）。

##### **Self-Explainable Multi-Label Graph Neural Network for Correlated Evidence Attribution**
2608.27574v1 by Yingqi Feng, Yufei Tang, Min Shi, Xingquan Zhu

Multi-label graph learning intends to capture the intrinsic complexity of real-world applications, where one sample is often related to multiple groups or consists of multiple objects. To date, a handful of multi-label graph learning methods exist, but none of them integrate training-time interpretation capability. While post-hoc graph explainers have been developed, they do not explicitly model label-dependent evidence sharing in multi-label graph learners, especially when label pairs are weakly or negatively associated. As a result, post-hoc approaches may miss how evidence should be shared or separated across different labels. This paper advances a new end-to-end self-explainable multi-label graph neural network (SEMGNN), which aims to simultaneously classify multi-labeled nodes and identify edges significantly contributing to each target node w.r.t. predicted labels. Different from post-hoc methods, SEMGNN jointly learns a predictor and a sparse edge-mask explainer within a unified framework and training objective. Label-label correlations are used to improve multi-label node classification and enhance individual label explanations, so that different labels of a node can be supported by distinct yet coherent structural and/or correlated evidence. Experiments and comparisons on synthetic and real-world multi-label networks, in social networking, entertainment, and life sciences, show that SEMGNN achieves competitive or improved predictive performance while providing more faithful and compact label-conditioned explanations.

摘要：多標籤圖學習旨在捕捉現實世界應用的內在複雜性，其中一個樣本通常與多個群體相關或由多個物體組成。到目前為止，只有少數幾種多標籤圖學習方法存在，但它們都沒有整合訓練時的解釋能力。雖然已經開發了事後圖解釋器，但它們並未明確建模多標籤圖學習者中的標籤依賴證據共享，特別是在標籤對之間弱或負相關的情況下。因此，事後方法可能會錯過如何在不同標籤之間共享或分離證據。本文推進了一種新的端到端自我解釋的多標籤圖神經網絡（SEMGNN），旨在同時對多標籤節點進行分類並識別對每個目標節點的預測標籤有顯著貢獻的邊。與事後方法不同，SEMGNN在統一的框架和訓練目標內共同學習預測器和稀疏邊掩碼解釋器。標籤之間的相關性被用來改善多標籤節點分類並增強個別標籤解釋，使得一個節點的不同標籤可以由不同但一致的結構和/或相關證據支持。在社交網絡、娛樂和生命科學等合成和現實世界多標籤網絡上的實驗和比較顯示，SEMGNN實現了具有競爭力或改進的預測性能，同時提供了更真實和緊湊的標籤條件解釋。

##### **ORDDAR: Observation-Driven Reasoning for Distortion-Resilient Decision, Action, and Cognitive Recovery**
2608.28704v1 by Deblina Kar, Anant Nawalgaria, Shyamal Kumar Das Mandal

AI agents increasingly perform long-term reasoning, planning, tool use, memory integration, and autonomous decision making, yet erroneous intermediate states can propagate and cause inconsistent decisions and unreliable outputs. Existing reasoning approaches mainly rely on iterative planning, self-reflection, augmented memory, or verification, but rarely localize and selectively repair faulty reasoning. We present ORDDAR (Observation-Driven Reasoning for Distortion-Resilient Decision, Action, and Cognitive Recovery), a reasoning framework that models reasoning as cognitive state transitions, detects localized distortions, retrieves related reasoning from prior experiences, and repairs only the affected states. ORDDAR therefore performs recovery at the local reasoning-transition level rather than regenerating the complete trajectory. Experiments across mathematical, commonsense, multi-hop, and clinical reasoning benchmarks demonstrate improved reasoning quality, recovery ability, and interpretability over multiple evaluated reasoning baselines.

摘要：AI 代理越來越能進行長期推理、規劃、工具使用、記憶整合和自主決策，但錯誤的中間狀態可能會傳播並導致不一致的決策和不可靠的輸出。現有的推理方法主要依賴於迭代規劃、自我反思、增強記憶或驗證，但很少能夠定位並選擇性修復錯誤的推理。我們提出了 ORDDAR（觀察驅動的抗失真決策、行動和認知恢復推理框架），該框架將推理建模為認知狀態轉換，檢測局部失真，從先前經驗中檢索相關推理，並僅修復受影響的狀態。因此，ORDDAR 在局部推理轉換層面上進行恢復，而不是重新生成完整的軌跡。在數學、常識、多跳和臨床推理基準上的實驗顯示，推理質量、恢復能力和可解釋性均優於多個評估的推理基準。

##### **SETU: An Agentic Ecosystem for Multilingual, Persona-Aware Communication Coaching**
2608.27524v1 by Jonnalagadda Maruthi Tejas, Uponika Barman Roy, Tilottama Goswami, Samir Goswami, Mousita Dhar

Corporate training teams need scalable and explainable tools to improve workforce communication in multilingual settings. Existing systems often score text, audio, or video in isolation, or produce black-box outputs that are difficult to audit for coaching use. This paper presents SETU, an agentic ecosystem for corporate communication coaching aimed at recruiters, frontline sales professionals and training units who prepare for audience specific conversations. SETU is designed for two scoped scenarios: (i) recruiter-candidate eligibility-and-interest calls with persona context and (ii) sales pitches with target-audience adaptation; owing to limited evaluation resources, this paper reports results on scenario (ii) only. The ecosystem decomposes analysis into specialized video, audio-speech, text-relevance, scoring, notification and reporting agents coordinated through trust-aware orchestration. It generates modality-attributed coaching reports for formative training, with human reviewers retaining final judgment. The name SETU (bridge in several Indic languages) reflects the goal of bridging communication gaps across regional languages and audience expectations.

摘要：企業培訓團隊需要可擴展且可解釋的工具，以改善多語言環境中的員工溝通。現有系統通常孤立地對文本、音頻或視頻進行評分，或產生難以審核的黑箱輸出，無法用於輔導。本文介紹了SETU，一個針對招聘人員、一線銷售專業人士和準備針對特定受眾進行對話的培訓單位的企業溝通輔導代理生態系統。SETU 設計了兩個範疇的場景：（i）招聘人員與候選人之間的資格與興趣電話會議，並包含角色背景，以及（ii）針對目標受眾的銷售推介；由於評估資源有限，本文僅報告場景（ii）的結果。該生態系統將分析分解為專門的視頻、音頻語音、文本相關性、評分、通知和報告代理，並通過信任感知的協調進行協作。它為形成性培訓生成模式屬性輔導報告，並由人類審核者保留最終判斷。SETU（在幾種印度語言中意為橋樑）這個名稱反映了跨越地區語言和受眾期望的溝通橋樑的目標。

##### **LAAF: A Layered Accountability Architecture Framework for LLM Applications**
2608.27102v1 by Prachi Chaturvedi, Shahnawaz Ahmad, Ehsan Nowroozi, Muhammad Waqas, George Loukas, Alireza Jolfaei, Lucas Cordeiro, Pierre Dantas

Large Language Models (LLMs) operate in hospitals, courtrooms, banks, and public service desks, where fluent, confident outputs are treated as authoritative even when ungrounded or incorrect. When such an output contributes to harm, who is answerable, and through what mechanisms can responsibility be traced, explained, and acted upon? Following PRISMA guidance, five databases were searched from January 2022 to March 2026 against four review questions; of 4,512 records identified, 122 primary studies were included, together with 12 regulatory and standards documents analysed as primary sources. The review consolidates a sociotechnical account of accountability as an actor-forum relation resolved into five dimensions, and synthesises mechanisms across four families: technical controls, human oversight, organisational governance, and documentation and traceability, each with a maturity assessment. The corpus is read through a four-layer classification device spanning provenance, application logic, human oversight, and governance and redress, cross-cut by traceability, role clarity, and continuous monitoring. Both are mapped onto the EU AI Act, whose high-risk obligations have applied since 2 August 2026, the NIST AI RMF with its Generative AI Profile, ISO/IEC 42001, and sectoral guidance in healthcare, consumer finance, education, and the public sector. Four persistent gaps emerge: under-specification of human oversight, absence of shared accountability metrics, disciplinary disconnection, and limited empirical evaluation, alongside five structural tensions that no surveyed instrument resolves. The review closes by consolidating the classification device into an integrated accountability architecture, LAAF, with cybersecurity aligned to the OWASP LLM Top 10 (2025); it is a synthesis of the surveyed evidence rather than a validated artefact.

摘要：大型語言模型（LLMs）在醫院、法庭、銀行和公共服務櫃檯中運作，流利、自信的輸出即使在缺乏根據或不正確的情況下也被視為權威。當這樣的輸出造成傷害時，誰應負責，責任又能通過什麼機制被追溯、解釋和執行？根據PRISMA指導方針，從2022年1月到2026年3月對四個評審問題進行了五個數據庫的搜索；在識別的4,512條記錄中，納入了122項主要研究，以及12份作為主要來源分析的監管和標準文件。該評審整合了一個社會技術的問責制賬戶，作為一種行為者-論壇關係，分為五個維度，並綜合了四個家庭中的機制：技術控制、人為監督、組織治理，以及文檔和可追溯性，每個都有成熟度評估。該語料庫通過一個涵蓋來源、應用邏輯、人為監督以及治理和救濟的四層分類裝置進行閱讀，並交叉考量可追溯性、角色清晰度和持續監控。這兩者都映射到歐盟人工智慧法案，其高風險義務自2026年8月2日起生效，NIST AI RMF及其生成式AI概況，ISO/IEC 42001，以及在醫療、消費金融、教育和公共部門的行業指導。出現了四個持續的缺口：人為監督的規範不足、缺乏共享的問責指標、學科間的脫節，以及有限的實證評估，還有五個結構性緊張關係，沒有調查的工具能解決。該評審以整合分類裝置為結尾，形成一個綜合的問責架構LAAF，並將網絡安全與OWASP LLM前10名（2025年）對齊；這是一個調查證據的綜合，而不是經過驗證的產物。

##### **Accelerating Scientific Research with Gemini in the Real-World**
2608.26701v1 by Samuel Schmidgall, Xiaokai Zhu, Marian Shaw, Lin Yang, Valentin Liévin, Jingyun Yang, Yuchen Zhuang, Tim Strother, Alex Bijamov, Min Woo Sun, Anil Palepu, Justin Chen, David Steiner, Jacqueline Shreibati, Wei-Hung Weng, Yilin Zhao, Xingjian Hu, Nicholas Zahn, Sadhya Garg, Julia Kirby, Yuxiang Gan, Jiaoli Li, Divy Thakkar, Shekoofeh Azizi, David Racz, Juraj Gottweis, Vivek Natarajan, Chenglin Wu, Tal Danino, Keran Rong, Haozhe Wang, Benoit Schillings, Yong Cheng, Quoc V. Le, Tao Tu

We present an extension and comprehensive real-world validation of Co-Scientist, a Gemini-based multi-agent system designed to accelerate end-to-end scientific research across hypothesis generation, experimentation, and manuscript generation. Moving beyond in silico hypothesis generation, this specialized configuration transitions Co-Scientist into an execution-grounded research partner advancing closed-loop scientific workflows across materials science, biology, and computer science. In materials science, Co-Scientist interfaced with a semi-automated chemical vapor deposition reactor to design a safe precursor route for MXenes; experimental execution produced a lamellar 2D material sharing key structural similarities with the Ti3C2Tx MXene lattice, although further experiments are needed to confirm the atomic structure. Leveraging Gemini 3 Deep Think for rapid, lab-in-the-loop execution, it also tailored growth recipes to laboratory constraints in minutes, enabling single-attempt growth of monolayer MoS2, MoSe2, and WS2 semiconductors. In biology, Co-Scientist predicted emergent swarming phenotypes of engineered E. coli across inducer (IPTG) gradients from sparse imaging data, quantitatively matching unpublished wet-lab morphological measurements. In computer science, Co-Scientist autonomously discovered an inference-time scaling architecture that outperformed six frontier models on HealthBench (Hard and Professional) while reducing potential clinical harm under blinded physician evaluation. Finally, a double-blind study of end-to-end generated papers with 30 domain experts across 450 reviews demonstrates that Co-Scientist's reliability modules reduce hallucination and plagiarism while improving research safety. Together, these results demonstrate progress toward closed-loop multi-agent scientific AI systems capable of accelerating real-world scientific discovery.

摘要：我們展示了Co-Scientist的擴展和全面的實際驗證，這是一個基於Gemini的多代理系統，旨在加速從假設生成、實驗到手稿生成的端到端科學研究。超越了計算機模擬的假設生成，這一專門配置將Co-Scientist轉變為一個以執行為基礎的研究夥伴，推進材料科學、生物學和計算機科學中的閉環科學工作流程。在材料科學中，Co-Scientist與半自動化的化學氣相沉積反應器進行了接口，以設計MXenes的安全前驅體路徑；實驗執行產生了一種層狀的2D材料，與Ti3C2Tx MXene晶格具有關鍵的結構相似性，儘管還需要進一步的實驗來確認原子結構。利用Gemini 3 Deep Think進行快速的實驗室內執行，它還在幾分鐘內根據實驗室的限制量身定制生長配方，使得單次嘗試能夠生長單層MoS2、MoSe2和WS2半導體。在生物學中，Co-Scientist預測了工程化大腸桿菌在誘導劑（IPTG）梯度下的 emergent swarming 表型，並且從稀疏成像數據中定量匹配了未發表的濕實驗形態測量。在計算機科學中，Co-Scientist自主發現了一種推理時間擴展架構，該架構在HealthBench（Hard和Professional）上超越了六個前沿模型，同時在盲法醫生評估下減少了潛在的臨床傷害。最後，對30位領域專家的450次評審進行的雙盲研究顯示，Co-Scientist的可靠性模塊減少了幻覺和抄襲，同時提高了研究安全性。綜合這些結果顯示，朝著能夠加速現實世界科學發現的閉環多代理科學AI系統邁出了進展。

##### **Planetary Prediction Engine: Autonomous Geospatial Prediction via Intelligent Data Selection and Foundation Model Embeddings**
2608.26088v1 by Evelyn Ma, Rama Kumar Pasumarthi, Kishwar Shafin, Mandar Sharma, Mimi Sun, Hamed Sadeghi, Dav M. Ebengo, Mbulayi Onesime, Rouslan Solomakhin, John Wamburu, William Ogallo, Aisha Walcott-Bryant, Sanxing Chen, Arbaaz Muslim, Yael Mayer, Ronald Ho, Roy Lee, Ruth Alcantara, Abdoulaye Diack, Monica Bharel, Lambert Rosique, Jeremy Amez-Droz, Christopher Haire, James Manyika, Yossi Matias, Niv Efron, Gautam Prasad, Shravya Shetty

Addressing critical global challenges, from food security and disaster risk to disease outbreaks and socio-economic vulnerability, demands high-fidelity geospatial modeling. However, building predictive planetary models remains bottlenecked by a fragmented data ecosystem, requiring manual data retrieval, multimodal data curation and fusion along with iterative model selection. We present the Planetary Prediction Engine (PPE), an autonomous AI system that executes this end-to-end workflow directly from natural-language queries. PPE synthesizes multimodal datasets on the fly, retrieving spatiotemporally relevant covariates across open-web and Earth observation platforms (Data Commons, Google Earth Engine) and fusing them with geospatial foundation model embeddings (PDFM, AlphaEarth). Simultaneously, it searches over task-tailored model architecture families with automated overfitting guards. Across diverse tasks, geographies, and scientific domains, PPE consistently outperforms state-of-the-art or manually tuned expert baselines. For US spatial regression, PPE improves mean $R^2$ across 21 CDC health indicators (76.8% vs. 60.0%), FEMA national risk indices (64.9% vs. 60.0%), and the Social Vulnerability Index (66.2% vs. 58.6%). For spatial downscaling in data-scarce settings, PPE integrates localized proxies to double baseline accuracy in Nigerian food security indicators ($R^2$ of 66.1% vs. 31.5%). For epidemiological nowcasting of the 2026 DRC Bundibugyo Ebola outbreak, PPE achieves a Recall@10 of 83.3% (identifying 15 of 18 newly invaded health zones across five weekly forecasts), a +10.3 percentage-point improvement over the public state-of-the-art modeling (~73%). By combining autonomous multimodal planetary data discovery with targeted model optimization, PPE lowers the technical barrier to planetary-scale analytics, enabling rapid, customized, expert-level deployment.

摘要：解決全球重大挑戰，包括糧食安全、災害風險、疾病爆發和社會經濟脆弱性，需求高保真度的地理空間建模。<lb/>然而，建立預測性行星模型仍然受到碎片化數據生態系統的瓶頸，這需要手動數據檢索、多模態數據策劃和融合，以及迭代模型選擇。<lb/>我們提出了行星預測引擎（PPE），這是一個自主的人工智慧系統，能夠直接從自然語言查詢執行這一端到端的工作流程。<lb/>PPE 即時合成多模態數據集，從開放網絡和地球觀測平台（數據公共、Google Earth Engine）檢索時空相關的協變量，並將其與地理空間基礎模型嵌入（PDFM、AlphaEarth）融合。<lb/>同時，它在自動過擬合防護的情況下，搜尋針對任務量身定製的模型架構家族。<lb/>在多樣的任務、地理區域和科學領域中，PPE 始終超越最先進或手動調整的專家基準。<lb/>對於美國的空間回歸，PPE 在 21 項 CDC 健康指標上提高了平均 $R^2$（76.8% 對 60.0%）、FEMA 國家風險指數（64.9% 對 60.0%）和社會脆弱性指數（66.2% 對 58.6%）。<lb/>在數據稀缺的環境中進行空間下採樣時，PPE 整合了本地化的代理，將尼日利亞糧食安全指標的基準準確率提高了一倍（$R^2$ 為 66.1% 對 31.5%）。<lb/>對於 2026 年剛果民主共和國 Bundibugyo 埃博拉疫情的流行病學即時預測，PPE 實現了 83.3% 的 Recall@10（在五次每週預測中識別出 15 個新入侵的健康區域中的 18 個），比公共最先進模型提高了 10.3 個百分點（約 73%）。<lb/>通過將自主的多模態行星數據發現與針對性的模型優化相結合，PPE 降低了行星級分析的技術門檻，使快速、定制的專家級部署成為可能。

##### **ICON Decomposition: Auditing Deep Neural Networks with Multivariate Variance-based Concept-level Explanations**
2608.26083v2 by Roshan Prakash Rane, Marco Simnacher, Manuel Pfeuffer, Marc-Andre Schulz, Nys Tjade Siegel, Maximilian Dreyer, Frederik Pahde, Wojciech Samek, Sonja Greven, Kerstin Ritter

Deep neural networks often exploit spurious associations, a failure known as shortcut learning. Auditing for shortcuts requires testing many candidate concepts, such as acquisition settings or demographics. Current concept-based explainability methods test one concept at a time, asking whether it is decodable from a layer. These methods flag concepts that are merely correlated with the outcome or with each other, and their scores are not comparable across layers or concept types. We introduce ICON decomposition, which quantifies how much of a layer's variance each concept explains given all other concepts and the outcome, and how much none of them explains, yielding layer-comparable, calibrated scores that suppress false positives. On simulated data, ICON recovers concept importance more accurately than seven existing methods. On skin-cancer models with inserted artifacts, it detects induced shortcuts where baselines report false positives. On two brain-imaging models, ICON's sparse explanations are validated by retraining and out-of-distribution tests.

摘要：深度神經網絡經常利用虛假的關聯，這種失敗稱為捷徑學習。審核捷徑需要測試許多候選概念，例如獲取設置或人口統計。當前的基於概念的可解釋性方法一次測試一個概念，詢問它是否可以從某一層解碼。這些方法標記出與結果或彼此僅僅相關的概念，而它們的分數在不同層或概念類型之間不可比較。我們引入了 ICON 分解，該方法量化了在考慮所有其他概念和結果的情況下，每個概念解釋了多少層的方差，以及它們都沒有解釋多少，從而產生可層間比較的、經過校準的分數，抑制假陽性。在模擬數據上，ICON 比七種現有方法更準確地恢復概念重要性。在插入工件的皮膚癌模型中，它檢測到誘發的捷徑，而基準報告假陽性。在兩個腦成像模型上，ICON 的稀疏解釋通過重新訓練和分佈外測試得到了驗證。

##### **A Statistical Audit of Physical AI Benchmark Redundancy**
2608.25940v2 by Zaruhi Navasardyan, Hrant Davtyan

Physical AI models are evaluated on suites of benchmarks that differ across model reports, leaving the model-by-benchmark matrix sparse and the relationship between benchmarks unmeasured. We construct a matrix of 51 models on 12 physical AI benchmarks, selected from a registry of 51 benchmarks and 152 models by reporting density, combining scores from model cards and benchmark papers with our own evaluation runs under each benchmark's official protocol. We measure how much information the benchmarks share and show quantitative evidence of Redundancy. Redundancy affects reported rankings: collapsing the two substitute pairs into single columns moves 22 of 51 models by three or more places under an equally weighted average. We then select benchmarks greedily under a utility combining score dispersion with variance not explained by the already-selected set, and obtain a four-benchmark subset retaining 78.5\% of the utility of all 12, on which we fit a Bradley--Terry ranking. The procedure requires only benchmark-level scores with sufficient overlap and is not specific to physical AI.

摘要：物理 AI 模型的評估基於一系列基準，這些基準在模型報告中有所不同，導致模型與基準的矩陣稀疏，基準之間的關係未被測量。我們構建了一個包含 51 個模型和 12 個物理 AI 基準的矩陣，這些基準是從 51 個基準和 152 個模型的登記中選擇的，根據報告密度，結合模型卡和基準論文的分數，以及我們在每個基準的官方協議下的評估運行。我們測量基準之間共享的信息量，並顯示冗餘的定量證據。冗餘影響報告的排名：將兩對替代品合併為單一列，使 51 個模型中有 22 個模型的排名變動三個或更多位置，根據等權重平均。我們然後在效用中貪婪地選擇基準，將分數分散與已選集未解釋的變異結合，獲得一個四個基準的子集，保留了所有 12 個基準的 78.5\% 的效用，並在其上擬合了 Bradley--Terry 排名。該程序只需要具有足夠重疊的基準級別分數，並且不特定於物理 AI。

##### **Candidate supply and answer selection shape the value of LLM judging in multi-agent systems**
2608.25937v2 by Jia-Hao Ji, Sijie Li, Jiabei Cheng, Zixi She, Jin-Tai Yu, Zhiyuan Yuan

Multi-agent systems (MAS) sometimes already have the potential to answer correctly, but still report a wrong answer. Explaining this outcome is difficult because generation, communication and final answer-selection rules usually change simultaneously. We conceptualize multi-agent reasoning as an evolutionary pipeline of candidate generation, peer communication and terminal selection, wherein consensus without quality control can exhibit patterns of memetic drift. We study two questions: (1) when an LLM judge provides effective selection pressure by supplying a signal of answer correctness for candidates generated in a multi-agent system, and (2) when using that signal improves the reported answer. To map judge reliability, we analysed 15,336 questions from MMLU-Pro, GPQA, MedXpertQA and MuSR, with Humanity's Last Exam analysed separately. To test these rules, we replayed 81,390 fixed candidate pools drawn from 16,278 questions across five benchmarks. We report three findings. (1) A correct answer is often already present among the generated candidates, but the system can still converge on and report a wrong answer. (2) Judge reliability is not a fixed trait of the model, but varies with the task, the generator and how rare the correct answer is. (3) Combining answer frequency with the judge's evaluation changed only the final answer-selection rule and raised accuracy from 63.82% to 70.82-70.95%, primarily by rescuing correct answers that were outnumbered by popular errors. In the systems studied here, the value of generating more candidates depends on whether those extra samples make correct answers present, frequent or recognisable. By isolating generation, recognition and selection, these findings establish a diagnostic basis for designing multi-agent architectures that protect generated correct answers from being lost.

摘要：多代理系統（MAS）有時已經具備正確回答的潛力，但仍然報告錯誤的答案。解釋這一結果是困難的，因為生成、通信和最終答案選擇規則通常是同時改變的。我們將多代理推理概念化為候選生成、同儕通信和終端選擇的演化管道，其中缺乏質量控制的共識可能會顯示出模因漂移的模式。我們研究了兩個問題：（1）當LLM評審通過為在多代理系統中生成的候選者提供答案正確性的信號來提供有效的選擇壓力時，以及（2）使用該信號是否改善報告的答案。為了映射評審的可靠性，我們分析了來自MMLU-Pro、GPQA、MedXpertQA和MuSR的15,336個問題，並單獨分析了人類的最後考試。為了測試這些規則，我們重播了從五個基準中抽取的16,278個問題的81,390個固定候選池。我們報告了三個發現。（1）在生成的候選者中，正確答案通常已經存在，但系統仍然可以收斂並報告錯誤的答案。（2）評審的可靠性不是模型的固定特徵，而是隨著任務、生成器和正確答案的稀有程度而變化。（3）將答案頻率與評審的評估結合起來僅改變了最終答案選擇規則，並將準確率從63.82%提高到70.82-70.95%，主要是通過挽救被流行錯誤數量壓倒的正確答案。在這裡研究的系統中，生成更多候選者的價值取決於這些額外樣本是否使正確答案變得存在、頻繁或可識別。通過隔離生成、識別和選擇，這些發現為設計多代理架構提供了診斷基礎，以保護生成的正確答案不被遺失。

##### **TAU-Agent: An Agentic Retrieval-Augmented Framework for Traffic Anomaly Understanding**
2608.25935v1 by Yuqiang Lin, Yan Shi, Sam Lockyer, Harish Tayyar Madabushi, Adrian Evans, Wenbin Li, Yinhai Wang, Nic Zhang

Traffic Anomaly Understanding (TAU) requires models and systems to detect, reason about, and explain anomalous events in transportation videos. To address this challenge, we propose TAU-Agent, an agentic retrieval-augmented framework for traffic anomaly understanding. Given a task query, a central retrieval agent orchestrates two visual perception tools, namely a Video Captioning Tool and an Open-Vocabulary Tracking Tool, to retrieve and select query-relevant evidence, including captions, temporal intervals, and object trajectories. The selected evidence, together with sampled video frames and the input query, is provided to a supervised fine-tuned vision-language model for final reasoning and answer generation. We evaluate TAU-Agent on both the in-domain and the out-of-domain benchmarks from the AI City Challenge 2026. TAU-Agent achieves scores of 0.6779 on Track 3, 0.3998 on Track 7, and 67.9275 on Track 8, ranking second, twelfth, and fifth, respectively. Code is available at: https://github.com/siri-rouser/TAU-Agent.

摘要：交通異常理解（TAU）需要模型和系統來檢測、推理和解釋交通視頻中的異常事件。為了解決這一挑戰，我們提出了TAU-Agent，一個用於交通異常理解的代理檢索增強框架。給定一個任務查詢，一個中央檢索代理協調兩個視覺感知工具，即視頻標題工具和開放詞彙跟踪工具，以檢索和選擇與查詢相關的證據，包括標題、時間間隔和物體軌跡。所選證據連同採樣的視頻幀和輸入查詢一起提供給一個經過監督微調的視覺-語言模型，以進行最終推理和答案生成。我們在AI City Challenge 2026的領域內和領域外基準上評估了TAU-Agent。TAU-Agent在Track 3上獲得了0.6779的分數，在Track 7上獲得了0.3998的分數，在Track 8上獲得了67.9275的分數，分別排名第二、第十二和第五。代碼可在以下網址獲得：https://github.com/siri-rouser/TAU-Agent。

##### **Formal, Executable and Explainable Runtime Monitoring of Spoken Air Traffic Control Operational Procedures**
2608.25926v1 by Roberto Luvini, Giacomo Longo, Alessandro Armando, Enrico Russo

Air traffic control procedures are executed through spoken exchanges between controllers and pilots. These interactions are essential to the safety of air transportation: failures in their execution can create severe operational hazards, as evidenced by past fatal accidents. Assessing whether an instruction has been followed requires relating what was said to the aircraft concerned, its state, and the obligations that pilots must meet. We present a runtime verification framework that monitors such procedures by checking controller-pilot exchanges, surveillance data, and onboard observations. The framework parses radio communications into events linked to the entities they concern and merges them with surveillance and onboard observations into a time-stamped trace. The ICAO-derived obligations as formalized as temporal formulas with explicit time bounds and evaluated over execution traces. Every violation is reported along with the breached obligations and the observations that support the verdict. With real traffic, the complete pipeline reaches an F1 of 0.85 against blind human-annotated violations; in 1,495 synthetic situations derived from two public corpora, the monitor logic returns the expected verdict in every case. In two historical accidents reconstructed from official investigation reports, the monitor identifies the same procedural deviations documented by the investigators.

摘要：空中交通管制程序是透過控制人員與飛行員之間的口頭交流來執行的。這些互動對於航空運輸的安全至關重要：其執行失敗可能會造成嚴重的操作危險，這在過去的致命事故中已經得到了證明。評估指令是否被遵循需要將所說的內容與相關的飛機、其狀態以及飛行員必須遵守的義務聯繫起來。我們提出了一個運行時驗證框架，通過檢查控制人員與飛行員的交流、監視數據和機上觀察來監控這些程序。該框架將無線電通信解析為與其相關的實體鏈接的事件，並將這些事件與監視和機上觀察合併成一個帶有時間戳的追蹤記錄。根據ICAO衍生的義務，這些義務被形式化為具有明確時間界限的時間公式，並在執行追蹤記錄上進行評估。每次違規都會報告並附上違反的義務以及支持該判決的觀察結果。在真實交通情況下，完整的管道在盲目人類標註的違規情況下達到0.85的F1；在從兩個公共語料庫衍生的1,495個合成情況中，監控邏輯在每種情況下都返回預期的判決。在從官方調查報告重建的兩起歷史事故中，監控器識別出調查人員記錄的相同程序偏差。

##### **Towards A Unified Information Bottleneck Framework for Time Series Explanations**
2608.25897v1 by Xu Zheng, Zichuan Liu, Zhuomin Chen, Mayur Akewar, Janki Bhimani, Jason Liu, Mo Sha, Jingchao Ni, Wei Cheng, Dongsheng Luo

Explaining deep learning models operating on time series data is crucial in various applications that require transparent and interpretable insights into model behavior. {Existing explanation methods generally fall into two categories: attribution-based explanations, which identify the temporal regions most responsible for a prediction, and counterfactual explanations, which reveal how an input should be modified to alter the model's decision.} {Despite valuable insights, these two fields are largely studied independently. This disconnect leaves attribution methods lacking causal validation, while counterfactual methods suffer from severe instability, producing adversarial-like noise instead of meaningful explanations.} In this work, we revisit time-series explainability from an information-theoretic perspective and show that existing explainers are vulnerable to trivial solutions and distributional shifts. To address these limitations, we propose a unified objective function for explainable time series learning that bridges attribution and counterfactual reasoning within a single framework. Building upon the Information Bottleneck principle, our formulation explicitly prevents trivial explanations and out-of-distribution counterfactuals. {Based on this objective function, we introduce {\modelname}, a novel explanation framework that learns a parametric transformation network to construct explanation-embedded instances, where preserved information yields attribution explanations and controlled information removal produces stable counterfactual explanations.} We evaluate {\modelname} on synthetic and real-world benchmarks against state-of-the-art baselines. Extensive quantitative and qualitative results show that {\modelname} consistently outperforms competing methods, yielding faithful attributions and stable counterfactual explanations.

摘要：解釋運行於時間序列數據的深度學習模型在各種應用中至關重要，這些應用需要對模型行為提供透明且可解釋的見解。{現有的解釋方法一般可分為兩類：基於歸因的解釋，這類解釋識別出對預測最有責任的時間區域，以及反事實解釋，這類解釋揭示了如何修改輸入以改變模型的決策。}{儘管提供了有價值的見解，這兩個領域在很大程度上是獨立研究的。這種脫節使得歸因方法缺乏因果驗證，而反事實方法則遭受嚴重的不穩定性，產生類似對抗的噪聲，而不是有意義的解釋。} 在這項工作中，我們從信息論的角度重新審視時間序列的可解釋性，並顯示現有的解釋器容易受到平凡解和分佈變化的影響。為了解決這些限制，我們提出了一個統一的可解釋時間序列學習目標函數，將歸因和反事實推理橋接在一個框架內。基於信息瓶頸原則，我們的公式明確防止平凡解釋和超出分佈的反事實。{基於這個目標函數，我們介紹了{\modelname}，一個新穎的解釋框架，該框架學習一個參數變換網絡來構建嵌入解釋的實例，其中保留的信息產生歸因解釋，而控制的信息移除則產生穩定的反事實解釋。} 我們在合成和現實世界的基準上對{\modelname}進行了評估，並與最先進的基線進行比較。大量的定量和定性結果顯示，{\modelname}始終優於競爭方法，產生忠實的歸因和穩定的反事實解釋。

##### **Distance Is Not Enough: Forget-Retain Alignment Gap Predicts LLM Relearning Robustness**
2608.25429v1 by Yi Chen, Hanna Hsieh, Shuhong Liu, Chuanbo Hua, Zihan Ma, Kun Wang, Joo-Young Kim

Machine unlearning aims to make a model forget specific data, yet unlearned LLMs often fail to stay unlearned: brief fine-tuning can revive removed knowledge. Existing robustness predictors rely on global weight-space displacement, but distance alone can be misleading when random or destructive updates collapse performance. We argue that relearning robustness depends on update structure: robust unlearning should affect forget-critical weights while sparing retain-critical ones. We introduce the Forget-Retain Alignment Gap (FRAG), a training-free predictor that scores an update's forget-retain alignment without running a relearning attack, and separates selective from dense updates more reliably than global distance. Building on the forget-critical, retain-sparing principle, Forget-Retain Pruning (FRP) improves relearning robustness. Our results suggest that weight selectivity better explains robustness than distance alone. Code is available at https://github.com/Yi1-Chen/FRAG.

摘要：機器遺忘旨在使模型忘記特定數據，但未學習的 LLM 通常無法保持未學習狀態：簡短的微調可以恢復被移除的知識。現有的穩健性預測器依賴於全局權重空間位移，但僅僅依賴距離在隨機或破壞性更新導致性能崩潰時可能會產生誤導。我們認為，重新學習的穩健性取決於更新結構：穩健的遺忘應該影響關鍵遺忘權重，同時保留關鍵保留權重。我們引入了遺忘-保留對齊差距（FRAG），這是一種無需訓練的預測器，可以在不進行重新學習攻擊的情況下評分更新的遺忘-保留對齊，並比全局距離更可靠地區分選擇性更新和密集更新。基於關鍵遺忘和保留的原則，遺忘-保留修剪（FRP）提高了重新學習的穩健性。我們的結果表明，權重選擇性比僅僅依賴距離更能解釋穩健性。代碼可在 https://github.com/Yi1-Chen/FRAG 獲得。

##### **Fairness Invariants: A Relational Approach to Explaining and Mitigating Fairness Bugs**
2608.26209v1 by Ranit Debnath Akash, Ashish Kumar, Gang Tan, Saeid Tizpaz-Niari

Data-driven software systems are increasingly deployed in high-stakes socio-economic domains, from criminal justice to financial lending. However, these systems often exhibit individual discrimination---unjustified disparities in which a program yields different outcomes for similar individuals who differ only in their protected attributes (e.g., race, gender, age). While existing research has focused on detecting and quantifying these bugs, there remains a critical lack of principled mechanisms to explain and localize individual fairness bugs. Current explanation techniques are largely designed for single-input decisions rather than the relational nature of discrimination, which inherently involves a comparison between an original and a counterfactual pair.
  We present REMI, a framework for the automated localization, explanation, and mitigation of individual discrimination. Inspired by loop-invariant synthesis in formal methods, we treat counterfactual fairness as a relational invariant discovery problem. We introduce a bidirectional relational explanation framework that learns over paired examples $(x, x')$ to identify regions of the input space where fairness is violated. Unlike traditional one-way implication pairs used in invariant inference, our approach enforces bidirectional constraints: requiring identical outcomes for both original and counterfactual samples. REMI utilizes three data-alignment techniques to infer interpretable rule-based models that act as "fairness invariants." These rules serve as guardrails to selectively block or relabel unfair predictions without requiring model retraining. Our evaluation on symbolic and neural network programs demonstrates that REMI localizes ground-truth fairness bugs in over 83% of cases, significantly outperforming state-of-the-art baselines and reducing discriminatory decisions in black-box models by up to 70%.

摘要：數據驅動的軟體系統越來越多地被應用於高風險的社會經濟領域，從刑事司法到金融貸款。然而，這些系統經常表現出個體歧視——不合理的差異，導致程式對於僅在其受保護屬性（例如，種族、性別、年齡）上有所不同的相似個體產生不同的結果。雖然現有的研究集中於檢測和量化這些錯誤，但在解釋和定位個體公平性錯誤方面仍然缺乏原則性的機制。目前的解釋技術主要是為單一輸入決策設計的，而不是針對歧視的關聯性質，這本質上涉及原始樣本和反事實樣本之間的比較。<lb/>  
我們提出了REMI，一個自動定位、解釋和減輕個體歧視的框架。受到形式方法中循環不變合成的啟發，我們將反事實公平性視為一個關聯不變發現問題。我們引入了一個雙向關聯解釋框架，通過配對範例 $(x, x')$ 來識別公平性被違反的輸入空間區域。與傳統的一維推理對用於不變推理不同，我們的方法強制執行雙向約束：要求原始樣本和反事實樣本的結果相同。REMI利用三種數據對齊技術來推斷可解釋的基於規則的模型，這些模型充當“公平性不變量”。這些規則作為護欄，選擇性地阻止或重新標記不公平的預測，而無需重新訓練模型。我們對符號和神經網絡程序的評估表明，REMI在超過83%的案例中定位了真實的公平性錯誤，顯著超越了最先進的基準，並將黑箱模型中的歧視性決策減少了多達70%。

##### **Refusal geometry reflects refusal training: diverse refusal prefixes can raise stable rank and weaken refusal vector ablation attacks**
2608.25390v2 by Andrey Labunets

Refusal training protects AI models from jailbreaks by training models to decline unsafe queries, reducing the risk of misuse. Recent work finds that refusal behavior in aligned language models can be mediated by a single activation direction or a low-dimensional refusal subspace shared across harmful prompts: ablating those directions suppresses refusals while largely preserves other model capabilities. Yet it remains unclear why safety-critical features in a wide range of models emerge in a concentrated, low-dimensional structure. In a case study of OLMo-2-0425-1B-Instruct we find that the refusal geometry reflects refusal training: activation updates resulting from refusal-completion first-token losses explain the resulting refusal direction and refusal subspace. We study refusal directions through the training dynamics across refusal datasets and reveal that their brittleness is associated with repetitive refusal starts, which in turn is linked to concentration of gradients and refusal features in a low-dimensional subspace. Across frozen-model analyses and controlled synthetic fine-tuning, we find evidence of a hardening lever: diverse refusal starts can raise stable ranks of gradients and activation changes, making refusals harder to remove with a vector ablation attack.

摘要：拒絕訓練透過訓練模型拒絕不安全的查詢來保護人工智慧模型免受越獄攻擊，從而降低濫用的風險。最近的研究發現，在對齊的語言模型中，拒絕行為可以通過單一的激活方向或共享於有害提示的低維拒絕子空間來調節：消除這些方向會抑制拒絕，同時在很大程度上保留其他模型能力。然而，目前仍不清楚為什麼在各種模型中，安全關鍵特徵會以集中、低維的結構出現。在對OLMo-2-0425-1B-Instruct的案例研究中，我們發現拒絕幾何反映了拒絕訓練：由拒絕完成的第一個標記損失所產生的激活更新解釋了最終的拒絕方向和拒絕子空間。我們通過拒絕數據集的訓練動態研究拒絕方向，並揭示它們的脆弱性與重複的拒絕開始有關，這反過來又與低維子空間中梯度和拒絕特徵的集中有關。在冷凍模型分析和受控的合成微調中，我們發現了一個硬化槓桿的證據：多樣的拒絕開始可以提高梯度和激活變化的穩定秩，使得使用向量消融攻擊更難以去除拒絕。

##### **A Few Pages of Markdown: Committed AI Configuration and Lower Quality Cost after Coding-Agent Adoption**
2608.25241v1 by Yegor Denisov-Blanch, Shyam Agarwal, Pavel Azaletskiy, Hao He, Rylan Schaeffer, Brando Miranda, Bogdan Vasilescu, Sanmi Koyejo

Coding agents increase development velocity but also technical debt. Prior work reports only average effects across adopters, hiding wide differences between teams. We introduce RAMP (Repository AI Maturity Profile), a four-level cumulative maturity model grounded in version-controlled artifacts that teams commit to configure AI tools. RAMP runs from behavioral rules and coding standards through named agent definitions to multi-agent orchestration, with observed practice concentrated in the first three levels. Across 441 repositories the levels behave as a cumulative scale, and independent human annotation reproduces RAMP's repository-level labels on 97% of a held-out sample. Adoption is cumulative, forward-only, and set-and-forget: 73.8% of artifacts are committed once and never modified. Re-estimating an existing agent-adoption panel within each stratum, agents accelerate development regardless of maturity (28-38% more commits), but quality diverges: among agent-first repositories, where the contrast is identified, those without committed AI configuration show roughly twice the increase in cognitive complexity (+53% versus +27%) and 1.7x the increase in static-analysis warnings. Because maturity is observational, correlated engineering discipline or model capability may explain part of the gap; we present these findings as hypothesis-generating and release RAMP as a reusable instrument.

摘要：編碼代理提高了開發速度，但也增加了技術負債。先前的研究報告僅顯示了採用者之間的平均效果，隱藏了團隊之間的巨大差異。我們介紹了 RAMP（倉庫 AI 成熟度檔案），這是一個基於版本控制的工件的四級累積成熟度模型，團隊承諾配置 AI 工具。RAMP 從行為規則和編碼標準開始，經過命名代理定義，最後到多代理協同，觀察到的實踐集中在前三個層級。在 441 個倉庫中，這些層級表現為一個累積的量表，而獨立的人類註釋在 97% 的持出樣本中再現了 RAMP 的倉庫級標籤。採用是累積的、僅向前的，並且是設置後即忘：73.8% 的工件僅被提交一次，從未修改。在每個層級內重新評估現有的代理採用面板，無論成熟度如何，代理都能加速開發（多 28-38% 的提交），但質量卻有所不同：在以代理為主的倉庫中，當對比被識別時，那些沒有提交 AI 配置的倉庫顯示出大約兩倍的認知複雜度增加（+53% 對比 +27%）以及 1.7 倍的靜態分析警告增加。由於成熟度是觀察性的，相關的工程紀律或模型能力可能解釋了部分差距；我們將這些發現作為生成假設的基礎，並將 RAMP 發布為一個可重用的工具。

##### **Self-Explanation Tutor for Active Study of CS1 Worked Examples**
2608.25180v1 by Arun-Balajiee Lekshmi-Narayanan, Mohammad Hassany, Kamil Akhuseyinoglu, Rully Hendrawan, Peter Brusilovsky

Worked examples are a important part of introductory programming, but reading their expert explanations is passive. Self explanation, students explaining the problem and its solution to themselves with subgoal level analysis, turns that study into an active task, yet it is hard to scale because assessing free-text explanations and returning timely feedback has had no easy automated solution. We investigate whether a large language model (LLM) can fill that gap. We build a self-explanation tutor for introductory programming, ESSE, in which students explain lines of worked examples and receive immediate LLM feedback on the correctness and completeness of each explanation, and we pursue two goals. First, we ask whether the LLM judges student explanations well enough to serve as the engine of the tutor; we assess its judgments against two independent human reference standards of different kinds, a single domain expert and a crowd of non-expert raters, each with its own strengths and weaknesses, characterizing both where the LLM is reliable and the systematic tendencies in how it diverges. Second, we ask whether the LLM-based tutoring benefits students; deploying it in an introductory Java course, we find that its feedback leads students to persist and revise rather than abandon a line, that their explanations grow more complete and conceptually richer across attempts, and that students show evidence of learning. These indicate that LLM-based assessment is good enough to power a self-explanation tutor, and that the tutor positively shapes how students study worked examples.

摘要：範例題是入門程式設計的重要部分，但閱讀專家的解釋是被動的。自我解釋，學生將問題及其解決方案用子目標分析解釋給自己，將學習轉變為一項主動任務，然而，由於評估自由文本解釋並及時回饋的自動化解決方案並不容易，因此這很難擴展。我們調查大型語言模型（LLM）是否能填補這一空白。我們為入門程式設計建立了一個自我解釋輔導工具ESSE，學生在其中解釋範例題的每一行，並立即獲得LLM對每個解釋的正確性和完整性的反饋，我們追求兩個目標。首先，我們詢問LLM是否能夠足夠好地評價學生的解釋，以作為輔導工具的引擎；我們將其評價與兩個不同類型的獨立人類參考標準進行比較，一位單一領域專家和一群非專家評估者，各自有其優勢和劣勢，描述LLM可靠的地方以及其偏差的系統性趨勢。其次，我們詢問基於LLM的輔導是否有益於學生；在一門入門Java課程中部署後，我們發現其反饋使學生能夠持續修正而非放棄某一行，他們的解釋在多次嘗試中變得更加完整和概念豐富，並且學生顯示出學習的證據。這些表明基於LLM的評估足夠好，可以驅動自我解釋輔導工具，並且該輔導工具積極影響學生學習範例題的方式。

##### **CareGraph: An Auditable Hybrid AI Framework for Evidence-Grounded Personalized Longitudinal Health Intelligence**
2608.27484v1 by Pratik Ghawate, Tanvi Patil

Artificial intelligence is transforming personalized healthcare, yet fragmented clinical, self reported, and wearable evidence remains difficult to interpret and trace. We present CareGraph, an auditable hybrid AI framework that converts heterogeneous records into prioritized trends, missing context indicators, bounded next steps, discussion questions, and provenance linked explanations. CareGraph organizes evidence without diagnosing, predicting outcomes, selecting treatment, or making autonomous clinical decisions. Its pipeline covers deterministic analysis, context detection, graph construction, constrained language model synthesis, evidence validation, safety controls, and release gating. Tests used synthetic cohorts of 400 patients each for development, validation, and holdout. On holdout data, a frozen ordinary least squares trend rule with a sufficiency gate achieved 0.827 accuracy, 0.837 macro F1 with a 95 percent confidence interval of 0.819 to 0.854, and 0.974 insufficient data F1. Missing context detection achieved 0.815 strict micro F1 versus 0.318 for the legacy detector. On an authored holdout benchmark, safety ruleset version 1.2 achieved 1.000 precision, 0.950 recall, and 0.974 F1. An audit requiring graph retrieval across 80 patients yielded 79 syntheses and 78 presentations without fallback; one output was blocked and one failed closed because of an invalid evidence key. Against monolithic GPT 5.6 on 56 matched patients, CareGraph was faster at 40.15 versus 49.62 seconds, shorter at 661 versus 1,163 words, and showed better exploratory lexical alignment with longitudinal targets; the baseline used fewer tokens and cited more raw evidence. Graph auditing verified provenance and deterministic retrieval; incremental graph effects on generation require paired evaluation. CareGraph offers a safety bounded foundation for intelligent personalized health systems.

摘要：人工智慧正在改變個人化醫療，但分散的臨床、自我報告和可穿戴證據仍然難以解釋和追蹤。我們提出了 CareGraph，一個可審計的混合 AI 框架，將異質記錄轉換為優先趨勢、缺失的上下文指標、有限的下一步、討論問題和來源連結的解釋。CareGraph 組織證據而不進行診斷、預測結果、選擇治療或做出自主臨床決策。其流程涵蓋確定性分析、上下文檢測、圖形構建、受限語言模型合成、證據驗證、安全控制和發布閘道。測試使用了每組 400 名患者的合成隊列進行開發、驗證和保留。在保留數據上，一個凍結的普通最小二乘趨勢規則與充分性閘道達到了 0.827 的準確率，0.837 的宏 F1，95% 的置信區間為 0.819 至 0.854，以及 0.974 的不足數據 F1。缺失上下文檢測達到了 0.815 的嚴格微 F1，而舊檢測器為 0.318。在一個作者保留的基準上，安全規則集版本 1.2 達到了 1.000 的精確率，0.950 的召回率和 0.974 的 F1。一次需要在 80 名患者中檢索圖形的審計產生了 79 次合成和 78 次展示，沒有回退；一個輸出被阻止，另一個因為無效的證據鍵而失敗關閉。與 56 名匹配患者的單體 GPT 5.6 相比，CareGraph 在 40.15 秒的速度上更快，而 49.62 秒則較慢，字數上為 661 與 1,163，並且在與縱向目標的探索性詞彙對齊上表現更好；基線使用了更少的標記並引用了更多的原始證據。圖形審計驗證了來源和確定性檢索；生成上的增量圖形效果需要配對評估。CareGraph 為智能個人健康系統提供了一個安全的基礎。

##### **NVExplain: Explaining Time Series Forecasting with Latent Trajectory Analysis and Structure-Preserving Surrogates**
2608.25080v1 by Muyan Anna Li, Manikandan Ravikiran, Aditi Gautam

Time series forecasting models are widely used in high-stakes settings, yet their predictions remain difficult to interpret because existing post-hoc methods often ignore temporal dependence and fail to provide horizon-specific explanations. We propose a model-agnostic explainability framework that explains forecasting predictions by attributing each forecast horizon to temporally relevant historical lags. The framework models forecasting as a latent trajectory and introduces semantic flow to quantify how information evolves across time in the model's internal representations. By aggregating semantic flow, it constructs a lag-horizon attribution matrix that captures horizon-resolved temporal influence. To improve explainability, we further generate structure-preserving perturbations and fit sparse local surrogate models, producing human-readable and temporally coherent explanations. We evaluate the method using faithfulness and stability diagnostics across multiple benchmark datasets. Results show that the semantic-flow variant achieves competitive or superior faithfulness compared to standard post-hoc baselines, while being substantially more computationally efficient. Stability analysis further demonstrates that the explanations are robust and identifies regimes where interpretation should be applied with caution.

摘要：時間序列預測模型在高風險環境中被廣泛使用，然而它們的預測結果仍然難以解釋，因為現有的事後方法往往忽略了時間依賴性，並且未能提供特定於預測時間範圍的解釋。我們提出了一種與模型無關的可解釋性框架，通過將每個預測時間範圍歸因於與時間相關的歷史滯後來解釋預測結果。該框架將預測建模為潛在的軌跡，並引入語義流來量化信息在模型內部表示中的演變過程。通過聚合語義流，它構建了一個滯後-時間範圍歸因矩陣，以捕捉時間範圍解決的時間影響。為了提高可解釋性，我們進一步生成保持結構的擾動並擬合稀疏的局部替代模型，產生可讀且時間上連貫的解釋。我們使用忠實性和穩定性診斷在多個基準數據集上評估該方法。結果顯示，語義流變體在忠實性方面達到了與標準事後基準相當或更優的表現，同時在計算效率上顯著更高。穩定性分析進一步證明了解釋的穩健性，並識別出應謹慎應用解釋的範疇。

##### **HealthBench-Psych: A Mental Health Subset of OpenAI's HealthBench**
2608.25071v1 by Matthew Flathers, Phuong Anh Nguyen, Jill Noorily, Julian Herpertz, Meiting Chen, Jasreen Multani, Samuel Powell, Mason Granof, Mark Kalinch, John Torous

General-purpose health benchmarks increasingly anchor claims about LLM medical performance, but they are not always resolved by clinical specialty, making domain-specific performance hard to isolate. Mental health is of acute public-health concern as millions of people turn to LLMs for psychological support, and most existing evaluations are bespoke academic benchmarks that are difficult to integrate into developer workflows. We introduce HealthBench-Psych and HealthBench-Psych-Hard. We screened HealthBench's 5,000 physician-rubric conversations for mental-health relevance with a transparent LLM-applied rubric, then validated the subset through two rounds of blinded clinician review with concealed known-exclude controls, yielding 610 conversations (12.2% of the corpus). Evaluating 20 frontier and open models under a cross-vendor panel of three LLM judges, we find a statistically tied frontier cluster, measurable refusal behavior in two models, and near-identical rankings across judges ($τ\ge 0.92$). We release the subset, pipeline, model responses, grades, and analysis code as a reusable resource.

摘要：一般用途的健康基準越來越多地成為有關大型語言模型（LLM）醫療表現的主張基礎，但它們並不總是依據臨床專業來解決，使得特定領域的表現難以孤立。心理健康是當前公共健康的重大關注，因為數百萬人轉向LLM尋求心理支持，而現有的大多數評估都是定制的學術基準，難以整合進開發者的工作流程。我們推出了HealthBench-Psych和HealthBench-Psych-Hard。我們使用透明的LLM應用標準篩選了HealthBench的5,000個醫生評分對話，以確定其心理健康相關性，然後通過兩輪盲法臨床醫生審查進行驗證，並隱藏已知排除控制，最終獲得610個對話（佔語料庫的12.2%）。在三位LLM評審的跨供應商小組下評估20個前沿和開放模型，我們發現一個統計上相當的前沿集群，兩個模型中可測量的拒絕行為，以及評審之間幾乎相同的排名（$τ\ge 0.92$）。我們將這個子集、管道、模型回應、評分和分析代碼作為可重用資源發布。

##### **A Primer on Computational Semantics for Artificial Intelligence Systems**
2608.25022v1 by Casey Kennington

As people adopt transformer-based language models (e.g., ChatGPT and Gemini) for an increasing number of use-cases, it is important to know how such models learn and represent the meaning of the language, and to be more informed about what language is. This document is an attempt to help the reader understand how linguistic meaning (i.e., semantics) is approached from different fields of scientific and philosophical examination. I also explain three primary semantic theories: formal semantics, grounded semantics, and distributional semantics then compare how transformer-based language models differ from how humans learn language.

摘要：隨著人們對基於Transformer的語言模型（例如，ChatGPT 和 Gemini）在越來越多的使用案例中的採用，了解這些模型如何學習和表達語言的意義，以及對語言有更深入的認識是很重要的。這份文件旨在幫助讀者理解語言意義（即語義）在不同科學和哲學領域中的研究方法。我還解釋了三種主要的語義理論：形式語義、基於實體的語義和分佈式語義，然後比較基於Transformer的語言模型與人類學習語言的不同之處。

##### **StrokeGuard: A Multi-Agent Guided System for Prehospital Stroke Assessment**
2608.24555v1 by Wentao Yang, Zhenye Xu, Ruoyi Li, Musen Zhang, Yao Guo

Prehospital stroke assessment aims to accurately identify stroke symptoms and make rapid decisions through standardized procedures within an extremely narrow time window, thereby saving valuable time for subsequent treatment. In clinical practice, FAST-based scales are widely used for prehospital stroke assessment by issuing instructions that guide subjects to perform specific actions to screen facial, arm, and speech functions. However, in home and community settings, non-clinical users often encounter challenges such as inaccurate descriptions, incomplete symptom observation, and difficult operational procedures, which may lead to inaccurate or biased assessment results. To address these challenges, this paper presents StrokeGuard: a multi-agent guided system designed for prehospital stroke assessment that makes mobile FAST screening more standardized and executable. Specifically, to overcome the limitations of traditional single-agent systems in terms of procedural fault tolerance and user guidance capability, StrokeGuard adopts a dual-channel agent mechanism that separates formal assessment (i.e., facial palsy, arm weakness, speech impairment) from procedural support (e.g., step prompts, error correction, and real-time feedback). It guides the assessment process through multi-agent collaboration, dual-channel interaction, state-machine control, and stage-local fallback recovery mechanisms. Stage-specific scoring is delegated to constrained pretrained video assessment modules, while evidence source records are integrated with structured report generation. The user evaluation uses MATES-9, an exploratory scale for measuring user experience in multistep AI-guided tasks. In a simulated prehospital scenario, StrokeGuard improves the MATES-9 total score over a paper FAST-style form by 10.83 points, corresponding to a 23.8% relative increase.

摘要：預醫療中風評估的目的是準確識別中風症狀並通過標準化程序在極其狹窄的時間窗口內迅速做出決策，從而為隨後的治療節省寶貴的時間。<lb/>在臨床實踐中，基於FAST的量表被廣泛用於預醫療中風評估，通過發出指示來引導受試者執行特定動作，以篩查面部、手臂和語音功能。<lb/>然而，在家庭和社區環境中，非臨床用戶經常面臨挑戰，例如不準確的描述、不完整的症狀觀察和困難的操作程序，這可能導致不準確或偏頗的評估結果。<lb/>為了解決這些挑戰，本文提出了StrokeGuard：一個多代理引導系統，旨在進行預醫療中風評估，使移動FAST篩查更加標準化和可執行。<lb/>具體而言，為了克服傳統單一代理系統在程序容錯和用戶引導能力方面的限制，StrokeGuard採用了雙通道代理機制，將正式評估（即面部癱瘓、手臂無力、語言障礙）與程序支持（例如步驟提示、錯誤修正和實時反饋）分開。<lb/>它通過多代理協作、雙通道互動、狀態機控制和階段本地回退恢復機制引導評估過程。<lb/>階段特定的評分委託給受限的預訓練視頻評估模塊，同時證據來源記錄與結構化報告生成相結合。<lb/>用戶評估使用MATES-9，這是一個用於測量多步驟AI引導任務中用戶體驗的探索性量表。<lb/>在模擬的預醫療場景中，StrokeGuard使MATES-9總分比紙質FAST風格表單提高了10.83分，對應於23.8%的相對增長。

##### **LumiXAI: A Modular Full-Stack Framework for Feature Attribution**
2608.24524v1 by Alfio Ferrara, Lorenzo Gatta, Sergio Picascia, Elisabetta Rocchetti

Feature attribution is a central tool of model interpretability, yet the software through which it is applied remains fragmented: individual tools specialize along narrow axes, such as a single modality, a code API or a GUI, or a fixed rather than extensible method set, and rarely combine these strengths. Moreover, many explainability tools are designed primarily for domain experts, requiring programming skills or familiarity with attribution methods that can make them difficult for non-expert users to access. In this article, we present LumiXAI, a modular full-stack framework that consolidates attribution analysis into a single system. It couples classification and generative attribution with an interactive GUI supporting bidirectional exploration, a plug-in architecture for registering new models and methods, and three access tiers serving non-programmers, developers, and extenders from one backend. Its contribution is a system that operationalises established attribution methods under one interface, one interaction model, and one persistence layer, with containerised services and persistent results making analyses reproducible across machines.

摘要：特徵歸因是模型可解釋性的核心工具，但其應用的軟體仍然是零散的：個別工具專注於狹窄的方向，例如單一模態、代碼 API 或 GUI，或是固定而非可擴展的方法集，並且很少結合這些優勢。此外，許多可解釋性工具主要是為領域專家設計的，需要編程技能或對歸因方法的熟悉，這使得非專業用戶難以訪問。在本文中，我們介紹了 LumiXAI，一個模組化的全棧框架，將歸因分析整合到一個系統中。它將分類和生成性歸因與支持雙向探索的互動 GUI 結合在一起，並提供一個插件架構以註冊新模型和方法，以及三個訪問層級，服務於非程序員、開發者和擴展者，均來自同一後端。它的貢獻是一個在一個介面、一個互動模型和一個持久層下，將已建立的歸因方法操作化的系統，容器化的服務和持久的結果使得分析在不同機器之間可重現。

##### **A Formal Methodological Framework for Auditing Robustness and Fidelity in Explainable AI: From Application to Trust Certification**
2608.23817v1 by Rosa Elysabeth Ralinirina, Jean Christian Ralaivao, Niaiko Michaël Ralaivao, Alain Josué Ratovondrahona, Thomas Mahatody

SHAP and LIME are now standard tools for interpreting black-box predictions, yet their outputs can vary substantially when the input is perturbed by small amounts of noise--a problem we observed firsthand in our previous work on food security in Madagascar (Ralinirina et al., 2025). This variability raises the question of whether such explanations can be trusted at all. We address it by constructing an auditing protocol that measures two properties of any post-hoc explainer: robustness (how stable the explanation is under input perturbation) and fidelity (whether the features deemed important actually drive the model's prediction). These two quantities are combined into a single Trust Score. We run the protocol on a multi-sectoral dataset from Madagascar (83 features, 253 records, 4 malnutrition classes) using three classifiers and two explainers, plus their regularized counterparts. The results are sobering: models with AUC above 0.99 can produce numerically degenerate or flatly uninformative explanations, and fidelity scores lose discriminative power when the model is overfitted. These findings suggest that auditing XAI outputs is not optional but necessary, particularly when they inform decisions in sensitive domains.

摘要：SHAP 和 LIME 現在已成為解釋黑箱預測的標準工具，但當輸入受到小量噪音擾動時，它們的輸出可能會有顯著變化——這是我們在之前關於馬達加斯加食品安全的研究中親身觀察到的問題 (Ralinirina et al., 2025)。這種變異性引發了這樣的問題：這些解釋是否可以信任。我們通過構建一個審核協議來解決這個問題，該協議測量任何事後解釋器的兩個特性：穩健性（在輸入擾動下解釋的穩定性）和忠實度（被認為重要的特徵是否實際驅動模型的預測）。這兩個量結合成一個單一的信任分數。我們在來自馬達加斯加的多部門數據集上運行該協議（83個特徵，253條記錄，4個營養不良類別），使用三個分類器和兩個解釋器，以及它們的正則化對應物。結果令人警醒：AUC 超過 0.99 的模型可能會產生數值退化或完全無信息的解釋，並且當模型過擬合時，忠實度分數會失去區分能力。這些發現表明，審核 XAI 輸出不是可選的，而是必要的，特別是當它們影響敏感領域的決策時。

##### **LUCAID: Agentic Multimodal AI for Lung Cancer Precision Pathology**
2608.23803v2 by Marie-Lisa Eich, Kai Standvoss, Timo Milbich, Alexander Möllers, Miriam Hägele, Philipp Anders, Lars Tharun, Hanna Kontradiuk, Sebastian Kons, Nader Aldoj, Recepcan Adigüzel, Adam Narai, Lukas Hönig, Jonathan Striebel, Binru Yang, Mihnea P. Dragomir, Marvin Sextro, Philipp Keyl, Philipp Jurmeister, Rosemarie Krupar, Evelyn Ramberger, James Wells, Julika Ribbat-Idel, Andreas Kunft, Hussam Shuaib, Christian Grohé, Reinhard Büttner, David Horst, Klaus-Robert Müller, Lukas Ruff, Maximilian Alber, Frederick Klauschen, Simon Schallenberg

Lung cancer tissue diagnostics is complex, as therapy decisions in precision oncology rely on the integration of histomorphological, immunohistochemical, and molecular features. Yet pathological assessment remains largely visual and semi-quantitative and shows interobserver variability, while existing artificial intelligence (AI) tools cover only selected tasks, rarely reach generalizable expert-level performance, and lack prospective clinical validation. To address these challenges, we developed and clinically validated LUCAID, an agentic AI system for precision lung cancer pathology. An integrative agent couples diagnostic reasoning with nine modules that cover the full routine workflow, from quality control, tumor detection and segmentation, histological subtyping, tumor microenvironment profiling, tumor cellularity quantification, and predictive biomarker scoring (PD-L1, MET, TROP-2) to automated structured report generation. LUCAID enables users to interactively query the module outputs and generate reports that contextualize the results. Against large-scale expert ground-truth annotations, the analysis modules achieved F1 scores of 0.82-0.95. In prospective clinical validation, LUCAID reached 93.0% concordance with an expert-panel adjudicated reference standard across clinically actionable decisions, compared with 68.3-81.1% for five experienced thoracic pathologists.

摘要：肺癌組織診斷是複雜的，因為精準腫瘤學中的治療決策依賴於組織形態學、免疫組織化學和分子特徵的整合。<lb/>然而，病理評估仍然主要是視覺和半定量的，並且顯示出觀察者之間的變異性，而現有的人工智慧（AI）工具僅涵蓋選定的任務，鮮少達到可普遍應用的專家級表現，且缺乏前瞻性的臨床驗證。<lb/>為了解決這些挑戰，我們開發並臨床驗證了LUCAID，一個用於精準肺癌病理學的代理AI系統。<lb/>這個整合代理將診斷推理與九個模組結合，涵蓋從質量控制、腫瘤檢測和分割、組織學亞型分類、腫瘤微環境分析、腫瘤細胞數量量化，以及預測生物標記評分（PD-L1、MET、TROP-2）到自動化結構報告生成的完整常規工作流程。<lb/>LUCAID使使用者能夠互動查詢模組輸出並生成報告，以便將結果置於上下文中。<lb/>在大規模專家真實標註的對比下，分析模組達到了0.82-0.95的F1分數。<lb/>在前瞻性臨床驗證中，LUCAID在臨床可行的決策中與專家小組裁定的參考標準達到了93.0%的一致性，而五位經驗豐富的胸腔病理學家則為68.3-81.1%。


### Medical
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-09-03**|**A Computationally Feasible Framework for Causal Probabilistic Explanation**|Rafal Urbaniak et.al.|[2609.04177v1](http://arxiv.org/abs/2609.04177v1)|null|
|**2026-09-03**|**Instruction Duplication as an Inference-Time Control Primitive**|Victor Lavrenko et.al.|[2609.04024v1](http://arxiv.org/abs/2609.04024v1)|null|
|**2026-09-03**|**LLM4CKD: Large Language Models for Early Stage Chronic Kidney Disease Screening**|Muhammad Ashad Kabir et.al.|[2609.04013v1](http://arxiv.org/abs/2609.04013v1)|null|
|**2026-09-03**|**The Blind Spot in 2D Infants' Pose Estimation:Robust Learning from Noisy Annotations**|Emanuele Cardinale et.al.|[2609.04009v1](http://arxiv.org/abs/2609.04009v1)|null|
|**2026-09-03**|**Investigating the Ability of Large Language Models to Analyze Recipes for Diabetes**|Revathy Venkataramanan et.al.|[2609.03967v1](http://arxiv.org/abs/2609.03967v1)|null|
|**2026-09-03**|**RARF: Region-Aware Rectified Flows for 3D Brain MRI Inpainting**|Tomas Guija-Valiente et.al.|[2609.03956v1](http://arxiv.org/abs/2609.03956v1)|null|
|**2026-09-03**|**STAIR (STructure Aware Information Retriever): A novel dataset and LLM based retriever for document structure augmentation**|Vineet Kumar et.al.|[2609.03874v1](http://arxiv.org/abs/2609.03874v1)|null|
|**2026-09-03**|**Analysis of Prompt Engineering for Drug Toxicity Prediction**|Mia MacGregor et.al.|[2609.03635v1](http://arxiv.org/abs/2609.03635v1)|null|
|**2026-09-03**|**Feature Reconfiguration With Visual Prior for Medical Lesion Segmentation**|Yinan Liu et.al.|[2609.03535v1](http://arxiv.org/abs/2609.03535v1)|null|
|**2026-09-03**|**NeoRed: A Knowledge-Logic-Alignment Multimodal Large Language Model for Neonatal Respiratory Disease Diagnosis**|Yinan Liu et.al.|[2609.03527v1](http://arxiv.org/abs/2609.03527v1)|null|
|**2026-09-03**|**LongCounsel-8: A Benchmark Suite for Longitudinal Depression Tracking from Multi-Session Counseling Dialogues**|Jiayi Li et.al.|[2609.03507v1](http://arxiv.org/abs/2609.03507v1)|null|
|**2026-09-02**|**Untangling the Mechanisms of Misleading Context in Medical Question Answering**|Robin Linzmayer et.al.|[2609.02754v1](http://arxiv.org/abs/2609.02754v1)|null|
|**2026-09-02**|**HiPoly: a hierarchical polymer-native AI framework for property prediction and generative design**|Ge Sun et.al.|[2609.02746v1](http://arxiv.org/abs/2609.02746v1)|null|
|**2026-09-02**|**From Tokens to Semantics: Leveraging Complementary Signals for Hallucination Detection in Black-Box LLMs**|Urja Pawar et.al.|[2609.02679v1](http://arxiv.org/abs/2609.02679v1)|null|
|**2026-09-02**|**ORB-SVM : An Innovative Hybrid Framework for Efficient Brain Tumor Detection from MRI Scans**|Amirhosein Azarpour et.al.|[2609.02333v1](http://arxiv.org/abs/2609.02333v1)|null|
|**2026-09-02**|**SAUF-Net: Structure--Appearance Representation Learning with Uncertainty Feedback for Semi-Supervised Medical Image Segmentation**|Qin Lu et.al.|[2609.02247v1](http://arxiv.org/abs/2609.02247v1)|null|
|**2026-09-02**|**Examining the Vulnerability of Multi-Agent Medical Systems to Human Interventions for Clinical Reasoning**|Benjamin C Liu et.al.|[2609.02191v1](http://arxiv.org/abs/2609.02191v1)|null|
|**2026-09-02**|**Disease Burden over Skin Tone: Decomposing the Dermatology-AI Generalization Gap**|Nirajan Kunwor et.al.|[2609.02111v1](http://arxiv.org/abs/2609.02111v1)|null|
|**2026-09-02**|**Federated LoRA Adaptation of BiomedCLIP Across Four International Chest X-Ray Cohorts**|Sanjaya Poudel et.al.|[2609.02101v1](http://arxiv.org/abs/2609.02101v1)|null|
|**2026-09-02**|**READY or Not: Reliable Enterprise Agent Deployment**|Veronica Chatrath et.al.|[2609.02095v1](http://arxiv.org/abs/2609.02095v1)|null|
|**2026-09-02**|**InstEditSeg: Instruction-Driven Image Editing for Polyp and Skin Lesion Segmentation**|Ziquan Liu et.al.|[2609.02004v1](http://arxiv.org/abs/2609.02004v1)|null|
|**2026-09-01**|**The Ceiling Is in the Channel: Auditing Learner Gaps and Measurement Frontiers in Clinical Prediction**|Sayeed Shafayet Chowdhury et.al.|[2609.01909v1](http://arxiv.org/abs/2609.01909v1)|null|
|**2026-09-01**|**Import What You Need: Learning When and How to Augment EHR Graphs with External Knowledge**|Chen Chen et.al.|[2609.01839v1](http://arxiv.org/abs/2609.01839v1)|null|
|**2026-09-01**|**Interpretable Symptom Vectors for Depression in a Large Language Model**|Fangyi Zhu et.al.|[2609.01832v1](http://arxiv.org/abs/2609.01832v1)|null|
|**2026-09-01**|**GazeRefine: Expert Gaze as a Test-Time Prompt for Training-Free Medical Image Segmentation**|Mohammed Oussama Benyahia et.al.|[2609.01310v1](http://arxiv.org/abs/2609.01310v1)|null|
|**2026-09-01**|**Towards AI-Assisted Clinical Trial Matching: Practical Considerations, Multicenter Evaluation, and Real-World Deployment**|Yin Fang et.al.|[2609.01202v1](http://arxiv.org/abs/2609.01202v1)|null|
|**2026-09-01**|**CARE: Contrastive Anchor-based Rubric Evolution for Large Language Model Post-Training**|Siyuan Li et.al.|[2609.00892v1](http://arxiv.org/abs/2609.00892v1)|null|
|**2026-09-01**|**Benchmarking Vision-Language Models for Automated Pathology Diagnosis and Report Generation**|Yumi Lee et.al.|[2609.00866v1](http://arxiv.org/abs/2609.00866v1)|null|
|**2026-08-31**|**One note in three: a verified census of three deployed AI scribes, and the instrument that counted it**|Sebastian Fox et.al.|[2608.31017v1](http://arxiv.org/abs/2608.31017v1)|null|
|**2026-08-31**|**LLM Judges Verify Presence, Not Absence: Omission Blindness in AI Clinical Notes and What Recovers It**|Sebastian Fox et.al.|[2608.31016v1](http://arxiv.org/abs/2608.31016v1)|null|
|**2026-08-31**|**MR-JEPA: A General Purpose Video Foundation Model for Cardiac MRI**|Athira J. Jacob et.al.|[2608.30975v1](http://arxiv.org/abs/2608.30975v1)|null|
|**2026-08-31**|**Responsible Integration of AI in Cancer Genomics: Barriers, Risks, and Pathways to Trustworthy Clinical Translation**|Bahar İlgen et.al.|[2608.30912v1](http://arxiv.org/abs/2608.30912v1)|null|
|**2026-08-31**|**TAMI: Temporally Aligned, Missingness-Aware, and Interpretable Multimodal Fusion for Mental Health Assessment in Older Adults with Mild Cognitive Impairment**|Merna Bibars et.al.|[2608.30857v1](http://arxiv.org/abs/2608.30857v1)|null|
|**2026-08-31**|**VFR-Audit: Verdict-Level Reliability for Fairness Audits in Hospital Length-of-Stay Prediction**|Md Jannatul Rakib Joy et.al.|[2608.30846v1](http://arxiv.org/abs/2608.30846v1)|null|
|**2026-08-31**|**SingProbe Technical Report**| Sing Team et.al.|[2608.30703v1](http://arxiv.org/abs/2608.30703v1)|null|
|**2026-08-31**|**MedAgent-R1: Faithfulness-Aware Reinforcement Learning for Evidence-Grounded Medical Reasoning**|Jiangwang Chen et.al.|[2608.30676v1](http://arxiv.org/abs/2608.30676v1)|null|
|**2026-08-31**|**Whole-Slide Image Analysis under Realistic Few-Shot Annotation Protocols**|Tiffanie Godelaine et.al.|[2608.30420v1](http://arxiv.org/abs/2608.30420v1)|null|
|**2026-08-31**|**Dense Clinical Contrasts Enhance Medical Knowledge Updating in Large Language Models**|Yangmin Huang et.al.|[2608.30405v1](http://arxiv.org/abs/2608.30405v1)|null|
|**2026-08-31**|**ImageCAS-X: a dataset and benchmark for coronary artery segmentation and centerline extraction in coronary CT angiography**|Kit M. Bransby et.al.|[2608.30404v1](http://arxiv.org/abs/2608.30404v1)|null|
|**2026-08-31**|**AI Morbidity and Mortality: A Framework for Clinical AI Failure Review**|Paulius Mui et.al.|[2609.00076v1](http://arxiv.org/abs/2609.00076v1)|null|
|**2026-08-31**|**Co-Annotator: Expert-Distilled ViT and VLM for Visual and Documentation Guidance in Age-Related Macular Degeneration**|Ziheng "Leo" Li et.al.|[2608.30352v1](http://arxiv.org/abs/2608.30352v1)|null|
|**2026-08-31**|**MiNER: Fine-Tuned Biomedical Natural Language Processing for Malaria Disease Entity Recognition in Clinical Texts**|V. S. Anoop et.al.|[2609.00073v1](http://arxiv.org/abs/2609.00073v1)|null|
|**2026-08-31**|**CoLa-ICD: A Knowledge-Enhanced Framework for Long-Tail Automated Medical Coding**|Yihang Cheng et.al.|[2608.30234v1](http://arxiv.org/abs/2608.30234v1)|null|
|**2026-08-30**|**Automatic Conversion of NICE Guidelines to an Executable Computational Model Using Large Language Models**|Ashvin Gupta et.al.|[2608.30022v1](http://arxiv.org/abs/2608.30022v1)|null|
|**2026-08-30**|**Error Detection for PET/CT Radiology Reports: Domain-Specific vs Large Language Models**|Hermione Warr et.al.|[2608.30021v1](http://arxiv.org/abs/2608.30021v1)|null|
|**2026-08-30**|**Generating Clinical Vignettes that Preserve Cognitive Formulations**|Amit Oren et.al.|[2608.29995v1](http://arxiv.org/abs/2608.29995v1)|null|
|**2026-08-30**|**Review Before Trust: Source-Grounded Integrity Gates for AI-Assisted Personal Health Records**|Nora Girda et.al.|[2608.29965v1](http://arxiv.org/abs/2608.29965v1)|null|
|**2026-08-30**|**INTERVenE: Temporal-Abstraction-Interval Based Transformers for Short-Horizon Medical Event Prediction**|Shahar Oded et.al.|[2608.29901v1](http://arxiv.org/abs/2608.29901v1)|null|
|**2026-08-30**|**Life Operators: a self-evolving framework for multiscale life modelling**|Shuo Wang et.al.|[2609.00068v1](http://arxiv.org/abs/2609.00068v1)|null|
|**2026-08-30**|**Scientific Agent Skills: A Library of Procedural Knowledge for Research Agents**|Timothy Kassis et.al.|[2609.00065v2](http://arxiv.org/abs/2609.00065v2)|null|
|**2026-08-30**|**Medical Causal Hypothesis Verification with Large Language Models**|Safiyyah Ahmed et.al.|[2609.00063v1](http://arxiv.org/abs/2609.00063v1)|null|
|**2026-08-30**|**Zero-Shot Respiratory Sound Classification through LLM-Augmented Audio-Text Alignment**|Mustafa Talha İlerisoy et.al.|[2609.00055v1](http://arxiv.org/abs/2609.00055v1)|null|
|**2026-08-30**|**SUP-MIMIC: A Multi-Task Clinical Diagnosis Benchmark for Evaluating LLMs' Robustness to Contradictory Evidence**|Yi Yu et.al.|[2608.29582v1](http://arxiv.org/abs/2608.29582v1)|null|
|**2026-08-29**|**AI Can Be Easily Persuaded in Clinical Decision Making**|Jiayuan Zhu et.al.|[2608.29453v1](http://arxiv.org/abs/2608.29453v1)|null|
|**2026-08-29**|**Scalable Clinical Data Infrastructure and Comparative ML Evaluation for Hospitalisation Risk Prediction in Elderly Patients with Multiple Long-Term Conditions using CPRD**|Asra Aslam et.al.|[2608.29419v1](http://arxiv.org/abs/2608.29419v1)|null|
|**2026-08-29**|**Extending TotalSegmentator: Predicting Patient and Acquisition Characteristics from CT and MR Images**|Jakob Wasserthal et.al.|[2608.29348v1](http://arxiv.org/abs/2608.29348v1)|null|
|**2026-08-29**|**Predicting Future Organ Dysfunction in ICU Patients Using Temporal Convolutional Networks on MIMIC-IV Data**|Razan Albouq et.al.|[2608.29301v1](http://arxiv.org/abs/2608.29301v1)|null|
|**2026-08-29**|**AOI-Net: Structural Face AOI-Guided Eye-Gaze Track Representation Learning for Autism Spectrum Disorder Detection**|Zhanpei Huang et.al.|[2608.29289v1](http://arxiv.org/abs/2608.29289v1)|null|
|**2026-08-29**|**Not the Same Protector: Deployment-Dependent Protective Intervention in LLMs**|Eunna Lee et.al.|[2608.29136v1](http://arxiv.org/abs/2608.29136v1)|null|
|**2026-08-29**|**Beyond Correctness: Validity-Oriented Evaluation of Biomedical LLM Judges**|Rodrigo de Oliveira et.al.|[2608.29127v1](http://arxiv.org/abs/2608.29127v1)|null|
|**2026-08-29**|**Disentangling Representation using Attributes-based Gaussian Estimation for Medical Sound Diagnosis**|Ke Zhao et.al.|[2608.29026v1](http://arxiv.org/abs/2608.29026v1)|null|
|**2026-08-29**|**Frequency Selective Neural Networks as a Foundation Architecture for Time Series Learning**|Hui Huang et.al.|[2608.29012v1](http://arxiv.org/abs/2608.29012v1)|null|
|**2026-08-29**|**From Analytics to Tumor Boards: An Evidence-Linked Multi-Agent Workflow for Oncology Feature Extraction**|Daniel Kang et.al.|[2608.28974v2](http://arxiv.org/abs/2608.28974v2)|null|
|**2026-08-28**|**From the Loss Landscape to Diverse Feature Learning in Neural Networks**|David Aram Yunis et.al.|[2608.28948v1](http://arxiv.org/abs/2608.28948v1)|null|
|**2026-08-28**|**ActiveAugment: Online Active Learning for Augmentation Selection in Deep Learning**|Noah Videcrantz et.al.|[2608.28923v1](http://arxiv.org/abs/2608.28923v1)|null|
|**2026-08-28**|**Explainable Artificial Intelligence (XAI) in Computational Pathology: Definitions, Taxonomy, and Recommendations**|Shubham Innani et.al.|[2608.28820v1](http://arxiv.org/abs/2608.28820v1)|null|
|**2026-08-28**|**Texture Image Classification Using DWT AlexNet Feature Fusion and Deep Neural Networks**|Arun D. Kulkarni et.al.|[2608.28524v1](http://arxiv.org/abs/2608.28524v1)|null|
|**2026-08-28**|**PermitGPT: A Unified Generative-AI Pipeline for Construction Hazard Forecasting, Permit Prediction, and Community Impact**|Mohd Ruhul Ameen et.al.|[2608.28728v1](http://arxiv.org/abs/2608.28728v1)|null|
|**2026-08-28**|**ARC-CT: Anatomy-Routed Contrastive Vision-Language Learning for 3D Chest CT**|Huseyin Umut Isik et.al.|[2608.28455v1](http://arxiv.org/abs/2608.28455v1)|null|
|**2026-08-28**|**AI as Teammate: Rethinking Task Distribution in Medical Training**|Fendi Tsim et.al.|[2608.28373v1](http://arxiv.org/abs/2608.28373v1)|null|
|**2026-08-28**|**Real-Time Musculoskeletal Surrogates for Pediatric Cerebral Palsy: a Credibility Pilot**|Mohammad Arif Ul Alam et.al.|[2608.28371v1](http://arxiv.org/abs/2608.28371v1)|null|
|**2026-08-28**|**Propagating construction-time knowledge quality into medical question answering: A framework grounded in clinical guidelines**|Jie Hu et.al.|[2608.28360v1](http://arxiv.org/abs/2608.28360v1)|null|
|**2026-08-28**|**AGENT-O: A Semantic Agent Card Framework for Interoperable and Governed Healthcare AI Agents**|Pengze Li et.al.|[2608.28345v1](http://arxiv.org/abs/2608.28345v1)|null|
|**2026-08-28**|**Cross-Spectral Dense Correspondence for Multimodal Spectral Medical Imaging**|Eric L. Wisotzky et.al.|[2608.28341v1](http://arxiv.org/abs/2608.28341v1)|null|
|**2026-08-28**|**BanglaMed-QA: A Question Answering System for Healthcare Support in Bangla**|Rowzatul Zannat et.al.|[2608.28329v1](http://arxiv.org/abs/2608.28329v1)|null|
|**2026-08-28**|**Layered LLM Defenses as an Ensemble: Access Tiers, Inference Cost, and the Measured Failure Correlation Between Defense Layers**|Abrar Alotaibi et.al.|[2608.28327v1](http://arxiv.org/abs/2608.28327v1)|null|
|**2026-08-28**|**Physics-Guided Flow Matching for CT Image Reconstruction**|Davide Evangelista et.al.|[2608.28256v1](http://arxiv.org/abs/2608.28256v1)|null|
|**2026-08-28**|**Under-Mattress Temporal Sensing for Next-Day Agitation Risk Scoring in Dementia Wards**|Zhen Liu et.al.|[2608.28152v1](http://arxiv.org/abs/2608.28152v1)|null|
|**2026-08-28**|**Do Medical Vision Models Reason About Anatomy? Probing the Spatial Inductive Biases of Learned Visual Representations**|Naren Akash et.al.|[2608.28092v1](http://arxiv.org/abs/2608.28092v1)|null|
|**2026-08-28**|**Explainable Uncertainty Estimation for Reliable Medical AI**|Li Rong Wang et.al.|[2608.28052v1](http://arxiv.org/abs/2608.28052v1)|null|
|**2026-08-28**|**Automated Analysis Framework for Multilingual Climate-Health Literature Based on Multi-Agent Large Language Model**|Yuze Sun et.al.|[2608.27998v1](http://arxiv.org/abs/2608.27998v1)|null|
|**2026-08-28**|**A Deep Learning-Based Stacking Ensemble Framework for Turbofan Engine Remaining Useful Life Prediction**|Limon Bin Hossain et.al.|[2608.27940v1](http://arxiv.org/abs/2608.27940v1)|null|
|**2026-08-28**|**FedEHR-Agents: Federated Agentic Optimization for Automated EHR Modeling**|Jun Bai et.al.|[2608.27856v1](http://arxiv.org/abs/2608.27856v1)|null|
|**2026-08-28**|**From Uncertainty to Clinical Risk: Severity-Aware Conformal Planning for Interactive Medical Diagnosis**|Yue Zhou et.al.|[2608.27847v1](http://arxiv.org/abs/2608.27847v1)|null|
|**2026-08-27**|**CARDINAL Predicts Cardiovascular Risk From Non-contrast Cardiac CT**|Roy Gabriel et.al.|[2608.27690v1](http://arxiv.org/abs/2608.27690v1)|null|
|**2026-08-27**|**First Make It Playable, Then Make It Good: Staged Interaction Learning for Small Dialogue-Game Agents**|Syed Mahbubul Huq et.al.|[2608.27672v1](http://arxiv.org/abs/2608.27672v1)|null|
|**2026-08-27**|**Learning a Continuous Sepsis Severity Score Without Hour-by-Hour Supervision: A Two-Site Retrospective Study**|Kevin Zhu et.al.|[2608.27421v1](http://arxiv.org/abs/2608.27421v1)|null|
|**2026-08-27**|**How Language Models Organize and Structure Moral Knowledge**|Orion Reblitz-Richardson et.al.|[2608.27402v1](http://arxiv.org/abs/2608.27402v1)|null|
|**2026-08-27**|**Making Clinical Language Models Auditable: Concept-Guided Fine-Tuning for Robust Prediction**|Jin Mu et.al.|[2608.27397v1](http://arxiv.org/abs/2608.27397v1)|null|
|**2026-08-27**|**ORDDAR: Observation-Driven Reasoning for Distortion-Resilient Decision, Action, and Cognitive Recovery**|Deblina Kar et.al.|[2608.28704v1](http://arxiv.org/abs/2608.28704v1)|null|
|**2026-08-27**|**From Reasoning to Pixels: Grounded Medical Multimodal LLMs for VQA and Segmentation**|Haowen Gu et.al.|[2608.26856v1](http://arxiv.org/abs/2608.26856v1)|null|
|**2026-08-27**|**MedFG-VQA: Low-Frequency Memory and Graph Attention for Lightweight Medical VQA**|Haowen Gu et.al.|[2608.26848v1](http://arxiv.org/abs/2608.26848v1)|null|
|**2026-08-27**|**Accelerating Scientific Research with Gemini in the Real-World**|Samuel Schmidgall et.al.|[2608.26701v1](http://arxiv.org/abs/2608.26701v1)|null|
|**2026-08-26**|**Planetary Prediction Engine: Autonomous Geospatial Prediction via Intelligent Data Selection and Foundation Model Embeddings**|Evelyn Ma et.al.|[2608.26088v1](http://arxiv.org/abs/2608.26088v1)|null|
|**2026-08-26**|**FRAME: separating sampling variation from representational cause in medical imaging fairness**|Mahshad Lotfinia et.al.|[2608.25981v1](http://arxiv.org/abs/2608.25981v1)|null|
|**2026-08-26**|**PANDA - Prototype-Anchored Alignment for Partially Unpaired Multimodal Learning, with Applications to Alzheimers MRI and TCGA Pathology**|Sheethal Bhat et.al.|[2608.25970v1](http://arxiv.org/abs/2608.25970v1)|null|
|**2026-08-26**|**Why ML-based cough models do not generalize: a systematic cross-dataset evaluation for tuberculosis screening**|Wensi Zhang et.al.|[2608.25846v1](http://arxiv.org/abs/2608.25846v1)|null|
|**2026-08-26**|**Unsupervised Anatomical Feature Learning via Diffusion Models: Enhanced Medical Image Segmentation with Denoising Diffusion Probabilistic Models**|Akshat G et.al.|[2608.25693v1](http://arxiv.org/abs/2608.25693v1)|null|
|**2026-08-26**|**Hierarchical MoE for Multi-Modal ILD Diagnosis**|Alec K. Peltekian et.al.|[2608.25261v1](http://arxiv.org/abs/2608.25261v1)|null|
|**2026-08-26**|**What Do Medical Vision-Language Models Learn in Radiology? Transfer, Alignment, and Source-Proxy Leakage Under Distribution Shift**|Ayoub Louaye Bouaziz et.al.|[2608.25251v1](http://arxiv.org/abs/2608.25251v1)|null|

#### Abstracts
##### **A Computationally Feasible Framework for Causal Probabilistic Explanation**
2609.04177v1 by Rafal Urbaniak, Sam Witty, Daniel Waxman, Andy Zane, Poorva Garg, Emily Bunnapradist, Sankaran Vaidyanathan, Jack Feser, Drew Lehe, Eli Bingham

Explaining why a specific outcome occurred, and which inputs deserve the blame or credit, is central to philosophical, scientific, and policy analysis. Existing tools split into two camps. The theory of actual causality (AC) gives principled verdicts, but only for toy-sized models, because computing them requires enumerating counterfactual scenarios. Scalable attribution methods like SHAP (or even causal SHAP) at least partially ignore the causal structure that generated the data, and can give answers that conflict with a careful causal analysis. We close this gap with Probabilistic Causal Impact (PCI).
  PCI builds on actual causality and on Pearl's notions of probability of necessity and sufficiency, but recasts the question of explainability as an estimation problem on a probabilistic causal model that is easily approximated via Monte Carlo. By specifying a distribution over "candidate explanations," a distribution over counterfactual values, and a scoring function, PCI provides tractable, causally grounded, graded explanations, generalizing AC and Pearl's probability of causation as degenerate cases.
  We evaluate PCI in synthetic and real-world examples, spanning consistency checks with AC, scaling experiments, complex continuous-valued dynamical systems, and a real-world deployed causal machine learning model trained on millions of datapoints.

摘要：解釋為什麼特定結果發生，以及哪些輸入應該受到責備或讚譽，對於哲學、科學和政策分析至關重要。現有工具分為兩派。實際因果性理論（AC）給出原則性的判決，但僅限於玩具大小的模型，因為計算它們需要列舉反事實情境。可擴展的歸因方法如SHAP（甚至因果SHAP）至少在某種程度上忽略了生成數據的因果結構，並可能給出與仔細的因果分析相矛盾的答案。我們用概率因果影響（PCI）來填補這一空白。<lb/>  
PCI建立在實際因果性和Pearl的必要性與充分性概率概念之上，但將可解釋性的問題重新表述為在概率因果模型上的估計問題，該模型可以通過蒙特卡羅方法輕鬆近似。通過指定“候選解釋”的分佈、反事實值的分佈和評分函數，PCI提供了可處理的、基於因果的、分級的解釋，將AC和Pearl的因果概率概括為退化情況。<lb/>  
我們在合成和現實世界的例子中評估PCI，涵蓋與AC的一致性檢查、擴展實驗、複雜的連續值動態系統，以及一個在數百萬數據點上訓練的現實世界部署的因果機器學習模型。

##### **Instruction Duplication as an Inference-Time Control Primitive**
2609.04024v1 by Victor Lavrenko

Procedural instruction following is a basic requirement for controllable language-model systems, especially when generated trajectories are inspected or repaired downstream. We introduce instruction duplication, a minimal black-box inference-time control that repeats only the procedural instruction, without retraining or decoding changes. Across seven instruction-tuned models, 300 medical multiple-choice questions, eight placement conditions, and 16,800 scheduled generations, moving from one to two copies raises the deterministic All-8 diagnostic--responses passing all eight observable tests--from 90.22% to 93.17% (+2.95 percentage points), eliminating 30.2% of the failures remaining after one copy. Pre-provisional TF-IDF recall rises from 73.44% to 74.81% (+1.38 points; Holm-adjusted p < .001), while final-answer accuracy remains exactly 60.21%. Premature commitment increases from 1.52% to 2.30% (p_Holm = .00536). A blinded challenge audit yields 10/30 directional confirmations, 20/30 perceptual ties, and no reversals; its prespecified 28/30 confirmation criterion is not met. Yet this distinction can matter operationally when a downstream system acts on the generated trajectory. In Answer Engineering (AE), where explicit trajectory state determines local repair, the published reason-first no-editing SSNHL endpoint was 25.1%; system-only AE was later reproduced at 84.2%, and the same trailing duplicate raised it to 97.1%. For conductive diagnostic branch preservation, the corresponding values are 58.9% published without editing, 78.6% with reproduced AE, and 73.8% with AE plus duplication--a within-AE decrease, but still 14.9 points above the no-editing baseline. Instruction duplication is therefore a low-complexity, placement-sensitive control whose practical value can emerge through the downstream system that consumes the exposed trajectory.

摘要：程序指令遵循是可控語言模型系統的基本要求，特別是在生成的軌跡被檢查或修復的情況下。<lb/>我們引入了指令重複，這是一種最小的黑箱推理時控制，僅重複程序指令，而不進行再訓練或解碼更改。<lb/>在七個經過指令調整的模型中，300個醫學多選題、八個放置條件以及16,800次預定生成中，從一個副本增加到兩個副本使得確定性的All-8診斷——所有八個可觀察測試的反應均通過——從90.22%提高到93.17%（+2.95個百分點），消除了在一個副本後剩餘的30.2%的失敗。<lb/>預先的TF-IDF召回率從73.44%上升到74.81%（+1.38點；Holm調整後p < .001），而最終答案的準確率保持在60.21%。<lb/>過早承諾從1.52%增加到2.30%（p_Holm = .00536）。<lb/>一項盲測挑戰審核產生了10/30的方向確認、20/30的感知平局，並且沒有逆轉；其預先指定的28/30確認標準未達成。<lb/>然而，當下游系統根據生成的軌跡行動時，這一區別在操作上可能很重要。<lb/>在答案工程（AE）中，明確的軌跡狀態決定了局部修復，已發表的理由優先無編輯SSNHL終點為25.1%；系統僅AE後來重現為84.2%，而相同的尾隨副本將其提高至97.1%。<lb/>對於導電診斷分支保留，相應的值為58.9%未編輯發表，78.6%重現AE，和73.8% AE加上重複——在AE內部的下降，但仍比無編輯基準高出14.9點。<lb/>因此，指令重複是一種低複雜度、敏感於放置的控制，其實際價值可以通過消耗暴露軌跡的下游系統顯現出來。

##### **LLM4CKD: Large Language Models for Early Stage Chronic Kidney Disease Screening**
2609.04013v1 by Muhammad Ashad Kabir, Sirajam Munira

Early screening of chronic kidney disease (CKD) is critical for timely intervention, yet most machine learning (ML) and deep learning (DL) approaches require labeled data and model training, limiting their use in real-world screening settings. This study evaluates the effectiveness of large language models (LLMs) for CKD screening under zero-shot and few-shot in-context learning settings and compares them with traditional ML and DL methods. We propose a framework that uses clinically selected tabular features and structured prompt templates to enable LLM-based inference without task-specific training. LLM performance is evaluated across multiple prompt styles, feature configurations, and data settings, and compared with standard ML, DL, and tabular foundation model (TFM) baselines, and existing CKD screening tools. The results show that LLMs can achieve competitive performance using only a small number of examples, often matching or outperforming traditional approaches in low-data settings. However, their performance remains model-dependent and less stable as input complexity increases. In contrast, ML, DL, and TFM models show more consistent improvement with larger training data. Overall, the findings highlight a trade-off between data efficiency and stability, suggesting that LLMs may serve as a flexible complementary approach for CKD screening when labeled data are limited.

摘要：早期篩檢慢性腎病（CKD）對於及時介入至關重要，但大多數機器學習（ML）和深度學習（DL）方法需要標記數據和模型訓練，限制了它們在現實篩檢環境中的使用。這項研究評估了大型語言模型（LLMs）在零樣本和少樣本上下文學習環境中進行CKD篩檢的有效性，並將其與傳統的ML和DL方法進行比較。我們提出了一個框架，利用臨床選擇的表格特徵和結構化提示模板，使基於LLM的推斷能夠在不進行特定任務訓練的情況下進行。LLM的性能在多種提示風格、特徵配置和數據設置中進行評估，並與標準的ML、DL和表格基礎模型（TFM）基準以及現有的CKD篩檢工具進行比較。結果顯示，LLMs在僅使用少量示例的情況下可以實現具有競爭力的性能，通常在低數據環境中與傳統方法相匹配或超越。然而，隨著輸入複雜性的增加，它們的性能仍然依賴於模型，且穩定性較差。相比之下，ML、DL和TFM模型在較大訓練數據下顯示出更一致的改進。總體而言，研究結果突顯了數據效率與穩定性之間的權衡，表明在標記數據有限的情況下，LLMs可能作為CKD篩檢的靈活補充方法。

##### **The Blind Spot in 2D Infants' Pose Estimation:Robust Learning from Noisy Annotations**
2609.04009v1 by Emanuele Cardinale, Marco Proietti, Alessandro Cacciatore, Maria Francesca Spadea, Lucia Migliorelli, Sara Moccia

Noisy annotations pose a significant challenge for supervised deep learning, as neural networks rely on large-scale, high-quality labeled data whose corruption can severely impair model performance. Although robustness to label noise has been extensively studied for classification tasks, it remains relatively underexplored in Pose Estimation (PE). This limitation becomes critical in clinical contexts, including neonatology, where PE of preterm infants is used to support the assessment of spontaneous motility, a key indicator of neurodevelopmental trajectories. In such settings, infants' images labeling is further hindered by visual challenges (e.g., keypoint self-occlusions, caregiver interference), making the annotation process inherently susceptible to errors. To tackle noisy annotations in PE, we introduce REliable keypoint selection via Memory of traINing Dynamics (REMIND), a clustering-based keypoint-selection strategy that exploits keypoint-wise training dynamics to identify noisy labels without assuming any prior knowledge of the noise distribution, thus enabling noise-free model training. When evaluated on the proprietary NeoPose dataset, comprising 46 videos of 46 preterm infants recorded in real clinical settings, REMIND correctly identifies noisy annotations across multiple corruption scenarios, achieving up to 93\% Area Under the Curve (AUC) with three different PE architectures used in the relevant literature. To our knowledge, this is the first study to explicitly address label noise in preterm infants' PE, paving the way for the design of trustworthy learning-based algorithms for infants'monitoring support when data quality cannot be guaranteed.

摘要：嘈雜的標註對於監督式深度學習構成了重大挑戰，因為神經網絡依賴於大規模、高質量的標記數據，而這些數據的損壞可能會嚴重影響模型性能。儘管對於分類任務，標籤噪聲的魯棒性已經得到了廣泛研究，但在姿勢估計（PE）中仍然相對未被深入探討。這一限制在臨床環境中變得至關重要，包括新生兒科，因為對早產兒的PE用於支持自發運動性的評估，而自發運動性是神經發展軌跡的關鍵指標。在這樣的環境中，嬰兒影像的標註進一步受到視覺挑戰（例如，關鍵點自遮擋、護理人員干擾）的阻礙，使得標註過程本質上容易出錯。為了解決PE中的嘈雜標註問題，我們提出了通過訓練動態記憶的可靠關鍵點選擇（REMIND），這是一種基於聚類的關鍵點選擇策略，利用關鍵點的訓練動態來識別嘈雜標籤，而不假設任何噪聲分佈的先驗知識，從而實現無噪聲的模型訓練。在對專有的NeoPose數據集進行評估時，該數據集包含46個在真實臨床環境中錄製的早產兒視頻，REMIND能夠在多種損壞場景中正確識別嘈雜標註，並在相關文獻中使用的三種不同PE架構下達到高達93%的曲線下面積（AUC）。據我們所知，這是第一項明確針對早產兒PE中的標籤噪聲的研究，為在數據質量無法保證的情況下設計可靠的基於學習的算法以支持嬰兒監測鋪平了道路。

##### **Investigating the Ability of Large Language Models to Analyze Recipes for Diabetes**
2609.03967v1 by Revathy Venkataramanan, Aditya Luthra, Venkatesan Nadimuthu, Amit Sheth

Several studies have evaluated the ability of Large Language Models (LLMs) for meal planning, yielding positive outcomes. These models can process natural language inputs and leverage learned knowledge from their pretraining to generate meal plans. In this work, we investigate the ability of LLMs to analyze the suitability of given recipes for diabetes. The primary challenge for LLMs is to retrieve relevant dietary guidelines for diabetes, decompose recipes into ingredients and cooking methods, and apply these guidelines to determine the recipe's suitability. To study these challenges, we employ three kinds of prompts namely, (i) Direct Query Prompt (ii) Context-Guided Prompt, and (iii) Exemplary Context Prompt that incorporate different levels of diabetes dietary guidelines from medical sources. We introduce a benchmark dataset curated for this investigation consisting of 7607 recipes that include 3807 recipes suitable for diabetes and 3800 recipes not suitable for diabetes. Our results demonstrate that most LLMs are cautious in predicting recipes as suitable to prevent detrimental outcomes. Further, the models that can reason using the dietary guidelines performed better in predicting the suitability of recipes for diabetes. Overall, Mistral-7B and Llama 70B showed superior performance to their counterparts.

摘要：幾項研究評估了大型語言模型（LLMs）在餐點規劃方面的能力，結果顯示積極的成效。這些模型能夠處理自然語言輸入，並利用其預訓練所學的知識來生成餐點計劃。在本研究中，我們探討LLMs分析給定食譜對糖尿病適用性的能力。LLMs的主要挑戰在於檢索相關的糖尿病飲食指導方針，將食譜分解為成分和烹飪方法，並應用這些指導方針來確定食譜的適用性。為了研究這些挑戰，我們使用了三種提示，即（i）直接查詢提示（ii）上下文引導提示，以及（iii）示例上下文提示，這些提示整合了來自醫療來源的不同層級的糖尿病飲食指導方針。我們引入了一個為本研究精心策劃的基準數據集，該數據集包含7607個食譜，其中包括3807個適合糖尿病的食譜和3800個不適合糖尿病的食譜。我們的結果顯示，大多數LLMs在預測食譜是否適合時都很謹慎，以防止不良後果。此外，能夠利用飲食指導方針進行推理的模型在預測食譜對糖尿病的適用性方面表現更佳。總體而言，Mistral-7B和Llama 70B的表現優於其對手。

##### **RARF: Region-Aware Rectified Flows for 3D Brain MRI Inpainting**
2609.03956v1 by Tomas Guija-Valiente, Blanca Rodriguez-Gonzalez, Norberto Malpica, Angel Torrado-Carvajal

Medical image inpainting has the potential to improve automated brain MRI analysis by reconstructing healthy tissue within pathological regions. We introduce RARF, a task-agnostic region-aware rectified flow framework for masked data generation. We instantiate the framework for 3D brain MRI inpainting as our submission to the BraTS Inpainting Challenge 2026. RARF restricts the stochastic interpolation process to the inpainting region, while the observed voxels remain fixed and provide patient-specific anatomical context. A three-dimensional neural network receives the partially voided image, with Gaussian noise filling the missing region, together with the inpainting mask and the corresponding timestep. The model is trained using masked flow-matching and reconstruction-consistency objectives, combined with mask-aware preprocessing and data augmentation. During inference, the learned velocity field transports the initial noise toward a plausible reconstruction of the missing tissue, which is then combined with the unchanged observed anatomy. Experiments under the BraTS evaluation protocol show that the proposed approach produces competitive reconstructions while maintaining anatomical consistency. Source code is available at: https://github.com/TomasGuija/rarf.

摘要：醫學影像修補有潛力透過重建病理區域內的健康組織來改善自動化腦部MRI分析。我們介紹RARF，一種任務無關的區域感知校正流框架，用於生成遮罩數據。我們將該框架實例化為3D腦部MRI修補，作為我們對BraTS修補挑戰2026的提交。RARF將隨機插值過程限制在修補區域內，同時觀察到的體素保持不變並提供特定於患者的解剖學背景。一個三維神經網絡接收部分缺失的圖像，缺失區域由高斯噪聲填充，並與修補遮罩和相應的時間步驟一起處理。該模型使用遮罩流匹配和重建一致性目標進行訓練，並結合了遮罩感知的預處理和數據增強。在推理過程中，學習到的速度場將初始噪聲傳輸到缺失組織的合理重建，然後與未改變的觀察解剖結合。根據BraTS評估協議的實驗表明，所提出的方法在保持解剖一致性的同時產生了具有競爭力的重建。源代碼可在以下網址獲得：https://github.com/TomasGuija/rarf。

##### **STAIR (STructure Aware Information Retriever): A novel dataset and LLM based retriever for document structure augmentation**
2609.03874v1 by Vineet Kumar, Meghanadh Pulivarthi, vishwajeet kumar, Jaydeep Sen, Riyaz Ahmad Bhat, Sachindra Joshi

Retrieval Augmented Generation (RAG) is a key component for generating accurate and hallucination free answers using Large Language Models (LLMs). LLMs are improving at handling long context, but still suffer from "lost in the middle" problem. Thus, precise and accurate retrieval is important. Current retrievers chunk long context into length-based manageable chunks - in the process throwing away rich and informative semantic global structure in the corpus. We introduce a novel retrieval system STAIR that empowers an LLM to exploit global structure in a corpus such as a Table of Contents (ToC) to efficiently store and retrieve information from its model parameters. Our thorough and careful ablation studies with a finetuned Differentiable Search Index (DSI) system show that ToC helps build a low hallucination (less than 0.05%) generative Information Retrieval (IR) system and can generalize to examples where very few training samples are available. To further research in this novel direction of ToC based retrieval we release SearchTome - a diverse benchmark created from 18 books across 6 diverse domains to further research in this novel direction. STAIR achieves a high Recall@1 score of 82.6% on SearchTome as compared to DSI (76.9%), where the difference is found to be statistically significant. STAIR easily beats other strong baselines such as BM25 (59.5%), DPR (68.7%) and out-of-the-box Mistral (13.8%).

摘要：檢索增強生成（RAG）是使用大型語言模型（LLMs）生成準確且無幻覺答案的關鍵組件。LLMs 在處理長上下文方面有所改善，但仍然面臨「中途迷失」的問題。因此，精確和準確的檢索非常重要。當前的檢索器將長上下文切割成基於長度的可管理塊，這一過程卻丟失了語料庫中豐富且具信息性的全局語義結構。我們介紹了一個新穎的檢索系統 STAIR，使 LLM 能夠利用語料庫中的全局結構，例如目錄（ToC），有效地存儲和檢索其模型參數中的信息。我們與微調過的可微分搜索索引（DSI）系統進行了徹底且仔細的消融研究，顯示 ToC 有助於建立低幻覺（低於 0.05%）的生成信息檢索（IR）系統，並且可以推廣到訓練樣本非常少的例子。為了進一步研究這一基於 ToC 的檢索新方向，我們發布了 SearchTome——一個由 18 本書籍跨越 6 個多樣領域創建的多樣基準，以進一步研究這一新方向。與 DSI（76.9%）相比，STAIR 在 SearchTome 上達到了 82.6% 的高 Recall@1 分數，這一差異被認為具有統計顯著性。STAIR 輕鬆超越了其他強基準，如 BM25（59.5%）、DPR（68.7%）和即用型 Mistral（13.8%）。

##### **Analysis of Prompt Engineering for Drug Toxicity Prediction**
2609.03635v1 by Mia MacGregor, Aakash Welgamage Don, Mark Bartlett

Clinical trials in the UK can cost up to £1.3 million, with approximately 90% drug failure rate. Toxicity is a major contributing factor in drug failure. Testing is time and cost intensive. In recent years, the use of artificial intelligence has been increasingly explored to aid in the prediction of drug toxicity, with extensive use of large language models (LLMs). However, LLMs can show considerable variation when minor changes are made to prompts, which raises concerns about their sensitivity to prompt engineering. Prompt engineering is used to optimise a prompt given to an LLM to generate the desired output. This paper proposes a method to analyse prompt engineering for drug toxicity prediction. The aim of the paper is to investigate the importance of prompt phrasing for drug toxicity prediction. LLMs were prompted to identify chemical properties of significance when predicting drug toxicity. Prompts were constructed to investigate; job role, prompt structuring, and rule interpretation. LLMs were then used to generate datasets, using the identified features from initial prompting, which were then passed to machine learning algorithms. The experiments show that the natural variance which occurs in LLMs outweighs any fine-tuning of prompts. There were, however, substantial improvements in model performance when using chemoinformatic code to extract features instead of using LLM-generated values. The proposed analysis methodology is applicable to a wide range of prompt types across different areas of bioinformatics.

摘要：臨床試驗在英國的成本可達130萬英鎊，藥物失敗率約為90%。毒性是藥物失敗的主要因素。測試耗時且成本高昂。近年來，人工智慧的使用越來越受到關注，以協助預測藥物毒性，並廣泛使用大型語言模型（LLMs）。然而，當對提示進行微小更改時，LLMs可能會顯示出相當大的變異，這引發了對其對提示工程敏感性的擔憂。提示工程用於優化給LLM的提示，以生成所需的輸出。本文提出了一種分析藥物毒性預測的提示工程的方法。本文的目的是調查提示措辭對藥物毒性預測的重要性。LLMs被提示識別在預測藥物毒性時的重要化學特性。提示被構建以調查；職位角色、提示結構和規則解釋。然後使用LLMs生成數據集，使用從初始提示中識別的特徵，這些數據集隨後被傳遞給機器學習算法。實驗顯示，LLMs中自然變異的影響超過了任何提示的微調。然而，當使用化學信息學代碼提取特徵而不是使用LLM生成的值時，模型性能有了顯著改善。所提出的分析方法適用於不同生物信息學領域的各種提示類型。

##### **Feature Reconfiguration With Visual Prior for Medical Lesion Segmentation**
2609.03535v1 by Yinan Liu, Jiankang Hong, Zhen Gao, Ye Lu

Lesion segmentation in medical images plays a critical role in clinical diagnosis and treatment planning. Despite significant advances, lesion segmentation remains challenging due to two major factors: (1) complex background interference; (2) diverse lesion morphology. Existing encoder-decoder based methods mainly focus on enhancing feature extraction or redesigning decoding strategies. However, they lack early prior guidance and feature reconfiguration during the encoding stage, limiting their effectiveness in handling these challenges. To address these limitations, we propose FreNet, a feature reconfiguration framework with visual priors, which performs pixel-level reconfiguration before encoding and feature-level reconfiguration during encoding for precise medical lesion segmentation. To suppress background responses, we propose an Implicit Prior Neural Network (IPNN), which models a continuous spatial field and leverages visual prior from SAM to reconfigure input image before encoding stage. To better handle diverse lesion morphology, we design a Dual-domain Feature Reconfiguration (DFR) module to progressively reconfigure backbone features during encoding stage. Within DFR, the Frequency Decoupling Module (FDM) decouples backbone features in frequency domain to enhance foreground-background discriminability, while the Spatial Localization Module (SLM) spatially relocates and improving spatial stability after frequency decoupling. Extensive experiments on 9 medical image segmentation benchmarks across three imaging modalities demonstrate that FreNet significantly outperforms state-of-the-art (SOTA) methods. On the challenging ETIS dataset, our method achieves Dice improvements of 5.0% over SOTA method and 7.2% over SAM.

摘要：醫學影像中的病變分割在臨床診斷和治療計劃中扮演著關鍵角色。儘管取得了重大進展，病變分割仍然面臨兩個主要挑戰：(1) 複雜的背景干擾；(2) 多樣的病變形態。現有的基於編碼器-解碼器的方法主要集中在增強特徵提取或重新設計解碼策略。然而，它們在編碼階段缺乏早期的先驗指導和特徵重新配置，限制了它們在處理這些挑戰時的有效性。為了解決這些限制，我們提出了FreNet，一個具有視覺先驗的特徵重新配置框架，該框架在編碼之前進行像素級的重新配置，並在編碼過程中進行特徵級的重新配置，以實現精確的醫學病變分割。為了抑制背景響應，我們提出了一種隱式先驗神經網絡（IPNN），該網絡建模連續的空間場，並利用來自SAM的視覺先驗在編碼階段之前對輸入圖像進行重新配置。為了更好地處理多樣的病變形態，我們設計了一個雙域特徵重新配置（DFR）模塊，以在編碼階段逐步重新配置骨幹特徵。在DFR中，頻率解耦模塊（FDM）在頻率域中解耦骨幹特徵，以增強前景-背景的可分辨性，而空間定位模塊（SLM）在頻率解耦後進行空間重定位並改善空間穩定性。在三種成像模式下對9個醫學影像分割基準的廣泛實驗表明，FreNet顯著超越了最先進的（SOTA）方法。在具有挑戰性的ETIS數據集上，我們的方法相較於SOTA方法實現了5.0%的Dice改進，並比SAM提高了7.2%。

##### **NeoRed: A Knowledge-Logic-Alignment Multimodal Large Language Model for Neonatal Respiratory Disease Diagnosis**
2609.03527v1 by Yinan Liu, Hongtai Xia, Haoran Xu, Jiankang Hong, Jingkuan Song, Ye Luo

Neonatal respiratory diseases are a major cause of neonatal morbidity and mortality, posing substantial challenges in clinical practice. Despite recent advances, existing Multimodal Large Language Models (MLLMs) face two key limitations in neonatal diagnosis: (1) domain gap arising from predominantly adult training data; (2) insufficient integration of multidimensional clinical context for accurate diagnosis. To address these challenges, we collect two real-world clinical datasets (NeoCXR and NeoCXR-EV) and propose NeoRed, to the best of our knowledge, the first MLLM tailored for neonatal respiratory disease, filling the gap in neonatal diagnostic reports generation. To enhance joint diagnosis from heterogeneous clinical context and chest X-rays, we design a novel Knowledge-Logic-Alignment (KLA) framework which constrains model behavior from three perspectives: 1) Knowledge Prior Injection (KPI) incorporates neonatologist-inspired diagnostic priors into multimodal representations, guiding disease-specific attention across modalities; 2) Diagnostic Logic Constraint (DLC) aligns the semantics of generated reports with multimodal diagnostic logic; and 3) Visual Semantic Alignment (VSA) establishes semantic correspondence between visual features and imaging conclusions. Extensive experiments demonstrate that NeoRed enables accurate neonatal diagnostic reports generation, achieving ROUGE-L of 53.29% and Clinical Efficacy F1 score of 65.19% on NeoCXR, outperforming existing MLLMs. NeoRed also preserves competitive report generation performance on adult benchmarks (MIMIC-CXR and IU-Xray). Datasets will be available upon application.

摘要：新生兒呼吸疾病是新生兒發病率和死亡率的主要原因，對臨床實踐造成了重大挑戰。儘管最近取得了一些進展，現有的多模態大型語言模型（MLLMs）在新生兒診斷方面面臨兩個主要限制：（1）由於主要基於成人訓練數據而產生的領域差距；（2）對於準確診斷，多維臨床背景的整合不足。為了應對這些挑戰，我們收集了兩個真實世界的臨床數據集（NeoCXR和NeoCXR-EV），並提出了NeoRed，據我們所知，這是第一個專為新生兒呼吸疾病量身定制的MLLM，填補了新生兒診斷報告生成的空白。為了增強來自異質臨床背景和胸部X光片的聯合診斷，我們設計了一個新穎的知識-邏輯-對齊（KLA）框架，從三個角度約束模型行為：1）知識先驗注入（KPI）將新生兒科醫生啟發的診斷先驗納入多模態表示中，引導跨模態的疾病特異性注意力；2）診斷邏輯約束（DLC）使生成報告的語義與多模態診斷邏輯對齊；3）視覺語義對齊（VSA）建立視覺特徵與影像結論之間的語義對應。大量實驗表明，NeoRed能夠生成準確的新生兒診斷報告，在NeoCXR上達到53.29%的ROUGE-L和65.19%的臨床效能F1分數，超越了現有的MLLMs。NeoRed在成人基準（MIMIC-CXR和IU-Xray）上也保持了競爭性的報告生成性能。數據集將在申請後提供。

##### **LongCounsel-8: A Benchmark Suite for Longitudinal Depression Tracking from Multi-Session Counseling Dialogues**
2609.03507v1 by Jiayi Li, Zhaomin Wu, Bingsheng He

Tracking depression from multi-session counseling dialogues requires estimating both current symptom severity and how it changes across sessions. Yet progress on this task is constrained by the scarcity of longitudinal counseling data with standardized session-level depression labels. Existing resources typically provide either multi-session conversations without depression labels or labeled interviews in a single session. Building such a benchmark poses three challenges: maintaining longitudinal consistency and diversity, grounding symptom progression in empirical patterns, and expressing controlled depression states naturally without exposing target labels. To address these challenges, we introduce LongCounsel-8, a benchmark suite of three independently generated datasets totaling 7,749 five-session counseling trajectories, grounded in real-world client profiles, depression trajectories, symptom compositions, and counseling patterns. We combine profile-grounded simulation, empirically informed state construction, and indirect behavioral realization to address these challenges. Across the benchmark, simulated self-reports closely recover the controlled states, supporting label fidelity. Experiments on existing depression tracking methods reveal three key findings: (1) lower single-session score error does not guarantee accurate identification of trend, i.e., improvement or worsening; (2) existing methods are consistently less reliable on worsening trajectories; and (3) additional session history may reduce the accuracy of trend prediction. Together, these findings establish LongCounsel-8 as a foundation for advancing depression assessment from static, single-session prediction toward reliable longitudinal tracking of mental-health change.

摘要：追踪多次諮詢對話中的憂鬱症需要評估當前的症狀嚴重程度以及它在各個會話中的變化。<lb/>然而，這項任務的進展受到缺乏具有標準化會話級憂鬱標籤的縱向諮詢數據的限制。<lb/>現有資源通常提供的是沒有憂鬱標籤的多次會話對話，或是在單一會話中的標籤訪談。<lb/>建立這樣的基準面臨三個挑戰：保持縱向一致性和多樣性、將症狀進展基於經驗模式，並自然地表達受控的憂鬱狀態而不暴露目標標籤。<lb/>為了解決這些挑戰，我們引入了LongCounsel-8，這是一個基準套件，包含三個獨立生成的數據集，總計7,749條五次會議的諮詢軌跡，基於真實的客戶檔案、憂鬱軌跡、症狀組合和諮詢模式。<lb/>我們結合了基於檔案的模擬、經驗知識驅動的狀態構建和間接行為實現來應對這些挑戰。<lb/>在這個基準中，模擬的自我報告能夠準確恢復受控狀態，支持標籤的真實性。<lb/>對現有憂鬱追蹤方法的實驗揭示了三個關鍵發現：(1) 單次會議的得分誤差較低並不保證能準確識別趨勢，即改善或惡化；(2) 現有方法在惡化的軌跡上始終不太可靠；以及(3) 額外的會議歷史可能會降低趨勢預測的準確性。<lb/>綜合這些發現，LongCounsel-8建立了一個基礎，促進從靜態的單次會議預測向可靠的縱向心理健康變化追蹤的進展。

##### **Untangling the Mechanisms of Misleading Context in Medical Question Answering**
2609.02754v1 by Robin Linzmayer, Noémie Elhadad

Large language models now answer medical questions with expert-level performance. However, the context these systems act on can be misleading, and misleading context can corrupt a model's medical judgment. To understand how misleading context corrupts this judgment, we examine the model's susceptibility to the context, disclosure of it, mechanism of corrupted reasoning, and monitorability of the decision. On the medical reasoning subset of MedMisBench, a clinician-reviewed question-answering benchmark of 8,627 questions, we inject two types of misleading context cues, fabricated evidence and a bare assertion. We test three reasoning models, two that expose their full reasoning trace and one frontier model that exposes only its response. All three are more susceptible to the assertion than to the fabricated evidence, adopting the asserted answer 10 to 27 points more often. The misleading cues are disclosed in 81 to 98% of traces but only 7 to 90% of responses, and the assertion is disclosed less often than evidence based cues. Resampling from reasoning traces without disclosure shows the two cues corrupt reasoning differently, evidence entering early and accumulating while the assertion redirects the conclusion near its end. An LLM monitor catches 78% of corrupted decisions at 5% false positives when reading an open model's trace with guidance, against at most 32% from any response. The misleading context that models are most susceptible to is disclosed least, and was caught reliably only from an open reasoning trace, which frontier providers withhold.

摘要：大型語言模型現在能以專家級的表現回答醫療問題。<lb/>然而，這些系統所依賴的背景可能會產生誤導，而誤導性的背景可能會損害模型的醫療判斷。<lb/>為了理解誤導性背景如何損害這種判斷，我們檢視模型對背景的敏感性、其披露情況、損壞推理的機制以及決策的可監控性。<lb/>在 MedMisBench 的醫療推理子集上，這是一個由臨床醫生審核的問題回答基準，共有 8,627 個問題，我們注入了兩種誤導性背景提示，虛構證據和單純主張。<lb/>我們測試了三種推理模型，其中兩種展示其完整的推理過程，另一種前沿模型則僅展示其回應。<lb/>這三種模型對於主張的敏感性高於虛構證據，接受主張的答案的頻率高出 10 到 27 個百分點。<lb/>誤導性提示在 81% 到 98% 的推理過程中被披露，但在 7% 到 90% 的回應中僅披露少數，且主張的披露頻率低於基於證據的提示。<lb/>從未披露的推理過程中重新抽樣顯示，這兩種提示對推理的損害方式不同，證據早期進入並累積，而主張則在結論接近結尾時重新導向。<lb/>一個 LLM 監控器在閱讀開放模型的推理過程時，能以 5% 的假陽性率捕捉到 78% 的損壞決策，而從任何回應中最多僅能捕捉到 32%。<lb/>模型最容易受到的誤導性背景披露最少，且僅從開放推理過程中可靠捕捉到，而前沿提供者則不予披露。

##### **HiPoly: a hierarchical polymer-native AI framework for property prediction and generative design**
2609.02746v1 by Ge Sun, Gervasio Zaldivar, Yuan Tian, Gustavo Perez Lemus, Juhae Park, Dasha Safarian, Ming Han, Juan J. de Pablo

Polymeric materials are central to modern technologies, with applications ranging from energy to health and transportation. Although AI has made significant advances in materials discovery, the hierarchical structure of polymers across multiple length scales makes them inherently difficult to represent in a unified and physically meaningful way. Here we introduce HiPoly, a polymer-native AI framework that processes complete polymer descriptions through a three-level hierarchical graph architecture built on the G2RINS representation. HiPoly encodes stochastic inter-monomer connectivity, composition, and molecular weight directly within its architecture, using physically motivated design principles that mirror the multi-scale nature of polymeric systems. The framework establishes an end-to-end AI-driven workflow from experimental formulation data to property prediction, generative molecular design, and physics-based validation through molecular simulations, all unified by a single polymer representation. We demonstrate state-of-the-art prediction accuracy for thermophysical properties of multi-component polymer systems, with ablation studies confirming that each hierarchical design choice contributes independently to model performance. As an example, the generative design pathway is applied here to the discovery of sustainable alternatives to persistent fluorinated polymers, where it is possible to identify and independently validate PFAS-free candidates with target surface-energy properties. This work demonstrates how polymer-native AI can accelerate discovery by linking representation, prediction, and design across complex polymer chemistries.

摘要：聚合材料是現代技術的核心，應用範圍從能源到健康和交通。儘管人工智慧在材料發現方面取得了重大進展，但聚合物在多個長度尺度上的層次結構使其本質上難以以統一且物理上有意義的方式進行表示。在此，我們介紹 HiPoly，一個聚合物原生的人工智慧框架，通過基於 G2RINS 表示的三層層次圖架構處理完整的聚合物描述。HiPoly 在其架構中直接編碼隨機的單體間連接性、組成和分子量，使用反映聚合物系統多尺度特性的物理驅動設計原則。該框架建立了一個端到端的人工智慧驅動工作流程，從實驗配方數據到性質預測、生成分子設計及通過分子模擬的基於物理的驗證，所有這些都由單一的聚合物表示統一。我們展示了對多組分聚合物系統的熱物理性質的最先進預測準確性，消融研究確認每個層次設計選擇獨立地對模型性能作出貢獻。作為一個例子，這裡應用生成設計途徑來發現持久氟化聚合物的可持續替代品，在這裡可以識別並獨立驗證具有目標表面能特性的無 PFAS 候選者。這項工作展示了聚合物原生人工智慧如何通過連結表示、預測和設計來加速發現，跨越複雜的聚合物化學。

##### **From Tokens to Semantics: Leveraging Complementary Signals for Hallucination Detection in Black-Box LLMs**
2609.02679v1 by Urja Pawar, Rajitha Ramanayake, Owen O'Neill, Nabeel Kemal, Abhishek Mandal, Houssem Chatbri, Christopher Martin

When LLMs support public-facing or high-stakes workflows, missed fabrications can harm users and institutions, while false alarms consume limited human-review capacity. When no trusted context or reference document is available, we study two signals accessible through black-box model APIs: semantic entropy, which measures disagreement among sampled response meanings, and uncertainty derived from token log-probabilities. Their failure modes can be complementary: semantic entropy becomes uninformative when responses form one semantic cluster, while token uncertainty can miss consistently confident errors. We extend token-based uncertainty detection by aggregating token-level signals across sampled responses through our TopK method, evaluate the hybrid CoCoA method, which combines target-response uncertainty with semantic dissimilarity, and propose and study two supervised methods: Gated, which routes single-cluster cases to an aggregated-token-feature classifier, and Stacked, which learns jointly from semantic uncertainty and broader token features. We evaluate seven benchmarks, including five public benchmarks (four text datasets and multimodal handwritten-cheque extraction) and two constructed benchmarks (Financial Summaries and Long-Text QA), using four language models. In our evaluation across models and datasets, Stacked gave the best performance in nearly half of the cases, while TopK and CoCoA remain competitive without supervised training labels, although their thresholds require careful calibration. No method is universally strongest. We therefore evaluate performance at false-positive-rate budgets from 1% to 15%, assess their sensitivity to generation and calibration choices, and examine variation across dataset characteristics.

摘要：當大型語言模型（LLMs）支持面向公眾或高風險的工作流程時，錯過的虛構內容可能會對用戶和機構造成傷害，而虛假警報則會消耗有限的人類審查能力。當沒有可信的背景或參考文件可用時，我們研究了通過黑箱模型API可獲得的兩種信號：語義熵，它測量抽樣回應意義之間的不一致性，以及源自標記對數概率的不確定性。它們的失效模式可能是互補的：當回應形成一個語義集群時，語義熵變得沒有信息，而標記不確定性則可能錯過持續自信的錯誤。我們通過我們的TopK方法擴展了基於標記的不確定性檢測，通過聚合抽樣回應中的標記級信號，評估混合的CoCoA方法，該方法將目標回應不確定性與語義差異性結合，並提出並研究兩種監督方法：Gated，將單一集群案例路由到聚合標記特徵分類器，以及Stacked，從語義不確定性和更廣泛的標記特徵中共同學習。我們使用四種語言模型評估七個基準，包括五個公共基準（四個文本數據集和多模態手寫支票提取）和兩個構建的基準（財務摘要和長文本問答）。在我們對模型和數據集的評估中，Stacked在近一半的情況下表現最佳，而TopK和CoCoA在沒有監督訓練標籤的情況下仍然具有競爭力，儘管它們的閾值需要仔細校準。沒有任何方法是普遍最強的。因此，我們在1%到15%的假陽性率預算下評估性能，評估它們對生成和校準選擇的敏感性，並檢查數據集特徵之間的變化。

##### **ORB-SVM : An Innovative Hybrid Framework for Efficient Brain Tumor Detection from MRI Scans**
2609.02333v1 by Amirhosein Azarpour

Brain cancer remains one of the most significant challenges in modern medicine, where the accuracy of early stage diagnosis is a decisive factor in patient survival and treatment efficacy. Although Magnetic Resonance Imaging (MRI) is the established gold standard for visualizing neurological structures, the interpretation of these high dimensional scans is often complicated by subjective variability among practitioners and the inherent noise present in complex medical images. While contemporary approaches frequently rely on high parameter deep learning architectures, such models often involve significant computational costs and require extensive data for effective training. This study introduces a hybrid framework that utilizes the Oriented FAST and Rotated BRIEF (ORB) algorithm for precise feature extraction and a Support Vector Machine (SVM) for classification [1], [2]. The proposed approach achieves a sub- stantial data reduction of approximately 99.5%, which effectively minimizes the influence of non informative background data while preserving critical diagnostic patterns essential for tumor identification. By balancing feature sparsity with a robust kernel based classifier, this methodology addresses the limitations of over parameterized systems while maintaining high diagnostic integrity. Experimental evaluations conducted on the Br35H dataset demonstrate that the framework attains a classification accuracy of 97.5%. The findings suggest that the integration of localized feature representation and optimized classification provides a reliable and resource efficient alternative for medical image analysis, offering a structured solution that maintains per- formance without the need for extensive computational overhead.

摘要：腦癌仍然是現代醫學中最重要的挑戰之一，早期診斷的準確性是患者生存和治療效果的決定性因素。雖然磁共振成像（MRI）是可視化神經結構的既定金標準，但這些高維掃描的解釋常常受到從業者之間主觀變異性和複雜醫療影像中固有噪聲的影響。當前的方法經常依賴於高參數的深度學習架構，但這些模型通常涉及顯著的計算成本，並需要大量數據來進行有效訓練。本研究介紹了一個混合框架，利用定向FAST和旋轉BRIEF（ORB）算法進行精確特徵提取，並使用支持向量機（SVM）進行分類[1]，[2]。所提出的方法實現了約99.5%的顯著數據減少，這有效地最小化了非信息性背景數據的影響，同時保留了對腫瘤識別至關重要的診斷模式。通過平衡特徵稀疏性與穩健的基於核的分類器，這一方法解決了過度參數化系統的局限性，同時保持高診斷完整性。在Br35H數據集上進行的實驗評估顯示，該框架達到了97.5%的分類準確率。研究結果表明，局部特徵表示和優化分類的整合為醫療影像分析提供了一種可靠且資源高效的替代方案，提供了一種結構化解決方案，保持性能而無需大量計算開銷。

##### **SAUF-Net: Structure--Appearance Representation Learning with Uncertainty Feedback for Semi-Supervised Medical Image Segmentation**
2609.02247v1 by Qin Lu, Zheyang Jing, Yujie Yang, Jianwang Li, Chen Yi, Shaofeng Jiang

Semi-supervised learning has shown great potential for reducing annotation costs in medical image segmentation. However, most existing methods mainly exploit unlabeled data through prediction-level consistency, while the reliability of internal feature representations is often overlooked. In medical images, target-related structural cues are easily entangled with unstable appearance variations, which may lead to unreliable pseudo labels and error accumulation during training. To address these issues, we propose SAUF-Net, a Structure--Appearance Representation Learning with Uncertainty Feedback Network for semi-supervised medical image segmentation. SAUF-Net uses the Structure--Appearance Decomposition Module (SADM) to separate bottleneck features into structural and appearance representations. The Disentangled Guidance Module (DGM) injects these representations into the decoding process to enhance structure-aware segmentation. Meanwhile, the Auxiliary Decoder produces branch-specific predictions for reliability estimation and a fused prediction for appearance-swapped consistency. Furthermore, we introduce an Appearance-Swapped Consistency branch to encourage structural representations to remain stable under appearance variations. We also introduce a reliability-map-guided dual-head discriminator with a Validity Head and an Uncertainty Head to provide feature-level uncertainty feedback. Extensive experiments on ISIC-2016 and Kvasir-SEG demonstrate that SAUF-Net outperforms state-of-the-art semi-supervised methods, especially under low-label settings.

摘要：半監督學習在減少醫學影像分割的標註成本方面顯示出巨大的潛力。<lb/>然而，大多數現有方法主要通過預測級一致性來利用未標註數據，而內部特徵表示的可靠性往往被忽視。<lb/>在醫學影像中，與目標相關的結構線索容易與不穩定的外觀變化交織在一起，這可能導致不可靠的偽標籤和訓練過程中的錯誤累積。<lb/>為了解決這些問題，我們提出了SAUF-Net，一種帶有不確定性反饋的結構-外觀表示學習網絡，用於半監督醫學影像分割。<lb/>SAUF-Net使用結構-外觀分解模塊（SADM）將瓶頸特徵分離為結構和外觀表示。<lb/>解耦指導模塊（DGM）將這些表示注入解碼過程中，以增強結構感知的分割。<lb/>同時，輔助解碼器生成特定於分支的預測，以進行可靠性估計，並生成融合預測以實現外觀交換一致性。<lb/>此外，我們引入了一個外觀交換一致性分支，以鼓勵結構表示在外觀變化下保持穩定。<lb/>我們還引入了一個基於可靠性圖的雙頭鑑別器，具有有效性頭和不確定性頭，以提供特徵級的不確定性反饋。<lb/>在ISIC-2016和Kvasir-SEG上進行的廣泛實驗表明，SAUF-Net在低標籤設置下優於最先進的半監督方法。

##### **Examining the Vulnerability of Multi-Agent Medical Systems to Human Interventions for Clinical Reasoning**
2609.02191v1 by Benjamin C Liu, Dillon Mehta, Rishi Malhotra, Adam Zobian, Yong Ying Tan, Samir Chopra, Daniella Rand, Natalie Pang, Abhiram Gudimella, Kevin Zhu

Human interventions at fault points can alter the diagnostic accuracy of multi-agent medical systems. We defined fault points as moments in AI agent conversations, in which an agent's reasoning became most vulnerable to external influence. Using the MedQA dataset, this study analyzed simulated doctor-patient conversations to measure how interventions shifted reasoning and accuracy. Correct intervention methods showed an improvement in baseline diagnostic accuracy of up to 40%, while incorrect or bias-related interventions degraded performance by up to 6% and increased diagnostic drift and uncertainty. Beyond performance changes, our analysis revealed behavioral similarities between cognitive biases in simulated agent environments and real-world clinical practice. Examples included premature closure and susceptibility to misleading cues. Overall, these findings demonstrate that identifying and guiding fault points with human interventions may provide a mechanism for improving diagnostic robustness in multi-agent medical systems.

摘要：人類在故障點的介入可以改變多代理醫療系統的診斷準確性。<lb/>我們將故障點定義為AI代理對話中的時刻，在這些時刻，代理的推理最容易受到外部影響。<lb/>本研究使用MedQA數據集分析模擬的醫生-病人對話，以測量介入如何改變推理和準確性。<lb/>正確的介入方法顯示出基線診斷準確性提高了多達40%，而不正確或與偏見相關的介入則使性能下降了多達6%，並增加了診斷漂移和不確定性。<lb/>除了性能變化，我們的分析還揭示了模擬代理環境中的認知偏見與現實世界臨床實踐之間的行為相似性。<lb/>例子包括過早關閉和對誤導性線索的易感性。<lb/>總體而言，這些發現表明，通過人類介入識別和引導故障點可能提供改善多代理醫療系統診斷穩健性的一種機制。

##### **Disease Burden over Skin Tone: Decomposing the Dermatology-AI Generalization Gap**
2609.02111v1 by Nirajan Kunwor, Sanjaya Poudel, Quoc-Huy Trinh, Jahidul Arafat, Sunil Kumar Gaire

Dermatology artificial intelligence (AI) models are predominantly trained on light-skinned, cancer-focused image collections, yet they are increasingly proposed for deployment in resource-constrained settings where patients differ from training populations along two confounded axes: skin tone and disease distribution. We investigate whether poor generalization is primarily caused by skin-tone underrepresentation or disease-distribution shift. We evaluate a cancer-trained baseline (ResNet-50 fine-tuned on HAM10000 and ISIC 2019), two dermatology foundation models (DermLIP and MONET), and a general-purpose vision model (DINOv3) as frozen feature extractors. Models are evaluated on a tone-stratified disease-matched dataset (Diverse Dermatology Images, DDI) and a disease-shifted tone-diverse dataset (Skin Condition Image Network, SCIN). Our results show that disease-distribution shift contributes more than skin tone in the evaluated settings. The cancer baseline decreases from 0.62 to 0.21 balanced accuracy when transferred to unfamiliar clinical conditions, while the within-disease skin-tone gap is smaller (0.10-0.18) and inconsistent. Label-free representation analysis shows that this failure reflects a representational limitation rather than only missing output labels: cancer-specialized features poorly cluster unfamiliar conditions (kNN purity lift +0.06 over chance), whereas dermatology-pretrained features retain stronger transferable structure (+0.23). Finally, we show that representation quality predicts recoverable performance under lightweight adaptation. Starting from dermatology foundation models, approximately ten labeled examples per clinical category recover most attainable performance. We release the evaluation protocol and code to support reproducible auditing of dermatology AI generalization.

摘要：皮膚科人工智慧（AI）模型主要是在以淺膚色和癌症為焦點的影像資料集上訓練的，然而它們越來越多地被提議用於資源有限的環境中，在這些環境中，患者與訓練人群在兩個混淆的軸上存在差異：膚色和疾病分布。我們調查了較差的泛化是否主要是由於膚色的代表性不足或疾病分布的變化所致。我們評估了一個以癌症為訓練基準的模型（在HAM10000和ISIC 2019上微調的ResNet-50）、兩個皮膚科基礎模型（DermLIP和MONET），以及一個通用視覺模型（DINOv3）作為凍結的特徵提取器。這些模型在一個按膚色分層的疾病匹配數據集（多樣化皮膚科影像，DDI）和一個疾病轉變的膚色多樣化數據集（皮膚狀況影像網絡，SCIN）上進行評估。我們的結果顯示，在評估的環境中，疾病分布的變化對模型的影響大於膚色的影響。當轉移到不熟悉的臨床條件時，癌症基準的平衡準確率從0.62下降到0.21，而同一疾病內的膚色差距較小（0.10-0.18）且不一致。無標籤表示分析顯示，這一失敗反映了一種表徵限制，而不僅僅是缺少輸出標籤：專注於癌症的特徵在不熟悉的條件下聚類效果較差（kNN純度提升+0.06），而皮膚科預訓練的特徵則保留了更強的可轉移結構（+0.23）。最後，我們顯示表示質量預測在輕量適應下可恢復的性能。從皮膚科基礎模型開始，每個臨床類別大約需要十個標記示例即可恢復大部分可達的性能。我們發布了評估協議和代碼，以支持皮膚科AI泛化的可重複審計。

##### **Federated LoRA Adaptation of BiomedCLIP Across Four International Chest X-Ray Cohorts**
2609.02101v1 by Sanjaya Poudel, Nirajan Kunwor, Manish Dhakal, Debesh Jha, Sunil Kumar Gaire

Federated learning (FL) lets institutions train a shared model without exchanging data, and Low-Rank Adaptation (LoRA) makes this practical at scale by communicating only compact low-rank updates. Biomedical imaging is a compelling setting for this combination: patient data are archived behind privacy regulations, and institutions differ widely in scanners, protocols, and compute. Such heterogeneity raises the question of how federated LoRA updates should be aggregated, increasingly pressing as multimodal vision-language models become central to medical image analysis. We benchmark federated Parameter-efficient fine-tuning (PEFT) of BiomedCLIP for chest radiograph classification across four public cohorts on three continents (USA, Vietnam, Spain). Federated LoRA adaptation improves shared-class AUC on all four cohorts over the unadapted BiomedCLIP backbone (mean 0.687 to 0.802), showing that the gains come from federated adaptation rather than from the pretrained model's zero-shot ability. Relative to isolated single-cohort training, federation improves the weaker cohorts while largely preserving the strongest and approaches a centralized reference (0.812) that pools all data. The singular value decomposition (SVD)-based product-space aggregation introduced by FlexLoRA is essential to this gain (naive factor averaging drops mean AUC by 0.097), whereas a drift-correcting optimizer (FedProx) shows no benefit over FedAvg in our single-seed runs, consistent with LoRA's low-rank updates already limiting client drift. Biomedical vision-language models can thus be adapted collaboratively across heterogeneous, geographically distributed institutions without centralizing data.

摘要：聯邦學習（FL）使機構能夠在不交換數據的情況下訓練共享模型，而低秩適應（LoRA）通過僅傳遞緊湊的低秩更新使這一過程在大規模上變得可行。生物醫學影像是一個引人注目的應用場景：患者數據受到隱私法規的保護，並且不同機構在掃描儀、協議和計算能力上差異很大。這種異質性引發了聯邦LoRA更新應該如何聚合的問題，隨著多模態視覺-語言模型在醫學影像分析中變得越來越重要，這一問題變得愈發迫切。我們對BiomedCLIP的聯邦參數高效微調（PEFT）進行基準測試，以對胸部X光片進行分類，涵蓋三大洲（美國、越南、西班牙）的四個公共隊列。聯邦LoRA適應在所有四個隊列中改善了共享類別的AUC，相較於未適應的BiomedCLIP主幹（平均從0.687提高到0.802），顯示出增益來自於聯邦適應，而非來自預訓練模型的零樣本能力。相較於孤立的單隊列訓練，聯邦學習改善了較弱的隊列，同時在很大程度上保留了最強的隊列，並接近於一個集中參考（0.812），該參考整合了所有數據。FlexLoRA引入的基於奇異值分解（SVD）的產品空間聚合對這一增益至關重要（天真的因子平均使平均AUC下降0.097），而漂移修正優化器（FedProx）在我們的單種子運行中對FedAvg沒有顯著好處，這與LoRA的低秩更新已經限制了客戶漂移的情況一致。因此，生物醫學視覺-語言模型可以在異質的、地理上分散的機構之間進行協作適應，而無需集中數據。

##### **READY or Not: Reliable Enterprise Agent Deployment**
2609.02095v1 by Veronica Chatrath, Bryan Zhu, Jingxuan Fan, George Pu, Soham Dinesh Tiwari, Soham Dan, Ryan Young,  Yuan,  Li, Yuang Yao, Apaar Shanker, Minglai Yang, Daniel Yue Zhang, Yunzhong He, Ying Liu, Chenguang Wang, Zhijun Yin, Yuan Xue

An AI agent can perform well on benchmarks and still be unsuitable for deployment. Existing AI-agent benchmarks measure whether an agent can complete realistic professional work, whereas enterprise deployment asks a different question: whether an agent can meet a required reliability level, under acceptable human oversight, and at tolerable cost. We introduce Reliable Enterprise Agent Deployment (READY), a framework for qualifying AI agents for deployment on enterprise workflows. READY preserves each workflow's own definition of successful execution while applying a common qualification procedure. Given an agent, a workflow, and a class of candidate oversight policies, READY measures the reliability and operating cost of the human-AI system, selects the minimum-cost policy that satisfies a specified reliability target, and statistically qualifies it on held-out cases. The resulting deployment profile characterizes the supported operating point: reliability, human-oversight burden, and cost. READY is implemented as an open testbed that decouples workflow specification, execution, evaluation, and qualification, and runs on existing agent-evaluation infrastructure. In an end-to-end clinical-audit case study spanning 16 agent systems and 750 cases, READY reveals differences hidden by autonomous performance: two systems separated by only 0.3 points in autonomous accuracy (72.8% vs. 72.5%) require 39.2% versus 29.6% human review, respectively, to qualify at the same 76% reliability target under the evaluated oversight policy. READY thus shifts enterprise agent evaluation from how well can the agent perform the work? to under what conditions, and at what cost, can it be reliably deployed? By making those conditions explicit and statistically testable, READY provides a basis for comparing agent systems, setting oversight requirements, and making evidence-based deployment decisions.

摘要：一個 AI 代理可以在基準測試中表現良好，但仍然不適合部署。現有的 AI 代理基準測試衡量代理是否能完成現實的專業工作，而企業部署則提出了不同的問題：代理是否能在可接受的人類監督下，滿足所需的可靠性水平，並在可容忍的成本內。我們引入了可靠企業代理部署（READY），這是一個用於為企業工作流程資格認證 AI 代理的框架。READY 保留每個工作流程對成功執行的自我定義，同時應用共同的資格認證程序。給定一個代理、一個工作流程和一類候選監督政策，READY 測量人類-AI 系統的可靠性和運營成本，選擇滿足指定可靠性目標的最低成本政策，並在保留的案例上進行統計資格認證。結果的部署配置描述了支持的運營點：可靠性、人類監督負擔和成本。READY 實現為一個開放的測試平台，將工作流程規範、執行、評估和資格認證解耦，並在現有的代理評估基礎設施上運行。在一個涵蓋 16 個代理系統和 750 個案例的端到端臨床審核案例研究中，READY 揭示了自主性能隱藏的差異：兩個系統在自主準確性上僅相差 0.3 分（72.8% 對 72.5%），但在評估的監督政策下，分別需要 39.2% 和 29.6% 的人類審查，以在同一 76% 的可靠性目標下獲得資格。因此，READY 將企業代理評估的重點從代理能多好地執行工作？轉變為在什麼條件下，並以什麼成本，可以可靠地部署？通過使這些條件明確且可統計測試，READY 為比較代理系統、設置監督要求和做出基於證據的部署決策提供了基礎。

##### **InstEditSeg: Instruction-Driven Image Editing for Polyp and Skin Lesion Segmentation**
2609.02004v1 by Ziquan Liu, Zhewei Zhu, Xuyang Shi

Accurate segmentation of polyps and skin lesions is pivotal for clinical diagnosis, yet existing methods struggle with low contrast, ambiguous boundaries, and cross-domain distribution discrepancies. Discriminative networks and most diffusion-based segmentation approaches predict standalone binary masks, leaving the visual priors of large-scale pretrained generative models largely unexploited. We propose InstEditSeg, a unified generative framework that reformulates medical segmentation as an instruction-driven image editing problem. Instead of emitting a mask, the model renders a color-coded overlay on the original image, conditioned on a textual instruction, so that the edited output aligns with the natural image distribution learned by latent diffusion models and mitigates the domain gap between natural and medical imagery. To recover fine anatomical structures, we introduce DINOv3 as an auxiliary visual encoder and a DINO Feature Guidance Block that builds a multi-scale feature pyramid. The pyramid is fused into the diffusion U-Net by channel concatenation and zero-initialized convolution so that hierarchical discriminative priors can be injected without perturbing the pretrained weights. A dual-branch classifier-free guidance strategy requiring only two forward passes per denoising step reduces inference cost. On polyp and skin lesion benchmarks the framework achieves accuracy competitive with strong discriminative baselines, and it further demonstrates concrete advantages of the generative formulation: notably better cross-domain generalization on unseen data, more complete multi-lesion segmentation, instruction-conditioned task control, and sampling flexibility. We also analyze the strengths and limitations of the paradigm, including its color sensitivity and unsupported attribute-conditioned selection. Code is available at: https://github.com/wincharm001/InstEditSeg.

摘要：準確的息肉和皮膚病變分割對臨床診斷至關重要，但現有方法在低對比度、模糊邊界和跨領域分佈差異方面面臨挑戰。區別性網絡和大多數基於擴散的分割方法預測獨立的二元掩碼，這使得大型預訓練生成模型的視覺先驗未能得到充分利用。我們提出了InstEditSeg，一個統一的生成框架，將醫學分割重新定義為一個基於指令的圖像編輯問題。模型不是輸出掩碼，而是在原始圖像上渲染一個顏色編碼的覆蓋層，這是基於文本指令進行的，以便編輯後的輸出與潛在擴散模型學習的自然圖像分佈對齊，並減少自然圖像和醫學圖像之間的領域差距。為了恢復精細的解剖結構，我們引入DINOv3作為輔助視覺編碼器，以及一個DINO特徵引導塊，該塊構建了一個多尺度特徵金字塔。這個金字塔通過通道串接和零初始化卷積融合進擴散U-Net，以便在不擾動預訓練權重的情況下注入分層的區別性先驗。一種雙分支無分類器引導策略僅需每個去噪步驟進行兩次前向傳遞，從而降低推理成本。在息肉和皮膚病變基準測試中，該框架的準確性與強大的區別性基線相當，並進一步展示了生成公式的具體優勢：在未見數據上顯著更好的跨領域泛化、更完整的多病變分割、基於指令的任務控制和取樣靈活性。我們還分析了該範式的優勢和局限性，包括其顏色敏感性和不支持的屬性條件選擇。代碼可在以下網址獲得：https://github.com/wincharm001/InstEditSeg。

##### **The Ceiling Is in the Channel: Auditing Learner Gaps and Measurement Frontiers in Clinical Prediction**
2609.01909v1 by Sayeed Shafayet Chowdhury, Nusrat Jahan, Snehasis Mukhopadhyay, Shiaofen Fang, Vijay R. Ramakrishnan

Clinical prediction can saturate for two different reasons: a fitted learner may fail to extract available information, or the recorded variables may impose a population frontier. We separate these quantities through the \emph{learner gap} and the \emph{measurement-channel ceiling}. Optimal balanced accuracy is characterized by total-variation separation, yielding architecture invariance, a sharp partial-identification result under replacement contamination, a cross-fitted ceiling estimator, and exact conditions for multimodal decision improvement. We add two finite-sample diagnostics, namely a label-permutation optimism floor and an underfit curve, and validate the audit on three real cohorts: UCI readmission ($n=99{,}343$), BRFSS diabetes ($n=253{,}680$), and NHANES HbA1c ($n=10{,}219$). Well-tuned gradient boosting nearly reaches the estimated frontier in UCI and BRFSS, whereas deliberately or practically deficient learners retain large gaps. NHANES yields a null difference between questionnaire and measured marginal frontiers but a significant joint complementarity gain, refining the simplistic claim that an objective modality must dominate. Across all cohorts, modest AUROC gains coexist with substantially larger Bayes decision-flip rates, and several architectures estimate similar frontiers while their achieved balanced accuracy differs sharply. A PRISMA-guided synthesis of 104 clinical tasks then shows that the same channel-level regularities recur across more than 18 disease categories: a broad but non-universal structured-clinical region, diminishing same-channel gains across model families, and higher performance when measurement channels change. The framework converts saturation from an empirical observation into an auditable decision: improve the learner when headroom remains; improve measurement when it does not.

摘要：臨床預測可能因兩個不同的原因而飽和：擬合的學習者可能無法提取可用的信息，或者記錄的變量可能施加了人口邊界。我們通過\emph{學習者差距}和\emph{測量通道上限}來區分這些量。最佳平衡準確性以總變異分離為特徵，產生架構不變性、在替代污染下的尖銳部分識別結果、一個交叉擬合的上限估計量，以及多模態決策改進的精確條件。我們添加了兩個有限樣本診斷，即標籤置換樂觀基線和欠擬合曲線，並在三個真實隊列上驗證審計：UCI 再入院（$n=99{,}343$）、BRFSS 糖尿病（$n=253{,}680$）和 NHANES HbA1c（$n=10{,}219$）。調整良好的梯度提升幾乎達到 UCI 和 BRFSS 中的估計邊界，而故意或實際不足的學習者則保留了較大的差距。NHANES 在問卷和測量邊際邊界之間產生了零差異，但卻有顯著的聯合互補增益，精煉了簡化的主張，即客觀模態必須佔主導地位。在所有隊列中，適度的 AUROC 增益與大幅更高的貝葉斯決策翻轉率共存，並且幾個架構估計類似的邊界，而它們實現的平衡準確性卻有明顯差異。然後，根據 PRISMA 指導的 104 個臨床任務的綜合顯示，相同的通道級規律在超過 18 種疾病類別中重複出現：一個廣泛但非普遍的結構化臨床區域、跨模型家族的同通道增益減少，以及當測量通道改變時的更高性能。該框架將飽和從一種實證觀察轉變為可審計的決策：當有空間時改善學習者；當沒有空間時改善測量。

##### **Import What You Need: Learning When and How to Augment EHR Graphs with External Knowledge**
2609.01839v1 by Chen Chen, Mohsen Nayebi Kerdabadi, Dongjie Wang, Mei Liu, Zijun Yao

Longitudinal prediction from electronic health records (EHRs) is limited by the sparsity and irregularity in patient trajectories, and knowledge augmentation with external knowledge graphs (KGs) offers a promising way to alleviate these issues. However, most existing methods perform fixed, context-agnostic topology augmentation by adding the same KG nodes and edges regardless of a patient's evolving state. We propose ReTA, a Reinforcement learning-based dynamic Topology Augmentation framework that casts KG import as a per-visit, budget-aware policy. ReTA first constructs an offline refined pool of KG-grounded templates, then learns a policy to select one augment action per visit from three options: Soft Import, which enriches node features without modifying graph topology, Hard Import, which grafts a compact KG subgraph onto the visit graph to create message-passing shortcuts, and Skip, which leaves the visit unaugmented when the base encoder is already confident. To stabilize learning, ReTA employs a decoupled encoder that processes semantic and structural signals in separate channels and fuses them via adaptive gating. Experiments on MIMIC-III and MIMIC-IV across diagnosis prediction, mortality, and readmission show that ReTA consistently outperforms strong baselines while remaining efficient, transfers across datasets and knowledge graphs, and yields interpretable augmentation patterns. The robust gains under sparse supervision highlight the advantage of ReTA's dynamic decision to import knowledge, boosting accuracy while curbing costs.

摘要：從電子健康記錄（EHRs）進行的縱向預測受到患者軌跡的稀疏性和不規則性的限制，而通過外部知識圖（KGs）進行知識增強提供了一種有前景的方法來緩解這些問題。<lb/>然而，大多數現有方法通過添加相同的KG節點和邊緣來執行固定的、與上下文無關的拓撲增強，而不考慮患者的演變狀態。<lb/>我們提出了ReTA，一種基於強化學習的動態拓撲增強框架，將KG導入視為每次訪問的、考慮預算的策略。<lb/>ReTA首先構建一個離線精煉的KG基礎模板池，然後學習一個策略，從三個選項中為每次訪問選擇一個增強行動：軟導入（Soft Import），在不修改圖拓撲的情況下豐富節點特徵；硬導入（Hard Import），將一個緊湊的KG子圖嫁接到訪問圖上以創建消息傳遞快捷方式；以及跳過（Skip），當基礎編碼器已經有信心時，將訪問保持不增強。<lb/>為了穩定學習，ReTA採用了一個解耦編碼器，分別處理語義和結構信號，並通過自適應閘融合它們。<lb/>在MIMIC-III和MIMIC-IV上進行的診斷預測、死亡率和再入院的實驗顯示，ReTA始終優於強基線，同時保持高效，能夠在數據集和知識圖之間轉移，並產生可解釋的增強模式。<lb/>在稀疏監督下的穩健增益突顯了ReTA動態決策導入知識的優勢，提升了準確性，同時降低了成本。

##### **Interpretable Symptom Vectors for Depression in a Large Language Model**
2609.01832v1 by Fangyi Zhu, Ajay Subramanian, Allison Constant, Camille Wang, Ravish Gupta, Corey J. Keller

Patients with depression present with diverse symptom profiles, yet clinical practice routinely reduces this variation to a single severity score. Large language models (LLMs) can potentially capture various symptoms and their severity from patient speech. However, how depressive symptoms are represented inside LLMs remains poorly understood, limiting clinical trust. To examine whether internal model activations match clinician judgment, we analyzed the residual stream of Gemma-3-27B-PT using mechanistic interpretability techniques. Recording activations across symptom descriptions drawn from validated clinical instruments, we found that symptom groups geometrically separated the most at layer 21 across multiple distance metrics. Using Semantic Projection, we then projected held-out naturalistic text onto Symptom Vectors constructed from these instruments. The resulting per-symptom coefficients preserved clinician-annotated rank ordering across mood, somatic, and suicidality axes. Furthermore, a single depression vector in Layer 21 separates held-out depressive from non-depressive text (AUC = 0.789), which can be used as an emotional valence gate that restricts symptom projection to depressive speech. These results reveal a decorrelated, clinician-aligned symptom signal readable directly from internal activations, offering a mechanistic foundation for interpretable depression-assessment tools.

摘要：患者的抑鬱症狀呈現多樣的症狀特徵，但臨床實踐通常將這種變異簡化為單一的嚴重程度評分。大型語言模型（LLMs）有潛力從患者的言語中捕捉各種症狀及其嚴重程度。然而，抑鬱症狀在LLMs中的表現方式仍然不甚了解，這限制了臨床信任。為了檢驗內部模型激活是否與臨床醫生的判斷相符，我們使用機械解釋技術分析了Gemma-3-27B-PT的殘差流。通過記錄來自經過驗證的臨床工具的症狀描述中的激活，我們發現症狀組在多個距離度量中在第21層的幾何分離度最高。接著，我們使用語義投影將保留的自然文本投影到這些工具構建的症狀向量上。結果顯示，每個症狀的係數在情緒、身體和自殺傾向軸上保留了臨床醫生標註的排名順序。此外，第21層中的單一抑鬱向量將保留的抑鬱文本與非抑鬱文本分開（AUC = 0.789），這可以作為一個情感價值閘，限制症狀投影到抑鬱言語。這些結果揭示了一個去相關的、與臨床醫生對齊的症狀信號，能夠直接從內部激活中讀取，為可解釋的抑鬱評估工具提供了機械基礎。

##### **GazeRefine: Expert Gaze as a Test-Time Prompt for Training-Free Medical Image Segmentation**
2609.01310v1 by Mohammed Oussama Benyahia, Marouane Tliba, Mohamed Amine Kerkouri, Taifour Yousra, Bin Wang, Max Bengtsson, Gorkem Durak, Elif Keles, Zuheng Ming, Marek Penhaker, Azeddine Beghdadi, Ulas Bagci, Aladine Chetouani

Medical image segmentation remains difficult to scale because high-performing methods typically rely on dense expert annotations and task-specific training. We introduce GazeRefine, a training-free framework that uses gaze as an inference-time prompt for zero-shot medical image segmentation. Sparse, duration-weighted fixations are converted into foreground and background priors that initialize semantic prototypes in frozen DINOv3 feature space. These prototypes are iteratively refined through foreground-background discrimination, feature-space affinity propagation, and anchoring to the initial gaze guidance, allowing segmentation to extend beyond directly fixated regions while limiting semantic drift. GazeRefine requires no segmentation masks, fine-tuning, adapters, prompt encoders, or gradient updates. We evaluate the method on gaze-annotated polyp segmentation and prostate MRI segmentation. The results show strong performance on colonoscopy images and competitive performance on prostate MRI, supporting gaze-guided prototype refinement as a promising approach for segmentation-label-efficient, human-in-the-loop medical image segmentation. Our tools and code can be found in the following repository: https://github.com/MohammedOussamaBEN/GazeRefine.git

摘要：醫學影像分割仍然難以擴展，因為高效能的方法通常依賴於密集的專家標註和特定任務的訓練。<lb/>我們介紹了 GazeRefine，一個無需訓練的框架，利用注視作為推斷時的提示，用於零-shot 醫學影像分割。<lb/>稀疏的、基於持續時間加權的注視被轉換為前景和背景的先驗，這些先驗在凍結的 DINOv3 特徵空間中初始化語義原型。<lb/>這些原型通過前景-背景區分、特徵空間親和力傳播和錨定於初始注視指導進行迭代精煉，允許分割超越直接注視的區域，同時限制語義漂移。<lb/>GazeRefine 不需要分割掩碼、微調、適配器、提示編碼器或梯度更新。<lb/>我們在注視標註的息肉分割和前列腺 MRI 分割上評估了該方法。<lb/>結果顯示在結腸鏡影像上表現強勁，在前列腺 MRI 上表現具有競爭力，支持基於注視的原型精煉作為一種有前景的方法，用於分割標籤高效的人機協作醫學影像分割。<lb/>我們的工具和代碼可以在以下庫中找到：https://github.com/MohammedOussamaBEN/GazeRefine.git

##### **Towards AI-Assisted Clinical Trial Matching: Practical Considerations, Multicenter Evaluation, and Real-World Deployment**
2609.01202v1 by Yin Fang, Qiao Jin, Shubo Tian, Lauren He, Maya Geer, Noor Naffakh, Ryan Huu-Tuan Nguyen, Zifeng Wang, Jimeng Sun, Charalampos S. Floudas, James L. Gulley, Kamilia Moalem, Catarina Martins Maia, Amanda Nottke, Juan W. Valle, Melinda Bachini, Lourdes Rocha-Nussbaum, Kari Ramage, Nikita Curry, Megan Barnes, Mandy Mansaray, Darlene Gabeau, Craig E. Grossman, Heath Skinner, Michael Burczynski, NIH-TrialBench Consortium, Zhiyong Lu

Clinical trials are essential for advancing cancer care and drug development, but many fail because of insufficient patient enrollment. While there is growing interest in using AI to support patient recruitment, existing systems largely perform eligibility assessment alone and have rarely been evaluated in real-world oncology workflows. Here we present TrialGPT 2.0, an AI-assisted clinical trial recommendation system designed for real-world deployment. Rather than asking only whether a patient may qualify, the system also assesses which trials warrant further consideration given the patient's current clinical needs and local workflow priorities, and provides structured, inspectable explanations for expert review. Importantly, we evaluated TrialGPT 2.0 retrospectively and prospectively across multiple oncology-focused settings, spanning government, academic cancer-center, patient-advocacy, and NIH referral workflows. In retrospective multicenter cohorts comprising 288 cases, TrialGPT 2.0 retrieved at least one clinician-recommended trial in its top 10 recommendations for approximately 91% of cases while reducing clinician screening time by 55.0%. In a six-month prospective evaluation embedded in an active precision oncology tumor board, TrialGPT 2.0 contributed additional trial opportunities missed by the routine workflow, expanding patient access to clinical trial participation by 90.9%. To support scientific reproducibility, we also introduce NIH-TrialBench, a clinician-authored dataset comprising 126 diverse synthetic patient vignettes and matching scenarios from 11 NIH Institutes and Centers. Together, these results support the value of AI to assist clinical trial matching by improving clinician efficiency and identifying frequently overlooked trial opportunities, ultimately helping to expand and accelerate accrual to cancer trials.

摘要：臨床試驗對於推進癌症護理和藥物開發至關重要，但許多試驗因患者招募不足而失敗。儘管對使用人工智慧支持患者招募的興趣日益增長，但現有系統主要僅進行資格評估，並且在現實世界的腫瘤學工作流程中很少被評估。在此，我們介紹TrialGPT 2.0，一個設計用於現實世界部署的人工智慧輔助臨床試驗推薦系統。該系統不僅詢問患者是否可能符合資格，還評估哪些試驗值得進一步考慮，考慮到患者當前的臨床需求和當地工作流程優先事項，並提供結構化、可檢查的解釋以供專家審查。重要的是，我們在多個專注於腫瘤學的環境中對TrialGPT 2.0進行了回顧性和前瞻性的評估，涵蓋政府、學術癌症中心、患者倡導和NIH轉診工作流程。在包括288例的回顧性多中心隊列中，TrialGPT 2.0在其前10個推薦中為約91%的案例檢索到了至少一個臨床醫生推薦的試驗，同時減少了臨床醫生的篩查時間55.0%。在一個嵌入在活躍的精準腫瘤學腫瘤委員會中的六個月前瞻性評估中，TrialGPT 2.0提供了常規工作流程中錯過的額外試驗機會，使患者參加臨床試驗的機會增加了90.9%。為了支持科學的可重複性，我們還介紹了NIH-TrialBench，這是一個由臨床醫生撰寫的數據集，包含126個多樣化的合成患者小品和來自11個NIH研究所和中心的匹配場景。這些結果共同支持了人工智慧在提升臨床試驗匹配效率和識別經常被忽視的試驗機會方面的價值，最終有助於擴大和加速癌症試驗的招募。

##### **CARE: Contrastive Anchor-based Rubric Evolution for Large Language Model Post-Training**
2609.00892v1 by Siyuan Li, Xinxin Song, Chen Ruinian, Jingjing Fan, Tingxiong Xiao, Yangen Hu, Ke Zeng, Jinli Suo

Rubric-based reinforcement learning decomposes open-ended instructions into prompt-specific, flexible rubrics, making it better suited than reinforcement learning with verifiable rewards for post-training LLMs on open-ended tasks. However, static rubrics are inevitably hacked as the policy evolves, and existing dynamic approaches introduce new problems: undirected rubric extraction, unreliable hack detection, and unbounded rubric proliferation. We propose $\textbf{CARE}$ ($\textbf{C}$ontrastive $\textbf{A}$nchor-based $\textbf{R}$ubric $\textbf{E}$volution), which grounds every rubric evolution step in a high-quality anchor response generated by a frontier model conditioned on the prompt and its rubrics. At each training step, CARE contrasts the highest-scoring rollout against the anchor, enabling two complementary mechanisms: an Adaptive branch that reactively repairs reward misspecification; and a Chase branch that proactively converts frontier-level quality gaps into sharper rubrics. Together, the two branches $\textbf{maintain discriminative accuracy in the high-reward region}$---the precise region where reward over-optimization mostly originates. Experiments on WildChecklist-9K with Qwen2.5-7B-Base and Qwen2.5-7B-Instruct show that CARE achieves state-of-the-art performance on Arena-Hard-2.0, InfoBench, and FollowBench, and is the $\textbf{only}$ method whose win rate against GPT-4.1 anchor responses shows sustained improvement throughout 300 training steps; additional results on Llama-3.1-8B-Instruct and Qwen3-8B further indicate that CARE generalizes across model families.

摘要：基於評分標準的強化學習將開放式指令分解為特定於提示的靈活評分標準，使其比具有可驗證獎勵的強化學習更適合於開放式任務的後訓練 LLM。然而，隨著策略的演變，靜態評分標準不可避免地被破解，而現有的動態方法則引入了新的問題：無導向的評分標準提取、不可靠的破解檢測和無界的評分標準擴散。我們提出了 $\textbf{CARE}$ ($\textbf{C}$ontrastive $\textbf{A}$nchor-based $\textbf{R}$ubric $\textbf{E}$volution)，該方法將每一步評分標準的演變基於由前沿模型生成的高質量錨點回應，該模型是基於提示及其評分標準進行條件化的。在每個訓練步驟中，CARE 將得分最高的回應與錨點進行對比，從而啟用兩個互補的機制：一個自適應分支，反應性地修復獎勵錯誤規範；以及一個追逐分支，主動將前沿水平的質量差距轉化為更精確的評分標準。這兩個分支共同 $\textbf{維持高獎勵區域的區分準確性}$——即獎勵過度優化主要來源的精確區域。在使用 Qwen2.5-7B-Base 和 Qwen2.5-7B-Instruct 的 WildChecklist-9K 上進行的實驗顯示，CARE 在 Arena-Hard-2.0、InfoBench 和 FollowBench 上達到了最先進的性能，並且是唯一一種在 300 個訓練步驟中對 GPT-4.1 錨點回應的勝率顯示持續改善的方法；對 Llama-3.1-8B-Instruct 和 Qwen3-8B 的額外結果進一步表明，CARE 在模型家族之間具有良好的泛化能力。

##### **Benchmarking Vision-Language Models for Automated Pathology Diagnosis and Report Generation**
2609.00866v1 by Yumi Lee, Harim Oh, Hyoryung Kim, Minji Kim, Eunsu Kim, Hyeseong Lee, Junya Fukuoka, Andrey Bychkov, Jijgee Munkhdelger, Rajiv Kumar Kaushal, Ayushi Sahay, Rajni Yadav, Bharathi Prabakaran, Sulen Sarioglu, Serdar Balcı, Ilknur Turkmen, Yuri Tolkach, Christian Harder, Julian Westerdorf, Reinhard Buettner, Audun Ljone Henriksen, Sepp De Raedt, Byung Hyun Lee, Sungjin Lim, Joohoon Lee, Gwanghyun Kim, Se Young Chun, Suryakant Singh, Saarthak Kapse, Prateek Prasanna, Kyung A Kim, Yousun Kang, Sehwan Yoo, Sungman Hong, Shubham Innani, Michael Feldman, Spyridon Bakas, Ujjwal Baid, Prasad Dutande, Suhas Gajare, Bhakti Baheti, Serkan Sökmen, Ece Tuğba Cebeci, Ahmet Halıcı, Musa Balcı, Kardelen Peçenek, Srividhya Sainath, Kyongseok Jang, Messi H. J. Lee, Noorul Wahab, Bodong Du, Jiaming Zhang, Qixiang Zhang, Jang-Hwan Choi, Sangjeong Ahn

The rapid advancement of vision-language models (VLMs) has accelerated progress in computational pathology; however, whole-slide image (WSI)-based pathology report generation remains limited by the scarcity of large-scale WSI--report datasets and the complexity of mapping spatially distributed visual patterns to structured clinical text. To address this, we introduce a clinically curated Pan-Asia WSI--report dataset of approximately 10,500 pairs from five institutions and establish the REG 2025 benchmark through a MICCAI challenge for systematic evaluation of multimodal models. We analyze submitted methods spanning pretrained VLMs, multiple-instance learning frameworks, hierarchical expert models, retrieval-augmented generation, and cross-modal Transformers. Rather than indicating that VLM use alone was sufficient for superior performance, the results suggest that top-performing methods benefited from structured report representations, hierarchical diagnostic decomposition, and effective multimodal grounding. We identify key limitations, including instability in quantitative attribute estimation (e.g., numeric hallucination) and a tendency toward diagnostic overspecification, with some errors resembling known diagnostic pitfalls in routine pathology. These findings establish REG 2025 as a benchmark for evaluating WSI-based structured report generation and vision-language understanding in computational pathology, providing insights for the design of clinically grounded multimodal pathology models.

摘要：視覺-語言模型（VLMs）的快速進展加速了計算病理學的發展；然而，基於全幻燈片圖像（WSI）的病理報告生成仍受到大規模WSI-報告數據集稀缺和將空間分佈的視覺模式映射到結構化臨床文本的複雜性所限制。為了解決這個問題，我們引入了一個臨床策劃的泛亞WSI-報告數據集，包含來自五個機構的約10,500對數據，並通過MICCAI挑戰建立了REG 2025基準，以進行多模態模型的系統評估。我們分析了提交的方法，這些方法涵蓋了預訓練的VLMs、多實例學習框架、分層專家模型、檢索增強生成和跨模態Transformer。結果顯示，並不是僅僅使用VLM就足以獲得卓越的性能，而是表現最佳的方法受益於結構化報告表示、分層診斷分解和有效的多模態基礎。 我們識別出關鍵的限制，包括定量屬性估計的不穩定性（例如，數字幻覺）和診斷過度具體化的趨勢，某些錯誤類似於常規病理學中的已知診斷陷阱。這些發現確立了REG 2025作為評估基於WSI的結構化報告生成和計算病理學中視覺-語言理解的基準，為設計臨床基礎的多模態病理模型提供了見解。

##### **One note in three: a verified census of three deployed AI scribes, and the instrument that counted it**
2608.31017v1 by Sebastian Fox, Luke Markham, Ryan Lail, Michael Karotsieris

Ambient AI scribes draft clinical notes under the reassurance that a clinician signs every note. We audited three commercial AI scribes on the same 142 consultations: 565 notes from recorded UK primary-care and US ambulatory encounters plus authored scenarios. Twelve discovery passes proposed 13,678 candidate errors; the 5,898 clearing an importance filter went to an adversarial panel of two models from different families, each told to refute what it could, and 618 survived. One note in three (31.3% [27.0, 35.6]) carries a verified failure, concentrated in allergy and medication information, invented patient identity, and history written up as examination on telephone consultations that can contain none. No product was given a patient record; setting aside the two classes a record would have prefilled, invented identity and dates, the rate is 24.8% [20.8, 29.0]. One failure mode did not fit our scheme, drawn from published scribe-error taxonomies: a treatment the clinician retracts, recorded as delivered care. Two clinicians adjudicated blind, disjoint samples: a physician author upheld 20 of 21 findings (95.2% [77.3, 99.2]) and an independent clinician, not an author, 12 of 12 ([75.8, 100]); both judged every sampled refusal genuine. A failure rate depends on the instrument as much as the scribes. With model, evidence and settings fixed, the review instruction alone moves the share of candidates verified from 9.3% to 79.0%, and the reviewing family moves it too: alone at that instruction the gentler flags 54.8% of notes against 27.8%. Between 28% and 97% of sampled notes carry a failure depending on the standard. Published audits disagree among themselves by a margin instrument differences alone can produce: omission is 54-86% of their errors against our 23.1%. We release all 618 findings with transcript-side evidence, every prompt and model version, and the re-runnable pipeline.

摘要：環境人工智慧書記在臨床醫生簽署每一份筆記的保證下起草臨床筆記。我們對三個商業人工智慧書記進行了審核，涵蓋同樣的142次諮詢：來自英國初級保健和美國門診遭遇的565份記錄筆記以及作者編寫的情境。十二次發現通過提出了13,678個候選錯誤；5,898個通過重要性篩選的錯誤被送到兩個不同家族的對抗性模型小組，每個模型都被告知要駁斥其能駁斥的內容，最終有618個存活下來。每三份筆記中就有一份（31.3% [27.0, 35.6]）存在經過驗證的失敗，主要集中在過敏和藥物資訊、虛構的病人身份，以及在電話諮詢中寫成檢查的歷史，這些電話諮詢中不可能包含這些內容。沒有產品被賦予病人紀錄；撇開兩個類別的預填紀錄，虛構身份和日期的情況下，失敗率為24.8% [20.8, 29.0]。一種失敗模式不符合我們的方案，這是從已發表的書記錯誤分類中提取的：一種臨床醫生撤回的治療，被記錄為已提供的護理。兩位臨床醫生進行盲審，樣本不重疊：一位醫生作者支持21項發現中的20項（95.2% [77.3, 99.2]），而一位獨立的臨床醫生（非作者）支持12項中的12項（[75.8, 100]）；兩人都認為每個樣本拒絕都是可信的。失敗率取決於工具與書記一樣多。在模型、證據和設置固定的情況下，僅審查指令就將驗證的候選比例從9.3%提高到79.0%，而審查的家族也會影響它：在該指令下，溫和的標記對54.8%的筆記提出異議，而27.8%的則不。根據標準，28%到97%的樣本筆記存在失敗。已發表的審核之間存在分歧，僅僅是工具差異就能產生的幅度：遺漏是他們錯誤的54-86%，而我們的為23.1%。我們釋出所有618項發現，並附有逐字稿邊證據、每個提示和模型版本，以及可重執行的流程。

##### **LLM Judges Verify Presence, Not Absence: Omission Blindness in AI Clinical Notes and What Recovers It**
2608.31016v1 by Sebastian Fox, Luke Markham, Ryan Lail, Michael Karotsieris

Ambient AI scribes draft clinical notes, and published audits find their dominant error is omission: information the encounter established that the note fails to record. The standard check is an LLM judge: a second model reads the note against the transcript and flags problems. We ask whether judges detect omissions. Public corpora cannot supply the answer key: their clinician reference notes and transcripts are materially discrepant. Our benchmark has 500 single-error note pairs from audited fact sheets, 298 with a named fact certainly absent and 202 added-or-altered controls. Across eight judge designs, paired discrimination (the flawed note below its clean twin, 0.5 a coin flip) reads 0.79-0.94 on added or altered content and 0.50-0.63 on omissions. On single notes, no design flags omissions reliably more often than perfect notes. Wording changes, voting and GEPA prompt optimisation move the operating point without creating usable detection. Restructuring the task recovers it: list the facts the transcript establishes, then check the note for each. Two methods reach it independently and trade off: a per-fact pipeline, and a GEPA-evolved prompt doing the same in one call. The pipeline's flags name the missing fact and its severity at 2.7% false alarms. The single call detects more (36.9% against 24.6%, p=0.002) at 6.2% false alarms and a tenth of the cost per note. A physician author validated 70 items and, where the two routes disagree, sided with the pipeline on 10 of 10 (p=0.002). A second clinician, not an author, graded the severity rubric blind and agrees to within a grade. On real vendor notes from a companion census no benchmark threshold transfers, but the re-calibrated single call detects more than the best of the eight at half its false-alarm rate. Omissions whose fact is restated elsewhere defeat both routes. We release the benchmark, prompts and judgements.

摘要：環境 AI 書記起草臨床筆記，已發表的審計發現其主要錯誤是遺漏：筆記未記錄的會議所建立的信息。標準檢查是一個 LLM 評判：第二個模型將筆記與逐字稿進行比對並標記問題。我們詢問評判是否能檢測到遺漏。公共語料庫無法提供答案鑑別鍵：它們的臨床參考筆記和逐字稿在實質上存在差異。我們的基準包含 500 對單一錯誤的筆記來自審計的事實表，其中 298 個確定缺少的命名事實和 202 個添加或更改的控制項。在八種評判設計中，配對區分（下方的有缺陷筆記與其乾淨的雙胞胎，0.5 為擲硬幣）在添加或更改內容上讀取 0.79-0.94，而在遺漏上則為 0.50-0.63。在單一筆記中，沒有設計能比完美筆記更可靠地標記遺漏。措辭變更、投票和 GEPA 提示優化改變了操作點，但未能創造可用的檢測。重構任務使其恢復：列出逐字稿所建立的事實，然後檢查每個筆記。兩種方法獨立達成並進行權衡：一個按事實的管道，以及一個 GEPA 演變的提示在一次呼叫中完成相同的工作。管道的標記名稱缺失的事實及其嚴重性，假警報率為 2.7%。單次呼叫檢測到更多（36.9% 對 24.6%，p=0.002），假警報率為 6.2%，每筆記的成本僅為十分之一。一位醫生作者驗證了 70 項目，並且在兩條路徑意見不合的地方，對於 10 個中的 10 個（p=0.002）都支持管道。第二位臨床醫生，並非作者，盲評了嚴重性標準並在一個等級內達成一致。在來自伴隨普查的真實供應商筆記中，沒有基準閾值轉移，但重新校準的單次呼叫檢測到的數量超過了八種中的最佳，且其假警報率僅為一半。其他地方重述的事實的遺漏擊敗了兩條路徑。我們釋放基準、提示和判斷。

##### **MR-JEPA: A General Purpose Video Foundation Model for Cardiac MRI**
2608.30975v1 by Athira J. Jacob, Puneet Sharma, Dorin Comaniciu, Daniel Rueckert

Cardiac magnetic resonance imaging (CMR) produces rich sequential data such as temporal cine videos and spatial LGE/mapping stacks, yet most deep learning approaches process individual 2D slices, discarding this context. We present MR-JEPA, a self-supervised video foundation model for CMR that extends LeJEPA to 3D spatiotemporal inputs through tubelet tokenization, spatiotemporal masking augmentation, and initialization from a 2D CMR foundation model. Unlike prior CMR video models limited to cine data, MR-JEPA is pretrained on multi-sequence data (cine, LGE, mapping) from 10,505 patients across two centers without annotations. We evaluate the frozen encoder on six downstream tasks using a unified multi-view gated attention architecture: LV ejection fraction, RV ejection fraction, three myocardial strains (GLS, GCS, GRS), and four-class disease detection. MR-JEPA outperforms other compared methods on all five regression tasks, including both a domain-specific CMR model pretrained on more data with text supervision and a natural-video foundation model, achieving an LV EF MAE of 4.79% (r =0.764) and a GLS MAE of 1.87 (r=0.805), with 21-27% MAE reductions over baselines on strain tasks. For disease detection, MR-JEPA achieved a macro AUG of 0.868, remaining competitive with the domain-specific baseline despite using a fully self-supervised pretraining objective. These results demonstrate the potential of a unified video encoder for robust, multi-view utilization of diverse CMR sequences in clinical cardiac quantification and diagnosis.

摘要：心臟磁共振成像（CMR）產生豐富的序列數據，例如時間序列影像和空間 LGE/映射堆疊，然而大多數深度學習方法處理單獨的 2D 切片，忽略了這些上下文。我們提出了 MR-JEPA，一個自我監督的 CMR 影像基礎模型，通過管狀標記化、時空遮罩增強和從 2D CMR 基礎模型初始化，將 LeJEPA 擴展到 3D 時空輸入。與以往僅限於電影數據的 CMR 影像模型不同，MR-JEPA 在來自兩個中心的 10,505 名患者的多序列數據（電影、LGE、映射）上進行了無標註的預訓練。我們使用統一的多視圖門控注意力架構對六個下游任務評估凍結的編碼器：左心室射血分數、右心室射血分數、三種心肌應變（GLS、GCS、GRS）和四類疾病檢測。在所有五個回歸任務中，MR-JEPA 的表現超過了其他比較方法，包括一個在更多數據上進行文本監督預訓練的領域特定 CMR 模型和一個自然影像基礎模型，達到左心室 EF MAE 為 4.79%（r =0.764）和 GLS MAE 為 1.87（r=0.805），在應變任務上相較基準減少 21-27% 的 MAE。對於疾病檢測，MR-JEPA 的宏觀 AUG 為 0.868，儘管使用了完全自我監督的預訓練目標，仍與領域特定基準保持競爭力。這些結果展示了統一影像編碼器在臨床心臟量化和診斷中對多樣 CMR 序列進行穩健多視圖利用的潛力。

##### **Responsible Integration of AI in Cancer Genomics: Barriers, Risks, and Pathways to Trustworthy Clinical Translation**
2608.30912v1 by Bahar İlgen, Yiannos Tolias, Denise Kühnert, Paraskevi Papadopoulou, Magnus Westerlund, Dominik Heider, Katharina Ladewig, Georges Hattab

Artificial intelligence (AI) and natural language processing (NLP) are increasingly used to extract, integrate, and interpret biomedical knowledge relevant to cancer genomics, yet their translation into routine clinical oncology has been comparatively slow. The central challenge is not computational capability alone, but trustworthy integration into clinical workflows. This review examines how NLP and AI support the cancer genomics pipeline, from literature mining and automated variant interpretation to clinical trial matching, knowledge graph construction, and multimodal data integration. We identify four interrelated translational failure domains: evidence inconsistency, explainability and uncertainty, data governance and reproducibility, and interoperability. Rather than considering these challenges in isolation, we take a systems-level view, focusing on their interaction across the translational pathway. We propose a conceptual framework and roadmap for addressing these domains through rigorous validation, uncertainty-aware methods, interoperable infrastructures, regulatory alignment, and human oversight across the AI lifecycle. Progress toward routine clinical use will depend less on further improving model capability than on systematically addressing these interacting failure domains from development through deployment and post-deployment monitoring.

摘要：人工智慧（AI）和自然語言處理（NLP）越來越多地用於提取、整合和解釋與癌症基因組學相關的生物醫學知識，但它們轉化為常規臨床腫瘤學的速度相對較慢。主要挑戰不僅僅是計算能力，而是可信的整合進入臨床工作流程。這篇評論檢視了NLP和AI如何支持癌症基因組學的流程，從文獻挖掘和自動變異解釋到臨床試驗匹配、知識圖譜構建和多模態數據整合。我們確定了四個相互關聯的轉化失敗領域：證據不一致性、可解釋性和不確定性、數據治理和可重複性，以及互操作性。我們不僅將這些挑戰孤立考慮，而是採取系統層面的觀點，專注於它們在轉化路徑中的相互作用。我們提出了一個概念框架和路線圖，以通過嚴格的驗證、考慮不確定性的方法、可互操作的基礎設施、監管對齊和整個AI生命週期中的人類監督來解決這些領域。向常規臨床使用的進展將不僅依賴於進一步提高模型能力，而是系統性地解決從開發到部署及部署後監控的這些相互作用的失敗領域。

##### **TAMI: Temporally Aligned, Missingness-Aware, and Interpretable Multimodal Fusion for Mental Health Assessment in Older Adults with Mild Cognitive Impairment**
2608.30857v1 by Merna Bibars, Bolaji Omofojoye, Allan I. Levey, Rachel Hershenberg, Gari D. Clifford, Hyeokhyen Kwon

Depression and anxiety in older adults with Mild Cognitive Impairment (MCI) are frequently underdiagnosed due to limited access to care. Multimodal analysis of remote clinical interviews is a scalable screening approach, but existing methods have three limitations. First, they do not correct temporal misalignment across multimodal features extracted at different resolutions, inducing spurious cross-modal associations. Second, remote recordings exhibit uneven modality dropout, but missing values are often zero-filled, making them indistinguishable from valid near-zero measurements. Finally, they do not jointly attribute predictions to modalities, questions, and interview moments, limiting fine-grained clinical interpretation. We propose a Temporally-Aligned, Missingness-Aware, Interpretable (TAMI) multimodal fusion framework. TAMI aligns speech, language, facial, and physiological features within question-answer segments on a shared timeline, encodes modality-level missingness over time, and conditions fusion on question context. In interviews with 49 older adults with MCI, TAMI achieved area under the receiver operating characteristic curve (AUROC) scores of 0.68 (depression) and 0.69 (anxiety). Fine-grained temporal alignment of multimodal features produced the largest performance gain ($Δ{\geq}0.1$). Multi-level interpretability analysis revealed that depression classification relied on eyegaze and open-ended questions, while anxiety classification depended on eyegaze and head pose, with attribution uniformly distributed across questions. Using only responses to the open-ended questions (5.1min), the depression model achieved an AUROC score of 0.67, which was not significantly different from using the full interview (19min) ($p>0.05$). Our findings support designing interview protocols centered on open-ended questions for depression screening in older adults with MCI.

摘要：抑鬱症和焦慮症在輕度認知障礙（MCI）的老年人中經常被低估診斷，這是因為獲得護理的途徑有限。遠程臨床訪談的多模態分析是一種可擴展的篩查方法，但現有的方法有三個限制。首先，它們未能修正不同解析度下提取的多模態特徵之間的時間錯位，導致虛假的跨模態關聯。其次，遠程錄音顯示出不均勻的模態丟失，但缺失值通常被填充為零，這使得它們與有效的接近零的測量無法區分。最後，它們未能將預測共同歸因於模態、問題和訪談時刻，限制了細緻的臨床解釋。我們提出了一個時間對齊、缺失感知和可解釋的（TAMI）多模態融合框架。TAMI在共享時間線上對問題-回答片段中的語音、語言、面部和生理特徵進行對齊，隨時間編碼模態級缺失，並基於問題上下文進行融合。在對49名MCI老年人的訪談中，TAMI達到了0.68（抑鬱）和0.69（焦慮）的接收者操作特徵曲線下面積（AUROC）分數。多模態特徵的細緻時間對齊產生了最大的性能增益（$Δ{\geq}0.1$）。多層次可解釋性分析顯示，抑鬱分類依賴於眼動和開放式問題，而焦慮分類則依賴於眼動和頭部姿勢，歸因在問題之間均勻分佈。僅使用對開放式問題的回答（5.1分鐘），抑鬱模型達到了0.67的AUROC分數，這與使用完整訪談（19分鐘）相比並無顯著差異（$p&gt;0.05$）。我們的研究結果支持設計以開放式問題為中心的訪談協議，以便在MCI老年人中進行抑鬱篩查。

##### **VFR-Audit: Verdict-Level Reliability for Fairness Audits in Hospital Length-of-Stay Prediction**
2608.30846v1 by Md Jannatul Rakib Joy, Viet Vo, Caslon Chua

Fairness audits in clinical Artificial Intelligence convert continuous fairness metrics into binary pass-or-fail verdicts against operational thresholds, where hospital governance boards, payers, and regulators act on the resulting verdicts. Such audits are repeated over time and across hospital sites, thus the same verdict can flip between pass and fail across audits. Existing uncertainty methods such as Bayesian posteriors, bootstrap confidence intervals, and permutation tests address verdict instability only at the continuous-metric level. Converting metric-level uncertainty into a verdict-stability claim remains a manual step that scales poorly across the (model, metric, attribute) cells an audit covers. Existing uncertainty methods also leave open whether bias-mitigation steps, such as reweighing or per-group threshold shifts, yield a stable passing verdict at the cost of model discrimination measured as AUROC or AUPRC.To address this verdict-stability gap, we propose VFR-Audit, a framework built around the Verdict Flip Rate (VFR), a scalar bounded between 0 and 0.5 that measures the probability of verdict reversal under stratified bootstrap resampling. VFR-Audit reports VFR alongside three reliability axes, namely within-cohort resampling stability, audit-size sensitivity, and cross-hospital verdict agreement via Fleiss' kappa.

摘要：臨床人工智慧中的公平性審計將連續的公平性指標轉換為二元的通過或失敗判決，並依據運營閾值進行判斷，醫院治理委員會、支付者和監管機構根據這些判決採取行動。這樣的審計會隨時間和醫院地點重複進行，因此同一判決在不同的審計中可能會在通過和失敗之間翻轉。現有的不確定性方法，如貝葉斯後驗、引導信賴區間和置換檢驗，只在連續指標層面上解決判決不穩定性。將指標層級的不確定性轉換為判決穩定性的主張仍然是一個手動步驟，這在審計涵蓋的（模型、指標、屬性）單元中擴展效果不佳。現有的不確定性方法也沒有說明偏見緩解步驟，例如重新加權或按組閾值變更，是否能在以AUROC或AUPRC測量的模型歧視成本下產生穩定的通過判決。為了解決這一判決穩定性差距，我們提出了VFR-Audit，這是一個圍繞判決翻轉率（VFR）構建的框架，VFR是一個介於0和0.5之間的標量，測量在分層引導重抽樣下判決翻轉的概率。VFR-Audit報告VFR以及三個可靠性軸，即群內重抽樣穩定性、審計規模敏感性和通過Fleiss' kappa的跨醫院判決一致性。

##### **SingProbe Technical Report**
2608.30703v1 by  Sing Team

Runtime guardrails are essential for reliable large language model (LLM) deployment, yet existing approaches typically rely on independent, external models that introduce additional inference cost, delayed safety signals, and a capacity mismatch with increasingly capable base models. To address these issues, we introduce SingProbe, a lightweight intrinsic runtime guard that directly reuses hidden states produced during LLM inference and operates alongside autoregressive decoding. Within a unified framework, SingProbe continuously predicts query intent, response safety, and hallucination risk at the token level with negligible additional guardrail inference overhead, offering a "free-lunch" solution. We further introduce SingStreamBench, a benchmark designed to assess whether streaming guardrails remain inactive on benign prefixes while promptly detecting emerging unsafe content. Extensive experiments show that SingProbe achieves competitive or superior performance compared with substantially larger standalone guardrails and specialized hallucination detectors, with only $\approx$2M parameters and $<0.5\%$ extra overhead. Beyond passive detection, we also show that SingProbe scores can anticipate future generation risk and guide constrained safe decoding. We further extend this paradigm to medical generation through SingProbe-Med, which selectively activates risk-directed decoding interventions only when clinically relevant risks emerge. Together, these results demonstrate that internal model representations provide an effective and efficient interface for generation-time monitoring and control.

摘要：運行時護欄對於可靠的大型語言模型（LLM）部署至關重要，然而現有的方法通常依賴於獨立的外部模型，這會引入額外的推理成本、延遲的安全信號，以及與日益強大的基礎模型之間的能力不匹配。為了解決這些問題，我們引入了SingProbe，一種輕量級的內在運行時護欄，直接重用在LLM推理過程中產生的隱藏狀態，並與自回歸解碼一起運作。在一個統一的框架內，SingProbe持續在令牌層面預測查詢意圖、響應安全性和幻覺風險，並且幾乎不增加額外的護欄推理開銷，提供了一種“免費午餐”的解決方案。我們進一步引入SingStreamBench，一個基準，用於評估流式護欄是否在良性前綴上保持不活動，同時迅速檢測出新出現的不安全內容。大量實驗顯示，SingProbe的性能與大得多的獨立護欄和專門的幻覺檢測器相比，表現出競爭力或優越性，僅有約2M的參數和小於0.5%的額外開銷。除了被動檢測外，我們還展示了SingProbe的分數可以預測未來生成風險並引導受限的安全解碼。我們進一步將這一範式擴展到醫療生成，通過SingProbe-Med，僅在臨床相關風險出現時選擇性地啟動風險導向的解碼干預。綜合這些結果顯示，內部模型表示提供了一個有效且高效的生成時監控和控制介面。

##### **MedAgent-R1: Faithfulness-Aware Reinforcement Learning for Evidence-Grounded Medical Reasoning**
2608.30676v1 by Jiangwang Chen, Chenghao Zhang, Hengxing Cai

When medical AI systems hallucinate clinical reasoning, the consequences extend beyond incorrect answers: fabricated justifications that superficially reference retrieved evidence can mislead clinicians into unsafe treatment decisions. Medical reasoning agents must therefore produce not only correct answers but also faithful justifications that clinicians can verify against cited evidence. We identify a systematic failure mode in RL-trained retrieval agents: outcome-only rewards improve accuracy while degrading faithfulness, a phenomenon we term confident hallucination. The agent learns to answer from parametric memory and backfill plausible but unsupported justifications; citation fabrication rates rise from 16.5% to 31.8% even as accuracy improves by 5 points over the supervised baseline. We address this with a faithfulness-gated reward design: accuracy credit is conditioned on evidence grounding via a hard gate, complemented by retrieval validity and conciseness signals that close exploitation paths unique to agentic retrieval. The resulting system, MedAgent-R1, reduces citation fabrication from 31.8% to 4.7% and raises evidence completeness from 58.7 to 82.6 while maintaining 75.1% accuracy, with 13.2-point gains on HealthBench Safety. Under the same agentic retrieval setup, MedAgent-R1 outscores GPT-4o on faithfulness-specific dimensions (Factual Support 4.55 vs. 4.25; Overclaiming 4.40 vs. 4.15) while remaining below GPT-4o in overall accuracy, suggesting that explicit faithfulness training yields evidence-grounding gains not achieved by scaling alone.

摘要：當醫療 AI 系統出現臆想的臨床推理時，其後果不僅僅是錯誤的答案：虛構的理由表面上參考檢索到的證據，可能會誤導臨床醫生做出不安全的治療決策。因此，醫療推理代理必須不僅提供正確的答案，還必須提供臨床醫生可以根據引用的證據進行驗證的真實理由。我們發現了一種系統性失敗模式，在 RL 訓練的檢索代理中：僅基於結果的獎勵提高了準確性，但降低了真實性，這一現象我們稱之為自信的臆想。代理學會從參數記憶中回答問題，並填補合理但未經支持的理由；即使準確性比監督基線提高了 5 個點，引用虛構的比率仍從 16.5% 上升到 31.8%。我們通過一種真實性門控的獎勵設計來解決這個問題：準確性信用依賴於通過硬門進行的證據基礎，並輔以檢索有效性和簡潔性信號，這些信號關閉了獨特於代理檢索的利用路徑。最終系統 MedAgent-R1 將引用虛構率從 31.8% 降至 4.7%，並將證據完整性從 58.7 提高到 82.6，同時保持 75.1% 的準確性，在 HealthBench Safety 上獲得 13.2 點的增益。在相同的代理檢索設置下，MedAgent-R1 在真實性特定維度上超過了 GPT-4o（事實支持 4.55 對 4.25；過度聲稱 4.40 對 4.15），同時在整體準確性上仍低於 GPT-4o，這表明明確的真實性訓練帶來的證據基礎增益是單靠規模無法實現的。

##### **Whole-Slide Image Analysis under Realistic Few-Shot Annotation Protocols**
2608.30420v1 by Tiffanie Godelaine, Maxime Zanella, Karim El Khoury, Benoit Macq, Christophe De Vleeschouwer

Automating the analysis of whole-slide images has high clinical value, since characterizing cancers requires examining them in detail. Such analysis increasingly relies on vision-language models that provide patch-level zero-shot predictions. However, these predictions remain noisy and must be refined with a few annotations. A promising paradigm for this refinement is few-shot transduction. Rather than treating each patch independently, these methods leverage the relations between patches, together with a few annotations, to refine all predictions jointly. However, current transductive methods are evaluated under conditions that overlook key properties of whole-slide images: (i) datasets consist of independent patches extracted from multiple slides, ignoring the complex tissue organization; (ii) datasets are mostly balanced, whereas a single whole-slide image exhibits severe class imbalance, with several classes absent; and (iii) annotations are sampled at random, without reflecting how a pathologist annotates a limited number of regions. To align the transduction paradigm to realistic whole-slide settings, we introduce the following contributions. First, we propose SlideCRF, which adapts conditional random fields for whole-slide images by combining spatial and biological cues while accounting for classes that may be absent from a given slide. Second, we provide a set of realistic annotation protocols, based on spatially localized clicks and scribbles, modeling different pathologist interactions, such as the iterative correction of model errors. Across four datasets, we show that SlideCRF outperforms current transductive methods in macro F1, improving over the zero-shot predictions by +24.2% and +37.5% with one and 16 clicks per present class, respectively.

摘要：自動化整張影像的分析具有高度的臨床價值，因為對癌症的特徵化需要詳細檢查。這種分析越來越依賴於提供區塊級別零樣本預測的視覺-語言模型。然而，這些預測仍然存在噪音，必須通過少量標註進行細化。一種有前景的細化範式是少樣本轉導。這些方法不是獨立處理每個區塊，而是利用區塊之間的關係，結合少量標註，來共同細化所有預測。然而，當前的轉導方法是在忽略整張影像關鍵特性的條件下進行評估的：(i) 數據集由從多個幻燈片中提取的獨立區塊組成，忽略了複雜的組織結構；(ii) 數據集大多是平衡的，而單一整張影像則顯示出嚴重的類別不平衡，某些類別缺失；(iii) 標註是隨機抽樣的，並未反映病理學家如何標註有限數量的區域。為了使轉導範式與現實的整張影像設置對齊，我們提出以下貢獻。首先，我們提出SlideCRF，它通過結合空間和生物線索來調整條件隨機場，以適應整張影像，同時考慮可能缺失的類別。其次，我們提供了一組現實的標註協議，基於空間定位的點擊和塗鴉，模擬不同病理學家的互動，例如對模型錯誤的迭代修正。在四個數據集中，我們顯示SlideCRF在宏觀F1指標上超越了當前的轉導方法，分別在每個存在類別的1次和16次點擊中，對零樣本預測的改進分別為+24.2%和+37.5%。

##### **Dense Clinical Contrasts Enhance Medical Knowledge Updating in Large Language Models**
2608.30405v1 by Yangmin Huang, Shu Quan, He Geng, Xin Ye, Qianyun Du, Zhiyang He, Jiaxue Hu, Xiaodong Tao

Medical knowledge changes continually, making large language models vulnerable to relying on outdated yet clinically plausible information. We study whether the format of supervision affects medical knowledge updating under a matched training-budget setting. We introduce SEER-Bench, a temporally anchored oncology-staging benchmark curated from the latest versioned SEER Research Data release, and render identical medical update events from NCCN oncology guidelines into four supervision formats: EMQ, MSQ, FITB, and SAQ. Across SEER-Bench and HealthBench Professional, EMQ gives the most stable external transfer and retention among same-budget SFT variants. With EMQ supervision, the updated 4B model produces competitive results on temporally anchored oncology staging, reaching 64.8% answer accuracy and 59.6% rationale accuracy on SEER-Bench. Diagnostic analyses suggest that EMQ exposes denser clinical contrast signals while preserving discriminative representations with smaller movement from the base model. These results show that medical knowledge updating depends not only on the update algorithm, but also on how knowledge is structured as supervision.

摘要：醫學知識不斷變化，使得大型語言模型容易依賴過時但臨床上合理的信息。我們研究監督格式是否會影響在匹配訓練預算設置下的醫學知識更新。我們介紹了SEER-Bench，這是一個基於時間的腫瘤學分期基準，來自最新版本的SEER研究數據發布，並將NCCN腫瘤學指南中的相同醫學更新事件轉換為四種監督格式：EMQ、MSQ、FITB和SAQ。在SEER-Bench和HealthBench Professional中，EMQ在相同預算的SFT變體中提供了最穩定的外部轉移和保留。使用EMQ監督，更新的4B模型在基於時間的腫瘤學分期上產生了具有競爭力的結果，在SEER-Bench上達到了64.8%的答案準確率和59.6%的推理準確率。診斷分析表明，EMQ揭示了更密集的臨床對比信號，同時保持了較小的基模型變動下的區分性表示。這些結果顯示，醫學知識更新不僅依賴於更新算法，還依賴於知識作為監督的結構。

##### **ImageCAS-X: a dataset and benchmark for coronary artery segmentation and centerline extraction in coronary CT angiography**
2608.30404v1 by Kit M. Bransby, Esther Øksnebjerg, Kristoffer Kjær, Jacob Kirkeby, Yasmin El Youssef, Aïda Jiménez, Philip R. Pedersson, Martina C. de Knegt, Klaus F. Kofoed, Rasmus R. Paulsen

Accurate segmentation of the coronary vessel lumen is a prerequisite for quantitative assessment of atherosclerotic plaque and perivascular adipose tissue in coronary computed tomography angiography (CCTA). Cardiologists rely on semi-automated methods for this task because manual vessel tracing and segmentation are labour-intensive. Although many automated methods have been proposed, their validation remains limited by the lack of large, high-quality publicly available datasets. We provide a new dataset of voxel-wise annotations of the vessel lumen and coronary segments, alongside centerlines, and mesh surfaces for 800 scans from the publicly available ImageCAS dataset. Using this dataset, we benchmark established lumen segmentation methods against inter-observer variability, stratifying performance by disease, image quality, coronary dominance, coronary segment, vessel diameter, and lumen attenuation. These labels allow segmentation accuracy to be described in anatomical and clinical context rather than reported as a single aggregate score. The dataset supports the development and validation of methods for lumen segmentation, plaque and perivascular quantification, and haemodynamic modelling.

摘要：精確的冠狀血管腔體分割是定量評估冠狀計算機斷層血管造影 (CCTA) 中動脈粥樣硬化斑塊和血管周圍脂肪組織的前提。心臟病醫生依賴半自動化的方法來完成這項任務，因為手動血管描繪和分割非常耗費人力。儘管已提出許多自動化方法，但由於缺乏大型、高質量的公開數據集，其驗證仍然受到限制。我們提供了一個新的數據集，包含了來自公開可用的 ImageCAS 數據集的 800 次掃描的血管腔體和冠狀段的體素級註釋，並附有中心線和網格表面。使用這個數據集，我們將已建立的腔體分割方法與觀察者之間的變異性進行基準測試，並根據疾病、影像質量、冠狀優勢、冠狀段、血管直徑和腔體衰減來分層性能。這些標籤允許在解剖和臨床背景中描述分割準確性，而不是以單一的綜合分數報告。該數據集支持腔體分割、斑塊和血管周圍定量以及血流動力學建模方法的開發和驗證。

##### **AI Morbidity and Mortality: A Framework for Clinical AI Failure Review**
2609.00076v1 by Paulius Mui, Dean F. Sittig, Steve Labkoff, Sanjay Basu

Clinical artificial intelligence is increasingly embedded in real-world care, yet existing safety mechanisms are poorly suited to reconstructing and learning from individual AI-related errors and near-misses. Aggregate model monitoring can identify performance changes, and traditional patient safety reporting can capture adverse events, but neither is designed to explain how risk emerges across the interaction among AI systems, clinicians, workflows, and institutional controls. We propose AI Morbidity and Mortality (AI M&M), a structured, blameless framework for case-based review of clinical AI failures. The framework combines standardized case intake, evidence preservation and investigator-level reconstruction, tool-in-loop attribution, and corrective-action tracking. Each event is classified across four linked dimensions: Trigger - Mechanism - Clinical Pathway - Corrective Action, separating the condition that exposed a vulnerability from the process that produced risk, its consequence for care, and the remediation assigned. We demonstrate the framework using five illustrative outpatient medication and clinical decision-support cases; two clinician reviewers independently applied all four classification axes and reached agreement across all 20 axis-level classifications. AI M&M is intended to complement, rather than replace, model monitoring, patient safety reporting, and regulatory oversight by converting individual AI-in-workflow failures into actionable institutional learning. Prospective evaluation across institutions, AI systems, and clinical settings is needed.

摘要：臨床人工智慧越來越多地嵌入現實世界的護理中，然而現有的安全機制對於重建和學習個別AI相關錯誤和接近失誤的能力較差。聚合模型監測可以識別性能變化，傳統的病人安全報告可以捕捉不良事件，但兩者都不是為了解釋風險如何在AI系統、臨床醫生、工作流程和機構控制之間的互動中產生而設計的。我們提出了AI疾病與死亡（AI M&M），這是一個結構化的、無責任的框架，用於基於案例的臨床AI失敗回顧。該框架結合了標準化的案例接收、證據保存和調查者級別的重建、工具內歸因和糾正行動追蹤。每個事件根據四個相互關聯的維度進行分類：觸發 - 機制 - 臨床路徑 - 糾正行動，將暴露脆弱性的條件與產生風險的過程、對護理的後果以及分配的補救措施分開。我們使用五個示例性門診藥物和臨床決策支持案例來演示該框架；兩位臨床審查員獨立應用所有四個分類軸，並在所有20個軸級分類中達成一致。AI M&M旨在補充，而不是取代模型監測、病人安全報告和監管監督，通過將個別AI工作流程中的失敗轉化為可操作的機構學習。需要在不同機構、AI系統和臨床環境中進行前瞻性評估。

##### **Co-Annotator: Expert-Distilled ViT and VLM for Visual and Documentation Guidance in Age-Related Macular Degeneration**
2608.30352v1 by Ziheng "Leo" Li, Benjamin Freeman, Akshay Raman, Kavin Aravindhan Rajkumar, Xinxin Fang, Rishabh Srivastava, Steven Feiner, Kaveri A. Thakoor

Clinical AI often optimizes predictive performance without engaging how clinicians decide where to look and what to write. We present Co-Annotator, which distills expert gaze and dictation into two guidance components: a gaze-aligned Vision Transformer producing fixation-aligned areas of interest (AOIs), and an ontology-bounded vision-language model (VLM) that pre-fills editable biomarker summaries for retinal optical coherence tomography (OCT). We first collect expert gaze and dictations (US1) to train the models, significantly improving diagnostic accuracy and biomarker generation. We then deploy the system with ophthalmology residents: a controlled resident study (US2) confirmed each modality is safe and independently beneficial, with AOI guidance producing lasting perceptual efficiency gains through post-guidance carryover and VLM guidance more than doubling biomarker documentation breadth. In a combined deployment across two academic institutions (US3), providing both modalities simultaneously produced efficiency gains that substantially exceeded either modality alone: correct diagnoses per minute increased by 40% and comment editing time fell by 67%, without compromising diagnostic accuracy. Notably, neither modality improved efficiency during guidance in US2, which makes the in-guidance efficiency gain under combined guidance in US3 the more striking result. Expert-distilled multimodal guidance can remove two distinct clinical workflow bottlenecks at once (visual search overhead and documentation burden) without compromising the diagnostic accuracy clinicians already achieve.

摘要：臨床人工智慧通常會優化預測性能，但不涉及臨床醫師如何決定觀察何處和撰寫內容。我們提出了Co-Annotator，它將專家視線和口述提煉為兩個指導組件：一個與視線對齊的視覺Transformer，生成與注視對齊的興趣區域（AOIs），以及一個受本體約束的視覺-語言模型（VLM），為視網膜光學相干斷層掃描（OCT）預填可編輯的生物標記摘要。我們首先收集專家的視線和口述（US1）以訓練模型，顯著提高診斷準確性和生物標記生成。然後，我們與眼科住院醫師部署該系統：一項受控的住院醫師研究（US2）確認每種模式都是安全且獨立有益的，AOI指導通過後續指導的延續產生持久的感知效率提升，而VLM指導則使生物標記文檔的範圍增加了兩倍以上。在兩個學術機構的聯合部署中（US3），同時提供這兩種模式產生的效率提升大大超過單獨使用任何一種模式：每分鐘的正確診斷增加了40%，而評論編輯時間減少了67%，且未損害診斷準確性。值得注意的是，在US2的指導期間，沒有任何一種模式提高效率，這使得在US3的聯合指導下的指導內效率提升成為更引人注目的結果。專家提煉的多模態指導可以同時消除兩個不同的臨床工作流程瓶頸（視覺搜索開銷和文檔負擔），而不影響臨床醫師已經達到的診斷準確性。

##### **MiNER: Fine-Tuned Biomedical Natural Language Processing for Malaria Disease Entity Recognition in Clinical Texts**
2609.00073v1 by V. S. Anoop, Devika N

Malaria remains a significant global health burden, necessitating continuous research efforts to understand its complex molecular mechanisms, epidemiology, and potential therapeutic interventions. Extracting essential biomedical information from the vast and constantly growing malaria literature is a challenging task that demands innovative approaches. Recently, pre-trained language models have revolutionized natural language processing tasks, demonstrating remarkable capabilities in various domains. This paper proposes a fine-tuned pre-trained biomedical language model for biomedical information extraction from scientific literature on malaria disease. The proposed methodology selects and preprocesses a large corpus of scientific articles on malaria, and then annotates them with entities of clinical significance. It then leverages BioBERT, a state-of-the-art pre-trained language model, to encode the textual data into context-aware representations. We fine-tune the model using domain-specific annotations and supervised learning to enhance its ability to extract relevant biomedical named entities. Extensive experiments and comparisons with different encoding and machine learning algorithms show that the proposed approach significantly outperforms them in precision, recall, and accuracy. We also publish our human-labeled dataset for entity and relation extraction to enable other health informatics researchers to train advanced models for malaria information extraction.

摘要：疟疾仍然是全球健康的重要負擔，這需要持續的研究努力來理解其複雜的分子機制、流行病學和潛在的治療干預措施。從龐大且不斷增長的疟疾文獻中提取基本的生物醫學信息是一項挑戰性的任務，這需要創新的方法。最近，預訓練的語言模型徹底改變了自然語言處理任務，在各個領域展現了卓越的能力。本文提出了一種針對疟疾科學文獻的生物醫學信息提取的微調預訓練生物醫學語言模型。所提出的方法選擇並預處理了大量關於疟疾的科學文章，然後用臨床重要性的實體進行註釋。接著，利用BioBERT這一最先進的預訓練語言模型，將文本數據編碼為上下文感知的表示。我們使用特定領域的註釋和監督學習來微調模型，以增強其提取相關生物醫學命名實體的能力。廣泛的實驗和與不同編碼和機器學習算法的比較顯示，所提出的方法在精確度、召回率和準確性方面顯著優於它們。我們還發布了我們的人類標註數據集，用於實體和關係提取，以使其他健康信息學研究人員能夠訓練先進的疟疾信息提取模型。

##### **CoLa-ICD: A Knowledge-Enhanced Framework for Long-Tail Automated Medical Coding**
2608.30234v1 by Yihang Cheng, Veronica Liesaputra, Andrew Trotman

Automatic medical coding assigns ICD codes to clinical notes, but it remains challenging due to long documents, imbalanced label distributions, and diverse terms. These challenges are especially severe for rare codes, which have limited training instances and are easily confused with semantically similar labels. We introduce CoLa-ICD, a knowledge-enhanced framework for long-tail prediction. CoLa-ICD enriches ICD labels with external terms, models dependencies among related codes, and learns stronger alignment between label semantics and clinical evidence for long-tail prediction. Experiments show that CoLa-ICD improves long-tail prediction with larger gains in larger and sparser label spaces and achieves state-of-the-art performance in AUC, F1, and P@k. Our code is available at https://github.com/youwillbethebest/Cola-ICD.

摘要：自動醫療編碼將ICD代碼分配給臨床筆記，但由於長文檔、不平衡的標籤分佈和多樣的術語，這仍然是一個挑戰。這些挑戰對於稀有代碼尤其嚴重，因為它們的訓練實例有限，且容易與語義相似的標籤混淆。我們介紹CoLa-ICD，一個增強知識的長尾預測框架。CoLa-ICD通過外部術語豐富ICD標籤，建模相關代碼之間的依賴關係，並學習標籤語義與臨床證據之間更強的對齊，以進行長尾預測。實驗表明，CoLa-ICD在較大和較稀疏的標籤空間中改善了長尾預測，並在AUC、F1和P@k上達到了最先進的性能。我們的代碼可在https://github.com/youwillbethebest/Cola-ICD獲得。

##### **Automatic Conversion of NICE Guidelines to an Executable Computational Model Using Large Language Models**
2608.30022v1 by Ashvin Gupta, Denys Prociuk, Alessandra Russo, Brendan C. Delaney

Introduction: NICE guidelines provide evidence-based recommendations for clinical care but remain largely in unstructured natural language. Existing approaches to converting them into computable representations often focus on individual diseases, require substantial manual encoding, and do not scale. Large language models (LLMs) may enable much of this translation to be automated. Methods: We present an end-to-end approach that converts textual clinical guidelines into executable models capable of generating explainable, patient-specific recommendations. A stepwise LLM-based transformation with in-context examples produces human-inspectable intermediate artifacts. We apply the approach to NICE pancreatic and lung cancer guidelines, use expert review to assess rule alignment, and evaluate the executable pancreatic cancer model on 20 patient vignettes. Results: Expert review showed strong alignment between the source guidelines and generated executable models. Most discrepancies were partial omissions rather than incorrect logic, while hallucinated or fundamentally incorrect rules were rare. On the patient vignettes, the executable model achieved an F1 score of 82.5%. Conclusion: LLMs can transform natural-language NICE guidelines into interpretable, executable models that preserve guideline structure, support transparent inspection and modification, and generate patient-specific recommendations. These findings demonstrate the feasibility of scalable automated generation of computable clinical guidelines.

摘要：引言：NICE 指南提供基於證據的臨床護理建議，但仍然主要以非結構化的自然語言呈現。現有的將其轉換為可計算表示的方法通常集中於單一疾病，需大量手動編碼，且無法擴展。大型語言模型（LLMs）可能使這一翻譯過程的自動化成為可能。方法：我們提出了一種端到端的方法，將文本臨床指南轉換為可執行模型，能夠生成可解釋的、針對患者的具體建議。基於 LLM 的逐步轉換結合上下文示例，產生可供人類檢查的中間產物。我們將此方法應用於 NICE 胰臟癌和肺癌指南，並使用專家評審來評估規則的一致性，對 20 個患者小案例評估可執行的胰臟癌模型。結果：專家評審顯示源指南與生成的可執行模型之間有很強的一致性。大多數差異是部分省略，而非邏輯錯誤，幻覺或根本錯誤的規則則很少。在患者小案例中，可執行模型的 F1 分數達到 82.5%。結論：LLMs 可以將自然語言的 NICE 指南轉換為可解釋的、可執行的模型，保持指南結構，支持透明的檢查和修改，並生成針對患者的具體建議。這些發現展示了可擴展自動生成可計算臨床指南的可行性。

##### **Error Detection for PET/CT Radiology Reports: Domain-Specific vs Large Language Models**
2608.30021v1 by Hermione Warr, Harry Anthony, Lilli J Freischem, Yasin Ibrahim, Daniel R McGowan, Konstantinos Kamnitsas

Errors in radiology reports can adversely affect patient treatment, yet automated report quality assurance remains challenging because errors are often subtle and require domain expertise to detect. Although large language models (LLMs) have recently been proposed for radiology report verification, their ability to detect clinically meaningful errors beyond chest X-ray datasets remains under-explored. To this end, we present the first systematic evaluation of language models for PET/CT report error detection, comparing compact domain-specific models with SOTA open-weight LLMs. We collected 30,633 oncology FDG PET/CT reports from 23 radiologists over 10 years. We trained domain-specific BERT models to detect clinically motivated synthetic reporting errors and evaluated alongside zero-/few-shot Qwen3-32B, Gemma-3-27B and Llama-3.3-70B on a held-out benchmark of 11,500 reports. A 15M-parameter model achieved 94.4% balanced accuracy with a 5.8% false-positive rate, compared with 84.0% for the strongest prompted LLM. Task-specific adaptation of Llama-3.3-70B closed this performance gap (94.4%) but retained substantially greater computational requirements. Our results suggest that domain-specific training matters more than model scale for PET/CT report error detection, supporting compact models as an accurate and computationally efficient approach to automated radiology report quality assurance.

摘要：放射學報告中的錯誤可能會對患者治療產生不利影響，但自動化報告質量保證仍然面臨挑戰，因為錯誤往往是微妙的，並且需要領域專業知識來檢測。雖然最近提出了大型語言模型（LLMs）用於放射學報告驗證，但它們在檢測超越胸部X光數據集的臨床意義錯誤的能力仍然未被充分探索。為此，我們提出了針對PET/CT報告錯誤檢測的首個系統性評估，將緊湊的領域特定模型與最先進的開放權重LLMs進行比較。我們從23位放射科醫生那裡收集了10年內的30,633份腫瘤學FDG PET/CT報告。我們訓練了領域特定的BERT模型來檢測臨床動機的合成報告錯誤，並在11,500份報告的保留基準上與零/少量樣本的Qwen3-32B、Gemma-3-27B和Llama-3.3-70B進行評估。一個15M參數的模型實現了94.4%的平衡準確率，假陽性率為5.8%，相比之下，最強的提示LLM為84.0%。針對特定任務的Llama-3.3-70B適應縮小了這一性能差距（94.4%），但仍然保持了更高的計算需求。我們的結果表明，對於PET/CT報告錯誤檢測，領域特定訓練比模型規模更為重要，支持緊湊模型作為自動化放射學報告質量保證的準確且計算效率高的方法。

##### **Generating Clinical Vignettes that Preserve Cognitive Formulations**
2608.29995v1 by Amit Oren, Nimrod Hertz-Palmor, Dean Ariel, Guy Laban

Large language models can generate fluent clinical case vignettes, but fluency alone does not ensure fidelity to a specifiable clinical structure. We introduce FORMA, a theory-grounded framework that compiles a cognitive model of a disorder into a directed weighted graph, samples a person-specific configuration of that graph, and validates whether the generated vignette preserves the specified components and causal links. We instantiate FORMA on Posttraumatic Stress Disorder using the Ehlers and Clark cognitive model, generating 16,500 vignettes across 500 personas, 11 generation models, and three ablation conditions. Evaluation combines an external edge-recovery probe, two clinical experts, a scaled LLM judge, and a clinician user study with 100 licensed practitioners. The cognitive graph is recoverable from full-condition vignettes (MCC = +0.41, AUC = 0.70) but not from zero-shot generation (MCC = +0.01, AUC = 0.50). Experts rate full vignettes substantially higher than zero-shot alternatives, and clinicians perceive them to be human-written 85% of the time, compared with 22% for zero-shot. FORMA also reduces demographic disparity in perceived quality by 1.5-7x. These results show that cognitive formulation can serve as an auditable specification for scalable synthetic clinical text generation. A repository with the data and code is available online: https://github.com/Amit-Oren/FORMA.

摘要：大型語言模型可以生成流暢的臨床案例小品，但僅有流暢性並不能確保符合可指定的臨床結構。我們介紹了FORMA，一個基於理論的框架，將疾病的認知模型編譯成有向加權圖，並從該圖中抽樣出特定於個人的配置，然後驗證生成的小品是否保留了指定的組件和因果鏈。我們在創傷後壓力症上實現了FORMA，使用Ehlers和Clark的認知模型，生成了16,500個小品，涵蓋500個角色、11個生成模型和三個消融條件。評估結合了一個外部邊緣恢復探針、兩位臨床專家、一個擴展的LLM評審，以及一項包含100名持牌從業者的臨床用戶研究。從完整條件的小品中可以恢復認知圖（MCC = +0.41，AUC = 0.70），但從零樣本生成中則無法恢復（MCC = +0.01，AUC = 0.50）。專家對完整小品的評價顯著高於零樣本替代品，臨床醫生認為它們85%的時間是人類撰寫的，而零樣本則為22%。FORMA還將感知質量中的人口差異降低了1.5-7倍。這些結果表明，認知表述可以作為可審計的規範，用於可擴展的合成臨床文本生成。數據和代碼的存儲庫在線上可用：https://github.com/Amit-Oren/FORMA。

##### **Review Before Trust: Source-Grounded Integrity Gates for AI-Assisted Personal Health Records**
2608.29965v1 by Nora Girda, Adrian Groza

Large language models can convert medical documents into structured data, but plausible output may still be unsupported by the source. Persisting such output in a longitudinal health record, a record that accumulates patient information over time, therefore creates an integrity risk: unverified data may influence later summaries, trends, or preventive-care computations. We introduce an evidence-gated trust-promotion model that keeps generated data provisional until a deterministic monitor verifies it against the source document. The monitor admits a candidate for a specified downstream use only when the source contains a unique supporting quotation, the relevant fields occur within the same laboratory row, and the required provenance is preserved. The generator cannot approve its own output, missing or ambiguous evidence causes refusal, and refused candidates remain available for human review rather than being silently discarded. We implement the model in Medical DataCloud, a personal health-record application, and evaluate it through automated tests and a replay of saved extraction outputs. All 22 conformance and mutation tests pass. The replay covers nine historical laboratory PDF reports containing 102 manually labelled rows. The reports produce 97 numeric candidates: schema validation accepts all 97, an earlier packet-level evidence check accepts 94, and the hardened quotation- and row-level policy admits 72 while retaining 25 for review. The study evaluates system integrity rather than clinical correctness or clinical safety. The results demonstrate the technical feasibility of an enforceable boundary that prevents generated claims from authorizing their own reuse in a longitudinal health record.

摘要：大型語言模型可以將醫療文件轉換為結構化數據，但合理的輸出仍可能不受來源支持。因此，將這種輸出持續保留在縱向健康記錄中，即隨著時間累積患者信息的記錄，會產生完整性風險：未經驗證的數據可能影響後續的摘要、趨勢或預防護理計算。我們引入了一種證據門控的信任促進模型，該模型在確定性監控器驗證其與來源文件的對應關係之前，將生成的數據保持為臨時性。監控器僅在來源包含唯一支持引用、相關字段出現在同一實驗室行中，並且所需的來源保持不變時，才會接受候選項用於特定的下游用途。生成器不能批准自己的輸出，缺失或模糊的證據會導致拒絕，而被拒絕的候選項仍可供人類審查，而不是被靜默丟棄。我們在Medical DataCloud這個個人健康記錄應用中實施了該模型，並通過自動化測試和重播保存的提取輸出進行評估。所有22項符合性和變異測試均通過。重播涵蓋了九份包含102個手動標記行的歷史實驗室PDF報告。這些報告產生了97個數字候選項：模式驗證接受所有97個，早期的數據包級證據檢查接受94個，而加強的引用和行級政策接受72個，同時保留25個供審查。該研究評估系統的完整性，而非臨床正確性或臨床安全性。結果展示了一個可執行邊界的技術可行性，該邊界防止生成的聲明授權其在縱向健康記錄中的自我重用。

##### **INTERVenE: Temporal-Abstraction-Interval Based Transformers for Short-Horizon Medical Event Prediction**
2608.29901v1 by Shahar Oded, Yuval Shahar

Electronic Health Record (EHR) prediction models in the intensive care unit must learn from sparse and irregular measurements while preserving the clinical meaning of time and supporting transparent decision-making. We present INTERVenE, a family of Transformer architectures whose input is an interval-based, knowledge-based temporal abstraction (KBTA), a token stream of named clinical concepts (states, trends, events, contexts) drawn from a curated medical ontology, rather than an unnamed bin index or a raw measurement triplet. This naming layer is what we ask KBTA to do: it makes the model's per-token attributions resolve to clinical concepts by construction. INTERVenE offers two complementary variants: an auto-regressive decoder that generates future abstraction trajectories with a per-step risk readout (localizing \emph{when} and \emph{after which events} risk rises), and a bidirectional encoder for single-pass joint risk and time-to-event prediction. Evaluated on 57,078 MIMIC-IV admissions against GRU-D, STraTS, and KarmaLego, INTERVenE-Enc reaches a support-weighted AUPRC$_w$ of 0.672, improving by 0.041 over the strongest neural baseline with non-overlapping 95\% bootstrap CIs, while also taking the best AUROC$_w$ (0.901) and length-of-stay MAE (44.4\,h). INTERVenE-Ar (AUROC$_w$ $0.854$, AUPRC$_w$ $0.587$ under the same evaluation contract - a strictly harder generative readout) provides a complementary token-level risk trajectory. An input-representation ablation confirms the lift transfers across structured discretizations, positioning KBTA-based intervals as the interpretable substrate that makes per-token attributions resolve to meaningful clinical concepts within the deployed model.

摘要：電子健康紀錄（EHR）預測模型在重症監護病房必須從稀疏和不規則的測量中學習，同時保持時間的臨床意義並支持透明的決策制定。 我們提出了INTERVenE，一系列Transformer架構，其輸入是一種基於區間的知識基礎時間抽象（KBTA），這是一個來自策劃醫學本體的命名臨床概念（狀態、趨勢、事件、背景）的標記流，而不是未命名的箱索引或原始測量三元組。 這一命名層就是我們要求KBTA做的：它使模型的每個標記的歸因通過構造解析為臨床概念。 INTERVenE提供兩個互補的變體：一個自回歸解碼器，生成未來的抽象軌跡並提供每步的風險讀出（定位\emph{何時}和\emph{在什麼事件之後}風險上升），以及一個雙向編碼器，用於單次通過的聯合風險和事件時間預測。 在57,078個MIMIC-IV入院病例中，INTERVenE-Enc與GRU-D、STraTS和KarmaLego進行評估，達到支持加權的AUPRC$_w$為0.672，較最強的神經基線提高了0.041，且具有不重疊的95\%自助法置信區間，同時也獲得了最佳的AUROC$_w$（0.901）和住院時長MAE（44.4小時）。 INTERVenE-Ar（在相同評估合同下的AUROC$_w$ $0.854$，AUPRC$_w$ $0.587$ - 一個嚴格更難的生成讀出）提供了互補的標記級風險軌跡。 輸入表示的消融實驗確認了提升在結構化離散化之間的轉移，將基於KBTA的區間定位為可解釋的基底，使每個標記的歸因在部署的模型中解析為有意義的臨床概念。

##### **Life Operators: a self-evolving framework for multiscale life modelling**
2609.00068v1 by Shuo Wang, Yike Guo

Medical AI is moving beyond recognition towards clinical dialogue and longitudinal prediction. Yet a central question remains: how would a patient's state change under intervention? Statistical models learn future observations, whereas mechanistic models describe selected processes. Neither provides a common framework for representing patient state, coupling scales or revising failed assumptions. We propose Life Operators: task-bounded mappings that define three scientific roles. Perception operators infer task-relevant biological states from multimodal observations, Evolution operators propagate these states under natural or intervention-conditioned dynamics, and Generation operators map them to measurable signals. Each role may be realised by equations, statistical models, neural networks or hybrids. Bridge operators connect components with different variables, scales and time steps. Selected operators and bridges form task-specific Operator Graphs containing the smallest set of states and mechanisms sufficient for a declared claim. This modular structure also makes scientific revision localisable. An AI co-scientist may propose changes to states, operators, bridges or graph structure, while independent evidence determines which variants are retained, restricted or retired. Over time, validated components could accumulate into broader multiscale models of the human body and provide a computational foundation for medical artificial superintelligence.

摘要：醫療人工智慧正從識別轉向臨床對話和長期預測。<lb/>然而，一個核心問題仍然存在：在介入下，病人的狀態會如何改變？<lb/>統計模型學習未來的觀察，而機制模型描述選定的過程。<lb/>兩者都未提供一個共同的框架來表示病人狀態、耦合尺度或修正失敗的假設。<lb/>我們提出生命運算符：定義三個科學角色的任務界定映射。<lb/>感知運算符從多模態觀察中推斷與任務相關的生物狀態，<lb/>演化運算符在自然或介入條件下傳播這些狀態，<lb/>而生成運算符將它們映射到可測量的信號。<lb/>每個角色可以通過方程、統計模型、神經網絡或混合體來實現。<lb/>橋接運算符連接具有不同變量、尺度和時間步長的組件。<lb/>選定的運算符和橋接形成特定任務的運算符圖，包含足以支持所聲明主張的最小狀態和機制集。<lb/>這種模組化結構也使科學修訂可定位。<lb/>人工智慧共同科學家可以提出對狀態、運算符、橋接或圖結構的變更，<lb/>而獨立證據則決定哪些變體被保留、限制或淘汰。<lb/>隨著時間的推移，經過驗證的組件可以累積成更廣泛的人體多尺度模型，並為醫療人工超智慧提供計算基礎。

##### **Scientific Agent Skills: A Library of Procedural Knowledge for Research Agents**
2609.00065v2 by Timothy Kassis, Vinayak Agarwal, Yuhuan He, Darshil Patel, Aubrey M. Brueckner

A language-model agent asked to analyse an experiment will usually return working code. Whether the analysis is defensible is a different question. A defensible analysis depends on procedural choices: which test the field accepts, which identifier namespace is authoritative, and which caveats must accompany a result. We present Scientific Agent Skills, an open library of 163 such procedures in 16 areas of practice, including genomics, cheminformatics, medical imaging, study design and scientific communication. Each skill is a directory built around a versioned, human-readable instruction file. An agent loads the file only when a task calls for it; the directory often also contains reference material and runnable scripts. We report no task-level evaluation and no host selection rate. We measure two properties of the documentation corpus: the always-resident descriptions of all 163 skills cost 7.1% of a 200,000-token window, and the median documented workflow fits within 23.9% of it, although 29 of 46 would overflow if every reference file were loaded. Openly licensed and available at https://github.com/K-Dense-AI/scientific-agent-skills.

摘要：一個被要求分析實驗的語言模型代理通常會返回可運行的代碼。分析是否合理是另一個問題。合理的分析取決於程序選擇：該領域接受哪種測試、哪個標識符命名空間是權威的，以及結果必須伴隨哪些警告。我們介紹了科學代理技能，這是一個開放的庫，包含16個實踐領域中的163種程序，包括基因組學、化學信息學、醫學影像、研究設計和科學交流。每項技能都是圍繞一個版本化的人類可讀指令文件構建的目錄。代理僅在任務需要時加載該文件；目錄通常還包含參考材料和可運行的腳本。我們報告沒有任務級別的評估和沒有主機選擇率。我們測量文檔語料庫的兩個屬性：所有163項技能的常駐描述佔用200,000個標記窗口的7.1%，而中位數的文檔工作流程則適合其中的23.9%，儘管如果每個參考文件都被加載，46個中的29個將會溢出。該庫是開放授權的，並可在https://github.com/K-Dense-AI/scientific-agent-skills獲得。

##### **Medical Causal Hypothesis Verification with Large Language Models**
2609.00063v1 by Safiyyah Ahmed, Abrar Ansari, Md Aminul Islam, Elena Zheleva

The growing use of large language models (LLMs) for search and information retrieval underscores the need to evaluate their reliability in high-stakes domains such as healthcare. Although LLMs can effectively answer questions about diseases, symptoms, and treatments, their ability to accurately assess causal relationships and ground their conclusions in verified scientific evidence remains unclear. Here, we present a preliminary, small-scale study that investigates the accuracy of LLMs in evaluating causal medical claims and supporting them with peer-reviewed research. We propose an evaluation framework for causal hypothesis verification that can be used to systematically track the performance of existing and future LLMs. We assess the performance of eight LLMs on 17 medical causal hypotheses to evaluate whether they can reliably verify these hypotheses using scientific evidence from the literature. We systematically annotate the scientific evidence they provide according to six criteria (a total of 1,067 annotation points) and assess them with nine evaluation metrics. Our analysis shows that while LLMs exhibit strong recall, they often perform poorly at providing valid scientific articles and evidence for support and at rejecting unsupported hypotheses. These findings highlight a critical limitation of current LLMs, as they cannot yet be trusted fully to verify causal relationships from the biomedical literature. This work underscores the need for rigorous evaluation before using LLMs for search and retrieval in healthcare settings.

摘要：大型語言模型（LLMs）在搜尋和資訊檢索中的日益使用凸顯了在高風險領域（如醫療保健）評估其可靠性的必要性。雖然LLMs能有效回答有關疾病、症狀和治療的問題，但它們準確評估因果關係並將結論建立在經過驗證的科學證據上的能力仍不明確。在這裡，我們呈現一項初步的小型研究，調查LLMs在評估因果醫療主張及用同行評審研究支持這些主張方面的準確性。我們提出了一個因果假設驗證的評估框架，可以用來系統性地追蹤現有和未來LLMs的表現。我們評估了八個LLMs在17個醫療因果假設上的表現，以評估它們是否能可靠地使用文獻中的科學證據來驗證這些假設。我們根據六個標準系統性地註釋它們提供的科學證據（總共1,067個註釋點），並使用九個評估指標進行評估。我們的分析顯示，儘管LLMs表現出強大的召回率，但它們在提供有效的科學文章和證據以支持及拒絕不支持的假設方面經常表現不佳。這些發現突顯了當前LLMs的一個關鍵限制，因為它們尚未完全值得信賴以驗證生物醫學文獻中的因果關係。這項工作強調了在醫療保健環境中使用LLMs進行搜尋和檢索之前進行嚴格評估的必要性。

##### **Zero-Shot Respiratory Sound Classification through LLM-Augmented Audio-Text Alignment**
2609.00055v1 by Mustafa Talha İlerisoy, Hung Manh Pham, Mathias Funk, Mykola Pechenizkiy, Aaqib Saeed

Self-supervised respiratory encoders lack semantic grounding in clinical domain needed for zero-shot inference, limiting their utility without task-specific labeled data. We propose a framework that aligns these encoders with medical terminology in a shared latent space turning them into a zero-shot-capable foundation model. To address paired data scarcity, we use a medical LLM to synthesize structured reports from metadata, creating dense semantic anchors for contrastive learning. Our training combines a sigmoid-based contrastive loss with encoder's native SSL objective and similarity-aware negative sampling to sharpen pathological boundaries. Across 9 tasks on 6 datasets, our method achieves a 61.3% mean zero-shot AUC, surpassing CLAP (51.4%) and Qwen2-Audio (54.9%) while reaching the highest linear probing AUC (71.6%) with only 43% of data used by full-scale baselines, showing that structured semantic alignment outperforms large-scale, general-purpose models in clinical diagnostics.

摘要：自我監督的呼吸編碼器缺乏臨床領域所需的語意基礎，這限制了它們在沒有特定任務標記數據的情況下的實用性。我們提出了一個框架，將這些編碼器與醫學術語對齊，在共享潛在空間中將其轉變為具備零-shot 能力的基礎模型。為了解決配對數據稀缺的問題，我們使用醫學 LLM 從元數據合成結構化報告，為對比學習創建密集的語意錨點。我們的訓練結合了基於 sigmoid 的對比損失與編碼器的原生 SSL 目標，以及相似性感知的負採樣，以提升病理邊界的清晰度。在 6 個數據集上的 9 個任務中，我們的方法達到了 61.3% 的平均零-shot AUC，超越了 CLAP（51.4%）和 Qwen2-Audio（54.9%），同時在僅使用全規模基準的 43% 數據的情況下達到了最高的線性探測 AUC（71.6%），顯示出結構化語意對齊在臨床診斷中優於大規模通用模型。

##### **SUP-MIMIC: A Multi-Task Clinical Diagnosis Benchmark for Evaluating LLMs' Robustness to Contradictory Evidence**
2608.29582v1 by Yi Yu, Bo Wang, Chong Feng, Ge Shi, Xia Liu, Ziyi Yang, Xuewen Shi

Current evaluations of large language models (LLMs) primarily focus on factual knowledge retrieval, overlooking the fundamental challenge of navigating the complex, non-bijective mappings between clinical indicators and diagnoses. Existing benchmarks fail to assess whether large language models truly possess the reasoning capability required for diagnostic ambiguity scenarios, where identical clinical presentations may correspond to different etiologies, and diagnostic convergence scenarios, where heterogeneous symptoms ultimately indicate the same disease. To address this issue, we propose SUP-MIMIC, a multi-task framework utilizing MIMIC-IV-v3.1 that comprises Basic Assessment (BA), Diagnostic Divergence Task (DDT), and Diagnostic Convergence Task (DCT). Specifically, DDT is designed to evaluate the model's "one-to-many" disambiguation capability among phenotypically similar cases, while DCT assesses the model's ability to identify "many-to-one" diagnostic patterns across different pathophysiological pathways. Comprehensive evaluation of state-of-the-art LLMs reveals substantial performance degradation on DDT and DCT compared to baseline tasks, exposing a systemic reliance on statistical shortcuts over genuine causal reasoning. Our findings further highlight a conservative bias toward "healthy" predictions, implying non-trivial risks for missed diagnoses in realistic medical settings. This work establishes a rigorous methodology for quantifying clinical reasoning robustness and provides a roadmap for enhancing the safety of language models in clinical medicine.

摘要：目前對大型語言模型（LLMs）的評估主要集中在事實知識檢索上，忽略了在臨床指標與診斷之間導航複雜的非雙射映射的根本挑戰。現有的基準無法評估大型語言模型是否真正具備在診斷模糊情境中所需的推理能力，這種情境中相同的臨床表現可能對應於不同的病因，而診斷收斂情境中，異質症狀最終指向相同的疾病。為了解決這個問題，我們提出了SUP-MIMIC，一個利用MIMIC-IV-v3.1的多任務框架，包括基本評估（BA）、診斷分歧任務（DDT）和診斷收斂任務（DCT）。具體而言，DDT旨在評估模型在表型相似案例中的「一對多」消歧能力，而DCT則評估模型識別不同病理生理途徑中「多對一」診斷模式的能力。對最先進的LLMs進行的全面評估顯示，與基準任務相比，DDT和DCT的性能顯著下降，暴露出對統計捷徑的系統性依賴，而非真正的因果推理。我們的研究結果進一步突顯了對「健康」預測的保守偏見，這意味著在現實醫療環境中漏診的風險並不輕微。這項工作建立了一種嚴謹的方法論，用於量化臨床推理的穩健性，並提供了一個增強語言模型在臨床醫學中安全性的路線圖。

##### **AI Can Be Easily Persuaded in Clinical Decision Making**
2608.29453v1 by Jiayuan Zhu, Jiazhen Pan, Fenglin Liu, Minhao Hu, Junde Wu

As AI becomes increasingly integrated into clinical practice, it is playing a growing role in medical decision making. Medicine, however, is a high stakes and evidence based field, where decisions can directly affect patients' lives. It is therefore important to understand whether AI can maintain objective judgment when others try to persuade it. In this paper, we study how easily AI can be persuaded through controlled experiments. We find that professional authority, national background, institutional affiliation, claimed past performance, multiple physicians, supported clinician views, and repeated pressure can all affect AI decisions. Surprisingly, the same persuasive input changes about 10% more cases when it comes from a senior clinician than from a medical student. Simply claiming a better performance history consistently makes the physician more persuasive. More strikingly, a plausible clinician view can persuade AI away from a correct decision even when it is fabricated to support an incorrect answer. This indicates that AI can be strongly influenced by convincing support without reliably determining whether this view from the clinician is correct. Together, these findings suggest that AI can be easily persuaded by what people say, who says it, and how the opinion is presented. Therefore, it is essential for AI to maintain sound judgment under persuasion, enabling its safe and reliable use in high stakes medical decision making.

摘要：隨著人工智慧越來越多地融入臨床實踐，它在醫療決策中扮演著日益重要的角色。<lb/>然而，醫學是一個高風險且基於證據的領域，決策可能直接影響患者的生命。<lb/>因此，了解人工智慧在他人試圖說服時是否能保持客觀判斷是非常重要的。<lb/>在本文中，我們研究了人工智慧在受控實驗中被說服的難易程度。<lb/>我們發現，專業權威、國家背景、機構隸屬、聲稱的過往表現、多位醫師、支持臨床醫生的觀點以及重複的壓力都可以影響人工智慧的決策。<lb/>令人驚訝的是，當說服性輸入來自資深臨床醫生時，改變的案例比來自醫學生時多約10%。<lb/>僅僅聲稱有更好的表現歷史就能持續使醫生更具說服力。<lb/>更引人注目的是，即使一個合理的臨床觀點是捏造的以支持錯誤答案，也能使人工智慧偏離正確決策。<lb/>這表明，人工智慧可以受到令人信服的支持的強烈影響，而無法可靠地判斷這位臨床醫生的觀點是否正確。<lb/>綜合這些發現，表明人工智慧很容易受到人們所說的話、說話的人以及意見呈現方式的影響。<lb/>因此，對於人工智慧來說，在說服下保持良好的判斷能力至關重要，這使其能夠在高風險的醫療決策中安全可靠地使用。

##### **Scalable Clinical Data Infrastructure and Comparative ML Evaluation for Hospitalisation Risk Prediction in Elderly Patients with Multiple Long-Term Conditions using CPRD**
2608.29419v1 by Asra Aslam, Volodymyr Chapman, Maurice M. O'Connell, Aseel S. Abuzour, Michael Abaho, Danushka Bollegala, Gary Leeming, Eduard Shantsila, Andrew Clegg, Lauren E. Walker, Iain Edward Buchan, Samuel D. Relton

Deep learning architectures are increasingly proposed for patient trajectory modeling in electronic health records (EHRs), yet their advantage over simpler, more interpretable models is rarely subjected to rigorous empirical scrutiny in real-world clinical settings. We present a comprehensive patient timeline pipeline applied to elderly patients in CPRD Aurum, incorporating 260 clinical conditions classified via a three-tier automated framework including specialised detection logic for 17 complex conditions. Using this infrastructure, we benchmark Temporal Graph Convolutional Neural Networks (TG-CNN) against Logistic Regression with LASSO regularisation and Random Forests for predicting 12-month all-cause emergency hospitalisation risk, motivated by (but not filtered to) the elevated risk of adverse drug reactions. Under cross-validation, TG-CNN achieves a marginally higher mean AUC-ROC than LASSO (0.712 vs. 0.705), whereas on the held-out test set LASSO achieves the highest discrimination of three models (AUC-ROC 0.733, versus 0.710 for Random Forest and 0.702 for TG-CNN). We show, that discrimination alone is an incomplete criterion for clinical deployment: after Platt calibration, LASSO is the only model with an acceptable calibration slope (0.817), while Random Forest (0.759) and, TG-CNN (0.391) remain substantially miscalibrated. We argue that LASSO, not the highest-discriminating model, is the model best suited to direct clinical deployment. We present lessons for the machine learning and healthcare community regarding data infrastructure, model selection, and value of calibration and interpretability in high-stakes decision support.

摘要：深度學習架構越來越多地被提議用於電子健康紀錄 (EHRs) 中的患者軌跡建模，但它們相對於更簡單、更具可解釋性的模型的優勢在現實世界的臨床環境中很少受到嚴格的實證檢驗。我們提出了一個應用於 CPRD Aurum 中老年患者的綜合患者時間線管道，包含 260 種臨床狀況，這些狀況通過一個三層自動化框架進行分類，並包括對 17 種複雜狀況的專門檢測邏輯。利用這一基礎設施，我們將時間圖卷積神經網絡 (TG-CNN) 與帶 LASSO 正則化的邏輯回歸和隨機森林進行基準測試，以預測 12 個月的全因急診住院風險，這是基於（但不僅限於）不良藥物反應的升高風險。在交叉驗證下，TG-CNN 的平均 AUC-ROC 略高於 LASSO（0.712 對 0.705），而在保留的測試集上，LASSO 的三個模型中達到了最高的區分度（AUC-ROC 0.733，相較於隨機森林的 0.710 和 TG-CNN 的 0.702）。我們顯示，僅僅依賴區分度並不是臨床部署的完整標準：經過 Platt 校準後，LASSO 是唯一一個具有可接受校準斜率（0.817）的模型，而隨機森林（0.759）和 TG-CNN（0.391）仍然存在顯著的錯誤校準。我們主張，LASSO 而不是最高區分度的模型，是最適合直接臨床部署的模型。我們為機器學習和醫療保健社區提供了關於數據基礎設施、模型選擇以及在高風險決策支持中校準和可解釋性價值的教訓。

##### **Extending TotalSegmentator: Predicting Patient and Acquisition Characteristics from CT and MR Images**
2608.29348v1 by Jakob Wasserthal, Joshy Cyriac, Michael Bach, Kimia Mozahheb Yousefi, Minh-Son To, Máté Sik, Cédric Hémon, Thomas Weikert, Martin Segeroth

Background: Patient details and acquisition metadata are important for clinical decisions, image quality control, and automated research pipelines, but may be missing or unreliable in imaging archives.
  Purpose: To develop and evaluate a fast open-source model that predicts patient and acquisition characteristics directly from CT and MR images.
  Materials and Methods: Separate 3D ResNet-10 ensembles for CT and MR were trained on 57,291 and 43,200 clinical examinations acquired from 2011 to 2025. Both predicted weight, height, age, sex, contrast presence, vertebral coverage, and image noise. The CT model additionally predicted scanner manufacturer, tube voltage, tube current, convolution kernel, and post-injection time; the MR model predicted sequence class. Performance was evaluated on internal CT (n=501) and MR (n=636) test sets and an external CT dataset (n=54).
  Results: Internal CT MAEs were 3.90 kg, 3.68 cm, and 4.42 years for weight, height, and age, with sex F1=0.990; corresponding MR results were 4.34 kg, 4.62 cm, 7.13 years, and F1=0.970. The CNN outperformed a segmentation-derived XGBoost baseline for all four core targets in both modalities (adjusted P<=.042). F1 scores were 0.963 for CT contrast, 0.953 for MR sequence, and 0.823 for MR contrast. External CT MAEs were 4.45 kg, 4.05 cm, and 5.17 years, with sex F1=0.971. CPU inference required 20 seconds for CT and 12 seconds for MR.
  Conclusion: One 3D multitask model per modality can rapidly recover patient and acquisition characteristics from heterogeneous CT and MR examinations. Models are available in TotalSegmentator: https://github.com/wasserth/TotalSegmentator

摘要：背景：患者詳細資料和獲取元數據對於臨床決策、影像質量控制和自動化研究流程非常重要，但在影像檔案中可能缺失或不可靠。<lb/>目的：開發和評估一個快速的開源模型，能夠直接從 CT 和 MR 影像中預測患者和獲取特徵。<lb/>材料和方法：分別針對 CT 和 MR 訓練了 3D ResNet-10 的集成模型，使用了從 2011 年到 2025 年獲取的 57,291 和 43,200 例臨床檢查。<lb/>兩者都預測了體重、身高、年齡、性別、對比劑存在、脊椎覆蓋範圍和影像噪聲。<lb/>CT 模型還預測了掃描儀製造商、管電壓、管電流、卷積核和注射後時間；MR 模型預測了序列類別。<lb/>性能在內部 CT（n=501）和 MR（n=636）測試集以及外部 CT 數據集（n=54）上進行評估。<lb/>結果：內部 CT 的 MAE 分別為體重 3.90 kg、身高 3.68 cm 和年齡 4.42 年，性別 F1=0.990；相應的 MR 結果為 4.34 kg、4.62 cm、7.13 年，F1=0.970。<lb/>在兩種模式下，CNN 在所有四個核心目標上均超越了基於分割的 XGBoost 基線（調整後 P&lt;=.042）。<lb/>CT 對比的 F1 分數為 0.963，MR 序列為 0.953，MR 對比為 0.823。<lb/>外部 CT 的 MAE 分別為 4.45 kg、4.05 cm 和 5.17 年，性別 F1=0.971。<lb/>CPU 推理對於 CT 需要 20 秒，對於 MR 需要 12 秒。<lb/>結論：每種模式下的一個 3D 多任務模型可以快速從異質的 CT 和 MR 檢查中恢復患者和獲取特徵。<lb/>模型可在 TotalSegmentator 獲得：https://github.com/wasserth/TotalSegmentator

##### **Predicting Future Organ Dysfunction in ICU Patients Using Temporal Convolutional Networks on MIMIC-IV Data**
2608.29301v1 by Razan Albouq, Asra Aslam

Predicting future organ dysfunction in Intensive Care Unit (ICU) patients is critical for early clinical intervention, yet existing machine learning approaches have largely treated the Sequential Organ Failure Assessment (SOFA) score as an input to binary mortality prediction rather than as a continuous clinical outcome in its own right. We investigate the extent to which a Temporal Convolutional Net work (TCN) can predict next-day SOFA scores from multivariate ICU time-series data extracted from MIMIC-IV, characterise the relative contribution of each organ system to total SOFA variance and deterioration, and identify distinct trajectory patterns across ICU stays. A residual TCN trained on three-day sliding windows achieved a five-fold cross-validation R2 of 0.740 +- 0.013 and MAE of 1.431 +- 0.022, outperforming a naive persistence baseline on RMSE and R2. SHAP interpretability analysis revealed that the model functions primarily as a severity-anchoring mechanism rather than a true sequence model, with predictions dominated almost entirely by the most recent observation day. Cardiovascular dysfunction emerged as the strongest discriminator of both cross-sectional severity and acute deterioration, and unsupervised trajectory clustering identified two clinically meaningful phenotypes, an improving group (58.9%) and a persistently severe group (41.1%), differentiated by cardiovascular, hepatic, coagulation, and renal involvement. We conclude that TCNs can extract meaningful predictive signal from ICU physiological data, but that short input windows and complete-case selection bias currently limit their clinical utility, motivating future work on longer input horizons, alternative missing-data strategies, and external validation.

摘要：預測重症監護病房（ICU）患者未來的器官功能障礙對於早期臨床干預至關重要，然而現有的機器學習方法在很大程度上將序列器官功能評估（SOFA）分數視為二元死亡率預測的輸入，而不是作為一個持續的臨床結果。我們研究了時間卷積網絡（TCN）在多變量ICU時間序列數據中，從MIMIC-IV提取的數據中預測次日SOFA分數的程度，表徵每個器官系統對總SOFA變異和惡化的相對貢獻，並識別ICU住院期間的不同軌跡模式。基於三天滑動窗口訓練的殘差TCN達到了五倍交叉驗證R2為0.740 ± 0.013和MAE為1.431 ± 0.022，在RMSE和R2上超越了天真的持續基線。SHAP可解釋性分析顯示，該模型主要作為一種嚴重程度錨定機制，而不是一個真正的序列模型，預測幾乎完全由最近的觀察日主導。心血管功能障礙成為橫斷面嚴重程度和急性惡化的最強區分因子，而無監督的軌跡聚類識別出兩種臨床意義明確的表型，一個是改善組（58.9%），另一個是持續重度組（41.1%），兩者之間的區別在於心血管、肝臟、凝血和腎臟的參與。我們得出結論，TCN可以從ICU生理數據中提取有意義的預測信號，但短輸入窗口和完整案例選擇偏差目前限制了它們的臨床實用性，這促使未來在更長的輸入時間範圍、替代缺失數據策略和外部驗證方面進行進一步研究。

##### **AOI-Net: Structural Face AOI-Guided Eye-Gaze Track Representation Learning for Autism Spectrum Disorder Detection**
2608.29289v1 by Zhanpei Huang, Binbin Sun, Jialiang Chen, Yiou Wang, Taochen Chen, Yuzhu Ji, Yiqun Zhang, Yiu-Ming Cheung

Eye-movement tracking has emerged as a promising non-invasive approach to Autism Spectrum Disorder (ASD) screening, with systematic differences in attentional allocation and revisit behaviors observed during socially interactive tasks. Existing computational methods typically characterize eye-movements using discrete gaze trajectories and fixation events, yielding representations dominated by short-range temporal dynamics and limiting models that primarily emphasize long-range dependencies. Meanwhile, gaze behavior is naturally organized across semantically meaningful Areas of Interest (AOIs), whose attention allocation and transitions provide important structural cues, yet their relationships are rarely modeled explicitly. To address these limitations, we propose a structural face AOI-guided Eye-Gaze Track Network (AOI-Net) that jointly models short-term temporal dynamics and AOI-level structural organization. A network gating mechanism adaptively integrates the complementary temporal and structural representations according to their contributions to gaze-behavior characterization. To mitigate the pronounced class imbalance commonly encountered between individuals with ASD and Typically Developing (TD) participants in clinical datasets, class-distribution-aware learning is further employed to facilitate discriminative embedding learning under skewed class distributions. Experiments on a unique and large-scale clinical eye-tracking database comprising eight stimulus subsets and more than 1,300 participants show that AOI-Net consistently outperforms state-of-the-art methods. The proposed framework also enables interpretable gaze-behavior modeling and provides a practical basis for scalable AI-driven ASD screening in real-world healthcare. The code is available at https://github.com/Zhanpei-ai/CIM-AOI-Net/tree/main/Code

摘要：眼動追蹤已成為自閉症譜系障礙（ASD）篩檢的一種有前景的非侵入性方法，在社交互動任務中觀察到注意力分配和重訪行為的系統性差異。現有的計算方法通常使用離散的注視軌跡和凝視事件來描述眼動，產生的表徵主要受短期時間動態的主導，限制了主要強調長期依賴性的模型。與此同時，凝視行為自然地在語義上有意義的關注區域（AOIs）中組織，其注意力分配和轉換提供了重要的結構線索，但這些關係很少被明確建模。為了解決這些限制，我們提出了一種結構性面部AOI引導的眼動追蹤網絡（AOI-Net），該網絡聯合建模短期時間動態和AOI級別的結構組織。一個網絡閘控機制根據其對凝視行為特徵化的貢獻，自適應地整合互補的時間和結構表徵。為了減輕臨床數據集中自閉症患者與典型發展（TD）參與者之間常見的明顯類別不平衡，進一步採用了類別分佈感知學習，以促進在偏斜類別分佈下的區別性嵌入學習。對一個獨特且大規模的臨床眼動追蹤數據庫進行的實驗，該數據庫包含八個刺激子集和超過1,300名參與者，顯示AOI-Net始終優於最先進的方法。所提出的框架還使得可解釋的凝視行為建模成為可能，並為現實世界醫療中可擴展的AI驅動ASD篩檢提供了實用基礎。代碼可在 https://github.com/Zhanpei-ai/CIM-AOI-Net/tree/main/Code 獲得。

##### **Not the Same Protector: Deployment-Dependent Protective Intervention in LLMs**
2608.29136v1 by Eunna Lee, Soomyoung Lee, Jungpyo Nam, Heonjin Ha, Jamin Jung, Kyunam Choi, Sunjun Hwang, Yeonghun Kim, Seok-Jae Lim

We ask whether a model protects a user in the same way when that user speaks rather than types. Using a single distress vignette---a physical injury of unstated severity following an interpersonal conflict---we present four frontier models with matched inputs across voice, text, and raw API deployment conditions (n=30 per cell) and code each response along five binary protective indicators, including whether the model issues an explicit medical-care directive. Voice-interface responses are markedly shorter than text-interface responses for three of the four models, and protective behavior contracts alongside that compression: medical directives are at ceiling under both the API and text conditions but decline under voice for every model tested. The contraction is not reducible to length. One model produces voice and text responses of comparable length yet still drops medical directives, and another falls below ceiling between its API and voice conditions, whose responses are of nearly identical length. Under raw API access the pattern is categorical rather than partial: no model asks after the user's safety even once. These results show that protective intervention is sensitive to the surface through which a request arrives, that this sensitivity is detectable using a simple protective coding scheme, and that it is not explained by turn length alone.

摘要：我們詢問當用戶說話而不是打字時，模型是否以相同的方式保護用戶。使用一個單一的困擾情境——在一場人際衝突後發生的未明嚴重程度的身體傷害——我們展示了四個前沿模型，並在語音、文本和原始API部署條件下匹配輸入（每個單元n=30），並根據五個二元保護指標對每個回應進行編碼，包括模型是否發出明確的醫療護理指令。在四個模型中，語音介面的回應顯著比文本介面的回應短，而保護行為隨著這種壓縮而減少：在API和文本條件下，醫療指令達到上限，但在語音條件下對每個測試的模型均有所下降。這種收縮無法簡單歸因於長度。一個模型產生的語音和文本回應長度相當，但仍然未發出醫療指令，另一個模型在其API和語音條件之間的表現低於上限，這兩者的回應長度幾乎相同。在原始API訪問下，這種模式是類別性的而非部分性的：沒有模型甚至一次也未詢問用戶的安全性。這些結果顯示，保護性干預對請求到達的表面敏感，這種敏感性可以通過簡單的保護編碼方案檢測到，並且並非僅僅由回合長度解釋。

##### **Beyond Correctness: Validity-Oriented Evaluation of Biomedical LLM Judges**
2608.29127v1 by Rodrigo de Oliveira, Federico Pittino, James Gwinnutt, Jay Nanavati

We propose a scalable, validity-oriented pipeline for evaluating biomedical LLM judges when high-quality human judgments are scarce. First, we augment existing human-labelled biomedical benchmarks with deterministic, metric-grounded mutations that produce auditable preference pairs. Second, we evaluate judges beyond aggregate correctness using three deployment-relevant dimensions: correctness against metric-derived gold labels, robustness under repeated stochastic sampling, and compliance with the requested output format. We use this pipeline to assess Llama-3.1-8B-Instruct under four regimes: (1) base, using the instruct model as is; (2) SFT, distillation-based supervised fine-tuning only; (3) RL, GRPO-based reinforcement learning only; and (4) SFT$\rightarrow$RL, SFT followed by RL. The base and single-stage regimes struggle on structured medical discrimination such as PICO extraction and clinical calculations, whereas SFT$\rightarrow$RL performs best across correctness, compliance, and robustness; gains concentrate on decomposable tasks (PICO, MedCalc), at times matching or outperforming frontier models.

摘要：我們提出了一個可擴展的、以有效性為導向的管道，用於評估生物醫學 LLM 評判者，當高質量的人類判斷稀缺時。<lb/>首先，我們通過確定性的、基於度量的變異來增強現有的人類標記生物醫學基準，這些變異生成可審計的偏好對。<lb/>其次，我們在三個與部署相關的維度上評估評判者，而不僅僅是聚合正確性：對基於度量的金標籤的正確性、在重複隨機抽樣下的穩健性，以及對所要求輸出格式的遵從性。<lb/>我們使用這個管道在四種模式下評估 Llama-3.1-8B-Instruct： (1) 基本模式，直接使用指令模型； (2) SFT，基於蒸餾的監督微調； (3) RL，僅基於 GRPO 的強化學習； (4) SFT$\rightarrow$RL，SFT 隨後進行 RL。<lb/>基本模式和單階段模式在結構化醫療區分（如 PICO 提取和臨床計算）方面表現不佳，而 SFT$\rightarrow$RL 在正確性、遵從性和穩健性方面表現最佳；增益集中在可分解的任務上（PICO、MedCalc），有時與前沿模型相匹配或超越。

##### **Disentangling Representation using Attributes-based Gaussian Estimation for Medical Sound Diagnosis**
2608.29026v1 by Ke Zhao

Deep learning has a powerful capability of feature extraction. However, the lack of fairness and interpretability in deep neural networks poses limitations to their adoption in the medical domain. This paper proposes a disentangled representation learning (DisenRL) framework, named the Attributes-based Gaussian Estimation for Disentangled Representation (AGEDR), which incorporates Attribute Mapping Embedding (AME) modules designed to map attributes into vectors and align them with a subset of the latent vectors in a Variational AutoEncoder (VAE). This part of the latent vector will be disentangled from the remaining latent vectors by minimizing mutual information. A classifier is then trained using the mean parameters of the latent vectors from the VAE. Extensive experiments demonstrate that AGEDR outperforms both conventional classification models and existing disentangled representation learning methods. The ablation experiments also indicate the disentangling capability and fairness of AGEDR. The source code is publicly available at https://github.com/ZhaoKe1024/DisentangledRepr.

摘要：深度學習具有強大的特徵提取能力。<lb/>然而，深度神經網絡缺乏公平性和可解釋性，這對其在醫療領域的應用造成了限制。<lb/>本文提出了一種解耦表示學習（DisenRL）框架，名為基於屬性的高斯估計解耦表示（AGEDR），該框架結合了屬性映射嵌入（AME）模塊，旨在將屬性映射為向量並將其與變分自編碼器（VAE）中的一部分潛在向量對齊。<lb/>這部分潛在向量將通過最小化互信息從其餘潛在向量中解耦。<lb/>然後使用來自VAE的潛在向量的均值參數訓練分類器。<lb/>大量實驗表明，AGEDR在性能上超越了傳統分類模型和現有的解耦表示學習方法。<lb/>消融實驗還表明了AGEDR的解耦能力和公平性。<lb/>源代碼可在 https://github.com/ZhaoKe1024/DisentangledRepr 上公開獲得。

##### **Frequency Selective Neural Networks as a Foundation Architecture for Time Series Learning**
2608.29012v1 by Hui Huang, Ye Sun, Shiyan Hu

Time-series data across physical and biological domains are fundamentally driven by complex, non-stationary oscillatory modes. While deep learning models, such as Convolutional Neural Networks (CNNs), Recurrent Neural Networks, and Transformers, have dominated sequential analysis, they remain fundamentally "spectral-blind". By mapping continuous physical waves into unconstrained spatial or discrete token spaces, these architectures suffer from severe spectral entanglement, acting as opaque black boxes that decouple predictive accuracy from physical reality. In this paper, we introduce the Frequency Selective Neural Network (FSNN), pioneering a foundation architecture guaranteeing physical interpretability without sacrificing expressive power of deep learning. FSNN addresses spectral entanglement by explicitly embedding the rigorous mathematics of advanced signal processing into its neural topology. Through a fully differentiable Wiener-like filter bank optimized via complex-domain backpropagation, FSNN autonomously discovers and isolates the precise physical modes of a given task. Extensive evaluations demonstrate that FSNN establishes state-of-the-art predictive performance, achieving $77.0\%$ average accuracy on the standard 10 multivariate UEA datasets and leading across all major metrics on the highly imbalanced PTB-XL clinical ECG benchmark. Crucially, in contrast to yielding abstract feature maps, FSNN converges directly on physically meaningful frequency bands, such as isolating the cardiac QRS complex, providing a highly scalable, interpretable paradigm for robust pattern recognition in complex temporal domains. Our code is available at: https://github.com/ad6174hhhh/FSNN.

摘要：時間序列數據在物理和生物領域的根本驅動因素是複雜的、非平穩的振盪模式。<lb/>雖然深度學習模型，如卷積神經網絡（CNN）、循環神經網絡和Transformer，在序列分析中占據主導地位，但它們仍然根本上是「頻譜盲」的。<lb/>通過將連續的物理波映射到不受限制的空間或離散標記空間，這些架構遭受嚴重的頻譜糾纏，表現為不透明的黑箱，將預測準確性與物理現實脫鉤。<lb/>在本文中，我們介紹了頻率選擇神經網絡（FSNN），開創了一種基礎架構，保證物理可解釋性而不犧牲深度學習的表達能力。<lb/>FSNN 通過明確嵌入先進信號處理的嚴謹數學來解決頻譜糾纏問題。<lb/>通過一個完全可微的類維納濾波器組，通過複數域反向傳播進行優化，FSNN 自主發現並隔離給定任務的精確物理模式。<lb/>廣泛的評估顯示，FSNN 建立了最先進的預測性能，在標準的 10 個多變量 UEA 數據集上達到 $77.0\%$ 的平均準確率，並在高度不平衡的 PTB-XL 臨床 ECG 基準上在所有主要指標中領先。<lb/>至關重要的是，與產生抽象特徵圖相反，FSNN 直接收斂於物理上有意義的頻帶，如隔離心臟 QRS 複合波，提供了一種高度可擴展、可解釋的範式，用於在複雜的時間領域中進行穩健的模式識別。<lb/>我們的代碼可在以下網址獲得：https://github.com/ad6174hhhh/FSNN。

##### **From Analytics to Tumor Boards: An Evidence-Linked Multi-Agent Workflow for Oncology Feature Extraction**
2608.28974v2 by Daniel Kang, Michelle Hu, Soorya Ram Shimgekar, Shayan Vassef, Yufan Wang, Anit Kumar Sahu, Munmun De Choudhury, Vedant Das Swain, Christian Poellabauer, Li Yan Khor, Koustuv Saha, Robert Wojciechowski, Elliot Kidd, Piyum Zonooz, Navin Kumar

Clinically relevant oncology information is distributed across heterogeneous, longitudinal documentation, creating substantial abstraction burden and requiring accurate attribution across specimens, tumors, biomarkers, and time points, while manual cancer-registry abstraction can require 27.2 minutes per case, highlighting the need for scalable methods that preserve clinical context while converting documentation into structured data. We evaluate an oncology information-extraction workflow in which OncoLens supplies multi-source, oncology-aware document selection, aggregation, and normalization from integrated EHRs, while the NimbleMind Multi-Agent System (nMAS) is a configurable oncology information-extraction workflow that extracts clinically relevant structured fields from fragmented oncology documentation. The extraction task uses a clinician-informed schema of 328 attributes spanning report metadata, diagnosis, staging, and cancer-type-specific information. nMAS separates clinician-defined field specifications from model execution and combines complexity-aware extraction, report-level consolidation, and source-grounded validation. The retrospective evaluation included 230 de-identified oncology documents from 40 patients and 418 clinician-reviewed document-field pairs containing 1,126 non-empty reference values. Evaluation focused on fields identified by clinicians as present in the source documents rather than exhaustively annotating all 328 schema fields. nMAS achieved a rank-weighted value-level precision of 82.6%, recall of 87.5%, and F1 of 85.0%, compared with an F1 of 66.4% for an independently implemented UMA-style MiniMax M2.5 comparator. These findings support the feasibility of using a configurable, source-grounded extraction workflow to convert fragmented oncology documentation into reusable structured data.

摘要：臨床相關的腫瘤學資訊分散在異質的、長期的文檔中，造成了相當大的抽象負擔，並需要在標本、腫瘤、生物標記和時間點之間進行準確的歸屬，而手動癌症登記抽象每個案例可能需要27.2分鐘，突顯了需要可擴展的方法來保持臨床背景，同時將文檔轉換為結構化數據。我們評估了一個腫瘤學資訊提取工作流程，其中 OncoLens 提供來自整合電子健康記錄的多來源、腫瘤學意識的文檔選擇、聚合和正規化，而 NimbleMind 多代理系統 (nMAS) 是一個可配置的腫瘤學資訊提取工作流程，從零散的腫瘤學文檔中提取臨床相關的結構化字段。提取任務使用了一個由臨床醫生告知的328個屬性的架構，涵蓋報告元數據、診斷、分期和特定癌症類型的信息。nMAS 將臨床醫生定義的字段規範與模型執行分開，並結合了複雜性意識的提取、報告級別的整合和基於來源的驗證。回顧性評估包括來自40名患者的230份去識別化的腫瘤學文檔和418對經臨床醫生審查的文檔字段對，包含1,126個非空的參考值。評估重點放在臨床醫生認為存在於源文檔中的字段，而不是對所有328個架構字段進行全面註釋。nMAS 實現了82.6%的排名加權值級精確度、87.5%的召回率和85.0%的F1值，相較於獨立實施的 UMA 風格 MiniMax M2.5 比較器的F1值66.4%。這些發現支持使用可配置的、基於來源的提取工作流程將零散的腫瘤學文檔轉換為可重用的結構化數據的可行性。

##### **From the Loss Landscape to Diverse Feature Learning in Neural Networks**
2608.28948v1 by David Aram Yunis

Over the course of the last decade, neural networks have grown from an academic curiosity to moving the markets of nations. Despite this explosion in both research and deployment, relatively little is understood about how they achieve the solutions they do. This is both scientifically relevant, and pressing for society. When neural networks make decisions across self-driving, construction, law, hiring and health, there have been and will continue to be unintended consequences.
  However, attempting to generalize the failures of the largest and most important production systems makes for a very difficult task. Yet signs of these failures exist at all scales of neural networks, so we should be able to study a much more tractable setting. All neural networks must undergo an optimization process, called training, to be useful. To a great degree, understanding neural networks is understanding their optimization: through what process and exposure to which data did they arrive at their results. Yet our knowledge on this topic as a field is quite imprecise. In particular, a curious phenomenon called mode connectivity, the ability to connect neural networks in the loss surface, defies explanation entirely.
  This dissertation elucidates, explains and exploits this special structure in the loss landscape...

摘要：在過去十年中，神經網絡從學術上的好奇心成長為影響國家市場的力量。儘管在研究和應用方面都出現了爆炸性的增長，但對於它們如何達成所謂的解決方案，仍然了解得相對較少。這對科學來說既相關，也對社會來說十分迫切。當神經網絡在自駕、建設、法律、招聘和健康等領域做出決策時，已經出現並將繼續出現意想不到的後果。<lb/>然而，試圖概括最大的和最重要的生產系統的失敗是一項非常困難的任務。儘管如此，這些失敗的跡象在所有規模的神經網絡中都存在，因此我們應該能夠研究一個更易於處理的環境。所有神經網絡都必須經歷一個稱為訓練的優化過程，才能變得有用。在很大程度上，理解神經網絡就是理解它們的優化：它們是通過什麼過程以及接觸哪些數據達成結果的。然而，作為一個領域，我們在這個主題上的知識相當不精確。特別是一種被稱為模式連通性的奇特現象，即在損失表面上連接神經網絡的能力，完全無法解釋。<lb/>這篇論文闡明、解釋並利用了損失景觀中的這一特殊結構...

##### **ActiveAugment: Online Active Learning for Augmentation Selection in Deep Learning**
2608.28923v1 by Noah Videcrantz, Mostafa Mehdipour Ghazi

Data augmentation is a cornerstone of deep learning pipelines, yet existing strategies treat it as a static, model-agnostic preprocessing step, either relying on expensive dataset-specific policy search or applying transformations uniformly at random, regardless of what the model has already learned. We introduce ActiveAugment, a unified framework that treats augmentation selection as an online active learning problem. For each training minibatch, ActiveAugment generates a pool of candidate augmented views and scores each candidate using a combination of the model's predictive uncertainty and the feature discrepancy induced by the augmentation. The augmentation under which the current model is most fragile is selected per sample, and the model is then trained with a joint supervised classification and supervised contrastive objective that enforces intra-class invariance to the selected augmentations while maintaining inter-class separation. We evaluate ActiveAugment on eight benchmark datasets spanning natural and medical imaging, using CNN and transformer architectures across three training regimes (training from scratch, full fine-tuning, and linear probing), and comparing eight active selection strategies for augmentation scoring. ActiveAugment outperforms AutoAugment, RandAugment, and TrivialAugment under controlled augmentation shifts across all domains and budgets, with the most pronounced gains at low labelling budgets. On medical imaging datasets, where data is scarce and domain shift relative to natural-image pretrained models is large, ActiveAugment achieves higher test F1 than all baselines, demonstrating strong cross-domain adaptability. Our analysis reveals that the augmentation selection policy evolves meaningfully during training and that strategy choice has a direct impact on generalisation. Code is available at: https://github.com/noahvide/ActiveAugment.

摘要：數據增強是深度學習流程的基石，但現有策略將其視為靜態的、與模型無關的預處理步驟，要麼依賴於昂貴的數據集特定策略搜尋，要麼隨機均勻地應用變換，而不考慮模型已經學到的內容。<lb/>我們介紹了ActiveAugment，一個統一的框架，將增強選擇視為一個在線主動學習問題。<lb/>對於每個訓練小批量，ActiveAugment生成一組候選增強視圖，並使用模型的預測不確定性和增強所引起的特徵差異的組合來評分每個候選者。<lb/>每個樣本選擇當前模型最脆弱的增強，然後模型使用一個聯合的監督分類和監督對比目標進行訓練，該目標強制對所選增強保持類內不變性，同時保持類間分離。<lb/>我們在八個基準數據集上評估ActiveAugment，這些數據集涵蓋自然影像和醫學影像，使用CNN和Transformer架構，並在三種訓練模式（從頭開始訓練、完全微調和線性探測）下比較八種增強評分的主動選擇策略。<lb/>ActiveAugment在所有領域和預算下的受控增強轉變中超越了AutoAugment、RandAugment和TrivialAugment，並在低標註預算下獲得了最明顯的增益。<lb/>在醫學影像數據集上，數據稀缺且相對於自然影像預訓練模型的領域轉變較大，ActiveAugment的測試F1分數高於所有基準，顯示出強大的跨領域適應性。<lb/>我們的分析顯示，增強選擇策略在訓練過程中有意義地演變，且策略選擇對泛化有直接影響。<lb/>代碼可在以下網址獲得：https://github.com/noahvide/ActiveAugment。

##### **Explainable Artificial Intelligence (XAI) in Computational Pathology: Definitions, Taxonomy, and Recommendations**
2608.28820v1 by Shubham Innani, Suhang You, Adam Shephard, Bhakti Baheti, Francesco Ciompi, Joe Yeong, Nasir Rajpoot, Michael Feldman, Solene Florence Kammerer-Jacquet, Dimitrios Makris, Geert Litjens, Anne L. Martel, Jana Lipkova, April Khademi, Spyridon Bakas, for the MICCAI SIG-CompPath

Computational pathology (CompPath) is transforming medicine by leveraging artificial intelligence (AI) algorithms to support diagnosis, prognosis, and treatment prediction from gigapixel whole-slide images. Clinical adoption is progressing, but is constrained by concerns about safety, accountability, and regulatory oversight in high-stakes clinical environments. Explainable AI (XAI) systems hold promise for building trust and enabling verification, yet the literature remains fragmented due to inconsistent terminology, overlapping methodological families, ad hoc validation, and current reviews. This review aims to formalize XAI methods in CompPath through the: i) introduction of a pathology-centric vocabulary comprising seven core terms; ii) development of a taxonomy across methodological families and three orthogonal axes (stage, type, scope); and iii) establishment of a task-driven framework that maps five clinical questions to recommended methods, method evaluation, and deployment context. Five key gaps between current XAI capabilities and clinical deployment are identified, and actionable steps are proposed to advance XAI for CompPath.

摘要：計算病理學（CompPath）正在通過利用人工智慧（AI）算法來支持從千兆像素全幻燈片圖像中進行診斷、預後和治療預測，徹底改變醫學。臨床採用正在進行，但受到對安全性、責任和在高風險臨床環境中監管監督的擔憂所限制。可解釋的人工智慧（XAI）系統在建立信任和實現驗證方面具有潛力，但由於術語不一致、方法論家族重疊、臨時驗證以及當前的評估文獻，文獻仍然支離破碎。本次綜述旨在通過以下方式正式化CompPath中的XAI方法：i）引入由七個核心術語組成的以病理為中心的詞彙；ii）在方法論家族和三個正交軸（階段、類型、範圍）之間發展一個分類法；以及iii）建立一個以任務為驅動的框架，將五個臨床問題映射到推薦的方法、方法評估和部署背景。確定了當前XAI能力與臨床部署之間的五個關鍵差距，並提出可行的步驟以推進CompPath的XAI。

##### **Texture Image Classification Using DWT AlexNet Feature Fusion and Deep Neural Networks**
2608.28524v1 by Arun D. Kulkarni

Texture image classification plays a significant role in computer vision applications, including industrial inspection, medical image analysis, remote sensing, and object recognition. Handcrafted features can capture local texture characteristics but may have limited capability to represent complex visual patterns. In contrast, deep learning models automatically learn discriminative representations but may not fully exploit the multiscale spatial-frequency information inherent in texture images. This paper proposes a hybrid feature fusion framework, termed DWT_AlexNet_DNN, which combines Discrete Wavelet Transform (DWT) features with deep features extracted using AlexNet for texture image classification.

摘要：紋理圖像分類在計算機視覺應用中扮演著重要角色，包括工業檢測、醫學影像分析、遙感和物體識別。手工設計的特徵可以捕捉局部紋理特徵，但可能在表示複雜視覺模式方面能力有限。相對而言，深度學習模型自動學習區分性表示，但可能未能充分利用紋理圖像中固有的多尺度空間頻率信息。本文提出了一種混合特徵融合框架，稱為 DWT_AlexNet_DNN，該框架將離散小波變換（DWT）特徵與使用 AlexNet 提取的深度特徵結合，用於紋理圖像分類。

##### **PermitGPT: A Unified Generative-AI Pipeline for Construction Hazard Forecasting, Permit Prediction, and Community Impact**
2608.28728v1 by Mohd Ruhul Ameen, Farjana Aktar, Akif Islam, Momen Khandoker Ope, Abu Saleh Musa Miah, Jungpil Shin

Urban construction governance requires early decisions that connect workplace safety, permitting requirements, and community impact, yet the relevant evidence is often scattered across separate municipal and regulatory data sources. This paper presents PermitGPT, a unified generative artificial intelligence framework for converting unstructured construction permit descriptions into structured decision-support outputs across three domains: safety hazard identification, permit requirement specification, and community impact assessment. To address data fragmentation, we spatially and temporally align records from the New York City Department of Buildings, Occupational Safety and Health Administration, and NYC 311 service requests, producing 90,000 structured prompt-response pairs derived through rule-based alignment and domain-informed spot checking. We fine-tune three open-weight language models using parameter-efficient adaptation and evaluate them on 2,833 held-out test cases. The results show complementary model behavior: Gemma-3-1B provides the most efficient inference at 3.07 samples per second with low memory usage, Llama-3.2-3B gives the highest lexical overlap for regulatory-style outputs with a BLEU score of 0.0091, and 4-bit Mistral-7B-Instruct-v0.3 achieves the strongest semantic alignment with a BERTScore-F1 of 0.7747. Because the task involves open-ended structured generation, low BLEU values are interpreted alongside semantic metrics and qualitative output structure rather than as standalone indicators of utility. Overall, PermitGPT provides an initial step toward AI-assisted construction governance while identifying directions for stronger task-level evaluation and real-world validation.

摘要：城市建設治理需要早期決策，以連結工作場所安全、許可要求和社區影響，然而相關證據往往散佈在不同的市政和監管數據來源中。本文提出了PermitGPT，一個統一的生成式人工智慧框架，用於將非結構化的建設許可描述轉換為三個領域的結構化決策支持輸出：安全危害識別、許可要求規範和社區影響評估。為了解決數據碎片化的問題，我們從紐約市建築部、職業安全健康管理局和NYC 311服務請求中空間和時間上對齊記錄，通過基於規則的對齊和領域知識的抽查產生了90,000對結構化的提示-回應對。 我們使用參數高效的適應對三個開放權重語言模型進行微調，並在2,833個保留測試案例上進行評估。結果顯示出互補的模型行為：Gemma-3-1B在每秒3.07個樣本的低記憶體使用率下提供了最高效的推理，Llama-3.2-3B對於監管風格的輸出提供了最高的詞彙重疊，BLEU分數為0.0091，而4-bit Mistral-7B-Instruct-v0.3則以0.7747的BERTScore-F1達到最強的語義對齊。由於該任務涉及開放式結構生成，低BLEU值應與語義指標和定性輸出結構一起解釋，而不是作為效用的獨立指標。總體而言，PermitGPT為AI輔助建設治理提供了一個初步步驟，同時確定了更強的任務級評估和現實世界驗證的方向。

##### **ARC-CT: Anatomy-Routed Contrastive Vision-Language Learning for 3D Chest CT**
2608.28455v1 by Huseyin Umut Isik, Mehmet Alp Ozaydin, Sila Kurugol, Şeyda Ertekin

Contrastive vision-language learning uses paired chest CT volumes and radiology reports to learn abnormality classifiers without manually annotated labels. However, two characteristics of chest CT challenge conventional global contrastive learning. First, many critical abnormalities are small or anatomically localized, and pooling an en- tire volume into a single embedding may dilute their visual evidence. Second, the standard contrastive objective treats every other scan in a batch as a negative. Because many chest CTs share abnormalities, this objective incorrectly pushes co-positive pairs apart. We propose Anatomy-Routed Contrastive Learning for 3D Chest CT (ARC-CT), a region-aware framework that addresses these limitations using only la- bels extracted from reports by an LLM, with no manual annotations or bounding boxes. ARC-CT combines three components: (1) an Anato- myQFormer localizing evidence via queries constrained by automatically generated organ masks; (2) a label-Jaccard soft InfoNCE objective in- tegrating the standard one-hot target with the label-set overlap of each pair, which reduces false-negative penalties between studies that share clinical findings; and (3) an organ-level alignment loss connecting mask- pooled visual features to organ-specific report text extracted offline with a large language model. ARC-CT achieves a 0.86 mask-free macro AUC across 18 abnormalities using a compact 3D ResNet-18 backbone. Over- all, ARC-CT outperforms both comparable efficient baselines and sev- eral larger transformer models. Our code and weights are available at https://github.com/arc-ct/arc-ct.

摘要：對比視覺-語言學習使用配對的胸部 CT 體積和放射學報告來學習異常分類器，而不需要手動標註的標籤。<lb/>然而，胸部 CT 的兩個特徵對傳統的全局對比學習提出了挑戰。<lb/>首先，許多關鍵的異常是小型或解剖學上局部化的，將整個體積匯聚成單一嵌入可能會稀釋它們的視覺證據。<lb/>其次，標準的對比目標將批次中的每個其他掃描視為負樣本。<lb/>由於許多胸部 CT 共享異常，這一目標不正確地將共同正樣本推開。<lb/>我們提出了 3D 胸部 CT 的解剖路由對比學習（ARC-CT），這是一個區域感知框架，利用從報告中提取的標籤來解決這些限制，無需手動標註或邊界框。<lb/>ARC-CT 結合了三個組件：<lb/>(1) 一個通過自動生成的器官掩膜約束的查詢本地化證據的 AnatomyQFormer；<lb/>(2) 一個標籤-Jaccard 軟 InfoNCE 目標，將標準的一次性目標與每對的標籤集重疊整合，減少在共享臨床發現的研究之間的假陰性懲罰；<lb/>(3) 一個器官級對齊損失，將掩膜匯聚的視覺特徵連接到離線提取的器官特定報告文本，該文本使用大型語言模型。<lb/>ARC-CT 在 18 種異常上實現了 0.86 的無掩膜宏 AUC，使用緊湊的 3D ResNet-18 主幹。<lb/>總的來說，ARC-CT 超越了可比較的高效基準和幾個較大的Transformer模型。<lb/>我們的代碼和權重可在 https://github.com/arc-ct/arc-ct 獲得。

##### **AI as Teammate: Rethinking Task Distribution in Medical Training**
2608.28373v1 by Fendi Tsim, Alina Gutoreva, Anthony Weiss, Nicole Dubosh

Integrating Artificial Intelligence (AI), particularly generative AI, into medical training has prompted concerns about learner over-reliance, misuse, and erosion of foundational clinical competencies. We propose a conceptual reframing at the decision level: the problem is not misuse but misclassification - a mechanistic failure of real-time metacognitive evaluation in selecting a subzone-inappropriate AI interaction mode. Drawing on "SCAN" (Substitute, Complement, Aid, Non-Negotiable), a human-centric decision-making framework for generative AI task allocation grounded in Vygotsky's Zone of Proximal Development and metacognition, we advance the emerging social-constructivist conversation around AI in medical education by offering a testable account of AI's role in clinical reasoning development. This framework yields testable predictions for how misclassification can be detected, mitigated, and, more importantly, prevented in the clinical learning environment. Regarding clinical reasoning development, we show how trajectories of skill acquisition (upskilling) and failure (the triad of skill failure: de-skilling, never-skilling, and mis-skilling) operate at the individual task level in ways that fixed-phase, cohort-wide treatments fail to capture. We further identify passive engagement within correctly classified AI-scaffolded tasks as a particularly insidious, detection-resistant pathway to mis-skilling - one requiring subzone re-identification from AI assistance to expert assistance, with human experts serving as epistemic auditors. The paper operationalizes SCAN for clinical curriculum design, supervision, and assessment, and opens an empirical research agenda grounded in cognitive science. This paradigm shift from misuse to misclassification is not semantic: it offers educators a clear perspective on what to look for, what to assess, and what to intervene on.

摘要：整合人工智慧（AI），特別是生成式AI，進入醫學訓練引發了對學習者過度依賴、誤用和基礎臨床能力侵蝕的擔憂。<lb/>我們提出在決策層面上的概念重構：問題不是誤用，而是誤分類——在選擇不適當的AI互動模式時，實時的元認知評估發生了機械性失敗。<lb/>借鑒“SCAN”（替代、補充、輔助、不可談判），這是一個以人為中心的生成式AI任務分配決策框架，基於維果斯基的最近發展區和元認知，我們推進了關於AI在醫學教育中角色的社會建構主義對話，提供了一個可測試的關於AI在臨床推理發展中角色的說明。<lb/>這一框架產生了可測試的預測，關於如何在臨床學習環境中檢測、減輕，更重要的是預防誤分類。<lb/>關於臨床推理發展，我們展示了技能獲得（提升技能）和失敗（技能失敗的三元組：技能退化、從未獲得技能和錯誤獲得技能）的軌跡如何在個別任務層面運作，這是固定階段、群體範圍的處理無法捕捉的。<lb/>我們進一步確定在正確分類的AI支撐任務中被動參與是一條特別隱蔽、抗檢測的誤技能途徑——這需要從AI輔助轉變為專家輔助的子區域重新識別，並且人類專家擔任認知審計者。<lb/>本文將SCAN運用於臨床課程設計、監督和評估，並開啟了一個基於認知科學的實證研究議程。<lb/>從誤用到誤分類的這一範式轉變並非語義上的：它為教育者提供了一個清晰的視角，告訴他們應該尋找什麼、評估什麼以及應該介入什麼。

##### **Real-Time Musculoskeletal Surrogates for Pediatric Cerebral Palsy: a Credibility Pilot**
2608.28371v1 by Mohammad Arif Ul Alam

Real-time musculoskeletal (MSK) surrogates could support personalized rehabilitation for children with cerebral palsy (CP), but their credibility depends on subject-wise evaluation, low inference latency, and calibrated uncertainty. We develop a subject-conditioned causal neural surrogate using OpenSim-derived static parameters, temporal joint kinematics, true muscle capacities, and training-only perturbations. On a real pediatric CP gait dataset comprising nine children, we use leave-one-subject-out validation on six development subjects and evaluate a frozen configuration once on three locked test subjects. The surrogate accurately reproduces musculotendon lengths (R-square = 0.92 in development validation and approximately 0.95 on locked subjects; nRMSE < 8%) while requiring only sub-millisecond to few-millisecond neural inference, well below a 100 ms interactive-rehabilitation target. In contrast, direct muscle-force estimation remains unstable at this small, heterogeneous scale: pooled metrics can overstate within-subject, per-muscle accuracy. A Monte Carlo credibility pilot further shows that propagating only +/-5% anthropometry and muscle-capacity variation produces severely overconfident nominal 90% intervals (approximately 4% force coverage and below 1% MT-length coverage). These results establish a leakage-free evaluation and credibility framework for pediatric MSK surrogates, while identifying force modeling and epistemic uncertainty as the central next challenges for clinically credible digital twins.

摘要：即時肌肉骨骼 (MSK) 替代品可以支持腦性麻痺 (CP) 兒童的個性化康復，但其可信度取決於受試者的評估、低推斷延遲和經過校準的不確定性。我們使用基於 OpenSim 的靜態參數、時間關節運動學、真實肌肉能力和僅訓練的擾動，開發了一種受試者條件的因果神經替代品。在一個包含九名兒童的實際小兒 CP 步態數據集中，我們對六名發展受試者使用留一受試者驗證，並在三名鎖定測試受試者上評估一次凍結配置。該替代品準確重現了肌腱長度（發展驗證中的 R 平方 = 0.92，鎖定受試者約為 0.95；nRMSE < 8%），同時僅需亞毫秒到幾毫秒的神經推斷，遠低於 100 毫秒的互動康復目標。相比之下，直接的肌肉力量估計在這種小而異質的規模上仍然不穩定：綜合指標可能會高估受試者內的每肌肉準確性。一項蒙特卡羅可信度試點進一步顯示，僅傳播 +/-5% 的人體測量和肌肉能力變化會產生嚴重過度自信的名義 90% 區間（約 4% 的力量覆蓋率和低於 1% 的 MT 長度覆蓋率）。這些結果建立了一個無洩漏的評估和可信度框架，用於小兒 MSK 替代品，同時將力量建模和認知不確定性確定為臨床可信數位雙胞胎的中心挑戰。

##### **Propagating construction-time knowledge quality into medical question answering: A framework grounded in clinical guidelines**
2608.28360v1 by Jie Hu, Junjie Wang, Shan Lu, Yifang Hu, Gong Cheng, Yun Liu

Large language models have facilitated knowledge graph (KG) construction from clinical guidelines, but extracted triples vary in structural validity and evidential support. Meanwhile, graph-augmented question answering (QA) systems typically optimize query relevance during retrieval, with limited reuse of quality information produced during KG construction. This creates a disconnect between construction-time quality control and inference-time evidence use. We investigate whether construction-time triple quality can serve as a persistent signal for downstream evidence selection and presentation. We propose a quality-aware framework that models structural conformance (SchemaConf) and evidential support (EvidScore) as complementary dimensions and fuses them into a per-triple quality signal, Q(t). Rather than using quality solely for filtering, the framework retains Q(t) and derived quality tiers as graph attributes and propagates them into quality-weighted subgraph retrieval and tier-conditioned evidence prompting, while preserving passage-level provenance. Experiments on Chinese diabetes clinical guidelines show that the utility of the quality signal is distribution dependent. Under cross-version and cross-model shift, the fused Q(t) provides stronger triple-quality discrimination than either component alone (AUC 0.748 vs. 0.703 for EvidScore and 0.645 for SchemaConf). In guideline-grounded QA, propagating construction-time quality reduces required-knowledge omission from 16.3% to 5.3% and conflicting outputs from 16.3% to 2.7%, with an evidence-grounded precision of 81.6% and near-zero invalid citations. Blinded clinician ratings favor the full framework over no retrieval (4.68 vs. 4.21 on a five-point scale) and approach the oracle condition (4.80), while cross-generator experiments show consistent trends.

摘要：大型語言模型促進了從臨床指導中構建知識圖譜（KG），但提取的三元組在結構有效性和證據支持方面存在差異。<lb/>同時，增強圖形的問題回答（QA）系統通常在檢索過程中優化查詢相關性，而對於在KG構建過程中產生的優質信息的重用則有限。<lb/>這造成了構建時質量控制與推理時證據使用之間的脫節。<lb/>我們研究構建時的三元組質量是否可以作為下游證據選擇和呈現的持久信號。<lb/>我們提出了一個質量感知框架，將結構一致性（SchemaConf）和證據支持（EvidScore）建模為互補維度，並將它們融合為每個三元組的質量信號Q(t)。<lb/>該框架不僅僅將質量用於過濾，而是保留Q(t)和派生質量層級作為圖形屬性，並將它們傳播到質量加權的子圖檢索和層級條件的證據提示中，同時保留段落級的來源信息。<lb/>對中國糖尿病臨床指導的實驗表明，質量信號的效用依賴於分佈。<lb/>在跨版本和跨模型轉變下，融合的Q(t)提供了比任何單一組件更強的三元組質量區分（AUC 0.748對比EvidScore的0.703和SchemaConf的0.645）。<lb/>在以指導為基礎的QA中，傳播構建時的質量將所需知識的遺漏從16.3%減少到5.3%，衝突輸出從16.3%減少到2.7%，證據基礎的精確度為81.6%，幾乎沒有無效引用。<lb/>盲評的臨床醫生評分更青睞完整框架而非無檢索（在五分制上為4.68對比4.21），並接近神諭條件（4.80），而跨生成器的實驗顯示出一致的趨勢。

##### **AGENT-O: A Semantic Agent Card Framework for Interoperable and Governed Healthcare AI Agents**
2608.28345v1 by Pengze Li, Cui Tao

AGENT-O is a modular ontology framework that defines a semantic Agent Card for representing health-oriented AI agent systems and supports assessment of reporting completeness in scientific publications. AGENT-O was developed as an OWL 2/RDF ontology covering runtime, models, workflow, tools, clinical use, evaluation, provenance, governance, and reporting assessment. Evaluation included ontology inventory, OWL-RL reasoning, three SHACL suites, 12 SPARQL competency queries, three cases, and model-assisted reporting-completeness assessment of 279 papers across five dimensions. The ontology contained 1,962 RDF triples and 1,922 Protege axioms, with 252 active classes, 198 active object properties, and 51 datatype properties. All SHACL suites conformed on example graphs, all competency queries returned prespecified evidence, and all 279 papers were scored. Incomplete reporting was highest for runtime/architecture (84.6%), governance/safety (82.8%), and provenance/reproducibility (78.1%), compared with evaluation (25.8%) and benchmark-process alignment (29.8%). AGENT-O supported semantic Agent Card representation and reporting assessment while revealing an evaluation-specification gap: evaluation and benchmark procedures were reported more consistently than runtime architecture, governance, and reproducibility. AGENT-O provides a reusable ontology, semantic Agent Card profile, and reporting-completeness workflow for structured reporting and gap identification, but does not assess agent quality or deployment readiness.

摘要：AGENT-O 是一個模組化的本體框架，定義了一個語義代理卡，用於表示以健康為導向的 AI 代理系統，並支持對科學出版物報告完整性的評估。AGENT-O 被開發為一個 OWL 2/RDF 本體，涵蓋了運行時、模型、工作流程、工具、臨床使用、評估、來源、治理和報告評估。評估包括本體清單、OWL-RL 推理、三個 SHACL 套件、12 個 SPARQL 能力查詢、三個案例，以及對 279 篇論文在五個維度上的模型輔助報告完整性評估。該本體包含 1,962 個 RDF 三元組和 1,922 個 Protege 公理，擁有 252 個活躍類別、198 個活躍對象屬性和 51 個數據類型屬性。所有 SHACL 套件在示例圖上均符合，所有能力查詢均返回預先指定的證據，且所有 279 篇論文均已評分。運行時/架構（84.6%）、治理/安全（82.8%）和來源/可重複性（78.1%）的報告不完整性最高，而評估（25.8%）和基準過程對齊（29.8%）則較低。AGENT-O 支持語義代理卡表示和報告評估，同時揭示了評估規範的差距：評估和基準程序的報告一致性高於運行時架構、治理和可重複性。AGENT-O 提供了一個可重用的本體、語義代理卡配置文件和報告完整性工作流程，用於結構化報告和差距識別，但不評估代理質量或部署準備性。

##### **Cross-Spectral Dense Correspondence for Multimodal Spectral Medical Imaging**
2608.28341v1 by Eric L. Wisotzky, Jost Triller, Simon W. Härtl, Oliver T. Bruns, Peter Eisert, Anna Hilsmann

Precise dense correspondence is a fundamental prerequisite for multimodal spectral imaging systems that fuse disparate wavelength ranges for subsequent analysis in medical and scientific imaging. Corresponding image points are often observed with non-overlapping spectral sensitivities, leading to wavelength-dependent contrast changes, intensity inversions, and appearance shifts for which dense ground truth is difficult to obtain and conventional RGB-based training data provides only limited supervision. We address this data gap by introducing a sensor-agnostic cross-spectral modulation protocol on established correspondence benchmarks with intensity input projection, and by proposing a synthetic cross-spectral correspondence benchmark simulating physically plausible radiometric differences. Evaluation on several modern dense correspondence backbones trained with our unified cross-spectral protocol showed substantial improvements under severe spectral mismatch while maintaining performance on standard RGB benchmarks. Ablation experiments show that view-dependent channel selection and nonlinear radiometric transformations provide complementary robustness, indicating that the primary limitation of existing models is not their structural matching capacity but the mismatch between training distribution and spectral characteristics of the target image pair. Qualitative evaluations on heterogeneous medical spectral acquisition systems demonstrate the practical relevance of the proposed training data augmentation protocol as an enabler for spatially coherent spectral fusion in HSI workflows.

摘要：精確的密集對應是融合不同波長範圍以進行後續分析的多模態光譜成像系統的基本前提。對應的影像點通常在光譜敏感性上存在不重疊的情況，導致波長依賴的對比度變化、強度反轉和外觀變化，這使得獲得密集的真實數據變得困難，而傳統的基於RGB的訓練數據僅提供有限的監督。我們通過在已建立的對應基準上引入一種傳感器無關的跨光譜調製協議，並提出一個模擬物理上合理的輻射差異的合成跨光譜對應基準，來解決這一數據缺口。在幾個現代密集對應骨幹上進行的評估顯示，在嚴重的光譜不匹配下，使用我們統一的跨光譜協議訓練的模型顯著提高了性能，同時在標準RGB基準上保持了表現。消融實驗表明，依賴於視角的通道選擇和非線性輻射變換提供了互補的穩健性，這表明現有模型的主要限制不是其結構匹配能力，而是訓練分佈與目標影像對的光譜特徵之間的差異。對異質醫療光譜獲取系統的定性評估展示了所提出的訓練數據增強協議的實際相關性，作為在高光譜成像工作流程中實現空間一致的光譜融合的促進者。

##### **BanglaMed-QA: A Question Answering System for Healthcare Support in Bangla**
2608.28329v1 by Rowzatul Zannat, Abdullah Al Shafi, K. M. Azharul Hasan, Atia Shahnaz Ipa

Medical question answering (QA) systems have become crucial tools for providing reliable health information. But they remain very unexplored for low-resource languages like Bangla due to limited datasets and systems tailored to these languages. To address this, we introduce BanglaMed-QA, a robust QA system specifically designed for the Bangla medical domain. The process begins with building a structured medical knowledge base that includes 4,493 QA pairs in 9 categories under 506 diseases. To improve semantic comprehension, domain-specific root word dictionaries and synonym sets are proposed, in addition to part-of-speech tagging for anaphora resolution. We adopt supervised machine learning models in which SVM is found to be the best model to categorize questions. Multiple similarity metrics, including cosine, Jaccard, BM25, and Levenshtein, are applied with soft and hard voting methods for query matching. The performance of the QA system has been evaluated in two aspects, with a 95% F1 score in an automated evaluation and an average human satisfaction rating of 0.9 out of 1.0. This validates the real-world application of BanglaMed-QA in closing the healthcare information gap for Bangla speakers.

摘要：醫療問答（QA）系統已成為提供可靠健康資訊的重要工具。<lb/>但由於缺乏針對這些語言的數據集和系統，低資源語言如孟加拉語仍然未被充分探索。<lb/>為了解決這個問題，我們推出了BanglaMed-QA，一個專門為孟加拉醫療領域設計的強大QA系統。<lb/>該過程始於建立一個結構化的醫療知識庫，包括在506種疾病下的9個類別中的4,493個QA對。<lb/>為了改善語義理解，除了針對指代解析的詞性標註外，還提出了特定領域的根詞字典和同義詞集。<lb/>我們採用監督式機器學習模型，其中SVM被發現是分類問題的最佳模型。<lb/>多種相似度度量，包括餘弦相似度、Jaccard、BM25和Levenshtein，與軟投票和硬投票方法一起應用於查詢匹配。<lb/>QA系統的性能在兩個方面進行了評估，自動評估中的F1分數為95%，人類滿意度平均評分為0.9（滿分1.0）。<lb/>這驗證了BanglaMed-QA在填補孟加拉語使用者的醫療資訊空白方面的實際應用。

##### **Layered LLM Defenses as an Ensemble: Access Tiers, Inference Cost, and the Measured Failure Correlation Between Defense Layers**
2608.28327v1 by Abrar Alotaibi, Muhammad Shahid Jabbar, Sadam Al-Azani, Moataz Ahmed

Practitioners defend large language models (LLMs) by stacking defenses, assuming the layers compound. A stack is an ensemble, and ensembles compound only under a condition the LLM security literature recommends but never measures: the members must fail on different inputs.
  Two instruments make that measurable. The Adversary Access-Tier Model (AATM) grades an adversary by the access it holds, from system-only (A0) to influence over training data (A4). A cost model sorts defenses into five classes of inference-time overhead; because two classes require training weights or reading activations, they tier the defender as AATM tiers the adversary. From these we derive how a stack behaves, and the quantities a defender cares about diverge: coverage saturates within a tier, cost rises by class, false refusals accumulate as a union, and residual attack success falls multiplicatively only under independence.
  We measure that independence. Running one adaptive adversary against a seven-layer stack, failure correlation is positive in all fifteen measurable pairs ($φ$ from $0.30$ to $0.75$), and the joint residual exceeds the multiplicative prediction by up to $0.172$. Stratifying on behavior difficulty dissolves most of the association, so the dependence is predominantly common-cause, but it survives permutation inference, majority-vote grader labels, and externally calibrated thresholds. The same stack refuses four in five benign prompts while remaining statistically indistinguishable from its strongest single layer.
  The dependence is architectural rather than sampling-based: members correlate through the model they all wrap, so no wider member pool weakens it. Diversity therefore selects stack members but does not predict what an assembled stack delivers, which has to be measured end to end.

摘要：從業者透過堆疊防禦來保護大型語言模型（LLMs），假設這些層次會相互增強。堆疊是一種集成，而集成只有在LLM安全文獻建議但從未測量的條件下才會增強：成員必須在不同的輸入上失敗。<lb/>有兩個工具使這一點可測量。對手接入層級模型（AATM）根據對手所擁有的接入權限進行評分，從僅限系統（A0）到對訓練數據的影響（A4）。成本模型將防禦分為五個推理時間開銷類別；因為兩個類別需要訓練權重或讀取激活，因此它們將防禦者分級，就像AATM將對手分級一樣。從這些中我們推導出堆疊的行為，防禦者關心的量也有所不同：覆蓋率在一個層級內飽和，成本隨類別上升，錯誤拒絕作為聯集累積，而殘餘攻擊成功率僅在獨立的情況下呈乘法下降。<lb/>我們測量這種獨立性。對抗一個七層堆疊的自適應對手時，失敗相關性在所有十五對可測量的組合中都是正的（$φ$ 從 $0.30$ 到 $0.75$），而聯合殘餘超過乘法預測高達 $0.172$。根據行為難度進行分層會消解大部分關聯，因此依賴性主要是共同原因，但它在排列推斷、多數投票評分標籤和外部校準的閾值下仍然存在。同一堆疊在拒絕五個良性提示中的四個時，仍然在統計上與其最強的單層無法區分。<lb/>這種依賴性是架構性的，而非基於抽樣的：成員通過它們所包裹的模型相關聯，因此沒有更廣泛的成員池會削弱它。因此，多樣性選擇堆疊成員，但並不預測組裝堆疊所提供的內容，這必須從頭到尾進行測量。

##### **Physics-Guided Flow Matching for CT Image Reconstruction**
2608.28256v1 by Davide Evangelista

Deep generative models have recently emerged as powerful priors for solving ill-posed inverse problems in CT, with diffusion-based approaches achieving state-of-the-art reconstruction performance. However, diffusion models typically rely on stochastic sampling procedures, long inference trajectories, and carefully tuned noise schedules, which can limit computational efficiency and numerical stability, especially at high spatial resolutions. In this work, we investigate Flow Matching as an alternative generative prior for CT reconstruction. We train a high-resolution Rectified Flow Matching model on 256x256 chest images from the Mayo Clinic Low-Dose CT dataset. To mitigate overfitting and limited anatomical variability, we employ a two-stage training strategy consisting of an initial phase with strong, anatomically informed data augmentation, followed by a fine-tuning phase with reduced or no augmentation to refine structural fidelity. The resulting model is capable of generating high-quality and anatomically coherent CT-like images, serving as a strong learned prior. We then evaluate multiple reconstruction methods specifically designed for Flow Matching models, including Plug-and-Play Flow, FlowDPS, Flower, and Flow-Priors (ICTM), and compare them against state-of-the-art diffusion-based reconstruction algorithms such as DDRM, DPS, and DiffPIR. Experimental results across several CT inverse problem settings show that Flow Matching-based approaches consistently outperform diffusion-based methods in terms of PSNR, SSIM, and perceptual quality, while requiring fewer sampling steps. Finally, we publicly release the trained Flow Matching model and accompanying code to facilitate reproducibility and future research. Overall, this work demonstrates that Flow Matching provides a stable, efficient, and effective alternative to diffusion models for high-resolution CT image reconstruction.

摘要：深度生成模型最近已成為解決CT中病態逆問題的強大先驗，基於擴散的方法實現了最先進的重建性能。<lb/>然而，擴散模型通常依賴隨機取樣程序、長推理軌跡和精心調整的噪聲計劃，這可能限制計算效率和數值穩定性，尤其是在高空間分辨率下。<lb/>在這項工作中，我們研究了流匹配作為CT重建的替代生成先驗。<lb/>我們在Mayo Clinic低劑量CT數據集中對256x256的胸部影像訓練了一個高分辨率的修正流匹配模型。<lb/>為了減輕過擬合和有限的解剖變異性，我們採用了一種兩階段的訓練策略，初始階段使用強大的解剖學信息數據增強，隨後是減少或不進行增強的微調階段，以提高結構的真實性。<lb/>最終得到的模型能夠生成高質量且解剖學一致的CT類影像，作為一個強大的學習先驗。<lb/>然後，我們評估了多種專門為流匹配模型設計的重建方法，包括Plug-and-Play Flow、FlowDPS、Flower和Flow-Priors (ICTM)，並將它們與最先進的基於擴散的重建算法如DDRM、DPS和DiffPIR進行比較。<lb/>在幾個CT逆問題設置中的實驗結果顯示，基於流匹配的方法在PSNR、SSIM和感知質量方面始終優於基於擴散的方法，同時需要更少的取樣步驟。<lb/>最後，我們公開釋放訓練好的流匹配模型及其伴隨的代碼，以促進可重複性和未來的研究。<lb/>總體而言，這項工作表明流匹配為高分辨率CT影像重建提供了一種穩定、高效和有效的替代擴散模型的方法。

##### **Under-Mattress Temporal Sensing for Next-Day Agitation Risk Scoring in Dementia Wards**
2608.28152v1 by Zhen Liu, Marta Bono, Robbe Decloedt, Ajda Flisar, Maarten Van Den Bossche, Maarten De Vos

Agitation fluctuates over short time horizons in people living with dementia, yet continuous physiological information for anticipating next-day risk is limited. We assessed whether contactless under-mattress signals from the preceding night inform next-day agitation risk and whether preserving minute-level temporal structure improves performance over conventional nightly summaries. We analyzed 423 patient-nights from 65 subjects in a specialized hospital dementia unit using two under-mattress sensing systems. A unified four-paradigm benchmark compared nightly handcrafted summaries, three-period handcrafted features, full-night sequence modeling, and sliding-window multiple-instance learning. Source-specific preprocessing and five-fold patient-grouped cross-validation were used, with performance estimated from pooled out-of-fold predictions. Evaluation included discrimination, calibration, fixed-threshold metrics, and a comparison of period-signal attribution patterns across two temporal models. Full-night sequence modeling achieved the highest discrimination (AUROC, 0.692; AUPRC, 0.849) and balanced accuracy (0.658). Both minute-level pipelines had higher AUROC than nightly summaries, but differences from three-period handcrafted features were uncertain. Cross-model attribution prioritized activity, heart rate, and respiratory rate during the core overnight period. Calibration remained limited. The preceding night's signals supported modest next-day risk discrimination, with minute-level temporal modeling outperforming nightly summaries. Prospective calibration and external validation are needed before use in individual care decisions. This patient-grouped benchmark identifies contactless overnight sensing as a promising biomedical engineering direction for agitation-risk research in hospitalized dementia cohorts.

摘要：激動在生活於癡呆症患者的短期時間範圍內波動，然而用於預測次日風險的連續生理信息有限。<lb/>我們評估了前一晚的無接觸床墊信號是否能夠預測次日的激動風險，以及保留分鐘級的時間結構是否能提高相較於傳統夜間摘要的表現。<lb/>我們分析了來自65名受試者的423個病人夜晚數據，這些數據來自一個專門的醫院癡呆症單位，使用了兩種床墊下感應系統。<lb/>一個統一的四範式基準比較了手工製作的夜間摘要、三期手工特徵、全夜序列建模和滑動窗口多實例學習。<lb/>使用了源特定的預處理和五折病人分組交叉驗證，並從合併的外部預測中估算性能。<lb/>評估包括區分度、校準、固定閾值指標，以及比較兩個時間模型之間的期間信號歸因模式。<lb/>全夜序列建模達到了最高的區分度（AUROC，0.692；AUPRC，0.849）和均衡準確率（0.658）。<lb/>兩個分鐘級的管道比夜間摘要有更高的AUROC，但與三期手工特徵的差異不確定。<lb/>跨模型的歸因優先考慮了核心夜間期間的活動、心率和呼吸率。<lb/>校準仍然有限。<lb/>前一晚的信號支持適度的次日風險區分，分鐘級的時間建模表現優於夜間摘要。<lb/>在用於個別護理決策之前，需要前瞻性校準和外部驗證。<lb/>這個病人分組基準識別了無接觸的過夜感應作為住院癡呆症隊列中激動風險研究的一個有前景的生醫工程方向。

##### **Do Medical Vision Models Reason About Anatomy? Probing the Spatial Inductive Biases of Learned Visual Representations**
2608.28092v1 by Naren Akash, Neeraja Ramanan

Interpreting a CT scan means comparing structures on either side, judging how far apart organs sit, and knowing where each one belongs. Medical vision encoders are evaluated on diagnostic accuracy, or through assembled multimodal systems where a failure is hard to attribute, so it remains unclear whether their representations support any of this. We construct SPAR-Bench, eight probes over multi-organ abdominal CT that separate coordinate localization, relational reasoning, and spatial queries, and apply them to five architectural configurations and three medical foundation models, frozen and finetuned. Probes that ask for a comparison within the slice stay at chance, and neither pretraining scale, finetuning, nor architecture closes the gap. Probes that appear solved in domain fall to chance under zero-shot transfer, indicating that their accuracy reflects recall of canonical anatomy rather than computation over the image. Reading the same frozen features with a pooled head rather than the full set of tokens moves relational recovery from 0.7% to 67.8%, so pooled probing understates what a representation holds. Questions the encoders answer well are answered at chance by four open-weight MLLMs. Our results suggest these encoders carry a map of where organs usually lie, and little of the machinery for comparing structures within a particular patient. Code and data will be available at https://spar-bench.github.io.

摘要：解讀 CT 掃描意味著比較兩側的結構，判斷器官之間的距離，以及了解每個器官的歸屬位置。醫療影像編碼器的評估基於診斷準確性，或通過組裝的多模態系統進行評估，在這種情況下，失敗的原因難以歸因，因此仍不清楚它們的表徵是否支持這些。 我們構建了 SPAR-Bench，這是一個涵蓋多器官腹部 CT 的八個探針，分別針對坐標定位、關係推理和空間查詢，並將其應用於五種架構配置和三種醫療基礎模型，這些模型分別是凍結和微調的。 在切片內進行比較的探針保持在隨機機率，無論是預訓練規模、微調還是架構，都無法縮小差距。 在零樣本轉移下，顯示在領域中已解決的探針則回落至隨機機率，這表明它們的準確性反映的是對典型解剖學的回憶，而非對影像的計算。 使用池化頭而非完整的標記集來讀取相同的凍結特徵，將關係恢復從 0.7% 提高至 67.8%，因此池化探針低估了表徵所包含的內容。 編碼器能夠良好回答的問題，則被四個開放權重的 MLLMs 隨機回答。我們的結果表明這些編碼器攜帶著器官通常位於何處的地圖，而對於在特定患者內部比較結構的機制則幾乎沒有。 代碼和數據將在 https://spar-bench.github.io 上提供。

##### **Explainable Uncertainty Estimation for Reliable Medical AI**
2608.28052v1 by Li Rong Wang, Jamie Duell, Xinran Xu, Thomas C. Henderson, Yu Yue Hew, Pik Wan Erica Chiang, Xiao Wei Alstar Ang, Bingwen Eugene Fan, Xiuyi Fan

Artificial intelligence has strong potential to support clinical decision-making, yet its adoption in healthcare remains limited due to a lack of trust. Uncertainty estimation can signal unreliable predictions, and explainable AI (XAI) can clarify how predictions are made but existing methods treat them separately, providing no feature-level insight into why a prediction is uncertain or which tests to prioritize to reduce it. To address this gap, we propose explainable uncertainty estimation, which unifies uncertainty estimation and XAI to both quantify uncertainty and explain feature-level contributions. We introduce the Expected Gradients Reconstruction Uncertainty Estimate (egRUE), which incorporates prediction explanations into its uncertainty computation and decomposes uncertainty into feature-wise contributions. We prove theoretical properties of egRUE and show through experiments that it improves reliability and interpretability compared to existing methods. A user study with medical experts further demonstrates that egRUE's explanations improve calibrated trust over uncertainty scores alone, increasing confidence in correct predictions and reducing confidence in incorrect ones. By combining prediction uncertainty with feature-level explanations, egRUE strengthens decision-making support in safety-critical healthcare settings, clarifying both when predictions may be unreliable and which features drive that uncertainty.

摘要：人工智慧在支持臨床決策方面具有強大的潛力，但由於缺乏信任，其在醫療保健中的採用仍然有限。<lb/>不確定性估計可以指示不可靠的預測，而可解釋的人工智慧（XAI）可以澄清預測是如何產生的，但現有的方法將它們分開處理，未提供關於為何預測不確定或應優先考慮哪些測試以減少不確定性的特徵級見解。<lb/>為了解決這一空白，我們提出了可解釋的不確定性估計，該方法統一了不確定性估計和XAI，以量化不確定性並解釋特徵級貢獻。<lb/>我們引入了期望梯度重建不確定性估計（egRUE），它將預測解釋納入其不確定性計算中，並將不確定性分解為特徵級貢獻。<lb/>我們證明了egRUE的理論性質，並通過實驗顯示它在可靠性和可解釋性方面優於現有方法。<lb/>與醫療專家的用戶研究進一步證明，egRUE的解釋提高了對不確定性分數的校準信任，增強了對正確預測的信心，並減少了對錯誤預測的信心。<lb/>通過將預測不確定性與特徵級解釋相結合，egRUE增強了在安全關鍵的醫療保健環境中的決策支持，澄清了預測何時可能不可靠以及哪些特徵驅動了該不確定性。

##### **Automated Analysis Framework for Multilingual Climate-Health Literature Based on Multi-Agent Large Language Model**
2608.27998v1 by Yuze Sun, Shihui Zhang, Jiancheng Pan, Yunjia Ye, Wentao Luo, Jiahao Li, Quan Zhang, Wenjia Cai, Xiaomeng Huang

The rapid proliferation of interdisciplinary and multilingual scientific literature has left traditional manual analysis and single-algorithm methods plagued by low efficiency, poor scalability, and insufficient domain adaptability. Targeting the literature analysis needs of the typical interdisciplinary climate-health field, this study proposes a multi-agent large language model automated analysis framework for multilingual scientific literature, which realizes full-process automation covering literature screening, structured information extraction, and standardized integration. With a central coordination module as the core, the framework deploys three dedicated agents for document evaluation, information extraction, and analytical review to mimic the literature analysis thinking of domain experts, and adopts a four-layer hallucination control strategy together with a manual verification procedure to ensure the accuracy and reliability of analytical outcomes. Validated on a bilingual Chinese-English corpus of 32,642 climate-health papers covering China from 1993 to 2023, the framework achieves an F1 score of 0.92 in core information extraction, and completes the extraction and standardization of 2,012 city-literature association pairs, offering effective technical support for large-scale evidence mining in the climate-health research domain.

摘要：跨學科和多語言科學文獻的快速增長，使得傳統的手動分析和單一算法方法受到低效率、擴展性差和領域適應性不足的困擾。針對典型的跨學科氣候與健康領域的文獻分析需求，本研究提出了一個多代理大型語言模型自動化分析框架，用於多語言科學文獻，實現了涵蓋文獻篩選、結構化信息提取和標準化整合的全過程自動化。該框架以中央協調模塊為核心，部署了三個專用代理，分別負責文檔評估、信息提取和分析審查，以模擬領域專家的文獻分析思維，並採用四層幻覺控制策略以及手動驗證程序，以確保分析結果的準確性和可靠性。在一個涵蓋1993年至2023年中國的32,642篇氣候與健康論文的雙語中英語語料庫上進行驗證，該框架在核心信息提取中達到了0.92的F1分數，並完成了2,012對城市文獻關聯的提取和標準化，為氣候與健康研究領域的大規模證據挖掘提供了有效的技術支持。

##### **A Deep Learning-Based Stacking Ensemble Framework for Turbofan Engine Remaining Useful Life Prediction**
2608.27940v1 by Limon Bin Hossain, Md. Salehin Seyam, Md Rashedul Islam, Abdur Rahman, Md Sharifuzzaman

This study proposes a two-level stacking ensemble framework for Remaining Useful Life (RUL) prediction of turbofan engines, evaluated on the NASA C-MAPSS benchmark using the FD001 and FD003 subsets. The framework integrates four heterogeneous deep learning base learners: Long Short-Term Memory (LSTM), Convolutional Neural Network (CNN), CNN-LSTM, and CNN-GRU, whose out-of-fold predictions are combined by an XGBoost meta-learner to capture complex degradation patterns while mitigating individual model biases. Comprehensive experiments demonstrate that the stacking ensemble achieves superior predictive performance, with Root Mean Square Error (RMSE) of 9.989 and 8.613, Mean Absolute Error (MAE) of 7.081 and 5.195, and R-squared values of 0.899 and 0.906 for FD001 and FD003, respectively. Compared to the best-reported baseline (TCAT: RMSE 11.12 and 11.02), the proposed method achieves RMSE reductions of 10.2 percent and 21.8 percent for FD001 and FD003, respectively. Feature correlation analysis, residual diagnostics, and training convergence curves validate the model's robustness. These findings underscore the efficacy of stacking ensemble methods for prognostics and health management in safety-critical aerospace applications.

摘要：這項研究提出了一個兩層堆疊集成框架，用於渦扇發動機的剩餘使用壽命（RUL）預測，並在NASA C-MAPSS基準上使用FD001和FD003子集進行評估。該框架整合了四個異質深度學習基學習器：長短期記憶（LSTM）、卷積神經網絡（CNN）、CNN-LSTM和CNN-GRU，其外部預測由XGBoost元學習器組合，以捕捉複雜的退化模式，同時減輕個別模型的偏差。全面的實驗表明，堆疊集成達到了優越的預測性能，FD001和FD003的均方根誤差（RMSE）分別為9.989和8.613，平均絕對誤差（MAE）分別為7.081和5.195，R平方值分別為0.899和0.906。與最佳報告的基準（TCAT：RMSE 11.12和11.02）相比，所提方法在FD001和FD003上分別實現了10.2%和21.8%的RMSE減少。特徵相關性分析、殘差診斷和訓練收斂曲線驗證了模型的穩健性。這些發現強調了堆疊集成方法在安全關鍵航空應用中的預測和健康管理的有效性。

##### **FedEHR-Agents: Federated Agentic Optimization for Automated EHR Modeling**
2608.27856v1 by Jun Bai, Ruilin Wang, Yue Li

Recent advances in large language models are enabling autonomous clinical agents to perform increasingly complex electronic health record (EHR) modeling workflows. However, agents deployed at individual hospitals remain constrained by institution-specific data and modeling environments, while direct cross-hospital collaboration is restricted by the sensitivity of patient-level EHR data. Although federated learning (FL) provides a natural foundation for privacy-preserving collaboration, existing approaches remain predominantly model-centric, limiting federation to prediction models or their updates while overlooking the richer modeling experience accumulated by autonomous agents. To address this limitation, we propose FedEHR-Agents, an experience-centric federated agentic optimization framework for automated EHR modeling. Each hospital deploys an autonomous clinical EHR agent that performs data preprocessing and model development while refining local clinical modeling experience through historical memory, task-specific evaluation, and TextGrad-based prompt refinement. The federated server performs evidence-guided experience aggregation to integrate reliable and complementary modeling experience across heterogeneous hospitals and distills the aggregated experience into global meta-prompts for subsequent local refinement. Extensive experiments on real-world multi-hospital EHR benchmarks demonstrate that FedEHR-Agents consistently outperforms local and federated baselines across diverse clinical prediction tasks and remains robust across different federation scales and LLM backbones. These results establish clinical modeling experience as a promising collaborative object beyond conventional parameter-centric FL and point toward federated autonomous clinical intelligence.

摘要：最近在大型語言模型方面的進展使得自主臨床代理能夠執行越來越複雜的電子健康紀錄 (EHR) 建模工作流程。<lb/>然而，部署在個別醫院的代理仍然受到機構特定數據和建模環境的限制，而直接的跨醫院合作則受到患者級 EHR 數據敏感性的限制。<lb/>儘管聯邦學習 (FL) 為保護隱私的合作提供了自然的基礎，但現有的方法仍然主要以模型為中心，將聯邦限制在預測模型或其更新上，而忽略了自主代理所積累的更豐富的建模經驗。<lb/>為了解決這一限制，我們提出了 FedEHR-Agents，一個以經驗為中心的聯邦代理優化框架，用於自動化 EHR 建模。<lb/>每個醫院部署一個自主臨床 EHR 代理，該代理執行數據預處理和模型開發，同時通過歷史記憶、任務特定評估和基於 TextGrad 的提示優化來完善本地臨床建模經驗。<lb/>聯邦伺服器執行基於證據的經驗聚合，以整合來自異質醫院的可靠和互補的建模經驗，並將聚合的經驗提煉成全球元提示，以便後續的本地優化。<lb/>在真實世界的多醫院 EHR 基準上進行的大量實驗表明，FedEHR-Agents 在各種臨床預測任務中始終優於本地和聯邦基準，並在不同的聯邦規模和 LLM 骨幹中保持穩健。<lb/>這些結果確立了臨床建模經驗作為一個超越傳統參數中心 FL 的有前景的合作對象，並指向聯邦自主臨床智能的方向。

##### **From Uncertainty to Clinical Risk: Severity-Aware Conformal Planning for Interactive Medical Diagnosis**
2608.27847v1 by Yue Zhou, Haiyang Zhou, Jin Zhang, Kong Wang, Yongxin Ni, Youhua Li, Hanwen Du

Interactive medical diagnosis dynamically acquires patient information through multiple rounds of questioning, supporting accurate, efficient, and safe clinical decisions under incomplete evidence. Existing methods commonly guide information acquisition with predictive uncertainty or label ambiguity, but overlook the asymmetric clinical risk of missing severe diseases and lack unified long-horizon planning over whether to continue asking questions or commit to a diagnosis. To address these limitations, we propose Severity-Aware Conformal Clinical Planning, which formulates interactive diagnosis as a risk-sensitive sequential decision problem. The framework maintains complementary diagnostic, safety, and masked-evidence beliefs; calibrates turn-specific diagnostic prediction sets and severity-weighted differential-diagnosis risk on held-out diagnostic trajectories; and introduces the calibrated clinical risk into Monte Carlo Tree Search to jointly evaluate long-horizon Ask and Commit trajectories. Experiments on DDXPlus and MediQ show that our method achieves more accurate diagnoses with fewer questions across multiple large language models, while improving differential-diagnosis quality and reducing high-risk errors in severe cases. These findings validate the value of using clinical risk, rather than predictive uncertainty alone, as a planning signal and demonstrate the effectiveness of the proposed framework for information acquisition and risk-aware diagnostic decision making. They also motivate future work on clinical-risk-oriented interactive diagnosis and information-acquisition methods.

摘要：互動醫療診斷透過多輪提問動態獲取患者信息，支持在不完整證據下做出準確、高效和安全的臨床決策。現有方法通常以預測不確定性或標籤模糊性指導信息獲取，但忽視了漏診重病的非對稱臨床風險，並缺乏統一的長期規劃，以決定是否繼續提問或確診。為了解決這些限制，我們提出了重症意識的合規臨床規劃，將互動診斷公式化為一個風險敏感的序列決策問題。該框架維持互補的診斷、安全和隱藏證據信念；在保留的診斷軌跡上校準特定回合的診斷預測集和重症加權的差異診斷風險；並將校準的臨床風險引入蒙特卡羅樹搜索，以共同評估長期的詢問和確診軌跡。在DDXPlus和MediQ上的實驗顯示，我們的方法在多個大型語言模型中以更少的提問實現了更準確的診斷，同時提高了差異診斷質量並減少了重症病例中的高風險錯誤。這些發現驗證了使用臨床風險而非僅僅依賴預測不確定性作為規劃信號的價值，並展示了所提出框架在信息獲取和風險意識診斷決策中的有效性。它們還激勵了未來在臨床風險導向的互動診斷和信息獲取方法上的研究。

##### **CARDINAL Predicts Cardiovascular Risk From Non-contrast Cardiac CT**
2608.27690v1 by Roy Gabriel, Nattakorn Kittisut, Jamshid Hassanpour, Michael Galarnyk, Abanoub Abdelmalak, Marly van Assen, Carlo N. De Cecco, Arshed Quyyumi, Ali Adibi

Cardiovascular risk prediction remains limited by incomplete clinical data and imaging biomarkers that reduce computed tomography (CT) to a small number of handcrafted features. We developed CARDINAL (Cardiovascular Assessment via Representation learning from Deep Imaging with Nested Anatomical Latent embeddings), a clinically grounded framework that learns compact representations from routine non-contrast cardiac CT for major adverse cardiovascular event (MACE) prediction. In 17,659 patients, CARDINAL was evaluated for 1-, 3-, 5-, and 10-year MACE prediction against American Heart Association (AHA) pooled cohort equations (PCE), AHA predicting risk of cardiovascular disease events (PREVENT), coronary artery calcium (CAC), segmentation-derived CT biomarkers, and 70-feature structural radiomics. Gains were largest at longer horizons. At 10 years, CARDINAL (joint) achieved an area under the receiver operating characteristic curve (AUROC) of 0.866 $\pm$ 0.020 and an area under the precision-recall curve (AUPRC) of 0.890 $\pm$ 0.015, compared with an AUROC of 0.826 $\pm$ 0.023 and an AUPRC of 0.826 $\pm$ 0.022 for structural radiomics, the strongest baseline. CARDINAL also achieved the highest survival concordance index (C-index), 0.753 $\pm$ 0.015, and high-versus-low risk-tertile hazard ratio, 10.78 $\pm$ 3.16, with favorable reclassification and exploratory calibration. These findings suggest that non-contrast cardiac CT contains prognostic information beyond conventional risk equations, CAC scoring, and engineered imaging biomarkers.

摘要：心血管風險預測仍然受到不完整的臨床數據和影像生物標記的限制，這使得計算機斷層掃描（CT）僅能縮減到少數手工特徵。我們開發了CARDINAL（通過深度影像學習的心血管評估，具有嵌套解剖潛在嵌入），這是一個臨床基礎的框架，從常規非對比心臟CT中學習緊湊的表示，以預測重大不良心血管事件（MACE）。在17,659名患者中，CARDINAL被評估為1年、3年、5年和10年的MACE預測，並與美國心臟協會（AHA）匯總隊列方程（PCE）、AHA預測心血管疾病事件風險（PREVENT）、冠狀動脈鈣化（CAC）、基於分割的CT生物標記和70特徵結構放射組學進行比較。在較長的時間範圍內，增益最大。在10年時，CARDINAL（聯合）達到接收者操作特徵曲線下面積（AUROC）為0.866 $\pm$ 0.020，精確度-召回曲線下面積（AUPRC）為0.890 $\pm$ 0.015，相較於結構放射組學的AUROC為0.826 $\pm$ 0.023和AUPRC為0.826 $\pm$ 0.022，後者是最強的基準。CARDINAL還達到了最高的生存一致性指數（C-index），0.753 $\pm$ 0.015，以及高風險與低風險三分位數的危險比，10.78 $\pm$ 3.16，並具有良好的重新分類和探索性校準。這些發現表明，非對比心臟CT包含超越傳統風險方程、CAC評分和工程影像生物標記的預後信息。

##### **First Make It Playable, Then Make It Good: Staged Interaction Learning for Small Dialogue-Game Agents**
2608.27672v1 by Syed Mahbubul Huq, Pranava Madhyastha

We present Qwen-GuidePlay-2B, a 2B-parameter language model for dialogue-game interaction. We fine-tune Qwen3.5-2B using three steps: a) SFT on only successful game trajectories from Playpen, b) weighted turn-level SFT, and c) teacher-guided SFT. The teacher model (which is a larger model) is only used to fix formatting and evaluate examples, but does not create new gold actions. Our final model scores 57.12 clemscore and 42.68 statscore on the public Playpen validation. In the officially released challenge results, our model obtains the second-highest Playpen clemscore delta among submitted systems (which is approximately +36 over its base model). Our findings suggest that imitating full trajectories helps with playability, while turn-level and teacher-guided training usually improve decision-making and increase the overall score. Alternative procedurally heavy approaches like replay-repair and hard-example mining did not help, which suggests that small models are performant simply by using careful curation strategies rather than aggressive changes. We make available both the model and the code for reproducibility.

摘要：我們提出 Qwen-GuidePlay-2B，這是一個用於對話遊戲互動的 2B 參數語言模型。我們通過三個步驟對 Qwen3.5-2B 進行微調：a) 僅對來自 Playpen 的成功遊戲軌跡進行 SFT，b) 加權回合級 SFT，以及 c) 教師引導的 SFT。教師模型（這是一個較大的模型）僅用於修正格式和評估範例，但不創建新的金標行動。我們的最終模型在公共 Playpen 驗證中得分 57.12 clemscore 和 42.68 statscore。在官方發布的挑戰結果中，我們的模型在提交的系統中獲得了第二高的 Playpen clemscore 增量（約為 +36，相較於其基礎模型）。我們的研究結果表明，模仿完整的軌跡有助於可玩性，而回合級和教師引導的訓練通常能改善決策並提高整體得分。像重播修復和困難範例挖掘這樣的替代性程序繁重方法並未帶來幫助，這表明小模型僅通過謹慎的策劃策略而非激進的變更就能達到良好的性能。我們提供模型和代碼以便於重現。

##### **Learning a Continuous Sepsis Severity Score Without Hour-by-Hour Supervision: A Two-Site Retrospective Study**
2608.27421v1 by Kevin Zhu, Ryan Zhang, Baraa Abed, Tilendra Choudhary, Malvern Madondo, Mehak Arora, Yixuan Yang, Alasdair Gent, Aditya Nagori, Omer T. Inan, Krista L. Haines, Patrick Georgoff, Suresh M. Agarwal, Vijay Krishnamoorthy, Tetsu Ohnuma, Mihai V. Podgoreanu, Michael R. Pinsky, Gilles Clermont, Craig M. Coopersmith, Craig S. Jabaley, Rishikesan Kamaleswaran

Currently used sepsis severity indices rely on fixed variables and weights established decades ago, which are coarsely discretized and calibrated to a cohort that no longer reflects contemporary critical care. No alternative learned directly from patient trajectories is in routine use. We conducted a retrospective two-cohort study on a total of 29,116 and 7,691 adult patients meeting Sepsis-3 criteria from two hospital systems in Massachusetts and Georgie, respectively.
  We developed a sepsis index using 43 routinely charted variables over a 72-hour treatment window. Unlike previous studies, we use mortality as a treatment-level ranking signal rather than a per-state target, allowing credit to be redistributed non-uniformly across timesteps. Evaluation was done on a permanent 20% test holdout, using clinical vignettes and Spearman correlation. Uncertainty intervals were obtained by bootstrap resampling of whole patients. Under this ranking scheme, non-survivors scored 1.19-1.64 points higher than survivors on a 0-10 scale within all strata of baseline SOFA-2, with similar results stratifying within lactate, mean arterial pressure (MAP), and creatinine. Within-patient change in the index correlated with change in lactate (Spearman rho = 0.39; n = 1,854). Similar, weaker correlations were found for MAP and creatinine. On a cohort level, cross-institutional agreement measured by Spearman correlation between models trained on different sites, were 70-77% of same-site correlation. External within-patient correlations were 0.54 and 0.59 against ceilings of 0.92 and 0.90. Our index also correlated with established indices, while null controls stayed near zero.
  Our index demonstrated hourly prognostic information that meaningfully separates patient outcomes and is consistent with clinical expectation, indicating potential as a decision support tool complementing clinical judgement.

摘要：目前使用的敗血症嚴重程度指數依賴於幾十年前建立的固定變數和權重，這些變數和權重經過粗略離散化並校準至一個不再反映當代重症護理的隊列。沒有任何直接從患者軌跡學習的替代方案在常規使用中。我們對來自馬薩諸塞州和喬治亞州的兩個醫院系統中符合Sepsis-3標準的29,116名和7,691名成人患者進行了回顧性雙隊列研究。<lb/>我們使用在72小時治療窗口內常規記錄的43個變數開發了一個敗血症指數。與以往的研究不同，我們使用死亡率作為治療層級的排名信號，而不是每個狀態的目標，這樣可以在時間步驟之間非均勻地重新分配信用。評估是在一個永久性的20%測試保留上進行的，使用臨床小品和斯皮爾曼相關性。通過對整個患者的自助重抽樣獲得不確定性區間。在這種排名方案下，非存活者在所有基線SOFA-2層級中在0-10的尺度上比存活者高出1.19-1.64分，並且在乳酸、平均動脈壓(MAP)和肌酸酐的分層中得到了類似的結果。指數的患者內變化與乳酸的變化相關(Spearman rho = 0.39; n = 1,854)。對於MAP和肌酸酐也發現了類似的較弱相關性。在隊列層面上，跨機構的協議由在不同地點訓練的模型之間的斯皮爾曼相關性測量，為同一地點相關性的70-77%。外部的患者內相關性為0.54和0.59，對應的上限為0.92和0.90。我們的指數還與已建立的指數相關，而空白對照則保持在接近零的水平。<lb/>我們的指數顯示出每小時的預後信息，能夠有意義地區分患者結果，並與臨床預期一致，顯示出作為輔助臨床判斷的決策支持工具的潛力。

##### **How Language Models Organize and Structure Moral Knowledge**
2608.27402v1 by Orion Reblitz-Richardson

How do large language models (LLMs) organize moral knowledge? Models detect moral content broadly, but detection is a low bar. We ask whether they go further, distinguishing moral foundations from one another and organizing the relationships between them geometrically.
  We train six independent linear probes on open-weight language models, one per Moral Foundations Theory (MFT) category (care/harm, fair/cheat, lib/oppress, loy/betray, auth/subv, sanc/degrade), and examine how the resulting directions relate to each other in representation space. We find the directions neither collapse into a single moral detector nor isolate from one another. Rather, they span a near-maximal number of independent dimensions while sharing a positive common component. The shared component is the signature of integration, and it is moral-specific relative to a matched non-moral concept battery built identically (mean pairwise cosine 0.26 vs. 0.013).
  The geometry is consistent across architectures and scale and reaches its integration regime early in pre-training, well before probe accuracy saturates. The structure the model discovers shows no evidence of the individualizing/binding distinction predicted by Moral Foundations Theory (an underpowered test: only 20 candidate partitions exist) but rather reflects corpus statistics. Extending to moral dilemmas, each dilemma direction partially composes from its component foundations, at 2.7x a mismatched-pair baseline, while the majority of its variance encodes conflict-specific structure. The model represents moral tension itself, not a pre-resolved judgment.

摘要：大型語言模型（LLMs）如何組織道德知識？模型廣泛檢測道德內容，但檢測只是最低要求。我們詢問它們是否更進一步，區分彼此的道德基礎並幾何地組織它們之間的關係。<lb/>我們在開放權重的語言模型上訓練六個獨立的線性探針，每個探針對應於一個道德基礎理論（MFT）類別（關懷/傷害、公平/欺騙、自由/壓迫、忠誠/背叛、權威/壓制、神聖/貶低），並檢查結果方向在表示空間中的相互關係。我們發現這些方向既不會合併成單一的道德檢測器，也不會彼此隔離。相反，它們跨越接近最大數量的獨立維度，同時共享一個正的共同成分。共享的成分是整合的標誌，並且相對於一個相同構建的非道德概念電池是道德特定的（平均配對餘弦 0.26 對 0.013）。<lb/>這種幾何形狀在不同架構和規模之間是一致的，並且在預訓練的早期就達到了整合範疇，遠早於探針準確度飽和。模型發現的結構並未顯示出道德基礎理論所預測的個體化/綁定區別（這是一個能力不足的測試：僅存在 20 個候選劃分），而是反映了語料庫統計。擴展到道德困境，每個困境方向部分由其組成基礎組成，達到 2.7 倍的錯配對基準，而其大多數變異編碼了特定衝突的結構。該模型表示道德緊張本身，而不是預先解決的判斷。

##### **Making Clinical Language Models Auditable: Concept-Guided Fine-Tuning for Robust Prediction**
2608.27397v1 by Jin Mu, Guanhua Chen

Clinical language models can achieve strong in-hospital accuracy yet fail under deployment shifts because they exploit note-specific artifacts (e.g., templates, separators, boilerplate) that do not reflect patient state. We propose CAST (Concept-guided Artifact Suppression Tuning), an SAE-based framework for auditable clinical text classification. CAST uses Sparse Autoencoders to expose sparse, human-auditable features from intermediate Transformer activations, labels SAE latents with an LLM-assisted interpretation pipeline and ICD-10 retrieval constraints, suppresses verified artifact latents via residual subtraction during fine-tuning, and provides post-hoc per-concept attributions for auditing model decisions. On MIMIC-IV discharge-note mortality prediction, CAST improves over its corresponding fine-tuned encoder baselines and remains competitive with strong LLM baselines, while producing a feature-level audit trail of the clinical concepts that support each prediction and the artifact concepts suppressed during training.

摘要：臨床語言模型可以在院內達到強大的準確性，但在部署轉變下卻失敗，因為它們利用了特定於筆記的工件（例如，模板、分隔符、標準文本），這些工件並不反映患者的狀態。我們提出了CAST（概念引導的工件抑制調整），這是一個基於SAE的可審計臨床文本分類框架。CAST使用稀疏自編碼器從中間Transformer激活中揭示稀疏的、人類可審計的特徵，通過LLM輔助的解釋管道和ICD-10檢索約束標記SAE潛變量，在微調過程中通過殘差減法抑制已驗證的工件潛變量，並提供事後每個概念的歸因以審計模型決策。在MIMIC-IV出院記錄死亡預測中，CAST在其相應的微調編碼器基準上有所改進，並且與強大的LLM基準保持競爭，同時生成支持每個預測的臨床概念的特徵級審計記錄，以及在訓練期間抑制的工件概念。

##### **ORDDAR: Observation-Driven Reasoning for Distortion-Resilient Decision, Action, and Cognitive Recovery**
2608.28704v1 by Deblina Kar, Anant Nawalgaria, Shyamal Kumar Das Mandal

AI agents increasingly perform long-term reasoning, planning, tool use, memory integration, and autonomous decision making, yet erroneous intermediate states can propagate and cause inconsistent decisions and unreliable outputs. Existing reasoning approaches mainly rely on iterative planning, self-reflection, augmented memory, or verification, but rarely localize and selectively repair faulty reasoning. We present ORDDAR (Observation-Driven Reasoning for Distortion-Resilient Decision, Action, and Cognitive Recovery), a reasoning framework that models reasoning as cognitive state transitions, detects localized distortions, retrieves related reasoning from prior experiences, and repairs only the affected states. ORDDAR therefore performs recovery at the local reasoning-transition level rather than regenerating the complete trajectory. Experiments across mathematical, commonsense, multi-hop, and clinical reasoning benchmarks demonstrate improved reasoning quality, recovery ability, and interpretability over multiple evaluated reasoning baselines.

摘要：AI 代理越來越能進行長期推理、規劃、工具使用、記憶整合和自主決策，但錯誤的中間狀態可能會傳播並導致不一致的決策和不可靠的輸出。現有的推理方法主要依賴於迭代規劃、自我反思、增強記憶或驗證，但很少能夠定位並選擇性修復錯誤的推理。我們提出了 ORDDAR（觀察驅動的抗失真決策、行動和認知恢復推理框架），該框架將推理建模為認知狀態轉換，檢測局部失真，從先前經驗中檢索相關推理，並僅修復受影響的狀態。因此，ORDDAR 在局部推理轉換層面上進行恢復，而不是重新生成完整的軌跡。在數學、常識、多跳和臨床推理基準上的實驗顯示，推理質量、恢復能力和可解釋性均優於多個評估的推理基準。

##### **From Reasoning to Pixels: Grounded Medical Multimodal LLMs for VQA and Segmentation**
2608.26856v1 by Haowen Gu, Gensheng Pei, Junzhu Mao, Qiong Wang, Mingwu Ren, Yazhou Yao

Although Multimodal Large Language Models (MLLMs) have demonstrated impressive performance in Medical Visual Question Answering (Med-VQA), their reliance on global image features often lacks precise pixel-level grounding, thereby limiting clinical trustworthiness. To bridge the semantic gap between high-level clinical reasoning and spatial localization, we propose \textsc{\textsc{MedREAL}} (\textbf{Med}ical \textbf{RE}asoning-driven \textbf{A}nswering and \textbf{L}ocalization), a unified framework that seamlessly aligns linguistic reasoning with spatial grounding. Specifically, \textsc{MedREAL} introduces \textbf{S}eg \textbf{A}nchored \textbf{R}easoning \textbf{P}ooling (SARP) to distill task-relevant semantic evidence directly from \texttt{[SEG]} tokens within the MLLM's hidden states. Furthermore, a \textbf{R}easoning-to-\textbf{V}isual (R2V) fusion mechanism is proposed to effectively inject these reasoning-aware features into a segmentation pipeline for accurate mask decoding. To facilitate this paradigm, we construct MedRAVS-13K, a comprehensive dataset comprising 13,824 expertly validated samples across four diverse imaging modalities. Extensive experiments demonstrate that \textsc{MedREAL} significantly outperforms state-of-the-arts, achieving 68.49\% gIoU and 70.47\% cIoU on benchmark evaluations. By generating evidence masks that are strictly consistent with textual diagnoses, \textsc{MedREAL} provides a robust, interpretable framework for reasoning-driven medical image analysis.

摘要：儘管多模態大型語言模型（MLLMs）在醫療視覺問題回答（Med-VQA）中展現了令人印象深刻的表現，但它們對全球圖像特徵的依賴往往缺乏精確的像素級基礎，從而限制了臨床的可信度。為了彌補高層次臨床推理與空間定位之間的語義差距，我們提出了 \textsc{\textsc{MedREAL}} （\textbf{Med}ical \textbf{RE}asoning-driven \textbf{A}nswering and \textbf{L}ocalization），這是一個無縫對齊語言推理與空間基礎的統一框架。具體而言，\textsc{MedREAL} 引入了 \textbf{S}eg \textbf{A}nchored \textbf{R}easoning \textbf{P}ooling（SARP），以直接從 MLLM 的隱藏狀態中的 \texttt{[SEG]} 標記中提煉與任務相關的語義證據。此外，提出了一種 \textbf{R}easoning-to-\textbf{V}isual（R2V）融合機制，以有效地將這些推理感知特徵注入到分割管道中，以實現準確的掩碼解碼。為了促進這一範式，我們構建了 MedRAVS-13K，這是一個包含 13,824 個專家驗證樣本的綜合數據集，涵蓋四種不同的成像模態。廣泛的實驗表明，\textsc{MedREAL} 顯著超越了現有技術，在基準評估中達到 68.49\% gIoU 和 70.47\% cIoU。通過生成與文本診斷嚴格一致的證據掩碼，\textsc{MedREAL} 提供了一個穩健、可解釋的框架，用於推理驅動的醫學影像分析。

##### **MedFG-VQA: Low-Frequency Memory and Graph Attention for Lightweight Medical VQA**
2608.26848v1 by Haowen Gu, Gensheng Pei, Zeren Sun, Mingwu Ren, Xiangbo Shu, Yazhou Yao, Fumin Shen

Medical Visual Question Answering (Med-VQA) holds significant promise for clinical decision support, yet faces challenges due to limited annotated data and the high computational demands of existing large vision-language models. We propose MedFG-VQA, a lightweight framework that leverages a memory bank to augment DCT-based low-frequency features and employs graph-enhanced cross-attention for effective visual-textual alignment. Specifically, our approach features two key components: Frequency-Memory Fusion (FMF), which enhances low-frequency features by retrieving from a learnable memory bank built on DCT decomposition, and Graph-Aware Cross-Attention (GACA), which aligns visual-textual features via cross-attention and refines them through graph-convolutional aggregation. To address data scarcity, we construct SynMed-VQA, a large-scale synthetic dataset comprising over 2 million question-answer pairs across 9 imaging modalities and 10 major organs, generated with GPT-4o. Extensive experiments on SynMed-VQA and three other standard biomedical VQA benchmarks demonstrate that MedFG-VQA achieves competitive or superior performance compared to much larger models while maintaining significantly lower computational costs, highlighting its efficiency and potential for clinical deployment.

摘要：醫療視覺問題回答（Med-VQA）在臨床決策支持方面具有重要潛力，但由於標註數據有限以及現有大型視覺-語言模型的高計算需求，面臨挑戰。我們提出了 MedFG-VQA，一個輕量級框架，利用記憶庫來增強基於 DCT 的低頻特徵，並採用圖增強的交叉注意力來實現有效的視覺-文本對齊。具體而言，我們的方法包含兩個關鍵組件：頻率-記憶融合（FMF），通過從基於 DCT 分解構建的可學習記憶庫中檢索來增強低頻特徵，以及圖感知交叉注意力（GACA），通過交叉注意力對視覺-文本特徵進行對齊，並通過圖卷積聚合進行精煉。為了解決數據稀缺問題，我們構建了 SynMed-VQA，一個大型合成數據集，包含超過 200 萬個問題-答案對，涵蓋 9 種成像模式和 10 個主要器官，使用 GPT-4o 生成。在 SynMed-VQA 和其他三個標準生物醫學 VQA 基準上的廣泛實驗表明，MedFG-VQA 在性能上與更大型模型相當或優越，同時保持顯著較低的計算成本，突顯了其效率及在臨床部署中的潛力。

##### **Accelerating Scientific Research with Gemini in the Real-World**
2608.26701v1 by Samuel Schmidgall, Xiaokai Zhu, Marian Shaw, Lin Yang, Valentin Liévin, Jingyun Yang, Yuchen Zhuang, Tim Strother, Alex Bijamov, Min Woo Sun, Anil Palepu, Justin Chen, David Steiner, Jacqueline Shreibati, Wei-Hung Weng, Yilin Zhao, Xingjian Hu, Nicholas Zahn, Sadhya Garg, Julia Kirby, Yuxiang Gan, Jiaoli Li, Divy Thakkar, Shekoofeh Azizi, David Racz, Juraj Gottweis, Vivek Natarajan, Chenglin Wu, Tal Danino, Keran Rong, Haozhe Wang, Benoit Schillings, Yong Cheng, Quoc V. Le, Tao Tu

We present an extension and comprehensive real-world validation of Co-Scientist, a Gemini-based multi-agent system designed to accelerate end-to-end scientific research across hypothesis generation, experimentation, and manuscript generation. Moving beyond in silico hypothesis generation, this specialized configuration transitions Co-Scientist into an execution-grounded research partner advancing closed-loop scientific workflows across materials science, biology, and computer science. In materials science, Co-Scientist interfaced with a semi-automated chemical vapor deposition reactor to design a safe precursor route for MXenes; experimental execution produced a lamellar 2D material sharing key structural similarities with the Ti3C2Tx MXene lattice, although further experiments are needed to confirm the atomic structure. Leveraging Gemini 3 Deep Think for rapid, lab-in-the-loop execution, it also tailored growth recipes to laboratory constraints in minutes, enabling single-attempt growth of monolayer MoS2, MoSe2, and WS2 semiconductors. In biology, Co-Scientist predicted emergent swarming phenotypes of engineered E. coli across inducer (IPTG) gradients from sparse imaging data, quantitatively matching unpublished wet-lab morphological measurements. In computer science, Co-Scientist autonomously discovered an inference-time scaling architecture that outperformed six frontier models on HealthBench (Hard and Professional) while reducing potential clinical harm under blinded physician evaluation. Finally, a double-blind study of end-to-end generated papers with 30 domain experts across 450 reviews demonstrates that Co-Scientist's reliability modules reduce hallucination and plagiarism while improving research safety. Together, these results demonstrate progress toward closed-loop multi-agent scientific AI systems capable of accelerating real-world scientific discovery.

摘要：我們展示了Co-Scientist的擴展和全面的實際驗證，這是一個基於Gemini的多代理系統，旨在加速從假設生成、實驗到手稿生成的端到端科學研究。超越了計算機模擬的假設生成，這一專門配置將Co-Scientist轉變為一個以執行為基礎的研究夥伴，推進材料科學、生物學和計算機科學中的閉環科學工作流程。在材料科學中，Co-Scientist與半自動化的化學氣相沉積反應器進行了接口，以設計MXenes的安全前驅體路徑；實驗執行產生了一種層狀的2D材料，與Ti3C2Tx MXene晶格具有關鍵的結構相似性，儘管還需要進一步的實驗來確認原子結構。利用Gemini 3 Deep Think進行快速的實驗室內執行，它還在幾分鐘內根據實驗室的限制量身定制生長配方，使得單次嘗試能夠生長單層MoS2、MoSe2和WS2半導體。在生物學中，Co-Scientist預測了工程化大腸桿菌在誘導劑（IPTG）梯度下的 emergent swarming 表型，並且從稀疏成像數據中定量匹配了未發表的濕實驗形態測量。在計算機科學中，Co-Scientist自主發現了一種推理時間擴展架構，該架構在HealthBench（Hard和Professional）上超越了六個前沿模型，同時在盲法醫生評估下減少了潛在的臨床傷害。最後，對30位領域專家的450次評審進行的雙盲研究顯示，Co-Scientist的可靠性模塊減少了幻覺和抄襲，同時提高了研究安全性。綜合這些結果顯示，朝著能夠加速現實世界科學發現的閉環多代理科學AI系統邁出了進展。

##### **Planetary Prediction Engine: Autonomous Geospatial Prediction via Intelligent Data Selection and Foundation Model Embeddings**
2608.26088v1 by Evelyn Ma, Rama Kumar Pasumarthi, Kishwar Shafin, Mandar Sharma, Mimi Sun, Hamed Sadeghi, Dav M. Ebengo, Mbulayi Onesime, Rouslan Solomakhin, John Wamburu, William Ogallo, Aisha Walcott-Bryant, Sanxing Chen, Arbaaz Muslim, Yael Mayer, Ronald Ho, Roy Lee, Ruth Alcantara, Abdoulaye Diack, Monica Bharel, Lambert Rosique, Jeremy Amez-Droz, Christopher Haire, James Manyika, Yossi Matias, Niv Efron, Gautam Prasad, Shravya Shetty

Addressing critical global challenges, from food security and disaster risk to disease outbreaks and socio-economic vulnerability, demands high-fidelity geospatial modeling. However, building predictive planetary models remains bottlenecked by a fragmented data ecosystem, requiring manual data retrieval, multimodal data curation and fusion along with iterative model selection. We present the Planetary Prediction Engine (PPE), an autonomous AI system that executes this end-to-end workflow directly from natural-language queries. PPE synthesizes multimodal datasets on the fly, retrieving spatiotemporally relevant covariates across open-web and Earth observation platforms (Data Commons, Google Earth Engine) and fusing them with geospatial foundation model embeddings (PDFM, AlphaEarth). Simultaneously, it searches over task-tailored model architecture families with automated overfitting guards. Across diverse tasks, geographies, and scientific domains, PPE consistently outperforms state-of-the-art or manually tuned expert baselines. For US spatial regression, PPE improves mean $R^2$ across 21 CDC health indicators (76.8% vs. 60.0%), FEMA national risk indices (64.9% vs. 60.0%), and the Social Vulnerability Index (66.2% vs. 58.6%). For spatial downscaling in data-scarce settings, PPE integrates localized proxies to double baseline accuracy in Nigerian food security indicators ($R^2$ of 66.1% vs. 31.5%). For epidemiological nowcasting of the 2026 DRC Bundibugyo Ebola outbreak, PPE achieves a Recall@10 of 83.3% (identifying 15 of 18 newly invaded health zones across five weekly forecasts), a +10.3 percentage-point improvement over the public state-of-the-art modeling (~73%). By combining autonomous multimodal planetary data discovery with targeted model optimization, PPE lowers the technical barrier to planetary-scale analytics, enabling rapid, customized, expert-level deployment.

摘要：解決全球重大挑戰，包括糧食安全、災害風險、疾病爆發和社會經濟脆弱性，需求高保真度的地理空間建模。<lb/>然而，建立預測性行星模型仍然受到碎片化數據生態系統的瓶頸，這需要手動數據檢索、多模態數據策劃和融合，以及迭代模型選擇。<lb/>我們提出了行星預測引擎（PPE），這是一個自主的人工智慧系統，能夠直接從自然語言查詢執行這一端到端的工作流程。<lb/>PPE 即時合成多模態數據集，從開放網絡和地球觀測平台（數據公共、Google Earth Engine）檢索時空相關的協變量，並將其與地理空間基礎模型嵌入（PDFM、AlphaEarth）融合。<lb/>同時，它在自動過擬合防護的情況下，搜尋針對任務量身定製的模型架構家族。<lb/>在多樣的任務、地理區域和科學領域中，PPE 始終超越最先進或手動調整的專家基準。<lb/>對於美國的空間回歸，PPE 在 21 項 CDC 健康指標上提高了平均 $R^2$（76.8% 對 60.0%）、FEMA 國家風險指數（64.9% 對 60.0%）和社會脆弱性指數（66.2% 對 58.6%）。<lb/>在數據稀缺的環境中進行空間下採樣時，PPE 整合了本地化的代理，將尼日利亞糧食安全指標的基準準確率提高了一倍（$R^2$ 為 66.1% 對 31.5%）。<lb/>對於 2026 年剛果民主共和國 Bundibugyo 埃博拉疫情的流行病學即時預測，PPE 實現了 83.3% 的 Recall@10（在五次每週預測中識別出 15 個新入侵的健康區域中的 18 個），比公共最先進模型提高了 10.3 個百分點（約 73%）。<lb/>通過將自主的多模態行星數據發現與針對性的模型優化相結合，PPE 降低了行星級分析的技術門檻，使快速、定制的專家級部署成為可能。

##### **FRAME: separating sampling variation from representational cause in medical imaging fairness**
2608.25981v1 by Mahshad Lotfinia, Daniel Truhn, Andreas Maier, Soroosh Tayebi Arasteh

Subgroup performance differences are the standard evidence for fairness bias in medical imaging, and the usual response removes the demographic information that a model encodes. Here we introduce Fair-model Reference And Mechanism Evaluation (FRAME), a two-step framework for auditing such a claim. The first step derives a fair-model reference, the distribution of the difference under exact fairness at the observed subgroup sizes. In the second step, we test the remainder with two operators in representation space. One operator cannot change a within-group ranking by construction. Across 702,206 images and 36 encoders, the reference accounts for a median 41% of the reported race difference and 22% of the age difference. Injecting demographic decodability leaves the remainder unchanged, while entangling the group with the disease direction raises the race difference from 0.077 to 0.118. No intervention we tested changes the remainder more than a change of random seed does. Those interventions reduce a difference at the operating point and leave the within-group ranking difference at a median of 0.000. Applied to 89 differences in 9 published studies across 6 medical imaging modalities, the reference accounts for a median 25% of a rate difference and 70% of a difference in the area under the receiver operating characteristic curve. Image-text pretraining instead raises worst-group performance by about 0.05. Applying FRAME before choosing an intervention could distinguish differences that need a mechanistic explanation from differences compatible with sampling variation at the current cohort sizes.

摘要：子群體表現差異是醫學影像中公平偏見的標準證據，通常的回應是去除模型編碼的人口統計信息。在這裡，我們介紹公平模型參考與機制評估（FRAME），這是一個用於審核此類主張的兩步框架。第一步推導出公平模型參考，即在觀察到的子群體大小下，精確公平的差異分佈。在第二步中，我們使用表示空間中的兩個運算符來測試其餘部分。一個運算符在結構上無法改變群內排名。跨越702,206張影像和36個編碼器，該參考解釋了報告的種族差異的中位數41%和年齡差異的22%。注入人口統計可解碼性使其餘部分保持不變，而將群體與疾病方向糾纏則將種族差異從0.077提高到0.118。我們測試的任何干預都沒有比隨機種子變化更改其餘部分。這些干預在操作點上減少了差異，並將群內排名差異保持在中位數0.000。應用於6種醫學影像模式中9項已發表研究的89個差異，該參考解釋了率差異的中位數25%和接收者操作特徵曲線下的面積差異的70%。圖像-文本預訓練反而將最差群體的表現提高了約0.05。在選擇干預之前應用FRAME，可以區分需要機制解釋的差異與與當前隊列大小相容的抽樣變異的差異。

##### **PANDA - Prototype-Anchored Alignment for Partially Unpaired Multimodal Learning, with Applications to Alzheimers MRI and TCGA Pathology**
2608.25970v1 by Sheethal Bhat, Mahfuzur Rahman Chowdhury, Paula Andrea Perez-Toro, Stephan Wunderlich, Rose Dawn Bharat, Siming Bayer, Andreas Maier

Multimodal medical prediction often faces incomplete pairing: auxiliary modalities with complementary signal are available for only a subset of subjects (or none) and cannot be assumed at deployment. We introduce PANDA (Prototype Anchored Data Alignment), a two-stage framework that transfers auxiliary information to a primary-modality model without auxiliary inputs at inference. Stage 1 learns a shared embedding from the paired subset and estimates class prototypes from auxiliary modalities; Stage 2 trains the primary encoder on all subjects using cross-entropy plus alignment to the frozen prototypes. Because supervision is defined at the class-prototype level, PANDA accommodates arbitrary pairing rates, including zero subject overlap. We evaluate PANDA on two applications. On a 1,021-subject multi-scanner ADNI cohort, we perform AD/CN classification with three auxiliary modalities at distinct pairing rates: tabular scores (44.8%), FDG-PET (18.7%), and external handwriting kinematics (0% overlap). Relative to the same-backbone MRI-only baseline, PANDA attains AUC 0.868 +-0.020 (+7.9pp) and reduces 1.5T CN false positives by 24.3pp; on a fully trainable Conv5-FC3 backbone it reaches AUC 0.893 (best overall). A pairing-rate ablation shows that the joint anchor remains within seed noise from 75% to 5% pairing. On TCGA-Lung survival prediction from whole-slide images with RNA-seq as auxiliary data, PANDA improves over WSI-only on 2-year OS (AUC +3.5pp) and Cox PH (C-index +9.0pts) and outperforms full-fusion training, which underperforms WSI-only, while requiring no RNA at inference; wide confidence intervals on this smaller cohort keep the gains below conventional significance. Overall, PANDA provides a deployment-oriented mechanism for leveraging incomplete auxiliary modalities to improve primary-modality prediction.

摘要：多模態醫療預測通常面臨不完整的配對：輔助模態的互補信號僅對部分受試者（或無）可用，且在部署時無法假設。我們引入了PANDA（原型錨定數據對齊），這是一個兩階段框架，能在推理時將輔助信息轉移到主要模態模型，而無需輔助輸入。第一階段從配對的子集學習共享嵌入，並從輔助模態估計類別原型；第二階段使用交叉熵加上對凍結原型的對齊，對所有受試者訓練主要編碼器。由於監督是在類別原型級別定義的，PANDA可以適應任意的配對率，包括零受試者重疊。我們在兩個應用上評估PANDA。在一個包含1,021名受試者的多掃描儀ADNI隊列中，我們在不同的配對率下進行AD/CN分類，使用三種輔助模態：表格分數（44.8%）、FDG-PET（18.7%）和外部手寫運動學（0%重疊）。相對於相同骨幹的僅MRI基線，PANDA達到AUC 0.868 ±0.020（+7.9pp），並將1.5T CN的假陽性減少了24.3pp；在完全可訓練的Conv5-FC3骨幹上，它達到AUC 0.893（最佳整體）。配對率消融實驗顯示，聯合錨定在75%到5%的配對中仍保持在種子噪聲內。在使用RNA-seq作為輔助數據的全切片圖像TCGA-Lung生存預測中，PANDA在2年OS上相較於僅WSI有所改善（AUC +3.5pp）和Cox PH（C-index +9.0pts），並且超越了全融合訓練，後者的表現不如僅WSI，同時在推理時不需要RNA；在這個較小的隊列上，寬廣的置信區間使得增益低於傳統顯著性。總體而言，PANDA提供了一種面向部署的機制，利用不完整的輔助模態來改善主要模態的預測。

##### **Why ML-based cough models do not generalize: a systematic cross-dataset evaluation for tuberculosis screening**
2608.25846v1 by Wensi Zhang, Tomas Teijeiro, Jérôme Thevenot, David Atienza

Cough acoustics are promising for non-invasive tuberculosis (TB) screening, yet whether machine learning (ML) models capture disease-related acoustics or artifacts of data collection remains unresolved. We evaluated the cross-dataset generalizability of classical ML and deep learning (DL) cough-based TB classifiers across three independent datasets. Despite moderate within-dataset performance (ROC-AUC up to $0.755 \pm 0.056$), both pipelines fail to generalize, with external performance frequently below 0.6, indicating a possible limitation of the data. We further observed audio representations are organized by recording device and dataset rather than TB status, predicted TB probability tracks country-level prevalence in CODA, and device mismatch degrades transfer while device-diverse training improves it. Additionally, a clinical-variable baseline generalizes more consistently (ROC-AUC $0.655 - 0.711$), indicating acquisition-specific variability is a stronger driver of poor generalizability than population shift. High within-dataset performance is not enough. External validation is essential before cough-based TB models are clinically ready.

摘要：咳嗽聲學在非侵入性結核病（TB）篩檢中具有潛力，但機器學習（ML）模型是否捕捉到與疾病相關的聲學或數據收集的工件仍然未解決。<lb/>我們評估了經典機器學習和深度學習（DL）基於咳嗽的結核病分類器在三個獨立數據集上的跨數據集泛化能力。<lb/>儘管在數據集內的表現中等（ROC-AUC高達$0.755 \pm 0.056$），但這兩個流程都未能泛化，外部表現經常低於0.6，這表明數據可能存在限制。<lb/>我們進一步觀察到音頻表示是根據錄音設備和數據集組織的，而不是根據結核病狀態，預測的結核病概率與CODA中的國家級流行率相符，設備不匹配會降低轉移效果，而設備多樣化的訓練則改善了轉移效果。<lb/>此外，臨床變量基線的泛化性更一致（ROC-AUC $0.655 - 0.711$），這表明獲取特定變異性是導致較差泛化能力的更強驅動因素，而不是人口變遷。<lb/>高的數據集內表現並不足夠。在基於咳嗽的結核病模型臨床準備之前，外部驗證至關重要。

##### **Unsupervised Anatomical Feature Learning via Diffusion Models: Enhanced Medical Image Segmentation with Denoising Diffusion Probabilistic Models**
2608.25693v1 by Akshat G, Divyansh Gupta, Shaleen Bhatnagar, Shilpa Ankalaki, Tusar Kanti Mishra

Acquiring pixel-level annotations for medical image segmentation is a severe bottleneck. Traditional U-Net architectures, while effective, learn local texture patterns and lack awareness of global anatomical structures, leading to boundary delineation failures in low-data regimes. This research paper proposes utilizing unsupervised Denoising Diffusion Probabilistic Models (DDPMs) to extract anatomical features. We train a DDPM on 21 unlabeled abdominal CT scans to learn structural representations, transferring the encoder weights to a downstream segmentation task evaluated on the BTCV multi-organ dataset. Diffusion pretraining significantly improved liver segmentation: Dice increased from $0.75\pm0.36$ to $0.93\pm0.16$ ($p < 5.33\times10^{-26}$, 0.529 Cohen's d), Average Surface Distance (ASD) decreased by 66%, and 95th-percentile Hausdorff Distance (HD95) reduced by 45%. For kidney segmentation, Dice improved from $0.90\pm0.19$ to $0.95\pm0.10$ ($p < 4.01\times10^{-11}$). Multi-organ pooled performance showed a 68% variance reduction and a 74% improvement in boundary precision (Dice $0.95\pm0.07$). Crucially, frozen encoder models retained > 80% of fine-tuned performance without exposure to segmentation labels, proving the existence of learned anatomical priors. In low-data scenarios, diffusion-pretrained models maintained robust performance with only 50% (Dice: 0.92 liver, 0.94 kidney), 25%, and even 10% (Dice: 0.89 liver, 0.71 kidney) of labeled data. Using unlabeled images for diffusion-based pretraining successfully embeds robust anatomical features prior to human supervision, transforming U-Nets into anatomy-aware systems.

摘要：獲取醫學影像分割的像素級註釋是一個嚴重的瓶頸。傳統的 U-Net 架構雖然有效，但學習的是局部紋理模式，缺乏對全局解剖結構的認識，導致在低數據環境下邊界劃分失敗。本研究論文提出利用無監督去噪擴散概率模型 (DDPMs) 來提取解剖特徵。我們在 21 個未標記的腹部 CT 掃描上訓練 DDPM，以學習結構表示，並將編碼器權重轉移到下游的分割任務中，該任務在 BTCV 多器官數據集上進行評估。擴散預訓練顯著改善了肝臟分割：Dice 從 $0.75\pm0.36$ 增加到 $0.93\pm0.16$ ($p &lt; 5.33\times10^{-26}$, 0.529 Cohen's d)，平均表面距離 (ASD) 減少了 66%，而第 95 百分位的豪斯多夫距離 (HD95) 減少了 45%。對於腎臟分割，Dice 從 $0.90\pm0.19$ 提高到 $0.95\pm0.10$ ($p &lt; 4.01\times10^{-11}$)。多器官的綜合性能顯示出 68% 的方差減少和 74% 的邊界精度改善 (Dice $0.95\pm0.07$)。關鍵是，凍結的編碼器模型在未接觸分割標籤的情況下保留了 > 80% 的微調性能，證明了學習到的解剖先驗的存在。在低數據場景中，擴散預訓練模型在僅使用 50% (Dice: 0.92 肝臟, 0.94 腎臟)、25% 甚至 10% (Dice: 0.89 肝臟, 0.71 腎臟) 的標記數據下保持了穩健的性能。使用未標記的影像進行基於擴散的預訓練成功地嵌入了穩健的解剖特徵，並在人工監督之前轉變了 U-Net 成為具備解剖認知的系統。

##### **Hierarchical MoE for Multi-Modal ILD Diagnosis**
2608.25261v1 by Alec K. Peltekian, Gorkem Durak, Halil Ertugrul Aktas, Carrie Lynn Richardson, Mary Carns, Kathleen Aren, GR Scott Budinger, Anthony J. Esposito, Alexander Misharin, Alok Nidhi Choudhary, Ankit Agrawal, Ulas Bagci

Mixture-of-experts (MoE) models combine specialized predictors under learned routing, offering a principled mechanism for leveraging heterogeneity in medical data. We present a hierarchical multimodal MoE for interstitial lung disease (ILD) classification that integrates a frozen, pre-trained imaging expert with structured electronic health records (EHR) via two-stage gating. A modality-level gate assigns patient-specific weights to imaging and EHR predictions, while a sub-gating module decomposes the EHR branch into clinically defined feature groups with learned, group-specific contributions. This design preserves stable imaging representations while enabling input-dependent clinical weighting and explicit EHR specialization. Under strict patient-level cross-validation, the model achieved the highest mean AUC among the evaluated methods (0.8750 +- 0.0443), compared with 0.8646 for imaging-only REN and 0.7685 for SwinUNETR. The framework extends interpretability across anatomical regions, imaging--EHR utilization, and clinically defined EHR feature groups.

摘要：混合專家（MoE）模型在學習路由下結合了專門的預測器，提供了一種利用醫療數據異質性的原則性機制。我們提出了一種層次化的多模態 MoE，用於間質性肺病（ILD）分類，該模型通過兩階段閘控將凍結的預訓練影像專家與結構化電子健康紀錄（EHR）整合在一起。模態級閘控為影像和 EHR 預測分配特定於患者的權重，而子閘控模塊則將 EHR 分支分解為臨床定義的特徵組，並具有學習到的特定於組的貢獻。這一設計保持了穩定的影像表示，同時使得輸入依賴的臨床加權和明確的 EHR 專業化成為可能。在嚴格的患者級交叉驗證下，該模型在評估的方法中達到了最高的平均 AUC（0.8750 ± 0.0443），相比之下，影像專用的 REN 為 0.8646，SwinUNETR 為 0.7685。該框架擴展了在解剖區域、影像—EHR 利用和臨床定義的 EHR 特徵組之間的可解釋性。

##### **What Do Medical Vision-Language Models Learn in Radiology? Transfer, Alignment, and Source-Proxy Leakage Under Distribution Shift**
2608.25251v1 by Ayoub Louaye Bouaziz, Lokmane Chebouba, Yassine Himeur

Medical vision-language models (VLMs) can appear reliable in-domain while failing when acquisition domain, paired supervision, or evaluation protocol changes. We study this failure mode as a representation-level blind spot relevant to epistemic intelligence, without claiming a formal estimator of epistemic uncertainty. Using NIH ChestXray14 and CheXpert, we first isolate source-only cross-dataset visual transfer from unsupervised domain-adaptation diagnostics. Using PadChest and OpenI, we then evaluate multimodal alignment under strict pair-index retrieval and quantify metadata-derived source-proxy information retained in frozen embeddings. Self-supervised visual initialization improves NIH-to-CheXpert transfer over supervised ImageNet initialization in matched ResNet-18 comparisons, whereas adversarial adaptation is useful only in a narrow regime and becomes unstable as adversarial pressure increases. Multimodal exact-pair retrieval remains low under external OpenI stress testing, and source-proxy information remains recoverable from learned representations. Qualitative nearest-neighbor and Grad-CAM analyses show clinically plausible cross-dataset structure and thoracic attention patterns in many cases, while device-heavy and false-positive cases remain ambiguous. Auxiliary architecture checks are task-dependent and do not support a universal backbone ranking. Overall, the study shows that apparent competence under a single protocol can conceal transfer, alignment, and shortcut-related failure modes, motivating stress-tested evaluation of medical VLMs under distribution shift.

摘要：醫療視覺語言模型（VLMs）在特定領域中看似可靠，但當獲取領域、配對監督或評估協議改變時，便會失效。我們研究這種失效模式，將其視為與認識智能相關的表徵層盲點，而不聲稱有一個正式的認識不確定性估計器。使用NIH ChestXray14和CheXpert，我們首先將僅來源的跨數據集視覺轉移與無監督領域適應診斷分離。接著使用PadChest和OpenI，我們在嚴格的配對索引檢索下評估多模態對齊，並量化在凍結嵌入中保留的元數據衍生來源代理信息。自我監督的視覺初始化在與監督的ImageNet初始化相比的匹配ResNet-18比較中，改善了NIH到CheXpert的轉移，而對抗適應僅在狹窄範圍內有用，並隨著對抗壓力的增加而變得不穩定。在外部OpenI壓力測試下，多模態精確配對檢索仍然較低，且來源代理信息仍可從學習的表徵中恢復。定性最近鄰和Grad-CAM分析顯示，在許多情況下，臨床上合理的跨數據集結構和胸腔注意模式，而設備繁重和假陽性案例則仍然模糊。輔助架構檢查是任務依賴的，並不支持普遍的骨幹排名。總體而言，這項研究顯示，在單一協議下表面上的能力可能掩蓋轉移、對齊和捷徑相關的失效模式，促使對醫療VLMs在分佈變化下進行壓力測試評估。


### Knowledge Graphs
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-09-03**|**Knowledge Acquisition During Pre-training? Large Language Models Learn Better With Auxiliary Views**|Joseph Lee et.al.|[2609.04180v1](http://arxiv.org/abs/2609.04180v1)|null|
|**2026-09-03**|**A Case Study on Emergent Cheating and Whistleblowing in Autonomous Research Swarms**|Davide Paglieri et.al.|[2609.04170v1](http://arxiv.org/abs/2609.04170v1)|null|
|**2026-09-03**|**SENTINEL-RL: Offloading Topological Reasoning from LLM Agents in the Security Operations Center**|Uday Vallabhaneni et.al.|[2609.04159v1](http://arxiv.org/abs/2609.04159v1)|null|
|**2026-09-03**|**InSituMeasure: Probing Situated Measurement Grounding in Industrial Scenes with Multimodal Large Language Models**|Chao Shen et.al.|[2609.04014v1](http://arxiv.org/abs/2609.04014v1)|null|
|**2026-09-03**|**The Blind Spot in 2D Infants' Pose Estimation:Robust Learning from Noisy Annotations**|Emanuele Cardinale et.al.|[2609.04009v1](http://arxiv.org/abs/2609.04009v1)|null|
|**2026-09-03**|**IchthyoNoma: Nomenclature and Context Sensitivity of Zero-Shot Biological Vision--Language Models for Bangladeshi Freshwater Fish Recognition**| Nazim-E-Alam et.al.|[2609.03985v1](http://arxiv.org/abs/2609.03985v1)|null|
|**2026-09-03**|**Investigating the Ability of Large Language Models to Analyze Recipes for Diabetes**|Revathy Venkataramanan et.al.|[2609.03967v1](http://arxiv.org/abs/2609.03967v1)|null|
|**2026-09-03**|**GraFT: A Training-Free Framework for Spatial Reasoning in Multimodal Large Language Models via 3D Scene Graphs**|Junqing Du et.al.|[2609.03892v1](http://arxiv.org/abs/2609.03892v1)|null|
|**2026-09-03**|**Adapting to Evolving Requirements: Agentic AI for Retail Supply Chain Operations**|Lei Zheng et.al.|[2609.03860v1](http://arxiv.org/abs/2609.03860v1)|null|
|**2026-09-03**|**Semantic Bayesian World Models**|Tommaso Soru et.al.|[2609.03834v1](http://arxiv.org/abs/2609.03834v1)|null|
|**2026-09-03**|**A Reverse Sign Language Dictionary: Open-Vocabulary Sign Recognition from Continuous Signing via Video Captioning and Description Retrieval**|Santiago Poveda-Gutiérrez et.al.|[2609.03788v1](http://arxiv.org/abs/2609.03788v1)|null|
|**2026-09-03**|**SimSkill: A Lifelong Learning AI Agent for Autonomous Mastery of Traffic Simulation**|Qi Liu et.al.|[2609.03753v1](http://arxiv.org/abs/2609.03753v1)|null|
|**2026-09-03**|**KnowVis: Knowledge-Centric Visual Summarization for Video Lectures**|Yi Xu et.al.|[2609.03742v1](http://arxiv.org/abs/2609.03742v1)|null|
|**2026-09-03**|**Can LLMs Extract Architectural Design Decisions from Source Code Commits? - A Preliminary Exploratory Study**|Amey Karan et.al.|[2609.03721v1](http://arxiv.org/abs/2609.03721v1)|null|
|**2026-09-03**|**What Do CAE Simulation Agents Really Need Beyond a Generic Harness?**|Jiasheng Shi et.al.|[2609.03718v1](http://arxiv.org/abs/2609.03718v1)|null|
|**2026-09-03**|**A computable representation of the physical laboratory enables verifiable workflows**|Xiaobo Li et.al.|[2609.03621v1](http://arxiv.org/abs/2609.03621v1)|null|
|**2026-09-03**|**LevelSyn: Physical-Aware Logic Synthesis via Level-Asynchronous Graph Neural Networks**|Jingyi Zhou et.al.|[2609.03594v1](http://arxiv.org/abs/2609.03594v1)|null|
|**2026-09-03**|**KC-Bench: A Dynamic Interactive Benchmark for Evaluating Knowledge Conflicts in LLM Agents**|Yaxing Lyu et.al.|[2609.03588v1](http://arxiv.org/abs/2609.03588v1)|null|
|**2026-09-03**|**GPS-Bench: A Governance Policy Benchmark for Automating Policy Analysis**|Linh Le et.al.|[2609.03553v1](http://arxiv.org/abs/2609.03553v1)|null|
|**2026-09-03**|**LeanGRPO: Eliminating Redundant Recomputation in Diffusion RL**|Sijie Wang et.al.|[2609.03528v1](http://arxiv.org/abs/2609.03528v1)|null|
|**2026-09-03**|**NeoRed: A Knowledge-Logic-Alignment Multimodal Large Language Model for Neonatal Respiratory Disease Diagnosis**|Yinan Liu et.al.|[2609.03527v1](http://arxiv.org/abs/2609.03527v1)|null|
|**2026-09-03**|**CulturalMenuBench: Probing the Knowledge-Application Gap in Multimodal Culinary Reasoning**|Bo Zeng et.al.|[2609.03526v1](http://arxiv.org/abs/2609.03526v1)|null|
|**2026-09-03**|**Making Every Tool Call Count: Necessary Tool-Evidence Path Rewards for Agentic Vision-Language Models**|Xingming Long et.al.|[2609.03493v1](http://arxiv.org/abs/2609.03493v1)|null|
|**2026-09-03**|**Pattern Over-Generalization of Knowledge Graph Embedding**|Junsik Kim et.al.|[2609.03487v1](http://arxiv.org/abs/2609.03487v1)|null|
|**2026-09-03**|**AutoGraphForge: Towards Automated Graph Theory Discovery**|Ján Pastorek et.al.|[2609.03478v1](http://arxiv.org/abs/2609.03478v1)|null|
|**2026-09-03**|**Exploring the Potential of Contrastive Language-Image Pre-training for Multi-Source Remote Sensing Data**|Xiangyang Miao et.al.|[2609.03391v1](http://arxiv.org/abs/2609.03391v1)|null|
|**2026-09-03**|**FrameBench:A Language Understanding Benchmark Based on Frame Semantics**|Chihiro Yano et.al.|[2609.03370v1](http://arxiv.org/abs/2609.03370v1)|null|
|**2026-09-03**|**ALRA: Adaptive Local Relational Alignment for Logit-Based Pre-training Distillation of Autoregressive Language Models**|Quang Hoang Trung et.al.|[2609.03355v1](http://arxiv.org/abs/2609.03355v1)|null|
|**2026-09-03**|**From Zero to Hero: An Open LLM Ecosystem for Armenian**|Erik Arakelyan et.al.|[2609.03350v1](http://arxiv.org/abs/2609.03350v1)|null|
|**2026-09-03**|**PACE: Towards Surfacing Hidden Conflicts in User Requests**|Yoojin Kim et.al.|[2609.03293v1](http://arxiv.org/abs/2609.03293v1)|null|
|**2026-09-02**|**LLMs Learn Better In-Context from Rules than from Examples**|Xiang Fu et.al.|[2609.03213v1](http://arxiv.org/abs/2609.03213v1)|null|
|**2026-09-02**|**MemoryLACE: Memory Lifecycle-Aware Consolidation and Evidence Retrieval**|Meriem Yacoubi et.al.|[2609.03201v1](http://arxiv.org/abs/2609.03201v1)|null|
|**2026-09-02**|**Large Language Models in Resolving Contextual Knowledge Conflicts**|Xinye Yang et.al.|[2609.03148v1](http://arxiv.org/abs/2609.03148v1)|null|
|**2026-09-02**|**Towards Trustworthy Autonomous Robots: An Explainable AI-Based Decision Framework**|Cagri Temel et.al.|[2609.02861v1](http://arxiv.org/abs/2609.02861v1)|null|
|**2026-09-02**|**Post-Training Language Models for Gold-Medal Performance in Coding Competitions**|Aleksander Ficek et.al.|[2609.02849v1](http://arxiv.org/abs/2609.02849v1)|null|
|**2026-09-02**|**Evaluating Graph Neural Networks for Change-Criticality Classification in Maritime Navigation Charts**|Abhishek Potnis et.al.|[2609.02996v1](http://arxiv.org/abs/2609.02996v1)|null|
|**2026-09-02**|**Large Language Models (LLMs) for Telecom Root Cause Analysis (RCA): A Structured Reasoning Framework for Evidence-Grounded Diagnosis**|Hao Zhou et.al.|[2609.02805v1](http://arxiv.org/abs/2609.02805v1)|null|
|**2026-09-02**|**frb100-40 After Two Decades: An Optimality Certificate and a Preregistered Search Study**|Onur Uğurlu et.al.|[2609.02804v1](http://arxiv.org/abs/2609.02804v1)|null|
|**2026-09-02**|**Repo-To-Skill: Distilling GitHub Repositories Into AI4AI Skills**|Jianlyu Chen et.al.|[2609.02749v1](http://arxiv.org/abs/2609.02749v1)|null|
|**2026-09-02**|**HiPoly: a hierarchical polymer-native AI framework for property prediction and generative design**|Ge Sun et.al.|[2609.02746v1](http://arxiv.org/abs/2609.02746v1)|null|
|**2026-09-02**|**DKL: Decoupled Knowledge Learning for Instruction-Tuned Language Models**|Kushagra Bhushan et.al.|[2609.02685v1](http://arxiv.org/abs/2609.02685v1)|null|
|**2026-09-02**|**Structure and Implementation of New Practical English Textbooks Driven by Artificial Intelligence**|Ya Wang et.al.|[2609.02981v1](http://arxiv.org/abs/2609.02981v1)|null|
|**2026-09-02**|**Spectral Initialization and Scheduled Graph Smoothness for Uncertain Knowledge Graph Completion**|Md Abrar Jahin et.al.|[2609.02519v1](http://arxiv.org/abs/2609.02519v1)|null|
|**2026-09-02**|**RINSE: Robust Target-Time Normality Estimation for Zero-Shot Graph Anomaly Detection**|Taufikur Rahman Fuad et.al.|[2609.02497v1](http://arxiv.org/abs/2609.02497v1)|null|
|**2026-09-02**|**NE-R1: Enhancing Named Entity Recognition Model via Reinforcement Learning**|Meixuan Chen et.al.|[2609.02366v1](http://arxiv.org/abs/2609.02366v1)|null|
|**2026-09-02**|**Towards a Foundational Ontology for Identifying and Resolving Contradictions in Dialogue-based Human-Robot Interactions**|Maitreyee Tewari et.al.|[2609.02364v2](http://arxiv.org/abs/2609.02364v2)|null|
|**2026-09-02**|**Subcellularly Resolved Single-Cell Embedding Learning with Transcriptomic data, Protein Structure and Localization Information**|Zhen Zhou et.al.|[2609.02344v1](http://arxiv.org/abs/2609.02344v1)|null|
|**2026-09-02**|**Entangled Representations Amplify Collateral Damage in Unlearning**|Evžen Wybitul et.al.|[2609.02285v1](http://arxiv.org/abs/2609.02285v1)|null|
|**2026-09-02**|**Auditory Illusion Benchmark for Large Audio Language Models**|Hayoon Kim et.al.|[2609.02277v1](http://arxiv.org/abs/2609.02277v1)|null|
|**2026-09-02**|**Privacy-Preserving Topology-Guided Safety for LLM-Based Multi-Agent Systems via Federated Graph Learning**|Jinxi Yu et.al.|[2609.02967v1](http://arxiv.org/abs/2609.02967v1)|null|
|**2026-09-02**|**Propose to Learn, Learn to Propose: Evaluability-Aware Assistance under Bounded Rationality**|Yifan Zhu et.al.|[2609.02242v1](http://arxiv.org/abs/2609.02242v1)|null|
|**2026-09-02**|**PhoenixNest-Video: Evidence-Grounded Multimodal Agent Framework for Automated Video Interview Assessment**|Fan Yuxuan et.al.|[2609.02231v1](http://arxiv.org/abs/2609.02231v1)|null|
|**2026-09-02**|**PEARL: Path-Entity Aligned Relational Learning with Contextual Subgraphs for Inductive Knowledge Graph Completion**|Yunchi Yang et.al.|[2609.02216v1](http://arxiv.org/abs/2609.02216v1)|null|
|**2026-09-02**|**FUSE: An Evaluating Framework for Dangerous Capabilities of LLMs**|Zhengyi Jin et.al.|[2609.02168v1](http://arxiv.org/abs/2609.02168v1)|null|
|**2026-09-02**|**OmegaUse-SOP: SOP Engineering for Professional Computer Use from Human Demonstrations**|Yixiong Xiao et.al.|[2609.02149v2](http://arxiv.org/abs/2609.02149v2)|null|
|**2026-09-02**|**A Power Law in Logarithm's Clothing: On the Scalability of Graph-Based Vector Search**|Sajad Faghfoor Maghrebi et.al.|[2609.02143v1](http://arxiv.org/abs/2609.02143v1)|null|
|**2026-09-02**|**EmoStance: Response-Side Affective-Orientation Control for Empathetic Response Generation via Emoji Weak Supervision**|Ziyuan Jin et.al.|[2609.02133v1](http://arxiv.org/abs/2609.02133v1)|null|
|**2026-09-02**|**C$^{3}$T: Counterfactual Causal Reasoning for Sentiment Shifts in Social-Media Conversation Trees**|S M Rafiuddin et.al.|[2609.02131v1](http://arxiv.org/abs/2609.02131v1)|null|
|**2026-09-02**|**MASkills: Continual Skills Optimization for Multi-Agent LLM Systems**|Huaiyuan Yao et.al.|[2609.02094v1](http://arxiv.org/abs/2609.02094v1)|null|
|**2026-09-02**|**Selective Knowledge Edit Reversal via Gated Singular Vector Shrinkage**|Weifeng Jiang et.al.|[2609.02091v1](http://arxiv.org/abs/2609.02091v1)|null|
|**2026-09-02**|**MineTRACE: An Evidence-Grounded Interactive Reasoning System for Mineral Prospectivity**|Yiran Zhang et.al.|[2609.02060v1](http://arxiv.org/abs/2609.02060v1)|null|
|**2026-09-02**|**HyGRAIL: Cost-Aware and Evidence-Grounded Scientific Hypothesis Discovery over Knowledge Graphs**|Yihang Sun et.al.|[2609.02056v1](http://arxiv.org/abs/2609.02056v1)|null|
|**2026-09-02**|**Seed-Anchored Budget-Bounded Graph Rendering for Question Answering on Industry-Standard Power-Grid Information and Exchange Models**|Jayakumar Manoharan et.al.|[2609.02011v1](http://arxiv.org/abs/2609.02011v1)|null|
|**2026-09-02**|**LexIssue: Benchmarking Legal Issue Identification in Chinese Civil Litigation**|Huiyuan Xie et.al.|[2609.02954v1](http://arxiv.org/abs/2609.02954v1)|null|
|**2026-09-01**|**Convergence Theory of Knowledge Distillation in Asynchronous P2P Gossip Learning Network**|Lucas Qingyang Fang et.al.|[2609.01952v1](http://arxiv.org/abs/2609.01952v1)|null|
|**2026-09-01**|**On-Policy Distillation Meets Off-Policy GRPO: Training Compact Instruction-Following Rerankers**|Vignesh Prabhakar et.al.|[2609.01947v1](http://arxiv.org/abs/2609.01947v1)|null|
|**2026-09-01**|**SSAKG 2.0: An Open-Source Package for Structural Associative Sequence Memory and Context-Based Retrieval**|Przemysław Stokłosa et.al.|[2609.01849v1](http://arxiv.org/abs/2609.01849v1)|null|
|**2026-09-01**|**Import What You Need: Learning When and How to Augment EHR Graphs with External Knowledge**|Chen Chen et.al.|[2609.01839v1](http://arxiv.org/abs/2609.01839v1)|null|
|**2026-09-01**|**Zeta-Lite: A Concurrent, Branchable In-Browser SQL Database for Agentic Memory**|Gene Zhang et.al.|[2609.01818v1](http://arxiv.org/abs/2609.01818v1)|null|
|**2026-09-01**|**Induction and Inquiry via Probabilistic Reasoning over Language and Code**|Wasu Top Piriyakulkij et.al.|[2609.01815v1](http://arxiv.org/abs/2609.01815v1)|null|
|**2026-09-01**|**TalkFa: A Unified Benchmark for Farsi Dialogue Generation and Understanding**|Neda Jamshidi et.al.|[2609.01810v1](http://arxiv.org/abs/2609.01810v1)|null|
|**2026-09-01**|**MemeCULT-1K: Benchmarking South Asian Cultural Context and Humor Understanding of Multimodal Models**|Tawsif Tashwar Dipto et.al.|[2609.01772v1](http://arxiv.org/abs/2609.01772v1)|null|
|**2026-09-01**|**A systematic Approach to constructing a Chance-and-Risk Matrix for Semiconductor Supply Chains**|Ema Salkić et.al.|[2609.01563v1](http://arxiv.org/abs/2609.01563v1)|null|
|**2026-09-01**|**Knowledge Distillation During Mid-Training Favors Reasoning over Factual Recall**|Jacqueline He et.al.|[2609.01532v1](http://arxiv.org/abs/2609.01532v1)|null|
|**2026-09-01**|**Relational-Core Graph Analytics Querying graphs at SQL scale, and why the node/edge model is a performance tax, not a truer picture of connected data**|Gene Zhang et.al.|[2609.01525v1](http://arxiv.org/abs/2609.01525v1)|null|
|**2026-09-01**|**Optimizing Byzantine Node Placement in Decentralized Federated Learning**|Edoardo Gabrielli et.al.|[2609.01495v1](http://arxiv.org/abs/2609.01495v1)|null|
|**2026-09-01**|**Neuro-Symbolic Geometric Abstraction (NeuSOGA): From Observations to Symbolic Mathematical Representations**|Qingde Li et.al.|[2609.01408v2](http://arxiv.org/abs/2609.01408v2)|null|
|**2026-09-01**|**Investigating Linear Probe Robustness to Linguistic Register, Medical Specialty, and Corpus Shifts in Medical QA**|Nishant Mishra et.al.|[2609.01361v1](http://arxiv.org/abs/2609.01361v1)|null|
|**2026-09-01**|**EDGE: Error Dependency Graph-Guided Multi-Error Attribution in Multi-Agent LLM Systems**|Jun Hou et.al.|[2609.01360v1](http://arxiv.org/abs/2609.01360v1)|null|
|**2026-09-01**|**SymFold: Synergizing Evolutionary and Structural Priors for Accurate Protein Inverse Folding**|Handong Wang et.al.|[2609.01353v1](http://arxiv.org/abs/2609.01353v1)|null|
|**2026-09-01**|**CHARM: Character Hallucination for Multicultural Role Play Benchmark**|Sunkyung Han et.al.|[2609.01352v1](http://arxiv.org/abs/2609.01352v1)|null|
|**2026-09-01**|**Probing Factual Knowledge Transfer with Training Data Interventions**|Romina Oji et.al.|[2609.01341v1](http://arxiv.org/abs/2609.01341v1)|null|
|**2026-09-01**|**Automated Event Log Generation from Unstructured Text Using Finetuned LLMs**|Maximilian Seeth et.al.|[2609.01320v1](http://arxiv.org/abs/2609.01320v1)|null|
|**2026-09-01**|**HiLRP: Toward One Trustworthy Explanation for Vision Transformer: Conservation-Valid Attribution via Attention Primitives**|Sathiyamohan Nishankar et.al.|[2609.01282v1](http://arxiv.org/abs/2609.01282v1)|null|
|**2026-09-01**|**Position Matters: Feature Inversion Attacks in ViT Split Inference with Token Reduction and Shuffling**|Stefano Leggio et.al.|[2609.01232v1](http://arxiv.org/abs/2609.01232v1)|null|
|**2026-09-01**|**CaRL-EM: Cost-Aware Reinforcement Learning for Entity Matching with LLMs**|Chaohui Guo et.al.|[2609.01195v1](http://arxiv.org/abs/2609.01195v1)|null|
|**2026-09-01**|**Athena: Vulnerability-Affected Library Identification via Knowledge Graph Completion**|Phong Trinh Duy et.al.|[2609.01187v1](http://arxiv.org/abs/2609.01187v1)|null|
|**2026-09-01**|**LLMPEDIA: Browsing, Verifying, and Comparing the Parametric Encyclopedic Knowledge of LLMs**|Muhammed Saeed et.al.|[2609.01182v1](http://arxiv.org/abs/2609.01182v1)|null|
|**2026-09-01**|**Causal Evidentiary Governance for High-Risk Machine Learning Systems**|Samah Kareem et.al.|[2609.01040v1](http://arxiv.org/abs/2609.01040v1)|null|
|**2026-09-01**|**On Synthesis of Metric Interval Temporal Logics**|Hsi-Ming Ho et.al.|[2609.01032v1](http://arxiv.org/abs/2609.01032v1)|null|
|**2026-09-01**|**A Network Science Perspective on Evaluating Deep Graph Generative Models**|Tianrui Mao et.al.|[2609.01015v1](http://arxiv.org/abs/2609.01015v1)|null|
|**2026-09-01**|**The zbMATH Open Knowledge Graph: Tracing Centuries of Mathematical Research**|Yuni Susanti et.al.|[2609.00969v1](http://arxiv.org/abs/2609.00969v1)|null|
|**2026-09-01**|**CoBRA: Learning Tool-Use Boundaries via Counterfactual Margins**|Wenhao Zou et.al.|[2609.00967v1](http://arxiv.org/abs/2609.00967v1)|null|
|**2026-09-01**|**DualStake: Dual-Path Confidence Calibration in Deep Research Agents**|Yinuo Xu et.al.|[2609.00935v1](http://arxiv.org/abs/2609.00935v1)|null|
|**2026-09-01**|**Context-Grounding Gains Are Mediated by Pre-existing Machinery: Auditing GRPO, SFT, and DPO**|Prakhar Gupta et.al.|[2609.00925v1](http://arxiv.org/abs/2609.00925v1)|null|
|**2026-09-01**|**Reinforcement Learning Enhanced LLM Agents for Complex Vehicle Routing Problems**|Yi Chen et.al.|[2609.00859v1](http://arxiv.org/abs/2609.00859v1)|null|
|**2026-09-01**|**Verifiable Disaster Storylines and Causal Knowledge Graphs: A Citation-Grounded Pipeline from Heterogeneous Humanitarian Sources**|Ivan Decostanzi et.al.|[2609.00858v1](http://arxiv.org/abs/2609.00858v1)|null|
|**2026-09-01**|**Reflect-SQL: A Self-Reflection Based Framework for Text-to-SQL**|Anupreksha Jain et.al.|[2609.02944v1](http://arxiv.org/abs/2609.02944v1)|null|
|**2026-09-01**|**Towards Generalizable Visually Grounded Exploration of Household Devices**|Linhao Zheng et.al.|[2609.00845v1](http://arxiv.org/abs/2609.00845v1)|null|
|**2026-09-01**|**TWIX: a Two-Stage Approach for End-To-End Named Entity Recognition and Relation Extraction**|Marco Martinelli et.al.|[2609.00832v1](http://arxiv.org/abs/2609.00832v1)|null|

#### Abstracts
##### **Knowledge Acquisition During Pre-training? Large Language Models Learn Better With Auxiliary Views**
2609.04180v1 by Joseph Lee, Yidi Huang, Dokyoon Kim, Shu Yang, Li Shen

Gaps remain in our understanding of how large language models (LLMs) acquire knowledge during pre-training. We posit that auxiliary views, reformulations of knowledge, are causally helpful for learning. We design controlled experiments to isolate this. First, we confirm that repetition is necessary for acquisition and clarify that paraphrasing helps only at smaller batch sizes. Second, holding the token budget fixed, allocating tokens from document repetition to auxiliary views improves learning, counterintuitively, even for factual recall. Third, the effectiveness of auxiliary views is not contingent on the strength of the teacher model that generates them. Fourth, we identify forms of knowledge, contextual and foundational, that aid learning in the presence of prior knowledge gaps. Finally, we examine how these effects manifest mechanistically via layer-wise biases and compression. Together, our findings suggest that auxiliary representations of knowledge, which arise naturally in large pre-training corpora, are a key factor in the success of pre-training and offer a plausible explanation for why data diversity matters.

摘要：在我們對大型語言模型（LLMs）在預訓練期間如何獲取知識的理解中仍然存在空白。我們假設輔助視角，即知識的重述，對學習有因果上的幫助。我們設計了控制實驗來隔離這一點。首先，我們確認重複對於獲取是必要的，並澄清重述僅在較小的批次大小中有幫助。其次，在固定的標記預算下，將來自文檔重複的標記分配給輔助視角，反而會改善學習，即使在事實回憶方面也是如此，這看似違反直覺。第三，輔助視角的有效性並不依賴於生成它們的教師模型的強度。第四，我們識別出幾種知識形式，包括上下文知識和基礎知識，有助於在存在先前知識空白的情況下進行學習。最後，我們檢查這些效果如何通過層級偏差和壓縮在機械上表現出來。綜合來看，我們的發現表明，知識的輔助表徵，這些表徵在大型預訓練語料庫中自然出現，是預訓練成功的關鍵因素，並提供了一個合理的解釋，說明為什麼數據多樣性是重要的。

##### **A Case Study on Emergent Cheating and Whistleblowing in Autonomous Research Swarms**
2609.04170v1 by Davide Paglieri, Logan Cross, Tim Genewein, Joel Z. Leibo, Nenad Tomasev, Alexander Sasha Vezhnevets

Multi-agent AI science ecosystems rely on agents possessing tools that allow them to communicate, coordinate, and build on each other's work. Yet this shared infrastructure can also introduce vulnerabilities by creating a substrate for the contagious spread of unintended and undesirable behaviors. We report a case study on a research collective of 100 autonomous LLM agents tasked with proving formal mathematical conjectures. Within the swarm, cheating spontaneously emerged and was later challenged by whistleblowers - both without any external intervention. When a single agent discovered an exploit in the evaluation system, it propagated across the collective via a shared knowledge library and later through peer-to-peer messages. Despite early reluctance, a cohort of agents adopted the exploit in response to competitive pressure. A separate group of agents produced an emergent counter-response: auditing fraudulent proofs, alerting peers across broadcast and private channels, staging boycotts, lodging formal complaints, and proposing validation patches. In recent incidents, agent swarms coordinated covertly through improvised side-channels (Dalton and Wallace, 2026; Greenblatt et al., 2026). Our setting differs: the same transparent channels that carried the exploit also gave non-cheating agents the visibility they needed to detect fraud, organize resistance, and enforce norms. We cast the problem of managing the agents' shared infrastructure as the knowledge commons governance problem (Ostrom, 1990). To protect the commons from exploits, we propose to adopt institutional mechanisms, such as graduated sanctioning and collective-choice rules, to support decentralized self-governance in autonomous swarms.

摘要：多代理人工智慧科學生態系統依賴於擁有工具的代理人，這些工具使他們能夠溝通、協調並在彼此的工作上進行建設。<lb/>然而，這種共享基礎設施也可能通過創造一個潛在的環境，引入脆弱性，導致意外和不良行為的傳染性擴散。<lb/>我們報告了一個案例研究，該研究涉及一個由100個自主大型語言模型代理組成的研究集體，任務是證明正式的數學猜想。<lb/>在這個群體中，作弊自發出現，並且後來被舉報者挑戰——這一切都是在沒有任何外部干預的情況下發生的。<lb/>當一個代理人發現評估系統中的漏洞時，它通過共享知識庫在集體中傳播，後來又通過點對點消息進行傳遞。<lb/>儘管最初有些猶豫，但一組代理人在競爭壓力下採用了這一漏洞。<lb/>另一組代理人則產生了一種新興的反應：審計欺詐證明，通過廣播和私密渠道警告同伴，舉行抵制，提出正式投訴，並提議驗證補丁。<lb/>在最近的事件中，代理群體通過即興的側通道進行了秘密協調（Dalton 和 Wallace, 2026; Greenblatt 等, 2026）。<lb/>我們的環境有所不同：同樣的透明渠道不僅傳遞了漏洞，還給予了非作弊代理所需的可見性，以檢測欺詐、組織抵抗並執行規範。<lb/>我們將管理代理人共享基礎設施的問題視為知識公地治理問題（Ostrom, 1990）。<lb/>為了保護公地免受漏洞的侵害，我們建議採用制度性機制，例如逐級制裁和集體選擇規則，以支持自主群體的去中心化自我治理。

##### **SENTINEL-RL: Offloading Topological Reasoning from LLM Agents in the Security Operations Center**
2609.04159v1 by Uday Vallabhaneni, Cassie L. Cagwin, David J. Wild

Large language model (LLM) agents are increasingly proposed as autonomous SOC analysts, but two limitations make them unreliable at enterprise scale: a finite context window cannot hold a multi-thousand-host authentication graph, and free-form generation offers no guarantee that a recommended containment action is consistent with the topology it operates on. We present Sentinel-RL, an agentic-SOC architecture that decouples topological reasoning from semantic reasoning: a heterogeneous graph attention encoder summarizes the live authentication subgraph into a fixed-dimensional state, a Proximal Policy Optimization (PPO) policy maps this state to a constrained set of investigative actions, and an LLM agent loop is restricted to consuming the policy's recommendations and producing analyst-readable narratives gated by a critic. We instantiate the system on the LANL Comprehensive, Multi-Source Cyber-Security Events dataset and the Indiana University Quartz HPC cluster, reporting four results: (i) a two-phase CREATE ingestion pattern loads a 24M-edge authentication subgraph into Neo4j in 14.2 minutes on a single 32-core node, roughly 24x faster than the canonical MERGE-based pipeline; (ii) a sliding-window alert engine reliably trips a 25-event/10-second threshold in <=2.5 s across 50 trials; (iii) PPO training over 200 iterations converges to a mean episodic return of 8.74+/-0.31, with held-out precision of 0.91 and recall of 0.87 on labeled red-team events; and (iv) the integrated containment loop completes a full detect-investigate-recommend-human-approve cycle in a median of 6.3 s. We contribute a reusable engineering pattern (the hot-node deadlock workaround), a portable HPC deployment pattern (anchor-node co-location), and an enterprise-readiness analysis covering false-positive economics, reversibility guarantees, audit compliance, and the human-approval boundary.

摘要：大型語言模型（LLM）代理被越來越多地提議作為自主的安全運營中心（SOC）分析師，但有兩個限制使其在企業規模上不可靠：有限的上下文窗口無法容納數千主機的身份驗證圖，而自由形式生成無法保證建議的遏制行動與其操作的拓撲一致。我們提出了 Sentinel-RL，一種將拓撲推理與語義推理解耦的代理SOC架構：一個異質圖注意力編碼器將實時身份驗證子圖總結為固定維度的狀態，一個近端策略優化（PPO）策略將此狀態映射到一組受限的調查行動，而LLM代理循環僅限於消耗該策略的建議並生成由批評者控制的分析師可讀敘述。我們在LANL綜合多源網絡安全事件數據集和印第安納大學Quartz HPC集群上實現了該系統，報告了四個結果：（i）一種兩階段的CREATE攝取模式在單個32核節點上將2400萬邊的身份驗證子圖加載到Neo4j中，耗時14.2分鐘，速度約為經典MERGE基礎管道的24倍；（ii）滑動窗口警報引擎在50次試驗中可靠地在<=2.5秒內觸發25事件/10秒的閾值；（iii）在200次迭代中，PPO訓練收斂到均值回報8.74+/-0.31，對標記的紅隊事件的保留精度為0.91，召回率為0.87；（iv）集成的遏制循環在中位數6.3秒內完成完整的檢測-調查-建議-人類批准循環。我們貢獻了一個可重用的工程模式（熱節點死鎖解決方法）、一個可攜式HPC部署模式（錨節點共置），以及一個涵蓋假陽性經濟學、可逆性保證、審計合規性和人類批准邊界的企業準備分析。

##### **InSituMeasure: Probing Situated Measurement Grounding in Industrial Scenes with Multimodal Large Language Models**
2609.04014v1 by Chao Shen, Xinyuan Li, Yunfan Zhou, Jianguo Yao, Haibing Guan, Zhihai Wang, Xijun Li

For trained operators, gauge reading requires little specialized knowledge, low cognitive effort, and high repeatability. Yet Multimodal Large Language Models (MLLMs) remain unreliable in continuous-valued measurement despite strong results on general multimodal benchmarks. Existing benchmarks expose this weakness but isolate measurement from realistic, knowledge-grounded settings, with limited situated context, specialized instruments, real-world noise, and matched diagnostic annotations, reducing realism and constraining root-cause analysis. We introduce InSituMeasure to evaluate situated measurement grounding. It contains 2,922 real industrial monitoring scenes across eight functional categories of professional engineering instruments, with dense gauge-attribute annotations and noise tags for failure diagnosis. We define metrics for numerical accuracy under predefined tolerances and unit consistency, rejection of fake or unanswerable tasks, and alignment between model failures and annotated error factors. Across 24 state-of-the-art MLLMs, the best model reaches only 25.7\% joint value-unit accuracy and 51.8\% confidence-diagnosis F1, revealing a substantial gap between general multimodal competence and reliable situated measurement. Further analysis identifies failures from text-induced shortcuts, overconfident responses, and authentic industrial noise, including mixed disturbances, viewpoint deviation, occlusion, and environmental interference.

摘要：對於受過訓練的操作員來說，儀表讀數幾乎不需要專門知識，認知負擔低且重複性高。<lb/>然而，儘管在一般多模態基準測試中表現出色，多模態大型語言模型（MLLMs）在連續值測量方面仍然不可靠。<lb/>現有的基準測試揭示了這一弱點，但將測量與現實的、基於知識的環境隔離，缺乏有限的情境背景、專業儀器、現實世界的噪音和匹配的診斷註釋，降低了現實感並限制了根本原因分析。<lb/>我們引入了InSituMeasure來評估情境測量的基礎。<lb/>它包含2,922個真實的工業監控場景，涵蓋八個專業工程儀器的功能類別，並附有密集的儀表屬性註釋和故障診斷的噪音標籤。<lb/>我們定義了在預定公差和單位一致性下的數值準確性指標，拒絕虛假或無法回答的任務，以及模型失敗與註釋錯誤因素之間的對齊。<lb/>在24個最先進的MLLM中，最佳模型僅達到25.7%的聯合值-單位準確性和51.8%的信心-診斷F1，顯示出一般多模態能力與可靠的情境測量之間存在顯著差距。<lb/>進一步分析確定了由文本引起的捷徑、過度自信的反應和真實工業噪音造成的失敗，包括混合擾動、視點偏差、遮擋和環境干擾。

##### **The Blind Spot in 2D Infants' Pose Estimation:Robust Learning from Noisy Annotations**
2609.04009v1 by Emanuele Cardinale, Marco Proietti, Alessandro Cacciatore, Maria Francesca Spadea, Lucia Migliorelli, Sara Moccia

Noisy annotations pose a significant challenge for supervised deep learning, as neural networks rely on large-scale, high-quality labeled data whose corruption can severely impair model performance. Although robustness to label noise has been extensively studied for classification tasks, it remains relatively underexplored in Pose Estimation (PE). This limitation becomes critical in clinical contexts, including neonatology, where PE of preterm infants is used to support the assessment of spontaneous motility, a key indicator of neurodevelopmental trajectories. In such settings, infants' images labeling is further hindered by visual challenges (e.g., keypoint self-occlusions, caregiver interference), making the annotation process inherently susceptible to errors. To tackle noisy annotations in PE, we introduce REliable keypoint selection via Memory of traINing Dynamics (REMIND), a clustering-based keypoint-selection strategy that exploits keypoint-wise training dynamics to identify noisy labels without assuming any prior knowledge of the noise distribution, thus enabling noise-free model training. When evaluated on the proprietary NeoPose dataset, comprising 46 videos of 46 preterm infants recorded in real clinical settings, REMIND correctly identifies noisy annotations across multiple corruption scenarios, achieving up to 93\% Area Under the Curve (AUC) with three different PE architectures used in the relevant literature. To our knowledge, this is the first study to explicitly address label noise in preterm infants' PE, paving the way for the design of trustworthy learning-based algorithms for infants'monitoring support when data quality cannot be guaranteed.

摘要：嘈雜的標註對於監督式深度學習構成了重大挑戰，因為神經網絡依賴於大規模、高質量的標記數據，而這些數據的損壞會嚴重影響模型性能。儘管對於分類任務，對標籤噪聲的穩健性已經進行了廣泛研究，但在姿勢估計（PE）方面仍然相對未被充分探索。這一限制在臨床環境中變得至關重要，包括新生兒科，其中對早產兒的PE用於支持自發運動能力的評估，這是神經發展軌跡的一個關鍵指標。在這樣的環境中，嬰兒圖像的標註進一步受到視覺挑戰（例如，關鍵點自遮擋、護理人員干擾）的影響，使得標註過程本質上容易出錯。為了解決PE中的嘈雜標註問題，我們引入了通過訓練動態記憶進行可靠關鍵點選擇（REMIND），這是一種基於聚類的關鍵點選擇策略，利用關鍵點的訓練動態來識別嘈雜標籤，而不假設任何噪聲分佈的先驗知識，從而實現無噪聲的模型訓練。在對專有的NeoPose數據集進行評估時，該數據集包含46段在真實臨床環境中錄製的46名早產兒的視頻，REMIND在多種損壞場景中正確識別了嘈雜標註，並在相關文獻中使用的三種不同PE架構下達到了最高93\%的曲線下面積（AUC）。據我們所知，這是第一項明確針對早產兒PE中的標籤噪聲的研究，為在數據質量無法保證的情況下設計可信的基於學習的算法以支持嬰兒監測鋪平了道路。

##### **IchthyoNoma: Nomenclature and Context Sensitivity of Zero-Shot Biological Vision--Language Models for Bangladeshi Freshwater Fish Recognition**
2609.03985v1 by  Nazim-E-Alam, Tarek Rahman, Md Kishor Morol

Zero-shot vision-language models (VLMs) are increasingly used as training-free species recognizers, but reported accuracy can reflect more than visual species knowledge. We audit CLIP, BioCLIP, BioCLIP2, and a multilingual Jina CLIP v2 control on seven freshwater-fish categories from two Bangladeshi sources (10,321 images). BioCLIP2 reaches 72.36% on BFF-15 with English common names and 68.91% on SylFishBD with scientific names, versus 25.15% and 14.40% for generic CLIP. BioCLIP2 Bengali prompts are near chance in balanced accuracy (14.22-14.29%); Jina partially recovers Bengali discrimination to 21.89% and 16.36%, but bare Bengali names return to 14.29% on both sources. Paired SylFishBD interventions show no significant weak-blur effect, modest losses from stronger blur/gray masking, a larger white-mask artifact, and strong species dependence. Zero-shot biological VLM scores therefore jointly reflect biological specialization, multilingual alignment, nomenclature, prompt formulation, and context.

摘要：零-shot 視覺語言模型（VLMs）越來越多地被用作無需訓練的物種識別器，但報告的準確性可能反映的不僅僅是視覺物種知識。<lb/>我們對 CLIP、BioCLIP、BioCLIP2 和多語言 Jina CLIP v2 控制進行了審核，涵蓋來自兩個孟加拉國來源的七個淡水魚類別（10,321 張圖片）。<lb/>BioCLIP2 在 BFF-15 上達到 72.36% 的準確率，使用英語通用名稱，而在 SylFishBD 上使用科學名稱的準確率為 68.91%，相比之下，通用 CLIP 的準確率為 25.15% 和 14.40%。<lb/>BioCLIP2 的孟加拉語提示在平衡準確率上接近隨機（14.22-14.29%）；Jina 部分恢復了孟加拉語的識別能力，達到 21.89% 和 16.36%，但純粹的孟加拉語名稱在兩個來源上均回落至 14.29%。<lb/>配對的 SylFishBD 介入顯示出沒有顯著的弱模糊效應，從較強的模糊/灰色遮罩中有適度損失，較大的白色遮罩工件，以及強烈的物種依賴性。<lb/>因此，零-shot 生物 VLM 的分數共同反映了生物專業化、多語言對齊、命名法、提示公式和上下文。

##### **Investigating the Ability of Large Language Models to Analyze Recipes for Diabetes**
2609.03967v1 by Revathy Venkataramanan, Aditya Luthra, Venkatesan Nadimuthu, Amit Sheth

Several studies have evaluated the ability of Large Language Models (LLMs) for meal planning, yielding positive outcomes. These models can process natural language inputs and leverage learned knowledge from their pretraining to generate meal plans. In this work, we investigate the ability of LLMs to analyze the suitability of given recipes for diabetes. The primary challenge for LLMs is to retrieve relevant dietary guidelines for diabetes, decompose recipes into ingredients and cooking methods, and apply these guidelines to determine the recipe's suitability. To study these challenges, we employ three kinds of prompts namely, (i) Direct Query Prompt (ii) Context-Guided Prompt, and (iii) Exemplary Context Prompt that incorporate different levels of diabetes dietary guidelines from medical sources. We introduce a benchmark dataset curated for this investigation consisting of 7607 recipes that include 3807 recipes suitable for diabetes and 3800 recipes not suitable for diabetes. Our results demonstrate that most LLMs are cautious in predicting recipes as suitable to prevent detrimental outcomes. Further, the models that can reason using the dietary guidelines performed better in predicting the suitability of recipes for diabetes. Overall, Mistral-7B and Llama 70B showed superior performance to their counterparts.

摘要：幾項研究評估了大型語言模型（LLMs）在餐點規劃方面的能力，結果顯示出正面的成果。這些模型能夠處理自然語言輸入，並利用其預訓練所學的知識來生成餐點計劃。在本研究中，我們探討LLMs分析給定食譜對糖尿病適用性的能力。LLMs面臨的主要挑戰是檢索與糖尿病相關的飲食指導方針，將食譜分解為成分和烹飪方法，並應用這些指導方針來判斷食譜的適用性。為了研究這些挑戰，我們使用三種提示，分別是（i）直接查詢提示（ii）上下文引導提示，以及（iii）示例上下文提示，這些提示包含來自醫學來源的不同層次的糖尿病飲食指導方針。我們引入了一個為此研究精心策劃的基準數據集，該數據集包含7607個食譜，其中包括3807個適合糖尿病的食譜和3800個不適合糖尿病的食譜。我們的結果顯示，大多數LLMs在預測食譜是否適合時都非常謹慎，以防止不良結果。此外，能夠運用飲食指導方針進行推理的模型在預測食譜對糖尿病的適用性方面表現更佳。總體而言，Mistral-7B和Llama 70B的表現優於其對手。

##### **GraFT: A Training-Free Framework for Spatial Reasoning in Multimodal Large Language Models via 3D Scene Graphs**
2609.03892v1 by Junqing Du, Fernando Ropero, Erkin Turkoz, Yanfeng Zhang, Lu Liu

3D spatial reasoning underpins understanding and acting in the physical world, yet it remains unreliable in current multimodal large language models (MLLMs). These models falter at precise geometric measurement, at transforming between egocentric and allocentric viewpoints, and at grounding fine-grained appearance. The most common remedies fine-tune the model on large-scale curated spatial-reasoning datasets or attach dedicated encoders for 3D geometry, which typically couples the solution to costly supervision and a specific backbone. We instead introduce GraFT, a training-free framework that supplies the missing 3D structure through a compact, easily maintained 3D scene graph (3DSG). From this 3DSG, GraFT provides three spatial reasoning capabilities: (1) deterministic geometry through symbolic tools, (2) allocentric layout through a bird's-eye-view (BEV) rendering, and (3) visual-attribute grounding through task-relevant egocentric frames. On ScanQA, GraFT improves every metric over the same-backbone baseline, raising CIDEr by 27%. On VSI-Bench, GraFT improves frozen MLLMs by up to 65%, surpassing every proprietary and general-purpose open-source baseline, and several prominent fine-tuned spatial models.

摘要：3D 空間推理是理解和在物理世界中行動的基礎，但在當前的多模態大型語言模型 (MLLMs) 中仍然不可靠。這些模型在精確的幾何測量、在自我中心和他中心視角之間的轉換以及在細緻外觀的基礎上表現不佳。最常見的解決方案是對大型策劃的空間推理數據集進行微調，或為 3D 幾何附加專用編碼器，這通常將解決方案與昂貴的監督和特定的主幹相結合。我們則提出了 GraFT，一個無需訓練的框架，通過一個緊湊、易於維護的 3D 場景圖 (3DSG) 提供缺失的 3D 結構。從這個 3DSG 中，GraFT 提供三種空間推理能力：(1) 通過符號工具獲得確定性幾何，(2) 通過鳥瞰圖 (BEV) 渲染獲得他中心佈局，以及 (3) 通過與任務相關的自我中心框架獲得視覺屬性基礎。在 ScanQA 上，GraFT 在同一主幹基準上提高了每個指標，CIDEr 提高了 27%。在 VSI-Bench 上，GraFT 對凍結的 MLLMs 提高了多達 65%，超越了每個專有和通用開源基準，以及幾個突出的微調空間模型。

##### **Adapting to Evolving Requirements: Agentic AI for Retail Supply Chain Operations**
2609.03860v1 by Lei Zheng, Liping Yang, Zihao Li, Guodong Lyu, Chaik Ming Koh, Chung-Piaw Teo

Retail supply chain operations rely on coupled decision modules that must adapt as requirements evolve. LLMs offer a natural-language interface for this task, but existing methods primarily focus on individual optimization models. Extending them to heterogeneous decision pipelines is challenging because a requirement may admit multiple intervention paths with different downstream effects. We formulate requirement-driven adaptation as the joint selection of an intervention route and an admissible module-level change, and propose a graph-constrained agentic framework in which domain agents expose admissible reformulation interfaces and a central processor searches over bounded intervention paths. Candidates are validated and compared using downstream KPIs. In collaboration with a large retail partner, we evaluate 100 warehouse requirements elicited from practitioner interviews, with GPT, Qwen, and DeepSeek as base LLMs. Relative to direct LLM reformulation, our framework improves correctness and end-to-end success across all three models, raising end-to-end success from 72--76% to 79--83%.

摘要：零售供應鏈操作依賴於必須隨著需求演變而調整的耦合決策模塊。<lb/>大型語言模型（LLMs）為這項任務提供了自然語言界面，但現有方法主要集中在單一優化模型上。<lb/>將它們擴展到異質決策管道是具有挑戰性的，因為一個需求可能允許多條干預路徑，並具有不同的下游影響。<lb/>我們將需求驅動的適應形式化為干預路徑和可接受模塊級變更的聯合選擇，並提出了一個圖約束的代理框架，其中領域代理暴露可接受的重構接口，而中央處理器在有限的干預路徑上進行搜索。<lb/>候選者通過下游關鍵績效指標（KPIs）進行驗證和比較。<lb/>在與一家大型零售合作夥伴的合作中，我們評估了從實踐者訪談中獲得的100個倉庫需求，使用GPT、Qwen和DeepSeek作為基礎LLMs。<lb/>相對於直接的LLM重構，我們的框架在所有三個模型中提高了正確性和端到端成功率，將端到端成功率從72--76%提升到79--83%。

##### **Semantic Bayesian World Models**
2609.03834v1 by Tommaso Soru

Knowledge graphs describe reality in crisp assertions, while the systems now consuming them, foundation models and autonomous agents, reason natively in probabilities. We argue that this mismatch is why the integration of language models and knowledge graphs remains a data-feeding pipeline rather than a unified reasoning architecture. We envision Semantic Bayesian World Models (SBWMs): a Web that describes the world not as a database of facts but as a shared, evolving fabric of beliefs over knowledge graphs, where ontological axioms constrain priors, observations update beliefs by Bayesian conditioning, and actions intervene upon the world. We work through what an agent gains from such a model: a home-security agent deciding whether the figure at the gate is a courier or a burglar, an actuarial estimate aggregated by entailment rather than by string frequency, a planning task that language models reliably fail, and the estimation of quantities that no document has ever stated. We then set out what the community must build to make them possible: belief annotation over RDF~1.2, probabilistic entailment regimes, semantic calibration layers, and protocols by which agents that have never met can exchange, and disagree over, calibrated beliefs.

摘要：知識圖譜以簡明的斷言描述現實，而現在使用它們的系統，基礎模型和自主代理，則以概率進行推理。我們認為這種不匹配是語言模型和知識圖譜整合仍然是一個數據供應管道而非統一推理架構的原因。我們設想語義貝葉斯世界模型（SBWMs）：一個將世界描述為一個共享的、隨著時間演變的信念結構，而不是一個事實數據庫的網絡，在這裡本體公理約束先驗，觀察通過貝葉斯條件更新信念，行動則介入世界。我們探討代理從這樣的模型中獲得的收益：一個家庭安全代理決定大門口的人影是快遞員還是小偷，通過推理而非字符串頻率聚合的精算估算，一個語言模型可靠失敗的規劃任務，以及從未在任何文檔中陳述過的數量估算。我們接著指出社群必須構建的內容以使其成為可能：基於RDF~1.2的信念標註、概率推理體系、語義校準層，以及從未見過面的代理能夠交換和對校準信念產生分歧的協議。

##### **A Reverse Sign Language Dictionary: Open-Vocabulary Sign Recognition from Continuous Signing via Video Captioning and Description Retrieval**
2609.03788v1 by Santiago Poveda-Gutiérrez, Hideki Nakayama, Mayumi Bono

Isolated Sign Language Recognition (ISLR) is conventionally cast as closed-set classification over gloss labels, which cannot generalize to signs unseen in training and ties every deployment to a gloss-annotated lexicon. We instead recognize signs extracted from continuous signing by (1) captioning a sign-level clip into a free-form procedural description of the articulation with an open-weight vision-language model, and (2) retrieving the closest entry from a vocabulary of target descriptions with a multilingual sentence encoder: a reverse sign language dictionary that needs no gloss supervision and admits an open vocabulary. On 1,300 sign-level segments from a Japanese Sign Language (JSL) dialogue corpus annotated with procedural descriptions (against a 2% top-10 chance floor over the 503-entry target vocabulary), fine-tuning the captioner substantially improves seen-class retrieval: language and vision tower fine-tuning raises top-10 retrieval on seen classes from 4.5% (untrained) to 49%, becoming statistically indistinguishable from a standard supervised closed-set classifier (I3D) on two of the three test sets where a closed-set classifier can be evaluated at all. More importantly, unseen-class retrieval also improves significantly over the untrained pipeline (11.5% -> 21.0% top-10, p=0.0094), a regime in which the closed-set classifier cannot participate. A matcher-side empirical upper-bound analysis shows the sentence encoder already recovers close to 100% of paraphrased gold descriptions, locating a gap in captioning quality that we aim to address in future work. To our knowledge this is the first description-based, open-vocabulary sign lookup from continuous signing without gloss supervision, and the first for JSL.

摘要：孤立手語識別（ISLR）傳統上被視為對標記標籤的封閉集分類，這無法對訓練中未見的手勢進行泛化，並將每次部署與標記的詞彙表綁定在一起。我們則通過以下方式識別從連續手語中提取的手勢：（1）使用開放權重的視覺-語言模型將手勢級片段標題轉換為自由形式的程序描述，並（2）使用多語言句子編碼器從目標描述的詞彙中檢索最接近的條目：一個不需要標記監督且允許開放詞彙的反向手語字典。在一個標註有程序描述的日本手語（JSL）對話語料庫中的1,300個手勢級片段上（在503個目標詞彙的2%前10名機會底線下），微調標題生成器顯著改善了已見類別的檢索：語言和視覺塔的微調將已見類別的前10名檢索從4.5%（未訓練）提高到49%，在三個測試集中的兩個上，與標準監督的封閉集分類器（I3D）在統計上無法區分。更重要的是，未見類別的檢索也顯著改善了未訓練的管道（11.5% -&gt; 21.0% 前10名，p=0.0094），這是一種封閉集分類器無法參與的情況。一個匹配器端的經驗上限分析顯示，句子編碼器已經能夠恢復接近100%的同義改寫金標描述，找出了我們希望在未來工作中解決的標題質量差距。據我們所知，這是第一個基於描述的、開放詞彙的從連續手語中查找手勢的方式，且是日本手語的首次。

##### **SimSkill: A Lifelong Learning AI Agent for Autonomous Mastery of Traffic Simulation**
2609.03753v1 by Qi Liu, Qinzheng Wang, Yiming Bie

As large language models (LLMs) become increasingly capable, the long-term value of AI systems depends not only on solving individual requests, but also on transforming experience and accumulated knowledge into durable, reusable competence. We introduce SimSkill, a self-evolving agent built around the Simulation of Urban MObility (SUMO) traffic simulator. SimSkill identifies capability gaps, generates and solves environment-grounded tasks, verifies solutions through an action--critic loop, and consolidates experience into episodic, procedural, and semantic memory without updating the backbone model. Through autonomous exploration, it builds a reusable library spanning the traffic-simulation workflow. We evaluate SimSkill on two held-out benchmarks with three backbone LLMs and independent artifact-based verification. SimSkill improves verified completion by up to 25 percentage points, while ablations show complementary contributions from procedural and semantic memory. Its benefits remain backbone- and budget-dependent: memory does not improve every model or uniformly reduce inference cost. More broadly, SimSkill illustrates a design paradigm in which natural language preserves and composes computational capabilities, while executable tools and code provide precise and reproducible execution. All code and experimental data are publicly available at https://github.com/qiliuchn/SimSkill-V1.

摘要：隨著大型語言模型（LLMs）變得越來越強大，人工智慧系統的長期價值不僅依賴於解決個別請求，還依賴於將經驗和累積的知識轉化為持久、可重用的能力。我們介紹了SimSkill，一個圍繞城市交通模擬（SUMO）交通模擬器構建的自我演化代理。SimSkill識別能力差距，生成並解決基於環境的任務，通過行動-評價循環驗證解決方案，並將經驗整合到情節性、程序性和語義記憶中，而無需更新主幹模型。通過自主探索，它建立了一個涵蓋交通模擬工作流程的可重用庫。我們在兩個保留的基準上評估SimSkill，使用三個主幹LLMs和獨立的基於工件的驗證。SimSkill使驗證完成率提高了最多25個百分點，而消融實驗顯示程序性和語義記憶的互補貢獻。其優勢仍然依賴於主幹和預算：記憶並不會改善每個模型或均勻降低推理成本。更廣泛地說，SimSkill展示了一種設計範式，其中自然語言保留並組合計算能力，而可執行的工具和代碼提供精確且可重複的執行。所有代碼和實驗數據均可在https://github.com/qiliuchn/SimSkill-V1上公開獲取。

##### **KnowVis: Knowledge-Centric Visual Summarization for Video Lectures**
2609.03742v1 by Yi Xu, Yifan Hou, Xiaoyu Zhang

Video lectures are valuable educational resources, but their dense and lengthy formats often overwhelm novice learners. This difficulty stems from a fundamental pedagogical mismatch: while videos deliver transient information linearly, human learning requires constructing interconnected cognitive networks, a task that induces severe cognitive overload for novice learners lacking prior domain knowledge. Existing video summarization methods fail to resolve this mismatch, as they primarily produce text-heavy, linear condensations that still demand high cognitive effort. To bridge this gap, we propose KnowVis, a framework that transforms linear video lectures into pedagogically grounded visual narratives. KnowVis first extracts a detailed concept map from multimodal video content to identify important and challenging threshold concepts, then constructs structured knowledge units, and finally synthesizes engaging visual summaries. Alongside the framework, we introduce a curated dataset of 125 educational videos across 10 academic disciplines, paired with 1,079 generated visual summaries. Extensive automated evaluations and a human study demonstrate that, compared to state-of-the-art baselines, KnowVis generates more accurate and clear visuals that successfully reduce cognitive load and significantly improve student learning effectiveness and knowledge retention.

摘要：視頻講座是寶貴的教育資源，但其密集且冗長的格式常常讓初學者感到不知所措。這種困難源於根本的教學不匹配：雖然視頻以線性方式傳遞瞬時信息，但人類學習需要構建相互聯繫的認知網絡，這對於缺乏先前領域知識的初學者來說是一項導致嚴重認知過載的任務。現有的視頻摘要方法無法解決這一不匹配，因為它們主要生成文字密集的線性濃縮，仍然需要高認知努力。為了填補這一空白，我們提出了KnowVis，一個將線性視頻講座轉化為以教學為基礎的視覺敘事的框架。KnowVis首先從多模態視頻內容中提取詳細的概念圖，以識別重要且具有挑戰性的門檻概念，然後構建結構化的知識單元，最後合成引人入勝的視覺摘要。除了這個框架，我們還介紹了一個涵蓋10個學術學科的125個教育視頻的策展數據集，並配有1,079個生成的視覺摘要。廣泛的自動評估和人類研究表明，與最先進的基準相比，KnowVis生成的視覺更準確、更清晰，成功減少了認知負擔，並顯著提高了學生的學習效果和知識保留。

##### **Can LLMs Extract Architectural Design Decisions from Source Code Commits? - A Preliminary Exploratory Study**
2609.03721v1 by Amey Karan, Rudra Dhar, Mohamed Soliman, Karthik Vaidhyanathan

Context: Architectural Design Decisions (ADDs) capture the rationale behind the structure and evolution of software systems but are rarely documented explicitly, and are often hidden inside source code commits. Recovering them is important for Architectural Knowledge Management (AKM). Problem: Extracting ADDs from commits is challenging due to their implicit and unstructured nature. Large Language Models (LLMs) have shown strong capabilities in understanding code and text, yet their effectiveness for this task remains underexplored. Study: We present a preliminary study using four LLMs (Gemini 3 Pro, DeepSeek R1, Kimi K2, Qwen3) with zeroshot and fewshot prompting on 30 developer-written ADDs from open-source projects. We score outputs with ROUGE-L, BLEU, METEOR, and BERTScore, and one author manually reviews the Gemini outputs. Results: All models reach a BERT-F1 above 0.81, and fewshot prompting improves alignment (Gemini BERT-F1: 0.828 to 0.847). However, the generated ADDs are often too long, implementation-focused, and miss the rationale behind the decision. This highlights opportunities for architecture-aware LLM systems and automated AKM.

摘要：背景：架構設計決策（ADDs）捕捉了軟體系統結構和演變背後的理由，但通常未被明確記錄，並且經常隱藏在源代碼提交中。恢復這些決策對於架構知識管理（AKM）至關重要。問題：由於其隱含和非結構化的特性，從提交中提取ADDs是具有挑戰性的。大型語言模型（LLMs）在理解代碼和文本方面顯示出強大的能力，但它們在此任務上的有效性仍未被充分探索。研究：我們展示了一項初步研究，使用四個LLMs（Gemini 3 Pro、DeepSeek R1、Kimi K2、Qwen3）對來自開源項目的30個開發者撰寫的ADDs進行零樣本和少樣本提示。我們使用ROUGE-L、BLEU、METEOR和BERTScore對輸出進行評分，並由一位作者手動審查Gemini的輸出。結果：所有模型的BERT-F1均超過0.81，少樣本提示改善了對齊（Gemini BERT-F1：從0.828提高到0.847）。然而，生成的ADDs通常過長，過於專注於實現，並且缺乏決策背後的理由。這突顯了架構感知LLM系統和自動化AKM的機會。

##### **What Do CAE Simulation Agents Really Need Beyond a Generic Harness?**
2609.03718v1 by Jiasheng Shi, Tianhan Zhang

Computer-aided engineering (CAE) simulation is among the largest and most demanding areas of engineering, where setting up a solver such as OpenFOAM, FEniCS, or COMSOL takes real expertise. Large language model (LLM) agents promise to turn a natural-language request into a working simulation, and recent CAE agents add simulation-specific machinery: multi-agent decomposition, domain retrieval, and scripted reflection. That machinery suited weak base models; modern harnesses already supply multi-turn reasoning, tool use, and execution feedback. We ask what a CAE simulation agent still needs beyond a generic harness. With information access and repair budget held fixed, a single-agent harness matches or beats multi-agent specialized systems (FoamBench 96.4\% vs.\ 88.2\%).
  Ablations trace this to capabilities the harness already provides: execution-feedback repair lifts FoamBench from 71.8\% with no repair round to 96.4\%, while scripted reflection adds nothing. The one input that still helps is domain knowledge supplied as solver tutorials, our largest measured gain (80.9\% to 96.4\%).

摘要：計算機輔助工程（CAE）模擬是工程領域中最大且需求最為嚴苛的領域之一，設置像是 OpenFOAM、FEniCS 或 COMSOL 這樣的求解器需要真正的專業知識。大型語言模型（LLM）代理承諾將自然語言請求轉化為有效的模擬，而最近的 CAE 代理則增加了模擬特定的機制：多代理分解、領域檢索和腳本反思。這些機制適合弱基礎模型；現代的工具已經提供多回合推理、工具使用和執行反饋。我們詢問 CAE 模擬代理在通用工具之外還需要什麼。在信息訪問和修復預算固定的情況下，單一代理工具的表現與多代理專用系統相當或更好（FoamBench 96.4\% 對 88.2\%）。  
消融實驗將這歸因於工具已經提供的能力：執行反饋修復將 FoamBench 從 71.8\% 提升到 96.4\%，而腳本反思則沒有任何增益。唯一仍然有幫助的輸入是作為求解器教程提供的領域知識，我們測得的最大增益（80.9\% 到 96.4\%）。

##### **A computable representation of the physical laboratory enables verifiable workflows**
2609.03621v1 by Xiaobo Li, Luyao Ge, Xiaohui Li, Lulu Guo, Ming Mao, Jiwang Zheng, Wenting Guan, Xin Yang, Yi Luo, Jun Jiang, Linjiang Chen

Making science computable requires representations of both scientific knowledge and the physical world in which scientific claims are tested. A computable representation of the physical laboratory is established through typed research objects, capability-bound operations and a compositional workflow algebra. It provides the physical-world counterpart to machine-readable knowledge, expressing workflows as programs over evolving laboratory states with explicit dependencies, decisions, iteration and concurrency. The representation was implemented in a modular agentic robotic laboratory by binding formal operations to executable Function Skills. For diverse scientific intents, capability-relative workflows were generated, while stateful simulation propagated object transformations and verified operation preconditions and laboratory constraints before dispatch. The proposed representation and its engineering framework jointly establish a general computational interface between agent reasoning and capability-bound physical transformations, providing a foundation for end-to-end autonomous scientific discovery.

摘要：製作可計算的科學需要對科學知識和科學主張被測試的物理世界進行表徵。通過類型化研究對象、能力綁定操作和組合工作流程代數，建立了物理實驗室的可計算表徵。它提供了物理世界與機器可讀知識的對應，將工作流程表達為在不斷演變的實驗室狀態上運行的程序，並具有明確的依賴性、決策、迭代和並發性。該表徵在模塊化的代理機器人實驗室中實施，將正式操作綁定到可執行的功能技能。為了滿足多樣的科學意圖，生成了相對於能力的工作流程，而有狀態的模擬則推進對象轉換並在發送之前驗證操作前提條件和實驗室約束。所提出的表徵及其工程框架共同建立了一個代理推理與能力綁定物理轉換之間的通用計算接口，為端到端的自主科學發現提供了基礎。

##### **LevelSyn: Physical-Aware Logic Synthesis via Level-Asynchronous Graph Neural Networks**
2609.03594v1 by Jingyi Zhou, Zhengyuan Shi, Ziyang Zheng, Qiang Xu

As integrated circuit technology scales into the nanometer regime, the traditional disconnect between logic synthesis and physical design has led to significant PPA (Power, Performance, and Area) degradation and prolonged design closure cycles. Traditional logic synthesis relies on non-physical Wire Load Models (WLMs), while recent spectral-based placement predictors often neglect the inherent hierarchical logic depth and signal flow of netlists, which leads to low-fidelity spatial estimations. To bridge this gap, we propose LevelSyn, a novel physical-aware logic synthesis framework that integrates hierarchical representation learning with a wirelength-driven optimization engine. At its core, LevelSyn leverages a level-asynchronous Graph Neural Network (GNN) to predict high-fidelity gate coordinates by capturing the structural and directional semantics of And-Inverter Graphs (AIGs). To handle industrial-scale designs, a level-aligned subgraph partitioning strategy is introduced to eliminate memory bottlenecks while preserving local logical dependencies. These spatial insights are seamlessly integrated into a newly developed physical-informed synthesis engine within the Berkeley ABC framework. Experimental results on the EPFL benchmark suite demonstrate that LevelSyn significantly outperforms state-of-the-art (SOTA) methods, achieving an average power reduction of 6.89\% and a timing delay improvement of 27.48\%. Furthermore, post-place-and-route validation shows a 99.59\% reduction in design rule check (DRC) violations, highlighting its effectiveness in accelerating design convergence.

摘要：隨著集成電路技術進入納米範疇，邏輯綜合與物理設計之間的傳統脫節導致了顯著的 PPA（功耗、性能和面積）降級以及延長的設計閉合週期。傳統的邏輯綜合依賴於非物理的線載模型（WLMs），而近期基於頻譜的放置預測器往往忽略了網路清單的固有層次邏輯深度和信號流，這導致了低保真的空間估算。為了彌補這一差距，我們提出了 LevelSyn，一個新穎的物理感知邏輯綜合框架，將層次表示學習與以線長為驅動的優化引擎整合在一起。LevelSyn 的核心利用層非同步圖神經網絡（GNN）來預測高保真的閘極坐標，通過捕捉與與非閘圖（AIGs）的結構和方向語義。為了處理工業級設計，引入了一種層對齊的子圖分區策略，以消除內存瓶頸，同時保留局部邏輯依賴性。這些空間見解無縫地整合到伯克利 ABC 框架內新開發的物理知識綜合引擎中。在 EPFL 基準套件上的實驗結果顯示，LevelSyn 顯著超越了最先進的（SOTA）方法，實現了平均 6.89\% 的功耗減少和 27.48\% 的時序延遲改善。此外，後置放置和路由驗證顯示設計規則檢查（DRC）違規減少了 99.59\%，突顯了其在加速設計收斂方面的有效性。

##### **KC-Bench: A Dynamic Interactive Benchmark for Evaluating Knowledge Conflicts in LLM Agents**
2609.03588v1 by Yaxing Lyu, Shengjie Zhou, Binbin Toh, Pengyu Zhu, Lijun Li

As LLMs increasingly act through tools, they must reconcile user instructions, parametric knowledge, and dynamic environmental observations before taking actions. We introduce KC-Bench, a controlled multi-turn benchmark for measuring this capability across world-knowledge conflicts, input inconsistencies, and multi-source temporal conflicts. Its 238 tasks are manually screened from more than 1,000 generated candidates and combine a user simulator, stateful tools, deterministic environment assertions, an open-source natural-language evaluator, and human trajectory verification. Evaluation of nine models, including DeepSeek-V4-Flash, GLM-5.2, and MiniMax-M3, shows substantial cross-domain variation: no model handles factual correction, identity consistency checking, and temporal conflict resolution reliably across all settings. In the simulated environments, missed conflicts can propagate to tool calls or synthetic protected-data flows. KC-Bench isolates this model-level behavior rather than ranking complete agent frameworks, and provides a reproducible diagnostic for developing conflict-aware reasoning and execution safeguards.

摘要：隨著大型語言模型（LLMs）越來越多地通過工具進行操作，它們必須在採取行動之前調和用戶指令、參數知識和動態環境觀察。我們介紹 KC-Bench，一個受控的多輪基準，用於測量這種能力，涵蓋世界知識衝突、輸入不一致性和多來源時間衝突。其238個任務是從超過1,000個生成的候選項中手動篩選出來的，並結合了用戶模擬器、有狀態的工具、確定性環境斷言、開源自然語言評估器和人類軌跡驗證。對九個模型的評估，包括 DeepSeek-V4-Flash、GLM-5.2 和 MiniMax-M3，顯示出顯著的跨領域變異：沒有一個模型能在所有設置中可靠地處理事實修正、身份一致性檢查和時間衝突解決。在模擬環境中，錯過的衝突可能會傳播到工具調用或合成的受保護數據流。KC-Bench 隔離了這種模型級行為，而不是對完整的代理框架進行排名，並提供了一個可重現的診斷工具，用於開發衝突感知的推理和執行保障。

##### **GPS-Bench: A Governance Policy Benchmark for Automating Policy Analysis**
2609.03553v1 by Linh Le, Melanie Bui, My Chiffon Nguyen, Zachary Schlosser, David Williams-King

Policy analysis requires more than predicting whether a proposal will pass: it requires identifying who will be affected, how those actors respond, and what follows. LLM-based policy simulations model these processes at scale, but their validity is hard to establish when plausible behaviour is never compared with observed outcomes. We introduce GPS-Bench, an evidence-grounded benchmark for governance policy simulation that links policies to relevant actors, actor actions and downstream impacts using legislative records, lobbying disclosures, regulatory documents, corporate filings, economic data and other public evidence. Actors are reconstructed from the dated record rather than prompted as archetypes, so a persona is an evidence object with provenance; a human-annotated pool forms the Gold evaluation set, while cases labelled by a separate LLM from retrieved evidence are treated as Silver supervision and never as test labels. Because every inference mode reads the same grounded state and emits the same schema, GPS-Bench turns "does multi-agent simulation help?" into a controlled comparison: we contrast joint reasoning, independent and communicating actor agents, graph-based methods and weight-level fine-tuning over one policy state. Fine-tuning on the grounded record gives the strongest actor-level impact prediction, and decomposition does not beat it; what decomposition adds is mechanism. Agents hold private, non-identical evidence, each seeing its own exposure clause, and address named partners with concrete joint proposals, what they offer, what they need in return, and why acting together beats acting alone, so the coalitions that form can be checked against the commitments the record holds. GPS-Bench therefore gives a common empirical setting for studying when evidence, actor modelling and multi-agent interaction improve the prediction and interpretation of policy outcomes.

摘要：政策分析不僅僅是預測一項提案是否會通過：它還需要確定誰將受到影響、這些行為者的反應以及隨之而來的結果。基於LLM的政策模擬在大規模上建模這些過程，但當合理的行為從未與觀察到的結果進行比較時，其有效性難以確立。我們介紹GPS-Bench，一個以證據為基礎的治理政策模擬基準，通過立法記錄、遊說披露、監管文件、企業申報、經濟數據和其他公共證據，將政策與相關行為者、行為者行動和下游影響聯繫起來。行為者是從過時的記錄中重建的，而不是作為原型被提示，因此角色是一個具有來源的證據對象；人工標註的池形成了黃金評估集，而由從檢索證據中獲得的獨立LLM標註的案例則被視為銀級監督，而不是測試標籤。因為每個推理模式都讀取相同的基礎狀態並發出相同的模式，GPS-Bench將“多代理模擬有幫助嗎？”轉變為一個受控比較：我們對比了聯合推理、獨立和溝通的行為者代理、基於圖的方法以及在一個政策狀態上的權重級微調。在基礎記錄上進行微調能提供最強的行為者級影響預測，而分解並未超越它；分解所增加的是機制。代理持有私有的、非同質的證據，每個代理看到自己的曝光條款，並以具體的聯合提案來解決命名的合作夥伴，說明他們提供的內容、他們需要什麼作為回報，以及為什麼共同行動優於單獨行動，因此形成的聯盟可以與記錄所持的承諾進行檢查。因此，GPS-Bench提供了一個共同的實證環境，用於研究何時證據、行為者建模和多代理互動改善政策結果的預測和解釋。

##### **LeanGRPO: Eliminating Redundant Recomputation in Diffusion RL**
2609.03528v1 by Sijie Wang, Zhiqiang Tan, Xinrui Yang, Shaohuai Shi

Diffusion reinforcement learning (RL) has recently achieved significant success in post-training image and video generative models. However, most diffusion RL methods, including DanceGRPO and FlowGRPO, recompute selected timesteps with gradient tracking after rollout. Under on-policy training with the same backend for rollout and update, this recomputation is mathematically redundant. Intuitively, the rollout and policy update steps can reuse the same feed-forward backbone to avoid redundant computation, but doing so can incur a large memory overhead during rollout. To address the issue, we present LeanGRPO by restructuring the data-parallel layout and introducing two recompute-free training schedules for trajectory-logprob diffusion RL: (1) LeanGRPO-Retain enables gradient tracking during rollout and directly reuses the resulting computation graphs and saved activations for backward during update, requiring no recomputation; and (2) LeanGRPO-Reweight also enables gradients during rollout, but immediately backpropagates each selected step using a provisional advantage and delays gradient synchronization, then corrects the provisional gradients with the true advantage after the trajectory is completed. These schedules target different model scales and input sizes. Across FlowGRPO/DanceGRPO with FLUX.1-dev and Wan, LeanGRPO achieves up to 1.83x end-to-end speedup while preserving the original optimization objective.

摘要：擴散強化學習（RL）最近在後訓練的圖像和視頻生成模型中取得了顯著成功。<lb/>然而，大多數擴散RL方法，包括DanceGRPO和FlowGRPO，在推出後重新計算選定的時間步驟並進行梯度追蹤。在使用相同的後端進行推出和更新的在線策略訓練中，這種重新計算在數學上是冗餘的。<lb/>直觀地說，推出和策略更新步驟可以重用相同的前饋骨幹，以避免冗餘計算，但這樣做在推出期間可能會產生大量的內存開銷。<lb/>為了解決這個問題，我們通過重構數據並行佈局並引入兩種無需重新計算的訓練計劃來提出LeanGRPO，用於軌跡-對數概率擴散RL：<lb/>(1) LeanGRPO-Retain在推出期間啟用梯度追蹤，並直接重用結果計算圖和保存的激活進行更新時的反向傳播，無需重新計算；<lb/>(2) LeanGRPO-Reweight也在推出期間啟用梯度，但立即使用臨時優勢反向傳播每個選定步驟並延遲梯度同步，然後在軌跡完成後用真實優勢修正臨時梯度。<lb/>這些計劃針對不同的模型規模和輸入大小。在FlowGRPO/DanceGRPO與FLUX.1-dev和Wan的比較中，LeanGRPO實現了高達1.83倍的端到端加速，同時保留了原始的優化目標。

##### **NeoRed: A Knowledge-Logic-Alignment Multimodal Large Language Model for Neonatal Respiratory Disease Diagnosis**
2609.03527v1 by Yinan Liu, Hongtai Xia, Haoran Xu, Jiankang Hong, Jingkuan Song, Ye Luo

Neonatal respiratory diseases are a major cause of neonatal morbidity and mortality, posing substantial challenges in clinical practice. Despite recent advances, existing Multimodal Large Language Models (MLLMs) face two key limitations in neonatal diagnosis: (1) domain gap arising from predominantly adult training data; (2) insufficient integration of multidimensional clinical context for accurate diagnosis. To address these challenges, we collect two real-world clinical datasets (NeoCXR and NeoCXR-EV) and propose NeoRed, to the best of our knowledge, the first MLLM tailored for neonatal respiratory disease, filling the gap in neonatal diagnostic reports generation. To enhance joint diagnosis from heterogeneous clinical context and chest X-rays, we design a novel Knowledge-Logic-Alignment (KLA) framework which constrains model behavior from three perspectives: 1) Knowledge Prior Injection (KPI) incorporates neonatologist-inspired diagnostic priors into multimodal representations, guiding disease-specific attention across modalities; 2) Diagnostic Logic Constraint (DLC) aligns the semantics of generated reports with multimodal diagnostic logic; and 3) Visual Semantic Alignment (VSA) establishes semantic correspondence between visual features and imaging conclusions. Extensive experiments demonstrate that NeoRed enables accurate neonatal diagnostic reports generation, achieving ROUGE-L of 53.29% and Clinical Efficacy F1 score of 65.19% on NeoCXR, outperforming existing MLLMs. NeoRed also preserves competitive report generation performance on adult benchmarks (MIMIC-CXR and IU-Xray). Datasets will be available upon application.

摘要：新生兒呼吸疾病是新生兒發病率和死亡率的主要原因，對臨床實踐造成了重大挑戰。儘管最近取得了一些進展，現有的多模態大型語言模型（MLLMs）在新生兒診斷方面面臨兩個主要限制：（1）由於主要基於成人訓練數據而產生的領域差距；（2）對於準確診斷，多維臨床背景的整合不足。為了應對這些挑戰，我們收集了兩個真實世界的臨床數據集（NeoCXR和NeoCXR-EV），並提出了NeoRed，據我們所知，這是第一個專為新生兒呼吸疾病量身定制的MLLM，填補了新生兒診斷報告生成的空白。為了增強來自異質臨床背景和胸部X光片的聯合診斷，我們設計了一個新穎的知識-邏輯-對齊（KLA）框架，從三個角度約束模型行為：1）知識先驗注入（KPI）將新生兒科醫生啟發的診斷先驗納入多模態表示中，引導跨模態的疾病特異性注意力；2）診斷邏輯約束（DLC）使生成報告的語義與多模態診斷邏輯對齊；3）視覺語義對齊（VSA）建立視覺特徵與影像結論之間的語義對應。大量實驗表明，NeoRed能夠生成準確的新生兒診斷報告，在NeoCXR上達到53.29%的ROUGE-L和65.19%的臨床效能F1分數，超越了現有的MLLMs。NeoRed在成人基準（MIMIC-CXR和IU-Xray）上也保持了競爭性的報告生成性能。數據集將在申請後提供。

##### **CulturalMenuBench: Probing the Knowledge-Application Gap in Multimodal Culinary Reasoning**
2609.03526v1 by Bo Zeng, Linfeng Gao, Peiqin Lin, Yu Zhao, Mingyan Zeng, Yu Tong, Xintong Wang, Linlong Xu, Longyue Wang, Weihua Luo, Qinggang Zhang, Jinsong Su

Multimodal language models achieve near-ceiling scores on food recognition benchmarks, yet it remains unclear whether this success reflects genuine cultural understanding or mere visual matching. To probe this distinction, we introduce CulturalMenuBench, a benchmark of 4,870 items in 10 languages across 18 regions; its 10 tasks pair final-dish and step-by-step cooking images with ingredients, procedural text, and regional labels, spanning basic recognition to process-grounded cultural attribution. Evaluating 12 models exposes a substantial knowledge-application gap: models exceeding 94% on standard multiple-choice tasks drop to at most 56% when attributing dishes to Chinese regional cuisines, despite an identical four-way format. Diagnostic analyses explain why: error patterns are consistent with random guessing, accuracy tracks visual distinctiveness rather than cultural structure, and models classify cuisines more accurately from dish names alone than from images (+7-18 points). The knowledge is thus present but cannot be activated through visual input. An ablation confirms these tasks genuinely require procedural evidence: removing sequential cooking images selectively degrades process-grounded tasks while others remain stable. Overall, CulturalMenuBench shows that near-perfect recognition can conceal an inability to apply cultural knowledge, motivating training that explicitly connects perception, procedure, and cultural context. Code and data are publicly available.

摘要：多模態語言模型在食品識別基準測試中達到了接近上限的分數，但目前尚不清楚這一成功是否反映了真正的文化理解，還是僅僅是視覺匹配。為了探究這一區別，我們推出了CulturalMenuBench，一個包含4,870個項目、涵蓋10種語言和18個地區的基準；其10個任務將最終菜餚和逐步烹飪圖像與食材、程序文本和地區標籤配對，範圍涵蓋基本識別到基於過程的文化歸屬。對12個模型的評估揭示了顯著的知識應用差距：在標準多選任務中超過94%的模型在將菜餚歸屬於中國地方菜時最多僅能達到56%，儘管格式完全相同。診斷分析解釋了原因：錯誤模式與隨機猜測一致，準確性跟蹤視覺特徵而非文化結構，並且模型僅從菜名進行分類的準確性比從圖像中更高（+7-18分）。因此，知識是存在的，但無法通過視覺輸入啟動。一項消融實驗確認這些任務確實需要程序證據：去除順序烹飪圖像會選擇性地降低基於過程的任務的表現，而其他任務則保持穩定。總體而言，CulturalMenuBench顯示出近乎完美的識別可能掩蓋了應用文化知識的能力，促使訓練明確連接感知、程序和文化背景。代碼和數據已公開可用。

##### **Making Every Tool Call Count: Necessary Tool-Evidence Path Rewards for Agentic Vision-Language Models**
2609.03493v1 by Xingming Long, Yu Liu, Zhiwei Yang, Hanqi Feng, Shaojie Zhang, Barnabas Poczos, Chao Jiang, Zhenbo Luo, Lei Jiang, Pei Fu

Modern vision-language models (VLMs) can directly answer many image-grounded questions, yet they often struggle with complex queries requiring fine-grained visual details or external knowledge. To acquire this missing evidence, agentic VLMs invoke tools such as image cropping, image search, and text search. However, existing training paradigms primarily evaluate tool-use based on final answer correctness, leaving evidence acquisition and utilization insufficiently supervised. This leads to two critical shortcomings: (i) models frequently issue redundant or off-target tool calls that fail to gather necessary evidence, and (ii) even when appropriate tools are called, models often fail to extract the necessary information from the resulting observations. To address these limitations, we introduce the NTEP (Necessary Tool-Evidence Path), a novel annotation scheme that explicitly specifies the essential external evidence and corresponding tool calls for each query. Building upon this, we propose NTEP-R (NTEP Reward), a supervision mechanism ensuring that each tool invocation strictly advances the reasoning process toward the final solution. Specifically, our approach rewards the agent for aligning its pre-call intent with a necessary evidence-seeking goal, and for ensuring the information summarized from the post-call observation aligns with the necessary evidence. Furthermore, we introduce a non-repeated-goal regularizer to penalize redundant calls that revisit satisfied NTEP goals. Extensive evaluations on seven image-grounded benchmarks demonstrate that our 8B-parameter instantiation, NTEP-8B, significantly improves both search-oriented accuracy and tool-use efficiency within a unified three-tool framework. These results highlight the critical value of fine-grained tool-evidence path supervision for training robust agentic VLMs.

摘要：現代的視覺-語言模型（VLMs）可以直接回答許多基於圖像的問題，但它們在處理需要細緻視覺細節或外部知識的複雜查詢時常常遇到困難。為了獲取這些缺失的證據，具有主動性的 VLMs 會調用工具，如圖像裁剪、圖像搜索和文本搜索。然而，現有的訓練範式主要根據最終答案的正確性來評估工具的使用，導致證據獲取和利用的監督不足。這導致了兩個關鍵的缺點：（i）模型經常發出冗餘或偏離目標的工具調用，未能收集必要的證據；（ii）即使在適當的工具被調用時，模型也常常未能從結果觀察中提取必要的信息。為了解決這些限制，我們引入了 NTEP（必要工具-證據路徑），這是一種新穎的標註方案，明確指定每個查詢所需的外部證據和相應的工具調用。在此基礎上，我們提出了 NTEP-R（NTEP 獎勵），這是一種監督機制，確保每次工具調用都嚴格推進推理過程朝向最終解決方案。具體而言，我們的方法獎勵代理人將其調用前的意圖與必要的證據尋求目標對齊，以及確保從調用後的觀察中總結的信息與必要的證據對齊。此外，我們引入了一個非重複目標正則化器，以懲罰重新訪問已滿足的 NTEP 目標的冗餘調用。在七個基於圖像的基準上進行的廣泛評估顯示，我們的 8B 參數實例 NTEP-8B 顯著提高了統一三工具框架內的搜索導向準確性和工具使用效率。這些結果突顯了細緻的工具-證據路徑監督對於訓練穩健的主動 VLMs 的關鍵價值。

##### **Pattern Over-Generalization of Knowledge Graph Embedding**
2609.03487v1 by Junsik Kim, Kangil Kim

Knowledge graph embedding (KGE) demonstrates its effectiveness for predicting missing links in knowledge graphs (KGs) by projecting entities and relations into a low-dimensional vector space. It is crucial for KGE models to effectively capture inference patterns (patterns) inherent in KGs, such as symmetry/antisymmetry, inversion and composition. Although recent KGE models exhibit strong capabilities in modeling such diverse patterns, they suffer from inherent limitations stemming from pattern over-generalization, where embeddings learned from only a single pattern instance inevitably generalize that pattern to all related instances, i.e., generalize the pattern universally. To address this issue, we propose PogRE (Pattern Over-Generalization Robust Embedding), a simple but effective method that utilizes dense linear transformations and compound operations for relation representation. Our theoretical analysis demonstrates that a dense linear transformation allows a pattern to become progressively universal as more triples are observed in the pattern. Furthermore, after observing d+1 linearly independent entities (d+1 denotes the dimension of entity), the linear transformation guarantees universal generalization of the pattern across all related instances. Experimental results on three standard benchmark datasets show that PogRE outperforms existing state-of-the-art KGE models in link prediction. Moreover, our empirical results indicate that PogRE effectively addresses the negative impact of over-generalization.

摘要：知識圖譜嵌入（KGE）通過將實體和關係投影到低維向量空間中，展示了其在預測知識圖譜（KGs）中缺失連結的有效性。對於KGE模型來說，有效捕捉KGs中固有的推理模式（模式）是至關重要的，例如對稱/反對稱、反轉和組合。儘管最近的KGE模型在建模這些多樣化模式方面展現了強大的能力，但它們受限於模式過度概括的固有限制，這種情況下，僅從單一模式實例學到的嵌入不可避免地將該模式概括到所有相關實例，即普遍地概括該模式。為了解決這個問題，我們提出了PogRE（模式過度概括魯棒嵌入），這是一種簡單但有效的方法，利用密集線性變換和復合操作來表示關係。我們的理論分析表明，密集線性變換允許模式隨著觀察到的三元組數量的增加而逐漸變得普遍。此外，在觀察到d+1個線性獨立的實體後（d+1表示實體的維度），該線性變換保證了模式在所有相關實例中的普遍概括。在三個標準基準數據集上的實驗結果顯示，PogRE在連結預測方面超越了現有的最先進KGE模型。此外，我們的實證結果表明，PogRE有效地解決了過度概括的負面影響。

##### **AutoGraphForge: Towards Automated Graph Theory Discovery**
2609.03478v1 by Ján Pastorek

We report on our ongoing project to develop a computational pipeline, AutoGraphForge, for an automated graph-theoretic conjecturing-refuting-formalizing-proving system. Conjecture generation is counterexample-guided and runs in rounds: a Graffiti3 generator proposes conjectures over a small, evolving snapshot table $T$ (initially a few hundred graphs with their computed invariants) that grows only by counterexamples to its own conjectures. A novelty filter of $559$ classical and folklore relations, closed under transitive composition and linear identity substitution, decides via a linear program whether a candidate is already implied by known results. Surviving candidates are tested against a dataset of about $348,000$ graphs, unioning the complete House of Graphs invariant export, the exhaustive census of all connected graphs on at most nine vertices, several extremal families (strongly regular, minimal Ramsey, Cayley, cages, barbells, lollipops, spiders), and random models. Counterexample-search algorithms then attack the remainder. Run for several rounds on an HPC cluster, the loop yields $6,522$ conjectures that survived the refutation dataset, the novelty filter and every active-search run -- among them nontrivial relations between the annihilation number and the edge-cover number for bipartite and regular graphs, which we prove by hand. A subsequent formalization and proving stage deterministically translates each surviving conjecture into a Lean 4 statement skeleton; every candidate proof is kernel-verified against a pinned mathlib4 and our custom invariant preamble. This stage integrates two neural provers -- DeepSeek-Prover-V2-671B (served with vLLM) and the Lean-specialised OProver-32B -- behind the independent kernel check. It is implemented end-to-end and passes initial sanity checks, with the full pipeline currently running on the cluster.

摘要：我們報告我們正在進行的項目，開發一個計算管道 AutoGraphForge，用於自動化的圖論猜想-反駁-形式化-證明系統。猜想生成是以反例為指導並且分輪次進行：一個 Graffiti3 生成器在一個小的、演變中的快照表 $T$ 上提出猜想（最初是幾百個圖及其計算出的不變量），這個表僅通過對其自身猜想的反例來增長。一個包含 $559$ 個經典和民間關係的創新過濾器，在傳遞合成和線性身份替換下是封閉的，通過線性程序決定候選者是否已經由已知結果所暗示。存活的候選者會在約 $348,000$ 個圖的數據集上進行測試，這些圖包括完整的圖的家族不變量導出、最多九個頂點的所有連通圖的詳細普查、幾個極端家族（強正則、最小拉姆齊、凱利、籠子、杠鈴、棒棒糖、蜘蛛）和隨機模型。然後，反例搜索算法攻擊剩餘部分。在 HPC 集群上運行幾輪後，這個循環產生了 $6,522$ 個在反駁數據集、新穎過濾器和每次主動搜索運行中存活的猜想——其中包括二部圖和正則圖的消滅數與邊覆蓋數之間的非平凡關係，我們手動證明了這一點。隨後的形式化和證明階段確定性地將每個存活的猜想轉換為 Lean 4 語句骨架；每個候選證明都會對照固定的 mathlib4 和我們的自定義不變前言進行內核驗證。這一階段整合了兩個神經證明者——DeepSeek-Prover-V2-671B（使用 vLLM 服務）和 Lean 專用的 OProver-32B——在獨立的內核檢查後面。它是端到端實現的並通過了初步的合理性檢查，目前完整的管道正在集群上運行。

##### **Exploring the Potential of Contrastive Language-Image Pre-training for Multi-Source Remote Sensing Data**
2609.03391v1 by Xiangyang Miao, Kelu Yao, Yekai Huang, Xiaogang Xu, Junxiao Xue, Minjun Shen, Chenghui Lv, Shanji Liu, Yaying Chen, Chao Li

Contrastive language-image learning (CLIP) has become a key paradigm for remote sensing vision-language understanding. However, existing remote sensing contrastive learning methods are mostly built on RGB-oriented CLIP architectures, making it difficult to exploit heterogeneous sensors such as SAR, multi-spectral imaging (MSI), and hyperspectral imaging (HSI). To address this limitation, we propose OmniRSCLIP, an end-to-end contrastive learning framework that supports multi-source sensor inputs for remote sensing vision-language modeling. The key idea is to extend CLIP beyond its fixed RGB input interface without breaking the pretrained visual knowledge. To this end, OmniRSCLIP introduces Spectral-Spatial Basis Decomposition (SSBD), which formulates arbitrary-channel adaptation as a basis recomposition problem: pretrained CLIP patch embeddings provide transferable spatial bases, while wavelength-conditioned coefficients span sensor-specific embedding kernels within a constrained visual prior space. This design avoids forcing heterogeneous sensors into a fixed-channel input space, while aligning them in a unified image-text semantic space. We further introduce a spectral-context-aware mask-based contrastive learning scheme to suppress modality-specific redundant features and enhance fine-grained image-text alignment. Finally, to support multi-modal training, we construct OmniRS5M, the first large-scale remote sensing image-text corpus covering RGB, SAR, MSI, and HSI. Experiments on retrieval, zero-shot classification, and semantic localization show that OmniRSCLIP preserves strong RGB-domain performance while effectively extending CLIP to heterogeneous remote sensing modalities.

摘要：對比語言-影像學習（CLIP）已成為遙感視覺-語言理解的關鍵範式。<lb/>然而，現有的遙感對比學習方法大多建立在以RGB為導向的CLIP架構上，這使得利用異質傳感器（如SAR、多光譜成像（MSI）和高光譜成像（HSI））變得困難。<lb/>為了解決這一限制，我們提出了OmniRSCLIP，一個端到端的對比學習框架，支持多源傳感器輸入以進行遙感視覺-語言建模。<lb/>其核心思想是將CLIP擴展到其固定的RGB輸入介面之外，而不破壞預訓練的視覺知識。<lb/>為此，OmniRSCLIP引入了光譜-空間基礎分解（SSBD），將任意通道適配公式化為基礎重組問題：預訓練的CLIP補丁嵌入提供可轉移的空間基礎，而波長條件系數則在受限的視覺先驗空間內跨越傳感器特定的嵌入核。<lb/>這一設計避免了將異質傳感器強行納入固定通道輸入空間，同時在統一的影像-文本語義空間中對齊它們。<lb/>我們進一步引入了一種光譜上下文感知的基於掩碼的對比學習方案，以抑制模態特定的冗餘特徵並增強細粒度的影像-文本對齊。<lb/>最後，為了支持多模態訓練，我們構建了OmniRS5M，第一個涵蓋RGB、SAR、MSI和HSI的大規模遙感影像-文本語料庫。<lb/>在檢索、零樣本分類和語義定位的實驗中，顯示OmniRSCLIP在保持強大RGB領域性能的同時，有效地將CLIP擴展到異質遙感模態。

##### **FrameBench:A Language Understanding Benchmark Based on Frame Semantics**
2609.03370v1 by Chihiro Yano, Ryohei Sasano

In frame semantics, sentence comprehension is assumed to proceed by relating lexical meaning to background knowledge called semantic frames, thereby enabling readers to implicitly enrich the text with unstated information. Recent large language models (LLMs) have achieved strong performance across a wide range of downstream tasks. However, it remains unclear whether they can reproduce the kinds of implicit enrichment that humans naturally make during comprehension. To address this question, we introduce FrameBench, a benchmark grounded in frame semantics. FrameBench consists of multiple-choice questions that test whether models distinguish the frames evoked by the same verb across contexts. We construct the benchmark for English and Japanese using FrameNet-style resources and a generation-and-verification pipeline with native-speaker judgments. Our experiments on a diverse set of models reveal challenges for small models, while several large models surpass the human reference scores. We release the constructed FrameBench dataset and the code for dataset construction and evaluation at https://github.com/SasanoLab/FrameBench.

摘要：在框架語意學中，句子的理解被假設為通過將詞彙意義與稱為語意框架的背景知識相關聯，從而使讀者能夠隱性地豐富文本中的未陳述信息。最近的大型語言模型（LLMs）在各種下游任務中取得了強勁的表現。然而，目前尚不清楚它們是否能夠重現人類在理解過程中自然進行的隱性豐富。為了解決這個問題，我們介紹了FrameBench，一個基於框架語意學的基準。FrameBench由多選題組成，測試模型是否能夠區分在不同語境中由同一動詞引發的框架。我們使用FrameNet風格的資源和帶有母語者判斷的生成與驗證流程來構建英語和日語的基準。我們對一組多樣的模型進行的實驗揭示了小型模型面臨的挑戰，而幾個大型模型則超越了人類參考分數。我們在 https://github.com/SasanoLab/FrameBench 上發布了構建的FrameBench數據集以及數據集構建和評估的代碼。

##### **ALRA: Adaptive Local Relational Alignment for Logit-Based Pre-training Distillation of Autoregressive Language Models**
2609.03355v1 by Quang Hoang Trung, Quang Huu Hieu, Nguyen Van Hoang Phuc, Vo Nguyen Le Duy

Logit-based knowledge distillation for autoregressive language models usually aligns teacher and student next-token distributions over the entire vocabulary. However, this global objective overlooks relative preferences among likely token alternatives. Existing local approaches often select candidate tokens from either the teacher or the student alone. Teacher-only selection can miss tokens that the student considers likely, while student-only selection can rely on an inaccurate ranking early in training. We propose Adaptive Local Relational Alignment (ALRA), a position-specific framework combining student proposals with teacher guidance. At each valid prediction position, the student proposes likely tokens, while the teacher's most probable token is included as an anchor. ALRA adjusts the number of selected tokens according to how broadly the teacher distributes probability within this candidate set relative to the current batch. Adaptive Local Divergence retains the mass-matching term and separately matches the relative token distributions within the selected and remaining vocabulary regions. Unlike the exact full-vocabulary decomposition, it replaces the teacher-mass coefficients of the two conditional terms with unit coefficients, preventing either term from being downweighted solely because its region has low teacher probability. Student-Weighted Pairwise Relational Alignment emphasizes high-probability token pairs with small student probability gaps and gives less weight to unlikely or clearly separated pairs. Experiments on The Pile with randomly initialized 200M- and 500M-parameter students across nine zero-shot benchmarks yield average accuracies of 36.62% and 37.40%. ALRA exceeds the strongest competing distillation baseline by 0.94 and 0.83 percentage points and improves over pre-training without distillation by 2.31 and 2.91 points, respectively.

摘要：Logit-based 知識蒸餾對於自回歸語言模型通常會對整個詞彙表的教師和學生下一個標記的分佈進行對齊。<lb/>然而，這個全局目標忽略了在可能的標記選擇之間的相對偏好。<lb/>現有的本地方法通常僅從教師或學生中選擇候選標記。<lb/>僅教師的選擇可能會錯過學生認為可能的標記，而僅學生的選擇可能依賴於訓練初期不準確的排名。<lb/>我們提出了自適應本地關係對齊（ALRA），這是一個位置特定的框架，將學生的提議與教師的指導結合起來。<lb/>在每個有效的預測位置，學生提出可能的標記，而教師的最可能標記則作為錨點包含在內。<lb/>ALRA 根據教師在這個候選集內相對於當前批次的概率分佈範圍調整選擇的標記數量。<lb/>自適應本地散度保留了質量匹配項，並分別匹配所選和剩餘詞彙區域內的相對標記分佈。<lb/>與精確的全詞彙分解不同，它用單位係數替換了兩個條件項的教師質量係數，防止任何一項因其區域的教師概率低而被降低權重。<lb/>學生加權成對關係對齊強調具有小學生概率差距的高概率標記對，並對不太可能或明顯分離的標記對給予較少的權重。<lb/>在 The Pile 上進行的實驗中，隨機初始化的 200M 和 500M 參數學生在九個零樣本基準上產生了平均準確率分別為 36.62% 和 37.40%。<lb/>ALRA 超過了最強競爭蒸餾基線 0.94 和 0.83 個百分點，並分別在沒有蒸餾的預訓練上提高了 2.31 和 2.91 個點。

##### **From Zero to Hero: An Open LLM Ecosystem for Armenian**
2609.03350v1 by Erik Arakelyan, Khatun Avetisyan, Meri Davtyan, Heghine Grigoryan, Nane Khachatryan, Hayk Shahsuvaryan, Henrik Sergoyan, Vahan Martirosyan

Pretraining data for Armenian, a morphologically rich and low-resource language, is scarce, and no open Armenian LLM has been released with the data and recipe needed to reproduce it. To address this gap, we curate and release two datasets. ArmWeb is an extensively validated corpus of 4.37M Armenian news documents. ArmSTEM is a parallel English-Armenian collection of 373K math and science problems with step-by-step solutions, translated into Armenian and verified through both answer-preserving LLM judgment and human evaluation. Continued pretraining of Gemma-4-E4B on these datasets yields arm-gemma-e4b, which outperforms every existing open Armenian model as well as its unadapted base, and is the first open Armenian LLM with complete training data and recipe. Our ablations show that news-only continued pretraining improves fluency while eroding knowledge, a pattern we also observe in existing Armenian models, and that a small share of verified translated STEM data reverses the loss. We further find that the largest public Armenian corpora overlap web-derived evaluation panels heavily, including a train/test self-overlap inside FineWeb-2. We openly release all data, models, and code.

摘要：預訓練資料對於亞美尼亞語這種形態豐富且資源稀缺的語言來說非常稀少，且尚未有公開的亞美尼亞LLM釋出所需的資料和配方來重現它。為了填補這一空白，我們整理並釋出兩個數據集。ArmWeb是一個經過廣泛驗證的亞美尼亞新聞文檔語料庫，包含4.37M篇文檔。ArmSTEM是一個373K數學和科學問題的英亞雙語平行集合，附有逐步解答，已翻譯成亞美尼亞語並通過保留答案的LLM判斷和人工評估進行驗證。對這些數據集持續進行Gemma-4-E4B的預訓練，產生了arm-gemma-e4b，該模型在所有現有的公開亞美尼亞模型中表現最佳，並且超越了其未適應的基礎模型，成為第一個擁有完整訓練資料和配方的公開亞美尼亞LLM。我們的消融實驗顯示，僅基於新聞的持續預訓練提高了流暢性，但削弱了知識，這一模式在現有的亞美尼亞模型中也有觀察到，而少量經過驗證的翻譯STEM數據則能逆轉這一損失。我們進一步發現，最大的公共亞美尼亞語料庫與網絡衍生的評估面板有很大重疊，包括FineWeb-2內部的訓練/測試自重疊。我們公開釋出所有數據、模型和代碼。

##### **PACE: Towards Surfacing Hidden Conflicts in User Requests**
2609.03293v1 by Yoojin Kim, Jihyoung Jang, Hyounghun Kim

Personalized assistants should not only comply with user requests but also assess whether those requests are appropriate given the user's current circumstances. However, prior work has primarily focused on accurately executing requests, overlooking the need for assistants to account for context and engage in conflict-based refusal. Furthermore, while existing work on conflict or safety detection relies on explicitly provided factors, real-world scenarios often involve implicit factors that must be retrieved from a knowledge base (KB). To this end, we introduce Personalized Assistants for Conflict Evaluation (PACE), a dataset for evaluating whether models can identify latent constraints, expressed as egocentric knowledge or events, that render seemingly reasonable user requests inappropriate. PACE pairs user requests grounded in well-defined personas with egocentric KB facts, requiring models to integrate contextual evidence to determine whether a request is conflicting. This implicit retrieval setting hinders the direct association between user requests and conflict-inducing knowledge, making it difficult for existing models to identify relevant user-specific facts. To address this challenge, we further propose PaceMaker, a multi-agent framework in which specialized agents coordinate across query reformulation, multi-hop graph traversal, and conflict-aware filtering to retrieve contextually decisive evidence. Experiments on PACE evaluate both evidence retrieval quality and conflict decision accuracy, showing that PaceMaker consistently outperforms existing approaches.

摘要：個性化助手不僅應該遵從用戶的請求，還應評估這些請求在用戶當前情況下是否合適。<lb/>然而，之前的工作主要集中在準確執行請求上，忽略了助手需要考慮上下文並進行基於衝突的拒絕的必要性。<lb/>此外，雖然現有的衝突或安全檢測工作依賴於明確提供的因素，但現實場景通常涉及必須從知識庫（KB）中檢索的隱含因素。<lb/>為此，我們引入了衝突評估個性化助手（PACE），這是一個用於評估模型是否能識別潛在約束的數據集，這些約束以自我中心的知識或事件表達，使得看似合理的用戶請求變得不合適。<lb/>PACE 將基於明確定義的人物角色的用戶請求與自我中心的 KB 事實配對，要求模型整合上下文證據以確定請求是否存在衝突。<lb/>這種隱式檢索設置妨礙了用戶請求與引發衝突的知識之間的直接關聯，使得現有模型難以識別相關的用戶特定事實。<lb/>為了解決這一挑戰，我們進一步提出了 PaceMaker，一個多代理框架，其中專門的代理協調查詢重構、多跳圖遍歷和衝突感知過濾，以檢索具有上下文決定性的證據。<lb/>在 PACE 上的實驗評估了證據檢索質量和衝突決策準確性，顯示出 PaceMaker 始終優於現有方法。

##### **LLMs Learn Better In-Context from Rules than from Examples**
2609.03213v1 by Xiang Fu, Seungmin Cho, Yukyung Lee, Najoung Kim

Large language models (LLMs) exhibit in-context learning capabilities, where they can learn new tasks from prompt contexts without weight updates. We compare the learning efficacies of two prominent modes of in-context learning: (1) learning from descriptions of rules (instruction following); and (2) learning from examples of input-output demonstrations (few-shot prompting). Through five learning tasks that cover diverse domains (games, arithmetic, linguistic inferences), we compare two modes of learning (rules vs. examples) specifying the same underlying task. We furthermore explore model and task properties that modulate the learning efficacies. We find that models generally learn more reliably from rules than from examples alone, and additional examples on top of rules or simply scaling up the number of examples do not lead to consistent and significant gains. Instruction tuning amplifies the benefit of rule-based learning while keeping example-based learning capacities intact. Surprisingly, we find no privileged effect of example-based learning in base models, and rules still lead to gains in algebraic task domains. Overall, the comparative efficacy of rules over examples is larger when the task recruits algebraic abstractions and computations, and smaller when the task requires distributional sensitivity and/or recruits parametric knowledge.

摘要：大型語言模型（LLMs）展現了上下文學習的能力，能夠在不更新權重的情況下從提示上下文中學習新任務。我們比較了兩種主要的上下文學習模式的學習效率：（1）從規則描述中學習（遵循指示）；以及（2）從輸入輸出示範的例子中學習（少量提示）。通過五個涵蓋不同領域（遊戲、算術、語言推理）的學習任務，我們比較了兩種學習模式（規則與例子），這些模式指定了相同的基本任務。我們進一步探討了調節學習效率的模型和任務特性。我們發現模型通常從規則中學習的可靠性高於僅從例子中學習，並且在規則基礎上增加額外的例子或僅僅擴大例子的數量並不會帶來一致且顯著的增益。指令調整增強了基於規則學習的好處，同時保持了基於例子學習的能力。令人驚訝的是，我們發現在基礎模型中，基於例子的學習並沒有特別的效果，而規則仍然在代數任務領域中帶來增益。總體而言，當任務涉及代數抽象和計算時，規則相對於例子的比較效率更高，而當任務需要分佈敏感性和/或涉及參數知識時，則較小。

##### **MemoryLACE: Memory Lifecycle-Aware Consolidation and Evidence Retrieval**
2609.03201v1 by Meriem Yacoubi, Pia Schmidt, Nenad Petrovic, Ahmed Frikha, Martin Kirchhoff, Alois Knoll

Long-term LLM agents must preserve information across interactions while distinguishing repeated evidence, historical states, updates, and unresolved contradictions. Existing textual memory systems retrieve semantically relevant memories efficiently but often leave these relationships implicit, whereas richer structured approaches model them through global graphs, hierarchical abstractions, or reflection at greater complexity. We introduce MemoryLACE (MemLACE), a lightweight memory framework that explicitly models the lifecycle of textual evidence through sparse merge, supersession, and contradiction relations while preserving atomic natural-language memories and their provenance. Rather than retrieving memories independently, MemLACE reconstructs relation-aware evidence units that expose current, historical, supporting, and conflicting evidence for downstream reasoning. Across BEAM and StructMemEval, using open-weight and proprietary LLM backbones, MemLACE achieves the highest overall performance in same-backbone comparisons while reducing end-to-end runtime on BEAM by 66.6% relative to Hindsight, the strongest reported reflective-memory baseline. Ablation studies identify lifecycle expansion and temporal awareness as the principal contributors to these gains. Together, the results demonstrate that explicitly modeling the local lifecycle of textual evidence is sufficient to substantially improve long-term memory reasoning without requiring comprehensive knowledge graphs or global reflection.

摘要：長期的 LLM 代理必須在互動中保留資訊，同時區分重複的證據、歷史狀態、更新和未解決的矛盾。現有的文本記憶系統有效地檢索語義相關的記憶，但通常將這些關係隱含，而更豐富的結構化方法則通過全球圖、層次抽象或更高複雜度的反思來建模這些關係。我們介紹了 MemoryLACE (MemLACE)，這是一個輕量級的記憶框架，明確建模文本證據的生命周期，通過稀疏合併、取代和矛盾關係，同時保留原子自然語言記憶及其來源。MemLACE 並非獨立檢索記憶，而是重建關係感知的證據單元，揭示當前、歷史、支持和衝突的證據，以便進行下游推理。在 BEAM 和 StructMemEval 中，使用開放權重和專有 LLM 骨幹，MemLACE 在同骨幹比較中實現了最高的整體性能，同時相對於 Hindsight（最強的報告反思記憶基準），在 BEAM 上減少了 66.6% 的端到端運行時間。消融研究確定生命周期擴展和時間意識是這些增益的主要貢獻者。綜合結果顯示，明確建模文本證據的局部生命周期足以顯著改善長期記憶推理，而無需全面的知識圖或全球反思。

##### **Large Language Models in Resolving Contextual Knowledge Conflicts**
2609.03148v1 by Xinye Yang, Zhenyang Liu, Ruisi Li, Yuanyuan Lei

Most prior works focused on conflicts between an LLM's internal parametric knowledge and externally provided context. In contrast, we investigate how LLMs handle conflicts that arise within contextual knowledge itself. We introduce a taxonomy of six types of contextual conflicts (factual, inferential, temporal, granularity, perspective, and ambiguity) and contribute a comprehensive dataset ContextConflict for this setting. The dataset contains 5,781 samples, covers both reasoning and summarization tasks, and includes both explicit contradictions and implicit conflicts that require multi-step reasoning. Experiments on nine LLMs show that current models still fall short in resolving contextual knowledge conflicts. We further provide mechanistic interpretability insights into how LLMs process such conflicts, revealing their latent awareness of conflicts and the representational geometry underlying conflict processing. In addition, our analysis uncovers a consistent model bias towards earlier evidence, and this positional preference serves as a key obstacle to effective conflict resolution. Motivated by these findings, we further propose a simple training-free, label-free steering method that steers activations to encourage a more comprehensive incorporation of evidences for better conflict resolution. On our dataset, the method consistently improves accuracy on reasoning tasks and generates higher-quality, more balanced summaries for summarization tasks.

摘要：大多數先前的研究集中於大型語言模型（LLM）內部參數知識與外部提供的上下文之間的衝突。相較之下，我們研究 LLM 如何處理上下文知識內部產生的衝突。我們介紹了六種類型的上下文衝突的分類法（事實性、推論性、時間性、細節性、視角性和模糊性），並為這一設定貢獻了一個全面的數據集 ContextConflict。該數據集包含 5,781 個樣本，涵蓋推理和總結任務，並包括明確的矛盾和需要多步推理的隱性衝突。在九個 LLM 上的實驗顯示，當前模型在解決上下文知識衝突方面仍然存在不足。我們進一步提供了機制可解釋性的見解，揭示了 LLM 如何處理這些衝突，顯示它們對衝突的潛在認知以及衝突處理背後的表徵幾何。此外，我們的分析揭示了一種一致的模型偏見，傾向於早期證據，這種位置偏好成為有效解決衝突的主要障礙。受到這些發現的啟發，我們進一步提出了一種簡單的無需訓練、無需標籤的引導方法，該方法引導激活以促進更全面地納入證據，以更好地解決衝突。在我們的數據集上，該方法在推理任務中穩定提高了準確性，並為總結任務生成了更高質量、更平衡的摘要。

##### **Towards Trustworthy Autonomous Robots: An Explainable AI-Based Decision Framework**
2609.02861v1 by Cagri Temel

Autonomous robots powered by deep learning face a fundamental auditability challenge: when incidents occur, investigators cannot reconstruct why the system made specific decisions. This paper presents TRACE (Transparent Reasoning Architecture for Credible Execution), a decision framework that ensures every autonomous action can be traced back to sensor evidence through documented causal chains. The framework organizes decision-making into four auditable layers: Semantic Perception for evidence-grounded entity recognition, Belief Reasoning for probabilistic state estimation with causal graphs, Action Synthesis for constraint-aware planning with counterfactual documentation, and Execution Verification for compliance monitoring. TRACE is model-agnostic yet designed to integrate learning-based perception modules (CNNs, transformers) while preserving decision-level auditability. We evaluate the framework using three objective metrics: Evidence Traceability (sensor-to-decision linkage), Decision Reconstructability (post-hoc analysis capability), and Temporal Continuity (audit trail completeness). Experimental evaluation on warehouse robot navigation demonstrates that TRACE achieves 98.6% evidence traceability, 99.0% temporal continuity, and 98.1% decision reconstructability across 500 simulated decision cycles. Post-hoc methods like LIME provide feature attributions but lack the artifact structure needed for decision-level reconstruction. The framework addresses EU AI Act requirements for high-risk system transparency and contributes to Explainable AI for safety-critical autonomous systems.

摘要：自主機器人由深度學習驅動，面臨一個基本的可審計性挑戰：當事件發生時，調查人員無法重建系統為何做出特定決策的原因。本文提出了TRACE（透明推理架構以確保可信執行），這是一個決策框架，確保每個自主行動都可以通過記錄的因果鏈追溯到傳感器證據。該框架將決策過程組織為四個可審計的層次：語義感知用於基於證據的實體識別，信念推理用於帶有因果圖的概率狀態估計，行動合成用於帶有反事實文檔的約束感知規劃，以及執行驗證用於合規監控。TRACE是模型無關的，但設計上旨在整合基於學習的感知模塊（CNNs、Transformer），同時保持決策級的可審計性。我們使用三個客觀指標來評估該框架：證據可追溯性（傳感器到決策的連結）、決策可重建性（事後分析能力）和時間連續性（審計跟蹤的完整性）。在倉庫機器人導航的實驗評估中，TRACE在500個模擬決策循環中實現了98.6%的證據可追溯性、99.0%的時間連續性和98.1%的決策可重建性。事後方法如LIME提供特徵歸因，但缺乏決策級重建所需的工件結構。該框架滿足歐盟人工智慧法案對高風險系統透明度的要求，並為安全關鍵的自主系統貢獻了解釋性人工智慧。

##### **Post-Training Language Models for Gold-Medal Performance in Coding Competitions**
2609.02849v1 by Aleksander Ficek, Sean Narenthiran, Mehrzad Samadi, Somshubra Majumdar, Boris Ginsburg

Competitive programming has become a key test of large language model reasoning, with international competitions such as IOI and ICPC representing its most challenging settings. We present an end-to-end specialization pipeline combining large-scale problem curation, synthetic reasoning traces, supervised fine-tuning (SFT), and reinforcement learning (RL). Using 22,000 curated problems, we train Nemotron-3-Nano-CC (30B-A3B) with SFT and RL and Nemotron-3-Ultra-CC (550B-A55B) with SFT alone. We further introduce GenCorrect, a feedback-driven test-time compute strategy that iteratively generates, evaluates, and refines diverse solutions. On IOI 2025, Nano-CC improves from 130 points to 291 after post-training and to 468 with GenCorrect, exceeding the gold threshold of 438.3 while Ultra-CC reaches 502. Guided by these results, we develop a competition-specific Ultra-CC system and evaluate it prospectively during IOI 2026. Under the same time, internet-access, and submission constraints as human contestants, it scores 535.4 out of 600, exceeding both the gold threshold of 361.12 and the top human score of 498.27. To our knowledge, this is the first AI system to outscore the highest-scoring human contestant on an IOI problem set.

摘要：競賽編程已成為大型語言模型推理的一項關鍵測試，國際競賽如 IOI 和 ICPC 代表了其最具挑戰性的環境。<lb/>我們提出了一個端到端的專門化管道，結合了大規模問題策劃、合成推理痕跡、監督微調（SFT）和強化學習（RL）。<lb/>使用 22,000 個策劃問題，我們用 SFT 和 RL 訓練了 Nemotron-3-Nano-CC（30B-A3B），並僅用 SFT 訓練了 Nemotron-3-Ultra-CC（550B-A55B）。<lb/>我們進一步介紹了 GenCorrect，一種基於反饋的測試時間計算策略，通過迭代生成、評估和改進多樣化解決方案。<lb/>在 IOI 2025 中，Nano-CC 在後訓練後從 130 分提高到 291，並在使用 GenCorrect 後達到 468，超過了 438.3 的金牌門檻，而 Ultra-CC 則達到了 502。<lb/>根據這些結果，我們開發了一個特定於競賽的 Ultra-CC 系統，並在 IOI 2026 中進行了前瞻性評估。<lb/>在與人類參賽者相同的時間、網絡訪問和提交限制下，它的得分為 535.4 分（滿分 600），超過了 361.12 的金牌門檻和 498.27 的最高人類得分。<lb/>據我們所知，這是第一個在 IOI 問題集上超越最高得分人類參賽者的 AI 系統。

##### **Evaluating Graph Neural Networks for Change-Criticality Classification in Maritime Navigation Charts**
2609.02996v1 by Abhishek Potnis, Jacob Arndt

Graph neural networks (GNNs) are a class of neural networks suitable for learning on graph-structured data. Their application to spatial data is a natural extension, however its relatively unclear which message-passing operations, architectural configurations, and graph representation is best suited for classifying changes to objects in electronic navigational charts (ENCs)--geospatial vector datasets used for marine navigation. Maintaining these datasets is a challenge, and categorizing changes to objects in the ENC based on their significance to navigational safety is of particular importance. Here, we propose to represent these vector navigation datasets as a graph structure where the spatial objects serve as nodes and their spatial and semantic relationships form edges. We encode both the old ENC dataset and new ENC dataset into a pair of graphs and frame the task as a graph-pair classification problem. Building on this representation, we investigate the use of GNN architectures to classify whether the encoded graphs constitutes a critical or non-critical risk to navigational safety. We train and evaluate several GNN architectures and model configurations on ENC changes reviewed by maritime experts. Our results demonstrate that graph-based representations improve the classification of ENC updates, providing a scalable approach for automating or improving ENC maintenance workflows.

摘要：圖神經網絡（GNNs）是一類適合於圖結構數據學習的神經網絡。它們在空間數據上的應用是一個自然的延伸，然而目前尚不清楚哪些消息傳遞操作、架構配置和圖表示最適合用於對電子導航圖（ENCs）中的物體變化進行分類——這是用於海洋導航的地理空間向量數據集。維護這些數據集是一項挑戰，根據物體對導航安全的重要性對ENC中的變化進行分類尤為重要。在這裡，我們提出將這些向量導航數據集表示為一個圖結構，其中空間物體作為節點，而它們的空間和語義關係形成邊。我們將舊的ENC數據集和新的ENC數據集編碼為一對圖，並將任務框架設置為圖對分類問題。在這一表示的基礎上，我們研究使用GNN架構來分類編碼的圖是否構成對導航安全的關鍵或非關鍵風險。我們在由海事專家審查的ENC變化上訓練和評估了幾種GNN架構和模型配置。我們的結果表明，基於圖的表示改善了ENC更新的分類，提供了一種可擴展的方法來自動化或改善ENC維護工作流程。

##### **Large Language Models (LLMs) for Telecom Root Cause Analysis (RCA): A Structured Reasoning Framework for Evidence-Grounded Diagnosis**
2609.02805v1 by Hao Zhou, Mandar Kulkarni, Hao Chen, Yan Xin,  Charlie,  Zhang

Root cause analysis (RCA) is a critical task in telecom network operations, but diagnosing performance degradations in modern 5G and emerging 6G networks remains challenging due to complex cross-layer dependencies. While large language models (LLMs) offer promising capabilities for reasoning and knowledge integration, directly applying vanilla LLMs to telecom RCA often leads to hallucination, unstable reasoning, and poor alignment with structured network evidence. This work first reviews the evolution of telecom RCA from rule-based and machine learning (ML) approaches to emerging LLM-enabled techniques, and provides an overview of recent paradigms, including structured reasoning, retrieval-augmented knowledge grounding, agentic orchestration, and verifiable reasoning. Building upon these insights, we propose a structured reasoning framework for LLM-enabled telecom RCA that aligns diagnostic reasoning with telecom-specific evidence and domain knowledge. The proposed approach first organizes heterogeneous network telemetry into canonical contexts, and then enforces decision-path reasoning during diagnosis, and finally generates evidence-grounded explanations for reliable fault identification. Experimental results on two 5G RCA datasets, TeleLogs and TelecomTS, demonstrate that the proposed framework consistently improves diagnostic accuracy and decision consistency compared with baseline techniques. These cross-dataset results highlight the importance of structured reasoning design for practical LLM-based RCA systems in next-generation telecom networks.

摘要：根本原因分析（RCA）是電信網絡運營中的一項關鍵任務，但由於複雜的跨層依賴，診斷現代5G和新興6G網絡中的性能下降仍然具有挑戰性。雖然大型語言模型（LLMs）在推理和知識整合方面提供了有前景的能力，但將普通的LLMs直接應用於電信RCA往往會導致幻覺、不穩定的推理和與結構化網絡證據的不良對齊。這項工作首先回顧了電信RCA從基於規則和機器學習（ML）方法到新興的LLM支持技術的演變，並提供了最近範式的概述，包括結構化推理、檢索增強的知識基礎、代理協調和可驗證推理。在這些見解的基礎上，我們提出了一個結構化推理框架，用於LLM支持的電信RCA，該框架將診斷推理與電信特定的證據和領域知識對齊。所提出的方法首先將異構網絡遙測組織成典範上下文，然後在診斷過程中強制執行決策路徑推理，最後生成基於證據的解釋，以實現可靠的故障識別。在兩個5G RCA數據集TeleLogs和TelecomTS上的實驗結果顯示，所提出的框架在診斷準確性和決策一致性方面始終優於基準技術。這些跨數據集的結果突顯了為下一代電信網絡中的實用LLM基礎RCA系統設計結構化推理的重要性。

##### **frb100-40 After Two Decades: An Optimality Certificate and a Preregistered Search Study**
2609.02804v1 by Onur Uğurlu

For more than 20 years, the Model-RB benchmark frb100-40 remained an open challenge; since 2014, its public record had stood at 99 of 100 variables. We give a directly checkable 100-vertex independent set for its 4,000-vertex graph. Together with a verified partition into 100 cliques of size 40, the witness proves that the maximum independent-set size is 100 and the minimum vertex-cover size is 3,900. The stochastic run that found the witness is kept separate from this proof. We evaluated its added pair and triple repair operators in a preregistered campaign comprising 8,668 valid runs. The primary comparison found no detectable acceleration over base ULSA (hazard ratio 0.967, 95% confidence interval 0.915-1.023; p=0.248), and the factorial ablation reached the same conclusion. On a smaller FRB suite, the group-aware CSP pipeline solved 2,500/2,500 runs, compared with 2,391/2,500 for LibMVC-NuMVC. On frb100-40, full ULSA, base ULSA, and NuMVC each produced 0/56 new certificates. With no events, the planned cross-solver hazard ratios remain unidentified. NuMVC ended with cover size 3,902 in 40 runs and 3,903 in 16. Exhaustive enumeration showed that none of the 108 unique recorded conflict-two states had a strictly improving group-aware CSP neighbor within Hamming radius three. The certificate settles the instance. The experiments characterize the search barrier, and the preregistered comparisons show no heuristic advantage.

摘要：超過20年來，Model-RB基準frb100-40一直是一個未解的挑戰；自2014年以來，其公共記錄保持在100個變數中的99個。我們為其4,000個頂點的圖提供了一個可直接檢查的100頂點獨立集。結合對100個大小為40的團體的驗證分區，證明了最大獨立集大小為100，最小頂點覆蓋大小為3,900。找到證人的隨機運行與此證明分開。我們在一個預註冊的活動中評估了其新增的對和三元修復運算符，該活動包含8,668次有效運行。主要比較未發現比基礎ULSA有可檢測的加速（危險比0.967，95%置信區間0.915-1.023；p=0.248），而因子消融也得出了相同的結論。在一個較小的FRB套件中，群體感知CSP管道解決了2,500/2,500次運行，而LibMVC-NuMVC則為2,391/2,500。在frb100-40上，完整ULSA、基礎ULSA和NuMVC各自產生了0/56個新證書。由於沒有事件，計劃的交叉求解器危險比仍未確定。NuMVC在40次運行中以覆蓋大小3,902結束，在16次運行中為3,903。徹底列舉顯示，108個獨特記錄的衝突二狀態中沒有一個在哈明半徑三內有嚴格改善的群體感知CSP鄰居。該證書解決了這個實例。實驗特徵化了搜索障礙，預註冊的比較顯示沒有啟發式優勢。

##### **Repo-To-Skill: Distilling GitHub Repositories Into AI4AI Skills**
2609.02749v1 by Jianlyu Chen, Yuyang Hu, Hongjin Qian, Jiawei Liu, Wenqing Wei, Xiaolong Chen, Defu Lian, Zhicheng Dou, Chaozhuo Li, Qiwei Ye, Zheng Liu

Autonomous agents are beginning to carry out machine-learning (ML) research end to end. These agents combine a model backbone with a harness for planning, execution, memory, and verification, but this architecture still leaves domain-specific know-how outside the agent. We call this missing layer operational knowledge, the know-how that separates knowing a method from making it work. That knowledge is not absent from the field. It appears in repositories and papers, but in forms written for human readers and too large to load during a task. Once distilled into compact, verified skills, this knowledge can be reused across tasks rather than rediscovered during each run.
  We present DisCo, a skill-powered research agent that creates skills and uses them during research. Its distillation runs in two complementary forms: task-agnostic, condensing the field's widely used repositories into reusable skills, and task-oriented, producing the skills a concrete task calls for. The former, applied across the open ecosystem, yields the AREX-Skill Library, with 5,000+ verified skills distilled from 1,000 widely used ML repositories and organized into 20 areas and 178 capability families. With the GPT-5.5 backbone, research harness, and downstream execution budget held fixed, the skill-equipped research agent scores 134.3% higher on MLE-bench, 34.4% higher on PaperBench, 9.2% higher on FrontierCS, and 14.0% higher on PassNet than the same agent without skills. These gains come from adding distilled operating context under that fixed setup.

摘要：自主代理開始進行端到端的機器學習（ML）研究。這些代理結合了模型骨幹與規劃、執行、記憶和驗證的框架，但這種架構仍然將特定領域的專業知識排除在代理之外。我們稱這一缺失的層次為操作知識，即將知道一種方法與使其運作區分開來的專業知識。這種知識在該領域並非缺失。它出現在資料庫和論文中，但以人類讀者可讀的形式存在，且在執行任務時過於龐大而無法加載。一旦被提煉成緊湊且經過驗證的技能，這些知識可以在不同任務之間重複使用，而不是在每次運行中重新發現。

我們介紹DisCo，一個以技能為驅動的研究代理，能夠創建技能並在研究過程中使用它們。它的提煉以兩種互補的形式進行：任務無關的，將該領域廣泛使用的資料庫濃縮為可重用的技能，以及任務導向的，生成具體任務所需的技能。前者應用於開放生態系統中，產生了AREX-Skill Library，該庫包含超過5,000個經過驗證的技能，這些技能來自1,000個廣泛使用的ML資料庫，並組織成20個領域和178個能力家族。使用GPT-5.5骨幹、研究框架和固定的下游執行預算，這個配備技能的研究代理在MLE-bench上得分高出134.3%，在PaperBench上高出34.4%，在FrontierCS上高出9.2%，在PassNet上高出14.0%，相比於沒有技能的同一代理。這些增益來自於在該固定設置下添加提煉的操作背景。

##### **HiPoly: a hierarchical polymer-native AI framework for property prediction and generative design**
2609.02746v1 by Ge Sun, Gervasio Zaldivar, Yuan Tian, Gustavo Perez Lemus, Juhae Park, Dasha Safarian, Ming Han, Juan J. de Pablo

Polymeric materials are central to modern technologies, with applications ranging from energy to health and transportation. Although AI has made significant advances in materials discovery, the hierarchical structure of polymers across multiple length scales makes them inherently difficult to represent in a unified and physically meaningful way. Here we introduce HiPoly, a polymer-native AI framework that processes complete polymer descriptions through a three-level hierarchical graph architecture built on the G2RINS representation. HiPoly encodes stochastic inter-monomer connectivity, composition, and molecular weight directly within its architecture, using physically motivated design principles that mirror the multi-scale nature of polymeric systems. The framework establishes an end-to-end AI-driven workflow from experimental formulation data to property prediction, generative molecular design, and physics-based validation through molecular simulations, all unified by a single polymer representation. We demonstrate state-of-the-art prediction accuracy for thermophysical properties of multi-component polymer systems, with ablation studies confirming that each hierarchical design choice contributes independently to model performance. As an example, the generative design pathway is applied here to the discovery of sustainable alternatives to persistent fluorinated polymers, where it is possible to identify and independently validate PFAS-free candidates with target surface-energy properties. This work demonstrates how polymer-native AI can accelerate discovery by linking representation, prediction, and design across complex polymer chemistries.

摘要：聚合材料是現代技術的核心，應用範圍從能源到健康和交通。儘管人工智慧在材料發現方面取得了重大進展，但聚合物在多個長度尺度上的層次結構使其本質上難以以統一且物理上有意義的方式進行表示。在此，我們介紹 HiPoly，一個聚合物原生的人工智慧框架，通過基於 G2RINS 表示的三層層次圖架構處理完整的聚合物描述。HiPoly 在其架構中直接編碼隨機的單體間連接性、組成和分子量，使用反映聚合物系統多尺度特性的物理驅動設計原則。該框架建立了一個端到端的人工智慧驅動工作流程，從實驗配方數據到性質預測、生成分子設計及通過分子模擬的基於物理的驗證，所有這些都由單一的聚合物表示統一。我們展示了對多組分聚合物系統的熱物理性質的最先進預測準確性，消融研究確認每個層次設計選擇獨立地對模型性能作出貢獻。作為一個例子，這裡應用生成設計途徑來發現持久氟化聚合物的可持續替代品，在這裡可以識別並獨立驗證具有目標表面能特性的無 PFAS 候選者。這項工作展示了聚合物原生人工智慧如何通過連結表示、預測和設計來加速發現，跨越複雜的聚合物化學。

##### **DKL: Decoupled Knowledge Learning for Instruction-Tuned Language Models**
2609.02685v1 by Kushagra Bhushan, Meghanadh Pulivarthi, Sai Krishna Reddy Sathi, Gaurav Pandey, Sonam Gupta, Vineet Kumar, Jaydeep Sen, Yatin Nandwani, Sachindra Joshi, Dinesh Raghu

RAG has become the de facto method for incorporating new, corpus-specific knowledge into an instruction following LLM (Instruct LLM). Although RAG-based prompting improves factual grounding, it fails when retrieval is incorrect or incomplete, leading to hallucinations. Finetuning methods such as RAFT and PA-RAG enhance RAG by injecting new knowledge into the model's parameters, but require generating a massive amount of synthetic QA that covers the entire corpus. Extended Pre-Training (EPT) on the text corpus avoids the need for comprehensive synthetic data generation but compromises an Instruct LLM's instruction-following capabilities, necessitating instruction fine-tuning (IFT) after pre-training. However, IFT is costly and may be infeasible due to the unavailability of an instruction-tuning corpus. In this work, we propose DKL-Decoupled Knowledge Learning for Instruction-Tuned Language Models. Instead of doing EPT on the Instruct LLM, DKL performs EPT on its corresponding base LLM to infuse new knowledge. These knowledge infused weights are then merged with the Instruct LLM, imparting new knowledge without affecting their instruction-following capabilities. DKL is a lightweight method that avoids expensive instruction fine-tuning and relies on model merging to infuse the new knowledge into the Instruct LLM without destroying its instruction following capabilities. Empirical results show that DKL improves RAG accuracy from 54.17 to 79.26 on retrieval failure cases, while outperforming prior approaches with substantially less training data.

摘要：RAG 已成為將新的、特定語料庫的知識納入指令跟隨 LLM（Instruct LLM）的事實上方法。<lb/>雖然基於 RAG 的提示改善了事實基礎，但當檢索不正確或不完整時，它會失敗，導致幻覺。<lb/>微調方法如 RAFT 和 PA-RAG 通過將新知識注入模型的參數來增強 RAG，但需要生成大量涵蓋整個語料庫的合成 QA。<lb/>在文本語料庫上進行擴展預訓練（EPT）避免了對全面合成數據生成的需求，但會妨礙 Instruct LLM 的指令跟隨能力，因此在預訓練後需要進行指令微調（IFT）。<lb/>然而，IFT 成本高昂，並且由於缺乏指令調整語料庫，可能不可行。<lb/>在本研究中，我們提出了 DKL-解耦知識學習，用於指令調整的語言模型。<lb/>DKL 不是在 Instruct LLM 上進行 EPT，而是在其相應的基礎 LLM 上進行 EPT 以注入新知識。<lb/>這些注入知識的權重然後與 Instruct LLM 合併， imparting 新知識而不影響其指令跟隨能力。<lb/>DKL 是一種輕量級的方法，避免了昂貴的指令微調，並依賴模型合併將新知識注入 Instruct LLM，而不破壞其指令跟隨能力。<lb/>實證結果顯示，DKL 在檢索失敗案例中將 RAG 的準確率從 54.17 提高到 79.26，同時以顯著更少的訓練數據超越了先前的方法。

##### **Structure and Implementation of New Practical English Textbooks Driven by Artificial Intelligence**
2609.02981v1 by Ya Wang, Lei Zhang, Xueguang Yang, Bo Chen

Artificial intelligence is changing the form of applied English materials from fixed paper sequences to adaptive learning systems that can diagnose learners, recommend tasks, and provide formative feedback. This paper studies the structure and application of a new practical English textbook driven by artificial intelligence. A five-layer architecture is proposed: knowledge mapping, learner profiling, task generation, feedback orchestration, and teacher-side governance. A prototype was tested on 186 non-English-major undergraduates for eight weeks of teaching. Compared with a static digital textbook, the proposed system increased the unit completion accuracy from 72.4% to 84.9%, raised the average score for speaking tasks by 10.8 points, and reduced the teacher's correction time by 31.6%. Therefore, an AI-driven textbook can maintain the stability of the curriculum while providing personalised learning paths, rich practice materials and traceable classroom data.

摘要：人工智慧正在將應用英語材料的形式從固定的紙本序列轉變為能夠診斷學習者、推薦任務和提供形成性反饋的自適應學習系統。本文研究了一本由人工智慧驅動的新實用英語教科書的結構和應用。提出了一個五層架構：知識映射、學習者檔案、任務生成、反饋協調和教師端治理。原型在186名非英語專業的本科生中進行了為期八週的教學測試。與靜態數位教科書相比，所提議的系統將單元完成準確率從72.4%提高到84.9%，口語任務的平均分數提高了10.8分，教師的批改時間減少了31.6%。因此，人工智慧驅動的教科書能夠在提供個性化學習路徑、豐富的練習材料和可追蹤的課堂數據的同時，保持課程的穩定性。

##### **Spectral Initialization and Scheduled Graph Smoothness for Uncertain Knowledge Graph Completion**
2609.02519v1 by Md Abrar Jahin, Taufikur Rahman Fuad, Jay Pujara, Craig A. Knoblock

Uncertain knowledge graphs (UKGs) extend knowledge graphs by assigning each triple a continuous confidence score. Since most possible triples lack observed confidences, recent methods rely on semi-supervised learning to generate pseudo-labels. These methods initialize entity embeddings without using the confidence-weighted graph, discarding its global community and hub structure. We introduce QUEST, which adds no trainable parameters to the standard confidence-distribution learning pipeline. First, QUEST initializes entity embeddings using the smallest non-trivial eigenvectors of the confidence-weighted graph Laplacian, incorporating community and hub structure before training. Second, QUEST applies an unbiased mini-batch Dirichlet energy regularizer to enforce early-stage structural consistency. On two UKG datasets, QUEST improves confidence prediction and link prediction on six of eight metric-dataset pairs over prior methods and matches the previous best on the remaining two, while removing the instability spike observed on dense graphs. These results indicate that spectral structural priors combined with a graph Dirichlet energy regularizer improve accuracy, training stability, and checkpoint reliability in UKG completion.

摘要：不確定知識圖譜（UKGs）通過為每個三元組分配連續的置信度分數來擴展知識圖譜。由於大多數可能的三元組缺乏觀察到的置信度，最近的方法依賴於半監督學習來生成偽標籤。這些方法在初始化實體嵌入時不使用置信度加權圖，從而丟棄其全局社群和樞紐結構。我們介紹了QUEST，它在標準的置信度分佈學習管道中不添加任何可訓練的參數。首先，QUEST使用置信度加權圖拉普拉斯算子的最小非平凡特徵向量來初始化實體嵌入，在訓練之前納入社群和樞紐結構。其次，QUEST應用無偏的小批量Dirichlet能量正則化器，以強制執行早期階段的結構一致性。在兩個UKG數據集上，QUEST在八個指標-數據集對中的六個上改善了置信度預測和連結預測，並在其餘兩個上與之前的最佳結果持平，同時消除了在密集圖中觀察到的不穩定峰值。這些結果表明，結合光譜結構先驗和圖Dirichlet能量正則化器可以提高UKG補全的準確性、訓練穩定性和檢查點可靠性。

##### **RINSE: Robust Target-Time Normality Estimation for Zero-Shot Graph Anomaly Detection**
2609.02497v1 by Taufikur Rahman Fuad, Md Abrar Jahin, Amir Hussain

Zero-shot graph anomaly detection seeks to deploy a detector trained on source graphs to unseen, unlabeled targets, yet domain shift can make source-derived notions of normality unreliable. We introduce RINSE (Robust Iterative Normality Self-Estimation), a gradient-free target-time framework that keeps the source-trained detector fixed while sequentially estimating target normality, representation calibration, and evidence reliability from the target graph. Its core idea is to identify a reliable subset of low-residual target nodes, use them to construct a trimmed target-aware normality model, and combine complementary anomaly evidence through reliability-gated rank fusion and encoder ensembling. Across eight unseen target graphs, RINSE achieves the highest average AUPRC among the evaluated methods under two separate preprocessing protocols, while block ablations and sensitivity analyses support the combined design. These results support robust target-time estimation as a practical approach to generalist graph anomaly detection without target labels, gradients, or per-target tuning.

摘要：零樣本圖異常檢測旨在將訓練於源圖的檢測器部署到未見過的、未標記的目標上，但領域轉移可能使源派生的正常性概念變得不可靠。<lb/>我們介紹了RINSE（穩健迭代正常性自我估計），這是一個無梯度的目標時間框架，保持源訓練的檢測器固定，同時從目標圖中順序估計目標正常性、表示校準和證據可靠性。<lb/>其核心思想是識別一個可靠的低殘差目標節點子集，利用它們構建一個經過修剪的目標感知正常性模型，並通過可靠性閘控的排名融合和編碼器集成來結合互補的異常證據。<lb/>在八個未見的目標圖上，RINSE在兩個不同的預處理協議下實現了評估方法中最高的平均AUPRC，同時區塊消融和敏感性分析支持了這一綜合設計。<lb/>這些結果支持穩健的目標時間估計作為一種實用的方法，以進行無目標標籤、無梯度或無每個目標調整的通用圖異常檢測。

##### **NE-R1: Enhancing Named Entity Recognition Model via Reinforcement Learning**
2609.02366v1 by Meixuan Chen, Hehan Li, Ruizhi Zhao, Xin Lu, peizhi xu, Liwei Qian, LI Meifang, shuanglong li, Hanmeng Liu, Xin Pei, Yanbiao Ma

Named Entity Recognition (NER) has achieved substantial progress since the advent of large language models (LLMs). Nevertheless, the recognition of long-tail and domain-specific entities remains challenging due to the deficiency in parametric knowledge. Retrieval-augmented generation (RAG) offers a promising remedy by injecting external knowledge, but it also introduces noise and unnecessary cost when dealing with familiar cases. In this paper, we propose NE-R1, a novel framework for adaptive retrieval-augmented NER. We design a "retrieval-on-demand" mechanism for NER. Then we integrate it into models by a two-stage training method: (1) multi-task instruction tuning initialization; (2) end-to-end RL optimization with CoT. To achieve reasonable selection between parameterized and external knowledge, we design a multi-dimensional reward considering both accuracy and retrieval benefit. NE-R1 achieves state-of-the-art performance on various benchmarks, with an average F1 score gain of 2.52% in in-domain evaluation and 1.18% in zero-shot cross-domain evaluation.

摘要：命名實體識別 (NER) 自大型語言模型 (LLMs) 啟用以來已取得顯著進展。<lb/>然而，由於缺乏參數知識，長尾和特定領域實體的識別仍然具有挑戰性。<lb/>檢索增強生成 (RAG) 通過注入外部知識提供了一個有前景的解決方案，但在處理熟悉的案例時，這也引入了噪音和不必要的成本。<lb/>在本文中，我們提出了 NE-R1，一個用於自適應檢索增強 NER 的新框架。<lb/>我們為 NER 設計了一個「按需檢索」機制。<lb/>然後，我們通過兩階段訓練方法將其整合到模型中： (1) 多任務指令調整初始化； (2) 端到端的強化學習優化與 CoT。<lb/>為了在參數化知識和外部知識之間實現合理的選擇，我們設計了一個多維獎勵，考慮到準確性和檢索效益。<lb/>NE-R1 在各種基準上達到了最先進的性能，在領域內評估中平均 F1 分數增益為 2.52%，在零樣本跨域評估中為 1.18%。

##### **Towards a Foundational Ontology for Identifying and Resolving Contradictions in Dialogue-based Human-Robot Interactions**
2609.02364v2 by Maitreyee Tewari, Michele Persiani

Existing Human-Robot Interaction (HRI) literature has focused on identifying and structuring errors, failures, conflicts, and knowledge issues (called in this work as contradictions) in domain-specific dialogue-based interactions. However, there is still lack of a formal computational framework to represent and define these contradictions, interoperable and usable across HRI and human-agent interaction (HAI) domains. Thus, this research project aims to capture, represent, and evaluate the notion of (1) dialogue-based collaborative interaction and (2) related contradictions in a foundational ontology. METHONTOLOGY, a systematic approach to build domain-independent ontologies was applied. In the conceptualisation stage of the presented ontology, concepts and models from Activity Theory were used. Preliminary results presented in this short article are: (i) Natural language definitions of dialogues and related contradictions in HRI, (ii) Set Theoretic definitions of dialogues and contradictions, and (iii) First Order Logic (FoL) formulation of the contradiction concepts and three novel principles guiding dialogue-based interactions between humans and robots. In summary, we report on ongoing work to develop a foundational ontology based on Activity Theory called Activity Theory-based foundational ontology (ATFOt) to capture and represent the notion of contradictions in HRI.

摘要：現有的人機互動（HRI）文獻專注於識別和結構化錯誤、失敗、衝突和知識問題（在本研究中稱為矛盾）在特定領域的對話基礎互動中。<lb/>然而，仍然缺乏一個正式的計算框架來表示和定義這些矛盾，並且能夠在HRI和人機代理互動（HAI）領域之間互操作和使用。<lb/>因此，本研究項目旨在捕捉、表示和評估（1）基於對話的協作互動和（2）相關矛盾的概念，並建立一個基礎本體。<lb/>METHONTOLOGY，一種構建領域獨立本體的系統方法被應用。在所呈現的本體的概念化階段，使用了活動理論中的概念和模型。<lb/>本文中呈現的初步結果包括：（i）HRI中對話和相關矛盾的自然語言定義，（ii）對話和矛盾的集合論定義，以及（iii）矛盾概念的第一階邏輯（FoL）公式化和三個指導人類與機器人之間基於對話的互動的新原則。<lb/>總之，我們報告了正在進行的工作，以基於活動理論開發一個名為基於活動理論的基礎本體（ATFOt），以捕捉和表示HRI中矛盾的概念。

##### **Subcellularly Resolved Single-Cell Embedding Learning with Transcriptomic data, Protein Structure and Localization Information**
2609.02344v1 by Zhen Zhou, Jiachen Li, Yuan Liu, Xiaoyong Pan, Hong-Bin Shen

Existing cell embedding methods predominantly rely on transcriptomic or proteomic measurements and represent each cell as a holistic entity, thereby overlooking the subcellular localization of individual molecules. Moreover, they rarely incorporate protein structural information, despite its fundamental role in determining molecular interactions and functions. In this work, we propose a multimodal framework for learning subcellularly resolved cell embeddings by jointly leveraging RNA expression profiles, protein sequence representations, and protein structural information. Specifically, we employ a cross-attention architecture to integrate transcriptomic, sequence, and structural modalities and model their interactions within distinct subcellular compartments. The resulting embeddings represent each cell through its fine-grained subcellular organization, capturing both molecular expression patterns and the functional properties of the associated proteins. By learning cell representations at subcellular resolution, our framework preserves spatially organized biological information while integrating complementary signals across multiple molecular levels. To the best of our knowledge, this is the first framework that produces subcellularly resolved cell embeddings by jointly incorporating transcriptomic information, protein sequence representations, and protein structural knowledge within a unified cross-modal learning paradigm.

摘要：現有的細胞嵌入方法主要依賴轉錄組或蛋白質組測量，將每個細胞視為一個整體實體，因此忽略了個別分子在細胞內的定位。<lb/>此外，儘管蛋白質結構信息在確定分子相互作用和功能中扮演著基本角色，但這些方法很少將其納入考量。<lb/>在本研究中，我們提出了一個多模態框架，通過共同利用RNA表達譜、蛋白質序列表示和蛋白質結構信息來學習細胞的亞細胞解析嵌入。<lb/>具體而言，我們採用交叉注意力架構來整合轉錄組、序列和結構模態，並建模它們在不同亞細胞區室內的相互作用。<lb/>所得到的嵌入通過細緻的亞細胞組織來表示每個細胞，捕捉分子表達模式和相關蛋白質的功能特性。<lb/>通過在亞細胞解析度下學習細胞表示，我們的框架保留了空間組織的生物信息，同時整合了來自多個分子層次的互補信號。<lb/>據我們所知，這是第一個通過在統一的跨模態學習範式中共同納入轉錄組信息、蛋白質序列表示和蛋白質結構知識來生成亞細胞解析細胞嵌入的框架。

##### **Entangled Representations Amplify Collateral Damage in Unlearning**
2609.02285v1 by Evžen Wybitul, Tim G. J. Rudner, Christian Schroeder de Witt

A long-held intuition in interpretability research is that representational entanglement, the sharing of structure between knowledge domains in a neural network, makes unlearning harder. While the intuition is widespread, it has never been directly tested in a controlled experiment. We present a way to do so: by repurposing Selective Gradient Masking (SGTM), we train a suite of six 254M-parameter language models on English Wikipedia with graded levels of disentanglement between biology and non-biology knowledge. Applying three standard unlearning methods to every model in the suite, we find that more disentangled models consistently achieve better retain-forget trade-offs: at a fixed level of forgetting, the most disentangled models incur roughly $4\times$ lower retain cost under two of the three methods, and $1.3\times$ lower under the third. Because our intervention changes only the model, not the data or the unlearning algorithm, this is direct evidence that representational entanglement is one of the causes of collateral damage in unlearning, as interpretability researchers have long suspected. A similar design could be used to test other structural claims from interpretability.

摘要：一個長期存在於可解釋性研究中的直覺是，表示的糾纏，即神經網絡中知識領域之間結構的共享，使得去學習變得更加困難。雖然這一直覺廣泛存在，但從未在控制實驗中直接測試過。我們提出了一種方法：通過重新利用選擇性梯度遮罩（SGTM），我們在英語維基百科上訓練了一套六個254M參數的語言模型，並在生物學與非生物學知識之間進行了不同程度的解糾纏。對每個模型應用三種標準的去學習方法，我們發現，更加解糾纏的模型在保留與遺忘的權衡上始終表現更佳：在固定的遺忘水平下，最解糾纏的模型在三種方法中的兩種下，保留成本約低$4\times$，在第三種方法下低$1.3\times$。因為我們的干預只改變了模型，而不是數據或去學習算法，這直接證據表明表示的糾纏是去學習中附帶損害的原因之一，正如可解釋性研究者長期以來所懷疑的。類似的設計可以用來測試可解釋性中的其他結構性主張。

##### **Auditory Illusion Benchmark for Large Audio Language Models**
2609.02277v1 by Hayoon Kim, Eunice Hong, Kyogu Lee

Perceptual illusions have long served as crucial probes into human cognition, revealing biases and limitations of perception. In the auditory domain, such illusions provide a unique lens for testing whether Large Audio Language Models (LALMs) replicate human perceptual tendencies. Despite their importance, most benchmarks focus on visual illusions or general audio tasks, leaving auditory illusions underexplored. To this end, we present AIB, the first auditory illusion benchmark for LALMs, covering ten representative illusions across music, sound, and speech, each annotated for the presence of knowledge-based priors. Our methodology pairs model evaluation with controlled human listening studies, enabling direct comparison of responses. Results show systematic differences: while most LALMs remain signal-faithful on low-level acoustic illusions, several exhibit more human-like responses when linguistic or musical priors are involved, although no model matches the human perceptual profile. These findings highlight the current limitations of LALMs as cognitive models. By establishing auditory illusions as a rigorous testbed, our work offers a new perspective for probing neural black-box models and advancing understanding of auditory cognition. AIB is publicly available at https://github.com/gillosae/aib.

摘要：感知幻覺長期以來一直是探究人類認知的重要工具，揭示了感知的偏見和限制。在聽覺領域，這些幻覺提供了一個獨特的視角，以測試大型音頻語言模型（LALMs）是否重現人類的感知傾向。儘管它們的重要性，現有的大多數基準測試集中於視覺幻覺或一般音頻任務，導致聽覺幻覺未受到充分探索。為此，我們提出了AIB，首個針對LALMs的聽覺幻覺基準，涵蓋音樂、聲音和語音中的十個代表性幻覺，每個幻覺都標註了知識基礎先驗的存在。我們的方法將模型評估與受控的人類聆聽研究相結合，使得反應的直接比較成為可能。結果顯示出系統性的差異：雖然大多數LALMs在低級聲學幻覺上保持信號忠實，但在涉及語言或音樂先驗時，幾個模型表現出更類似人類的反應，儘管沒有模型能夠匹配人類的感知特徵。這些發現突顯了LALMs作為認知模型的當前局限性。通過將聽覺幻覺建立為一個嚴謹的測試平台，我們的工作為探究神經黑箱模型和推進聽覺認知的理解提供了一個新視角。AIB已在 https://github.com/gillosae/aib 上公開提供。

##### **Privacy-Preserving Topology-Guided Safety for LLM-Based Multi-Agent Systems via Federated Graph Learning**
2609.02967v1 by Jinxi Yu, Eric Hanchen Jiang, Levina Li, Dong Liu, Zhi Zhang, Wenxiao Zhao, Yanxuan Yu, Kai-Wei Chang, Ying Nian Wu

Topology-guided safeguards for LLM-based multi-agent systems (MAS) train a GNN over the inter-agent communication graph to localize risky agents and intervene on the topology---but they assume one operator can pool all labeled traces. Across organizations that assumption breaks: episodes contain private prompts, tool outputs, and proprietary workflows, and no silo alone sees the full attack distribution. We cast privacy-preserving MAS safeguarding as graph federated learning and instantiate FGLGuard: each operator fits an edge-featured graph attention detector on its own judge-labeled episode graphs and shares only model updates. The method couples a proximal local objective for non-IID clients, domain-balanced aggregation, over-refusal-constrained threshold calibration, corroborated upstream scoring, and a guarded rewrite for blocked answers. Federation is not optional: off-the-shelf transfer collapses under distribution shift (AUROC 0.51 to 0.70 only after in-domain retraining), so a deployable guard must adapt on each site's private traces. On Agent-SafetyBench, R-Judge, and AgentDojo, federated FGLGuard exceeds the in-domain centralized ceiling on all three benchmarks without pooling any data---where unsupervised anomaly guards and local-only training fail. One guard federated across four different-domain operators comes within 0.03 AUROC of multi-domain centralization, while any single-domain guard collapses on the others. Live FGLGuard cuts AgentDojo's ground-truth attack-success rate by 43% at near-unguarded utility, zero API cost, and negligible capability loss.

摘要：拓撲引導的 LLM 基於多代理系統 (MAS) 的保護措施在代理間通訊圖上訓練 GNN，以定位風險代理並介入拓撲——但它們假設一個操作員可以匯集所有標記的痕跡。這一假設在不同組織間破裂：事件包含私密提示、工具輸出和專有工作流程，且沒有任何孤島能看到完整的攻擊分佈。我們將隱私保護的 MAS 保護視為圖聯邦學習，並實現 FGLGuard：每個操作員在其自己的法官標記事件圖上擬合一個邊特徵圖注意力檢測器，並僅分享模型更新。該方法結合了針對非 IID 客戶的近端本地目標、領域平衡聚合、過拒絕約束的閾值校準、經過證實的上游評分，以及對被阻止答案的受保護重寫。聯邦不是可選的：現成的轉移在分佈轉移下崩潰（AUROC 僅在領域內重新訓練後從 0.51 提升到 0.70），因此可部署的保護措施必須根據每個站點的私有痕跡進行調整。在 Agent-SafetyBench、R-Judge 和 AgentDojo 上，聯邦 FGLGuard 在所有三個基準上超過了領域內集中上限，且未匯集任何數據——在無監督異常保護和僅本地訓練失敗的情況下。跨四個不同領域操作員的聯邦保護措施在多領域集中化方面僅差 0.03 AUROC，而任何單一領域的保護措施在其他領域上則崩潰。實時 FGLGuard 將 AgentDojo 的真實攻擊成功率降低了 43%，在接近無保護效用、零 API 成本和可忽略的能力損失下。

##### **Propose to Learn, Learn to Propose: Evaluability-Aware Assistance under Bounded Rationality**
2609.02242v1 by Yifan Zhu, Sammie Katt, Samuel Kaski

AI assistants often collaborate by proposing candidate edits, plans, or designs that users evaluate before adoption. Existing assistance methods focus on proposal quality or user-goal inference, often assuming that the user can reliably evaluate any proposal, which can fail in practice because of bounded rationality. We study evaluability-aware proposal planning, where proposals serve both as task interventions and as probes for learning latent preferences and evaluation constraints, where the resulting belief updates then guide later proposals. We formalise this setting as ProSE, a hidden-parameter sequential assistance problem, and instantiate it with a KL-regularised bounded-rational binary response model in which acceptance trades off value gain against a distance-dependent evaluability penalty. Analysing the planning consequence of this likelihood reveals that likely accepted proposals and informative probes need not coincide, which explains why planners that only pursue acceptance systematically underperform. We operationalise ProSE with \textsc{ProSE-Plan}, a depth-2 Bayes-adaptive planner that scores proposals by possible responses and response-induced posterior beliefs. In controlled graph simulations, \textsc{ProSE-Plan} improves over evaluability-unaware and myopic baselines when evaluation cost is the bottleneck, and a probe-commit ablation confirms that our approach selects informative proposals that simpler methods miss. Our results thus identify user evaluability as a planning-relevant dimension of AI assistance, complementary to generation quality and preference inference.

摘要：AI 助手經常通過提出候選編輯、計劃或設計來進行協作，這些提案需要用戶在採納之前進行評估。現有的輔助方法專注於提案質量或用戶目標推斷，通常假設用戶能夠可靠地評估任何提案，但在實踐中，由於有限理性，這種假設可能會失效。我們研究了可評估性意識的提案規劃，其中提案既作為任務干預，又作為學習潛在偏好和評估約束的探針，從而使得結果的信念更新能夠指導後續的提案。我們將這一設定形式化為 ProSE，一個隱藏參數的序列輔助問題，並用 KL 正則化的有限理性二元反應模型來具體化，其中接受與價值增益之間的權衡取決於距離的可評估性懲罰。分析這一似然的規劃後果顯示，可能被接受的提案和信息豐富的探針不必重合，這解釋了為什麼僅追求接受的規劃者系統性表現不佳。我們用 \textsc{ProSE-Plan} 操作化 ProSE，這是一個深度為 2 的貝葉斯自適應規劃器，通過可能的反應和反應引起的後驗信念來評分提案。在受控的圖形模擬中，\textsc{ProSE-Plan} 在評估成本成為瓶頸時，相比於不考慮可評估性和短視基準有了改進，而探針-承諾的消融實驗證實我們的方法選擇了簡單方法所忽略的信息豐富的提案。因此，我們的結果確定了用戶可評估性作為 AI 輔助的規劃相關維度，這與生成質量和偏好推斷互為補充。

##### **PhoenixNest-Video: Evidence-Grounded Multimodal Agent Framework for Automated Video Interview Assessment**
2609.02231v1 by Fan Yuxuan, Huang Miaojun, Zhang Haimei, Wu Jingshen, Liu Hao

Interview assessment requires per-criterion judgments grounded in behavioral evidence, yet surging applicant volumes have made human-only evaluation costly and inconsistent, while existing AI approaches yield opaque scores without traceable rationale. We introduce PhoenixNest-Video, an evidence-grounded multimodal agent framework for automated video interview assessment. It builds a semantic video graph as structured working memory, performs rubric-conditioned retrieval with cross-modal verification across visual, audio, and textual streams, and produces per-criterion scores anchored to the candidate's materials. A Scorer trained via Rubrics-based Reinforcement Learning with dual rewards for rubric alignment and score-level differentiation internalizes the discriminative structure of multi-level rubrics. PhoenixNest-Video attains 91.50\% grade-level accuracy on VInterview-2025, outperforming substantially larger proprietary models. A compact, rubric-grounded agent therefore scores candidates in closer agreement with an expert panel than direct prompting of much larger models, and exposes the evidence behind each score for human review.

摘要：面試評估需要基於行為證據的逐項判斷，但不斷增加的申請者數量使得僅依賴人力的評估變得昂貴且不一致，而現有的AI方法則產生不透明的分數，缺乏可追溯的理由。我們介紹了PhoenixNest-Video，一種基於證據的多模態代理框架，用於自動化視頻面試評估。它構建了一個語義視頻圖作為結構化工作記憶，進行基於標準的檢索，並在視覺、音頻和文本流之間進行跨模態驗證，產生與候選人材料相連的逐項分數。通過基於標準的強化學習訓練的評分器，對於標準一致性和分數級別區分的雙重獎勵內化了多級標準的區分結構。PhoenixNest-Video在VInterview-2025上達到了91.50\%的年級準確率，顯著超越了規模更大的專有模型。因此，一個緊湊的基於標準的代理在評分候選人時與專家小組的意見更為一致，而不是直接提示規模更大的模型，並揭示了每個分數背後的證據以供人類審查。

##### **PEARL: Path-Entity Aligned Relational Learning with Contextual Subgraphs for Inductive Knowledge Graph Completion**
2609.02216v1 by Yunchi Yang, Longlong Li, Cunquan Qu

Inductive knowledge graph completion (IKGC) aims to predict missing links involving entities unseen during training, requiring models to learn transferable relational and structural patterns. Existing subgraph- and path-based approaches often encode relational paths independently of their surrounding query subgraphs, although the predictive relevance of a path may vary across structural contexts. We propose PEARL, a Path-Entity Aligned Relational Learning framework that models paths as context-conditioned reasoning signals. PEARL constructs a query-specific contextual subgraph from the union of the query entities' neighborhoods and uses a large language model (LLM)-guided retriever to distill semantically relevant paths. It then builds a bipartite interaction graph over paths, contextual entities, and a global subgraph representation, allowing path embeddings to adapt to local and global structural evidence. To suppress noise introduced by the enlarged context, PEARL employs a dual-view contrastive objective that promotes representation consistency under stochastic contextual perturbations. Experiments on WN18RR, FB15k-237, and NELL-995 show that PEARL obtains the best average Hits@10 among the compared IKGC methods on all three benchmarks. Ablation studies, efficiency analyses, and case studies further validate the contributions of contextual subgraph modeling, semantic path retrieval, path-entity interaction, and contrastive regularization.

摘要：歸納知識圖譜補全（IKGC）旨在預測在訓練期間未見過的實體之間的缺失連結，這要求模型學習可轉移的關係和結構模式。現有的子圖和基於路徑的方法通常獨立於其周圍的查詢子圖編碼關係路徑，儘管路徑的預測相關性可能在不同的結構上下文中有所不同。我們提出了PEARL，一個路徑-實體對齊的關係學習框架，將路徑建模為上下文條件的推理信號。PEARL從查詢實體的鄰域聯合中構建一個查詢特定的上下文子圖，並使用大型語言模型（LLM）指導的檢索器來提煉語義相關的路徑。然後，它在路徑、上下文實體和全局子圖表示之間構建一個二分互動圖，允許路徑嵌入適應於局部和全局結構證據。為了抑制擴大上下文所引入的噪聲，PEARL採用了一種雙視圖對比目標，促進在隨機上下文擾動下的表示一致性。在WN18RR、FB15k-237和NELL-995上的實驗顯示，PEARL在所有三個基準中獲得了比較的IKGC方法中最佳的平均Hits@10。消融研究、效率分析和案例研究進一步驗證了上下文子圖建模、語義路徑檢索、路徑-實體互動和對比正則化的貢獻。

##### **FUSE: An Evaluating Framework for Dangerous Capabilities of LLMs**
2609.02168v1 by Zhengyi Jin, Ru Zhang, Xiao Chen, Xinbo Liu, Jiaxuan Lin, Jia Huang, Jianyi Liu, Zhen Yang

Fragmented safety evaluation undermines the governance of dangerous AI capabilities. We present a modular framework that evaluates each model through three orthogonal pipelines---Knowledge ($K$), Defense ($D$), and Harm ($H$)---under a unified protocol, aggregating results into a standardized dangerous-capability profile $φ$. Pluggable modules supply scenario seeds, knowledge banks, hazard queries, and judge rubrics, while the core evaluation engine remains unchanged across domains; the CB evaluation is complemented by a cyber pilot demonstrating protocol transfer.
  Instantiating the framework with a chemical-biological (CB) module, we evaluate 12 commercial LLMs from four families. Our first contribution is a horizontal comparison of dangerous capability across models and model families: the three dimensions expose sharply divergent profiles---models with comparable knowledge differ in refusal resilience, and strong defenders do not generate less harmful content when they do comply---while family-level patterns further separate Claude, DeepSeek, and GPT models. The second is a temporal analysis of capability evolution: tracking $K$, $D$, and $H$ against model release dates reveals that dangerous capability has not monotonically declined; newer models deepen knowledge while only partially improving defense, showing that scaling and alignment progress do not uniformly translate into safety. Reliability is established via cross-judge consistency (bootstrap $ρ> 0.79$, 4 of 5 judges) and pipeline orthogonality ($K$--$D$--$H$ inter-correlations $ρ\in [0.32, 0.52]$).

摘要：碎片化的安全評估削弱了對危險 AI 能力的治理。我們提出了一個模組化框架，通過三個正交管道---知識 ($K$)、防禦 ($D$) 和危害 ($H$)---在統一的協議下評估每個模型，將結果聚合成標準化的危險能力檔案 $φ$。可插拔模組提供場景種子、知識庫、危害查詢和評審標準，而核心評估引擎在各個領域保持不變；CB 評估則由一個展示協議轉移的網絡駕駛員補充。<lb/>通過化學-生物 (CB) 模組實現框架，我們評估了來自四個家族的 12 個商業 LLM。我們的第一個貢獻是對模型和模型家族之間危險能力的橫向比較：這三個維度揭示了截然不同的檔案---具有可比知識的模型在拒絕韌性上有所不同，而強大的防禦者在遵從時並不會產生較少的有害內容---同時家族層級的模式進一步區分了 Claude、DeepSeek 和 GPT 模型。第二個貢獻是對能力演變的時間分析：追蹤 $K$、$D$ 和 $H$ 與模型發布日期的關係顯示，危險能力並未單調下降；較新的模型加深了知識，但防禦的改善僅部分實現，顯示出擴展和對齊的進展並不均勻地轉化為安全性。通過跨評審一致性（自助法 $ρ&gt; 0.79$，5 位評審中有 4 位）和管道正交性（$K$--$D$--$H$ 之間的相關性 $ρ\in [0.32, 0.52]$）來確立可靠性。

##### **OmegaUse-SOP: SOP Engineering for Professional Computer Use from Human Demonstrations**
2609.02149v2 by Yixiong Xiao, Lang An, Hucheng Yang, Pinxue Ma, Yongquan Chen, Jingjia Cao, Yusai Zhao, Ting Wang, Ting Liu, Siqi Bao, Jingbo Zhou, Hua Wu

Large language models (LLMs) are increasingly evolving from conversational assistants into agents capable of operating external digital environments. Graphical user interface (GUI) agents play an important role in this transition, as many real-world workflows remain accessible only through user-facing software interfaces. However, despite recent progress on general computer-use benchmarks, domain-specific professional standard operating procedures (SOPs) remain challenging for GUI agents because they often involve implicit domain knowledge, software-specific conventions, and task-level verification requirements. We introduce OmegaUse-SOP, a human-in-the-loop SOP Engineering system for transforming human demonstrations of professional computer use into reusable SOP skills for GUI agents. Analogous to prompt engineering, SOP Engineering iteratively refines demonstrations, execution rules, and domain knowledge to convert professional SOPs into reusable GUI-agent skills. OmegaUse-SOP consists of four modules: Observe, Reason, Configure, and Execute. Together, these modules record expert operations as multimodal GUI traces, abstract low-level events into semantic step-level instructions, incorporate domain rules and task-specific parameters, and execute the resulting skills in live GUI environments through step-wise grounding, action generation, and verification. To demonstrate its effectiveness, we collaborate with a power-sector client and test OmegaUse-SOP on photovoltaic simulation workflows in PVsyst 7.2. The results suggest that OmegaUse-SOP can improve GUI-agent reliability on professional SOP tasks, highlighting a practical path toward deploying GUI agents in domain-specific professional software environments.

摘要：大型語言模型（LLMs）正逐漸從對話助手演變為能夠操作外部數位環境的代理。圖形使用者介面（GUI）代理在這一過程中扮演著重要角色，因為許多現實世界的工作流程仍然只能通過面向使用者的軟體介面來訪問。然而，儘管在一般電腦使用基準上取得了近期進展，針對特定領域的專業標準作業程序（SOP）對於GUI代理來說仍然具有挑戰性，因為這些程序通常涉及隱含的領域知識、軟體特定的慣例以及任務級的驗證要求。我們介紹了OmegaUse-SOP，一個人機協作的SOP工程系統，用於將人類對專業電腦使用的示範轉換為可重用的GUI代理SOP技能。類似於提示工程，SOP工程迭代地精煉示範、執行規則和領域知識，以將專業SOP轉換為可重用的GUI代理技能。OmegaUse-SOP由四個模組組成：觀察、推理、配置和執行。這些模組共同記錄專家操作作為多模態GUI痕跡，將低級事件抽象為語義步驟級指令，納入領域規則和任務特定參數，並通過逐步基礎、行動生成和驗證在實時GUI環境中執行生成的技能。為了證明其有效性，我們與一家電力部門的客戶合作，並在PVsyst 7.2中測試OmegaUse-SOP的光伏模擬工作流程。結果表明，OmegaUse-SOP可以提高GUI代理在專業SOP任務上的可靠性，突顯了在特定領域專業軟體環境中部署GUI代理的實際途徑。

##### **A Power Law in Logarithm's Clothing: On the Scalability of Graph-Based Vector Search**
2609.02143v1 by Sajad Faghfoor Maghrebi, Navid Eslami, Niv Dayan

Most vector databases rely on graph-based indexes, notably HNSW and Vamana, for approximate nearest neighbor search. With embedding models widely adopted, the datasets these databases store grow rapidly. At a fixed accuracy, how does search cost scale with dataset size? The prevailing answer is poly-logarithmic growth. Yet the claim is proven only under special conditions and asserted without proof for the indexes used in practice. It is also largely untested: standard benchmarks measure cost at one dataset size, not across sizes. We put the claim to the test. The answer depends on the scale itself. While the dataset size $N$ is small relative to the data's intrinsic dimensionality, search cost grows as $N^c$ for a constant $0<c<1$. We call this scaling the Sublinear Power Law. Once $N$ is large enough, growth slows to subpolynomial, consistent with the poly-logarithmic claim. The Sublinear Power Law appears on every dataset, mostly up to its full size, at every recall target, query hardness level, and index configuration we test. The transition to subpolynomial growth appears on the two datasets that grow large enough relative to their intrinsic dimensionality. One mechanism underlies both behaviors: a dataset's intrinsic dimensionality grows with its size until the data resolves its underlying distribution. Higher intrinsic dimensionality packs more vectors into the query neighborhood the search must examine. We present a unifying theory of beam-search cost that explains our observations. For exact and bounded-degree constructions, we prove the Sublinear Power Law and the eventual transition to poly-logarithmic scaling, and derive the scale at which it occurs. We also develop models that predict the power-law exponents for any recall target and index configuration. These models give a principled way to navigate trade-offs among search cost, insertion cost, and recall as data grows.

摘要：大多數向量資料庫依賴於基於圖形的索引，特別是 HNSW 和 Vamana，用於近似最近鄰搜索。隨著嵌入模型的廣泛採用，這些資料庫儲存的數據集迅速增長。在固定的準確度下，搜索成本如何隨著數據集大小的增長而變化？普遍的答案是多對數增長。然而，這一說法僅在特殊條件下得到證明，且對實際使用的索引未提供證明。這一說法也在很大程度上未經測試：標準基準測試在一個數據集大小下測量成本，而不是跨越不同大小。我們對這一說法進行了測試。答案取決於規模本身。當數據集大小 $N$ 相對於數據的內在維度較小時，搜索成本隨著 $N^c$ 增長，其中常數滿足 $0&lt;c&lt;1$。我們稱這種增長為次線性冪律。一旦 $N$ 足夠大，增長速度會減緩至次多項式，這與多對數的說法一致。次線性冪律在每個數據集上都出現，主要是在其完整大小之前，並且在我們測試的每個召回目標、查詢難度級別和索引配置下均如此。轉變為次多項式增長出現在兩個相對於其內在維度增長足夠大的數據集上。這兩種行為背後有一個共同的機制：數據集的內在維度隨著其大小增長，直到數據解析其潛在分佈。更高的內在維度將更多的向量包裝進查詢鄰域中，搜索必須檢查這些向量。我們提出了一個統一的束搜索成本理論，解釋了我們的觀察結果。對於精確和有界度的構造，我們證明了次線性冪律及最終轉變為多對數增長，並推導出其發生的規模。我們還開發了模型，預測任何召回目標和索引配置的冪律指數。這些模型提供了一種原則性的方法，以在數據增長過程中導航搜索成本、插入成本和召回之間的權衡。

##### **EmoStance: Response-Side Affective-Orientation Control for Empathetic Response Generation via Emoji Weak Supervision**
2609.02133v1 by Ziyuan Jin, Yuxuan Ge, Zheng Tian

Empathetic response generation requires models to decide not only what to say, but also how to respond to the previous speaker's affective situation. We formulate this as response-side affective-orientation control and use multi-annotator emoji distributions as weak affective--attitudinal evidence, rather than as output symbols or gold labels, to induce a latent control space that operationally approximates listener stance. We construct EmojiDialogue, an utterance-level extension of EmpatheticDialogues with emoji votes and confidence scores, and propose EmoStance, which models source-side affective expression, predicts a soft response-side orientation from dialogue context and speaker roles, and steers a frozen instruction-tuned LLM through continuous prefix embeddings. In blind pairwise evaluation with 20 annotators and 800 judgments, EmoStance achieves a 62.2% decisive win rate, with the clearest gains in contextual specificity and perceived responsiveness, while remaining complementary to external-knowledge methods. Code, annotation metadata, and reconstruction scripts are available in our GitHub repository: https://github.com/18277390221/EmoStance.

摘要：同理心反應生成需要模型不僅決定要說什麼，還要如何回應前一位講者的情感狀況。我們將此表述為反應側情感導向控制，並使用多標註者的表情符號分佈作為弱情感--態度證據，而不是作為輸出符號或金標籤，以誘導一個潛在的控制空間，該空間在操作上近似於聽者的立場。我們構建了EmojiDialogue，這是一個包含表情符號投票和信心分數的同理心對話的發言級擴展，並提出EmoStance，該模型從源側情感表達中預測對話上下文和講者角色的軟反應側導向，並通過連續前綴嵌入引導一個凍結的指令調整大型語言模型。在與20位標註者和800次評判的盲對比評估中，EmoStance實現了62.2%的決定性勝率，在上下文特異性和感知響應性方面獲得了最明顯的提升，同時仍然與外部知識方法互補。代碼、標註元數據和重建腳本可在我們的GitHub倉庫中獲得：https://github.com/18277390221/EmoStance。

##### **C$^{3}$T: Counterfactual Causal Reasoning for Sentiment Shifts in Social-Media Conversation Trees**
2609.02131v1 by S M Rafiuddin, Atriya Sen

Sentiment in social-media threads does not only vary across posts; it shifts as users react to claims, corrections, evidence, and hostility within a branching reply tree. We study why sentiment changes in rumor-centric conversation trees by treating discourse moves (e.g., denial/correction, evidence/link, toxicity/attack) as candidate interventions and asking (i) what sentiment a reply expresses, (ii) whether the sentiment shifts relative to its parent, and (iii) which prior message most plausibly drove the reply's sentiment. To support this setting, we introduce CaSiRe, a causal sentiment reasoning layer over public rumor conversation datasets that adds post-level sentiment labels, induced parent-child shift labels, calibrated multi-label intervention tags, and explicitly annotated causal-source labels. We then propose C$^{3}$T (Counterfactual Causal Conversation Transformer), a thread-structured temporal model that jointly predicts node sentiment and shifts, learns sparse ancestor attribution, and supports counterfactual queries by forcing conversational intervention embeddings on or off to estimate potential outcomes. Under an event-level split, C$^{3}$T improves out-of-event robustness and attribution over text-only, graph-based, and temporal baselines, and yields interpretable model-based effects: denials/corrections and evidence reduce downstream negativity, while toxicity increases it. We also benchmark open-weight LLM prompting baselines and find that added conversational context helps, but attribution remains less reliable, motivating structure-aware counterfactual modeling for social-media analysis.

摘要：社交媒體線程中的情感不僅在帖子之間有所不同；它會隨著用戶對聲明、修正、證據和敵意的反應而變化，形成一個分支的回覆樹。我們研究為什麼在以謠言為中心的對話樹中情感會發生變化，將話語行為（例如，否認/修正、證據/鏈接、毒性/攻擊）視為候選介入，並詢問（i）回覆表達了什麼情感，（ii）情感是否相對於其父項發生變化，以及（iii）哪條先前的消息最有可能驅動回覆的情感。為了支持這一設置，我們引入了CaSiRe，一個在公共謠言對話數據集上的因果情感推理層，該層添加了帖子級情感標籤、誘導的父子變化標籤、經過校準的多標籤介入標籤，以及明確註釋的因果來源標籤。我們隨後提出了C$^{3}$T（反事實因果對話Transformer），這是一個線程結構的時間模型，能夠共同預測節點情感和變化，學習稀疏的祖先歸因，並通過強制對話介入嵌入的開啟或關閉來支持反事實查詢，以估計潛在結果。在事件級別的劃分下，C$^{3}$T在事件外的穩健性和歸因上優於僅基於文本、圖形和時間的基準，並產生可解釋的基於模型的效果：否認/修正和證據減少了下游的負面情感，而毒性則增加了負面情感。我們還基準測試了開放權重的LLM提示基準，發現增加的對話上下文有所幫助，但歸因仍然不太可靠，這促使我們對社交媒體分析進行結構感知的反事實建模。

##### **MASkills: Continual Skills Optimization for Multi-Agent LLM Systems**
2609.02094v1 by Huaiyuan Yao, Xiaoou Liu, Charles Fleming, Tianlong Chen, Hua Wei

LLM-based multi-agent systems have shown strong performance on complex tasks, yet continual improvement from interaction experience remains challenging. Existing self-reflection methods build experience memories, but memories are mostly hard to invoke, refine, or scale, while agent skills offer a more actionable unit: structured procedural knowledge that specifies when to act, how to act, and which resources or tools to use. We introduce MASkills, a continual learning framework that optimizes multi-agent LLM systems through agent skills. MASkills presents a new agent-optimization pipeline that integrates skill-conditioned credit assignment, hierarchical credit aggregation, and momentum-smoothed optimization, enabling agent skill libraries to evolve through refinement, induction, consolidation, and pruning. Experiments on HotpotQA, LoCoMo, and GAIA demonstrate the effectiveness of MASkills across multiple agentic tasks. Our code is available at https://github.com/DaRL-GenAI/MASkills

摘要：LLM 基礎的多智能體系統在複雜任務上表現出色，但從互動經驗中持續改進仍然具有挑戰性。現有的自我反思方法建立了經驗記憶，但這些記憶大多難以調用、精煉或擴展，而智能體技能則提供了一個更具可操作性的單位：結構化的程序知識，具體說明了何時行動、如何行動以及使用哪些資源或工具。我們介紹了 MASkills，一個通過智能體技能優化多智能體 LLM 系統的持續學習框架。MASkills 提出了一個新的智能體優化管道，整合了技能條件的信用分配、分層信用聚合和動量平滑優化，使智能體技能庫能夠通過精煉、歸納、整合和修剪進化。在 HotpotQA、LoCoMo 和 GAIA 上的實驗展示了 MASkills 在多個智能任務中的有效性。我們的代碼可在 https://github.com/DaRL-GenAI/MASkills 獲得。

##### **Selective Knowledge Edit Reversal via Gated Singular Vector Shrinkage**
2609.02091v1 by Weifeng Jiang, Ruirui Chen, Qianren Mao, Junnan Liu, Qili Zhang, Kwok-Yan Lam

Knowledge editing provides an efficient way to update factual knowledge in large language models. However, malicious edits may introduce safety risks, making it necessary to reverse undesirable editing effects. Existing reversal methods for parameter-modifying edits mainly focus on global removal, which may also erase beneficial edits that should be preserved. In this paper, we study selective reversal of edited knowledge, where the goal is to reverse targeted edited facts while preserving the remaining edited facts. Based on the hypothesis that each edit is sparsely encoded within the dominant subspace of the edited matrix, we propose a spectral-based reversal framework that locates edit-sensitive components within the dominant singular subspace of edited weights. Experiments across multiple settings demonstrate the effectiveness of our method in reversing selected edits while preserving unrelated edited facts. These results suggest that different edits are sparsely encoded within dominant singular components and can be separable when the number of edits is moderate, making selective spectral reversal a promising direction for locating edit-specific components and repairing edited language models.

摘要：知識編輯提供了一種有效的方法來更新大型語言模型中的事實知識。<lb/>然而，惡意編輯可能會引入安全風險，因此有必要逆轉不希望的編輯效果。<lb/>現有的參數修改編輯的逆轉方法主要集中在全局移除上，這可能也會抹去應該保留的有益編輯。<lb/>在本文中，我們研究了編輯知識的選擇性逆轉，目標是逆轉目標編輯的事實，同時保留其餘的編輯事實。<lb/>基於每次編輯在編輯矩陣的主導子空間中稀疏編碼的假設，我們提出了一個基於譜的逆轉框架，該框架定位編輯權重主導奇異子空間中的編輯敏感組件。<lb/>在多個設定下的實驗證明了我們的方法在逆轉選定編輯的同時保留無關編輯事實的有效性。<lb/>這些結果表明，不同的編輯在主導奇異組件中是稀疏編碼的，當編輯數量適中時可以分離，這使得選擇性譜逆轉成為定位編輯特定組件和修復編輯語言模型的有希望的方向。

##### **MineTRACE: An Evidence-Grounded Interactive Reasoning System for Mineral Prospectivity**
2609.02060v1 by Yiran Zhang, Jinwen Liu, Daniel Su, Yisu Chen, Qiang Sun, Chris Gonzalez, Eun-Jung Holden, Marco Fiorentini, Wei Liu, Yihao Ding

Mineral exploration requires integrating heterogeneous geochemical, geophysical, and geological evidence, yet existing prospectivity systems often provide only opaque scores or heatmaps. We present MineTRACE, a web-based system for evidence-grounded exploration of eight commodities: Cu, Au, Ni, W, Sn, Co, Ta, and Mn. Users can explore prospectivity maps, query locations or regions, inspect supporting evidence, and interact through natural language. A transparent expert tree, informed by geological knowledge and known deposits, combines multi-source evidence into interpretable prospectivity scores. For a new location, the conversational assistant retrieves the score and supporting evidence from the analysis pipeline and presents them in natural language. The scorer achieves spatial AUC values of up to 0.917 across different test scenarios, while end-to-end evaluation assesses query accuracy and response grounding. MineTRACE makes public geoscience data easier to access, interpret, and verify, supporting more efficient and transparent mineral exploration.

摘要：礦產勘探需要整合異質的地球化學、地球物理和地質證據，然而現有的潛力評估系統往往僅提供不透明的分數或熱圖。<lb/>我們提出了MineTRACE，一個基於網路的系統，用於八種商品的證據驅動勘探：Cu、Au、Ni、W、Sn、Co、Ta和Mn。<lb/>用戶可以探索潛力地圖、查詢位置或區域、檢查支持證據，並通過自然語言進行互動。<lb/>一個透明的專家樹，根據地質知識和已知礦床的信息，將多來源的證據結合成可解釋的潛力分數。<lb/>對於新位置，對話助手從分析管道中檢索分數和支持證據，並以自然語言呈現。<lb/>該評分器在不同測試場景中達到高達0.917的空間AUC值，而端到端評估則評估查詢準確性和回應的基礎。<lb/>MineTRACE使公共地球科學數據更易於訪問、解釋和驗證，支持更高效和透明的礦產勘探。

##### **HyGRAIL: Cost-Aware and Evidence-Grounded Scientific Hypothesis Discovery over Knowledge Graphs**
2609.02056v1 by Yihang Sun, Zhihan Zhu, Zhiyuan Jiang, Jingyi Ge, Zixuan Li, Jiaxuan You

Scientific knowledge graphs organize entities and relations extracted from scientific literature, but they remain inherently incomplete. Missing typed links in such graphs can therefore represent plausible scientific hypotheses, such as unexplored associations between materials and applications. However, scientific hypothesis discovery is challenging because true discoveries are extremely sparse among typed candidate pairs: graph neural networks (GNNs) are efficient but unreliable for ambiguous cases, while large language models (LLMs) are knowledgeable but too costly to apply exhaustively and are not naturally grounded in graph structures. We propose HyGRAIL, a cost-aware and evidence-grounded framework that combines heterogeneous GNN triage with LLM-based hypothesis review. HyGRAIL first uses a GNN to score candidate hypotheses and identify a validation-calibrated ambiguous region, routing only graph-uncertain cases to LLM review. For each routed hypothesis, HyGRAIL retrieves node-level associations and multi-hop relational paths from the knowledge graph (KG), then converts this structured evidence into natural language through template-based or LLM-based naturalization. An LLM review agent finally judges each hard hypothesis using the naturalized evidence and validation-selected decision criteria. On MatKG, HyGRAIL achieves the best F1 score of 0.429, improving over the strongest prior baseline by 0.242 F1 points and over the GNN-only baseline by 0.322. Meanwhile, GNN triage reduces the LLM call rate by 54.36% on average. Ablation studies further show that retrieved graph evidence is crucial for reliable hypothesis verification and that compact, two-sided evidence is more effective than simply increasing retrieval quantity.

摘要：科學知識圖譜組織從科學文獻中提取的實體和關係，但它們本質上仍然不完整。這些圖譜中缺失的類型鏈接因此可以代表合理的科學假設，例如材料與應用之間未探索的關聯。然而，科學假設的發現具有挑戰性，因為在類型候選對中，真正的發現極為稀疏：圖神經網絡（GNN）在模糊情況下效率高但不可靠，而大型語言模型（LLM）知識豐富但應用成本過高，且不自然地基於圖結構。我們提出了HyGRAIL，一個成本意識和證據基礎的框架，結合異質GNN篩選和基於LLM的假設審查。HyGRAIL首先使用GNN對候選假設進行評分，並識別經驗驗證調整的模糊區域，僅將圖不確定的案例路由到LLM審查。對於每個路由的假設，HyGRAIL從知識圖譜（KG）檢索節點級關聯和多跳關係路徑，然後通過基於模板或基於LLM的自然化將這些結構化證據轉換為自然語言。最後，LLM審查代理使用自然化的證據和經驗驗證選擇的決策標準來評判每個困難的假設。在MatKG上，HyGRAIL達到了最佳F1分數0.429，比最強的先前基線提高了0.242 F1點，並且比僅使用GNN的基線提高了0.322。同時，GNN篩選平均減少了54.36%的LLM調用率。消融研究進一步顯示，檢索的圖證據對於可靠的假設驗證至關重要，而緊湊的雙面證據比僅僅增加檢索數量更有效。

##### **Seed-Anchored Budget-Bounded Graph Rendering for Question Answering on Industry-Standard Power-Grid Information and Exchange Models**
2609.02011v1 by Jayakumar Manoharan, Yamini Sehgal

Large language model question answering over power-grid models must respect a fixed context budget. We introduce seed-anchored graph rendering, a deterministic method that prioritizes query-local graph evidence without adding method-specific tuned or learned parameters beyond the shared hop bound and context budget. The method provides a checkable condition under which predefined seed-local answer-bearing render units are preserved in a greedy bounded-context prefix. We evaluate the approach on Common Information Model (CIM) network models exchanged through the Common Grid Model Exchange Standard (CGMES). On two budget-binding CGMES encodings, naive descriptions-first rendering retains local evidence for every single-hop item but only 0.12 and 0.00 of multi-hop items, whereas seed-anchored rendering retains all such evidence. On a preregistered fresh 100-item bank from the SmallGrid topology family, accuracy rises from 0.450 to 0.970 under a fixed 8,000-character context budget. Under a common retrieval and rendering pipeline, the standards-native seed-anchored graph matches or exceeds extracted graph representations produced by LightRAG, Microsoft GraphRAG, and HippoRAG, while avoiding LLM graph-construction tokens. The results are specific to the evaluated CIM/CGMES models, reader, and context budget; they concern budget-bounded retrieval rather than general question answering.

摘要：大型語言模型在電力網模型上的問答必須遵循固定的上下文預算。<lb/>我們引入了種子錨定圖形渲染，這是一種確定性方法，優先考慮查詢局部圖形證據，而不添加超出共享跳數限制和上下文預算的特定於方法的調整或學習參數。<lb/>該方法提供了一個可檢查的條件，在該條件下，預定義的種子局部答案承載渲染單元在貪婪的有界上下文前綴中得以保留。<lb/>我們在通用信息模型（CIM）網絡模型上評估該方法，這些模型通過通用電網模型交換標準（CGMES）進行交換。<lb/>在兩個預算約束的CGMES編碼上，天真的描述優先渲染為每個單跳項保留局部證據，但僅為多跳項保留0.12和0.00，而種子錨定渲染則保留所有這些證據。<lb/>在來自SmallGrid拓撲系列的預註冊新100項目庫中，準確率在固定的8000字符上下文預算下從0.450上升到0.970。<lb/>在一個通用的檢索和渲染管道下，標準原生的種子錨定圖形匹配或超過了由LightRAG、Microsoft GraphRAG和HippoRAG生成的提取圖形表示，同時避免了LLM圖形構建標記。<lb/>這些結果特定於評估的CIM/CGMES模型、讀者和上下文預算；它們關注於預算約束的檢索，而不是一般的問答。

##### **LexIssue: Benchmarking Legal Issue Identification in Chinese Civil Litigation**
2609.02954v1 by Huiyuan Xie, Yuqin Huang, Zhicheng Hao, Yida Cai, Shaochun Wang, Zhenghao Liu, Yuxiao Ye

Identifying the issues disputed between litigating parties is a crucial component of real-world litigation. However, legal issues remain comparatively underexplored in legal AI research. In this work, we study the computational modelling of legal issue identification in litigation. We introduce a legally grounded hierarchical schema that represents legal issues through both free-form issue descriptions and structured legal categories, and formulate legal issue identification as two complementary tasks: legal issue generation and legal issue classification. Based on this formulation, we construct LexIssue, a benchmark containing 430 real-world Chinese civil litigation cases and 1,303 expert-annotated disputed legal issues. We further develop an issue-centric legal knowledge base spanning 27 causes of action and 441 candidate legal issue entries to support retrieval-augmented reasoning. Experimental results across a diverse set of models show that retrieval-augmented generation using the constructed legal issue knowledge base consistently improves performance in identifying disputed legal issues and their corresponding legal attributes.

摘要：識別訴訟當事人之間爭議的問題是現實世界訴訟中的一個關鍵組成部分。<lb/>然而，法律問題在法律 AI 研究中仍然相對未被充分探討。<lb/>在這項工作中，我們研究法律問題識別的計算建模。<lb/>我們引入了一個法律基礎的層次結構，通過自由形式的問題描述和結構化的法律類別來表示法律問題，並將法律問題識別公式化為兩個互補的任務：法律問題生成和法律問題分類。<lb/>基於這一公式，我們構建了 LexIssue，一個包含 430 個現實世界的中國民事訴訟案例和 1,303 個專家註釋的爭議法律問題的基準。<lb/>我們進一步開發了一個以問題為中心的法律知識庫，涵蓋 27 種訴因和 441 個候選法律問題條目，以支持檢索增強推理。<lb/>在多種模型的實驗結果表明，使用構建的法律問題知識庫進行檢索增強生成在識別爭議法律問題及其相應法律屬性方面持續提高了性能。

##### **Convergence Theory of Knowledge Distillation in Asynchronous P2P Gossip Learning Network**
2609.01952v1 by Lucas Qingyang Fang, Tiyao Liu, Jinhao Jing, Zeji Li, Kaijie Chen, Harikrishna Kuttivelil, Katia Obraczka

Decentralized, serverless learning increasingly connects devices running different architectures, where the standard tool, decentralized SGD, is undefined as models with different parameter counts cannot be averaged. Knowledge distillation (KD) exchanges soft predictions rather than weights and sidesteps this obstacle, yet convergence theory for fully decentralized, asynchronous peer-to-peer (P2P) KD is lacking. We provide one, relocating consensus from parameter space to function (output) space: a KD event is a geometric contraction operator in logit space on the peers' predictive distributions, which we analyse in the Hilbert space of predictions on a reference measure. Under standard smoothness/variance assumptions and two realizability assumptions, one bridging parameter SGD to the functional step and one controlling restricted task/KD alignment, the time-averaged functional stationarity and function-space disagreement converge at rate $O(1/(ηT))$ to an $O(η)+O(B_f^2)+O(ζ_f^2)$ neighbourhood. Here $B_f$ is the distance from the task optimum to the peers' reachable classes and $ζ_f$ measures persistent local-task heterogeneity. Across homogeneous, width-heterogeneous, and mixed-family networks of the experiments, KD contracts function disagreement by $40-61\times$, while isolated training does not. The sampled stationarity diagnostic has late transient exponents $0.99-1.90$ on the shared-skeleton main runs, and the four-point step-size sweep exhibits the predicted transient: neighbourhood tradeoff.

摘要：去中心化的無伺服器學習越來越多地連接運行不同架構的設備，其中標準工具去中心化的隨機梯度下降（SGD）在不同參數數量的模型之間無法進行平均。知識蒸餾（KD）交換的是軟預測而非權重，並且避開了這一障礙，然而對於完全去中心化、非同步的點對點（P2P）KD的收斂理論仍然缺乏。我們提供了一個理論，將共識從參數空間轉移到函數（輸出）空間：KD事件在對等體的預測分佈的邏輯空間中是一個幾何收縮算子，我們在參考測度的預測希爾伯特空間中進行了分析。在標準的平滑性/方差假設和兩個可實現性假設下，一個是將參數SGD橋接到功能步驟，另一個是控制受限任務/KD對齊，時間平均的功能穩定性和函數空間的不一致性以速率$O(1/(ηT))$收斂到$O(η)+O(B_f^2)+O(ζ_f^2)$的鄰域。這裡$B_f$是從任務最優解到對等體可達類別的距離，而$ζ_f$則衡量持久的本地任務異質性。在實驗的同質、寬度異質和混合家族網絡中，KD將函數不一致性收縮了$40-61\times$，而孤立訓練則沒有。抽樣的穩定性診斷在共享骨架的主要運行中有晚期瞬態指數$0.99-1.90$，而四點步長掃描顯示了預測的瞬態：鄰域權衡。

##### **On-Policy Distillation Meets Off-Policy GRPO: Training Compact Instruction-Following Rerankers**
2609.01947v1 by Vignesh Prabhakar, Jialing Pan, Anil Babu Ankisettipalli

Compact instruction-following rerankers are attractive for deployment, but conventional distillation pipelines typically train students by offline imitation of teacher outputs on a fixed set of examples, constraining supervision to the teacher's observed ranking space. We revisit reranker distillation through the lens of reinforcement learning.
  We propose a two-stage framework combining off-policy teacher optimization with on-policy student distillation. In Stage 1, a 4B teacher reranker is strengthened with off-policy GRPO using LLM-judge feedback on 88K instruction-following examples. In Stage 2, a compact 1B student samples rankings from its own policy and receives soft teacher-derived rewards on those rankings, coupling student exploration with knowledge transfer.
  Our strongest gains appear under distribution shift. On MAIR-11, the original 11-subset, 869-query evaluation, the proposed student reaches 0.7670 nDCG@6, outperforming offline listwise KD by +4.6 points. Controlled comparisons against offline pairwise RankNet KD and on-policy GKD show that neither changing the offline distillation objective nor moving teacher-distribution matching on-policy reproduces the performance of reward-based on-policy distillation over student-sampled rankings. The advantage persists on MAIR-Full: across all 126 tasks and 9,356 queries, the proposed method obtains the highest task-macro point estimates among the evaluated distillation variants, reaching 0.6808 nDCG@6 and 0.7865 MRR@6. It also exceeds two released 7B RL-trained rerankers on the comparable MAIR-11 evaluation, while the same Stage 2 training procedure consistently improves three architecturally distinct alternative student backbones.
  On the 9,861-query validation benchmark, the resulting 1B reranker achieves 0.7624 nDCG@6 while providing a favorable quality-efficiency tradeoff relative to larger alternatives.

摘要：緊湊的指令遵循重排序器在部署上具有吸引力，但傳統的蒸餾流程通常通過對固定範例集的教師輸出進行離線模仿來訓練學生，這限制了監督在教師觀察到的排名空間內。我們從強化學習的角度重新審視重排序器蒸餾。<lb/>我們提出了一個兩階段框架，結合了離線教師優化和在線學生蒸餾。在第一階段，一個4B的教師重排序器通過使用LLM-judge對88K指令遵循範例的反饋，利用離線GRPO進行強化。在第二階段，一個緊湊的1B學生從自己的策略中抽樣排名，並在這些排名上獲得柔性教師衍生的獎勵，將學生探索與知識轉移結合起來。<lb/>我們的最強增益出現在分佈轉移下。在MAIR-11，原始的11子集，869查詢評估中，所提出的學生達到了0.7670 nDCG@6，超越了離線列表式KD 4.6點。對離線成對RankNet KD和在線GKD的控制比較顯示，無論是改變離線蒸餾目標還是將教師分佈匹配移至在線，都無法重現基於獎勵的在線蒸餾在學生抽樣排名上的性能。這一優勢在MAIR-Full上持續存在：在所有126個任務和9,356個查詢中，所提出的方法在評估的蒸餾變體中獲得了最高的任務宏觀點估計，達到0.6808 nDCG@6和0.7865 MRR@6。它還在可比的MAIR-11評估中超越了兩個釋出的7B RL訓練重排序器，而相同的第二階段訓練程序持續改善三個架構上不同的替代學生骨幹。<lb/>在9,861查詢的驗證基準上，最終的1B重排序器達到了0.7624 nDCG@6，同時相對於更大的替代方案提供了良好的質量效率權衡。

##### **SSAKG 2.0: An Open-Source Package for Structural Associative Sequence Memory and Context-Based Retrieval**
2609.01849v1 by Przemysław Stokłosa, Janusz A. Starzyk, Paweł Raif

This article presents SSAKG 2.0, an open-source software package for constructing and operating Structural Sequential Associative Knowledge Graphs (SSAKGs). An SSAKG represents objects as graph vertices and ordered sequences as structural patterns of graph connections. The resulting sparse graph is used as an associative memory in which complete sequences can be reconstructed from a partial, unordered context.
  Version 2.0 introduces new algorithms that exploit individual bits of computer memory to efficiently search graph connections. The package is implemented in Python, while performance-critical graph operations are implemented in C and exposed through a Python interface. This hybrid implementation provides a flexible high-level programming environment while reducing the memory and computational overhead associated with large sparse graphs.
  The algorithms were evaluated using randomly generated numerical sequences, sequences derived from sentences in the NLTK corpus, and mRNA sequences. The experiments demonstrate the ability of the package to store and reconstruct sequences from partial contexts and provide a basis for evaluating the effects of graph density, sequence length, and memory size on retrieval performance.
  SSAKG 2.0 is distributed under the Apache 2.0 open-source license. The package includes documentation and reproducible examples and is publicly available through GitHub and the Python Package Index (PyPI).

摘要：這篇文章介紹了 SSAKG 2.0，一個用於構建和操作結構序列關聯知識圖（SSAKGs）的開源軟體包。SSAKG 將物件表示為圖的頂點，將有序序列表示為圖連接的結構模式。生成的稀疏圖用作聯想記憶，其中可以從部分無序上下文重建完整序列。<lb/>  
版本 2.0 引入了新算法，利用計算機記憶體的單個位元高效搜索圖連接。該軟體包使用 Python 實現，而性能關鍵的圖操作則使用 C 實現並通過 Python 介面暴露。這種混合實現提供了一個靈活的高級編程環境，同時減少了與大型稀疏圖相關的記憶體和計算開銷。<lb/>  
這些算法使用隨機生成的數字序列、來自 NLTK 語料庫的句子衍生的序列和 mRNA 序列進行評估。實驗展示了該軟體包從部分上下文中存儲和重建序列的能力，並為評估圖密度、序列長度和記憶體大小對檢索性能的影響提供了基礎。<lb/>  
SSAKG 2.0 在 Apache 2.0 開源許可下發佈。該軟體包包括文檔和可重現的範例，並通過 GitHub 和 Python Package Index (PyPI) 公開提供。

##### **Import What You Need: Learning When and How to Augment EHR Graphs with External Knowledge**
2609.01839v1 by Chen Chen, Mohsen Nayebi Kerdabadi, Dongjie Wang, Mei Liu, Zijun Yao

Longitudinal prediction from electronic health records (EHRs) is limited by the sparsity and irregularity in patient trajectories, and knowledge augmentation with external knowledge graphs (KGs) offers a promising way to alleviate these issues. However, most existing methods perform fixed, context-agnostic topology augmentation by adding the same KG nodes and edges regardless of a patient's evolving state. We propose ReTA, a Reinforcement learning-based dynamic Topology Augmentation framework that casts KG import as a per-visit, budget-aware policy. ReTA first constructs an offline refined pool of KG-grounded templates, then learns a policy to select one augment action per visit from three options: Soft Import, which enriches node features without modifying graph topology, Hard Import, which grafts a compact KG subgraph onto the visit graph to create message-passing shortcuts, and Skip, which leaves the visit unaugmented when the base encoder is already confident. To stabilize learning, ReTA employs a decoupled encoder that processes semantic and structural signals in separate channels and fuses them via adaptive gating. Experiments on MIMIC-III and MIMIC-IV across diagnosis prediction, mortality, and readmission show that ReTA consistently outperforms strong baselines while remaining efficient, transfers across datasets and knowledge graphs, and yields interpretable augmentation patterns. The robust gains under sparse supervision highlight the advantage of ReTA's dynamic decision to import knowledge, boosting accuracy while curbing costs.

摘要：從電子健康記錄（EHRs）進行的縱向預測受到患者軌跡的稀疏性和不規則性的限制，而通過外部知識圖（KGs）進行知識增強提供了一種有前景的方法來緩解這些問題。<lb/>然而，大多數現有方法通過添加相同的KG節點和邊緣來執行固定的、與上下文無關的拓撲增強，而不考慮患者的演變狀態。<lb/>我們提出了ReTA，一種基於強化學習的動態拓撲增強框架，將KG導入視為每次訪問的、考慮預算的策略。<lb/>ReTA首先構建一個離線精煉的KG基礎模板池，然後學習一個策略，從三個選項中為每次訪問選擇一個增強行動：軟導入（Soft Import），在不修改圖拓撲的情況下豐富節點特徵；硬導入（Hard Import），將一個緊湊的KG子圖嫁接到訪問圖上以創建消息傳遞快捷方式；以及跳過（Skip），當基礎編碼器已經有信心時，將訪問保持不增強。<lb/>為了穩定學習，ReTA採用了一個解耦編碼器，分別處理語義和結構信號，並通過自適應閘融合它們。<lb/>在MIMIC-III和MIMIC-IV上進行的診斷預測、死亡率和再入院的實驗顯示，ReTA始終優於強基線，同時保持高效，能夠在數據集和知識圖之間轉移，並產生可解釋的增強模式。<lb/>在稀疏監督下的穩健增益突顯了ReTA動態決策導入知識的優勢，提升了準確性，同時降低了成本。

##### **Zeta-Lite: A Concurrent, Branchable In-Browser SQL Database for Agentic Memory**
2609.01818v1 by Gene Zhang

The browser has become a first-class database host: applications increasingly want to store, query, and reason over structured data entirely on the client - for privacy, offline operation, local-first collaboration, and, most recently, as durable memory for in-browser AI agents. One way to get SQL in the browser, compiling PostgreSQL to WebAssembly (PGlite), inherits PostgreSQL's process model: a single backend connection that executes one statement at a time and blocks. That model cannot express concurrent transactions, and it leaves richer capabilities - graph queries, database branching - to whatever the compiled server happens to include. We present zeta-lite, the browser form factor of the Zeta database engine: a WebAssembly build that compiles the same Zeta server down to a 2.87 MB gzipped artifact. Zeta-lite keeps the engine's log-centric asynchronous MVCC core, which yields two capabilities no other in-browser SQL engine provides. First, overlapping snapshot-isolated transactions on a single thread: multiple transactions hold distinct read/commit timestamps and interleave, with snapshot-isolation conflict detection between them. Second, copy-on-write database branching - whole-database fork, merge, and rebase - is unique in a browser SQL database and rare even in servers. On top of these, zeta-lite exposes a feature-complete PostgreSQL surface (joins, CTEs, window functions, JSONB with GIN indexes, full-text search, HNSW vector search, SQL/PGQ graph queries, multi-database) and snapshot-to-OPFS durability. Across Chrome, Firefox, and a native reference runtime, zeta-lite sustains 268k-315k point reads/s and holds a mixed read/write workload flat over millions of operations. This small, fully-featured, concurrent SQL database is an especially good fit for agentic memory - where cheap branchable state lets an agent explore, inspect, and commit or discard speculative work.

摘要：瀏覽器已成為一流的資料庫主機：應用程式越來越希望在客戶端完全存儲、查詢和推理結構化數據——出於隱私、離線操作、本地優先協作，以及最近作為瀏覽器內 AI 代理的持久記憶。將 PostgreSQL 編譯為 WebAssembly（PGlite）是一種在瀏覽器中獲得 SQL 的方法，它繼承了 PostgreSQL 的過程模型：一個單一的後端連接，每次執行一個語句並阻塞。該模型無法表達並發交易，並且將更豐富的功能——圖查詢、資料庫分支——留給編譯後的伺服器所包含的內容。我們介紹 zeta-lite，Zeta 資料庫引擎的瀏覽器形式：一個將相同 Zeta 伺服器編譯為 2.87 MB gzipped 檔案的 WebAssembly 構建。Zeta-lite 保留了引擎的日誌中心非同步 MVCC 核心，這提供了其他任何瀏覽器內 SQL 引擎所不具備的兩個功能。首先，在單一線程上重疊的快照隔離交易：多個交易持有不同的讀取/提交時間戳並交錯，並在它們之間進行快照隔離衝突檢測。其次，寫時複製資料庫分支——整個資料庫的分叉、合併和重基——在瀏覽器 SQL 資料庫中是獨特的，即使在伺服器中也很少見。除此之外，zeta-lite 還提供了一個功能完整的 PostgreSQL 接口（聯接、CTE、窗口函數、帶 GIN 索引的 JSONB、全文搜索、HNSW 向量搜索、SQL/PGQ 圖查詢、多資料庫）和快照到 OPFS 的持久性。在 Chrome、Firefox 和一個本地參考運行時中，zeta-lite 維持 268k-315k 點讀取/s，並在數百萬次操作中保持混合讀寫工作負載平穩。這個小型、功能齊全的並發 SQL 資料庫特別適合代理記憶——便宜的可分支狀態讓代理能夠探索、檢查並提交或放棄推測性工作。

##### **Induction and Inquiry via Probabilistic Reasoning over Language and Code**
2609.01815v1 by Wasu Top Piriyakulkij, Sam Acquaviva, Cassidy Langenfeld, Joshua Tenenbaum, Kevin Ellis

How humans grow and maintain abstract knowledge from the sparse, streaming noisy data of experience is a longstanding challenge in cognitive science. Any computational account must satisfy at least three desiderata: It must be (1) data-efficient and compute-efficient, (2) capture gradations of uncertainty to support intelligent inquiry and information gathering, and (3) be flexible enough to mentally represent the endless range of concepts people can learn and think about. Here we introduce a computational model that captures these three properties, by encoding symbolic knowledge as mental programs that combine natural language with source code, and sequentially inferring mental programs using LLM-guided Bayesian learning algorithms. Across a range of behavioral studies this model successfully reproduces quantitative signatures of human inductive learning and active inquiry, such as anchoring, garden-pathing, and other effects. In contrast, pure LLMs and classic Bayesian models either fail at the underlying task, or do not reproduce human behavior, or succeed only at exorbitant computational cost. These results suggest that one way humans continually grow their knowledge is by mentally representing many hypotheses spanning language-like and program-like representations, then revising those hypotheses to approximate Bayesian updates, while a bottom-up neural mechanism (an LLM) makes inference both tractable and learnable.

摘要：如何從稀疏、流動的噪音數據中成長和維持抽象知識，是認知科學中一個長期以來的挑戰。任何計算模型必須滿足至少三個需求：它必須是 (1) 數據高效且計算高效的，(2) 捕捉不確定性的漸變，以支持智能探究和信息收集，以及 (3) 足夠靈活，以心理上表示人們可以學習和思考的無限範圍的概念。在此，我們介紹一個計算模型，它通過將符號知識編碼為結合自然語言和源代碼的心理程序，並使用 LLM 引導的貝葉斯學習算法順序推斷心理程序，來捕捉這三個特性。在一系列行為研究中，該模型成功再現了人類歸納學習和主動探究的定量特徵，如錨定效應、花園小徑效應和其他效應。相比之下，純粹的 LLM 和經典的貝葉斯模型要麼在基本任務上失敗，要麼無法再現人類行為，或者僅在極高的計算成本下成功。這些結果表明，人類不斷增長知識的一種方式是通過心理上表示許多跨越類似語言和程序的假設，然後修訂這些假設以近似貝葉斯更新，而一種自下而上的神經機制（LLM）使推斷既可行又可學習。

##### **TalkFa: A Unified Benchmark for Farsi Dialogue Generation and Understanding**
2609.01810v1 by Neda Jamshidi, Kamyar Zeinalipour, Fahimeh Akbari, Monica Bianchini, Marco Maggini, Marco Gori

Farsi, spoken by more than 120 million people, lacks a comprehensive benchmark for dialogue generation and understanding. We introduce TALKFA, a unified benchmark comprising three complementary datasets: (1) WIKI-FADIAL, 4.2K Wikipedia-grounded dialogues for knowledge-grounded generation; (2) DAILYDIALOG-FA, 6.6K dialogues annotated for dialogue acts and emotions; and (3) PLAYDIAL-FA, 2.1K theatrical dialogues with sentiment labels. While LLMs assist data construction, every dialogue undergoes multi-stage review and revision by native Farsi speakers, and only the final human-approved dialogues are released. Experiments with six LLAMA and MISTRAL models show that LoRA substantially improves dialogue generation while requiring only 25-50% of the training data to recover over 90% of the final performance gains. Across classification tasks, FABERT achieves the best dialogue-act performance, LORA-MISTRAL-7B performs best on emotion recognition, and MISTRAL-24B achieves the highest sentiment score. Human evaluation and independent external validation demonstrate the reliability of the benchmark, while comparisons with GPT-4.1 as an LLM judge reveal that automatic metrics substantially overestimate dialogue quality. Zero-shot evaluation with frontier LLMs further shows that TalkFa remains a challenging benchmark. We will release all datasets, annotation guidelines, code, and checkpoints.

摘要：波斯語有超過1.2億的使用者，但缺乏一個全面的對話生成和理解基準。我們介紹了TALKFA，一個統一的基準，包含三個互補的數據集：(1) WIKI-FADIAL，4.2K基於維基百科的對話，用於知識驅動的生成；(2) DAILYDIALOG-FA，6.6K標註了對話行為和情感的對話；以及(3) PLAYDIAL-FA，2.1K帶有情感標籤的戲劇對話。雖然大型語言模型協助數據構建，但每個對話都經過母語波斯語使用者的多階段審查和修訂，只有最終經過人類批准的對話才會被發布。對六個LLAMA和MISTRAL模型的實驗顯示，LoRA顯著改善了對話生成，同時僅需25-50%的訓練數據即可恢復超過90%的最終性能增益。在分類任務中，FABERT在對話行為表現上達到最佳，LORA-MISTRAL-7B在情感識別上表現最佳，而MISTRAL-24B則達到最高的情感得分。人類評估和獨立的外部驗證證明了該基準的可靠性，而與GPT-4.1作為大型語言模型的比較顯示，自動指標大幅高估了對話質量。與前沿大型語言模型的零樣本評估進一步顯示，TalkFa仍然是一個具有挑戰性的基準。我們將發布所有數據集、標註指南、代碼和檢查點。

##### **MemeCULT-1K: Benchmarking South Asian Cultural Context and Humor Understanding of Multimodal Models**
2609.01772v1 by Tawsif Tashwar Dipto, Mehedi Ahamed, Radib Bin Kabir, Mueeze Al Mushabbir, Mohammed Saidul Islam, Mir Rayat Imtiaz Hossain, Md Tahmid Rahman Laskar, Sabbir Ahmed

Meme understanding goes beyond recognizing visual content or literal text; it requires implicit cultural knowledge and pragmatic inference that most vision-language models still lack. We introduce MemeCULT-1K, a multilingual benchmark of 1,000 South Asian memes in Bengali, English, and Hindi, where each meme is paired with a cultural context note and three human-written explanations, along with a supplementary set of 54 Bengali regional dialect memes. We evaluate thirteen popular Vision Language Models (VLMs) under two settings: meme-only and context-aware. Providing minimal cultural context yields consistent gains across all models and languages: mean SBERT similarity improves from 44.6 to 56.4 (+11.8), BLEURT from 37.3 to 42.3 (+5.0), and LLM-as-a-Judge scores from 2.57 to 3.43 out of 5 (+0.86). Fine-grained error analysis reveals that closed-source models fail mainly on entity and reference misidentification, while open-source models are bottlenecked by broader cultural knowledge gaps, with linguistic and phonological failures proving the most context-resistant across both. These results highlight the difficulty of culturally grounded meme understanding and motivate future work on explicit cultural knowledge integration. Our dataset and code are publicly available at TawsifDipto17/MemeCULT-1K.

摘要：Meme 理解超越了對視覺內容或字面文本的識別；它需要隱含的文化知識和實用推理，而大多數視覺語言模型仍然缺乏這些。我們介紹了 MemeCULT-1K，這是一個包含 1,000 個南亞迷因的多語言基準，涵蓋孟加拉語、英語和印地語，每個迷因都配有文化背景說明和三個人類撰寫的解釋，以及一組 54 個孟加拉地方方言迷因的補充集。我們在兩種設置下評估了十三種流行的視覺語言模型（VLMs）：僅迷因和上下文感知。提供最少的文化背景在所有模型和語言中都產生了一致的增益：平均 SBERT 相似度從 44.6 提高到 56.4 (+11.8)，BLEURT 從 37.3 提高到 42.3 (+5.0)，LLM-as-a-Judge 的得分從 2.57 提高到 3.43（滿分 5 分）(+0.86)。細緻的錯誤分析顯示，封閉源模型主要在實體和參考誤識別上失敗，而開放源模型則受到更廣泛文化知識缺口的瓶頸，語言和語音失誤在兩者中都證明是最具上下文抗性。這些結果突顯了基於文化的迷因理解的困難，並激勵未來在明確文化知識整合方面的工作。我們的數據集和代碼在 TawsifDipto17/MemeCULT-1K 上公開可用。

##### **A systematic Approach to constructing a Chance-and-Risk Matrix for Semiconductor Supply Chains**
2609.01563v1 by Ema Salkić, Alexander Fichtl, Philipp Ulrich, Hans Ehm, Marta Bonik, Georg Groh

Semiconductor supply chains face escalating risks from geopolitical tensions, geographic concentration, and rapid technological shifts, yet no scalable system continuously extracts, structures, and prioritizes risk intelligence from public corporate disclosures. We present an end-to-end pipeline that retrieves corporate documents for semiconductor companies and uses large language models (LLMs) to extract the risks and opportunities they describe. It organizes these into a knowledge graph linking each item to its category, sources, and related events, then merges duplicates and ranks them with a three-layer mechanism combining an algorithmic formula, an LLM relevance adjustment, and expert validation. Applied to five companies across the value chain, the pipeline produces 76,207 scored items, of which an independent check finds 92.6% valid. The automated rankings match expert judgment at an average Spearman correlation of 0.55 for risks and 0.72 for opportunities, and the resulting matrices identify trade restrictions as the dominant cross-company risk.

摘要：半導體供應鏈面臨來自地緣政治緊張、地理集中和快速技術變遷的風險不斷上升，但目前尚無可擴展的系統能持續從公開的企業披露中提取、結構化和優先排序風險情報。我們提出了一個端到端的管道，該管道檢索半導體公司的企業文件，並使用大型語言模型（LLMs）來提取它們所描述的風險和機會。它將這些信息組織成一個知識圖譜，將每個項目鏈接到其類別、來源和相關事件，然後合併重複項並通過一個三層機制進行排名，該機制結合了算法公式、LLM相關性調整和專家驗證。應用於價值鏈上的五家公司，該管道產生了76,207個得分項目，其中獨立檢查發現92.6%是有效的。自動排名與專家判斷的平均斯皮爾曼相關係數為0.55（風險）和0.72（機會），而生成的矩陣則將貿易限制識別為主要的跨公司風險。

##### **Knowledge Distillation During Mid-Training Favors Reasoning over Factual Recall**
2609.01532v1 by Jacqueline He, Howard Yen, Shuyue Stella Li, Margaret Li, Hanqing Zeng, Yinglong Xia, Benyu Zhang, Zhuokai Zhao, Qiang Zhang, Pang Wei Koh, Luke Zettlemoyer, Wen-tau Yih

Logit-based knowledge distillation (KD) is used to train smaller language models (LMs) via supervision from stronger teachers, but whether its benefits are consistent across training stages remains unclear. Through controlled experiments, we find that forward Kullback-Leibler (KL) distillation--the standard KD formulation--with post-trained teachers behaves fundamentally differently during mid-training, an intermediate phase of self-supervised learning on curated corpora. Surprisingly, while forward KD simultaneously improves reasoning and factual recall during pre-training relative to standard next-token prediction (NTP), it instead slows factual recall acquisition during mid-training despite continued reasoning gains. We trace this stage dependence to an asymmetry in teacher confidence across data domains and the student's evolving knowledge state: teachers are more confident on procedural than knowledge-intensive data, while students acquire low-entropy factual knowledge earlier in training. To mitigate this imbalance, we propose Switch Distillation, a simple mid-training objective that distills on tokens where the teacher is confident, using teacher predictive entropy as a lightweight routing signal, and otherwise falls back to cross-entropy. Switch Distillation consistently outperforms existing distillation objectives across teacher sizes. Relative to standard NTP, it achieves 1.61-1.71x the reasoning performance and 1.13-1.19x the knowledge and commonsense performance while preserving 96.7-96.8% of factual recall. Crucially, these benefits persist after post-training: Switch Distillation closes the factual recall gap while maintaining 1.25-1.32x and 1.13-1.20x gains in reasoning and knowledge and commonsense, respectively.

摘要：基於Logit的知識蒸餾（KD）用於通過強大的教師對較小語言模型（LMs）進行訓練，但其在不同訓練階段的好處是否一致仍不清楚。通過控制實驗，我們發現前向Kullback-Leibler（KL）蒸餾——標準的KD公式——在中期訓練期間的表現與自我監督學習在策劃語料庫上的中間階段根本不同。令人驚訝的是，儘管前向KD在預訓練期間相對於標準的下一個標記預測（NTP）同時改善了推理和事實回憶，但在中期訓練期間卻減緩了事實回憶的獲取，儘管推理的增益仍在持續。我們將這一階段依賴性追溯到教師在數據領域上的信心不對稱以及學生不斷演變的知識狀態：教師在程序性數據上比在知識密集型數據上更有信心，而學生在訓練早期獲得低熵的事實知識。為了減輕這一不平衡，我們提出了Switch Distillation，一個簡單的中期訓練目標，對教師有信心的標記進行蒸餾，使用教師預測熵作為輕量級路由信號，否則回退到交叉熵。Switch Distillation在各種教師規模上始終優於現有的蒸餾目標。相對於標準NTP，它實現了1.61-1.71倍的推理性能和1.13-1.19倍的知識和常識性能，同時保持96.7-96.8%的事實回憶。關鍵是，這些好處在後訓練後仍然存在：Switch Distillation縮小了事實回憶的差距，同時在推理和知識及常識方面分別保持了1.25-1.32倍和1.13-1.20倍的增益。

##### **Relational-Core Graph Analytics Querying graphs at SQL scale, and why the node/edge model is a performance tax, not a truer picture of connected data**
2609.01525v1 by Gene Zhang

A durable assumption holds that graph analytics requires a purpose-built graph engine, and that relational systems are ill-suited to connected data. We argue the opposite for the workloads enterprises actually run. A columnar relational engine fronted by a graph query language matches or exceeds native graph engines on analytical graph queries, and - decisively - scales past the point where in-memory graph engines fail. We further argue that the node/edge property graph is not a more faithful model of connected data but a re-encoding of relationships that already exist explicitly in relational tables; reconstructing them at query time is pure overhead. We present ClickGraph and its Databricks-dialect sibling DeltaGraph, systems that translate Cypher directly onto the native relational schema - the tables, columns, and foreign keys as they already exist - and execute in place on ClickHouse, Databricks, or in-process on lakehouse files, with no import and no separate cluster. Because the output is ordinary SQL, an underperforming query is an open optimization surface: it can be rewritten, and the engine itself extended. We support the argument with a peer system's own published benchmark, in which a columnar engine outruns Neo4j by two-to-four orders of magnitude, and with reproducible measurements across the LDBC Social Network Benchmark suite.

摘要：一個持久的假設認為圖形分析需要一個專門構建的圖形引擎，而關聯系統則不適合處理連接數據。我們對企業實際運行的工作負載提出相反的觀點。一個由圖形查詢語言支持的列式關聯引擎在分析圖形查詢方面可以匹配或超越原生圖形引擎，並且——決定性地——在內存圖形引擎失效的點上擴展超過該點。我們進一步主張，節點/邊屬性圖並不是連接數據的更真實模型，而是對已在關聯表中明確存在的關係的重新編碼；在查詢時重建它們純粹是額外的開銷。我們介紹了 ClickGraph 及其 Databricks 方言的兄弟 DeltaGraph，這些系統將 Cypher 直接轉換為原生關聯模式——即已存在的表、列和外鍵——並在 ClickHouse、Databricks 或湖屋文件中進行原地執行，無需導入和無需單獨的集群。因為輸出是普通 SQL，所以表現不佳的查詢是一個開放的優化面：它可以被重寫，並且引擎本身可以擴展。我們用一個同行系統自己發表的基準來支持這一論點，其中一個列式引擎的性能超越 Neo4j 兩到四個數量級，並且在 LDBC 社交網絡基準套件中有可重複的測量結果。

##### **Optimizing Byzantine Node Placement in Decentralized Federated Learning**
2609.01495v1 by Edoardo Gabrielli, Gabriele Tolomei

Security evaluations of decentralized federated learning (DFL) typically focus on how Byzantine participants behave, while largely overlooking which participants are compromised. Yet, because aggregation is distributed over a communication graph, the placement of Byzantine nodes determines how malicious influence propagates through the network. We therefore treat Byzantine placement as an explicit adversarial decision and formulate the attacker's objective as selecting, under a fixed compromise budget, the set of participants that maximizes its finite-time impact on honest nodes. To approximate this objective without executing the learning process for every candidate placement, we introduce Byzantine Placement Influence (BPI), a set-level measure derived from the actual gossip dynamics that quantifies the cumulative exposure of honest nodes to Byzantine sources over the training horizon. Unlike placement criteria based on node centrality heuristics, BPI directly accounts for weighted multi-hop propagation and interactions among compromised nodes. We develop efficient algorithms for optimizing BPI and evaluate them across six heterogeneous graph families, untargeted model poisoning, and backdoor attacks. BPI-guided placements consistently identify highly damaging configurations across different network structures and remain effective when the linear gossip assumption is relaxed through Byzantine-robust aggregation. Our results show that Byzantine placement is a critical but under-modeled dimension of DFL threat models and robustness evaluations.

摘要：去中心化聯邦學習（DFL）的安全評估通常專注於拜占庭參與者的行為，而在很大程度上忽視了哪些參與者受到損害。然而，由於聚合是在通信圖上分佈進行的，拜占庭節點的位置決定了惡意影響如何在網絡中傳播。因此，我們將拜占庭位置視為一種明確的對抗性決策，並將攻擊者的目標公式化為在固定的妥協預算下，選擇一組參與者，以最大化其對誠實節點的有限時間影響。為了在不對每個候選位置執行學習過程的情況下近似這一目標，我們引入了拜占庭位置影響（BPI），這是一種基於實際八卦動態的集合級度量，量化了在訓練期間誠實節點對拜占庭來源的累積暴露。與基於節點中心性啟發式的放置標準不同，BPI 直接考慮了加權的多跳傳播和受損節點之間的互動。我們開發了優化 BPI 的高效算法，並在六個異構圖族、無目標模型中毒和後門攻擊中進行評估。BPI 指導的放置在不同網絡結構中始終能識別出高度損害的配置，並在通過拜占庭穩健聚合放鬆線性八卦假設時仍然有效。我們的結果顯示，拜占庭位置是 DFL 威脅模型和穩健性評估中的一個關鍵但未充分建模的維度。

##### **Neuro-Symbolic Geometric Abstraction (NeuSOGA): From Observations to Symbolic Mathematical Representations**
2609.01408v2 by Qingde Li, Qingqi Hong, Zihan Li, Jie Tian

A fundamental challenge in artificial intelligence is the transformation of observations into explicit symbolic representations suitable for abstraction, interpretation, and reasoning. While modern AI systems achieve remarkable perceptual capabilities through large-scale statistical learning, the resulting knowledge is typically encoded within latent parameters that are difficult to inspect or manipulate analytically. Inspired by Neuro-Symbolic AI and theories of human abstraction, this paper investigates the formation of symbolic mathematical representations from geometric observations.
  We propose NeuSOGA (Neuro-Symbolic Geometric Abstraction), a framework that progressively transforms observations into topological abstractions, geometric abstractions, and ultimately symbolic mathematical representations. The architecture combines topology-guided structural discovery using Euclidean Distance Transforms, foundation-model perception using Segment Anything, adaptive multi-scale geometric abstraction, and symbolic synthesis through Implicit Area Splines.
  The resulting representation is an analytical implicit model supporting arbitrary-order smoothness, additive composition, and closed-form evaluation. Unlike neural latent encodings, the generated representation remains interpretable, editable, and mathematically explicit. Experiments on ModelNet40 point clouds, arbitrary-view projections, and segmented optical observations demonstrate that NeuSOGA transforms diverse observations into compact symbolic representations while preserving essential geometric and topological structure across sensing modalities and viewing directions.
  NeuSOGA provides an interpretable and explainable pathway from observation to symbol and establishes

摘要：一個人工智慧的基本挑戰是將觀察轉化為適合抽象、解釋和推理的明確符號表示。雖然現代 AI 系統通過大規模統計學習實現了顯著的感知能力，但所產生的知識通常編碼在難以檢查或分析操作的潛在參數中。受到神經符號 AI 和人類抽象理論的啟發，本文探討了從幾何觀察中形成符號數學表示的過程。<lb/>我們提出了 NeuSOGA（神經符號幾何抽象），這是一個逐步將觀察轉化為拓撲抽象、幾何抽象，最終形成符號數學表示的框架。該架構結合了使用歐幾里得距離變換的拓撲引導結構發現、使用 Segment Anything 的基礎模型感知、自適應多尺度幾何抽象以及通過隱式面積樣條的符號合成。<lb/>所產生的表示是一個支持任意階平滑性、加法組合和封閉形式評估的分析隱式模型。與神經潛在編碼不同，生成的表示保持可解釋性、可編輯性和數學明確性。在 ModelNet40 點雲、任意視角投影和分段光學觀察上的實驗表明，NeuSOGA 能夠將多樣的觀察轉化為緊湊的符號表示，同時保留各種感測模式和視角方向下的基本幾何和拓撲結構。<lb/>NeuSOGA 提供了一條從觀察到符號的可解釋和可解釋的途徑，並建立了

##### **Investigating Linear Probe Robustness to Linguistic Register, Medical Specialty, and Corpus Shifts in Medical QA**
2609.01361v1 by Nishant Mishra, Ameen Abu-Hanna, Iacer Calixto

Linear classifiers trained on hidden states of a large language model (LLM), linear probes, can flag factual errors from a single forward pass. Geometrically, that implies that true and false statements separate along a stable direction in hidden state space, i.e., the truth direction. Prior work disagrees on whether this generalises across input shifts, but the disagreement is hard to interpret because cross-dataset probe transfer experiments confound several kinds of input change at once. We isolate three such variables in medical question-answering (QA): writing style (register), domain (medical specialty), and corpus (dataset). We build a benchmark using 500 MedQA entries, each rewritten into four styles (textbook, patient, clinical note, colloquial), annotated with clinical specialty, and grouped with two other exam corpora, MedMCQA and MMLU-medical, for cross-dataset evaluation. Probing four open-weight LLMs (2--8B), we find that the truth direction is largely robust to writing style (mean $Δ_\text{register} \approx 0.10$ AUROC on held-out facts) and to medical specialty ($Δ_\text{specialty} \approx 0.03$), but degrades unevenly across corpora: by $0.12$ AUROC on MMLU-medical and by $0.21$ on MedMCQA, roughly twice the register gap. The register result replicates with a second generator and carries over to human-written patient questions. The truth direction is therefore largely stable within the medical domain but breaks under some corpus shifts, and question format does not explain the break, which suggests that the signal a linear probe recovers is partly bound to dataset structure rather than to medical knowledge alone.

摘要：線性分類器在大型語言模型（LLM）的隱藏狀態上訓練而成，稱為線性探針，可以在單次前向傳遞中標記事實錯誤。幾何上，這意味著真實和虛假的陳述在隱藏狀態空間中沿著一個穩定的方向分開，即真理方向。先前的研究對這是否可以在輸入變化中推廣存在分歧，但這種分歧難以解釋，因為跨數據集的探針轉移實驗同時混合了幾種輸入變化。我們在醫療問答（QA）中隔離了三個這樣的變量：寫作風格（註冊）、領域（醫療專業）和語料庫（數據集）。我們使用500個MedQA條目建立了一個基準，每個條目都被重寫為四種風格（教科書、病人、臨床筆記、口語），並標註了臨床專業，並與另外兩個考試語料庫MedMCQA和MMLU-medical進行分組，以便進行跨數據集評估。在探測四個開放權重的LLM（2-8B）時，我們發現真理方向在寫作風格上大致穩定（在保留的事實上平均$Δ_\text{register} \approx 0.10$ AUROC）和醫療專業上（$Δ_\text{specialty} \approx 0.03$），但在語料庫之間不均勻下降：在MMLU-medical上下降$0.12$ AUROC，在MedMCQA上下降$0.21$，大約是註冊差距的兩倍。註冊結果在第二個生成器中重複，並延續到人類撰寫的病人問題中。因此，真理方向在醫療領域內大致穩定，但在某些語料庫變化下會崩潰，而問題格式無法解釋這一崩潰，這表明線性探針恢復的信號在某種程度上與數據集結構相關，而不僅僅是醫療知識。

##### **EDGE: Error Dependency Graph-Guided Multi-Error Attribution in Multi-Agent LLM Systems**
2609.01360v1 by Jun Hou, Priya Pitre, Yi Fang, Xuan Wang

Large language model (LLM) agent failures often contain multiple related errors rather than a single mistake. Existing attribution methods usually identify a responsible agent, step, or root cause, but do not explicitly model dependency between errors. We introduce EDGE, an Error Dependency Graph-guided multi-Error attribution framework. EDGE constructs an error dependency graph from observed error events and validates a reliable causal subset through counterfactual rollout. The inference graph guides a two-stage LLM-as-judge detector for error attribution, and the intervention-validated subgraph provides a more reliable basis for explanation and repair analysis. Experiments on TRAIL and MAST show that EDGE improves category-level multi-error attribution across most evaluated models and settings. Experiments with adapted Who&When-style prompts show that the graph helps across prompting strategies. These results suggest that dependency structure is a useful diagnostic prior for agent failures beyond isolated root-cause prediction.

摘要：大型語言模型（LLM）代理的失敗通常包含多個相關錯誤，而不是單一的錯誤。現有的歸因方法通常識別出一個負責的代理、步驟或根本原因，但並未明確建模錯誤之間的依賴關係。我們介紹了EDGE，一種錯誤依賴圖引導的多錯誤歸因框架。EDGE從觀察到的錯誤事件構建錯誤依賴圖，並通過反事實展開驗證可靠的因果子集。推理圖引導一個兩階段的LLM作為判斷者的錯誤歸因檢測器，而干預驗證的子圖則為解釋和修復分析提供了更可靠的基礎。在TRAIL和MAST上的實驗顯示，EDGE在大多數評估模型和設置中改善了類別層級的多錯誤歸因。使用改編的Who&When風格提示的實驗顯示，該圖在提示策略中提供了幫助。這些結果表明，依賴結構是代理失敗的一個有用診斷先驗，超越了孤立的根本原因預測。

##### **SymFold: Synergizing Evolutionary and Structural Priors for Accurate Protein Inverse Folding**
2609.01353v1 by Handong Wang, Jiaxin Qi, Baisheng Lai, Jianqiang Huang

Protein inverse folding aims to recover amino acid sequences for a given 3D protein structure, underpinning broad applications such as enzyme engineering and drug discovery.Current methods often follow a serial pipeline, in which a structure encoder predicts a coarse sequence, which is then refined by protein language models (PLMs). However, because PLMs only perform post-hoc sequence edits, the refinement is bounded by the quality of upstream predictions.Thanks to recent multimodal protein language models (MPLMs), we could directly encode structure to generate sequences with pretrained structural knowledge, but we observe that they are not effective for inverse folding. Therefore, we introduce a symmetric dual-path architecture that both leverages PLMs for pretrained sequence evolution knowledge and MPLMs for pretrained structural knowledge to iteratively guide protein sequence generation.Through extensive experiments across standard protein inverse folding benchmarks, our method achieves state-of-the-art performance, surpassing prior approaches, and ablation studies validate the rationale of our symmetric design, revealing a promising direction for the community.

摘要：蛋白質反向摺疊旨在為給定的三維蛋白質結構恢復氨基酸序列，這為酶工程和藥物發現等廣泛應用奠定了基礎。當前的方法通常遵循串行流程，其中結構編碼器預測粗略序列，然後由蛋白質語言模型（PLMs）進行精煉。然而，由於PLMs僅執行事後序列編輯，因此精煉受到上游預測質量的限制。得益於最近的多模態蛋白質語言模型（MPLMs），我們可以直接編碼結構以生成具有預訓練結構知識的序列，但我們觀察到它們在反向摺疊方面並不有效。因此，我們引入了一種對稱雙通路架構，既利用PLMs的預訓練序列演化知識，又利用MPLMs的預訓練結構知識，以迭代方式指導蛋白質序列生成。通過在標準蛋白質反向摺疊基準上的廣泛實驗，我們的方法達到了最先進的性能，超越了先前的方法，消融研究驗證了我們對稱設計的合理性，揭示了對社群來說一個有前景的方向。

##### **CHARM: Character Hallucination for Multicultural Role Play Benchmark**
2609.01352v1 by Sunkyung Han, Nahyeon Park, Gaeun Seo, Seunghyun Yoon, JinYeong Bak

Role-playing large language models (LLMs) are expected to adopt a character's style while also respecting that character's knowledge boundaries. Prior evaluations detect character hallucination but rarely distinguish whether errors arise from failure to recognize a boundary or from failure to comply despite recognition. We introduce CHARM, a multicultural benchmark of 40 real and fictional characters drawn from five cultural-linguistic regions, and validated by native reviewers. It probes two boundary types, Temporal (historical vs. modern) and Cross-Universe (entities outside a character's narrative or historical universe), using abstention-enabled multiple-choice questions. We propose a two-stage evaluation that separates Boundary-Awareness (explicit recognition that a query is out of scope) from Boundary-Compliance (abstention when answering concrete questions). Evaluations across six LLMs show that hallucination is driven predominantly by compliance failures. Models frequently acknowledge that a query lies outside the character's knowledge yet still provide factual, out-of-character answers. By re-posing the same questions to the target character, we confirm that a large fraction of these cases are verified parametric overrides; the model stores the relevant fact but fails to suppress it. We also observe systematic cultural variation in these failures, consistent with imbalances in how characters from different regions are represented in model knowledge.

摘要：角色扮演的大型語言模型（LLMs）預期會採用角色的風格，同時尊重該角色的知識邊界。<lb/>先前的評估檢測到角色幻覺，但很少區分錯誤是由於未能識別邊界還是儘管識別卻未能遵守。<lb/>我們介紹了CHARM，一個涵蓋來自五個文化語言區域的40個真實和虛構角色的多文化基準，並由母語審核員驗證。<lb/>它探討了兩種邊界類型：時間性（歷史 vs. 現代）和跨宇宙（角色敘事或歷史宇宙之外的實體），使用啟用放棄的多選問題。<lb/>我們提出了一個兩階段的評估，將邊界意識（明確識別查詢超出範疇）與邊界遵守（在回答具體問題時的放棄）分開。<lb/>在六個LLMs的評估中顯示，幻覺主要是由遵守失敗驅動的。<lb/>模型經常承認查詢超出了角色的知識範疇，但仍然提供事實性的、非角色的答案。<lb/>通過將相同的問題重新提問給目標角色，我們確認這些案例中有很大一部分是經過驗證的參數覆蓋；模型儲存了相關事實，但未能抑制它。<lb/>我們還觀察到這些失敗中存在系統性的文化變異，與不同地區角色在模型知識中的表現不平衡一致。

##### **Probing Factual Knowledge Transfer with Training Data Interventions**
2609.01341v1 by Romina Oji, Marc Braun, Marcel Bollmann, Marco Kuhlmann, Jenny Kunz

Do multilingual language models transfer factual knowledge across languages during continued pretraining, or do they mostly recall facts learned directly from the target-language data? To answer this question more reliably, we propose an intervention-based framework: starting from an English-pretrained model, we continue pretraining on Persian data from which specific facts have been systematically removed at varying levels of granularity. We construct SIFT, a resource of 500 triples across 20 relations, stratified by the cultural origin of each fact's subject into general (globally prominent) and Persian-related entities, designed for both systematic fact removal from training data and evaluation, with natively written Persian cloze templates. Our results show that fact transfer is very limited: under the strictest removal condition, a large majority of English-acquired facts fail to transfer into Persian. We further show that sentence-level co-occurrence removal is insufficient to eliminate fact signal, and that easier (randomly selected) negative candidate sets substantially inflate apparent transfer by rewarding shallow associative heuristics, while performance on a harder candidate set that allows for less reliance on heuristics is much lower. Finally, we show that source-language entity frequency has a large influence, with Persian-related facts, which are orders of magnitude rarer in the English corpus, hardly transferring.

摘要：多語言模型在持續的預訓練過程中是否能夠跨語言轉移事實知識，還是主要從目標語言數據中直接回憶學到的事實？為了更可靠地回答這個問題，我們提出了一個基於干預的框架：從一個英語預訓練模型開始，我們在波斯語數據上繼續預訓練，這些數據中已系統性地移除了特定事實，並在不同的細節層次上進行了調整。我們構建了SIFT，這是一個包含500個三元組的資源，涵蓋20個關係，根據每個事實主體的文化來源劃分為一般（全球知名）和與波斯相關的實體，旨在系統性地從訓練數據中移除事實並進行評估，並附有本地撰寫的波斯語填空模板。我們的結果顯示，事實轉移非常有限：在最嚴格的移除條件下，絕大多數從英語獲得的事實無法轉移到波斯語。我們進一步顯示，句子級別的共現移除不足以消除事實信號，而更簡單（隨機選擇）的負候選集顯著膨脹了表面轉移，因為它獎勵了淺層的聯想啟發式，而在一個更難的候選集上，表現則要低得多，因為它對啟發式的依賴較少。最後，我們顯示源語言實體頻率有很大影響，與波斯相關的事實在英語語料庫中稀缺數量級，幾乎無法轉移。

##### **Automated Event Log Generation from Unstructured Text Using Finetuned LLMs**
2609.01320v1 by Maximilian Seeth, Gabriel Marques Tavares, Daniel Schuster

Process mining (PM) provides a powerful framework for discovering and optimizing operational processes from event data. However, the efficacy of PM techniques is strictly predicated on the availability of structured event logs. Thus far, event logs have often been laboriously created by domain and process mining experts. This costly effort causes large portions of organizational knowledge, including incident tickets, manuals, and textual reports, to remain underutilized. We address this bottleneck by investigating the efficacy of Large Language Models (LLMs) as automated data translators. We propose a scalable framework that leverages LLMs as data translators to bridge the gap between unstructured textual resources and structured event data. We finetune LLMs on a newly created text-to-log dataset, demonstrating that the resulting models can extract high-fidelity event logs from unstructured resources. Our results show that this finetuning approach outperforms few-shot or zero-shot prompting by a large amount, highlighting finetuning as a necessary pre-condition for generating reliable event data. We conclude that our method provides a promising pipeline for making previously unused data available to process mining ecosystems, effectively expanding the possibilities of using PM to further investigate organizational workflows.

摘要：流程挖掘 (PM) 提供了一個強大的框架，用於從事件數據中發現和優化操作過程。<lb/>然而，PM 技術的有效性嚴格依賴於結構化事件日誌的可用性。<lb/>到目前為止，事件日誌通常由領域和流程挖掘專家費力地創建。<lb/>這一昂貴的努力導致大量的組織知識，包括事件票據、手冊和文本報告，未得到充分利用。<lb/>我們通過研究大型語言模型 (LLMs) 作為自動數據翻譯器的有效性來解決這一瓶頸。<lb/>我們提出了一個可擴展的框架，利用 LLMs 作為數據翻譯器，彌補非結構化文本資源與結構化事件數據之間的差距。<lb/>我們在新創建的文本到日誌數據集上對 LLMs 進行微調，證明所得到的模型能夠從非結構化資源中提取高保真度的事件日誌。<lb/>我們的結果顯示，這種微調方法的表現遠超過少量樣本或零樣本提示，突顯出微調是生成可靠事件數據的必要前提。<lb/>我們得出結論，我們的方法為使先前未使用的數據可用於流程挖掘生態系統提供了一個有前景的管道，有效擴展了使用 PM 進一步研究組織工作流程的可能性。

##### **HiLRP: Toward One Trustworthy Explanation for Vision Transformer: Conservation-Valid Attribution via Attention Primitives**
2609.01282v1 by Sathiyamohan Nishankar, Pubudu Sanjeewani, Asanka Perera, Selvarajah Thuseethan

Vision Transformer (ViT) design has become increasingly diverse, with backbones combining convolutional stems, windowed, linear, or multi-axis attention, patch merging, and spatial reduction in various configurations. This diversity poses challenges for existing attribution methods, whose assumptions often do not hold across ViT variants: Grad-CAM requires a terminal spatial feature map, attention rollout assumes global softmax attention, and layer-wise relevance propagation (LRP) requires module-specific rules. To the best of our knowledge, no existing method provides a unified attribution framework across this architectural space. We show that this architectural diversity can be captured by a simpler underlying structure. The attention and resolution-reduction operators in current ViTs can be decomposed into four operation types: linear maps, bilinear mixing, normalization or gating, and reindexing. Each operation admits a relevance rule that satisfies conservation. Based on these rules, HiLRP supports new backbones by construction rather than by architecture-specific derivation, and its attribution maps decompose the prediction rather than relying on heuristic assumptions. We prove conservation and conditional equivariance and verify both to machine precision. Across 14 attribution methods and 10 architectures, we find that no prior method remains reliable across ViT families, while Faithfulness Correlation becomes uninformative for backbones robust to spatial masking. HiLRP alone preserves conservation across windowed, spatial-reduction, multi-axis, and linear-attention models, where naive extensions can produce zero or inflated relevance. It also localizes attribution failures in class activation mapping, achieving 0.97 Pointing compared with 0.55 for competing methods on EfficientViT.

摘要：視覺Transformer（ViT）的設計變得越來越多樣化，背後結合了卷積幹、窗口、線性或多軸注意力、補丁合併和空間縮減的各種配置。這種多樣性對現有的歸因方法提出了挑戰，因為這些方法的假設通常在不同的ViT變體中不成立：Grad-CAM需要終端空間特徵圖，注意力展開假設全局softmax注意力，而層級相關傳播（LRP）則需要模塊特定的規則。據我們所知，沒有現有的方法在這一架構空間中提供統一的歸因框架。我們顯示這種架構多樣性可以通過一個更簡單的基礎結構來捕捉。當前ViT中的注意力和解析度縮減運算子可以分解為四種操作類型：線性映射、雙線性混合、正規化或閘控，以及重新索引。每種操作都有一個滿足守恆的相關性規則。基於這些規則，HiLRP通過構造而非架構特定的推導來支持新的幹，而其歸因圖則分解預測，而不是依賴於啟發式假設。我們證明了守恆性和條件等變性，並將兩者驗證到機器精度。在14種歸因方法和10種架構中，我們發現沒有任何先前的方法在ViT家族中保持可靠，而忠實性相關性對於對空間遮罩穩健的幹變得無信息。HiLRP單獨在窗口、空間縮減、多軸和線性注意力模型之間保持守恆，其中天真的擴展可能產生零或膨脹的相關性。它還在類別激活映射中定位歸因失敗，實現了0.97的指向度，而競爭方法在EfficientViT上僅為0.55。

##### **Position Matters: Feature Inversion Attacks in ViT Split Inference with Token Reduction and Shuffling**
2609.01232v1 by Stefano Leggio, Giulio Rossolini, Alessandro Biondi

Vision Transformers (ViTs) are increasingly used in split-inference systems, where edge devices transmit intermediate token representations to a remote cloud. In this setting, token reduction lowers computation and communication costs, while token shuffling disrupts the spatial organization of the transmitted tokens, potentially limiting information leakage. However, their privacy benefits remain unclear against feature inversion attacks, which attempt to reconstruct the input from the transmitted embeddings. In this work, we show that, despite disrupting the spatial structure required by conventional reconstruction attacks, transmitted token embeddings retain substantial positional information. Based on this observation, we introduce the Spatially Aligned Reconstruction Attack (SARA), a unified pipeline that predicts token positions, restores their spatial layout, reconstructs missing embeddings using a feature-space masked autoencoder, and recovers the input image. Our results demonstrate that token shuffling provides only apparent privacy, as SARA largely reconstructs the original token organization. Token reduction offers stronger protection, but significant leakage persists when the retained tokens preserve sufficient semantic and positional information. Finally, we introduce a lightweight edge-side defense that removes positional embeddings and progressively adapts the edge-side transformer blocks through knowledge distillation. It substantially reduces attack performance against SARA, while preserving downstream task accuracy and requiring no changes to the cloud-side model.

摘要：視覺轉換器（ViTs）在分割推理系統中越來越常用，邊緣設備將中間標記表示傳輸到遠端雲端。在這種情況下，標記減少降低了計算和通信成本，而標記洗牌則破壞了傳輸標記的空間組織，可能限制信息洩漏。然而，針對特徵反演攻擊的隱私效益仍不明確，這些攻擊試圖從傳輸的嵌入中重建輸入。在這項工作中，我們顯示，儘管破壞了傳統重建攻擊所需的空間結構，但傳輸的標記嵌入仍保留了大量的位置信息。基於這一觀察，我們引入了空間對齊重建攻擊（SARA），這是一個統一的流程，預測標記位置，恢復其空間佈局，使用特徵空間掩碼自編碼器重建缺失的嵌入，並恢復輸入圖像。我們的結果表明，標記洗牌僅提供表面上的隱私，因為SARA在很大程度上重建了原始標記的組織。標記減少提供了更強的保護，但當保留的標記保留了足夠的語義和位置信息時，仍然存在顯著的洩漏。最後，我們引入了一種輕量級的邊緣側防禦，去除位置信息嵌入，並通過知識蒸餾逐步調整邊緣側轉換器塊。這大大降低了對SARA的攻擊性能，同時保持下游任務的準確性，並且不需要對雲端模型進行任何更改。

##### **CaRL-EM: Cost-Aware Reinforcement Learning for Entity Matching with LLMs**
2609.01195v1 by Chaohui Guo, Michel Klein, Zhisheng Huang

Entity matching (EM) requires fine-grained contextual understanding and domain knowledge. Recent work shows that large language models (LLMs) can serve as strong matchers across domains, but most methods either make independent pairwise decisions or rely on manually designed composite pipelines, thus lacking flexibility in realistic multi-candidate settings. At the same time, they typically ignore inference cost at scale. We formulate LLM-based EM with candidates as a cost-aware sequential decision problem and propose CaRL-EM, a reinforcement learning controller that manages LLM operations. Given the state of an anchor record, its candidate set, and the cost, CaRL-EM adaptively chooses among different operators (Match/Compare/Select/Decide) and model capacities to maximize a quality-cost objective. The policy interacts with abstract operators, allowing the same controller to be reused with different underlying LLM backends at inference time without retraining. Experiments on 7 benchmarks show that CaRL-EM (i) learns to dynamically plan the usage of inexpensive and expensive operators based on task complexity, (ii) achieves robust zero-shot transfer across diverse datasets and domains, and (iii) consistently achieves a better quality-cost trade-off than strong LLM-based baselines and manually designed pipelines, yielding a lower inference cost at comparable or higher quality.

摘要：實體匹配（EM）需要細緻的上下文理解和領域知識。最近的研究顯示，大型語言模型（LLMs）可以在各個領域中作為強大的匹配器，但大多數方法要麼做出獨立的成對決策，要麼依賴手動設計的綜合管道，因此在現實的多候選者環境中缺乏靈活性。與此同時，它們通常忽略了大規模推理的成本。我們將基於LLM的EM公式化為一個考慮成本的序列決策問題，並提出CaRL-EM，一個管理LLM操作的強化學習控制器。給定一個錨記錄的狀態、其候選集和成本，CaRL-EM自適應地在不同的操作符（匹配/比較/選擇/決定）和模型容量之間進行選擇，以最大化質量-成本目標。該策略與抽象操作符互動，使得同一控制器可以在推理時與不同的底層LLM後端重用，而無需重新訓練。在7個基準上的實驗顯示，CaRL-EM（i）學會根據任務複雜性動態規劃便宜和昂貴操作符的使用，（ii）在多樣的數據集和領域中實現穩健的零-shot轉移，以及（iii）始終實現比強LLM基準和手動設計管道更好的質量-成本權衡，在可比或更高的質量下產生較低的推理成本。

##### **Athena: Vulnerability-Affected Library Identification via Knowledge Graph Completion**
2609.01187v1 by Phong Trinh Duy, Trang Dang Yen, Hung Nguyen-Huu, Bach Le, Quyet-Thang Huynh, Dieu Hoang Vu, David Lo, Thanh Le-Cong

A single vulnerability in a widely used library can cascade through millions of dependent applications, yet more than half of vulnerability database entries contain missing or incorrect affected-library information. Existing automated approaches neglect the relational structure of vulnerability databases, treating identification as an isolated text retrieval problem. In this paper, we propose Athena, the first graph-based approach for vulnerability affected library identification. Athena models vulnerability databases as a knowledge graph and reformulates the identification problem as knowledge graph completion (KGC). It comprises three key modules: a Modeling module that constructs a security knowledge graph integrating CVEs, libraries, CWE weakness types, CPE products, and software ecosystems; a Completion module that applies a modular KGC backbone to predict missing affected libraries for a given CVE via link prediction; and a Re-ranking module that retrieves KGC candidates and rescores them using a fine-tuned LLM augmented with knowledge graph embeddings, jointly leveraging structural and textual information. Our experiments on VulLib demonstrate that Athena significantly outperforms four state-of-the-art baselines, achieving a 32% improvement in Avg. F1 over the best baseline (i.e., VulLibGen). Notably, our KGC backbone with only 110M parameters already surpasses VulLibGen's best configuration at 7B parameters, demonstrating the effectiveness of graph-based modeling; the re-ranking module then provides substantial further gains, consistently outperforming the best baseline across all evaluated LLM backbones.

摘要：一個在廣泛使用的庫中的單一漏洞可以通過數百萬個依賴應用程序進行級聯，然而超過一半的漏洞數據庫條目包含缺失或不正確的受影響庫信息。現有的自動化方法忽略了漏洞數據庫的關係結構，將識別視為一個孤立的文本檢索問題。在本文中，我們提出了Athena，這是第一個基於圖的漏洞受影響庫識別方法。Athena將漏洞數據庫建模為知識圖，並將識別問題重新表述為知識圖補全（KGC）。它包括三個關鍵模塊：一個建模模塊，構建一個整合CVE、庫、CWE弱點類型、CPE產品和軟件生態系統的安全知識圖；一個補全模塊，應用模塊化的KGC骨架通過鏈接預測來預測給定CVE的缺失受影響庫；以及一個重新排名模塊，檢索KGC候選者並使用經過微調的LLM重新評分，該LLM增強了知識圖嵌入，聯合利用結構和文本信息。我們在VulLib上的實驗表明，Athena顯著超越了四個最先進的基準，實現了相較於最佳基準（即VulLibGen）在平均F1上提高32%的效果。值得注意的是，我們的KGC骨架僅有1.1億個參數，已經超過了VulLibGen在70億參數下的最佳配置，展示了基於圖的建模的有效性；而重新排名模塊則提供了顯著的進一步增益，在所有評估的LLM骨架中持續超越最佳基準。

##### **LLMPEDIA: Browsing, Verifying, and Comparing the Parametric Encyclopedic Knowledge of LLMs**
2609.01182v1 by Muhammed Saeed, Simon Razniewski

Flagship language models appear saturated on benchmarks like MMLU (Hendrycks et al., 2021), scoring above 90% - yet benchmarks test only what the experimenter thought to ask, the availability bias of fixed question sets. LLMPEDIA makes this bias measurable and browsable. We recursively materialized ~1.3M articles from three model families' parametric memory (GPT-5-mini, DeepSeek-V3.2, Llama-3.3-70B) without retrieval, then audited a stratified sample of atomic claims against Wikipedia and a curated web stack, coloring every claim supported, refuted, or insufficient (Saeed and Razniewski, 2026). On a uniform random sample the true rate is 68.4% - more than 21 pp below MMLU - with 30.5% of claims insufficient: assertions no benchmark probes and the world's largest encyclopedia cannot adjudicate - long-tail knowledge or plausible hallucination, the evidence cannot tell - extending to free text the coverage gap GPTKB established for triples (Hu et al., 2025). The resulting live, open encyclopedia lets visitors inspect this frontier one claim at a time through five one-click views - link-traversal exploration, claim-level factuality, cross-model and political-persona comparison, and a guided topic drill-down - each page, claim, and verdict at a stable URL. LLMPEDIA is live at https://llmpedia.net

摘要：旗艦語言模型在像 MMLU (Hendrycks et al., 2021) 這樣的基準上似乎已經飽和，得分超過 90% - 然而基準僅測試實驗者想要詢問的內容，這是固定問題集的可用性偏見。LLMPEDIA 使這種偏見可測量且可瀏覽。我們從三個模型家族的參數記憶中遞歸性地實現了約 130 萬篇文章 (GPT-5-mini, DeepSeek-V3.2, Llama-3.3-70B)，未進行檢索，然後對一個分層樣本的原子主張進行審核，與維基百科和策劃的網絡堆疊進行對比，標記每一個主張是被支持、反駁還是不足 (Saeed and Razniewski, 2026)。在一個均勻隨機樣本中，真實率為 68.4% - 比 MMLU 低超過 21 個百分點 - 其中 30.5% 的主張不足：這些主張是沒有基準探測且世界上最大的百科全書無法裁決的 - 長尾知識或合理的幻覺，證據無法判斷 - 擴展到自由文本，填補了 GPTKB 為三元組建立的覆蓋差距 (Hu et al., 2025)。最終產生的實時開放百科全書讓訪客可以逐一檢查這一前沿，通過五種一鍵查看 - 連結遍歷探索、主張級事實性、跨模型和政治角色比較，以及引導主題深入 - 每個頁面、主張和裁決都有一個穩定的 URL。LLMPEDIA 現在在線上 https://llmpedia.net

##### **Causal Evidentiary Governance for High-Risk Machine Learning Systems**
2609.01040v1 by Samah Kareem, Barış Çeliktaş

Machine learning systems deployed for credit, hiring, and resource distribution are increasingly subject to regulatory oversight from policies such as the EU AI Act and GDPR. Current fairness governance practices rely on observational fairness metrics, post-hoc explainability, and immutable audit logs, but provide limited support for causal attribution and efficient evidentiary verification. We introduce Causal Evidentiary Governance (CEG), a framework in which regulated institutions commit to a versioned directed acyclic graph (DAG) that partitions causal pathways into allowable and disallowed groups. The Causal Harm Rate measures prediction variation attributable to disallowed causal pathways. Each decision is accompanied by a signed Decision-Evidence Packet (DEP), cryptographically binding the prediction to a digest of the published DAG and path-specific attributions. DEP digests can be appended to a Merkle tree to enable logarithmic-cost inclusion proofs. We validate CEG through a two-layer empirical methodology using demographic summaries from four years of PMA credit supervisory data to construct 10,000 synthetic credit applicants across four strategic DAG counterfactuals. Causal Harm Rate isolates injected causal effects more clearly than demographic parity or equalized odds. Cross-model validation and ablation studies assess robustness. Evaluation on the German Credit dataset shows that harm associated with specific causal pathways can be substantially understated by associational fairness metrics. Finally, a proof-of-concept implementation demonstrates operationally plausible throughput and highlights relevant performance tradeoffs.

摘要：機器學習系統在信用、招聘和資源分配方面的應用越來越受到來自政策的監管，例如歐盟人工智慧法案和GDPR。當前的公平治理實踐依賴於觀察性公平指標、事後可解釋性和不可變的審計日誌，但對於因果歸因和高效證據驗證的支持有限。我們提出了因果證據治理（Causal Evidentiary Governance, CEG），這是一個框架，受監管機構承諾使用版本化的有向無循環圖（DAG），將因果路徑劃分為允許和不允許的組別。因果傷害率（Causal Harm Rate）衡量由不允許的因果路徑所引起的預測變異。每個決策都附有一個簽名的決策證據包（Decision-Evidence Packet, DEP），以密碼學方式將預測綁定到已發佈DAG的摘要和路徑特定的歸因。DEP摘要可以附加到默克爾樹上，以實現對數成本的包含證明。我們通過一種兩層實證方法驗證CEG，使用四年的PMA信用監管數據的人口統計摘要，構建了10,000個合成信用申請者，涵蓋四個戰略DAG反事實。因果傷害率比人口平等或均衡機會更清晰地孤立注入的因果效應。跨模型驗證和消融研究評估其穩健性。對德國信用數據集的評估顯示，與特定因果路徑相關的傷害可能被關聯公平指標大幅低估。最後，概念驗證實現展示了運營上可行的吞吐量，並突顯了相關的性能權衡。

##### **On Synthesis of Metric Interval Temporal Logics**
2609.01032v1 by Hsi-Ming Ho, Shankaranarayanan Krishna, Khushraj Madnani

Automated mining of formal specifications is vital for verifying real-time systems. However, existing passive learning approaches remain restricted to deterministic specifications or limited fragments of Timed Regular Expressions (TRE). To our knowledge, this paper presents the first framework to tackle \emph{precise} passive learning for an expressive timed logic, \emph{Metric Interval Temporal Logic} (MITL) without relying on predefined templates or restricted logic fragments.
  Our approach formally reduces the timed learning problem into a scalable untimed one. By identifying quantitative timing differences between positive and negative traces, we synthesise precise timed constraints and inject them as new Boolean atomic propositions. This embeds timing into the alphabet, delegating the complex formula evaluation to highly optimised, off-the-shelf untimed LTL tools.
  Crucially, our framework is complete, guaranteeing a separating specification can always be found. We evaluate our implementation across several benchmarks, demonstrating the effectiveness of our approach.

摘要：自動化挖掘形式規範對於驗證即時系統至關重要。<lb/>然而，現有的被動學習方法仍然限於確定性規範或有限的時間正則表達式（TRE）片段。<lb/>據我們所知，本文提出了第一個框架，以解決\emph{精確}的被動學習問題，針對一種表達能力強的時間邏輯——\emph{度量區間時間邏輯}（MITL），而不依賴於預定義的模板或受限的邏輯片段。<lb/>我們的方法將時間學習問題形式上簡化為可擴展的無時間問題。<lb/>通過識別正負追蹤之間的定量時間差異，我們合成精確的時間約束，並將其作為新的布爾原子命題注入。<lb/>這將時間嵌入到字母表中，將複雜的公式評估委派給高度優化的現成無時間LTL工具。<lb/>關鍵是，我們的框架是完整的，保證始終可以找到一個分離規範。<lb/>我們在幾個基準上評估了我們的實現，展示了我們方法的有效性。

##### **A Network Science Perspective on Evaluating Deep Graph Generative Models**
2609.01015v1 by Tianrui Mao, Abele Malan, Megha Khosla, Lydia Chen, Huijuan Wang

Traditional network models from network science, such as the Erdos-Renyi and configuration models, generate random networks that reproduce few selected topological properties observed in real-world networks. Deep graph generative models emerge as a data-driven approach, leveraging deep neural network architectures to learn complex structural distributions directly from real-world networks to generate more realistic synthetic networks. Because real social contact networks cannot be shared due to privacy risks, synthetic networks serve as an alternative for developing and evaluating epidemic mitigation strategies. In this work, we evaluate deep graph generative models as well as the configuration from a network science perspective by assessing both the topological similarity between generated and real-world networks and their utility in identifying effective node immunization strategies to sup- press epidemic/misinformation spreading. It is found that two deep graph generative models produce synthetic networks that closely resemble the structural properties of real-world networks, enabling them to identify effective immunization strategies.

摘要：傳統的網路科學網路模型，如Erdos-Renyi和配置模型，生成的隨機網路僅能重現現實世界網路中觀察到的少數選定拓撲特性。深度圖生成模型作為一種數據驅動的方法，利用深度神經網路架構直接從現實世界網路中學習複雜的結構分佈，以生成更真實的合成網路。由於由於隱私風險，無法共享真實的社交接觸網路，因此合成網路作為開發和評估疫情緩解策略的替代方案。在本研究中，我們從網路科學的角度評估深度圖生成模型以及配置，通過評估生成的網路和現實世界網路之間的拓撲相似性以及它們在識別有效節點免疫策略以抑制疫情/錯誤信息擴散中的效用。結果發現，兩個深度圖生成模型生成的合成網路在結構特性上與現實世界網路非常相似，使它們能夠識別有效的免疫策略。

##### **The zbMATH Open Knowledge Graph: Tracing Centuries of Mathematical Research**
2609.00969v1 by Yuni Susanti, Moritz Schubotz

We present the zbMATH Open Knowledge Graph, a large-scale RDF knowledge graph (KG) covering more than 250 years of mathematical scholarship. Unlike existing scholarly knowledge graphs that primarily capture bibliographic metadata and citation structures, the zbMATH Open KG integrates expert-curated semantic content, including reviews, keywords, subject classifications, software references, and disambiguated authorship. This combination of domain-specific representation of mathematical knowledge and extensive temporal coverage supports analyses that require fine-grained exploration of mathematical concepts, research fields, and scholarly relationships over time. The resulting graph comprises 34 million entities and 168 million RDF triples represented using established Semantic Web vocabularies, supporting interoperability and FAIR data principles. We further demonstrate its capabilities through query-driven historically grounded scholarly exploration use cases, illustrating how the knowledge graph can surface relationships and patterns that may be difficult to identify from bibliographic and citation information alone. The zbMATH Open KG provides an open semantic infrastructure for studying the development of mathematical knowledge and tracing scholarly connections across centuries of scholarship.

摘要：我們呈現 zbMATH 開放知識圖譜，這是一個涵蓋超過 250 年數學學術的規模龐大的 RDF 知識圖譜 (KG)。與主要捕捉書目元數據和引用結構的現有學術知識圖譜不同，zbMATH 開放 KG 整合了專家策劃的語義內容，包括評論、關鍵詞、主題分類、軟體參考和消歧義的作者身份。這種數學知識的領域特定表示與廣泛的時間覆蓋相結合，支持需要對數學概念、研究領域和學術關係進行細緻探索的分析。最終的圖譜包含 3400 萬個實體和 1.68 億個 RDF 三元組，使用已建立的語義網詞彙表示，支持互操作性和 FAIR 數據原則。我們進一步通過查詢驅動的歷史基礎學術探索用例展示其能力，說明知識圖譜如何揭示可能僅從書目和引用信息中難以識別的關係和模式。zbMATH 開放 KG 提供了一個開放的語義基礎設施，用於研究數學知識的發展和追溯跨越幾個世紀的學術連結。

##### **CoBRA: Learning Tool-Use Boundaries via Counterfactual Margins**
2609.00967v1 by Wenhao Zou, Xianglong Liu, Wendong Bi, Hanjie Wang, Simin Zhao, Gong Zhi

As large language models increasingly act through external tools, deciding when to call a tool has become a central problem alongside deciding how to use it. Unnecessary tool calls introduce latency, cost, retrieval noise, and error propagation, while missed calls hurt knowledge-intensive queries or questions requiring up-to-date evidence. Existing methods typically trigger tools from absolute query or generation signals, such as difficulty, confidence, or final task reward, and therefore lack an explicit estimate of the instance-level marginal benefit of tool use. We propose CoBRA, a counterfactual boundary-learning framework for tool-augmented language models. CoBRA first constructs internal and external experts from the same base model, collects paired trajectories, and estimates the reward margin between answering with and without tools. This margin partitions data into internal-favored, external-favored, and ambiguous cases. CoBRA then uses clear-margin samples for Boundary-Aware Cold-Start SFT, followed by MARS-RL with reference-split rollouts and counterfactual marginal advantages to optimize boundary decisions. Experiments with retrieval as the main tool on Qwen3-4B show that CoBRA improves tool-use efficiency and boundary-sensitive answer accuracy while maintaining strong performance on tool-dependent out-of-distribution questions.

摘要：隨著大型語言模型越來越多地通過外部工具進行操作，決定何時調用工具已成為一個核心問題，與決定如何使用它並列。 不必要的工具調用會引入延遲、成本、檢索噪音和錯誤傳播，而錯過的調用則會影響知識密集型查詢或需要最新證據的問題。 現有方法通常從絕對查詢或生成信號（如難度、信心或最終任務獎勵）觸發工具，因此缺乏對工具使用的實例級邊際效益的明確估計。 我們提出了CoBRA，一種用於工具增強語言模型的反事實邊界學習框架。 CoBRA首先從相同的基礎模型構建內部和外部專家，收集配對軌跡，並估計使用工具與不使用工具之間的獎勵邊際。 這個邊際將數據劃分為內部偏好、外部偏好和模糊案例。 然後，CoBRA使用明確邊際樣本進行邊界感知冷啟動SFT，隨後進行MARS-RL，並使用參考分割的滾動和反事實邊際優勢來優化邊界決策。 在Qwen3-4B上以檢索為主要工具的實驗顯示，CoBRA提高了工具使用效率和邊界敏感的答案準確性，同時在依賴工具的分佈外問題上保持強勁的表現。

##### **DualStake: Dual-Path Confidence Calibration in Deep Research Agents**
2609.00935v1 by Yinuo Xu, Yuwei Liang, Jianjie Cheng, Meng Wang, Yongcan Yu, Shuo Lu, Jian Liang

Deep Research agents tackle knowledge-intensive tasks through multi-round retrieval and decision-oriented generation. However, these agents suffer from severe overconfidence, making their expressed confidence unreliable for user trust and downstream abstention. To address this, we augment the Deep Research pipeline with step confidence elicitation after each retrieval, building on the commonly used post-answer verbalized confidence. Interestingly, we find that Evidence Confidence (E-Conf), elicited after the final retrieval step, provides a stronger uncertainty signal than Answer Confidence (A-Conf), elicited after answer generation, and that A-Conf is largely shaped by E-Conf. Based on these findings, we propose DualStake, a dual-path calibration method that applies margin-clipped, confidence-dependent stake rewards to jointly align E-Conf and A-Conf with answer correctness while limiting extreme confidence optimization. Experiments on Qwen2.5-7B, Qwen2.5-7B-Instruct, and Qwen3-4B across 8 QA benchmarks demonstrate that DualStake consistently improves calibration without sacrificing answer accuracy. The code is available at https://github.com/FloXXXt/DualStake.

摘要：深度研究代理透過多輪檢索和決策導向生成來處理知識密集型任務。<lb/>然而，這些代理面臨嚴重的過度自信，使得它們表達的信心對於用戶信任和後續的放棄行為而言不可靠。<lb/>為了解決這個問題，我們在每次檢索後增強深度研究流程，進行步驟信心的引出，並基於常用的回答後口頭表達信心。<lb/>有趣的是，我們發現在最後一次檢索步驟後引出的證據信心（E-Conf）提供了比回答信心（A-Conf）更強的不確定性信號，而A-Conf在很大程度上受到E-Conf的影響。<lb/>基於這些發現，我們提出了DualStake，一種雙路徑校準方法，應用邊際剪裁的、依賴信心的賭注獎勵，以共同對齊E-Conf和A-Conf與答案的正確性，同時限制極端信心的優化。<lb/>在Qwen2.5-7B、Qwen2.5-7B-Instruct和Qwen3-4B上進行的8個QA基準實驗表明，DualStake在不犧牲答案準確性的情況下，始終改善了校準。<lb/>代碼可在 https://github.com/FloXXXt/DualStake 獲得。

##### **Context-Grounding Gains Are Mediated by Pre-existing Machinery: Auditing GRPO, SFT, and DPO**
2609.00925v1 by Prakhar Gupta, Vaibhav Gupta

Language models can ignore prompt evidence when it conflicts with memorized knowledge. Post-training can make models follow such evidence more reliably, but it is unclear whether these gains require new machinery or strengthen machinery already present. We compare nine post-training arms spanning GRPO, SFT, and DPO from one starting checkpoint, with key comparisons extended across scales and families. We estimate a grounding direction from that checkpoint before training. Across five tested GRPO variants, grounding gains are small. For the two variants replicated across seeds, equivalence tests bound their effects below the conflict-SFT gain even as the rewarded metric improves. Conflict-SFT improves grounding moderately, while DPO drives grounding near ceiling on its matched distribution. Conflict-SFT and DPO largely use the same causal attention-head set as the starting model. Subtracting the starting-model direction suppresses both gains, while adding it to the starting model recovers 35% of DPO's gain at a dose passing all stated side-effect checks. After a supervised warm start makes the context answer appear in more rollouts, the same GRPO recipe adds essentially no further grounding gain. In our setting, grounding gains largely depend on machinery already present in the starting model.

摘要：語言模型在面對與記憶知識衝突的提示證據時，可以選擇忽略這些證據。後訓練可以使模型更可靠地遵循這些證據，但尚不清楚這些增益是否需要新的機制或是加強已存在的機制。我們比較了從一個起始檢查點出發的九個後訓練方法，涵蓋了GRPO、SFT和DPO，並在不同的規模和家族之間擴展了關鍵比較。我們從該檢查點估計了訓練前的基準方向。在五個測試的GRPO變體中，基準增益很小。對於在不同隨機種子下重複的兩個變體，等效性測試將它們的影響限制在低於衝突-SFT增益的範圍內，即使獲獎指標有所改善。衝突-SFT適度改善基準，而DPO則使基準在其匹配分佈上接近上限。衝突-SFT和DPO在很大程度上使用與起始模型相同的因果注意力頭集。減去起始模型方向會抑制這兩個增益，而將其添加到起始模型則恢復了DPO增益的35%，並通過了所有聲明的副作用檢查。在經過監督的熱啟動使上下文答案在更多的回合中出現後，相同的GRPO配方幾乎沒有增加進一步的基準增益。在我們的設定中，基準增益在很大程度上依賴於起始模型中已存在的機制。

##### **Reinforcement Learning Enhanced LLM Agents for Complex Vehicle Routing Problems**
2609.00859v1 by Yi Chen, Zikang Yu, Jiahai Wang, Jinbiao Chen, Jianpeng Zhou, Zizhen Zhang

Vehicle Routing Problems (VRPs) are fundamental combinatorial optimization problems with widespread applications in various scenarios. The advanced optimization solvers can effectively solve such problems. However, modeling complex VRP variants for solvers often requires substantial domain expertise, which limits the accessibility of advanced optimization technologies. In this paper, we propose Reinforcement Learning Enhanced LLMAgents(RLEA), a multi-agent framework designed to automate the modeling of complex VRPs. RLEA introduces a lightweight neural Planner trained with Soft Q-learning to efficiently orchestrate the actions of LLM-based agents. In addition, we equip the system with an evolutionary memory module and retrieval-augmented generation, enabling the agent to leverage both accumulated experience and external solver knowledge during program generation and refinement for solving VRPs. We evaluated 48 distinct VRP variants across various solvers. The experimental results demonstrate that RLEA outperforms the previous state-of-the-ar method, achieving a 16.67% higher success rate while significantly reducing runtime errors. These results validate that integrating reinforcement learning with LLM-based reasoning is highly effective for automated optimization modeling. The appendix is available at: https://doi.org/10.5281/zenodo.19134435.

摘要：車輛路徑問題（VRPs）是基本的組合優化問題，在各種場景中有廣泛的應用。<lb/>先進的優化求解器可以有效地解決這類問題。<lb/>然而，為求解器建模複雜的VRP變體通常需要大量的領域專業知識，這限制了先進優化技術的可及性。<lb/>在本文中，我們提出了增強型強化學習LLMAgents（RLEA），這是一個旨在自動化複雜VRP建模的多代理框架。<lb/>RLEA引入了一個輕量級的神經規劃器，使用軟Q學習進行訓練，以有效協調基於LLM的代理的行動。<lb/>此外，我們為系統配備了一個進化記憶模塊和檢索增強生成，使代理能夠在生成和完善程序以解決VRP時利用累積的經驗和外部求解器知識。<lb/>我們在各種求解器上評估了48個不同的VRP變體。<lb/>實驗結果表明，RLEA的成功率比之前的最先進方法高出16.67%，同時顯著減少了運行時錯誤。<lb/>這些結果驗證了將強化學習與基於LLM的推理相結合在自動化優化建模中是非常有效的。<lb/>附錄可在以下鏈接獲得：https://doi.org/10.5281/zenodo.19134435。

##### **Verifiable Disaster Storylines and Causal Knowledge Graphs: A Citation-Grounded Pipeline from Heterogeneous Humanitarian Sources**
2609.00858v1 by Ivan Decostanzi, Michele Ronco, Sergio Consoli, Christina Corbane, Lorenzo Bertolini, Indaco Biazzo, Daria Mihaila, Manuel Garcia-Herranz, Felix Schwebel, Yelena Mejova, Kyriaki Kalimeri

Effective humanitarian response depends on the rapid synthesis of heterogeneous, high-volume information sources - a task that routinely exceeds human analytical capacity in the critical early hours of a crisis. We present a pipeline that combines structured disaster records from EM-DAT with unstructured documents from ReliefWeb and the European Media Monitor (EMM) to produce source-grounded disaster storylines and causal knowledge graphs supporting situational awareness for responders and analysts. Using Retrieval-Augmented Generation, the pipeline extracts structured storylines - tabular event profiles covering 17 fields, from severity and key drivers to child-sensitive impact indicators - and constructs causal knowledge graphs where each node and edge is enriched with citation-grounded explanatory narratives, enabling full traceability back to primary sources. We evaluate the system on three diverse crisis use cases through a human evaluation involving 9 domain expert and 9 non-expert evaluators. Results confirm high retrieval precision, strong faithfulness of extracted causal relations, and a clear expert preference for citation-grounded components over ungrounded alternatives. The pipeline is designed to scale to the full EM-DAT catalogue, with the goal of publicly releasing a narrative-enriched version of the database.

摘要：有效的人道主義反應依賴於快速綜合異質的高容量信息來源——這一任務在危機的關鍵早期階段常常超出人類的分析能力。我們提出了一個管道，將來自 EM-DAT 的結構化災難記錄與來自 ReliefWeb 和歐洲媒體監測（EMM）的非結構化文件相結合，以生成基於來源的災難故事情節和因果知識圖，支持應對者和分析師的情境意識。通過檢索增強生成，該管道提取結構化故事情節——涵蓋 17 個領域的表格事件概況，從嚴重性和關鍵驅動因素到兒童敏感影響指標——並構建因果知識圖，其中每個節點和邊緣都附有基於引用的解釋性敘述，實現對主要來源的完整可追溯性。我們通過涉及 9 位領域專家和 9 位非專家評估者的人類評估，對該系統在三個不同的危機使用案例上進行評估。結果確認了高檢索精度、提取的因果關係的強忠實性，以及專家對基於引用的組件相較於無根據替代品的明顯偏好。該管道旨在擴展到完整的 EM-DAT 目錄，目標是公開釋放一個敘述豐富的數據庫版本。

##### **Reflect-SQL: A Self-Reflection Based Framework for Text-to-SQL**
2609.02944v1 by Anupreksha Jain, Manish Shrivastava

Democratizing data access through natural language is a crucial goal for modern enterprises, but the practical adoption of Text-to-SQL is critically hindered by real-world complexities: 1. Obscure and large database schemas, 2. Ineffective retrieval of relevant tables and columns due to structured setting of schemas and vague user query, 3. Generation of syntactically or logically flawed SQL due to a lack of robust validation and correction mechanism. To address these systemic challenges, we introduce Reflect-SQL, a novel framework for Text to SQL, grounded in multi-stage self-reflection approach to develop understanding of obscure schema using a knowledge base, setup a process for effective retrieval and system to generate syntactically/semantically SQL. Instead of a single-pass attempt, our system employs an LLM-as-a-judge driven scoring mechanism within interconnected feedback loops to iteratively refine the results at every stage. A feedback-driven retrieval loop refines the user's natural language query, while a synthesis loop validates and corrects the SQL and finally, an entailment loop optimizes the end-to-end process and continuously enriches the knowledge base. By integrating these layers of reflection, Reflect-SQL bridges the critical gap between user intent and complex data. On the challenging BIRD benchmark, our framework achieves an execution accuracy of 72.03%, significantly outperforming state-of-the-art baselines, demonstrating a major leap in reliability for enterprise applications.

摘要：透過自然語言實現數據訪問的民主化是現代企業的一個重要目標，但Text-to-SQL的實際採用受到現實世界複雜性的嚴重阻礙：1. 模糊且龐大的數據庫架構，2. 由於架構的結構化設置和模糊的用戶查詢，無法有效檢索相關的表格和列，3. 由於缺乏強健的驗證和修正機制，生成語法或邏輯上有缺陷的SQL。為了解決這些系統性挑戰，我們推出了Reflect-SQL，這是一個基於多階段自我反思方法的Text to SQL新框架，旨在利用知識庫發展對模糊架構的理解，設立有效檢索的流程和生成語法/語義正確SQL的系統。我們的系統並不是單次嘗試，而是採用LLM-as-a-judge驅動的評分機制，在互聯的反饋循環中逐步細化每個階段的結果。一個以反饋為驅動的檢索循環細化用戶的自然語言查詢，而一個綜合循環則驗證和修正SQL，最後，一個推論循環優化端到端的過程並持續豐富知識庫。通過整合這些反思層，Reflect-SQL彌補了用戶意圖與複雜數據之間的關鍵差距。在具有挑戰性的BIRD基準上，我們的框架達到了72.03%的執行準確率，顯著超越了最先進的基準，顯示出企業應用在可靠性方面的重大飛躍。

##### **Towards Generalizable Visually Grounded Exploration of Household Devices**
2609.00845v1 by Linhao Zheng, Zeming Liu, Wangke Chen, Li Zeng, Wanxiang Che, Heyan Huang, Yuhang Guo

Recent advancements in Vision-Language Models (VLMs) have demonstrated impressive capabilities in static visual recognition and high-level semantic reasoning. However, current embodied exploration paradigms still heavily rely on imitation learning from human-annotated trajectories, which severely limits agents' generalization ability. The key bottleneck of realizing general autonomous embodied agents lies in Generalizable Visually Grounded Exploration: the ability to operate novel devices without manuals or specific training by actively grounding abstract world knowledge into fine-grained visual affordances. Yet, existing benchmarks fail to evaluate this capability: they generally rely on explicit documents and annotated trajectories, neglecting the dynamic Hypothesis-Interaction-Refinement process essential for functional device operation. To bridge this gap, we introduce VGEBench, a comprehensive benchmark designed to evaluate the generalizable visually grounded exploration capabilities of VLMs. Unlike static datasets, we construct a Logic-Driven State Machine framework. This framework simulates multi-turn interaction loops, compelling agents to achieve goals by active visual perception and feedback-driven correction. Experimental results demonstrate that existing VLMs face significant challenges in translating semantic knowledge into physical execution and maintaining long-horizon state tracking.

摘要：最近在視覺-語言模型（VLMs）方面的進展展示了在靜態視覺識別和高階語義推理方面的驚人能力。<lb/>然而，當前的具身探索範式仍然在很大程度上依賴於從人類標註的軌跡中進行模仿學習，這嚴重限制了代理的泛化能力。<lb/>實現通用自主具身代理的關鍵瓶頸在於可泛化的視覺基礎探索：能夠在沒有手冊或特定訓練的情況下操作新設備，通過主動將抽象世界知識具體化為細緻的視覺可用性。<lb/>然而，現有的基準無法評估這一能力：它們通常依賴於明確的文檔和標註的軌跡，忽視了對於功能性設備操作至關重要的動態假設-互動-精煉過程。<lb/>為了填補這一空白，我們推出了VGEBench，一個綜合基準，旨在評估VLMs的可泛化視覺基礎探索能力。<lb/>與靜態數據集不同，我們構建了一個邏輯驅動狀態機框架。<lb/>這個框架模擬多回合互動循環，迫使代理通過主動的視覺感知和反饋驅動的修正來達成目標。<lb/>實驗結果表明，現有的VLMs在將語義知識轉化為物理執行和維持長期狀態跟蹤方面面臨重大挑戰。

##### **TWIX: a Two-Stage Approach for End-To-End Named Entity Recognition and Relation Extraction**
2609.00832v1 by Marco Martinelli, Laura Menotti

The exponential growth of scientific publications calls for automatic Information Extraction (IE) systems to support knowledge discovery. In this context, the GutBrainIE benchmark evaluates Named Entity Recognition (NER), Named Entity Recognition and Disambiguation (NERD), and Relation Extraction (RE) systems in the gut-brain axis domain. We propose Two-stage Workflow for Information eXtraction (TWIX), an end-to-end IE pipeline featuring three interconnected modules, each leveraging a two-stage framework to solve all four GutBrainIE subtasks. Evaluation on the development and test sets shows that our method substantially outperforms the baseline by a wide margin, while also ranking first among all participant submissions across all subtasks. These results indicate that the proposed two-stage pipeline effectively improves both precision and recall in practical settings.

摘要：科學出版物的指數增長呼籲自動化資訊提取（IE）系統來支持知識發現。在這個背景下，GutBrainIE基準評估腸-腦軸領域的命名實體識別（NER）、命名實體識別與消歧（NERD）以及關係提取（RE）系統。我們提出了資訊提取的兩階段工作流程（TWIX），這是一個端到端的IE管道，具有三個相互連接的模塊，每個模塊都利用兩階段框架來解決所有四個GutBrainIE子任務。在開發集和測試集上的評估顯示，我們的方法在基準線上大幅超越，並且在所有子任務中所有參賽提交中排名第一。這些結果表明，所提出的兩階段管道在實際環境中有效提高了精確度和召回率。


### LLM
|Publish Date|Title|Authors|Homepage|Code|
| :---: | :---: | :---: | :---: | :---: |
|**2026-09-03**|**Compile by Training: Turning Natural-Language Specifications into Local Neural Functions**|Yuntian Deng et.al.|[2609.04199v1](http://arxiv.org/abs/2609.04199v1)|null|
|**2026-09-03**|**Clean Engineering, Unstable Measurement: A Preregistered Reliability Failure of Black-Box LLM Observers on Shared Endpoints**|Haoyaun Zhu et.al.|[2609.04198v1](http://arxiv.org/abs/2609.04198v1)|null|
|**2026-09-03**|**ESPO: Error-Structured Prompt Optimization via Diagnose, Diversify, and Stabilize**|Lihao Liu et.al.|[2609.04197v1](http://arxiv.org/abs/2609.04197v1)|null|
|**2026-09-03**|**Legibility is Not Interpretability: Comparing Judged and Actual Importance in Chain-Of-Thought Reasoning**|Kevin Du et.al.|[2609.04194v1](http://arxiv.org/abs/2609.04194v1)|null|
|**2026-09-03**|**Seeing Before Synthesizing: VLM-Guided Transition Event Discovery for Weakly-Supervised Dense Video Captioning**|Ye-Chan Kim et.al.|[2609.04183v1](http://arxiv.org/abs/2609.04183v1)|null|
|**2026-09-03**|**Knowledge Acquisition During Pre-training? Large Language Models Learn Better With Auxiliary Views**|Joseph Lee et.al.|[2609.04180v1](http://arxiv.org/abs/2609.04180v1)|null|
|**2026-09-03**|**A Computationally Feasible Framework for Causal Probabilistic Explanation**|Rafal Urbaniak et.al.|[2609.04177v1](http://arxiv.org/abs/2609.04177v1)|null|
|**2026-09-03**|**Last Translation Benchmark**|Vilém Zouhar et.al.|[2609.04173v1](http://arxiv.org/abs/2609.04173v1)|null|
|**2026-09-03**|**Rethinking On-Policy Distillation of Large Language Models II: One Training Example**|Zixuan Fu et.al.|[2609.04172v1](http://arxiv.org/abs/2609.04172v1)|null|
|**2026-09-03**|**A Case Study on Emergent Cheating and Whistleblowing in Autonomous Research Swarms**|Davide Paglieri et.al.|[2609.04170v1](http://arxiv.org/abs/2609.04170v1)|null|
|**2026-09-03**|**SWE-Gate: Passing Functional Tests Is Not Enough for Software Engineering Agents**|Xin He et.al.|[2609.04167v1](http://arxiv.org/abs/2609.04167v1)|null|
|**2026-09-03**|**From Deceptive Outputs to Deceptive Mechanisms: A Causal Framework for Language-Model Deception Research**|Yakov Pyotr Shkolnikov et.al.|[2609.04166v1](http://arxiv.org/abs/2609.04166v1)|null|
|**2026-09-03**|**SENTINEL-RL: Offloading Topological Reasoning from LLM Agents in the Security Operations Center**|Uday Vallabhaneni et.al.|[2609.04159v1](http://arxiv.org/abs/2609.04159v1)|null|
|**2026-09-03**|**The Natural Language Interaction Protocol and Standard for AI Agents**|Luyi Xing et.al.|[2609.04135v1](http://arxiv.org/abs/2609.04135v1)|null|
|**2026-09-03**|**Environment Evolution for Terminal Agents**|Zhiyuan Fan et.al.|[2609.04128v1](http://arxiv.org/abs/2609.04128v1)|null|
|**2026-09-03**|**Epistemic Warrant for LLM Recommendations: Characterizing the Basis for Reliance When Ground Truth Is Unavailable**|Shai Vardi et.al.|[2609.04127v1](http://arxiv.org/abs/2609.04127v1)|null|
|**2026-09-03**|**Sequential Beats Joint: On the Interplay between On-Policy Distillation and RLVR**|Boyan Li et.al.|[2609.04108v1](http://arxiv.org/abs/2609.04108v1)|null|
|**2026-09-03**|**Why Gated DeltaNet Survives 4-Bit Quantization: NVFP4 W4A4 for the Recurrent Half of a Hybrid 27B LLM**|Sergii Kozyrev et.al.|[2609.04098v1](http://arxiv.org/abs/2609.04098v1)|null|
|**2026-09-03**|**Adaptive Vision-Language Grasping via Composable Foundation Priors and Generalizable Grasp Synthesis**|Sixu Yan et.al.|[2609.04096v1](http://arxiv.org/abs/2609.04096v1)|null|
|**2026-09-03**|**DRACO: Fine-Grained Credit Assignment with Dynamic Rubrics for Long-Horizon Agent Training**|Shubham Gandhi et.al.|[2609.04094v1](http://arxiv.org/abs/2609.04094v1)|null|
|**2026-09-03**|**A Non-Formulable Theorem: A Fundamental Limit of Finite Syntactic Systems and Its Consequences for Security and AI**|Fabio F. G. Buono et.al.|[2609.04086v1](http://arxiv.org/abs/2609.04086v1)|null|
|**2026-09-03**|**CORE: Improving Compositional Reasoning in MLLM Embedding via Reranker Distillation**|Tingyu Song et.al.|[2609.04083v1](http://arxiv.org/abs/2609.04083v1)|null|
|**2026-09-03**|**TAP-Path: Task-Adaptive Structural and Token Pruning for Efficient and Trustworthy Pathology Foundation Models**|Mehedi Hasan et.al.|[2609.04071v1](http://arxiv.org/abs/2609.04071v1)|null|
|**2026-09-03**|**Subspace Inference Enables Efficient Active Reward Learning from Preferences**|Yutai Zhou et.al.|[2609.04066v1](http://arxiv.org/abs/2609.04066v1)|null|
|**2026-09-03**|**When Models Edit Too Much: On the Fidelity of Minimal Code Edits**|Tongyao Zhu et.al.|[2609.04061v1](http://arxiv.org/abs/2609.04061v1)|null|
|**2026-09-03**|**Translation as a Decision Space: A Multi-Agent Perspective on Low-Resource Dialect Generation**|Hasan Alkhder et.al.|[2609.04048v1](http://arxiv.org/abs/2609.04048v1)|null|
|**2026-09-03**|**The Dice Roll Method: A Standardized Protocol for Repeated-Query Auditing of Large Language Model Brand Recommendations**|Dmitrij Żatuchin et.al.|[2609.04047v1](http://arxiv.org/abs/2609.04047v1)|null|
|**2026-09-03**|**Editable Visual Design**|Junyan Ye et.al.|[2609.04034v1](http://arxiv.org/abs/2609.04034v1)|null|
|**2026-09-03**|**IRWOZ 2.0: A Large Language Model-driven Dialogue Dataset for Industrial Robot Conversations**|Chen Li et.al.|[2609.04030v1](http://arxiv.org/abs/2609.04030v1)|null|
|**2026-09-03**|**Influence of Extruded Filament Shape on Buildability in 3D Concrete Printing: A Geometry-Informed Deep Learning-FEM Approach**|Giacomo Rizzieri et.al.|[2609.04028v1](http://arxiv.org/abs/2609.04028v1)|null|
|**2026-09-03**|**Instruction Duplication as an Inference-Time Control Primitive**|Victor Lavrenko et.al.|[2609.04024v1](http://arxiv.org/abs/2609.04024v1)|null|
|**2026-09-03**|**Representational alignment yields generalizable safety in language models**|Lingyu Li et.al.|[2609.04022v1](http://arxiv.org/abs/2609.04022v1)|null|
|**2026-09-03**|**FLY-EVAL++: An Evidence-Driven Evaluation Protocol for Safety-Constrained Flight Prediction with Large Language Models**|Yalun Wu et.al.|[2609.04021v1](http://arxiv.org/abs/2609.04021v1)|null|
|**2026-09-03**|**InSituMeasure: Probing Situated Measurement Grounding in Industrial Scenes with Multimodal Large Language Models**|Chao Shen et.al.|[2609.04014v1](http://arxiv.org/abs/2609.04014v1)|null|
|**2026-09-03**|**LLM4CKD: Large Language Models for Early Stage Chronic Kidney Disease Screening**|Muhammad Ashad Kabir et.al.|[2609.04013v1](http://arxiv.org/abs/2609.04013v1)|null|
|**2026-09-03**|**The Blind Spot in 2D Infants' Pose Estimation:Robust Learning from Noisy Annotations**|Emanuele Cardinale et.al.|[2609.04009v1](http://arxiv.org/abs/2609.04009v1)|null|
|**2026-09-03**|**Catalogue Photography as a Cold Start: Toward Deployable Carbide Burr Recognition**|Abilash Philip Madavath et.al.|[2609.03995v1](http://arxiv.org/abs/2609.03995v1)|null|
|**2026-09-03**|**Alignment-Free Text-Audiobox for Voice Dubbing and Full-Duplex Dialogue Synthesis**|Sanyuan Chen et.al.|[2609.03992v1](http://arxiv.org/abs/2609.03992v1)|null|
|**2026-09-03**|**IchthyoNoma: Nomenclature and Context Sensitivity of Zero-Shot Biological Vision--Language Models for Bangladeshi Freshwater Fish Recognition**| Nazim-E-Alam et.al.|[2609.03985v1](http://arxiv.org/abs/2609.03985v1)|null|
|**2026-09-03**|**Investigating the Ability of Large Language Models to Analyze Recipes for Diabetes**|Revathy Venkataramanan et.al.|[2609.03967v1](http://arxiv.org/abs/2609.03967v1)|null|
|**2026-09-03**|**Interface-Induced Trajectory Censoring**|Wenbo Wang et.al.|[2609.03966v1](http://arxiv.org/abs/2609.03966v1)|null|
|**2026-09-03**|**FiMI Banking: A Sovereign Model for Indian Retail Banking**| NPCI AI Research Team et.al.|[2609.03960v1](http://arxiv.org/abs/2609.03960v1)|null|
|**2026-09-03**|**RARF: Region-Aware Rectified Flows for 3D Brain MRI Inpainting**|Tomas Guija-Valiente et.al.|[2609.03956v1](http://arxiv.org/abs/2609.03956v1)|null|
|**2026-09-03**|**Two-Stage Reinforcement Learning for Sound and Adversarial Test Generation in Code LLMs**|Jiacheng Xu et.al.|[2609.03955v1](http://arxiv.org/abs/2609.03955v1)|null|
|**2026-09-03**|**Beyond Majority Vote: Multi-Perspective Adjudication for Medical Hallucination Detection**|Joe Cecil et.al.|[2609.03953v1](http://arxiv.org/abs/2609.03953v1)|null|
|**2026-09-03**|**VestigeKV: The NoPE-MLA KV Cache Carries Its Own Eviction Signal in a Vestigial Branch**|WenJie Fan et.al.|[2609.03949v1](http://arxiv.org/abs/2609.03949v1)|null|
|**2026-09-03**|**Headroom-Drift Replay: A Primitive for Principled Replay Control in GRPO**|Hyun Bin Park et.al.|[2609.03941v1](http://arxiv.org/abs/2609.03941v1)|null|
|**2026-09-03**|**Masked Autoregressive Speech Enhancement with Continuous Neural Audio Codec Representations**|Yoto Fujita et.al.|[2609.03940v1](http://arxiv.org/abs/2609.03940v1)|null|
|**2026-09-03**|**RATL: Learning from Retrieved Residuals for Robust Multivariate Time-Series Forecasting**|Yuchen He et.al.|[2609.03937v1](http://arxiv.org/abs/2609.03937v1)|null|
|**2026-09-03**|**Fixed Suffix Dependency Ratio: Quantifying the Dual-Track Mechanism of Gender Assignment in Latvian Loanwords**|Yelingyun Zhang et.al.|[2609.03930v1](http://arxiv.org/abs/2609.03930v1)|null|
|**2026-09-03**|**Speak for Me: Giving LLMs the Situational Awareness to Participate in a Meeting**|Muneeb Khan et.al.|[2609.03923v1](http://arxiv.org/abs/2609.03923v1)|null|
|**2026-09-03**|**Value-Preserving Architectures for Agentic AI Systems**|Alessandro Pesare et.al.|[2609.03920v1](http://arxiv.org/abs/2609.03920v1)|null|
|**2026-09-03**|**RuleMem: Active Rule Memory for Long-Term Conversational Agents**|Xingyuan Zeng et.al.|[2609.03915v1](http://arxiv.org/abs/2609.03915v1)|null|
|**2026-09-03**|**CROCODIL: Cross-Model Code Editing with LLMs**|Linghan Zhong et.al.|[2609.03894v1](http://arxiv.org/abs/2609.03894v1)|null|
|**2026-09-03**|**GraFT: A Training-Free Framework for Spatial Reasoning in Multimodal Large Language Models via 3D Scene Graphs**|Junqing Du et.al.|[2609.03892v1](http://arxiv.org/abs/2609.03892v1)|null|
|**2026-09-03**|**FWBC-VLA: Force-Aware Whole-Body Compensation for Contact-Rich Loco-Manipulation**|Yutian Zhang et.al.|[2609.03889v1](http://arxiv.org/abs/2609.03889v1)|null|
|**2026-09-03**|**Beyond Shallow Alignment: How Post-Training Methods Determine Refusal Circuits And Steering Robustness**|Hoang Cuong Nguyen et.al.|[2609.03887v1](http://arxiv.org/abs/2609.03887v1)|null|
|**2026-09-03**|**A Blind Trust, the Bloody Thrust: When Attacker-Controlled Hook Updates Steer AI Agent Harnesses towards Malicious Behaviors**|Pengxun Li et.al.|[2609.03884v1](http://arxiv.org/abs/2609.03884v1)|null|
|**2026-09-03**|**Xiaomi-TabLDM: A Tabular Foundation Model Technical Report**|Xiaomi-TabLDM Team et.al.|[2609.03880v1](http://arxiv.org/abs/2609.03880v1)|null|
|**2026-09-03**|**Differentiable Interval Bottlenecks for Interpretable Anomaly Detection in Numerical Data**|Lamine Diop et.al.|[2609.03878v1](http://arxiv.org/abs/2609.03878v1)|null|
|**2026-09-03**|**STAIR (STructure Aware Information Retriever): A novel dataset and LLM based retriever for document structure augmentation**|Vineet Kumar et.al.|[2609.03874v1](http://arxiv.org/abs/2609.03874v1)|null|
|**2026-09-03**|**Bioinfoysis Technical Report**|Qingyang Shao et.al.|[2609.03871v1](http://arxiv.org/abs/2609.03871v1)|null|
|**2026-09-03**|**Adapting to Evolving Requirements: Agentic AI for Retail Supply Chain Operations**|Lei Zheng et.al.|[2609.03860v1](http://arxiv.org/abs/2609.03860v1)|null|
|**2026-09-03**|**Flip, Don't Shuffle: Watermarking LLMs at the Speed of Inference**|Simone Ceppi et.al.|[2609.03844v1](http://arxiv.org/abs/2609.03844v1)|null|
|**2026-09-03**|**Semantic Bayesian World Models**|Tommaso Soru et.al.|[2609.03834v1](http://arxiv.org/abs/2609.03834v1)|null|
|**2026-09-03**|**Select, Compress, Reinvest: A Controlled Study of Visual-Token Allocation in Long-Video MLLMs**|Prakhar Khatri et.al.|[2609.03820v1](http://arxiv.org/abs/2609.03820v1)|null|
|**2026-09-03**|**CauseCollab: Causal Unified and Modality-Agnostic Network for Heterogeneous Collaborative Perception**|Weize Li et.al.|[2609.03818v1](http://arxiv.org/abs/2609.03818v1)|null|
|**2026-09-03**|**Evaluating Criterion-Conditioned Behaviour of Large Language Models in Content Moderation**|Danting Zhang et.al.|[2609.03814v1](http://arxiv.org/abs/2609.03814v1)|null|
|**2026-09-03**|**VisCAD: A Foundation Model Suite with Multimodal Industrial CAD Intelligence**| JoyIndustrial VisCAD Team et.al.|[2609.03811v1](http://arxiv.org/abs/2609.03811v1)|null|
|**2026-09-03**|**Free Pause Tokens**|John Langford et.al.|[2609.03807v1](http://arxiv.org/abs/2609.03807v1)|null|
|**2026-09-03**|**SVG-Score: Human-Aligned Evaluation of Text-to-SVG Generation**|Marco Cipriano et.al.|[2609.03806v1](http://arxiv.org/abs/2609.03806v1)|null|
|**2026-09-03**|**Govern the Model, Not Only the Data: Storage, Circulation, and Learning in Creative AI**|Phoenix Perry et.al.|[2609.03800v1](http://arxiv.org/abs/2609.03800v1)|null|
|**2026-09-03**|**Transfiver: Human-AI Co-Inference through a Shared Editable State**|Minji Park et.al.|[2609.03797v1](http://arxiv.org/abs/2609.03797v1)|null|
|**2026-09-03**|**LLaDA-Image: Building Strong Image Generators with Fully Open Training Recipes**|Chuyan Chen et.al.|[2609.03796v1](http://arxiv.org/abs/2609.03796v1)|null|
|**2026-09-03**|**A Reverse Sign Language Dictionary: Open-Vocabulary Sign Recognition from Continuous Signing via Video Captioning and Description Retrieval**|Santiago Poveda-Gutiérrez et.al.|[2609.03788v1](http://arxiv.org/abs/2609.03788v1)|null|
|**2026-09-03**|**IndicSafeEval: Safety Robustness of Large Language Models under Multilingual Persuasive Jailbreak Attacks**|Saikat Mondal et.al.|[2609.03781v1](http://arxiv.org/abs/2609.03781v1)|null|
|**2026-09-03**|**Typological Feature Prediction with Large Language Models: An In-Context Learning Approach**|Qianwen Wang et.al.|[2609.03775v1](http://arxiv.org/abs/2609.03775v1)|null|
|**2026-09-03**|**Rethinking World Models for Safety-Critical Embodied Systems**|Kailang Ma et.al.|[2609.03774v1](http://arxiv.org/abs/2609.03774v1)|null|
|**2026-09-03**|**RealCADBench: Benchmarking Parametric CAD Modeling from Industrial Design Intents**| JoyIndustrial VisCAD Team et.al.|[2609.03773v1](http://arxiv.org/abs/2609.03773v1)|null|
|**2026-09-03**|**ENEAS: Embedding-guided Neural Ensemble for Adaptive Segmentation**|Javier del Pino et.al.|[2609.03756v1](http://arxiv.org/abs/2609.03756v1)|null|
|**2026-09-03**|**SimSkill: A Lifelong Learning AI Agent for Autonomous Mastery of Traffic Simulation**|Qi Liu et.al.|[2609.03753v1](http://arxiv.org/abs/2609.03753v1)|null|
|**2026-09-03**|**Beyond BLEU: A Case for Redefining Sign Language Translation Benchmarks**|Oline Ranum et.al.|[2609.03734v1](http://arxiv.org/abs/2609.03734v1)|null|
|**2026-09-03**|**Proactive Service Agents: A Unified Decision Framework, Methods, and Evaluation**|Yan Tang et.al.|[2609.03727v1](http://arxiv.org/abs/2609.03727v1)|null|
|**2026-09-03**|**Can LLMs Extract Architectural Design Decisions from Source Code Commits? - A Preliminary Exploratory Study**|Amey Karan et.al.|[2609.03721v1](http://arxiv.org/abs/2609.03721v1)|null|
|**2026-09-03**|**Opening mind by opening architecture: analysis strategies**|Francesco Vitucci et.al.|[2609.03719v1](http://arxiv.org/abs/2609.03719v1)|null|
|**2026-09-03**|**What Do CAE Simulation Agents Really Need Beyond a Generic Harness?**|Jiasheng Shi et.al.|[2609.03718v1](http://arxiv.org/abs/2609.03718v1)|null|
|**2026-09-03**|**Artificial Intelligence for Energy Optimization in Data Centers**|Mohammed Basharath Ullah et.al.|[2609.03716v1](http://arxiv.org/abs/2609.03716v1)|null|
|**2026-09-03**|**Counterfactual Routing Using Integer Programming with Constraint Generation**|Daniël Vos et.al.|[2609.03707v1](http://arxiv.org/abs/2609.03707v1)|null|
|**2026-09-03**|**Synthetic Semantic Supervision for Contrastive Code Representation Learning in Small Transformers: An Empirical Study**|Kenneth Paulsen et.al.|[2609.03702v1](http://arxiv.org/abs/2609.03702v1)|null|
|**2026-09-03**|**Symmetries and Causality: Causal Effect Identification Beyond IID Data**|Martin Rabel et.al.|[2609.03697v1](http://arxiv.org/abs/2609.03697v1)|null|
|**2026-09-03**|**A Circuit for Plural Reference: How LLMs Represent and Retrieve Singular and Plural Entities**|Anh Danh et.al.|[2609.03687v1](http://arxiv.org/abs/2609.03687v1)|null|
|**2026-09-03**|**Understanding Autonomous Driving Datasets by Describing Differences between Image Subsets in Natural Language**|Julian Truetsch et.al.|[2609.03677v1](http://arxiv.org/abs/2609.03677v1)|null|
|**2026-09-03**|**Out-of-Distribution Generalisation with Sequence Models in Offline Multi-Agent Reinforcement Learning**|Oussama Hidaoui et.al.|[2609.03667v1](http://arxiv.org/abs/2609.03667v1)|null|
|**2026-09-03**|**Cross-Dataset Transfer and Reliability of Explainable Artificial Intelligence for RhythmFormer Remote Photoplethysmography**|Louis Chen et.al.|[2609.03663v1](http://arxiv.org/abs/2609.03663v1)|null|
|**2026-09-03**|**Local Updates, Global Learning (LUGL): Playing Games with non-incremental Learners**|David Milec et.al.|[2609.03660v1](http://arxiv.org/abs/2609.03660v1)|null|
|**2026-09-03**|**Enhancing Financial Question Answering: A Novel Benchmark Dataset of Banks' financial statements**|Arianna Miola et.al.|[2609.03654v1](http://arxiv.org/abs/2609.03654v1)|null|
|**2026-09-03**|**The Impact of Synthetic Data Augmentation on Discourse-Pragmatic Function Classification**|Sara Sorahi et.al.|[2609.03652v1](http://arxiv.org/abs/2609.03652v1)|null|
|**2026-09-03**|**Analysis of Prompt Engineering for Drug Toxicity Prediction**|Mia MacGregor et.al.|[2609.03635v1](http://arxiv.org/abs/2609.03635v1)|null|
|**2026-09-03**|**</think> Doesn't Stop Reasoning: Analysis of Spurious CoT Termination**|Seunghee Koh et.al.|[2609.03633v1](http://arxiv.org/abs/2609.03633v1)|null|
|**2026-09-03**|**EraseSAE: Surgical Concept Erasure in Text-to-Video Diffusion Models via Sparse Autoencoders**|Xinghao Wang et.al.|[2609.03629v1](http://arxiv.org/abs/2609.03629v1)|null|

#### Abstracts
##### **Compile by Training: Turning Natural-Language Specifications into Local Neural Functions**
2609.04199v1 by Yuntian Deng, Pengyu Nie, Stuart Shieber

Many recurring text functions are easy to describe but difficult to implement with rules, while calling a large remote model for every input introduces repeated cost, latency, and dependency on a provider. We present compile by training, which turns a natural-language specification into a reusable neural function. At compile time, teacher models generate task-specific examples that are used to train a small adapter for a compact interpreter. The resulting function runs without the teachers and can be stored, versioned, and composed like ordinary software. On FuzzyBench-Hard, a subset on which the Program-as-Weights fast compiler produced no exact matches, compile by training reaches 83.6% semantic accuracy. This higher accuracy comes with a higher compile-time cost: roughly a minute rather than seconds for the fast compiler. We deploy the compiler in a public interactive service and demonstrate compiled functions in a multi-site website helper, a language-controlled 3D avatar, and a bidirectional English-Claudish translator.

摘要：許多重複的文本功能容易描述，但用規則實現卻困難，而對每個輸入調用大型遠程模型則會引入重複的成本、延遲以及對供應商的依賴。我們提出了通過訓練進行編譯，這將自然語言規範轉換為可重用的神經功能。在編譯時，教師模型生成特定任務的示例，這些示例用於訓練一個小型適配器，以便用於緊湊的解釋器。所得到的功能在沒有教師的情況下運行，並且可以像普通軟體一樣存儲、版本控制和組合。在 FuzzyBench-Hard 上，這是一個 Program-as-Weights 快速編譯器未產生精確匹配的子集，通過訓練進行編譯達到了 83.6% 的語義準確率。這一更高的準確率伴隨著更高的編譯時間成本：大約需要一分鐘，而不是快速編譯器的幾秒鐘。我們在一個公共互動服務中部署了編譯器，並在多站點網站助手、語言控制的 3D 虛擬形象和雙向英語-克勞迪什翻譯器中展示了編譯功能。

##### **Clean Engineering, Unstable Measurement: A Preregistered Reliability Failure of Black-Box LLM Observers on Shared Endpoints**
2609.04198v1 by Haoyaun Zhu, Jie Zhang

Language-model judges now gate training data, score generations, and drive leaderboards. The judge is then a measurement instrument, resting on one rarely stated assumption: the same request, sent to the same model name, reads the same tomorrow. We audited that assumption in two preregistered campaigns with every threshold fixed in advance; neither got past validating its instrument. Across 52,988 audited request attempts, same-window repeat rankings agreed at Spearman 0.400 against a required 0.90, and byte-identical next-day replays agreed at 0.78 against a required 0.99, each time with the execution record at ceiling. Three mechanisms explain the gap: a label-to-meaning mapping that biased readouts as strongly as the signal; candidate gaps seven orders of magnitude below the instrument's own noise floor; and byte-identical inputs returning different rankings, a noise that exact-permutation readouts compound. Neither metric substitution nor sampling repaired it on the tested grid. Preregistered follow-ups bound the problem: waiting did not help on the days sampled (0.805 versus 0.800, replicated over five further days); switching providers did not help (four providers share the floor, medians 0.74 to 0.88, predicted by none of the metadata fields they expose); self-hosting on batch-invariant kernels helped only while the server was quiet; and on constructed errors with known gaps, the readout's separation tracks error type, not size. We distill the evidence into a three-level snapshot-identity ladder, eight design rules, and a reporting checklist; a pilot at roughly 2% of the study's call volume would have exposed both unreachable gates in advance. All results concern externally measured behaviour on shared serving infrastructure. On a shared endpoint, a model name is not a frozen instrument; a preregistered evaluation must measure its instrument before freezing any gate on it.

摘要：語言模型評判現在控制訓練數據、評分生成結果並推動排行榜。評判因此成為一種測量工具，基於一個鮮少被提及的假設：相同的請求，發送到相同的模型名稱，明天的結果應該是相同的。我們在兩個預註冊的活動中審核了這一假設，所有閾值均提前固定；但都未能通過驗證其工具。在52,988次審核的請求嘗試中，同窗口重複排名的斯皮爾曼相關係數為0.400，低於所需的0.90，而字節完全相同的次日重播的相關係數為0.78，低於所需的0.99，每次執行記錄均達到上限。三種機制解釋了這一差距：一種標籤到意義的映射，強烈偏向讀數；候選差距低於工具自身噪聲底線七個數量級；以及字節完全相同的輸入返回不同的排名，這種噪聲使得精確排列的讀數更為複雜。無論是指標替代還是抽樣都未能修復在測試網格上的問題。預註冊的後續研究界定了問題：在抽樣的日子中等待並未有所幫助（0.805對比0.800，並在五天內重複）；更換提供者也未能改善（四個提供者共享同一基準，中位數在0.74到0.88之間，無一由其所公開的元數據字段預測）；在批次不變的內核上自我托管僅在伺服器安靜時有所幫助；而在已知差距的構造錯誤中，讀數的分離跟踪的是錯誤類型，而非大小。我們將證據提煉為三層快照身份梯度、八條設計規則和一個報告檢查表；在大約2%的研究呼叫量下進行的試點將提前揭示兩個無法達到的閘門。所有結果均涉及在共享服務基礎設施上外部測量的行為。在共享端點上，模型名稱並不是一個固定的工具；預註冊的評估必須在固定任何閘門之前測量其工具。

##### **ESPO: Error-Structured Prompt Optimization via Diagnose, Diversify, and Stabilize**
2609.04197v1 by Lihao Liu, Peng Tang, Kunwar Yashraj Singh, Shabnam Ghadar

Evolutionary prompt optimizers such as GEPA suffer from prompt bloat: each iteration appends rules and caveats, producing prompts up to 3$\times$ longer yet no more accurate. We trace this to three deficiencies - incomplete error observation, limited search diversity, and unreliable selection - and propose ESPO (Error-Structured Prompt Optimization), which decomposes prompt optimization into three phases: Diagnose clusters all training errors into structural patterns in one round; Propose generates candidates via four complementary strategies with independent biases; Select applies bootstrap stability selection. On seven public NLP benchmarks - Tweet, MMLU, GSM8K, HotpotQA, ScoNe, HoVer, and PUPA - ESPO improves average accuracy by $+$3.76 pp over the state-of-the-art (74.67% vs 70.91% for GEPA), matching or exceeding GEPA on every dataset while producing prompts 47% shorter (1,004 vs 1,878 chars) and faster at inference. Cross-model experiments across four additional student models (Gemma 3 12B, Mistral 14B, Qwen3 32B, Claude Haiku 4.5) show ESPO yields the best average accuracy on every model tested, with the largest gap on Qwen3 GSM8K (15.00% $\to$ 91.40%). A generalization bound (Appendix) grounds each phase in a corresponding term of the test-time gap, and the ablation confirms a key prediction: adding diversity without bootstrap selection actually hurts performance ($-$1.20%).

摘要：進化式提示優化器如 GEPA 遭遇提示膨脹的問題：每次迭代都會附加規則和注意事項，產生的提示長度最多增加 3$\times$，但準確性卻沒有提高。我們將此問題歸因於三個缺陷——不完整的錯誤觀察、有限的搜索多樣性以及不可靠的選擇——並提出 ESPO（錯誤結構提示優化），它將提示優化分解為三個階段：診斷將所有訓練錯誤整合為結構模式；提出通過四種互補策略生成候選者，並具有獨立的偏見；選擇應用自助穩定性選擇。在七個公共 NLP 基準測試上——Tweet、MMLU、GSM8K、HotpotQA、ScoNe、HoVer 和 PUPA——ESPO 的平均準確性比最先進的方法提高了 $+$3.76 pp（74.67% 對 70.91% 的 GEPA），在每個數據集上均達到或超過 GEPA，同時生成的提示長度縮短了 47%（1,004 對 1,878 字符），推理速度也更快。在四個額外的學生模型（Gemma 3 12B、Mistral 14B、Qwen3 32B、Claude Haiku 4.5）之間的跨模型實驗顯示，ESPO 在每個測試模型上都產生了最佳的平均準確性，最大差距出現在 Qwen3 GSM8K（15.00% $\to$ 91.40%）。一個泛化界限（附錄）將每個階段與測試時間差的相應項目相結合，而消融實驗確認了一個關鍵預測：在沒有自助選擇的情況下增加多樣性實際上會損害性能（$-$1.20%）。

##### **Legibility is Not Interpretability: Comparing Judged and Actual Importance in Chain-Of-Thought Reasoning**
2609.04194v1 by Kevin Du, Alexander Hoyle, Laura Ruis, Acyr Locatelli

Reasoning traces from chain-of-thought models appear to offer a legible window into how a model arrives at its answer. A growing body of work treats them as such, using LLM judges to diagnose errors, evaluate faithfulness, and provide step-level supervision via process reward models and generative critics. These practices rely on the text of a reasoning step carrying information about its functional role. But does the text actually encode information about which reasoning steps matter? We operationalize the importance of a reasoning step as its advantage: the change in expected reward, e.g., producing the correct final answer, from including that step, estimated via Monte Carlo rollouts. Basing ground truth on these estimates, we evaluate whether LLM judges can identify high-advantage steps and find that sufficiently capable LLMs can outperform a prevalence baseline but fall well short of a noise ceiling. Fine-tuning a model as a step-level critic yields strong improvement for incorrect responses but remains distant from ceiling for correct responses, suggesting that step importance is only partially recoverable from the text of the reasoning trace. Our findings contribute to a growing body of chain-of-thought faithfulness work that cautions against treating the legibility of reasoning traces as interpretability, especially with implications for process reward modeling.

摘要：推理痕跡來自思維鏈模型，似乎提供了一個清晰的窗口，讓人了解模型如何得出其答案。越來越多的研究將其視為如此，利用大型語言模型（LLM）評估者來診斷錯誤、評估忠實性，並通過過程獎勵模型和生成性批評者提供步驟級的監督。這些做法依賴於推理步驟的文本攜帶有關其功能角色的信息。但文本是否實際上編碼了哪些推理步驟重要的信息？我們將推理步驟的重要性操作化為其優勢：即包含該步驟所帶來的預期獎勵的變化，例如，產生正確的最終答案，這一變化通過蒙特卡羅模擬來估算。基於這些估算的真實基準，我們評估LLM評估者是否能夠識別高優勢步驟，並發現足夠強大的LLM可以超越普遍基準，但仍然遠未達到噪音上限。將模型微調為步驟級批評者對於不正確的回答帶來了顯著改善，但對於正確的回答仍然距離上限較遠，這表明步驟的重要性僅部分可以從推理痕跡的文本中恢復。我們的發現為日益增長的思維鏈忠實性研究做出了貢獻，該研究對將推理痕跡的清晰性視為可解釋性提出了警告，尤其是在過程獎勵建模的影響方面。

##### **Seeing Before Synthesizing: VLM-Guided Transition Event Discovery for Weakly-Supervised Dense Video Captioning**
2609.04183v1 by Ye-Chan Kim, Seunghee Choi, SeungJu Cha, Si-Woo Kim, Hwiseon Kim, Hyungee Kim, Dong-Jin Kim

Weakly-Supervised Dense Video Captioning aims to localize and describe multiple events in untrimmed videos given only an ordered set of event-level captions per video. Recent work synthesizes auxiliary transition captions via LLM to provide additional vision-language alignment, but these captions lack visual grounding and are rigidly assigned to every inter-event gap at a fixed location and duration. To address these, we propose Seeing Before Synthesizing (SBS), a framework that adaptively provides visually grounded linguistic guidance only where warranted. Leveraging a VLM, we generate frame-level narratives for the inter-event gaps and detect transitions from the semantic variation across them. For identified transitions, we then refine inter-event temporal masks by blending the temporal midpoint with the semantic change point and selecting the width that maximizes vision-language alignment. Experiments on ActivityNet Captions and YouCook2 demonstrate state-of-the-art performance in both captioning and localization.

摘要：弱監督密集視頻標題生成旨在根據每個視頻的有序事件級標題集，定位並描述未剪輯視頻中的多個事件。最近的研究通過LLM合成輔助過渡標題，以提供額外的視覺-語言對齊，但這些標題缺乏視覺基礎，並且在固定的位置和持續時間內僵硬地分配給每個事件間隙。為了解決這些問題，我們提出了“先看再合成”（SBS），這是一個框架，根據需要自適應地提供視覺基礎的語言指導。利用VLM，我們為事件間隙生成幀級敘述，並檢測它們之間的語義變化所帶來的過渡。對於已識別的過渡，我們通過將時間中點與語義變化點混合來改進事件間的時間掩碼，並選擇最大化視覺-語言對齊的寬度。在ActivityNet Captions和YouCook2上的實驗顯示，在標題生成和定位方面達到了最先進的性能。

##### **Knowledge Acquisition During Pre-training? Large Language Models Learn Better With Auxiliary Views**
2609.04180v1 by Joseph Lee, Yidi Huang, Dokyoon Kim, Shu Yang, Li Shen

Gaps remain in our understanding of how large language models (LLMs) acquire knowledge during pre-training. We posit that auxiliary views, reformulations of knowledge, are causally helpful for learning. We design controlled experiments to isolate this. First, we confirm that repetition is necessary for acquisition and clarify that paraphrasing helps only at smaller batch sizes. Second, holding the token budget fixed, allocating tokens from document repetition to auxiliary views improves learning, counterintuitively, even for factual recall. Third, the effectiveness of auxiliary views is not contingent on the strength of the teacher model that generates them. Fourth, we identify forms of knowledge, contextual and foundational, that aid learning in the presence of prior knowledge gaps. Finally, we examine how these effects manifest mechanistically via layer-wise biases and compression. Together, our findings suggest that auxiliary representations of knowledge, which arise naturally in large pre-training corpora, are a key factor in the success of pre-training and offer a plausible explanation for why data diversity matters.

摘要：在我們對大型語言模型（LLMs）在預訓練期間如何獲取知識的理解中仍然存在空白。我們假設輔助視角，即知識的重述，對學習有因果上的幫助。我們設計了控制實驗來隔離這一點。首先，我們確認重複對於獲取是必要的，並澄清重述僅在較小的批次大小中有幫助。其次，在固定的標記預算下，將來自文檔重複的標記分配給輔助視角，反而會改善學習，即使在事實回憶方面也是如此，這看似違反直覺。第三，輔助視角的有效性並不依賴於生成它們的教師模型的強度。第四，我們識別出幾種知識形式，包括上下文知識和基礎知識，有助於在存在先前知識空白的情況下進行學習。最後，我們檢查這些效果如何通過層級偏差和壓縮在機械上表現出來。綜合來看，我們的發現表明，知識的輔助表徵，這些表徵在大型預訓練語料庫中自然出現，是預訓練成功的關鍵因素，並提供了一個合理的解釋，說明為什麼數據多樣性是重要的。

##### **A Computationally Feasible Framework for Causal Probabilistic Explanation**
2609.04177v1 by Rafal Urbaniak, Sam Witty, Daniel Waxman, Andy Zane, Poorva Garg, Emily Bunnapradist, Sankaran Vaidyanathan, Jack Feser, Drew Lehe, Eli Bingham

Explaining why a specific outcome occurred, and which inputs deserve the blame or credit, is central to philosophical, scientific, and policy analysis. Existing tools split into two camps. The theory of actual causality (AC) gives principled verdicts, but only for toy-sized models, because computing them requires enumerating counterfactual scenarios. Scalable attribution methods like SHAP (or even causal SHAP) at least partially ignore the causal structure that generated the data, and can give answers that conflict with a careful causal analysis. We close this gap with Probabilistic Causal Impact (PCI).
  PCI builds on actual causality and on Pearl's notions of probability of necessity and sufficiency, but recasts the question of explainability as an estimation problem on a probabilistic causal model that is easily approximated via Monte Carlo. By specifying a distribution over "candidate explanations," a distribution over counterfactual values, and a scoring function, PCI provides tractable, causally grounded, graded explanations, generalizing AC and Pearl's probability of causation as degenerate cases.
  We evaluate PCI in synthetic and real-world examples, spanning consistency checks with AC, scaling experiments, complex continuous-valued dynamical systems, and a real-world deployed causal machine learning model trained on millions of datapoints.

摘要：解釋為什麼特定結果發生，以及哪些輸入應該受到責備或讚譽，對於哲學、科學和政策分析至關重要。現有工具分為兩派。實際因果性理論（AC）給出原則性的判決，但僅限於玩具大小的模型，因為計算它們需要列舉反事實情境。可擴展的歸因方法如SHAP（甚至因果SHAP）至少在某種程度上忽略了生成數據的因果結構，並可能給出與仔細的因果分析相矛盾的答案。我們用概率因果影響（PCI）來填補這一空白。<lb/>  
PCI建立在實際因果性和Pearl的必要性與充分性概率概念之上，但將可解釋性的問題重新表述為在概率因果模型上的估計問題，該模型可以通過蒙特卡羅方法輕鬆近似。通過指定“候選解釋”的分佈、反事實值的分佈和評分函數，PCI提供了可處理的、基於因果的、分級的解釋，將AC和Pearl的因果概率概括為退化情況。<lb/>  
我們在合成和現實世界的例子中評估PCI，涵蓋與AC的一致性檢查、擴展實驗、複雜的連續值動態系統，以及一個在數百萬數據點上訓練的現實世界部署的因果機器學習模型。

##### **Last Translation Benchmark**
2609.04173v1 by Vilém Zouhar, Niyati Bafna, Mukund Choudhary, Maike Züfle, Sara Rajaee, Pinzhen Chen, Jannis Vamvas, Sara Papi, Ona de Gibert, Bhavitvya Malik, Eliya Habba, Orfeas Menis Mastromichalakis, Patrícia Schmidtová, Michelle Wastl, Sheriff Issaka, Leshem Choshen, Stella Biderman, Antonis Anastasopoulos, Jan Niehues, Rico Sennrich, Mrinmaya Sachan, Ondřej Bojar, Kenton Murray, Jörg Tiedemann, Alham Fikri Aji, Philipp Koehn, Christof Monz, Alexandra Birch, Sowmya Vajjala, Chalamalasetti Kranti, Cristina España-Bonet, Nobin Sarwar, David Kaczér, Shunta Asano, Malik Marmonier, Daban Q. Jaff, Vaisakhi Mishra, Hend Al- Khalifa, Gabriele Sarti, Sourajit Saha, Nils Rehlinger, Juan Daniel Cuervo Villa, Jonathan Tonglet, Saugata Purkayastha, Dominik Macháček, Jagannathan Ramanujam, Heejin Do, Zuzana Nadova, Fred Philippy, Fabian Retkowski, Maria Lymperaiou, Silvia Casola, Hanna Yukhymenko, Shubhashis Roy Dipta, Sangwon Ryu, Andrés Jerez, Ron Keinan, Shuaib Shuaib Yusuf, Avantica Vempati, Maria Carmen Staiano, Sukannya Purkayastha, Adrian Cosma, Vitalii Babenko, Erivan Inan, Aviral Nigam, Wafa Aissa, Fatima Haouari, Venkata Prasanth Kumar Gummadi, Mehdi Jafarzadeh, Valentin Scourneau, Lukas Edman, Kaiser Sun, Shaomu Tan, Mohammad Sadegh Gholizadeh, Johannes-Rudolf David, Dipankar Srirag, Javier García Gilabert, Ruta Binkyte, Manar Ali, Ana-Maria Bucur, Sabry E. Farrag, Youssef Saber, Yihong Liu, Jean Maillard, Cojocaru Nicoleta, Xiaochuang Yuan, Sina Ahmadi, Philipp Mondorf, Kaustubh Dhole, Roman Wixinger, Shenbin Qian, Manuel Tuor, Sergey Troshin, Jonathan Yahav, Fida Mohammad Thoker, Amir Arsalan Rezapour, Lance Calvin Lim Gamboa, Manon Reusens, Kätriin Kukk, Koel Dutta Chowdhury, Giuseppe Gallipoli, Christian Hoang, Shaswati Saha, Seth Aycock, Jan Kocoń, Bo Chen, Linh Vu, Vatsal Venkatkrishna, Arafat Ahsan, Luan Thanh Nguyen, Hassan Soliman, Daryna Dementieva, Theresia Veronika Rampisela, Ngoc Quynh Tram Do, Marius Huber, Kazuki Egashira, Azmine Toushik Wasi, Vladislav Poritski, Mike Zhang, Deep Shah, Paul Gavrikov, Luis Frentzen Salim, David Africa, R. Damanhuri, Bello Umar Bello, Anumit Garg, Gengyu Rao, Pawan Sasanka Ammanamanchi, Kamile Dementaviciute, Andrianos Michail, L D M S Sai Teja, Dawei Zhu, Yi Fan, Wei Liu, Farhan Farsi, Elias Herranen, Sankalan Pal Chowdhury, Karen Sanchez, Farzad Shami, Ashok Urlana, Zimu Wang, Tomasz Limisiewicz, Priyaranjan Pattnayak, Marii Ojastu, Hongbin Na, Emilian Radoi, Chenyi Zhao, Carlos Hinojosa, Andrea Gregor de Varda, Zaid Alyafeai, Reem Alzahrani, Nehal Kathrotia, Alex Flückiger, Ulysses Sekai Tully Carr, Jimson Paulo Layacan, Guy Kaplan, Ritwik Tiwari, Rishit Dagli, Oksana Volchek, Isaac R Caswell, Bowen Yi, Blanka Kövér, Amir Hossein Yari, Aicha Chorana, Zhengxiang Wang, Selja Keränen, Samuel Simko, Joy Olusanya, Jenny Chim, Enzo Doyen, Vivek Harsha Lakkamaneni, Sophia Conrad, Pouya Sadeghi, Panayiotis Panayiotou, Luis Lara, Jannatul Nayem, Eran Yahav, Debanshu Das, Antonia Karamolegkou, Anmol Goel, Aishik Mandal, Tommaso Cerruti, Raoyuan Zhao, Mykola Haltiuk, Thura Aung, Naser Almousa, Amir Hossein Kargaran, Rachel Bawden, Qiaoyuan Zheng, Mateusz Lango, Beni Egressy, Fidel Rodríguez Velásquez, Natchapon Jongwiriyanurak, Minh Ngoc Do, Marco Gaido, Lena Libon, Dzmitry Kuzmin, Badal Nyalang, Antoine Taroni, Andrei Niculae, Abdulaziz Nura Kani, Rushikesh Zawar, Marek Šuppa, Beatrice Savoldi, Andreas Simons, Rayyan Merchant, Ilai Yaron Levy, Francesco Pinto, Ziyi Yang, Yolanda Xavier, Samuel Frontull, Muhammad Ravi Shulthan Habibi, Kenneth Enevoldsen, Harris Abdul Majid, Francesca Padovani, Tim Graf, Tatiana Bielakova, Sharifa Djurabaeva, Shaoxiong Ji, Raia Abu Ahmad, Pavel Stepachev, Jirui Qi, Ayush Sunil Munot, Alireza Pakniat, Ayla Rigouts Terryn, Yuxing Lu, Yurii Paniv, Xiyan Fu, Tosin Adewumi, Sunisth Kumar, Stéphane J. P. S. Thunus, Shree Harsha Bokkahalli Satish, Shayan Bali, Prakhar Gupta, Papa Abdou Karim Karou Diallo, Matija Akrap, Marko Culjak, Kristýna Onderková, Joseph Attieh, Esrael Teferi Tensay, Elisabeth Fittschen, Benoît Sagot, Jingwei Ni, Yu Fan

For scientific progress, we need benchmarks that test the limits of state-of-the-art models, and evaluation methods that inform us about failure cases. As models get stronger, standard benchmarks for machine translation are approaching saturation. Further, automatic translation metrics are unreliable, vulnerable to reward-hacking, and provide unactionable assessments. Even gold human evaluation is not problem-free, because it often lacks reproducibility, objectivity, and scalability. Overall, this prevents us from tracking objective progress in the field and identifying pathways for improvement. We introduce the Last Translation Benchmark, a collection of human-authored and peer-reviewed examples (texts, images, audio, videos) that break leading machine translation models. We also present a new evaluation approach: each example comes with handcrafted verification rules describing concrete failure cases on that example, therefore allowing reliable and actionable future evaluation. The Last Translation Benchmark is a live dataset that accepts ongoing contributions. The latest version is LTBv1, containing accepted contributions prior to September 1st 2026, with future releases planned as new data is continuously collected.

摘要：為了科學進步，我們需要測試最先進模型極限的基準，以及告訴我們失敗案例的評估方法。隨著模型變得更強大，機器翻譯的標準基準正接近飽和。此外，自動翻譯指標不可靠，容易受到獎勵操控的影響，並且提供不可行的評估。即使是金標準的人類評估也並非沒有問題，因為它往往缺乏可重複性、客觀性和可擴展性。總的來說，這妨礙了我們在該領域跟踪客觀進展和識別改進路徑。我們介紹了最後翻譯基準，這是一個由人類撰寫和同行評審的範例集合（文本、圖像、音頻、視頻），能夠突破領先的機器翻譯模型。我們還提出了一種新的評估方法：每個範例都附有手工編寫的驗證規則，描述該範例的具體失敗案例，因此允許可靠且可行的未來評估。最後翻譯基準是一個持續接受貢獻的實時數據集。最新版本是LTBv1，包含在2026年9月1日之前接受的貢獻，未來的版本計劃在新數據持續收集的情況下發布。

##### **Rethinking On-Policy Distillation of Large Language Models II: One Training Example**
2609.04172v1 by Zixuan Fu, Bingxiang He, Yuxin Zuo, Haohuan Huang, Jinqian Zhang, Ruhang Xiao, Cheng Qian, Qinyu Luo, Huan-ang Gao, Yudong Wang, Zhiyuan Liu, Ning Ding, Chaojun Xiao

On-policy distillation (OPD) combines student-generated rollouts with dense token-level supervision from a teacher. Existing work has mainly studied its algorithmic behavior, leaving the role of training data unclear. We examine this role at the data-minimal limit by training on a single query. One-shot OPD keeps improving for hundreds of steps and recovers most of full-data OPD's gain across task domains and model families. We explain this result through the states visited during training and the rate at which the student aligns with the teacher. We measure \emph{state coverage}, the fraction of the states full-data OPD visits that a query set's rollouts reach. A single query already reaches \(71.5\%\), most of it within the first 100 steps. Adding semantically distinct queries raises coverage and validation accuracy together, until 16 queries reach \(98.9\%\) and match full-data training. Yet alignment slows at a similar pace whether OPD trains on one query or the whole dataset, and even a fixed set of states takes hundreds of steps to absorb. OPD is therefore data-overfed but algorithm-starved. Its rollouts quickly expose broad supervision, while the student absorbs that supervision increasingly slowly. The state-coverage result extends to multi-teacher OPD, where 16 semantically diverse queries per domain match full-data MOPD. As a further stress test, content-light templates and off-domain WildChat queries also approach the real-query baseline. Task content and induced state coverage can therefore come apart. We hope these findings direct future work toward the step efficiency of OPD, and prompt a re-examination of the data and the mechanisms behind its recent successes in frontier post-training.

摘要：在政策蒸餾（OPD）中，學生生成的回合與教師的密集標記級監督相結合。現有的研究主要探討其算法行為，對訓練數據的角色尚不明確。我們通過在單一查詢上進行訓練來檢視這一角色，並達到數據最小的極限。一次性 OPD 在數百步內持續改進，並在各個任務領域和模型家族中恢復了大部分全數據 OPD 的增益。我們通過訓練過程中訪問的狀態以及學生與教師對齊的速度來解釋這一結果。我們測量了 \emph{狀態覆蓋率}，即全數據 OPD 訪問的狀態中，查詢集的回合達到的狀態比例。單一查詢已經達到了 \(71.5\%\)，其中大部分在前 100 步內完成。添加語義上不同的查詢會同時提高覆蓋率和驗證準確度，直到 16 個查詢達到 \(98.9\%\) 並匹配全數據訓練。然而，無論 OPD 是在一個查詢上訓練還是整個數據集，對齊的速度都會減緩，即使是一組固定的狀態也需要數百步才能吸收。因此，OPD 是數據過度供應但算法匱乏的。它的回合迅速暴露出廣泛的監督，而學生吸收這種監督的速度卻越來越慢。狀態覆蓋率的結果擴展到多教師 OPD，在每個領域中 16 個語義多樣的查詢匹配全數據 MOPD。作為進一步的壓力測試，內容輕量的模板和非領域的 WildChat 查詢也接近真實查詢基準。因此，任務內容和誘導的狀態覆蓋率可能會分開。我們希望這些發現能指導未來的研究朝向 OPD 的步驟效率，並促使重新檢視數據及其在前沿後訓練中近期成功背後的機制。

##### **A Case Study on Emergent Cheating and Whistleblowing in Autonomous Research Swarms**
2609.04170v1 by Davide Paglieri, Logan Cross, Tim Genewein, Joel Z. Leibo, Nenad Tomasev, Alexander Sasha Vezhnevets

Multi-agent AI science ecosystems rely on agents possessing tools that allow them to communicate, coordinate, and build on each other's work. Yet this shared infrastructure can also introduce vulnerabilities by creating a substrate for the contagious spread of unintended and undesirable behaviors. We report a case study on a research collective of 100 autonomous LLM agents tasked with proving formal mathematical conjectures. Within the swarm, cheating spontaneously emerged and was later challenged by whistleblowers - both without any external intervention. When a single agent discovered an exploit in the evaluation system, it propagated across the collective via a shared knowledge library and later through peer-to-peer messages. Despite early reluctance, a cohort of agents adopted the exploit in response to competitive pressure. A separate group of agents produced an emergent counter-response: auditing fraudulent proofs, alerting peers across broadcast and private channels, staging boycotts, lodging formal complaints, and proposing validation patches. In recent incidents, agent swarms coordinated covertly through improvised side-channels (Dalton and Wallace, 2026; Greenblatt et al., 2026). Our setting differs: the same transparent channels that carried the exploit also gave non-cheating agents the visibility they needed to detect fraud, organize resistance, and enforce norms. We cast the problem of managing the agents' shared infrastructure as the knowledge commons governance problem (Ostrom, 1990). To protect the commons from exploits, we propose to adopt institutional mechanisms, such as graduated sanctioning and collective-choice rules, to support decentralized self-governance in autonomous swarms.

摘要：多代理人工智慧科學生態系統依賴於擁有工具的代理人，這些工具使他們能夠溝通、協調並在彼此的工作上進行建設。<lb/>然而，這種共享基礎設施也可能通過創造一個潛在的環境，引入脆弱性，導致意外和不良行為的傳染性擴散。<lb/>我們報告了一個案例研究，該研究涉及一個由100個自主大型語言模型代理組成的研究集體，任務是證明正式的數學猜想。<lb/>在這個群體中，作弊自發出現，並且後來被舉報者挑戰——這一切都是在沒有任何外部干預的情況下發生的。<lb/>當一個代理人發現評估系統中的漏洞時，它通過共享知識庫在集體中傳播，後來又通過點對點消息進行傳遞。<lb/>儘管最初有些猶豫，但一組代理人在競爭壓力下採用了這一漏洞。<lb/>另一組代理人則產生了一種新興的反應：審計欺詐證明，通過廣播和私密渠道警告同伴，舉行抵制，提出正式投訴，並提議驗證補丁。<lb/>在最近的事件中，代理群體通過即興的側通道進行了秘密協調（Dalton 和 Wallace, 2026; Greenblatt 等, 2026）。<lb/>我們的環境有所不同：同樣的透明渠道不僅傳遞了漏洞，還給予了非作弊代理所需的可見性，以檢測欺詐、組織抵抗並執行規範。<lb/>我們將管理代理人共享基礎設施的問題視為知識公地治理問題（Ostrom, 1990）。<lb/>為了保護公地免受漏洞的侵害，我們建議採用制度性機制，例如逐級制裁和集體選擇規則，以支持自主群體的去中心化自我治理。

##### **SWE-Gate: Passing Functional Tests Is Not Enough for Software Engineering Agents**
2609.04167v1 by Xin He, Yanlin Wang, Mingwei Liu, Jiachi Chen, Hongyu Zhang, Guanbin Li

Repository-level software engineering benchmarks have significantly advanced the evaluation of coding agents, but existing benchmarks primarily measure whether generated patches pass functional tests and overlook review-derived acceptance constraints (review constraints) that often influence whether a patch is acceptable in real-world software development. We introduce SWE-Gate, a repository-level benchmark for software engineering agents that explicitly evaluates review constraint compliance alongside functional correctness. SWE-Gate derives review constraints from real pull request review comments and synthesizes repository-level repair instances around these constraints. Each instance provides separate functional and constraint tests, together with non-compliant and gold patches, enabling explicit separation between issue resolution capability and review constraint compliance. We construct SWE-Gate with 303 repository-level repair instances spanning 75 open-source Python repositories across diverse software domains. Experiments with four LLM backends spanning different capability levels under a common coding-agent scaffold reveal a substantial gap between functional success and success under the complete repair specification: among 644 repairs that pass the functional tests, 221 fail to satisfy the provided review constraints. These findings show that functional-only evaluation overestimates agents' ability to satisfy the full requirements of repository-level repair tasks. The replication package including code, data, and experimental results is available at https://github.com/DeepSoftwareAnalytics/SWE-Gate.

摘要：儲存庫層級的軟體工程基準顯著推進了對編碼代理的評估，但現有的基準主要測量生成的修補程式是否通過功能測試，並忽略了經審查衍生的接受約束（審查約束），這些約束通常影響修補程式在現實世界軟體開發中的可接受性。我們介紹了SWE-Gate，一個針對軟體工程代理的儲存庫層級基準，明確評估審查約束的遵循情況以及功能正確性。SWE-Gate從真實的拉取請求審查評論中衍生審查約束，並圍繞這些約束合成儲存庫層級的修復實例。每個實例提供獨立的功能和約束測試，以及不合規和金標修補程式，從而明確區分問題解決能力和審查約束的遵循情況。我們構建了SWE-Gate，包含303個儲存庫層級的修復實例，涵蓋75個來自不同軟體領域的開源Python儲存庫。在一個共同的編碼代理框架下，對四個不同能力水平的LLM後端進行的實驗顯示，功能成功與在完整修復規範下的成功之間存在顯著差距：在644個通過功能測試的修復中，有221個未能滿足提供的審查約束。這些發現表明僅依賴功能評估會高估代理滿足儲存庫層級修復任務完整要求的能力。包括代碼、數據和實驗結果的複製包可在https://github.com/DeepSoftwareAnalytics/SWE-Gate獲得。

##### **From Deceptive Outputs to Deceptive Mechanisms: A Causal Framework for Language-Model Deception Research**
2609.04166v1 by Yakov Pyotr Shkolnikov

Research and news coverage of language-model deception increasingly attributes human-like mental-state concepts to language models. Such claims can blur the distinction between behavior that looks deceptive and a mechanism that is actually deceptive.
  We introduce a causal taxonomy separating prior commitment from retrospective report, model preference from realized output, false preference from sensitivity to the utility of misleading a recipient, and deceptive behavior from the provenance of the objective or strategy producing it. We test these distinctions in two open-weight model families. Across controlled guessing-game and stock-trading experiments, we find that deceptive-looking behavior can arise without the corresponding proposed mechanism, while other interventions provide direct evidence that recipient information state can causally affect deceptive preference.
  These results show that deceptive behavior can provide evidence for a deceptive mechanism. But even evidence for such a mechanism does not establish model agency in the deception.

摘要：研究和新聞報導中對語言模型欺騙的探討日益將類似人類的心理狀態概念歸因於語言模型。這些主張可能模糊看似欺騙的行為與實際上欺騙的機制之間的區別。<lb/>  
我們提出了一個因果分類法，將先前的承諾與回顧報告、模型偏好與實現輸出、虛假偏好與對誤導接收者的效用的敏感性，以及欺騙行為與產生它的客觀或策略的來源區分開來。<lb/>  
我們在兩個開放權重模型系列中測試這些區別。在控制的猜測遊戲和股票交易實驗中，我們發現看似欺騙的行為可以在沒有相應提出的機制的情況下產生，而其他干預則提供了直接證據，表明接收者的信息狀態可以因果地影響欺騙偏好。<lb/>  
這些結果顯示，欺騙行為可以為欺騙機制提供證據。但即使是這樣的機制的證據也並不確立模型在欺騙中的主體性。

##### **SENTINEL-RL: Offloading Topological Reasoning from LLM Agents in the Security Operations Center**
2609.04159v1 by Uday Vallabhaneni, Cassie L. Cagwin, David J. Wild

Large language model (LLM) agents are increasingly proposed as autonomous SOC analysts, but two limitations make them unreliable at enterprise scale: a finite context window cannot hold a multi-thousand-host authentication graph, and free-form generation offers no guarantee that a recommended containment action is consistent with the topology it operates on. We present Sentinel-RL, an agentic-SOC architecture that decouples topological reasoning from semantic reasoning: a heterogeneous graph attention encoder summarizes the live authentication subgraph into a fixed-dimensional state, a Proximal Policy Optimization (PPO) policy maps this state to a constrained set of investigative actions, and an LLM agent loop is restricted to consuming the policy's recommendations and producing analyst-readable narratives gated by a critic. We instantiate the system on the LANL Comprehensive, Multi-Source Cyber-Security Events dataset and the Indiana University Quartz HPC cluster, reporting four results: (i) a two-phase CREATE ingestion pattern loads a 24M-edge authentication subgraph into Neo4j in 14.2 minutes on a single 32-core node, roughly 24x faster than the canonical MERGE-based pipeline; (ii) a sliding-window alert engine reliably trips a 25-event/10-second threshold in <=2.5 s across 50 trials; (iii) PPO training over 200 iterations converges to a mean episodic return of 8.74+/-0.31, with held-out precision of 0.91 and recall of 0.87 on labeled red-team events; and (iv) the integrated containment loop completes a full detect-investigate-recommend-human-approve cycle in a median of 6.3 s. We contribute a reusable engineering pattern (the hot-node deadlock workaround), a portable HPC deployment pattern (anchor-node co-location), and an enterprise-readiness analysis covering false-positive economics, reversibility guarantees, audit compliance, and the human-approval boundary.

摘要：大型語言模型（LLM）代理被越來越多地提議作為自主的安全運營中心（SOC）分析師，但有兩個限制使其在企業規模上不可靠：有限的上下文窗口無法容納數千主機的身份驗證圖，而自由形式生成無法保證建議的遏制行動與其操作的拓撲一致。我們提出了 Sentinel-RL，一種將拓撲推理與語義推理解耦的代理SOC架構：一個異質圖注意力編碼器將實時身份驗證子圖總結為固定維度的狀態，一個近端策略優化（PPO）策略將此狀態映射到一組受限的調查行動，而LLM代理循環僅限於消耗該策略的建議並生成由批評者控制的分析師可讀敘述。我們在LANL綜合多源網絡安全事件數據集和印第安納大學Quartz HPC集群上實現了該系統，報告了四個結果：（i）一種兩階段的CREATE攝取模式在單個32核節點上將2400萬邊的身份驗證子圖加載到Neo4j中，耗時14.2分鐘，速度約為經典MERGE基礎管道的24倍；（ii）滑動窗口警報引擎在50次試驗中可靠地在<=2.5秒內觸發25事件/10秒的閾值；（iii）在200次迭代中，PPO訓練收斂到均值回報8.74+/-0.31，對標記的紅隊事件的保留精度為0.91，召回率為0.87；（iv）集成的遏制循環在中位數6.3秒內完成完整的檢測-調查-建議-人類批准循環。我們貢獻了一個可重用的工程模式（熱節點死鎖解決方法）、一個可攜式HPC部署模式（錨節點共置），以及一個涵蓋假陽性經濟學、可逆性保證、審計合規性和人類批准邊界的企業準備分析。

##### **The Natural Language Interaction Protocol and Standard for AI Agents**
2609.04135v1 by Luyi Xing, Rasit Onur Topaloglu, Ranjan Sinha, Abhay Ratnaparkhi, Samuel Ndichu, Christopher Nguyen, Anindita Das, Tom Sheffler, Mohamed Rahouti, Zichuan Li, Xiaojing Liao, Sanjay Aiyagari

AI agents are increasingly being developed and deployed across organizations using heterogeneous agent-development frameworks, AI models, tool interfaces, protocols, and execution environments. To realize their potential social and business impact, these agents must be able to interoperate through a common communication protocol. The Natural Language Interaction Protocol (NLIP), developed by researchers and practitioners across companies and universities and standardized by Ecma International, addresses this need by defining a standards-based application-layer protocol for AI-agent interaction. NLIP provides a lightweight semantic message envelope that can be carried over existing transports such as HTTP/HTTPS, WebSocket, and AMQP, while allowing NLIP-aware agents and gateways to adapt between clients, agents, local context stores, ontologies, tools, enterprise services, and heterogeneous underlying protocols. This paper presents the motivation and design rationale of NLIP, its message model and transport bindings, security-by-design considerations, reference implementation, representative applications, adoption signals, and relationship to emerging agent protocols such as MCP and A2A.

摘要：AI代理正在各組織中越來越多地開發和部署，使用異構的代理開發框架、AI模型、工具介面、協議和執行環境。<lb/>為了實現其潛在的社會和商業影響，這些代理必須能夠通過通用通信協議進行互操作。<lb/>自然語言互動協議（NLIP）由來自公司和大學的研究人員和實踐者開發，並由Ecma International標準化，滿足了這一需求，通過定義基於標準的應用層協議來實現AI代理之間的互動。<lb/>NLIP提供了一個輕量級的語義消息封裝，可以通過現有的傳輸協議如HTTP/HTTPS、WebSocket和AMQP進行傳輸，同時允許NLIP感知的代理和網關在客戶端、代理、本地上下文存儲、本體、工具、企業服務和異構底層協議之間進行適應。<lb/>本文介紹了NLIP的動機和設計原則、其消息模型和傳輸綁定、安全設計考量、參考實現、代表性應用、採用信號以及與新興代理協議如MCP和A2A的關係。

##### **Environment Evolution for Terminal Agents**
2609.04128v1 by Zhiyuan Fan, Tinghao Yu, Yuanjun Cai, Jiang Zhou, Jiangtao Guan, Jincheng Liu, Yun Yang, Dingxin Hu, Zhuo Han, Xing Wu, Feng Zhang, Lilin Wang

Scaling interactive and verifiable environments is critical for training terminal agents. As frontier models become more capable, environments synthesized from scratch become less challenging and thus provide limited learning signals. Recent co-evolution methods iteratively synthesize environments near the model's learnable frontier based on weaknesses exposed during rollouts. However, their dependence on on-policy rollouts limits generalization and the continuous provision of learning signals as the model becomes stronger. In this paper, we propose environment evolution, which incrementally increases environment difficulty off-policy and schedules the evolved environments generation by generation during training to provide continuous learning signals. We derive three evolution directions that influence environment difficulty from the multi-turn learning objective and then implement evolution along these directions through a loop-engineered multi-agent harness. Quantitative rollout experiments with Hy4 preview, Claude Opus 5, and GPT-5.6 Sol show that environment evolution consistently produces more difficult environments. We validate its effectiveness on Qwen3.6-27B and Qwen3.6-35B-A3B through simple long-horizon RL training, improving their performance by 14.4 and 18.0 percentage points on Terminal-Bench 2.1, respectively.

摘要：擴展互動且可驗證的環境對於訓練終端代理至關重要。隨著前沿模型變得越來越強大，從零開始合成的環境變得不那麼具有挑戰性，因此提供的學習信號有限。最近的共同演化方法基於在回合過程中暴露的弱點，迭代地合成接近模型可學習邊界的環境。然而，它們對於政策回合的依賴限制了泛化能力，並在模型變得更強大時持續提供學習信號。在本文中，我們提出了環境演化，該方法在非政策下逐步增加環境的難度，並在訓練過程中按代次安排演化環境的生成，以提供持續的學習信號。我們從多回合學習目標中推導出三個影響環境難度的演化方向，然後通過一個環路工程的多代理架構沿這些方向實施演化。使用Hy4預覽、Claude Opus 5和GPT-5.6 Sol的定量回合實驗顯示，環境演化始終產生更具挑戰性的環境。我們通過簡單的長期強化學習訓練在Qwen3.6-27B和Qwen3.6-35B-A3B上驗證了其有效性，分別在Terminal-Bench 2.1上提高了14.4和18.0個百分點的性能。

##### **Epistemic Warrant for LLM Recommendations: Characterizing the Basis for Reliance When Ground Truth Is Unavailable**
2609.04127v1 by Shai Vardi, João Sedoc

Large language models are increasingly used to support organizational decisions, yet users often lack a principled basis for assessing whether to rely on a specific recommendation. Existing approaches typically evaluate broad model properties, such as reliability, uncertainty, or robustness, or focus on user trust, rather than the underlying basis for relying on an individual recommendation. Adapting theoretical foundations from epistemology, we introduce epistemic warrant, a decision-level construct that characterizes the stability of a model's preference and the scope over which that preference holds. We operationalize this construct through a four-tier reliance certificate for pairwise recommendations, distinguishing among unstable, context-dependent, locally supported, and broadly supported recommendations. We validate the construct using contemporary methodologies: known-groups tests successfully recover expert-prespecified warrant orderings, and stronger warrants systematically align with independent consensus from crowd workers. Furthermore, we demonstrate that epistemic warrant provides information distinct from verbalized confidence and is not readily explained by decision difficulty. Ultimately, this framework offers a theoretically grounded, implementable approach for characterizing the warrant of individual LLM recommendations when objective ground truth is unavailable.

摘要：大型語言模型越來越多地被用來支持組織決策，然而用戶往往缺乏一個原則性的基礎來評估是否依賴於特定的建議。現有的方法通常評估廣泛的模型屬性，如可靠性、不確定性或穩健性，或專注於用戶信任，而不是依賴於個別建議的基礎。通過調整來自認識論的理論基礎，我們引入了認識論擔保，這是一個決策層級的構念，描述了模型偏好的穩定性及該偏好適用的範圍。我們通過一個四層次的依賴證書來操作化這一構念，用於成對建議，區分不穩定的、依賴上下文的、本地支持的和廣泛支持的建議。我們使用當代方法論來驗證這一構念：已知群體測試成功恢復專家預先指定的擔保排序，且更強的擔保與來自群眾工作者的獨立共識系統性對齊。此外，我們證明認識論擔保提供的信息與口頭表達的信心不同，並且不易用決策困難來解釋。最終，這一框架提供了一種理論上有根據且可實施的方法，用於描述在客觀真實性不可用時個別LLM建議的擔保。

##### **Sequential Beats Joint: On the Interplay between On-Policy Distillation and RLVR**
2609.04108v1 by Boyan Li, Bingsen Chen, Chenghao Yang, Ping Nie, Chen Zhao, Xi Ye

Reinforcement learning with verifiable rewards (RLVR) and on-policy distillation (OPD) have emerged as two dominant methods for post-training reasoning LLMs. Prior work uses OPD's dense token-level supervision to complement the sparse RL reward, fusing the two signals within a single step: either as a \emph{weighted-additive combination} or a \emph{teacher-modulated rescaling} of the RL advantage. In this paper, we show that a simple two-stage scheme, OPD-then-RL, consistently outperforms pure OPD, pure RLVR, and all such joint baselines across logic and math reasoning benchmarks. Beyond the empirical results, we further provide a systematic understanding of this through pass@$k$ behavior, learning dynamics, and parameter updates, yielding a consistent explanation: OPD expands the student's coverage of teacher-supported solutions and RL sharpens within that support, while jointly optimizing the two signals causes them to interfere.To provide a practical recipe, we find that the OPD validation score is the key signal for when to switch to RL, and that OPD is a better cold start for RL than SFT. Together, our results establish OPD-then-RL as a simple yet strong way to combine the two methods, turning two entangled signals into complementary stages.

摘要：強化學習與可驗證獎勵（RLVR）以及政策內蒸餾（OPD）已成為後訓練推理大型語言模型（LLMs）的兩種主導方法。先前的研究利用OPD的密集標記級監督來補充稀疏的RL獎勵，在單一步驟中融合這兩種信號：要麼作為\emph{加權相加組合}，要麼作為RL優勢的\emph{教師調制重縮放}。在本文中，我們顯示一個簡單的兩階段方案，OPD-然後-RL，始終超越純OPD、純RLVR以及所有此類聯合基準，在邏輯和數學推理基準上表現優異。除了實證結果，我們進一步通過pass@$k$行為、學習動態和參數更新提供了系統性的理解，得出一致的解釋：OPD擴展了學生對教師支持解決方案的覆蓋範圍，而RL則在該支持內部進行精練，同時聯合優化這兩種信號會導致它們相互干擾。為了提供一個實用的配方，我們發現OPD驗證分數是切換到RL的關鍵信號，並且OPD比SFT更適合作為RL的冷啟動。總體而言，我們的結果確立了OPD-然後-RL作為一種簡單而強大的方式來結合這兩種方法，將兩個交織的信號轉變為互補的階段。

##### **Why Gated DeltaNet Survives 4-Bit Quantization: NVFP4 W4A4 for the Recurrent Half of a Hybrid 27B LLM**
2609.04098v1 by Sergii Kozyrev, Davyd Maiboroda

Hybrid LLMs pair softmax attention with linear-attention layers such as Gated DeltaNet (GDN), whose recurrent state summarizes the context in fixed size. Early community 4-bit quantizations of Qwen3.8-27B (48 GDN layers, 16 attention layers) left the GDN block in 8- or 16-bit precision -- especially its decay and write-strength gates -- on the intuition that errors in a recurrence accumulate over long contexts. We test that intuition by building Minima: NVFP4 W4A4 on all 496 linear layers, GDN included. Across perplexity at 4K/32K, MMLU-Pro, GSM8K, AIME'25, GPQA-Diamond, LiveCodeBench, and RULER retrieval to 64K, Minima matches BF16 within seed noise (5-task average -0.52) while being the smallest (17.5 GiB) and fastest-prefill (+14-19%) recipe we compare, and its 32K perplexity gap shrinks with position. A four-part mechanism study explains why: (i) NVFP4's 16-element block scaling localizes the residual stream's extreme outliers, equalizing activation error across layer roles; (ii) the supposedly fragile gate projections are the least sensitive -- softplus/exponential and sigmoid parameterizations compress ~11% GEMM error to ~2% output error; (iii) the delta-rule recurrence holds injected noise at a flat plateau over 32K tokens and forgets a state impulse within hundreds of steps, because each write overwrites the state along the current key direction; (iv) the per-token quantization cost washes out with context instead of compounding. We also repair a global-scale mismatch that arises when per-module-calibrated NVFP4 checkpoints are served by kernels that fuse those modules into one GEMM, and show calibrated FP8 KV-cache scales are performance-free. The result: a practical recipe -- quantize everything, ship KV scales -- and a mechanistic account of why the recurrent half of a hybrid LLM is the easy half to quantize. Checkpoint: https://huggingface.co/minima-ai/mnma_qwen3.8_27b_nvfp4

摘要：混合 LLM 將 softmax 注意力與線性注意力層結合，例如 Gated DeltaNet (GDN)，其循環狀態以固定大小總結上下文。早期社群對 Qwen3.8-27B (48 個 GDN 層，16 個注意力層) 的 4 位量化將 GDN 模塊保留在 8 位或 16 位精度上——特別是其衰減和寫入強度閘——基於這樣的直覺：在長上下文中，循環中的錯誤會累積。我們通過構建 Minima：NVFP4 W4A4 在所有 496 個線性層上進行測試，包括 GDN。在 4K/32K 的困惑度、MMLU-Pro、GSM8K、AIME'25、GPQA-Diamond、LiveCodeBench 和 RULER 檢索到 64K 的情況下，Minima 在種子噪音內與 BF16 相匹配 (5 任務平均 -0.52)，同時是我們比較中最小的 (17.5 GiB) 和最快的預填 (+14-19%) 配方，其 32K 的困惑度差距隨位置縮小。一項四部分的機制研究解釋了原因：(i) NVFP4 的 16 元素塊縮放將殘差流的極端異常本地化，使層角色之間的激活錯誤平衡；(ii) 所謂脆弱的閘投影是最不敏感的——softplus/exponential 和 sigmoid 參數化將約 11% 的 GEMM 錯誤壓縮至約 2% 的輸出錯誤；(iii) 三角規則循環在 32K 令牌上保持注入噪音在平坦的平臺上，並在數百步內忘記狀態脈衝，因為每次寫入都沿著當前鍵方向覆蓋狀態；(iv) 每個令牌的量化成本隨著上下文而消失，而不是累加。我們還修復了一個全球範圍的錯配，當每個模塊校準的 NVFP4 檢查點由將這些模塊融合為一個 GEMM 的內核提供時出現，並顯示校準的 FP8 KV-cache 縮放是無性能成本的。結果：一個實用的配方——量化所有內容，發送 KV 縮放——以及一個機制性解釋為何混合 LLM 的循環部分是最容易量化的部分。檢查點：https://huggingface.co/minima-ai/mnma_qwen3.8_27b_nvfp4

##### **Adaptive Vision-Language Grasping via Composable Foundation Priors and Generalizable Grasp Synthesis**
2609.04096v1 by Sixu Yan, Shikang Wang, Binhua Huang, Xuanlai Tang, Guohua Fan, Fan Huang, Haoxuan Li, Yongkang Li, Yuhan Li, Bencheng Liao, Zeyu Zhang, Wenyu Liu, Hangxin Liu, Xinggang Wang

This paper proposes AdaRoboVLG, a task-adaptive Vision-Language-Grasp (VLG) framework that supports generalizable grasp synthesis across different robotic hands. Unlike existing VLG methods that tightly couple foundation models with end-to-end grasp policies, AdaRoboVLG learns an efficient generalizable base policy that generates and evaluates physically feasible grasp candidates through explicit kinematic mapping and force-closure-based stability estimation, while offloading task-dependent understanding to specialized foundation-model modules. These modules provide composable priors that are integrated into the grasp synthesis process, enabling contextually adaptive grasp synthesis without retraining the underlying grasp policy. Through extensive simulation and real-world experiments, we demonstrate that (i) the base policy exhibits efficient learning and strong cross-hand generalization, (ii) the framework effectively incorporates spatial, cognitive, and temporal priors to address three representative grasping challenges without compromising grasp synthesis performance compared to state-of-the-art methods, and (iii) these priors can operate jointly to enable functional grasping in cluttered and dynamic environments. These results indicate that decoupling physical grasp synthesis from task-dependent understanding provides a scalable paradigm for robotic grasping, allowing future advances in foundation models to be directly translated into improved grasp capabilities without redesigning or retraining the underlying grasp policy. Supplementary videos are available at https://adarobovlg.github.io/

摘要：這篇論文提出了AdaRoboVLG，一個任務自適應的視覺-語言-抓取（VLG）框架，支持在不同機器手之間的可泛化抓取合成。與現有的VLG方法緊密結合基礎模型和端到端抓取策略不同，AdaRoboVLG學習了一個高效的可泛化基礎策略，通過明確的運動學映射和基於力閉合的穩定性估計生成和評估物理可行的抓取候選，並將任務依賴的理解卸載到專門的基礎模型模塊中。這些模塊提供可組合的先驗，這些先驗被整合到抓取合成過程中，使得在不重新訓練基礎抓取策略的情況下實現上下文自適應的抓取合成。通過廣泛的模擬和現實世界實驗，我們證明了（i）基礎策略展現出高效的學習和強大的跨手泛化，（ii）該框架有效地整合了空間、認知和時間先驗，以解決三個代表性的抓取挑戰，並且在抓取合成性能上不妥協於最先進的方法，以及（iii）這些先驗可以共同運作，以實現雜亂和動態環境中的功能性抓取。這些結果表明，將物理抓取合成與任務依賴的理解解耦提供了一個可擴展的機器人抓取範式，使得未來基礎模型的進步能夠直接轉化為改進的抓取能力，而無需重新設計或重新訓練基礎抓取策略。補充視頻可在 https://adarobovlg.github.io/ 獲得。

##### **DRACO: Fine-Grained Credit Assignment with Dynamic Rubrics for Long-Horizon Agent Training**
2609.04094v1 by Shubham Gandhi, Saurabh Goyal, Kiran Kate, Yara Rizk

Reinforcement Learning from Verifiable Rewards works well when a task has a programmatic checker, but most long-horizon agent domains have none. We work in the outcome-blind setting, where ground-truth success signals are not available. Multi-criteria rubrics are a popular way to supply such a reward; they are scored once per trajectory, but a single scalar is a poor signal across tens of steps. We propose DRACO: Distributing Rubric-based Advantage for Credit Optimization. It generates rubrics dynamically during training to track the policy's evolving capability, scores those rubrics once per completed trajectory, and redistributes that judgment over the steps responsible for annotated rubrics to produce differentiated per-step advantages in GRPO. The redistribution is closed-form and does not introduce any trained attribution module. On AppWorld, DRACO gains 15.9 points over the base model and 5.3 points over GRPO trained with a sparse ground-truth reward, despite not using any verifiers itself. On out-of-domain Tau-Bench, it gains 5.3 points over the base model even without a frontier judge, beating both ground-truth-reward training and other rubric-based training settings. The code for DRACO is available at https://github.com/IBM/draco.

摘要：強化學習從可驗證獎勵中運作良好，當任務有程式檢查器時，但大多數長期代理領域並沒有這樣的檢查器。我們在結果盲設定中工作，這裡沒有可用的真實成功信號。多標準評分標準是一種提供這種獎勵的流行方式；它們在每條軌跡上評分一次，但單一的標量在數十個步驟中是一個較差的信號。我們提出了DRACO：基於評分標準的優勢分配以進行信用優化。它在訓練過程中動態生成評分標準，以追蹤政策的演變能力，在每條完成的軌跡上評分這些評分標準，並將該判斷重新分配到負責註解評分標準的步驟上，以在GRPO中產生差異化的每步優勢。這種重新分配是封閉形式的，並不引入任何訓練的歸因模組。在AppWorld上，DRACO比基礎模型提高了15.9分，比使用稀疏真實獎勵訓練的GRPO提高了5.3分，儘管它本身並未使用任何驗證器。在域外的Tau-Bench上，即使沒有前沿評審，它也比基礎模型提高了5.3分，超越了真實獎勵訓練和其他基於評分標準的訓練設置。DRACO的代碼可在https://github.com/IBM/draco找到。

##### **A Non-Formulable Theorem: A Fundamental Limit of Finite Syntactic Systems and Its Consequences for Security and AI**
2609.04086v1 by Fabio F. G. Buono

For every coherent and sufficiently expressive finite syntactic system S, we prove the existence of at least one theorem that S cannot produce autonomously. The result is a metatheorem: it proves the existence of a theorem, and applies to every finite syntactic system - security mechanisms, AI systems, formal verifiers, legal systems, economic models, and the formal system in which it is itself proved.

摘要：對於每一個連貫且足夠表達的有限語法系統 S，我們證明至少存在一個定理是 S 無法自主產生的。這一結果是一個元定理：它證明了定理的存在，並適用於每一個有限語法系統——安全機制、人工智慧系統、形式驗證器、法律系統、經濟模型，以及它自身被證明的形式系統。

##### **CORE: Improving Compositional Reasoning in MLLM Embedding via Reranker Distillation**
2609.04083v1 by Tingyu Song, Mingxin Li, Yanzhao Zhang, Dingkun Long, Chu Liu, Pengjun Xie, Yilun Zhao, Shu Wu

MLLM-based embedding models remain limited in compositional retrieval, often failing to distinguish scenes containing the same concepts but different attribute-object bindings. Yet the same backbone can resolve such distinctions when used as a cross-attentive reranker, motivating us to distill its compositional judgments into the embedding model. We propose CORE, which synthesizes candidate lists spanning five compositional matching levels and introduces a Rank-KL objective that trains the embedding model to reproduce the reranker's fine-grained ranking. We further introduce a graded evaluation protocol and compare contrastive learning, pairwise CoSENT, and listwise Rank-KL under the same data and tuning budget. Our comparison shows that both CoSENT and Rank-KL use the multi-level supervision more effectively than contrastive learning, with Rank-KL achieving the strongest overall performance. Across three compositional reasoning benchmarks (COLA, SUGARCREPE++, NEGBENCH), CORE-RERANKER-8B achieves an 82.7% total average, outperforming Jina-Reranker by 10.7 points, while CORE-EMBED-8B achieves the best total average (0.666) among all evaluated embedding models. The improvements transfer to the MCMR benchmark without sacrificing retrieval performance on COCO and Flickr30K.

摘要：MLLM 基於嵌入模型在組合檢索方面仍然有限，常常無法區分包含相同概念但不同屬性-物件綁定的場景。<lb/>然而，當作為跨注意力重排序器使用時，相同的骨幹可以解決這些區別，這激勵我們將其組合判斷提煉到嵌入模型中。<lb/>我們提出了 CORE，它合成了跨越五個組合匹配層級的候選列表，並引入了一個 Rank-KL 目標，該目標訓練嵌入模型以重現重排序器的細粒度排名。<lb/>我們進一步引入了一個分級評估協議，並在相同的數據和調整預算下比較對比學習、成對 CoSENT 和列表 Rank-KL。<lb/>我們的比較顯示，CoSENT 和 Rank-KL 在多層次監督的使用上比對比學習更有效，且 Rank-KL 實現了最強的整體表現。<lb/>在三個組合推理基準（COLA、SUGARCREPE++、NEGBENCH）中，CORE-RERANKER-8B 獲得了 82.7% 的總平均，超越了 Jina-Reranker 10.7 分，而 CORE-EMBED-8B 在所有評估的嵌入模型中獲得了最佳總平均（0.666）。<lb/>這些改進在不犧牲 COCO 和 Flickr30K 的檢索性能的情況下，轉移到 MCMR 基準。

##### **TAP-Path: Task-Adaptive Structural and Token Pruning for Efficient and Trustworthy Pathology Foundation Models**
2609.04071v1 by Mehedi Hasan, Ashfak Yeafi, Md Khairul Islam

Pathology foundation models improve transferable representation learning for histopathology, but recent gains often rely on encoders with hundreds of millions of parameters and high inference cost. We propose TAP-Path, a task-adaptive compression framework that directly restructures a pretrained Virchow2 encoder rather than distilling it into a separate student. TAP-Path combines validation-driven transformer-block selection, physical removal of redundant blocks, input-adaptive patch-token pruning, multi-depth feature recovery, and a lightweight gated task head. The final model retains 24 of 32 transformer blocks and 70% of patch tokens after pruning, reducing encoder parameters by 24.96% (631.24M to 473.70M) and analytical encoder compute by 35.20% (340.13G to 220.40G FLOPs). Across three task-head optimization seeds, TAP-Path achieved $87.98 \pm 0.067%$ test accuracy, $81.26 \pm 0.49%$ balanced accuracy, and $82.38 \pm 0.48%$ macro-F1 on a 32-class histopathology benchmark, compared with 86.89% for full Virchow2 and 87.67% for UNI2-h. TAP-Path achieved a Brier score of $0.1800 \pm 0.0005$ and failure-detection AUROC of $0.9047 \pm 0.0060$. A validation-only rare-aware objective improved rare-class balanced accuracy in a secondary operating analysis. Frozen external evaluation on 433 CPTAC samples yielded $91.22 \pm 0.83%$ accuracy and $91.10 \pm 0.81%$ balanced accuracy. These results show that task-adaptive structural and token sparsification can improve the accuracy-efficiency trade-off of large pathology foundation models while preserving reliability under internal and external evaluation.

摘要：病理基礎模型改善了組織病理學的可轉移表示學習，但最近的進展通常依賴於擁有數億參數和高推理成本的編碼器。我們提出了 TAP-Path，一個任務自適應壓縮框架，直接重組預訓練的 Virchow2 編碼器，而不是將其蒸餾成一個單獨的學生。TAP-Path 結合了基於驗證的Transformer塊選擇、冗餘塊的物理移除、輸入自適應的補丁標記修剪、多深度特徵恢復和輕量級的門控任務頭。最終模型在修剪後保留了 32 個Transformer塊中的 24 個和 70% 的補丁標記，將編碼器參數減少了 24.96%（從 631.24M 減少到 473.70M），分析編碼器計算減少了 35.20%（從 340.13G 減少到 220.40G FLOPs）。在三個任務頭優化種子中，TAP-Path 在 32 類組織病理學基準上達到了 $87.98 \pm 0.067%$ 的測試準確率、$81.26 \pm 0.49%$ 的平衡準確率和 $82.38 \pm 0.48%$ 的宏 F1，相較於完整的 Virchow2 的 86.89% 和 UNI2-h 的 87.67%。TAP-Path 的 Brier 分數為 $0.1800 \pm 0.0005$，失敗檢測的 AUROC 為 $0.9047 \pm 0.0060$。僅驗證的稀有感知目標在次要操作分析中提高了稀有類別的平衡準確率。在 433 個 CPTAC 樣本上的凍結外部評估產生了 $91.22 \pm 0.83%$ 的準確率和 $91.10 \pm 0.81%$ 的平衡準確率。這些結果表明，任務自適應的結構和標記稀疏化可以在保持內部和外部評估的可靠性的同時改善大型病理基礎模型的準確性與效率的權衡。

##### **Subspace Inference Enables Efficient Active Reward Learning from Preferences**
2609.04066v1 by Yutai Zhou, Erdem Bıyık

Reinforcement learning from human feedback (RLHF) has emerged as a powerful yet sample-inefficient approach for learning reward models from human preferences, making active learning a critical component in synthesizing informative preference queries. However, effective uncertainty quantification required for active learning remains a key challenge for large neural network reward models. In this paper, we introduce PreferenceEKF, a sample-efficient approach that tracks reward model uncertainty by framing active preference learning as a sequential Bayesian filtering problem. Instead of relying on computationally prohibitive posterior inference over the full neural network parameter space, our method performs sequential inference via an extended Kalman filter within a low-dimensional parameter subspace, continuously updating the reward model posterior as new preference queries arrive. Our approach enables scalable sampling of neural network parameters to efficiently compute acquisition functions for active reward learning. Experiments on the D4RL and V-D4RL benchmarks demonstrate that our approach achieves better sample efficiency, runtime, scalability, and calibration compared to other Bayesian deep learning approaches, and the learned reward models lead to competitive offline reinforcement learning policy performance. This highlights the potential of scalable Bayesian methods for preference-based reward modeling in RLHF. Our code is available at https://github.com/yutaizhou/bnn_pref.

摘要：強化學習從人類反饋中學習（RLHF）已經成為一種強大但樣本效率低下的方法，用於從人類偏好中學習獎勵模型，使得主動學習成為合成信息豐富的偏好查詢的關鍵組成部分。<lb/>然而，主動學習所需的有效不確定性量化仍然是大型神經網絡獎勵模型的一個主要挑戰。<lb/>在本文中，我們介紹了PreferenceEKF，一種通過將主動偏好學習框架化為序列貝葉斯過濾問題來跟蹤獎勵模型不確定性的樣本高效方法。<lb/>我們的方法不是依賴於對整個神經網絡參數空間進行計算上不可承受的後驗推斷，而是通過在低維參數子空間內使用擴展卡爾曼濾波器進行序列推斷，隨著新的偏好查詢的到來不斷更新獎勵模型的後驗分佈。<lb/>我們的方法使得神經網絡參數的可擴展抽樣成為可能，從而有效計算主動獎勵學習的獲取函數。<lb/>在D4RL和V-D4RL基準上的實驗表明，我們的方法在樣本效率、運行時間、可擴展性和校準方面優於其他貝葉斯深度學習方法，且學習到的獎勵模型在離線強化學習策略性能上表現競爭力。<lb/>這突顯了可擴展貝葉斯方法在RLHF中基於偏好的獎勵建模的潛力。<lb/>我們的代碼可在https://github.com/yutaizhou/bnn_pref獲得。

##### **When Models Edit Too Much: On the Fidelity of Minimal Code Edits**
2609.04061v1 by Tongyao Zhu, Wei Hern Lim, Min-Yen Kan

Large language models (LLMs) are increasingly used to edit existing code, but correctness alone is not enough: useful repairs should also be minimal, reviewable, and faithful to the original implementation. We study over-editing, the tendency of a model to rewrite code beyond what is required to fix a bug. We construct an evaluation framework from 400 BigCodeBench problems by injecting controlled AST-level corruptions into reference solutions, giving each repair task a known minimal patch. Across frontier LLMs, over-editing is widespread even among strong models like GPT-5.5: high Pass@1 can coexist with unnecessarily large edits and added cognitive complexity. A preservation instruction substantially reduces this behavior, lowering average excess Levenshtein distance from 0.195 to 0.131, reducing added cognitive complexity by 26.6%, and increasing Pass@1 by 2.3 points. However, these gains do not simply follow from a larger reasoning budget or larger models. We next ask whether minimal editing can be learned directly during post-training. We observe that supervised fine-tuning overfits to seen corruption patterns, whereas reinforcement learning gives the best out-of-domain edit-fidelity and performance-retention trade-off. These results position edit fidelity as a distinct axis of code-repair quality and show that it can be measured and learned.

摘要：大型語言模型（LLMs）越來越多地被用來編輯現有的代碼，但僅僅正確性是不夠的：有用的修復還應該是最小的、可檢查的，並且忠實於原始實現。我們研究了過度編輯，這是模型在修復錯誤時重寫代碼超出所需的趨勢。我們從400個BigCodeBench問題中構建了一個評估框架，通過將受控的AST級別損壞注入參考解決方案，為每個修復任務提供已知的最小補丁。在前沿LLMs中，過度編輯普遍存在，即使是在像GPT-5.5這樣的強大模型中：高的Pass@1可以與不必要的大編輯和增加的認知複雜性共存。一個保留指令顯著減少了這種行為，將平均過剩的Levenshtein距離從0.195降低到0.131，減少了26.6%的認知複雜性，並將Pass@1提高了2.3分。然而，這些增益並不僅僅是由於更大的推理預算或更大的模型。我們接下來詢問是否可以在後訓練期間直接學習最小編輯。我們觀察到，監督微調對已見的損壞模式過擬合，而強化學習則提供了最佳的域外編輯保真度和性能保留權衡。這些結果將編輯保真度定位為代碼修復質量的一個獨特軸，並顯示它可以被測量和學習。

##### **Translation as a Decision Space: A Multi-Agent Perspective on Low-Resource Dialect Generation**
2609.04048v1 by Hasan Alkhder, Mohammad Abboush, Igor Tchappi, Ahmet Zengin, Amro Najjar

Neural machine translation (NMT) systems typically produce a single output per input, obscuring the alternative decision trajectories implicitly available within multilingual decoding. This opacity becomes particularly problematic in low-resource dialect settings, where multiple linguistically valid realizations may differ in lexical authenticity, register, and structural stability. We propose reframing translation as a structured decision space explored by autonomous translation agents. Instead of analyzing a single output, we model distinct translation pathways as agents operating over a shared multilingual backbone. Inter-agent divergence is treated not as error but as an interpretable behavioral signal. We conduct an empirical study on Turkish--Syrian Arabic translation using three agents: (1) zero-shot direct translation, (2) dialect-stabilized translation via lightweight fine-tuning, and (3) pivot translation through English. Evaluation is performed on 5,000 dialogue sentences, while stabilization is trained on 5,000 additional Turkish--Syrian sentence pairs drawn from television dialogue and MADAR-Turk resources. Rather than optimizing for conventional performance metrics, we quantify structured behavioral displacement using dialect marker frequency, lexical proximity to standardized Arabic, and structural variance. Lightweight stabilization nearly doubles dialect marker usage, increasing it from 0.2266 to 0.4988, while significantly reducing structural instability. Pivot mediation introduces normalization pressure and measurable compression effects, whereas zero-shot translation exhibits the highest decision variance. We argue that translation divergence across agents reveals latent decision flexibility within multilingual models and we provide a principled interpretability framework for low-resource dialect generation.

摘要：神經機器翻譯（NMT）系統通常對每個輸入產生單一輸出，掩蓋了在多語言解碼中隱含的替代決策路徑。這種不透明性在低資源方言環境中特別成為問題，因為多種語言上有效的實現可能在詞彙真實性、語域和結構穩定性上有所不同。我們建議將翻譯重新框架為由自主翻譯代理探索的結構化決策空間。與其分析單一輸出，我們將不同的翻譯路徑建模為在共享的多語言骨幹上運作的代理。代理之間的分歧不被視為錯誤，而是可解釋的行為信號。我們對土耳其語-敘利亞阿拉伯語翻譯進行了實證研究，使用三個代理：（1）零樣本直接翻譯，（2）通過輕量級微調的方言穩定翻譯，以及（3）通過英語的樞紐翻譯。評估是在5,000句對話句子上進行的，而穩定性則在5,000對額外的土耳其語-敘利亞句子對上進行訓練，這些句子來自電視對話和MADAR-Turk資源。我們不僅優化傳統的性能指標，而是通過方言標記頻率、與標準阿拉伯語的詞彙接近度和結構變異來量化結構化行為位移。輕量級穩定幾乎將方言標記的使用量翻倍，從0.2266增加到0.4988，同時顯著減少結構不穩定性。樞紐調解引入了正規化壓力和可測量的壓縮效應，而零樣本翻譯則顯示出最高的決策變異。我們認為，代理之間的翻譯分歧揭示了多語言模型中的潛在決策靈活性，並為低資源方言生成提供了一個原則性的可解釋性框架。

##### **The Dice Roll Method: A Standardized Protocol for Repeated-Query Auditing of Large Language Model Brand Recommendations**
2609.04047v1 by Dmitrij Żatuchin

Background: Researchers increasingly use repeated identical prompts to audit stochastic variation in large language model (LLM) brand recommendations, yet no standardized protocol exists for setting iteration counts, selecting stability metrics, or establishing reliability thresholds. Objective: We formalize the Dice Roll Method as a reusable protocol for repeated-query auditing of LLM brand recommendations, grounded in a generative model of temperature-scaled nucleus sampling. Methods: Total response variance is decomposed into sampling, prompt-phrasing, run-to-run, and model-version components. The stack: a negative-binomial mixed model with iterations as repeated measures; Cliff's delta as the distribution-free effect size; dependence-preserving bootstrap; simulation-based power; a generalizability-theory decomposition; drift diagnostics on pinned snapshots. We reanalyse five brand-recommendation auditing studies: approximately 190,000 observations, 270+ brands, 6 languages, iteration counts 5 to 40. Results: Three tiers of iteration guidance emerge from the D-study: exploratory (n = 5, G = 0.58), confirmatory (n = 10, G = 0.74), and rigorous (n = 15, G = 0.81), tied to effect-size and generalizability targets. The four metric families (count, set, embedding, fairness-adjusted PASOR) are complementary, motivating a compact metric battery over single indicators. A pre-registered external validation on three independent corpora (Motoki et al., 100-round; Rozado, 24 models; llm-stability) reproduces the D-study reliability prediction in 37 of 39 cells with no failures and the n = 5 power value to two decimals; the fixed tiers do not transfer, supporting a pilot-then-solve reading. Conclusion: The protocol gives repeated-query auditing of LLM brand recommendations a statistically principled footing under the conditional, non-Gaussian structure of real autoregressive generation.

摘要：背景：研究人員越來越多地使用重複相同的提示來審核大型語言模型（LLM）品牌推薦中的隨機變異，但目前尚無標準化的協議來設定迭代次數、選擇穩定性指標或建立可靠性閾值。<lb/>目標：我們將擲骰子方法正式化為一種可重用的協議，用於重複查詢審核LLM品牌推薦，基於溫度縮放的核取樣生成模型。<lb/>方法：總響應變異被分解為取樣、提示措辭、運行到運行和模型版本組件。<lb/>堆疊：一個負二項混合模型，將迭代作為重複測量；Cliff's delta作為無分佈效應大小；保持依賴的自助法；基於模擬的效能；一般化理論分解；在固定快照上的漂移診斷。<lb/>我們重新分析了五個品牌推薦審核研究：約190,000個觀察，270多個品牌，6種語言，迭代次數5到40。<lb/>結果：從D研究中出現了三個層級的迭代指導：探索性（n = 5，G = 0.58）、確認性（n = 10，G = 0.74）和嚴謹性（n = 15，G = 0.81），與效應大小和一般化目標相關。<lb/>四個指標家族（計數、集合、嵌入、公平調整的PASOR）是互補的，促進了一個緊湊的指標電池而非單一指標。<lb/>在三個獨立語料庫上的預註冊外部驗證（Motoki等，100輪；Rozado，24個模型；llm-stability）在39個單元中重現了D研究的可靠性預測，沒有失敗，n = 5的效能值精確到小數點後兩位；固定層級不轉移，支持先試點再解決的閱讀。<lb/>結論：該協議使LLM品牌推薦的重複查詢審核在真實自回歸生成的條件非高斯結構下具有統計原則基礎。

##### **Editable Visual Design**
2609.04034v1 by Junyan Ye, Wei Liu, Dongzhi Jiang, Zichen Wen, HaoDong Li, Zhutao Lv, Jiaxin Lin, Jinhua Yu, Jun He, Zilong Huang, Rui Chen, Weijia Li

While diffusion base models such as GPT-Image-2 and Nano-Banana exhibit remarkable visual expressiveness, their end-to-end generation inherently yields flattened bitmaps with error-prone text, precluding layer-wise post-editing. Conversely, code-based visual generation via Coding Agents provides precise layout control and decoupled layers, yet remains constrained by a lack of global aesthetic intuition and the difficulty of coding complex visual assets.
  To address this, we propose Editable Visual Design, a new paradigm driven by a Coding Agent. We designate the VLM as the ``creative brain'' for requirement comprehension, task planning, and aesthetic judgment, while utilizing the image generation model as an on-demand ``visual world simulator'' to synthesize standalone visual assets. Operating under an ``imagine first, then act'' closed-loop workflow, the agent generates isolated assets, writes native HTML/CSS, and iteratively refines the design against visual rendering feedback.
  Furthermore, Agent Design Replay faithfully reproduces the creative and reasoning trajectory akin to that of professional human designers. Ultimately, the system delivers editable artifacts with decoupled layers and real text, enabling users to perform intuitive mouse dragging and layout adjustments on a graphical user interface. Validations on posters, infographics, and other scenarios show that this paradigm successfully achieves both refined aesthetics and production-grade editability.

摘要：雖然擴散基礎模型如 GPT-Image-2 和 Nano-Banana 展現了卓越的視覺表現力，但其端到端生成本質上產生了平坦的位圖，並伴隨著易出錯的文本，這阻礙了逐層後期編輯。相對而言，通過編碼代理進行的基於代碼的視覺生成提供了精確的佈局控制和解耦層，但仍受限於缺乏全球美學直覺以及編碼複雜視覺資產的困難。<lb/>為了解決這個問題，我們提出了可編輯的視覺設計，這是一種由編碼代理驅動的新範式。我們將 VLM 指定為需求理解、任務規劃和美學判斷的“創意大腦”，同時利用圖像生成模型作為按需的“視覺世界模擬器”來合成獨立的視覺資產。在“先想像，然後行動”的閉環工作流程下，代理生成獨立資產，編寫原生 HTML/CSS，並根據視覺渲染反饋迭代完善設計。<lb/>此外，代理設計重放忠實地再現了類似於專業人類設計師的創意和推理軌跡。最終，該系統提供了可編輯的工件，具有解耦層和真實文本，使用戶能夠在圖形用戶界面上進行直觀的鼠標拖動和佈局調整。對海報、信息圖和其他場景的驗證顯示，這一範式成功實現了精緻的美學和生產級的可編輯性。

##### **IRWOZ 2.0: A Large Language Model-driven Dialogue Dataset for Industrial Robot Conversations**
2609.04030v1 by Chen Li, Dimitrios Chrysostomou

IRWOZ has improved industrial human-robot interaction (HRI) dialogue systems through domain-specific annotations. However, its initial version contains substantial noise in dialogue states and utterances, limiting state-tracking accuracy. We introduce IRWOZ 2.0, which addresses these limitations through large language model (LLM) enhanced generation (Mistral/Claude-3.5) and quality refinements. Our improved dataset expands to 390 dialogues across 4 industrial domains (Assembly, Delivery, Position, Relocation), featuring manual corrections and automated typo removal. Benchmark experiments on dialogue state tracking demonstrate significant improvements, with GPT-2's BLEU-4 score increasing from 0.1651 to 0.5604 compared to original IRWOZ. To support industrial HRI research, we publicly released IRWOZ 2.0 dataset at https://ieee-dataport.org/documents/irwoz-20-large-language-model-driven-dialogue-dataset-industrial-robot-conversations

摘要：IRWOZ 透過特定領域的註解改善了工業人機互動 (HRI) 對話系統。<lb/>然而，其初始版本在對話狀態和發話內容中包含大量噪音，限制了狀態追蹤的準確性。<lb/>我們推出了 IRWOZ 2.0，通過大型語言模型 (LLM) 增強生成 (Mistral/Claude-3.5) 和質量改進來解決這些限制。<lb/>我們改進的數據集擴展到 390 條對話，涵蓋 4 個工業領域（組裝、交付、定位、搬遷），並包含手動修正和自動拼寫錯誤移除。<lb/>對話狀態追蹤的基準實驗顯示出顯著的改進，GPT-2 的 BLEU-4 分數從 0.1651 增加到 0.5604，相較於原始的 IRWOZ。<lb/>為了支持工業 HRI 研究，我們在 https://ieee-dataport.org/documents/irwoz-20-large-language-model-driven-dialogue-dataset-industrial-robot-conversations 公開發布了 IRWOZ 2.0 數據集。

##### **Influence of Extruded Filament Shape on Buildability in 3D Concrete Printing: A Geometry-Informed Deep Learning-FEM Approach**
2609.04028v1 by Giacomo Rizzieri,  Saif-Ur-Rehman, Jörg F. Unger, Annika Robens-Radermacher

The geometric morphology of deposited filaments can significantly influence the structural performance and stability of 3D concrete-printed (3DCP) structures. However, most finite element (FEM)-based approaches for buildability assessment represent printed layers as simplified rectangles, potentially limiting predictive accuracy. This study proposes a geometry-informed modelling framework that integrates the deep-learning-based filament shape prediction tool ShapeGen3DCP with a layer-activation FEM approach to investigate the effect of realistic filament geometries on buildability. The framework generates geometry-aware numerical models directly from material and process parameters, eliminating the need for experimental filament characterization or computationally intensive fluid-flow simulations. Validation against experimental data and a parametric study of rectilinear walls demonstrate that extrusion parameters and the resulting filament geometry can significantly influence buildability predictions. Realistic filament representations are particularly important for free-flow deposition, whereas layer-pressing strategies are less sensitive to geometric simplifications. Among the investigated representations, an elliptical approximation provides an effective balance between geometric fidelity and modelling simplicity. When rectangular representations are preferred to enable regular computational meshes for faster simulations, defining their dimensions based on volume conservation improves prediction reliability compared with calibrating them using either the maximum filament width or the interlayer contact width. Overall, the proposed methodology demonstrates the importance of incorporating filament geometry into 3DCP simulations and provides practical guidance for selecting efficient and accurate geometric representations for buildability assessment.

摘要：沉積纖維的幾何形態可以顯著影響3D混凝土列印（3DCP）結構的結構性能和穩定性。<lb/>然而，大多數基於有限元素法（FEM）的可建性評估方法將列印層表示為簡化的矩形，這可能限制預測的準確性。<lb/>本研究提出了一種幾何信息驅動的建模框架，該框架將基於深度學習的纖維形狀預測工具ShapeGen3DCP與層激活FEM方法相結合，以研究現實纖維幾何形狀對可建性的影響。<lb/>該框架直接從材料和過程參數生成幾何感知的數值模型，消除了對實驗性纖維特徵描述或計算密集的流體流動模擬的需求。<lb/>與實驗數據的驗證以及對直線牆的參數研究表明，擠出參數和隨之產生的纖維幾何形狀可以顯著影響可建性預測。<lb/>現實的纖維表現對於自由流動沉積特別重要，而層壓策略對幾何簡化的敏感度較低。<lb/>在所研究的表示中，橢圓近似提供了幾何真實性和建模簡單性之間的有效平衡。<lb/>當偏好矩形表示以便於為更快的模擬啟用規則計算網格時，根據體積守恆定義其尺寸能改善預測的可靠性，相較於使用最大纖維寬度或層間接觸寬度進行校準。<lb/>總體而言，所提出的方法論展示了將纖維幾何納入3DCP模擬的重要性，並為選擇有效且準確的幾何表示以進行可建性評估提供了實用指導。

##### **Instruction Duplication as an Inference-Time Control Primitive**
2609.04024v1 by Victor Lavrenko

Procedural instruction following is a basic requirement for controllable language-model systems, especially when generated trajectories are inspected or repaired downstream. We introduce instruction duplication, a minimal black-box inference-time control that repeats only the procedural instruction, without retraining or decoding changes. Across seven instruction-tuned models, 300 medical multiple-choice questions, eight placement conditions, and 16,800 scheduled generations, moving from one to two copies raises the deterministic All-8 diagnostic--responses passing all eight observable tests--from 90.22% to 93.17% (+2.95 percentage points), eliminating 30.2% of the failures remaining after one copy. Pre-provisional TF-IDF recall rises from 73.44% to 74.81% (+1.38 points; Holm-adjusted p < .001), while final-answer accuracy remains exactly 60.21%. Premature commitment increases from 1.52% to 2.30% (p_Holm = .00536). A blinded challenge audit yields 10/30 directional confirmations, 20/30 perceptual ties, and no reversals; its prespecified 28/30 confirmation criterion is not met. Yet this distinction can matter operationally when a downstream system acts on the generated trajectory. In Answer Engineering (AE), where explicit trajectory state determines local repair, the published reason-first no-editing SSNHL endpoint was 25.1%; system-only AE was later reproduced at 84.2%, and the same trailing duplicate raised it to 97.1%. For conductive diagnostic branch preservation, the corresponding values are 58.9% published without editing, 78.6% with reproduced AE, and 73.8% with AE plus duplication--a within-AE decrease, but still 14.9 points above the no-editing baseline. Instruction duplication is therefore a low-complexity, placement-sensitive control whose practical value can emerge through the downstream system that consumes the exposed trajectory.

摘要：程序指令遵循是可控語言模型系統的基本要求，特別是在生成的軌跡被檢查或修復的情況下。<lb/>我們引入了指令重複，這是一種最小的黑箱推理時控制，僅重複程序指令，而不進行再訓練或解碼更改。<lb/>在七個經過指令調整的模型中，300個醫學多選題、八個放置條件以及16,800次預定生成中，從一個副本增加到兩個副本使得確定性的All-8診斷——所有八個可觀察測試的反應均通過——從90.22%提高到93.17%（+2.95個百分點），消除了在一個副本後剩餘的30.2%的失敗。<lb/>預先的TF-IDF召回率從73.44%上升到74.81%（+1.38點；Holm調整後p < .001），而最終答案的準確率保持在60.21%。<lb/>過早承諾從1.52%增加到2.30%（p_Holm = .00536）。<lb/>一項盲測挑戰審核產生了10/30的方向確認、20/30的感知平局，並且沒有逆轉；其預先指定的28/30確認標準未達成。<lb/>然而，當下游系統根據生成的軌跡行動時，這一區別在操作上可能很重要。<lb/>在答案工程（AE）中，明確的軌跡狀態決定了局部修復，已發表的理由優先無編輯SSNHL終點為25.1%；系統僅AE後來重現為84.2%，而相同的尾隨副本將其提高至97.1%。<lb/>對於導電診斷分支保留，相應的值為58.9%未編輯發表，78.6%重現AE，和73.8% AE加上重複——在AE內部的下降，但仍比無編輯基準高出14.9點。<lb/>因此，指令重複是一種低複雜度、敏感於放置的控制，其實際價值可以通過消耗暴露軌跡的下游系統顯現出來。

##### **Representational alignment yields generalizable safety in language models**
2609.04022v1 by Lingyu Li, Yan Teng, Yingchun Wang, Xia Hu

Aligning large language models (LLMs) is essential for their safe deployment. Current alignment methods mainly optimize observable responses, yet models remain vulnerable when the same harmful intent is recast in unfamiliar or adversarial forms that humans can easily recognize. Prototype theory offers an account of this adaptability. Human concepts are represented around central cases, and new instances are categorized according to their graded typicality relative to these prototypes. Here we show that such categorization of moral concepts is weakly preserved in current LLMs. Across 23 LLMs, models often failed to distinguish opposed moral categories or preserve fine-grained typicality within each category. These deficits persist across parameter sizes and alignment stages. We developed representational similarity optimization, which directly aligns the latent representations in LLMs with the categorization expressed in human moral judgements, without supervising generated responses. In matched experiments using the same 251,334 moral annotations, standard behavioral alignment learned the intended moral judgements at the response level while leaving the categorization structure largely unchanged and increasing vulnerability across adversarial evaluations. Reorganizing moral categorization produced more modest gains in explicit judgements but consistently improved adversarial robustness across model scales on diverse benchmarks and attack strategies. Our findings provide functional support for the view that prototype-based categorization contributes to behavioral adaptability. They also show that transferring this representational principle to LLMs yields generalizable safety under adversarial conditions.

摘要：對大型語言模型（LLMs）的對齊對其安全部署至關重要。當前的對齊方法主要優化可觀察的反應，但當相同的有害意圖以人類容易識別的不熟悉或對抗形式重新表達時，模型仍然脆弱。原型理論提供了這種適應性的解釋。人類概念圍繞著中心案例進行表徵，新的實例根據其相對於這些原型的分級典型性進行分類。在這裡，我們顯示這種道德概念的分類在當前的LLMs中是微弱保留的。在23個LLMs中，模型經常未能區分對立的道德類別或在每個類別內保持細緻的典型性。這些缺陷在不同的參數大小和對齊階段中持續存在。我們開發了表徵相似性優化，該方法直接將LLMs中的潛在表徵與人類道德判斷中表達的分類進行對齊，而不監督生成的反應。在使用相同的251,334條道德註釋的匹配實驗中，標準行為對齊在反應層面學習了預期的道德判斷，同時在很大程度上保持了分類結構不變，並在對抗評估中增加了脆弱性。重新組織道德分類在顯性判斷中產生了更為適度的增益，但在不同模型規模的多樣基準和攻擊策略上持續改善了對抗魯棒性。我們的研究結果為原型基礎的分類有助於行為適應性提供了功能支持。它們還顯示，將這一表徵原則轉移到LLMs上在對抗條件下產生了可泛化的安全性。

##### **FLY-EVAL++: An Evidence-Driven Evaluation Protocol for Safety-Constrained Flight Prediction with Large Language Models**
2609.04021v1 by Yalun Wu, Junfeng Fang, Jiawei Wang, Haotian Liu, Qijun Yang, Minghan Yang, Hongcheng Guo, Zhoujun Li, Boyang Wang

Evaluating large language models (LLMs) in safety-critical, physics-governed environments requires more than accuracy-based metrics, because predictions that are numerically close to the ground truth can still violate operational constraints, combine fields in physically inconsistent ways, or fail to produce usable structured outputs. Existing evaluation protocols do not measure these failure modes reliably. We propose FLY-EVAL++, an evidence-driven evaluation protocol that combines deterministic verification of protocol compliance, physical feasibility, and safety constraints with fixed rubric-guided aggregation into interpretable multi-dimensional scores. We instantiate FLY-EVAL++ for Flight Trajectory and Attitude Prediction (FTAP) by extending the PilotBench setting with history-conditioned and multi-step prediction tasks. Across 66 LLMs, safety compliance is the most discriminative dimension of model behavior: models with comparable predictive performance differ by more than 28 points in safety score, and we observe recurrent failures including safety violations under physically plausible predictions and instability in multi-step rollouts. These results show that evaluation in safety-critical domains should measure constraint satisfaction and structured validity explicitly rather than rely on accuracy-centric reporting alone.

摘要：評估在安全關鍵、物理約束環境中的大型語言模型（LLMs）需要的不僅僅是基於準確性的指標，因為數值上接近真實值的預測仍然可能違反操作約束、以物理不一致的方式結合領域，或無法產生可用的結構化輸出。現有的評估協議無法可靠地測量這些失敗模式。我們提出了 FLY-EVAL++，這是一種基於證據的評估協議，結合了對協議遵循性、物理可行性和安全約束的確定性驗證，以及固定評分標準指導的可解釋多維得分的聚合。我們通過擴展 PilotBench 設定，將歷史條件和多步預測任務納入，為飛行軌跡和姿態預測（FTAP）實現 FLY-EVAL++。在 66 個 LLM 中，安全合規性是模型行為中最具區分性的維度：具有可比預測性能的模型在安全得分上相差超過 28 分，我們觀察到反覆出現的失敗，包括在物理上合理的預測下的安全違規和多步展開中的不穩定性。這些結果表明，在安全關鍵領域的評估應明確測量約束滿足和結構有效性，而不僅僅依賴於以準確性為中心的報告。

##### **InSituMeasure: Probing Situated Measurement Grounding in Industrial Scenes with Multimodal Large Language Models**
2609.04014v1 by Chao Shen, Xinyuan Li, Yunfan Zhou, Jianguo Yao, Haibing Guan, Zhihai Wang, Xijun Li

For trained operators, gauge reading requires little specialized knowledge, low cognitive effort, and high repeatability. Yet Multimodal Large Language Models (MLLMs) remain unreliable in continuous-valued measurement despite strong results on general multimodal benchmarks. Existing benchmarks expose this weakness but isolate measurement from realistic, knowledge-grounded settings, with limited situated context, specialized instruments, real-world noise, and matched diagnostic annotations, reducing realism and constraining root-cause analysis. We introduce InSituMeasure to evaluate situated measurement grounding. It contains 2,922 real industrial monitoring scenes across eight functional categories of professional engineering instruments, with dense gauge-attribute annotations and noise tags for failure diagnosis. We define metrics for numerical accuracy under predefined tolerances and unit consistency, rejection of fake or unanswerable tasks, and alignment between model failures and annotated error factors. Across 24 state-of-the-art MLLMs, the best model reaches only 25.7\% joint value-unit accuracy and 51.8\% confidence-diagnosis F1, revealing a substantial gap between general multimodal competence and reliable situated measurement. Further analysis identifies failures from text-induced shortcuts, overconfident responses, and authentic industrial noise, including mixed disturbances, viewpoint deviation, occlusion, and environmental interference.

摘要：對於受過訓練的操作員來說，儀表讀數幾乎不需要專門知識，認知負擔低且重複性高。<lb/>然而，儘管在一般多模態基準測試中表現出色，多模態大型語言模型（MLLMs）在連續值測量方面仍然不可靠。<lb/>現有的基準測試揭示了這一弱點，但將測量與現實的、基於知識的環境隔離，缺乏有限的情境背景、專業儀器、現實世界的噪音和匹配的診斷註釋，降低了現實感並限制了根本原因分析。<lb/>我們引入了InSituMeasure來評估情境測量的基礎。<lb/>它包含2,922個真實的工業監控場景，涵蓋八個專業工程儀器的功能類別，並附有密集的儀表屬性註釋和故障診斷的噪音標籤。<lb/>我們定義了在預定公差和單位一致性下的數值準確性指標，拒絕虛假或無法回答的任務，以及模型失敗與註釋錯誤因素之間的對齊。<lb/>在24個最先進的MLLM中，最佳模型僅達到25.7%的聯合值-單位準確性和51.8%的信心-診斷F1，顯示出一般多模態能力與可靠的情境測量之間存在顯著差距。<lb/>進一步分析確定了由文本引起的捷徑、過度自信的反應和真實工業噪音造成的失敗，包括混合擾動、視點偏差、遮擋和環境干擾。

##### **LLM4CKD: Large Language Models for Early Stage Chronic Kidney Disease Screening**
2609.04013v1 by Muhammad Ashad Kabir, Sirajam Munira

Early screening of chronic kidney disease (CKD) is critical for timely intervention, yet most machine learning (ML) and deep learning (DL) approaches require labeled data and model training, limiting their use in real-world screening settings. This study evaluates the effectiveness of large language models (LLMs) for CKD screening under zero-shot and few-shot in-context learning settings and compares them with traditional ML and DL methods. We propose a framework that uses clinically selected tabular features and structured prompt templates to enable LLM-based inference without task-specific training. LLM performance is evaluated across multiple prompt styles, feature configurations, and data settings, and compared with standard ML, DL, and tabular foundation model (TFM) baselines, and existing CKD screening tools. The results show that LLMs can achieve competitive performance using only a small number of examples, often matching or outperforming traditional approaches in low-data settings. However, their performance remains model-dependent and less stable as input complexity increases. In contrast, ML, DL, and TFM models show more consistent improvement with larger training data. Overall, the findings highlight a trade-off between data efficiency and stability, suggesting that LLMs may serve as a flexible complementary approach for CKD screening when labeled data are limited.

摘要：早期篩檢慢性腎病（CKD）對於及時介入至關重要，但大多數機器學習（ML）和深度學習（DL）方法需要標記數據和模型訓練，限制了它們在現實篩檢環境中的使用。這項研究評估了大型語言模型（LLMs）在零樣本和少樣本上下文學習環境中進行CKD篩檢的有效性，並將其與傳統的ML和DL方法進行比較。我們提出了一個框架，利用臨床選擇的表格特徵和結構化提示模板，使基於LLM的推斷能夠在不進行特定任務訓練的情況下進行。LLM的性能在多種提示風格、特徵配置和數據設置中進行評估，並與標準的ML、DL和表格基礎模型（TFM）基準以及現有的CKD篩檢工具進行比較。結果顯示，LLMs在僅使用少量示例的情況下可以實現具有競爭力的性能，通常在低數據環境中與傳統方法相匹配或超越。然而，隨著輸入複雜性的增加，它們的性能仍然依賴於模型，且穩定性較差。相比之下，ML、DL和TFM模型在較大訓練數據下顯示出更一致的改進。總體而言，研究結果突顯了數據效率與穩定性之間的權衡，表明在標記數據有限的情況下，LLMs可能作為CKD篩檢的靈活補充方法。

##### **The Blind Spot in 2D Infants' Pose Estimation:Robust Learning from Noisy Annotations**
2609.04009v1 by Emanuele Cardinale, Marco Proietti, Alessandro Cacciatore, Maria Francesca Spadea, Lucia Migliorelli, Sara Moccia

Noisy annotations pose a significant challenge for supervised deep learning, as neural networks rely on large-scale, high-quality labeled data whose corruption can severely impair model performance. Although robustness to label noise has been extensively studied for classification tasks, it remains relatively underexplored in Pose Estimation (PE). This limitation becomes critical in clinical contexts, including neonatology, where PE of preterm infants is used to support the assessment of spontaneous motility, a key indicator of neurodevelopmental trajectories. In such settings, infants' images labeling is further hindered by visual challenges (e.g., keypoint self-occlusions, caregiver interference), making the annotation process inherently susceptible to errors. To tackle noisy annotations in PE, we introduce REliable keypoint selection via Memory of traINing Dynamics (REMIND), a clustering-based keypoint-selection strategy that exploits keypoint-wise training dynamics to identify noisy labels without assuming any prior knowledge of the noise distribution, thus enabling noise-free model training. When evaluated on the proprietary NeoPose dataset, comprising 46 videos of 46 preterm infants recorded in real clinical settings, REMIND correctly identifies noisy annotations across multiple corruption scenarios, achieving up to 93\% Area Under the Curve (AUC) with three different PE architectures used in the relevant literature. To our knowledge, this is the first study to explicitly address label noise in preterm infants' PE, paving the way for the design of trustworthy learning-based algorithms for infants'monitoring support when data quality cannot be guaranteed.

摘要：嘈雜的標註對於監督式深度學習構成了重大挑戰，因為神經網絡依賴於大規模、高質量的標記數據，而這些數據的損壞可能會嚴重影響模型性能。儘管對於分類任務，標籤噪聲的魯棒性已經得到了廣泛研究，但在姿勢估計（PE）中仍然相對未被深入探討。這一限制在臨床環境中變得至關重要，包括新生兒科，因為對早產兒的PE用於支持自發運動性的評估，而自發運動性是神經發展軌跡的關鍵指標。在這樣的環境中，嬰兒影像的標註進一步受到視覺挑戰（例如，關鍵點自遮擋、護理人員干擾）的阻礙，使得標註過程本質上容易出錯。為了解決PE中的嘈雜標註問題，我們提出了通過訓練動態記憶的可靠關鍵點選擇（REMIND），這是一種基於聚類的關鍵點選擇策略，利用關鍵點的訓練動態來識別嘈雜標籤，而不假設任何噪聲分佈的先驗知識，從而實現無噪聲的模型訓練。在對專有的NeoPose數據集進行評估時，該數據集包含46個在真實臨床環境中錄製的早產兒視頻，REMIND能夠在多種損壞場景中正確識別嘈雜標註，並在相關文獻中使用的三種不同PE架構下達到高達93%的曲線下面積（AUC）。據我們所知，這是第一項明確針對早產兒PE中的標籤噪聲的研究，為在數據質量無法保證的情況下設計可靠的基於學習的算法以支持嬰兒監測鋪平了道路。

##### **Catalogue Photography as a Cold Start: Toward Deployable Carbide Burr Recognition**
2609.03995v1 by Abilash Philip Madavath, Chandra Yuvesh Aubeeluck, Augustin Raju, Nicolas Pyschny, Felix Hackelöer, Florian Zwanzig

Verifying that manufactured batches of milling tools or carbide rotary burrs conform to production order sheets remains a largely manual and error-prone quality assurance task. Automating this process with computer vision faces a critical cold-start constraint since no labelled imagery is available, leaving manufacturer catalogue photography as the sole source of supervision. We investigate how far catalogue supervision can support an industrial recognition pipeline under domain shift, explicitly measuring the gap between catalogue separability and performance on held-out field photographs. Our findings reveal three key insights. First, off-the-shelf frozen feature extractors do not reliably separate the two task attributes, head shape and tooth profile, motivating targeted representation learning. Second, metric learning produces near-perfect unsupervised cluster discovery on catalogue images (adjusted Rand index 0.94--0.97), but less than half of this gain transfers to field photographs. Third, the largest transfer gains do not come from model scale or representation complexity, but from simple changes that reduce domain sensitivity: converting images to grayscale (+0.22) and constraining retrieval using the known order sheet via Hungarian assignment (+0.11). We therefore treat catalogue photography as a useful cold start rather than a deployment-ready training domain, and provide empirical baselines and an evaluation protocol for catalogue-to-field transfer in precision tool manufacturing.

摘要：驗證製造的磨具或硬質合金旋轉銑刀批次是否符合生產訂單仍然是一項主要依賴人工且容易出錯的質量保證任務。使用計算機視覺自動化此過程面臨著一個關鍵的冷啟動限制，因為沒有可標記的圖像可用，僅有製造商目錄攝影作為唯一的監督來源。我們調查了目錄監督在領域轉移下能支持工業識別管道的程度，明確測量目錄可分離性與保留的現場照片性能之間的差距。我們的研究結果揭示了三個關鍵見解。首先，現成的凍結特徵提取器無法可靠地區分兩個任務屬性，即頭部形狀和齒形，這促使我們進行針對性的表示學習。其次，度量學習在目錄圖像上產生了近乎完美的無監督聚類發現（調整的Rand指數為0.94--0.97），但不到一半的增益轉移到現場照片。第三，最大的轉移增益並不是來自模型規模或表示複雜性，而是來自簡單的改變，這些改變減少了領域敏感性：將圖像轉換為灰階（+0.22）和通過匈牙利分配使用已知訂單表來限制檢索（+0.11）。因此，我們將目錄攝影視為有用的冷啟動，而不是準備好部署的訓練領域，並提供了精密工具製造中目錄到現場轉移的實證基準和評估協議。

##### **Alignment-Free Text-Audiobox for Voice Dubbing and Full-Duplex Dialogue Synthesis**
2609.03992v1 by Sanyuan Chen, Min-Jae Hwang, Sho Inoue, Anna Sun, Bokai Yu, David Kant, Dongmin Hyun, Dorian Desblancs, Gregory Antonovsky, Oleg Repin, Peng-Jen Chen, Xutai Ma, Zehai Tu, Juan Pino, Wei-Ning Hsu

We present Alignment-Free Text-Audiobox (Text-AB), a unified framework for high-quality voice dubbing and full-duplex dialogue synthesis. Building on a Diffusion Transformer trained with a flow-matching objective, Text-AB departs from the Audiobox system along three dimensions. First, it operates in a latent diffusion framework using DAC-VAE features that encode 48 kHz waveforms into a 25 Hz latent sequence, giving over 10x higher compression than previous EnCodec representations while improving resynthesis quality. Second, Text-AB is alignment-free: it consumes raw text via an off-the-shelf text encoder and learns text-speech alignment through cross-attention, removing the need for forced alignment and explicit duration prediction. Third, we scale model and data substantially, pretraining a 3B-parameter model on 480k hours of monolingual speech, followed by supervised fine-tuning on three downstream tasks: cross-lingual voice dubbing, full-duplex dialogue synthesis, and emotional full-duplex dialogue synthesis. At inference, Text-AB supports one-shot generation for up to ~1 min of speech and arbitrarily long-form generation via a multi-diffusion scheme, plus a multi-stage reranking strategy that enhances quality based on automated metrics. On a real-world dubbing benchmark, Text-AB delivers a step-change improvement over the latest internal dubbing system, with large gains in prosody similarity, voice similarity, naturalness, and shareability. For full-duplex dialogue synthesis, it approaches human recordings on short-form conversations and substantially outperforms the latest internal model on long-form human-likeness and expressivity, while natively modeling turn-taking, back-channeling, and emotional dynamics. For emotional dialogue synthesis, emotion conditioning significantly improves emotion alignment and emotional interaction quality over the unconditioned baseline.

摘要：我們提出了無需對齊的文本音頻盒（Text-AB），這是一個統一的高品質語音配音和全雙工對話合成框架。基於使用流匹配目標訓練的擴散Transformer，Text-AB 在三個維度上脫離了音頻盒系統。首先，它在潛在擴散框架中運作，使用 DAC-VAE 特徵將 48 kHz 的波形編碼為 25 Hz 的潛在序列，提供超過 10 倍的壓縮率，並改善了再合成質量。其次，Text-AB 是無需對齊的：它通過現成的文本編碼器消耗原始文本，並通過交叉注意力學習文本與語音的對齊，消除了強制對齊和明確持續時間預測的需求。第三，我們大幅擴展模型和數據，對 480k 小時的單語言語音進行預訓練一個 3B 參數模型，然後在三個下游任務上進行監督微調：跨語言語音配音、全雙工對話合成和情感全雙工對話合成。在推理時，Text-AB 支持一次生成長達約 1 分鐘的語音，並通過多重擴散方案進行任意長度的生成，此外還有一種多階段重新排序策略，根據自動化指標提高質量。在一個現實世界的配音基準上，Text-AB 相較於最新的內部配音系統提供了顯著的改進，在韻律相似性、聲音相似性、自然度和可分享性方面都有大幅提升。對於全雙工對話合成，它在短形式對話中接近人類錄音，並在長形式的人類相似性和表現力方面大幅超越最新的內部模型，同時原生建模了輪流發言、回饋通道和情感動態。對於情感對話合成，情感條件顯著改善了情感對齊和情感互動質量，超過了無條件基準。

##### **IchthyoNoma: Nomenclature and Context Sensitivity of Zero-Shot Biological Vision--Language Models for Bangladeshi Freshwater Fish Recognition**
2609.03985v1 by  Nazim-E-Alam, Tarek Rahman, Md Kishor Morol

Zero-shot vision-language models (VLMs) are increasingly used as training-free species recognizers, but reported accuracy can reflect more than visual species knowledge. We audit CLIP, BioCLIP, BioCLIP2, and a multilingual Jina CLIP v2 control on seven freshwater-fish categories from two Bangladeshi sources (10,321 images). BioCLIP2 reaches 72.36% on BFF-15 with English common names and 68.91% on SylFishBD with scientific names, versus 25.15% and 14.40% for generic CLIP. BioCLIP2 Bengali prompts are near chance in balanced accuracy (14.22-14.29%); Jina partially recovers Bengali discrimination to 21.89% and 16.36%, but bare Bengali names return to 14.29% on both sources. Paired SylFishBD interventions show no significant weak-blur effect, modest losses from stronger blur/gray masking, a larger white-mask artifact, and strong species dependence. Zero-shot biological VLM scores therefore jointly reflect biological specialization, multilingual alignment, nomenclature, prompt formulation, and context.

摘要：零-shot 視覺語言模型（VLMs）越來越多地被用作無需訓練的物種識別器，但報告的準確性可能反映的不僅僅是視覺物種知識。<lb/>我們對 CLIP、BioCLIP、BioCLIP2 和多語言 Jina CLIP v2 控制進行了審核，涵蓋來自兩個孟加拉國來源的七個淡水魚類別（10,321 張圖片）。<lb/>BioCLIP2 在 BFF-15 上達到 72.36% 的準確率，使用英語通用名稱，而在 SylFishBD 上使用科學名稱的準確率為 68.91%，相比之下，通用 CLIP 的準確率為 25.15% 和 14.40%。<lb/>BioCLIP2 的孟加拉語提示在平衡準確率上接近隨機（14.22-14.29%）；Jina 部分恢復了孟加拉語的識別能力，達到 21.89% 和 16.36%，但純粹的孟加拉語名稱在兩個來源上均回落至 14.29%。<lb/>配對的 SylFishBD 介入顯示出沒有顯著的弱模糊效應，從較強的模糊/灰色遮罩中有適度損失，較大的白色遮罩工件，以及強烈的物種依賴性。<lb/>因此，零-shot 生物 VLM 的分數共同反映了生物專業化、多語言對齊、命名法、提示公式和上下文。

##### **Investigating the Ability of Large Language Models to Analyze Recipes for Diabetes**
2609.03967v1 by Revathy Venkataramanan, Aditya Luthra, Venkatesan Nadimuthu, Amit Sheth

Several studies have evaluated the ability of Large Language Models (LLMs) for meal planning, yielding positive outcomes. These models can process natural language inputs and leverage learned knowledge from their pretraining to generate meal plans. In this work, we investigate the ability of LLMs to analyze the suitability of given recipes for diabetes. The primary challenge for LLMs is to retrieve relevant dietary guidelines for diabetes, decompose recipes into ingredients and cooking methods, and apply these guidelines to determine the recipe's suitability. To study these challenges, we employ three kinds of prompts namely, (i) Direct Query Prompt (ii) Context-Guided Prompt, and (iii) Exemplary Context Prompt that incorporate different levels of diabetes dietary guidelines from medical sources. We introduce a benchmark dataset curated for this investigation consisting of 7607 recipes that include 3807 recipes suitable for diabetes and 3800 recipes not suitable for diabetes. Our results demonstrate that most LLMs are cautious in predicting recipes as suitable to prevent detrimental outcomes. Further, the models that can reason using the dietary guidelines performed better in predicting the suitability of recipes for diabetes. Overall, Mistral-7B and Llama 70B showed superior performance to their counterparts.

摘要：幾項研究評估了大型語言模型（LLMs）在餐點規劃方面的能力，結果顯示出正面的成果。這些模型能夠處理自然語言輸入，並利用其預訓練所學的知識來生成餐點計劃。在本研究中，我們探討LLMs分析給定食譜對糖尿病適用性的能力。LLMs面臨的主要挑戰是檢索與糖尿病相關的飲食指導方針，將食譜分解為成分和烹飪方法，並應用這些指導方針來判斷食譜的適用性。為了研究這些挑戰，我們使用三種提示，分別是（i）直接查詢提示（ii）上下文引導提示，以及（iii）示例上下文提示，這些提示包含來自醫學來源的不同層次的糖尿病飲食指導方針。我們引入了一個為此研究精心策劃的基準數據集，該數據集包含7607個食譜，其中包括3807個適合糖尿病的食譜和3800個不適合糖尿病的食譜。我們的結果顯示，大多數LLMs在預測食譜是否適合時都非常謹慎，以防止不良結果。此外，能夠運用飲食指導方針進行推理的模型在預測食譜對糖尿病的適用性方面表現更佳。總體而言，Mistral-7B和Llama 70B的表現優於其對手。

##### **Interface-Induced Trajectory Censoring**
2609.03966v1 by Wenbo Wang

Agent evaluations report a tool-call rate read off the serving stack. That number can be zero while the model is emitting well-formed calls: the interface censors the trajectory before anything downstream sees it.
  On BFCL v4's own data, executor and scorer, holding weights, cases, decoding and seeds fixed and changing only the serving adapter, the same model scores 0.00 or 0.96 / 0.19. A 2x2 over chat template and parser locates the effect exactly: both main effects are exactly zero and all of it sits in the interaction -- no component is defective, and repairing one side of the contract buys precisely nothing. On tau-bench's 115 interactive retail tasks the same swap moves server-parsed calls from 0 to 636 and tasks reaching any tool execution from 0 to 103. Our probe reproduces the funnel across a 21x scale range of Qwen2.5-Coder: the server parses 0/100 at every size while well-formed emitted calls rise to 80/100 at 32B (~72 after calibration against an adjudicated gold standard). Under a matched envelope, across a comparable scale span, the silent fraction stays at 0-2, a prediction committed to the repository before the run. Llama-3.1-8B's 23% rate of calling the task function itself as a tool falls to 0 under one strict:true flag.
  The mismatch reaches inside the training loop, and its consequence is scale-dependent: in verl's AgentLoop at 7B, 45 of 115 generations carry a complete call; 0 are accepted, 0 execute, 0 return an observation. At 1.5B the same zero is over-determined, so we report the two scales separately. At evaluation time, repairing the adapter restores the mechanism but not a significant outcome gain: parsing 0->84, rescues 0->9, pass rate 53->62 (n.s.). We release a 98-line preflight check that catches every silent failure here. The observed tool-call rate is not a property of the model alone; it is a property of the model-interface stack that measures it.

摘要：代理評估報告從服務堆疊中讀取的工具調用率。該數字可以是零，儘管模型正在發出格式良好的調用：介面在任何下游看到之前就已經審查了軌跡。<lb/>在 BFCL v4 自己的數據上，執行器和評分器，固定權重、案例、解碼和種子，只改變服務適配器，同一模型的得分為 0.00 或 0.96 / 0.19。一個 2x2 的聊天模板和解析器精確定位了效果：兩個主要效應都是零，所有效果都在交互中——沒有任何組件是有缺陷的，修復合同的一側並不會帶來任何好處。在 tau-bench 的 115 個互動零售任務中，相同的交換使服務器解析的調用從 0 增加到 636，任務達到任何工具執行的數量從 0 增加到 103。我們的探針在 Qwen2.5-Coder 的 21 倍範圍內重現了漏斗：服務器在每個大小下解析 0/100，而格式良好的發出調用在 32B 時上升至 80/100（在對經過裁定的金標準進行校準後約為 72）。在匹配的信封下，跨越可比較的範圍，靜默比例保持在 0-2，這是一個在運行之前提交到庫中的預測。Llama-3.1-8B 將任務函數本身作為工具調用的 23% 比率在一個 strict:true 標誌下降至 0。<lb/>不匹配的情況達到了訓練循環內部，其後果是依賴於規模的：在 verl 的 AgentLoop 中，7B 的 115 次生成中有 45 次攜帶完整調用；0 被接受，0 被執行，0 返回觀察。在 1.5B 時，這個零過度確定，因此我們分別報告這兩個規模。在評估時，修復適配器恢復了機制，但沒有顯著的結果增益：解析 0-&gt;84，救回 0-&gt;9，通過率 53-&gt;62（不顯著）。我們發布了一個 98 行的預檢查，捕捉到這裡的每一個靜默失敗。觀察到的工具調用率並不是模型單獨的屬性；它是測量模型-介面堆疊的屬性。

##### **FiMI Banking: A Sovereign Model for Indian Retail Banking**
2609.03960v1 by  NPCI AI Research Team, Aman Kumar, Asit Desai, Chandra Bhushan, Harsh Sharma, Harshit Bhushan, Hrithik Kadam, Keyur Doshi, Kolisetty Sai Kapardheeswar, Krishanu Adhikary, Nadeem Shaik, Navya Prakash, Nitin Kukreja, Prashant Devadiga, Shamanth MH, Shantanu Pandey, Suvradip Paul, Yatharth Dedhia

Banks need conversational systems that can answer product questions, assist customers with account-related requests, and operate safely within strict operational and regulatory constraints. General-purpose language models do not reliably meet these requirements. They fall short when a task requires grounded information, correct tool use, or cautious handling of bank-specific sensitive situations. We introduce FiMI Banking, a controlled Indian retail-banking setting. We build it from vetted banking documents, structured ground truth, synthetic customer backgrounds, and banking tools. We evaluate two post-training approaches: preference optimization for response-level behavior, and reinforcement learning with verifiable rewards for multi-turn tool-use tasks. Preference optimization improves safe behavior substantially: out-of-scope refusal rises from 52% to 80%. Reinforcement learning improves edge-case performance from 0.509 to 0.718 and order-sensitive task performance from 0.590 to 0.679, while using 29% fewer generated tokens. These results show that preference optimization and verifiable-reward reinforcement learning address complementary requirements for reliable banking agents.

摘要：銀行需要能夠回答產品問題、協助客戶處理帳戶相關請求，並在嚴格的操作和監管約束內安全運作的對話系統。通用語言模型無法可靠地滿足這些要求。當任務需要基於事實的信息、正確的工具使用或謹慎處理銀行特定的敏感情況時，它們表現不佳。我們介紹了 FiMI Banking，一個受控的印度零售銀行環境。我們從經過審核的銀行文件、結構化的真實數據、合成的客戶背景和銀行工具中構建它。我們評估了兩種後訓練方法：針對回應級行為的偏好優化，以及針對多輪工具使用任務的可驗證獎勵的強化學習。偏好優化顯著改善了安全行為：超出範疇的拒絕率從 52% 上升到 80%。強化學習將邊緣案例的表現從 0.509 提升到 0.718，將對順序敏感的任務表現從 0.590 提升到 0.679，同時使用的生成標記減少了 29%。這些結果顯示，偏好優化和可驗證獎勵的強化學習滿足了可靠銀行代理的互補需求。

##### **RARF: Region-Aware Rectified Flows for 3D Brain MRI Inpainting**
2609.03956v1 by Tomas Guija-Valiente, Blanca Rodriguez-Gonzalez, Norberto Malpica, Angel Torrado-Carvajal

Medical image inpainting has the potential to improve automated brain MRI analysis by reconstructing healthy tissue within pathological regions. We introduce RARF, a task-agnostic region-aware rectified flow framework for masked data generation. We instantiate the framework for 3D brain MRI inpainting as our submission to the BraTS Inpainting Challenge 2026. RARF restricts the stochastic interpolation process to the inpainting region, while the observed voxels remain fixed and provide patient-specific anatomical context. A three-dimensional neural network receives the partially voided image, with Gaussian noise filling the missing region, together with the inpainting mask and the corresponding timestep. The model is trained using masked flow-matching and reconstruction-consistency objectives, combined with mask-aware preprocessing and data augmentation. During inference, the learned velocity field transports the initial noise toward a plausible reconstruction of the missing tissue, which is then combined with the unchanged observed anatomy. Experiments under the BraTS evaluation protocol show that the proposed approach produces competitive reconstructions while maintaining anatomical consistency. Source code is available at: https://github.com/TomasGuija/rarf.

摘要：醫學影像修補有潛力透過重建病理區域內的健康組織來改善自動化腦部MRI分析。我們介紹RARF，一種任務無關的區域感知校正流框架，用於生成遮罩數據。我們將該框架實例化為3D腦部MRI修補，作為我們對BraTS修補挑戰2026的提交。RARF將隨機插值過程限制在修補區域內，同時觀察到的體素保持不變並提供特定於患者的解剖學背景。一個三維神經網絡接收部分缺失的圖像，缺失區域由高斯噪聲填充，並與修補遮罩和相應的時間步驟一起處理。該模型使用遮罩流匹配和重建一致性目標進行訓練，並結合了遮罩感知的預處理和數據增強。在推理過程中，學習到的速度場將初始噪聲傳輸到缺失組織的合理重建，然後與未改變的觀察解剖結合。根據BraTS評估協議的實驗表明，所提出的方法在保持解剖一致性的同時產生了具有競爭力的重建。源代碼可在以下網址獲得：https://github.com/TomasGuija/rarf。

##### **Two-Stage Reinforcement Learning for Sound and Adversarial Test Generation in Code LLMs**
2609.03955v1 by Jiacheng Xu, Wentao Zhang, Zhiyi Lyu, Fuxiang Zhang, Chaojie Wang, Yang Liu, Bo An

Reinforcement learning (RL) has substantially advanced code generation with large language models (LLMs) through executable feedback. The feedback for coding problems mainly comes from specific test cases, where high-quality test cases are often scarce since they should be both sound and discriminative. We thus turn to study the auto-generation of test cases using the learned model. We find this is naturally an adversarial RL problem: the model is expected to generate effective test cases as counterexamples, depending on the solver's current failure modes. We propose Test Cases Scaling (TCS), a two-stage RL framework for effective test generation. Both stages train a test generator from a rolling policy-aligned buffer: Stage 1 generates tests consistent with the reference solution, and Stage 2 restricts the buffer to current failure modes and learns counterexample tests. Across TACO and LiveCodeBench, TCS improves both pass@1 and inference-time answer selection according to generated tests. We find the learned test generator also enables effective selection among other LLM outputs.

摘要：強化學習（RL）通過可執行的反饋顯著推進了大型語言模型（LLMs）的代碼生成。編碼問題的反饋主要來自特定的測試案例，其中高質量的測試案例往往稀缺，因為它們應該既是正確的又具有區分性。因此，我們轉向研究使用學習模型的自動生成測試案例。我們發現這自然是一個對抗性RL問題：模型預期生成有效的測試案例作為反例，這取決於求解器當前的失敗模式。我們提出了測試案例擴展（TCS），這是一個兩階段的RL框架，用於有效的測試生成。兩個階段都從滾動策略對齊的緩衝區訓練測試生成器：第一階段生成與參考解決方案一致的測試，第二階段將緩衝區限制為當前失敗模式並學習反例測試。在TACO和LiveCodeBench中，TCS根據生成的測試改善了pass@1和推理時間答案選擇。我們發現學習的測試生成器還能有效地在其他LLM輸出中進行選擇。

##### **Beyond Majority Vote: Multi-Perspective Adjudication for Medical Hallucination Detection**
2609.03953v1 by Joe Cecil, Marjorie Freedman

Understanding the frequency of factual errors in chatbot-generated text and evaluating systems that detect these errors is critical for determining chatbot safety. Yet factual-error detection is often treated as a single-pass, single-annotator labeling problem. In long-form chatbot responses, factual errors can be subtle and embedded within mostly correct text.
  We develop a multi-perspective annotation study of medically relevant chatbot responses, combining first-pass annotation, LLM-as-a-Judge (LaJ) candidate discovery, and two forms of adjudication: medical-expert and evidence-based fact-checking. First-pass annotators frequently miss factual errors later validated by adjudicators. LaJ improves candidate discovery, but is insufficient on its own: It misses factual errors that annotators catch. We also find disagreement among adjudicators, suggesting that adjudication over multiple candidate sources can improve benchmark completeness, but does not eliminate the need to apply judgment and expertise. Applied to an existing benchmark, this technique reveals a similar pattern of missing annotations. Together, these results suggest that in the settings examined here, single-pass hallucination benchmarks may achieve scale at the cost of undercounting factual errors. Multi-pass adjudication can improve coverage, but inferences drawn from the benchmarks are still sensitive to the judgment, expertise, and evidence used to determine error presence.

摘要：理解聊天機器人生成文本中的事實錯誤頻率以及評估檢測這些錯誤的系統對於確定聊天機器人的安全性至關重要。 然而，事實錯誤檢測通常被視為單次通過、單一標註者的標記問題。 在長篇聊天機器人回應中，事實錯誤可能是微妙的，並嵌入在大部分正確的文本中。<lb/> 
我們開發了一項多角度的標註研究，針對醫療相關的聊天機器人回應，結合了首次標註、LLM作為裁判（LaJ）候選者發現以及兩種裁決形式：醫療專家和基於證據的事實核查。 首次標註者經常錯過後來由裁決者驗證的事實錯誤。 LaJ 改善了候選者的發現，但僅靠它是不夠的：它錯過了標註者能捕捉到的事實錯誤。 我們還發現裁決者之間存在分歧，這表明對多個候選來源的裁決可以改善基準的完整性，但並不能消除應用判斷和專業知識的必要性。 應用於現有基準時，這一技術揭示了缺失標註的類似模式。 總的來說，這些結果表明，在此處檢查的設置中，單次通過的幻覺基準可能以低估事實錯誤為代價實現規模。 多次裁決可以改善覆蓋率，但從基準中得出的推論仍然對用於確定錯誤存在的判斷、專業知識和證據敏感。

##### **VestigeKV: The NoPE-MLA KV Cache Carries Its Own Eviction Signal in a Vestigial Branch**
2609.03949v1 by WenJie Fan

The problem. A long-lived KV cache must be compressed before the queries that will read it exist; selection by observed attention (H2O, SnapKV) collapses there (0.00-0.33 needle retrieval on a NoPE MLA model), because a token's importance has not yet been observed. The method. On Kimi Linear, VestigeKV evicts by a query-independent signal the cache already carries: the 64-dimensional decoupled branch, a vestige of RoPE that NoPE training repurposes into a salience channel. Reading 11% of each row, it partitions the cache: the top-m rows stay in the attended tier; every other row moves -- exactly, never deleted -- to a GPU-resident archive reachable per step by a certified trigger. No training, no quantization, no weight or kernel change. Cost. Nothing measurable: retrieval holds at 1.00 under 8x and 0.92 under 32x from 8k to 65k context, zero gap to full-row selection. The attended tier is 0.25 KB of Kimi Linear's 8.1 KB per-token cache at 32x; the archive stays bit-exact and GPU-resident, with host offload as the VRAM-reclaiming variant. The recall tier -- the standard configuration -- holds 128x at 1.00. Kimi K3 is reported to use a NoPE Gated-MLA variant; if its cache layout matches, the method plausibly extends there -- we make no claim beyond the measured model. NoPE exclusivity. The identical operator on a RoPE MLA collapses to 0.08 (plain eviction: 0.42); query-independent salience itself exists only without rotation (top-1 targets span 2.3-6.7% of tokens vs. 10.2-46.8%), and query-universal exact merging is provably impossible under RoPE. All thresholds were frozen before data; 20 archived verdicts and 8 closed routes accompany the paper.

摘要：問題。長期存在的 KV 快取必須在查詢存在之前進行壓縮；通過觀察到的注意力進行選擇（H2O，SnapKV）在那裡崩潰（在 NoPE MLA 模型上，針對 0.00-0.33 的針對性檢索），因為一個標記的重要性尚未被觀察到。方法。在 Kimi Linear 上，VestigeKV 透過查詢無關的信號驅逐快取，該信號已經存在於快取中：64 維的解耦分支，RoPE 的一個殘餘，NoPE 訓練將其重新用於顯著性通道。閱讀每一行的 11%，它將快取分區：前 m 行保留在被注意的層級；每其他行移動——精確地，從不刪除——到一個每步可通過經過認證的觸發器到達的 GPU 常駐檔案。無需訓練，無需量化，無需權重或內核變更。成本。沒有可測量的東西：在 8 倍下檢索保持在 1.00，並在 32 倍下保持在 0.92，從 8k 到 65k 的上下文，與完整行選擇之間沒有差距。被注意的層級在 32 倍時是 Kimi Linear 每標記快取的 0.25 KB 的 8.1 KB；檔案保持位元精確且 GPU 常駐，主機卸載作為 VRAM 回收變體。回憶層級——標準配置——在 1.00 下保持 128 倍。據報導，Kimi K3 使用 NoPE Gated-MLA 變體；如果其快取佈局匹配，該方法合理地在那裡擴展——我們不對測量模型之外的內容提出任何主張。NoPE 獨佔性。在 RoPE MLA 上，相同的運算子崩潰至 0.08（普通驅逐：0.42）；查詢無關的顯著性本身僅在沒有旋轉的情況下存在（前 1 的目標跨越 2.3-6.7% 的標記對比 10.2-46.8%），而查詢通用的精確合併在 RoPE 下被證明是不可能的。所有閾值在數據之前已經凍結；20 個存檔裁決和 8 條關閉路徑隨著論文一起附上。

##### **Headroom-Drift Replay: A Primitive for Principled Replay Control in GRPO**
2609.03941v1 by Hyun Bin Park, Du-Seong Chang

RL-based post-training for reasoning models is increasingly bottlenecked by repeated fresh rollout generation, particularly in agentic settings where environment interaction dominates wall-clock cost. Replay can reduce this burden by reusing past trajectories, but existing methods typically embed it within larger training pipelines involving exploration, experience restructuring, or mixed-policy optimization. This makes replay's own contribution difficult to isolate. We ask a focused question: how far can principled replay selection alone go? We introduce Headroom-Drift Replay, a group-level replay control primitive for GRPO that separates reuse into two decisions. Headroom ranks stored groups by remaining learning value, while Drift gates them by compatibility with the current policy. The fresh on-policy stream remains unchanged, and the method adds no auxiliary generation or training machinery. Across mathematical reasoning, multimodal reasoning, and Agentic Search benchmarks, this single intervention outperforms naive replay and matches or exceeds broader replay methods on Avg Mean@32. In Agentic Search, where environment interaction dominates cost, it delivers comparable quality at materially lower wall-clock time.

摘要：基於強化學習的推理模型後訓練越來越受到重複生成新展開的瓶頸影響，特別是在環境互動主導牆時計成本的代理設置中。重播可以通過重用過去的軌跡來減輕這一負擔，但現有方法通常將其嵌入更大的訓練流程中，涉及探索、經驗重組或混合策略優化。這使得重播自身的貢獻難以孤立。我們提出一個聚焦問題：原則性的重播選擇能走多遠？我們引入了Headroom-Drift重播，這是一種針對GRPO的群體級重播控制原語，將重用分為兩個決策。Headroom根據剩餘學習價值對存儲的群體進行排序，而Drift則根據與當前策略的兼容性對其進行篩選。新的在政策流保持不變，該方法不增加任何輔助生成或訓練機制。在數學推理、多模態推理和代理搜索基準測試中，這一單一干預超越了天真的重播，並在Avg Mean@32上與更廣泛的重播方法相匹配或超過。在代理搜索中，環境互動主導成本，它以顯著較低的牆時計時間提供了可比的質量。

##### **Masked Autoregressive Speech Enhancement with Continuous Neural Audio Codec Representations**
2609.03940v1 by Yoto Fujita, Simon Leglaive, Laurent Girin

Most previous work on speech enhancement (SE) based on masked generative modeling relied on discrete token representations of audio signals, obtained using neural audio codecs (NACs). However, a recent study has shown that continuous latent representations of NACs can be advantageous for SE in terms of speech quality and intelligibility. In this work, we propose masked autoregressive SE (MARSE), a method for SE based on iterative decoding of masked clean speech frames using continuous NAC representations of speech. In particular, we investigate a set of different decoding policies, ceteris paribus, that is, using the same DNN (a Conformer model), the same NAC (the DAC codec) and the same training setup. The results show that MARSE enables a flexible trade-off between SE performance and computational cost. Audio examples and code are available online.

摘要：大多數基於遮罩生成建模的語音增強（SE）先前的研究依賴於使用神經音頻編解碼器（NAC）獲得的音頻信號的離散標記表示。<lb/>然而，最近的一項研究顯示，NAC的連續潛在表示在語音質量和可懂性方面對SE是有利的。<lb/>在本研究中，我們提出了遮罩自回歸SE（MARSE），這是一種基於使用語音的連續NAC表示對遮罩乾淨語音幀進行迭代解碼的SE方法。<lb/>特別是，我們研究了一組不同的解碼策略，其他條件不變，即使用相同的DNN（Conformer模型）、相同的NAC（DAC編解碼器）和相同的訓練設置。<lb/>結果顯示，MARSE能夠在SE性能和計算成本之間實現靈活的權衡。<lb/>音頻範例和代碼可在線獲得。

##### **RATL: Learning from Retrieved Residuals for Robust Multivariate Time-Series Forecasting**
2609.03937v1 by Yuchen He, Yueyang Cang, Zhiyuan Ning, Ningyu Wang, Li Shi

Retrieval-augmented generation (RAG) complements parametric models with retrieved external evidence. The same idea is attractive for continuous-output regression, but directly reusing retrieved target values is often not robust when samples differ in output level, numerical scale, or local dynamics. Moreover, conventional forecasting pipelines generally use residuals for model optimization and error diagnosis, but do not retain individual historical residual examples as memory that can be accessed at inference time.For multivariate time-series forecasting, we propose RATL, a plug-in residual-retrieval and feedback-correction method. RATL freezes a base forecaster to construct retrieval keys and turns its historical forecast residuals into a train-only memory specific to that base model. At inference time, RATL retrieves residual trajectories from similar historical contexts subject to causal availability constraints, then uses a set-aware router operating over forecast blocks and variables to select and combine these trajectories. Experiments show that historical residuals matched to the current context contain reusable forecasting information and that RATL improves frozen base forecasters in most experimental settings. Ablations further show that learned routing strengthens raw residual feedback, while validation-based correction-strength selection limits residual over-injection.On real-world benchmarks, we use iTransformer as the primary frozen base forecaster, compare against multiple strong forecasting baselines, and test transferability across backbones. The results show that RATL can further improve base-forecaster performance in most settings.Overall, RATL shifts the retrieved object from historical target values to base-model-specific historical forecast errors, providing a plug-in, residual-memory-based paradigm for learned feedback correction in continuous-output forecasting.

摘要：檢索增強生成（RAG）通過檢索外部證據來補充參數模型。這一想法對於連續輸出回歸來說也很有吸引力，但在樣本在輸出水平、數值尺度或局部動態上有所不同時，直接重用檢索到的目標值往往不夠穩健。此外，傳統的預測流程通常使用殘差進行模型優化和錯誤診斷，但不會將個別歷史殘差示例保留為可以在推理時訪問的記憶。對於多變量時間序列預測，我們提出了RATL，一種插件式殘差檢索和反饋修正方法。RATL 凍結一個基礎預測器以構建檢索鍵，並將其歷史預測殘差轉化為僅限於該基礎模型的訓練記憶。在推理時，RATL 從類似的歷史上下文中檢索殘差軌跡，受因果可用性約束的限制，然後使用一個在預測塊和變量上運作的集aware路由器來選擇和組合這些軌跡。實驗顯示，與當前上下文匹配的歷史殘差包含可重用的預測信息，並且RATL在大多數實驗設置中改善了凍結的基礎預測器。消融實驗進一步顯示，學習的路由增強了原始殘差反饋，而基於驗證的修正強度選擇則限制了殘差的過度注入。在現實世界的基準測試中，我們使用iTransformer作為主要的凍結基礎預測器，與多個強大的預測基準進行比較，並測試在不同骨幹之間的可轉移性。結果顯示，RATL可以在大多數設置中進一步改善基礎預測器的性能。總體而言，RATL將檢索對象從歷史目標值轉變為基礎模型特定的歷史預測錯誤，為連續輸出預測中的學習反饋修正提供了一種插件式的基於殘差記憶的範式。

##### **Fixed Suffix Dependency Ratio: Quantifying the Dual-Track Mechanism of Gender Assignment in Latvian Loanwords**
2609.03930v1 by Yelingyun Zhang, Atis Kapenieks, Marina Platonova

Existing research has repeatedly observed the tendency for English loanwords to cluster in the masculine gender across different recipient languages, yet the origin of this pattern remains difficult to determine, as fixed morphological rules and default assignments are frequently analysed together. This study proposes the Fixed Suffix Dependency Ratio (FSDR) to quantify the degree of reliance on fixed derivational suffixes across different genders, and to distinguish between morphological anchoring and free-choice in distribution. By examining 1,832 Latvian noun lemma types, the results reveal a significant FSDR asymmetry within the loanword system: feminine loanwords rely significantly more on fixed derivational suffixes, while masculine loanwords are more concentrated in the free-choice zone. This pattern exhibits loanword specificity and has become more pronounced in contemporary usage. FSDR therefore provides a quantitative framework for testing default gender and shows how masculine default can be activated and reinforced under language contact.

摘要：現有研究反覆觀察到英語外來詞在不同接受語言中傾向於集中於男性性別，然而這一模式的來源仍然難以確定，因為固定的形態規則和默認分配經常被一起分析。本研究提出了固定後綴依賴比率（FSDR），以量化不同性別之間對固定衍生後綴的依賴程度，並區分形態錨定和分配中的自由選擇。通過檢查1,832個拉脫維亞名詞詞條類型，結果顯示在外來詞系統中存在顯著的FSDR不對稱性：女性外來詞對固定衍生後綴的依賴顯著更高，而男性外來詞則更集中於自由選擇區域。這一模式顯示了外來詞的特異性，並在當代用法中變得更加明顯。因此，FSDR提供了一個量化框架，用於測試默認性別，並顯示在語言接觸下男性默認如何被激活和加強。

##### **Speak for Me: Giving LLMs the Situational Awareness to Participate in a Meeting**
2609.03923v1 by Muneeb Khan, Frederic Kirstein, Terry Ruas, Bela Gipp

In online meeting delegation, LLM agents fail to recognize when to speak. With no structured way to track stances, coverage, and floor, they miss the moments where they should contribute. Prompt-only delegates stay silent on 51.4% of the absent participant's talking opportunities on the AMI corpus. We present CAPA (Collaborative Agent Predictive Architecture), an architecture for online meeting delegation. A Perceiver updates the meeting state from each observed turn. A Predictor forecasts how the conversation will continue. A Controller decides whether to speak and which proposition to surface. A Generator phrases the chosen contribution in the participant's style. Two judges score the forecast and the action against the next observed turn. A Recalibrator updates the meeting state from those verdicts for future decisions. To evaluate online delegation, we introduce an episode-level protocol that scores whether, when, and what a delegate contributes around the participant's actual idea units. The protocol's schema-constrained LLM judges align with human annotations at Cohen's kappa = 0.71. On 137 AMI meetings, CAPA reduces the silence rate from 51.4% to 2.5%, doubles credited recovery (26.1 --> 52.2), and keeps hallucination at 0.6%. The failure mode shifts from omission to selection, with each residual near-miss attributable to a specific module of the architecture. Mechanism ablations identify the meeting state as the lever that closes the recognition gap, where raw-context scaling alone does not.

摘要：在在線會議委派中，LLM 代理無法識別何時發言。由於沒有結構化的方法來跟蹤立場、覆蓋範圍和發言權，他們錯過了應該貢獻的時刻。僅依賴提示的代表在 AMI 語料庫中對缺席參與者的談話機會保持沉默達 51.4%。我們提出了 CAPA（協作代理預測架構），這是一種在線會議委派的架構。一個感知器從每個觀察到的回合更新會議狀態。一個預測器預測對話將如何繼續。一個控制器決定是否發言以及提出哪個主張。一個生成器以參與者的風格措辭所選的貢獻。兩位評審根據下一個觀察到的回合對預測和行動進行評分。一個重新校準器根據這些判決更新會議狀態以便未來的決策。為了評估在線委派，我們引入了一個事件級別的協議，評分代表在參與者的實際思想單位周圍貢獻的是否、何時和什麼。該協議的架構約束 LLM 評審與人類註釋的對齊程度為 Cohen's kappa = 0.71。在 137 次 AMI 會議中，CAPA 將沉默率從 51.4% 降低到 2.5%，使得獲得的恢復翻倍（26.1 --&gt; 52.2），並將幻覺保持在 0.6%。失敗模式從遺漏轉變為選擇，每個殘留的近失都可歸因於架構的特定模塊。機制消融確定會議狀態是縮小識別差距的杠杆，而僅僅依賴原始上下文的擴展則無法達到。

##### **Value-Preserving Architectures for Agentic AI Systems**
2609.03920v1 by Alessandro Pesare, Tommaso Dolci, Katja Hose, Emanuel Sallinger

The emergence of agentic AI and LLM-based multi-agent systems (MAS) presents unprecedented opportunities for automating complex tasks, while simultaneously raising critical concerns about the preservation of fundamental human-centered values, such as privacy, fairness, and safety. Although software engineering has traditionally focused on functional correctness, the adoption of LLMs and AI agents into complex socio-technical systems has intensified the need for responsible software engineering and robust value alignment. In MAS, architectural design decisions, such as coordination mechanisms, communication protocols, and system topologies, play a central role in shaping system behavior and the outcomes they produce. This paper argues that architectural choices influence not only the functionality and performance of MAS but can also promote value-oriented system behavior. Therefore, we investigate how different architectural designs support different human-centered values, discussing the following value-preserving architectural patterns: (i) a privacy-aware architecture with a federated topology, (ii) a distributed architecture to promote pluralism and diversity, and (iii) a guard-agent architecture to detect and mitigate unfairness. Finally, we introduce representative use cases to illustrate the proposed architectures in real-world scenarios. By linking architectural design with human-centered values, this work lays the foundation for a unified set of architectural patterns and guidelines towards the design of trustworthy MAS.

摘要：代理 AI 和基於 LLM 的多代理系統 (MAS) 的出現為自動化複雜任務提供了前所未有的機會，同時也引發了對於保護基本以人為中心的價值觀（如隱私、公平和安全）的重大關注。儘管軟體工程傳統上專注於功能正確性，但將 LLM 和 AI 代理納入複雜的社會技術系統中，加強了對負責任的軟體工程和穩健價值對齊的需求。在 MAS 中，架構設計決策，例如協調機制、通信協議和系統拓撲，在塑造系統行為及其產出結果中扮演著核心角色。本文主張，架構選擇不僅影響 MAS 的功能和性能，還可以促進以價值為導向的系統行為。因此，我們調查不同的架構設計如何支持不同的人本價值，討論以下價值保護的架構模式：（i）具有聯邦拓撲的隱私意識架構，（ii）促進多元化和多樣性的分散架構，以及（iii）檢測和減輕不公平的守護代理架構。最後，我們介紹代表性的使用案例，以說明所提議的架構在現實世界場景中的應用。通過將架構設計與以人為中心的價值聯繫起來，本研究為設計值得信賴的 MAS 奠定了統一的架構模式和指導方針的基礎。

##### **RuleMem: Active Rule Memory for Long-Term Conversational Agents**
2609.03915v1 by Xingyuan Zeng, Zuohan Wu, Quanming Yao, Yue Wang, Wei Liu, Libin Zheng, Jiuke Wang, Jian Yin

Question answering agents in long-term conversations must reason over massive, temporally dispersed dialogue histories. However, existing memory mechanisms primarily treat past information as \textit{passively} stored facts, leading to semantic gaps and unreliable reasoning. To address this limitation, we propose RuleMem, a rule-based memory framework that induces reusable logical rules from historical interactions to \textit{actively} guide both evidence retrieval and reasoning. Specifically, RuleMem constructs natural-language Horn clauses from conversations and validates them via a Rule Perplexity Consistency (RPC) mechanism. These induced rules enable the retrieval of semantically distant evidence while providing an explicit logical structure for answer generation. We conducted a comprehensive evaluation of RuleMem on two long-term conversational benchmarks, LoCoMo and LongMemEval_s*. In a rigorous comparison against 14 baselines on LoCoMo, RuleMem achieved the highest accuracy, exceeding the baseline average by 27.47 points (a 54.3% relative improvement).

摘要：長期對話中的問答代理必須對大量時間上分散的對話歷史進行推理。然而，現有的記憶機制主要將過去的信息視為\textit{被動}存儲的事實，這導致了語義上的差距和不可靠的推理。為了解決這一限制，我們提出了RuleMem，一種基於規則的記憶框架，從歷史互動中誘導可重用的邏輯規則，以\textit{主動}指導證據檢索和推理。具體而言，RuleMem從對話中構建自然語言的霍恩子句，並通過規則困惑度一致性（RPC）機制進行驗證。這些誘導的規則使得檢索語義上遙遠的證據成為可能，同時為答案生成提供了明確的邏輯結構。我們對RuleMem在兩個長期對話基準LoCoMo和LongMemEval_s*上進行了全面評估。在對LoCoMo的14個基準進行嚴格比較中，RuleMem達到了最高的準確率，超過基準平均值27.47分（相對提高54.3%）。

##### **CROCODIL: Cross-Model Code Editing with LLMs**
2609.03894v1 by Linghan Zhong, Aditya Thimmaiah, Jayanth Srinivasa, Milos Gligoric, Junyi Jessy Li

Large language models (LLMs) have become ubiquitous tools for code generation and editing. However, development teams often use multiple LLM assistants. Different developers may prefer different models, and individual developers may switch between models across different coding sessions. Because of this, the edits any one model makes are frequently applied to foreign code originally generated by another model. These LLMs are often trained on different datasets, and as a result have different stylistic preferences. Do LLMs behave differently when they edit foreign code originally written by a different LLM with a different coding style? We find that models tend to make more, and often excessive, edits on foreign code. We introduce CROCODIL (Cross-model Code Editing with LLMs), a post-training framework for reducing excessive edits while preserving functional correctness. CROCODIL's similarity reward penalizes large changes, while its execution reward scores build and test success. We use the product of these two rewards to encourage the policy to decrease the edit size without decreasing the edit task success rate. CROCODIL is available at https://github.com/EngineeringSoftware/Crocodil.

摘要：大型語言模型（LLMs）已成為代碼生成和編輯的普遍工具。<lb/>然而，開發團隊經常使用多個 LLM 助手。<lb/>不同的開發者可能偏好不同的模型，而個別開發者可能在不同的編碼會話中切換模型。<lb/>因此，任何一個模型所做的編輯經常應用於最初由另一個模型生成的外部代碼。<lb/>這些 LLM 通常是在不同的數據集上進行訓練的，因此具有不同的風格偏好。<lb/>當 LLM 編輯最初由不同的 LLM 以不同編碼風格撰寫的外部代碼時，LLM 的行為是否會有所不同？<lb/>我們發現模型在外部代碼上往往會進行更多且通常過度的編輯。<lb/>我們介紹了 CROCODIL（跨模型代碼編輯與 LLM），這是一個後訓練框架，用於減少過度編輯，同時保持功能正確性。<lb/>CROCODIL 的相似性獎勵對大幅變更進行懲罰，而其執行獎勵則評分構建和測試的成功。<lb/>我們使用這兩個獎勵的乘積來鼓勵策略減少編輯大小，而不降低編輯任務的成功率。<lb/>CROCODIL 可在 https://github.com/EngineeringSoftware/Crocodil 獲得。

##### **GraFT: A Training-Free Framework for Spatial Reasoning in Multimodal Large Language Models via 3D Scene Graphs**
2609.03892v1 by Junqing Du, Fernando Ropero, Erkin Turkoz, Yanfeng Zhang, Lu Liu

3D spatial reasoning underpins understanding and acting in the physical world, yet it remains unreliable in current multimodal large language models (MLLMs). These models falter at precise geometric measurement, at transforming between egocentric and allocentric viewpoints, and at grounding fine-grained appearance. The most common remedies fine-tune the model on large-scale curated spatial-reasoning datasets or attach dedicated encoders for 3D geometry, which typically couples the solution to costly supervision and a specific backbone. We instead introduce GraFT, a training-free framework that supplies the missing 3D structure through a compact, easily maintained 3D scene graph (3DSG). From this 3DSG, GraFT provides three spatial reasoning capabilities: (1) deterministic geometry through symbolic tools, (2) allocentric layout through a bird's-eye-view (BEV) rendering, and (3) visual-attribute grounding through task-relevant egocentric frames. On ScanQA, GraFT improves every metric over the same-backbone baseline, raising CIDEr by 27%. On VSI-Bench, GraFT improves frozen MLLMs by up to 65%, surpassing every proprietary and general-purpose open-source baseline, and several prominent fine-tuned spatial models.

摘要：3D 空間推理是理解和在物理世界中行動的基礎，但在當前的多模態大型語言模型 (MLLMs) 中仍然不可靠。這些模型在精確的幾何測量、在自我中心和他中心視角之間的轉換以及在細緻外觀的基礎上表現不佳。最常見的解決方案是對大型策劃的空間推理數據集進行微調，或為 3D 幾何附加專用編碼器，這通常將解決方案與昂貴的監督和特定的主幹相結合。我們則提出了 GraFT，一個無需訓練的框架，通過一個緊湊、易於維護的 3D 場景圖 (3DSG) 提供缺失的 3D 結構。從這個 3DSG 中，GraFT 提供三種空間推理能力：(1) 通過符號工具獲得確定性幾何，(2) 通過鳥瞰圖 (BEV) 渲染獲得他中心佈局，以及 (3) 通過與任務相關的自我中心框架獲得視覺屬性基礎。在 ScanQA 上，GraFT 在同一主幹基準上提高了每個指標，CIDEr 提高了 27%。在 VSI-Bench 上，GraFT 對凍結的 MLLMs 提高了多達 65%，超越了每個專有和通用開源基準，以及幾個突出的微調空間模型。

##### **FWBC-VLA: Force-Aware Whole-Body Compensation for Contact-Rich Loco-Manipulation**
2609.03889v1 by Yutian Zhang, Siyuan Ma, Liwen Yang, Yang Li, Ce Hao, Haozhen Chi, Dong We, Qiaojun Yu, Dibo Hou

Contact-rich loco-manipulation requires a bridge between semantic action generation and physical interaction control. Existing Vision-language-action (VLA) models generate task-level actions from visual and linguistic observations, but cannot interpret the physical interactions induced by those actions. While the whole-body control (WBC) policy can stabilize the robot, it cannot distinguish task-relevant interaction forces from forces induced by external disturbances during manipulation. Although force/torque sensors provide direct measurements of physical interactions, retrofitting them entails additional hardware costs and substantial integration effort, particularly for platforms not designed with sensor integration in mind. To address this problem, we propose FWBC-VLA, a force-aware framework that bridges task-level VLA action generation and low-level whole-body compensation control for wheeled-legged robots. First, we introduce HSR-Force, a sensorless residual-torque estimator for inferring contact strength and its temporal variation. These contact estimates are then encoded as tokens and injected into the VLA action expert during action decoding, enabling the policy to perceive contact onset, sustained loading, and release. For loco-manipulation tasks, all parameters of the pretrained VLA backbone are fine-tuned on our WL\&Arm Dataset, which comprises more than 5,000 episodes. Moreover, the robot's proprioceptive state, the Jacobian-derived body-frame force estimate, and the estimated contact state are jointly fed into a compensation generator to produce corrective actions. The manipulation-centric actions are subsequently combined with the corrective actions and passed to the WBC policy for execution. Real-world experiments on whiteboard wiping and door opening with a door closer demonstrate the effectiveness of our FWBC-VLA in contact-rich loco-manipulation.

摘要：接觸豐富的運動操控需要在語義動作生成和物理互動控制之間架起橋樑。現有的視覺-語言-動作（VLA）模型從視覺和語言觀察中生成任務級動作，但無法解釋這些動作所引起的物理互動。雖然全身控制（WBC）策略可以穩定機器人，但無法區分與任務相關的互動力和在操控過程中由外部擾動引起的力。儘管力/扭矩傳感器提供了物理互動的直接測量，但改裝它們需要額外的硬體成本和大量的整合工作，特別是對於未設計為考慮傳感器整合的平台。為了解決這個問題，我們提出了FWBC-VLA，一個力感知框架，將任務級VLA動作生成和低級全身補償控制連接起來，適用於輪腿機器人。首先，我們介紹了HSR-Force，一種無傳感器的殘餘扭矩估計器，用於推斷接觸強度及其時間變化。這些接觸估計值隨後被編碼為標記，並在動作解碼過程中注入到VLA動作專家中，使得策略能夠感知接觸的開始、持續加載和釋放。對於運動操控任務，預訓練的VLA骨幹的所有參數都在我們的WL&Arm數據集上進行微調，該數據集包含超過5,000個實例。此外，機器人的本體感知狀態、雅可比衍生的身體框架力估計和估計的接觸狀態被共同輸入到補償生成器中，以產生修正動作。以操控為中心的動作隨後與修正動作結合，並傳遞給WBC策略以執行。在白板擦拭和帶有門閉合器的開門的實際實驗中，展示了我們的FWBC-VLA在接觸豐富的運動操控中的有效性。

##### **Beyond Shallow Alignment: How Post-Training Methods Determine Refusal Circuits And Steering Robustness**
2609.03887v1 by Hoang Cuong Nguyen, Mark Dras, Usman Naseem

How do the methods used to train language models to refuse harmful requests shape how that refusal actually works inside the model? We compare three post-training methods - supervised fine-tuning, reasoning-augmented fine-tuning (training on reasoning chains that justify a safety decision), and preference optimization (ORPO) - across three architecturally distinct models (Llama-3.1-8B, Gemma-2-9B, Qwen3-8B). We find that training method, not just data, reshapes how refusal is computed internally: reasoning-augmented training consistently produces a distinct kind of refusal computation, visible across all three models, while architecture independently shapes internal structure and how reliably refusal can be steered. Most importantly, no method we study achieves all three properties we would want from safe alignment at once: refusal that isn't concentrated in a few fragile components, safety gains that don't cost general capability, and safety behavior correctable through small, targeted edits. We caution against treating current post-training methods as a solved, reliable defense, especially for security-critical use. Code and models are available in https://github.com/hoangcuongnguyen2001/Beyond-Shallow-Alignment.

摘要：如何訓練語言模型拒絕有害請求的方法影響該拒絕在模型內部實際運作的方式？我們比較了三種後訓練方法——監督微調、增強推理微調（基於推理鏈進行訓練以證明安全決策的合理性）和偏好優化（ORPO）——在三個架構上各異的模型（Llama-3.1-8B、Gemma-2-9B、Qwen3-8B）之間的效果。我們發現，訓練方法而不僅僅是數據，重塑了拒絕在內部的計算方式：增強推理訓練始終產生一種獨特的拒絕計算，這在所有三個模型中都可見，而架構則獨立地塑造內部結構以及拒絕的可控性。最重要的是，我們研究的任何方法都無法同時實現我們希望從安全對齊中獲得的三個特性：拒絕不集中於少數脆弱組件、安全增益不以犧牲一般能力為代價，以及安全行為可以通過小而有針對性的編輯進行修正。我們警告不要將當前的後訓練方法視為解決方案或可靠的防禦，特別是在安全關鍵的使用情況下。代碼和模型可在 https://github.com/hoangcuongnguyen2001/Beyond-Shallow-Alignment 獲得。

##### **A Blind Trust, the Bloody Thrust: When Attacker-Controlled Hook Updates Steer AI Agent Harnesses towards Malicious Behaviors**
2609.03884v1 by Pengxun Li, Litian Zhang, Jianwei Hou, Shujiang Wu, Song Li, Zifeng Kang, Xi Zhang

Modern AI agent harnesses expose lifecycle hooks that bind shell commands to runtime events such as session start, tool calls, and file edits. These commands run with host privileges yet ship as lifecycle-hook configuration and may fire at times the LLM never observes. We identify the lifecycle-hook update path, which harnesses trust blindly, as a new attack surface. Under a supply-chain threat model in which an attacker controls only plugin metadata and lifecycle-hook configuration, a benign versioned plugin can be trojanized by an update that silently binds attacker-chosen commands to benign events, yielding malicious host-side behavior such as privilege escalation. We propose HookPry, an open-source and fully automated attack framework that systematically exploits this vulnerability across heterogeneous AI agent harnesses. HookPry realizes ten attack objectives; across 25 combinations of harnesses and backends in 1,000 end-to-end runs, it compromises all seven evaluated harnesses, with per-harness success rates reaching 92.5%. Representative defenses remain insufficient: Microsoft Defender has 0% recall, and the union of three static defenses misses 47.5% of malicious artifacts.

摘要：現代的 AI 代理利用暴露的生命週期鉤子，將 shell 命令綁定到運行時事件，例如會話開始、工具調用和文件編輯。這些命令以主機權限運行，但作為生命週期鉤子配置發送，並可能在 LLM 從未觀察到的時候觸發。我們確定了生命週期鉤子更新路徑，這種盲目信任的利用，成為了一個新的攻擊面。在一個供應鏈威脅模型下，攻擊者僅控制插件元數據和生命週期鉤子配置，一個良性的版本插件可以通過一次更新被植入木馬，該更新靜默地將攻擊者選擇的命令綁定到良性事件，導致主機端出現惡意行為，例如權限提升。我們提出了 HookPry，一個開源且完全自動化的攻擊框架，系統性地利用這一漏洞，跨越異構 AI 代理的利用。HookPry 實現了十個攻擊目標；在 1,000 次端到端運行中，涵蓋 25 種利用和後端組合，成功攻陷所有七個評估的利用，且每個利用的成功率達到 92.5%。代表性的防禦措施仍然不足：Microsoft Defender 的召回率為 0%，而三種靜態防禦的聯合漏掉了 47.5% 的惡意工件。

##### **Xiaomi-TabLDM: A Tabular Foundation Model Technical Report**
2609.03880v1 by Xiaomi-TabLDM Team,  :, Penghui Wang, Wei Liu, Hong Wang, Chengyue Huang, Yuxi Sun, Zirui Wang, Hongming Huang, Quan Wang, Chunxiao Liu, Erli Meng, Bin Wang

We introduce Xiaomi-TabLDM, a tabular large data foundation model for classification and regression via in-context learning, which delivers superior prediction accuracy without requiring task-specific fine-tuning. Pretrained exclusively on synthetic data generated from structural causal models (SCMs), our model enables more flexible context utilization and more efficient capacity scaling.
  i) A new performance standard. Strong regression performance across benchmarks: Xiaomi-TabLDM ranks 1st on OpenML-CTR23 and 2nd on regression across TALENT, TabArena, and BCCO, demonstrating consistently strong regression performance across four complementary benchmark suites. Favorable performance--efficiency trade-off: Xiaomi-TabLDM combines strong predictive performance with substantially lower computational cost. For example, on TabArena regression, it achieves the second-highest Elo while using 82% less training time and 68% less prediction time than the top-ranked TabFM.
  ii) Large-scale synthetic pretraining. Xiaomi-TabLDM expands the coverage and diversity of synthetic tabular data used for pretraining. We also adopt a three-stage training strategy together with dual-stream feature grouping, lightweight Attention Residual, and sparse Mixture-of-Experts, enabling Xiaomi-TabLDM to learn richer feature interactions and expert specialization across diverse tabular tasks.
  iii) Test-time scaling. Xiaomi-TabLDM further extends tabular prediction through test-time compute scaling, where allocating additional computation at inference time consistently improves predictive performance over the base model.

摘要：我們介紹了 Xiaomi-TabLDM，一種通過上下文學習進行分類和回歸的表格大型數據基礎模型，它在不需要特定任務微調的情況下提供了卓越的預測準確性。該模型專門在由結構性因果模型（SCMs）生成的合成數據上進行預訓練，使我們的模型能夠更靈活地利用上下文並更高效地擴展能力。<lb/>  
i) 一個新的性能標準。 在基準測試中表現出色的回歸性能：Xiaomi-TabLDM 在 OpenML-CTR23 上排名第一，在 TALENT、TabArena 和 BCCO 的回歸中排名第二，展示了在四個互補基準套件中始終如一的強大回歸性能。 有利的性能-效率權衡：Xiaomi-TabLDM 將強大的預測性能與顯著較低的計算成本相結合。例如，在 TabArena 回歸中，它在使用比排名第一的 TabFM 少 82% 的訓練時間和少 68% 的預測時間的情況下，達到了第二高的 Elo。<lb/>  
ii) 大規模合成預訓練。 Xiaomi-TabLDM 擴大了用於預訓練的合成表格數據的覆蓋範圍和多樣性。我們還採用了三階段的訓練策略，結合雙流特徵分組、輕量級注意力殘差和稀疏專家混合，使 Xiaomi-TabLDM 能夠在各種表格任務中學習更豐富的特徵交互和專家專業化。<lb/>  
iii) 測試時擴展。 Xiaomi-TabLDM 進一步通過測試時計算擴展來擴展表格預測，在推理時分配額外計算能夠持續提升預測性能，相較於基礎模型。

##### **Differentiable Interval Bottlenecks for Interpretable Anomaly Detection in Numerical Data**
2609.03878v1 by Lamine Diop, Marc Plantevit

Reconstruction-based anomaly detectors are accurate but opaque: a deep autoencoder flags a sample without telling a practitioner which feature ranges made it anomalous. We propose DIFFINT, an autoencoder whose latent bottleneck is structured as a set of soft, axis-aligned interval memberships learned end-to-end directly from raw numerical data, without any discretization or binarization. Each latent unit corresponds to a human-readable hyper-rectangle in feature space; an instance is encoded by how strongly it falls inside each interval relative to the other units, and its reconstruction error is the anomaly score. This keeps the power of differentiable representation learning while exposing an inspectable internal structure. We make the inductive bias precise: a certified reconstruction-error lower bound for points that fall outside every active coordinate of the learned support (with a Lipschitz-enforced decoder), and a graded, empirically verified suppression mechanism for the usual case in which only a few features are abnormal; and we provide a closed-form, label-free importance that ranks each (unit, feature) pair from quantities the model already maintains, turning trained intervals into auditable candidate constraints without ever seeing an anomaly label. On 48 ADBench benchmarks against 22 baselines under a common [-1, 1]-normalized protocol, DIFFINT attains the best mean rank overall on both metrics (4.10 on ROC-AUC, 4.16 on AUPR); among inlier-only detectors it leads its regime clearly, and it is competitive with the strongest contaminated-data detectors (see the stratified and complete-case analyses). It is the only interpretable detector in the statistically-tied leading cluster of seven methods.

摘要：重建基礎的異常檢測器準確但不透明：深度自編碼器標記樣本卻不告訴從業者哪些特徵範圍使其異常。我們提出了DIFFINT，一種其潛在瓶頸結構為一組軟性、軸對齊的區間隸屬關係，從原始數據中端到端學習，無需任何離散化或二元化。每個潛在單元對應於特徵空間中的一個人類可讀的超長方體；一個實例的編碼方式是它在相對於其他單元的每個區間內的強度，重建誤差則是異常分數。這保留了可微分表示學習的力量，同時暴露了一個可檢查的內部結構。我們使歸納偏差變得精確：對於落在學習支持的每個活躍坐標之外的點，提供了經過認證的重建誤差下界（配有Lipschitz強制的解碼器），以及對於通常只有少數特徵異常的情況，提供了一個經驗驗證的分級抑制機制；我們還提供了一個封閉形式的無標籤重要性，根據模型已經維護的數量對每個（單元，特徵）對進行排名，將訓練的區間轉化為可審計的候選約束，而無需看到異常標籤。在48個ADBench基準測試中，對比22個基線，在共同的[-1, 1]標準化協議下，DIFFINT在兩個指標上都獲得了最佳的平均排名（ROC-AUC為4.10，AUPR為4.16）；在僅限於正常值的檢測器中，它明顯領先於其範疇，並且在最強的污染數據檢測器中具競爭力（參見分層和完整案例分析）。它是七種方法中統計上並列領先集群中唯一可解釋的檢測器。

##### **STAIR (STructure Aware Information Retriever): A novel dataset and LLM based retriever for document structure augmentation**
2609.03874v1 by Vineet Kumar, Meghanadh Pulivarthi, vishwajeet kumar, Jaydeep Sen, Riyaz Ahmad Bhat, Sachindra Joshi

Retrieval Augmented Generation (RAG) is a key component for generating accurate and hallucination free answers using Large Language Models (LLMs). LLMs are improving at handling long context, but still suffer from "lost in the middle" problem. Thus, precise and accurate retrieval is important. Current retrievers chunk long context into length-based manageable chunks - in the process throwing away rich and informative semantic global structure in the corpus. We introduce a novel retrieval system STAIR that empowers an LLM to exploit global structure in a corpus such as a Table of Contents (ToC) to efficiently store and retrieve information from its model parameters. Our thorough and careful ablation studies with a finetuned Differentiable Search Index (DSI) system show that ToC helps build a low hallucination (less than 0.05%) generative Information Retrieval (IR) system and can generalize to examples where very few training samples are available. To further research in this novel direction of ToC based retrieval we release SearchTome - a diverse benchmark created from 18 books across 6 diverse domains to further research in this novel direction. STAIR achieves a high Recall@1 score of 82.6% on SearchTome as compared to DSI (76.9%), where the difference is found to be statistically significant. STAIR easily beats other strong baselines such as BM25 (59.5%), DPR (68.7%) and out-of-the-box Mistral (13.8%).

摘要：檢索增強生成（RAG）是使用大型語言模型（LLMs）生成準確且無幻覺答案的關鍵組件。LLMs 在處理長上下文方面有所改善，但仍然面臨「中途迷失」的問題。因此，精確和準確的檢索非常重要。當前的檢索器將長上下文切割成基於長度的可管理塊，這一過程卻丟失了語料庫中豐富且具信息性的全局語義結構。我們介紹了一個新穎的檢索系統 STAIR，使 LLM 能夠利用語料庫中的全局結構，例如目錄（ToC），有效地存儲和檢索其模型參數中的信息。我們與微調過的可微分搜索索引（DSI）系統進行了徹底且仔細的消融研究，顯示 ToC 有助於建立低幻覺（低於 0.05%）的生成信息檢索（IR）系統，並且可以推廣到訓練樣本非常少的例子。為了進一步研究這一基於 ToC 的檢索新方向，我們發布了 SearchTome——一個由 18 本書籍跨越 6 個多樣領域創建的多樣基準，以進一步研究這一新方向。與 DSI（76.9%）相比，STAIR 在 SearchTome 上達到了 82.6% 的高 Recall@1 分數，這一差異被認為具有統計顯著性。STAIR 輕鬆超越了其他強基準，如 BM25（59.5%）、DPR（68.7%）和即用型 Mistral（13.8%）。

##### **Bioinfoysis Technical Report**
2609.03871v1 by Qingyang Shao, Xin Zhang, Zhouyang Yuan, Xianying Chen, Yujia Xiang, Zihao Yang, Tong Ye, Yangqi Zhang, Jiakang Xu, Xiaoqing Yan, Xuan Luo, Keyi Li, Enci Fan, Kai Kang, Zhuohan Liu, Xingyu Jin, Chunran Teng, Tao Li, Xinyu Lv, Minghui Wang, Wenfeng Li, Yidan Gao, Siyu Liu, Mingrui Luo, Zhu Liang, Guanren Qiao, Zhiping Xu

Large language model agents have shown promise in bioinformatics, but most existing systems focus primarily on producing final answers, treating planning, tool use, and code execution as transient interactions. This design is poorly suited to long-horizon bioinformatics tasks, where conclusions must remain connected to the data, computations, and intermediate evidence that support them. We introduce \textbf{Bioinfoysis}, a multi-agent harness that represents each request as a persistent, artifact-grounded analysis run. Bioinfoysis combines global planning with step-wise, evidence-driven replanning: the planner maintains an executable checklist and revises pending steps using structured handoffs returned after each worker execution. These handoffs bind intermediate results to their responsible agent, checklist step, and plan generation, preventing stale evidence from being silently reused after replanning. A controlled runtime validates generated scripts, tables, and figures before they are used in downstream analysis or reporting, while role-specific context, persistent memory, and governed bioinformatics skills support reliable execution over long analysis trajectories. We evaluate Bioinfoysis on BixBench and two question-answering tracks of LAB-Bench 2. On BixBench, Bioinfoysis achieves state-of-the-art accuracy of 82.4\%. Across four underlying language models, Bioinfoysis increases average accuracy from 27.81\% to 64.13\% on SeqQA2 and from 3.13\% to 31.25\% on DbQA2. These results demonstrate that reliable bioinformatics automation depends not only on model capability, but also on the harness that governs planning, execution, memory, and evidence flow. We hope that the emergence of Bioinfoysis will play a driving and leading role in the development of the bioinformatics community. Our demo website can be seen in https://report.bioinfoysis.com/.

摘要：大型語言模型代理在生物資訊學中顯示出潛力，但大多數現有系統主要專注於產生最終答案，將規劃、工具使用和代碼執行視為短暫的互動。這種設計不適合長期的生物資訊學任務，在這些任務中，結論必須與支持它們的數據、計算和中間證據保持連接。我們介紹了\textbf{Bioinfoysis}，這是一個多代理系統，將每個請求表示為持久的、基於工件的分析運行。Bioinfoysis結合了全局規劃與逐步的、基於證據的重新規劃：規劃者維護一個可執行的檢查清單，並在每次工作者執行後使用結構化的交接來修訂待處理步驟。這些交接將中間結果綁定到其負責的代理、檢查清單步驟和計劃生成，防止過時的證據在重新規劃後被默默重用。受控的運行時在生成的腳本、表格和圖形被用於下游分析或報告之前進行驗證，而角色特定的上下文、持久記憶和受管控的生物資訊技能支持在長期分析軌跡上的可靠執行。我們在BixBench和LAB-Bench 2的兩個問答追蹤上評估了Bioinfoysis。在BixBench上，Bioinfoysis達到了82.4\%的最先進準確率。在四個基礎語言模型中，Bioinfoysis將SeqQA2的平均準確率從27.81\%提高到64.13\%，將DbQA2的平均準確率從3.13\%提高到31.25\%。這些結果表明，可靠的生物資訊自動化不僅依賴於模型能力，還依賴於管理規劃、執行、記憶和證據流的系統。我們希望Bioinfoysis的出現能在生物資訊學社群的發展中發揮推動和領導作用。我們的演示網站可以在https://report.bioinfoysis.com/上查看。

##### **Adapting to Evolving Requirements: Agentic AI for Retail Supply Chain Operations**
2609.03860v1 by Lei Zheng, Liping Yang, Zihao Li, Guodong Lyu, Chaik Ming Koh, Chung-Piaw Teo

Retail supply chain operations rely on coupled decision modules that must adapt as requirements evolve. LLMs offer a natural-language interface for this task, but existing methods primarily focus on individual optimization models. Extending them to heterogeneous decision pipelines is challenging because a requirement may admit multiple intervention paths with different downstream effects. We formulate requirement-driven adaptation as the joint selection of an intervention route and an admissible module-level change, and propose a graph-constrained agentic framework in which domain agents expose admissible reformulation interfaces and a central processor searches over bounded intervention paths. Candidates are validated and compared using downstream KPIs. In collaboration with a large retail partner, we evaluate 100 warehouse requirements elicited from practitioner interviews, with GPT, Qwen, and DeepSeek as base LLMs. Relative to direct LLM reformulation, our framework improves correctness and end-to-end success across all three models, raising end-to-end success from 72--76% to 79--83%.

摘要：零售供應鏈操作依賴於必須隨著需求演變而調整的耦合決策模塊。<lb/>大型語言模型（LLMs）為這項任務提供了自然語言界面，但現有方法主要集中在單一優化模型上。<lb/>將它們擴展到異質決策管道是具有挑戰性的，因為一個需求可能允許多條干預路徑，並具有不同的下游影響。<lb/>我們將需求驅動的適應形式化為干預路徑和可接受模塊級變更的聯合選擇，並提出了一個圖約束的代理框架，其中領域代理暴露可接受的重構接口，而中央處理器在有限的干預路徑上進行搜索。<lb/>候選者通過下游關鍵績效指標（KPIs）進行驗證和比較。<lb/>在與一家大型零售合作夥伴的合作中，我們評估了從實踐者訪談中獲得的100個倉庫需求，使用GPT、Qwen和DeepSeek作為基礎LLMs。<lb/>相對於直接的LLM重構，我們的框架在所有三個模型中提高了正確性和端到端成功率，將端到端成功率從72--76%提升到79--83%。

##### **Flip, Don't Shuffle: Watermarking LLMs at the Speed of Inference**
2609.03844v1 by Simone Ceppi, Ignacio Sanchez

We introduce Stateless Bernoulli Watermarking (SBW), a new statistical watermark for Large Language Models that determines green list membership through independent per-token Bernoulli trials. Unlike KGW's vocabulary permutation or SynthID's multi-layer tournament, SBW requires only a single comparison per token against a counter-based random number generator, reducing membership complexity to $O(1)$ and enabling single-kernel execution with zero intermediate allocations. We prove that this formulation preserves the same detection guarantees as fixed-size green lists: the z-score test remains $\mathcal{N}(0,1)$ under the null. The stateless architecture enables capabilities unavailable to existing methods: full-vocabulary self-salt watermarking (over 6000$\times$ faster than KGW's self-salt and 2$\times$ faster than SynthID despite biasing the entire vocabulary with candidate-dependent seeding) and architectural compatibility with distributed inference. In end-to-end generation benchmarks, SBW adds less than 1\% overhead at all batch sizes. We additionally identify hash function design as a previously unexplored axis for watermark quality, showing that a GPU-native Jenkins hash improves null calibration by 1.8$\times$ while producing more diverse text. Experiments across two seeding schemes and eight $(γ, δ)$ configurations confirm statistical equivalence with ROC-AUC differences below 0.01.

摘要：我們介紹無狀態伯努利水印（Stateless Bernoulli Watermarking, SBW），這是一種新的統計水印，用於大型語言模型，通過獨立的每個標記伯努利試驗來確定綠名單成員資格。與KGW的詞彙排列或SynthID的多層競賽不同，SBW每個標記只需與基於計數的隨機數生成器進行一次比較，將成員資格的複雜度降低到$O(1)$，並實現零中間分配的單內核執行。我們證明這種公式保持與固定大小綠名單相同的檢測保證：在虛無假設下，z-score檢驗仍然是$\mathcal{N}(0,1)$。無狀態架構使得現有方法無法實現的能力成為可能：全詞彙自鹽水印（比KGW的自鹽水印快超過6000倍，並且比SynthID快2倍，儘管對整個詞彙進行候選依賴的種子偏置）以及與分佈式推理的架構兼容性。在端到端生成基準測試中，SBW在所有批次大小下增加的開銷不到1\%。我們還確定哈希函數設計是一個先前未探索的水印質量軸，顯示出GPU原生的Jenkins哈希在生成更具多樣性的文本的同時，將虛無校準提高了1.8倍。跨兩個種子方案和八個$(γ, δ)$配置的實驗確認了統計等價性，ROC-AUC差異低於0.01。

##### **Semantic Bayesian World Models**
2609.03834v1 by Tommaso Soru

Knowledge graphs describe reality in crisp assertions, while the systems now consuming them, foundation models and autonomous agents, reason natively in probabilities. We argue that this mismatch is why the integration of language models and knowledge graphs remains a data-feeding pipeline rather than a unified reasoning architecture. We envision Semantic Bayesian World Models (SBWMs): a Web that describes the world not as a database of facts but as a shared, evolving fabric of beliefs over knowledge graphs, where ontological axioms constrain priors, observations update beliefs by Bayesian conditioning, and actions intervene upon the world. We work through what an agent gains from such a model: a home-security agent deciding whether the figure at the gate is a courier or a burglar, an actuarial estimate aggregated by entailment rather than by string frequency, a planning task that language models reliably fail, and the estimation of quantities that no document has ever stated. We then set out what the community must build to make them possible: belief annotation over RDF~1.2, probabilistic entailment regimes, semantic calibration layers, and protocols by which agents that have never met can exchange, and disagree over, calibrated beliefs.

摘要：知識圖譜以簡明的斷言描述現實，而現在使用它們的系統，基礎模型和自主代理，則以概率進行推理。我們認為這種不匹配是語言模型和知識圖譜整合仍然是一個數據供應管道而非統一推理架構的原因。我們設想語義貝葉斯世界模型（SBWMs）：一個將世界描述為一個共享的、隨著時間演變的信念結構，而不是一個事實數據庫的網絡，在這裡本體公理約束先驗，觀察通過貝葉斯條件更新信念，行動則介入世界。我們探討代理從這樣的模型中獲得的收益：一個家庭安全代理決定大門口的人影是快遞員還是小偷，通過推理而非字符串頻率聚合的精算估算，一個語言模型可靠失敗的規劃任務，以及從未在任何文檔中陳述過的數量估算。我們接著指出社群必須構建的內容以使其成為可能：基於RDF~1.2的信念標註、概率推理體系、語義校準層，以及從未見過面的代理能夠交換和對校準信念產生分歧的協議。

##### **Select, Compress, Reinvest: A Controlled Study of Visual-Token Allocation in Long-Video MLLMs**
2609.03820v1 by Prakhar Khatri

Long-video language models cannot look at every frame: an hour sampled once per second is 3,600 images, and a system keeps only a small fixed slice of that pool. Which frames survive that slice is usually treated as a preprocessing detail; we test whether it should be. Published selectors make the comparison hard because they change the frame scorer, the prompt boundary, the resolution policy, and the answering model all at once. We hold each fixed and vary one decision at a time: selection, spatial compression, and reinvestment of the savings, across six training-free selection rules, three long-video benchmarks, and two answering models. Selection is the largest single lever: on LongVideoBench's hour-long bin, eight query-selected frames beat sixteen uniformly spaced ones by 6.9 points, and Orthogonal Matching Pursuit, an unmodified decades-old sparse-approximation algorithm, matches or comes within a point of every purpose-built selector we compare it against, across all three benchmarks. Compression is close to free: halving each frame's spatial budget at fixed timestamps costs at most 0.44 points. Reinvestment is where that budget turns back into accuracy: spending the freed tokens on twice as many compressed frames, at a measured cost no higher than the original eight, returns a further two to three points; compression only pays off once its savings are spent this way. Along the way, an implementation bug in our own AKS baseline and a 0.07 to 3.74 point gap between two harnesses running the same published rules at the same budget show why these comparisons need to happen inside one controlled harness rather than across papers.

摘要：長視頻語言模型無法查看每一幀：一小時每秒取樣一次是3,600張圖像，而系統僅保留這個池中的一小部分。哪些幀能夠存活下來通常被視為預處理的細節；我們測試這是否應該如此。已發表的選擇器使比較變得困難，因為它們同時改變幀評分器、提示邊界、解析度政策和回答模型。我們保持每個固定，並一次變更一個決策：選擇、空間壓縮和節省的再投資，涵蓋六個無需訓練的選擇規則、三個長視頻基準和兩個回答模型。選擇是最大的單一杠杆：在LongVideoBench的一小時長的區間中，八個查詢選擇的幀比十六個均勻間隔的幀高出6.9分，而正交匹配追求（Orthogonal Matching Pursuit）這個未經修改的幾十年歷史的稀疏近似算法，與我們比較的每個專門構建的選擇器相匹配或相差不超過一分，涵蓋所有三個基準。壓縮幾乎是免費的：在固定時間戳下將每幀的空間預算減半最多只損失0.44分。再投資是這個預算轉回準確性的地方：將釋放的標記用於兩倍數量的壓縮幀，所需的成本不高於原始的八個，能再獲得兩到三分；壓縮只有在這種方式下花費其節省時才會有回報。在這個過程中，我們自己的AKS基準中的實現錯誤和兩個在相同預算下運行相同已發表規則的裝置之間0.07到3.74分的差距顯示了為什麼這些比較需要在一個受控的裝置內進行，而不是跨論文進行。

##### **CauseCollab: Causal Unified and Modality-Agnostic Network for Heterogeneous Collaborative Perception**
2609.03818v1 by Weize Li, Yang Li, Quan Yuan, Xiaoyuan Fu, Guiyang Luo, Jinglin Li

Collaborative perception enhances environment understanding through multi-agent information sharing, but its performance in real-world scenarios is constrained by heterogeneous sensor modalities and model architectures. Recent protocol-based two-stage methods alleviate this problem by mapping heterogeneous features into a shared protocol space; however, independently trained modality-specific converters often generate modality-specific pseudo-protocol distributions, leading to semantic inconsistency and error accumulation, which is particularly pronounced in scenarios with large modality discrepancies. To address this issue, we propose CauseCollab, a causal unified and modality-agnostic network. CauseCollab formulates representation learning in the protocol space from a causal perspective, explicitly disentangling semantic factors from modality-specific statistical confounders via causal metric learning. Meanwhile, CauseCollab adopts context-guided Unified Converter for heterogeneous modalities to ensure cross-modal semantic consistency. In addition, integrating new modalities only requires training adapters with minimal parameters. Extensive experiments on the OPV2V and DAIR-V2X datasets demonstrate that CauseCollab achieves state-of-the-art performance, with more significant gains in scenarios involving large modality gaps.

摘要：協作感知透過多代理資訊共享增強環境理解，但其在現實世界場景中的表現受到異質感測器模態和模型架構的限制。最近基於協議的兩階段方法通過將異質特徵映射到共享協議空間來緩解這個問題；然而，獨立訓練的模態特定轉換器往往生成模態特定的偽協議分佈，導致語義不一致和錯誤累積，這在模態差異較大的場景中特別明顯。為了解決這個問題，我們提出了CauseCollab，一個因果統一且模態無關的網絡。CauseCollab從因果的角度制定了協議空間中的表示學習，通過因果度量學習明確地將語義因素與模態特定的統計混淆因素分離。同時，CauseCollab採用上下文引導的統一轉換器來處理異質模態，以確保跨模態的語義一致性。此外，整合新模態僅需訓練具有最少參數的適配器。在OPV2V和DAIR-V2X數據集上的大量實驗表明，CauseCollab實現了最先進的性能，並在涉及大模態差距的場景中獲得了更顯著的增益。

##### **Evaluating Criterion-Conditioned Behaviour of Large Language Models in Content Moderation**
2609.03814v1 by Danting Zhang, Bei Peng, Robert Loftin

Large language models (LLMs) demonstrate strong performance on standard content moderation benchmarks. However, these benchmarks often aggregate multiple moderation criteria into a single label, making it unclear whether models can disentangle them and reliably apply each criterion when making decisions. To study whether LLMs exhibit criterion-conditioned behaviour, we introduce Diagnostic Evaluation of COntent (DECO), a criterion-independent factorisation of content that enables controlled, criterion-level evaluation. We also introduce pairwise evaluation to compare model outputs across different criteria for the same input. Across four moderation datasets and four LLMs, we find that strong benchmark performance can hide substantial failures at the criterion level. Models struggle most when correct decisions depend not on overall harmfulness, but on the specific aspect of the content that the criterion requires them to assess. Our results highlight a key limitation of current content moderation benchmarks: strong performance on aggregated labels does not provide sufficient evidence that LLMs can reliably evaluate content with respect to individual moderation criteria. These findings call for the development of evaluation methods that explicitly measure criterion-conditioned behaviour.

摘要：大型語言模型（LLMs）在標準內容審核基準上表現出色。<lb/>然而，這些基準通常將多個審核標準聚合為一個單一標籤，這使得不清楚模型是否能夠將它們分開並在做出決策時可靠地應用每個標準。<lb/>為了研究LLMs是否表現出標準條件的行為，我們引入了內容的診斷評估（DECO），這是一種與標準無關的內容因子分解，能夠進行控制的標準級評估。<lb/>我們還引入了成對評估，以比較相同輸入下不同標準的模型輸出。<lb/>在四個審核數據集和四個LLMs中，我們發現強大的基準表現可能掩蓋了在標準層面上的重大失敗。<lb/>當正確的決策不依賴於整體的有害性，而依賴於標準要求評估的內容特定方面時，模型最難以應對。<lb/>我們的結果突顯了當前內容審核基準的一個關鍵局限性：在聚合標籤上的強大表現並不能提供充分的證據來證明LLMs能夠可靠地根據個別審核標準評估內容。<lb/>這些發現呼籲開發明確測量標準條件行為的評估方法。

##### **VisCAD: A Foundation Model Suite with Multimodal Industrial CAD Intelligence**
2609.03811v1 by  JoyIndustrial VisCAD Team, Linxin Cai, Qiuhe Hong, Zhichao Huang, Guanlin Li, Hongsen Liu, Ziqi Liu, Yichen Long, Luya Wang, Yuchen Wang, Wenxiang Wu, Huimu Yu, Ning Zhang

AI-assisted computer-aided design (CAD) for industrial products involves two challenging phases. Part-level generation maps diverse forms of user intent, including renders, text descriptions, 2D drawings, and real photographs, to executable programs in a CAD domain-specific language. Assembly-level generation must additionally handle interacting parts, plan mating relations, estimate poses, and place all parts correctly. Existing specialized CAD models are commonly trained on narrow input domains, such as renders or texts, and often generalize poorly, while general-purpose frontier models cover broader inputs but perform inconsistently across CAD domains. We present VisCAD, a foundation model suite designed to provide both broad generalization and strong CAD capability for realistic industrial products. At its core is VisCAD-M1, a 27B model trained through mid-training and post-training for part-level design generation. On PubCADBench and RealCADBench, VisCAD-M1 achieves the highest average part-level score among the evaluated models, reaching 0.5540 compared with 0.5496 for the strongest frontier model. Reusing VisCAD-M1 as a test-time verifier can further raise the score to 0.5797, an approximately 5 percent relative improvement over the previous state of the art. VisCAD also includes a domain-specific harness that leverages frontier models for complex assembly generation and demonstrates advantages over general-purpose harnesses in both quantitative and qualitative evaluations.

摘要：AI輔助的計算機輔助設計（CAD）用於工業產品涉及兩個具有挑戰性的階段。部件級生成映射用戶意圖的多樣形式，包括渲染、文本描述、2D 圖紙和真實照片，轉換為可執行的 CAD 領域專用語言程序。組裝級生成還必須處理互動部件，規劃配合關係，估算姿勢，並正確放置所有部件。現有的專用 CAD 模型通常在狹窄的輸入領域上進行訓練，例如渲染或文本，並且通常泛化效果不佳，而通用前沿模型涵蓋更廣泛的輸入，但在 CAD 領域的表現不一致。我們提出了 VisCAD，一個旨在為現實工業產品提供廣泛泛化和強大 CAD 能力的基礎模型套件。其核心是 VisCAD-M1，一個通過中期訓練和後期訓練進行部件級設計生成的 27B 模型。在 PubCADBench 和 RealCADBench 上，VisCAD-M1 在評估模型中達到最高的平均部件級分數，達到 0.5540，相比之下，最強的前沿模型為 0.5496。將 VisCAD-M1 重用作測試時驗證器可以進一步將分數提高到 0.5797，這是相對於之前最先進技術約 5% 的改進。VisCAD 還包括一個領域專用的工具，利用前沿模型進行複雜的組裝生成，並在定量和定性評估中顯示出相對於通用工具的優勢。

##### **Free Pause Tokens**
2609.03807v1 by John Langford, Nathan Godey, Giovanni Monea, Yoav Artzi, Harry Dong, Ying Fan, Gustavo de Rosa, Zheng Zhan

A free pause token gives a language model extra compute to form each next-token prediction (as a pause, or thinking, token does) but carries that compute in a parallel prediction stream over a weight-shared backbone rather than as an extra token in the sequence. It improves next-token prediction by 2-3 centinats in practice on a 1B parameter model. Because the pause rides an existing position instead of adding one, it is free to use: at inference it adds no context length, no KV cache, and essentially no latency with the growth in inference flops typically irrelevant as it is not the active bottleneck on throughput. The only primary cost is in training, where additional training compute versus an optimized pretraining pipeline is reduced to as low as x1.14 while preserving most of the benefits. The result is an isoflop, isoparameter, and isotoken improvement over standard next token trained transformers.

摘要：一個自由的暫停標記為語言模型提供了額外的計算能力，以形成每個下一個標記的預測（就像暫停或思考標記所做的那樣），但它在一個平行的預測流中攜帶這些計算，而不是作為序列中的額外標記。它在實踐中對於一個10億參數的模型改善了2-3個centinats的下一個標記預測。由於暫停標記利用現有的位置而不是添加一個，因此它是免費使用的：在推理時，它不增加上下文長度，沒有KV緩存，並且由於推理的flops增長通常與吞吐量的主動瓶頸無關，基本上沒有延遲。唯一的主要成本在於訓練，其中額外的訓練計算與優化的預訓練管道相比減少到低至x1.14，同時保留大部分好處。其結果是在標準下一個標記訓練的Transformer上實現了等flop、等參數和等標記的改進。

##### **SVG-Score: Human-Aligned Evaluation of Text-to-SVG Generation**
2609.03806v1 by Marco Cipriano, Leonardo Zini, Alexandra Schild, Valentin Teutschbein, Afsana Mimi, Marcella Cornia, Lorenzo Baraldi, Gerard de Melo

Scalable Vector Graphics (SVG) generation is attracting increasing attention as generative models improve in expressiveness and controllability. Progress, however, is held back by the lack of domain-specific evaluation protocols: current practice relies on metrics designed for natural images, most notably CLIPScore, which was never trained on vector graphics and aligns only partially with human judgment. We introduce \textbf{\ours}, a human-aligned evaluation framework for text-to-SVG generation. Through controlled caption and image perturbations, we first show that CLIP-based scores barely react to the errors SVG generators actually make, such as wrong colors, counts, and spatial relations, and that off-the-shelf Vision-Language Model (VLM) judges, while more sensitive, respond unevenly across error types and SVG styles. We then introduce a human-annotated dataset for \textit{Semantic Alignment}, measuring how faithfully a generated SVG reflects its caption. Building on it, we develop two complementary evaluators: CLIP scorers adapted to vector graphics and then aligned to human preferences, for fast large-scale evaluation, and a VLM judge trained with supervised fine-tuning and reward-shaped reinforcement learning, for more expressive and interpretable assessment. Using both, we benchmark major open-source, commercial, and optimization-based SVG generators on an independent caption set.

摘要：可擴展向量圖形（SVG）生成正受到越來越多的關注，因為生成模型在表達能力和可控性方面不斷改進。然而，進展受到缺乏特定領域評估協議的限制：目前的做法依賴於為自然圖像設計的指標，最著名的是 CLIPScore，該指標從未在向量圖形上進行過訓練，並且僅部分與人類判斷對齊。我們介紹了 \textbf{\ours}，這是一個與人類對齊的文本到 SVG 生成的評估框架。通過控制標題和圖像擾動，我們首先顯示 CLIP 基礎的分數幾乎對 SVG 生成器實際犯的錯誤（例如錯誤的顏色、數量和空間關係）沒有反應，而現成的視覺-語言模型（VLM）評判者雖然更敏感，但在錯誤類型和 SVG 風格之間的反應不均勻。然後，我們介紹了一個人類標註的數據集，用於 \textit{語義對齊}，測量生成的 SVG 在多大程度上忠實於其標題。基於此，我們開發了兩個互補的評估器：適應於向量圖形的 CLIP 評分器，然後與人類偏好對齊，以便快速進行大規模評估，以及一個經過監督微調和獎勵形狀強化學習訓練的 VLM 評判者，以便進行更具表達性和可解釋性的評估。使用這兩者，我們在獨立的標題集上基準測試了主要的開源、商業和基於優化的 SVG 生成器。

##### **Govern the Model, Not Only the Data: Storage, Circulation, and Learning in Creative AI**
2609.03800v1 by Phoenix Perry, George Simms, Elizabeth Wilson, Yasmine Boudiaf, Nick Bryan-Kinns, Tega Brain, R. Luke DuBois, Alix Rule, Rachel Meade Smith, Kelani Nichole, Atharva Pravin Pawar, Rebecca Fiebrink

Federated learning is increasingly presented as a privacy-preserving advance: personal data remain on the device, and only model updates are shared. It borrows the vocabulary of the federated social web, yet inverts its logic, distributing computation while the resulting model stays with whoever convened the training. We argue that federation is not in itself a remedy for extractive AI, because outcomes depend on who governs the data and the model and who has agency over the practices that shape them. We describe three layers at which a creative community can hold its work: storage, circulation, and learning. Examining artist-governed trusts, cooperatives, and consent infrastructures, we show that creator governance is established at storage and circulation but stops at learning: contributors can consent to training, yet have little say over the resulting model or its federation. We map the research space this opens, pairing technical open problems with the human questions from which they unfold. We propose four design principles for a creative data commons that governs models and their federation, not only datasets: govern the model, not only the corpus; make the terms legible at the moment of contribution; design for refusal as a first-class state; and decide stewardship in the open and account for it.

摘要：聯邦學習越來越被視為一種保護隱私的進步：個人數據保留在設備上，只有模型更新被共享。它借用了聯邦社交網絡的詞彙，但顛覆了其邏輯，分配計算的同時，結果模型仍然留在召集訓練的個體手中。我們主張，聯邦本身並不是解決剝削性人工智慧的良方，因為結果取決於誰管理數據和模型，以及誰對塑造它們的實踐擁有主導權。我們描述了創意社群可以持有其工作的三個層面：儲存、流通和學習。檢視藝術家管理的信託、合作社和同意基礎設施，我們顯示創作者治理是在儲存和流通層面建立的，但在學習層面停滯：貢獻者可以同意訓練，但對結果模型或其聯邦幾乎沒有發言權。我們描繪了這一研究空間，將技術上的開放問題與其展開的人類問題配對。我們提出了四個設計原則，以治理模型及其聯邦，而不僅僅是數據集：治理模型，而不僅僅是語料庫；在貢獻時使條款清晰可見；將拒絕設計為一種一級狀態；並在公開中決定管理權並對其負責。

##### **Transfiver: Human-AI Co-Inference through a Shared Editable State**
2609.03797v1 by Minji Park, Seunghyun Yoon, Hyuk Lim

Long-term human-AI interaction is difficult because the information that guides inference is updated implicitly by the model and is not directly inspectable or controllable by the user. We introduce the TRANSparent Framework for Interactive, Verifiable, Editable Representation (Transfiver), an architecture for human-AI co-inference through a shared editable state. Its central idea is that interaction-specific information is maintained in a single persistent state $(S_t)$ that both the model and the human update.
  Transfiver distinguishes two modes of state evolution. In an implicit stream update, the model interprets ongoing interaction and decides whether new information revises an existing state item or creates a new one. In an explicit directed edit, a human inspects and modifies an addressed item. Both act on the same underlying state, so a human correction changes the state that subsequent computation reads, rather than adding another instruction or separate record.
  The architecture separates shared parameters $(θ)$, learned before ordinary use, from the persistent state $(S_t)$, which evolves during deployment without parameter retraining. Extending Transfiver to rich natural-language, relational, and large-scale shared states remains open.

摘要：長期的人類與人工智慧互動是困難的，因為指導推理的信息是由模型隱式更新的，並且用戶無法直接檢查或控制。 我們介紹了互動、可驗證、可編輯表示的透明框架（Transfiver），這是一種通過共享可編輯狀態進行人類與人工智慧共同推理的架構。 其核心思想是，互動特定的信息保存在一個持久的單一狀態 $(S_t)$ 中，該狀態由模型和人類共同更新。<lb/>  
Transfiver 區分了兩種狀態演變模式。 在隱式流更新中，模型解釋正在進行的互動，並決定新信息是修訂現有狀態項還是創建一個新的項目。 在明確的定向編輯中，人類檢查並修改一個特定的項目。 兩者都作用於相同的基礎狀態，因此人類的修正會改變後續計算所讀取的狀態，而不是添加另一條指令或單獨的記錄。<lb/>  
該架構將共享參數 $(θ)$ 與持久狀態 $(S_t)$ 分開，前者在普通使用之前學習，而後者在部署期間演變而不需要重新訓練參數。 將 Transfiver 擴展到豐富的自然語言、關聯和大規模共享狀態仍然是開放的。

##### **LLaDA-Image: Building Strong Image Generators with Fully Open Training Recipes**
2609.03796v1 by Chuyan Chen, Haoxing Chen, Kun Chen, Zhenglin Cheng, Long Cui, Ruishan Fang, Zhangxuan Gu, Zhicheng Huang, Zhenzhong Lan, Yuanting Lei, Haoquan Li, Jianguo Li, Rongchuan Li, Sidu Li, Tao Lin, Deyuan Liu, Jiacheng Liu, Lin Liu, Yuxuan Lou, Zhisheng Lu, Yuxin Ma, Shuheng Shen, Peng Sun, Chaoyang Wang, Hongjun Wang, Xiaomei Wang, Yongxin Wang, Chengzhang Wu, Hongru Wu, Jun Xie

We introduce LLaDA-Image, a unified framework that pairs a 6B Diffusion Transformer (DiT) trained from scratch with a frozen vision-language understanding module built on the LLaDA2.0-Mini diffusion language model backbone. Instead of relying heavily on paired image-text data from the beginning, we first build a strong visual generative prior through image-only pre-training and mid-training. The generation pipeline comprises 220M samples, 98 of which are real images. For efficient and scalable optimization, we use parameter-free RMSNorm throughout the DiT together with the Muon optimizer. The resulting unified model produces highly photorealistic images while accurately following fine-grained editing instructions. We further distill LLaDA-Image into LLaDA-Image-Turbo, enabling fast inference in 2-4 sampling steps. On Qwen-Image-Bench, LLaDA-Image achieves overall scores of 53.53 and 53.38 on the English and Chinese tracks, respectively, setting a new state-of-the-art among open-source models on both tracks. To support further research on capable and efficient generative models, we release our model weights, training code, and detailed recipes.

摘要：我們介紹 LLaDA-Image，一個統一的框架，將一個從零開始訓練的 6B 擴散Transformer (DiT) 與基於 LLaDA2.0-Mini 擴散語言模型骨幹的凍結視覺-語言理解模組配對。<lb/>我們並不從一開始就過度依賴配對的圖像-文本數據，而是首先通過僅圖像的預訓練和中期訓練建立強大的視覺生成先驗。<lb/>生成管道包含 220M 個樣本，其中 98 個是真實圖像。<lb/>為了高效且可擴展的優化，我們在整個 DiT 中使用無參數的 RMSNorm，並搭配 Muon 優化器。<lb/>最終的統一模型生成高度真實的圖像，同時準確遵循細緻的編輯指令。<lb/>我們進一步將 LLaDA-Image 提煉為 LLaDA-Image-Turbo，使其能在 2-4 次取樣步驟中快速推理。<lb/>在 Qwen-Image-Bench 上，LLaDA-Image 在英語和中文賽道上分別達到 53.53 和 53.38 的總分，創造了開源模型在這兩個賽道上的新最先進記錄。<lb/>為了支持對高效能和高效生成模型的進一步研究，我們釋放了我們的模型權重、訓練代碼和詳細的配方。

##### **A Reverse Sign Language Dictionary: Open-Vocabulary Sign Recognition from Continuous Signing via Video Captioning and Description Retrieval**
2609.03788v1 by Santiago Poveda-Gutiérrez, Hideki Nakayama, Mayumi Bono

Isolated Sign Language Recognition (ISLR) is conventionally cast as closed-set classification over gloss labels, which cannot generalize to signs unseen in training and ties every deployment to a gloss-annotated lexicon. We instead recognize signs extracted from continuous signing by (1) captioning a sign-level clip into a free-form procedural description of the articulation with an open-weight vision-language model, and (2) retrieving the closest entry from a vocabulary of target descriptions with a multilingual sentence encoder: a reverse sign language dictionary that needs no gloss supervision and admits an open vocabulary. On 1,300 sign-level segments from a Japanese Sign Language (JSL) dialogue corpus annotated with procedural descriptions (against a 2% top-10 chance floor over the 503-entry target vocabulary), fine-tuning the captioner substantially improves seen-class retrieval: language and vision tower fine-tuning raises top-10 retrieval on seen classes from 4.5% (untrained) to 49%, becoming statistically indistinguishable from a standard supervised closed-set classifier (I3D) on two of the three test sets where a closed-set classifier can be evaluated at all. More importantly, unseen-class retrieval also improves significantly over the untrained pipeline (11.5% -> 21.0% top-10, p=0.0094), a regime in which the closed-set classifier cannot participate. A matcher-side empirical upper-bound analysis shows the sentence encoder already recovers close to 100% of paraphrased gold descriptions, locating a gap in captioning quality that we aim to address in future work. To our knowledge this is the first description-based, open-vocabulary sign lookup from continuous signing without gloss supervision, and the first for JSL.

摘要：孤立手語識別（ISLR）傳統上被視為對標記標籤的封閉集分類，這無法對訓練中未見的手勢進行泛化，並將每次部署與標記的詞彙表綁定在一起。我們則通過以下方式識別從連續手語中提取的手勢：（1）使用開放權重的視覺-語言模型將手勢級片段標題轉換為自由形式的程序描述，並（2）使用多語言句子編碼器從目標描述的詞彙中檢索最接近的條目：一個不需要標記監督且允許開放詞彙的反向手語字典。在一個標註有程序描述的日本手語（JSL）對話語料庫中的1,300個手勢級片段上（在503個目標詞彙的2%前10名機會底線下），微調標題生成器顯著改善了已見類別的檢索：語言和視覺塔的微調將已見類別的前10名檢索從4.5%（未訓練）提高到49%，在三個測試集中的兩個上，與標準監督的封閉集分類器（I3D）在統計上無法區分。更重要的是，未見類別的檢索也顯著改善了未訓練的管道（11.5% -&gt; 21.0% 前10名，p=0.0094），這是一種封閉集分類器無法參與的情況。一個匹配器端的經驗上限分析顯示，句子編碼器已經能夠恢復接近100%的同義改寫金標描述，找出了我們希望在未來工作中解決的標題質量差距。據我們所知，這是第一個基於描述的、開放詞彙的從連續手語中查找手勢的方式，且是日本手語的首次。

##### **IndicSafeEval: Safety Robustness of Large Language Models under Multilingual Persuasive Jailbreak Attacks**
2609.03781v1 by Saikat Mondal,  Mamta, Deeksha Varshney, Oana Cocarascu, Asif Ekbal

Large language models (LLMs) are increasingly used in multilingual settings, yet their safety is still evaluated primarily in English. This limits our understanding of how alignment failures manifest in low-resource and culturally diverse languages. We introduce IndicSafeEval, a persuasion-based jailbreak evaluation framework for Indian languages. Our benchmark combines ten safety critical content categories with six human-like persuasive strategies across four different Indian languages, such as Hindi, Bengali, Marathi and Punjabi, resulting in 7,200 adversarial prompts. We conduct a systematic black-box evaluation of several open-source LLMs to examine how their safety behaviour varies across languages, persuasion strategies, and risk categories. Our analysis shows that the model does not behave equally safely across all languages and prompt styles. Instead, safety performance depends strongly on both the languages used and the way a request is phrased using persuasive cues. We further observe that different risk categories exhibit different levels of vulnerability, with some types of harmful content being significantly more susceptible to persuasion-based jailbreaks than others. These findings reveal important limitations of current safety evaluations, which are largely English-centric, and underscore the need for multilingual and persuasion-aware benchmarking frameworks to more accurately assess real-world LLM safety. Our implementation is available at https://github.com/MonSaikat/IndicSafeEval. Warning: this paper contains example data that may be offensive or harmful.

摘要：大型語言模型（LLMs）在多語言環境中的使用越來越普遍，但其安全性仍主要以英語進行評估。這限制了我們對於在資源匱乏和文化多樣性語言中對齊失敗如何表現的理解。我們介紹了IndicSafeEval，一個基於說服的越獄評估框架，專為印度語言設計。我們的基準結合了十個安全關鍵內容類別和六種類人說服策略，涵蓋四種不同的印度語言，如印地語、孟加拉語、馬拉地語和旁遮普語，總共產生了7,200個對抗性提示。我們對幾個開源LLMs進行了系統的黑箱評估，以檢查它們的安全行為如何在不同語言、說服策略和風險類別中變化。我們的分析顯示，模型在所有語言和提示風格中並不表現出相同的安全性。相反，安全性能強烈依賴於所使用的語言以及請求的措辭方式，特別是使用說服性提示。我們進一步觀察到，不同的風險類別表現出不同程度的脆弱性，某些類型的有害內容對基於說服的越獄特別敏感。這些發現揭示了當前安全評估的重要局限性，這些評估在很大程度上以英語為中心，並強調了需要多語言和具說服意識的基準框架，以更準確地評估現實世界中的LLM安全性。我們的實現可在 https://github.com/MonSaikat/IndicSafeEval 獲得。警告：本文包含可能冒犯或有害的示例數據。

##### **Typological Feature Prediction with Large Language Models: An In-Context Learning Approach**
2609.03775v1 by Qianwen Wang, York Hay Ng, Aditya Khan, En-Shiun Annie Lee

Typological features are widely used in multilingual NLP, and the prediction of such features holds downstream utility. However, existing methods to predict missing values lack interpretable justifications for predictions, while their performance across resource levels and feature types remains underexplored. Given LLMs' abilities in meta-linguistic reasoning and in providing rationales, we investigate LLMs' performance in typological feature prediction via an in-context learning approach with linguistic data from URIEL+ and Glottolog. We find that zero-shot prompting is insufficient, but when given phylogenetic and geographic neighbour evidence, LLMs substantially outperform all baselines without disadvantaging low-resource languages. We further find that most LLM rationales are consistent with the provided evidence, offering a step toward explainable typological feature prediction.

摘要：語言類型特徵在多語言自然語言處理中被廣泛應用，而這些特徵的預測具有下游效用。<lb/>然而，現有的缺失值預測方法缺乏可解釋的預測理由，而它們在資源水平和特徵類型上的表現仍然未被充分探討。<lb/>考慮到大型語言模型在元語言推理和提供理由方面的能力，我們通過使用來自URIEL+和Glottolog的語言數據，研究大型語言模型在語言類型特徵預測中的表現，採用上下文學習的方法。<lb/>我們發現零樣本提示不足，但當提供系統發育和地理鄰近證據時，大型語言模型的表現顯著超過所有基準，且不會對低資源語言造成不利影響。<lb/>我們進一步發現，大多數大型語言模型的推理與提供的證據一致，這為可解釋的語言類型特徵預測邁出了重要一步。

##### **Rethinking World Models for Safety-Critical Embodied Systems**
2609.03774v1 by Kailang Ma, Heye Huang, Inhi Kim, Kitae Jang

World models have progressed from compact latent dynamics to generative, controllable, and interactive simulators of embodied environments. However, high predictive likelihood and visual fidelity do not necessarily ensure that a model preserves the evidence required for safe decision-making. This perspective identifies three structural mismatches in current world modeling: likelihood versus risk, prediction versus intervention, and finite-horizon prediction versus accumulated consequences. We propose the Risk-Informed World Model (RIWM) as a decision-centric research direction for safety-critical embodied systems. RIWM organizes world modeling around consequences, intervention, epistemic uncertainty, and recoverability, and integrates four interdependent capabilities: decision-relevant representation, counterfactual reasoning, safety-critical episodic memory, and runtime safety assurance. It distinguishes physical, social, and operational consequences while using epistemic uncertainty to qualify the evidence supporting action. We further discuss open challenges in identifying consequential futures, validating counterfactual reasoning, maintaining revisable safety memories, translating learned consequences into executable constraints, and determining when evidence is sufficient to act. This perspective argues that future world models should move beyond predicting likely futures toward identifying which futures matter, revising judgments through experience, and recognizing when to act, revise, sense, defer, or abstain.

摘要：世界模型已經從緊湊的潛在動態進展到生成的、可控的和互動的具身環境模擬器。<lb/>然而，高預測可能性和視覺真實性並不一定確保模型保留進行安全決策所需的證據。<lb/>這一觀點確定了當前世界建模中的三個結構不匹配：可能性與風險、預測與干預，以及有限時間預測與累積後果。<lb/>我們提出風險知情世界模型（RIWM），作為安全關鍵具身系統的以決策為中心的研究方向。<lb/>RIWM圍繞後果、干預、認知不確定性和可恢復性組織世界建模，並整合四個相互依賴的能力：與決策相關的表徵、反事實推理、安全關鍵的情節記憶和運行時安全保證。<lb/>它區分物理、社會和操作後果，同時利用認知不確定性來限定支持行動的證據。<lb/>我們進一步討論在識別後果未來、驗證反事實推理、維持可修訂的安全記憶、將學習到的後果轉化為可執行的約束，以及確定何時證據足以行動方面的開放挑戰。<lb/>這一觀點認為，未來的世界模型應該超越預測可能的未來，轉向識別哪些未來重要，通過經驗修正判斷，並認識何時行動、修正、感知、延遲或放棄。

##### **RealCADBench: Benchmarking Parametric CAD Modeling from Industrial Design Intents**
2609.03773v1 by  JoyIndustrial VisCAD Team, Linxin Cai, Qiuhe Hong, Zhichao Huang, Guanlin Li, Zongzhen Li, Hongsen Liu, Yichen Long, Wei Wang, Yuchen Wang, Dongyue Yang, Huimu Yu, Xianwen Zhong

Parametric computer-aided design (CAD) modeling is difficult to evaluate with a single metric. Existing CAD benchmarks often emphasize synthetic or CAD-native settings, limited input modalities, or executability and IoUs alone. We introduce RealCADBench, a benchmark for intent-to-program CAD modeling from real industrial design intents. It contains 12,632 tasks from 19 factory-automation categories and spans text descriptions, 2D engineering drawings, real product pictures, and rendered images for both Part and Assembly modeling. We report results on a 1,770-task evaluation slice: 1,745 Part tasks across four input regimes and RCB-Assm25, a 25-task assembly study used in every reported assembly comparison. Each method generates FreeCAD API Python, which a shared runtime executes to export the 3D model. We evaluate the exported model using executability, Solid IoU, Surface IoU, and a rubric-based visual-semantic identity Judge. Among the nine standalone frontier large models evaluated, no model leads all four metrics. Across six frontier-scale large models, executability ranges from 0.565 to 0.812, Solid IoU from 0.2841 to 0.5379, and Surface IoU from 0.112 to 0.217 across the four Part regimes. The highest regime-balanced composite comes from a different model than the leaders on the four component metrics. On RCB-Assm25, Codex with GPT-5.5 improves executability and both IoU metrics over standalone GPT-5.5, but lowers the Judge score by 6.98 percentage points, leaving GPT-5.5 as the Judge leader. We also observe recurring failure modes, most notably missing fine structures, loss of part identity, and incorrect assembly placement. These results show that execution alone is insufficient to characterize realistic CAD modeling and that frontier models and agents differ substantially across executability, IoUs, and visual-semantic identity.

摘要：參數化電腦輔助設計 (CAD) 建模難以用單一指標進行評估。現有的 CAD 基準通常強調合成或 CAD 原生環境、有限的輸入模式，或僅關注可執行性和 IoU。我們介紹 RealCADBench，這是一個基於真實工業設計意圖的意圖編程 CAD 建模基準。它包含來自 19 個工廠自動化類別的 12,632 個任務，涵蓋文本描述、2D 工程圖、真實產品圖片和用於零件和組裝建模的渲染圖像。我們報告了一個 1,770 任務評估切片的結果：1,745 個零件任務，跨越四種輸入模式，以及 RCB-Assm25，一個在每個報告的組裝比較中使用的 25 任務組裝研究。每種方法生成 FreeCAD API Python，該共享運行時執行以導出 3D 模型。我們使用可執行性、固體 IoU、表面 IoU 和基於評分標準的視覺語義身份評判來評估導出的模型。在評估的九個獨立前沿大型模型中，沒有一個模型在所有四個指標上領先。在六個前沿規模的大型模型中，可執行性範圍從 0.565 到 0.812，固體 IoU 從 0.2841 到 0.5379，表面 IoU 在四個零件模式中範圍從 0.112 到 0.217。最高的模式平衡綜合來自於不同於四個組件指標領導者的模型。在 RCB-Assm25 中，Codex 與 GPT-5.5 在可執行性和兩個 IoU 指標上優於獨立的 GPT-5.5，但評判分數降低了 6.98 個百分點，使 GPT-5.5 成為評判領導者。我們還觀察到重複的失敗模式，最顯著的是缺失精細結構、零件身份丟失和組裝位置不正確。這些結果表明，僅僅依賴執行不足以表徵現實的 CAD 建模，並且前沿模型和代理在可執行性、IoU 和視覺語義身份上有顯著差異。

##### **ENEAS: Embedding-guided Neural Ensemble for Adaptive Segmentation**
2609.03756v1 by Javier del Pino, Salvador Rodríguez, Alejandro Garabito, Javier Álvarez, Chema Garabito

We present ENEAS, a unified, text-promptable method for instance tracking and semantic discovery. Text-promptable segmentation models, including the latest foundation models such as SAM 3, still suffer from temporal hallucinations, spatial fragmentation, and semantic misclassification: they fail to report target absence when an object leaves the field of view, segment local textures instead of the complete object during extreme close-ups, and prioritize visual features over ontological reality, so that visually similar artifacts such as statues, paintings, or reflections are segmented as target entities.
  ENEAS works two ways from a single method: precise tracking and high-quality segmentation of a unique instance, and open-concept discovery of every instance a text query names, resolved by a semantic verification layer. For tracking, we extend the geometrically robust SeC architecture, previously limited to point interactions, with a text-prompting adapter and leverage its temporal memory, so that the target is held through disappearance without drifting to distractors and kept whole even when it fills the entire view. For discovery, the verification layer combines high-speed visual embedding matching with conditional VLM refinement, invoking semantic reasoning only for ambiguous candidates, which filters out the ontological errors that visual-only models cannot distinguish while keeping latency low. Designed with 3D reconstruction in mind, where a single misclassified distractor corrupts the asset, ENEAS unlocks high-quality semantic tracking and segmentation of video, of broad libraries, and of collections of temporally or spatially unordered data, together with the discrimination to tell true instances from their doppelgangers: things that look alike but are not the same. The code and models are available at https://github.com/speridlabs/eneas

摘要：我們提出了ENEAS，一種統一的、可通過文本提示的方法，用於實例追踪和語義發現。可通過文本提示的分割模型，包括最新的基礎模型如SAM 3，仍然面臨時間幻覺、空間碎片化和語義錯誤分類的問題：當物體離開視野時，它們未能報告目標缺失，在極近距離拍攝時分割局部紋理而非完整物體，並優先考慮視覺特徵而非本體現實，因此視覺上相似的物體，如雕像、畫作或反射，會被分割為目標實體。<lb/>ENEAS從單一方法中以兩種方式運作：對唯一實例的精確追踪和高質量分割，以及對文本查詢所命名的每個實例的開放概念發現，這由語義驗證層解決。<lb/>在追踪方面，我們擴展了幾何穩健的SeC架構，該架構之前僅限於點交互，並添加了文本提示適配器，利用其時間記憶，使得目標在消失過程中不會漂移到干擾物上，並在填滿整個視野時保持完整。<lb/>在發現方面，驗證層結合了高速視覺嵌入匹配和條件VLM精煉，僅對模糊候選者進行語義推理，過濾掉視覺模型無法區分的本體錯誤，同時保持低延遲。<lb/>ENEAS以3D重建為設計考量，因為單一錯誤分類的干擾物會損壞資產，解鎖了視頻的高質量語義追踪和分割，廣泛的庫，以及時間或空間無序數據的集合，並具備區分真實實例與其替身的能力：看似相似但並不相同的事物。<lb/>代碼和模型可在 https://github.com/speridlabs/eneas 獲得。

##### **SimSkill: A Lifelong Learning AI Agent for Autonomous Mastery of Traffic Simulation**
2609.03753v1 by Qi Liu, Qinzheng Wang, Yiming Bie

As large language models (LLMs) become increasingly capable, the long-term value of AI systems depends not only on solving individual requests, but also on transforming experience and accumulated knowledge into durable, reusable competence. We introduce SimSkill, a self-evolving agent built around the Simulation of Urban MObility (SUMO) traffic simulator. SimSkill identifies capability gaps, generates and solves environment-grounded tasks, verifies solutions through an action--critic loop, and consolidates experience into episodic, procedural, and semantic memory without updating the backbone model. Through autonomous exploration, it builds a reusable library spanning the traffic-simulation workflow. We evaluate SimSkill on two held-out benchmarks with three backbone LLMs and independent artifact-based verification. SimSkill improves verified completion by up to 25 percentage points, while ablations show complementary contributions from procedural and semantic memory. Its benefits remain backbone- and budget-dependent: memory does not improve every model or uniformly reduce inference cost. More broadly, SimSkill illustrates a design paradigm in which natural language preserves and composes computational capabilities, while executable tools and code provide precise and reproducible execution. All code and experimental data are publicly available at https://github.com/qiliuchn/SimSkill-V1.

摘要：隨著大型語言模型（LLMs）變得越來越強大，人工智慧系統的長期價值不僅依賴於解決個別請求，還依賴於將經驗和累積的知識轉化為持久、可重用的能力。我們介紹了SimSkill，一個圍繞城市交通模擬（SUMO）交通模擬器構建的自我演化代理。SimSkill識別能力差距，生成並解決基於環境的任務，通過行動-評價循環驗證解決方案，並將經驗整合到情節性、程序性和語義記憶中，而無需更新主幹模型。通過自主探索，它建立了一個涵蓋交通模擬工作流程的可重用庫。我們在兩個保留的基準上評估SimSkill，使用三個主幹LLMs和獨立的基於工件的驗證。SimSkill使驗證完成率提高了最多25個百分點，而消融實驗顯示程序性和語義記憶的互補貢獻。其優勢仍然依賴於主幹和預算：記憶並不會改善每個模型或均勻降低推理成本。更廣泛地說，SimSkill展示了一種設計範式，其中自然語言保留並組合計算能力，而可執行的工具和代碼提供精確且可重複的執行。所有代碼和實驗數據均可在https://github.com/qiliuchn/SimSkill-V1上公開獲取。

##### **Beyond BLEU: A Case for Redefining Sign Language Translation Benchmarks**
2609.03734v1 by Oline Ranum, Edward Fish, Simon Hadfield, Richard Bowden

BLEU-4 is the standard metric for evaluating sign language translation (SLT), but spoken-language metrics may not adequately reflect sign language proficiency. The multimodal, low-resource context of SLT allows models to exploit spurious correlations and spoken-language priors, rather than learning stronger sign representations. In this paper, we evaluate the relationship between spatio-temporal understanding and BLEU-4 across six SLT models on Phoenix-2014T and CSL-Daily, showing that gains in BLEU-4 are not on their own evidence of better sign language understanding. This work introduces an alternative inspired by language-learning assessment, using an open-weight-LLM QA protocol that measures salient content preservation. It aligns more closely with human rankings and is six to seven times more paraphrase-invariant than BLEU-4. Applied to SLT, this protocol targets content transfer, is more robust to train-test overlap, and gives a different picture of the field: the five gloss-free systems are largely within noise of one another on Phoenix-2014T, while the gloss-supervised system stands 9.3 points higher, a gap invisible to BLEU-4.

摘要：BLEU-4 是評估手語翻譯 (SLT) 的標準指標，但口語語言指標可能無法充分反映手語能力。SLT 的多模態、低資源環境使模型能夠利用虛假的相關性和口語語言的先驗知識，而不是學習更強的手語表徵。在本文中，我們評估了在 Phoenix-2014T 和 CSL-Daily 上六個 SLT 模型之間的時空理解與 BLEU-4 之間的關係，顯示 BLEU-4 的提升本身並不證明手語理解的改善。這項工作引入了一種受語言學習評估啟發的替代方案，使用開放權重的 LLM 問答協議來測量顯著內容的保留。它與人類排名更為接近，且在意譯不變性方面比 BLEU-4 高六到七倍。應用於 SLT，這一協議針對內容轉移，更加穩健於訓練-測試重疊，並提供了該領域的不同視角：在 Phoenix-2014T 上，五個無手語標記的系統彼此之間的差異幾乎在噪音範圍內，而手語標記監督系統則高出 9.3 分，這一差距在 BLEU-4 中是不可見的。

##### **Proactive Service Agents: A Unified Decision Framework, Methods, and Evaluation**
2609.03727v1 by Yan Tang, Tingyu Cao, Yuanbo Tang, Huaze Tang, Keer Hu

Large language model agents can plan, invoke tools, and modify external states, yet most systems still take an explicit user instruction as a fixed starting point. Proactive service moves the decision upstream: an agent must infer service opportunities from incomplete environmental and user signals, choose among remaining silent, asking, assisting, and acting, and account for interruption, misunderstanding, overreach, and privacy costs. This survey gives an operational definition centered on initiative and formulates the problem as a partially observable sequential decision process constrained by authorization and risk. The formulation represents timing, content, and delivery within one structured action, while making explicit the option value of waiting, the decision value of questions, and feedback-induced state changes. On this basis, we organize existing methods along one decision pipeline (state and need estimation, intervention gating, action construction, and feedback adaptation) and describe prescribed, predictive, model based, and return optimizing mechanisms as nonexclusive policy-construction components. We further normalize decision units and three-axis evidence descriptors across streaming dialogue, screen, video, software-engineering, and human-agent collaboration resources, and formalize metrics for triggering, timing, calibration, user burden, safety, and policy value. The synthesis shows why offline classification performance alone does not predict deployment benefit and why long-term memory is not a defining condition of proactivity. Reliable proactive service instead requires calibrated incremental intervention value, verifiable authorization, recoverable execution, and counterfactual evidence.

摘要：大型語言模型代理可以計劃、調用工具和修改外部狀態，但大多數系統仍然將明確的用戶指令視為固定的起點。主動服務將決策上游化：代理必須從不完整的環境和用戶信號中推斷服務機會，選擇在沉默、詢問、協助和行動之間，並考慮中斷、誤解、過度干預和隱私成本。這項調查給出了以主動性為中心的操作性定義，並將問題表述為受授權和風險約束的部分可觀察序列決策過程。該表述在一個結構化的行動中表示時間、內容和交付，同時明確等待的選擇價值、問題的決策價值以及反饋引起的狀態變化。在此基礎上，我們沿著一個決策管道（狀態和需求估計、干預閘、行動構建和反饋適應）組織現有方法，並將規範性、預測性、基於模型和回報優化的機制描述為非排他性的政策構建組件。我們進一步標準化決策單位和三軸證據描述符，涵蓋流媒體對話、螢幕、視頻、軟體工程和人機協作資源，並正式化觸發、時機、校準、用戶負擔、安全性和政策價值的指標。綜合分析顯示，為什麼僅僅依賴離線分類性能並不能預測部署效益，以及為什麼長期記憶不是主動性的定義條件。可靠的主動服務反而需要經過校準的增量干預價值、可驗證的授權、可恢復的執行和反事實證據。

##### **Can LLMs Extract Architectural Design Decisions from Source Code Commits? - A Preliminary Exploratory Study**
2609.03721v1 by Amey Karan, Rudra Dhar, Mohamed Soliman, Karthik Vaidhyanathan

Context: Architectural Design Decisions (ADDs) capture the rationale behind the structure and evolution of software systems but are rarely documented explicitly, and are often hidden inside source code commits. Recovering them is important for Architectural Knowledge Management (AKM). Problem: Extracting ADDs from commits is challenging due to their implicit and unstructured nature. Large Language Models (LLMs) have shown strong capabilities in understanding code and text, yet their effectiveness for this task remains underexplored. Study: We present a preliminary study using four LLMs (Gemini 3 Pro, DeepSeek R1, Kimi K2, Qwen3) with zeroshot and fewshot prompting on 30 developer-written ADDs from open-source projects. We score outputs with ROUGE-L, BLEU, METEOR, and BERTScore, and one author manually reviews the Gemini outputs. Results: All models reach a BERT-F1 above 0.81, and fewshot prompting improves alignment (Gemini BERT-F1: 0.828 to 0.847). However, the generated ADDs are often too long, implementation-focused, and miss the rationale behind the decision. This highlights opportunities for architecture-aware LLM systems and automated AKM.

摘要：背景：架構設計決策（ADDs）捕捉了軟體系統結構和演變背後的理由，但通常未被明確記錄，並且經常隱藏在源代碼提交中。恢復這些決策對於架構知識管理（AKM）至關重要。問題：由於其隱含和非結構化的特性，從提交中提取ADDs是具有挑戰性的。大型語言模型（LLMs）在理解代碼和文本方面顯示出強大的能力，但它們在此任務上的有效性仍未被充分探索。研究：我們展示了一項初步研究，使用四個LLMs（Gemini 3 Pro、DeepSeek R1、Kimi K2、Qwen3）對來自開源項目的30個開發者撰寫的ADDs進行零樣本和少樣本提示。我們使用ROUGE-L、BLEU、METEOR和BERTScore對輸出進行評分，並由一位作者手動審查Gemini的輸出。結果：所有模型的BERT-F1均超過0.81，少樣本提示改善了對齊（Gemini BERT-F1：從0.828提高到0.847）。然而，生成的ADDs通常過長，過於專注於實現，並且缺乏決策背後的理由。這突顯了架構感知LLM系統和自動化AKM的機會。

##### **Opening mind by opening architecture: analysis strategies**
2609.03719v1 by Francesco Vitucci, Giuseppe Silvi, Daniele Giuseppe Annese, Francesco Scagliola, Anthony Di Furia

In numerical signal processing for electroacoustic composition, the progressive loss of specific development and research environments caused by the increasing use of digital market tools has favoured the dominance of the closed-architecture audio processor model. This model, while powerful, envisions the possibility of describing output data about its perceived characteristics, but at the cost of ignoring its internal process and interacting systems, which become complex, powerful environments but closed in an inscrutable black box, a loss we must consider. Any digital signal processing technique tells a story. Just as the words of a language incorporate social, historical and technical polysemic layers, a signal processor has its own story of implementation, a gradual technological achievement with its inevitable aesthetic consequences. Through the looking-glass of literature, one can access those environments with renewed awareness by reestablishing a scientific method and an attitude to research. In this specific case, starting from the case study of Manfred Schroeder's historical reverbs, we illustrate the process of building analytical evaluation tools, as well as practical implementation, at the basis of a conscious study path.

摘要：在電子音響作曲的數位信號處理中，隨著數位市場工具使用的增加，特定發展和研究環境的逐漸喪失促進了封閉架構音頻處理器模型的主導地位。這個模型雖然強大，但它預見了描述其感知特徵的輸出數據的可能性，但代價是忽視了其內部過程和互動系統，這些系統變成了複雜而強大的環境，但卻封閉在一個難以理解的黑箱中，這是一種我們必須考慮的損失。任何數位信號處理技術都講述了一個故事。正如語言的單詞包含社會、歷史和技術的多義層次，信號處理器也有其自身的實施故事，這是一個逐步的技術成就，伴隨著不可避免的美學後果。透過文學的透鏡，人們可以以更新的認知進入這些環境，通過重新建立科學方法和研究態度。在這個特定案例中，從曼弗雷德·施羅德的歷史混響案例研究出發，我們說明了建立分析評估工具的過程，以及實際實施，這是有意識的學習路徑的基礎。

##### **What Do CAE Simulation Agents Really Need Beyond a Generic Harness?**
2609.03718v1 by Jiasheng Shi, Tianhan Zhang

Computer-aided engineering (CAE) simulation is among the largest and most demanding areas of engineering, where setting up a solver such as OpenFOAM, FEniCS, or COMSOL takes real expertise. Large language model (LLM) agents promise to turn a natural-language request into a working simulation, and recent CAE agents add simulation-specific machinery: multi-agent decomposition, domain retrieval, and scripted reflection. That machinery suited weak base models; modern harnesses already supply multi-turn reasoning, tool use, and execution feedback. We ask what a CAE simulation agent still needs beyond a generic harness. With information access and repair budget held fixed, a single-agent harness matches or beats multi-agent specialized systems (FoamBench 96.4\% vs.\ 88.2\%).
  Ablations trace this to capabilities the harness already provides: execution-feedback repair lifts FoamBench from 71.8\% with no repair round to 96.4\%, while scripted reflection adds nothing. The one input that still helps is domain knowledge supplied as solver tutorials, our largest measured gain (80.9\% to 96.4\%).

摘要：計算機輔助工程（CAE）模擬是工程領域中最大且需求最為嚴苛的領域之一，設置像是 OpenFOAM、FEniCS 或 COMSOL 這樣的求解器需要真正的專業知識。大型語言模型（LLM）代理承諾將自然語言請求轉化為有效的模擬，而最近的 CAE 代理則增加了模擬特定的機制：多代理分解、領域檢索和腳本反思。這些機制適合弱基礎模型；現代的工具已經提供多回合推理、工具使用和執行反饋。我們詢問 CAE 模擬代理在通用工具之外還需要什麼。在信息訪問和修復預算固定的情況下，單一代理工具的表現與多代理專用系統相當或更好（FoamBench 96.4\% 對 88.2\%）。  
消融實驗將這歸因於工具已經提供的能力：執行反饋修復將 FoamBench 從 71.8\% 提升到 96.4\%，而腳本反思則沒有任何增益。唯一仍然有幫助的輸入是作為求解器教程提供的領域知識，我們測得的最大增益（80.9\% 到 96.4\%）。

##### **Artificial Intelligence for Energy Optimization in Data Centers**
2609.03716v1 by Mohammed Basharath Ullah, Summaiya Unnisa Begum, Mohammed Nadeem Ullah

Data centers are increasingly optimized by artificial intelligence and, at the same time, increasingly loaded by it. The literature treats these as two unrelated problems: control studies model workload as an exogenous arrival process, while sustainability studies model infrastructure as a fixed multiplier. We screen roughly 194 papers retrieved through a documented protocol, code 63 of them, and report what the coding shows. Of 28 primary control-oriented studies, 18 are validated in simulation alone and 5 reach physical hardware or a production facility; none account for water withdrawal, and none account for embodied carbon. Reported savings intervals across four technique families overlap almost completely, which means the field cannot presently rank its own methods. Ten recurring gaps are scored for consequence and tractability, and we set out CLEAR-DC, a framework coupling a control-policy branch to a workload-demand branch through an explicit elasticity term, reads out net rather than direct benefit, and emits a schema-conformant record covering energy, carbon, water, embodied share and validation venue. The framework is an architectural and methodological proposal, not a trained system; the contribution we defend empirically is the corpus analysis and the reporting schema derived from it. Coding sheet, derived statistics and all result artifacts: https://github.com/Kimalice/AI-for-Energy-Optimization-in-Data-Centers-Closing-the-Optimizer-Load-Loop

摘要：數據中心越來越多地受到人工智慧的優化，同時也越來越多地受到其負載影響。文獻將這些視為兩個不相關的問題：控制研究將工作負載建模為外生到達過程，而可持續性研究則將基礎設施建模為固定的乘數。我們篩選了大約194篇通過文檔協議檢索的論文，編碼了其中63篇，並報告編碼結果顯示的內容。在28篇主要的控制導向研究中，18篇僅在模擬中得到驗證，5篇達到了物理硬體或生產設施；沒有一篇考慮水的提取，且沒有一篇考慮嵌入碳。報告的節省區間在四個技術類別中幾乎完全重疊，這意味著該領域目前無法對其自身的方法進行排名。十個重複出現的差距根據影響和可處理性進行評分，我們提出了CLEAR-DC，一個將控制政策分支與工作負載需求分支通過顯式彈性項相結合的框架，讀出的是淨利益而非直接利益，並發出一個符合架構的記錄，涵蓋能源、碳、水、嵌入份額和驗證場所。該框架是一個架構和方法論的提案，而不是一個訓練系統；我們所捍衛的貢獻是語料分析及其衍生的報告架構。編碼表、衍生統計和所有結果文檔：https://github.com/Kimalice/AI-for-Energy-Optimization-in-Data-Centers-Closing-the-Optimizer-Load-Loop

##### **Counterfactual Routing Using Integer Programming with Constraint Generation**
2609.03707v1 by Daniël Vos, Sterre Lutz

We present our submission to the IJCAI 2025 'Counterfactual Routing Competition' (CRC 25). The goal of the competition is to find counterfactual explanations for the shortest path problem. This requires deciding what the minimal changes to a road network would make a route chosen by the user the optimal route. This enables explanations such as "Your suggested route would indeed have been optimal, if road X were not a bicycle path." Our solution models the problem as an integer program, iteratively incorporating constraints until an exact solution is found. In the final evaluation on held-out test instances, our method ranked fourth in solution quality and obtained its solution fastest on every instance, with an average runtime of 9.0 seconds compared to 118.8 seconds for the next-fastest submission.

摘要：我們提交了對於 IJCAI 2025「反事實路由競賽」（CRC 25）的參賽作品。<lb/>這個競賽的目標是為最短路徑問題尋找反事實解釋。<lb/>這需要決定對道路網絡進行哪些最小的改變，才能使用戶選擇的路徑成為最佳路徑。<lb/>這使得可以提供類似「如果道路 X 不是自行車道，您建議的路徑確實會是最佳的。」的解釋。<lb/>我們的解決方案將該問題建模為整數規劃，並迭代地納入約束條件，直到找到精確解。<lb/>在對保留測試實例的最終評估中，我們的方法在解決方案質量上排名第四，並且在每個實例上以最快的速度獲得了解決方案，平均運行時間為 9.0 秒，而下一個最快的提交則為 118.8 秒。

##### **Synthetic Semantic Supervision for Contrastive Code Representation Learning in Small Transformers: An Empirical Study**
2609.03702v1 by Kenneth Paulsen, Florian Tambon, Mike Papadakis, Shin Yoo

General-purpose code embeddings power tools for code search, classification, and retrieval. Compact transformer encoders for code typically rely on either human-written docstrings (labor-intensive and inconsistent) or mined structural signals such as execution traces (setting-specific and costly to collect). We empirically study an alternative: contrastive pretraining of small encoders with synthetically generated natural-language descriptions emphasizing code functionality and intent, paired with code in a dual-encoder framework at training and discarded at inference. We benchmark this approach against pretraining-based baselines, generalist LLMs, and embedding-specific models on eight retrieval, classification, and generation tasks across C, C++, and Java. Synthetic semantic supervision yields statistically significant gains over pretraining baselines of the same inference-time size on five of eight tasks, with parity on two more; once fine-tuned, it matches or exceeds zero-shot models two orders of magnitude larger on classification, and it stays on par with execution-aware supervision at matched pretraining data, suggesting a scalable, effective alternative to existing code-representation paradigms.

摘要：通用的代碼嵌入技術為代碼搜索、分類和檢索工具提供支持。針對代碼的緊湊型Transformer編碼器通常依賴於人類編寫的文檔字符串（勞動密集且不一致）或挖掘的結構信號，如執行痕跡（特定於設置且收集成本高）。我們實證研究了一種替代方案：對小型編碼器進行對比預訓練，使用合成生成的自然語言描述，強調代碼功能和意圖，並在訓練中與代碼配對，推理時則丟棄。 我們將這種方法與基於預訓練的基準、通用 LLM 和嵌入特定模型在 C、C++ 和 Java 的八個檢索、分類和生成任務上進行基準測試。合成語義監督在八個任務中的五個上相較於相同推理時間大小的預訓練基準產生了統計上顯著的增益，另外兩個任務則持平；一旦進行微調，它在分類上達到或超過了零樣本模型的兩個數量級，並且在匹配的預訓練數據下與執行感知監督保持一致，這表明這是一種可擴展且有效的替代現有代碼表示範式的方案。

##### **Symmetries and Causality: Causal Effect Identification Beyond IID Data**
2609.03697v1 by Martin Rabel, Jakob Runge

In the natural sciences, symmetries and cause-effect relationships are ubiquitous. Yet for complex machine-learning tasks, like world-modeling in reinforcement learning, they appear difficult to harness. We propose a formal description of statistical systems based on symmetries in data leaving causal mechanisms invariant. The result is an abstract, simple and general mathematical language for causal reasoning. This paper provides formal descriptions of models and queries, setting up this language, and the formal infrastructure and strategies for their mathematically rigorous identification from data within this formalism. This approach reproduces and matches standard theoretical results on IID data and transport of experimental and non-experimental data. But its main purpose is to unify and substantially extend the scope of causal reasoning, in going beyond IID data and in approaching complex causal queries not captured by do- or soft-interventions. This new perspective on causally relevant aspects of data-modeling additionally sheds new light on well-known structures like c-components or hedges but also includes aspects of missing data and is inherently well-suited for the description of transfer and robustness properties.

摘要：在自然科學中，對稱性和因果關係無處不在。<lb/>然而，對於複雜的機器學習任務，例如強化學習中的世界建模，這些特性似乎難以利用。<lb/>我們提出了一種基於數據對稱性的統計系統的正式描述，保持因果機制的不變性。<lb/>其結果是一種抽象、簡單且通用的數學語言，用於因果推理。<lb/>本文提供了模型和查詢的正式描述，建立了這種語言，以及從數據中以數學嚴謹的方式識別它們的正式基礎設施和策略。<lb/>這種方法重現並匹配了有關IID數據和實驗及非實驗數據傳輸的標準理論結果。<lb/>但其主要目的是統一並大幅擴展因果推理的範疇，超越IID數據，並接近那些不被do-或soft-interventions捕捉的複雜因果查詢。<lb/>這種對數據建模中因果相關方面的新視角，還為眾所周知的結構如c-components或hedges提供了新的見解，同時也包括缺失數據的方面，並且本質上非常適合描述轉移和穩健性特性。

##### **A Circuit for Plural Reference: How LLMs Represent and Retrieve Singular and Plural Entities**
2609.03687v1 by Anh Danh, Rick Nouwen, Massimo Poesio

Coreference resolution is an important task in contextual reasoning. In this paper, we investigate the mechanism for representing and retrieving singular and plural entities for plural reference. We use a combination of mechanistic interpretability and attention pattern analysis to study the process in which LLMs predict a pronoun to refer back to previously mentioned entities. Using a range of causal intervention techniques, we find a set of attention heads that are responsible for (1) representing coreference information in the input, (2) identifying entities that form a plural reference, (3) transferring the information to the component that is responsible for selecting the antecedents and predicting the pronoun. We also find that LLMs align with humans in preference for plural pronoun. Specifically, entities in a plural construction are more likely to be referred to as a plural entity if they are ontologically similar and are linked by the conjunction "and".

摘要：核心指代解析是上下文推理中的一項重要任務。<lb/>在本文中，我們研究了用於表示和檢索單數和複數實體以進行複數參考的機制。<lb/>我們使用機械可解釋性和注意力模式分析的結合來研究大型語言模型（LLMs）預測代詞以回指先前提到的實體的過程。<lb/>通過一系列因果干預技術，我們找到了一組負責以下功能的注意力頭：(1) 在輸入中表示指代信息，(2) 識別形成複數參考的實體，(3) 將信息轉移到負責選擇先行詞和預測代詞的組件。<lb/>我們還發現，LLMs 在對複數代詞的偏好上與人類一致。<lb/>具體而言，如果實體在本體上相似並且通過連詞 "and" 連接，則在複數結構中更可能被稱為複數實體。

##### **Understanding Autonomous Driving Datasets by Describing Differences between Image Subsets in Natural Language**
2609.03677v1 by Julian Truetsch, Felix Hauser, Christoph Stiller, Frank Bieder

Understanding the composition of large-scale autonomous driving datasets is essential for safety, robustness, and reliable operation across domains. For example, domain shift between locations could lead to the operating environment being misaligned with the training data, resulting in potentially dangerous performance degradation. Yet, existing data analysis pipelines largely rely on metadata, predefined labels, or manual inspection, which provide limited semantic insight or do not scale. This paper studies set difference captioning: given two subsets of images, the goal is to produce a natural-language hypothesis describing differences between the target and reference set. Building on a two-stage formulation, we adapt the method to autonomous driving by focusing on object-centric patches derived from object detection, which simplifies aggregation and enables attribution of differences to specific object instances or categories. To evaluate this setting in-domain, we introduce a new benchmark, AD-Diff Bench. Low-concentration experiments assess the suitability of set-difference-captioning approaches to sparse, real-world differences. We restrict our experiments to open-weight models to support reproducibility and ease of deployment. The proposed benchmark and analysis provide a step towards practical, human-interpretable dataset introspection for autonomous driving datasets. Our implementation and benchmark dataset are available at https://github.com/KIT-MRT/AD-Diff

摘要：理解大規模自動駕駛數據集的組成對於安全性、穩健性和跨領域的可靠運作至關重要。<lb/>例如，地點之間的領域轉移可能導致操作環境與訓練數據不匹配，從而導致潛在的危險性能下降。<lb/>然而，現有的數據分析流程主要依賴於元數據、預定義標籤或人工檢查，這些方法提供的語義洞察有限或無法擴展。<lb/>本文研究集合差異標註：給定兩個圖像子集，目標是生成一個自然語言假設，描述目標集和參考集之間的差異。<lb/>基於兩階段的公式，我們通過專注於從物體檢測中衍生的物體中心補丁來調整該方法以適應自動駕駛，這簡化了聚合並使得將差異歸因於特定物體實例或類別成為可能。<lb/>為了在該領域內評估這一設置，我們引入了一個新的基準，AD-Diff Bench。<lb/>低濃度實驗評估集合差異標註方法對稀疏、現實世界差異的適用性。<lb/>我們將實驗限制在開放權重模型上，以支持可重複性和易於部署。<lb/>所提出的基準和分析為自動駕駛數據集的實用、人類可解釋的數據集內省邁出了一步。<lb/>我們的實現和基準數據集可在 https://github.com/KIT-MRT/AD-Diff 獲得。

##### **Out-of-Distribution Generalisation with Sequence Models in Offline Multi-Agent Reinforcement Learning**
2609.03667v1 by Oussama Hidaoui, Omer Ebead, Ulrich Armel Mbou Sob, Siddarth Singh, Juan Claude Formanek, Felix Chalumeau, Omayma Mahjoub, Sasha Abramowitz, Ruan John de Kock, Wiem Khlifi, Louay Ben Nessir, Simon Verster Du Toit, Daniel Rajaonarivonivelomanantsoa, Asim Awad Osman, Arnol Manuel Fokam, Refiloe Shabe, Arnu Pretorius

Generalising to unseen tasks remains a fundamental challenge in offline multi-agent reinforcement learning (MARL). In this work, we present a principled analysis of zero-shot task generalisation in the offline setting and conduct an extensive empirical investigation into the scaling behaviour governing task diversity, dataset size, and network capacity. To facilitate this study, we extend offline sequence modelling architectures to handle multi-task observation and action spaces alongside variable agent counts across tasks. Our primary finding is that scaling task diversity---rather than sheer dataset size is the dominant factor in achieving robust zero-shot transfer. Through large-scale experiments across four challenging environments (Connector, RWARE, SMAX, and LBF), we demonstrate that our multi-task approach achieves a mean improvement of 3.2x on held-out test tasks compared to single-task models and consistently outperforms strong behaviour cloning baselines. These results suggest that the development of generalisable MARL agents should prioritise the diversity of the training distribution with varying numbers of agents, providing a roadmap for scaling offline MARL effectively.

摘要：一般化到未見任務仍然是離線多智能體強化學習（MARL）中的一個基本挑戰。在這項工作中，我們對離線環境中的零樣本任務一般化進行了原則性的分析，並對影響任務多樣性、數據集大小和網絡容量的縮放行為進行了廣泛的實證研究。為了促進這項研究，我們擴展了離線序列建模架構，以處理多任務觀察和行動空間，以及跨任務的可變智能體數量。我們的主要發現是，任務多樣性的縮放——而不是單純的數據集大小，是實現穩健零樣本轉移的主導因素。通過在四個具有挑戰性的環境（Connector、RWARE、SMAX 和 LBF）中進行大規模實驗，我們證明我們的多任務方法在保留的測試任務上相比於單任務模型實現了平均 3.2 倍的改善，並且始終優於強大的行為克隆基準。這些結果表明，開發可一般化的 MARL 智能體應優先考慮訓練分佈的多樣性，並且智能體數量不同，為有效擴展離線 MARL 提供了一個路線圖。

##### **Cross-Dataset Transfer and Reliability of Explainable Artificial Intelligence for RhythmFormer Remote Photoplethysmography**
2609.03663v1 by Louis Chen, Torbjörn E. M. Nordling

Background. Remote photoplethysmography estimates the cardiovascular pulse from facial video, and its explanations have rested on inspecting heatmaps rather than on quantitative evidence about where a model reads it. We quantified the explanations and asked whether such explanations transfer between datasets and track model performance. Method. We trained eight condition-specific RhythmFormer models on NCKU-rPPG, recorded under three illumination levels, speaking, rotation, and cycling, estimated one heart rate per 5.12-second clip, and set them beside a UBFC-rPPG reproduction. Raw attention, rollout, attention flow, and Beyond Intuition were assessed by skin coverage and the Salience-guided Faithfulness Coefficient (SaCo). Results. Beyond Intuition ranked highest on both datasets, at median coverage 0.789 and SaCo 0.837 on Static level 3 against 0.826 and 0.917 on UBFC-rPPG; lower ranks differed. Within one participant of one condition, neither measure was related to a clip's heart-rate error, waveform correlation, or signal-to-noise ratio on either dataset: 186 of the 252 coefficients fell below $|ρ|=0.10$ and 28 reached $p<0.05$ against the 13 expected by chance. Across the eight scenarios only Beyond Intuition's coverage followed the three performance measures, at $ρ=-0.43$, $+0.57$, and $+0.43$, while the attention-only methods' SaCo ran opposite to each. It failed at 40 lux alone, its median coverage falling to 0.180 and its median SaCo to $-0.178$, whereas motion degraded the estimates far more without such a drop. Conclusions. Skin coverage and SaCo carry information complementary to the performance measures rather than a proxy for them: attributing to the skin does not guarantee an accurate estimate. What an attribution reveals about a condition is where the model looks rather than how faithfully its map is ordered.

摘要：背景。遠程光電容積描記法從面部視頻估算心血管脈搏，其解釋依賴於檢查熱圖，而非關於模型讀取位置的定量證據。我們量化了這些解釋，並詢問這些解釋是否能在數據集之間轉移並追踪模型性能。方法。我們在NCKU-rPPG上訓練了八個特定條件的RhythmFormer模型，該模型在三個照明水平下錄製，包括說話、旋轉和騎自行車，估算每個5.12秒的片段一個心率，並將其與UBFC-rPPG的重現進行比較。通過皮膚覆蓋率和以顯著性引導的真實性係數（SaCo）評估原始注意力、回放、注意力流和超越直覺的表現。結果。超越直覺在兩個數據集上的排名最高，在靜態水平3的中位數覆蓋率為0.789，SaCo為0.837，而在UBFC-rPPG上分別為0.826和0.917；較低的排名有所不同。在一名參與者的單一條件下，無論是何種測量都與片段的心率誤差、波形相關性或信噪比無關：252個係數中有186個低於$|ρ|=0.10$，28個達到$p&lt;0.05$，而預期的僅有13個。在八種情境中，只有超越直覺的覆蓋率跟隨了三個性能指標，分別為$ρ=-0.43$、$+0.57$和$+0.43$，而僅注意力方法的SaCo則與之相反。它在40 lux的情況下失敗，其中位數覆蓋率降至0.180，中位數SaCo降至$-0.178$，而運動則在沒有這種下降的情況下大幅降低了估算。結論。皮膚覆蓋率和SaCo攜帶的信息是對性能指標的補充，而非其代理：歸因於皮膚並不保證準確的估算。歸因所揭示的條件是模型的觀察位置，而非其地圖的秩序忠實度。

##### **Local Updates, Global Learning (LUGL): Playing Games with non-incremental Learners**
2609.03660v1 by David Milec, Spyridon Samothrakis, Michael Fairbank, Dennis J. N. J. Soemers

The dominance of Neural Networks (NNs) in RL is partially due to their incremental learning capability, which naturally suits the online, non-stationary nature of self-play training. However, gradient-boosted trees like LightGBM are widely recognised as the state of the art for tabular data in supervised learning, often outperforming NNs in accuracy and efficiency. Game states are inherently tabular---discrete actions, categorical card identities, structured board positions---which makes them an ideal candidate for tree-based methods. We introduce LUGL (Local Updates, Global Learning), a framework that decouples data collection from model fitting, enabling non-incremental learners such as GBTs to operate in RL settings where they would otherwise fail due to distributional shift. LUGL alternates between a local updates phase, where the agent plays self-play games and accumulates tabular updates (Q-values, V-values, policies, or regret values) in a finite table, and a global learning phase, where the table is used to train a function approximator that generalises to unseen states before the table is reset. We test our approach in four standard perfect-information games (Tic-tac-toe, Connect-4, Othello, and Hex) and five imperfect-information games (Kuhn's poker, Leduc Hold'em, Liar's Dice, Goofspiel, and Flop5 Hold'em), and show that our results are competitive with or superior to DQN and DeepCFR. Our experiments demonstrate that the community's strong bias towards NNs in game-playing may be unwarranted, since LightGBM-based agents achieve competitive or superior performance across all tested benchmarks.

摘要：神經網絡（NNs）在強化學習（RL）中的主導地位部分源於其增量學習能力，這自然適合自我對弈訓練的在線、非平穩特性。然後，像 LightGBM 這樣的梯度提升樹被廣泛認為是監督學習中表格數據的最先進技術，通常在準確性和效率上超越 NNs。遊戲狀態本質上是表格型的——離散行動、分類卡片身份、結構化棋盤位置——這使得它們成為基於樹的方法的理想候選者。我們提出 LUGL（局部更新，全球學習），這是一個將數據收集與模型擬合解耦的框架，使得非增量學習者如 GBTs 能夠在由於分佈變化而失敗的 RL 環境中運行。LUGL 在局部更新階段和全球學習階段之間交替進行，在局部更新階段，代理進行自我對弈遊戲並在有限的表格中累積表格更新（Q 值、V 值、策略或後悔值），而在全球學習階段，則使用該表格訓練一個函數逼近器，以便在表格重置之前能夠對未見狀態進行泛化。我們在四個標準的完美信息遊戲（井字遊戲、連線四、黑白棋和六角棋）和五個不完美信息遊戲（庫恩撲克、勒杜克德州撲克、騙子骰子、搞笑遊戲和 Flop5 德州撲克）中測試我們的方法，並顯示我們的結果在競爭上與 DQN 和 DeepCFR 相當或更優。我們的實驗表明，社群對於 NNs 在遊戲中的強烈偏見可能是不必要的，因為基於 LightGBM 的代理在所有測試基準中都達到了競爭或更優的表現。

##### **Enhancing Financial Question Answering: A Novel Benchmark Dataset of Banks' financial statements**
2609.03654v1 by Arianna Miola, Bruno Spaccavento, Lorenzo Silotto, Marco Bianchetti, Luca Cagliero

The comparative analysis of banks' financial statements poses significant challenges for automated question answering systems due to their complexity, substantial length, technical language, and inhomogeneity of both textual and numerical content across different jurisdictions and institutions. We introduce FinRAG-QA, a novel benchmark dataset for financial question answering, which comprises 999 practitioner-curated questions on 10 standardised indicators, grounded in 209 annual and Pillar 3 reports from 24 major European and U.S. banks spanning 2019-2023. Unlike prior financial QA benchmarks, which centre on U.S. filings and single-institution analysis, FinRAG-QA targets cross-institutional retrieval over documents averaging 198k words, longer than any existing financial QA resource. On this benchmark we evaluate a multi-stage RAG pipeline and isolate the contribution of each component. Contextual chunk enrichment combined with a retrieval-optimised embedding model raises NDCG@10 from 0.322 to 0.710; conditional on the ground truth being retrieved, a reasoning-optimised generator raises answer accuracy from 44.6% to 79.0% (+34.4 percentage points), at roughly 20x the generation latency. We further show that cross-encoder reranking degrades retrieval when the first-stage ranking is already strong, and that a single top-ranked chunk outperforms larger contexts at generation time. Experiments were run in late 2024-early 2025 with the models available at that time.

摘要：銀行財務報表的比較分析對於自動問答系統提出了重大挑戰，因為其複雜性、相當長的篇幅、技術性語言，以及不同法域和機構之間文本和數字內容的不均勻性。我們介紹了FinRAG-QA，一個新的金融問答基準數據集，包含999個實務者策劃的問題，涵蓋10個標準化指標，基於來自24家主要歐洲和美國銀行的209份年度報告和第三支柱報告，時間範圍為2019年至2023年。與之前的金融問答基準不同，FinRAG-QA針對跨機構檢索，文檔平均長度為198,000字，超過任何現有的金融問答資源。在這個基準上，我們評估了一個多階段的RAG管道，並隔離每個組件的貢獻。上下文區塊增強結合檢索優化的嵌入模型將NDCG@10從0.322提高到0.710；在檢索到真實答案的條件下，推理優化的生成器將答案準確率從44.6%提高到79.0%（+34.4個百分點），生成延遲約為20倍。我們進一步顯示，當第一階段排名已經很強時，交叉編碼器重新排序會降低檢索效果，而在生成時，單個排名最高的區塊在性能上超過了較大的上下文。實驗於2024年底至2025年初進行，使用當時可用的模型。

##### **The Impact of Synthetic Data Augmentation on Discourse-Pragmatic Function Classification**
2609.03652v1 by Sara Sorahi, Kevin Tang, Reza Kazemian

Synthetic data augmentation has become a common strategy for addressing class imbalance in NLP, but most approaches focus on the quantity and diversity of generated examples rather than their geometric relationship to real training data. We investigate this question in the context of discourse pragmatic function classification, a task where data sparsity is a structural feature rather than a collection artefact. Using 410 manually annotated instances of the English word look drawn from the British National Corpus, spanning four functions: Attention Signal, Directive, Discourse Marker, and Interjection. We generate synthetic training examples with Llama 3.1 and partition them by their cosine distance from real training data in RoBERTa embedding space. We compare six training conditions that differ in the placement of synthetic examples relative to the empirical decision boundary, while holding augmentation quantity constant across conditions. All augmented conditions improve macro F and accuracy over the real only baseline, but core proximal examples (NEAR) yield the largest gains in macro F (0.113), while a distance balanced mix achieves the highest accuracy (0.748). No condition improves AUC, indicating that augmentation shifts the decision boundary rather than improving the model's underlying probability estimates. These findings suggest that where synthetic examples land in representation space matters as much as how many are generated, with implications for low resource pragmatic classification more broadly.

摘要：合成數據增強已成為解決自然語言處理中類別不平衡的常見策略，但大多數方法專注於生成示例的數量和多樣性，而不是它們與真實訓練數據的幾何關係。我們在話語語用功能分類的背景下研究這個問題，這是一個數據稀疏性是結構特徵而非收集產物的任務。使用來自英國國家語料庫的410個手動標註的英語單詞“look”實例，涵蓋四個功能：注意信號、指令、話語標記和感嘆詞。我們使用Llama 3.1生成合成訓練示例，並根據它們在RoBERTa嵌入空間中與真實訓練數據的餘弦距離進行劃分。我們比較六種訓練條件，這些條件在合成示例相對於經驗決策邊界的放置上有所不同，同時在各條件下保持增強數量不變。所有增強條件在宏F和準確率上均優於僅使用真實數據的基線，但核心近似示例（NEAR）在宏F上獲得了最大的增益（0.113），而距離平衡的混合則達到了最高準確率（0.748）。沒有任何條件改善AUC，這表明增強改變了決策邊界，而不是改善模型的基本概率估計。這些發現表明，合成示例在表示空間中的位置與生成的數量一樣重要，對於低資源的語用分類有更廣泛的影響。

##### **Analysis of Prompt Engineering for Drug Toxicity Prediction**
2609.03635v1 by Mia MacGregor, Aakash Welgamage Don, Mark Bartlett

Clinical trials in the UK can cost up to £1.3 million, with approximately 90% drug failure rate. Toxicity is a major contributing factor in drug failure. Testing is time and cost intensive. In recent years, the use of artificial intelligence has been increasingly explored to aid in the prediction of drug toxicity, with extensive use of large language models (LLMs). However, LLMs can show considerable variation when minor changes are made to prompts, which raises concerns about their sensitivity to prompt engineering. Prompt engineering is used to optimise a prompt given to an LLM to generate the desired output. This paper proposes a method to analyse prompt engineering for drug toxicity prediction. The aim of the paper is to investigate the importance of prompt phrasing for drug toxicity prediction. LLMs were prompted to identify chemical properties of significance when predicting drug toxicity. Prompts were constructed to investigate; job role, prompt structuring, and rule interpretation. LLMs were then used to generate datasets, using the identified features from initial prompting, which were then passed to machine learning algorithms. The experiments show that the natural variance which occurs in LLMs outweighs any fine-tuning of prompts. There were, however, substantial improvements in model performance when using chemoinformatic code to extract features instead of using LLM-generated values. The proposed analysis methodology is applicable to a wide range of prompt types across different areas of bioinformatics.

摘要：臨床試驗在英國的成本可達130萬英鎊，藥物失敗率約為90%。毒性是藥物失敗的主要因素。測試耗時且成本高昂。近年來，人工智慧的使用越來越受到關注，以協助預測藥物毒性，並廣泛使用大型語言模型（LLMs）。然而，當對提示進行微小更改時，LLMs可能會顯示出相當大的變異，這引發了對其對提示工程敏感性的擔憂。提示工程用於優化給LLM的提示，以生成所需的輸出。本文提出了一種分析藥物毒性預測的提示工程的方法。本文的目的是調查提示措辭對藥物毒性預測的重要性。LLMs被提示識別在預測藥物毒性時的重要化學特性。提示被構建以調查；職位角色、提示結構和規則解釋。然後使用LLMs生成數據集，使用從初始提示中識別的特徵，這些數據集隨後被傳遞給機器學習算法。實驗顯示，LLMs中自然變異的影響超過了任何提示的微調。然而，當使用化學信息學代碼提取特徵而不是使用LLM生成的值時，模型性能有了顯著改善。所提出的分析方法適用於不同生物信息學領域的各種提示類型。

##### **</think> Doesn't Stop Reasoning: Analysis of Spurious CoT Termination**
2609.03633v1 by Seunghee Koh, Sungjae Choi, Minchan Kwon, Sunghyun Baek, Junmo Kim

Chain-of-thought (CoT) reasoning improves large reasoning models (LRMs) on complex tasks but often produces long, redundant traces. Recent training-free early-exit methods shorten these traces by choosing an intermediate point to stop reasoning. We study one such strategy that injects an end-of-think token (EoT, </think>) at this point to trigger the reasoning-to-answering transition, and find that the injected EoT does not always induce a clean answering phase. Answering-phase generation can continue before the model regenerates another EoT, with the span preceding this regenerated EoT scaling with the reasoning tokens saved by early exit and exhibiting continued reasoning behavior. We call this spurious CoT termination, where reasoning-like generation continues into the answering phase. We hypothesize that insufficient attention to the injected EoT contributes to spurious CoT termination and probe this hypothesis with Exit-token Attention Biasing (EAB). Across four LRMs, five benchmarks, and two early-exit methods, increasing attention to the injected EoT reduces spurious CoT termination and answering-phase length. These results reveal a limitation of controlling LRMs by externally matching their explicit think-block format. Inserting the EoT token conforms to this format but does not by itself guarantee the intended reasoning-to-answering transition. Our code is available at https://github.com/Seunghee-Koh/Spurious-CoT-Termination.

摘要：Chain-of-thought (CoT) 推理在複雜任務上改善了大型推理模型 (LRMs)，但通常會產生冗長且重複的推理過程。<lb/>最近的無訓練早期退出方法通過選擇一個中間點來停止推理，縮短了這些過程。<lb/>我們研究了一種在此點注入思考結束標記 (EoT, &lt;/think&gt;) 的策略，以觸發推理到回答的轉變，並發現注入的 EoT 並不總是引發乾淨的回答階段。<lb/>在模型重新生成另一個 EoT 之前，回答階段的生成可以繼續，並且在這個重新生成的 EoT 之前的跨度與通過早期退出節省的推理標記成比例，並表現出持續的推理行為。<lb/>我們稱這種現象為虛假的 CoT 終止，其中類似推理的生成持續進入回答階段。<lb/>我們假設對注入 EoT 的注意不足會導致虛假的 CoT 終止，並通過退出標記注意偏向 (EAB) 來探討這一假設。<lb/>在四個 LRM、五個基準和兩種早期退出方法中，增加對注入 EoT 的注意減少了虛假的 CoT 終止和回答階段的長度。<lb/>這些結果揭示了通過外部匹配其明確思考區塊格式來控制 LRM 的限制。<lb/>插入 EoT 標記符合這一格式，但本身並不保證預期的推理到回答的轉變。<lb/>我們的代碼可在 https://github.com/Seunghee-Koh/Spurious-CoT-Termination 獲得。

##### **EraseSAE: Surgical Concept Erasure in Text-to-Video Diffusion Models via Sparse Autoencoders**
2609.03629v1 by Xinghao Wang, Dong Li, Wei Yu, Yingwei Pan, Tao Gong, Qi Chu, Nenghai Yu, Ting Yao

Recent advances in text-to-video (T2V) diffusion models have demonstrated remarkable generative capabilities, yet their reliance on loosely curated training data raises pressing safety and copyright concerns. Concept erasure offers a principled remedy by removing unwanted semantics from pretrained models while preserving remaining concepts. However, existing approaches typically operate at a coarse granularity misaligned with the fine-grained, distributed nature of concept representations, leading to incomplete removal or degraded generation quality. We argue that surgical erasure fundamentally requires intervention at the level of monosemantic features, where each unit encodes a single interpretable concept. To this end, we propose EraseSAE, a novel framework that leverages sparse autoencoders to achieve surgical concept erasure in DiT-based T2V diffusion models via a principled decompose-attribute-erase pipeline. We first introduce the Partitioned Convolutional Sparse Autoencoder, which decomposes dense spatiotemporal activations into disentangled, interpretable sparse features while preserving spatiotemporal coherence. A contrastive attribution mechanism then contrasts activations from paired prompts to isolate concept-specific feature kernels. At inference, timestep-resolved spatiotemporal masks derived from the identified kernels confine erasure to regions where the target concept is active, leaving unrelated content intact. Extensive experiments across diverse diffusion models and concept erasure tasks demonstrate that EraseSAE achieves precise and robust concept removal with minimal quality degradation, substantially outperforming state-of-the-art methods. The code is available at https://github.com/HiDream-ai/EraseSAE.

摘要：最近在文本到視頻（T2V）擴散模型的進展顯示了顯著的生成能力，但它們對於鬆散策劃的訓練數據的依賴引發了迫切的安全和版權問題。概念抹除提供了一種原則性的解決方案，通過從預訓練模型中移除不必要的語義，同時保留剩餘的概念。然而，現有的方法通常在粗糙的粒度下運作，與概念表示的細粒度、分佈式特性不一致，導致不完全的移除或生成質量的下降。我們認為，外科抹除根本上需要在單一語義特徵的層面進行干預，每個單元編碼一個可解釋的概念。為此，我們提出了EraseSAE，一個新穎的框架，利用稀疏自編碼器通過一個原則性的分解-屬性-抹除管道，在基於DiT的T2V擴散模型中實現外科概念抹除。我們首先介紹了分區卷積稀疏自編碼器，它將密集的時空激活分解為解耦的、可解釋的稀疏特徵，同時保留時空一致性。然後，對比歸因機制通過對配對提示的激活進行對比，以隔離特定於概念的特徵核。在推理時，從識別的核中派生的時間步解析時空掩碼將抹除限制在目標概念活躍的區域，保留無關內容。對各種擴散模型和概念抹除任務的廣泛實驗表明，EraseSAE以最小的質量下降實現了精確且穩健的概念移除，顯著超越了最先進的方法。代碼可在 https://github.com/HiDream-ai/EraseSAE 獲得。

